numpy>=1.24
scipy>=1.10
matplotlib>=3.7
h5py>=3.8
# GPU acceleration (optional, install ONE):
#   PyTorch ROCm (AMD): pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
#   PyTorch CUDA (NVIDIA): pip install torch --index-url https://download.pytorch.org/whl/cu124
#   CuPy (NVIDIA): pip install cupy-cuda12x
