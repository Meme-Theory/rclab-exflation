---
name: s114-w4-1-w3-2-landings
description: S114 W4-1 (A_s functional-selection FAIL) + W3-2 (HK-170X-DM mis-attrib INFO) falsifier-inventory landings, SHARPENED at S116 W3 (DM mass graph-ANCHORED, S112 Reading-B WITHDRAWN, 170× re-typed) — the §EVOI.BF inventory-side-only re-tag discipline + the "robust/unchanged ≠ a delta" rule + the status-flip-inside-an-already-swept-window consistency-cross-link rule
metadata:
  type: project
---

S114 Wave-3/Wave-4 falsifier-surface annotations (mine, sole-writer, both landed `sessions/framework/registry/falsifier-master-inventory.md`). Rows live in the registry (AMRI — NOT duplicated here); what's worth keeping is the two judgment calls.

**W4-1** `### Row #12.compute-S114-W4-1-AS-FUNCTIONAL-SELECTION` (line ~2529), `audit_sha256=395f6800…`: A_s functional-SELECTION FAIL ⇒ FUNCTIONAL-PLURALISM PERMANENT. Discharges the `CF-AS-FUNCTIONAL-SELECTION` gate the S113 WS-5 row pre-registered. Magnitude-as-Planck-NUMBER is now a physical d.o.f. like the CC ratio (lizzi-signature, RD scheme-dependent). Structural sub-test `d|β_k̂|²/d(a_0/a_2)=0` EXACT (floor a_0/a_2-invariant); cross-functional spread 1.2590 OOM, `substrate_canonical_selector_exists=False`. Floor POINT (A_s_FW=1.5367e-08) + floor INEQUALITY (`>1`, 3-axis) UNTOUCHED.

**W3-2** `### Row #79.compute-S114-W3-2-HK-170X-DM-MISATTRIB` (line ~2580), `audit_sha256=b6a7727b…`: HK-170X-DM CLOSED as mis-attributed — the 25.5×Δ_BCS "DM shortfall" is the n_s Wall-W9 transplant `m_required/Δ_BCS`, NOT a DM free-streaming requirement (z_tr=6.75e29, 22-OOM margin). Phantom DM-ledger gap removed. σ_SI=1.299e-63 NULL = ROBUST/UNCHANGED.

**W3-2 SHARPENED (S116 W3)** `### Row #79.compute-S116-W3-DISORDER-CLOSURE` (line ~2598), anchor `S116-W3-GOLDSTONE-M2` FAIL `audit_sha256=2959c503…` × the S116-W3-DISORDER-CLOSURE workshop (artifact-existence, no verdict line). The mis-attribution reading is upgraded to the full closure: (i) the disorder→Goldstone-mass route is CLOSED-BY-PROTECTION-MAGNITUDE-EXCLUSION at EVERY D_K projection (Tier-1a intra-(0,0) frac170 ≤ 0.0118; Tier-1b inter-band-via-disorder ≈ 5e-4 analytic, below 1a); (ii) the DM mass is graph-ANCHORED at the clean Leggett 5.5571 M_KK (C11-conditional) — **landau WITHDREW S112 WS-S112-4 Reading B** ("Josephson-graph-unanchored"); (iii) the 170× is RE-TYPED (cross-pillar n_s/Leggett ratio, x_target=30.12, not a missing-mass shortfall), discharge ROUTED to CF-S117-FREESTREAM-AT-ANCHOR. **Current-state pointer (load-bearing for future-me):** the Row #79.audit-S113-WS4 sub-row HEADER still reads "the unanchored Leggett DM mass" — that is RETAINED-for-audit-trail, NOT current. For the DM-mass-anchor state read the LATEST Row #79 sub-row (compute-S116-W3-DISORDER-CLOSURE: graph-ANCHORED), not the S113 WS-4 header.

**Discipline kept (reusable):**

1. **§EVOI.BF re-tag is inventory-side-ONLY.** When a gate re-tags the A_s axis ("open, may converge" → "open, structurally"), I record the re-tag INSIDE the falsifier-inventory row's cross-link/annotation citing the gate's audit_sha256 — I do NOT edit `evoi-framework.md`'s priority TABLE. **Why:** the EVOI table is sagan's; /rclab-plan maintains it. **How to apply:** precedent is the S113 WS-5 row's "§EVOI.BF A_s liability RE-LOCALIZED ... the §EVOI.BF table cell itself is the sagan-owned EVOI surface" language. The team-lead spawn explicitly forbade editing the table. The falsifier-inventory carries the falsifier-side annotation; the table cell is a separate surface. Links [[reference_key-constraints]].

2. **"Robust/unchanged ≠ a delta" for a falsifier the gate didn't touch.** When a gate (W3-2) explicitly does NOT touch a falsifier row (Row #79 σ_SI NULL), I record "robust/unchanged" + the *reason* the gate left it alone (clause-α resolved, but σ_SI never depended on the mass anchor), NOT an invented sharpen/delta on the value. **Why:** the WP §W3-2 §5 said "the σ_SI NULL ... SHARPENS regardless — the gate did NOT change it"; inventing a numeric delta where the source says unchanged is fabrication. **How to apply:** the team-lead's instruction was literally "if the NULL is unchanged, record 'robust/unchanged' rather than inventing a delta." General rule for any routed annotation: if the source gate's verdict-mapping section disclaims touching the falsifier cell, the landing is a status/context annotation, not a value edit. Links [[feedback_shared-wp-writer-unique-anchor]].

3. **FAIL/INFO ⇒ NO canonical_constants pin.** Both gates were FAIL/INFO (no new prediction value), so canonical write-order Step 2 (update_constant) is N/A — only Step 3 (inventory row) fires. A FAIL that pins an *epistemic type* (magnitude = physical d.o.f.) mints no number. (S116 W3 also FAIL/workshop ⇒ same, Step 2 N/A.)

4. **A status-flip that lands INSIDE an already-proven robustness window does NOT weaken the falsifier — record a consistency cross-link, not a re-derivation (S116 W3).** The S113 WS-4 row proved σ_SI NULL is anchor-mass-INDEPENDENT across `M_DM ∈ [M_Leggett, 170·M_Leggett]`. When S116 W3 flipped the mass from unanchored→graph-ANCHORED at `M_Leggett` (5.5571 M_KK, the LOW end of that window), the correct landing is: the anchor lands inside the swept window ⇒ σ_SI NULL preserved (now anchor-PINNED, not weakened). **Why:** a falsifier already certified robust across a window is robust at any point inside it; inventing a new σ-distance for the pinned point would be fabrication (same root as discipline 2). **How to apply:** when a downstream gate pins a previously-swept input, cross-link the prior robustness-window row and state "lands inside ⇒ preserved," don't recompute the falsifier. Separately: a RE-TYPING (170× → cross-pillar ratio) is **ASSERTED-AND-ROUTED, not asserted-closed** — it carries zero evidential weight on the physical question (structure formation) until the discharge compute runs (`CF-S117-FREESTREAM-AT-ANCHOR`); record the route, never an implied closure. Links [[reference_key-constraints]].
