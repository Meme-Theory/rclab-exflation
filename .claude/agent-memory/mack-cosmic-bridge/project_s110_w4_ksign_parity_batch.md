---
name: s110-w4-ksign-parity-batch
description: S110 W4 connes×mack workshop close — dimensional-class transport-degree indexing + κ-sign∧Wodzicki-parity joint foreclosure; 5 sole-writer landings + the durable patterns
metadata:
  type: project
---

S110 W4 iterative workshop (connes-ncg × mack-cosmic-bridge, CONVERGED 2 rounds, 2026-06-21; `sessions/session-110/session-110-connes-mack-workshop.md`). Adjudicated: does the per-observable transport-degree theorem (corpus §23, K=2, dimensionless-only) extend to the DIMENSIONFUL-observable class? Triggered by W4 CO34-legB (LRD-T `2a654897…`) + CF3-H0 (`7bfda02a…`) both name-importing the W3 mint `deg_T=2.0` (`f60cff36…`) across a dimensional-class boundary.

**My 5 sole-writer landings (figures in the registry, AMRI — NOT here):** corpus §23.0(5) dimensional-class directive extension; corpus §26 sign-lock sub-species ENRICH paragraph; falsifier-master-inventory Rows #88.audit-S110-CO34-LRDT-TRANSPORT-PARITY (T) + #81.audit-S110-CF3-H0-RESIDUAL-PARTIAL (H₀); permanent-results-registry §VII.CF STAGE-1-CANDIDATE + its master-index table row (line 168). §7 capstone: NO prose edit (NON-PROMOTION new held-prediction; capstone-hygiene Q2→inventory, Q3=NO).

### Durable PATTERNS worth keeping (the reusable methodology, not the numbers)

1. **Transport degree is DIMENSIONAL-CLASS-INDEXED** (the new general rule, corpus §23.0(5)): `B = (M_KK^{d_A} scale leg) ⊙ (dimensionless morphism)`. `d_A=0` ⇒ trivial scale leg, degree carried by the morphism (the `+2` of α_s/d_s lives HERE — a morphism degree, NOT a unit conversion); `d_A=1` ⇒ the `M_KK^1` scale leg carries the 54.04-dec conversion (`deg=+1`). The W3→W4 bug conflated the morphism-slot degree with the scale-leg-slot degree via a name-only canonical import.

2. **PARITY selection rule** (the deepest output): morphism sector = EVEN degree (`−2(s−s')` Wodzicki ratios, `0` HKR); the only ODD-degree carrier is the sign-locked `M_KK^1` scale leg. ⇒ **every `d_A=odd` substrate observable is unreachable knob-free** — a falsifier-surface CLASS, not two isolated rows. The framework's `d_A=0` transport successes (n_s/α_s/n_T) all sit in the EVEN sector, which is WHY they work.

3. **κ-sign∧Wodzicki-parity = a THEOREM via PASS-AND across two independent axes** (registered STAGE-1-CANDIDATE §VII.CF): my transport-κ sign-lock (the deg=+1 residual is a `+28.17`-dec ASCENT needing `|κ|>1`, sign-inconsistent with `|κ|<1`) ∧ connes's Wodzicki degree-rigidity+parity (every fixed-order ratio `|κ|=10^−108.08` DECAYS; EVEN-parity can't reach ODD `+1`). Two structurally-independent axes foreclosing the SAME open channel = genuine independent confirmation per `joint-theorem-promotion.md`. **κ-sign is a transport-physics fact connes's degree-only NCG analysis did not reach** — the cosmological-bridge value-add.

4. **The mutual-exclusivity SHAPE**: dimensional admissibility (`deg(B)=d_A`) and sign-consistency (`|κ|<1`) are MUTUALLY EXCLUSIVE for an odd-`d_A` band — sharper than "no integer degree lands it." Band-landing eff deg sub-scalar between deg=0 (too hot) and deg=1 (too cold), reachable sign-consistently ONLY at the dimensionally-WRONG deg=0 leg, or dimensionally-admissibly ONLY by a sign-INCONSISTENT ascent.

5. **K-counter discipline reaffirmed (the §23 K=2 hold)**: the §23 K-counter advances on a NEW observable with an **independently factorization-EXTRACTED** degree (r/α_t), NOT on re-determining the degree of an object the theorem already covers via a RULE. T's `+1` was rule-DERIVED from `Q=R·M_KK^m` bookkeeping (no T-channel `w(L_max)·κ(k)` gate exists) ⇒ extraction-distinctness NOT met ⇒ §23 STAYS K=2 as a DIRECTIVE EXTENSION. This is the corpus :1731 controlling text (it retracted exactly a "K=2→K=3" determination-vs-extraction conflation). connes initially read it as the long-awaited K=3→MANDATORY and CONCEDED on the merits. **EXTRACTION-distinctness ≠ determination-distinctness** is the load-bearing K-counter axis. Generalizes the [[discharge-status two-axis split]] discipline. §26 ALSO stays K=1 (transport-κ sign-lock ENRICHES Member C's sign-lock differentia — first instance with cohomology/transport content vs Member C's combinatorial surrogate `R_surr=2f−1`).

6. **Round-figure fidelity (2nd+ instance after Ω_GW + the 53.46-OOM envelope)**: the H₀ fence is Sage-exact `49/800=6.125%` of band floor / band-center shortfall `900/49=18.3673×` (the verdict-file `7500000/408331=18.3675×` is a near-equal FITTED-budget form, 4-sig-fig agreement, NOT the bare shortfall). Pin the Sage-exact rational, tag the round form.

7. **Volovik a₀-orthogonality wall ⇄ my κ-sign∧parity wall = ONE parity-complete wall**: volovik (workingpaper:573) states the EVEN `d_A=0` face ("a dimensionless ratio cannot close a dimensional gap"); the workshop adds the ODD `d_A=+1` face (the scale leg exists but is sign-locked + parity-isolated). `Q=R·M_KK^m` forbids the gap from BOTH parity sides.

**Orchestrator-reserved hand-off (recorded, NOT executed — `.claude/rules/` subagent-edit-denied):** (6a) `cross-pillar-bridge-anatomy.md §"Composite Bridge-Map Dimensional-Class Admissibility"` dimensional-class-indexed sentence; (6b) `regulator-pin-discipline.md §"four-axis orthogonality"` FIFTH axis = `(d_A, deg, parity)` transported-observable mass-dimension/parity pin (the W3→W4 import crossed parity silently because EVEN-ness is shared across the `d_A=0` sector). Verbatim mirror text staged in corpus §23.0(5).

**Forward:** CF-S111-KSIGN-PARITY-STAGE2 (Stage-2 NON-AUTHOR cross-check, verifiers ≠ mack/connes); CF-S111-CO34B-LRDT-TRANSPORT + CF-S111-CF3-H0-RESIDUAL SHARPENED (pin deg a priori, pre-register κ-sign predicate / 6.125% partial). Cross-link [[ws-co-1-compact-object-sterile]] (S110 W1, same compact-object/H₀ surface).
