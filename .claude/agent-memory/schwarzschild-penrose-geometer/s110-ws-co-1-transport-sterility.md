# S110 WS-CO-1 — Compact-object anchor-free-falsifier adjudication (Reading-STERILE)

**Workshop**: mack (Reading-ESCAPE) × schwarzschild-penrose (Reading-STERILE). Source: `sessions/investigation/_rollup-compact-object.md` §4. File: `sessions/session-110/workshops/ws-co-1.md`. Investigation-track (NOT yet a session gate).

## The adjudication
Does the BUILT compact-object sector (interior Lobo-DE gravastar w_core=-0.92 horizonless trapped + exterior a4 +blueshift QNM/+tidal Love at 0 free params + EoS-direction + formation + bulk GL KK-bubble) yield an anchor-free FALSIFIER? Every detectable magnitude is OOM-suppressed (exterior QNM/tidal ~73 OOM sub-detectable; LRD T 25 OOM high; M_max ~12x soft). ESCAPE = a dimensionless RATIO cancels M_KK. STERILE = every ratio re-entangles M_KK or is pure-number.

## The decisive geometric fact (Reading-STERILE grounding)
**S93-W7-1** (`S93-W7-1-ALPHA-S-W-KAPPA-FACTORIZATION-DEG-TRANSPORT-BZ-PIVOT`, PASS): `deg(T_{BZ->pivot}) = +2 NON-SCALAR`, `factorization_holds=False`, `alpha_s_substrate = -0.08587279` vs `alpha_s_pivot = 0.0`. The SAME dimensionless ratio (alpha_s = n_s^2 - 1, manifestly M_KK-free) has TWO values 54.04 decades apart because the BZ->lab transport is degree-2 non-scalar. Canonical identity (`Phononic-C-Causality.md`): `O^pivot = O^substrate IFF deg(T) is T2-VACUOUS (scalar)`.

## STERILE structural statement (the pincer)
A substrate-IS ratio R = O_1/O_2 is an anchor-free falsifier only if BOTH:
- (A) Transport-triviality: deg(T_{BZ->pivot}) on R is T2-VACUOUS (scalar) so R^pivot = R^substrate.
- (B) Discriminating content: R^substrate differs from the Kerr/GR value (not a pure number).

**The pincer**: a non-scalar transport preserves R only if O_1, O_2 read at the SAME substrate scale. But content-bearing ratios (overtone ladder omega_n/omega_0; strong-field-vs-orbital R/M; tidal-vs-compactness Lambda/C^k) are DIFFERENT-scale BY CONSTRUCTION -> transport corrupts (fails A). Same-scale ratios are trivially 1 (fails B) or pure spectral-moment numbers (a4/a2=0.4865) with no dimensionful-detector form (fails observability bridge -> INFO).

## Candidate verdicts (5, exhaustive)
1. Echo spacing omega_n/omega_0: fails (A) [n=426 modes span [0.0345,19.44] M_KK, ~563x, sample T across ~3 decades -> non-scalar acts differently]; (B) fragile [uniform ladder generic to any horizonless cavity, Cardoso-Pani CURV-15].
2. a4 QNM-fraction delta_omega/omega = (a4/a2)(g1/g0): a4/a2=0.4865 PASSES (A) as T2 ratio, but observable form delta_omega = (delta_omega/omega)*omega_GR re-enters M_KK (omega_GR is M_KK-set) -> INFO. The ONE door left open: single-scale ratio-of-ratios (delta_omega/omega)_{l=2}/(delta_omega/omega)_{l=3}.
3. Tidal/compactness Lambda/C^k: fails (A) [Lambda~(l_KK/r_obs)^2 vs lab length; two independently-transported legs].
4. R/M (the inv-1 sagan headline): fails (A) deepest [R via QNM/tidal, M via orbital — DIFFERENT transport maps T_R, T_M; need T_R=T_M scalar].
5. Area-law slope (inv-7 W3-1): fails (B) [area law is consistency not discriminator; shared with Kerr].

## Verdict brought to R1
Reading-STERILE: sector sign-built but falsifier-sterile. CV-9 is the FIFTH confirmation of the M_KK-keystone master diagnosis (`_cross-investigation-synthesis §5 headline 1`: sign-PASS/magnitude-FAIL IS rank-1 M_KK §VII.BS via O=w*Ohat), NOT the escape from it. STERILE = honest constraint-map geometry (consistent with retired-GW walls=0 EXACT S96, S106 §5.4). CF-CO-2 should be a genuine gate with a "does-not-fire" branch keyed on deg(T)!=0 for the chosen ratio. ESCAPE triggers CF-CO-2; STERILE closes-not-run.

## Door left open for ESCAPE
The ONLY surviving ESCAPE route: a same-scale ratio-of-ratios with content (Candidate-b rescue). SP2 Q2 asks mack to name it. If no same-scale-with-content object exists, ESCAPE has no member.
