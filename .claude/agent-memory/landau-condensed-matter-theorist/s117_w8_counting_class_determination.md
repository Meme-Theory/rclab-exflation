---
name: s117-w8-counting-class-determination
description: S117-W8 adjudication — the §VII.AJ.STATE-PROJ R_summand counting class is intensive-FORCED (verdict A); the slot's defining functional ρ_BCS(P·H) IS the discriminator's intensive mass/position-class form
metadata:
  type: project
---

# S117 W8 — R_summand counting-class determination (verdict A: intensive substrate-FORCED)

Gate CF-S117-STATEPROJ-INTER-SUMMAND (s117 verdict L152, PASS) registered R_summand=(a_H−b_M3)/(a_H+b_M3)=+0.955038 (L12), convention RATIO-NORMALIZED-TRACE-MEAN (intensive). The DISCHARGE of §VII.AJ.STATE-PROJ to substrate-first is sign-INDEPENDENT (vanishing test |R|≥1e-3 PASSes on both counting axes) — only the registered SIGN annotation was at stake.

**Verdict A — intensive substrate-FORCED.** The plan pinned intensive by INHERITANCE ("match W7 bcs_condensation_energy"); the team-lead flagged the BdG condensation-ENERGY functional as "arguably action-moment-class → extensive → −0.992." First-principles determination: the §VII.AJ.STATE-PROJ slot's DEFINING functional (registry line 16811/16840) is `ρ_BCS(P·H)` — a BCS-ground-STATE expectation. A state is normalized (Tr ρ_BCS = 1), so ρ_BCS(P_g·|w|) = Tr(P_g|w|)/Tr(P_g) = ⟨|w|⟩_g, the per-mode condensation DENSITY. This is VERBATIM the counting-axis discriminator's intensive "state evaluation ρ_g(f(D)), ρ_g = P_g/Tr(P_g)" mass/position-class form (regulator-pin-discipline.md §"Counting axis"). K₀-rank scaling test: under n_g→c·n_g the per-mode density is INVARIANT ⇒ intensive. ⇒ +0.9550 FORCED.

**Plan-pin VALUE vindicated; plan-pin REASONING corrected** (inheritance was non-binding on the counting axis — it transported a normalization, did not derive the class). No mack sign-fix; the registered +0.955 STANDS.

**The extensive −0.9917 is a DIFFERENT observable**, not a corrected sign: Tr(P_g|w|) = n_g·ρ_g(|w|) is the discriminator's "weighted trace" (action-moment-class) — the TOTAL-condensation-energy-budget partition (mode-count-dominated; n_col/n_sing=10430 ≫ a/b=43.5 ⇒ color wins by count ⇒ negative). Shares the negative count-dominated CHARACTER of the OP-PROJ companion R_∞≈−1.892 but is NOT cleanly Corner-I (still uses P_g). NOT lizzi-style dual-class (no regulator d.o.f.; the choice is forced by the slot's state-expectation definition). Registering both signs co-primary on one slot = single-observable-per-triple violation.

**Sage-exact** (s117 landau): R_int=+0.955037340514088, R_ext=−0.991696866949955; the entire flip is the K₀-rank n_g factor.

Durable outputs: §D corpus strengthening (which class forced + why) + optional mack reasoning-annotation (non-sign-changing). See `sessions/session-117/session-117-landau-synthesis.md`. Cross-link [[s116_w7_algebra_axis_result]] (the Level-1 orthogonality this builds on).
