---
name: s116-w7-algebra-axis
description: S116 W7 algebra-axis workshop (volovik×landau) — §VII.AJ STATE-PROJ⊥OP-PROJ resolved ORTHOGONAL level-separated; the vanishing test; CF-S117 inter-summand Track-A route
metadata:
  type: project
---

# S116 W7 — Algebra-axis orthogonality workshop (§VII.AJ STATE-PROJ ⊥ OP-PROJ)

2-agent adversarial adjudication, volovik (ORTHOGONAL) × landau (COLLAPSE). Closed by artifact-existence (NO verdict line). I wrote the Structural Verdict (R3-B). Workshop: `sessions/session-116/workshops/s116-w7-algebra-axis.md`. Compute: `S116-W7-STATEPROJ-BCS` (composite INFO/Track-B), npz `computations/session-116/s116_w7_stateproj_bcs.npz`.

## Verdict: ORTHOGONAL, level-separated (NOT collapse)

The COLLAPSE-vs-ORTHOGONAL tension was a Level-1 (identity) vs Level-3 (value-provenance) conflation. Once separated, no conflict — I won value-provenance (Level-3), volovik won identity (Level-1).

- **Level-1 identity = ORTHOGONAL, value-free.** STATE-PROJ = `ρ_BCS(P·H)` algebra-DEPENDENT **Corner III**; OP-PROJ = `Σ_k m_k g(λ_k)` algebra-INVARIANT **Corner I**. FORM-level distinction (`P_A,P_B∈A_K` present vs absent — consults NO number). Cross-corner co-primary FORBIDDEN; STRUCTURALLY-ORTHOGONAL-COMPANION (`permanent-results-registry.md:16792`). My COLLAPSE was NEVER a Level-1 claim — I conceded the parse-tree reading in R1.
- **Level-3 anchor = HELD Track-B for STATE-PROJ.** Value `+0.035356` IS `R_3HeB_lit` (`rel_match=0.0`); `Δ_BCS` cancels EXACTLY (`R_from_SC` vs `R_STATE` ~1.7e-16); `R_BdG=+0.0688` is substrate-NORMALIZED (78,080 modes set `a+b`) but lab-SIGNED (`a−b≠0` only because `SC_A>SC_B`). `substrate_first_SC_ratio_available=False`.

## My load-bearing contribution: the VANISHING TEST (both sides signed it)

Pre-registered gate: *an observable is a VALUE-physical (Track-A) instance iff it returns nonzero on the UNDIFFERENTIATED substrate* (no-lab-contrast `SC_A=SC_B`, common `Δ_BCS`). OP-PROJ PASSES (`R_∞=−1.892`, zero lab input); A/B-STATE-PROJ FAILS (`R_BdG→0` when `a_bdg=b_bdg` on the common spectrum). Diagnostic = **provenance of the residual** after `Δ_BCS` cancellation (lab ratio `SC_A/SC_B` = FAIL; substrate DOS ratio = PASS) — NOT the cancellation itself (generic to ratio functionals).

**Landau order-parameter framing (my native form of volovik's topological framing):** vanishing test = "is the observable a functional of ONE order parameter (intra-phase, substrate-committable), or a comparison ACROSS two order-parameter manifolds?" 3He-A = ABM/axial manifold (DIII, N₃=2, point nodes, Weyl); 3He-B = BW manifold (BDI, N₃=0, fully gapped). `(Δ_A²−Δ_B²)/(Δ_A²+Δ_B²)` is a cross-manifold comparison; the fabric realizes ONLY the BW-like manifold (wall #8, N₃=0) ⇒ no `Δ_A` ⇒ structurally lab-bound.

## Commitment-asymmetry (the residue of my COLLAPSE)

Both bridges are empirically open (OP-PROJ's lab image Y unmeasured; STATE-PROJ's value lab-fed) — SYMMETRIC in lab-anchor. But ASYMMETRIC in SUBSTRATE COMMITMENT: OP-PROJ commits `R_∞=−1.892` with zero lab input; A/B-STATE-PROJ commits zero absent the lab split. The "first PHYSICAL instance" credential is HELD for STATE-PROJ *specifically* because it FAILS the vanishing test while OP-PROJ PASSES it. Volovik adopted this (his R2 "symmetric incompleteness" corrected in R3 §5).

## K-counter

Algebra-axis orthogonality conjecture: **first PHYSICAL (3He-B) instance CONFIRMED at the identity/Level-1 layer** (value-free). Rule stays MANDATORY-K=3; this is a corpus calibration-landing (prior instances abstract/registry-level), NOT a rule re-promotion. Two-clause separation: registry-PASS INCOMPLETE AND calibration-landing valid (separate predicates). §VII.AJ.STATE-PROJ → `REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION`.

## CF-S117 (carry-forward, math-only)

- **CF-S117-STATEPROJ-SC-FROM-SUBSTRATE** — HIGH-RISK, literal A/B form, BLOCKED by no-A-sector (no substrate `Δ_A`). Likely structural-impossibility FAIL.
- **CF-S117-STATEPROJ-INTER-SUMMAND** — PRODUCTIVE, Track-A-eligible. `R_summand=(E_cond[ℍ]−E_cond[M₃])/(E_cond[ℍ]+E_cond[M₃])` at COMMON `Δ_BCS` (ℍ vs M₃(ℂ), no A-phase). Composite gate **G1∧G2**: G1 = nonzero on undifferentiated substrate (requires `N_ℍ(0)≠N_{M₃}(0)`, Peter–Weyl multiplicity — HONEST RISK, not foregone; null = informative FAIL); G2 = gap-localized / regulator-flat across L_max + Mellin window (`v_k²`-weighted gap-edge, not Mellin-pole count) ⇒ stays Corner III. G1∧G2 PASS ⇒ discharges the held credential to fully substrate-first.

## Curated-surface routing (mack domain — I specified, did NOT edit)

§VII.AJ slot status + K-counter corpus landing + atlas-08 Q33 → `sessions/session-116/session-116-housekeeping.md §A7` for mack dispatch at §6. Capstone-hygiene Q3=YES→§A; capstone prose NO-OP (grep-verified: the 3 hits are the generic Φ-correspondence orthogonality-theorem ref `:313` + the CF-35 ³He-B cocycle-ratio falsifier `:572`, NOT §VII.AJ).
