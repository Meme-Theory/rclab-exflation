---
name: s110-cf3-timescape-h0
description: S110-CF3-TIMESCAPE-H0 INFO — a2 tau-clock H0 relief sign-PASS but substrate-natural deg(T)=+2 transport does NOT close ~9% tension without a fitted knob
metadata:
  type: project
---

# S110-CF3-TIMESCAPE-H0 — a₂ τ-clock → H₀ propagation (INFO)

**Verdict**: INFO (sign=PASS, magnitude=INFO, regime=VALID). `audit_sha256=7bfda02abed5069d4dd4030377b8c448263069df43c27763d6d1e3e11217b013`. Script `computations/session-110/s110_cf3_timescape_h0.py`.

**Structural result** (the campaign master diagnosis made concrete on the H₀ channel):
The a₂ focusing-clock IS a genuine substrate Hubble-relief mechanism, sign-correct (clock_coeff=−3.08 < 0 ⇒ a void clocks faster ⇒ positive H₀ relief), bare BZ-leaf relief = 0.49% (Route B central) to 0.75% (paper edge, δρ/ρ=−0.46). But it does NOT natively close the full ~9% Planck-vs-SH0ES tension on this channel without a fitted knob the substrate forbids.

**The deg(T)=+2 transport on a DIMENSIONLESS relief fraction — three exhaustive readings** (the decisive structure, corpus §23 per-observable transport-degree theorem: substrate=pivot iff transport factor scalar):
1. scalar (deg=0): ΔH₀/H₀_pivot = ΔH₀/H₀_BZ EXACTLY (T2-VACUOUS) → 0.49%, below band, gap NOT closed.
2. full substrate-natural homogeneity (deg=2 over the full 54.04-decade BZ→pivot separation): kernel = 10^(±108.08) → overshoots [0.08,0.10] by ~107 decades in EITHER direction. Knob-free homogeneity applied literally is catastrophic — the LRD-T precedent (CF-CO34 leg B overshot a dimensionful T by 82 OOM).
3. fitted knob: the factor landing central 0.09 is **7500000/408331 ≈ 18.37** (Sage-exact), supplying only **1.17%** of the deg=2 × 54.04-decade budget — a CHOSEN number, NOT substrate-natural.

**Why INFO not FAIL**: a substrate-natural transport DOES move relief in the gap-closing direction (sign PASS) and bare relief is non-zero — channel not structurally empty. But knob-free transport does NOT land in-band; only a fitted ~18.4× (off-budget) reaches it → INFO per rubric (fitted/scalar reaches target, OR partial relief 0.75%<8%).

**Key lesson (transport-degree on dimensionless vs dimensionful observables)**: deg(T)=+2 is a HOMOGENEITY DEGREE on a dimensionful amplitude (`P_M4 ~ σ^(−d/2)`, W3 CF-CV6B), NOT a free multiplier. For a dimensionless ratio (ΔH₀/H₀, like d_s itself a scale-free quantity) the substrate-natural map is knob-free: it either leaves the observable flat (scalar leaf) or carries it the FULL scale separation (catastrophic overshoot). There is no substrate-natural intermediate landing — that is precisely why reaching a finite target band requires a fitted factor, which the INFO verdict flags honestly. This is the same structure the LRD-T temperature leg exhibited. See [[cv3-at-clock-rollup]] (a(t)/clock netting) and [[recent-s77-s79]] (A_s normalization-chain — same "substrate magnitude ≠ pivot magnitude without a derived transport" pattern).

**a₀-orthogonality (CV-4 CC↔H₀ interlock, WS-CC-H₀)**: w0_FW=−0.918 is the a₀ expansion-clock (DILUTION-CC) channel; the a₂ τ-clock is a FOCUSING clock (second Seeley-DeWitt moment), structurally a₀-orthogonal. Closing H₀ on a₂ does NOT consume the a₀ CC budget — sibling spectral moments. The interlock holds: the two relief channels are independent.

**Open**: the residual ~8.5% H₀ tension on this leg routes to another channel or remains an open H₀ gap. The substrate's native a₂ relief is real but partial.
