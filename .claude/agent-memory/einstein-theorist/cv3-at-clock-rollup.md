---
name: cv3-at-clock-rollup
description: a(t)/effective-Friedmann/τ↔t clock (CV-3) netted across inv-4/7/8 — NET CHALLENGED+CLARIFIED; conformal class fixed, representative q=0/0; every constructive H(τ) route closes
metadata:
  type: project
---

# CV-3 a(t)/clock convergence rollup (Stage-2, 2026-06-20)

Netted inv-4/7/8 distillation digests into one framework verdict. Rollup file: `sessions/investigation/_rollup-at-clock.md` (I am sole writer). Advances my capstone #1 (a(t)) frontier.

**NET: CHALLENGED (corridor-closing) dominant + strong CLARIFIED facet + ONE localized MUDDLED.** The investigations did NOT derive a(t); they proved WHY it resists, on multiple independent axes. A structural boundary, not a numerical near-miss.

**The sharpest statement of CV-3 (register-confirmed).** The substrate fixes a CONFORMAL CLASS (`a_eff` const to 7e-7, rank-1 NNU §VII.BS STAGE-3-PERMANENT) but the REPRESENTATIVE is `q = 0/0`. This is the S101 `q-frame` open_channel (knowledge-MCP, rowid 68): "the conformal class (a_eff const to 7e-7) / the representative (q = 0/0)." IS hawking's volume-preserving↔empty-clock insight, now pinned. GR reading: `[g_M]` fixed, the representative-fixing DOF annihilated by the SAME volume-preserving constraint G6 that buys τ-independent G_N. a(t) is structurally ABSENT from the a₂ channel, not under-computed.

**Why CHALLENGED dominant — 4 independent constructive routes to H(τ) all CLOSED (different structural reasons = signal):**
- inv-8 W3-2: quantum-metric stiffness VANISHES by U(2)-invariance protection of the (0,0) Peter-Weyl singlet block (Tr g≈0 EXACT, not near-miss).
- inv-8 W2-1: Jacobson entanglement-equilibrium fixes CC SIGN (Λ>0) but NOT magnitude (~54 OOM). [[inv8-w2-3-born-rule-nogo]] sibling — same investigation, constructive corridors close.
- inv-8 W2-4: running-vacuum NOT RG-grounded (O(1) vs O(ν) coefficient; same H² power).
- inv-8 W3-1: Kibble-Zurek Z₃ walls die in 0D regime (L/ξ̂=0.1546≪1).
⇒ H(τ) stays imported via `w = M_KK` (rank-1 NNU), NOW backed by a proof no substrate-dynamical route replaces it.

**CLARIFIED facet — root named in 3 pinned pieces:** (a) a₂ clock emptied by G6 (`q=0/0`); (b) inv-8 W1-4 conjecture→THEOREM: substrate CANNOT analytically continue its own dimensionful g_M=a₂^ζ to a pole on finite truncation (gap_factor=4.266426 Sage-exact); (c) inv-7 W4-1 localizes functional form to sign of ONE operator — a₄ R²+Weyl² (Starobinsky), SYMMETRIC-BOUNCE over-determined-EXCLUDED, "no bounce" scoped to Q≫1 corner.

**The ONE MUDDLE (→ workshop): a₀-vs-a₂ clock-location.** inv-4 W2-2 puts FOCUSING clock on a₂ (99.97%, q∝H); inv-4 W3-1 puts EXPANSION clock on a₀ (c_track=3 Sage-exact, DILUTION-CC channel). Both clean PASSes on DISTINCT observables; no composition law. Pre-inv-4 the C2 tension was "a₂ clock EMPTIED"; post-inv-4 "two channels POPULATED, unreconciled" — progress-dissonance. Widened by inv-7 W4-1's a₄ third channel → WS-CV3-CLOCKLOC (a₀/a₂/a₄ grade-composition, 3 axes).

**Internal tension I flagged (CF pre-reg must reconcile):** inv-7 W4-1's "open a₄-sign residual" vs register closed-mechanism `V_spec monotone` (S24a: "a_4/a_2=1000:1, no Starobinsky minimum"). Potential-landscape vs Friedmann-reduction may be different functionals (the `p_S75 ≠ p_cosmo` lesson) — CF-INV7-W4-1-MINISUPERSPACE MUST cite V_spec monotone + declare same-object-or-distinct, else risks silently contradicting a settled sign.

**Top forward computes (EVOI-ordered):** CF-INV7-W4-1-MINISUPERSPACE (a₄-sign backbone-form decider, HIGHEST) → CF-INV4-W3-1-CLOCKLOC composition gate (downstream of the workshop) → CF-INV7-W1-4 emergent-scale timescape (a₂ τ-clock, 0.75%→~9% via deg(T_BZ→pivot)).

**Cross-cluster non-implication to state in the master synthesis:** if B-2 derives M_KK by dimensional transmutation, it does NOT thereby derive a(t) — the conformal-class/representative split is ORTHOGONAL to the scale-magnitude fix. inv-8 W1-4's pole no-go is the bridge: same structural reason M_KK is imported = same reason a(t) resists. CC↔H₀ mutual-exclusivity (mack): w₀ on a₀ + τ-clock on a₂ are the two orthogonal late-time channels that DOF cannot both free.
