---
name: plan-w4-robustness-determinacy
description: Authoring a robustness/determinacy-closure wave-plan — within-family L_max drift (Seeley-DeWitt survival INFO→PASS) + determinacy-knob band-scan (consistency-pinned vs independently-pinned). Re-read of existing substrate-IS observables; no new prediction.
metadata:
  type: feedback
---

When a wave RE-READS an existing substrate-IS observable to test a robustness/determinacy threshold (NOT a new prediction), the gate is a CONTINUATION of the prior producing script, and the prior npz already carries the metric machinery.

**Why:** S99 W4 plan-authoring. W4-1 (`S99-W4-A0A2-LMAX13`) extends the S98 V.8 full-PV a₀/a₂ continuation; the S98 V.8 npz (`s98_a0a2_tier2_pv_invariance.npz`) ALREADY held `d_PV_within_family_drift=0.05703`, `ratio_PV_L10=0.51060`, `ratio_PV_L12=0.48148`, `eps_FI=0.05`, `info_band=0.1`, `O_FI_a0Mellin_over_a2Mellin=0.21756`, regulator-pin/poleconv/CLASS fields. The drift metric is `|ratio_PV(L)−ratio_PV(L10)|/|ratio_PV(L10)|` (step-relative). Do not re-derive it; pin the existing key.

**How to apply (the recipe):**
1. **Within-family drift INFO→PASS**: the drift sits in the INFO band (0.05,0.10] at the current L_max pair; the gate extends L_max to test if it shrinks below ε_FI. PASS<ε_FI / INFO (ε_FI, info_band] / FAIL no-shrink. The survival LABEL is regulator-INVARIANT already (S98 V.8: SURVIVE under both anchors, byte-identical) — the gate touches the RD-localized-to-L_max residual, NOT the survival label itself. Frame the FAIL as "RD is structural on the L_max axis" — it does NOT retract the regulator-invariant survival fact.
2. **L_max≥13 feasibility (LOAD-BEARING per `math-scripts.md` D_K block-diagonality pre-check)**: irrep construction at p+q≥13 may TIMEOUT. ALWAYS check for an existing cache FIRST — `find computations -name '*spectrum_cache_L1[3-9]*'`. An L14 cache (`s87_spectrum_cache_L14_tau019.npz`) EXISTS ⇒ L_max_operational=14 ≥ L_max_plan=13, NO new irrep construction. Cache-existence IS the Casimir-bound cross-check route; pin BOTH L_max_plan + L_max_operational in the gate.
3. **Full-physical PV pin**: the "_pauli_villars_subtraction.py" name in the context file is a LABEL — the actual full-physical PV evaluator is `computations/_shared/_analytic_zeta.py` (off-pole Mellin continuation + GPU heat-kernel factory), PV mass set c_j={2,−1}, m_j²/M_KK²={1,2} (NOT schematic Casimir-fraction). CLASS=FULL, NO `-SCHEMATIC` suffix, NO `# tier_pin=TIER-2` row (the S98 V.8 / S97 W2-1 precedent). regulator_pin a₀^{Pauli-Villars} (pole_in_s=4,n=0) / a₂^{Pauli-Villars} (pole_in_s=3,n=2), poleconv-A-double, d=8.
4. **Determinacy band-scan (W4-2)**: a "does an independent pin exist" gate is NON-PHONONIC gate-OBJECT (the knob's epistemic status) but the underlying κ-dependent observables are substrate-IS. PASS ≥1-in-band ⇒ INDEPENDENTLY-PINNED; FAIL/INFO none-in-band ⇒ stays CONSISTENCY-PINNED. WATCH the flat-grid trap: `Omega_peak_grid` is κ-INDEPENDENT (flat 9.15e-5 across the 121-pt κ-grid) — the κ-VARYING observable is the FREQUENCY axis, and that axis is ALREADY closed (S98 V.7 FAIL, f_obs=8.48e+39 Hz, +28.9 dec above HF ceiling). So the scan must target the COMPLEMENT observable set (seconds-scale predictions other than f_obs), and the gate honestly cites V.7 as the closed-axis baseline.
5. **audit-SHA vs content-SHA**: the context file cites `4522ea7e` for S98 V.8 — that is the verdict-line `audit_sha256` (closure over the input-pin map), NOT the npz CONTENT sha (`77f8ea52…`). Input-SHA ledger pins the CONTENT sha (runtime-verified); the verdict-audit-SHA is a provenance cross-ref only.

Pipeline: both gates `[SIGN]` → schema-v2 3-tuple companion row required. Producing scripts to `computations/_shared/` (or session-99); npz/png to `computations/session-99/`; verdict to `computations/session-99/s99_gate_verdicts.txt` via `emit_verdict`. Related: [[plan_authoring_r3_yaml]], [[cross_layer_shared_input_covariance]].
