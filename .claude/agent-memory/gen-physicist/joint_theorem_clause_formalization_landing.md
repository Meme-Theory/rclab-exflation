---
name: joint-theorem-clause-formalization-landing
description: Landing a STAGE-1-CANDIDATE JOINT cross-axis theorem by FORMALIZING a frozen workshop three-projection chain (not byte-extracting a LaTeX span) — f-string brace trap, Edit-tool mtime-race remedy, static slot pre-allocation under parallel landing.
metadata:
  type: feedback
---

Sibling of [[verbatim_extraction_registry_landing]] for the case where the Stage-0 source is a WORKSHOP convergence chain reproduced VERBATIM in the plan-block (substitution_chain / strict_PASS_boundary / joint_clause_attribution / stage_2_axis_pair), NOT a giant frozen LaTeX proof span on disk. The deliverable is the JOINT-clause FORMALIZATION wrapper (authored prose), re-deriving nothing per `joint-theorem-promotion.md` Stage-0→Stage-1. S111 W1-5 §VII.CH spectral-triple-no-holonomy-flux root (3 projections: operator d/dρ[Tr f]=0 / parameter no-kinematic-ρ_c / causal no-two-sided-bounce; JOINT single-root spectral-triple≠holonomy-flux-algebra; PASS).

**Why:** these JOINT registrations recur (sibling CLOCKLOC3 §VII.CG landed same wave, same pattern). The frozen chain is already in YOUR gate-block — you don't fetch a workshop file; you formalize the plan-block Steps into clause-structured registry prose. The procedural traps below cost real time if rediscovered.

**How to apply:**

1. **Source = the plan-block, not a disk span.** The frozen Stage-0 content is the substitution_chain Steps 1-5 + Conclusion + joint_clause_attribution + stage_2_axis_pair IN THE GATE BLOCK. Cite the workshop line-refs the plan gives (`ws-*.md:NNN-MMM`) as provenance; do NOT read other plan/WP/workshop docs (spawn forbids it). The `input_files:` pins (canonical_constants + permanent-results-registry + the distinctness-citation npz) are the only disk reads.

2. **f-STRING BRACE TRAP (cost a crash; caught by dry-run).** The promotion BODY contains literal LaTeX/markdown braces — `{λ_k(τ)}`, `{c, p~a²}`, `L^{-α}`, `√3/(32π²γ³)`. If `body` is an f-string, Python parses these as replacement fields → `NameError: name 'α' is not defined`. FIX: make `body` a PLAIN (non-f) triple-quoted string; inject computed anchors (tau_fold, dS_fold) via `__TAU__`/`__DS__` sentinel `.replace()` (no braces to double across a 10k-char body). The HEADER (no literal braces) can stay an f-string. ALWAYS dry-run `build_promotion_text` via a throwaway `python -c` import (pure function, ZERO disk I/O) BEFORE the real run — header-anchor symmetry + all `must_contain` markers + re_read round-trip on a fake-registry splice.

3. **EDIT-TOOL MTIME-RACE under concurrent WP writers (fired 3× before I switched).** The shared `session-N-wW-workingpaper.md` is edited by ALL gate-agents in the wave; the Edit tool is mtime-conditional, so a sibling's write between your Read and your Edit → "File has been modified since read" — repeatedly, indefinitely, while siblings are in_progress. REMEDY per `epistemic-discipline.md §"Registry-Write Hygiene under Parallel-Writer Race"` item 2: a single-shot Python read-modify-write helper. Anchor on a substring UNIQUE to YOUR section (the NOHOLOFLUX-specific hypothesis line + the §VII.CH-naming pending blocks — never a bare `**Status**: NOT STARTED` which every section has). Assert `count(OLD)==1` before replace; verify the 4 `must_contain` markers in the §Wx ... next-§Wx window after write; bounded retry. The helper is EPHEMERAL scaffolding (content fully captured in the section it writes) → `rm` it after (no-tech-debt); python-validate WARN on it (no canonical_constants import) is benign for a text-writer util.

4. **STATIC SLOT PRE-ALLOCATION deconflicts parallel landings.** When two gates land §VII entries in the SAME wave, the plan pre-allocates (CLOCKLOC3→§VII.CG, NOHOLOFLUX→§VII.CH). Take YOUR pinned slot; re-verify it free over ALL header levels (`^#{2,4}\s*§VII\.<slot>\b`) at write-time; reroute next-free + FAIL-with-remediation ONLY on FOREIGN occupancy. The pre-allocation means NO collision even if the sibling's slot hasn't landed yet at your write-time (I landed CH while CG was still unwritten — fine). No s87-lockfile needed when the plan statically pre-allocates.

5. **Registry-write byte-integrity (same as the verbatim recipe).** Snapshot PRE_LEN + PRE_SHA + PRE_CRLF (raw bytes) BEFORE the write. Binary-append `'ab'` LF-terminated, NO neighbor flatten. AFTER: assert POST_LEN−PRE_LEN == appended bytes; raw-byte CRLF UNCHANGED (read_text() text-mode normalizes \r\n→\n so its count is 0/0 — the REAL check is `read_bytes().count(b'\r\n')` unchanged); `sha256(b[:PRE_LEN])` == PRE_SHA (prefix bit-identical ⇒ no prior entry corrupted). On-disk header convention is `### §VII.X — …` (3 hashes + em-dash + space) — header-anchor symmetry per [[verbatim_extraction_registry_landing]].

5b. **A §VII LANDING HAS TWO SURFACES — body append ≠ complete (S111 W1-5 backfill, team-lead-flagged).** `permanent-results-registry.md` carries (1) a compact MASTER-INDEX TABLE near the TOP (`| §VII.X | THM | dense-summary | author | date |` rows, ~line 168) AND (2) the full SECTION BODIES far below (~line 22k). A single-shot EOF body-append lands the BODY but leaves the INDEX-TABLE ROW MISSING → `E_REGISTRY_VS_TABLE_DRIFT` (VII-SLOT-AUDIT flags it; a sibling correctly will NOT touch your slot to fix it — one-writer-per-slot). FIX YOURSELF: a SECOND single-shot Python read-modify-write that INSERTS one index row at the boundary `after | §VII.<prev-frontier> | THM | row's \r\n, before the next row`. The index-table region is **CRLF-terminated** → your new row MUST end `\r\n` (NOT the LF the body region uses) so the CRLF count goes +1 (your one row) and neighbors are byte-identical. Integrity proof: reconstruct-original = new minus (row+CRLF), assert its SHA == PRE_SHA (proves every other byte untouched). WATCH: `len(row)` is a CHAR count; the file delta is BYTES — a row full of `§λτ√²⁴≠⇒∧—` runs ~56 bytes longer than chars, so check `len(row.encode('utf-8'))+2`, NOT `len(row)+2` (a char-vs-byte assertion bug fails the verify on a correct write). Compose the row FROM YOUR OWN section body so axis-attribution + the verdict audit_sha256 are correct, never guessed; match the sibling rows' closing phrase "single-shot AFTER-pattern, slot runtime-verified next-free over ALL header levels + master-index table [frontier §VII.<prev>]; section body at §VII.X". No new verdict line owed (this finalizes the SAME landing). PRE-EMPT: make the BODY-landing script ALSO insert the index row in the same run, so the drift never opens.

6. **JOINT-theorem specifics.** The single-root statement is the JOINT clause (PASS-AND'd across BOTH Stage-2 reviewers). Per-axis clauses split by axis (Axis-A NCG-axiomatic; Axis-B cosmological-bridge). Stage-2 is a SEPARATE S112+ gate, NOT this gate — pre-register the axis pair, both NON-AUTHORS (exclude the Stage-0 workshop authors incl. successor-inheritance reach). DISSENT reach-tags (P1 all-orders exact vs P2 leading-order) carry INTO the registry text → this is the INFO-meaning content, but the gate PASSes as STAGE-1-CANDIDATE (declaration explicit, not OPEN-pending). Falsifier-status notes route to mack-cosmic-bridge (sole writer of falsifier-master-inventory.md) — do NOT touch the inventory.

7. **Verdict predicate** = section-nonempty(re-read) ∧ all required markers present(re-read) ∧ STAGE-1-CANDIDATE present ∧ CRLF-unchanged ∧ no-foreign-collision, ALL from the re-read-from-disk section. Single-shot AFTER-pattern (build→write→re-read→verify→emit ONCE); no conditional rewrite. `gate_type: compute` so it DOES emit a verdict line (registry-landing) via `emit_verdict`; value string names the slot + clause-checklist outcome.
