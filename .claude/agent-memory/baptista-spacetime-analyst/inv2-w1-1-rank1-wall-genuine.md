---
name: inv2-w1-1-rank1-wall-genuine
description: INV2-W1-1 FAIL — the rank-1 Yukawa wall is GENUINE physics (not a Schur artifact) under the minimal su(2)-split transverse modulus; generation degeneracy is protected deeper than U(2)-equivariance
metadata:
  type: project
---

# INV2-W1-1 (investigation 2, wave 1): off-U(2) Dirac Yukawa — rank-1 wall is GENUINE

**Verdict: FAIL** (sign=FAIL, magnitude=FAIL, regime=VALID). Investigation track; verdict in `computations/investigation-2/inv2_gate_verdicts.txt`. Script `computations/investigation-2/inv2_w1_off_u2_dirac_yukawa.py`. audit_sha256 `1481b77521449272961fced583e3827e9131e6f7b9c2085a9e665fc6728f833b`.

**Result.** A single transverse su(2)-split modulus (deformed_su2_split_metric: L2·I_3 → diag(L2 e^{+2δ}, L2 e^{-δ}, L2 e^{-δ}), volume-preserving det-ratio=1 EXACT) does NOT lift the per-generation (C²-coset) Yukawa degeneracy. On the genuinely-degenerate same-sign C²-coset multiplet (|λ|=0.8409, d=2 in the (1,0) fundamental; full ±|λ| mult = 4), the off-diagonal `Y_12(δ) = V_g†(1j D(δ))V_g` stays ~1e-17 across δ∈[0,0.20]; intra-multiplet split S(δ)~1e-15 (rigid shift, no fan-out); `dY_12/dδ|_0 = 1.9e-15 ≪ eps_lift=1e-3`; even the **cubic** Bridge-1 response is null (c3~1.8e-11). rank(Y) = 1 at every δ.

**Why genuine (the deep point — SHARPENED at S110 WS-C2COSET).** Global distinct signed eigenvalues DO lift 22→30 under the deformation — but that is **isospin-multiplet splitting** (SU(2)→U(1), expected at higher levels), NOT generation-splitting. The generation degeneracy is protected by **leg-membership**, not by "a symmetry deeper than U(2)" (my original vague formulation, now superseded): generations live on the **multiplicity leg `ℂ^{m(p,q)}`** of `H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}` (the right-regular Peter-Weyl factor; `t=(p−q) mod 3`), which is the COMMUTANT of every left-invariant frame operator (those act on the left-regular irrep leg `V_{(p,q)}` and `⊗1` on multiplicity). So the S62 "Rank-1 Yukawa" wall persists OFF the U(2) surface for ANY left-invariant transverse direction — su(2), C², all 28 left-invariant params. Per dual_prior → 0.90 Track B. **The protection is membership in the algebra-DEPENDENT (state-pair-functional) family vs the algebra-INVARIANT (spectrum-only) family** (`cross-pillar-bridge-anatomy.md` algebra-axis orthogonality, MANDATORY K=3); Morita-invariant module-membership (registry §VII.BL line 21155), not spectrum.

**Decisive method lesson (where errors hid).** Generation copies are NOT the algebraically-smallest signed eigenvalues — the Dirac spectrum is ±symmetric, so the lowest signed evals straddle DIFFERENT |λ| levels (different physical states); their off-diagonals are spurious cross-level elements, NOT generation-splitting. My first draft made this error and got a fake "lift" of 0.0377 + a fake BREAKDOWN regime. The CORRECT observable: project the FULL H(δ)=1j·D(δ) onto a FIXED δ=0 **same-sign degenerate multiplet** eigenbasis V_g; the resulting d×d Hermitian splitting block IS the generation Yukawa. Regime metric must be perturbativity (cond(g), 2.1→2.6 → VALID), NOT eigenvalue gaps (which are the exact ±λ structure, not a breakdown).

**C²-coset corridor CLOSED — prediction LANDED as a compute (S110-CF1-YUK-C2COSET, FAIL).** I had predicted the C²-coset anisotropy modulus (T4, split L3·I_4 → diag) "may couple to the generation multiplet at FIRST order where the su(2)-split could not" via O'Neill submersion anisotropy. The WS-C2COSET panel (baptista × van-den-dungen) refuted this STRUCTURALLY; the S110-CF1-YUK-C2COSET compute (session track; `computations/session-110/s110_cf1_yuk_c2coset.py`; audit_sha256 `86356de698e98b6564941ee9a00358d30cbb90e14b86c2a7367f009b9efa5bb0`) then confirmed it EMPIRICALLY: **FAIL (sign=FAIL, magnitude=FAIL, regime=VALID)**, `|dY_12/dδ|_0 = 8.727e-16 ≪ eps_lift=1e-3`. T4 is a LEFT-INVARIANT metric (block-diag by G10/BLOCK-DIAG-GENERAL-61), so it acts `⊗1` on the multiplicity leg like every left-invariant deformation — multiplicity-scalar by the W2 homogeneity wall, left-invariance ALONE (NOT Ad-equivariance, which lives on the irrep leg). My BAP1 step-3 lift-mechanism ("breaking Ad(SU(2)) lifts the rank") was leg-misrouted: the T3 su(2)-split ALREADY broke Ad(SU(2)) and returned dY_12/dδ=1.9e-15. The off-U(2) internal geometric hierarchy corridor (CV-8 Arm-G) is closed for ALL left-invariant moduli; hierarchy home = external non-LI `ε_LX` (§VII.BL STAGE-3-PERMANENT). Dual prior → 0.95 Track B.

**Two non-obvious things the C²-coset COMPUTE surfaced (beyond the panel's structural reasoning).** (1) **The dominant-stiffness modulus is MORE impotent, not less.** J_C2=0.933 is 15.8× the su(2) stiffness J_su2=0.059, yet `|dY_12/dδ|_0`(C²)=8.73e-16 is 2.2× SMALLER than the su(2) baseline 1.94e-15. Directional-stiffness magnitude on the irrep leg has NO bearing on the multiplicity-leg commutant projection (which is 0 regardless of J) — the leg-membership theorem made numerical. (2) **The intra-split rank-rise is an irrep-leg TRAP for this gate too.** The C²-coset split DOES raise `distinct_evals` 1→2 (at δ=0.005) via DIAGONAL eigenvalue drift (intra_split=3.56e-3 at δ=0.20; the two block evals 0.84075/0.84459 fan apart) — but that is rigid irrep-leg-coupled diagonal motion, NOT inter-generation mixing. The genuine generation observable is the OFF-DIAGONAL `max|Y_ij(i≠j)|≈1.3e-17`, flat at Schur-zero across the whole scan. The plan operator is the CONJUNCTION (`|dY_12/dδ|_0 > eps_lift AND rank 1→≥2`); it FAILs because the off-diagonal lift indicator (NOT the diagonal distinct-eval count) is at the numerical floor. **Method lesson restated for the C²-coset: distinct_evals can rise from irrep-leg diagonal drift while the generation off-diagonal stays zero — read the off-diagonal `max|Y_ij(i≠j)|`, not the distinct-eval count, as the generation-lift indicator.** Selection-rule pre-flight: triality t(gen)=1, t(O)=0, admissible=True (NOT triality-forbidden — the obstruction is leg-membership, not a center-character selection rule; so the numerical zero had to be MEASURED, not shortcut).

**What SURVIVES from my Reading A (re-homed, not discarded):** T4 has REAL first-order action on the IRREP leg — INV12-W2-1 computes a nonzero off-Jensen `||T||²`=7.8e-04 (fiber-modulus second-fundamental-form); T4 is the dominant directional stiffness (J_C2=0.933 vs J_su2=0.059) and the right lever for an a₂-moment / Weinberg-angle / gauge-normalization off-Jensen study (CF-S111-WEINBERG-C2COSET-OFFJENSEN, the response SLOPE `d(sin²θ_W)/dδ_C²|_0`, distinct from INV12-W2-1's a₂-additivity-leak bound). The geometry was real; I routed it to the wrong observable (generations, multiplicity leg) instead of gauge/gravity (irrep leg).

**Reusable infrastructure authored (C²-coset).** `deformed_c2coset_split_metric(B_ab,L1,L2,L3,delta)` in `s110_cf1_yuk_c2coset.py` — the 4-bond analog of `deformed_su2_split_metric`: splits the C²-coset block `L3·I_4 → L3·diag(e^{+3δ},e^{−δ},e^{−δ},e^{−δ})`, volume-preserving within the block (3δ−δ−δ−δ=0 ⇒ det-ratio=1 EXACT, verified 0.0). δ=0 recovers the U(2)-invariant metric bit-for-bit (max|g_split(0)−g_jensen|=0.0). This is the helper CF-S111-WEINBERG-C2COSET-OFFJENSEN should reuse (swap to the a₂/sin²θ_W response observable on the irrep leg).

**Reusable infrastructure authored.** `deformed_su2_split_metric(B_ab,L1,L2,L3,delta)` — the first transverse (U(2)-breaking) left-invariant metric helper; reuses the dirac_spectrum.py assembly chain (orthonormal_frame → frame_structure_constants → connection_coefficients → spinor_connection_offset → dirac_operator_on_irrep) with the split metric. INV2-W1-2 (Weinberg orbit-volume) was planned to reuse it (Bridge-2=Bridge-1). Pattern for any future transverse Milnor-complement deformation: same chain, different block split.

Links: [[fermion-hierarchy-fiber-overlap]] (S99/S100a on-surface dual-Z₃ overlap work — the COMPLEMENT: on-surface texture vs this off-surface metric deformation); [[permanent-results]] (S62 Rank-1 Yukawa PROVEN; Schur Y=λ·I_4 on U(2)-invariant).
