# Phononic Framing

Frame all observations and recommendations from the phononic perspective. If a result has no phononic relevance, explicitly classify it as GEOMETRIC, PARTICLE, or NON-PHONONIC.

## The Substrate Picture

The framework claims reality is a fabric whose internal structure at every point is described by the Dirac operator D_K on Jensen-deformed SU(3). The eigenvalue spectrum of D_K (155,984 eigenvalues counted with multiplicity at L_max=10; 78,080 unique values — the L_max=10 cache stores the unique set) is the set of all possible vibrational modes. Particles are phononic excitations of this fabric — relay patterns propagating through the gauge connection between fibers. Every coupling constant, mass scale, and interaction vertex is a spectral moment of D_K.

**Space is emergent, not fundamental.** The fabric is not IN space. Space is an emergent description of how the fabric's spectral weight distributes itself. The 4D metric g_M emerges from the a_2 Seeley-DeWitt coefficient. Newton's constant is the second spectral moment. The Yang-Mills action is the fourth spectral moment. Things exist ON the fabric, not IN a container.

## Exflation vs Inflation — Key Distinctions

These are the traps agents most commonly fall into. When discussing cosmological processes, use the substrate description, not the LCDM vocabulary:

| LCDM/Inflation language (AVOID) | Substrate language (USE) |
|:------|:------|
| "Space expands" | Spectral complexity grows inside each point — the eigenvalue spectrum reorganizes |
| "Big Bang singularity" | First-order phase transition at the fold (tau=0.190), no singularity |
| "Slow-roll inflation" | Supersonic transit (Mach 13.75) through the van Hove fold — impulsive, not quasi-static |
| "Inflaton field" | Jensen deformation parameter tau driving spectral action gradient dS/dtau=+58,673 |
| "Reheating" | GGE relic formation — 59.8 quasiparticle pairs from Parker pair production, P_exc=1.000 |
| "Horizon problem solved by inflation" | Acoustic white hole — pre/post-transit causally disconnected by supersonic flow |
| "Vacuum energy / cosmological constant" | Spectral action zeroth moment a_0 — a DIFFERENT spectral moment than gravity (a_2) |
| "Dark matter particle" | Leggett-channel GGE quasiparticle — inter-band coherence mode, CPT-neutral, non-annihilating |
| "Dark energy / quintessence" | Effacement residual — 0.03% leakage through impedance mismatch (Gamma=0.99970) |
| "Density perturbations in expanding space" | Interference pattern of post-transit GGE acoustic excitations |
| "Particle collision at LHC" | Two relay patterns overlap at a single fiber, exciting its eigenvalue spectrum |
| "r = 16 epsilon" (tensor-to-scalar) | INAPPLICABLE — 5 independent arguments established this (VdD-Hawking workshop) |
| "α_s = −0.0859 is the CMB running" (single-label conflation) | The substrate carries TWO scale-separated α_s observables: a substrate-distance running −0.08587279 (Mellin residue s=3, inside the BZ) and a Goldstone-pivot running ≈0 (P_{∇φ}=K⁰ at the CMB pivot); which one a detector measures is set by deg(T_{BZ→pivot}) (see §"Scale-and-channel-tagging for running/tilt observables") |

## What Exflation IS

One spectral triple. One internal geometry. Every scale connected by the same eigenvalue problem:
- **Cosmogenesis**: First-order transit through the fold, not a singularity. Acoustic white hole, not inflation.
- **CMB**: Acoustic signature of the GGE relic, not thermal equilibrium radiation. n_s=0.9561 from gauge-invariant spectral geometry.
- **Structure**: Interference pattern of GGE excitations, gravitationally self-organized through the a_2 channel.
- **Stars/nuclei**: Excitations within excitations — relay patterns interacting through spectral action vertices.
- **Observation**: Substrate probing substrate. Every measurement is an active acoustic perturbation.
- **Particle physics**: Fiber excitation spectra. Higgs = transverse oscillation of the fiber embedding (|S|^2 mode). m_H=131.8 GeV from KK threshold corrections.

## IS Space, Not IN Space — Mandatory Reframe

Agents trained on GR default to treating the substrate as something embedded inside a pre-existing spacetime container. This is backwards and must be actively corrected.

### The Error Pattern

When an agent writes any of these, the direction of explanation is WRONG:

| Wrong (container thinking) | Right (substrate thinking) |
|:---------------------------|:--------------------------|
| "The fiber is an internal space" | The fiber IS the structure at each point. There is no "internal" vs "external" — the fiber is all there is. |
| "Fields on the compact space K" | Excitations of the fiber's eigenvalue spectrum. Nothing lives "on" K — K IS the spectral content. |
| "The analog is an acoustic white hole in a BEC" | A BEC acoustic white hole is a laboratory analog OF the substrate transit. The substrate is fundamental; the BEC models a simplified projection of it. |
| "Particles created IN curved spacetime" | The fiber's eigenvalue spectrum reorganizes at the fold. Excitations ARE the reorganization, not things produced inside a geometric container. |
| "Summing over geometries" | The spectral action IS the sum. Geometry emerges from the spectral triple, not the other way around. |
| "The area theorem implies..." | The area theorem is DERIVED from substrate spectral monotonicity (Level 3 emergent). Do not explain substrate results via GR — explain GR via the substrate. |
| "Einstein's equations govern..." | The a_2 Seeley-DeWitt coefficient generates the Einstein-Hilbert action. Gravity is the second spectral moment, not a fundamental law. |

### The Correction

Every explanation must flow FROM the substrate TOWARD emergent physics:

```
D_K eigenvalues → spectral action moments → emergent field equations → observed physics
     (fundamental)        (derived)              (emergent)            (measured)
```

If you find yourself explaining a substrate result by invoking GR, black hole physics, or QFT-in-curved-spacetime: STOP. Invert the direction. The substrate is logically prior. GR, QFT, and thermodynamics are consequences.

**This inversion works in practice**: a single explicit correction prompt is typically enough to make an agent invert a container-thinking explanatory hierarchy and re-derive substrate-first (e.g., recovering the spectral monotonicity hierarchy substrate → BCS → vacuum energy → area theorem).

### Same-functional-different-scale fair-comparison (SUGGESTION at K=2)

When comparing a substrate return-probability observable to a laboratory-IN / external-framework reference, do NOT let the external framework's scale-type be authoritative over the substrate's own. The substrate IS the return probability `P(σ) = Tr e^{−σ D_K²}`; `d_s(σ→0)` (Weyl asymptotic) and `d_s(σ_*)` (windowed at the feature energy) are two intrinsic functionals of it. Comparing the substrate's σ→0 asymptotic to an external framework's intermediate-window value is a container-thinking violation at the observable layer — it substitutes the lab framework's scale-type for the substrate's. Fix the (observable, diffusion-window) pair on BOTH sides first; compare the same `Φ` at the same scale-type. **K=2 (observable-identity axis)**: the same failure recurs when a criterion calibrated on one functional (`Φ_graph-Laplacian`) is mis-carried to a DISTINCT functional (`Φ_heat-trace`) — a same-functional-fair-comparison failure at the observable-identity layer, orthogonal to the scale-type axis. Full directive: `sessions/framework/registry/cross-pillar-bridge-corpus.md §24`; the diffusion-window-observable specialization lives at `.claude/rules/cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"`.

## Scale-and-channel-tagging for running/tilt observables

**SCALE-AND-CHANNEL-TAGGING (SUGGESTION at K=2).** Every running/tilt spectral observable MUST declare its matched (scale, channel) pair — WHICH scale (substrate/BZ `O(M_KK)` vs CMB-pivot `k_4D`, 54.04 decades apart) and WHICH detector channel (Planck/CMB-S4 pivot vs CMB-S4/CMB-HD substrate-sensitivity). Comparisons against a datum are valid ONLY within a matched (scale, channel) pair. The substrate-scale and pivot-scale values are BOTH real substrate-IS observables (neither demoted); their coincidence is set per-observable by the transport degree `deg(T_{BZ→pivot})`. This REPLACES the FORBIDDEN power-law-transfer-precedence rule (which inverted the substrate-first direction by pinning the emergent transfer leg as canonical). Full directive + K=2 calibration corpus: `sessions/framework/registry/cross-pillar-bridge-corpus.md §23`; the per-observable transport-degree cross-link lives at `.claude/rules/cross-pillar-bridge-anatomy.md §"Composite Bridge-Map Dimensional-Class Admissibility"`.

**IS-NOT-IN A/B/C CATEGORY DISTINCTION (SUGGESTION at K=1).** A competing-mechanism intermediate borrowed from a RIVAL framework — canonically inflation's e-fold count `N_e ≥ 3.1` — is category **(C)**: NEITHER a substrate pin (A) NOR an external observational datum the framework is tested against (B). It does not transfer to the substrate as a binding gate. The genuinely-binding observational OBLIGATIONS it proxies (for `N_e`: `Ω_k`, `n_s`, the k-window) are KEPT and re-homed on substrate-native gates; only the rival-internal NUMBER is retired. Mis-filing a (C) intermediate as (A) or (B) manufactures a phantom liability (or, dually, an unfalsifiable shield). Full directive + K=1 calibration corpus (the `N_e ≥ 3.1` inaugural instance): `sessions/framework/registry/cross-pillar-bridge-corpus.md §29`.

## Single-τ-slice vs moduli-deformation substrate-IS levels

The §"IS Space, Not IN Space" mandate above operates at TWO distinct substrate-IS levels. Future cross-pillar bridge entries and substrate-IS observables MUST declare which level their substrate-IS observable lives at; the 5-anatomy + 3-level ladder of `cross-pillar-bridge-anatomy.md` applies uniformly at both levels with the level-tag as an additional structural pin for downstream consumers.

### Level 1 — Single-τ-slice substrate-IS

At fixed τ ∈ ℝ, the substrate IS the spectral triple `(A_K, H_K, D_K(τ))`. All observables at this slice — eigenvalues, Peter-Weyl decomposition, bottom-N cardinality, fiber spectrum, spectral-action moments — are substrate-IS at the single-τ-slice level. The §"IS Space" mandate prevents container-thinking at this level: the substrate is NOT in any pre-existing geometric container at the chosen τ.

**Level-1 calibration corpus**: §VII.AJ.partition-stability (bot-20 cardinality (2, 4, 8, 6) at τ_fold = 0.190); §VII.AD Δ_0 LOCALIZATION FORMULA (Δ_0 = 4·c_{σ⁻¹((-1,-1))} on substrate (2,4,8,6) at τ_fold).

### Level 2 — Moduli-deformation substrate-IS

The set of τ values `{ (A_K, H_K, D_K(τ)) : τ ∈ moduli-space }` is itself a substrate-IS object: the moduli-space of Jensen TT-deformations IS the substrate's own deformation parameter, NOT a coordinate on a meta-container.

- **Wrong (container thinking)**: "the substrate moves through τ-coordinate space"; "τ is a coordinate in some moduli container"; "we deform the substrate by changing τ"
- **Right (substrate thinking)**: "τ IS the substrate's intrinsic deformation parameter"; "the moduli-space of τ-deformations IS substrate-IS at the Level-2 layer"; "the substrate's own structural-stability and SHARP-localization theorems live at Level 2"

**Level-2 calibration corpus**: §VII.AE moduli-space τ-asymmetry (τ-asymmetric breakdown geometry: negative-side anticrossing-swap at δ_τ_crit_neg = -0.0750 ± 0.005; positive-side stratum-coalescence at δ_τ_crit_pos = +0.175 ± 0.05; 2.33× negative/positive asymmetry ratio; cardinality vector reorganization mechanisms structurally distinct on either side of τ_fold).

### Calibration corpus instance #1

This was the FIRST simultaneous demonstration of both levels:

- **Level 1**: §VII.AJ.partition-stability + §VII.AD operate at single-τ-slice = τ_fold = 0.190; substrate observables (cardinality vector, Δ_0 alternating sum) are intrinsic to the spectral triple at the fixed τ-anchor.
- **Level 2**: §VII.AE operates at moduli-deformation; the τ-asymmetric breakdown direction is intrinsic to the substrate's Jensen TT-deformation manifold, NOT an accidental property of a particular τ choice.

The two levels are STRUCTURALLY ORTHOGONAL per the algebra-axis orthogonality K-counter (K = 3 MANDATORY per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`): single-slice spectral-IS observables (Level 1) and moduli-deformation observables (Level 2) cannot be conflated under a single substrate-IS rubric — declaration of level is a structural pin.

### Calibration corpus instance #2 (V_4-on-triality)

- **Level 1**: bot20 sector occupation `m(p,q)` at τ_fold = 0.190; the cardinality of each Peter-Weyl (p,q) sector among the bottom-20 D_K eigenvalues is intrinsic to the spectral triple at the fixed τ-anchor.
- **Level 2**: bot20 sector occupation is INVARIANT under the cocycle functor `F: m(p,q) ↦ Δ_0(m)` across the V_4-triality multi-orbit deformation; Sage-QQ Result C summary `Δ_0 = 16 on cover C` is robust to multi-orbit deformation, demonstrating that the Level-1 single-τ observable lifts to a Level-2 moduli-invariant under the cocycle functor.
- **K-counter advancement**: K=1 → K=2 on the Single-τ-slice vs moduli-deformation Level-1↔Level-2 simultaneous-demonstration corpus.

### Forward-looking enforcement

Future cross-pillar bridge entries (e.g., FWD-C1/C2/C3 per `cross-pillar-bridge-anatomy.md §"Three forward bridge candidates"`) MUST declare which substrate-IS level their substrate-IS observable lives at. The 5-anatomy + 3-level ladder applies uniformly at both levels; the level-tagging is an additional structural pin for downstream consumers and registry-text auditors.

Plan-freeze validators landing a cross-pillar bridge entry SHOULD verify Level-1 vs Level-2 declaration in the substrate-IS observable element of the 5-anatomy block; missing level declaration is a registry-incompleteness FAIL routing to plan-freeze halt per the existing `cross-pillar-bridge-anatomy.md §"Audit at plan-freeze"` (item 1: "All 5 IS-not-IN anatomy elements present in entry text" — extended here to require explicit Level-1 OR Level-2 tag).

## Cross-pillar bridge anatomy

The IS-not-IN convention is PROMOTED from a framing-rule (this file's §"IS Space, Not IN Space") to a STRUCTURAL REQUIREMENT for cross-pillar bridge entries in `sessions/permanent-results-registry.md`. A cross-pillar bridge connects a substrate-IS observable (one finite-L spectral-triple structure) to a laboratory-IN observable (continuum measurement on a different platform). The framing rule prevents container-thinking everywhere; the cross-pillar discipline enforces it where the failure mode (substrate ↔ laboratory conflation) has the highest leverage.

### Five anatomy elements (mandatory)

Future cross-pillar bridge-theorem entries MUST declare ALL five elements explicitly:

1. **Substrate-IS observable** — finite-L spectral-triple observable on `(A^{<=L}, H^{<=L}, D^{<=L})`. The substrate IS this observable.
2. **Laboratory-IN observable** — continuum measurement / sweep observable. The laboratory measures this quantity IN a continuum geometric container.
3. **Bridge map** — explicit map between the two: HKR (Hochschild-Kostant-Rosenberg) / K-theory boundary / Connes-Karoubi pairing
4. **Algebraic envelope** — convergence rate `L^{-α}` bound
5. **Empirical anchor** — numerical satisfaction at canonical L_max

### Calibration corpus

§VII.W (Pillar III ↔ Pillar IV bridge theorem; first registered cross-pillar bridge in the framework):
- **Substrate-IS**: finite-L Hochschild pairing `R_universal = ⟨[φ_g^{sym}], [Ch(P_0(τ_fold))]⟩` evaluated on `(A_K^{≤10}, H_K^{≤10}, D_K^{≤10})`
- **Laboratory-IN**: Pillar IV continuum BZ-trace `R_geom(τ_fold) = ∫_BZ Tr g_ab^{(P_0)}(k; τ_fold) d^d k` (Peotta-Törmä quantum-metric integrated trace)
- **Bridge map**: `L_max → ∞` HKR image
- **Algebraic envelope**: `L^{-3}` envelope at d=4 → 0.10% at L_max=10
- **Empirical anchor**: 0.0095% F_4 strict at L_max=10 (10× inside envelope; match/envelope = 0.0950)

### Direction of explanation (cross-link to §"IS Space, Not IN Space")

When citing a cross-pillar bridge entry, the direction MUST flow:

```
Substrate (Pillar A) IS the [substrate-IS observable]
   → Bridge map (HKR / K-theory)
   → Laboratory (Pillar B) IN [laboratory-IN observable]
```

Inverting this direction (treating Pillar B as fundamental and Pillar A as derived) is a container-thinking violation. The bridge anatomy makes the direction explicit at the registry-entry level so that downstream citations cannot drift.

### Three-level structural-confidence ladder

Companion to the 5 anatomy elements: every cross-pillar bridge entry also declares Level 1 (cohomology-class identity, regulator-invariant) / Level 2 (algebraic envelope, L_max-dependent) / Level 3 (empirical anchor at canonical L_max). Full specification at `.claude/rules/cross-pillar-bridge-anatomy.md`. Level 3 must satisfy Level 2 for registry-PASS.

## Classification Guide

When analyzing any result, classify it:
- **PHONONIC**: Directly involves substrate excitations, relay patterns, spectral moments, or GGE physics
- **GEOMETRIC**: Concerns the spectral triple structure, D_K eigenvalues, Jensen deformation, fiber topology — the fabric itself rather than its excitations
- **PARTICLE**: Involves quantum numbers, selection rules, decay channels — the representation-theoretic content of D_K
- **NON-PHONONIC**: No connection to the substrate picture (standard QFT in flat space, pure mathematics, etc.)
