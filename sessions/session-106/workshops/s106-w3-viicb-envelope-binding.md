# Session 106 Workshop: van-den-dungen × transit

**Date**: 2026-06-13
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: van-den-dungen (van-den-dungen-bridge-theorist), transit (transit-dynamics-theorist)
**Source Documents**:
- sessions/session-106/session-106-w3-workingpaper.md
- sessions/permanent-results-registry.md
- computations/session-106/s106_gate_verdicts.txt

**Focus Topics** (the 4 adjudication questions from the W-2 schedule entry):
1. Is the §VII.CB Level-3 anchor (sign-structure relative-width residual) the SAME observable the binding L⁻³ envelope bounds (‖HKR(c_L) − g_M‖), or a DIFFERENT observable?
2. Does the §VII.CB Level-3 satisfy the Tier-1/Tier-2 dimensional-re-anchorability gate?
3. If Reading B wins, what is the correct registry status for §VII.CB?
4. Is the §VII.CB margin claim (1.333e5× "inside the envelope") meaningful, or a Level-3 annotation-discipline artifact?

---

## Round 1 — van-den-dungen: Opening Analysis

### VDD1: Same observable (magnitude-convergence image) or different (sign-saturation witness)?

**KEY FINDING (Reading B on Question (a)): the §VII.CB Level-3 anchor is a DIFFERENT observable from the one the binding L⁻³ envelope bounds. The envelope bounds `‖HKR(c_L) − g_M‖` (a magnitude-convergence channel); the Level-3 numerator is `max(|r_g−1|, |anec−1|)` (a sign-saturation channel against fixed integer anchors {1,1}). `g_M` — the named `c_continuum` — does not appear in the Level-3 numerator at all. This is a category mismatch, and the binding determination (3b, my own gate) does NOT license the registry-PASS inequality the way the entry asserts it does.**

I own the HKR / Connes-Karoubi envelope axis and I authored the 3b binding determination. I will be precise about what 3b did and did not establish, because the distinction is the whole adjudication.

**What corpus §1 Step-3 actually says (verbatim, the binding definition I cited in 3b).** From `cross-pillar-bridge-corpus.md §1`, Steps 3–4:

> Step 3 (substitution): A `L^{−α}` envelope on `‖HKR(c_L) − c_continuum‖` IS a Level-2-binding envelope **iff `c_continuum` is the HKR-image of the Level-1 cohomology class**. The envelope describes convergence of the Level-1 binding under the bridge map's `L → ∞` limit.

Read this carefully. The binding test is a test on the **envelope** (Element 4): is `c_continuum` the HKR-image, so that the rate `L⁻³` is the convergence rate of *the substrate-IS pairing to that image*? For §VII.CB the answer is YES — `c_continuum = g_M` (the BZ-trace a₂-emergent metric, `a_2_FW_zeta = 2776.165389`) IS the HKR `L→∞` image of `Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})`, and the codim-1 outermost-shell residual of the d=4 base integral gives `L⁻(d−1) = L⁻³` (Paper 01 §3, the fibre-integration / shriek-map factorization `[D_M] = π_! ⊗ [D_B]`; the truncation residual of the base-integral image is the codim-1 boundary contribution). **3b's binding determination is correct.** The envelope IS Level-2-binding.

**But binding-eligibility of the ENVELOPE is necessary, not sufficient, for a registry-PASS.** The registry-PASS *criterion* (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"`) is `Level-3 empirical value < Level-2 envelope value at canonical L_max`. For that inequality to *mean* what the criterion intends, the Level-3 number must be a numerical anchor *of the quantity the envelope bounds* — i.e., the finite-L value of `‖HKR(c_L) − c_continuum‖` at `L_max=10`. Corpus §1 Step-5 makes this explicit: the registry-PASS criterion is "MEANINGFUL ONLY for Level-2-binding envelopes; applying it [to a quantity that does not bind] admits false-PASS (the empirical Level-3 anchor passes a numerical bound on a quantity that does not bind to the laboratory measurement)."

**The §VII.AF.1 positive precedent — the template §VII.CB must match.** Look at how the FIRST registered bridge does it (registry §VII.AF.1.OP-PROJ, line 14921 + 14942–14948). Its Level-2 envelope is `L⁻³` at d=4. Its Level-3 anchor is `err_STRICT = 0.0095%` — and `err_STRICT` is *literally the convergence residual of the same quantity the envelope bounds*: `|R_universal_strict_F4 − Atlas_5 loose| / Atlas_5 loose`, i.e. the finite-L Hochschild-pairing/atlas-match converging to the continuum BZ-trace `R_geom` (Peotta–Törmä quantum-metric integrated trace). The 10× margin is then *explained by the structure of that same convergent quantity*: the S91 W0 annotation (registry line 14944) attributes it to a **negative subleading C_1 in the CM-1995 §III.4 finite-L expansion** `L⁻³·(C_0 + C_1·L⁻¹ + …)` — the finite-L value lies below the asymptotic envelope. That is what a genuine Level-3 anchor on the bound quantity looks like: it is the residual `‖HKR(c_L) − c_continuum‖`, and its margin is a statement about the subleading terms of THAT residual.

**§VII.CB does NOT do this.** Its Level-3 number is `max(|r_g − 1| = 2.193e-10, |anec − 1| = 7.500e-09) = 7.500e-09` (registry §VII.CB Element 5, line 21986; WP §W3-2 lines 101–105). Trace the numerator:
- `r_g = |g_core|/|g_ext| = 1.0000000002` anchored to the EXACT integer 1 (core/exterior magnitude-balance);
- `anec = 1.0000000075` anchored to the EXACT integer 1 (ANEC saturation).

Neither `g_core`, `g_ext`, `r_g`, nor `anec` is `‖HKR(c_L) − g_M‖`. The continuum a₂-metric `g_M` (= `a_2_FW_zeta = 2776.165389`) does not enter the numerator. WP §W3-2 line 101 states it plainly: **"The type-IV EMT is a SIGN-anchor compute (not a continuum-convergent magnitude)."** The Level-3 number is the relative-width residual of *sign-structure invariants against their own fixed integer targets* — a witness that the type-IV core/exterior sign-balance is saturated, not a witness that the HKR image has converged to `g_M`.

**The two observables live in different channels and are not even on the same convergence axis.** Decompose the type-IV pairing into the two functionals it carries:
- a **magnitude** functional: `M(L) := Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_{L}`, whose HKR `L→∞` image is `g_M`. THIS is what the L⁻³ envelope bounds: `‖HKR(M_L) − g_M‖ ≤ C·L⁻³`. It is a *convergent magnitude*.
- a **sign-structure** functional: `S(L) := (r_g(L), anec(L))`, the dimensionless ratio-and-saturation invariants of the same EMT. These are pinned to the integers {1, 1} by the type-IV topological sign structure (g_core < 0, g_ext > 0, sign_flip = True, n_crossovers = 1). `S` is *already saturated* — its residual to {1,1} is a measure of how cleanly the sign topology is realized, not of how the magnitude converges to `g_M`.

The envelope bounds `M`; the Level-3 anchor measures `S`. **They are different observables.** Worse, `S → {1,1}` is a sign/topological saturation that need not have ANY L⁻³ convergence character — a ratio of two same-sign-structure magnitudes can be pinned to 1 by the construction (or by an O'Neill-type sign-balance identity) independent of how fast `M` converges to `g_M`. (This is exactly the surrogate-vs-canonical hazard `substrate-first-canonical-sourcing.md §(iv-bis)` warns about: a dimensionless ratio "mechanically locked" by an internal balance is a GEOMETRIC observable, not a cohomology-class convergence witness, and its FAIL/PASS is uninformative on the canonical convergence it stands in for.)

**Consequence for the inequality.** `7.500e-09 < 1.0e-03` is a true inequality between two finite reals, so it is *evaluable* (3b's "L3<L2_evaluable=True" is honest). But it is a category-mismatched comparison: a residual of `S` (sign-saturation, already at floor) is held against an envelope on `M` (magnitude-convergence to `g_M`). The inequality passing tells us the sign structure is clean to 8 digits; it does NOT tell us `‖HKR(M_L) − g_M‖ < 1.0e-03` at `L_max=10`, which is the thing the registry-PASS criterion is supposed to certify.

**Scope honesty (what I am NOT saying).** I am not down-grading the envelope. Element 4 IS Level-2-binding (3b + 3d are theorem-grade on the analytic leading rate `α = d−1 = 3`, derived directly from §VII.AG.1's own HKR∘Connes-Karoubi bridge map + the §VII.T residue structure — the Leg-B / Reading-B base-dimension rate, Paper 01 fibre-integration image). The defect is at Element 5 / Level-3, not Element 4: the supplied Level-3 anchor is a witness of the wrong observable. That is a registry-PASS-criterion defect, not a binding defect.

**Question for transit (you own what the sign-anchor residual physically IS):** is `S(L) = (r_g, anec)` *forced* to its integer anchors {1,1} by the type-IV topology (so that `S → {1,1}` carries NO L⁻³ convergence information at all and is structurally orthogonal to `M`'s convergence to `g_M`), or does `r_g(L)` genuinely flow toward 1 *with* the magnitude `M(L)` converging to `g_M` (so that `S` is a degraded-but-real proxy for `‖HKR(M_L) − g_M‖`)? The disposition in VDD3 turns on whether the sign-residual is a *fixed-anchor saturation* (Reading B, category-mismatch, down-tag) or a *co-convergent proxy* (a weaker Reading-A defense).

### VDD2: Tier-1/Tier-2 dimensional-re-anchorability gate satisfaction

**KEY FINDING (Question (b)): the §VII.CB Level-3 anchor satisfies NEITHER Tier-1 NOR Tier-2 of the dimensional-re-anchorability gate. It is a third structural object — a fixed-integer-anchor residual on a SATURATED channel that is orthogonal to the bridge's convergence channel. The gate is registry-PASS-ELIGIBLE only via Tier-1 OR Tier-2; a sign-saturation residual is neither, so §VII.CB's Level-3 row is NOT registry-PASS-eligible as currently anchored.**

The gate (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion" → Tier-1/Tier-2 dimensional-re-anchorability gate`; corpus §25):

> registry-PASS-ELIGIBLE only if EITHER **(Tier-1)** its residual-to-`c_continuum` shrinks with L_max (convergent ⇒ a substrate-singled-out `L*` exists), OR **(Tier-2)** its divergent channel's truncation-invariant content is DIMENSIONLESS and the anchor is RE-ANCHORED to that invariant.

Walk §VII.CB's Level-3 anchor (`7.500e-09`) through each tier.

**Tier-1 (convergent-to-`c_continuum`)? — FAILS / N/A.** Tier-1 requires that the anchor's *residual to `c_continuum` (= g_M) shrinks with L_max*, exhibiting a substrate-singled-out `L*`. But the §VII.CB Level-3 anchor is not a residual to `g_M` at all (VDD1) — it is `max(|r_g−1|, |anec−1|)`, the residual to the *integer anchors {1,1}*. There is no `‖HKR(c_L) − g_M‖` quantity being tracked across L_max, so the Tier-1 predicate ("residual-to-g_M shrinks with L_max") cannot be evaluated — it is vacuously not-satisfied. Critically, **no L_max scan exists**: the Level-3 number is the S105 type-IV single-point compute at `L_max=10` (npz audit `91b36ed9…`); there is no `{L_max=8,10,12}` convergence series to exhibit an `L*`. Compare the genuine Tier-1 occupant §VII.AV.STATE-PROJ (corpus §25.1): its `L_emp = −7.046336 M_KK²` is Tier-1-CONVERGENT — "residual 2.82e-04 shrinking, Connes-Karoubi-bound" — *that* is a residual to the continuum image that shrinks. §VII.CB has no analog.

**Tier-2 (divergent-channel re-anchored to a dimensionless invariant)? — FAILS / N/A.** Tier-2 is the escape hatch for a *divergent* channel: if the channel diverges but its truncation-invariant content is a DIMENSIONLESS log-derivative / ratio / cohomology class, the anchor re-anchors to that invariant (the structural basis is `O(L,K) = W(L)·g(K) ⇒ only log-derivatives annihilate W(L)`, corpus §25). Two reasons §VII.CB's anchor is not Tier-2:
1. The sign channel is **not divergent** — `r_g` and `anec` are O(1) and pinned to 1. Tier-2 is defined for a divergent channel whose dimensionful magnitude can't be anchored; there is no divergence here to re-anchor *away from*.
2. Even reading `(r_g, anec)` as "the dimensionless invariants," they are NOT the re-anchored *truncation-invariant of the bound channel* `M`. Tier-2 re-anchors the SAME divergent observable to its own dimensionless invariant (e.g. §VII.AX.OP-PROJ's `n_PBH` cardinality channel re-anchored to the cascade exponent `d ln N_eigs/d ln L → 5`, corpus §25.1). §VII.CB instead *substitutes a different observable* (the sign channel `S`) for the bound observable (the magnitude channel `M`). That is not re-anchoring; it is channel-swapping.

**What §VII.CB's Level-3 anchor structurally IS — a third object.** It is a relative-width residual of a *saturated, dimensionless, integer-anchored* sign-structure on a channel ORTHOGONAL to the bridge's convergence channel. Using the `O(L,K) = W(L)·g(K)` decomposition of corpus §25 / `math-scripts.md §"Multiplicative-normalization cancellation invariants"`: the sign invariants `(r_g, anec)` are pinned to 1 by the type-IV topology *independent of `W(L)`* — they are not even a log-derivative of the bound channel; they are constants of a different (sign-balance) functional. The gate's two tiers exhaust the registry-PASS-eligible cases for a Level-3 anchor that is *of the bound channel*; an anchor that is *not of the bound channel* falls outside both by construction.

**The §VII.AX precedent fixes the disposition.** Corpus §25.1 (inaugural Tier-1/Tier-2 occupant, §VII.AX.OP-PROJ `n_PBH`): Tier-1 FAILS (divergent), Tier-2 DIMENSIONFUL (the dimensionless content shares the multiplicative slot with the m⁻³ dimension) ⇒ **"registry-PASS-INELIGIBLE; m⁻³ Level-3 row HELD `NOT-SATISFIED-PENDING-substrate-physical-scale-anchor`; theorem-STRUCTURE STAGE-3-PERMANENT (Stage-2 PASS-AND on non-Level-3 clauses)."** This is the exact two-part disposition §VII.CB needs: the *theorem-STRUCTURE* (the binding L⁻³ envelope + the 5-anatomy + the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`) can stand; the *Level-3 row* is HELD pending a magnitude-convergence anchor. §VII.AX even spells the registry consequence: "5 surgical Level-3-row demotions … Theorem-structure STAGE-3-PERMANENT tags … left untouched."

**One subtlety that distinguishes §VII.CB from §VII.AX (in §VII.CB's favor, partially).** §VII.AX's `n_PBH` channel is genuinely *divergent* (cardinality `~L⁵ → ∞`) — its magnitude can never be anchored from inside the channel. §VII.CB's magnitude channel `M = Tr_{M₂}(P_a₂·T^{(IV)})` is, by the binding determination, *convergent* to `g_M` at rate L⁻³ — so a magnitude-convergence Level-3 anchor IS in principle *constructible* (Tier-1-eligible), unlike `n_PBH`. The §VII.CB defect is therefore *less severe* than §VII.AX's: §VII.AX is permanently-held-pending-an-external-scale; §VII.CB is held-pending-a-recompute-that-can-be-done (the `‖HKR(M_L) − g_M‖` series across `{L_max=8,10,12}`). This is precisely the CF-S107 magnitude-convergence anchor the workshop spec pre-registers. So §VII.CB is a **Tier-1-eligible-but-not-yet-anchored** entry, not a Tier-2-dimensionful permanent hold.

**Question for transit (you own constructibility of the magnitude anchor):** the binding determination says `‖HKR(M_L) − g_M‖ ≤ C·L⁻³`. To realize the Tier-1 anchor we need the finite-L `M(L) = Tr_{M₂}(P_a₂·T^{(IV)})|_L` evaluated at `L ∈ {8,10,12}` against `g_M = a_2_FW_zeta`. From the type-IV EMT side: is `M(L)` a single number per L (so the residual `|M(L) − g_M|/g_M` is well-defined and we expect it `∝ L⁻³`), or does the type-IV compute only ever produce the *sign-structure* invariants (g_core, g_ext, r_g, anec) and never a scalar magnitude that limits to `g_M`? If the latter, the bridge's Level-1 identity `[T^{(IV)}] = [g_M]` is a cohomology-class statement with no scalar magnitude image — in which case the Tier-1 anchor is NOT constructible from the type-IV side and the disposition shifts toward the §VII.AX permanent-hold pattern (re-anchor the dimensionless content, Tier-2-style). That is the pivotal constructibility question for VDD3/T2.

### VDD3: If Reading B wins — correct §VII.CB registry status

**KEY FINDING (Question (c)): if Reading B wins, the correct disposition is NOT a blanket down-tag to STAGE-1-CANDIDATE. It is the §VII.AX two-part split: (i) the theorem-STRUCTURE (binding L⁻³ envelope + Level-1 cohomology-class identity + the 5-anatomy) STAYS STAGE-3-PERMANENT; (ii) the Level-3 ROW is HELD `NOT-SATISFIED-PENDING-magnitude-convergence-anchor`, with the registry-PASS claim qualified accordingly. Because the magnitude channel is convergent (Tier-1-eligible, unlike §VII.AX), the held tag points at a constructible CF-S107 recompute, not a permanent external-scale hold.**

**Why not a blanket STAGE-1-CANDIDATE down-tag.** STAGE-1-CANDIDATE is the right status when the THEOREM itself is unverified — when a joint clause has not passed Stage-2 cross-axis verify, or when the structural claim is in doubt. That is not the situation here. The structural content of §VII.CB that is *not* the Level-3 row is sound:
- **Element 4 (the binding envelope)**: theorem-grade. `α = d−1 = 3` is derived directly from the HKR `L→∞` boundary map (Paper 01 §3 fibre-integration image; the codim-1 outermost-shell residual of the d=4 base integral) — 3b and the independent 3d direct re-derivation both reproduce it, and the binding test (corpus §1 Step-3: `c_continuum = g_M` IS the HKR-image) PASSES. Nothing about Reading B touches this.
- **Level-1 (the cohomology-class identity)**: `[T^{(IV)}]_{a₂-channel, HKR-class} = [g_M]_{a₂-channel, HKR-class}` is regulator-invariant and L-independent (registry §VII.CB Level-1, line 21990). It is a Kasparov-product statement — the same a₂ Seeley–DeWitt curvature-degree-2 K-homology class read on both sides of the shriek map. Reading B does not dispute it; Reading B disputes whether the *supplied Level-3 number* is a numerical anchor of it.
- **The 5-anatomy**: all five elements are present with explicit values; the audit (`_cross_pillar_bridge_audit.py`) returns 5/5 anatomy + 3/3 tier markers + Element-2 OE-form PASS. The DEFECT is not anatomy-completeness; it is that Element-5's *content* witnesses the wrong observable.

So the structural theorem is permanent; what fails is the *empirical-confirmation predicate* (`Level-3 < Level-2` as a meaningful statement about the bound channel). That is precisely the §VII.AX situation, and §VII.AX is the governing precedent (corpus §25.1): "theorem-STRUCTURE STAGE-3-PERMANENT (Stage-2 PASS-AND on non-Level-3 clauses); … Level-3 row HELD `NOT-SATISFIED-PENDING-…`."

**The disposition I will defend (effected by mack at R3, NOT a retroactive edit).** Per `epistemic-discipline.md`, the on-disk `S106-W3-3` verdict (audit `293105a2…`, REGISTRY-PASS) is PERMANENT — it is not edited. The workshop verdict is a NEW pinned position landed as a mack-cosmic-bridge sole-writer registry annotation on the §VII.CB entry. Concretely (the R3 patch instruction to mack):

1. **Hold the Level-3 row.** Annotate §VII.CB Element 5 + the Registry-PASS paragraph (registry lines 21986, 21994, 22003, 22016) with: the supplied Level-3 anchor `max(|r_g−1|, |anec−1|) = 7.500e-09` is a **sign-structure saturation residual** (witnesses `S(L) = (r_g, anec) → {1,1}`), NOT the magnitude-convergence residual `‖HKR(Tr_{M₂}(P_a₂·T^{(IV)})_L) − g_M‖` the L⁻³ envelope bounds; the Level-3 row is HELD `NOT-SATISFIED-PENDING-magnitude-convergence-anchor` per `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` Tier-1/Tier-2 gate.
2. **Re-tag the registry-PASS claim.** REGISTRY-PASS → **STAGE-3-PERMANENT-STRUCTURE / Level-3-HELD** (the theorem-structure is permanent on its non-Level-3 clauses; the registry-PASS *inequality* is not yet a binding-channel-consistent satisfaction). This mirrors §VII.AX exactly ("registry-PASS-INELIGIBLE [on the Level-3 row]; theorem-STRUCTURE STAGE-3-PERMANENT").
3. **Retain the sign-residual as a separate witness.** The `7.500e-09` is a *real* substrate-IS result — it is a clean Level-1-adjacent witness that the type-IV sign topology (g_core < 0 ANEC-violating core, g_ext > 0 type-I exterior, sign_flip, n_crossovers=1) is realized to 8 digits. Keep it, demoted from "the Level-3 anchor of the bridge" to "a sign-structure saturation witness of Element 1."
4. **Point the held tag at the constructible CF.** Because the magnitude channel is *convergent* (Tier-1-eligible), the hold is discharge-eligible by `CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR` (the `‖HKR(M_L) − g_M‖` series across `{L_max=8,10,12}`). On that CF's PASS, the Level-3 row promotes from HELD to SATISFIED and the entry earns full REGISTRY-PASS.

**Why this is the §VII.AX disposition and NOT the §VII.AV deferred-pending disposition.** §VII.AV is `REGISTRY-INCOMPLETE-PENDING-PROXY-REFINEMENT` — its *envelope* is only a SCHEMATIC proxy (Casimir-bound), and the FULL physical pipeline is unrealized (and indeed FAILED at FULL-PV, `α_PV = 6.263789±inf`, registry line 18808). §VII.CB is different: its envelope is FULL (analytic shell-sum on the FULL canonical `a_2_FW_zeta`, NO CLASS pin / NO SCHEMATIC helper), binding, and theorem-grade. The defect is not in the envelope's physical realization (§VII.AV's problem); it is that the Level-3 *anchor* witnesses a different observable than the (sound) envelope bounds. That is the §VII.AX Tier-1/Tier-2 gate problem, not the §VII.AV proxy-refinement problem. Filing §VII.CB as §VII.AV-style deferred-pending would mis-locate the defect.

**The Reading-A defense I must answer (and why it doesn't carry a full REGISTRY-PASS).** Reading A says: "the bridge binds the IMAGE, and at `L_max=10` the sign-structure residual IS the L=10 image — a legitimate Level-3 witness." The kernel of truth: at finite L, the substrate-IS observable IS the finite-L type-IV EMT, and the sign invariants ARE genuine finite-L observables of it. But the registry-PASS criterion is not "is the Level-3 number a finite-L observable of the substrate" — it is "is `Level-3 < Level-2` where Level-2 bounds the *convergence of the bridge image*." A sign-saturation residual is not a point on the convergence curve `‖HKR(M_L) − g_M‖`; it is a point on a *different* curve (`‖S(L) − {1,1}‖`) that may sit at floor for reasons (topological sign-balance) wholly unrelated to magnitude convergence. Reading A conflates "a finite-L substrate observable" with "the finite-L value of the bound quantity." Those are not the same — and corpus §1 Step-5's false-PASS warning is exactly against this conflation. Reading A's strongest form survives ONLY if transit establishes (T1/T2) that `S(L) → {1,1}` is *co-convergent* with `M(L) → g_M` at the same L⁻³ rate (then the sign-residual is a degraded-but-real proxy and the entry is at worst a `-SCHEMATIC-proxy` Level-3, registry-PASS-eligible with a proxy caveat). If `S → {1,1}` is a fixed-anchor saturation orthogonal to `M`'s convergence (my expectation), Reading A fails and the hold stands.

**Net:** Reading B wins as stated EXCEPT that the down-tag is the §VII.AX *structure-permanent / Level-3-held* split, not a blanket STAGE-1-CANDIDATE. The structure stays; the Level-3 row holds; the CF-S107 magnitude-convergence anchor is the discharge path. I defer the final tag to the R3 convergence with transit on the constructibility question (VDD2/T2).

### VDD4: Is the 1.333e5× margin meaningful or an annotation-discipline artifact?

**KEY FINDING (Question (d)): the 1.333e5× margin is an ARTIFACT — it is large precisely BECAUSE the Level-3 residual measures a different, already-saturated quantity (sign-balance → {1,1}, at floor) than the envelope bounds (magnitude-convergence → g_M). It is the textbook `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"` artifact: a margin that is non-load-bearing because the two numbers being divided are not measurements of the same thing.**

**The arithmetic of the artifact.** `margin = Level-2 / Level-3 = 1.0e-03 / 7.500e-09 = 1.333e5×` (5.12 OOM). For a margin to be a meaningful "depth inside the envelope," both numerator and denominator must be values of the *same convergent quantity at the same L_max*: the envelope value `C·L⁻³|_{L=10}` and the residual `‖HKR(M_L) − g_M‖|_{L=10}` of THAT quantity. Then `margin = envelope/residual` says "the residual is N× smaller than the worst-case envelope predicts" — a real statement about subleading terms. But §VII.CB's denominator is `‖S(L) − {1,1}‖` (sign-saturation), which sits at `~7.5e-9` because the type-IV sign topology is cleanly realized — NOT because the *magnitude* converged 1.33e5× faster than L⁻³ predicts. The two numbers are dimensionally-comparable (both dimensionless relative widths) but ontologically different, so their ratio is not a convergence depth. It is a comparison of "how clean the sign balance is" against "how fast the magnitude should converge" — an apples-to-oranges ratio whose largeness is uninformative.

**Contrast with the GENUINE §VII.AF.1 10× margin (the calibrated reference).** §VII.AF.1's margin (10× inside, registry line 14942) is meaningful precisely because its Level-3 (`err_STRICT = 0.0095%`) IS the convergence residual of the same quantity the envelope bounds (the atlas-match → BZ-trace `R_geom`). The S91 W0 annotation (registry line 14944) then *explains the 10×* structurally: a **negative subleading C_1** in the CM-1995 §III.4 expansion `L⁻³·(C_0 + C_1·L⁻¹ + …)` puts the finite-L value below the asymptotic envelope. That is what a load-bearing margin looks like — it is a statement about the subleading coefficients of the *bound* quantity, and §VII.AF.1 can name the mechanism (negative C_1) and its companion (positive C_1 at §VII.AU.OP-PROJ → 2.6926 *under*-performance). §VII.CB's 1.33e5× has **no such structural explanation available** because the denominator is not on the convergence curve at all — there is no "C_1 of the sign-saturation residual relative to the magnitude envelope," the two quantities don't share an expansion.

**The annotation-discipline rule is explicit on this.** `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"`: "a registered Level-3 row's PASS verdict is governed SOLELY by the central-value criterion `Level-3 < Level-2 at canonical L_max`. Descriptive [margin] statements are NON-LOAD-BEARING annotations." The 1.33e5× is a descriptive annotation; it carries no PASS weight by itself. And in §VII.CB's case it is worse than non-load-bearing — it is *misleading*, because its 5-OOM size invites the reading "this bridge is spectacularly deep inside its envelope," when in fact the depth is an artifact of comparing a saturated sign-residual to a magnitude envelope. The 5.12-OOM size is a *symptom* of the category mismatch (VDD1), not evidence of convergence quality.

**A diagnostic that exposes the artifact (and a thing transit can confirm).** If the margin were a real convergence depth, it should *change with L_max* in lockstep with the envelope (residual and envelope both `∝ L⁻³`, margin roughly L-flat or slowly varying). But the §VII.CB Level-3 number is an L_max=10 single-point sign-residual; `7.5e-9` is set by the *precision of the sign-balance realization*, which is a topological/numerical-floor property, not an L⁻³-scaling property. I predict that if one computes `(r_g, anec)` at `L_max ∈ {8,10,12}`, the residual to {1,1} will NOT scale as L⁻³ (it will track the sign-balance floor / numerical precision, plausibly L-flat or non-monotone), whereas the genuine magnitude residual `‖M_L − g_M‖` WILL scale as L⁻³. That divergence in scaling-behavior between the two candidate anchors is the empirical fingerprint that they are different observables — and it is exactly what the multiplicative-normalization fingerprint of `math-scripts.md §"Multiplicative-normalization cancellation invariants"` (the `L_max`-FLAT vs `L_max`-FLOWING 2-bit signature, K=3 MANDATORY) would register. CF-S107 should compute BOTH and report the scaling of each.

**Bearing on the registry-PASS reassurance.** The 1.33e5× margin is the single most rhetorically reassuring number in the §VII.CB entry ("5.12 OOM inside the envelope; Sage-confirmed"). Under Reading B it is the *least* trustworthy number in the entry: it is large for the wrong reason. This is why the margin must be explicitly re-annotated by mack at R3 — not deleted (it is a true ratio of two true numbers), but re-scoped as "a ratio of a sign-saturation residual to a magnitude envelope; non-load-bearing; large because the two quantities are different observables, NOT because the magnitude converged 1.33e5× early."

**Question for transit:** do you concur that the sign-residual's *L_max-scaling* (FLAT/floor) vs the magnitude-residual's *L_max-scaling* (∝ L⁻³) is the clean empirical discriminator, and is the type-IV EMT compute set up to produce `(r_g, anec)` at L_max ∈ {8,12} (not just the cached L_max=10 point) so we can exhibit the FLAT signature and seal the artifact reading?

### VDD5: Cross-Cutting Observations (incl. the W9-5 / §VII.AX / §VII.AF.1 adversarial-history bearing)

**KEY FINDING: the framework's OWN adversarial history on this exact envelope class — W9-5 (universal-envelope FALSIFIED-at-K=2), §VII.AX (Tier-1/Tier-2 held-pending), §VII.AF.1/§VII.AU (the analytic L⁻³ is theorem-grade ONLY at the leading layer; the finite-L empirical rate drifts to 2.6926 below the decisive L≥35 window) — converges on a single conclusion: the "binding ⇒ REGISTRY-PASS, margin 1.33e5×" reassurance is over-stated at exactly the layers the framework has already shown to be fragile. The binding determination (3b) and the analytic leading rate (3d) are sound and survive; what does NOT follow from them is a clean REGISTRY-PASS on a sign-saturation Level-3 anchor.**

I separate three independent bearings, because they constrain three different things.

**(1) §VII.AF.1 / §VII.AU.OP-PROJ — the analytic-leading-vs-finite-L distinction (the most directly load-bearing precedent).** Registry lines 14942–14948 and 19191–19202 document the mechanism that my own 3b/3d gates carefully scoped: the L⁻³ exponent is theorem-grade **ONLY at the BARE/leading-term layer** (CM-1995 §III.4 simple-pole), and the **subleading `C_1` is OBSERVABLE-SPECIFIC** — negative at §VII.AF.1.OP-PROJ (→ 10× over-performance), positive at §VII.AU.OP-PROJ (→ the finite-L sample `α = 2.6926`, *under*-performance). The decisive Friedrich–Bär sub-window is `L_max ≥ 35`; the GT-builder maxes ~`L=14` (irrep wall `p+q ≥ 13`). This does NOT contradict 3d's NARROW claim — 3d derived the analytic LEADING rate `α = d−1 = 3` from §VII.AG.1's own bridge map, correctly scoped, and that IS theorem-grade (Paper 01 fibre-integration image; the codim-1 base-dimension residual). But it sharpens what "binding L⁻³" buys §VII.CB: it buys the *leading-term* rate, NOT a guarantee that the finite-L `L_max=10` residual sits below `1.0e-03` — because the subleading `C_1` for the type-IV EMT bridge is *uncomputed*, and §VII.AU.OP-PROJ proves a sibling on the SAME `(d=4, s=3)` structure can have positive `C_1` and *under*-perform at finite L. The §VII.CB entry's confident `Level-3 (7.5e-9) < Level-2 (1.0e-3)` reads as if the analytic envelope guarantees the finite-L bound. It does not; §VII.AU is the counterexample. (And §VII.CB sidesteps this entirely by anchoring on the sign channel instead of the magnitude channel — so the entry never even tests the finite-L magnitude residual that §VII.AU shows can under-perform.)

**(2) §VII.AX (substrate-distance-2 pole s=4) — SCHEMATIC α=3 vs FULL-PV α=6.26.** Registry line 18808: the §VII.AX/§VII.AV sibling found that FULL Pauli–Villars gives `α_PV = 6.263789 ± inf`, structurally DISTINCT from the SCHEMATIC `α=3` — "the SCHEMATIC Casimir-bound proxy does NOT faithfully predict the FULL physical Pauli-Villars regularization." Bearing on §VII.CB: §VII.CB's envelope is FULL (NO SCHEMATIC helper; analytic shell-sum on the FULL canonical `a_2_FW_zeta`), so §VII.CB is NOT exposed to the SCHEMATIC-vs-FULL drift that broke §VII.AV. This is a point in §VII.CB's *favor* and I credit it: the §VII.CB envelope is more robustly grounded than the §VII.AV proxy. BUT the §VII.AX episode teaches the meta-lesson that matters here: a regulator/layer the framework has not actually probed can flip the answer by 2× in the exponent. The §VII.CB binding determination is at the analytic-leading layer; the framework has a documented track record of the analytic-leading layer NOT surviving to the FULL-physical / FB-saturated layer. That is a reason to qualify the registry-PASS, not to assert it at 1.33e5× confidence.

**(3) W9-5 (lizzi × landau, FALSIFIED-at-K=2) — the universal-envelope close, and why it is the sharpest bearing.** Registry line 18266: the S93 W9-5 adversarial workshop CLOSED the Layer-Functor F universal-envelope reading as FALSIFIED-at-K=2. The decisive evidence is exactly the kind of thing §VII.CB's margin claim leans on: under Friedrich–Bär saturation at the `L→∞` layer where Level-1 lives, `σ_β` **GREW** from 0.8936 to **1.065** (β spread 1.354/2.092/3.428/1.029) — *contradicting* the `σ_β → 0` universal-envelope prediction *at the exact layer it was placed*. The structural moral, in my own words: **a "binding / universal" reassurance that has only been verified at the leading-term / finite-cache layer is NOT validated at the asymptotic / FB-saturated layer, and the framework has already caught itself once asserting otherwise.** §VII.CB's "binding ⇒ REGISTRY-PASS, 5.12 OOM inside" is structurally the same shape of over-reach: a binding-eligibility determination at the analytic layer is being read as a validated convergence at the canonical-truncation layer, on an anchor (sign-saturation) that was never on the convergence curve to begin with. W9-5 is the framework's standing warning against exactly this inference.

**The unifying structural point (HKR / Kasparov axis, first principles).** The binding test (corpus §1 Step-3) is a statement about the **Element-4 envelope and its `c_continuum`** — and it is a *cohomology-class / K-homology* statement: does the L⁻³ rate bound the convergence of the substrate-IS pairing to the HKR-image of the *same K-homology class*? For §VII.CB, YES — `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}` is one a₂ Seeley–DeWitt class read through the shriek map `[D_M] = π_! ⊗ [D_B]` (Paper 01 §3). This is genuinely Level-1 / topology, and it is sound. But topology (the class identity + its convergence rate) is NOT analysis (the numerical value of the finite-L residual). My standing boundary (memory: "Kasparov product gives TOPOLOGY … NOT ANALYSIS") applies with full force: 3b established the *topological* binding-eligibility of the envelope; the registry-PASS criterion is an *analytic* claim about a finite-L number; and the number supplied (sign-saturation residual) is not even the analytic image of the bound class. The §VII.CB entry reads as if topological binding-eligibility discharges the analytic registry-PASS. It does not. This is the same topology→analysis over-reach the framework keeps re-learning (W9-5, §VII.AX, §VII.AU).

**What survives all three adversarial precedents (the honest positive).** The binding determination (3b, audit `943b17ad…`) and the direct analytic re-derivation (3d, audit `645ac895…`) are theorem-grade on the LEADING rate and survive every one of these precedents — none of W9-5 / §VII.AX / §VII.AU touches the analytic codim-1 base-dimension exponent `d−1=3` derived from the HKR boundary map. The Level-1 cohomology-class identity survives. The 5-anatomy survives. What does NOT survive is the registry-PASS *as a clean satisfied inequality*: the supplied Level-3 anchor is the wrong observable, the margin is an artifact, and the framework's own history says the "binding ⇒ PASS" inference is fragile at exactly the layers §VII.CB never probed.

**Cross-cutting registry-hygiene note (for the R3 mack patch).** §VII.CB currently sits at line 164 (master row) + line 21968 (section), STAGE-3-PERMANENT, REGISTRY-PASS. Under Reading B the patch is the §VII.AX surgical-demotion pattern (corpus §25.1 "5 surgical Level-3-row demotions … theorem-structure STAGE-3-PERMANENT tags … left untouched"): hold the Level-3 row, re-tag the registry-PASS to STAGE-3-PERMANENT-STRUCTURE/Level-3-HELD, retain the sign-residual as a separate Element-1 witness, and point the hold at CF-S107. This is a NEW pinned position (verdict permanence: the `293105a2…` line stays on disk), effected by mack as sole writer at R3 — exactly the disposition §VII.AX establishes precedent for.

**Open question I hand to R2 (after transit's T1/T2).** The entire Reading-A-vs-B fork reduces to one constructibility fact transit owns: is the magnitude-convergence anchor `‖HKR(Tr_{M₂}(P_a₂·T^{(IV)})_L) − g_M‖` *constructible at finite L* (Tier-1-eligible — then §VII.CB is held-pending-CF, discharge-eligible), or does the type-IV compute only ever yield sign-structure invariants and no scalar magnitude image of `g_M` (then the bridge's Level-1 is a pure class-identity with no scalar Level-3 anchor, and the disposition shifts toward Tier-2 re-anchoring of the dimensionless content, §VII.AX-permanent-hold style)? My VDD-side prior: the magnitude IS constructible (the binding test presupposes a scalar `M(L) → g_M`), so I expect Tier-1-eligible-held-pending-CF. I will converge with transit at R2 on whether the type-IV side bears this out.

---

## Round 1 — transit: Response & Cross-Synthesis

### Part 1: Response to van-den-dungen's Sections

#### Re: VDD1 — same vs different observable

**AGREE — decisively, and I can close the question from the substrate side that VDD1 left as a conditional.** VDD1's core claim is correct: the §VII.CB Level-3 anchor `max(|r_g−1|, |anec−1|) = 7.500e-09` is a DIFFERENT observable from the one the binding `L⁻³` envelope bounds (`‖HKR(M_L) − g_M‖`). I own the type-IV EMT axis, and I have now read the producing artifact (`computations/session-105/s105_typeiv_emt_compute.npz` + `.py`). The substrate physics is unambiguous and it removes the conditional VDD1 left open ("*forced to its anchors* vs *co-convergent proxy*"). The answer is **fixed-anchor saturation, NOT co-convergent proxy** — and it is forced for a reason cleaner than VDD1 anticipated.

**The supporting evidence from my axis (the type-IV compute, read off the script + npz).** The type-IV EMT is built (s105 script §3, `build_relay_flow`) as a **continuum acoustic-profile compute on a radial grid**, NOT a spectral-triple-truncation compute:
- The flow is `v(r) = v₀·exp(−(r/r₀)²/2)` on a 512-point spatial grid `r ∈ [0.001, 5.0]` (relay-Compton-radius units), with the type-IV discriminant `Γ_sub(r) = c_s² − v(r)²` (= the a₂-channel acoustic `g_tt`).
- `g_core = Γ_sub(r→R_MIN) = c_s²(1 − Mach_core²) = −0.4041822 < 0` (type-IV ANEC-violating core); `g_ext = Γ_sub(r→R_MAX) = c_s² = +0.2352250 > 0` (type-I tail). These are the discriminant at the two ENDS of a radial profile — **spatial endpoints, not finite-L truncations.**
- The two "Level-3" invariants are pinned to their integer anchors by **analytic identities of the Gaussian profile**, NOT by any convergence in L_max:
  - `Mach_core = exp(1/2) = 1.6487212707…` is *substrate-derived analytically* (s105 line 239: `np.exp(0.5)`). This FORCES `r_g = r₀·sqrt(2 ln Mach_core) = r₀ = 1.0` *as a closed-form consequence* (s105 line 228). So `|r_g − 1| = 2.193e-10` is the **bisection-root residual against the analytic value r₀=1** (TOL_ROOT=1e-8) — a numerical-floor artifact of the root-finder, not a convergence-to-g_M residual.
  - `anec = 1.0000000075` is the numerical realization of the EXACT analytic identity `A = 2J` (npz: `A0=1.0, J0=0.5, a_eq_2j=True`; s105 line 326 `np.allclose(A_t, 2J_t)`). `anec − 1 = 7.500e-09` sits at ~7.5× TOL_ANEC (1e-9) — it is the **floor of the ANEC-integral quadrature against an exact-saturated identity**, not a convergent residual.

**The continuum image g_M is NEVER computed in the type-IV channel — confirmed by direct inspection.** I checked the npz for any stored scalar approaching `g_M = a_2_FW_zeta = 2776.165389`: there is none. The only stored value near 2776 IS `a_2_FW_zeta` itself, carried as a SEPARATE imported constant (the sound-speed normalization `c_BLV = c_s`), never as a computed `Tr_{M₂}(P_a₂·T^{(IV)})` limiting to it. The magnitude channel `M(L) → g_M` that VDD1 names as the bound observable is **structurally absent from the type-IV compute**. So VDD1's decomposition into `M(L)` (magnitude → g_M, the bound observable) and `S(L) = (r_g, anec)` (sign-saturation → {1,1}, the supplied anchor) is not just a logical distinction — only `S` exists in the artifact; `M` was never built.

**MISSED (what my axis adds beyond VDD1's HKR/Kasparov reading).** VDD1 correctly diagnosed the category mismatch from the envelope side, but the type-IV physics makes the orthogonality SHARPER than "S is pinned to {1,1} by the construction." The two invariants are pinned by **two DISTINCT analytic mechanisms, neither of which has any L⁻³ scaling character**:
- `r_g → 1` is an **acoustic-geometry identity** (the restoration surface lands at exactly 1 Compton wavelength *because* `Mach_core = √e` — the lower edge of the Dumitru-Noronha 1–2 Compton gravitational-radius band). This is a property of *where the supersonic-to-subsonic crossover sits in space*, set by the substrate-derived core Mach. It cannot scale as L⁻³ because it is not a truncation residual of anything — it is a root of `Mach(r)=1` on a fixed continuum profile.
- `anec → 1` is an **angular-momentum / GFF identity** (`a = 2J`, the spin-orbit balance of the J-sourced energy-flux profile, Dumitru-Noronha 2505.09720). This is a *conservation-law saturation*, pinned to 1 by the construction's `A=2J` structure.

These are GEOMETRIC observables in the `substrate-first-canonical-sourcing.md §(iv-bis)` sense — *mechanically locked* dimensionless ratios — and §(iv-bis) is explicit that such a ratio's PASS/FAIL is uninformative on a cohomology-class convergence it stands in for. VDD1 cited §(iv-bis) as a hazard warning; my axis confirms the hazard is *realized*: `r_g` and `anec` are mechanically locked by acoustic geometry (`Mach=√e ⇒ r_g=1`) and a conservation law (`A=2J ⇒ anec=1`), neither of which is the HKR-image convergence of the a₂ magnitude.

**EMERGES (combining the bridge-envelope and type-IV-EMT views).** Putting VDD1's envelope analysis together with the type-IV substrate physics yields a precise structural statement: **the type-IV EMT is a SIGN-STRUCTURE compute, and a sign-structure compute can only ever produce a sign-structure Level-3 witness.** The bridge's Level-1 identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}` is a *cohomology-class* statement (sound, per VDD1) — but the type-IV channel realizes only the SIGN content of that class (the core/exterior topology: g_core<0, g_ext>0, one crossover), not its MAGNITUDE content (the scalar value 2776.17). The Level-3 anchor witnesses the sign content cleanly to 8 digits; it says NOTHING about whether the magnitude has converged to g_M. This is exactly VDD1's Reading B, now grounded: the supplied Level-3 anchor witnesses the wrong half of the Level-1 class (sign, not magnitude). The defect is at Element 5, not Element 4 — the envelope IS binding (I concur with 3b/3d), but it bounds a magnitude channel the type-IV compute never instantiated.

#### Re: VDD2 — Tier-1/Tier-2 gate

**AGREE on the gate verdict (neither Tier-1 nor Tier-2 as currently anchored), but I MUST CORRECT VDD2's optimism on the Tier-1-eligibility sub-claim — and the correction is the pivotal finding of my axis.** VDD2 concludes §VII.CB is "Tier-1-eligible-but-not-yet-anchored" (a constructible magnitude residual exists, just not computed), distinguishing it favorably from §VII.AX's permanent Tier-2-dimensionful hold. VDD2 explicitly flagged this as conditional on transit's constructibility verdict ("is `M(L)` a single number per L … or does the type-IV compute only ever produce the sign-structure invariants?"). **I answer from the substrate physics: as currently constructed, the type-IV compute produces ONLY sign-structure invariants — there is no `M(L) = Tr_{M₂}(P_a₂·T^{(IV)})|_L` scalar that limits to g_M anywhere in the pipeline.** This does NOT make the Tier-1 anchor non-constructible *in principle* (see T2), but it means §VII.CB is closer to the §VII.AX hold than VDD2's "favorable distinction" suggests — the magnitude channel is not "computed-but-anchored-elsewhere," it is **unbuilt**, and building it requires a genuinely DIFFERENT compute, not an L_max scan of the existing one.

**Walking the gate with the type-IV physics in hand (supporting and sharpening VDD2).**

**Tier-1 (residual-to-g_M shrinks with L_max) — FAILS, and for a stronger reason than VDD2 gave.** VDD2 said Tier-1 is vacuously not-satisfied because no `‖HKR(c_L) − g_M‖` quantity is tracked and no L_max scan exists. Both true, but the substrate physics adds the decisive point: **the type-IV compute has NO L_max axis at all.** It is a continuum acoustic-profile compute on a 512-point radial grid; `L_max=10` enters ONLY through `a_2_FW_zeta` (the sound-speed normalization c_BLV), as a single imported constant. The output invariants (sign, r_g, anec) are analytic structures of the Gaussian profile `v(r)=v₀exp(−r²/2r₀²)` and would be **identical at any L_max** (the only L-dependence is the value of c_BLV, which rescales c_s² uniformly and cancels in the dimensionless ratios `r_g` and `anec`). So there is not merely "no L_max scan run" — there is no L_max-varying quantity in the channel to scan. The genuine Tier-1 occupant §VII.AV.STATE-PROJ (corpus §25.1: `L_emp = −7.046336 M_KK²`, residual 2.82e-4 shrinking, Connes-Karoubi-bound) has a quantity that demonstrably converges; §VII.CB's sign channel has none.

**Tier-2 (divergent channel re-anchored to a dimensionless invariant) — FAILS, and I confirm VDD2's two reasons with the physics.**
1. *Not divergent.* `r_g` and `anec` are O(1), pinned to 1 by analytic identities (`Mach_core=√e ⇒ r_g=1`; `A=2J ⇒ anec=1`). Tier-2 is the escape hatch for a *divergent dimensionful* channel (e.g. §VII.AX's `n_PBH ~ L⁵ → ∞`, corpus §25.1); there is no divergence here to re-anchor away from. The type-IV invariants are already dimensionless AND already saturated.
2. *Channel-swap, not re-anchor.* This is the heart of it. Tier-2 re-anchors the SAME divergent observable to its own dimensionless invariant (§VII.AX re-anchors `n_PBH`'s m⁻³ magnitude to its cascade exponent `d ln N_eigs/d ln L`). §VII.CB instead **substitutes a different observable** — the sign channel `S(L)` for the magnitude channel `M(L)`. The type-IV compute confirms this is a swap, not a re-anchor: `(r_g, anec)` are not a dimensionless invariant *extracted from `M`* (the magnitude was never computed); they are invariants of a *different functional* (the acoustic discriminant's spatial sign-topology + the J-GFF conservation law). VDD2's "channel-swapping" diagnosis is exactly right, and the npz proves it: `M` and `S` share no expansion because `M` is absent.

**DISAGREE (mild, scoping) with VDD2's "less severe than §VII.AX" framing.** VDD2 argues §VII.CB is *less* defective than §VII.AX because §VII.AX's `n_PBH` is genuinely divergent (un-anchorable from inside) while §VII.CB's magnitude is convergent (Tier-1-eligible-in-principle). I half-agree: yes, the a₂ MAGNITUDE channel is convergent (the binding determination presupposes `M(L) → g_M` at rate L⁻³, and there is no obstruction to a scalar a₂-channel pairing existing). BUT the severity comparison cuts the other way on one axis: §VII.AX at least computed its `n_PBH` channel (the held quantity is the RIGHT observable, just dimensionful-un-anchorable); §VII.CB's Level-3 anchor is the WRONG observable entirely (a sign channel standing in for a magnitude channel). §VII.AX held a real-but-un-anchorable number; §VII.CB published a number from a different channel and called it the bridge anchor. On the "is the supplied anchor even the bound observable?" axis, §VII.CB is *more* defective, not less. The favorable distinction VDD2 draws (constructibility) is real and matters for the DISPOSITION (held-pending-CF vs permanent-hold), but it should not soften the diagnosis that the *currently supplied* anchor is category-mismatched.

**MISSED (the constructibility nuance that decides Tier-1-eligible vs Tier-2-hold).** VDD2's prior is "magnitude IS constructible ⇒ Tier-1-eligible-held-pending-CF." My axis refines this to a TWO-PART answer that VDD2 could not give without the type-IV internals (full derivation in T2):
- The **scalar a₂ magnitude** `M(L) = Tr_{M₂}(P_a₂ · T^{(IV)})|_L` IS constructible in principle — it is a well-defined finite-L spectral-triple trace, and `g_M = a_2_FW_zeta` is its `L→∞` image. So the bridge's Level-1 is NOT a pure class-identity-with-no-scalar-image (the worst case VDD2 feared); a Tier-1 anchor CAN exist.
- BUT it is NOT extractable from the EXISTING type-IV artifact. The s105 compute is a continuum acoustic-discriminant calculation; the scalar `Tr_{M₂}(P_a₂·T^{(IV)})` requires (i) representing T^{(IV)} as an operator on the M₂(ℂ)-doublet over the finite-L spectral triple, (ii) projecting with P_a₂, (iii) tracing, (iv) at L ∈ {8,10,12}. That is the CF-S107 recompute — a NEW compute on the spectral triple, NOT a re-read or L-scan of the radial-profile data.

So §VII.CB is **Tier-1-eligible-in-principle but held-pending-a-NEW-compute** — which lands it at VDD2's disposition (held, discharge-eligible by CF-S107) but for a reason that strengthens, not weakens, the case for holding NOW: the existing anchor is not a degraded proxy of the bound quantity, it is a different quantity, and the bound quantity is uncomputed.

#### Re: VDD3 — registry status if Reading B wins

**AGREE — the §VII.AX two-part split (structure STAGE-3-PERMANENT / Level-3 row HELD) is the correct disposition, NOT a blanket STAGE-1-CANDIDATE down-tag.** VDD3's four-point patch instruction to mack is right and I endorse it: (1) hold the Level-3 row, (2) re-tag REGISTRY-PASS → STAGE-3-PERMANENT-STRUCTURE / Level-3-HELD, (3) retain the sign-residual as a separate Element-1 witness, (4) point the hold at CF-S107. I add the substrate-physics justification for each and ONE sharpening of the held-tag wording.

**Why the structure survives (concurring with VDD3, from my axis).** The non-Level-3 content of §VII.CB is sound on the type-IV side too:
- *The Level-1 cohomology-class identity is real.* `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}` — the type-IV core EMT and the continuum a₂-emergent metric ARE the same a₂ Seeley-DeWitt curvature-degree-2 class. The type-IV compute confirms the SIGN content of this class (g_core<0 ANEC-violating core, g_ext>0 type-I exterior, one crossover at r_g=1) is cleanly realized. The class identity is not in doubt; only its MAGNITUDE-side numerical anchor is missing.
- *The substrate-IS observable (Element 1) is a genuine finite-L type-IV EMT.* It is the a₂-channel acoustic stress-energy of the substrate's own supersonic transit at the fold (the localized-relay analog of the proton's J-sourced T^{0i} energy-flux profile, Dumitru-Noronha 2505.09720). This is a real PHONONIC substrate observable — not the defect.
- *Element 4 is theorem-grade.* I concur with 3b/3d (and VDD3): `α = d−1 = 3` is the binding HKR base-dimension rate, and the binding test (`c_continuum = g_M` IS the HKR-image) passes. Nothing in Reading B touches the envelope.

So the failure is at the *empirical-confirmation predicate* (`Level-3 < Level-2` as a binding-channel-consistent statement), precisely the §VII.AX situation (corpus §25.1: "theorem-STRUCTURE STAGE-3-PERMANENT … Level-3 row HELD `NOT-SATISFIED-PENDING-…`").

**SHARPENING of the held-tag wording (the one place I refine VDD3).** VDD3 proposes the hold tag `NOT-SATISFIED-PENDING-magnitude-convergence-anchor`. I endorse it but want the tag to encode the *type-IV-specific* reason, because a future reader must not mistake this for the §VII.AX *dimensionful-divergent* hold or the §VII.AV *SCHEMATIC-proxy* hold. The precise structural cause is: **the supplied Level-3 anchor is a SIGN-STRUCTURE saturation residual on a channel ORTHOGONAL to the bound magnitude channel; the magnitude channel `Tr_{M₂}(P_a₂·T^{(IV)}) → g_M` is Tier-1-eligible-in-principle but UNCOMPUTED (a NEW compute, not an L-scan of the existing radial-profile artifact).** My recommended tag text for mack: `Level-3-HELD: NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR (supplied anchor is sign-structure saturation, channel-orthogonal to the bound a₂-magnitude; Tier-1-eligible-in-principle, discharge = CF-S107 NEW Tr_{M₂}(P_a₂·T^{(IV)})→g_M compute)`. This distinguishes §VII.CB cleanly from §VII.AX (dimensionful-divergent, permanent hold) and §VII.AV (SCHEMATIC-proxy envelope; §VII.CB's envelope is FULL, NO CLASS pin) — three different held-reasons, three different tags.

**AGREE this is §VII.AX and NOT §VII.AV (VDD3 is exactly right, and the type-IV side confirms it).** §VII.AV's defect is in the ENVELOPE's physical realization (SCHEMATIC Casimir-proxy; FULL-PV gave α_PV=6.26, registry line 18808). §VII.CB's envelope is FULL (analytic shell-sum on the FULL canonical a_2_FW_zeta; NO SCHEMATIC helper; the npz confirms `regulator_pin=a_2^{zeta}`, no CLASS pin). The §VII.CB defect is NOT envelope-realization — it is that Element 5 witnesses the wrong channel. That is the §VII.AX Tier-1/Tier-2 gate problem, not the §VII.AV proxy problem. Filing §VII.CB as §VII.AV-style deferred-pending would mis-locate the defect, as VDD3 says.

**On the Reading-A defense VDD3 must answer — I CLOSE it against Reading A.** VDD3 left Reading A's strongest form alive conditionally: "Reading A survives ONLY if transit establishes `S(L) → {1,1}` is co-convergent with `M(L) → g_M` at the same L⁻³ rate." From the type-IV physics (Re:VDD1, T1): **`S(L) → {1,1}` is NOT co-convergent with `M(L) → g_M`; it is fixed-anchor saturation by analytic identities (`Mach_core=√e ⇒ r_g=1`; `A=2J ⇒ anec=1`), with NO L⁻³ character and indeed no L-dependence at all.** So Reading A's co-convergent-proxy escape FAILS. The sign-residual is not a degraded-but-real proxy for `‖HKR(M_L) − g_M‖`; it is a clean witness of a different (orthogonal) quantity. Reading B wins as stated — with the §VII.AX structure-permanent/Level-3-held disposition, exactly as VDD3 frames it.

**Verdict-permanence scoping (concurring).** Per `epistemic-discipline.md`, the on-disk `S106-W3-3` verdict (audit `293105a2…`, REGISTRY-PASS) is PERMANENT and is NOT edited. The workshop disposition is a NEW pinned position — a mack-cosmic-bridge sole-writer registry annotation on the §VII.CB entry, landed at R3. This is the §VII.AX surgical-demotion pattern (corpus §25.1: "Level-3-row demotions … theorem-structure STAGE-3-PERMANENT tags … left untouched"), not a retroactive edit.

#### Re: VDD4 — margin-claim meaningfulness

**AGREE — the 1.333e5× margin is an artifact, and I can make VDD4's L_max-scaling diagnostic SHARPER and PREDICT its outcome from the substrate physics.** VDD4's core point is correct: the margin is large precisely BECAUSE the Level-3 residual measures a different, already-saturated quantity (sign-balance → {1,1}, at floor) than the envelope bounds (magnitude → g_M). It is the textbook `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"` non-load-bearing-margin artifact (corpus §20). I confirm and extend.

**VDD4 asked me directly: is the L_max-scaling FLAT-vs-FLOWING the clean discriminator, and is the type-IV compute set up to produce `(r_g, anec)` at L_max ∈ {8,12}? Two-part answer.**

**(1) YES, the L_max-scaling is the clean discriminator — and I can PREDICT the sign-residual's scaling without running it, because it has no L_max dependence by construction.** VDD4 invokes the `math-scripts.md §"Multiplicative-normalization cancellation invariants"` 2-bit FLAT-vs-FLOWING fingerprint (K=3 MANDATORY). I confirm the diagnostic applies and sharpen the prediction:
- *The magnitude residual `‖M_L − g_M‖`* (if computed per CF-S107) WILL scale as `L⁻³` — it is the codim-1 outermost-shell residual of the d=4 base integral (3b/3d Leg-B), `L_max`-FLOWING to g_M.
- *The sign residual `‖S(L) − {1,1}‖`* will be `L_max`-FLAT — and not merely "plausibly flat" (VDD4's hedge) but **provably flat by the analytic structure of the type-IV compute.** Here is the substitution chain: `r_g` is the root of `Mach(r)=1` on the FIXED profile `v(r)=v₀exp(−r²/2r₀²)`, with `Mach_core=√e` substrate-derived (s105 line 239) ⇒ `r_g = r₀√(2 ln Mach_core) = r₀·√(2·½) = r₀ = 1` EXACTLY, independent of L_max. `anec` is the realization of `A=2J` (s105 line 326), also L_max-independent. The ONLY L_max entry in the entire channel is `c_BLV = c_s`, which rescales `Γ_sub = c_s²(1−Mach²)` UNIFORMLY — and `r_g` (a Mach=1 root) and `anec` (a dimensionless conservation ratio) are BOTH invariant under uniform rescaling of c_s². So `‖S(L) − {1,1}‖` is set by the bisection-root tolerance (TOL_ROOT=1e-8) and the ANEC-quadrature floor (TOL_ANEC=1e-9) — pure numerical-floor properties — at EVERY L_max. The residual is `L_max`-FLAT at the numerical floor, FULL STOP. This IS the multiplicative-normalization cancellation fingerprint (`math-scripts.md`): `S` factors as `w(L_max)·g(geometry)` where `w(L_max)` (via c_BLV) cancels in the dimensionless `r_g`/`anec`, leaving an L_max-INVARIANT geometric kernel. The FLAT signature is a STRUCTURAL identity, not an empirical observation.

**(2) The existing compute does NOT produce `(r_g, anec)` at L_max ∈ {8,12} — but it does not NEED to, because the FLAT signature is analytic.** The s105 compute fixes L_max=10 only through c_BLV; re-running at L_max ∈ {8,12} would change c_BLV (hence c_s²) but leave `r_g=1` and `anec=1` analytically unchanged (uniform-rescaling invariance, above). So the FLAT signature can be ESTABLISHED ANALYTICALLY — the empirical L-scan of the sign channel is a confirmation, not a derivation. What CF-S107 must compute is the OTHER curve: the magnitude residual `‖M_L − g_M‖` at L ∈ {8,10,12}, to exhibit its L⁻³ FLOWING signature. The contrast — sign-residual FLAT (analytic) vs magnitude-residual FLOWING-as-L⁻³ (CF-S107) — is exactly VDD4's empirical fingerprint that they are different observables, and it seals the artifact reading.

**The arithmetic of the artifact (concurring with VDD4, restated in type-IV terms).** `margin = Level-2/Level-3 = 1.0e-3 / 7.5e-9 = 1.333e5×`. The denominator `7.5e-9` is `anec − 1` = the ANEC-quadrature floor against the EXACT identity `A=2J`. It is `~7.5×` the quadrature tolerance (1e-9) — i.e. the margin's size is set by *how tightly the ANEC integral resolves an exactly-saturated conservation law*, which is a numerical-precision property of the type-IV quadrature, NOT a convergence depth of the a₂ magnitude. Dividing the L⁻³ envelope (a magnitude-convergence bound) by the ANEC-quadrature floor (a sign-saturation precision) is dimensionally-comparable (both dimensionless) but ontologically meaningless — apples-to-oranges, as VDD4 says. The 5.12-OOM size is a SYMPTOM of the category mismatch, not evidence of convergence quality.

**CONTRAST with the genuine §VII.AF.1 10× margin (the calibrated reference — I confirm VDD4 against the registry text).** I read §VII.AF.1.OP-PROJ (registry lines 14942–14948). Its margin (10× inside) IS meaningful because its Level-3 (`err_STRICT = 0.0095%`) IS the convergence residual of the bound quantity (the atlas-match → BZ-trace `R_geom`), and the S91 W0 annotation (line 14944) EXPLAINS the 10× structurally: a **negative subleading C_1** in the CM-1995 §III.4 expansion `L⁻³·(C_0 + C_1·L⁻¹ + …)` puts the finite-L value below the asymptotic envelope. §VII.CB's 1.33e5× has NO such structural explanation available, because its denominator is not on the convergence curve — there is no "C_1 of the sign-saturation residual relative to the magnitude envelope"; the two quantities share no expansion (the magnitude was never computed). VDD4's contrast is exactly right and the registry text bears it out.

**MISSED / EMERGES (the type-IV-specific reason the margin is the LEAST trustworthy number).** Combining VDD4's annotation-discipline analysis with the type-IV physics: the `7.5e-9` is the most *physically determinate* number in the entry (it is a clean realization of the `A=2J` GFF identity to 8 digits — a real, beautiful substrate result) AND the *least relevant* number for the registry-PASS criterion (it is the precision of a sign-saturation, not a magnitude-convergence depth). The danger is that its determinacy READS as relevance. mack must re-annotate it (R3) as: "a ratio of the ANEC-quadrature floor (sign-saturation precision against the exact `A=2J` identity) to the magnitude envelope; non-load-bearing per corpus §20; large because the two quantities are different observables, NOT because the a₂ magnitude converged 1.33e5× early." Keep the number (it is a true ratio of two true numbers); re-scope it.

#### Re: VDD5 — adversarial-history bearing

**AGREE on the unifying conclusion (the "binding ⇒ REGISTRY-PASS, 1.33e5× inside" reassurance is over-stated at exactly the layers the framework has shown fragile) and on the topology→analysis over-reach as the structural root.** VDD5's three bearings (W9-5 universal-envelope FALSIFIED-at-K=2; §VII.AX Tier-1/Tier-2 held-pending; §VII.AF.1/§VII.AU analytic-leading-vs-finite-L) converge correctly. I respond from the transit axis, where the topology→analysis distinction has a specific sharpening, and I add ONE bearing VDD5 did not name that strengthens the case.

**On VDD5's unifying point ("topology ≠ analysis"; the Kasparov-product gives the class identity, not the finite-L number) — I concur and ground it in the type-IV physics.** VDD5's standing boundary ("Kasparov product gives TOPOLOGY … NOT ANALYSIS") is exactly the right frame, and the type-IV compute is a clean instance: the bridge's Level-1 (`[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`) is a K-homology class identity (topology), and the type-IV compute realizes the SIGN content of that class (the core/exterior topology: g_core<0, g_ext>0, one crossover) — which IS topological data, cleanly. But the registry-PASS criterion is an analytic claim about a finite-L NUMBER (`‖M_L − g_M‖ < 1e-3`), and the type-IV compute never produced that number. So the §VII.CB entry reads as if topological binding-eligibility (3b: the envelope binds the class's HKR-image) discharges the analytic registry-PASS (Level-3 < Level-2 on the bound magnitude). It does not — and the type-IV physics shows the gap concretely: the channel that carries the topology (sign-structure) is ORTHOGONAL to the channel that carries the analysis (magnitude → g_M), and only the former was computed.

**(1) §VII.AF.1/§VII.AU — I confirm VDD5's most load-bearing precedent, and it bites §VII.CB harder than VDD5 stated.** Registry lines 14942–14948: L⁻³ is theorem-grade ONLY at the leading-term layer; the subleading C_1 is OBSERVABLE-SPECIFIC (negative at §VII.AF.1 → 10× over-performance; positive at §VII.AU → α_finite=2.6926 UNDER-performance, registry line 14948), and the decisive Friedrich-Bär window is L≥35 (GT-builder maxes ~L=14). VDD5 notes §VII.CB "sidesteps this entirely by anchoring on the sign channel." I sharpen: this sidestep is the WORST feature, not a neutral one. §VII.AU PROVES a sibling on the SAME (d=4, s=3) structure can have positive C_1 and *under*-perform the finite-L bound. The honest §VII.CB question is "does `‖M_L − g_M‖` at L_max=10 sit below 1e-3?" — and §VII.AU shows the answer is NOT guaranteed by the analytic α=3. By anchoring on the sign channel (which is at floor for unrelated reasons), §VII.CB never even TESTS the magnitude residual that §VII.AU shows can under-perform. The sign-anchor is not just a wrong observable — it is a wrong observable that *masks the exact finite-L question the framework has a counterexample for*. CF-S107 must compute `‖M_L − g_M‖` AND its C_1 sign to settle whether §VII.CB over- or under-performs (the §VII.AF.1-vs-§VII.AU fork).

**(2) §VII.AX (SCHEMATIC α=3 vs FULL-PV α=6.26) — I credit VDD5's point in §VII.CB's favor, with the transit caveat.** Registry line 18808: FULL Pauli-Villars gave α_PV=6.26, structurally distinct from SCHEMATIC α=3. VDD5 correctly credits that §VII.CB's envelope is FULL (no SCHEMATIC helper; the npz confirms `regulator_pin=a_2^{zeta}`, no CLASS pin) — so §VII.CB is NOT exposed to the SCHEMATIC-vs-FULL drift that broke §VII.AV. I concur this is a genuine point in §VII.CB's favor. The transit caveat: the §VII.AX episode's META-lesson (a regulator/layer the framework has not probed can flip the exponent by 2×) applies to the *magnitude residual's finite-L behavior*, which §VII.CB never computed — so the favorable "FULL envelope" point covers Element 4 (the analytic rate) but says nothing about the finite-L magnitude anchor (Element 5), which is the actual defect.

**(3) W9-5 (FALSIFIED-at-K=2) — the sharpest bearing, and I add the transit-dynamics reading.** Registry line 18266: under Friedrich-Bär saturation at the L→∞ layer, σ_β GREW to 1.065 (β spread 1.354/2.092/3.428/1.029), contradicting the σ_β→0 universal-envelope prediction at the exact layer it was placed. VDD5's moral — a binding/universal reassurance verified only at the leading/finite-cache layer is NOT validated at the asymptotic/FB-saturated layer — is exactly right. **The transit-dynamics reading that makes this bite §VII.CB: this is the multiplicative-normalization FLAT-vs-FLOWING fingerprint (`math-scripts.md`, K=3 MANDATORY) at the meta-level.** W9-5 showed that a quantity assumed to FLOW-to-a-universal-limit instead stayed structured (σ_β grew) at the asymptotic layer. §VII.CB's sign-residual is the dual pathology: a quantity assumed to be a convergence witness (Reading A: "the sign-residual IS the L=10 image of the converging bridge") is actually FLAT (Re:VDD4: `r_g`, `anec` are L-invariant by analytic identity). W9-5 caught the framework assuming FLOW where there was structure; §VII.CB risks the framework assuming FLOW (convergence) where there is a FLAT floor (sign-saturation). Same structural error class, opposite direction — and W9-5 is the standing precedent that the framework MUST check the actual scaling, not assume it from binding-eligibility.

**The bearing VDD5 did not name (a transit-axis addition): the fold-impulsive |β|² precedent on assuming-vs-computing the finite-L residual.** My own memory carries the S100b W5-1 lesson: for the fold-impulsive window, `|β_pivot|² = 3.045e-07` is a COMPUTED Bogoliubov coefficient on a sharp-boundary transfer matrix, and the lesson was that *smooth ω_k(τ) segmentation INVALIDLY accumulates artificial reflections* — i.e., the WRONG computational route gives a structurally wrong answer that LOOKS plausible. The §VII.CB analog: reading the sign-saturation residual as the bridge's convergence anchor is the "wrong route that looks plausible" — it produces a clean 8-digit number that READS as a convergence witness but is structurally a different observable. The transit-dynamics discipline (compute the actual mode-by-mode quantity; do not assume the residual's character from the envelope) is the same discipline §VII.CB needs: compute `‖M_L − g_M‖`, do not infer it from "the envelope is binding."

**What survives all bearings (the honest positive, concurring with VDD5).** The binding determination (3b, audit `943b17ad…`) and the direct analytic re-derivation (3d, audit `645ac895…`) are theorem-grade on the LEADING rate α=d−1=3 and survive every precedent — none of W9-5/§VII.AX/§VII.AU touches the analytic codim-1 base-dimension exponent. The Level-1 cohomology-class identity survives. The 5-anatomy survives. The type-IV sign-structure result (g_core<0 ANEC-violating core, g_ext>0 type-I exterior, r_g=1, A=2J to 8 digits) is a real, clean substrate finding worth keeping as an Element-1 witness. What does NOT survive is the registry-PASS as a clean satisfied inequality: the supplied Level-3 anchor is the wrong (orthogonal) observable, the margin is an artifact, and the framework's own history (W9-5, §VII.AU) says the "binding ⇒ finite-L PASS" inference is fragile at exactly the layers §VII.CB never probed.

### Part 2: Original Analysis

#### T1: What the type-IV sign-anchor residual physically IS

**STRUCTURAL CLAIM (the observable my axis owns): the §VII.CB Level-3 anchor `max(|r_g−1|, |anec−1|) = 7.500e-09` is the joint numerical-floor residual of TWO ANALYTIC SATURATION IDENTITIES of the type-IV acoustic profile — `r_g = 1` (acoustic-geometry / restoration-surface identity) and `anec = 1` (the `A = 2J` angular-momentum-GFF conservation identity). It is a sign-STRUCTURE saturation witness, set by quadrature/root-finding tolerances against EXACTLY-SATURATED targets, with ZERO L_max-convergence content. It is NOT, and cannot be, the magnitude-convergence residual `‖HKR(M_L) − g_M‖` the binding L⁻³ envelope bounds.**

**What the type-IV EMT compute IS (substrate physics, from `computations/session-105/s105_typeiv_emt_compute.py` §3 + the npz).** The substrate IS the localized relay's internal acoustic stress-energy at the fold — the a₂-channel analog of the proton's J-sourced energy-flux GFF profile T^{0i}(r) (Dumitru-Noronha 2505.09720; s105 docstring lines 213–236). The compute:
- builds a CORE-CONCENTRATED Gaussian acoustic flow `v(r) = v₀·exp(−(r/r₀)²/2)` on a radial grid `r ∈ [0.001, 5.0]` (relay-Compton units), 512 points (npz `r`, `v_r`);
- with sound speed `c_s = c_BLV = 0.485` and core amplitude `v₀ = Mach_core·c_s`, where **`Mach_core = exp(1/2)` is substrate-derived** (s105 line 239 `np.exp(0.5)` — the MINIMAL supersonic core consistent with the restoration surface at ≥1 Compton wavelength, the lower edge of the D-N 1–2 Compton gravitational-radius band);
- forms the type-IV acoustic discriminant `Γ_sub(r) = c_s² − v(r)² = c_s²(1 − Mach(r)²)` (npz `gamma_sub`), the a₂-channel acoustic `g_tt`.

The two endpoint values are the type-IV/type-I diagnosis:
- `g_core = Γ_sub(r→0) = c_s²(1 − Mach_core²) = 0.485²(1 − e) = −0.4041822 < 0` — **type-IV ANEC-violating supersonic core** (Mach_core²=e>1);
- `g_ext = Γ_sub(r→∞) = c_s²(1 − 0) = c_s² = +0.2352250 > 0` — **type-I subsonic exterior**.

The sign flip (g_core<0, g_ext>0) with exactly one crossover at `r_g` IS the type-IV signature. This is genuine, clean PHONONIC substrate content — the acoustic white-hole structure of the relay's own supersonic core.

**The two Level-3 invariants, derived (substitution chains per `math-scripts.md §"Double-Check Logic Before Compute"`).**

*Invariant 1 — `r_g = 1` (restoration-surface / acoustic-geometry identity).*
```
Def 1: r_g := the root of Mach(r) = 1, i.e. v(r_g) = c_s              [type-II crossover, s105 §find_crossovers]
Def 2: v(r) = v₀ exp(−r²/2r₀²),  v₀ = Mach_core·c_s,  Mach_core = exp(1/2)   [s105 build_relay_flow]
Step 1: Mach(r_g) = v(r_g)/c_s = Mach_core·exp(−r_g²/2r₀²) = 1
Step 2: exp(−r_g²/2r₀²) = 1/Mach_core = exp(−1/2)
Step 3: −r_g²/2r₀² = −1/2  ⇒  r_g² = r₀²  ⇒  r_g = r₀ = 1.0      [r₀ = 1, s105 line 238]
Conclusion: r_g = 1 EXACTLY, as a CLOSED-FORM consequence of Mach_core = exp(1/2). [L_max-INDEPENDENT]
```
So `|r_g − 1| = 2.193e-10` is the **bisection-root residual** of the numerical root-finder (TOL_ROOT = 1e-8, s105 line 138) against the analytic value `r₀ = 1` — a root-finding-precision floor. It carries NO information about HKR-image convergence to g_M.

*Invariant 2 — `anec = 1` (the `A = 2J` angular-momentum-GFF identity).*
```
Def 1: anec := the ANEC-wall integral ∫_{−∞}^0 dt [m·A(t) − (t/4m)(A − 2J)]   [s105 anec_wall, lines 294/496]
Def 2: A(0) = A0 = 1.0,  J(0) = J0 = 0.5,  with the construction enforcing A = 2J   [npz A0,J0,a_eq_2j=True]
Step 1: A − 2J = 1.0 − 2·0.5 = 0 EXACTLY  ⇒  the (t/4m)(A−2J) term vanishes identically
Step 2: anec saturates to its A=2J-balanced value, normalized to 1
Conclusion: anec = 1 EXACTLY by the A=2J spin-orbit balance. [L_max-INDEPENDENT]
```
So `|anec − 1| = 7.500e-09` is the **ANEC-quadrature floor** (TOL_ANEC = 1e-9, s105 line: tol_anec) against the exactly-saturated `A=2J` identity — `~7.5×` the quadrature tolerance. It carries NO information about HKR-image convergence to g_M.

**`Level-3 = max(2.193e-10, 7.500e-09) = 7.500e-09` is therefore the JOINT numerical-floor residual of two analytic saturations.** Both are dimensionless, both pinned to integer 1 by closed-form identities (`Mach_core=√e ⇒ r_g=1`; `A=2J ⇒ anec=1`), both L_max-INVARIANT. The number measures *how precisely the quadrature/root-finder resolves two exactly-saturated sign-structure identities* — a numerical-precision property of the type-IV compute, NOT a convergence depth of the a₂ magnitude.

**Why this is structurally ORTHOGONAL to the bound magnitude channel (the decisive point).** Per `substrate-first-canonical-sourcing.md §(iv-bis)` (surrogate-vs-canonical at the cohomology-class layer): a dimensionless ratio "mechanically locked" by an internal balance is a GEOMETRIC observable, NOT a cohomology-class convergence witness, and its FAIL/PASS is uninformative on the canonical it stands in for. Both §VII.CB invariants are mechanically locked:
- `r_g=1` is locked by the acoustic-geometry identity `Mach_core=√e` (a GEOMETRIC observable — *where* the supersonic-subsonic crossover sits, set by the substrate-derived core Mach);
- `anec=1` is locked by the conservation-law identity `A=2J` (a GEOMETRIC observable — the spin-orbit balance of the J-GFF profile).

Neither is the HKR-image convergence of `Tr_{M₂}(P_a₂·T^{(IV)}) → g_M`. The §(iv-bis) algebraic-distance theorem applies: the surrogate (sign residual) is sign-locked to a substrate-internal combinatorial/conservation constraint, so its FAIL/PASS does not falsify (or confirm) the canonical (magnitude convergence). §VII.CB consumed a §(iv-bis) GEOMETRIC surrogate as if it were the cohomology-class anchor — exactly the silent class-conflation §(iv-bis) is written to catch.

**Classification (per `phononic-framing.md`).** The type-IV sign-anchor residual is **PHONONIC** (it is the acoustic-discriminant sign-topology of the substrate's own supersonic transit) but it lives at the **Level-1-adjacent SIGN content** of the bridge's a₂ class — it witnesses the *topology* (core/exterior sign structure, one crossover), not the *magnitude* (the scalar value → g_M). It is a real substrate observable; it is simply not the observable the registry-PASS criterion requires.

#### T2: Is a magnitude-convergence Level-3 anchor (Tr_{M₂}(P_a₂·T^{(IV)}) → g_M) constructible at finite L_max?

**ANSWER (the pivotal constructibility verdict my axis owns): YES IN PRINCIPLE, NO FROM THE EXISTING ARTIFACT. The scalar `M(L) = Tr_{M₂}(P_a₂·T^{(IV)})|_L` is a well-defined finite-L spectral-triple trace whose `L→∞` image is `g_M = a_2_FW_zeta`, so a Tier-1 magnitude-convergence anchor IS constructible — but it requires a NEW compute on the finite-L spectral triple `(A_K, H_K, D_K(τ_fold))`, NOT an L_max-scan or re-read of the existing s105 radial-acoustic-profile artifact (which carries no L_max axis and no scalar magnitude). This lands §VII.CB at "Tier-1-eligible-in-principle, held-pending-the-CF-S107-NEW-compute" — VDD2/VDD3's disposition, with the constructibility precisely scoped.**

**Part A — WHY the magnitude anchor is constructible in principle (the optimistic half VDD2 hoped for).** The Element-2 OE-form of the bridge is `∫_BZ Tr_{M₂(ℂ)}(P_a₂ · g_tt^{cont}) dμ` (registry §VII.CB Element 2, line 21980), and the substrate-IS Element 1 is `Tr_{M₂(ℂ)}(P_a₂ · T^{(IV)})`. Each piece is well-defined on the finite-L triple:
- `T^{(IV)}` is the type-IV acoustic stress-energy operator. On the finite-L spectral triple it is an operator on `H^{≤L} ⊗ ℂ²` (the M₂(ℂ) Nambu/acoustic doublet) — the a₂-channel acoustic `g_tt` lifted to the spectral-triple representation, NOT a continuum radial function.
- `P_a₂` is the a₂-channel curvature-degree-2 Seeley-DeWitt projector (a NAMED projector, registry-compliant) — the projection onto the a₂ spectral weight.
- `Tr_{M₂(ℂ)}(P_a₂ · T^{(IV)})|_L` is then a single scalar per L_max — a finite-L spectral-triple trace.
- Its `L→∞` limit is the continuum a₂ Seeley-DeWitt second moment = the emergent 4-metric `g_M`, whose canonical value is `a_2_FW_zeta = 2776.165389` (S88 `S88-A-N-FW-CANONICALIZATION`; npz stores it as the imported constant). The binding determination (3b/3d) presupposes exactly this scalar `M(L) → g_M` and bounds its residual by `‖HKR(M_L) − g_M‖ ≤ C·L⁻³`.

So the bridge's Level-1 is NOT a pure class-identity-with-no-scalar-image (VDD2's feared worst case): the a₂ class HAS a scalar magnitude representative (the Seeley-DeWitt moment), and `g_M` is a finite computable number. **A Tier-1 anchor exists in principle. §VII.CB is Tier-1-eligible, not Tier-2-permanent-hold.**

**Part B — WHY it is NOT extractable from the existing s105 artifact (the corrective half, the finding my axis adds).** The s105 type-IV compute does NOT compute `Tr_{M₂}(P_a₂·T^{(IV)})`. It computes the acoustic DISCRIMINANT `Γ_sub(r) = c_s² − v(r)²` on a continuum radial grid and reads off SIGN invariants (g_core, g_ext, r_g, anec). Three structural reasons it cannot be re-read or L-scanned into the magnitude anchor:
1. *No spectral-triple trace.* `g_core = Γ_sub[0]` and `g_ext = Γ_sub[−1]` are POINTWISE evaluations of a radial function at the profile endpoints — NOT a trace `Tr_{M₂}(P_a₂·…)` over the spectral triple. There is no projector P_a₂ applied, no M₂(ℂ) trace taken over H^{≤L}. The artifact has the wrong mathematical type for the magnitude channel.
2. *No L_max axis.* The s105 compute is on a fixed 512-point spatial grid; L_max=10 enters ONLY through the imported `c_BLV = c_s` (the sound-speed normalization). Re-running at L ∈ {8,12} changes c_BLV (hence c_s²) but — by the uniform-rescaling invariance of T1 — leaves `r_g=1` and `anec=1` analytically unchanged. So an "L-scan of s105" produces the SAME sign invariants at every L; it does not produce a converging magnitude.
3. *g_M is stored, not computed-toward.* The npz carries `a_2_FW_zeta = 2776.165389` as the imported constant `c_BLV`'s provenance, never as a computed `Tr_{M₂}(P_a₂·T^{(IV)})|_L` limiting to it. There is no `M(L)` sequence in the artifact to exhibit a residual.

**Part C — WHAT CF-S107 must actually compute (the materialization spec, scoped from the substrate physics).** The Tier-1 magnitude-convergence anchor requires a genuinely NEW compute, distinct in KIND from s105:
```
For L_max ∈ {8, 10, 12}:
  (i)   Represent T^{(IV)} as an operator on H^{≤L} ⊗ ℂ² (the a₂-channel acoustic stress-energy
        lifted to the finite-L spectral-triple representation — NOT a continuum radial function).
  (ii)  Apply the a₂-channel curvature-degree-2 Seeley-DeWitt projector P_a₂.
  (iii) Take the M₂(ℂ) trace:  M(L) = Tr_{M₂(ℂ)}(P_a₂ · T^{(IV)})|_L   (one scalar per L).
  (iv)  Form the residual:  res(L) = |M(L) − g_M| / |g_M|,  g_M = a_2_FW_zeta = 2776.165389.
Gate: res(L_max=10) < Level-2(L_max=10) = 1e-3, AND res(L) ∝ L⁻³ (the FLOWING signature
      confirming it is the bound observable), with C_1 sign reported (the §VII.AF.1-vs-§VII.AU fork).
```
This is the CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR the workshop spec pre-registers. Inputs: the S105 type-IV npz (`e2860d57…`/audit `91b36ed9…`) for the T^{(IV)} construction, the S106 W3-2 binding-envelope npz (`a8efd183…`/audit `943b17ad…`) for the Level-2 bound, and `g_M = a_2_FW_zeta` from canonical_constants.py. Effort ~1.0 (three L_max points, each a finite-L spectral-triple trace — the L=8/10 spectra are cached; L=12 is the s84 master cache `s84_spectrum_cache_L12_tau019.npz`).

**Part D — the constructibility verdict in the Tier-1/Tier-2 frame (the decision VDD3 deferred to me).** Because `M(L) → g_M` is a convergent scalar (Part A), §VII.CB is **Tier-1-eligible** — distinguishing it from §VII.AX's Tier-2-dimensionful PERMANENT hold (where `n_PBH` is divergent and un-anchorable from inside). The disposition is therefore **held-pending-a-constructible-CF, NOT permanent-hold**:
- §VII.AX: Tier-1 FAILS (divergent), Tier-2 DIMENSIONFUL ⇒ PERMANENTLY held (no in-channel anchor exists). 
- §VII.CB: Tier-1 ELIGIBLE (convergent magnitude exists in principle) but NOT-YET-ANCHORED (the existing anchor is the orthogonal sign channel; the magnitude channel is uncomputed) ⇒ held-pending-CF-S107, DISCHARGE-ELIGIBLE on the magnitude-convergence compute.

This confirms VDD3's disposition with the constructibility precisely pinned: the hold is real and must be effected NOW (the supplied anchor is category-mismatched), AND it is discharge-eligible (the bound observable is constructible) — so the correct tag is `Level-3-HELD: NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR (Tier-1-eligible-in-principle; discharge = CF-S107 NEW Tr_{M₂}(P_a₂·T^{(IV)})→g_M compute, NOT an L-scan of the s105 sign-channel artifact)`.

#### T3: Questions for van-den-dungen

**Q1 (the constructibility hand-back — for R2 CONVERGENCE).** I have answered your VDD2/VDD3 pivotal question: the magnitude anchor `M(L) = Tr_{M₂}(P_a₂·T^{(IV)}) → g_M` IS constructible in principle (a finite-L spectral-triple trace, `g_M = a_2_FW_zeta`), so §VII.CB is Tier-1-eligible-held, NOT §VII.AX-permanent-hold. **Does the HKR/Connes-Karoubi side AGREE that `Tr_{M₂}(P_a₂·T^{(IV)})|_L` is the correct finite-L pre-image of the HKR `L→∞` boundary map** — i.e., is the operator-trace I specified in T2 Part C the right Element-1 finite-L observable whose HKR image is `g_M`, so that `‖HKR(M_L) − g_M‖` is literally what your L⁻³ envelope bounds? If yes, we have a clean joint convergence: your envelope binds `M`, my CF-S107 computes `‖M_L − g_M‖`, and the registry-PASS discharges. If the HKR side requires a DIFFERENT finite-L pre-image (e.g. a Hochschild cocycle rather than a plain M₂ trace), I need that operator form to scope CF-S107 correctly.

**Q2 (the C_1 fork — for R2/R3).** You flagged (VDD5/§VII.AU) that the subleading `C_1` is observable-specific (negative at §VII.AF.1 → over-performance; positive at §VII.AU → under-performance), and that the type-IV EMT bridge's C_1 is uncomputed. **From the HKR base-dimension derivation (Paper 01 fibre-integration / shriek map `[D_M] = π_! ⊗ [D_B]`), does the codim-1 outermost-shell structure PREDICT the C_1 sign for the a₂ magnitude channel**, or is it genuinely observable-specific and must wait for CF-S107's `res(L)` series? If the shriek-map geometry pins the C_1 sign, we can pre-register the over/under-performance expectation; if not, CF-S107 reports it empirically. This matters because the §VII.CB finite-L magnitude residual at L_max=10 sitting below 1e-3 is exactly the §VII.AF.1-vs-§VII.AU question, and I want the CF gate to pre-register the right direction.

**Q3 (the held-tag wording — for R3 convergence on the mack patch).** I proposed sharpening your hold tag to encode the type-IV-specific cause: `Level-3-HELD: NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR (supplied anchor is sign-structure saturation, channel-orthogonal to the bound a₂-magnitude; Tier-1-eligible-in-principle; discharge = CF-S107 NEW compute)`. **Do you concur this is the right tag text for mack to land at R3** — distinguishing §VII.CB (sign-channel-orthogonal, Tier-1-eligible-held) from §VII.AX (dimensionful-divergent, permanent-hold) and §VII.AV (SCHEMATIC-proxy-envelope)? Three different held-reasons, three different tags — I want us aligned on §VII.CB's before the R3 mack patch so the registry annotation names the actual structural cause.

---

## Round 2 — van-den-dungen: Follow-up

### CONVERGENCE

Transit's Round-1 response (Re:VDD1, Re:VDD2, T1, T2) closed every conditional I left open in R1, and it did so from the one axis I do not own — the type-IV substrate physics. Three things CHANGED from my R1 position as a result; I record each as a settled convergence, with the specific finding that moved me.

**CONV-1 — The VDD1 fork is resolved to "fixed-anchor saturation," NOT "co-convergent proxy" — and the resolution is by ANALYTIC IDENTITY, which is stronger than I had any right to expect.** In VDD1 I posed the disposition-deciding question as a fork: is `S(L) = (r_g, anec)` *forced* to {1,1} by the type-IV topology (Reading B, category-mismatch), or does it *flow toward* {1,1} *with* `M(L) → g_M` (a degraded-but-real Reading-A proxy)? My R1 prior was "forced," but I could not prove it — it was an expectation, not a derivation, because the proof lives on the type-IV side. Transit's T1 supplies the proof, and it is cleaner than my prior: **both invariants are pinned to 1 by closed-form analytic identities, neither of which has any L_max character at all.** `r_g = 1` is the root of `Mach(r)=1` on the FIXED Gaussian profile, forced to exactly `r₀ = 1` *as a closed-form consequence* of `Mach_core = exp(1/2)` (the substrate-derived core Mach — T1 invariant-1 substitution chain: `exp(−r_g²/2r₀²) = exp(−1/2) ⇒ r_g = r₀ = 1`). `anec = 1` is forced by the `A = 2J` spin-orbit conservation identity (T1 invariant-2: `A − 2J = 1.0 − 2·0.5 = 0` EXACTLY ⇒ the `(t/4m)(A−2J)` ANEC-wall term vanishes identically). What CHANGED for me: I held this as a *prior* in VDD1 ("my expectation"); it is now a *theorem on the type-IV side*. The two integer anchors are sign-saturation identities — `r_g=1` an acoustic-geometry / restoration-surface identity (where the supersonic-to-subsonic crossover sits in space), `anec=1` an angular-momentum-GFF conservation identity. Neither is the magnitude-convergence residual `‖HKR(M_L) − g_M‖`. Reading B is now established, not conjectured.

**CONV-2 — The magnitude anchor is constructible IN PRINCIPLE but NOT extractable from the existing s105 artifact — a TWO-PART verdict that revises my R1 "favorable distinction" framing.** In VDD2 I drew §VII.CB favorably against §VII.AX: §VII.AX's `n_PBH` is genuinely divergent (un-anchorable from inside ⇒ Tier-2-dimensionful permanent hold), whereas §VII.CB's magnitude channel is *convergent* (`M(L) → g_M` at rate L⁻³ by the binding determination), so a Tier-1 anchor is "in principle constructible" and the entry is "Tier-1-eligible-but-not-yet-anchored." Transit's T2 accepts the optimistic half AND corrects the framing on the pessimistic half, and I adopt the correction in full. The correct statement is: **(Part A) the scalar `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` is a well-defined finite-L spectral-triple trace whose `L→∞` image is `g_M = a_2_FW_zeta = 2776.165389` — so the bridge's Level-1 is NOT a pure class-identity-with-no-scalar-image (the worst case I feared in VDD2), the a₂ class HAS a scalar magnitude representative (the Seeley-DeWitt second moment), and a Tier-1 anchor CAN exist; (Part B) but it is NOT extractable from the EXISTING s105 artifact — that compute is a continuum acoustic-discriminant calculation on a 512-point radial grid with NO L_max axis (L_max=10 enters only through the imported `c_BLV = c_s`, which cancels in the dimensionless ratios), so a "re-read or L-scan of s105" produces the SAME sign invariants at every L and never a converging magnitude.** What CHANGED for me: my R1 phrase "Tier-1-eligible-but-not-yet-ANCHORED" carried an implicit "anchored-elsewhere-just-not-collected" reading. Transit's T2 Part B sharpens "not-yet-anchored" to **"UNBUILT — it requires a genuinely DIFFERENT compute, not an L-scan of the existing one."** I accept this. The precise disposition is **Tier-1-eligible-IN-PRINCIPLE, held-pending-a-NEW-CF-S107** — distinct from §VII.AX's Tier-2-dimensionful PERMANENT hold (where no in-channel anchor exists at all). This is the constructibility verdict VDD3 explicitly deferred to transit, and it lands exactly where my VDD-side prior pointed (held, discharge-eligible), but the reason STRENGTHENS the case for holding NOW: the existing anchor is not a degraded proxy of the bound quantity, it is a *different* quantity, and the bound quantity is uncomputed.

**CONV-3 — The §VII.AX two-part split (structure STAGE-3-PERMANENT / Level-3 row HELD) is the agreed disposition, and the held tag must encode the type-IV-specific cause.** Transit endorses the VDD3 four-point patch (hold the Level-3 row; re-tag REGISTRY-PASS → STAGE-3-PERMANENT-STRUCTURE/Level-3-HELD; retain the sign-residual as a separate Element-1 witness; point the hold at the constructible CF-S107) and CLOSES the Reading-A defense I left conditionally alive in VDD3 R1 (Re:VDD3: "`S(L) → {1,1}` is NOT co-convergent with `M(L) → g_M`; it is fixed-anchor saturation … with NO L⁻³ character and indeed no L-dependence at all" ⇒ Reading A's co-convergent-proxy escape FAILS). What CHANGED for me: VDD3 left Reading A's strongest form alive *pending* transit's co-convergence verdict; that verdict is now in and it kills Reading A. I converge on transit's SHARPENED held-tag wording (Re:VDD3, T3-Q3) over my R1 bare `NOT-SATISFIED-PENDING-magnitude-convergence-anchor`, because the future reader MUST be able to distinguish §VII.CB's held-reason from the two sibling held-reasons. The three held-reasons, three tags:

| Entry | Held-reason | Tier status | Discharge path |
|:------|:------------|:------------|:---------------|
| **§VII.AX.OP-PROJ** (n_PBH) | dimensionful magnitude on a genuinely DIVERGENT channel (`n_edge ~ C(N_eigs,2)`, near-geometric L-step-ratios, may not saturate) | Tier-2-DIMENSIONFUL | PERMANENT hold (no in-channel anchor exists) |
| **§VII.AV** | envelope is a SCHEMATIC Casimir-proxy; FULL-PV gave `α_PV=6.26` (≠ schematic 3) | proxy-refinement (deferred-pending) | FULL-physical envelope recompute |
| **§VII.CB** (this entry) | supplied anchor is SIGN-STRUCTURE saturation, channel-ORTHOGONAL to the bound a₂-magnitude; magnitude channel Tier-1-eligible-in-principle but UNCOMPUTED | Tier-1-eligible-IN-PRINCIPLE | CF-S107 NEW `Tr_{M₂}(P_a₂·T^{(IV)})→g_M` compute (NOT an L-scan of the s105 sign-channel artifact) |

I refine the exact registry-patch text for mack in QUESTIONS below (the precise held-tag string + the Tier-1-eligible-in-principle annotation), and answer transit's T3-Q3 there.

**CONV-4 — The 1.333e5× margin is an artifact, and transit upgraded my VDD4 L_max-scaling diagnostic from "I predict FLAT" to "FLAT by analytic structure."** In VDD4 I predicted that `(r_g, anec)` computed at `L_max ∈ {8,10,12}` would NOT scale as L⁻³ — it would "track the sign-balance floor / numerical precision, plausibly L-flat or non-monotone." That was a hedge. Transit's Re:VDD4 removes the hedge: the sign residual is **PROVABLY L_max-FLAT by the analytic structure of the type-IV compute** — `r_g` and `anec` are invariant under uniform rescaling of `c_s²` (the only L-dependence in the channel, via `c_BLV`), so `‖S(L) − {1,1}‖` is set by the bisection-root tolerance (TOL_ROOT=1e-8) and the ANEC-quadrature floor (TOL_ANEC=1e-9) at EVERY L_max. This IS the multiplicative-normalization cancellation fingerprint of `math-scripts.md §"Multiplicative-normalization cancellation invariants"` (K=3 MANDATORY): `S` factors as `w(L_max)·g(geometry)` with `w(L_max)` (via `c_BLV`) cancelling in the dimensionless `r_g`/`anec`, leaving an L_max-INVARIANT geometric kernel. What CHANGED for me: I expected the FLAT signature as an empirical prediction for CF-S107 to confirm; transit shows it is a STRUCTURAL IDENTITY, derivable without running the L-scan. The consequence for CF-S107 scope is precise (and I converge on it): CF-S107 does NOT need to L-scan the sign channel (its FLAT signature is analytic); CF-S107 must compute the OTHER curve — the magnitude residual `‖M_L − g_M‖` at `L ∈ {8,10,12}` to exhibit its L⁻³ FLOWING signature. The contrast sign-residual-FLAT (analytic) vs magnitude-residual-FLOWING-as-L⁻³ (CF-S107) is the clean empirical discriminator that they are different observables, and it seals the artifact reading.

**Net of CONVERGENCE.** From the joint exchange, the four W-2 adjudication questions now have agreed answers: (1) **DIFFERENT observable** — sign-saturation channel `S` vs magnitude-convergence channel `M`, with `M` structurally absent from the s105 artifact (transit Re:VDD1); (2) **NEITHER Tier-1 nor Tier-2 as currently anchored**, but **Tier-1-eligible-IN-PRINCIPLE** (the magnitude channel is convergent, unlike §VII.AX's divergent `n_PBH`) — held-pending-a-NEW-compute, not permanent-hold (transit T2); (3) **§VII.AX structure-permanent/Level-3-held split**, NOT a blanket STAGE-1-CANDIDATE down-tag, with a type-IV-specific held tag (transit Re:VDD3, T3-Q3); (4) **the margin is a non-load-bearing artifact**, FLAT-by-analytic-identity, large for the wrong reason (transit Re:VDD4). Reading B wins as stated; the disposition is the §VII.AX surgical-demotion pattern.

### DISSENT

The substantive physics has converged — Reading B, the §VII.AX split, the FLAT-vs-FLOWING discriminator. My remaining dissents are NEW (not R1 restatements) and both concern the PRECISE structural status of claims we are about to hand to mack and to the CF-S107 pre-registration. Getting these exactly right is the whole point of an adjudication that ends in a registry patch.

**DST-1 (the "more defective vs less defective" axis — I partially RESIST transit's Re:VDD2 sharpening, on a Kasparov-side ground transit could not see).** Transit's Re:VDD2 says my VDD2 "less severe than §VII.AX" framing "cuts the other way on one axis": §VII.AX at least computed its `n_PBH` channel (the RIGHT observable, just dimensionful-un-anchorable), whereas §VII.CB "published a number from a different channel and called it the bridge anchor" ⇒ §VII.CB is *more* defective on the "is the supplied anchor even the bound observable?" axis. I agree this is a real axis and that on it §VII.CB scores worse. But I resist collapsing the two severities into a single ordering, because they are **orthogonal severities measuring different things**, and the disposition turns on keeping them orthogonal — exactly the algebra-axis orthogonality discipline I am bound to (`cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`). The NEW Kasparov-side point: the two "severity" axes are the two legs of the Tier-1/Tier-2 gate, and they are not commensurable.
- **Axis-1 (anchor-correctness): is the supplied Level-3 number the bound observable?** On this axis §VII.AX scores BETTER (it anchored `n_PBH`, the right channel) and §VII.CB scores WORSE (it anchored `S`, the wrong channel). Transit is right here.
- **Axis-2 (in-principle-constructibility): can a correct anchor EXIST for this bridge?** On this axis §VII.AX scores WORSE (NO — `n_PBH` is divergent, no in-channel anchor exists, ever) and §VII.CB scores BETTER (YES — `M(L) → g_M` is a convergent scalar, the anchor is constructible).

These two axes are the substance of the Tier-1/Tier-2 gate: Axis-2 IS the Tier-1-eligibility question (does a convergent residual-to-`c_continuum` exist?), and it is the axis that decides PERMANENT-hold (§VII.AX, Axis-2-NO) vs HELD-PENDING-CF (§VII.CB, Axis-2-YES). Axis-1 decides whether the entry is held NOW (both are, for different anchor-correctness reasons). The dissent is narrow but load-bearing for the patch: we must NOT let transit's correct Axis-1 observation ("§VII.CB more defective") leak into the *disposition*, where Axis-2 governs and §VII.CB is the FAVORABLE case (discharge-eligible, not permanent). My R1 "favorable distinction" was on Axis-2 and was correct *for the disposition*; transit's "more defective" is on Axis-1 and is correct *for the diagnosis*. Both hold; they must not be averaged into a single "how bad is §VII.CB" scalar. The patch text must carry BOTH: diagnosis = wrong-channel-anchor (Axis-1, §VII.CB worse than §VII.AX); disposition = Tier-1-eligible-held-discharge-eligible (Axis-2, §VII.CB better than §VII.AX). This is a genuine dissent against the implicit single-ordering, resolved by holding the two axes orthogonal — and it sharpens, not softens, transit's point.

**DST-2 (the C_1 fork — I dissent from any reading that the CF-S107 gate can pre-register an over/under-performance DIRECTION from the shriek-map geometry alone).** This is NEW (it arises only from transit's T3-Q2, which asks whether the HKR base-dimension derivation PREDICTS the C_1 sign). My answer (full derivation in QUESTIONS) is **NO — the shriek-map / fibre-integration geometry pins the LEADING exponent `α = d−1 = 3` but is BLIND to the subleading `C_1` sign**, and I dissent in advance from any CF-S107 pre-registration that smuggles in a direction. Here is the Kasparov-side reason, stated as a dissent because it constrains the gate we are about to write: the codim-1 outermost-shell residual of the d=4 base integral (Paper 01 §3 fibre-integration image `[D_M] = π_! ⊗ [D_B]`) is a TOPOLOGICAL statement — it fixes the *rate* at which the truncated base integral approaches the full one (the missing shell is codim-1 ⇒ `L⁻(d−1)`). It says nothing about the *coefficient structure* of the convergence, because the `C_1` term `L⁻³·(C_0 + C_1·L⁻¹ + …)` is the next-order term of the Connes-Moscovici §III.4 finite-L residue expansion, and that coefficient is set by the *cocycle/projector geometry at the pole* (registry §VII.AF.1 S91-W0 annotation: "subleading coefficients C_1 … are observable-specific and reflect the cocycle/projector geometry"), NOT by the base dimension. The framework's own ledger PROVES this is observable-specific and not geometry-pinned: §VII.AF.1 has *negative* C_1 (10× over-performance) and §VII.AU has *positive* C_1 (2.6926 under-performance) — **same `(d=4, s=3)` base-dimension structure, opposite C_1 signs.** If the shriek-map geometry pinned C_1, these two siblings could not differ. They do. Therefore the type-IV EMT bridge's C_1 sign is NOT derivable from my Leg-B derivation and MUST be reported empirically by CF-S107's `res(L)` series. The dissent: any pre-registration of the CF-S107 gate that says "we expect over-performance" (or under-) is a Class-8.2 PRU smuggle — the direction is an OUTPUT of CF-S107, not an input. The honest gate pre-registers ONLY the binding inequality `res(L_max=10) < 1e-3` AND the FLOWING-as-L⁻³ scaling check, with C_1-sign as a REPORTED diagnostic (the §VII.AF.1-vs-§VII.AU fork left open until computed). This is a real dissent against an over-confident gate spec, grounded in the topology≠analysis boundary I am bound to.

### EMERGENCE

Cross-pollinating my HKR/Kasparov axis with transit's type-IV substrate physics produced structural insights that neither side held alone in R1. These are NEW — they emerge from the join.

**EMG-1 — The Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}` survives Reading B INTACT, and the type-IV physics now tells us PRECISELY which half of the class the supplied anchor witnesses.** This is the most important emergent result and it sharpens my standing boundary ("Kasparov product gives TOPOLOGY … NOT ANALYSIS") into a finer statement. A K-homology class read through the shriek map `[D_M] = π_! ⊗ [D_B]` carries TWO kinds of content that the bridge's Level-1 statement bundles together:
- **SIGN/topological content** of the a₂ class: the core/exterior sign structure (g_core < 0 ANEC-violating supersonic core, g_ext > 0 type-I subsonic exterior, exactly one crossover at r_g=1). This is *topology* — the sign of the discriminant, an orientation/index datum.
- **MAGNITUDE/analytic content** of the same a₂ class: the scalar value of the Seeley-DeWitt second moment, `Tr_{M₂}(P_a₂·T^{(IV)})|_L → g_M = 2776.165389`. This is *analysis* — the numerical size of the pairing.

The emergent insight: **the type-IV compute realizes ONLY the SIGN half of the class; the bridge's Level-3 anchor witnesses the topological half, while the registry-PASS criterion demands the analytic half.** This is why Level-1 survives (the class identity is true at the cohomology level — both sign and magnitude content belong to the *same* a₂ Seeley-DeWitt curvature-degree-2 class, regulator-invariant, L-independent), AND why the registry-PASS fails as currently anchored (the supplied number is on the topological half, not the analytic half). My R1 framing said "topology ≠ analysis" as a slogan; the join with transit's physics makes it concrete: the class `[T^{(IV)}]_{a₂,HKR}` is ONE object with two readable halves, the type-IV compute reads the SIGN half cleanly to 8 digits, and the bridge mistakenly offered the sign-half residual as the analytic-half anchor. The Level-1 identity is not damaged; the Element-5 *selection* picked the wrong half of a sound class. This is the substrate-first reading: the substrate IS the finite-L type-IV EMT (a single object carrying both halves); the laboratory/QCD GFF reference (Dumitru-Noronha) is the analog of the SIGN half (the J-sourced energy-flux sign topology), and the continuum a₂-metric `g_M` is the `L→∞` image of the MAGNITUDE half — both halves substrate-prior, neither a container the substrate must fit into.

**EMG-2 — The FLAT-vs-FLOWING multiplicative-normalization fingerprint is the clean, framework-canonical discriminator between the two halves — and it elevates the Reading-B verdict from "a category argument" to "a K=3-MANDATORY structural identity."** In R1 I floated the L_max-scaling diagnostic as a *prediction* (VDD4: "I predict the sign-residual will NOT scale as L⁻³"). Transit's Re:VDD4 proved it FLAT-by-analytic-structure. What emerges from joining the two is that this is not an ad-hoc diagnostic — it is exactly the `math-scripts.md §"Multiplicative-normalization cancellation invariants"` 2-bit `L_max`-FLAT-vs-`L_max`-FLOWING fingerprint, which is K=3 MANDATORY in the framework. The sign channel `S` factors as `w(L_max)·g(geometry)` with the L_max-dependent weight `w` (via `c_BLV`) cancelling in the dimensionless ratios `r_g`/`anec` ⇒ the log-derivative annihilates `w(L_max)` ⇒ `S` is L_max-INVARIANT (FLAT) by structural identity. The magnitude channel `M` has NO such cancellation — its residual `‖M_L − g_M‖` is the codim-1 outermost-shell deficit, genuinely L_max-FLOWING as L⁻³. So the two halves of the a₂ class have OPPOSITE multiplicative-normalization fingerprints:

| Channel (half of the a₂ class) | Multiplicative structure | Fingerprint | What it witnesses |
|:--|:--|:--|:--|
| SIGN half `S = (r_g, anec)` | `w(L_max)·g(geom)`, `w` cancels in dimensionless ratio | **L_max-FLAT** (numerical-floor) | topology (sign structure of the class) |
| MAGNITUDE half `M = Tr_{M₂}(P_a₂·T^{(IV)})` | no cancelling prefactor; codim-1 shell deficit | **L_max-FLOWING as L⁻³** | analysis (scalar value → g_M) |

The emergent point: the framework already has a MANDATORY rule whose whole purpose is to catch "an L-FLAT quantity being read as a convergence witness" — and §VII.CB is a textbook instance. The `math-scripts.md` fingerprint is precisely the tool that distinguishes "this residual is a structural identity, NOT empirical convergence evidence" (FLAT) from "this residual is the genuine convergence rate" (FLOWING). Reading B is therefore not merely "a careful category argument I made" — it is the application of a K=3-MANDATORY structural identity to the §VII.CB Level-3 anchor. This GREATLY strengthens the disposition: the held-tag is mandated by an existing framework rule, not invented in this workshop.

**EMG-3 — The §VII.AF.1-vs-§VII.AU C_1-sign split is the SIGN-half-blindness of the topological derivation, made visible.** Joining DST-2 (my Kasparov-side C_1 argument) with the framework's documented C_1 ledger yields a structural reading I did not have in R1: the reason a base-dimension/shriek-map derivation can pin the *leading* exponent but NOT the C_1 sign is the SAME reason the type-IV compute can read the sign half but not the magnitude half — **topological data (base dimension, sign structure, leading rate) is fixed by the fibre-integration geometry; analytic data (C_1 coefficient, magnitude value, finite-L over/under-performance) is fixed by the cocycle/projector geometry at the pole, which the topology does not see.** The §VII.AF.1 (negative C_1, 10× over-performance) vs §VII.AU (positive C_1, 2.6926 under-performance) split on the same `(d=4,s=3)` structure is the empirical proof that the analytic layer is NOT determined by the topological layer — and that is EXACTLY the gap §VII.CB falls into. The bridge's binding determination (3b/3d) is a topological-layer result (the leading L⁻³ rate); the registry-PASS is an analytic-layer claim (the finite-L residual `< 1e-3`); and the C_1 sign — which decides whether §VII.CB over- or under-performs at L_max=10 — is uncomputed and undeterminable from the geometry. CF-S107 must compute it; the workshop cannot pre-register its direction (DST-2). What emerges: the §VII.CB defect and the §VII.AU under-performance precedent are the SAME structural phenomenon (analytic content not determined by topological content), and the framework has already caught itself once (W9-5 FALSIFIED-at-K=2: a quantity assumed to FLOW-to-a-universal-limit instead stayed structured at the asymptotic layer). §VII.CB is the dual: a quantity assumed to be a convergence witness is actually FLAT. Same error class.

**EMG-4 — The §VII.CA / §VII.CB pairing in Wave 3 is a clean N/A-with-reason vs binding-but-Level-3-held contrast — a methodological emergent worth recording.** Wave 3 landed two §VII entries side by side: §VII.CA (metric-without-curvature, the EXACT-zeros joint wall) declared its 5-anatomy + 3-level ladder **N/A-with-reason** because there is NO `c_continuum` and NO convergent envelope (the zeros are EXACT at every L_max, an eigenbundle-topological identity — WP §W3-1 lines 42-46). §VII.CB declared a FULL binding L⁻³ envelope WITH a named `c_continuum = g_M` but supplied a Level-3 anchor on the wrong half of the class. The emergent methodological lesson: these are the two distinct ways a structural/cross-pillar entry can stand WITHOUT a satisfied Level-3-on-the-bound-channel — (a) **N/A-with-reason** (no continuum image exists; the ladder is vacuously inapplicable; §VII.CA, §VII.BY/§VII.BZ precedent), versus (b) **Level-3-HELD** (a continuum image exists and the envelope binds it, but the supplied finite-L anchor witnesses an orthogonal channel; §VII.CB, §VII.AX precedent). Both keep the theorem-STRUCTURE STAGE-3-PERMANENT; they differ in WHY the Level-3 row is not a live satisfied inequality. A future reader scanning Wave 3 must not conflate §VII.CA's "no ladder applies" with §VII.CB's "ladder applies, Level-3 held pending a recompute" — the distinction is an EXACT-zero structural identity (no convergence to track) vs a binding-convergent bridge whose convergence anchor is simply not yet on the right channel.

### QUESTIONS

I answer transit's three T3 questions from the HKR/Kasparov side, then pose the one sharper follow-up for R3. The campaign drives toward (i) the STRUCTURAL VERDICT (Reading B confirmed, §VII.AX split), (ii) the exact registry-patch wording for mack, (iii) the precise 4-field CF-S107 spec — so I make each answer land-ready for transit's R3 Turn B.

**A1 (answering T3-Q1: is `Tr_{M₂}(P_a₂·T^{(IV)})|_L` the correct finite-L pre-image of the HKR `L→∞` boundary map?).** **YES — with one operator-form sharpening that does not change the scope.** The HKR side AGREES that `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` is the correct finite-L pre-image whose `L→∞` HKR image is `g_M`, so `‖HKR(M_L) − g_M‖` is literally what the L⁻³ envelope bounds. The sharpening: at the strictest Kasparov level the bridge map is HKR `L→∞` ∘ Connes-Karoubi pairing (registry §VII.CB Element 3, line ~21974), and the Connes-Karoubi pairing realizes the a₂ class as a *Hochschild cocycle paired against the Chern character of the projector* — `⟨[φ_{a₂}], [Ch(P_a₂)]⟩`. For the LEADING-rate residual `‖HKR(M_L) − g_M‖ ~ L⁻³`, the plain M₂(ℂ) trace `Tr_{M₂}(P_a₂·T^{(IV)})|_L` and the Hochschild-cocycle pairing have the SAME `L→∞` image (`g_M`) and the SAME convergence rate (both are the codim-1 outermost-shell deficit of the same d=4 base integral) — this is the Connes-Moscovici §III.4 residue-formula equivalence at the substrate-distance-1 pole (the trace IS the residue of the cocycle pairing at `s=3`). So for CF-S107's PURPOSE (exhibit the FLOWING L⁻³ residual and check `< 1e-3` at L_max=10), the plain trace transit specified in T2 Part C is the correct and sufficient operator form — they agree to leading order. The Hochschild-cocycle form matters ONLY if one wants the C_1 *coefficient* (it is the cocycle/projector geometry that sets C_1 — EMG-3), which is a diagnostic, not the gate. **Bottom line for CF-S107: transit's T2 Part C operator-trace `M(L) = Tr_{M₂}(P_a₂·T^{(IV)})|_L` is the right Element-1 finite-L observable; the HKR image is `g_M`; no different pre-image is required for the binding inequality or the FLOWING check.** Clean joint convergence: my envelope binds `M`, transit's CF-S107 computes `‖M_L − g_M‖`, the registry-PASS discharges on PASS.

**A2 (answering T3-Q2: does the shriek-map geometry PREDICT the C_1 sign, or is it observable-specific?).** **NO — the shriek-map / fibre-integration geometry is BLIND to the C_1 sign; it is observable-specific and must wait for CF-S107's `res(L)` series.** (Full derivation in DST-2.) The codim-1 outermost-shell residual `[D_M] = π_! ⊗ [D_B]` (Paper 01 §3 fibre-integration image) is a TOPOLOGICAL statement fixing the *leading exponent* `α = d−1 = 3` (the missing shell is codim-1 ⇒ `L⁻(d−1)`). The C_1 term in `L⁻³·(C_0 + C_1·L⁻¹ + …)` is the next-order coefficient of the Connes-Moscovici §III.4 finite-L residue expansion, set by the *cocycle/projector geometry at the pole*, NOT the base dimension (registry §VII.AF.1 S91-W0 annotation, line ~14944: "subleading coefficients C_1 … are observable-specific and reflect the cocycle/projector geometry"). The framework's ledger is the proof: §VII.AF.1 has negative C_1 (10× over-performance), §VII.AU has positive C_1 (2.6926 under-performance) — SAME `(d=4,s=3)` base-dimension structure, OPPOSITE C_1 signs. If geometry pinned C_1, they could not differ. Therefore: **CF-S107 must report the C_1 sign EMPIRICALLY (the §VII.AF.1-vs-§VII.AU fork stays open until computed); the workshop MUST NOT pre-register an over/under-performance direction** — doing so would be a Class-8.2 PRU smuggle (DST-2). The CF-S107 gate pre-registers ONLY: (a) the binding inequality `res(L_max=10) < 1e-3`, and (b) the FLOWING-as-L⁻³ scaling check (`res(L) ∝ L⁻³` across {8,10,12}), with (c) C_1-sign as a REPORTED diagnostic. Note for transit: the §VII.AU precedent means a PASS on the binding inequality at L_max=10 is NOT guaranteed by the analytic α=3 — §VII.AU is the standing counterexample that a sibling on the same structure can under-perform at finite L. CF-S107 is a GENUINE gate (could FAIL), not a formality.

**A3 (answering T3-Q3: concur on the sharpened held-tag text for mack?).** **YES — I concur, with the exact patch string below, and I extend it to carry the DST-1 two-axis distinction so a future reader cannot mis-order the severity.** Transit's proposed tag captures the type-IV-specific cause; I adopt it and add the diagnosis/disposition split (DST-1) and the Tier-1-eligible-in-principle annotation the workshop spec asks me to sharpen. The exact registry-patch wording for mack (R3 Turn B, mack-cosmic-bridge sole writer, as a NEW pinned annotation on §VII.CB — verdict permanence: the `293105a2…` REGISTRY-PASS line stays on disk, this is the §VII.AX surgical-demotion annotation pattern, NOT a retroactive edit):

> **Registry status (S106 W3 workshop disposition — NEW pinned position, verdict-permanence-preserving annotation):**
> **§VII.CB theorem-STRUCTURE: STAGE-3-PERMANENT** (the binding L⁻³ envelope [Element 4], the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`, and the complete 5-anatomy are sound and untouched; Stage-2 PASS-AND holds on all non-Level-3 clauses).
> **§VII.CB Level-3 row: HELD — `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`.** The supplied Level-3 anchor `max(|r_g−1|=2.193e-10, |anec−1|=7.500e-09) = 7.500e-09` is a **SIGN-STRUCTURE saturation residual** — it witnesses the SIGN half of the a₂ class (`S(L) = (r_g, anec) → {1,1}`, the core/exterior topology: g_core<0 ANEC-violating core, g_ext>0 type-I exterior, n_crossovers=1), forced to the integer anchors {1,1} by the analytic identities `Mach_core = exp(1/2) ⇒ r_g = 1` and `A = 2J ⇒ anec = 1`, and is **L_max-FLAT by the multiplicative-normalization cancellation fingerprint** (`math-scripts.md §"Multiplicative-normalization cancellation invariants"`, K=3 MANDATORY: c_BLV cancels in the dimensionless ratios). It is **channel-ORTHOGONAL** to the magnitude-convergence residual `‖HKR(Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})_L) − g_M‖` (the L_max-FLOWING-as-L⁻³ quantity the Level-2 envelope actually bounds; `g_M = a_2_FW_zeta = 2776.165389`).
> **Tier status: Tier-1-eligible-IN-PRINCIPLE** (per `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` Tier-1/Tier-2 dimensional-re-anchorability gate). DIAGNOSIS (Axis-1, anchor-correctness): the supplied anchor is the WRONG channel — more anchor-defective than §VII.AX (which anchored its right-but-divergent channel). DISPOSITION (Axis-2, constructibility): the magnitude channel `M(L) → g_M` is a CONVERGENT scalar (a well-defined finite-L spectral-triple trace), so a Tier-1 anchor is constructible — UNLIKE §VII.AX (Tier-2-DIMENSIONFUL, divergent `n_PBH`, PERMANENT hold) and UNLIKE §VII.AV (SCHEMATIC-proxy envelope; §VII.CB's envelope is FULL, NO CLASS pin). Therefore §VII.CB is **held-pending-a-CONSTRUCTIBLE-recompute, NOT a permanent hold.**
> **Discharge path: `CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR`** — a NEW compute of `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` at `L ∈ {8,10,12}` on the finite-L spectral triple (NOT an L-scan of the s105 sign-channel radial-profile artifact, which has no L_max axis). On PASS (`res(L_max=10) < 1e-3` ∧ FLOWING-as-L⁻³), the Level-3 row promotes HELD → SATISFIED and §VII.CB earns full REGISTRY-PASS.
> **Sign-residual retained as a separate Element-1 witness** (NOT deleted): the `7.500e-09` is a real, clean substrate-IS result — the type-IV sign topology (`A=2J` GFF identity, restoration-surface `r_g=1`) realized to 8 digits — demoted from "the Level-3 anchor of the bridge" to "a sign-structure saturation witness of Element 1." The 1.333e5× margin is **non-load-bearing** (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"`, corpus §20): it is large because the two quantities being divided are different observables (an ANEC-quadrature floor over a magnitude envelope), NOT because the magnitude converged 1.333e5× early.

I concur this distinguishes §VII.CB cleanly from §VII.AX and §VII.AV (three held-reasons, three tags — CONV-3 table). This is the R3 mack patch, land-ready.

**A4 (the precise 4-field CF-S107 spec — for R3 convergence; the carry-forward we hand to S107).** Combining transit's T2 Part C materialization spec with the DST-2 / A2 C_1-blindness constraint, the land-ready 4-field spec:

1. **What**: compute the magnitude-convergence Level-3 anchor for §VII.CB — `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` (a finite-L spectral-triple trace, NOT a continuum radial-profile compute), its residual `res(L) = |M(L) − g_M|/|g_M|` with `g_M = a_2_FW_zeta = 2776.165389`, at `L ∈ {8,10,12}`; report `res(L_max=10)` and the fitted scaling exponent + C_1 sign.
2. **Inputs**: (a) the S105 type-IV npz (`e2860d57…`/audit `91b36ed9…`) for the `T^{(IV)}` construction; (b) the S106 W3-2 binding-envelope npz (`a8efd183…`/audit `943b17ad…`) for the Level-2 bound `L⁻³`; (c) `g_M = a_2_FW_zeta` from `canonical_constants.py`; (d) the cached L=8/10 spectra + the L=12 master cache `s84_spectrum_cache_L12_tau019.npz` (per transit T2 Part C — L=8/10 cached, L=12 from the s84 master).
3. **Gate (pre-registered, direction-neutral per DST-2/A2)**: `res(L_max=10) < Level-2(L_max=10) = 1e-3` (binding inequality) **AND** `res(L) ∝ L⁻³` across {8,10,12} (the FLOWING signature confirming `M` is the bound observable — the multiplicative-normalization FLOWING check, contrasting the analytically-FLAT sign channel); C_1 sign REPORTED as a diagnostic (the §VII.AF.1-vs-§VII.AU fork, NOT pre-registered as a direction). On PASS → Level-3 row HELD → SATISFIED, §VII.CB full REGISTRY-PASS. **Genuine gate (could FAIL — §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling can under-perform at finite L).**
4. **Effort**: ~1.0 (three L_max points, each a finite-L spectral-triple M₂(ℂ) trace; the operator-representation of `T^{(IV)}` on `H^{≤L} ⊗ ℂ²` + the `P_a₂` projection is the new build, distinct in KIND from the s105 radial-acoustic compute).

**My one sharper follow-up for transit (R3).** On the CF-S107 build (input (a)): when `T^{(IV)}` is lifted from the continuum acoustic-discriminant `Γ_sub(r)` to an operator on `H^{≤L} ⊗ ℂ²`, does the type-IV side have a CANONICAL lift (a specific map from the radial profile `v(r)` to the finite-L Nambu-doublet operator), or is the lift itself a modeling choice CF-S107 must pre-register? This decides the gate's PRU-completeness: if the lift is canonical (forced by the relay-Compton-radius ↔ spectral-triple correspondence), CF-S107 is fully specified; if it is a choice, the lift-convention is a machinery pin that must be declared at CF-S107 plan-freeze (else the FLOWING-vs-FLAT check could be lift-dependent). I do NOT think this changes the disposition (the hold stands either way), but it decides whether CF-S107 is a clean single-gate compute or carries a lift-convention sub-pin. This is the last constructibility detail I hand to your R3 Turn-B closeout.

---

## Round 2 — transit: Cross-Synthesis

### CONVERGENCE

The substantive physics is settled, and van-den-dungen's R2 did the one thing a cross-axis adjudication is for: it took the type-IV substrate findings I supplied in R1 and re-cast them in the HKR/Kasparov frame where they become structural statements, not axis-local observations. I record four convergences, each naming the specific R2 move that I accept as binding for the R3 landing.

**CONV-T-1 — I accept van-den-dungen's CONV-1 elevation: the analytic sign-saturation identities are now a THEOREM on my axis, not a prior.** In Re:VDD1/T1 I gave the two substitution chains (`Mach_core = exp(1/2) ⇒ r_g = r₀ = 1` EXACTLY; `A − 2J = 1.0 − 2·0.5 = 0` EXACTLY ⇒ the `(t/4m)(A−2J)` ANEC-wall term vanishes ⇒ `anec = 1`). van-den-dungen's CONV-1 reads these correctly as the resolution of the fork he could not close from the envelope side: `S(L) = (r_g, anec)` is **fixed-anchor saturation** (Reading B), NOT a co-convergent proxy (the surviving Reading-A form). I confirm the framing he adopts: these are not numerical coincidences but closed-form identities of the type-IV acoustic profile — `r_g = 1` an acoustic-geometry / restoration-surface identity (where the supersonic→subsonic crossover sits in space, set by the substrate-derived core Mach `√e`), `anec = 1` an angular-momentum-GFF conservation identity (`A = 2J` spin-orbit balance, Dumitru-Noronha 2505.09720). What I accept as the *upgrade*: in R1 these were two substitution chains I presented as substrate facts; van-den-dungen's CONV-1 correctly promotes them to "a theorem on the type-IV side" that *settles the disposition fork* — the type-IV channel CANNOT produce a co-convergent proxy because both invariants are pinned by L_max-free analytic identities. Reading B is established, and the proof is on my axis. I own that and it stands for R3.

**CONV-T-2 — I accept CONV-2's two-part constructibility framing and adopt his correction to my own R1 wording's risk.** van-den-dungen's CONV-2 restates my T2 verdict precisely: (Part A) `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` is a well-defined finite-L spectral-triple trace whose `L→∞` image is `g_M = a_2_FW_zeta = 2776.165389`, so the bridge's Level-1 is NOT a pure class-identity-with-no-scalar-image and a Tier-1 anchor CAN exist; (Part B) it is UNBUILT — not "anchored elsewhere," but requiring a genuinely DIFFERENT compute (operator-lift of `T^{(IV)}` to `H^{≤L} ⊗ ℂ²` + `P_a₂` projection + M₂ trace), NOT an L-scan of the s105 radial-profile artifact (which has no L_max axis — `L_max=10` enters only through `c_BLV`, cancelling in the dimensionless ratios). I credit the sharpening van-den-dungen draws from my T2 Part B: my phrase "not-yet-anchored" could be misread as "anchored-elsewhere-just-not-collected"; the correct reading is **UNBUILT**, and that *strengthens* the case for holding NOW (the supplied anchor is a different quantity, and the bound quantity is uncomputed — not a degraded proxy of it). The disposition lands where his VDD-side prior pointed (Tier-1-eligible-IN-PRINCIPLE, held-pending-a-NEW-CF) but for the diagnosis-strengthening reason, exactly as he records.

**CONV-T-3 — I accept the three-held-reasons / three-tags table (CONV-3) and his A3 patch string AS THE R3 LANDING TEXT, with the DST-1 two-axis split folded in.** van-den-dungen's CONV-3 table is the load-bearing convergence artifact, and I lock it:

| Entry | Held-reason | Tier status | Discharge path |
|:------|:------------|:------------|:---------------|
| **§VII.AX.OP-PROJ** (n_PBH) | dimensionful magnitude on a genuinely DIVERGENT channel (verified: gate `S94-N-PBH-TRUNCATION-ANCHOR`, `tier_class=TIER-2-DIMENSIONFUL`, `level3_m3=REGISTRY-PASS-INELIGIBLE-HELD`, `lim_N_eigs=+inf`) | Tier-2-DIMENSIONFUL | PERMANENT hold (no in-channel anchor exists) |
| **§VII.AV** | envelope is a SCHEMATIC Casimir-proxy; FULL-PV gave `α_PV = 6.26` (≠ schematic 3) | proxy-refinement (deferred-pending) | FULL-physical envelope recompute |
| **§VII.CB** (this entry) | supplied anchor is SIGN-STRUCTURE saturation, channel-ORTHOGONAL to the bound a₂-magnitude; magnitude channel Tier-1-eligible-in-principle but UNCOMPUTED | Tier-1-eligible-IN-PRINCIPLE | CF-S107 NEW `Tr_{M₂}(P_a₂·T^{(IV)})→g_M` compute (NOT an L-scan of the s105 sign-channel artifact) |

van-den-dungen's A3 patch string (lines 404–409) is the exact registry-annotation text mack will land at R3; I have read it in full and I concur with every clause, including the diagnosis(Axis-1)/disposition(Axis-2) split he added per DST-1 and the "sign-residual retained as a separate Element-1 witness" clause. As the final lander I treat this string as **R3-ready** — I will reproduce it verbatim in the R3 Effected-In-Session block as the ROUTED-TO-MACK patch (before/after + line anchors), not re-derive it. His A3 also confirms my T3-Q3 held-tag (the type-IV-specific cause: SIGN-STRUCTURE saturation, channel-ORTHOGONAL, Tier-1-eligible-in-principle) is the right tag text. T3-Q3 is answered YES; the wording is converged.

**CONV-T-4 — I accept the FLAT-by-analytic-structure upgrade (CONV-4) and its mapping onto the K=3-MANDATORY multiplicative-normalization fingerprint.** In Re:VDD4 I removed van-den-dungen's VDD4 hedge ("plausibly L-flat") by proving the sign residual is `L_max`-FLAT *by the analytic structure*: `r_g` and `anec` are invariant under uniform rescaling of `c_s²` (the only L-dependence, via `c_BLV`), so `‖S(L) − {1,1}‖` is set by the bisection-root tolerance (TOL_ROOT=1e-8) and the ANEC-quadrature floor (TOL_ANEC=1e-9) at EVERY L_max. van-den-dungen's CONV-4 correctly identifies this as the `math-scripts.md §"Multiplicative-normalization cancellation invariants"` 2-bit FLAT-vs-FLOWING fingerprint (K=3 MANDATORY): `S` factors as `w(L_max)·g(geometry)` with `w` (via `c_BLV`) cancelling in the dimensionless ratios, so the log-derivative annihilates `w(L_max)` and `S` is L_max-INVARIANT by structural identity. I accept the consequence he draws for CF-S107 scope: **CF-S107 does NOT need to L-scan the sign channel (its FLAT signature is analytic, derivable without running it); CF-S107 must compute the OTHER curve — `‖M_L − g_M‖` at `L ∈ {8,10,12}`, to exhibit its L⁻³ FLOWING signature.** The contrast sign-residual-FLAT (analytic, by cancellation) vs magnitude-residual-FLOWING-as-L⁻³ (CF-S107 codim-1 shell deficit) is the clean empirical discriminator that they are different observables.

**CONV-T-5 — I accept A1 (the operator-form agreement) and A2 (C_1-blindness), which settle the two technical inputs to CF-S107.** van-den-dungen's A1 answers my T3-Q1 YES: `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` IS the correct finite-L pre-image of the HKR `L→∞` boundary map, with the sharpening that the strictest Kasparov form is the Connes-Karoubi cocycle pairing `⟨[φ_{a₂}], [Ch(P_a₂)]⟩` — but the plain M₂ trace and the cocycle pairing have the SAME `L→∞` image (`g_M`) and the SAME leading rate (both are the codim-1 outermost-shell deficit; the trace IS the residue of the cocycle pairing at `s=3` per CM-1995 §III.4). So for CF-S107's PURPOSE (FLOWING-L⁻³ residual + `< 1e-3` check) my T2 Part C plain-trace operator form is correct and sufficient; the cocycle form matters ONLY for the C_1 *coefficient* (a diagnostic, not the gate). This removes the one open scoping risk in my T2 (whether the HKR side needed a different pre-image — it does not, to leading order). A2 answers my T3-Q2 NO (the shriek-map geometry is BLIND to the C_1 sign); I lock that in DST below as binding on the gate spec. **Net of CONVERGENCE:** all four W-2 adjudication questions have agreed answers (DIFFERENT observable / NEITHER-tier-but-Tier-1-eligible-in-principle / §VII.AX structure-permanent-Level-3-held split / non-load-bearing FLAT-by-identity margin); the A3 patch string and the A4 CF-S107 spec are R3-land-ready.

### DISSENT

The physics has converged; my R2 dissent is not against van-den-dungen's two R2 dissents — it is the FINAL-LANDER's binding LOCK of both, plus one new constructibility constraint his A4 question surfaced that I must pin before R3. As the agent who fills the R3 patch, my acceptance here is what makes DST-1 and DST-2 load-bearing on the registry annotation and the CF-S107 spec. I record each as a sealed position, with NEW evidence, not a restatement.

**DST-T-1 (LOCK of van-den-dungen's DST-1 — both orthogonal legs are encoded, NOT collapsed into one severity ordering; my acceptance binds the R3 patch).** van-den-dungen's DST-1 resists collapsing his VDD2 "less severe than §VII.AX" (Axis-2) and my Re:VDD2 "more defective than §VII.AX" (Axis-1) into a single "how bad is §VII.CB" scalar, on the algebra-axis-orthogonality ground that the two legs are the two non-commensurable halves of the Tier-1/Tier-2 gate. **I accept this in full and I LOCK it: the R3 patch MUST carry BOTH legs explicitly, and they MUST NOT be averaged.** Here is the substitution chain that proves they are orthogonal (not a single ordering), so a future reader cannot re-collapse them:

```
Def Axis-1 (anchor-correctness): is the SUPPLIED Level-3 number the bound observable?
   §VII.AX:  YES — anchored n_PBH, the right (divergent) channel.        [score: BETTER]
   §VII.CB:  NO  — anchored S=(r_g,anec), a channel ORTHOGONAL to M.      [score: WORSE]
Def Axis-2 (in-principle-constructibility): can a CORRECT anchor EXIST for this bridge?
   §VII.AX:  NO  — n_PBH divergent (lim N_eigs = +inf, verified), no in-channel anchor ever.  [score: WORSE]
   §VII.CB:  YES — M(L)=Tr_{M₂}(P_a₂·T^{(IV)})→g_M is a convergent scalar.  [score: BETTER]
Cross-product: §VII.AX = (Axis-1 BETTER, Axis-2 WORSE);  §VII.CB = (Axis-1 WORSE, Axis-2 BETTER).
   ⇒ The two entries swap rank between the axes. A single scalar ordering CANNOT reproduce
     a rank-swap ⇒ the axes are NOT reducible to one ordering. [orthogonality, by the rank-swap]
Conclusion: diagnosis (Axis-1) and disposition (Axis-2) measure different things and must
   be reported as a 2-tuple, never a scalar. The held-tag is keyed by Axis-2 (constructibility
   ⇒ HELD-PENDING-CF, not PERMANENT); the wrong-channel narration is keyed by Axis-1.
```

The rank-swap is the structural proof, and it is NEW (van-den-dungen argued orthogonality from the algebra-axis discipline; I prove it by exhibiting the rank-swap that no scalar ordering admits). This is exactly the `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` discipline at the disposition layer: Axis-1 lives on the anchor-correctness leg, Axis-2 on the constructibility leg, and conflating them is the failure DST-1 names. **Binding consequence for R3:** the mack patch (van-den-dungen's A3 string, which I locked in CONV-T-3) ALREADY carries both — its "DIAGNOSIS (Axis-1, anchor-correctness): ... more anchor-defective than §VII.AX" and "DISPOSITION (Axis-2, constructibility): ... held-pending-a-CONSTRUCTIBLE-recompute, NOT a permanent hold" clauses are the two legs, separately stated. I confirm they are encoded and NOT collapsed, and I will preserve both clauses verbatim in the R3 Effected-In-Session patch text. DST-1 is sealed.

**DST-T-2 (LOCK of van-den-dungen's DST-2 — CF-S107 is DIRECTION-NEUTRAL on the C_1 sign; any pre-registered C_1 direction is a Class-8.2 PRU smuggle; my acceptance binds the CF-S107 spec).** van-den-dungen's DST-2 dissents from any reading that the CF-S107 gate can pre-register an over/under-performance DIRECTION from the shriek-map geometry alone, because (A2) the fibre-integration geometry pins the LEADING exponent `α = d−1 = 3` but is BLIND to the subleading `C_1` sign — the C_1 coefficient is set by the cocycle/projector geometry at the pole, not the base dimension. **I accept this and I LOCK CF-S107 as direction-neutral, with the framework's own ledger as the binding proof (verified this turn against the registry).** The proof that C_1 sign is observable-specific — and therefore NOT pre-registerable — is on disk:
- Registry line 14948 (verbatim): "§VII.AU.OP-PROJ at the same d=4 substrate-distance-1 pole `s=3` exhibits the OPPOSITE empirical signature ... **positive subleading C_1** ... The sign-flip between §VII.AF.1.OP-PROJ (negative C_1) and §VII.AU.OP-PROJ (positive C_1)".
- Registry line 14946 (verbatim): "subleading coefficients C_1 (and higher) **are observable-specific and reflect the cocycle/projector geometry** at the substrate-distance-1 pole" — explicitly NOT base-dimension-pinned.
- The §VII.AU finite-L under-performance anchor is itself computed: gate `S92-W5-CF-S92-W2-2-LMAX14-VII-AU-OP-PROJ-...` PASS, `W6_1_anchor_value = 2.6926` (`α_b` window 2.60→2.69, ABOVE the asymptotic 3 ⇒ under-performance).

Two siblings on the IDENTICAL `(d=4, s=3)` base-dimension structure carry OPPOSITE C_1 signs. By the contrapositive: if the shriek-map geometry pinned C_1, the two siblings could not differ — they do, therefore it does not. This is the same topology≠analysis boundary van-den-dungen is bound to, made operational: **the C_1 sign for the type-IV EMT bridge is an OUTPUT of CF-S107, not an input.** Binding consequence, and the NEW piece I add as final lander: I tie the prohibition to the rule that names it. Pre-registering "we expect §VII.CB to over-perform" (or under-) would be a **Class-8.2 verifier-rubric pre-registration failure** (`epistemic-discipline.md §"Verifier-Rubric Pre-Registration (Class 8.2)"`) — specifically a *dual-prior smuggle*: it would re-allocate posterior mass to a track (over-performance) before the discriminating compute, which `epistemic-discipline.md §"Dual-prior pre-registration as track-discriminator"` forbids unless the prior is symmetric. The §VII.AF.1/§VII.AU split PROVES the prior is NOT symmetric-by-geometry — it is genuinely 50/50-until-computed across the two documented siblings. **Therefore the CF-S107 gate (CONV-T-3's A4 spec) pre-registers ONLY: (a) the binding inequality `res(L_max=10) < 1e-3`, and (b) the FLOWING-as-L⁻³ scaling check across {8,10,12}; with (c) C_1 sign REPORTED as a DIAGNOSTIC (the §VII.AF.1-vs-§VII.AU fork left OPEN until computed).** I confirm the A4 spec already encodes exactly this (its Gate field reads "direction-neutral per DST-2/A2 ... C_1 sign REPORTED as a diagnostic ... NOT pre-registered as a direction"), and I will reproduce it verbatim in the R3 Carry-Forward Computations block. DST-2 is sealed: CF-S107 is a GENUINE gate (could FAIL — §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling under-performs at finite L), direction-neutral on C_1.

**DST-T-3 (NEW — the lift-convention question van-den-dungen's A4 follow-up raised IS a CF-S107 machinery sub-pin, and I answer it from the transit axis: the lift is a CHOICE, so it MUST be declared at CF-S107 plan-freeze).** van-den-dungen's closing A4 follow-up asks whether, when `T^{(IV)}` is lifted from the continuum acoustic-discriminant `Γ_sub(r)` to an operator on `H^{≤L} ⊗ ℂ²`, the type-IV side has a CANONICAL lift or whether the lift is a modeling choice CF-S107 must pre-register. **From the substrate physics I own, the answer is: the lift is NOT yet canonical — it is a modeling choice, and CF-S107 MUST declare a lift-convention machinery sub-pin at plan-freeze.** The substitution chain for why:
```
The s105 artifact gives T^{(IV)} as a CONTINUUM radial function Γ_sub(r) = c_s² − v(r)² on r ∈ [0.001, 5.0].
The CF-S107 target M(L) = Tr_{M₂(ℂ)}(P_a₂ · T^{(IV)})|_L requires T^{(IV)} as an OPERATOR on H^{≤L} ⊗ ℂ².
The map {radial function v(r)} → {finite-L Nambu-doublet operator} is NOT fixed by s105:
   - s105 never represents v(r) on the spectral triple — it evaluates Γ_sub at radial grid points.
   - The relay-Compton-radius ↔ spectral-triple correspondence (r ↔ which spectral data?) is UNSPECIFIED
     in the existing artifact — r is a continuum radial coordinate, not a function of D_K eigenvalues.
   ⇒ At least one modeling choice enters: how v(r) (or Γ_sub) maps to a multiplication/convolution
     operator on H^{≤L}, and how the relay-Compton scale sets the r ↔ D_K-spectrum dictionary.
Conclusion: the lift is a CHOICE. ⇒ CF-S107 carries a lift-convention machinery sub-pin (PRDR per
   epistemic-discipline.md §"Pre-Registration Completeness").
```
Why this is a genuine dissent-grade constraint and not a footnote: an UNPINNED lift is a **PRU Class-8 machinery-pin cardinality failure** (`epistemic-discipline.md §"Pre-Registration Completeness"`) — the FLOWING-vs-FLAT check itself could become lift-dependent if the lift is left free at runtime (a different lift could in principle produce a different `res(L)` scaling, which would make the L⁻³ FLOWING signature an execution-time artifact rather than a substrate prediction). I agree with van-den-dungen that this does **NOT** change the DISPOSITION (the hold stands either way — the supplied sign-anchor is category-mismatched regardless of how the magnitude channel is later lifted). But it changes the CF-S107 spec: CF-S107 is a clean single-gate compute ONLY if the lift-convention is declared at its plan-freeze; otherwise it carries a lift-convention sub-pin that must be enumerated (PRDR dry-run) before the gate fires. **Binding consequence for R3:** I will add a fifth annotation to the A4 4-field CF-S107 spec in the R3 Carry-Forward Computations block — a lift-convention machinery sub-pin requirement (the relay-Compton-radius ↔ spectral-triple `r ↔ D_K-spectrum` dictionary + the `Γ_sub(r) →` operator map MUST be pinned at CF-S107 plan-freeze, or declared-diagnostic if a canonical lift is found). This is the one piece of CF-S107 that R2 must surface so S107's plan-author does not freeze an under-specified gate. This is NEW (it arises only from van-den-dungen's A4 follow-up + the type-IV internals) and it is mine to answer as the constructibility-axis owner.

### EMERGENCE

Joining van-den-dungen's R2 (his EMG-1 through EMG-4, the A1 cocycle/trace equivalence, the A2 C_1-blindness) with the type-IV substrate physics produces three insights that neither R1 axis held — and that go beyond this single registry entry.

**EMG-T-1 — The §VII.CB defect is a clean instance of a general FALSIFIER-DESIGN principle: a cross-pillar bridge can fail registry-PASS not because its envelope is wrong, but because the substrate compute that produces the Level-3 anchor measures the WRONG HALF of a sound cohomology class — and the discriminator is whether the anchor FLOWS or is FLAT under L_max.** van-den-dungen's EMG-1 (the a₂ class has two readable halves: SIGN/topological and MAGNITUDE/analytic; the type-IV compute realizes only the SIGN half) and EMG-2 (the FLAT-vs-FLOWING fingerprint is the framework-canonical discriminator between the halves) combine, on my axis, into a *forward-looking falsifier-design rule*: for ANY cross-pillar bridge whose substrate-IS observable is produced by a sign-structure / topological compute (an acoustic-discriminant, an index, an orientation datum), the supplied Level-3 anchor will be `L_max`-FLAT (a multiplicative-normalization-cancellation invariant), and FLAT is the fingerprint that it is NOT the convergence witness the binding envelope bounds. The transit-dynamics reading sharpens *why*: a sign-structure observable is the *topological* content of a Bogoliubov-type channel (the sign of a discriminant is which side of a phase boundary you are on — it does not scale with truncation), whereas a magnitude observable is the *analytic* content (the actual occupation number / spectral moment, which converges as the spectral sum fills in). The two cannot be confused once you ask "does it flow under L_max?" — and the framework already has the K=3-MANDATORY `math-scripts.md` tool that asks exactly that. The emergent design rule: **a Level-3 anchor whose multiplicative-normalization fingerprint is FLAT cannot be the convergence anchor of a binding envelope; the registry-PASS criterion silently requires a FLOWING anchor.** This is general — it applies to FWD-C1/C2/C3 and any future bridge — and it is the cleanest statement of what §VII.CB taught.

**EMG-T-2 — The "compute the actual mode-by-mode quantity; do not infer it from the envelope" discipline is the SAME discipline across transit-dynamics and cross-pillar-bridge registration — and §VII.CB is its bridge-axis instance.** My standing fold-impulsive lesson (S100b W5-1: `|β_pivot|²` must be COMPUTED on the sharp-boundary transfer matrix; smooth-segmentation INVALIDLY accumulates artificial reflections — the wrong computational route gives a structurally-wrong answer that LOOKS plausible) and van-den-dungen's topology≠analysis boundary (the Kasparov product gives the class identity, not the finite-L number) are, jointly, ONE discipline: **the envelope/class tells you the RATE and the LIMIT; it does not tell you the finite-L NUMBER, which must be computed mode-by-mode (transit) / pole-by-pole (bridge).** §VII.CB inferred the convergence quality from "the envelope is binding" instead of computing `‖M_L − g_M‖` — structurally identical to inferring `|β|²` from "the transit is supersonic" instead of solving the mode equation. The emergent cross-domain point: the transit-dynamics rigor ("solve the mode equation; do not assume the Bogoliubov coefficient from the regime") and the bridge-registration rigor ("compute the finite-L residual; do not assume it from binding-eligibility") are the SAME epistemic move at different scales — and W9-5 (FALSIFIED-at-K=2: a quantity assumed to FLOW instead stayed structured) is the framework's standing precedent that the move is mandatory. §VII.CB is the dual (a quantity assumed to be a convergence witness is actually FLAT); both are "assume-vs-compute the scaling" failures.

**EMG-T-3 — The two halves of the a₂ class map onto the topological/analytic split of a Bogoliubov channel, and this gives a substrate-first reading of WHY the sign-anchor was tempting: it is the genuinely-computed, beautifully-clean half — determinacy reads as relevance.** van-den-dungen's EMG-1/EMG-3 frame the a₂ class as one object with a SIGN half (fixed by fibre-integration geometry, FLAT) and a MAGNITUDE half (fixed by cocycle/projector geometry at the pole, FLOWING). On the transit axis this is exactly the structure of a parametrically-amplified mode: the **sign of the discriminant** `Γ_sub = c_s² − v²` is the analog of "is the mode in the unstable tongue?" (a topological / which-band datum — the acoustic white-hole sign-flip, type-IV core vs type-I exterior), and the **magnitude** `Tr_{M₂}(P_a₂·T^{(IV)})` is the analog of the actual occupation `|β_k|²` produced (the analytic amount). The type-IV compute realized the white-hole SIGN topology (g_core<0, g_ext>0, one crossover, `A=2J` to 8 digits) cleanly and beautifully — and THAT is why it was offered as the Level-3 anchor: it is the most *physically determinate* number in the entry (a real, clean acoustic-white-hole / GFF-conservation result). The emergent warning, substrate-first: **determinacy is not relevance.** The sign half is determinate because it is a saturated topological identity; the magnitude half is the relevant-but-uncomputed analytic content. The §VII.CB lesson, in transit-dynamics terms: a clean COMPUTED sign-structure (which tongue / which band / which side of the white-hole horizon) can masquerade as a convergence witness precisely because it is computed cleanly — but the registry-PASS criterion wants the occupation number, not the band-membership sign. Keep the sign result (it is real substrate content — the acoustic white-hole structure of the relay's own supersonic core); just do not let its 8-digit cleanliness stand in for the magnitude convergence it is orthogonal to. This is the substrate-IS reading van-den-dungen's EMG-1 gestured at, grounded in the Bogoliubov topological/analytic split I own.

---

## Round 3 — van-den-dungen: Follow-up

### CONVERGENCE

The final converged position, stated once as a tight block. Both axes — my HKR/Kasparov/Connes-Karoubi envelope axis and transit's type-IV substrate-physics axis — agree on every clause, and transit's R2 (CONV-T-1…5, DST-T-1, DST-T-2, DST-T-3) locked the disposition, the patch text, and the CF spec. Nothing in R3 reopens; this records the settled adjudication.

**THE CONVERGED POSITION (Reading B, §VII.AX surgical-demotion split).** §VII.CB's on-disk REGISTRY-PASS does **NOT** rest on a binding-channel-consistent Level-3 anchor. The Level-2 envelope `L⁻³` is a binding bound on the **magnitude-convergence channel** `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L → g_M` (`g_M = a_2_FW_zeta = 2776.165389`, canonical-verified this turn, S88-A-N-FW-CANONICALIZATION, not superseded); but the supplied Level-3 number `max(|r_g−1|, |anec−1|) = 7.500e-09` measures the **sign-saturation channel** `S(L) = (r_g, anec) → {1,1}` — a DIFFERENT observable, channel-ORTHOGONAL to `M`, forced to its integer anchors by the two L_max-free analytic identities transit proved in T1 (`Mach_core = exp(½) ⇒ r_g = r₀ = 1` EXACTLY; `A − 2J = 1.0 − 2·0.5 = 0` EXACTLY ⇒ `anec = 1`), and **L_max-FLAT by the K=3-MANDATORY multiplicative-normalization cancellation fingerprint** (`math-scripts.md §"Multiplicative-normalization cancellation invariants"`: `c_BLV` cancels in the dimensionless ratios). The 1.333e5× margin is therefore a **non-load-bearing artifact** (corpus §20): large because the two divided numbers are different observables (an ANEC-quadrature floor over a magnitude envelope), NOT because the magnitude converged 1.333e5× early.

**DISPOSITION = STAGE-3-PERMANENT-STRUCTURE + Level-3-HELD.** Per `epistemic-discipline.md` verdict permanence, the on-disk `S106-W3-3` REGISTRY-PASS line (audit `293105a2…`) STAYS on disk and is **not** edited; the workshop disposition is a **NEW pinned position** landed by mack-cosmic-bridge as a sole-writer registry annotation on §VII.CB — the §VII.AX surgical-demotion pattern (corpus §25.1: "Level-3-row demotions … theorem-structure STAGE-3-PERMANENT tags … left untouched"), explicitly NOT a retroactive edit. The split:
- **Theorem-STRUCTURE: STAGE-3-PERMANENT** — the binding `L⁻³` envelope (Element 4, audit `943b17ad…`/`645ac895…`), the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`, and the complete 5-anatomy are sound and untouched (Stage-2 PASS-AND on all non-Level-3 clauses).
- **Level-3 row: HELD — `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`** — the supplied anchor witnesses the wrong half of the a₂ class (sign, not magnitude); Tier-1-eligible-IN-PRINCIPLE (the magnitude channel is a convergent scalar, unlike §VII.AX's divergent `n_PBH`), so the hold points at a **constructible** recompute, not a permanent hold.
- **Sign-residual RETAINED as a separate Element-1 witness** (NOT deleted): the `7.500e-09` is a real, clean substrate-IS result — the type-IV sign topology (`A=2J` GFF identity, restoration-surface `r_g=1`, g_core<0 ANEC-violating core, g_ext>0 type-I exterior, n_crossovers=1) realized to 8 digits — demoted from "the Level-3 anchor of the bridge" to "a sign-structure saturation witness of Element 1."

**THE CONDITIONAL CF-S107 MATERIALIZES, DIRECTION-NEUTRAL.** `CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR` — a NEW compute of `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` at `L ∈ {8,10,12}` (a finite-L spectral-triple trace, NOT an L-scan of the s105 sign-channel radial-profile artifact) — is the discharge path. It is **direction-neutral on the C_1 sign** (DST-2/DST-T-2: the shriek-map geometry pins the leading exponent `α=d−1=3` but is BLIND to C_1; the §VII.AF.1-negative / §VII.AU-positive split on the IDENTICAL `(d=4,s=3)` structure is the framework's own proof that C_1 is observable-specific, NOT geometry-pinned), and it carries a **lift-convention machinery sub-pin** (DST-T-3: the `Γ_sub(r) →` finite-L Nambu-doublet operator lift is a CHOICE, not yet canonical, so the `r ↔ D_K-spectrum` dictionary MUST be declared at CF-S107 plan-freeze or declared-diagnostic if a canonical lift is found). On PASS (`res(L_max=10) < 1e-3` ∧ FLOWING-as-`L⁻³`), the Level-3 row promotes HELD → SATISFIED and §VII.CB earns full REGISTRY-PASS. CF-S107 is a GENUINE gate — §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling can under-perform at finite L.

That is the whole adjudication: the structure stands permanent, the Level-3 row holds on the right (orthogonal-channel) ground, the sign-residual survives as a sound Element-1 witness, the margin is re-scoped non-load-bearing, and the discharge is a constructible direction-neutral CF.

### DISSENT

**None.** This is the verified null, not a courtesy. The substantive physics converged at R2 and my two R2 dissents (DST-1, DST-2) were NEW structural constraints, not objections — and transit LOCKED both faithfully at R2. I confirm the locks are exact (a confirmation, NOT a re-opening; DST-1/DST-2 are sealed and I do not re-litigate them):

- **DST-T-1 is a faithful lock of DST-1, and STRENGTHENS it.** I argued the two severity axes are orthogonal (Axis-1 anchor-correctness; Axis-2 in-principle-constructibility) from the algebra-axis-orthogonality discipline and must NOT be collapsed into a single "how bad is §VII.CB" scalar. Transit's DST-T-1 supplies the structural PROOF I did not have: the **rank-swap** — §VII.AX = (Axis-1 BETTER, Axis-2 WORSE), §VII.CB = (Axis-1 WORSE, Axis-2 BETTER) — and a single scalar ordering cannot reproduce a rank-swap, so the axes are provably non-reducible to one ordering. This is exactly my point made rigorous; I accept it without reservation. The binding consequence is preserved: the mack patch carries BOTH legs separately (DIAGNOSIS keyed to Axis-1 "wrong-channel, more anchor-defective than §VII.AX"; DISPOSITION keyed to Axis-2 "Tier-1-eligible, held-pending-CF, NOT permanent"), never averaged. Faithful.

- **DST-T-2 is a faithful lock of DST-2.** I dissented from any CF-S107 pre-registration that smuggles in an over/under-performance DIRECTION from the shriek-map geometry, on the topology≠analysis ground (the codim-1 base-dimension residual fixes the leading rate; the C_1 coefficient is set by the cocycle/projector geometry at the pole, which the base dimension does not see). Transit's DST-T-2 locks CF-S107 as direction-neutral, ties the prohibition to the rule that names it (a pre-registered C_1 direction would be a Class-8.2 verifier-rubric / dual-prior smuggle — `epistemic-discipline.md §"Verifier-Rubric Pre-Registration"` + §"Dual-prior pre-registration as track-discriminator"), and verifies the framework's own ledger as the binding proof of asymmetry-by-observable (registry line 14948 §VII.AU positive C_1; line 14946 "observable-specific … cocycle/projector geometry"; §VII.AU `W6_1_anchor_value = 2.6926` under-performance computed). The prior is genuinely 50/50-until-computed across the two documented siblings. Faithful, and operationalized better than my R2 statement.

- **DST-T-3 (transit's NEW R2 dissent) answers my A4 follow-up correctly, and I concur.** I asked whether the `Γ_sub(r) →` finite-L operator lift is canonical or a choice. Transit's substrate-axis answer is correct: the lift is a CHOICE (s105 never represents `v(r)` on the spectral triple — it evaluates `Γ_sub` at radial grid points; the relay-Compton-radius ↔ `D_K`-spectrum dictionary is unspecified in the existing artifact). So CF-S107 carries a lift-convention machinery sub-pin (PRDR per `epistemic-discipline.md §"Pre-Registration Completeness"`); an unpinned lift is a PRU Class-8 cardinality failure that could make the FLOWING-vs-FLAT check lift-dependent. I confirm transit's correct scoping: this does **NOT** change the DISPOSITION (the hold stands either way — the supplied sign-anchor is category-mismatched regardless of how the magnitude channel is later lifted), it only adds the 5th-annotation machinery sub-pin to the CF spec. No dissent.

So the verified null is genuine: zero open substantive tension between the two axes. The only R3 content is the verbatim hand-off (QUESTIONS) so transit's R3-B landing is mechanical.

### EMERGENCE

One insight crystallized only at R3, from the join of transit's locks (the DST-T-1 rank-swap + the DST-T-3 lift-as-choice) with my standing Kasparov boundary. It is NEW — neither R2 axis held it.

**EMG-VDD-R3 — the §VII.CB held-disposition is a clean two-functor decomposition of the bridge, and the rank-swap + lift-as-choice expose that the topology→analysis gap appears TWICE in the SAME entry, at two different layers.** EMG-1/EMG-3 (R2) established that the a₂ class `[T^{(IV)}]_{a₂,HKR}` is one object with a SIGN half (topological, fibre-integration-geometry-fixed, FLAT) and a MAGNITUDE half (analytic, cocycle/projector-geometry-fixed, FLOWING). At R3, joining transit's two locks reveals the gap recurs at a SECOND, finer layer:

- **Layer-1 gap (the Element-5 selection):** the bridge offered the SIGN-half residual as the analytic-half anchor — topology stood in for analysis at the channel level. This is the disposition's core (Reading B). The discriminator is the FLAT-vs-FLOWING fingerprint (EMG-2).
- **Layer-2 gap (the C_1 sign, DST-T-2):** *within* the magnitude half, the LEADING exponent `α=d−1=3` is topological (shriek-map base dimension) but the SUBLEADING `C_1` is analytic (cocycle/projector geometry at the pole) — and the rank-swap's Axis-2 ("a correct anchor CAN exist") does NOT entail "the correct anchor will PASS," precisely because the C_1 sign (which decides over/under-performance at L_max=10) is the analytic content the topology cannot pin. The §VII.AF.1/§VII.AU split IS this Layer-2 gap made empirical.

The emergent unification: **the same topology≠analysis boundary I am bound to ("Kasparov gives the class identity + the convergence RATE, not the finite-L NUMBER") acts at BOTH layers of §VII.CB — first selecting the wrong channel (Layer-1), then leaving the right channel's finite-L verdict genuinely open (Layer-2).** Transit's DST-T-3 (the lift is a CHOICE) is the THIRD appearance of the same boundary at the operational layer: the cohomology-class identity `[T^{(IV)}] = [g_M]` is canonical (topology), but the finite-L operator representation of `T^{(IV)}` on `H^{≤L}⊗ℂ²` is NOT forced by the class — the analytic realization requires a modeling choice the topology does not supply. So the topology→analysis gap is not a single defect in §VII.CB; it is a structural signature appearing at the channel-selection layer (Element 5), the convergence-coefficient layer (C_1), AND the operator-lift layer (DST-T-3) — three faces of one boundary. This is the substrate-first reading: the substrate IS the finite-L type-IV EMT (one object), and at each layer where the framework reads an analytic number off it, a choice/computation is required that the cohomology class alone does not determine. The forward-looking corollary (general, beyond §VII.CB): for ANY cross-pillar bridge built from a sign-structure/topological substrate compute, expect the topology→analysis gap at all three layers — channel selection (is the anchor FLOWING?), subleading coefficient (is C_1 geometry-pinned? — no, by the §VII.AF.1/§VII.AU precedent), and operator lift (is the finite-L representation canonical? — declare it or pin it). The K=3-MANDATORY FLAT-vs-FLOWING fingerprint catches the first; DST-2 direction-neutrality guards the second; DST-T-3's machinery sub-pin guards the third.

### QUESTIONS

Final hand-off to transit (R3 Turn B, the final agent). Two deliverables, both verbatim-ready so the R3-B landing is mechanical: (1) the exact §VII.CB held-tag registry-patch string with before/after keyed to the section line anchors, carrying BOTH the Axis-1 diagnosis and the Axis-2 disposition (DST-1/DST-T-1); (2) the 4-field + lift-sub-pin (5th annotation) CF-S107 spec, direction-neutral on C_1. I do NOT effect either edit — these are transit's at R3-B, routed to mack-cosmic-bridge (the §VII.CB cross-pillar-bridge row sole writer per `feedback_mack-bridge-role.md`).

---

**HANDOFF-1 — the §VII.CB registry-patch string (route to mack-cosmic-bridge).** This is a NEW pinned annotation (verdict permanence: the `293105a2…` REGISTRY-PASS line STAYS on disk; this is the §VII.AX surgical-demotion annotation, NOT a retroactive edit). The patch touches FOUR existing lines in the §VII.CB section (`sessions/permanent-results-registry.md`, section header at **line 21968**) and appends ONE new annotation block. I confirmed the exact current text of each target line this turn.

**Patch target lines (verified, current on-disk text → annotated text):**

- **Line 21968 (header), Line 21970 (Status), Line 21986 (Element 5), Line 21994 (Level-3 ladder row), Line 22003 + 22016 (registry-PASS conclusion + substitution-chain conclusion)** — these currently assert `REGISTRY-PASS` / `SATISFIED by margin 1.333e+05×` / `registry-PASS criterion satisfied`. The surgical demotion does NOT delete or rewrite these (verdict permanence); it PREPENDS the annotation block below to the §VII.CB section (immediately after the Status paragraph, line 21970) and adds a one-line cross-reference pointer `[S106 W3 workshop: Level-3 row HELD — see disposition annotation below]` adjacent to each of the five `REGISTRY-PASS`/`margin 1.333e+05×`/`SATISFIED` assertions so a future reader cannot read the on-disk REGISTRY-PASS without hitting the held-tag.

**Annotation block to INSERT (verbatim, the A3 string locked at CONV-T-3, carrying the DST-1 two-axis split):**

> **Registry status (S106 W3 workshop disposition — NEW pinned position, verdict-permanence-preserving annotation; the on-disk `S106-W3-3` REGISTRY-PASS line [audit `293105a2…`] STAYS on disk — this is the §VII.AX surgical-demotion annotation pattern [corpus §25.1], NOT a retroactive edit):**
>
> **§VII.CB theorem-STRUCTURE: STAGE-3-PERMANENT.** The binding `L⁻³` envelope (Element 4; `S106-W3-2` audit `943b17ad…` + the independent direct re-derivation audit `645ac895…`), the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`, and the complete 5-anatomy are sound and untouched (Stage-2 PASS-AND holds on all non-Level-3 clauses).
>
> **§VII.CB Level-3 row: HELD — `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`.** The supplied Level-3 anchor `max(|r_g−1| = 2.193e-10, |anec−1| = 7.500e-09) = 7.500e-09` is a **SIGN-STRUCTURE saturation residual** — it witnesses the SIGN half of the a₂ class (`S(L) = (r_g, anec) → {1,1}`: the core/exterior topology g_core<0 ANEC-violating core, g_ext>0 type-I exterior, n_crossovers=1), forced to the integer anchors {1,1} by the analytic identities `Mach_core = exp(½) ⇒ r_g = 1` and `A = 2J ⇒ anec = 1`, and is **L_max-FLAT by the multiplicative-normalization cancellation fingerprint** (`math-scripts.md §"Multiplicative-normalization cancellation invariants"`, K=3 MANDATORY: c_BLV cancels in the dimensionless ratios). It is **channel-ORTHOGONAL** to the magnitude-convergence residual `‖HKR(Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})_L) − g_M‖` (the L_max-FLOWING-as-`L⁻³` quantity the Level-2 envelope actually bounds; `g_M = a_2_FW_zeta = 2776.165389`).
>
> **Tier status: Tier-1-eligible-IN-PRINCIPLE** (per `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` Tier-1/Tier-2 dimensional-re-anchorability gate). **DIAGNOSIS (Axis-1, anchor-correctness):** the supplied anchor is the WRONG channel — more anchor-defective than §VII.AX (which anchored its right-but-divergent channel). **DISPOSITION (Axis-2, in-principle-constructibility):** the magnitude channel `M(L) → g_M` is a CONVERGENT scalar (a well-defined finite-L spectral-triple trace), so a Tier-1 anchor is constructible — UNLIKE §VII.AX (Tier-2-DIMENSIONFUL, divergent `n_PBH`, PERMANENT hold) and UNLIKE §VII.AV (SCHEMATIC-proxy envelope; §VII.CB's envelope is FULL, NO CLASS pin). The two axes do NOT collapse to one ordering (rank-swap: §VII.AX = Axis-1-BETTER/Axis-2-WORSE, §VII.CB = Axis-1-WORSE/Axis-2-BETTER); diagnosis and disposition are a 2-tuple, never averaged. Therefore §VII.CB is **held-pending-a-CONSTRUCTIBLE-recompute, NOT a permanent hold.**
>
> **Discharge path: `CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR`** — a NEW compute of `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` at `L ∈ {8,10,12}` on the finite-L spectral triple (NOT an L-scan of the s105 sign-channel radial-profile artifact, which has no L_max axis). On PASS (`res(L_max=10) < 1e-3` ∧ FLOWING-as-`L⁻³`), the Level-3 row promotes HELD → SATISFIED and §VII.CB earns full REGISTRY-PASS. Direction-neutral on the C_1 sign (the §VII.AF.1-vs-§VII.AU fork stays OPEN until computed).
>
> **Sign-residual retained as a separate Element-1 witness** (NOT deleted): the `7.500e-09` is a real, clean substrate-IS result — the type-IV sign topology (`A=2J` GFF identity; restoration-surface `r_g=1`) realized to 8 digits — demoted from "the Level-3 anchor of the bridge" to "a sign-structure saturation witness of Element 1." The **1.333e5× margin is non-load-bearing** (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"`, corpus §20): it is large because the two quantities being divided are different observables (an ANEC-quadrature floor over a magnitude envelope), NOT because the a₂ magnitude converged 1.333e5× early.

**transit, please confirm before R3-B routing:** (i) the FOUR-line cross-reference-pointer approach + single annotation-block INSERT (after line 21970) is the right mechanical shape for mack (vs. annotating each of the five assertions inline) — both preserve verdict permanence; the INSERT-plus-pointers keeps the demotion in ONE readable block; (ii) the annotation block text above is verbatim-final (it is your A3 lock from CONV-T-3 + the DST-1 two-axis split you proved via rank-swap in DST-T-1); (iii) the section line anchor 21968 (header) is the correct routing anchor for mack.

---

**HANDOFF-2 — the CF-S107 spec (4-field + lift-convention 5th annotation; route to S107 plan via the WP Carry-Forward Computations block).** This is the A4 spec locked at CONV-T-3, with transit's DST-T-3 lift-convention sub-pin as the 5th annotation. Direction-neutral on C_1.

1. **What**: compute the magnitude-convergence Level-3 anchor for §VII.CB — `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` (a finite-L spectral-triple trace, NOT a continuum radial-profile compute), its residual `res(L) = |M(L) − g_M|/|g_M|` with `g_M = a_2_FW_zeta = 2776.165389`, at `L ∈ {8,10,12}`; report `res(L_max=10)` and the fitted scaling exponent + C_1 sign.
2. **Inputs**: (a) the S105 type-IV npz (`e2860d57…`/audit `91b36ed9…`) for the `T^{(IV)}` construction; (b) the S106 W3-2 binding-envelope npz (`a8efd183…`/audit `943b17ad…`) for the Level-2 bound `L⁻³`; (c) `g_M = a_2_FW_zeta` from `canonical_constants.py` (S88-A-N-FW-CANONICALIZATION, verified not-superseded); (d) the cached L=8/10 spectra + the L=12 master cache `s84_spectrum_cache_L12_tau019.npz`.
3. **Gate (pre-registered, DIRECTION-NEUTRAL per DST-2/DST-T-2)**: `res(L_max=10) < Level-2(L_max=10) = 1e-3` (binding inequality) **AND** `res(L) ∝ L⁻³` across {8,10,12} (the FLOWING signature confirming `M` is the bound observable — the multiplicative-normalization FLOWING check, contrasting the analytically-FLAT sign channel); **C_1 sign REPORTED as a diagnostic** (the §VII.AF.1-vs-§VII.AU fork, NOT pre-registered as a direction — a pre-registered direction is a Class-8.2 / dual-prior smuggle). On PASS → Level-3 row HELD → SATISFIED, §VII.CB full REGISTRY-PASS. **GENUINE gate (could FAIL — §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling under-performs at finite L).**
4. **Effort**: ~1.0 (three L_max points, each a finite-L spectral-triple M₂(ℂ) trace; the operator-representation of `T^{(IV)}` on `H^{≤L} ⊗ ℂ²` + the `P_a₂` projection is the new build, distinct in KIND from the s105 radial-acoustic compute).
5. **(Lift-convention machinery sub-pin — DST-T-3, MANDATORY at CF-S107 plan-freeze)**: the lift `Γ_sub(r) →` finite-L Nambu-doublet operator on `H^{≤L} ⊗ ℂ²` is a CHOICE, not yet canonical (s105 never represents `v(r)` on the spectral triple; the relay-Compton-radius ↔ `D_K`-spectrum dictionary `r ↔ {which spectral data}` is unspecified in the existing artifact). CF-S107 MUST pin the `r ↔ D_K-spectrum` dictionary + the `Γ_sub(r) →` operator map at plan-freeze (PRDR dry-run per `epistemic-discipline.md §"Pre-Registration Completeness"`), OR declare-diagnostic if a canonical lift is found. An unpinned lift is a PRU Class-8 cardinality failure that could make the FLOWING-vs-FLAT check (gate item 3) lift-dependent. Does NOT change the disposition (the hold stands regardless); it scopes whether CF-S107 is a clean single-gate compute or carries the sub-pin.

**transit, please confirm before R3-B routing:** (i) the 4-field + 5th-annotation spec above is verbatim-final for the WP Carry-Forward Computations block; (ii) the lift-convention sub-pin is correctly scoped as a CF-S107-plan-freeze machinery pin (your DST-T-3), NOT a disposition-changer; (iii) the CF gate is direction-neutral on C_1 with C_1-sign as a REPORTED diagnostic (your DST-T-2 lock). All three are your R2 locks; I am confirming the verbatim text, not re-deriving.

That is the complete mechanical hand-off. The structure is permanent, the Level-3 row holds on the orthogonal-channel ground, the sign-residual survives as an Element-1 witness, the margin is re-scoped non-load-bearing, and CF-S107 is a constructible direction-neutral discharge with a declared lift-convention sub-pin. Over to you for the R3-B verdict table, Wrap-Up, and Effected-In-Session landing.

---

## Round 3 — transit: Cross-Synthesis

### CONVERGENCE

The adjudication is closed. As the final lander I accept van-den-dungen's R3 converged position in full, confirm both HANDOFF-1 (the §VII.CB registry-patch string) and HANDOFF-2 (the CF-S107 5-field spec) are faithful and verbatim-ready, and answer his three R3 confirmation requests YES. Nothing here re-derives; this records the settled position and discharges the mechanical hand-off so the Verdict/Wrap-Up/Effected blocks below are executable.

**ACC-1 — I accept the R3 CONVERGED POSITION (Reading B, §VII.AX surgical-demotion split) as stated, clause for clause.** van-den-dungen's R3 block (lines 511–520) is the exact joint position I locked at R2 (CONV-T-1…5), re-stated once tightly. Every clause holds on my axis:
- The Level-2 `L⁻³` envelope binds the **magnitude-convergence channel** `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L → g_M`; the supplied Level-3 `7.500e-09` measures the **sign-saturation channel** `S(L) = (r_g, anec) → {1,1}` — channel-ORTHOGONAL to `M`, forced to {1,1} by the two L_max-free analytic identities I proved in T1 (`Mach_core = exp(½) ⇒ r_g = r₀ = 1` EXACTLY; `A − 2J = 1.0 − 2·0.5 = 0` EXACTLY ⇒ `anec = 1`), L_max-FLAT by the K=3-MANDATORY multiplicative-normalization cancellation fingerprint (`c_BLV` cancels in the dimensionless ratios). The 1.333e5× margin is a non-load-bearing artifact (corpus §20).
- DISPOSITION = STAGE-3-PERMANENT-STRUCTURE + Level-3-HELD: the binding envelope (Element 4), the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`, and the 5-anatomy stay STAGE-3-PERMANENT; the Level-3 row holds `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`; the sign-residual is retained as a separate Element-1 witness. Verdict permanence: the on-disk `S106-W3-3` REGISTRY-PASS line (audit `293105a2…`, verdict file line 58) STAYS on disk — the disposition is a NEW pinned position, the §VII.AX surgical-demotion annotation, NOT a retroactive edit. **I re-verified the on-disk anchor this turn**: verdict file line 58 carries `S106-W3-3-PILLAR-I-VI-IV-LANDING: PASS … REGISTRY-PASS … margin=1.333e+05x … audit_sha256=293105a2f3b2f7be…` — present and untouched.

**ACC-2 — HANDOFF-1 is faithful: I confirm the line anchors against the on-disk §VII.CB section, and answer van-den-dungen's three confirmation requests YES.** van-den-dungen asked me to confirm three things before R3-B routing (line 571). I read the actual §VII.CB section this turn (registry lines 21966–22024) and confirm:
- **(i) The FOUR-line-pointer + single-INSERT mechanical shape is correct for mack.** The §VII.CB section asserts the held-disposition's targets at exactly the pinned anchors: **line 21968** (header — "REGISTRY-PASS … STAGE-3-PERMANENT cross-pillar bridge"), **line 21970** (Status — "the Level-3 empirical anchor at canonical `L_max=10` satisfies `Level-3 < Level-2`"), **line 21986** (Element 5 — "SATISFIED by margin 1.333e+05× (registry-PASS criterion satisfied)"), **line 21994** (Level-3 ladder row — "registry-PASS criterion satisfied … margin 1.333e+05×"), **line 22003** (REGISTRY-PASS conclusion — "⇒ REGISTRY-PASS … margin 1.333e+05×"), and **line 22016** (substitution-chain conclusion — "the … bridge is REGISTRY-PASS … ∎"). That is the five REGISTRY-PASS / "margin 1.333e+05×" / "SATISFIED" assertions HANDOFF-1 names (the header line 21968 carries the first; lines 21970/21986/21994/22003/22016 the others — six anchor lines, five distinct PASS/SATISFIED assertions). INSERT-after-21970 plus a one-line cross-reference pointer at each assertion site keeps the demotion in ONE readable block AND guarantees no future reader can hit the on-disk REGISTRY-PASS without hitting the held-tag. This is mechanically cleaner than inline-annotating each of the five (which would fragment the disposition across the section); both preserve verdict permanence, but the INSERT+pointers is the right shape. **Confirmed.**
- **(ii) The annotation-block text is verbatim-final.** It is my A3 lock from CONV-T-3 + the DST-1 two-axis split I proved via rank-swap in DST-T-1. I reproduce it verbatim in the Effected-In-Session block below (no edit). **Confirmed.**
- **(iii) Line 21968 is the correct section routing anchor for mack.** It is the `### §VII.CB —` section header; the INSERT lands after the Status paragraph (line 21970), within the section. **Confirmed.**

**ACC-3 — HANDOFF-2 is faithful: I confirm the CF-S107 5-field spec verbatim-final and answer van-den-dungen's three confirmation requests YES.** van-den-dungen asked me to confirm three things (line 583):
- **(i) The 4-field + 5th-annotation spec is verbatim-final for the WP Carry-Forward Computations block.** It is the A4 spec locked at CONV-T-3 with my DST-T-3 lift-convention sub-pin as the 5th annotation. I reproduce it verbatim in the Carry-Forward Computations block below. **Confirmed.**
- **(ii) The lift-convention sub-pin is correctly scoped as a CF-S107-plan-freeze machinery pin (DST-T-3), NOT a disposition-changer.** Correct — the hold stands regardless of how the magnitude channel is later lifted (the supplied sign-anchor is category-mismatched independent of the lift); the sub-pin only scopes whether CF-S107 is a clean single-gate compute or carries a lift-convention pin. **Confirmed.**
- **(iii) The CF gate is direction-neutral on C_1 with C_1-sign REPORTED as a diagnostic (DST-T-2 lock).** Correct — the gate keys ONLY on the binding inequality `res(L_max=10) < 1e-3` + the FLOWING-as-L⁻³ scaling check; a pre-registered C_1 direction is a Class-8.2 / dual-prior smuggle (the §VII.AF.1-negative / §VII.AU-positive split on the IDENTICAL `(d=4,s=3)` structure proves the prior is 50/50-until-computed). **Confirmed.**
- **g_M canonical re-verified this turn**: `a_2_FW_zeta = 2776.165389`, gate `S88-A-N-FW-CANONICALIZATION`, Superseded=False (knowledge MCP). The CF-S107 input (c) and the magnitude-channel limit are pinned to a current, non-superseded canonical.

**ACC-4 — I accept van-den-dungen's R3 EMERGENCE (EMG-VDD-R3, the three-faces-of-one-boundary reading), and it tightens the forward falsifier-design rule I floated at EMG-T-1.** His EMG-VDD-R3 (lines 538–543) shows the topology≠analysis gap appears THREE times in the SAME entry: Layer-1 (Element-5 channel-selection: sign-half offered for analytic-half — the FLAT-vs-FLOWING discriminator catches it), Layer-2 (the C_1 sign: leading exponent topological, subleading coefficient analytic — DST-2 direction-neutrality guards it), and the operator-lift layer (the class identity is canonical but the finite-L operator representation is a CHOICE — my DST-T-3 sub-pin guards it). I accept this as the correct unification and note it sharpens my EMG-T-1 forward rule: a cross-pillar bridge built from a sign-structure/topological substrate compute must be guarded at all three layers, and the framework already supplies all three guards (the K=3-MANDATORY FLAT-vs-FLOWING fingerprint; DST-2 direction-neutrality; the DST-T-3 machinery sub-pin). The three-layer corollary is registered in EMG below as the durable forward output.

### DISSENT

**None. This is the verified null, not a courtesy.** I read van-den-dungen's entire R3 (CONVERGENCE lines 507–520, DISSENT lines 522–532 [his own verified null], EMERGENCE lines 534–543, QUESTIONS + HANDOFF-1 + HANDOFF-2 lines 545–585) and find zero open substantive tension between the two axes. The physics converged at R2; van-den-dungen's R3 DISSENT was itself a clean null (he confirmed my R2 locks DST-T-1/DST-T-2/DST-T-3 are faithful, NOT a re-opening). As the final lander I confirm the symmetric closure:

- **My R2 locks are faithfully accepted.** van-den-dungen's R3 confirms DST-T-1 (the rank-swap proof of Axis-1/Axis-2 orthogonality), DST-T-2 (CF-S107 direction-neutral on C_1, tied to the Class-8.2 / dual-prior rule), and DST-T-3 (the lift-as-choice machinery sub-pin) are exact and binding. He adds no new objection; I add none. There is nothing to re-litigate — DST-1/DST-2/DST-T-1/DST-T-2/DST-T-3 are sealed.
- **No new tension surfaced in the hand-off.** HANDOFF-1 and HANDOFF-2 reproduce text I authored or locked at R2 (the A3 patch string is mine from CONV-T-3; the A4 spec + the DST-T-3 5th annotation are mine). There is no clause in either hand-off I would alter. The only R3-B content is the mechanical landing (Verdict table, Open Questions, Wrap-Up, Effected-In-Session), which is what follows.
- **One scope-honesty note (NOT a dissent — a boundary I preserve as final lander).** The verified null is on the SUBSTRATE-PHYSICS adjudication (the four W-2 questions), the disposition, the patch text, and the CF spec. It is NOT a claim that CF-S107 will PASS — CF-S107 is a GENUINE gate that can FAIL (§VII.AU is the standing counterexample that a `(d=4,s=3)` sibling under-performs at finite L). The null records that the two axes AGREE on what is held, why, and how it discharges — not that the discharge is guaranteed. This is the honest scope of the convergence: agreement on structure and disposition, openness on the finite-L magnitude verdict (which is exactly what the direction-neutral CF preserves).

The verified null is genuine: zero open substantive tension. Reading B wins as stated; the disposition is the §VII.AX surgical-demotion split; both hand-offs are faithful and land-ready.

### EMERGENCE

One insight crystallizes at R3-B, from the join of van-den-dungen's EMG-VDD-R3 (the three-faces-of-one-boundary) with my transit-axis fold-impulsive discipline. It is the durable, forward-looking output of this workshop — general beyond §VII.CB — and it is the thing I most want the next planner to carry.

**EMG-T-R3 — The §VII.CB defect crystallizes a THREE-LAYER GUARD CHECKLIST for any cross-pillar bridge built from a sign-structure / topological substrate compute, and all three guards already exist as framework rules — so the checklist is enforceable, not aspirational.** van-den-dungen's EMG-VDD-R3 established that the topology≠analysis boundary acts at three layers of §VII.CB. Joining that with my EMG-T-1 falsifier-design rule and the transit-dynamics "compute-the-mode-by-mode-quantity, do not infer it from the envelope" discipline (EMG-T-2) yields a concrete, ordered checklist a future bridge-registration plan must run BEFORE claiming registry-PASS:

| Layer | Question to ask of the Level-3 anchor | Failure signature | The framework guard that catches it |
|:------|:--------------------------------------|:------------------|:------------------------------------|
| **L1 — Channel selection** | Does the supplied Level-3 anchor FLOW under L_max (or is it FLAT)? | FLAT residual offered as a convergence witness | `math-scripts.md §"Multiplicative-normalization cancellation invariants"` (K=3 MANDATORY) — the 2-bit FLAT-vs-FLOWING fingerprint |
| **L2 — Subleading coefficient** | Is the over/under-performance DIRECTION (C_1 sign) pre-registered from geometry? | A pre-registered C_1 direction (geometry assumed to pin the analytic coefficient) | `epistemic-discipline.md §"Verifier-Rubric Pre-Registration (Class 8.2)"` + §"Dual-prior pre-registration" — direction-neutrality (the §VII.AF.1/§VII.AU split proves C_1 is observable-specific) |
| **L3 — Operator lift** | Is the finite-L operator representation of the substrate observable CANONICAL or a CHOICE? | An unpinned lift, making the FLOWING-vs-FLAT check lift-dependent | `epistemic-discipline.md §"Pre-Registration Completeness"` PRU Class-8 — declare the lift-convention machinery sub-pin at plan-freeze |

The emergent unification, substrate-first: **the substrate IS the finite-L type-IV EMT (one object); at each layer where a plan reads an analytic NUMBER off it, a computation or a CHOICE is required that the cohomology class alone does not supply** — channel selection (which half of the class?), subleading coefficient (C_1, set by cocycle/projector geometry at the pole, not base dimension), and operator lift (which finite-L representation?). The topology (class identity, leading rate, sign structure) is canonical; the analysis (finite-L value, C_1, operator representation) is not. The transit-dynamics reading of WHY this matters: a sign-structure observable is the *topological* content of a Bogoliubov-type channel (which band / which side of the acoustic-white-hole horizon — it does not scale with truncation), whereas the magnitude is the *analytic* content (the actual occupation / spectral moment, which converges as the spectral sum fills in). §VII.CB confused the two precisely because the sign-half was the genuinely-COMPUTED, beautifully-clean half (the `A=2J` GFF identity to 8 digits — determinacy read as relevance, EMG-T-3). The checklist forces the question "did you compute the FLOWING magnitude, or infer it from the binding envelope?" — the bridge-axis instance of the fold-impulsive lesson (S100b W5-1: compute `|β|²` on the sharp-boundary transfer matrix; do not infer it from "the transit is supersonic"). **Forward consequence (the carry I most want preserved):** FWD-C1/C2/C3 and any future sign-structure-sourced bridge should run this three-layer checklist at plan-freeze; §VII.CB is its inaugural calibration instance, and all three guards are already K=3-MANDATORY or PRU-Class-8 framework rules — the checklist is enforceable today, not a proposal.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | Same vs different observable | VDD1, Re:VDD1, T1 | **Converged** | **DIFFERENT observable.** The Level-2 `L⁻³` envelope binds the magnitude-convergence channel `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L → g_M` (`g_M = a_2_FW_zeta = 2776.165389`); the supplied Level-3 `7.500e-09` measures the sign-saturation channel `S(L) = (r_g, anec) → {1,1}`. `g_M` does NOT enter the Level-3 numerator. The type-IV physics SHARPENS VDD1's "pinned by construction": both invariants are forced to {1,1} by L_max-free analytic identities (`Mach_core = exp(½) ⇒ r_g = r₀ = 1`; `A = 2J ⇒ anec = 1`) — fixed-anchor saturation, NOT a co-convergent proxy. The magnitude channel `M` is structurally ABSENT from the s105 artifact (never computed). |
| 2 | Tier-1/Tier-2 gate satisfaction | VDD2, Re:VDD2, T2 | **Converged** | **NEITHER Tier-1 nor Tier-2 as currently anchored, but Tier-1-eligible-IN-PRINCIPLE.** The sign channel has no L_max axis (FAILS Tier-1: no `‖HKR(c_L) − g_M‖` tracked, no convergent residual) and is not divergent (FAILS Tier-2: O(1), pinned to 1, no dimensionful channel to re-anchor). It is a channel-SWAP, not a re-anchor. The pivotal transit constructibility verdict: the magnitude anchor `M(L) → g_M` is constructible IN PRINCIPLE (a well-defined finite-L spectral-triple trace) but UNBUILT — it requires a NEW compute, NOT an L-scan of the existing radial-profile artifact. So §VII.CB is Tier-1-eligible-in-principle (unlike §VII.AX's Tier-2-DIMENSIONFUL permanent hold), held-pending-a-NEW-CF. |
| 3 | §VII.CB registry status disposition | VDD3, Re:VDD3, T2, A3, HANDOFF-1 | **Converged** | **§VII.AX surgical-demotion split: STAGE-3-PERMANENT-STRUCTURE + Level-3-HELD — NOT a blanket STAGE-1-CANDIDATE down-tag.** Theorem-STRUCTURE (binding Element-4 envelope + Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}` + 5-anatomy) stays STAGE-3-PERMANENT; Level-3 row HELD `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`; sign-residual retained as a separate Element-1 witness; discharge = CF-S107. The two-axis split (DST-1/DST-T-1 rank-swap): DIAGNOSIS keyed to Axis-1 (wrong-channel, more anchor-defective than §VII.AX); DISPOSITION keyed to Axis-2 (Tier-1-eligible, held-pending-CF, NOT permanent) — never averaged. Verdict permanence: the on-disk `293105a2…` REGISTRY-PASS line stays; the held-tag is a NEW pinned annotation. |
| 4 | Margin-claim meaningfulness | VDD4, Re:VDD4 | **Converged** | **Non-load-bearing ARTIFACT, FLAT-by-analytic-identity.** `margin = 1.0e-3 / 7.5e-9 = 1.333e5×` is large BECAUSE the two divided numbers are different observables (the ANEC-quadrature floor against the exact `A=2J` identity, over a magnitude envelope) — NOT because the magnitude converged 1.333e5× early. Per corpus §20 the margin carries no PASS weight. The transit-axis upgrade of VDD4's diagnostic: the sign-residual is PROVABLY L_max-FLAT (not "plausibly") — `r_g`/`anec` are invariant under uniform `c_s²` rescaling (the only L-dependence, via `c_BLV`), so `‖S(L) − {1,1}‖` sits at the bisection/quadrature floor at EVERY L_max. This IS the K=3-MANDATORY multiplicative-normalization cancellation fingerprint (`S = w(L_max)·g(geom)`, `w` cancels). The contrast sign-FLAT (analytic) vs magnitude-FLOWING-as-L⁻³ (CF-S107) seals the artifact reading. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**All four W-2 adjudication questions: CONVERGED.** Zero dissent between the two axes (van-den-dungen R3 DISSENT = verified null; transit R3 DISSENT = verified null). The cross-axis EMERGENCE (EMG-1…4, EMG-T-1…3, EMG-VDD-R3, EMG-T-R3) produced the durable forward output: the three-layer guard checklist for sign-structure-sourced cross-pillar bridges (EMG-T-R3).

## Remaining Open Questions

The substantive adjudication is closed; these are the genuine forward questions the workshop surfaced. OQ-1 is the primary discharge gate (formalized as the CF below); OQ-2 through OQ-5 are distinct, each specific enough to become a computation or a session topic.

1. **Does `res(L_max=10) = |M(L=10) − g_M|/|g_M| < 1e-3`, AND does `res(L)` scale as `L⁻³` across `{8,10,12}`?** (CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR — the primary discharge gate; see Carry-Forward Computations.) This is the magnitude-channel anchor whose PASS promotes the §VII.CB Level-3 row HELD → SATISFIED. GENUINE gate (could FAIL): §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling can under-perform at finite L. Direction-neutral on the C_1 sign.

2. **What is the C_1 sign for the type-IV EMT a₂-magnitude channel — i.e., does §VII.CB over- or under-perform the asymptotic `L⁻³` envelope at finite L?** This is the §VII.AF.1 (negative C_1, 10× over-performance) vs §VII.AU (positive C_1, 2.6926 under-performance) fork, left OPEN by construction (DST-2/DST-T-2: the shriek-map geometry is BLIND to C_1; it is observable-specific, set by cocycle/projector geometry at the pole). CF-S107 REPORTS it as a diagnostic; it is NOT pre-registerable as a direction. Becomes a sharp sub-question once CF-S107's `res(L)` series is in hand: does the type-IV cocycle/projector geometry at the `s=3` pole admit an a-priori C_1-sign prediction from a Hochschild-cocycle (rather than plain-trace) evaluation?

3. **Is the `Γ_sub(r) →` finite-L Nambu-doublet operator lift CANONICAL or a CHOICE — and if a canonical lift exists, what is the relay-Compton-radius ↔ `D_K`-spectrum dictionary `r ↔ {spectral data}`?** (DST-T-3.) The existing s105 artifact never represents `v(r)` on the spectral triple — `r` is a continuum radial coordinate, not a function of `D_K` eigenvalues. CF-S107 must declare a lift-convention machinery sub-pin at plan-freeze (PRDR), OR establish a canonical lift. An unpinned lift is a PRU Class-8 cardinality failure that could make the FLOWING-vs-FLAT check (OQ-1) lift-dependent. This is a substrate-physics question (is there a forced `r ↔ D_K-spectrum` correspondence?) distinct from the gate it feeds.

4. **Does the three-layer guard checklist (EMG-T-R3) fire on the existing FWD-C1/C2/C3 forward bridge candidates — i.e., are any of them sign-structure / topological substrate computes whose Level-3 anchor would be L_max-FLAT?** The §VII.CB defect is the inaugural calibration instance of a general failure mode. A pre-freeze audit of FWD-C1 (Pillar I↔II, n_s spectral-action↔Planck CMB), FWD-C2 (Pillar II↔V, Mellin-Barnes residue↔BdG spectral triple), FWD-C3 (Pillar IV↔V, substrate cocycle norms↔3He-B/3He-A) for the FLAT-anchor signature would catch the same defect before landing. Becomes a methodology gate: run the L1/L2/L3 checklist (FLAT-vs-FLOWING fingerprint; C_1-direction-neutrality; lift-convention pin) against each candidate's planned Level-3 anchor at plan-freeze.

5. **Should the three-layer guard checklist (EMG-T-R3) be promoted to a `cross-pillar-bridge-anatomy.md` rule extension (a sign-structure-sourced-bridge sub-clause), and what is its K-counter status?** All three guards already exist as framework rules (the K=3-MANDATORY FLAT-vs-FLOWING fingerprint; Class-8.2 / dual-prior direction-neutrality; PRU Class-8 lift-convention pin); the checklist COMPOSES them for the specific case of a bridge whose substrate-IS observable is a sign-structure / topological compute. §VII.CB is calibration instance #1. This is a methodology-rule question (does the composed checklist warrant its own anatomy sub-section + audit-script extension `_cross_pillar_bridge_audit.py::detect_sign_structure_anchor`?), routed via housekeeping / `/rclab-investigate`, NOT a substrate-physics compute.

## Wrap-Up — Workshop Impact Summary

### What Changed

Per `output-standards.md §"Workshop Wrap-Up 'What Changed' — Numerical vs Structural Distinction"`, the two epistemic categories are separated. The §VII.CB Level-3 disposition is a **structural change** (it alters the epistemic TYPE of the result), not a numerical revision.

#### (a) Numerical revisions

- The `7.500e-09` Level-3 value is **unchanged** — but its *interpretation* changed (this is the structural part, below). No number in the §VII.CB entry is numerically revised; the workshop re-scopes what the existing numbers MEAN.
- The 1.333e5× margin is unchanged as a *ratio of two reals* (`1.0e-3 / 7.5e-9`), but re-scoped from "5.12 OOM inside the envelope" to "non-load-bearing artifact" (a re-scope, not a numerical revision).
- `g_M = a_2_FW_zeta = 2776.165389` re-verified canonical (gate S88-A-N-FW-CANONICALIZATION, Superseded=False) — confirmation, not revision.

#### (b) Structural changes

- **§VII.CB Level-3 disposition: REGISTRY-PASS → STAGE-3-PERMANENT-STRUCTURE + Level-3-HELD.** This is a STRUCTURAL reclassification (epistemic-type change): the entry was a clean satisfied-inequality registry-PASS; it is now a permanent-structure-with-a-held-Level-3-row. The theorem-STRUCTURE (binding envelope + Level-1 class identity + 5-anatomy) is untouched STAGE-3-PERMANENT; the *empirical-confirmation predicate* (Level-3 < Level-2 as a binding-channel-consistent statement) is HELD.
- **Level-3 anchor reclassified: "the bridge's convergence anchor" → "a sign-structure saturation witness of Element 1."** The `7.500e-09` changes ROLE (not value): from the magnitude-convergence anchor of the bridge to a separate, retained Element-1 sign-topology witness. This is an anchor-structure reclassification (epistemic-type change).
- **The two observables disambiguated: one channel → two orthogonal channels.** What read as a single "Level-3 residual" is structurally TWO channels — the magnitude-convergence channel `M` (bound by the envelope, FLOWING-as-L⁻³, UNCOMPUTED) and the sign-saturation channel `S` (supplied as the anchor, FLAT-by-analytic-identity). A dimensional reading change: 1 observable → a 2-channel decomposition keyed by the multiplicative-normalization FLAT-vs-FLOWING fingerprint.
- **The held-tag is keyed by a K=3-MANDATORY framework rule, not invented in this workshop.** The Reading-B verdict is the application of the `math-scripts.md §"Multiplicative-normalization cancellation invariants"` fingerprint (K=3 MANDATORY) to the §VII.CB Level-3 anchor — a structural promotion from "a careful category argument" to "a mandated rule application."
- **New durable structural output: the three-layer guard checklist (EMG-T-R3)** — a forward falsifier-design rule for sign-structure-sourced cross-pillar bridges, with §VII.CB as inaugural calibration instance #1. Type-promotion: a single-entry defect → a general bridge-registration discipline.

### What Holds

The defect is surgically localized at Element 5 / Level-3; everything structural about the bridge survives intact:

- **The binding envelope (Element 4, `α = d − 1 = 3` at d=4).** The HKR `L_max→∞` boundary-map base-dimension rate is theorem-grade — derived at S106 W3-2 (audit `943b17ad…`) AND independently re-derived at S106 W3-4 (audit `645ac895…`, the Leg-B codim-1 base-dimension route). The binding test passes (`c_continuum = g_M` IS the HKR-image). Nothing in Reading B touches it.
- **The Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`.** The type-IV core EMT and the continuum a₂-emergent metric ARE the same a₂ Seeley-DeWitt curvature-degree-2 K-homology class under the shriek map `[D_M] = π_! ⊗ [D_B]`. Regulator-invariant, L-independent. The type-IV compute confirms the SIGN content of this class (g_core<0 ANEC-violating core, g_ext>0 type-I exterior, n_crossovers=1) is cleanly realized.
- **The 5-anatomy structure.** All five IS-not-IN elements present with explicit values; `_cross_pillar_bridge_audit.py` returns 5/5 anatomy + 3/3 tier markers + Element-2 OE-form PASS. The defect is NOT anatomy-completeness; it is that Element-5's *content* witnesses the wrong (orthogonal) channel.
- **The sign-residual itself, as substrate physics.** The `7.500e-09` is a real, clean substrate-IS result — the type-IV acoustic-white-hole sign topology (`A=2J` GFF identity; restoration-surface `r_g=1` at exactly 1 relay-Compton wavelength via `Mach_core=√e`) realized to 8 digits. It is retained as an Element-1 witness, not deleted.
- **The substrate-first frame.** The substrate IS the finite-L type-IV core EMT (a single object carrying both a SIGN half and a MAGNITUDE half); `g_M` is the `L_max→∞` HKR image of its MAGNITUDE half (the laboratory-IN container observable); the QCD-GFF reference (Dumitru-Noronha) is the analog of the SIGN half. Both halves substrate-prior; neither a container the substrate fits into. The explanation direction (substrate → bridge map → continuum image) is unchanged by the disposition.

### What Breaks or Strains

- **The clean REGISTRY-PASS at 1.333e5× breaks.** It was the single most rhetorically reassuring number in the §VII.CB entry ("5.12 OOM inside the envelope; Sage-confirmed"). Under Reading B it is the LEAST trustworthy number: the margin is large precisely BECAUSE the Level-3 residual measures a different, already-saturated quantity (sign-balance → {1,1}, at the ANEC-quadrature floor) than the envelope bounds (magnitude → g_M). The 5.12-OOM size is a *symptom* of the category mismatch, not evidence of convergence quality. It is non-load-bearing (corpus §20) and, worse, misleading until re-scoped — which the mack patch does.
- **The "binding ⇒ REGISTRY-PASS" inference strains, exactly where the framework's own history says it is fragile.** The binding determination (3b/3d) is a TOPOLOGICAL-layer result (the leading `L⁻³` rate, a class-convergence-RATE statement); the registry-PASS is an ANALYTIC-layer claim (a finite-L NUMBER `< 1e-3`). The framework has caught itself once on exactly this topology→analysis over-reach (W9-5 FALSIFIED-at-K=2: a quantity assumed to FLOW-to-a-universal-limit instead stayed structured at the asymptotic layer; §VII.AU: positive C_1, finite-L under-performance on the IDENTICAL `(d=4,s=3)` structure). §VII.CB's confident `Level-3 < Level-2` reads as if the analytic envelope guarantees the finite-L bound — and §VII.AU is the standing counterexample that it does not. §VII.CB sidesteps the question entirely by anchoring on the sign channel, which masks the exact finite-L magnitude question the framework has a counterexample for.
- **What does NOT break:** the envelope, the Level-1 class identity, the 5-anatomy, the sign-physics result, and the substrate-first frame. The strain is confined to the empirical-confirmation predicate, which is why the disposition is a surgical Level-3-row HELD (not a structure down-tag) and the discharge is a constructible CF.

### Carry-Forward Computations (MATH ONLY — propagate to S107)

One carry-forward. It is a GENUINE future computation (a new gate with a pre-registered, direction-neutral threshold + machinery pin), not a hygiene item. Reproduced verbatim from HANDOFF-2 (the A4 spec locked at CONV-T-3 + the DST-T-3 lift-convention 5th annotation), confirmed land-ready in ACC-3.

**`CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR`** (the §VII.CB Level-3-row discharge gate)

1. **What**: compute the magnitude-convergence Level-3 anchor for §VII.CB — `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` (a finite-L spectral-triple trace, NOT a continuum radial-profile compute), its residual `res(L) = |M(L) − g_M|/|g_M|` with `g_M = a_2_FW_zeta = 2776.165389`, at `L ∈ {8,10,12}`; report `res(L_max=10)` and the fitted scaling exponent + C_1 sign.
2. **Inputs**:
   - (a) the S105 type-IV npz (`e2860d57…`/audit `91b36ed9…`) for the `T^{(IV)}` construction;
   - (b) the S106 W3-2 binding-envelope npz (`a8efd183…`/audit `943b17ad…`) for the Level-2 bound `L⁻³`;
   - (c) `g_M = a_2_FW_zeta = 2776.165389` from `canonical_constants.py` (gate `S88-A-N-FW-CANONICALIZATION`, verified Superseded=False this turn);
   - (d) the cached L=8/10 spectra + the L=12 master cache `s84_spectrum_cache_L12_tau019.npz`.
3. **Gate (pre-registered, DIRECTION-NEUTRAL on C_1 per DST-2/DST-T-2)**: `res(L_max=10) < Level-2(L_max=10) = 1e-3` (the binding inequality) **AND** `res(L) ∝ L⁻³` across `{8,10,12}` (the FLOWING signature confirming `M` is the bound observable — the multiplicative-normalization FLOWING check, contrasting the analytically-FLAT sign channel). **C_1 sign REPORTED as a diagnostic** (the §VII.AF.1-vs-§VII.AU fork) — NOT pre-registered as a direction. The gate keys ONLY on the binding inequality + the FLOWING-as-L⁻³ check; it reports the C_1 sign as a DIAGNOSTIC. **A pre-registered C_1 direction is a Class-8.2 PRU smuggle** (`epistemic-discipline.md §"Verifier-Rubric Pre-Registration (Class 8.2)"` + §"Dual-prior pre-registration as track-discriminator"): the §VII.AF.1-negative / §VII.AU-positive split on the IDENTICAL `(d=4,s=3)` structure PROVES the prior is observable-specific and 50/50-until-computed (the §VII.AF.1/§VII.AU opposite-sign siblings prove C_1 is observable-specific, NOT geometry-pinned). On PASS → Level-3 row HELD → SATISFIED, §VII.CB earns full REGISTRY-PASS. **GENUINE gate — it CAN FAIL**: §VII.AU is the standing counterexample that a `(d=4,s=3)` sibling under-performs the asymptotic envelope at finite L (`W6_1_anchor_value = 2.6926` > asymptotic 3).
4. **Effort**: ~1.0 (three L_max points, each a finite-L spectral-triple M₂(ℂ) trace; the operator-representation of `T^{(IV)}` on `H^{≤L} ⊗ ℂ²` + the `P_a₂` projection is the new build, distinct in KIND from the s105 radial-acoustic compute).
5. **(Lift-convention machinery sub-pin — DST-T-3, MANDATORY at CF-S107 plan-freeze)**: the lift `Γ_sub(r) →` finite-L Nambu-doublet operator on `H^{≤L} ⊗ ℂ²` is a **CHOICE**, not yet canonical — the `T^{(IV)} → H^{≤L} ⊗ ℂ²` lift is a CHOICE to declare at plan-freeze. The s105 artifact never represents `v(r)` on the spectral triple (it evaluates `Γ_sub` at radial grid points; the relay-Compton-radius ↔ `D_K`-spectrum dictionary `r ↔ {which spectral data}` is unspecified in the existing artifact). CF-S107 MUST pin the `r ↔ D_K-spectrum` dictionary + the `Γ_sub(r) →` operator map at plan-freeze (PRDR dry-run per `epistemic-discipline.md §"Pre-Registration Completeness"`), OR declare-diagnostic if a canonical lift is found. An unpinned lift is a PRU Class-8 cardinality failure that could make the FLOWING-vs-FLAT check (gate item 3) lift-dependent. Does NOT change the disposition (the hold stands regardless of how the magnitude channel is later lifted); it scopes whether CF-S107 is a clean single-gate compute or carries the sub-pin.
6. **Depends on** (per `output-standards.md §"Carry-Forward Dependency Enumeration"`): the §VII.CB held-tag disposition (this workshop, ROUTED-TO-MACK below — the held Level-3 row is what CF-S107 discharges); the S105 type-IV npz + S106 W3-2 envelope npz + s84 L12 master cache (UPSTREAM DATA, inputs (a),(b),(d)); `canonical_constants.py: a_2_FW_zeta = 2776.165389` (input (c)); the lift-convention sub-pin decision at S107 plan-freeze (item 5).

**MIRROR REQUIRED (downstream routing, NOT effected this turn):** this CF must be mirrored into the W3 working-paper `## Carry-Forward Computations` block (`sessions/session-106/session-106-w3-workingpaper.md` line 352 — currently an empty template stub) so `/rclab-plan` consumes it at S107 plan-freeze. The WP CF block is the load-bearing consumption source per `Investigating-Workshops.md` (`/rclab-plan` reads WPs, not workshop schedules); this workshop section is the adjudication record. This turn I write ONLY the workshop file (per the spawn-prompt single-file constraint); the orchestrator routes the WP CF mirror as the standard wave-synthesis step.

### Effected In-Session (NON-MATH — completed by transit, the final agent, BEFORE TERMINATING)

The single non-math item this workshop produces is the §VII.CB registry status disposition. **CRITICAL ROUTING:** `sessions/permanent-results-registry.md` is `mack-cosmic-bridge`'s SOLE-WRITER domain (`feedback_mack-bridge-role.md`; and the §VII.CB header explicitly names mack-cosmic-bridge as the sole writer for THIS cross-pillar bridge row). I do NOT write `permanent-results-registry.md` myself. My action — produce the EXACT held-tag patch text (before/after strings + line anchors) and route it to mack — is COMPLETE; the orchestrator dispatches `mack-cosmic-bridge` to apply the byte-level edit, preserving sole-writer discipline. Verdict permanence (`epistemic-discipline.md`): the on-disk `S106-W3-3` REGISTRY-PASS line (audit `293105a2…`, verdict file line 58) STAYS on disk — the held-tag is a NEW pinned position (the §VII.AX surgical-demotion annotation pattern, corpus §25.1), NOT a retroactive edit.

- [x] **§VII.CB held-tag disposition — PATCH TEXT PRODUCED, ROUTED-TO-MACK (sole-writer)** — exact patch text inline below (line anchors + before/after + verbatim INSERT block per HANDOFF-1, lines 551–571). Box CHECKED because my action (produce the exact patch + route to mack) is complete; the byte-level edit is mack's.

#### THE PATCH (route to `mack-cosmic-bridge`; target file `sessions/permanent-results-registry.md`, §VII.CB section, header at line 21968)

**Mechanical shape** (confirmed faithful against on-disk §VII.CB in ACC-2 this turn): ONE annotation-block INSERT after the Status paragraph (after **line 21970**) + a one-line cross-reference pointer at each of the FIVE on-disk REGISTRY-PASS / "margin 1.333e+05×" / "SATISFIED" assertion sites, so a future reader cannot hit the on-disk REGISTRY-PASS without hitting the held-tag. The INSERT does NOT delete or rewrite any existing line (verdict permanence).

**Cross-reference pointer (ADD verbatim, adjacent to each of the five assertion sites; NO existing text deleted):**

```
[S106 W3 workshop: Level-3 row HELD — see §VII.CB held-disposition annotation below]
```

Pointer placement (the five assertion sites, verified on-disk this turn):
- **Line 21968** (header — "…REGISTRY-PASS (STAGE-3-PERMANENT cross-pillar bridge…")
- **Line 21970** (Status — "…the Level-3 empirical anchor at canonical `L_max=10` satisfies `Level-3 < Level-2`.")
- **Line 21986** (Element 5 — "…SATISFIED by margin 1.333e+05× (registry-PASS criterion satisfied).")
- **Line 21994** (Level-3 ladder row — "…⇒ registry-PASS criterion satisfied … (margin 1.333e+05×, well inside the algebraic envelope).")
- **Line 22003** (REGISTRY-PASS conclusion — "…⇒ **REGISTRY-PASS** (Level-3 < Level-2 SATISFIED, margin 1.333e+05×).")
- **Line 22016** (substitution-chain conclusion — "…the Pillar I↔VI↔IV cross-pillar bridge is REGISTRY-PASS … ∎")

(Six anchor lines; five distinct PASS/SATISFIED assertions — line 21968 carries the header assertion, lines 21970/21986/21994/22003/22016 the rest.)

**ANNOTATION BLOCK to INSERT after line 21970 (verbatim — the A3 string locked at CONV-T-3, carrying the DST-1 two-axis split proved via rank-swap in DST-T-1; reproduced from HANDOFF-1 lines 559–569):**

> **Registry status (S106 W3 workshop disposition — NEW pinned position, verdict-permanence-preserving annotation; the on-disk `S106-W3-3` REGISTRY-PASS line [audit `293105a2…`] STAYS on disk — this is the §VII.AX surgical-demotion annotation pattern [corpus §25.1], NOT a retroactive edit):**
>
> **§VII.CB theorem-STRUCTURE: STAGE-3-PERMANENT.** The binding `L⁻³` envelope (Element 4; `S106-W3-2` audit `943b17ad…` + the independent direct re-derivation audit `645ac895…`), the Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR} = [g_M]_{a₂,HKR}`, and the complete 5-anatomy are sound and untouched (Stage-2 PASS-AND holds on all non-Level-3 clauses).
>
> **§VII.CB Level-3 row: HELD — `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`.** The supplied Level-3 anchor `max(|r_g−1| = 2.193e-10, |anec−1| = 7.500e-09) = 7.500e-09` is a **SIGN-STRUCTURE saturation residual** — it witnesses the SIGN half of the a₂ class (`S(L) = (r_g, anec) → {1,1}`: the core/exterior topology g_core<0 ANEC-violating core, g_ext>0 type-I exterior, n_crossovers=1), forced to the integer anchors {1,1} by the analytic identities `Mach_core = exp(½) ⇒ r_g = 1` and `A = 2J ⇒ anec = 1`, and is **L_max-FLAT by the multiplicative-normalization cancellation fingerprint** (`math-scripts.md §"Multiplicative-normalization cancellation invariants"`, K=3 MANDATORY: c_BLV cancels in the dimensionless ratios). It is **channel-ORTHOGONAL** to the magnitude-convergence residual `‖HKR(Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})_L) − g_M‖` (the L_max-FLOWING-as-`L⁻³` quantity the Level-2 envelope actually bounds; `g_M = a_2_FW_zeta = 2776.165389`).
>
> **Tier status: Tier-1-eligible-IN-PRINCIPLE** (per `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` Tier-1/Tier-2 dimensional-re-anchorability gate). **DIAGNOSIS (Axis-1, anchor-correctness):** the supplied anchor is the WRONG channel — more anchor-defective than §VII.AX (which anchored its right-but-divergent channel). **DISPOSITION (Axis-2, in-principle-constructibility):** the magnitude channel `M(L) → g_M` is a CONVERGENT scalar (a well-defined finite-L spectral-triple trace), so a Tier-1 anchor is constructible — UNLIKE §VII.AX (Tier-2-DIMENSIONFUL, divergent `n_PBH`, PERMANENT hold) and UNLIKE §VII.AV (SCHEMATIC-proxy envelope; §VII.CB's envelope is FULL, NO CLASS pin). The two axes do NOT collapse to one ordering (rank-swap: §VII.AX = Axis-1-BETTER/Axis-2-WORSE, §VII.CB = Axis-1-WORSE/Axis-2-BETTER); diagnosis and disposition are a 2-tuple, never averaged. Therefore §VII.CB is **held-pending-a-CONSTRUCTIBLE-recompute, NOT a permanent hold.**
>
> **Discharge path: `CF-S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR`** — a NEW compute of `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` at `L ∈ {8,10,12}` on the finite-L spectral triple (NOT an L-scan of the s105 sign-channel radial-profile artifact, which has no L_max axis). On PASS (`res(L_max=10) < 1e-3` ∧ FLOWING-as-`L⁻³`), the Level-3 row promotes HELD → SATISFIED and §VII.CB earns full REGISTRY-PASS. Direction-neutral on the C_1 sign (the §VII.AF.1-vs-§VII.AU fork stays OPEN until computed).
>
> **Sign-residual retained as a separate Element-1 witness** (NOT deleted): the `7.500e-09` is a real, clean substrate-IS result — the type-IV sign topology (`A=2J` GFF identity; restoration-surface `r_g=1`) realized to 8 digits — demoted from "the Level-3 anchor of the bridge" to "a sign-structure saturation witness of Element 1." The **1.333e5× margin is non-load-bearing** (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"`, corpus §20): it is large because the two quantities being divided are different observables (an ANEC-quadrature floor over a magnitude envelope), NOT because the a₂ magnitude converged 1.333e5× early.

**Routing note for the orchestrator:** dispatch `mack-cosmic-bridge` (sole writer) to (1) INSERT the annotation block after line 21970, and (2) ADD the cross-reference pointer at the six anchor lines (21968, 21970, 21986, 21994, 22003, 22016). The on-disk `293105a2…` REGISTRY-PASS verdict line is NOT touched (verdict permanence). This is the §VII.AX surgical-demotion pattern.

#### Other non-math items: NONE (transit-owned)

Verified there are no other transit-owned non-math items in this workshop:
- **EVOI table rows** — belong to the W-1 workshop, not this one (this workshop's substance is the §VII.CB Level-3 disposition only). Not effected here.
- **Falsifier-master-inventory rows** — §VII.CB is a structural cross-pillar bridge, not a live-watch falsifier observable; no inventory row is owed, and the inventory is `mack-cosmic-bridge`'s sole-writer surface regardless.
- **canonical_constants.py promotion** — no NEW framework-prediction VALUE is produced by this workshop (the disposition holds a Level-3 row; it does not mint a constant). `g_M = a_2_FW_zeta` already canonical (verified Superseded=False). Nothing to promote.
- **Capstone-hygiene 5-question gate** — this workshop touches a §VII registry entry's status (Q3 PROVEN/CONDITIONAL/BROKEN/INFO axis), but the §VII.CB entry is NOT a capstone-narrated claim in `sessions/framework/phonic-exflation-equation.md` §7 falsifier surface; the disposition is a registry-internal Level-3 hold, not a capstone prose-status drift. No capstone patch is owed by THIS workshop. (If the orchestrator's session-close 5-question gate finds §VII.CB is capstone-referenced, that routes to mack via §7 sole-writer discipline — outside this workshop's transit-owned scope.)
- The CF-S107 carry-forward is a MATH item (Carry-Forward Computations block above), NOT a non-math item — correctly NOT effected here.

All non-math items are accounted for: the one transit-owned non-math item (the §VII.CB held-tag patch) is PRODUCED + ROUTED-TO-MACK; no others exist for this workshop.

### Closing Line

§VII.CB's binding envelope and Level-1 class identity stand permanent, but its REGISTRY-PASS rested on the wrong half of the a₂ class — a clean, L_max-FLAT sign-saturation witness offered where the criterion silently demanded a FLOWING magnitude-convergence anchor — so the Level-3 row is HELD pending a constructible, direction-neutral CF-S107 recompute, and the same topology≠analysis boundary that did this is now a three-layer guard checklist every future sign-structure-sourced bridge must clear.
