# Seed — Chunk w4 (Stage-2 verification cohort §VII.BZ + §VII.AD)

**Date**: 2026-06-13   **Investigator**: phonon-first-cosmologist   **Source**: session-106-w4-workingpaper.md + session-106-plan-w4.md
**Wave summary**: 2 gates, both **composite PASS** by full all-PASS conjunction. `S106-VIIBZ-STAGE2-VERIFY` (audit `566cdcb5…`) and `S106-VIIAD-STAGE2-VERIFY` (audit `ac0bfe80…`) each ran the `joint-theorem-promotion.md` Stage-2 two-agent blind cross-axis independent-verify: §VII.BZ {vdd Axis-A / landau Axis-B} on the `+1/2` EMERGENCE-1 identification; §VII.AD {vdd Axis-A / kitaev Axis-B} on the SOURCE-DOUBLE-CITE-CO-PRIMARY chain identity. Both substrate-input-orthogonality predicates SATISFIED → structural CEILING, no overlap caveat. Both entries flipped STAGE-1-CANDIDATE → STAGE-3-PERMANENT (orchestrator-direct at session close, housekeeping §A items 2+3). The PASS verdicts and the tag-flips are pre-specified-protocol verification + bookkeeping — NOT workshop-eligible by the rule's "is NOT" item 2. The ONE substantive tension is an INFO-inside-a-PASS reviewer divergence on the §VII.AD anchor-structure tag (vdd vs kitaev), which the housekeeping ledger §"Q2 marker" already routes here to `/rclab-investigate`.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

### S1-1 — §VII.AD anchor-structure: SOURCE-DOUBLE-CITE-CO-PRIMARY (sequential) vs PRIMARY + INDEPENDENT-CROSS-CHECK (parallel)?
**What this answers**: Is the registered §VII.AD derivational-anchor STRUCTURE tag correct? The §VII.AD theorem `Δ_0 = 4·c_{σ⁻¹((-1,-1))}` is registered with `convention=SOURCE-DOUBLE-CITE-CO-PRIMARY` — a SEQUENTIAL V_input→A_F→C_output chain where neither anchor alone fixes the conclusion (`registry-landing.md §"SOURCE-DOUBLE-CITE-CO-PRIMARY"`). The two blind Stage-2 reviewers diverged on whether that tag is the most precise classification:
- **vdd (Axis-A, NCG-axiomatic)** recorded INFO-inside-PASS: "from a pure NCG-axiomatic standpoint the factorization argument is ITSELF already a complete exact-QQ proof of the localization formula for arbitrary (c,σ) — each step is an exact finite-value-set identity — so an arguably-more-precise structure tag is **PRIMARY (V structural proof) + INDEPENDENT-CROSS-CHECK (C exhaustive enumeration)**, since the two routes are plausibly PARALLEL." (`s106_w4_viiad_reviewer_vdd_axisA_verdict.json` clause c)
- **kitaev (Axis-B, combinatorial)** took the opposite reading: "a mechanism statement alone does not discharge closure over the full discrete domain of 24 faithful characters × 24 orderings… NOT PRIMARY+CONFIRMATION (the anchors do not independently reproduce the conclusion via parallel routes; the certification is downstream of the mechanism)." (`s106_w4_viiad_reviewer_kitaev_axisB_verdict.json` clause c)
- kitaev independently STRENGTHENED the C_output to a generic-QQ symbolic proof over all 24 faithful V_4 bijections with generic partition (c0,c1,c2,c3) — all 24 give `lhs−rhs=0` EXACT (WP Methodology item (ii)). This is the load-bearing fact: it means the C-route, like the V-route, establishes the formula for ALL rational partitions, not just the substrate-specific 576-instance sweep — which is precisely vdd's "parallel routes" claim, surfaced from the C side.
**Why solo, not workshop**: The discriminator is a single, well-defined classification rule with a pinned text criterion — `registry-landing.md §"When PRIMARY+CONFIRMATION is wrong"`: the tag is CO-PRIMARY iff Anchor-2 cannot be invoked without first invoking Anchor-1 (sequential, non-fungible); it is PRIMARY + INDEPENDENT-CROSS-CHECK iff the two anchors independently reproduce the same conclusion via DIFFERENT parallel routes. The question reduces to ONE decidable predicate: does kitaev's generic-QQ symbolic proof independently reproduce the localization formula WITHOUT consuming the vdd V_input Schur factorization? If YES → parallel → the tag should be PRIMARY + INDEPENDENT-CROSS-CHECK. A single reviewer can read both JSONs + the registered entry + the rule's detection criteria and decide; cross-rebuttal is not needed because the two reviewers do not actually contradict each other on the PHYSICS (both PASS clause c) — they contradict on the registry-classification of an already-proven identity. (Both readings retain V_input→A_F as a necessary structural premise, which is all clause (c) certified — so neither reviewer's PASS is at risk regardless of the tag outcome.)
**Agents**: `connes-ncg-theorist` (default 1 = NCG-axiomatic owner of the V_input Schur-orthogonality derivation and the registry-landing CO-PRIMARY vs PRIMARY+CROSS-CHECK detection criteria; NOT a §VII.AD Stage-0 author — Stage-0 = connes + volovik per registry line 16834; NOTE the Stage-0 exclusion applies to the Stage-2 VERIFY, which is closed PASS — this is a downstream registry-text classification review, not a re-verify, so the Stage-0 author IS the right domain owner for the anchor-structure rule. If the orchestrator prefers strict author-distinctness, substitute `van-den-dungen-bridge-theorist`, who already authored the INFO-inside-PASS and owns the NCG-axiomatic axis.)
**Source anchors**: §VII.AD registry block (lines 16834–16889, `convention=SOURCE-DOUBLE-CITE-CO-PRIMARY`, ANCHOR-1 V_input / ANCHOR-2 C_output); `S106-VIIAD-STAGE2-VERIFY` verdict line 74 + reviewer JSONs `s106_w4_viiad_reviewer_vdd_axisA_verdict.json` (clause c INFO) and `s106_w4_viiad_reviewer_kitaev_axisB_verdict.json` (clause c PASS + WP item (ii) generic-QQ proof); WP §W4-2 Methodology items (i) + (ii); `registry-landing.md §"SOURCE-DOUBLE-CITE-CO-PRIMARY"` detection criteria 1–4; `registry-landing.md §"When PRIMARY+CONFIRMATION is wrong"`.
**Output**: a structural verdict on the §VII.AD anchor-structure tag — either (A) CO-PRIMARY confirmed (the C-route's generic-QQ proof still consumes the V_input Schur mechanism, so sequential-non-fungible holds), or (B) re-tag to PRIMARY + INDEPENDENT-CROSS-CHECK (the two routes are genuinely parallel and independently complete). Output is a registry-text classification recommendation routed to `mack-cosmic-bridge` (registry sole-writer) for the tag edit, with the detection-criterion derivation pinned. STAGE-3-PERMANENT status of §VII.AD is UNAFFECTED either way (an anchor-structure re-tag is a `registry-landing.md` hygiene refinement, not a theorem down-tag). Carry-forward (4-field) if the verdict requires a fresh Sage-QQ independence check: what = does kitaev's generic-(c0,c1,c2,c3) QQ proof depend on the V_input factorization or is it self-contained / inputs = the two reviewer JSONs + `s87_w11_hypercube_vertex_identity.npz` / gate = parallel-route independence boolean / effort = 0.25.

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

(none — see reasoning under S1-1: the anchor-structure question is a single decidable classification predicate, not a ledger-dissonance requiring R1-steelman/R2-rebuttal/R3-converge. The two reviewers diverge on a registry TAG, not on competing first-principles physics readings of the substrate observable; both PASS clause (c). Per the rule's §"Discriminating decision" default — "Default to Slot 1 when uncertain — workshops are expensive" — this routes to a solo review, not a workshop. The §VII.BZ EMERGENCE-1 `+1/2` identification produced NO reviewer divergence whatsoever — both vdd and landau PASS clause (c) with mutually-reinforcing, non-competing rationales (vdd certifies the conditional trace+faithfulness consequence; landau certifies the CdGM-minigap = Wightman-floor structural identity from the BdG side); the SCOPE NOTES in both JSONs are each reviewer correctly DEFERRING the off-axis half to the PASS-AND partner, which is the protocol working as designed, NOT a tension.)

## Cross-wave flags (surface for consolidator; NOT resolved here)
- **§VII.AD anchor-structure (S1-1) ↔ W1 crystalline-vs-Poisson tension**: the housekeeping ledger §"Q2 marker" (line 7) names TWO S106 Q1 workshop candidates — "the 1b/1d crystalline-vs-Poisson tension" (a W1 substrate-commensurability item, NOT my wave) AND "the §VII.AD anchor-structure CO-PRIMARY-vs-PRIMARY+CROSS-CHECK question" (my S1-1). These are INDEPENDENT (W1 is a substrate-physics discriminator on Jensen-deformation arithmetic; S1-1 is a registry-classification predicate on an already-proven identity); no dependency or conflict between them — surfaced only so the consolidator can confirm both are distinct schedule entries and neither is double-counted. The W1 investigator owns the crystalline-vs-Poisson seed; I own §VII.AD.
- **§VII.AD STAGE-3-PERMANENT flip ↔ S1-1 anchor-structure review**: the W4-2 PASS already flipped §VII.AD to STAGE-3-PERMANENT in-session (housekeeping §A item 3). S1-1 is a POST-PROMOTION registry-text refinement — it does NOT reopen the promotion (the anchor-structure tag is orthogonal to PROVEN-status per `registry-landing.md`). The consolidator should NOT route S1-1 as a "re-verify §VII.AD" task; it is a classification-tag review only. Flagged so the dependency direction is unambiguous.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)
- **[Q2-hygiene]** — §VII.AD legacy-path registry-text fix: ALREADY EFFECTED in-session (housekeeping §A item 4 — the bare path `computations/s87_w11_hypercube_vertex_identity.npz` corrected to `computations/session-87/…` via `replace_all`, 3 occurrences). NOT a carry-forward; recorded here only to confirm the WP §W4-2 Methodology item (iv) observation was closed. Drop.
- **[Q2-hygiene]** — §VII.BZ stale queued-gate-name + harvested-co-author observations (WP §W4-1 reviewer findings 4+5): the §VII.BZ Status line referenced "a future cross-axis verify" with no pinned gate-ID and named the pre-gate as `S105-OMEGA-FAITHFUL-NORMAL` (on-disk canonical `S105-W2-2-OMEGA-FAITHFUL-NORMAL`); the mack harvested-co-author observation is a process note (mack WAS correctly in the EXCLUDED reviewer set). These route orchestrator-direct/mack at session close per the WP — registry-text hygiene, NOT a compute CF. Confirm closed at session-close synthesis; not a schedule entry.
- **[Q-other]** — S107 remediation CF: N/A. Both gates closed PASS with NO clause FAIL, so the plan's Decision-Point remediation CF (re-audit a FAILing clause) does NOT fire. The forward Stage-2 cohort {K2, K7, K8, K9, K11} stays in `open-channel-ledger.md §C` capacity-deferred (plan §"Forward-cohort note") — NOT lifted here, NOT a workshop, NOT an S106 carry-forward.

## Wave-by-wave digest (consolidator background)
**Wave 4 — Stage-2 verification cohort (REGISTER-SOURCED from `open-channel-ledger.md §C`)**. Two `[VERIFY-THEOREM]` set-membership verdict-aggregation gates (no `[SIGN]` 3-tuple; pure JSON aggregation, no linear algebra). Both are the Stage-2 leg of the `joint-theorem-promotion.md` 4-stage pathway.

- **§W4-1 `S106-VIIBZ-STAGE2-VERIFY`** (PHONONIC): §VII.BZ BDI Horizon-Faithfulness Protection on the emergent crossed product `A_hor = A_K ⋊_{σ^ω} ℝ` (Type-II_∞). Reviewers {vdd Axis-A connes/Tomita-Takesaki, landau Axis-B volovik/BDI-CdGM}. Clause partition: (a) BDI/N₃=0 + CdGM-vs-Weyl ladder [volovik, landau-only]; (b) Type-II trace uniqueness + Tomita-Takesaki construction [connes, vdd-only]; (c) JOINT = the `+1/2` EMERGENCE-1 identification (bosonic Wightman floor = fermionic CdGM minigap = single BDI datum fixing BOTH trace AND faithfulness). All three clauses PASS-AND PASS. landau RE-derived the Fermi-Dirac closed form `f_a = 1/(exp(β_a E_a)+1)` at `T_GGE=0.668` (β=1.497006) bit-exact across all 12 Peter-Weyl blocks; `f ∈ [0.157, 0.435]` strict-interior, `W_global_min = 0.561207 = 1/2 + 0.061207` (the +1/2 floor + GGE relic excess), `P_exc=1.000` ⇒ ω faithful. vdd certified the Takesaki-duality Type-II_∞ trace-scaling `τ̃ ∘ θ_s = e^{−s} τ̃` and the Tomita-Takesaki `S = J·Δ^{1/2}` construction. Composite PASS → STAGE-3-PERMANENT eligible; flipped in-session (housekeeping §A item 2). Substrate-input-orthogonality SATISFIED (`s105_w2_2_omega_faithful_normal.npz` loaded by landau ONLY). Reviewer registry-text-hygiene findings (5 items, documented NOT registry-edited): landau's −½ sign-label note (branch convention, load-bearing `|E_0|=+1/2·ω_0≠0` correct); landau's clause-(c) scope deferral (correctly defers the trace-defining leg to vdd's Axis-A half); vdd's no-over-claim note (II_∞ trace residual scalar = physical second moment, picks representative without over-claiming); the mack harvested-co-author observation; the stale `S89/S105-OMEGA-FAITHFUL-NORMAL` queued-gate-name drift. NO reviewer divergence on physics.
- **§W4-2 `S106-VIIAD-STAGE2-VERIFY`** (GEOMETRIC): §VII.AD Δ_0 LOCALIZATION FORMULA `Δ_0(σ;(c1,c2,c3,c4)) = 4·c_{σ⁻¹((-1,-1))}` EXACT in QQ on substrate (2,4,8,6) at τ_fold (the `phononic-framing.md` Level-1 single-τ-slice calibration-corpus instance; the oldest cohort member with no prior Stage-2 motion — supersedes the never-executed `S89-DELTA-0-LOCALIZATION-INDEPENDENT-VERIFY` placeholder). Reviewers {vdd Axis-A NCG-axiomatic V_input, kitaev Axis-B Sage-QQ exhaustive C_output}. Clause partition: (a) V_input Schur factorization `[1−σ_1][1−σ_2]=4·1_{σ_1=σ_2=−1}` [vdd-only]; (b) C_output 576-config Sage-QQ enumeration [kitaev-only]; (c) JOINT = SOURCE-DOUBLE-CITE-CO-PRIMARY V_input→A_F→C_output chain identity. All three PASS-AND PASS → STAGE-3-PERMANENT eligible; flipped in-session (housekeeping §A item 3). kitaev STRENGTHENED clause (b) to a generic-(c0,c1,c2,c3)-QQ symbolic proof over all 24 faithful V_4 bijections (proves the formula for ALL rational partitions, subsuming the 576-instance sweep) and reconciled the S88 `cc2_delta_0_match=False`/`delta_0_formula_QQ=8` FAIL field as a NON-faithful-labeling artifact OUTSIDE the theorem's stated faithful-bijection domain (WP items (ii)+(iii)). **The one live tension** (→ Slot 1 S1-1): vdd recorded INFO-inside-PASS arguing the V_input factorization is itself a complete exact-QQ proof, so the routes are PARALLEL and the tag should be PRIMARY + INDEPENDENT-CROSS-CHECK, not the registered sequential CO-PRIMARY; kitaev's clause-(c) rationale explicitly disagrees (C is downstream of the V mechanism, NOT parallel). Both PASS clause (c); the divergence is purely on the registry classification of an already-proven identity. Substrate-input-orthogonality SATISFIED. Legacy-path hygiene fix effected in-session (housekeeping §A item 4).

Both verdict lines carry full dual-SHA + annotation rows (verdict file lines 74–84). No FAIL, no INFO at the composite level; no cross-wave conflict with W1/W2/W3 results from W4's vantage. The Stage-2 protocol is settled methodology (joint-theorem-promotion.md 4-stage) — the verification gates themselves are NOT workshops (rule "is NOT" item 2); the only workshop-investigation-eligible substance is the S1-1 anchor-structure classification divergence.
