# Seed — Chunk w2 (ω'_z Hawking-dressed-relic modular successor + CdGM rigidity)

**Date**: 2026-06-13   **Investigator**: phonon-first-cosmologist   **Source**: session-106-w2-workingpaper.md + session-106-plan-w2.md
**Wave summary**: 1 PASS (W2-1 existence) / 2 INFO (W2-2 area-clock composite; W2-3 CdGM characterization) / 0 FAIL. Both INFOs are CLEAN PRE-REGISTERED outcomes that followed their own frozen discriminators — neither leaves a competing structural reading open. The wave is the planner-pinned execution of the GEM-workshop ROQ #1-#3 seeds (EVOI Tier-3 rank 14b / §6 item 2). No genuine adversarial tension survives in-wave.

## No candidates

This wave produced ZERO Slot-1 and ZERO Slot-2 candidates. It is the canonical "clean verdicts, no surviving tension" case the rule's §"No workshops is a valid output" anticipates. Reasoning gate-by-gate against the four-condition workshop definition + the Q1 markers:

- **W2-1 (`S106-OMEGAPRIME-Z-CONSTRUCTION`, PASS)** — a clean existence gate. ω'_z is faithful-normal on the 714-mode bulk (Layer-2 margin 3.523e-04 ≫ EPS=1e-12), the 6 floor modes are cleanly empty-Fock (K_floor·z_floor → +∞ via the N₃=0/BDI gap E_floor=Δ_B3=0.176>0, NOT 0·∞ — DSr3-1, substitution-chain verified), and Δ_{ω'_z}^{it} is constructed. No competing reading; the floor-mode empty-Fock is a topologically-GUARANTEED boundary condition, not a discovery. Not a workshop.

- **W2-2 (`S106-OMEGAPRIME-AREA-CLOCK`, composite INFO: sign=PASS / magnitude=FAIL)** — the gate PRE-REGISTERED a dual prior (Track A 0.6 = no area-clock / Track B 0.4 = new bridge) with a frozen discriminator (FAIL/INFO → 0.9 Track A). The INFO outcome (op-norm gap 1.163407 ≫ tol 1e-3; Tolman regrade closed only 34.41% toward Ĝ_τ; cocycle sign −1 = S97) maps DETERMINISTICALLY to Track A by that pre-registration. The two readings were resolved BY the gate's own frozen discriminator — there is no surviving competing reading to adjudicate. Q1 = NO (the disposition is pre-assigned, not contested). The tol=1e-3 is precedent-matched to the W2-3 ω-side test by design; contesting it would be convention-shopping, not a genuine divergence. Not a workshop.

- **W2-3 (`S106-CDGM-SPACING-CROSS-BLOCK`, INFO-by-construction)** — carries the substantive dissociation finding: R1 (doubled minigap 2·E_0) is CROSS-BLOCK-INVARIANT (relvar 3.2408e-04, 30× inside thr=1e-2); R2 (upper-level consecutive spacing) is WITHIN-BLOCK-ACCIDENT (relvar 2.8290e-02). Two readings of ω_0 give OPPOSITE verdicts — but the gate RESOLVES which one carries the cross-block invariant ("the rigidity that survives χ is the MINIGAP, not the upper-level spacing"; R1 = BDI/N₃=0-protected SHARED-FLOOR feature, R2 = Peter-Weyl multiplicity dispersion). Q1 marker (iii) — "the workshop's job is to derive which reading is correct" — is already DISCHARGED in-gate from first principles. This is a derived finding, not an unresolved tension. INFO-class was the workshop's OWN pre-registration; the verdict carries the characterization, exactly as planned. Not a workshop.

No FAIL admitting multiple readings; no borderline INFO with competing structural interpretations left open; no cross-wave contradiction (W2-3 sharpens — does not contradict — the co-monotone-plus-shared-floor bridge that W2-2's disposition relies on); no genuinely-divergent convention question. Honest count: 0 / 0.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **None requiring adjudication.** W2's three gates are axis-orthogonal to Wave 1 (horizon-block axis at L_max=10 ⊥ Wave-1 L_max-envelope/cache axis; plan §"axis orthogonality" cross-wave constraint 4 — no shared pins). W2-3 explicitly SHARPENS (does not contradict) the GEM-workshop "co-monotone-plus-shared-floor" floor result: the rank-≥3-in-sector ladder-spacing clause holds cross-block ONLY at the protected-minigap level, NOT the upper-spacing. This is internally consistent and is the wave's intended contribution to that surviving bridge. Flagged for consolidator awareness only — no contradiction with any other wave's verdict that I can see from this wave's vantage.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other / already-correctly-gated, NOT a new CF]** — A FUTURE ω'_z geometric-area-clock 5-anatomy + 3-level cross-pillar registration was conditional on a **2b PASS**. 2b landed composite INFO, so this registration **does NOT fire** (WP §W2-2 Disposition: "the session-index obligation (v) — route a 2b PASS's 5-anatomy registration to S107 — does not fire"). Element-2 OPERATOR-EXPRESSION lab-IN observable was already unspecified this session; the EMr3-2 3He-B vortex-core lab-side companion seed stays capacity-deferred. No new compute item — the EVOI rank-14b row already tracks this, and the WP "Carry-Forward Computations" block correctly notes both items as conditional/deferred. Nothing to add to S107.

- **[Q2-process observation, ALREADY FIXED in-session — flag for housekeeping completeness only]** — The plan §W2-3 substitution-chain Step 1-3 asserted `ω_0 = gap = 0.176 ⇒ Var_pq = 0 EXACTLY` as "verified at plan-freeze." The executing agent found this plan-freeze check was a **constant-reuse tautology**: the W2-2 script set `per_block_json['gap'] := Δ_B3` (the canonical input constant, line 343-345), NOT a measured spectral spacing — so R0's variance-0 is tautological, not a measurement of ladder rigidity. The agent CORRECTED this in-gate by substituting the MEASURED doubled CdGM minigap 2·E_0 (R1), documented the departure fully in the WP "Rigid-ladder baseline correction" paragraph, and the corrected R1 is the result. This is a documented in-session correction (fix is on disk), NOT a live CF and NOT a workshop. Surfaced here per the rule's §"Enforcement at /rclab-investigate" first-surfacing clause: it is a Q2-class plan-substitution-chain observation that the S106 housekeeping ledger §A does not currently list (the ledger's 6 §A items cover registry/path hygiene, not this plan-freeze substitution-chain tautology). Routes to housekeeping.md §A as a record of a completed in-session fix, if the orchestrator wants ledger completeness; it does NOT propagate to S107 and adds no compute work.

- **[Q-other diagnostic, non-gated, no fillable 4-field spec → NOT a CF]** — W2-3's cross-sector diagnostic found the minigap-based 2·E_0 cross-block invariance holds for ALL THREE BdG sectors (B3=3.241e-04, BCS=7.123e-06, B2=1.160e-06), variance growing as the gap shrinks (ξ/Δ effect on the floor-mode perturbation to E_0=√(ξ²+Δ²)). This strengthens the minigap-rigidity result (robust across the named-block structure, not a B3-specific artifact). It is a complete diagnostic already in-WP; there is no future computation it demands. Noted for the consolidator's digest, not a carry-forward.

## Wave-by-wave digest (consolidator background)

**W2-1 `S106-OMEGAPRIME-Z-CONSTRUCTION`** (connes-ncg-theorist) — **PASS**, 3-tuple sign=PASS/magnitude=PASS/regime=VALID, audit_sha256=`4dd27aee…44e916`. Extracts the missing substrate input −g_00^eff(λ) = (|λ|−lam_horizon)/(λ_ref−lam_horizon) from the S47 acoustic construction (lam_horizon=0.8197411121, λ_ref=1.6695681988); builds the Hawking-dressed occupation f'_a = 1/(1+e^{K_a z_a}) with Tolman weight z_a=1/√(−g_00^eff). Bulk faithful on 714 modes (Layer-2 margin 3.523e-04 ≫ EPS=1e-12, witness emitted BEFORE downstream use — guard (b)); 6 floor modes cleanly empty-Fock (guard (c), DSr3-1: K_floor·z_floor → +∞ via gap E_floor=Δ_B3=0.176>0); Δ_{ω'_z}^{it} constructed (dim 714, unitary at t=1). Index-aligned to K_modular bit-for-bit (maxdiff 0.0). Convention `ACOUSTIC-FROZEN-OMEGAPRIME-Z;TOLMAN-REGRADE-K-HAT-ONLY;FLOOR-INTERP-(i)`. Benign canonical_constants plan→runtime SHA drift documented (S106 W1 added constants; verdict invariant per §(ii.B)). Unblocks 2b. Structural status: CONSTRUCTED, registry-CANDIDATE-eligible pending its own 5-anatomy at a future session (NO S106 registry write).

**W2-2 `S106-OMEGAPRIME-AREA-CLOCK`** (connes-ncg-theorist) — **composite INFO** via the plan-frozen composite-precedence operator (sign=PASS+magnitude=FAIL+regime=VALID → INFO, overriding generic-collapse FAIL; companion `# composite-precedence:` row emitted per gate-verdicts.md), audit_sha256=`3ec79a94…a744bc`. GATED on 2a PASS — ran as a genuine comparison. ‖K̂·z − Ĝ_τ‖_op = 1.163407 ≫ tol=1e-3 (magnitude FAIL); W2-3 ungraded precedent 1.773745; Tolman regrade reduced the gap 34.41% but not below tol. Cocycle-generator sign −1 = S97 dS/d(a0/a2) reference (sign PASS; Sage + Python verified). Â=a_2^{ζ}=2776.165389. Guards (a)/(b)/(c) all satisfied (σ_t^{ω'_z}-only comparison; 2a faithfulness witness pre-emitted; floor excluded from op-norm). Dual-prior → 0.9 Track A (Hawking-dressed relic ALSO lacks a geometric area-clock; the Tolman regrade does NOT close the gap below the same bar the ω-side used). Disposition: composite INFO ⇒ NO new bridge candidate, NO S106 registry write, NO mack falsifier-surface row. The surviving registrable relation is the co-monotone-plus-shared-floor bridge (sign −1 + N₃=0 shared-floor empty-Fock), which stands independent of the area-clock outcome.

**W2-3 `S106-CDGM-SPACING-CROSS-BLOCK`** (volovik-superfluid-universe-theorist) — **INFO** (INFO-class by construction; [AUDIT], no 3-tuple), audit_sha256=`90c7eb8a…03b7a1`. INDEPENDENT, ran unconditionally. Per-block CdGM ladder reconstructed from the S84 cache per-mode |λ| (input-path drift s105_w2_2 summary → s84cache per-mode documented per §(ii.B); recon_match=True bit-for-bit). Two readings of ω_0 give OPPOSITE cross-block verdicts: R1 (doubled minigap 2·E_0) relvar 3.2408e-04 = CROSS-BLOCK-INVARIANT (30× inside thr=1e-2); R2 (median consecutive ΔE) relvar 2.8290e-02 = WITHIN-BLOCK-ACCIDENT (2.8× over thr). The gate DERIVES that the χ-surviving rigidity is the MINIGAP (BDI/N₃=0-protected, theorem K12 S105: CdGM +1/2 minigap = bosonic Wightman floor), NOT the upper-level spacing (which tracks Peter-Weyl multiplicity, lstsq max|resid| growing 0.018→0.103). Corrected the plan's tautological R0 baseline (per_block_json `gap` field = canonical Δ_B3 input, not a measured spacing) by using the measured 2·E_0. Cross-sector diagnostic: minigap invariance robust across all three BdG sectors (B3/BCS/B2). Sharpens the surviving co-monotone-plus-shared-floor bridge's rank-≥3-in-sector clause: cross-block at the protected-minigap level only.
