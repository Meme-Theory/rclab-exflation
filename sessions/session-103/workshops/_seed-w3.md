# Seed — Chunk w3 (S103 Wave 3: transit / holonomy)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: session-103-w3-workingpaper.md + session-103-plan-w3.md
**Wave summary**: 2/2 closed — 1 INFO (`S103-FAMP-TOLERANCE-REPIN`, composite collapse sign=PASS/magnitude=INFO/regime=VALID) + 1 PASS (`S103-B2-WZ-HOLONOMY-COSET2`, Track A). Structural reading: a single transit/holonomy carry-forward wave from S102 W7. The INFO is a Class-8.3 publication-precision re-pin whose verdict and resolution path (mpmath/300-bit re-derivation) are BOTH pre-registered in the plan's `INFO_meaning` clause and already carried as CF-S104-W3-SWMAX-MPMATH-EDGE — a settled knife-edge, not a contested reading. The PASS is a `[VERIFY]` gate clearing all three conjuncts by 2.6–5 OOM and reproducing the first-doublet `f_WZ` to 4 sig figs — completing the C² coset span with no reading-divergence. No FAILs, no borderline INFOs admitting competing substrate readings, no cross-wave conflict, no contested convention.

## No candidates

This wave produces ZERO workshop candidates (neither Slot 1 nor Slot 2). The four workshop conditions (TWO+ agents with COMPETING perspectives / genuine ledger-dissonance / multi-round adjudication / structural verdict resolving competing claims) are not met by either gate, and neither gate matches any signal in the rule's §"How to identify a real workshop in session substance":

- **`S103-FAMP-TOLERANCE-REPIN` (INFO) is NOT a workshop (Q1 NO → Q-other carry-forward, already WP-resident).** The INFO is not a FAIL/INFO admitting multiple structural readings. The plan pre-registers the exact outcome: `INFO_meaning` (plan lines 260–263) = "`deviation == S_W_max−1` to within publication precision (knife-edge persists at the exact edge) ... routes to a higher-precision (mpmath) re-derivation carry-forward." The data triggers precisely that clause (deviation and `PASS_TOL` identical at 5 AND 6 sig figs; relative separation 1.79e-06 at the 5.7th sig fig, below the 5-sf publication pin). The substrate reading is single-valued and shared across the WP, the verdict line, and the plan: the re-pin RESOLVED the sign (deviation at-or-inside the EXACT asymmetric edge `S_W_max−1 = 2b²+2ab`; the half-spread `2|a||b|` and literal `0.0029` were both confirmed the WRONG comparison edge) and VINDICATED the substrate (`within_envelope=True`; the DERIVED phase saturates the upper endpoint at `cos_phi_off_axis≈+1`). What remains is a pure float64-vs-300-bit precision question (`<` strict interior vs `==` exact saturation below the float64 floor) — a solo compute follow-up with a pre-registered threshold and machinery pin, NOT a math/physics adjudication between competing readings. The resolution is "run the higher-precision re-derivation," not "derive which of two readings is correct." It is already filed as CF-S104-W3-SWMAX-MPMATH-EDGE in the W3 WP `## Carry-Forward Computations` (consumed by `/rclab-plan`, not the schedule).

- **`S103-B2-WZ-HOLONOMY-COSET2` (PASS) is NOT a workshop (verification gate, "is NOT" item 2).** A clean `[VERIFY]` 3-conjunct membership PASS: `frame_resid = 2.665e-15 < 1e-10` (5 OOM inside the floor), `f_WZ = 2.888785e-06 > 1e-8` (2.6 OOM above threshold; CONVERGES, `loop_conv_delta = 2.72e-11`, not a 1/N artifact), `slope_angle = 1.9999 ∈ [1.8, 2.2]`. The frame-invariance precondition rests on the cyclic-trace identity carried VERBATIM from the W7-3 first-doublet substitution chain (the [3,5] doublet shares the SAME 4-fold-degenerate B2 band and the SAME projector/link construction), so there is nothing to adjudicate — the pre-registered protocol is fully specified and the witness is frame-INVARIANT by construction. The dual-prior re-allocates cleanly (0.6B/0.4A → TrackA=0.9) with no competing structural reading; the retired W5-4 `f_nonAb = 8.89e4` frame-dependent eigh-artifact is cited only for the retirement narrative, not as a live alternative. The Track-A-vs-Track-B dichotomy was pre-registered as a discriminator gate, and the data lands decisively on Track A — there is no INFO-borderline where domain agents would disagree (the `non_scalar_frac = 1.0000` genuine-non-abelian diagnostic is unambiguous, and Schur-consistency holds with no m1/m2 contradiction).

There are no cross-wave conflicts originating in W3 (the W3-2 companion relationship to the W1 §VII.BR/§VII.BV landing is non-blocking and was reconciled in-session — see Cross-wave flags below). Methodology is settled (the bit-exact decomposition correction is explicitly tagged "closed in-session, do NOT propagate" in the WP synthesis). This is the rule's canonical "no workshops is a valid output" case: clean verdicts, pre-registered outcomes, no ledger-dissonance.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W3-2 (`S103-B2-WZ-HOLONOMY-COSET2`, PASS, audit `49705bbc`) COMPANION-STRENGTHENS — but does not gate — the W1 §VII.BR/§VII.BV B2-isotropy-breaking registry landing.** The W3-2 plan's `fb_pair.backward` (plan lines 450–458) names `S103-B2-ISOBREAK-REGISTRY-LANDING` (S103 Wave 1) as the downstream consumer, and the verdict-file annotation (`s103_gate_verdicts.txt` L72) records this as a NON-BLOCKING refinement: "PRIMARY citation remains the S-4-reconciled / Item-4-patched §VII.BR clause + W7-3 first doublet (NOT co-primary)." Per the W3→W4 plan decision point (plan line 558) the W1 landing "cites the S-4 reconciled clause, not this coset2 result, so it does not block." This appears already-reconciled in-session — surfaced only because one investigator per wave means no investigator sees both W1 and W3. The consolidator should confirm the W1 chunk's seed does not raise a conflicting reading of whether the coset2 PASS is a co-primary anchor (it is explicitly NOT co-primary; the C² coset span completion is a coverage-extension companion). If the W1 seed agrees, this is a closed cross-wave consistency, NOT a workshop seed.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other]** — CF-S104-W3-SWMAX-MPMATH-EDGE (higher-precision mpmath/Sage-300-bit re-derivation of `S_W_max` and `deviation` from the frozen W-stage Bogoliubov pair to adjudicate `deviation < S_W_max−1` strict vs `== S_W_max−1` below the float64 floor). ALREADY RESIDENT in `session-103-w3-workingpaper.md §"Carry-Forward Computations"` (4-field spec: what / inputs `s102_w7_ladder_phase_resolved.npz` + `s103_famp_tolerance_repin.npz` / gate = `sign(deviation − (S_W_max−1))` at ≥300-bit ∈ {−1 strict PASS / 0 structural-identity / +1 re-opens S79 sufficiency} / effort 0.25 gate). This is a solo compute follow-up per "is NOT" item 1 — `/rclab-plan` lifts it directly from the WP; no schedule entry and no action required here (flagged only to confirm the investigator saw it and correctly classified it NON-workshop).

No NEW Q2-hygiene item was surfaced by this wave that is absent from `session-103-housekeeping.md`. The W3 wave's only Q2-adjacent items — the bit-exact `2|β_W|²+2|a||b|` decomposition correction and the append-only `canonical_constants.py` SHA-drift handling (§(ii.B)) — are both recorded as in-session process observations in the WP Wave-3 Synthesis ("closed in-session, do NOT propagate"), neither consumes a registry/rule/constant beyond the verdict line, and neither needs a ledger row. No wave-synthesis miss to report.

## Wave-by-wave digest (consolidator background)

**Wave 3 — Transit / holonomy (2 gates, both COMPLETED):**

| Gate | Trigger | Verdict | Result | audit_sha256 (head) |
|:-----|:--------|:--------|:-------|:--------------------|
| `S103-FAMP-TOLERANCE-REPIN` | `[SIGN]` | INFO (sign=PASS / magnitude=INFO / regime=VALID) | Class-8.3 publication-precision re-pin of the S102 phase-resolved F_amp comparator. `PASS_TOL` re-pinned from the down-rounded literal `0.0029` to the EXACT asymmetric upper-envelope edge `S_W_max−1 = 2b²+2ab = 2.9150926e-03` (frozen `envelope_upper_dev`, bit-exact resid 7.6e-17). Deviation `2.9150874e-03` lands AT-OR-INSIDE by +5.21e-09 (sign PASS) BUT identical at 5–6 sf (knife-edge persists → magnitude INFO per pre-registered `INFO_meaning`). Half-spread (+4.23e-06) and literal `0.0029` (+1.51e-05) both confirmed WRONG edges. F_amp slot 0.3885 (UNIFIED-AS-79) + S79 magnitudes-only anchor UNDISTURBED. Routes to CF-S104-W3-SWMAX-MPMATH-EDGE. | `3455a7da` |
| `S103-B2-WZ-HOLONOMY-COSET2` | `[VERIFY]` | PASS (Track A) | Frame-invariant Wilczek-Zee Wilson-loop holonomy witness on the orthogonal off-block C² coset doublet (array-indices [3,5], the W7-3 pre-identified `next_pair`). `f_WZ([3,5]) = 2.888785e-06` matches the first-doublet `f_WZ([4,6]) = 2.8888e-06` to 4 sig figs; `frame_resid = 2.665e-15`, `slope_angle = 1.9999`, `non_scalar_frac = 1.0000`, `n_broken = 4/4` (Release condition R). Completes the C² coset span (λ₄..λ₇): U(2) breaks ISOTROPICALLY across the full coset. Frame-invariance is the cyclic-trace analytic identity carried verbatim from W7-3 (same 4-fold-degenerate B2 band, same projector/link); the retired W5-4 `f_nonAb = 8.89e4` frame-dependent eigh-artifact cited for retirement narrative only. Companion-strengthens (non-blocking) the W1 §VII.BR/§VII.BV landing. | `49705bbc` |

**Standout structural findings (background):**
- The SU(1,1) phase-resolved window `S_W(φ) = |α_W + β_W e^{2iφ}|²` is ASYMMETRIC about 1: the Bogoliubov unitarity offset puts the center at `1 + 2|β_W|²`, not 1, so the exact upper edge is `2|β_W|² + 2|α_W||β_W|` (the half-spread `2|a||b|` is the wrong edge for an asymmetric window). This is the substrate-physics content of the Class-8.3 re-pin — the comparator's PASS predicate is now F-consistent with the SU(1,1) window it tests against. (PHONONIC: `D_K(τ)` trajectory through the van Hove fold → SU(1,1) Bogoliubov coefficients → window factor → F_amp slot → A_s.)
- The B2 quadruplet is exactly 4-fold-degenerate at |λ| = 0.845212 (spread 1.67e-15), so the `eigh` frame IS arbitrary — exactly the artifact source the projector/link witness is immune to by trace-cyclicity. The two orthogonal C² coset directions carrying identical curvature flux (`f_WZ` agreeing to 4 sig figs) is the substrate's statement that the fold's B2 isotropy-breaking is non-abelian across the ENTIRE coset, not coset-anisotropic. (GEOMETRIC: the ε² deformation is the substrate's own off-block metric reorganization at the fold, not a field on a container.)
