# Seed — Chunk w2 (S103 Wave 2: NCG / spectral refinement + external validation)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: session-103-w2-workingpaper.md + session-103-plan-w2.md
**Wave summary**: 4/4 gates closed — 3 PASS + 1 FAIL (substitution-chain-PREDICTED). Structural reading: a clean NCG/spectral-triple registry-refinement wave on `(A_K, H_K, D_K)`. Three of four verdicts matched their pre-registered substitution-chain predictions exactly (W2-1 rank-1, W2-2 strict-< re-anchor, W2-3 FAIL-at-L=10); the fourth (W2-4) is a bit-exact reproducibility PASS at machine zero. No verdict admits a competing structural reading; every forward item is already routed (WP CF or housekeeping §A/§B). Verdicts: W2-1 NNU-BUNDLE PASS (`ac1dbb28`, rank(Cov_aug)=1, σ₂/σ_max=1.07e-17 ≪ 2.3e-11); W2-2 S7-LC-TIER1-REANCHOR composite PASS via Option-A supersedes (`8fe5dc22` supersedes `266a3dfc`; Part-1 strict-< by 6.32 OOM ∧ Stage-2 PASS-AND, both BLIND axes); W2-3 VIIAM-LINDEXED-ANCHOR FAIL (`b47ccf98`, ratio 1.158 > 1 at L=10); W2-4 FOREIGN-STACK-B1B2 PASS (`2d1ed622`, gate_max_diff=0.000e+00 bit-exact on (0,0)/(0,1)/(1,1)).

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

(none)

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

(none)

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W2-3 FAIL ⟷ Tier-2 #7c `S103-BRANCH-IV-DEEP-TRUNCATION` (W5) — SAME structural pattern, not a tension.** Both are §VII Level-3-vs-Level-2 registry-PASS evaluations whose FAIL/INFO routes to a DEEPER-TRUNCATION gate (W2-3 → CF-S104-W2-VIIAM-L11-ANCHOR at L=11; the branch-iv evaluator → L∈{12,13,14} / Friedrich-Bär envelope). The consolidator should note these are two independent instances of the same "L_max=10 truncation not yet inside the dimensionless `L^{−α}` convergence-rate envelope" corridor — NOT a cross-wave contradiction. Cite W2-3 verdict `b47ccf98` + the EVOI Tier-2 #7c row. No adjudication needed; flagged only so the consolidator does not mistake the shared deeper-truncation route for a ledger conflict.

- **W2-1 PASS ⟷ W1 item 6 §VII.BS clause-(b) SCOPE-ANNOTATION (Wave 1) — documented soft coupling, already resolved.** Plan §"Wave 2 Decision Point Prerequisites" states the two touch §VII.BS from DIFFERENT layers (W2-1 = the COMPUTE testing the bundle-exhaustiveness premise; W1 item 6 = the ANNOTATION scoping clause-(b)). On the W2-1 rank-1 PASS the annotation upgrades "standing premise → result" — but that upgrade is CF-S104-HK-1 (housekeeping §B + WP CF, NOT a W1↔W2 edge this session, per the plan's Wave 1→2 decision point). Cite W2-1 `ac1dbb28` + W1-6 `2c27b197`. No tension; surfaced so the consolidator sees the §VII.BS surfaces are touched by two waves.

- **W2-3 canonical_constants.py SHA drift ⟷ W5-2 mid-session COMMIT — cross-wave coupling, zero numeric impact, disclosed.** W5-§W5-2 appended `n_s_FW_sqrt_cutoff` to `canonical_constants.py` (append-only COMMIT) mid-session, drifting the SHA from the plan-freeze pin (`9f2fe998…` → `9cd89e61…`). W2-3 re-pinned at runtime and disclosed per `substrate-first-canonical-sourcing.md §(ii.B)`; `Gamma_effacement=0.99970` unchanged ⇒ zero impact on the W2-3 verdict. Cite W2-3 `b47ccf98` + housekeeping A4. Surfaced only as a within-session ordering coupling (W5 wrote a canonical W2 read); NOT a tension.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other]** — `CF-S104-W2-VIIAM-L11-ANCHOR` (already in WP `## Carry-Forward Computations` + Wave 2 Synthesis): §VII.AM envelope-row Level-3-vs-Level-2 at L=11 under the SAME pre-registered L-indexed rule (anchor(L) := dGamma_over_Gamma[L]; L=11 deviation 2.11e-05 vs the re-evaluated envelope candidates). Solo compute follow-up of the W2-3 FAIL (deeper-truncation route), pre-registered threshold + machinery pin, comparator PRE-REGISTERED at plan-freeze (anti-comparator-shopping). NOT a workshop (per rule "is NOT" item 1) — already routed; listed here only to confirm the W2-3 FAIL's forward item is captured.

- **[Q2-hygiene]** — CF-S104-HK-1 (§VII.BS clause-(b) "standing premise → result" wording upgrade) is ALREADY ROUTED in `session-103-housekeeping.md §B` + WP CF. Do NOT re-propose (a registry prose-wording promotion, designated-writer reviewed patch, no compute). Listed for completeness; no new Q2 item surfaced this wave.

(No NEW Q2 item surfaced for the first time by this investigator — the §VII.BT Stage-3 promotion (A5/A6), the §VII.AM L-indexed annotation (A7), the peel field-label correction (A6), and the W2-4 plan-anchor correction (process obs #3 / verdict extra-row) were all effected in-session per the housekeeping ledger. No wave-synthesis miss to log.)

## Wave-by-wave digest (consolidator background)

**Wave 2 — NCG / spectral-triple registry refinement on `(A_K, H_K, D_K)` (4 gates, all GEOMETRIC-class, connes-ncg-theorist-owned axis):**

- **W2-1 `S103-NNU-BUNDLE-EXHAUSTIVENESS` (gen-physicist) — PASS `ac1dbb28`.** Augmented the borrowed-`H` shift-covariance power matrix with a second candidate scale w2 = m_H/v_ew (the a_4-dressed |S|² KK-threshold transverse-fiber mode); `rank(Cov_aug)=1` (σ₂/σ_max = 1.07e-17 ≪ rank_threshold 2.3e-11). FAIL signature (a w2-touching decorrelated pair) absent (`n_w2_decorrelated=0`, min|Corr|=1.0). Rank-2 synthetic control returns rank 2 (σ₂/σ_max=0.0726 ≫ threshold) ⇒ the discriminator IS sensitive to a genuine second scale; rank-1 is substantive, not a test degeneracy. 5×5 dagger sub-block reproduces the s102 Cov bit-for-bit. Substrate reading: m_H IS the a_4-dressed |S|²-mode of the spectral triple carrying the single M_KK cutoff power, NOT an independent EW scale the substrate lives in — the substrate's own normalization bundle is rank-1-exhausted by the one imported cutoff. Resolves Open Q6 (§VII.BS clause-(b) bundle-exhaustiveness premise) in favor of clause-(b) sufficiency for the augmented bundle. Dual-prior 0.65/0.35 → 0.9 Track A.

- **W2-2 `S103-S7-LC-TIER1-REANCHOR` (connes-ncg-theorist) — composite PASS `8fe5dc22` (supersedes PRE-REG-INC `266a3dfc` via Option-A).** Two-part gate. Part 1 (Tier-1 dimensionless re-anchor): re-pinned the §VII.BT Level-3 from the DIMENSIONFUL residue magnitude |a₂^{Mellin}(LC)| = 0.0126 M_KK² (Tier-2-dimensionful, registry-PASS-INELIGIBLE) to the DIMENSIONLESS truncation invariant `peel_heldout_nolog = 4.95474088e-12`; strict Level-3 < Level-2 (1.039022e-05) HOLDS by 6.322 OOM. Part 2 (Stage-2 cross-axis verify): TWO BLIND reviewers (Axis-A lizzi spectral/NCG; Axis-B volovik substrate/superfluid) dispatched IN PARALLEL by the orchestrator (the executing subagent has no Agent/Task tool — process obs #1), both returned overall PASS; JOINT clauses J1 (Mellin/genesis simple-pole-tower identity) + J2 (Tier-1 re-anchor validity / substrate-natural-binding) PASS in BOTH (logical AND). Axis-B selection 3-condition + substrate-input-orthogonality SATISFIED at the structural ceiling (the genesis-structure Hessian-nondegeneracy witness loaded by Axis-B only). Plan-text drift on the peel field (withlog 1.223e-11 vs log-free 4.95e-12) resolved to npz ground-truth per §(ii.B); strict-< holds under BOTH. Effected in-session (housekeeping A5/A6): §VII.BT STAGE-1-CANDIDATE → STAGE-3-PERMANENT; Level-3 row HELD → registry-PASS (inaugural Tier-1 conversion calibrating `cross-pillar-bridge-anatomy.md` corpus §25); peel field-label correction across 6 spans; the dimensionful M_KK² magnitude stays HELD (Non-Promotion-by-Held-Number differentia preserved).

- **W2-3 `S103-VIIAM-LINDEXED-ANCHOR` (connes-ncg-theorist) — FAIL `b47ccf98` (substitution-chain-PREDICTED).** Re-derived the §VII.AM envelope-row Level-3 anchor as L_max-indexed (`anchor(L=10) = dGamma_over_Gamma[idx 2] = 4.39680e-05`, the per-L convergent effacement deviation) at empirical α=4.690533. Strict anchor(L=10) < envelope(L=10) does NOT hold even with the more-favorable prefactored envelope: ratio 4.39680e-05 / 3.79745e-05 = 1.1578 > 1 (bare envelope ratio 2.156). The L-indexing reduces the anchor 6.82× vs the fixed 3.0e-4 anchor (closing the S102 gap from ratio 7.9 down to 1.16) but does not cross at L=10. Pre-registered L=10-slice rule was the arbiter (NOT a deeper-L comparator search — at L=11 the deviation 2.11e-05 would sit marginally below the bare envelope, but the gate evaluates L=10 ONLY). Theorem-STRUCTURE (§VII.AM Universal Lock Condition Level-1, STAGE-3-PERMANENT per S100a) explicitly UNTOUCHED — only the envelope-ROW Level-2-vs-Level-3 value FAILs. Closes the corridor "L-indexing alone rescues the §VII.AM envelope row at canonical L=10"; forward route is the deeper-truncation L=11 gate (CF-S104-W2-VIIAM-L11-ANCHOR). §VII.AM dated status annotation effected in-session (housekeeping A7). Dual-prior 0.30/0.70 → ~0.9 Track B.

- **W2-4 `S103-FOREIGN-STACK-B1B2` (spectral-geometer) — PASS `2d1ed622`.** Independent foreign-stack re-implementation of the D_K Peter-Weyl block construction, extended from the S102 (1,1)-block to the bottom-of-spectrum (0,0) and (0,1) blocks; reproduces the canonical eigenvalues BIT-EXACTLY (gate_max_diff = 0.000e+00 < pass_eps 1e-12; ≈12 OOM margin) via the H=iD eigvalsh pin (W3-12 Hermiticity-enforced extraction — D_K is anti-self-adjoint, so the physical real eigenvalues come from the Hermitized iD; no general eig / svd surrogate). (1,1) sentinel reproduces the S102 result max_diff=0.0 exactly (generalization introduced no construction drift). Foreign infrastructure self-validates independent of dirac_spectrum.py (Killing form = 3·I, f_123=1, f_458=√3/2, all anti-Hermiticity residuals 0.00e+00). Monoculture-remedy coverage now {(0,0)=B1=0.819741, (0,1)=B2=0.835894, (1,1)=B3=0.872975}. Plan-anchor correction logged (process obs #3 + verdict extra-row): plan W2-4's "B2 ~ 0.845 for (0,1)" mis-assigned — the actual (0,1)/3-bar bottom is 0.835894 (0.845212 is a (0,0)-sector eigenvalue); the gate anchored to the correct cache value and the bit-exact reproduction confirms it. Substrate reading: GEOMETRIC reproducibility check — the eigenvalue spectrum IS the set of all possible vibrational modes; the gate certifies that set is implementation-robust at its three lowest Peter-Weyl blocks. (0,0) is C₂=0 EXACT, the unique lowest SU(3) quadratic Casimir ⇒ the substrate's lowest vibrational mode provably sits in (0,0).

**Why no workshops (honest count):** all four verdicts are single-reading. The three predicted verdicts (W2-1 rank-1, W2-2 strict-< by 6.32 OOM, W2-3 FAIL-at-L=10) matched their pre-registered substitution chains exactly — no FAIL admitting multiple structural readings, no borderline INFO, no convention dispute where two perspectives genuinely diverge. W2-2's Stage-2 was a clean PASS-AND (both BLIND axes PASS on all clauses — convergence, not adversarial divergence). W2-4 is a bit-exact reproducibility PASS. No cross-wave PASS/INFO/FAIL contradiction (the W2-3 ⟷ W5 branch-iv coupling is the SAME deeper-truncation corridor, not a conflict). Every forward item is a pre-registered solo compute (CF-S104-W2-VIIAM-L11-ANCHOR) or an in-session-effected status edit (housekeeping A5/A6/A7) or an already-routed hygiene patch (CF-S104-HK-1). Per `.claude/rules/Investigating-Workshops.md §"No workshops is a valid output"`, this wave produced zero genuine workshops and zero solo-review candidates.
