# Seed — Chunk w3 (External validation / spectral core: Fegan keystone chain, stop-at-first-failure)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: sessions/session-102/session-102-w3-workingpaper.md
**Wave summary**: 4 gates, all `spectral-geometer`, run STRICTLY IN ORDER with stop-at-first-failure gated behind the item-11 keystone. **3 PASS / 0 FAIL / 1 INFO**. W3-11 `S102-FEGAN-TAU0-SPECTRUM-VALIDATION` PASS (substrate τ=0 spectrum == Fegan/Parthasarathy–Kostant 1987 closed form, max diff 8.882e-15, 45 sectors, exact multiplicities, 0 n/36-violations, GPU-vs-CPU 0.00e+00). W3-12 `S102-FOREIGN-STACK-PW-BLOCK-REIMPL` PASS ((1,1) adjoint block rebuilt in Sage CyclotomicField(12) exact arithmetic, max|foreign−canonical|=0.000e+00 bit-exact over 128 |λ|). W3-13 `S102-STRATUM1-LIT-SWEEP` PASS (novelty CONFIRMED, 11 query records, 0 prior-art, 6 re-scoping adjacencies). W3-14 `S102-TRD2-MONOTONICITY-ANALYTIC` INFO (sign=PASS / mag=FAIL / regime=VALID — E7 λ²-moment monotonicity PROVEN in exact closed form; literal magnitude cross-check FAILed on a plan-side functional conflation, anchor=|λ|-action f=√x=58672.8 vs proof=λ²-moment f=x=213991.8). Structurally this is the external-validation spine of the Stratum-1 math-paper pre-submission checklist: all four cold-read boxes ticked. The stop-at-first-failure protocol never fired — the keystone held. The wave is overwhelmingly clean (3 unambiguous machine-ε PASSes + 1 honestly-scoped INFO whose sign-content is proven). Self-validating GEOMETRIC checks, not contested substrate-physics readings.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

### S1-1 — Independent proof-check of the W3-14 λ²-moment monotonicity closed-form theorem before registry landing

**What this answers**: Is the W3-14 closed-form proof of strict ⟨λ²⟩(τ) monotonicity an actually-valid proof, fit to be registered as a permanent theorem (the numerical→analytic upgrade of E7)? Three load-bearing steps deserve an independent eye before CF-S103-W3-2 lands it as a registry row: (i) the exact trace split `Tr D_π² = 16·Casimir_g(p,q;τ) + d(p,q)·Tr(Ω²)(τ)` with the cross-term ≡ 0 (claimed from su(3) tracelessness; machine-ε checked at p+q≤3 only — does it hold symbolically for general (p,q)?); (ii) the EQUIPARTITION THEOREM step `S_su2:S_c2:S_u1 = 3:4:1 = block dimensions` (claimed max-dev 2.84e-13 over p+q≤7 — a *numerical fit certified*, not a symbolic derivation, per §W3-14 Results item 1); (iii) the load-bearing inference `dM₂/dτ > 0 term-by-term per sector ⇒ PW sum monotone ⇒ L-UNIFORM` (the (u−1)·all-positive-cofactor Sage-QQ factorization is exact, but the term-by-term ⇒ L-uniform jump is the structural claim a referee will test). Source anchors: §W3-14 (audit `87163c330d34a118`), verdict line 86 of `s102_gate_verdicts.txt`, the Weitzenböck/Lichnerowicz `D_K²=−∇²+R_K/4` (E5/E3, `baptista-operator-dk-tau.md`), R_K(τ) E3 closed form. Output: a PASS/HOLD on whether the proof closes for general (p,q) symbolically (not just the numerically-certified equipartition fit), feeding the CF-S103-W3-2 registry-landing gate's Stage-2 readiness.

**Why solo, not workshop**: There is no competing reading and no ledger-dissonance — the gate author and a reviewer agree on the goal (validate the proof). This is a structural proof-check (does each step justify the next; is the equipartition step symbolic or fit-certified; does term-by-term imply L-uniform), which one independent agent reads and adjudicates against the source — no cross-rebuttal needed. The `proof-check` skill is purpose-built for exactly this.

**Agents**: `connes-ncg-theorist` (spectral-functional / Weitzenböck-Lichnerowicz owner; the proof is a Tr D_K² spectral-moment identity in its native domain) — default 1.

**Source anchors**: §W3-14 `S102-TRD2-MONOTONICITY-ANALYTIC` (INFO, audit `87163c330d34a1182de49833fcb4568001973f613596a20107ae144f12f28bc3`); verdict-file line 86; CF-S103-W3-2 (the registry-landing gate this de-risks); E5/E3 in `baptista-operator-dk-tau.md`; baseline-findings-s66 S37 (E7 9,600-numerical Structural Monotonicity Theorem).

**Output**: a structural verdict on whether the three load-bearing steps (symbolic cross-term cancellation for general (p,q); equipartition as theorem vs fit; term-by-term ⇒ L-uniform) close cleanly — i.e., whether CF-S103-W3-2 can land the λ²-theorem as STAGE-1-CANDIDATE with the proof intact, or whether the equipartition step needs a symbolic derivation first. (If the reviewer finds a genuine gap, that becomes a compute carry-forward, not a workshop.)

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

*None.* See `## No Slot 2 candidates` reasoning below.

## No Slot 2 candidates

This wave produced no genuine workshop (no Q1a two-agent ledger-dissonance). The four gates are external-validation / reproducibility checks whose verdicts are machine-ε equalities or an honestly-scoped INFO; none admits TWO competing first-principles readings that two agents must cross-rebut to converge. The single INFO (W3-14 magnitude FAIL) is NOT a contested reading: the algebraic fact that the |λ|-action (f=√x, 58672.8) and the λ²-moment (f=x, 213991.8) are different functionals is settled, not adjudicable — the "tension" resolves to a registry anchor re-scope (Q2, already routed to CF-S103-W3-2), not a substrate-physics debate. The only candidate carrying genuine math content for an outside eye is the proof-check (S1-1), which is a solo independent reading, not an adversarial workshop.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W3-14 INFO ↔ EVOI rank 9c (this wave's own keystone row) — STATUS RECONCILED, no tension.** EVOI §2 rank 9c `FEGAN-TAU0-SPECTRUM-VALIDATION` is marked "LANDED S102 W3-11 — PASS (machine-ε 8.882e-15 … whole W3 keystone chain closed)". This is consistent with the WP. Flagging only so the consolidator confirms the EVOI 9c cell already reflects the W3 closure (it does — `sessions/evoi-framework.md:61`); no re-route needed.

- **W3-14 λ²-theorem ↔ the capstone §5.1 E7 anchor + `phonic-exflation-equation.md` dS/dτ narration (potential capstone-hygiene touch).** §W3-14 proves the λ²-MOMENT gradient is `dM₂/dτ = 213991.8` at the fold, distinct from the |λ|-action `dS_fold = 58672.80` (S42) that the capstone §5.1 / EVOI floor-line narrate as "dS/dτ=+58,673". The two numbers are different functionals of the SAME construction (anchor cross-check confirms bit-identical D_K). The WP routes the anchor re-scope to CF-S103-W3-2. **Flag for the consolidator**: this is a capstone-hygiene-gate Q3 touch (a §5.1 status/anchor-functional clarification — "+58,673 is the f=√x |λ|-action gradient, NOT the f=x λ²-moment gradient"), and per `.claude/rules/capstone-hygiene-gate.md` it should appear in the S102 housekeeping ledger if the session's wave-synthesis touched the capstone register. Not a workshop; surfaced so it is not lost between the WP CF and the capstone surface. (The phonon-first memory already carries `dS/dtau=+58673` as the spectral-action gradient — both numbers are real; only the functional-label needs to be pinned.)

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

The wave's WP already enumerates BOTH carry-forwards with full 4-field specs (verified present at §"Carry-Forward Computations", lines 322–341). I re-state them here only for the consolidator's filter; they are NOT schedule entries and require no new action — `/rclab-plan` reads them from the WP directly:

- **[Q2-hygiene]** — **CF-S103-W3-2 — λ²-moment closed-form theorem registration + |λ|-anchor re-scope** (gate `S103-LAMBDA2-MONOTONICITY-REGISTRY-LANDING`). Register the proven λ²-moment monotonicity closed form as a registry theorem row AND re-scope the +58672.8 anchor as the |λ|-action SIGN-corollary of E7's "ALL monotone f" (NOT a magnitude target for the λ²-proof). Resolution = a registry-landing (AFTER-pattern single-shot per `registry-landing.md`) + anchor re-tag = Q2 (registry-state / mechanical promotion). Inputs: `s102_trd2_monotonicity_analytic.npz` (audit `87163c330d34a118`); E7 registry row + capstone §5.1. The S1-1 proof-check above de-risks this landing's Stage-2 readiness; this is the registry-landing itself. Already in WP §"Carry-Forward Computations".

- **[Q-other]** — **CF-S103-W3-1 — Foreign-stack extension to the (0,0)/(0,1) sectors (B1/B2 anchors)** (gate `S103-FOREIGN-STACK-B1B2`). Solo compute follow-up: extend the W3-12 foreign-stack monoculture remedy from the (1,1) block to the bottom-of-spectrum triple; PASS iff max|foreign−canonical|<1e-12 per block, eigenvalue extraction via H=iD eigvalsh ONLY (the W3-12 methodology pin). A verification-gate-style reproducibility compute with a pre-registered machine-ε threshold — NOT a workshop. Already in WP §"Carry-Forward Computations".

- **[Q2-hygiene / process-observation]** — **cross-stack extraction methodology pin** (W3-12 finding): degenerate anti-Hermitian D requires H=iD + `eigvalsh` extraction; a general complex eigensolver fabricates spurious mismatches (Sage `.eigenvalues()` produced 33 spurious distinct |λ| where the true spectrum had 3) on a bit-identical construction. The WP already closed this in-session as a constraint-map documentation entry (line 350). No further action — both a methodology-aware reviewer and the author agree; a rule/audit-note both would derive the same way = Q2. Noted so the consolidator does not re-promote it.

## Wave-by-wave digest (consolidator background)

**Wave 3 — cold-read keystone chain (4 gates, spectral-geometer, stop-at-first-failure).**

- **W3-11 `S102-FEGAN-TAU0-SPECTRUM-VALIDATION`** — **PASS** (KEYSTONE, checklist box 1, referee M8(a)). The substrate's own τ=0 bi-invariant SU(3) Dirac spectrum reproduces Fegan/Parthasarathy–Kostant 1987 closed form `|λ|²=(1/6)[C₂(μ)+C₂(p,q)]+1/4` ≡ `n/36` integer mesh: max eigenvalue-multiset abs-diff 8.882e-15 < 1e-12; 0 degeneracy/block-length/n-36 mismatches; 45 sectors; GPU(eigvalsh)-vs-numpy 0.00e+00. The (1/6, 1/4) normalization is READ OFF the substrate's own |B|-frame (slope from g₀=|B|=3·δ, offset from trivial-sector RHO_OFFSET=¼), NOT fitted. Three incommensurate corpus conventions (`C₂+3/4`, `C₂+3`, `√(7/3)`) explicitly identified and excluded — the convention trap is closed. The single check no internal agent can bias; everything downstream of the τ=0 construction is now externally anchored. audit `24cdba4bf29ce6a3`.

- **W3-12 `S102-FOREIGN-STACK-PW-BLOCK-REIMPL`** — **PASS** (box 2, referee M8(b) monoculture remedy). The (1,1) adjoint Peter-Weyl block at τ_fold rebuilt end-to-end on a foreign stack (Sage CyclotomicField(12) exact arithmetic + independent numpy path, NEITHER importing `dirac_spectrum.py`/`branching_computation.py` for construction): max|foreign−canonical| = 0.000e+00 bit-exact over all 128 |λ|; cache cross-check 0.00e+00; foreign-numpy vs Sage-exact agree to 2.22e-16. B3 anchor 0.872975 reproduced. The block spectrum is implementation-independent — a property of the geometry, not the numpy pipeline. Methodology finding (closed in-session): cross-stack Dirac extraction MUST route through H=iD eigvalsh (a general complex eigensolver scatters the degenerate anti-Hermitian spectrum). audit `e3770af3cb1168d9`.

- **W3-13 `S102-STRATUM1-LIT-SWEEP`** — **PASS** (box 3, novelty confirmation; NON-COMPUTE search-trace gate). Item-6 (full Jensen-line SU(3) Dirac spectrum, 155,984 eigenvalues w/ multiplicity at L_max=10) + item-7 (van Hove DOS cusp at τ_fold) → **CANDIDATE-NOVEL CONFIRMED**. 11 documented query records across 6 pinned families (zbMATH/arXiv/MathSciNet-MRef); 0 prior-art hits for the item-6/7 core; 6 re-scoping adjacencies: the only published SU(3) Dirac spectrum is the bi-invariant τ=0 endpoint (Lai-Teh Zbl 900147539 / Teh thesis MR3153451), the left-invariant *deformation* literature is Laplace-only (Lauret school), complete-Dirac-spectrum results stop at SU(2)/3-spheres and lens spaces (Boldt-Lauret). The adjacencies re-scope (cite, frame as the Dirac analog of the Laplace-deformation program), not refute. Tooling note: MathSciNet MCP is FREE-tier (MRef-only, no keyword search); zbMATH+arXiv cover the surface; Google-Scholar avoided per the rate-limit memory. audit `2117a30846ddf583`.

- **W3-14 `S102-TRD2-MONOTONICITY-ANALYTIC`** — **INFO** (sign=PASS / mag=FAIL / regime=VALID; box 4, timeboxed proof attempt, E7 numerical→analytic promotion). The literal E7 content — strict monotonicity of ⟨λ²⟩(τ) — is **PROVEN in exact closed form**: `M₂(p,q;τ) = (2/3)C₂d(3e^{2τ}+4e^{−τ}+e^{−2τ}) + d(5e^{2τ}+4e^{−τ}+2e^{−2τ}+½e^{−4τ}+½)`, and `dM₂/dτ = d·[C₂·gC + gS]` with `gC·e^{2τ}=(u−1)(4u³+4u²+4u+4/3)` and `gS·e^{4τ}=(u−1)(10u⁵+10u⁴+10u³+6u²+2u+2)` (u=e^τ, Sage-QQ remainders 0) — both factors `(u−1)·(all-positive-coefficient cofactor)`, zero iff τ=0, strictly >0 for ALL τ>0, term-by-term per sector ⇒ L-UNIFORM (min over p+q≤10, τ∈(0,τ_NEC) = +1.733e-04). The magnitude cross-check FAILed (ratio 2.647) because the +58672.8 anchor is the |λ|-spectral-action gradient (f=√x) while the proof delivers the λ²-moment gradient (f=x = 213991.8) — a DIFFERENT functional; explicitly NOT redefined to manufacture a PASS (would be convention-shopping). Composite collapsed to INFO via the plan-frozen-operator precedence clause (`composite-precedence` disclosure row, pre-declared in plan via `INFO_meaning` + `dual_prior`). Anchor cross-check reproduced `dS_full/dτ|_fold=58672.819` vs canonical 58672.802 (rel err 2.87e-07) — the D_K construction is bit-identical to the S42 anchor, so the functional difference is real, not an artifact. E7's 9,600-numerical status UNCHANGED; the λ²-theorem is additive. (Process note: the W3-14 agent's host process died in a crash AFTER writing all 5 artifacts; on-disk state complete and verified; only its final report message was lost.) audit `87163c330d34a118`.

**Net structural reading**: the wave is the external-validation spine for the Stratum-1 math-paper. It establishes (i) an external closed-form anchor no internal agent can bias (Fegan, machine ε), (ii) implementation independence (foreign exact-arithmetic stack, bit-exact), (iii) confirmed Jensen-line novelty (documented trace, 0 prior art), (iv) the exflationary gradient's SIGN as an analytic theorem. Substrate-first throughout: D_K eigenvalues → ⟨λ²⟩(τ) → the spectral-complexification gradient that IS exflation. The one downstream action carrying genuine math content for an outside eye is the proof-check of the λ²-theorem before it lands as a registry row (S1-1).
