# Convergence Rollup — A_s amplitude-normalization wall (CV-1)

**Synthesizer:** lizzi-spectral-functional-theorist.  **Date:** 2026-06-20.  **Digests netted:** inv-3, inv-4, inv-5, inv-6, inv-10, inv-12.

**What this is.** A Stage-2 net of the six independent attacks on the framework's #1 observational gap — the scalar-amplitude A_s — back against the inv-1 CV-1 framing. The deliverable is the single framework-level four-verb verdict: after six routes, did the A_s wall converge or fracture? Register status verified against the knowledge MCP this session (AMPLITUDE-NORM-66 FAIL 3.15 OOM; CF23/A_s-floor HARDENED-PERMANENT S83; `S_IC=1+2n_k≥1` PROVEN `proven_1097`; TRANSIT-PS-67 OPEN/CRITICAL; falsifier-rigor Row 8 `SCHEME-DEPENDENT` 5.078e-9 TD-canonical vs 2.099e-9 Planck).

**Substrate-first frame (held throughout).** A_s is the post-transit GGE-relic acoustic squeezing modulus, NOT a ΛCDM inflaton normalization. The arrow is `D_K eigenvalues → transit Bogoliubov {α_k, β_k} → produced occupation n_k = |β_k|² → squeezing modulus S_IC = 1+2n_k → relic power-spectrum amplitude A_s`. "The wall" is a statement about that produced amplitude relative to its Bunch-Davies infimum, not about a container the perturbation lives in.

---

## 1. The convergence (inv-1 framing)

**The claim (inv-1 §CV-1, 8 agents — gen-physicist, kaluza-klein, spectral-geometer, landau, transit-dynamics, quantum-acoustics, hawking, feynman).** A_s is the framework's single largest observational gap, and the survey diagnosed a single root cause: *an equilibrium free-energy functional applied to a sudden (impulsive) quench*. The prescribed fix was unanimous across vantages — a **Bogoliubov |β_k|² / Parker-Bogoliubov power spectrum with adiabatic regularization**, not slow-roll `1/ε_H`.

**The incoherence (the reason CV-1 was the canonical MUDDLED-test).** The "permanent wall" disagreed with itself: the A_s miss was quoted at **3.02 / 3.15 / 4.56 / 9.5 OOM across the corpus, and even sign-flipped** (S66 under-production vs S74/S77 over-production) — a ~12.6-OOM sign-flipping spread on a single observable. inv-1's verdict: *no A_s gate has a meaningful threshold until one canonical number is defined.*

**The bridges.**
- **B-1 (TRANSIT-PS-67)** — run the transit power spectrum as a Parker-Bogoliubov |β_k|² spectrum; the gate the framework's own constraint-mega-matrix calls **CRITICAL** ("resolves α_s falsification 5.0σ, A_s normalization 3.15 OOM, n_s(k) simultaneously") and that **had never been run** (only MIGRATED placeholder entries). inv-1's single highest-EVOI item.
- **B-13 (a₄ conformal-anomaly)** — the one un-closed geometric spectral-action CC channel, cross-listed because A_s and the CC both probe whether `Tr f(D²)` is the substrate's effective action.

**The prior (register state entering the investigations).** CF23 / A_s-floor was **HARDENED to PERMANENT WALL** (S83): "the A_s amplitude floor (3.02× Planck) is a permanent structural-position wall, not remediable at the substrate-IC layer." The AMPLITUDE-NORM-66 gate stands **FAIL (marginal)**, 3.15 OOM, "right ratios, wrong amplitudes." So the entering prior is **PERMANENT structural-position, but SCHEME-DEPENDENT in magnitude and internally OOM-incoherent**. The Stage-2 question: did six attacks dissolve the wall, close corridors around it, pin one canonical number (resolving the incoherence), or leave it fractured?

---

## 2. Per-route ledger

The six routes are structurally distinct attacks on the same wall (this is what makes CV-1 a genuine 6-route hub, not six restatements). Each row: the route, its gate(s), verdict, the digest's four-verb tag for the A_s-bearing finding, and the magnitude that route reports.

| Investigation | A_s route / gate | Verdict | Digest verb | Magnitude (OOM / σ / structural) |
|:---|:---|:---|:---|:---|
| **inv-3** (kitaev rev.) | Near-floor-DOS / ζ′(0) amplitude floor — `INV3-W2-3` (A_s = (M_KK/M_Pl)²·F_nearfloor) | INFO (sign PASS/mag INFO/regime VALID) | **CLARIFIED** | **gap_OOM = +6.008** (single n_s-selected, a_n^ζ FULL, reduced M_Pl); F_nearfloor=2.30 intensive/robust (span 0.19 OOM); ALL weakness in the dimensionful prefactor (M_KK/M_Pl)²=9.3e-4 |
| **inv-4** (einstein rev.) | Exit-horizon greybody via **black-hole-thermodynamics** — `INV4-W1-4-EXIT-GREYBODY-A-S-NORMALIZATION` (A_s=\|β_fold\|²∫Γdω) | **FAIL** (sign PASS/mag FAIL/regime VALID) | **CHALLENGED** (corridor closed, wall strengthened) | greybody supplies only **−0.04 OOM** (`f_grey=0.913`), ~80× short of the 3.15-OOM gap; model-independent to 1.3e-6 (unitarity-bounded) |
| **inv-5** (lizzi rev.) | Impulse-quench Bogoliubov \|β_k\|² / KZ-cell normalization — `INV5-W2-1` | INFO (sign PASS/mag INFO/regime VALID) | **CLARIFIED** (strongly) | one number: **+0.86 OOM** (A_s=1.54e-8, k̂=53.3 M_KK); the **+9.16-OOM** figure reproduced EXACTLY by the naive aggregate-occupation dump `n_pairs/2π²` (the sign-flip artifact identified) |
| **inv-6** (kaku rev.) | Parker-Bogoliubov P_ζ(k) + adiabatic counterterm — `INV6-W2-2-TRANSIT-PS-PARKER-BOGOLIUBOV` | **FAIL** (regime BREAKDOWN) | **CHALLENGED** (constructive; direction-correct) | **+1.455 OOM** (A_s=5.99e-8; adiabatic reg moved A_s DOWN 1.695 from +3.15, sign-definite-negative); K_pivot=0.975 M_KK derived; adiabatic regime valid only **49.25% < 50%** (BREAKDOWN) |
| **inv-10** (transit-dyn rev.) | **B-1 TRANSIT-PS-67** build (mode-by-mode P(k)) — `W2-1` (BUILT); `W4-1` A_s-normalization adjudication — **NOT STARTED** | INFO (sign PASS/mag PASS/regime MARGINAL) + **gate-finalization gap** | **CLARIFIED** (BUILD) / **MUDDLED-edge** (tilt-leaf) | TRANSIT-PS-67 **assembled end-to-end for the first time** (89 BZ modes, L-saturated); BZ-leaf is k³ BLUE n_s=2.9998; red tilt lives only at Goldstone-pivot leaf (54.04-decade scale-tag). **A_s scoped OUT** (`convention=ABSOLUTE-SHAPE-ONLY`) → handed to W4-1, which **never ran**. The amplitude itself was not pinned by inv-10. |
| **inv-12** (connes rev.) | **3-route hub**: reference-state `W1-2-A-S-GGE-MODULAR-REFERENCE` / families-index `W2-5-FWD-C1-BISMUT-CHEEGER-ETA` / dynamical `W3-4-GREYBODY-FROM-BDG` (+ `W3-2` Floquet, `W3-5` CF21) | W1-2 **FAIL** · W2-5 **INFO** · W3-4 **FAIL** · W3-2 **PASS** · W3-5 **PASS** | **CHALLENGED** (floor hardened, 3rd vantage) + **CLARIFIED** (floor↔edge split) | floor **reference-state-INDEPENDENT** (`Δ=+1.04e-13 > 0` — GGE reference RAISED A_s by 0.002%; `K_sub∈[1.00002,1.00384]≥1`); η-form **≡0** EXACT (signed channel carries no normalization); greybody `∫Γ_derived=0.036` vs fitted 0.512 (filter knob exposed, 14× short); Floquet `fraction_resonance=0.0` (relic survives → overproduction genuine) |

**Cross-route consolidation facts (load-bearing for the net):**
- **The two greybody routes agree by independent machinery.** inv-4 W1-4 (black-hole-thermodynamics) and inv-12 W3-4 (analog-gravity BdG-fluctuation) are the SAME object Γ(ω) reached two ways, and BOTH find the substrate-derived transmission far short of what an A_s collapse needs (−0.04 OOM and ∫Γ=0.036-vs-fitted-0.512). Do NOT merge — the two-route agreement that the exit filter is non-substrate is the strength.
- **The two impulse/Parker routes agree on direction and rough magnitude.** inv-5 (+0.86 OOM, ξ̂³ KZ-volume normalization) and inv-6 (+1.455 OOM, Parker adiabatic counterterm) BOTH land the impulse-quench A_s at sub-2-OOM OVER-production, sign-definite-positive — confirming the inv-1 root-cause diagnosis (equilibrium-functional-on-quench) AND its fix direction (the wrong functional inflated the number; the right one collapses the 9.5-OOM cliff to <2 OOM).
- **inv-3 localizes the residual to the dimensionful prefactor.** Its +6.008-OOM is the near-floor-DOS functional (a DIFFERENT, IR-dominated functional than the impulse-quench), and it isolates that ALL the weakness is the intensive→extensive prefactor (M_KK/M_Pl)²=9.3e-4 (the M_KK keystone, CV-2), with the dimensionless part F_nearfloor=2.30 robust. This is the FUNCTIONAL-DEPENDENT magnitude axis made explicit: the number depends on which functional you weight the spectrum with.
- **inv-12 is the hub** — the only investigation attacking the wall on all three structurally-orthogonal axes (reference-state / families-index / dynamical) simultaneously, with the integrative W4-3 synthesis. Its net (floor confirmed, magnitude+filter relocated) is the anchor for the cluster net below.

---

## 3. NET framework verdict

**DOMINANT: CLARIFIED.** The six routes CONVERGED, and the convergence is a clean **functional-independent / functional-dependent decomposition** of an object the register had been narrating as a single number. The inv-1 incoherence — "the wall disagrees with itself, 3.02/3.15/4.56/9.5 OOM + sign-flip" — is **RESOLVED, not deepened**: those are not four measurements of one quantity that disagree, they are **the SAME sign-locked floor evaluated under different spectral functionals + occupation inputs + H̃-branches**. The OOM-spread incoherence was a category error in the register prose (conflating a sign-locked floor with a scheme-soft magnitude), and the six investigations independently dissolved it. This is the lizzi-signature outcome in its purest form: *what survives all functional choices is structural (the floor); what depends on the choice is a physical/scheme degree of freedom (the magnitude) that must be pinned by one canonical convention, not functional-shopped.*

It is **NOT MUDDLED** — the routes do not disagree; they triangulate. It is **NOT BOLSTERED** — no route dissolved the wall or lowered A_s toward Planck (the closest any came was inv-12 W1-2's `Δ=+1.04e-13 > 0`, a 0.002% RAISE). It is **NOT (net) CHALLENGED** — although four of the six route-findings are individually CHALLENGED-corridor-closures, those closures CONFIRM the floor and ELIMINATE escape corridors; the framework-level effect on CV-1 is clarification (the wall stands, sharpened) rather than constraint of a still-live claim.

### Did the OOM-spread incoherence resolve or persist? — RESOLVED.

This is the heart of the deliverable, so I state it precisely. The 12.6-OOM sign-flipping spread inv-1 flagged decomposes, after six routes, into a 2-layer structure that is internally coherent:

| inv-1 corpus figure | What it actually is (post-convergence) | Route that pinned it |
|:---|:---|:---|
| **+9.5 OOM (over, S74/S77)** | the naive **aggregate-occupation dump** `n_pairs/2π²` — a normalization artifact, NOT a per-mode amplitude | inv-5 W2-1 (reproduced it EXACTLY, then discarded it) |
| **−3.02 / −3.15 OOM (under, S66/S83)** | the **slow-roll / equilibrium-functional** route (`1/ε_H`) — the WRONG functional inv-1 diagnosed | inv-1 root-cause; inv-3/5/6 superseded it |
| **+0.86 → +1.455 OOM (over)** | the **impulse-quench Bogoliubov \|β_k\|²** route — the RIGHT functional, sub-2-OOM over | inv-5 (+0.86), inv-6 (+1.455) |
| **+6.008 OOM (over)** | the **near-floor-DOS ζ′(0)** functional — IR-dominated, prefactor-(M_KK/M_Pl)²-limited | inv-3 W2-3 |
| **4.56 OOM (atlas-04 figure)** | a **STALE H̃-space figure**; W3-5 corrects it to 4.76 OOM (=2×2.38 via CC3), an H̃-window identification | inv-12 W3-5 |
| **5.078e-9 / 0.384 OOM (TD-canonical)** | the **falsifier-rigor Row 8** SCHEME-DEPENDENT canonical (vs 2.099e-9 Planck) | register (S84 AS-PIN-MAP-COMMIT) |

The sign-flip is explained: the under-production figures are the slow-roll functional, the over-production figures are the impulse-quench functional, and they are the SAME floor seen through two functionals — not a self-contradiction. **What is permanent is the inequality `A_s ≥ A_s^{BD}` (the sign); what floats is the number.** The spread did not persist as an incoherence; it persists only as the (correct, registered) statement that the magnitude is SCHEME-DEPENDENT.

### Did the investigations finally pin ONE canonical A_s number? — NO, and that is the correct outcome, not a failure.

No single canonical A_s number was pinned, but this is now understood to be **structural, not a gap to be closed by harder work**: the magnitude is genuinely FUNCTIONAL-DEPENDENT (a physical/scheme d.o.f.), and the routes pin it to a **narrow band of sub-2-OOM over-production under the physically-correct (impulse-quench) functional** [inv-5 +0.86, inv-6 +1.455], with the residual spread traced to two named legs (occupation input + H̃-branch). The framework's honest A_s status, netted across six routes:

> **A_s is bounded BELOW by a permanent FUNCTIONAL-INDEPENDENT positivity wall** (`A_s ≥ A_s^{BD}`, `S_IC=1+2n_k≥1`, forced by `n_k≥0` and `|α|²−|β|²=1`; PROVEN `proven_1097`; confirmed on three orthogonal axes — reference-state, families-index η-form, dynamical-Bogoliubov), **and bounded ABOVE by a scheme-dependent magnitude + a currently-fitted exit filter** (∫Γ_derived=0.036 vs fitted 0.512; underived). "Bounded, not predicted."

### Facet sub-verdicts

- **Floor (sign, `A_s ≥ A_s^{BD}`): BOLSTERED → now structural.** Three orthogonal axes (inv-12 W1-2 reference-state / W2-5 families-index / W3 dynamical) independently confirm it; inv-5 and inv-6 confirm the produced-side over-production directly. This is genuine independent confirmation (orthogonal derivations, not shared-context agreement) per `joint-theorem-promotion.md` / `epistemic-discipline.md`. **FUNCTIONAL-INDEPENDENT — permanent classification.** The S83 "permanent structural-position wall" is CORRECT for this object and now confirmed from two vantages S83 lacked.
- **Magnitude (the number 3.02/2.42/1.57/+6.008/+0.86/+1.455): CLARIFIED → SCHEME-DEPENDENT.** Pinned to sub-2-OOM over-production under the impulse-quench functional; the spread is the (registered, correct) SCHEME-DEPENDENT tag, traced to occupation-input + H̃-branch. **FUNCTIONAL-DEPENDENT — permanent classification.** The "3.02× permanent" register prose OVER-STATES: only the inequality `>1` deserves "permanent," not the number.
- **Exit filter (greybody ∫Γ): CHALLENGED → relocated, OPEN.** Two independent machineries (inv-4 black-hole-thermo, inv-12 analog-gravity-BdG) agree the static substrate barrier reflects ~96% of the band; the fitted 0.512 is a tuning knob with no static substrate scale. Relocated to ONE decisive forward compute (dynamical near-horizon resonance scan from the finite quench rate τ̇).
- **B-1 / TRANSIT-PS-67 (the bridge inv-1 ranked #1-EVOI): partially BOLSTERED.** The gate was **BUILT end-to-end for the first time** (inv-10 W2-1) — the "never run" hole is closed for the P(k) shape and tilt — **but the A_s amplitude leg was scoped OUT and its dedicated adjudication (inv-10 W4-1) never ran.** So B-1 is built-for-shape, un-run-for-amplitude. The amplitude is carried by the inv-5/inv-6 impulse-quench routes instead.

---

## 4. Adversarial tensions (→ WORKSHOP, Q1)

I surface **one** genuine A_s-cluster Q1 adjudication. Honest-count discipline (`Investigating-Workshops.md`): the cluster's two headline incoherences (the OOM-spread, the floor-vs-edge reading) were RESOLVED in-investigation (inv-12 W4-1/W4-3 converged; inv-5 dissolved the sign-flip), so they are NOT live workshops. The two inv-10 chartered A_s workshops (W4-1, W4-2) **never ran** — they are gate-finalization gaps, not adjudicated tensions; their Q1-content is real but they route to re-dispatch/escalation, not to a NEW seed (they are recorded in §5 as the build-CF gap). The one surviving genuine ledger-dissonance:

### WS-AS-1 — Is the impulse-quench A_s over-production REAL, or a remaining reference/occupation-state artifact? (real-overproduction vs reference-state-artifact, sharpened to the occupation-input axis)

- **Tension (A vs B), both cited:**
  - **Reading A — real over-production (inv-5 W2-1 + inv-6 W2-2 + inv-12 W3-2):** the impulse-quench |β_k|² is a genuine produced amplitude. inv-12 W3-2 (`fraction_resonance=0.0`, relic NOT re-pumped) certifies the over-production survives the post-fold ringing; inv-5 lands +0.86 OOM and inv-6 +1.455 OOM, both sign-definite-over. The floor is the physical produced state.
  - **Reading B — the over-production magnitude is occupation-input-soft to the point of being scheme-chosen (inv-12 W3-1 + W1-2 + the falsifier-rigor SCHEME-DEPENDENT tag):** inv-12 W3-1's summed ρ_relic carries a truncation band (p+q≤7→8, rel 0.43, `truncation_consistent=False`), and `K_sub−1` ranges ~2000× across occupation inputs (locked-relic n̄≈2.74e-4 vs S43-band n≈0.5). Under Reading B, the "+0.86 to +1.455 OOM over-production" is not a pinned physical number but the artifact of an un-converged occupation input — the magnitude is genuinely undetermined until the relic content converges in L_max.
- **Why Q1 (not Q2/Q3):** the disagreement is about WHAT the impulse-quench A_s magnitude IS — a converged physical over-production (Reading A) vs an L_max-/occupation-soft quantity that cannot be pinned without a convergence argument (Reading B). The two readings invoke different machinery (the locked per-mode {β_k} ODE + Floquet survival vs the Casimir-ceiling truncation band on summed ρ_relic) and **cannot both be the final word**: either the sub-2-OOM number is a result, or it is a band awaiting a Friedrich-Bär/Casimir convergence verdict on the relic content. This is the one A_s tension the six investigations did NOT resolve — inv-5/inv-6 report it as a number, inv-12 W3-1 reports its source as banded.
- **2-agent question:** **lizzi-spectral-functional-theorist ↔ transit-dynamics-theorist**, with **nazarewicz-nuclear-structure-theorist** as the Friedrich-Bär/Casimir-truncation method cross-reviewer. R1: each steelmans whether the summed relic occupation `Σ mult·|β_k|²` converges in L_max (transit: the per-mode {β_k} are locked and L-saturated for the shape; lizzi: the SUMMED content is a Casimir-ceiling-banded extensive quantity, R-Protection-fragile, not L-saturated). R2: rebut + converge on whether the impulse-quench A_s magnitude is (a) a converged physical number, (b) an L_max-soft band requiring a convergence-extrapolation verdict, or (c) genuinely scheme-chosen (no convergence pins it). STRUCTURAL VERDICT: which of {A-real / B-banded / scheme-chosen} is correct, and — if banded — the pre-registered L_max convergence test. *Note: this does NOT touch the FLOOR (`A_s≥A_s^{BD}`, permanent on three axes either way); it adjudicates the over-production MAGNITUDE's epistemic type only.*

*(Honest-count check: I considered promoting the "wrong-functional vs wrong-signature" CC/SA tension and the two-greybody-route question. Neither is an A_s-cluster Q1 workshop: the SA-functional tension is inv-5/inv-12's CV-4 territory (CROSS-LINK §6), resolved to a typed-repair COMPUTE-CF, not a live A_s reading; the two greybody routes AGREE (both find the filter non-substrate), so there is no dissonance to adjudicate — they fold into the single greybody-scan COMPUTE-CF below. I do not manufacture A_s workshops from tensions the investigations resolved or that route to compute.)*

---

## 5. Validated forward bridges (→ COMPUTE-CF)

Four A_s-cluster compute carry-forwards survived/became actionable. EVOI-ordered. Each 4-field (what / inputs / gate / effort) + EVOI tier. These are the constructive output of the convergence — the routes that closed corridors leave a sharp, named forward gate behind each closure.

### CF-AS-1 — Session-promote TRANSIT-PS-67 (the B-1 build) into a `session-{N}` gate. **EVOI: HIGH.**
- **What:** re-compute the inv-10 W2-1 mode-by-mode `P(k)` + two-leaf n_s/α_s under a session-{N} gate so the framework's #1-flagged CRITICAL gate (constraint-mega-matrix) becomes a PERMANENT result. Investigation results enter the index ONLY via session migration (`gate-verdicts.md §Track-local boundary`). Carry the BZ-leaf-vs-Goldstone-leaf scale-tag (S57/S62 Mode-Independent Occupation, deg(T_BZ→pivot)=+2) explicitly so the BLUE-vs-red tilt-leaf distinction is registered, not re-litigated.
- **Inputs:** `inv10_w2_transit_ps_build.npz` (P(k), n_k, 89 BZ modes); `s84_spectrum_cache_L12_tau019.npz`; canonical `n_s_framework=0.9561`, S57/S62 theorem, deg(T_BZ→pivot)=+2.
- **Gate:** PASS iff the session re-run reproduces n_s(CMB-pivot)=0.9561 in-band AND |α_s|<0.019 at the Goldstone leaf AND `truncation_consistent=True`; INFO if the regime stays MARGINAL (all-frozen-superhorizon).
- **Effort:** 0.5 wave (script exists; work is the session-gate wrapper + scale-tag registration).
- **Why HIGH:** closes inv-1's single highest-EVOI item into permanence. NOTE — this promotes the SHAPE/tilt; the AMPLITUDE leg is CF-AS-2 (the inv-10 W4-1 amplitude adjudication that never ran). The two must be migrated together so TRANSIT-PS-67 lands whole, not shape-only. This subsumes the un-run inv-10 W4-1/W4-2 gate-finalization gaps for the amplitude side.

### CF-AS-2 — A_s upper-edge greybody: dynamical near-horizon resonance scan (THE decisive A_s magnitude/filter compute). **EVOI: HIGH.**
- **What:** scan the BdG fluctuation potential at the τ≈0.16 exit horizon for ANY substrate-derived barrier — static OR dynamical-resonance from the finite quench rate τ̇(τ) — whose half-transmission `ω_½=√V₀^sub` lands inside the relic band [0.94, 3.72] M_KK AND reproduces the fitted ∫Γ=0.512±10%, OR prove none exists. The un-scanned candidate is the **dynamical** near-horizon resonance (finite-rate Floquet-WKB for the time-dependent ω_k(τ)), NOT the static surface-gravity barrier inv-4 W1-4 AND inv-12 W3-4 both ruled out.
- **Inputs:** inv-12 W3-1 locked {α_k,β_k} + pair_band [0.94,3.72]; inv-12 W3-4 V_eff scanner + Pöschl-Teller machinery; inv-4 W1-4 black-hole-thermo greybody (the two-route cross-check — consolidate both V_eff machineries); τ̇(τ) near-exit trajectory (S95-W4-2-HAWKING-ANALOG-T-LEDGER); canonical κ_exit=47.6146, Δ_BCS=0.4642547, T_acoustic=0.112; fitted comparator ∫Γ_fitted=0.512.
- **Gate:** PASS (A_s bounded-AND-derived) iff ∃ substrate V₀^sub with √V₀^sub∈band AND ∫Γ=0.512±10% (RATIO tol); FAIL (A_s bounded-but-filter-fitted, permanent upper-edge knob) iff every substrate scale gives ω_½ outside band; INFO iff a resonance lands in-band but ∫Γ misses 0.512 by >10%. Pre-register finite-rate WKB regime_verdict (auto-shortening clause).
- **Effort:** ~1 agent-session (transit-dynamics-theorist); single decisive gate, all inputs on disk.
- **Why HIGH:** positive-EVOI by the inv-12 W4-1 argument — the outcome is contingent on un-run quench dynamics τ̇(τ), NOT theorem-forced. It decides the LAST open leg of the framework's #1 wall: "A_s bounded-and-derived" vs "bounded-but-filter-fitted." Consolidates the inv-4 + inv-12 two-route greybody machinery (do not run twice).

### CF-AS-3 — Session-promote the impulse-quench A_s canonical pin (the magnitude under the correct functional). **EVOI: MEDIUM.**
- **What:** session-promote the impulse-quench A_s number — inv-5's +0.86 OOM (A_s=1.54e-8, k̂=53.3 M_KK, k̂/k_pivot=3.72 deg(T_BZ→pivot) mapping) cross-checked against inv-6's Parker +1.455 OOM — to `canonical_constants.py` (the 3 named quantities) so the impulse-quench A_s becomes import-visible, and have `mack-cosmic-bridge` (sole writer) update falsifier-rigor Row 8. GATED on / informed by WS-AS-1 (if the magnitude is banded, the pin records the band, not a point).
- **Inputs:** `inv5_w2_1_*.npz`, `inv6_w2_2_*.npz`; falsifier-rigor Row 8 (current 5.078e-9 TD-canonical); canonical k_pivot, deg(T_BZ→pivot)=+2.
- **Gate:** PASS iff one defensible impulse-quench A_s magnitude (or band, per WS-AS-1) lands with its scheme-tag + OOM-distance; the falsifier-rigor row keeps `SCHEME-DEPENDENT` for the magnitude AND adds the FUNCTIONAL-INDEPENDENT/PERMANENT floor sub-annotation (3 orthogonal axes). Closure: canonical_constants entry + PROVENANCE + mack row (artifact-existence).
- **Effort:** ~0.3 wave (mechanical promotion with the WS-AS-1-determined value/band).
- **Why MEDIUM:** the single most-cited stranded item across the cluster (A_s is the framework's largest observational gap; the impulse-quench number must reach `canonical_constants.py` to be import-visible). Lower than CF-AS-1/2 because it is a pin, not a new physics gate — and it depends on WS-AS-1 settling whether it is a point or a band.

### CF-AS-4 — CF23 prose split (register reconciliation; Q3 capstone-hygiene). **EVOI: MEDIUM** (high register-leverage, low compute).
- **What:** split the atlas-08 CF23 bullet (and the falsifier-rigor Row 8 read) into **(a) FLOOR** (`A_s≥A_s^{BD}`, `S_IC≥1`): PERMANENT structural-position wall, now CONFIRMED from the modular-weight (inv-12 W1-2/W4-1) AND families-index (inv-12 W2-5) vantages in addition to the substrate-IC layer (S83); and **(b) MAGNITUDE + upper-edge FILTER**: SCHEME-DEPENDENT and OPEN — relocated to CF-AS-2 (greybody scan) and the H̃/occupation-input question (WS-AS-1). Per `capstone-hygiene-gate.md` Q3 this is a PROVEN/CONDITIONAL distinction-sharpening, designated register writer, reviewed patch NOT bulk append; the §7 falsifier-surface side is `mack-cosmic-bridge` sole-writer.
- **Inputs:** inv-12 §V + this rollup §3; atlas-08-open-questions.md CF23; falsifier-rigor Row 8; atlas-04 assumptions; the six route verdicts.
- **Gate:** capstone-hygiene Q3 status-change reconciliation; closure by artifact-existence (no numerical verdict). Substrate-first framing preserved (the arrow `D_K → {α_k,β_k} → n_k → S_IC → A_s` unchanged; the tag merely scopes confidence floor-permanent / magnitude-open).
- **Effort:** orchestrator-direct register edit at session-promotion; <1 agent-session.
- **Why MEDIUM:** corrects the register's standing OVER-STATEMENT ("3.02× permanent") that the convergence directly refutes — it institutionalizes the floor-vs-magnitude distinction so the OOM-incoherence cannot regenerate. The single highest-leverage register fix the cluster produces.

---

## 6. Cross-links to other convergences

- **CV-4 (SA ≠ free energy; B-13 a₄):** the deepest cross-link. A_s and the CC both test whether `Tr f(D²)` is the substrate's effective action — and the SAME functional-pluralism logic governs both. The A_s magnitude is SCHEME-DEPENDENT for the same reason the a₀/a₂ CC ratio is (inv-5 W1-5: the entropy functional sign-flips the ratio −0.499 vs +2.320; B-13 a₄-anomaly closed monotone, inv-5 W1-2). **Net:** what survives all functionals is structural (the A_s floor; the geometric-CC monotonicity wall); what depends on the functional is a d.o.f. (the A_s magnitude; the a₀/a₂ ratio — both **permanently SCHEME-DEPENDENT**). The inv-5 + inv-12 W4-2 "typed SA-repair candidate" (|D_BdG|-linear Krein-modular ground-state-energy, CF-2 in both digests — KEEP SEPARATE from the A_s CFs) is the constructive complement: CV-4 asks WHICH functional is the effective action, CV-1 shows the A_s magnitude's functional-dependence is the observable consequence of that question being open. Stage-2 should net CV-1 + CV-4 as **two faces of one lizzi-signature finding: the spectral functional is an unpinned physical d.o.f., and A_s + the CC are where it shows up observationally.**

- **CV-5 (GGE asset; B-1, B-5):** A_s IS a GGE-relic observable — the squeezing modulus `S_IC=1+2n_k` of the produced relic state. The CV-5 net (GGE is the richest under-exploited asset) and the CV-1 net share the SAME object: inv-10 W2-1 (TRANSIT-PS built) + inv-12 W3-1 (relic locked) + W3-2 (relic survives the ringing) are the GGE-relic spine, and the A_s floor is the relic's positivity wall. The GGE-bispectrum sub-bridge (inv-10 W2-3 τ_NL=1.527696; inv-13 INV13-W1-1 collider-null) is the SAME relic queried for non-Gaussianity — Stage-2 should net A_s (2-point amplitude) + f_NL/τ_NL (3-/4-point) as the relic's full power-spectrum signature, all under the `max_f_NL_FW=1.505` envelope.

- **CV-2 (M_KK keystone; B-2):** inv-3 W2-3 localizes the A_s residual weakness to the dimensionful prefactor (M_KK/M_Pl)²=9.3e-4 — i.e. **the A_s magnitude's dimensionful leg IS the M_KK keystone gap.** The CV-2 net (M_KK is the sole imported scale, rank-1 PROVEN §VII.BS, derivability relocated to the BCS dimensional-transmutation / gauge-a₄-loop gates) directly bounds the A_s magnitude: once M_KK is derived (CV-2's CF-INV6-W4-A gauge-loop or inv-11's BCS exp(−c/λ) route), the A_s dimensionful prefactor is fixed. Stage-2: the A_s magnitude is `(dimensionless functional-dependent factor) × (M_KK/M_Pl)²` — the first factor is CV-1's scheme-dependent leg, the second is CV-2's keystone. They are orthogonal and BOTH must land for a derived A_s.

- **Floquet/in-band resonance (inv-1 §3 #3):** inv-12 W3-2 (`fraction_resonance=0.0`, relic survives → A_s overproduction genuine) is load-bearing for the CV-1 net (Reading A of WS-AS-1) — BUT it **directly contradicts inv-10 W2-4** (`max Re μ_F=0.249>0`, LIVE DTC-candidate). That W2-4↔INV12-W3-2 Floquet contradiction is its OWN Q1 workshop (inv-10 WS-1, transit-dynamics ↔ quantum-acoustics ± berry method-cross-review) — NOT an A_s workshop, but the A_s net depends on its resolution: if inv-10 W2-4 wins (relic IS re-pumped), the "overproduction is genuine" leg of the A_s floor weakens. **Stage-2 MUST flag** that the A_s-floor "genuine overproduction" sub-claim is contingent on the Floquet adjudication landing on INV12-W3-2's side.

- **CV-3 (a(t)/effective-Friedmann):** weak link — inv-12 W3-3 (relic back-reaction q-direction PASS, dust trajectory lower-band; the relic-Friedmann q ≠ SCALE-FACTOR-54 Connes-proxy q structural theorem) touches both the A_s relic and the a(t) gap. The A_s relic's back-reaction is the bridge: a diluting positive-energy relic decelerates (CV-3 focusing) AND sources the curvature perturbation (CV-1 amplitude). Stage-2 net only if the a(t) rollup needs the relic energy density — otherwise orthogonal.

---

*End of A_s amplitude-normalization wall (CV-1) convergence rollup. Synthesizer: lizzi-spectral-functional-theorist (neutral Stage-2; netted inv-3/4/5/6/10/12 as equal peer digests). Register verified against knowledge MCP 2026-06-20: AMPLITUDE-NORM-66 FAIL 3.15 OOM; CF23 HARDENED-PERMANENT S83; `S_IC=1+2n_k≥1` PROVEN proven_1097; TRANSIT-PS-67 OPEN/CRITICAL; falsifier-rigor Row 8 SCHEME-DEPENDENT 5.078e-9 TD-canonical vs 2.099e-9 Planck. Investigation-track exploratory; results enter the permanent index only via session-{N} promotion of the §5 CFs.*
