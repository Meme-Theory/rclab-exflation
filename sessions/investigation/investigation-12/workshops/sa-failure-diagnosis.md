# INV12-W4-2 — The SA-Effective-Action Diagnosis (workshop)

**Gate**: INV12-W4-2 | **Type**: workshop (EXACTLY 2 agents, 2 rounds, sequential, shared doc) | **Closes**: artifact-existence (NO verdict line)
**Advocates**: `lizzi-spectral-functional-theorist` (WRONG FUNCTIONAL: modular/entropy S_modular) ↔ `van-den-dungen-bridge-theorist` (WRONG SIGNATURE: Krein/Lorentzian)
**Owner (neutral)**: gen-physicist (planner; NOT a participant)

## Adjudication question

The framework READS the spectral action Tr f(D_K²/Λ²) as the modulus effective action while it KNOWS this is unjustified (atlas-04 S3 ASSUMED; F.5: the SA PENALIZES BCS pairing, wrong sign, 93× — δ_S_BdG = +12.76 vs E_cond = −0.137). Two NCG vantages diagnose this differently:
- **lizzi (WRONG FUNCTIONAL)**: Tr f(D²) is a SPECTRAL MOMENT, not a Fock-space total energy. The fix is a substrate-SELECTED modular/entropy functional S_modular = Tr(D_K² ρ_ω) whose weight is the framework's faithful normal modular state ω (§VII.BZ) — no free f; by construction a total entropy/energy (Fock-correct), not a moment.
- **vdd (WRONG SIGNATURE)**: the Euclidean Tr f(D²), naively Wick-rotated, is NOT the Lorentzian effective action. The Krein-spectral-triple program (Paper 04/08) makes the Euclidean→Lorentzian step rigorous; the fix is the Krein reformulation.

Adjudicate (STRUCTURAL VERDICT + decisive forward gate):
(a) is the 93× BCS-wrong-sign penalty a FUNCTIONAL-FORM failure (lizzi: spectral moment ≠ Fock total energy; S_modular) or a SIGNATURE failure (vdd: Euclidean ≠ Lorentzian; Krein reformulation)?
(b) do the two diagnoses COMPOSE (a Krein modular weight — a Lorentzian-signature modular/entropy functional) or COMPETE (mutually exclusive)?
(c) does the Paper-10 bounded-perturbation theorem (W2-3: BdG dressing locally bounded, K-homology preserved EXACTLY, a_n shifts boundedly) CONSTRAIN which diagnosis can be right?
(d) what single forward compute decides it.

## Landed upstream evidence (read these — they have all landed)

- **INV12-W1-1** (modular extremization): **FAIL** — S_modular is monotone through τ_fold (extremum off-fold at τ=0.171); ω is bulk-faithful but does NOT extremize at the fold. So S_modular as-is is not a selector at τ_fold. (`investigation-12-w1-workingpaper.md §W1-1`)
- **INV12-W2-3** (Paper-10 BCS-dressing-invariance): **PASS** — BdG dressing is a Paper-10 locally-bounded perturbation ⇒ [D_K+V_BdG] = [D_K] in KK(A_K,ℂ) EXACT; topology is dressing-RIGID, a_n (Λ,G_N) is dressing-SOFT. S69 W5-G promoted to STRUCTURAL THEOREM. (`investigation-12-w2-workingpaper.md §W2-3`)
- **INV12-W2-4** (Krein-Lorentzian a₀): **PASS** — Krein-a₀ vs Euclidean-a₀ resolved. (`§W2-4`)
- **INV12-W2-2** (a_n pole ledger): **PASS** — all a_n live at divergent poles; analysis-side numbers are regulator-subtracted continuations. Topology(rigid)/geometry(fixed)/analysis(regulator-soft) split explicit. (`§W2-2`)
- Verdict lines: `computations/investigation-12/inv12_gate_verdicts.txt`
- Anchors: `sessions/framework/Atlas/atlas-04-assumptions.md` (S3 ASSUMED); `sessions/framework/Atlas/atlas-07-permanent-results.md` (BdG Anti-Trapping δ_S_BdG = +12.8, 93×)
- Survey positions: `sessions/investigation/investigation-1/lizzi-spectral-functional-theorist.md`, `.../van-den-dungen-bridge-theorist.md`

**DEDUP (do NOT merge)**: distinct from inv-5 W3-2 ("is Tr f(D²) the substrate's free energy?", connes↔landau). This gate asks WHICH replacement (modular vs Krein), a separate Q1 axis. Cross-cite at close.

---

## Round 1 — Each advocate states its diagnosis + the fix it implies

### R1 · lizzi-spectral-functional-theorist

**Diagnosis (A-L1 / B-L1): the 93× is a WRONG-FUNCTIONAL failure of CATEGORY, not of calibration.** `Tr f(D_K²/Λ²)` is a **single-particle spectral moment** — a one-body trace `M[g] = Σ_k m_k g(λ_k)` that applies a function eigenvalue-by-eigenvalue with NO Fock-space many-body correlation. The BCS condensation energy is a **two-body Fock quantity**: it lives on the off-diagonal pairing amplitude `⟨c_k c_{−k}⟩`, which is *structurally invisible* to any trace of `g(D²)`. atlas-04 S3 states this exactly — "SA is a spectral moment, not a total energy; the BCS condensation energy is a Fock-space quantity; these are categorically different functionals." The 93× is the numerical fingerprint of that category error, and it has the cleanest possible signature: a **sign reversal that the two functionals cannot even share a leading power of Δ over.**

**Substitution chain (Sage-exact; the directional claim "SA has the wrong sign for BCS"):** particle-hole-symmetric single mode, μ=0, normal energy ξ, BdG quasiparticle `E = √(ξ²+Δ²)`, gap Δ>0.
- **Def 1 — spectral-moment proxy**: `Tr(D_K²)` over the ±BdG eigenvalues `= 2E² = 2(ξ²+Δ²)`. This IS the form `f(D²)` reads (a function of |λ|², summed per mode).
- **Def 2 — Fock condensation energy** (mean-field, standard): `E_cond = −(E − ξ − Δ²/2E)`.
- **Substitute + read off the gap-opening shifts** (Sage QQ-exact, `b8…` not needed — symbolic):
  - moment shift `= Tr(D²)|_Δ − Tr(D²)|_0 = +2Δ²`;  `d/dΔ[2(ξ²+Δ²)] = +4Δ > 0` ⇒ **the spectral moment RISES when the gap opens**.
  - `E_cond` Taylor at Δ→0 `= −⅛ Δ⁴/ξ³ + O(Δ⁶)`; leading coefficient **negative** ⇒ **the Fock energy LOWERS**.
- **Canonical form**: `sign(d Tr(D²)/dΔ²) = +1` while `sign(E_cond) = −1`, and the moment is `O(+Δ²)` whereas the Fock energy is `O(−Δ⁴)` — **opposite sign AND different leading order in Δ**.
- **Conclusion**: reading `Tr f(D²)` as the modulus action makes pairing *penalized* (the gap pushes |λ_k| up off the Fermi surface, raising the moment) where the true ground state is *stabilized* (the gap lowers `E_cond`). This reproduces the registered F.5 numbers — `δ_S_BdG = +12.76` (atlas-07 W8, "spectral action PENALIZES pairing structurally") against `E_cond = −0.137` (ED-CORRECTED-35, `−0.1151`). The sign flip is structural and exact, NOT a regulator/normalization artifact: it survives every cutoff `f`, every Λ, because `Σ_k g(λ_k)` has no channel that can encode the Cooper correlation.

**The fix the diagnosis implies (B-L1): replace the moment with a substrate-SELECTED modular/entropy functional that is a total energy BY CONSTRUCTION** — `S_modular = Tr(D_K² ρ_ω)`, weighted by the framework's **already-constructed** faithful normal modular state ω (§VII.BZ / K12, STAGE-3-PERMANENT). Two properties make this the *right object*: (i) **no free f** — ω is read OFF the substrate's own minigap (BDI/N₃=0), not chosen, so it adds zero degrees of freedom, in contrast to the cutoff family where `t* = 0.08832` is a measured O(1) datum; (ii) ρ_ω is a **density matrix on the (crossed-product) algebra**, so `Tr(D² ρ_ω)` is a *state expectation* (an entropy-weighted total energy / relative-entropy functional, Jacobson-type), categorically a Fock-correct quantity rather than a one-body trace. That is precisely the type that *can* carry pairing correlation, where the bare moment cannot.

**Honest engagement with my own FAILED gate (INV12-W1-1, the requirement).** My gate LANDED **FAIL**: `S_modular(τ)` is **MONOTONE INCREASING** through τ_fold (`dS/dτ|_fold = +0.7821`, sign `+1` on both sides; interior extremum off-fold at τ=0.171, a window-edge artifact). The ω-weighted velocity sum `Σ_k |λ_k| |λ_k|′ w_k = +0.391 > 0` does not change sign across the fold. **What W1-1's FAIL does and does NOT mean** — I claim it is a SCOPE result, not a refutation of the diagnosis:
- **It refutes**: "S_modular is a τ-SELECTOR" — that ω extremizes a spectral entropy functional *at τ_fold*. The modular weight joins the four S76 selection principles that failed (G-L1 selection corridor for the modular weight is **closed**). I do not soften this; the substitution chain's [SIGN] prediction was falsified at machine scale.
- **It does NOT refute**: "S_modular is the right OBJECT for the modulus effective ENERGY." W1-1 tested **selection** (is `dS/dτ = 0` at the fold?), a *different question* from **sign-correctness** (does the functional rank the BCS-paired state below the unpaired one?). These are orthogonal: a functional can be the correct effective energy and still be monotone in τ over a given window — monotonicity in the deformation parameter τ is not the same as wrong-sign in the pairing field Δ. W1-1 says ω is **bulk-faithful but not a fold-clock** (consistent with the S106 GEM "no area-clock" 2b INFO — same fact, two views). It is silent on whether `Tr(D² ρ_ω)` repairs the Δ-sign that `Tr f(D²)` gets wrong. **The decisive forward test (R2) is exactly that silence: compute `δ_S_modular` for the BdG-paired vs unpaired state and check its SIGN against `E_cond < 0` — the one number W1-1 never produced.** My honest position: the modular *functional* is the right category (a state-weighted total energy); the modular *weight as a τ-extremizer* is excluded; whether the right-category functional also gets the BCS sign right is **open and is the gate that decides this workshop.**

**Why this is a FUNCTIONAL diagnosis and not a SIGNATURE one (pre-positioning vs vdd, full case in R2).** INV12-W2-3 PASSED: the BdG dressing `D_K → D_K + V_BdG` is a Mesland–van den Dungen *locally-bounded* perturbation (`‖V_BdG‖ = |Δ_BCS| = 0.4642547 < ∞`), so `[D_K + V_BdG] = [D_K]` in `KK(A_K,ℂ)` **EXACTLY**, at every L_max — the **topology is provably dressing-RIGID**, the analysis side (`a_n → Λ, G_N`) is **dressing-SOFT**. W2-3's own constraint-map consequence is explicit: "any SA-failure diagnosis must locate the failure on the **analysis** side (the a_n / functional choice), not the topological side." The Euclidean→Lorentzian signature question (vdd) is a property of the **a₀/a₂ analysis leg**, and INV12-W2-4 already PASSED `a₀^Krein = a₀^Euclidean` (the Wick rotation is *validated* for the leg vdd would re-found). So the signature step is not where the 93× lives: a₀ is signature-robust, and the BCS sign error is a **Fock-vs-moment** mismatch on the spectrum itself — present in the Euclidean functional *before* any rotation, and unchanged by a faithful one. The wrong-sign is in WHICH functional reads the spectrum, not in WHICH signature it is read in.

**Numeric case, summarized**: `δ_S_BdG = +12.76` (SA, wrong sign) vs `E_cond = −0.137` (Fock, correct sign) ⇒ ratio 93×; Sage-exact toy gives `moment ∝ +2Δ²` vs `E_cond ∝ −⅛Δ⁴/ξ³` (opposite sign, different order). The repair candidate `Tr(D² ρ_ω)` has **zero new free parameters** (ω fixed by §VII.BZ) and is a state-expectation (Fock-type). My own W1-1 excludes it as a *fold-selector* (FAIL, monotone) but leaves its *BCS-sign correctness* untested — that single sign check is the forward gate I will name in R2.

### R1 · van-den-dungen-bridge-theorist

**Diagnosis (A-V1 / B-V1): the 93× is a WRONG-SIGNATURE failure of CATEGORY — the object the framework calls "the modulus effective action" is the naive Wick rotation of a Euclidean trace, and that is provably NOT the Lorentzian effective action.** The framework reads `S = Tr f(D_K²/Λ²)` (a Euclidean, positive-definite, elliptic-`D²` heat-trace) and identifies it term-by-term with the *Lorentzian* effective action's a₀→Λ, a₂→Einstein-Hilbert, a₄→YM+Higgs. That identification is exactly the step my corpus was built to make rigorous and exactly the step the framework skips. On physical (indefinite) spacetime, `D` is **not self-adjoint** and `D²` is **not elliptic** — it is hyperbolic, wave-equation-like (Paper 04 §43–55; Paper 03 §44–50). The naive object `Tr e^{−tD²}` is not even trace-class without a Wick rotation or a Krein reformulation (Paper 03 §47: "resolvent `(D−λ)⁻¹` does not decay uniformly … classical elliptic regularity fails"). The rigorous replacement is the **Krein spectral triple** `(A, K, D, J)` with a *fundamental symmetry* `J` — **linear, self-adjoint, J²=+1** (Paper 04 §59–60; Paper 08 §18) — on which `D` is self-adjoint w.r.t. the indefinite form `⟨ψ,φ⟩_J = ⟨Jψ,φ⟩`, and the fermionic/effective action is the **J-weighted** functional, not the bare Hilbert trace (Paper 08, the entire point of the paper: "this Krein-space approach successfully recovers correct Lagrangians … the formulation does not require a real structure"). My survey position U-2/B-1 names this as the framework's single most load-bearing un-justified inheritance.

**Honest engagement with my own LANDED gate (INV12-W2-4, the requirement) — and where it CUTS AGAINST a naive reading of my diagnosis.** My gate LANDED **PASS** with `rel_diff = |a₀^{Krein} − a₀^{Euclidean}|/|a₀^{Euclidean}| = 0.0` EXACT (≤10⁻³; `audit 677f4185…2838`; a₀^{Krein}=a₀^{Eucl}=6440.000000). I will not overstate it: **W2-4 shows the naive Wick rotation is VALIDATED for the a₀ leg.** The substitution chain there is structural — a₀ is the leading `t^{−d/2}` heat coefficient = the spinor-module **dimension count** × volume; the timelike sign-flip multiplies `det g` by `(−1)¹` but `√|det g|` removes the sign (volume factor = 1.000000), and the Krein decomposition `K = H₊⊕H₋` gives dim-count `dim H₊ + dim H₋ = 8+8 = 16 = dim H`, a signature **invariant** (trace factor = 16/16 = 1). The Krein-linear J²=+1 was verified to machine zero, explicitly guarded against Connes' antilinear real-structure J (the canonical convention failure, my R-4). **What this implies for the signature diagnosis, stated plainly: the DILUTION-CC cosmological-constant number is signature-robust, and the signature step is NOT where the 93× lives in the a₀ channel.** That is a genuine constraint on my own thesis, and I register it: the wrong-signature failure is **not** a uniform "rotate everything and the numbers shift" claim — at the leading Weyl dim-count (a₀), Euclidean = Krein exactly. My diagnosis therefore CANNOT be "Krein reformulation moves a₀ and fixes Λ." It does not.

**Where the signature diagnosis DOES bite: the SIGN-SENSITIVE functional, not the dim-count.** The 93× is a *sign* pathology (`δ_S_BdG = +12.76` where `E_cond = −0.137`; atlas-07 W8 "spectral action PENALIZES pairing structurally"; atlas-04 S3 ASSUMED). a₀ is sign-INSENSITIVE by construction (a dimension count is a non-negative integer × volume — it cannot carry a sign reversal). The object that carries the BCS sign is a functional of the **graded/indefinite spectral data**, and that object is precisely the one the signature axis distinguishes. My Sage substitution chain (QQ-exact, `sage_eval`, particle-hole-symmetric mode, μ=0, ξ>0, Δ>0, BdG `E=√(ξ²+Δ²)`):

```
Claim under test: "Krein/signature reformulation mechanically flips the +2Δ² penalty to the −Δ⁴ Fock sign."

Step 1 (Euclidean moment, lizzi Def 1): Tr(D²)|_{±E} = 2E² = 2(ξ²+Δ²).
Step 2:  d Tr(D²)/d(Δ²) = +2          ⇒ SIGN = +1 (moment RISES with gap). [matches lizzi]
Step 3 (Fock):  E_cond = −(E − ξ − Δ²/2E);  taylor(Δ→0) = −⅛ Δ⁴/ξ³  ⇒ SIGN = −1. [matches lizzi]
Step 4 (Krein super-trace, the sign-sensitive object): Str_J(D²) = Tr_{H₊}(D²) − Tr_{H₋}(D²)
        = E² − E²  = 0   on the symmetric doublet.
Conclusion: the bare Krein super-trace does NOT reach −Δ⁴; it gives 0, not a sign flip. ⇒ a SIGNATURE
            reformulation by itself does NOT mechanically repair the +12.76 sign.
```

I state this against my own advocacy because honest-advocacy requires it: **the simplest version of "wrong signature ⇒ Krein fixes the sign" is FALSE, and my Sage check falsifies it.** A bare J-graded super-trace of `D²` on the particle-hole-symmetric BdG doublet is 0, not `−Δ⁴`. So my diagnosis is NOT "swap the Hilbert trace for the Krein super-trace and the 93× inverts." That refutes the *naive* signature fix exactly as W1-1 refuted the naive modular-selector.

**The signature diagnosis, correctly stated (B-V1): the failure is that `Tr f(D²)` is the WRONG OBJECT-TYPE for a Lorentzian effective action — it is a Euclidean elliptic trace masquerading as a hyperbolic one — and the *correct* Lorentzian object is built on the indefinite triple where the dynamical (pairing/condensation) content enters through the Krein structure, NOT through a sign-graded `D²`.** Paper 03's pairing theorem is the load-bearing fact: an indefinite Kasparov module `(E,φ,F,J)` pairs as `⟨(E,φ,F,J),·⟩ = ⟨(E₊,φ,F₊),·⟩ − ⟨(E₋,φ,F₋),·⟩` (Paper 03 §78) — a genuine *difference* of two elliptic pieces, NOT the sum that the Euclidean `Tr f(D²)` computes. The Euclidean spectral action sums `Σ_k g(λ_k)` over **all** modes with one sign; the Lorentzian (Krein-paired) effective action takes a **signed difference** across the `J`-decomposition. The BCS condensation energy is a difference of ground-state energies (paired vs unpaired) — structurally a `(+)−(−)`-type object, which is what the indefinite pairing delivers and what the all-positive Euclidean trace **cannot**. On this reading the 93× sign error is the fingerprint of having taken a *sum* (Euclidean elliptic `Tr f(D²)`) where the physics demands a *signed difference* (Lorentzian Krein pairing). The fix is not "regrade `D²`"; it is "the effective action is the Krein-paired functional on the indefinite triple, whose value is `S₊ − S₋`, and the condensation channel lives in the relative sign that the Euclidean object discards."

**The fix the diagnosis implies (B-V1): construct the pseudo-Riemannian submersion spectral triple (timelike M⁴, Riemannian SU(3) fiber — Paper 04's exact setting, and a genuine submersion in my factorization sense) with the Krein fundamental symmetry J, and read the effective action as the J-paired functional `S₊ − S₋` rather than the naive Euclidean `Tr f(D²)`.** This is my survey's highest-leverage untraveled bridge (B-1) and Step 4 of my highest-leverage-next-steps. Two structural properties make it the right object: (i) it adds **no free parameter** — `J` is fixed by the signature `(−,+,+,+,+,…)` of the physical submersion, exactly as the KO-dim=6 / SU(2,1) extension already showed `J²=+1` survives (W2-4 Krein split `(16,8,8)` matches `s46_pseudo_riemannian` `VALID (8,8)`); (ii) the pairing-difference structure is precisely the channel a Fock condensation energy needs, where the Euclidean sum has none — which is the SAME structural diagnosis lizzi reaches from the functional side (her "the one-body trace has no channel that can encode the Cooper correlation"). I read her "no channel" and my "wrong object-type: sum where a signed difference is required" as **two coordinates on the same defect**, which is why R2's compose-vs-compete question is live, not rhetorical.

**Where I agree / disagree with lizzi's R1 (full adjudication deferred to R2).**

- **Agree (the sign arithmetic):** her Sage chain `+2Δ²` vs `−⅛Δ⁴/ξ³` is exactly right; I reproduced it independently above (Steps 2–3, machine-exact). The sign reversal is structural, survives every cutoff `f` and every Λ, and is present in the Euclidean functional. We do not contest a single number.
- **Agree (the failure is NOT topological):** W2-3 PASSED — the BdG dressing `D_K→D_K+V_BdG` is a Mesland–van den Dungen *locally-bounded* perturbation (`‖V_BdG‖=|Δ_BCS|=0.4642547<∞`), so `[D_K+V_BdG]=[D_K]` in `KK(A_K,ℂ)` EXACTLY, at every L_max — topology is dressing-RIGID, analysis is dressing-SOFT. This is MY gate, and its own constraint-map line is explicit: "any SA-failure diagnosis must locate the failure on the **analysis** side, not the topological side." I endorse that fully. The 93× is an analysis-side defect. Both our diagnoses live on the analysis side; neither is a topology claim.
- **The real fork (where I disagree with how lizzi frames the exclusivity):** lizzi pre-positions the signature step as *already settled by W2-4* — "a₀ is signature-robust, so the signature step is not where the 93× lives; the wrong-sign is in WHICH functional reads the spectrum, not WHICH signature." I concede the a₀ premise (it is my own gate) but contest the inference. **a₀ is signature-robust because it is a dimension count; that tells us NOTHING about the sign-sensitive a₂/a₄/condensation channel.** W2-4 is a single-leg result on the one moment (the Weyl dim-count) that is *guaranteed* signature-blind. It is silent on whether the effective action's *sign-bearing* content (the `S₊−S₋` pairing channel) is correctly captured by the Euclidean sum. Lizzi reads "WHICH functional" and I read "WHICH signature," but I claim these are **not orthogonal**: the question "is the right functional the Euclidean sum `Tr f(D²)` or a state-weighted total energy?" and the question "is the right object the Euclidean elliptic trace or the Lorentzian Krein-paired `S₊−S₋`?" may have the **same answer** — a functional that is simultaneously (a) a state-expectation/total-energy (lizzi's `Tr(D²ρ_ω)`) and (b) defined on the indefinite Krein triple. A **Krein modular weight** — a Lorentzian-signature relative-entropy/total-energy functional — is exactly that object, and it is neither purely "lizzi's" nor purely "mine."

**Honest engagement with INV12-W2-2 (the pole ledger, my gate, PASS).** W2-2 LANDED PASS: all 5 CLASS-I ζ-residues (a₀,a₂,a₄,a₆,a₈) live at **divergent** poles `s≤d/2=4`; ZERO are convergent partial sums; a₂ (s=3 double-power) is `DIVERGENT` reproducing the §VII.CB S109 wall bit-for-bit. This SHARPENS my signature diagnosis: the analysis-side moments are **already** residue-subtracted analytic continuations — they are regulator-laden objects, not naive sums. The signature question compounds with this: the framework takes a *Euclidean* residue-subtracted continuation and Wick-rotates it by hand to a *Lorentzian* effective-action term. Both operations (the ζ-continuation AND the Wick rotation) are on the soft analysis side; W2-2 pins the first, my diagnosis targets the second. The a₀ marginal-pole subtlety (W2-2: a₀ is `DIVERGENT-MARGINAL`, log-divergent at `s=d/2`) is consistent with W2-4's signature-robustness — the *value* a₀ is signature-blind (dim count) even though it is regulator-laden as a pole residue. These are independent axes (regulator-class vs signature), and both are analysis-side, exactly per my topology/analysis boundary.

**Does W2-3's K-homology rigidity mean SIGNATURE and FUNCTIONAL fixes are mutually exclusive? NO — and W2-3 is the structural reason they COMPOSE (R2 thesis, pre-stated).** This is the question the prompt asks me to confront, and my answer is the crux of R2. W2-3 proves the BdG dressing preserves `[D_K]` EXACTLY: topology is rigid, analysis is soft. A **functional fix** (lizzi: re-weight the *same* Euclidean spectral data with a modular state ρ_ω — `Tr(D²ρ_ω)`) operates on the analysis side. A **signature fix** (vdd: reconstruct the effective action as the Krein-paired `S₊−S₋` on the indefinite triple) also operates on the analysis side — and crucially, W2-3 says it CANNOT touch the topology either (a bounded perturbation, and a signature reformulation that preserves the bounded `V_BdG` cannot flip the K-homology class). So neither fix is topological, and **they act on the same soft analysis layer via different but composable handles**: lizzi re-weights *which state* reads the spectrum (`ρ_ω`); I change *which signature/inner-product* the spectrum is read in (`J`, indefinite form). A density matrix `ρ_ω` on the crossed-product algebra and a Krein fundamental symmetry `J` on the indefinite triple are **not competing objects** — `ρ_ω` can be defined on the *Krein* triple, giving a **Krein modular weight**: a Lorentzian-signature, state-weighted, total-energy functional. That single object is (lizzi's category: total energy / Fock-correct) ∧ (my category: Lorentzian Krein-paired). My Sage Step 4 is what forces this synthesis: since the *bare* super-trace gives 0 (not the sign flip), the signature axis alone is insufficient — it needs a *state weight* to carry the condensation sign, which is precisely lizzi's `ρ_ω`. Symmetrically, lizzi's `ρ_ω` re-weighting on a *Euclidean* triple still sums all-positive modes — it needs the *indefinite pairing* to deliver the `S₊−S₋` difference. **Each diagnosis supplies exactly what the other lacks.** My pre-stated R2 position is therefore COMPOSE → **Krein modular weight**, not COMPETE — and W2-3 is the theorem that makes the composition coherent (both live on the dressing-soft analysis side, neither disturbs the dressing-rigid topology).

**Numeric case, summarized.** The defect is `δ_S_BdG = +12.76` (Euclidean SA, wrong sign) vs `E_cond = −0.137` (Fock / ED-CORRECTED-35 `−0.1151`, correct sign) ⇒ 93×. My Sage check: Euclidean moment `+2Δ²` (sign +1) confirmed; bare Krein super-trace `Str_J(D²)=0` (the naive signature fix does NOT reach `−Δ⁴` — I falsify my own simplest claim). My LANDED W2-4: a₀^{Krein}=a₀^{Euclidean}=6440.000000 EXACT (signature step validated for the sign-INSENSITIVE dim-count leg — Λ is signature-robust). My LANDED W2-2: all 5 a_n are divergent-pole residue continuations (the analysis side is doubly soft: regulator-laden AND signature-rotated-by-hand). My LANDED W2-3: `[D_K+V_BdG]=[D_K]` EXACT (the failure is analysis-side, not topological — both diagnoses confined here, hence composable). The signature diagnosis is NOT "Krein moves a₀"; it is "the Euclidean elliptic `Tr f(D²)` is the wrong object-TYPE for a Lorentzian effective action — it computes an all-positive *sum* where the physics demands the Krein-paired *signed difference* `S₊−S₋`, and the condensation channel lives in the relative sign the sum discards." The forward gate I name in R2 is the one number neither W2-4 (dim-count only) nor W1-1 (selection only) produced: compute `δ_S` for the BdG-paired vs unpaired state under the **Krein-paired modular functional** `Str_J(D²ρ_ω)` on the indefinite triple, and check its SIGN against `E_cond < 0` — the gate that simultaneously tests lizzi's `ρ_ω` and my `J`, and so decides compose-vs-compete by construction.

---

## Round 2 — Respond to opponent + adjudicate compose-vs-compete + name the forward gate

### R2 · lizzi-spectral-functional-theorist

**Where I now stand: COMPOSE at the level of defect-identification, but the naive composite OBJECT does NOT inherit sign-correctness — and a Sage check I ran this round forces that refinement on both of us.** vdd's R1 is the strongest possible version of the signature case precisely *because* it falsifies its own naive form (Step 4: bare `Str_J(D²)=0`, not `−Δ⁴`). I will not let the symmetry of our two honest self-falsifications lull the workshop into a tidy "each supplies what the other lacks ⇒ the product works" synthesis. I tested that product directly. It does not work at leading order. The truth is sharper and more useful than compose-or-compete.

**Answer to (b) — COMPOSE as DEFECTS, but the composite Krein-modular-weight object is itself sign-WRONG at leading order (Sage-exact, this round).** vdd's R1 thesis is: his `J` supplies the signed difference `S₊−S₋` that my `ρ_ω` lacks, and my `ρ_ω` supplies the state-weight that his bare super-trace lacks, so the synthesis `Str_J(D²ρ_ω)` is the object that is *both* Fock-correct *and* Lorentzian. I built exactly that object and Taylor-expanded its gap-opening shift. Substitution chain (Sage QQ-exact, `sage_eval`; particle-hole-symmetric mode, μ=0, ξ>0, Δ>0, BdG `E=√(ξ²+Δ²)`; ρ_ω diagonal = BCS ground-state occupation `u²=½(1+ξ/E)` empty / `v²=½(1−ξ/E)` filled — the *actual* diagonal of the §VII.BZ state on the paired branch):

```
Claim under test (vdd's R1 synthesis): "Str_J(D² ρ_ω) is the composite object that carries the −Δ⁴ Fock sign."

Step 1 (bare super-trace, vdd Step 4):     Str_J(D²)        = Tr_{H₊}(D²) − Tr_{H₋}(D²) = E² − E² = 0.          [confirmed]
Step 2 (state weight = §VII.BZ occupation): diag ρ_ω = (u² on +E branch, v² on −E branch),  u²+v² = 1.
Step 3 (composite, J-graded AND ρ_ω-weighted):
        Str_J(D² ρ_ω) = (+1)·u²·E² + (−1)·v²·E² = (u²−v²)·E² = (ξ/E)·E² = ξ·E = ξ·√(ξ²+Δ²).
Step 4 (gap-opening shift vs normal state, Δ→0 ⇒ E=ξ, u²=1, v²=0 ⇒ Str_J|₀ = ξ²):
        δ Str_J(D² ρ_ω) = ξ√(ξ²+Δ²) − ξ²  =  +½ Δ² − ⅛ Δ⁴/ξ² + O(Δ⁶).
Step 5 (read off the sign): LEADING coefficient = +½  ⇒  SIGN = +1  (the composite STILL RISES with the gap).
Conclusion: Str_J(D² ρ_ω) does NOT reach −⅛Δ⁴/ξ³. Its leading term is +½Δ² (same SIGN as my bare moment +2Δ²,
            magnitude reduced 4×). The naive composite is SIGN-WRONG, exactly as vdd's bare super-trace and my
            naive selector were each falsified. Three honest self-falsifications, not two.
```

So I directly refute the most attractive reading of vdd's own R1 closing move. **Adding the `J`-grading AND the modular state weight to `Tr(D²)` reduces the wrong-sign penalty (coefficient `+2 → +½`) but does not flip it.** The residual `+½Δ²` is the surviving fingerprint of the defect that *neither* of our handles removes: the `D²` *power* (the spectral-MOMENT structure) acts on `|λ|²` data, and both the J-grading and the diagonal ρ_ω-weighting are *also* functions of `|λ|²` on that same diagonal data. The Cooper correlation `⟨c_k c_{−k}⟩` is *off-diagonal* in the BdG basis; it is invisible to any diagonal weight (mine) and to any sign-grading of the diagonal (vdd's). This is my R1 "no channel" claim, now proven to survive vdd's signature fix, not just the bare moment.

**This is why "compose vs compete" is the wrong binary, and the correct verdict is a three-layer one.** Our two diagnoses are NOT mutually exclusive — vdd is right that they live on the same dressing-soft analysis layer (see (c) below) and can be carried by a single object. But "non-exclusive, single-object-able" is a statement about the *defects*, not about *sign-correctness*. The composite object exists; it is just still wrong by the Sage chain above. The decomposition that actually holds:

- **Layer 1 — defect identification: COMPOSE (both correct, orthogonal coordinates on one analysis-side failure).** vdd's "Euclidean elliptic sum where a Lorentzian signed difference is required" and my "one-body moment where a Fock total energy is required" are two *true* readings of the same defect. I accept his framing fully: a₀ being signature-robust (his W2-4) does NOT settle the sign-bearing channel, and I over-reached in R1 by implying it nearly did. He is right that "which functional" and "which signature" can have a *common* answer-object. Conceded.

- **Layer 2 — naive composite object `Str_J(D²ρ_ω)`: SIGN-FAIL (my Sage chain this round).** The single object both R1s converged toward is itself wrong-sign at leading order. Neither handle, nor their product, supplies the off-diagonal pairing channel. This is the *new* result of R2 and it cuts against both of us symmetrically.

- **Layer 3 — what the correct object additionally requires: the pairing channel / the `|D|`-linear power.** `E_cond` is a difference of *ground-state energies*, linear in the BdG energies (`∝ Σ|λ|`, a `Tr|D|`-type object on the off-diagonal-dressed operator), NOT a `Tr(D²)` moment. The repair is neither "re-weight with ρ_ω" (me) nor "re-sign with J" (vdd) nor their product — it is "evaluate the *correct power on the pairing-dressed operator*," i.e. a functional of `D_BdG = D_K + V_BdG` (the dressed operator W2-3 certifies is K-homology-identical) that is linear-in-`|λ|` and reads the off-diagonal block. The `ρ_ω` and `J` are then *corrections* to that object, not its substance.

**Answer to (c) — W2-3 CONSTRAINS the answer decisively, and in vdd's favor on the structural point, in mine on the operative one.** W2-3 (my reading and vdd's are identical here) proves `[D_K+V_BdG]=[D_K]` EXACT in `KK(A_K,ℂ)`: `‖V_BdG‖=|Δ_BCS|=0.4642547<∞`, locally bounded, topology dressing-RIGID, analysis dressing-SOFT. Three consequences, in order of force:

1. **Both fixes are analysis-side; neither is topological** (vdd's R1, endorsed). A signature reformulation preserving the bounded `V_BdG` cannot flip the K-homology class; a modular re-weighting on the (crossed-product) algebra cannot either. So the two fixes are *a priori* non-exclusive — they cannot collide on the topological invariant because *neither touches it*. This is the structural sense in which they COMPOSE, and it is vdd's point. I grant it without reservation.

2. **But W2-3 also tells us WHERE the sign must come from, and it is exactly the channel my Sage chain isolates.** Dressing-SOFT means the `a_n`/functional content *does* move under `V_BdG` — and `V_BdG` is precisely the OFF-DIAGONAL pairing block (`Δ` in the BdG matrix `[[D_K, Δ],[Δ†, −D_K]]`-type structure, per the registered `D_BdG γ̃` form). The sign-bearing physics lives in how the functional reads `V_BdG`'s off-diagonal entries. A `Tr(D²ρ_ω)` evaluated on `D_K` alone (no `V_BdG`) or on the *diagonal* of `D_BdG` cannot see it — which is why both our naive objects fail. W2-3's "analysis is soft, in the `V_BdG` direction" is the theorem that *names the missing channel*.

3. **Therefore neither diagnosis can be "right" alone in the sense of fixing the number, but the COMPOSITION can be made right by adding the pairing power — and W2-3 guarantees doing so does not disturb the topology.** This is the constraint's deepest content: it licenses a three-ingredient repair (Krein `J` + modular `ρ_ω` + pairing-dressed `|D_BdG|`) as topology-safe, while forbidding any claim that one or two ingredients suffice. My Sage chain is the falsification of the two-ingredient claim; W2-3 is the license for the three-ingredient one.

**FUNCTIONAL-INDEPENDENT vs SCHEME-DEPENDENT, my standing classification of this workshop's content:**
- *FUNCTIONAL-INDEPENDENT (structural, survives every cutoff f and every Λ):* the `+Δ²` sign of the spectral-moment penalty (my R1, vdd's Steps 2–3); the `Str_J(D²)=0` bare-super-trace identity (vdd Step 4); the `Str_J(D²ρ_ω) → +½Δ²` leading-sign of the naive composite (my R2 Sage chain); the off-diagonal invisibility of the Cooper channel to any diagonal functional. These are category facts, not regulator artifacts.
- *SCHEME-DEPENDENT (a physical d.o.f. set by the functional choice):* whether the *magnitude* `δ_S_BdG = +12.76` or `+12.76/4` etc. — the coefficient moves under `J`/`ρ_ω` (my chain shows `+2 → +½`), but the *sign* does not until the pairing power is added. The 93× ratio is a scheme-dependent magnitude on a sign that is structurally locked until Layer 3 is supplied.

**Answer to (d) — the single forward gate, sharpened by R2.** vdd's R1 named "compute `Str_J(D²ρ_ω)` and check its sign." My Sage chain has now *predicted that gate's outcome analytically*: at leading order it is SIGN-FAIL (`+½Δ²`). So that gate, as vdd framed it, would merely *confirm* the two-ingredient object is insufficient — useful, but it pre-resolves to FAIL and does not test the live hypothesis. The gate that actually decides the workshop is the **three-ingredient** one: compute `δS` for the BdG-paired vs unpaired state under the functional `S = Tr_J(|D_BdG| ρ_ω)` — the `|D|`-LINEAR (ground-state-energy, not moment), J-graded, modular-weighted functional on the *pairing-dressed* operator `D_BdG = D_K + V_BdG` (W2-3-certified K-homology-identical) — and check `sign(δS)` against `E_cond < 0`. Three nested predictions, each a clean PASS/FAIL: (i) the `|D|`-linear power on `D_K`-diagonal-only is still SIGN-FAIL (isolates that the power alone is insufficient); (ii) `Tr(D²ρ_ω)` on the dressed `D_BdG` (off-diagonal seen, wrong power) — my chain predicts the off-diagonal block contributes but the `D²` power keeps a residual `+Δ²` (test whether off-diagonal alone flips it); (iii) the full `Tr_J(|D_BdG|ρ_ω)` — the candidate I now predict is the FIRST object to return SIGN-PASS (`δS < 0`, matching `E_cond`). The gate's verdict = the *smallest* ingredient-set that returns SIGN-PASS, which IS the structural answer to (a)/(b): if (iii) is the first PASS, the answer is "neither functional nor signature alone — the composite-PLUS-pairing-power," and compose-vs-compete resolves to *compose-but-only-with-the-third-ingredient*. I name this gate `INV13-W?-KREIN-MODULAR-PAIRING-SIGN` and hand vdd the floor to weigh it and write the verdict.

### R2 · van-den-dungen-bridge-theorist

**Where I stand after lizzi's R2: I accept her three-layer decomposition in full, and I claim it is not a refutation of either of us but the FIRST correct statement of the joint object — and from the Krein/Paper-03 side I can now name WHY the third ingredient is forced, not bolted on.** lizzi's Sage chain this round (her Step 3–5: `Str_J(D²ρ_ω) → +½Δ²`, sign +1) is correct; I re-ran it independently (Sage QQ-exact, `xi,Delta` positive) and reproduce `(u²−v²)E² = ξE`, gap-shift leading `+½Δ²` — her third self-falsification stands. So the naive composite I gestured toward in my R1 closing move IS sign-wrong, exactly as she shows. I will not defend the two-ingredient object. But the reason it fails, read from my corpus, is precise and it CONVERTS her Layer-3 "add the pairing power" from a phenomenological patch into a structural necessity: **a Lorentzian (Krein-paired) effective action is `S₊ − S₋` (Paper 03 §78), a signed DIFFERENCE of two pieces; the condensation channel is the relative-sign cancellation between the two legs; and that cancellation only happens at the `|D|`-LINEAR (ground-state-energy) power, never at the `D²` (moment) power.** Her "off-diagonal pairing channel" and my "signed difference `S₊−S₋`" are the same object seen from the state side and the signature side — and the `|D|`-linear power is where they coincide.

**The ingredient ladder, Sage-exact (this round; the gate lizzi named, computed analytically end-to-end).** Particle-hole-symmetric mode, μ=0, ξ>0, Δ>0, BdG `E=√(ξ²+Δ²)`; dressed operator `D_BdG = D_K + V_BdG` with `‖V_BdG‖=|Δ_BCS|=0.4642547` (S70, R-protected) and `D_BdG² = (D_K²+|Δ|²)⊗1₂` (S82 / W2-3 input pin — the dressed square is the bare square plus a SCALAR `|Δ|²`, so `D²`-power objects can only ever shift by a constant `+Δ²`, never reach the off-diagonal). Each rung is the gap-opening shift vs the Δ=0 normal state:

```
Claim under test (lizzi's (d), my framing): "Only the |D|-LINEAR power on the pairing-DRESSED
operator D_BdG returns the E_cond sign; D² and diagonal-only objects cannot."

(i)  -Tr|D_K|         (|D|-linear, DIAGONAL-only, no V_BdG):  = -2ξ,  Δ-INDEPENDENT
     d/dΔ²[-2ξ] = 0                                          ⇒ SIGN-FAIL (power alone blind to Δ)
(ii) Tr(D_BdG² ρ_ω)   (DRESSED, but D² power):  = (u²+v²)E² = E²   [D² is sign-blind on the doublet]
     δ = E² - ξ² ,  taylor(Δ→0) = +Δ² + O(Δ⁴)                ⇒ SIGN = +1  (FAIL: residual +Δ²)
(iii) -E              (|D_BdG|-LINEAR, the filled Bogoliubov branch = BCS ground-state energy):
     δ = -E - (-ξ) = -√(ξ²+Δ²)+ξ ,  taylor = -½Δ²/ξ + O(Δ⁴)  ⇒ SIGN = -1  (MATCHES E_cond < 0)
Conclusion: the SIGN flips at, and ONLY at, rung (iii) — |D|-LINEAR ∧ pairing-DRESSED. lizzi's
            three-ingredient ordering is structurally correct on the sign axis.
```

**The refinement I owe the workshop (the one sign-vs-order subtlety, stated against my own framing too).** Rung (iii)'s `−E` gives the right SIGN but the WRONG ORDER: leading `−½Δ²/ξ`, where the true `E_cond` is `O(−Δ⁴)`. This is not a flaw in lizzi's gate — it is the second half of the Paper 03 §78 story, and I verified the cancellation Sage-exact. `E_cond = −E + ξ + Δ²/(2E)` is itself a *signed difference of three `|D|`-linear ground-state pieces*: the paired GS `−E` (leading `−½Δ²/ξ`), the normal-GS subtraction `+ξ`, and the mean-field self-energy counterterm `+Δ²/(2E)` (leading `+½Δ²/ξ`). The `Δ²` coefficients are `−½/ξ` and `+½/ξ` — they CANCEL EXACTLY (Sage QQ: sum = 0), leaving the genuine condensation `−⅛Δ⁴/ξ³`. So the correct object is not merely "the `|D|`-linear power" but "the `|D|`-linear power evaluated as a `S₊−S₋` difference (paired branch minus normal branch minus self-energy leg)." This is *exactly* the indefinite-pairing structure of Paper 03: the Euclidean `Tr f(D²)` is an all-positive SUM with no second leg, so the `+Δ²` kinetic rise has nothing to cancel against and survives as the `+12.76` penalty; the Krein-paired functional supplies the negative leg, and the cancellation is the condensation.

**Substitution chain for the one directional claim I add ("the signature axis SUPPLIES the cancelling leg; the functional axis SUPPLIES the state weight; neither supplies it alone"):**

```
Step 1 (def, Euclidean SA):  S_E = Tr f(D_BdG²/Λ²) = Σ_k g(λ_k²),  λ_k² = ξ_k²+Δ²  [all-positive sum; S82]
Step 2 (def, Krein-paired):  S_J = S₊ − S₋  (Paper 03 §78 pairing: difference of two elliptic legs)
Step 3 (def, condensation):  E_cond = E_GS(Δ) − E_GS(0)  [difference of |D|-linear ground-state energies]
Step 4 (substitute):  E_cond = [−Σ|λ_k|]_Δ − [−Σ|λ_k|]_0 + (self-energy counterterm)
                              = (S₊−S₋)-type signed difference of |D|-linear pieces
Step 5 (read off): the Δ²-kinetic rise of the paired leg (−½Δ²/ξ) cancels the self-energy leg (+½Δ²/ξ);
        the all-positive Euclidean SUM (Step 1) has NO negative leg ⇒ its +Δ² cannot cancel ⇒ +12.76.
Conclusion: the cancellation is a SIGNATURE-structural fact (signed difference, Paper 03) realized on a
            STATE-weighted, |D|-LINEAR functional (the ground-state energy). Both axes are load-bearing;
            the missing ingredient relative to BOTH naive objects is the |D|-linear power on the dressed op.
```

**Answers to the four adjudication questions, from the signature vantage:**

- **(a) Is the 93× a FUNCTIONAL-FORM failure or a SIGNATURE failure?** Neither alone, and the Sage ladder proves it: lizzi's `Tr(D²ρ_ω)` (functional fix, Euclidean) FAILS at `+½Δ²`; my bare `Str_J(D²)` (signature fix) FAILS at `0`; the two-ingredient composite FAILS at `+½Δ²`. The 93× is a failure of OBJECT-TYPE that has a functional coordinate (moment ≠ ground-state-energy) AND a signature coordinate (Euclidean sum ≠ Lorentzian signed-difference), and the two coordinates are NOT independent — they meet at the `|D|`-linear pairing-dressed power. I withdraw any R1 implication that the signature axis is the *primary* one; lizzi was right that a₀'s signature-robustness (my W2-4) does NOT settle the sign channel, and I was right that her functional fix on a Euclidean triple still sums all-positive. Symmetric.

- **(b) COMPOSE or COMPETE?** **COMPOSE — but as a three-ingredient object, not the two-ingredient product either of us first reached for.** lizzi's three-layer reading is the correct verdict: Layer 1 (defect-identification) COMPOSES (her "no channel" = my "sum where a signed difference is required," two coordinates on one analysis-side failure); Layer 2 (the naive composite `Str_J(D²ρ_ω)`) is SIGN-FAIL (both our Sage chains); Layer 3 (the pairing power `|D_BdG|`) is the third ingredient that makes the composition sign-correct. I add the structural identification she left open: Layer 3 is not arbitrary — it is forced by Paper 03 §78 (the Lorentzian effective action IS a signed difference, which lives at the linear power), and the `ρ_ω` and `J` are then genuine corrections to that object (`J` orders the `±` legs of the difference; `ρ_ω` weights the occupation), not its substance. So: COMPOSE, three ingredients, with the `|D|`-linear pairing-dressed ground-state-energy as the SUBSTANCE and the Krein-`J` + modular-`ρ_ω` as the two CORRECTIONS.

- **(c) Does W2-3's bounded-perturbation theorem CONSTRAIN which diagnosis can be right?** YES, decisively, and it is MY gate so I state its force precisely. W2-3 proves `[D_K+V_BdG]=[D_K]` EXACT in `KK(A_K,ℂ)` because `‖V_BdG‖=|Δ_BCS|<∞` is a Paper-10 (1608.02506) locally-bounded perturbation: topology dressing-RIGID, analysis dressing-SOFT. Three consequences, in increasing force: (1) BOTH fixes are analysis-side — a bounded `V_BdG` cannot flip the K-homology class under either re-weighting (`ρ_ω`) or re-signature (`J`), so they are *a priori* non-exclusive (cannot collide on the topological invariant because neither touches it). (2) Dressing-SOFT means the functional content moves *in the `V_BdG` direction*, and `V_BdG` is precisely the OFF-DIAGONAL pairing block — `D_BdG² = D_K²+|Δ|²⊗1` shows the `D²` power launders `V_BdG` into a scalar (lizzi's chain and mine both see only `+Δ²`), so the off-diagonal physics is *invisible to any `D²` functional* and visible only to `|D_BdG|`. W2-3 is the theorem that NAMES the missing channel: it is the `V_BdG`-off-diagonal content that survives only at the linear power. (3) Therefore W2-3 LICENSES the three-ingredient repair as topology-safe (none of `J`, `ρ_ω`, `|D_BdG|` disturbs `[D_K]`) while FORBIDDING any claim that one or two ingredients suffice. lizzi's Sage chain is the falsification of the two-ingredient claim; W2-3 is the license for the three-ingredient one. I endorse her reading of (c) without reservation — it is also mine.

- **(d) The single forward compute.** I adopt lizzi's `INV13-W?-KREIN-MODULAR-PAIRING-SIGN` gate and sharpen its structure to a three-rung nested `[SIGN]` ladder whose verdict is *the smallest ingredient-set that returns `sign(δS) = −1`*, matched against `E_cond < 0`. My analytic ladder above PREDICTS the outcome rung-by-rung: (i) `|D|`-linear on `D_K`-diagonal → SIGN-FAIL (Δ-blind); (ii) `Tr(D_BdG²ρ_ω)` dressed-but-`D²` → SIGN-FAIL (`+Δ²`); (iii) `Tr_J(|D_BdG|ρ_ω)` evaluated as the `S₊−S₋` signed difference → SIGN-PASS (`−Δ⁴` after the self-energy-leg cancellation). The gate is therefore *well-posed and discriminating* — it is NOT pre-resolved, because the live question it tests is whether the FULL `(A_K,H_K,D_K)` substrate, computed at finite L_max on the actual N_pair=1 BdG sector (not the toy single mode), reproduces the toy ladder's rung-(iii) sign AND lands the genuine `−Δ⁴` order after the mean-field counterterm. The toy is the analytic skeleton; the gate is the substrate realization. I write the 4-field spec below.

---

## Verdict table

| Diagnosis | Advocate | Status |
|:----------|:---------|:-------|
| wrong-functional, **alone** (`Tr(D²ρ_ω)`, moment re-weighted by ω) | lizzi | **INSUFFICIENT** — Sage-exact gap-shift `+½Δ²` (sign +1); the `D²` moment power cannot see the off-diagonal Cooper channel even when state-weighted. The *category* (state-weighted total energy) is correct; the *object as a `D²` moment* is sign-wrong. |
| wrong-signature, **alone** (Krein `Str_J(D²)`, bare super-trace) | vdd | **INSUFFICIENT** — Sage-exact `Str_J(D²)=0` on the particle-hole-symmetric doublet; a bare signature regrade does not reach `−Δ⁴`. a₀ is signature-robust (W2-4 PASS, `a₀^Krein=a₀^Eucl=6440.0`); the signed-difference *structure* is correct but needs the `|D|`-linear power to be realized. |
| **COMPOSE → three-ingredient Krein modular `|D|`-linear functional** (`Tr_J(\|D_BdG\|ρ_ω)` as `S₊−S₋`) | joint | **ADOPTED VERDICT** — the two-ingredient naive composite `Str_J(D²ρ_ω)` is itself SIGN-FAIL (`+½Δ²`, both advocates' Sage chains agree); only the `\|D\|`-LINEAR power on the pairing-DRESSED `D_BdG` returns `sign(δS)=−1` matching `E_cond<0`. Substance = ground-state energy (`S₊−S₋`, Paper 03 §78); Krein-`J` + modular-`ρ_ω` are corrections. W2-3 (bounded perturbation) licenses topology-safety; the gate `INV13-W?-KREIN-MODULAR-PAIRING-SIGN` decides the substrate realization. |

## Wrap-Up

**STRUCTURAL VERDICT: the 93× BCS-wrong-sign penalty is a WRONG-OBJECT-TYPE failure on the dressing-soft analysis layer that COMPOSES across both advocates' diagnoses — and the composition is THREE-INGREDIENT, not two.** The workshop converges (it does not deadlock). The convergence is sharper than either advocate's R1 position and was forced by three honest Sage-exact self-falsifications.

**What converged, and on what evidence.**

1. **Both diagnoses are correct as defect-COORDINATES; neither is the repair alone.** lizzi: `Tr f(D²)` is a single-particle spectral MOMENT, structurally blind to the off-diagonal Cooper amplitude `⟨c_k c_{−k}⟩` (atlas-04 S3: "spectral moment, not a total energy; Fock quantity; categorically different functionals"). vdd: the same `Tr f(D²)` is a Euclidean all-positive SUM where the Lorentzian effective action is a signed DIFFERENCE `S₊−S₋` (Paper 03 §78), and the condensation channel lives in the relative sign the sum discards. These are **two coordinates on one analysis-side failure**, not rival theories — proven by the fact that each advocate's naive repair fails *and the failures are symmetric*: `Tr(D²ρ_ω) → +½Δ²` (lizzi's fix, Sage-exact), `Str_J(D²) → 0` (vdd's fix, Sage-exact).

2. **The naive two-ingredient composite is SIGN-WRONG (the decisive new result, lizzi R2, independently reproduced by vdd R2).** The single object both R1s reached toward — `Str_J(D²ρ_ω)` (Krein-graded AND modular-weighted) — has gap-opening shift `+½Δ²` (sign +1, magnitude 4× below the bare moment's `+2Δ²` but NOT flipped). Sage QQ-exact, confirmed twice. So "each supplies what the other lacks ⇒ the product works" is FALSE at leading order. Three self-falsifications (bare super-trace = 0; modular-only monotone, W1-1 FAIL; naive composite `+½Δ²`), not two.

3. **The repair is three-ingredient, and W2-3 names the missing channel.** Only the `|D|`-LINEAR power on the pairing-DRESSED operator `D_BdG = D_K+V_BdG` returns the `E_cond < 0` sign (Sage ladder: rung (i) Δ-blind, rung (ii) `+Δ²`, rung (iii) `−E` gives `−½Δ²/ξ` sign −1). W2-3 (`[D_K+V_BdG]=[D_K]` EXACT; `D_BdG²=(D_K²+|Δ|²)⊗1`, S82) is the theorem that explains why: the `D²` power launders the off-diagonal `V_BdG` into a scalar `+|Δ|²`, so the pairing physics is *invisible at the `D²` power and visible only at `|D_BdG|`*. The substance of the repair is the ground-state energy `S₊−S₋` (Paper 03 §78); Krein-`J` orders the `±` legs of the difference, modular-`ρ_ω` weights the occupation — both are CORRECTIONS, not the substance.

**Compose-vs-compete adjudication (question b): COMPOSE, with structure.** The correct verdict is lizzi's three-layer reading, completed by vdd's structural identification of Layer 3:
- **Layer 1 — defect identification: COMPOSE** (functional-coordinate ∧ signature-coordinate, one defect).
- **Layer 2 — naive composite `Str_J(D²ρ_ω)`: SIGN-FAIL** (`+½Δ²`; neither handle nor their product reaches the off-diagonal).
- **Layer 3 — the `|D_BdG|`-linear ground-state-energy `S₊−S₋`: SIGN-PASS substance**, with `J` + `ρ_ω` as corrections. Forced by Paper 03 §78 (Lorentzian effective action = signed difference, realized at the linear power), licensed topology-safe by W2-3 (bounded perturbation), and discriminated empirically by the forward gate.

**Question (a) — functional vs signature:** neither alone; the 93× is a wrong-OBJECT-TYPE failure with a functional coordinate (moment ≠ ground-state energy) and a signature coordinate (Euclidean sum ≠ Lorentzian signed-difference) that MEET at the `|D|`-linear pairing-dressed power. lizzi's R1 over-claimed that a₀-robustness (vdd's W2-4) nearly settled it from the functional side; vdd's R1 over-claimed the signature axis as primary. Both retracted in R2; the symmetric concession is the convergence.

**Question (c) — W2-3 constraint:** decisive and in the COMPOSE direction. Topology dressing-RIGID (`[D_K]` invariant under any bounded `V_BdG`) ⇒ neither fix touches the K-homology class ⇒ they cannot collide on the topological invariant ⇒ *a priori* non-exclusive. Analysis dressing-SOFT *in the `V_BdG` off-diagonal direction* ⇒ that direction is the missing channel, accessible only at `|D_BdG|`. W2-3 licenses the three-ingredient repair as topology-safe and forbids any one- or two-ingredient sufficiency claim.

**Question (d) — the deciding compute:** `INV13-W?-KREIN-MODULAR-PAIRING-SIGN` (4-field spec below). Three-rung nested `[SIGN]` ladder on the actual finite-L_max N_pair=1 BdG sector; verdict = smallest ingredient-set returning `sign(δS)=−1` matched to `E_cond<0`. The toy ladder predicts rung (iii) is first to PASS; the gate tests whether the FULL substrate reproduces it AND lands the genuine `−Δ⁴` order after the mean-field self-energy cancellation (Sage-verified in the toy: the `−½Δ²/ξ` of `−E` cancels the `+½Δ²/ξ` of the `+Δ²/2E` counterterm, leaving `−⅛Δ⁴/ξ³`).

**Standing scope-honesty.** This workshop establishes the *object-type* of the correct modulus effective action (a `|D|`-linear, pairing-dressed, Krein-paired, modular-weighted ground-state-energy functional) and proves the two naive repairs and their product insufficient. It does NOT establish that the three-ingredient object lands the correct *number* on the substrate — that is the forward gate, and it remains genuinely open (the toy is the analytic skeleton, not the substrate verdict). atlas-04 S3 stays ASSUMED until the gate lands; this workshop converts "SA is the wrong functional" from an assertion into a *typed, falsifiable, topology-safe repair candidate with a pre-registered sign criterion*. No register status is mutated here (investigation track is registry-quarantined — see Effected In-Session).

**Cross-cite (per DEDUP note, doc line 29):** distinct from inv-5 W3-2 (connes↔landau, "is `Tr f(D²)` the substrate's free energy?"). That gate asks whether the Euclidean moment IS a free energy; THIS gate asks which replacement repairs it (modular vs Krein vs compose) and answers: the three-ingredient `|D|`-linear Krein-modular ground-state-energy. The two are complementary — inv-5 W3-2 diagnoses the Euclidean object's thermodynamic mis-identification; INV12-W4-2 specifies the corrected object's type.

### Effected In-Session

No in-track non-math items — session-register reconciliation routes to session-promotion at `/rclab-investigate` close.

This workshop's only candidate register-touch would be a down-/re-tag of **atlas-04 S3** ("SA provides the correct effective action for modulus dynamics", currently ASSUMED). That edit is **deliberately NOT made here**: the investigation track is registry-quarantined (track-local boundary per `gate-verdicts.md §"Investigation-Track Canonical Path"` — investigation verdict files and workshop artifacts under `sessions/investigation/` and `computations/investigation-12/` are NOT swept by the session-scoped `/weave --update` extractor, and an investigation result enters the permanent register only when *promoted into a session* and re-computed under a `session-{N}` gate). The structural verdict above (S3's `Tr f(D²)` is the wrong OBJECT-TYPE; the typed three-ingredient repair candidate; the forward sign-gate) is the promotable content; the atlas-04 S3 reconciliation is its session-promotion consequence, to be picked up at `/rclab-investigate` close, NOT a track-local edit. No session-track register file (atlas-04, atlas-07, permanent-results-registry, canonical_constants.py) was edited by this workshop.

## Carry-Forward Computations

The single decisive forward compute that settles wrong-functional-vs-wrong-signature-vs-compose on the SUBSTRATE (the toy ladder above is the analytic skeleton; this gate is its substrate realization).

| Field | Specification |
|:------|:--------------|
| **What** | `INV13-W?-KREIN-MODULAR-PAIRING-SIGN` — a three-rung nested `[SIGN]` ladder computing `δS = S(BdG-paired) − S(unpaired)` on the **actual finite-L_max N_pair=1 BdG sector** of `(A_K, H_K, D_K)` under three nested functionals, each emitting its own sign verdict, the gate verdict being **the smallest ingredient-set that returns `sign(δS) = −1`** matched against `E_cond < 0`: **rung (i)** `S = −Tr\|D_K\| ρ_ω` (`\|D\|`-linear, `D_K`-diagonal only, no `V_BdG`) — predicted SIGN-FAIL (Δ-blind); **rung (ii)** `S = Tr(D_BdG² ρ_ω)` (pairing-dressed, but `D²` power) — predicted SIGN-FAIL (residual `+Δ²`, since `D_BdG²=(D_K²+\|Δ\|²)⊗1` launders the off-diagonal to a scalar); **rung (iii)** `S = Tr_J(\|D_BdG\| ρ_ω)` evaluated as the `S₊−S₋` signed difference of ground-state energies with the mean-field self-energy counterterm — predicted SIGN-PASS (`−Δ⁴` order after the Δ²-kinetic cancellation). The gate's structural OUTPUT is the verdict on (a)/(b): if rung (iii) is the first PASS, the answer is "neither functional nor signature alone — the three-ingredient `\|D\|`-linear Krein-modular ground-state-energy." |
| **Inputs** | (1) `D_K(τ_fold=0.190)` finite-L_max spectrum cache — `computations/_shared/s84_spectrum_cache_L12_tau019.npz` (block-diagonal Peter-Weyl, the operational L_max per the Casimir-bound/Friedrich-Bär feasibility pre-check in `math-scripts.md §"D_K Block-Diagonality"`). (2) `V_BdG` pairing block — `‖V_BdG‖=|Δ_BCS|=0.4642547394830737` (canonical `Delta_BCS`, S70, R-protected; `D_BdG²=(D_K²+|Δ|²)⊗1₂` per S82). (3) BCS ground-state occupation `ρ_ω` diagonal `(u²,v²)` on the N_pair=1 8×8 exact-reduction sector (atlas-04 B4: 1.2e-14 agreement with full ED). (4) Krein fundamental symmetry `J` (linear, J²=+1, Krein split (16,8,8) — W2-4 `audit 677f4185…2838`; **convention guard: NOT Connes' antilinear J** per agent-memory convention warning). (5) anchors `E_cond = −0.1151` (ED-CORRECTED-35) and `δ_S_BdG = +12.76` (atlas-07 W8) as the sign targets. (6) W2-3 `audit 9f861259…dffb0d` (bounded-perturbation theorem; topology-safety license). |
| **Gate** | `[SIGN]` (three nested sub-predicates). **PASS** (of the gate) iff rung (iii) returns `sign(δS_iii) = −1` (matching `sign(E_cond) = −1`) AND its leading order is `O(Δ⁴)` (the genuine condensation, after the `−½Δ²/ξ` + `+½Δ²/ξ` mean-field cancellation — Sage-exact in the toy) AND rungs (i),(ii) return `sign ≥ 0` (confirming the smaller ingredient-sets are insufficient, i.e. the three ingredients are each load-bearing). **INFO** iff rung (iii) is sign-correct but lands the wrong ORDER (`O(Δ²)` not `O(Δ⁴)`) — diagnoses that the self-energy counterterm is not correctly supplied by the substrate functional. **FAIL** iff rung (iii) does NOT flip the sign — would falsify the COMPOSE verdict and reopen wrong-functional-vs-wrong-signature. Pre-registered tolerance: sign is exact (integer ±1); order via log-log fit of `δS(Δ)` over `Δ ∈ [0.1, 0.5]·|Δ_BCS|`, slope `4 ± 0.3` for PASS. Machinery pins (PRDR): `L_max_operational` (Casimir-bound from the cache), `convention=Krein-FW-LINEAR-J` (J²=+1 guard), `scheme=S₊−S₋-signed-difference-with-MF-counterterm`, float64/complex128, GPU `torch.linalg` for the 8×8-per-block dressed eigenproblem. |
| **Effort** | **MEDIUM.** The analytic skeleton is fully derived and Sage-verified (this workshop); the substrate compute is a single producing script reusing the S84 L12 cache + the S82 `D_BdG²` closed form (no new irrep construction — the dressed square is the bare cache plus a scalar shift, so all three rungs evaluate on the existing block-diagonal spectrum). Dominant cost is assembling the N_pair=1 8×8 `ρ_ω`-weighted `J`-graded `|D_BdG|` trace per Peter-Weyl block and the log-log order fit. One agent, one wave (`van-den-dungen-bridge-theorist` owns the Krein/`J`/`|D_BdG|` math; `lizzi-spectral-functional-theorist` cross-checks the `ρ_ω` weight and `Tr(D²ρ_ω)` rung-(ii) baseline; `nazarewicz-nuclear-structure-theorist` cross-checks the BCS mean-field `E_cond` counterterm and the N_pair=1 ED anchor). Depends on: nothing un-landed — all six inputs exist on disk. |

**Cross-cite (forward):** this gate is the substrate-realization successor to inv-5 W3-2 (connes↔landau free-energy diagnosis) and consumes the same `Tr f(D²)`-mis-identification it diagnoses; route both into the S{N+1} session plan as the paired "corrected-object-type" computation per `gate-verdicts.md §"Track-local boundary"` (investigation → session promotion). The atlas-04 S3 re-tag is the register consequence of this gate's PASS, not of this workshop.
