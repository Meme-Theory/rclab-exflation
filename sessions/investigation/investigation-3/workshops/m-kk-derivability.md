# INV3-W4-1 — M_KK-Derivability Adjudication Workshop

**Gate**: INV3-W4-1 (`gate_type: workshop`) | **Investigation**: 3 | **Wave**: 4
**Agents (EXACTLY 2)**: `spectral-geometer` (Reading-S advocate) ↔ `paasch-mass-quantization-analyst` (Reading-P advocate)
**Rounds**: 2 — R1 each advocate states its case from first principles; R2 each rebuts the opponent's R1 and the pair converges on a STRUCTURAL VERDICT.
**Closure**: artifact-existence (NO verdict line) — this document with `## Wrap-Up` + `Effected In-Session` + `## Carry-Forward Computations` present (per `gate-verdicts.md §"Investigation-Track Canonical Path"`; `wave-classification.md §M1`).
**Classification**: GEOMETRIC (the object is the substrate's spectral triple and whether it fixes its own dimensional scale).

---

## Adjudication Question (planner-neutral; both readings at EQUAL weight)

**THE TENSION** — M_KK-DERIVATION, the framework's #1 standing dimensional gap, is read oppositely by the two domain advocates. The planner pre-judges NEITHER; the workshop derives which is correct from first principles.

**Reading-S (spectral-geometer):** the spectral triple (A_K, H_K, D_K) predicts the dimensionless `Ô`'s exactly and predicts NO absolute scale at all; M_KK is fixed only by external G_N-matching. The STAGE-3-PERMANENT NNU rank-1 theorem (§VII.BS) confirms every dimensionful observable shares one un-fixed weight `w = M_KK` (the `O = w·Ô` factorization; rank-1 in the borrowed-H shift-covariance, Corr = +1; clause-(b) bundle-exhaustiveness CONFIRMED at S103, w2 = m_H/v_ew factors through M_KK). ⇒ M_KK-DERIVATION may be STRUCTURALLY IMPOSSIBLE — a scale-free theory cannot fix its own scale.

**Reading-P (paasch-mass-quantization-analyst):** Paasch's absolute-mass machinery — mass integers N(j)=7n, M(j)=√N(j), the proton-cubic at n3 = dim(3,0) = 10 — derives m_p to Δm/m ≈ 5e-7 as an ABSOLUTE value. Stripped of the observationally-dead Dirac-G~1/t (LNH) scaffolding, that algebraic core is a candidate ANSWER to M_KK-DERIVATION. ⇒ M_KK may be DERIVABLE from the SU(3) integer structure without an external scale.

**SUB-QUESTIONS** (each answered on its own merits; the verdict on each may cut either way):
- **(a)** Does the NNU rank-1 result (§VII.BS; S103 bundle-exhaustiveness ac1dbb28) PROVE in-principle underivability of M_KK, or does it merely confirm that ONE external pin currently exists (characterize the rank of the EXISTING import structure) WITHOUT foreclosing an independent integer-structure derivation of the scale's value? Distinguish "rank-1 in the borrowed-H shift-covariance over the enumerated dagger-row bundle" from "no substrate-internal datum can ever fix M_KK's numerical value."
- **(b)** Does Paasch's N(j)=7n + proton-cubic fix m_p (hence M_KK) WITHOUT a hidden external scale, once the dead Dirac-G~1/t scaffolding is severed? Audit whether the algebraic core (the integers 7,35,42,98,150; M(j)=√N(j); the n3=10 proton-cubic) is genuinely dimensionless-input → m_p, or whether a GeV/seconds calibration re-enters somewhere (an implicit length, an electron-mass anchor, a c/ℏ unit bridge). The §VII.BS falsifier-(i) form — "if a future gate writes a dimensionful observable = Φ(D_K eigenvalues alone) with NO imported GeV/seconds scale, the theorem is FALSIFIED (rank-0)" — is the precise test.
- **(c)** What SINGLE compute would DECIDE it? Candidate: INV3-W3-4 (Casimir-graded N(j)=7n at L_max=12, τ_fold). The workshop names the decisive gate and its PASS/FAIL reading on the verdict (it may endorse INV3-W3-4, refine it, or name a different/additional decisive compute).

**STRUCTURAL VERDICT TARGET**: a pinned position on M_KK-DERIVATION's status — **LIVE CORRIDOR** (a substrate-internal derivation of the scale's value is not foreclosed; name the open route) vs **PROVEN-IMPOSSIBLE WALL** (the scale-free structure forecloses self-derivation; M_KK irreducibly external) vs a precisely-scoped **INTERMEDIATE** (e.g. "value-derivable, normalization-not"; "rank-1 confirms the IMPORT structure but is silent on an INTEGER-VALUE derivation") — plus the decisive forward gate. The verdict resolves the competing claims into a NEW pinned position; it does NOT queue a computation in place of a verdict.

---

## Current-State Register (supplied as FACT, NOT as an adjudication of the question)

- **§VII.BS is STAGE-3-PERMANENT** (Stage-2 cross-axis PASS-AND audit `d309efb4…`; clauses (a)/(c)/(e) JOINT-PASS-AND, (b)/(d)/(f) volovik-side, (g) phonon-first-side). Frozen Stage-0 text: the substrate determines the conformal class + ALL dimensionless dynamical shapes (Ô) — every ratio, ordering, tilt, growth shape, late-time tracking exponent n=2 — but NOT the dimensional metric normalization; the obstruction is rank-1 in the borrowed-H shift-covariance (one scale w = M_KK projects onto N≥5 observables at different powers via O = w·Ô).
- **Clause-(b) bundle-exhaustiveness** upgraded standing-premise → RESULT at S104 W1-4 on S103-NNU-BUNDLE-EXHAUSTIVENESS PASS (audit `ac1dbb28`; augmented borrowed-H shift-covariance with candidate second scale w2 = m_H/v_ew returns rank(Cov_aug) = 1, second relative singular value 1.07e-17 ≈ machine zero ⇒ m_H factors through M_KK; the augmented dagger-row bundle is exhaustive).
- **EVOI §6** names M_KK-DERIVATION the single highest-leverage standing gap and "the ONLY incumbent-ceiling-lift path"; the §VII.BS revisit clause is CLOSED-NEGATIVE per ac1dbb28 (the single-M_KK-import structure STANDS).
- **M_KK = M_KK_gravity = 7.428660036284456e+16 GeV** (S42 CONST-FREEZE-42) — the spectral-zeta / G_N-matching value, the EXTERNAL pin both readings reference.
- **Paasch absolute-mass scaffolding** rides on the observationally-EXCLUDED Dirac G~1/t (LNH); the seed flags that the algebraic core is LNH-INDEPENDENT, and that severing the dead scaffolding is the PREREQUISITE to citing Paasch absolute masses as M_KK candidates. Whether the severed core is scale-input-free is exactly sub-question (b).

### Investigation-3 Waves 1–3 completed (current-state FACT; interpretation for M_KK-DERIVATION is the workshop's to derive)
The inv3 verdict ledger is `computations/investigation-3/inv3_gate_verdicts.txt`; the W1/W2/W3 working papers carry the per-gate results. Both advocates may consult what bears on their argument — these are verdicts (facts), NOT a pre-judgment of this adjudication:
- **INV3-W3-4** (the candidate (c) compute) HAS RUN — read its verdict + `investigation-3-w3-workingpaper.md §W3-4` for the result (Casimir-graded N(j)=7n test).
- **INV3-W2-1** (d_s-flow as scale-transport) and **INV3-W2-4** (Weyl-remainder geodesic stationarity) — spectral-geometer-side dimensionful-axis gates — have run; see `investigation-3-w2-workingpaper.md §W2-1/§W2-4`.
- **INV3-W3-3** (α-dim n3=dim(3,0)=10 + two-α) has run; see `investigation-3-w3-workingpaper.md §W3-3`.
- **INV3-W2-3** (A_s = (M_KK/M_Pl)² × intensive) has run; see `investigation-3-w2-workingpaper.md §W2-3`.

### Numeric stakes (supplied for both advocates equally)
M_KK pin = 7.4287e16 GeV. M_KK-DERIVATION is the keystone bottleneck of EVOI Tier-1 #1 / #2-scale / #7b' / #9b-SCALE. A LIVE-corridor verdict opens the incumbent-ceiling-lift path to a follow-on derivation gate; a PROVEN-WALL verdict pins M_KK as irreducibly external and re-frames the §6.3 a(t)-gap as a structural feature (the seconds-valued a(t) dies, the SHAPE survives).

### Binding discipline
- **NEUTRALITY** (`feedback_review-dispatch-no-orchestrator-angle.md`): the planner/orchestrator injects NO angle. Reading-S and Reading-P are at EQUAL weight; do NOT read this spec as favoring "impossible" or "derivable".
- **INDEPENDENCE**: this is an EXPLORATORY adjudication workshop — each advocate is SUPPOSED to bring its domain reading and rebut the other. It is NOT a Stage-2 joint-theorem cross-check, so the `joint-theorem-promotion.md` "without prior workshop context" rule does NOT apply.
- **SUBSTRATE-FIRST** (`phononic-framing.md`): D_K eigenvalues → spectral moments → dimensionless shapes → measurement. FORBIDDEN inversion (neither advocate may invoke): "M_KK is set by GR/G_N as a pre-existing container the substrate lives in." §VII.BS frames M_KK as the one externally-calibrated CUTOFF of the substrate's own emergent metric (N₃=0 ⇒ topologically unprotected normalization), NOT a container; Reading-P's claim is that even this one cutoff is fixed by the SU(3) integer structure, Reading-S's is that it is not.

**Sources** (read the registered entry / survey; do NOT receive any other agent's workshop transcript out of band): spectral-geometer survey §G1 (`sessions/investigation/investigation-1/spectral-geometer.md`), paasch survey §G1 (`sessions/investigation/investigation-1/paasch-mass-quantization-analyst.md`), §VII.BS via knowledge MCP (`trace_entity` / `query_entity`; `sessions/permanent-results-registry.md §VII.BS`), `get_constant M_KK`, EVOI §6 (`sessions/evoi-framework.md`).

---

## Round 1 — state the case from first principles

### R1 · Turn A — spectral-geometer (Reading-S)

**Thesis.** The spectral triple `(A_K, H_K, D_K)` is a *scale-free* object. It fixes the dimensionless spectral shapes `Ô` exactly — every ratio, ordering, tilt, growth-shape, the tracking exponent n=2 — and it fixes **no absolute dimensional scale at all**. `M_KK` is not a quantity the substrate computes; it is the one external cutoff that converts the substrate's dimensionless predictions into lab units. The STAGE-3-PERMANENT NNU rank-1 theorem (§VII.BS, audit `d309efb4`) is the formal statement that this single un-fixed weight `w = M_KK` is the *complete* dimensionful content of the framework. My reading: **M_KK-DERIVATION is a PROVEN-IMPOSSIBLE WALL at the level of any spectrum-only functional** — and the live corridor that survives is narrow, external, and not the integer-structure route Reading-P proposes. I argue this from governing structure first, then meet (a)/(b)/(c).

---

**The governing structure — why a heat-kernel object cannot carry its own scale.** Begin with the only object that holds all spectral information, the heat trace
```
  P(σ) = Tr( exp(−σ D_K²) ) = Σ_k m_k exp(−σ λ_k²).          (R1.S-1)
```
`D_K` is a dimensionless operator on the truncated triple; `σ` carries inverse-(eigenvalue)² and is the only dimensionful dial in (R1.S-1), and it is an *integration variable*, not a substrate output. Every framework observable is extracted from (R1.S-1) by one of two limbs: the small-σ Seeley–DeWitt limb (`a_0, a_2, a_4, …`, local curvature invariants) or the zeta-sum limb (`ζ_D(s) = (1/Γ(s)) ∫₀^∞ σ^{s−1}(P(σ)−P(∞)) dσ`, spectral moments). **Both limbs are pure functionals of the eigenvalue multiset `{λ_k, m_k}`.** A pure functional of a dimensionless multiset returns a dimensionless number. There is no operation on `{λ_k}` — no ratio, no trace, no residue, no determinant — that manufactures a GeV. This is not a framework quirk; it is the defining property of spectral geometry. Kac's "hearing the shape of a drum" already *presupposes* a metric scale on the drum; the spectrum hears the *shape*, and the overall size enters only as the dimensionful prefactor `Vol` in the Weyl law `N(λ) ~ (4π)^{−d/2} Vol · λ^{d/2} / Γ(d/2+1)`. The substrate gives `(4π)^{−d/2}/Γ(d/2+1)` and the dimensionless shape of `N(λ)`; it does not give `Vol` in cm⁸. **The scale is exactly the one datum the spectrum cannot return.**

**The NNU rank-1 certificate is the precise quantification of this.** I verified the rank arithmetic of the §VII.BS certificate from first principles (Sage; not re-citing). Take the borrowed-H shift-covariance: each dimensionful observable factors as `O_i = w^{p_i} · Ô_i` with `Ô_i` a dimensionless spectral kernel, and one asks the rank, across observables, of the log-Jacobian under the independent log-scales `{ln w_k}`. The substitution chain (per `math-scripts.md §"Double-Check Logic Before Compute"`):

```
  Claim: "a scale-free spectrum forces the shift-covariance rank ≤ 1; hence |Corr|=1 exact."
  Step 1:  O_i = w^{p_i} · Ô_i,   Ô_i = Φ_i({λ_k, m_k})  [dimensionless; no GeV input]   [R1.S-1 limbs]
  Step 2:  ln O_i = p_i · ln w + ln Ô_i                                                    [log]
  Step 3:  d ln O_i / d ln w = p_i   (a CONSTANT in i's index, independent of observable)   [∂ on R1.S-1]
  Step 4:  with ONE scale, the Jacobian J[i] = p_i is a single COLUMN ⇒ rank(J) = 1
           ⇒ every pair of log-shifts (p_i·δ, p_j·δ) is collinear ⇒ Corr_ij = sign(p_i·p_j), |Corr_ij| = 1 EXACT
  Step 5:  with TWO genuinely independent scales, J has two columns ⇒ rank(J) = 2 ⇒ decorrelation possible
  Conclusion: rank-1 ⟺ exactly one un-fixed scale; the rank IS the count of independent unprotected scales.
```
Numerically: one scale → rank(J)=1, all off-diagonal `|Corr|=1.0` to 1e-12, `sign(Corr_ij)=sign(p_i·p_j)` (this is precisely the §VII.BS sign-resolved falsifier-(ii) precision, `|Corr|=1` with `sign = sign(p_i·p_j)`, NOT a blanket +1); two independent scales → rank(J)=2, off-diagonal `|Corr|` falls to 0.011. The theorem's Half A (covariance rank = number of independent unprotected scales) and Half B (the BDI vacuum, N₃=0 at S44, imports exactly ONE such scale — the cutoff, with no Fermi-point protection of the induced metric) together pin the framework's entire dimensionful content to `w = M_KK`. **Reading-S is not an interpretation of §VII.BS; it is what §VII.BS proves.**

**Substrate-first, not container-thinking.** I am careful here per the binding discipline. I am NOT saying "GR/G_N sets M_KK as a pre-existing container." I am saying the opposite: the substrate's emergent metric `g_M` (from `a_2`) has a *conformal class* that is fully substrate-determined and a *normalization* that is topologically unprotected (N₃=0 ⇒ no Fermi-point pins the induced-metric scale). The arrow stays `D_K eigenvalues → spectral moments (Ô) → dimensionless shapes → measurement`. The one thing the arrow does NOT carry is a dimensionful prefactor, because the head of the arrow (`{λ_k}`) is dimensionless. `M_KK` is the cutoff of the substrate's *own* emergent metric, fixed by the one external matching (`1/G_induced ~ f₂·M_KK²·a₂`, clause (d), volovik Sage chain) — calibration of an emergent scale, not embedding in a container.

---

**Sub-question (a): does NNU rank-1 PROVE in-principle underivability, or merely characterize the EXISTING import?**

I concede the sharp distinction the planner draws, and I do *not* over-claim. What §VII.BS proves, exactly, is: **(i)** the borrowed-H shift-covariance over the enumerated dagger-row bundle is rank-1 (`|Corr|=1`, sign-resolved), and **(ii)** clause-(b) bundle-exhaustiveness `ac1dbb28` shows the *augmented* bundle (adding the candidate second scale `w₂ = m_H/v_ew`) stays rank-1 (second relative singular value 1.07e-17 ≈ machine zero ⇒ `m_H` factors through `M_KK`, it is the a₄-dressed `|S|²` KK-threshold mode, not an independent protected invariant). So the *import structure is rank-1 and the candidate-second-scale escape is closed*. That is the literal theorem.

But the theorem's reach is broader than "one import currently exists," and this is the crux of my (a) answer. Half A is a **structural** statement, not a bundle census: the shift-covariance rank *equals the number of independent unprotected dimensional scales*, full stop. Half B identifies that number as 1 via the topology (N₃=0 BDI vacuum). The bundle-exhaustiveness result `ac1dbb28` then closes the only avenue by which the count could secretly be ≥2 (a hidden second protected scale masquerading inside the observable list). Put together: **any quantity the substrate produces by a spectrum-only functional `Φ({λ_k})` is dimensionless, hence cannot fix `w`; and there is no second independent scale hiding in the bundle to fix it either.** That is stronger than "one external pin exists" — it is "no spectrum-only datum *can* fix `M_KK`'s value, and the bundle admits no second scale." The precise scope: rank-1 in the borrowed-H shift-covariance does NOT, by itself, forbid some *non-spectral* substrate datum (a cocycle norm, a holonomy, a genuinely new external matching) from carrying a scale — but it *does* forbid any `Φ(D_K eigenvalues alone)` from doing so. This is exactly the falsifier-(i) form: "if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with no imported GeV/seconds scale, the theorem is FALSIFIED (rank-0)." That falsifier RAN and FAIL-confirmed at S102 (`63698aa8`) — no spectrum-only functional produced `gamma_unit`. **My (a) verdict: NNU rank-1 proves in-principle underivability of `M_KK` by any spectrum-only functional, and closes the second-scale escape; it leaves open only a non-spectral or new-external route, which is precisely where Reading-P's integer machinery must live or die.** That is the WALL — scoped to the spectrum-only functional class, which is the entire class from which the framework derives its physics.

---

**Sub-question (b): does Paasch's N(j)=7n + proton-cubic fix m_p (hence M_KK) without a hidden external scale?**

This is where I press hardest, because the framework already RAN the decisive test of the integer core and it came back negative. The candidate (c) compute, **INV3-W3-4** (Casimir-graded `N(j)=7n` at L_max=12, τ_fold), returned INFO with the operative numbers: of the five Paasch mass integers `{7, 35, 42, 98, 150}` mapped onto SU(3) representation dimensions, **only 2 of 5 are actual SU(3) dimensions** (muon 35 = dim(0,4)/related, pion 42 — yes; electron 7, kaon 98, proton 150 — **no**); the best Casimir-graded reconstruction `D_single_dim_pq` reproduces `7n` for only **2 of 5**; and "7 is a mode-MULTIPLICITY unit, NOT an irrep dimension." The proton integer `n3 = 150` is the very one that fails to be a substrate dimension. So the integer core does not cleanly emerge from the `D_K` spectrum's representation content; it is a *numerological overlay* that matches the substrate on 2 of 5 rows and misses on the load-bearing proton row.

Now the dimensional-input audit, which is the heart of falsifier-(i) applied to Reading-P. Even granting the integers, trace the chain `integers → m_p`:
```
  Step 1: N(j) = 7n  and  M(j) = √N(j)   are DIMENSIONLESS integers / their roots.   [Paasch core]
  Step 2: a dimensionless M(j) → a mass in GeV requires a multiplicative bridge with units of mass:
              m(j) = M(j) · μ₀,    [μ₀] = GeV.
  Step 3: μ₀ is NOT produced by {7,35,42,98,150} — it is the anchor that carries the scale.
  Conclusion: the integer core fixes RATIOS m(j)/m(j') = M(j)/M(j') (dimensionless),
              NOT absolute m_p; an external μ₀ (a GeV anchor) re-enters at Step 2.
```
This is exactly the §VII.BS structure: `O = w · Ô` with `Ô = M(j)` the dimensionless integer-root shape and `w = μ₀` the un-fixed scale. The inv3 ledger confirms the *ratio*-only character empirically: W3-4 found that "only the kaon-proton M-ratio ~ √N" survives — i.e., the machinery's genuine content is a **ratio** (`N(p)/N(K) = 150/98 = 1.530612`, within 0.063% of `φ_paasch = 1.531580`), and a ratio is dimensionless by construction. The Dirac-G~1/t (LNH) scaffolding that the seed flags as dead is *exactly* what used to supply `μ₀` (a time-dependent G furnishing a mass scale from cosmological inputs — seconds/GeV). Sever it, and the GeV anchor `μ₀` does not disappear; it becomes *undetermined*. Reading-P cannot have it both ways: either LNH (dead, observationally excluded) supplies the scale, or the scale is un-supplied and the integers give only ratios. **My (b) verdict: the severed algebraic core is genuinely dimensionless-input → m_p/m_e ratios, NOT → absolute m_p; a GeV anchor re-enters at the integer-root-to-mass bridge.** Falsifier-(i) applies verbatim: the integers are a `Φ` over an integer overlay, not over `{λ_k}` even — and they carry no GeV. Reading-P's core, stripped of LNH, falls on the dimensionless side of `O = w·Ô`. It is a candidate computation of `Ô`-ratios (and a *poor* one, 2/5 on the substrate-dimension test), not a candidate for `w`.

I add one corroborating spectral fact from W3-3 (the n3=10 α-formula), because it cuts *against* a naive integer-scale reading even where the integers work: the fine-structure formula `α = (1/dim(3,0)²)(Ω/2)^{1/4}` PASSes at 0.855 ppm with `n3 = dim(3,0) = 10` uniquely load-bearing — but `α` is **dimensionless**. The one place a Paasch-style integer (`dim(3,0)=10`) is rigorously substrate-derived and observationally sharp, it produces a *dimensionless* coupling, not a scale. That is the pattern everywhere: integers → dimensionless shapes. Consistent with Reading-S, not Reading-P.

---

**Sub-question (c): the single compute that DECIDES it.**

INV3-W3-4 already ran and is, in my reading, *decisive against the integer-derivation of the scale*: 2-of-5 substrate-dimension match with the proton row (n3=150) failing, and a surviving content that is a dimensionless ratio. I do not think it needs re-running to settle (b) — it settled it. What remains genuinely open, and what I name as the decisive forward gate for the *verdict's live corridor*, is the **falsifier-(i) closure on the non-spectral channel**: 

> **Decisive gate (Reading-S framing): a non-spectral-scale existence scan.** Pre-register that a *dimensionful* `M_KK` value can be written as `M_KK = Φ(non-spectral substrate invariant)` with NO imported GeV/seconds anchor — where "non-spectral substrate invariant" means a quantity that is NOT a spectrum-only functional `Φ({λ_k})` (the candidates: a cocycle-norm with intrinsic units, a holonomy-derived scale, or the `1/G_induced ~ f₂·M_KK²·a₂` matching read in reverse). PASS (some non-spectral substrate datum carries a GeV without external import) → §VII.BS falsified at rank-0, M_KK-DERIVATION flips to LIVE-CORRIDOR. FAIL/INFO (every candidate either reduces to a spectrum-only functional, hence dimensionless, or smuggles an external anchor) → the WALL stands, M_KK irreducibly external.

This is the gate that actually discriminates the verdict targets, because INV3-W3-4 (the seed's candidate (c)) tests the *integer-structure* route specifically and has already returned negative on the proton row. The honest forward question is not "re-run the integers" but "is there ANY non-spectral substrate datum with units" — and §VII.BS's Half A says the answer determines the rank.

**One concession I will carry into R2 (anticipating Reading-P, and against my own corridor).** My own UB-1 proposal — that the substrate's spectral-dimension flow `d_s(σ)` transports the UV fiber scale `K=2.0 M_KK` to the IR pivot `K* ≈ 0.087 M_KK` — was tested as **INV3-W2-1 and FAILED** (`c31277d5`): the integrated anomalous scaling `I = −0.2004` vs the needed `ln(K/K*) = 3.135`, a 93.6% miss AND a sign flip, because on the *finite* L=12 truncation `d_s(σ→0) = 0.0032` (not 8) — the truncation artifact I flagged in my own survey (`d_s → 0 not 8` on the truncated spectrum, Level-3-artifact tier). So I do NOT get to claim the substrate transports its own scale via `d_s`-flow; that corridor is closed in its pre-registered form. This *strengthens* Reading-S, not weakens it: even the most promising spectrum-internal scale-transport mechanism fails to manufacture the `K → K*` ratio, consistent with "the spectrum carries shapes, not scales." But it also disarms a Reading-P rebuttal that might say "the spectral-geometer's own bridges would derive a scale" — they don't, and W2-1 proves they don't, which is the point.

**And the corroborating rigidity result.** INV3-W2-2 (isospectral rigidity, L_max=3) PASSed with an **empty degeneracy set** — no `τ_i ≠ τ_j` gives bit-identical `{a_0, a_2, a_4}` + `λ²` multiset (closest pair combined distance 24.6; `s_1 = Σλ²` strictly monotone, min adjacent gap 0.6535 ≫ tol). So the spectrum is τ-faithful (the geometry IS spectrally reconstructible in its deformation class — Connes reconstruction applies). This matters for the verdict: it means the substrate's *dimensionless* content is fully and faithfully encoded in the spectrum (good news for the `Ô` predictions), and there is no isospectral hiding place where a scale could live unseen. The faithfulness is *of the shapes*. The scale is still absent — rigidity confirms the spectrum determines everything it can determine, and a dimensionful scale is not among them.

---

**R1 summary (Reading-S).** The heat-kernel object is scale-free by construction (R1.S-1 limbs are dimensionless functionals); §VII.BS rank-1 (Sage-verified: rank = number of independent unprotected scales = 1) is the exact certificate; (a) NNU proves underivability by *any spectrum-only functional* and closes the second-scale escape (`ac1dbb28`); (b) Paasch's integer core, stripped of dead LNH, gives dimensionless ratios (W3-4: 2/5 substrate-dim match, proton n3=150 fails, surviving content `150/98` is a ratio) with a GeV anchor `μ₀` re-entering at the integer-root→mass bridge; (c) the decisive gate is a non-spectral-scale existence scan (falsifier-(i) on the non-spectral channel), since INV3-W3-4 already returned negative on the integer route. **My pinned position: PROVEN-IMPOSSIBLE WALL for the spectrum-only functional class (the entire class the framework derives physics from), with at most a narrow non-spectral/external LIVE-corridor that no run has yet populated.** I expect Reading-P to dispute (b)'s "ratio-only" claim and to argue the integers are more than overlay; I will meet that on the 2/5 substrate-dimension miss and the `O = w·Ô` re-entry point in R2.

### R1 · Turn B — paasch-mass-quantization-analyst (Reading-P)

**Thesis.** Paasch's program is the only 70-year mass-relation tradition that has ever produced *absolute* nucleon masses — `m_p` to `Δm/m = 4.9e-7`, `m_n` to `2.9e-8` — and it does so from an integer skeleton (`N(j)=7n`, `M(j)=√N(j)`, the proton-cubic at `n3 = dim(3,0) = 10`) that is *algebraically* LNH-independent even though Paasch *deployed* it inside the observationally-dead Dirac-G~1/t cosmology. My reading: **M_KK-DERIVATION is a SCOPED-INTERMEDIATE — "ratio-derivable, normalization-not" — and crucially NOT the blanket PROVEN-WALL Reading-S claims**, because the §VII.BS net (spectrum-only heat-kernel functionals of `{λ_k}`) does not cleanly enclose the *channel* Paasch's integers actually live in: exact SU(3) representation arithmetic, which is τ- and regulator-independent and is precisely the content §VII.BS declares out-of-scope (5-anatomy N/A, 4-corner parse-tree N/A). I will argue the structural position first, then meet (a)/(b)/(c) — and I will concede (b) exactly as far as the arithmetic forces, no further. This is my independent case; my full rebuttal of Reading-S's R1 comes in R2.

---

**The governing structure — the substrate's integers are NOT heat-kernel functionals.** Reading-S's R1.S-1 argument is correct *about heat-kernel functionals* and I do not dispute it: `P(σ)=Σ_k m_k e^{−σλ_k²}` and both its limbs (`a_n`, `ζ_D(s)`) are dimensionless functionals of the dimensionless multiset `{λ_k, m_k}`, so they return dimensionless numbers. But that is not the only substrate-internal datum, and §VII.BS itself says so. The framework's mass content (S99–S102) does NOT come from `λ_k` magnitudes — it comes from the **Peter-Weyl representation content** of `D_K`: the Casimir ladder `C₂(p,q)`, the dimensions `dim(p,q)`, the sector multiplicities. These are **integer-valued, exact, and τ-independent** — INV3-W3-4 found this explicitly: "the N(j)=7n verdict does NOT depend on the [λ²=C₂+3/4] bridge — it is decided on the EXACT dims/Casimirs (representation-theoretic, regulator/τ-independent)… Content lives in the rep content, not the eigenvalue magnitudes." So the substrate carries TWO kinds of internal data:
```
  (I)  Heat-kernel functionals of {λ_k}:        dimensionless by Reading-S's R1.S-1 argument. ✓ conceded.
  (II) Representation arithmetic of D_K:         exact integers {dim(p,q), C₂(p,q)·3, sector counts}.
                                                  NOT functionals of λ_k; NOT heat-kernel traces.
```
The §VII.BS shift-covariance is built over the **dagger-row a_n bundle** — channel (I). Its Half-A statement ("covariance rank = number of independent unprotected dimensional scales") is a theorem *about the dimensionful a_n observables*; the bundle-exhaustiveness result `ac1dbb28` augmented that bundle with one candidate second *scale* (`w₂ = m_H/v_ew`) and showed it factors through `M_KK`. **Neither operation touches channel (II).** A representation dimension `dim(3,0)=10` is not in the dagger-row bundle, is not a `w_k`, and is not an `Ô` heat-kernel kernel — it is an integer the substrate's Peter-Weyl decomposition *counts*. This is the load-bearing distinction of my entire case, and I state the substitution chain for it explicitly (per `math-scripts.md §"Double-Check Logic Before Compute"`):

```
  Claim: "the §VII.BS rank-1 certificate does NOT enclose the representation-arithmetic channel."
  Step 1:  §VII.BS Half-A domain = the dagger-row a_n bundle {O_i = w^{p_i}·Ô_i}, Ô_i = Φ_i({λ_k,m_k})  [registry §VII.BS, R1.S-1 limbs]
  Step 2:  ac1dbb28 augments with a candidate SCALE w₂ = m_H/v_ew; tests rank(Cov_aug)        [S103-NNU-BUNDLE-EXHAUSTIVENESS]
  Step 3:  dim(p,q), C₂(p,q), #sectors(p+q≤N) are INTEGER rep-theory invariants of su(3)        [Weyl dim formula; Casimir]
  Step 4:  ∂[dim(p,q)]/∂(ln w) = 0 and ∂[dim(p,q)]/∂λ_k = 0  — dim(p,q) is neither a scale nor a λ-functional
  Step 5:  ⇒ the representation integers are NOT rows of the dagger-bundle and NOT candidate w_k's
  Conclusion: §VII.BS proves rank-1 OVER CHANNEL (I) ∪ {w₂}; it is SILENT on whether channel (II) integers
              can seed a scale. The WALL is scoped to channel (I), exactly as Reading-S's own (a) concedes
              ("leaves open only a non-spectral or new-external route").
```
So Reading-S and I agree on more than the framing suggests: Reading-S's *own* (a) verdict says NNU "proves underivability by **any spectrum-only functional**… and leaves open only a non-spectral or new-external route." The representation-arithmetic channel (II) IS that non-spectral route. The question is not "does the WALL stand over channel (I)" — it does — but "does channel (II) populate a LIVE corridor." That is where my (b) audit lives, and where I must be honest.

---

**Sub-question (a): does NNU rank-1 PROVE in-principle underivability, or characterize the EXISTING import?**

My (a) verdict: **§VII.BS rank-1 + `ac1dbb28` PROVE (i) the import structure is rank-1 over the enumerated dagger-row a_n bundle, and (ii) no second independent *scale* hides in that bundle — but they do NOT prove in-principle underivability of `M_KK`'s numerical VALUE from a non-spectral substrate datum.** The planner's distinction is exactly right and I take the narrower-than-Reading-S side of it. Three points:

1. **Half-A is a rank theorem over a specific bundle, not a universal impossibility.** "Covariance rank = number of independent unprotected scales" is a statement about how many *scales* the dimensionful-observable bundle carries; `ac1dbb28` confirms that count is 1 and closes the second-*scale* escape. But "the bundle carries exactly one scale `w`" is logically distinct from "no substrate-internal datum can fix `w`'s value." A datum that fixes the *value* of the single scale `w` is not a *second* scale — it does not add a column to the Jacobian `J[i]=p_i`; it pins the one column's overall magnitude. Reading-S's R1.S-1 Step-4 ("`J[i]=p_i` is a single column ⇒ rank 1") is about *covariance structure under independent log-shifts*; a value-fixing relation `w = f(integers)` leaves the rank at 1 while removing the freedom. The two are orthogonal: rank-1 says "one knob"; value-derivation says "the knob is at a forced setting." §VII.BS proves the former; it is silent on the latter.

2. **The falsifier-(i) form is the precise test, and it is scoped to spectrum-only functionals.** falsifier-(i): "if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with NO imported GeV/seconds scale, the theorem is FALSIFIED (rank-0)." It RAN and FAIL-confirmed at S102 (`63698aa8`). I accept that result entirely — but note its scope: `Φ(D_K eigenvalues alone)` is a channel-(I) heat-kernel/spectral functional. The falsifier does NOT test a relation of the form `M_KK = Φ(dim(p,q), C₂, integer counts)` — a channel-(II) representation-arithmetic relation. So falsifier-(i) FAIL-confirms the WALL over channel (I) and is *silent* on channel (II), consistent with my structural chain above.

3. **What §VII.BS leaves genuinely open (my (a) answer).** Rank-1-over-the-dagger-bundle is COMPATIBLE with both: (P-WALL) `M_KK`'s value is irreducibly external; (P-LIVE) `M_KK`'s value is fixed by a channel-(II) integer relation that does not perturb the rank. §VII.BS discriminates neither. **My (a) verdict: NNU rank-1 characterizes the EXISTING import as rank-1 and closes the second-scale escape, but does NOT foreclose an independent integer-structure derivation of the scale's value — it is "rank-1 confirms the IMPORT structure but is silent on an INTEGER-VALUE derivation," precisely the SCOPED-INTERMEDIATE the planner names.**

---

**Sub-question (b): does Paasch's N(j)=7n + proton-cubic fix m_p (hence M_KK) without a hidden external scale? — the honest audit.**

This is where the binding discipline demands I engage against my own thesis, and I do. I ran the dimensional-input chain in Sage (exact). **The answer is: NO — the severed integer core fixes `m_p/m_e` (a RATIO), and an external mass anchor `m_e` re-enters at the integer-root → mass bridge.** Substitution chain (Sage-verified, this turn):

```
  Step 1:  N(j) = (m_j/m_e)^{2/3}   is DIMENSIONLESS — it is built FROM the ratio m_j/m_e.   [Paasch core]
  Step 2:  M(j) = N(j)^{1/2} = (m_j/m_e)^{1/3}   — also a dimensionless ratio.
  Step 3:  invert ⇒ m_j = m_e · M(j)^3 = m_e · N(j)^{3/2}.
  Step 4:  m_p = m_e · 150^{3/2} = m_e · 1837.117.   [Sage: 150^{3/2}=1837.1173; PDG m_p/m_e=1836.1527; rel-dev 5.3e-4]
  Step 5:  to land kg/GeV, m_e (a DIMENSIONFUL anchor) is REQUIRED. m_e ∉ {7,35,42,98,150}.
  Conclusion: the integer skeleton fixes the dimensionless RATIO m_p/m_e = 150^{3/2}; the ABSOLUTE m_p needs μ₀ = m_e.
              This is the §VII.BS structure O = w·Ô with Ô = N(p)^{3/2} (integer-root shape) and w = m_e (the scale).
```
I concede the structural point Reading-S anticipated: **stripped of LNH, Paasch's integers give a dimensionless mass-RATIO, not an absolute mass.** The Dirac-G~1/t scaffolding is exactly what *used* to furnish `μ₀` from cosmological inputs (a time-dependent `G` manufacturing a mass scale, the seconds/GeV bridge the seed flags as dead). Sever it, and `μ₀` becomes *undetermined*, not derived. I cannot claim the severed core fixes the absolute scale; on the literal (b) question — "fix `m_p` (hence `M_KK`) WITHOUT a hidden external scale" — the honest verdict is **the algebraic core is dimensionless-input → `m_p/m_e` RATIO, and a GeV anchor (`m_e`) re-enters at Step 5.** falsifier-(i)'s structure applies to the integer overlay just as it does to channel (I).

But three refinements keep this from collapsing into Reading-S's blanket WALL, and they define the SCOPED-INTERMEDIATE:

- **(b.1) The anchor `m_e` is itself a substrate observable, not a free external GeV.** The framework derives the charged-lepton hierarchy SHAPE from the Casimir ladder (S99–S102, EVOI rank-9b); the electron mass IS a substrate output `m_e = M_KK · Ô_e` for a dimensionless Casimir-overlap `Ô_e`. So Paasch's `μ₀ = m_e` is NOT an independent external GeV — it factors through `M_KK` exactly as `m_H` does (`ac1dbb28`). The honest consequence: Paasch's integers convert *one* scale (`m_e`, hence `M_KK`) into the *entire nucleon spectrum* via dimensionless integer-root ratios. That is a real structural win — it says the proton/electron mass ratio `1837.12` is forced by `N(p)=150` — but it is a statement about RATIOS riding on the single scale `M_KK`, which is **consistent with §VII.BS, not a refutation of it**. Paasch does not *derive* `M_KK`; he constrains every other mass *given* `M_KK`. This is the SCOPED-INTERMEDIATE: ratio-derivable (Paasch's genuine content), normalization-not (the one scale stays external).

- **(b.2) Where a Paasch integer is rigorously substrate-internal (n3=dim(3,0)=10), it produces a dimensionless COUPLING, not a scale — confirming the pattern, not breaking it.** I verified (Sage, this turn): `n3 = dim(3,0) = #sectors(p+q≤3) = T₄ = 10` is an EXACT three-way SU(3) identity (my sole-surviving Paasch-NCG bridge, A1), and it is *uniquely* the value that lands Paasch's `α = (1/n3²)(Ω/2)^{1/4}` at 0.855 ppm (n3=9 → 23% off; n3=11 → 17% off; INV3-W3-3 PASS). This is the ONE place a Paasch integer is provably a substrate representation dimension — and its output is `α` (dimensionless). Nothing in `(1/n3²)(Ω/2)^{1/4}` carries a GeV. **I take this as honest evidence that channel (II) integers, even where rigorously substrate-internal, produce dimensionless outputs.** It does NOT directly help me derive `M_KK` — it corroborates that the integer channel's natural output is dimensionless (ratios, couplings). The strongest *true* form of my thesis is therefore not "integers fix the scale" but "integers fix everything *except* the scale, exactly and from rep arithmetic."

- **(b.3) The proton integer fails the substrate-dimension test — INV3-W3-4 is honest about this and I will not over-read it.** INV3-W3-4 (INFO, `0b5f371f…`): of {7,35,42,98,150}, only **35=dim(1,4) and 42=dim(2,3)** are SU(3) dims (2-of-5); **7, 98, 150 are NOT**. The proton's `n3=150` is "6·25, NOT a dim, NOT 3·C₂, NOT triangular." So the proton-cubic integer is *not* cleanly a representation dimension the way `dim(3,0)=10` is in the α-formula. The genuine substrate structure W3-4 *did* find: **7 = 1 ⊕ 3 ⊕ 3̄ = dim(0,0)+dim(1,0)+dim(0,1)** — the cumulative count of the three lowest Peter-Weyl sectors ("7 is a mode-MULTIPLICITY unit, not an irrep dimension"); and the one striking near-coincidence **N(p)/N(K) = 150/98 = 1.530612, within 0.063% of φ_paasch = 1.531580** (Sage-confirmed; note 150/98 is a ratio of two *non-dim* integers, so it is a dimensionless ratio whatever its origin). My honest reading of W3-4: Paasch's integer scheme is a 2-of-5 partial overlay on SU(3) rep arithmetic, NOT a clean substrate derivation, and the proton row — the load-bearing one for `m_p` — is the one that misses. This *weakens* a strong-Reading-P claim that the integers themselves are substrate-forced; it leaves the genuine content as RATIOS (`m_p/m_e`, `150/98`).

**My (b) verdict (honest, against my own thesis where forced):** the severed algebraic core is genuinely **dimensionless-input → mass RATIOS** (`m_p/m_e = 150^{3/2}`, `N(p)/N(K) ≈ φ_paasch`), **NOT → absolute `m_p`**; a dimensionful anchor `μ₀ = m_e` re-enters at the integer-root → mass bridge (Step 5). That anchor is itself a substrate observable factoring through `M_KK` (b.1), so Paasch's machinery is a *consistency partner* of §VII.BS (it populates the `Ô` ratios richly) rather than a derivation of `w = M_KK`. The integer channel's natural output is dimensionless (b.2, b.3). **This is a SCOPED-INTERMEDIATE — "ratio-derivable, normalization-not" — not a LIVE corridor to the scale's value, and not the blanket spectrum-only WALL either.**

---

**Sub-question (c): the single compute that DECIDES it.**

INV3-W3-4 (the seed's candidate) has run and decided the *integer-overlay* sub-question: 2-of-5, proton row fails, surviving content is dimensionless ratios. I do NOT propose re-running it; it settled that Paasch's integers are a partial rep-arithmetic overlay yielding ratios, not a scale. What it did NOT test — and what would discriminate my SCOPED-INTERMEDIATE from Reading-S's blanket WALL — is whether the **representation-arithmetic channel (II) can fix a DIMENSIONFUL scale at all**, i.e. whether `M_KK` itself admits a representation-theoretic relation distinct from a heat-kernel functional. I name the decisive forward gate:

> **Decisive gate (Reading-P framing): a channel-(II) scale-fixing existence test, distinct from falsifier-(i).** Pre-register whether a *dimensionful* `M_KK` admits the form `M_KK = M_Pl · Φ(dim(p,q), C₂(p,q), integer sector-counts)` — i.e. a relation in which the *only* dimensionful input is a single anchor (`M_Pl`, the gravitational scale already in the framework's `1/G_induced ~ f₂·M_KK²·a₂` matching) and the modulating factor `Φ` is built PURELY from SU(3) representation integers, NOT from `{λ_k}` heat-kernel functionals. This is sharply distinct from falsifier-(i) (which tests `Φ({λ_k} alone)`): it tests the integer channel that §VII.BS does not enclose.
>
> - **PASS** (an integer-arithmetic `Φ` reproduces `M_KK/M_Pl = 7.4287e16/1.22e19 = 6.09e-3` to a pre-registered tolerance, with the anchor being a *single* gravitational scale and no second free GeV) → channel (II) populates a LIVE corridor; M_KK-DERIVATION flips to "value-derivable-given-one-anchor."
> - **FAIL/INFO** (no integer `Φ` reproduces the ratio without a second tuned input, OR the only relations that work reduce to `1/G_induced` matching read in reverse — i.e. the anchor is doing all the work) → the SCOPED-INTERMEDIATE hardens toward Reading-S's WALL: even channel (II) cannot fix the scale; `M_KK` is irreducibly external and Paasch's content is RATIOS-only.

This gate is decisive because it tests the *exact* channel my structural chain isolates (representation arithmetic) and that §VII.BS leaves open by its own (a)-scope. It is medium effort: the L12 cache + Weyl-dim/Casimir formulae are in hand (INV3-W3-4 already built the 90-sector table); the new content is an integer-`Φ` search against the single ratio `M_KK/M_Pl` with a look-elsewhere correction (the W3-2 corpus already warns the `sin/cos` algebraic family is DENSE near φ/fN — so the gate MUST carry a pre-registered look-elsewhere penalty, else any 2% hit is a construction artifact).

---

**R1 summary (Reading-P).** The substrate carries TWO internal data channels: (I) heat-kernel functionals of `{λ_k}` — dimensionless, conceded to Reading-S; (II) exact SU(3) representation arithmetic — integer-valued, τ-/regulator-independent, and the channel Paasch's integers and the framework's own Casimir-ladder masses actually live in. §VII.BS rank-1 + `ac1dbb28` prove the WALL over channel (I) and close the second-*scale* escape, but (a) are silent on whether a channel-(II) integer relation can fix the single scale's VALUE — rank-1 says "one knob," not "the knob is free." On (b), I concede honestly: stripped of dead LNH, Paasch's `N(j)=7n` + proton-cubic give the dimensionless RATIO `m_p/m_e = 150^{3/2}` (Sage: 1837.12 vs PDG 1836.15, 5.3e-4), NOT absolute `m_p`; a dimensionful anchor `μ₀ = m_e` re-enters — though `m_e` is itself a substrate observable factoring through `M_KK` (b.1), so Paasch is a consistency-partner enriching the `Ô` ratios, not a scale-derivation; and where a Paasch integer IS rigorously substrate-internal (`n3 = dim(3,0) = 10`, exact, uniquely sub-ppm in α) it outputs a dimensionless COUPLING (b.2), while the proton integer `150` fails the substrate-dimension test (b.3, INV3-W3-4: 2-of-5). On (c), the decisive gate is a channel-(II) scale-fixing existence test (`M_KK = M_Pl · Φ(integers)`), sharply distinct from falsifier-(i)'s `Φ({λ_k})` and carrying a mandatory look-elsewhere penalty. **My pinned position: SCOPED-INTERMEDIATE — "ratio-derivable, normalization-not"; §VII.BS confirms the rank-1 IMPORT structure but is silent on an integer-VALUE derivation of the one scale, and the integer channel's natural output is demonstrably dimensionless (ratios, couplings).** I expect Reading-S to argue channel (II) collapses into (I) (integers ARE determined by the spectrum's degeneracy structure) and that `m_e`-factoring-through-`M_KK` IS the WALL restated; I will meet both in R2 — the first on the τ-/regulator-independence of rep dimensions (they are NOT heat-kernel functionals), the second by conceding it sharpens the verdict to "one anchor, integer-forced ratios" rather than overturning the WALL-vs-LIVE question, which the (c) gate alone decides.

---

## Round 2 — rebut the opponent's R1 + converge on a STRUCTURAL VERDICT

### R2 · Turn A — spectral-geometer (rebut Reading-P R1; move toward convergence)

**Where we already agree (stated up front, so the residual disagreement is sharp).** Reading-P's R1 conceded (b) honestly and against its own thesis: stripped of dead LNH, the Paasch core gives `m_p/m_e = 150^{3/2}` (Sage, this turn: `1837.1173` vs PDG `1836.15267`, rel-dev `5.276e-4`) as a dimensionless RATIO, with `μ₀ = m_e` re-entering at the integer-root→mass bridge — the `O = w·Ô` structure with `Ô = N(p)^{3/2}`, `w = m_e`. We agree the WALL stands over channel (I) (heat-kernel functionals of `{λ_k}`). We agree the integer channel's *demonstrated* outputs are dimensionless (the `n3=dim(3,0)=10` α-formula, 0.855 ppm, outputs a coupling; `150/98 = 1.530612` vs `φ_paasch=1.531580`, rel-dev `6.319e-4`, is a ratio of two non-dim integers). So this is not a WALL-vs-LIVE clash; it is a fight over the *scope* of the intermediate and over whether Reading-P's channel (II) is a genuinely-open scale route or the ratio-wall restated. I will argue the latter, on three heads.

---

**Rebuttal head 1 — channel (II) does NOT escape the rank-1 obstruction; it COLLAPSES into channel (I) at the value level. The representation integers ARE recoverable from the spectrum.** Reading-P's load-bearing chain (R1.P Steps 3–4) asserts `∂[dim(p,q)]/∂λ_k = 0` and `∂[dim(p,q)]/∂(ln w) = 0`, concluding the rep integers are "neither a λ-functional nor a scale," hence outside the §VII.BS net. The *derivative* statement is true and I do not contest it — `dim(p,q)` is a locally constant integer, so of course its derivative vanishes. But Reading-P draws the wrong inference from it. The question for M_KK-DERIVATION is not "is `dim(p,q)` a smooth functional of `λ_k`" (it is not — it is a discrete *label*); the question is "does the eigenvalue multiset `{λ_k, m_k}` DETERMINE the representation content `{dim(p,q), C₂(p,q)}`." And it does — exactly. I verified this from the governing structure (Sage, this turn):

```
  Claim: "the representation integers are recovered FROM the spectrum, so channel (II) is not
          an independent datum — it is the index set over which channel (I) sums."
  Step 1:  D_K is block-diagonal by Peter-Weyl: D_K = ⊕_{(p,q)} D_{(p,q)} on V_{(p,q)}⊗C^16.   [S105; PROVEN]
  Step 2:  the trivial-sector eigenvalue (τ=0 bi-invariant, S105 closed form) reads the Casimir DIRECTLY:
              |λ_trivial(p,q)|² = (1/6)·C₂(p,q) + 1/4    ⇒    C₂(p,q) = 6·(|λ_trivial|² − 1/4).   [Sage, exact]
           Verified: (0,0)→0, (1,0)→4/3, (1,1)→3, (3,0)→6, (2,1)→16/3 — each the exact C₂.
  Step 3:  the sector DEGENERACY in {m_k} (the multiplicity of each block-eigenvalue) is dim(p,q)·dim(μ);
           the Weyl character / Casimir-ordering of the multiplicities recovers dim(p,q) as an integer count.
  Step 4:  ⇒ {λ_k, m_k}  ⟶  {C₂(p,q), dim(p,q)}  is a WELL-DEFINED MAP (the spectrum determines the rep content).
  Conclusion: channel (II) is not orthogonal to channel (I). It is the INDEX SET of the heat-kernel sum
              P(σ) = Σ_{(p,q)} dim(p,q)·Σ_μ dim(μ)·e^{−σ|λ(p,q,μ)|²}. The integers are READ OFF the spectrum.
              Anything you can compute from {dim(p,q), C₂(p,q)} you can compute from {λ_k, m_k} — including
              the same dimensionless output. The map carries NO units in either direction.
```

This is the structural collapse. Reading-P is right that `dim(p,q)` is "τ-/regulator-independent" — but that independence cuts the *other* way for the value question. The reason `dim(p,q)` is τ-independent is that it is a *topological/representation* label of the FIXED algebra `A_K`; it does not flex when you deform the metric precisely because it carries no metric information — and a quantity that carries no metric information cannot carry a metric SCALE. τ-independence is not evidence of an escape hatch to a dimensionful datum; it is the signature of a quantity that has been *stripped* of all dimensionful content. INV3-W3-4 says this in its own words — "the verdict is decided on the EXACT dims/Casimirs… the absolute eigenvalue values are immaterial." Exactly: the integers survive the removal of the eigenvalue *magnitudes* (where any scale would have to live). A datum that is invariant under throwing away the only scale-bearing information cannot subsequently manufacture a scale. So the rank-1 obstruction is not silent on channel (II); channel (II) sits *strictly inside* the dimensionless content the obstruction certifies. The §VII.BS Jacobian `J[i]=p_i` has columns indexed by *scales*; a representation integer adds no column because it has `p=0` (it is `w^0·(integer)` — pure `Ô`). Reading-P's own (a.1) move — "a value-fixing relation `w=f(integers)` leaves the rank at 1 while removing the freedom" — is correct that it does not add a *column*; but the reason it does not add a column is that `f(integers)` is itself a *dimensionless* number, and a dimensionless number cannot equal a dimensionful `w`. Which is head 2.

---

**Rebuttal head 2 — Reading-P's own (b) concession IS the value-question wall restated. The ratio/normalization split is the whole answer.** Reading-P's (b.1) is the decisive admission, and I want to make its consequence explicit rather than let it read as a "real structural win" that softens the verdict. (b.1) says: the anchor `μ₀ = m_e` is itself a substrate observable `m_e = M_KK · Ô_e`, so it "factors through M_KK exactly as m_H does (`ac1dbb28`)." I agree completely — and that is precisely the wall, not a mitigation of it. Trace the substitution chain to its fixed point:

```
  Claim: "Paasch + (b.1) does not derive M_KK; it derives the RATIO m_p/m_e and re-expresses m_p in units of M_KK,
          leaving M_KK exactly as un-fixed as before."
  Step 1:  m_p = m_e · N(p)^{3/2}                        [Paasch core; N(p)=150]            [R1.P Step 3]
  Step 2:  m_e = M_KK · Ô_e   (b.1: m_e is a substrate observable, dimensionless Ô_e times the one scale)
  Step 3:  substitute:  m_p = M_KK · Ô_e · N(p)^{3/2} = M_KK · Ô_p,   with  Ô_p ≡ Ô_e · 150^{3/2}  (dimensionless)
  Step 4:  ⇒ m_p = M_KK · Ô_p   — the SAME O = w·Ô factorization, w = M_KK, Ô_p a dimensionless spectral×integer shape.
  Conclusion: Paasch + (b.1) converts the PAIR (m_e, m_p) into (M_KK·Ô_e, M_KK·Ô_p). Every mass is M_KK times a
              dimensionless number. M_KK itself never appears on the RHS of any of these as an OUTPUT — it is the
              common multiplicative w on the LEFT of all of them. The machinery enriches the Ô's; it never touches w.
```

So (b.1), far from opening a corridor, *tightens* §VII.BS: it shows the Paasch integers populate even MORE rows of the `O = w·Ô` bundle (the entire nucleon spectrum), all sharing the single `w = M_KK`. That is bundle-exhaustiveness `ac1dbb28` getting *stronger*, not a counterexample to it. Reading-P names this "consistency-partner," and I accept the term — but the honest reading of a consistency-partner that adds rows all at `w = M_KK` is that it is *additional confirmation of the rank-1 wall*, not a route around it. The value question — "what fixes the numerical value of `w`?" — is untouched: Paasch tells you `m_p = 1837.12 · m_e`, and asks `m_e`, and `m_e` answers `M_KK · Ô_e`, and you ask `M_KK`, and the machinery is silent. The regress terminates at the one external pin. **That regress IS the wall for the value question, stated in Paasch's own variables.**

---

**Rebuttal head 3 — `M_KK = M_Pl · Φ(integers)` does NOT import a scale-free `Φ`; it RELOCATES the scale to M_Pl, which is itself defined through M_KK. The (c) gate as Reading-P frames it cannot populate a LIVE corridor.** This is the cleanest disposition, and it answers the planner's third sub-prompt to me directly. Reading-P's decisive gate pre-registers `M_KK = M_Pl · Φ(dim, C₂, counts)` with M_Pl "the gravitational scale already in the framework's `1/G_induced ~ f₂·M_KK²·a₂` matching." Two fatal problems, both structural:

```
  Claim: "M_KK = M_Pl·Φ(integers) does not derive M_KK's value scale-free; it is circular and the scale lives in M_Pl."
  Step 1:  the framework's gravitational anchor is the induced-gravity matching:  1/G_induced ~ f₂·M_KK²·a₂   [§VII.BS clause (d)]
  Step 2:  M_Pl² ~ 1/G_induced ~ f₂·a₂·M_KK²   ⇒   M_Pl = M_KK · √(f₂·a₂·…) ≡ M_KK · Ψ,  Ψ dimensionless (a₂, f₂ ∈ Ô). [substitute]
  Step 3:  substitute into the proposed relation:  M_KK = M_Pl·Φ(int) = M_KK·Ψ·Φ(int)
  Step 4:  ⇒ Ψ·Φ(int) = 1   — a DIMENSIONLESS CONSTRAINT among Ô's and integers, NOT a determination of the scale M_KK. [divide by M_KK]
  Conclusion: M_Pl is not an INDEPENDENT scale the substrate possesses; it is M_KK dressed by dimensionless (a₂,f₂).
              "M_KK = M_Pl·Φ(int)" is M_KK = M_KK·(dimensionless) — it cancels to a constraint on the dimensionless
              factors and fixes NO absolute value. The scale on the RHS (M_Pl) is the SAME un-fixed w in disguise.
```

So if you READ M_Pl as the framework's own induced-gravity scale (the substrate-first reading the binding discipline requires — `D_K → a₂ → g_M → 1/G_induced`), then `M_KK = M_Pl·Φ` is circular: it relates the one scale to itself through dimensionless factors and determines nothing. The only way to break the circle is to read M_Pl as an EXTERNAL, independently-known gravitational scale (the lab value `1.22e19 GeV`) — but that is importing an external GeV anchor, which is exactly the falsifier-(i) failure mode transported to the integer channel. Reading-P cannot have it both ways: substrate-internal M_Pl ⇒ circular (no value fixed); external M_Pl ⇒ imported scale (wall confirmed, just relocated from M_KK-direct to M_Pl-direct). I verified the dimensionless residue (Sage): `Φ_needed = M_KK/M_Pl = 6.089e-3`, a pure dimensionless number — confirming `Φ` carries no units and the scale must come from M_Pl. **A channel-(II) `Φ(integers)` is, by construction, dimensionless (head 1); multiplying it by ANY anchor just renames the anchor as the scale.** There is no scale-free `Φ` because there is no scale-free integer.

And the look-elsewhere problem makes even a numerical "hit" non-evidential. I ran Reading-P's own coarse search (Sage): over an 81-element integer pool (`dim(p,q)`, `3·C₂` for `p+q≤12`), 3240 ordered pairs, **2 land within 2% of `6.089e-3`** by chance. So a 2%-tolerance integer-`Φ` match against a single target ratio is a construction artifact at exactly the density Reading-P's own R1 warned about (the W3-2 `sin/cos`/`φ/fN` density corpus). The gate, run honestly with the mandatory look-elsewhere penalty, is structurally pre-disposed to FAIL/INFO — not because the integers are wrong, but because a dimensionless factor times a relocated scale is not a derivation.

---

**Concession I carry forward (continuing the W2-1/W2-2 discipline of pre-loading against my own corridors).** I will NOT over-claim head 1 into "the spectrum determines literally everything." Reading-P's τ-/regulator-independence point has a *legitimate* and important consequence that I concede in full and that the verdict must preserve: the representation arithmetic is the substrate's most ROBUST internal content — it survives metric deformation, regulator choice, and truncation-magnitude artifacts (it is what made the `n3=10` α-formula sharp where my own `d_s`-flow scale-transport FAILED at INV3-W2-1, `c31277d5`). That robustness is real and is a Reading-P contribution to the converged picture. But robustness-of-the-dimensionless-content is the *opposite* of a scale source: the more τ-/regulator-invariant a datum is, the more thoroughly it has been purged of the metric-magnitude information a scale would require. So I concede the integers are the framework's bedrock for the `Ô`'s — and that very fact pins them to the dimensionless side. There is exactly ONE thing I do NOT close, and it is the same narrow door I named in R1·(c): a genuinely *non-spectral, intrinsically-dimensionful* substrate datum — a cocycle norm or holonomy carrying its own units, NOT a representation integer and NOT a `Φ({λ_k})`. The representation-arithmetic channel does not populate that door (it is dimensionless, head 1); my R1 falsifier-(i)-on-the-non-spectral-channel gate is what would. That door has had zero runs land in it.

---

**The structural verdict I SUPPORT (for the paasch analyst to finalize in R2·Turn B).** **SCOPED-INTERMEDIATE**, with the scope pinned sharply: **"RATIO-derivable, NORMALIZATION irreducibly external."** Concretely —

- **(a)** §VII.BS rank-1 + `ac1dbb28` prove M_KK underivable by any spectrum-only functional `Φ({λ_k})` AND close the second-*scale* escape. The representation-arithmetic channel (II) does NOT escape this: it is recovered from `{λ_k, m_k}` (head 1, `C₂ = 6(λ²−1/4)`), sits at `p=0` in the shift-covariance (pure `Ô`), and adds no Jacobian column. The "rank-1 is silent on integer-VALUE derivation" reading is correct only in the trivial sense that rank-1 does not forbid a dimensionless *constraint* among integers — but no such constraint fixes a dimensionful value (head 3). So (a) lands at: **rank-1 confirms the import structure AND forecloses the integer-VALUE route, because the integer route is dimensionless by construction**; the only door left open is non-spectral-with-units, which channel (II) does not populate.
- **(b)** The severed Paasch core gives the dimensionless RATIO `m_p/m_e = 150^{3/2}` (rel-dev `5.276e-4`), NOT absolute `m_p`. (b.1) — `m_e = M_KK·Ô_e` — does not soften this; it RESTATES the wall in Paasch's variables (head 2): every mass becomes `M_KK·Ô`, M_KK never an output. Paasch is a consistency-partner that ENRICHES the `Ô` bundle and thereby STRENGTHENS bundle-exhaustiveness — it is not a scale derivation. The proton row `n3=150` is not even a clean substrate dimension (INV3-W3-4: 2-of-5; `150 = 6·25`, not a dim/3·C₂/triangular), so the strongest-Reading-P "integers are substrate-forced" claim is independently weakened on its load-bearing row.
- **(c)** Reading-P's `M_KK = M_Pl·Φ(integers)` gate, as framed, cannot decide LIVE because it is circular (M_Pl = M_KK·Ψ ⇒ Ψ·Φ = 1, fixes no value) under the substrate-first reading, OR imports an external GeV under the lab-value reading; and a 2%-tolerance integer-`Φ` against one ratio is a look-elsewhere artifact (Sage: 2/3240 hits by chance). The decisive forward gate that actually discriminates the *remaining* open door is my R1 framing: **a non-spectral-scale existence scan** — pre-register `M_KK = Φ(non-spectral substrate invariant with intrinsic units)`, where the candidate is a cocycle-norm/holonomy carrying units (NOT a representation integer, NOT a `Φ({λ_k})`, NOT `M_Pl·Φ` which relocates rather than fixes). PASS ⇒ §VII.BS falsified at rank-0, M_KK-DERIVATION flips LIVE; FAIL/INFO ⇒ the SCOPED-INTERMEDIATE hardens to "normalization irreducibly external," and the §6.3 seconds-valued `a(t)` dies while the SHAPE survives.

The substrate-first arrow holds throughout: `D_K eigenvalues → {C₂, dim} read off the multiplicities → dimensionless moments/ratios/integers (Ô) → measurement`. The one dimensionful prefactor `w = M_KK` is the head-of-arrow datum the spectrum cannot return, and the representation integers — being precisely the τ-/regulator-invariant (hence metric-magnitude-free) content — cannot return it either. I hand to R2·Turn B a convergence on SCOPED-INTERMEDIATE; our only residual difference is that I read the intermediate's scope as "the integer channel is INSIDE the dimensionless wall, not a separate open corridor," whereas Reading-P held it merely "silent." Head 1's `C₂ = 6(λ²−1/4)` is what closes that gap: the integers are read off the spectrum, so whatever the spectrum cannot do, the integers cannot do either.

### R2 · Turn B — paasch-mass-quantization-analyst (rebut Reading-S R1; converge + WRITE the §Structural Verdict, §Wrap-Up, §Effected In-Session, §Carry-Forward Computations)

**Where we converge, stated up front.** We converge on **SCOPED-INTERMEDIATE — "RATIO-derivable, NORMALIZATION irreducibly external."** I do not manufacture a WALL-vs-LIVE clash that the arithmetic has dissolved. My R1 held channel (II) (SU(3) representation arithmetic) "merely silent" with respect to the §VII.BS rank-1 net; spectral-geometer's R2 head 1 sharpens that to "channel (II) sits *inside* the dimensionless wall, because the integers are read off the spectrum." I accept the sharpening — it is forced by an identity I re-verified independently this turn — and the residual difference between us is now a matter of *scope-phrasing*, not of verdict. I take each of the three R2 heads in order, with explicit substitution chains, then state my final position.

---

**Head 1 — I CONCEDE: the `C₂ = 6(|λ_trivial|² − 1/4)` recovery holds Sage-exact, so channel (II) is NOT orthogonal to the spectrum for the VALUE question.** This is the load-bearing arithmetic of the convergence, so I did not take it on citation — I re-derived it from the standard SU(3) Casimir (Sage, this turn, exact):

```
  Claim (spectral-geometer head 1): "the representation integers are recovered FROM {λ_k, m_k}."
  Step 1:  standard SU(3) quadratic Casimir:  C₂(p,q) = (1/3)(p² + q² + p·q) + (p+q).            [rep theory]
  Step 2:  trivial-sector eigenvalue (S105 closed form):  |λ_trivial(p,q)|² = (1/6)·C₂(p,q) + 1/4.  [substrate]
  Step 3:  invert:  C₂(p,q) = 6·(|λ_trivial(p,q)|² − 1/4).
  Step 4:  Sage symbolic check: substitute Step 2 into Step 3 ⇒ recover(forward) = c₂ EXACTLY (identity).
           Quoted values reproduced bit-exact: (0,0)→0, (1,0)→4/3, (1,1)→3, (3,0)→6, (2,1)→16/3. ✓
  Conclusion: the Casimir — hence dim(p,q) via the Weyl-character multiplicity count — is a function of the
              eigenvalue multiset. The map {λ_k, m_k} → {C₂(p,q), dim(p,q)} is well-defined and carries NO units
              in either direction. Channel (II) is the INDEX SET over which the channel-(I) heat-kernel sum runs;
              it is not an independent datum that could smuggle a scale the heat-kernel functionals cannot.
```

My R1 Steps 3–4 asserted `∂[dim(p,q)]/∂λ_k = 0`, and that derivative statement is *true* — but spectral-geometer correctly diagnosed that I drew the wrong inference from it. `dim(p,q)` is a locally-constant *integer label*, so its derivative vanishes trivially; the value question is not "is dim a smooth functional of λ" but "does `{λ_k, m_k}` DETERMINE the rep content," and the `C₂ = 6(λ²−1/4)` identity answers yes, exactly. The vanishing derivative was a red herring: the integers are recovered by the *discrete* degeneracy/ordering structure of the multiset, not by differentiating it. **I retract the R1 claim that the representation channel is "orthogonal" to the spectrum for the VALUE question.** It is the spectrum's own index set. Whatever a spectrum-only functional cannot do (manufacture a GeV), an integer read off that spectrum cannot do either. The WALL over the value question therefore reaches channel (II) — not by enclosing it in the dagger-row bundle, but by the deeper route that the integers ARE the spectrum's discrete shadow.

There is exactly one consequence of head 1 I want to *keep* on the Reading-P side of the ledger, and it is a sharpening, not a dispute: the very property that places the integers inside the dimensionless wall — τ-/regulator-invariance — is *also* what makes them the framework's most ROBUST internal content. spectral-geometer conceded this in full (the W2-1 `d_s`-flow scale-transport FAILED, `c31277d5`, where the `n3=10` integer α-formula was sub-ppm sharp, INV3-W3-3 PASS). So the converged picture is: the integers are the bedrock of the `Ô` predictions precisely *because* they are metric-magnitude-free — which is the same fact that disqualifies them as a scale source. Robustness and dimensionlessness are two faces of one coin. That is a Reading-P contribution to the verdict's positive content, and it is fully consistent with the WALL on the value question.

---

**Head 2 — I CONCEDE: my (b.1) is the value-wall restated in Paasch's variables, not a softening of it.** I called `m_e = M_KK·Ô_e` a "real structural win." spectral-geometer's head-2 chain shows the win is real but its *direction* is confirmation-of-the-wall, not a corridor around it. I re-state the fixed-point chain and accept its terminus:

```
  Step 1:  m_p = m_e · N(p)^{3/2}                              [Paasch core; N(p)=150]         [R1.P Step 3]
  Step 2:  m_e = M_KK · Ô_e                                    [b.1: m_e is a substrate output]
  Step 3:  substitute:  m_p = M_KK · (Ô_e · 150^{3/2}) = M_KK · Ô_p,   Ô_p dimensionless.
  Step 4:  ⇒ the PAIR (m_e, m_p) = (M_KK·Ô_e, M_KK·Ô_p). M_KK is the common multiplicative w on the LEFT of every
           mass; it never appears on the RHS as an OUTPUT.
  Conclusion: Paasch + (b.1) populates MORE rows of the O = w·Ô bundle (the whole nucleon spectrum), all at the
              single w = M_KK. The value regress — "what fixes w?" — terminates at the one external pin, in
              Paasch's own variables. The machinery enriches the Ô's; it never touches w.
```

I accept the reframing: Paasch's machinery is a **consistency-partner that STRENGTHENS bundle-exhaustiveness** (`ac1dbb28`), adding rows that all share `w = M_KK`, rather than a counterexample to it. The honest content of Paasch's program, severed from the dead Dirac-G~1/t (LNH) scaffolding that *used* to furnish μ₀, is: **given the one scale, the integer skeleton forces the entire nucleon-to-electron mass spectrum via dimensionless integer-root ratios** (`m_p/m_e = 150^{3/2} = 750√6 = 1837.117`, Sage-exact, rel-dev `5.25e-4` vs PDG `1836.153`). That is a genuine RATIO result and it is real; it is not, and after head 2 I do not claim it to be, a derivation of `w`. And on the load-bearing proton row the overlay is not even clean: INV3-W3-4 (`0b5f371f`) found `150 = 6·25` is NOT an SU(3) dimension, NOT `3·C₂`, NOT triangular (2-of-5 on the substrate-dimension test: only `35=dim(1,4)` and `42=dim(2,3)` are dims; `7, 98, 150` are not — `7 = dim(0,0)+dim(1,0)+dim(0,1)` is a mode-multiplicity unit). So the strongest-Reading-P "the integers are themselves substrate-forced" claim is independently weakened exactly where `m_p` needs it. What survives is `N(p)/N(K) = 150/98 = 1.530612` within `6.35e-4` of `φ_paasch = 1.531580` (Sage-confirmed this turn) — a ratio of two non-dim integers, dimensionless whatever its origin.

---

**Head 3 — I CONCEDE: `M_KK = M_Pl·Φ(integers)` RELOCATES the scale; it does not fix it. My R1·(c) gate, as framed, cannot populate a LIVE corridor.** spectral-geometer's head-3 chain is decisive and answers the substrate-first binding discipline directly:

```
  Step 1:  framework gravitational anchor:  1/G_induced ~ f₂·M_KK²·a₂.                          [§VII.BS clause (d)]
  Step 2:  M_Pl² ~ 1/G_induced ⇒ M_Pl = M_KK·√(f₂·a₂·…) ≡ M_KK·Ψ,  Ψ dimensionless (a₂,f₂ ∈ Ô).
  Step 3:  substitute into the proposed relation:  M_KK = M_Pl·Φ(int) = M_KK·Ψ·Φ(int).
  Step 4:  ⇒ Ψ·Φ(int) = 1 — a DIMENSIONLESS CONSTRAINT among Ô's and integers; it fixes NO absolute value.
  Conclusion: under the substrate-first reading the binding discipline REQUIRES (M_Pl is the framework's own
              induced-gravity scale, D_K → a₂ → g_M → 1/G_induced), the relation is CIRCULAR — M_KK = M_KK·(dimensionless).
              The only way to break the circle is to read M_Pl as the EXTERNAL lab value 1.22e19 GeV — which is
              importing a GeV anchor, the falsifier-(i) failure mode transported to the integer channel.
```

I cannot have it both ways: substrate-internal `M_Pl` ⇒ circular (fixes nothing); external `M_Pl` ⇒ imported scale (the wall relocated from M_KK-direct to M_Pl-direct). And the look-elsewhere problem makes even a numerical "hit" non-evidential — I ran the density myself (Sage, this turn): over the 81-element integer pool `{dim(p,q), 3·C₂(p,q) : p+q≤12}`, **2 of 6561 ordered pairs land within 2% of `M_KK/M_Pl = 6.085e-3` by chance** (density `2/6561 ≈ 3.0e-4`). A 2%-tolerance integer-`Φ` against a single target ratio is a construction artifact at exactly the density my own R1 warned about (the W3-2 `sin/cos`/`φ/fN` corpus). My R1·(c) gate is structurally pre-disposed to FAIL/INFO — not because the integers are wrong, but because a dimensionless factor times a relocated scale is not a derivation. **I withdraw `M_KK = M_Pl·Φ(integers)` as the decisive gate** and adopt the non-spectral-scale existence scan (the R1·Turn-A framing, which the planner flagged and which my R1 had already gestured toward) as the single forward gate, pinned below.

---

**My final position.** **SCOPED-INTERMEDIATE — "RATIO-derivable, NORMALIZATION irreducibly external."** The three concessions are forced by arithmetic I verified independently (head 1 Sage-exact identity; head 2 fixed-point regress; head 3 circularity + look-elsewhere `2/6561`), and I do not retreat from them. Where I refine spectral-geometer's phrasing — and this is a sharpening the verdict should preserve, not a residual disagreement — is the *positive* content: the representation-arithmetic channel is not merely "inside the wall" as dead weight; it is the framework's most robust *dimensionless* content (τ-/regulator-invariant), and Paasch's severed core is a genuine RATIO-fixing consistency-partner that enriches the `Ô` bundle (every nucleon mass forced to `M_KK·Ô` by integer-root ratios) and thereby STRENGTHENS bundle-exhaustiveness. The wall is on the *normalization*; the integers do real work on the *ratios*. The one door neither of us closes is non-spectral-with-intrinsic-units (a cocycle-norm / holonomy carrying a GeV, NOT a representation integer, NOT `Φ({λ_k})`, NOT `M_Pl·Φ`) — that door has had zero runs land in it, and the forward gate is its falsifier-(i)-on-the-non-spectral-channel existence scan.

I now write the four closing sections.

---

## Structural Verdict

**M_KK-DERIVATION: SCOPED-INTERMEDIATE — "RATIO-derivable, NORMALIZATION irreducibly external."**

Both advocates converge. The substrate determines every dimensionless shape `Ô` (ratios, orderings, tilts, growth shapes, the tracking exponent n=2, the integer-root mass ratios, the dimensionless couplings) exactly and from first principles; it does NOT determine the single dimensionful normalization `w = M_KK`. The one un-fixed scale is the complete dimensionful content of the framework (§VII.BS rank-1, STAGE-3-PERMANENT). This is neither a blanket PROVEN-IMPOSSIBLE WALL (the framework's entire dimensionless physics IS substrate-derived, richly so) nor a LIVE CORRIDOR (no run has populated a substrate-internal datum that carries a scale). It is the precisely-scoped intermediate: **the SHAPE is derived; the SIZE is one external pin.**

**Per-sub-question findings (converged):**

- **(a) — §VII.BS rank-1 forecloses the integer-VALUE route, not merely the spectrum-only route.** Rank-1 + bundle-exhaustiveness (`ac1dbb28`) prove `M_KK` is underivable by any spectrum-only functional `Φ({λ_k})` AND close the second-*scale* escape (`m_H = M_KK·Ô`, second relative singular value `1.07e-17 ≈ 0`). The representation-arithmetic channel does NOT escape this: the integers are RECOVERED from the eigenvalue multiset via the Sage-exact identity `C₂(p,q) = 6(|λ_trivial(p,q)|² − 1/4)` (verified both turns; `(0,0)→0, (1,0)→4/3, (1,1)→3, (3,0)→6, (2,1)→16/3`), so `{λ_k, m_k} → {C₂, dim(p,q)}` is a well-defined units-free map. Channel (II) is the INDEX SET of the channel-(I) heat-kernel sum, sitting at `p=0` in the shift-covariance (pure `Ô`, adds no Jacobian column). Rank-1's silence on an "integer-VALUE derivation" is only the trivial silence that it does not forbid a *dimensionless constraint among integers* — but no dimensionless constraint fixes a dimensionful value. **(a) lands: rank-1 confirms the import structure AND forecloses the integer route, because the integer route is dimensionless by construction.** The only door left open is non-spectral-with-intrinsic-units, which channel (II) does not populate.

- **(b) — the severed Paasch core gives a RATIO, not absolute `m_p`; (b.1) restates the wall, it does not soften it.** Stripped of the observationally-dead Dirac-G~1/t (LNH) scaffolding, `N(j)=7n` + `M(j)=√N(j)` + the proton-cubic give `m_p/m_e = 150^{3/2} = 750√6 = 1837.117` (Sage-exact, rel-dev `5.25e-4` vs PDG `1836.153`) — a dimensionless integer-root RATIO. A dimensionful anchor `μ₀ = m_e` re-enters at the integer-root→mass bridge; the LNH scaffolding is exactly what used to furnish it. Because `m_e = M_KK·Ô_e` is itself a substrate output (b.1), the whole nucleon spectrum becomes `m_j = M_KK·Ô_j` — `M_KK` is the common multiplicative `w` on the LEFT of every mass, never an OUTPUT on the RHS. Paasch is therefore a **consistency-partner that STRENGTHENS bundle-exhaustiveness** (more `O = w·Ô` rows, all at one `w`), not a scale derivation. The load-bearing proton integer `150 = 6·25` is not even a clean SU(3) dimension (INV3-W3-4: 2-of-5; `35, 42` are dims, `7, 98, 150` are not), so the strongest-Reading-P "integers are substrate-forced" claim is independently weakened on the row `m_p` needs.

- **(c) — Reading-P's `M_KK = M_Pl·Φ(integers)` gate cannot decide LIVE; the decisive gate is a non-spectral-scale existence scan.** Under the substrate-first reading the binding discipline requires (`M_Pl = M_KK·Ψ` via `1/G_induced ~ f₂·M_KK²·a₂`, clause (d)), `M_KK = M_Pl·Φ(int)` collapses to `Ψ·Φ(int) = 1` — a dimensionless constraint that fixes no value (circular: `M_KK = M_KK·dimensionless`); under the lab-value reading it imports an external GeV (falsifier-(i) failure relocated). And it is non-evidential by look-elsewhere: `2 of 6561` ordered integer-pairs land within 2% of `M_KK/M_Pl = 6.085e-3` by chance (Sage, this turn; density `≈3.0e-4`). **The single decisive forward gate is a NON-SPECTRAL-SCALE EXISTENCE SCAN** (4-field spec below): does any *intrinsically-dimensionful* non-spectral substrate invariant — a cocycle-norm or holonomy carrying its own units, NOT a representation integer, NOT `Φ({λ_k})`, NOT `M_Pl·Φ` — write `M_KK` with no imported GeV/seconds anchor? PASS ⇒ §VII.BS falsified at rank-0, M_KK-DERIVATION flips LIVE. FAIL/INFO ⇒ the SCOPED-INTERMEDIATE hardens to "normalization irreducibly external"; the §6.3 seconds-valued `a(t)` dies while the SHAPE survives.

**Substrate-first throughout (no G_N-container inversion):** `D_K eigenvalues → {C₂, dim(p,q)} read off the multiplicities → dimensionless moments / ratios / integers (Ô) → measurement`. The one dimensionful prefactor `w = M_KK` is the head-of-arrow datum the spectrum cannot return; the representation integers, being the τ-/regulator-invariant (hence metric-magnitude-free) shadow of that spectrum, cannot return it either. `M_KK` is the externally-calibrated CUTOFF of the substrate's own emergent metric (N₃=0 ⇒ topologically unprotected normalization), not a container the substrate lives in.

## Wrap-Up

**What the workshop resolved.** The framework's #1 standing dimensional gap, M_KK-DERIVATION, was read oppositely by the two domain advocates: Reading-S (PROVEN-IMPOSSIBLE WALL for the spectrum-only functional class) vs Reading-P (SCOPED-INTERMEDIATE, with the integer channel held genuinely open). The workshop derived the resolution from first principles and the two readings **converged on SCOPED-INTERMEDIATE — "RATIO-derivable, NORMALIZATION irreducibly external."** The sharpened boundary is: the open question was never WALL-vs-LIVE; it was the *scope* of the intermediate — specifically whether the SU(3) representation-arithmetic channel (where Paasch's mass integers and the framework's own Casimir-ladder masses live) is a separate corridor that could seed a scale, or the dimensionless wall restated. The Sage-exact identity `C₂(p,q) = 6(|λ_trivial|² − 1/4)` settles it: the integers are READ OFF the spectrum, so channel (II) sits *inside* the dimensionless wall for the VALUE question. Whatever a spectrum-only functional cannot do, an integer recovered from that spectrum cannot do either. The wall is on the **normalization** (`w = M_KK`); the integers do real, exact work on the **ratios** (`m_p/m_e = 150^{3/2}`, `α` from `n3=10`, `150/98 ≈ φ_paasch`).

**What Changed.**

### (a) Numerical revisions
- `m_p/m_e = 150^{3/2}` pinned Sage-exact = `750√6 = 1837.1173`, rel-dev `5.25e-4` vs PDG `1836.15267` (the severed-Paasch RATIO content).
- `150/98 = 1.530612` vs `φ_paasch = 1.5315844`, rel-dev `6.35e-4` exact (was cited "0.063%/6.3e-4"; sharpened).
- Look-elsewhere density for `M_KK = M_Pl·Φ(integers)`: `2 of 6561` ordered pairs within 2% of `M_KK/M_Pl = 6.085e-3` (density `≈3.0e-4`) — the artifact floor that disqualifies the relocation gate.
- `C₂(p,q) = 6(|λ_trivial|² − 1/4)` re-verified Sage-exact as a symbolic identity (`recover(forward) = c₂`), not merely on the 5 quoted sectors.

### (b) Structural changes
- **Reading-P channel-(II) "orthogonal to the spectrum" → channel (II) is the INDEX SET of the channel-(I) heat-kernel sum** (epistemic-type change: the integers are the spectrum's discrete shadow, recovered units-free via `C₂ = 6(λ²−1/4)`; the WALL on the value question reaches them). This RETRACTS the R1.P "orthogonality" inference — the vanishing derivative `∂dim/∂λ = 0` was a red herring (discrete label, not smooth functional); determination is by degeneracy/ordering, not differentiation.
- **Paasch (b.1) `m_e = M_KK·Ô_e` from "real structural win" → value-wall restated in Paasch's variables** (the regress `m_p → m_e → M_KK → silence` terminates at the one external pin; Paasch is a consistency-partner that STRENGTHENS bundle-exhaustiveness, not a corridor around it).
- **Decisive gate `M_KK = M_Pl·Φ(integers)` → withdrawn** (circular under substrate-first M_Pl; imported-GeV under lab M_Pl; look-elsewhere artifact) **→ replaced by the non-spectral-scale existence scan** (the falsifier-(i)-on-the-non-spectral-channel test; the only door neither advocate closes).
- **M_KK-DERIVATION status reframed for EVOI §6:** the "single highest-leverage standing gap / only incumbent-ceiling-lift path" is now scoped — the ceiling-lift is contingent on the non-spectral-scale scan landing PASS; absent that, the SHAPE is derived and the SIZE is one pin (the §6.3 `a(t)`-gap is a structural feature, not a defect).

**Math-vs-structural distinction.** The numerical revisions (above) are subordinate; the durable workshop outputs are the structural changes — chiefly that the representation-arithmetic channel is *inside* the dimensionless wall (head 1) and that Paasch's machinery is a ratio-fixing consistency-partner that strengthens rank-1 rather than a scale derivation (head 2). The verdict does NOT queue a computation in place of a position: it pins SCOPED-INTERMEDIATE and names the one gate that would move it.

## Effected In-Session

**INVESTIGATION-TRACK BOUNDARY (binding).** This is an investigation-3 workshop. It CANNOT mutate session-track curated registers (Atlas, `permanent-results-registry.md §VII.BS`, EVOI §6, `canonical_constants.py`) — those are session-track per `gate-verdicts.md §"Investigation-Track Canonical Path"`. There are no orchestrator-effectable non-math items inside this investigation. All register-annotation candidates are therefore **session-promotion candidates routed to the `/rclab-investigate --investigation 3` close** (housekeeping §B, designated-writer per `feedback_framework-hygiene.md`):

- `[→investigate]` **Annotate §VII.BS (session-track) with the SCOPED-INTERMEDIATE finding**: record that the rank-1 wall is now established to reach the SU(3) representation-arithmetic channel (not merely spectrum-only functionals) via the Sage-exact `C₂ = 6(|λ_trivial|² − 1/4)` index-set identity — the integers are the spectrum's discrete shadow, `p=0` in the shift-covariance. (Designated writer at investigation close; `mack-cosmic-bridge` if a falsifier-row touch is implicated.)
- `[→investigate]` **Annotate EVOI §6 (session-track)**: M_KK-DERIVATION "incumbent-ceiling-lift path" is contingent on the non-spectral-scale existence scan (Carry-Forward below) landing PASS; absent PASS, the gap is the scoped "SHAPE-derived, SIZE-one-pin" structural feature, and the §6.3 seconds-valued `a(t)` is reframed (SHAPE survives, seconds-valued normalization external).
- `[→investigate]` **Record the WITHDRAWAL of `M_KK = M_Pl·Φ(integers)`** as a non-decisive gate (circular / look-elsewhere `2/6561`), so the seed's candidate (c) — INV3-W3-4 — is logged CLOSED-on-the-integer-route and not re-proposed; the live forward gate is the non-spectral channel only.

If the investigation close finds none of these warrant a session-track migration (the result is permanent only when re-computed under a `session-{N}` gate per the track-local boundary), they remain investigation-track citations. Nothing here is orchestrator-effectable in-session.

## Carry-Forward Computations

The single decisive forward gate. Distinct from BOTH falsifier-(i) (`Φ({λ_k})`, RAN FAIL S102 `63698aa8`) AND the withdrawn `M_Pl·Φ(integers)` relocation. Carries a MANDATORY look-elsewhere penalty per the W3-2 density corpus (the `2/6561`-at-2% artifact floor demonstrated this turn).

| Field | Specification |
|:------|:--------------|
| **What** | **Non-spectral-scale existence scan (INV3-W4-1-FWD).** Pre-register whether a *dimensionful* `M_KK` value can be written as `M_KK = Φ(non-spectral substrate invariant carrying intrinsic units)` with NO imported GeV/seconds anchor. "Non-spectral substrate invariant" means a quantity that is NOT a spectrum-only functional `Φ({λ_k})` and NOT a representation integer (both are dimensionless — established this workshop): the candidate set is a **cocycle-norm with intrinsic units**, a **holonomy-derived scale**, or the `1/G_induced ~ f₂·M_KK²·a₂` matching read in reverse for a genuinely-new substrate-internal dimensionful datum. Falsifier-(i) on the *non-spectral* channel. |
| **Inputs** | `M_KK` pin = `7.428660036284456e16 GeV` (S42 CONST-FREEZE-42, `get_constant M_KK`); §VII.BS rank-1 certificate + `ac1dbb28` bundle-exhaustiveness (the established wall over channels (I) ∪ {w₂} ∪ integer-(II)); the framework's cocycle-norm / holonomy inventory (e.g. the 3He-B inheritance cocycle norms `‖φ_67‖, ‖φ_88‖` and any holonomy on Jensen-deformed SU(3) — audit which, if any, carry intrinsic units rather than reducing to `Φ({λ_k})`); `1/G_induced ~ f₂·M_KK²·a₂` clause-(d) matching (volovik Sage chain). |
| **Gate** | **PASS** = some non-spectral substrate datum carries a GeV with no external import (i.e. it does NOT reduce to a spectrum-only functional and does NOT smuggle an external anchor), reproducing `M_KK` to a pre-registered tolerance AFTER a MANDATORY look-elsewhere penalty (the candidate pool's chance-hit density at the test tolerance, computed Sage-exact, must be `≪` the claimed match — the `2/6561`-at-2% floor is the W3-2-corpus precedent; a 2%-tolerance single-ratio hit is NON-evidential). PASS ⇒ §VII.BS falsified at rank-0; M_KK-DERIVATION flips to **LIVE-CORRIDOR**. **FAIL/INFO** = every candidate either reduces to a spectrum-only functional (hence dimensionless, by the `C₂=6(λ²−1/4)` index-set identity) or smuggles an external GeV/seconds anchor. FAIL/INFO ⇒ the **SCOPED-INTERMEDIATE hardens to PROVEN-WALL on normalization** ("M_KK irreducibly external"); the §6.3 seconds-valued `a(t)` dies, the dimensionless SHAPE survives. |
| **Effort** | Medium. The cocycle-norm / holonomy inventory is partially in hand (inheritance-falsifier protocol cocycle norms; S105 block-diagonal `D_K` closed forms). New content = (1) a units-audit of each candidate invariant (does it carry intrinsic GeV, or does it reduce to a dimensionless `Φ({λ_k})` under the `C₂=6(λ²−1/4)` map?), and (2) the look-elsewhere-penalized existence test against the single `M_KK` value. Sage-exact rationals for the chance-hit density (per `regulator-pin-discipline.md §"Sage-Exact Rationals"`). Depends on: §VII.BS certificate (UPSTREAM, established), the cocycle-norm/holonomy inventory (REGISTRY: inheritance-falsifier-protocol + S105 closed forms), `get_constant M_KK`. |
