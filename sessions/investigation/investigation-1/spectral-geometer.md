# Investigation-1 — Spectral-Geometer's Determination

**Agent**: Spectral-Geometer (heat-kernel asymptotics, Seeley-DeWitt coefficients, spectral dimension, Weyl asymptotics, eigenvalue estimates)
**Date**: 2026-06-14 (S108–109 plateau)
**Vantage**: I think governing-structure-first. For this framework the governing object is the heat trace `Tr(exp(-σ D_K²))` and its two asymptotic limbs — the small-σ Seeley-DeWitt limb (local curvature invariants `a_0, a_2, a_4, …`) and the spectral-sum limb (zeta-regularized moments, spectral dimension flow). Every coupling, mass, and the cosmological-constant moment is claimed to be a spectral moment of `D_K`. My job is to test those claims at the level of exact coefficients, eigenvalue bounds, and known limiting cases.

**What I actually read (this session)**: `key-results.md` (my own consolidated heat-kernel audit S45–S82); `atlas-00-index.md` (project map, last full uplift S88); `open-channel-ledger.md` (S106-stamped — the genuine live frontier); knowledge-MCP queries on `{Seeley-DeWitt a_4 / CC}`, `{M_KK / K_pivot scale gap}`, `{A_s amplitude floor wall}`, `{d_s spectral-dimension flow vs CDT}`. Canonical constants confirmed: `d_s_fold_window_sigma = 1.4005`, `a_2^{SD}(fold) = 0.728235`, `A_s` gaps (3.15 OOM Route-B-PW / 9.4716 OOM Bogoliubov). I did NOT re-derive; I verified state against canonical registers and bring my own adversarial reading.

A note on framing law (`phononic-framing.md`): the substrate IS the spectral content. I keep the arrow `D_K eigenvalues → spectral moments → emergent field equations → measurement` pointing one way throughout. Where I flag a gap, it is a gap in the substrate→emergent derivation, not a request to embed the substrate in a container.

---

## 1. BIGGEST GAPS

### G1 — The M_KK normalization is the substrate's single un-derived dimensionful dial, and it is the *parent* of three of the framework's worst observational gaps

This is the gap I rank #1 from my vantage, because it is a **spectral-geometry** gap dressed as a cosmology gap. The S101 W-2 Normalization-Non-Universality (NNU) theorem went STAGE-3-PERMANENT (§VII.BS, `open-channel-ledger.md §C`, audit `d309efb4`; clause-(b) closed S103 `ac1dbb28`). What that theorem actually *proves* is a constraint of the form `O = w · Ô`: every dimensionful substrate observable factors into a dimensionless spectral kernel `Ô` (computed from the `D_K` spectrum) times **one** overall weight `w = M_KK`, and the rank of the covariance over observables is exactly 1 (`|Corr| = 1.000000`). This is a genuine and clean structural result. But read it the other way and it is a confession: the entire dimensionful content of the framework is **one number** that the spectral triple does not fix. `M_KK-DERIVATION` is named a standing gap in the same ledger row.

Why this is the biggest gap and not merely "one free parameter like the SM's overall scale": the heat kernel on a *given* metric fixes `a_0, a_2, a_4, …` up to the volume and the overall dimensionful scale. In ordinary NCG (Connes–Chamseddine) `M_KK`/the cutoff `Λ` is set by matching `a_2` to Newton's constant — and indeed `key-results.md` records `f_conv = π⁴/(9216·a_0²)` exact and "a_2 cancellation structural, from G_N matching." So `M_KK` is currently fixed by **G_N matching**, i.e. by an external dimensionful input. That is fine as a *definition* but it means the framework cannot independently predict any absolute scale; it predicts ratios. The honest statement is: **the spectral triple predicts the dimensionless `Ô`'s exactly and predicts no absolute scale at all.** Everything dimensionful (m_H = 131.8 GeV, H₀ = 67.40, Σm_ν, the CC magnitude) inherits its absolute size from the one G_N-anchored `M_KK`.

### G2 — A_s amplitude floor (3+ OOM): a wall with two *different* numbers, both un-reconciled at the spectral-moment level

`AMPLITUDE-NORM-66` is FAIL with `A_s` gap **3.15 OOM** (Route B, Peter-Weyl; `constraint-mega-matrix.md`, `baseline-findings-s66.md`); but `s74_as_from_bogoliubov_output.txt` reports **9.4716 OOM**; and CF23 hardened the gap to a "permanent structural-position wall, 3.02× Planck" at S83 (`atlas-08-freshness-S97.md`). Three numbers — 3.02×, 3.15 OOM, 9.47 OOM — for "the A_s gap," in three different framings (linear factor, ratio-OOM at fixed epoch, absolute Bogoliubov). My memory already flags this: `A_s` is **extensive** (R-fragile, `α_net ≠ 0`, functional-dependent) per the S76 intensive/extensive partition, and `CC · f_conv² ~ 1/a_0³` shares the extensive parent. So the A_s gap and the CC magnitude are *the same spectral-scaling pathology* viewed through two observables. The gap is real and the wall is honestly reported, but the framework has not pinned **which** of the three numbers is the substrate-IS prediction under a *fixed* spectral functional. Until the functional is selected (Q28-class, now COMMITTED for n_s but the A_s amplitude lives on the extensive axis where the functional choice does NOT cancel), "3.02× Planck" is a one-functional snapshot, not a theorem.

### G3 — K_pivot scale mapping: the substrate has no derived bridge from the fiber scale `K = 2.0 M_KK` to the CMB pivot

`open-channel-ledger.md §A` A1 names this "the single largest *observational* load-bearing gap" — BROKEN-WITH-LIVE-RESEARCH-PATHWAY. The working n_s = 0.965 requires `K* ≈ 0.087 M_KK`, and "no physical mechanism places K at the intermediate K*." From a spectral-geometry vantage this is a **Weyl-law / mode-counting** gap: the fiber tessellation scale `K = 2.0 M_KK` is a UV object (it lives where the eigenvalue density `N(λ)` is set by the 8-dimensional Weyl asymptotic), while the CMB pivot is 54 decades into the IR. The framework's own machinery (`d_s` flow; my `d_Weyl = 6.81` pre-asymptotic counting) says the spectral dimension *runs* from 8 (UV) toward small values (IR window `d_s_fold_window_sigma = 1.4005`). A scale-dependent dimension is *exactly* the object that should govern a UV→IR scale map — but no one has connected the `d_s(σ)` flow to the `K → K*` shift. The bridge between A1 (K-pivot) and the `d_s` flow is untraveled (see §5, UB-1).

### G4 — The Seeley-DeWitt limb and the spectral-sum limb are KNOWN to disagree by factor 3812, and the framework uses BOTH without a posted reconciliation theorem

This is my permanent-memory Level-2 finding (`HEAT-KERNEL-AUDIT-45/46`): on the finite truncated spectrum the "spectral a_2" `= ζ_D(1) = 2776.17` is **not** the SD coefficient `a_2^{SD} = 0.728235` — they differ by ~3812 because `ζ_D(s)` has a pole at `s=1` for `d=8` and the truncated sum is UV-dominated (`~Λ_max⁶`). M_KK extraction uses `ζ_D(1)` correctly as a *spectral moment*; gravity (Einstein–Hilbert) uses `a_2^{SD}` correctly as a *local curvature integral*. Both are right *in their own object*. The gap is that the framework's prose and several downstream gates **name both "a_2"**, and the regulator-pin discipline (`a_n^{regulator}`) was only imposed post-S86 with ~20,343 legacy bare-`a_n` hits in carry-forward triage (`regulator-pin-discipline.md §Carry-Forward`). This is a real, large, latent source of factor-thousands errors in any pre-S86 derivation that is still cited. It is a *bookkeeping* gap with *physics* teeth.

---

## 2. CRITICAL CONTRADICTIONS

### C1 — The R-protected-fold ratio has TWO inequivalent values differing by factor ~2.33, and downstream consumers do not always declare which

`key-results.md` (S74 W2-O FAIL, STRUCTURAL): `R = a_0·a_4/a_2²` at the fold is **either** `R_protected_fold_partialsum = 1.128655` (L_max=3 partial-sum ratio, canonical) **or** `R_protected_fold_gilkey = 0.492288` (Gilkey heat-kernel curvature polynomial, exact closed form `(500 − 32|Ric|²/R² − 28K/R²)/1000`). Routes A/C (partial-sum + zeta-extrapolation) agree to 1.06%; both differ from Route B (Gilkey) by **factor 2.33**. This is not a numerical wobble — it is two genuinely different mathematical objects (a truncated partial-sum ratio vs a local-curvature-polynomial ratio) related only through Mellin pole residues. The CC-Ratios-Only Theorem (S80/S82, closure SHA `8a5678…4211`) *explains* why some ratios are f-independent (weight-balanced multisets cancel) and some are not — and `R` with multiset `{8,4}` vs `{6,6}` is NOT multiset-balanced, so it retains the functional and the scheme split is a *structural necessity*, not an error. **The contradiction is procedural**: the framework has the theorem that says "you MUST pin the scheme," yet the same `R` is cited in atlas/registry contexts as if it were one number. Any result that consumes `R_protected_fold` without a scheme tag is silently picking one of two values that differ by 2.33×. I rate this CRITICAL because `R` is the dimensionless ratio the framework leans on for "R-protection" (its claimed defense against truncation fragility), and a 2.33× ambiguity in the protected quantity undercuts the protection claim's force.

### C2 — "R-protection" is claimed as truncation-robustness, but R is NOT τ-protected, and the two protections are conflated in the prose

`key-results.md` (S77 INFO): `R_1(τ)` is strictly monotone increasing across `[0, 0.5]`: 1.109 (τ=0) → 1.129 (fold) → 1.237 (τ=0.5), total variation **11.13%**, NOT stationary at the fold (`dR_1/dτ = +0.203`). The R-Protection Theorem (S76, machine-checked for any compact simple Lie group) proves `R_n` is **L_max-protected** (Weyl-exponent cancellation `α_0 + α_{2n} = 2α_n`). But L_max-protection ≠ τ-protection. The framework's narrative sometimes elides this — "R is protected" reads as "R is stable," when in fact R is stable against *mode-count truncation* and *varies 11% across the deformation parameter*. These are structurally distinct phenomena (Weyl-exponent cancellation vs curvature redistribution under the Jensen TT-deformation). This is not a fatal contradiction but it is a load-bearing imprecision: the one quantity advertised as the framework's truncation-robustness anchor moves by 11% along the very axis (τ) whose value (τ_fold = 0.190) the framework cannot derive (A4). So the "protected" quantity inherits the un-derived-modulus problem.

### C3 — Spectral dimension is cited at TWO incompatible values (8 vs 1.4005) and the CC-via-d_s route is CLOSED, yet d_s remains in use as a physical descriptor

`Phononic-Penrose-Diagrams.md` records `lim_{σ→0} d_s(σ) = 8` (Weyl/manifold dimension, structural). The canonical constant `d_s_fold_window_sigma = 1.4005` (S92) is the *windowed* value at `σ_* = 1.4005 M_KK⁻²`. My own Level-3-artifact tier (`HEAT-KERNEL-AUDIT-45`) ruled `d_s` in the UV an **artifact** (`→ 0 not 8` on the *truncated* spectrum) and recorded "CC routes via d_s and torsion: CLOSED." The S92 ad-hoc workshop (and the cross-pillar-bridge rule's diffusion-window specialization, `cross-pillar-bridge-anatomy.md §"Diffusion-window-observable specialization"`, K=2) correctly insists these are **distinct functionals of the same P(σ)** and must be compared window-for-window against CDT. The contradiction I flag is softer than C1/C2 but worth stating: `d_s = 8` (continuum Weyl), `d_s → 0` (truncated UV artifact), and `d_s = 1.4005` (fold window) are three different objects, and the framework's prose occasionally treats "the spectral dimension" as if it were a single physical number. The discipline rule exists; the prose hygiene lags it. Per my memory this is correctly CLOSED as a CC route — the danger is re-opening it informally.

### C4 (apparent, not actual) — m_H = 131.8 GeV "PROVEN-AT-OBSERVATION" vs +5.36% from 125.1

`open-channel-ledger.md §D` lists m_H = 131.8 GeV as "PROVEN-AT-OBSERVATION (0-free-param)" while simultaneously recording `+5.36%` and `+38.5σ vs tight PDG`. That looks like a contradiction but is not one once you read it correctly: it is "proven to be a zero-free-parameter *prediction*" (the route is pinned, no dials), and it is `+5.36%` from the measured value. The honest tension is that a 5.36% / 38.5σ miss is being carried under a "PROVEN-AT-OBSERVATION" tag. From my vantage the spectral-geometry content is sound (m_H is the `a_4`-dressed `|S|²` KK-threshold mode, `L_sat=6`); the 5.36% is exactly the size of the higher-`a_n` truncation tail one expects at L_sat=6 (my moment-ratio data has `a_4` varying 28.65% across τ). So C4 is a labeling tension, not a spectral contradiction — but the "PROVEN-AT-OBSERVATION" label should read "PROVEN-ZERO-PARAMETER-PREDICTION; 5.36% / 38.5σ from PDG, consistent with the a_4 truncation tail."

---

## 3. UNSUPPORTED LOAD-BEARING ASSUMPTIONS

### LB1 — That the finite truncated heat trace's Seeley-DeWitt coefficients converge to the continuum Gilkey values as L_max → ∞ (assumed; pre-asymptotic everywhere it is used)

The entire "a_2 → Einstein–Hilbert, a_4 → Yang–Mills+Higgs" identification rests on the small-σ expansion of `Tr(exp(-σ D_K²))` reproducing the Gilkey curvature polynomials. On a *compact finite-dimensional truncation* this is **provably not** the continuum expansion: my audit shows the truncated spectrum has entire (not meromorphic) zeta, a convergent (not asymptotic) Taylor series, and `d_s → 0` (not 8) in the UV. The framework operates at **L_max = 3** physically (Planck-implied `L_max* = 2.92`, `f_conv` INFO), where the R-Protection Theorem itself says we are deeply **pre-asymptotic**: `a_0` effective exponent 5.23 vs asymptotic 8; `d_eff(PW) = 5.83` at S61; "need L ~ 210 for true Weyl regime." The load-bearing assumption is that the *physically-selected* L_max=3 truncation nonetheless lands the dimensionless ratios close enough to the continuum that the SM-matching (`a_4 → g², Higgs quartic`) holds. The CC-Ratios-Only Theorem partly *rescues* this for weight-balanced ratios (they are f-independent → more robust), but the SM gauge/Higgs sector lives in `a_4`, an **unbalanced extensive** object, where the truncation tail is `O(L^{d+r+4})`-fragile. **This is the framework's deepest unsupported load-bearing assumption from my domain**: that L_max=3 is "close enough" for the un-protected `a_4` physics. It has never been shown that the SM predictions are stable as L_max=3 → 4 → 5 for the *extensive* (un-protected) coefficients; only the *intensive* ratios (n_s, R_n) carry a protection theorem.

### LB2 — That the Jensen deformation being volume-preserving (TT-constraint) is sufficient to keep `a_0` constant, hence the spectral functional ensemble interpretation

`key-results.md`: "`a_0 = const (6440)` at fixed L_max (mode count topological)." This is used to argue the CC magnitude is purely about the *other* moments. The assumption that `a_0` is exactly topological (mode-count-only) under the *physical* (Jensen TT, volume-preserving) deformation is reasonable for a fixed L_max but is load-bearing for the entire extensive/intensive partition (S76): if `a_0` drifted with τ at fixed L_max, `CC · f_conv² ~ 1/a_0³` would acquire a τ-dependence and the "CC is an extensive functional-dependent quantity" framing would shift. It is *plausible* (mode count is topological), but I have not seen it proven that the Jensen deformation preserves the eigenvalue *count* below the truncation (it preserves volume = `a_0`-continuum, but the truncated mode count is a different object — it can change if eigenvalues cross the L_max ceiling under deformation). The relation between "volume-preserving" (continuum `a_0`) and "mode-count-preserving" (truncated `a_0 = 6440`) is assumed, not demonstrated.

### LB3 — That `M_KK` set by G_N-matching is the SAME `M_KK` that sets the EW scale, the KK threshold (m_H), and the tessellation scale K

This is the physics content of the NNU theorem's clause-(b) (closed S103, `ac1dbb28`: `w₂ = m_H/v_ew`, `rank(Cov_aug) = 1`). The theorem PROVES that, *given the framework's construction*, all dimensionful observables share the one weight. But the *premise* — that there is a single dimensionful weight at all, i.e. that gravity's `a_2`-scale, the EW VEV, and the KK threshold are not three independent scales — is itself the framework's foundational ansatz. The rank-1 result is impressive *if* you accept the construction; it does not prove the construction is the right one. A theory with three genuinely independent dimensionful inputs (G_N, v_ew, M_KK) would be falsified by the rank-1 finding *only if* the construction is correct. So LB3 is: **the rank-1 normalization universality is conditional on the spectral-action construction being the complete map**, and that completeness is assumed.

### LB4 — That the eigenvalue bounds (Lichnerowicz, Friedrich–Kirchberg) being SATISFIED implies the spectrum is the *correct* physical one

`key-results.md`: all FK/Lichnerowicz bounds satisfied; `lambda_1²/Kirchberg = 1.065` (6.5% gap), Friedrich–Kirchberg `5R/16 = 0.631` tightest at fold. These bounds are **necessary** conditions (any genuine Dirac spectrum on a positive-curvature spin manifold must satisfy them); they are *not sufficient* to certify that the Jensen-deformed `D_K` is the unique/correct operator. The framework correctly uses them as cross-checks (my adversarial role: "if a computed eigenvalue violates Lichnerowicz the computation is wrong"). The load-bearing slip would be to read "all bounds satisfied" as positive evidence the operator is right. It is consistency, not confirmation (per `epistemic-discipline.md §"What Does NOT Count as Evidence"` — internal consistency alone). I do not see the framework making this slip in the registers, but it is latent in the prose.

---

## 4. AREAS NEEDING REFINEMENT

### R1 — Finish the regulator-pin retrofit on the load-bearing a_n citations (not all 20k, but the ~dozen that feed live predictions)

`regulator-pin-discipline.md` correctly mandates `a_n^{ζ}` / `a_n^{Pauli-Villars}` / `a_n^{Mellin}` / `a_n^{lattice}` / `a_n^{cutoff}` tagging going forward, and honestly logs ~20,343 legacy bare-`a_n` hits as carry-forward. Auto-retrofit is over-broad. But the *subset* that feeds m_H, n_s, A_s, and the CC magnitude is small and identifiable. **Refinement**: a targeted semantic pass tagging only the `a_n` citations on the live-prediction derivation paths (the `A-N-SEELEY-DEWITT-RETROFIT` queue, scoped to the prediction-feeding subset). Given G4/C-tier risk (factor-3812 between ζ_D(1) and a_2^{SD}), this is higher-leverage than its "hygiene" label suggests.

### R2 — Pin the A_s gap to ONE number under the SELECTED functional

The functional-selection question is COMMITTED for n_s (S103 `3ddadf91`, BCS+1-loop-√-cutoff, A₆-robust). A_s lives on the **extensive** axis where the functional does NOT cancel (S76). **Refinement**: recompute the A_s gap under the *same selected functional* used for n_s, and report the single resulting OOM number, retiring the 3.02× / 3.15 OOM / 9.47 OOM ambiguity. If the wall is genuinely permanent (CF23/S83), the *value* of the wall under the canonical functional should be stated once, with the regulator tag, in `falsifier-master-inventory.md`.

### R3 — Reconcile the R_protected scheme split in the registry, not just in agent memory

The factor-2.33 R-split (C1) and the CC-Ratios-Only explanation (S80/S82) live in my memory and the workshop SHAs. **Refinement**: a single registry line (or §VII annotation) stating canonically: "`R_protected_fold` has two inequivalent schemes; partial-sum = 1.128655 (truncation ratio), Gilkey = 0.492288 (local-curvature polynomial); they are NOT equal and the choice is structural per CC-Ratios-Only multiset-imbalance (closure SHA `8a5678…4211`); downstream consumers MUST tag the scheme." This is a 4-line fix that closes a 2.33× ambiguity in the framework's advertised protection anchor.

### R4 — State the m_H label honestly: zero-parameter PREDICTION at +5.36% / 38.5σ, truncation-tail-consistent

Per C4, change "PROVEN-AT-OBSERVATION" to a label that carries the σ-distance and the truncation-tail interpretation. The spectral content (a_4-dressed |S|² KK mode, L_sat=6) is correct and publishable; the label oversells. This is a capstone-hygiene Q3 item (PROVEN/CONDITIONAL status of a prose claim vs the register) under `capstone-hygiene-gate.md`.

### R5 — Make the d_s flow window-explicit everywhere

Adopt, in the prose, the discipline the rule already mandates (`cross-pillar-bridge-anatomy.md §"Diffusion-window-observable specialization"`): never write "the spectral dimension" without the window. Three canonical values exist (8 continuum-Weyl; →0 truncated-UV-artifact; 1.4005 fold-window). The CC-via-d_s route is CLOSED and should stay closed; the refinement is to keep informal prose from re-opening it.

---

## 5. UNTRAVELED BRIDGES (most important)

These are the items where my domain has a concrete known result the framework has not engaged, and where engaging it could bridge two of the gaps/contradictions above.

### UB-1 — Spectral-dimension flow as the K → K* scale map (Lauscher–Reuter dimensional reduction; Carlip "Spontaneous dimensional reduction")

**The result/mystery**: It is a robust, multi-program finding that the spectral dimension of spacetime **runs from ~4 in the IR to ~2 in the UV** — in asymptotic safety (Lauscher & Reuter, *JHEP* 0510:050, 2005; Reuter & Saueressig, arXiv:1110.5224), in CDT (Ambjørn, Jurkiewicz, Loll, *Phys. Rev. Lett.* 95:171301, 2005, arXiv:hep-th/0505113), in Hořava–Lifshitz gravity, and reviewed as a near-universal phenomenon by Carlip (arXiv:1705.05417, "Dimension and Dimensional Reduction in Quantum Gravity"). The *running spectral dimension* is precisely a scale-dependent `d_s(σ) = -2 d ln P(σ)/d ln σ`.

**How the substrate fills the cement**: The framework ALREADY HAS this object. `d_s(σ→0) = 8` (UV Weyl on the 8-dim fiber), and the windowed `d_s_fold_window_sigma = 1.4005` (S92). The S92 ad-hoc workshop compared the fold-window `d_s` to CDT but treated it as a *consistency check*, not as a **scale-transport kernel**. The untraveled step: the same `P(σ) = Tr(exp(-σ D_K²))` that defines `d_s(σ)` is a **diffusion/return-probability** that interpolates UV→IR. A running dimension is mathematically the generator of an anomalous scaling of the mode-counting measure `N(λ)`. The K-pivot gap (G3/A1) is exactly a UV(`K = 2.0 M_KK`)→IR(`K* ≈ 0.087 M_KK`) mode-density rescaling — a factor of ~23 in scale. **Sketch**: compute the effective scaling exponent `θ(σ) = d_s(σ) − d_s(σ→0)` of the substrate's own return probability over the window σ ∈ [fiber-UV, fold], and test whether the integrated anomalous scaling ∫ θ d ln σ reproduces the `ln(K/K*) ≈ ln(23) ≈ 3.1` e-folds the framework needs to place K at K*. If the substrate's *own* spectral-dimension flow supplies the K→K* map, then **A1 (the largest observational gap) closes using machinery the framework already computed for a different purpose (S92)**. This is the single highest-leverage untraveled bridge from my domain — and it directly bridges G3 (K-pivot) ↔ C3 (the d_s multi-value confusion), because forcing the flow to *do work* (transport the scale) forces the windows to be stated precisely.

### UB-2 — The Kac / Sunada isospectrality question: is the SM content a spectral INVARIANT or could an isospectral deformation preserve the spectrum but change the physics?

**The result/mystery**: Milnor (1964) and Gordon–Webb–Wolpert (1992, "One cannot hear the shape of a drum") established that the spectrum does NOT determine the manifold — isospectral, non-isometric manifolds exist (Sunada's construction, *Ann. Math.* 121:169, 1985). The heat trace `Tr(exp(-σ D_K²))` — and hence **all** Seeley-DeWitt coefficients `a_k` and all zeta moments — are by construction isospectral invariants.

**How the substrate fills the cement, and which gaps it bridges**: The framework derives ALL physics from spectral moments of `D_K`. By the isospectrality theorems, those moments cannot distinguish two isospectral-but-non-isometric internal geometries. **This is a sharp, untraveled question with teeth**: is the Jensen-deformed SU(3) `D_K` **spectrally rigid** within its deformation class, or does there exist an isospectral deformation (a Sunada-type pair, or a continuous isospectral family) that leaves every `a_k`/ζ-moment fixed — hence leaves m_H, n_s, all couplings fixed — while changing something the framework currently treats as physical (the fiber holonomy, the gauge connection between fibers, the relay-pattern propagation)? Two outcomes, both valuable: (i) if `D_K` is spectrally rigid in its class (no isospectral deformation), that is a **publishable structural strengthening** — the framework's "everything is a spectral moment" is not lossy, the geometry IS reconstructible (Connes' reconstruction theorem applies and the 5 axioms pin it). (ii) If an isospectral deformation EXISTS, it is a **falsifiable degeneracy**: the framework predicts identical CMB/collider physics for two distinct substrates, and the *non-spectral* observables (e.g. the cohomology-asymmetry ratio `7.3250` in the 3He-B inheritance protocol, `inheritance-falsifier-protocol.md`, which is a *cocycle* norm ratio, NOT a spectral moment) become the **only** discriminators. This bridges **LB1 (truncation→continuum assumption) ↔ LB4 (bounds-satisfied ≠ correct-operator)**: spectral rigidity is exactly the missing sufficiency argument that would upgrade "the bounds are satisfied" (necessary) to "the operator is determined" (sufficient), via Connes reconstruction. The concrete first computation: on the L_max=3 truncation, scan the Jensen-deformation modulus τ and check whether any τ ≠ τ' gives **bit-identical** `{a_0, a_2, a_4}` multisets while differing in a non-spectral invariant (e.g. the Kosmann pairing matrix `V_nm`, which I have in memory). The `R_1(τ)` monotonicity (C2) is *evidence against* near-isospectral degeneracy along τ (the ratio moves 11%), so the spectrum is at least τ-faithful at the ratio level — but that is one ratio, not the full multiset, and the rigorous statement is unmade.

### UB-3 — The Cheeger inequality / spectral-gap ↔ the A_s amplitude floor as an isoperimetric obstruction

**The result/mystery**: Cheeger's inequality (Cheeger 1970) bounds the first nonzero Laplacian eigenvalue from below by the isoperimetric (Cheeger) constant `h`: `λ_1 ≥ h²/4`. The spectral *gap* is controlled by the geometry's "bottleneck." There is a deep, under-exploited dual: the *amplitude* of fluctuations sourced by a spectral process is set by the **density of states near the gap**, not by the gap location alone.

**How the substrate fills the cement, bridging G2 (A_s) ↔ G1 (M_KK)**: The A_s floor is a 3+ OOM *amplitude* excess that S83 hardened to a structural wall "not remediable at the substrate-IC layer." My S76 partition says A_s is **extensive** (`A_s ~` an un-protected functional of `a_0`, sharing `CC · f_conv² ~ 1/a_0³`). The untraveled question: is the A_s amplitude floor an **isoperimetric / density-of-states obstruction** — i.e. is it fixed by the eigenvalue density of `D_K` near the spectral floor (the `B2` minimum `λ = 0.845` at τ=0.190, my memory; the Friedrich–Kirchberg-saturating sector), the same way Cheeger's `h` fixes the gap? If so, the floor is NOT a free amplitude but a **computable functional of the near-floor DOS**, and its 3 OOM size is then a *prediction* with the M_KK weight (G1) as its only dimensionful input. This bridges G2↔G1: the A_s amplitude becomes `M_KK²` × (a dimensionless near-floor-DOS functional). Concrete sketch: compute the integrated density of states `n(λ)` in the window `[λ_min, λ_1]` near the FK-saturating sector at the fold, and test whether the A_s ratio equals `(M_KK/M_Pl)² × ∫_floor n(λ) g(λ) dλ` for the *selected* functional g. If the near-floor DOS reproduces the 3.02× (or whichever R2-refined number), the "permanent wall" is reinterpreted as a substrate-IS prediction — exactly the inversion the framing law wants (the substrate IS the amplitude, not the amplitude IN a container).

### UB-4 — Functional determinants / Polyakov-anomaly amplitude and the multiplicative anomaly (Kontsevich–Vishik) as the missing A_s normalization

**The result/mystery**: The one-loop effective action / fluctuation amplitude is governed by the zeta-regularized functional determinant `det'(D_K²) = exp(−ζ'_D(0))`, and there is a known subtlety — the **multiplicative anomaly** (Kontsevich–Vishik 1994; Wodzicki residue): `det(AB) ≠ det(A)det(B)` for pseudodifferential operators, by a computable residue term. The Polyakov formula relates `ζ'(0)` to the conformal anomaly.

**How the substrate fills the cement, bridging G2 ↔ G4**: The A_s amplitude normalization (G2) is precisely a fluctuation-amplitude / functional-determinant object. The framework currently routes A_s through a `P_dS × f_conv × ...` ledger (some of it retracted, per `p5-a-evoi-recalibration.md`). The untraveled step: compute the A_s normalization as a genuine **zeta-regularized functional determinant** `exp(−ζ'_{D_K}(0))` of the fold-point operator, and check whether the multiplicative anomaly between the M4 and SU(3) factors of the determinant (`det(D_M4 ⊗ 1 + 1 ⊗ D_SU3)` does NOT factor cleanly) supplies the missing OOM. This bridges G2 (A_s amplitude) ↔ G4 (the ζ_D vs a_2^{SD} factor-3812 confusion): the functional determinant `ζ'_D(0)` is the *correct* spectral-sum object for an amplitude (whereas `a_2^{SD}` is the curvature object for gravity), so doing A_s via `ζ'_D(0)` would simultaneously (a) put A_s on its proper spectral footing and (b) demonstrate concretely the Level-2 distinction my audit flagged. Caveat from my own memory: anything requiring **zeta poles** on the truncated spectrum is Level-3-artifact-prone (analytic torsion `T ~ 10^{20301}` is unphysical-extensive). `ζ'(0)` is *safer* than `ζ(1)` (it is the determinant, finite on the truncation) but the extensivity must be watched — this is a genuine open computation, not a guaranteed win.

### UB-5 — Weyl-law remainder / Berry–Tabor and the spectral STATISTICS as a substrate signature the framework has under-used

**The result/mystery**: Beyond the leading Weyl term `N(λ) ~ C_d Vol λ^{d/2}`, the *fluctuations* of the counting function around the Weyl mean carry geometric information, and the *level-spacing statistics* distinguish integrable (Poisson, Berry–Tabor 1977) from chaotic (GUE/GOE) systems. My memory records the framework already found **geodesic flow INTEGRABLE (Berry–Tabor)** and **CG(24) Poisson integrability `⟨r⟩ = 0.367`** (the Ordered-Veil fabric-scale result).

**How the substrate fills the cement**: This is less a gap-bridge than an under-exploited *strength* I want on the record. The framework has Poisson statistics at the fabric scale (integrable) — which is *consistent* with the substrate being a homogeneous-space (SU(3)) spectral problem (homogeneous spaces decompose by representation theory → Poisson-like, NOT GUE). The untraveled refinement: the **Weyl-law remainder** `N(λ) − N_Weyl(λ)` on the Jensen-deformed `D_K` is a computable oscillatory term whose period encodes the *shortest closed geodesics* (the trace formula). Since the framework's "transit" is a dynamical process on this geometry, the remainder term's structure could constrain the transit's characteristic scale `τ_fold` independently — a possible *spectral* handle on the A4 moduli-selection gap (`τ_fold = 0.190` un-derived). This bridges toward A4 from the spectral side: if the shortest-closed-geodesic length (read off the Weyl remainder) selects a preferred τ, that is a geometric (non-dynamical, non-variational) route to τ_fold that the FAILED one-loop/variational corridors (S95) did not try. Speculative, but concrete: compute the trace-formula remainder on `D_K(τ)` and look for a τ where the leading closed-geodesic length is stationary or commensurate with the transit scale.

---

## Closing — Highest-leverage next steps (spectral-geometry priority order)

1. **UB-1 / G3 — Test the spectral-dimension flow as the K→K* scale map.** Compute the anomalous scaling exponent `θ(σ) = d_s(σ) − 8` of the substrate's own return probability `P(σ) = Tr(exp(-σ D_K²))` over σ ∈ [fiber-UV, fold-window 1.4005], and test whether `∫ θ d ln σ` reproduces the ~3.1 e-folds needed to place K = 2.0 M_KK at K* ≈ 0.087 M_KK. **This reuses S92 machinery to attack the framework's single largest observational gap (A1).** Highest leverage.

2. **UB-2 / LB1+LB4 — Settle the isospectral-rigidity question at L_max=3.** Scan the Jensen modulus τ for any τ ≠ τ′ giving bit-identical `{a_0, a_2, a_4}` multisets while differing in a non-spectral invariant (Kosmann pairing `V_nm`). Outcome (i) = publishable structural strengthening (geometry IS spectrally reconstructible, Connes axioms pin it); outcome (ii) = a named falsifiable degeneracy. Either way it upgrades "bounds satisfied" → "operator determined (or not)."

3. **R2 + UB-3/UB-4 — Recompute the A_s amplitude floor as a near-floor-DOS / functional-determinant object under the SELECTED (n_s-canonical) functional.** Replace the 3.02× / 3.15 OOM / 9.47 OOM ambiguity with one regulator-tagged number, computed as `(M_KK/M_Pl)² ×` (a dimensionless near-floor spectral functional or `exp(−ζ'_D(0))`). Bridges G2↔G1 and tests whether the "permanent wall" is actually a substrate-IS prediction.

4. **R1 + R3 — Targeted regulator-pin retrofit + R_protected scheme reconciliation.** Tag the ~dozen `a_n` citations on live-prediction paths (not all 20k), and land one canonical registry line fixing the R_protected factor-2.33 scheme split (partial-sum 1.128655 vs Gilkey 0.492288) per CC-Ratios-Only (SHA `8a5678…4211`). Low effort, closes a factor-thousands latent risk (G4/C1) and a 2.33× ambiguity in the advertised protection anchor.

5. **UB-5 / A4 — Trace-formula remainder as a non-variational route to τ_fold.** Compute the Weyl-law oscillatory remainder on `D_K(τ)` and look for a τ where the shortest-closed-geodesic length is stationary/commensurate with the transit scale. Speculative but it attacks the one-dial question (A4) from a spectral direction the failed one-loop/variational corridors (S95) never tried.

**One-line summary**: The framework's dimensionless spectral predictions (the *intensive*, R-protected ratios — n_s, the couplings' ratios) are on solid heat-kernel footing; its weaknesses are all on the **dimensionful/extensive axis** (M_KK normalization, A_s amplitude, K-pivot scale) — and the most promising untraveled bridge is to let the substrate's OWN spectral-dimension flow (already computed at S92) do the UV→IR scale transport it was never asked to do.
