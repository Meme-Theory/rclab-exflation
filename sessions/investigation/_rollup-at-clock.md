# Convergence Rollup — a(t) / effective-Friedmann / τ↔t clock (CV-3)

**Synthesizer:** einstein-theorist.  **Date:** 2026-06-20.  **Digests netted:** inv-4, inv-7, inv-8.

**Method binding.** Per `_distillation-method.md`: this rollup runs the funnel in reverse for ONE convergence cluster (CV-3), netting the per-route four-verb tags of three Stage-1 digests into a single framework-level verdict. I authored the inv-4 digest in Stage 1; per the brief I treat all three as equal peer inputs. Register status verified against the knowledge MCP (atlas-04 C1/C2, §VII.BS rank-1 NNU STAGE-3-PERMANENT, the S101 `q-frame` open_channel, the white-hole zero-count gates, `w0_FW`, `V_spec monotone` closed-mechanism) — not re-adjudicated, but each load-bearing claim's register cell was read before it entered a verb.

**Disciplinary stance (my own, per agent-standards).** I own the structural characterization, not the probability — no P(pass) numbers here (that is Sagan's). I report this as a constraint map: constraint → implication → surviving space. Substrate-first throughout: the arrow is `D_K eigenvalues → a_n Seeley-DeWitt moments → emergent (a,τ) congruence → measurement`, never GR-first. The cluster's whole content is a statement about which Seeley-DeWitt moment can and cannot source a cosmological clock.

---

## 1. The convergence (inv-1 framing)

**The claim (CV-3, ≈7 vantages).** The framework's own #1 frontier. `phonon-first`, `hawking`, `schwarzschild-penrose`, `van-den-dungen`, `loop-quantum-gravity`, `gen-physicist`, `nazarewicz` independently surfaced: the rank-1 NNU theorem (§VII.BS, STAGE-3-PERMANENT) fixes the **conformal class** and the dimensionless tracking shape, but **a conformal class is not a cosmology — the Hubble backbone H(τ) is still imported**. The τ↔t map (atlas-04 **C1**: "τ parameterizes cosmic time", POSTULATED/ASSUMED since S1, register-confirmed) is the **master load-bearing assumption** of the entire cosmological sector.

**hawking's structural insight (the deepest of the whole survey).** The volume-preserving constraint **G6** that delivers the clean τ-independent `G_N` is the *same* constraint that empties the a₂ conformal clock: `a_eff` stationary ⇒ `q = 0/0`. This is now register-pinned as the S101 `q-frame` open_channel — *"the conformal class (a_eff const to 7e-7) / the representative (q = 0/0)."* In GR terms: the substrate supplies the conformal class `[g_M]` but the volume-preserving constraint annihilates the representative-fixing degree of freedom. a(t) has resisted derivation for ~40 sessions because it is not under-computed — it is **structurally absent from the a₂ channel by the same constraint that makes G_N clean**. The register had never named G6-buys-G_N and G6-empties-the-clock together as one entry; the survey's contribution was to identify them as a single structure.

**Bridge B-4 (the proposed fix).** Effective Friedmann `H²(ρ) + Raychaudhuri focusing` for the reduced `(a,τ)` congruence — LQC template `H²=(8πG/3)ρ(1−ρ/ρ_c)`; Raychaudhuri focusing. Proposers `loop-quantum-gravity`, `schwarzschild-penrose`, `phonon-first`. Cost: ~1 compute gate. §6.3 of the capstone itself names LQC's effective-Friedmann as the missing object.

**The prior (inv-1).** CV-3 stood as the #1 frontier — an OPEN conceptual hole. The three investigations were dispatched to test whether B-4 closes it: does the substrate DERIVE a(t), or does it resist?

---

## 2. Per-route ledger

One row per (investigation, route). Verdicts/verbs are the Stage-1 digest tags; magnitudes as the digests recorded them. "Verb" is the digest's four-verb classification of the finding's effect on CV-3 (or its sibling claim).

| Investigation | Route / gate | Verdict | Digest verb | Magnitude |
|:--------------|:-------------|:--------|:------------|:----------|
| **inv-4** | INV4-W2-2 — invariant Raychaudhuri (a,τ) congruence; q∝H, focusing-source decomposition | PASS | BOLSTERED + CLARIFIED | q_Raych=0.525833 (dev 0.0 vs S101); single un-fixed scale w=M_KK localizes to ONE rank-1 focusing term (SVD ratio 3.3e-17); focusing 99.97% a₂; internal geom pure Kasner shear σ²=5τ̇², tr K=0 |
| **inv-4** | INV4-W3-1 — a₀ de Sitter first law `dE=−T_dS dS_dS` → `ρ_vac=Λ/(8πG)` | PASS | CLARIFIED + BOLSTERED | c_track=3 Sage-exact; reduction_residual=0; makes DILUTION-CC "∼" exact; relocates expansion clock to a₀ |
| **inv-4** | a₀-vs-a₂ clock-location (cross-wave W2-2 vs W3-1) | — (MUDDLE) | **MUDDLED** | two POPULATED clock channels (a₀ expansion / a₂ focusing) with no composition law; uncertainty about WHERE the clock lives INCREASED (one-emptied-channel → two-populated-unreconciled) → WS-INV4-CLOCKLOC |
| **inv-4** | INV4-W2-1 — c_s(τ) zero-count from real a₂(τ) stiffness (white-hole) | PASS | CHALLENGED | N_zeros=1 single-asymmetric-open; van-Hove dip 0.485→0.473 too shallow to re-cross; S85 sealed-pair = modeling artifact |
| **inv-4** | INV4-W2-4 — dynamical Gregory-Laflamme KK-bubble (first compact-object structure) | PASS | BOLSTERED | min ω²_eff=−44.26 M_KK² at fold; λ_GL=0.944 M_KK⁻¹; static-limit reproduces GL-STABILITY-63 bit-for-bit; TRANSIENT (τ̇²-gated) |
| **inv-7** | INV7-W4-1 — effective-Friedmann-functional-form workshop (S_SA homogeneous-sector reduction) | closed (workshop) | CLARIFIED | binary→3-branch; "no bounce" SCOPED to Q≫1 corner (converged, zero dissent); SYMMETRIC-BOUNCE over-determined-EXCLUDED (3 legs); residual = sign of the a₄ R²+Weyl² (Starobinsky) operator → CF-INV7-W4-1-MINISUPERSPACE |
| **inv-7** | INV7-W1-3 — w₀=−0.918 vs DESI-DR2-BAO (late-time a₀ readout) | PASS | BOLSTERED | χ²/N=1.7399<4, Δχ²(FW−ΛCDM)=−22.06; the −0.509 exclusion was a superseded-INVERSION artifact; conditional on the U2 single-global-FRW borrow |
| **inv-7** | INV7-W1-4 — KBC void τ(ρ)-timescape (a₂ τ-clock, z≈0 face) | INFO | CHALLENGED (constructive) | ΔH₀/H₀=0.75% (Route B) vs ~9% needed; sign-correct (void clocks faster); Route A grav-backreaction 117 OOM short; w₀-orthogonal (a₂ not a₀) |
| **inv-7** | INV7-W3-2 — GFT controlled-sum of bare-divergent a₀ (5th A_s route) | INFO | CHALLENGED (constructive) | bare a₀ diverges; controlled sum converges (Cauchy 4.8e-4) but magnitude NOT condensate-pinned (ratio 2.27→0.91→0.43 over σ) |
| **inv-8** | INV8-W3-2 — quantum-metric stiffness → H(τ) (phonon-first route) | FAIL | CHALLENGED (corridor closed) | R_stiff=8.2e-18 (17 dec below); Tr g=4.0e-27≈0, Berry=1.4e-28≈0 EXACT by U(2)-invariance protection of the (0,0) Peter-Weyl singlet block |
| **inv-8** | INV8-W2-1 — Jacobson entanglement-equilibrium → CC (a₀ moment) | FAIL | CHALLENGED (sign BOLSTERED) | CC SIGN fixed Λ>0 (every chain factor +, Sage-verified); magnitude FAIL ~54 OOM; S62 "17-OOM" RETRACTED dimensionally-inconsistent |
| **inv-8** | INV8-W1-4 — finite-L analytic-continuation pole no-go (g_M=a₂^ζ self-computation) | PASS | BOLSTERED | conjecture→THEOREM; ζ-native diverges from above (Weyl +2), bare block converges from below 4.27× short; gap_factor=4.266426 Sage-exact |
| **inv-8** | INV8-W2-4 — running-vacuum a₀ tracking vs Solà RVM | FAIL | CHALLENGED (corridor closed) | same H²-power (n_eff=2 both) but coefficient O(1) vs O(ν); C10 stays CONFIRMED-TRACKING-FORM, NOT RG-grounded |
| **inv-8** | INV8-W3-1 — Kibble-Zurek Z₃ wall network → w_a (a(t)-adjacent dynamics) | FAIL | CHALLENGED (corridor closed) | walls admitted (homotopy correct) but fold is 0D regime (L/ξ̂=0.1546≪1); frozen-modulus w_a=0 lock stands sharper |
| **inv-8** | INV8-W3-3 — high-L_max spectral-dimension d_s(σ) vs CDT (UV→IR transport candidate) | INFO | CLARIFIED | d_s(σ→0)=8.02 Weyl; matched-window d_s(σ_*)=8.52 NOT ~2 → CDT reduction NOT a D_K-fiber property (it is M4-foam, S52); the "resonance" was a scale-type mismatch |

**Ledger reading.** Of the routes that bear directly on *deriving the backbone* (as opposed to the late-time readout): every constructive derivation route is CHALLENGED-corridor-closed (inv-8 W3-2 stiffness, W2-1 magnitude, W2-4 RG-grounding, W3-1 KZ-walls) or relocated-but-not-derived (inv-4 W2-2 focusing, W3-1 a₀-clock). The two genuine PASSes that touch a(t) directly (inv-4 W2-2, W3-1) are **invariant/relocation** results — they describe the structure of the (a,τ) congruence and pin the late-time tracking coefficient, but neither yields H(τ). The one BOLSTERED route that *constrains the derivation* (inv-8 W1-4) proves the substrate **cannot** compute its own dimensionful g_M at a pole on a finite truncation — a no-go that sharpens *why* the backbone resists. The late-time readouts (inv-7 W1-3 w₀, W1-4 timescape) are real PASSes/INFOs but are downstream of the borrowed FRW, not derivations of it.

---

## 3. NET framework verdict

**Dominant tag: CHALLENGED (corridor-closing), with a strong CLARIFIED facet and one localized MUDDLED.**

**Did the investigations DERIVE a(t)? No. Did they prove WHY it resists? Yes — on multiple independent axes, and the proof is now structural, not incidental.** This is the honest net, and it is the more durable outcome (a structural boundary outranks a numerical near-miss per `output-standards.md`).

The constraint map after inv-4/7/8:

- **CHALLENGED (dominant) — every constructive route to H(τ) closes.** Four independent attacks on *sourcing the backbone from substrate dynamics* all FAILED, and they failed for *different structural reasons*, which is the signal: (i) quantum-metric stiffness vanishes by **representation-theoretic protection** (U(2)-invariance of the (0,0) Peter-Weyl singlet block — Tr g≈0 EXACT, not a near-miss; inv-8 W3-2); (ii) Jacobson entanglement-equilibrium fixes the CC **sign but not magnitude** (~54 OOM; inv-8 W2-1); (iii) running-vacuum tracking is **not RG-grounded** (O(1) vs O(ν) coefficient; inv-8 W2-4); (iv) Kibble-Zurek wall dynamics **die in the 0D regime** (inv-8 W3-1). Per `epistemic-discipline.md`, these are boundaries that shrink the solution space — and they **strengthen the survivor**: H(τ) stays imported via the §VII.BS rank-1 NNU `w = M_KK` (STAGE-3-PERMANENT), and that import is now backed by a *proof that no substrate-dynamical route replaces it*, not by an absence of attempts. The single deepest boundary is the register-confirmed `q-frame` structure: the conformal class is fixed to 7e-7, the representative is `q = 0/0`. **The volume-preserving↔empty-clock identity is real and it closes corridors.**

- **CLARIFIED (strong facet) — the gap's structural ROOT is now named and pinned.** Before inv-4/7/8, CV-3 was "a(t) is imported (and we half-suspect why)." After, the root has three pinned pieces: **(a)** the a₂ conformal clock is emptied by G6 — the `q=0/0` representative-indeterminacy (S101 `q-frame`, register-confirmed); **(b)** the substrate provably *cannot* analytically continue its own dimensionful g_M to a pole on any finite truncation (inv-8 W1-4, conjecture→THEOREM, gap_factor=4.266426 Sage-exact) — so the dimensionful backbone is not extractable by the obvious spectral route; **(c)** the whole question of the *functional form* localizes to the sign of ONE operator — the a₄ R²+Weyl² (Starobinsky) term in the S_SA homogeneous-sector reduction (inv-7 W4-1), with SYMMETRIC-BOUNCE over-determined-EXCLUDED and "no bounce" cleanly scoped to the Q≫1 transit corner. CV-3 has been converted from an open conceptual hole into a **named, compute-decidable, single-operator residual**. That is genuine clarification: the truth-value ("imported") is unchanged; the precision and scope are sharply up.

- **MUDDLED (localized, one instance) — a₀-vs-a₂ clock-location.** inv-4's two clean PASSes *populate two different Seeley-DeWitt moments with a clock*: W2-2 puts the **focusing** clock on a₂ (99.97%, q∝H deceleration); W3-1 puts the **expansion** clock on a₀ (c_track=3, de Sitter vacuum tracking). Both are correct readings of *distinct* substrate observables, but the #1 frontier cannot have its clock in two moments without a composition law, and inv-4 did not (by design) adjudicate it. This is a genuine ledger-dissonance and the ONE workshop this cluster surfaces (§4). Crucially, it is a *progress-dissonance*: pre-inv-4 the C2 tension was "the a₂ conformal clock is **emptied**"; post-inv-4 it is "two channels are **populated**, unreconciled." The uncertainty moved from emptiness to over-population — a richer problem, but still unresolved.

**One genuine internal tension the net exposes (flag for the workshop).** inv-7 W4-1 localizes the open residual to "the sign of the a₄ R²+Weyl² Starobinsky operator," but the register carries a closed mechanism **`V_spec monotone` (S24a): "a_4/a_2=1000:1, no Starobinsky minimum."** These are not obviously consistent: if V_spec is monotone with no Starobinsky minimum, the a₄-operator sign may already be settled in the spectral-action *potential*, while W4-1 treats it as the open knob in the *homogeneous-sector effective Friedmann*. The two could be reconciled (potential-landscape vs Friedmann-reduction are different functionals — the same lesson as `p_S75 ≠ p_cosmo`), but the reconciliation is NOT in the register. The CF-INV7-W4-1-MINISUPERSPACE pre-registration MUST cite `V_spec monotone` as an input and state whether the minisuperspace a₄-sign is the same object or a distinct one — otherwise it risks re-deriving a settled sign or, worse, contradicting it silently. (This is the substrate-first analog of "computation beats gedankenexperiment when the potential landscape is nontrivial" — the landscape here is already partly mapped.)

---

## 4. Adversarial tensions (→ WORKSHOP, Q1)

**Honest count: ONE genuine cross-investigation workshop, plus one workshop-internal-already-converged tension I explicitly DECLINE to re-open, and one register-reconciliation I route to the CF pre-registration (not a workshop).** Per `Investigating-Workshops.md` honest-count discipline, I resist padding.

### WS-CV3-CLOCKLOC — a₀-vs-a₂ cosmological-clock location (the one genuine Q1)

**The dissonance.** Two competing readings of *which Seeley-DeWitt moment carries the cosmological clock*, both backed by clean PASSes on distinct observables, that cannot both be the sole answer:

- **a₂ side** (inv-4 W2-2, `inv4_w2_raychaudhuri_focusing.npz`): the clock is the **focusing** source — q∝H deceleration lives in the a₂ Einstein-Hilbert grade (99.97%), internal geometry a pure Kasner shear. This is the channel hawking's insight says is *emptied* by volume-preservation (`q=0/0`) — yet W2-2 reads a populated focusing clock off it. That juxtaposition (emptied conformal clock vs populated focusing source, same grade) is itself part of the tension.
- **a₀ side** (inv-4 W3-1, `inv4_w3_de_sitter_clock_tracking.npz`): the clock is the **expansion** source — de Sitter vacuum-energy tracking, c_track=3, the DILUTION-CC channel. W3-1 deliberately relocates the clock here to *sidestep* the emptied-a₂ problem.

**Why Q1 (all markers fire).** The disagreement is about WHAT the clock IS (vacuum-energy conjugacy vs Ricci-focusing source-grade); the two readings invoke different machinery (a₀ Gibbons-Hawking first law vs a₂ Raychaudhuri focusing); they cannot both be the sole answer — the workshop must derive whether a₀ is primary, a₂ is primary, or both are simultaneously correct (a₀ for expansion-energy tracking, a₂ for focusing) **with an explicit composition law** + a decisive forward gate. Not a status-tag edit, not a mechanical promotion — a substrate-physics adjudication producing a NEW structural verdict.

**Cross-investigation scope (why this is a Stage-2 workshop, not an inv-4-local one).** inv-4 identified WS-INV4-CLOCKLOC as its single genuine workshop; the cross-investigation rollup *confirms and widens* it. inv-7's W4-1 attacks the SAME backbone from the **Friedmann-functional-form** angle (S_SA homogeneous reduction, a₄-operator sign) — a third clock-candidate channel (a₄), orthogonal to inv-4's a₀/a₂ pair. The workshop should net all three: **does the cosmological clock live in a₀ (expansion-tracking), a₂ (focusing), or a₄ (Starobinsky-driven dynamics), and what is the composition law across the three Seeley-DeWitt grades?** This is the natural superset of inv-4's a₀-vs-a₂ and inv-7's a₄-sign residual.

- **Both sides, ≥2 axes.** a₀/expansion side: hawking (de Sitter first law) + mack (cosmological tracking) + volovik (q-theory vacuum partition). a₂/focusing side: schwarzschild-penrose (Raychaudhuri/causal structure) + einstein (Kasner shear / EIH grade). a₄/Friedmann-form side: loop-quantum-gravity (effective-Friedmann template) + the W4-1 co-authors. R1 steelman / R2 respond / R3 converge on a grade-composition verdict.
- **Inputs.** `inv4_w3_de_sitter_clock_tracking.npz` (c_track=3); `inv4_w2_raychaudhuri_focusing.npz` (99.97% a₂); the S101 `q-frame` open_channel + atlas-04 C1/C2; `S101-W1-QEQ-SELFCONS`; the inv-7 W4-1 workshop doc (a₄-sign residual); the `V_spec monotone` S24a closed mechanism (the a₄-sign constraint that must be reconciled).
- **Output.** A structural verdict pinning the clock-grade composition (which grade is primary for which observable; the composition law) + a single decisive forward gate. This is the prerequisite that scopes CF-INV7-W4-1-MINISUPERSPACE and the inv-4 HK-4 a₀-clock promotion.

### DECLINED (already converged in-cluster — NOT re-opened)

- **inv-7 W4-1 effective-Friedmann-functional-form workshop.** This RAN and CONVERGED (zero dissent on the Q≫1 "no bounce" scoping; SYMMETRIC-BOUNCE over-determined-EXCLUDED on three independent legs). Its residual is a COMPUTE (the a₄-operator sign), not a reading-divergence. Per `Investigating-Workshops.md`, a converged workshop's compute-residual is a carry-forward, NOT a new workshop. I fold its a₄-sign question into WS-CV3-CLOCKLOC as the third grade-channel (above) rather than re-running it standalone — the clock-location adjudication is the natural home for "which grade sources the dynamics."
- **inv-8 W2-1 / W3-2 (the constructive-derivation attacks).** Both produced clean CHALLENGED verdicts with no surviving two-reading tension — they CLOSED corridors. Not workshops.

### ROUTED TO CF-PRE-REGISTRATION (not a workshop — a Q2-adjacent reconciliation)

- **`V_spec monotone` (S24a) vs the W4-1 a₄-sign residual.** As flagged in §3, this is a register-consistency question (is the minisuperspace a₄-sign the same object as the settled spectral-action potential sign?). It is NOT a two-agent adversarial tension — it is an input-reconciliation the CF-INV7-W4-1-MINISUPERSPACE pre-registration must perform (cite `V_spec monotone`; state same-object-or-distinct). Routes to the compute-CF's PRDR pin block, not the workshop schedule.

---

## 5. Validated forward bridges (→ COMPUTE-CF)

**B-4 status: PARTIALLY VALIDATED, RELOCATED to a single-operator compute.** B-4 (LQC effective-Friedmann + Raychaudhuri) was the proposed fix. The investigations executed its *Raychaudhuri half* (inv-4 W2-2 — the invariant focusing equation for the (a,τ) congruence PASSED, q∝H reproduced) but found its *effective-Friedmann half* localizes to the a₄-operator sign (inv-7 W4-1). So B-4 is not "run it and close the gap"; it is "the focusing structure is in hand; the H²(ρ) form is one operator-sign away, GATED on the clock-location adjudication (§4)." The LQC template itself is confirmed structurally analogous (inv-8 W3-3 even rules out one false lead — the CDT reduction the LQC analogy might have predicted is an M4-foam effect, not a D_K-fiber property, so the LQC analogy must be applied to the M4 summand, not the SU(3) fiber).

The validated forward computes, EVOI-ordered (leverage × P(pass)-sensitivity; ordinal, not calibrated):

### CF-1 — CF-INV7-W4-1-MINISUPERSPACE (HIGHEST EVOI — the backbone-form decider)

1. **What:** reduce `S_SA(τ) = a₀ − a₂ + a₄` to the homogeneous-isotropic sector; read off `sign(∂H²/∂ρ_relic)` across a ρ-grid, deciding MONOTONE-RAMP vs orphan ONE-SIDED-CEILING — the whole functional-form question localizes to the sign of the a₄ R²+Weyl² (Starobinsky) operator.
2. **Inputs:** `a_n_FW_zeta` (a₀=6440 / a₂=2776.165389 / a₄=1350.7216) + τ-derivatives; `ρ_relic=26.553854 M_KK`; `λ_min(τ)` (S17a, 0.790); G_eff; M_KK; the S20a a₂/a₄ balance theorem; **MANDATORY: `V_spec monotone` (S24a) cited as input with a same-object-or-distinct declaration** (§4 reconciliation); **GATED on WS-CV3-CLOCKLOC** (the clock-grade composition scopes which grade the H²(ρ) form reads from).
3. **Gate:** `MINISUPERSPACE-EFFECTIVE-FRIEDMANN-FORM` — PASS=MONOTONE (both reduction schemes agree); INFO=schemes SPLIT (scheme-dependent form); FAIL=ill-posed. Pre-register `reduction_scheme ∈ {gap-as-density-ceiling, holonomy-analog}` (BOTH run) + μ̄-analog vs μ_0-analog pin (convention-shoppable without the pin, PROHIBITED_ACTIONS Class 1).
4. **Effort:** ~1–2 sessions (trajectories cached; new work is the reduction + constraint inversion).

**EVOI: HIGHEST.** Closes the framework's #1 frontier as far as it can be closed — δ_P large on every branch (MONOTONE strengthens §6.2 beyond the scoped form; CEILING refutes the monotone prior; SPLIT is itself a finding). This is the inv-7 digest's top item and the cluster's top item.

### CF-2 — CF-INV4-W3-1-CLOCKLOC composition gate (HIGH EVOI — the §4 workshop's decisive forward gate)

1. **What:** the decisive forward gate the WS-CV3-CLOCKLOC workshop pre-registers — a composition-law test deciding whether a₀ (expansion-tracking), a₂ (focusing), and a₄ (Friedmann-form) compose into a single consistent (a,τ) dynamics, or whether one grade is primary.
2. **Inputs:** the two inv-4 npz (c_track=3; 99.97% a₂); the S101 `q-frame` structure; `S101-W1-QEQ-SELFCONS`; the WS-CV3-CLOCKLOC R3 verdict (this CF is *downstream* of the workshop).
3. **Gate:** pre-registered by the workshop (composition-consistency PASS / single-grade-primary INFO / inconsistent FAIL).
4. **Effort:** ~1 session, after the workshop.

**EVOI: HIGH.** Without it the two populated clock channels remain unreconciled (the §3 MUDDLE persists); it is the structural prerequisite that the a(t) backbone needs before any H(τ) magnitude is meaningful.

### CF-3 — CF-INV7-W1-4 emergent-scale timescape refinement (MEDIUM EVOI — the late-time a₂ τ-clock face)

1. **What:** the KBC void τ(ρ)-timescape delivered 0.75% H₀ relief (sign-correct, ~8% short). Test whether the a₂ τ-clock channel, propagated through an emergent-scale transport (the same `deg(T_{BZ→pivot})` machinery the n_s/α_s observables need), reaches the ~9% target without a fitted knob.
2. **Inputs:** `inv7_w1_4` npz (ΔH₀/H₀=0.75%, clock_coeff=−3.08); the `deg(T_{BZ→pivot})` transport machinery; the KBC-void amplitude; w0_FW=−0.918 (the orthogonal a₀ channel, for the CV-4 mutual-exclusivity cross-check).
3. **Gate:** `TIMESCAPE-EMERGENT-TRANSPORT` — PASS iff substrate-natural NON-SCALAR transport lands ΔH₀/H₀ in the [target] band; INFO iff transport is fitted/scalar; FAIL iff no substrate-natural transport reaches the target.
4. **Effort:** ~1–2 waves (gated on `deg(T_{BZ→pivot})` being independently derived — shared dependency with the n_s/α_s sector).

**EVOI: MEDIUM.** The a₂ τ-clock is one of two orthogonal late-time channels (the other is w₀ on a₀); a PASS would give the framework a native Hubble-tension mechanism, but the magnitude shortfall + the shared transport dependency keep it medium.

**NOT carried as compute-CFs (corridors CLOSED, recorded not carried):** quantum-metric stiffness → H(τ) (inv-8 W3-2, U(2)-protection, structural); Jacobson → CC magnitude (inv-8 W2-1, ~54 OOM); running-vacuum RG-grounding (inv-8 W2-4, O(1) vs O(ν)); Kibble-Zurek w_a (inv-8 W3-1, 0D regime); CDT reduction on the D_K fiber (inv-8 W3-3, it lives in M4). These are the boundaries that make the surviving import-of-H(τ) the structurally-forced position.

---

## 6. Cross-links to other convergences

- **CC / dark cluster (CV-4) — the CC↔H₀ mutual-exclusivity is the load-bearing cross-link.** mack's inv-1 insight: ONE tracking-vacuum DOF (the ρ_vac~H² freedom) drives 3 of 5 cosmology gaps, and the H²-freedom the CC closure needs **structurally forbids a fixed-magnitude H₀**. This cluster supplies BOTH sides of that exclusivity: inv-7 W1-3 (w₀=−0.918 PASS on a₀, the CC-tracking channel) and inv-7 W1-4 (the a₂ τ-clock, the H₀ channel) are *the two orthogonal late-time channels the mutual-exclusivity says cannot both be free*. inv-4 W3-1's c_track=3 makes the a₀ tracking law exact, sharpening the CC side. **Stage-2 action for the CC/dark rollup:** the a(t)/clock net and the CC net must be reconciled jointly — the a₀ clock (here) IS the DILUTION-CC channel (there); the clock-location workshop (§4) and the CC-magnitude rollup share the a₀ moment as their common object. The de Sitter a₀ horizon (inv-4 W3-1) is the explicit hand-off to the CC cluster.

- **M_KK / scale cluster (CV-2) — M_KK is the missing scale, and it is THE remaining import.** The §VII.BS rank-1 NNU theorem (STAGE-3-PERMANENT) proves `w = M_KK` is the sole imported dimensional weight; the a(t) backbone is exactly the place that import bites (H(τ) carries the M_KK weight that the conformal class cannot fix). inv-8 W1-4's finite-L pole no-go is the cross-cluster bridge: the substrate provably cannot self-compute its dimensionful g_M (=a₂^ζ) on a finite truncation — the *same* structural reason M_KK must be imported is the reason a(t) cannot be derived from the spectral moments alone. **Stage-2 action:** the M_KK rollup's "derive M_KK by dimensional transmutation" (B-2) and this cluster's "H(τ) is imported" are the SAME hole viewed from the scale axis vs the dynamics axis — if B-2 derives M_KK, it does NOT thereby derive a(t) (the conformal-class/representative split is orthogonal to the scale-magnitude fix); the two rollups must state this non-implication explicitly so the planner does not treat M_KK-derivation as closing a(t).

- **A_s cluster (CV-1) — shared a₀ object via the 5th route.** inv-7 W3-2's GFT controlled-sum of the bare-divergent a₀ is simultaneously an A_s-route data point and an a(t)-adjacent result (a₀ is the de Sitter/expansion-clock moment). The convergence-but-no-magnitude-pin outcome parallels the a(t) net: structure resolves, dimensionful magnitude does not. **Stage-2 action:** the A_s rollup should note that the a₀ moment is contested across BOTH clusters (A_s floor vs cosmological clock) — the same `a_0_FW_zeta=6440` carries an A_s normalization role AND an expansion-clock role; whichever cluster pins its magnitude first informs the other.

- **CDT / spectral-dimension (CV-6) — closed as an a(t) lead, redirected to M4.** inv-8 W3-3 (with inv-3) establishes the CDT dimensional-reduction is an M4-foam effect, NOT a D_K-fiber property. For the a(t) cluster this CLOSES the "run d_s(σ) as the UV→IR transport that sources H(τ) on the fiber" lead and redirects it: any d_s-flow contribution to the backbone must come from the M4 summand (the d_s≈3.91 channel n_s needs), not the SU(3) fiber. **Stage-2 cross-link to the CV-6 rollup:** the transport map `deg(T_{BZ→pivot})` (shared dependency of CF-3 here and the n_s/α_s sector) is the object that must be derived on the M4 summand — it is the common missing piece across a(t)-timescape, n_s, α_s, and the LRD-temperature axis.

- **Compact-object cluster (CV-9) — adjacent, via the transit-phase KK bubble.** inv-4 W2-4's transient Gregory-Laflamme KK bubble (λ_GL=0.944 M_KK⁻¹) is a transit-phase object gated by τ̇² — i.e., it exists *because* of the a(t) dynamics (the contracting-SU(2) extrinsic term). Whether it is the same object as inv-7/inv-8's PBH-from-fold-transit (both FAILED on abundance) is a compact-object-cluster question, but the dependency runs through a(t): the bubble's growth-rate-vs-transit-duration (inv-4 CF-2) needs the transit kinematics the backbone sets.

---

*Track-local boundary (`gate-verdicts.md`): every CF and register reconciliation above is investigation-track exploratory. None mutates a curated register; they enter the permanent index only when promoted into a `session-{N}` gate via `/rclab-plan` session-mode. The WS-CV3-CLOCKLOC workshop and the three CFs are the promotion targets.*
