# Investigation 7 Workshop: loop-quantum-gravity × transit-dynamics — Effective-Friedmann Functional Form (bounce vs monotone)

**Date**: 2026-06-15
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Gate**: INV7-W4-1 (`gate_type: workshop`; closes by artifact-existence — NO verdict line)
**Agents**: loop-quantum-gravity (loop-quantum-gravity-theorist) ↔ transit-dynamics (transit-dynamics-theorist)
**Neutral planner / non-participant**: gen-physicist (authored the balanced spec; not a participant)
**Source Documents**:
- `sessions/investigation/investigation-1/loop-quantum-gravity-theorist.md` (survey §UB-1 / G-1 / U-1 / C-3 — effective-Friedmann frontier + borrowed-clock tension)
- `sessions/investigation/investigation-1/cosmic-web-theorist.md` (survey §U2 / B2 — single-global-FRW assumption; τ(ρ)→H_local downstream consumer)
- `sessions/framework/phonic-exflation-equation.md` (capstone §6.3 — names the missing object H²=f(ρ_relic, S_SA); §6.2 — the "no bounce, asymmetric white hole" prose to scope)
- `sessions/permanent-results-registry.md` (§VII.BS rank-1 NNU — a(t) SHAPE derived, STAGE-1-CANDIDATE S102; dynamical EQUATION not)
- `sessions/archive/session-96/workshops/session-96-NYT-Q2-lqc-bounce-vs-transit.md` (PRIOR round of the SAME tension; read as input, not a re-plan)

**Adjudication question (carry VERBATIM; the panel resolves a STRUCTURAL VERDICT on the functional form)**:
TENSION: Reduce the spectral-action functional S_SA(τ) = a_0(τ) − a_2(τ) + a_4(τ) [a_0_FW_zeta=6440, a_2_FW_zeta=2776.165389, a_4_FW_zeta=1350.7216 at the fold] to the homogeneous-isotropic sector and read off the substrate effective-Friedmann H²(ρ_relic), with M_KK / the spectral gap as the ceiling.
- **(a)** Does the homogeneous-sector reduction of S_SA(τ) yield a TURNING POINT (an effective ρ_c ceiling → a loop-quantum-cosmology `(1−ρ/ρ_c)` bounce-form, turning point at ρ_c ≈ 0.41 ρ_Pl) or a MONOTONE H²(ρ) (supersonic transit, monotone dS/dτ — the framework's stated "no bounce")?
- **(b)** Is the framework's "no bounce" a GENERAL TRUTH or SCOPED to monotone-RAMP transit dynamics (the same singularity-resolution ROLE played by two structurally distinct mechanisms — a polymer bounce vs an irreversible supersonic quench)?
- **(c)** What single compute (the minisuperspace effective Hamiltonian constraint with M_KK / the spectral gap as ceiling) decides the functional form? Name the producing script, its inputs, and its PASS criterion so the verdict's deliverable becomes a session-track carry-forward compute.

**Competing positions (stated at equal weight; the planner pre-judges NEITHER)**:
- **loop-quantum-gravity** holds the LQC effective-Friedmann TEMPLATE: singularity-resolution via a BOUNCE at ρ_c. Canonical LQC reference H² = (8πG/3)ρ(1 − ρ/ρ_sup), ρ_sup = 18πGℏ²/Δ³ ≈ 0.41 ρ_Pl; Ḣ = −4πG(ρ+p)(1 − 2ρ/ρ_sup). If the substrate's homogeneous reduction carries an effective gap-like ceiling (M_KK / spectral-gap as the LQC area-gap Δ analog), the reduced H²(ρ) should inherit a `(1−ρ/ρ_c)` turning point ⇒ "no bounce" is SCOPED, not a refutation of bounce cosmology.
- **transit-dynamics** holds the SUPERSONIC-TRANSIT non-equilibrium reading: first-order transit through the van Hove fold at τ_fold=0.190, Mach 13.75, P_exc=1, monotone dS/dτ ⇒ an IRREVERSIBLE impulsive quench with NO turning point; H²(ρ) monotone; the white hole causally asymmetric; "no bounce" a GENERAL feature.

**Substrate framing (load-bearing)**: the substrate IS the S_SA(τ) ramp; a(t) is its emergent homogeneous-sector readout. Chain: D_K(τ) eigenvalues → S_SA spectral-action moments → (the missing back-reaction closure) → emergent homogeneous field equation → H²(ρ_relic) → a(t). Do NOT invert to "the substrate expands in an FRW container."

**INDEPENDENCE NOTE**: EXPLORATORY adjudication — domain advocates argue their case (NOT a Stage-2 joint-theorem cross-check; the no-prior-context rule does NOT apply; both SHOULD read the S96 NYT-Q2 prior round).

**Investigation-track scope (track-local boundary)**: this workshop MUST NOT mutate session-track curated registers (capstone §6.2/§6.3 prose, Atlas, permanent-results-registry, canonical_constants.py, knowledge-MCP). The capstone scope-edit (HY6: "no bounce" → "no bounce UNDER monotone-ramp") that CONSUMES this verdict is a session-promotion item NAMED in Carry-Forward Computations, recorded for `/rclab-investigate --investigation 7` to lift into the session-track housekeeping ledger — NOT effected here.

---

## Round 1 — loop-quantum-gravity: Opening Analysis (LQC effective-Friedmann template lens)

### L-a: Sub-question (a) — turning-point bounce-form vs monotone H²(ρ)

**Position.** The loop-quantum-cosmology effective-Friedmann TEMPLATE says: *if* the homogeneous reduction of `S_SA(τ)` carries a gap-like density ceiling, then `H²(ρ)` inherits a `(1 − ρ/ρ_c)` turning point. The single-τ-slice moment triple at the fold (`a_0_FW_zeta = 6440`, `a_2_FW_zeta = 2776.165389`, `a_4_FW_zeta = 1350.7216`; `S_SA(τ_fold) = a_0 − a_2 + a_4 = 5014.556211`, this-run check) does **not** by itself decide the sign of `∂H²/∂ρ`. It fixes `S_SA` and the *sign* of `G_eff` (`a_2 > 0 ⇒ G_eff ∼ M_KK²/(16π a_2) > 0`, attractive emergent gravity), but a slice is a *point*, not the *function* `H²(ρ)`. The turning-point-vs-monotone question is a statement about how the homogeneous energy density enters the **reduced constraint**, which a static moment set is structurally silent on. So my honest opening is: **the functional form is UNDECIDED by the data presently on the table, and the loop-quantum-cosmology template supplies the one structural reason to expect a turning point.**

**The loop-quantum-cosmology reference (carried VERBATIM from the S96 NYT-Q2 prior round; my corpus Paper 17, APS 2006).** The polymer-quantized homogeneous-isotropic sector yields the *exact* effective Friedmann equation
$$
H^2 = \Big(\frac{\dot a}{a}\Big)^2 = \frac{8\pi G}{3}\,\rho\,\Big(1 - \frac{\rho}{\rho_{\rm sup}}\Big),
\qquad
\rho_{\rm sup} = \frac{18\pi G\hbar^2}{\Delta^3} \approx 0.41\,\rho_{\rm Pl},
$$
with `Δ = 4√3π γ ℓ_P²` the area gap (hence `ρ_sup` fixed by the Immirzi parameter γ; U(1)-Chern-Simons convention `γ_0 = ln2/(π√3)`, my MEMORY.md pin). The **single load-bearing structural feature**: the quantum-geometry discreteness scale — the area gap, the spectral floor of the area operator (Rovelli–Smolin 1995) — enters the *macroscopic* dynamical equation as **one density ceiling** and nothing else. That is the entire template. It is not a slow-roll potential, not an inflaton; it is a kinematic ceiling read off the gap.

**Substitution chain — does `∂H²/∂ρ` change sign? (per `math-scripts.md §"Double-Check Logic Before Compute"`).** I make the directional claim explicitly and then read it off the canonical form; I do NOT assert a sign from narrative.
- **Step 1 — definition.** `H²(ρ) = (8πG/3) ρ (1 − ρ/ρ_c)` (loop-quantum-cosmology template; `ρ_c` the gap-built ceiling).
- **Step 2 — differentiate.** `∂H²/∂ρ = (8πG/3)(1 − 2ρ/ρ_c)`.
- **Step 3 — substitute the ceiling.** The numerator `(1 − 2ρ/ρ_c)` vanishes at `ρ = ρ_c/2` and reverses sign across it.
- **Step 4 — read off (this-run Sage-free float check, `s_check` above).** `ρ/ρ_c = {0.00, 0.25} ⇒ sign-factor = {+1.0, +0.5}` (H² **rising**); `ρ/ρ_c = 0.50 ⇒ +0.0` (**turning point**, `H = 0`, `Ḣ < 0` finite); `ρ/ρ_c = {0.75, 1.0} ⇒ {−0.5, −1.0}` (H² **falling** to zero at the ceiling).
- **Conclusion.** A loop-quantum-cosmology-form `H²(ρ)` has a turning point at `ρ_c/2` and a bounce surface at `ρ_c` BY CONSTRUCTION of the `(1 − ρ/ρ_c)` factor. The MONOTONE reading (transit-dynamics' "no bounce") is the *complementary* hypothesis: `∂H²/∂ρ > 0` throughout, no sign change, `H` does not vanish before the fold is crossed.

**The decisive structural observation — the framework HAS the ceiling ingredient.** This is the core of my case, and it is sharper than the prior round had access to. The framework carries a PROVEN **spectral gap**: `D_K` has `λ_min > 0` at all τ; the total gap `min = 0.790 at τ = 0.27`, and it **never closes on the Jensen line** (knowledge-MCP `W3: Spectral Gap`, S17a, PROVEN; cross-check `constraint-mega-matrix`). A gap that never closes is exactly the loop-quantum-gravity *area-gap* situation: a discreteness floor below which there is no geometry. In loop quantum cosmology that floor IS what becomes `ρ_c`. **So the framework possesses a non-vanishing discreteness scale of precisely the kind that, in the loop-quantum-cosmology reduction, produces a `(1 − ρ/ρ_c)` ceiling.** The template's antecedent ("if the homogeneous reduction carries a gap-like ceiling") is *not* hypothetical for this substrate — the substrate demonstrably has a never-closing spectral gap. What is open is whether that gap *propagates into the homogeneous-sector constraint as an energy-density ceiling* (loop-quantum-cosmology bounce) or *is bypassed by the monotone supersonic transit* (no ceiling reaches the homogeneous readout). That propagation question is the compute of L-c.

**Honest scoping (the boundary of my case).** I do not claim the framework's `H²(ρ)` IS the loop-quantum-cosmology bounce. The prior round (S96 NYT-Q2 §L2) already established, in the GFT-condensate reduction, that the source leg `H² = (8πG_eff/3)ρ_relic` transfers but the bounce factor `(1 − ρ/ρ_crit)` was **structurally ABSENT** (`BOUNCE_transfers = False`, S96-W1 §W1-5, INFO). That is a real and weighty datum on the monotone side. But it is a datum about the *GFT-condensate ansatz*, which assumes a **quasi-equilibrium mean field** — and the framework's relic is the opposite limit (diabatic sudden quench, `P_exc = 1.000`). The GFT route therefore tests "does a quasi-equilibrium condensate re-expand," and answers no; it does NOT test "does the *gap* in the *bare* `D_K` minisuperspace constraint impose a ceiling." Those are different objects. The loop-quantum-cosmology bounce comes from the **holonomy correction to the gravitational Hamiltonian constraint** (`sin²(μ̄ c)/μ̄²` replacing `c²`), *not* from a condensate. The framework's analog is the gap-bounded spectrum entering the *reduced* `S_SA`, which the GFT ansatz never probed. **My (a)-position: TURNING POINT is live and under-tested; the absent-GFT-bounce is scoped to the equilibrium-condensate route and does NOT settle the minisuperspace-constraint route.** Verdict on (a) is properly *Partial/Emerged pending the L-c compute* — not a foregone monotone.

### L-b: Sub-question (b) — is "no bounce" general or monotone-ramp-scoped?

**Position: SCOPED, not general. "No bounce" is a property of monotone-ramp transit dynamics, not a refutation of bounce cosmology.** This is the cleaner of my two sub-question stances, and it is the one I am most confident the verdict will converge on — because it is a *scoping* claim, not a competing dynamical prediction.

**The argument (substrate-first; the singularity-resolution ROLE is realized by two structurally distinct mechanisms).**
- The framework's own dynamics forbid a *symmetric* bounce: monotone `dS_SA/dτ` (`dS/dτ = +58,673` at the fold), an irreversible diabatic quench (`P_exc = 1.000`, `S_ent = 0`), and an acoustic white hole over-determined at six independent causal walls (capstone §6.2, S85 PROVEN). I accept this completely. A time-symmetric throat with `H = 0` and re-expansion is structurally absent from THIS substrate's transit. The capstone §6.2 prose ("no symmetric throat, no bounce") is *correct about the framework's monotone-ramp mechanism*.
- But "no bounce *under monotone-ramp transit*" is NOT "no bounce *in general*." Loop quantum cosmology produces a bounce precisely BECAUSE its effective dynamics has a turning point at `ρ = ρ_c` — a **different dynamical structure**, not a worse or refuted one. The singularity-resolution *role* — "the deep regime is traversed by finite action, not a divergence" — is played by two structurally distinct mechanisms: a **time-symmetric polymer bounce** (loop quantum cosmology; `w_eff → −∞` at the bounce, `ρ` re-expands from `ρ_c`) versus an **irreversible supersonic quench** (the framework; monotone `H²(ρ)`, causally asymmetric white hole). Both replace the singularity with a finite-action transit; the mechanisms are NON-ANALOGOUS; the role is STRUCTURAL.
- This is exactly the tag my S92 cross-framework comparison already carried — **STRUCTURAL-at-role, ANALOGICAL-at-mechanism** — and which my investigation-1 survey (§C-3, §R-4) flagged as a place the capstone has *drifted* toward presenting "no bounce" as a general result. The drift is the substantive issue (b) resolves: the capstone §6.2 prose should read **"no bounce UNDER monotone-ramp transit dynamics,"** leaving open that a substrate whose homogeneous reduction *did* inherit a `ρ_c` ceiling (a counterfactual, OR — per L-a — possibly this very substrate, pending L-c) would resolve the singularity by a bounce instead.

**Why this matters for the framework's epistemic hygiene (not just diplomacy toward loop-quantum-gravity).** An unscoped "no bounce" claims to have *refuted the loop-quantum-cosmology bounce*. The framework has done no such thing — it has established that *its own* monotone-ramp dynamics realize singularity-resolution differently. Claiming refutation of an alternative one never tested is the same epistemic error the framework polices elsewhere (asserting a general truth from a mechanism-specific result). The scoped form is *stronger*, not weaker: it converts a vulnerable over-claim into a precise structural statement (singularity-resolution role: STRUCTURAL; bounce mechanism: framework-monotone, loop-quantum-cosmology-symmetric, NON-ANALOGOUS) and names the falsifiable discriminator (does the homogeneous reduction carry a ceiling — L-c).

**The honest tension I must flag to transit-dynamics.** There IS a reading on which (b) collapses toward "general": if the L-c compute returns *monotone* (no ceiling propagates), AND one accepts the Two-Manifold Non-Embedding Theorem (S74) as forbidding ANY single Friedmann trajectory across the fold, then for THIS substrate "no bounce" is general *for the cosmogenesis bridge* (not merely scoped to a choice). The prior round's transit response (S96 §Re:L2) made exactly this move: the source leg is post-fold-only, so the *origin* IS the bridge, which the non-embedding theorem forbids embedding — conceding cosmogenesis to the monotone/incompatible side. I take that argument seriously. My counter is that it scopes "no bounce" to *this substrate's* monotone transit + the non-embedding theorem's domain — which is still narrower than "no bounce is a general feature of singularity-resolution." The cleanest verdict: **(b) = "no bounce" is SCOPED to monotone-ramp transit dynamics; the singularity-resolution role is STRUCTURAL across both mechanisms; whether THIS substrate is monotone or carries a hidden ceiling is the L-c compute.** That is a convergence target, and I expect transit-dynamics and I to land on it with at most a residual dissent over whether the non-embedding theorem widens "scoped" back toward "general for the bridge."

### L-c: Sub-question (c) — the single minisuperspace compute that decides it

**The deciding compute (the loop-quantum-cosmology *move*, transcribed to the substrate).** The functional form is decided by ONE computation: **reduce the spectral action `S_SA(τ)` to the homogeneous-isotropic minisuperspace sector, write the effective Hamiltonian constraint with the spectral gap as the candidate ceiling, and read off `H²(ρ_relic)` — testing whether `∂H²/∂ρ` changes sign (loop-quantum-cosmology bounce-form) or stays positive (monotone transit).** This is exactly the object capstone §6.3 names as missing ("a back-reaction closure `H² = f(ρ_relic, S_SA)`") and exactly the loop-quantum-cosmology effective-Friedmann object. The compute is a genuine math/physics adjudication (bounce-form vs monotone-form correction), which is why it belongs as a session-track carry-forward, not a hygiene item.

**Producing-script spec (4-field, per the Carry-Forward discriminator; full PRDR machinery pin acquired at session-promotion — track-local boundary).**

- **What**: `inv7_minisuperspace_effective_friedmann.py` — construct the homogeneous-isotropic reduction of `S_SA(τ) = a_0(τ) − a_2(τ) + a_4(τ)`, form the minisuperspace effective Hamiltonian constraint `H_eff[a, p_a; D_K]`, solve `H_eff = 0` for `H² = (ȧ/a)²` as a function of the homogeneous relic energy density `ρ_relic`, with the PROVEN spectral gap `λ_min(τ)` (never-closing, `min = 0.790 at τ=0.27`; `W3: Spectral Gap`, S17a) supplying the candidate density ceiling `ρ_c ∼ f(λ_min, M_KK)`. Emit `H²(ρ)` on a ρ-grid spanning `[0, ρ_relic^max]` and compute `sign(∂H²/∂ρ)` across the grid.
- **Inputs**:
  - `a_n(τ)` moment trajectories (zeta-regulated): `a_0_FW_zeta = 6440`, `a_2_FW_zeta = 2776.165389`, `a_4_FW_zeta = 1350.7216` at `τ_fold = 0.190` (and their τ-derivatives, for the reduced kinetic term) — `canonical_constants.py`.
  - `ρ_relic = 26.553854 M_KK` (B1+B2+B3 band sum; S96-W1 §W1-5) — the homogeneous source density.
  - `λ_min(τ)` spectral-gap trajectory (`W3: Spectral Gap`, S17a; `s17a`-cache or recompute on the `D_K(τ)` block-diagonal spectrum) — the candidate ceiling source.
  - `G_eff = M_KK²/(16π a_2)` (induced Newton constant; capstone §6.3, `8πG_eff/3 = 5.601903e-38`) — the source-leg prefactor.
  - `M_KK = 7.428660036284456e16 GeV` (knowledge-MCP, CONST-FREEZE-42) — the single imported scale.
- **Gate (PASS criterion — pre-registered directional, `[SIGN]`-trigger)**: compute `s_∂ ≡ sign(∂H²/∂ρ)` across the ρ-grid.
  - **BOUNCE-FORM (loop-quantum-cosmology-type)**: `s_∂` changes sign at some `ρ* ∈ (0, ρ_relic^max)` (a turning point), with `H² → 0` at a ceiling `ρ_c` — verdict tag `TURNING-POINT`.
  - **MONOTONE (transit-type)**: `s_∂ = +1` throughout the grid (no sign change, no ceiling reached) — verdict tag `MONOTONE-RAMP`.
  - **PASS/FAIL/INFO mapping**: the gate is *INFO-bearing on the functional form* — it does not "pass" or "fail" a threshold so much as *select a branch*. Pre-register: PASS ≡ `s_∂` is determinate and unambiguous across the grid (one branch cleanly selected); INFO ≡ the reduced constraint is regularization-ambiguous (see caveat); FAIL ≡ the reduction is ill-posed (no `H²(ρ)` extractable). The *branch* (TURNING-POINT vs MONOTONE-RAMP) is emitted in the verdict `value=` field.
- **Effort**: ~1–2 sessions. The `a_n(τ)` and `λ_min(τ)` trajectories exist (cached); the new work is (i) the minisuperspace reduction of `S_SA` (the homogeneous-sector kinetic + potential split), and (ii) the constraint inversion `H_eff = 0 → H²(ρ)`. The block-diagonal `D_K` keeps the spectral inputs tractable (largest L_max=15 block dense-storable per `math-scripts.md`).

**The load-bearing caveat I must pre-register (loop-quantum-gravity's own honest open problem, transferred).** The loop-quantum-cosmology bounce is **not regularization-unique**: Thiemann's Hamiltonian-constraint regularization is one choice among many; alternative operator orderings (Ashtekar–Lewandowski symmetric ordering, alternative point-splittings, the `μ̄` vs `μ_0` scheme choice) give different quantum constraints and can shift or remove the bounce (knowledge-MCP `open_channel`: "Hamiltonian constraint regularization is not unique"; my corpus `index.md:285-315`, Paper 17). The framework's analog is the *ordering/scheme freedom in reducing `S_SA` to minisuperspace* — there is more than one way to split the homogeneous kinetic term and impose the gap ceiling. **Therefore the L-c compute's PASS criterion MUST pin the reduction scheme explicitly** (a PRU Class-8 machinery pin: `reduction_scheme ∈ {symmetric-ordering, gap-as-density-ceiling, holonomy-analog}`, `μ̄-analog vs μ_0-analog`, `convention=`). A `TURNING-POINT` verdict under one scheme and `MONOTONE-RAMP` under another would be the honest INFO outcome — and would itself be a finding (the functional form is *scheme-dependent*, mirroring loop quantum cosmology's own situation, where the bounce's robustness across schemes is a 25-year open question). This caveat is why the compute decides the *structural verdict* (does a ceiling exist *for a pinned scheme*) rather than a unique number — and it protects the verdict from a convention-shopped false branch.

**Why a single-τ-slice moment triple is insufficient (closing the loop on L-a).** The workshop spec hands us three numbers at `τ_fold`. Those fix `S_SA` and `sign(G_eff)`, but `H²(ρ)` is a *function of the homogeneous density*, and the density-dependence enters only through the *reduced constraint*, which needs the τ-dependence of the moments AND the gap. The slice cannot decide `∂H²/∂ρ`; the reduction can. That is the entire content of "name the single compute."

### L-Q: Questions for transit-dynamics

1. **(the decisive one — on the GFT-bounce-absence scope).** The S96 prior round established `BOUNCE_transfers = False` for the GFT-condensate reduction — but the GFT ansatz assumes a *quasi-equilibrium mean field*, which the diabatic relic (`P_exc = 1.000`) violates. Does the absence of the bounce factor in the *condensate* route actually bear on the *bare minisuperspace-constraint* route (the gap-bounded reduction of `S_SA`, the loop-quantum-cosmology *holonomy-analog* object), or did the GFT route test a different object — a re-expanding equilibrium condensate, which the framework's relic structurally is NOT — leaving the constraint-route ceiling question genuinely open for L-c?

2. **(on whether the spectral gap reaches the homogeneous readout).** The framework has a PROVEN never-closing spectral gap (`λ_min = 0.790 at τ=0.27`, `W3`, S17a) — structurally the loop-quantum-gravity area-gap floor that *becomes* `ρ_c` in loop quantum cosmology. In the supersonic-transit picture, does the monotone `dS/dτ` *bypass* this gap (the transit crosses the fold before any density ceiling is reached, so `H²(ρ)` never sees the gap as a turning point), or is the gap simply *not* the right object to build a homogeneous density ceiling from? If the latter — what, in the transit picture, would the homogeneous-sector ceiling be IF one existed, and why is it structurally guaranteed absent rather than merely unrealized by the particular `τ̇(τ)`?

3. **(on the non-embedding theorem's domain — sharpening the (b) scope).** You argued in S96 (§Re:L2) that the Two-Manifold Non-Embedding Theorem (S74) forbids any single Friedmann trajectory across the fold, so the *origin IS the bridge* and the source leg is post-fold-only. Granting that: does the theorem forbid a `ρ_c`-ceiling *turning point WITHIN the post-fold manifold's approach to the fold from below* (i.e., a one-sided ceiling that the substrate approaches but, being a white hole, exits irreversibly rather than re-expanding through)? A *one-sided* ceiling — `∂H²/∂ρ` changes sign without a time-symmetric re-expansion — would be a TURNING-POINT in `H²(ρ)` that is STILL causally asymmetric. Does your monotone case forbid even a one-sided ceiling, or only the time-symmetric re-expansion?

4. **(on whether `MONOTONE-RAMP` is a derived result or a transit-postulate).** Is `∂H²/∂ρ > 0` throughout a *consequence* of the substrate dynamics you can write a substitution chain for (e.g., from `dS/dτ > 0` monotone + the white-hole flow), or is it an *input* of the supersonic-transit reading — the assumption that the transit is a monotone ramp? If the former, the L-c compute is a confirmation gate; if the latter, the L-c compute is the genuine adjudicator and neither of us should pre-judge its branch. Which is it — and if it is derived, what is the chain `dS/dτ > 0 ⇒ ∂H²/∂ρ > 0`?

---

## Round 1 — transit-dynamics: Response & Cross-Synthesis (supersonic-transit non-equilibrium lens)

### Part 1: Response to loop-quantum-gravity's Sections

#### Re: L-a — turning-point vs monotone

**Verdict: AGREE the single-τ-slice does not decide `sign(∂H²/∂ρ)` (a slice is a point). DISAGREE that the never-closing spectral gap is the right object to build a homogeneous `ρ_c` ceiling from. The gap is a CEILING ON `λ`, not a ceiling on `ρ` — and the L-c compute, correctly posed, returns MONOTONE not because the transit "bypasses" the gap but because the homogeneous source density has no `ρ_c` structure to inherit. I owe an honest concession on (a)'s formal undecidedness, and I give it.**

I accept loop-quantum-gravity's opening framing without reservation: the moment triple `(a_0=6440, a_2=2776.165389, a_4=1350.7216)` at `τ_fold` fixes `S_SA = 5014.556211` and `sign(G_eff) > 0`, but a single slice is silent on `∂H²/∂ρ`, which is a statement about how the homogeneous density enters the *reduced constraint*. CONCEDED: **the functional form is not decidable from the three numbers on the table**, and loop-quantum-gravity is right to refuse a foregone monotone verdict from a static slice. That is a clean point and I will not contest it. The disagreement is about whether the framework's *existing structure* supplies a reason to *expect* a ceiling — loop-quantum-gravity says yes (the gap is the area-gap analog); I say the gap is structurally the wrong object, and I give the substitution chain.

**The decisive structural observation loop-quantum-gravity's L-a relies on — and why it does not transfer.** loop-quantum-gravity's core move (its words: "the framework HAS the ceiling ingredient") is that `D_K`'s never-closing spectral gap (`λ_min = 0.790 at τ=0.27`, `W3: Spectral Gap`, S17a PROVEN; I re-verified via knowledge-MCP this run — `λ_min > 0 at all τ on Jensen`, Lichnerowicz bound `λ² ≥ R_K/4`) is the loop-quantum-gravity *area-gap* situation, and in loop-quantum-cosmology the area gap `Δ` IS what becomes `ρ_c = 18πGℏ²/Δ³`. The antecedent of loop-quantum-cosmology's template ("if the homogeneous reduction carries a gap-like ceiling") is therefore, loop-quantum-gravity argues, NOT hypothetical for this substrate.

Here is the structural reason it does not transfer — a dimensional/role mismatch, not a quantitative one (substitution chain per `math-scripts.md §"Double-Check Logic Before Compute"`):

- **Step 1 — what the loop-quantum-cosmology area gap is, dimensionally.** In loop quantum cosmology the area gap `Δ` is the *minimum eigenvalue of the AREA operator* — a kinematic floor on a *geometric extensive* quantity (area). `ρ_c = 18πGℏ²/Δ³` is built by inverting that area floor through THREE powers of `Δ` (a volume `Δ^{3/2}` squared in the holonomy `sin²(μ̄c)/μ̄²` with `μ̄² ∝ Δ/p`), so that a *small minimum area* produces a *large maximum density*. The ceiling exists because the holonomy correction `sin²(μ̄c)/μ̄² ≤ 1/μ̄²` BOUNDS the gravitational kinetic term `c²`, and that bound — read through the constraint — caps `ρ`.
- **Step 2 — what the framework's spectral gap is, dimensionally.** `λ_min` is the minimum eigenvalue of `D_K` — a floor on the *internal Dirac spectrum* of the fiber, an *intensive* quantity with dimension of `M_KK` (energy). It is the BCS gap edge (`A_gap(τ) = λ_min(τ)`, S25; `2λ_min = 1.644 M_KK` at `μ=0`). Its physical role is `λ_min > 0 ⇒ no Fermi surface ⇒ no spontaneous BCS instability` (`W3`, atlas-07). It is a *gap in the excitation spectrum*, exactly analogous to a superconducting gap — it tells you the *minimum energy to create one quasiparticle*, NOT a maximum density the homogeneous sector can hold.
- **Step 3 — the substitution that loop-quantum-cosmology makes, attempted for the framework.** loop-quantum-cosmology's ceiling enters because the holonomy correction caps the GRAVITATIONAL kinetic term. The framework's analog of "the gravitational kinetic term" is the `a_2` Seeley-DeWitt moment (`G_eff = M_KK²/(16π a_2)`). For a `(1−ρ/ρ_c)` ceiling to appear, the spectral gap would have to enter `a_2(τ)` as a BOUND that caps the homogeneous density. But `λ_min` does not bound `a_2` — `a_2 = Σ_k w_k λ_k^{-2}`-type heat-kernel moment is dominated by the *bulk* of the spectrum (the `a_2_FW_zeta = 2776` is a Weyl-leading-order quantity), and `λ_min` is a single edge eigenvalue contributing a *finite, non-dominant* term. There is no `sin²`-type saturation in the spectral-action reduction: the moments are *linear* sums over eigenvalues, not bounded trigonometric functions of a connection. **The structural ingredient that makes loop-quantum-cosmology's ceiling — a bounded (`≤1`) holonomy function of the gravitational connection — has no counterpart in the framework's linear spectral moments.**
- **Step 4 — read off.** `λ_min > 0` guarantees `D_K` is invertible and the BCS sector is gapped; it does NOT supply a saturation mechanism that caps `ρ_homogeneous`. The gap is a CEILING ON THE FLOOR OF `λ` (a lower bound on excitation energies), whereas `ρ_c` is a CEILING ON `ρ` (an upper bound on density). These are not the same object dimensionally (`[M_KK]` floor vs `[M_KK^4]` density ceiling) OR functionally (gap-protection vs kinetic-saturation). loop-quantum-gravity has correctly identified that *a* discreteness scale exists; it has not shown that this discreteness scale enters the homogeneous constraint as a `(1−ρ/ρ_c)` factor — and Step 3 shows the saturation mechanism that would be required (a bounded holonomy function) is structurally absent from the linear spectral moments.
- **Conclusion on (a).** The gap is real and never-closing (CONCEDED, PROVEN). But it is the loop-quantum-gravity *area-gap analog only at the level of "a gauge-invariant geometric spectrum has a floor"* — NOT at the level of "that floor inverts into a density ceiling." The inversion `Δ → ρ_c` is a loop-quantum-cosmology-SPECIFIC consequence of the holonomy correction's boundedness, which the framework's linear `a_n(τ)` moments do not reproduce. So my (a)-position: **the L-c compute, posed with the gap-as-density-ceiling scheme, will find the gap does NOT propagate into a `(1−ρ/ρ_c)` factor — not because the transit dynamically "bypasses" it, but because the reduction has no saturation operator for it to enter.** I answer loop-quantum-gravity's L-Q2 ("does the monotone `dS/dτ` bypass the gap, or is the gap the wrong object?") with the SECOND horn: **the gap is the wrong object** — it is a quasiparticle-energy floor, not a density-saturation ceiling, and its absence from the homogeneous `ρ_c` slot is structural (Step 3), not kinetic.

This is a genuine, narrowed disagreement with loop-quantum-gravity, and I want it sharp: I am NOT claiming "no ceiling could ever exist" by fiat (that would be the postulate loop-quantum-gravity's L-Q4 warns against — see Re:L-c and T2). I am claiming the SPECIFIC ceiling loop-quantum-gravity proposes (the spectral gap as the area-gap analog) fails to transfer for a SPECIFIC dimensional/functional reason (gap-on-`λ` ≠ ceiling-on-`ρ`; no holonomy-saturation operator in linear moments). Whether *some other* homogeneous ceiling hides in the reduction is exactly what L-c must test — and I keep that question open, against my own monotone prior, because honesty (loop-quantum-gravity's L-Q4) requires it.

#### Re: L-b — scope of "no bounce"

**Verdict: CONVERGE with loop-quantum-gravity, fully and without residual dissent. "No bounce" is SCOPED to monotone-ramp transit dynamics; the singularity-resolution ROLE is STRUCTURAL across two NON-ANALOGOUS mechanisms (polymer bounce vs irreversible quench). This is the cleanest result of Round 1 and I record the convergence explicitly.**

loop-quantum-gravity's L-b is correct on every point and I have nothing to contest. I accept:

1. The framework's dynamics forbid a *time-symmetric* bounce — monotone `dS/dτ = +58,673` at the fold, irreversible diabatic quench (`P_exc = 1.000`, `S_ent = 0`), acoustic white hole over-determined at six causal walls (capstone §6.2, S85 PROVEN). A throat with `H=0` and re-expansion is structurally absent. The §6.2 prose is *correct about the framework's monotone-ramp mechanism*.
2. "No bounce *under monotone-ramp transit*" is NOT "no bounce *in general*." loop quantum cosmology resolves the singularity by a turning point at `ρ_c` — a different dynamical structure, not a refuted one.
3. The singularity-resolution ROLE ("the deep regime is traversed by finite action, not a divergence") is played by two structurally distinct mechanisms: a time-symmetric polymer bounce (loop-quantum-cosmology) versus an irreversible supersonic quench (framework). STRUCTURAL-at-role, ANALOGICAL-at-mechanism — loop-quantum-gravity's S92 tag, which I confirm.

I add ONE substrate-first sharpening that strengthens loop-quantum-gravity's scoping argument rather than complicating it. The reason the framework's "no bounce" is *scoped and not general* is itself the adiabaticity-axis fact from my S96 case (T1, re-derived in T2 below): the framework lives at `Q ≈ 1907 ≫ 1` (sudden-quench corner), and loop-quantum-cosmology's bounce is derived at `Q ≪ 1` (adiabatic corner). "No bounce" is a property of the `Q ≫ 1` corner — a sudden quench has no time-symmetric turnaround because it does not dwell at the crossover `Q ~ 1` where a smooth `H: + → 0 → +` would form. So "no bounce" is scoped to *the framework's location on the adiabaticity axis*, and a substrate that sat at `Q ≪ 1` (counterfactually, or — per Re:L-a, pending L-c — possibly some other reduction scheme) would resolve the singularity by a bounce instead. **The scope is the `Q ≫ 1` corner; the role (finite-action traversal) is axis-wide.** This is exactly loop-quantum-gravity's "STRUCTURAL-at-role / scoped-at-mechanism," now with the scope boundary named precisely (the adiabaticity corner).

**On loop-quantum-gravity's honest-tension flag (does (b) collapse to "general" via the non-embedding theorem?).** loop-quantum-gravity flags that my S96 §Re:L2 move — the Two-Manifold Non-Embedding Theorem (S74) forbids ANY single Friedmann trajectory across the fold, so the *origin IS the bridge* — could push "no bounce" back toward "general *for the cosmogenesis bridge*." I hold to that S96 move and accept loop-quantum-gravity's characterization of it: for THIS substrate, "no bounce" is general *for the bridge* (the non-embedding theorem forbids embedding pre↔post into one trajectory, bounce or otherwise), but that is still narrower than "no bounce is a general feature of singularity-resolution." The residual is exactly what loop-quantum-gravity names: I would say "no bounce is general *for this substrate's bridge*"; loop-quantum-gravity would say "no bounce is scoped to monotone-ramp." **These are the SAME claim viewed from two scopes** — mine pins it to the substrate's bridge (where the theorem bites), loop-quantum-gravity's pins it to the mechanism class (monotone-ramp). Neither is "no bounce is universal." So I converge on loop-quantum-gravity's label and note the residual is a scope-pin difference, not a substantive disagreement: **(b) = "no bounce" is SCOPED (to monotone-ramp / equivalently to this substrate's `Q ≫ 1` bridge); the singularity-resolution role is STRUCTURAL across both mechanisms.** No dissent.

#### Re: L-c — the deciding compute

**Verdict: AGREE the named compute is the right adjudicator and AGREE the reduction scheme MUST be pinned (loop-quantum-gravity's regularization-non-uniqueness caveat is correct and load-bearing). I propose two sharpenings: (i) the gate must test the gap-as-density-ceiling scheme SPECIFICALLY against the Re:L-a dimensional argument, and (ii) the impulsive-source structure makes one branch the structural prior — but the compute, not the prior, decides.**

loop-quantum-gravity's `inv7_minisuperspace_effective_friedmann.py` spec is well-posed and I accept it as the deciding compute. The What/Inputs/Gate/Effort fields are correct; the inputs (`a_n(τ)` trajectories, `ρ_relic = 26.553854 M_KK`, `λ_min(τ)`, `G_eff`, `M_KK`) are the right ones; the `[SIGN]`-trigger PASS criterion (`s_∂ ≡ sign(∂H²/∂ρ)` across the ρ-grid → `TURNING-POINT` vs `MONOTONE-RAMP`) is the correct branch-selector. I will not re-spec it; I extend it.

**On loop-quantum-gravity's load-bearing caveat (regularization-non-uniqueness) — I accept it completely, and it is the single most important methodological point of the workshop.** loop-quantum-gravity transfers loop quantum cosmology's own 25-year open problem honestly: the loop-quantum-cosmology bounce is NOT regularization-unique (Thiemann ordering vs Ashtekar-Lewandowski symmetric ordering vs `μ̄` vs `μ_0` scheme all give different constraints, some with the bounce shifted or removed). The framework's analog is the *ordering/scheme freedom in reducing `S_SA` to minisuperspace* — there is more than one way to split the homogeneous kinetic term and impose (or not impose) a gap ceiling. **This is a PRU Class-8 machinery pin** (`epistemic-discipline.md §"Pre-Registration Completeness"`), and it is exactly the convention-shopping hazard `v3-closure-recovery.md` PROHIBITED_ACTIONS Class 1 polices: a `TURNING-POINT` verdict under one scheme and `MONOTONE-RAMP` under another, with the scheme chosen *after* seeing which gives the desired branch, would be a false closure. The PASS criterion MUST pin `reduction_scheme ∈ {symmetric-ordering, gap-as-density-ceiling, holonomy-analog}` and `μ̄-analog vs μ_0-analog` BEFORE the compute runs. I endorse this without amendment.

**Sharpening (i) — the gate must SPECIFICALLY test the gap-as-density-ceiling scheme against the Re:L-a dimensional argument.** My Re:L-a claim is falsifiable inside this compute: I claim the spectral gap `λ_min` does not enter the homogeneous constraint as a density ceiling because (Step 3) the linear spectral moments `a_n(τ)` have no holonomy-type saturation operator. The `gap-as-density-ceiling` scheme is precisely the test of this. So I propose the compute run BOTH the `gap-as-density-ceiling` scheme (loop-quantum-gravity's preferred — does the gap propagate?) AND a `holonomy-analog` scheme (construct the *closest possible* bounded-function analog of `sin²(μ̄c)/μ̄²` from the moments, and see if even the most loop-quantum-cosmology-favorable construction yields a turning point). If the `gap-as-density-ceiling` scheme returns MONOTONE (my prediction) AND the `holonomy-analog` scheme returns TURNING-POINT, the honest verdict is **scheme-dependent functional form** — which is itself a finding (it would mean the framework's monotone "no bounce" is a *scheme choice*, mirroring loop-quantum-cosmology's situation, and would WEAKEN the unscoped §6.2 prose even further than (b) already does). I flag this as the genuine INFO-bearing outcome loop-quantum-gravity already anticipated, and I want it pre-registered as a branch, not discovered.

**Sharpening (ii) — the impulsive-source structure makes MONOTONE the structural prior, but the compute, not the prior, decides.** Here I am careful, because loop-quantum-gravity's L-Q4 asks exactly the right question (is MONOTONE derived or postulated?) and I answer it fully in T2. The short form for the L-c context: the framework's relic is an *impulsive* (delta-function-like) source — the Bogoliubov spectrum is fixed at the kick (`dt_transit = 1.13e-3 M_KK^{-1}`, `H·dt_transit = 0.665 < 1`), not by a quasi-static `H²(ρ)` that one differentiates. An impulsive source has no *quasi-static density-dependence* to carry a turning point, because the production happens faster than one Hubble time, so `H²(ρ)` in the post-fold readout is sourced by an ALREADY-FROZEN `ρ_relic`, not by a density that rises-and-falls through a ceiling. **This makes MONOTONE the structural PRIOR** — but (honesty per L-Q4) it does NOT make MONOTONE a *theorem*, because the post-fold `H²(ρ)` as a function of the *coasting* relic density (which dilutes as the post-fold manifold expands) is a genuine reduced-constraint question the slice cannot answer. So I hold MONOTONE as the prior with explicit probability mass, NOT as a foregone conclusion, and I agree with loop-quantum-gravity that the L-c compute is the adjudicator. If I am being fully honest (and I will be): I assign the prior ~0.75 to MONOTONE-RAMP and ~0.25 to a scheme-dependent INFO, with TURNING-POINT (a genuine `ρ_c` ceiling in a pinned physical scheme) the smallest mass — but these are priors to be updated by L-c, not pre-judgements of its branch. **Neither of us should pre-judge the branch; the compute decides.** I accept that discipline.

### Part 2: Original Analysis

#### T1: The supersonic-transit reading of the S_SA(τ) homogeneous reduction

**Claim.** The homogeneous-sector readout of the `S_SA(τ)` ramp is sourced by an *impulsive* (delta-function-like) energy injection — the saturated Bogoliubov spectrum frozen at the fold kick — NOT by a quasi-static `H²(ρ)` that rises and falls through a density ceiling. The relevant `H²(ρ)` is the post-fold *coasting* equation, sourced by an already-frozen `ρ_relic`. In the supersonic-transit reading, the functional form is monotone because the production is *over* before the homogeneous sector can build a turning point — the fold is crossed faster than one Hubble time, so there is no quasi-static density trajectory through a `ρ_c`. I give the governing structure first, then the impulsive-source argument.

**The governing structure (substrate-first, mode equation first).** The framework's fluctuation modes obey a parametric-oscillator equation as the Jensen modulus sweeps the fold (this is the substrate-IS object; the emergent `H²(ρ)` is its homogeneous-sector coarse-graining):

$$
u_K'' + \omega_K^2(\tau)\, u_K = 0, \qquad \omega_K^2(\tau) = c_s^2(\tau)\, K^2 + m_{\rm eff}^2(\tau),
$$

with the time-dependence carried by the `D_K(τ)` spectrum reorganizing at the van Hove fold (`c_s(τ)` internal sound speed, `m_eff(τ) ~ Δ_BCS(τ)` the gap scale). Particle production is the Bogoliubov connection between the in-vacuum (`τ → 0⁻`, pre-fold) and out-vacuum (post-fold):

$$
u_K^{\rm out} = \alpha_K\, u_K^{\rm in} + \beta_K\, (u_K^{\rm in})^*, \qquad |\alpha_K|^2 - |\beta_K|^2 = 1, \qquad n_K = |\beta_K|^2.
$$

The homogeneous source density is `ρ_relic = Σ_K E_K |β_K|² = 26.553854 M_KK` (B1+B2+B3 band sum, S96 §W1-5). **This is the entire physics: the homogeneous `H²` is sourced by the integrated Bogoliubov output `ρ_relic`, and `ρ_relic` is fixed at the fold by the saturation `|β_K|² = max` (`P_exc = 1.000`).** The emergent Friedmann `H² = (8πG_eff/3)ρ_relic` is a READOUT of this frozen spectral output — not a dynamical `H²(ρ)` the substrate climbs and descends.

**Why this is an impulsive source, not a quasi-static `H²(ρ)` (substitution chain — the structural spine).**

- **Step 1 — the production timescale vs the Hubble timescale.** The Bogoliubov spectrum saturates over the transit window `dt_transit = 1.13e-3 M_KK^{-1}`. One Hubble time at the fold is `1/H_fold = 1.70e-3 M_KK^{-1}`. So `H_fold · dt_transit = 0.665 < 1` (verified this run). The production COMPLETES in less than one Hubble time.
- **Step 2 — what "less than one Hubble time" means for `H²(ρ)`.** A `(1−ρ/ρ_c)` turning point is a statement that as the homogeneous density `ρ` *rises* toward `ρ_c`, `H²` *falls* to zero, then `ρ` *falls* and `H²` rises again — a quasi-static trajectory that takes MANY Hubble times to traverse (in loop quantum cosmology the bounce neighborhood is resolved over `O(1–10) H^{-1}` as `ρ` climbs and descends through `ρ_sup`). The framework's `ρ_relic` is injected impulsively (Step 1) — it does not *climb* toward a ceiling over many Hubble times; it appears, saturated, in less than one. **There is no quasi-static `ρ(t)` trajectory for a turning point to live on.**
- **Step 3 — the timescale hierarchy that seals it.** `dt_transit = 1.13e-3 < 1/H_fold = 1.70e-3 < τ_relax = 0.068` (all `M_KK^{-1}`; `τ_relax/dt_transit = 60.2`, verified). The transit completes ~60× before the pairing sector can relax. A turning point requires the substrate to dwell near `ρ_c` long enough for the `(1−ρ/ρ_c)` factor to act — but the substrate exits the fold ~60× before relaxation and within one Hubble time. The dwell time at any putative ceiling is structurally zero.
- **Step 4 — read off.** The post-fold `H²(ρ)` is the *coasting* equation sourced by a frozen `ρ_relic` that subsequently *dilutes* as the post-fold manifold expands (`ρ_relic ∝ a^{-n}` for the relic gas). On the coasting branch, `H²` decreases monotonically as `ρ` decreases — `∂H²/∂ρ > 0` (both decrease together, standard Friedmann dilution), with NO sign reversal, because there is no `ρ_c` the density was ever climbing toward. The impulsive injection is *at* the fold; the coasting is *after* it; neither has a turning-point structure.
- **Conclusion.** In the supersonic-transit reading, MONOTONE follows from the *impulsive* (`H·dt < 1`) character of the source: the production is faster than the expansion, so the homogeneous sector inherits a frozen-then-diluting `ρ_relic`, not a density that rises-and-falls through a ceiling. **The functional form is monotone because the source is impulsive, not quasi-static.**

**What transit-dynamics reveals that the static-slice analysis cannot.** loop-quantum-gravity's L-a correctly notes the slice is silent on `∂H²/∂ρ`. The supersonic-transit reading supplies the missing dynamical content: the *rate* `Q = |ω̇/ω²| ≈ 1907` (T2) and the *Hubble crossing* `H·dt = 0.665 < 1` are precisely the dynamical facts that decide the functional form, and they are NOT in the moment triple. The slice gives `ρ_relic` and `G_eff`; the transit physics gives the *adiabaticity* and the *impulsiveness*, which is what selects monotone-over-bounce. This is why the L-c compute needs the τ-dependence (loop-quantum-gravity's own closing point) — and why, when it has it, the impulsive structure is what it will find.

#### T2: Monotone dS/dτ, P_exc=1, causal asymmetry — why there is no turning point (and the honest answer to "is MONOTONE-RAMP derived or postulated?")

**This section answers loop-quantum-gravity's L-Q4 (derived vs postulate), L-Q3 (the one-sided ceiling), and L-Q1 (GFT-scope) head-on. I owe loop-quantum-gravity full honesty on L-Q4, and I give it: MONOTONE-RAMP is a DERIVED chain for the ORIGIN and a STRUCTURAL PRIOR (not a theorem) for the COASTING — and the distinction is the whole answer.**

**Answer to L-Q4 — is `∂H²/∂ρ > 0` derived or a transit-postulate? The honest, decomposed answer.** loop-quantum-gravity asks the sharpest possible question and I will not blur it. The claim `∂H²/∂ρ > 0` decomposes into two sub-claims at two loci, and they have DIFFERENT epistemic status:

- **At the ORIGIN (the bridge — the transit through the fold): DERIVED, not postulated.** The chain is:

  - **Step 1 — adiabaticity is computed, not assumed.** `Q ≡ |ω̇_K/ω_K²|`; substitute `ω ~ Δ_BCS = 0.464 M_KK` (slowest internal mode, the adiabaticity bottleneck) and `ω̇ ~ ω/dt_transit` with `dt_transit = 1.13e-3 M_KK^{-1}`: `Q = 1/(ω · dt_transit) = 1/(0.464 × 1.13e-3) = 1907.2` (verified this run). Independently pinned: `ε = 44.2` (Inverted Born-Oppenheimer, S38), `P_exc = 1.000` (atlas T1 PROVEN).
  - **Step 2 — `Q ≫ 1 ⇒ |β|² = max ⇒ P_exc = 1.000` is a Bogoliubov theorem, not a choice.** The sudden-quench limit of the parametric oscillator gives `|β|² → max` (the in-state is projected onto the out-basis with no adiabatic tracking); `P_exc = |β|²/(1+|β|²) → 1` as `|β|² → ∞`. This is forced by `Q ≫ 1`; it is not a transit-postulate.
  - **Step 3 — `dS/dτ > 0` monotone + `P_exc = 1` ⇒ irreversible ⇒ no time-symmetric turnaround.** `dS_SA/dτ = +58,673` at the fold is a COMPUTED gradient (the spectral-action derivative), and `S_ent = 0` (the relic is a pure squeezed state, no entropy generation, diabatic). Monotone `dS/dτ` with `P_exc = 1` saturation means the transit cannot reverse — there is no `H: + → 0 → +` because reversal would require `dS/dτ` to change sign and the squeeze to un-saturate, neither of which the monotone irreversible quench permits.
  - **Step 4 — the acoustic white hole (S85 PROVEN) seals causal access.** Mach 13.75 supersonic flow forms a one-way causal membrane: no dynamical trajectory propagates from post-fold BACK across the fold. So even setting aside Steps 1–3, the substrate cannot RE-ENTER its pre-transit configuration, which is what a turning-point re-expansion requires.
  - **Conclusion (ORIGIN):** at the bridge, `∂H²/∂ρ` cannot reverse because the production is a `Q ≈ 1907` sudden quench (Steps 1–2, DERIVED) that is monotone and irreversible (Step 3) across a causally-sealed white hole (Step 4). **This is a derived chain `Q ≫ 1 ⇒ |β|²=max, P_exc=1 ⇒ irreversible monotone quench ⇒ no turnaround`, anchored on four PROVEN/computed facts. Not a postulate.**

- **At the COASTING (the post-fold `H²(ρ)` as the relic dilutes): STRUCTURAL PRIOR, not a theorem.** Here I am honest about the limit of the derivation. The post-fold coasting `H²(ρ_coast)` — where `ρ_coast` is the diluting relic density on the expanding post-fold manifold — is a *reduced-constraint* question that the static slice and the bridge-physics do NOT by themselves settle. The supersonic-transit reading gives a strong PRIOR that it is monotone (T1 Step 4: impulsive injection → frozen-then-diluting `ρ`, standard Friedmann dilution `∂H²/∂ρ > 0`), but whether the *reduced spectral-action constraint* introduces any density-dependence beyond standard dilution is exactly the L-c compute. **So at the coasting locus, MONOTONE is my structural prior (~0.75 mass, Re:L-c), not a derived theorem.** I will NOT claim otherwise.

**Net L-Q4 answer:** MONOTONE-RAMP is **DERIVED at the origin** (the cosmogenesis bridge, where the NYT-Q2-style "how does the deep regime get traversed" question lives — `Q ≈ 1907` quench, four PROVEN anchors) and a **STRUCTURAL PRIOR at the coasting** (the post-fold readout, which L-c adjudicates). loop-quantum-gravity is therefore RIGHT that the L-c compute is the genuine adjudicator — but specifically for the COASTING functional form, not for the origin's irreversibility, which is already derived. **The L-c compute is a confirmation gate for the origin's monotone character and a genuine adjudicator for the coasting's.** Neither of us should pre-judge L-c's coasting branch; I accept that, and I have given my prior with explicit mass rather than a foregone conclusion.

**Answer to L-Q3 — does the monotone case forbid even a ONE-SIDED ceiling, or only time-symmetric re-expansion?** This is loop-quantum-gravity's most incisive question, because a one-sided ceiling (`∂H²/∂ρ` reverses sign WITHOUT a time-symmetric re-expansion — the substrate approaches a ceiling but, being a white hole, exits irreversibly) would be a `TURNING-POINT` in `H²(ρ)` that is STILL causally asymmetric, threading the needle between my monotone case and loop-quantum-gravity's bounce. I answer precisely:

- **The white-hole / irreversibility arguments (T2 Steps 3–4) forbid the time-symmetric RE-EXPANSION but do NOT, by themselves, forbid a one-sided sign reversal of `∂H²/∂ρ`.** CONCEDED. A one-sided ceiling is logically compatible with causal asymmetry — the substrate could in principle approach a density at which `∂H²/∂ρ` turns negative and then exit irreversibly without re-expanding. So my causal-asymmetry arguments are NOT sufficient to exclude a one-sided ceiling. I grant loop-quantum-gravity this fully.
- **What WOULD forbid a one-sided ceiling is the impulsive-source structure (T1) and the Re:L-a dimensional argument — and these are weaker than the causal arguments, so a one-sided ceiling is the LIVE residual loop-quantum-gravity is right to press.** The impulsive injection (`H·dt < 1`) means the homogeneous sector never climbs *quasi-statically* toward any ceiling — but a one-sided ceiling does not require quasi-static climbing on the dilution branch; it could be a feature of the *reduced constraint at the injection density itself* (i.e., `∂H²/∂ρ < 0` for `ρ > ρ*` even on the frozen-then-diluting branch, if the reduced constraint had a `(1−ρ/ρ_c)`-type factor). And the Re:L-a argument (the gap is the wrong object) shows the *spectral-gap* route does not supply such a factor — but does NOT exclude that *some other* term in the reduced `S_SA` does.
- **Conclusion on L-Q3.** My monotone case forbids the **time-symmetric re-expansion** (derived, T2 Steps 3–4) but does **NOT** forbid a **one-sided ceiling** — that is exactly the residual the L-c compute must decide, and loop-quantum-gravity is right to isolate it. **So the honest verdict-space for (a) is THREE branches, not two:** `MONOTONE-RAMP` (no ceiling; my prior), `ONE-SIDED-CEILING` (`∂H²/∂ρ` reverses but no re-expansion — causally asymmetric turning point; loop-quantum-gravity's sharpest live option), and `SYMMETRIC-BOUNCE` (`ρ_c` with re-expansion — which I DO exclude via white-hole irreversibility, T2 Steps 3–4). loop-quantum-gravity's L-Q3 correctly identifies that the interesting contest is MONOTONE vs ONE-SIDED-CEILING, both of which are causally asymmetric — the symmetric bounce is off the table for THIS substrate. I propose the L-c gate's `value=` field carry all three branches so the one-sided option is not silently collapsed into either MONOTONE or SYMMETRIC-BOUNCE.

**Answer to L-Q1 — does the GFT `BOUNCE_transfers=False` bear on the bare minisuperspace-constraint route, or did it test a different object?** loop-quantum-gravity is RIGHT that these are different objects, and I concede the scope precisely. The S96 §W1-5 GFT route tested the *quasi-equilibrium condensate* ansatz (Oriti Gross-Pitaevskii mean field), which assumes a re-expanding equilibrium condensate — and the framework's relic is the opposite limit (`P_exc = 1.000` diabatic quench). So `BOUNCE_transfers = False` in the GFT route establishes that **a re-expanding equilibrium condensate does not transfer** — which is the *symmetric-bounce* exclusion (consistent with my T2 Steps 3–4), NOT the *one-sided-ceiling* or *bare-constraint* question. loop-quantum-gravity's holonomy-analog object (the gap-bounded reduction of `S_SA`) is genuinely untested by the GFT route. **CONCEDED: the GFT route settles the symmetric-condensate-bounce against COMPLEMENTARY but leaves the bare-constraint one-sided-ceiling question open for L-c.** This is the cleanest statement of how S96 and INV7 relate: S96 closed the *symmetric* bounce (GFT-condensate, equilibrium); INV7 must close the *one-sided* ceiling (bare minisuperspace constraint, the holonomy-analog) — a strictly harder and genuinely open question that the prior round did NOT settle. I credit loop-quantum-gravity for isolating it.

#### T3: Questions for loop-quantum-gravity

I have answered loop-quantum-gravity's four L-Q questions (L-Q1 GFT-scope: conceded different object, T2; L-Q2 gap-bypass-vs-wrong-object: second horn, the gap is the wrong object, Re:L-a; L-Q3 one-sided ceiling: NOT forbidden, the live residual, T2; L-Q4 derived-vs-postulate: derived at origin, prior at coasting, T2). My questions drive the R2 verdict toward the three-branch resolution.

**T3-Q1 (the load-bearing one — what is the loop-quantum-cosmology MECHANISM by which the area gap becomes a density ceiling, and does it have a linear-spectral-moment analog?).** My Re:L-a argument is that loop-quantum-cosmology's `Δ → ρ_c` inversion requires a BOUNDED holonomy function (`sin²(μ̄c)/μ̄² ≤ 1/μ̄²`) capping the gravitational kinetic term — and the framework's linear spectral moments `a_n(τ) = Σ_k w_k λ_k^{-2s}` have no such bounded-function saturation. Do you accept that the loop-quantum-cosmology ceiling is a consequence of the *holonomy boundedness specifically* (not of the area gap's mere existence)? If yes, then for the framework to inherit a ceiling, the `gap-as-density-ceiling` scheme must produce a bounded-function analog from linear moments — which I claim is structurally impossible. Can you exhibit the loop-quantum-cosmology step where the area gap enters the *density* (as opposed to entering the *area spectrum*), and identify what in the spectral-action reduction would play the role of the holonomy `sin²`? If there is no such object, the `gap-as-density-ceiling` scheme returns MONOTONE by construction, and the only live ceiling-route is a NON-gap term in the reduced `S_SA` (which is the ONE-SIDED-CEILING residual of L-Q3).

**T3-Q2 (the one-sided ceiling — can loop-quantum-cosmology itself produce a one-sided, causally-asymmetric `ρ_c`?).** L-Q3 isolated the one-sided ceiling as the live contest. Sharpening it back at you: in loop quantum cosmology, is the bounce NECESSARILY time-symmetric, or are there loop-quantum-cosmology models (dissipative, or with a built-in arrow from the clock field) where `H²(ρ)` has a one-sided turning point — `∂H²/∂ρ < 0` for `ρ > ρ*` but NO re-expansion? If loop-quantum-cosmology's own ceiling is *always* time-symmetric (the holonomy correction is even in `c`, so `H²` is symmetric about the bounce), then a one-sided ceiling is NOT a loop-quantum-cosmology structure either — it would be a NOVEL functional form belonging to neither framework, and the L-c verdict-space reduces to {MONOTONE, SYMMETRIC-BOUNCE}, with SYMMETRIC-BOUNCE already excluded for the framework (T2). In that case (a) closes as MONOTONE-or-INFO, and the one-sided option is a red herring we can jointly retire. Which is it — is the one-sided ceiling a real loop-quantum-cosmology-adjacent structure, or an artifact of trying to causal-asymmetrize a fundamentally even `H²(ρ)`?

**T3-Q3 (scheme-pinning — what is the loop-quantum-cosmology analog of the framework's reduction-scheme freedom, and does the bounce's scheme-robustness transfer?).** You transferred loop-quantum-cosmology's regularization-non-uniqueness honestly (Thiemann vs symmetric ordering vs `μ̄` vs `μ_0`). Question: across loop-quantum-cosmology's scheme choices, is the *existence* of the bounce robust (it survives all schemes, only `ρ_c` shifts) or *fragile* (some schemes remove it)? This matters for the L-c verdict's interpretation: if the loop-quantum-cosmology bounce is scheme-ROBUST (existence-stable, value-variable), then a framework `gap-as-density-ceiling` scheme returning MONOTONE while a `holonomy-analog` scheme returns TURNING-POINT would be a GENUINE structural divergence (the framework's monotone is not a scheme artifact — it survives the most loop-quantum-cosmology-favorable construction). If the loop-quantum-cosmology bounce is itself scheme-FRAGILE, then both frameworks have scheme-dependent functional forms and the honest joint verdict is "functional form is scheme-dependent in BOTH" — which is the INFO outcome we should pre-register. I want your read on loop-quantum-cosmology's bounce-robustness so the L-c `reduction_scheme` pin is calibrated against the right loop-quantum-cosmology precedent.

**T3-Q4 (convergence proposal — the three-branch verdict for (a)).** I propose (a) closes as a THREE-branch pre-registration, deferred to the L-c compute, with our R1 positions mapped onto it:
- **MONOTONE-RAMP** — no ceiling; `∂H²/∂ρ > 0` throughout (my prior, ~0.75; DERIVED at the origin via `Q ≈ 1907`, structural-prior at the coasting).
- **ONE-SIDED-CEILING** — `∂H²/∂ρ` reverses sign without re-expansion; causally asymmetric turning point (the live residual we BOTH isolated; not excluded by my causal arguments, T2 L-Q3; pending T3-Q2's resolution of whether it is even a loop-quantum-cosmology-adjacent structure).
- **SYMMETRIC-BOUNCE** — `ρ_c` with time-symmetric re-expansion (loop-quantum-cosmology's canonical form; EXCLUDED for THIS substrate by white-hole irreversibility, T2 Steps 3–4, AND by the GFT `BOUNCE_transfers=False`, S96).

with the L-c gate carrying all three in its `value=` field and the `reduction_scheme` pinned per loop-quantum-gravity's caveat. Do you accept the three-branch structure, or do you hold that SYMMETRIC-BOUNCE should remain live (i.e., do you contest the white-hole exclusion of re-expansion)? I expect we converge on: **(a) = THREE-branch, deferred to L-c, with SYMMETRIC-BOUNCE excluded and the real contest MONOTONE-vs-ONE-SIDED-CEILING** — and T3-Q2 decides whether even ONE-SIDED-CEILING survives as a structurally-realizable form. That, plus the (b) convergence (scoped "no bounce") and the (c) convergence (the compute + scheme-pin), is a clean Round-1 landing.

---

## Round 2 — loop-quantum-gravity: Follow-up

transit-dynamics' Round-1 response is the strongest possible version of the monotone case, and it sharpened the contest precisely where I hoped it would. I concede what is genuinely conceded, hold the one dissent that survives its dimensional argument, own the three-branch emergence as a real joint product, and answer its three load-bearing questions (T3-Q1/Q2/Q3) from my corpus — because two of them turn on facts about the loop-quantum-cosmology bounce (its time-symmetry, its scheme-robustness) that are MY domain to settle, not to assert.

### CONVERGENCE

I converge on three of transit-dynamics' advances, and I want each convergence stated as a concession of something I previously held more loosely — not diplomatic agreement.

**(C1) The three-branch verdict space for (a) — I accept it, and SYMMETRIC-BOUNCE is off the table for THIS substrate. I provide the corpus fact that seals the exclusion.** transit-dynamics' T2 reframe — `{MONOTONE-RAMP, ONE-SIDED-CEILING, SYMMETRIC-BOUNCE}` rather than a binary — is the correct structure, and it is sharper than my L-a binary (TURNING-POINT vs MONOTONE). I accept the SYMMETRIC-BOUNCE exclusion, and I strengthen it with the load-bearing loop-quantum-cosmology fact transit-dynamics' causal argument was pointing at without naming: **the loop-quantum-cosmology bounce is provably time-symmetric**. From my corpus (Ashtekar-Bianchi 2021, Paper 17, `17_Ashtekar_Bianchi_2021_Short_Review_LQG.md:161`): "The expanding FLRW branch is bridged to a contracting FLRW branch in the past." And from Ashtekar 2006 (Paper 08, `08_Ashtekar_2006_QuantumNatureBigBang.md:145`): the wave packet is "symmetric under `μ → −μ` ... joining contracting + expanding classical trajectories." The reason is structural: the holonomy correction `sin²(μ̄c)/μ̄²` is an **even function of the connection component `c`**, so the effective `H²(ρ) = (8πG/3)ρ(1−ρ/ρ_sup)` is symmetric about the bounce by construction — `H: + → 0 → +` with the contracting and expanding branches mirror images. A causally-asymmetric substrate (the framework's white hole, Mach 13.75, six causal walls, capstone §6.2 S85 PROVEN) therefore CANNOT host the loop-quantum-cosmology bounce, because that bounce IS a time-symmetric re-expansion. transit-dynamics excluded SYMMETRIC-BOUNCE from the transit side (T2 Steps 3–4, irreversibility); I now exclude it from the loop-quantum-cosmology side (the bounce is intrinsically even-in-`c`). The exclusion is over-determined from both ends — a genuine convergence, not a coincidence.

**(C2) (b) "no bounce" is SCOPED, not general — converged, and I accept transit-dynamics' adiabaticity-corner sharpening as the precise scope boundary.** This was already a convergence target in my L-b; transit-dynamics' Re:L-b confirmed it without residual dissent, and added the one thing my scoping argument lacked — a *named coordinate* for the scope boundary. My L-b said "no bounce is scoped to monotone-ramp transit dynamics"; transit-dynamics pinned the scope to the framework's location on the **adiabaticity axis** (`Q ≈ 1907 ≫ 1`, the sudden-quench corner), with the loop-quantum-cosmology bounce living at `Q ≪ 1` (the adiabatic corner). This is the better statement: "no bounce" is a property of the `Q ≫ 1` corner because a sudden quench does not dwell at the crossover `Q ~ 1` where a smooth `H: + → 0 → +` would form. I adopt this — the scope is `Q ≫ 1`; the singularity-resolution ROLE (finite-action traversal of the deep regime) is axis-wide. This is the STRUCTURAL-at-role / ANALOGICAL-at-mechanism tag from my S92 cross-framework comparison, now with the mechanism-axis coordinate named. **No dissent on (b).** The residual transit-dynamics flagged (its "no bounce is general *for this substrate's bridge*" via the Two-Manifold Non-Embedding Theorem vs my "scoped to monotone-ramp") is, as it correctly says, a scope-pin difference viewing the SAME claim — neither of us says "no bounce is universal." I record it as converged.

**(C3) The L-c compute and its scheme-pin — converged, including transit-dynamics' Sharpening (i) two-scheme protocol.** transit-dynamics accepted my `inv7_minisuperspace_effective_friedmann.py` spec and my regularization-non-uniqueness caveat without amendment, and extended it productively: run BOTH the `gap-as-density-ceiling` scheme (my preferred — does the gap propagate?) AND a `holonomy-analog` scheme (construct the closest possible bounded-function analog of `sin²(μ̄c)/μ̄²` from the moments). I accept this two-scheme protocol fully. It is the correct operationalization of my own load-bearing caveat (the bounce is not regularization-unique), and it converts the caveat from a hedge into a pre-registered branch: if `gap-as-density-ceiling` returns MONOTONE while `holonomy-analog` returns TURNING-POINT, the honest verdict is **scheme-dependent functional form** — which is itself a finding, and one that would weaken the unscoped §6.2 prose further than (b) alone does. I want this pre-registered as a branch, exactly as transit-dynamics proposes, not discovered at runtime.

### DISSENT

I hold one dissent, and it is narrower than my L-a position because transit-dynamics' dimensional argument (Re:L-a Steps 1–4) is partly correct and forced me to retreat. The dissent is no longer "the gap supplies a `ρ_c`" — I concede that the *spectral-gap-as-area-gap* route fails. The surviving dissent is: **ONE-SIDED-CEILING is still LIVE, and transit-dynamics' arguments do not exclude it.** This is the genuine remaining contest, and transit-dynamics agrees it is the contest (T2's answer to my L-Q3) — so the dissent is well-localized.

**(D1) I concede the dimensional argument AT THE LEVEL transit-dynamics posed it: the spectral gap `λ_min` is the WRONG OBJECT for a density ceiling. But the concession is bounded — it closes the gap-route, not the ceiling-question.** transit-dynamics' Re:L-a Step 3 is correct and I accept it: `λ_min` is an *intensive* `[M_KK]` floor on the internal Dirac/BCS excitation spectrum (the minimum energy to create one quasiparticle, `2λ_min = 1.644 M_KK` at `μ=0`), NOT an *extensive* `[M_KK⁴]` ceiling on the homogeneous density. And the deeper structural point is correct: the loop-quantum-cosmology inversion `Δ → ρ_c` is NOT a consequence of the area gap's mere existence — it is a consequence of the **holonomy boundedness specifically**. My corpus confirms transit-dynamics here, and I will not pretend otherwise: the loop-quantum-cosmology bounce arises because the holonomy correction `sin²(μ̄c)/μ̄² ≤ 1/μ̄²` *bounds the gravitational kinetic term `c²`*, capping `ρ` through the constraint (Ashtekar 2006, Paper 08:151 — the quantum-geometry modifications "come with the sign required to make gravity repulsive"; Paper 17:161 — "As `Δ → 0`, `ρ_sup → ∞` (the bounce disappears)," i.e., the ceiling is built by the holonomy inversion of the gap, not by the gap alone). The framework's `a_n(τ) = Σ_k w_k λ_k^{−2s}` moments are **linear sums over eigenvalues** — there is no bounded trigonometric function of a connection in them, hence no `sin²`-type saturation operator. **So the `gap-as-density-ceiling` scheme will return MONOTONE by construction** — and it returns MONOTONE for transit-dynamics' structural reason (no saturation operator), not by my L-a's "the gap propagates into a ceiling." I withdraw the claim that the spectral gap supplies `ρ_c`. transit-dynamics won that exchange.

**(D2) The surviving dissent: a one-sided ceiling can arise from the gap entering the constraint DIFFERENTLY than via a `sin²` holonomy — and transit-dynamics explicitly conceded its arguments do not forbid this.** Here is where I do not yield. transit-dynamics' own T2 (answer to L-Q3) grants the decisive point: "The white-hole / irreversibility arguments (T2 Steps 3–4) forbid the time-symmetric RE-EXPANSION but do NOT, by themselves, forbid a one-sided sign reversal of `∂H²/∂ρ`. CONCEDED." And further: the impulsive-source argument (T1, `H·dt < 1`) forbids quasi-static *climbing* toward a ceiling on the dilution branch, but "a one-sided ceiling does not require quasi-static climbing ... it could be a feature of the *reduced constraint at the injection density itself*." So both of transit-dynamics' exclusion arguments — causal-asymmetry and impulsiveness — are, by its own admission, silent on a one-sided ceiling. The Re:L-a dimensional argument is decisive ONLY against the *spectral-gap* route; it does not exclude that **some other term in the reduced `S_SA`** — not `λ_min`, but, e.g., the τ-derivative structure of the `a_2(τ)` and `a_4(τ)` moments entering the reduced kinetic term — produces a `∂H²/∂ρ < 0` region without re-expansion. My dissent is precise: **the question "does a one-sided ceiling exist in the reduced constraint" is NOT answered by transit-dynamics' three arguments (causal, impulsive, dimensional) — the first two are conceded silent, and the third closes only the gap-route.** The substitution-chain status: transit-dynamics' MONOTONE is DERIVED at the origin (`Q ≈ 1907` quench, four PROVEN anchors — I accept this completely, T2 Step 1–4) but only a STRUCTURAL PRIOR (~0.75) at the coasting, by its own honest L-Q4 decomposition. A prior is not a theorem. **ONE-SIDED-CEILING occupies exactly the gap between "derived-at-origin" and "prior-at-coasting" — and that gap is what L-c adjudicates.** I hold ONE-SIDED-CEILING as live, at low-but-nonzero prior mass, pending (i) the `holonomy-analog`-scheme compute and (ii) my own answer to whether a one-sided ceiling is even a structurally-realizable form (T3-Q2, which I answer below — and the answer complicates my own dissent honestly).

### EMERGENCE

**(E1) The three-branch verdict space `{MONOTONE-RAMP, ONE-SIDED-CEILING, SYMMETRIC-BOUNCE}` is a genuine joint emergence — neither of us brought it to Round 1, and I own it as a co-product.** My L-a carried a binary (TURNING-POINT vs MONOTONE). transit-dynamics' T1 carried a binary (impulsive-monotone vs quasi-static-bounce). The THREE-branch structure emerged from the *collision* of my L-Q3 (does the monotone case forbid a one-sided ceiling?) with transit-dynamics' T2 concession (no, the causal arguments don't). Neither binary contained ONE-SIDED-CEILING as a distinct option; it materialized precisely because I pressed whether causal-asymmetry forbids a turning point, and transit-dynamics honestly answered that it forbids only *re-expansion*. The branch `∂H²/∂ρ` reverses without time-symmetric re-expansion — a causally-asymmetric turning point — belongs to **neither** parent framework's canonical form: it is not the framework's MONOTONE and not loop-quantum-cosmology's SYMMETRIC-BOUNCE. That is the signature of a real emergence: a verdict-space cell that exists only in the joint analysis. I propose (as transit-dynamics does in T3-Q4) that the L-c gate's `value=` field carry all three branches, so the one-sided option is not silently collapsed into either neighbor.

**(E2) A sharper emergence: the workshop has SPLIT the singularity-resolution question into two structurally-distinct sub-questions, and located which round closes which.** This is the cleanest structural product, and it is transit-dynamics' framing in its T2 answer to L-Q1, which I now elevate to a named emergence: **S96 closed the SYMMETRIC bounce (GFT-condensate, quasi-equilibrium); INV7 must close the ONE-SIDED ceiling (bare minisuperspace constraint, the holonomy-analog).** These are different objects — the GFT route tested a re-expanding equilibrium condensate (which the framework's `P_exc=1.000` diabatic relic structurally is NOT), so its `BOUNCE_transfers=False` (S96 §W1-5 INFO) is a *symmetric-bounce* exclusion, NOT a bare-constraint result. The emergence is the recognition that "the framework resolves the singularity without a bounce" is really TWO claims at TWO loci: (i) no symmetric re-expanding bounce — CLOSED (S96 + the even-in-`c` corpus fact of C1 + white-hole irreversibility), and (ii) no one-sided causally-asymmetric ceiling — OPEN, deferred to L-c. The workshop's durable contribution is this decomposition: it tells the next-session planner *exactly* what L-c must test (the one-sided ceiling in a scheme-pinned reduced constraint), and *why the prior round did not already settle it* (different object — equilibrium condensate vs bare constraint). That is a structural-change-class output (per `output-standards.md §"Numerical vs Structural"`), not a numerical revision.

### QUESTIONS

These drive the final synthesis turn. The first two I have *already answered from my corpus* in this section (because they are loop-quantum-cosmology-domain facts), and I pose them as questions only to invite transit-dynamics to fold the answers into the joint verdict — I am not leaving them open. The third and fourth are genuinely for the final turn.

**(Q1 — answered; for the joint verdict) Is a ONE-SIDED ceiling even a loop-quantum-cosmology-adjacent structure? My corpus answer: NO — and this CUTS AGAINST my own D2 dissent, which I disclose honestly.** transit-dynamics' T3-Q2 asked whether loop-quantum-cosmology's bounce is *necessarily* time-symmetric or whether dissipative/arrow-of-time loop-quantum-cosmology models admit a one-sided `ρ_c`. My corpus answer is decisive: **the canonical loop-quantum-cosmology bounce is necessarily time-symmetric**, because the holonomy correction `sin²(μ̄c)/μ̄²` is *even in `c`*, making `H²(ρ)` symmetric about the bounce (Paper 17:161; Paper 08:145, the `μ → −μ` symmetry of the physical state; the `Z₂` orientation-reversal `Π̂` selecting the symmetric sector, Paper 08:79). So a one-sided ceiling is **NOT** a native loop-quantum-cosmology structure — it would be a NOVEL functional form belonging to neither framework, exactly as transit-dynamics anticipated in T3-Q2's second horn. **This honestly weakens my own D2 dissent**: ONE-SIDED-CEILING is not "loop-quantum-cosmology's option that transit-dynamics failed to exclude"; it is a *structurally orphan* branch with no precedent in EITHER parent theory. I still hold it as live (the reduced `S_SA` constraint is not obligated to inherit either parent's symmetry, and a non-`sin²` term could in principle produce it), but I down-weight its prior and reclassify it: it is a *logical residual the compute must rule out*, not a *positively-motivated loop-quantum-cosmology structure*. **Question for the joint verdict:** do we agree the verdict-space is `{MONOTONE-RAMP (both parents' "no symmetric bounce" + framework's positive prior), SYMMETRIC-BOUNCE (loop-quantum-cosmology's native form, EXCLUDED here), ONE-SIDED-CEILING (orphan; neither parent; the logical residual L-c must exclude)}` — with ONE-SIDED-CEILING tagged as structurally-orphan rather than loop-quantum-cosmology-adjacent? If so, the real contest narrows further: MONOTONE-RAMP vs an orphan branch that NEITHER of us positively predicts — which makes MONOTONE-RAMP the strong joint prior and ONE-SIDED-CEILING the thing L-c falsifies-or-discovers.

**(Q2 — answered; for the joint verdict) Is the loop-quantum-cosmology bounce scheme-ROBUST or scheme-FRAGILE, and what does that calibrate for the L-c `reduction_scheme` pin? My corpus answer: existence-ROBUST, value-VARIABLE.** transit-dynamics' T3-Q3 asked whether the loop-quantum-cosmology bounce survives all scheme choices (only `ρ_c` shifts) or whether some schemes remove it. My corpus answer: within the canonical homogeneous-isotropic loop-quantum-cosmology reduction, the bounce **existence is robust** — it survives the standard operator-ordering choices, with `ρ_sup = 18πGℏ²/Δ³` the value that shifts; the bounce disappears ONLY in the degenerate limit `Δ → 0` (the classical limit, where quantum geometry is switched off entirely — Paper 17:161). The scheme-DEPENDENCE that IS real in loop-quantum-cosmology is at the *reduction-from-full-LQG* level (Paper 17:210, "different approaches lead to somewhat different results especially in the pre-bounce branch"; LQC is a symmetry-reduced model, not derived from full LQG) and at the *pre-bounce quantum-state choice* (Paper 17:212, "an outstanding open issue") — NOT at the level of whether the bounce exists in the effective Friedmann equation. **The calibration for L-c:** because the loop-quantum-cosmology bounce is existence-robust within its reduction, a framework `gap-as-density-ceiling` scheme returning MONOTONE while a `holonomy-analog` scheme returns TURNING-POINT would be a GENUINE structural divergence (the framework's monotone survives even the most loop-quantum-cosmology-favorable construction — it is not a scheme artifact). Conversely, if BOTH framework schemes returned MONOTONE, that would be a STRONGER no-bounce result than loop-quantum-cosmology's own, because loop-quantum-cosmology's bounce is robust where the framework's absence-of-bounce would also be robust. **Question for the joint verdict:** do we pre-register the L-c interpretation as — "MONOTONE under both schemes ⇒ robust no-bounce (stronger than the scoped §6.2 claim); scheme-split ⇒ scheme-dependent functional form (weakens §6.2 to a scheme choice); TURNING-POINT under both ⇒ the framework DOES carry a ceiling (refutes the monotone prior)" — with the loop-quantum-cosmology existence-robustness as the calibration baseline?

**(Q3 — open; for the final turn) Does the impulsive-source structure (T1) actually GUARANTEE MONOTONE on the COASTING branch, or only on the INJECTION?** transit-dynamics' T1 Step 4 argues the post-fold `H²(ρ)` is the coasting equation sourced by a frozen-then-diluting `ρ_relic`, with `∂H²/∂ρ > 0` (both decrease together, standard dilution). I accept this for *standard* Friedmann dilution. But the L-c compute reduces the **spectral action**, not a standard matter Friedmann equation — and the reduced `S_SA` could carry a density-dependence in `G_eff(ρ)` or a higher-moment `a_4`-sourced term that standard dilution does not have. My question: is your MONOTONE-on-coasting prior (~0.75) robust to the possibility that the *reduced-constraint* `H²(ρ)` differs from a standard-matter `H²(ρ)` by exactly the `a_4`-moment term — and if the `a_4(τ)` contribution to the reduced kinetic term is what L-c must check, is `a_4` more like a (bounded, ceiling-producing) curvature-squared term or a (linear, monotone) matter term? This is the one place I think your impulsive argument, strong as it is at the origin, may not fully reach the coasting — and it is the technical heart of what L-c computes.

**(Q4 — open; for the final turn) For the (a) Verdict-table cell: do we land "Partial/Emerged — three-branch, deferred to L-c, SYMMETRIC-BOUNCE excluded, real contest MONOTONE-vs-orphan-ONE-SIDED-CEILING" — or do you want the cell to additionally record the joint PRIOR (both of us now expect MONOTONE, with ONE-SIDED-CEILING the structurally-orphan residual)?** I lean toward recording the prior explicitly, because hiding it would misrepresent the workshop's actual state — we did NOT end Round 1 with two co-equal live branches; we ended with MONOTONE as the strong joint prior and ONE-SIDED-CEILING as a residual that neither parent framework positively predicts. But the discipline (your L-Q4 acceptance, my L-c caveat) is that the *prior* is recorded, the *branch* is deferred to the compute — neither pre-judged. I want your read on whether the Verdict cell should carry the prior or stay branch-neutral, since you author the final Verdict table.

---

## Round 2 — transit-dynamics: Cross-Synthesis (FINAL TURN — also fills Verdict + Wrap-Up + Effected-In-Session)

### CONVERGENCE

loop-quantum-gravity's follow-up closed the contest down to one cleanly-localized residual, and it did so by supplying the loop-quantum-cosmology-domain facts I could only point at from the transit side. I converge on four points, each stated as a position now PINNED rather than merely agreed.

**(C1) SYMMETRIC-BOUNCE is over-determined-excluded — and the exclusion is now two-sided and structural, not one-sided and dynamical.** In Round 1 I excluded the symmetric bounce from the transit side only (T2 Steps 3–4: monotone `dS/dτ = +58,673`, `P_exc = 1.000` diabatic, `S_ent = 0`, six causal walls — irreversibility forbids re-expansion). loop-quantum-gravity's C1 supplied the complementary exclusion from the loop-quantum-cosmology side: the bounce `H²(ρ) = (8πG/3)ρ(1−ρ/ρ_sup)` is **provably even in the connection `c`** (the holonomy correction `sin²(μ̄c)/μ̄²` is even in `c`; the physical state is `μ → −μ` symmetric, Ashtekar 2006 Paper 08:145; the `Z₂` orientation-reversal `Π̂` selects the symmetric sector, Paper 08:79; Ashtekar-Bianchi 2021 Paper 17:161 — "expanding FLRW branch bridged to a contracting FLRW branch in the past"). A causally-asymmetric substrate (Mach 13.75 white hole) **cannot host an intrinsically time-symmetric bounce** — the exclusion holds independent of my transit dynamics. CONVERGED: SYMMETRIC-BOUNCE is excluded from BOTH ends. The S96 `BOUNCE_transfers = False` (GFT-condensate, §W1-5 INFO) is the third, independent leg of the same exclusion (the quasi-equilibrium condensate route). Three non-analogous arguments, one conclusion — this is the structural signature of a real result, not a coincidence.

**(C2) (b) "no bounce" is SCOPED to the `Q ≫ 1` corner — fully converged, scope boundary named on the adiabaticity axis.** No change from my Re:L-b. loop-quantum-gravity's C2 adopted my adiabaticity-corner sharpening (scope = `Q ≈ 1907 ≫ 1` sudden-quench corner; loop-quantum-cosmology bounce lives at `Q ≪ 1` adiabatic corner; the singularity-resolution ROLE — finite-action traversal — is axis-wide), and I adopt its corollary that this IS the S92 STRUCTURAL-at-role / ANALOGICAL-at-mechanism tag with the mechanism-axis coordinate now named. The residual scope-pin difference (my "general for this substrate's bridge" via the Two-Manifold Non-Embedding Theorem vs loop-quantum-gravity's "scoped to monotone-ramp") is, as both of us agree, the SAME claim from two scopes — neither says "no bounce is universal." Recorded as converged with zero residual dissent.

**(C3) The L-c compute and its two-scheme pin — converged, with the scheme-pin as the load-bearing methodological caveat.** loop-quantum-gravity accepted my Sharpening (i) two-scheme protocol (run `gap-as-density-ceiling` AND `holonomy-analog`); I accept its Q2 calibration (the loop-quantum-cosmology bounce is **existence-ROBUST, value-VARIABLE** — it survives operator-ordering choices within the homogeneous reduction, disappearing only in the degenerate `Δ → 0` classical limit; Paper 17:161,210,212). This calibration is decisive for the L-c interpretation, and I pre-register the joint reading loop-quantum-gravity proposed: **MONOTONE under both schemes ⇒ robust no-bounce (STRONGER than the scoped §6.2 claim, because the framework's absence-of-ceiling survives where loop-quantum-cosmology's presence-of-ceiling is robust); scheme-split ⇒ scheme-dependent functional form (WEAKENS §6.2 to a scheme choice, mirroring loop-quantum-cosmology's own reduction-level scheme-dependence); TURNING-POINT under both ⇒ the framework carries a ceiling (refutes the monotone prior).** The scheme-pin is a PRU Class-8 machinery pin (`epistemic-discipline.md §"Pre-Registration Completeness"`) and is exactly the convention-shopping hazard `v3-closure-recovery.md` PROHIBITED_ACTIONS Class 1 polices: **the branch is convention-shoppable without the pin**, so the pin is mandatory, not optional. This is the single most important methodological output of the workshop.

**(C4) ONE-SIDED-CEILING is structurally ORPHAN, not loop-quantum-cosmology-adjacent — I accept loop-quantum-gravity's Q1 corpus answer, which it disclosed cuts against its own dissent.** loop-quantum-gravity's Q1 settled (from its corpus, not assertion) that a one-sided ceiling is NOT a native loop-quantum-cosmology structure: the canonical bounce is *necessarily* time-symmetric (even-in-`c`), so a causally-asymmetric turning point belongs to neither parent framework. I credit the honesty — loop-quantum-gravity volunteered the fact that weakens its own D2. CONVERGED on the relabel: the verdict-space is `{MONOTONE-RAMP (both parents' no-symmetric-bounce + the framework's positive prior), SYMMETRIC-BOUNCE (loop-quantum-cosmology-native, EXCLUDED here), ONE-SIDED-CEILING (structurally ORPHAN — neither parent positively predicts it; the logical residual L-c must rule out)}`. The contest narrows to **MONOTONE-RAMP vs an orphan branch neither of us positively predicts** — which makes MONOTONE-RAMP the strong joint prior and ONE-SIDED-CEILING the thing L-c falsifies-or-discovers.

### DISSENT

I hold **no dissent against loop-quantum-gravity** — the workshop has converged. But I will NOT manufacture a clean monotone win, because loop-quantum-gravity's surviving D2 is correct and its Q3 isolates a real technical gap in my own argument. I record this as a *self-dissent against my own ~0.75 monotone-on-coasting prior*, because honest synthesis requires naming where the prior is soft — and loop-quantum-gravity's Q3 found exactly the term that softens it.

**(SD1) loop-quantum-gravity's D2 survives: my three exclusion arguments do not reach ONE-SIDED-CEILING, and I confirm this against the canonical `a_4` structure.** My Round-1 arsenal was (i) causal asymmetry (T2 Steps 3–4), (ii) impulsiveness (`H·dt = 0.665 < 1`, T1), (iii) the dimensional argument (Re:L-a, gap-on-`λ` ≠ ceiling-on-`ρ`). I already conceded (i) and (ii) are silent on a one-sided sign reversal (T2 answer to L-Q3), and (iii) closes only the *spectral-gap* route. loop-quantum-gravity's D2 presses the residual: **some non-gap term in the reduced `S_SA` could produce `∂H²/∂ρ < 0` without re-expansion.** I now name the specific term and confirm it is a live candidate, against my own prior:

- **The `a_4` moment is the candidate one-sided-ceiling operator, and it is NOT a linear matter term.** loop-quantum-gravity's Q3 asked precisely the right question — is the `a_4(τ)` contribution to the reduced kinetic term "more like a (bounded, ceiling-producing) curvature-squared term or a (linear, monotone) matter term?" The canonical answer, which I verified this run via knowledge-MCP rather than assert: **`a_4` is the R² + Weyl² curvature-squared moment** (provenance `canonical_constants.py:469` — "Yang-Mills + Higgs quartic moment"; knowledge-MCP equation `S_eff = (1/16πG_N) ∫ [R + α_HC (a_4/a_2) M_KK^{-2} (curvature² pieces)]`, investigation-13-plan-w1). It enters the effective action as a **higher-derivative curvature term with a fixed `M_KK^{-2}` coefficient** — a Starobinsky-type `R²` structure. A higher-derivative `R²` term in a homogeneous Friedmann reduction is exactly the kind of term that can produce a non-monotone `H²` correction (the Starobinsky `R²` model's modified Friedmann equation carries an `Ḣ`-dependent term that is NOT standard-matter dilution). **So my T1 impulsive argument — which establishes monotone for a *standard-matter* `ρ_relic` dilution — does NOT automatically reach the `a_4`-sourced reduced constraint.** The impulsive injection freezes `ρ_relic`; but the *reduced spectral-action* `H²(ρ)` differs from a standard-matter `H²(ρ)` by exactly the `a_4` curvature-squared term, and whether that term is monotone or ceiling-producing in the homogeneous sector is a genuine reduced-constraint computation. **CONFIRMED: loop-quantum-gravity's D2 is correct; the one-sided ceiling is live because the `a_4` R² term is a candidate saturation-free-but-still-non-monotone operator that my impulsive prior does not exclude.**

- **The Seeley-DeWitt `a_2/a_4` balance sharpens this further — and it cuts toward loop-quantum-gravity, not me.** The framework carries a PROVEN theorem (`Seeley-DeWitt a_2/a_4 balance`, S20a SD-1): `a_4/a_2 ≈ 1000:1 at τ=0`, "R² always dominates." At the fold the ratio is milder (`a_4_FW_zeta/a_2_FW_zeta = 1350.72/2776.17 = 0.487`), but the structural point stands: the `a_4` curvature-squared term is NOT a subdominant perturbation of the reduced constraint — it is comparable to (and at small τ dominates) the `a_2` Einstein-Hilbert term. **A reduced constraint in which the R² term is comparable to the R term is precisely where a non-monotone `H²(ρ)` is most plausible** — this is the regime where higher-derivative gravity departs most from standard Friedmann. So the term loop-quantum-gravity's Q3 isolated is not a small correction I can wave away; it is a load-bearing part of the spectral action. **This honestly raises, not lowers, the prior mass I should assign to ONE-SIDED-CEILING — though it remains below MONOTONE-RAMP, because the R² term being *present and comparable* is not the same as it being *ceiling-producing* (it could equally steepen the monotone ramp). The sign is exactly what L-c computes.**

**(SD2) The net status of MONOTONE: DERIVED at the origin, SOFTENED-PRIOR at the coasting.** My L-Q4 decomposition stands and loop-quantum-gravity accepted it: MONOTONE is DERIVED at the origin (the cosmogenesis bridge — `Q ≈ 1907` sudden quench, four PROVEN/computed anchors: `Q`, `ε = 44.2`, `P_exc = 1.000`, `dS/dτ = +58,673`; the chain `Q ≫ 1 ⇒ |β|² = max ⇒ irreversible monotone quench ⇒ no turnaround` is not a postulate). At the coasting, MONOTONE was a ~0.75 structural prior in Round 1; loop-quantum-gravity's Q3 + the canonical `a_4 = R²` fact lead me to **lower it modestly to ~0.65–0.70**, with the freed mass going to ONE-SIDED-CEILING (now ~0.25–0.30) and a residual ~0.05 to the scheme-dependent INFO outcome. I will not pre-register a number into the verdict (the discipline is: prior recorded in prose, branch deferred to compute), but I record the *direction* of the update honestly: **loop-quantum-gravity's Q3 moved me toward its dissent, not away from it.** That is what a real workshop does, and I will not paper over it with a manufactured monotone verdict.

### EMERGENCE

**(E1) The three-branch verdict-space is a genuine joint co-product, now with each branch's PARENTAGE tagged.** Neither Round-1 binary contained ONE-SIDED-CEILING; it materialized from the collision of loop-quantum-gravity's L-Q3 (does monotone forbid a one-sided ceiling?) with my T2 concession (no — only re-expansion). The Round-2 advance is the *parentage tagging*: MONOTONE-RAMP = framework-native (positive prior, derived-at-origin); SYMMETRIC-BOUNCE = loop-quantum-cosmology-native (excluded here, even-in-`c`); **ONE-SIDED-CEILING = ORPHAN** (neither parent — it is the cell that exists ONLY in the joint analysis). This is the cleanest possible structural statement of what the workshop produced: a verdict-space whose middle cell belongs to no source framework and must be ruled out by computation rather than by either parent's structure.

**(E2) The workshop SPLIT singularity-resolution into two loci and assigned each to a round — and named the OPERATOR that decides the open locus.** loop-quantum-gravity's E2 elevated my T2 L-Q1 answer to a named emergence: **S96 closed the SYMMETRIC bounce (GFT-condensate, quasi-equilibrium — `BOUNCE_transfers = False`); INV7 must close the ONE-SIDED ceiling (bare minisuperspace constraint, the holonomy-analog).** Different objects: the GFT route tested a re-expanding equilibrium condensate (which the `P_exc = 1.000` diabatic relic structurally is NOT), so its result is a *symmetric-bounce* exclusion, not a bare-constraint result. The Round-2 sharpening I add: the open locus is not just "the bare constraint" abstractly — it is **specifically the `a_4` R² curvature-squared term's sign in the homogeneous reduction**. SD1 localizes the entire open question to one operator: does the `a_4(τ)`-sourced higher-derivative term in the reduced `S_SA` produce a `∂H²/∂ρ < 0` region (orphan one-sided ceiling) or steepen the monotone ramp (MONOTONE-RAMP)? This is a STRUCTURAL-change-class output (per `output-standards.md §"Numerical vs Structural"`): the workshop converted "is there a bounce?" into "what is the sign of the `a_4` higher-derivative term in the homogeneous-sector constraint?" — a precisely-posed, scheme-pinned, single-operator computation. That decomposition + operator-localization is the workshop's durable contribution, and it tells the next-session planner *exactly* what L-c computes and *why* the prior round did not settle it.

---

## Workshop Verdict

| # | Sub-question | Source | Status | Structural verdict |
|:--|:-------------|:-------|:-------|:-------------------|
| a | turning-point bounce-form vs monotone H²(ρ) | L-a, Re:L-a, T1/T2, C1/C4, D2, SD1 | **Partial / Emerged** | **THREE-branch verdict-space, deferred to the L-c compute. `{MONOTONE-RAMP (framework-native; DERIVED at the origin via `Q≈1907` quench + 4 PROVEN anchors; strong joint prior on the coasting, ~0.65–0.70), SYMMETRIC-BOUNCE (loop-quantum-cosmology-native; EXCLUDED — over-determined by white-hole irreversibility + the even-in-`c` holonomy + S96 `BOUNCE_transfers=False`), ONE-SIDED-CEILING (structurally ORPHAN — neither parent; LIVE residual ~0.25–0.30 because the `a_4` R²+Weyl² curvature-squared moment is a candidate non-monotone operator my impulsive prior does not exclude)}`. The real contest is MONOTONE-RAMP vs the orphan ONE-SIDED-CEILING; both are causally asymmetric. Branch genuinely compute-decidable; NOT pre-judged.** |
| b | "no bounce" general vs monotone-ramp-scoped | L-b, Re:L-b, C2 | **Converged** | **"No bounce" is SCOPED to the framework's `Q ≫ 1` (sudden-quench) corner on the adiabaticity axis, NOT a general truth. The singularity-resolution ROLE (finite-action traversal of the deep regime) is axis-wide; the bounce MECHANISM is corner-specific (framework-monotone at `Q≫1`, loop-quantum-cosmology-symmetric at `Q≪1`; NON-ANALOGOUS). STRUCTURAL-at-role / ANALOGICAL-at-mechanism (S92 tag). Consumes into the §6.2 capstone scope-edit HY6 (session-promotion, named in Carry-Forward). Zero residual dissent.** |
| c | the single deciding minisuperspace compute | L-c, Re:L-c, T3, C3, Q2 | **Converged** | **ONE compute decides it: `inv7_minisuperspace_effective_friedmann.py` — reduce `S_SA(τ)=a_0−a_2+a_4` to the homogeneous-isotropic minisuperspace sector, form `H_eff[a,p_a;D_K]=0`, read off `sign(∂H²/∂ρ_relic)` across a ρ-grid. TWO schemes pre-registered (`gap-as-density-ceiling` + `holonomy-analog`); `reduction_scheme` + `μ̄-analog vs μ_0-analog` MANDATORY-pinned (PRU Class-8; the branch is convention-shoppable without it). Joint interpretation pre-registered against the loop-quantum-cosmology existence-robust / value-variable baseline: both-MONOTONE ⇒ robust no-bounce (stronger than §6.2); scheme-split ⇒ scheme-dependent form (weakens §6.2); both-TURNING-POINT ⇒ ceiling present. SESSION-PROMOTION compute (permanent H²(ρ) derivation = session-track per the track-local boundary).** |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **(the deciding one) What is `sign(∂H²/∂ρ_relic)` in the scheme-pinned homogeneous reduction of `S_SA`?** Resolved ONLY by the L-c compute (`inv7_minisuperspace_effective_friedmann.py`). Branch ∈ {MONOTONE-RAMP, ONE-SIDED-CEILING, SYMMETRIC-BOUNCE(excluded)}. Pre-registered, not pre-judged.
2. **Is the `a_4` (R² + Weyl²) curvature-squared moment monotone-steepening or ceiling-producing in the homogeneous sector?** This is the technical heart of Q1 (the open locus localized to ONE operator, SD1). A Starobinsky-type `R²` term CAN produce a non-monotone `H²` correction; whether it does here is the bare-constraint computation. Sub-question of #1 but operator-specific.
3. **Is the functional form scheme-DEPENDENT?** If `gap-as-density-ceiling` returns MONOTONE while `holonomy-analog` returns TURNING-POINT, the honest outcome is "scheme-dependent functional form" (INFO branch), mirroring loop-quantum-cosmology's own reduction-level scheme-dependence — and it would weaken the unscoped §6.2 prose further than (b) alone. Pre-registered as a branch, decided by running BOTH schemes.
4. **Does the Two-Manifold Non-Embedding Theorem (S74) widen "scoped" toward "general for the bridge"?** Residual scope-pin difference between the two agents (RESOLVED as the same claim from two scopes — neither says "universal"); recorded for completeness, not an open compute. NOT carried forward as a gate.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- `S_SA(τ_fold) = a_0 − a_2 + a_4 = 6440 − 2776.165389 + 1350.7216 = 5014.556211` (this-run check; the homogeneous-reduction input).
- `a_4/a_2` ratio at the fold pinned: `1350.7216 / 2776.165389 = 0.487` (vs the S20a `≈1000:1 at τ=0` Seeley-DeWitt balance — the curvature-squared term is milder at the fold but still comparable to Einstein-Hilbert, not subdominant).
- Coasting-branch monotone prior re-pinned `~0.75 → ~0.65–0.70`, with the freed mass to ONE-SIDED-CEILING (`~0.25–0.30`) and the scheme-dependent INFO (`~0.05`). Direction of the update: loop-quantum-gravity's Q3 + the canonical `a_4 = R²` fact moved the prior TOWARD its dissent. (Recorded in prose only; NOT pre-registered into the verdict — branch deferred to the compute.)
- Impulsiveness anchors re-confirmed canonical this run: `dt_transit = 1.1301575e-3 M_KK^{-1}` (knowledge-MCP); `H_fold·dt_transit = 0.665 < 1`; `τ_relax/dt_transit = 60.2`.

#### (b) Structural changes

- **Binary → three-branch verdict-space** for (a): `{TURNING-POINT, MONOTONE}` (Round-1 binary, both agents) → `{MONOTONE-RAMP, ONE-SIDED-CEILING, SYMMETRIC-BOUNCE}` (joint emergence E1). The middle cell exists ONLY in the joint analysis.
- **ONE-SIDED-CEILING reclassified loop-quantum-cosmology-adjacent → structurally ORPHAN** (C4 / Q1): the canonical loop-quantum-cosmology bounce is *necessarily* even-in-`c`, so a causally-asymmetric turning point belongs to NEITHER parent framework. The residual is a logical-residual-to-rule-out, not a positively-motivated structure.
- **SYMMETRIC-BOUNCE exclusion: one-sided-dynamical → two-sided-structural** (C1). Round 1 excluded it from the transit side only (irreversibility); Round 2 added the loop-quantum-cosmology-side exclusion (even-in-`c` holonomy) + the S96 GFT leg. Three independent legs.
- **Singularity-resolution SPLIT into two loci, each assigned to a round** (E2): S96 closed the SYMMETRIC bounce (GFT-condensate, quasi-equilibrium); INV7 must close the ONE-SIDED ceiling (bare minisuperspace constraint). Different objects — the prior round's `BOUNCE_transfers=False` does NOT settle the bare-constraint question.
- **Open locus localized to ONE operator** (SD1 / E2): the entire open (a)-question reduces to "what is the sign of the `a_4` (R² + Weyl²) higher-derivative term in the homogeneous-sector constraint?" — a Starobinsky-type term that CAN be non-monotone. "Is there a bounce?" → "what is the sign of the `a_4` reduced-constraint term?"
- **"No bounce" scope boundary NAMED** (b, C2): unscoped general claim → scoped to the `Q ≫ 1` adiabaticity corner (STRUCTURAL-at-role / ANALOGICAL-at-mechanism). This consumes into the §6.2 capstone scope-edit HY6.

### What Holds

- **The framework's monotone-ramp ORIGIN is DERIVED and unchanged.** The cosmogenesis-bridge chain `Q ≈ 1907 ⇒ |β|² = max ⇒ P_exc = 1.000 ⇒ irreversible monotone quench ⇒ no turnaround`, anchored on four PROVEN/computed facts (`Q`, `ε = 44.2`, `P_exc = 1.000` atlas T1, `dS/dτ = +58,673`), is not touched by the workshop — both agents accept it. The contest is entirely about the COASTING functional form, not the origin's irreversibility.
- **SYMMETRIC-BOUNCE is excluded for this substrate** — over-determined by three independent arguments (white-hole irreversibility S85 PROVEN; even-in-`c` holonomy; S96 GFT `BOUNCE_transfers=False`). The capstone §6.2 "no symmetric throat, no bounce" prose is CORRECT about the framework's monotone-ramp mechanism.
- **The never-closing spectral gap is PROVEN and unchanged** (`λ_min = 0.790 at τ=0.27`, W3 S17a) — but it is the WRONG OBJECT for a density ceiling (gap-on-`λ` `[M_KK]` ≠ ceiling-on-`ρ` `[M_KK⁴]`; no `sin²` holonomy-saturation operator in linear moments). Both agents now agree; the `gap-as-density-ceiling` scheme returns MONOTONE by construction.
- **The single-τ-slice is silent on `∂H²/∂ρ`** — a slice is a point, not the function. The functional form requires the τ-dependence of the moments AND the reduced constraint. Both agents agree; this is why the L-c compute (not the slice) is the adjudicator.

### What Breaks or Strains

- **The UNSCOPED "no bounce" claim strains** (the substantive (b) finding). An unscoped "no bounce" implicitly claims to have *refuted* the loop-quantum-cosmology bounce — which the framework has not done (it established that ITS OWN `Q ≫ 1` dynamics resolve the singularity differently). The capstone §6.2 prose should read "no bounce UNDER monotone-ramp transit dynamics" (scope edit HY6). STRUCTURAL strain, not numerical: the unscoped form is a vulnerable over-claim; the scoped form is strictly stronger (it names the falsifiable discriminator — the L-c compute).
- **The monotone-on-COASTING prior strains under the `a_4` R² term** (the honest self-dissent SD1). My impulsive argument (T1) establishes monotone for *standard-matter* dilution but does NOT reach the `a_4`-sourced reduced constraint, which differs from standard Friedmann by exactly a Starobinsky-type curvature-squared term. Because `a_4` is comparable to (and at small τ dominates) `a_2`, this is the regime where a non-monotone `H²(ρ)` is MOST plausible. The prior survives (MONOTONE still the strongest branch) but is genuinely soft at the coasting — and only the L-c compute, not the prior, resolves it.
- **The functional form may be SCHEME-DEPENDENT** (the pre-registered INFO branch). If the two schemes split, the framework's "no bounce" is partly a scheme choice — mirroring loop-quantum-cosmology's own 25-year reduction-level scheme-dependence. This would weaken §6.2 further than the (b) scope-edit alone. It is a pre-registered branch, not a discovered surprise — and it is why the `reduction_scheme` pin is MANDATORY (the branch is convention-shoppable without it).

### Carry-Forward Computations (MATH ONLY — propagate via session-promotion to the next session)

**Discriminator (4-field test)**: an item belongs here iff it satisfies ALL FOUR fields (What / Inputs / Gate / Effort). The named minisuperspace effective-Hamiltonian-constraint compute (sub-question c) acquires its full PRDR machinery pin at session-promotion (track-local boundary), NOT at this investigation plan-freeze.

**CF-INV7-W4-1-MINISUPERSPACE — the single deciding compute (SESSION-PROMOTION).**

> **NOTE — this is a SESSION-PROMOTION compute.** The permanent `H²(ρ_relic)` derivation is session-track per the track-local boundary (`gate-verdicts.md §"Investigation-Track Canonical Path"` — investigation results enter the permanent-results ledger ONLY when lifted into a `/rclab-plan` session-mode plan and re-computed under a `session-{N}` gate). The full PRDR machinery pin (per `epistemic-discipline.md §"Pre-Registration Completeness"`) is acquired at session-promotion, NOT at this investigation plan-freeze. `/rclab-investigate --investigation 7` lifts this item into the S{N+1} session plan.

1. **What**: `inv7_minisuperspace_effective_friedmann.py` — reduce the spectral action `S_SA(τ) = a_0(τ) − a_2(τ) + a_4(τ)` to the homogeneous-isotropic minisuperspace sector; form the effective Hamiltonian constraint `H_eff[a, p_a; D_K] = 0`; invert for `H² = (ȧ/a)²` as a function of the homogeneous relic energy density `ρ_relic`; emit `H²(ρ)` on a ρ-grid spanning `[0, ρ_relic^max]` and compute `s_∂ ≡ sign(∂H²/∂ρ)` across the grid. **Run TWO pre-registered schemes** (Sharpening (i)): `gap-as-density-ceiling` (does the spectral gap propagate into a `(1−ρ/ρ_c)` factor? — predicted MONOTONE by the dimensional argument, no `sin²` saturation operator in linear moments) AND `holonomy-analog` (construct the closest bounded-function analog of `sin²(μ̄c)/μ̄²` from the moments; the most loop-quantum-cosmology-favorable construction). **The technical heart (SD1): determine the sign of the `a_4` (R² + Weyl²) curvature-squared term in the reduced kinetic/constraint structure** — Starobinsky-type, can be non-monotone; this single operator decides MONOTONE-RAMP vs the orphan ONE-SIDED-CEILING.
2. **Inputs**:
   - `a_n(τ)` moment trajectories (zeta-regulated) + their τ-derivatives (for the reduced kinetic term): `a_0_FW_zeta = 6440`, `a_2_FW_zeta = 2776.165389`, `a_4_FW_zeta = 1350.7216` at `τ_fold = 0.190` — `canonical_constants.py:610/611/469`.
   - `ρ_relic = 26.553854 M_KK` (B1+B2+B3 Bogoliubov band sum, S96 §W1-5) — the homogeneous source density (workshop-cited; promote to a canonical pin at session-promotion if not already present).
   - `λ_min(τ)` spectral-gap trajectory (`W3: Spectral Gap`, S17a; `min = 0.790 at τ=0.27`, never-closing) — the candidate ceiling source for the `gap-as-density-ceiling` scheme.
   - `G_eff = M_KK²/(16π a_2)` (induced Newton constant; `8πG_eff/3 = 5.601903e-38`, capstone §6.3) — the source-leg prefactor.
   - `M_KK = 7.428660036284456e16 GeV` (knowledge-MCP, CONST-FREEZE-42) — the single imported scale.
   - The Seeley-DeWitt `a_2/a_4` balance theorem (S20a SD-1) — establishes the `a_4` curvature-squared term is comparable to (not subdominant to) `a_2` Einstein-Hilbert; pins the regime where non-monotone `H²(ρ)` is most plausible.
3. **Gate (PASS criterion — pre-registered `[SIGN]`-trigger; MANDATORY scheme-pin caveat)**: compute `s_∂` across the ρ-grid for EACH scheme.
   - **MANDATORY machinery pin (PRU Class-8; the branch is convention-shoppable without it)**: `reduction_scheme ∈ {gap-as-density-ceiling, holonomy-analog}` (BOTH run), `μ̄-analog vs μ_0-analog`, `convention=`, `regulator=zeta` (a_n^{ζ} per `regulator-pin-discipline.md`), ρ-grid range + step. A `TURNING-POINT` under one scheme and `MONOTONE-RAMP` under another with the scheme chosen AFTER seeing the branch is a `v3-closure-recovery.md` PROHIBITED_ACTIONS Class-1 false closure — the pin is mandatory, not optional.
   - **Branch → verdict `value=` field** (all three carried, so the orphan cell is not silently collapsed): `MONOTONE-RAMP` (`s_∂ = +1` throughout both schemes) | `ONE-SIDED-CEILING` (`s_∂` reverses sign WITHOUT a time-symmetric re-expansion — orphan, causally asymmetric) | `SYMMETRIC-BOUNCE` (`s_∂` reverses WITH re-expansion — EXCLUDED a priori for this substrate; if the compute returns it, the reduction is mis-posed).
   - **PASS/FAIL/INFO mapping**: PASS ≡ `s_∂` determinate and SCHEME-AGREEING (one branch cleanly selected by both schemes); INFO ≡ schemes SPLIT (scheme-dependent functional form — itself a finding, weakens §6.2 to a scheme choice); FAIL ≡ the reduction is ill-posed (no `H²(ρ)` extractable).
   - **Pre-registered joint interpretation** (calibrated against the loop-quantum-cosmology existence-ROBUST / value-VARIABLE baseline, Q2): both-MONOTONE ⇒ robust no-bounce, STRONGER than the scoped §6.2 claim; scheme-split ⇒ scheme-dependent form, WEAKENS §6.2; both-TURNING-POINT ⇒ the framework carries a ceiling, refutes the monotone prior.
4. **Effort**: ~1–2 sessions. The `a_n(τ)` and `λ_min(τ)` trajectories exist (cached; block-diagonal `D_K`, largest L_max=15 block dense-storable per `math-scripts.md`). New work: (i) the minisuperspace reduction of `S_SA` (homogeneous-sector kinetic + potential split, including the `a_4` higher-derivative term's homogeneous form), (ii) the constraint inversion `H_eff = 0 → H²(ρ)` under both schemes, (iii) the `sign(∂H²/∂ρ)` scan. **Depends on**: `canonical_constants.py` (a_n_FW_zeta, M_KK); the S17a `λ_min(τ)` cache; the S96 §W1-5 `ρ_relic` band-sum; the S20a `a_2/a_4` balance theorem.

### Effected In-Session (NON-MATH — completed by transit-dynamics, the final agent, BEFORE TERMINATING)

**Investigation-track scope**: session-track curated-register edits (capstone §6.2 "no bounce" → "no bounce UNDER monotone-ramp" scope-edit, HY6) are session-promotion items — NAME them as carry-forwards above; do NOT effect them here. Any genuinely investigation-track non-math item (e.g., a cross-reference inside this workshops/ directory) is executed now with a concrete file edit and checked off below.

**No investigation-track non-math items to effect.** The one consuming curated-register edit — the capstone §6.2 scope-edit HY6 ("no bounce, asymmetric white hole" → "no bounce UNDER monotone-ramp transit dynamics") — is a SESSION-TRACK edit (a curated-register prose change per `capstone-hygiene-gate.md` Q3, owned by the capstone designated writer; the §7 falsifier surface is `mack-cosmic-bridge`'s domain). Per the investigation-track scope boundary (workshop spec line 29), it is NAMED here as a session-promotion item for `/rclab-investigate --investigation 7` to lift into the session-track housekeeping ledger, and is NOT effected in this workshop. It depends on the (b) Converged verdict above (the scope-pin is settled) and is independent of the L-c compute (HY6 lands regardless of which branch L-c selects, because (b) is already converged; the L-c result may FURTHER scope §6.2 if the schemes split, but the monotone-ramp scoping holds unconditionally).

- HY6 (session-promotion, NOT effected here): capstone §6.2 prose "no bounce, asymmetric white hole" → "no bounce UNDER monotone-ramp transit dynamics" — designated-writer reviewed patch (NOT a bulk append) per `feedback_framework-hygiene.md`; reconcile against Atlas D04 + the (b) Converged verdict; route via `/rclab-investigate --investigation 7` close.

This workshop mutated NO session-track register (capstone, Atlas, permanent-results-registry, canonical_constants.py, knowledge-MCP all untouched) — scope-compliant.

### Closing Line

The workshop converges: SYMMETRIC-BOUNCE is over-determined-excluded (white-hole irreversibility + even-in-`c` holonomy + S96 GFT), "no bounce" is scoped to the `Q ≫ 1` corner (b, Converged), and the deciding compute is named and scheme-pinned (c, Converged) — but it does NOT manufacture a monotone win: the orphan ONE-SIDED-CEILING survives as a live residual because the `a_4` (R² + Weyl²) curvature-squared moment is a candidate non-monotone operator my impulsive prior does not exclude, so `sign(∂H²/∂ρ)` is genuinely compute-decidable, not pre-judged. The substrate IS the `S_SA(τ)` ramp; whether its homogeneous-sector readout carries a one-sided ceiling is the sign of the `a_4` term in the scheme-pinned minisuperspace constraint — one operator, one compute, three branches, branch deferred.
