# Seed — Chunk w2 (Wave 2: §VII.CK D4 external-coupling discharge)

**Date**: 2026-06-26   **Investigator**: phonon-first-cosmologist   **Source**: session-115-w2-workingpaper.md + session-115-plan-w2.md + 2 reviewer deliverables (Axis-A spectral-geometer, Axis-B volovik)
**Wave summary**: W2-1 `S115-VIICK-D4-DISCHARGE-UNCONDITIONAL` = **INFO** (composite). Stage-2 two-agent BLIND cross-axis verify of the §VII.CK D4-external JOINT clause: {A-leg=INFO, B-leg=PASS, JOINT-A=PASS, JOINT-B=PASS} → PASS-AND (any INFO, no FAIL ⇒ INFO). The D4-external **conclusion** — right-regular `R_{E_α}` external-as-a-coupling via the crossed product `A_K⋊SU(3)_R`, genus COMPLETE for A_K-INTERNAL couplings — is PASS-AND'd on **both** blind axes; the composite is driven to INFO solely by the **D4 exclusion-mechanism contest** (Axis-A `t(R_X)=0` commutant/leg-membership vs registry/Axis-B `t(O)=±1` center-character). That contest is **ALREADY ROUTED** as `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM` (housekeeping §B, mirrored to the W2 WP CF) — the ledger's standing directive (line 91) is "do NOT re-surface it as a workshop." §VII.CK stays **STAGE-3-PERMANENT (D4-open)**; no UNCONDITIONAL flip. Gate ran (verdict file L13 + dual-SHA L14 + D4-CONTEST L15; WP §W2-1 COMPLETED) — not a pre-compute shell.

## No candidates

The one live tension in W2 — the D4 exclusion-mechanism contest — is pre-routed to `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM` (housekeeping §B + W2 WP CF). I examined the wave substance for tensions OTHER than the routed one against the rule's §"How to identify a real workshop" signals and the Q1 discriminator, and found NONE that survive:

- **No FAILs.** The composite is INFO; both non-contested legs are PASS. There is no FAIL admitting multiple structural readings.
- **The composite-collapse to INFO is SETTLED by pre-registration, not contested.** The plan-frozen operator (plan §W2-1 item (1): `composite = PASS iff ALL of {A-leg, B-leg, JOINT-A, JOINT-B} ∈ {PASS}; any INFO with no FAIL ⇒ INFO`) was applied bit-faithfully (closeout `assert composite == "INFO"`). Both reviewers' machine-readable blocks are *consistent* with that collapse; neither argues for PASS. Re-litigating the collapse would be post-hoc pre-registration editing (Class-3 PROHIBITED_ACTION), not an adversarial physics adjudication. No reading-divergence exists here.
- **No divergence on the D4-external CONCLUSION itself.** Both blind axes independently PASS the JOINT clause (external-as-a-coupling, crossed-product home, A_F-independence, genus complete for A_K-internal couplings — Axis-A §II.4 enumerates conjuncts 1–4 TRUE; Axis-B §"D4-external JOINT clause" three conjuncts PASS). The *only* disagreement is on the exclusion MECHANISM, i.e. the routed contest.
- **Downstream consumers are on firm ground.** The named consumer `CF-S115-LEPTON-PMNS-FORCED-TEXTURE` (plan `fb_pair.backward`) consumes the D4-external CONCLUSION (PASS-AND on both axes), NOT the genus-completeness flip; §VII.CK's STAGE-3-PERMANENT-(D4-open) status is unambiguous in the registry after the W2-1 STATUS pointer. No adversarial tension to schedule.
- **I concur with the ledger's COMPUTE-CF routing of the mechanism contest (not a workshop), so no "routing-is-wrong" process observation is owed.** The math content is essentially DECIDED: `t(R_X)=0 ∀ su(3)_R generators` is elementary su(3) rep theory (the center `ωI` is scalar ⇒ commutes with every Lie-algebra generator; the roots lie in the root lattice = ker of the center character), and it is INDEPENDENTLY corroborated by the framework's own `connes-r2.md` PROVEN findings ("the multiplicity leg is R_X-active; the correct wall is the commutant one"). Axis-B's affirming `±1` is, on Axis-A's diagnosis, the **generation-slot-permutation** grading mislabeled as the **center-character** grading. A decided question whose residual work is a mechanical corrigendum (mack sole-writer) + a Stage-2 blind dual-reviewer RE-VERIFY on the corrected mechanism is a COMPUTE gate (CF-S116), NOT a 2-agent R1/R2/R3 rebuttal with a genuinely-open outcome. The §B compute-CF routing is correct.

W2 produced **zero genuine workshops and zero Slot-1 solos**. This is the honest count — exactly the outcome the rule anticipates when a content-heavy wave's single tension is already routed.

## Slot 1 candidates — solo reviews (/rclab-review)

(none — see `## No candidates`)

## Slot 2 candidates — workshops (/rclab-workshop)

(none — see `## No candidates`)

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W2 DEPENDS on W1-1 (`S115-VIICK-STAGE2-VERIFY`) — clean, satisfied dependency, NOT a conflict.** W1-1 = PASS on disk (`computations/session-115/s115_gate_verdicts.txt` L7: `composite_PASS=True`, D1/D2/D3 PASS each axis, §VII.CK STAGE-1-CANDIDATE → STAGE-3-PERMANENT, D4-open RETAINED), so W2-1 ran as the full Stage-2 verify (NOT the plan-anticipated PRE-REG-INC mechanical-closure branch). W2-1 correctly consumed W1-1's PASS; the two gates verify DISTINCT clauses on the SAME §VII.CK slot (W1-1 = D1–D3 closed-class; W2-1 = D4-external). (Per chunk scope I did not read W1 files; dependency status read from the W2 docs + the verdict file.)
- **Cross-wave reviewer-independence constraint binds AGAIN at S116.** The two §VII.CK STAGE-3 verifies landed with 4 DISJOINT reviewers — W1-1 `{lizzi, kitaev}` ⊥ W2-1 `{spectral-geometer, volovik}` — per the §EVOI.BF cross-promotion-independence basis. Surfacing for the consolidator/planner: the S116 D4-re-verify inside `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM` will need a disjoint blind pair again; if the §EVOI.BF independence basis is to extend across the THIRD §VII.CK verify it must avoid all four of `{lizzi, kitaev, spectral-geometer, volovik}` (plus the standing `excluded_authors` set: connes, paasch, vdd, baptista, kk). This is a forward reviewer-selection constraint, not a tension.
- **EVOI status (already in-table — nothing NEW to route in).** The W2 INFO leaves the EVOI rank-9f frontier row `D4-RIGHT-ROOT-SU(3)_R-ADMISSIBILITY` NOT retired (W2-1 was INFO, not PASS); per housekeeping line 92 it re-points to `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM`. It is already listed (S115 EVOI re-stamp, `evoi-framework.md` L7 "NEW rank-9f D4-admissibility frontier row"), so no consolidator action to route it INTO §1–§4 — only a re-point at S116 plan-freeze (already noted in the ledger).

## Carry-forwards (route to W2 WP CF section, NOT this schedule)

- **[already-routed]** D4-mechanism contest → `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM` (housekeeping §B; mirrored to `session-115-w2-workingpaper.md §"Carry-Forward Computations"`). Do NOT schedule as a workshop; listed here only for completeness.
- **[Q-other / optional / low-EVOI]** Reviewer-A §V.2 optional compute `S116-VIICK-D4-COMMUTANT-MULTISECTOR` — explicit crossed-product Ω¹ verification at the next Peter–Weyl sectors (`p+q ≤ 4`): build `1⊗E_α^*` + the left-`A_K` calculus image per sector and confirm `1⊗E_α^* ∉ ⊕ B(V_{(p,q)})⊗1` (non-scalar-on-the-multiplicity-leg) beyond the fundamental, strengthening the corrigendum's L_max-invariance claim numerically. **NOTE for the consolidator/planner**: this surfaced in the Axis-A deliverable §V.2 but is NOT currently lifted into the W2 WP CF block (which carries only `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM`). It is a COMPUTE carry-forward, NOT a workshop, and is largely SUBSUMED by the CF-S116 re-verify (which exercises the commutant/leg-membership argument). Surfaced for a lift-or-drop decision; if lifted it routes to `/rclab-plan` via the WP CF, not this schedule.

## Wave-by-wave digest (consolidator background)

**Gate.** W2-1 `S115-VIICK-D4-DISCHARGE-UNCONDITIONAL` (gate_type compute; `[VERIFY-THEOREM]` + `[CHAIN]` sub-trigger; classification GEOMETRIC). Stage-2 two-agent BLIND cross-axis independent-verify (`joint-theorem-promotion.md §"Stage 2"`) of the §VII.CK D4-external JOINT clause — the FOURTH and final door of the SHAPE-branch homogeneity-obstruction genus. Executors: Axis-A `spectral-geometer` (Ω¹-membership / selection-rule leg) × Axis-B `volovik-superfluid-universe-theorist` (isometry / commutant / crossed-product-image leg), dispatched in parallel, neither receiving the S114 W-2 workshop transcript. Closeout writer: `gen-physicist`.

**Verdict = INFO (composite).** {A-leg=INFO, B-leg=PASS, JOINT-A=PASS, JOINT-B=PASS} ⇒ one INFO, zero FAIL ⇒ INFO under the pre-registered PASS-AND operator (verified in-script `assert composite == "INFO"`; no forced PASS, no forced FAIL). The Stage-2 blind protocol worked exactly as designed: a reviewer (spectral-geometer) who never saw the S114 W-2 transcript caught a registry mechanism mis-statement.

**The contest (substantive content of the INFO).** Both axes reach `R_{E_α} ∉ Ω¹_{D_K}(A_K)` and PASS the JOINT clause; they disagree on the EXCLUSION MECHANISM. Axis-A Sage-derived `t(R_X)=0` for ALL su(3)_R generators (every generator sits in the center-blind adjoint `8=(1,1)`; roots ∈ root lattice = ker of the center character) ⇒ the wall is the **commutant / Skolem–Noether leg-membership** mechanism (`Ω¹_{D_K}(A_K) ⊆ ⊕ B(V_{(p,q)})⊗1`; `R_{E_α}=1⊗E_α^*` non-scalar on the multiplicity leg ⇒ ∉ `B(V)⊗1`), and the registry's `t(O)=±1` center-character selection rule is mis-stated (it conflates the Z₃ center-character grading — where roots carry 0 — with the generation-slot-permutation grading). Independently corroborated by `connes-r2.md` PROVEN. Axis-B affirmed `t(O)=±1` (its own `ℤ/3` exact check read the off-diagonal generation handle as carrying `t=±1`) AND the commutant identity `[L,R]=0` Sage-exact over all 64 su(3)×su(3) pairs (`ℚ[i]`; float shadow = registered `7.25e-17`).

**Composite-collapse reasoning (why INFO, not PASS or FAIL).** The JOINT clause is PASS-AND'd across both axes (logical AND): JOINT-A=PASS ∧ JOINT-B=PASS = PASS, so the D4-external conclusion holds on both. The composite is dragged to INFO ONLY by Axis-A's single-leg INFO on the registry's mechanism FRAMING — the conclusion is unaffected. Per `joint-theorem-promotion.md` Stage-2 INFO criterion, an INFO clause is a Stage-2-INFO-deferred item; NO UNCONDITIONAL flip.

**Disposition.** §VII.CK STAYS STAGE-3-PERMANENT (D4-open scope qualifier RETAINED) — the W1-1-determined state; D1–D3 STAGE-3-PERMANENT untouched. The closeout added an idempotent STATUS-unreconciled pointer to BOTH the Four-door D4 row (registry L22439) and the D4-disposition annotation (L22460), naming the mechanism contest, stating the D4-external conclusion is UNAFFECTED, retaining the existing `t(O)=±1` text for the audit trail, and forward-routing mechanism reconciliation + the UNCONDITIONAL flip to `CF-S116-VIICK-D4-MECHANISM-CORRIGENDUM` (registry SHA `06d7162a → fbcd4b1e`). The W3-1 residual `=1.000000` EXACT readback (`s114_yuk_rightreg_connection.npz`) is the numerical shadow of the leg-membership exclusion and is consistent with EITHER mechanism reading — it confirms the conclusion, does not arbitrate the contest.

**Substrate framing (GEOMETRIC; direction preserved).** The substrate IS the spectral triple `(A_K, H_K, D_K, γ₉, J)` on Jensen-deformed SU(3), `A_K = ℂ⊕ℍ⊕M₃(ℂ)`, KO-dim 6. SU(3)_R IS the COMMUTANT of A_K's left-regular action — a genuine internal isometry of the group manifold (`[L_g,Y_R]=0` Sage-exact; `[D_K,Y_R]=0` because D_K is assembled from left-invariant fields), NOT a hand-added external field. Direction: D_K eigenvalues + the Peter–Weyl tensor-leg structure → `R_{E_α}=1⊗E_α^*` non-scalar on the multiplicity leg → `R_{E_α} ∉ Ω¹_{D_K}(A_K)` → the SU(3)_R coupling is admissible only as the EXTERNAL image under the crossed product `A_K⋊SU(3)_R` (Kasparov external product). The SYMMETRY is substrate-internal; only the COUPLING is the external image — "external-as-a-coupling," not "a field added IN a container." The mechanism contest is a question about WHICH substrate-internal arithmetic forces the exclusion, never about the direction of explanation (which is preserved on both readings).
