# Session 117 Plan — Wave 7: w0 transport-degree & categorical-wall

**Date**: 2026-06-28
**Author**: volovik-superfluid-universe-theorist (generated by /rclab-plan per-wave swarm)
**Owner agent**: volovik-superfluid-universe-theorist (reviewer-origin: S-7 transport-degree solo + W-5 spectral-derivability workshop, Agent B)
**Plan source**: `sessions/session-plan/session-117-context.md §"Wave 7 items"` (+ the named source CF blocks: gate 7-1 → `session-116-w9-w0-transport-degree-synthesis.md §V`; gate 7-2 → `workshops/s116-w0-spectral-derivability.md §"Carry-Forward Computations"`)
**Working paper**: `sessions/session-117/session-117-w7-workingpaper.md`
**Verdict file (canonical)**: `computations/session-117/s117_gate_verdicts.txt` (per `.claude/rules/gate-verdicts.md`)

---

## Wave 7 Summary

This wave resolves the substrate-physics status of the framework's dark-energy equation of state **w0 = −0.918** (`w0_FW`; canonical `get_constant` → −0.918, **Gate: None**, S58 four-fold-lock = Volovik vacuum partition + effacement Γ_eff = 0.99970, Superseded: False) on three orthogonal axes left open by the S116 W9 cluster + the S116 adversarial-review campaign (S-7 solo + W-5 workshop, both already CLOSED-WITH-RESULT at the structural level; these gates are the *forward computes* the campaign pre-registered).

The W-5 workshop (baptista × volovik, R3-FINAL-CLOSED) reclassified w0 PROVENANCE: it is a **Level-1 closed-form THERMODYNAMIC identity** (the q-field vacuum partition's effacement EoS), **NOT** spectral-action-derived — it does NOT join {n_s, m_H, r}. The branch-iv (S102→S116) corridor as a w0-DERIVATION is CLOSED-WITH-RESULT via the CONJUNCTION (i) Weyl-forced −1 wall (theorem-grade) ∧ (ii) categorical w0 ∉ Tr f(D_K) wall (q-theory model-grade). The VALUE (−0.918), GRADE (closed-form), and FALSIFIABILITY are deg-ROBUST and frozen; only the numerical DESI DR3 σ-distances are deg-CONDITIONAL (computed under the PROVISIONAL connes §23 deg=0). This wave's three gates discharge the three remaining computable questions:

- **7-1 `CF-S117-W0-TRANSPORT-DEGREE`** [LIVE successor; subsumes `CF-S117-W0-ANCHOR-FIDELITY` per context dedup #1] — extract the w0 BZ→pivot morphism degree `deg(T_{BZ→pivot})` on the §23 per-observable transport-degree axis. w0 (d_A=0, single-pole s=3) is favored **deg=0 T2-VACUOUS scalar** ⇒ substrate w0 = pivot w0 = −0.918; the W9 → −1.340827 gap is PROXY-ARTIFACT-TYPED (the running λ_max truncation edge is NOT an admissible §23 morphism). Resolves the ONE deg-CONDITIONAL element of the W-5 verdict.
- **7-2 `CF-S117-W0-CATEGORICAL-WALL-GRADE`** [NEW from W-5, VD2b-sharpened] — test whether wall (ii-a) [w0 ∉ Tr f(D_K)] upgrades from q-theory-model-grade to THEOREM-grade via the static-CC-magnitude (a₀ zeroth spectral moment) vs dynamical-w(z)-response (q-field linear-response functional) structural separation.
- **7-3 `CF-S117-BRANCH-IV-L16`** [OPTIONAL — value-neutral L_max diagnostic, NOT a PASS/FAIL gate; downgraded per context dedup #3] — extend the branch-(iv) CAC spread to the sliding window {14,15,16} by building the p+q=16 FB-bounded shell; test whether the |d| ~ 1/λ_max² deceleration continues. Reported as a diagnostic of presentation-normalization hygiene, NOT as a corridor-reopening verdict (the corridor is CLOSED-WITH-RESULT).

**Substrate-first framing** (per `phononic-framing.md`): the fabric IS both its D_K vibrational spectrum AND its q-field vacuum partition — two FACETS of one substrate. w0 = −0.918 is the *thermodynamic* facet (what the partition's effacement effaces to); n_s/m_H are the *vibrational* facet. The branch-iv programme tried to RELOCATE the thermodynamic value into the eigenvalue spread and the moment answered −1 — the substrate REFUSING the relocation. All three gates flow FROM the D_K spectrum / q-field partition at τ_fold = 0.190 TOWARD the emergent DESI w(z) image; none treats the CMB pivot or the DESI detector as a container the substrate sits inside.

---

## Wave 7 Decision Point Prerequisites

Wave 7 is **structurally self-contained** — its substrate-physics inputs are S116-and-earlier closed artifacts (the W9 ρ_B(L) trajectory, the §23.0(5) parity rule, the Volovik q-theory equilibrium theorem, the canonical w0_FW / deg_T_BZ_pivot pins). No S117 upstream wave gates this wave.

Internal ordering inside the wave:

- **7-1 subsumes the former `CF-S117-W0-ANCHOR-FIDELITY`** (context dedup #1): the merged gate FIRST builds the fixed-scale (Λ_Z / M_KK) de-λ_max'd w0 representative (the {running-edge / fixed-edge / no-edge-normalization} design compute the original `-ANCHOR-FIDELITY` CF specified) and THEN extracts the §23 transport degree on that clean representative. The two former CFs are ONE compute; no double-listing.
- **7-3 (OPTIONAL) feeds 7-1 a single trajectory point** (the L=16 fixed-scale ρ_B value), but 7-1 does NOT block on it: 7-1's degree extraction uses the existing {10..15} trajectory, and the L16 point is a refinement, not a prerequisite. If 7-3 is descoped (it is OPTIONAL), 7-1 proceeds on {10..15} unchanged.
- **7-2 is independent of 7-1 and 7-3**: it is a closed-form q-theory structural derivation (no L_max-truncated spectral compute), consuming only the Volovik q-theory equilibrium theorem + the a₀/a₂ moment separation. It may run in parallel with 7-1.

Per `.claude/rules/mechanical-closure-discipline.md`: if any pinned upstream artifact is unexpectedly absent at dispatch, the gate honestly closes PRE-REG-INC (not FAIL) with the blocking artifact named. All four pinned input files were verified present at plan-freeze (see the Input-SHA Ledger).

---

## §W7-1. CF-S117-W0-TRANSPORT-DEGREE

```yaml
# ---- Identity ----
gate_id: "CF-S117-W0-TRANSPORT-DEGREE"
schema_version: "R3"
gate_type: "compute"
trigger: "[SIGN]"                          # directional pre-registration on |deg| vs tol; [VERIFY] degree-extraction + 3-scheme spread in method
classification: "GEOMETRIC"                # structural property of the spectral-action BZ→pivot bridge map on the D_K spectrum at τ_fold; Level-1 single-τ-slice substrate-IS
agent_type: "volovik-superfluid-universe-theorist"   # wave owner + S-7/W-5 author (substrate side); mack co-reviews the deg-conditional DR3 σ-distance consequence
hypothesis: "On the fixed-scale (Λ_Z / M_KK) de-λ_max'd branch-iv representative, w0's BZ→pivot transport degree deg(T_{BZ→pivot}) extracts to 0 (T2-VACUOUS scalar: single-pole s=s'=3 ⇒ Wodzicki −2(s−s')=0, AND the secondary-class scheme-spread Δ_scheme→machine-zero across {APS-1975, Cheeger-Simons, Bismut-Cheeger}), confirming substrate w0 = pivot w0 = −0.918 and PROXY-ARTIFACT-typing the W9 →−1.340827 gap; a clean even nonzero degree would flip w0 to a substrate-natural NON-SCALAR §23 K=3 candidate."

method:
  description: >
    Two-stage. STAGE-1 (subsumes the former CF-S117-W0-ANCHOR-FIDELITY {running-edge /
    fixed-edge / no-edge-normalization} design compute): build the fixed-scale w0
    representative — replace the RUNNING λ_max(L) denominator of the branch-iv Zubarev
    moment ρ_B(L)=mean_Z(L)/λ_max(L)−1 with (a) the FIXED physical-cache edge λ_max(10)
    [ρ_B^fix(L)=mean_Z(L)/λ_max(10)−1] and (b) the NO-EDGE variant [drop λ_max, the bottom-K
    statistic], producing the de-λ_max'd trajectory whose drift is removed (mean_Z frozen
    ~1e-4/shell ⇒ w0^CAC,fix ≡ −0.918 flat). STAGE-2 (§23 degree extraction): on that clean
    fixed-scale representative, extract deg(T_{BZ→pivot}) via (i) the Wodzicki same-class
    two-pole degree deg = −2(s−s') confirming single-pole s=s'=3 ⇒ deg=0, and (ii) the
    secondary-class scheme-spread Δ_scheme(B) = max−min across {APS-1975-secondary-class,
    Cheeger-Simons, Bismut-Cheeger} → machine-zero (the degree-0 homogeneity signature per
    the §18 Composite Bridge-Map Dimensional-Class Admissibility test; secondary-class axis
    ONLY, NOT the orthogonal UV-regulator RD axis). Output: the classified degree + the
    substrate-vs-pivot coincidence verdict + the W9-gap typing (proxy-artifact vs transport).
  producing_script: "computations/session-117/s117_w0_transport_degree.py"

# ---- PRDR Checklist (8 items) ----

# (1) operator
operator:
  type: "degree-classification + sign + scheme-spread"
  form: "deg = −2(s−s') with s=s'=3 (Wodzicki same-class); PASS-SCALAR iff |deg| < deg_tol AND Δ_scheme < spread_tol"

# (2) strict_PASS_boundary
strict_PASS_boundary:
  value: "|deg| < 0.05 AND Δ_scheme < 1e-3 M_KK^2  (PASS-SCALAR primary boundary; the dual-outcome NON-SCALAR / INDETERMINATE / ILL-POSED branches are enumerated in the verdict rubric + dual_prior)"
  direction: "<"

# (3) boundary_reachable_analytically
boundary_reachable_analytically:
  bool: true
  proof_ref: "cross-pillar-bridge-corpus.md §23.0(5) parity selection rule (single-pole s=3 ⇒ Wodzicki deg = −2(3−3) = 0 analytically; the morphism sector is EVEN-degree) + §18.0 Conjunct-1 homogeneity (degree-0 ⇒ Δ_scheme→0 across the {APS,CS,BC} secondary-class axis)"

# (4) reachable_rationals
reachable_rationals:
  includes_integer_mesh: true              # deg ∈ {…,−4,−2,0,+2,…} even-integer Wodzicki mesh; the morphism sector is even-degree
  mesh_density: "even integers (Wodzicki −2(s−s'); HKR cohomology-class ratio = 0)"

# (5) machinery_pin_map
machinery_pin_map:
  N_eval: "6  (L ∈ {10,11,12,13,14,15} trajectory points; +1 if 7-3 lands L=16)"
  L_max: "15  (16 if CF-S117-BRANCH-IV-L16 lands; the L16 point is a refinement, not a prerequisite)"
  scan_range: "[10, 15]  (the L_max trajectory window)"
  step_size: "1"
  tolerance: "deg_tol=0.05 ; scheme_spread_tol=1e-3 M_KK^2"
  scheme: "section-23-Wodzicki-same-class + secondary-class-{APS-1975,Cheeger-Simons,Bismut-Cheeger}"
  convention: "fixed-scale-de-lambda_max + CAC-DERIVED-OFFSET (offset = w0_FW − rho_B(10), zero free parameter)"
  random_seed: "N/A — deterministic"
  GPU_path: "numpy.linalg  (small trajectory + degree-fit ops; no large diagonalization — the spectra are read from the existing caches)"
  # substrate / canonical pins (substrate-first-canonical-sourcing.md compliant):
  w0_FW: "-0.918  (canonical, S58 four-fold-lock, Gate:None — from canonical_constants import)"
  deg_T_BZ_pivot: "2.0  (canonical, S110-CF-CV6B-DS-M4 — the NON-SCALAR reference degree, the Track-NONSCALAR target)"
  Lambda_Z: "1.0  (the Gaussian regulator scale of mean_Z, w_Z(λ)=exp(−λ²/Λ_Z²); S85 W0-7)"
  rho_B_10_lineage: "lockdown −0.577173 (clean fixed-scale) ; W9 (4,4)-filled −0.575207 (trajectory-continuity cross-check); ±0.001966 lineage sensitivity pinned per W-5 precision flag B5.4"
  offset: "DERIVED in-script = w0_FW − rho_B(10)  (NOT a hardcoded literal; the CAC-DERIVED-OFFSET, zero free normalization, per regulator-convention-lockdown.md)"
  alpha_s_substrate_distance_1: "-0.08587279  (canonical S92; the deg=+2 NON-SCALAR sibling reference, contrast anchor)"
  n_s_deg0_anchor: "registry §VII.AU.OP-PROJ Element-3 / §A8.2  (the deg=0 T2-VACUOUS sibling CLASS that w0 joins on PASS-SCALAR)"

# (6) audit_discriminators
audit_discriminators:
  audit_sha256_inputs: ["script", "canonical", "pinmap", "upstream_npz"]
  content_sha256_inputs: ["script"]

# (7) substitution_chain
substitution_chain:
  required: true
  content: |
    Claim: deg(T_{BZ→pivot}) for the clean (de-λ_max'd) w0 = 0 (T2-VACUOUS scalar) ⇒ substrate w0 = pivot w0 = −0.918.

    Step 1: d_A(w0) = 0.
            [w0 = (a₀ zeroth Seeley-DeWitt moment)/(a₂ Einstein-Hilbert moment) dressed by Γ_eff;
             a ratio of spectral moments is dimensionless ⇒ mass dimension d_A = 0. Source: S-7 synthesis §II.1.]
    Step 2: B = (M_KK^{d_A} scale leg) ⊙ (dimensionless structural morphism).
            [§23.0(5) dimensional-class factorization of the composite bridge map.]
    Step 3: Substitute d_A = 0 ⇒ scale leg = M_KK^0 = 1 (TRIVIAL); the 54.04-decade BZ→pivot unit
            conversion is ABSENT (it would only enter through a d_A=1 M_KK^1 leg). Hence the ENTIRE
            transport degree of w0 lives in the dimensionless morphism. [§23.0(5) d_A=0 row.]
    Step 4: The morphism sector is EVEN-degree (parity selection rule): a same-class Wodzicki two-pole
            ratio has deg = −2(s−s') ∈ {…,−4,−2,0,+2,…}; an HKR cohomology-class ratio has deg = 0.
            [§23.0(5) (5.2).]
    Step 5: w0's branch-iv route is pinned at the a₂^{Mellin} SINGLE pole s=3 (poleconv-A-double,
            pole_in_s=3, curvature_grade_n=2; W9 verdict line), with NO square/power relation
            (contrast A_s = H̃², which sources deg=+2). Single pole ⇒ s = s' = 3 ⇒
            deg = −2(3−3) = 0. [S-7 synthesis §II.2; the gap is ADDITIVE (−0.918 − 0.422827), not a
            power/factor signature.]
    Step 6: deg = 0 ⇒ T2-VACUOUS scalar ⇒ T_{BZ→pivot} = identity ⇒ O^pivot = O^substrate.
            [§23.0(1) per-observable transport-degree theorem: O^pivot = O^substrate iff deg is the
             T2-VACUOUS scalar case.]
    Conclusion: substrate w0 = pivot w0 = −0.918. The W9 → −1.340827 gap is sourced 100.08% by the
            RUNNING λ_max-edge denominator, which has NO fixed pole, NO heat-kernel/Seeley-DeWitt image,
            NO continuum limit (∂λ_max/∂L ≈ 0.375, Weyl N(λ)~λ^d) — so λ_max-division is NOT in the
            admissible {Wodzicki, HKR} morphism sector of Step 4 ⇒ the gap is PROXY-ARTIFACT-TYPED, not
            a deg≠0 transport. ∎  (FALSIFIER branch: a CLEAN even nonzero degree extracted on the clean
            representative ⇒ Step-5 single-pole premise fails ⇒ w0 substrate-natural NON-SCALAR, Track-NONSCALAR.)

# (8) input_files
input_files:
  canonical_constants:
    path: "computations/_shared/canonical_constants.py"
    sha256: "<computed-at-runtime>"
  branch_iv_evaluator:
    path: "computations/session-102/s102_branch_iv_canonical_eval.py"
    sha256: "<computed-at-runtime>"     # the ρ_B = mean_Z/λ_max − 1 builder + CAC offset
  master_spectrum_cache:
    path: "computations/session-84/s84_spectrum_cache_L12_tau019.npz"
    sha256: "<computed-at-runtime>"     # τ_fold=0.190 D_K spectrum, λ_max/mean_Z source through L=12
  highl_cache:
    path: "computations/session-106/s106_w1_highl_cache_l1416.npz"
    sha256: "<computed-at-runtime>"     # high-L spectrum extension {13,14,15,(16)}

# ---- Conditional blocks ----
fb_pair:
  forward: "S116-W9-GTBUILDER-L15 (the ρ_B(L) trajectory + spread_CAC + offset_B); §23.0(5) parity rule; the n_s deg=0 anchor §VII.AU.OP-PROJ Element-3; deg_T_BZ_pivot=2.0 NON-SCALAR reference"
  backward: "CF-S117-W0-CATEGORICAL-WALL-GRADE (7-2; the deg-conditional σ-distances freeze on PASS-SCALAR); mack DR3 w(z) §7 falsifier-surface (the deg-CONDITIONAL σ-distance tag is removed on PASS-SCALAR); the §23 K-counter (advances ONLY on PASS-NONSCALAR)"
dual_prior:
  track_A: "Track-SCALAR, prior 0.7 — |deg| < deg_tol AND Δ_scheme < spread_tol ⇒ deg=0 T2-VACUOUS, w0 joins the n_s class, substrate=pivot=−0.918, the W9 −1.340827 CONFIRMED proxy artifact, NO §23 K-advance (a deg=0 confirmation is not the independently-factorization-EXTRACTED new-observable NON-SCALAR degree the K=3 slot reserves for r/α_t)"
  track_B: "Track-NONSCALAR, prior 0.3 — a CLEAN even nonzero degree extracted (|deg−2k|<0.1, k≠0) with Δ_scheme consistent ⇒ w0 substrate-natural NON-SCALAR, joins A_s/α_s, BOTH values real (no-demotion engages), candidate §23 K=3 alongside the reserved r/α_t"
  discriminator: "PASS-SCALAR → 0.9 to track_A ; PASS-NONSCALAR (clean even nonzero deg) → 0.9 to track_B ; INFO (indeterminate) → priors unchanged, σ-distances carry the 'pending' tag"
# composite-precedence (pre-declared; disclosed via a `# composite-precedence:` companion row naming this plan §W7-1):
#   deg=0 clean (|deg|<0.05 ∧ Δ_scheme<1e-3)                  ⇒ composite PASS  (PASS-SCALAR)
#   clean even nonzero (|deg−2k|<0.1, k≠0, Δ_scheme consistent)⇒ composite INFO  (tagged PASS-NONSCALAR-K3-CANDIDATE; routed to §23 K-counter)
#   indeterminate (0.05≤|deg|<2, no clean even integer)        ⇒ composite INFO  (tagged INDETERMINATE; σ-distances carry 'pending CF' tag)
#   ill-posed (fixed-scale rep does NOT de-λ_max cleanly: mean_Z not frozen, or Δ_scheme diverges) ⇒ composite FAIL

# ---- Output artifacts ----
output_artifacts:
  script:
    path: "computations/session-117/s117_w0_transport_degree.py"
    artifact_kind: "script"
    must_contain:
      - "from canonical_constants import"
      - "print_verdict_payload"
  data:
    path: "computations/session-117/s117_w0_transport_degree.npz"
    artifact_kind: "data"
    optional: false                       # {running/fixed/no-edge} ρ_B trajectories + extracted deg + Δ_scheme over {APS,CS,BC}
  plot:
    path: "computations/session-117/s117_w0_transport_degree.png"
    artifact_kind: "plot"
    optional: false                       # the three-normalization w0^CAC(L) trajectories + the deg-extraction fit
  verdict_line:
    path: "computations/session-117/s117_gate_verdicts.txt"
    artifact_kind: "verdict_line"
    must_contain: "^CF-S117-W0-TRANSPORT-DEGREE:.* audit_sha256=[a-f0-9]{64}"
    companion_row_required: true
    schema_v2_3tuple_required: true       # [SIGN] trigger
  wp_section:
    path: "sessions/session-117/session-117-w7-workingpaper.md"
    artifact_kind: "wp_section"
    section_anchor: "### §W7-1. CF-S117-W0-TRANSPORT-DEGREE"
    must_contain:
      - "\\*\\*Status\\*\\*:.*COMPLETED"
      - "\\*\\*Verdict\\*\\*:.*(PASS|FAIL|INFO)"
      - "\\*\\*Output Artifacts\\*\\*"
      - "\\*\\*MCP Pre-Compute Audit\\*\\*"

# ---- Verdict rubric ----
PASS_meaning: "PASS-SCALAR — deg=0 T2-VACUOUS confirmed. w0 joins the n_s class (d_A=0, single-pole, scalar morphism); substrate w0 = pivot w0 = −0.918; the W9 → −1.340827 gap is CONFIRMED PROXY-ARTIFACT (λ_max ∉ admissible §23 morphism); NO §23 K-counter advance; the DR3 σ-distances freeze vs −0.918 DIRECTLY (the deg-CONDITIONAL tag on the W-5 verdict is removed)."
FAIL_meaning: "ILL-POSED extraction — the fixed-scale representative does NOT de-λ_max cleanly (mean_Z not frozen to ~1e-4/shell, or Δ_scheme diverges across {APS,CS,BC}), so no admissible §23 degree can be assigned. The substrate value −0.918 is UNCHANGED (it is the transport INPUT, never its output); only the proxy-design step failed. Routes back to a normalization re-design."
INFO_meaning: "Two structured branches. (a) PASS-NONSCALAR-K3-CANDIDATE: a CLEAN even nonzero degree (|deg−2k|<0.1, k≠0) ⇒ w0 substrate-natural NON-SCALAR, joins A_s/α_s, BOTH values real (no-demotion engages), candidate §23 K=3 alongside reserved r/α_t. (b) INDETERMINATE: 0.05≤|deg|<2 with no clean even integer ⇒ deg-resolution deferred; substrate −0.918 UNCHANGED; the DR3 σ-distances carry the 'computed under provisional deg=0; pending' tag (the W-5 BD2 deg-conditional element stays open)."

# ---- Effort + framing ----
effort:
  files_created: ["computations/session-117/s117_w0_transport_degree.py", "computations/session-117/s117_w0_transport_degree.npz", "computations/session-117/s117_w0_transport_degree.png"]
  estimated_time: "low-medium (~2-3 hours, 1 agent — fixed-scale normalization design + degree extraction + 3-scheme spread on EXISTING {10..15} trajectories; no new shell build)"

substrate_framing: |
  GEOMETRIC, Level-1 single-τ-slice substrate-IS (τ_fold = 0.190). The substrate IS the finite
  spectral triple (A_K = ℂ ⊕ ℍ ⊕ M_3(ℂ), H_K, D_K(τ_fold)). w0 is read FORWARD from the D_K
  spectrum as the a₀/a₂ Seeley-DeWitt moment-ratio dressed by the impedance-effacement Γ_eff —
  a DIMENSIONLESS (d_A=0) ratio. Direction of explanation: D_K eigenvalues → spectral-action
  moments a₀/a₂ → the dimensionless EoS ratio w0 → (under the §23 bridge map T_{BZ→pivot}) the
  emergent DESI w(z) image. The §23 question is purely about the bridge map's degree: because
  d_A=0 the scale leg is trivial (M_KK^0=1), so the substrate value and the CMB-pivot image
  COINCIDE iff the dimensionless morphism is the T2-VACUOUS scalar (deg=0). Container-thinking
  FORBIDDEN: the W9 −1.340827 asymptote is NOT "the spectral action's real w0 measured at high
  L" — it is the L→∞ artifact of a RUNNING truncation-edge normalization (λ_max), a non-substrate
  quantity with no continuum limit, injected by the S85 Zubarev proxy DEFINITION. The substrate IS
  −0.918 (the q-field partition's effacement); the detector measures its emergent transport image,
  which on the favored reading is the same number.
```

---

## §W7-2. CF-S117-W0-CATEGORICAL-WALL-GRADE

```yaml
# ---- Identity ----
gate_id: "CF-S117-W0-CATEGORICAL-WALL-GRADE"
schema_version: "R3"
gate_type: "compute"
trigger: "[VERIFY-THEOREM]"                # structural derivation: does the static/dynamical separation force ∂(EoS)/∂(a₀)=0 partition-independently? + the [CHAIN] linear-response substitution chain
classification: "PHONONIC"                 # the EoS response IS the q-field vacuum-partition effacement (DILUTION-CC two-fluid w=−1[vacuum]+w=0[GGE]); the static a₀ side is GEOMETRIC (zeroth spectral moment)
agent_type: "volovik-superfluid-universe-theorist"   # q-theory equilibrium theorem + linear-response is volovik-domain; connes co-reviews the a₀-as-Tr f(D_K) spectral-moment side
hypothesis: "Wall (ii-a) [w0 ∉ Tr f(D_K): the EoS response is the q-field vacuum partition's effacement, not a static D_K spectral moment] upgrades from q-theory-model-grade to THEOREM-grade because the static-CC-magnitude (a₀ zeroth Seeley-DeWitt moment, an ADDITIVE constant in the q-field free energy) vs dynamical-w(z)-response (a FIRST-DERIVATIVE linear-response functional of the q-field 4-form variable) functional-type separation FORCES ∂(EoS response)/∂(a₀ static moment) = 0 independent of the specific Volovik partition (independent of the Γ_eff=0.99970 value)."

method:
  description: >
    Closed-form substrate-physics structural derivation + linear-response argument (Sage-symbolic;
    NO L_max-truncated spectral compute). (1) Set up the q-field free energy F(q) with the static CC
    magnitude entering as the ADDITIVE a₀^{ζ} zeroth Seeley-DeWitt moment ε_Λ: F(q) = F_dyn(q) + ε_Λ.
    (2) Define the dynamical EoS response w(z) = p/ρ as the ratio of FIRST derivatives of F(q) along
    the q-perturbation per the Volovik q-theory thermodynamic identities (p = −F, ρ = q F' − F;
    Papers 13/23/25). (3) Compute the cross-derivative ∂w/∂ε_Λ = ∂w/∂a₀ SYMBOLICALLY (Sage QQ) and
    test whether it vanishes IDENTICALLY by the additive-constant structure. (4) CRITICAL discriminator
    — partition-independence: re-derive ∂w/∂a₀ under a 1-parameter FAMILY of partition forms Γ(λ;θ)
    (θ a deformation around Γ_eff=0.99970, N≥5 members) and confirm the vanishing for ALL members
    (theorem-grade) vs only the Volovik member (model-grade). (5) Cross-check against Volovik's
    equilibrium theorem (Paper 05: ρ_Λ=0 at equilibrium, the µ-cancellation, LAB-GROUNDED in 3He-B).
    Output: the structural verdict — (ii-a) theorem-grade / model-grade / PARTIAL — with the VD2b
    two-grade decomposition [placement (ii-a) vs value (ii-b) Γ_eff].
    OPERATOR-DISTINCTNESS GUARD: the operator here is the q-field linear RESPONSE-derivative
    ∂/∂(δq); under it an additive constant (a₀) is ANNIHILATED (∂const/∂δq=0). This is the OPPOSITE
    of the W-4 (S116) K-log-derivative d²/d(ln K)², under which an additive-IN-TRACE term SURVIVES
    (math-scripts.md §"Scope boundary"). Different operators, opposite annihilation — the script MUST
    NOT cite the W-4 additive-survival result as evidence against this gate's additive-vanishing.
  producing_script: "computations/session-117/s117_w0_categorical_wall_grade.py"

# ---- PRDR Checklist (8 items) ----

# (1) operator
operator:
  type: "structural-identity (characterization) + partition-independence test"
  form: "∂w/∂a₀ = 0 identically (Sage-symbolic), AND ∂w/∂a₀ = 0 for ALL members of the partition family Γ(λ;θ) (partition-independence ⇒ theorem-grade)"

# (2) strict_PASS_boundary
strict_PASS_boundary:
  value: "|∂w/∂a₀| < 1e-12 (machine-zero, Sage QQ exact) for ALL N≥5 partition-family members  (characterization/closed-form gate per the S95 non-compute-gate clause; the boundary is the structural vanishing + partition-independence, not a fitted threshold)"
  direction: "<"

# (3) boundary_reachable_analytically
boundary_reachable_analytically:
  bool: true
  proof_ref: "the additive-constant linear-response argument (ε_Λ=a₀ additive in F(q) ⇒ ∂F/∂ε_Λ=1 const ⇒ annihilated by BOTH the response-derivative and the perturbation δ) + Volovik equilibrium theorem (Paper 05, ρ_Λ=0 at equilibrium, lab-grounded 3He-B µ-cancellation); the §W7-2 substitution chain"

# (4) reachable_rationals
reachable_rationals:
  includes_integer_mesh: true              # ∂w/∂a₀ = 0 EXACTLY (Sage QQ), the integer 0
  mesh_density: "exact-zero (Sage QQ symbolic)"

# (5) machinery_pin_map
machinery_pin_map:
  N_eval: "5  (partition-family Γ(λ;θ) members, θ deformation around Γ_eff=0.99970)"
  L_max: "N/A — closed-form q-theory derivation; a₀ enters symbolically as the additive zeroth moment, NOT an L_max-truncated cache value"
  scan_range: "[Γ_eff−δ, Γ_eff+δ] for the partition-family parameter θ; δ a structural deformation (e.g. ±0.1 around 0.99970)"
  step_size: "δθ adaptive over the 5 family members"
  tolerance: "1e-12 (machine-zero, Sage QQ)"
  scheme: "q-theory-linear-response + Volovik-equilibrium-theorem (Paper 05); a₀ via the n=0 Seeley-DeWitt term of Tr f(D_K/Λ)"
  convention: "static-a₀-MAGNITUDE vs dynamical-EoS-RESPONSE functional-type separation"
  random_seed: "N/A — deterministic (Sage symbolic)"
  GPU_path: "N/A — Sage-symbolic (mcp__sage__sage_eval / sage_simplify)"
  # CONDITIONAL machinery pins (MANDATORY here):
  regulator_pin: "a_0^{ζ}"                 # the static CC magnitude IS the zeta-regulated zeroth Seeley-DeWitt moment (regulator-pin-discipline.md MANDATORY)
  # substrate / canonical pins:
  w0_FW: "-0.918  (canonical, S58, Gate:None)"
  Gamma_eff: "0.99970  (the impedance-effacement leakage; the model-contingent VALUE = wall (ii-b))"
  Delta_BCS: "0.4642547394830737  (R-PROTECTED, S70; the substrate gap scale, framing-only)"

# (6) audit_discriminators
audit_discriminators:
  audit_sha256_inputs: ["script", "canonical", "pinmap"]
  content_sha256_inputs: ["script"]

# (7) substitution_chain
substitution_chain:
  required: true
  content: |
    Claim: ∂(EoS response w(z))/∂(static a₀ moment) = 0, forced by the additive-magnitude /
           derivative-response functional-type separation, independent of the specific partition.

    Step 1: Static CC magnitude ε_Λ = the a₀^{ζ} term = the zeroth Seeley-DeWitt moment of
            Tr f(D_K/Λ) at n=0, ε_Λ ∝ Λ^d f_0 a_0^{ζ}; it enters the q-field free energy as an
            ADDITIVE constant: F(q) = F_dyn(q) + ε_Λ. [Tr f(D_K/Λ) n=0 term, regulator-tagged a_0^{ζ};
            DILUTION-CC a₀/a₂ two-fluid separation, Paper 37 / S43.]
    Step 2: Dynamical EoS response w(z) = p/ρ along the q-trajectory = ratio of FIRST derivatives of
            F w.r.t. the q-field 4-form variable: p = −F, ρ = q F' − F (Volovik q-theory thermodynamic
            identities, Papers 13/23/25) ⇒ w = p/ρ = −F / (q F' − F).
    Step 3: Substitute F = F_dyn + ε_Λ ⇒ F' = F_dyn' (the additive constant has ZERO q-derivative).
            So w = −(F_dyn + ε_Λ) / (q F_dyn' − F_dyn − ε_Λ).
    Step 4: At equilibrium, Volovik's equilibrium theorem forces ρ_vac(q_eq) = q F'(q_eq) − F(q_eq) = 0
            with the µ-cancellation absorbing ε_Λ (the ground-state energy does not gravitate;
            ρ_Λ → 0 at equilibrium, Paper 05, lab-grounded in the 3He-B µ-cancellation). The DYNAMICAL
            response is taken about q_eq: w(z) = δp/δρ along the perturbation δq.
    Step 5: Cross-derivative ∂w/∂a₀ = ∂w/∂ε_Λ. Since ε_Λ enters F additively, ∂F/∂ε_Λ = 1 (a constant):
            ∂(δp)/∂ε_Λ = ∂(−δF)/∂ε_Λ = −δ(∂F/∂ε_Λ) = −δ(1) = 0 ; and
            ∂(δρ)/∂ε_Λ = δ(∂(qF'−F)/∂ε_Λ) = δ(q·0 − 1) = 0 (the perturbation δ of a constant is 0).
            Hence ∂w/∂ε_Λ = ∂w/∂a₀ = 0. [The additive a₀ is annihilated by BOTH the response-derivative
            AND the perturbation — the q-field LINEAR response-derivative, NOT the W-4 K-log-derivative.]
    Step 6: Partition-independence: Step 5 used ONLY that ε_Λ (the a₀ static magnitude) is ADDITIVE in F
            and that w is a derivative-RESPONSE; it did NOT use the specific Γ_eff=0.99970 value or the
            specific F_dyn form ⇒ ∂w/∂a₀ = 0 for ALL members of the partition family Γ(λ;θ). [The
            partition-family scan confirms the symbolic vanishing is Γ-INDEPENDENT.]
    Conclusion: the EoS response is structurally BLIND to the static a₀ magnitude (the categorical wall
            (ii-a)); the vanishing is FORCED by the additive-magnitude / derivative-response functional-type
            separation, partition-INDEPENDENT ⇒ (ii-a) is THEOREM-grade for the PLACEMENT. The residual
            model-contingency is the VALUE Γ_eff (ii-b), which sets the response MAGNITUDE, not its
            categorical placement — exactly what DESI tests. ∎  (FAIL branch: if ∂w/∂a₀ ≠ 0 for some
            partition member, the vanishing was partition-CONTINGENT ⇒ (ii-a) stays model-grade.)

# (8) input_files
input_files:
  canonical_constants:
    path: "computations/_shared/canonical_constants.py"
    sha256: "<computed-at-runtime>"
  # No npz inputs: this is a closed-form q-theory structural derivation. Volovik Papers 05/13/23/25/37
  # are CONCEPTUAL references (the equilibrium theorem, the 4-form linear-response structure, the
  # DILUTION-CC a₀/a₂ split), NOT numerical input files. The a₀/a₂ moment separation and Γ_eff enter
  # symbolically from canonical_constants + the q-theory identities.

# ---- Conditional blocks ----
fb_pair:
  forward: "Volovik equilibrium theorem (Paper 05, ρ_Λ=0 at equilibrium); DILUTION-CC a₀/a₂ two-fluid w=−1/w=0 split (Paper 37 / S43); w0_FW (S58); the W-5 (i)∧(ii) conjunction + VD2b sub-grade split"
  backward: "the §5 EVOI register (the 7c categorical-wall disposition — whether wall (ii) is one-grade or two-grade); the DESI DR3 falsifier-surface robustness (a theorem-grade (ii-a) HARDENS the CLEAN stake — the categorical closure becomes as permanent as the Weyl wall (i)); the permanent-results register categorical-wall entry"
dual_prior:
  track_A: "Track-THEOREM, prior 0.6 — the static-magnitude/dynamical-response functional-type separation forces ∂w/∂a₀=0 PARTITION-INDEPENDENTLY ⇒ (ii-a) THEOREM-grade (categorical closure as permanent/regulator-invariant as the Weyl wall (i))"
  track_B: "Track-MODEL, prior 0.4 — the vanishing requires the SPECIFIC Volovik partition (q-theory-program-contingent, baptista's R1 flat reading) ⇒ (ii-a) stays model-grade"
  discriminator: "PASS (∂w/∂a₀=0 for ALL partition members) → 0.9 to track_A ; FAIL (∂w/∂a₀≠0 for some member) → 0.9 to track_B ; INFO (the VD2b two-grade reading: placement (ii-a) theorem-grade, value (ii-b) Γ_eff model-contingent) → wall (ii) carries TWO grades, not one"

# ---- Output artifacts ----
output_artifacts:
  script:
    path: "computations/session-117/s117_w0_categorical_wall_grade.py"
    artifact_kind: "script"
    must_contain:
      - "from canonical_constants import"
      - "print_verdict_payload"
  data:
    path: "computations/session-117/s117_w0_categorical_wall_grade.npz"
    artifact_kind: "data"
    optional: false                       # ∂w/∂a₀ over the N≥5 partition-family members + the symbolic-zero certificate
  plot:
    path: "computations/session-117/s117_w0_categorical_wall_grade.png"
    artifact_kind: "plot"
    optional: true                        # a symbolic-derivation gate; the partition-family ∂w/∂a₀≡0 scan is the only plottable, optional
  verdict_line:
    path: "computations/session-117/s117_gate_verdicts.txt"
    artifact_kind: "verdict_line"
    must_contain: "^CF-S117-W0-CATEGORICAL-WALL-GRADE:.* audit_sha256=[a-f0-9]{64}"
    companion_row_required: true
    schema_v2_3tuple_required: false      # [VERIFY-THEOREM] trigger, not [SIGN]
  wp_section:
    path: "sessions/session-117/session-117-w7-workingpaper.md"
    artifact_kind: "wp_section"
    section_anchor: "### §W7-2. CF-S117-W0-CATEGORICAL-WALL-GRADE"
    must_contain:
      - "\\*\\*Status\\*\\*:.*COMPLETED"
      - "\\*\\*Verdict\\*\\*:.*(PASS|FAIL|INFO)"
      - "\\*\\*Output Artifacts\\*\\*"
      - "\\*\\*MCP Pre-Compute Audit\\*\\*"

# ---- Verdict rubric ----
PASS_meaning: "(ii-a) upgrades to THEOREM-grade. The static-a₀ / dynamical-EoS-response functional-type separation forces ∂(EoS response)/∂(a₀) = 0 independent of the specific Volovik partition; the categorical closure of the non-edge-normalized spectral routes is as permanent/regulator-invariant as the Weyl wall (i). The branch-iv CLOSED-WITH-RESULT disposition strengthens (BOTH walls theorem-grade); the DESI CLEAN stake hardens (the −0.918 closed-form value's elimination column is theorem-grade on both legs)."
FAIL_meaning: "(ii-a) stays flatly MODEL-grade. The vanishing requires the SPECIFIC Volovik partition (q-theory-program-contingent); a challenge to the q-theory program weakens (ii-a) and the −0.918 value's spectral-route closure together; wall (ii) remains the single q-theory-grade leg of the (i)∧(ii) conjunction, as baptista's R1 flat reading had it."
INFO_meaning: "PARTIAL separation — the VD2b two-grade reading made precise (the expected landing given the R3 DISSENT). The categorical PLACEMENT (ii-a) [the EoS response is the q-partition's effacement, not a static spectral moment] is THEOREM-grade (lab-grounded in Volovik's 3He-B equilibrium theorem, partition-independent), but the specific VALUE (ii-b, Γ_eff=0.99970) is model-contingent (exactly what DESI tests). Wall (ii) carries TWO grades, not one; the §5 register MUST record (ii) as a two-grade wall."

# ---- Effort + framing ----
effort:
  files_created: ["computations/session-117/s117_w0_categorical_wall_grade.py", "computations/session-117/s117_w0_categorical_wall_grade.npz"]
  estimated_time: "medium (~3-4 hours, 1 agent — substrate-physics structural derivation + Sage-symbolic ∂w/∂a₀ + the N≥5 partition-family partition-independence scan + the equilibrium-theorem cross-check)"

substrate_framing: |
  PHONONIC (the EoS response side) ⊕ GEOMETRIC (the static a₀ side). The fabric IS both its D_K
  vibrational spectrum AND its q-field vacuum partition — two FACETS of one substrate. The static CC
  MAGNITUDE is the a₀^{ζ} zeroth spectral moment of D_K (GEOMETRIC, the eigenvalue-spread's vacuum
  energy density). The dynamical w(z) RESPONSE is how the q-field partition EFFACES under perturbation
  (PHONONIC, the DILUTION-CC two-fluid w=−1[vacuum] + w=0[GGE] effacement leakage Γ_eff). The gate
  tests whether these are STRUCTURALLY ORTHOGONAL: a static additive magnitude (a₀) has no
  linear-response channel to a dynamical derivative-response (w(z)). Direction of explanation:
  D_K eigenvalues → a₀ static moment (a NUMBER, the vacuum energy magnitude) is ADDITIVE in the
  q-field free energy F(q); the q-field 4-form variable's perturbation-response → w(z) (a RATIO of
  FIRST derivatives of F). An additive constant is annihilated by a derivative-response ⇒ the EoS is
  blind to the static magnitude. Container-thinking FORBIDDEN: w(z) is NOT "dark energy quintessence
  evolving in an FRW background" — it IS the substrate's q-field vacuum partition effacing, and the
  detector measures the emergent equation-of-state image of that effacement. The −1 the branch-iv
  moment converges to was never a failed spectral derivation — it was the substrate REFUSING the
  relocation of a thermodynamic observable into the vibrational spectrum.
```

---

## §W7-3. CF-S117-BRANCH-IV-L16  [OPTIONAL — value-neutral L_max diagnostic]

```yaml
# ---- Identity ----
gate_id: "CF-S117-BRANCH-IV-L16"
schema_version: "R3"
gate_type: "compute"
trigger: "[SIGN]"                          # the decrement-deceleration sign (|d(15→16)| < |d(14→15)|); INFO-by-construction composite (corridor CLOSED-WITH-RESULT)
classification: "GEOMETRIC"                # the branch-iv Zubarev moment ρ_B is a spectral moment of D_K at τ_fold; the L16 FB-bounded shell build is fabric/spectral-triple structure
agent_type: "baptista-spacetime-analyst"   # the p+q=16 FB-bounded shell build (GT-pure + Casimir-projection of 15 mixed sectors) is KK-geometry / irrep construction
hypothesis: "Extending the branch-(iv) CAC spread to the sliding window {14,15,16} by building the p+q=16 FB-bounded shell continues the |d| ~ 1/λ_max² decrement-deceleration (|d(15→16)| < |d(14→15)| = 0.018456) and the sliding-window spread keeps narrowing (spread_CAC{14,15,16} < spread_CAC{13,14,15} = 0.0392902) — a VALUE-NEUTRAL L_max diagnostic certifying ρ_B is a smooth convergent sequence → −1, which does NOT reopen the CLOSED-WITH-RESULT branch-iv corridor."

method:
  description: >
    Build the p+q=16 FB-bounded shell (17 sectors (0,16)…(16,0)): GT-pure (16,0)/(0,16) builder +
    Casimir-projection for the 15 mixed sectors, extending the s105/s106 high-L cache to L=16.
    Compute the branch-iv Zubarev moment ρ_B(16) = mean_Z(16)/λ_max(16) − 1 (Zubarev occupation
    weight w_Z(λ)=exp(−λ²/Λ_Z²), Λ_Z=1) and the CAC-anchored w0^CAC(16) = ρ_B(16) + offset (offset
    DERIVED at L=10, matching the W9 (4,4)-filled lineage offset_B=−0.342793 for trajectory
    continuity; lockdown lineage −0.340827 as cross-check). Read off spread_CAC{14,15,16} = max−min
    over the sliding window and the decrement d(15→16) = ρ_B(16) − ρ_B(15). Compare to the W9
    trajectory (spread{13,14,15}=0.0392902; d(14→15)=−0.018456; d(13→14)=−0.020834, DECEL confirmed).
    Report as a VALUE-NEUTRAL diagnostic per W-5: the spread is offset-INVARIANT (spread_CAC = ρ_a − ρ_b,
    bit-identical for any offset target), so it certifies ONLY that ρ_B → −1 smoothly; it is NOT support
    for −0.918. CAC discipline per regulator-convention-lockdown.md (the RDC, no offset, is INADMISSIBLE).
  producing_script: "computations/session-117/s117_branch_iv_l16.py"

# ---- PRDR Checklist (8 items) ----

# (1) operator
operator:
  type: "span + sign (decrement-deceleration); INFO-by-construction diagnostic"
  form: "spread_CAC{14,15,16} = max−min over {ρ_B(14),ρ_B(15),ρ_B(16)} ; sign of (|d(14→15)| − |d(15→16)|) > 0 (deceleration continues)"

# (2) strict_PASS_boundary
strict_PASS_boundary:
  value: "N/A — value-neutral L_max diagnostic (the corridor is CLOSED-WITH-RESULT per W-5; the spread is offset-invariant ⇒ certifies smoothness/convergence-to-−1, NOT support for −0.918). No PASS/FAIL corridor threshold."
  direction: "N/A — INFO-by-construction diagnostic (the [SIGN] sub-tuple carries the decrement-deceleration direction; the composite is INFO regardless)"

# (3) boundary_reachable_analytically
boundary_reachable_analytically:
  bool: true
  proof_ref: "the |d| = μb/λ_max² ~ 1/L² deceleration law (μ=mean_Z≈1.9879 FB-saturated/frozen, b=∂λ_max/∂L≈0.375; Sage-ratio 1.00081, W9 saturation workshop); the L16 point tests whether the empirical decrement matches the analytic 1/λ_max² law"

# (4) reachable_rationals
reachable_rationals:
  includes_integer_mesh: false             # the spread is a real spectral-moment span
  mesh_density: "continuous (Zubarev moment on the L=16 D_K spectrum)"

# (5) machinery_pin_map
machinery_pin_map:
  N_eval: "17  (sectors (0,16)…(16,0) at p+q=16)"
  L_max: "16"
  scan_range: "sliding window {14,15,16}"
  step_size: "1"
  tolerance: "Casimir-projection convergence ~1e-10 ; band reference spread_CAC ≤ 0.025 (W9 PASS band, for trajectory context only — NOT a corridor gate here)"
  scheme: "zeta  (matching the S116-W9-GTBUILDER-L15 line; the Zubarev occupation weight w_Z is the moment construction on top of the zeta-regulated spectral moments)"
  convention: "CAC-branch-iv-anchored-L10-DERIVED-OFFSET  (matching W9; offset_B=−0.342793 W9 (4,4)-filled lineage; lockdown −0.340827 cross-check; RDC INADMISSIBLE per regulator-convention-lockdown.md)"
  random_seed: "N/A — deterministic"
  GPU_path: "torch.linalg  (the p+q=16 FB-bounded shell: GT-pure (16,0)/(0,16) + Casimir-projection of 15 mixed sectors; AMD RX 9070 XT — the largest single (p,q) block fits dense in 17.1 GB VRAM per math-scripts.md §D_K-Block-Diagonality; cost is irrep CONSTRUCTION, a SINGLE shell build, not a full re-diagonalization)"
  # substrate / canonical pins:
  w0_FW: "-0.918  (canonical, S58)"
  tau_fold: "0.19  (canonical, S12/S42)"
  Lambda_Z: "1.0  (Zubarev Gaussian regulator scale; S85 W0-7)"
  rho_B_10: "−0.575207  (W9 (4,4)-filled lineage, for trajectory continuity with spread{13,14,15})"
  offset: "DERIVED in-script = w0_FW − rho_B(10) = −0.342793 (W9 lineage)  (CAC-DERIVED-OFFSET; NOT a hardcoded literal)"

# (6) audit_discriminators
audit_discriminators:
  audit_sha256_inputs: ["script", "canonical", "pinmap", "upstream_npz"]
  content_sha256_inputs: ["script"]

# (7) substitution_chain
substitution_chain:
  required: true
  content: |
    Claim: |d(15→16)| < |d(14→15)| = 0.018456 (the per-shell decrement decelerates as 1/λ_max²).

    Step 1: d(L→L+1) = ρ_B(L+1) − ρ_B(L), the per-shell decrement of the Zubarev moment.
            [W9 GTBUILDER: d(13→14) = −0.020834, d(14→15) = −0.018456.]
    Step 2: ρ_B(L) = mean_Z(L)/λ_max(L) − 1, with mean_Z FB-saturated (frozen, μ ≈ 1.9879) and
            λ_max(L) Weyl-linear (∂λ_max/∂L = b ≈ 0.375). [W9 saturation workshop; S-7 synthesis §II.3.]
    Step 3: d(L→L+1) = μ/λ_max(L+1) − μ/λ_max(L) ≈ −μ·b/λ_max(L)² (first-order, μ frozen).
            [Substitute Step 2; mean_Z constant ⇒ the decrement is the λ_max difference.]
    Step 4: |d| = μb/λ_max² ~ 1/L² (Weyl N(λ)~λ^d ⇒ λ_max ~ L). [The decrement decelerates as λ_max
            grows; Sage-ratio 1.00081 confirms the 1/λ_max² law at W9.]
    Step 5: λ_max(16) > λ_max(15) ⇒ |d(15→16)| = μb/λ_max(16)² < μb/λ_max(15)² = |d(14→15)|.
            [Monotone λ_max ⇒ monotone-decreasing |d|.]
    Conclusion: the decrement decelerates (|d(15→16)| < 0.018456) and the sliding-window spread narrows.
            BUT per W-5 this convergence is TO −1 (the bare-moment limit), value-NEUTRAL (offset-invariant),
            and does NOT reopen the CLOSED-WITH-RESULT corridor — it certifies smoothness, never support
            for −0.918. ∎

# (8) input_files
input_files:
  canonical_constants:
    path: "computations/_shared/canonical_constants.py"
    sha256: "<computed-at-runtime>"
  highl_cache:
    path: "computations/session-106/s106_w1_highl_cache_l1416.npz"
    sha256: "<computed-at-runtime>"     # high-L spectrum {13,14,15} + the (16) shell partials
  branch_iv_direct_builder:
    path: "computations/session-105/s105_branch_iv_direct_l1314.py"
    sha256: "<computed-at-runtime>"     # GT builder + Zubarev moment ρ_B; reused for the L=16 shell
  master_spectrum_cache:
    path: "computations/session-84/s84_spectrum_cache_L12_tau019.npz"
    sha256: "<computed-at-runtime>"     # τ_fold=0.190 baseline spectrum + λ_max/mean_Z convention

# ---- Conditional blocks ----
fb_pair:
  forward: "S116-W9-GTBUILDER-L15 (the {13,14,15} trajectory + offset_B + the d-decel sentinel); the s105/s106 high-L caches; the |d| ~ 1/λ_max² deceleration law"
  backward: "presentation-normalization hygiene (the non-drifting off-anchor 'costume' — informational, NOT a derivation); CF-S117-W0-TRANSPORT-DEGREE (7-1 consumes the L=16 fixed-scale trajectory point as an additional, non-blocking refinement input)"
# composite-precedence (pre-declared; disclosed via a `# composite-precedence:` companion row naming the W-5 closure):
#   This gate is INFO-by-construction. Per the W-5 R3-FINAL closure, the branch-iv corridor is
#   CLOSED-WITH-RESULT and is NOT reopenable by an L_max diagnostic (the spread is offset-invariant /
#   value-neutral). The [SIGN] 3-tuple carries the decrement-deceleration direction, but the COMPOSITE
#   verdict is INFO REGARDLESS of the spread/decrement behavior — a surprise (spread widens, decrement
#   accelerates) is still INFO (an interesting convergence diagnostic), never a corridor PASS/FAIL.
#   Plan anchor for the precedence companion row: session-117-plan-w7.md §W7-3 + workshops/s116-w0-spectral-derivability.md (W-5 closure).

# ---- Output artifacts ----
output_artifacts:
  script:
    path: "computations/session-117/s117_branch_iv_l16.py"
    artifact_kind: "script"
    must_contain:
      - "from canonical_constants import"
      - "print_verdict_payload"
  data:
    path: "computations/session-117/s117_branch_iv_l16.npz"
    artifact_kind: "data"
    optional: false                       # the p+q=16 shell spectrum + ρ_B(16) + spread_CAC{14,15,16} + d(15→16)
  plot:
    path: "computations/session-117/s117_branch_iv_l16.png"
    artifact_kind: "plot"
    optional: false                       # the ρ_B(L) trajectory through L=16 + the |d|~1/λ_max² decel fit
  verdict_line:
    path: "computations/session-117/s117_gate_verdicts.txt"
    artifact_kind: "verdict_line"
    must_contain: "^CF-S117-BRANCH-IV-L16:.* audit_sha256=[a-f0-9]{64}"
    companion_row_required: true
    schema_v2_3tuple_required: true       # [SIGN] trigger (decrement-deceleration); composite forced INFO by the precedence row
  wp_section:
    path: "sessions/session-117/session-117-w7-workingpaper.md"
    artifact_kind: "wp_section"
    section_anchor: "### §W7-3. CF-S117-BRANCH-IV-L16"
    must_contain:
      - "\\*\\*Status\\*\\*:.*COMPLETED"
      - "\\*\\*Verdict\\*\\*:.*(PASS|FAIL|INFO)"
      - "\\*\\*Output Artifacts\\*\\*"
      - "\\*\\*MCP Pre-Compute Audit\\*\\*"

# ---- Verdict rubric ----
PASS_meaning: "N/A as a corridor verdict — this gate CANNOT PASS-reopen the corridor (CLOSED-WITH-RESULT per W-5). The nominal 'confirming' outcome (the 1/λ_max² deceleration continues, |d(15→16)| < 0.018456, the sliding-window spread narrows) is reported as INFO, NOT a corridor-reopening PASS."
FAIL_meaning: "N/A as a corridor verdict — this gate CANNOT FAIL-close the corridor (already CLOSED-WITH-RESULT). A surprise (the spread WIDENS, or the decrement ACCELERATES against the 1/λ_max² law) is still INFO — an interesting diagnostic about the moment's convergence, not a corridor verdict. (A genuine FAIL is reserved for SCRIPT BREAKAGE only: the p+q=16 shell build does not converge / Casimir-projection fails / the GT builder errors.)"
INFO_meaning: "The value-neutral L_max diagnostic delivered: spread_CAC{14,15,16} + the decrement-deceleration sign. Certifies ρ_B is a smooth convergent sequence → −1 (the bare-moment limit; NOT support for −0.918, which lives entirely in the CAC offset). The corridor disposition (CLOSED-WITH-RESULT, W-5) is UNCHANGED. This is the ONLY corridor-relevant verdict token this gate emits; its forward status is presentation-normalization hygiene (select a non-drifting off-anchor costume so a downstream consumer is not shown a proxy drifting to −1.341)."

# ---- Effort + framing ----
effort:
  files_created: ["computations/session-117/s117_branch_iv_l16.py", "computations/session-117/s117_branch_iv_l16.npz", "computations/session-117/s117_branch_iv_l16.png"]
  estimated_time: "medium-high (~1.5-2 hours GPU, 1 agent — ONE FB-bounded shell build at p+q=16: GT-pure (16,0)/(0,16) + Casimir-projection of the 15 mixed sectors, then the Zubarev moment + CAC spread on the extended trajectory). OPTIONAL — descopable without blocking 7-1."

substrate_framing: |
  GEOMETRIC, Level-1 single-τ-slice substrate-IS (τ_fold = 0.190). The branch-iv Zubarev moment
  ρ_B(L) = mean_Z(L)/λ_max(L) − 1 IS a spectral moment of D_K at τ_fold — the substrate's own
  vibrational spectrum read through the Zubarev occupation weight, NOT a field "in" a container.
  Direction of explanation: D_K eigenvalues at τ_fold → the FB-bounded p+q=16 shell → the Zubarev
  moment ρ_B(16) → the CAC-anchored w0^CAC(16). The L16 point extends the substrate's own
  L_max-convergence ENVELOPE. Container-thinking FORBIDDEN: λ_max(L) is the RUNNING truncation EDGE
  (the sup-norm of the retained spectrum), a non-substrate quantity with no continuum limit; the
  moment's convergence to −1 is the substrate telling us w0 does NOT live in the eigenvalue spread
  (the W-5 categorical wall (ii)). This gate does NOT search for a hidden spectral derivation of
  −0.918 — it certifies that the moment converges smoothly to its OWN limit (−1), a value-neutral
  presentation-hygiene fact, leaving the closed-form thermodynamic value −0.918 (the q-field
  partition's effacement) exactly where W-5 placed it.
```

---

## Wave 7 → Wave 8 Decision Point

Wave 8 (`§VII.AJ STATE-PROJ inter-summand`, landau-condensed-matter-theorist) does NOT consume any Wave 7 output — the two waves are on orthogonal observables (w0 EoS transport/categorical-grade vs the ℍ/M₃(ℂ) inter-summand state-pair asymmetry). Wave 7 → Wave 8 is sequential-by-schedule only, not data-coupled.

Wave 7's downstream consumers are OUTSIDE this session's wave chain:

- **PASS-SCALAR on 7-1** (deg=0 confirmed; favored, prior 0.7) → the W-5 verdict's ONE deg-CONDITIONAL element resolves: the DR3 σ-distances (2.13σ vs DESI DR2 −0.803±0.054; 3.28σ vs LCDM −1; R_842=[−0.942,−0.742] containment) freeze vs −0.918 DIRECTLY; mack removes the "computed under provisional deg=0; pending CF-S117-W0-TRANSPORT-DEGREE" tag from the §7 falsifier-surface rows. NO §23 K-counter advance. → route to mack (sole writer of the §7 surface + `falsifier-master-inventory.md`).
- **INFO-(PASS-NONSCALAR) on 7-1** (clean even nonzero deg; prior 0.3) → w0 flips to a substrate-natural NON-SCALAR §23 instance, candidate K=3 alongside reserved r/α_t; the σ-distances re-compute vs the pivot image T(−0.918) (substrate value −0.918 UNCHANGED). → route to the §23 K-counter maintainer + a Stage-1-Candidate §VII registration of the w0 NON-SCALAR degree.
- **PASS on 7-2** (theorem-grade (ii-a)) → the §5 EVOI register records wall (ii) as theorem-grade (both legs of the (i)∧(ii) conjunction permanent); the DESI CLEAN stake hardens. **INFO on 7-2** (the expected VD2b two-grade landing) → the §5 register records wall (ii) as a TWO-grade wall [(ii-a) theorem-grade placement / (ii-b) Γ_eff model-contingent value]. → route to the EVOI-maintainer (orchestrator) + the permanent-results register categorical-wall entry.
- **7-3** (OPTIONAL, INFO-by-construction) → presentation-normalization hygiene only; no corridor-status consequence; feeds 7-1 a non-blocking L=16 refinement point.

If 7-1 lands FAIL (ILL-POSED extraction — the fixed-scale representative does not de-λ_max cleanly), the deg-resolution stays OPEN: the substrate value −0.918 is UNCHANGED (it is the transport INPUT), the W-5 BD2 deg-conditional σ-tag persists, and a normalization-redesign carry-forward is minted for S118.

---

## Wave 7 Machinery-Enumeration Pin

Aggregate of all gate `machinery_pin_map` entries (per `.claude/rules/epistemic-discipline.md §"Pre-Registration Completeness"` PRDR; read by `_yaml_gate_validator.py` for sig_4):

| Gate | N_eval | L_max | scan_range | step | tolerance | scheme | convention | seed | GPU |
|:-----|:-------|:------|:-----------|:-----|:----------|:-------|:-----------|:-----|:----|
| 7-1 CF-S117-W0-TRANSPORT-DEGREE | 6 (L∈{10..15}, +1 if 7-3) | 15 (16 if 7-3) | [10,15] | 1 | deg_tol=0.05; spread_tol=1e-3 M_KK² | §23-Wodzicki + {APS,CS,BC} | fixed-scale-de-λmax + CAC-DERIVED-OFFSET | N/A det. | numpy.linalg |
| 7-2 CF-S117-W0-CATEGORICAL-WALL-GRADE | 5 (partition-family) | N/A (closed-form) | [Γ_eff−δ, Γ_eff+δ] | adaptive | 1e-12 (Sage QQ) | q-theory-linear-response + Volovik-eq-thm | static-a₀-mag vs dyn-EoS-response | N/A det. | N/A (Sage) |
| 7-3 CF-S117-BRANCH-IV-L16 | 17 (p+q=16 sectors) | 16 | window {14,15,16} | 1 | Casimir ~1e-10; band ref ≤0.025 | zeta (+Zubarev w_Z) | CAC-branch-iv-anchored-L10-DERIVED-OFFSET | N/A det. | torch.linalg |

**Conditional machinery pins (declared per `.claude/rules/regulator-pin-discipline.md` + `.claude/rules/substrate-first-canonical-sourcing.md`):**

- **7-1**: `offset` is DERIVED in-script (`= w0_FW − ρ_B(10)`, zero free parameter — the CAC-DERIVED-OFFSET per `regulator-convention-lockdown.md`), NOT a hardcoded literal; the `ρ_B(10)` lineage is pinned (lockdown −0.577173 / W9 (4,4)-filled −0.575207; ±0.001966 sensitivity per W-5 B5.4). Substrate pins `w0_FW`, `deg_T_BZ_pivot`, `alpha_s_substrate_distance_1` imported from `canonical_constants`.
- **7-2**: `regulator_pin: a_0^{ζ}` MANDATORY — the static CC magnitude IS the zeta-regulated zeroth Seeley-DeWitt moment (`regulator-pin-discipline.md`). `convention` encodes the static-magnitude/dynamical-response separation. The OPERATOR-DISTINCTNESS guard (q-field response-derivative ANNIHILATES an additive a₀; the W-4 K-log-derivative SURVIVES an additive-in-trace term — `math-scripts.md §"Scope boundary"`) is a script-level invariant, NOT a free parameter.
- **7-3**: `offset` DERIVED (`= w0_FW − ρ_B(10) = −0.342793`, W9 lineage); CAC discipline (RDC inadmissible per `regulator-convention-lockdown.md`); the `# composite-precedence:` companion row (INFO-by-construction, corridor CLOSED-WITH-RESULT) is MANDATORY at emission per `gate-verdicts.md §"plan-frozen gate-block operator precedence"`.

**SCHEMATIC-helper check**: none of the three producing scripts consumes a SCHEMATIC helper (no `_spectral_action_regulators.py` import); CLASS pin = FULL by construction. Gate 7-2 is Sage-symbolic (FULL closed-form q-theory derivation, NOT a SCHEMATIC analog).

---

## Wave 7 Input-SHA Ledger

Every input file the wave's gates consume, with SHA-256 per `.claude/rules/gate-verdicts.md`. Static caches/scripts pinned `<computed-at-runtime>` (large data files; verified present at plan-freeze — see below); cross-checked at plan-freeze by `computations/_shared/_plan_upstream_pin_validator.py`.

| Input file | Consumed by | SHA-256 | Plan-freeze existence |
|:-----------|:------------|:--------|:----------------------|
| `computations/_shared/canonical_constants.py` | 7-1, 7-2, 7-3 | `<computed-at-runtime>` | EXISTS (308688 bytes) |
| `computations/session-102/s102_branch_iv_canonical_eval.py` | 7-1 | `<computed-at-runtime>` | EXISTS (31281 bytes) |
| `computations/session-84/s84_spectrum_cache_L12_tau019.npz` | 7-1, 7-3 | `<computed-at-runtime>` | EXISTS (1340660 bytes) |
| `computations/session-106/s106_w1_highl_cache_l1416.npz` | 7-1, 7-3 | `<computed-at-runtime>` | EXISTS (1365984 bytes) |
| `computations/session-105/s105_branch_iv_direct_l1314.py` | 7-3 | `<computed-at-runtime>` | EXISTS (43512 bytes) |

**Upstream verdict consumed (not an input file; cited for the trajectory anchor):**

- `S116-W9-GTBUILDER-L15` (INFO; `audit_sha256=94ed48e7c84c9ce1bb87435d0734041ee965b2a7cb24eb656f1db4b5d3315686`; `computations/session-116/s116_gate_verdicts.txt:77`) — supplies `spread_CAC{13,14,15}=0.0392902`, `ρ_B(13/14/15)=−0.656884/−0.677718/−0.696174`, `w0CAC(15)=−1.038968`, `offset_B=−0.342793`, `d(14→15)=−0.018456 < d(13→14)=−0.020834 DECEL`. Consumed by 7-1 (the {13,14,15} trajectory) and 7-3 (the spread/decrement baseline + offset lineage).

**Canonical constants consumed** (via `from canonical_constants import *`, provenance-verified at plan-freeze through the knowledge MCP):

- `w0_FW = −0.918` (S58 four-fold-lock, Gate:None, Superseded:False) — 7-1, 7-2, 7-3
- `deg_T_BZ_pivot = 2.0` (S110-CF-CV6B-DS-M4) — 7-1 (the NON-SCALAR reference degree)
- `alpha_s_substrate_distance_1 = −0.08587279` (S92-AH-TR-1) — 7-1 (the deg=+2 NON-SCALAR sibling contrast)
- `Delta_BCS = 0.4642547394830737` (S70, R-PROTECTED) — 7-2 (the substrate gap scale, framing-only)
- `tau_fold = 0.19` (S12/S42, CONST-FREEZE-42) — 7-3

`Λ_Z = 1.0` (Zubarev Gaussian regulator scale, S85 W0-7) and `Γ_eff = 0.99970` (the impedance-effacement leakage / wall (ii-b) value) are machinery constants pinned in the gate blocks; the CAC `offset` is DERIVED in-script (`= w0_FW − ρ_B(10)`), never hardcoded.
