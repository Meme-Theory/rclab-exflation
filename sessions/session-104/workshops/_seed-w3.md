# Seed — Chunk w3 (Wave 3: Spectral-functional diagnostics)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: session-104-w3-workingpaper.md + session-104-plan-w3.md
**Wave summary**: 2/2 gates landed, both **INFO** (0 PASS / 0 FAIL / 2 INFO), dual-SHA + `[SIGN]` 3-tuple, sig_5-unique. Both are NEW spectral-functional readings of the ALREADY-classified `D_K` fold eigenvalue content (spectral-functional pluralism), and both returned readings CONSISTENT with standing structural walls. W3-1 (Krylov KCP) confirms the load-bearing SIGN (`sign=PASS`: KCP(fold)=6.0916 > KCP(τ=0 ref)=4.9061, matching sign(β_fold−β_ref)=+1) — KCP joins ⟨r⟩/OTOC/SFF as the 4th internally-consistent chaos functional; INFO is purely the magnitude band (rel_residual 0.2288 > 0.15 on the qualitative-monotone Huh relation). W3-2 (log-periodic Im(s)) finds NO regulator-robust log-periodic line in the heat-trace residual (`Im(s)=0` detectable); the PROVEN CM-1995 simple-real dimension-spectrum wall is uncontradicted on the frequency axis. INFO (not FAIL) because the pre-registered cross-axis-instability clause fired (scheme-dependent sidelobes; γ/d-vs-SDW peak-bin spread = 28 ≫ ±1) at the S61 residual's limited dynamic range.

## Slot 1 candidates — solo reviews (`/rclab-review`)

(none)

## Slot 2 candidates — workshops (`/rclab-workshop`)

(none)

## No candidates

Wave 3 produced ZERO workshop candidates and ZERO solo-review candidates. The honest reading of the four-condition workshop definition and the 3-question discriminator finds no genuine ledger-dissonance in this wave's substance.

- **W3-1 (KCP)**: The plan's Q1-workshop seed was CONDITIONAL on a SIGN MISMATCH (plan `discriminator`: `FAIL (sign mismatch) → 0.9 to Track B → Q1 workshop seed`). The sign MATCHED (`sign_verdict=PASS`). The INFO is purely a magnitude statement on a relation the plan itself pre-registers as qualitative-monotone, not a calibrated height law — explicitly NOT a cross-functional tension (a tension would be a SIGN mismatch, which did not occur). The WP synthesis states this directly ("No Q1 workshop seed — no sign mismatch"); the housekeeping ledger §"Consumption pointers" confirms it ("the W3-1 KCP sign test did NOT mismatch"). Manufacturing a tension here would violate honest-count discipline. Not a workshop; not even a solo — the diagnostic closes in-direction with no open question and no downstream numerical pin.

- **W3-2 (log-periodic Im(s)) — the interior-line guard is settled by the disclosure discipline, NOT a Q1 adjudication.** The criterion-faithfulness question (was the literal band-argmax the pre-registration, or was the Hoffer-Lapidus "single SHARP peak" prose the pre-registration the guard faithfully encodes?) resolves cleanly AGAINST a workshop on inspection of the plan text. The plan pre-registers the SHARP-peak prose explicitly in TWO fields — the operator `form` (plan §W3-2 line 268) and `boundary_reachable_analytically.proof_ref` (line 278: "a genuine complex dimension is a single SHARP peak (Hoffer-Lapidus 2508.09512)") — alongside the numerical 10× floor. The interior-local-maximum guard (`scipy.find_peaks`, endpoint-excluding) is the faithful encoding of that pre-registered prose; it is forced AGAINST the PASS direction (it can only REJECT a DC-envelope-boundary shoulder, never manufacture an interior line), which is structurally incompatible with iterate-until-PASS (Class-6, which by definition moves TOWARD PASS). This is exactly the `v3-closure-recovery.md` Class-1 boundary: in-session structural correction WITH disclosure (WP §W3-2 §Methodology + verdict value string) is NOT convention-shopping. The competing reading "the literal band-argmax was THE pre-registration and the guard is post-hoc criterion-shopping" is not defensible against the plan text — both candidate agents, reading the pre-registration honestly, converge that the guard is faithful. That is parallel-agreement (rule §"is NOT" item 6), not adversarial physics, and the resolution is a methodology-rule APPLICATION already governed by the disclosure discipline (Q2-marker territory), not a new structural derivation. No Q1.

The genuine residual in W3-2 — whether the scheme-dependent sidelobe structure is FFT-ringing of the single shell-bump at the 200-point/992-mode dynamic range (the substrate-first prediction) OR a finite-L log-periodic artifact persisting to high L (the Track-B tension with CM-1995) — is NOT a current-ledger dissonance between two readings of an existing result. It is an unresolved-pending-COMPUTE question whose resolution is a higher-dynamic-range RE-RUN (same pinned pipeline on the s84 L=12 spectrum, 155,984 eigenvalues), not an adjudication between two first-principles readings. The WP already routes it correctly as the pre-registered compute carry-forward `CF-S105-LOG-PERIODIC-HDR-RERUN` (4-field spec, conditional-CF-on-INFO, plan §"Wave 3 → Wave 4 Decision Point"). That is a Q1-fails → solo-compute carry-forward (rule §"is NOT" item 1), already in the WP and already consumed by `/rclab-plan` — NOT workshop-eligible.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W4-1 candidate Q1 tension is already ledger-identified (cross-chunk; do not double-route).** The S104 housekeeping ledger (`session-104-housekeeping.md` §"Consumption pointers", `/rclab-investigate` pointer, line 79) flags ONE candidate Q1 tension for investigator attention — the **W4-1** two-handle internal inconsistency (power-law-EOS vs steepening-DOS reading of n_T's origin), currently routed as the compute carry-forward `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION`. Per the ledger, it becomes a Q1 workshop ONLY if the S105 compute FAILs under BOTH readings; at S104 close it is a compute CF, not a workshop. This belongs to chunk w4, not w3 — surfaced here only so the consolidator does not lose the ledger's pre-identified pointer and does NOT escalate it to a workshop prematurely. No w3 gate conflicts with W4-1.

- **No cross-wave conflict involving W3.** Both W3 INFOs are consistent with standing structural walls (ordered-not-chaotic level statistics across 4 functionals; CM-1995 simple-real dimension spectrum on the frequency axis). Neither W3 verdict conflicts with any other wave's PASS/INFO/FAIL on the framework ledger.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

(none new) — The sole genuine compute carry-forward from this wave, **CF-S105-LOG-PERIODIC-HDR-RERUN**, ALREADY exists in the WP `## Carry-Forward Computations` block (4-field spec; conditional-CF-on-INFO, fired). Per the spawn-prompt constraint I do NOT duplicate it. No Q2-hygiene, Q3-wave-together, or Q-other items surfaced for the first time by this investigator: the S104 housekeeping ledger §§A–F is complete for this wave (§D explicitly records the W3-1 saddle-guard methodology surface as INACTIVE — its linear-b_n-growth trigger did not occur, so no rule-file diff is required this session).

**EVOI-table check (no new frontier)**: neither W3 INFO surfaces a high-leverage open item ABSENT from `evoi-framework.md` §1–§4. CF-S105-LOG-PERIODIC-HDR-RERUN routes to the already-listed EVOI Tier-4 #17 (HIGHER-MOMENTS / MELLIN-POLES); KCP is a closing diagnostic with no downstream pin and no open frontier. No EVOI-routing flag needed.

## Not investigated (only if shell)

(N/A — both W3 gates executed: verdict lines present in `computations/session-104/s104_gate_verdicts.txt`, both INFO, both WP §-sections Status: COMPLETED. Not a pre-compute shell.)

## Wave-by-wave digest (consolidator background)

**Wave 3 — Spectral-functional diagnostics (gem-sourced: rmt + zeta mines).** Theme: two NEW spectral-functional readings of already-classified `D_K` fold eigenvalue content, executing the spectral-functional-pluralism principle on the chaos and complex-dimension axes. Both reuse on-disk artifacts (no irrep construction).

- **W3-1 `S104-KRYLOV-KCP`** (kitaev-quantum-chaos-theorist) — **INFO** (`e134597f…`; `sign=PASS / magnitude=INFO / regime=VALID`). Krylov-complexity peak on Peter-Weyl sector (2,1): KCP(fold τ=0.190)=6.0916 > KCP(τ=0 ref)=4.9061, matching sign(β_fold−β_ref)=+1 (β_fold=0.633 INTEG-39; β_ref=0 Fegan-closed τ=0). The 4th internally-consistent chaos functional after ⟨r⟩ (CHAOS-1), OTOC (CHAOS-2), SFF (FACTOR-46) — "have you checked complexity too?" gap closed in-direction. INFO solely on magnitude (rel_residual 0.2288 > 0.15 band; Huh KCP↔β map qualitative-monotone). Saddle guard (Bhattacharjee) INACTIVE: b_n ladders SATURATE (slopes ≈ −1e-4, r² ≈ 0.008) — bounded/integrable signature, λ_L=0; the A₂-saddle caveat never reached, documented as a standing methodology surface (GEM-TRIAGE Rank 12, flagged not self-written). τ=0 reference built FULL-physical via the Fegan/Sym^p primary path (s92 fallback NOT used; fold-cache fidelity 8.44e-15). No Q1 seed (no sign mismatch). No downstream numerical pin.

- **W3-2 `S104-LOG-PERIODIC-IMS`** (lizzi-spectral-functional-theorist) — **INFO** (`60e67494…`; `sign=N/A / magnitude=INFO / regime=MARGINAL`). FFT of the on-disk HK-OSCILLATION-61 heat-trace oscillatory residual in `ln t`, detrended `g(u)=K_osc(e^u)·e^{4u}` (Re(s)*=4 = n=0 curvature-grade pole, poleconv-A; Mellin label `(pole_in_s=4, curvature_grade_n=0)`; regulator `a_n^{ζ}`). The residual is a SINGLE non-oscillatory shell-correction bump (one lobe; 96.5% of band power in the DC-envelope shoulder), not a log-oscillation. Under the faithful Hoffer-Lapidus interior-local-maximum guard there is NO regulator-robust log-periodic line — candidate interior features are window-DEPENDENT sidelobes (Hann bin 33 vs rectangular bin 5; γ/d-vs-SDW axes disagree by 28 bins ≫ ±1 ⇒ `cross_axis_peak_stable=False`). `implied_complex_dim_pair=[4, 0]`; `Im(s)=0` detectable; the PROVEN CM-1995 simple-real dimension-spectrum wall is UNCONTRADICTED on the frequency axis (orthogonal to the closed magnitude axis HK-OSCILLATION-61, R_osc=2.23e-5). INFO (not FAIL) because the pre-registered cross-axis-instability clause fired at the S61 residual's limited dynamic range. Methodology disclosure (honest, anti-PASS): the literal band-argmax prominence criterion would have returned a false-positive off the DC-envelope boundary shoulder; the interior-line guard (forced against PASS) is the faithful encoding of the plan's pre-registered "single SHARP peak" prose — disclosed in WP §Methodology + verdict value string; `v3-closure-recovery.md` Class-1 boundary honored. Routes to `CF-S105-LOG-PERIODIC-HDR-RERUN` (already in WP CF; EVOI Tier-4 #17).

**Investigator note (process)**: the two obvious "tension" candidates in this wave (KCP sign-mismatch seed; log-periodic guard-vs-literal-criterion) are both pre-empted in the substance — the KCP sign matched (no mismatch occurred), and the log-periodic guard is governed by the already-applied disclosure discipline (parallel-agreement, not adversarial divergence). Honest count: 0 workshops, 0 solos. The session's one ledger-identified Q1-candidate (W4-1 n_T transfer) lives in another chunk and is correctly routed as compute pending S105.
