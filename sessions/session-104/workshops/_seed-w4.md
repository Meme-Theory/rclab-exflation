# Seed — Chunk w4 (Wave 4: Transit-shape)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: session-104-w4-workingpaper.md + session-104-plan-w4.md
**Wave summary**: 2 gates, both executed (NOT shell). 1 FAIL (W4-1, composite: sign=PASS / magnitude=FAIL / regime=VALID) + 1 INFO (W4-2). Verdicts on disk at `s104_gate_verdicts.txt` lines 7 + 37; all six artifacts (2 scripts / 2 npz / 2 png) present. Structural reading: W4-1 is a localized INTERNAL inconsistency between two GR-universal transit-shape handles (memory-tail slope ⟺ n_T transfer), already routed as a single forward compute with a named suspect leg; W4-2 is a clean nameable bridge spec blocked on ONE named ingredient. Neither admits a present two-reading adversarial adjudication.

## Slot 1 candidates — solo reviews (`/rclab-review`)

(none — see `## No candidates` reasoning)

## Slot 2 candidates — workshops (`/rclab-workshop`)

(none — see `## No candidates` reasoning)

## No candidates

Wave 4 produces ZERO workshops and ZERO solo reviews. Both gates close cleanly to forward computes whose suspect legs / unpinned ingredients are already NAMED, and neither presents a present-tense two-reading adjudication that would require cross-agent rebuttal.

**W4-1 FAIL (the wave's live substance) is Q-other (solo compute), NOT Q1.** The gate is a genuine INTERNAL inconsistency: on the stiff Zel'dovich transit EOS (w=1), the Ünal-Veske memory-tail slope (p=2 EXACT, Sage-QQ; round-trips to effective-w_slope = 1.000000) and the independently-pinned blue tilt n_T(transit) = +0.4676 (effective-w_nT = 0.536764 under the pre-registered SLOW-ROLL-CONSISTENCY-AT-TRANSIT-SCALE map) disagree at 46.3% ≫ the 20% band. Because nonlinear memory is GR-universal, the emergent metric must reproduce it, so this is a real internal falsifier — but the suspect leg is ALREADY LOCALIZED to a single named pin-vs-derivation mismatch: the **power-law-EOS reading** (the memory tail treats the transit as a clean w=1 background, which would demand n_T = +1) vs the **steepening-DOS reading** (n_T(transit) is set by the van Hove fold's `dln ε_H/dτ = +10.29` DOS spike per the BLUE-65 provenance, not by power-law w-re-entry). Applying the rule's "FAILs that admit MULTIPLE structural readings" test rigorously: the two readings do NOT presently disagree as competing first-principles transfer maps that two agents would adversarially derive. One reading (DOS-steepening) is the named-unpinned candidate map; the other (power-law) is the pre-registered map that already FAILed. The resolution is a SINGLE pre-registerable compute with a pre-registered PASS criterion (`|w_slope − w_nT,DOS|/w_slope ≤ 0.20`) — exactly the carry-forward `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION` the WP and plan already carry — not a derivation that produces a new structural claim from a genuine ledger-dissonance. By the 3-question discriminator, first-YES-wins: Q1 = NO (no present two-reading adjudication — one reading is a candidate compute, not a competing derivation), Q2 = NO (not a status-tag/hygiene/rule-diff/audit-extension/mechanical-re-run — it is a genuine substrate-physics compute), Q3 = NO (single axis, single gate, no N-condition AND-closeout). It falls to Q-other / "is NOT" item 1 (solo compute follow-up with pre-registered threshold + machinery pin → next session's plan via `/rclab-plan`). The Q1 trigger is a CONDITIONAL FUTURE: the CF itself pre-registers that IF the S105 compute FAILs under BOTH readings, the stiff-EOS pin becomes the suspect and the tension escalates to a Q1 adjudication — but that adversarial tension does not exist NOW; it is gated on a compute outcome. The S104 housekeeping ledger's consumption pointer states this disposition verbatim ("it becomes a Q1 workshop ONLY if the S105 compute FAILs under BOTH readings").

**W4-2 INFO is Q-other (solo compute), NOT a workshop.** The candidate Pillar-I-acoustic ↔ Pillar-VI-Hawking-transit ↔ Pillar-IV-a₂-emergent-metric bridge (proton-core Hawking-Ellis type-IV EMT ⟺ the PROVEN S85-W6-1 acoustic white-hole interior) is fully nameable in substrate variables: Γ_sub(r) := c_s²(1 − Mach(r)²) is the a₂-channel acoustic g_tt, Sage-verified sign-correspondent to the Dumitru-Noronha discriminant (subsonic ⇒ type-I; Mach=1 ⇒ type-II boundary; supersonic core ⇒ type-IV); the restoration radius is the relay's Mach(r)=1 acoustic horizon. EXACTLY ONE ingredient is unpinned and NAMED: the localized-relay internal acoustic-flow profile v(r)/Mach(r) (the substrate's relay patterns are standing-wave superpositions — flow-free ⇒ Γ_sub > 0 everywhere ⇒ degenerate type-I; the only constructed flow is the global transit = dead-BLV map, EXCLUDED by pin). The next step is the construction-and-sign-test compute `CF-S105-RELAY-VR-CONSTRUCTION`, a solo follow-up with a pre-registered PASS criterion (`sign(Γ_sub) < 0` core ∧ `> 0` exterior ∧ Mach=1 crossover ∧ ANEC wall). There is no adversarial tension: the spec author and the cross-reviewer (schwarzschild-penrose-geometer) AGREED the identity is nameable and the one gap is named. By the discriminator: Q1 = NO (no competing readings — the identity is stateable and the gap is a missing construction, not a contested interpretation), Q2 = NO, Q3 = NO → Q-other / "is NOT" item 1.

No cross-wave PASS/INFO conflicts originate in W4 (both W4 gates are self-contained on frozen prior-session inputs; the plan confirms neither feeds nor depends on any other S104 wave). No borderline-σ detection (W4-1 carries zero amplitude/detectability content — Ω_GW amplitude RETIRED, Row #7.audit-3/-4; W4-2 is a SPEC gate, no numerical detection). No methodology-vs-substrate blur. No registered theorem flagged for adversarial re-test. Honest count per `Investigating-Workshops.md §"Honest count discipline"`: 0 Slot-1, 0 Slot-2.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W4-1 ↔ EVOI Tier-1 #1 (A(t)-FRIEDMANN-RECONCILE / §6.3 a(t)-gap) — DEPENDENCY, not conflict.** The W4-1 FAIL localizes a tension in the SUBSTRATE-side transit-EOS derivation chain (which reading of n_T(transit)'s origin — power-law w=1 vs DOS-steepening — is faithful). The DOS-steepening reading is grounded in the BLUE-65 provenance `dln ε_H/dτ = +10.29`, which is a transit-dynamics quantity adjacent to the rank-1 a(t)/effective-Friedmann gap (the §6.3 dimensionless dynamical content). The `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION` outcome (which leg carries the 46.3%) bears on how the stiff transit EOS is read in the emergent-metric a₂ self-coupling sector. Surfacing so the consolidator notes the adjacency at S105 plan-freeze — NOT a workshop and NOT a new EVOI row (the transfer-adjudication is downstream of an existing Tier-1 row, not a new frontier). Both waves cited: W4 (this chunk) ↔ EVOI §1 rank-1.
- **W4-2 ↔ EVOI Tier-3 #14 (PAGE-CURVE) — ADJACENCY, already noted in EVOI.** The EVOI Tier-3 #14 note flags "the Rank-6 area-operator↔modular-generator bridge-spec (S104 Wave 5, gem-sourced)" as the genuinely-new Page-curve-adjacent object. The W4-2 type-IV-EMT ↔ acoustic-white-hole-interior bridge is a SISTER gem-sourced bridge spec on overlapping pillars (Pillar VI Hawking transit). Surfacing only to note that S105's two bridge-construction CFs (`CF-S105-RELAY-VR-CONSTRUCTION` from W4, plus any Wave-5 area-modular successor) share the Hawking-transit pillar and the "transit mathematics shows up at a smaller scale" structural pattern — a potential JOINT-CONSIDERATION pairing at S105 plan-freeze, NOT a workshop. Both waves cited: W4 (this chunk) ↔ EVOI §3 rank-14 + S104 Wave 5 (NOT in this chunk; flagged by reference only).

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

(none NEW — the WP already carries both genuine forward computes, and the S104 housekeeping ledger §B confirms both as primary substrate-physics CFs, not Q2 hygiene:)

- `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION` — ALREADY in WP §"Carry-Forward Computations" (the W4-1 suspect-leg remediation: derive the n_T → effective-w map under the DOS-steepening reading and re-run the two-handle consistency; gate `S105-MEMORY-NT-TRANSFER`, PASS iff ≤ 0.20 under the DOS map, FAIL-under-both → escalate to Q1). Do NOT duplicate.
- `CF-S105-RELAY-VR-CONSTRUCTION` — ALREADY in WP §"Carry-Forward Computations" (the W4-2 unpinned-ingredient construction: build the localized-relay v(r)/Mach(r) on the a₂ channel, evaluate sign(Γ_sub) small-r vs large-r, extract the Mach=1 crossover, operationalize the ANEC wall; gate `S105-TYPEIV-EMT-COMPUTE`). Do NOT duplicate.

No upstream wave-synthesis miss detected: both Q-other items were correctly identified at wave-synthesis time and carried as primary WP CFs; the housekeeping ledger §B/§C/§D are empty of W4 items by correct classification (these are genuine computes, not Q2 hygiene).

## Not investigated (only if shell)

(N/A — both W4 gates are fully executed; not a pre-compute shell.)

## Wave-by-wave digest (consolidator background)

**Wave 4 — Transit-shape (gem-sourced from the S103 research-sweep triage).** Two transit-machinery gems, both on frozen prior-session inputs, neither cross-wave-coupled.

- **W4-1 `S104-W4-1-NONLINEAR-MEMORY-IR-SLOPE` (quantum-acoustics-theorist; cross-check hawking-theorist) — FAIL (composite).** Evaluates the Ünal-Veske (2511.08514) universal nonlinear-memory deep-IR slope `p(w) = 3 − 2|(3w−1)/(3w+1)|` on the stiff Zel'dovich transit EOS (w_stiff = 1, EXACT). Result: p(1) = 2 EXACT (Sage-QQ), w=1 > 1/3 ⇒ memory-tail branch, `Ω_GW,mem ∝ f²` — the branch classification is robust and stands regardless of the consistency verdict. The FAIL is the internal-consistency half: the memory-slope effective-w (= 1.000000, round-trip exact) and the n_T(transit) = +0.4676 effective-w (= 0.536764 under the named SLOW-ROLL-CONSISTENCY-AT-TRANSIT-SCALE map `n_T(w) = 2(3w−1)/(3w+1)`) disagree at dev = 0.463236 ≫ τ = 0.20. 3-tuple sign=PASS (memory-tail direction correct) / magnitude=FAIL (46.3% > 20%) / regime=VALID (closed-form exact maps, no truncation breakdown) → composite FAIL, dual-prior reallocated 0.90 to Track_B (genuine INTERNAL inconsistency, NOT dead-detector). Three hard fences held: EOS slot-distinction (w_phonon = 0.202392 relic-gas NEVER substituted as the memory driver), transit-scale matching (CMB-pivot images n_T_PathH = −0.000934 / n_T_PathC = −0.00146644 recorded FORBIDDEN, 54.04-decade scale separation), and the RETIREMENT note (zero amplitude/detectability content — the Ω_GW amplitude flagship is RETIRED, Row #7.audit-3/-4, LISA-STERILE). Suspect leg NAMED: power-law-w=1 reading (memory tail) vs DOS-steepening origin (n_T, `dln ε_H/dτ = +10.29` van Hove spike). Routed to `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION`.

- **W4-2 `S104-W4-2-TYPEIV-EMT-BRIDGE-SPEC` (transit-dynamics-theorist; cross-check schwarzschild-penrose-geometer) — INFO.** SPEC-first bridge: the candidate identity between the Dumitru-Noronha (2505.09720) proton-core Hawking-Ellis type-IV EMT (`Γ = (P_t+T₀₀)² − 4M⃗² < 0` ⇒ complex eigenvalues, no static rest frame) and the PROVEN S85-W6-1 acoustic white-hole interior. Verdict INFO = identity STATEABLE + 4-field S105 spec EMITTED, exactly ONE ingredient unpinned and NAMED. (a) Γ_sub object NAMED: `Γ_sub(r) := c_s²(1 − Mach(r)²)` = a₂-channel acoustic g_tt, Sage-verified sign-correspondent to the Dumitru discriminant (driver map: flow momentum-density v² dominating c_s² ⟺ J-GFF energy-flux dominating diagonal stress — the same does-a-static-rest-frame-exist eigenvector-causality question). (b) restoration radius NAMED: the relay's Mach(r)=1 acoustic horizon (localized analog of the PROVEN S85 fold/exit horizons τ_H = 0.1831/0.1969 bracketing τ_fold = 0.19, Mach=13.75). (c) 4-field S105 spec EMITTED. The unpinned ingredient: the localized-relay internal acoustic-flow profile v(r) — relay patterns are flow-free standing-wave superpositions (⇒ Γ_sub > 0 everywhere ⇒ degenerate type-I); the only constructed flow is the dead-BLV global transit (N_pair=1, EXCLUDED by pin). Limit cross-checks reproduce the paper (pion T^{0i}=0 → type-I; tail v→0 → type-I). Routed to `CF-S105-RELAY-VR-CONSTRUCTION`.

**Synthesis-level reading (team-lead):** both gates run the substrate-first arrow `D_K → transit EOS / a₂ emergent-metric channel → shape observable`. W4-1's FAIL constrains the substrate-side derivation chain (which n_T-origin reading is faithful), explicitly NOT any detector. W4-2 asserts "transit mathematics inside a proton" as a nameable candidate, never proton physics explaining the substrate. No registry slot landed (W4-2 is a SPEC gate; a future §VII promotion adopts the 5-anatomy + 3-level discipline and the Stage-0 authoring-exclusion, NOT the S85/S97 authors). No Q2-class items surfaced from this wave (housekeeping §B/§C/§D empty of W4 by correct compute-vs-hygiene classification).
