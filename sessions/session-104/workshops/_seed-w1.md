# Seed — Chunk w1 (Wave 1: Standing precision CFs)

**Date**: 2026-06-10   **Investigator**: phonon-first-cosmologist   **Source**: session-104-w1-workingpaper.md + session-104-plan-w1.md
**Wave summary**: 4 gates, all COMPLETED. **3 PASS** (W1-1 `S104-VIIAM-L11-ANCHOR` `3d4a8049`; W1-2 `S104-SWMAX-MPMATH-EDGE` `f4375036`; W1-4 `S104-VIIBS-CLAUSE-B-WORDING` `ead021c6`) + **1 PRE-REG-INC** (W1-3 `S104-BRANCH-IV-DIRECT-L1314` `b48b609f`). Structural reading: the wave's theme is "deeper-resolution re-runs of already-pinned S103 verdicts, NOT new physics" — and it delivered exactly that. Two PASSes did not merely confirm but **decisively settled their pre-registered dual priors** (W1-1 closed the deeper-truncation pathway the S103 L=10 FAIL left open; W1-2 turned the S103 float64 knife-edge into a *proven* strict-interior point via a Sage exact-algebraic-field `is_zero()=False` theorem). One PRE-REG-INC honestly closed against a now-CHARACTERIZED dense-3^p irrep-construction wall (Sym^13=40.7 TB / Sym^14=366 TB), with the GT-builder route named and a CF already filed. One PASS is a METHODOLOGY-class designated-writer prose patch (Q2 by construction). Verified on disk: 4 verdict lines present (grep), all script/npz/png artifacts present (glob), all 4 WP sections Status: COMPLETED. NOT a pre-compute shell.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

(none — see `## No candidates` below)

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

(none — see `## No candidates` below)

## No candidates

Wave 1 produced ZERO workshop-eligible (Slot 2) and ZERO solo-review (Slot 1) candidates. Each gate was tested against the rule's six workshop signals (FAILs with multiple readings · borderline INFOs · cross-wave tensions · methodology-vs-physics blurs · genuinely-divergent convention questions · existing claims needing adversarial testing); none fired a Q1 math/physics adjudication.

- **W1-1 PASS** is clean and convention-settled. The PASS holds under the prefactored comparator (ratio_prefac(11)=0.868635<1) while the bare comparator still FAILs (ratio_bare=1.617565) — but the prefactored 'ii' arbiter was **pinned at plan-freeze** as the sole gating comparator (anti-comparator-shopping, S104-context Wave1 item-1.3), with a hard `prefactored_arbiter_confirmed` assertion verifying the upstream npz declared it the Registry-PASS arbiter before the inequality was evaluated. There is no DISAGREEMENT to adjudicate: the comparator choice is a closed pre-registration, the L=10 sentinel reproduced bit-exact (resid 0.000e+00), and the dual prior resolved 0.85 → Track A. Q1 NO.
- **W1-2 PASS** *extinguished* its own workshop seed. The S103 W3-1 result was a genuine borderline INFO (deviation == S_W_max−1 to 5+6 sig figs), and the gate carried a real two-reading dual prior (Track A strict interior / Track B exact saturation). This gate decided it definitively at ≥300-bit mpmath AND in Sage's exact algebraic field: `Δ.is_zero() = False` is a **theorem** (a provable algebraic non-identity, not a numerical estimate), so Track B (exact saturation) is *proven* excluded and Track A holds with a real sub-float64 margin (+5.211e-09). The two readings cannot both be right and the math already decided which — there is nothing left for an adversarial panel to converge on. Q1 NO.
- **W1-3 PRE-REG-INC** is the canonical "is NOT a workshop" item 1 (solo compute follow-up): the resolution is a new Gelfand-Tsetlin / monomial-basis (p,0) irrep builder, not a contested reading. The wall is a dense-3^p `irrep_symmetric_power` engineering artifact (the (p,0) irreps are finite-dimensional, dim 105/120, and exist) — an engineering obstruction, NOT a physics obstruction or an interpretive divergence. The moment-evaluator sentinel reproduced bit-exact (ρ_B(8/10/12) diff 0.0), the span is offset-independent (substitution-chain-verified — the w0_FW vs W0_B anchor choice cancels), and the S103 FB-envelope INFO (spread≈0.0221) stands as the best bound. The forward work is already filed as `CF-S105-BRANCH-IV-GT-BUILDER` in the WP CF section. Q1 NO → Q2/compute-CF (already routed).
- **W1-4 PASS** is METHODOLOGY-class by construction: a designated-writer registry-prose patch upgrading the §VII.BS clause-(b) annotation 'standing premise (Open Q6)' → 'result', licensed by two verified S103 preconditions, with the frozen Stage-0 blockquote byte-IMMUNE (e669ccd2, len 2514, pre==post==pin) and the theorem grade UNCHANGED (STAGE-3-PERMANENT, 19→19). This is a confidence-EQUALITY fix (prose brought UP TO register status), capstone-hygiene Q3=NO — the rule's "is NOT a workshop" item 7 (registry-state / prose hygiene). It is already recorded in `session-104-housekeeping.md §A2`. Q1 NO → Q2 (already routed).

Cross-wave check: the S104 housekeeping ledger's `## Consumption pointers` explicitly names the ONLY candidate Q1 tension for the whole session as the *W4-1* two-handle internal inconsistency (power-law-EOS vs steepening-DOS reading of n_T's origin) — that is in chunk w4, not this chunk, and per the ledger it becomes a Q1 workshop ONLY if the S105 compute `CF-S105-MEMORY-NT-TRANSFER-ADJUDICATION` FAILs under BOTH readings (it is currently routed as a compute CF, not a workshop). No w1 result feeds a workshop seed.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **JOINT-CONSIDERATION (dedup, not a tension)** — w1's `CF-S105-BRANCH-IV-GT-BUILDER` (Gelfand-Tsetlin (p,0) builder + direct ρ_B(13)/ρ_B(14) deep-truncation re-run; the Friedrich-Bär envelope replacement) carries a plan-freeze JOINT-CONSIDERATION constraint with w5's `CF-S105-LOOP-COUNTING-BINDING-REDUCTION` (the lab-anchored loop-counting / L^{−α}-truncation-envelope sibling). Per `session-104-housekeeping.md §"Consumption pointers"` + the EVOI §6 standing-gaps note: **no double-scheduling of α_loop vs Friedrich-Bär** — both touch the L_max truncation-envelope / Level-2-binding-vs-identity discriminator on the same DR3-readiness corridor (Tier-2 #7c). This is a scheduling/dedup constraint for `/rclab-plan` Step-1c, NOT an adversarial physics tension, and it is already documented in the housekeeping ledger; surfaced here only because one-investigator-per-wave means no single investigator sees both endpoints. Cite both CFs together at plan-freeze.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other / already-filed, do NOT duplicate]** — `CF-S105-BRANCH-IV-GT-BUILDER` (Gelfand-Tsetlin / monomial-basis (p,0) irrep builder constructing the rep directly in the dim_sym=(p+1)(p+2)/2 space, never forming 3^p; build (0,13)/(13,0)/(0,14)/(14,0); union with the 12 cached mixed level-13 sectors in `s104_sym_p_chain_cache_L1314.npz` + the s84 L≤12 cache; re-run DIRECT spread_CAC over L∈{12,13,14} vs the UNCHANGED W5-2 band PASS≤0.025 | INFO (0.025,0.050] | FAIL>0.050). Effort ~1–1.5 gates.) is ALREADY present in `session-104-w1-workingpaper.md §"Carry-Forward Computations"` as a primary MATH CF — listed here only for cross-reference completeness; it is the W1-3 PRE-REG-INC's named lift path (Tier-2 #7c DR3-readiness successor). No NEW carry-forward beyond it: the wave-synthesis captured the only forward compute the wave generated.

(No Q2-hygiene items surfaced for the first time by this investigator: the W1-4 prose patch and the §VII.AM dated status append are already in `session-104-housekeeping.md §A` (A1/A2/A4), so no upstream wave-synthesis miss to route. No Q3 parallel-compute-wave structure in this wave.)

## Wave-by-wave digest (consolidator background)

**Wave 1 — Standing precision CFs (owner: gen-physicist; theme: deeper-resolution re-runs of pinned S103 verdicts, NOT new physics).** Four cross-session precision carry-forwards inherited from the S103 working papers, each a deeper-resolution re-run of an already-pinned S103 verdict.

- **W1-1 `S104-VIIAM-L11-ANCHOR`** — GEOMETRIC, [SIGN], `connes-ncg-theorist`. **PASS** (`3d4a8049`). anchor(11)=2.109477e-05 < env_prefac(11)=2.428498e-05 strict under the pre-registered prefactored 'ii' comparator ⇒ ratio_prefac(11)=0.868635<1. The §VII.AM fold-transit envelope row converges INTO its Level-2 envelope one truncation deeper than canonical (Level-3 < Level-2 at L=11), closing the deeper-truncation pathway the S103 L=10 FAIL (`b47ccf98`, ratio 1.1578) left open. Mechanism: the anchor decays ×0.4798 across L=10→11 vs the envelope's ×0.6395, crossing from outside (1.1578) to inside (0.8686). Bare comparator stays outside (1.6176, REPORTED-NOT-GATING). L=10 sentinel reproduced bit-exact (resid 0.000e+00); all 5 hard assertions passed; all 4 input-SHAs matched. Row registry-PASS-eligible at L=11; Level-1 cohomology-class theorem-STRUCTURE STAGE-3-PERMANENT and UNTOUCHED. Dated status append landed at `permanent-results-registry.md:16772` (housekeeping §A1).
- **W1-2 `S104-SWMAX-MPMATH-EDGE`** — PHONONIC, [SIGN], `transit-dynamics-theorist`. **PASS** (`f4375036`). sign(deviation − (S_W_max−1)) = −1 at mp.prec=320 bits AND in Sage exact real-algebraic-field (AA). The W-stage SU(1,1) Bogoliubov-window deviation lies STRICTLY INSIDE the asymmetric upper edge (S_W_max−1 = 2|β_W|²+2|α_W||β_W|, window centered at 1+2|β_W|²≠1); Δ = −5.210998e-09, margin +5.211e-09 inside the edge. `Δ.is_zero()=False` in AA is a THEOREM ⇒ the S103 float64 knife-edge (5+6-sf agreement) was a publication-precision near-miss, NOT an algebraic identity (exact-saturation Track B definitively excluded). S79 magnitudes-only sufficiency holds with real margin; F_amp slot 0.3885 vindicated as window-interior. Edge-vs-frozen resid 7.629e-17 reproduces the S103 record; unitarity |α_W|²−|β_W|²−1 = −2.451e-16 (noise floor). Dual prior → 0.85 Track A. No `# composite-precedence:` row (sign ≠ 0).
- **W1-3 `S104-BRANCH-IV-DIRECT-L1314`** — GEOMETRIC, [VERIFY], `gen-physicist`. **PRE-REG-INC** (`b48b609f`). The pre-registered FEASIBILITY FALLBACK fired: DIRECT ρ_B(13)/ρ_B(14) requires the pure-symmetric sectors (0,13)/(13,0)/(0,14)/(14,0), but `irrep_symmetric_power` materializes a dense 3^p×3^p tensor space (Sym^13=40.7 TB, Sym^14=366 TB — physically impossible; no recursive (p,0) builder avoids the 3^p blowup, grep-verified). 12/14 level-13 MIXED sectors built cleanly on the RX 9070 XT (87.8 s, iD_herm_err ≤1.0e-15, cached to `s104_sym_p_chain_cache_L1314.npz`); have_13=False/have_14=False ⇒ DIRECT span uncomputable. The ρ_B reproduction sentinel PASSED bit-exact (ρ_B(8/10/12) diff 0.0 ≤ 1e-12 ⇒ the evaluator IS the consumed one). Span shown offset-independent by substitution chain (offset_B cancels; the w0_FW=−0.918 vs W0_B=−0.842454 anchor choice is immaterial to the verdict). Honest mechanical closure per `mechanical-closure-discipline.md`, deferred to S105 — NOT a FAIL. The S103 Friedrich-Bär envelope-bound INFO (`508c7cf3`, spread≈0.0221, PASS-band-interior) stands as the best available bound. CF: `CF-S105-BRANCH-IV-GT-BUILDER`. Continues Tier-2 #7c (DR3-readiness).
- **W1-4 `S104-VIIBS-CLAUSE-B-WORDING`** — NON-PHONONIC (METHODOLOGY-class), [AUDIT], `gen-physicist` (designated writer). **PASS** (`ead021c6`). Both S103 preconditions verified PASS at runtime (S103-VIIBS-CLAUSE-B-SCOPE-ANNOTATION `2c27b197` ∧ S103-NNU-BUNDLE-EXHAUSTIVENESS `ac1dbb28`, rank_aug=1, second_rel_sv=1.0658e-17) ⇒ upgrade LICENSED. The §VII.BS clause-(b) bundle-exhaustiveness characterization upgraded 'standing premise (Open Q6)' → 'result' on all 3 annotation surfaces (registry lines 157 / 21392 / 21399), citing the rank-1 certificate. Frozen Stage-0 blockquote SHA `e669ccd2` (len 2514) byte-IMMUNE and UNCHANGED (pre==post==pin, HARD-asserted); theorem grade UNCHANGED (STAGE-3-PERMANENT, count 19→19). Confidence-EQUALITY fix (prose brought UP TO register status); capstone-hygiene Q3=NO. AFTER-pattern single-shot (build→write_atomic_with_fsync→re-read+verify→one emit). METHODOLOGY-class M1∧M2∧M3∧M4 all satisfied (M4 allowlist row landed at plan-freeze, sha `e7275804`). Housekeeping §A2.

**Cross-wave / EVOI note for the consolidator**: W1-3 directly continues EVOI Tier-2 row 7c (`W₀-BRANCH-IV-EVALUATOR / DR3 readiness`); the GT-builder CF is the already-named successor on that row's DR3-readiness corridor — no NEW high-leverage frontier surfaced (nothing to route INTO §1–§4 of the EVOI table that is not already tracked). The wave touched ONLY Level-2/Level-3 numerical ladders and one prose annotation surface; no Level-1 / cohomology-class / STAGE-3-PERMANENT theorem was consumed or altered (W1-1, W1-4 both HARD-assert this).
