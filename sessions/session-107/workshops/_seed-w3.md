# Seed — Chunk w3 (SDW 2nd-moment + ⟨r⟩-trend)

**Date**: 2026-06-14  **Investigator**: phonon-first-cosmologist  **Source**: w3 WP + plan

**Wave summary**: Wave 3 is two GEOMETRIC spectral-functional diagnostics, both ran, both INFO, both pre-registered. `S107-SDW-2ND-MOMENT-EFT` (EVOI Tier-3 #12, register-sourced): the deepest-closed-cone 2nd SDW-layer EFT-control ratio `r_2nd = (a_8/a_6)·(Λ_sp/M_KK)⁻² = 0.1604` lands in the marginal band [0.1, 0.5) — under the S96 loose `<0.5` band but NOT the tight EVOI-#12 `<0.1` ceiling → EVOI #12 RESOLVED-MARGINAL, retires to §5. `S107-W1-RTREND-L1416` (OPTIONAL/non-blocking precision item): ⟨r⟩ flat-Poisson across L∈{12,14,16}, 0.4118/0.4254/0.4200, both new points in the incommensurate-Poisson band [0.37,0.44], no drift toward GOE (0.53) or clustered (0.27) → #9e-B confirmed truncation-stable, stays CLOSED at L12. Both outcomes matched their pre-registered dispositions to the digit (SDW: analytic 0.160424, INFO band; ⟨r⟩: flat-Poisson [VERIFY] PASS-on-band-membership, INFO-qualified by the disclosed L16-incompleteness). Zero competing-reading adjudications in this chunk.

## Slot 1 candidates — solo reviews (/rclab-review)

(none — see `## No candidates` below)

## Slot 2 candidates — workshops (/rclab-workshop)

(none — see `## No candidates` below)

## No candidates

Chunk w3 produced **0 Slot-1 and 0 Slot-2 candidates**. This is an honest count per `Investigating-Workshops.md §"How to identify a real workshop"` + §"'No workshops' is a valid output", not a thin pass:

- **No FAILs admitting multiple readings.** Both gates closed INFO. `S107-SDW-2ND-MOMENT-EFT` has a single pre-registered reading (plan §W3-1 INFO_meaning + dual_prior: INFO ⇒ 0.9 posterior mass to Track B, "layer hierarchy representation-theoretic/numerical at the 2nd layer, extending the S96 verdict"). The computed `r_2nd = 0.160420` reproduces the plan's analytic 0.160424 and the independent S96 npz `r_driver_sp[3]=0.16041964` to `|dev|=0.0e+00`. There is nothing for two agents to disagree about — the value, the band, and the disposition were all pinned at plan-freeze.
- **The borderline INFO is not a marginal-detection dispute.** `r_2nd=0.1604` is marginal-band, but the marginal disposition was the pre-registered most-likely outcome with a pre-assigned posterior re-allocation; it is not a ~0.5σ–1σ detection where domain agents would split on detection-vs-noise. Likewise the `S107-W1-RTREND-L1416` INFO is qualified SOLELY by the transparently-disclosed L16-incompleteness (operational L=15; the 17 top p+q=16 sectors FB-bounded analytic-tail, η_FB=0.3928) — a pre-registered INFO-qualifier (plan §W3-2 INFO_meaning), not a FAIL and not a tension. The band-membership PASS predicate (`both_in_band=True`, no GOE/clustered drift) is satisfied and reported separately.
- **The lizzi functional-sensitivity contrast is settled, not contested.** The same D_K spectrum gives opposite EFT-control verdicts under Gaussian-cutoff (f₂/f₄=4.19 AMPLIFIES → 0.673) vs Mellin-f* (f₂/f₄=0.033 CRUSHES → 0.0053), but the WP and plan both fix the resolution non-adversarially: only the scheme-INDEPENDENT a-ratio driver `a_{2(k+1)}/a_{2k}` is FUNCTIONAL-INVARIANT (any common `w(L_max)` spectral-support prefactor cancels — the multiplicative-normalization cancellation invariant, `math-scripts.md`, MANDATORY K=3), so it alone is the structurally-defensible gated object; the f-modulation is reported AS a diagnostic, explicitly "NOT the gated quantity." This is lizzi-functional-pluralism content already established (`truncation-robust` (FI) ≠ `parametrically-controlled` are different properties), not a competing-reading seed.
- **No cross-wave tension in this chunk.** Both rows retire/confirm cleanly; neither conflicts with W1 (§VII.CB), W2 (Stage-2 cohort), or W4 (DESI-DR3). The 3-question discriminator (first YES wins) lands BOTH outcomes at **Q2** (the RESOLVED-MARGINAL #12 row-retire and the #9e-B truncation-stable confirmation are EVOI-register maintenance / status edits, orchestrator-direct), NOT Q1 — there is no two-reading divergence requiring a first-principles structural verdict.
- **The housekeeping ledger (authoritative Q2 filter) agrees.** `session-107-housekeeping.md §"Q2 marker"` records "No adversarial-physics workshops surfaced," and the EVOI maintenance for #12/#9e is already catalogued at §A item 2 (effected in-session). Per `Investigating-Workshops.md §"Enforcement at /rclab-investigate"`, every entry in that ledger is structurally a non-workshop — dropped, not propagated. The one borderline workshop-candidate the ledger names (the grandfathered/audit-substituted completeness gap vs a Stage-2-structurally-PASS bridge's STAGE-3 promotion) lives entirely in **W2**, not chunk w3, and was resolved conservatively in-session; it is not surfaceable from this chunk.

## Cross-wave flags

- **No new high-leverage EVOI open item from chunk w3.** Both gates are documented in EVOI §1–§4 already: row #12 (`SDW-2ND-MOMENT-EFT`, Tier-3, line 72 → RESOLVED-MARGINAL, retires to §5) and row #9e (`SUBSTRATE-COMMENSURABILITY-DISCRIMINATOR`, line 63 → RESOLVED via SPLIT at S106; the `CF-S107-W1-RTREND-L1416` annotation reads EXECUTED → INFO, #9e-B stays CLOSED). Neither topic appears in the §6 actionable queue (grep over lines 168+ returned no SDW / RTREND / 9e-B / commensurability match), which is correct — both are closures/confirmations, not forward computes. **Nothing to route INTO the EVOI table from this chunk.** (EVOI #12 is the row the campaign spawn-note flagged RESOLVED-MARGINAL this session; confirmed consistent with the WP constraint-map and the §A ledger.)
- **No cross-wave conflict involving chunk w3 gates.** The only S107 borderline adjudication candidate (W2 grandfathered-completeness-gap-vs-STAGE-3-promotion, resolved conservatively per the ledger §"Q2 marker") is W2-internal and does not touch the W3 spectral-functional/level-statistics gates; no PASS↔INFO/FAIL contradiction crosses into this chunk.
- **No housekeeping miss surfaced.** Both W3 Q2 items (EVOI #12 retire + #9e-B confirmation annotation) were already caught at wave-synthesis and catalogued in `session-107-housekeeping.md §A` item 2 + the mack sole-writer §VII.CC L-trend precision annotation (§A item 3); no first-time-surfaced Q2 candidate requiring routing-to-housekeeping per `Investigating-Workshops.md §"Enforcement"`.

## Carry-forwards (route to WP CF, NOT this schedule)

(none from chunk w3) — both wave outcomes closed in-session by intent. The W3 WP `## Carry-Forward Computations` block is deliberately empty: `S107-SDW-2ND-MOMENT-EFT` resolves EVOI Tier-3 #12 as a *disposition* (RESOLVED-MARGINAL → §5), not a new compute; `S107-W1-RTREND-L1416` is precision-only and #9e-B is already CLOSED at L12 (the optional gate carried no verdict-dependency for any other item, and could have been dropped without consequence). No 4-field-spec carry-forward computation, no Q3 parallel-wave structure, no methodology-rule extension originates in this chunk.

## Wave-by-wave digest

### Wave 3 — Spectral-functional diagnostics + commensurability precision

- **`S107-SDW-2ND-MOMENT-EFT` (lizzi-spectral-functional-theorist) → INFO** [SIGN]: sign=PASS, magnitude=INFO, regime=VALID. `r_2nd = (a_8/a_6)·(Λ_sp/M_KK)⁻² = (521.183178/765.593826)·(2.06)⁻² = 0.680757 × 0.235649 = 0.160420` ∈ [0.1, 0.5) marginal band. The 2nd SDW layer is under the S96 loose-control band but NOT the tight EVOI-#12 `<0.1` ceiling. The a-ratio ladder is MONOTONE INCREASING toward 1 (k=0→3: 0.431 → 0.487 → 0.567 → 0.681) — every higher SDW layer is closer to the EFT-control boundary; the deepest (k=3) is the worst case and lands marginal, extending the S96 base-layer reading to the 2nd layer. Cross-checks: E38 per-branch cache cross-check (a₀/a₂/a₄ reproduce bit-exact ⇒ a₆/a₈ pinned on the same zeta footing) PASS; S96 npz `r_driver_sp[3]` matches to `|dev|=0.0e+00`. Functional-sensitivity DIAGNOSTIC (not gated): a-ratio driver FUNCTIONAL-INVARIANT (`w(L_max)` cancels); f-modulated FUNCTIONAL-DEPENDENT (Gaussian AMP→0.673 / Mellin CRUSH→0.0053). Disposition: EVOI Tier-3 #12 RESOLVED-MARGINAL → §5. **Settled in-session; no adversarial tension.**
- **`S107-W1-RTREND-L1416` (spectral-geometer) → INFO** [VERIFY], OPTIONAL/non-blocking: ⟨r⟩ SPEC-B flat-Poisson across L∈{12,14,16_op} = 0.4118/0.4254/0.4200 (Δ_{14−12}=+0.0136, Δ_{16−14}=−0.0054), both new points IN the Poisson-incommensurate band [0.37,0.44], drift sentinel fires on neither (`any_chaos_or_clustered_drift=False`). σ-insensitive Weyl-smooth FI cross-check sits at the Poisson asymptote `2ln2−1=0.38629` at every L (0.3888/0.3842/0.3828); SPEC-A brackets from above. INFO qualified SOLELY by the disclosed L16-incompleteness: L16 is OPERATIONAL-15 (136 dense sectors p+q≤15; the 17 p+q=16 top-shell sectors FB-bounded analytic-tail only, η_FB_lower=0.3928, NOT diagonalized) — a pre-registered INFO-qualifier, not a FAIL. #9e-B (length-spectrum incommensurate-Poisson) stays CLOSED at L12; this gate converts the L12-only datum into a truncation-stable trend and cannot reopen the SPLIT verdict. **Precision-tightening confirmation; no adversarial tension.**

**Process note**: the W3-2 optional ⟨r⟩-trend agent finished all artifacts then hung in a post-completion WP edit-retry loop (TaskStop'd after on-disk verification) — recorded at `session-107-housekeeping.md §A item 8` as a within-wave-multi-gate-aggregator atomic-writer lesson; not a workshop, not a carry-forward, already logged.
