# Seed — Chunk w1 (§VII.CB magnitude FAIL)

**Date**: 2026-06-14  **Investigator**: phonon-first-cosmologist  **Source**: w1 WP + plan

**Wave summary**: Wave 1 ran exactly one gate, `S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR` (transit-dynamics-theorist), to discharge the single HELD Level-3 row of the EXISTING §VII.CB cross-pillar bridge (Pillar I↔VI↔IV; theorem-STRUCTURE STAGE-3-PERMANENT at S106). The gate computed the MAGNITUDE channel `M(L) = Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` and tested it against the binding `L⁻³` Level-2 envelope. **Verdict: FAIL (robust, direction-neutral).** `res(L=10) = 0.2941` (~294× above the 1e-3 envelope; PRIMARY FALSE) and `alpha_fit = −0.954` (flows toward `g_M` but at ~L⁻¹, far below the [2,4] FLOWING band; SECONDARY FALSE). The FAIL is lift-robust (all three lift dictionaries D1/D2/D3 FAIL both sub-criteria), so it is a genuine gate closure, not a lift-dependent INFO. C_1 sign = −1 (§VII.AF.1-negative fork; reported diagnostic, dual-prior left at 50/50, NOT chained). The WP diagnoses the structural reason as a partial-sum↔ζ-sum gap: `g_M = a_2_FW_zeta = 2776.165389` is the ζ-regularized a₂ (analytic-continuation value), not the limit of the truncated moment `Σ_{k≤L}|λ_k|⁻⁶` (Z(L)≈383–431 at L≤12, Richardson limit ~632 — 4–7× below g_M). The Level-1 cohomology-class identity `[T^{(IV)}]_{a₂,HKR}=[g_M]_{a₂,HKR}` and the §VII.CB theorem-STRUCTURE are UNAFFECTED — only the finite-L *numerical* Level-3 anchor on the magnitude channel fails. Routed to `CF-S108-VIICB-MAGNITUDE-REMEDIATION` (WP CF; consumed by `/rclab-plan`).

## Slot 1 candidates — solo reviews (`/rclab-review`)

### S-1 — Is the §VII.CB partial-sum↔ζ-sum gap STRUCTURAL or a slow-convergence artifact? (analytic pre-screen for the S108 remediation)

**What this answers**: The W1 WP makes a substantive interpretive claim it asserts but does NOT establish — that the magnitude channel's failure to bind is STRUCTURAL ("the partial-sum vs zeta-sum gap is structural, not a slow-convergence artifact", WP §"Solution-space consequence" + Wave-1 Synthesis). The WP's own evidence is suggestive only: the truncated curvature-degree-2 moment `Z(L) = Σ_{k≤L}|λ_k|⁻⁶ ≈ 383–431` at L∈{8,10,12} (Richardson limit ≈632, own exponent p≈0.52) sits 4–7× below the ζ-regularized target `g_M = a_2_FW_zeta = 2776.165389`. The discriminating question — can a *truncated* partial moment of `D_K²` at the s=3 curvature grade EVER reach the *ζ-regularized* (analytically-continued) a₂ at the binding `L⁻³` rate, or is the ζ-value irreducibly tail-dominated? — is a heat-kernel / Seeley-DeWitt / spectral-zeta analysis question answerable analytically, INDEPENDENT of running the S108 compute. A spectral-geometer reading determines whether S108's remediation (higher-L mesh {12,14,16} OR Abel/Richardson reconstruction of g_M) has any chance of binding below 1e-3, or whether the correct route is a ζ-NATIVE Level-3 observable (so §VII.CB's Level-3 row is permanently held on the partial-sum channel and re-anchored). This is a pre-screen that could save the S108 wave from running toward a foregone conclusion AND name the ζ-native re-anchor target if the gap is structural.

**Why solo-not-workshop**: There is no competing-reading divergence on disk — the W1 WP gives ONE reading (structural gap, asserted) and routes the discriminating test to a compute (`CF-S108-VIICB-MAGNITUDE-REMEDIATION`). No second agent holds an opposing position; the housekeeping ledger explicitly records "No adversarial-physics workshops surfaced" for S107. The task is an independent first-principles assessment (spectral-zeta vs truncated-moment asymptotics), not an adjudication between two agents who must rebut each other. A single domain-owner (spectral-geometer) reads the WP + the §VII.AF.1.OP-PROJ over-performance precedent + the §VII.AU under-performance precedent and writes the analytic verdict on the partial-sum↔ζ-sum reachability. This is exactly the rule's "EXISTING claims that need ADVERSARIAL TESTING — audit what we already claimed", which is solo synthesis, not a 2-agent workshop.

**Agents**: `spectral-geometer` (owns heat-kernel asymptotics, Seeley-DeWitt coefficients, spectral-zeta regularization vs Weyl partial-sum truncation — the primary author of the partial-sum↔ζ-sum reachability question).

**Source anchors**:
- `sessions/session-107/session-107-w1-workingpaper.md` §W1-1 Results NUMBERS table (Z(L), M(L), res(L) at L∈{8,10,12}; alpha_fit=−0.954; Richardson limit ~632) + §"Substrate-IS assessment" + §"Solution-space consequence".
- `sessions/session-plan/session-107-plan-w1.md` §W1-1 substitution chains Claim A/B; PASS/FAIL/INFO rubric; `g_M = a_2_FW_zeta = 2776.165389` (canonical_constants.py line 610, gate S88-A-N-FW-CANONICALIZATION, regulator a₂^{ζ}); `Level2(L=10)=1e-3` (S86 W5 `L_envelope_d4_Lmax10`).
- `sessions/permanent-results-registry.md` §VII.AF.1.OP-PROJ (the d=4 substrate-distance-1 pole over-performance regime, `err_STRICT=0.0095%`, 10× INSIDE envelope, lines 14918–14951) + §VII.AU (the d=4,s=3 under-performance sibling, empirical exponent 2.6926<3).
- `CF-S108-VIICB-MAGNITUDE-REMEDIATION` (W1 WP §"Carry-Forward Computations") — the compute this solo pre-screens.

**Output**: An analytic verdict on whether the truncated `Σ_{k≤L}|λ_k|⁻⁶` partial moment can reach the ζ-regularized `g_M` at the `L⁻³` rate (STRUCTURAL gap vs slow-convergence artifact), with first-principles backing (spectral-zeta analytic-continuation tail vs Weyl partial-sum); if STRUCTURAL, name the ζ-native Level-3 re-anchor observable §VII.CB should use; if ARTIFACT, predict the L at which `res<1e-3` and confirm the S108 {12,14,16}/Abel route is worth running. A 4-field math carry-forward (what/inputs/gate/effort) that SHARPENS or SUPERSEDES `CF-S108-VIICB-MAGNITUDE-REMEDIATION` (e.g., re-pointing it at a ζ-native observable instead of a higher-L partial-sum mesh).

## Slot 2 candidates — workshops (`/rclab-workshop`)

(none — see `## No candidates` rationale below; my chunk's single gate is a robust, lift-tested, direction-neutral FAIL with ONE asserted reading and a pre-registered compute route, not a competing-reading adjudication. The one workshop-candidate the S107 housekeeping ledger names lives in W2, outside this chunk — flagged below.)

## Cross-wave flags

- **W2 holds the only S107 workshop-candidate (NOT in my chunk).** The S107 housekeeping ledger (`sessions/session-107/session-107-housekeeping.md` §"Q2 marker" + §"Consumption pointers") names exactly ONE genuine borderline adjudication for the whole session: *does a grandfathered / audit-substituted Element-2 completeness gap block a Stage-2-structurally-PASS bridge's STAGE-3 promotion?* — touching the W2 Stage-2 cohort {K2/K7/K9/K11} (§VII.AC1/§VII.XW41/§VII.X2NEC/§VII.AC4). It was resolved conservatively in-session (hold the promotion; record the structural PASS-AND; route the completeness fix to S108, symmetric across all four). The ledger states it surfaces as a workshop ONLY if the conservative precedent is contested. This is W2 substance — the W2 chunk investigator owns the decision; I flag it here so the consolidator does not double-count or miss it. Cite: `session-107-housekeeping.md:10` + `:75`; W2 gate IDs at `:4`.
- **EVOI tier of the W1 FAIL's parent row (informational, no routing owed).** The §VII.CB discharge is the S107 "1 blocking discharge CF (CAN FAIL)" per `sessions/evoi-framework.md:7` (S107 re-stamp); §VII.CB's parent observational adjacency is the §VII.AF.1/§VII.AU cross-pillar-bridge calibration corpus (Tier-2/Tier-3 band). The FAIL is already EVOI-table-consistent (the row was pre-registered CAN-FAIL); no NEW high-leverage open item is created that is ABSENT from §1–§4 — the partial-sum↔ζ-sum structural question is captured by `CF-S108-VIICB-MAGNITUDE-REMEDIATION` and (if S-1 promotes it) a ζ-native re-anchor CF. No routing INTO the EVOI table owed from this chunk beyond the S108 CF already mirrored in the WP.

## Carry-forwards (route to WP CF, NOT this schedule)

- `CF-S108-VIICB-MAGNITUDE-REMEDIATION` **[Q-other]** — ALREADY in the W1 WP `## Carry-Forward Computations` (4-field spec) and cross-referenced in `session-107-housekeeping.md:46`. Re-test the §VII.CB magnitude channel binding under (a) an alternative DST-T-3 lift reaching the type-IV core OR (b) a higher-L mesh {12,14,16}, AND decide whether the partial-sum↔ζ-sum gap is structural via a Richardson/Abel reconstruction of the ζ-regularized a₂ from the truncated moment. This is a solo compute follow-up with a pre-registered threshold + machinery pin (`Investigating-Workshops.md` "is NOT" item 1) — it belongs in `/rclab-plan` via the WP CF, NOT in the workshop schedule. Listed here for completeness only; it is NOT a workshop. (If Slot-1 S-1 lands a ζ-native re-anchor, it SHARPENS this CF; the sharpened spec still routes to `/rclab-plan`, not here.)

## Wave-by-wave digest

**Wave 1 (1 gate) — `S107-VIICB-MAGNITUDE-CONVERGENCE-ANCHOR`: FAIL (robust, direction-neutral).** Magnitude channel `M(L)=Tr_{M₂(ℂ)}(P_a₂·T^{(IV)})|_L` measured against the binding `L⁻³` Level-2 envelope on the EXISTING §VII.CB bridge. `res(L=10)=2.941453e-01` (~294× above the 1e-3 envelope, PRIMARY FALSE); `alpha_fit=−0.954042` (decreasing — sign correct — but `|alpha|=0.954<2.0`, far below the [2,4] FLOWING band, SECONDARY FALSE); composite FAIL = PRIMARY-FALSE ∧ SECONDARY-FALSE. Lift-robust: all three dictionaries tested (D1 exterior-only res=0.294/|α|=0.954; D2 core-only res=1.885/|α|=0.241; D3 band-centered res=2.207/|α|=0.016, D2/D3 even DIVERGE) FAIL both sub-criteria, so this is a genuine FAIL, not a lift-dependent INFO/PRE-REG-INC. C_1 sign=−1 (`delta_L10=M(L=10)−g_M=−816.60<0`; §VII.AF.1-negative fork; diagnostic, NOT chained; 50/50 dual-prior unmoved). All four Input-SHA pins re-verified at runtime (no drift). Diagnosed structural reason: partial-sum (Z(L)≈383–431, Richardson ~632) ↔ ζ-sum (`g_M=2776.165389`) gap — the ζ-regularized a₂ lives in the analytic-continuation tail the L≤12 partial sum cannot reach at the `L⁻³` rate. §VII.CB Level-3 row STAYS HELD `NOT-SATISFIED-PENDING-MAGNITUDE-CONVERGENCE-ANCHOR`; theorem-STRUCTURE (STAGE-3-PERMANENT, S106) + Level-1 identity UNAFFECTED; §VII.AU's finite-L under-performance confirmed a GENERIC (d=4,s=3) phenomenon reaching §VII.CB. Routed to `CF-S108-VIICB-MAGNITUDE-REMEDIATION`.

**Candidate counts (this chunk)**: Slot 1 = 1 · Slot 2 = 0.
