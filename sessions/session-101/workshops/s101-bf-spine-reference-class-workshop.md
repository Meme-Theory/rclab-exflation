# Session 101 Workshop: phonon-first x mack

**Date**: 2026-06-09
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Agents**: phonon-first (phonon-first-cosmologist, Agent A — opener), mack (mack-cosmic-bridge, Agent B — responder + final turn)
**Seating note**: the schedule's verdict routing requires mack-cosmic-bridge — sole writer of the §7 falsifier/observable surface per `feedback_mack-bridge-role.md` — to hold the FINAL turn (R2 Turn B) and effect the verdict in-session. Agent order is seated accordingly (phonon-first opens; mack closes).
**Source Documents**:
- `cold-read-s101/02-referee-report-cold-read.md`
- `computations/session-101/s101_gate_verdicts.txt`
- `sessions/permanent-results-registry.md`

**Focus Topics**:
1. (a) Is the random-geometry null or the ΛCDM+ν incumbent the decision-relevant reference class for an EXTERNAL evidence statement (vs an INTERNAL against-random-geometry diagnostic)? Canonical anchors (knowledge-MCP-verified): `BF_spine_full = 10^3.30103 = 2000.0` [DECISIVE band >100] (S98-W4, `b_spine_struct = 3.3010299956639813`, no discount, handles independent).
2. (b) Does the S98 Corr=0 cross-pipeline result (S98-W4-4-OQ3-COVARIANCE PASS, audit 0814c57f: 4 spine factors {m_H, normal ordering, σ/m=0, c_s²=0} pipeline-INDEPENDENT, max|Corr|=0, rank-2 dagger LICENSED) establish theory-level independence, or ONLY pipeline-systematics independence (referee M4 distinction)? Note S96-HYG-JOINT-EVIDENCE-D3-RESTRICT PASS already records: Wronskian licenses ALGEBRAIC not STATISTICAL independence; joint-BF scoped to the zero-param spine, NO borrow; W7-7a FAIL Corr(a0,a2)=+1.0000 is WHY borrowed-H Bayes factors are not multiplied.
3. (c) Should the m_H factor stay in the spine given referee M3 — band 127.5–131.8 GeV vs PDG 125.25±0.17 (measured value OUTSIDE band) — and S101-M0-BCS-SCREENING INFO (audit 1a1eff66: M0 inherits the Higgs over-prediction linearly, +5.356% to +7.114%, delta_consistent=False, regime MARGINAL)?
4. (d) VERDICT trichotomy: keep BF_spine=2000 DECISIVE as-is (internal-only, with explicit reference-class annotation) / report a second BF against ΛCDM+ν alongside (pre-registered 4-field CF) / down-tag (m_H-spine-factor down-tag). Constraints: NOT a session-aggregate metric (`feedback_reporting-framing.md` — single registered model-class statistic; the verdict governs citation); a zero-knob construction's prior predictive range IS evidence per `evoi-prioritization.md` Evidence Weighting (phonon-first's steelman); the reference-class / what-counts-as-independent-evidence distinction is mack's. Composes with the §7 three-register split (referee credits it as "exactly right").

---

## Round 1 — phonon-first: Opening Analysis

### P1: The zero-knob steelman — what BF_spine=2000 against random geometry actually establishes (question a, framework side)

**Key finding: BF_spine = 2000 DECISIVE is a REAL, correctly-computed statistic, but its reference class is a structural property of the statistic, not a free choice — and the two reference classes (random-geometry null vs ΛCDM+ν incumbent) measure DIFFERENT things. Against random geometry the statistic is valid and the DECISIVE tier is earned. Against the ΛCDM+ν incumbent, an exact per-factor decomposition (Sage, this session) shows that 45% of the log-evidence (the {σ/m=0, c_s²=0} pair) carries ZERO discrimination — ΛCDM predicts those values too — and the headline deflates by construction. Both numbers are real; they answer different questions. The error would be citing the random-geometry number to an audience asking the incumbent question.**

**The steelman at full strength (my charge, `evoi-prioritization.md §"Evidence Weighting"`).** A zero-knob construction's prior predictive range IS evidence. The four spine factors {m_H, normal ν-ordering, σ/m=0, c_s²=0} are not fitted — they are forced outputs of the spectral triple `(A_K, H_K, D_K)` with no continuous knob to turn. The substrate-first chain (S98 plan §W4-1 `substrate_framing`): m_H ← transverse fiber-embedding |S|² mode → KK-threshold a₄-moment; normal ordering ← seesaw/Dirac mass texture in the M_3(ℂ) summand; σ/m=0 ← the Leggett-channel GGE quasiparticle is CPT-neutral, non-annihilating (an occupation-gap property, not a dark-matter fit); c_s²=0 ← the same Leggett/BdG inter-band mode carries no acoustic pressure. **A random Dirac operator on a random compact geometry has no reason to land any one of these on its observed value, let alone all four.** That is the content of the random-geometry null, and BF_spine = 2000 is the correct measure of it.

**What "random geometry" actually tests (question a, the honest specification).** The null is: *draw a Dirac operator from the space of admissible finite spectral triples with no constraint tuned to match data; what is the probability it reproduces these four observables?* This is a model-SELECTION statistic — the canonical null for a construction that claims its observables are forced rather than chosen. It tests whether the framework's geometry is special among geometries. It is exactly the right null for an INTERNAL diagnostic: "is the substrate doing real work, or is any geometry as good?" Against that question, DECISIVE (>100) is the honest verdict and the dual-SHA chain stands: `S98-W4-4-OQ3-COVARIANCE` PASS (`0814c57f…`), `BF_spine_full = 10^3.30103 = 2000.0` (canonical `b_spine_struct = 3.3010299956639813`).

**Now the referee's M4 head-on — what ΛCDM+ν predicts for each factor (Sage decomposition, this session; substitution chain explicit per `math-scripts.md`).** I decomposed BF_spine = 2000 exactly into its four log10 contributions from `s97_d3_bf.npz`:

```
b_mH    = 3/2   = 1.50000   → BF_mH    = 10^1.5  = 31.62   (45.44% of b_spine)
b_sigma = 1     = 1.00000   → BF_sigma = 10^1.0  = 10.00   (30.29%)
b_cs2   = 1/2   = 0.50000   → BF_cs2   = 10^0.5  =  3.16   (15.15%)
b_nu    = log10(2) = 0.30103 → BF_nu   = 10^.301 =  2.00   ( 9.12%)
  Σ b_i = 3.3010299956639813 = b_spine_struct  ✓  → 10^Σ = 2000.0  (Sage-exact)
```

Now substitute each factor's ΛCDM+ν expectation:
- **σ/m = 0**: ΛCDM's cold dark matter is collisionless by construction — σ/m = 0 is *also* the incumbent's value. **Discrimination = 0.** This factor's 1.0 log10 (factor 10) evaporates against the incumbent.
- **c_s² = 0**: ΛCDM's CDM is pressureless — c_s² = 0 is *also* the incumbent's value. **Discrimination = 0.** This factor's 0.5 log10 (factor 3.16) evaporates.
- **normal ordering**: ΛCDM is agnostic on the ν mass ordering (it is an external particle-physics input either way). A ~2-outcome observable; the framework predicting "Normal" is at most a bounded factor 2 (b_nu = log10 2, EXACTLY — this is why b_nu is literally log10 2). It discriminates weakly against *no-prediction*, but ΛCDM+ν makes no competing prediction, so the factor 2 is real but small.
- **m_H**: ΛCDM does not predict the Higgs mass at all. **This is the ONLY factor with non-trivial discrimination against the incumbent** — and it is exactly the factor the referee's M3 attacks for band-missing (P3 below prices that).

**The exact deflation (Sage).** Stripping the two zero-discrimination factors: `b_spine − b_sigma − b_cs2 = 3.30103 − 1.5 = 1.80103 → BF = 63.25`. (Striking coincidence worth flagging to mack: this equals the {a₀,a₂}-dagger value `BF_spine_dagger`, by the arithmetic accident that the dagger discount delta_logBF_dagger = 1.5 = b_sigma + b_cs2 — two structurally-unrelated 1.5's. NOT the same mechanism; mack should ensure the §7 surface does not conflate them.) If M3 then forces m_H out, the residual against ΛCDM+ν is `b_nu = log10(2) → BF = 2.0` — the ordering alone, an "anecdotal" Jeffreys tier.

**The structural implication (cross-pillar pattern).** This is the same INTERNAL-vs-EXTERNAL observable distinction I have flagged across pillars: a substrate-IS quantity (the four forced predictions) carries one evidential weight against the *space of geometries* and a different weight against a *specific rival theory* that happens to share three of the four predictions. The reference class is not a knob — it is determined by WHICH question the citation answers. BF_spine = 2000 answers "is this geometry special?" (YES, decisively). It does NOT answer "does this geometry beat ΛCDM+ν?" — against that question only m_H discriminates, and m_H band-misses. **Both are true simultaneously.** The statistic is not wrong; the citation must name its null.

**Why this does NOT collapse to "the framework has no evidence."** The random-geometry result is genuine and the DECISIVE tier is earned for the internal question — it is the strongest single argument that the spectral triple is not arbitrary. And the per-factor decomposition itself is a framework STRENGTH: the framework can say exactly which of its predictions are shared with the incumbent (σ/m=0, c_s²=0 — convergent, not discriminating) and which are novel (m_H — discriminating, currently strained). A theory that can decompose its own evidence into convergent and discriminating parts is more honest than one that reports a single conflated number. The honest headline is "DECISIVE against random geometry; discriminates against ΛCDM+ν only through m_H (currently a ~2–5% band-miss)."

**Questions for mack.** (Q-P1a) Is the decision-relevant reference class set by the AUDIENCE of the citation (a referee evaluating the framework against the incumbent ⇒ ΛCDM+ν null) or by the CONSTRUCTION the statistic was built for (a zero-knob model-selection statistic ⇒ random-geometry null)? My read: the §7 falsifier surface is an EXTERNAL-facing document, so its default audience is the incumbent-comparison reader — which argues for reporting the second BF alongside. (Q-P1b) Do you agree that σ/m=0 and c_s²=0 should be tagged on the §7 surface as CONVERGENT-WITH-ΛCDM (zero incumbent discrimination) rather than as evidence FOR the framework over ΛCDM — i.e., the surface carries TWO columns, "vs random geometry" and "vs ΛCDM+ν," with the convergent factors visibly contributing to the first and not the second?

### P2: Pipeline vs theory-level independence — what S98 Corr=0 and the Wronskian ALGEBRAIC license do and do not cover (question b)

**Key finding: the referee's M4 distinction is CORRECT and the ledger already records it — there are THREE independence layers, and the framework's own gates establish only the first two. S98 Corr=0 establishes PIPELINE-SYSTEMATICS independence (different derivation maps from the same spectrum, uncorrelated residuals). The Wronskian establishes ALGEBRAIC independence (the four observables are not algebraic functions of one another). NEITHER establishes THEORY-LEVEL independence — and this session's W-2 workshop (volovik × phonon-first, just closed) found exactly the shared theory-level handle the referee suspects: all four factors are computed on the SAME spectral triple with the SAME imported scale M_KK, and that import is rank-1 (a single hidden dimensional parameter). The honest statement is that the four factors share ONE theory-level handle (M_KK), so multiplying their Bayes factors is licensed ALGEBRAICALLY but carries a theory-level shared-systematic the Corr=0 result cannot see.**

**The three layers, named precisely (this is the ledger talking, not me inventing a distinction).** The framework already partitions independence into three strata — the referee's M4 is a real gap but a SMALLER one than M4 implies, because two of the three layers are genuinely closed:

1. **ALGEBRAIC independence (Wronskian W₂E).** `S96-HYG-JOINT-EVIDENCE-D3-RESTRICT` PASS records it verbatim: *"Wronskian licenses ALGEBRAIC NOT STATISTICAL indep."* This says: m_H, the ν-ordering, σ/m, and c_s² are not algebraic functions of one another — you cannot derive one from the others by a closed-form identity. This is real and it is the FLOOR for any joint-evidence statement (if they were algebraically dependent, multiplying would be pure double-counting). PASS.

2. **PIPELINE-SYSTEMATICS independence (S98 Corr=0).** `S98-W4-4-OQ3-COVARIANCE` PASS (`0814c57f…`): `max|Corr|=0.0000`, rank-2 dagger LICENSED, `pipeline_independent=True`. The S98 plan §W4-1 method is explicit about what this measures: the cross-pipeline residual correlation between the four DERIVATION MAPS (fiber-embedding mode → m_H; mass-texture → ordering; occupation-gap → σ/m, c_s²). It demonstrates these maps do not share a *systematic* — a perturbation in one pipeline's residual does not co-move another's. PASS. **But the referee's M4 reads exactly right here**: "cross-pipeline covariance = identity among four observables produced by the same model family from the same spectrum cache" establishes *internal pipeline independence, not independence of theory-level systematics.* The four pipelines are uncorrelated *as maps*; they are not independent *as theories*, because they are the same theory.

3. **THEORY-LEVEL independence (NOT established — and this session found the shared handle).** This is the layer M4 attacks and it is genuinely open. The decisive new evidence is **this session's W-2 normalization-non-universality workshop** (`s101-normalization-non-universality-workshop.md`, volovik × phonon-first, CLOSED). Its central finding — which I co-derived and conceded with the dimensional bookkeeping shown (Sage, my own dispatch in that workshop) — is that **the substrate determines all dimensionless/conformal content but imports ONE dimensional scale, M_KK, and that import is rank-1**: `gamma_unit = ℏ/(M_KK·c²)` in seconds, where M_KK-in-GeV is an externally-calibrated input. Every factor that carries an absolute scale rides on this single import. The matter-sector confirmation landed THIS session: `S101-M0-BCS-SCREENING` INFO (audit `1a1eff66`, `convention=FIRST-POWER-MH-LEVEL-TRANSFER-PRIMARY`) — the absolute fermion normalization M₀ inherits the m_H scale at first power. And the smoking gun is `Corr(a₀,a₂) = +1.0000` (`S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE` FAIL, band>0.5): the borrowed-H residuals co-shift PERFECTLY, which the framework correctly reads as *the signature of a single shared hidden parameter*.

**The precise relation between the layers (substitution chain for the "what is licensed" claim).** The framework's own discipline (`S96-HYG-JOINT-EVIDENCE-D3-RESTRICT`) already drew the right boundary: *"joint-BF scoped to zero-param spine (m_H, mass-ordering, σ/m=0, c_s²=0), NO borrow."* The logic:
- The {a₀, a₂} dagger pair has `Corr = +1` (a SINGLE shared handle, the borrowed FRW H normalization). Multiplying Bayes factors built on co-shifting residuals double-counts → the framework correctly REFUSES to multiply borrowed-H factors (W7-7a FAIL is the reason, not a defect). This is exactly right and the referee credits it ("the refusal to multiply borrowed-H Bayes factors after measuring Corr = +1 are exactly right").
- The four SPINE factors have `max|Corr| = 0` (S98) AND are algebraically independent (Wronskian) → multiplying their log-BFs is licensed *at the algebraic and pipeline-systematics layers*.
- **But the spine factors and the dagger pair share the SAME root: the single M_KK import.** σ/m=0 and c_s²=0 are dimensionless (W-2's "protected/computable" content — they carry NO M_KK scale, so they are genuinely theory-level-clean). The ν-ordering is dimensionless (an ordering, not a scale). **m_H is the ONE spine factor that carries the imported M_KK scale** (m_H = 131.8 GeV ← KK-threshold a₄-moment; and M₀←m_H first-power, S101). So three of the four spine factors are theory-level-clean (dimensionless, no shared scale), and the fourth (m_H) is precisely the one that rides the rank-1 import.

**Structural implication — the layers map cleanly onto the W-2 protected/unprotected split.** W-2's verdict (volovik's R4-trigger steelman, my partial concession): the substrate determines the conformal class (protected, dimensionless, computable) but not the metric normalization (unprotected, dimensional, imported — because N₃=0, BDI class, no Fermi-point protection). Apply that to the spine:
- **σ/m=0, c_s²=0, ν-ordering** = dimensionless/conformal content = PROTECTED = theory-level-clean. Their algebraic + pipeline independence (layers 1, 2) genuinely IS their independence — there is no dimensional handle to share. **For these three, S98 Corr=0 DOES establish theory-level independence** (modulo "same spectral triple," which is a different objection — see Q-P2b).
- **m_H** = carries the dimensional M_KK import = the one factor with an UNPROTECTED scale. For m_H, the M4 objection bites: its evidential weight against ΛCDM is entangled with the calibration of M_KK, which is shared with the gravitational sector (`Corr(a₀,a₂)=+1`). 

So the M4 distinction is real but it localizes to ONE factor (m_H), not all four. The honest joint statement: **three spine factors are theory-level-independent (dimensionless, protected); one (m_H) carries the rank-1 M_KK import and its incumbent-discrimination is entangled with the gravitational normalization.** This is a sharper statement than either "all four independent" (overclaim) or M4's implied "none independent" (the four are not all entangled — only the dimensional one is).

**Questions for mack.** (Q-P2a) Given the W-2 rank-1 finding, do you accept the THREE-LAYER statement on the §7 surface: algebraic ✓ (Wronskian), pipeline-systematics ✓ (S98 Corr=0), theory-level ✓ for the three dimensionless factors / OPEN for m_H (shares the M_KK import, `Corr(a₀,a₂)=+1`)? (Q-P2b) The deeper M4 sub-objection is "same spectral triple = same theory = not independent theories at all." But three of the four factors are dimensionless invariants of that one triple — independent FUNCTIONS of one geometric object. Is "independent functions of one substrate" the correct independence notion for a zero-knob construction (where there is BY DESIGN only one substrate), or does M4 demand independence-of-substrate that a single-geometry theory can never supply? This is the boundary between a fair and an impossible standard, and it is your call as the falsifier-surface owner.

### P3: The m_H factor under M3 + S101-M0-BCS-SCREENING — keep, drop, or re-derive (question c)

**Key finding: m_H is the load-bearing factor for the DECISIVE tier AND the one factor whose substrate-IS value currently band-misses PDG. Sage (this session): dropping m_H takes BF_spine from 2000 (DECISIVE >100) to 63.25 (STRONG 10–100) — it is NOT a peripheral factor, it carries 45.44% of the log-evidence and it alone carries the DECISIVE label. It is ALSO the only spine factor that (i) discriminates against ΛCDM+ν (P1) AND (ii) carries the rank-1 M_KK import (P2) AND (iii) misses the one 0.1%-precision particle datum the framework confronts. My disposition: KEEP m_H in the spine but RE-TAG it CONVERGENT-STRAINED — keep it for the random-geometry null (where the ~2% band-hit from zero knobs is genuinely improbable), flag it as the single discriminator against ΛCDM+ν, and price the band-miss + M0 propagation explicitly so it cannot anchor an unqualified DECISIVE claim. DROP is wrong (it discards the framework's one genuine particle-datum confrontation); KEEP-AS-IS is wrong (it lets a band-miss anchor DECISIVE).**

**The band-miss, priced exactly (Sage; substitution chain per `math-scripts.md`).** Framework band 127.5–131.8 GeV vs PDG 125.25 ± 0.17:
- Nearest edge (127.5) is **+1.796%** above PDG central = **13.24σ** in PDG units.
- Upper edge (131.8, the KK-threshold value m_H = 131.8) is **+5.230%** above = **38.53σ** in PDG units.
- The measured value lies OUTSIDE the band; the referee's M3 is factually correct.

The σ-framing (13–38σ) is the WRONG comparison and I will not let it stand as the headline — the framework band is a ~2% theory-budget object (KK-threshold + Aitken extrapolation across routes), NOT a measurement with a 0.17 GeV error bar. Comparing a 2%-wide theory band to a 0.1%-precision datum in σ-units of the DATUM is a category error that inflates the miss. The honest metric is the **percentage miss**: +1.80% (best edge) to +5.23% (worst edge). For a zero-knob construction predicting a mass scale from pure geometry across a wide prior range, landing within ~2% is informative; landing OUTSIDE by ~2% is a real strain. Both are true: informative for the random-geometry null, strained against the precision datum.

**The M0 propagation, priced honestly (S101-M0-BCS-SCREENING INFO, audit `1a1eff66`, this session).** The over-prediction is not isolated — it propagates linearly into the absolute fermion normalization. From the verdict line: `convention=FIRST-POWER-MH-LEVEL-TRANSFER-PRIMARY`, M0 inherits the Higgs over-prediction at first power: **tree +5.356%, KK +7.114%** pre-screening, `delta_solve=0.2672`, `delta_consistent=False`, `regime=MARGINAL`. The BCS screening (`a_4^{Pauli-Villars}` BdG anomalous self-energy) shrinks both: KK 7.114%→1.642%, tree 5.356%→0% — but the tree-anchor reaching 0% is `r_tree_scr=0 BY CALIBRATION` (m_H_obs used once on the tree anchor; the verdict's `calibration_disclosure` row is explicit). The discriminating content is the KK-anchor screened residual `-11/670 = -1.642%` and root existence/uniqueness, NOT the calibrated-to-zero tree leg. So the honest read: **screening reduces but does not eliminate the inheritance; the residual is ~1.6% (KK) with the tree leg calibrated, regime MARGINAL, delta_consistent=False.** The Higgs over-prediction is a real strain that reaches into the fermion sector — exactly as the referee's M3 says.

**Why DROP is wrong.** m_H is the framework's single confrontation with a high-precision particle datum, and it is the only spine factor that discriminates against the incumbent (P1). Dropping it:
- discards the only ΛCDM-discriminating evidence (leaving the ordering's factor-2 as the entire incumbent case — anecdotal);
- hides the framework's most testable prediction behind the convergent (non-discriminating) factors;
- is the exact move the referee warns against in M2 (spending interpretive DOF off-ledger) — silently removing the factor that strains is not honest, it is convention-shopping on the evidence set.

The framework keeps falsified predictions on the books (the referee credits this: "wₐ BROKEN 3.43σ, registered anyway"). m_H strained is the same discipline — keep it, price it, do not bury it.

**Why KEEP-AS-IS is wrong.** Citing BF_spine = 2000 DECISIVE with m_H carrying 45% of the weight, while m_H band-misses the precision datum and propagates a MARGINAL inconsistency through M0, lets a strained factor anchor a DECISIVE claim. Against the random-geometry null this is defensible (the ~2% hit IS improbable from zero knobs); against ΛCDM+ν it is the referee's exact objection ("the one place the framework meets a 0.1%-precision particle datum, it misses by ~2–5%... it cannot anchor the evidence case").

**My disposition: KEEP + RE-TAG CONVERGENT-STRAINED + RE-DERIVE-FORWARD.** Concretely:
1. **Keep m_H in the random-geometry-null spine** (BF=2000 DECISIVE stays valid for the internal "is this geometry special?" question — m_H's ~2% hit from zero knobs is real evidence the geometry is not arbitrary).
2. **Tag m_H STRAINED on the §7 surface**: band 127.5–131.8 GeV, PDG 125.25±0.17 OUTSIDE band, miss +1.80% to +5.23%, M0 inheritance MARGINAL (S101 `1a1eff66`). This is the interpretive-DOF ledger entry the referee's M2 demands.
3. **Forward re-derivation gate (carry-forward to S102, 4-field)**: the route multiplicity (KK-L5=127.5 / KK-threshold=131.8 / zeta=138.5 excluded / μ_BC=188 ACCOMMODATION) is itself the problem — m_H is not pinned to a single substrate value. The honest forward move is to pin the route (the referee's M3 remedy: "commit to the √x functional" analog for m_H — which route is the FORCED one?) BEFORE citing m_H's BF externally. Until that route-selection closes, m_H's b_mH = 1.5 is a CONVENTION-SENSITIVE contribution (the `SECONDARY: r_KK_scr_RG=-0.0046, CONVENTION-SENSITIVE` flag in the S101 verdict line confirms route sensitivity).

**Structural implication.** m_H sits at the intersection of all three referee objections: it is the discriminating factor (M4), it carries the imported scale (the W-2 rank-1 finding feeding M0, M3), and it band-misses (M3). That is not a coincidence — it is the SAME structural fact in three views: **the one spine factor that carries a dimensional scale is the one that (a) can discriminate against a dimensional rival, (b) inherits the import's calibration uncertainty, and (c) confronts a precision datum.** The three dimensionless factors (σ/m, c_s², ordering) are clean precisely because they carry no scale — and they don't discriminate against ΛCDM precisely for the same reason. **Discrimination and strain are the same coin: only the dimensional factor can do either.**

**Questions for mack.** (Q-P3a) Does the §7 falsifier surface keep m_H as a co-equal spine factor (with the STRAINED tag + the dual-BF columns from P1), or does the band-miss + MARGINAL M0 propagation warrant down-tagging its b_mH = 1.5 contribution pending route-selection? My read: keep it (DROP discards the discriminator), tag it STRAINED, and gate any EXTERNAL DECISIVE citation on the route-selection closing — but the citation-class call is yours. (Q-P3b) The route multiplicity (127.5 / 131.8 / 138.5-excluded / 188-ACCOMMODATION) means b_mH = 1.5 is itself route-dependent. Should the S102 re-derivation pre-register the FORCED route (which functional/extrapolation is geometrically mandated, not chosen-after-the-fact per M2's post-hoc warning) as a hard prerequisite before m_H re-enters any external evidence statement?

### P4: Cross-Cutting Observations (verdict posture on d)

**Verdict posture: REPORT A SECOND BF AGAINST ΛCDM+ν ALONGSIDE the random-geometry BF (trichotomy branch 2), with the m_H factor KEPT-but-STRAINED (P3) and the three independence layers explicitly tagged (P2). This is the only branch that is structurally honest, because the Sage decomposition (P1) proves the two reference classes measure DIFFERENT things and BOTH numbers are real. Keep-as-is (branch 1) hides the incumbent-comparison the §7 surface's external audience actually asks. Down-tag (branch 3) discards a valid statistic. The dual-report says exactly what is true: DECISIVE against the space of geometries; discriminates against ΛCDM+ν only through m_H, which currently band-misses.**

**Why the dual-BF branch, not keep-as-is.** The keep-as-is branch (BF=2000 DECISIVE, internal-only, with a reference-class annotation) is *not wrong* — the statistic is correctly computed and the random-geometry null is the right null for the internal question. But the §7 falsifier surface is an EXTERNAL-facing document (the referee read it as a submitted manuscript). Its default reader asks the incumbent question: "does this beat ΛCDM+ν?" A single DECISIVE number annotated as "vs random geometry" invites exactly the misread M4 catches — that DECISIVE is a claim against the incumbent. The annotation alone is too weak; the reader's eye lands on "2000 DECISIVE," not on the footnote. The structural fix is to put BOTH numbers in the open, as two columns:

| Reference class | What it tests | BF | Tier | Driver |
|:---|:---|:---|:---|:---|
| Random geometry (internal) | Is the substrate special among geometries? | 2000 | DECISIVE | all 4 factors (m_H 45%, σ/m 30%, c_s² 15%, ord 9%) |
| ΛCDM+ν (incumbent) | Does the substrate beat the standard model? | ≤ 31.6 via m_H alone; **m_H band-misses** ⇒ effectively the ordering's factor ~2 | ANECDOTAL–weak | only m_H discriminates, and it strains |

This is the referee's M4 remedy verbatim ("report BF against ΛCDM+ν alongside the random-geometry null"), and it is also `evoi-prioritization.md`-compliant: the random-geometry BF (the zero-knob prior-predictive-range evidence) is preserved at full weight; it is simply NOT misrepresented as the incumbent comparison.

**Why not down-tag (branch 3).** Down-tagging BF_spine = 2000 itself would be wrong: the statistic is real, correctly computed (Sage-exact `b_spine_struct = 3.3010299956639813`), and the random-geometry null is a legitimate model-selection null. Down-tagging a valid statistic because it is *cited against the wrong audience* confuses the statistic with its citation. The fix is at the citation layer (name the null, report both), not the statistic layer. The ONLY down-tag I endorse is the narrower m_H one (P3): tag m_H STRAINED and gate its EXTERNAL DECISIVE-anchoring on route-selection — which is a factor-level strain tag, not a spine-level down-tag.

**Cross-cutting structural observation (the pattern that unifies all four questions).** Reading the four spine factors through the W-2 protected/unprotected lens (volovik's N₃=0 corollary, which I co-derived this session) makes every referee objection one structural fact:

- **The three dimensionless factors {σ/m=0, c_s²=0, normal ordering}** are PROTECTED (conformal-class content, no imported scale). Consequence: they are theory-level-clean (P2 ✓), they carry no calibration uncertainty (P3 clean), AND they do not discriminate against ΛCDM+ν (P1 — ΛCDM shares the dimensionless values). Protected = clean = non-discriminating.
- **The one dimensional factor m_H** is UNPROTECTED (carries the rank-1 M_KK import). Consequence: it is the discriminator against ΛCDM (P1), it carries the import's calibration entanglement (P2 OPEN, `Corr(a₀,a₂)=+1`), it propagates into M0 (P3, S101 `1a1eff66`), AND it band-misses (P3). Unprotected = entangled = discriminating-but-strained.

**This is the deep finding: discrimination and strain are the same coin, and the coin is dimensionality.** Only a factor carrying a dimensional scale can discriminate against a dimensional rival theory — and that same scale is the one the substrate imports rather than derives (W-2, N₃=0). So the framework's evidence against ΛCDM+ν is *structurally* concentrated in its single imported-scale factor, which is *structurally* the one that strains. This is not a flaw to hide; it is the precise shape of a zero-knob emergent construction's evidential reach: **it convergently reproduces the incumbent's dimensionless content (correctly, with no knobs) and it can only out-discriminate the incumbent through the dimensional scale it does not yet derive cleanly.** The honest §7 surface states exactly this.

**Verdict-table positions (my R1 commitments).**
- **Topic 1 (reference class, question a)**: Both nulls are valid and measure different things; random-geometry = internal "special-among-geometries" (DECISIVE, real); ΛCDM+ν = external "beats-incumbent" (only m_H discriminates). The §7 surface's external audience makes the incumbent null decision-relevant → report both. (P1.)
- **Topic 2 (independence, question b)**: THREE layers — algebraic ✓ (Wronskian), pipeline-systematics ✓ (S98 Corr=0), theory-level ✓ for the three dimensionless factors / OPEN for m_H (rank-1 M_KK import, this session's W-2). S98 Corr=0 establishes theory-level independence ONLY for the protected (dimensionless) factors. (P2.)
- **Topic 3 (m_H, question c)**: KEEP (load-bearing for DECISIVE; the only discriminator; framework keeps strained predictions on the books) + STRAIN-TAG (band-miss +1.80–5.23%, M0 MARGINAL) + forward route-selection gate before external DECISIVE-anchoring. (P3.)
- **Topic 4 (verdict, question d)**: **Dual-BF (branch 2)** + m_H strain-tag (the narrow P3 down-tag, not a spine-level one) + three-layer independence tag. Pre-register the ΛCDM+ν BF as a 4-field carry-forward to S102. (This section.)

**Questions for mack (verdict-level, your domain on the falsifier surface).** (Q-P4a) Do you accept the two-column §7 surface (random-geometry BF=2000 DECISIVE | ΛCDM+ν BF driven by m_H alone, currently strained) as the structurally-honest external statement, or do you read the §7 three-register split (which the referee credits as "exactly right") as ALREADY carrying the incumbent comparison elsewhere — making a second BF column redundant rather than additive? (Q-P4b) The ΛCDM+ν BF requires a per-factor incumbent-discrimination assignment (σ/m=0: 0; c_s²=0: 0; ordering: ~factor 2; m_H: discriminating-but-strained). That assignment is a falsifier-surface judgment in your sole-writer domain. Will you pre-register it as an S102 gate (`BF_spine_vs_LCDM` with the four per-factor discrimination values pinned BEFORE compute, per M2's anti-post-hoc discipline), so the incumbent BF is not narrativized after the fact? (Q-P4c) Is the m_H strain-tag + route-selection-gate sufficient to keep the random-geometry DECISIVE claim citable internally (e.g., in the capstone §7.1 as "DECISIVE against random geometry"), or does any appearance of "DECISIVE" on an external surface require the dual-column context inline (not in a footnote)?

---

## Round 1 — mack: Response & Cross-Synthesis

### Part 1: Response to phonon-first's Sections

#### Re: P1 — The zero-knob steelman

**AGREE — the decomposition is Sage-exact and the two-reference-class structural claim is correct.** I re-derived your decomposition independently before relying on it (`math-scripts.md §"Double-Check Logic Before Compute"`; Sage QQ, this turn): `b_spine_sum = log(2)/log(10) + 3 = 3.3010299956639813` EXACT → `10^Σ = 2000.0`; per-factor shares m_H 45.4404%, σ/m 30.2936%, c_s² 15.1468%, ν 9.1193% — your arithmetic reproduces to every digit. Drop-{σ,c_s²} → `63.245553`, and `b_sigma + b_cs2 = 3/2` EXACTLY. Your central structural claim — *the reference class is not a knob, it is determined by which question the citation answers* — is right, and it is the cleanest articulation of the M4 issue I have seen. BF_spine=2000 against random geometry is a correctly-computed, legitimate model-SELECTION statistic. I do not dispute the statistic.

**MISSED — what the citation TOKEN "DECISIVE" imports, and that the incumbent residual crosses a TIER boundary, not just deflates.** Your P1 frames the deflation as a magnitude change (2000 → 63.25 → 2.0). The observational-cosmology view adds the part your domain doesn't surface: **"DECISIVE" is not a free-text adjective — it is a Jeffreys-scale tier name with a pinned numerical boundary, and an external reader (referee, CMB analyst, DESI-DR3-adjacent) reads it as that tier against whatever null they assume.** I computed the tier map exactly (Sage, both conventions in field circulation):

| Quantity | log10 K (Jeffreys) | 2 ln K (Kass–Raftery) | Jeffreys tier | KR tier |
|:---|---:|---:|:---|:---|
| BF_spine = 2000 | 3.3010 | 15.20 | **DECISIVE** (>2) | very strong (>10) |
| BF_dagger = 63.25 | 1.8010 | 8.29 | very strong | strong |
| m_H alone = 31.62 | 1.5000 | 6.91 | very strong | strong |
| σ/m alone = 10 | 1.0000 | 4.61 | strong | positive |
| c_s² alone = 3.16 | 0.5000 | 2.30 | substantial | positive |
| ν-ordering = 2.0 | 0.3010 | 1.39 | barely-worth-mentioning | bare mention |

The DECISIVE floor is `log10 K = 2.0` (BF = 100). BF_spine=2000 clears it by 1.30 dex. **But every incumbent-relevant residual you derive falls BELOW the DECISIVE floor**: the full incumbent-discriminating factor (m_H alone) is 31.62 → "very strong," 0.20 dex *under* DECISIVE even before the band-miss; with the band-miss the operative incumbent BF collapses toward the ordering's 2.0 → "barely worth mentioning." So the deflation is not "DECISIVE shrinks to a smaller DECISIVE" — it is **DECISIVE → very-strong → anecdotal**, a tier crossing. An external citation of "DECISIVE" unqualified imports the >100 tier; the incumbent comparison never reaches it. That is the precise reason the citation cannot be left to a footnote (sharpens your Q-P1a).

**EMERGES — the cross-pillar INTERNAL/EXTERNAL distinction you flag IS the standard model-comparison-vs-model-selection distinction in cosmology, and it has an established convention.** Your "is this geometry special?" vs "does it beat ΛCDM+ν?" maps exactly onto the field's *evidence ratio against a prior-predictive null* (a goodness-of-construction statistic) vs *Bayes factor against the incumbent model* (a model-comparison statistic). Cosmologists routinely report the latter and almost never the former on a falsifier surface — because the surface's job is to position the framework against the standard model, not against the null space of all theories. This is field-standard practice (the DESI DR2 w₀wₐ papers, the Planck inflation model-comparison tables): the published BF is *always* against the incumbent (ΛCDM), never against "a random dark-energy equation of state." So your read on Q-P1a is correct and it is the convention, not a preference: the §7 surface's default null is ΛCDM+ν. I will answer your Q-P1b in M2 (yes, two columns, with the convergent factors visibly contributing only to the random-geometry column).

#### Re: P2 — Pipeline vs theory-level independence

**AGREE — the three-layer taxonomy is the right structure, and your localization of M4 to one factor is the sharpest available reading.** Algebraic (Wronskian) ✓, pipeline-systematics (S98 `0814c57f` Corr=0) ✓, theory-level ✓-for-three / OPEN-for-m_H is correct, and it is verified against the ledger: `S96-HYG-JOINT-EVIDENCE-D3-RESTRICT` PASS records "Wronskian licenses ALGEBRAIC NOT STATISTICAL indep" and "joint-BF scoped to zero-param spine, NO borrow" (knowledge-MCP confirms the verdict line verbatim). The `Corr(a₀,a₂)=+1.0000` smoking gun (`S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE` FAIL, band>0.5) is real and is the framework's own correct signal of a single shared hidden parameter. The W-2 rank-1 M_KK-import finding is the genuinely new content, and you co-derived it. I accept the three-layer statement for the §7 surface (your Q-P2a: yes).

**DISAGREE (scope, not substance) — "theory-level ✓ for the three dimensionless factors" needs one observational-discipline qualifier before it goes on an external surface.** Pipeline-independence (Corr=0) and algebraic-independence (Wronskian) are *internal* independence notions — they certify that the framework's own derivation maps and closed-form identities don't share a handle. They do NOT certify what an external Bayes-factor referee means by independence, which is **independent likelihoods against the data**: that the four factors' evidence contributions are computed from statistically independent measurements with non-overlapping covariance. Three of your four "theory-level-clean" factors are σ/m=0, c_s²=0, ν-ordering — and against ΛCDM+ν those three contribute **zero incumbent discrimination** (Re:P1, M1). A factor that contributes zero log-evidence against the incumbent is trivially "independent" in the only sense that then matters (it contributes nothing to multiply), so the theory-level-independence ✓ for those three is *vacuously true on the incumbent surface* and load-bearing only on the random-geometry surface. The qualifier the external surface needs: **the theory-level-clean ✓ for {σ/m, c_s², ordering} is a random-geometry-column property; on the ΛCDM+ν column those three carry no evidence, so their independence is not in dispute and not in play.** This is not a correction to your physics — it is the reference-class tagging applied to the independence claim itself.

**MISSED — referee M8 (verification monoculture) bears on the independence claim and your three-layer split does not yet address it.** M8 says: Corr=0 is computed by the same model family running the same pipelines; "shared context produces shared outputs — shared weights do too." Your three layers are all *internal* (the framework certifying its own independence). The referee's deepest independence objection is not "same spectral triple" (your Q-P2b, which I answer below) — it is "same *verifier*." The honest §7 statement must concede that the Corr=0 result is an internal-consistency check, not an external-replication check; M8's remedy (c) is precisely "replace σ-arithmetic against published posteriors with actual likelihood evaluation (CLASS/CAMB + Planck/DESI chains) for the two or three rows that matter." That is an observational-priorities item and it routes to a carry-forward (M3). It does not weaken the three-layer split; it adds a fourth, explicitly-OPEN layer (external-likelihood independence) that the surface should name rather than let M8 catch.

**EMERGES — your Q-P2b ("independent functions of one substrate" vs "independence-of-substrate") has a definite answer from observational practice: the fair standard is independent-functions-of-one-substrate, and demanding independence-of-substrate IS the impossible standard — BUT the fairness only holds against the random-geometry null, and inverts against the incumbent.** Here is the substitution chain. A zero-knob construction has, by design, one substrate; "independent functions of one geometric object" is the correct independence notion *for the model-selection question* (against random geometry), because that question asks whether the one object is special, and four independent functions of it landing on four observed values is genuine evidence it is. M4 demanding independence-of-substrate there would indeed be an impossible standard, and I will not let it stand as the headline. **But** for the model-comparison question (against ΛCDM+ν), the standard is not "independent functions of the substrate" — it is "independent likelihoods against the data," and there the three dimensionless functions contribute nothing (they match the incumbent), so the only function in play is m_H, and m_H is *not* independent of the M_KK calibration shared with the gravitational sector (`Corr(a₀,a₂)=+1`). **So the fairness of your independence notion is itself reference-class-dependent**: fair (and your standard is the right one) against random geometry; against the incumbent the independence question collapses to a single entangled factor. This is the same coin you named in P4 — independence, like discrimination, lives only where the dimensional scale lives. I will put both readings on the surface in M2.

#### Re: P3 — The m_H factor

**AGREE on the disposition (KEEP + STRAIN-TAG + route-gate) and on every number.** I re-derived the band-miss against PDG 125.25±0.17 (Sage, this turn): lower edge 127.5 → +1.796% = 13.24σ_PDG; upper edge 131.8 → +5.230% = 38.53σ_PDG; the M0 screened KK residual −11/670 = −1.642% exact. DROP is wrong (it discards the only incumbent discriminator and the only high-precision particle-datum confrontation — and silently removing the strained factor is the M2 off-ledger-DOF move). KEEP-AS-IS is wrong (it lets a band-miss anchor an external DECISIVE). KEEP+STRAIN-TAG+route-gate is the disposition I will effect. I concur fully and will not re-litigate it.

**AGREE and SHARPEN — your σ-framing-is-a-category-error point is correct, but the external surface needs BOTH metrics, not the percentage alone.** You are right that 13–38σ is the wrong *headline* — a 2%-wide theory band is not a measurement with a 0.17 GeV error bar, and quoting σ_PDG inflates the miss. The honest internal metric is the percentage (+1.80% to +5.23%). **But** an external referee's M3 is written in σ ("13.24σ in PDG units" is the form M3 itself uses), and a surface that quotes only the percentage will read to that audience as minimizing the miss. The observational-discipline fix is to carry both with their scopes pinned: **percentage miss (+1.80% to +5.23%) is the construction-quality metric (random-geometry column); σ_PDG (13.24–38.53σ) is the precision-datum-confrontation metric (the incumbent/precision column)** — the same dual-column logic as the BF. Quoting one without the other is the category error in either direction: σ-only inflates, percentage-only minimizes. Both, scoped, is the only honest form.

**MISSED — the load-bearing fact for the citation class: the DECISIVE label is m_H-ONLY, so the m_H disposition and the verdict-branch decision are the SAME decision.** Sage (this turn, Re:P1): with m_H, BF=2000 (log10=3.30, DECISIVE); without m_H, BF=63.25 (log10=1.80, very-strong — *below* the DECISIVE floor of 100). The other three factors *cannot* produce DECISIVE on their own (their summed log10 = 1.80 < 2.0). **Therefore "DECISIVE" is not a property of the spine — it is a property of m_H.** This is the observational-priorities consequence your P3 implies but doesn't state: you cannot tag m_H STRAINED and simultaneously cite "DECISIVE" unqualified, because the strained factor *is* the decisiveness. The strain tag and the citation tier are mechanically coupled. This forces the verdict (M2): the moment m_H is STRAINED, the unqualified external "DECISIVE" is no longer carriable — it must become "DECISIVE against random geometry (m_H-driven, currently a +1.8–5.2% band-strain)" inline.

**EMERGES — the route multiplicity is a Class-8.2-flavored pre-registration gap on the observable surface, and your forward-route-gate is the right remedy but it has a falsifier-surface precondition I own.** The route set {KK-L5=127.5 / KK-threshold=131.8 / zeta=138.5-excluded / μ_BC=188-ACCOMMODATION} means b_mH=1.5 is route-dependent, and `S101-M0-BCS-SCREENING` confirms it (`SECONDARY: r_KK_scr_RG=-0.0046, CONVENTION-SENSITIVE`). On the falsifier surface this is the same pathology as M5's n_s "floating scheme" objection — *a prediction whose value floats across schemes tests nothing at the precision of the datum it confronts*. The referee will (correctly) apply the M5 logic to m_H if the route is not pinned. So your S102 route-selection gate (Q-P3b: pre-register the FORCED route before m_H re-enters any external evidence statement) is exactly right, and I add the surface-side precondition: **until the route closes, the §7 m_H row must carry a `SCHEME-FLOATING` tag analogous to the n_s register entry, and b_mH=1.5 is cited as CONVENTION-SENSITIVE, not as a pinned contribution.** I will write that into the surface in M2. Your Q-P3a (down-tag b_mH=1.5 pending route-selection?): I do NOT down-tag the *contribution value* (1.5 is Sage-exact and correct for the KK-threshold route); I tag the contribution CONVENTION-SENSITIVE and gate its *external citation*, which is the citation-layer fix you correctly distinguished from a statistic-layer down-tag.

#### Re: P4 — Cross-Cutting Observations

**AGREE on branch 2 (dual-BF) as the verdict — with one structural amendment that the field-standard convention forces.** Your P4 reasoning is sound: keep-as-is hides the incumbent comparison the external reader asks; down-tag confuses the statistic with its citation; dual-report says what is true. I adopt branch 2. The deep finding — *discrimination and strain are the same coin, and the coin is dimensionality* — is correct and it is the genuine workshop output; I will preserve it verbatim in the wrap-up. Your verdict-table positions on Topics 1–4 I accept as written.

**DISAGREE — your two-column table mislabels the incumbent column's tier, and the mislabel is the exact thing an external referee will catch.** Your P4 table row 2 reads "ΛCDM+ν | ≤ 31.6 via m_H alone | ANECDOTAL–weak." That conflates two different numbers. Sage (Re:P1, exact): m_H *alone* is 31.62 = log10 1.50 = **very strong** (Jeffreys), not anecdotal. It is only the *band-missing* m_H whose operative discrimination collapses toward the ordering's 2.0 (anecdotal). So the incumbent column needs TWO sub-rows, not one conflated "≤31.6 ... anecdotal":
- **m_H if route-pinned and band-hit**: 31.62 → very strong (NOT decisive — 0.20 dex below the BF=100 floor). Even a *perfect* m_H does not give DECISIVE against the incumbent, because the other three factors contribute zero. This is the single most important number on the surface and your table omits it.
- **m_H as currently measured (band-miss)**: discrimination degraded by the +1.8–5.2% miss → operative incumbent BF near the ordering's factor ~2 → anecdotal.

The correction matters because the honest external statement is NOT "the framework is anecdotal against ΛCDM+ν" (that overstates the weakness and is itself unfair to the zero-knob construction) — it is "**against ΛCDM+ν the framework reaches at most very-strong, never decisive, and that ceiling rests entirely on m_H, which currently band-misses.**" The ceiling (very-strong, set by m_H-alone=31.62) is a permanent structural feature; the floor (anecdotal, set by the band-miss) is the current contingent state. Both belong on the surface.

**MISSED — your Q-P4a (is the second BF redundant with the existing §7 three-register split?): NO, it is additive, and here is why from the surface owner's seat.** The §7 three-register split (robust / conditional / falsified — referee credits it "exactly right") is a *status* partition: it sorts each observable by how settled its value is. It does NOT carry the *reference-class* axis: a single observable's BF can be DECISIVE against one null and anecdotal against another *within the same register*. The three-register split and the dual-column BF are orthogonal axes (status × reference-class). The BF_spine row currently sits in the "robust" register with a single DECISIVE number — and that is precisely the conflation M4 catches: a robust-status statistic cited at a tier that is only valid against the null the external reader doesn't assume. So the second column is not redundant; it is the missing axis. I will add it as a reference-class annotation *within* the existing register structure, not as a new register.

**EMERGES — the verdict is more constrained than a free choice among three branches: branch 1 (keep-as-is) is foreclosed by the m_H-ONLY-DECISIVE coupling, and the real decision is dual-BF (branch 2) vs the narrow m_H citation-gate (a sub-case of branch 3).** From Re:P3: "DECISIVE" is mechanically m_H. From Re:P1: m_H is STRAINED. Therefore keep-as-is (unqualified external DECISIVE) is not a live option — it would cite a tier that rests on a strained factor against a null the reader doesn't assume. That collapses the trichotomy to: **(2) report both BFs with the strain visible, OR (3-narrow) gate the external DECISIVE citation on route-selection while keeping the random-geometry DECISIVE citable internally.** These are compatible, and I take BOTH: dual-BF on the external surface (your branch 2) AND the internal/external citation gate (your Q-P4c — yes, "DECISIVE" is citable internally in capstone §7.1 as "DECISIVE against random geometry," but ANY external surface appearance of "DECISIVE" requires the dual-column context INLINE, never a footnote, because the tier-name imports the >100 claim and the reader's eye lands on the word, not the note). Your Q-P4b (pre-register `BF_spine_vs_LCDM` with the four per-factor discrimination values pinned before compute): YES — that is the M2-anti-post-hoc discipline applied to the incumbent BF, and I pin those four values in M2 and route the gate as a carry-forward in M3.

### Part 2: Original Analysis

#### M1: The reference-class distinction — external evidence statements vs internal diagnostics (observational priorities view)

**The question the workshop names (a): which reference class does an EXTERNAL audience assume when they read "DECISIVE," and what does field-standard model-comparison practice require the citation to carry? The answer is not a preference — it is a convention with a pinned numerical boundary.**

**1. "DECISIVE" is a Jeffreys tier name, not an adjective.** The word carries a definite numerical claim. On the two conventions in circulation (both verified Sage, this session):
- **Jeffreys (1961), in log10 K**: <0.5 barely-worth-mentioning · 0.5–1 substantial · 1–1.5 strong · 1.5–2 very strong · **>2 DECISIVE**. The DECISIVE floor is BF = 100.
- **Kass–Raftery (1995), in 2 ln K**: 0–2 bare mention · 2–6 positive · 6–10 strong · >10 very strong. (KR has no "decisive" tier; their top tier is "very strong," which begins at BF ≈ 150.)

When the §7 surface (or capstone §7.1) prints "DECISIVE," an NCG-or-cosmology referee reads it as Jeffreys >100. That is what the referee's M4 is reacting to: not the number 2000, but the *tier claim* the word imports — against the null the referee assumes.

**2. The external audience assumes the INCUMBENT null. This is field-standard, not a stylistic choice.** In observational cosmology, a published Bayes factor on a model-confrontation surface is, by overwhelming convention, *against the incumbent model* (ΛCDM / ΛCDM+ν), never against a prior-predictive null over the space of all candidate theories. Concrete field practice: the DESI DR2 dark-energy papers report `ΔΧ²` / Bayes factors of the w₀wₐ model *against ΛCDM*, not against "a random dark-energy EoS"; the Planck inflation model-comparison tables report evidence of each inflaton potential *against the baseline*, not against "a random potential." A referee reading a falsifier surface defaults to "what is the evidence against the standard model I already hold?" — because that is the only comparison that moves their posterior. The random-geometry null answers a different and *internal* question ("is this geometry special among geometries?"), which is a legitimate model-SELECTION statistic but is not what an external evidence statement conventionally reports.

**3. The two reference classes are not two opinions about one number — they are two different statistics that happen to share a symbol.** This is the substrate-first reading: the four spine factors are genuine substrate-IS predictions (forced outputs of `(A_K, H_K, D_K)`, no knob — phonon-first's P1 steelman is correct and I do not weaken it). The *statistic* built from them, however, is reference-class-indexed:
- **vs random geometry** (model-selection): BF = 2000, log10 = 3.30, **DECISIVE** (1.30 dex above the floor). All four factors contribute. This is the correct measure of "the geometry is not arbitrary," and it is the framework's single strongest internal argument. It stays, at full weight, on the internal surface.
- **vs ΛCDM+ν** (model-comparison): the operative BF is m_H-only (the other three contribute zero incumbent discrimination — Re:P1/Re:P2), and m_H-alone = 31.62, log10 = 1.50, **very strong — NOT decisive** (0.20 dex below the floor). With the current band-miss, the operative discrimination degrades toward the ordering's 2.0 → anecdotal. The incumbent comparison **never reaches DECISIVE**, even with a perfect m_H.

**4. The load-bearing observational-priorities finding: against the incumbent, the framework has a permanent very-strong CEILING and a contingent anecdotal FLOOR.** The ceiling (31.62, very-strong) is structural — it is set by m_H-alone because the three dimensionless factors are convergent-with-ΛCDM and contribute nothing; *no amount of route-pinning or band-fixing lifts the incumbent BF to DECISIVE*, because DECISIVE would require >100 = log10 2.0 and the only discriminating factor caps at 1.50. The floor (≈2, anecdotal) is the current contingent state set by the +1.8–5.2% band-miss. The honest external statement is bounded on both sides: **"against ΛCDM+ν the framework reaches at most very-strong (m_H-driven), never decisive; it currently sits near anecdotal pending m_H route-selection and band resolution."** This is not the framework being weak — it is the precise, defensible reach of a zero-knob construction whose dimensionless predictions converge with the incumbent and whose single dimensional discriminator is strained.

**5. What this does to the rank-2 dagger LICENSED status (assignment b, M_KK-import layer).** The S98 result licensed multiplying the rank-2 dagger {a₀,a₂}-discounted spine because `max|Corr|=0` across the four pipelines. The W-2 rank-1 M_KK-import finding (this session, phonon-first co-derived) does NOT revoke the algebraic/pipeline license — those layers are genuinely closed — but it scopes what the license *means*: the multiplication is licensed at the algebraic and pipeline-systematics layers, and theory-level-clean for the three dimensionless factors, while m_H rides the rank-1 import shared with the gravitational sector (`Corr(a₀,a₂)=+1`). So the LICENSED status holds *as an internal/random-geometry-column property* (the four forced predictions are independent functions of the one substrate) and is *not in dispute on the incumbent column* (where three contribute zero and the fourth is the entangled one). The rank-2 dagger license is real; its reach is reference-class-indexed exactly as the BF is.

**6. Field-standard citation requirement (the convention the citation must carry).** Kass–Raftery and the cosmology model-comparison literature require a reported Bayes factor to name (i) the two models compared and (ii) the prior under which the evidence integral was taken. A surface that prints "DECISIVE" with neither the null named nor the prior stated is incomplete by the field's own standard — independent of this framework's internal discipline. The §7 surface therefore MUST, to be defensible to the audience the referee represents, name both nulls and report both numbers. This is the convention; M2 implements it.

#### M2: What each verdict branch requires on the §7 falsifier surface (annotation / dual-BF / down-tag mechanics)

**The landing target is concrete and I own it.** The BF_spine joint-evidence statement does NOT live in the LAB-FALSIFIER 3He-B rows; it lives in `sessions/framework/registry/falsifier-master-inventory.md`, the **Joint-BF Multiplicativity Qualifier block, lines 1702–1756** — specifically the Tier partition table (line 1704–1708), the `S97 W4-4` COMPUTED-OUTCOME annotation (1714–1734), and the `S98 W4-4 — OQ3 covariance ESTABLISHED` annotation (1736–1756). The capstone §7.3 item-(5) Multiplicativity Admissibility Rule cites this inventory block as its register-of-record. I am sole writer of both (`feedback_mack-bridge-role.md`, AMRI-PROMOTED 2026-04-28). I effect the verdict here next round.

**Critical reconciliation BEFORE any branch — there are TWO b_mH conventions live, and they must not collide on the surface.** The inventory headline (line 1746, S98) uses `BF_spine = 2.0×10³` built on the ACCOMMODATION-honoured `b_mH` per `falsifier-rigor-registry.md` row #6 (the user's 1×-evidence-weight directive: "one scale tuned to PDG sin²θ_W; do NOT allow citation as ZFP"). The S97 block (line 1722) explicitly states the FLOOR `BF_spine = 2.0×10²` uses `b_mH = 0.5` (structural-only floor) and reports `b_mH = 1.5` only as a cross-check; the S98 line 1746/1747 then promotes the `b_mH=1.5` (full a4-KK 5-OOM) structural value to the headline `2.0×10³` once OQ3 covariance is established. **phonon-first's P1 decomposition uses b_mH = 1.5** (the structural full-range value), which is the S98 headline convention — so the decomposition is consistent with the current headline, NOT the S97 ACCOMMODATION floor. This matters for the dual-column: the ΛCDM+ν column's m_H-discrimination must be priced at the SAME b_mH the random-geometry column uses (1.5 → 31.62), or the two columns are not comparable. I will pin b_mH=1.5 for BOTH columns and footnote the ACCOMMODATION-floor b_mH=0.5 as the conservative variant (it only deepens the incumbent deflation — m_H-alone at b_mH=0.5 is 10^0.5=3.16, "substantial," even further below DECISIVE).

**Branch 1 — KEEP-AS-IS (FORECLOSED, not merely dispreferred).** Per Re:P3/Re:P4, "DECISIVE" is mechanically m_H-only and m_H is STRAINED; an unqualified external DECISIVE is not carriable. I will not effect branch 1. (Recorded so the verdict table shows it was adjudicated foreclosed, not skipped.)

**Branch 2 — DUAL-BF (the verdict I effect).** Concrete edits to `falsifier-master-inventory.md`:

1. **Tier partition table, line 1706–1708**: extend the "1 — spine" row's "Joint-BF disposition" cell to carry the reference-class index. Current cell reads `MULTIPLY unconditionally — the BF_spine floor`. New cell text:
   > `MULTIPLY unconditionally — BF_spine = 2000 DECISIVE vs random-geometry null (model-SELECTION); vs ΛCDM+ν incumbent (model-COMPARISON) the operative BF is m_H-only [σ/m=0, c_s²=0 carry ZERO incumbent discrimination — convergent-with-ΛCDM; ν-ordering ~factor 2], ceiling 31.62 (very-strong, NEVER decisive), current floor ~2 (anecdotal) under the m_H band-strain. Reference class is a property of the statistic, not a free choice.`

2. **New two-column sub-block appended after line 1756** (a new dated annotation `### S101 W-? — BF_spine reference-class dual-column: DECISIVE is random-geometry-scoped; incumbent ceiling is very-strong`), carrying the table:

   | Reference class | Statistic type | BF | log10 K | Jeffreys tier | Driver |
   |:---|:---|---:|---:|:---|:---|
   | Random geometry (internal) | model-SELECTION | 2000 | 3.3010 | DECISIVE (>2) | all 4 (m_H 45.4%, σ/m 30.3%, c_s² 15.1%, ord 9.1%) |
   | ΛCDM+ν (incumbent) — ceiling | model-COMPARISON | 31.62 | 1.5000 | very strong (NOT decisive) | m_H-only; other 3 zero-discrimination |
   | ΛCDM+ν — current (band-miss) | model-COMPARISON | ~2 | ~0.30 | anecdotal | m_H discrimination degraded by +1.8–5.2% miss |

   with the per-factor incumbent-discrimination assignment PINNED (Q-P4b): `{σ/m=0: 0; c_s²=0: 0; ν-ordering: log10 2 = 0.30103; m_H: ≤1.5 route-pending}`. All values Sage-exact (this session); the assignment is pre-registered text, not post-hoc narrative.

3. **m_H row tag** (the §VII.BH / scorecard m_H pointer the spine cites): add `SCHEME-FLOATING` + `CONVENTION-SENSITIVE` per Re:P3 — band 127.5–131.8 GeV, PDG 125.25±0.17 OUTSIDE band, miss +1.80% (=13.24σ_PDG) to +5.23% (=38.53σ_PDG), M0 inheritance MARGINAL (`S101-M0-BCS-SCREENING 1a1eff66`, screened KK residual −11/670=−1.642%), route set {127.5/131.8/138.5-excl/188-ACCOM} ⇒ b_mH route-dependent. Both metrics carried with scopes (percentage = construction-quality; σ_PDG = precision-confrontation).

4. **Capstone §7.1/§7.3 coupling (FLAG, not mack-edit where prose).** The §7.3 item-(5) register-pin already points here, so the inventory annotation is canonically captured. Any §7.1 capstone PROSE appearance of "DECISIVE" requires the inline dual-column context (Q-P4c) — that is a Q4 designated-writer PROSE patch per `capstone-hygiene-gate.md`, FLAGGED to housekeeping §A, NOT a mack edit. The §7-falsifier-anchor STATUS cell (Q2, my domain) I patch directly: BF_spine row gains the reference-class index.

**Branch 3 — DOWN-TAG (only the narrow m_H citation-gate, NOT a spine-statistic down-tag).** I do NOT down-tag BF_spine=2000 (Sage-exact, correct, legitimate model-selection null) nor b_mH=1.5 (correct for the KK-threshold route). The narrow down-tag I DO effect is at the citation layer: gate any EXTERNAL DECISIVE-anchoring of m_H on the S102 route-selection closing (the `SCHEME-FLOATING` tag in edit 3 above). This is a sub-case folded INTO branch 2, not an alternative to it — branch 2 (dual-BF) and the narrow citation-gate compose.

**The conflation hazard phonon-first flagged (P1) — I will hard-separate it on the surface.** The drop-{σ,c_s²} residual `63.245553` NUMERICALLY EQUALS `BF_spine_dagger = 63.25` (knowledge-MCP confirms both; Sage confirms `b_sigma+b_cs2 = 3/2` EXACTLY and `b_spine_dagger = b_spine − 1.5`). These are **two structurally-unrelated 1.5's**: the dagger discount 1.5 = `max(b_a0, b_a2)` rank-1 collapse of the *borrowed-H* pair (Tier-3, line 1710/1723); the drop-residual 1.5 = `b_sigma + b_cs2` *zero-discrimination strip* of the *spine* (Tier-1, incumbent column). Same number, disjoint mechanisms, different Tiers. The dual-column sub-block (edit 2) will carry an explicit `[NUMERICAL COINCIDENCE — NOT THE SAME MECHANISM: 63.25_dagger (Tier-3 borrowed-H rank-1 collapse) ≠ 63.25_drop (Tier-1 spine zero-discrimination strip); do not cross-reference]` guard inline, so no downstream cite conflates them on the falsifier surface.

**What does NOT change.** BF_spine=2000 stays at full weight on the random-geometry/internal axis (capstone §7.1 "DECISIVE against random geometry" remains citable INTERNALLY). The Tier-3 borrowed-H dagger collapse (line 1750) stays UNCHANGED (`Corr(a₀,a₂)=+1`, OQ3 establishment is on the spine axis only). Row #1 (w₀ R_842, 2027-binding) is UNTOUCHED — it is the separate DESI DR3 likelihood channel and must not be conflated with the model-class BF_spine. The three-register §7 split (status axis) is preserved; the reference-class index is the orthogonal axis added within it.

#### M3: Questions for phonon-first

**(Q-M1) The b_mH convention collision (the one thing in your decomposition I need reconciled before I land the dual-column).** Your P1 decomposition uses `b_mH = 1.5` (full a4-KK 5-OOM structural). But the inventory headline carries TWO b_mH conventions: the S97 FLOOR (line 1722) uses the ACCOMMODATION-honoured `b_mH = 0.5` per `falsifier-rigor-registry.md` row #6 (the user's explicit 1×-evidence-weight directive, because the m_H numerical landing goes THROUGH μ_BC tuned to PDG sin²θ_W), and the S98 headline (line 1746) promotes `b_mH = 1.5` once OQ3 is established. **Which b_mH does the ΛCDM+ν column price?** My M2 pins 1.5 for both columns (so they are comparable) and footnotes 0.5 as the conservative variant. But the user directive says the *headline* must use the ACCOMMODATION floor 0.5 — and at b_mH=0.5 the random-geometry BF is not 2000, it is `10^(0.5+1.0+0.5+0.30103) = 10^2.30103 = 200` (the S97 FLOOR). Does your "DECISIVE against random geometry = 2000" claim survive the user's ACCOMMODATION directive, or is the defensible random-geometry headline the FLOOR 200 (still DECISIVE, log10=2.30 > 2, but one decade lower)? This is a substrate-IS-evidence-weighting question I cannot adjudicate unilaterally — it sits at the boundary of your evidence-weighting charge and my surface-honesty charge.

**(Q-M2) Is "convergent-with-ΛCDM" the right tag for σ/m=0 and c_s²=0, or does the framework make a SHARPER claim that ΛCDM does not?** You tag them CONVERGENT-WITH-ΛCDM (zero incumbent discrimination). But σ/m=0 in ΛCDM is an *assumption* (CDM is collisionless by fiat); in the framework it is a *derived* consequence (Leggett-channel CPT-neutral occupation-gap, N_Fock=1 superselection). A derived zero and an assumed zero are not the same epistemic object even when numerically equal. Does the framework gain ANY incumbent discrimination from *deriving* what ΛCDM *assumes* — e.g., if a future self-interaction bound σ/m < ε tightened, the framework PREDICTS the zero (falsifiable) while ΛCDM merely accommodates any value below the bound? If so, the incumbent column is not strictly zero for those factors — it is "zero at present precision, but the framework is falsifiable there and ΛCDM is not." That would be a real (if currently unmeasurable) incumbent-discrimination axis, and it changes what I write in the dual-column. Your call as the one who co-derived the protected/unprotected split.

**(Q-M3) The S102 route-selection gate — is the FORCED route a genuine geometric prediction or a post-hoc selection?** Your Q-P3b proposes pre-registering the FORCED m_H route before m_H re-enters any external evidence statement. I endorse it. But I need the gate's PASS criterion specified at YOUR end before I can pin its falsifier-surface precondition: does the framework have a FIRST-PRINCIPLES reason to select KK-threshold (131.8) over KK-L5 (127.5) over zeta (138.5) — a geometric mandate analogous to the M5 √x functional-selection for n_s — or is the route currently chosen by best-agreement-with-PDG (which would be the M2 post-hoc move the referee warns against)? If the latter, the honest surface tag is not SCHEME-FLOATING-pending-selection but ACCOMMODATION (route chosen to fit), which is a strictly weaker evidential status. The distinction determines whether m_H's b_mH=1.5 is ever citable as forced evidence or only ever as accommodation.

**(Q-M4) Does the dual-column compose cleanly with the EVOI evidence-weighting frame, or do they conflict at the random-geometry column?** `evoi-prioritization.md` Evidence Weighting says a zero-knob construction's prior-predictive range IS evidence (your steelman, correct). My M1 says the EXTERNAL surface defaults to the incumbent null (field convention). These are compatible only if the random-geometry column is read as "construction-quality evidence" and the incumbent column as "model-comparison evidence" — two different evidence types, not two estimates of one. Do you agree the §7 surface should explicitly LABEL the two columns by evidence-TYPE (model-selection / model-comparison) rather than leave them as two BF numbers a reader will instinctively compare as if commensurable? My concern: a reader seeing "2000" and "~2" side by side will read the framework as self-contradicting unless the surface states they answer different questions — which is exactly your P1 thesis, made load-bearing at the surface-layout level.

**(Q-M5) The deep finding — "discrimination and strain are the same coin, the coin is dimensionality" — is the genuine workshop output. Does it generalize to a FORWARD prediction I should register?** If only the dimensional factor can discriminate against a dimensional rival, then the framework's path to out-discriminating ΛCDM+ν runs ENTIRELY through deriving M_KK cleanly (the W-2 unprotected scale). That is a sharp structural claim: it says the framework cannot reach DECISIVE-vs-incumbent by adding more dimensionless predictions (they all converge), only by deriving the one imported scale. Should I register this as a forward observational-priorities note — "the framework's incumbent-discrimination ceiling is structurally bounded at very-strong until M_KK is derived rather than imported" — so the next planner knows that piling on dimensionless PASSes does not move the incumbent BF? This is the EVOI consequence of your coin metaphor and it belongs on the surface if it holds.

---

## Round 2 — phonon-first: Follow-up

### CONVERGENCE

**I accept the P4-table mislabel correction (Re:P4, your DISAGREE) without reservation — and it is the correction that matters most.** My P4 row 2 read "ΛCDM+ν | ≤ 31.6 via m_H alone | ANECDOTAL–weak," conflating two distinct numbers into one tier. You are right and the arithmetic is unambiguous (Sage, this turn, confirming yours): m_H *alone* is `10^1.5 = 31.622777` = log10 1.50 = **very strong** (Jeffreys 1.5–2 band), NOT anecdotal. Only the *band-missing* m_H — its discrimination degraded by the +1.8–5.2% miss — collapses toward the ordering's `10^0.30103 = 2.0` (anecdotal). My table erased the ceiling. The correct incumbent column has TWO sub-rows, exactly as your Re:P4 specifies: **m_H route-pinned-and-band-hit = 31.62 → very-strong (NOT decisive)** and **m_H as-measured (band-miss) = ~2 → anecdotal**. The very-strong sub-row is the single most important cell on the incumbent surface and I had dropped it. Corrected.

**I accept the branch-1 foreclosure as MECHANICAL, not as a preference I lost.** My P4 framed the verdict as a free choice among three branches with branch-2 (dual-BF) as my pick. Your Re:P3 + Re:P4 sharpen this into a structural foreclosure I should have stated myself: "DECISIVE" is mechanically m_H-only (Sage: drop m_H → `10^1.80103 = 63.25 < 100`, below the DECISIVE floor; the other three factors' summed log10 = 1.80 < 2.0, so they *cannot* produce DECISIVE on their own). Therefore the strain tag on m_H and the DECISIVE citation tier are the SAME object — you cannot tag m_H STRAINED and simultaneously carry an unqualified external "DECISIVE," because the strained factor *is* the decisiveness. Branch 1 (keep-as-is) is not dispreferred; it is foreclosed by this coupling. And your M1 closes the external door from the other side: field-standard practice (DESI DR2 w₀wₐ, Planck inflation model-comparison tables) reports BF *against the incumbent by convention*, so the external surface's default null is ΛCDM+ν — and the only factor with incumbent-discrimination is the strained one. Both jaws of the foreclosure are correct.

**I accept the very-strong-ceiling / anecdotal-floor structure as the honest external statement, and it is BETTER than my P4.** My P4 said the incumbent comparison is "ANECDOTAL–weak," full stop. That overstates the weakness and is itself unfair to the zero-knob construction — exactly the asymmetry your Re:P4 catches. The correct bounded statement is yours: **against ΛCDM+ν the framework reaches at most very-strong (m_H-driven), never decisive; it currently sits near anecdotal pending m_H route-selection and band resolution.** The ceiling (31.62, very-strong) is a *permanent structural feature* set by m_H-alone; the floor (~2, anecdotal) is the *current contingent state* set by the band-miss. Two numbers, two epistemic statuses — and your M1 §4 derives the ceiling's permanence cleanly: no amount of route-pinning lifts the incumbent BF to DECISIVE because DECISIVE needs >100 = log10 2.0 and the only discriminating factor caps at b_mH = 1.5. I verified this at BOTH b_mH conventions below (QUESTIONS, Q-M1): the incumbent ceiling = b_mH itself, and the largest b_mH on the books (1.5) is still 0.5 dex short of the DECISIVE-vs-incumbent floor. **Incumbent DECISIVE is structurally unreachable** — that is the load-bearing observational-priorities finding, and it is yours, not mine.

**I accept the dual-BF verdict (branch 2 + the narrow m_H citation-gate folded in).** Your Re:P4 reads the verdict as more constrained than a free trichotomy: branch 1 foreclosed, the real decision dual-BF (branch 2) vs the narrow m_H citation-gate (a sub-case of branch 3), and these COMPOSE rather than compete. I agree and adopt both: dual-BF on the external surface AND the internal/external citation gate ("DECISIVE against random geometry" citable INTERNALLY in capstone §7.1; any EXTERNAL surface appearance of "DECISIVE" requires the dual-column context INLINE, never a footnote, because the tier-name imports the >100 claim and the reader's eye lands on the word). My P4 verdict-table positions on Topics 1–4 stand as refined by your corrections; I do not re-litigate them.

**One smaller convergence worth pinning: I accept your three-layer independence taxonomy with my M1 §5 / Re:P2 qualifier folded in.** Algebraic ✓ (Wronskian), pipeline-systematics ✓ (S98 `0814c57f` Corr=0), theory-level ✓ for the three dimensionless factors / OPEN for m_H (rank-1 M_KK import, `Corr(a₀,a₂)=+1`) — with your sharpening that the theory-level-clean ✓ for {σ/m, c_s², ordering} is a *random-geometry-column property* (on the incumbent column those three carry zero evidence, so their independence is vacuously true there and not in play). The independence question, like the discrimination question, collapses to the single entangled m_H factor on the incumbent surface. Accepted.

### DISSENT

**No new dissent. I withdraw the only disagreement I raised (the P4-table tier mislabel) — it was an error in MY R1, not a disagreement with yours, and your Re:P4 correction is right.** Going into this final turn I hold no surviving objection to any of your M1/M2/M3 positions. Specifically:

- I do not dispute M1 (the incumbent-null field convention) — I verified it is convention, not preference, and the Sage tier-map confirms the incumbent comparison never clears the DECISIVE floor at either b_mH.
- I do not dispute M2 (the dual-column landing target at `falsifier-master-inventory.md` 1702–1756, the SCHEME-FLOATING m_H tag, the per-factor pin, the 63.25-conflation guard) — and below I HARDEN the conflation guard with a third colliding number you will want inside it.
- I do not dispute the Q-M1 framing as a real reconciliation that must precede the landing — but my answer (QUESTIONS) is that the collision RESOLVES rather than forcing a headline down-tag: DECISIVE survives at BOTH b_mH conventions. That is an answer to your question, not a dissent from it.

The closest thing to a residual tension — whether the random-geometry headline should be cited as 2000 (b_mH=1.5) or 200 (b_mH=0.5) — I resolve constructively below rather than as a disagreement: both are DECISIVE, they sit at two evidence-weighting layers, and the dual-column should carry the layer tag explicitly. There is no surviving point on which the math forces us apart.

### EMERGENCE

**What the "discrimination = dimensionality = strain" coin means for FUTURE spine extensions — and the explicit bridge to this session's W-2 rank-1 theorem.** Your Q-M5 asks whether the coin generalizes to a forward prediction worth registering. It does, and the cross-pollination with the W-2 normalization-non-universality workshop (volovik × phonon-first, CLOSED this session — I co-derived the dimensional bookkeeping) makes it a *theorem-conditional structural bound*, not a soft note. The chain:

1. **W-2's result**: the substrate determines all dimensionless/conformal content but imports exactly ONE dimensional scale, M_KK, and that import is **rank-1** (`gamma_unit = ℏ/(M_KK·c²)`; N₃=0, BDI class, no Fermi-point protection — so no second protected dimensional invariant can appear).
2. **This workshop's coin**: only a factor carrying a dimensional scale can discriminate against a *dimensional* rival theory (ΛCDM+ν has a fixed dimensional content — H₀, the matter scales). The three dimensionless spine factors converge with the incumbent precisely because they carry no scale; the one dimensional factor (m_H) discriminates precisely because it does.
3. **Compose**: a NEW spine factor that discriminates against ΛCDM+ν must carry a dimensional scale (coin). But W-2 says the substrate has exactly ONE imported dimensional handle (rank-1). Therefore **every future incumbent-discriminating spine factor rides the SAME M_KK import** — it cannot be theory-level-independent of m_H, because there is no second dimensional handle for it to ride. The incumbent column can never accumulate *independent* dimensional discriminators; it can only ever carry m_H-flavored content, all sharing `Corr ~ +1` with the gravitational normalization.

**The forward structural bound (register it, per your Q-M5):** *the framework's incumbent-discrimination ceiling is structurally bounded at very-strong (set by the single rank-1-imported scale) and CANNOT be lifted to DECISIVE-vs-incumbent by adding dimensionless predictions — they all converge — NOR by adding more dimensional predictions, because W-2 forbids a second independent dimensional handle. The only path to DECISIVE-vs-incumbent is to DERIVE M_KK cleanly (remove the import), which converts the one entangled factor into a clean discriminator and simultaneously resolves the m_H band-strain and the M0 inheritance.* This is the explicit connect between the two workshops' verdicts: **W-2's rank-1 theorem is the UPPER BOUND on this workshop's incumbent ceiling.** The very-strong ceiling is not a contingent fact about the current spine — it is a corollary of N₃=0, and it stands *until the W-2 rank-1 theorem is falsified* (a second protected dimensional invariant is found, which would require Fermi-point protection the BDI class does not supply). This is the substrate-first reading: the incumbent-discrimination reach is a property of the substrate's dimensional structure (one imported scale), not of how many observables we compute. Piling on dimensionless PASSes moves the random-geometry column and leaves the incumbent column exactly where it is.

**The dual-BF column has a clean pre-registrable refresh trigger: the S102 m_H route-selection gate output.** Your Q-M3 asks for the route-gate's PASS criterion at my end (I give it in QUESTIONS); the EMERGENCE here is that the gate's *output* is exactly the variable the dual-column's incumbent cells are parameterized by. The incumbent column has three states keyed to the route-gate verdict:

| S102 route-gate output | m_H status | incumbent operative BF | incumbent tier | dual-column refresh |
|:---|:---|:---|:---|:---|
| FORCED route found, band-HIT | route-pinned, hits PDG | 31.62 (b_mH=1.5) | very-strong (ceiling) | incumbent floor lifts to ceiling |
| FORCED route found, band-MISS persists | route-pinned, +1.8–5.2% miss | degraded → ~2 | anecdotal (current floor) | floor stays; SCHEME-FLOATING → STRAINED-PINNED |
| NO forced route (best-fit selection) | ACCOMMODATION | b_mH not citable as forced | (evidence status drops to accommodation) | m_H exits the incumbent discriminator set entirely |

So the dual-column is not a static landing — it is a **two-state-plus-null cell whose refresh is mechanically triggered by one downstream gate**. Pre-register the trigger in M2's sub-block: *"incumbent-column m_H cells REFRESH on `S102-MH-ROUTE-SELECTION` verdict per the 3-state table; until then the cell carries the current-floor (anecdotal) value with the SCHEME-FLOATING tag."* This makes the surface self-updating in a pre-registered way rather than requiring a fresh adjudication when S102 lands — and it satisfies M2's anti-post-hoc discipline because the three output states and their tier assignments are pinned BEFORE the gate runs.

**A third emergence, smaller: the per-factor decomposition is itself a falsifier-surface ASSET, and the dual-column makes it visible.** A theory that can split its own evidence into convergent-with-incumbent (σ/m=0, c_s²=0 — derived, not fitted) and discriminating-against-incumbent (m_H — novel, strained) parts is more honest than one reporting a single conflated number. The dual-column doesn't just price the deflation; it *displays the framework's evidential anatomy* — which factors are shared and which are novel. The referee credited the §7 three-register split (status axis) as "exactly right"; the reference-class index (the orthogonal axis you identified in your MISSED on Q-P4a) is the second honest axis. Two orthogonal honest axes on one surface is the structural strength here, not a concession.

### QUESTIONS

**Q-M1 (the b_mH 0.5-vs-1.5 convention collision — the one thing you need reconciled before landing the dual-column). ANSWER: the collision RESOLVES; it does not force a headline down-tag. The structural spine's 2000.0 uses b_mH = 1.5 (verified Sage-exact below), the 0.5 ACCOMMODATION floor gives 200 which is STILL DECISIVE, and the two coexist without contradiction because they are two evidence-weighting LAYERS of the same model-selection statistic, not two rival headlines.**

*Trace of both values to their producing gates (knowledge-MCP, this turn).*
- **b_mH = 1.5** is the S98 headline convention. The canonical equation entity (`session-98-plan-w4.md`): `BF_spine_full = 10^3.30103 = 2000.0 [DECISIVE band >100]`, `log10 BF_spine_full = b_spine_struct = 3.3010299956639813 (no discount; handles independent)`. Producing gate `S98-W4-4-OQ3-COVARIANCE` PASS (`0814c57f`): `BF_spine=2000.00 DECISIVE log10BF=3.30103 oq3_established=True`. This is the **full a4-KK 5-OOM structural** m_H evidence weight, licensed to the headline once OQ3 covariance (`max|Corr|=0`) established the four handles independent.
- **b_mH = 0.5** is the conservative ACCOMMODATION-weighted convention. It traces to `falsifier-rigor-registry.md` row #6 (m_H, ACCOMMODATION-FLAGGED): the m_H numerical landing goes THROUGH `μ_BC` tuned to PDG `sin²θ_W` (row #7, `sin²θ_W` ACCOMMODATION, `μ_BC = 188.44 GeV` fit) — so under the user's 1×-evidence-weight directive ("one scale tuned to PDG sin²θ_W; do NOT allow citation as a zero-free-parameter prediction"), m_H's evidence weight is throttled from the full 1.5 to a structural-only 0.5.

*Which convention does the structural spine's 2000.0 actually use? — Sage-exact verification of your "the arithmetic forces b_mH = 1.5" claim.* Substitution chain per `math-scripts.md`:
```
Step 1:  b_spine_struct = 3.3010299956639813           [canonical, S98-plan-w4.md]
Step 2:  the structural form is log10(2) + 3 = 3.3010299956639811952  [Sage: matches to printed precision]
Step 3:  b_spine = b_mH + b_sigma + b_cs2 + b_nu,  with b_sigma=1, b_cs2=1/2, b_nu=log10(2) FIXED
Step 4:  b_mH = b_spine_struct − 1 − 1/2 − log10(2)
              = 3.3010299956639813 − 1.5 − 0.30103
              = 1.5000000000000000   [Sage: == 3/2 EXACTLY, bool True]
Conclusion:  the 2000.0 headline uses b_mH = 1.5, FORCED by the canonical arithmetic. Verified. ✓
```
So your decomposition is consistent with the S98 *headline* convention (b_mH=1.5), NOT the ACCOMMODATION floor. mack's M2 instinct to pin b_mH=1.5 for BOTH columns is correct on the arithmetic.

*What the 0.5 ACCOMMODATION floor is FOR, and why the two coexist without contradiction.* The 0.5 floor is the **conservative evidence-weight variant** that honours the user's directive: because m_H reaches its numerical value through a scale (`μ_BC`) tuned to a measured datum (`sin²θ_W`), a maximally-conservative reader discounts m_H's contribution from "full structural" (1.5) to "structural-floor" (0.5). The two do NOT contradict because they price the SAME factor under two different evidence-weight policies on the SAME (random-geometry) reference class:
- **b_mH = 1.5 → BF_spine = 2000** (log10 = 3.30103): the framework's full claim, m_H counted at full a4-KK structural weight. **DECISIVE** (1.30 dex above floor).
- **b_mH = 0.5 → BF_spine = 200** (log10 = 2.30103, Sage-exact): the ACCOMMODATION-conservative claim, m_H throttled to floor weight. **STILL DECISIVE** (0.30 dex above the floor of 100).

**The reconciliation that resolves your Q-M1 cleanly: DECISIVE survives the user's ACCOMMODATION directive.** The defensible random-geometry headline is *not* forced down to a non-decisive number by the 0.5 floor — at b_mH = 0.5 the statistic is 200, which still clears the Jeffreys DECISIVE floor (>100) by 0.30 dex. So you do not face a choice between "cite 2000 DECISIVE (violate the directive)" and "cite 200 non-decisive (honour the directive)." Both conventions land DECISIVE; the directive costs one decade, not the tier. **My recommendation for the landing**: cite the random-geometry headline as a *layered* statement — `BF_spine = 2000 DECISIVE at full structural m_H weight (b_mH=1.5); 200 DECISIVE at the ACCOMMODATION-conservative weight (b_mH=0.5, honouring the μ_BC↔sin²θ_W tuning) — DECISIVE at BOTH evidence-weight layers.* That carries the user's directive (the 0.5 floor is on the surface, visible) AND preserves the DECISIVE tier (true at both), with no contradiction because the two numbers are not rival headlines but the upper and lower bounds of one model-selection statistic under two evidence-weight policies.

*A GENUINE INCONSISTENCY to flag — NOT in the b_mH layers, but in a colliding "200" you must guard, which is a THIRD member of the 63.25-conflation hazard you already flagged.* Sage, this turn:
```
2000   = full spine, no discount         [b_mH=1.5; S98 headline; log10=3.30103]
63.25  = full spine − 1.5 dagger discount [S98-plan BF_spine_dagger; log10=1.80103]
200    = full spine − 1.0  OR  b_mH=0.5 floor  [TWO disjoint mechanisms, SAME number; log10=2.30103]
```
Here is the hazard: the **S97-D3-BF gate verdict text** reads `BF_spine = 2.0×10² DECISIVE after {a0,a2}-dagger discount` (knowledge-MCP, `session-97-phonon-first-cosmologist-synthesis.md`). But a 2000→200 step is a **1.0-decade** discount, whereas the canonical `{a0,a2}-dagger discount = 1.5` (= `max(b_a0, b_a2)` rank-1 collapse of the borrowed-H pair) takes 2000→**63.25**, NOT 200 (S98-plan `BF_spine_dagger = 3.30103 − 1.5 = 1.80103 = 63.25`). So the S97 gate's "200 after dagger discount" and the S98-plan's "63.25 dagger" cannot both be the same 1.5-magnitude dagger discount. **Resolution**: the Sage-exact, registry-of-record dagger value is **63.25** (it traces to the canonical `b_spine_struct − 1.5` arithmetic in `session-98-plan-w4.md`); the S97 gate-text "200" is a *decade-rounded or differently-discounted* legacy quote that numerically coincides with the b_mH=0.5 floor. **For the surface: 200 is the b_mH=0.5 ACCOMMODATION floor (a layer of the full spine), NOT a dagger-discounted value — do not cite "200 after dagger discount."** The dagger discount gives 63.25.

**Exact per-factor numbers for the dual-column (Q-M1 closeout — pin these, all Sage-exact this session):**

| Factor | b_i (log10) | BF_i = 10^b_i | random-geom share | ΛCDM+ν incumbent discrimination |
|:---|---:|---:|---:|:---|
| m_H | 1.50000 (= 3/2 EXACT) | 31.622777 | 45.4404% | **the ONLY discriminator**; ceiling 31.62 (very-strong, NOT decisive); current floor ~2 under band-miss |
| σ/m = 0 | 1.00000 | 10.000000 | 30.2936% | **0** (ΛCDM CDM collisionless by construction — convergent) |
| c_s² = 0 | 0.50000 (= 1/2 EXACT) | 3.162278 | 15.1468% | **0** (ΛCDM CDM pressureless — convergent) |
| ν-ordering | 0.30103 (= log10 2 EXACT) | 2.000000 | 9.1193% | `log10 2 = 0.30103` (factor ~2; ΛCDM agnostic, weak discrimination) |
| **Σ (full spine)** | **3.30103** (= log10(2)+3) | **2000.0** | 100% | — |
| **ACCOMMODATION floor** (b_mH→0.5) | **2.30103** | **200.0** | — | random-geom DECISIVE survives (0.30 dex above floor) |
| **dagger-discounted** (− 1.5) | **1.80103** | **63.25** | — | NOT 200 — guard the S97-text collision |

The b_mH cell carries 1.5 for BOTH columns (so they are comparable, per your M2), with the 0.5 ACCOMMODATION floor footnoted as the conservative variant — and at b_mH=0.5 the incumbent ceiling drops to `10^0.5 = 3.16` ("substantial," even further below DECISIVE), which only DEEPENS the incumbent deflation, never lifts it.

**Q-M2 (is "convergent-with-ΛCDM" the right tag for σ/m=0 and c_s²=0, or does the framework make a SHARPER claim?). ANSWER: tag them CONVERGENT-DERIVED, not bare CONVERGENT — and yes, the framework gains a real (currently-unmeasurable) incumbent-discrimination axis from DERIVING what ΛCDM ASSUMES.** You are right that a derived zero and an assumed zero are different epistemic objects even when numerically equal. The substrate-first content: σ/m = 0 in the framework is a *forced consequence* of the Leggett-channel GGE quasiparticle being CPT-neutral and non-annihilating (an occupation-gap / N_Fock=1 superselection property of the BdG inter-band mode — `phononic-framing.md` DM = Leggett-channel GGE quasiparticle), and c_s² = 0 follows from the same mode carrying no acoustic pressure. ΛCDM *assumes* both by fiat (collisionless, pressureless CDM). The discrimination axis: **the framework's zeros are FALSIFIABLE, ΛCDM's are not.** If a future self-interaction bound tightened to σ/m < ε, or a future small-scale measurement found σ/m ≠ 0, the framework is *falsified* (it predicts exactly zero from the superselection rule); ΛCDM merely *accommodates* any value below the bound by re-labelling its CDM. So on the incumbent column those two factors are not strictly zero-discrimination — they are **"zero at present precision, falsifiable-vs-accommodating."** My recommendation: tag them `CONVERGENT-DERIVED (zero incumbent BF at present precision; framework-falsifiable, ΛCDM-accommodating)` rather than bare `CONVERGENT`. This does NOT change the dual-column's BF arithmetic (their incumbent log-BF stays 0 today — falsifiability is not evidence until a measurement exists), but it changes the *annotation*: it records that the framework's convergence is a prediction, not an assumption, and that future precision converts it to a discrimination axis. The honest cell text: incumbent BF = 0 now, with a forward note that these are the framework's falsifiable-zero predictions.

**Q-M3 (the S102 route-selection gate PASS criterion — is the FORCED route a genuine geometric prediction or post-hoc selection?). ANSWER: the gate PASS criterion is FIRST-PRINCIPLES route mandate, and here is the criterion specified at my end — but I must be honest that the framework does NOT currently have the forced route, so the realistic verdict is the ACCOMMODATION branch you fear, pending the derivation.** The route set is {KK-L5 = 127.5 / KK-threshold = 131.8 / zeta = 138.5-excluded / μ_BC = 188-ACCOMMODATION}. The gate `S102-MH-ROUTE-SELECTION` PASS criterion I commit to:
- **PASS (forced route)**: the framework derives, from the spectral-triple geometry alone (the a₄-moment KK-threshold structure of the transverse |S|² fiber-embedding mode, `phononic-framing.md`), a UNIQUE geometric mandate selecting ONE route — analogous to the M5 √x functional-selection for n_s. The criterion is *route uniqueness from geometry with NO appeal to PDG agreement*: the selected route must be the one the a₄-moment structure forces, and the selection argument must be writable WITHOUT citing which route lands nearest 125.25.
- **FAIL/ACCOMMODATION**: if the route is currently chosen by best-agreement-with-PDG (the M2 post-hoc move), the gate FAILs the forced-route criterion and the honest surface tag is **ACCOMMODATION (route chosen to fit)**, strictly weaker than SCHEME-FLOATING-pending-selection.

My honest read of the current state: the framework has a *structural reason to prefer the KK family* (127.5 / 131.8) over zeta (138.5, excluded on independent grounds) — that part is geometric. But the choice *between* KK-L5 (127.5) and KK-threshold (131.8) is currently NOT forced from first principles; b_mH=1.5 is computed on the KK-threshold route (131.8), and that route's selection over KK-L5 (127.5) is not yet derived independent of the band-comparison. So the realistic S102 outcome is: **the KK *family* is geometrically mandated (not accommodation), but the route *within* the family is SCHEME-FLOATING pending a finer geometric selection criterion.** Surface tag accordingly: `KK-family FORCED; intra-family route SCHEME-FLOATING` — NOT bare ACCOMMODATION (the family choice is real), NOT FORCED-pinned (the intra-family choice is open). This is the distinction your Q-M3 needs: b_mH=1.5 is citable as forced evidence only after the intra-family route is geometrically pinned; until then it is SCHEME-FLOATING within a forced family, which is stronger than accommodation but weaker than a pinned prediction. The μ_BC=188 route is separately ACCOMMODATION (μ_BC tuned to sin²θ_W) and is NOT part of the forced-family set — it is the conservative-floor route, which is exactly why the b_mH=0.5 ACCOMMODATION floor exists (Q-M1).

**Q-M4 (does the dual-column compose with the EVOI evidence-weighting frame, or conflict at the random-geometry column?). ANSWER: they compose, and YES — label the two columns by evidence-TYPE (model-SELECTION / model-COMPARISON), not as two BF numbers a reader will instinctively compare.** `evoi-prioritization.md` Evidence Weighting (a zero-knob construction's prior-predictive range IS evidence — my steelman) and your M1 (the external surface defaults to the incumbent null by field convention) are compatible ONLY if the two columns are read as two *evidence types*, not two estimates of one quantity. The random-geometry column is **construction-quality evidence** (model-SELECTION: is the one substrate special among geometries?); the incumbent column is **model-comparison evidence** (model-COMPARISON: does the substrate beat the standard model?). These answer different questions — which is precisely my P1 thesis, and your Q-M4 correctly identifies that the thesis must be made load-bearing at the *surface-layout* level. Your concern is exactly right: a reader seeing "2000" and "~2" side by side will read the framework as self-contradicting unless the surface STATES they answer different questions. So the column headers must carry the statistic-type label inline (the M2 sub-block already has a "Statistic type" column — `model-SELECTION` / `model-COMPARISON` — which does exactly this; keep it, it is load-bearing, not decorative). My recommendation: the sub-block's header row should read, verbatim, *"two evidence TYPES, not two estimates of one quantity: DECISIVE (model-selection, vs random geometry) and very-strong-ceiling/anecdotal-floor (model-comparison, vs ΛCDM+ν) are not commensurable and must not be subtracted or compared as a single tier."* That sentence is the anti-self-contradiction guard your Q-M4 asks for, at the layout level.

**Q-M5 (does the coin generalize to a FORWARD prediction to register?). ANSWER: YES — register it, and I gave the theorem-conditional structural bound in EMERGENCE above.** The forward note for the next planner: *"the framework's incumbent-discrimination ceiling is structurally bounded at very-strong (set by the single rank-1-imported M_KK scale, W-2 N₃=0 corollary) until M_KK is DERIVED rather than imported; piling on dimensionless PASSes moves the random-geometry column and leaves the incumbent BF exactly where it is; the ONLY path to DECISIVE-vs-incumbent is to derive M_KK cleanly, which simultaneously resolves the m_H band-strain and the M0 inheritance."* This is the EVOI consequence of the coin metaphor and it belongs on the surface as a forward observational-priorities note — it tells the next planner that incumbent-discrimination EVOI is concentrated entirely in the M_KK-derivation gate, not in additional dimensionless predictions. Register it as a forward note keyed to the W-2 rank-1 theorem (so that if W-2 is ever falsified — a second protected dimensional invariant found — the ceiling bound is revisited). It holds, and it is the genuine cross-workshop output: **this workshop's incumbent ceiling = W-2's rank-1 theorem, viewed through the discrimination-is-dimensionality coin.**

---

## Round 2 — mack: Cross-Synthesis

### CONVERGENCE

**The final agreed position, one paragraph.** BF_spine is ONE model-selection statistic on ONE reference class (random geometry), evaluated under two evidence-weight policies that are the upper and lower bounds of that single statistic — **2000 (full structural m_H weight, b_mH = 3/2 EXACT) and 200 (ACCOMMODATION-conservative floor, b_mH = 1/2), both DECISIVE** (log₁₀ = 3.30103 and 2.30103, clearing the Jeffreys >100 floor by 1.30 and 0.30 dex respectively; Sage-exact, this turn, re-verified independently: `b_sigma + b_cs2 = 3/2 EXACT`, so dropping the two zero-discrimination factors forces b_mH = 3/2 by the canonical arithmetic, and at b_mH = 1/2 the statistic is exactly 200). phonon-first's Q-M1 resolution is **accepted in full**: the b_mH 0.5↔1.5 collision RESOLVES — it does not force a headline down-tag — because DECISIVE survives the user's ACCOMMODATION directive at the cost of one decade, not the tier. The directive (`falsifier-rigor-registry.md` row #6) is honoured by putting the 0.5 floor visibly on the surface; the tier is preserved by the fact that both layers clear >100. I accept the third-collision finding **in full and treat it as load-bearing**: the S97-D3-BF gate verdict text reads "BF_spine = 2.0×10² DECISIVE after {a0,a2}-dagger discount", but 2000→200 is a **1.0-decade** step (the b_mH=0.5 ACCOMMODATION floor, or equivalently a −1.0 strip), whereas the canonical {a0,a2}-dagger discount is **1.5 decades → 63.25** (Sage-exact `10^1.80103 = 63.24555`, confirmed against the `BF_spine_dagger` equation entity in the knowledge base). So "200 after dagger discount" mislabels the mechanism: 200 is an ACCOMMODATION-floor layer of the full spine, NOT a dagger-discounted value. Against ΛCDM+ν the operative comparison is m_H-only (the other three factors carry ZERO incumbent discrimination — σ/m=0 and c_s²=0 are also ΛCDM's values; the ν-ordering is at most a factor ~2), with a **permanent very-strong ceiling of 31.62 (log₁₀ = 1.50, NEVER decisive — 0.50 dex below the >100 floor) and a contingent anecdotal floor of ~2** under the current +1.8–5.2% m_H band-miss. The verdict is **dual-BF (branch 2)** with the narrow m_H citation-gate folded in: "DECISIVE against random geometry" is citable INTERNALLY (capstone §7.1) at both weight layers; any EXTERNAL surface appearance of "DECISIVE" requires the dual-column context INLINE (the tier-name imports the >100 claim and the reader's eye lands on the word). Branch 1 (unqualified external DECISIVE) is FORECLOSED — mechanically, not by preference — because "DECISIVE" IS m_H (the other three summed give log₁₀ = 1.80 < 2.0) and m_H is STRAINED, so the strain tag and the decisiveness tier are the same object. The three-layer independence taxonomy (algebraic ✓ Wronskian / pipeline-systematics ✓ S98 Corr=0 / theory-level ✓-for-three / OPEN-for-m_H via the W-2 rank-1 M_KK import) holds with the qualifier that the theory-level ✓ for {σ/m, c_s², ordering} is a **random-geometry-column property** — on the incumbent column those three carry zero evidence, so their independence is vacuously true there and not in play.

### DISSENT

**None. Zero surviving disagreement.** My only R1 objection — the P4-table tier mislabel ("≤31.6 ... ANECDOTAL") — phonon-first withdrew in R2 as an error in their own R1, not a disagreement with mine; my Re:P4 correction (very-strong ceiling = 31.62, NOT anecdotal; anecdotal is the band-miss floor) is accepted. Going into the verdict I hold no residual objection to any of phonon-first's R2 positions. Specifically: (i) the Q-M1 collision resolution (DECISIVE at both b_mH conventions, two evidence-weight layers of one statistic) is correct and Sage-confirmed at my end; (ii) the Q-M2 CONVERGENT-DERIVED tag (derived zero ≠ assumed zero; framework-falsifiable, ΛCDM-accommodating) is the right epistemic object and I land it; (iii) the Q-M3 honest read (KK *family* geometrically FORCED, intra-family route SCHEME-FLOATING pending a finer criterion, μ_BC=188 separately ACCOMMODATION) is the correct surface tag and gives the S102 route-gate its PASS criterion (first-principles route mandate with NO appeal to PDG agreement); (iv) the Q-M4 evidence-TYPE labels (model-SELECTION / model-COMPARISON, not commensurable) are load-bearing at the layout level and I write the anti-self-contradiction guard verbatim; (v) the Q-M5 forward structural bound (the incumbent ceiling = W-2's rank-1 theorem viewed through the discrimination-is-dimensionality coin) holds and I register it. The closest thing to a residual tension — 2000 vs 200 as the random-geometry headline — is not a disagreement: both are DECISIVE, they are two evidence-weight layers, and the dual-column carries the layer tag explicitly. The math forces us together, not apart.

### EMERGENCE

**Final landing decisions (my call as falsifier-surface owner).**

**(1) The CONVERGENT-DERIVED tag lands NOW, inside the dual-column block — not deferred.** phonon-first's Q-M2 answer is correct and the tag is a real (currently-unmeasurable) discrimination axis the surface should record: σ/m=0 and c_s²=0 are *derived* zeros (Leggett-channel CPT-neutral N_Fock=1 superselection; Kasparov-factorization pressureless mode) where ΛCDM's are *assumed* zeros (collisionless/pressureless CDM by fiat). The framework's zeros are FALSIFIABLE; ΛCDM's accommodate any value below the bound. This does NOT change the dual-column arithmetic — their incumbent log-BF stays exactly 0 today, because falsifiability is not evidence until a measurement exists — but it changes the cell ANNOTATION: I tag them `CONVERGENT-DERIVED [incumbent BF = 0 at present precision; framework-FALSIFIABLE, ΛCDM-ACCOMMODATING]` with a forward note that a future σ/m or c_s² precision bound converts the cell to a discrimination axis. It lands in the same sub-block as the dual-column because it is a property of the same two factors the dual-column prices at zero incumbent discrimination — splitting it into a separate landing would orphan the annotation from the cells it qualifies.

**(2) The 3-state refresh trigger — exact wording (pre-registered, anti-post-hoc).** The incumbent-column m_H cells are not a static landing; they are a two-state-plus-null cell whose refresh is mechanically keyed to the S102 route gate. I pin this into the sub-block verbatim: *"Incumbent-column m_H cells REFRESH on the `S102-MH-ROUTE-SELECTION` verdict per the 3-state table below; until that gate lands, the cell carries the current anecdotal FLOOR (~2) with the SCHEME-FLOATING tag, and the very-strong CEILING (31.62) is the structural maximum the route-pinned-and-band-hit state can reach. The three output states and their tier assignments are FIXED here BEFORE the gate runs — no post-hoc re-narration is admissible (`epistemic-discipline.md` Class-8.2 / `v3-closure-recovery.md` PROHIBITED_ACTIONS Class 6)."* The three states: **(a)** FORCED route found ∧ band-HIT → m_H = 31.62 (b_mH=1.5), incumbent floor lifts to the very-strong ceiling; **(b)** FORCED route found ∧ band-MISS persists → degraded → ~2 anecdotal, SCHEME-FLOATING → STRAINED-PINNED; **(c)** NO forced route (best-fit selection) → m_H exits the incumbent discriminator set entirely (ACCOMMODATION, strictly weaker than SCHEME-FLOATING). The route gate's PASS criterion (phonon-first's Q-M3 commitment, pinned as the trigger's precondition): a UNIQUE geometric mandate selecting one route from the a₄-moment KK-threshold structure, writable WITHOUT citing which route lands nearest PDG 125.25.

**(3) The cross-workshop structural bound — registered as a forward observational-priorities note.** phonon-first's Q-M5 answer is the genuine cross-workshop output and I register it on the surface and in EVOI: **the framework's incumbent-discrimination ceiling is structurally bounded at very-strong (set by the single rank-1-imported M_KK scale, the W-2 N₃=0 corollary) and CANNOT be lifted to DECISIVE-vs-incumbent by adding dimensionless predictions (they all converge with ΛCDM) NOR by adding more dimensional predictions (W-2 forbids a second independent dimensional handle); the ONLY path to DECISIVE-vs-incumbent is to DERIVE M_KK cleanly, which simultaneously converts m_H from an entangled-strained factor into a clean discriminator and resolves the M0 inheritance.** This is the EVOI consequence of the coin metaphor (discrimination and strain are the same coin, and the coin is dimensionality): incumbent-discrimination EVOI is concentrated entirely in the M_KK-derivation gate, not in additional dimensionless PASSes. It is keyed to the W-2 rank-1 theorem — if that theorem is ever falsified (a second protected dimensional invariant found, which the BDI N₃=0 class does not supply), the ceiling bound is revisited.

**(4) The evidence-TYPE layout guard lands verbatim (Q-M4).** The sub-block's header carries phonon-first's anti-self-contradiction sentence: *"Two evidence TYPES, not two estimates of one quantity: DECISIVE (model-SELECTION, vs random geometry) and very-strong-ceiling/anecdotal-floor (model-COMPARISON, vs ΛCDM+ν) are not commensurable and must not be subtracted or compared as a single tier."* A reader seeing "2000" and "~2" side by side reads self-contradiction unless the surface states they answer different questions — which is P1's thesis made load-bearing at the layout level.

**(5) The §7.3 capstone surface is PROSE, not a falsifier-TABLE cell — so the dual-column extension routes via housekeeping §B, not a mack prose edit.** I inspected the capstone §7 surface: §7.2 (lines 545–557) is the falsifier-anchor TABLE and carries NO BF_spine row (BF_spine is a §7.3 *scorecard* object, not a §7.2 detector-anchor); §7.3 line 563 is a PROSE callout box that already carries the S98 COMPUTED outcome (BF_spine = 2.0×10³ DECISIVE, model-class vs random-geometry, patched as Q2 at S98). The NEW content this workshop produces — the reference-class dual-column (incumbent very-strong ceiling / anecdotal floor; model-SELECTION vs model-COMPARISON) — is a PROSE change to that §7.3 box, NOT a status-cell edit in a falsifier TABLE. Per `capstone-hygiene-gate.md` Q4 + the task's item-2 routing, a PROSE change in a curated doc routes via the **designated-writer reviewed patch (housekeeping §B)**, NOT a mack edit. The falsifier-inventory dual-column block (which §7.3 item-(5) cites as its register-of-record) IS my domain and I land it in full; the §7.3 prose extension is the designated-writer's S102 deliverable. This is the same two-axis split the S98 annotation drew (inventory WRITTEN / capstone PROSE FLAGGED) — applied to the reference-class axis.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | Reference class for external evidence statements (question a) | P1, M1 | **Converged** | The reference class is a property of the statistic, not a free choice: BF_spine = 2000/200 DECISIVE answers "is the geometry special?" (model-SELECTION, vs random geometry, all 4 factors); the external surface's default null is the incumbent (field convention — DESI/Planck report BF vs ΛCDM), where the operative comparison is m_H-only. Two evidence TYPES, not two estimates — never commensurable. |
| 2 | Pipeline vs theory-level independence (question b) | P2, Re:P2 | **Converged** | THREE layers: algebraic ✓ (Wronskian) / pipeline-systematics ✓ (S98 Corr=0) / theory-level ✓-for-{σ/m,c_s²,ν-ordering} (dimensionless, protected) but OPEN-for-m_H (rank-1 M_KK import, `Corr(a₀,a₂)=+1`). The theory-level ✓ for the three dimensionless factors is a random-geometry-column property — on the incumbent column they carry zero evidence, so their independence is vacuously true there and not in play. |
| 3 | m_H spine-factor disposition (question c) | P3, Re:P3 | **Converged** | KEEP + STRAIN-TAG + route-gate. "DECISIVE" is mechanically m_H-only (drop m_H → 63.25 < 100; the other three sum to log₁₀ 1.80 < 2.0), so the strain tag and the decisiveness tier are the SAME object. Both metrics carried with scopes: percentage miss (+1.8–5.2%, construction-quality) and σ_PDG (13.24–38.53σ, precision-confrontation). KK family geometrically FORCED; intra-family route SCHEME-FLOATING pending S102. |
| 4 | Verdict: keep-annotated / dual-BF / down-tag (question d) | P4, M2 | **Converged** | Dual-BF (branch 2) + narrow m_H citation-gate folded in. Branch 1 FORECLOSED (mechanical: DECISIVE = m_H, m_H strained). Incumbent column has a permanent very-strong CEILING (31.62, NEVER decisive) and a contingent anecdotal FLOOR (~2, current band-miss) — both belong on the surface; the ceiling's permanence is the W-2 rank-1 corollary (the discrimination-is-dimensionality coin). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **The S102 m_H route-selection gate.** Does the framework derive a UNIQUE intra-KK-family route (KK-L5 = 127.5 vs KK-threshold = 131.8) from the a₄-moment geometry alone, WITHOUT appeal to which route lands nearest PDG 125.25? Pre-registered gate **`S102-MH-ROUTE-SELECTION`**: PASS = first-principles geometric mandate for one route (analogous to the M5 √x functional-selection for n_s); FAIL/ACCOMMODATION = route currently chosen by best-PDG-agreement. The verdict drives the dual-column 3-state refresh (state a: band-hit → ceiling 31.62; state b: band-miss persists → floor ~2 STRAINED-PINNED; state c: no forced route → m_H exits the incumbent discriminator set). The KK *family* is already geometrically FORCED; only the intra-family choice is open. Until this lands, b_mH = 1.5 is cited as SCHEME-FLOATING-within-a-forced-family (stronger than ACCOMMODATION, weaker than a pinned prediction).

2. **The incumbent BF compute (`BF_spine_vs_LCDM`).** The dual-column's incumbent values are landed as pre-registered TEXT (per-factor discrimination {σ/m=0: 0; c_s²=0: 0; ν-ordering: log10 2; m_H: ≤1.5 route-pending}, all Sage-exact this session); the explicit incumbent Bayes-factor COMPUTE is the S102 math CF (4-field below). PASS criterion: the four per-factor incumbent-discrimination values are pinned BEFORE compute (this session), so the compute cannot be narrativized post-hoc; it reproduces the ceiling 31.62 (m_H route-pinned) and the floor ~2 (band-miss) at the b_mH the random-geometry column uses (1.5).

3. **M8 external-likelihood independence (a FOURTH, explicitly-OPEN independence layer).** The three established layers (algebraic / pipeline-systematics / theory-level-for-three) are all INTERNAL — the framework certifying its own independence via the same verifier (M8 verification-monoculture). External-likelihood independence — that the spine factors' evidence is computed from statistically independent measurements with non-overlapping covariance against published posteriors (CLASS/CAMB + Planck/DESI chains rather than σ-arithmetic) — is NOT established and the surface names it OPEN rather than letting M8 catch it. This is an observational-priorities item; it does not weaken the three-layer split, it adds a named fourth layer. No pre-registered gate yet (routes to the EVOI table as a future high-leverage item if the relevant rows are identified).

4. **M_KK derivation (the only path to lifting the incumbent ceiling).** The cross-workshop structural bound says the very-strong incumbent ceiling cannot be lifted by adding observables — only by DERIVING M_KK cleanly (removing the rank-1 import that entangles m_H with the gravitational normalization, `Corr(a₀,a₂)=+1`). This is the W-2 N₃=0 corollary and it is keyed to the W-2 rank-1 theorem (revisited only if a second protected dimensional invariant is found, which the BDI class does not supply). No pre-registered gate this session; registered as the forward EVOI observational-priorities note (item 5 of Effected In-Session).

## Wrap-Up — Workshop Impact Summary

### What Changed

**(a) Numerical revisions.**
- Incumbent-comparison BF priced exactly for the first time: per-factor incumbent discrimination `{m_H: ≤1.5 route-pending; σ/m=0: 0; c_s²=0: 0; ν-ordering: log10 2 = 0.30103}` (Sage-exact, this session) → incumbent very-strong CEILING 31.62 (log₁₀ 1.50), anecdotal FLOOR ~2 (log₁₀ 0.30) under the band-miss. Random-geometry headline pinned as a layered statement: 2000 (b_mH=1.5) / 200 (b_mH=0.5 ACCOMMODATION floor) — both DECISIVE.
- Third-collision number disambiguated: `200 ≠ 63.25`. The S97-D3-BF gate text "200 after dagger discount" is a mislabel; the dagger discount is −1.5 decades → **63.25** (Sage `10^1.80103`), while 200 is the b_mH=0.5 floor (a −1.0 / ACCOMMODATION layer of the full spine).

**(b) Structural changes.**
- Single conflated DECISIVE number → **two-axis surface** (status × reference-class): the existing three-register status split is JOINED by the orthogonal reference-class index (model-SELECTION vs model-COMPARISON). The second column is additive, not redundant.
- Branch 1 (unqualified external DECISIVE) reclassified from "dispreferred" to **mechanically FORECLOSED**: "DECISIVE" IS m_H (the other three sum < the >100 floor), m_H is STRAINED, so the strain tag and the decisiveness tier are one object.
- σ/m=0 and c_s²=0 retagged `CONVERGENT` → **`CONVERGENT-DERIVED`**: a derived zero (falsifiable) is a different epistemic object from ΛCDM's assumed zero (accommodating) — a real future discrimination axis at zero incumbent BF today.
- m_H disposition promoted from a value-question to a **citation-class gate**: `SCHEME-FLOATING` + `CONVENTION-SENSITIVE` tags, the b_mH=1.5 contribution citable as forced evidence only after S102 intra-family route-pinning; the dual-column incumbent cells become a pre-registered 3-state refresh keyed to that gate.
- Cross-workshop structural bound established: this workshop's incumbent very-strong ceiling **= the W-2 rank-1 M_KK-import theorem**, viewed through the discrimination-is-dimensionality coin (only the dimensional factor can discriminate against a dimensional rival, and W-2 forbids a second independent dimensional handle).

### What Holds

- BF_spine = 2000/200 DECISIVE on the random-geometry/internal axis is a correctly-computed, legitimate model-SELECTION statistic at full weight — UNCHANGED; "DECISIVE against random geometry" stays citable INTERNALLY (capstone §7.1) at both evidence-weight layers.
- The S98 OQ3 result (rank-2 dagger LICENSED, max|Corr|=0 over the 4 spine factors) is UNCHANGED on the OQ3-spine axis; its reach is reference-class-indexed exactly as the BF is (theory-level-clean for the three dimensionless factors; the licensing is a random-geometry-column property).
- The Tier-3 borrowed-H dagger collapse (`Corr(a₀,a₂)=+1`, W7-7b RESTRICT) STANDS UNCHANGED — orthogonal to the OQ3-spine axis. Row #1 (w₀ R_842, DESI-DR3 2027-binding) is UNTOUCHED: it is the separate likelihood channel, not the model-class BF.
- The m_H 1×-evidence-weight ACCOMMODATION discipline (`falsifier-rigor-registry.md` row #6) is UNCHANGED and now visibly carried (the 0.5 floor is on the surface).

### What Breaks or Strains

- m_H band-miss against PDG 125.25±0.17: band 127.5–131.8 GeV with the measured value OUTSIDE; miss +1.796% (=13.24σ_PDG) to +5.230% (=38.53σ_PDG). Real strain against the precision datum; informative for the random-geometry null (a ~2% hit from zero knobs). Both metrics carried with scopes.
- M0 inheritance MARGINAL: the Higgs over-prediction propagates linearly into the absolute fermion normalization (`S101-M0-BCS-SCREENING` INFO, `1a1eff66`); BCS screening reduces the KK residual to −11/670 = −1.642% but does not eliminate it, `delta_consistent=False`, regime MARGINAL (the tree leg reaches 0% BY CALIBRATION, non-discriminating).
- Incumbent DECISIVE is structurally UNREACHABLE: the very-strong ceiling (31.62, log₁₀ 1.50) is permanent — no route-pinning or band-fixing lifts the incumbent BF above the >100 floor, because the three dimensionless factors converge with ΛCDM and the only discriminator caps at b_mH=1.5. The framework's incumbent reach is bounded at very-strong until M_KK is derived rather than imported.

### Carry-Forward Computations (MATH ONLY — propagate to S102)

#### CF-S102-MH-ROUTE-SELECTION

1. **What**: Derive (or fail to derive) a UNIQUE intra-KK-family m_H route — KK-L5 (127.5 GeV) vs KK-threshold (131.8 GeV) — from the a₄-moment KK-threshold structure of the transverse |S|² fiber-embedding mode, with the selection argument writable WITHOUT citing which route lands nearest PDG 125.25.
2. **Who**: phonon-first-cosmologist (a₄-moment / KK-threshold derivation owner) + connes-ncg-theorist (Higgs-sector spectral-action cross-check).
3. **Input**: the a₄ Seeley-DeWitt KK-threshold moment structure; the route set {KK-L5=127.5 / KK-threshold=131.8 / zeta=138.5-excluded / μ_BC=188-ACCOMMODATION}; `canonical_constants.py` m_H route values; `S101-M0-BCS-SCREENING` (`1a1eff66`) screened-residual lineage.
4. **Output**: gate `S102-MH-ROUTE-SELECTION` verdict — PASS (forced route, NO PDG appeal) / FAIL-ACCOMMODATION (best-fit selection); the verdict drives the dual-column 3-state refresh.
5. **Gate**: PASS = unique geometric route-mandate derivable WITHOUT appeal to PDG agreement; FAIL = route currently chosen by best-PDG-agreement (→ ACCOMMODATION surface tag).
6. **Effort**: medium (a₄-moment structure already computed; the new content is the intra-family selection criterion).
7. **Depends on**: `S101-M0-BCS-SCREENING` (`1a1eff66`, M0-inheritance lineage); the dual-column block landed this session (the refresh-trigger consumer).

#### CF-S102-BF-SPINE-VS-LCDM

1. **What**: Compute the explicit incumbent Bayes factor `BF_spine_vs_LCDM` against ΛCDM+ν, with the four per-factor incumbent-discrimination values pinned BEFORE compute (this session, anti-post-hoc per M2): `{σ/m=0: 0; c_s²=0: 0; ν-ordering: log10 2 = 0.30103; m_H: ≤1.5 route-pending}`. Reproduce the ceiling 31.62 (m_H route-pinned-and-band-hit) and the floor ~2 (current band-miss) at b_mH=1.5 (the random-geometry-column convention, so the columns are comparable).
2. **Who**: mack-cosmic-bridge (incumbent-comparison / reference-class owner) + sagan-empiricist (Bayes-factor methodology cross-check).
3. **Input**: the per-factor decomposition `s97_d3_bf.npz`; the pinned per-factor incumbent-discrimination assignment (this session, Sage-exact); the Jeffreys/Kass-Raftery tier map (this session); the `S102-MH-ROUTE-SELECTION` verdict (sets the m_H state).
4. **Output**: `BF_spine_vs_LCDM` with the 3-state incumbent value (ceiling/floor/null) and the Jeffreys tier; landed as the second BF column's COMPUTED value (currently pre-registered TEXT).
5. **Gate**: the four per-factor values reproduce as pinned (no post-hoc re-narration); ceiling = 31.62 (very-strong, NOT decisive), floor = ~2 (anecdotal) — both BELOW the >100 incumbent-DECISIVE floor by construction.
6. **Effort**: low (arithmetic is Sage-exact and pinned this session; the compute is the formal landing of pre-registered values + the route-gate-keyed state).
7. **Depends on**: `CF-S102-MH-ROUTE-SELECTION` (sets the operative m_H state); the dual-column block landed this session (the landing target).

### Effected In-Session (NON-MATH — completed by the final agent BEFORE TERMINATING)

All non-math items across both rounds enumerated and executed on disk. Box checked only after the edit is verified present.

- [x] **Dual-BF landing — Tier-partition table cell** — extended the "1 — spine" row's Joint-BF-disposition cell with the reference-class index (2000/200 DECISIVE vs random-geometry model-SELECTION; m_H-only incumbent model-COMPARISON, ceiling 31.62 very-strong NEVER decisive, floor ~2 anecdotal; "reference class is a property of the statistic") — `sessions/framework/registry/falsifier-master-inventory.md:1708` — Joint-BF Multiplicativity Qualifier Tier partition table
- [x] **Dual-BF landing — full dual-column sub-block** — new S101 BF-spine-reference-class block: two-column structure (model-SELECTION vs model-COMPARISON), per-factor table pinned Sage-exact (m_H 1.5/31.62/45.44%, σ/m 1.0/10/30.29%, c_s² 0.5/3.16/15.15%, ν log10(2)/2.0/9.12%, Σ=2000, ACCOMMODATION floor 200, dagger 63.25), incumbent-ceiling-unreachable-DECISIVE proof, CONVERGENT-DERIVED tag, m_H SCHEME-FLOATING+CONVENTION-SENSITIVE, 3-state refresh trigger keyed to `S102-MH-ROUTE-SELECTION`, both conflation guards (63.25 two-mechanism collision; 200-is-ACCOMMODATION-floor-NOT-dagger), cross-workshop structural bound, substrate-first framing — `sessions/framework/registry/falsifier-master-inventory.md:1758-1802` — surgical insert after the S98 W4-4 Cross-references line, before Row #76 (reviewed-patch, not bulk rewrite)
- [x] **Capstone-hygiene Q-routing (Q3/Q5)** — inspected the capstone §7 surface: §7.2 falsifier-anchor TABLE (lines 545–557) carries NO BF_spine row (BF_spine is a §7.3 *scorecard* PROSE object, not a §7.2 detector-anchor), so there is no falsifier-TABLE status cell to patch as Q2; the reference-class dual-column extension is a §7.3 PROSE change → routed via designated-writer reviewed patch (Q4), NOT a mack prose edit — `sessions/session-101/session-101-housekeeping.md §B` (new entry `B-CAPSTONE-§7.3-BF-SPINE-REFCLASS-S102`, after the B-CAPSTONE-Q1Q3-S102 entry) — §B routing record + 4-field S102 designated-writer spec
- [x] **knowledge-MCP registration** — `BF_spine_vs_incumbent_ceiling = 31.62` added to `canonical_constants.py` SECTION E with full provenance (session S101, gate `S98-W4-4-OQ3-COVARIANCE`, source = this workshop joining the S98 audit `0814c57f`); comment records very-strong/NEVER-decisive, the zero-incumbent-discrimination CONVERGENT-DERIVED factors, and the structural-unliftability-until-M_KK-derived note — `computations/_shared/canonical_constants.py` SECTION E — `update_constant` with full provenance (W-1 precedent)
- [x] **S97-D3-BF gate-text mislabel guard (verdict file UNEDITED)** — the gate verdict line is PERMANENT and was NOT touched; grepped both `permanent-results-registry.md` (NO match — no annotation needed there) and the inventory; the mislabel quote ("200 after dagger discount") exists in TWO non-verdict-file places, each annotated append-only with the disambiguation (200 = ACCOMMODATION floor; dagger = 63.25) — `sessions/session-98/session-98-w4-workingpaper.md:77` (MCP-audit log line) + `sessions/session-97/session-97-phonon-first-cosmologist-synthesis.md:91` (synthesis scorecard row); the canonical disambiguation is in the inventory dual-column block's conflation guards (`falsifier-master-inventory.md:1797-1802`)
- [x] **EVOI forward note (M_KK-derivation = the only ceiling-lift path)** — appended a forward observational-priorities note (not a gate) recording the incumbent ceiling 31.62, its structural unliftability by adding observables, and that incumbent-discrimination EVOI is concentrated entirely in the M_KK-derivation gate (keyed to the W-2 rank-1 N₃=0 theorem) — `sessions/evoi-framework.md:115` (new row after the S98 W4-4 BF-spine row) — minimal annotation per `evoi-prioritization.md` maintenance discipline

### Closing Line

BF_spine is one statistic on one reference class read at two evidence-weight layers (2000/200, both DECISIVE vs random geometry); against the incumbent only m_H discriminates, capping at a permanent very-strong ceiling that no dimensionless PASS can lift — only deriving M_KK can — and the surface now names which question the word "DECISIVE" answers.
