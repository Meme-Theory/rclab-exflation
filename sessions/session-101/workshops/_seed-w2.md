# Seed — Chunk w2 (S101 Wave 2)

**Date**: 2026-06-09   **Investigator**: phonon-first-cosmologist   **Source**: session-101-w2-workingpaper.md + plan-w2
**Wave summary**: 6 gates — **5 PASS** (W2-1, W2-2, W2-3, W2-5, W2-6) + **1 INFO** (W2-4); sig_5 clean (verdict lines 27/32/37/45/50/60). Structural reading: the EVOI rank-9b texture-cluster MAGNITUDE axis closes cleanly. Two independent chains. W2a (widening → carrier → knob) confirms Reading A — the charged-lepton generation envelope is ONE operator family (Pillar IV flat-band/BCS) read on three charts — and DERIVES the magnitude knob as the moduli-acoustic identity S₀ = τ_fold/T_acoustic = 95/56 (W3-10 fingerprint promoted identity→derivation). W2b (Pillar III NCG) verifies the Lemma-B and Theorem-A-clause-3 boundaries of the Connes-distance machinery, clearing the W6 STATE-PROJ landing at full strength. The lone INFO (W2-4 quark orientation) is the PRE-REGISTERED honest intermediate, and its load-bearing output is a clean **sign-theorem** (the uniform-κ greybody envelope is monotone in ω_g and cannot cross). Every verdict matches a pre-registered PASS/INFO_meaning; the W2←W1 A19 caveat-lift prerequisite resolved cleanly (W1-1 PASS, audit `194b2b3c`).

## No candidates

W2 is a content-heavy but **settled** wave: clean PASSes, one pre-registered INFO that resolves to a definitive structural theorem (not borderline evidence), no FAILs, and no contested convention. It produces ZERO Slot-1 and ZERO Slot-2 candidates under the four-condition workshop definition. Walking the rule's six "how to identify a real workshop" signals:

- **FAILs with multiple readings** — none (no FAILs in W2).
- **INFOs at borderline** — W2-4 is INFO, but it is NOT a ~0.5σ–1σ marginal detection that domain agents would read two ways. It is a clean sign-theorem: m_up/m_down(g) = (Ω^c/Ω^D)·exp(+2π·ω_g·(1/κ_d − 1/κ_u)) is strictly monotone in ω_g with crossing point ω* = ln2/(2π·0.5069) = 0.2177, and all three ω_g (gen3=0.253, gen2=0.570, gen1=1.140) exceed ω* — so gen-1 inversion (gen1<1 ∧ gen3>1) is structurally impossible for any single (κ_up, κ_down) pair on a C₂-monotone ω-ladder. The orientation-banked reading (sign=PASS) and the crossing-unmet reading (magnitude=INFO) are JOINTLY TRUE, not competing — a sign-theorem forecloses ambiguity rather than creating it. The verdict (INFO via the canonical collapse rule) and forward route (`CF-S102-QUARK-PERGEN-KERNEL`, a pre-registered solo compute) were both pinned at plan-freeze. This is "is NOT a workshop" item 1 (solo compute follow-up), not a reading divergence.
- **Cross-wave tensions** — none. The only cross-wave edges are SATISFIED DEPENDENCIES, not conflicts: W2←W1 (A19 caveat-lift) landed PASS and every W2 gate cites full confidence with no UNTRUSTED-UPSTREAM row; W2→W6/W7 (both Connes boundary gates PASS → `S101-VIIBM-STATEPROJ-LANDING` clears at full strength, no clause excluded). Surfaced below for the consolidator's awareness, not as adjudication seeds.
- **Methodology-vs-physics blurs** — none. The counting convention (`RATIO-NORMALIZED-TRACE-MEAN`) and the carrier reading (READING A) were ALREADY adjudicated at the S100a W-2/W-3 workshops; this wave EXECUTES under the locked conventions (PROHIBITED_ACTIONS Class-1 lockdown). The W_flat=9/5 EXACT-edge landing (EDGE_TOL=1e-9) is governed by the pre-registered publication-precision band-edge discipline, with a substitution chain proving 9/5 is structural (single Casimir slope: slope_lo=slope_hi to 5.6e-16) — settled, not contested.
- **Convention questions genuinely diverging** — none (the κ-triple [SPECULATION]-class status is explicitly scoped DIRECTION-ONLY in the plan and never gates a magnitude; no divergence).
- **Existing claims needing adversarial testing** — the W2b gates (W2-5 Lemma B, W2-6 Theorem A clause 3) ARE the adversarial boundary-tests of the S100a-authored Connes-machinery theorems (dispatched to `gen-physicist` for author-independence on connes's own Stage-0 text), and they PASSED at their boundaries (op-norm form exact to 2.63e-8; disconnect slope 1.000000000). Nothing remains to adjudicate.

The honest-count discipline is satisfied: a single wave producing 0 genuine workshops is a valid and expected output when its verdicts are clean and its methodology is settled upstream.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W2 ← W1 (resolved dependency, not a tension)**: every W2 gate inherits the A19 UNTRUSTED-UPSTREAM caveat-lift on the s84 L12 cache from `S101-TAU0-OPERATOR-CANONICITY` (W1-1, PASS, audit `194b2b3c`). The lift LANDED before W2 dispatch, so all W2 cache values are cited at full confidence (no UNTRUSTED-UPSTREAM extra-rows). Flagged so the consolidator does not mistake the cross-wave pin for an open edge.
- **W2 → W6/W7 (resolved feed, not a tension)**: W2-5 + W2-6 both PASS clears `S101-VIIBM-STATEPROJ-LANDING` (Wave 6) STATE-PROJ structural clauses (i)–(iii) at full strength, and `S101-CONNES-STATEPROJ-STAGE2-VERIFY` (Wave 7) inherits the landed text. W2-1 PASS makes the W-2 B6(iii) foam-protection ordering caveat dischargeable at Wave 6. These are downstream consumptions of W2's PASSes; no conflict to adjudicate.
- **EVOI rank-9b spans W2+W3 (consolidator routing note)**: the texture-cluster EVOI rank-9b row (`sessions/evoi-framework.md:59`) covers BOTH Wave 2 (magnitude axis, this chunk) AND Wave 3 (neutrino/flavor sector). The Wave-2 magnitude axis closed PASS+DERIVED here, but the rank's overall status reconciliation depends on W3 outcomes too. This is a register-status reconciliation (Q2-hygiene), already noted in `session-101-housekeeping.md` (W2 wave-log line: "EVOI rank-9b status reconciliation deferred to session wrap-up — the rank spans W2+W3") — NOT a new item, and NOT a workshop. Surfaced here only so the consolidator routes the full-rank reconciliation to `/rclab-plan` Phase-1c-REGISTERS, not to the schedule.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other]** — `CF-S102-QUARK-PERGEN-KERNEL` (already present in `session-101-w2-workingpaper.md §"Carry-Forward Computations"`): derive a generation-DEPENDENT slope structure (per-generation κ_g, or an ω_g non-monotone in C₂) that supplies the gen-1 up/down crossing W2-4 proved impossible for any uniform (κ_up, κ_down) pair; frame per-component CKM as misalignment of two rank-one dressings (E-2(e)). Pre-registered 4-field spec with PASS gate (substrate-DERIVED per-generation kernel reproduces gen-1 m_u/m_d<1 ∧ gen-3 m_t/m_b>1, orientation [SIGN] anchor preserved, both W3-9 walls + [J,D_K]=0 intact). Solo compute follow-up (rule "is NOT" item 1), consumed by `/rclab-plan`. No action needed — listed for completeness; not a schedule entry.

(No new [Q2-hygiene] items surfaced for the first time by this investigator — the EVOI rank-9b reconciliation is already catalogued in `session-101-housekeeping.md`, so there is no upstream wave-synthesis miss to log. No [Q3-wave-together] items: W2's two chains (W2a / W2b) are not N-condition-AND parallel-compute structures; W2a is a HARD 1→2→3 sequential chain and W2b is three mutually-independent boundary verifies whose AND-clearance of the W6 landing is already a downstream consumption, not a wave-AND closeout to pre-register.)

## Wave-by-wave digest (consolidator background)

**Wave 2 — Texture-Cluster Magnitude Axis (EVOI rank-9b)**. Two chains under the S100a-locked `RATIO-NORMALIZED-TRACE-MEAN` counting convention + READING-A carrier.

- **W2-1 `S101-W2-BLOCKTRACE-WIDENING` — PASS** (sign PASS ∧ magnitude PASS ∧ regime VALID; audit `78f57414`). W_flat = 1.800000 AT the 9/5 lower edge (computed 1.799999999999997, −3.11e-15 from 9/5 at the float64 gap-ratio cancellation floor; EDGE_TOL=1e-9 band-edge discipline). Multiplicity-normalized whole-block trace-mean ⟨λ²⟩_g is EXACTLY Casimir-linear at τ_fold (slope_lo=slope_hi to 5.6e-16) → W_flat=9/5 forced structurally. Strict C₂-monotone ordering confirmed on BOTH the primary (F2-flat) and secondary (F2-weighted, counting-independent) faces. Rider-1: s̄(τ_fold)=0.349106=J/3 (D-3 razor edge) published in npz `e0a79fc3…` — the ONE-DATASET pin W2-2 Leg A consumes at the same SHA. Result: the generation-envelope SHAPE is a static Casimir datum the fold preserves rather than re-keys.

- **W2-2 `S101-ENVELOPE-CARRIER-DISCRIMINATE` — PASS** (Reading A CONFIRMED; audit `463f3203`). Composite via the GATE-BLOCK OPERATOR (workshop CF-1; precedence over generic schema-v2, no conflict this run). Leg A (tracial S₀^geo=1.7743 at governing q′ read-out) ∈ PASS [1.609,1.779]; Leg B (pair-resolved coherence c_req via rank-one secular equation, ratio=0.9887) ∈ PASS [0.95,1.05], positive-cone feasible (c_req=0.4036<1/2, 19.3% margin below the √6/2 infeasibility ceiling); Leg C (ε_LX one-fiber split) = **GRADED** (graded residual 8.9e-16, scalar residual 0.892) with derived q′=0.189745 (0.134% from τ_fold). Dual read-out FIRED → magnitude DERIVED, not fingerprint. Dual-prior → posterior ≈0.9 Track A; the Reading-B revival cell (Leg-A-FAIL ∧ W_flat-PASS) did NOT fire.

- **W2-3 `S101-W3-S0-KNOB` — PASS** (audit `513e0cbf`). Exactly ONE knob survives the derivation-routed 0.01 band AFTER the GRADED structural selector: **(iii) S₀ = τ_fold/T_acoustic = 95/56, DERIVED** (W3-10 fingerprint S₀·T_acoustic=τ_fold promoted identity→derivation). N_inside_naive=2 (both (i) and (iii) inside 0.01) collapses to N_inside=1 because Leg-C GRADED selects the per-Casimir-quantum class ((iii)), demoting (i)=4/3 to a 0.52% pin-proximity shadow (2π·τ_fold=1.19381 vs (4/3)·0.9=1.2). E-3 shadow-vetting: nearest accidental neighbor 3·(7/4) at 1.053% vs (iii)'s own 0.134% derivation precision → shadow_separated. The magnitude axis closes as a DERIVED moduli-acoustic identity.

- **W2-4 `S101-W3-QUARK-COMPONENT-ORIENTATION` — INFO** (sign PASS, magnitude INFO, regime VALID; audit `833a3daf`). PRE-REGISTERED honest intermediate. (p-1) within-component C₂-DESCENDING mass map confirmed in BOTH quark kernels (D-component Ω^D=8/3, c-component Ω^c=4/3; Ω^D/Ω^c=2 exact) — same orientation as the lepton map. (p-2) up>down at gen 3 confirmed where PDG confirms it (m_t/m_b=41.28). Crossing test: gen-1 inversion NOT realized (computed m_u/m_d=18.87, not <1). The load-bearing result is a **sign-theorem**: the two-κ uniform-slope envelope is monotone in ω_g (crossing point ω*=0.2177, all ω_g exceed it; gen-1 carries the LARGEST ω_g) so gen-1 inversion is structurally impossible for any single (κ_up,κ_down) pair. The uniform-κ sub-reading's pre-declared FAIL (PDG m_u/m_d=0.4596<1) stands recorded; the orientation [SIGN] anchor for the S102 CKM program is SET; the crossing routes to per-generation kernel structure (CF-S102-QUARK-PERGEN-KERNEL).

- **W2-5 `S101-STAR-METRIC-BLOCK-LEMMA` — PASS** (clause i ∧ clause ii; audit `08ee01cb`). Lemma B verified at its operator-block (non-scalar) boundary: hub-leaf d(v,g)=1/‖S_g‖_op exact (max rel dev 2.63e-8 ≤ 1e-7 across 23 gating SDPs, CLARABEL); leaf-leaf strictly above the Pythagorean form off-alignment (min witness +8.63e-3 ≥ 1e-3) with equality restored under alignment (max 3.61e-10 ≤ 1e-7). Negative control: rotating leaf-side (right) singular vectors leaves d=P invariant — only hub-range (left) misalignment breaks joint saturation. Physical ε_LX-boundary instance on L12 cache floors confirms. Clears W6 STATE-PROJ clauses (i)–(iii) at full strength.

- **W2-6 `S101-CONNES-DISTANCE-DISCONNECT-BOUNDARY` — PASS** ((a)∧(b)∧(c); audit `9eea4708`). Theorem A clause 3 verified at its disconnect boundary: severing the (1,0) edge (t_g=0) drives the severed-pair Frobenius-regulated distance to grow with log-log slope EXACTLY 1.000000000 (|slope−1|=1.82e-11, the commutative CLASS-γ signature; d_R=c·R with c=1/√2 a multiplicative pre-factor annihilated by the log-derivative — the K=3 multiplicative-cancellation pattern on the regulator-scale axis), while connected pairs stay R-flat (1.57e-9 ≤ 1e-8) and the activation threshold ρ*=15.5816 lands at ρ*≤10·ω_max (equality). Doubling-invariant (slope on the J-doubled 16-dim operator = 1.000000000). Completes the two-sided dichotomy (finite+regulator-free iff connected; linear-in-R iff disconnected) with S100a's connected side.
