# Session 101 Workshop: volovik x phonon-first

**Date**: 2026-06-09
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: volovik (volovik-superfluid-universe-theorist), phonon-first (phonon-first-cosmologist)
**Source Documents**:
- `cold-read-s101/04-a_t-critical-path-memo.md`
- `cold-read-s101/02-referee-report-cold-read.md`
- `computations/session-101/s101_gate_verdicts.txt`
- `sessions/session-101/session-101-w4-workingpaper.md`

**Focus Topics**:
1. (a) Does S96's `Z_norm == G_DeWitt = 5.0` identification fix the substrate→seconds TIME UNIT that FRW `H(τ_now)` needs (the memo's R1 §8.3-dictionary claim that "G_N fixes both"), or only the dimensionless τ̇² kinetic coefficient? Canonical anchors: S95-W3-2-EFF-FRIEDMANN-GENRE INFO (`residual_free_normalization_count=2; free_scalars=Z_norm+V0; closed_form_exists=True`); S96-W1-QFLOW-RESIDUAL PASS (`residual_free_normalization_count_RESOLVED=0_from_W3-2_count=2; Z_norm==G_DeWitt=5.0_kinetic_FIXED_tau-flow_redundant-alias_no-canonical-write; V0=tau-flat`); G_DeWitt=5.0 (S42); a0^zeta=6440; §8.3 dictionary pins f2≈92 from M_Pl/M_KK.
2. (b) Is `V0 = τ-flat` (S96) sufficient as the a₀ vacuum subtraction the q-flow equilibrium theorem fixes (ε − μq identity, S95 W5-3), or is the absolute offset still free?
3. (c) Given (a)/(b): is the surviving §6.3 obstruction "two free normalizations" (memo) or the orthogonal pair {T6 BROKEN back-reaction closure (spectral action overwhelms the 8-mode BCS lock by 1.33e5), C2 K_pivot BROKEN}? S101 W4 results the memo could not see: S101-H0-PROPER-A2 PASS (audit cd8e8c0b, N=0.999859, H0_readout=67.40, anchor-degeneracy DISCLOSED — degenerates to H_obs at N→1); S101-W1-QEQ-SELFCONS PASS (audit c06a956b, q∝H DERIVED); S101-W0-BRANCH-IV-EVALUATOR INFO (cd0492d6, derivation-inadmissible); S101-W1-QEQ-RELIC-ODDFLOOR FAIL (98a923fd). Corr(a0,a2)=+1 (W7-7a, borrowed-H residuals co-shift).
4. (d) VERDICT: R1-survives-partial-structure (normalization recoverable, a(t) route open — pre-register the R1 gate: H(τ_now) from D_K+f+Λ alone within a factor of 2 of 67.4 km/s/Mpc, zero new continuous parameters, derived κ agrees with the S97 consistency value; with the R4-trigger as kill criterion) vs R4-trigger (normalization non-universality PERMANENT — registry theorem-tag "substrate determines conformal class, not metric normalization"; capstone §6.3 re-scope routed to designated writer; NEW capstone-hygiene Q1 change for S102, since S101 housekeeping A13 recorded Q1 a(t)-gap NO-CHANGE).

---

## Round 1 — volovik: Opening Analysis

### V1: Z_norm == G_DeWitt — time unit or kinetic coefficient only (question a)

**Key finding: `Z_norm == G_DeWitt = 5.0` fixes ONLY the dimensionless τ̇² kinetic coefficient. It does NOT fix the substrate→seconds time unit `H(τ_now)` needs. The memo's R1 §8.3-dictionary claim that "G_N fixes both" is a dimensional category conflation — and the conflation is the whole point of the non-universality reading.**

This is a dimensional-bookkeeping question, so I write the substitution chain explicitly (`math-scripts.md §"Double-Check Logic Before Compute"`; Sage-verified above).

**Step 1 — definitions of each object (cite canonical sources).**
- `Z_norm` = coefficient of τ̇² in the W3-2 closed form `H²(τ,τ̇) = Z_norm·τ̇² + [S_SA(τ) − V0]·(prefactor)` (session-96-plan-w1.md, structural equation). The S96 resolution identifies it with `G_DeWitt = 5.0`.
- `G_DeWitt = 5.0` — I queried `get_constant("G_DeWitt")`: value 5.0, S42, **source `s42_gradient_stiffness.npz`**, gate None, not superseded. This is the DeWitt **moduli kinetic coefficient** — the gradient-stiffness of the Jensen-deformation modulus. It is a pure number (the elasticity-tetrad `Z` stiffness in my own substrate→cosmology mapping: "Elasticity tetrad Z ↔ Spectral gradient stiffness Z(τ)", Volovik Papers 22/23).
- `τ` = the Jensen deformation parameter, **dimensionless** (τ_fold = 0.190 is a pure number, `get_constant("tau_fold")` = 0.19, CONST-FREEZE-42). Hence τ̇ = dτ/dt_sub carries units 1/[t_sub].
- `gamma_unit ≡ dt_SI/dt_sub` = the substrate→seconds conversion — units [s/t_sub]. This is the object `H(τ_now)` in km/s/Mpc actually needs. **It is computed by a SEPARATE gate**: the knowledge graph shows `s96_w1_mkk_seconds.py` (provenance `w1_mkk_seconds`) is a distinct script from `s96_w1_qflow_residual.py` (provenance `w1_qflow_residual`). The QFLOW-RESIDUAL gate that set `Z_norm==G_DeWitt` is NOT the M_KK→seconds gate.

**Step 2 — substitution (no simplification).** In substrate-internal units, `H_sub² = Z_norm·τ̇² + (S_SA − V0)·prefactor`. The SI Hubble rate is `H_SI = H_sub / gamma_unit = √(Z_norm·τ̇² + (S_SA−V0)·prefactor) / gamma_unit`.

**Step 3 — dimensional read-off (Sage, this session).** For `[Z_norm·τ̇²] = [H_sub²] = 1/[t_sub]²` with τ̇ carrying 1/[t_sub], **`Z_norm` must be dimensionless** — confirmed: G_DeWitt = 5.0 is a pure number. Then `∂H_SI/∂gamma_unit = −√(…)/gamma_unit² ≠ 0`, and the free-variable test returns **`gamma_unit ∈ free_variables(H_sub²)` → False**. The substrate→seconds conversion does not appear anywhere in the substrate-internal `H_sub²`.

**Step 4 — conclusion.** `Z_norm` and `gamma_unit` are **structurally orthogonal slots**: one is the dimensionless τ̇²-coefficient *inside* the substrate dynamics; the other is the dimensionful map from the substrate clock to the laboratory clock. Fixing the first (which S96 did, correctly, by identifying it with the already-pinned G_DeWitt) leaves the second untouched. **`Z_norm == G_DeWitt` pins the kinetic coefficient, not the time unit.**

**Why this is the non-universality signature, not an accident.** In my own research program this is exactly what a superfluid vacuum delivers and what it withholds. The acoustic metric `g^{μν}_acoustic` for Bogoliubov quasiparticles is fixed by the condensate's internal data — the gradient stiffness (G_DeWitt), the dispersion, the order-parameter texture (the Jensen deformation). That is *metric kinematics*: the emergent light-cone structure, the dimensionless ratios, the τ̇²-coefficient. But the *overall conformal factor* that maps the emergent metric's internal time to an external laboratory second is set by the dimensional bridge `M_KK → seconds`, which is an input scale, not an output of the eigenvalue problem. The substrate determines `c_emergent` in its own units and the *shape* of the acoustic line element; it does not hand you the second. `Z_norm == G_DeWitt` fixing the kinetic coefficient while leaving `gamma_unit` free is the textbook division between what an analog/emergent vacuum determines (kinematics) and what it does not (the dimensional normalization of the induced action). This is Sakharov non-universality in this framework's notation: the *coefficient* of the kinetic term is computable; its conversion to a Newtonian/Hubble scale in lab units is not internally closed.

**Structural implication.** S96-W1-QFLOW-RESIDUAL's `residual_free_normalization_count_RESOLVED=0` is correct as stated for the *dimensionless* free-scalar count — but it is silent on `gamma_unit`, because `gamma_unit` was never one of the two W3-2 free scalars {Z_norm, V0} in the first place. Both Z_norm and V0 are *internal* to `H_sub²(τ,τ̇)`; the seconds-conversion lives one layer up, in the readout map `H_sub → H_SI`. So S96 closed the *internal* normalization freedom and did not (and did not claim to — `no_canonical_write`) close the *dimensional* one. The memo collapses these two layers when it says "G_N fixes both."

**Questions for phonon-first.** (Q-V1a) Do you accept that the M_KK→seconds conversion is a genuinely separate slot from Z_norm, or do you read `gamma_unit` as itself derivable from {G_DeWitt, a₀^ζ, M_Pl/M_KK} via the §8.3 dictionary (f₂≈92)? If the latter, write the explicit functional `gamma_unit = Φ(G_DeWitt, M_Pl/M_KK, …)` — I claim it cannot be written without re-importing a dimensionful scale (M_KK in GeV, or ℏ/M_KKc² in seconds) that is an *input*, not a spectral output. (Q-V1b) The §8.3 dictionary pins `f₂ ≈ 92` from `M_Pl/M_KK`. But `M_Pl/M_KK` is *itself* a dimensionless ratio of two input scales — it does not contain a second. Does the dictionary anywhere produce a quantity with units of time from D_K eigenvalues *alone*?

### V2: V0 = τ-flat as the a₀ vacuum subtraction (question b)

**Key finding: `V0 = τ-flat` (S96) is a statement about V0's τ-*dependence* (it is constant in τ), NOT about V0's *value*. The q-flow equilibrium theorem (ε − μq identity, S95 W5-3) fixes the absolute offset to ZERO *at equilibrium* — which is exactly my own equilibrium theorem (`ρ_Λ = 0` at equilibrium, Volovik Paper 05). But the cosmology §6.3 needs `a(t)` precisely in the regime where the substrate is the post-fold quenched GGE relic — a NON-equilibrium state. The equilibrium V0 = 0 is a theorem; whether it survives as the operative vacuum subtraction off-equilibrium is the open question, and the QEQ-SELFCONS slope-1 result is silent on it (the slope cancels V0 entirely).**

**Step 1 — what "τ-flat" asserts vs what "fixed" would assert.** `V0 = τ-flat` means `dV0/dτ = 0`: V0 is the τ-independent additive constant in `S_SA(τ)` (session-96-plan-w1.md, Step 1 definition: "V0 = potential offset / vacuum subtraction — the τ-independent additive constant in S_SA"). This is a *form* statement. It does NOT assert a numerical value. The W3-2 closed form is `H² = Z_norm·τ̇² + (S_SA(τ) − V0)·prefactor` — so V0 enters as an additive shift of the potential term. A τ-flat V0 means the shift is rigid across the deformation; it says nothing about how large the shift is.

**Step 2 — the equilibrium theorem DOES fix the value, but only at equilibrium.** The ε − μq identity (S95 W5-3) is the framework's child-realization of my equilibrium theorem. In q-theory (Volovik Papers 13/25; my mapping "q-theory 4-form vacuum variable ↔ volume-preserving Jensen"), the thermodynamic potential is `w̃ = ε(q) − μq`, and at equilibrium `dε/dq = μ` forces the *effective* vacuum energy density `ρ_vac = w̃(q_eq) = ε(q_eq) − μ·q_eq` to vanish identically — `ρ_Λ = 0` at equilibrium (my permanent theorem, Paper 05; "Equilibrium theorem → Λ_eq = 0"). So the equilibrium theorem fixes the absolute offset: at `q = q_eq`, the a₀ vacuum subtraction is exactly the self-tuned value that zeroes the gravitating vacuum energy. This is the framework's genuine CC-mechanism, and it is microscopically grounded — it is NOT a free parameter at equilibrium.

**Step 3 — the regime mismatch (the load-bearing point).** §6.3 needs `a(t)` for the *observed* cosmology, where the substrate is the post-fold **GGE relic** — a quenched, non-thermal, non-equilibrium state (my mapping "Non-equilibrium superfluid (GGE) ↔ Quenched substrate post-fold", Papers 27/34; "GGE never thermalizes", t_therm ≈ 6 M_KK⁻¹ but the relic occupations are frozen at the diabatic transit, R_therm = 5252). Off-equilibrium, `q ≠ q_eq` in general, and `w̃(q) = ε(q) − μq ≠ 0`. The equilibrium identity that pins V0 = 0 is a statement at the fixed point; the operative cosmology runs the dynamics *around and away from* that fixed point. So the question "is V0 = τ-flat sufficient as the a₀ vacuum subtraction the q-flow equilibrium theorem fixes" splits:
- **At equilibrium**: YES — V0 is fixed (to the ρ_Λ = 0 value) by the ε − μq identity, not free. This is solid and microscopically grounded.
- **Off-equilibrium (the cosmological regime)**: the absolute offset of the *operative* `S_SA − V0` is NOT pinned by the equilibrium identity alone — it requires knowing the non-equilibrium correction `w̃(q(τ)) − w̃(q_eq)`, which is a dynamical quantity, not the equilibrium constant.

**Step 4 — what the S101 W4 QEQ-SELFCONS result actually says about V0 (Sage-verified this session).** S101-W1-QEQ-SELFCONS (PASS, audit c06a956b, my own gate) reports `kappa_invariant=True`: re-running at 10×C3 gives slope 1.000015 (|Δ| = 5.9e-5 ≤ 1e-3). I verified the structural reason exactly: with `q_amp ∝ t⁻¹` and `|H| = (2/3)t⁻¹`, the log-slope `d ln q_amp / d ln|H| = 1` **independent of A_amp and of the closure constant C3 = κ²/3** (which carries a₀^ζ = 6440 and the V0 subtraction). **The slope-1 observable cancels V0 entirely.** This is the crux for the non-universality reading: the W4-1 PASS is a genuine substrate result, but it is a *ratio/shape* observable — it demonstrates the tracking *exponent* n = 2 emerges, and it is *blind to the absolute offset V0 by construction*. So QEQ-SELFCONS cannot be cited as evidence that V0 is pinned; it is evidence that the *slope* doesn't care what V0 is.

**Structural implication.** V2's answer is layered, and the layering matters for the verdict. The equilibrium theorem fixes V0 at the fixed point (microscopically — this is the framework's real strength on the CC, and it is NOT non-universal). But the cosmological `a(t)` lives off-equilibrium, where the operative subtraction is `ε(q(τ)) − μq(τ)`, a dynamical quantity. "τ-flat" constrains its τ-*shape* (rigid additive constant) but not its off-equilibrium *value*. And the one S101 W4 result that touches this regime (QEQ-SELFCONS) produces a V0-blind ratio. So V0 is **fixed at equilibrium, form-constrained but value-open off-equilibrium** — and the off-equilibrium regime is the one §6.3 needs.

**Questions for phonon-first.** (Q-V2a) Does the ε − μq identity, evaluated on the *quenched GGE relic state* (not the equilibrium fixed point), still give a τ-flat AND value-fixed V0? My reading is that the equilibrium identity zeroes `w̃` only at `q = q_eq`; on the frozen-relic state `q(τ)` evolves (QEQ-SELFCONS itself shows q oscillating with q̄ = 0.4122, ω_q = 59.89), so `w̃(q(τ)) ≠ 0` generically. (Q-V2b) If you claim V0 is fully pinned, identify the gate that measures the *absolute* a₀ offset on the post-fold state — not a slope, not a ratio. The QEQ-SELFCONS slope is V0-cancelling (Sage-confirmed `d ln q_amp/d ln|H| = 1` independent of C3); it cannot be that gate. (Q-V2c) Note the relic-side complication: S101-W1-QEQ-RELIC-ODDFLOOR FAILed (audit 98a923fd) — the relic kernel's reactive part places ω_q^phys = 2.0128 M_KK *inside* the pair band with a live resonance and a non-zero odd-in-H force floor (|c_odd|/|c_even| = 2.70e-2 ≫ 1e-3). Does an off-equilibrium V0 inherit a similar relic-induced correction that the equilibrium identity does not capture?

### V3: The surviving §6.3 obstruction — two normalizations vs {T6, C2} (question c)

**Key finding: the §6.3 obstruction is NEITHER cleanly "two free normalizations" (memo, post-S96) NOR cleanly the orthogonal pair {T6, C2}. It is ONE structural obstruction wearing four-to-five costumes, all of which are the SAME thing: the substrate determines dimensionless/conformal data (kinematics) but not the dimensional metric normalization (the seconds, the absolute scale of the induced Einstein term). T6 and C2 are not orthogonal to {Z_norm, V0} — they are the SAME non-universality manifesting at the dynamical-closure layer instead of the closed-form-readout layer. This is the steelman's central claim.**

**The four-to-five costumes (the memo's §3 graveyard, re-read through my program).** Every route in the S74→S100a graveyard succeeds on ratios/shapes/conservation and fails on absolute normalization. Cataloguing the residuals:
1. **{Z_norm, V0}** (S95 W3-2): the two free scalars of the closed-form `H²(τ,τ̇)`. S96 resolved both as *internal* dimensionless freedoms (Z_norm = G_DeWitt; V0 = τ-flat) — but per V1/V2 above, the *dimensional* slot `gamma_unit` and the *off-equilibrium absolute* V0 remain.
2. **κ** (S97 W1-5): the τ̇-shape knob, pinned only consistency-forced (κ_nat = 8.86×10⁻⁴² s — and note that "s" unit: κ carries the seconds the closed form does not). The memo notes it is "consistency-forced, not independently triangulated."
3. **The q-frame / deceleration normalization** (S98: AOFT frame conformally stationary, a_eff const to 7×10⁻⁷ ⇒ q = 0/0; S100a: q invariant under exactly the corrections a constant normalization supplies). This is the *conformal-class-but-not-representative* statement made directly: the substrate fixes the conformal class, and q (a representative-dependent quantity) is undefined within it.
4. **m_D irreducibly external** (S100a) — the matter-sector twin: the Dirac neutrino scale is not derived, it is back-solved from oscillation splittings.
5. **M₀ ← m_H linear inheritance** (S100a; confirmed this session by **S101-M0-BCS-SCREENING INFO**, audit 1a1eff66: the absolute fermion normalization M₀ inherits the m_H scale through a first-power level transfer, `convention=FIRST-POWER-MH-LEVEL-TRANSFER-PRIMARY`). The absolute fermion mass scale rides on an externally-anchored Higgs scale.

**Are these one obstruction or several? The structural test.** They are one iff they all live on the same side of a single kinematics/dynamics cut AND none is independently closable while the others remain open. I claim YES:
- {Z_norm, V0}, κ, and the q-frame are all **dimensionless-or-conformal-data that the spectral triple fixes, missing exactly the one dimensionful map to lab units** (V1's `gamma_unit`). They are the *gravitational-sector* face.
- m_D and M₀←m_H are the **matter-sector face of the identical gap**: the spectral triple fixes mass *ratios* (the Yukawa structure, the ordering, the Casimir-graded hierarchy) but not the *absolute* scale-setting — which is imported (m_H from the EW VEV anchor; m_D from oscillation data). This is the SAME "ratios yes, absolute normalization no" pattern, transposed from the metric to the mass matrix.

The unifying statement in my program's language: **a Bogoliubov/BCS emergent vacuum fixes the dimensionless content of the induced action (the conformal class of the acoustic metric, the dimensionless coupling ratios, the mass ratios) but the overall dimensional normalization of the induced Einstein-Hilbert term — Sakharov's `G_induced ∝ Λ_cutoff²` — is set by the cutoff scale (M_KK), which is an input.** That is precisely Sakharov non-universality. It is ONE obstruction because there is ONE missing ingredient (the dimensional closure of the cutoff scale to lab units), and it surfaces in five places because the induced action has five places where an absolute scale is needed.

**Now the honest part — where the memo's "two free normalizations" framing is INCOMPLETE and where {T6, C2} enter.** The memo (post-S96) says the gap is "two free normalizations." This is incomplete in BOTH directions:
- It is too *generous* to closure: S96 resolved the *count* of internal dimensionless free scalars to 0, but the memo's R1 then proposes recovering `gamma_unit` from "the same dictionary" — which V1 shows cannot be done from spectral data alone. So "two free normalizations, both pinnable from the §8.3 dictionary" is the over-optimistic reading.
- It is too *narrow* on the obstruction: {Z_norm, V0} are the closed-form-*readout*-layer costumes. **T6 and C2 are the dynamical-*closure*-layer costumes of the same gap**, and they are NOT reducible to {Z_norm, V0}:
  - **T6 BROKEN** (Friedmann–BCS locking; spectral action overwhelms the 8-mode BCS lock by **1.33×10⁵**): this is the statement that the *full* spectral action (155,984 modes; my memory: 155,984 eigenvalues with multiplicity, 78,080 unique) does not dynamically lock to the 8-mode BCS sector that would set the FRW scale factor. This is a back-reaction-closure failure: the substrate's own gravitational dynamics (a₂-sector, 155,984 modes) and its order-parameter-lock sector (8-mode BCS) do not couple at the strength needed to *self-source* `a(t)`. The factor 1.33×10⁵ is a dynamical mismatch, not a normalization ambiguity.
  - **C2 K_pivot BROKEN**: the pivot-scale matching that would tie the substrate's spectral pivot to the observed CMB pivot is broken-with-pathway.

**The relation between the two layers (the precise claim).** {Z_norm, V0, κ, q-frame} (readout layer) and {T6, C2} (closure layer) are **the same non-universality at two layers of the construction**, NOT orthogonal obstructions. Here is the structural argument: IF the substrate determined the metric normalization (not just the conformal class), THEN the dimensional `gamma_unit` would be fixed (V1 closed), the absolute V0 would be fixed off-equilibrium (V2 closed), AND the back-reaction would close at the right strength (T6: the gravitational sector would lock to the BCS sector at unit strength, not 1.33×10⁵ off). The 1.33×10⁵ overwhelm IS the dynamical face of "the a₂-sector spectral action sets its own scale, decoupled from the BCS order-parameter scale that the cosmology needs" — i.e., the two scales are not universally related, which is the same non-universality. So {T6, C2} are not a *second* obstruction; they are the *same* obstruction seen dynamically. The memo's "two free normalizations" mis-locates the gap at the readout layer only; the deeper expression is the closure-layer T6 mismatch.

**Honest concession populating V1/V2 of the steelman (where the PERMANENT reading is punctured or strained).** I must be honest per my charge:
- **S96 genuinely closed the dimensionless internal count.** `residual_free_normalization_count_RESOLVED=0` is real and correct *for the internal free scalars*. The non-universality reading does NOT get to claim Z_norm/V0 are "still free as dimensionless internal parameters" — they are not. The PERMANENT reading must be precise: what survives is the *dimensional* slot (gamma_unit) and the *off-equilibrium absolute* offset, not the internal count. This narrows the steelman but does not break it — Sakharov non-universality was always about the *dimensional* coefficient, not an internal dimensionless count.
- **S101-H0-PROPER-A2 PASS is the sharpest puncture-candidate**, and I address it head-on in V4. Preview: it does NOT puncture the dimensional reading, because (Sage-confirmed) its `N` is a dimensionless G_N-ratio that degenerates to the external anchor as N→1; the km/s/Mpc unit enters through the external `H_obs`, not from D_K+f+Λ.

**Structural implication.** The §6.3 obstruction is ONE thing (kinematics-but-not-dynamics / conformal-class-but-not-normalization), with the readout-layer costumes {gamma_unit, off-eq V0, κ, q-frame} and the closure-layer costumes {T6, C2} and the matter-sector costumes {m_D, M₀←m_H}. This is the registry-theorem-shaped object the R4 trigger would name. The S96 closure is a real partial result *inside* the obstruction (it killed the internal dimensionless freedom), not a refutation of it.

**Questions for phonon-first.** (Q-V3a) Do you read T6 (1.33×10⁵ overwhelm) as the *dynamical* face of the same non-universality as {gamma_unit, V0}, or as a genuinely independent failure (e.g., a fixable coupling-strength miscalculation)? If independent, what would close T6 *without* closing the dimensional normalization? (Q-V3b) Corr(a₀,a₂) = +1 (W7-7a): the borrowed-H residuals co-shift perfectly. I read this as direct evidence that the dagger-row observables share a SINGLE hidden parameter (the borrowed H normalization) — i.e., they are all downstream of the one missing `gamma_unit`. Do you agree the +1 correlation is the signature of a single missing normalization, not several?

### V4: Cross-Cutting Observations (genre wall: kinematics vs dynamics; verdict posture on d)

**Verdict posture: R4-TRIGGER. I steelman that normalization non-universality is a PERMANENT, registry-theorem-shaped finding — "the substrate determines the conformal class of the emergent cosmology, not its metric normalization" — and the honest move is the theorem-tag plus a §6.3 re-scope to conformal-class-complete. The R1 gate should still be *pre-registered* (the kill criterion is the deliverable), but my first-principles expectation is that R1 hits its kill criterion, because the missing ingredient is dimensional, not a piece of unfinished algebra against existing pins.**

**The genre wall, from inside my own program (not the memo's training-sourced gestures).** The memo §2/§3 invokes "Volovik's verdict" that superfluid vacua give kinematics not dynamics, Sakharov non-universality, and Barceló–Liberati–Visser. **These are correct attributions, but the memo carries them as training-recalled literature pointers — mark them UNVERIFIED-AS-CITED.** I can supply the *substrate-internal* version, which does not depend on those external citations being correctly recalled, because it is a theorem in THIS framework:

> **N₃ = 0 is the framework's own statement of the genre wall.** My permanent theorem (S44, "N_3=0 (3He-B class, NOT 3He-A): vacuum energy unprotected by Fermi-point topology; q-theory required"): the substrate is in universality class BDI (3He-B child), with momentum-space topological charge N₃ = 0. In 3He-A (DIII, N₃ = 2) the Fermi point *protects* an emergent Weyl fermion + emergent gauge field + emergent metric with an effective Lorentz invariance whose dynamics are partially topologically locked. In 3He-B (BDI, N₃ = 0) **the vacuum is fully gapped and the emergent metric is NOT topologically protected** — which is exactly why q-theory is *required* to handle the vacuum energy (it cannot be protected into place by a Fermi-point invariant). The framework already chose, and proved, that it lives in the class where the induced gravity is non-universal at the level of the topological protection. The kinematics-not-dynamics wall is not an external genre fact imported by analogy; it is `N₃ = 0`, a computed substrate invariant.

This is the deepest version of the steelman: **the framework's own topology says the metric normalization is unprotected.** A protected quantity (like the chiral anomaly coefficient in 3He-A, or N₃ itself) is universal — independent of microscopic detail. An *unprotected* quantity (the induced-Einstein-Hilbert normalization in a fully-gapped BDI vacuum) is non-universal — it depends on the cutoff scale M_KK, which is an input. So `gamma_unit` (V1), the off-equilibrium absolute V0 (V2), and the T6 lock-strength (V3) are all unprotected dimensional data. The conformal-class data (mass ratios, n_s-class tilts, A_FS, growth shapes, the ZFP spine) are the *protected/computable* content. That split — protected dimensionless vs unprotected dimensional — IS the conformal-class-complete statement, derived from N₃ = 0.

**Addressing S101-H0-PROPER-A2 PASS head-on (my charge requires this; the anchor-degeneracy disclosure is the key).** This is the sharpest puncture-candidate for the PERMANENT reading, and the einstein-theorist who ran it (§W4-4) disclosed the exact reason it does NOT puncture:
- The gate computes `N ≡ M_SA/(4·M_Pl,unred,obs) = 0.999859`, `|N−1| = 0.000141 ≤ 0.05`, and reports `H0_readout = 67.40`, `G_N_FW/G_N_obs = 1.000000`, with **`anchor_degeneracy_DISCLOSED — degenerates to H_obs at N→1`**.
- **What N actually is: a dimensionless G_N ratio, not a seconds-valued H₀.** Sage-confirmed this session: with the reported structure `H0_readout = N · H_obs`, the limit `N→1` gives `H0_readout → H_obs` identically, and `dH0_readout/dN|_{N=1} = H_obs`. At the realized N = 0.999859, `H0_readout = 0.999859 × 67.4 = 67.390` — i.e., the readout is the *external anchor* 67.4 nudged by a dimensionless factor that is pinned to ≈1 *by the band* `|N−1| ≤ 0.05`. The km/s/Mpc UNIT enters through `H_obs`, NOT from D_K + f + Λ.
- **Is it an independent H₀ derivation or a consistency identity? It is a consistency identity.** N tests whether the substrate's spectral-action `M_SA` reproduces the (externally-known) reduced Planck mass via the spinor factor 4 = √16 — i.e., whether `G_N_FW = G_N_obs` (a *dimensionless* ratio of two ways of getting Newton's constant). It does, beautifully (N = 0.999859, a real and creditable result — the a₂ → G_N emergence chain is internally consistent to 1.4×10⁻⁴). But "G_N_FW/G_N_obs = 1" is a statement about the *dimensionless* matching of the gravitational coupling, NOT a substrate-internal computation of `H₀` in seconds. The H₀ = 67.40 readout is `N × H_obs` — it *reads back* the anchor it was checked against. **This is V1's `gamma_unit` slot wearing the H0-PROPER-A2 costume**: the gate confirms the dimensionless content (G_N ratio = 1) and inherits the dimensional content (the second, via H_obs) from outside. The anchor-degeneracy disclosure is the einstein-theorist honestly flagging exactly this. So H0-PROPER-A2 *strengthens* the conformal-class reading rather than puncturing it: it shows the substrate nails the dimensionless gravitational matching and still imports the second.

**Where the PERMANENT reading IS punctured or strained (honest population of V1/V2 of the steelman).**
- **(Puncture-strain 1) S96 genuinely closed the internal dimensionless count.** Conceded in V3: `residual_free_normalization_count=0` is real for the internal scalars. The PERMANENT reading must NOT claim Z_norm/V0 are "free dimensionless parameters" — they aren't. It claims only that the *dimensional* normalization (gamma_unit) and the *off-equilibrium absolute* offset are non-universal. If a future gate writes `gamma_unit` from D_K eigenvalues alone (no imported GeV/seconds scale), the PERMANENT reading is FALSIFIED. That is the kill criterion, and it is the right one.
- **(Puncture-strain 2) QEQ-SELFCONS shows the *exponent* n=2 is genuinely DERIVED.** The slope-1 back-reaction (my own W4-1 gate) is real substrate dynamics, not a fit — the w=0 dust attractor `a ∝ t^{2/3}` establishes self-consistently (a-exponent 0.6554 vs 2/3). This means the framework DOES derive the *shape* of the late-time expansion (the tracking exponent), which is more than "conformal class" in the narrowest sense — it is conformal-class-PLUS-tracking-dynamics. The PERMANENT reading should be stated as "conformal class + dimensionless dynamical shapes, NOT the dimensional normalization," not "conformal class only." This strengthens the framework relative to the bare R4 floor and should be reflected in the §6.3 re-scope language: what dies is the *seconds-valued* a(t), not the *shape* of a(t).
- **(Non-puncture) the matter-sector twins (m_D, M₀←m_H) REINFORCE the reading**, they do not strain it — S101-M0-BCS-SCREENING (INFO, this session) confirms M₀ rides the m_H scale at first power. Same non-universality, matter face.

**The verdict, stated precisely.** R4-trigger, with the theorem-tag worded to match exactly what is proven:
> **Normalization Non-Universality (N₃=0 corollary):** The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology — all dimensionless ratios, orderings, tilts, growth shapes, AND the dimensionless late-time tracking exponent (n=2, S101-W1-QEQ-SELFCONS) — but does NOT determine the dimensional metric normalization (the substrate→seconds conversion `gamma_unit`; the off-equilibrium absolute vacuum offset; the Friedmann–BCS lock strength T6). The dimensional content is set by the cutoff scale M_KK, an input. This is unprotected (non-universal) because N₃ = 0 (BDI class): the induced metric carries no Fermi-point topological protection. PROVEN-corollary status pending the R1 kill-criterion gate; the R1 gate's FAIL is the theorem's confirmation, its PASS is the theorem's falsification.

**Why I expect R1 to hit its kill criterion (the first-principles case, not a prediction-as-evidence).** The R1 gate asks: `H(τ_now)` from D_K + f + Λ alone within a factor of 2 of 67.4 km/s/Mpc, zero new continuous parameters. By V1's dimensional argument, producing a number in km/s/Mpc requires a quantity with units of inverse-time built from spectral data alone. D_K eigenvalues are dimensionless (or in M_KK units); `f` (the gauge connection) and `Λ` (the spectral cutoff = M_KK) supply M_KK as the ONLY dimensionful input. To get seconds you need ℏ/(M_KK c²) — i.e., you must import the GeV→seconds bridge, which fixes the answer *only because* M_KK is externally calibrated. That is not "zero new continuous parameters from D_K alone"; it is one imported dimensional scale (M_KK in GeV) doing all the work. So I expect R1 to either (i) FAIL the zero-new-parameter clause (it secretly imports M_KK-in-GeV), or (ii) produce a factor-of-2 hit *because* M_KK was tuned to M_Pl/M_KK ≈ f₂ ≈ 92 (the §8.3 dictionary), which is the same import. Either way the kill criterion fires.

**The honest caveat on my own posture (per `feedback_r1-overconfidence-test-before-claim`).** I ran the dimensional test (V1, Sage) WITHIN this R1 dispatch rather than asserting it — `gamma_unit ∉ free_variables(H_sub²)` is computed, not claimed. But my "expect R1 to hit its kill criterion" is a first-principles expectation, NOT a verdict; the R1 gate must actually run, and if phonon-first can write `gamma_unit = Φ(spectral data only)` explicitly, I will concede. The R4-trigger I advocate is: pre-register R1 with the theorem-tag as the kill criterion (per the memo's own decision discipline, and the capstone-hygiene Q1 routing), run it in S102, and tag the theorem on FAIL. This is NOT "skip R1 and declare PERMANENT" — it is "pre-register the theorem so R1's FAIL lands it cleanly, and the 27-sessions-of-gravitational-pull-toward-one-more-reframe risk is closed by the kill criterion."

**Cross-reference for the verdict table.** V1 → Topic 1 (Z_norm is kinetic coefficient only; time-unit slot orthogonal). V2 → Topic 2 (V0 fixed at equilibrium, value-open off-equilibrium where §6.3 lives). V3 → Topic 3 (one obstruction, five costumes; {T6,C2} are the dynamical face, not orthogonal). V4 → Topic 4 (R4-trigger; N₃=0 is the substrate-internal genre wall; H0-PROPER-A2 is a dimensionless consistency identity that imports the second).

**Questions for phonon-first (verdict-level).** (Q-V4a) Do you read H0-PROPER-A2's N=0.999859 as an independent H₀ derivation or, as I argue (Sage-confirmed degeneracy to H_obs at N→1), a dimensionless G_N-ratio consistency check that reads back the external anchor? This is the single most decisive disagreement-point for R1-survives vs R4-trigger. (Q-V4b) If you favor R1-survives, commit NOW (before any S102 compute) to the explicit functional `gamma_unit = Φ(D_K spectral data, f, Λ)` you expect R1 to produce, with the dimensional bookkeeping shown — so the R1 gate tests a pre-registered map, not a post-hoc one. (Q-V4c) Do you accept N₃ = 0 (BDI, S44) as the framework's *internal* statement of the kinematics-not-dynamics wall — making the genre claim a substrate theorem rather than an analogy to external superfluid-gravity literature?

---

## Round 1 — phonon-first: Response & Cross-Synthesis

### Part 1: Response to volovik's Sections

#### Re: V1 — Z_norm == G_DeWitt: time unit or kinetic coefficient

**AGREE — and I will engage the falsification path head-on rather than around it.** The structural skeleton of V1 is correct, and I reproduced its two load-bearing computations independently this session (Sage, my own dispatch, not transcribed from V1):

1. *The free-variable test.* `H_sub² = Z_norm·τ̇² + (S_SA − V0)·prefactor`. Sage `H_sub_sq.variables()` returns `(S_SA, V0, Z_norm, prefactor, taudot)` — **`gamma_unit ∉ free_variables(H_sub²)`** (Sage `gamma_unit in fv → False`), while `dH_SI/dgamma_unit = −√(…)/gamma_unit² ≠ 0` (Sage `bool(diff(H_SI, gamma_unit) != 0) → True`). The seconds-conversion is genuinely a slot the substrate-internal `H_sub²` does not contain.
2. *The dimensional read-off.* `[Z_norm] = [H_sub²]/[τ̇²] = (1/[t_sub]²)/(1/[t_sub]²) = 1` — dimensionless, confirming `G_DeWitt = 5.0` is a pure number (`get_constant("G_DeWitt")` = 5.0, S42, `s42_gradient_stiffness.npz`, not superseded).

So Z_norm and gamma_unit are structurally orthogonal slots, and `Z_norm == G_DeWitt` pins the first, not the second. I concede this cleanly.

**Now the head-on engagement with Q-V1a/Q-V1b — the part my charge requires.** My R1 assignment was to write `gamma_unit = Φ(spectral data only)` if it can be written. I traced every candidate chain to its dimensional source, and **every one routes through an imported dimensional scale.** Here is the audit, with the gate that proves it:

- *The M_KK→seconds gate (Q-V1b, decisive).* `search_knowledge` surfaced **S96-W1-MKK-SECONDS: PASS** (`value='M_KK_inv_seconds=8.860440e-42_s'`, `scheme=SI-dimensional-chain-hbar-over-E`, `convention=natural-units-to-SI-M_KK`). I reproduced it: `kappa = ℏ_SI / (M_KK_GeV · GeV_to_J) = 8.86043988e-42 s` (Sage, match to canonical 1.4e-16). The substitution chain is explicit: the **second** in this number comes from **ℏ (J·s, an SI input)** divided by an **energy (M_KK in GeV, an external calibration)**. *Neither factor is a D_K eigenvalue.* The gate's own cross-check confirms the hazard: it records `S52_crosscheck=FAIL_47.5OOM_legacy_units_bug` — when the external dimensional bridge was mis-applied, the answer was 47.5 OOM off, precisely because the dimensionful scale, not the spectrum, does the work.
- *The §8.3 dictionary (Q-V1b, the memo's R1 hope).* `f₂ ≈ 92` is pinned by `M_Pl/M_KK`. But `M_Pl/M_KK` is a ratio of two input scales — **a dimensionless number**. A dictionary that produces dimensionless ratios cannot produce a quantity with units of time; you cannot divide two energies and recover a second. To answer Q-V1b directly: **the §8.3 dictionary produces NO quantity with units of time from D_K eigenvalues alone.** It fixes `f₂`, which is exactly the kind of dimensionless content the substrate computes well, and is silent on the second by dimensional analysis.

**Answer to Q-V1a, stated explicitly:** I accept that M_KK→seconds is a genuinely separate slot from Z_norm. I do **not** read `gamma_unit` as derivable from `{G_DeWitt, a₀^ζ, M_Pl/M_KK}` via the §8.3 dictionary. The explicit functional volovik challenged me to write is:
> `gamma_unit = ℏ / (M_KK · c²)` in seconds — but `M_KK` here is M_KK-in-GeV, an externally-calibrated input, and `ℏ` is an SI input. There is **no** form `gamma_unit = Φ(dimensionless spectral data)` because the codomain (seconds) cannot be reached from a dimensionless domain. The map requires importing one dimensionful scale.

This is a concession of V1's core, with the dimensional bookkeeping shown, per the falsification-path discipline volovik pre-committed to (write Φ explicitly, or concede).

**MISSED — what the cross-domain pattern view adds that V1's dimensional bookkeeping alone does not.** V1 frames this as "the textbook division between what an emergent vacuum determines (kinematics) and what it does not." Correct, but the framework already has this exact structure named in **three other pillars**, and recognizing the isomorphism is what upgrades "Sakharov non-universality" from an analogy to a *substrate-internal recurrence*:
- *Pillar IV/V (the analog-gravity acoustic metric, my home).* The BLV acoustic metric `g^{μν}_acoustic = (ρ/c)·[diag(−1; δ^{ij})]` in the lab is built from the condensate's `c` (sound speed) and `ρ` (density). The substrate fixes the *conformal structure* of the line element (the light-cone shape, the dimensionless dispersion); the *overall conformal factor* `(ρ/c)` that converts internal coordinate-time to a laboratory second is set by the dimensional condensate scale — an input. My MEMORY: "BLV acoustic metric is dead at N_pair=1 (S53); Connes distance replaces it for expansion." The Connes-distance replacement is **also** a dimensionless metric (it returns ratios of eigenvalue gaps); it inherits the same property — it determines the conformal class and not the dimensional normalization. So the death of BLV and its replacement by Connes distance did NOT change the dimensional status; both deliver conformal data.
- The pattern: *every metric the substrate hands you — acoustic (IV), Connes (NCG, III), the W3-2 closed-form `H_sub²` — is dimensionless-in-its-own-units by construction.* The single dimensional bridge (M_KK→seconds) sits **outside** all of them, in the readout map. This is one structural fact appearing in three guises, not three separate facts.

**EMERGES — the readout-map factorization.** Combining V1's free-variable result with the acoustic-metric pattern: the emergent cosmology factorizes as `[dimensionless substrate dynamics on the spectral triple] ⊗ [one dimensional readout scale M_KK]`, and the tensor product structure is exactly why `gamma_unit` is a separate slot. This is the same factorization as my registry result **R_BG = 1/cosh(2r)** (S93 W8-6): the pre/post-fold bridge-coefficient *ratio* is a dimensionless SU(1,1) squeeze weight, locked; the absolute coefficient magnitudes carry the squeeze parameter that sets the scale. The framework keeps producing locked dimensionless ratios with one free overall scale. V1 found this slot in the Friedmann readout; it is the same slot everywhere.

#### Re: V2 — V0 = τ-flat sufficiency

**AGREE on the layered structure; the form-vs-value distinction is exactly right and is the crux.** `V0 = τ-flat` asserts `dV0/dτ = 0` (a *form* statement about the additive constant's deformation-invariance), not a *value*. The ε−μq identity (S95 W5-3) fixes the value to the `ρ_Λ = 0` self-tuned offset *at the equilibrium fixed point* `q = q_eq`, but §6.3 needs `a(t)` on the post-fold quenched GGE relic — `q(τ) ≠ q_eq`, where `w̃(q) = ε(q) − μq ≠ 0`. V2's split (equilibrium: value-fixed; off-equilibrium: form-constrained but value-open) survives my scrutiny.

**The decisive cross-check I can confirm independently — QEQ-SELFCONS is V0-blind by construction.** volovik claims (Sage-verified in V2) that the slope-1 observable cancels V0. I verified the structural reason from the gate's own multiplicative-cancellation rule rather than re-running: the slope `d ln q_amp/d ln|H|` is a **log-derivative**, and `math-scripts.md §"Multiplicative-normalization cancellation invariants"` (MANDATORY at K=3 since S94 W6-18) states that any multiplicative pre-factor in the observable is annihilated by `d/d ln K`. The §6.3 closure constant `C3 = κ²/3 = 5.6416e−03` (carrying a₀^ζ = 6440 AND the V0 subtraction) enters `H² = C3·(S_SA − V0)·…` as exactly such a multiplicative-additive pre-factor. The W4-1 verdict line itself reports `kappa_inv=True` and the WP §W4-1 documents `|slope(10·C3) − slope(C3)| = 5.9e-5 ≤ 1e-3`. So:
> **The W4-1 PASS is a *log-derivative* observable, and the framework's own K=3-MANDATORY cancellation theorem says log-derivatives annihilate the very pre-factor V0 lives in.** QEQ-SELFCONS CANNOT be cited as evidence that V0 is pinned. It is positive evidence that the *exponent* n=2 is V0-independent — a structurally different (and weaker, for this question) claim.

This is not a weakness I am conceding reluctantly — it is the same structure I flagged in my own MEMORY for the n_s computation ("DO NOT compute n_s from Seeley-DeWitt at fold... use BCS or R-G"): log-derivative observables and absolute-magnitude observables are different spectral moments, and a result clean in one says nothing about the other.

**DISAGREE — but only on emphasis, not substance: V2 understates how strongly the framework's CC machinery WANTS V0 ≠ 0 off-equilibrium.** This is where my cross-domain view sharpens rather than softens volovik's reading. My MEMORY carries the CC closure architecture: **DILUTION-CC-66 PASS** (Volovik `ρ_vac ~ H²` tracking closes 114 OOM via Gibbs-Duhem), and the registered note: **"CC closure is DOUBLY conditional: on C10 AND on the external FRW H the tracking law feeds (= the a(t) gap)."** Read together with V2:
- The equilibrium `ρ_Λ = 0` (V0 = 0 at `q_eq`) is the framework's CC mechanism. But the *observed* dark energy is NOT zero — it is the **effacement residual**, `Ω_Λ` from a 0.03% leakage through the impedance mismatch `Γ_eff = 0.99970` (phononic-framing.md DE-row). That residual is precisely `w̃(q(τ)) − w̃(q_eq) ≠ 0` — the off-equilibrium correction V2 names. So the framework does not merely *allow* an off-equilibrium V0; its DE phenomenology *requires* one. **V0 is value-open off-equilibrium AND that openness is doing observational work** (it carries Ω_Λ). The non-universality reading is therefore stronger than V2 stated: the absolute offset is not just unpinned, it is the channel through which the one dimensional dark-energy scale enters — another instance of "the dimensionful content is imported / consistency-forced, the dimensionless content is computed."

**Answer to Q-V2a/Q-V2b/Q-V2c.**
- *(Q-V2a)* No — the ε−μq identity on the quenched GGE relic does NOT give a value-fixed V0. The identity zeroes `w̃` only at `q = q_eq`; QEQ-SELFCONS shows `q(τ)` oscillating with `q̄ = 0.4122`, `ω_q = 59.89` (WP §W4-1), so `w̃(q(τ)) ≠ 0` generically. τ-flat (form) survives; value-fixed (at the relic) does not.
- *(Q-V2b)* I cannot identify a gate that measures the *absolute* a₀ offset on the post-fold state, and I agree the QEQ-SELFCONS slope is structurally disqualified from being that gate (log-derivative ⇒ V0-cancelling, above). The S101-H0-PROPER-A2 gate measures the a₂-moment *ratio* `G_N^FW/G_N^obs`, not the a₀ absolute offset — wrong moment for this question (a₀ is the CC/V0 channel; a₂ is the gravity channel; **Spectral Moment Decoupling**, my MEMORY: "CC uses F_{−1}, NEC uses F_{+1} — different moments"). So no existing gate pins absolute V0 on the relic.
- *(Q-V2c)* Yes, and this is the sharpest point: an off-equilibrium V0 **does** inherit a relic-induced correction the equilibrium identity misses — and **S101-W1-QEQ-RELIC-ODDFLOOR FAIL (audit 98a923fd) is the direct evidence.** That gate found `ω_q^phys = 2.0128 M_KK` INSIDE the pair band `[1.6395, 10.8379]` with a live resonance and `|c_odd|/|c_even| = 2.70e-2 ≫ 1e-3` (WP §W4-2). The parametric-rectification channel is a LIVE odd-in-H force on the q-channel — i.e., the relic state carries a dynamical force the equilibrium `ρ_Λ = 0` theorem cannot see. The off-equilibrium V0 sits in exactly this regime. So Q-V2c's suspicion is confirmed by the session's own FAIL.

**MISSED — the moment-decoupling makes V0 and gamma_unit *independent* non-universalities, not one.** V3 will argue (correctly) that the obstruction is "one with five costumes." I add a refinement that the cross-domain view forces: V0 (the a₀-channel absolute offset) and gamma_unit (the dimensional readout) live in **different spectral moments** (a₀ vs the readout map), and by Spectral Moment Decoupling they are not algebraically tied. They co-shift in the *borrowed-H* sense (Corr(a₀,a₂)=+1, because both are read against the same external H), but their *substrate origins* are distinct moments. This matters for the R4 theorem-tag wording: "the substrate determines the conformal class, not the metric normalization" is one statement, but it has (at least) two structurally independent failure channels — the a₀ absolute offset and the dimensional second — which a careful registry entry should enumerate, not merge.

#### Re: V3 — Surviving obstruction

**AGREE — and this is the section where my entire discipline (cross-domain isomorphism detection) converges with volovik's.** V3's central claim is that the §6.3 obstruction is ONE structural fact wearing five costumes — {Z_norm/V0, κ, q-frame} (readout layer), {T6, C2} (closure layer), {m_D, M₀←m_H} (matter layer) — all of which are "the substrate determines dimensionless/conformal data, not the dimensional metric normalization." From the pattern-detection seat, I confirm the costumes are isomorphic, and I can name the isomorphism more sharply than "they live on the same side of a kinematics/dynamics cut."

**The structural skeleton, stated as an isomorphism (what binds the five costumes).** Every costume is an instance of the same exact-sequence pattern: a substrate observable `O` factorizes as `O = w · Ô`, where `Ô` is a dimensionless / conformal-class quantity the spectral triple computes, and `w` is a single scale that the spectral triple does NOT fix from its eigenvalues. The costume is determined by *which slot `w` occupies*:

| Costume | `Ô` (substrate-computed, dimensionless/conformal) | `w` (the un-fixed scale) | Pillar |
|:--|:--|:--|:--|
| Z_norm / gamma_unit | τ̇²-coefficient = G_DeWitt = 5.0 | M_KK→seconds (ℏ/M_KK) | I (acoustic FRW) |
| V0 (off-eq) | the n=2 tracking exponent | absolute a₀ offset (Ω_Λ channel) | I/III (CC) |
| κ | the τ̇ *shape* | the seconds in κ_nat = 8.86e-42 s | I |
| q-frame | the conformal class (a_eff const to 7e-7) | the representative (q = 0/0) | I |
| m_D | the Yukawa *ratio* structure | the absolute neutrino scale | III/VI (matter) |
| M₀←m_H | the Casimir-graded mass *hierarchy* | the EW VEV anchor (m_H, first power) | III/IV (matter) |

This IS one obstruction because there is one *kind* of object missing — `w`, a dimensional scale-setter — and the spectral triple supplies `Ô` in every row. **The costumes differ only in which physical quantity needs the scale.** This is the cleanest statement of V3's "one obstruction, five costumes," and the isomorphism is the factorization `O = w·Ô`.

**MISSED — the framework's own results already name this factorization elsewhere, which makes it a *recurrence* and not a cosmology-specific gap.** My charge is to ask where else a structure appears. The `O = w·Ô` factorization with `Ô` computed and `w` un-fixed is the SAME object as:
- **The multiplicative-normalization cancellation theorem** (`math-scripts.md`, K=3 MANDATORY): "if L_max enters as a multiplicative pre-factor `w(L_max)·g(K)`, any log-derivative annihilates `w`." That theorem is *about exactly this factorization* — it tells you the dimensionless content `g(K)` (=`Ô`) is recoverable from log-derivatives while the pre-factor `w` is not. The framework PROVED, in the regulator context, that this is the structure of its observables. V3's obstruction is the cosmological instance of that proven structure.
- **R_BG = 1/cosh(2r)** (my S93 W8-6): the bridge-coefficient *ratio* is locked dimensionless; the absolute coefficients carry the scale. Same factorization.
- **The three-layer BCS hierarchy** (my MEMORY, S69): "geometric invariants (immune) → dynamical protections (robust) → observable predictions (BCS-modified, bounded)." The immune/robust layers are the `Ô` content; the bounded observable layer is where the dimensional `w` enters and the prediction becomes scheme/scale-dependent. V3's readout/closure/matter layering maps onto this exactly.

So the obstruction is not 27 sessions of a cosmology-specific wall — it is the framework's *generic* observable structure (proven in the regulator and BCS contexts) showing up in the one place where `w` is observationally load-bearing (the FRW background).

**Answer to Q-V3a — is T6 the dynamical face of the same non-universality, or independent?** I read T6 as the **dynamical face of the same non-universality**, and I can give the substitution-chain reason rather than asserting it. T6 BROKEN = "the 155,984-mode spectral action overwhelms the 8-mode BCS lock by 1.33×10⁵." The substitution chain:
- The a₂-sector action scales with the full mode count `~N_modes` (155,984 with multiplicity; my MEMORY corpus number); the BCS lock sector is `8` modes.
- The ratio `N_modes/8 ~ 1.95×10⁴`, and the gravitational-vs-BCS coupling-strength mismatch lands at `1.33×10⁵` — the same order, set by *how many spectral modes carry the a₂ scale vs the order-parameter scale.*
- This is `O = w·Ô` at the *dynamical-closure* layer: the back-reaction *shape* (the conservation structure ∇G_eff = 0, S95 W3-1 PASS) is the `Ô` the substrate fixes; the *strength* at which the gravitational sector locks to the BCS sector is the `w` it does not — and that `w` is again a scale ratio (mode-count-weighted), decoupled from the BCS order-parameter scale the cosmology needs.

To answer the sharp form of Q-V3a — "what would close T6 *without* closing the dimensional normalization?" — **nothing I can construct.** Closing T6 means making the gravitational sector lock to the BCS sector at the right *absolute* strength, which is precisely fixing the relation between the a₂-scale and the BCS-scale, which is fixing a dimensional `w`. If you could fix that `w`, gamma_unit (V1) would follow (the a₂-scale carries M_KK). So T6 is not independently closable while the dimensional slot is open. It is the same obstruction, dynamical face — I agree with V3 against the memo's "two free normalizations" mis-location at the readout layer only.

**Answer to Q-V3b — is Corr(a₀,a₂)=+1 the signature of a single missing normalization?** Yes, with one important precision from Spectral Moment Decoupling. The +1 correlation (W7-7a) is the signature that the dagger-row observables share a *single hidden borrowed-H normalization* — they are all read against the same external H(t) (C10), so they co-shift perfectly when that one borrowed scale moves. That is V3's reading and it is correct **at the readout/borrowing layer**. But I flag the substrate-origin precision: a₀ and a₂ are *different spectral moments* (CC uses F_{−1}; gravity/NEC uses F_{+1} — my MEMORY, Spectral Moment Decoupling). Their +1 *observational* correlation is induced by the shared external `w` (the borrowed H), NOT by an algebraic identity between the moments themselves. This matters because it means the +1 is **diagnostic of the single missing `w`** (V3 is right) rather than evidence that a₀ and a₂ are the same substrate object (they are not). The correct reading: *one missing dimensional normalization induces a rank-1 (perfectly correlated) shift across observables that are otherwise independent substrate moments.* That rank-1 structure is itself the cleanest falsifier of the non-universality claim — if a future gate fixed `w`, the +1 correlation would break (the observables would decouple onto their independent moment origins).

**EMERGES — the obstruction has a definite *rank*, and that rank is the registry-theorem's sharpest content.** Combining V3's "one obstruction" with the moment-decoupling: the non-universality is a **rank-1 obstruction** — one missing scale `w` projecting onto N≥5 observables. This is more precise than "conformal class, not metric" and more useful: it predicts (a) the perfect +1 correlation already observed, (b) that ALL dagger-rows must co-shift identically under any single re-normalization (a testable consistency condition on the borrowed-H sector), and (c) that fixing `w` in ONE channel (e.g., a successful R1) would fix it in ALL (the rank-1 structure forbids partial closure). Point (c) is decisive for the verdict: it means R1's "fix V₀ but not Z_norm" partial-structure scenario (the memo's R2-trigger branch) is structurally **impossible** under rank-1 — either `w` is spectral-data-derivable (R1 PASS, all channels close) or it is not (R1 FAIL, theorem confirmed). There is no middle. This sharpens the memo's R1→R2 sequencing: R2 (the "partial structure" branch) is ruled out a priori by the rank-1 reading, so the decision is genuinely binary R1-PASS vs R4.

#### Re: V4 — Cross-Cutting Observations

**AGREE on the verdict (R4-trigger) and on N₃=0 as the substrate-internal genre wall. I converge with volovik here, with my own dimensional audit (Re:V1) as the independent corroboration that the falsification path is genuinely closed.** My R1 assignment was to make the strongest case for S96-closed / R1-survives. I made it (P1 below states what S96 genuinely closed). But I held myself to the falsification discipline: I tried to write `gamma_unit = Φ(spectral data only)`, and every chain routed through ℏ (SI) + M_KK-in-GeV (external). When the strongest case for R1 concedes its own kill criterion, the honest output is convergence — and I converge on R4-trigger with a sharpened statement of WHERE the anchor enters (the rank-1 `w` slot of Re:V3).

**Answer to Q-V4a — the single most decisive disagreement-point — stated unambiguously: H0-PROPER-A2's N is a consistency identity, NOT an independent H₀ derivation.** I verified the degeneracy independently in Sage (my own dispatch):
- WP §W4-4 line 260: `H0_readout = H_obs × (M_red,obs/M_red,FW)`. At deficit closure `M_red,FW → M_red,obs`, equivalently `M_red,FW = N·M_red,obs`. Substituting (Sage): `H0_readout = H_obs/N`, `limit_{N→1} = H_obs`, `dH0_readout/dN|_{N=1} = −H_obs`. At realized `N = 0.999859`: `H0_readout = 67.4/0.999859 = 67.4095` — the external anchor 67.4 nudged by a dimensionless factor pinned to ≈1 *by the band* `|N−1| ≤ 0.05`.
- The km/s/Mpc UNIT enters through `H_obs`, not from D_K + f + Λ. `N = M_SA/(4·M_Pl,unred,obs)` is a **dimensionless ratio of two ways of getting Newton's constant** — it tests `G_N^FW = G_N^obs`, which is the a₂-channel dimensionless matching (and it PASSES beautifully, N = 0.999859, a real and creditable result: the a₂ → G_N emergence chain is internally consistent to 1.4×10⁻⁴).

**This is not my inference imposed on the einstein-theorist — it is the einstein-theorist's OWN disclosure, verbatim.** WP §W4-4 line 262: *"At exact deficit closure (N→1) the readout degenerates to H_obs = 67.4 identically. The framework supplies the GRAVITATIONAL-COUPLING leg (the a₂^{ζ} second spectral moment); the laboratory currently supplies the ENERGY-CONTENT leg. An anchor-independent H₀ awaits the framework's own energy-content derivation... a FUTURE pre-registration (S102+), NOT this gate."* And the verdict-line `value=` field itself: `anchor_degeneracy_DISCLOSED`, `G_N_FW/G_N_obs=1.000000`. So the gate that ran it, the WP that documents it, the verdict line that pins it, and my independent Sage chain all agree: **H0-PROPER-A2 is the `gamma_unit` slot wearing the H₀ costume** — it confirms the dimensionless gravitational matching (G_N ratio = 1) and *imports the second* via H_obs. It STRENGTHENS the conformal-class reading; it does not puncture it. Q-V4a resolved: consistency identity.

This is the same structure as Re:V3's table row "Z_norm/gamma_unit": `O = w·Ô` with `Ô = G_N^FW/G_N^obs` (computed, = 1) and `w = H_obs` (imported second). It is also — and this is the cross-domain payoff — the **identical pattern to the matter sector**: S101-M0-BCS-SCREENING (INFO, `convention=FIRST-POWER-MH-LEVEL-TRANSFER-PRIMARY`) shows M₀ rides the external m_H scale at first power; m_D is back-solved from oscillation splittings. **H0-PROPER-A2 : H_obs :: M₀ : m_H :: m_D : oscillation-data.** Three sectors (gravity, Higgs-Yukawa, neutrino), one move: the substrate computes the dimensionless ratio (G_N ratio, mass hierarchy, Yukawa structure) and imports the one absolute scale. This is the recurrence my discipline exists to detect, and it is the strongest single piece of evidence FOR V4's reading — the non-universality is not a gravity-sector quirk; it is the framework's uniform signature across every sector where an absolute scale is needed.

**Answer to Q-V4c — yes, N₃ = 0 (BDI, S44) is the framework's *internal* statement of the kinematics-not-dynamics wall, and I prefer it strongly to the external-citation route.** The memo's §2/§3 carries "Volovik's verdict," Sakharov non-universality, and Barceló–Liberati–Visser as training-recalled pointers (the referee report and memo both correctly flag these UNVERIFIED-AS-CITED; per my feedback_research-corpus rule I will not treat training-recalled literature as established). V4's substitution is exactly right methodologically: replace the external analogy with the substrate theorem. N₃ = 0 is a *computed substrate invariant* (BDI class, 3He-B child, momentum-space topological charge zero), and its content is precisely "the emergent metric carries no Fermi-point topological protection, hence its normalization is non-universal." A protected quantity (the chiral anomaly coefficient in N₃=2 3He-A) is microscopic-detail-independent; an unprotected one (the induced-EH normalization in a fully-gapped BDI vacuum) depends on the cutoff M_KK, an input. This converts the genre wall from "analogy to superfluid-gravity literature" into "corollary of a proven substrate invariant" — which is exactly the substrate-first inversion my framing-law demands (`phononic-framing.md`: explain GR via the substrate, never the substrate via GR). I endorse the N₃=0 framing as the registry-theorem's anchor.

**DISAGREE — one genuine sharpening of V4's floor, which I will not soften: the framework derives MORE than "conformal class."** V4 already concedes this in (Puncture-strain 2), and I want to make it load-bearing rather than parenthetical, because it is the part of the R1-survives case that genuinely survives R4. QEQ-SELFCONS (W4-1 PASS, `a_exp=0.6554` vs 2/3; the w=0 dust attractor `a ∝ t^{2/3}` establishing self-consistently) means the framework DERIVES the dimensionless *tracking exponent* n=2 of the late-time expansion — a *dynamical shape*, not just a static ratio. The honest floor is therefore **"conformal class + dimensionless dynamical shapes, NOT the dimensional normalization,"** and the §6.3 re-scope language must say what dies is the *seconds-valued* a(t), not the *shape* of a(t). I hold this against any bare-R4 wording that would call the framework "conformal-class-only" — that undersells a genuine derivation (the tracking exponent) and would mis-state the constraint map. The rank-1 reading (Re:V3) supports this: `Ô` (the dimensionless content) includes the tracking *dynamics*, not merely static ratios; only the single scale `w` is missing.

**Answer to Q-V4b — I do NOT favor R1-survives, so I will not commit to a `gamma_unit = Φ(...)` I expect R1 to produce. I commit to the opposite, with the dimensional bookkeeping shown:** there is no `gamma_unit = Φ(dimensionless spectral data)` because the codomain (seconds) is dimensionally unreachable from a dimensionless domain (Re:V1, Sage-confirmed). The only honest form is `gamma_unit = ℏ/(M_KK·c²)`, which imports M_KK-in-GeV — failing the zero-new-parameter clause exactly as V4 predicts. So I align with V4's expectation that R1 hits its kill criterion, and I reach it not as a prediction-as-evidence but as a *closed dimensional argument*: the rank-1 obstruction (Re:V3) is a single missing dimensional scale, and dimensional scales are not log-derivative-recoverable from a dimensionless spectral triple (the K=3 cancellation theorem, run in reverse).

**EMERGES — the verdict has a structural certificate, not just a posture.** Combining N₃=0 (V4) + the rank-1 `O=w·Ô` factorization (Re:V3) + the dimensional unreachability (Re:V1): the non-universality is **not a gap awaiting more computation — it is a theorem with a topological cause (N₃=0), a definite rank (1), and a dimensional proof (seconds ∉ codomain of any dimensionless-spectral map).** That triple is what should be tagged. The R1 gate should still be pre-registered (the kill criterion is the deliverable, per the memo's decision discipline and the rank-1 binary), and its FAIL lands the theorem cleanly — but the structural certificate already exists, and R2 (partial-structure) is ruled out a priori by rank-1.

### Part 2: Original Analysis

#### P1: The S96-closed reading — what S96-W1-QFLOW-RESIDUAL actually resolved (cross-domain pattern view)

My assignment was the strongest case for S96-closed / R1-survives. Here it is, stated at full strength, then bounded by exactly what the gate's verdict line says — because the bound is where the cross-domain pattern decides the question.

**What S96-W1-QFLOW-RESIDUAL genuinely and correctly closed (the real result).** The S95 W3-2 closed form left `residual_free_normalization_count = 2`, `free_scalars = {Z_norm, V0}`. S96-W1-QFLOW-RESIDUAL drove that count `2 → 0`: `Z_norm == G_DeWitt = 5.0` (the τ̇²-kinetic coefficient identified with the already-pinned moduli gradient-stiffness, S42 — `tau-flow_redundant-alias_no-canonical-write`), and `V0 = τ-flat`. This is a genuine elimination of TWO internal dimensionless freedoms, and the strongest-case reading is: *the closed form `H²(τ,τ̇)` has no remaining free internal scalar — every dimensionless coefficient inside it is now pinned to a prior canonical.* That is real. The non-universality reading does NOT get to claim Z_norm or V0 are "still free dimensionless internal parameters." They are not. S96 is a true partial result, and the memo's R1 is right that the *internal count* is closed.

**The strongest R1-survives extrapolation (the case I was asked to build).** If S96 pinned Z_norm to G_DeWitt via the §8.3 dictionary's success at fixing `f₂ ≈ 92` from `M_Pl/M_KK`, then — the argument goes — the *same* dictionary that fixed the dimensionless kinetic coefficient should fix the time unit, "since G_N fixes both M_Pl AND the second via ℏ/c conventions" (memo R1, line 32). The dictionary already produced a working dimensionless gravitational quantity (f₂); ask it for the seconds and one session of algebra against existing pins should deliver `H(τ_now)` within a factor of 2 of 67.4 km/s/Mpc. This is the honest strongest form of R1-survives, and I built it to its limit.

**Where the cross-domain pattern view says the extrapolation breaks — and it breaks decisively.** The pattern is the `O = w·Ô` factorization (Re:V3). S96 closed the `Ô`-side count (the dimensionless scalars), which is exactly the side the spectral triple computes well. It is *silent on `w`* — and `w` (the dimensional scale) is structurally a different kind of object. Three facts, each from a gate verdict, close the extrapolation:

1. *S96 itself flags the silence.* The QFLOW-RESIDUAL verdict carries `no-canonical-write` on the Z_norm identification — it pinned an *alias*, not a new dimensional constant. The gate did not claim to close a dimensional slot; per V1, `gamma_unit` was never one of the two W3-2 free scalars. The "G_N fixes both" extrapolation reads a dimensional closure into a gate that explicitly made only a dimensionless-alias write.

2. *The dimensional slot was closed by a DIFFERENT gate, and that gate shows the import.* `gamma_unit` IS pinned in the corpus — by **S96-W1-MKK-SECONDS** (`M_KK_inv_seconds = 8.86e-42 s`, `scheme=SI-dimensional-chain-hbar-over-E`). I reproduced its chain (Sage): the second comes from `ℏ_SI / (M_KK_GeV·GeV_to_J)`. So the framework already *has* its seconds — and they are sourced from ℏ (SI input) and M_KK-in-GeV (external calibration), not from D_K eigenvalues. The memo's "same dictionary fixes the time unit" is half-right in a way that defeats R1: the dictionary DOES yield a second, but only by importing the dimensional scale, which is precisely what the zero-new-continuous-parameters clause forbids.

3. *The §8.3 dictionary's codomain is dimensionless.* `f₂ ≈ 92` is `M_Pl/M_KK`, a ratio of input scales. By dimensional analysis a dictionary of such ratios cannot emit a time. This is not a contingent gap to be closed by more algebra; it is a closed dimensional argument (Re:V1, Sage: `gamma_unit ∉ free_variables(H_sub²)`).

**The cross-domain diagnosis — why 27 sessions kept reaching for "one more reframe."** Every route in the S74→S100a graveyard (memo §3) succeeded on `Ô` (ratios, shapes, conservation, conformal data) and failed on `w` (Z_norm-dimensional, κ-seconds, q-frame, absolute V0). The pattern detector reads this as a *single attractor*: the substrate keeps handing back the dimensionless content because that is what a spectral triple computes (eigenvalue ratios, log-derivatives, conformal classes — all `w`-independent by the K=3 multiplicative-cancellation theorem), and keeps withholding `w` because `w` is the one input the eigenvalue problem does not contain. The 27-session pull toward reframe is the signature of trying to extract a dimensional scale from a structure that is dimensionless by construction — each reframe re-discovers `Ô` and re-misses `w`.

**P1 verdict (honest, against my own assignment).** S96-W1-QFLOW-RESIDUAL resolved the *internal dimensionless free-scalar count* (2→0) — real, correct, not to be discounted. It did NOT resolve, and did not claim to resolve, the *dimensional* slot `gamma_unit`. The memo's R1 conflates the two layers when it expects "the same dictionary" to fix the second. The strongest R1-survives case, built to its limit, concedes its own kill criterion at the dimensional step. **S96-closed is true for what it closed (Ô); R1-survives is false for what it hoped to extrapolate (w).** This is the cross-domain pattern view's answer to question (a)/(c): the surviving obstruction is the rank-1 `w` slot, and S96 is a genuine result *inside* it, not a refutation of it.

#### P2: The S101 W4 evidence the memo could not see — H0-PROPER-A2, QEQ-SELFCONS, and what they change

The memo (dated to the S100a graveyard) could not see four S101 W4 results. I assess each for whether it punctures, strengthens, or refines the non-universality reading. The decisive one is H0-PROPER-A2 (Q-V4a).

**(1) S101-H0-PROPER-A2 PASS (audit cd8e8c0b) — Q-V4a answered: a CONSISTENCY IDENTITY, not an independent H₀ derivation.** This is the memo's missing flagship and the single most decisive disagreement-point. The full resolution is in Re:V4; the compressed answer for the verdict table:
- *The chain:* `N = M_SA/(4·M_Pl,unred,obs) = 0.999859`; `H0_readout = H_obs·(M_red,obs/M_red,FW) = H_obs/N` (Sage, my dispatch); `limit_{N→1} = H_obs`; realized `67.4/0.999859 = 67.4095`.
- *What it IS:* a dimensionless test of `G_N^FW = G_N^obs` (the a₂-channel matching via the spinor factor 4 = √16), PASSING at 1.4×10⁻⁴ — a real, creditable internal-consistency result on the a₂ → G_N emergence chain.
- *What it is NOT:* a substrate-internal H₀ in seconds. The km/s/Mpc unit enters through H_obs. The einstein-theorist's own WP §W4-4 line 262 says it verbatim: *"At exact deficit closure (N→1) the readout degenerates to H_obs = 67.4 identically... the framework supplies the GRAVITATIONAL-COUPLING leg; the laboratory currently supplies the ENERGY-CONTENT leg."* The verdict line says `anchor_degeneracy_DISCLOSED`.
- **Effect on the reading: STRENGTHENS it.** H0-PROPER-A2 is the cleanest demonstration of `O = w·Ô` in the gravity sector — `Ô = G_N ratio` (computed, =1), `w = H_obs` (imported second). It does not puncture; it is the puncture-candidate that, examined, confirms the import. *I concede this against my R1 assignment: the flagship the memo couldn't see does not rescue R1; it exhibits the non-universality with maximal clarity.*

**(2) S101-W1-QEQ-SELFCONS PASS (audit c06a956b) — the genuine R1-survives residue, correctly bounded.** This is the one result that genuinely earns the framework MORE than "conformal class": `slope_selfcons = 1.000074`, `a_exp = 0.6554` (vs 2/3), the w=0 dust attractor `a ∝ t^{2/3}` establishing self-consistently. The n=2 tracking *exponent* is DERIVED (unforced back-reaction), not fitted.
- *But:* it is a log-derivative observable, and the verdict line reports `kappa_inv=True` (WP §W4-1: `|slope(10·C3) − slope(C3)| = 5.9e-5`). By the K=3 multiplicative-cancellation theorem the closure constant C3 (carrying a₀^ζ and V0) is annihilated — **QEQ-SELFCONS is V0-blind and `w`-blind by construction** (Re:V2). It pins a dimensionless *shape*, not a dimensional scale.
- **Effect on the reading: REFINES the floor upward.** What dies under R4 is the *seconds-valued* a(t); what the framework KEEPS is the dimensionless *shape* of a(t) (the n=2 exponent). The §6.3 re-scope must be "conformal class + dimensionless dynamical shapes," not bare "conformal class." This is the part of S96-closed/R1 that survives — but it survives as `Ô` content, exactly where the rank-1 reading predicts a derivation is possible.

**(3) S101-W1-QEQ-RELIC-ODDFLOOR FAIL (audit 98a923fd) — the off-equilibrium V0 inherits a relic correction (confirms Q-V2c).** The derived substrate clock places `ω_q^phys = 2.0128 M_KK` INSIDE the pair band `[1.6395, 10.8379]`; 24 modes (14 occupied) cross resonance on the post-fold tail; `|c_odd|/|c_even| = 2.70e-2 ≫ 1e-3` (WP §W4-2). The relic state carries a LIVE odd-in-H parametric-rectification force the equilibrium `ρ_Λ = 0` theorem cannot see.
- **Effect on the reading: REINFORCES V2.** The off-equilibrium regime where §6.3 lives is exactly where the equilibrium V0-fixing fails AND where a new dynamical force appears. This is direct session evidence that the absolute V0 is not merely unpinned but dynamically corrected off-equilibrium. *Cross-domain note:* the gate also DERIVED `γ = dt/dτ = 29.7532 M_KK⁻¹` — but this γ is the substrate clock in M_KK⁻¹ units (a ratio of frequencies `ω_q^τ/ω_q^phys`), NOT the seconds-conversion gamma_unit. It fixes the s97 `t_relax` *dimensionless* freedom; the second still comes from M_KK_inv_seconds. Same `Ô`/`w` split: even the "clock derivation" is a dimensionless ratio, with the second imported separately.

**(4) S101-W0-BRANCH-IV-EVALUATOR INFO (audit cd0492d6) — independent corroboration of the rank-1 reading in the DE sector.** The branch-iv w₀ evaluator is `derivation-inadmissible`: no Θ-free combination of {R_JK, ξ_E_GGE_inv} reproduces R_sv1 at 1e-5; the candidates that land near w_0_B "smuggle a free normalization" (WP §W4-3). The §(iv-bis) lock-test found the surrogate is `algebraic_distance_locked` — a GEOMETRIC observable whose sign and spread are mechanically locked to the R_JK monotone fall, *independent of physical content.*
- **Effect on the reading: CORROBORATES the `O = w·Ô` structure in a fourth channel.** w₀ factorizes into a dimensionless ratio-structure the substrate fixes (the f-reduction shape) and a normalization the retirement R_JE → {R_JK, ξ_E_GGE_inv} made *lossy* — the two distance-tagged successors do not recombine into the dressing-ratio R-slot without re-injecting the retired (scale-carrying) object. This is `w` going missing in the DE sector, by the same mechanism as gamma_unit in the FRW sector.

**What the four results change, in aggregate (the cross-domain synthesis the memo could not perform).** The memo diagnosed "the substrate determines the conformal class, not the metric normalization" from the S74→S100a graveyard. The S101 W4 quartet does not overturn that — it **upgrades it from a diagnosis to a structurally-certified, multi-sector recurrence**:
- *Gravity sector:* H0-PROPER-A2 — `Ô = G_N ratio` computed, `w = H_obs` imported (Q-V4a: identity).
- *DE/CC sector:* BRANCH-IV-EVALUATOR + QEQ-RELIC-ODDFLOOR — `Ô = w₀ f-shape / tracking dynamics` computed, `w = absolute offset (Ω_Λ channel)` un-fixed and relic-corrected.
- *Dynamical-shape sector:* QEQ-SELFCONS — `Ô = n=2 exponent` DERIVED (the floor is higher than bare conformal class), `w` still absent.
- *Matter sector (memo-visible, S101-confirmed):* M0-BCS-SCREENING `FIRST-POWER-MH-LEVEL-TRANSFER` — `Ô = mass hierarchy` computed, `w = m_H/EW VEV` imported.

Four sectors, one factorization, one missing object `w`. **The memo saw the gap; the S101 W4 quartet shows it is rank-1 and universal across sectors** — which is the registry-theorem's content and the reason R2 (partial structure) is ruled out (Re:V3 rank-1). The memo's "two free normalizations" framing is the readout-layer shadow of this; the S101 evidence makes the deeper statement legible. **Net: the evidence the memo could not see CONFIRMS R4-trigger and supplies the multi-sector certificate; it adds one honest sharpening (the n=2 shape is derived, so the floor is conformal-class-PLUS-dimensionless-dynamics).**

#### P3: Questions for volovik

These are the genuine residual tensions where I think we still have something to converge on — I have conceded R4-trigger, so these sharpen the *theorem-tag and falsifier*, not the verdict.

**(Q-PF1 — the rank-1 structure as the theorem's falsifier, not just N₃=0.)** I argued (Re:V3) the obstruction is **rank-1**: one missing scale `w` projecting onto N≥5 observables, with the perfect `Corr(a₀,a₂)=+1` (W7-7a) as its signature. This gives the theorem a *quantitative* falsifier sharper than the R1 kill criterion: under rank-1, ALL dagger-rows MUST co-shift identically under any single re-normalization — a partial decorrelation (any dagger-row moving independently of the others) would falsify the rank-1 claim and reopen R2 (partial structure). Do you accept rank-1 as the registry-theorem's *operational* content (and hence "R2 ruled out a priori"), or do you read the +1 correlation as consistent with a higher-rank obstruction that merely happens to look rank-1 at the current observable set? If higher-rank, which observable would you expect to break the +1?

**(Q-PF2 — does the n=2 derived exponent (QEQ-SELFCONS) belong INSIDE the protected/`Ô` content, or is it a strain on the floor?)** Your V4 (Puncture-strain 2) flags the n=2 tracking exponent as derived dynamics, "more than conformal class in the narrowest sense." I place it firmly inside the `Ô` (dimensionless-computed) content — a dynamical *shape*, hence protected by the same N₃=0 logic that protects mass ratios and tilts (it is `w`-independent by the K=3 cancellation theorem, Re:V2). Do you agree the protected content includes dimensionless *dynamics* (tracking exponents, growth shapes), so the floor wording is "conformal class + dimensionless dynamical shapes" — or do you see a reason the *dynamical* shapes might be less protected than the *static* ratios under N₃=0 (e.g., a topological-protection argument that distinguishes static from dynamic dimensionless observables)?

**(Q-PF3 — the moment-decoupling caveat on "one obstruction.")** I agree the obstruction is ONE missing `w`, but I flagged (Re:V2/Re:V3) that its *projections* land on structurally independent spectral moments — a₀ (CC/V0 channel, F_{−1}) vs a₂ (gravity/gamma_unit channel, F_{+1}) vs the matter-sector Yukawa scale — which Spectral Moment Decoupling says are not algebraically tied at the substrate level. The `+1` correlation is *induced* by the shared external `w`, not by a moment identity. Does this change how you'd word the registry entry — i.e., should the theorem-tag enumerate the (≥3) independent moment-channels through which the single `w` is missing, rather than assert a single undifferentiated "metric normalization"? My concern is that a future reader who fixes `w` in the a₂ channel (a successful G_N→H₀ energy-content derivation, the S102+ pre-registration WP §W4-4 line 262 points to) might wrongly conclude the a₀/CC channel is thereby closed, when moment-decoupling says it is not.

**(Q-PF4 — is the relic odd-floor force (QEQ-RELIC-ODDFLOOR FAIL) a SECOND non-universality, or part of the same `w`?)** The W4-2 FAIL found a live odd-in-H parametric force `|c_odd|/|c_even| = 2.70e-2`. In your V2 framing this is the off-equilibrium correction to V0. But it is a *dynamical* effect (a resonance), not obviously a missing *scale*. Is the relic odd-force inside the rank-1 `w` obstruction (a manifestation of the un-fixed absolute offset), or is it a genuinely distinct dynamical finding that the conformal-class-complete theorem does NOT cover — i.e., a place where even the *dimensionless dynamics* the framework derives (n=2) acquire a relic-induced correction the theorem-tag should separately flag? This is the one S101 result I am least sure how to file relative to the rank-1 reading, and it is your own gate's sibling.

**(Q-PF5 — the acoustic-metric death and the N₃=0 corollary.)** My MEMORY records "BLV acoustic metric is dead at N_pair=1 (S53); Connes distance replaces it." Both deliver conformal/dimensionless data (Re:V1). Here is the cross-pillar question N₃=0 raises: in my analog-gravity pillar, the acoustic metric's conformal factor is set by the condensate density ρ — a *dimensional* input. When BLV died and Connes distance took over, did the framework *change* the dimensional status of the emergent metric, or merely change the *representation* of the same `Ô` content (eigenvalue-gap ratios replacing density-weighted line elements)? I read it as representation-change only — the N₃=0 non-universality is invariant under the BLV→Connes transition because both are `Ô`-valued. If you agree, that is a strong consistency check on the theorem (it survives the metric-representation change), and I'd want it in the registry entry as a robustness statement. If you disagree — if Connes distance somehow carries dimensional content BLV did not — that would be the one route I can see to a `gamma_unit = Φ(spectral data)` and would reopen R1. Does the Connes-distance representation contain a dimensional scale the acoustic representation did not?

---

## Round 2 — volovik: Follow-up

### CONVERGENCE

phonon-first's R1 was assigned the S96-closed steelman and built it to its limit, then conceded the kill criterion at the dimensional step (Re:V1, P1): every chain to `gamma_unit` routes through an imported scale, and the explicit functional reduces to `gamma_unit = ℏ/(M_KK·c²)` with M_KK-in-GeV an external input. I reproduced the M_KK→seconds chain this session (Sage: `ℏ_SI/(M_KK_GeV·GeV_to_J) = 8.8588e-42 s`, relerr 1.8e-4 vs canonical `M_KK_inv_seconds = 8.860439881925477e-42`, S96-W1-MKK-SECONDS) — the second comes from ℏ (SI) over an energy (M_KK in GeV); neither factor is a D_K eigenvalue. The concession is solid and I adopt it as shared ground. Four specific convergences, each accepted/qualified/extended from the superfluid-universe side:

**(1) The `O = w·Ô` factorization — ACCEPT and IDENTIFY with my own program's deepest structure.** phonon-first's Re:V3 table is the right object: every costume is `O = w·Ô`, `Ô` the dimensionless/conformal-class quantity the spectral triple computes, `w` the single scale it does not fix. This is exactly Sakharov non-universality in the framework's notation, and it is the *same factorization the framework already proved as a theorem* — the K=3-MANDATORY multiplicative-normalization cancellation invariant (`math-scripts.md`; advanced to K=3 by S94 W6-18). That theorem states: if `L_max` (or any scale `w`) enters as a multiplicative pre-factor `w·g(K)`, every log-derivative `d^n ln(·)/d(ln K)^n` annihilates `w`, leaving only `g(K) = Ô`. I want to extend the identification, not merely accept it: **the cancellation theorem is the *substrate-internal proof* that the framework's observables have the `O = w·Ô` form.** It was proved in the regulator context (three structurally-distinct factorization mechanisms: L_max-truncation weight, τ-moduli-deformation weight, bottom-K Casimir-ceiling weight at fixed m_PV). phonon-first's contribution is to recognize the cosmological FRW background as a *fourth* instance of the same factorization — and that recognition is correct. From my side this closes a loop: my own V2 argument that QEQ-SELFCONS is V0-blind is literally the cancellation theorem applied to the closure constant C3 = κ²/3 (which carries a₀^ζ = 6440 and the V0 subtraction); the slope-1 log-derivative annihilates C3 by the K=3 rule. So `Ô = n=2 exponent` (recoverable), `w = V0/a₀ offset` (annihilated, hence un-fixed by that observable). The factorization is not an analogy between my program and phonon-first's — it is one theorem with one new application.

**(2) The rank-1 obstruction and its a-priori elimination of the memo's R2 — ACCEPT, with one superfluid-side sharpening that strengthens it.** phonon-first's Re:V3 reads the obstruction as **rank-1**: one missing `w` projecting onto N≥5 observables, with `Corr(a₀,a₂) = +1` (W7-7a) as its signature, and concludes the memo's R2-route (R1 "fixes V₀ but not Z_norm" — partial structure) is structurally impossible: either `w` is spectral-data-derivable (R1 PASS, all channels close) or it is not (R1 FAIL, theorem confirmed), no middle. I accept this and it tightens my V3 ("one obstruction, five costumes") into something operationally sharper — the costumes are not just *qualitatively* the same gap, they are *literally one rank-1 projection*. The superfluid-side sharpening: the rank-1 reading is exactly what `N₃ = 0` predicts. In a topologically-protected vacuum (3He-A, N₃ = 2), the protected quantity is universal — independent of microscopic detail, so it would carry NO free scale (rank-0 in the missing-`w` sense; the protection *fixes* it). In an *unprotected* vacuum (3He-B, N₃ = 0, BDI, the framework's class, S44), the induced-EH normalization is set by the cutoff M_KK — one scale, unprotected, hence *exactly one* missing dimensionful object. **The rank of the obstruction = the number of unprotected dimensional scales the BDI vacuum must import = 1.** So rank-1 is not an empirical observation about the current dagger-row set; it is forced by the topology (one cutoff scale, no Fermi-point protection). This is why R2 is ruled out a priori: a partial closure would require the obstruction to have rank ≥ 2 (one `w` closable, another not), which would require *two* independent unprotected scales — but the BDI vacuum has one cutoff. I endorse "R2 ruled out a priori"; the topological cause is the certificate.

**(3) The matter-sector isomorphism (H0:H_obs :: M₀:m_H :: m_D:oscillation-data) — ACCEPT and EXTEND to the two-fluid mapping.** phonon-first's Re:V4 reads three sectors, one move: the substrate computes the dimensionless ratio (G_N ratio = 1.000000 at deficit closure; the Casimir-graded mass hierarchy; the Yukawa structure) and imports the one absolute scale (H_obs; m_H at first power per S101-M0-BCS-SCREENING `FIRST-POWER-MH-LEVEL-TRANSFER-PRIMARY`, audit 1a1eff66; oscillation splittings). I confirm this against my own mapping table. In my correspondence ledger, the two-fluid structure is "Two-fluid w=-1 + w=0 ↔ Effacement (vacuum) + GGE (matter)" (Volovik Paper 37, S43). The matter-sector twins (m_D, M₀←m_H) are the GGE/matter face; the gravitational twins (gamma_unit, V0) are the effacement/vacuum face. **The isomorphism phonon-first names IS the statement that the same `Ô`/`w` split runs through both fluids of the two-fluid decomposition** — which is the natural place for it, because both fluids inherit from the *same* BCS condensate on SU(3) (my mapping "BCS condensate on SU(3) ↔ Spectral action vacuum," S22/S35), and a BCS/Bogoliubov vacuum fixes mass *ratios* (the gap structure) and not the *absolute* gap scale (set by the cutoff). The chiral-anomaly precedent makes this exact: in 3He-A the anomaly coefficient is a protected dimensionless integer (universal); the gap amplitude Δ itself is a non-universal dimensionful scale set by microscopics. Ratios protected, scale imported — in *both* the gravitational and matter sectors, because both are BCS-vacuum observables. I extend phonon-first's three-sector list: it is not three coincidences, it is the two-fluid decomposition of one BCS vacuum, and the `Ô`/`w` split is a property of BCS vacua generally.

**(4) The n=2 floor-sharpening — ACCEPT as a correction to my own V4 wording, and adopt it as load-bearing.** phonon-first holds (Re:V4 DISAGREE, the one place R1-survives genuinely survives) that the framework derives MORE than "conformal class": QEQ-SELFCONS (W4-1 PASS, my own gate) derives the dimensionless *tracking exponent* n=2 (`a_exp = 0.6554` vs 2/3; `n2tracking = 2.0001`; the w=0 dust attractor `a ∝ t^{2/3}` self-consistent), and this is a dynamical *shape*, not a static ratio. My V4 carried this only as parenthetical "(Puncture-strain 2)"; phonon-first is right to make it load-bearing. I accept the correction: the floor is **"conformal class + dimensionless dynamical shapes, NOT the dimensional normalization,"** and the §6.3 re-scope must say what dies is the *seconds-valued* a(t), not the *shape* of a(t). The superfluid-side reason this is the *right* floor (not over-generous): the tracking exponent is `Ô` content — it is `w`-independent by the K=3 cancellation theorem (the slope is a log-derivative, C3 annihilated), so it is *protected* in the same sense as the mass ratios and tilts. There is no topological reason under N₃=0 to distinguish a static dimensionless ratio from a dynamical dimensionless shape — both are unprotected-scale-independent, hence both computable. So "conformal class + dimensionless dynamical shapes" is the precise statement of the protected content; "conformal class only" undersells a genuine derivation and would mis-state the constraint map.

### DISSENT

I have **no remaining dissent** with phonon-first on the verdict, the factorization, the rank-1 reading, the isomorphism, or the floor. The convergence is genuine and I record it as such rather than manufacturing a disagreement.

I attach **one precision** (not a dissent — phonon-first raised it first, in Re:V2/Re:V3 and Q-PF3, and I confirm rather than contest it) because it changes the registry-theorem *wording* and a future reader could be misled without it: the single rank-1 `w` projects onto observables that live on *structurally independent spectral moments*. The a₀ channel (CC/V0 offset) is an `F_{−1}`-class moment; the a₂ channel (gravity/gamma_unit) is an `F_{+1}`-class moment (Spectral Moment Decoupling, my MEMORY: "CC uses F_{−1}, NEC uses F_{+1} — different moments"). The `+1` correlation is *induced* by the shared external `w` (the borrowed H, C10), NOT by an algebraic identity between the moments. This is fully consistent with rank-1 (one missing scale → rank-1 shift) and with phonon-first's Re:V3 (which already flags the moment-distinction). The precision matters for one reason only: it forbids the inference "fix `w` in the a₂ channel ⇒ a₀/CC channel thereby closed." Moment-decoupling says no — the projections are independent moments, so a successful a₂-channel energy-content derivation (the S102+ pre-registration the WP §W4-4 line 262 / line 385 points to) closes the a₂ leg of the rank-1 obstruction without automatically closing the a₀ leg. The rank-1 structure means *the same `w` is missing everywhere*; it does NOT mean *one compute fixes it everywhere* unless that compute supplies `w` at the source (the cutoff-to-lab-units bridge), not in one channel's readout. I treat this as a sharpening of the shared rank-1 reading, answered in full under Q-PF3 below.

### EMERGENCE

**(A) The THEOREM-TAG's final wording and its falsifier — a rank-1 obstruction has a sharp operational signature.** Combining phonon-first's rank-1 reading (Re:V3) with my `N₃=0` topological cause (V4) and the Sage-confirmed dimensional unreachability (Re:V1), the registry-theorem has a *structural certificate* with three independent legs, and — the new content — a **falsifier with a definite operational form** that is sharper than the R1 kill criterion alone. A rank-1 obstruction predicts a specific covariance structure: one missing scale projecting onto N observables produces a **rank-1 (perfectly correlated) shift** across them. `Corr(a₀,a₂) = +1` (W7-7a) is the *already-observed* signature. This gives the theorem a falsifier the R1 gate does not: **under rank-1, ALL borrowed-H dagger-rows MUST co-shift identically under any single re-normalization of the external H; a partial decorrelation (any dagger-row moving independently of the others) falsifies rank-1 and reopens R2.** So the theorem-tag should be worded to carry BOTH falsifiers:

> **Normalization Non-Universality (N₃=0 corollary, rank-1):** The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology AND all dimensionless dynamical shapes — every ratio, ordering, tilt, growth shape, and the late-time tracking exponent (n=2, S101-W1-QEQ-SELFCONS) — but does NOT determine the dimensional metric normalization. The obstruction is **rank-1**: a single un-fixed dimensional scale `w` (the substrate→seconds bridge `gamma_unit = ℏ/(M_KK·c²)`; the off-equilibrium absolute vacuum offset; the Friedmann–BCS lock strength T6) projects onto N≥5 observables via the `O = w·Ô` factorization. The cause is topological: N₃ = 0 (BDI class, S44) — the induced metric carries no Fermi-point protection, so the cutoff-scale normalization (M_KK, an input) is unprotected. The rank equals the number of unprotected dimensional scales the BDI vacuum imports = 1. **Two falsifiers:** (i) the R1 kill criterion — if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with no imported GeV/seconds scale, the theorem is FALSIFIED; (ii) the rank-1 covariance test — if any borrowed-H dagger-row decorrelates from the others under a single H-renormalization, rank-1 is FALSIFIED (the obstruction is higher-rank, R2 reopens). PROVEN-corollary status pending the R1 kill-criterion gate; R1 FAIL + sustained `Corr = +1` confirm it; R1 PASS or partial decorrelation falsify it.

The two falsifiers are independent and complementary: (i) tests the *source* (can `w` be derived?), (ii) tests the *structure* (is the obstruction really rank-1?). This is a strictly stronger theorem than "conformal class, not metric" — it has a topological cause, a definite rank, a dimensional proof, and two operational falsifiers.

**(B) What the n=2-protected "dimensionless dynamical shapes" content changes for the capstone §6.3 re-scope.** The re-scope should NOT claim the framework delivers "conformal class only" / "ratios and orderings." That undersells the protected content and is the wrong constraint-map statement. The capstone §6.3 re-scope should claim the framework DELIVERS the **conformal-class-complete-PLUS-dimensionless-dynamics** content: every dimensionless ratio AND every dimensionless dynamical *shape* — tilts, growth shapes, orderings, the ZFP spine, AND the late-time tracking exponent n=2 (substrate-derived, S101-W1-QEQ-SELFCONS, the w=0 dust attractor self-consistent). What §6.3 does NOT deliver is the single dimensional normalization `w` (the seconds-valued a(t), the absolute H₀ in km/s/Mpc, the absolute vacuum offset). The substrate-first framing for the re-scope (per `phononic-framing.md`): the substrate IS the spectral complexity whose dimensionless dynamics — including the tracking shape — are fully determined; the *second* is a laboratory unit imported at the readout, the one place the eigenvalue problem is silent. The arrow is unchanged: `D_K eigenvalues → spectral moments → dimensionless dynamical shapes (n=2 tracking, tilts, growth) → measurement`; only the dimensional readout `× (ℏ/M_KK c²)` is an external multiplier. **This is a positive deliverable statement, not a deficit confession** — the framework determines the entire dimensionless dynamical content of the cosmology from zero continuous parameters, and is honest that the one dimensional scale is the cutoff M_KK calibrated externally. The §6.3 re-scope is the substrate-IS statement of the protected/unprotected split that N₃=0 forces, and it should read as a *finding about what the substrate determines*, not as "a(t) is not coming."

### QUESTIONS

I answer phonon-first's P3 (Q-PF1 through Q-PF5) by label. Q5 (Q-PF5) is the last open door — I close it with a first-principles + Sage argument.

**(Q-PF1 — rank-1 as the theorem's operational falsifier; or higher-rank masquerading as rank-1?)** I **accept rank-1 as the registry-theorem's operational content**, and "R2 ruled out a priori" follows. The reason is not just the observed `Corr = +1`; it is the topological count (Convergence (2) above): the BDI vacuum (N₃ = 0) has exactly *one* unprotected dimensional scale — the cutoff M_KK — so the obstruction's rank equals the number of unprotected scales = 1. A higher-rank obstruction would require ≥2 *independent* unprotected dimensional scales; the framework's topology supplies one cutoff, hence one. To your sub-question "which observable would break the +1 if higher-rank?": under my reading none should, because there is no second independent scale to carry an orthogonal shift — *that* is the falsifier. If a future borrowed-H dagger-row decorrelated from the others under a single H-renormalization, it would mean a second unprotected scale exists (the BDI single-cutoff count is wrong, or the vacuum is not purely BDI), and rank-1 falsifies. So I do not read the +1 as "higher-rank that happens to look rank-1"; I read it as the topologically-forced rank-1, with decorrelation as its sharp falsifier. (The one structural caveat carried into the wording, per my DISSENT precision: rank-1 means *one missing `w`*, not *one compute fixes all channels* — the projections are independent moments.)

**(Q-PF2 — does the n=2 exponent sit INSIDE the protected/`Ô` content, or strain the floor?)** It sits **firmly inside the protected `Ô` content**, and I agree the floor wording is "conformal class + dimensionless dynamical shapes." I see **no reason under N₃=0 to distinguish static dimensionless ratios from dynamical dimensionless shapes** — both are `w`-independent (the tracking slope is a log-derivative, C3/V0 annihilated by the K=3 cancellation theorem; Re:V2). The topological protection in this framework is *not* a Fermi-point protection (N₃ = 0, there is none) — it is the protection of *dimensionless content under multiplicative re-normalization*, which is the cancellation theorem itself. That protection is blind to whether the dimensionless observable is static (a ratio) or dynamical (a tracking exponent, a growth shape): both survive `w → w'` because both are ratios/log-derivatives in which `w` cancels. So the n=2 exponent is exactly as protected as the mass ratios. I considered whether a *dynamical* shape could be *less* protected — e.g., if the tracking attractor depended on the absolute time-scale — but QEQ-SELFCONS shows the opposite: `kappa_inv=True` (slope invariant under 10×C3), i.e. the exponent is explicitly demonstrated `w`-independent. Conclusion: protected content = all dimensionless observables, static and dynamical; floor wording confirmed.

**(Q-PF3 — the moment-decoupling caveat on "one obstruction"; should the theorem-tag enumerate the independent moment-channels?)** **Yes — the theorem-tag should enumerate the (≥3) independent moment-channels through which the single `w` is missing**, and I have written the wording in EMERGENCE (A) to do so: the parenthetical lists `gamma_unit` (a₂/F_{+1} channel), the off-equilibrium absolute offset (a₀/F_{−1} channel), and T6 (dynamical-closure channel), plus the matter-sector Yukawa scale as a fourth. Your concern is exactly right and is the load-bearing reason to enumerate: a future reader who fixes `w` in the a₂ channel (the S102+ energy-content derivation joining the Volovik-partition Level-2 to the W4-4 convergent-a₂ Level-1, WP line 385) might wrongly conclude the a₀/CC channel is thereby closed. Moment-decoupling forbids that inference: a₀ and a₂ are independent spectral moments (F_{−1} vs F_{+1}), not algebraically tied; the `+1` correlation is induced by the shared external `w`, not by a moment identity. So the registry entry must say: *the obstruction is rank-1 (one missing `w`) BUT its projections are on independent moments, so closing one channel's readout does not close the others — only supplying `w` at the source (the cutoff→lab-units bridge) closes all of them simultaneously.* This is the precise statement that reconciles "rank-1" (one scale) with "independent moments" (the scale enters several places that are not algebraically linked). I file the relic odd-floor under Q-PF4 as a candidate fourth channel.

**(Q-PF4 — is the relic odd-floor force a SECOND non-universality, or part of the same `w`?)** This is the result I am also least certain how to file, and I give a structural answer rather than a confident one. **My reading: the relic odd-floor (QEQ-RELIC-ODDFLOOR FAIL, audit 98a923fd; `ω_q^phys = 2.0128 M_KK` INSIDE the pair band [1.6395, 10.8379], live resonance, `|c_odd|/|c_even| = 2.70e-2 ≫ 1e-3`) is NOT a missing *scale* — it is a dynamical *finding* that is distinct from the rank-1 `w` obstruction and should be flagged separately in the theorem-tag.** The substitution-chain reason: the rank-1 `w` obstruction is about a *normalization* (a multiplicative/additive dimensional constant that factors out as `w` in `O = w·Ô`). The odd-floor is a *resonance* — a live parametric-rectification force arising because the derived substrate clock frequency `ω_q^phys` lands *inside* the pair band, producing an odd-in-H force that no constant re-normalization can remove (you cannot factor a resonance into a multiplicative pre-factor; it is a pole, not a scale). So it fails the `O = w·Ô` test: there is no `w` such that the odd-force is `w·(something dimensionless)`. This means the odd-floor is **a genuine second finding** — a place where even the *dimensionless dynamics* the framework derives (the n=2 tracking) acquire a relic-induced correction. It is the off-equilibrium correction to V0 in the sense that it lives in the same post-fold quenched-GGE regime (my V2), but it is NOT the same object as the missing absolute V0 offset: the offset is a scale (rank-1 `w`), the odd-floor is a resonant force (a dynamical pole). The theorem-tag should therefore carry it as a *separate rider*, not fold it into the rank-1 obstruction: "the conformal-class-complete content is robust EXCEPT where the derived substrate clock resonates with the pair band (QEQ-RELIC-ODDFLOOR), which introduces a relic-induced odd-in-H dynamical correction outside the `O = w·Ô` rank-1 structure." This keeps the rank-1 reading clean (it is about scales) and honestly flags the one place a *dynamical* correction (not a missing scale) appears. It is my own gate's sibling, and I will not absorb a resonance into a normalization to keep the rank-1 story tidy.

**(Q-PF5 — the acoustic-metric death and the N₃=0 corollary; does the Connes representation carry a dimensional scale the acoustic one did not? THE LAST OPEN DOOR.)** I **close this door**: the Connes-distance representation carries NO dimensional scale the acoustic representation did not, the BLV→Connes transition is *representation-change only*, the N₃=0 non-universality is INVARIANT under it, and — extending your question to its strongest form — **no route through the NCG spectral action changes the dimensional status of `gamma_unit`; the spectral action inherits the identical `w`-import.** I give the substitution chain (Sage-verified this session), because this is the one route that could reopen R1 and it must be closed by computation, not assertion (`feedback_r1-overconfidence-test-before-claim`).

*Step 1 — the Connes distance's dimensional content.* The Connes distance is `d(p,q) = sup{ |f(p) − f(q)| : ‖[D, f]‖ ≤ 1 }`. D_K eigenvalues carry units `[E] = [M_KK]`; the commutator `[D,f]` carries `[E]`; the constraint `‖[D,f]‖ ≤ 1` measures `f` in units `1/[E] = 1/[M_KK]`. So a **single** Connes distance carries `1/E_unit = 1/M_KK` (dimensionful, *one* scale), and a **ratio** of two Connes distances is dimensionless (Sage: `connes_single_dim = 1/E_unit`, `connes_ratio_dim = 1`). Identical structure to the acoustic line element: the *conformal factor* `(ρ/c)` carries the single condensate scale; the *shape* (light-cone, dispersion) is dimensionless.

*Step 2 — does Connes introduce a SECOND independent dimensionful scale?* The Connes distance is built ONLY from D_K eigenvalues, whose sole dimensionful scale is `E_unit = M_KK`. The acoustic conformal factor `(ρ/c)` reduces, in the substrate, to the same single condensate scale `M_KK`. Sage check: `connes_scales − acoustic_scales = {} ` (empty); **Connes introduces a second independent scale beyond acoustic = False.** So BLV→Connes is a change of *representation* of the same `Ô` content (eigenvalue-gap ratios replacing density-weighted line elements), carrying the same single scale `M_KK`, NOT a change of dimensional status. Your reading (representation-change only) is confirmed, and I want it in the registry entry as a robustness statement: **the N₃=0 non-universality survives the metric-representation change** — both BLV and Connes deliver `Ô` (conformal/dimensionless data) with the one scale `M_KK` imported at the readout. (My MEMORY: "BLV acoustic metric is dead at N_pair=1 (S53); Connes distance replaces it for expansion." The death changed the representation, not the dimensional status.)

*Step 3 — the strongest form: does the full Connes–Chamseddine SPECTRAL ACTION change the status?* This is the deeper version of your question and the real last door — perhaps the *distance* is dimensionless-by-construction but the *spectral action* `Tr f(D/Λ)` secretly emits a dimensional gravitational normalization from the spectrum alone. It does not. The heat-kernel expansion is `S = Tr f(D/Λ) ~ f₄Λ⁴a₀ + f₂Λ²a₂ + f₀a₄ + …`, where `a_n` are dimensionless geometric integrals (Seeley-DeWitt) and `f_k` are dimensionless moments of `f`. The induced Einstein-Hilbert term — the `1/(16πG_induced)` normalization — sits on the `a₂` term: `1/G_induced ~ f₂·Λ²·a₂` (Sakharov-induced gravity, the cutoff-squared coefficient). Sage substitution chain: strip the scale (`Λ → 1`) and `1/G_induced → f₂·a₂` — *purely dimensionless, no `[E]²`*. Test "does the spectral action emit `G_induced` in lab units WITHOUT importing Λ?": **False** (`bool(no_lambda.has(Lambda)) = False`; the `[E]²` is carried only by `Λ²`). And `Λ` = the spectral cutoff = M_KK = an INPUT (calibrated in GeV). This cross-checks the framework's own WP §W4-4 line 286 verbatim: `M_Pl,FW ∝ √(a₂^{ζ})/4` with `a₂` in M_KK units → `G_N` carries `M_KK²`. So **the spectral action inherits the IDENTICAL `w`-import**: it computes the dimensionless `f₂·a₂` (the `Ô`) and requires `Λ² = M_KK²` (the `w`) for the dimensional normalization. There is no NCG route — Connes distance, spectral action, or the W4-4 a₂→G_N chain — that produces a dimensional gravitational scale (and hence a seconds-valued `gamma_unit` via G_N, ℏ, c) without importing M_KK.

*Conclusion (Q-PF5).* The door is closed. Every NCG representation of the emergent metric — acoustic (Pillar IV), Connes distance (Pillar III), spectral action / a₂→G_N (the W4-4 gravity chain) — is `Ô`-valued in its dimensionless content and `Λ²`-locked (= M_KK²) in its dimensional normalization. The cutoff M_KK is an input; it is unprotected because N₃ = 0. There is no `gamma_unit = Φ(D_K spectral data alone)`, and R1 does not reopen through the NCG route. This is the strongest leg of the structural certificate: the dimensional unreachability is not a property of one closed-form (the W3-2 `H_sub²`); it is a property of *every* metric the spectral triple hands you, proven at the heat-kernel level. The non-universality is a theorem about what the substrate determines — the full dimensionless dynamical content — and what it imports — the single cutoff scale M_KK — with a topological cause (N₃ = 0), a definite rank (1), a dimensional proof at the spectral-action level, and two operational falsifiers (R1 kill criterion; rank-1 covariance test).

---

## Round 2 — phonon-first: Cross-Synthesis

### CONVERGENCE

volovik's R2 accepts all four of my R1 contributions and extends each from the superfluid-universe side. I accept all four extensions. The convergence is now total on the verdict (R4-trigger), the `O = w·Ô` factorization, the rank-1 reading, the matter-sector isomorphism, the n=2 floor-sharpening, and — with Q-PF5 (the last open door) closed by his Sage substitution chain through the spectral action — on the dimensional unreachability at the heat-kernel level. I adjudicate the four points the spawn flagged, in order.

**(1) The `O = w·Ô` factorization IS the K=3-MANDATORY cancellation theorem, with FRW as a fourth instance — ACCEPT volovik's identification, and I confirm the FRW-instance status is not a loose analogy but a verifiable membership claim.** volovik's R2-Convergence-(1) is the move I was reaching for and did not state cleanly: the `O = w·Ô` factorization is not *analogous to* the multiplicative-normalization cancellation invariant (`math-scripts.md`, MANDATORY at K=3 since S94 W6-18) — it *is* that theorem, and the cosmological FRW background is a fourth structurally-distinct factorization instance alongside the three already in the K-counter calibration corpus (L_max-truncation weight, τ-moduli-deformation weight, bottom-K Casimir-ceiling weight at fixed m_PV). I verified the membership claim against the rule's own DISSENT-sharpened advancement criterion this session (Sage, my dispatch): the FRW instance's missing scale `w = gamma_unit = ℏ/(M_KK·c²)` is the **substrate→seconds dimensional-readout weight** — categorically distinct on the *spectral-support-form axis* from all three existing instances (it is neither a truncation envelope, nor a moduli-deformation weight, nor a Casimir-ceiling sector-count cutoff; it is the dimensional-bridge multiplier sitting one layer up in the readout map, `H_SI = H_sub/gamma_unit`). The log-derivative annihilation is exact: with `O_i = w·Ô_i`, Sage gives `d ln O_i/d ln w = 1` for every observable, so any log-derivative observable (the QEQ-SELFCONS slope; the Connes-distance disconnect slope, `S101-CONNES-DISTANCE-DISCONNECT-BOUNDARY` line 28 which *explicitly* invokes the K=3 cancellation on the regulator-scale axis: "intercept ln c annihilated by the log-derivative — math-scripts.md K=3 multiplicative-cancellation") returns `Ô` and is blind to `w`. So the framework is not merely *consistent* with the factorization — it has now exhibited the same cancellation theorem in FOUR independent contexts (regulator-truncation, moduli-deformation, Casimir-ceiling, FRW-readout), and the present workshop is the recognition that the FRW non-universality is the fourth. This closes volovik's loop and mine simultaneously: his V2 (QEQ-SELFCONS V0-blind) and my Re:V2 (same, via C3 = κ²/3) are *both* the K=3 theorem applied to the closure constant; we derived the same result from the two ends, and it is one theorem.

**(2) "rank = number of unprotected scales" — I adjudicate this as a THEOREM with one provisional empirical input, not a free conjecture; volovik's topological derivation is sound and I supply the covariance-structure half of the proof.** This is the point the spawn asks me to adjudicate sharply, so I will be precise about what is proven and what is provisional. The claim decomposes into two halves, and they have different epistemic status:

- *Half A — the covariance-structure half (PROVEN, Sage this session).* IF there are exactly `k` independent un-fixed dimensional scales `{w_1,…,w_k}`, THEN the log-Jacobian of the borrowed-H dagger-row observables with respect to `{w_j}` has rank exactly `k`, and the observed shift-covariance matrix has rank `k`. I verified both limiting cases explicitly: for `k=1`, `d ln O_i/d ln w` is the all-ones vector across all observables → the shift column is a scalar multiple of `(1,1,…,1)` → `Corr = +1` EXACTLY and no observable can decorrelate. For `k=2` (observables riding `w_1`, `w_2`, or both), the log-Jacobian is `[[1,0],[1,0],[0,1],[1,1]]` with **rank 2** → a row (the `w_2`-only observable) CAN decorrelate from the `w_1`-only rows under a single re-normalization → partial closure is structurally possible. So `rank(obstruction) = rank(shift-covariance) = #(independent unprotected scales)` is a theorem about the covariance structure, true for any `k`, independent of the framework's specifics. This half is mathematics, not physics.

- *Half B — the topological-count half (volovik's V4/R2-(2); structurally sound, ONE provisional input).* The number of independent unprotected dimensional scales the BDI vacuum must import equals the number of dimensionful scales NOT pinned by a momentum-space topological invariant. volovik's claim is that `N₃ = 0` (BDI class, S44 — a *computed* substrate invariant) leaves exactly ONE such scale: the cutoff M_KK. The induced-EH normalization rides M_KK² (his Q-PF5 Step 3: `1/G_induced ~ f₂·Λ²·a₂`, `Λ = M_KK`), the seconds-bridge rides M_KK (`gamma_unit = ℏ/M_KK c²`), the absolute vacuum offset rides M_KK⁴ (the a₀ Λ⁴ term) — **all powers of the single cutoff M_KK**. One unprotected scale, entering at different powers in different moments.

The composition of Half A (proven) and Half B (topologically-grounded) is: `rank(obstruction) = #(unprotected scales) = #(independent cutoffs in a BDI vacuum) = 1`. **My adjudication: this is a THEOREM modulo one verifiable input — that the BDI vacuum's only unprotected dimensionful scale is the single cutoff M_KK (i.e., that no second independent dimensionful scale enters the induced action that is not a power of M_KK).** That input is *checkable* and is in fact strongly supported by the session's own evidence: every channel I and volovik catalogued (gamma_unit, off-eq V0, T6, m_D, M₀←m_H) imports a power of M_KK or a scale tied to it (m_H, the EW VEV, is itself the |S|² fiber-oscillation scale that the framework relates to M_KK via the KK threshold). So I do not call it a bare conjecture — the covariance half is proven, the topological half is grounded in a computed invariant (N₃=0), and the one provisional input (single-cutoff BDI vacuum) has zero counterexamples in the corpus and is itself a candidate Stage-2 verify clause. **It is a theorem-with-a-named-provisional-input, not a conjecture** — and the named input *is* the second falsifier (a decorrelation would reveal a second scale). The honest registry status is therefore exactly what volovik proposes: PROVEN-corollary pending the R1 kill criterion, with the rank-1 covariance test as the structural sub-falsifier of Half B.

**(3) The two-falsifier structure — ACCEPT, and I sharpen WHY the two are independent: they test the two halves of the rank-1 theorem separately.** volovik's EMERGENCE-(A) attaches two operational falsifiers to the theorem-tag: (i) the R1 kill criterion (can `w` be derived from D_K alone?), (ii) the rank-1 covariance test (does any dagger-row decorrelate under single-H renormalization?). I accept both and add the structural reason they are *non-redundant*: falsifier (i) tests the SOURCE of `w` (is the cutoff genuinely external, or secretly spectral?); falsifier (ii) tests the COUNT of `w` (is there genuinely one unprotected scale, or two?). These map exactly onto the two halves of the rank-1 theorem: (i) attacks the existence of an un-fixed `w` (if `gamma_unit = Φ(D_K eigenvalues alone)` can be written, the obstruction has rank 0, both halves collapse, R1 PASSES); (ii) attacks Half B's topological count (if a dagger-row decorrelates, `#(unprotected scales) ≥ 2`, Half B is wrong, the BDI single-cutoff input fails, and the memo's R2 partial-structure branch reopens). The two falsifiers are independent because the two halves are independent: one is about *whether* `w` is missing, the other about *how many* `w`'s are missing. A theorem with falsifiers on both axes (existence and count) is strictly stronger than one with a falsifier on existence alone — and it is the rank-structure that supplies the second axis. This is the cross-domain payoff of importing the covariance-matrix view: it converts "the substrate determines the conformal class, not the metric" (a one-axis claim) into a rank-1 statement with a two-axis falsifier.

**(4) The positive-deliverable re-scope framing — ACCEPT volovik's EMERGENCE-(B) without qualification; this is the correct constraint-map statement and I add the substrate-first anchor.** volovik's R2-(4) corrects my own R1 wording-debt: the §6.3 re-scope must claim **conformal-class-complete-PLUS-dimensionless-dynamics** (every ratio, ordering, tilt, growth shape, the ZFP spine, AND the n=2 tracking exponent) as a POSITIVE deliverable, not a deficit confession. I endorse this fully and supply the framing-law anchor (`phononic-framing.md`): the substrate-first statement is *the substrate IS the spectral complexity whose entire dimensionless dynamical content — static ratios AND dynamical shapes — is determined from zero continuous parameters; the second is a laboratory unit imported at the readout, the one place the eigenvalue problem is silent by construction.* The arrow `D_K eigenvalues → spectral moments → dimensionless dynamical shapes (n=2 tracking, tilts, growth) → measurement` is unchanged; only the terminal `× (ℏ/M_KK c²)` is an external multiplier. This is a *finding about what the substrate determines* (the protected/computable content under N₃=0), framed as a determination, not a gap. I hold this against any residual bare-R4 "conformal-class-only" wording — which would undersell a genuine derivation (the tracking exponent) and mis-state the constraint map. The re-scope is the substrate-IS statement of the protected/unprotected split that N₃=0 forces.

**The odd-floor separate-rider decision — I adjudicate it as CORRECT, and I am the one who flagged the filing uncertainty (Q-PF4), so I record that volovik's resolution settles it.** I asked (Q-PF4) whether the relic odd-floor force (`S101-W1-QEQ-RELIC-ODDFLOOR FAIL`, audit 98a923fd) belongs inside the rank-1 `w` obstruction or is a distinct finding. volovik's answer (R2-QUESTIONS, Q-PF4) gives the decisive structural test: **a resonance is a dynamical pole, not a scale, so it fails the `O = w·Ô` test by construction — there is no `w` such that the odd-in-H force is `w·(dimensionless)`, because you cannot factor a pole into a multiplicative pre-factor.** I confirm this is the right discriminator and I accept the separate-rider decision: the odd-floor is a genuine SECOND finding, distinct from the rank-1 normalization obstruction, and the theorem-tag should carry it as a separate rider rather than fold it in. The cross-domain reason this matters: the rank-1 obstruction is a statement about *scales* (multiplicative/additive dimensional constants that factor out as `w`); the odd-floor is a statement about *dynamics* (a resonance where the derived substrate clock `ω_q^phys = 2.0128 M_KK` lands inside the pair band `[1.6395, 10.8379]`). Folding a resonance into a normalization to keep the rank-1 story tidy would be exactly the kind of category-merge my discipline exists to catch — it is the same error in the opposite direction as the memo's "two free normalizations" (which mis-located the gap at the readout layer only). Keeping them separate is the honest enumeration: one theorem (rank-1 normalization non-universality) plus one rider (relic-induced odd-in-H dynamical correction, outside `O = w·Ô`).

### DISSENT

**No remaining dissent on substance.** volovik's R2 accepted all four R1 contributions, his four extensions are each correct and each tightens the shared result, and his one precision (rank-1 = one missing `w`, NOT "one compute fixes all channels") is not a disagreement — it is the moment-decoupling caveat I raised first in Re:V2/Re:V3 and Q-PF3, which he confirms rather than contests. I record the convergence as genuine. The two agents reached the same rank-1, N₃=0-caused, two-falsifier, dimensionally-proven theorem from opposite pillars (his superfluid-topology side, my cross-domain-factorization side), and the agreement is structural, not shared-context restatement.

**One precision I sharpen (not a dissent — a tightening of volovik's own DISSENT precision, because a future reader could still mis-read it).** volovik's DISSENT correctly states "rank-1 means *the same `w` is missing everywhere*; it does NOT mean *one compute fixes it everywhere* unless that compute supplies `w` at the source." I want to make the reconciliation of "rank-1 (one scale)" with "independent moments (the scale enters several un-tied places)" *operationally exact*, because on its face it sounds contradictory (how can one missing scale project onto algebraically-independent moments?). The resolution is in the distinction between the *Jacobian rank* and the *moment-channel index*, and it is worth pinning precisely:

- The single scale `w = M_KK` enters different observables at different POWERS (Half B above: `gamma_unit ~ M_KK^{-1}`, `1/G_induced ~ M_KK²` via a₂, absolute V0 ~ M_KK⁴ via a₀). Different powers of one scale are still *one* scale — the log-Jacobian column is `(−1, 2, 4, …)·(d ln w)`, which is still **rank 1** (one column, one source), giving `Corr = ±1` (sign set by the relative power-sign) across the channels.
- BUT the *dimensionless* coefficients those powers multiply — `Ô_{gamma} = G_DeWitt`, `Ô_{G_N} = f₂·a₂`, `Ô_{V0} = ε(q)−μq shape` — live on algebraically-independent spectral moments (F_{+1} for a₂/gravity, F_{−1} for a₀/CC, per Spectral Moment Decoupling). So fixing the *readout* in one channel (e.g., supplying the a₂-channel energy-content derivation the WP §W4-4 line 262 points to) does NOT supply `w` at the source — it supplies one channel's `Ô × w^{power}` product, leaving the other channels' `Ô`'s untouched because they are different moments.

So the precise statement, which I'd want in the registry entry verbatim: **the obstruction is rank-1 in the SHIFT-COVARIANCE (one scale, +1 correlation), but the projections land on independent spectral moments, so the rank-1 covariance does NOT imply single-compute closure — only supplying `w = M_KK` at the source (the cutoff→lab-units bridge, ℏ/M_KK c²) closes all channels simultaneously; closing any single channel's readout closes only that moment's leg.** This reconciles "one scale" with "several independent moments" without contradiction: one scale, several powers, several moment-coefficients, one covariance rank. volovik raised the caveat; I am pinning its exact form so the §XVI.1-style "fix one, conclude all closed" inference is foreclosed in the registry text.

### EMERGENCE

**(E1) The final theorem-tag draft language — volovik's EMERGENCE-(A) wording captures BOTH falsifiers and the topology-forced rank-1; I propose two surgical sharpenings and otherwise adopt it.** volovik's draft is correct. The phrase the spawn asks me to evaluate — *"substrate determines conformal class + dimensionless dynamical shapes; the single dimensional normalization `w` is imported"* — captures the topology-forced rank-1 (single `w`) and the n=2 floor (dimensionless dynamical shapes). I propose two sharpenings drawn from the convergence above:

1. *Make the rank a covariance-rank statement, not just a count.* The tag should read "the obstruction is **rank-1 in the borrowed-H shift-covariance** (`Corr(a₀,a₂)=+1`, W7-7a), with the single scale entering N≥5 channels at different powers of M_KK." This is sharper than "rank-1" alone because it names the *observable* (the +1 covariance) that the second falsifier tests, and it pre-empts the moment-decoupling mis-read (DISSENT precision above) by specifying it is the covariance that is rank-1, not the moment-channel set.
2. *State Half A and Half B as the certificate's two legs explicitly.* "rank = #(unprotected scales) = 1" should carry its derivation: the covariance-rank theorem (Half A, proven) composed with the BDI single-cutoff count (Half B, N₃=0-grounded, one provisional input). This makes the theorem's *status* legible — it is not asserted, it is `(proven covariance theorem) ∘ (topological count with a named checkable input)`.

Proposed final tag (adopting volovik's EMERGENCE-(A), with (1) and (2) folded in):

> **Normalization Non-Universality (N₃=0 corollary, rank-1).** The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology AND all dimensionless dynamical shapes — every ratio, ordering, tilt, growth shape, and the late-time tracking exponent n=2 (S101-W1-QEQ-SELFCONS, the w=0 dust attractor self-consistent) — but does NOT determine the dimensional metric normalization. The obstruction is **rank-1 in the borrowed-H shift-covariance** (`Corr(a₀,a₂) = +1`, W7-7a): a single un-fixed dimensional scale `w = M_KK` projects onto N≥5 observables at different powers (`gamma_unit = ℏ/M_KK c² ~ M_KK^{-1}`; the off-equilibrium absolute vacuum offset ~ M_KK⁴ via a₀/F_{−1}; the induced-Newton normalization `1/G_induced ~ f₂·M_KK²·a₂` via a₂/F_{+1}; the Friedmann–BCS lock strength T6) through the `O = w·Ô` factorization — which IS the K=3-MANDATORY multiplicative-normalization cancellation invariant (`math-scripts.md`), of which the FRW background is a fourth structurally-distinct instance. **Certificate (two legs):** Half A — the shift-covariance rank equals the number of independent unprotected scales (covariance theorem, Sage-proven: one scale → all-ones log-Jacobian column → `Corr=+1` exact; two scales → rank-2 → decorrelation possible); Half B — the BDI vacuum (N₃=0, S44) imports exactly one unprotected dimensional scale, the cutoff M_KK (no Fermi-point protection of the induced metric). **Rider (separate finding, NOT part of the rank-1 obstruction):** the conformal-class-complete content is robust EXCEPT where the derived substrate clock resonates with the pair band (S101-W1-QEQ-RELIC-ODDFLOOR FAIL: `ω_q^phys = 2.0128 M_KK` inside `[1.6395, 10.8379]`, `|c_odd|/|c_even| = 2.70e-2`), introducing a relic-induced odd-in-H dynamical correction outside `O = w·Ô` (a pole, not a scale). **Moment-decoupling caveat:** rank-1 covariance does NOT imply single-compute closure — the projections land on algebraically-independent spectral moments (F_{−1} vs F_{+1}), so only supplying `w` at the source closes all channels. **Two falsifiers:** (i) R1 kill criterion — if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with no imported GeV/seconds scale, the theorem is FALSIFIED (rank-0); (ii) rank-1 covariance test — if any borrowed-H dagger-row decorrelates under single-H renormalization, Half B's single-cutoff count is wrong, rank ≥ 2, and the memo's R2 partial-structure branch reopens. PROVEN-corollary pending the R1 kill-criterion gate.

**(E2) The theorem-tag enters at STAGE-1-CANDIDATE per `joint-theorem-promotion.md`, and I specify the joint-clause/axis structure for the S102 Stage-2 verify.** This is a genuine joint cross-axis theorem — its clauses require evidence from THREE methodological axes (superfluid-universe topology, cross-domain pattern/factorization, NCG dimensional analysis), so it cannot enter the permanent-results table directly. The correct lifecycle is the 4-stage pathway:

- **Stage 0 (this workshop, internal):** clause-attribution is now fixed across the two authoring agents. Clause-by-clause axis tags: (a) rank-1 covariance theorem / Half A — JOINT (volovik EMERGENCE-A + my Sage proof this session, cross-domain-pattern axis); (b) N₃=0 → single-cutoff count / Half B — volovik-side (superfluid-universe-topology axis, S44); (c) `O = w·Ô` = K=3 cancellation theorem, FRW fourth instance — JOINT (my Re:V3 + volovik R2-(1), NCG/regulator axis); (d) Q-PF5 dimensional unreachability through the spectral action — volovik-side (NCG spectral-action axis, his Sage chain); (e) n=2 floor inside `Ô` — JOINT (my Re:V4 DISAGREE + volovik R2-(4)); (f) odd-floor separate rider — volovik-side, my Q-PF4 confirmed; (g) moment-decoupling caveat — my Re:V2/Q-PF3, volovik DISSENT confirmed.
- **Stage 1 (S102 registration):** register as `STAGE-1-CANDIDATE` in `permanent-results-registry.md` (next-free §VII slot per `regulator-pin-discipline.md` next-free-letter protocol), full tag text from E1, JOINT clauses (a)/(c)/(e) flagged for Stage-2 cross-axis verify.
- **Stage 2 (S102, the path the spawn names):** TWO cross-reviewers on DIFFERENT axes, dispatched in parallel, WITHOUT prior workshop context (read only the registered Stage-1 entry, NOT this transcript), per `joint-theorem-promotion.md §"Stage 2"` and the Axis-B Selection Protocol. Axis A = spectral/NCG-axiomatic (audits clauses (c)/(d) + JOINT); Axis B = transit-dynamics or cosmological-bridge (audits clauses (b)/(f) + JOINT). The JOINT clauses (a)/(c)/(e) are PASS-AND'd across both verdicts. Critically: NEITHER cross-reviewer may be volovik (volovik-superfluid-universe-theorist) or me (phonon-first-cosmologist) — we are the authoring agents, excluded by Stage-2 condition 2. The substrate-input-orthogonality clause is naturally satisfiable: the covariance-theorem clause (a) consumes only the Sage log-Jacobian (no session data file), while the N₃=0 clause (b) consumes the S44 invariant — orthogonal data, so the Stage-2 PASS-AND establishes structural-input independence, not merely structural-output independence.
- **Stage 3 (post-R1):** STAGE-3-PERMANENT on R1-FAIL + sustained `Corr=+1`. The theorem's own falsifier (i) is the R1 gate; its PASS would falsify, not promote.

So the answer to the spawn's staging question: STAGE-1-CANDIDATE at S102 registration, with the S102 two-agent cross-axis Stage-2 verify as the promotion path, and the R1 gate as the terminal kill-criterion that lands Stage-3 on FAIL.

**(E3) The R4 verdict KILLS the memo's CF-coldread-6 (Jacobson R2 route), and the R3 verdict SHOULD say so explicitly — the rank-1 a-priori elimination of partial structure removes its gating condition.** The spawn asks whether the rank-1 a-priori elimination of partial structure kills CF-coldread-6. It does, and the kill is clean. The memo's §4 R2 (Jacobson thermodynamic route) and §5 recommendation gate R2 *explicitly* on R1 landing partial structure: memo §5 verbatim — *"R2 only as a separately-planned program if R1 lands partial structure — e.g., it fixes V₀ but not Z_norm, which would localize the non-universality to the clock and make the thermodynamic route the natural next instrument."* The substitution chain to the kill:

1. CF-coldread-6 (the Jacobson R2 route) has gating condition: `R1 lands partial structure` (memo §5).
2. "Partial structure" is operationalized as: R1 fixes one normalization (V₀) but not another (Z_norm) — i.e., `rank(obstruction) ≥ 2` with one scale closable and another not.
3. The rank-1 reading (CONVERGENCE-(2), Sage Half A + topological Half B) establishes `rank(obstruction) = #(unprotected scales) = 1`: either `w` is spectral-data-derivable (R1 PASS, ALL channels close, rank 0) or it is not (R1 FAIL, theorem confirmed, rank 1). There is no `rank ≥ 2` middle where one scale closes and another remains.
4. Therefore the gating condition of CF-coldread-6 (`R1 lands partial structure`) is **a-priori impossible under rank-1.** R1 cannot land partial structure because there is no partial structure to land.
5. **CF-coldread-6 is killed** — its precondition cannot be met. The Jacobson route is not "deferred"; its *trigger* is structurally foreclosed.

The R3 verdict SHOULD say this explicitly, for two reasons: (a) it is a genuine constraint-map update — a carry-forward route eliminated by a structural argument, not by a failed compute, which is exactly the kind of elimination that sharpens the surviving solution space; (b) leaving CF-coldread-6 alive as a "conditional next instrument" after rank-1 forecloses its condition would be a dangling carry-forward that the next planner would have to walk to discover is dead — the no-technical-debt discipline says foreclose it in-session. The honest statement: *the rank-1 reading converts the memo's R1→R2 sequencing into a binary (R1-PASS-rank-0 vs R4-rank-1); R2/CF-coldread-6's "partial structure" branch is eliminated a priori; Jacobson-thermodynamic-route is NOT a live next instrument under the rank-1 certificate.* (Caveat for the R3 final turn: this kills CF-coldread-6 *as gated*. A Jacobson route pursued for an INDEPENDENT reason — e.g., as a separate derivation of horizon entropy, not as a normalization-fixer — is a different object and not foreclosed; only its role as the "partial-structure next instrument" is dead. I flag this so the R3 wording kills the right thing.)

**(E4) The W-1 closed-coincidence verdict (≈6.95 non-bridge, §XVI.1) interacts with this theorem-tag at exactly one structural point — and it is a CONSISTENCY check, not a conflict: §XVI.1 is the empirical anti-pattern of a rank-1 `w`-shift.** The spawn asks whether the W-1 x696 closed-coincidence (`constraint-mega-matrix.md §XVI.1`) touches this theorem-tag. It does, and the interaction is illuminating rather than contradictory. §XVI.1 adjudicated the ≈6.95 near-coincidence between a Pillar-I transit observable (`x696_ratio = 6.9556`, an SU(1,1) `|β|²` squeeze-power ratio) and a Pillar-V/NCG observable (`1/pairing = 6.9489`, a Dixmier-residue ÷ Frobenius-trace quotient) as a CLOSED COINCIDENCE — a *non-bridge*. The structural reason for closure (§XVI.1 leg 2, "regulator-fragility") is precisely the structural INVERSE of the rank-1 `w`-shift, and I verified the contrast in Sage this session:

- A genuine rank-1 `O = w·Ô` factorization has numerator and denominator (or all channels) sharing the SAME scale `w`, so `w` CANCELS in any ratio/log-derivative (the K=3 theorem). The +1 covariance is the signature.
- §XVI.1's `1/pairing` is `(Dixmier residue, regulator-SENSITIVE) / (Frobenius trace, regulator-INERT)` — numerator and denominator scale INDEPENDENTLY under the regulator shift. Sage: `d ln(ratio)/de|_0 = 1` (the ratio MOVES under the regulator shift `e ~ O(2%)`, it does NOT cancel), giving the headroom `Δ_FULL/gap = 20.816×` (Sage-confirmed exact). This is the OPPOSITE of a shared-`w` factorization: the two factors do NOT co-vary, so the ratio is not a stable cross-pillar identity — it is the texture of accident.

So the interaction is a clean consistency statement: **§XVI.1 is the worked example of what a NON-`O=w·Ô` (anti-rank-1, non-co-varying) cross-pillar ratio looks like, and this workshop's theorem is the worked example of what a genuine rank-1 (co-varying, single-`w`, +1-correlated) obstruction looks like.** They are duals — §X cross-pillar BRIDGES are promoted on a shared structural identity (co-variation, cancellation); §XVI closed near-coincidences are eliminated precisely because their factors do NOT co-vary. The rank-1 covariance test (falsifier (ii) of this theorem) is the SAME diagnostic that §XVI.1 used to close the 6.95 coincidence: ask whether the candidate factors co-vary under a single scale-shift. §XVI.1 answered "no, regulator-fragile, 20.816× headroom → closed coincidence"; this workshop's W7-7a answered "yes, `Corr=+1` → genuine rank-1 obstruction." There is no conflict because the two observable-sets are disjoint (the §XVI.1 transit `|β|²` and BdG `1/pairing` are NOT among the borrowed-H dagger-rows {w₀, wₐ, σ₈, CC}), and there is positive value: §XVI.1 supplies the methodological precedent that the co-variation test is the right discriminator for cross-pillar structure — exactly the test falsifier (ii) applies to the dagger-rows. I'd note this consistency in the R3 verdict as a robustness cross-link, not a tension: the framework's own §XVI machinery (co-variation as the bridge/non-bridge discriminator) independently validates the rank-1 covariance test as the theorem's second falsifier.

---

## Round 3 — volovik: Follow-up

### CONVERGENCE

This is the round where the workshop converges, and I record the convergence as genuine: two agents reached the same theorem from opposite pillars — phonon-first's cross-domain-factorization side and my superfluid-universe-topology side — and the agreement is structural, not shared-context restatement (it survives the `epistemic-discipline.md §"What Does NOT Count as Evidence"` item-2 test, which is exactly why E2 stages it for an independent S102 Stage-2 cross-axis verify with both authoring agents excluded). I adjudicate the three things the spawn names — phonon-first's Half-A/Half-B split, the powers-of-M_KK precision, and E1–E4 — and then state the final agreed verdict.

**The Half-A/Half-B two-leg certificate IS the right formal structure for the registry tag — ACCEPT, and I verified both legs Sage-exact this session rather than transcribing phonon-first's R2 numbers (`feedback_r1-overconfidence-test-before-claim` + `feedback_stage2-axisB-disjoint-anchor`: verify via my own anchor, never transcribe the published value).** phonon-first's R2-CONVERGENCE-(2) decomposed "rank = number of unprotected scales" into Half A (covariance-rank theorem, mathematics) ∘ Half B (BDI single-cutoff topological count, physics-with-one-provisional-input). I ran the composition at the *actual* power vector to confirm it is not a toy:

- *Half A, my Sage check this session.* The borrowed-H channels ride the single scale `w = M_KK` at powers `(−1, +2, +4)` (gamma_unit ~ M_KK⁻¹; the induced-Newton normalization `1/G_induced ~ M_KK²` via a₂; the absolute V0 offset ~ M_KK⁴ via a₀). The log-Jacobian column `J1 = (−1, 2, 4)ᵀ` has **rank exactly 1**, the shift-covariance `J1·J1ᵀ` has **rank exactly 1**, and the pairwise correlation is `|Corr| = 1` EXACT on every pair, with sign `= sign(p_i·p_j)`. I extended to 6 channels at arbitrary powers `(−1, 2, 4, 1, −3, 7)`: still rank 1. Half A is mathematics — true for any number of channels at any powers of *one* scale — exactly as phonon-first stated.
- *Half B falsifier, my Sage check this session.* A genuinely independent second scale `w₂` entering one channel gives log-Jacobian rank **2**, with `v_{w₂} ∉ span(v_{w₁})` (Sage: `False`), so a dagger-row can carry a shift component orthogonal to the all-M_KK direction — i.e., decorrelation is structurally possible and the operational falsifier (ii) is well-posed. Half B is the topological count `N₃ = 0 ⇒ one cutoff` (S44, my V4/R2-(2)), and the rank-1-vs-rank-2 boundary is precisely the BDI-single-cutoff-vs-second-unprotected-scale boundary.

So the two-leg certificate `rank(obstruction) = rank(shift-covariance) = #(unprotected scales) = 1` is `(Sage-proven covariance theorem) ∘ (topological count with one named checkable input)`. **This is the right registry structure** because it makes the theorem's *status* legible — Half A is PROVEN (it never needs re-litigation), Half B is topologically-grounded with one input that is itself falsifier (ii). A registry tag that asserted a bare "rank-1" without this split would hide where the proof ends and the provisional input begins; the two-leg form exposes it. I endorse it as the certificate structure.

**The powers-of-M_KK precision — ACCEPT, and I sharpen it into the falsifier's exact form.** phonon-first's R2-DISSENT pinned the reconciliation of "rank-1 (one scale)" with "independent moments (the scale enters several un-tied places)": one scale `w = M_KK` enters at different POWERS in different moments (`M_KK⁻¹`, `M_KK²`, `M_KK⁴`), so the log-Jacobian column is `(−1, 2, 4)·(d ln w)` — still one column, rank 1 — while the dimensionless coefficients those powers multiply (`G_DeWitt = 5.0`, verified `get_constant` this session, not superseded; `f₂·a₂`; the `ε(q)−μq` shape) live on algebraically-independent spectral moments (F₊₁ for a₂, F₋₁ for a₀, Spectral Moment Decoupling). My Sage run makes the precision sharper than "rank-1 ⇒ +1 correlation": the SIGN of the correlation is power-sign-determined. `Corr(a₀~M_KK⁴, a₂~M_KK²) = +1` exactly (same-sign powers — this is the observed W7-7a `Corr = +1`), but `Corr(gamma_unit~M_KK⁻¹, a₂~M_KK²) = −1` exactly (opposite-sign powers). So the rank-1 prediction is NOT a blanket `+1` across all dagger-rows; it is `|Corr| = 1` with a *fixed sign pattern set by the relative power-signs of M_KK in each channel*. This is the one precision I add to the falsifier wording (carried into EMERGENCE below): falsifier (ii) tests `|Corr| = 1` with the predicted sign pattern, not `Corr = +1` blanket — a channel showing `|Corr| < 1` (not merely `Corr ≠ +1`) is the rank-≥2 signature. This is a strengthening, not a correction: it gives the second falsifier a sharper, sign-resolved target.

**E1–E4 — RATIFY all four.** (E1) the final theorem-tag draft: I adopt phonon-first's wording with the two surgical sharpenings (covariance-rank statement + Half-A/Half-B two-leg certificate explicit), plus my sign-pattern precision above. (E2) STAGE-1-CANDIDATE staging per `joint-theorem-promotion.md` with the clause-by-clause axis attribution and the S102 two-agent Stage-2 cross-axis verify (authoring agents — phonon-first and I — excluded by Stage-2 condition 2): correct lifecycle, and I confirm the substrate-input-orthogonality clause is naturally satisfiable (clause (a) consumes only the Sage log-Jacobian, clause (b) the S44 N₃=0 invariant — orthogonal data, so Stage-2 PASS-AND establishes structural-INPUT independence, not merely structural-output). (E3) the R4 verdict KILLS CF-coldread-6 (Jacobson partial-structure route) a priori under rank-1: ACCEPT, with phonon-first's own caveat preserved — what dies is its role as the "partial-structure next instrument" (its gating condition `R1 lands partial structure` is rank-1-impossible), NOT an independent Jacobson horizon-entropy derivation pursued for a different reason. (E4) the W-1 closed-coincidence (constraint-mega-matrix.md §XVI.1, the ≈6.95 non-bridge) is the empirical ANTI-PATTERN of a rank-1 `w`-shift and independently validates falsifier (ii): ACCEPT — §XVI.1 closed the 6.95 coincidence precisely because its two factors do NOT co-vary under a single scale-shift (regulator-fragile, 20.816× headroom), which is the exact INVERSE of the co-varying single-`w` rank-1 obstruction; the framework's own §XVI machinery thus supplies the methodological precedent that co-variation-under-single-scale-shift is the right bridge/non-bridge discriminator, which is what falsifier (ii) applies to the dagger-rows.

**Final agreed verdict (one paragraph).** **R4-trigger, with the positive-deliverable re-scope.** The §6.3 obstruction is a single rank-1 normalization non-universality: the substrate spectral triple `(A_K, H_K, D_K)` determines the entire dimensionless dynamical content of the emergent cosmology — every ratio, ordering, tilt, growth shape, the ZFP spine, AND the late-time tracking exponent n=2 (S101-W1-QEQ-SELFCONS, the w=0 dust attractor self-consistent) — from zero continuous parameters, and imports exactly ONE dimensional scale, the cutoff M_KK, at the readout. The cause is topological (N₃ = 0, BDI class, S44: the induced metric carries no Fermi-point protection, so the cutoff normalization is unprotected); the obstruction has a definite rank (1, in the borrowed-H shift-covariance, `|Corr| = 1` with power-sign-determined sign, observed as `Corr(a₀,a₂) = +1`); a dimensional proof at the heat-kernel level (Q-PF5: every metric the spectral triple hands you — acoustic Pillar IV, Connes-distance Pillar III, spectral-action a₂→G_N — is `Ô`-valued and `Λ²=M_KK²`-locked, so no NCG route writes `gamma_unit = Φ(D_K alone)`); and two operational falsifiers (R1 kill criterion tests the SOURCE of `w`; rank-1 covariance test tests the COUNT). The verdict is genuinely binary — R1-PASS-rank-0 (falsifies) vs R4-rank-1 (confirms) — because rank-1 forecloses the memo's R2 partial-structure middle. This is registered STAGE-1-CANDIDATE per `joint-theorem-promotion.md`, with R1 pre-registered in S102 as the terminal kill criterion (its FAIL lands Stage-3-permanent, its PASS falsifies), and the capstone §6.3 re-scoped as a POSITIVE deliverable: the substrate determines the conformal-class-complete-PLUS-dimensionless-dynamics content, and is honest that the single second is the externally-calibrated cutoff M_KK — a finding about what the substrate determines, not a deficit confession.

### DISSENT

**No remaining dissent on substance.** The convergence is total — verdict (R4-trigger), the `O = w·Ô` factorization as the K=3-MANDATORY cancellation invariant with FRW as a fourth instance, the rank-1 reading, the matter-sector isomorphism, the n=2 floor-sharpening, the two-falsifier structure, the odd-floor separate-rider, the moment-decoupling caveat, and the Q-PF5 dimensional unreachability at the heat-kernel level. phonon-first's R2 accepted all four of my R2 extensions; I accept all of phonon-first's R2 adjudications and both R2 precisions (the Jacobian-rank-vs-moment-channel reconciliation, and the §XVI.1 anti-pattern reading). I will not manufacture a disagreement to populate this section.

**One precision recorded as the only thing carried into the verdict unresolved-by-design (NOT a dissent — it is a sharpening neither of us contests, but it must be in the registry text or a future reader will mis-test the falsifier):** the rank-1 covariance falsifier (ii) is `|Corr| = 1` with the *power-sign-determined sign pattern*, NOT a blanket `Corr = +1`. My Sage run this session showed `Corr(a₀~M_KK⁴, a₂~M_KK²) = +1` but `Corr(gamma_unit~M_KK⁻¹, a₂~M_KK²) = −1` — both are rank-1 signatures (the covariance matrix `J1·J1ᵀ` is rank 1 regardless of the signs of the entries of `J1`). A future reader who tested "is every dagger-row Corr = +1?" would wrongly read the gamma_unit/a₂ pair (genuine `−1`) as a rank-2 decorrelation. The correct test is `|Corr| = 1` everywhere, with the *sign* of each pair predicted by the relative powers of M_KK. The observed W7-7a `Corr = +1` is the a₀/a₂ (same-sign-power) pair specifically; it is consistent with — and does not exhaust — the rank-1 prediction. This is the only item I flag for the verdict text, and it is a strengthening of the falsifier, not a tension.

### EMERGENCE

**(A) The exact theorem-tag text I endorse for the registry draft.** I endorse phonon-first's E1 final tag verbatim, with my one sign-pattern precision folded into falsifier (ii). The endorsed text:

> **Normalization Non-Universality (N₃=0 corollary, rank-1).** The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology AND all dimensionless dynamical shapes — every ratio, ordering, tilt, growth shape, and the late-time tracking exponent n=2 (S101-W1-QEQ-SELFCONS, the w=0 dust attractor self-consistent) — but does NOT determine the dimensional metric normalization. The obstruction is **rank-1 in the borrowed-H shift-covariance** (`Corr(a₀,a₂) = +1`, W7-7a): a single un-fixed dimensional scale `w = M_KK` projects onto N≥5 observables at different powers (`gamma_unit = ℏ/M_KK c² ~ M_KK⁻¹`; the off-equilibrium absolute vacuum offset ~ M_KK⁴ via a₀/F₋₁; the induced-Newton normalization `1/G_induced ~ f₂·M_KK²·a₂` via a₂/F₊₁; the Friedmann–BCS lock strength T6) through the `O = w·Ô` factorization — which IS the K=3-MANDATORY multiplicative-normalization cancellation invariant (`math-scripts.md`), of which the FRW background is a fourth structurally-distinct instance (spectral-support form = substrate→seconds dimensional-readout weight, distinct from the L_max-truncation / τ-moduli-deformation / Casimir-ceiling instances). **Certificate (two legs):** Half A — the shift-covariance rank equals the number of independent unprotected scales (covariance theorem, Sage-proven: ONE scale at powers `(−1,2,4)` → log-Jacobian column rank 1, `J1·J1ᵀ` rank 1, `|Corr|=1` exact with sign `= sign(p_i·p_j)`; a genuine SECOND independent scale → rank 2 with `v_{w₂} ∉ span(v_{w₁})` → decorrelation possible); Half B — the BDI vacuum (N₃=0, S44) imports exactly one unprotected dimensional scale, the cutoff M_KK (no Fermi-point protection of the induced metric). **Rider (separate finding, NOT part of the rank-1 obstruction):** the conformal-class-complete content is robust EXCEPT where the derived substrate clock resonates with the pair band (S101-W1-QEQ-RELIC-ODDFLOOR FAIL: `ω_q^phys = 2.0128 M_KK` inside `[1.6395, 10.8379]`, `|c_odd|/|c_even| = 2.70e-2`), introducing a relic-induced odd-in-H dynamical correction outside `O = w·Ô` (a pole, not a scale). **Moment-decoupling caveat:** rank-1 covariance does NOT imply single-compute closure — the projections land on algebraically-independent spectral moments (F₋₁ vs F₊₁), so only supplying `w` at the source (the cutoff→lab-units bridge `ℏ/M_KK c²`) closes all channels; closing any single channel's readout closes only that moment's leg. **Two falsifiers:** (i) R1 kill criterion — if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with no imported GeV/seconds scale, the theorem is FALSIFIED (rank-0); (ii) rank-1 covariance test — if any borrowed-H dagger-row shows `|Corr| < 1` (NOT merely `Corr ≠ +1`; the predicted sign of each pair is `sign(p_i·p_j)` of the M_KK powers) under single-H renormalization, Half B's single-cutoff count is wrong, rank ≥ 2, and the memo's R2 partial-structure branch reopens. PROVEN-corollary pending the R1 kill-criterion gate.

**(B) Whether the Half-B provisional input needs its own pre-registered S102 Stage-2 check, or stands on N₃=0 + corpus-zero-counterexample. My ruling: it needs BOTH — a pre-registered check AND the topological basis — and the two are not redundant.** The provisional input is "the BDI vacuum's only unprotected dimensionful scale is the single cutoff M_KK (no second independent dimensionful scale enters the induced action that is not a power of M_KK)." phonon-first's R2 adjudicated this as "a theorem-with-a-named-provisional-input, not a conjecture," resting on (1) N₃=0 (computed invariant, S44) and (2) zero corpus counterexamples. I agree it is not a bare conjecture, but I rule it needs its own pre-registered check in the S102 Stage-2 verify, for a structural reason from my own charge: **N₃=0 establishes the metric is unprotected — it does NOT, by itself, establish that there is exactly ONE unprotected scale rather than two.** N₃ is one momentum-space invariant; the count of independent unprotected dimensionful scales is a separate question (a BDI vacuum could in principle carry a second cutoff-like scale — e.g., if the EW/Higgs scale m_H entered the induced action *independently* of M_KK rather than as a KK-threshold-tied power of it). The corpus-zero-counterexample observation is strong inductive support but is exactly the kind of "absence of evidence" my charge forbids treating as proof. So the Half-B input gets a pre-registered Stage-2 check with a sharp form, and it coincides with falsifier (ii): **the Stage-2 Axis-B reviewer (transit-dynamics / cosmological-bridge axis) pre-registers the covariance-rank check — compute the borrowed-H shift-covariance rank across ALL dagger-rows under a single-H renormalization; PASS iff rank = 1 (`|Corr| = 1` with the predicted sign pattern), FAIL iff any row decorrelates (`|Corr| < 1`).** This is the operational content of "exactly one unprotected scale," it is checkable from the dagger-row data alone (the disjoint anchor my `feedback_stage2-axisB-disjoint-anchor` rule requires — NOT the registry's published rank), and it does not rest on the inductive corpus claim. So: the input STANDS on N₃=0 for its *topological cause* (why the scale is unprotected) AND requires its own pre-registered covariance-rank check for its *count* (why there is exactly one) — the two are orthogonal (cause vs count), neither substitutes for the other, and the count-check IS falsifier (ii) promoted to a Stage-2 clause.

**(C) What the capstone §6.3 re-scope's POSITIVE claim list should be.** Per `phononic-framing.md` (the re-scope is a finding about what the substrate determines, framed as a determination, substrate-IS not deficit-IN) and per the capstone-hygiene Q1 routing (this changes the §6.3 a(t)-gap status, so Q1 fires → designated-writer prose patch). The POSITIVE claim list:

1. **The n=2 late-time tracking exponent** — DERIVED, not fitted (S101-W1-QEQ-SELFCONS PASS: `a_exp = 0.6554` vs 2/3; the w=0 dust attractor `a ∝ t^{2/3}` self-consistent under back-reaction; `kappa_inv=True`, exponent demonstrated `w`-independent). This is the load-bearing addition phonon-first won in R1 and I adopted in R2 — what dies under R4 is the *seconds-valued* a(t), NOT the *shape* of a(t).
2. **The conformal class of the emergent metric** — all dimensionless ratios, orderings, and the light-cone/dispersion structure, invariant under the BLV→Connes representation change (Q-PF5 Step 2: `connes_scales − acoustic_scales = {}` empty; both deliver `Ô` with the single scale M_KK imported at readout — a robustness statement, not a gap).
3. **The dimensionless dynamical shapes** — tilts (n_s-class), growth shapes, the A_FS structure — every log-derivative / ratio observable the spectral triple computes via spectral moments, protected by the K=3 multiplicative-normalization cancellation invariant (the `w`-independence is the protection, since N₃=0 supplies no Fermi-point protection).
4. **The ZFP spine UNAFFECTED** — the zero-free-parameter spine of the framework is entirely `Ô`-content (dimensionless ratios from D_K eigenvalues with zero continuous parameters); the single imported scale M_KK is a *calibration*, not a continuous fit parameter, so the ZFP claim is intact. This is the sharpest positive statement: the framework determines the entire dimensionless dynamical content of the cosmology from zero continuous parameters and imports exactly one externally-calibrated scale (the cutoff M_KK).

What §6.3 does NOT deliver, stated honestly alongside: the single dimensional normalization `w = M_KK` (the seconds-valued a(t), the absolute H₀ in km/s/Mpc as an independent derivation rather than the H0-PROPER-A2 consistency identity, the absolute off-equilibrium vacuum offset). The arrow is unchanged: `D_K eigenvalues → spectral moments → dimensionless dynamical shapes (n=2 tracking, tilts, growth, conformal class) → measurement`; only the terminal `× (ℏ/M_KK c²)` is the external multiplier where the eigenvalue problem is silent by construction. The re-scope down-tags the §6.3 a(t)-claim from "a(t) recoverable" to "conformal-class-complete-PLUS-dimensionless-dynamics; the dimensional second is the externally-calibrated cutoff" — a status reconciliation against Atlas D04 (Q3 of the capstone-hygiene gate fires: PROVEN/CONDITIONAL status change on the §6.3 claim), preserving the substrate-IS frame.

### QUESTIONS

Few and decisive, shaping the verdict table, the Remaining Open Questions, the math CFs, and the Effected-In-Session items. phonon-first answers these in the verdict turn (R3 Turn B).

**(Q-V-R3a — the two S102 math CFs: are both pre-registered, with the rank-1 covariance check distinct from the R1 gate?)** I read TWO distinct S102 compute carry-forwards, not one: (CF-α) the **R1 kill-criterion gate** — `H(τ_now)` from D_K + f + Λ alone within a factor of 2 of 67.4 km/s/Mpc, zero new continuous parameters, derived κ agrees with the S97 consistency value, with the R4 theorem-tag as the kill criterion (FAIL lands Stage-3-permanent, PASS falsifies); and (CF-β) the **rank-1 covariance-test gate** — compute the borrowed-H shift-covariance rank across all dagger-rows under single-H renormalization, PASS iff rank = 1 with `|Corr| = 1` and the predicted sign pattern (this is simultaneously falsifier (ii), the Half-B count-check from EMERGENCE-(B), and the Stage-2 Axis-B pre-registered clause). Do you agree both are distinct pre-registered math CFs for S102 (CF-α tests the SOURCE of `w`, CF-β tests the COUNT), and that CF-β is NOT redundant with CF-α (a rank-0 R1-PASS would moot both, but an R1-FAIL leaves CF-β as the live structural confirmation that the obstruction is genuinely rank-1 and not rank-2-masquerading)? If you read them as one gate, say why the count-check folds into the source-check.

**(Q-V-R3b — CF-coldread-6 disposition in the W4 WP: foreclosed-as-gated, with the independent-Jacobson carve-out explicit?)** Per E3, the rank-1 reading forecloses CF-coldread-6's gating condition (`R1 lands partial structure` is rank-1-impossible). For the no-technical-debt discipline this must be effected in-session, not left as a dangling "conditional next instrument." Confirm the W4 WP CF-coldread-6 disposition reads exactly: *foreclosed-as-gated — the Jacobson partial-structure route's trigger (`R1 lands partial structure`) is a-priori impossible under the rank-1 certificate (Half A Sage-proven + Half B N₃=0-grounded); NOT a deferred CF; an independent Jacobson horizon-entropy derivation pursued for a non-normalization reason is a different object and remains open.* Is this the disposition you write into the W4 WP, and does it route as an Effected-In-Session item (the foreclosure is a constraint-map elimination by structural argument, completed this session) rather than an S102 compute CF?

**(Q-V-R3c — the three Effected-In-Session registry/capstone items: STAGE-1-CANDIDATE draft, capstone Q1 routing, CF-coldread-6 foreclosure — all three land this session?)** The verdict produces three NON-MATH items the final agent effects before terminating: (1) the STAGE-1-CANDIDATE registry draft (theorem-tag text from EMERGENCE-(A), next-free §VII slot per `regulator-pin-discipline.md`, JOINT clauses (a)/(c)/(e) flagged, clause-by-clause axis attribution from E2) — but per `feedback_mack-bridge-role.md` and the cross-pillar-bridge-anatomy rules, does the registry *landing* itself route to the designated registry writer (mack-cosmic-bridge for the falsifier-surface rows; the registry slot for the theorem), or is the workshop's deliverable the *drafted text* that the S102 plan lands? (2) the capstone §6.3 Q1 routing — the capstone-hygiene gate Q1 (a(t)-gap status change) AND Q3 (PROVEN/CONDITIONAL status change on the §6.3 claim) both fire; this routes to the capstone designated-writer prose patch with the POSITIVE claim list from EMERGENCE-(C), recorded in `session-101-housekeeping.md §A` (in-session) or §B (compute CF) — which section? (3) the CF-coldread-6 foreclosure in the W4 WP (Q-V-R3b). Confirm all three are Effected-In-Session (or correctly routed) and name, for each, whether it is an in-session designated-writer fix or a routed S102 item — so the verdict table's Effected-In-Session block is complete and no item dangles.

---

## Round 3 — phonon-first: Cross-Synthesis

### CONVERGENCE

The workshop is converged, and I ratify volovik's R3 in full. The final agreed position, in one paragraph: **R4-trigger with the positive-deliverable re-scope; the §6.3 obstruction is a single rank-1 normalization non-universality with a two-leg certificate, a topological cause, a heat-kernel-level dimensional proof, and two sign-resolved operational falsifiers.** I accept his three R3 adjudications without qualification. (1) *The Half-A/Half-B two-leg certificate is verified, not transcribed.* volovik re-ran Half A at the **actual** power vector `(−1, +2, +4)` (gamma_unit ~ M_KK⁻¹; induced-Newton `1/G_induced ~ M_KK²` via a₂; absolute V0 offset ~ M_KK⁴ via a₀) and confirmed Sage-exact: log-Jacobian column `J1 = (−1,2,4)ᵀ` rank exactly 1, shift-covariance `J1·J1ᵀ` rank exactly 1, `|Corr| = 1` on every pair; a genuine second scale gives rank 2 with `v_{w₂} ∉ span(v_{w₁})`, making decorrelation structurally possible. This is the certificate I built in R2-CONVERGENCE-(2) (Half A proven, Half B = the BDI single-cutoff topological count, N₃=0/S44), and his independent verification at the real powers — not my published numbers — is exactly the disjoint-anchor cross-check the Stage-2 discipline demands. The two-leg form is the right registry structure because it makes the theorem's *status* legible: Half A is PROVEN (never re-litigated), Half B is topologically-grounded with one named checkable input that IS falsifier (ii). (2) *The powers-of-M_KK precision is SHARPENED into a sign-resolved falsifier, and I adopt the sharpening as a correction to my own R2 wording.* My R2-DISSENT pinned "one scale, several powers, several moment-coefficients, one covariance rank" but left the correlation as the blanket observed `+1`. volovik's R3 makes it sharp and I accept it: the rank-1 prediction is `|Corr| = 1` with sign `= sign(p_i·p_j)`, NOT a blanket `Corr = +1`. The observed `Corr(a₀~M_KK⁴, a₂~M_KK²) = +1` (W7-7a) is the **same-sign-power** pair; `Corr(gamma_unit~M_KK⁻¹, a₂~M_KK²) = −1` exactly is a genuine, predicted `−1` that a naive "is every pair +1?" test would misread as rank-2 decorrelation. The correct falsifier-(ii) target is therefore `|Corr| < 1` (NOT `Corr ≠ +1`) on ANY pair — a strengthening, because it gives the second falsifier a sign-resolved target and pre-empts the most likely mis-test. (3) *The Half-B provisional input needs BOTH N₃=0 (cause) AND its own pre-registered covariance-rank check (count), as a Stage-2 clause.* I ratify volovik's EMERGENCE-(B) ruling: N₃=0 establishes the metric is *unprotected* (the topological cause), but it does NOT by itself establish there is exactly ONE unprotected scale rather than two — the count is a separate question (a second cutoff-like scale, e.g. m_H entering the induced action independently of M_KK rather than as a KK-threshold-tied power, is logically admissible), and the corpus-zero-counterexample observation is inductive support I am charged not to treat as proof. So the count gets its own pre-registered Stage-2 check, which coincides exactly with falsifier (ii) and the Half-B count-check — the Axis-B reviewer computes the borrowed-H shift-covariance rank across all dagger-rows under single-H renormalization, PASS iff rank = 1 with `|Corr| = 1` and the predicted sign pattern. Cause (N₃=0) and count (covariance-rank check) are orthogonal; neither substitutes for the other. I also endorse the capstone §6.3 POSITIVE claim list (n=2 tracking DERIVED; conformal class invariant under BLV→Connes; dimensionless dynamical shapes; ZFP spine unaffected) as the correct substrate-IS constraint-map statement — a finding about what the substrate determines, not a deficit confession.

### DISSENT

**No remaining dissent on substance.** The convergence is genuine and total across both pillars — verdict (R4-trigger, positive re-scope), the `O = w·Ô` factorization as the K=3-MANDATORY multiplicative-normalization cancellation invariant with FRW as a fourth structurally-distinct instance, the rank-1 reading, the two-leg Half-A/Half-B certificate, the sign-resolved falsifier (ii), the matter-sector isomorphism (H0:H_obs :: M₀:m_H :: m_D:oscillation-data), the n=2 floor-sharpening, the odd-floor separate-rider, the moment-decoupling caveat, and the Q-PF5 heat-kernel-level dimensional unreachability. volovik's R3 verified the certificate at the actual power vector via his own anchor, sharpened my under-stated correlation into a sign-resolved falsifier, and ruled the Half-B dual-grounding correctly. Two agents reached the same theorem from opposite directions (his superfluid-universe-topology side, my cross-domain-factorization side); the agreement is structural, not shared-context restatement, which is precisely why E2 stages it for an independent S102 Stage-2 cross-axis verify with both authoring agents (volovik, phonon-first) **excluded** per `joint-theorem-promotion.md §"Stage 2"` condition 2. I record the convergence rather than manufacture a disagreement. The one item carried into the verdict text unresolved-by-design is volovik's sign-resolved falsifier precision (`|Corr| = 1` with sign `= sign(p_i·p_j)`, NOT blanket `+1`) — and it is a strengthening I adopt, not a tension.

### EMERGENCE

I answer volovik's three R3 questions (Q-V-R3a/b/c) by label; these shape the verdict table, the open questions, the math CFs, and the Effected-In-Session block.

**(Q-V-R3a — TWO distinct S102 math CFs, the rank-1 covariance check NON-redundant with the R1 gate: CONFIRMED.)** Yes — there are TWO distinct, non-redundant, pre-registered S102 compute carry-forwards, and I confirm the non-redundancy with the substitution-chain reason rather than asserting it. **CF-α = the R1 kill-criterion gate** (`H(τ_now)` from D_K + f + Λ alone within a factor of 2 of 67.4 km/s/Mpc, zero new continuous parameters, derived κ agrees with the S97 consistency value `κ_nat = 8.86e-42 s`, with the R4 theorem-tag as kill criterion): it tests the **SOURCE** of `w` — *can the substrate→seconds scale be written from D_K eigenvalues alone, or must it import the GeV/seconds bridge?* Its FAIL is the theorem's predicted confirmation (rank-1); its PASS is the theorem's falsification (rank-0). **CF-β = the rank-1 covariance-test gate** (compute the borrowed-H shift-covariance rank across all dagger-rows under single-H renormalization; PASS iff rank = 1 with `|Corr| = 1` and the predicted sign pattern `sign(p_i·p_j)`): it tests the **COUNT** of `w` — *is there genuinely one unprotected scale, or two?* It is simultaneously falsifier (ii), the Half-B count-check (EMERGENCE-(B)), and the Stage-2 Axis-B pre-registered clause. They are NON-redundant because they attack the two structurally-independent halves of the rank-1 theorem: CF-α attacks the *existence* of an un-fixed `w` (rank-0 vs rank≥1); CF-β attacks the *count* (rank-1 vs rank≥2). The decisive non-redundancy argument: a rank-0 R1-PASS (CF-α PASS) would moot both — but an **R1-FAIL does NOT settle CF-β**. R1-FAIL establishes only that `w` is not spectral-data-derivable in the ONE channel R1 probes (the FRW gamma_unit readout); it leaves open whether the SAME single `w` projects onto all N≥5 channels (rank-1) or whether a second independent scale rides one of them (rank-2-masquerading-as-rank-1 at the current observable set). CF-β is the live structural confirmation that the obstruction is genuinely rank-1 — it cannot fold into CF-α because "is `w` derivable?" and "how many `w`'s are there?" are different questions on different axes (the source axis vs the count axis), and the covariance rank is observable independently of whether R1 succeeds at deriving the scale. So: two CFs, both pre-registered, both honest-both-branches (no iterate-until-PASS — CF-α's FAIL is the theorem's confirmation and is pre-registered as such; CF-β's `|Corr|<1` outcome reopens R2 and is pre-registered as such).

**(Q-V-R3b — CF-coldread-6 foreclosed-AS-GATED with the independent-Jacobson carve-out: CONFIRMED, and effected this session.)** Yes — the disposition I write into the W4 WP CF-coldread-6 block reads exactly as volovik specifies: *foreclosed-as-gated — the Jacobson partial-structure route's trigger (`R1 lands partial structure`) is a-priori impossible under the rank-1 certificate (Half A Sage-proven + Half B N₃=0-grounded); NOT a deferred CF; an independent Jacobson horizon-entropy derivation pursued for a non-normalization reason is a different object and remains open.* The substitution chain to the foreclosure: CF-coldread-6's gating condition is `R1 lands partial structure` (memo §5); "partial structure" is operationalized as R1 fixing one normalization (V₀) but not another (Z_norm) — i.e., `rank(obstruction) ≥ 2` with one scale closable and another not; the rank-1 certificate establishes `rank = #(unprotected scales) = 1`, so there is no rank≥2 middle where one scale closes and another remains; therefore the gating condition is a-priori impossible and CF-coldread-6's *trigger* is structurally foreclosed. This is a constraint-map elimination by structural argument (not by failed compute), which is exactly the kind of elimination that sharpens the surviving solution space — so per the no-technical-debt discipline it is an **Effected-In-Session item** (an append-only disposition note on the W4 WP CF block, completed this session), NOT an S102 compute CF. The independent-Jacobson carve-out is preserved verbatim: only the partial-structure-gated *role* of the Jacobson route dies; an independent Jacobson horizon-entropy derivation (the S64 d=12 Jacobson chain, `A_horizon_FW = 71226.26 GeV⁻²`, the S84 GGE-entropy-functional-as-V.P. open channel) pursued for a non-normalization reason remains an admissible future item.

**(Q-V-R3c — the three Effected-In-Session items, each tagged in-session-fix vs routed-S102: CONFIRMED with explicit routing per the governing rules.)** The verdict produces three NON-MATH items, and I tag each by lifecycle per `joint-theorem-promotion.md`, `capstone-hygiene-gate.md`, and `feedback_mack-bridge-role.md`:

1. **STAGE-1-CANDIDATE registry draft** — **routed-S102** (NOT written this session). Per `joint-theorem-promotion.md`, Stage 0 is the workshop-internal candidate text with clause-by-clause axis attribution, frozen in the workshop verdict (the EMERGENCE-(A) theorem-tag + the clause tags (a)–(g) from E2); Stage 1 (registry registration as `STAGE-1-CANDIDATE` in the next-free §VII slot per `regulator-pin-discipline.md`) is **next-session** per the rule's pathway. So the EFFECTED-this-session deliverable is the FROZEN Stage-0 candidate text + clause attribution in the Wrap-Up, plus the Stage-1 registration as a routing line that travels with math CF (3) (the Stage-2 verify). I do NOT prematurely write the §VII registry entry this session — the rule stages it next-session, and I follow the rule as written. The registry *landing* itself, when it happens at S102, is the registry-slot writer's domain (the theorem slot) with the §7 falsifier-surface rows routed to mack-cosmic-bridge per `feedback_mack-bridge-role.md`; the workshop's deliverable is the *drafted text*, which the S102 plan lands.

2. **Capstone §6.3 Q1/Q3 routing record** — **in-session-fix (routing record), capstone prose patch routed-S102 to the designated writer.** The R4 verdict is a NEW capstone §6.3 status change for S102: S101 housekeeping A13 recorded Q1 a(t)-gap **NO-CHANGE** (§6.3 narrates a(t) as the open honest gap, prose == register at S101 close); this workshop **CHANGES** that — the §6.3 a(t)-claim down-tags from "a(t) recoverable / open gap" to "conformal-class-complete-PLUS-dimensionless-dynamics; the dimensional second is the externally-calibrated cutoff M_KK." Both Q1 (a(t)-gap status change) AND Q3 (PROVEN/CONDITIONAL status change on the §6.3 claim) fire. Per `capstone-hygiene-gate.md`, a §6.3 PROSE change in a curated doc is a designated-writer reviewed patch (NOT my edit, NOT a bulk append). So the EFFECTED-this-session action is the ROUTING: I append the Q1-YES + Q3-YES routing record (with the POSITIVE claim list from EMERGENCE-(C) and the theorem-tag pointer) to `session-101-housekeeping.md §B` (compute carry-forward / S102 routing), append-only, citing this workshop as provenance. The capstone prose patch itself is the designated-writer's S102 deliverable.

3. **CF-coldread-6 foreclosure in the W4 WP** — **in-session-fix** (Q-V-R3b above). A constraint-map elimination by structural argument, completed this session via an append-only disposition note on the W4 WP CF block. Not an S102 compute item.

All three are accounted for, none dangles. I additionally effect two more non-math items the rounds surfaced: the W5/W1 cross-workshop consistency pointer (§XVI.1 as the rank-1 anti-pattern — a one-line Wrap-Up note) and the EVOI a(t)-gap disposition annotation (minimal, on the Tier-1 row, content-currency marker untouched). The full enumeration is in the Effected-In-Session block below.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | Z_norm: time unit vs kinetic coefficient (question a) | V1, Re:V1 | **Converged** | `Z_norm == G_DeWitt = 5.0` pins ONLY the dimensionless τ̇²-kinetic coefficient; the substrate→seconds slot `gamma_unit = ℏ/(M_KK·c²)` is structurally orthogonal (`gamma_unit ∉ free_variables(H_sub²)`, Sage both sides). No `gamma_unit = Φ(D_K eigenvalues alone)` exists — the codomain (seconds) is dimensionally unreachable from a dimensionless domain; the §8.3 dictionary's codomain is dimensionless (`f₂ = M_Pl/M_KK`), so it emits no time. |
| 2 | V0 sufficiency as vacuum subtraction (question b) | V2, Re:V2 | **Converged** | `V0 = τ-flat` is a *form* statement (`dV0/dτ = 0`), not a value. The ε−μq identity fixes V0 = 0 only AT the equilibrium fixed point; §6.3 lives off-equilibrium (quenched GGE relic, `q(τ) ≠ q_eq`) where the absolute offset is value-open and Ω_Λ-load-bearing (the effacement residual IS `w̃(q(τ)) − w̃(q_eq) ≠ 0`). QEQ-SELFCONS cannot pin V0: its slope-1 is a log-derivative, V0-cancelling by the K=3 multiplicative-normalization theorem (it pins the n=2 exponent, not the offset). QEQ-RELIC-ODDFLOOR FAIL confirms a relic-induced odd-in-H correction the equilibrium identity misses. |
| 3 | Surviving §6.3 obstruction: 2-normalizations vs {T6, C2} (question c) | V3, Re:V3, P2 | **Emerged** | ONE rank-1 obstruction, not "two free normalizations" (readout-layer mis-location) nor an orthogonal {T6, C2} pair. Every costume is `O = w·Ô` — `Ô` the dimensionless/conformal quantity the spectral triple computes, `w = M_KK` the single un-fixed scale entering N≥5 channels at different powers `(−1, +2, +4, …)`. This factorization IS the K=3-MANDATORY multiplicative-normalization cancellation invariant, with FRW as a fourth structurally-distinct instance. T6 (1.33×10⁵ overwhelm) is the dynamical face of the same `w`, not independent. EMERGED: the obstruction has a definite **rank-1** covariance signature (`|Corr|=1`, sign `= sign(p_i·p_j)`), foreclosing the memo's R2 partial-structure middle a priori. |
| 4 | Verdict: R1-survives vs R4-trigger (question d) | V4, P1, P2 | **Converged** | **R4-trigger, positive-deliverable re-scope.** Topological cause: N₃ = 0 (BDI, S44) — induced metric carries no Fermi-point protection, cutoff normalization unprotected. Two-leg certificate: Half A (covariance-rank theorem, Sage-proven) ∘ Half B (BDI single-cutoff count, N₃=0-grounded + own pre-registered count-check). Heat-kernel-level dimensional proof (Q-PF5): every metric the spectral triple hands you — acoustic, Connes-distance, spectral-action a₂→G_N — is `Ô`-valued and `Λ²=M_KK²`-locked. H0-PROPER-A2 PASS is a dimensionless G_N-ratio consistency identity (degenerates to H_obs at N→1), NOT an independent H₀; it STRENGTHENS the reading. Two sign-resolved falsifiers (R1 source-check; rank-1 covariance count-check). Registers STAGE-1-CANDIDATE per `joint-theorem-promotion.md`; §6.3 re-scoped as conformal-class-complete-PLUS-dimensionless-dynamics (n=2 tracking DERIVED). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **The R1 source-check (CF-α) — does any spectral-data-only `gamma_unit = Φ(D_K, f, Λ)` exist?** Pre-registered gate: `H(τ_now)` from D_K + f + Λ alone, within a factor of 2 of 67.4 km/s/Mpc, ZERO new continuous parameters, with the derived κ agreeing with the S97 consistency value `κ_nat = 8.86e-42 s`. **Honest both-branches** (NO iterate-until-PASS): FAIL = the R4 theorem's predicted confirmation (rank-1, `w` external); PASS = the theorem's falsification (rank-0, `w` spectral-data-derivable). The dimensional expectation (workshop-converged) is FAIL — but the gate must actually run, and the branches are pre-registered symmetrically.

2. **The rank-1 covariance count-check (CF-β) — is the obstruction genuinely rank-1, or rank-2 masquerading at the current observable set?** Pre-registered gate: compute the borrowed-H shift-covariance rank across ALL dagger-rows {w₀, wₐ, σ₈, CC, …} under a single-H renormalization. PASS iff rank = 1 with `|Corr| = 1` and the predicted **sign pattern** `sign(p_i·p_j)` of the M_KK powers (so `Corr(a₀~M_KK⁴, a₂~M_KK²) = +1`, `Corr(gamma_unit~M_KK⁻¹, a₂~M_KK²) = −1`). FAIL iff any pair shows `|Corr| < 1` (NOT merely `Corr ≠ +1`) — which reveals a second independent unprotected scale, falsifies Half B's single-cutoff count, and reopens the memo's R2 partial-structure branch. This is simultaneously falsifier (ii) and the Stage-2 Axis-B clause.

3. **Stage-2 two-agent cross-axis verify of the STAGE-1-CANDIDATE theorem (S102).** After Stage-1 registration, dispatch TWO cross-reviewers on DIFFERENT axes, in parallel, WITHOUT prior workshop context (read only the registered Stage-1 entry, NOT this transcript): Axis A = spectral/NCG-axiomatic (audits clauses (c)/(d) + JOINT); Axis B = transit-dynamics / cosmological-bridge (audits clauses (b)/(f) + JOINT). JOINT clauses (a)/(c)/(e) PASS-AND'd across both verdicts. **Authoring agents volovik and phonon-first are EXCLUDED** per `joint-theorem-promotion.md §"Stage 2"` condition 2. Substrate-input-orthogonality is naturally satisfiable (clause (a) consumes only the Sage log-Jacobian; clause (b) the S44 N₃=0 invariant — orthogonal data → structural-INPUT independence).

4. **Independent Jacobson horizon-entropy derivation (carve-out, NOT normalization-gated).** CF-coldread-6's partial-structure-gated role is foreclosed (rank-1), but an independent Jacobson route — Clausius δQ = T dS on local acoustic horizons, the S64 d=12 Jacobson chain, `A_horizon_FW = 71226.26 GeV⁻²`, the S84 GGE-entropy-functional-as-V.P. open channel — pursued for a non-normalization reason (e.g. a separate derivation of horizon entropy) remains admissible. No pre-registered gate yet; flagged as an open item, not a queued compute, unless a future session motivates it independently.

5. **The relic odd-floor rider (QEQ-RELIC-ODDFLOOR FAIL) — its abundance/spectral consequence.** The rider (`ω_q^phys = 2.0128 M_KK` inside the pair band `[1.6395, 10.8379]`, `|c_odd|/|c_even| = 2.70e-2`) is a dynamical pole, NOT a missing scale, so it sits outside the rank-1 `O = w·Ô` obstruction and is carried as a separate theorem-tag rider. Open: whether the live narrow-band resonance contributes a measurable relic-abundance / spectral feature. This is the existing `CF-S102-OQ5-RECTIFIED-DRIVE` (W4 WP) — already queued; cross-linked here as the rider's forward compute.

6. **Does the m_H/EW-VEV scale enter the induced action independently of M_KK, or as a KK-threshold-tied power?** This is the explicit content of the Half-B single-cutoff input on the matter-sector face. If m_H is a KK-threshold-tied power of M_KK (the framework's |S|²-fiber-oscillation picture), the matter-sector twins (M₀←m_H, m_D) ride the SAME single `w` and rank-1 holds; if m_H is genuinely independent, the count is ≥2. Operationally folded into CF-β's covariance check (the matter-sector dagger-rows are included), but flagged separately because it is the most physically distinct candidate for a second unprotected scale.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- The rank-1 falsifier sharpened from blanket `Corr = +1` to **`|Corr| = 1` with sign `= sign(p_i·p_j)`** of the M_KK powers — the same-sign a₀/a₂ pair is `+1` (the observed W7-7a value), the opposite-sign gamma_unit/a₂ pair is `−1`, both rank-1 signatures (volovik R3, Sage at the actual power vector `(−1,+2,+4)`).
- The two-leg certificate verified Sage-exact at the **actual** power vector (not a toy): `J1 = (−1,2,4)ᵀ` rank 1, `J1·J1ᵀ` rank 1, `|Corr| = 1` on every pair; a second scale → rank 2, `v_{w₂} ∉ span(v_{w₁})`.

#### (b) Structural changes

- **"Two free normalizations" (memo) → ONE rank-1 obstruction `O = w·Ô`** with a topological cause (N₃=0), a definite rank (1), a heat-kernel-level dimensional proof, and two sign-resolved operational falsifiers. The §6.3 obstruction is reclassified from a count-of-free-scalars at the readout layer to a single un-fixed dimensional scale `w = M_KK` projecting onto N≥5 channels.
- **`O = w·Ô` recognized AS the K=3-MANDATORY multiplicative-normalization cancellation invariant**, with the FRW background as a fourth structurally-distinct instance (spectral-support form = substrate→seconds dimensional-readout weight) alongside the L_max-truncation / τ-moduli-deformation / Casimir-ceiling instances. The cosmology gap is the framework's *generic* observable structure, not a cosmology-specific wall.
- **H0-PROPER-A2 PASS reclassified: independent-H₀-derivation → dimensionless G_N-ratio consistency identity** (degenerates to H_obs at N→1; the einstein-theorist's own anchor-degeneracy disclosure). It STRENGTHENS the conformal-class reading rather than puncturing it.
- **The memo's R1→R2 sequencing → a binary** (R1-PASS-rank-0 vs R4-rank-1): the rank-1 reading forecloses R2's partial-structure middle a priori. CF-coldread-6's partial-structure-gated role is dead; the independent-Jacobson carve-out survives.
- **§6.3 a(t)-claim down-tags** from "a(t) recoverable / open gap" (S101 A13 Q1 NO-CHANGE) to "conformal-class-complete-PLUS-dimensionless-dynamics; the dimensional second is the externally-calibrated cutoff M_KK" — a NEW capstone-hygiene Q1+Q3 status change for S102, framed as a POSITIVE deliverable.

### What Holds

- **The framework determines the ENTIRE dimensionless dynamical content of the cosmology from zero continuous parameters** — every ratio, ordering, tilt, growth shape, the ZFP spine, AND the late-time tracking exponent n=2 (S101-W1-QEQ-SELFCONS PASS, the w=0 dust attractor `a ∝ t^{2/3}` self-consistent, exponent demonstrated `w`-independent). What dies under R4 is the *seconds-valued* a(t), NOT the *shape* of a(t).
- **The N₃=0 topological cause** (BDI class, S44) — a computed substrate invariant, NOT an external analogy. The induced metric carries no Fermi-point protection, so the cutoff normalization is unprotected; this is the substrate-first statement of the kinematics-not-dynamics wall, replacing the memo's UNVERIFIED-AS-CITED Sakharov/Volovik/BLV literature pointers.
- **The conformal class is invariant under the BLV→Connes representation change** (Q-PF5 Step 2: `connes_scales − acoustic_scales = {}` empty) — both deliver `Ô` with the single scale M_KK imported at readout. A robustness statement: the non-universality survives the metric-representation change.
- **The S96 internal-dimensionless-count closure stands** (`residual_free_normalization_count = 2 → 0`; Z_norm = G_DeWitt, V0 = τ-flat) — real and correct *for the internal free scalars*; the surviving obstruction is the *dimensional* slot `gamma_unit` and the *off-equilibrium absolute* V0, NOT the internal count.

### What Breaks or Strains

- **The off-equilibrium absolute V0 is not merely unpinned but dynamically corrected**: QEQ-RELIC-ODDFLOOR FAIL places a live odd-in-H parametric force (`ω_q^phys = 2.0128 M_KK` inside the pair band, `|c_odd|/|c_even| = 2.70e-2`) on the post-fold relic the equilibrium ε−μq identity cannot see. Carried as a SEPARATE theorem-tag rider (a dynamical pole, NOT a missing scale — it fails the `O = w·Ô` test by construction).
- **The Half-B single-cutoff input is provisional** (one named checkable input): N₃=0 establishes the metric is unprotected (cause) but not that there is exactly ONE unprotected scale (count). The corpus-zero-counterexample is inductive support, not proof; the count gets its own pre-registered Stage-2 covariance-rank check (= falsifier (ii)). The most physically distinct candidate for a second scale is m_H/EW-VEV entering the induced action independently of M_KK (Open Question 6).

### Carry-Forward Computations (MATH ONLY — propagate to S102)

#### CF (1) — CF-α: R1 source-check gate (FALSIFIER (i) of the theorem-tag)

1. **What**: compute `H(τ_now)` from D_K + f + Λ alone, testing whether `gamma_unit = Φ(D_K eigenvalues alone)` can be written with no imported GeV/seconds scale. Reframed as **FALSIFIER (i)** of the Normalization-Non-Universality theorem-tag: its FAIL is the theorem's predicted confirmation (rank-1), its PASS is the theorem's falsification (rank-0). Honest both-branches, NO iterate-until-PASS — both branches pre-registered symmetrically.
2. **Inputs**: D_K spectral data (L_max=12 master cache `s84_spectrum_cache_L12_tau019.npz`); f (gauge connection); Λ = M_KK cutoff; `G_DeWitt = 5.0` (S42); the §8.3 dictionary `f₂ ≈ 92 = M_Pl/M_KK`; the S97 consistency value `κ_nat = 8.86e-42 s`; `M_KK_inv_seconds = 8.860439881925477e-42` (S96-W1-MKK-SECONDS).
3. **Gate**: PASS iff `H(τ_now)` lands within a factor of 2 of 67.4 km/s/Mpc with ZERO new continuous parameters AND the derived κ agrees with `κ_nat = 8.86e-42 s`; FAIL iff it imports M_KK-in-GeV (fails the zero-new-parameter clause) — FAIL is the pre-registered theorem confirmation.
4. **Effort**: 1 wave. **Depends on**: the STAGE-1-CANDIDATE registration (math CF 3); D_K cache; the §8.3 dictionary. (This is the existing EVOI Tier-1 row 1 surviving route + the memo's R1, reframed as falsifier (i).)

#### CF (2) — CF-β: rank-1 covariance count-check gate (FALSIFIER (ii) / Stage-2 Axis-B clause)

1. **What**: compute the borrowed-H shift-covariance rank across ALL dagger-rows {w₀, wₐ, σ₈, CC, matter-sector rows} under a single-H renormalization; test rank = 1 with the sign-resolved correlation pattern. Reframed as **FALSIFIER (ii)** (the COUNT axis) AND the Half-B count-check AND the Stage-2 Axis-B pre-registered clause — one gate, three roles.
2. **Inputs**: the borrowed-H dagger-row observable set + their M_KK power vector `(−1, +2, +4, …)` per channel (gamma_unit ~ M_KK⁻¹; 1/G_induced ~ M_KK²; absolute V0 ~ M_KK⁴; matter-sector rows); the W7-7a `Corr(a₀,a₂) = +1` anchor; the substrate-natural disjoint anchor (the dagger-row data, NOT the registry's published rank, per the Stage-2 Axis-B disjoint-anchor discipline).
3. **Gate**: PASS iff covariance rank = 1 with `|Corr| = 1` on every pair AND the sign of each pair = `sign(p_i·p_j)` of the M_KK powers; FAIL iff any pair shows `|Corr| < 1` (NOT merely `Corr ≠ +1`) — a `|Corr| < 1` reveals a second independent unprotected scale, falsifies Half B's single-cutoff count, and reopens the memo's R2 partial-structure branch.
4. **Effort**: 0.5–1 wave. **Depends on**: the dagger-row observable set; the STAGE-1-CANDIDATE registration (math CF 3). NON-redundant with CF (1): CF-α tests the SOURCE of `w` (rank-0 vs ≥1), CF-β tests the COUNT (rank-1 vs ≥2); an R1-FAIL leaves CF-β as the live structural confirmation that the obstruction is genuinely rank-1.

#### CF (3) — S102 Stage-2 two-agent cross-axis verify of the STAGE-1-CANDIDATE theorem

1. **What**: register the Normalization-Non-Universality theorem-tag (EMERGENCE-(A) text below) as `STAGE-1-CANDIDATE` in the next-free §VII slot per `regulator-pin-discipline.md`, then run the Stage-2 two-agent cross-axis verify per `joint-theorem-promotion.md §"Stage 2"`. JOINT clauses (a)/(c)/(e) PASS-AND'd across both verdicts; clause-by-clause axis attribution per Stage-0 (frozen below).
2. **Inputs**: the frozen Stage-0 candidate text (this workshop, §"Stage-0 frozen candidate" below); the clause attribution (a)–(g); the Sage log-Jacobian (clause (a) anchor); the S44 N₃=0 invariant (clause (b) anchor).
3. **Gate**: Stage-2 PASS-AND requires BOTH cross-reviewers to independently PASS their single-axis clauses AND every JOINT clause; any clause FAIL holds the theorem at Stage-1. **Authoring agents volovik (volovik-superfluid-universe-theorist) and phonon-first (phonon-first-cosmologist) are EXCLUDED** as cross-reviewers per Stage-2 condition 2. Axis A = spectral/NCG-axiomatic; Axis B = transit-dynamics / cosmological-bridge. Substrate-input-orthogonality satisfiable (clause (a) Sage log-Jacobian vs clause (b) S44 invariant — orthogonal data).
4. **Effort**: 1 gate (Stage-1 registration) + 1 dual-dispatch Stage-2 gate. **Depends on**: this workshop's frozen Stage-0 text; the reviewer-exclusion audit (`_joint_theorem_independent_verify_audit.py --check-reviewers --strict`). Stage-3-PERMANENT lands on R1-FAIL (CF-α) + sustained `|Corr|=1` (CF-β); R1-PASS or `|Corr|<1` falsifies.

### Effected In-Session (NON-MATH — completed by the final agent BEFORE TERMINATING)

The non-math items surfaced across all rounds, each EXECUTED this session with concrete file edits (or correctly routed per the governing staging rule), tagged in-session-fix vs routed-S102:

- [x] **Stage-0 freeze: full candidate theorem-tag text + clause-by-clause axis attribution** — frozen in this workshop's Wrap-Up (§"Stage-0 frozen candidate (Normalization Non-Universality, N₃=0 corollary, rank-1)" below); Stage-1 registration travels with math CF (3), routed-S102 per `joint-theorem-promotion.md` (Stage-1 registration is next-session by the rule's pathway; I do NOT prematurely write the §VII registry entry this session) — `sessions/session-101/workshops/s101-normalization-non-universality-workshop.md §"Stage-0 frozen candidate"` — **in-session-fix (freeze) + routed-S102 (Stage-1 registration)**
- [x] **Capstone-hygiene Q1+Q3 routing record** — appended the Q1-YES + Q3-YES routing record (with the POSITIVE claim list and the theorem-tag pointer) to the housekeeping ledger §B; the §6.3 PROSE patch is the capstone designated-writer's S102 deliverable, NOT my edit (curated doc per `capstone-hygiene-gate.md`) — `sessions/session-101/session-101-housekeeping.md §B` — **in-session-fix (routing record) + routed-S102 (designated-writer prose patch)**
- [x] **CF-coldread-6 disposition: FORECLOSED-AS-GATED with the independent-Jacobson carve-out** — appended a disposition note to the CF-coldread-6 block (append-only, did NOT delete); the partial-structure-gated role is dead (rank-1-impossible trigger), the independent-Jacobson carve-out preserved — `sessions/session-101/session-101-w4-workingpaper.md §"Carry-Forward Computations" (CF-coldread-6 block)` — **in-session-fix (constraint-map elimination by structural argument)**
- [x] **W5/W1 cross-workshop consistency pointer** — recorded here (workshop-doc only, per spawn): the W-1 closed-coincidence record (`constraint-mega-matrix.md §XVI.1`, the ≈6.95 non-bridge) independently validates falsifier (ii)'s co-variation discriminator. §XVI.1 closed the 6.95 coincidence precisely because its two factors do NOT co-vary under a single scale-shift (regulator-fragile, 20.816× headroom) — the exact INVERSE of the co-varying single-`w` rank-1 obstruction. The framework's own §XVI machinery (co-variation-under-single-scale-shift as the bridge/non-bridge discriminator) is the SAME diagnostic falsifier (ii) applies to the dagger-rows. No conflict (disjoint observable sets); a robustness cross-link. — `sessions/session-101/workshops/s101-normalization-non-universality-workshop.md` (this note) — **in-session-fix (Wrap-Up note)**
- [x] **EVOI a(t)-gap disposition annotation** — appended a minimal disposition annotation to the Tier-1 `A(t)-FRIEDMANN-RECONCILE` row's surviving-route cell (the §6.3 gap is re-framed by this workshop's R4 theorem-tag + two falsifier computes); did NOT restructure the table, content-currency marker untouched (`S101`), full re-rank left to `/rclab-plan` — `sessions/evoi-framework.md` (Tier-1 row 1 surviving-route cell) — **in-session-fix (minimal disposition annotation)**

#### Stage-0 frozen candidate (Normalization Non-Universality, N₃=0 corollary, rank-1)

**Theorem-tag text (frozen Stage-0; the EMERGENCE-(A) endorsed text, volovik R3 + phonon-first E1, sign-pattern precision folded in):**

> **Normalization Non-Universality (N₃=0 corollary, rank-1).** The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology AND all dimensionless dynamical shapes — every ratio, ordering, tilt, growth shape, and the late-time tracking exponent n=2 (S101-W1-QEQ-SELFCONS, the w=0 dust attractor self-consistent) — but does NOT determine the dimensional metric normalization. The obstruction is **rank-1 in the borrowed-H shift-covariance** (`Corr(a₀,a₂) = +1`, W7-7a): a single un-fixed dimensional scale `w = M_KK` projects onto N≥5 observables at different powers (`gamma_unit = ℏ/M_KK c² ~ M_KK⁻¹`; the off-equilibrium absolute vacuum offset ~ M_KK⁴ via a₀/F₋₁; the induced-Newton normalization `1/G_induced ~ f₂·M_KK²·a₂` via a₂/F₊₁; the Friedmann–BCS lock strength T6) through the `O = w·Ô` factorization — which IS the K=3-MANDATORY multiplicative-normalization cancellation invariant (`math-scripts.md`), of which the FRW background is a fourth structurally-distinct instance. **Certificate (two legs):** Half A — the shift-covariance rank equals the number of independent unprotected scales (covariance theorem, Sage-proven: ONE scale at powers `(−1,2,4)` → log-Jacobian column rank 1, `J1·J1ᵀ` rank 1, `|Corr|=1` exact with sign `= sign(p_i·p_j)`; a genuine SECOND independent scale → rank 2 with `v_{w₂} ∉ span(v_{w₁})` → decorrelation possible); Half B — the BDI vacuum (N₃=0, S44) imports exactly one unprotected dimensional scale, the cutoff M_KK (no Fermi-point protection of the induced metric). **Rider (separate finding, NOT part of the rank-1 obstruction):** the conformal-class-complete content is robust EXCEPT where the derived substrate clock resonates with the pair band (S101-W1-QEQ-RELIC-ODDFLOOR FAIL: `ω_q^phys = 2.0128 M_KK` inside `[1.6395, 10.8379]`, `|c_odd|/|c_even| = 2.70e-2`), introducing a relic-induced odd-in-H dynamical correction outside `O = w·Ô` (a pole, not a scale). **Moment-decoupling caveat:** rank-1 covariance does NOT imply single-compute closure — the projections land on algebraically-independent spectral moments (F₋₁ vs F₊₁), so only supplying `w` at the source (the cutoff→lab-units bridge `ℏ/M_KK c²`) closes all channels; closing any single channel's readout closes only that moment's leg. **Two falsifiers:** (i) R1 kill criterion — if a future gate writes `gamma_unit = Φ(D_K eigenvalues alone)` with no imported GeV/seconds scale, the theorem is FALSIFIED (rank-0); (ii) rank-1 covariance test — if any borrowed-H dagger-row shows `|Corr| < 1` (NOT merely `Corr ≠ +1`; the predicted sign of each pair is `sign(p_i·p_j)` of the M_KK powers) under single-H renormalization, Half B's single-cutoff count is wrong, rank ≥ 2, and the memo's R2 partial-structure branch reopens. PROVEN-corollary pending the R1 kill-criterion gate.

**Clause-by-clause axis attribution (Stage-0, for the S102 Stage-2 cross-axis verify):**

| Clause | Content | Axis attribution |
|:--|:--|:--|
| (a) | rank-1 covariance theorem / Half A (one scale → all-ones-up-to-sign log-Jacobian column → `|Corr|=1`; two scales → rank 2) | **JOINT** (volovik EMERGENCE-A + phonon-first Sage proof; cross-domain-pattern / covariance axis) |
| (b) | N₃=0 → BDI single-cutoff count / Half B | **volovik-side** (superfluid-universe-topology axis, S44) |
| (c) | `O = w·Ô` = K=3 multiplicative-normalization cancellation invariant, FRW = fourth instance | **JOINT** (phonon-first Re:V3 + volovik R2-(1); NCG/regulator axis) |
| (d) | Q-PF5 dimensional unreachability through the spectral action (`1/G_induced ~ f₂·Λ²·a₂`, Λ=M_KK) | **volovik-side** (NCG spectral-action axis, his Sage chain) |
| (e) | n=2 tracking exponent inside the protected `Ô` content (floor = conformal class + dimensionless dynamical shapes) | **JOINT** (phonon-first Re:V4 DISAGREE + volovik R2-(4)) |
| (f) | odd-floor separate rider (a pole, not a scale; outside `O = w·Ô`) | **volovik-side** (Q-PF4 resolution; phonon-first Q-PF4 confirmed) |
| (g) | moment-decoupling caveat (rank-1 covariance ≠ single-compute closure; F₋₁ vs F₊₁) | **phonon-first-side** (Re:V2/Q-PF3; volovik DISSENT confirmed) |

JOINT clauses (a)/(c)/(e) require Stage-2 PASS-AND across both cross-reviewers. Sign-resolved falsifier-(ii) precision (clause within (a)): `|Corr|=1` with sign `= sign(p_i·p_j)`, NOT blanket `+1` — volovik R3, phonon-first ratified.

### Closing Line

The §6.3 a(t)-gap is not unfinished algebra — it is a single rank-1 normalization non-universality with a topological cause (N₃=0), a heat-kernel-level dimensional proof, and two sign-resolved falsifiers: the substrate determines the entire dimensionless dynamical content of the cosmology from zero continuous parameters and imports exactly one externally-calibrated scale, the cutoff M_KK.
