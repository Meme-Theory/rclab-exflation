# Seed — Chunk w1 (Wave 1: L_max-envelope axis)

**Date**: 2026-06-11   **Investigator**: phonon-first-cosmologist   **Source**: session-105-w1-workingpaper.md + plan-w1
**Wave summary**: 2 gates, both INFO, 0 PASS / 0 FAIL. JOINT-CONSIDERATION wave on the L_max-envelope axis with structurally disjoint machinery (no shared observable, scheme, convention, or L_max ceiling). §W1-1 (`S105-BRANCH-IV-DIRECT-L1314`): `spread_CAC = 0.0443091` lands cleanly in the pre-registered INFO band (0.025, 0.050] — FB-envelope-bounded (< 0.05) at the deep set {12,13,14} by DIRECT evaluation, NOT W5-2-band-PASS (≤ 0.025); the direct span essentially coincides with the independently-derived S103 Friedrich-Bär upper bound (0.044330), confirming rather than contesting the envelope. §W1-2 (`S105-LOOP-COUNTING-BINDING`): `binding_class = Case-B-Level-2-non-binding` — the normalized Hermitian moment `M_2 = Tr|D_K|²/Tr|D_K|⁰` diverges (`≈ 1.6993·L − 6.0771`, no finite continuum target) and the HKR image is non-nameable on two INDEPENDENT structural grounds; the W16 wall stands. Durable infrastructure: the Sym^13/14 dense-3^p irrep-construction wall was LIFTED by a Gelfand-Tsetlin bosonic-ladder builder (bit-exact vs `irrep_symmetric_power`, sentinel 7.51e-14 over 24 cached sectors).

## No candidates

Wave 1 produced ZERO workshops and ZERO solo reviews. Both gates are INFO, and neither INFO admits a reading-divergence — the defining requirement for a workshop (`Investigating-Workshops.md §"A WORKSHOP IS"` condition 1-2: two+ agents who *disagree* on a concrete number/sign/structural reading).

Walking the rule's §"How to identify a real workshop in session substance" signal-by-signal:

- **FAILs with multiple readings** — none; both gates INFO.
- **INFOs at borderline** — neither INFO is *contested*. §W1-1's `0.0443091` sits inside a pre-registered 3-way band with a pre-registered dual-prior discriminator (INFO → 0.5/0.5 unchanged); it coincides with the S103 FB upper bound (0.044330) computed by an *independent* (Casimir-bound, non-direct) route, so both a saturation-skeptic and a saturation-optimist agree it is "envelope-bounded but not band-converged." §W1-2's Case-B is a *decisive* non-binding determination: both conjuncts of the pre-registered Case-A test (rate-match AND HKR-nameable) returned False on orthogonal grounds — the unbounded D_K gives a divergent moment with no finite continuum target (so `r(m,L)` is undefined, not borderline), and a spectrum-only functional carries no projector/cocycle/Chern structure to bind in *any* regulator scheme (the lizzi-axis functional-robustness note makes this scheme-independence explicit). No marginal-detection-vs-noise disagreement exists.
- **Cross-wave tensions** — Wave 1 is internally JOINT-CONSIDERATION with disjoint machinery (plan §"Wave 1 Machinery-Enumeration Pin" confirms no shared pin); no intra-wave conflict. The only cross-corridor coupling (DESI-WZ-LENSING-BIAS) is pre-registered and resolved deterministically by the INFO outcome (stays capacity-deferred) — surfaced below as a flag, not a tension.
- **Methodology-vs-substrate blurs** — none; both gates ran their locked conventions (CAC mandatory per `regulator-convention-lockdown.md`; `SUBSTRATE-IS-TRUNCATION-ENVELOPE-NAMING`). No contested rule-shape.
- **Convention questions where two perspectives diverge** — the CAC-vs-RDC choice is forced by the demarcation theorem (RDC FORBIDDEN, `w_0^CAC(L=10)=w_0_FW` EXACT by construction, residual 0.0e+00), not adjudicable. The Level-2 binding/non-binding sub-class is exactly what the gate computed, and it landed non-binding decisively.
- **Existing claims needing adversarial testing** — §W1-2 *confirms* the W16 wall and the algebra-axis orthogonality distinction rather than challenging them; §W1-1's branch-(iv) `w_0 = −0.842454` DR3 anchor is honestly tagged as resting on an envelope-bounded-but-not-band-converged moment, with the downstream trigger pre-registered to stay deferred. Nothing is over-asserted in a way that invites adversarial review.

The two genuine Q1 workshop seeds this session (GEM-WORKSHOP from W2-3, GEM-COMMENSURABILITY from W7-4, both pre-recorded in `session-105-housekeeping.md §"Workshop candidates surfaced this session"`) belong to OTHER waves outside this chunk. The Wave-1 substance is a clean two-INFO characterization wave whose outcomes were determined by pre-registered discriminators — the honest count is zero.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **DR3-readiness corridor coupling (NOT a tension; pre-registered resolution)** — §W1-1's INFO holds the `DESI-WZ-LENSING-BIAS` trigger pin (EVOI Tier-3 #11) at **capacity-deferred** per its pre-registered condition ("queue at S106 IF `S105-BRANCH-IV-DIRECT-L1314` lands PASS; on INFO/FAIL stays capacity-deferred" — `sessions/evoi-framework.md` §6 standing-gaps line 203, plan-w1 "Wave 1 → Wave 2 Decision Point" Item-1 table). The INFO outcome correctly fires the deferred branch. The EVOI table already records this corridor (Tier-2 #7c branch-iv evaluator at line 57; Tier-3 #11 at line 69); the S104 note flags it also couples to the gem-triage §7-D cosmic-dipole / large-scale-frame frontier. **No EVOI-table action needed** — the open item and its trigger condition are already in the guiding star; the W1 INFO simply leaves both rows in their existing capacity-deferred state. Flagged only so the consolidator can confirm the cross-corridor coupling is already EVOI-registered and not a newly-surfaced frontier.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **(none — genuine future computation)** — the W1 WP `## Carry-Forward Computations` section correctly records "No carry-forwards: all wave outcomes closed in-session." The plan's two conditional CFs did not fire (the S106 §VII registry-landing required §W1-2 Case-A — it landed Case-B; the L≥15 feasibility study required a §W1-1 FAIL — it landed INFO). I concur: no genuine forward-compute CF arises from Wave 1.

- **Process observation (Q2-hygiene; already handled in-session — NOT a new CF)** — §W1-1's plan-pinned sentinel tolerance read "max|λ_GT − λ_cache| = 0.0 EXACT (bit-exact)" while the realized sentinel was 7.51e-14 (machine-epsilon FP-reorder noise on the same `eigvalsh(i·D)` path the cache used). This precision-pin-vs-realization gap was honestly disclosed in §W1-1 and the companion Hermiticity-guard dimension-scaling correction (1.0e-15 → max(1.0e-15, √D·ε)) is listed under the WP's "Effected In-Session (NON-MATH)" block (verdict unchanged, regime flipped MARGINAL→VALID only). This is Q2-class (a plan-text precision-pin reconciliation, not a derivation producing a new structural claim) and was already effected in-session — it is recorded here as a one-sentence process observation only, NOT propagated as a workshop or a new carry-forward, and it is NOT a wave-synthesis miss (the WP already disclosed it). A forward-design note for future Sym^p GT-builder sentinels: pin the eigenvalue-sentinel tolerance to the machine-epsilon floor `√(D_max)·ε` rather than literal `= 0.0`, since the `eigvalsh` path cannot deliver bit-exact-zero reorder agreement.

## Wave-by-wave digest (consolidator background)

**Wave 1 — L_max-envelope axis (JOINT-CONSIDERATION; both INFO).**

Two coordinated computes descending from S104 machinery-blocked (not physics-blocked) carry-forwards, sharing only the read-only `s84_spectrum_cache_L12_tau019.npz` and the L_max axis — disjoint observables, schemes, conventions, and L_max ceilings by construction (plan-w1 "Wave 1 Machinery-Enumeration Pin").

- **§W1-1 `S105-BRANCH-IV-DIRECT-L1314` (spectral-geometer) — INFO** (`audit 71c162b0…`). The S104 dense-3^p irrep wall (Sym^13 ≈ 40.7 TB, Sym^14 ≈ 366 TB) is LIFTED by a Gelfand-Tsetlin / bosonic-ladder (p,0) builder constructing directly in the dim_sym = (p+1)(p+2)/2 occupation space (0.002 s, never forming 3^p; bit-exact 1e-16..1e-15 vs `irrep_symmetric_power`; Phase-1 sentinel PASS 7.51e-14 over 24 cached p≤12 sectors; level-14 mixed via the existing Casimir-projection path, herm_err 1.13e-15). `spread_CAC = max−min over {12,13,14} = 0.0443091` (offset-cancellation residual 1.11e-16 — the CAC additive offset_B = −0.340827 cancels EXACTLY in the span, substitution-chain verified). Anchor identity `w_0^CAC(L=10) = −0.918` EXACT (resid 0.0e+00; demarcation theorem holds by construction). Decrement monotone-decreasing and DECELERATING (d(12→13)=−0.0236, d(13→14)=−0.0207; flattening toward a truncation asymptote; 3-tuple sign=PASS/magnitude=INFO/regime=VALID → composite INFO). The direct spread coincides with the S103 FB upper bound 0.044330 (the S103 mid-point prior slightly under-estimated the tail). Solution-space: branch-(iv) `w_0` Mellin-zeta channel is FB-envelope-BOUNDED (< 0.05) but NOT truncation-SATURATED into the W5-2 PASS band (≤ 0.025); the DR3 falsifier prediction `w_0_B = −0.842454` rests on an envelope-bounded-but-not-band-converged moment; DESI-WZ-LENSING-BIAS trigger stays capacity-deferred (dual-prior INFO → 0.5/0.5).

- **§W1-2 `S105-LOOP-COUNTING-BINDING` (lizzi-spectral-functional-theorist) — INFO** (`audit a3c4bf51…`). Normalized Hermitian second moment `M_2^(L) = Tr|D_K|²/Tr|D_K|⁰` on the s84 L=12 fold spectrum: 7.617 (L=8) → 14.422 (L=12), monotonically DIVERGING with shell increments themselves GROWING (1.5565 → 1.8667; linear fit `≈ 1.6993·L − 6.0771`). The substrate D_K is an UNBOUNDED operator (spectral ceiling grows linearly, 3.92→5.42), so the normalized mean-square has NO finite thermodynamic limit (`w_m = +∞`); `r(m,L)` is undefined (nan), `alpha_fit` undefined, `rate_match=False`. The Case-A test is a CONJUNCTION (rate-match AND HKR-nameable); BOTH fail. HKR-nameability fails on two independent grounds: (1) `M_2` is an algebra-INVARIANT spectrum-only functional (sum over |λ|, no projector/cocycle/Chern character) carrying no HP^1 cohomology-class content — the §VII.AF.1 binding image is an algebra-DEPENDENT cocycle pairing with no domain here (algebra-axis orthogonality); (2) even setting (1) aside, the divergence leaves no finite continuum target for any image to land on. Verdict: `binding_class = Case-B-Level-2-non-binding` — the W16 wall stands, NO registry landing licensed; S104 `registry=INCOMPLETE-PENDING` → `BINDING-DECIDED-non-binding` (dual-prior 0.9 → Track B). Functional-robustness note (lizzi-axis): the non-binding verdict is scheme-independent — the spectrum-only observable carries no cohomology-class to bind in *any* regulator (cutoff/zeta/Mellin all weight the same unbounded |λ| support); what §VII.AF.1 has and this moment lacks is a *projector* (band-0 Chern character) regulating the trace into a convergent pairing.

**Team-lead synthesis** (WP §"Wave 1 Synthesis"): JOINT-CONSIDERATION outcome — both gates INFO; the L_max-envelope axis is characterized, not closed. Machinery disjointness held by construction. Enabling infrastructure (GT builder + the normalization-trap hazard) recorded in `.claude/agent-memory/spectral-geometer/gt-builder-high-L.md`. No carry-forwards (both conditional CFs unmet). Constraint-map: branch-(iv) channel S103-FB-INFO → S105-DIRECT-INFO; loop-counting S104-INCOMPLETE-PENDING → BINDING-DECIDED-non-binding; DESI-WZ-LENSING-BIAS unchanged capacity-deferred.
