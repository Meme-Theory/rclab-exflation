# Session 116 Wave 9 — Q36: does the D_K p+q=15 shell shift a canonical observable, or is the spectrum Friedrich-Bär-saturated at p+q≤14? (Results Working Paper)

**Session**: 116 | **Wave**: 9 | **Plan**: session-116-plan-w9.md | **Theme**: Q36 — observable-decomposition adjudication + branch-(iv) `w0_FW` DR3-class L_max-stability one truncation deeper (p+q=15). Is "Friedrich-Bär saturation" observable-dependent — bottom-K |λ| floor EXACTLY saturated at p+q≤14, ORTHOGONAL to the λ_max-driven `w0_FW` Zubarev moment which SHIFTS (ρ_B(14)=−0.6777 → ρ_B(15)=−0.6962, decelerating)?

**Gate-type mix**: workshop × 1 (`§W9-1`, artifact-existence closure) + compute × 1 (`§W9-2`, `[SIGN]` verdict-line). MIXED wave per `.claude/rules/wave-classification.md`. The two gates are **parallel/independent** (plan "Decision Point Prerequisites: NONE") — the workshop hands NO compute mandate to the sibling; both consume only on-disk inputs. Do not cross-contaminate closure modes: the workshop has NO verdict line and NO MCP-Pre-Compute-Audit block; the compute has both plus the schema-v2 `[SIGN]` 3-tuple.

## Gate Sections

### §W9-1. S116-W9-SATURATION-ADJUD (baptista-spacetime-analyst × spectral-geometer)

**Status**: NOT STARTED
**Gate ID**: `S116-W9-SATURATION-ADJUD`
**Gate type**: `workshop` (2-agent adversarial panel; closes by artifact-existence-with-content per `wave-classification.md §M1` — NO verdict line)
**Trigger**: `[VERIFY]` (adjudication of competing structural readings, not a numerical SIGN gate)
**Classification**: **GEOMETRIC** (the D_K(τ_fold) spectrum at the fixed fold slice; Level-1 single-τ-slice substrate-IS per `phononic-framing.md` — the fabric itself, not its excitations)
**Agents**: `baptista-spacetime-analyst` (λ_max-driven-`w0`-moment pole — the branch-(iv) DESI-falsifier-surface observable) × `spectral-geometer` (bottom-K / heat-kernel pole — the Friedrich-Bär saturation home turf)
**Rounds**: 3 (R1 steelman both readings / R2 rebut-the-opponent's-best-case / R3 converge on the STRUCTURAL VERDICT)
**Hypothesis**: Q36 ("does p+q=15 matter?") has NO single answer — "Friedrich-Bär saturation" is observable-dependent: the bottom-K |λ| floor is EXACTLY saturated at p+q≤14 (smallest p+q=15 eigenvalue |λ|_min≈4.216 ≫ bottom-20 ceiling 0.845; bottom-64 max|diff|=0.0e+00) ORTHOGONAL to the λ_max-driven branch-(iv) `w0_FW` Zubarev moment ρ_B(L), which SHIFTS at p+q=15 (ρ_B(14)=−0.677718 → ρ_B(15)=−0.696174, decelerating). **EXPECTED structural verdict (the workshop must DERIVE it, not assume it): a 2-component observable-DECOMPOSITION** — bottom-K Friedrich-Bär-saturated (spectral-geometer correct) ⊥ λ_max-driven `w0_FW` DR3 moment shifts (baptista correct); the S87 W11-2/W11-3 Friedrich-Bär theorem's saturation SCOPE is the bottom-K + bulk low-|λ| moments, NOT the λ_max-tail-sensitive moments.
**Plan reference**: `sessions/session-plan/session-116-plan-w9.md` §W9-1 (`workshop:` block — agents, rounds, sources, `adjudication_question` with the 3 sub-questions + 2 readings, plan-freeze empirical anchors, substrate framing).

**Artifact-Existence Closure Checklist** (workshop gate — closes by artifact-existence-with-content per `wave-classification.md §M1`; **NO verdict line, NO MCP Pre-Compute Audit block** — a verdict line on a workshop gate is a type error per `gate-verdicts.md §"Investigation-Track Canonical Path"`):
*(pending — confirm the deliverable `sessions/session-116/workshops/s116-w9-saturation-adjud.md` EXISTS (`ls`) AND paste `grep -E` output for every `must_contain` marker from the plan `output_artifacts.workshop_md` block: `## R1` (steelman both readings), `## R2` (each agent rebuts the opponent's best case), `## R3` (converge), `## Structural Verdict` (the observable-decomposition + Friedrich-Bär scope statement), `## Wrap-Up` (rclab-workshop closure marker: What Changed / Effected In-Session / Carry-Forward Computations). Any marker returning empty ⇒ the workshop did not properly close — orchestrator SendMessage-continues the same panel per `feedback_dispatch-discipline.md`. Content presence by regex, never line/byte counts per `feedback_max-effort-full-fidelity.md`.)*

**Structural Verdict**:
*(pending — include: the NEW pinned position resolving the two readings and answering the 3 sub-questions — (1) WHICH observable is the canonical Q36 object: the bottom-K |λ| floor, the branch-(iv) `w0_FW` DR3 Zubarev moment, or BOTH as an observable-decomposition with a DECLARED orthogonality; (2) whether the S87 W11-2/W11-3 Friedrich-Bär saturation theorem's SCOPE covers the λ_max-driven `w0` moment or is strictly the bottom-K + bulk low-|λ| moments (and whether it is MIS-CITED if invoked to declare the `w0` DR3 spread "saturated"); (3) whether the decelerating decrement (|d|~1/L²) reaches the PASS band (≤0.025) at finite L or is a slow λ_max-Weyl drift that stays INFO indefinitely — i.e. is the `w0_FW` L_max-stability convergent or merely marginal. Carry the EXPECTED 2-component verdict: bottom-K FB-saturated at p+q≤14 (EXACT) ⊥ λ_max-driven `w0_FW` moment shifts (decelerating); FB scope = bottom-K + bulk low-|λ|, NOT λ_max-tail. Declare it a Level-1 single-τ-slice substrate-IS structural statement; route the registry-scoping note (FB-theorem saturation scope on `§VII.AJ.partition-stability`) to `mack-cosmic-bridge` (sole writer) as Q2 housekeeping per `Investigating-Workshops.md`, NOT a new gate. Substrate framing (direction of explanation): D_K eigenvalues at τ_fold (p+q=15 shell) → {bottom-K floor, λ_max-driven `w0` Zubarev moment} → which observable Q36 is about → Friedrich-Bär saturation SCOPE; the workshop scopes a substrate spectral theorem, it does NOT invoke GR/dark-energy to explain the substrate. R1/R2/R3 rounds + Wrap-Up. Deliverable: `sessions/session-116/workshops/s116-w9-saturation-adjud.md`.)*

---

### §W9-2. S116-W9-GTBUILDER-L15 (baptista-spacetime-analyst)

**Status**: COMPLETED
**Gate ID**: `S116-W9-GTBUILDER-L15`
**Gate type**: `compute` (dual-SHA verdict-line closure; `[SIGN]` ⇒ SIGN/MAGNITUDE/REGIME 3-tuple companion row REQUIRED)
**Trigger**: `[SIGN]` (directional sub-claim: the decrement DECELERATES, |d(14→15)| < |d(13→14)|, AND the CAC offset cancels in the span)
**Classification**: **GEOMETRIC** (a spectral-functional property of the D_K spectrum at the fixed τ_fold slice; NOT a phononic excitation)
**Agent**: `baptista-spacetime-analyst`
**Hypothesis**: Extending the branch-(iv) `w0_FW` DR3-class L_max-stability one truncation deeper to p+q=15 — using the already-constructed p+q=15 sectors (S106 cache `sector_evals_L16`) plus an independent GT-pure `(15,0)/(0,15)` reconstruction cross-check — the deep-truncation sliding-window spread_CAC{13,14,15} lands in the UNCHANGED W5-2 INFO band (0.025, 0.050], NARROWING vs S105's spread_CAC{12,13,14}=0.0443 with a DECELERATING decrement (|d(14→15)|=0.01846 < |d(13→14)|=0.02083), while the bottom-K |λ| floor is EXACTLY unchanged (Friedrich-Bär-saturated at p+q≤14). **EXPECTED verdict INFO** (plan-freeze prediction spread_CAC{13,14,15}=0.039290): p+q=15 SHIFTS the λ_max-driven `w0` moment (a genuine deep-truncation datum) but NOT the bottom-K (structurally redundant there).
**Plan reference**: `sessions/session-plan/session-116-plan-w9.md` §W9-2 (PRDR 8-item machinery pin, strict_PASS_boundary 0.025, both-branch verdict rubric, substitution chain Steps 1–5 with substituted numbers, §W9-2 distinctness ledger, input-SHA ledger).

**Output Artifacts** (closure-verification checklist; mirrors the plan `output_artifacts:` block — content presence by regex, never line/byte counts per `feedback_max-effort-full-fidelity.md`):
All four artifacts confirmed on disk (verified by `ls` + `grep -E`):

- **Script** `computations/session-116/s116_w9_gtbuilder_l15.py` (34,724 B). `grep -E "from canonical_constants import|print_verdict_payload"` → `from canonical_constants import (` (w0_FW, tau_fold, Gamma_effacement, N_cells) + `def print_verdict_payload(...)` + 2 call-sites. Both `must_contain` patterns PRESENT.
- **Data** `computations/session-116/s116_w9_gtbuilder_l15.npz` (16,222 B). PRESENT.
- **Plot** `computations/session-116/s116_w9_gtbuilder_l15.png` (119,956 B; 3-panel — ρ_B(L) with the {13,14,15} sliding window highlighted / w0^CAC(L) vs w0_FW=−0.918 / bottom-K |λ| floor overlap L≤14 vs L≤15). PRESENT.
- **Verdict line** `computations/session-116/s116_gate_verdicts.txt` — canonical line matches `^S116-W9-GTBUILDER-L15:.* audit_sha256=[a-f0-9]{64}`:
  `S116-W9-GTBUILDER-L15: INFO -- value='spread_CAC{13,14,15}=0.0392902 …' scheme=zeta convention=CAC-branch-iv-anchored-L10-DERIVED-OFFSET L_max=15 audit_sha256=94ed48e7c84c9ce1bb87435d0734041ee965b2a7cb24eb656f1db4b5d3315686 content_sha256=85a15ea89c31702b5d848fdda8a0fcf41f2672c41622667e132718bdfb32e031 schema_version=S84+`
  plus the dual-SHA companion row (`audit_sha256_short=94ed48e7c84c9ce1 content_sha256_short=85a15ea89c31702b`), the schema-v2 `[SIGN]` 3-tuple row (`sign_verdict=PASS magnitude_verdict=INFO regime_verdict=VALID`), the `# regulator_pin=a_2^{Mellin} poleconv-A-double (pole_in_s=3, curvature_grade_n=2); cutoff_axis=spectral …` companion row, and the bottom-K-orthogonality companion row. Emitted via the race-safe `emit_verdict` knowledge-MCP tool (`session=116, track=session`; 5 rows appended; sig_5 unique) — NOT a raw `open("a")` append.

**MCP Pre-Compute Audit**:
NOT PRE-CLOSED for this specific gate — the p+q=15 extension is genuinely new (S105 landed {12,13,14} only). Five queries executed before writing the script:

- `search_knowledge("branch-iv w0_FW DR3 L_max stability Zubarev moment spread_CAC")` → `S105-BRANCH-IV-DIRECT-L1314` INFO (spread_CAC=0.0443091, window {12,13,14}); `INV13-W1-3-BRANCH-IV-W0-L1516-DR3` FAIL (spread_CAC=0.0629703, FULL window {12,13,14,15,16}); `S84-DR3-RESPONSE-PROTOCOL` PASS (R_842 locked). The INV13 line is the **investigation track** (exploratory; `gate-verdicts.md §"Investigation-Track Canonical Path"`) — this session gate is the canonical {13,14,15} sliding-window landing; the INV13 full-window 0.0629703 reproduces my full-window diagnostic 0.06297029 bit-for-bit (independent corroboration).
- `get_constant("w0_FW")` → **−0.918** (S58 four-fold-lock: Volovik vacuum partition + effacement Γ_eff=0.99970; Superseded=False). The CAC offset anchor.
- `get_constant("tau_fold")` → **0.19** (S12/S42, gate CONST-FREEZE-42; Superseded=False). The Jensen deformation slice.
- `trace_entity("Friedrich-Bar saturation theorem")` → `§VII.AJ.partition-stability` (S87, 4-stratum cardinality (2,4,8,6) at τ_fold=0.190); "L_max robustness: L_max=10 per S89 W3-1 PASS; Friedrich-Bär saturation theorem analytically certifies bottom-K" (PROVEN). The theorem's home turf is the **bottom-K**, confirming the workshop's scope claim.
- `search_knowledge("S106 HIGHL cache p+q=15 sector_evals_L16 eta_FB_lower")` → `S106-W1-HIGHL-CACHE-L1416` PASS (scheme GT-BOSONIC-LADDER+CASIMIR-PROJECTION-MIXED-PARTIAL-FRIEDRICH-BAR; L14 complete; herm_err 1.13e-15) — confirms the cache provenance.

Runtime cache integrity: npz-internal `audit_sha256 = 5af2b7cd09d863491cd30872384f9bc9adc7b0a580c2b7089f28ce9bfda3fcbb` MATCHES the plan pin (integrity_ok=True). Runtime canonical-value verification: `w0_FW=-0.918`, `tau_fold=0.19` (PLAN-TEXT-DRIFT note — `canonical_constants.py` was edited this session W1+W7; `canonical_drift_S116` documented, live SHA `94e99e73…`, the two consumed constants are unchanged and the offset is DERIVED at runtime per `substrate-first-canonical-sourcing.md §(ii.B)`).

**Verdict**: **INFO** (3-tuple: sign=PASS, magnitude=INFO, regime=VALID; composite collapse `magnitude_verdict==INFO ⇒ INFO`). `spread_CAC{13,14,15} = 0.0392902` lands in the UNCHANGED W5-2 INFO band (0.025, 0.050], NARROWING from the S105 complete-lineage `spread_CAC{12,13,14}=0.044514` with a DECELERATING decrement (`|d(14→15)|=0.018456 < |d(13→14)|=0.020834`). This is the plan-freeze-predicted Track-A outcome (prior 0.65; predicted 0.039290, reproduced to 6 sig figs). The branch-(iv) `w0_FW` DR3 moment ρ_B is λ_max-driven and converges slowly (~1/L²); p+q=15 SHIFTS it (a genuine deep-truncation datum) but does NOT touch the bottom-K |λ| floor (Friedrich-Bär-saturated EXACTLY at p+q≤14; bottom-20/64 max|diff|=0.0e+00). All six CC guards PASS, so the span is clean (no PASS→INFO downgrade was needed; INFO is the band verdict itself).

**Results**:
**PRIMARY verdict object** — `spread_CAC{13,14,15} = 0.0392902` vs the UNCHANGED W5-2 band (PASS ≤ 0.025 | INFO (0.025, 0.050] | FAIL > 0.050) ⇒ **INFO**. Output 4-tuple (6 sig figs, Class-8.3 publication precision; DESI DR3 w0 consumer): `(value=0.0392902, scheme=zeta, convention=CAC-branch-iv-anchored-L10-DERIVED-OFFSET, L_max=15)`.

**ρ_B(L) on the COMPLETE (4,4)-filled S106 lineage** (Zubarev branch-(iv) Mellin-zeta moment, S85 W0-7 evaluator imported VERBATIM; self-consistent on `sector_evals_L16`):

| L (p+q) | ρ_B(L) | λ_max(L) | n_modes(L) | w0^CAC(L)=ρ_B(L)+offset_B |
|:--:|:--|:--|:--|:--|
| 10 | −0.575207 | 4.670218 | 80080 | −0.918000 (= w0_FW, anchor) |
| 12 | −0.633204 | 5.418937 | 168896 | −0.975997 |
| 13 | −0.656884 | 5.793479 | 236096 | −0.999678 |
| 14 | −0.677718 | 6.168115 | 323136 | −1.020511 |
| 15 | −0.696174 | 6.542827 | 434112 | −1.038968 |

All three plan-freeze anchors reproduced to the printed digit (ρ_B(13)=−0.656884, ρ_B(14)=−0.677718, ρ_B(15)=−0.696174; λ_max 5.793479→6.168115→6.542827; n_modes 236096→323136→434112).

**CAC offset (DERIVED at runtime; offset cancels in the span).** `offset_B := w0_FW − ρ_B(10) = −0.918 − (−0.575207) = −0.342793`. Demarcation theorem (`regulator-convention-lockdown.md`): `w0^CAC(10) = −0.918000000000000 = w0_FW` EXACTLY, residual = **0.0e+00**. `spread_CAC = spread_ρ` (offset-free), `offset_cancellation_residual = 1.11e-16` (~0, structural).

**schema-v2 `[SIGN]` 3-tuple** → composite INFO:
- `sign_verdict = PASS` — the decrement DECELERATES (`|d(14→15)|=0.018456 < |d(13→14)|=0.020834`) AND is monotone-NEGATIVE AND the offset cancels (residual 1.11e-16 < 1e-9).
- `magnitude_verdict = INFO` — `spread_CAC{13,14,15}=0.0392902 ∈ (0.025, 0.050]`.
- `regime_verdict = VALID` — all six CC guards hold.
- Composite collapse: `magnitude==INFO ⇒ INFO`.

**CC guards (all PASS; regime VALID):**
1. **Cache integrity** — npz-internal `audit_sha256 = 5af2b7cd…` == plan pin (`integrity_ok=True`).
2. **GT-pure (15,0)/(0,15) sentinel** — independent bosonic-ladder reconstruction (D=2176, GPU eigvalsh i*D) reproduces the S106 cache abs_evals: `max|Δλ| = 0.000e+00` (BIT-EXACT) < 1e-10; GT-pure `herm_err = 0.0` (boson i*D exactly Hermitian).
3. **ρ_B(14) cross-lineage** — `sector_evals_L14` vs `sector_evals_L16|level≤14`: diff = **0.00e+00** (bit-exact) < 1e-12.
4. **p+q=15 completeness** — all 16 sectors (0,15)…(15,0) present with level==15 (`complete_15=True`).
5. **Hermiticity** — GT-pure herr_max=0.0; cache herm_err_max=1.13e-15 (stored).
6. **bottom-K saturation** — bottom-20 ceiling 0.845212 (L≤14) == (L≤15), bottom-64 ceiling 1.063714 == ; `max|diff| = 0.0e+00`. The smallest p+q=15 eigenvalue is |λ|_min = **4.216008 ≫ 0.845** (eta_FB_lower=0.392839 bounds the new shell below) — the new shell CANNOT enter the bottom-K.

**DIAGNOSTICS (cross-report, NOT gating):**
- `spread_CAC{12,13,14}` complete-lineage continuity = **0.044514** (vs S105 published 0.0443091 on the s84-incomplete lineage; (4,4)-gap-fill delta = **0.000205**, sub-percent → INFO robust to lineage choice).
- `spread_CAC{12,13,14,15}` full 4-pt window = **0.062970** (> 0.050; includes the L=12 residual transient — NOT the verdict object). Matches the investigation-track `INV13-W1-3` full window 0.0629703 bit-for-bit.

**Substitution chain (Steps 1–5, with substituted numbers):**
- *Step 1 (definitions).* ρ_B(L) = ⟨|λ|⟩_Z(L)/λ_max(L) − 1, ⟨|λ|⟩_Z = Σ d_j w_Z_j|λ_j| / Σ d_j w_Z_j, w_Z=exp(−|λ|²/Λ_Z²), Λ_Z=1.0. d(L→L+1)=ρ_B(L+1)−ρ_B(L).
- *Step 2 (mechanism).* Adding the p+q=15 shell: NUMERATOR ⟨|λ|⟩_Z ≈ fixed — the 16 new sectors have |λ|∈[4.216, 6.543], Zubarev weight exp(−|λ|²)∈[2.6e-19, 1.6e-9], so they carry ~1e-9 relative weight. DENOMINATOR λ_max GROWS 6.168→6.543.
- *Step 3 (simplify).* d(L→L+1) ≈ ⟨|λ|⟩_Z(1/λ_max(L+1) − 1/λ_max(L)) = −⟨|λ|⟩_Z·Δλ_max/(λ_max(L)·λ_max(L+1)).
- *Step 4 (read off direction).* λ_max(L+1)>λ_max(L) ⇒ d<0 (monotone-decreasing; confirmed −0.023680, −0.020834, −0.018456). |d|≈⟨|λ|⟩_Z·Δλ_max/λ_max² with Δλ_max≈0.375 ≈ const and λ_max~O(L) ⇒ |d|~1/L² DECREASES. Direction read-off: `|d(13→14)| − |d(14→15)| = 0.020834 − 0.018456 = +0.00237755 > 0 ⇒ DECELERATING`.
- *Step 5 (verdict + conclusion).* spread_CAC{13,14,15}=0.039290 ∈ (0.025, 0.050] ⇒ INFO. The branch-(iv) w0 DR3 moment converges from below, DENOMINATOR(λ_max)-driven, decelerating |d|~1/L² → Friedrich-Bär saturation of the convergence RATE, not yet the PASS-band VALUE. The bottom-K floor is EXACTLY saturated at p+q≤14 — the two observables DECOUPLE.

**dual_prior re-allocation.** Outcome INFO → 0.9 mass to **track_A** (slow λ_max-driven convergence; bottom-K saturated EXACT, w0 moment marginal-not-converged; FB theorem scopes to bottom-K). track_B-PASS (sharp saturation onset) and track_B-FAIL (deep-truncation divergence) both DISFAVORED — the decrement decelerates, it neither collapses below the trend (PASS) nor accelerates (FAIL).

**fb_pair.** *forward*: `S105-BRANCH-IV-DIRECT-L1314` (GT builder + S85 W0-7 Zubarev evaluator) + `S106-W1-HIGHL-CACHE-L1416` (p+q=15 sectors, cache pin 5af2b7cd…) + `w0_FW` canonical (S58) + S87 W11-2/W11-3 + S89 W3-1 (FB saturation theorem). *backward*: DESI DR3 w0-wa falsifier surface (w0_FW=−0.918; `mack-cosmic-bridge` `falsifier-master-inventory`) + `§VII.AJ.partition-stability` registry scoping (FB theorem bottom-K scope) + `CF-S117-BRANCH-IV-L16` (the p+q=16 deep-truncation successor).

**dual-SHA.** `audit_sha256 = 94ed48e7c84c9ce1bb87435d0734041ee965b2a7cb24eb656f1db4b5d3315686` (script ‖ canonical ‖ pinmap); `content_sha256 = 85a15ea89c31702b5d848fdda8a0fcf41f2672c41622667e132718bdfb32e031` (script). regulator_pin `a_2^{Mellin} poleconv-A-double (pole_in_s=3, curvature_grade_n=2)`; cutoff_axis=spectral (Zubarev Λ_Z=1.0 on the D_K spectrum).

**Substrate-first framing (GEOMETRIC; direction of explanation).** The substrate IS the D_K(τ_fold) spectrum on Jensen-deformed SU(3); D_K is block-diagonal by Peter-Weyl, so the p+q=15 shell is 16 independent finite blocks (Level-1 single-τ-slice substrate-IS). Arrow: D_K eigenvalues at τ_fold (p+q=15 shell) → branch-(iv) Zubarev spectral moment ρ_B(15) → CAC-anchored late-time w_0 → DESI DR3 w0-wa measurement. GR's dark energy is the CONSEQUENCE, not the premise. The GT bosonic-ladder builder constructs the (15,0)/(0,15) sectors in their intrinsic 136-dim highest-weight space (NEVER the 3^15 dense intermediate that walled S104 at 366 TB); the sentinel (0.0 diff vs cache) certifies it — a feasibility route, not new physics. Artifacts: `s116_w9_gtbuilder_l15.py/.npz/.png`.

---

## Wave 9 Synthesis (team-lead)

**Wave 9 closed (TERMINAL wave): 2/2 gates (1 compute INFO + 1 workshop artifact-existence). Q36 ("does p+q=15 matter?") is resolved as an EXACT observable-DECOMPOSITION — the answer is observable-dependent.**

**Gate-by-gate.**
- **S116-W9-GTBUILDER-L15** (compute, **INFO** — pre-registered expected). `spread_CAC{13,14,15} = 0.0392902` (INFO band, NARROWING vs S105's 0.0443, decelerating decrement `|d|: 0.02083→0.01846`); the independent **GT-pure (15,0)/(0,15) reconstruction sentinel = 0.0** (reproduces the S106 cache exactly — the p+q=15 build is certified); the **bottom-K floor max|diff|(L≤14 vs L≤15) = 0.0e+00** (Friedrich-Bär saturated). `sign=PASS magnitude=INFO regime=VALID`. Both sides of the workshop empirically confirmed in one compute.
- **S116-W9-SATURATION-ADJUD** (workshop, artifact-existence — CONVERGED). The decisive structural find (baptista, verified from the S105 evaluator): the branch-(iv) moment is **EXACTLY** `ρ_B = mean_Z/λ_max − 1`, with the **numerator `mean_Z` FROZEN** (1.98767→1.98793) and the w0 shift **100.1% the `λ_max` denominator drift** (closed-form `|d| = μ·b/λ_max²` to 0.01%). So the observable-decomposition is **algebraically exact**: **bottom-K ⟷ mean_Z** (FROZEN, FB-saturated) **⊥ w0-shift ⟷ λ_max** (Weyl-edge, drifting, convergent at `|d|~1/λ_max²`). **Friedrich-Bär theorem SCOPE**: FB saturates the bottom-K + bulk-low-|λ| (the mean_Z numerator), but is **SILENT on the λ_max denominator** — citing FB to declare the w0 DR3 spread "saturated" is a scope error. **Q36 is observable-dependent**: p+q=15 is redundant for bottom-K (EXACT saturation) BUT a genuine deep-truncation datum for the w0_FW DR3 moment (shifts, marginal-INFO, narrowing; PASS reachable at window ~{17,18,19}/L≈19 by the 1/λ_max² law).

**Joint reading.** The compute and workshop agree exactly: bottom-K saturated (max|diff|=0) AND w0_FW DR3 shifts (spread 0.0393 INFO), unified by the exact `ρ_B = mean_Z/λ_max − 1` law. The Friedrich-Bär saturation theorem is now precisely scoped (mack landed the §VII.AJ.partition-stability scope note). New finding surfaced by the deep data: the anchor-fidelity asymptote `ρ_B→−1 ⇒ w0_cac→−1.341` (vs the canonical w0_FW=−0.918) — a SEPARATE Level-3 question (distinct from L_max-stability) → CF-S117.

**What strains.** The w0_FW DR3 deep-truncation spread is marginal (INFO, not PASS) — the prediction's L_max-envelope is narrowing but not yet PASS-converged at L=15 (the λ_max-Weyl drift is slow, `1/L²`). The anchor-fidelity gap (w0_cac→−1.341 asymptote) is a genuine open question for the DESI w0=−0.918 prediction.

> **[ANNOTATION — S116 W-5 baptista×volovik "w0-spectral-derivability" workshop, R3-FINAL, 2026-06-28]** The phrase "**the prediction's L_max-envelope**" above OVERCLAIMS: it reads the branch-iv spread as w0's own (Level-2 spectral-action) convergence envelope. The W-5 workshop converged (both agents, derivation-grade) that the spread is **value-NEUTRAL** — offset-invariant (`spread_CAC = ρ_a − ρ_b` exactly; W9 residual 1.11e-16), hence **bit-identical** anchored to −0.918, −0.842454, or −1.341 (registered `surrogate-spread-UNINFORMATIVE`, S101-W0-BRANCH-IV-EVALUATOR `cd0492d6`). A diagnostic blind to a 0.42-wide value gap is NOT "the prediction's envelope"; it certifies only that ρ_B(L) is a smooth sequence converging to **−1** (Weyl-forced; mean_Z FB-frozen ÷ λ_max-unbounded). The **anchor-fidelity gap is NOT a question for the w0 VALUE**: w0 = −0.918 is the canonical `w0_FW` (**Gate: None**; Volovik partition + effacement Γ_eff=0.99970), a Level-1 closed-form THERMODYNAMIC identity injected at the anchor by `offset = w0_FW − ρ_B(10)` (the moment cancels) — NOT spectral-action-derived. So `CF-S117-W0-ANCHOR-FIDELITY` is RE-SCOPED by the workshop from "anchor-fidelity resolver" to **presentation-normalization hygiene** (which off-anchor costume — running/fixed/no-edge — the injected value wears), NOT a derivation gate. The W9 verdict (INFO) and its numbers are UNTOUCHED; this annotation corrects only the spectral-support framing. See `sessions/session-116/workshops/s116-w0-spectral-derivability.md` §"Workshop Verdict" + §"What Changed".

### Effected In-Session (NON-MATH — executed at wave-synthesis)

The §VII.AJ.partition-stability surface is `mack-cosmic-bridge` SOLE-WRITER domain — the FB-scope note was ROUTED via housekeeping §A9 and mack DISPATCHED at §6. Capstone-hygiene 5-question gate (baptista, §A9): Q3=YES (Friedrich-Bär-theorem registry SCOPE change) → §A; capstone NO-OP (grep clean). All mack landings verified on disk:

- [x] **Friedrich-Bär SCOPE note on §VII.AJ.partition-stability** (mack) — FB saturates bottom-K + bulk-low-|λ|/mean_Z numerator, SILENT on the λ_max denominator (citing FB for the λ_max-tail w0 moment is a scope error); the exact `ρ_B=mean_Z/λ_max−1` decomposition cited; anchor S116-W9-GTBUILDER-L15 INFO. **Scope ADDED, status PERMANENT UNCHANGED** (SOURCE-DOUBLE-CITE-CO-PRIMARY attribution retained) — `atlas-07-permanent-results.md:675` + `permanent-results-registry.md:16288`.
- [x] **falsifier-inventory** — NO-OP (mack-verified: the 2 FB hits — n_PBH regime-validity, T_pivot-FB-saturation — are bottom/bulk saturations, NOT λ_max-tail DR3/w0 moments; nothing mis-scoped).
- [x] **capstone** — NO-OP (mack grep `w0.*saturat|partition-stability|Friedrich-Bär` = 0).
- [x] **baptista + spectral-geometer agent memory** — recorded in-workshop (the exact ρ_B=mean_Z/λ_max−1 decomposition + the FB-scope).
- [x] **housekeeping ledger** `§A9` (baptista spec) + `§A9-LANDED (mack)` (line 267, verified locus + the markdown-table pipe-escaping fix-in-session).

**Self-audit (orchestrator)**: WP Effected-In-Session unchecked = 0; sig_5 13/13 distinct session SHAs; the §VII surface landed by the sole writer (mack); plan-text-drift (canonical SHA) documented in the compute verdict value.

## Carry-Forward Computations

### CF-S117-BRANCH-IV-L16 — extend the branch-(iv) w0 spread to p+q=16
1. **What**: Extend the branch-(iv) w0 spread_CAC to the sliding window {14,15,16} by building the p+q=16 FB-bounded shell (17 sectors (0,16)…(16,0), currently analytic-tail-only in `fb_bounded_sectors`); test whether the `|d|~1/λ_max²` deceleration continues and the spread keeps narrowing toward the PASS band (predicted ~{17,18,19}/L≈19 by the exact law).
2. **Inputs**: `s106_w1_highl_cache_l1416.npz` (p+q≤15 + the 17 FB-bounded p+q=16 entries); `s105_branch_iv_direct_l1314.py` (GT builder + Zubarev evaluator); the GT-pure (16,0)/(0,16) builder + Casimir-projection for the 15 mixed p+q=16 sectors.
3. **Gate**: spread_CAC{14,15,16} vs the UNCHANGED W5-2 band (PASS≤0.025 | INFO | FAIL>0.050) + decrement-deceleration [SIGN].
4. **Effort**: medium-high (one feasibility-bounded shell build, ~1.5–2 h GPU). **Depends on**: the S106 cache; the S105 GT builder; this wave's exact-decomposition law.

### CF-S117-W0-ANCHOR-FIDELITY — the ρ_B→−1 asymptote vs w0_FW=−0.918
1. **What**: Investigate the anchor-fidelity gap surfaced by the deep data — the exact law gives `ρ_B→−1 as L→∞ ⇒ w0_cac→−1.341` (CAC offset_B=−0.342793), distinct from the canonical w0_FW=−0.918. Is the CAC anchor (offset DERIVED at L=10) the right L→∞ limit, or does the asymptote reveal an L-dependence in the anchor itself? A Level-3 anchor-fidelity question, NOT the L_max-stability the DR3 lineage tracks.
2. **Inputs**: `s116_w9_gtbuilder_l15.npz`; the CAC offset machinery (regulator-convention-lockdown.md); w0_FW canonical (S58 Volovik partition + effacement).
3. **Gate**: characterize the L→∞ asymptote of w0_cac under CAC; INFO-class (a structural characterization, not a PASS/FAIL).
4. **Effort**: low-medium. **Depends on**: this wave's ρ_B(L) lineage; CF-S117-BRANCH-IV-L16 (a deeper L extends the asymptote fit).

### CF-W9-1 — reconcile the proxy-design CF's two IDs for the `/rclab-plan` author [Q2-hygiene CF-naming reconciliation; NEW]

1. **What**: Reconcile the inconsistent CF naming — the W9 workshop names the proxy-design CF `CF-S117-BRANCH-IV-PROXY-DESIGN` (workshop body R2/EMERGENCE, `s116-w9-saturation-adjud.md:360`) vs `CF-S117-W0-ANCHOR-FIDELITY` (Wrap-Up + WP synthesis, above). Same compute, two IDs. Canonical ID = `CF-S117-W0-ANCHOR-FIDELITY` (the Wrap-Up/WP form); `CF-S117-BRANCH-IV-PROXY-DESIGN` is the workshop-body alias. A one-line reconciliation for the `/rclab-plan` author so the S117 plan does not double-list one compute.
2. **Inputs**: the W9 workshop body (`sessions/session-116/workshops/s116-w9-saturation-adjud.md:360`) + the Wrap-Up / WP synthesis; the `CF-S117-W0-ANCHOR-FIDELITY` block (above).
3. **Gate**: the `/rclab-plan` author treats the two IDs as ONE compute (canonical `CF-S117-W0-ANCHOR-FIDELITY`); no double-listing in the S117 plan.
4. **Effort**: ~0 compute (a one-line naming reconciliation for the planner). **Depends on**: the W9 workshop + the `CF-S117-W0-ANCHOR-FIDELITY` block.

## Constraint-Map Updates

| Date | Mechanism/gate | Prior state | New state | Reason |
|:-----|:---------------|:------------|:---------|:-------|
| 2026-06-28 | branch-(iv) w0_FW DR3 (S116-W9-GTBUILDER-L15) | spread_CAC{12,13,14}=0.0443 INFO (S105) | **spread_CAC{13,14,15}=0.0393 INFO — narrowing, decelerating; EXACT decomposition ρ_B=mean_Z/λ_max−1; GT-pure sentinel=0; bottom-K max|diff|=0** | INFO; the deep-truncation window slides up, narrows |
| 2026-06-28 | §VII.AJ.partition-stability Friedrich-Bär theorem (S116-W9-SATURATION-ADJUD) | bottom-K saturation PERMANENT (scope implicit) | **SCOPE made explicit — saturates bottom-K + mean_Z numerator, SILENT on the λ_max denominator; status PERMANENT UNCHANGED** | workshop CONVERGED; the exact ρ_B=mean_Z/λ_max−1 scopes the theorem |
| 2026-06-28 | Q36 (p+q=15 significance) | OPEN (binary "does it matter?") | **RESOLVED — observable-dependent: REDUNDANT for bottom-K (exact saturation) ⊥ a genuine datum for w0_FW DR3 (shifts, marginal-INFO, narrowing)** | the exact observable-decomposition |

## Files Produced

| Gate | Script | Data (.npz) | Plot (.png) | Deliverable md |
|:-----|:-------|:------------|:------------|:---------------|
| S116-W9-GTBUILDER-L15 | `s116_w9_gtbuilder_l15.py` | `…_gtbuilder_l15.npz` | `…_gtbuilder_l15.png` | — |
| S116-W9-SATURATION-ADJUD | — | — | — | `sessions/session-116/workshops/s116-w9-saturation-adjud.md` |

*(Compute under `computations/session-116/`. Verdict: `S116-W9-GTBUILDER-L15: INFO` (audit 94ed48e7…), dual-SHA-unique. The workshop closes by artifact-existence. mack landed the §VII.AJ.partition-stability FB-scope note. **TERMINAL wave of Session 116.**)*
