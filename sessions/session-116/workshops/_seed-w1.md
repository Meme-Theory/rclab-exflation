# Seed — Chunk w1 (Wave 1 — Q23 A_s normalization)

**Date**: 2026-06-28   **Investigator**: phonon-first-cosmologist   **Source**: S116 W1 (WP `session-116-w1-workingpaper.md` + `s116-w1-htilde-recon.md` workshop + `session-116-plan-w1.md`)
**Wave summary**: 1 workshop (`S116-W1-HTILDE-RECON`, closed by artifact-existence) + 3 compute (`CFB1` PASS, `CF2` FAIL, `CF3` INFO; all dual-SHA-unique). **Structural reading: the wave closed cleanly.** The 4-member, 1.331-OOM "route family" reduced to (Layer A) the airtight CC3-conjugate pair `2.38` (H̃-space) ↔ `4.76` (A_s-space) — figure-multiplicity CLOSED — plus (Layer B) ONE open scalar `𝒩` on an orthogonal axis. Both workshop agents converged COMPLETELY on structure and pre-committed the SAME gate (`CF-S117`) with OPPOSITE predictions (TD: `𝒩` stable → convention-blocked; mack: `𝒩` swings → physics-blocked). `CFB1` pinned the box-delta squeeze floor (+0.864 OOM, POINT, L_max-stable); `CF2` closed the exit greybody as irreducibly fitted (structural-closure); `CF3` confirmed S115 PLURALISM (routes do not collapse). Genuine future work = `CF-S117-T-FOLD-EXIT-NORMALIZATION` + `CF-S117-ROUTE-B-PW-SOCC` (both already minted; route to `/rclab-plan`, NOT this schedule). The two residuals below are the **soundness of the pre-committed discriminator** and the **falsifiable content of the magnitude leg post-CF2** — neither adjudicated in-session.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

### S1-1 — Is CF-S117's `𝒩`-spread discriminator SOUND? (does the matching-surface scan test inter-grid SELECTION, or only intra-grid robustness Parker-invariance trivially guarantees?)
**What this answers**: The workshop reduced the convention-vs-physics fork to ONE pre-registered discriminator — `CF-S117`: `𝒩`-spread ≤ 0.1 OOM over a ≥5-point post-fold matching-surface scan → convention-blocked; > 0.1 OOM → physics-blocked (Structural Verdict (II), workshop L490–493; CF-S117 spec L565–573). But the *actual* fork (Structural Verdict (II) + Sub-(a) L499) is **which GRID** carries the `deg=+2` transport to the CMB pivot: the `ξ_KZ` produced-relic grid (→ `+0.864`, box-delta) vs the `H̃` vacuum-envelope grid (→ `+0.196`, `INV12-W3-5` PASS). The discriminator as written is an **INTRA-grid robustness scan** (vary matching surfaces within a grid). By TD's own §2d (L431) `|β_k|²` is a Parker adiabatic invariant in the smooth post-fold region, so `𝒩` is stable across matching surfaces *within either grid by construction* — Sage-confirmed: the `ξ_KZ` grid returns `𝒩≈1` (+0.864) AND the `H̃` grid returns `𝒩=0.2147` (+0.196), **both PASSING** the ≤0.1-OOM intra-grid test. Worse, CF-S117's "grid discipline" (spec L567) rejects only the FOLD-GEOMETRY grid (the `OOM_naive_extrap=9.37` artifact) — NOT the `H̃` vacuum-envelope grid, which is a distinct, gate-passed reading. So a CF-S117 run disciplined to the `ξ_KZ` grid would return `+0.864`-stable **by construction** (baking in TD's grid choice) and would NOT discriminate against the legitimate `H̃`/`+0.196` reading — a circular confirmation of TD's pre-committed prediction, not an adjudication of the fork. The review audits whether the matching-surface `𝒩`-spread test discriminates the `ξ_KZ`-vs-`H̃` grid SELECTION (the actual fork) and, if not, specifies the missing grid-selection sub-discriminator (a substrate-IS principle that picks one grid) the gate needs BEFORE it is run.
**Why solo, not workshop**: the two competing readings (TD §2d: Parker-invariance ⇒ deterministic ⇒ stable ⇒ convention-blocked; adversary: intra-grid stability is guaranteed and does not test inter-grid selection) are already fully on the table in the workshop transcript. What is needed is an independent derivation/verdict on the gate's discriminating power, not fresh adversarial generation.
**Agents**: `volovik-superfluid-universe-theorist` (default 1 — transit/Bogoliubov/emergent-metric-fluent AND independent of the W1 TD/mack pre-commitments; alternative `phonon-first-cosmologist` for the cross-domain pre-registration-soundness angle). NOT `transit-dynamics-theorist` (pre-committed the convention-blocked prediction).
**Source anchors**: workshop `s116-w1-htilde-recon.md` Structural Verdict (II) L490–493 + Sub-(a) L499 + §2d L431 (Parker invariance) + §2c L412–429 (TWO-SPECTRA-TWO-ROLES grid distinction); CF-S117 spec L565–573; Remaining Open Questions #1 L517; `S116-W1-AS-CF3` INFO (collapse_dist=0.6682, within_sudden_N_gap=0.668 = the `𝒩`-gap).
**Output**: a verdict on whether the pre-registered `𝒩`-spread ≤ 0.1-OOM discriminator tests the `ξ_KZ`-vs-`H̃` grid SELECTION; if not, a pre-registration sharpening (the grid-selection sub-discriminator) for `/rclab-plan` to fold into CF-S117 at plan-freeze. (If the consolidator judges this absorbable into CF-S117's PRDR at plan-time, that is acceptable alternative routing — but the Parker-invariance-circularity insight must travel with the gate either way.)

### S1-2 — What is the FALSIFIABLE content of the A_s MAGNITUDE leg, now that CF2 closed the exit greybody as irreducibly fitted?
**What this answers**: `CF2` (`S116-W1-AS-CF2` FAIL) established the exit greybody is "irreducibly fitted … the A_s upper-edge is NOT substrate-derivable" (WP L121/L154; the fitted `Γ=0.512` lives only at the A2-knob band-midpoint 0.9418). `CFB1` pinned the floor at `+0.864` OOM ≈ 451σ above Planck (Sage 451.3σ; workshop 451.5σ). The workshop flagged this as STRAIN (L560: "the A_s upper-edge is not substrate-derivable on the filter axis EITHER … independently strains any claim the A_s magnitude is currently substrate-single-valued") but did NOT verdict it. The question: given (a) the floor is ≈451σ above Planck, (b) the only known bring-down mechanism (the greybody `Γ`) is now a fitted O(1) free parameter (CF2 FAIL), and (c) the floor itself may be a ≈410σ fork (CF-S117 pending) — what does the A_s magnitude leg FALSIFIABLY predict? Is it only the FLOOR inequality (`A_s > A_s^BD`, permanent/3-axis) + tilt-flatness (`α_s ≈ 0`, NEW-1/OQ4 L523), with the absolute normalization carrying one fitted `Γ` and therefore NOT a stake-claim? Or does CF-S117 + a future substrate-derived `Γ` leave a genuine absolute-magnitude prediction? This scopes the genuine testable content for the falsifier inventory and for any publication claim about A_s.
**Why solo, not workshop**: this is an observational-falsifiability scoping (sagan's domain) of an already-landed verdict set (CF2 FAIL + CFB1 floor). mack (in-workshop) already conceded the FLOOR is the genuine prediction and the magnitude is open (L552, R1 honest-scope), so there is no live two-sided physics fork — what is needed is a clean independent scoping of falsifiable content, which mack then lands as an inventory sub-row.
**Agents**: `sagan-empiricist` (default 1).
**Source anchors**: `S116-W1-AS-CF2` FAIL (value-string `structural_closure=greybody_irreducibly_fitted`, WP L121/L154); `S116-W1-AS-CFB1` PASS (floor +0.864, ≈451σ); workshop What Holds L552 + What Breaks/Strains L560; OQ2 L519 (filter axis) + OQ4 L523 (α_s≈0 corollary); CF23 FLOOR/FILTER split; `falsifier-master-inventory.md` Row #12.
**Output**: a structural scoping of the A_s magnitude leg's falsifiable content (FLOOR inequality + tilt-flatness as the genuine predictions vs the fitted-`Γ` absolute normalization), with a sub-question on whether "NOT substrate-derivable" generalizes beyond CF2's near-horizon-barrier construction (CF2 scanned barrier scales {ω_q, relic_rms, γ_clock, 2Δ_BCS, κ_exit} only; an alternative substrate-IS greybody construction — spectral-moment-ratio / Connes-distance transmission — is untested). Feeds a mack falsifier-inventory scoping sub-row (Q2 carry-forward).

## Slot 2 candidates — workshops (`/rclab-workshop`)

**None.** The wave's primary tension (convention-blocked vs physics-blocked) was run to convergence in the in-session workshop (3 rounds, complete structural convergence, both agents pre-committed the SAME gate `CF-S117` with opposite predictions). The residuals above (S1-1 gate-soundness, S1-2 falsifiable-content) are independent-reading audits, not fresh adversarial forks — the competing arguments are already in the workshop transcript. No candidate requires live cross-rebuttal the existing transcript does not supply.

## Cross-wave flags (surface for consolidator; NOT resolved here)
- **`n_s` provenance shared W1-CF3 ↔ W3.** W1-`CF3` (WP L219–227; verdict row 23) treats the `n_s` scheme-split (`0.9590` sqrt-cutoff / `0.9561` framework, spread 0.69σ, REGULATOR-CONSISTENT) as cutoff-scheme images of the same tilt `1−2ε_H` carried by `deg(T_BZ→pivot)=+2`. The W3 DM-mass reconciliation (housekeeping §A3) RE-TYPES the 170× factor as a cross-pillar ratio of a "Pillar-II `n_s` SA-Goldstone correlator-crossover `K*`" over a Pillar-V Leggett mass. Both rest on the `n_s` observable / the `deg=+2` transport. Consolidator: confirm W1-CF3's `n_s` provenance (S85 `0.9561` / S103 `0.9590`) and W3's "Pillar-II `n_s` `K*`" cite the same `n_s` object + the same `deg=+2` convention; a mismatch would couple the A_s and DM-mass ledgers.
- **`deg(T_BZ→pivot)=+2` load-bearing in W1.** The +2 transport degree is the spine of NEW-M1 (degree/normalization orthogonality, workshop L299–314 / L349) and of CF-S117 (`ζ_k̂(exit)=𝒩·(k̂/aH)^{+2}·|β|`). A_s is a `d_A=0` (dimensionless) observable, so +2 is even-morphism-sector-consistent (no parity-selection violation per the S110 W4 mass-dimension/parity rule). If any other S116 wave re-pins/re-derives `deg_T_BZ_pivot`, W1's NEW-M1 lemma and CF-S117's transport ansatz inherit it — confirm consistent citation across waves.

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)
- **[Q-other / already-minted]** `CF-S117-T-FOLD-EXIT-NORMALIZATION` — the fork discriminator (Radau MS propagation + matching-surface `𝒩`-spread scan). Already minted in the workshop `## Carry-Forward Computations` L565–573; routes to `/rclab-plan`. (S1-1 above audits its discriminator's SOUNDNESS before it runs — distinct from relisting it.)
- **[Q-other / already-minted]** `CF-S117-ROUTE-B-PW-SOCC` — the `S_occ`-corrected Route-B-PW recompute (resolves OQ3 L521; tests whether the corrected `3.15` reduces to the box-delta/CC3 image or lands a third value). Already minted L575–581; routes to `/rclab-plan`.
- **[Q2-falsifier-scoping]** `α_s(primordial) ≈ 0` corollary falsifier (NEW-1/OQ4 L523): any surviving A_s regime reading MUST be tilt-flat (k-flat produced occupation ⇒ magnitude-only). Flagged "registrable-now" in the workshop, but the `Row #12.audit-S116-W1-HTILDE-RECON` landing recorded the figure reconciliation + CF-S117-conditional magnitude, NOT this corollary. Route to mack (falsifier-inventory sole-writer) to land as a tilt constraint on CF-S117's output (independent of the `𝒩` it returns).
- **[Q2-reconciliation]** `CF3` "two-cluster axis" vs workshop "demoted adiabatic cluster." `CF3` (verdict rows 19–20) counts Parker (+1.455) and Connes (+1.527) as a live "adiabatic cluster" and reports `cross_cluster_gap=0.591` as a live obstruction → "S115 two-cluster axis confirmed." The workshop (Wrap-Up L534, R2/R3) DEMOTED that cluster as off-regime-at-the-fold, reducing the live family to the 2-member {+0.196, +0.864} `𝒩`-gap. Downstream consumers must NOT cite CF3's "S115 two-cluster axis confirmed" as established live plurality — the adiabatic half is workshop-demoted (and its possible relocation to the post-fold adiabatic transfer leg, mack R2 NEW-M2 reservation, is exactly what a CF-S117 `𝒩`-swing would confirm). Route to mack/consolidator to reconcile the CF3 verdict-row language with the workshop verdict (the live A_s plurality is the 2-member `𝒩`-gap, not a 4-member/two-cluster axis).

## Wave-by-wave digest (consolidator background)
- **S116-W1-HTILDE-RECON** (workshop, artifact-existence). 3-round adversarial adjudication (TD convention-blocked × mack physics-blocked). Converged COMPLETELY: CC3 `2.38↔4.76` airtight (figure-multiplicity CLOSED); the `(n_s,A_s)` joint-consistency objection DEAD (Mode-Independent Occupation, NEW-1); NEW-M1 degree/normalization orthogonality lemma (`deg=+2` silent on `𝒩`); `3.15`-raw RETIRED; fork → CONDITIONAL-on-CF-S117 (convention-blocked Layer-A settled + Layer-B magnitude CF-S117-conditional). Both agents pre-committed CF-S117 with opposite predictions. Pinned: `2.38` (H̃-space) unconditional; `+0.864` (A_s-space) PINNED-conditional. STRAIN noted (not verdicted): CF2-fitted-filter + ≈451σ floor strain any single-valued-magnitude claim.
- **S116-W1-AS-CFB1** PASS (sign/mag/regime = PASS/PASS/VALID). Box-delta squeeze A_s = 1.5367e-8, OOM=+0.864 IN S115 axis, epistemic type POINT (rel_dev_Lmax=5.43e-05, Friedrich-Bär L12-saturated). Round-trips `A_s_FW` to rel-dev 0.00. Floor PERMANENT-3axis; magnitude SCHEME-DEPENDENT. (audit `f44a7b42…`)
- **S116-W1-AS-CF2** FAIL (N/A / FAIL / VALID). EXACT finite-rate Floquet/Numerov greybody ∫Γ reaches 0.512 at NO substrate barrier scale (best 0.278 ≫ 0.10) in a regime VALID by ODE-convergence (f_used=1.00; eps_WKB=7.34 DECOUPLED — S110's breakdown was a WKB-method artifact). Structural-closure: greybody irreducibly fitted, A_s upper-edge NOT substrate-derivable, magnitude PLURALISM. Finite-rate correction ≤7e-4 (Kapitza averaging); Floquet stable (max|Tr M|=1.999994<2). (audit `c7bb96b6…`)
- **S116-W1-AS-CF3** INFO (PASS / INFO / VALID). Regime-tagged routes do NOT collapse onto the workshop figure (collapse_dist=0.6682 ≫ 0.1); reproduce the S115 sudden↔adiabatic axis (S115 PLURALISM CONFIRMED). Product squeeze×(fitted)filter = 7.87e-9, OOM=+0.574 (still overproduces; one fitted `Γ` cannot map N routes to one Planck output). `n_s` scheme-split regulator-consistent (0.69σ spread, both RED, 1.40σ/2.10σ to Planck). Correctly rejected per-route MIN=0.000 as load-and-compare-to-self. (audit `c34cadf3…`)
