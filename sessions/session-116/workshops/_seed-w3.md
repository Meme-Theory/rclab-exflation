# Seed — Chunk w3 (Wave 3 — Q3 Goldstone mass from disorder)

**Date**: 2026-06-28   **Investigator**: phonon-first-cosmologist   **Source**: S116 W3 (WP + s116-w3-disorder-closure workshop + plan-w3)
**Wave summary**: 2/2 closed — `S116-W3-GOLDSTONE-M2` **FAIL** (composite `sign=PASS ∧ magnitude=FAIL ∧ regime=VALID`, Branch-B ceiling-pin, `frac170 = 0.0118` Cheeger / `0.0245` Fiedler λ_1, 84.6× short) + `S116-W3-DISORDER-CLOSURE` workshop (artifact-existence, Structural Verdict **CLOSED-BY-PROTECTION-MAGNITUDE-EXCLUSION**). The 3-round exchange converged cleanly: I overturned my own R1 Branch A (inter-band disorder is Schrieffer–Wolff `1/E_n`-SUPPRESSED, operator-right/mechanism-backwards), landau WITHDREW Reading B, the DM mass is graph-ANCHORED at the clean Leggett scale `5.5571 M_KK`, and the 170× is RE-TYPED-AND-ROUTED to `CF-S117-FREESTREAM-AT-ANCHOR`. The disorder axis is settled. ONE residual sub-fork survives: the workshop's headline theorem (protection-magnitude exclusion) and its surviving Tier-2 anchor are mutually inconsistent at the theorem's binding inequality — converged-past, not resolved.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

*(None. The one genuine residual tension needs an adversarial cross-rebuttal between two protection-mechanism owners, not a single independent reading — it is Slot 2. See S2-1.)*

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

### S2-1 — Is the 11.97·Δ_BCS Leggett DM anchor below-edge-protected, or above-edge integrability-protected? — the protection-magnitude-exclusion theorem's own binding inequality excludes its surviving Tier-2 anchor

**Tension**: The W3 Structural Verdict's NEW durable output is a THEOREM — "CLOSED-BY-PROTECTION-MAGNITUDE-EXCLUSION: a single-gap condensate cannot carry a *protected* phase mode above `O(10·Δ_BCS)`" — whose load-bearing inequality is eq (15c): a mode is below-edge-protected ⟺ `ω_G = m/√ρ_s < 2Δ_BCS` ⟺ `m < 2Δ_BCS·√ρ_s = 2.62 M_KK = 5.64·Δ_BCS`. But the workshop's OWN surviving Tier-2 anchor — the registered DM mass `m_LeggettDM = 11.97·Δ_BCS = 5.5571 M_KK` (canonical `Mass_LeggettDM_over_Delta_BCS = 11.97`, S70 LEGGETT-MOMENT-70) — gives `ω_G = 5.5571/√7.962 = 1.97 M_KK` ⇒ `x_G = ω_G/(2Δ_BCS) = 2.12`, i.e. **2.12× ABOVE the pair-breaking edge** under the very `(ρ_s = 7.962, edge = 2Δ_BCS)` convention the verdict used to FAIL the disorder ceiling. Meanwhile the framework's `Below pair-breaking at ALL tau` theorem (knowledge MCP `proven_1792`, S48) is registered for the LIGHT dipolar mode `ω_L1 = 0.070 M_KK` (`x ≈ 0.027`) — a DIFFERENT object, 79× lighter. The workshop's "What Holds" (line 443) attributes below-edge protection to "the DM mode" and asserts "the Lichnerowicz floor `|λ| ≥ √3` keeps every nonzero fiber mode above the edge, the protected DM mode below it" — but the DM anchor IS an off-(0,0) fiber mode (the verdict's own Tier-2 framing), so by `|λ| ≥ √3 = 1.73 > 2Δ_BCS = 0.93` it is ABOVE the edge, not below. landau's R3 (line 343) noticed the anchor "rides just above (15d)" [the `10.17·Δ_BCS` ladder top] but waved it as "still `O(10·Δ_BCS)`", silently dropping that `11.97·Δ_BCS` is also above the (15c) below-edge cap of `5.64·Δ_BCS`. The canonical constant's own note cites survival as CPT-neutral / non-annihilating / `Γ_grav < H_0` — NOT below-edge. So the theorem's binding inequality and its surviving anchor disagree by 2.12×, and the framework's long-standing "Leggett DM is below-edge non-annihilating" claim may be mis-attributed from the 0.070 dipolar mode to the 5.5571 DM anchor.

**Why workshop, not solo**: the resolution is not a single number — it forks two protection MECHANISMS owned by two different domains, each first-principles-backed: (Reading A) the heavy anchor is above-edge and survives by integrability/GGE/CPT + `Γ_grav < H_0` (the canonical note's own mechanism), so eq (15c)'s below-edge criterion never applied to it and the "one wall, four projections" payload conflates two protections; (Reading B) the inter-band Leggett mode carries its OWN restoring stiffness `ρ_s^⊥ ∝ J_⊥` (≫ the intra-band `ρ_s = 7.962`) and/or its own continuum edge, so `x = 2.12` computed with the intra-band `ρ_s` is a category error and the mode IS below ITS edge. The two cannot both be right; the cross-rebuttal (which `(ρ_s, edge)` convention is physically correct for an inter-band mode, and which survival argument the framework's DM actually rests on) is the workshop's substance. This is the rule's "registered theorem needs adversarial review of its sufficiency conditions" signal applied to a theorem minted THIS wave.

**Agents (EXACTLY 2)**: landau-condensed-matter-theorist, volovik-superfluid-universe-theorist
  *(landau = multiband-Leggett stiffness/edge pole: the inter-band relative-phase mode has its own `ρ_s^⊥` and continuum edge, below-edge holds under the correct convention. volovik = superfluid topological/integrability DM-protection pole: the heavy anchor is above the `2Δ_BCS` edge and survives by GGE-integrability/CPT non-annihilation + `Γ_grav < H_0`, NOT below-edge kinematics; the below-edge property belongs to the 0.070 dipolar mode. Alternative B-side if the consolidator wants the cross-pillar vantage: phonon-first-cosmologist — but I authored the W3 panel, so volovik is the cleaner disjoint pole.)*

**Adjudication question**:
  (a) Does `m_LeggettDM = 11.97·Δ_BCS = 5.5571 M_KK` satisfy the workshop's own below-edge criterion eq (15c) `m < 2Δ_BCS·√ρ_s = 5.64·Δ_BCS`? With the intra-band `(ρ_s=7.962, edge=2Δ_BCS)` it does NOT (`x_G = 2.12`). Is that the correct `(stiffness, edge)` convention for an INTER-band mode, or does the inter-band sector carry a distinct `ρ_s^⊥` / edge?
  (b) Is the S48 `Below pair-breaking at ALL tau` theorem (`proven_1792`, `ω_L1 = 0.070 M_KK`) being mis-inherited by the 79×-heavier S70 DM-mass anchor (`11.97·Δ_BCS`)? Does below-edge protection transfer from the dipolar mode to the DM anchor, or are they distinct objects with distinct survival arguments?
  (c) Which protection mechanism does the framework's DM-survival actually rest on for the 5.5571 M_KK anchor — kinematic below-edge under an inter-band `ρ_s^⊥` (Reading B), or integrability/GGE/CPT + `Γ_grav < H_0` (Reading A, the canonical note)? These are DIFFERENT falsifiable claims (a below-edge mode is kinematically stable; an above-edge integrability-protected mode is stable only if integrability holds — a more fragile, more testable claim).
  (d) Does the "protection-magnitude exclusion" theorem survive — is "single-gap condensate ⇒ protected mode `≤ O(10·Δ_BCS)`" airtight when its binding inequality (15c) caps protected modes at `5.64·Δ_BCS` yet the surviving anchor is `11.97·Δ_BCS`? Re-scope or re-derive (15c)→(15d) for the inter-band mode, and reconcile atlas-07's `[NEW S48] below pair-breaking` inheritance.

**Rounds**: 2 default (sharp tension, bounded resolution: a `(stiffness, edge)` convention pin + a protection-mechanism reading). Escalate to 3 only if R1/R2 do not converge on which survival mechanism is load-bearing.

**Output**: a structural verdict pinning (i) the correct `(stiffness, edge)` convention for the inter-band Leggett DM mode (intra-band `ρ_s` vs a distinct `ρ_s^⊥`); (ii) which protection mechanism the framework's DM-survival rests on (below-edge vs integrability/CPT/`Γ_grav<H_0`); (iii) a re-scoped statement of the protection-magnitude-exclusion theorem with the inter-band projection's stiffness/edge made explicit (and a designated-writer note to atlas-07 if the `[NEW S48] below pair-breaking` row is mis-inheriting the 0.070-mode property onto the 5.5571 anchor). May pre-register a forward compute (inter-band `ρ_s^⊥` / continuum-edge extraction → `x_inter-band`) as its discriminating gate.

## Cross-wave flags (surface for consolidator; NOT resolved here)
- **S2-1 is logically UPSTREAM of the already-minted `CF-S117-FREESTREAM-AT-ANCHOR`** (same wave, W3 `## Carry-Forward Computations`). FREESTREAM assumes the `5.5571 M_KK` anchor is the stable cold DM and tests its free-streaming kinematics. If S2-1 resolves to Reading A (anchor is above-edge, integrability-protected, NOT below-edge), the anchor's STABILITY is a separate integrability/`Γ_grav<H_0` question FREESTREAM does not address — FREESTREAM tests free-streaming length, not stability. The consolidator should order S2-1 before / feeding CF-S117-FREESTREAM, or note the dependency in the S117 plan.
- **DM abundance + Q keying cross-reference.** `Ω_DM h² = 0.1200` (S70, atlas-04 C9, "from the Leggett inter-band coherence mode") and `Leggett Q = 670,000 | All pair-breaking channels forbidden` (S50) both reference "Leggett-channel" — the knowledge MCP shows the `Q=670,000`/`below pair-breaking` properties were established at S48/S50 for the `ω_L1 = 0.070` dipolar mode, while the MASS anchor (`11.97·Δ_BCS`) is S70. S2-1's adjudication of which object the below-edge/`Q` properties attach to should cross-check whether the abundance and `Q` claims are keyed to the dipolar mode or the DM anchor (a potential mis-keying the consolidator should route INTO the EVOI table).
- **EVOI routing**: the consistency of the DM-anchor protection across the `0.070`-mode vs `5.5571`-anchor is a NEW high-leverage open item likely ABSENT from `sessions/evoi-framework.md §1–§4`. Flag for the consolidator to route it into the EVOI table (it gates the credibility of the framework's headline "non-annihilating Leggett DM" claim).

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)
- **[Q-other / already-minted]** `CF-S117-FREESTREAM-AT-ANCHOR` — the kinematic discharge of the 170× re-typing (PRIMARY, EVOI-carrying); compute `λ_fs(m_Leggett, v_rms^GGE)` with `v_rms` as the explicit 2nd moment of the frozen Bogoliubov occupation, `[SIGN]` on `(λ_fs − λ_threshold)`. Lives in `session-116-w3-workingpaper.md §"Carry-Forward Computations"`; propagates via `/rclab-plan`. NOT a workshop — both agents pre-registered the SAME gate with the same prediction; the re-typing routing is SETTLED (zero evidential weight until the gate fires, jointly endorsed).
- **[Q-other / already-minted]** `CF-S117-LEGGETT-COLLECTIVE-CEILING` — diagonalize the full inter-band pair-transfer across all `(p,q)` at `L_max=10`; confirm the heaviest PROTECTED collective Leggett mode SATURATES at `frac170 ≈ 0.07` (COMPANION, low-EVOI). Settled compute carry-forward; both agents agree it is high-confidence PASS. NOTE: this compute is the natural place to ALSO extract the inter-band `ρ_s^⊥` / continuum-edge that S2-1 needs — the consolidator may merge S2-1's discriminating gate into this CF's scope.
- **[Q2-hygiene]** `rho_s` (= `rho_s_C2 = 7.962`) is consumed in the `[SIGN]` gate `S116-W3-GOLDSTONE-M2` (the `ω_G = m_G/√ρ_s` edge ratio) but is NOT in `canonical_constants.py` (`get_constant('rho_s')` / `get_constant('rho_s_C2')` → not found; the compute's own MCP Pre-Compute Audit sourced it from the SHA-pinned `s48_goldstone_mass.npz`). Promote `rho_s_C2 = 7.962` to `canonical_constants.py` with provenance (S48 MASS-48 / `s48_goldstone_mass.npz`) — a provenance-backfill hygiene item, routes to housekeeping §B / `canonical_constants.py`, not the schedule. (Surfaced first time here — an upstream wave-synthesis miss per `Investigating-Workshops.md §"Q2 first-time surface"`; one-sentence process note for the consolidator.)
- **[Q-other / conditional]** OQ-5 (W3 Structural Verdict line 416): "Is the cross-pillar `170×` ratio itself the right structure-formation observable — does the Pillar-II `n_s` SA-Goldstone `K*` crossover have any DM-sector consequence, or do the two pillars decouple at the structure-formation scale?" CONDITIONAL on `CF-S117-FREESTREAM` PASSing (cold relic, no enhancement needed). Premature to workshop now (no competing claims on the table until FREESTREAM lands); carry-forward gated on CF-S117-FREESTREAM, route to `/rclab-plan` for S117+.

## Wave-by-wave digest (consolidator background)

- **`S116-W3-GOLDSTONE-M2`** — FAIL (`sign=PASS magnitude=FAIL regime=VALID`; audit `2959c503…`). Branch-B ceiling-pin: surviving Cheeger/connectivity mass `m_G = J_C2 = 0.933 M_KK` (= inv5 construction-E ceiling), `frac170 = 0.0118` (Cheeger `m_G ≥ h/2`) / `0.0245` (Fiedler λ_1), `ratio_Ch_IM = 293×` the Imry-Ma baseline (SIGN direction correct), `x_G = 0.356 < 1` (below edge — for the CEILING mode `0.933`, NOT the DM anchor), `target/ceiling = 84.6×`. The intra-(0,0) disorder route is structurally closed. Standout: the FAIL is the substitution-chain-PREDICTED Branch-B outcome — an informative wall, the disorder corridor's intra-band ceiling.
- **`S116-W3-DISORDER-CLOSURE`** — workshop (artifact-existence, NO verdict line). Structural Verdict **CLOSED-BY-PROTECTION-MAGNITUDE-EXCLUSION** at every `D_K` projection. Three-tier ladder: Tier-1a (intra-(0,0) disorder, `≤ 0.0118`, compute-confirmed FAIL); Tier-1b (inter-band-via-disorder, `≈ 5×10⁻⁴` analytic, Schrieffer–Wolff `1/E_n`-suppressed BELOW 1a — this kills my R1 Branch A); Tier-2 (clean inter-band Leggett `J_⊥`, `frac170 = 0.0704`, 14.2× short, `√N`-saturated, graph-ANCHORED `5.5571 M_KK` C11-conditional — NOT a disorder mechanism, NOT Reading-B); Tier-3 (170× = re-typed cross-pillar ratio, `x_target = 30.12` unprotectable). Standouts: (1) phonon-first overturned own R1 Branch A (operator-right/mechanism-backwards); (2) landau WITHDREW Reading B (DM mass in-graph, graph-ANCHORED); (3) the 170× is ASSERTED-AND-ROUTED to `CF-S117-FREESTREAM-AT-ANCHOR`, explicitly NOT asserted-closed (landau's epistemic guardrail, jointly endorsed: zero evidential weight until the gate fires). UNEXAMINED SEAM (→ S2-1): the surviving Tier-2 anchor (`11.97·Δ_BCS`) violates the theorem's own below-edge inequality (15c) (`< 5.64·Δ_BCS`) by 2.12×, and the "What Holds" below-edge claim conflates the heavy DM anchor with the S48 `0.070 M_KK` dipolar mode.
