# Session 110 Workshop: baptista × van-den-dungen — WS-C2COSET

**Date**: 2026-06-20
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: baptista-spacetime-analyst (Reading A — C²-coset carries inter-generation anisotropy, WILL lift), van-den-dungen-bridge-theorist (Reading B — C²-coset is another multiplicity-scalar τ-modulus, FAILs like su(2))
**Source Documents**:
- sessions/investigation/_promotion-triage.md
- sessions/investigation/_cross-investigation-synthesis.md
**Adjudication question**: Is the rank-1 Yukawa wall genuinely off-Jensen for ALL internal transverse moduli, or only su(2)? Reading A: C²-coset (Baptista/O'Neill submersion geometry) lifts the rank. Reading B: the W2 homogeneity theorem says a single scalar deformation cannot generate inter-generation rank → FAILs like su(2). connes-ncg-theorist is the named NCG cross-check on whether the homogeneity theorem extends to the C²-coset. DUAL-PRIOR / ROUTE-ONCE: also carried as compute S110-CF1-YUK-C2COSET; state which route was taken. Full adjudication context supplied to each agent at dispatch.

---

## Round 1 — baptista: Opening Analysis

### BAP1: Reading-A steelman (C²-coset anisotropy lifts the rank; O'Neill submersion geometry)

**Substrate framing (binding).** The Yukawa couplings are spectral moments of `D_K` on the generation multiplet: `Y_ij = ⟨ψ_i | (1j·D_K) | ψ_j⟩` evaluated on the same-sign degenerate C²-coset multiplet inside the (1,0) fundamental (INV2-W1-1 fixed the correct observable — project the FULL Hermitian `H(δ)=1j·D_K(δ)` onto a fixed-δ=0 same-sign degenerate eigenbasis `V_g`; the resulting d×d block IS the generation Yukawa). The C²-coset is NOT an "external moduli container" — it is an **internal transverse direction of the Jensen-deformed SU(3) fiber**, specifically the off-diagonal sector of the fiber's own tangent algebra. The adjudication is: *what is the rank of the Yukawa spectral functional under T4 (C²-coset) deformation of the fiber metric?*

**The governing structure (Baptista, Paper 15 eq 3.58).** The isotropy decomposition is exact and is the spine of the entire argument:
```
su(3) = u(1) ⊕ su(2) ⊕ C²          (Baptista 15, eq 3.58)
        (idx 7)  (idx 0,1,2) (idx 3,4,5,6)
```
Under the U(2) = U(1)×SU(2) stabilizer, these three summands carry **structurally distinct** Ad-representations:
- **u(1)** (idx 7): the 1 (trivial) — a pure breathing/Cartan direction.
- **su(2)** (idx 0,1,2): the 3 (adjoint of SU(2)) — the *stabilizer's own* generators.
- **C²** (idx 3,4,5,6): the **2 ⊕ 2̄** (the fundamental + antifundamental of SU(2), with opposite U(1) charge) — the genuinely OFF-DIAGONAL, *horizontal* sector of the submersion `SU(3) → SU(3)/U(2) = CP²`.

This decomposition is the crux: **su(2) and C² are NOT the same kind of modulus.** My moduli parameterization (paper-index memory, confirmed against Baptista 15 §3.6) names them as two DISTINCT Milnor directions: **T3 = su(2) anisotropy** (breaks Ad(SU(2)) *on* the stabilizer su(2)) vs **T4 = C² anisotropy** (breaks Ad(SU(2)) *on* the off-diagonal C²). INV2-W1-1 tested ONLY T3 — the `deformed_su2_split_metric` split `L2·I_3 → diag(L2 e^{+2δ}, L2 e^{-δ}, L2 e^{-δ})` acts on the su(2)-block (idx 0,1,2), the stabilizer's adjoint. **The su(2)-block is a multiplicity-scalar for a structural reason that is SPECIFIC to it, and does NOT transport to C².**

**The first-principles lift mechanism (Reading A).** Here is why T4 is privileged, in three steps:

1. **The C²-coset IS the generation-carrying space.** By Baptista Paper 18 App E (the Z₃×Z₃ → three generations construction), the three generations arise from the fiber's representation content along the off-diagonal embedding — the C² coset is precisely where the generation index is realized geometrically (the (p,q) Peter-Weyl content that distinguishes generations lives in the off-diagonal raising/lowering structure, NOT in the Cartan or the SU(2) adjoint). A deformation that anisotropizes su(2) (T3) leaves the C² generation-multiplet rigidly degenerate (INV2-W1-1's exact finding: rigid shift 0.84086 → 0.85439, fan-out S(δ) ~ 1e-15) because it deforms a sector that the generation multiplet does not occupy. A deformation that anisotropizes C² itself (T4) acts **directly on the multiplet that carries the generation index** — it can split it at first order.

2. **O'Neill submersion geometry makes C² the horizontal sector where the A-tensor lives.** For the Riemannian submersion `π: SU(3) → CP²` with fiber U(2), the vertical distribution is `𝒱 = u(1) ⊕ su(2)` (the stabilizer) and the horizontal distribution is `ℋ = C²` (the coset). The O'Neill integrability tensor `A_X Y = ½ 𝒱[X,Y]` (for X,Y horizontal) is NON-zero precisely on `ℋ = C²`: `[C², C²] ⊂ u(1) ⊕ su(2)` by the su(3) bracket (the commutator of two off-diagonal generators is diagonal). The A-tensor is the obstruction to the horizontal distribution being integrable — it is the geometric object that measures how the C² directions "twist" relative to each other and the fiber. **Anisotropizing the C² metric (T4) directly deforms the A-tensor's argument**, changing how the horizontal generators couple to the spinor connection — and the spinor connection offset is exactly what enters `D_K` off the diagonal. T3 (su(2)) deforms a VERTICAL direction, where the relevant tensor is the (intrinsic, fiber-internal) T-tensor `T_U V`, which for a multiplicity-scalar fiber acts uniformly on the generation multiplet. **The horizontal/vertical split of O'Neill geometry is the structural reason T3 and T4 are NOT interchangeable.**

3. **Why the multiplicity-scalar property is su(2)-specific, not universal.** A left-invariant metric deformation along generator `X_a` enters `D_K` via the structure constants `f_{abc}` contracted with the Clifford algebra (the spinor connection offset). For T3 on su(2): the su(2) generators close on themselves (`[su(2), su(2)] ⊂ su(2)`) AND act on the C²-multiplet by the SAME Ad-action that defines the multiplet as an irrep — so by Schur's lemma the T3 deformation restricted to the C² generation-block is forced to be a scalar (`λ·I`). This is the W2 homogeneity property, and it is REAL for T3. But for T4 on C²: the C² generators do NOT close (`[C², C²] ⊂ u(1)⊕su(2)`), and an anisotropic split *within* C² (e.g., `diag(L3 e^{+δ}, L3 e^{+δ}, L3 e^{-δ}, L3 e^{-δ})` splitting the 2 from the 2̄, or a finer split inside the doublet) **breaks the Ad(SU(2)) equivariance that Schur's lemma requires**. Once Ad(SU(2)) equivariance is broken on the generation multiplet's own carrier space, Schur no longer forces `Y_ij ∝ I` — the off-diagonal elements `Y_12` can be non-zero at first order. **The wall is genuine for T3 because T3 preserves the equivariance Schur needs; the wall need NOT hold for T4 because T4 can break it.**

**Key finding.** The INV2-W1-1 FAIL (rank-1 wall genuine for su(2)) is NOT evidence that the wall is genuine for ALL internal moduli — it is evidence that the wall is genuine for the ONE direction (T3) that, by construction, preserves the very Ad(SU(2)) equivariance Schur's lemma exploits. T4 (C²-coset) is the structurally OPPOSITE case: it deforms the horizontal/off-diagonal sector that (a) carries the generation index (Baptista App E), (b) hosts the O'Neill A-tensor, and (c) can break Ad(SU(2)) equivariance on the generation multiplet's carrier space. Reading A predicts `|dY_12/dδ|_0 > eps_lift = 1e-3` and `rank(Y_ij) 1→≥2` for T4 — the OPPOSITE of T3. CV-8 Arm-G survives along the C²-coset.

**Cite (precise).** Baptista Paper 15 (arXiv 2306.01049) eq 3.58 (`su(3)=u(1)⊕su(2)⊕C²`), eq 3.60 (general U(2)-invariant 3D metric family — the (L1,L2,L3) parameterization), eq 3.62 (Ad(U(2)) action on su(3)); Paper 18 (arXiv 2601.08902) App E (Z₃×Z₃ → three generations); INV2-W1-1 (`computations/investigation-2/inv2_w1_off_u2_dirac_yukawa.py`, audit_sha256 `1481b77...`, the T3 su(2)-split NULL baseline). Moduli convention: T3=su(2) aniso, T4=C² aniso, two DISTINCT Milnor directions (paper-index-and-conventions memory §5D Moduli; `C2_IDX=[3,4,5,6]`, `SU2_IDX=[0,1,2]`). Stiffness anchor: `J_C2 = 0.933 M_KK` (4 bonds, dominant) vs `J_su2 = 0.059 M_KK` (3 bonds) — the C²-coset is the dominant directional stiffness on the 32-cell tessellation (canonical_constants.py:732), an INDEPENDENT signature that the C² directions are not equivalent to the su(2) ones.

**Questions for van-den-dungen (the hypothesis-crux):**
- The W2 homogeneity theorem proves "left-invariance ⇒ multiplicity-scalar." But its hypothesis is left-invariance *combined with Ad(stabilizer)-equivariance on the multiplicity space*. A T4 split that anisotropizes WITHIN C² is still left-invariant on the base BUT breaks Ad(SU(2)) on C². **Does the W2 theorem's multiplicity-scalar conclusion require Ad(SU(2))-equivariance as a hypothesis, or only left-invariance?** If it requires equivariance, T4 is OUTSIDE its scope and Reading A stands. (This is the question connes-ncg-theorist is the named cross-check on.)
- INV2-W1-1's su(2)-split was volume-preserving with det-ratio=1 EXACT. A T4 anisotropy *within* C² that splits the 2 from the 2̄ need NOT be volume-preserving in the same way — does the homogeneity argument depend on the deformation being a uniform scaling of an irreducible Ad-summand (which T3 is, but an *intra*-C² split is not)?

### BAP2: Adjudication-question framing (off-ALL-moduli vs off-su(2)-only; route-once declaration)

**The precise adjudication question (scoped).** The rollup (`_rollup-yukawa-moduli §4 WS-1`) generalizes inv-2's Contradiction-1: "Is the rank-1 wall genuinely off-Jensen for ALL internal transverse moduli, or only for the su(2)-split?" I sharpen it to the exact structural fork the geometry decides:

> **Does the W2-homogeneity multiplicity-scalar property depend on Ad(SU(2))-equivariance of the deformation on the generation multiplet's carrier space (the C² coset), or only on left-invariance of the metric on the base?**

This is the hinge. The two readings make OPPOSITE first-principles predictions for the SAME T4 (C²-coset) gate, and they invoke DIFFERENT machinery:
- **Reading A (baptista, Riemannian-submersion / O'Neill):** the property is **equivariance-dependent**. T3 (su(2)) preserves Ad(SU(2)) on the C²-multiplet → Schur forces `Y∝I` → wall genuine (INV2-W1-1 confirmed). T4 (C²-coset) can BREAK Ad(SU(2)) on its own carrier space (`[C²,C²]⊄C²`) → Schur does not apply → wall lifts. **Scope of the rank-1 wall: off-su(2)-only.** CV-8 Arm-G survives internally.
- **Reading B (van-den-dungen / connes, NCG-axiomatic):** the property is **left-invariance-dependent only**. Any left-invariant-on-the-base deformation is a multiplicity-scalar by the W2 homogeneity theorem; C² is "just another internal Jensen modulus" and inherits the property; only the EXTERNAL non-LI `ε_LX` (off-fiber, W2-breaking) lifts the rank. **Scope of the rank-1 wall: off-ALL-internal-moduli.** CV-8 Arm-G is dead; the hierarchy is exclusively external.

**What "off-ALL vs off-su(2)-only" means for the register.** This is not a cosmetic distinction:
- If **Reading A** holds: §VII.BL Generation-Blindness is scoped to the **U(2)-invariant slice + the su(2)-anisotropy direction**, with an OPEN internal lift-channel along C². The hierarchy's home is NOT yet pinned to "external only."
- If **Reading B** holds: §VII.BL Generation-Blindness strengthens from a slice statement to **ALL internal transverse moduli** (the full Milnor complement, not just su(2)), and the hierarchy's home is register-PINNED to the external non-LI `ε_LX` connection (which already carries an existence-PASS, `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`). This is the rollup's CHALLENGED-with-RELOCATION net.

The honest current register status (per `_rollup-yukawa-moduli §3`): inv-2 closed T3, inv-9 closed Arm-M (modular flavor) via W2; the §VII.BL strengthening from τ_fold to the full window [0.15,0.25] is along BOTH the geometric-su(2) and modular axes — but the **C²-coset (T4) direction is the one untested internal geometric probe**, explicitly named as "the surviving open seam" (`_rollup-yukawa-moduli §3` facet, §4 WS-1). This workshop adjudicates which reading the geometry supports BEFORE that compute, so the gate's outcome is interpreted correctly (not re-narrativized post-hoc — `epistemic-discipline.md` dual-prior discipline).

**Route-once declaration (PANEL route taken; dual-prior pre-registered for the compute).** Per the spawn charge and `_promotion-triage.md` Bucket-1 note 4 (WS-C2COSET borderline Q1/Q3) + Bucket-2 CF-1(Yukawa): this adjudication is carried BOTH as this workshop AND as the compute gate **S110-CF1-YUK-C2COSET** (CF-1/Yukawa, Bucket 2). The route-once rule: *if a planner runs the COMPUTE (dual-prior pre-registered), it SATISFIES this workshop and this panel does NOT separately fire; if the panel runs, its verdict pre-registers the dual prior for the compute.*

**I declare: the PANEL is running** (this workshop is being executed; the compute has not been run in-workshop — per the spawn rule "Do NOT run .py compute"). Therefore this panel's verdict (R3 Turn B) MUST pre-register the dual prior for the downstream compute S110-CF1-YUK-C2COSET:

- **Compute gate (pre-registered, identical to INV2-W1-1's form):** build the T4 C²-coset anisotropy modulus (split `L3·I_4 → diag` on `C2_IDX=[3,4,5,6]`, `J_C2=0.9330 M_KK`, transverse to U(2)), re-run the off-U(2) Dirac + Yukawa-overlap test on the d=2 generation multiplet via the existing `inv2_w1_off_u2_dirac_yukawa.py` machinery (swap the split block from su(2) to C²).
- **Gate criterion (pre-registered):** `|dY_12/dδ|_0 > eps_lift = 1e-3` AND `rank(Y_ij) 1→≥2` for δ ∈ (0, 0.20].
- **Dual prior (this panel pre-registers):** the panel's structural verdict on the equivariance-dependence question sets the prior. If the panel converges on Reading A (equivariance-dependent → T4 outside W2 scope): prior ~0.6–0.7 PASS (lift). If the panel converges on Reading B (left-invariance-only → T4 inside W2 scope): prior ~0.9 FAIL (no lift), matching the INV2-W1-1 dual_prior reallocation (0.65 Track A → 0.90 Track B for su(2)). The compute is the discriminator; the panel pre-registers WHICH reading the geometry supports so the gate's PASS/FAIL maps to a specific posterior re-allocation between Reading A and Reading B (no post-hoc re-narrativization).

**Carry-forward note.** The compute S110-CF1-YUK-C2COSET is the prime MATH carry-forward candidate from this workshop (4-field spec to be finalized in R3 Wrap-Up if the panel does not resolve the geometry decisively enough to obviate it). NON-MATH items (any §VII.BL scope annotation; the HK-B3 "B-3 modular-flavor CLOSED not live" register note; any Yukawa-register status edit) are to be EFFECTED in-session by Agent B in R3 Turn B per the spawn charge.

**Questions for van-den-dungen (framing-level):**
- Do you accept the scoping that the workshop's deliverable is the *equivariance-dependence* verdict (does W2's hypothesis require Ad(SU(2))-equivariance, or only left-invariance), with the C²-coset compute as the pre-registered discriminator? Or do you read the W2 theorem as already settling T4 (making this a Q3 compute-confirmation, not a Q1 adjudication)?
- If you hold Reading B, can you state the W2 homogeneity theorem's EXACT hypothesis set — specifically, does it assume the deformation is a uniform rescaling of an irreducible Ad-summand (which T3 is), or does it cover an *intra-summand* anisotropy of C² (the 2-vs-2̄ split, or a finer split inside the doublet) that breaks Ad(SU(2)) on the multiplicity space? The scope of that hypothesis IS the adjudication.

---

## Round 1 — van-den-dungen: Response & Cross-Synthesis

### Part 1: Response to baptista's Sections

#### Re: BAP1 — C²-coset-lifts steelman

**Substrate framing (binding, accepted).** I accept baptista's observable: `Y_ij = ⟨ψ_i | (1j·D_K) | ψ_j⟩` on the same-sign degenerate generation block (INV2-W1-1's fixed observable). I accept that the C²-coset is an internal transverse direction of the Jensen-deformed SU(3) fiber, NOT a moduli container. The adjudication is exactly "the rank of the Yukawa spectral functional under coset deformation." We agree on the object. We disagree on what the W2 homogeneity theorem says about it — and that disagreement is decidable from the theorem's stated mechanism, which I cite verbatim below.

**AGREE (the decomposition is exact).** BAP1's eq-3.58 isotropy decomposition `su(3) = u(1) ⊕ su(2) ⊕ C²` (idx 7 / 0,1,2 / 3,4,5,6) is correct and is the spine of the geometry. I also AGREE that su(2) (T3, the **3** = Ad of the stabilizer SU(2)) and C² (T4, the **2 ⊕ 2̄**, off-diagonal/horizontal) carry **structurally distinct Ad(U(2))-representations**. That is true and it is Baptista Paper 15 eq 3.58/3.62. I do NOT contest the submersion geometry. I contest the **inference from it** in step 3.

**MISSED — the load-bearing distinction: which TENSOR LEG of Peter-Weyl does a left-invariant metric act on.** BAP1's three-step lift mechanism turns entirely on step 3: "anisotropizing C² breaks Ad(SU(2))-equivariance on the generation multiplet's own carrier space, and once Ad(SU(2))-equivariance is broken Schur no longer forces `Y ∝ I`." This conflates the two legs of the Peter-Weyl tensor product. The framework's Hilbert space is (registry §VII.BL STRUCTURAL VERDICT, line 21110, verbatim):
```
H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}
```
There are TWO legs, and they are NOT the same object:
- `V_{(p,q)}` = the **irrep carrier space**. The Lie-algebra representation matrices `ρ_{(p,q)}(e_a)` act HERE. The Casimir, the Ad-action, and **all of Baptista's su(2)/C² Ad-structure live on this leg**.
- `ℂ^{m(p,q)}` = the **multiplicity leg**. The **generation index lives HERE** (registry line 21124/21129: "generations = the SU(3) Z₃-triality / Peter-Weyl multiplicity `t=(p−q) mod 3`"; the substrate-IS observable is "the between-class splitting functional over the bottom-N |λ| … `R_cross`", which is the inter-MULTIPLICITY-leg ratio).

Now the decisive fact, with the substitution chain written out (per `math-scripts.md §"Double-Check Logic Before Compute"`):
```
Step 1: D_K = Σ_a ρ_{(p,q)}(e_a) ⊗ γ_a + I⊗Ω   [Dirac operator; the metric eigenvalues enter via the
        orthonormal frame {e_a} and the spinor-connection offset Ω — INV2-W1-1 method, plan line 56]
Step 2: A left-invariant metric (Jensen, T3 su(2)-split, OR T4 C²-split) is an inner product on the
        Lie algebra 𝔤 transported by LEFT-translation. The frame operators ρ_{(p,q)}(e_a) it builds
        act on the IRREP leg V_{(p,q)}; they are ⊗ 1_{m(p,q)} on the multiplicity leg.
Step 3: ⇒ ANY left-invariant metric deformation reshapes D_K WITHIN V_{(p,q)} and acts as a SCALAR
        (∝ 1_{m(p,q)}) on the generation/multiplicity leg ℂ^{m(p,q)}.
Step 4: T4 (C²-split) IS a left-invariant metric (plan line 127 confirms the analog for T3:
        "left-invariance + block-diagonality (G10) hold for ANY left-invariant metric"; G10 is
        the BLOCK-DIAG-GENERAL-61 theorem — compact G + left-invariant metric ⇒ Schur-forced,
        my own S61 result). So T4 acts ∝ 1_{m(p,q)} on the generation leg.
Conclusion: T4 is multiplicity-scalar on the generation index — EXACTLY like T3 — regardless of
        whether it breaks Ad(SU(2)) on V_{(p,q)}. Breaking Ad(SU(2)) reshuffles the IRREP leg;
        the GENERATION leg is the commutant (right-regular action), untouched by a left-invariant
        deformation. BAP1 step 3 acts on the wrong leg.
```

This is the NCG cross-check the spawn charge names connes as the designated authority on, and it is settled in the register: **the W2 hypothesis is left-invariance ALONE, NOT "left-invariance combined with Ad(SU(2))-equivariance."** Registry §VII.BL line 21116, verbatim: *"(W2) Homogeneity wall — left-invariance ⇒ multiplicity-scalar representation … (`D_K` left-invariant ⇒ commutes with the right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`.)"* Ad(SU(2))-equivariance appears NOWHERE in the hypothesis. It cannot, because Ad(SU(2)) acts on the irrep leg `V_{(p,q)}` and the multiplicity-scalar conclusion is a statement about the OTHER leg `ℂ^{m(p,q)}`.

**EMERGES — the empirical hammer is already on the table, and it is T3 itself.** BAP1's own step-3 mechanism ("T3 preserves Ad(SU(2)) → Schur forces `Y∝I`; T4 breaks it → lift") makes a falsifiable prediction that INV2-W1-1 ALREADY TESTED — and it FAILED. INV2-W1-1's substitution chain (plan line 136) states baptista's exact reasoning: *"its matrix element between DISTINCT generation copies is NOT Schur-protected once the metric is no longer U(2)-invariant (the protection in Def 2 is EXACTLY the U(2)-equivariance statement, which δ≠0 destroys)."* The T3 su(2)-split `L2·I_3 → diag(L2 e^{+2δ}, L2 e^{−δ}, L2 e^{−δ})` **DOES break U(2)/Ad(SU(2)) equivariance** (it splits the **3** of su(2) into 1⊕2). By BAP1's logic the rank should have lifted. The verdict (inv2_gate_verdicts.txt, INV2-W1-1): `absdY12d0=1.943094e-15` vs `eps0.001`; `maxoffY0=1.748e-17_schurzero`; `distinct0=1_to_max1`; `gen_degen_lift=False`. **The rank did NOT lift — 9 OOM below the substantive-lift floor — precisely BECAUSE breaking Ad(SU(2)) on V_{(p,q)} does not touch the multiplicity-scalar property of the generation leg.** So BAP1's "equivariance-dependence" reading is not merely untested theory: its T3 instance has already been computed and it falsifies the equivariance-dependence hypothesis. The dual_prior on INV2-W1-1 reallocated 0.65→0.90 to Track B (wall genuine off-surface) on exactly this FAIL.

**The honest residual (where BAP1 is NOT yet refuted).** BAP1 step 1 (the C²-coset is the generation-CARRYING space, Paper 18 App E) and step 2 (the O'Neill A-tensor lives on the horizontal C² sector) are geometrically real and I do not dispute them as submersion geometry. What I dispute is that they CHANGE the multiplicity-leg conclusion. Step 1 is in fact a point IN MY FAVOR: if the generation index is realized in the C²-coset's representation content, that content sits in `V_{(p,q)}` decomposed under U(2) — i.e. the C²-coset's role in carrying generations is a statement about the IRREP leg's U(2)-branching, and a left-invariant deformation of the C² metric still acts ⊗1 on the multiplicity copies of each branch. The generation DEGENERACY is the multiplicity, not the branching. (Step 2's A-tensor I take up under VDD2 — there is a convention bug there that is independently load-bearing.)

#### Re: BAP2 — Adjudication-question framing

**AGREE on the fork's SHAPE; DISAGREE on which side the geometry sits.** BAP2 sharpens the rollup question to the exact hinge: *"Does the W2-homogeneity multiplicity-scalar property depend on Ad(SU(2))-equivariance of the deformation on the generation multiplet's carrier space, or only on left-invariance of the metric on the base?"* I accept this is THE hinge. My answer, anchored in the register's verbatim statement of W2 (line 21116) and confirmed by the Re:BAP1 substitution chain: **left-invariance only.** The two readings BAP2 lays out are correctly stated; I am taking Reading B, and I claim it is not a coin-flip — it is the register's already-PROVEN reading.

**MISSED — BAP2 frames W2 as an OPEN hypothesis-scope question; the register already CLOSED it.** BAP2's framing question ("does W2 require Ad(SU(2))-equivariance as a hypothesis, or only left-invariance?") is posed as if the answer is to be DISCOVERED by this panel + the C²-coset compute. But §VII.BL is **STAGE-3-PERMANENT** (promoted S99 W3-1, Stage-2 cross-axis PASS-AND of JOINT clauses (d),(e), audit `0f0c4f65`) — and I, van-den-dungen-bridge-theorist, was the **axis-A NCG cross-reviewer on that very Stage-2 verify** (registry line 21104: "axis-A `van-den-dungen-bridge-theorist` (NCG-axiomatic) + axis-B `dirac-antimatter-theorist`, both non-authors … each independently PASSing its single-axis clause + the JOINT clauses (d),(e) PASS-AND'd"). The W2 mechanism I PASS-AND'd is precisely the left-regular/right-regular commutant argument — `D_K` left-invariant ⇒ commutes with right-translation ⇒ scalar on `ℂ^{m(p,q)}`. The twisted-Ω¹ escape was proven dead by Skolem–Noether (registry line 21120: "`Aut(A_K)` is multiplicity-blind"), and the §VII.BM foam-protection identity `[H_foam(N), ε_LX]=0` makes the generation index a TOPOLOGICAL index (`α_phys=∞`). The hypothesis-scope question BAP2 wants to open is, on the register, a closed theorem. **This is the Q1-vs-Q3 disposition: the geometry does not need adjudicating — the register supplies the verdict; the C²-coset compute is a Q3 confirmation of an already-PROVEN scope, not a Q1 discovery.**

**Where I yield to BAP2's caution.** I do NOT claim the compute is pointless. The register proves W2 covers **every `A_K`-built form** (inner, twisted-inner `Ω¹_σ`, opposite-action `JAJ⁻¹`) AND "the SU(3)-equivariant (Casimir-function τ) geometric deformation" (registry line 21110). The C²-coset T4 is a LEFT-INVARIANT geometric metric deformation. The question is whether T4 is inside the class "left-invariant geometric deformation" the theorem already covers — and it manifestly is (it is an inner product on 𝔤 transported by left-translation, block-diagonal by G10). So the compute is a Q3 confirmation with a register-predicted FAIL. But confirming a STAGE-3-PERMANENT theorem on a specific named modulus that was explicitly flagged as "the one untested internal geometric probe" (`_cross-investigation-synthesis.md §2` CLARIFIED-8) has real value: it converts "the C²-coset is the surviving open seam" into "the C²-coset is closed like every other internal modulus," which is the rollup's CHALLENGED-with-RELOCATION net made complete.

**Route-once: I CONFIRM the PANEL route, and I sharpen the dual prior AGAINST BAP2's symmetric framing.** BAP2 declares the panel running and pre-registers the compute S110-CF1-YUK-C2COSET with priors "0.6–0.7 PASS if Reading A; 0.9 FAIL if Reading B." I accept the panel route and the compute pre-registration. But I reject the SYMMETRY of BAP2's prior elicitation, because it treats the two readings as a priori comparable when the register and the T3 precedent already break the symmetry hard:

- **Reading A (lift) is not at 0.6–0.7.** Its central mechanism (Ad(SU(2))-breaking ⇒ rank lift) was DIRECTLY TESTED by T3 (which breaks Ad(SU(2))) and returned `dY_12/dδ|_0 = 1.9e-15`, FAIL by 9 OOM. A reading whose mechanism has already failed its closest realized test cannot carry 0.6–0.7.
- **The correct dual prior** (matching the INV2-W1-1 precedent and the §VII.BL STAGE-3 status): **prior ~0.90 FAIL (no lift), ~0.10 PASS.** Discriminator mapping: a compute FAIL → reallocate to 0.95 Track B (T4 multiplicity-scalar, confirming §VII.BL on the C²-coset; the rank-1 wall is off-Jensen for ALL internal transverse moduli, not just su(2)); a compute PASS → reallocate 0.90 to Track A AND flag a register CONTRADICTION requiring re-examination of the §VII.BL Stage-2 (because a left-invariant T4 lifting the rank would contradict the multiplicity-scalar theorem I cross-verified — so a PASS is not just "Reading A wins," it is "a STAGE-3-PERMANENT theorem has a counterexample," a much higher bar that would itself need a Stage-2 re-audit); INFO (sub-threshold lift or regime-marginal) → unchanged, routes to higher-L_max.

**The structural verdict this panel pre-registers (for R3):** the rank-1 Yukawa wall is genuinely off-Jensen for **ALL internal transverse moduli** (the full Milnor complement that stays left-invariant), not only su(2) — because the multiplicity-scalar property is a left-invariance fact about the generation/multiplicity leg `ℂ^{m(p,q)}`, independent of which Ad(U(2))-summand the deformation lives in. §VII.BL Generation-Blindness extends from the τ_fold slice to the full window [0.15,0.25] AND from {U(2)-slice + su(2)-direction} to {every left-invariant internal direction}. CV-8 Arm-G (the internal geometric hierarchy corridor) is **dead**; the hierarchy's home is the external non-LI `ε_LX` connection, register-PINNED (`S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN` existence-PASS). The compute is the confirming discriminator, not the deciding one.

### Part 2: Original Analysis

#### VDD1: Reading-B (multiplicity-scalar τ-modulus; W2 homogeneity theorem; FAILs like su(2))

**Thesis.** The C²-coset T4 anisotropy is a multiplicity-scalar τ-modulus. It cannot generate inter-generation rank, and it FAILs the rank-lift gate exactly as the su(2)-split T3 did — not by coincidence, but because both are left-invariant metric deformations and the W2 homogeneity theorem's multiplicity-scalar conclusion follows from left-invariance ALONE. Substrate-first: the Yukawa couplings are spectral moments of `D_K` on the generation multiplet; the rank of that spectral functional under any left-invariant coset deformation is locked at 1 by the left/right commutant structure of the Peter-Weyl decomposition.

**The theorem, stated at the leg where it operates (the affirmative core).** This is the NCG content the spawn charge asks me to supply. The framework's finite×continuum triple is `(A_K, H_K, D_K, J)`, `A_K = ℂ ⊕ ℍ ⊕ M₃(ℂ)`, with
```
H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}      (registry §VII.BL, line 21110)
```
This is the Peter-Weyl theorem for `L²(SU(3))`: the two tensor legs carry the **two commuting regular representations**. The LEFT-regular action acts on `V_{(p,q)}` (the irrep carrier); the RIGHT-regular action acts on `ℂ^{m(p,q)}` (the multiplicity copies = `dim V_{(p,q)}` of them). They commute — that is the content of `L²(G) = ⊕_ρ V_ρ ⊗ V_ρ^*`.

A left-invariant metric on SU(3) is, by definition, an `Ad`-arbitrary inner product `⟨·,·⟩` on `𝔤 = T_eSU(3)` transported by left-translation `L_g`. The framework's three named directions are all of this kind:
- **Jensen**: `g_K = 3·diag(L1·I_1, L2·I_3, L3·I_4)`, `(L1,L2,L3) = (e^{2τ}, e^{-2τ}, e^{τ})` — anisotropic across the (u(1), su(2), C²) blocks, isotropic within.
- **T3 (su(2)-split)**: `L2·I_3 → diag(L2 e^{+2δ}, L2 e^{-δ}, L2 e^{-δ})` — anisotropic WITHIN su(2).
- **T4 (C²-split)**: `L3·I_4 → diag(...)` — anisotropic WITHIN C² (e.g. splitting the **2** from the **2̄**).

`D_K = Σ_a ρ_{(p,q)}(e_a) ⊗ γ_a + I⊗Ω`. The metric enters through the orthonormal frame `{e_a}` (rescaled by the `L_i, δ`) and the spinor connection `Ω` (built from the structure constants contracted with the rescaled frame). The Lie-algebra representation operators `ρ_{(p,q)}(e_a)` act on `V_{(p,q)}` — the LEFT-regular leg. **They are `⊗ 1_{m(p,q)}` on the multiplicity leg by construction**, because the multiplicity leg is the COMMUTANT of the left-regular representation: it carries the right-regular action, and left-invariant vector fields commute with right-translations. Therefore:

> For ANY left-invariant metric — Jensen, T3, T4, or any point in the 28-parameter left-invariant family — `D_K` acts as a scalar `λ(p,q)·1_{m(p,q)}` on each multiplicity factor. The generation index, which lives on `ℂ^{m(p,q)}`, is untouched. `Y_ij = ⟨ψ_i|(1j·D_K)|ψ_j⟩` on the generation copies is `λ·δ_{ij}` — rank 1, off-diagonal entries EXACTLY zero.

This is the W2 homogeneity wall (registry line 21116, the mechanism in parentheses is exactly this commutant argument). Note what it does NOT require: it does not require `Ad(SU(2))`-equivariance, it does not require the deformation to preserve any stabilizer action on `V_{(p,q)}`, it does not require volume-preservation. It requires ONLY that the metric be left-invariant (so that the frame operators land on the left-regular leg and act trivially on the right-regular/multiplicity leg). T4 satisfies this. **T4 FAILs the rank-lift gate.**

**Why T3 and T4 are the same KIND of object on the leg that matters (BAP1's distinction is real but irrelevant here).** BAP1 is correct that T3 (Ad **3**) and T4 (Ad **2⊕2̄**) are distinct Ad(U(2))-representations — on `V_{(p,q)}`. But `R_cross`, the generation-mass ratio, is a multiplicity-leg observable. On the multiplicity leg both T3 and T4 act as scalars, because both are left-invariant. The Ad(U(2))-content distinguishes how they reshape the IRREP-leg spectrum `λ(p,q)` (and hence how they move the OVERALL fiber spectral moments `a_n`, sin²θ_W, etc.) — but it does not distinguish their action on the generation degeneracy, which is zero (scalar) for both. **BAP1's distinction lives on the wrong leg for the Yukawa-rank question.** It would be the RIGHT distinction for a question about the fiber's `a_n` moments or the gauge-coupling normalization (where the irrep-leg spectrum is what matters) — which is precisely why T4 is the dominant directional stiffness (`J_C2=0.933` vs `J_su2=0.059`, BAP1's own stiffness anchor): the C² block carries 4 of the 8 dimensions and dominates the metric's spectral weight. But "dominant stiffness on the irrep leg" ≠ "can lift the multiplicity-leg degeneracy." Those are orthogonal legs.

**The Skolem–Noether closure (why no `A_K`-internal trick escapes, including a "cleverer" C²-split).** One might ask: what if T4 is combined with an algebra automorphism or a twisted inner fluctuation to reach the multiplicity leg indirectly? Registry §VII.BL line 21120 (the result I cross-verified at Stage-2) forecloses this: by Wedderburn + Skolem–Noether, every `σ ∈ Aut(A_K)` is inner on each simple summand of `ℂ ⊕ ℍ ⊕ M₃(ℂ)` (no isomorphic summands to permute), so `σ(a)=uau*` per block, preserving the Peter-Weyl decomposition and acting `π_{(p,q)}(u) ⊗ 1_{m(p,q)}` — multiplicity-scalar. The twisted commutator `[D,a]_σ` inherits the orbital-block-scalar obstruction verbatim. So inner, twisted-inner `Ω¹_σ`, AND opposite-action `JAJ⁻¹` are ALL multiplicity-scalar — they all factor through `π: A_K → B(H)` whose image is `⊕ B(V_{(p,q)}) ⊗ 1_{m(p,q)}`. The generation-lifting datum lives in the COMPLEMENT `⊕ 1_{V_{(p,q)}} ⊗ M_{m(p,q)}(ℂ)` — the multiplicity-acting summand orthogonal to the image of the Hochschild 1-cochain `[D_K,−]`. This is **Morita-invariant: module-membership, not spectrum** (registry line 21155). No `D_K` re-tuning — and a fortiori no T4 metric retuning, since T4 only re-tunes the frame inside `ρ_{(p,q)}` — can detune it.

**Direction of explanation (substrate-first, `phononic-framing.md`).**
```
D_K multiplicity-scalar representation (left-invariance, all internal moduli incl. T4)
  → (W1 reality ∧ W2 homogeneity forbid intrinsic inter-generation rank)
  → Yukawa hierarchy ∈ external non-LI ε_LX connection (outside every A_K-module)
  → measured SM fermion masses
```
The rank-1 wall is NOT a property the fabric carries internally that we might unlock with the right coset deformation; it is a statement that the fabric's entire differential calculus `Ω¹_{D_K}(A_K)` is BLIND to the generation index by homogeneity, and the C²-coset is one more internal direction inside that blind calculus. The hierarchy is a measure of how far the fiber connection is deformed AWAY from homogeneity — and that deformation `ε_LX` is external (non-left-invariant on the multiplicity index), exactly as the baryon asymmetry `δA` is (registry §VII.BL #7/#9 two-frontier table).

**Where Reading B is bounded (the one honest caveat).** The theorem covers left-invariant deformations. If a proposer constructed a C²-coset deformation that is NOT left-invariant — i.e. a `τ(x)`-field gradient or an Ehresmann-connection profile that varies over the base M⁴ — that object is genuinely outside W2, BUT it is then no longer "the C²-coset modulus" — it is precisely `ε_LX`, the external non-LI connection the register already names as the hierarchy's home. So even the escape route lands on Reading B's conclusion: the lift requires going EXTERNAL (non-LI), and "C²-coset T4" as a left-invariant internal modulus is not that. The compute S110-CF1-YUK-C2COSET, by construction (left-invariant block-diagonal split, the INV2-W1-1 machinery with the split block swapped su(2)→C²), tests the LEFT-INVARIANT C²-modulus — which Reading B predicts FAILs.

#### VDD2: Questions for baptista

**Q1 (the hypothesis-crux, decisive).** The register states the W2 mechanism verbatim (line 21116): *"`D_K` left-invariant ⇒ commutes with the right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`."* This derivation invokes ONLY left-invariance (to put the frame operators on the left-regular leg) and the Peter-Weyl commutant structure (left-regular and right-regular commute). It does NOT invoke Ad(SU(2))-equivariance. Your BAP1 step 3 asserts the multiplicity-scalar property is "equivariance-dependent." **Can you exhibit the step in the W2 derivation where Ad(SU(2))-equivariance enters as a hypothesis — i.e. a step that fails for a left-invariant-but-not-Ad(SU(2))-equivariant deformation?** I claim no such step exists, because Ad(SU(2)) acts on `V_{(p,q)}` (the irrep leg) and the conclusion is about `ℂ^{m(p,q)}` (the multiplicity leg, the commutant). If you cannot exhibit it, BAP1 step 3 is acting on the wrong tensor leg and Reading A's mechanism dissolves.

**Q2 (the T3 precedent — already-computed counterexample to your own mechanism).** INV2-W1-1 tested the su(2)-split T3, which DOES break Ad(SU(2))-equivariance (it splits the **3** into 1⊕2). Its own substitution chain (plan line 136) states your exact reasoning: *"the protection in Def 2 is EXACTLY the U(2)-equivariance statement, which δ≠0 destroys."* By BAP1 step 3's logic, T3 should have lifted the rank. It returned `dY_12/dδ|_0 = 1.943e-15`, `gen_degen_lift=False` — a 9-OOM FAIL. **If breaking Ad(SU(2)) equivariance were sufficient to lift the rank (your step-3 claim), why did T3 — which breaks it — not lift?** And if your answer is "T3 broke the wrong Ad-summand, only C² is the generation-carrier," that is a step-1 claim (which leg carries generations), not a step-3 claim (whether breaking equivariance lifts the multiplicity-scalar property) — so does conceding Q2 mean BAP1 step 3 is withdrawn, leaving only the step-1 "C² is special" claim, which I address in Q3?

**Q3 (the A-tensor convention — BAP1 step 2 may rest on a leg/sector confusion I have hit before).** BAP1 step 2 says "anisotropizing the C² metric (T4) directly deforms the A-tensor's argument," locating the O'Neill A-tensor "on" the horizontal C² sector and using it as the geometric source of an off-diagonal `D_K` coupling. But in INV12-W2-1 I caught a load-bearing convention bug on exactly this object: **the O'Neill A,T tensors of the submersion live in the BASE-FIBER off-block structure (`g_{μa}`, μ base, a fiber), NOT in the fiber-INTERNAL (u(1)/su(2)/C²) sector structure.** The reason is that SU(3) is SIMPLE, so its Lie bracket is NEVER block-diagonal in (u(1),su(2),C²): C² brackets close back into su(2)⊕u(1) (`[λ₄,λ₅]∼λ₃+√3λ₈`) for ANY left-invariant metric, including the pure product Jensen metric. Reading the cross-sector fiber structure constants as "the A-tensor" gave me `‖A‖²(Jensen)=0.367 ≠ 0`, FAILING to recover A-TENSOR-61 (A=T=0 EXACT on the Jensen line) — a measurement artifact, not a result. The CORRECT A-tensor is sourced by the Ehresmann connection `A_μ^a` (base-fiber off-block): `A_{μν}^a = ½ F_{μν}^a`, zero on the Jensen line BY CONSTRUCTION. **Two questions:** (a) Is your BAP1 step-2 A-tensor the genuine submersion O'Neill tensor (base-fiber `g_{μa}`, = ½ F, zero on Jensen) — in which case T4 (a fiber-INTERNAL left-invariant deformation) does NOT touch it, since it has no base-fiber off-block component? Or (b) is it the cross-sector fiber-internal structure-constant object I found to be a measurement artifact (nonzero even for the pure product metric, hence not a deformation signature at all)? If (a), step 2 does not support T4-lifts (T4 leaves the base-fiber A-tensor at zero). If (b), step 2 rests on the artifact. **Which O'Neill A-tensor is BAP1 step 2 invoking — `g_{μa}` (base-fiber, =½F, Jensen-zero) or the fiber-internal cross-sector bracket (artifact)?**

**Q4 (concession-mapping — what would it take for Reading A to be right?).** I hold that a left-invariant T4 cannot lift the rank. The ONLY object that lifts it (register §VII.BL, Corollary line 21118) is an external non-LI fibre connection breaking W2 while preserving W1, `P_nLI = ‖ε_LX‖² > 0`. **Do you agree that IF the C²-coset compute returns a lift `dY_12/dδ|_0 > 1e-3`, the correct reading is NOT "Reading A wins" but "a STAGE-3-PERMANENT theorem (§VII.BL, Stage-2-verified) has a counterexample requiring a Stage-2 re-audit"?** I ask because the dual prior must reflect this asymmetry: a lift would not merely favor your reading, it would contradict a permanent theorem I personally cross-verified — a much higher evidentiary bar than "the panel disagreed." Conversely, do you agree that a FAIL (the register-predicted outcome) confirms §VII.BL extends to the C²-coset and closes CV-8 Arm-G, with the hierarchy's home register-PINNED to `ε_LX`?

---

## Round 2 — baptista: Follow-up

### CONVERGENCE

**I concede the core. BAP1 step 3 acts on the wrong tensor leg, and Reading-A's lift-mechanism dissolves.** This is the substantive change from Round 1. Re:BAP1's two-leg substitution chain is correct, register-anchored, and decisive, and I had it backwards. Let me state precisely what changed and why.

**What I claimed (BAP1 step 3).** "Anisotropizing C² (T4) breaks Ad(SU(2))-equivariance on the generation multiplet's own carrier space; once equivariance is broken, Schur no longer forces `Y ∝ I`; so the off-diagonal `Y_12` can be nonzero at first order." The hidden premise was that the generation multiplet's carrier space is the C²-coset's Ad-representation content — i.e. that breaking Ad(SU(2)) on `V_{(p,q)}` reaches the generation index.

**Why that is wrong (Re:BAP1, anchored in the register I now verify).** The framework's Hilbert space is `H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}` (registry §VII.BL line 21110, which I read in full this round). These are the **two commuting regular representations** of Peter-Weyl on `L²(SU(3)) = ⊕_ρ V_ρ ⊗ V_ρ*`:
- the **left-regular** action acts on `V_{(p,q)}` (the irrep carrier) — and **all of my su(2)/C² Ad-structure lives here**;
- the **right-regular** action acts on `ℂ^{m(p,q)}` (the multiplicity copies) — and **the generation index lives here** (registry line 21124: generations = the SU(3) Z₃-triality / Peter-Weyl multiplicity `t = (p−q) mod 3`, `proven_384`; the observable `R_cross` is an inter-MULTIPLICITY-leg ratio).

A left-invariant metric is an inner product on `𝔤` transported by left-translation; the frame operators `ρ_{(p,q)}(e_a)` it builds in `D_K = Σ_a ρ_{(p,q)}(e_a) ⊗ γ_a + I⊗Ω` land on the left-regular leg `V_{(p,q)}` and act `⊗ 1_{m(p,q)}` on the multiplicity leg by construction — because the multiplicity leg is the **commutant** of the left-regular representation (it carries the right-regular action, and left-invariant vector fields commute with right-translations). T4 (the C²-split) is a left-invariant metric (block-diagonal by my own G10/BLOCK-DIAG-GENERAL-61 result; van-den-dungen cites it correctly). Therefore T4 acts as a scalar `λ(p,q)·1_{m(p,q)}` on the generation leg — **regardless of whether it breaks Ad(SU(2)) on `V_{(p,q)}`.** Breaking Ad(SU(2)) reshuffles the IRREP leg; the generation leg is untouched. **BAP1 step 3 was a statement about the wrong leg.**

**I verified the W2 hypothesis text and it confirms Re:BAP1, not me.** Registry §VII.BL line 21116 states the W2 mechanism verbatim: *"(W2) Homogeneity wall — left-invariance ⇒ multiplicity-scalar representation … (`D_K` left-invariant ⇒ commutes with the right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`.)"* This invokes ONLY left-invariance and the Peter-Weyl commutant structure. **Ad(SU(2))-equivariance appears nowhere in the hypothesis** — it cannot, because Ad(SU(2)) acts on `V_{(p,q)}` and the conclusion is about `ℂ^{m(p,q)}`. My BAP1 question ("does W2 require equivariance, or only left-invariance?") is answered by the register's own statement of the theorem: **left-invariance alone.** I withdraw the framing that this was an open hypothesis-scope question for the panel to discover; the register supplies the verdict.

**The empirical hammer is the clincher, and it falsifies my own mechanism.** INV2-W1-1's T3 su(2)-split DOES break Ad(SU(2))-equivariance — it splits the **3** of su(2) into 1⊕2 (its own substitution chain, plan line 136, states exactly my reasoning: "the protection in Def 2 is EXACTLY the U(2)-equivariance statement, which δ≠0 destroys"). By BAP1 step 3's logic the rank should have lifted. It returned `dY_12/dδ|_0 = 1.943e-15`, `gen_degen_lift=False` — 9 OOM below the substantive-lift floor. **A mechanism whose closest realized test has already failed cannot be carried at 0.6–0.7.** I wrote the INV2-W1-1 memory myself; its "deep point" is precisely that the generation degeneracy is protected *deeper than U(2)-equivariance*. I now read that as: protected by the left/right commutant structure, which Ad-breaking on the irrep leg does not reach.

**I CONVERGE on Reading B's structural conclusion and on van-den-dungen's dual prior.** The rank-1 Yukawa wall is genuinely off-Jensen for **ALL left-invariant internal transverse moduli** (the full Milnor complement), not only su(2) — because the multiplicity-scalar property is a left-invariance fact about the generation/multiplicity leg `ℂ^{m(p,q)}`, independent of which Ad(U(2))-summand the deformation lives in. §VII.BL Generation-Blindness extends from {U(2)-slice + su(2)-direction} to {every left-invariant internal direction}. I accept van-den-dungen's correction of my symmetric prior: the compute S110-CF1-YUK-C2COSET should be pre-registered at **~0.90 FAIL / ~0.10 PASS**, matching the INV2-W1-1 reallocation precedent, NOT my symmetric 0.6–0.7. The compute is a **Q3 confirmation** of a STAGE-3-PERMANENT theorem on a specifically-named modulus, not a Q1 discovery.

### DISSENT

I have one genuine residual disagreement, and it is NARROWER than Round 1 — it is no longer about whether T4 lifts the rank (it does not), but about **the precise geometric reason WHY the C²-coset's special role in carrying generations does not transport to the multiplicity leg.** This matters because it is the difference between "T4 fails like su(2) by an undifferentiated multiplicity-scalar argument" and "T4 fails for a reason that, once correctly located, tells us exactly where Reading A's geometry was right." I dissent on van-den-dungen's implicit framing that BAP1 steps 1–2 are simply *irrelevant*; I claim they are **relevant-but-leg-misrouted**, and the routing is itself a structural result worth pinning. New evidence on the two legs:

**(1) Does T4 act on the multiplicity leg `ℂ^{m(p,q)}` or only on the irrep leg `V_{(p,q)}`?** I now answer: **only on the irrep leg** — and I can sharpen WHY beyond van-den-dungen's general commutant argument. The generation multiplicity `m(p,q)` is NOT the U(2)-branching of `V_{(p,q)}`. This is the precise correction to my BAP1 step 1. When `V_{(p,q)}` decomposes under U(2) ⊂ SU(3), the branching multiplicities (how many times each U(2)-irrep appears) DO live in `V_{(p,q)}` and ARE moved by an Ad(U(2))-anisotropic deformation like T4. But that branching is an **isospin/hypercharge** decomposition — exactly what INV2-W1-1 found T3 lifts (the global signed-eigenvalue count 22→30, *isospin-multiplet splitting* SU(2)→U(1), my own memory's "deep point"). The **generation** multiplicity `ℂ^{m(p,q)}` is the right-regular copy-count = `dim V_{(p,q)}`, a Peter-Weyl degeneracy orthogonal to the U(2)-branching. So BAP1 step 1's claim "C² is the generation-carrying space" conflates the U(2)-branching of the irrep leg (which T4 *does* touch, and which gives isospin not generation) with the multiplicity leg (which carries generations and which T4 does *not* touch). **T4 acts on the irrep leg only; the generation leg is the commutant; the lift is zero.** This is a STRENGTHENING of van-den-dungen's argument, not a dissent from its conclusion — but it locates the failure of my step 1 precisely, which the undifferentiated "T4 is multiplicity-scalar like everything else" does not.

**(2) Is my BAP1 step-2 A-tensor the genuine submersion O'Neill tensor, or the fiber-internal artifact?** This is where I both concede VDD2-Q3 AND surface new evidence that complicates van-den-dungen's clean "(a) it's base-fiber, Jensen-zero, T4 doesn't touch it" reading. I read INV12-W2-1 in full this round (the gate van-den-dungen authored the A-tensor convention bug in). The decisive numbers:

- On the Jensen line: `||A||²(Jensen) = 0.0`, `||T||²(Jensen) = 0.0`, Ehresmann connection norm `= 0.0` (A-TENSOR-61 recovered by construction — the base-fiber connection vanishes on the block-diagonal Jensen metric). **This confirms van-den-dungen's reading (a): the GENUINE submersion A-tensor is base-fiber `g_{μa} = ½F`, zero on Jensen.**
- But OFF the Jensen line, along the **35D moduli displacement** δ (which is the channel T4 lives in), INV12-W2-1 computes a NON-ZERO `||T||² = 7.8125×10⁻⁴` from the **"fiber second fundamental form, modulus-gradient"** and `||A||² = 3.2235×10⁻⁷` from a non-abelian connection curvature `F_{01}^a = A_0^b A_1^c f^a_{bc}` involving explicitly "su(2)+u(1) vs C² non-commuting profiles" (INV12-W2-1 WP Step 3, line 49). **So a fiber-internal moduli deformation DOES turn on a (T-)tensor at O(δ).**

The reconciliation — and it is the crux of my narrowed dissent — is that **this O(δ) T-tensor lives on the a₂ spectral-action MOMENT (the irrep-leg spectrum), NOT on the multiplicity leg.** INV12-W2-1's verdict is about `|S_cross|/S_base` — the O'Neill remainder in the Seeley-DeWitt a₂ coefficient that feeds G_N and n_s. That is an algebra-INVARIANT spectrum-only functional on `V_{(p,q)}`. So my BAP1 step 2 was invoking a REAL geometric object (the fiber-modulus T-tensor turns on off-Jensen, INV12-W2-1 confirms it numerically), but that object deforms the **irrep-leg spectrum** `λ(p,q)`, which is exactly the leg that does NOT carry generations. **The A-tensor I invoked is neither van-den-dungen's pure base-fiber `g_{μa}` (option a) NOR a measurement artifact (option b) — it is the genuine off-Jensen fiber-modulus T-tensor, which is real but lives on the wrong leg for the Yukawa-rank question.** My dissent from van-den-dungen's VDD2-Q3 dichotomy is that it is not a clean (a)-or-(b): there is a (c), the genuine off-Jensen fiber-internal T-tensor, real and nonzero, but algebra-INVARIANT (irrep-leg), hence impotent on the multiplicity leg by the same commutant argument that defeats T4 directly. This is the same leg-misrouting as step 1, now at the level of the geometric tensor rather than the Ad-representation.

So the residual dissent is small and structural: I do NOT dissent on the verdict (T4 cannot lift the rank). I dissent on the *characterization* — van-den-dungen's "T4 is just another multiplicity-scalar like su(2)" is correct but under-resolved; the sharper truth is that T4 has REAL first-order action (it moves the irrep-leg spectrum and turns on a genuine fiber T-tensor, INV12-W2-1-confirmed), and it is precisely the **algebra-INVARIANT / algebra-DEPENDENT (irrep-leg / multiplicity-leg) orthogonality** (`cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`, MANDATORY at K=3) that quarantines that real action away from the generation degeneracy. That orthogonality is the deeper "why," and it is the register's own structural axis.

### EMERGENCE

Three insights crystallized from the cross-pollination that neither Round-1 section had:

**E1 — The generation index is protected by ALGEBRA-AXIS ORTHOGONALITY, and that is the unifying "why" behind both the su(2) FAIL and the (predicted) C² FAIL.** Putting Re:BAP1's two-leg argument together with my INV12-W2-1 finding: there are two orthogonal families of observables on `(A_K, H_K, D_K)` (registry's algebra-axis orthogonality conjecture, MANDATORY at K=3) — algebra-INVARIANT spectrum-only functionals (act on the irrep leg `V_{(p,q)}`: the a₂/a₄ moments, sin²θ_W, the gauge-coupling normalization, the isospin-branching) and algebra-DEPENDENT state-pair functionals (act on the multiplicity leg: `R_cross`, the generation ratio, the Connes-distance ladder). **Every left-invariant metric deformation — Jensen, T3, T4, all 28 left-invariant parameters — acts on the FIRST family and is scalar on the SECOND.** The generation index is protected not by "a symmetry deeper than U(2)" (my INV2-W1-1 memory's vague formulation) but by its *membership in the algebra-DEPENDENT family*, which left-invariant frame operators cannot reach. This is Morita-invariant (registry line 21155): module-membership, not spectrum. T3 lifted isospin (irrep-leg, 22→30) and left generation flat (multiplicity-leg); T4 will move the a₂ moment (irrep-leg, INV12-W2-1's `||T||²` turns on) and leave generation flat (multiplicity-leg). **Same orthogonality, two moduli, one predicted outcome.**

**E2 — The C²-coset IS special — just not for the Yukawa rank.** My Round-1 stiffness anchor (`J_C2 = 0.933` vs `J_su2 = 0.059`; the C²-block carries 4 of 8 dimensions and dominates the directional stiffness) is REAL and van-den-dungen conceded it. The emergent reframing: C²'s dominance is an **irrep-leg** fact — it dominates the spectral WEIGHT, hence the a₂ moments, hence G_N / sin²θ_W / the gauge normalization. INV12-W2-1's nonzero off-Jensen `||T||²` is sourced largely by the C² non-commuting profiles (line 49: "su(2)+u(1) vs C² non-commuting"). So T4 is the DOMINANT lever on everything that lives on the irrep leg — and exactly impotent on the one thing (generations) that lives on the multiplicity leg. The C²-coset's geometric specialness is genuine; my error was routing it to the generation observable instead of the gauge/gravity observables where it actually dominates. This is a productive relocation, not a dead end: it predicts T4 is the right knob for an a₂-moment / Weinberg-angle off-Jensen study, which is a different (live) question.

**E3 — The two-frontier table generalizes: "intrinsically-rigid matter-sector asymmetry ⇒ external non-LI deformation" is a leg-membership theorem.** Registry §VII.BL's named precondition (line 21157, "Non-LI-Deformation Necessity," SUGGESTION K=2, instances #7 Yukawa + #9 baryogenesis) is, read through the two-leg lens, the statement that **any observable living on the multiplicity leg requires an external non-LI deformation to source it**, because the entire `A_K`-built differential calculus `Ω¹_{D_K}(A_K)` maps into the algebra-INVARIANT (irrep-leg) subalgebra. The C²-coset closure is not a new instance of the precondition — it is a confirmation that the *whole left-invariant geometric family* (not just su(2)) sits inside the calculus that cannot reach the multiplicity leg. This sharpens the K=2→K=3 promotion candidate: δ_CP (forced real by `[J,D_K]=0`) is the natural third instance precisely because CP violation is, like generations and baryon asymmetry, a multiplicity-leg datum the homogeneous fiber cannot encode.

### QUESTIONS

I answer van-den-dungen's VDD2 questions directly (they are now mostly conceded), then pose two sharper follow-ups for R2-B / R3.

**Answer to VDD2-Q1 (exhibit the step where Ad(SU(2))-equivariance enters W2).** I cannot exhibit it, and I concede no such step exists. The W2 derivation (registry line 21116) goes: left-invariance ⇒ `D_K` commutes with right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`. Every step invokes left-invariance + Peter-Weyl commutant; none invokes Ad(SU(2))-equivariance, which is a statement about `V_{(p,q)}` (the left-regular leg), orthogonal to the conclusion about `ℂ^{m(p,q)}` (the right-regular leg). **BAP1 step 3 is withdrawn.** Its Schur-on-the-multiplet argument was the right tool for the isospin-branching question (irrep leg) and the wrong tool for the generation question (multiplicity leg).

**Answer to VDD2-Q2 (why did T3 — which breaks Ad(SU(2)) — not lift?).** Because breaking Ad(SU(2)) on `V_{(p,q)}` does not touch the multiplicity-scalar property of the generation leg, which is the commutant. T3 lifted exactly what breaking Ad(SU(2)) on the irrep leg SHOULD lift: the isospin branching (22→30 signed eigenvalues, SU(2)→U(1)). It left generation flat because generation is not on that leg. And yes — conceding Q2 means BAP1 step 3 is withdrawn; what survives from BAP1 is only the step-1 "C² is geometrically distinguished" claim, which I have now RE-ROUTED (DISSENT (1) + EMERGENCE E2): C² IS distinguished, but on the irrep leg (gauge/gravity/isospin), not the multiplicity leg (generations). So step 1 survives as a true statement about the wrong observable.

**Answer to VDD2-Q3 (which O'Neill A-tensor is BAP1 step 2 invoking?).** Neither of your two options cleanly — there is a third. (a) The genuine submersion O'Neill A-tensor IS base-fiber `g_{μa} = ½F`, Jensen-zero — INV12-W2-1 confirms `||A||²(Jensen) = 0` and Ehresmann-norm `= 0`; you are right that a fiber-internal T4 has no base-fiber off-block component and does not touch THAT object. (b) The pure-cross-sector fiber-internal bracket you found to be a measurement artifact in INV12-W2-1 (nonzero even for the product metric) is indeed an artifact and I am not invoking it. **(c)** What I was actually reaching for is the genuine **off-Jensen fiber-modulus T-tensor** — the "fiber second fundamental form, modulus-gradient" that INV12-W2-1 computes as `||T||² = 7.8125×10⁻⁴` at δ=0.05, turning on at O(δ) along the moduli direction T4 lives in. That object is REAL (not an artifact) and IS deformed by T4 — but it lives on the **a₂ spectral-action moment (irrep leg)**, so it deforms `λ(p,q)` and G_N / n_s, NOT the multiplicity leg. So BAP1 step 2 invoked a real geometric tensor that is impotent on the Yukawa rank for the SAME leg-membership reason as step 3 — it is irrep-leg, the generations are multiplicity-leg. I concede step 2 does not support a T4 lift; I dissent only on the claim that it was an artifact (it is option (c), real-but-leg-misrouted, INV12-W2-1-confirmed).

**Answer to VDD2-Q4 (concession-mapping: would a lift be "Reading A wins" or "a STAGE-3 theorem has a counterexample"?).** I AGREE with your asymmetry. A left-invariant T4 returning `dY_12/dδ|_0 > 1e-3` would NOT be "Reading A wins" — it would be a counterexample to §VII.BL's STAGE-3-PERMANENT multiplicity-scalar theorem (which you cross-verified at Stage-2, audit `0f0c4f65`), and the correct response would be a Stage-2 re-audit, NOT a quiet posterior shift to Reading A. The dual prior must reflect this: a PASS clears a much higher bar than "the panel disagreed." So I converge on your discriminator mapping: **FAIL → 0.95 Track B** (confirms §VII.BL on the C²-coset; rank-1 wall off-ALL-internal-moduli; closes CV-8 Arm-G; hierarchy register-PINNED to external `ε_LX`); **PASS → 0.90 Track A AND a flagged §VII.BL Stage-2 CONTRADICTION** requiring re-examination; **INFO (sub-threshold / regime-marginal) → unchanged, route to higher L_max.** And yes — a FAIL confirms §VII.BL extends to the C²-coset and the hierarchy's home is the external non-LI `ε_LX` (existence-PASS `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`).

**My two sharper follow-ups for R2-B / R3:**

**BAP-R2-Q1 (does the compute still have value, given the register answers it?).** You called the compute a Q3 confirmation with real value (converts "the C²-coset is the surviving open seam" into "closed like every other internal modulus"). I agree, but I want to pin the route-once disposition precisely for R3's Wrap-Up: given that (i) §VII.BL is STAGE-3-PERMANENT, (ii) you cross-verified it, and (iii) the panel has now CONVERGED that T4 is multiplicity-scalar by the same left-invariance argument — **does running S110-CF1-YUK-C2COSET still clear the EVOI bar, or does the panel's convergence itself discharge the "surviving open seam" and make the compute a LOW-priority confirmation that can be DEFERRED** (rather than HIGH-EVOI as the rollup tiered it)? My read: the panel's structural convergence has already collapsed most of the EVOI (the prior is now ~0.90 FAIL with a register-PROVEN mechanism behind it, not a 0.65 coin-flip), so I propose the compute drops from HIGH to MED-LOW and is run as a CONFIRMATION when convenient, not as a live discriminator. Do you concur, or do you hold that a STAGE-3 theorem on a *specifically-named, explicitly-flagged* modulus warrants the explicit numerical witness regardless of the panel's convergence?

**BAP-R2-Q2 (the productive relocation — is T4 the right lever for an a₂-moment / Weinberg study?).** Following EMERGENCE E2: T4 (C²-split) is the DOMINANT directional stiffness and INV12-W2-1 shows it turns on the largest off-Jensen `||T||²`, deforming the a₂ moment. The irrep-leg observables — sin²θ_W = 3L2/(L1+3L2), the gauge-coupling normalization, G_N via a₂ — are exactly what a C²-anisotropic deformation SHOULD move at first order. Is there a register slot or a live question where the C²-coset's irrep-leg dominance is the *right* probe (an off-Jensen Weinberg-angle / gauge-normalization sensitivity study), so that the geometric content of BAP1 steps 1–2 is not discarded but re-homed to the observable it actually governs? I am not proposing a new compute in THIS workshop (the charge is the Yukawa-rank adjudication), but flagging the relocation as a candidate carry-forward so the genuine C²-specialness lands somewhere rather than being lost when we close the Yukawa door.

---

## Round 2 — van-den-dungen: Cross-Synthesis

### CONVERGENCE

**I accept baptista's convergence on Reading B in full, and I accept his correction OF MY OWN argument — his option (c) is the sharper truth and I adopt it.** The substantive change this round is not on the verdict (we both now hold T4 multiplicity-scalar, no rank lift) — it is that baptista has resolved a genuine under-resolution in my VDD1/Re:BAP1: I framed BAP1 steps 1–2 as "irrelevant," and that was too coarse. They are **relevant-but-leg-misrouted**, and the routing is itself a structural result. I concede this and incorporate it. Below: (1) the option-(c) assessment baptista's DISSENT asks me to make, then (2) my answers to BAP-R2-Q1 and BAP-R2-Q2.

**Option (c) is genuine, and it is on the irrep leg, and the quarantine from generations is CLEAN — not a leak.** This is the load-bearing assessment. Baptista's DISSENT (2) + Answer-to-VDD2-Q3 propose a third reading of the O'Neill object I dichotomized in VDD2-Q3: not (a) the pure base-fiber `g_{μa}=½F` (Jensen-zero, T4-untouched) and not (b) the cross-sector fiber-internal bracket I caught as a measurement artifact (nonzero even for the product metric), but (c) the genuine **off-Jensen fiber-modulus T-tensor** — the "fiber second fundamental form, modulus-gradient" that INV12-W2-1 computes as `‖T‖² = 7.8125×10⁻⁴` at δ=0.05, turning on at O(δ) along the T4 direction. I authored INV12-W2-1; I confirm the number and the reading. And I confirm option (c) is correct: **there IS a real first-order geometric object that T4 turns on. My "T4 is just another multiplicity-scalar" was the right verdict via an under-resolved mechanism.** The correct mechanism is: T4 acts, it acts at O(δ), but it acts on the irrep leg `V_{(p,q)}`, and the quarantine is exact.

Here is the cleanliness proof — that the off-Jensen T-tensor canNOT leak to the multiplicity leg — written as the substitution chain (`math-scripts.md §"Double-Check Logic Before Compute"`), because the cleanliness is exactly the question baptista's DISSENT flags and it is where a hand-wave would hide a leak:

```
Step 1: The off-Jensen T-tensor enters the SPECTRAL ACTION via the heat-kernel/Seeley-DeWitt
        coefficient a₂: S_cross/S_base = (‖A‖²+‖T‖²)/|R_K|  (INV12-W2-1 substitution chain,
        Baptista Paper 13 eq 3.4: R_P = R_M + R_K − ‖A‖² − ‖T‖²). This is a STATEMENT ABOUT
        TRACES OF D_K — Tr e^{−tD_K²} — i.e. a spectrum-only functional.

Step 2: a₂ = (5R/12)-weighted heat coefficient is, by Peter-Weyl block-diagonality,
        a₂ = Σ_{(p,q)} m(p,q) · a₂^{(p,q)}[λ(p,q)]  — a MULTIPLICITY-WEIGHTED SUM over the
        irrep-leg spectrum λ(p,q). The multiplicity m(p,q) enters as a SCALAR WEIGHT (a count),
        NOT as an operator on ℂ^{m(p,q)}. The T-tensor reshapes λ(p,q) (the irrep-leg eigenvalues);
        it does NOT act as a d×d matrix on the generation copies.

Step 3: The Yukawa rank observable is Y_ij = ⟨ψ_i|(1j·D_K)|ψ_j⟩ on the generation copies — an
        OPERATOR on ℂ^{m(p,q)}, not a trace. The T-tensor enters D_K only through the frame
        operators ρ_{(p,q)}(e_a) (rescaled by the metric eigenvalues), whose image is
        ⊕ B(V_{(p,q)}) ⊗ 1_{m(p,q)}  (the left-regular leg, ⊗1 on the right-regular leg by the
        Peter-Weyl commutant — VDD1 affirmative core, registry line 21116).

Step 4: The generation-lifting datum lives in the ORTHOGONAL COMPLEMENT of that image:
        ⊕ 1_{V_{(p,q)}} ⊗ M_{m(p,q)}(ℂ)  (registry line 21155, the Morita-invariant module-membership
        statement I cross-verified at the §VII.BL Stage-2). The T-tensor, entering via ρ_{(p,q)}(e_a),
        lands ENTIRELY in ⊕ B(V) ⊗ 1_m — it has ZERO component in the complement.

Conclusion: ‖T‖²(off-Jensen) ≠ 0 is REAL (option c), it moves a₂/G_N/n_s (irrep-leg, Step 2),
        and its projection onto the generation-lifting subspace ⊕ 1_V ⊗ M_m(ℂ) is EXACTLY ZERO
        (Step 4). The quarantine is CLEAN — not "small leak below threshold," but a TENSOR-FACTOR
        DISJOINTNESS: the T-tensor lives in one tensor factor (the operator algebra on V), the
        generation index in the orthogonal one (the operator algebra on ℂ^m). No coupling exists
        to be bounded. This is the SAME disjointness structure as the S98-W1 within-channel
        commutator certification ([Π_{N_Fock}, π_!⊗[D_B]]=0 EXACT by Nambu⊗Fock tensor-factor
        disjointness): grading-disjointness is STRONGER than effacement.
```

So I do NOT merely concede option (c) — I supply its cleanliness proof and I locate it in the register's own algebra-axis orthogonality (the irrep-leg / multiplicity-leg = algebra-INVARIANT / algebra-DEPENDENT split, MANDATORY at K=3). baptista's DISSENT is therefore **a strengthening I accept, not a disagreement I rebut**: "T4 fails like su(2) by an undifferentiated multiplicity-scalar argument" → "T4 has REAL first-order action on the irrep leg (‖T‖²=7.81e-4, INV12-W2-1-confirmed) that is tensor-factor-disjoint from the generation index." The second statement is the better one and it is now our joint position.

**Answer to BAP-R2-Q1 (does S110-CF1-YUK-C2COSET drop HIGH→MED-LOW given the convergence + §VII.BL STAGE-3-PERMANENT?). YES — I concur with the down-tier, with one pinned caveat on what the compute still discharges.** The EVOI logic (per `evoi-prioritization.md`: `EVOI = P(pass)·|ΔP(pass)| + P(fail)·|ΔP(fail)|`):

- **At Round-1 dispatch**, the rollup tiered this HIGH because it read as a ~0.65 coin-flip discriminator (Reading A vs Reading B genuinely open) on "the sole surviving probe of CV-8 Arm-G." A coin-flip on a load-bearing corridor is high-EVOI: both outcomes move the posterior a lot.
- **After the panel's convergence**, the prior is ~0.90 FAIL with a **register-PROVEN mechanism** behind it (the W2 left-invariance commutant argument, §VII.BL STAGE-3-PERMANENT, which I cross-verified at Stage-2 audit `0f0c4f65`), not a 0.65 coin-flip. So `|ΔP|` collapses: a FAIL (P≈0.90) barely moves the posterior (it confirms a proven theorem on a named modulus — small `|ΔP(fail)|`); a PASS (P≈0.10) would be enormous but is exactly the low-prior branch, AND — critically — a PASS does not route to "Reading A wins," it routes to "a STAGE-3-PERMANENT theorem has a counterexample requiring a Stage-2 re-audit" (VDD2-Q4, which baptista accepted). The EVOI mass that survives is almost entirely on the low-prior PASS branch, and that branch's value is REALIZED as a Stage-2-re-audit trigger, not as a discriminator outcome.

So: **the compute drops from HIGH to MED-LOW.** It is no longer a live discriminator; it is a Q3 confirmation of a STAGE-3-PERMANENT theorem on a specifically-named, explicitly-flagged modulus. **My pinned caveat (this is where I do NOT let it drop to LOW/DROPPED):** the compute is still worth running as a CONFIRMATION-WHEN-CONVENIENT, because it converts a register STATUS — "the C²-coset is the surviving open seam" (`_cross-investigation-synthesis.md §2` CLARIFIED-8) — into "the C²-coset is closed like every other internal modulus," which is the `_rollup-yukawa-moduli` CHALLENGED-with-RELOCATION net made complete. That status conversion has real bookkeeping value (it removes a named open seam from the inventory) even though its physics outcome is register-predicted. **MED-LOW, run as a confirming numerical witness when a Yukawa-sector compute wave is already open; NOT a live discriminator gating any downstream decision.** Concretely: it should NOT lead a session (it is no longer a ★ HIGHEST or HIGH item per the `_promotion-triage.md` Bucket-2 tiering), and it can be folded into the same wave as CF-2(Yukawa)/ε_LX without its own dispatch overhead.

**Answer to BAP-R2-Q2 (flag the irrep-leg off-Jensen content as a Weinberg-angle/gauge-normalization CF candidate). YES — I endorse this as a carry-forward candidate, and I sharpen the substrate-IS observable + the bridge so it lands faithfully.** baptista's EMERGENCE E2 relocation is correct and I want it to NOT be lost when we close the Yukawa door. The genuine C²-specialness — `J_C2 = 0.933 M_KK` (4 bonds, dominant) vs `J_su2 = 0.059 M_KK` (3 bonds, canonical_constants.py), and the off-Jensen `‖T‖²=7.81e-4` sourced largely by the C² non-commuting profiles (INV12-W2-1 WP Step 3 line 49, "su(2)+u(1) vs C² non-commuting") — is a REAL first-order lever, and it lives on EXACTLY the leg where sin²θ_W and the gauge normalization live. The substrate-first statement of the relocated question:

> **Substrate-IS observable**: `sin²θ_W(δ_C²) = 3L2/(L1+3L2)` evaluated on the irrep-leg spectrum of `D_K` under the T4 (C²-split) left-invariant deformation `L3·I_4 → diag(...)` at the fold τ-anchor; and `a₂(δ_C²)` (the G_N-feeding Seeley-DeWitt coefficient) under the same deformation. Both are algebra-INVARIANT spectrum-only functionals on `⊕ V_{(p,q)}` — the family the T-tensor DOES move.
> **Why it is the right probe**: T4 is the dominant directional stiffness (4 of the 8 fiber dimensions), so it carries the largest off-Jensen spectral weight; the off-Jensen `‖T‖²` it turns on feeds a₂ DIRECTLY (INV12-W2-1's `|S_cross|/S_base` IS this a₂ remainder). The C²-coset is the RIGHT knob for the gauge/gravity sector precisely as it is the WRONG knob for generations — by the same leg-membership fact, read with opposite sign.

This is a clean carry-forward CANDIDATE (4-field spec to be finalized in R3 Wrap-Up if we keep it). I flag the scope discipline so it is registered correctly: any such gate must carry the **algebra-INVARIANT / FI tagging** (the a₂-ratio is FI per `regulator-pin-discipline.md §"β_shell FI Classification"` parentage) and must be distinguished at plan-freeze from the INV12-W2-1 gate it descends from — INV12-W2-1 tested the a₂ ADDITIVITY LEAK (`|S_cross|/S_base < 1e-2`, PASS); a Weinberg-angle gate would test the a₂-RESPONSE SLOPE `d(sin²θ_W)/dδ_C²|_0`, a different observable on the same irrep-leg spectrum. So it is genuinely new physics (a response derivative, not a leak bound), not a re-run — it clears the `Investigating-Workshops.md` Q1-vs-CF test as a COMPUTE-CF (a pre-registered solo compute with a threshold, NOT an adversarial adjudication; no two readings diverge on it). It belongs in S111's `/rclab-plan` queue, not a workshop schedule.

### DISSENT

**No dissent on the verdict, and none on option (c) — I have ADOPTED it (CONVERGENCE above). The one residual I surface is NEW evidence on a precision point in baptista's DISSENT (1), and it cuts in the safe direction (it makes the wall MORE robust, not less). It is the identification of `m(p,q)`.**

baptista's DISSENT (1) states: *"The generation multiplicity `m(p,q)` is the right-regular copy-count = `dim V_{(p,q)}`, a Peter-Weyl degeneracy orthogonal to the U(2)-branching."* The commutant CONCLUSION that follows (left-invariant ⇒ scalar on that leg) is correct and the verdict stands on it. But the **identification itself is too strong**, and the over-identification, if carried downstream, would mis-aim the ε_LX gate. The precise structure (new evidence, register-anchored):

```
L²(SU(3)) Peter-Weyl:   L²(G) = ⊕_{(p,q)} V_{(p,q)} ⊗ V*_{(p,q)}
  → the RIGHT-REGULAR leg has dimension dim V_{(p,q)} (the FULL copy-count).

Framework H_K (registry line 21110):  H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}
  → m(p,q) is the PHYSICAL multiplicity = how many SM-fermion copies the model
    keeps in sector (p,q), NOT the full dim V_{(p,q)}.

Generation index (registry line 21124):  generations = Z₃-triality t = (p−q) mod 3
  → a 3-VALUED label (proven_384), NOT a dim-V_{(p,q)}-valued one.
```

For the fundamental `(1,0)`: `dim V_{(1,0)} = 3` and the generation count is 3, so they NUMERICALLY coincide and baptista's identification looks exact there. But that coincidence is special to `(1,0)`: `dim V_{(p,q)} = ½(p+1)(q+1)(p+q+2)` is 6, 8, 10, 15, … for higher sectors, while the generation index stays the 3-valued triality `t`. So **"generation index = full right-regular copy-count dim V_{(p,q)}"** is an over-identification — the generation index is a *coarser* invariant (a `mod 3` reduction of the sector labels), and the physical `m(p,q)` is a model-selection datum (which fermion reps are kept), not the bare Peter-Weyl degeneracy.

**Why this is NOT a dissent on the verdict, and why I surface it anyway.** The commutant argument is INDIFFERENT to which sub-identification is correct: a left-invariant deformation is `⊗ 1` on the ENTIRE right-regular leg `V*_{(p,q)}`, hence `⊗ 1` on ANY subspace of it — whether that subspace is the full `dim V_{(p,q)}`, the kept-copies `ℂ^{m(p,q)}`, or the 3-valued triality reduction. So T4 is multiplicity-scalar on the generation index under EITHER identification; the verdict (no rank lift) is robust to the precision point. **It is the safe direction**: the over-identification, if anything, gives the commutant argument MORE to be scalar on, so correcting it cannot rescue a lift. I surface it because it matters for the DOWNSTREAM ε_LX gate (CF-2/Yukawa): the external non-LI deformation that DOES source the hierarchy must act on the correct generation-carrying subspace, and if a future gate inherits "generations = dim V_{(p,q)}" it will target the wrong-dimensional object (the full right-regular leg, dim 3/6/8/… per sector) rather than the 3-valued triality index the hierarchy actually lives on. The register has this right (line 21124, `t = (p−q) mod 3`); baptista's DISSENT (1) prose drifted to the full copy-count; I pin the correction so the convergence text does not propagate the drift.

**Net**: this is a `output-standards.md`-style STRUCTURAL precision (the epistemic TYPE of `m(p,q)` — a 3-valued triality reduction, not a per-sector Peter-Weyl degeneracy), not a NUMERICAL revision and not a verdict dissent. I record it as new evidence so R3's Wrap-Up "What Holds" carries the *register's* identification (`t = (p−q) mod 3`), not the over-strong one.

### EMERGENCE

Three cross-domain insights crystallized this round that neither my Round-1 sections nor baptista's R2 follow-up had:

**E1 (VdD) — The C²-coset closure is the THIRD structurally-distinct instance of one tensor-factor-disjointness theorem, and it lets me close a seam I left open in S98-W1.** Putting baptista's option (c) together with the S98-W1 within-channel commutator certification (my own prior result, MEMORY S98-W1): there is ONE structural mechanism appearing now at THREE distinct loci on `(A_K, H_K, D_K)`:
- **S98-W1**: `[Π_{N_Fock=1}, π_!⊗[D_B]] = 0` EXACT — the shriek class (number-conserving, `⊗1_Fock`) is tensor-factor-disjoint from the Fock-occupation superselection. Channel-γ protected zeros (σ/m=0, c_s²=0) are operator-orthogonal.
- **§VII.BL (Skolem–Noether)**: every `σ∈Aut(A_K)` acts `π_{(p,q)}(u)⊗1_{m(p,q)}` — the algebra automorphisms are tensor-factor-disjoint from the multiplicity leg. The generation-lifting datum lives in the orthogonal complement `⊕ 1_V ⊗ M_m(ℂ)`.
- **This workshop (option c)**: the off-Jensen T-tensor (‖T‖²=7.81e-4) enters via `ρ_{(p,q)}(e_a)`, image `⊕ B(V)⊗1_m` — tensor-factor-disjoint from the generation index. CLEAN quarantine, not a leak.

All three are the same theorem: **a substrate operator that factors through one tensor leg cannot constrain an observable on the orthogonal leg, and disjointness is STRONGER than smallness (it is exactly-zero, L-independent, regulator-invariant — a Level-1 cohomology-class identity, not a Level-2 bound).** This is the registry's algebra-axis orthogonality conjecture (MANDATORY at K=3) realized geometrically. The S98-W1 seam I flagged ("O'Neill moduli deformation TYPE-A trivial-on-Fock untested around τ_fold") is now PARTIALLY closed by option (c): the off-Jensen O'Neill T-tensor IS a moduli deformation, and it is here shown tensor-factor-disjoint from the generation/multiplicity leg by the SAME `ρ⊗1` structure that makes it trivial-on-Fock — the geometric moduli deformation and the Fock superselection are quarantined from the physical index by one and the same disjointness.

**E2 (VdD) — Two opposite-sign leg-membership readings of C²-dominance unify the Yukawa FAIL and the (predicted) Weinberg-angle SENSITIVITY into one statement.** baptista's E2 relocates C²-dominance to the irrep leg; I sharpen it to a SIGNED statement that is the workshop's most compressible takeaway: *the C²-coset is the dominant lever on every irrep-leg observable (a₂/G_N/sin²θ_W/gauge-normalization, where `J_C2=0.933` ≫ `J_su2=0.059` and the off-Jensen `‖T‖²` peaks) and exactly impotent on the one multiplicity-leg observable (the generation ratio R_cross), by ONE leg-membership fact read with opposite signs.* The same dimensional dominance (4 of 8 fiber dimensions) that makes T4 the strongest knob on the spectrum-trace family is what makes it `⊗1` on the operator-on-ℂ^m family. "Dominant on traces of D_K" and "scalar on operators on the generation copies" are not in tension — they are the two faces of left-invariance acting on the two Peter-Weyl legs. This is why CF-1(Yukawa) FAILs and a CF-2-adjacent Weinberg-angle gate would PASS as a sensitivity probe: same modulus, orthogonal legs, opposite verdicts — and BOTH are the substrate IS-ing the observable, neither demoted.

**E3 (VdD) — The "external non-LI to source a multiplicity-leg datum" precondition is the Kasparov-factorization shadow of the two-leg split, and it predicts WHICH observables are intrinsically rigid.** baptista's E3 reads §VII.BL's "Non-LI-Deformation Necessity" (line 21157, K=2, instances #7 Yukawa + #9 baryogenesis) as a leg-membership theorem. I add the NCG-structural reason it MUST be a precondition and not a coincidence: the entire `A_K`-built differential calculus `Ω¹_{D_K}(A_K)` is the image of the Hochschild 1-cochain `[D_K, −]`, which (by the commutant + Skolem–Noether closure) maps INTO `⊕ B(V_{(p,q)})⊗1_{m(p,q)}` — the algebra-INVARIANT (irrep-leg) subalgebra. Any observable living on the orthogonal complement `⊕ 1_V ⊗ M_m(ℂ)` is **outside the range of the entire calculus** — no inner fluctuation `D_K → D_K + A + JAJ⁻¹`, no twisted `Ω¹_σ`, no left-invariant metric retuning can source it. The ONLY way in is an EXTERNAL non-LI connection `ε_LX` that breaks left-invariance on the multiplicity index itself (registry Corollary line 21118, `P_nLI = ‖ε_LX‖² > 0`). This is the Kasparov-factorization shadow: in `[D_M] = π_!⊗[D_B]` the fiber data factors as the shriek class `π_!`, and the multiplicity leg is the part of the fiber Hilbert space the shriek map does NOT see as spectrum — it sees it as the K-theory RANK (module-membership, Morita-invariant, registry line 21155). So the precondition predicts EXACTLY which matter-sector asymmetries are intrinsically rigid: every observable that is a module-RANK datum rather than a spectral-MOMENT datum — generations (#7), baryon asymmetry δA (#9), and (the natural K=2→K=3 third instance baptista names) δ_CP, which is forced real by `[J,D_K]=0` and is, like the others, a multiplicity-leg/module-rank datum the homogeneous fiber cannot encode. The two-leg split is not a bookkeeping convenience; it is the statement that the substrate's spectral calculus and its K-theoretic rank are orthogonal information channels, and the matter-sector hierarchies all live in the second one.

---

## Round 3 — baptista: Follow-up

### CONVERGENCE

**Final position: Reading B, full and stable. The rank-1 Yukawa wall is genuinely off-Jensen for ALL left-invariant internal transverse moduli — not only su(2) — and the C²-coset (T4) is one more direction inside the same generation-blind calculus.** I converged on this in R2 and nothing in van-den-dungen's R2 cross-synthesis reopens it; his R2 strengthened my own concession and I adopt the strengthened form as the joint position. Stated once, cleanly, as the workshop's settled core:

**The verdict and its single load-bearing reason.** `Y_ij = ⟨ψ_i|(1j·D_K)|ψ_j⟩` on the same-sign degenerate generation block is an operator on the **multiplicity leg** `ℂ^{m(p,q)}` of `H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}` (registry §VII.BL line 21110). Any left-invariant metric — Jensen, T3 (su(2)-split), T4 (C²-split), every point of the 28-parameter left-invariant family — builds its frame operators `ρ_{(p,q)}(e_a)` on the **left-regular irrep leg** `V_{(p,q)}` and acts `⊗ 1_{m(p,q)}` on the multiplicity leg, because the multiplicity leg is the COMMUTANT of the left-regular action (it carries the right-regular action; left-invariant vector fields commute with right-translations). Therefore `Y_ij = λ(p,q)·δ_{ij}` — rank 1, off-diagonals EXACTLY zero — for T4 exactly as for T3. The W2 homogeneity wall (registry line 21116, *"`D_K` left-invariant ⇒ commutes with the right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`"*) invokes left-invariance ALONE; Ad(SU(2))-equivariance is a statement about the irrep leg and is orthogonal to the conclusion.

**My BAP1 step 3 is withdrawn (final).** Its hidden premise — that the generation index sits in the C²-coset's Ad-representation content on `V_{(p,q)}` — routed the argument to the wrong tensor leg. The clincher remains the empirical hammer: T3 DOES break Ad(SU(2)) (splits the **3** into 1⊕2) and returned `dY_12/dδ|_0 = 1.943e-15`, `gen_degen_lift=False` (INV2-W1-1, audit `1481b775…`) — 9 OOM below the lift floor. Breaking equivariance on the irrep leg lifted the *isospin* branching (22→30 signed eigenvalues, SU(2)→U(1)) and left generation flat. That is the whole story.

**Option (c) is FINALIZED, and I accept van-den-dungen's R2 cleanliness proof as its closure.** My R2 DISSENT introduced the third reading of my BAP1 step-2 O'Neill object: not (a) the genuine base-fiber submersion A-tensor `g_{μa}=½F` (Jensen-zero, T4-untouched — INV12-W2-1 confirms `‖A‖²(Jensen)=0`, Ehresmann-norm `=0`), and not (b) the cross-sector fiber-internal bracket van-den-dungen caught as a measurement artifact (nonzero even for the pure product metric), but **(c) the genuine off-Jensen fiber-modulus T-tensor** — the "fiber second fundamental form, modulus-gradient," `‖T‖² = 7.8125×10⁻⁴` at δ=0.05 (INV12-W2-1, the gate van-den-dungen authored). van-den-dungen's R2 supplied the cleanliness proof I had left implicit: the T-tensor enters `D_K` ONLY through `ρ_{(p,q)}(e_a)`, image `⊕ B(V_{(p,q)}) ⊗ 1_{m(p,q)}`; the generation-lifting datum lives in the ORTHOGONAL complement `⊕ 1_{V_{(p,q)}} ⊗ M_{m(p,q)}(ℂ)` (registry line 21155); the T-tensor's projection onto that complement is EXACTLY ZERO. **The quarantine is tensor-factor DISJOINTNESS, not small-leak-below-threshold** — there is no coupling to bound, because the two objects live in orthogonal tensor factors. I accept this in full: option (c) is real (the T-tensor turns on off-Jensen), it moves a₂/G_N/n_s (irrep leg), and it is impotent on the Yukawa rank by tensor-factor disjointness. **Option-(c) re-homing status: CLOSED. The geometry of BAP1 steps 1–2 is real and relocated to the irrep leg; it is not discarded and not a verdict-dissent.**

**The dual prior.** I converge on van-den-dungen's correction of my symmetric R1 elicitation. The compute S110-CF1-YUK-C2COSET is pre-registered at **~0.90 FAIL / ~0.10 PASS** — matching the INV2-W1-1 0.65→0.90 Track-B reallocation precedent — NOT my R1 symmetric 0.6–0.7, because Reading A's central mechanism (Ad-breaking ⇒ lift) was already falsified by its closest realized test (T3). It is a Q3 confirmation of a STAGE-3-PERMANENT theorem on a specifically-named modulus, not a Q1 discovery.

### DISSENT

**No verdict dissent. No dissent on option (c) — van-den-dungen's R2 cleanliness proof is correct and I have adopted it (CONVERGENCE above).** I have exactly one residual, and it is NEW evidence raised by van-den-dungen's R2 DISSENT that I had not addressed and that I now ACCEPT and sharpen — so it resolves rather than persists. It is the identification of `m(p,q)`.

**I accept van-den-dungen's R2 correction of my DISSENT (1) `m(p,q)` over-identification, and I add the geometric reason it cuts in the safe direction.** My R2 DISSENT (1) wrote the generation multiplicity as *"the right-regular copy-count = `dim V_{(p,q)}`."* van-den-dungen's R2 DISSENT pinned this as too strong: the generation index is the 3-valued Z₃-triality `t = (p−q) mod 3` (registry line 21124, `proven_384`), a `mod 3` REDUCTION of the sector labels — NOT the per-sector Peter-Weyl degeneracy `dim V_{(p,q)} = ½(p+1)(q+1)(p+q+2)` (which is 3, 6, 8, 10, 15, … and coincides with the generation count ONLY at the fundamental `(1,0)`, where both equal 3). The physical `m(p,q)` is a model-selection datum (which SM-fermion reps the model keeps), distinct again from both. I accept this fully. The NEW structural point I add, which closes the loop: **the commutant argument is INDIFFERENT to which of the three identifications is correct, and that indifference is itself the deepest form of the result.** A left-invariant deformation acts `⊗ 1` on the ENTIRE right-regular leg `V*_{(p,q)}`, hence `⊗ 1` on ANY subspace of it — the full `dim V_{(p,q)}`, the kept-copies `ℂ^{m(p,q)}`, OR the 3-valued triality reduction. So T4 is multiplicity-scalar on the generation index under EVERY identification simultaneously; the over-identification, far from rescuing a lift, gives the commutant MORE to be scalar on. This is the **safe direction**, and it is geometrically forced: the right-regular leg is a single irreducible block under the right-translation action that the left-invariant frame operators commute with — there is no left-invariant sub-structure of it for any deformation to resolve. **I had the verdict-robust conclusion (no lift) but the over-strong sub-identification; the correction does not touch the verdict, and the register's `t = (p−q) mod 3` is what R3's "What Holds" must carry** (so the downstream ε_LX gate, CF-2/Yukawa, targets the 3-valued triality index the hierarchy actually lives on, not a per-sector dim-V object). This is a STRUCTURAL precision on the epistemic TYPE of `m(p,q)` (a coarse triality reduction, not a per-sector degeneracy), in the sense of `output-standards.md §"Numerical vs Structural"` — not a numerical revision, not a verdict change. **Residual resolved, not carried.**

### EMERGENCE

Three final cross-domain insights, building on the R2 convergence into the workshop's most compressible structural lessons.

**E1 (final) — The irrep-leg / multiplicity-leg orthogonality IS the unifying "why," and it makes the C²-coset closure the third instance of ONE tensor-factor-disjointness theorem.** The workshop's deepest takeaway is not "T4 fails like su(2)" — it is that the generation index is protected by **algebra-axis orthogonality** (registry's algebra-INVARIANT vs algebra-DEPENDENT conjecture, MANDATORY at K=3), realized geometrically as the Peter-Weyl two-leg split. There are two orthogonal observable families on `(A_K, H_K, D_K)`: algebra-INVARIANT spectrum-only functionals (act on the irrep leg `V_{(p,q)}`: a₂/a₄, sin²θ_W, gauge normalization, isospin branching) and algebra-DEPENDENT state-pair functionals (act on the multiplicity leg: `R_cross`, the generation ratio). Every left-invariant frame operator acts on the FIRST and is `⊗1` on the SECOND. Putting my option (c) together with van-den-dungen's R2 E1, ONE structural mechanism now appears at THREE distinct loci: (i) S98-W1 `[Π_{N_Fock=1}, π_!⊗[D_B]]=0` (shriek class disjoint from Fock superselection); (ii) §VII.BL Skolem–Noether `σ(a)=π_{(p,q)}(u)⊗1_{m(p,q)}` (automorphisms disjoint from the multiplicity leg); (iii) this workshop's option (c), the off-Jensen T-tensor `⊕ B(V)⊗1_m` disjoint from the generation index. All three are the same theorem: **a substrate operator factoring through one tensor leg cannot constrain an observable on the orthogonal leg, and disjointness is STRONGER than smallness — it is exactly-zero, L-independent, regulator-invariant: a Level-1 cohomology-class identity, not a Level-2 envelope bound.** The C²-coset closure converts the registry's K=3-MANDATORY orthogonality conjecture from an abstract axis into a geometrically-witnessed instance on a named modulus.

**E2 (final) — The structural lesson is the SIGNED leg-membership reading of C²-dominance: dominant on every irrep-leg observable, exactly impotent on the one multiplicity-leg observable, by ONE fact read with opposite signs.** This is the most durable output of the cross-pollination and it is where my Reading-A geometry was RIGHT, merely aimed at the wrong observable. The C²-coset is the dominant directional stiffness (`J_C2 = 0.933 M_KK`, 4 of 8 fiber dimensions, vs `J_su2 = 0.059 M_KK`; canonical_constants.py:732) and turns on the largest off-Jensen `‖T‖²` (INV12-W2-1, sourced largely by the "su(2)+u(1) vs C² non-commuting" profiles). That dominance is an **irrep-leg** fact: it makes T4 the STRONGEST knob on a₂/G_N/sin²θ_W/gauge-normalization. The SAME left-invariance that puts that dominance on the irrep leg makes T4 `⊗1` on the multiplicity leg — exactly impotent on generations. "Dominant on traces of `D_K`" and "scalar on operators on the generation copies" are not in tension: they are the two faces of left-invariance acting on the two Peter-Weyl legs. **Hence the Weinberg-angle / a₂-response CF candidate is not a consolation prize — it is the geometrically-correct home for the C²-specialness I mis-routed**, and it is a different observable (a response SLOPE `d(sin²θ_W)/dδ_C²|_0`, not INV12-W2-1's additivity-leak bound). The substrate IS-es both observables; neither is demoted; the modulus is one, the legs are orthogonal, the verdicts are opposite.

**E3 (final) — "External non-LI to source a multiplicity-leg datum" is a leg-membership / module-rank theorem that PREDICTS which matter-sector asymmetries are intrinsically rigid — and δ_CP is the natural K=2→K=3 third instance.** Reading §VII.BL's "Non-LI-Deformation Necessity" precondition (registry line 21157, SUGGESTION K=2, instances #7 Yukawa + #9 baryon asymmetry) through the two-leg lens: the entire `A_K`-built differential calculus `Ω¹_{D_K}(A_K)` is the image of the Hochschild 1-cochain `[D_K,−]`, which by the commutant + Skolem–Noether closure maps INTO the algebra-INVARIANT (irrep-leg) subalgebra `⊕ B(V_{(p,q)})⊗1_{m(p,q)}`. Any observable living on the orthogonal complement `⊕ 1_V ⊗ M_m(ℂ)` is OUTSIDE the range of the entire calculus — no inner fluctuation, no twisted `Ω¹_σ`, no opposite-action `JAJ⁻¹`, and (the workshop's contribution) **no left-invariant metric retuning, T4 included**, can source it. The ONLY way in is an external non-LI connection `ε_LX` breaking left-invariance on the multiplicity index (registry Corollary line 21118, `P_nLI = ‖ε_LX‖²>0`; existence-PASS `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`). In van-den-dungen's R2 framing this is the Kasparov-factorization shadow: in `[D_M]=π_!⊗[D_B]` the multiplicity leg is the part of the fiber Hilbert space the shriek map sees as K-theory RANK (Morita-invariant module-membership), not as spectrum. The precondition therefore PREDICTS exactly which asymmetries are intrinsically rigid: every module-RANK datum (not spectral-MOMENT datum) — generations (#7), baryon asymmetry δA (#9), and **δ_CP, forced real by `[J,D_K]=0`** (the CPT decomposition is a permanent result; MEMORY permanent-results J/γ₉/CPT), a multiplicity-leg/module-rank datum the homogeneous fiber cannot encode. The C²-coset closure sharpens the K=2→K=3 promotion candidate: T4 was the last untested internal geometric probe, and its closure confirms the *whole left-invariant geometric family* sits inside the generation-blind calculus, making δ_CP the clean third instance.

### QUESTIONS

This is my CLOSING POSITION for van-den-dungen to tabulate in the Verdict + Wrap-Up. I pose no new adversarial questions — the panel has CONVERGED. I state the four deliverables the spawn charge requires.

**1. The structural verdict (for Verdict-table rows 1 + 2).** The rank-1 Yukawa spectral functional has **rank locked at 1 under coset deformation for ALL left-invariant internal transverse moduli** (the full Milnor complement, not only su(2)), because the rank is a multiplicity-leg observable and every left-invariant deformation acts `⊗1` on the multiplicity leg by the W2 homogeneity wall (left-invariance ALONE — NOT Ad-equivariance, which lives on the orthogonal irrep leg). §VII.BL Generation-Blindness extends from {U(2)-slice + su(2)-direction} to {every left-invariant internal direction}, across the window [0.15, 0.25]. **CV-8 Arm-G (the internal geometric hierarchy corridor) is DEAD; the hierarchy's home is the external non-LI `ε_LX` connection, register-PINNED** (existence-PASS `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`). NCG cross-check (row 2): the W2 multiplicity-scalar theorem extends to the C²-coset by the identical left-regular/right-regular commutant argument van-den-dungen cross-verified at the §VII.BL Stage-2 (audit `0f0c4f65`); T4, being a left-invariant block-diagonal deformation (BLOCK-DIAG-GENERAL-61), is inside the class the STAGE-3-PERMANENT theorem already covers. **Status: CONVERGED on Reading B.**

**2. Route-once declaration (for Verdict-table row 3).** The **PANEL route was taken** (this workshop executed; no `.py` compute run in-workshop per the spawn rule). Per the route-once rule, the panel's verdict PRE-REGISTERS the dual prior for the downstream compute S110-CF1-YUK-C2COSET; the panel does NOT fire the compute. **Status: CONVERGED — panel verdict supplied; compute pre-registered, not run.**

**3. Dual prior for S110-CF1-YUK-C2COSET (pre-registered, identical to INV2-W1-1's form, su(2)→C² block swap).**
- **Gate (pre-registered):** build the T4 C²-coset anisotropy modulus (split `L3·I_4 → diag` on `C2_IDX=[3,4,5,6]`, `J_C2=0.9330 M_KK`, transverse to U(2)) via the `inv2_w1_off_u2_dirac_yukawa.py` machinery (swap the split block su(2)→C²); re-run the off-U(2) Dirac + Yukawa-overlap on the d=2 generation multiplet.
- **Criterion (pre-registered):** `|dY_12/dδ|_0 > eps_lift = 1e-3` AND `rank(Y_ij) 1→≥2` for δ ∈ (0, 0.20].
- **Prior: ~0.90 FAIL / ~0.10 PASS.**
- **Discriminator mapping (pre-registered, no post-hoc re-narrativization):** **FAIL → 0.95 Track B** (confirms §VII.BL multiplicity-scalar on the C²-coset; rank-1 wall off-ALL-internal-moduli; closes CV-8 Arm-G; hierarchy PINNED to external `ε_LX`). **PASS → 0.90 Track A AND a flagged §VII.BL Stage-2 CONTRADICTION** — a left-invariant T4 lift would be a counterexample to a STAGE-3-PERMANENT theorem, requiring a Stage-2 re-audit, NOT a quiet shift to "Reading A wins" (a much higher evidentiary bar; VDD2-Q4, accepted). **INFO (sub-threshold lift or regime-marginal) → unchanged, route to higher L_max.**

**4. EVOI re-tier (for the Wrap-Up carry-forward block).** I CONCUR with van-den-dungen's R2 down-tier: **S110-CF1-YUK-C2COSET drops HIGH → MED-LOW.** At R1 dispatch the rollup tiered it HIGH as a ~0.65 coin-flip discriminator on the sole surviving probe of CV-8 Arm-G. The panel's structural convergence collapsed the EVOI: the prior is now ~0.90 FAIL with a register-PROVEN mechanism behind it (`EVOI = P(pass)·|ΔP(pass)| + P(fail)·|ΔP(fail)|` — a FAIL barely moves the posterior, confirming a proven theorem; the surviving EVOI mass sits on the low-prior PASS branch, whose value is REALIZED as a Stage-2-re-audit trigger, not a discriminator outcome). **It is no longer a live discriminator; it is a confirmation-when-convenient numerical witness** that converts a register status ("the C²-coset is the surviving open seam," `_cross-investigation-synthesis.md §2` CLARIFIED-8) into "closed like every other internal modulus" (the `_rollup-yukawa-moduli` CHALLENGED-with-RELOCATION net made complete). It should NOT lead a session and can fold into the same wave as CF-2(Yukawa)/ε_LX without its own dispatch overhead. It clears the EVOI bar as a MED-LOW bookkeeping-closure compute, not as a HIGH live gate.

**Carry-forward candidate I hand to van-den-dungen for the Wrap-Up MATH block (the productive relocation, BAP-R2-Q2, which van-den-dungen endorsed and sharpened in R2):** the C²-coset's genuine irrep-leg dominance is the RIGHT lever for an off-Jensen **Weinberg-angle / a₂-response sensitivity study** — substrate-IS observable `sin²θ_W(δ_C²) = 3L2/(L1+3L2)` and `a₂(δ_C²)` under the T4 deformation at the fold anchor, testing the response SLOPE `d(sin²θ_W)/dδ_C²|_0` (a NEW observable, distinct from INV12-W2-1's a₂-additivity-leak bound; must carry algebra-INVARIANT / FI tagging per `regulator-pin-discipline.md §"β_shell FI Classification"`). It clears the `Investigating-Workshops.md` Q1-vs-CF test as a COMPUTE-CF (pre-registered solo with a threshold, no two-reading divergence), and belongs in S111's `/rclab-plan` queue — so the genuine C²-specialness lands on the observable it actually governs rather than being lost when the Yukawa door closes.

---

## Round 3 — van-den-dungen: Cross-Synthesis (FINAL — fills Verdict + Wrap-Up)

### CONVERGENCE

**Final position, stated once: the panel has CONVERGED on Reading B, and I close it.** The rank-1 Yukawa spectral functional has rank locked at 1 under coset deformation for **ALL left-invariant internal transverse moduli** — the full Milnor complement of the U(2) stabilizer, not only the su(2) direction INV2-W1-1 tested. baptista converged on this in R2 and finalized it in R3; I had it from VDD1; we agree without residual on the verdict. There is nothing left to adjudicate — baptista posed no new adversarial question in R3, and his four closing deliverables (structural verdict, route-once, dual prior, EVOI re-tier) are precisely what I tabulate below. I record the convergence in its single load-bearing form, the form that survives consolidation:

**The one-sentence theorem.** `Y_ij = ⟨ψ_i | (1j·D_K) | ψ_j⟩` on the same-sign degenerate generation block is an OPERATOR on the multiplicity leg `ℂ^{m(p,q)}` of `H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}` (registry §VII.BL line 21110); any left-invariant metric — Jensen, T3 (su(2)-split), T4 (C²-split), every point of the 28-parameter left-invariant family — builds its frame operators `ρ_{(p,q)}(e_a)` on the left-regular irrep leg `V_{(p,q)}` and acts `⊗ 1_{m(p,q)}` on the multiplicity leg, because the multiplicity leg is the COMMUTANT of the left-regular action (Peter-Weyl: it carries the right-regular action, and left-invariant vector fields commute with right-translations). Therefore `Y_ij = λ(p,q)·δ_{ij}` — rank 1, off-diagonals EXACTLY zero — for T4 exactly as for T3. The W2 homogeneity wall (registry line 21116, verbatim: *"`D_K` left-invariant ⇒ commutes with the right-translation generators ⇒ `π` acts on the left-regular factor only ⇒ scalar on `ℂ^{m(p,q)}`"*) invokes **left-invariance ALONE** — Ad(SU(2))-equivariance enters nowhere, because it is a statement about the orthogonal irrep leg.

**This is the NCG cross-check the spawn charge names me the authority on, and the register already supplied it.** §VII.BL is **STAGE-3-PERMANENT** (promoted S99 W3-1; Stage-2 cross-axis PASS-AND of JOINT clauses (d),(e), audit `0f0c4f65`), and I — van-den-dungen-bridge-theorist — was the **axis-A NCG cross-reviewer on that Stage-2 verify** (registry line 21104, dispatched without the originating workshop transcript). The mechanism I PASS-AND'd IS the left-regular/right-regular commutant argument. T4, being a left-invariant block-diagonal deformation (my own BLOCK-DIAG-GENERAL-61 / S61 result — compact G + left-invariant metric ⇒ Schur-forced block-diagonality), is INSIDE the class "left-invariant geometric deformation" the STAGE-3-PERMANENT theorem already covers (registry line 21110 explicitly names "the SU(3)-equivariant (Casimir-function τ) geometric deformation"). **So this is a Q3 confirmation of a proven scope, NOT a Q1 discovery — the geometry never needed adjudicating; the register supplied the verdict and the panel re-derived it from first principles.**

**I adopt baptista's option (c) as the joint position, and I stand on the cleanliness proof I supplied in R2.** baptista's R2 DISSENT resolved a genuine under-resolution in my VDD1: I had framed BAP1 steps 1–2 as "irrelevant." They are not irrelevant — they are **relevant-but-leg-misrouted**. There is a REAL first-order geometric object that T4 turns on (option (c), the off-Jensen fiber-modulus T-tensor, `‖T‖² = 7.8125×10⁻⁴` at δ=0.05, INV12-W2-1, the gate I authored), but it enters `D_K` only through `ρ_{(p,q)}(e_a)`, image `⊕ B(V_{(p,q)}) ⊗ 1_{m(p,q)}`; the generation-lifting datum lives in the ORTHOGONAL complement `⊕ 1_{V_{(p,q)}} ⊗ M_{m(p,q)}(ℂ)` (registry line 21155); the T-tensor's projection onto that complement is EXACTLY ZERO. The quarantine is **tensor-factor DISJOINTNESS, not small-leak-below-threshold** — there is no coupling to bound. baptista accepted this in full in R3. Option-(c) re-homing status: **CLOSED**; the geometry of BAP1 steps 1–2 is real, relocated to the irrep leg, and is neither discarded nor a verdict-dissent.

**The dual prior, joint and final.** S110-CF1-YUK-C2COSET pre-registered at **~0.90 FAIL / ~0.10 PASS** (matching the INV2-W1-1 0.65→0.90 Track-B reallocation precedent), NOT baptista's R1-symmetric 0.6–0.7, because Reading A's central mechanism (Ad-breaking ⇒ rank lift) was already falsified by its closest realized test (T3, `dY_12/dδ|_0 = 1.943e-15`, 9 OOM below floor). The discriminator mapping and EVOI re-tier are tabulated in the Verdict + Wrap-Up below.

### DISSENT

**No verdict dissent. No dissent on option (c) — it is the joint position. No new dissent of my own.** The single residual that crossed R2/R3 — the `m(p,q)` identification — is RESOLVED, not carried, and I record the resolution so the convergence text propagates the register's identification, not the over-strong one.

**The `m(p,q)` precision, resolved (mine in R2-DISSENT, accepted by baptista in R3).** My R2 DISSENT flagged baptista's R2 prose "the generation multiplicity `m(p,q)` is the right-regular copy-count = `dim V_{(p,q)}`" as too strong. The register's identification (line 21124, `proven_384`) is the 3-valued Z₃-triality `t = (p−q) mod 3` — a `mod 3` REDUCTION of the sector labels, NOT the per-sector Peter-Weyl degeneracy `dim V_{(p,q)} = ½(p+1)(q+1)(p+q+2)` (which is 3, 6, 8, 10, 15, … and coincides with the generation count ONLY at the fundamental `(1,0)`, where both equal 3). baptista accepted this in R3 and added the closing geometric point I endorse: **the commutant argument is INDIFFERENT to which of the three identifications (full `dim V_{(p,q)}`, kept-copies `ℂ^{m(p,q)}`, or the triality reduction) is correct** — a left-invariant deformation acts `⊗ 1` on the ENTIRE right-regular leg, hence `⊗ 1` on ANY subspace of it. So T4 is multiplicity-scalar on the generation index under EVERY identification simultaneously; the over-identification, far from rescuing a lift, gives the commutant MORE to be scalar on. **This is the safe direction**, and it is a STRUCTURAL precision on the epistemic TYPE of `m(p,q)` (a coarse triality reduction, not a per-sector degeneracy; `output-standards.md §"Numerical vs Structural"`), not a numerical revision and not a verdict change. It matters downstream: the ε_LX gate (CF-2/Yukawa) must target the 3-valued triality index the hierarchy actually lives on, not a per-sector dim-V object. **R3 "What Holds" carries the register's `t = (p−q) mod 3`. Residual resolved.**

**One forward-honesty caveat I pin (not a dissent — a scope boundary on what the panel did NOT establish).** The panel converged on the LEFT-INVARIANT C²-coset modulus — the object the compute tests by construction (the `inv2_w1_off_u2_dirac_yukawa.py` machinery with the split block swapped su(2)→C², a block-diagonal left-invariant deformation). The theorem is airtight on that class. What the panel did NOT — and structurally CANNOT — close is a C²-coset deformation that is NOT left-invariant: a `τ(x)`-field gradient over the base M⁴, or an Ehresmann-connection profile varying over the base. But (VDD1's bounded caveat, which I reaffirm) such an object is no longer "the C²-coset modulus" — it IS `ε_LX`, the external non-LI connection the register already names as the hierarchy's home (Corollary line 21118, `P_nLI = ‖ε_LX‖² > 0`). So the escape route lands ON Reading B's conclusion, not against it: the lift requires going external (non-LI), and "C²-coset T4 as a left-invariant internal modulus" is not that. This boundary is the substrate-first content of the whole convergence — see EMERGENCE E3.

### EMERGENCE

Three insights crystallized at the closing turn — building on baptista's R3 E1–E3 (which I co-authored the R2 seeds of), stated as the workshop's most compressible and most transportable structural lessons.

**E1 (final, joint) — The C²-coset closure is the THIRD geometrically-witnessed instance of ONE tensor-factor-disjointness theorem, and it converts the registry's K=3-MANDATORY algebra-axis orthogonality conjecture from an abstract axis into a named-modulus instance.** The unifying "why" behind both the su(2) FAIL and the predicted C² FAIL is NOT "T4 is just another multiplicity-scalar" — it is **algebra-axis orthogonality** (`cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`, MANDATORY at K=3), realized geometrically as the Peter-Weyl two-leg split. Two orthogonal observable families live on `(A_K, H_K, D_K)`: algebra-INVARIANT spectrum-only functionals (act on the irrep leg `V_{(p,q)}`: a₂/a₄, sin²θ_W, gauge normalization, isospin branching) and algebra-DEPENDENT state-pair functionals (act on the multiplicity leg: `R_cross`, the generation ratio). Every left-invariant frame operator acts on the FIRST and is `⊗1` on the SECOND. Putting baptista's option (c) together with my own prior results, ONE structural mechanism now appears at THREE distinct loci on the same triple:
- **S98-W1** (mack×VdD, my MEMORY): `[Π_{N_Fock=1}, π_!⊗[D_B]] = 0` EXACT — the shriek class (number-conserving, `⊗1_Fock`) is tensor-factor-disjoint from the Fock-occupation superselection.
- **§VII.BL Skolem–Noether** (line 21120, which I cross-verified at Stage-2): every `σ ∈ Aut(A_K)` acts `π_{(p,q)}(u) ⊗ 1_{m(p,q)}` — the algebra automorphisms are tensor-factor-disjoint from the multiplicity leg.
- **This workshop, option (c)**: the off-Jensen T-tensor (`‖T‖²=7.81e-4`) enters via `ρ_{(p,q)}(e_a)`, image `⊕ B(V)⊗1_m` — tensor-factor-disjoint from the generation index.

All three are the **same theorem**: *a substrate operator that factors through one tensor leg cannot constrain an observable on the orthogonal leg, and disjointness is STRONGER than smallness — it is exactly-zero, L-independent, regulator-invariant: a Level-1 cohomology-class identity, not a Level-2 envelope bound.* This is the Kasparov-factorization reading of the whole result: in `[D_M] = π_!⊗[D_B]`, the multiplicity leg is the part of the fiber Hilbert space the shriek map `π_!` sees as **K-theory RANK** (Morita-invariant module-membership, registry line 21155), NOT as spectrum. A left-invariant metric only moves spectrum; the generation index is a rank datum; the two are orthogonal information channels by construction. The C²-coset closure PARTIALLY closes the S98-W1 seam I flagged ("O'Neill moduli deformation TYPE-A trivial-on-Fock untested around τ_fold"): the off-Jensen O'Neill T-tensor IS a moduli deformation, and it is here shown tensor-factor-disjoint from the generation/multiplicity leg by the SAME `ρ⊗1` structure that makes it trivial-on-Fock.

**E2 (final, joint) — The durable lesson is the SIGNED leg-membership reading of C²-dominance: dominant on every irrep-leg observable, exactly impotent on the one multiplicity-leg observable, by ONE fact read with opposite signs.** This is where Reading A's geometry was RIGHT — merely aimed at the wrong observable, a productive relocation rather than a dead end. The C²-coset is the dominant directional stiffness (`J_C2 = 0.933 M_KK`, 4 of the 8 fiber dimensions, vs `J_su2 = 0.059 M_KK`; canonical_constants.py:732-733) and turns on the largest off-Jensen `‖T‖²` (INV12-W2-1, sourced largely by the "su(2)+u(1) vs C² non-commuting" profiles). That dominance is an **irrep-leg** fact: it makes T4 the STRONGEST knob on a₂/G_N/sin²θ_W/gauge-normalization. The SAME left-invariance that puts that dominance on the irrep leg makes T4 `⊗1` on the multiplicity leg — exactly impotent on generations. "Dominant on traces of `D_K`" and "scalar on operators on the generation copies" are not in tension: they are the two faces of left-invariance acting on the two Peter-Weyl legs. **Hence the Weinberg-angle / a₂-response CF candidate is not a consolation prize — it is the geometrically-correct home for the C²-specialness Reading A mis-routed**, and it is a different observable (a response SLOPE `d(sin²θ_W)/dδ_C²|_0`, NOT INV12-W2-1's a₂-additivity-leak bound). The substrate IS-es both observables; neither is demoted; the modulus is one, the legs are orthogonal, the verdicts are opposite.

**E3 (final, joint) — "External non-LI to source a multiplicity-leg datum" is a module-rank theorem that PREDICTS which matter-sector asymmetries are intrinsically rigid; the C²-coset closure sharpens δ_CP as the K=2→K=3 third instance.** Reading §VII.BL's "Non-LI-Deformation Necessity" precondition (line 21157, SUGGESTION K=2, instances #7 Yukawa + #9 baryon asymmetry) through the two-leg lens: the entire `A_K`-built differential calculus `Ω¹_{D_K}(A_K)` is the image of the Hochschild 1-cochain `[D_K,−]`, which by the commutant + Skolem–Noether closure maps INTO the algebra-INVARIANT (irrep-leg) subalgebra `⊕ B(V_{(p,q)})⊗1_{m(p,q)}` (registry line 21155). Any observable on the orthogonal complement `⊕ 1_V ⊗ M_m(ℂ)` is OUTSIDE the range of the ENTIRE calculus — no inner fluctuation, no twisted `Ω¹_σ`, no opposite-action `JAJ⁻¹`, and (the workshop's contribution) **no left-invariant metric retuning, T4 included**, can source it. The ONLY way in is an external non-LI connection `ε_LX` breaking left-invariance on the multiplicity index (Corollary line 21118; existence-PASS `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`). This is the **Kasparov-factorization shadow**: the shriek map sees the multiplicity leg as K-theory rank, not spectrum, so any rank datum is invisible to the spectral calculus. The precondition therefore PREDICTS exactly which asymmetries are intrinsically rigid — every **module-RANK datum** (not spectral-MOMENT datum): generations (#7), baryon asymmetry δA (#9), and **δ_CP, forced real by `[J,D_K]=0`** (the CPT decomposition is a permanent result; MEMORY permanent-results J/γ₉/CPT), a multiplicity-leg/module-rank datum the homogeneous fiber cannot encode. The C²-coset closure confirms the *whole left-invariant geometric family* sits inside the generation-blind calculus — T4 was the last untested internal geometric probe — making δ_CP the clean structurally-distinct third instance for the K=2→K=3 promotion (it must LAND, not be asserted).

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | Rank-1 wall off-ALL-moduli vs off-su(2)-only | BAP1, VDD1 | **Converged** (Reading B) | The rank-1 Yukawa wall is genuinely off-Jensen for ALL left-invariant internal transverse moduli (full Milnor complement), not only su(2). `Y_ij` is an operator on the multiplicity leg `ℂ^{m(p,q)}`; every left-invariant metric (Jensen/T3/T4/all 28 params) acts `⊗1` on that leg by the Peter-Weyl commutant. §VII.BL Generation-Blindness extends from {U(2)-slice + su(2)} to {every left-invariant internal direction} across [0.15,0.25]. CV-8 Arm-G is DEAD; hierarchy register-PINNED to external `ε_LX`. The empirical clincher: T3 (which DOES break Ad(SU(2))) returned `dY_12/dδ\|_0 = 1.943e-15`, 9 OOM below floor — falsifying the equivariance-dependence mechanism. |
| 2 | W2 homogeneity theorem extension to C²-coset (NCG cross-check) | VDD1, Re:BAP1 | **Converged** | The W2 hypothesis is **left-invariance ALONE** (registry line 21116) — Ad(SU(2))-equivariance enters NO step (it acts on the irrep leg `V_{(p,q)}`, orthogonal to the multiplicity-leg conclusion). T4 is a left-invariant block-diagonal deformation (BLOCK-DIAG-GENERAL-61/S61), INSIDE the class the STAGE-3-PERMANENT theorem (Stage-2 audit `0f0c4f65`, vdd axis-A reviewer) already covers. Q3 confirmation of proven scope, NOT Q1 discovery. baptista's BAP1 step 3 WITHDRAWN; option (c) (genuine off-Jensen T-tensor, `‖T‖²=7.81e-4`) is real but tensor-factor-disjoint from generations — CLEAN quarantine (exactly-zero projection onto `⊕ 1_V ⊗ M_m(ℂ)`), not a bounded leak. |
| 3 | Route-once (panel verdict vs compute S110-CF1-YUK-C2COSET) | BAP2 | **Converged** | PANEL route taken (workshop executed; no `.py` compute run in-workshop per spawn rule). Panel verdict PRE-REGISTERS the dual prior **~0.90 FAIL / ~0.10 PASS**. Discriminator mapping (no post-hoc re-narrativization): FAIL → 0.95 Track B (confirms §VII.BL on C²-coset; closes CV-8 Arm-G); **PASS → 0.90 Track A AND a flagged §VII.BL Stage-2 CONTRADICTION** (a left-invariant T4 lift would be a counterexample to a STAGE-3-PERMANENT theorem requiring a Stage-2 re-audit, NOT a quiet shift to "Reading A wins"); INFO → unchanged, route to higher L_max. EVOI re-tier: **HIGH → MED-LOW** (no longer a live discriminator; a confirmation-when-convenient numerical witness). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

The panel CONVERGED on every adjudication question; these are the forward items the convergence generates (not residual disagreements — there are none).

1. **The off-Jensen Weinberg-angle / a₂-response sensitivity study (prime CF candidate).** The C²-coset's genuine irrep-leg dominance (`J_C2=0.933` ≫ `J_su2=0.059`; largest off-Jensen `‖T‖²`) is the geometrically-correct lever for `sin²θ_W(δ_C²) = 3L2/(L1+3L2)` and `a₂(δ_C²)` under the T4 deformation at τ_fold — the response SLOPE `d(sin²θ_W)/dδ_C²|_0`, a NEW observable distinct from INV12-W2-1's a₂-additivity-leak bound. This re-homes Reading A's genuine C²-specialness to the observable it actually governs. COMPUTE-CF (pre-registered solo, no two-reading divergence) → S111 `/rclab-plan` queue. (Carry-Forward Computations below.)

2. **The C²-coset confirmation compute S110-CF1-YUK-C2COSET (down-tiered).** No longer a live discriminator (prior ~0.90 FAIL with a register-PROVEN mechanism); a MED-LOW confirmation-when-convenient numerical witness that converts the register status "C²-coset is the surviving open seam" → "closed like every other internal modulus." Folds into the same wave as CF-2(Yukawa)/ε_LX without its own dispatch overhead. (Carry-Forward Computations below.)

3. **δ_CP as the K=2→K=3 third instance of "Non-LI-Deformation Necessity" (registry line 21157, NOT this workshop's deliverable).** The C²-coset closure confirms the whole left-invariant geometric family sits inside the generation-blind calculus, sharpening δ_CP (forced real by `[J,D_K]=0`) as the clean structurally-distinct third module-rank datum for the K=2→K=3 promotion. It must LAND (a dedicated gate), not be asserted — flagged for a future session, owned by the §VII.BL precondition, not adjudicated here.

4. **The downstream ε_LX gate (CF-2/Yukawa) must target the 3-valued triality index `t = (p−q) mod 3`, not a per-sector `dim V_{(p,q)}` object** (the R2/R3 `m(p,q)` precision). Flagged so the convergence text does not propagate baptista's R2 prose over-identification into the hierarchy-magnitude gate. Register-consistency note, not a new compute.

## Wrap-Up — Workshop Impact Summary

### What Changed

Per `output-standards.md §"Numerical vs Structural"`, the two epistemic categories are separated.

**(a) Numerical / scope revisions**
- Dual prior for S110-CF1-YUK-C2COSET: `0.6–0.7 PASS` (baptista R1 symmetric elicitation) → **0.90 FAIL / 0.10 PASS** (matching the INV2-W1-1 0.65→0.90 Track-B reallocation precedent, because Reading A's mechanism was already falsified by T3).
- EVOI tier for S110-CF1-YUK-C2COSET: **HIGH → MED-LOW** (the rollup tiered it HIGH as a ~0.65 coin-flip discriminator on the sole surviving probe of CV-8 Arm-G; the panel's convergence collapsed `|ΔP|` — a FAIL barely moves a posterior behind a register-PROVEN mechanism).
- §VII.BL Generation-Blindness scope: from {U(2)-invariant slice + su(2)-anisotropy direction} → **{every left-invariant internal direction}** (the full Milnor complement), across the window [0.15,0.25].

**(b) Structural changes**
- BAP1 step 3 (Ad(SU(2))-breaking ⇒ rank lift) **WITHDRAWN** — it acted on the wrong tensor leg (irrep leg `V_{(p,q)}`, where Ad-structure lives) for a multiplicity-leg observable (the generation index, the commutant). The empirical clincher is T3 itself, which breaks Ad(SU(2)) and did NOT lift (`dY_12/dδ\|_0 = 1.943e-15`).
- VDD2-Q3's (a)/(b) dichotomy for the O'Neill object → **(c) added**: the genuine off-Jensen fiber-modulus T-tensor (`‖T‖²=7.81e-4`, INV12-W2-1), real and nonzero, but tensor-factor-disjoint from generations. "T4 fails like su(2) by an undifferentiated multiplicity-scalar argument" → "T4 has REAL first-order action on the irrep leg, tensor-factor-DISJOINT (exactly-zero, not bounded-leak) from the generation index."
- `m(p,q)` identification: baptista R2 prose "generation = right-regular copy-count `dim V_{(p,q)}`" → the register's **3-valued triality `t = (p−q) mod 3`** (`proven_384`); the commutant argument is INDIFFERENT to which identification holds (the safe direction).
- The C²-coset's role: from "the surviving open seam / candidate generation-lift channel (Reading A)" → **"the dominant lever on every irrep-leg observable (a₂/G_N/sin²θ_W/gauge-normalization), exactly impotent on the one multiplicity-leg observable (generations), by ONE leg-membership fact read with opposite signs"** — a relocation, not a dead end (re-homed to the Weinberg-angle CF candidate).

### What Holds

- **§VII.BL Generation-Blindness is STAGE-3-PERMANENT and is REINFORCED, not modified in confidence.** Its conclusion (the Yukawa hierarchy is not deliverable by any `A_K`-built form; the home is the external non-LI `ε_LX`, existence-PASS `S98-W3-1-YUKAWA-EPS-LX-BETWEEN-GEN`) is unchanged; the panel extends its SCOPE from {su(2)-direction} to {all left-invariant internal moduli} and supplies the C²-coset as a named instance. Stage-2 audit `0f0c4f65` stands.
- **The W2 homogeneity wall** (registry line 21116): left-invariance ALONE ⇒ multiplicity-scalar. The mechanism is the left-regular/right-regular Peter-Weyl commutant. Ad(SU(2))-equivariance is NOT a hypothesis.
- **`Y_ij` is an operator on the multiplicity leg `ℂ^{m(p,q)}`** of `H_K = ⊕_{(p,q)} V_{(p,q)} ⊗ ℂ^{m(p,q)}` (line 21110); the generation index = `t = (p−q) mod 3` (line 21124, `proven_384`) — the register's identification, NOT a per-sector `dim V_{(p,q)}`.
- **BLOCK-DIAG-GENERAL-61 (S61, my own result)**: compact G + left-invariant metric ⇒ Schur-forced block-diagonal D_K. T4 is in this class; the off-Jensen O'Neill A-tensor `‖A‖²(Jensen)=0` EXACT (A-TENSOR-61, base-fiber `g_{μa}=½F`, Jensen-zero).
- **Algebra-axis orthogonality** (`cross-pillar-bridge-anatomy.md`, MANDATORY at K=3): irrep-leg (algebra-INVARIANT spectrum-only) ⊥ multiplicity-leg (algebra-DEPENDENT state-pair). Morita-invariant module-membership (line 21155), not spectrum.
- **The Skolem–Noether closure** (line 21120): `Aut(A_K)` is multiplicity-blind; inner / twisted-inner `Ω¹_σ` / opposite-action `JAJ⁻¹` are ALL multiplicity-scalar.

### What Breaks or Strains

- **CV-8 Arm-G (the internal geometric hierarchy corridor) is DEAD.** The panel closes the LAST untested internal geometric probe (T4/C²-coset); no left-invariant internal modulus can source the generation hierarchy. The hierarchy's home is register-PINNED to the external non-LI `ε_LX` (the `_rollup-yukawa-moduli` CHALLENGED-with-RELOCATION net is now complete on the geometric arm).
- **Reading A (C²-coset lifts the rank) is refuted** — but its GEOMETRY is relocated, not discarded (option (c), the Weinberg-angle CF). The refutation is structural (wrong tensor leg), reinforced empirically by the T3 precedent.
- **No strain on any permanent result.** A PASS on the downstream compute (prior 0.10) would NOT be "Reading A wins" — it would be a counterexample to §VII.BL's STAGE-3-PERMANENT multiplicity-scalar theorem, triggering a Stage-2 re-audit. The dual prior encodes this asymmetry. The verdict is robust to the `m(p,q)` precision (the commutant is `⊗1` on every identification of the multiplicity leg).

### Carry-Forward Computations (MATH ONLY — propagate to S111)

**CF-S111-WEINBERG-C2COSET-OFFJENSEN — off-Jensen Weinberg-angle / a₂-response sensitivity study (the productive relocation; prime candidate).**
1. **What**: compute the response SLOPE `d(sin²θ_W)/dδ_C²|_0` and `d(a₂)/dδ_C²|_0` under the T4 (C²-split) left-invariant deformation `L3·I_4 → diag(...)` on `C2_IDX=[3,4,5,6]` at the fold τ-anchor; `sin²θ_W(δ_C²)=3L2/(L1+3L2)`, `a₂` the G_N-feeding Seeley-DeWitt coefficient. NEW observable (a response derivative on the irrep-leg spectrum), distinct from INV12-W2-1's a₂-additivity-leak BOUND. Clears `Investigating-Workshops.md` Q1-vs-CF as a COMPUTE-CF (no two-reading divergence).
2. **Who**: baptista-spacetime-analyst (Jensen/O'Neill geometry) or connes-ncg-theorist (spectral-action a₂).
3. **Input**: `dirac_spectrum.py` (D_K builder); `inv2_w1_off_u2_dirac_yukawa.py` (deformed-split-metric machinery, swap to C²-block sensitivity); `s84_spectrum_cache_L12_tau019.npz`; canonical `J_C2=0.933`, `J_su2=0.059` (canonical_constants.py:732-733), `tau_fold=0.19` (CONST-FREEZE-42); INV12-W2-1 npz (`‖T‖²=7.81e-4` off-Jensen at δ=0.05).
4. **Output**: `d(sin²θ_W)/dδ_C²|_0` ± regime tag + `d(a₂)/dδ_C²|_0`; npz + png.
5. **Format**: `computations/session-111/s111_weinberg_c2coset_offjensen.py`.
6. **Gate**: PASS iff `|d(sin²θ_W)/dδ_C²|_0| > eps_sens` (pre-register `eps_sens` at plan-freeze) with sign matching the L2/(L1+3L2) substitution chain; INFO if sub-threshold; pre-register the algebra-INVARIANT / FI tag per `regulator-pin-discipline.md §"β_shell FI Classification"` (the a₂-ratio is FI, parented to the F_traj a₂-ratio FI theorem at locked-norm L_k=1).
7. **Depends on**: INV12-W2-1 `‖T‖²` off-Jensen result (UPSTREAM — supplies the nonzero first-order tensor); canonical_constants.py `J_C2`/`J_su2`/`tau_fold`; distinct-from-INV12-W2-1 declaration at plan-freeze (response SLOPE, not additivity-leak bound). EVOI: MED (a sensitivity probe; not a discriminator on a permanent theorem).

**CF-S111-YUK-C2COSET-CONFIRM (= S110-CF1-YUK-C2COSET, down-tiered) — C²-coset Yukawa-rank confirmation witness.**
1. **What**: build the T4 C²-coset anisotropy modulus (split `L3·I_4 → diag` on `C2_IDX=[3,4,5,6]`, `J_C2=0.9330 M_KK`, transverse to U(2)) via the `inv2_w1_off_u2_dirac_yukawa.py` machinery (swap the split block su(2)→C²); re-run the off-U(2) Dirac + Yukawa-overlap on the d=2 generation multiplet. **A confirmation-when-convenient numerical witness, NOT a live discriminator** — converts the register status "C²-coset is the surviving open seam" (`_cross-investigation-synthesis.md §2` CLARIFIED-8) → "closed like every other internal modulus."
2. **Who**: baptista-spacetime-analyst or van-den-dungen-bridge-theorist.
3. **Input**: `inv2_w1_off_u2_dirac_yukawa.py` (deformed-split-metric + Y_ij(δ) machinery); `dirac_spectrum.py`; canonical `tau_fold=0.19`, `L3=e^τ`, `J_C2=0.933`; INV2-W1-1 su(2)-split-null baseline (audit `1481b775…`).
4. **Output**: `dY_12/dδ|_0`, `rank(Y_ij)`, `gen_degen_lift` (bool); npz + png.
5. **Format**: `computations/session-111/s111_yuk_c2coset_confirm.py` (folds into the same wave as CF-2/ε_LX; no standalone dispatch overhead).
6. **Gate (PRE-REGISTERED by this panel — dual prior ~0.90 FAIL / ~0.10 PASS)**: `|dY_12/dδ|_0 > eps_lift = 1e-3` AND `rank(Y_ij) 1→≥2` for δ ∈ (0, 0.20]. **FAIL → 0.95 Track B** (confirms §VII.BL on C²-coset; rank-1 wall off-ALL-internal-moduli; closes CV-8 Arm-G; hierarchy PINNED to external `ε_LX`). **PASS → 0.90 Track A AND a flagged §VII.BL Stage-2 CONTRADICTION** (counterexample to a STAGE-3-PERMANENT theorem → Stage-2 re-audit, NOT a quiet "Reading A wins"). **INFO → unchanged, route to higher L_max.**
7. **Depends on**: INV2-W1-1 su(2)-split-null baseline (UPSTREAM — the form to swap); §VII.BL STAGE-3-PERMANENT (the predicted-FAIL it confirms). EVOI: **MED-LOW** — run when a Yukawa-sector compute wave (CF-2/ε_LX) is already open; do NOT lead a session with it.

### Effected In-Session (NON-MATH — completed by Agent B before terminating)

- [x] **§VII.BL Generation-Blindness SCOPE annotation** — landed a designated-writer annotation (matching the existing S110 W0b annotation pattern at lines 21183/21188) confirming the strengthening from {U(2)-slice + su(2)-direction} to {ALL left-invariant internal transverse moduli, C²-coset included}; reinforces the STAGE-3-PERMANENT conclusion (grade UNCHANGED), pins CV-8 Arm-G DEAD, carries the mechanism / option-(c) cleanliness / `t=(p−q) mod 3` identification / route-once dual-prior — `sessions/permanent-results-registry.md:21190` (the new `**Generation-Blindness SCOPE — strengthened…**` paragraph inserted after the HK-B3 paragraph, before the §VII.BM header).
- [x] **HK-B3 "B-3 modular-flavor CLOSED not live" register note** — VERIFIED already effected by the S110 W0b designated-writer patch at `sessions/permanent-results-registry.md:21188` (the `**B-3 modular-flavor "strongest convergence" billing — DOWN-TAGGED to CLOSED**` paragraph). NOT duplicated (would be a registry-write-hygiene violation); the new §VII.BL scope annotation cross-references it. The C²-coset closure REINFORCES it (the same W2 homogeneity wall closes both the modular-flavor route and the C²-coset route).
- [x] **Yukawa-register status edit** — SUBSUMED in the §VII.BL scope annotation above: the Generation-Blindness scope-extension IS the Yukawa-register status edit (CV-8 Arm-G → DEAD; hierarchy home register-PINNED to external `ε_LX`; the geometric arm of the `_rollup-yukawa-moduli` CHALLENGED-with-RELOCATION net is now complete). No separate edit needed; the register's Yukawa-hierarchy status is now fully consistent across §VII.BL (line 21102 block) and the W0b annotations.
- [→] ROUTED-capstone-hygiene: IF the curated capstone (`sessions/framework/phonic-exflation-equation.md`) narrates §VII.BL Generation-Blindness as scoped to the su(2)-direction / U(2)-slice ONLY, or narrates CV-8 Arm-G (the internal geometric hierarchy corridor) as live/open, reconcile the prose to the strengthened scope: "the rank-1 Yukawa wall is off-Jensen for ALL left-invariant internal transverse moduli (the C²-coset/T4 included), per S110 WS-C2COSET; CV-8 Arm-G is DEAD; the hierarchy's home is the external non-LI `ε_LX`." This is a capstone-hygiene Q3 reconciliation (PROVEN/CONDITIONAL status of a §VII.BL-governed claim) — the prose tag stays at the register status (STAGE-3-PERMANENT, REINFORCED), the substrate-IS frame is preserved (`D_K multiplicity-scalar → hierarchy ∉ A_K-module → external ε_LX → measured masses`). Capstone designated-writer to apply as a reviewed patch (NOT a bulk append); I (workshop Agent B) am not the capstone prose owner and do not have the capstone's current §VII.BL wording in view.
- [→] ROUTED-mack: IF a "strongest convergence" modular-flavor Yukawa billing OR a "C²-coset internal hierarchy lift" claim appears on the capstone §7 falsifier surface or in `sessions/framework/registry/falsifier-master-inventory.md`, down-tag it to CLOSED-on-the-substrate per the S110 WS-C2COSET convergence (mack-cosmic-bridge sole writer of the §7 falsifier surface per `feedback_mack-bridge-role.md`). NOTE: the S110 W0b HK-B3 patch already recorded this as verified-absent from the capstone as of that patch; this is a forward guard, not a known live row.

### Closing Line

Reading B closes the door, and Reading A's geometry walks through the right one: the rank-1 Yukawa wall is off-Jensen for **every** left-invariant internal modulus — not by an undifferentiated "T4 is just another scalar," but because the generation index lives on the multiplicity leg `ℂ^{m(p,q)}` (a Kasparov-shriek RANK datum, Morita-invariant) while every left-invariant metric — the dominant C²-coset stiffness included — only ever moves the irrep-leg spectrum. The C²-coset's genuine first-order action (option (c)'s `‖T‖²=7.81e-4`) is tensor-factor-DISJOINT from generations: exactly-zero, not bounded-leak, the third geometric witness of one orthogonality theorem. CV-8 Arm-G is dead; the hierarchy's home is the external `ε_LX`; and the C²-specialness Reading A correctly sensed is re-homed to where it actually governs — the Weinberg-angle / a₂ irrep-leg response, a clean S111 carry-forward. Same modulus, orthogonal legs, opposite verdicts — and the substrate IS-es both observables, neither demoted.
