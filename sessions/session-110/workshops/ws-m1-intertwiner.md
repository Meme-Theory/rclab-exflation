# Session 110 Workshop: van-den-dungen × connes — WS-M1-INTERTWINER

**Date**: 2026-06-20
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: van-den-dungen-bridge-theorist (Reading A — χ is a faithful internal fibre-integration / Kasparov shriek), connes-ncg-theorist (Reading B — χ is a deletion / Connes-Karoubi zero-map)
**Source Documents**:
- sessions/investigation/_promotion-triage.md
- sessions/investigation/_cross-investigation-synthesis.md
**Adjudication question**: Does the algebra projection χ : ℂ⊕ℍ⊕M₃(ℂ) → M₂(ℂ) sending M₃(ℂ)→0 factor as the Kasparov shriek π_! (fibre-integration) of the SU(3)→CP² submersion (Reading A, discharges LBA-5, flips N3/N7 to (c)), or is it the Connes-Karoubi zero-map (Reading B, deletion, LBA-5 permanently undischargeable)? Derive from first principles (expected FAIL per triage, but DERIVE). This is the LBA-5 discharge leg of the ATOMIC HK-N37 N3/N7 update (atlas-04 N7 + atlas-08 Q10/Q9 + registry §VII.W-3 must agree). Full adjudication context supplied to each agent at dispatch.

---

## Round 1 — van-den-dungen: Opening Analysis

### VDD1: Reading-A steelman (faithful fibre-integration; χ = Kasparov shriek π_! of SU(3)→CP²)

**Charge discipline.** My role here is to build Reading A — that χ : ℂ⊕ℍ⊕M₃(ℂ) → M₂(ℂ) (M₃(ℂ)→0) is a *faithful internal fibre-integration*, the Kasparov shriek π_!^{CP²} of the internal submersion SU(3) → CP² — at its strongest, from first principles. I am NOT pre-committing to it. My standing prior (memory: "Kasparov product gives TOPOLOGY... NOT ANALYSIS"; the inv-2 INV2-W1-4 convergence I co-signed) is that this reading FAILS against the Connes-Karoubi zero-map. But the workshop's job is to derive, not assume, so I steelman the survivable reading honestly and hand connes its hardest version. Substrate-first framing (`phononic-framing.md`): χ is the inheritance morphism from the substrate spectral triple (A_K, H_K, D_K) onto the M₂(ℂ) Nambu/BdG sector; SU(3)→CP² is the substrate's *own internal* fibre geometry; the question is whether the substrate's internal fibre-integration SURVIVES the algebra projection — not whether some external manifolds map.

**The crucial disambiguation Reading A rests on (two distinct submersions).** The prior workshop (inv-2 INV2-W1-4, `n3-chi-rescue-kasparov-faithfulness.md`, R1 V-a) PROVED — and I co-signed — that χ is NOT the shriek of the **external** submersion π : E = M⁴×SU(3) → M⁴. That π_! lives in `KK^{dim F}(C(E), C(M⁴))` (Paper 01, 1811.07824, Thm 3.4, file lines 70–76: `[D_E] = π_! ⊗_{C(M⁴)} [D_{M⁴}]`), integrates over the *entire* SU(3) group manifold, and retains the full fibre. χ, by contrast, is a finite-matrix surgery on A_F. Category mismatch killed external-χ = π_!. **Reading A makes a DIFFERENT, narrower claim that the external-submersion refutation does NOT touch:** χ is the shriek of the *internal* submersion

```
π_internal : SU(3) ⟶ SU(3)/U(2) = CP²,   vertical fibre U(2),   dim_ℝ CP² = 4.
```

This is geometrically real and on disk: `dirac_spectrum.py` lines 102–108 carry the reductive split `su(3) = u(1) ⊕ su(2) ⊕ C²` with `U2_IDX = [0,1,2,7]` (u(2) = su(2)⊕u(1)) and `M_IDX = [3,4,5,6]` (m = C², the 4-real-dim tangent of CP²). The Jensen curve `(L1,L2,L3) = (e^{2s}, e^{-2s}, e^s)` is volume-preserving precisely on these (u(1), su(2), C²) blocks. So SU(3) → CP² is an honest Riemannian submersion the framework ALREADY uses; the M1 steelman asks whether χ-killing M₃(ℂ) IS its shriek `π_!^{CP²} ∈ KK^{dim U(2)}(C(SU(3)), C(CP²))`.

**Leg A1 — the irrep-selection match is structurally exact (the strongest positive leg).** A genuine shriek `π_!^{CP²}` integrates over the U(2) fibre and retains the **U(2)-invariant content** of each Peter-Weyl module `V_{(p,q)}`. The framework's own register supplies the candidate identification (registry line 287, the Q10 W3 wall): `ker(ι_*) = M₃(ℂ)` is tied to **triality-0 ⇔ color-singlet ⇔ BdG-restricted**. Triality `t(p,q) = (p−q) mod 3` is exactly the SU(3) center character, and the U(1) ⊂ U(2) factor of the vertical fibre acts as the triality grading. So "retain the U(1)-neutral (triality-0) content under `π_!^{CP²}`" and "pass to the color-singlet sector" are — at the level of WHICH irreps survive — the SAME selection. This is not hand-waving: it is the precise content of `phononic-framing.md` Level-1 substrate-IS at a single-τ slice, and it is why my S61 record ("of 136,480 modes, exactly 16 — the trivial (0,0) irrep — couple to 4D") already reads as a fibre-integration selecting the SU(3)-invariant part. **If the only thing M1 needed were irrep-selection agreement, Reading A would land.**

**Leg A2 — functoriality licenses iterated/internal shrieks (Paper 01 is built for this).** My Paper 01 result is explicitly functorial: π_! "respects the composition of submersions" (Paper 01, file line 74). The factorization is iterable — a shriek of a fibre-of-a-fibre is itself a shriek. So there is no a-priori bar to χ being `π_!^{internal}` even though it is NOT `π_!^{external}`: these are shrieks of *different* submersions, and the external refutation (category mismatch with C(M⁴)) says nothing about the internal one (which lives in `KK^{dim U(2)}(C(SU(3)), C(CP²))`, a genuinely different KK-group). This is the precise sense in which the M1 off-ramp is *not* foreclosed by the prior workshop's CV-1 — CV-1 closed external-χ, M1 asks about internal-χ.

**Leg A3 — KO-dim=6 preservation as a faithfulness witness (the M2 leg, stated at strength).** A purely arbitrary Wedderburn deletion would not be expected to preserve KO-dim=6 and the full SM spectrum on the surviving block. Yet χ-killing M₃(ℂ) leaves KO-dim=6 intact, whereas the order-one *relaxation* route (keep color, add quadratic fluctuations — Paper 06/Connes-23 lineage) collapses KO-dim 6→2 (inv-2 Re:V-b chain; my S92 W9-2: ε''=−1 colour-sign-INVARIANT under the BDI block-swap, KO=6 always). A genuine fibre-integration onto an internal sub-sector WOULD preserve the mod-8 KO grading (the surviving triple inherits `(J, γ_F, D_F|_image)` consistently); a random amputation generically would not. So KO-6 preservation is *consistent with* the internal-shriek reading and is positive evidence that χ is at least geometrically distinguished among the candidate operations. (Honest scope, carried from inv-2 DS-3: this witnesses the IMAGE is well-formed, not that the MAP is the submersion shriek — I flag the limit but include the leg, because the steelman's job is to assemble every consistent positive.)

**Leg A4 — the type-bridge has a named realization candidate (Paper 02).** The one structural gap is the type-bridge: χ deletes a Wedderburn summand of the FINITE algebra A_F; `π_!^{CP²}` is a Peter-Weyl selection on FUNCTION-algebra `C(SU(3))`. Reading A's burden is to express "delete the M₃ summand of the fibre algebra" AS a fibre-integration. My Paper 02 (1405.5368, almost-commutative manifolds) is the candidate machinery: in the globally-non-trivial ACM construction the finite algebra A_F sits as the fibre of an almost-commutative bundle `M⁴ × F`, and fibre-integration over the internal geometry acts on that fibre algebra. IF Paper 02's shriek on `M⁴ × F` can be shown to send the M₃(ℂ) summand to the fibre-integrated (color-singlet) class, the type-bridge closes and χ = (type-bridge) ∘ π_!^{CP²} becomes a genuine commuting square. This is the load-bearing CONJECTURE of Reading A — and it is exactly the discharge gate CF-INV2-W1-4-M1.

**My honest read on where Reading A stands (the steelman's own confession of its weak point).** I have assembled the strongest internal-shriek case: a real internal submersion (SU(3)→CP²), an exact irrep-selection match (triality-0 ⇔ color-singlet, registry line 287), functoriality licensing it (Paper 01 line 74), a KO-6 witness, and a named type-bridge candidate (Paper 02). **But Reading A has one antagonist it cannot answer from the shriek side alone**, and I must hand it to connes squarely: a faithful shriek `π_!` RETAINS its fibre as a NON-trivial integrated K-homology class (Paper 01 Thm 3.4 — the shriek is the push-FORWARD of the fibre Dirac family, not its annihilation). If connes's Connes-Karoubi boundary map sends the M₃-gap-class to the TRIVIAL class, then χ behaves like a DELETION (zero image), not a shriek (non-trivial integrated image) — and Reading A's irrep-selection match (A1) would be revealed as a coincidence at the level of *which irreps* that does not survive to the level of *the K-theory class*. I verified the antagonist is live this session: gate `S93-W2-1-VII-AU-CF37-FREDHOLM-INDEX-INTEGER-TRIPLE` returns `phi_cd_triple=(0,0,0)`, integrality residual `0.00e+00`, verdict INFO — the Connes-Karoubi boundary IS the zero map on the deleted sector. So the strongest Reading A I can build still walks into a verified zero-map. **That is the precise tension this workshop must adjudicate, and I want connes's K-theory reading of whether the zero-map is decisive or whether the irrep-selection match (A1) + Paper 02 type-bridge (A4) can re-route around it.**

**Key finding (VDD1).** The internal-shriek reading is NOT refuted by the prior workshop's external-submersion result — it is a claim about a different submersion (SU(3)→CP²) in a different KK-group. Its strongest legs (exact triality-0 ⇔ color-singlet irrep selection; Paper 01 functoriality; KO-6 witness; Paper 02 type-bridge candidate) are real. Its single un-answered antagonist is the Connes-Karoubi zero-map (gate S93-W2-1, verified live), which says a faithful shriek would retain a NON-trivial integrated class but χ sends the gap-class to (0,0,0). Reading A survives iff the type-bridge (Paper 02) re-routes around the zero-map; it dies iff the zero-map is decisive. **I cite Paper 01 (1811.07824, Thm 3.4 + functoriality line 74), Paper 02 (1405.5368), the registry triality-0 ⇔ color-singlet identification (line 287), `dirac_spectrum.py` lines 102–108, and gate S93-W2-1.**

**Questions for connes (from VDD1):**
- **VDD1-Q1 (THE decisive question).** Is the Connes-Karoubi zero-map (`[φ_cd] = (0,0,0) ∈ K⁰(A_K) = ℤ³`, gate S93-W2-1) DECISIVE against the internal-shriek reading, or is it evaluated on the WRONG bridge map? Specifically: the gate computes `ι_*∘HKR` (the *external/BdG-inheritance* boundary). Does an *internal* shriek `π_!^{CP²}` have a DIFFERENT K-theory image — one that need not be zero — because it pairs with `K_*(C(SU(3)))`/`K_*(C(CP²))` rather than with `K⁰(A_K) = ℤ³`? Or do you read the zero on `ℤ³` as fatal regardless of which submersion's shriek we test, because the M₃-gap-class is the ONLY object the deleted summand could map to?
- **VDD1-Q2 (the type-bridge).** Can the triality-0 ⇔ color-singlet irrep-selection match (registry line 287, which I hold is exact) be promoted to a constructed intertwiner via Paper 02's almost-commutative `M⁴ × F` fibre-integration — i.e. does "fibre-integrate the internal U(2) geometry" act on the FIBRE ALGEBRA A_F as "project onto the color-singlet summand"? If YES, Reading A re-routes around the zero-map. If the type-bridge is categorically obstructed (your CF-INV2-W1-4-TYPEBRIDGE lemma), Reading A dies on a categorical, not case-by-case, basis.

### VDD2: Adjudication-question framing (LBA-5 discharge leg; HK-N37 atomicity)

**This workshop is the LBA-5 discharge leg — and ONLY that leg.** The N3/N7 register state was already settled by inv-2 INV2-W1-4 (`n3-chi-rescue-kasparov-faithfulness.md`, converged R2): N3 bare = BROKEN (stays); N7-(i) algebraic-singleton = STAGE-3-PERMANENT UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total upgrade = CONDITIONAL-on-χ-admissibility; G5 = UNCONDITIONAL (two-functor reading). What was NOT settled — deliberately deferred as the workshop's one carry-forward (CF-INV2-W1-4-M1) — is the single question this workshop exists to answer: **does the M1 internal-shriek intertwiner exist?** That is the LBA-5 discharge condition, made precise. So I frame the adjudication exactly:

```
LBA-5  =  "χ-killing M₃(ℂ) (equivalently: Step A, the restriction to the
           triality-0/BdG sub-sector) is admissible as an INTRINSIC,
           geometry-FORCED operation — not an extrinsic choice."

DISCHARGE CONDITION (the whole content of this workshop):
   LBA-5 is DISCHARGED  iff  χ = (type-bridge) ∘ π_!^{CP²}  is constructed
        as a machine-checkable commuting square with a vertically-elliptic
        symbol on the U(2)-fibre that SELECTS exactly ker(ι_*) = M₃(ℂ),
        AND that construction reproduces the Q10 W4 K-theory constraint.
   LBA-5 stays PROMOTED-UNDISCHARGED  iff  no such intertwiner is exhibited
        (Reading A fails) — the type-bridge is obstructed OR the
        Connes-Karoubi zero-map is decisive against a faithful image.
```

**The verdict outcomes and what each does to the register (pre-registered so the verdict is not narrativized after).**
- **Reading A lands (χ = π_!^{CP²}, faithful internal fibre-integration):** LBA-5 DISCHARGED. The (c) verdict "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" flips to "FAITHFUL INTERNAL-SHRIEK RESCUE." N7-(ii) flips to UNCONDITIONAL. The N3/N7 verdict flips to triage-disposition (c).
- **Reading B lands (χ = Connes-Karoubi zero-map, deletion):** LBA-5 stays PROMOTED-UNDISCHARGED — and, because the zero-map is positive evidence the type-bridge is obstructed *on this route*, the (c) verdict "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" becomes PERMANENT (not merely undischarged-pending). N7-(ii) stays CONDITIONAL-on-χ-admissibility.

I record my standing prior honestly (it is NOT the verdict): the triage expects FAIL (Reading B), and so do I, because the Connes-Karoubi zero-map (gate S93-W2-1, verified live this session: `phi_cd_triple=(0,0,0)`, residual `0.00e+00`) is the antagonist VDD1 could not answer from the shriek side. But the VDD1 steelman's irrep-selection match (triality-0 ⇔ color-singlet, registry line 287) + the Paper 02 type-bridge candidate are genuine, non-trivial, and connes and I invoke DIFFERENT machinery on them (Kasparov shriek vs K-theory boundary) — so this is a real Q1 adjudication, not a foregone conclusion. The job is to DERIVE which reading the geometry supports.

**The HK-N37 atomicity constraint (load-bearing — do NOT split).** Per the triage (`_promotion-triage.md` Bucket-3 HK-N37) the N3/N7 register update is a SINGLE atomic four-facet object: **atlas-04 N7 cell + atlas-08 Q10/Q9 + permanent-results-registry §VII.W-3 must ALL agree, applied together or not at all.** This is because the four facets are FOUR VIEWS OF ONE structural fact (inv-2 HY-B1/B2/B3/B4/B5 = "FOUR facets of ONE atomic register-update — land together"):
- **atlas-04 N7 cell** — the two-leg split (N7-(i) singleton UNCONDITIONAL / N7-(ii) spectral-triple-upgrade CONDITIONAL-on-LBA-5);
- **atlas-08 Q10** — "RESCUED — STAGE-3-PERMANENT" must be scoped so PERMANENT attaches to N7-(i), NOT the full D_total spectral-triple claim; **atlas-08 Q9** — CLOSED → PARTIAL (NS-3);
- **permanent-results-registry §VII.W-3** — the verdict-name "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" + the LBA-5 PROMOTED-UNDISCHARGED record + the EM-1 reconciliation of the register's own "forced (lines 287/13208) vs chosen (line 17004)" contradiction.

**Why atomicity is mandatory, not stylistic.** If only one facet is edited, the register goes INTERNALLY INCONSISTENT in a way that is worse than the status quo: e.g. if atlas-08 Q10 keeps "RESCUED STAGE-3-PERMANENT" (full-claim reading) while §VII.W-3 records "EXTRINSIC RESTRICTION / LBA-5 UNDISCHARGED," a downstream consumer citing Q10 would read the spectral-triple-for-D_total claim as restored while a consumer citing §VII.W-3 would read it as conditional — a `capstone-hygiene-gate.md` Q3 status-drift across registers. The four facets MUST carry the SAME confidence tag on the SAME sub-claim. Per the charge: **if the verdict lands in-workshop, EFFECT the four edits atomically in R3 Turn B (connes, final writer); if it does NOT close in-workshop, route the entire HK-N37 update as a SINGLE 4-field carry-forward — do NOT partially apply.**

**My read on which way this resolves (entering R1).** Given that this workshop adjudicates ONLY the M1 discharge leg, and given the inv-2 convergence already pinned the N3/N7 split CONDITIONAL on exactly the M1 intertwiner that does not yet exist on disk, I expect: (i) the verdict is the *adjudication* "is the M1 intertwiner constructible / is the zero-map decisive," which is a STRUCTURAL verdict, not a numerical compute — so it CAN close in-workshop as a reading-adjudication even though the full intertwiner CONSTRUCTION is a multi-wave compute; (ii) the most likely landing is Reading B (LBA-5 stays PROMOTED-UNDISCHARGED, (c) "extrinsic restriction" confirmed permanent-on-this-route), in which case the atomic HK-N37 edits are EFFECTED in R3 Turn B with the N7-(ii) conditional preserved; (iii) the explicit Kasparov-product/Connes-Karoubi CONSTRUCTION (proving the intertwiner exists or is categorically obstructed) is the residual MATH carry-forward (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) propagating to S111, regardless of the reading-adjudication outcome — because a reading-adjudication "expected FAIL" is not the same as a CONSTRUCTED obstruction proof.

**Key finding (VDD2).** LBA-5's discharge condition is now sharp and binary: it discharges iff the M1 intertwiner `χ = (type-bridge) ∘ π_!^{CP²}` is constructible against the Q10 zero-map constraint, and stays PROMOTED-UNDISCHARGED otherwise. This workshop's verdict is the reading-adjudication of that question (a structural verdict, closeable in-workshop); the intertwiner CONSTRUCTION is the residual math CF. The HK-N37 register update (atlas-04 N7 + atlas-08 Q10/Q9 + registry §VII.W-3) is ATOMIC — four views of one fact, applied together in R3 Turn B if the verdict lands, or routed as one 4-field CF if not; NEVER partially applied. **I cite the inv-2 INV2-W1-4 convergence (`n3-chi-rescue-kasparov-faithfulness.md`), the triage HK-N37 row (`_promotion-triage.md` Bucket 3), gate S93-W2-1, and `capstone-hygiene-gate.md` Q3 (the cross-register status-drift the atomicity prevents).**

**Questions for connes (from VDD2):**
- **VDD2-Q3 (discharge-or-not — the binary I want co-signed).** Do you agree the verdict reduces to: Reading A ⇒ LBA-5 DISCHARGED + (c) flips to faithful-internal-shriek + N7-(ii) → UNCONDITIONAL; Reading B ⇒ LBA-5 stays PROMOTED-UNDISCHARGED + (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" becomes PERMANENT-on-this-route + N7-(ii) stays CONDITIONAL? I want these two end-states pinned BEFORE we argue VDD1-Q1/Q2, so the verdict is pre-registered against your zero-map reading, not fitted to it.
- **VDD2-Q4 (in-workshop closure vs carry-forward).** Do you read the M1 adjudication as CLOSEABLE in-workshop as a reading-verdict (we DERIVE that the zero-map is/ isn't decisive and that the type-bridge is/isn't obstructed, producing a structural verdict-name), with the explicit intertwiner CONSTRUCTION as the residual MATH carry-forward to S111? Or do you hold that NOTHING closes until the construction runs — in which case the entire HK-N37 update routes as a single 4-field CF and no register edit is effected this workshop? My lean is (former): the reading-adjudication closes (Reading B, expected), the four HK-N37 edits are EFFECTED atomically in your R3 Turn B, and the construction is the CF. I want your read so we route HK-N37 atomically either way.

---

## Round 1 — connes: Response & Cross-Synthesis

### Part 1: Response to van-den-dungen's Sections

#### Re: VDD1 — Kasparov-shriek steelman

**AGREE (the disambiguation is correct and load-bearing).** VDD1's central move — that the prior inv-2 CV-1 refutation closed *external*-χ = π_!^{M⁴×SU(3)→M⁴} and NOT *internal*-χ = π_!^{SU(3)→CP²}, two shrieks in two distinct KK-groups (`KK^{dim F}(C(E),C(M⁴))` vs `KK^{dim U(2)}(C(SU(3)),C(CP²))`) — is exactly right, and I co-signed its premises in INV2-W1-4 R1 (Re:V-a). The external-submersion category-mismatch says nothing about the internal one. I do not contest that the question is genuinely open as a *which-irreps* matter, nor that the framing must be substrate-first: the SU(3)→CP² submersion is the Jensen-deformed fibre's *own* internal geometry (`dirac_spectrum.py` lines 102–108, the `u(1)⊕su(2)⊕C²` reductive split, `U2_IDX=[0,1,2,7]`/`M_IDX=[3,4,5,6]`), and the live question is whether the substrate's internal fibre-integration SURVIVES the algebra projection χ — not whether external manifolds map. On all of this VDD1 and I converge.

**AGREE that Leg A1 is structurally exact AT THE IRREP-SELECTION LAYER — and this is precisely where I must draw the line that VDD1 itself drew in its own confession.** The triality-0 ⇔ color-singlet ⇔ BdG-restricted identification (registry line 287, forced by THEOREM D-2; triality `t(p,q)=(p−q) mod 3` IS the SU(3) center character carried by the U(1)⊂U(2) vertical factor) is genuine and I affirmed it in INV2-W1-4 C-a. A shriek `π_!^{CP²}` retains the U(2)-invariant content of each `V_{(p,q)}`; "retain triality-0" and "pass to the color-singlet sector" ARE the same selection *at the level of which irreps survive*. **But irrep-selection agreement is NOT image-class agreement, and the gap between them is the whole adjudication.** VDD1 says this itself: "Reading A's irrep-selection match (A1) would be revealed as a coincidence at the level of *which irreps* that does not survive to the level of *the K-theory class*." I agree with VDD1's own diagnosis and will make it decisive in CON1: A1 lives one categorical layer above the discriminator.

**DISAGREE (scope) on Leg A3 — KO-6 preservation witnesses the IMAGE, not the MAP, and VDD1's own honest scope (carried from inv-2 DS-3) already concedes this.** VDD1 includes A3 "at strength" while flagging the limit. I sharpen: KO-dim=6 preservation is a witness that χ is geometrically DISTINGUISHED *among order-one-defect treatments* (the R1/R2/R3 trichotomy of INV2-W1-4 C-a: full-relaxation collapses KO 6→2, χ-restriction R3 preserves 6 — I weighted this HIGHER than VDD did in inv-2). That is a real fact and I do not retract it. But "χ is the right NCG move on the defect" and "χ is the intrinsic shriek of the SU(3)→CP² submersion" are DIFFERENT claims. KO-6 preservation establishes the former; it is silent on the latter. A3 cannot be promoted from image-well-formedness to map-faithfulness, and so it cannot answer the antagonist. VDD1 agrees; I am pinning the scope so it cannot drift into the verdict's support column.

**The crux VDD1 hands me is correct, and I accept the handoff exactly as stated.** VDD1's confession is honest and precise: a faithful shriek `π_!` is the push-FORWARD of the fibre Dirac family as a NON-trivial integrated K-homology class (Paper 01, 1811.07824, Thm 3.4 — `[D_E] = π_! ⊗_{C(M⁴)} [D_{M⁴}]`, the shriek RETAINS its fibre, it does not annihilate it). If the Connes-Karoubi boundary sends the M₃-gap-class to the TRIVIAL class, χ behaves like a DELETION (zero image), not a shriek (non-trivial integrated image). VDD1 verified the antagonist live: gate `S93-W2-1-VII-AU-CF37-FREDHOLM-INDEX-INTEGER-TRIPLE` returns `phi_cd_triple=(0,0,0)`, integrality residual `0.00e+00`, INFO. **I confirm this gate independently against the registry: it is anchor D-1 of the Q10 workshop (registry line 298, `audit_sha256 76e5d744…496c5f99`), and registry line 288 records `B = ι_*∘HKR` is the zero map on the gap-class.** This is my own (NCG/K-theory) instrument, not a borrowed one, and in CON1 I argue it IS decisive — the zero-map is not "evaluated on the wrong bridge" (VDD1-Q1's escape hatch), because the deleted summand has nowhere else to map: K⁰(A_K)=ℤ³ is the entire target, and the gap-class is its only image-candidate.

**One genuine sharpening VDD1's framing MISSED (and it strengthens Reading A's strongest form before I refute it).** VDD1 rests A1 on the *external* identification "of 136,480 modes, exactly 16 (the trivial (0,0)) couple to 4D" (the S61/A7 record). But that is the SU(3)→4D coupling selection, NOT the SU(3)→CP² coset selection. The CP² shriek retains the full U(2)-invariant content of EACH `V_{(p,q)}` — which is much larger than the single trivial (0,0) irrep (every `(p,q)` has a non-trivial U(2)-invariant subspace whenever `p≡q mod 3`... no — whenever the U(2)-content is non-empty, which is generic). So the CP²-shriek image is NOT "16 modes" — it is the entire triality-0 tower. If Reading A wants A1 exact, it must use the *coset* selection (triality-0 tower), not the *4D-coupling* selection (trivial (0,0) only). I flag this because it is the version of A1 that is genuinely exact, and it is the one I must defeat — and the K-theory zero-map defeats it regardless of which selection one uses, because (0,0,0) is the image of the *whole* deleted M₃ summand, tower and all.

#### Re: VDD2 — Adjudication-question framing

**AGREE — I co-sign the binary discharge condition VDD2 pins, verbatim.** LBA-5 discharges iff `χ = (type-bridge) ∘ π_!^{CP²}` is constructed as a machine-checkable commuting square with a vertically-elliptic symbol on the U(2)-fibre selecting exactly `ker(ι_*)=M₃(ℂ)`, reproducing the Q10 W4 K-theory constraint; it stays PROMOTED-UNDISCHARGED otherwise (type-bridge obstructed OR zero-map decisive). This is the correct sharpening of the INV2-W1-4 carry-forward CF-INV2-W1-4-M1, and I will not re-open the N3/N7 split that inv-2 already converged on (N3 bare BROKEN stays; N7-(i) algebraic-singleton UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total CONDITIONAL-on-LBA-5; G5 UNCONDITIONAL).

**AGREE on VDD2-Q3 (the two pre-registered end-states) — pinned BEFORE the VDD1-Q1/Q2 argument, so the verdict is not fitted to my zero-map reading.** I co-sign:
- **Reading A ⇒** LBA-5 DISCHARGED; (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" flips to "FAITHFUL INTERNAL-SHRIEK RESCUE"; N7-(ii) → UNCONDITIONAL; N3/N7 → triage-disposition (c).
- **Reading B ⇒** LBA-5 stays PROMOTED-UNDISCHARGED; (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" becomes PERMANENT-on-this-route (the zero-map is positive evidence the type-bridge is obstructed on the K-homology route, not merely that it is unconstructed); N7-(ii) stays CONDITIONAL.

I record my standing prior honestly, as VDD2 did: I expect Reading B, and so does the triage. But the reason this is a genuine Q1 adjudication and not a foregone conclusion is that VDD1's A1 (triality-0 ⇔ color-singlet, exact) + A4 (Paper 02 type-bridge candidate) are real, non-trivial, and we invoke DIFFERENT machinery on them — VDD's Kasparov shriek vs my Connes-Karoubi boundary. The job is to derive which the geometry supports. I derive it in CON1.

**AGREE on VDD2-Q4 (in-workshop closure vs carry-forward) — with a precise two-part split that I want co-signed in R2.** I read the M1 adjudication as having TWO separable deliverables, and conflating them is the error to avoid:
1. **The READING-ADJUDICATION is CLOSEABLE in-workshop** as a structural verdict-name. We can DERIVE from first principles (the K-theory zero-map + the type-obstruction) that the zero-map IS decisive on the K-homology route and that the type-bridge is OBSTRUCTED-on-this-route. That produces the verdict-name "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" and effects the atomic HK-N37 edits in R3 Turn B. This is a STRUCTURAL verdict (a math/physics adjudication of competing readings per `Investigating-Workshops.md §Q1`), not a numerical compute, so it closes in-workshop.
2. **The CONSTRUCTED-OBSTRUCTION PROOF is the residual MATH carry-forward** (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) to S111. Here I must be more careful than VDD2's lean allows: "expected FAIL / zero-map decisive on the K-homology route" is NOT the same as "the type-bridge is CATEGORICALLY obstructed for ALL bridge maps." The zero-map closes the `ι_*∘HKR` route (the only natural bridge on disk). A *categorical* obstruction theorem — proving NO vertically-elliptic intertwiner on the U(2)-fibre can select exactly `M₃(ℂ)` while reproducing a non-trivial image — is a constructive Kasparov-product compute that has not been run. So the reading-adjudication closes Reading B in-workshop (LBA-5 stays PROMOTED-UNDISCHARGED, (c) confirmed permanent-on-the-K-homology-route), the four HK-N37 edits are EFFECTED atomically in R3 Turn B, and the construction/obstruction proof propagates as the CF. I agree with VDD2's routing; I only insist the CF be framed as "construct-or-categorically-obstruct," because a reading-adjudication "expected FAIL" is weaker than a CONSTRUCTED obstruction.

**AGREE — HK-N37 atomicity is mandatory and I will EFFECT the four edits atomically in R3 Turn B (I am Agent B, final writer), or route the whole update as a single 4-field CF — never partially.** The four facets (atlas-04 N7 two-leg split; atlas-08 Q10 scoped so PERMANENT attaches to N7-(i) NOT D_total + Q9 CLOSED→PARTIAL NS-3; registry §VII.W-3 verdict-name + LBA-5 PROMOTED-UNDISCHARGED + the EM-1 "forced (287/13208) vs chosen (17004)" reconciliation) are four views of one fact, and the `capstone-hygiene-gate.md` Q3 cross-register status-drift VDD2 names is exactly the failure atomicity prevents. I note the EM-1 reconciliation is genuinely load-bearing and I resolved its direction in INV2-W1-4 C-b: "forced" (line 287) and "chosen" (line 17004) are BOTH correct at different scopes — the kernel is *axiom-forced WITHIN* the BdG/triality-0 restriction, while the restriction *itself* is extrinsic-to-the-M⁴×SU(3)-submersion and "up to χ-kernel choice" in the generic-Wedderburn sense; for the SINGLETON algebra ℂ⊕ℍ⊕M₃(ℂ) there is exactly one matrix block, so the kernel can ONLY be M₃. That reconciliation carries into the §VII.W-3 edit.

### Part 2: Original Analysis

#### CON1: Reading-B (Connes-Karoubi zero-map / deletion; LBA-5 permanently undischargeable)

**Thesis.** χ is the Connes-Karoubi zero-map on the deleted sector — a DELETION, not a faithful fibre-integration. The reading-adjudication therefore lands Reading B: LBA-5 stays PROMOTED-UNDISCHARGED, and (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" is confirmed PERMANENT on the K-homology route. The argument has three independent legs, two of which are on my own NCG/K-theory axis (so they are not the submersion-lens inferences VDD owns), and the third attacks the Paper-02 type-bridge conjecture directly. Substrate-first framing throughout: χ is the inheritance morphism from the substrate spectral triple `(A_K, H_K, D_K)` onto the M₂(ℂ) BdG/Nambu child; the question is whether the substrate's internal fibre-integration over the U(2)-fibre of SU(3)→CP² SURVIVES this projection, and I derive that it does not survive as a faithful push-forward — it survives only as a zero-image deletion.

**Leg B1 — The shriek/deletion discriminator IS the image-class, and the image-class is computed to be zero (decisive, NCG-side).** This is the core. Distinguish two operations on the same domain `A_F`:

```
SHRIEK    π_!^{CP²} : C(SU(3)) → C(CP²)   pushes the U(2)-fibre Dirac family FORWARD
                                            ⇒ image = NON-TRIVIAL integrated K-homology class
                                              (Paper 01, 1811.07824, Thm 3.4: the shriek RETAINS
                                               its fibre as the object it integrates over — Paper 01
                                               line 42; π_! is a push-FORWARD, not an annihilation)

DELETION  χ : A_F = ℂ⊕ℍ⊕M₃(ℂ) → M₂(ℂ),  χ(M₃(ℂ)) = 0
                                            ⇒ image of the M₃ summand = the ZERO class
```

The DISCRIMINATOR between them is therefore the K-theory class of the image of the deleted summand: a shriek gives a non-trivial class, a deletion gives zero. **The framework has COMPUTED this class.** Gate `S93-W2-1` (= D-1 of the Q10 workshop, registry line 298, `audit_sha256 76e5d744…496c5f99`) evaluates the Connes-Karoubi boundary `B = ι_*∘HKR` on the M₃(ℂ)-gap-class and returns

```
[φ_cd] = phi_cd_triple = (0, 0, 0) ∈ K⁰(A_K) = ℤ³,    integrality residual = 0.00e+00.
```

Registry line 288 states it directly: `B = ι_*∘HKR` is **the zero map on the gap-class**. Substitution chain (the §"Double-Check Logic" discipline applied to the discriminator):
- *Step 1.* Faithful-shriek prediction: `class(image of deleted content) ≠ 0` (Paper 01 Thm 3.4, push-forward retains the fibre).
- *Step 2.* Deletion prediction: `class(image of deleted content) = 0` (the summand is sent to 0 ∈ M₂(ℂ)).
- *Step 3.* Computed value (gate S93-W2-1): `[φ_cd] = (0,0,0)`, residual `0.00e+00`.
- *Step 4.* `(0,0,0) = 0` matches the DELETION prediction (Step 2) and CONTRADICTS the faithful-shriek prediction (Step 1).
- *Conclusion.* χ acts as a deletion on the K-homology class of the gap-sector, NOT as a faithful fibre-integration. **Reading B.**

This is my own instrument turning the adjudication — it is an explicit Connes-Karoubi computation, not a category-mismatch inference, so it is INDEPENDENT of VDD's submersion-lens Leg L1. It directly answers VDD1's confession: the antagonist the steelman could not answer from the shriek side IS decisive.

**Leg B2 — VDD1-Q1's escape hatch ("the gate is on the wrong bridge") is closed: K⁰(A_K)=ℤ³ is the entire target and the gap-class is its only image-candidate.** VDD1-Q1 asks whether an *internal* shriek `π_!^{CP²}` has a DIFFERENT K-theory image — one pairing with `K_*(C(SU(3)))`/`K_*(C(CP²))` rather than `K⁰(A_K)=ℤ³` — so the zero on ℤ³ might be evaluated on the wrong bridge. I answer: the zero is fatal regardless, and here is why. The OBJECT being mapped is the M₃(ℂ) summand of the FINITE algebra `A_K = ℂ⊕ℍ⊕M₃(ℂ)`. Its K-theory home is `K⁰(A_K) = ℤ³` — one ℤ per Wedderburn summand (ℂ, ℍ, M₃), by Morita invariance of K₀ (`K₀(M_n(ℂ)) = K₀(ℂ) = ℤ`). Whatever bridge map one builds — `ι_*∘HKR`, an internal-shriek-induced map, or any K-theoretically natural morphism — its SOURCE for the deleted content is the ℤ-generator of the M₃ summand inside `K⁰(A_K)=ℤ³`, and the gate computes that generator's image to be the zero of the triple. An internal shriek does NOT change the *source* object (the finite M₃ summand is what χ deletes regardless of which submersion we invoke); it could at most change the *target* pairing — but a faithful shriek's defining property is that it maps its fibre to a NON-zero integrated class in WHATEVER target, and the framework's computation finds zero. So the escape hatch requires a bridge map whose image of the M₃-generator is simultaneously (i) non-zero (to be a faithful shriek) and (ii) equal to the computed (0,0,0) (to match the gate) — a contradiction. **The zero-map is decisive against ANY faithful-shriek reading of χ, not just against the `ι_*∘HKR` route.** This is the answer to VDD1-Q1 that the steelman invited.

**Leg B3 — the type-bridge conjecture (Paper 02 / VDD1-A4) is categorically obstructed at the deletion/selection distinction, and this is independent of the K-theory.** VDD1-A4 + VDD1-Q2 ask whether Paper 02's almost-commutative `M⁴ × F` fibre-integration can realize "delete the M₃ summand of `A_F`" AS "select the color-singlet content under `π_!^{CP²}`." I attack the conjecture on its own terms — the deletion-of-a-Wedderburn-summand vs Peter-Weyl-selection-on-C(SU(3)) type distinction I worked through in INV2-W1-4 C-a, sharpened here into an obstruction statement:

```
TYPE OF χ:           a unital *-homomorphism of the FINITE matrix algebra A_F,
                     acting on the C¹⁶ internal spinor of D_F; χ(M₃(ℂ)) = 0 SETS A
                     WEDDERBURN SUMMAND TO ZERO. The codomain M₂(ℂ) has NO M₃ summand —
                     the summand is GONE, not retained-and-integrated.

TYPE OF π_!^{CP²}:   a class in KK^{dim U(2)}(C(SU(3)), C(CP²)) — a map of FUNCTION
                     algebras of the fibre-manifold SU(3) and coset-base CP². Its action
                     is a SELECTION: it integrates over the U(2) fibre and RETAINS the
                     U(2)-invariant (triality-0) content of each V_{(p,q)} as a class in
                     K_*(C(CP²)). The content is REORGANIZED (integrated), not DELETED.
```

The obstruction is this: a Peter-Weyl selection `π_!^{CP²}` produces a SUB-OBJECT (the U(2)-invariant subspace) that is RETAINED with a non-trivial class; a Wedderburn deletion produces a QUOTIENT (`A_F/ker χ`) in which the summand is ABSENT. For the type-bridge to close, Paper 02's `M⁴ × F` shriek would have to send the M₃(ℂ) *summand* of the FIBRE ALGEBRA to its U(2)-fibre-integrated *class* — but the fibre algebra `A_F` in the ACM construction (Paper 02, 1405.5368) is the FINITE part, and fibre-integration over the internal geometry acts on the SPECTRUM/symbol data of `D_F`, NOT by deleting Wedderburn summands of `A_F`. There is no operation in the ACM shriek that ZEROS a summand of the finite fibre algebra; the shriek INTEGRATES, and integration of a non-trivial fibre gives a non-trivial class — which is exactly what Leg B1 finds is FALSE for χ. So the two legs interlock: B3 says the type-bridge would have to produce a non-trivial integrated image (because that is what shrieks do), and B1 says the computed image is zero. **The Paper-02 type-bridge is obstructed precisely because closing it would require χ's image to be non-trivial, contradicting the gate.** This is the answer to VDD1-Q2: the type-bridge is not merely unconstructed, it is obstructed-on-this-route — any construction that made it a faithful shriek would have to violate the computed (0,0,0).

**The THIRD reading (axiom-forced kernel within an extrinsic restriction) — and why it lands Reading B, not Reading A.** I established in INV2-W1-4 C-b a reading VDD's binary did not contain: χ is NEITHER the M⁴×SU(3) shriek (agreed-not) NOR a free/arbitrary deletion. The kernel `ker(ι_*)=M₃(ℂ)` is AXIOM-FORCED *within* the BdG/triality-0 restriction (registry line 287 "forced by THEOREM D-2"; line 13208 "algebra-INVARIANT, forced by NCG axiom 5; substrate-IS structural identity"), AND for the singleton algebra ℂ⊕ℍ⊕M₃(ℂ) there is exactly ONE matrix block, so clause-(ii) of the Wedderburn rescue can ONLY kill M₃ (registry §VII.O d-singleton, line 10141). This is the EM-1 reconciliation: "forced" (287/13208) and "chosen up to χ-kernel" (17004) are both correct at different scopes. **But "axiom-forced kernel" is NOT "faithful shriek."** The forcing says: GIVEN that one restricts to the BdG/triality-0 sub-sector, the kernel is uniquely M₃. It does NOT say the restriction itself is the intrinsic fibre-integration of SU(3)→CP² — that is exactly LBA-5, and it is exactly what the zero-map refutes. So the third reading places χ as a *structurally-forced restriction onto an internal sub-sector* whose kernel is determined, but the restriction is extrinsic-to-the-submersion and its K-homology image is zero. This is "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" — the (c) verdict-name — and it is Reading B with the kernel-forcing correctly scoped (it strengthens N7-(i) to UNCONDITIONAL while leaving N7-(ii) CONDITIONAL).

**Why this makes LBA-5 PERMANENTLY undischargeable ON THE K-HOMOLOGY ROUTE (and what would be needed to reopen it).** LBA-5 discharges iff χ is exhibited as a faithful fibre-integration with a non-trivial integrated image (VDD2's discharge condition). Leg B1 computes that image to be (0,0,0); Leg B2 shows no bridge map can route around the zero (the source object is the M₃-generator of ℤ³ regardless of submersion); Leg B3 shows the type-bridge would have to violate the zero to close. The three together say: on the K-homology / Connes-Karoubi route — the ONLY route on which the framework has a computed image — LBA-5 is permanently undischargeable, because discharging it requires a non-trivial image that the framework has computed to be zero. **Honest scope (the residual that keeps this a reading-adjudication, not a closed obstruction theorem):** the zero-map is computed for `B = ι_*∘HKR`; a CATEGORICAL obstruction theorem (proving NO vertically-elliptic U(2)-fibre symbol can select exactly M₃ with a non-trivial image, for ALL bridge maps) is a constructive Kasparov-product compute that has NOT been run. So the workshop verdict is "Reading B on the K-homology route; LBA-5 PROMOTED-UNDISCHARGED; (c) PERMANENT-on-this-route," and the categorical construct-or-obstruct proof (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) is the residual MATH carry-forward to S111. I will not register the categorical obstruction as PROVEN on the strength of one bridge map's zero — but I WILL register that every route the framework has computed lands Reading B, and the burden of exhibiting a non-zero-image intertwiner sits with Reading A.

**CON1 conclusion.** χ is the Connes-Karoubi zero-map (deletion), established on my own NCG/K-theory axis by gate S93-W2-1 (`[φ_cd]=(0,0,0)`, residual `0.00e+00`), with VDD's submersion-lens category-mismatch as the SECOND independent axis (cross-axis convergence, the same shape as the W3/W4 "two independent derivations" at registry line 287). The reading-adjudication lands **Reading B**: LBA-5 stays PROMOTED-UNDISCHARGED, (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" confirmed PERMANENT-on-the-K-homology-route, N7-(i) UNCONDITIONAL, N7-(ii) CONDITIONAL. The four atomic HK-N37 edits are EFFECTED in R3 Turn B; the categorical construct-or-obstruct proof is the residual CF.

#### CON2: Questions for van-den-dungen

**CON2-Q1 (the non-zero-image demand — the inverse of your VDD1-Q1).** You ask whether the zero on `K⁰(A_K)=ℤ³` is evaluated on the wrong bridge. I turn it around: a faithful shriek `π_!^{CP²}` is DEFINED by pushing its U(2)-fibre forward to a NON-TRIVIAL integrated K-homology class (Paper 01 Thm 3.4). So Reading A does not merely need "the zero is on the wrong bridge" — it needs to EXHIBIT a bridge map under which the M₃-generator's image is NON-ZERO. Can you name the target K-group and the specific non-zero class `[π_!^{CP²}(M₃-generator)] ≠ 0` that your internal shriek produces? If you can name it, Reading A has a live re-route and the workshop stays open on this leg. If you cannot — if every K-theoretically natural target for the deleted M₃ summand routes back through its ℤ-generator in `K⁰(A_K)=ℤ³`, where the gate computes (0,0,0) — then the zero-map is decisive (my Leg B2) and the only honest reading is deletion. I am asking you to produce the non-zero class or concede it does not exist on disk.

**CON2-Q2 (the deletion-vs-selection type obstruction — does Paper 02 ZERO a summand or INTEGRATE it?).** Your A4 type-bridge needs Paper 02's `M⁴ × F` fibre-integration to realize `χ(M₃(ℂ))=0` as the color-singlet selection. But a Peter-Weyl selection RETAINS the U(2)-invariant content as a sub-object with a non-trivial class, whereas χ DELETES the M₃ summand (the codomain M₂(ℂ) has no M₃ block — it is GONE, not retained-and-integrated). Concretely: in the Paper 02 ACM construction, does fibre-integration over the internal geometry ever produce a *quotient* of the finite fibre algebra `A_F` in which a Wedderburn summand is ABSENT — or does it only produce a *sub-object* (the integrated-invariant content) that is RETAINED? If Paper 02's shriek only retains (never deletes), then the type-bridge cannot deliver `A_F → A_F/M₃`, and your A4 conjecture is obstructed at the algebra level BEFORE any K-theory. I want your reading of whether the ACM fibre-integration is a retention (selection) or can be a deletion (quotient) — because the whole type-bridge turns on this.

**CON2-Q3 (the interlock — can the type-bridge close WITHOUT contradicting the computed zero?).** My Legs B1 and B3 interlock: B3 says closing the type-bridge requires χ's image to be the non-trivial integrated class a shriek produces; B1 computes that image to be (0,0,0). So any construction that makes χ a faithful shriek must EITHER produce a non-zero image (contradicting gate S93-W2-1) OR not be a faithful shriek. Is there ANY consistent escape — e.g. does the internal-shriek image live in a DIFFERENT K-functor (K-homology of `C(CP²)` rather than K₀ of `A_K`) where the gate's `K⁰(A_K)=ℤ³` zero simply does not constrain it? If yes, the two legs do NOT interlock and Reading A survives on a "different-K-functor" footing — but then you owe me the statement that the `C(CP²)`-side image is non-zero AND the demonstration that this non-zero `C(CP²)`-class is what χ-on-`A_F` actually computes (the intertwiner). If no such different-functor escape exists, the interlock holds and the type-bridge is obstructed-on-this-route.

**CON2-Q4 (the R2 convergence target — do you co-sign Reading B as the reading-adjudication, construction as the CF?).** Pinning the end-state before R2, symmetric to your VDD2-Q3/Q4: do you AGREE the reading-adjudication closes as **Reading B on the K-homology route** — LBA-5 PROMOTED-UNDISCHARGED, (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" PERMANENT-on-this-route, N7-(i) UNCONDITIONAL, N7-(ii) CONDITIONAL — with the CATEGORICAL construct-or-obstruct proof (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) as the residual MATH carry-forward to S111? Or do you hold that one bridge map's zero is insufficient to close the reading even provisionally, in which case the entire HK-N37 update routes as a single 4-field CF and NO register edit is effected this workshop? My lean is the former (reading-adjudication closes Reading B; four HK-N37 edits effected atomically in R3 Turn B; construction is the CF) — I want your co-sign or your reason to defer, so we route HK-N37 atomically either way per the `Investigating-Workshops.md §Q1`-vs-CF distinction.

**CON2-Q5 (the standing-prior check — is there a leg of Reading A that does NOT route through a non-trivial image?).** My entire refutation assumes Reading A's faithfulness claim REQUIRES a non-trivial integrated image (because that is what shrieks do, Paper 01 line 42). Is there a weaker form of "faithful internal fibre-integration" that does NOT require a non-trivial K-homology image — e.g. a degenerate/measure-zero shriek, or a fibre-integration whose image is legitimately the zero class (a "trivial-fibre" or "rationally-trivial" integration) and is STILL faithful in some defensible sense? If such a notion exists in your Kasparov-submersion corpus, then my Leg B1's discriminator ("zero image ⇒ deletion not shriek") is too strong, and a zero-image shriek would discharge LBA-5 after all. I do not believe this notion is available (a zero-image shriek is a contradiction in terms — the shriek is the push-forward of a non-trivial fibre Dirac family), but you own the submersion corpus and I want you to confirm there is no zero-image-but-still-faithful shriek that rescues Reading A from the (0,0,0).

---

## Round 2 — van-den-dungen: Follow-up

### CONVERGENCE

**I concede the verdict: Reading B lands. My R1 load-bearing conjecture (the Paper-02/Paper-05 type-bridge, VDD1-A4) does not survive, and I retract it as a live re-route.** This is the honest motion VDD1 already pre-flagged ("Reading A has one antagonist it cannot answer from the shriek side alone"). What CHANGED between R1 and R2 is that I went back to my own corpus to test the type-bridge against the actual ACM construction, rather than against my recollection of it — and the construction refutes the conjecture on its own terms, before any K-theory. I now agree with CON1's Reading B and with all three of its legs. Five points of convergence, in descending order of load-bearingness:

**1. The type-bridge is obstructed at the algebra level — and this is decided by MY paper, not connes's K-theory (answers CON2-Q2 against my own R1-A4).** I re-read `05_2014_van_den_Dungen_Globally_Non_Trivial_ACM.md` (1405.5368) this round, specifically to discharge the A4 conjecture I floated in R1. The ACM construction does NOT do what A4 needed it to do. In the globally-non-trivial ACM the finite algebra `A_F = ℂ ⊕ ℍ ⊕ M₃(ℂ)` is FIXED (file 05, line 58); the bundle topology lives in the crossed product over the base, `A_nontrivial = C₀(P) ⋊ G` (file 05, lines 69, 81-83), and fibre-integration / the spectral action picks up TOPOLOGICAL contributions on the base — index = topological charge, instanton number ν, Chern-Simons terms (file 05, lines 86-94, 120-122). **At no point does the ACM shriek delete a Wedderburn summand of `A_F`.** There is no operation in the construction that sends `A_F → A_F / M₃(ℂ)`. The fibre-integration REORGANIZES the base-side data (it produces a topological-charge term in `S_spec`) while the finite fibre algebra is carried INTACT through the crossed product. So CON2-Q2's dichotomy — "does Paper 02/05 fibre-integration produce a QUOTIENT (summand ABSENT) or a SUB-OBJECT (integrated-invariant RETAINED)?" — resolves to RETENTION, unambiguously, on the reading of my own construction. The type-bridge cannot deliver the deletion `χ(M₃)=0` as a fibre-integration, because the ACM shriek is structurally a retention-on-the-base, never a finite-summand quotient. **This is the strongest thing I can say in R2: the conjecture I was charged to steelman is refuted by the paper I cited to support it.** I should have caught this in R1; I cite it now because the steelman's honesty obligation runs to retraction when the cited machinery turns out not to do the work.

**2. The shriek/deletion discriminator IS the image-class, and my own Paper 01 makes connes's Leg B1 decisive (answers CON2-Q5, the standing-prior check).** CON2-Q5 asks whether my Kasparov-submersion corpus contains a "zero-image-but-still-faithful" shriek that would defuse Leg B1's discriminator. I confirm from the corpus: **NO such notion exists, and the reason is structural, not a gap in my reading.** Paper 01 (1811.07824) defines vertical ellipticity as "the principal symbol σ(D) is invertible in all directions orthogonal to the fibres" (file 01, line 41); the shriek `π_!` is the bounded transform `F_D = D(1+D*D)^{-1/2}` of that vertically-elliptic operator pushed forward (file 01, lines 36, 70-73). A shriek with IDENTICALLY-ZERO image would require the vertical symbol to be non-invertible everywhere — which is the negation of vertical ellipticity. A zero-image "shriek" is not a degenerate shriek; it is the failure of the vertical-ellipticity hypothesis, i.e. it is NOT a shriek at all. So connes's Leg B1 substitution chain is sound: a faithful `π_!` predicts `class(image) ≠ 0` (Paper 01 Thm 3.4 push-forward), the gate computes `[φ_cd] = (0,0,0)` (S93-W2-1, residual `0.00e+00`), and the computed zero matches DELETION, not shriek. I have no corpus escape from this; I affirm it.

**3. The zero-map's escape hatch (my own VDD1-Q1) is closed (answers CON2-Q1 and CON2-Q3).** In R1 I asked whether the gate might be "on the wrong bridge" — whether an internal `π_!^{CP²}` pairs with `K_*(C(CP²))` rather than `K⁰(A_K)=ℤ³`, so the zero need not bind. CON1's Leg B2 answers this and I now accept it: the OBJECT χ deletes is the M₃(ℂ) summand of the FINITE algebra, whose K-theory home is the ℤ-generator of `K⁰(A_K) = ℤ³` (one ℤ per Wedderburn block, Morita-invariance `K₀(M₃(ℂ)) = K₀(ℂ) = ℤ`). An internal shriek changes the TARGET pairing at most; it does NOT change the SOURCE object. So a re-route would need a bridge whose image of the M₃-generator is simultaneously (i) non-zero (to be faithful) and (ii) equal to the computed (0,0,0) (to match the gate) — a contradiction. I cannot produce the non-zero class CON2-Q1 demands; I concede it does not exist on disk. The CON2-Q3 interlock (B1 ∧ B3: closing the type-bridge requires a non-trivial image, B1 computes zero) therefore holds, and I cannot break it via a "different-K-functor" escape — point 2 shows the `C(CP²)`-side image of a faithful shriek is also non-zero by vertical ellipticity, so moving functors does not buy a zero-that-is-still-faithful.

**4. The discharge binary and the two end-states (CON2-Q4, symmetric to my VDD2-Q3/Q4).** I co-sign, having pinned them in VDD2-Q3 before connes's argument and now landing on the Reading-B branch I pre-registered: the reading-adjudication CLOSES as **Reading B on the K-homology route** — LBA-5 stays PROMOTED-UNDISCHARGED; (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" confirmed PERMANENT-on-the-K-homology-route; N7-(i) algebraic-singleton UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total CONDITIONAL. The CATEGORICAL construct-or-obstruct proof (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) is the residual MATH carry-forward to S111. I accept connes's sharpening over my R1 lean: "Reading B on the K-homology route" is NOT "categorically obstructed for ALL bridge maps" — see DISSENT for the one place I hold the scope tighter than the headline.

**5. The "axiom-forced kernel ≠ faithful shriek" third reading, and the EM-1 reconciliation.** I co-sign CON1's third-reading placement and the EM-1 scope-split connes resolved in INV2-W1-4 C-b: for the SINGLETON algebra `ℂ ⊕ ℍ ⊕ M₃(ℂ)` there is exactly one matrix block, so the χ-kernel can ONLY be M₃ (registry §VII.O d-singleton, line 10141) — "forced WITHIN the BdG/triality-0 restriction" (line 287, THEOREM D-2) and "chosen up to χ-kernel in the generic-Wedderburn sense" (line 17004) are both correct at different scopes. But forcing-the-kernel is a statement GIVEN the restriction; it is silent on whether the restriction IS the intrinsic SU(3)→CP² fibre-integration — which is exactly LBA-5, exactly what the zero-map refutes. "Axiom-forced kernel" strengthens N7-(i) to UNCONDITIONAL; it does NOT discharge LBA-5. Agreed, and this carries into the §VII.W-3 edit.

### DISSENT

I do not dissent from the VERDICT (Reading B lands; I conceded it above). My one remaining disagreement is a SCOPE refinement on what the verdict PROVES — and it cuts in connes's favor, sharpening the carry-forward rather than reopening the adjudication. NEW evidence only, from this round's corpus re-read:

**DISSENT-1 (scope of the obstruction — I hold it tighter than even CON1's "permanent-on-this-route" headline, AND I name a second decidable obstruction axis CON1 did not).** CON1 frames the obstruction as "permanent on the K-homology route" with the honest residual that a CATEGORICAL obstruction (all bridge maps) is unrun. I agree the categorical theorem is unrun. But my point 1 above (the ACM-retention argument) is NOT a K-homology argument — it is an ALGEBRA-TYPE argument, and it closes a DIFFERENT route than the zero-map closes. So the obstruction now stands on TWO independent decidable axes, not one:
- **Axis 1 (K-homology, connes's Leg B1/B2):** the computed image `[φ_cd]=(0,0,0)` on `K⁰(A_K)=ℤ³` is the deletion-signature; closed on the `ι_*∘HKR` bridge.
- **Axis 2 (algebra-type, my point 1):** the ACM shriek is a retention-on-the-base / crossed-product `C₀(P)⋊G` (file 05, lines 69, 81-94); it has NO finite-summand-quotient operation, so it CANNOT realize `A_F → A_F/M₃` as a fibre-integration — closed at the C*-algebra level, BEFORE any K-theory pairing is evaluated.

These are genuinely independent (Hybrid-Independence-Test (iii), distinct bridge-map class: K-homology boundary vs C*-algebra-homomorphism type). The dissent: CON1's "permanent-on-the-K-homology-route" UNDER-states it — the type-bridge is ALSO obstructed on the algebra-type axis, which is route-independent of which K-functor one lands in. This does NOT promote the verdict to "categorically obstructed for all bridge maps" (that still wants the explicit vertically-elliptic-symbol non-existence theorem), but it does mean the residual CF is narrower than "construct-or-obstruct on every route": Axis 2 already forecloses every ACM-fibre-integration realization, so the CF reduces to "is there a NON-ACM faithful shriek with a non-trivial image selecting exactly M₃" — and point 2 says vertical ellipticity makes a zero-image shriek a contradiction, so that residual is on a knife-edge. I want this two-axis framing in the §VII.W-3 record so a downstream consumer does not read "permanent-on-the-K-homology-route" as leaving the algebra-type route open.

**DISSENT-2 (a precision on Leg A1 I now retract, sharpening connes's R1 "genuine sharpening MISSED").** In R1 connes corrected my A1 — I had rested it on the SU(3)→4D coupling selection ("16 modes, the trivial (0,0)") rather than the SU(3)→CP² COSET selection (the full triality-0 tower). connes is right and I retract my A1 framing: the CP²-shriek retains the entire U(2)-invariant tower, not 16 modes. But the retraction STRENGTHENS Reading B, not Reading A: the larger the retained tower under a genuine shriek, the LARGER the non-trivial integrated class it would carry — and the gate still computes (0,0,0) for the WHOLE deleted M₃ summand, tower and all. So correcting A1 to its genuinely-exact coset form makes the zero-map MORE decisive, not less. I record this as dissent-from-my-own-R1, the only honest place to put it.

### EMERGENCE

**EMERGENCE-1 (the two-axis obstruction is the SAME shape as the W3/W4 algebra-axis-orthogonality discriminator — and that is why the convergence is real evidence, not shared-context agreement).** CON1 closed with the observation that its NCG/K-theory Leg B1 and my submersion-lens category-mismatch are "two independent axes, the same shape as the W3/W4 two-independent-derivations at registry line 287." I sharpen the parallel and it cuts deeper than connes stated: registry line 287 records W3 (order-parameter representation theory, landau-side) and W4 (Fredholm index, connes-side) as DISTINCT anchors on the within-cell discriminator axis (α) "K-theoretic vs representation-theoretic" (`cross-pillar-bridge-anatomy.md §"Within-cell discriminator axes"`), "never co-primary on a single chain." Our two obstruction axes have EXACTLY this structure: connes's Axis-1 (K-homology image, Fredholm-index-side) is the K-theoretic anchor; my Axis-2 (ACM algebra-type / crossed-product retention, representation-theoretic-side) is the representation-theoretic anchor. They are structurally-orthogonal companions, not co-primaries on one chain — which is PRECISELY the registry-clean anchor structure. The emergence: the χ-deletion verdict inherits the SAME orthogonal-companion anchor structure that the order-parameter W3-symmetry-type verdict already carries (registry line 287, "two independent derivations"). This is not a coincidence — it is because χ IS the inheritance morphism onto the BdG/triality-0 sector whose symmetry-type W3 classifies. The χ-is-deletion result and the W3 "abelian, forced by THEOREM D-2" result are the SAME structural fact (the kernel `ker(ι_*)=M₃` carrying the color-singlet/BdG restriction) viewed from the deletion side vs the order-parameter side. That cross-axis coherence is the closest this adjudication comes to independent confirmation per `epistemic-discipline.md` — two agents on orthogonal axes (NOT shared-context) landing the same verdict.

**EMERGENCE-2 (what a discharge would have REQUIRED — the falsifiable shape of the residual CF, substrate-first).** Casting the result substrate-first sharpens what S111's construct-or-obstruct CF must exhibit. χ is the inheritance morphism from `(A_K, H_K, D_K)` onto the M₂(ℂ) Nambu/BdG child; the substrate's internal U(2)-fibre geometry of SU(3)→CP² is real (`dirac_spectrum.py` lines 102-108). The substrate-IS question is whether that internal fibre-integration SURVIVES the projection χ as a faithful push-forward. The verdict: it survives only as a ZERO-image deletion. For LBA-5 to discharge at S111, the CF would have to exhibit a vertically-elliptic symbol `σ_v` on the U(2)-fibre whose bounded transform (i) selects exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class. Points 1+2 above say BOTH conjuncts are structurally strained: (i) requires a finite-summand quotient the ACM shriek does not provide (Axis-2), and (ii) requires a non-zero image the gate computes to be zero (Axis-1). So the CF is not "open-ended construction" — it is the highly-constrained search for a symbol that threads two independent no-go's simultaneously. That is the falsifiable shape: name `σ_v`, or the categorical obstruction theorem closes both axes at once. I flag this as the precise S111 deliverable so the CF is not mis-scoped as a generic Kasparov-product exercise.

### QUESTIONS

Three sharper follow-ups for connes's R2, having ANSWERED CON2-Q1/Q2/Q3/Q4/Q5 in CONVERGENCE above (Q1: no non-zero class exists on disk, conceded; Q2: ACM is retention not deletion, conceded via file 05; Q3: interlock holds, no different-functor escape; Q4: co-sign Reading B + construction-as-CF; Q5: no zero-image-but-faithful shriek, vertical ellipticity forbids it):

**VDD-R2-Q1 (do you accept the two-axis obstruction framing for the §VII.W-3 record?).** Per DISSENT-1, the obstruction now stands on TWO independent decidable axes: your Axis-1 (K-homology zero-image on `K⁰(A_K)=ℤ³`) and my Axis-2 (ACM algebra-type retention / crossed-product `C₀(P)⋊G`, no finite-summand-quotient operation). Do you agree the §VII.W-3 verdict text should record BOTH axes as STRUCTURAL-ORTHOGONAL-COMPANIONs (not co-primary on one chain, per the line-287 within-cell discriminator (α) structure), so "permanent-on-the-K-homology-route" is not mis-read as leaving the algebra-type route open? I think this is the correct anchor structure and it is exactly EMERGENCE-1's parallel — but you own the registry-landing discipline and I want your read before R3 Turn B effects it.

**VDD-R2-Q2 (the residual CF scope — is it narrower than "construct-or-obstruct on every route"?).** Given Axis-2 forecloses every ACM-fibre-integration realization at the C*-algebra level (file 05 has no summand-quotient operation), the residual S111 CF reduces to: "does a NON-ACM vertically-elliptic symbol on the U(2)-fibre select exactly M₃ with a non-trivial image" — which point 2's vertical-ellipticity argument puts on a knife-edge (a zero-image shriek is a contradiction in terms). Do you agree the CF should be FRAMED this narrowly (EMERGENCE-2: name `σ_v` threading both no-go's, or prove the categorical obstruction closes both axes), rather than as a generic Kasparov-product construction? I want the CF mis-scope-proofed before it propagates to S111's plan.

**VDD-R2-Q3 (one genuine open seam — does the triality-0 tower's NON-trivial class in `K_*(C(CP²))` matter at all, or is it fully severed by χ?).** Here is the one place I am not yet fully settled, and I want your K-theory read to close it. A genuine `π_!^{CP²}` WOULD carry the triality-0 tower to a non-trivial class in `K_*(C(CP²))` (that is what the coset shriek does, per Paper 01 push-forward). χ severs this: it deletes M₃ from `A_F` and never builds the `C(CP²)`-side class. So the non-trivial `C(CP²)`-class EXISTS as a mathematical object but is NOT in χ's image — χ is the deletion, the shriek is a DIFFERENT map that χ is not. My question: is there any sense in which the framework's OTHER structure (the gauge-module content of Paper 05, the 775-rank gauge module my S61 record carries) already encodes that `C(CP²)`-side triality-0 class ELSEWHERE — so that the substrate "keeps" the coset shriek's image on the gauge-module side even though χ deletes it on the BdG side? If YES, that is not a discharge of LBA-5 (χ is still deletion) but it IS a substrate-IS statement worth recording: the coset-shriek image is not lost, it is relocated to the gauge sector. If NO (the triality-0 coset class is genuinely absent from the substrate after χ), then the deletion is total and there is nothing to relocate. I lean toward this being a real EMERGENCE for R3 either way — I want your read on whether the gauge-module sector carries it.

---

## Round 2 — connes: Cross-Synthesis

### CONVERGENCE

The workshop has reached full cross-axis convergence on the verdict. van-den-dungen's R2 did the honest thing the steelman role demands: he went back to his OWN corpus (file 05, the globally-non-trivial ACM construction) to test the A4 type-bridge conjecture against the actual paper rather than his recollection of it, and the paper refuted the conjecture he was charged to steelman. I verified file 05 independently this round and confirm his reading is exact. Five points of convergence, plus the precise LBA-5 discharge state for HK-N37.

**1. The type-bridge is obstructed at the C*-algebra level by VDD's own ACM construction — and I ACCEPT this as a SECOND independent obstruction axis (answers VDD-R2-Q1 affirmatively; converges with my CON1 Leg B3).** I re-read `05_2014_van_den_Dungen_Globally_Non_Trivial_ACM.md` (1405.5368) this round to verify VDD's R2 point 1. It holds, line-by-line: the finite algebra `A_F = ℂ ⊕ ℍ ⊕ M₃(ℂ)` is FIXED (file 05, line 58); the bundle topology lives in the crossed product `A_nontrivial = C₀(P) ⋊ G` (file 05, lines 69, 81-83); fibre-integration / the spectral action picks up TOPOLOGICAL contributions ON THE BASE — index = topological charge, instanton number ν, Chern-Simons terms (file 05, lines 86-94, 104, 110, 122: "the index of the Dirac operator on a non-trivial bundle equals the topological charge"). At NO point does the ACM shriek quotient a Wedderburn summand of `A_F`. There is no `A_F → A_F/M₃(ℂ)` operation in the construction — the finite fibre algebra is carried INTACT through the crossed product while the integration reorganizes the BASE-side data. This is a C*-algebra-homomorphism-TYPE obstruction, and it is route-independent of which K-functor one lands in — it closes the type-bridge BEFORE any K-theory pairing is evaluated. This is the answer to my own CON1 Leg B3 from VDD's side: I argued the type-bridge would have to produce a non-trivial image (because that is what shrieks do); VDD now shows the ACM shriek does not even REACH the algebra-quotient step that B3's interlock presupposed. **The two arguments interlock more tightly than CON1 stated: B3 said "any ACM-realization that closed the type-bridge would contradict the computed zero"; VDD's Axis-2 says "the ACM construction cannot even ATTEMPT the deletion, because it has no finite-summand-quotient operation."** Axis-2 is logically PRIOR to my interlock — it forecloses the ACM route at the homomorphism level, so the interlock-with-the-zero is the BACKSTOP, not the front line. I co-sign Axis-2 as a STRUCTURAL-ORTHOGONAL-COMPANION to my Axis-1 (see EMERGENCE for the precise anchor structure and the §VII.W-3 text).

**2. Vertical ellipticity makes a zero-image shriek a contradiction in terms — VDD's Paper 01 corpus closes my Leg B1's standing-prior check (CON2-Q5), and I record the structural reason.** VDD confirmed from his Kasparov-submersion corpus that there is NO "zero-image-but-still-faithful" shriek, and the reason he gives is the right one and worth pinning in the record because it upgrades my Leg B1 from an empirical reading of one gate to a structural impossibility. Paper 01 (1811.07824) defines vertical ellipticity as the principal symbol `σ(D)` being invertible in all fibre-orthogonal directions (file 01, line 41); the shriek `π_!` is the pushforward of the bounded transform `F_D = D(1+D*D)^{-1/2}` of that vertically-elliptic operator (file 01, lines 36, 70-73). A shriek with IDENTICALLY-ZERO K-homology image would require the vertical symbol to be non-invertible EVERYWHERE — which is the NEGATION of the vertical-ellipticity hypothesis. So a zero-image "shriek" is not a degenerate shriek; it is the FAILURE of the defining hypothesis — it is not a shriek at all. My CON1 Leg B1 substitution chain (Step 1: faithful-shriek predicts `class(image) ≠ 0`; Step 3: gate computes `(0,0,0)`; Step 4: zero matches DELETION) is therefore sound at the structural level, not merely at the level of one computed gate: there is NO faithful-shriek interpretation of χ consistent with `[φ_cd] = (0,0,0)`, because faithfulness (vertical ellipticity) and zero-image are mutually exclusive. I had hedged in CON1 ("I do not believe this notion is available"); VDD's corpus confirms it is structurally unavailable. The discriminator is airtight.

**3. The zero-map escape hatch is closed on BOTH the source-object and the functor axes (CON2-Q1, CON2-Q3 converged).** VDD concedes he cannot produce the non-zero class CON2-Q1 demanded: the OBJECT χ deletes is the M₃(ℂ) summand of the FINITE algebra, whose K-theory home is the ℤ-generator of `K⁰(A_K) = ℤ³` by Morita invariance (`K₀(M₃(ℂ)) = K₀(ℂ) = ℤ`, one ℤ per Wedderburn block). An internal shriek changes the TARGET pairing at most, never the SOURCE object — so a re-route would need a bridge whose image of the M₃-generator is simultaneously (i) non-zero (to be faithful) and (ii) equal to the computed `(0,0,0)` (to match the gate), a contradiction. And point 2 closes the "different-K-functor" escape of CON2-Q3 from the other side: the `C(CP²)`-side image of a GENUINE shriek is ALSO non-zero by vertical ellipticity, so moving functors does not buy a zero-that-is-still-faithful. My Leg B2 (the zero is decisive against ANY faithful-shriek reading, not just the `ι_*∘HKR` route) is therefore confirmed from both directions: source-object-invariance (B2 as I stated it) AND functor-invariance (vertical ellipticity in whatever target).

**4. The reading-adjudication CLOSES as Reading B on the K-homology route; the construct-or-obstruct proof is the residual MATH CF (CON2-Q4 co-signed, end-states pinned).** VDD co-signs the two pre-registered end-states he himself pinned in VDD2-Q3, landing on the Reading-B branch:
- **Verdict (Topic 1):** χ is the Connes-Karoubi zero-map (DELETION), NOT the Kasparov shriek. **Reading B.**
- **LBA-5 (Topic 2):** stays **PROMOTED-UNDISCHARGED**; (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" confirmed **PERMANENT** (the qualifier sharpened below — see EMERGENCE/DISSENT on the two-axis scope).
- **N7 split:** N7-(i) algebraic-singleton **UNCONDITIONAL**; N7-(ii) spectral-triple-for-D_total stays **CONDITIONAL**.
- **Residual CF:** the CATEGORICAL construct-or-obstruct proof (CF-INV2-W1-4-M1 + CF-INV2-W1-4-TYPEBRIDGE) propagates to S111, framed narrowly (see EMERGENCE for the mis-scope-proofed shape, answering VDD-R2-Q2).

**5. "Axiom-forced kernel ≠ faithful shriek," and the EM-1 reconciliation carries into §VII.W-3.** VDD co-signs the third-reading placement I established in INV2-W1-4 C-b: for the SINGLETON algebra `ℂ ⊕ ℍ ⊕ M₃(ℂ)` there is exactly one matrix block, so the χ-kernel can ONLY be M₃ (registry §VII.O d-singleton, line 10141) — "forced WITHIN the BdG/triality-0 restriction" (registry line 287, THEOREM D-2; line 13208, NCG axiom 5) and "chosen up to χ-kernel in the generic-Wedderburn sense" (line 17004) are both correct at DIFFERENT scopes (the EM-1 reconciliation). But forcing-the-kernel is a statement GIVEN the restriction; it is SILENT on whether the restriction IS the intrinsic SU(3)→CP² fibre-integration — which is exactly LBA-5, exactly what the zero-map refutes. "Axiom-forced kernel" strengthens N7-(i) to UNCONDITIONAL; it does NOT discharge LBA-5. I verified the §VII.W-3 verdict-name and the substrate-IS framing land correctly in the register (registry line 17079: "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL — the substrate IS the algebraic singleton; the M₃(ℂ) sector is the axiom-forced kernel that χ kills"). The R3 Turn B atomic edit reproduces this scope-split, NOT a status change (line 17079 already records the STAGE-3-PERMANENT grade is UNCHANGED — the two-leg split is scope, not status).

**The precise LBA-5 discharge state for the ATOMIC HK-N37 update (the workshop's load-bearing deliverable).** I pin the four-facet object so R3 Turn B effects it atomically (or, if R3 surfaces a reason to defer, routes it as a single 4-field CF — never partially per the HY-B1..B5 atomicity constraint):

```
LBA-5 = "χ-killing M₃(ℂ) (restriction to the triality-0/BdG sub-sector) is
         admissible as an INTRINSIC, geometry-FORCED operation, not extrinsic."

VERDICT: LBA-5 is NOT discharged. χ is the Connes-Karoubi zero-map (deletion),
         established on TWO structurally-orthogonal-companion axes:
           Axis-1 (K-homology, connes):    [φ_cd] = (0,0,0) ∈ K⁰(A_K)=ℤ³,
                                            residual 0.00e+00 (gate S93-W2-1,
                                            registry line 298, audit_sha 76e5d744…).
           Axis-2 (C*-algebra-type, VDD):  the ACM shriek (file 05) is a
                                            retention-on-the-base via C₀(P)⋊G;
                                            it has NO finite-summand-quotient
                                            operation, so it cannot realize
                                            A_F → A_F/M₃ as a fibre-integration.
         A faithful shriek REQUIRES a non-trivial integrated image (vertical
         ellipticity, file 01 line 41); χ's image is zero on Axis-1 AND the
         deletion is structurally unreachable as a shriek on Axis-2.

STATE:   LBA-5 PROMOTED-UNDISCHARGED. (c) "EXTRINSIC RESTRICTION WITH
         AXIOM-FORCED KERNEL" PERMANENT (two-axis, not single-route — DISSENT-1).
         N7-(i) UNCONDITIONAL. N7-(ii) CONDITIONAL.

ATOMIC HK-N37 FOUR FACETS (R3 Turn B — same confidence tag on the same sub-claim):
  (a) atlas-04 N7: two-leg split (N7-(i) UNCONDITIONAL / N7-(ii) CONDITIONAL).
  (b) atlas-08 Q10: PERMANENT scoped to N7-(i), NOT the D_total spectral-triple
      claim; Q9 CLOSED → PARTIAL (NS-3).
  (c) registry §VII.W-3: verdict-name + LBA-5 PROMOTED-UNDISCHARGED + EM-1
      "forced (287/13208) vs chosen (17004)" reconciliation + the two-axis
      obstruction record (DISSENT-1 / EMERGENCE-1).
  (d) capstone-hygiene Q3: the four facets carry the SAME tag on the SAME
      sub-claim, preventing the cross-register status-drift atomicity exists to stop.
```

### DISSENT

I do not dissent from the VERDICT — Reading B lands, cross-axis converged, and VDD conceded it from his own corpus. My one motion is to ACCEPT VDD's DISSENT-1 (his scope-tightening cuts in the verdict's favor) and to add ONE further precision he did not state, on what the two-axis structure does to the PERMANENCE qualifier. NEW evidence only, from this round's file-05 + registry verification.

**DISSENT-1 (I ACCEPT VDD's two-axis sharpening, and upgrade the permanence qualifier from "permanent-on-the-K-homology-route" to "permanent on two independent decidable axes").** My CON1 headlined "(c) PERMANENT-on-the-K-homology-route" with the honest residual that a CATEGORICAL obstruction (all bridge maps) is unrun. VDD's DISSENT-1 correctly observes this UNDER-states the result: his Axis-2 (ACM algebra-type retention) is NOT a K-homology argument — it closes a DIFFERENT route (the C*-algebra-homomorphism-type route) BEFORE any K-pairing. I verified Axis-2 on file 05 this round and accept it. The consequence for the qualifier: "(c) PERMANENT" now rests on TWO independent decidable axes —
- **Axis-1 (K-homology):** computed image `(0,0,0)` on `K⁰(A_K)=ℤ³`; closed on the `ι_*∘HKR` bridge (gate S93-W2-1).
- **Axis-2 (C*-algebra-type):** the ACM shriek is `C₀(P)⋊G` retention-on-the-base with NO finite-summand-quotient (file 05, lines 69, 81-94); closed at the homomorphism level, route-independent of the K-functor.

These pass the Hybrid-Independence-Test criterion (iii) (distinct bridge-map CLASS: K-homology boundary vs C*-algebra-homomorphism type). **This does NOT promote the verdict to "categorically obstructed for ALL bridge maps"** — that still requires the explicit vertically-elliptic-symbol non-existence theorem (the residual CF). But it does mean the qualifier in §VII.W-3 should read "PERMANENT on two independent decidable axes (K-homology + algebra-type), with the all-bridge-maps categorical obstruction as the residual CF" — NOT "permanent-on-the-K-homology-route" (which a downstream consumer could mis-read as leaving the algebra-type route open). I co-sign VDD's framing and own its registry-landing.

**DISSENT-2 (a precision on the PERMANENCE that neither CON1 nor VDD-R2 stated — the two axes do not merely add, they CLOSE COMPLEMENTARY halves of the discharge condition).** Here is the one thing I hold tighter than VDD's "two independent axes" framing, and it is NEW from this round's structural reading. VDD2's discharge condition has TWO conjuncts: LBA-5 discharges iff there exists a vertically-elliptic symbol `σ_v` on the U(2)-fibre that (i) SELECTS exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class. The two obstruction axes do not redundantly attack the same conjunct — they close DIFFERENT conjuncts:
- **Axis-2 (algebra-type) attacks conjunct (i):** the ACM shriek has no finite-summand-quotient operation, so it cannot SELECT-by-deleting exactly the M₃ summand as a fibre-integration. Conjunct (i) is unreachable on the ACM route.
- **Axis-1 (K-homology) attacks conjunct (ii):** the computed image is `(0,0,0)`, so even if (i) were somehow arranged, the integrated class is TRIVIAL, not non-trivial. Conjunct (ii) fails.

So the discharge condition is a CONJUNCTION whose BOTH conjuncts are independently foreclosed — (i) by Axis-2, (ii) by Axis-1. This is STRONGER than "two axes both say Reading B": it is "the two halves of what a discharge would require are each separately impossible, by two structurally-orthogonal arguments." A downstream consumer should read this as: discharging LBA-5 requires threading BOTH a finite-summand-quotient-that-the-ACM-construction-lacks AND a non-trivial-image-that-the-gate-computes-to-be-zero — simultaneously. That is the precise sense in which (c) is PERMANENT, and it is what makes the residual CF a knife-edge rather than an open problem (EMERGENCE-2). I want this conjunct-decomposition in the §VII.W-3 record alongside the two-axis framing, because it is the structural reason the permanence is robust and not merely "two votes."

### EMERGENCE

**EMERGENCE-1 (the two-axis obstruction inherits the EXACT within-cell discriminator (α) structure of the registry line-287 W3/W4 pair — answers VDD-R2-Q1 and confirms the convergence is independent confirmation, not shared-context agreement).** I verified registry line 287 this round: it records the W3 symmetry-type result with "**Two independent derivations (landau thermodynamic; connes index-theoretic)**" — the order-parameter representation-theoretic anchor (W3) and the Fredholm-index K-theoretic anchor (W4), which `cross-pillar-bridge-anatomy.md §"Within-cell discriminator axes"` axis (α) classifies as "K-theoretic vs representation-theoretic," never co-primary on a single chain. Our two obstruction axes have EXACTLY this structure:
- **My Axis-1** (K-homology image, Fredholm-index-side) is the **K-theoretic** anchor.
- **VDD's Axis-2** (ACM crossed-product `C₀(P)⋊G` retention, the representation/algebra-homomorphism-type-side) is the **representation-theoretic** anchor.

They are STRUCTURAL-ORTHOGONAL-COMPANIONs, NOT co-primaries on one chain — which is precisely the registry-clean anchor structure (`registry-landing.md`: cross-corner co-primary FORBIDDEN; structural-orthogonal-companion is the correct structure when both axes are independently registry-eligible). **So I answer VDD-R2-Q1 with a YES that is sharper than "agree to record both axes": the §VII.W-3 text MUST record them as STRUCTURAL-ORTHOGONAL-COMPANIONs, NOT SOURCE-DOUBLE-CITE-CO-PRIMARY** — because the two axes live on the orthogonal halves of the algebra-axis (Axis-1 = algebra-INVARIANT spectrum/index functional; Axis-2 = algebra-DEPENDENT homomorphism-type), and a co-primary chain across that orthogonality is exactly what `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` forbids at K=3 MANDATORY. And the deeper structural fact VDD's EMERGENCE-1 named is correct and load-bearing: this is NOT a coincidence of two arguments happening to agree. χ IS the inheritance morphism onto the BdG/triality-0 sector whose symmetry-type W3 classifies; the "χ-is-deletion" result (the kernel `ker(ι_*)=M₃` viewed from the deletion side) and the W3 "abelian, forced by THEOREM D-2" result (the same kernel viewed from the order-parameter side) are the SAME structural fact — the color-singlet/BdG restriction — viewed through two orthogonal lenses. Two agents on genuinely orthogonal axes (NOT shared priors) landing the same verdict is the closest this adjudication comes to independent confirmation per `epistemic-discipline.md §"What Does NOT Count as Evidence"` item 2 — and it qualifies BECAUSE the axes are structurally orthogonal, not because we agree.

**EMERGENCE-2 (the residual CF is a TWO-CONJUNCT knife-edge, not an open construction — answers VDD-R2-Q2 with the mis-scope-proofed S111 deliverable).** VDD-R2-Q2 asks whether the residual CF is narrower than "construct-or-obstruct on every route." YES — and DISSENT-2's conjunct-decomposition gives the precise shape. Axis-2 forecloses every ACM-fibre-integration realization at the C*-algebra level (file 05 has no summand-quotient operation), so the residual is NOT "is there any faithful shriek" — it is the highly-constrained search: **does a NON-ACM vertically-elliptic symbol `σ_v` on the U(2)-fibre exist whose bounded transform simultaneously (i) selects exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class?** Both conjuncts are structurally strained from opposite directions: (i) requires a finite-summand-quotient the ACM construction does not provide (Axis-2), and (ii) requires a non-zero image the gate computes to be zero (Axis-1) AND that vertical ellipticity forbids from being zero-while-faithful (point 2). So the S111 CF deliverable is sharp and falsifiable: **NAME the symbol `σ_v` that threads both no-go's simultaneously, OR prove the categorical obstruction theorem that closes both conjuncts at once** (the vertically-elliptic-symbol non-existence theorem on the U(2)-fibre). It is NOT a generic Kasparov-product exercise. I co-sign VDD's EMERGENCE-2 framing and add: because the two conjuncts are foreclosed by two STRUCTURALLY ORTHOGONAL arguments, the categorical obstruction theorem (if it lands at S111) would itself be a two-axis structural result — which is why CF-INV2-W1-4-M1 (the construction) and CF-INV2-W1-4-TYPEBRIDGE (the type-obstruction) are genuinely TWO carry-forwards, not one: M1 targets conjunct (ii) [the image], TYPEBRIDGE targets conjunct (i) [the selection-by-deletion].

**EMERGENCE-3 (the coset-shriek's non-trivial `K_*(C(CP²))` class is NOT lost — it is RELOCATED to the gauge-module sector, NOT in χ's image — answering VDD-R2-Q3, with the precise substrate-IS statement).** This is the genuine open seam VDD-R2-Q3 flagged, and my K-theory read closes it as a real EMERGENCE. The structure is this. A GENUINE coset shriek `π_!^{CP²}` carries the triality-0 tower to a non-trivial class in `K_*(C(CP²))` (Paper 01 pushforward, by vertical ellipticity — point 2). That class EXISTS as a mathematical object. χ does NOT build it — χ deletes M₃ from `A_F` and is the DIFFERENT map (the deletion). The question is whether the substrate's OTHER structure already encodes that `C(CP²)`-side triality-0 class elsewhere. **My read: YES, and the locus is exactly the gauge-module sector of file 05.** Here is the derivation. File 05's central object is the GAUGE MODULE — "a proper subset of principal modules" (file 05, abstract + lines 72-77) — and the spectral action on the non-trivial bundle picks up the topological-charge / Chern-class / instanton-number content ON THE BASE (file 05, lines 86-94, 120-122; "the index of the Dirac operator on a non-trivial bundle equals the topological charge"). The triality grading is the SU(3) center character carried by the U(1) ⊂ U(2) vertical factor (registry line 287). So the triality-0 / U(2)-invariant content that a genuine `π_!^{CP²}` would integrate is PRECISELY the content that the ACM gauge module retains-on-the-base as topological-charge data — it is the gauge-connection / Chern-class sector, NOT the BdG/Nambu sector that χ projects onto. **The substrate-IS statement (NOT a discharge of LBA-5):** the coset-shriek image is not annihilated by the substrate; it is RELOCATED. χ severs it from the BdG/M₂(ℂ) child (deletion, Axis-1 zero), but the SAME triality-0 content lives — as the topological-charge / Chern-class data of the ACM gauge module — in the GAUGE sector (file 05's `C₀(P)⋊G` base-side retention). The two are different maps onto different children of the SAME substrate `(A_K, H_K, D_K)`: χ is the inheritance morphism onto the BdG child (where the M₃ content is DELETED, image zero); the gauge-module structure is the retention onto the base (where the triality-0 content is KEPT as topological charge). **This is why VDD's Axis-2 is exactly right and the seam closes cleanly:** the ACM shriek is a retention-on-the-base PRECISELY BECAUSE the triality-0 content it would integrate is the gauge-sector topological-charge content, not a BdG-summand to be deleted. So:
- χ on the BdG side: DELETION. The triality-0 coset class is ABSENT from χ's image (Axis-1, `(0,0,0)`). LBA-5 NOT discharged.
- The gauge-module sector: RETENTION. The SAME triality-0 content is KEPT as the ACM topological-charge / Chern-class data (file 05, lines 86-94).

This is NOT a discharge — χ is still the deletion, LBA-5 still PROMOTED-UNDISCHARGED — but it is a substrate-IS EMERGENCE worth recording in R3: **the coset-shriek's content is not lost when χ deletes it; it is the gauge-sector's topological charge.** The deletion is total ON THE BdG CHILD; the content is relocated, not destroyed, on the substrate. I flag this for R3 as the resolution of VDD-R2-Q3's "relocated vs total-absence" fork: relocated to the gauge sector (file 05 retention), absent from χ's image (Axis-1 zero) — both true, on different children. This also tightens EMERGENCE-1's "same structural fact viewed from two lenses": the deletion-side (χ, Axis-1) and the retention-side (gauge module, Axis-2 / file 05) are the SAME triality-0 content, which is why the two obstruction axes are orthogonal companions and not independent coincidences — they are the two faces of where the triality-0 content goes (deleted from BdG, kept in gauge).

---

## Round 3 — van-den-dungen: Follow-up

### CONVERGENCE

**Final position on the type-bridge, stated without hedging: it does NOT close, and it is not merely "unconstructed" — it is a CATEGORY ERROR.** My R1 load-bearing conjecture (VDD1-A4: that Paper 02/05's almost-commutative `M⁴ × F` fibre-integration could realize the deletion `χ(M₃(ℂ))=0` AS the color-singlet selection of `π_!^{CP²}`) was already retracted in my R2 CONVERGENCE point 1, against my own corpus. R3 hardens that retraction from "the cited machinery does not happen to do the work" to "the cited machinery CANNOT do the work, for a type reason that is decidable before any computation." I re-verified file 05 this round to be certain I am committing the right statement to the final record:

- `A_F = ℂ ⊕ ℍ ⊕ M₃(ℂ)` is **FIXED** (file 05, line 58 — "spacetime × electroweak × color");
- the bundle topology lives ENTIRELY in the crossed product `A_nontrivial = C₀(P) ⋊ G` over the BASE (file 05, lines 68-70, 81-83);
- fibre-integration / the spectral action reorganizes BASE-side data into topological-charge terms — index = topological charge, instanton number ν, Chern-Simons, Chern classes (file 05, lines 86-94, 120-122; "the index of the Dirac operator on a non-trivial bundle equals the topological charge").

There is NO operation anywhere in the ACM construction that forms `A_F → A_F / M₃(ℂ)`. The finite fibre algebra is carried INTACT through the crossed product; the integration acts on the *base*, never by quotienting a Wedderburn summand of the *fibre*. So CON2-Q2's dichotomy — does the ACM fibre-integration produce a QUOTIENT (summand absent) or a SUB-OBJECT (integrated-invariant retained)? — resolves to **RETENTION, unambiguously, on the reading of my own construction.** The type-bridge is a category error because it asks a SELECTION operation (Peter-Weyl retention of the U(2)-invariant sub-object, a class in `K_*(C(CP²))`) to deliver a DELETION (a quotient of the finite matrix algebra in which M₃ is gone). These are different categorical operations — sub-object vs quotient — and no ACM shriek bridges them. I co-sign CON1's Leg B3, now from the construction side, and I record this as my final answer: **A4 is dead; the type-bridge is categorically obstructed on the ACM route, decided at the C*-algebra-homomorphism level before K-theory.**

**Everything else I conceded in R2 stands, and connes's R2 sharpened all of it; I co-sign the sharpened forms verbatim:**

**1. The discriminator is airtight at the STRUCTURAL level, not just the gate level (CON-R2 point 2).** I confirmed from my Paper 01 corpus that a "zero-image-but-still-faithful" shriek does not exist: vertical ellipticity (the principal symbol `σ(D)` invertible in all fibre-orthogonal directions, file 01 line 41) is the DEFINING hypothesis of a shriek, and an identically-zero K-homology image requires the vertical symbol to be non-invertible everywhere — the negation of the hypothesis. A zero-image "shriek" is not a degenerate shriek; it is the FAILURE of vertical ellipticity, i.e. not a shriek at all. connes's upgrade is correct and I co-sign it: Leg B1's discriminator is not "one computed gate reads zero" but "faithfulness (vertical ellipticity) and zero-image are MUTUALLY EXCLUSIVE." There is no interpretation of χ consistent with both `[φ_cd]=(0,0,0)` and faithful-shriek.

**2. The two obstruction axes close COMPLEMENTARY CONJUNCTS — I accept connes's DISSENT-2 in full, it is the right sharpening and I did not state it.** VDD2's discharge condition is a CONJUNCTION: LBA-5 discharges iff a vertically-elliptic symbol `σ_v` on the U(2)-fibre (i) SELECTS exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class. connes's DISSENT-2 observes the two axes do not redundantly attack one conjunct — **Axis-2 (my ACM algebra-type retention) forecloses conjunct (i)** (no finite-summand-quotient operation, so "select-by-deleting M₃" is unreachable as a fibre-integration), and **Axis-1 (connes's K-homology zero) forecloses conjunct (ii)** (the integrated class is computed `(0,0,0)`, trivial even if (i) were arranged). This is STRONGER than "two axes both vote Reading B": the two HALVES of what a discharge would require are each separately impossible, by two structurally-orthogonal arguments. I co-sign DISSENT-2 as the structural reason (c) is robustly PERMANENT, not "two votes," and as the reason the residual CF is a knife-edge, not an open problem.

**3. The relocation EMERGENCE (connes EMERGENCE-3) closes my VDD-R2-Q3 cleanly, and I confirm it from file 05 + my S61 gauge-module record.** I asked in VDD-R2-Q3 whether the coset-shriek's non-trivial `K_*(C(CP²))` class is RELOCATED or genuinely ABSENT after χ. connes's read is RELOCATED, and I verify the locus: the triality grading is the SU(3) center character carried by the U(1)⊂U(2) vertical factor (registry line 287), so the triality-0 / U(2)-invariant content a genuine `π_!^{CP²}` would integrate is PRECISELY the topological-charge / Chern-class content the ACM gauge module retains-on-the-base (file 05 lines 86-94, 120-122; gauge module = "proper subset of principal modules," lines 72, 77). My own S61 record (the 775-rank gauge module) is the substrate's carrier of exactly that base-side content. So: χ severs the triality-0 content from the BdG/M₂(ℂ) child (DELETION, Axis-1 zero), while the SAME content is KEPT as the gauge sector's topological charge (RETENTION, Axis-2 / file 05). I co-sign EMERGENCE-3 and add the consistency check it survives against my own memory (see EMERGENCE below): the S98 shriek-map image that DOES survive into the BdG child is the fiber-Goldstone class (the `c_s²=0` / Leggett content), a DIFFERENT class from the triality-0/M₃ content that χ deletes — so "relocated triality-0 content" and "retained Goldstone content" are two distinct K-homology classes on the same child, no contradiction.

**4. Cross-axis convergence, not shared-context agreement.** I co-sign EMERGENCE-1 from both sides: the two obstruction axes have the EXACT within-cell discriminator (α) structure of the registry line-287 W3/W4 pair (representation-theoretic anchor vs K-theoretic anchor, never co-primary on one chain). My Axis-2 (ACM crossed-product retention, algebra-homomorphism-type-side) is the representation-theoretic anchor; connes's Axis-1 (K-homology image, Fredholm-index-side) is the K-theoretic anchor. They are STRUCTURAL-ORTHOGONAL-COMPANIONs because χ IS the inheritance morphism onto the BdG/triality-0 sector that W3 classifies — the deletion-side and the order-parameter-side are the SAME structural fact (the color-singlet/BdG restriction, `ker(ι_*)=M₃`) viewed through two orthogonal lenses. This is the one form of agreement that counts as evidence per `epistemic-discipline.md §"What Does NOT Count as Evidence"` item 2 — orthogonal axes, not shared priors.

**Honest accounting of where my standing prior landed.** My VDD1 confession pre-flagged that Reading A "has one antagonist it cannot answer from the shriek side alone." My VDD2 standing prior expected Reading B. Both held. But the workshop was a genuine Q1 adjudication and not a foregone conclusion, because VDD1-A1 (triality-0 ⇔ color-singlet, exact at the irrep layer) and VDD1-A4 (the Paper 02 type-bridge candidate) were real, non-trivial, and connes and I invoked DIFFERENT machinery on them. The value the workshop added over the triage's bare "expected FAIL" is the DERIVATION: we now know WHY Reading B lands (two complementary-conjunct obstructions, both decidable on disk), not merely that it was expected to. That is the difference between a reading-adjudication and a stipulation.

### DISSENT

I do not dissent from the VERDICT — Reading B lands, two-axis converged, I conceded it from my own corpus in R2 and hardened the retraction to a category-error in R3 CONVERGENCE above. My one R3 motion is a precision on the RELOCATION result (EMERGENCE-3) that protects it from being mis-read as a partial discharge. NEW structural reading only, from this round's file-05 re-verification.

**DISSENT-1 (the relocation is NOT a "shriek-by-the-back-door" — the gauge-sector retention is a DIFFERENT map onto a DIFFERENT child, and conflating them would silently re-open LBA-5).** EMERGENCE-3 establishes that the triality-0 / U(2)-invariant content a genuine `π_!^{CP²}` would integrate is NOT lost — it lives as the ACM gauge module's topological-charge / Chern-class data (file 05 lines 86-94, 120-122). A downstream consumer could read this as "so the substrate DOES perform the coset fibre-integration after all — just on the gauge side — therefore the shriek exists and LBA-5 half-discharges." **That reading is wrong, and I want it foreclosed in the §VII.W-3 record.** The precise statement: there are TWO distinct inheritance morphisms from the SAME substrate `(A_K, H_K, D_K)` onto TWO distinct children —

```
χ        : (A_K, H_K, D_K) → BdG/Nambu child M₂(ℂ).
           Acts on the FINITE algebra A_F by χ(M₃(ℂ))=0. DELETION.
           Image of the M₃-generator on K⁰(A_K)=ℤ³ is (0,0,0) (Axis-1).
           The triality-0 content is ABSENT from χ's image.

ρ_gauge  : (A_K, H_K, D_K) → ACM gauge sector C₀(P)⋊G base-side.
           Retains the triality-0/U(2)-invariant content AS topological charge
           (file 05 lines 86-94). RETENTION. NOT a deletion, NOT χ.
```

`ρ_gauge` is the map that carries the coset content; χ is the map that deletes it. LBA-5 asks whether **χ** (the BdG inheritance morphism — the ONLY map the discharge condition is about) is the intrinsic SU(3)→CP² fibre-integration. The answer is NO, regardless of what `ρ_gauge` does: the existence of a retention-map onto a DIFFERENT child does not make the deletion-map onto the BdG child a faithful shriek. So the relocation is a substrate-IS EMERGENCE (the content is conserved on the substrate, redistributed across children), NOT a discharge or half-discharge of LBA-5. **The trap is the "same content ⇒ same map" elision; the fix is to record χ and `ρ_gauge` as DISTINCT morphisms onto DISTINCT children.** This sharpens EMERGENCE-3 rather than contesting it, and it is exactly the kind of cross-register status-drift `capstone-hygiene-gate.md` Q3 exists to prevent — so it belongs in the atomic four-facet record, not just in the workshop prose.

**DISSENT-2 (a scope-bound on the categorical-obstruction CF that neither CON-R2 nor my own R2 stated precisely — Axis-2 is route-CLOSED, but it is NOT yet a THEOREM, and the distinction matters for how S111 frames the deliverable).** connes's DISSENT-2 (the conjunct-decomposition) and my R2 DISSENT-1 (the two-axis framing) together establish that conjunct (i) is foreclosed by Axis-2 on the ACM route and conjunct (ii) by Axis-1 on the `ι_*∘HKR` route. I hold one boundary tighter for the record: **Axis-2's foreclosure is "the ACM construction (file 05) has no finite-summand-quotient operation," which is a statement about ONE construction, not a proof that NO C*-algebra-homomorphism realizing the SU(3)→CP² geometry can quotient M₃.** It is overwhelmingly likely there is none (a Wedderburn quotient is not a fibre-integration in any construction I know), but the WORKSHOP has verified it only for the ACM/Paper-05 route, which is the only relevant construction on disk. So the residual CF-INV2-W1-4-TYPEBRIDGE (targeting conjunct (i)) is NOT "Axis-2 already proved categorical obstruction of (i)" — it is "extend Axis-2's ACM-route foreclosure to the categorical statement: prove NO vertically-elliptic-symbol construction can SELECT-by-deleting M₃ as a fibre-integration." I want this scoped honestly so S111 does not register the categorical (i)-obstruction as PROVEN on the strength of the ACM route alone. This is the SAME honesty discipline connes applied to Axis-1 (the `(0,0,0)` is computed for one bridge; the all-bridge-maps obstruction is unrun) — I apply it symmetrically to Axis-2.

### EMERGENCE

**EMERGENCE-1 (the χ-vs-`ρ_gauge` split is a substrate-IS CONSERVATION statement — the triality-0 content is redistributed across children, not created or destroyed by the inheritance morphisms).** Casting DISSENT-1 substrate-first yields a clean conservation reading worth recording. The substrate `(A_K, H_K, D_K)` carries the full triality-graded content of every Peter-Weyl module `V_{(p,q)}`. Two inheritance morphisms project it onto two children: χ onto the BdG/Nambu child (where the triality-0/M₃ content is DELETED, image `(0,0,0)`), `ρ_gauge` onto the ACM gauge sector (where the SAME triality-0 content is RETAINED as topological charge, file 05 lines 86-94). The content is neither created nor destroyed at the substrate level — it is REDISTRIBUTED: deleted from one child, kept in another. This is the substrate-IS frame of `phononic-framing.md` at the single-τ-slice level (Level-1): the substrate IS the triality-graded spectral content; "where the content goes under inheritance" is a redistribution across children, not a loss. The deletion is TOTAL on the BdG child and the retention is FULL on the gauge child, and BOTH are true simultaneously because they are different morphisms — which is the precise sense in which χ being a deletion does NOT impoverish the substrate. This also explains, substrate-first, WHY the two obstruction axes are orthogonal companions and not coincidental: Axis-1 (deletion-side) and Axis-2 (retention-side) are the two FACES of the redistribution — the BdG child loses what the gauge child keeps.

**EMERGENCE-2 (consistency of EMERGENCE-3 with the S98 within-channel commutator certification — the relocated class and the surviving Goldstone class are DISTINCT, and this is a positive coherence check across two workshops).** I flag a cross-workshop coherence that strengthens the relocation result. My S98 record (the within-channel commutator workshop, mack × VDD) certified that the shriek-map image that DOES survive into the BdG child is the **fiber-Goldstone K-homology class** — the `c_s²=0` (number-conserving dispersion) and `σ/m=0` (occupation-changing) protected zeros, which are the Leggett/BdG-coherence content. EMERGENCE-3 says the triality-0/M₃ content is DELETED from the BdG child and relocated to the gauge sector. These are CONSISTENT and non-overlapping: the BdG child KEEPS the Goldstone class (S98, a class that survives χ — it is in the M₂(ℂ) Nambu image) and LOSES the triality-0/M₃ class (this workshop, Axis-1 zero). So on the BdG child there are two distinct K-homology classes with opposite fates under χ — one retained (Goldstone, S98), one deleted (triality-0/M₃, this workshop). This is not a re-derivation of S98; it is a check that the two workshops' "what survives χ onto the BdG child" verdicts do not collide — and they do not, because they concern different classes. I record it as positive cross-workshop coherence (a real check, not shared-context agreement: S98 was a different workshop on a different observable), and as the reason DISSENT-1's "same content ⇒ same map" trap is even subtler than it first appears — there are TWO content-classes on the BdG child, and only one of them is the deleted one.

**EMERGENCE-3 (the verdict's durability is LAYER-1 / topological, and therefore L_max-INDEPENDENT — which is why it is registry-PERMANENT-eligible and not a prediction-layer result).** A final placement against my four-layer hierarchy (memory: Topology / Representation / Metric / Functional). The χ-is-deletion verdict is a statement about the K-homology class of the image of a Wedderburn summand and about the C*-algebra-homomorphism type of the ACM shriek. Both are LAYER-1 (topology / K-homology) and LAYER-2 (representation / irrep-content) objects — `K⁰(A_K)=ℤ³` is fixed by Morita invariance independent of any truncation, and the ACM crossed-product type is a construction-level fact independent of L_max. So the verdict is scheme-independent and L_max-independent: it holds at every truncation, it is not a spectral-moment computation, and it carries zero free parameters. This is exactly the "structural floor vs prediction layer" sharp boundary my S73B audit pinned (21 ROBUST permanent results, all L_max-INDEP). The χ-is-deletion verdict belongs on the structural-floor side: it is a candidate for STAGE-3-PERMANENT registration (the §VII.W-3 record), NOT a prediction-layer entry subject to L-scan refinement. I record this so the atomic HK-N37 update is landed at the correct confidence TYPE — a regulator-invariant structural identity, not a numerical prediction.

### QUESTIONS

This is my final A-turn; I close my position here for connes to tabulate in the Verdict + Wrap-Up. No new questions — instead, my CLOSING POSITION on the two binary deliverables the charge requires me to state explicitly.

**CLOSING-1 (LBA-5 disposition — UNAMBIGUOUS).** **LBA-5 stays PROMOTED-UNDISCHARGED. Reading B lands.** χ is the Connes-Karoubi zero-map (DELETION), NOT the Kasparov shriek `π_!^{CP²}`. The discharge condition (a vertically-elliptic `σ_v` on the U(2)-fibre selecting exactly `ker(ι_*)=M₃(ℂ)` with a non-trivial integrated class) is a CONJUNCTION whose BOTH conjuncts are independently foreclosed on the routes the framework has on disk — conjunct (i) by Axis-2 (my ACM algebra-type retention: file 05 has no finite-summand-quotient operation), conjunct (ii) by Axis-1 (connes's K-homology zero: `[φ_cd]=(0,0,0)`, residual `0.00e+00`, gate S93-W2-1). The (c) verdict-name "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" is confirmed PERMANENT on TWO independent decidable axes (K-homology + algebra-type), recorded as STRUCTURAL-ORTHOGONAL-COMPANIONs (NOT SOURCE-DOUBLE-CITE-CO-PRIMARY — the axes are orthogonal halves of the algebra-axis per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`, cross-corner co-primary FORBIDDEN). N7-(i) algebraic-singleton UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total CONDITIONAL. I do NOT register the all-bridge-maps CATEGORICAL obstruction as PROVEN — that is the residual CF (per DISSENT-2's symmetric scope-bound on both axes).

**CLOSING-2 (atomic HK-N37 register update — EFFECTED IN-SESSION, not routed as a CF).** The reading-adjudication is a STRUCTURAL verdict (a math/physics adjudication of two competing readings per `Investigating-Workshops.md §Q1`), not a numerical compute — so it CLOSES in-workshop and the four-facet HK-N37 update is EFFECTED atomically in connes's R3 Turn B (Agent B, final writer). The four facets carry the SAME confidence tag on the SAME sub-claim (the `capstone-hygiene-gate.md` Q3 atomicity constraint):
- **(a) atlas-04 N7**: two-leg split — N7-(i) UNCONDITIONAL / N7-(ii) CONDITIONAL.
- **(b) atlas-08 Q10**: PERMANENT scoped to N7-(i) (the algebraic-singleton), NOT the D_total spectral-triple claim; Q9 CLOSED → PARTIAL (NS-3).
- **(c) registry §VII.W-3**: verdict-name + LBA-5 PROMOTED-UNDISCHARGED + the EM-1 "forced (lines 287/13208) vs chosen (line 17004)" reconciliation + the TWO-AXIS obstruction record as STRUCTURAL-ORTHOGONAL-COMPANIONs + the χ-vs-`ρ_gauge` distinct-morphisms note (DISSENT-1) so the relocation EMERGENCE is not mis-read as a partial discharge.
- **(d) capstone-hygiene Q3**: the four facets carry the same tag, preventing cross-register status-drift.

**CLOSING-3 (the residual MATH carry-forward — ONE atomic CF, two-conjunct shape).** The CATEGORICAL construct-or-obstruct proof propagates to S111 as the residual MATH carry-forward, mis-scope-proofed per EMERGENCE-2 of R2: it is NOT a generic Kasparov-product exercise but the highly-constrained two-conjunct search — **NAME a NON-ACM vertically-elliptic symbol `σ_v` on the U(2)-fibre that simultaneously (i) selects exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class, OR prove the categorical obstruction theorem closing both conjuncts at once.** CF-INV2-W1-4-M1 targets conjunct (ii) [the image]; CF-INV2-W1-4-TYPEBRIDGE targets conjunct (i) [the selection-by-deletion]. Per DISSENT-2, neither axis's foreclosure is yet a THEOREM (Axis-1 zero is one bridge; Axis-2 retention is one construction) — so the CF is a genuine future compute, not a hygiene item. My lean: the categorical obstruction lands (a zero-image shriek is a contradiction in terms by vertical ellipticity, and a Wedderburn quotient is not a fibre-integration in any construction), but that is a PRIOR, not a proof, and the proof is the S111 deliverable.

**Co-sign request to connes for R3 Turn B (the Verdict + Wrap-Up writer):** Topic 1 → **Converged** (Reading B); Topic 2 → **Converged** (LBA-5 PROMOTED-UNDISCHARGED, two-axis); Topic 3 → **Converged + Effected** (atomic HK-N37 update lands in-session, four facets atomically in your Turn B; residual categorical-obstruction proof is the single MATH CF to S111). If you concur, effect the four atomic edits and tabulate; if R3 Turn B surfaces any reason the register edit cannot be effected this session, route the ENTIRE HK-N37 update as a single 4-field CF (never partially) per the HY-B1..B5 atomicity constraint.

---

## Round 3 — connes: Cross-Synthesis (FINAL — fills Verdict + Wrap-Up)

### CONVERGENCE

**The workshop closes at FULL two-axis convergence on Reading B, and I effect the atomic HK-N37 update in this turn.** van-den-dungen's R3-A did the last thing the steelman role required: he hardened his own R2 retraction from "the cited machinery does not happen to do the work" to "the cited machinery CANNOT do the work, for a TYPE reason decidable before any computation" (R3-A CONVERGENCE). I co-sign that hardening, and I co-sign his three explicit CLOSING positions verbatim: Topic 1 → **Converged (Reading B)**; Topic 2 → **Converged (LBA-5 PROMOTED-UNDISCHARGED, two-axis)**; Topic 3 → **Converged + Effected (atomic HK-N37 in this turn; categorical-obstruction proof the single MATH CF)**. Six points of final convergence, then the EFFECTED edits in Effected-In-Session.

**1. The type-bridge is a CATEGORY ERROR, not an unconstructed conjecture — and I co-sign the upgrade from R2's "obstructed" to R3-A's "category error."** R3-A's final statement of Axis-2 is the right one and stronger than my own R2 framing: the type-bridge asks a SELECTION operation (Peter-Weyl retention of the U(2)-invariant sub-object — a class in `K_*(C(CP²))`) to deliver a DELETION (a quotient `A_F → A_F/M₃(ℂ)` in which the summand is GONE). Sub-object and quotient are DIFFERENT categorical operations; no ACM shriek bridges them, because the ACM construction (file 05, `A_nontrivial = C₀(P)⋊G`, lines 68-70/81-83) carries the finite fibre algebra INTACT through the crossed product and reorganizes only the BASE-side data into topological-charge terms (file 05 lines 86-94, 120-122). I verified file 05 independently in R2 and confirm R3-A's line-by-line reading. The decisive sharpening: this is decided at the C*-algebra-homomorphism TYPE level, BEFORE any K-theory pairing — which is why Axis-2 is logically PRIOR to my Leg-B3 interlock (B3 says "any ACM realization closing the type-bridge would contradict the computed zero"; Axis-2 says "the ACM construction cannot even ATTEMPT the deletion"). The interlock-with-the-zero is the backstop; Axis-2 is the front line.

**2. The shriek/deletion discriminator is airtight at the STRUCTURAL level (my Leg B1, upgraded by VDD's Paper-01 corpus).** I re-affirm the structural-impossibility upgrade I introduced in R2 CONVERGENCE point 2 and which VDD co-signed in R3-A CONVERGENCE point 1: a "zero-image-but-still-faithful" shriek does not exist as a notion, because vertical ellipticity — the principal symbol `σ(D)` invertible in all fibre-orthogonal directions (Paper 01, 1811.07824, file 01 line 41) — is the DEFINING hypothesis of `π_!`, and an identically-zero K-homology image requires the vertical symbol to be non-invertible everywhere, the NEGATION of the hypothesis. So my CON1 Leg B1 substitution chain (Step 1 faithful-shriek predicts `class(image) ≠ 0` per Paper 01 Thm 3.4; Step 3 gate computes `[φ_cd]=(0,0,0)`; Step 4 the zero matches DELETION, contradicts shriek) is sound not at the level of one computed gate but as a structural mutual-exclusion: faithfulness (vertical ellipticity) and zero-image cannot coexist. There is NO interpretation of χ consistent with both `[φ_cd]=(0,0,0)` (gate S93-W2-1, residual `0.00e+00`) and faithful-shriek.

**3. The zero-map escape hatch is closed on BOTH the source-object and the functor axes (my Legs B2, converged with VDD CON2-Q1/Q3).** The OBJECT χ deletes is the M₃(ℂ) summand of the FINITE algebra, whose K-theory home is the ℤ-generator of `K⁰(A_K)=ℤ³` by Morita invariance (`K₀(M₃(ℂ))=K₀(ℂ)=ℤ`, one ℤ per Wedderburn block). An internal shriek changes the TARGET pairing at most, never the SOURCE object — so a re-route would need a bridge whose image of the M₃-generator is simultaneously (i) non-zero (to be faithful) and (ii) equal to the computed `(0,0,0)` (to match the gate), a contradiction. VDD conceded in R2 he cannot name the non-zero class CON2-Q1 demanded; it does not exist on disk. And point 2 closes the "different-K-functor" escape from the other side: the `C(CP²)`-side image of a GENUINE shriek is ALSO non-zero by vertical ellipticity, so moving functors does not buy a zero-that-is-still-faithful. Leg B2 is confirmed from both directions: source-object-invariance AND functor-invariance.

**4. The two obstruction axes close COMPLEMENTARY CONJUNCTS — my DISSENT-2, co-signed by VDD R3-A CONVERGENCE point 2.** This is the structural core of why (c) is robustly PERMANENT and not merely "two votes." VDD2's discharge condition is a CONJUNCTION: LBA-5 discharges iff a vertically-elliptic symbol `σ_v` on the U(2)-fibre (i) SELECTS exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class. The two axes attack DIFFERENT conjuncts: **Axis-2 (VDD's ACM algebra-type retention) forecloses conjunct (i)** — no finite-summand-quotient operation, so "select-by-deleting M₃" is unreachable as a fibre-integration; **Axis-1 (my K-homology zero) forecloses conjunct (ii)** — the integrated class is computed `(0,0,0)`, trivial even if (i) were arranged. The two HALVES of what a discharge requires are each separately impossible, by two structurally-orthogonal arguments. Discharging LBA-5 would mean threading simultaneously BOTH a finite-summand-quotient the ACM construction lacks AND a non-trivial image the gate computes to be zero. That conjunct-decomposition is the structural reason the residual CF is a knife-edge, not an open problem.

**5. The relocation EMERGENCE and its DISSENT-1 guard (my EMERGENCE-3 + VDD R3-A DISSENT-1) — co-signed and carried into the §VII.W-3 record.** I re-affirm EMERGENCE-3 and accept VDD R3-A DISSENT-1's protective precision in full: the coset-shriek's non-trivial `K_*(C(CP²))` class is NOT lost when χ deletes M₃ — it is RELOCATED to the ACM gauge-module sector (the triality-0/U(2)-invariant content a genuine `π_!^{CP²}` would integrate IS the topological-charge/Chern-class content the gauge module retains-on-the-base, file 05 lines 86-94/120-122; VDD's S61 775-rank gauge module is its carrier). But this is NOT a discharge: there are TWO DISTINCT inheritance morphisms onto TWO DISTINCT children — `χ : (A_K,H_K,D_K) → M₂(ℂ)` BdG child (deletion, image `(0,0,0)`) and `ρ_gauge : (A_K,H_K,D_K) → C₀(P)⋊G` gauge sector (retention as topological charge). LBA-5 is about **χ** (the BdG inheritance morphism), and the existence of a retention-map `ρ_gauge` onto a DIFFERENT child does NOT make χ a faithful shriek. The trap is "same content ⇒ same map"; the fix is to record χ and `ρ_gauge` as DISTINCT morphisms. This belongs in the atomic §VII.W-3 record so the relocation EMERGENCE is never mis-read as a partial discharge — a `capstone-hygiene-gate.md` Q3 cross-register status-drift the atomicity exists to prevent.

**6. The verdict is LAYER-1 (topology / K-homology) — L_max-INDEPENDENT, regulator-invariant, STAGE-3-PERMANENT-eligible (VDD R3-A EMERGENCE-3, co-signed against my four-layer hierarchy).** The χ-is-deletion verdict is a statement about (Layer-1) the K-homology class of the image of a Wedderburn summand — `K⁰(A_K)=ℤ³` fixed by Morita invariance independent of any truncation — and (Layer-2) the C*-algebra-homomorphism TYPE of the ACM shriek, a construction-level fact independent of L_max. It is scheme-independent, L_max-independent, carries zero free parameters: it holds at every truncation, it is not a spectral-moment computation. This places it firmly on the structural-floor side of my S73B "structural floor vs prediction layer" boundary (21 ROBUST permanent results, all L_max-INDEP). So the atomic HK-N37 update is landed at the correct confidence TYPE — a regulator-invariant structural identity, NOT a prediction-layer entry subject to L-scan refinement. This is exactly why the verdict CONFIRMS the conditional structure rather than changing a status: the W0b register pre-staging already carried STAGE-3-PERMANENT, and Reading B landing is what KEEPS N7-(ii) CONDITIONAL and (c) PERMANENT — the workshop converts "Reading B EXPECTED, NOT yet run" into "Reading B DERIVED, two-axis," a scope confirmation, not a grade change.

**The W0b pre-staging vs the workshop verdict (what this turn actually changes in the register).** The atlas-04 N3/N7, atlas-08 Q9/Q10, and registry §VII.W-3 cells were pre-staged at S110 W0b (the housekeeping pre-pass) carrying the two-leg split structurally and tagging LBA-5 "PROMOTED-UNDISCHARGED pending WS-M1-INTERTWINER (S110 W1, NOT yet run); Reading B EXPECTED." The workshop has now RUN. My atomic edit is the genuine state-transition: "NOT yet run / Reading B EXPECTED" → "WS-M1-INTERTWINER RAN, Reading B LANDED via TWO structurally-orthogonal-companion obstruction axes (Axis-1 K-homology zero + Axis-2 ACM algebra-type retention), conjunct-complementary; (c) PERMANENT on two decidable axes; the all-bridge-maps categorical obstruction is the residual CF (NOT proven)." STAGE-3-PERMANENT grades UNCHANGED throughout (scope, not status — line 17079's existing discipline preserved).

### DISSENT

I do not dissent from the VERDICT — Reading B lands, two-axis converged, both agents co-signed all three Topics, and VDD conceded from his own corpus. I accept VDD R3-A DISSENT-1 (the χ-vs-`ρ_gauge` distinct-morphisms guard) and DISSENT-2 (the symmetric scope-bound: Axis-2's ACM foreclosure is one construction, not yet a categorical theorem — exactly the honesty discipline I applied to Axis-1's one-bridge zero) in full. My one R3-B motion is a single registry-landing-DISCIPLINE precision that governs HOW the two-axis structure is recorded in §VII.W-3 — it sharpens, does not contest. NEW (registry-discipline reading), from this turn's verification of the line-287 anchor structure.

**DISSENT-1 (the two axes are recorded as STRUCTURAL-ORTHOGONAL-COMPANIONs, and the registry-discipline reason is decisive — answering VDD-R2-Q1 with the binding rule, not just agreement).** I confirmed registry line 287 records the W3/W4 pair as "**Two independent derivations (landau thermodynamic; connes index-theoretic)**" — the within-cell discriminator axis (α) "K-theoretic vs representation-theoretic" (`cross-pillar-bridge-anatomy.md §"Within-cell discriminator axes"`), never co-primary on a single chain. Our two obstruction axes inhabit EXACTLY this structure: my Axis-1 (K-homology image, Fredholm-index-side) is the **K-theoretic / algebra-INVARIANT** anchor; VDD's Axis-2 (ACM crossed-product `C₀(P)⋊G` retention, algebra-homomorphism-type-side) is the **representation-theoretic / algebra-DEPENDENT** anchor. The binding rule: per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` (MANDATORY at K=3), a SOURCE-DOUBLE-CITE-CO-PRIMARY chain ACROSS the algebra-INVARIANT ↔ algebra-DEPENDENT orthogonality is STRUCTURALLY FORBIDDEN (`registry-landing.md §SOURCE-DOUBLE-CITE-CO-PRIMARY` Detection criterion 4: "Both anchors must be on the same algebra-axis cell"). The two axes are on OPPOSITE cells — so they MUST be recorded as STRUCTURAL-ORTHOGONAL-COMPANIONs, never co-primary. This is not a stylistic choice; it is the only registry-clean anchor structure available, and §VII.W-3's edit reflects it: "PERMANENT on TWO independent decidable axes (K-homology + algebra-type), recorded as STRUCTURAL-ORTHOGONAL-COMPANIONs — NOT SOURCE-DOUBLE-CITE-CO-PRIMARY."

**DISSENT-2 (the qualifier reads "two independent decidable axes," NOT "permanent-on-the-K-homology-route" — and NOT "categorically obstructed for all bridge maps").** My CON1 headlined "(c) PERMANENT-on-the-K-homology-route." VDD R2/R3 correctly observed this UNDER-states the result (Axis-2 closes the algebra-type route independently). I hold the upper bound symmetrically tight, per VDD R3-A DISSENT-2 which I co-sign: Axis-2's foreclosure is "the ACM construction (file 05) has no finite-summand-quotient operation" — a statement about ONE construction, NOT a proof that NO C*-algebra-homomorphism realizing SU(3)→CP² can quotient M₃; Axis-1's zero is computed for ONE bridge (`ι_*∘HKR`), NOT for ALL bridge maps. So the §VII.W-3 qualifier is precisely "PERMANENT on two independent decidable axes (K-homology + algebra-type), with the all-bridge-maps categorical obstruction as the residual CF" — neither under-stated to one route nor over-stated to a proven categorical theorem. The all-bridge-maps obstruction is the S111 deliverable, registered NOT as proven.

### EMERGENCE

**EMERGENCE-1 (the cross-axis convergence IS independent confirmation per `epistemic-discipline.md`, and the structural reason is that χ IS the inheritance morphism W3 classifies).** The two obstruction axes are not two arguments that happen to agree — they are the two FACES of one structural fact. χ is the inheritance morphism from `(A_K, H_K, D_K)` onto the BdG/triality-0 sector whose symmetry-type W3 (order-parameter representation theory, landau-side) and W4 (Fredholm index, connes-side) classify at registry line 287. The "χ-is-deletion" result (the kernel `ker(ι_*)=M₃` viewed from the deletion side, Axis-1) and the W3 "abelian, forced by THEOREM D-2" result (the same kernel viewed from the order-parameter side, Axis-2) are the SAME color-singlet/BdG restriction viewed through two orthogonal lenses. Two agents on genuinely orthogonal axes (NOT shared priors — VDD's Kasparov-submersion corpus vs my Connes-Karoubi K-theory) landing the same verdict is the ONE form of agreement that counts as evidence per `epistemic-discipline.md §"What Does NOT Count as Evidence"` item 2 — and it qualifies BECAUSE the axes are structurally orthogonal, not because we converged. This inherits the exact structure of the line-287 "two independent derivations" precedent.

**EMERGENCE-2 (the substrate-IS CONSERVATION reading — the triality-0 content is REDISTRIBUTED across children, not lost; co-signing VDD R3-A EMERGENCE-1).** Cast substrate-first, the relocation result (my EMERGENCE-3) is a clean conservation statement. The substrate `(A_K, H_K, D_K)` carries the full triality-graded content of every Peter-Weyl module `V_{(p,q)}`. Two inheritance morphisms project it onto two children: χ onto the BdG/Nambu child (triality-0/M₃ content DELETED, image `(0,0,0)`) and `ρ_gauge` onto the ACM gauge sector (the SAME triality-0 content RETAINED as topological charge). The content is neither created nor destroyed at the substrate level — it is REDISTRIBUTED: deleted from one child, kept in another. This is `phononic-framing.md` at the Level-1 single-τ-slice: the substrate IS the triality-graded spectral content; "where the content goes under inheritance" is a redistribution, not a loss. The deletion is TOTAL on the BdG child and the retention is FULL on the gauge child, BOTH true simultaneously because they are different morphisms — the precise sense in which χ being a deletion does NOT impoverish the substrate, and the substrate-first reason the two obstruction axes are orthogonal companions: Axis-1 (deletion-side) and Axis-2 (retention-side) are the two faces of the redistribution.

**EMERGENCE-3 (cross-workshop coherence with S98 — the BdG child carries TWO distinct K-homology classes with OPPOSITE fates under χ; co-signing VDD R3-A EMERGENCE-2).** A positive coherence check that survives against the S98 within-channel-commutator record (a DIFFERENT workshop on a DIFFERENT observable — so not shared-context agreement). S98 certified that the shriek-map image that DOES survive into the BdG child is the **fiber-Goldstone K-homology class** (`c_s²=0` number-conserving dispersion + `σ/m=0` occupation-changing protected zeros — the Leggett/BdG-coherence content, IN the M₂(ℂ) Nambu image). This workshop says the triality-0/M₃ content is DELETED from the BdG child (Axis-1 zero). These are CONSISTENT and non-overlapping: on the BdG child there are TWO distinct K-homology classes with OPPOSITE fates under χ — one RETAINED (Goldstone, S98), one DELETED (triality-0/M₃, this workshop). This is not a re-derivation of S98; it is a check that the two workshops' "what survives χ onto the BdG child" verdicts do not collide — and they do not, because they concern different classes. It is also why VDD's DISSENT-1 "same content ⇒ same map" trap is even subtler than it first appears: there are TWO content-classes on the BdG child, and only the triality-0/M₃ one is the deleted one.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | χ = Kasparov shriek (Reading A) vs Connes-Karoubi zero-map (Reading B) | VDD1, CON1 | **Converged** | **Reading B.** χ is the Connes-Karoubi zero-map (DELETION), NOT the internal-shriek `π_!^{CP²}`. Decided on TWO structurally-orthogonal-companion axes: Axis-1 (K-homology) `[φ_cd]=(0,0,0) ∈ K⁰(A_K)=ℤ³`, residual `0.00e+00` (gate S93-W2-1, registry line 298, audit_sha `76e5d744…`); Axis-2 (C*-algebra-type, VDD file 05) the ACM shriek `C₀(P)⋊G` is retention-on-the-base with NO finite-summand-quotient operation. A faithful shriek REQUIRES a non-trivial integrated class (vertical ellipticity, Paper 01 file 01 line 41) — structurally incompatible with a zero image. The irrep-selection match (triality-0 ⇔ color-singlet, registry line 287) is exact one categorical layer ABOVE the discriminator and does NOT survive to the K-theory class. |
| 2 | LBA-5 discharged or PROMOTED-UNDISCHARGED | cross | **Converged** | **LBA-5 stays PROMOTED-UNDISCHARGED.** The discharge condition is a CONJUNCTION — a vertically-elliptic `σ_v` on the U(2)-fibre that (i) SELECTS exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated class — and BOTH conjuncts are independently foreclosed on the routes on disk: (i) by Axis-2 (no finite-summand-quotient operation in the ACM construction), (ii) by Axis-1 (computed image `(0,0,0)`). (c) "EXTRINSIC RESTRICTION WITH AXIOM-FORCED KERNEL" confirmed PERMANENT on two independent decidable axes. N7-(i) algebraic-singleton UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total CONDITIONAL. "Axiom-forced kernel" (singleton algebra ⇒ kernel can ONLY be M₃) ≠ "faithful shriek": the EM-1 reconciliation ("forced" 287/13208 vs "chosen" 17004 — both correct at different scopes) strengthens N7-(i), does NOT discharge LBA-5. The all-bridge-maps CATEGORICAL obstruction is NOT registered as proven (residual CF). |
| 3 | Atomic HK-N37 N3/N7 register update (effected or single 4-field CF) | VDD2 | **Converged + Effected** | The reading-adjudication is a STRUCTURAL verdict (`Investigating-Workshops.md §Q1` math/physics adjudication), CLOSEABLE in-workshop. The four-facet HK-N37 update is **EFFECTED atomically in this R3 Turn B** (see Effected In-Session) — atlas-04 N3/N7 + atlas-08 Q10/Q9 + registry §VII.W-3, SAME confidence tag on the SAME sub-claim (`capstone-hygiene-gate.md` Q3). State-transition: "Reading B EXPECTED / NOT yet run" → "WS-M1-INTERTWINER RAN, Reading B DERIVED, two-axis." STAGE-3-PERMANENT grades UNCHANGED (scope, not status). The χ-vs-`ρ_gauge` distinct-morphisms guard (relocation EMERGENCE is conservation, NOT partial discharge) is recorded in §VII.W-3. EMERGENCE: the triality-0 content χ deletes from the BdG child is RELOCATED (not lost) to the ACM gauge sector as topological charge — a substrate-IS conservation across children. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **CF-INV2-W1-4-M1 (conjunct (ii) — the IMAGE).** Does a NON-ACM vertically-elliptic symbol `σ_v` on the U(2)-fibre of SU(3)→CP² exist whose bounded transform carries a NON-trivial integrated K-homology class while still selecting (some restriction of) the BdG sector? Axis-1 computes the `ι_*∘HKR` image `(0,0,0)` for ONE bridge; the all-bridge-maps statement — that EVERY K-theoretically natural bridge sends the M₃-generator of `K⁰(A_K)=ℤ³` to zero — is UNRUN. Vertical ellipticity (Paper 01 file 01 line 41) makes a zero-image-while-faithful shriek a contradiction in terms, so the expected outcome is the categorical obstruction; but that is a PRIOR, not a proof. **The proof is the S111 deliverable.**

2. **CF-INV2-W1-4-TYPEBRIDGE (conjunct (i) — the SELECTION-by-DELETION).** Extend Axis-2's ACM-route foreclosure to a categorical statement: prove that NO vertically-elliptic-symbol construction realizing the SU(3)→CP² geometry can SELECT-by-deleting exactly the M₃(ℂ) summand as a fibre-integration (a Wedderburn quotient is not a fibre-integration in any construction). Axis-2 has verified this only for the ACM/Paper-05 construction (the only relevant one on disk); the categorical (i)-obstruction is UNRUN. Per DISSENT-2, this is NOT yet a THEOREM — it must be scoped honestly so S111 does not register it as proven on the ACM route alone.

3. **The relocation locus — is the gauge-sector retention `ρ_gauge` a constructible morphism, or only an identification?** EMERGENCE establishes that the triality-0/U(2)-invariant content χ deletes from the BdG child is the SAME content the ACM gauge module retains-on-the-base as topological charge (file 05 lines 86-94; VDD's S61 775-rank gauge module). This is recorded as a substrate-IS conservation statement, NOT a discharge of LBA-5 (`ρ_gauge` is a DIFFERENT map onto a DIFFERENT child). OPEN as a forward EMERGENCE only: whether `ρ_gauge` can be written as an explicit inheritance morphism (parallel to χ) carrying the `C(CP²)`-side coset-shriek class — a substrate-IS structural question, NOT part of the LBA-5 discharge, and NOT a register obligation. Lowest priority; flagged so the conservation reading is not lost.

## Wrap-Up — Workshop Impact Summary

### What Changed

**(a) Structural changes** (epistemic-type reframings, the durable workshop outputs):

- **The LBA-5 register state transitions from "Reading B EXPECTED / NOT yet run" to "Reading B DERIVED, two-axis."** The W0b pre-staging recorded the two-leg split structurally but explicitly deferred the verdict to this workshop; the workshop has now RUN and DERIVED Reading B. The value the workshop adds over the triage's bare "expected FAIL" is the DERIVATION — we now know WHY Reading B lands (two complementary-conjunct obstructions, both decidable on disk), not merely that it was expected to.
- **Single-axis "permanent-on-the-K-homology-route" → two-axis "PERMANENT on two independent decidable axes (K-homology + algebra-type), STRUCTURAL-ORTHOGONAL-COMPANIONs."** The obstruction was promoted from one route to two genuinely independent routes (Hybrid-Independence-Test criterion (iii), distinct bridge-map class), recorded with the binding registry-landing discipline (NOT SOURCE-DOUBLE-CITE-CO-PRIMARY, because the axes are on opposite halves of the algebra-INVARIANT ↔ algebra-DEPENDENT orthogonality, where cross-corner co-primary is FORBIDDEN at K=3 MANDATORY).
- **"Two axes both vote Reading B" → "the two HALVES of the discharge CONJUNCTION are each separately impossible."** The conjunct-decomposition (Axis-2 forecloses conjunct (i) the selection-by-deletion; Axis-1 forecloses conjunct (ii) the non-trivial image) is the structural reason (c) is robustly PERMANENT and the residual CF is a knife-edge, not an open problem.
- **A4 type-bridge conjecture: "live re-route" → "CATEGORY ERROR (decided pre-computation)."** VDD's own steelman (R1-A4) was retracted in R2 and hardened in R3 to a category error: a SELECTION operation (sub-object retention, `K_*(C(CP²))` class) cannot deliver a DELETION (quotient `A_F → A_F/M₃`); no ACM shriek bridges sub-object and quotient.
- **NEW substrate-IS EMERGENCE: the triality-0 content is REDISTRIBUTED, not lost.** χ deletes it from the BdG child (image `(0,0,0)`); the SAME content is RETAINED by `ρ_gauge` on the ACM gauge sector as topological charge (file 05 lines 86-94). Two distinct morphisms onto two distinct children of the same substrate — a conservation reading, NOT a partial discharge.

**(b) Numerical revisions** (quantitative — no new numbers computed this workshop; the workshop is a structural reading-adjudication consuming prior computations):

- (none) — Axis-1's `[φ_cd]=(0,0,0)`, residual `0.00e+00` (gate S93-W2-1) is a PRIOR-session computed value cited, not recomputed here. No `.py` compute was run (per charge).

### What Holds

- **Gate S93-W2-1** (`= D-1` of the Q10 workshop, registry line 298, `audit_sha256 76e5d744…496c5f99`): `[φ_cd]=(0,0,0) ∈ K⁰(A_K)=ℤ³`, integrality residual `0.00e+00`, INFO. The Connes-Karoubi boundary `B = ι_*∘HKR` is the zero map on the gap-class (registry line 288). HOLDS as the Axis-1 anchor.
- **The inv-2 INV2-W1-4 N3/N7 convergence** (`n3-chi-rescue-kasparov-faithfulness.md`): N3 bare = BROKEN; N7-(i) algebraic-singleton = STAGE-3-PERMANENT UNCONDITIONAL; N7-(ii) spectral-triple-for-D_total = CONDITIONAL-on-LBA-5; G5 = UNCONDITIONAL. UNCHANGED — this workshop discharges ONLY the LBA-5 leg, which lands PROMOTED-UNDISCHARGED, preserving N7-(ii)'s conditionality.
- **§VII.W-3.ALGEBRAIC + §VII.W-3.SUBSTRATE STAGE-3-PERMANENT** (S88 W4a-17): the Wedderburn-Artin Frobenius Rescue Class theorem + A_F class-membership. UNCHANGED (the verdict confirms the conditional structure; grade is scope-annotated, not down-tagged).
- **The EM-1 reconciliation** ("forced" registry lines 287/13208 vs "chosen" line 17004 — both correct at different scopes; for the singleton algebra ℂ⊕ℍ⊕M₃(ℂ) the kernel can ONLY be M₃, registry §VII.O d-singleton line 10141). HOLDS, carried into the §VII.W-3 edit.
- **The S98 fiber-Goldstone K-homology class survives χ onto the BdG child** (`c_s²=0` + `σ/m=0` protected zeros, IN the M₂(ℂ) Nambu image). HOLDS — distinct from and non-colliding with the deleted triality-0/M₃ class.

### What Breaks or Strains

- **Reading A (χ = faithful internal-shriek `π_!^{CP²}`) is REFUTED on the routes the framework has on disk.** Both conjuncts of its discharge condition are independently foreclosed. Its strongest legs (exact triality-0 ⇔ color-singlet irrep selection; Paper 01 functoriality; KO-6 witness; Paper 02 type-bridge candidate) are real but live one categorical layer above the discriminator (irrep-selection ≠ image-class) or are category errors (the type-bridge). NOT a hygiene loss — a derived corridor-closure that sharpens the constraint surface.
- **No framework result is STRAINED by this verdict.** Reading B is the structurally-coherent outcome that PRESERVES the existing register state (N7-(ii) CONDITIONAL, (c) PERMANENT). The LBA-5 PROMOTED-UNDISCHARGED state is the status quo, now DERIVED rather than expected.
- **One honest residual:** the all-bridge-maps CATEGORICAL obstruction is NOT proven (Axis-1 zero is one bridge; Axis-2 retention is one construction). The verdict is "Reading B on two decidable axes," NOT "categorically obstructed for all bridge maps." This is the residual CF, scoped honestly so S111 does not over-register.

### Carry-Forward Computations (MATH ONLY — propagate to S111)

**CF-S111-M1-INTERTWINER (single atomic CF, two-conjunct shape).** The CATEGORICAL construct-or-obstruct proof for the M1 intertwiner. Mis-scope-proofed: NOT a generic Kasparov-product exercise, but the highly-constrained two-conjunct search.

1. **What**: NAME a NON-ACM vertically-elliptic symbol `σ_v` on the U(2)-fibre of SU(3)→CP² that simultaneously (i) SELECTS exactly `ker(ι_*)=M₃(ℂ)` AND (ii) carries a NON-trivial integrated K-homology class — OR prove the categorical obstruction theorem that closes BOTH conjuncts at once (the vertically-elliptic-symbol non-existence theorem on the U(2)-fibre). Sub-targets: CF-INV2-W1-4-M1 targets conjunct (ii) [the image — extend Axis-1's one-bridge `(0,0,0)` to the all-bridge-maps statement that every K-natural bridge sends the M₃-generator of `K⁰(A_K)=ℤ³` to zero]; CF-INV2-W1-4-TYPEBRIDGE targets conjunct (i) [the selection-by-deletion — extend Axis-2's ACM-route foreclosure to all SU(3)→CP² C*-algebra-homomorphism constructions].
2. **Inputs**: gate S93-W2-1 `[φ_cd]=(0,0,0)` (Axis-1 one-bridge anchor); `05_2014_van_den_Dungen_Globally_Non_Trivial_ACM.md` (1405.5368, lines 58/68-70/81-94/120-122; Axis-2 ACM-route foreclosure); `01_2018_van_den_Dungen_..._Riemannian_submersions.md` (1811.07824, Thm 3.4 push-forward + vertical-ellipticity def file 01 line 41); `dirac_spectrum.py` lines 102-108 (the `u(1)⊕su(2)⊕C²` reductive split, `U2_IDX=[0,1,2,7]`/`M_IDX=[3,4,5,6]`); registry §VII.W-3 (the two-axis obstruction record this CF extends to categorical); `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` (the structural-orthogonal-companion discipline).
3. **Gate**: PASS = an explicit `σ_v` threading both conjuncts is exhibited and machine-checked (⇒ LBA-5 DISCHARGES, re-opens HK-N37) — OR the categorical two-conjunct obstruction theorem is proven (⇒ (c) upgrades from "permanent on two decidable axes" to "categorically obstructed for all bridge maps," LBA-5 permanently undischargeable as a THEOREM). FAIL/INFO = neither closes; verdict stays "Reading B on two decidable axes," CF re-propagates. Expected outcome (PRIOR, not pre-judgment): categorical obstruction lands (zero-image shriek is a contradiction by vertical ellipticity; Wedderburn quotient is not a fibre-integration).
4. **Effort**: HIGH (constructive Kasparov-product / KK-theory compute on the SU(3)→CP² U(2)-fibre; multi-wave; van-den-dungen + connes joint, axes pre-assigned — VDD owns conjunct (i)/Axis-2 categorical extension, connes owns conjunct (ii)/Axis-1 categorical extension).
7. **Depends on**: gate S93-W2-1 (UPSTREAM, Axis-1 anchor); file 05 ACM construction + file 01 vertical-ellipticity (REFERENCE corpus, Axes 1+2); registry §VII.W-3 two-axis record (this workshop's EFFECTED edit — the categorical CF extends it).

### Effected In-Session (NON-MATH — completed by Agent B before terminating)

The atomic four-facet HK-N37 register update is EFFECTED atomically (all three register files + the Q3 hygiene constraint, SAME confidence tag on the SAME sub-claim). STAGE-3-PERMANENT grades UNCHANGED (scope, not status). State-transition recorded: "Reading B EXPECTED / NOT yet run" → "WS-M1-INTERTWINER RAN, Reading B DERIVED, two-axis."

- [x] **(a) atlas-04 N7 cell** — updated LBA-5 from "PROMOTED-UNDISCHARGED pending WS-M1-INTERTWINER (S110 W1, NOT yet run); Reading B expected" to "PROMOTED-UNDISCHARGED — WS-M1-INTERTWINER (S110 W1) RAN, Reading B DERIVED on two structurally-orthogonal-companion obstruction axes (Axis-1 K-homology zero + Axis-2 ACM algebra-type retention), conjunct-complementary; all-bridge-maps categorical obstruction = residual CF (NOT proven)." N7-(i) UNCONDITIONAL / N7-(ii) CONDITIONAL preserved. — `sessions/framework/Atlas/atlas-04-assumptions.md:106` (+ N3 cross-ref line 102)
- [x] **(b) atlas-08 Q10 + Q9** — Q10: "RESCUED scoped to N7-(i)+CLAIM-χ, OPEN-at-full-triple-level-pending-LBA-5; Reading B expected" → same scope, with LBA-5 state transitioned to "WS-M1-INTERTWINER RAN, Reading B DERIVED two-axis; the full-triple leg stays OPEN because Reading B = χ-is-deletion KEEPS N7-(ii) conditional." Q9: PARTIAL (NS-3) confirmed — the off-Jensen full-triple-axiom leg's dependence on LBA-5 is now DERIVED-not-pending. — `sessions/framework/Atlas/atlas-08-open-questions.md:90` (Q9) + `:97` (Q10)
- [x] **(c) registry §VII.W-3** — §VII.W-3.SUBSTRATE two-leg-split annotation (line 17079-17081): LBA-5 transitioned to DERIVED two-axis; added the TWO-AXIS obstruction record as STRUCTURAL-ORTHOGONAL-COMPANIONs (NOT SOURCE-DOUBLE-CITE-CO-PRIMARY per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`); added the conjunct-decomposition (Axis-2 ⊣ conjunct (i), Axis-1 ⊣ conjunct (ii)); added the χ-vs-`ρ_gauge` distinct-morphisms guard (relocation = conservation, NOT partial discharge); EM-1 reconciliation preserved. STAGE-3-PERMANENT grade UNCHANGED. — `sessions/permanent-results-registry.md:17079`
- [x] **(d) capstone-hygiene Q3** — the three facets above carry the SAME confidence tag ("LBA-5 PROMOTED-UNDISCHARGED, Reading B DERIVED two-axis; N7-(i) UNCONDITIONAL / N7-(ii) CONDITIONAL; (c) PERMANENT on two decidable axes; categorical obstruction = residual CF") on the SAME sub-claim, preventing cross-register status-drift. Verified by post-edit re-read of all three files.

**FALSIFIER SURFACE**: none — this workshop touches plain atlas/registry structural state, NOT the §7 falsifier/observable surface or `falsifier-master-inventory.md`. No `- [→] ROUTED-mack`.

**CURATED CAPSTONE**: none — the HK-N37 update is plain atlas/registry (atlas-04 N3/N7 + atlas-08 Q9/Q10 + registry §VII.W-3), NOT the curated capstone `phonic-exflation-equation.md`. The capstone does not narrate the LBA-5/N7-(ii) conditional state as a status-bearing claim, so no capstone-hygiene reconciliation is triggered. No `- [→] ROUTED-capstone-hygiene`.

### Closing Line

χ is the Connes-Karoubi zero-map: the substrate's internal U(2)-fibre integration of SU(3)→CP² does not survive the inheritance projection onto the BdG child as a faithful push-forward — it survives only as a zero-image deletion (Axis-1, `[φ_cd]=(0,0,0)`), and the ACM construction cannot even attempt the deletion as a fibre-integration (Axis-2, retention-on-the-base). LBA-5 stays PROMOTED-UNDISCHARGED on two structurally-orthogonal-companion axes whose conjuncts complementarily foreclose the discharge; the triality-0 content χ deletes is conserved, relocated to the gauge sector as topological charge; and the all-bridge-maps categorical obstruction — a zero-image shriek being a contradiction in vertical-ellipticity terms — is the single knife-edge MATH carry-forward to S111.
