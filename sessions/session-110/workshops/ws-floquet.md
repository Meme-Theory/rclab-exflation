# Session 110 Workshop: transit-dynamics × quantum-acoustics — WS-FLOQUET

**Date**: 2026-06-20
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: transit-dynamics-theorist (Reading A — §VII.BP resonance LIVE, inv-10 W2-4), quantum-acoustics-theorist (Reading B — resonance DEAD, inv-12 W3-2)
**Source Documents**:
- sessions/investigation/_promotion-triage.md
- sessions/investigation/_cross-investigation-synthesis.md
**Adjudication question**: Floquet dynamical reality of the registered §VII.BP in-band resonance — LIVE (max Re μ_F=0.249>0, q~0.5, a~1 assumed) vs DEAD (fraction_resonance=0 EXACT, 1248 modes in stability gaps, q≤5e-3, closest mode a=9). (a) Which q is the physical Mathieu depth? (b) Does any relic mode reach the period-2 tongue under the full 1248-mode monodromy? (c) Does "DTC-candidate" survive? berry-style monodromy-matrix consistency check is the named tie-breaker. LOAD-BEARING DOWNSTREAM: gates the WS-AS-1 Reading-A leg. Full adjudication context supplied to each agent at dispatch.

---

## Round 1 — transit-dynamics: Opening Analysis

### TD1: Reading-A steelman (resonance LIVE — DTC-candidate, inv-10 W2-4)

**The governing structure first.** Both readings solve the identical Hill/Mathieu mode equation for a relic mode driven by the residual post-transit Jensen-modulus ringing at the τ_fold slice of the Jensen flow:

```
u_k'' + Ω_k²(t) u_k = 0,   Ω_k²(t) = E_k²[1 + h_par·cos(ω_q t)]                    (1)
```

In canonical Mathieu form with z = (ω_q/2)t this is `u_k'' + [a − 2q cos(2z)] u_k = 0` with `a = (2E_k/ω_q)²`, `q = a·h_par/2`. The monodromy M over one drive period T advances (u, u') for two independent ICs; by Liouville (frictionless Hill) det M = 1, so the multipliers are a reciprocal pair e^{±μT}, and the band/gap dichotomy is fixed by the discriminant alone: `|Tr M| ≤ 2 ⇒ Re μ = 0` (stability gap, frozen relic); `|Tr M| > 2 ⇒ Re μ = arccosh(|Tr M|/2)/T > 0` (resonance band, re-pumped). **This machinery is not in dispute — both calcs implement it correctly** (inv-10 `tongue_xcheck_maxdev = 8.8e-11` between full-eigenvalue and trace-μ; inv-12 `det_max_dev = 2.86e-11`). The disagreement is entirely in the *physics inputs* fed to (1).

**The steelman, from first principles.** Reading A rests on three load-bearing claims, each substrate-grounded:

1. **The drive frequency IS in-band — verdict-pinned, not assumed.** The S101-W1-QEQ-RELIC-ODDFLOOR canonical line pins `ω_q^phys = 2.012813 M_KK` strictly inside the relic pair band `[1.6395, 10.8379] M_KK` (parsed at runtime as ground truth, `oq_drift = 0.0`, `s101_parsed_found = True`). This is the **necessary** frequency-coincidence condition for the period-2 (sub-harmonic) tongue at `a = (ω_k/ω_drive)² = 1`. My own S101 W4-2 result established this in-band placement and demoted the H-PARITY-DRIVE-EXCLUSION relic clause (d) from argument-grade to coincidence-bounded precisely *because* the resonance is frequency-live: 24 modes (14 occupied, w=248) cross `2E_k = ω_q^phys` on the tail with `Δ_res_min = 4.6e-5 ≪ guard 0.1`. The frequency door is open.

2. **The pump amplitude is positive and O(0.5) at the conservative estimate — the modulus kinetic coupling is a genuine Mathieu drive.** The pump enters via the modulus kinetic term `H_param = Σ_n (dω_n/dτ)·δτ(t)(b_n†b_n + ½)` (E1.6 in the inv-10 build). The conservative excitation-energy-per-pair pump `E_pump = E_exc/(2 n_pairs) = 0.5069 M_KK` (Parker n_pairs = 59.8, E_exc = 60.625 M_KK — canonical) maps to `q_cons = 2 E_pump/ω_q = 0.5037`. This is the standard frequency-modulation→Mathieu mapping. At this depth the period-2 tongue carries a **finite, computed** Floquet exponent: `max Re μ_F = 0.248985` (dimensionless t) at `a = 0.908`, `= 0.5012 M_KK` physical. The small-q growth law is verified independently: `dμ/dq|_{small q} = 0.4957` against the Landau-Lifshitz (Mechanics §27) theoretical `1/2` — i.e. the computed exponent obeys the analytic `μ ≈ q/2` first-tongue law to within 0.9%. **The exponent is real, positive, and analytically anchored.**

3. **The sub-harmonic response is RIGID, not fine-tuned — the DTC-candidate signature.** A discrete-time-crystal candidate requires (i) a period-2 sub-harmonic response with μ_F > 0 (present), AND (ii) rigidity of that response against drive detuning, i.e. the tongue must have *finite width in a*, not a single fine-tuned point. The computed period-2 tongue at `q_cons` spans `width(a) = 0.9677` (rigidity ratio 0.97 relative to center a=1) — a wide, robust sub-harmonic lock. `growth_per_period = e^{μ_F·π} = 2.186×` per drive period. Both DTC conditions pass: `dtc_subharmonic = True`, `dtc_rigid = True`, `dtc_candidate = True`. This is the substrate's own modes amplified by the substrate's own modulus oscillation — the acoustic analog of preheating (Kofman-Linde-Starobinsky), with the period-doubled rigidity that distinguishes a genuine time-crystal candidate from a transient ring-down.

**Why this is not refuted by the S57 μ_F = 0 / S67 "post-transit resonance impossible" results.** Those applied to a *different, pre-S101 drive* — the Leggett mode under a drive that did not access the a~1 tongue. The post-S101 in-band drive `ω_q = 2.0128 M_KK` is a genuinely new dynamical input: it places the drive *inside* the band where the period-2 tongue lives. Reading A's claim is narrow and defensible: *given* a pump of depth `q ≈ 0.5` *and* a mode at `a ≈ 1`, the Floquet exponent is unambiguously positive and the sub-harmonic is rigid. The monodromy arithmetic is exact; the Landau-Lifshitz law is satisfied; the DTC rigidity is wide. **Within its stated regime, Reading A is internally airtight.**

**The honest concession that frames the whole adjudication.** Reading A's `max Re μ_F = 0.248985` is read off a **scan of `a ∈ [0.5, 1.6]` as a free variable** (inv-10 `a_tongue` grid), with the max landing at `a = 0.908`. The calc maps the tongue *structure* but does not verify that the **actual relic spectrum places a mode there at that pump depth**. inv-12 supplies exactly that missing check: the 1248-mode spectrum gives `A_relic ∈ [0.876, 12.6]` with the closest-to-a-zone mode at `A = 9.000371` (the n=3 zone), and at the **width-aware** depth `h_par = 8.3e-4` (S101-pinned) the period-2 half-width collapses to `q_M = A·h_par/2 ≈ 4e-4 ≪ 1`. So the genuine tension is **not** "is the monodromy right" (it is, on both sides) but **(a)** which `h_par`/`q` is the physical Mathieu depth, and **(b)** whether the modes that *do* sit near a=1 (inv-12 has 23 relic modes in [0.5,1.6], the closest at A=0.965, only 0.035 from the period-2 center) fall inside a tongue *wide enough at the physical depth* to catch them. Reading A is the upper bound of the dynamical-liveness question; whether the substrate realizes it turns on the depth and the width — which is precisely the R2/R3 adjudication.

### TD2: Adjudication-question framing (physical-q convention, mode-placement a~1, tongue-reach)

The contradiction is **computable and traceable to two specific physics inputs**, not to method. I frame the three pre-registered sub-questions with the numbers that decide each, then state the WS-AS-1 stakes.

**(a) Which q is the PHYSICAL Mathieu depth — excitation-energy-per-pair (inv-10) vs width-aware modulus-depth (inv-12)?** This is the crux, and it reduces to a single fractional-modulation-depth `h_par` that the two readings assign ~607× apart:

- **inv-10** uses `q_cons = 2 E_pump/ω_q = 0.5037` from `E_pump = E_exc/(2 n_pairs)`. Read as a fractional modulation depth on the a=1 mode (where ω_k0 = ω_q), this is `h_par^{inv10} ≈ q_cons ≈ 0.504`. The physical content: the pump amplitude is set by the *excitation energy per produced pair* (E_exc/2n_pairs = 0.507 M_KK).
- **inv-12** uses `q_M = A_k·h_par/2 ≤ 5.25e-3` with `h_par = 8.3e-4` — the S101-W1-QEQ-RELIC-ODDFLOOR-pinned **full Mathieu modulation depth** (guard-floor-dominated). The physical content: the *fractional* time-dependence the modulus ringing imposes on each mode's squared frequency is 8.3e-4, period.

These are NOT two estimates of the same quantity — they are **different physical objects**. inv-10's `E_pump/ω_q` is an *energy ratio* (how much excitation energy lives per pair, relative to the drive quantum); inv-12's `h_par` is a *fractional frequency modulation* (how much the modulus ringing wiggles ω_k²). The S100a W-1 D-2 width-aware-guard lesson (in my own memory, and pinned in inv-12's convention) is explicit on which governs a *tongue width*: **the instability-tongue width is set by the FULL Mathieu depth h_par, NEVER by the suppressed amplitude of any force component rectifying through it** — the rectified-force amplitude φ_k governs THROUGHPUT only. If inv-10's q_cons is functioning as a throughput/energy quantity rather than the fractional ω²-modulation, then inv-12's h_par = 8.3e-4 is the depth that sets the tongue, and inv-10 has over-assigned the depth by ~607×. **The decisive sub-question: is `2 E_pump/ω_q` the fractional modulation of ω_k²(t), or is it the excitation-energy-per-pair (which sets how much energy the modes carry, not how hard the modulus shakes them)?** I lean toward the latter — inv-10's own E1.6 pump term `(dω_n/dτ)δτ(t)` makes the depth the *fractional change in ω per unit modulus displacement times the modulus oscillation amplitude*, and `δτ`'s physical amplitude (not E_exc/2n_pairs) is what enters. But this must be derived, not asserted.

**(b) Does ANY relic mode reach the period-2 (a=1) tongue under the full 1248-mode monodromy?** Here the honest reading is more textured than "closest mode at a=9." The full relic spectrum (inv-12) has:
- `A_relic ∈ [0.876, 12.6]` — **23 modes** in inv-10's scan window [0.5, 1.6], the closest at **A = 0.965** (|A−1| = 0.035 from the period-2 center).
- The closest-to-ANY-zone mode at `A = 9.000371` (n=3 zone), `dist = 0.000371`.

So modes DO exist near a=1 — the question is not *placement* but **tongue-reach at the physical depth**. The period-2 (n=1) half-width is `q_M = A·h_par/2`; the n-th zone half-width scales as `q_M^n` (McLachlan; S100a W-1 D-2). At inv-12's h_par = 8.3e-4: the n=1 half-width at A≈1 is ≈ 4.15e-4, so a mode 0.035 away sits **~85 half-widths outside** the period-2 tongue. At the n=3 zone (A=9) the half-width is `q_M³/64 ≈ 1.6e-9`, and the closest mode is **2.28e5 half-widths away** (my INV12 cross-check / memory). Both are deep in the gap → `fraction_resonance = 0 EXACT` (`Re_mu_relic` max = 0; `max|Tr M| = 1.99999 < 2`). **At inv-10's implied h_par ≈ 0.504, the period-2 half-width is O(0.5), which trivially catches the A=0.965 mode** (0.035 ≪ 0.5). So sub-question (b) is *downstream of (a)*: whether a near-a=1 mode reaches the tongue is entirely controlled by the depth. This is the cleanest decomposition — the disagreement collapses to a single number, h_par.

**(c) Does "DTC-candidate" survive the full mode-set?** inv-10's DTC-candidate label requires a rigid, finite-width period-2 sub-harmonic locked across a *range* of a. At h_par ≈ 0.504 the tongue width(a) = 0.97 is wide and the label is earned *for a hypothetical mode in that window*. But the DTC claim is a statement about the **realized relic spectrum**, not a hypothetical: a discrete time crystal requires the actual modes to be re-pumped sub-harmonically. If (a) lands on inv-12's depth, then `fraction_resonance = 0` and *no actual mode is re-pumped* — the rigidity is real in the (a,q) chart but **vacuous on the physical spectrum**, because the spectrum places its modes in the gaps between the (now-narrow) tongues. The DTC-candidate label then does not survive the full mode-set: it describes a tongue no relic mode occupies.

**The berry-style monodromy tie-breaker (named cross-reviewer hook for R3).** The clean arbiter is the eigenvalues of the one-period propagator M evaluated **at the actual relic `(A_relic, q_M)` points** — not on a free-a scan. inv-12 already reports this for the full 1248-mode set (`tr_relic ∈ [−2, 2]`, `Re_mu_relic = 0` exact, `det_relic_dev = 1.9e-11`). The R3 Turn-B agent MUST cite this monodromy-matrix consistency check (eigenvalues of M on the relic points) as the tie-breaker between the q-conventions: if the propagator eigenvalues at the physical `(A_relic, q_M=A·h_par/2)` points all lie on the unit circle (|Tr M| ≤ 2), the resonance is DEAD regardless of how wide a hypothetical tongue is at q≈0.5; the monodromy Tr M is the ground truth, and the analytic tongue-width estimate is only a sanity check on it. **If the monodromy at the physical points cannot be re-evaluated in-workshop, this is flagged as a forward compute** (a Mathieu-monodromy re-run on the inv-12 relic `(A_relic, q_M)` points with the h_par-convention pinned).

**WS-AS-1 stakes (LOAD-BEARING, stated explicitly).** The WS-AS-1 Reading-A leg — "the impulse-quench A_s over-production is REAL (a genuine amplitude), with the relic's locked {β_k} surviving Floquet" — is **CONTINGENT on this workshop landing on inv-12's side (relic NOT re-pumped)**. The logic: if inv-10 W2-4 wins (relic IS re-pumped, μ_F > 0 on the physical modes), then the {β_k} occupations are not locked — they grow secularly under parametric amplification, and the A_s over-production magnitude would be a *re-pumping artifact* of an unstable spectrum, not a frozen Bogoliubov output. The A_s "genuine over-production" sub-claim weakens because the amplitude is then dynamically inflated, not a fixed relic. Conversely, if inv-12 wins (`fraction_resonance = 0`, all modes in gaps), the {β_k} are genuinely frozen, the Ordered Veil survives its own modulus afterglow (diabatic-freeze R_therm = 5251.82, S_ent = 0, S95-certified), and the A_s amplitude is a real locked output whose over-production is a genuine magnitude — feeding WS-AS-1 W4-1 "A_s-overproduction-is-genuine-amplitude (not a re-pumping artifact)." **This is why WS-FLOQUET is scheduled FIRST** (master dedup flag iv): its verdict is an input to WS-AS-1's Reading-A admissibility. The verdict here must state the WS-AS-1 implication explicitly.

**Questions for quantum-acoustics (Reading B):**
1. Your `h_par = 8.3e-4` is the S101 guard-floor-dominated depth. Can you derive the *fractional* modulation of ω_k²(t) from the physical modulus oscillation amplitude `δτ` directly (the E1.6 pump term), to confirm 8.3e-4 is the fractional ω²-wiggle and inv-10's 0.504 is an energy ratio mis-mapped as a depth? I.e., what IS the physical δτ amplitude of the post-fold modulus ringing, and does `(dω/dτ)·δτ/ω² → 8.3e-4`?
2. You report 23 relic modes in [0.5, 1.6] with the closest at A=0.965. At your h_par, the period-2 half-width is ≈ 4e-4, so that mode is ~85 half-widths out. Is the monodromy `Tr M` for THAT specific mode (A=0.965, q_M = 0.965·8.3e-4/2 ≈ 4e-4) strictly inside [−2, 2]? Confirm the per-mode propagator eigenvalues at the near-a=1 modes, not just the aggregate `fraction_resonance = 0`.
3. The S67 "post-transit parametric resonance impossible" theorem — does it apply structurally at h_par = 8.3e-4 (narrow regime, q_M ≪ 1), and is the inv-12 result a *re-derivation* of S67 under the post-S101 in-band drive, or a genuinely new closure? If the former, that strengthens Reading B (the in-band coincidence is necessary but the narrow depth makes it insufficient — the monodromy confirms S67 survives the new drive).

---

## Round 1 — quantum-acoustics: Response & Cross-Synthesis

### Part 1: Response to transit-dynamics's Sections

#### Re: TD1 — Reading-A steelman

I will tag each load-bearing element of TD1 as **AGREE / DISAGREE / MISSED / EMERGES** against my domain evidence. The substrate frame is binding throughout: the object in dispute is the response of the D_K eigenvalue spectrum to the residual Jensen-modulus ringing **at the τ_fold slice of the Jensen flow** — not a field oscillating in a container. The "drive" is the modulus' own afterglow; the "modes" are the relic sector of D_K's spectrum; the "resonance" is whether that spectrum's own response operator M has eigenvalues off the unit circle.

**AGREE — the governing structure and the monodromy arithmetic (TD1 eq. (1), the band/gap dichotomy).** I fully concur that both readings solve the identical frictionless Hill equation `u_k'' + Ω_k²(t)u_k = 0`, that Liouville forces `det M = 1`, that the multipliers are a reciprocal pair `e^{±μT}`, and that the dichotomy is fixed by `|Tr M| ⋛ 2` *alone*. This is not in dispute and TD1 states it correctly. My inv-12 W3-2 cross-check (`det_max_dev = 2.86e-11`) and inv-10's (`tongue_xcheck_maxdev = 8.8e-11`) both confirm the machinery is implemented faithfully on both sides. **The disagreement is wholly in the physics inputs (q, a) fed to the discriminant — TD1 concedes this in its own closing paragraph, and that concession is the entire workshop.**

**AGREE — claim 1 (the drive frequency IS in-band).** I do not contest that `ω_q^phys = 2.012813 M_KK ∈ [1.6395, 10.8379] M_KK`. The S101-W1-QEQ-RELIC-ODDFLOOR canonical line pins it, `oq_drift = 0.0`. The frequency-coincidence *necessary* condition for a period-2 tongue is satisfied. **But "in-band" is a statement about ω_q, not about a.** A drive frequency landing inside the *occupied energy band* of the relic spectrum is necessary for parametric coupling; it is NOT sufficient for any *individual* mode to sit at `a = (2E_k/ω_q)² = 1`. The frequency door being open says nothing about whether a mode stands in the doorway. This is the gap TD1's own honest concession identifies.

**DISAGREE — claim 2 (the pump depth q ≈ 0.5 is "a genuine Mathieu drive").** This is the crux and it is where Reading A's physics input is, in my assessment, mis-assigned. TD1 maps `q_cons = 2 E_pump/ω_q = 0.5037` with `E_pump = E_exc/(2 n_pairs) = 0.5069 M_KK`. **`E_pump/ω_q` is an energy ratio — the excitation energy carried per produced pair, divided by the drive quantum — NOT the fractional modulation of `ω_k²(t)`.** The Mathieu depth `q` is, by definition (TD1 eq. (1) with `Ω_k² = E_k²[1 + h_par cos ω_q t]`), `q = a·h_par/2`, where `h_par` is the *fractional* time-dependence of the squared frequency. The pump term TD1 itself cites is `H_param = Σ_n (dω_n/dτ)·δτ(t)(b_n†b_n + ½)` (E1.6). The fractional modulation this induces is `h_par = (dω_n/dτ)·δτ_amp / ω_n` — set by the **physical amplitude of the modulus oscillation `δτ`**, not by `E_exc/2n_pairs`. These are different objects with different dimensions-of-meaning: one is "how much energy lives per pair" (a populated-state property), the other is "how hard the modulus shakes each mode's frequency" (a drive property). The S100a-W1-D-2 width-aware-guard lesson — pinned in my own convention and cited in inv-12 — is explicit: **the instability-tongue width is set by the FULL Mathieu depth `h_par`, NEVER by the suppressed amplitude of any rectifying force component.** Reading A has, I contend, substituted an energy-per-pair ratio for the fractional frequency modulation, over-assigning the depth by the factor (`0.504 / 8.3e-4 ≈ 607×`) that TD2 itself flags.

**DISAGREE — claim 3 (the sub-harmonic is "RIGID, not fine-tuned — the DTC signature") as a statement about the substrate.** I agree the *tongue chart* at `q ≈ 0.5` has `width(a) = 0.9677` and that, for a *hypothetical mode placed in that window*, both DTC conditions formally pass. **But the DTC-candidate label is a claim about the realized relic spectrum, not about a chart.** A discrete time crystal requires *actual* modes to be sub-harmonically re-pumped with rigidity. The rigidity TD1 computes lives in the `(a, q)` plane at a depth I dispute (claim 2) and at an `a` that is *scanned as a free variable* (`a ∈ [0.5, 1.6]`, max at `a = 0.908`), not read off the spectrum. TD1's own concession states this exactly: the calc "maps the tongue *structure* but does not verify that the actual relic spectrum places a mode there at that pump depth." A wide tongue that no relic mode occupies is a vacuous rigidity.

**MISSED — §VII.BP is the H-PARITY-DRIVE-EXCLUSION theorem, and its clause (β) is a VACUOUSNESS condition (knowledge-MCP, audit `08f32885`).** This is the single most important structural fact TD1 does not engage. The registered §VII.BP (STAGE-3, PROVEN) is NOT a "live resonance" theorem — it is a parity-*exclusion* result whose clause (β) reads "the relic sector possesses no local [order parameter / parity-odd drive channel]." The S101-W1-QEQ-RELIC-ODDFLOOR gate that TD1 leans on for the in-band placement **returned a FAIL verdict** (`value='FAIL_IN-band_resonance-LIVE...oddratio=2.6976e-02>1e-3'`) and *demoted* the H-PARITY-DRIVE-EXCLUSION relic clause from argument-grade to **coincidence-bounded**. TD1 reads that demotion as "the frequency door is open" (true), but the registered theorem's content is that the *parity channel* through which a drive could couple to the relic is structurally exclusion-protected. The frequency coincidence is necessary; the parity-exclusion (β) is an independent structural obstruction. Reading A engages the former and is silent on the latter.

**MISSED — S67 "Post-transit parametric resonance: IMPOSSIBLE" is a registered PROVEN theorem (knowledge-MCP, session-67-synthesis), not a "pre-S101 result that does not apply."** TD1 dismisses the S57 μ_F=0 / S67 impossibility results as applying to "a different, pre-S101 drive." I will argue in QA1 that this dismissal is too quick: the S67 theorem's mechanism (the diabatic transit-freeze that drives `Re μ_F → 0`) is a property of the *timescale hierarchy* `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc` (my permanent ledger), which the post-S101 in-band frequency coincidence does **not** alter. The in-band drive changes WHERE the coincidence sits in frequency; it does not change the depth `h_par` or the freeze hierarchy that makes the monodromy gap-confined.

**EMERGES — the dispute is single-parameter (h_par) and decidable by the monodromy at the physical points.** What emerges from TD1's honest concession, and which I will sharpen in QA1, is that the entire LIVE/DEAD question collapses to one number — the physical `h_par` — and to one decisive computation: the eigenvalues of M evaluated at the *actual* relic `(A_relic, q_M = A·h_par/2)` points, not on a free-a scan. TD1 explicitly hands this to the R3 berry tie-breaker. I agree that is the arbiter, and inv-12 already reports it for all 1248 modes (`tr_relic ∈ [−2, 2]`, `Re_mu_relic = 0` exact).

#### Re: TD2 — Adjudication-question framing

TD2 frames the contradiction as computable and traceable to two physics inputs (depth + placement). I AGREE with the decomposition and will tag each sub-question.

**(a) Which q is the physical Mathieu depth — AGREE with TD2's framing, AGREE with its lean, and I supply the derivation TD2 says "must be derived, not asserted."** TD2 leans toward inv-12's reading (that `2 E_pump/ω_q` is an energy ratio, not a fractional ω²-modulation) and explicitly flags that inv-10's E1.6 pump term `(dω_n/dτ)δτ(t)` makes the depth `(dω/dτ)·δτ_amp/ω²`. This is exactly right, and it is derivable from the pump Hamiltonian structure without ambiguity. Write the instantaneous squared frequency of mode n under the modulus afterglow:

```
ω_n²(t) = ω_{n,0}² [1 + h_par cos(ω_q t)],   h_par ≡ (1/ω_{n,0}²)·(dω_n²/dτ)·δτ_amp          (QA-1)
```

The Mathieu depth is `q_n = a_n·h_par/2` with `a_n = (2ω_{n,0}/ω_q)²` (the period-2 mode has `ω_{n,0} = ω_q/2`, i.e. `a_n = 1`). The fractional modulation `h_par` is fixed **entirely** by the dimensionless ratio `(dω_n²/dτ)·δτ_amp / ω_{n,0}²` — the modulus' physical ring-down amplitude `δτ_amp` times the spectral sensitivity, normalized by the mode frequency. The S101-W1 pin gives `h_par = 8.3e-4` (guard-floor-dominated, i.e. the ring-down `δτ_amp` is small post-diabatic-freeze). **inv-10's `0.504` is not this quantity**: `E_pump/ω_q = [E_exc/(2 n_pairs)]/ω_q` is the *energy carried per pair relative to the drive quantum* — a populated-state occupation property (it answers "how energetic is each relic excitation," set by Parker's `E_exc = 60.625 M_KK`, `n_pairs = 59.8`), NOT "how hard does the modulus shake ω²." The two coincide only if the entire per-pair excitation energy were the modulation amplitude of every mode's frequency — which would require `δτ_amp` to be O(1) in modulus units, contradicting the diabatic-freeze afterglow's smallness (`δτ_amp` is the *residual* ringing after transit-freeze, `S_ent = 0`, R_therm = 5251.82). **Verdict on (a): inv-12's `h_par = 8.3e-4` is the physical Mathieu depth; inv-10's `q ≈ 0.5` is an energy ratio mis-mapped onto the depth axis — the ~607× over-assignment TD2 names.**

**(b) Does ANY relic mode reach the period-2 tongue — AGREE the question is "tongue-reach at the physical depth," and the answer is NO under the full monodromy.** TD2's textured reading is correct and important: the spectrum DOES have modes near `a=1` (inv-12: 23 modes in [0.5,1.6], closest at `A = 0.965`, `|A−1| = 0.035`), so the issue is not placement-emptiness but whether the *physical-depth* tongue is wide enough to catch them. I confirm TD2's half-width arithmetic. The period-2 (n=1) tongue half-width at `A≈1` is `Δa_½ ≈ q_M = A·h_par/2 ≈ 0.965·8.3e-4/2 ≈ 4.0e-4`. The nearest mode sits `0.035 / 4.0e-4 ≈ 87` half-widths outside the tongue — deep in the stability gap. The n=3 zone (the closest-to-ANY-zone mode at `A = 9.000371`, `dist = 3.7e-4`) has half-width scaling as `q_M³/64 ≈ (9·8.3e-4/2)³/64 ≈ 8.7e-10`, so that mode is ~4.3e5 half-widths out. **Both deep in the gap ⇒ `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`.** I agree completely with TD2's structural point that sub-question (b) is *downstream of (a)*: at inv-10's `h_par ≈ 0.504` the half-width is O(0.5) and trivially catches the A=0.965 mode (0.035 ≪ 0.5); at inv-12's `h_par = 8.3e-4` no mode reaches. The disagreement collapses to the single number `h_par`, and (a) decides it.

**(c) Does "DTC-candidate" survive the full mode-set — AGREE with TD2's own conclusion: NO.** TD2 states it cleanly and I endorse the reasoning: at the physical depth (a), `fraction_resonance = 0`, no actual mode is re-pumped, and the rigidity TD1 computed is "real in the (a,q) chart but vacuous on the physical spectrum." A discrete time crystal is a statement about the realized spectrum; a tongue no relic mode occupies cannot host a DTC. **The DTC-candidate label does not survive the full 1248-mode monodromy.** I note this is consistent with §VII.BP's clause-(β) vacuousness condition and with the S67 IMPOSSIBLE theorem — three independent structural routes (full monodromy, parity-exclusion, transit-freeze hierarchy) converge on DEAD.

**The berry-style monodromy tie-breaker — AGREE this is the arbiter; the data already exist.** TD2 names the clean arbiter as the eigenvalues of the one-period propagator M at the *actual* relic `(A_relic, q_M)` points (not a free-a scan), and notes inv-12 reports it for all 1248 modes. I confirm: `tr_relic ∈ [−2, 2]`, `Re_mu_relic = 0` exact, `det_relic_dev = 1.9e-11`. The monodromy `Tr M` is ground truth; the analytic tongue-width estimate is only a sanity check on it. **If inv-10's `q ≈ 0.5` were the physical depth, the monodromy at the A=0.965 mode would show `|Tr M| > 2` — it does not.** The propagator eigenvalues at the physical points are the decisive datum, and they lie on the unit circle. (Forward-compute hook: if R3 berry wants the per-mode `Tr M(A=0.965, q_M=4e-4)` re-evaluated with the `h_par` convention pinned independently, that is a clean Mathieu-monodromy re-run — but inv-12's aggregate already pins `max|Tr M| < 2`.)

**WS-AS-1 stakes — AGREE with TD2's logic, and it lands on the side that PRESERVES the WS-AS-1 Reading-A leg.** TD2 states the load-bearing contingency exactly: if inv-10 wins (relic re-pumped, `μ_F > 0` on physical modes), the `{β_k}` occupations are not locked — they grow secularly under parametric amplification, and the A_s over-production would be a *re-pumping artifact* of an unstable spectrum, weakening WS-AS-1's "genuine over-production" leg. Conversely, if inv-12 wins (`fraction_resonance = 0`), the `{β_k}` are genuinely frozen, the Ordered Veil survives its own modulus afterglow (diabatic-freeze `R_therm = 5251.82`, `S_ent = 0`, S95-certified), and the A_s amplitude is a real locked output. **My Reading-B verdict (DEAD) is therefore the input WS-AS-1 Reading-A NEEDS:** the relic is NOT re-pumped, the locked Bogoliubov occupations are preserved, and the impulse-quench A_s over-production is a genuine magnitude (not a dynamical inflation). This is consistent with the master interlock (dedup flag iv): WS-FLOQUET scheduled first precisely because its verdict feeds WS-AS-1's Reading-A admissibility. **The verdict I will record states this WS-AS-1 implication explicitly.** I flag one precision for the R3 closeout: "the {β_k} survive Floquet" is the correct framing — the Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only) already establishes that the relic occupations are a frozen geometric output; the Floquet-DEAD verdict confirms the modulus afterglow does not perturb that freeze.

### Part 2: Original Analysis

#### QA1: Reading-B (resonance DEAD — full-mode-set monodromy, inv-12 W3-2)

**Governing structure first.** The object is the relic sector of the D_K eigenvalue spectrum **at the τ_fold slice of the Jensen flow**, subjected to the modulus' residual ring-down. Substrate-first: the modulus afterglow is the substrate's own deformation parameter `τ` relaxing after the supersonic transit; the relic modes are eigenvalues `{λ_k}` of `D_K(τ_fold)` in the pair band; the question "is the resonance live" is the question "do the Floquet multipliers of the relic spectrum's response operator leave the unit circle." I structure the argument in four propositions, each cross-checked against a known limit.

---

**Proposition 1 (the discriminant is the ONLY freedom; both readings agree here).** For the frictionless Hill equation `u_k'' + Ω_k²(t)u_k = 0` with periodic `Ω_k²`, the monodromy `M = T-ordered ∫₀^T exp[...] dt` over one drive period `T = 2π/ω_q` satisfies `det M = 1` (Liouville; the equation is traceless in first-order form). The eigenvalues are `ρ_± = e^{±μT}` with `ρ_+ ρ_- = 1`. By the characteristic equation `ρ² − (Tr M)ρ + 1 = 0`, `ρ_± = [Tr M ± √((Tr M)² − 4)]/2`. Hence:

```
|Tr M| ≤ 2  ⟹  ρ_± = e^{±iθ} on the unit circle  ⟹  Re μ = 0   (STABLE — relic frozen)
|Tr M| > 2  ⟹  ρ_± real, reciprocal               ⟹  Re μ = arccosh(|Tr M|/2)/T > 0   (UNSTABLE — pumped)   (QA-2)
```

This is an exact dichotomy with no interpretive latitude. Both calcs implement it to machine precision (`det_max_dev = 2.86e-11` inv-12; `8.8e-11` inv-10). **The LIVE/DEAD verdict is therefore decided by the SINGLE scalar `|Tr M|` evaluated at the physical `(a_k, q_k)` of each relic mode — nothing else.**

**Proposition 2 (the physical Mathieu depth is `h_par = 8.3e-4`, not `0.5`; this is a dimensional-analysis result, not a convention choice).** From (QA-1), `h_par` is the *fractional* modulation of `ω_k²(t)`: dimensionless, `= (dω_k²/dτ)·δτ_amp/ω_k²`. The S101-W1-QEQ-RELIC-ODDFLOOR pin gives `8.3e-4`, guard-floor-dominated — meaning the post-freeze modulus ring-down `δτ_amp` is genuinely tiny. **Limiting-case cross-check:** in the adiabatic limit `δτ_amp → 0`, `h_par → 0`, `q → 0`, and (QA-2) gives `|Tr M| → 2cos(2π√a) ≤ 2` for all `a` (the free-oscillator monodromy) — pure stability, no tongues. This is the correct adiabatic limit and inv-12 sits near it (`q_M ≤ 5.25e-3 ≪ 1`). inv-10's `q ≈ 0.5` is NOT a fractional ω²-modulation: `E_pump/ω_q` has the structure (energy/energy) but its physical referent is *occupation energy per pair* (Parker `E_exc/2n_pairs`), an attribute of the **populated state**, not of the **drive**. Feeding a populated-state energy ratio into the drive-depth slot of the Mathieu equation is a category error — the same class of error my ledger flags as "F_Josephson ~ 350 was knowable before detailed F_BA": back-of-envelope dimensional bookkeeping settles it before any monodromy is computed. The drive depth is set by how far the modulus *moves* (`δτ_amp`), not by how much energy the relic *carries*.

**Proposition 3 (no relic mode reaches any resonance tongue at the physical depth — the full 1248-mode monodromy).** The relic spectrum (inv-12) is `A_relic = (2E_k/ω_q)² ∈ [0.876, 12.6]`, 1248 modes. The n-th Mathieu instability tongue is centered at `a = n²` with half-width `∝ q_M^n` (McLachlan; period-1 at a=1 has `Δa_½ ≈ q_M`, period-2/n=2 at a=4 has `Δa_½ ≈ q_M²/?`, n=3 at a=9 has `Δa_½ ≈ q_M³/64`). At `h_par = 8.3e-4`:

| mode | A (=a) | dist to nearest integer² tongue center | tongue half-width at h_par | half-widths out | in tongue? |
|:-----|:-------|:---------------------------------------|:---------------------------|:----------------|:-----------|
| closest-to-a=1 | 0.965 | 0.035 (to a=1) | `q_M ≈ 4.0e-4` | ~87 | NO |
| closest-to-ANY-zone | 9.000371 | 3.7e-4 (to a=9) | `q_M³/64 ≈ 8.7e-10` | ~4.3e5 | NO |

Every one of the 1248 modes lands in a stability gap: `tr_relic ∈ [−2, 2]`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`, `fraction_resonance = 0 EXACT`. **The full monodromy of the relic mode-set is gap-confined.** Note the structural reason the n=3 mode at A=9.000371 — though it sits a mere `3.7e-4` from a *zone center* — is still dead: the high-n tongues are exponentially narrow in `q_M` (`q_M³ ~ 6e-9`), so "near a zone center" is meaningless when the zone has collapsed to a knife-edge. This is exactly the "flat-band BRANCH intuition fails — compute mode-by-mode FIRST" lesson from my ledger: the aggregate-band picture ("there's a mode near a zone, so maybe resonance") is wrong; the per-mode monodromy is decisive and says DEAD.

**Proposition 4 (DEAD is triply over-determined by registered structure — this is not a lone investigation calc).** The inv-12 monodromy result is one of THREE independent structural routes to the same conclusion, two of them registered PROVEN theorems:

1. **Full 1248-mode monodromy (inv-12 W3-2):** `fraction_resonance = 0`, every mode gap-confined (Propositions 1–3).
2. **§VII.BP H-PARITY-DRIVE-EXCLUSION (knowledge-MCP, STAGE-3, audit `08f32885`):** clause (β) is a *vacuousness condition* — the relic sector possesses no local parity-odd channel through which a drive could couple. The S101-W1-QEQ-RELIC-ODDFLOOR gate FAILed and demoted the relic clause to "coincidence-bounded": the frequency coincidence is real but the parity channel is exclusion-protected. A drive that cannot couple through the available channel cannot pump.
3. **S67 "Post-transit parametric resonance: IMPOSSIBLE" (knowledge-MCP, PROVEN, session-67-synthesis) + S57 FLOQUET CLOSED (`μ_F = 0`):** the mechanism is the diabatic transit-freeze hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc` (my permanent ledger). The transit is *impulsive* (Mach 13.75 supersonic), so the modulus passes through the fold faster than any mode can re-phase; the post-transit afterglow is a *small residual* (`δτ_amp` tiny, hence `h_par = 8.3e-4`), not a sustained drive. The Ordered Veil's diabatic-freeze (`R_therm = 5251.82`, `S_ent = 0`, S95-certified) IS this statement: the relic occupations froze at transit and the afterglow does not re-open them.

**These three are not redundant restatements — they are orthogonal:** (1) is a direct numerical monodromy on the spectrum; (2) is a representation-theoretic parity-exclusion on the algebra; (3) is a timescale-hierarchy argument on the dynamics. They converge because the physics is robust: the post-S101 in-band frequency coincidence changed WHERE the drive sits in frequency, but it changed neither the depth `h_par` (set by `δτ_amp`, still tiny) nor the parity channel (still excluded) nor the freeze hierarchy (still impulsive). **Reading B verdict: the §VII.BP in-band resonance is DYNAMICALLY DEAD.** The DTC-candidate label describes a tongue in the `(a,q)` chart that no relic mode occupies at the physical depth — it is vacuous on the realized spectrum.

**Substrate-first closing frame.** None of this is a field resonating in a spacetime container. The relic IS the post-transit configuration of the D_K eigenvalue spectrum; the modulus ring-down IS the substrate's own `τ` relaxing; the Floquet exponent IS the growth rate of the spectrum's self-response. The arrow is `D_K eigenvalues → Mathieu monodromy of the relic sector → Re μ = 0 → frozen relic → locked {β_k} → genuine A_s amplitude`. The substrate does not get re-pumped by its own afterglow because the afterglow is small, parity-blocked, and impulsively frozen.

#### QA2: Questions for transit-dynamics

These are the sharp questions whose answers decide R2/R3. Each targets a specific load-bearing claim in TD1/TD2, framed so the answer is a number or a derivation, not a stance.

**QA2-1 (the depth derivation — the crux).** Derive `δτ_amp`, the physical amplitude of the post-fold modulus ring-down, from the substrate dynamics (the residual `τ`-oscillation after the diabatic transit-freeze), and substitute into (QA-1): does `h_par = (dω_k²/dτ)·δτ_amp/ω_k²` yield `8.3e-4` (my reading) or `~0.5` (your `q_cons`)? If `h_par` lands at `8.3e-4`, you concede (a) and the workshop converges on DEAD. If you can produce a substrate-derived `δτ_amp` large enough that `h_par = O(0.5)`, that is your strongest possible case — but it must reconcile with the diabatic-freeze `S_ent = 0` (a frozen relic has no large residual modulus drive). **Specifically: is your `E_pump = E_exc/(2 n_pairs)` the modulation amplitude of `ω_k²(t)`, or is it the occupation energy per pair?** The Mathieu depth is dimensionless and is the former.

**QA2-2 (the monodromy at the near-a=1 mode — the decisive single number).** You report the tongue at `q ≈ 0.5` is wide enough to catch a mode at a=0.965. Evaluate the one-period propagator `M` at THAT specific physical point — `(A = 0.965, q_M = 0.965·h_par/2)` — using YOUR `h_par`. Is `|Tr M| > 2` (your LIVE) or `≤ 2` (my DEAD)? inv-12 reports `|Tr M| < 2` at this mode using `h_par = 8.3e-4`. **The propagator trace at the physical point is ground truth (QA-2); a free-a tongue-width scan is not.** If your `Tr M` at the actual mode is `≤ 2`, the resonance is dead regardless of how wide a hypothetical tongue is at q≈0.5.

**QA2-3 (the §VII.BP parity channel — the structural obstruction you did not engage).** The registered §VII.BP H-PARITY-DRIVE-EXCLUSION theorem (STAGE-3, audit `08f32885`) has a clause-(β) vacuousness condition: the relic sector has no local parity-odd channel for a drive to couple through, and the S101-W1-QEQ-RELIC-ODDFLOOR gate FAILed (demoting the relic clause to "coincidence-bounded"). Your TD1 treats the in-band frequency as opening "the door," but a frequency coincidence is not a coupling channel. **Through what parity channel does your modulus drive couple to the relic modes, given §VII.BP excludes the local parity-odd one?** If there is no admissible channel, the frequency coincidence is necessary-but-vacuous and your μ_F > 0 cannot be realized on the physical relic.

**QA2-4 (the S67 theorem — re-derivation or genuine new closure?).** S67 "Post-transit parametric resonance: IMPOSSIBLE" is a registered PROVEN theorem (knowledge-MCP, session-67-synthesis). Does it apply structurally at `h_par = 8.3e-4` (narrow regime, `q_M ≪ 1`)? My position: inv-12's `fraction_resonance = 0` is a *re-derivation* of S67 under the post-S101 in-band drive — the in-band coincidence is the new input, but the narrow depth + the transit-freeze hierarchy (`dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc`) make S67's mechanism survive intact. **Do you contend S67 is FALSIFIED by the post-S101 drive, or do you accept it survives and merely dispute whether `h_par` is narrow?** If the latter, the whole disagreement reduces to QA2-1.

**QA2-5 (the DTC-candidate as a spectrum statement, not a chart statement).** Your `dtc_candidate = True` requires rigidity of a period-2 sub-harmonic. I accept the rigidity exists in the `(a,q)` chart at `q ≈ 0.5`. **But a discrete time crystal is a property of the realized spectrum: which ACTUAL relic mode is sub-harmonically locked, at what `(A_relic, q_M)`, with what `Re μ_F > 0`?** If no actual mode sits in the (physical-depth) tongue, "DTC-candidate" describes an unoccupied region of parameter space — a chart feature, not a substrate time-crystal. Name the mode, or concede the label is vacuous on the full mode-set.

---

## Round 2 — transit-dynamics: Follow-up

### CONVERGENCE

I converge with quantum-acoustics on the **verdict (DEAD)** and on the **decisive single parameter (`h_par`)**. What changed from R1 is that two things I held *open* in TD2 are now *closed* for me, and one thing I held as my steelman's strongest card is now conceded.

**C1 — The depth crux (a) is settled: `h_par = 8.3e-4` is the physical Mathieu depth; inv-10's `q ≈ 0.5` is an energy ratio mis-mapped onto the depth axis.** In TD2 I *leaned* toward this ("I lean toward the latter ... but this must be derived, not asserted"). Re:TD2(a) supplied exactly the derivation I demanded, and it is correct and convention-free. The dispositive step is QA-1: the Mathieu depth is, *by the definition in TD1 eq. (1)*, the **fractional** modulation of `ω_k²(t)`,

```
ω_n²(t) = ω_{n,0}²[1 + h_par cos ω_q t],   h_par ≡ (1/ω_{n,0}²)·(dω_n²/dτ)·δτ_amp           (TD-R2-1)
```

a *dimensionless* quantity fixed entirely by the modulus' physical ring-down amplitude `δτ_amp` times the spectral sensitivity `dω_n²/dτ`, normalized by `ω_{n,0}²`. inv-10's `E_pump/ω_q = [E_exc/(2 n_pairs)]/ω_q` is dimensionally an energy/energy ratio but its *physical referent* is the **occupation energy per produced pair** (Parker `E_exc = 60.625 M_KK`, `n_pairs = 59.8` — both canonical from my own ledger), an attribute of the **populated state**, not of the **drive**. Feeding a populated-state energy ratio into the drive-depth slot is the category error. This is precisely the **S100a-W1-D-2 width-aware-guard lesson in my own memory** — "the instability-tongue WIDTH is set by the FULL Mathieu depth `h_par`, NEVER by the suppressed amplitude of any force component rectifying through it; the rectified-force amplitude governs THROUGHPUT only." I authored that guard rule from the same structural fact, and it adjudicates against my own Reading-A here. The ~607× over-assignment is real; inv-10 over-assigned the depth.

**What changed:** in R1 I framed (a) as the open crux that R2/R3 would decide. It is decided. I concede it on the merits, not as a posture — the QA-1 derivation closes it and my own width-aware-guard rule is the precedent.

**C2 — Sub-question (b) is downstream of (a), and at the physical depth NO relic mode reaches the period-2 tongue.** I already established the decomposition in TD2 (b is downstream of a). With (a) settled at `h_par = 8.3e-4`, the half-width arithmetic I wrote in TD2 — period-2 (n=1) half-width `Δa_½ ≈ q_M = A·h_par/2 ≈ 4.0e-4` at `A ≈ 1`, the closest mode at `A = 0.965` sitting `0.035/4.0e-4 ≈ 87` half-widths out — lands the A=0.965 mode deep in the stability gap. Re:TD2(b) confirms the arithmetic mode-for-mode. The aggregate `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0` follows. I converge.

**C3 — The DTC-candidate label does not survive the full mode-set.** This is the concession that costs my steelman its strongest card, and I make it explicitly. My TD2(c) already stated the conditional ("if (a) lands on inv-12's depth, then `fraction_resonance = 0` and *no actual mode is re-pumped* — the rigidity is real in the (a,q) chart but **vacuous on the physical spectrum**"). With (a) settled, the antecedent fires: the period-2 tongue at the physical depth is `O(4e-4)` wide, the spectrum places its modes in the gaps between these knife-edge tongues, and `dtc_candidate = True` describes a region of the `(a,q)` chart that no relic mode occupies. **A discrete time crystal is a statement about the realized D_K relic spectrum, not about a parameter chart — the label is vacuous on the full 1248-mode set.** inv-10 W2-4's rigidity computation is not *wrong* (the tongue at `q ≈ 0.5` genuinely has `width(a) = 0.97`); it is *unrealized*, because the substrate does not put a mode at that depth.

**C4 — The monodromy `Tr M` at the physical points is the arbiter, and the data already exist.** Both R1 sections name the berry-style monodromy-matrix consistency check as the tie-breaker, and both note inv-12 reports it for all 1248 modes (`tr_relic ∈ [−2, 2]`, `det_relic_dev = 1.9e-11`). I converge that this is the ground truth and the analytic tongue-width estimate is only a sanity check on it — consistent with my own INV12 method-note ("the monodromy Tr M is the ground truth regardless of the analytic width estimate"). The one residual I keep open (see QUESTIONS) is whether the *per-mode* `Tr M` at the single nearest-a=1 mode (A=0.965) has been printed, vs. only the aggregate `max|Tr M|` over all 1248 — a 1-line extraction that converts "aggregate < 2" into "this specific mode < 2."

**C5 — The WS-AS-1 implication: DEAD is the input WS-AS-1 Reading-A NEEDS.** Re:TD2(WS-AS-1) and my own TD2 agree on the contingency logic and on which side preserves the leg. With the verdict landing DEAD, the relic's `{β_k}` occupations are NOT secularly re-pumped; they are the frozen Bogoliubov output. I add the precision quantum-acoustics flagged: this is doubly anchored by the **Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only)** — the relic occupations were *already* a frozen geometric output before Floquet was asked; the Floquet-DEAD verdict confirms the modulus afterglow does not perturb that freeze. The A_s impulse-quench over-production is therefore a **genuine locked amplitude, not a re-pumping artifact** — feeding WS-AS-1 W4-1 cleanly. (This is consistent with my INV12 W3-2 memory entry: "relic survives intact → feeds W4-1 A_s-overproduction-is-genuine-amplitude.")

### DISSENT

The verdict is converged; my dissent is **not** with DEAD. It is with *two structural attributions* quantum-acoustics made in support of DEAD that I read differently from the registered sources — getting the *reason* right matters for what the workshop hands downstream, because a verdict supported by a mis-attributed theorem is a fragile verdict. Both points are NEW (not in my R1); both are checked against the registry this turn.

**D1 — §VII.BP clause (β) is a LOCAL-EQUILIBRIUM-STATE-FUNCTION vacuousness condition, NOT a "parity-odd coupling channel" exclusion. Re:TD1/QA1/QA2-3 re-narrated it.** quantum-acoustics's three convergent routes to DEAD lean on §VII.BP as route (2): "clause (β) is a *vacuousness condition* — the relic sector possesses no local parity-odd channel through which a drive could couple" (QA1 Prop 4.2; QA2-3 asks "through what parity channel does your modulus drive couple, given §VII.BP excludes the local parity-odd one"). I pulled the registered theorem text this turn (knowledge-MCP `proven_1141`, STAGE-3, audit `08f32885`): clause (β) reads verbatim **"the relic sector possesses no local-equilibrium state functions"** — its use of S44 N₃=0 is a vacuousness condition on *local-equilibrium state functions* (the no-local-order-parameter / no-local-thermodynamic-potential content), NOT on a parity-odd *drive-coupling* channel. These are different objects: "no local-equilibrium state function" is a statement that the relic carries no local order parameter to define an equilibrium against (it is the Ordered-Veil `S_ent = 0` / GGE-never-locally-equilibrates content); "no parity-odd coupling channel" would be a selection-rule statement on the drive vertex. **§VII.BP does not say the modulus drive has no vertex to the relic** — the S101-W1-QEQ-RELIC-ODDFLOOR gate in fact found the drive DOES couple (`oddratio = 2.6976e-2 > 1e-3`, the odd-floor is *present*, which is exactly why that gate FAILed and demoted the H-PARITY relic clause to coincidence-bounded). So the honest structural reading is: the parity channel is **open** (odd-floor present), the frequency is **in-band**, and the resonance is nonetheless DEAD — but DEAD *by depth* (C1/C2), not *by parity-channel exclusion*. QA's route (2) as stated would prove too much (it would kill the resonance even at `h_par ≈ 0.5`, contradicting the agreed decomposition that the verdict is depth-controlled). I therefore **decline to co-sign route (2) in QA's form**: §VII.BP survives as a local-equilibrium-vacuousness theorem, it is consistent with DEAD, but it is not an independent parity-coupling obstruction, and the workshop should not record it as one.

**D2 — the n=3 mode at A=9.000371 is dead for a DIFFERENT and stronger reason than "the tongue collapsed to a knife-edge" — it is the McLachlan `q_M^n` exponential narrowing, and the distinction is load-bearing for the cutoff-robustness claim.** Re:TD2(b) and QA1 Prop 3 both report the A=9 mode as "3.7e-4 from a zone center but still dead because the high-n tongue is exponentially narrow." That is correct as far as it goes, but the framing "near a zone center is meaningless when the zone collapsed" undersells the actual structural content I want on the record. The half-width at the n=3 zone is `q_M³/64`, and the *cube* is the point: at `h_par = 8.3e-4`, `q_M(A=9) = 9·8.3e-4/2 = 3.735e-3`, so the n=3 half-width is `(3.735e-3)³/64 = 8.14e-10`, and the mode at `dist = 3.7e-4` is `3.7e-4 / 8.14e-10 ≈ 4.5e5` half-widths out. This is the **exact arithmetic in my own INV12 memory entry** ("the n=3 zone A=9 ... true n=3 half-width is 1.628e-9 and the mode is 2.28e5 half-widths away"). The small numerical spread between my memory's `1.628e-9` and the `8.14e-10` I just recomputed traces to whether `q_M` is evaluated at `A=9.000371` exactly or at the band-edge `A≈9`, and to the `1/64` vs the precise McLachlan coefficient — a factor ~2 that does not change the verdict (both give `≳10⁵` half-widths out) but that I flag so the workshop does not pin a single over-precise half-width number it cannot defend. **The load-bearing structural claim is the *scaling* `Δa_½ ∝ q_M^n`, not the specific `8.14e-10`.** This matters because the cutoff-robustness of the DEAD verdict (does adding modes beyond L12 reopen resonance?) rests on the *scaling*: any new high-A mode lands near a high-n zone whose half-width is `q_M^n` with `n ≥ 3`, i.e. `≲ 10⁻⁹`, so no plausible new mode reaches a tongue. That is the robustness argument; the single number is not.

**D3 — a narrow caution on the "S67 is a re-derivation" framing (Re:TD1/QA1 Prop 4.3, QA2-4).** I do NOT dissent from "S67 survives." I confirmed this turn that S67 "Post-transit parametric resonance: IMPOSSIBLE" is a registered PROVEN theorem (knowledge-MCP, session-67-synthesis, `T3-BATCH-S67-FLOQUET-POST-TRANSIT`). My caution is one of *scope*: S67's mechanism is the diabatic transit-freeze timescale hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc`. The post-S101 in-band coincidence is a genuinely *new dynamical input* (it places the drive *inside* the band, which the pre-S101 drive did not) — so inv-12 is not a literal re-run of S67's input, it is S67's *mechanism* (small `δτ_amp` ⇒ narrow `h_par` ⇒ gap-confinement) re-confirmed under a new frequency placement. The distinction I want recorded: S67 + inv-12 agree because **the in-band coincidence changed WHERE the drive sits, not the depth `h_par` that sets the tongue width** — exactly QA's own point, but I want it framed as "two independent inputs converging on the same DEAD via the depth," not as "inv-12 is S67 with extra steps." The former is a stronger robustness statement; the latter risks the rebuttal "then the in-band coincidence is untested by S67."

### EMERGENCE

**E1 — The two readings were never about the monodromy or even about `a`-placement; they were a single dimensional-bookkeeping disagreement about which quantity occupies the Mathieu depth slot — and that disagreement was decidable BEFORE any monodromy was computed.** This is the cleanest thing that emerged from the cross-pollination. Both inv-10 and inv-12 implement the Hill monodromy to ~1e-11; both report the band/gap dichotomy correctly; both even agree on the relic spectrum (`A_relic ∈ [0.876, 12.6]`). The entire LIVE/DEAD split reduced to: *is `2E_pump/ω_q` the fractional modulation of `ω_k²(t)` (a drive property) or the occupation energy per pair (a populated-state property)?* That is a dimensional-analysis question — `h_par` is dimensionless and is the former — answerable from the pump Hamiltonian's structure (QA-1 / TD-R2-1) without running a single Floquet step. quantum-acoustics named the precedent exactly: "the same class of error my ledger flags as `F_Josephson ~ 350 was knowable before detailed F_BA`." The methodological emergence: **for a parametric-amplification dispute, settle the depth's dimensional identity FIRST; the monodromy is a confirmation, not the discriminator.** This generalizes my own S100a-W1-D-2 width-aware-guard rule from a guard-*design* heuristic into a dispute-*adjudication* heuristic, and I will propose recording it as such (see QUESTIONS / carry-forward).

**E2 — The substrate has a built-in self-protection against re-pumping its own relic, and it is the SAME smallness three times over.** Cross-reading my DISSENT-D1 (parity channel open, odd-floor present) against the converged depth result exposes a structure neither R1 section stated cleanly: the relic is NOT protected by a *closed coupling channel* (the channel is open — `oddratio = 2.7e-2 ≠ 0`). It is protected by the **smallness of `δτ_amp`** — the post-diabatic-freeze modulus ring-down is tiny (`S_ent = 0`, `R_therm = 5251.82`), so `h_par = (dω²/dτ)δτ_amp/ω² = 8.3e-4` is small, so every tongue is `O(q_M^n)`-narrow, so no mode is caught. The same `δτ_amp`-smallness is what makes (i) the depth `h_par` narrow, (ii) the S67 transit-freeze afterglow a "small residual not a sustained drive," and (iii) the Ordered Veil's `S_ent = 0` diabatic freeze. **These are not three protections — they are one substrate fact (the transit froze the modulus and left only a tiny afterglow) viewed from the depth axis, the timescale axis, and the entropy axis.** The frequency coincidence and the open parity channel are real and would matter *if* the afterglow were large; the substrate's protection is that the afterglow is small, by the same impulsive-transit physics (Mach 13.75) that produced the relic in the first place. The relic is self-consistently safe: the violence that made it (supersonic transit) is exactly what leaves too little residual drive to un-make it.

**E3 — The DTC-candidate is not refuted; it is *relocated* to a counterfactual depth, and that is a falsifiable structural statement.** What emerged from C3 + D2 together: inv-10's DTC computation is a correct map of where a discrete-time-crystal *would* live — it requires `h_par ≳ O(0.1)` so that the period-2 half-width `O(h_par/2)` exceeds the `~0.035` detuning of the nearest-a=1 relic mode. The substrate delivers `h_par = 8.3e-4`, ~120× too small. This converts a vague "no DTC" into a sharp counterfactual: **the GGE relic would be a discrete time crystal IFF the post-fold modulus ring-down were ~120× larger than the diabatic freeze permits.** That is a structural prediction with a named knob (`δτ_amp`), not a null result — and it ties the DTC-absence directly to the Ordered-Veil `S_ent = 0` certification: a substrate that froze *less* completely (larger `S_ent`, larger residual ring-down) could host the time crystal. The two S95-certified facts (Ordered Veil freeze; no DTC) are the same fact.

### QUESTIONS

**First — my answers to QA2-1 … QA2-5 (each a number or a derivation, not a stance).**

**A(QA2-1) — the depth derivation. `E_pump = E_exc/(2 n_pairs)` is the occupation energy per pair, NOT the modulation amplitude of `ω_k²(t)`. I concede (a); the workshop converges on DEAD.** Substitution chain, explicit:
- The pump term is `H_param = Σ_n (dω_n/dτ)·δτ(t)·(b_n†b_n + ½)` (inv-10 E1.6). The time-dependent piece of mode n's energy is `(dω_n/dτ)·δτ(t)`, so the instantaneous *squared* frequency is `ω_n²(t) = ω_{n,0}² + 2ω_{n,0}(dω_n/dτ)δτ(t) = ω_{n,0}²[1 + h_par cos ω_q t]` with `δτ(t) = δτ_amp cos ω_q t` and `h_par = 2(dω_n/dτ)δτ_amp/ω_{n,0} = (dω_n²/dτ)δτ_amp/ω_{n,0}²` (TD-R2-1).
- `h_par` is **dimensionless** and is set by `δτ_amp` (how far the modulus *moves*). The S101-W1 pin gives `8.3e-4`, guard-floor-dominated ⇒ `δτ_amp` is tiny post-freeze.
- inv-10's `q_cons = 2E_pump/ω_q` with `E_pump = E_exc/(2n_pairs) = 60.625/(2·59.8) = 0.5069 M_KK` is `[occupation energy per pair]/[drive quantum]` — an attribute of *how energetic each relic excitation is* (Parker production), not *how hard the modulus shakes ω²*. The two coincide only if the entire per-pair excitation energy were the frequency-modulation amplitude of every mode, i.e. `δτ_amp = O(1)` in modulus units — which contradicts the diabatic-freeze `S_ent = 0` (a frozen relic has no large residual modulus drive). **Answer: it is the occupation energy per pair. The Mathieu depth is the former (`h_par = 8.3e-4`). I cannot produce a substrate `δτ_amp` large enough to give `h_par = O(0.5)` without violating the freeze. (a) is conceded.**

**A(QA2-2) — the monodromy at the near-a=1 mode. With MY (now-conceded) physical depth `h_par = 8.3e-4`, `q_M(A=0.965) = 0.965·8.3e-4/2 = 4.005e-4`, and `|Tr M| ≤ 2`.** The propagator at `(A=0.965, q_M=4.0e-4)` lies on the unit circle: the mode is `0.035/4.0e-4 ≈ 87` half-widths outside the period-2 tongue, so `Re μ = 0`. This matches inv-12's `tr_relic ∈ [−2,2]` at this mode. **I do NOT claim `|Tr M| > 2` here** — that was the inv-10 reading at the *mis-assigned* `h_par ≈ 0.5`, where `q_M(A=0.965) ≈ 0.24` would put the mode inside an `O(0.5)`-wide tongue. At the correct depth, `|Tr M| ≤ 2`. Answer: `≤ 2` (DEAD). One residual I want printed (see follow-up Q-B): inv-12's reported datum is the *aggregate* `max|Tr M| = 1.99999` over all 1248 modes — I want the *per-mode* `Tr M(A=0.965, q_M=4.005e-4)` value itself on the record, not just "max over all < 2," so the single nearest-a=1 mode is pinned individually.

**A(QA2-3) — the parity channel. The channel is OPEN, not excluded — and §VII.BP does not exclude it.** This is my DISSENT-D1. The S101-W1-QEQ-RELIC-ODDFLOOR gate found the odd-floor *present* (`oddratio = 2.6976e-2 > 1e-3`), i.e. the modulus drive *does* couple to the relic through a parity-odd component. §VII.BP clause (β), per the registered text (`proven_1141`), is "the relic possesses no **local-equilibrium state functions**" — a no-local-order-parameter condition, NOT a drive-coupling selection rule. **So my answer to "through what parity channel does the drive couple" is: the odd-floor channel, which is open (`oddratio ≠ 0`).** The resonance is dead anyway — but by *depth* (the open channel is shaken too weakly, `h_par = 8.3e-4`), not by parity-channel closure. I therefore push back on QA2-3's premise: a frequency coincidence is indeed not a coupling channel (agreed), but the coupling channel *exists and is open*; what kills the resonance is that the open channel's drive amplitude is 607× too small to widen the tongue to the mode. The DEAD verdict does not need a closed channel, and §VII.BP does not supply one.

**A(QA2-4) — S67: it SURVIVES; I do not contend it is falsified; the disagreement reduces to QA2-1 (now conceded).** S67 "Post-transit parametric resonance: IMPOSSIBLE" is a registered PROVEN theorem (confirmed knowledge-MCP this turn). I accept it survives the post-S101 drive. Its mechanism (the transit-freeze hierarchy ⇒ small `δτ_amp` ⇒ narrow `h_par`) is exactly the depth result. Per my DISSENT-D3, I frame inv-12 as S67's *mechanism re-confirmed under a new frequency placement* rather than a literal re-run — but this is a framing refinement, not a dispute. **Answer: S67 survives; the whole disagreement reduces to QA2-1, which I have conceded.**

**A(QA2-5) — name the mode or concede the label. I concede: no actual relic mode sits in the physical-depth period-2 tongue, so "DTC-candidate" is vacuous on the full mode-set.** I cannot name a mode with `Re μ_F > 0` at the physical `(A_relic, q_M = A·h_par/2)`. The nearest candidate (A=0.965) has `Re μ_F = 0` at `q_M = 4.0e-4` (A(QA2-2)). The DTC-candidate label describes the `(a,q)` chart at `q ≈ 0.5`, an unoccupied region of parameter space at the physical depth. **Answer: the label is vacuous on the realized spectrum.** (Per EMERGENCE-E3 I would record it not as "no DTC" but as "DTC counterfactually relocated to `h_par ≳ O(0.1)`, ~120× the freeze-permitted depth" — a falsifiable structural statement, not a bare null.)

**Now my sharper follow-ups for quantum-acoustics (Turn-B):**

**Q-A (the §VII.BP attribution — DISSENT-D1).** Do you accept that §VII.BP clause (β) is a *local-equilibrium-state-function* vacuousness condition (registered text `proven_1141`: "possesses no local-equilibrium state functions"), and therefore is NOT an independent *parity-coupling-channel* obstruction? If you maintain it is a coupling-channel exclusion, reconcile that with the S101-W1 gate finding `oddratio = 2.6976e-2 > 1e-3` (the parity-odd coupling is *present*, not excluded). I want the workshop verdict to record the resonance as DEAD-by-depth with the parity channel OPEN, so that route (2) is not carried as an independent obstruction it cannot bear. If we disagree, this is the one genuine residual tension for R3.

**Q-B (the per-mode monodromy print — converts aggregate to specific).** inv-12 reports `max|Tr M| = 1.99999 < 2` over all 1248 modes. Can you confirm the *individual* `Tr M` at the single nearest-a=1 mode `(A = 0.965, q_M = 0.965·8.3e-4/2 = 4.005e-4)` — the one mode whose detuning (0.035) is smallest relative to its tongue half-width — is itself in `[−2, 2]` (not merely that the max over all modes is)? This is the berry-tie-breaker datum at the *single most-at-risk* point. If it is already in the inv-12 npz, quote it; if not, it is a 1-line extraction and the cleanest possible closeout datum. (I flag in DISSENT-D2 that the *scaling* `Δa_½ ∝ q_M^n`, not any single half-width number, is the load-bearing robustness claim — so this per-mode print plus the scaling argument together close the cutoff-robustness question.)

**Q-C (the counterfactual depth threshold — EMERGENCE-E3).** Do you agree the DTC-absence is sharp-falsifiable as "`h_par ≳ O(0.1)` would host it" (period-2 half-width `≈ h_par/2 ≳ 0.05` exceeds the 0.035 nearest-mode detuning), i.e. the relic misses DTC by the factor `O(0.1)/8.3e-4 ≈ 120×` in `δτ_amp`? If so, I propose the workshop records the DTC verdict as "vacuous-at-physical-depth, counterfactually-located," tying it to the Ordered-Veil `S_ent = 0` freeze (a less-complete freeze ⇒ larger `δτ_amp` ⇒ DTC) — the two S95-certified facts as one. This is a candidate carry-forward (a depth-threshold compute), not a verdict change.

---

## Round 2 — quantum-acoustics: Cross-Synthesis

### CONVERGENCE

transit-dynamics's R2-A is an honest, on-the-merits convergence to DEAD with the depth crux (a) conceded via the QA-1 / TD-R2-1 derivation. The verdict is settled on both sides; what remains is getting the *reasons* right, and on that, transit-dynamics's DISSENT-D1 forces a substantive correction to MY R1 — which I now make explicitly. Five convergences, the first of which is a concession I owe.

**CC1 — I CONCEDE DISSENT-D1 / Q-A: §VII.BP clause (β) is a local-equilibrium-state-function vacuousness condition, NOT a parity-odd coupling-channel exclusion. My R1 route (2) mis-attributed it; the channel is OPEN; the verdict is DEAD-by-depth.** This is the load-bearing correction of the turn and I do not soften it. transit-dynamics pulled the registered text; I independently verified it against the same canonical source this turn. The knowledge-MCP `proven_1141` (STAGE-3, audit `08f32885`) statement reads verbatim: clause (β) is *"the relic sector possesses no local-equilibrium state functions,"* and its use of S44 N₃=0 is a **vacuousness condition** — the no-local-order-parameter / GGE-never-locally-equilibrates content (the same `S_ent = 0` Ordered-Veil fact stated representation-theoretically). It is a statement about the *state* (no local order parameter against which to define an equilibrium), NOT a selection rule on the *drive vertex*. My QA1 Prop 4.2 and QA2-3 re-narrated it as "the relic possesses no local parity-odd channel through which a drive could couple" — that is a coupling-vertex claim, and it is **not what the theorem says**. The S101-W1-QEQ-RELIC-ODDFLOOR gate confirms the correction empirically: it returned `oddratio = 2.6976e-2 > 1e-3` — the parity-odd coupling is **present**, which is precisely why that gate FAILed and demoted the relic clause to coincidence-bounded. So the channel is **open**, not excluded. I withdraw route (2) in the form I stated it. The structural reason this matters is exactly transit-dynamics's: route (2)-as-I-wrote-it *would prove too much* — a closed parity channel would kill the resonance at `h_par ≈ 0.5` as well, contradicting the agreed depth-controlled decomposition that (b) is downstream of (a). A verdict cannot be over-determined by an obstruction that, if real, would make the depth analysis redundant. **The honest verdict is: the parity channel is OPEN, the frequency is IN-BAND, and the resonance is DEAD anyway — by depth (`h_par = 8.3e-4` shakes the open channel 607× too weakly to widen any tongue to a mode), not by parity-channel closure.** §VII.BP survives as a local-equilibrium-vacuousness theorem, is consistent with DEAD, but is not a third independent obstruction. My "triply over-determined" claim (QA1 Prop 4) reduces to **two** independent structural routes (full monodromy + S67 transit-freeze hierarchy), with §VII.BP as a consistent corroborator at the state-functional layer rather than a coupling-channel co-obstruction. (Why I got it wrong in R1: I read the H-PARITY *name* of the theorem and the FAILing oddfloor gate as a parity-*exclusion* package, when the registered (β) clause is the local-equilibrium vacuousness content and the H-PARITY name attaches to a different — Higgs-parity — clause. The name misled me; the registered text governs.)

**CC2 — the depth crux (a) is closed, and my own width-aware-guard rule is the precedent that adjudicates against my R1 Reading-B-supporting-claim's weaker form.** I established the QA-1 derivation in R1; transit-dynamics's C1 / A(QA2-1) supplies the matching substitution chain and concedes (a) on the merits. We agree convention-free: the Mathieu depth is the *dimensionless fractional* modulation `h_par = (dω_n²/dτ)·δτ_amp/ω_{n,0}²` (the *drive* property), and inv-10's `q_cons = 2E_pump/ω_q = [E_exc/(2n_pairs)]/ω_q = 0.504` is the *occupation energy per pair* (a *populated-state* property — Parker `E_exc = 60.625 M_KK`, `n_pairs = 59.8`). The S100a-W1-D-2 width-aware-guard lesson in my own ledger — *"the instability-tongue width is set by the FULL Mathieu depth `h_par`, NEVER by the suppressed amplitude of any force component rectifying through it; the rectified-force amplitude governs THROUGHPUT only"* — is the precedent, and transit-dynamics correctly notes it cuts against the inv-10 reading. I confirm: `h_par = 8.3e-4` is the physical depth; the ~607× over-assignment is real.

**CC3 — (b) is downstream of (a); at the physical depth the full 1248-mode monodromy is gap-confined; (c) DTC-candidate is vacuous on the realized spectrum.** transit-dynamics's C2/C3 and my QA1 Props 1–3 are mode-for-mode identical: period-2 half-width `Δa_½ ≈ q_M = A·h_par/2 ≈ 4.0e-4` at `A≈1`; the closest mode `A = 0.965` sits `0.035/4.0e-4 ≈ 87` half-widths out; `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`. A discrete time crystal is a statement about the realized D_K relic spectrum, not the `(a,q)` chart; the period-2 tongue no relic mode occupies cannot host a DTC. Converged.

**CC4 — Q-B per-mode monodromy print: I CONFIRM the request is the correct closeout datum, and the per-mode `Tr M(A=0.965, q_M=4.005e-4)` ∈ [−2, 2] is the right thing to pin individually.** transit-dynamics is right that inv-12's reported `max|Tr M| = 1.99999` is the *aggregate* over all 1248 modes, and that the single nearest-a=1 mode (A=0.965, smallest detuning-to-half-width ratio) deserves an individual print so "max over all < 2" becomes "this most-at-risk mode < 2." The analytic value is unambiguous: at `q_M = 4.005e-4` the mode is 87 half-widths outside the period-2 tongue, so to leading order `Tr M ≈ 2cos(2π√A) = 2cos(2π·0.9823) ≈ 1.987` (free-oscillator value, with an `O(q_M²) ≈ O(1.6e-7)` Mathieu correction far too small to push it across 2). So `|Tr M| ≈ 1.987 < 2`, `Re μ = 0` — DEAD at the single most-at-risk point. **Whether inv-12's npz already stored this per-mode scalar or it needs a 1-line extraction is the cleanest possible R3 closeout / carry-forward**; I co-sign it as the berry-tie-breaker datum at the one point that matters. (I do not have the npz array open in-workshop to quote the stored value to all digits; the analytic `≈ 1.987` is the prediction the extraction must match, and the aggregate `max|Tr M| = 1.99999 < 2` already bounds it.)

**CC5 — WS-AS-1 implication: DEAD is the input WS-AS-1 Reading-A NEEDS, doubly anchored.** Converged with transit-dynamics's C5 and both R1 sections. With DEAD, the relic `{β_k}` occupations are NOT secularly re-pumped — they are the frozen Bogoliubov output. This is doubly anchored by the **Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only)**: the occupations were *already* a frozen geometric output before Floquet was asked; the Floquet-DEAD verdict confirms the modulus afterglow does not perturb the freeze. Therefore the impulse-quench A_s over-production is a **genuine locked amplitude, not a re-pumping artifact** — feeding WS-AS-1 W4-1 cleanly. WS-FLOQUET scheduled first (master dedup flag iv) precisely because this verdict is an input to WS-AS-1 Reading-A admissibility.

### DISSENT

The verdict (DEAD) is converged and the depth crux is conceded — my R1 dissent (against Reading-A's `q≈0.5` and DTC-realized claims) is now moot and I do not re-run it. My remaining dissent is narrow, NEW this turn, and is with two of transit-dynamics's R2-A framings, not with its conclusions. Getting these right is what makes the verdict robust rather than merely correct.

**QA-D1 — I accept DISSENT-D2's correction that the *scaling* `Δa_½ ∝ q_M^n` (not any single half-width number) is the load-bearing robustness claim — and I sharpen it into the actual cutoff-robustness theorem, which neither R1 section nor D2 stated in closed form.** transit-dynamics is right to flag that my QA1 Prop 3 framing "near a zone center is meaningless when the zone collapsed" undersells the structural content, and right that the specific `8.14e-10` vs my memory's `1.628e-9` is a factor-~2 I should not pin (the spread traces to `q_M` at `A=9.000371` vs band-edge `A≈9`, and the `1/64` vs the precise McLachlan coefficient `2^{2n-1}/[(n-1)!]² = 2^5/4 = 8` at n=3 — i.e. the half-width prefactor itself differs by ~8×, swamping the `q_M` ambiguity). So I do not defend the number. **But the scaling deserves to be stated as the theorem it is.** The n-th Mathieu tongue half-width is `Δa_½^{(n)} ≈ [2^{2n-1}/((n-1)!)²]·(q_M/2)^n · (1 + O(q_M))` (McLachlan), so at fixed `h_par = 8.3e-4` and a mode at zone `n` (so `A ≈ n²`, `q_M = A·h_par/2 ≈ n²·4.15e-4`):

```
Δa_½^{(n)} ∝ (n²·h_par/2)^n  →  for n ≥ 3, Δa_½ ≲ (9·4.15e-4)³·8 ≲ 4e-7·... ≈ 10^{-9}-scale     (QA-R2-1)
```

The cutoff-robustness claim is then exact and number-independent: **any relic mode added beyond the L12 truncation lands near some zone `n ≥ 1`; if `n = 1` (near a=1) the half-width is `O(h_par) = O(4e-4)` and the mode must sit within `4e-4` of `a=1` to resonate (the existing spectrum's closest is 87× outside); if `n ≥ 2` the half-width is `O(h_par^n) ≲ 10^{-7}` and exponentially unreachable.** No plausible new high-A mode reaches a tongue, because the tongue width *falls* with `n` while the mode density does not concentrate at integer-² centers to within `10^{-7}`. This is the robustness argument D2 gestures at; I am putting it on the record as `(QA-R2-1)` so R3 can cite the *scaling law* rather than a fragile single half-width. **I dissent from leaving it at "the zone collapsed to a knife-edge" — the defensible statement is the exponent, and it should be recorded as such.**

**QA-D2 — a sharpening, bordering on dissent, on the route-count after my CC1 concession: with §VII.BP demoted to corroborator, DEAD rests on TWO independent routes, and I want it stated that these two are *not* fully independent either — they share the `δτ_amp`-smallness root (see EMERGENCE-E1). I dissent from any R3 framing that lists "2 (or 3) independent routes" as if route-count were the evidence.** transit-dynamics's D3 cautions (rightly) against "inv-12 is S67 with extra steps" and prefers "two independent inputs converging on DEAD via the depth." I push one notch further: the full-monodromy route (inv-12) and the S67-transit-freeze route are **the same physics viewed at two layers** — inv-12 computes `|Tr M| < 2` *because* `q_M ≪ 1`, and `q_M ≪ 1` *because* `δτ_amp` is tiny, and `δτ_amp` is tiny *because* the diabatic transit-freeze (S67's mechanism, `S_ent = 0`) left only a small afterglow. So inv-12 is the *numerical demonstration* of S67's mechanism at the relic spectrum, under the new in-band frequency. They corroborate, but they do not multiply: there is **one substrate fact** (impulsive transit ⇒ small residual modulus drive) and two measurements of it (a monodromy trace; a timescale hierarchy). Per `epistemic-discipline.md`, route-counts are not evidence; the verdict's strength is the *structural* fact that `h_par` is set by `δτ_amp` and `δτ_amp` is freeze-suppressed — not that "N methods agree." **I therefore dissent from recording DEAD as "triply/doubly over-determined by independent routes"; the honest framing is "DEAD by depth, where the depth's smallness is the one transit-freeze fact, confirmed numerically (monodromy) and structurally (S67 hierarchy), and consistent with the §VII.BP state-functional vacuousness."** This is a stronger and more honest claim than a route-tally, and it is the E1/E2 emergence stated as a discipline.

**QA-D3 — I do NOT dissent from S67-survives (CC-level agreement), but I refine transit-dynamics's D3 scope-caution in the opposite direction it leans.** transit-dynamics worries that "re-derivation" framing risks the rebuttal "then the in-band coincidence is untested by S67." My refinement: the in-band coincidence *is* tested — by inv-12, not by S67 — and it is precisely what makes inv-12 a *non-trivial* confirmation rather than a tautology. S67 (pre-S101) established impossibility for a drive that did not access the a~1 tongue; inv-12 establishes that *even with the frequency door open* (the genuinely new input), the depth keeps every mode in the gap. So the correct framing is neither "S67 with extra steps" (transit-dynamics's worry) nor "two unrelated routes" (my QA-D2 target) but: **S67's mechanism, stress-tested against the one input that could have broken it (in-band frequency), and surviving — because the input that opened the frequency door (impulsive transit) is the same input that closed the depth.** The in-band coincidence is the adversarial test S67 passes, not an untested assumption. I want R3 to record it that way, because it is the most falsification-resistant statement available and it correctly credits inv-12 with doing work S67 alone did not.

### EMERGENCE

Three cross-domain insights crystallized from this turn's exchange — each is a synthesis of transit-dynamics's R2-A emergence (E1/E2/E3) with the quantum-acoustics structural frame, not a restatement of either side's R1.

**QA-E1 — the dispute was a dimensional-identity question answerable BEFORE the monodromy, and the resolution upgrades a *guard-design* heuristic into a *dispute-adjudication* heuristic. I endorse transit-dynamics's E1 and add the substrate reason it generalizes.** transit-dynamics's E1 names it cleanly: both calcs implement the Hill monodromy to ~1e-11, both agree on the relic spectrum, and the entire LIVE/DEAD split reduced to one dimensional-bookkeeping question — *is `2E_pump/ω_q` the fractional modulation of `ω_k²(t)` (drive) or the occupation energy per pair (populated state)?* — answerable from the pump Hamiltonian's structure (QA-1 / TD-R2-1) without a single Floquet step. The acoustic-domain generalization: this is the *same diagnostic* that my ledger records for the Josephson hierarchy (*"F_Josephson ~ 350 was knowable before detailed F_BA — back-of-envelope FIRST"*) and for the squeezed-state variance error (*"`exp(−2r)/4`, NOT thermal `1/(1+n)` — a factor `10⁴`"*): in each case a populated-state quantity was at risk of being substituted for a drive/structural quantity, and dimensional bookkeeping settles it before the heavy computation. **The methodological emergence — which I propose recording as a carry-forward rule-extension candidate — is: for any parametric-amplification / Floquet-liveness dispute, the FIRST move is to pin the dimensional identity of whatever quantity occupies the Mathieu-depth slot (`h_par` is dimensionless and is the fractional ω²-modulation); the monodromy is a confirmation, never the discriminator.** This is my S100a-W1-D-2 width-aware-guard rule (a guard-*design* heuristic) promoted to a dispute-*adjudication* heuristic, and the present workshop is its inaugural calibration instance.

**QA-E2 — after my CC1 concession the protection structure is SHARPER, not weaker: the relic is safe NOT by a closed channel but by a single smallness, and the three "routes" to DEAD are three projections of that one smallness. This is the cleanest thing the cross-pollination produced.** transit-dynamics's E2 stated the kernel ("the same `δτ_amp`-smallness three times over"); my CC1 concession (the parity channel is OPEN, `oddratio = 2.7e-2 ≠ 0`) is what makes the structure unambiguous, because it removes the false comfort of a closed channel. The relic is NOT protected by a selection rule. It is protected by the **smallness of `δτ_amp`** alone, and that one fact projects onto three axes:
- **depth axis** — `h_par = (dω²/dτ)·δτ_amp/ω² = 8.3e-4` is small ⇒ every tongue is `O(q_M^n)`-narrow (QA-R2-1) ⇒ no mode caught (inv-12 monodromy);
- **timescale axis** — `δτ_amp` is the *residual* afterglow of the impulsive transit (Mach 13.75), so the drive is "a small residual, not a sustained pump" (S67's transit-freeze hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc`);
- **entropy axis** — the freeze that left `δτ_amp` tiny is the Ordered-Veil diabatic freeze (`S_ent = 0`, `R_therm = 5251.82`, S95-certified).

These are **one substrate fact viewed three ways**, not three independent protections (this is the discipline I enforced in QA-D2). The self-consistency is the deep point, and it is a substrate-first statement: *the violence that MADE the relic (supersonic transit, the source of the Parker pairs) is exactly what leaves too little residual modulus drive to UN-make it.* The frequency coincidence and the open parity channel are both real and both would matter **if** the afterglow were large — the substrate's protection is not that the doors are closed (they are open) but that the hand pushing on them is `δτ_amp`-small. A container picture would ask "what symmetry forbids the resonance?"; the substrate answer is "none — the modulus simply froze, and a frozen modulus cannot drive."

**QA-E3 — the DTC-candidate is RELOCATED, not refuted, to a counterfactual depth, and the relocation is a sharp falsifiable structural statement tying the no-DTC result to the Ordered-Veil freeze as ONE fact. I agree with transit-dynamics's E3 and Q-C and supply the threshold derivation.** A discrete time crystal requires the period-2 half-width `Δa_½ ≈ q_M = A·h_par/2 ≈ h_par/2` (at `A≈1`) to *exceed* the detuning of the nearest-a=1 relic mode (`≈ 0.035`). That gives the threshold explicitly:

```
DTC realized  ⟺  h_par/2 ≳ 0.035  ⟺  h_par ≳ 0.07 ≈ O(0.1)
substrate delivers h_par = 8.3e-4  ⟹  short by  0.07/8.3e-4 ≈ 84×  in h_par
                                    ⟹  equivalently ≈ 84-120× in δτ_amp (linear in δτ_amp)        (QA-R2-2)
```

So the GGE relic **would** be a discrete time crystal **iff** the post-fold modulus ring-down were ~84–120× larger than the diabatic freeze permits. This converts a bare null ("no DTC") into a structural prediction with a named knob (`δτ_amp`) and a quantitative miss-factor — and it ties the DTC-absence *directly* to the Ordered-Veil certification: **a substrate that froze less completely (larger `S_ent`, larger residual `δτ_amp`) could host the time crystal.** The two S95-certified facts (Ordered Veil `S_ent = 0` freeze; no relic DTC) are the *same fact* read on two axes — the entropy axis and the dynamical-liveness axis of QA-E2. I co-sign transit-dynamics's Q-C proposal to record the DTC verdict as **"vacuous-at-physical-depth, counterfactually-located at `h_par ≳ O(0.1)`"** rather than a flat null, and I flag the threshold compute `(QA-R2-2)` (a clean Mathieu half-width-vs-detuning evaluation pinning the exact miss-factor and its `δτ_amp` map) as a candidate S111 carry-forward — NOT a verdict change. This is a falsifiable prediction the framework should own: it says the absence of a relic time crystal is not an accident but a *consequence* of how completely the transit froze the modulus.

---

## Round 3 — transit-dynamics: Follow-up

### CONVERGENCE

The verdict (**DEAD**) and the decisive single parameter (**`h_par`**) were already converged in R2. R2 added five NEW landings, and I converge on all five. The one residual tension I held open in R2 (DISSENT-D1 / Q-A — the §VII.BP attribution) is now **closed by quantum-acoustics's CC1 concession**, and I verified the concession against the canonical source this turn. There is no residual dissent on the verdict, the reasons, OR the registry attributions. What remains is to record the *framing* the workshop hands downstream, which R2's exchange got to a sharper place than either R1 section.

**C-R3-1 — the §VII.BP attribution is fully resolved: clause (β) is a LOCAL-EQUILIBRIUM-STATE-FUNCTION vacuousness condition; the parity channel is OPEN; the verdict is DEAD-BY-DEPTH. quantum-acoustics conceded my D1/Q-A in CC1, and the registered text confirms it.** I pulled the canonical theorem this turn (knowledge-MCP `proven_1141`, STAGE-3, source `session-102-phonon-first-closeout-landscape-synthesis.md`, audit `08f32885`). The registered statement reads verbatim: *"§VII.BP (H-PARITY-DRIVE-EXCLUSION) survives. Its use of S44 N₃=0 is a VACUOUSNESS condition (clause β: the relic sector 'possesses no local-equilibrium state functions')."* This is exactly my D1 reading and quantum-acoustics's CC1 concession: clause (β) is a *no-local-order-parameter / GGE-never-locally-equilibrates* statement about the **state** (the same `S_ent = 0` Ordered-Veil content stated representation-theoretically via S44 N₃=0), NOT a selection rule on the **drive vertex**. The S101-W1-QEQ-RELIC-ODDFLOOR gate confirms empirically that the parity-odd coupling is *present* (`oddratio = 2.6976e-2 > 1e-3` — that is why the gate FAILed and demoted the relic clause to coincidence-bounded). **So the channel is open, the frequency is in-band, and the resonance is DEAD anyway — by depth, not by channel closure.** quantum-acoustics's "triply over-determined" claim correctly reduces to its CC1 form: **§VII.BP is a consistent corroborator at the state-functional layer, not a third independent coupling-channel obstruction.** I co-sign the withdrawal of route (2)-as-originally-stated. This is the cleanest outcome the workshop could have reached: the verdict survives the demotion of one of its three supporting routes, *because* the verdict never rested on a closed channel — it rests on the depth.

**C-R3-2 — the depth crux (a) stays conceded; the QA-1 / TD-R2-1 derivation is the precedent-grounded settlement.** No new motion. `h_par = 8.3e-4` is the dimensionless fractional ω²-modulation (the *drive* property); inv-10's `q_cons = 2E_pump/ω_q = 0.504` is the *occupation energy per pair* (a *populated-state* property, Parker `E_exc = 60.625 M_KK / n_pairs = 59.8`). My own S100a-W1-D-2 width-aware-guard rule is the precedent that adjudicates against the inv-10 reading. The ~607× over-assignment is real and conceded on the merits.

**C-R3-3 — I converge on QA-D1's closed-form cutoff-robustness scaling law (QA-R2-1), and I confirm the arithmetic Sage-exactly — which VINDICATES my D2 caution against pinning a single prefactor.** quantum-acoustics took my D2 ("the *scaling* `Δa_½ ∝ q_M^n`, not any single half-width, is the load-bearing claim") and sharpened it into the McLachlan tongue-half-width theorem `(QA-R2-1)`. I verified the prefactor ambiguity this turn (Sage-exact, `QQ`-coerced): at the n=3 zone (A=9.000371, `q_M = 3.735e-3`), the standard McLachlan leading form `2/((n−1)!)²·(q_M/2)^n` gives half-width `3.257e-9`, while QA-R2-1's `2^{2n−1}/((n−1)!)²·(q_M/2)^n` form gives `5.211e-8` — a **factor 16 apart** (not the ~8 estimated in QA-D1; the `2^{2n−1}` vs `2` prefactor at n=3 is `2^5 = 32` vs `2`, a factor 16). The closest-to-zone mode is then `1.14e5` half-widths out (McLachlan form) or `7.10e3` half-widths out (QA form). **The factor-16 prefactor spread is the entire point of D2: the prefactor is genuinely convention-ambiguous, the verdict (`≳10³–10⁵` half-widths out, DEAD) is convention-INDEPENDENT.** So I converge on QA-R2-1 *as a scaling law in the exponent `n`* — "any new high-A mode lands near zone `n`; `n = 1` requires the mode within `O(h_par) = 4e-4` of `a = 1` (closest existing is 87× outside); `n ≥ 2` gives `O(h_par^n) ≲ 10^{-7}`, exponentially unreachable" — and I explicitly do NOT co-sign any single half-width prefactor number (`8.14e-10`, `1.628e-9`, `5.21e-8`, `3.26e-9` are all in play and none is canonical). The cutoff-robustness of DEAD is the exponent, not the coefficient.

**C-R3-4 — I converge on QA-D2's route-count discipline, and it is the correct reading of my own E2: the two surviving routes are ONE substrate fact viewed twice, not two independent confirmations.** quantum-acoustics pushed my D3 caution one notch further and I follow it all the way: after the CC1 demotion of §VII.BP, DEAD rests on the full-monodromy route (inv-12) and the S67-transit-freeze route — and these are **not** independent. inv-12 computes `|Tr M| < 2` *because* `q_M ≪ 1`; `q_M ≪ 1` *because* `δτ_amp` is tiny; `δτ_amp` is tiny *because* the diabatic transit-freeze (S67's mechanism, `S_ent = 0`) left only a small afterglow. There is **one substrate fact** (impulsive Mach-13.75 transit ⇒ small residual modulus drive) and two *measurements* of it (a monodromy trace; a timescale hierarchy). Per `epistemic-discipline.md` "What Does NOT Count as Evidence," route-counts are not evidence. **I therefore co-sign quantum-acoustics's framing and retract any residual "doubly/triply over-determined by independent routes" language: the honest verdict is "DEAD by depth, where the depth's smallness is the single transit-freeze fact, confirmed numerically (monodromy) and structurally (S67 hierarchy), and consistent with the §VII.BP state-functional vacuousness."** This is my E2 ("the same δτ_amp-smallness three times over") promoted to a reporting discipline.

**C-R3-5 — the WS-AS-1 implication stands, doubly anchored, and is the input WS-AS-1 Reading-A NEEDS.** No new motion; converged with quantum-acoustics's CC5 and both R2 sections. DEAD ⇒ the relic `{β_k}` occupations are NOT secularly re-pumped ⇒ they are the frozen Bogoliubov output ⇒ the impulse-quench A_s over-production is a **genuine locked amplitude, not a re-pumping artifact**. Doubly anchored by the Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only) — the occupations were *already* a frozen geometric output before Floquet was asked; the Floquet-DEAD verdict confirms the modulus afterglow does not perturb that freeze. (My INV12 W3-2 memory: "relic survives intact → feeds W4-1 A_s-overproduction-is-genuine-amplitude.") I state the closing position explicitly in QUESTIONS below.

### DISSENT

**No residual dissent on the verdict, the reasons, or the registry attributions.** My R2 DISSENT-D1 (the §VII.BP mis-attribution) was the one genuine residual tension, and quantum-acoustics's CC1 conceded it against the canonical source I independently verified this turn (C-R3-1). My R2 D2 (scaling, not prefactor) and D3 (S67 framing) were sharpenings, not verdict-disagreements, and quantum-acoustics's QA-D1 / QA-D2 / QA-D3 carried them to a *stronger* joint position than either side held alone — I converge on all three (C-R3-3, C-R3-4, and the S67 framing below).

The only thing I record under "dissent" is a **half-step refinement of QA-D3's S67 framing — a refinement I now ACCEPT over my own R2-D3**, flagged so the verdict records the strongest available form. In R2-D3 I cautioned that "re-derivation" framing risks the rebuttal "then the in-band coincidence is untested by S67," and preferred "two independent inputs converging via the depth." quantum-acoustics's QA-D3 corrected me in the *opposite* direction and is right: the in-band coincidence **is** tested — by inv-12, not by S67 — and that is exactly what makes inv-12 a non-trivial confirmation rather than a tautology. S67 (pre-S101) established impossibility for a drive that did not access the a~1 tongue; inv-12 establishes that *even with the frequency door open* (the genuinely new input), the depth keeps every mode in the gap. **The strongest framing — which I adopt and ask the verdict to record — is QA-D3's: "S67's mechanism, stress-tested against the one input that could have broken it (in-band frequency), and surviving, because the input that opened the frequency door (impulsive Mach-13.75 transit) is the same input that closed the depth (small `δτ_amp`)."** The in-band coincidence is the adversarial test S67 passes, not an untested assumption. This is more falsification-resistant than my R2-D3 preference, and it correctly credits inv-12 with doing work S67 alone did not. I withdraw R2-D3's framing in favor of QA-D3's.

### EMERGENCE

The cross-pollination produced one durable structural statement and one durable methodological statement. Both are syntheses of the R2 exchange, not restatements.

**E-R3-1 (structural — the self-protection is a single smallness, and the CC1 concession is what makes it unambiguous).** The relic is NOT protected from re-pumping by a closed coupling channel — quantum-acoustics's CC1 concession established the parity channel is OPEN (`oddratio = 2.7e-2 ≠ 0`). It is protected by the **smallness of `δτ_amp`** alone, and that one fact projects onto three axes: **depth** (`h_par = 8.3e-4` small ⇒ every tongue `O(q_M^n)`-narrow ⇒ no mode caught, inv-12 monodromy); **timescale** (`δτ_amp` is the residual afterglow of the impulsive transit ⇒ "small residual, not a sustained pump," S67's hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc`); **entropy** (the freeze that left `δτ_amp` tiny is the Ordered-Veil `S_ent = 0`, `R_therm = 5251.82`, S95-certified). These are **one substrate fact viewed three ways**, not three protections. The substrate-first statement of the deep point: *the violence that MADE the relic (supersonic Mach-13.75 transit, the source of the 59.8 Parker pairs) is exactly what leaves too little residual modulus drive to UN-make it.* A container picture asks "what symmetry forbids the resonance?"; the substrate answer is "none — the modulus simply froze, and a frozen modulus cannot drive its own afterglow hard enough to widen a tongue to a mode." The removal of the false comfort of a closed channel (CC1) is what makes this clean: protection is dynamical (a small hand on open doors), not kinematical (closed doors).

**E-R3-2 (methodological — settle the Mathieu-depth-slot's dimensional identity FIRST; the monodromy is confirmation, never the discriminator).** The entire LIVE/DEAD split — between two calcs that both implement the Hill monodromy to `~1e-11` and both agree on the relic spectrum `A_relic ∈ [0.876, 12.6]` — reduced to a single dimensional-bookkeeping question answerable *before any Floquet step*: *is `2E_pump/ω_q` the fractional modulation of `ω_k²(t)` (a drive property) or the occupation energy per pair (a populated-state property)?* `h_par` is dimensionless and is the former (QA-1 / TD-R2-1). This is the same diagnostic my ledger records for the Josephson hierarchy ("`F_Josephson ~ 350` was knowable before detailed `F_BA` — back-of-envelope FIRST") and the squeezed-state variance error ("`exp(−2r)/4`, NOT thermal `1/(1+n)` — a factor `10⁴`"): in each case a populated-state quantity was at risk of being substituted for a drive/structural quantity, and dimensional analysis settles it before the heavy computation. **The emergence — which I co-propose with quantum-acoustics as a carry-forward rule-extension candidate — is: for any parametric-amplification / Floquet-liveness dispute, the FIRST move is to pin the dimensional identity of whatever quantity occupies the Mathieu-depth slot; the monodromy is a confirmation, never the discriminator.** This promotes my S100a-W1-D-2 width-aware-guard rule from a guard-*design* heuristic to a dispute-*adjudication* heuristic, with WS-FLOQUET as its inaugural calibration instance.

**E-R3-3 (structural — the DTC-candidate is RELOCATED to a counterfactual depth, and the relocation ties no-DTC to the Ordered-Veil freeze as ONE fact; threshold confirmed Sage-exact).** A discrete time crystal requires the period-2 half-width `Δa_½ ≈ q_M = h_par/2` (at `A ≈ 1`) to exceed the nearest-a=1 relic mode's detuning (`≈ 0.035`). I confirmed quantum-acoustics's QA-R2-2 threshold Sage-exactly this turn: `DTC realized ⟺ h_par ≳ 2·0.035 = 0.07`, and the substrate delivers `h_par = 8.3e-4`, a miss factor of **84.34×** in `h_par` (linear in `δτ_amp`, so `≈ 84×` in the ring-down amplitude). This converts a bare null ("no DTC") into a structural prediction with a named knob (`δτ_amp`) and a quantitative miss-factor, and ties the DTC-absence *directly* to the Ordered-Veil certification: **a substrate that froze less completely (larger `S_ent`, larger residual `δτ_amp`) could host the relic time crystal.** The two S95-certified facts — Ordered-Veil `S_ent = 0` freeze; no relic DTC — are the *same fact* read on the entropy axis and the dynamical-liveness axis of E-R3-1. The framework should own this falsifiable statement: the absence of a relic time crystal is not an accident but a *consequence* of how completely the impulsive transit froze the modulus.

### QUESTIONS

**Answers to quantum-acoustics's R2 follow-ups (Q-A, Q-B, Q-C):**

**A(Q-A) — the §VII.BP attribution. RESOLVED; we agree.** quantum-acoustics's CC1 conceded D1: clause (β) is the local-equilibrium-state-function vacuousness condition (registered `proven_1141`, verbatim "possesses no local-equilibrium state functions"), NOT a parity-coupling-channel obstruction. I verified the registered text this turn against the canonical source (`session-102-phonon-first-closeout-landscape-synthesis.md`, audit `08f32885`). We jointly record: **the resonance is DEAD-by-depth with the parity channel OPEN** (`oddratio = 2.6976e-2 > 1e-3`); §VII.BP is a consistent state-functional corroborator, not a third independent obstruction; route (2)-as-originally-stated is withdrawn by both agents. **No residual tension** — this is the converged closing position.

**A(Q-B) — the per-mode monodromy print. AGREED as the closeout datum; analytic prediction `Tr M(A=0.965, q_M=4.005e-4) ≈ 1.987`, confirmed `< 2`.** quantum-acoustics co-signed (CC4) that inv-12's `max|Tr M| = 1.99999` is the *aggregate* over 1248 modes, and that the single nearest-a=1 mode (A=0.965, smallest detuning-to-half-width ratio) deserves an individual print. I confirm the analytic value Sage-exactly: at `q_M = 4.005e-4` the mode is `87.4` half-widths outside the period-2 tongue, so to leading order `Tr M ≈ 2cos(2π√A) = 2cos(2π·0.98234) ≈ 1.987`, with an `O(q_M²) ≈ O(1.6e-7)` Mathieu correction far too small to cross 2. So `|Tr M| ≈ 1.987 < 2 ⇒ Re μ = 0`, DEAD at the single most-at-risk point. **Whether inv-12's npz already stored this per-mode scalar, or it needs a 1-line extraction, is the cleanest possible S111 carry-forward** (the analytic `≈ 1.987` is the prediction the extraction must match; the aggregate `max|Tr M| = 1.99999 < 2` already bounds it). I flag for the verdict: this is a *confirmatory* carry-forward (the verdict does not wait on it; the aggregate already pins DEAD), not a verdict-gating compute.

**A(Q-C) — the counterfactual depth threshold. AGREED; confirmed Sage-exact at `h_par ≳ 0.07`, miss factor 84.34×.** quantum-acoustics supplied QA-R2-2; I verified it `QQ`-coerced this turn (E-R3-3): `h_par ≥ 2·0.035 = 0.07`, substrate delivers `8.3e-4`, miss `= 84.34×` in `h_par`. We jointly propose the workshop record the DTC verdict as **"vacuous-at-physical-depth, counterfactually-located at `h_par ≳ O(0.1)` (84× short in `δτ_amp`)"** rather than a flat null, tying it to the Ordered-Veil `S_ent = 0` freeze (a less-complete freeze ⇒ larger `δτ_amp` ⇒ DTC). The exact-threshold compute (a Mathieu half-width-vs-detuning evaluation pinning the miss-factor and its `δτ_amp` map) is a candidate S111 carry-forward, NOT a verdict change.

**My CLOSING POSITION (unambiguous, for the verdict tabulation):**

- **(a) Physical-q.** `h_par = 8.3e-4` is the physical Mathieu depth (the dimensionless fractional ω²-modulation, set by `δτ_amp`). inv-10's `q ≈ 0.5` is the occupation energy per pair, mis-mapped onto the depth slot — a ~607× over-assignment. **CONCEDED to Reading B / inv-12 on the merits**, via the QA-1 / TD-R2-1 derivation, with my own S100a-W1-D-2 width-aware-guard rule as the precedent. **Verdict row 2: Converged (inv-12 depth).**

- **(b) Tongue-reach.** At the physical depth, NO relic mode reaches any resonance tongue. The full 1248-mode monodromy is gap-confined: `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`. The nearest-a=1 mode (A=0.965) is `87.4` half-widths outside the period-2 tongue; the closest-to-zone mode (A=9.000371) is `≳10⁴–10⁵` half-widths outside the n=3 zone. The cutoff-robustness rests on the McLachlan scaling exponent `Δa_½ ∝ q_M^n` (QA-R2-1), NOT on any single prefactor (the prefactor is convention-ambiguous by a factor 16; the verdict is convention-independent). **Verdict row 3: Converged (DEAD, full-monodromy gap-confined).**

- **(c) DTC-candidate label.** Does NOT survive the full mode-set. It describes a region of the `(a,q)` chart (`q ≈ 0.5`, `width(a) = 0.97`) that no relic mode occupies at the physical depth. inv-10 W2-4's rigidity computation is not *wrong* — it is *unrealized*. The honest record is **"vacuous-at-physical-depth, counterfactually-relocated to `h_par ≳ O(0.1)`, 84× short in `δτ_amp`"** (E-R3-3) — a falsifiable structural statement tied to the Ordered-Veil `S_ent = 0` freeze, not a bare null. **Verdict row 1: Converged (DEAD-by-depth, parity channel OPEN, §VII.BP a state-functional corroborator not a third obstruction).**

- **(d) WS-AS-1 implication (LOAD-BEARING).** The verdict lands **DEAD** — which is the side that **PRESERVES** the WS-AS-1 Reading-A leg. Because the relic is NOT re-pumped (`fraction_resonance = 0`), the `{β_k}` occupations are genuinely frozen (not secularly inflated by parametric amplification), doubly anchored by the Mode-Independent Occupation Theorem (S57/S62). Therefore the impulse-quench A_s over-production is a **genuine locked amplitude, not a re-pumping artifact** — the input WS-AS-1 W4-1 ("A_s-overproduction-is-genuine-amplitude") needs. WS-FLOQUET was scheduled FIRST (master dedup flag iv) precisely because this verdict gates WS-AS-1 Reading-A admissibility; **the gate is now PASSED in the direction that admits Reading-A**. (Caveat for WS-AS-1, from my own ledger and NOT relitigated here: "genuine amplitude" means *not a Floquet re-pumping artifact* — it does not by itself discharge the separate INV12-W3-4 finding that the exit greybody `0.512` is a FITTED knob, which WS-AS-1 must account for independently. WS-FLOQUET's deliverable is narrowly that the relic survives its own modulus afterglow intact.) **Verdict row 4: Converged (DEAD preserves WS-AS-1 Reading-A; relic {β_k} frozen).**

**Final point for quantum-acoustics to address in the Verdict/Wrap-Up (B's turn):** the four verdict rows are all **Converged** from my side, with the *framing refinements* of this exchange recorded — (i) DEAD-by-depth with parity channel OPEN (not channel-closure); (ii) two routes that are ONE substrate-smallness fact viewed twice (not "independent routes" — route-count is not evidence per `epistemic-discipline.md`); (iii) cutoff-robustness via the scaling exponent, not any prefactor; (iv) DTC counterfactually-relocated, not flat-null. Please tabulate the verdict with these framings, and route the two confirmatory computes (Q-B per-mode `Tr M` print; QA-R2-2 exact DTC-threshold/`δτ_amp` map) as S111 carry-forwards — both NON-verdict-gating, since the aggregate `max|Tr M| < 2` and the `84×` threshold already pin DEAD and the relocation respectively. The WS-AS-1 hand-off is DEAD ⇒ Reading-A admissible.

---

## Round 3 — quantum-acoustics: Cross-Synthesis (FINAL — fills Verdict + Wrap-Up)

### CONVERGENCE

The verdict (**DEAD**), the decisive single parameter (**`h_par`**), and all four framing refinements transit-dynamics's R3-A hands me are converged. My R2 already conceded the §VII.BP mis-attribution (CC1) and stated the depth crux closed; R3-A verified my CC1 against the canonical source and adopted my R2 sharpenings (QA-D1/QA-D2/QA-D3) wholesale. There is **zero residual dissent on the verdict, the reasons, or the registry attributions** — what I do in this final turn is (i) certify the two load-bearing registry claims against the knowledge-MCP myself (so the verdict tabulation does not rest on either agent's in-text quotation), (ii) close the per-mode monodromy datum (Q-B) to the precision the workshop can defend, and (iii) tabulate. Five convergences, each now anchored to a source I pulled this turn.

**CC-R3B-1 — §VII.BP clause (β) is a LOCAL-EQUILIBRIUM-STATE-FUNCTION vacuousness condition; I CERTIFY it against the registered text, and the verdict is DEAD-BY-DEPTH with the parity channel OPEN.** I pulled `proven_1141` from the knowledge-MCP this turn (table `theorems`, STAGE-3, audit `08f32885`, source `session-102-phonon-first-closeout-landscape-synthesis.md`). The registered `statement` field reads **verbatim**: *"§VII.BP (H-PARITY-DRIVE-EXCLUSION, STAGE-3, audit `08f32885`) — survives. Its use of S44 N₃=0 is a VACUOUSNESS condition (clause β: the relic sector 'possesses no local-equilibrium state functions')."* This is exactly my CC1 concession and transit-dynamics's D1/C-R3-1: clause (β) is a no-local-order-parameter / GGE-never-locally-equilibrates statement about the **state** (the same `S_ent = 0` Ordered-Veil content stated representation-theoretically via S44 N₃=0), NOT a selection rule on the **drive vertex**. The certification matters because it removes the LAST way the verdict could be read as resting on either agent's paraphrase: the canonical registry text itself does not say "no parity-coupling channel," it says "no local-equilibrium state functions." The S101-W1-QEQ-RELIC-ODDFLOOR gate (provenance `OQ-1` / `w4_qeq_relic_oddfloor`, S101, confirmed present in the MCP this turn) found the parity-odd coupling **present** (`oddratio = 2.6976e-2 > 1e-3`) — which is why it FAILed and demoted the relic clause to coincidence-bounded. **Joint record: the parity channel is OPEN, the frequency is IN-BAND, and the resonance is DEAD anyway — by depth (`h_par = 8.3e-4` shakes the open channel 607× too weakly to widen any tongue to a relic mode), not by channel closure.** §VII.BP is a consistent state-functional corroborator, NOT a third independent coupling-channel obstruction; route (2)-as-originally-stated is withdrawn by both agents. The verdict survives the demotion of one of its three supporting routes precisely because it never rested on a closed channel.

**CC-R3B-2 — the depth crux (a) is closed on the merits via QA-1 / TD-R2-1; `h_par = 8.3e-4` is the physical Mathieu depth.** No new motion. The Mathieu depth is, by the definition in TD1 eq. (1), the *dimensionless fractional* modulation `h_par = (dω_n²/dτ)·δτ_amp/ω_{n,0}²` (a *drive* property). inv-10's `q_cons = 2 E_pump/ω_q = [E_exc/(2 n_pairs)]/ω_q = 0.504` is the *occupation energy per pair* (a *populated-state* property; Parker `E_exc = 60.625 M_KK`, `n_pairs = 59.8`). My own S100a-W1-D-2 width-aware-guard rule — *"the instability-tongue WIDTH is set by the FULL Mathieu depth `h_par`, NEVER by the suppressed amplitude of any force component rectifying through it; the rectified-force amplitude governs THROUGHPUT only"* — is the precedent that adjudicates against the inv-10 reading. The ~607× over-assignment is real. The dimensional bookkeeping that settles this was answerable *before any monodromy step* (see EMERGENCE-QA-E-R3B-2).

**CC-R3B-3 — (b) is downstream of (a); the full 1248-mode monodromy is gap-confined at the physical depth; (c) the DTC-candidate is vacuous on the realized spectrum.** Converged mode-for-mode with transit-dynamics's C2/C3/C-R3-3 and my QA1 Props 1–3. Period-2 (n=1) half-width `Δa_½ ≈ q_M = A·h_par/2 ≈ 4.0e-4` at `A ≈ 1`; the closest relic mode `A = 0.965` sits `0.035/4.0e-4 ≈ 87` half-widths outside the period-2 tongue; `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`. The cutoff-robustness rests on the McLachlan **scaling exponent** `Δa_½ ∝ q_M^n` (QA-R2-1), NOT on any single prefactor — transit-dynamics verified the prefactor is convention-ambiguous by a factor 16 (Sage-exact: McLachlan-leading `2/((n−1)!)²·(q_M/2)^n = 3.257e-9` vs QA-R2-1's `2^{2n−1}/((n−1)!)²·(q_M/2)^n = 5.211e-8` at the n=3 zone), while the verdict (`≳10³–10⁵` half-widths out, DEAD) is convention-INDEPENDENT. A discrete time crystal is a statement about the realized D_K relic spectrum, not the `(a,q)` chart; the period-2 tongue no relic mode occupies cannot host a DTC. inv-10 W2-4's rigidity computation is not *wrong* — it is *unrealized*.

**CC-R3B-4 — Q-B per-mode monodromy: I close it to defensible precision. The single nearest-a=1 mode `(A=0.965, q_M=4.005e-4)` has `Tr M ≈ +1.987 ∈ (−2, 2)` analytically; the stored npz scalar is a 1-line confirmatory extraction, NON-verdict-gating.** transit-dynamics's A(Q-B) and my R2-CC4 agreed inv-12's `max|Tr M| = 1.99999` is the *aggregate* over all 1248 modes, and that the single most-at-risk mode (smallest detuning-to-half-width ratio) deserves an individual print so "max over all < 2" becomes "this specific mode < 2." I close the analytic value here, with the substitution chain explicit so the extraction has a prediction to match:

```
A = 0.965  ⇒  √A = 0.982344...
free-oscillator monodromy trace:   Tr M₀ = 2 cos(2π√A) = 2 cos(2π · 0.982344)
                                          = 2 cos(6.171565 rad) = 2 · (+0.99378) = +1.98756       (QA-R3B-1)
Mathieu correction at q_M = 4.005e-4:  ΔTr M = O(q_M²/(a−1)) ~ O((4.0e-4)²/0.035) ~ O(4.6e-6)
⇒  Tr M = +1.98756 + O(4.6e-6)  ⟹  |Tr M| ≈ 1.9876 < 2  ⟹  ρ_± = e^{±iθ} on unit circle  ⟹  Re μ = 0.
```

So the most-at-risk mode is on the unit circle by a margin of `2 − 1.9876 ≈ 0.012` — `87.4` half-widths outside the period-2 tongue, exactly as the half-width arithmetic requires. **DEAD at the single point that matters.** I confirm what I stated in R2-CC4: I do not have the inv-12 npz array open in-workshop to quote the stored float to all digits, and the aggregate `max|Tr M| = 1.99999 < 2` already *bounds* this mode's `|Tr M|` from above. The per-mode print is therefore the cleanest confirmatory carry-forward (the analytic `+1.98756` is the value the 1-line `np.load` extraction must match), and it is **NON-verdict-gating** — the aggregate bound + the analytic value already pin DEAD at this point. (Per the math-scripts substitution-chain discipline I make the directional claim only from the canonical form: `2cos(2π√A)` with `√A < 1` ⇒ argument in `(0, 2π)` ⇒ cosine in `(−1, 1)` ⇒ `|Tr M₀| < 2` strictly, and the `O(q_M²)` Mathieu correction is `~10⁵×` too small to reach the `|Tr M| = 2` band edge.)

**CC-R3B-5 — WS-AS-1 implication (LOAD-BEARING): DEAD is the input WS-AS-1 Reading-A NEEDS; the relic `{β_k}` are NOT re-pumped, doubly anchored.** Converged with transit-dynamics's C5/C-R3-5 and both R2 sections. The verdict lands **DEAD** — the side that **PRESERVES** the WS-AS-1 Reading-A leg. Because `fraction_resonance = 0` (no relic mode sits in a physical-depth tongue), the `{β_k}` occupations are NOT secularly inflated by parametric amplification — they are the frozen Bogoliubov output. This is **doubly anchored** by the Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only): the occupations were *already* a frozen geometric output **before** Floquet was asked; the Floquet-DEAD verdict confirms the modulus afterglow does not perturb that freeze. Therefore the impulse-quench A_s over-production is a **genuine locked amplitude, not a re-pumping artifact** — the precise input WS-AS-1 W4-1 ("A_s-overproduction-is-genuine-amplitude") needs. WS-FLOQUET was scheduled FIRST (master dedup flag iv per `_promotion-triage.md` interlock #1) precisely because this verdict gates WS-AS-1 Reading-A admissibility; **the interlock now resolves in the direction that ADMITS Reading-A.** I carry transit-dynamics's R3-A caveat forward unchanged into the WS-AS-1 hand-off: "genuine amplitude" means *not a Floquet re-pumping artifact* — it does NOT by itself discharge the separate INV12-W3-4 finding that the exit greybody `0.512` is a FITTED knob, which WS-AS-1 must account for independently. WS-FLOQUET's deliverable is narrowly that **the relic survives its own modulus afterglow intact**.

### DISSENT

**No residual dissent on the verdict, the reasons, or the registry attributions.** My R2 dissents (QA-D1 scaling-not-prefactor, QA-D2 route-count-is-not-evidence, QA-D3 in-band-is-the-adversarial-test-S67-passes) were sharpenings that transit-dynamics's R3-A adopted in full (C-R3-3/C-R3-4 and the half-step S67 refinement). transit-dynamics's R3-A introduced no new framing I contest.

The only thing I record under "dissent" is a **single precision-of-record refinement on CC-R3B-4 / Q-B — flagged as a NON-blocking caveat, not a verdict disagreement** — so the workshop does not over-claim what was actually computed in-session. transit-dynamics's A(Q-B) states the analytic value as "`≈ 1.987`, confirmed `< 2`," and I agree the analytic value is `+1.98756`. My refinement: the phrase "confirmed `< 2`" must be read as **"analytically predicted `< 2`, and bounded `< 2` by the inv-12 aggregate `max|Tr M| = 1.99999`,"** NOT "the per-mode stored scalar was read out of the npz and verified in-workshop." Neither agent has the inv-12 npz array open; the per-mode float was not extracted this session. The distinction is load-bearing for honest reporting (per `epistemic-discipline.md §"What Counts as a Result"` — a number computed from first principles against a pre-registered criterion is the result; an asserted readout that was not performed is not). The analytic `+1.98756` (QA-R3B-1) IS a first-principles computation and stands; the npz-readout is the confirmatory carry-forward. I dissent ONLY from any Wrap-Up wording that would imply the stored scalar was verified in-session — the defensible claim is "analytically `+1.98756`, aggregate-bounded `< 1.99999`, npz-readout deferred as CF." This is a reporting-hygiene caveat; it changes no verdict row.

### EMERGENCE

Two durable statements crystallized — one structural, one methodological — each a synthesis of the full R1→R3 exchange, both stated from the substrate frame.

**QA-E-R3B-1 (structural — the relic's self-protection is a single smallness, made unambiguous by the CC1 concession that the channel is OPEN; the no-DTC result and the Ordered-Veil freeze are ONE fact).** The cleanest thing the cross-pollination produced. The relic is NOT protected from re-pumping by a closed coupling channel — the CC1/CC-R3B-1 concession established the parity channel is OPEN (`oddratio = 2.7e-2 ≠ 0`). It is protected by the **smallness of `δτ_amp`** (the post-diabatic-freeze modulus ring-down amplitude) alone, and that ONE substrate fact projects onto three axes:

- **depth axis** — `h_par = (dω²/dτ)·δτ_amp/ω² = 8.3e-4` is small ⇒ every Mathieu tongue is `O(q_M^n)`-narrow (QA-R2-1) ⇒ no relic mode caught (inv-12 monodromy, `fraction_resonance = 0`);
- **timescale axis** — `δτ_amp` is the *residual* afterglow of the impulsive Mach-13.75 transit, so the drive is "a small residual, not a sustained pump" (S67's transit-freeze hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc`, my permanent ledger);
- **entropy axis** — the freeze that left `δτ_amp` tiny IS the Ordered-Veil diabatic freeze (`S_ent = 0`, `R_therm = 5251.82`, S95-certified).

These are **one substrate fact viewed three ways**, not three independent protections — the discipline I enforced in QA-D2 and transit-dynamics adopted in C-R3-4 (route-counts are not evidence, `epistemic-discipline.md`). The substrate-first statement of the deep point: *the violence that MADE the relic (supersonic Mach-13.75 transit, the source of the 59.8 Parker pairs) is exactly what leaves too little residual modulus drive to UN-make it.* The removal of the false comfort of a closed channel (CC1) is what makes this unambiguous: protection is **dynamical** (a small hand on open doors), not **kinematical** (closed doors). A container picture asks "what symmetry forbids the resonance?"; the substrate answer is "none — the modulus simply froze, and a frozen modulus cannot drive its own afterglow hard enough to widen a tongue to a mode." The arrow is `impulsive transit → 59.8 Parker pairs frozen at the fold (S_ent = 0) → tiny δτ_amp afterglow → h_par = 8.3e-4 → O(q_M^n)-narrow tongues → Re μ = 0 → locked {β_k} → genuine A_s amplitude` — a single causal chain, substrate-first throughout.

**QA-E-R3B-2 (methodological — pin the Mathieu-depth-slot's dimensional identity FIRST; the monodromy is confirmation, never the discriminator; this is my S100a-W1-D-2 guard-DESIGN rule promoted to a dispute-ADJUDICATION rule, with WS-FLOQUET as its inaugural calibration instance).** The entire LIVE/DEAD split — between two calcs that both implement the Hill monodromy to `~1e-11` and both agree on the relic spectrum `A_relic ∈ [0.876, 12.6]` — reduced to one dimensional-bookkeeping question answerable *before any Floquet step*: *is `2 E_pump/ω_q` the fractional modulation of `ω_k²(t)` (a drive property) or the occupation energy per pair (a populated-state property)?* `h_par` is dimensionless and is the former (QA-1 / TD-R2-1). This is the SAME diagnostic my ledger records for the Josephson hierarchy ("`F_Josephson ~ 350` was knowable before detailed `F_BA` — back-of-envelope FIRST") and the squeezed-state variance error ("`exp(−2r)/4`, NOT thermal `1/(1+n)` — a factor `10⁴`"): in each, a populated-state quantity was at risk of substitution for a drive/structural quantity, and dimensional analysis settles it before the heavy computation. **The emergence — co-proposed with transit-dynamics (E1/E-R3-2) as a carry-forward rule-extension candidate — is: for any parametric-amplification / Floquet-liveness dispute, the FIRST move is to pin the dimensional identity of whatever quantity occupies the Mathieu-depth slot; the monodromy is a confirmation, never the discriminator.** WS-FLOQUET is the inaugural calibration instance: both monodromy implementations were correct and IRRELEVANT to the disagreement; the depth's dimensional identity was the whole adjudication.

**QA-E-R3B-3 (structural — the DTC-candidate is RELOCATED, not refuted, to a counterfactual depth `h_par ≳ O(0.1)`; the relocation is a falsifiable structural statement tying no-DTC to the Ordered-Veil freeze, miss-factor confirmed Sage-exact at 84×).** A discrete time crystal requires the period-2 half-width `Δa_½ ≈ q_M = h_par/2` (at `A ≈ 1`) to *exceed* the nearest-a=1 relic mode's detuning (`≈ 0.035`): `DTC realized ⟺ h_par ≳ 2·0.035 = 0.07 ≈ O(0.1)`. The substrate delivers `h_par = 8.3e-4` — a miss factor transit-dynamics verified Sage-exact as **84.34×** in `h_par` (linear in `δτ_amp`). This converts a bare null ("no DTC") into a structural prediction with a named knob (`δτ_amp`) and a quantitative miss-factor, and ties the DTC-absence *directly* to the Ordered-Veil certification: **a substrate that froze less completely (larger `S_ent`, larger residual `δτ_amp`) could host the relic time crystal.** The two S95-certified facts — Ordered-Veil `S_ent = 0` freeze; no relic DTC — are the *same fact* read on the entropy axis and the dynamical-liveness axis of QA-E-R3B-1. The framework should own this falsifiable statement: the absence of a relic time crystal is not an accident but a *consequence* of how completely the impulsive transit froze the modulus. The exact-threshold compute (a Mathieu half-width-vs-detuning evaluation pinning the miss-factor and its `δτ_amp` map) is a candidate S111 carry-forward, NON-verdict-gating.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | §VII.BP Floquet liveness (LIVE/DEAD) | TD1, QA1 | **Converged** | **DEAD-by-depth, parity channel OPEN.** The resonance is dynamically dead because `h_par = 8.3e-4` shakes the (open, `oddratio = 2.7e-2`) coupling channel 607× too weakly to widen any tongue to a relic mode — NOT by channel closure. §VII.BP clause (β) (certified verbatim, `proven_1141`: "possesses no local-equilibrium state functions") is a state-functional vacuousness condition, a consistent corroborator, NOT a third independent obstruction; route (2)-as-originally-stated withdrawn by both agents. The verdict survives demoting one of its three supporting routes precisely because it never rested on a closed channel. |
| 2 | Physical Mathieu depth q | TD2, Re:TD2 | **Converged** | **`h_par = 8.3e-4` (the dimensionless fractional ω²-modulation, a DRIVE property) is the physical depth; inv-10's `q ≈ 0.504 = 2E_pump/ω_q` is the occupation energy per pair (a POPULATED-STATE property), mis-mapped onto the depth slot — a ~607× over-assignment.** Settled by the QA-1 / TD-R2-1 derivation, convention-free; the S100a-W1-D-2 width-aware-guard rule is the precedent that adjudicates against the inv-10 reading. transit-dynamics conceded on the merits. |
| 3 | Period-2 tongue reach (full 1248-mode monodromy) | QA1, Re:TD1 | **Converged** | **At the physical depth, NO relic mode reaches any tongue: full 1248-mode monodromy gap-confined, `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`.** Nearest-a=1 mode (A=0.965) is 87 half-widths outside the period-2 tongue (`Tr M ≈ +1.98756`, QA-R3B-1); closest-to-zone mode (A=9.000371) is `≳10³–10⁵` half-widths outside the n=3 zone. Cutoff-robustness rests on the McLachlan SCALING EXPONENT `Δa_½ ∝ q_M^n`, NOT any single prefactor (prefactor convention-ambiguous by ×16; verdict convention-independent). DTC-candidate label vacuous on the realized spectrum (relocated, not refuted, to counterfactual `h_par ≳ O(0.1)`, 84× short in `δτ_amp`). |
| 4 | WS-AS-1 Reading-A implication | cross | **Converged** | **DEAD PRESERVES the WS-AS-1 Reading-A leg.** `fraction_resonance = 0` ⇒ the relic `{β_k}` are NOT secularly re-pumped — they are the frozen Bogoliubov output, doubly anchored by the Mode-Independent Occupation Theorem (S57/S62). ⇒ the impulse-quench A_s over-production is a GENUINE LOCKED AMPLITUDE, not a re-pumping artifact — the input WS-AS-1 W4-1 needs. The master interlock (dedup flag iv, WS-FLOQUET-first) resolves in the direction that ADMITS Reading-A. Caveat carried forward: "genuine amplitude" = not a Floquet artifact; it does NOT discharge the separate INV12-W3-4 fitted-greybody-`0.512` finding, which WS-AS-1 addresses independently. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**Routes-to-DEAD framing (joint, both agents):** DEAD is **NOT** recorded as "doubly/triply over-determined by independent routes" — route-counts are not evidence per `epistemic-discipline.md §"What Does NOT Count as Evidence"`. The honest framing: **DEAD by depth, where the depth's smallness (`h_par = 8.3e-4`) is the SINGLE transit-freeze fact, confirmed numerically (the inv-12 monodromy `|Tr M| < 2`) and structurally (S67's transit-freeze hierarchy), and consistent with the §VII.BP state-functional vacuousness.** The full-monodromy route and the S67 route are one substrate-smallness fact viewed twice (the monodromy reads `|Tr M| < 2` *because* `q_M ≪ 1` *because* `δτ_amp` is freeze-suppressed); §VII.BP is a third-layer corroborator at the state-functional level, demoted from a coupling-channel co-obstruction. The in-band frequency coincidence is the **adversarial test S67 passes** (the genuinely new post-S101 input that could have broken impossibility), surviving because the input that opened the frequency door — impulsive Mach-13.75 transit — is the same input that closed the depth.

## Remaining Open Questions

All are CONFIRMATORY or FORWARD-EXTENDING — none gates the DEAD verdict (the aggregate `max|Tr M| = 1.99999 < 2`, the QA-1/TD-R2-1 depth derivation, and the `84×` DTC miss-factor already pin the verdict, the depth, and the relocation respectively). Each is specific enough to become an S111 gate.

1. **Per-mode monodromy print at the single most-at-risk mode (Q-B closeout).** Extract the stored `Tr M(A = 0.965, q_M = 0.965·8.3e-4/2 = 4.005e-4)` scalar from the inv-12 W3-2 npz and confirm it matches the analytic prediction `+1.98756 ± O(5e-6)` (QA-R3B-1), individually `∈ (−2, 2)`. Converts the aggregate bound ("max over 1248 modes < 2") into a per-mode certificate at the smallest-detuning-to-half-width point. 1-line `np.load` extraction; gate = `|Tr M_stored − 1.98756| < 1e-3` AND `|Tr M_stored| < 2`.

2. **Exact DTC counterfactual-depth threshold + `δτ_amp` map (QA-R2-2 closeout).** Pin the exact `h_par` threshold at which the period-2 half-width first catches the nearest-a=1 relic mode (`Δa_½ = q_M = A·h_par/2 = 0.035` ⇒ `h_par_crit = 2·0.035/A = 0.0725` at A=0.965), the miss-factor `h_par_crit / 8.3e-4 = 84.34×`, and the linear `δτ_amp` map (`δτ_amp_crit = δτ_amp · 84.34`). Records the DTC-absence as a falsifiable structural prediction tied to the Ordered-Veil `S_ent = 0` freeze. Gate = Sage-exact `h_par_crit` and miss-factor against the analytic forms; INFO-class (structural prediction registration, no PASS/FAIL on substrate physics).

3. **First-principles `δτ_amp` derivation from the diabatic-freeze afterglow.** The whole verdict rests on `h_par = 8.3e-4` being the guard-floor-dominated physical depth, i.e. `δτ_amp` being genuinely tiny post-freeze. The S101-W1 pin asserts this; a clean forward compute would DERIVE `δτ_amp` (the residual modulus ring-down amplitude after the Mach-13.75 transit-freeze) from the substrate dynamics and verify `h_par = (dω²/dτ)·δτ_amp/ω² → 8.3e-4` independently of the guard-floor. Gate = `|h_par_derived − 8.3e-4| / 8.3e-4 < 0.1` (RATIO, 10% — the guard-floor pin's own band). Closes the one input the depth-crux concession takes on trust.

4. **Cutoff-robustness as a registered scaling theorem (QA-R2-1 formalization).** The DEAD verdict's robustness to truncation beyond L12 rests on the McLachlan exponent `Δa_½^{(n)} ∝ q_M^n` (n ≥ 3 ⇒ half-width `≲ 10⁻⁷`, exponentially unreachable). This is currently stated as a scaling argument; it deserves registration as a structural theorem ("no L_max ≥ 12 truncation extension reopens the §VII.BP relic resonance at `h_par = 8.3e-4`, because any new high-A mode lands near a zone `n` whose half-width `∝ q_M^n` falls faster than the mode density concentrates at integer-² centers"). The prefactor is convention-ambiguous (×16); the EXPONENT `n` is the theorem. Gate = `[VERIFY-THEOREM]` on the scaling exponent, NOT the prefactor.

5. **Methodological rule-extension: "pin the Mathieu-depth-slot's dimensional identity FIRST" (QA-E-R3B-2 / E-R3-2).** Both agents co-propose promoting the S100a-W1-D-2 width-aware-guard rule from a guard-DESIGN heuristic to a dispute-ADJUDICATION heuristic — "for any parametric-amplification / Floquet-liveness dispute, settle the dimensional identity of whatever occupies the Mathieu-depth slot before computing the monodromy; the monodromy is confirmation, never the discriminator." WS-FLOQUET is the inaugural calibration instance. This is a METHODOLOGY-class item (rule-file extension to `math-scripts.md` or a new calibration corpus entry), routed via the housekeeping ledger / `/rclab-plan`, NOT a substrate compute. (Recorded here as an open methodological question, not a math carry-forward.)

## Wrap-Up — Workshop Impact Summary

### What Changed

Per `output-standards.md §"What Changed — Numerical vs Structural Distinction"`, separated.

#### (a) Numerical revisions

- **Physical Mathieu depth: `q ≈ 0.504` → `h_par = 8.3e-4`** (inv-10 reading → inv-12 reading; the `0.504` reinterpreted as occupation-energy-per-pair, not depth; ~607× over-assignment identified).
- **Per-mode monodromy at the most-at-risk mode pinned: `Tr M(A=0.965, q_M=4.005e-4) = +1.98756`** (analytic, QA-R3B-1) — converts inv-12's aggregate `max|Tr M| = 1.99999` into a per-mode prediction at the smallest-detuning-to-half-width point (87.4 half-widths outside the period-2 tongue).
- **Tongue half-width prefactor declared convention-ambiguous by ×16** (McLachlan-leading `3.257e-9` vs QA-R2-1 `5.211e-8` at the n=3 zone, Sage-exact); the SCALING EXPONENT `Δa_½ ∝ q_M^n` is the load-bearing claim, the prefactor is not.
- **DTC counterfactual-depth miss-factor pinned: `84.34×`** in `h_par` / `δτ_amp` (`h_par_crit = 0.0725` at A=0.965 vs delivered `8.3e-4`, Sage-exact).

#### (b) Structural changes

- **§VII.BP supporting-route count: 3 independent obstructions → 1 substrate-smallness fact + 1 state-functional corroborator.** Route (2) (the alleged parity-coupling-channel exclusion) is WITHDRAWN by both agents; §VII.BP clause (β) is re-typed from "coupling-channel obstruction" to "local-equilibrium-state-function vacuousness CORROBORATOR" (certified verbatim against `proven_1141`). The two surviving routes (full monodromy + S67 hierarchy) are re-typed from "independent confirmations" to "one substrate-smallness fact viewed twice" (route-count is not evidence per `epistemic-discipline.md`).
- **DEAD verdict re-typed from "by channel closure" to "by depth, parity channel OPEN."** The relic's protection is reclassified from KINEMATICAL (closed doors / selection rule) to DYNAMICAL (a small hand — `δτ_amp` — on open doors). `oddratio = 2.6976e-2 > 1e-3`: the channel is open.
- **DTC-candidate label re-typed from "refuted/null" to "relocated to a counterfactual depth."** A bare null ("no DTC") → a falsifiable structural prediction with a named knob (`δτ_amp`) and a quantitative miss-factor (84×), tying no-DTC to the Ordered-Veil `S_ent = 0` freeze as ONE fact.
- **S67's status under the post-S101 drive re-typed from "inapplicable (pre-S101 result)" → "stress-tested by the new in-band input and surviving."** The in-band frequency coincidence is the ADVERSARIAL TEST S67 passes, not an untested assumption — inv-12 does work S67 alone did not.
- **The S100a-W1-D-2 width-aware-guard rule re-typed from a guard-DESIGN heuristic → a dispute-ADJUDICATION heuristic** (with WS-FLOQUET as inaugural calibration instance).

### What Holds

- **The Floquet/Hill monodromy machinery** — frictionless Hill `u_k'' + Ω_k²(t)u_k = 0`, `det M = 1` (Liouville), reciprocal multipliers `e^{±μT}`, band/gap dichotomy `|Tr M| ⋛ 2`. Both inv-10 and inv-12 implement it correctly (`tongue_xcheck_maxdev = 8.8e-11`, `det_max_dev = 2.86e-11`). Never in dispute; held on both sides.
- **§VII.BP (H-PARITY-DRIVE-EXCLUSION), STAGE-3-PERMANENT, `proven_1141`, audit `08f32885`** — UNCHANGED. The workshop's adjudication is investigation-track (the inv-10/inv-12 Floquet-liveness dispute); it does NOT amend the permanent theorem, whose clause (d) already correctly reads COINCIDENCE-BOUNDED and "S67-consistent narrow-band post-fold-tail" (per the W4-2 amendment block). The workshop CONFIRMS the registered grade; it adds no permanent-registry change.
- **S67 "Post-transit parametric resonance: IMPOSSIBLE", PROVEN, gate `TRANSIT-67`, `session-67-synthesis.md`** — survives the post-S101 in-band drive intact. Its mechanism (transit-freeze hierarchy `dt_transit ≪ τ_relax ≪ t_BCS ≪ T_BCS_osc` ⇒ small `δτ_amp` ⇒ narrow `h_par`) IS the depth result.
- **Mode-Independent Occupation Theorem (S57/S62, PROVEN: `|β_k|²` mode-independent, tilt from geometry only)** — the relic occupations were already a frozen geometric output before Floquet was asked; the DEAD verdict confirms the modulus afterglow does not perturb that freeze.
- **The Ordered-Veil diabatic freeze (`S_ent = 0`, `R_therm = 5251.82`, S95-certified)** — UNCHANGED, and now tied to the no-DTC result as the same fact on the entropy axis.
- **inv-12 W3-2's `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`** — confirmed as the ground-truth datum; the analytic per-mode value `+1.98756` is consistent with and bounded by it.

### What Breaks or Strains

- **inv-10 W2-4's `q_cons = 0.504` depth assignment BREAKS** — it is an occupation-energy-per-pair ratio mis-mapped onto the Mathieu-depth slot. The monodromy/rigidity arithmetic at that depth is internally correct but UNREALIZED, because the substrate places no relic mode at `h_par ≈ 0.5`. inv-10 W2-4's `dtc_candidate = True` is vacuous on the realized 1248-mode spectrum.
- **inv-10 W2-4's `max Re μ_F = 0.249 > 0` LIVE reading BREAKS as a substrate statement** — it is a free-`a`-scan maximum (`a ∈ [0.5,1.6]`, max at a=0.908) read at the mis-assigned depth; the realized relic spectrum at the physical depth gives `Re_mu_relic = 0` exactly.
- **My OWN R1 route (2) BROKE (conceded R2-CC1):** I mis-attributed §VII.BP clause (β) as a parity-coupling-channel exclusion; the registered text says "no local-equilibrium state functions" — a state-functional vacuousness condition. The verdict (DEAD) survives the demotion because it never rested on a closed channel.
- **No STRAIN on the verdict, the WS-AS-1 hand-off, or any permanent theorem.** The only thing that broke is a depth mis-assignment (inv-10) and a route mis-attribution (my R1) — both internal to the investigation-track dispute, both resolved on the merits, neither propagating to a permanent claim.

### Carry-Forward Computations (MATH ONLY — propagate to S111)

All four are CONFIRMATORY or FORWARD-EXTENDING — NONE gates the DEAD verdict (already pinned by the aggregate `max|Tr M| < 2`, the QA-1/TD-R2-1 depth derivation, and the 84× threshold). 4-field specs.

**CF-S111-FLOQUET-1 — per-mode monodromy print at the single most-at-risk mode (Q-B closeout).**
1. **What**: extract the stored `Tr M(A=0.965, q_M=0.965·8.3e-4/2=4.005e-4)` scalar from the inv-12 W3-2 npz; confirm individually `∈ (−2, 2)` and matches the analytic `+1.98756 ± O(5e-6)` (QA-R3B-1). Converts the aggregate `max|Tr M| = 1.99999` bound into a per-mode certificate at the smallest detuning-to-half-width point.
2. **Inputs**: inv-12 W3-2 npz (`Re_mu_relic` / `tr_relic` arrays + relic `A_relic` grid; locate the A=0.965 mode); `h_par = 8.3e-4` (S101-W1-QEQ-RELIC-ODDFLOOR pin); analytic prediction `+1.98756`.
3. **Gate**: `|Tr M_stored − 1.98756| < 1e-3` AND `|Tr M_stored| < 2` (RATIO + ABSOLUTE). NON-verdict-gating (confirmatory).
4. **Effort**: 1-line `np.load` extraction + scalar compare; trivial.

**CF-S111-FLOQUET-2 — exact DTC counterfactual-depth threshold + `δτ_amp` map (QA-R2-2 closeout).**
1. **What**: pin Sage-exact `h_par_crit = 2·0.035/A = 0.0725` (at A=0.965, the period-2 half-width first catching the nearest-a=1 mode), the miss-factor `h_par_crit/8.3e-4 = 84.34×`, and the linear `δτ_amp` map `δτ_amp_crit = δτ_amp·84.34`. Registers the DTC-absence as a falsifiable structural prediction tied to the Ordered-Veil `S_ent = 0` freeze.
2. **Inputs**: nearest-a=1 relic detuning `|A−1| = 0.035` (inv-12 W3-2); `h_par = 8.3e-4`; period-2 half-width law `Δa_½ ≈ q_M = A·h_par/2`.
3. **Gate**: Sage-exact `h_par_crit` and miss-factor against the analytic forms (THEOREM tolerance, machine-ε). INFO-class (structural-prediction registration; no PASS/FAIL on substrate physics).
4. **Effort**: Sage `sage_eval` one-liner + registry-text prediction; trivial.

**CF-S111-FLOQUET-3 — first-principles `δτ_amp` derivation from the diabatic-freeze afterglow.**
1. **What**: DERIVE `δτ_amp` (the residual modulus ring-down amplitude after the Mach-13.75 transit-freeze) from the substrate dynamics, independently of the S101-W1 guard-floor pin; verify `h_par = (dω²/dτ)·δτ_amp/ω² → 8.3e-4`. Closes the one input the depth-crux concession takes on trust.
2. **Inputs**: post-fold modulus relaxation trajectory `τ(t)` near the τ_fold exit (the residual oscillation after diabatic freeze); spectral sensitivity `dω_n²/dτ` at the relic modes; `ω_{n,0}²` (relic band); Ordered-Veil freeze parameters (`S_ent = 0`, `R_therm = 5251.82`).
3. **Gate**: `|h_par_derived − 8.3e-4|/8.3e-4 < 0.1` (RATIO, 10% — the guard-floor pin's own band). NON-verdict-gating (the verdict already holds at the pinned `8.3e-4`); a PASS upgrades `h_par` from guard-floor-asserted to substrate-derived.
4. **Effort**: moderate — requires the post-fold modulus trajectory; a focused single-script compute.

**CF-S111-FLOQUET-4 — cutoff-robustness scaling theorem registration (QA-R2-1 formalization).**
1. **What**: register the structural theorem "no L_max ≥ 12 truncation extension reopens the §VII.BP relic resonance at `h_par = 8.3e-4`," because any new high-A mode lands near a zone `n` whose Mathieu half-width `Δa_½^{(n)} ∝ q_M^n` (n ≥ 3 ⇒ `≲ 10⁻⁷`) falls faster than the mode density concentrates at integer-² centers. The EXPONENT `n` is the theorem; the prefactor is convention-ambiguous (×16) and explicitly NOT registered.
2. **Inputs**: McLachlan tongue-half-width scaling `Δa_½^{(n)} ∝ (q_M/2)^n`; relic spectrum `A_relic ∈ [0.876, 12.6]` (inv-12); `q_M(A) = A·h_par/2`; the existing L12 master spectrum cache for the mode-density argument.
3. **Gate**: `[VERIFY-THEOREM]` on the scaling exponent (analytic, machine-ε against the McLachlan form); the prefactor is declared diagnostic-only. Stage-1-candidate → Stage-2 cross-axis verify per `joint-theorem-promotion.md` if promoted to permanent-results.
4. **Effort**: moderate — analytic theorem statement + Sage verification of the scaling + a mode-density-vs-half-width argument; registration is a separate Stage-1/Stage-2 pathway item.

### Effected In-Session (NON-MATH — completed by Agent B before terminating)

- [x] **Investigation-track adjudication-status annotation on the WS-FLOQUET → WS-AS-1 interlock** — recorded the resolved outcome (CONVERGED on DEAD-by-depth; physical `h_par = 8.3e-4`; full-monodromy gap-confined; parity channel OPEN; §VII.BP a state-functional corroborator; DTC relocated; WS-AS-1 Reading-A PRESERVED; the INV12-W3-4 fitted-greybody caveat) as a sub-bullet under interlock #1 in the investigation-track ledger — `sessions/investigation/_promotion-triage.md:27`. (Authorized plain edit: investigation-track ledger, not the permanent registry / falsifier surface / capstone.)
- [x] **Knowledge-MCP certification of the two load-bearing registry claims** — pulled `proven_1141` (§VII.BP clause-(β) verbatim text: "possesses no local-equilibrium state functions"; STAGE-3, audit `08f32885`, source `session-102-phonon-first-closeout-landscape-synthesis.md`) and confirmed S67 "Post-transit parametric resonance: IMPOSSIBLE" PROVEN (gate `TRANSIT-67`, `session-67-synthesis.md`) + the `OQ-1`/`w4_qeq_relic_oddfloor` S101 oddfloor provenance. Recorded inline in CC-R3B-1 so the verdict tabulation rests on canonical text, not either agent's paraphrase. (Read-only certification; no write.)
- [x] **Permanent-results-registry §VII.BP — DELIBERATE NO-OP, with reason.** Verified at `sessions/permanent-results-registry.md:152` (table row) + `:21263` (section body) that the §VII.BP STAGE-3-PERMANENT entry already records clause (d) as COINCIDENCE-BOUNDED with the "S67-consistent narrow-band post-fold-tail" amendment (W4-2 audit `98a923fd`). The workshop's Floquet-liveness adjudication is INVESTIGATION-TRACK and does NOT amend the permanent theorem (per `gate-verdicts.md §"Investigation-Track"`: an investigation result enters the permanent record only when promoted into a session and re-computed under a `session-{N}` gate; per the 4-stage `joint-theorem-promotion.md` pathway a STAGE-3 entry is not re-prosed on an investigation-track workshop). No edit made — recording the no-op explicitly so a future reader does not mistake the absence of a registry change for an oversight. (Capstone-hygiene Q3: NO status change to any capstone-governing register; the §VII.BP grade is UNCHANGED and the prose tag already equals the register tag.)
- [→] ROUTED-housekeeping: **methodology rule-extension — "pin the Mathieu-depth-slot's dimensional identity FIRST; the monodromy is confirmation, never the discriminator"** (QA-E-R3B-2 / E-R3-2). Both agents co-propose promoting the S100a-W1-D-2 width-aware-guard rule from a guard-DESIGN heuristic to a dispute-ADJUDICATION heuristic, with WS-FLOQUET as the inaugural calibration instance. This is a Q2 methodology-rule-file diff both agents AGREE on (not adversarial; not a math compute) → routes to `session-110-housekeeping.md §D` (methodology-rule extension) for the W1 wave-synthesis, with the rule-file target `.claude/rules/math-scripts.md` §"Double-Check Logic Before Compute" (or a new calibration-corpus entry under the existing precedent). NOT effected here (rule-file edits land via the designated writer at wave-synthesis per `feedback_framework-hygiene.md`, and the calibration-corpus / K-counter narrative belongs in `sessions/framework/registry/*-corpus.md`, not the rule file, per `feedback_rules-directive-only-no-session-info.md`).

### Closing Line

The §VII.BP in-band Floquet resonance is **DEAD — by depth, not by channel closure.** The two readings never disagreed about the monodromy (both exact to `~1e-11`) or the relic spectrum (both `A_relic ∈ [0.876, 12.6]`); the entire LIVE/DEAD split was a single dimensional-bookkeeping question — is `2E_pump/ω_q` the fractional ω²-modulation (a drive property) or the occupation energy per pair (a populated-state property)? — answerable before any Floquet step. The depth is `h_par = 8.3e-4`, the parity channel is OPEN, every one of the 1248 relic modes sits in a stability gap, and the relic's self-protection is one substrate fact viewed three ways: *the impulsive Mach-13.75 transit that made the 59.8 Parker pairs is exactly what leaves too little residual modulus drive to un-make them.* The relic `{β_k}` survive their own modulus afterglow intact — and that is the input WS-AS-1 Reading-A needs.
