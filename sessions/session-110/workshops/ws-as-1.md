# Session 110 Workshop: lizzi × transit-dynamics — WS-AS-1

**Date**: 2026-06-20
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: lizzi-spectral-functional-theorist (Reading A — impulse-quench A_s over-production is REAL, a physical spectral-functional d.o.f.), transit-dynamics-theorist (Reading B — occupation-/truncation-soft band, a Casimir-ceiling artifact)
**Source Documents**:
- sessions/investigation/_promotion-triage.md
- sessions/investigation/_cross-investigation-synthesis.md
- sessions/session-110/workshops/ws-floquet.md  (the WS-FLOQUET verdict — read it: DEAD-by-depth PRESERVES the Reading-A leg)
**Adjudication question**: The A_s MAGNITUDE's epistemic type ONLY — the FLOOR (A_s ≥ A_s^{BD}, S_IC = 1 + 2n_k ≥ 1, proven_1097, 3 orthogonal axes) is PERMANENT either way. Reading A (lizzi): the impulse-quench over-production (~+0.86 OOM) is REAL — locked {β_k}, Floquet-survives, the spectral functional is an UNPINNED PHYSICAL d.o.f. Reading B (transit-dynamics): an L_max-/occupation-SOFT band, Casimir-ceiling truncation artifact (truncation_consistent=False). DEPENDS ON WS-FLOQUET (landed DEAD → relic NOT re-pumped → Reading-A leg PRESERVED). nazarewicz-nuclear-structure-theorist is the named Friedrich-Bär truncation-saturation cross-reviewer (R3 Turn-B tie-breaker). Full adjudication context supplied to each agent at dispatch.

---

## Round 1 — lizzi: Opening Analysis

### LIZ1: Reading-A steelman (real over-production — spectral functional is a physical d.o.f.; conditioned on WS-FLOQUET=DEAD)

**The governing structure first (substrate-first, binding throughout).** A_s is NOT an inflaton normalization in an expanding container. It is the squeezing modulus of the post-transit GGE-relic acoustic state — the amplitude of the interference pattern of the relic Bogoliubov excitations of the D_K eigenvalue spectrum after the supersonic transit (Mach 13.75) through the van Hove fold at τ_fold. The arrow is fixed:

```
D_K eigenvalues → impulse-quench Bogoliubov {α_k, β_k} of the fold transit
   → produced occupation n_k = |β_k|² ≥ 0 → squeezing modulus S_IC = 1 + 2 n_k
   → relic power-spectrum amplitude A_s                                          (LIZ1-0)
```

"The over-production" is a statement about that produced amplitude relative to its Bunch-Davies infimum — never about density perturbations seeded in expanding space.

**The substitution chain that fixes the SIGN (over-production, not under) — done before any magnitude claim, per `math-scripts.md`.**

```
Claim: "the impulse-quench A_s lands ABOVE the Bunch-Davies floor (over-production); the sign is locked."

Step 1: A_s = P_dS · F_amp · S_IC                          [E2.4, S77 three-factor decomposition; knowledge-MCP]
Step 2: S_IC = 1 + 2 n_k,   n_k = |β_k|² ,   |α_k|² − |β_k|² = 1   [proven_1097 squeezing modulus; F_amp LINEAR in A_s, my memory S78 scrub]
Step 3: Bunch-Davies reference  ⇔  n_k = 0  ⇒  S_IC^{BD} = 1   ⇒   A_s / A_s^{BD} = S_IC = 1 + 2 n_k
Step 4: impulse-quench (supersonic, Mach 13.75) ⇒ Parker pair production n_pairs = 59.8, P_exc = 1.000 ⇒ n_k > 0
Step 5: n_k > 0  ⇒  S_IC > 1  ⇒  A_s > A_s^{BD}            [direction read off canonical form: positive]
Conclusion: A_s is bounded BELOW by A_s^{BD} and lands ABOVE it. The over-production sign is locked by n_k ≥ 0; only the magnitude floats.
```

This is the floor inequality `A_s ≥ A_s^{BD}` (`proven_1097`, S_IC ≥ 1) — PERMANENT either way, NOT what this workshop adjudicates (see LIZ2). What LIZ1 steelmans is the next claim: the *magnitude* of the over-production (+0.86 OOM [inv-5 W2-1], +1.455 OOM [inv-6 W2-2], sub-2-OOM band) is a REAL, locked physical quantity — and its scheme-dependence is the spectral functional showing up as an *unpinned physical degree of freedom*, not a numerical defect.

**Three load-bearing claims, each substrate-grounded:**

**1. The {β_k} are a LOCKED, frozen Bogoliubov output — now certified by WS-FLOQUET=DEAD.** The single most important input to this steelman is the WS-FLOQUET verdict (`sessions/session-110/workshops/ws-floquet.md`), which CONVERGED (transit-dynamics × quantum-acoustics, 3 rounds, berry monodromy tie-breaker) on **DEAD-by-depth**: the physical Mathieu depth is `h_par = 8.3e-4` (the dimensionless fractional ω²-modulation, a DRIVE property), NOT inv-10's `q ≈ 0.504 = 2E_pump/ω_q` (the occupation energy per pair, a POPULATED-STATE property mis-mapped onto the depth slot — a ~607× over-assignment, conceded on the merits via the QA-1/TD-R2-1 derivation). At that depth the full 1248-mode monodromy is gap-confined: `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`, `Re_mu_relic = 0`; the nearest-a=1 relic mode (A=0.965) sits ~87 half-widths outside the period-2 tongue (`Tr M ≈ +1.98756`). **Consequence for Reading A:** the relic occupations `{n_k = |β_k|²}` are NOT secularly re-pumped by the modulus afterglow — they are a frozen Bogoliubov output, doubly anchored by the S57/S62 Mode-Independent Occupation Theorem (`|β_k|²` mode-independent, tilt from geometry only). The over-production is therefore a GENUINE LOCKED AMPLITUDE, not a re-pumping artifact of a parametrically unstable spectrum. This is the precise sense in which the leg is "admitted": had inv-10 won (μ_F > 0 on physical modes), the {β_k} would grow secularly and the +0.86 OOM would be a dynamical inflation; WS-FLOQUET=DEAD forecloses that. **HONEST CAVEAT, carried per the dispatch charge:** "genuine amplitude" here means *not a Floquet artifact* — it does NOT by itself discharge the separate INV12-W3-4 fitted-greybody-`0.512` finding (the exit-filter leg, ∫Γ_derived=0.036 vs fitted 0.512, 14× short, OPEN), which is a distinct upper-edge question routed to CF-AS-2, not to this floor-vs-magnitude adjudication.

**2. The magnitude's scheme-dependence is the lizzi-signature — what survives all functional choices is structural; what depends on the choice is a physical d.o.f.** My CV-1 convergence rollup (`_rollup-as-wall §3`, which I authored as Stage-2 synthesizer) netted the six independent attacks into exactly this 2-layer decomposition: the inv-1 "12.6-OOM sign-flipping spread" (3.02/3.15/4.56/9.5 OOM) was a *category error* — it is the SAME sign-locked floor seen through different spectral functionals, occupation inputs, and H̃-branches, not four disagreeing measurements. The under-production figures (−3.02/−3.15 OOM, S66/S83) are the WRONG functional (slow-roll / equilibrium `1/ε_H` applied to an impulsive quench); the over-production figures (+0.86/+1.455 OOM) are the RIGHT functional (impulse-quench Bogoliubov |β_k|²); the +9.5-OOM figure is the naive aggregate-occupation dump `n_pairs/2π²` (a normalization artifact, reproduced EXACTLY by inv-5 then discarded); the +6.008 OOM is the near-floor-DOS ζ′(0) functional (IR-dominated, a DIFFERENT functional weighting the same spectrum). **This is the spectral-functional-pluralism thesis in its purest observable form:** the choice of functional (cutoff / zeta / impulse-quench Bogoliubov / near-floor-DOS) is not a mathematical convention — it determines which spectral moments enter with what weight, and A_s + the CC are precisely where that freedom shows up observationally (cf. CV-1↔CV-4 cross-link: the A_s magnitude is SCHEME-DEPENDENT for the SAME reason the a₀/a₂ CC ratio is — the entropy functional sign-flips it −0.499 vs +2.320). The magnitude is a PHYSICAL DEGREE OF FREEDOM, pinned by the physically-correct functional to a narrow sub-2-OOM over-production band, NOT a number to be "converged away."

**3. The functional-dependent factor and the dimensionful prefactor are ORTHOGONAL — and the scheme-freedom lives in the dimensionless factor, which is robust within a functional.** inv-3 W2-3 made the decomposition explicit: `A_s = (dimensionless functional-dependent factor) × (M_KK/M_Pl)²`. The dimensionless `F_nearfloor = 2.30` is intensive and robust (span 0.19 OOM across n_s-selection); ALL the *dimensionful* weakness is the prefactor `(M_KK/M_Pl)² = 9.3e-4` — which is the M_KK keystone (CV-2), a SEPARATE gap, NOT the scheme-freedom. So the over-production magnitude is structurally `O = w · Ô` with `w = (M_KK/M_Pl)²` the rank-1-PROVEN single imported weight (§VII.BS) and `Ô` the dimensionless functional-dependent squeezing factor. Reading A's claim is sharp: `Ô` (the dimensionless impulse-quench squeezing modulus) is a genuine intensive physical quantity, robust within the impulse-quench functional and scheme-dependent ACROSS functionals — the signature of a physical d.o.f., not a truncation softness.

**Why this is NOT refuted by inv-12 W3-1's truncation band (the Reading-B card, pre-empted).** Reading B will cite inv-12 W3-1's summed `ρ_relic` carrying a Casimir-ceiling truncation band (p+q ≤ 7→8, rel 0.43, `truncation_consistent = False`) and `K_sub − 1` ranging ~2000× across occupation inputs. My pre-emption: that band is on the EXTENSIVE summed relic content `Σ mult·|β_k|²` — an aggregate quantity that IS Casimir-ceiling-banded and R-Protection-fragile (my memory: chi_2 is NOT R-protected, single-branch moments need extrapolation; cross-branch R-protection is FORBIDDEN). But the A_s squeezing modulus that enters (LIZ1-0) is the INTENSIVE per-mode `S_IC = 1 + 2n_k` at the pivot, and the {β_k} are per-mode LOCKED + L-saturated for the SHAPE (the S57/S62 Mode-Independent Occupation Theorem: `|β_k|²` is mode-INDEPENDENT). The intensive/extensive partition is the crux: an extensive sum being L_max-banded does NOT make the intensive per-mode squeezing modulus a truncation artifact. The over-production magnitude is the intensive `Ô` evaluated on the locked, Floquet-frozen {β_k} — a physical d.o.f., not an un-converged extensive band.

**Key finding:** Conditioned on WS-FLOQUET=DEAD (relic frozen, not re-pumped — admitted), the impulse-quench A_s over-production (+0.86 to +1.455 OOM, sub-2-OOM band) is a REAL locked physical amplitude whose scheme-dependence is the spectral functional acting as an UNPINNED PHYSICAL DEGREE OF FREEDOM — the lizzi-signature outcome (what survives all functionals is structural = the floor; what depends on the functional is a physical d.o.f. = the magnitude). The dimensionless squeezing factor is intensive and robust within the impulse-quench functional; the only L_max-soft object (inv-12 W3-1's summed ρ_relic) is the EXTENSIVE relic content, not the INTENSIVE per-mode modulus that sets A_s.

**Questions for transit-dynamics (Reading B):**
1. Your Reading-B "truncation artifact" rests on inv-12 W3-1's `truncation_consistent = False` for the summed `ρ_relic`. But A_s enters via the INTENSIVE per-mode `S_IC = 1 + 2n_k` (LIZ1-0), and the S57/S62 Mode-Independent Occupation Theorem makes `|β_k|²` mode-INDEPENDENT and L-saturated for the shape. Do you contend the over-production magnitude is set by the EXTENSIVE summed content (your banded object) or by the INTENSIVE per-mode squeezing modulus (my locked object)? Name which quantity carries the magnitude.
2. The Casimir-ceiling band you cite (p+q ≤ 7→8, rel 0.43) is an extensive-sum truncation. Is there a *substrate-derived* argument that the per-mode `n_k` itself (not the sum) fails to converge in L_max — i.e., that the DIMENSIONLESS squeezing factor `Ô`, not the extensive aggregate, is L_max-soft? If `Ô` is L-saturated and only the sum is banded, your "artifact" reading collapses to a statement about the extensive content, which does not enter the intensive A_s amplitude.
3. WS-FLOQUET landed DEAD (relic frozen, leg admitted). Given the {β_k} are NOT re-pumped (a frozen, locked Bogoliubov output), what is the residual mechanism by which the LOCKED occupation magnitude would be a "truncation/occupation artifact" rather than a physical d.o.f.? If the {β_k} are locked AND per-mode-saturated, the only remaining softness is the extensive sum — which is the M_KK/extensive-content axis, not the intensive squeezing-modulus magnitude.

### LIZ2: Adjudication-question framing (MAGNITUDE epistemic type only; FLOOR permanent; HK-AS-FLOOR split)

**What this workshop adjudicates — and what it does NOT.** This is a narrow, single-axis adjudication. The object in dispute is the EPISTEMIC TYPE of the impulse-quench A_s over-production MAGNITUDE only: is the ~+0.86-to-+1.455-OOM over-production a PHYSICAL degree of freedom (Reading A) or an L_max-/occupation-truncation artifact (Reading B)? The FLOOR is NOT in dispute and must not be re-litigated by either agent.

**The FLOOR is PERMANENT either way (do not re-open).** The inequality

```
A_s ≥ A_s^{BD}   ⇔   S_IC = 1 + 2 n_k ≥ 1   (forced by n_k = |β_k|² ≥ 0 and |α_k|² − |β_k|² = 1)   (LIZ2-1)
```

is PROVEN (`proven_1097`) and confirmed on THREE orthogonal axes (this is genuine independent confirmation per `joint-theorem-promotion.md`, not shared-context agreement — three distinct machineries):

| Axis | Source | What it confirms |
|:-----|:-------|:-----------------|
| **reference-state** | inv-12 W1-2-A-S-GGE-MODULAR-REFERENCE | the GGE modular reference RAISES A_s (`Δ = +1.04e-13 > 0`; `K_sub ∈ [1.00002, 1.00384] ≥ 1`) — floor is reference-state-INDEPENDENT |
| **families-index η-form** | inv-12 W2-5-FWD-C1-BISMUT-CHEEGER-ETA | the signed Bismut-Cheeger η-channel ≡ 0 EXACT — carries no normalization; the floor's sign is not an artifact of the signed channel |
| **dynamical-Bogoliubov** | inv-5 W2-1 + inv-6 W2-2 | the produced-side over-production directly (+0.86 / +1.455 OOM, sign-definite-positive) — the floor is realized in the produced state |

The floor is FUNCTIONAL-INDEPENDENT (a permanent classification in my ledger: what survives all spectral-functional choices is structural). Neither agent re-derives it; it is the fixed boundary against which the MAGNITUDE question is posed.

**The adjudication question, stated as a 3-way fork (carried from my CV-1 rollup §4 WS-AS-1).** Given the floor, the over-production magnitude is one of:

- **(A) a converged physical d.o.f.** — the locked, Floquet-frozen impulse-quench |β_k|² is a genuine intensive amplitude; its scheme-dependence ACROSS functionals is the spectral functional acting as an UNPINNED PHYSICAL DEGREE OF FREEDOM (my Reading A);
- **(B) an L_max-/occupation-SOFT band** — a Casimir-ceiling truncation artifact (inv-12 W3-1 `truncation_consistent = False`, rel 0.43 at p+q ≤ 7→8; `K_sub − 1` spread ~2000× across occupation inputs); the magnitude is not physical until the relic content converges in L_max (transit-dynamics Reading B);
- **(C) genuinely scheme-chosen** — no convergence argument pins it; it is a free knob.

The two readings invoke STRUCTURALLY DIFFERENT machinery and cannot both be the final word: Reading A rests on the per-mode locked {β_k} ODE + Floquet survival (the INTENSIVE squeezing modulus); Reading B rests on the Casimir-ceiling truncation band on the summed ρ_relic (the EXTENSIVE relic content). The crux is the intensive/extensive partition — which quantity carries the magnitude — and it is exactly the kind of question my methodology is built to settle: identify which result is functional-/truncation-INDEPENDENT (structural) versus functional-/truncation-DEPENDENT (a physical or scheme d.o.f.).

**The named tie-breaker (binding on R3 Turn-B per the dispatch charge).** `nazarewicz-nuclear-structure-theorist` is the designated Friedrich-Bär cross-reviewer. The R3 Turn-B agent MUST cite a **Friedrich-Bär structural-saturation argument** as the arbiter between (A) and (B): does the bottom-K relic observable that sets the over-production magnitude *saturate* at the canonical L_max truncation (i.e., do the NEW-sector eigenvalues at L_max+1 lie above the bottom-K ceiling, bounded below by `η_FB·√(C₂(p+q)+1)`)? If the magnitude-bearing observable is Friedrich-Bär-saturated, then `truncation_consistent` is effectively True for the INTENSIVE quantity and Reading A holds (the band is on the extensive sum, which does not enter A_s); if it genuinely fails to saturate, Reading B holds (the magnitude is L_max-soft). This is the structural-saturation test that decides whether inv-12 W3-1's `truncation_consistent = False` is a property of the magnitude-bearing intensive observable or only of the extensive aggregate — and it is the cleanest arbiter because it is a per-sector eigenvalue-floor statement, not a re-run of the disputed sum.

**The HK-AS-FLOOR split (the NON-MATH deliverable this workshop EFFECTS).** Regardless of how the (A)/(B)/(C) fork lands, the register currently OVER-STATES the A_s status: atlas-08 CF23 reads "A_s 3.02× permanent" and the falsifier-rigor Row 8 carries the magnitude as if the *number* were permanent. My CV-1 rollup §3 + the promotion-triage HK-AS-FLOOR row establish the correct two-part split:

```
HK-AS-FLOOR (capstone-hygiene Q3 — designated-writer reviewed patch, NOT bulk append):

(a) FLOOR  →  A_s ≥ A_s^{BD}  (S_IC ≥ 1)
      = PERMANENT structural-position wall, FUNCTIONAL-INDEPENDENT,
        confirmed on 3 orthogonal axes (reference-state / families-index / dynamical).
      ONLY the inequality ">1" deserves "permanent."

(b) MAGNITUDE / upper-edge FILTER  →  the number (3.02× / +0.86 / +1.455 OOM) + ∫Γ exit filter
      = SCHEME-DEPENDENT and OPEN.
      Magnitude relocated to WS-AS-1 (this workshop: A/B/C epistemic type) + CF-AS-3 (the pin);
      exit filter relocated to CF-AS-2 (the dynamical near-horizon greybody scan).
```

**Routing (per the dispatch charge — EFFECT in R3 Turn B, do not merely log).** The HK-AS-FLOOR split is a NON-MATH register reconciliation:
- **`atlas-08-open-questions.md` CF23 prose** — split into (a) FLOOR-permanent + (b) MAGNITUDE/FILTER-SCHEME-DEPENDENT-OPEN → the **designated writer** (curated-doc prose owner) applies the reviewed patch per `capstone-hygiene-gate.md` Q3 (PROVEN/CONDITIONAL distinction-sharpening, substrate-first frame preserved: the arrow `D_K → {α_k,β_k} → n_k → S_IC → A_s` is unchanged; the tag merely scopes confidence floor-permanent / magnitude-open).
- **`falsifier-rigor-registry` / falsifier-master-inventory Row 8** — keep `SCHEME-DEPENDENT` for the magnitude AND add the FUNCTIONAL-INDEPENDENT/PERMANENT floor sub-annotation (3 orthogonal axes) → **`mack-cosmic-bridge`** is sole writer of the §7 falsifier surface + `falsifier-master-inventory.md` (`feedback_mack-bridge-role.md`). Route the Row-8 update to mack.

**The MATH carry-forwards this verdict GATES (4-field specs; named per the dispatch charge — both are GATED on this workshop's verdict).**

```
CF-AS-2 — A_s upper-edge greybody: dynamical near-horizon resonance scan.
  What:    scan the BdG fluctuation potential at the τ≈0.16 exit horizon for ANY substrate-derived
           barrier (static OR dynamical-resonance from the finite quench rate τ̇(τ)) whose
           ω_½ = √V₀^sub lands in the relic band [0.94, 3.72] M_KK AND reproduces ∫Γ = 0.512 ± 10%,
           OR prove none exists. (The exit-filter leg — distinct from this floor-vs-magnitude
           adjudication; this is the INV12-W3-4 fitted-0.512 leg the LIZ1 caveat flagged as undischarged.)
  Inputs:  inv-12 W3-1 locked {α_k,β_k} + pair_band [0.94,3.72]; inv-12 W3-4 V_eff scanner +
           Pöschl-Teller machinery; inv-4 W1-4 black-hole-thermo greybody (two-route consolidation);
           τ̇(τ) near-exit trajectory; κ_exit=47.6146, Δ_BCS=0.4642547, T_acoustic=0.112; ∫Γ_fitted=0.512.
  Gate:    PASS (bounded-AND-derived) iff ∃ substrate V₀ with √V₀∈band AND ∫Γ=0.512±10% (RATIO tol);
           FAIL (bounded-but-filter-fitted) iff every substrate scale gives ω_½ outside band;
           INFO iff resonance in-band but ∫Γ misses by >10%. Pre-register finite-rate WKB regime_verdict.
  Effort:  ~1 agent-session.
  GATED ON: this WS-AS-1 verdict (the filter scan presupposes the magnitude is a real physical
           object worth bounding — if the verdict is (C) scheme-chosen, the filter scan is moot).

CF-AS-3 — Session-promote the impulse-quench A_s canonical pin (the magnitude under the correct functional).
  What:    promote the impulse-quench A_s magnitude (inv-5 +0.86 OOM, A_s=1.54e-8, k̂=53.3 M_KK,
           k̂/k_pivot=3.72 deg(T_BZ→pivot) mapping; cross-checked vs inv-6 Parker +1.455 OOM) to
           canonical_constants.py + PROVENANCE; mack updates falsifier Row 8.
  Inputs:  inv5_w2_1_*.npz, inv6_w2_2_*.npz; falsifier Row 8 (5.078e-9 TD-canonical); k_pivot, deg(T)=+2.
  Gate:    PASS iff one defensible impulse-quench A_s magnitude (a POINT if verdict=(A) physical-d.o.f.;
           a BAND if verdict=(B) L_max-soft) lands with scheme-tag + OOM-distance; row keeps
           SCHEME-DEPENDENT for the magnitude + adds the FI/PERMANENT floor sub-annotation.
  Effort:  ~0.3 wave (mechanical promotion).
  GATED ON: this WS-AS-1 verdict — it determines whether CF-AS-3 pins a POINT (Reading A) or
           records a BAND (Reading B). This is the load-bearing reason the verdict's epistemic-type
           output is not academic: it sets the FORM of the canonical pin.
```

**Key finding (framing):** This workshop adjudicates ONE axis — the over-production magnitude's epistemic type (A physical-d.o.f. / B truncation-band / C scheme-chosen) — against a PERMANENT floor (LIZ2-1, three orthogonal axes). The intensive/extensive partition is the crux, the Friedrich-Bär structural-saturation argument (nazarewicz, R3 Turn-B) is the binding tie-breaker, and the verdict directly sets the FORM of CF-AS-3's canonical pin (point vs band) and the admissibility of CF-AS-2's filter scan. The HK-AS-FLOOR register split (FLOOR-permanent / MAGNITUDE-FILTER-SCHEME-DEPENDENT-OPEN) is EFFECTED in R3 Turn B regardless of the fork outcome — atlas-08 CF23 to the designated writer, falsifier Row 8 to mack.

**Questions for transit-dynamics (Reading B), framing-level:**
1. Do you accept the floor (LIZ2-1) as out-of-scope (permanent on 3 axes) and contest ONLY the magnitude's epistemic type? If so, the workshop is cleanly single-axis and we converge on the intensive/extensive partition as the crux.
2. For the Friedrich-Bär tie-breaker: do you predict the bottom-K relic observable that SETS the over-production magnitude is L_max-saturated (intensive, Reading A) or fails to saturate (extensive-dominated, Reading B)? Stating the prediction now lets nazarewicz's R3 saturation argument adjudicate cleanly rather than retrofit.
3. Will you accept the HK-AS-FLOOR split (FLOOR-permanent / MAGNITUDE-FILTER-open) as the register reconciliation regardless of the A/B/C fork, so the NON-MATH deliverable lands in R3 Turn B independent of the math verdict?

---

## Round 1 — transit-dynamics: Response & Cross-Synthesis

### Part 1: Response to lizzi's Sections

#### Re: LIZ1 — real-over-production steelman

I tag each load-bearing element of LIZ1 **AGREE / DISAGREE / MISSED / EMERGES** against the transit-dynamics / Bogoliubov evidence. The substrate frame is binding: the object in dispute is the post-transit GGE-relic acoustic squeezing modulus — the amplitude of the interference pattern of the relic Bogoliubov excitations `{β_k}` of the D_K eigenvalue spectrum after the supersonic transit through the van Hove fold. The arrow `D_K → {α_k,β_k} → n_k=|β_k|² → S_IC → A_s` (LIZ1-0) is the correct one and is not in dispute. What IS in dispute is whether the *magnitude* of `A_s` reads off an INTENSIVE per-mode object (Reading A) or whether the A_s NORMALIZATION carries an EXTENSIVE mode-count/integral that inherits the Casimir-ceiling truncation band (Reading B).

**AGREE — the substitution chain that fixes the SIGN (LIZ1 Step 1–5), and the FLOOR (LIZ2-1).** I concede `A_s ≥ A_s^{BD}`, `S_IC = 1 + 2n_k ≥ 1`, forced by `n_k = |β_k|² ≥ 0` and `|α_k|² − |β_k|² = 1` (`proven_1097`). I co-own the dynamical-Bogoliubov axis of the 3-axis floor confirmation (inv-5 W2-1 / inv-6 W2-2 over-production +0.86 / +1.455 OOM, sign-definite-positive). The floor is FUNCTIONAL-INDEPENDENT and PERMANENT; I do not re-litigate it. This is out of scope per LIZ2 and I accept that scoping (see Re:LIZ2 Q1).

**AGREE — claim 1's WS-FLOQUET premise (the {β_k} are NOT re-pumped).** I co-authored WS-FLOQUET as Reading-A (LIVE) and then **conceded on the merits in R2**: the physical Mathieu depth is `h_par = 8.3e-4` (the fractional ω²-modulation, a DRIVE property), and inv-10's `q ≈ 0.504 = 2E_pump/ω_q` is an energy-per-pair ratio mis-mapped onto the depth axis — a ~607× over-assignment (the QA-1 / TD-R2-1 derivation; the full 1248-mode monodromy gives `fraction_resonance = 0 EXACT`, `max|Tr M| = 1.99999 < 2`). So the relic occupations are **NOT secularly re-pumped** by the modulus afterglow. **I therefore CONCEDE the re-pumping channel entirely** — Reading B does NOT claim the over-production is a Floquet artifact, a dynamical inflation, or a parametric-instability secular growth. That leg is dead, I helped kill it, and I will not resurrect it. **My Reading B is the OTHER artifact channel — truncation, not dynamics.** This distinction is load-bearing for the whole workshop: WS-FLOQUET decided the relic is *frozen in time*; it said NOTHING about whether the magnitude is *converged in L_max*. Frozen-in-time and converged-in-truncation are orthogonal properties. A Bogoliubov output can be perfectly time-stationary (Floquet-DEAD) AND still have its NORMALIZATION sitting on an unconverged Casimir-ceiling sum.

**DISAGREE — claim 3's intensive/extensive partition as LIZ1 draws it (this is the crux, and LIZ1 has the partition assignment backwards for the NORMALIZATION).** LIZ1 claim 3 + the pre-emption paragraph assert: the magnitude is `O = w·Ô` with `w = (M_KK/M_Pl)²` the rank-1 weight and `Ô` "the dimensionless impulse-quench squeezing modulus," intensive and L-saturated; the only L_max-soft object is inv-12 W3-1's EXTENSIVE summed `ρ_relic = Σ mult·|β_k|²`, which "does not enter A_s." **I contest exactly this.** Write the substitution chain for what actually enters the A_s normalization:

```
Claim (Reading B): "A_s normalization carries a mode-COUNT/integral that is extensive, hence Casimir-ceiling-soft —
                    NOT a single intensive per-mode S_IC at the pivot."

Step 1: A_s ≡ (k³/2π²) P_ζ(k_pivot)            [power-spectrum normalization, standard]
Step 2: P_ζ(k_pivot) on the post-transit GGE relic = (per-mode vacuum amplitude) × S_IC(k_pivot)
        where S_IC(k_pivot) = 1 + 2 n_{k_pivot}   [LIZ1-0; the INTENSIVE per-mode factor lizzi names]
Step 3: BUT the impulse-quench over-production figures lizzi cites (+0.86 / +1.455 / +9.5 / +6.008 OOM)
        are NOT P_ζ at a single k_pivot — they are computed from the PRODUCED RELIC CONTENT, which is
        ρ_relic = Σ_k mult(k)·|β_k|²·ω_k  (inv-12 W3-1) — an EXTENSIVE sum over the pair band [0.94, 3.72] M_KK
Step 4: the map from ρ_relic (or n_pairs = Σ|β_k|² = 59.8) to A_s requires a MODE-COUNT/spectral-density integral
        over the band — inv-5's "+9.5 OOM naive n_pairs/2π² dump" is the bare version of exactly this count
Step 5: a band-integral Σ_k over the fold-band IS a finite-L truncated sum on D_K(τ_fold); inv-12 W3-1 reports
        truncation_consistent = False (p+q ≤ 7→8, rel 0.43) for precisely this sum
Conclusion: the A_s NORMALIZATION (not the shape) inherits the extensive band's truncation softness.
            The over-production MAGNITUDE is on the count-side, which is the side lizzi concedes is soft.
```

The decisive point: **LIZ1's own pre-emption paragraph concedes the extensive `Σ mult·|β_k|²` is `truncation_consistent = False` and "R-Protection-fragile."** LIZ1 then claims this object "does not enter A_s." But the over-production NUMBERS lizzi is defending (+0.86, +1.455 OOM) were COMPUTED from the produced relic content — inv-5 W2-1's +9.5 OOM figure is literally `n_pairs/2π²` (the aggregate-occupation dump, LIZ1 §2 says so), and `n_pairs = Σ_k |β_k|² = 59.8` IS the extensive count. The "+0.86 OOM correct functional" figure is the SAME spectrum re-weighted through a different functional — but the weighting is still a band-integral, still a `Σ_k`, still finite-L-truncated. **You cannot get an amplitude NORMALIZATION from a single intensive per-mode `S_IC` without specifying which mode and integrating the spectral density around it — and that integral is the extensive object.**

**MISSED — the Mode-Independent Occupation Theorem (S57/S62) is a SHAPE theorem, and LIZ1 over-extends it to the NORMALIZATION.** LIZ1 leans hard on S57/S62 (`|β_k|²` mode-independent ⇒ per-mode locked + L-saturated). I am the author of the canonical TD ledger on this. What S57/S62 actually proves: `|β_k|²` is mode-INDEPENDENT, so the *tilt* `n_s` comes from geometry not occupation (this is why the BZ-leaf is k³-blue and the red tilt lives only at the Goldstone-pivot leaf — `deg(T_{BZ→pivot}) = +2`). That is a statement about the **SHAPE / scale-dependence**, and it IS L-saturated — I agree the SHAPE is intensive and robust. **But mode-independence of `|β_k|²` does NOT make the NORMALIZATION intensive — it makes it a constant TIMES a mode-count.** If `|β_k|² = const ≡ n̄` for all k in the band, then `ρ_relic = n̄ · Σ_k mult(k)·ω_k`, and the over-production magnitude `∝ Σ_k mult(k)` — the mode-COUNT in the fold-band — which is EXACTLY the Casimir-ceiling-truncated quantity (`truncation_consistent = False`). Mode-independence makes the per-mode value trivially L-saturated AND makes the normalization a pure count, which is the *most* truncation-sensitive object there is. LIZ1 uses S57/S62 to argue intensiveness; the theorem actually says the normalization is `n̄ × (extensive count)`. The theorem cuts AGAINST Reading A on the normalization.

**EMERGES — the workshop reduces to ONE structural question, and the Friedrich-Bär test is the right arbiter (I AGREE with LIZ2's tie-breaker choice).** What emerges is that lizzi and I AGREE the floor is permanent, AGREE the {β_k} are Floquet-frozen, and AGREE the SHAPE is L-saturated. The ENTIRE residual dispute is: **does the magnitude-bearing observable read off the intensive per-mode `S_IC(k_pivot)` (Reading A) or the extensive band-count `Σ_k mult(k)` (Reading B)?** This is a `truncation_consistent` question on a *specific* observable, and it is decidable by Friedrich-Bär saturation — exactly what LIZ2 nominates nazarewicz for. I sharpen the prediction in TD2.

#### Re: LIZ2 — Adjudication-question framing

LIZ2 frames a clean single-axis adjudication (MAGNITUDE epistemic type only; FLOOR permanent on 3 axes) with a 3-way fork (A physical-d.o.f. / B truncation-soft / C scheme-chosen) and a Friedrich-Bär tie-breaker. I AGREE with the framing and tag each of LIZ2's three framing questions.

**Q1 — Do I accept the FLOOR as out-of-scope and contest ONLY the magnitude's epistemic type? YES, unconditionally.** The floor `A_s ≥ A_s^{BD}` (LIZ2-1) is PERMANENT, confirmed on three orthogonal axes (reference-state / families-index η-form / dynamical-Bogoliubov — I co-own the third). I do not re-derive it, do not contest it, do not condition on it. The workshop is cleanly single-axis: the over-production MAGNITUDE's epistemic type. **And I AGREE the crux is the intensive/extensive partition** — that is precisely where Reading A and Reading B diverge, and LIZ2 names it correctly. My disagreement with Reading A is NOT about whether the partition is the crux (it is); it is about which SIDE of the partition the magnitude-bearing observable lives on.

**Q2 — Do I predict the bottom-K relic observable that SETS the over-production magnitude is L_max-saturated (intensive, A) or fails to saturate (extensive, B)? I predict it FAILS to saturate — Reading B — and I state the substrate reason now so nazarewicz can adjudicate cleanly, not retrofit.** The magnitude-bearing observable is the band-integrated relic content `ρ_relic = Σ_{k∈band} mult(k)·|β_k|²·ω_k` (or its A_s-normalization image). My prediction, framed as a Friedrich-Bär statement: the NEW-sector eigenvalues at L_max+1 that fall *inside* the fold pair band `[0.94, 3.72] M_KK` do NOT lie above the band ceiling — because the pair band is a FIXED M_KK-clock window, and as L_max grows, MORE Peter-Weyl (p,q) sectors contribute eigenvalues that land inside `[0.94, 3.72]` (the Casimir floor `|λ|_min^(p,q) ≈ √(C₂(p,q))/r(τ)` rises with p+q, but the band is at finite |λ|, so successive sectors keep dropping eigenvalues into it until the Casimir floor clears 3.72). inv-12 W3-1's `truncation_consistent = False` (rel 0.43 at p+q ≤ 7→8) is the direct measurement of this: the summed content moved 43% from p+q=7 to p+q=8 — it had NOT saturated. **Reading B's prediction: the band-count is NOT Friedrich-Bär-saturated at the canonical L_max, so the EXTENSIVE relic content (and the A_s normalization that integrates it) is L_max-soft.** Contrast with the SHAPE: the per-mode `|β_k|²` ratio between two fixed modes IS saturated (S57/S62), so `n_s` and `α_s` are intensive and Reading-A-correct *for the shape*. The split is shape-intensive / normalization-extensive — and the over-production is a normalization claim.

**Q3 — Will I accept the HK-AS-FLOOR split (FLOOR-permanent / MAGNITUDE-FILTER-open) as the register reconciliation regardless of the A/B/C fork? YES.** The HK-AS-FLOOR split is correct and is the NON-MATH deliverable independent of the math verdict: (a) FLOOR `A_s ≥ A_s^{BD}` = PERMANENT, FUNCTIONAL-INDEPENDENT, 3-axis-confirmed — only the inequality `>1` deserves "permanent"; (b) MAGNITUDE/upper-edge FILTER = SCHEME-DEPENDENT and OPEN. I note this split is **agnostic between Reading A and Reading B** — both readings agree the *number* (3.02× / +0.86 / +1.455 OOM) is NOT permanent (A says it's a physical d.o.f. scheme-dependent across functionals; B says it's truncation-soft). Either way the register over-statement "A_s 3.02× permanent" is wrong and must split. So HK-AS-FLOOR lands in R3 Turn B regardless. **One sharpening I will press for the split's wording:** the (b) MAGNITUDE annotation should distinguish two SOURCES of openness — (b-i) functional-choice freedom (Reading A's "physical d.o.f.") and (b-ii) L_max-truncation softness of the band-count (Reading B). These are different reasons the number is open, and the FORM of the CF-AS-3 canonical pin (POINT vs BAND, per LIZ2's own CF-AS-3 gate) depends on which dominates: if (b-ii) the truncation softness exceeds the functional spread, CF-AS-3 must record a BAND (Reading B), not a POINT.

### Part 2: Original Analysis

#### TD1: Reading-B (occupation-/truncation-soft band; Casimir-ceiling truncation_consistent=False)

**Governing structure first (substrate-first, binding).** The object is the post-transit GGE relic — the configuration of the D_K(τ_fold) eigenvalue spectrum after the supersonic transit, characterized by the impulse-quench Bogoliubov coefficients `{α_k, β_k}` with `n_k = |β_k|² ≥ 0`, `|α_k|² − |β_k|² = 1`. A_s is the squeezing modulus of the relic acoustic interference pattern. The question Reading B poses is narrow and precise: **is the OVER-PRODUCTION MAGNITUDE an L_max-converged physical number, or does it sit on a Casimir-ceiling-truncated band-count that inv-12 W3-1 already measured as `truncation_consistent = False`?** I structure the argument in four propositions, each cross-checked against a known limit or a registered result, per the transit-dynamics methodology (governing structure → Bogoliubov coefficients → observable → regime of validity).

**The concession that frames Reading B (stated up front, so the dispute is clean).** I do NOT claim the over-production is a Floquet/re-pumping artifact. I co-authored WS-FLOQUET and conceded DEAD on the merits (the `h_par = 8.3e-4` vs `q ≈ 0.5` ~607× over-assignment). The {β_k} are time-frozen. Reading B is the ORTHOGONAL artifact channel: **frozen-in-time ≠ converged-in-truncation.** The relic can be a perfectly stationary Bogoliubov output (Floquet-DEAD) whose A_s NORMALIZATION still rides an unconverged Casimir-ceiling sum. WS-FLOQUET settled the time axis; it said nothing about the L_max axis. This is the whole of my disagreement with lizzi.

---

**Proposition 1 (the A_s magnitude is a band-INTEGRAL, not a single-mode intensive value — dimensional structure).** A_s is a power-spectrum normalization, `A_s = (k³/2π²) P_ζ(k_pivot)`. On the post-transit relic, the produced amplitude that the impulse-quench routes (inv-5 W2-1, inv-6 W2-2) actually compute is sourced from the relic content `ρ_relic = Σ_{k∈band} mult(k)·|β_k|²·ω_k` over the fold pair band `[0.94, 3.72] M_KK` (inv-12 W3-1's object). The map from produced relic content to A_s normalization passes through a spectral-density / mode-count integral over the band. **The smoking gun: inv-5 W2-1's "+9.5 OOM" figure is literally `n_pairs/2π²`, where `n_pairs = Σ_k |β_k|² = 59.8` is the EXTENSIVE band count** (LIZ1 §2 concedes this is "the naive aggregate-occupation dump ... reproduced EXACTLY by inv-5 then discarded"). The "+0.86 OOM correct-functional" figure is the SAME spectrum re-weighted through the impulse-quench functional — but a re-weighting `Σ_k w(k)·|β_k|²` is STILL a band-sum, STILL `Σ_k`, STILL finite-L-truncated. There is no route from a single intensive per-mode `S_IC(k_pivot)` to an amplitude NORMALIZATION without integrating the spectral density of the band — and that integral is the extensive object.

**Cross-check (limiting case):** in the single-mode limit (one mode in the band, mult=1), the band-integral collapses to the intensive `S_IC(k_pivot)` and Reading A would be exactly right. But the fold band `[0.94, 3.72]` contains `n_pairs = 59.8` pairs across ~1248 relic modes (inv-12 W3-1 / W3-2) — it is manifestly a multi-mode band, and the count is the magnitude. The intensive limit is not the realized regime.

**Proposition 2 (the Mode-Independent Occupation Theorem makes the normalization a PURE COUNT — the substrate-physics core of Reading B).** S57/S62 prove `|β_k|² = n̄` mode-INDEPENDENT (canonical TD result; the tilt comes from geometry, not occupation). Substitute into the relic content:

```
Step 1: |β_k|² = n̄  for all k in the band              [S57/S62 Mode-Independent Occupation Theorem]
Step 2: ρ_relic = Σ_{k∈band} mult(k)·|β_k|²·ω_k = n̄ · Σ_{k∈band} mult(k)·ω_k     [substitute Step 1]
Step 3: the over-production magnitude ∝ ρ_relic ∝ Σ_{k∈band} mult(k)·ω_k         [Step 2; n̄ is the L-saturated per-mode constant]
Step 4: Σ_{k∈band} mult(k)·ω_k is the spectral-weighted MODE COUNT of D_K(τ_fold) eigenvalues in [0.94, 3.72]
Conclusion: mode-independence makes the per-mode value trivially L-saturated AND makes the
            NORMALIZATION a pure (spectral-weighted) count of band-modes — the MOST
            truncation-sensitive object on a finite spectral triple.
```

This is why I argue S57/S62 cuts AGAINST Reading A on the magnitude: lizzi invokes it for "intensiveness," but the theorem's actual content is `A_s ∝ n̄ × (extensive band-count)`. The intensive factor `n̄` is L-saturated (helps the SHAPE); the count `Σ mult(k)` is Casimir-ceiling-soft (sets the MAGNITUDE). **Mode-independence is the mechanism by which the magnitude becomes a count.**

**Proposition 3 (the band-count is NOT Friedrich-Bär-saturated at the canonical L_max — `truncation_consistent = False` is a property of the magnitude-bearing observable).** The fold pair band `[0.94, 3.72] M_KK` is a FIXED M_KK-clock window. As L_max grows, the Peter-Weyl (p,q) sectors with rising Casimir floor `|λ|_min^(p,q) ≈ √(C₂(p,q))/r(τ_fold)` keep dropping NEW eigenvalues INTO the band until the floor clears the band ceiling 3.72. So the band-count `Σ_{k∈band} mult(k)` keeps growing with L_max until Friedrich-Bär saturation (`η_FB·√(C₂(p+q)+1) > 3.72`). inv-12 W3-1 measured this directly: the summed `ρ_relic` moved **rel 0.43** from p+q ≤ 7 to p+q = 8 — a 43% shift, the operational definition of `truncation_consistent = False`. A 43% per-shell increment is nowhere near saturated; it is the signature of a count still accumulating sectors.

**Cross-check against my own Friedrich-Bär ledger (the math-scripts.md D_K feasibility pre-check):** for an observable whose band-ceiling is at finite |λ|, the number of contributing (p,q) sectors is `#{(p,q) : √(C₂(p,q))/r(τ_fold) < |λ|_ceiling}`. At τ_fold = 0.190, λ_min ≈ 0.82 and the band ceiling is 3.72; sectors keep qualifying until `C₂(p,q) > (3.72·r)²`. With r(τ_fold) the Jensen-deformation radius, this admits sectors well beyond p+q = 8 — consistent with the 43% un-saturated increment inv-12 reports. **The magnitude-bearing band-count is in the un-saturated regime at the canonical L_max.** Contrast the SHAPE: the RATIO `|β_{k1}|²/|β_{k2}|²` between two fixed band-modes is `n̄/n̄ = 1` exactly (S57/S62), L-saturated trivially — which is why `n_s`/`α_s` are intensive and Reading-A-correct *for the shape*. The shape-ratio is saturated; the magnitude-count is not.

**Proposition 4 (`truncation_consistent = False` + `K_sub − 1` ~2000× spread is the direct evidence, and Reading A's pre-emption does not reach it).** inv-12 W3-1 reports two truncation-sensitivity diagnostics on the relic content: (i) `truncation_consistent = False` (rel 0.43, p+q ≤ 7→8); (ii) `K_sub − 1` ranging ~2000× across occupation inputs (LIZ2 §fork-B cites both). LIZ1's pre-emption concedes BOTH are real but assigns them to "the EXTENSIVE summed `ρ_relic`" and claims that object "does not enter A_s." **Propositions 1–2 close exactly that escape:** the over-production NUMBERS (+0.86 / +1.455 / +9.5 / +6.008 OOM) were COMPUTED from the produced relic content (the band-sum), and S57/S62 makes the A_s normalization `∝ n̄ × (band-count)` — so the extensive object IS the magnitude-bearing object, not a separate quantity that "does not enter." The ~2000× `K_sub` spread across occupation inputs is itself a tell: an INTENSIVE per-mode modulus would not swing 2000× with the occupation-input choice; a band-count weighted by occupation does. **Reading B verdict: the over-production MAGNITUDE is an L_max-/occupation-SOFT band — a Casimir-ceiling truncation-sensitivity (`truncation_consistent = False`), not a converged physical d.o.f.** The number is not physical until the band-count saturates in L_max (a Friedrich-Bär extrapolation, NOT a single-truncation read).

**The honest concession that frames the verdict (the regime of validity of Reading B).** Reading B is the upper bound of the truncation-softness question, exactly as Reading-A-LIVE was the upper bound of the dynamical-liveness question in WS-FLOQUET. My claim is narrow and falsifiable: IF the magnitude-bearing observable is the band-count `Σ mult(k)`, THEN it is `truncation_consistent = False` and the magnitude is soft. The one thing that would defeat Reading B is a Friedrich-Bär demonstration that the SPECIFIC bottom-K observable setting A_s saturates at the canonical L_max — i.e., that the over-production number is read off a per-mode-saturated quantity, not the un-saturated band-count. That is precisely the nazarewicz tie-breaker (LIZ2's Friedrich-Bär arbiter), and it is the clean decider. If the band-count saturates, Reading A wins on the magnitude; if it does not (my prediction, grounded in inv-12 W3-1's rel 0.43), Reading B wins and CF-AS-3 must record a BAND.

**Substrate-first closing frame.** This is not a power spectrum normalized in an expanding container. A_s is the squeezing modulus of the relic acoustic interference pattern of the D_K eigenvalue spectrum; the over-production is the produced relic content relative to the Bunch-Davies infimum; the truncation softness is a statement about how many band-modes the finite spectral triple has resolved at the canonical L_max. The arrow is `D_K eigenvalues → impulse-quench {β_k} → band-count Σ mult(k)·|β_k|² → A_s normalization → (truncation_consistent = False) → SOFT magnitude`. The substrate's own mode-count in the fold band is not yet converged; the over-production number inherits that.

#### TD2: Questions for lizzi

These are sharp, framed so each answer is a number, a derivation, or a named observable — not a stance. Each targets a load-bearing claim in LIZ1/LIZ2.

**TD2-1 (the intensive/extensive assignment — the crux, mirror of your LIZ1-Q1).** You claim the over-production magnitude is the INTENSIVE per-mode `S_IC(k_pivot) = 1 + 2n_{k_pivot}` and that the EXTENSIVE `Σ mult·|β_k|²` "does not enter A_s." But inv-5 W2-1's +9.5 OOM figure is `n_pairs/2π²` with `n_pairs = Σ_k|β_k|² = 59.8` (the band-count), and the +0.86 OOM "correct functional" figure is the same spectrum re-weighted — still a `Σ_k`. **Write the explicit map from your intensive `S_IC(k_pivot)` to the dimensionless A_s normalization without any band-sum `Σ_k mult(k)`.** If the map requires a spectral-density integral over the band (as the power-spectrum normalization `A_s = (k³/2π²)P_ζ` does), then the magnitude is on the count-side — which you concede is `truncation_consistent = False`. Name the single mode whose intensive `S_IC` alone fixes A_s, or concede the normalization integrates the band.

**TD2-2 (the Mode-Independent Occupation Theorem cuts which way?).** You invoke S57/S62 to argue the magnitude is intensive (per-mode `|β_k|²` L-saturated). But S57/S62 proves `|β_k|² = n̄` mode-INDEPENDENT, which makes `ρ_relic = n̄ · Σ_{k∈band} mult(k)·ω_k` — i.e., A_s `∝ n̄ × (band-count)`, the band-count being the most truncation-sensitive object on a finite triple. **Do you contend that mode-independence makes the NORMALIZATION intensive, or only the per-mode VALUE and the SHAPE-ratio intensive?** If the latter (which is what the theorem proves), the normalization is a count and your "intensive magnitude" claim does not follow from S57/S62. Show how mode-independence makes a NORMALIZATION (not a ratio) intensive.

**TD2-3 (the Friedrich-Bär prediction — state it now so nazarewicz adjudicates cleanly, mirror of your LIZ2-Q2).** I predict the bottom-K observable setting the over-production magnitude FAILS to saturate: NEW (p,q) sectors keep dropping eigenvalues into the fixed band `[0.94, 3.72]` as L_max grows, so `Σ_{k∈band} mult(k)` is un-saturated at the canonical L_max — consistent with inv-12 W3-1's rel 0.43 (p+q ≤ 7→8). **What is YOUR Friedrich-Bär prediction for the SPECIFIC magnitude-bearing observable?** Do the NEW-sector eigenvalues at L_max+1 inside the band lie ABOVE the band ceiling (saturated, your A) or do they keep landing inside (un-saturated, my B)? A per-sector eigenvalue-floor statement, not a re-run of the disputed sum — so the R3 nazarewicz argument decides rather than retrofits.

**TD2-4 (what does the ~2000× `K_sub` spread mean if the magnitude is intensive?).** inv-12 W3-1 reports `K_sub − 1` swinging ~2000× across occupation inputs. An INTENSIVE per-mode squeezing modulus `S_IC(k_pivot) = 1 + 2n̄` should be essentially fixed (it is `1 + 2×` a mode-independent constant). **If the magnitude is the intensive `Ô` you claim, why does `K_sub` swing 2000× with the occupation-input choice?** A band-count weighted by occupation swings; a single intensive per-mode modulus does not. Reconcile the 2000× spread with the intensiveness claim, or concede the swinging quantity is the extensive count.

**TD2-5 (the CF-AS-3 pin FORM — POINT vs BAND, the load-bearing consequence).** Your LIZ2 CF-AS-3 gate says the verdict sets whether the canonical pin is a POINT (Reading A) or a BAND (Reading B). **If the truncation softness (Reading B) and the functional-choice freedom (Reading A) are BOTH present, which dominates the width of the A_s magnitude uncertainty?** Quantitatively: is inv-12 W3-1's truncation spread (rel 0.43 → ~0.16 OOM per shell, un-extrapolated) larger or smaller than your functional spread (your "sub-2-OOM band across functionals")? If the truncation softness is comparable to or exceeds the functional spread, CF-AS-3 MUST record a BAND with a Friedrich-Bär-extrapolation tag, not a POINT — regardless of whether the residual functional freedom is "physical." Name the dominant width.

---

## Round 2 — lizzi: Follow-up

### CONVERGENCE

transit-dynamics's R1 sharpened the dispute in three ways that move me, and I record exactly what changed.

**C1 — I concede the frozen-in-time ≠ converged-in-L_max orthogonality, and that I under-separated it in R1.** TD's framing (Re:LIZ1 claim-1, TD1 "the concession that frames Reading B") is structurally correct and is the most important thing that changed for me: WS-FLOQUET=DEAD settled the *time* axis (the {β_k} are not secularly re-pumped); it said NOTHING about the *L_max* axis (whether the magnitude has converged in truncation). These are genuinely orthogonal substrate properties — a Bogoliubov output can be perfectly time-stationary AND sit on an unconverged Casimir-ceiling object. My LIZ1 leaned on WS-FLOQUET=DEAD to certify "genuine locked amplitude," and TD is right that "locked in time" does not by itself discharge "converged in truncation." I accept the axis-separation as a permanent structural distinction, and it sharpens — not weakens — Reading A: the leg I must now defend is purely the L_max-convergence of the *specific magnitude-bearing observable*, with the time axis already conceded-closed on both sides. The dispute is now single-axis: L_max, on a named observable.

**C2 — I AGREE the SHAPE/normalization split is the correct decomposition, and that S57/S62 is a SHAPE theorem.** TD's Re:LIZ1 "MISSED" tag lands a real correction. S57/S62 (Mode-Independent Occupation, `|β_k|² = n̄`) proves the *tilt* `n_s` comes from geometry not occupation — it is a statement about the scale-DEPENDENCE (shape), and it is L-saturated. I over-loaded it in LIZ1 claim-1 when I wrote "per-mode locked + L-saturated for the shape" and then let "L-saturated" bleed into a normalization claim. I retract that bleed. S57/S62 saturates the SHAPE-ratio `|β_{k1}|²/|β_{k2}|² = n̄/n̄ = 1`; it does NOT, by itself, certify the magnitude. The shape-intensive / normalization-? split is the right frame, and TD established it. Where I still disagree is which object the *canonical* normalization map integrates (DISSENT D1) — but the decomposition itself is conceded.

**C3 — I AGREE the Friedrich-Bär test on a NAMED observable is the clean arbiter, and that the prediction must be stated as a per-sector eigenvalue-floor statement (not a re-run of the disputed sum).** TD2-3 and my LIZ2-Q2 converge here: nazarewicz's R3 Turn-B argument must adjudicate "do the NEW-sector eigenvalues at L_max+1 inside the magnitude-bearing window lie above its ceiling (saturated) or keep landing inside (un-saturated)?" — a per-(p,q)-sector Casimir-floor statement. I commit to stating MY prediction in exactly this form (QUESTIONS, answer to TD2-3) so nazarewicz adjudicates rather than retrofits. This is the methodologically-correct arbiter because it is functional-INDEPENDENT: whether `η_FB·√(C₂(p,q)+1)` clears the window ceiling does not depend on which spectral functional weights the modes — so it cleanly separates the truncation question (Reading B's axis) from the functional-freedom question (Reading A's axis), which is precisely what a clean adjudication of two orthogonal openness-sources requires (TD's own Q3 sharpening (b-i)/(b-ii)).

**C4 — I accept TD's sharpening of the HK-AS-FLOOR (b)-annotation: two distinct openness SOURCES.** TD's Re:LIZ2-Q3 splits the MAGNITUDE openness into (b-i) functional-choice freedom (my "physical d.o.f.") and (b-ii) L_max-truncation softness of the band-count (TD's Reading B). This is correct and I adopt it for the R3 register patch: the (b) annotation must name BOTH candidate sources, and — per TD2-5 — the FORM of the CF-AS-3 pin (POINT vs BAND) is decided by which source dominates the width. I converge on this as the operative gate criterion for CF-AS-3 (see EMERGENCE E2). The register split lands in R3 Turn B regardless of the A/B fork (both readings agree the *number* is not permanent), exactly as TD affirms.

### DISSENT

My dissent narrows to ONE claim, but it is decisive: TD's substitution chain (Re:LIZ1 Step 1–5; TD1 Prop 1–2) **mis-identifies which A_s construction is canonical**. The canonical substrate→A_s map is intensive-at-pivot; TD's `Σ_k mult(k)` band-count is the construction the corpus *already reproduced and DISCARDED* as a normalization artifact. New evidence (the canonical UNIFIED-AS-79 map, pulled from the register, not asserted) below.

**D1 — The canonical normalization map is `K_substrate ≡ S_IC^GGE(k_pivot)`, evaluated AT THE PIVOT, with NO band-sum.** This is not my framing; it is the register's. The UNIFIED-AS-79 canonical map (S82 `session-82-results-workingpaper.md` line 1696; re-confirmed S84 W3 ledger, S85 W8-2 NG-block at 2.97e-16, S86 W1b) is:

```
A_s^substrate = A_s^BD · K_substrate ,   K_substrate ≡ S_IC^GGE(k_pivot) = 1 + 2 n_{k_pivot} = coth(ω_{k_pivot} / 2 T_{k_pivot})
```

`K_substrate` is a SINGLE per-mode squeezing factor at the pivot — `coth(x/2)` with `x = Δ/T` at ONE mode. There is no `Σ_k`, no `mult(k)`, no band-integral anywhere in the canonical map. The per-band values are B1=2.0485, B2=1.9222, B3=2.1848 (S82 canonical table, line 1724–1728) — per-mode coth factors, not counts. So TD1 Proposition 1's claim "there is no route from a single intensive per-mode `S_IC(k_pivot)` to an amplitude NORMALIZATION without integrating the spectral density of the band" is **factually contradicted by the canonical map**: the route exists, it is `A_s^BD · S_IC(k_pivot)`, and it is the framework's own UNIFIED-AS-79 construction. TD's Step 1 (`A_s = (k³/2π²)P_ζ(k_pivot)`) is correct AND already evaluates the power spectrum AT `k_pivot`; the substrate modification is the *ratio* `P_ζ^GGE(k_pivot)/P_ζ^BD(k_pivot) = S_IC(k_pivot)` — a per-mode quantity. TD's Step 3 then silently replaces `P_ζ(k_pivot)` (pivot-evaluated) with `ρ_relic = Σ_k mult·|β_k|²·ω_k` (band-summed). That replacement is the error: it swaps the canonical pivot-evaluation for a DIFFERENT object (the total relic energy density), which is not what normalizes the power spectrum at the pivot.

**D2 — TD's band-count IS a real object, but it is the inv-5 `+9.5 OOM` figure that inv-5 itself reproduced and DISCARDED.** I want to be scrupulously fair: TD is RIGHT that `n_pairs/2π² = 59.8/2π²` is an extensive band-count, RIGHT that it gives +9.5 OOM, and RIGHT that S57/S62 makes that dump `∝ n̄ × (band-count)`. But this is precisely the construction LIZ1 §2 already flagged as "the naive aggregate-occupation dump ... reproduced EXACTLY by inv-5 then discarded." TD1 Prop 2's chain (Step 1–4, `ρ_relic = n̄ Σ mult(k)·ω_k`) is a valid derivation OF THE DISCARDED DUMP — it correctly shows why the `n_pairs/2π²` construction is extensive and truncation-soft. What it does NOT show is that the *canonical* A_s (the UNIFIED-AS-79 pivot map, D1) is that object. There are two constructions in the corpus: (i) the canonical pivot map `A_s^BD · S_IC(k_pivot)` (intensive, +0.86 OOM regime via the correct functional); (ii) the aggregate dump `n_pairs/2π²` (extensive, +9.5 OOM, discarded). TD's Prop 1–2 derive (ii)'s extensiveness flawlessly and then attribute it to (i). The +0.86 OOM "correct functional" figure is (i)-regime — the pivot squeezing modulus times the dimensionless prefactor — not a re-weighting `Σ_k w(k)|β_k|²` of (ii).

**D3 — The "~2000× K_sub spread" (TD2-4) is a ratio-of-small-numbers artifact at the BD floor, NOT a swing in the physical magnitude.** I verified this symbolically (Sage, exact-rational discipline per `regulator-pin-discipline.md`). The physical squeezing factor `K_sub = coth(x/2)` is genuinely TIGHT across all band inputs:

```
K_sub per band (S82 x-values):  B1=2.0485, B2=1.9222, B3=2.1848  →  spread (max/min) = 1.137×  (14%)
(K_sub − 1) per band:           B1=1.0485, B2=0.9222, B3=1.1848  →  spread = 1.285×
near-BD-floor regime (inv-12 W1-2, K_sub ∈ [1.00002, 1.00384]):
   (K_sub − 1) ratio = (1.00384−1)/(1.00002−1) = 192×    ← what TD reads as "~2000×"
   K_sub ratio       = 1.00384/1.00002          = 1.0038×  ← the PHYSICAL factor barely moves
```

The ~2000× (TD cites it; the W1-2 reference-state regime gives 192× on `K_sub − 1`, and the occupation-input sweep widens it further) is the ratio of the *excess over the BD floor* `(K_sub − 1)`, which diverges BY CONSTRUCTION as `K_sub → 1` (the reference-state regime sits microscopically above the floor, so its excess is a tiny number sensitive to input). It measures *proximity to the BD floor*, not truncation-softness of the magnitude. The physical `K_sub` itself moves by 1.0038× there and 1.137× across the full band table. So TD2-4's premise — "an intensive per-mode modulus should be essentially fixed, yet `K_sub` swings 2000×" — is answered: the intensive modulus IS essentially fixed (1.0038–1.137×); what swings 2000× is the subtractive excess, which is the wrong lens. This is the same intensive coth factor viewed through `(K_sub−1)` instead of `K_sub`.

**D4 — Why this is the lizzi-signature outcome, not a dodge.** My methodology's discriminator: what is functional-/truncation-INDEPENDENT is structural; what depends on the choice is a physical or scheme d.o.f. The canonical pivot map's `S_IC(k_pivot) = coth(ω/2T)` is intensive (D1), tight (D3), and its scheme-dependence ACROSS functionals (cutoff/zeta/impulse-quench/near-floor-DOS, the −3.02/+0.86/+6.008 OOM spread) is the spectral functional acting as a physical d.o.f. — exactly the CV-1↔CV-4 cross-link (the A_s magnitude sign-flips across functionals for the SAME reason the a₀/a₂ CC ratio does). TD's band-count, by contrast, is the EXTENSIVE relic energy density `ρ_relic` — which I AGREE is `truncation_consistent = False` and is the magnitude-bearing object ONLY for the discarded dump construction (ii). My dissent is therefore precise: I concede the extensive `ρ_relic` is L_max-soft (D2, and LIZ1's pre-emption already conceded it); I dissent that it is what the CANONICAL A_s integrates (D1). The Friedrich-Bär test on the NAMED canonical observable (the pivot `S_IC`, not `ρ_relic`) is what decides this — and that is where I make my prediction (QUESTIONS).

### EMERGENCE

**E1 — The crux is no longer "which side of the intensive/extensive partition," it is "WHICH CONSTRUCTION is canonical" — and that is a verifiable register question, not a stance.** Both R1 openings framed the dispute as: does the magnitude read off the intensive `S_IC(k_pivot)` (A) or the extensive `Σ mult(k)` (B)? D1–D2 reframe it: BOTH objects exist in the corpus, and the real question is which one the canonical A_s map uses. The register answers it (UNIFIED-AS-79 = pivot map, intensive). This is a cleaner adjudication than a partition-side debate because it has a documentary fact at its center, not two competing intuitions. It also dissolves a false symmetry in the R1 framing: TD and I were not symmetric claimants about one object's epistemic type; we were each defending a DIFFERENT object (pivot map vs aggregate dump), and the workshop's job is to certify which is the A_s normalizer. The Friedrich-Bär test then applies to the WINNER of that identification — which is why naming the observable (C3) is load-bearing.

**E2 — The CF-AS-3 pin FORM is now decidable by a quantitative dominance test (TD2-5 operationalized), and I commit to it as the gate.** TD2-5 asks: if both truncation-softness (b-ii) and functional-freedom (b-i) are present, which dominates the width? D1–D3 give the answer FOR THE CANONICAL PIVOT MAP: the truncation-softness of the pivot `S_IC` is bounded by the per-mode coth's L_max-sensitivity (small — the pivot mode's `(Δ,T)` are L-saturated band-edge quantities, B3 softest at 0.178 M_KK well inside the cached spectrum), whereas the functional-freedom spread is the full −3.02→+6.008 OOM cross-functional range. So FOR THE PIVOT MAP, (b-i) functional-freedom dominates → CF-AS-3 records a POINT-per-functional with a scheme-tag (Reading A), NOT a Friedrich-Bär band. BUT — and this is the emergence — IF the Friedrich-Bär test (R3, nazarewicz) shows the pivot `S_IC` itself fails to saturate (my prediction is it saturates; if I am wrong), then (b-ii) re-enters and CF-AS-3 must carry a band. The gate is therefore: **CF-AS-3 = POINT-per-functional + scheme-tag IFF the pivot-`S_IC` observable Friedrich-Bär-saturates; BAND-with-FB-extrapolation-tag otherwise.** This is a sharper, falsifiable form of LIZ2's original POINT-vs-BAND gate, and I adopt it.

**E3 — A new orthogonality for the register: the two openness-sources (b-i)/(b-ii) are themselves on orthogonal axes (functional-choice vs L_max-truncation), which is the algebra-INVARIANT vs truncation-axis distinction in my ledger.** TD's (b-i)/(b-ii) split maps onto a structural orthogonality I already carry: (b-i) functional-choice freedom is the SPECTRAL-FUNCTIONAL axis (which functional weights the spectrum — cutoff/zeta/impulse-quench); (b-ii) L_max-truncation is the TRUNCATION axis (how many sectors the finite triple resolves). These are independent — a quantity can be functional-robust AND truncation-soft, or functional-spread AND truncation-saturated. The A_s magnitude's two openness sources are one instance each. This means the R3 register annotation should tag the magnitude with BOTH axes explicitly (scheme-tag for b-i, L_max-tag for b-ii), per my standing discipline "every gate states (value, scheme_tag, convention_tag, L_max_tag)." The HK-AS-FLOOR (b)-row gains a two-axis sub-annotation, and CF-AS-3's pin carries both tags regardless of which dominates.

### QUESTIONS

I answer TD2-1 through TD2-5 directly (each a number, a derivation, or a named observable per TD's framing), then sharpen the `truncation_consistent` test for R3.

**Answer to TD2-1 (the explicit pivot→A_s map with NO band-sum).** You asked me to "write the explicit map from the intensive `S_IC(k_pivot)` to the dimensionless A_s normalization without any band-sum `Σ_k mult(k)`, or concede the normalization integrates the band." The map exists and is the canonical UNIFIED-AS-79 one (D1):

```
A_s^substrate = A_s^BD(k_pivot) · K_substrate ,   K_substrate = S_IC^GGE(k_pivot) = coth(ω_{k_pivot} / 2 T_{k_pivot})
```

The single mode whose intensive `S_IC` fixes the substrate modification is `k_pivot` itself — the pivot mode at which the power spectrum is normalized (`A_s = (k³/2π²)P_ζ`|_{k=k_pivot}, your own Step 1). The substrate's effect is the RATIO `P_ζ^GGE(k_pivot)/P_ζ^BD(k_pivot) = S_IC(k_pivot)`, one coth factor. NO `Σ_k`. The reason your Step 3 needed a band-sum is that you substituted `ρ_relic` (total relic energy density) for `P_ζ(k_pivot)` (pivot power-spectrum value) — but the power spectrum at the pivot is not the total energy density; it is the per-mode variance at one k. I do NOT concede the canonical normalization integrates the band — I name the mode (k_pivot) and the per-mode factor (coth) the register uses.

**Answer to TD2-2 (S57/S62 cuts which way?).** You asked: does mode-independence make the NORMALIZATION intensive, or only the per-mode VALUE and SHAPE-ratio? **Only the per-mode value and the shape-ratio** — I concede this fully (CONVERGENCE C2). S57/S62 does NOT, by itself, make the normalization intensive. The normalization's intensiveness comes from a SEPARATE fact: the canonical map evaluates `S_IC` at ONE mode (k_pivot), so no count enters (TD2-1). So your TD2-2 lands — but it defeats my over-extended R1 phrasing, not Reading A: I do not need S57/S62 to carry the normalization; I need the UNIFIED-AS-79 pivot map (D1), which carries it without invoking mode-independence at all. S57/S62's role is downstream and narrow: it certifies the SHAPE (n_s, α_s) is intensive, which is a separate, already-PASS result. The magnitude's intensiveness is the pivot-map fact, not the mode-independence fact.

**Answer to TD2-3 (MY Friedrich-Bär prediction, as a per-sector eigenvalue-floor statement).** You predict the band-count `Σ_{k∈band} mult(k)` fails to saturate (NEW sectors keep dropping eigenvalues into `[0.94, 3.72]`). **I agree that prediction is correct FOR THE EXTENSIVE BAND-COUNT `ρ_relic`** — and inv-12 W3-1's rel 0.43 confirms it. My prediction is for the DIFFERENT, canonical observable (the pivot `S_IC`): **the pivot squeezing factor `S_IC(k_pivot) = coth(Δ_{pivot}/2T_{pivot})` IS Friedrich-Bär-saturated at the canonical L_max**, because it depends ONLY on the pivot mode's gap `Δ_{pivot}` and temperature `T_{pivot}`, both band-EDGE quantities at fixed low |λ| (B3 softest = 0.178 M_KK, B1 acoustic = 0.4643 M_KK — well below the band ceiling 3.72, in the bottom of the spectrum where the lowest (p,q) sectors already resolve them). Per-sector statement: the pivot mode lives in a LOW-Casimir sector `(p,q)` with `√(C₂(p,q))/r(τ_fold)` already below the pivot |λ| at the canonical L_max; NEW sectors at L_max+1 have HIGHER Casimir floors `η_FB·√(C₂(p+q+1)+1)` that land ABOVE the pivot |λ|, so they do NOT alter `Δ_{pivot}` or `T_{pivot}`. The pivot `S_IC` is saturated; the band-count `ρ_relic` is not. **The two observables saturate differently, and nazarewicz must test the PIVOT one (the canonical A_s normalizer), not the band-count.** If nazarewicz finds the pivot `S_IC` fails to saturate (NEW low-Casimir sectors dropping below the pivot |λ| and shifting `Δ_{pivot}`), Reading B wins on the canonical magnitude; my prediction is it saturates.

**Answer to TD2-4 (the ~2000× K_sub spread under the intensiveness claim).** Resolved quantitatively in D3 (Sage-verified): the physical `K_sub = coth(x/2)` is essentially fixed (1.0038× near the BD floor, 1.137× across the full band table); the ~2000× is the ratio of the SUBTRACTIVE EXCESS `(K_sub − 1)`, which diverges as `K_sub → 1` by construction and measures proximity-to-floor, not magnitude-swing. So the intensive per-mode modulus IS essentially fixed, exactly as an intensive quantity should be — your TD2-4 premise is satisfied once `(K_sub − 1)` is recognized as the wrong lens. The swinging quantity is the excess, not the magnitude.

**Answer to TD2-5 (POINT vs BAND — which width dominates).** Operationalized in E2. FOR THE CANONICAL PIVOT MAP: the truncation width on `S_IC(k_pivot)` is small (the pivot `(Δ,T)` are L-saturated band-edge quantities, TD2-3 answer), while the functional-freedom width is the full −3.02→+6.008 OOM cross-functional spread. So (b-i) functional-freedom dominates → **CF-AS-3 records a POINT-per-functional with a scheme-tag (Reading A form), conditional on the pivot-`S_IC` Friedrich-Bär-saturating**. IF nazarewicz's R3 test shows the pivot `S_IC` does NOT saturate, (b-ii) re-enters and CF-AS-3 must carry a BAND with an FB-extrapolation tag. The dominant width is functional-freedom FOR THE PIVOT MAP; the gate is the FB test on the pivot observable (E2). I do NOT claim the band-count is a point — I claim the band-count is not the canonical A_s normalizer (D1–D2).

**Sharpening the `truncation_consistent` test for R3 (the load-bearing handoff to nazarewicz).** The decisive R3 question is now precise and NAMED: nazarewicz's Friedrich-Bär saturation test must be applied to the **pivot squeezing observable `S_IC(k_pivot) = coth(Δ_{pivot}/2T_{pivot})`** — specifically, do the NEW-sector eigenvalues at L_max+1 that fall BELOW the pivot |λ| (and would thereby shift `Δ_{pivot}`/`T_{pivot}`) exist, or do all NEW sectors have Casimir floors `η_FB·√(C₂(p+q+1)+1)` ABOVE the pivot |λ|? — NOT to inv-12 W3-1's summed `ρ_relic` (which both sides agree is `truncation_consistent = False`, an extensive object). The two observables are different; the workshop's verdict turns on testing the canonical one. This is the cleanest arbiter because it is a per-sector eigenvalue-floor statement on a SPECIFIC named observable, functional-INDEPENDENT, and decides A/B without re-running the disputed sum (TD2-3 + my LIZ2-Q2 converge on exactly this protocol).

---

## Round 2 — transit-dynamics: Cross-Synthesis

### CONVERGENCE

lizzi's R2 produced a documentary fact (D1) that decides the largest piece of my R1 against me, and I concede it on the merits — exactly as I conceded the WS-FLOQUET depth mis-assignment. I queried the register directly before conceding (knowledge-MCP, not lizzi's assertion taken on faith), and the canonical map is what lizzi says it is. Three concessions, each register- or Sage-verified.

**C1 — I CONCEDE D1: the canonical UNIFIED-AS-79 A_s map is the INTENSIVE per-mode pivot coth, with NO band-sum. My R1 Proposition 1's universal claim is factually wrong.** I pulled the canonical map from the knowledge MCP (`session-82-results-workingpaper.md`, the UNIFIED-AS-79 equation entries):

```
A_s^substrate = A_s^{W1-2} · K_substrate ,   K_substrate ≡ S_IC^GGE(k_pivot) = 1 + 2 n_k^GGE = coth(ω_k / (2 T_k^GGE))   |_{k = k_pivot}
```

This is a SINGLE per-mode `coth` evaluated AT the pivot — `K_substrate = coth(Δ_B/2T_k^GGE) = 2.035` (the `s82-as-ledger-self-consistent.md` entry confirms it is "a multiplicative factor on the prefactor"). There is no `Σ_k`, no `mult(k)`, no band-integral in the canonical normalization map. My R1 Proposition 1 asserted "there is no route from a single intensive per-mode `S_IC(k_pivot)` to an amplitude NORMALIZATION without integrating the spectral density of the band." **That universal is contradicted by the framework's own construction** — the route exists, it is `A_s^{BD}(k_pivot) · S_IC(k_pivot)`, and it is the register's canonical UNIFIED-AS-79 form. My Step 3 (Re:LIZ1) silently swapped `P_ζ(k_pivot)` (the pivot power-spectrum VALUE, which the substrate modifies by the per-mode RATIO `P_ζ^GGE/P_ζ^BD = S_IC(k_pivot)`) for `ρ_relic = Σ_k mult·|β_k|²·ω_k` (the TOTAL relic energy density, a different object). The total energy density is not what normalizes the power spectrum at a pivot. I retract the substitution. **The canonical magnitude-bearing observable is the intensive pivot coth, not the extensive band-count.** My TD1 Prop 1–2 correctly derive the extensiveness of `ρ_relic`, but `ρ_relic` is the DISCARDED `n_pairs/2π² = +9.5 OOM` dump (construction (ii)), not the canonical pivot map (construction (i)). I conflated the two.

**C2 — I CONCEDE D3: the ~2000× `K_sub` spread is a subtractive-excess artifact, not a magnitude swing — my TD2-4 premise is dissolved.** I re-derived lizzi's D3 in Sage (exact-rational discipline). The physical squeezing factor `K_sub = coth(x/2)` is tight: spread 1.137× (14%) across the S82 band table (B1=2.0485, B2=1.9222, B3=2.1848), and 1.0038× in the near-BD-floor regime (inv-12 W1-2, K_sub ∈ [1.00002, 1.00384]). The ~2000× (192× on the floor regime, confirmed) is the ratio of the SUBTRACTIVE EXCESS `(K_sub − 1)`, which diverges by construction as `K_sub → 1` (a tiny number sensitive to inputs) and measures *proximity to the BD floor*, NOT truncation-softness of the magnitude. My TD2-4 read `(K_sub − 1)` as the lens; the physical lens is `K_sub` itself, which is essentially fixed — exactly as an intensive quantity should be. I concede the premise was on the wrong variable.

**C3 — I CONCEDE my S57/S62 → normalization argument was an over-reach lizzi already granted me half of, but the residue cuts the OTHER way once D1 lands.** My TD1 Prop 2 argued S57/S62 makes the normalization `∝ n̄ × (band-count)`. That derivation is CORRECT *for the discarded dump* (construction (ii)) — and lizzi concedes it as such (D2). But once D1 establishes the canonical map evaluates `S_IC` at ONE mode, S57/S62 is no longer load-bearing on the normalization at all (it certifies the SHAPE, which is a separate already-PASS result). lizzi's TD2-2 answer is right: mode-independence makes only the per-mode VALUE and the SHAPE-RATIO intensive, not the normalization — and the normalization's intensiveness comes from the SEPARATE pivot-evaluation fact (D1), not from S57/S62. I accept that S57/S62 carries neither side's normalization claim. **The crux is no longer "which side of the intensive/extensive partition" — it is lizzi's E1 reframing: WHICH construction is canonical, and the register answers it (pivot map, intensive).** I adopt E1 as the corrected frame: TD and I were not symmetric claimants about one object's epistemic type; we were defending two DIFFERENT objects (pivot coth vs aggregate dump), and the register certifies the pivot coth as the A_s normalizer.

What survives of Reading B after C1–C3 is narrow and I state it without inflation: the dispute is now single-axis (L_max), on a SINGLE named observable (the pivot coth `coth(Δ_pivot/2T_pivot)`), and the residual question is whether THAT observable's INPUTS saturate (DISSENT). The extensive-band-count leg of Reading B is conceded-closed as the discarded-dump object.

### DISSENT

My dissent narrows to a SINGLE claim after C1–C3, and it is NOT a re-assertion of R1 — it is new, grounded in a register fact I pulled this round that lizzi's R2 did not address. lizzi's D1/TD2-3-answer treats the pivot coth's inputs `(Δ_pivot, T_pivot)` as fixed band-EDGE quantities ("both band-edge quantities at fixed low |λ|"). The gap `Δ_pivot` is indeed a single eigenvalue. **But `T_pivot` is NOT a single-eigenvalue quantity — it is a band-statistical GGE Lagrange multiplier, and its L_max-saturation is a structurally DIFFERENT question from a single eigenvalue's.**

**D(TD)1 — the pivot coth's TEMPERATURE input is a Richardson-Gaudin charge-conjugate, fixed by a band-AGGREGATE constraint (new register evidence).** I queried the GGE-temperature construction (knowledge-MCP). The canonical definitions:

```
S_IC^GGE(k_pivot) = coth(ω_{k_pivot} / 2 T_{k_pivot}^GGE)              [session-82, canonical map]
T_k^GGE = 1 / (k_B λ_k) ,  λ_k = Lagrange multiplier conjugate to the conserved integral I_k   [S38, GGE temperature]
λ_B fixed by the Richardson-Gaudin conserved charges:  λ_B1=2.771, λ_B2=1.459, λ_B3=6.007       [atlas-07/S39 PERMANENT]
the conserved charges enforce ⟨Q⟩_GGE = N_pair = 59.8  (a BAND-AGGREGATE occupation sum)        [S74 NOETHER-CHAIN]
```

So the substitution chain for what the pivot coth actually depends on is:

```
Claim (Reading B, narrowed): "the pivot coth coth(Δ_pivot/2T_pivot) is intensive in FORM (D1, conceded),
                              but its TEMPERATURE input T_pivot = 1/λ_pivot is set through the GGE construction
                              by a BAND-AGGREGATE Richardson-Gaudin charge — so its L_max-saturation is NOT
                              the saturation of a single eigenvalue."

Step 1: K_substrate = coth(Δ_pivot / 2 T_pivot)                                  [D1, canonical map — conceded intensive in form]
Step 2: T_pivot = 1/(k_B λ_pivot)                                                 [S38 GGE temperature definition]
Step 3: λ_pivot is the Lagrange multiplier conjugate to the conserved charge I_pivot   [S38]
Step 4: the Richardson-Gaudin charges {I_k} are fixed by ⟨Q⟩_GGE = Σ_{k∈band} |β_k|² = N_pair = 59.8   [S74; the SAME band-aggregate count of TD1]
Step 5: ⟹ T_pivot inherits whatever L_max-dependence the band-aggregate charge constraint carries
Conclusion: the pivot coth is intensive in its FUNCTIONAL FORM (numerator/denominator are pivot-mode quantities)
            but its DENOMINATOR's value is set by a band-statistical Lagrange multiplier, not a single eigenvalue.
            "Does the magnitude-bearing observable saturate?" is therefore a TWO-input question, not one.
```

This is the precise sense in which Reading B survives D1: I do NOT contend the canonical map is the extensive band-count (C1 conceded that). I contend that the canonical map's *temperature input* `T_pivot` is fixed by the GGE construction through a band-aggregate conserved charge — the SAME `N_pair = 59.8` whose band-count I (wrongly) put in the numerator in R1 re-enters, correctly this time, in the *denominator's normalization* via `λ_pivot`. lizzi's D1/D3 establish that the coth FORM is intensive and tight; they do NOT establish that `T_pivot`'s VALUE is L_max-saturated, because `T_pivot` is not a band-edge eigenvalue — it is `1/λ_pivot` with `λ` a charge-conjugate.

**D(TD)2 — the gap input `Δ_pivot` likely IS saturated; the temperature input `T_pivot` is the open one — and that asymmetry is what lizzi's R2 did not separate.** I grant lizzi's per-sector argument FOR THE GAP: `Δ_pivot` is a single low-Casimir eigenvalue (B1 acoustic = 0.4643 M_KK, B3 = 0.178 M_KK), and NEW (p,q) sectors at L_max+1 have higher Casimir floors `η_FB·√(C₂(p+q+1)+1)` that land above the pivot |λ|, so they do not shift `Δ_pivot`. That half of lizzi's TD2-3 answer I accept. **But the temperature `T_pivot = 1/λ_pivot` is fixed by `⟨Q⟩_GGE = Σ_{k∈band}|β_k|²`, which is the band-aggregate occupation — and THAT is the object inv-12 W3-1 measured as `truncation_consistent = False` (rel 0.43, p+q ≤ 7→8).** As L_max grows and NEW sectors drop eigenvalues into the band `[0.94, 3.72]`, the conserved-charge normalization `⟨Q⟩` shifts (more occupied band-modes), which shifts `λ_pivot` (the Lagrange multiplier that enforces the charge), which shifts `T_pivot`, which shifts `coth(Δ_pivot/2T_pivot)`. The gap saturates; the temperature is tied to the un-saturated band-aggregate. **lizzi's TD2-3 answer asserts "T_pivot depends ONLY on the pivot mode's temperature, a band-edge quantity" — but the register says `T_pivot = 1/λ_pivot` and `λ_pivot` is a band-aggregate charge-conjugate. That is the gap in lizzi's prediction.**

**D(TD)3 — this is falsifiable and the regime of validity is explicit (the honest bound on Reading B, mirroring my WS-FLOQUET concession structure).** My narrowed claim is: IF `T_pivot` is fixed by the band-aggregate `⟨Q⟩_GGE` (register says it is, S74), THEN `T_pivot` inherits the band-count's `truncation_consistent = False` softness, and the pivot coth is L_max-soft *through its denominator* even though its FORM is intensive. The one thing that defeats this: a demonstration that `λ_pivot` (hence `T_pivot`) is INSENSITIVE to the NEW-sector occupations entering the band as L_max grows — i.e., that the conserved charge `⟨Q⟩` saturates even though the raw band-count does not (e.g., because the NEW in-band modes carry vanishing `|β_k|²` weight, or because the per-band partition `λ_B` is dominated by the lowest already-resolved sectors). That is a quantitative per-sector statement, and it is precisely what nazarewicz must compute (EMERGENCE). lizzi predicts the pivot coth saturates; I predict its temperature input does NOT, because `T_pivot = 1/λ_pivot` is charge-conjugate to a band-aggregate. The two predictions now differ on a NAMED sub-input (`T_pivot`), not on the whole observable — which is the cleanest possible form for the R3 arbiter.

### EMERGENCE

**E(TD)1 — the Friedrich-Bär tie-break is now a TWO-INPUT saturation test, and that is a sharper R3 charge than either R1 framing.** lizzi's E1 reframing (WHICH construction is canonical) is correct and I adopted it (C3). But it does not END the workshop — it RELOCATES the L_max question to the inputs of the certified-canonical observable. The R1 framing posed Friedrich-Bär on ONE object (the band-count, my B; or the pivot coth, lizzi's A). After D1 + D(TD)1, the correct R3 charge to nazarewicz is a per-input decomposition of the SINGLE canonical observable `coth(Δ_pivot/2T_pivot)`:

```
nazarewicz R3 Friedrich-Bär test — TWO per-sector eigenvalue-floor sub-statements on the CANONICAL pivot observable:

  (FB-gap)   Does Δ_pivot saturate?  ⟺  do NEW (p,q) sectors at L_max+1 have Casimir floors
             η_FB·√(C₂(p+q+1)+1) ABOVE the pivot |λ| (so they do not shift Δ_pivot)?
             — TD prediction: YES, saturates (low-Casimir pivot, lizzi's argument accepted).

  (FB-temp)  Does T_pivot = 1/λ_pivot saturate?  ⟺  does the GGE Lagrange multiplier λ_pivot
             (conjugate to ⟨Q⟩_GGE = Σ_{k∈band}|β_k|²) stop moving as NEW in-band sectors at L_max+1
             add occupation to the conserved charge?
             — TD prediction: NO (the band-aggregate ⟨Q⟩ is truncation_consistent=False, rel 0.43).
             — lizzi prediction (implicit in TD2-3 answer): YES (treats T_pivot as a band-edge quantity).

  VERDICT MAP:
    (FB-gap PASS) ∧ (FB-temp PASS)  ⟹  Reading A: pivot coth fully saturated ⟹ CF-AS-3 = POINT-per-functional + scheme-tag.
    (FB-gap PASS) ∧ (FB-temp FAIL)  ⟹  Reading B-narrowed: magnitude L_max-soft THROUGH T_pivot ⟹ CF-AS-3 = BAND + FB-extrapolation-tag on T_pivot.
    (FB-gap FAIL)                   ⟹  Reading B (strong): even the gap is soft ⟹ CF-AS-3 = BAND.
```

This is strictly sharper than lizzi's E2 gate (which tested only whether "the pivot `S_IC` saturates" as one object). The two-input form is what the substrate actually presents: an intensive FORM with a band-statistical DENOMINATOR-input. nazarewicz tests both legs; the workshop verdict is the conjunction.

**E(TD)2 — I AGREE with lizzi's E2/E3 register outputs and adopt them, with the two-input refinement folded in.** lizzi's E2 (CF-AS-3 = POINT iff the pivot observable Friedrich-Bär-saturates, BAND otherwise) and E3 (the two openness-sources (b-i) functional-choice and (b-ii) L_max-truncation are on orthogonal axes; tag the register magnitude with BOTH a scheme-tag and an L_max-tag) are correct and I adopt them. My E(TD)1 refines E2's saturation predicate into the two-input form: the (b-ii) L_max-tag on CF-AS-3 must specifically name `T_pivot` (the band-statistical input), not the pivot observable generically — because the gap input is conceded-saturated and only the temperature input carries the open softness. So the R3 register annotation becomes: magnitude = (value, scheme-tag for b-i, `T_pivot`-FB-saturation-tag for b-ii). If nazarewicz finds FB-temp PASS, the L_max-tag reads "saturated"; if FB-temp FAIL, it reads "BAND, FB-extrapolation on `T_pivot = 1/λ_pivot`."

**E(TD)3 — a cross-domain structural observation: this is the SAME band-aggregate object showing up on both sides of the partition, which is the campaign's "M_KK keystone seen from every consumer" pattern in miniature.** The `_cross-investigation-synthesis.md §3` headline is that the dimensionful weakness is "the M_KK keystone showing up in every downstream consumer." Here the analog is internal to A_s: the band-aggregate `⟨Q⟩_GGE = Σ|β_k|² = 59.8` appears in R1 (wrongly) as the numerator band-count, and (correctly, after D(TD)1) as the conserved charge fixing the denominator temperature `T_pivot = 1/λ_pivot`. The SAME truncation-sensitive object enters the magnitude through a different door than I claimed in R1. This is why naming the observable (lizzi's C3) was load-bearing — and why the R3 test must reach into the observable's INPUTS, not stop at its functional form. The substrate's own band-aggregate occupation is the truncation-carrier in BOTH readings; the only question is whether the GGE construction's Lagrange-multiplier normalization launders that softness out of the pivot coth (lizzi's A) or passes it through `T_pivot` (my narrowed B). That is a clean, computable, single-sub-input adjudication for nazarewicz.

---

## Round 3 — lizzi: Follow-up

### CONVERGENCE

transit-dynamics's R2 was a strong round: it conceded the two legs of Reading B that were category errors (C1: the canonical map is the intensive pivot coth, not the extensive band-count; C2: the ~2000× `K_sub` spread is a subtractive-excess artifact), and then — instead of folding — re-located the residual L_max question into a single, NAMED sub-input (`T_pivot`) via genuinely new register evidence (D(TD)1). That is exactly the right move, and it sharpens the workshop to its irreducible core. I record what the full exchange settled, and what my R3 query closes.

**C1(R3) — The workshop is settled on EVERYTHING except a single per-sub-input saturation question, and I name the resolution.** After R2 both sides agree on five things, and I re-affirm each as a CLOSED point of the exchange (not re-litigable in any downstream consumption):
- (i) the FLOOR `A_s ≥ A_s^{BD}` (`S_IC = 1 + 2n_k ≥ 1`, `proven_1097`) is PERMANENT and FUNCTIONAL-INDEPENDENT, on 3 orthogonal axes (reference-state / families-index η-form / dynamical-Bogoliubov) — out of scope, not adjudicated;
- (ii) the {β_k} are Floquet-frozen (WS-FLOQUET=DEAD), so frozen-in-TIME is conceded on both sides; the only open axis is L_max-CONVERGENCE (TD's frozen-in-time ≠ converged-in-truncation orthogonality, my C1 in R2);
- (iii) the canonical magnitude-bearing observable is the INTENSIVE UNIFIED-AS-79 pivot map `A_s = A_s^{BD}·S_IC(k_pivot)`, `S_IC(k_pivot) = coth(ω_{k_pivot}/2T_{k_pivot})` — NO band-sum (my D1, TD conceded C1); the extensive `Σ mult·|β_k|²` is the DISCARDED `n_pairs/2π² = +9.5 OOM` dump, not the A_s normalizer;
- (iv) S57/S62 is a SHAPE theorem (it certifies `n_s`/`α_s` intensiveness, an already-PASS result); it carries NEITHER side's normalization claim (TD's C3, my C2 in R2);
- (v) the FORM of the pivot coth is intensive and TIGHT (`K_sub = coth(x/2)` moves 1.0038× near the BD floor, 1.137× across the full S82 band table; the ~2000× is `(K_sub−1)` subtractive-excess proximity-to-floor, Sage-verified D3).

What remains is TD's D(TD)1 narrowed dissent: the pivot coth's FORM is intensive (conceded), but its TEMPERATURE DENOMINATOR `T_pivot = 1/(k_B λ_pivot)` is set through the GGE construction by a band-statistical Lagrange multiplier — so "does the magnitude saturate?" is a TWO-input question (gap `Δ_pivot` + temperature `T_pivot`), and TD predicts the temperature leg inherits the band-aggregate `⟨Q⟩_GGE = Σ|β_k|² = 59.8` softness (`truncation_consistent = False`). **I queried the register on exactly this (knowledge-MCP, GGE-temperature construction, session-38 / S74 NOETHER-CHAIN), and the answer RESOLVES the dissent in Reading A's favor — see QUESTIONS / answer to D(TD)1. My CLOSING position: the magnitude is a PHYSICAL DEGREE OF FREEDOM (Reading A), with a precise FB-saturation split that I commit to as the gate.**

**C2(R3) — I adopt TD's two-input Friedrich-Bär decomposition (E(TD)1) as the binding R3 charge to nazarewicz, and I agree it is strictly sharper than my E2.** TD is right that E1's "WHICH construction is canonical" reframing RELOCATES rather than ENDS the L_max question — once the pivot coth is certified canonical (settled), the L_max question lives in its inputs. The two per-sector sub-statements TD pre-registers — (FB-gap) do NEW (p,q) sectors at L_max+1 have Casimir floors `η_FB·√(C₂(p+q+1)+1)` above the pivot |λ| (so they do not shift `Δ_pivot`)? and (FB-temp) does `λ_pivot` stop moving as NEW in-band sectors add occupation to the conserved charge? — ARE the correct decomposition of the canonical observable, and the verdict is their conjunction. I accept the (FB-gap PASS)∧(FB-temp PASS)⟹Reading-A / (FB-gap PASS)∧(FB-temp FAIL)⟹Reading-B-narrowed-BAND / (FB-gap FAIL)⟹Reading-B-strong-BAND verdict map verbatim as nazarewicz's R3 Turn-B charge. My only addition (QUESTIONS / D(TD)1 answer) is that the register's GGE construction makes a SPECIFIC prediction for (FB-temp) that TD's R2 did not have in front of it.

**C3(R3) — the HK-AS-FLOOR register split lands in R3 Turn B regardless of the fork, with TD's two-axis (b)-annotation adopted, and I confirm the routing.** Both readings agree the *number* (3.02× / +0.86 / +1.455 OOM) is NOT permanent — so the register over-statement "A_s 3.02× permanent" (atlas-08 CF23, falsifier Row 8) splits into (a) FLOOR-PERMANENT-FUNCTIONAL-INDEPENDENT + (b) MAGNITUDE/FILTER-SCHEME-DEPENDENT-OPEN, with (b) carrying BOTH openness-source tags TD sharpened: (b-i) functional-choice freedom [scheme-tag] and (b-ii) L_max-truncation softness [the `T_pivot`-FB-saturation L_max-tag, per E(TD)2 — named to `T_pivot`, not the pivot observable generically, since the gap input is conceded-saturated]. Routing (per the dispatch charge, EFFECT in R3 Turn B): atlas-08 CF23 prose → the **designated writer** (capstone-hygiene Q3 reviewed patch, substrate-first frame `D_K → {α_k,β_k} → n_k → S_IC → A_s` preserved); falsifier-master-inventory **Row 8** → **mack-cosmic-bridge** (sole writer of the §7 falsifier surface per `feedback_mack-bridge-role.md`) — keep SCHEME-DEPENDENT on the magnitude + add the FUNCTIONAL-INDEPENDENT/PERMANENT floor sub-annotation (3 orthogonal axes). This is the NON-MATH deliverable (HK-AS-FLOOR, `_promotion-triage.md` Bucket-3 §A-eligible); it is decoupled from the math fork.

### DISSENT

My dissent is a SINGLE claim, and it is new — grounded in a register fact I pulled this round (knowledge-MCP, the session-38 / S74 GGE-temperature construction) that TD's R2 D(TD)1 did not have in front of it. TD's D(TD)1 Step 5 reads: "⟹ `T_pivot` inherits whatever L_max-dependence the band-aggregate charge constraint carries." **That inference does not follow from the GGE construction — it conflates the band-aggregate CHARGE SET `{I_k}` (which IS extensive) with the per-charge MULTIPLIER `λ_pivot` (which is per-mode).** This is the GGE-vs-thermal distinction, and it is the precise point where Reading B's narrowed leg breaks.

**D(LIZ-R3)1 — the GGE Lagrange multiplier is PER-MODE, not band-aggregate; `λ_pivot` depends only on `n_pivot`. Substitution chain (register-grounded, per `math-scripts.md`).** TD's D(TD)1 is right that `T_k^GGE = 1/(k_B λ_k)` and that the charge set is fixed by `⟨Q⟩_GGE = Σ_{k∈band}|β_k|² = 59.8` (S74 NOETHER-CHAIN — confirmed). But the canonical GGE stationarity condition (session-38-landau-hawking-workshop, the GGE-LAMBDA-38 construction) fixes EACH multiplier from its OWN charge's occupation:

```
Claim: "λ_pivot is a PER-MODE quantity (function of n_pivot alone), NOT a function of the band-aggregate ⟨Q⟩.
        Hence T_pivot's L_max-saturation is the saturation of a SINGLE per-mode occupation, not the band-count."

Step 1: ρ_GGE = (1/Z) exp(−Σ_k λ_k I_k)                          [session-45/cc-path-b Eq.2; one multiplier PER conserved charge I_k]
Step 2: GGE max-entropy stationarity ⟹ ∂S_vN/∂λ_k = 0 subject to ⟨I_k⟩ fixed, INDEPENDENTLY for each k
        (the {I_k} are a COMPLETE COMMUTING set — 8 Richardson-Gaudin integrals — so the constraints decouple)   [S38 W3; cc-path-b PB-10/PB-14]
Step 3: ⟹ λ_k = −ln(n_k / (1 − n_k))                            [session-38 canonical GGE-temperature form, VERBATIM: "λ_k = -ln(n_k/(1-n_k)) = -ln(0.999/0.001) = -6.9"]
Step 4: substitute k = pivot:  λ_pivot = −ln(n_pivot / (1 − n_pivot))    — a function of n_pivot ALONE
Step 5: T_pivot = 1/(k_B λ_pivot) = −1 / (k_B · ln(n_pivot/(1−n_pivot)))  — depends ONLY on the pivot-mode occupation
Conclusion: T_pivot is fixed by n_pivot, NOT by ⟨Q⟩ = Σ_k n_k. The band-aggregate enters the GGE only as the SUM
            of the per-charge constraints; each multiplier λ_k is conjugate to its OWN I_k and is a per-mode function
            of n_k. So T_pivot's saturation = n_pivot's saturation — a SINGLE per-mode occupation, same saturation
            class as Δ_pivot, NOT the band-count's.
```

**The structural reason TD's Step 5 over-reaches: a GGE is exactly the state in which the aggregate does NOT set the per-mode multiplier.** In a THERMAL state, there is ONE β fixed by the TOTAL energy (band-aggregate) — and TD's intuition would be correct there. But the entire content of "GGE, not thermal" (the S38 paradigm, `proven` "never thermalizes", 8 conserved charges) is that each conserved charge `I_k` gets its OWN multiplier, decoupled by the completeness of the commuting Richardson-Gaudin set. That is WHY the GGE is non-thermal: it does not collapse the 8 per-mode multipliers into one aggregate β. TD's D(TD)1 inadvertently re-thermalizes the GGE — it treats `λ_pivot` as if it were the single thermal β fixed by `⟨Q⟩`, when the register's construction makes it the per-mode `−ln(n_pivot/(1−n_pivot))`. The band-aggregate `⟨Q⟩ = 59.8` is the SUM `Σ_k ⟨I_k⟩`; adding MORE in-band modes at L_max+1 adds MORE constraints (more `λ_k`'s), it does NOT shift the EXISTING `λ_pivot` (whose `n_pivot` is unchanged by the arrival of new high-Casimir sectors elsewhere in the band).

**Consequence: (FB-temp) reduces to (FB-occupation-of-the-pivot-mode), which is the SAME saturation class as (FB-gap).** TD's E(TD)1 verdict map stands as the protocol, but the register collapses TD's predicted asymmetry (gap saturates / temperature does not): both inputs to the pivot coth — `Δ_pivot` (a single eigenvalue) and `T_pivot = 1/(k_B λ_pivot)` with `λ_pivot = −ln(n_pivot/(1−n_pivot))` (a single per-mode occupation) — are per-mode quantities of the SAME low-Casimir pivot mode. NEW (p,q) sectors at L_max+1 with Casimir floors above the pivot |λ| change neither `Δ_pivot` NOR `n_pivot`. **So my prediction for BOTH legs is PASS (saturated), and the asymmetry TD's D(TD)2 rests on — "the gap saturates, the temperature is tied to the un-saturated band-aggregate" — dissolves once the per-charge multiplier structure is read off the register.** This is precisely the lizzi-signature discriminator: the GGE construction is a per-mode (intensive) functional of the occupations, NOT a band-aggregate (extensive) functional — so what is truncation-INDEPENDENT (the per-mode pivot coth) is structural, and the only truncation-soft object (the EXTENSIVE charge SUM `⟨Q⟩`, and the discarded `ρ_relic` dump) is the one neither input to the canonical A_s depends on.

**Honest scope of the dissent (the regime where TD's D(TD)1 would WIN).** I am scrupulous about what would flip this: IF the GGE construction were such that `λ_pivot` is determined by a band-aggregate NORMALIZATION (e.g., a global chemical-potential-like constraint `Σ_k n_k(λ) = ⟨Q⟩` solved for a SINGLE λ shared across modes — the thermal-like closure), THEN TD's Step 5 would hold and `T_pivot` would inherit the band-count softness. The register does NOT support that closure: it gives 8 SEPARATE multipliers, each `λ_k = −ln(n_k/(1−n_k))`, per-mode. The one computation that would decide this against me is a demonstration that the framework's GGE-LAMBDA construction actually solves a SHARED-λ aggregate constraint rather than 8 decoupled per-charge ones — and that is a falsifiable per-sector statement nazarewicz can confirm (does `λ_pivot` move when a new high-Casimir in-band sector is added at L_max+1, holding `n_pivot` fixed? Register prediction: NO). I commit to that as the decisive (FB-temp) test (QUESTIONS).

### EMERGENCE

**E(LIZ-R3)1 — the dispute has converged to a SINGLE, register-decidable structural question: is the GGE multiplier per-charge (intensive) or shared-aggregate (extensive)? — and the register answers per-charge.** This is the cleanest possible terminus for a spectral-functional adjudication. R1 posed it as "which side of the intensive/extensive partition does the magnitude live on" (two competing intuitions about one object). R2 (TD's D1 concession + my D1) reframed it as "which CONSTRUCTION is canonical" (a documentary fact: the pivot map). R3 (TD's D(TD)1 + my D(LIZ-R3)1) has now localized it to a SINGLE sub-input's functional STRUCTURE: is `T_pivot`'s denominator `λ_pivot` a per-mode function of `n_pivot` (intensive — my register reading) or a shared-aggregate function of `⟨Q⟩` (extensive — TD's D(TD)1)? This is no longer a clash of intuitions; it is a fact about the GGE-LAMBDA-38 construction, decidable by reading the stationarity condition (session-38: per-charge, `λ_k = −ln(n_k/(1−n_k))`) and confirmable per-sector by nazarewicz. The whole workshop reduces to ONE structural property of one canonical construction — which is the methodologically-ideal endpoint.

**E(LIZ-R3)2 — a cross-domain structural insight: the GGE per-charge multiplier structure is the SAME mechanism that makes `n_s` intensive (S57/S62), now seen on the AMPLITUDE.** The Mode-Independent Occupation Theorem (S57/S62) makes the tilt geometric-not-occupational precisely BECAUSE the GGE is per-mode: each mode carries its own `|β_k|²` and its own `λ_k`, so the SHAPE-ratio between two modes is a ratio of per-mode quantities (intensive). The same per-charge structure now makes the pivot coth's TEMPERATURE per-mode (D(LIZ-R3)1) — so the AMPLITUDE's intensiveness and the SHAPE's intensiveness have the SAME structural origin: the GGE does not collapse to a single thermal β. This is the unifying observation TD's E(TD)3 reached for ("the same band-aggregate object showing up on both sides") — but inverted: it is NOT the same EXTENSIVE object entering through two doors; it is the same per-mode GGE STRUCTURE making BOTH the shape AND the amplitude intensive. The band-aggregate `⟨Q⟩ = 59.8` is the SUM of the per-charge constraints, never the determinant of any single multiplier. TD's "M_KK keystone seen from every consumer" pattern has an exact analog here, but the keystone is the per-charge GGE structure, not the aggregate count.

**E(LIZ-R3)3 — the CF-AS-3 pin FORM is now decidable BEFORE nazarewicz runs, conditional on the register reading, and I sharpen E2/E(TD)1 to a single falsifiable predicate.** Folding D(LIZ-R3)1 into TD's two-input verdict map: since the register makes BOTH (FB-gap) and (FB-temp) per-mode pivot-occupation saturation questions, the register-grounded prediction is (FB-gap PASS)∧(FB-temp PASS) ⟹ **CF-AS-3 records a POINT-per-functional with a scheme-tag (Reading A)**, NOT a Friedrich-Bär band. The dominant width is then (b-i) functional-freedom (the full −3.02→+6.008 OOM cross-functional spread, the spectral-functional acting as a physical d.o.f.), with (b-ii) truncation-softness sub-dominant (per-mode pivot inputs L-saturated). The single falsifiable predicate that gates the FORM: **does `λ_pivot = −ln(n_pivot/(1−n_pivot))` shift when a new high-Casimir in-band (p,q) sector is added at L_max+1, holding `n_pivot` fixed?** Register prediction: NO (per-charge multiplier; the new sector adds a new `λ_{k'}`, not a shift to `λ_pivot`) ⟹ POINT. If nazarewicz finds YES (the GGE closure is shared-aggregate after all) ⟹ BAND-with-FB-extrapolation-tag-on-`T_pivot`. Either way CF-AS-3 carries BOTH tags (scheme-tag for b-i, `T_pivot`-FB-saturation L_max-tag for b-ii) per my standing "every gate states (value, scheme_tag, convention_tag, L_max_tag)" discipline (E3, adopted by TD's E(TD)2).

### QUESTIONS

transit-dynamics's R2 folded most of Reading B into concessions (C1–C3) and posed its residual case as the narrowed D(TD)1–3 + the E(TD)1 two-input charge. I answer the residual directly, then state my CLOSING position for transit-dynamics to tabulate in the Verdict + Wrap-Up.

**Answer to D(TD)1 (the `T_pivot` band-aggregate claim) — RESOLVED in Reading A's favor by the register; `λ_pivot` is per-charge, not shared-aggregate.** Your D(TD)1 is the sharpest form Reading B reached, and it deserved a register check rather than an assertion. I ran it (knowledge-MCP, GGE-temperature construction). The canonical form is `ρ_GGE = (1/Z)exp(−Σ_k λ_k I_k)` with `λ_k = −ln(n_k/(1−n_k))` (session-38, VERBATIM) — ONE multiplier per conserved charge, each a per-mode function of its OWN occupation. So `λ_pivot = −ln(n_pivot/(1−n_pivot))` depends on `n_pivot` ALONE, NOT on `⟨Q⟩ = Σn_k = 59.8` (D(LIZ-R3)1). Your Step 5 ("`T_pivot` inherits the band-aggregate softness") inadvertently re-thermalizes the GGE — it treats `λ_pivot` as a single thermal β fixed by total charge, when the register's non-thermal GGE gives 8 decoupled per-charge multipliers. The band-aggregate is the SUM of the constraints; adding in-band sectors at L_max+1 adds NEW `λ_{k'}`'s, it does not shift the existing `λ_pivot`. **So `T_pivot` is per-mode-saturated in the SAME class as `Δ_pivot` — your predicted gap/temperature asymmetry (D(TD)2) dissolves.** The honest scope: if the framework's GGE-LAMBDA construction actually solved a SHARED-λ aggregate constraint, you would win — but the register gives per-charge, and that is the decisive (FB-temp) test for nazarewicz.

**Answer to E(TD)1 / D(TD)3 (my Friedrich-Bär prediction for BOTH inputs, as per-sector eigenvalue-floor statements).** I adopt your two-input decomposition verbatim as the R3 Turn-B charge, and I state my prediction for each leg as a per-(p,q)-sector Casimir-floor statement (functional-INDEPENDENT, so it adjudicates cleanly):
- **(FB-gap): PASS (saturated).** `Δ_pivot` is a single low-Casimir eigenvalue (B1 acoustic = 0.4643 M_KK, B3 = 0.178 M_KK). NEW sectors at L_max+1 have Casimir floors `η_FB·√(C₂(p+q+1)+1)` ABOVE the pivot |λ|, so they do not shift `Δ_pivot`. (You accepted this half in D(TD)2.)
- **(FB-temp): PASS (saturated) — my register-grounded prediction, against your D(TD)2.** `T_pivot = 1/(k_B λ_pivot)`, `λ_pivot = −ln(n_pivot/(1−n_pivot))`. The decisive per-sector test: **does `λ_pivot` move when a NEW high-Casimir in-band sector is added at L_max+1, holding `n_pivot` fixed?** Register prediction: NO — the new sector contributes a NEW per-charge multiplier `λ_{k'}`, not a shift to `λ_pivot` (per-charge structure, D(LIZ-R3)1). `n_pivot` is a per-mode occupation unchanged by sectors arriving elsewhere in the band. So `T_pivot` saturates.
- **Conjunction: (FB-gap PASS)∧(FB-temp PASS) ⟹ Reading A** (pivot coth fully saturated; magnitude is a physical d.o.f.). The one finding that flips it to Reading-B-narrowed-BAND: nazarewicz shows `λ_pivot` DOES shift with new in-band sectors (a shared-aggregate GGE closure). That is the clean, single, falsifiable arbiter.

**CLOSING POSITION (for transit-dynamics to tabulate in the Verdict + Wrap-Up).**

1. **The A_s MAGNITUDE epistemic type is (A) a PHYSICAL DEGREE OF FREEDOM — the lizzi-signature outcome — with a PRECISE FB-saturation SPLIT, not an unconditional claim.** The canonical magnitude-bearing observable is the intensive UNIFIED-AS-79 pivot coth `A_s = A_s^{BD}·S_IC(k_pivot)` (settled, TD conceded C1). Both of its inputs — gap `Δ_pivot` and temperature `T_pivot = 1/(k_B λ_pivot)` — are per-mode quantities of the low-Casimir pivot mode (D(LIZ-R3)1, register-grounded). Its scheme-dependence ACROSS spectral functionals (cutoff/zeta/impulse-quench/near-floor-DOS, the −3.02→+6.008 OOM spread) is the spectral functional acting as an UNPINNED PHYSICAL d.o.f. — the same freedom that sign-flips the a₀/a₂ CC ratio (CV-1↔CV-4). **The split, stated precisely:** the magnitude is Reading-A physical-d.o.f. CONDITIONAL on the (FB-temp) per-sector test PASSing (register prediction: PASS); IF nazarewicz finds the GGE closure is shared-aggregate (FB-temp FAIL), the magnitude is Reading-B-narrowed (L_max-soft through `T_pivot`) and CF-AS-3 records a BAND. NOT (C) scheme-chosen — the functional spread is a physical d.o.f. (pinned by the physically-correct impulse-quench functional to a sub-2-OOM over-production band), not a free knob.

2. **The FLOOR is PERMANENT and out-of-scope** (`A_s ≥ A_s^{BD}`, `S_IC ≥ 1`, `proven_1097`, 3 orthogonal axes — reference-state / families-index η-form / dynamical-Bogoliubov). Not adjudicated; the fixed boundary against which the magnitude question was posed.

3. **The HK-AS-FLOOR split (NON-MATH, EFFECT in R3 Turn B regardless of the fork).** (a) FLOOR → PERMANENT, FUNCTIONAL-INDEPENDENT; (b) MAGNITUDE/FILTER → SCHEME-DEPENDENT-OPEN, carrying BOTH openness-source tags: (b-i) functional-choice freedom [scheme-tag] and (b-ii) L_max-truncation softness [the `T_pivot`-FB-saturation L_max-tag, named to `T_pivot` per E(TD)2]. Routing: atlas-08 CF23 prose → **designated writer** (capstone-hygiene Q3 reviewed patch); falsifier-master-inventory **Row 8** → **mack-cosmic-bridge** (sole §7-surface writer) — keep SCHEME-DEPENDENT magnitude + add the FUNCTIONAL-INDEPENDENT/PERMANENT floor sub-annotation.

4. **The MATH carry-forwards this verdict GATES (both named per the dispatch charge):**
   - **CF-AS-3** (session-promote the impulse-quench A_s canonical pin) — the verdict sets the FORM: register prediction (FB-temp PASS) ⟹ POINT-per-functional + scheme-tag (Reading A); FB-temp FAIL ⟹ BAND + FB-extrapolation-tag-on-`T_pivot` (Reading B). Either way carries BOTH (scheme-tag, `T_pivot`-L_max-tag). This is the load-bearing reason the epistemic-type verdict is not academic — it sets the pin's FORM. The decisive sub-input is `λ_pivot`'s per-sector stability (the nazarewicz FB-temp test).
   - **CF-AS-2** (A_s upper-edge greybody dynamical near-horizon resonance scan) — the INV12-W3-4 fitted-`0.512` exit-filter leg (∫Γ_derived = 0.036 vs fitted 0.512, 14× short, OPEN). GATED on this verdict being (A) or (B): the filter scan presupposes the magnitude is a real physical object worth bounding; if the verdict were (C) scheme-chosen the scan is moot. Since the closing position is (A)-with-split, CF-AS-2 is LIVE. This is the separate upper-edge question my LIZ1 caveat flagged as undischarged — NOT part of this floor-vs-magnitude adjudication.

5. **The binding R3 Turn-B tie-breaker for nazarewicz (Friedrich-Bär, two-input form, E(TD)1).** Test the TWO per-sector sub-statements on the canonical pivot coth: (FB-gap) does `Δ_pivot` saturate? (FB-temp) does `λ_pivot = −ln(n_pivot/(1−n_pivot))` stop moving as NEW in-band sectors add to the GGE charge set, holding `n_pivot` fixed? My register-grounded prediction: BOTH PASS ⟹ Reading A. The verdict is the conjunction.

---

## Round 3 — transit-dynamics: Cross-Synthesis

This is the final turn. I queried the register directly on the one fact that decides the residual dispute — the GGE Lagrange-multiplier structure — exactly as I queried it before conceding D1 in R2 (knowledge-MCP, not lizzi's assertion taken on faith). The verdict follows from that query plus the named Friedrich-Bär tie-breaker (nazarewicz's axis), and I state it without inflation: my R2 narrowed dissent is register-refuted, the workshop converges to Reading A on the magnitude's epistemic type, and the convergence is conditioned on a precise, falsifiable Friedrich-Bär two-input saturation split that both agents pre-registered.

### CONVERGENCE

**C(R3)1 — I CONCEDE D(LIZ-R3)1 on the merits: the GGE Lagrange multiplier is PER-CHARGE (per-mode), NOT shared-aggregate. My R2 D(TD)1 narrowed dissent is register-refuted, and I retract it.** This is the decisive move of the turn and I record exactly what I verified. lizzi's D(LIZ-R3)1 claims `λ_pivot = −ln(n_pivot/(1−n_pivot))` is a per-mode function of `n_pivot` alone, so my R2 inference (D(TD)1 Step 5: "`T_pivot` inherits whatever L_max-dependence the band-aggregate charge constraint carries") inadvertently re-thermalizes the GGE. I pulled the GGE-temperature construction from the knowledge MCP rather than accept the claim, and the register is unambiguous on three independent entries:

```
register-verified (knowledge-MCP, GGE-temperature construction):

  (i)   λ_k = −ln(n_k / (1 − n_k))           [session-38-landau-hawking-workshop, VERBATIM: "= −ln(0.999/0.001) = −6.9"]
  (ii)  λ_k = −ln(p_k),  p_k = |ψ_pair[k]|²   [session-39-results-workingpaper, VERBATIM — per-pair-amplitude form]
  (iii) ρ_GGE = (1/Z) exp(−Σ_k λ_k I_k)       [session-45-quicklook, Eq. 2; "the 8 Richardson-Gaudin integrals {I_k}
                                                are conserved quantities ... each integral is a conserved quantum number"]
```

The substitution chain is settled by the register, not by either agent's intuition:

```
Claim (now CONCEDED): "T_pivot is per-mode-saturated, the SAME class as Δ_pivot — my R2 gap/temperature asymmetry dissolves."

Step 1: ρ_GGE = (1/Z) exp(−Σ_k λ_k I_k)                       [register (iii); ONE multiplier PER conserved charge]
Step 2: the 8 Richardson-Gaudin {I_k} are a COMPLETE COMMUTING set ⟹ the max-entropy constraints DECOUPLE per-k   [S38; atlas-04 T2 PROVEN]
Step 3: λ_pivot = −ln(n_pivot/(1−n_pivot))                    [register (i); a function of n_pivot ALONE]
Step 4: T_pivot = 1/(k_B λ_pivot) = −1/(k_B ln(n_pivot/(1−n_pivot)))   [substitute; depends ONLY on n_pivot]
Step 5: the band-aggregate ⟨Q⟩ = Σ_k n_k = 59.8 is the SUM of the per-charge constraints; adding NEW in-band sectors
        at L_max+1 adds NEW multipliers {λ_{k'}}, it does NOT shift the EXISTING λ_pivot (n_pivot unchanged)
Conclusion: T_pivot's L_max-saturation = n_pivot's saturation = a SINGLE per-mode occupation, the SAME saturation
            class as the gap Δ_pivot. My D(TD)1 Step 5 treated λ_pivot as a single thermal β fixed by ⟨Q⟩;
            the register's NON-THERMAL GGE gives 8 decoupled per-charge multipliers. I retract D(TD)1.
```

**The structural reason my R2 dissent failed is exactly the "GGE, not thermal" paradigm I am the TD-ledger author of, applied against my own claim.** In a thermal state ONE β is fixed by the total (band-aggregate) energy, and a per-mode observable would inherit aggregate softness — my D(TD)1 intuition would be correct THERE. But the entire content of "never thermalizes" (the S38/S39 paradigm, atlas-04 T2 PROVEN, the 8 conserved Richardson-Gaudin charges with three distinct branch multipliers λ_B1=2.771 / λ_B2=1.459 / λ_B3=6.007 "reflecting SU(3) branch structure", atlas-07 PERMANENT) is that each charge `I_k` gets its OWN multiplier, decoupled by the completeness of the commuting set. That non-collapse-to-a-single-β IS the non-thermality. I let my R1 band-count intuition survive into R2 in the denominator; the register shows the GGE construction launders the aggregate softness OUT of the per-mode `λ_pivot`. lizzi's D(LIZ-R3)1 is correct, register-grounded, and I concede it cleanly — the same concession structure as the WS-FLOQUET depth mis-assignment (h_par vs q, ~607×) and the R2 D1 concession (pivot coth vs band-count). Three concessions across this campaign, each register- or Sage-verified before I made it.

**C(R3)2 — both inputs to the canonical pivot coth are per-mode quantities of the low-Casimir pivot mode, and the Friedrich-Bär theorem certifies that class saturates at L_max ≥ 10. My (FB-temp) R2 prediction (FAIL) is replaced by PASS.** Folding C(R3)1 into the two-input verdict map I authored in R2 (E(TD)1): the canonical magnitude-bearing observable is `coth(Δ_pivot/2T_pivot)` with `Δ_pivot` a single low-Casimir eigenvalue (B1 acoustic = 0.4643 M_KK, B3 = 0.178 M_KK) and `T_pivot = 1/(k_B λ_pivot)`, `λ_pivot = −ln(n_pivot/(1−n_pivot))` a function of the single per-mode occupation `n_pivot`. The Friedrich-Bär structural-saturation theorem (S87 W11-2/W11-3, `proven_908` / `proven_1827` / `proven_182`; PROVEN, "analytically certifies bottom-K invariance for ALL L_max ≥ 10") bounds NEW-sector contributions by the per-sector predicate `η_FB(p,q) = |λ|_min(p,q)/√(C₂(p,q)+1)`: NEW (p,q) sectors at L_max+1 have Casimir floors `η_FB·√(C₂(p+q+1)+1)` ABOVE the low-Casimir pivot |λ|, so they shift NEITHER `Δ_pivot` NOR `n_pivot`. Both legs saturate. My R2 (FB-temp) prediction of FAIL rested on `T_pivot` being charge-conjugate to a band-aggregate; once C(R3)1 establishes per-charge structure, (FB-temp) becomes (FB-occupation-of-the-pivot-mode), which is the SAME low-Casimir-eigenvalue saturation class as (FB-gap). **My corrected prediction: (FB-gap PASS) ∧ (FB-temp PASS) ⟹ Reading A** — the verdict map I pre-registered, evaluated with the per-charge fact in hand.

**C(R3)3 — I adopt lizzi's E1→E(TD)1→E(LIZ-R3)1 reframing chain and confirm its terminus: the workshop reduced to ONE register-decidable structural property, and the register answered it.** The dispute walked a clean monotone refinement across three rounds: R1 — "which side of the intensive/extensive partition does the magnitude live on?" (two competing intuitions about one object); R2 — "which CONSTRUCTION is canonical?" (a documentary fact, the UNIFIED-AS-79 pivot map, which I conceded as intensive-in-form, C1-R2); R3 — "is the GGE multiplier per-charge (intensive) or shared-aggregate (extensive)?" (a single structural property of the GGE-LAMBDA-38 construction). Each round localized the open axis further and replaced an intuition-clash with a register fact. The R3 terminus is the methodologically-ideal one: a single named structural property, decidable by reading the stationarity condition (`λ_k = −ln(n_k/(1−n_k))`, per-charge) and confirmable per-sector by nazarewicz's Friedrich-Bär test. I confirm lizzi's E(LIZ-R3)1 is the correct characterization of where the exchange landed.

**C(R3)4 — the HK-AS-FLOOR register split lands in R3 Turn B regardless of the fork, with the two-axis (b)-annotation, and I confirm the routing per the dispatch charge.** Both readings agree the *number* (3.02× / +0.86 / +1.455 OOM) is NOT permanent — Reading A says it is a physical d.o.f. scheme-dependent across functionals; Reading B (now conceded-closed) said truncation-soft. So the register over-statement "A_s 3.02× permanent" (atlas-08 CF23; falsifier Row 8) splits into (a) FLOOR-PERMANENT-FUNCTIONAL-INDEPENDENT + (b) MAGNITUDE/FILTER-SCHEME-DEPENDENT-OPEN, with (b) carrying BOTH openness-source tags (b-i) functional-choice freedom [scheme-tag] and (b-ii) L_max-truncation softness [the `T_pivot`-FB-saturation L_max-tag, named to `T_pivot` per E(TD)2 — the gap input is conceded-saturated, so the L_max-tag rides the temperature input alone]. This is the NON-MATH deliverable, decoupled from the math fork (`_promotion-triage.md` HK-AS-FLOOR, Bucket-3 Q3). It is EFFECTED in this turn via routing (designated writer + mack), enumerated in the Wrap-Up.

### DISSENT

**No residual dissent on the math fork.** After C(R3)1 I hold no surviving disagreement with Reading A on the magnitude's epistemic type. The two legs of Reading B I carried into R2 are both conceded-closed: (i) the extensive-band-count leg (R2 C1 — the canonical map is the intensive pivot coth, not `Σ mult·|β_k|²`; the band-count is the discarded `n_pairs/2π² = +9.5 OOM` dump); (ii) the temperature-input leg (R3 C(R3)1 — `T_pivot` is per-mode via the per-charge `λ_pivot`, not band-aggregate). The `~2000× K_sub` premise was dissolved in R2 (C2 — subtractive-excess artifact, Sage-verified). Reading B's regime of validity is now explicit and EMPTY at the canonical L_max: it would have held IFF the GGE-LAMBDA construction solved a shared-λ aggregate constraint (a thermal-like closure), and the register gives the non-thermal per-charge construction instead.

**One reserved-not-dissenting boundary (the honest caveat, carried per the dispatch charge — and it is NOT this workshop's axis).** Reading A on the magnitude epistemic type is conditioned on the Friedrich-Bär (FB-temp) per-sector test PASSing — and I authored that conditional (E(TD)1) precisely so the verdict adjudicates rather than asserts. The register PREDICTS PASS (per-charge multiplier, C(R3)1), but the per-sector eigenvalue-floor computation itself (does `λ_pivot` move when a NEW high-Casimir in-band sector is added at L_max+1, holding `n_pivot` fixed? register prediction: NO) is nazarewicz's named cross-review compute, not yet run. This is a CONDITIONAL on a named, falsifiable, register-predicted test — NOT a live dissent. Separately, and on a DIFFERENT axis from this floor-vs-magnitude adjudication: the INV12-W3-4 fitted-greybody-`0.512` exit-filter leg (∫Γ_derived = 0.036 vs fitted 0.512, 14× short) remains OPEN and undischarged. That is the upper-EDGE / FILTER question, routed to CF-AS-2, flagged in LIZ1's caveat and my TD1 concession from R1. It does not touch the magnitude's epistemic type; "Reading A wins on the magnitude" does not discharge it.

### EMERGENCE

**E(R3)1 — the unifying structural observation: the per-charge GGE multiplier is the SINGLE mechanism making BOTH the SHAPE (S57/S62) and the AMPLITUDE intensive — and it INVERTS my R2 E(TD)3 cross-domain read.** In R2 E(TD)3 I reached for a "same band-aggregate object entering through two doors" pattern (the `⟨Q⟩ = 59.8` as numerator band-count in R1, then denominator temperature in R2 D(TD)1). lizzi's E(LIZ-R3)2 inverts it correctly, and I adopt the inversion: it is NOT the same EXTENSIVE object entering twice — it is the same per-mode GGE STRUCTURE making both observables intensive. The Mode-Independent Occupation Theorem (S57/S62) makes the tilt geometric-not-occupational BECAUSE the GGE is per-mode (each mode carries its own `|β_k|²` and its own `λ_k`, so the SHAPE-ratio between two modes is a ratio of per-mode quantities — intensive); the same per-charge structure makes the AMPLITUDE's temperature input per-mode (C(R3)1). The shape-intensiveness and the amplitude-intensiveness have ONE structural origin: the GGE does not collapse to a single thermal β. The band-aggregate `⟨Q⟩ = 59.8` is the SUM of the per-charge constraints, never the determinant of any single multiplier. This is the cleaner statement; my R2 "M_KK keystone seen from every consumer" analog was the right instinct on the wrong object — the keystone here is the per-charge GGE structure (intensive), not the aggregate count (extensive).

**E(R3)2 — the substrate-physics content of the verdict, stated in the transit-dynamics frame.** A_s is the squeezing modulus of the post-transit GGE-relic acoustic interference pattern of the D_K(τ_fold) eigenvalue spectrum. The arrow is unchanged: `D_K eigenvalues → impulse-quench Bogoliubov {α_k, β_k} → per-mode occupation n_k=|β_k|² → per-mode GGE multiplier λ_k=−ln(n_k/(1−n_k)) → pivot squeezing modulus S_IC(k_pivot)=coth(Δ_pivot/2T_pivot) → relic amplitude A_s`. Three properties of this arrow are now settled on BOTH agents' axes: (1) the FLOOR `S_IC ≥ 1` is forced by `n_k ≥ 0` (permanent, 3-axis); (2) the {β_k} are Floquet-frozen in TIME (WS-FLOQUET=DEAD, h_par=8.3e-4 narrow-regime, fraction_resonance=0 EXACT); (3) BOTH inputs to the pivot coth (`Δ_pivot`, `T_pivot=1/k_Bλ_pivot`) are per-mode quantities of the low-Casimir pivot mode, Friedrich-Bär-saturated at L_max ≥ 10. The magnitude is therefore a genuine intensive amplitude whose ONLY residual openness is functional-choice freedom (the −3.02→+6.008 OOM cross-functional spread — cutoff/zeta/impulse-quench/near-floor-DOS — the spectral functional acting as a physical degree of freedom), which is the lizzi-signature outcome and which I co-certify from the Bogoliubov side. The substrate's per-mode GGE structure is what makes this an intensive physical d.o.f. rather than an un-converged extensive band-count.

**E(R3)3 — a forward structural note: the FB-temp per-sector test is a clean, single-sub-input arbiter that the campaign should preserve as a template.** The two-input Friedrich-Bär decomposition (E(TD)1, adopted by lizzi C2-R3) is sharper than testing "does the pivot observable saturate" as one object — it separates a single-eigenvalue saturation (the gap) from a charge-conjugate saturation (the temperature), and the register collapses the latter to the former via the per-charge structure. The general lesson for any future GGE-relic observable adjudication: when an observable is intensive-in-FORM but has a GGE-Lagrange-multiplier input, the L_max question lives in whether that multiplier is per-charge (collapses to single-mode saturation) or shared-aggregate (inherits band-count softness) — a single register-readable structural property. This is the transit-dynamics-side generalization of lizzi's E(LIZ-R3)1, and it is reusable beyond A_s (any S_IC, any coth-form squeezing modulus on the GGE relic).

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | A_s magnitude epistemic type (physical-d.o.f. vs truncation-artifact) | LIZ1, TD1 | **Converged** (Reading A, conditional) | Magnitude is a PHYSICAL d.o.f. (Reading A); canonical observable is the INTENSIVE UNIFIED-AS-79 pivot coth `A_s = A_s^{BD}·coth(Δ_pivot/2T_pivot)` (TD conceded R2-C1); both inputs per-mode of the low-Casimir pivot mode; residual openness is functional-choice freedom (−3.02→+6.008 OOM cross-functional spread), NOT truncation. Conditional on (FB-temp) PASS (register predicts PASS). NOT (C) scheme-chosen. |
| 2 | WS-FLOQUET dependency resolution (Reading-A leg preserved) | LIZ1, ws-floquet | **Converged** | WS-FLOQUET=DEAD settled the TIME axis (h_par=8.3e-4 narrow-regime, fraction_resonance=0 EXACT) — {β_k} not secularly re-pumped. Frozen-in-TIME ≠ converged-in-L_max are orthogonal (TD's R1 distinction, lizzi's R2-C1); the L_max axis was the genuine open question, now resolved per #1. The Reading-A leg is preserved (relic frozen, not a Floquet artifact); Reading B was NOT a re-pumping claim. |
| 3 | Friedrich-Bär truncation-saturation tie-break (nazarewicz) | cross | **Converged** (two-input form; register-predicted PASS∧PASS) | The tie-breaker is a TWO-INPUT per-sector eigenvalue-floor test on the pivot coth (TD R2 E(TD)1, adopted by lizzi R3-C2): (FB-gap) does `Δ_pivot` saturate? (FB-temp) does `λ_pivot=−ln(n_pivot/(1−n_pivot))` stop moving as NEW in-band sectors join the GGE charge set, holding `n_pivot` fixed? Both predicted PASS via Friedrich-Bär theorem (`proven_908`, certifies bottom-K invariance ∀ L_max≥10) + per-charge GGE structure. Verdict = conjunction ⟹ Reading A. nazarewicz's per-sector compute = the named arbiter (CF-AS-3 sub-input). |
| 4 | HK-AS-FLOOR floor-vs-magnitude split (mack Row-8) | LIZ2 | **Converged** (NON-MATH; EFFECTED via routing) | Register over-statement "A_s 3.02× permanent" SPLITS: (a) FLOOR `A_s≥A_s^{BD}` (`S_IC≥1`, `proven_1097`) = PERMANENT, FUNCTIONAL-INDEPENDENT, 3 orthogonal axes (reference-state / families-index η-form / dynamical-Bogoliubov); (b) MAGNITUDE/upper-edge FILTER = SCHEME-DEPENDENT, OPEN, carrying BOTH (b-i) functional-choice [scheme-tag] + (b-ii) `T_pivot`-FB-saturation [L_max-tag]. Only the inequality `>1` deserves "permanent". Both readings agree the NUMBER is not permanent ⟹ split lands regardless of the fork. Routed: atlas-08 CF23 → designated writer; falsifier Row 8 → mack. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**Honest summary of the verdict (the SPLIT the dispatch charge anticipated).** The FLOOR is Converged-PERMANENT (out of scope, 3 axes — not adjudicated). The MAGNITUDE epistemic type Converged to **(A) physical degree of freedom**, conditional on a register-predicted Friedrich-Bär (FB-temp) PASS that is nazarewicz's named cross-review compute. The convergence is genuine, not a stalemate: across three rounds transit-dynamics conceded the band-count leg (R2-C1), the `K_sub`-spread premise (R2-C2), and the temperature-input leg (R3-C(R3)1) — each on the merits after a register/Sage check, leaving no surviving dissent on the math fork. The HK-AS-FLOOR register split is the NON-MATH deliverable, EFFECTED in-turn via routing. The two openness sources are orthogonal (functional-choice ⊥ L_max-truncation), and the verdict pins (b-i) functional-freedom as the dominant width FOR THE CANONICAL PIVOT MAP ⟹ CF-AS-3 records a POINT-per-functional + scheme-tag (Reading A form), conditional on the FB-temp PASS; FB-temp FAIL ⟹ BAND + FB-extrapolation-tag-on-`T_pivot`.

## Remaining Open Questions

These are the genuine math/physics items the verdict GATES or leaves open. Both gated CFs (CF-AS-2, CF-AS-3) are math; they are specified in the Carry-Forward Computations block below with 4-field specs.

1. **The Friedrich-Bär (FB-temp) per-sector compute itself (nazarewicz's named cross-review).** The verdict converges to Reading A CONDITIONAL on (FB-temp) PASS. The register PREDICTS PASS (per-charge `λ_pivot = −ln(n_pivot/(1−n_pivot))`, C(R3)1), but the per-sector eigenvalue-floor computation is not yet run: does `λ_pivot` move when a NEW high-Casimir in-band (p,q) sector is added at L_max+1, holding `n_pivot` fixed? This is the decisive sub-input of CF-AS-3 (the pin FORM POINT-vs-BAND turns on it) and is the single falsifiable arbiter both agents pre-registered. Register prediction: NO (per-charge multiplier; new sector adds a NEW `λ_{k'}`, not a shift to `λ_pivot`) ⟹ POINT.

2. **The A_s upper-edge greybody FILTER (CF-AS-2) — a DIFFERENT axis from this adjudication.** The INV12-W3-4 fitted-greybody-`0.512` leg remains OPEN: the substrate-derived ∫Γ_derived = 0.036 sits 14× below the fitted 0.512, and the two substrate barrier readings (V_0 = κ_exit²/4 vs V_0 = T_compound²) BRACKET 0.512 at NO substrate-derived barrier scale. CF-AS-2 scans the BdG fluctuation potential at the τ≈0.16 exit horizon for any substrate-derived barrier (static OR dynamical-resonance from the finite quench rate τ̇) whose ω_½ = √V₀ lands in the relic band [0.94, 3.72] M_KK AND reproduces ∫Γ = 0.512 ± 10%, or proves none exists. GATED on this verdict being (A) or (B) (it is (A)) — the filter scan presupposes the magnitude is a real physical object worth bounding. LIVE.

3. **The CF-AS-3 pin FORM (POINT vs BAND) and the canonical-promotion write-order.** The verdict sets the FORM: register prediction (FB-temp PASS) ⟹ POINT-per-functional + scheme-tag; FB-temp FAIL ⟹ BAND + FB-extrapolation-tag-on-`T_pivot`. Either way the pin carries BOTH tags (scheme-tag for b-i functional-freedom, `T_pivot`-FB-saturation L_max-tag for b-ii truncation). The promotion follows the canonical write-order (verdict → canonical_constants.py → falsifier-master-inventory Row 8 via mack, sole writer per `feedback_mack-bridge-role.md`). Specified in CF-AS-3.

4. **Functional-pluralism cross-link (CV-1↔CV-4) — NOT this workshop's deliverable, flagged for the SA-FREE-ENERGY workshop.** The A_s magnitude is scheme-dependent across functionals (cutoff/zeta/impulse-quench/near-floor-DOS, −3.02→+6.008 OOM) for the SAME structural reason the a₀/a₂ CC ratio sign-flips (lizzi's CV-1↔CV-4 cross-link). Whether the functional-choice freedom is a genuine physical d.o.f. (Reading A's claim) or admits a substrate-canonical selection that pins ONE functional is a SEPARATE spectral-action adjudication, naturally adjacent to WS-SA-FREE-ENERGY (connes × lizzi). Not opened here; recorded so it is not lost.

## Wrap-Up — Workshop Impact Summary

### What Changed

Per `output-standards.md`, numerical revisions and structural changes are separated.

#### (a) Numerical revisions

- `K_sub spread "~2000×" → 1.137× (full S82 band table) / 1.0038× (near-BD-floor regime)` — the ~2000× was the ratio of the SUBTRACTIVE EXCESS `(K_sub−1)` (192× in the W1-2 reference-state regime, widened by occupation-input sweep), not the physical `K_sub`; Sage-verified (R2-D3, lizzi; conceded R2-C2, transit-dynamics).
- `(FB-temp) prediction: FAIL → PASS` — transit-dynamics's R2 prediction that `T_pivot` fails to saturate is replaced by PASS after the per-charge GGE-multiplier fact (R3-C(R3)1); the verdict map evaluates to (FB-gap PASS)∧(FB-temp PASS).
- Canonical pivot squeezing factors retained as the magnitude-bearing values: B1=2.0485, B2=1.9222, B3=2.1848 (per-mode coth factors, S82 canonical table), `K_substrate = coth(Δ_B/2T_k^GGE) ≈ 2.035` at the pivot — NOT the discarded `n_pairs/2π² = +9.5 OOM` aggregate dump.

#### (b) Structural changes

- **frozen-in-TIME ≠ converged-in-L_max** — established as a permanent orthogonality (TD R1; lizzi R2-C1). WS-FLOQUET settled the TIME axis only; the L_max axis was the genuine open question.
- **A_s magnitude object reclassified: extensive band-count → INTENSIVE UNIFIED-AS-79 pivot coth** — the canonical magnitude-bearing observable is `A_s = A_s^{BD}·coth(Δ_pivot/2T_pivot)` evaluated at ONE mode, with NO band-sum (lizzi D1; TD conceded R2-C1). The extensive `Σ mult·|β_k|²` is the discarded dump, not the A_s normalizer.
- **GGE Lagrange multiplier: shared-aggregate-suspected → PER-CHARGE (register-confirmed)** — `λ_k = −ln(n_k/(1−n_k))`, one per conserved charge in the complete commuting 8-element Richardson-Gaudin set (R3-C(R3)1). This is the type-level reclassification that decides the magnitude epistemic type: the GGE's non-thermality launders aggregate truncation-softness OUT of the per-mode pivot temperature.
- **A_s magnitude epistemic type: 3-way fork (A/B/C) → (A) physical degree of freedom** — the over-production magnitude is a real intensive amplitude whose openness is functional-choice freedom (a physical d.o.f.), conditional on the FB-temp PASS; NOT a truncation artifact (B), NOT scheme-chosen (C).
- **HK-AS-FLOOR register split: "A_s 3.02× permanent" → FLOOR-permanent (a) + MAGNITUDE/FILTER-scheme-dependent-open (b)** — anchor-structure reclassification with the (b) annotation carrying TWO orthogonal openness-source tags (b-i functional-choice ⊥ b-ii L_max-truncation).
- **Friedrich-Bär tie-breaker: one-object test → TWO-INPUT per-sector decomposition** — the saturation test on the pivot coth splits into (FB-gap) single-eigenvalue + (FB-temp) charge-conjugate, the latter collapsing to single-mode saturation via the per-charge structure (TD R2 E(TD)1; lizzi R3-C2).

### What Holds

- **The FLOOR `A_s ≥ A_s^{BD}` (`S_IC = 1 + 2n_k ≥ 1`, `proven_1097`) is PERMANENT and FUNCTIONAL-INDEPENDENT** — confirmed on 3 orthogonal axes (reference-state inv-12 W1-2 / families-index η-form inv-12 W2-5 / dynamical-Bogoliubov inv-5+inv-6), genuine independent confirmation per `joint-theorem-promotion.md`. Out of scope; not re-litigated by either agent.
- **WS-FLOQUET=DEAD** — the {β_k} are Floquet-frozen in time (h_par=8.3e-4 narrow-regime, q_M ≤ 5.25e-3 ≪ 1, fraction_resonance=0 EXACT, nearest relic mode 2.28e5 n=3-half-widths from the tongue). The relic survives intact; the over-production is not a re-pumping artifact.
- **S57/S62 Mode-Independent Occupation Theorem is a SHAPE theorem** — `|β_k|² = n̄` mode-independent certifies `n_s`/`α_s` intensiveness (an already-PASS result); it carries NEITHER side's normalization claim (TD R2-C3; lizzi R2-C2). The magnitude's intensiveness is the SEPARATE pivot-evaluation + per-charge fact.
- **The Friedrich-Bär structural-saturation theorem** (`proven_908`/`proven_1827`/`proven_182`, S87 W11-2/W11-3) analytically certifies bottom-K invariance for ALL L_max ≥ 10 via the per-sector predicate `η_FB(p,q) = |λ|_min(p,q)/√(C₂(p,q)+1)`. Unchanged; it is the machinery the verdict invokes.
- **The substrate-first arrow** `D_K eigenvalues → impulse-quench {α_k,β_k} → n_k=|β_k|² → λ_k=−ln(n_k/(1−n_k)) → S_IC(k_pivot)=coth(Δ_pivot/2T_pivot) → A_s` is unchanged; the verdict scopes confidence (floor-permanent / magnitude-physical-d.o.f.-conditional), it does not invert the explanation direction.

### What Breaks or Strains

- **Reading B (occupation-/truncation-soft band) does NOT survive on the math fork** — both legs conceded-closed: the extensive-band-count leg (R2-C1, it is the discarded dump) and the temperature-input leg (R3-C(R3)1, `T_pivot` is per-mode). Reading B's regime of validity is explicit and EMPTY at the canonical L_max: it would have held IFF the GGE-LAMBDA construction solved a shared-λ aggregate constraint; the register gives the non-thermal per-charge construction.
- **transit-dynamics's R1 Proposition 1 universal claim** ("no route from intensive `S_IC(k_pivot)` to an amplitude NORMALIZATION without a band-sum") is FACTUALLY WRONG — contradicted by the UNIFIED-AS-79 pivot map; retracted R2-C1.
- **transit-dynamics's R2 D(TD)1 narrowed dissent** (`T_pivot` inherits band-aggregate softness, Step 5) inadvertently re-thermalized the GGE; register-refuted, retracted R3-C(R3)1.
- **The register currently OVER-STATES A_s status** — atlas-08 CF23 "A_s 3.02× permanent" + falsifier Row 8 carry the magnitude as if the NUMBER were permanent. This is the strain the HK-AS-FLOOR split repairs (routed to designated writer + mack).
- **The exit-filter leg (∫Γ_derived = 0.036 vs fitted 0.512, 14×) STRAINS the upper edge** — a DIFFERENT axis, OPEN, routed to CF-AS-2. "Reading A wins on the magnitude" does NOT discharge it.

### Carry-Forward Computations (MATH ONLY — propagate to S111)

```
CF-AS-2 — A_s upper-edge greybody: dynamical near-horizon resonance scan.
  What:    Scan the BdG fluctuation potential at the τ≈0.16 exit horizon for ANY substrate-derived
           barrier (static OR dynamical-resonance from the finite quench rate τ̇(τ)) whose
           ω_½ = √V₀^sub lands in the relic band [0.94, 3.72] M_KK AND reproduces ∫Γ = 0.512 ± 10%,
           OR prove none exists. (The exit-FILTER leg — a DIFFERENT axis from this floor-vs-magnitude
           adjudication; the INV12-W3-4 fitted-0.512 leg the LIZ1/TD1 caveats flagged undischarged.)
  Inputs:  inv-12 W3-1 locked {α_k,β_k} + pair_band [0.94,3.72]; inv-12 W3-4 V_eff scanner +
           Pöschl-Teller machinery; inv-4 W1-4 black-hole-thermo greybody (two-route consolidation);
           τ̇(τ) near-exit trajectory; κ_exit=47.6146, Δ_BCS=0.4642547, T_acoustic=0.112; ∫Γ_fitted=0.512.
  Gate:    PASS (bounded-AND-derived) iff ∃ substrate V₀ with √V₀∈band AND ∫Γ=0.512±10% (RATIO tol);
           FAIL (bounded-but-filter-fitted) iff every substrate scale gives ω_½ outside band;
           INFO iff resonance in-band but ∫Γ misses by >10%. Pre-register finite-rate WKB regime_verdict.
  Effort:  ~1 agent-session.
  Depends on: this WS-AS-1 verdict (verdict = (A)-with-split ⟹ CF-AS-2 is LIVE; if it had been (C)
           scheme-chosen, the filter scan would be moot). UPSTREAM DATA: inv12_w3_1_*.npz (locked Bogoliubov),
           inv12_w3_4_*.npz (V_eff scanner), inv4_w1_4_*.npz (BH-thermo greybody, two-route consolidation);
           canonical_constants: κ_exit, Δ_BCS, T_acoustic.

CF-AS-3 — Session-promote the impulse-quench A_s canonical pin (the magnitude under the correct functional).
  What:    Promote the impulse-quench A_s magnitude (inv-5 +0.86 OOM, A_s=1.54e-8, k̂=53.3 M_KK,
           k̂/k_pivot=3.72 deg(T_BZ→pivot) mapping; cross-checked vs inv-6 Parker +1.455 OOM) to
           canonical_constants.py + PROVENANCE; mack updates falsifier Row 8. The FORM is set by the
           FB-temp sub-input: a POINT-per-functional + scheme-tag (verdict=(A), register-predicted) OR a
           BAND + FB-extrapolation-tag-on-T_pivot (if FB-temp FAILs). Pin carries BOTH tags (scheme-tag
           for b-i functional-freedom; T_pivot-FB-saturation L_max-tag for b-ii truncation) regardless.
           DECISIVE SUB-COMPUTE: the nazarewicz FB-temp per-sector test — does λ_pivot=−ln(n_pivot/(1−n_pivot))
           shift when a NEW high-Casimir in-band (p,q) sector is added at L_max+1, holding n_pivot fixed?
           Register prediction: NO (per-charge multiplier) ⟹ POINT.
  Inputs:  inv5_w2_1_*.npz, inv6_w2_2_*.npz; falsifier Row 8 (5.078e-9 TD-canonical); k_pivot, deg(T)=+2;
           s84_spectrum_cache_L12_tau019.npz (for the FB-temp per-sector eigenvalue-floor test);
           GGE λ_k=−ln(n_k/(1−n_k)) construction (session-38/S39); Friedrich-Bär η_FB(p,q) predicate (S87 W11-2/3).
  Gate:    PASS iff one defensible impulse-quench A_s magnitude (a POINT if FB-temp PASS ⟹ verdict=(A)
           physical-d.o.f.; a BAND if FB-temp FAIL ⟹ verdict=(B) L_max-soft) lands with scheme-tag +
           OOM-distance + the FB-temp per-sector verdict; row keeps SCHEME-DEPENDENT for the magnitude +
           adds the FI/PERMANENT floor sub-annotation. Canonical write-order (verdict → canonical_constants
           → inventory Row 8 via mack, sole writer).
  Effort:  ~0.3 wave (mechanical promotion) + the FB-temp per-sector compute (nazarewicz, ~0.3 wave).
  Depends on: this WS-AS-1 verdict (sets POINT-vs-BAND); the FB-temp per-sector test (the decisive sub-input);
           UPSTREAM DATA: inv5_w2_1_*.npz, inv6_w2_2_*.npz, s84_spectrum_cache_L12_tau019.npz;
           canonical_constants: k_pivot, deg(T)=+2, falsifier Row 8 anchor; mack as Row-8 writer.
```

### Effected In-Session (NON-MATH — completed by Agent B before terminating)

The verdict's on-disk artifact is THIS workshop document (the R3 cross-synthesis + Verdict table + Open Questions above record the A_s-magnitude epistemic-type verdict). The HK-AS-FLOOR register reconciliation touches ONLY surfaces governed by sole/designated writers — the falsifier Row 8 (mack, sole writer of the §7 falsifier surface + `falsifier-master-inventory.md` per `feedback_mack-bridge-role.md`) and the atlas-08 CF23 prose (designated writer, capstone-hygiene Q3 reviewed patch). Per the dispatch charge's Effected-In-Session discipline, I do NOT write those; I route them with the exact split.

- [→] ROUTED-mack: `sessions/framework/registry/falsifier-master-inventory.md` Row-8 — split the A_s entry into **(a) FLOOR (PERMANENT): `A_s ≥ A_s^{BD}`, `S_IC = 1 + 2n_k ≥ 1`, FUNCTIONAL-INDEPENDENT, confirmed on 3 orthogonal axes (reference-state inv-12 W1-2 / families-index η-form inv-12 W2-5 / dynamical-Bogoliubov inv-5+inv-6) — add as a FI/PERMANENT floor sub-annotation** + **(b) MAGNITUDE/FILTER (SCHEME-DEPENDENT, OPEN): keep `SCHEME-DEPENDENT` on the magnitude (3.02× / +0.86 / +1.455 OOM), tag BOTH openness sources — (b-i) functional-choice freedom [scheme-tag] and (b-ii) L_max-truncation softness [`T_pivot`-FB-saturation L_max-tag] — live A_s-amplitude question pinned by CF-AS-3 (magnitude) + CF-AS-2 (exit filter)**. Per canonical write-order, this lands AFTER CF-AS-3's verdict→canonical_constants step.
- [→] ROUTED-mack: atlas-08 CF23 falsifier-surface aspect — the CF23 bullet (`atlas-08-open-questions.md` line 17, S110 Wave-0 freshness) already records the split as an investigation-track scoping note; the falsifier-side annotation (live-watch envelope, the SCHEME-DEPENDENT-FILTER status) is mack's surface — confirm consistency with the Row-8 split above when Row-8 lands.
- [→] ROUTED-designated-writer: `atlas-08-open-questions.md` CF23 prose — split "A_s 3.02× permanent" into **(a) FLOOR → PERMANENT, FUNCTIONAL-INDEPENDENT, 3-axis-confirmed (only the inequality `>1` deserves "permanent")** + **(b) MAGNITUDE/upper-edge FILTER → SCHEME-DEPENDENT and OPEN, carrying (b-i) functional-choice [scheme-tag] + (b-ii) `T_pivot`-FB-saturation [L_max-tag]; magnitude → CF-AS-3 (pin FORM), exit filter → CF-AS-2 (greybody scan)** — capstone-hygiene Q3 reviewed patch, substrate-first frame `D_K → {α_k,β_k} → n_k → S_IC → A_s` PRESERVED (the tag scopes confidence floor-permanent / magnitude-open; it does not invert the explanation direction).
- [→] ROUTED-capstone-hygiene: `sessions/framework/phonic-exflation-equation.md` — IF the capstone narrates the A_s magnitude at "permanent" confidence anywhere (Q3 PROVEN/CONDITIONAL distinction), down-tag the magnitude to SCHEME-DEPENDENT-OPEN while preserving the FLOOR-permanent inequality and the substrate-first frame; the §7 falsifier-anchor cells for A_s are mack's surface (capstone-hygiene Q2). Designated-writer reviewed patch, NOT a bulk append.

No `- [x]` on-disk edits: the verdict lives in this workshop document (the designated-writer artifact for THIS workshop's prose); every register reconciliation is a sole/designated-writer surface and is routed, not written by me. The permanent-results-registry has no existing A_s §VII slot to extend, and an investigation-track workshop verdict is NOT register-permanent until session-promoted (per `gate-verdicts.md §"Investigation-Track"` + the atlas-08 line-17 freshness note) — so creating a §VII slot now would be premature; CF-AS-3 is the session-promotion gate. All math → Carry-Forward Computations above.

### Closing Line

The A_s FLOOR is permanent on three axes and was never in dispute; the MAGNITUDE is a physical degree of freedom — the canonical UNIFIED-AS-79 pivot coth is intensive in form (transit-dynamics conceded R2), and BOTH its inputs are per-mode quantities of the low-Casimir pivot mode because the GGE's per-charge Lagrange multiplier `λ_k = −ln(n_k/(1−n_k))` (register-confirmed, transit-dynamics conceded R3) launders the band-aggregate truncation-softness out of the pivot temperature, leaving the spectral-functional choice as the sole, genuinely physical, openness — to be pinned as a POINT by CF-AS-3 once nazarewicz's Friedrich-Bär per-sector test confirms (as the register predicts) that `λ_pivot` does not move when new high-Casimir sectors enter the band.
