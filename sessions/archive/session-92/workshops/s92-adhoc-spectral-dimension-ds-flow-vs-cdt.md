# Session 92 Ad-hoc Workshop: kk x landau — Spectral-Dimension d_s Flow vs CDT

**Date**: 2026-05-24
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: kk (kaluza-klein-theorist), landau (landau-condensed-matter-theorist)
**Source Documents**:
- `sessions/archive/session-92/workshops/_seed-adhoc-phonon-first.md` (AH-PF-1 seed — KB open-questions scan)
- `sessions/archive/session-52/session-52-results-workingpaper.md` (S52 "12D Reduction" results — the standing "no CDT-like reduction" position)

**Slot**: Session-92 workshop-schedule Slot 2b, entry AH-PF-1 (ad-hoc cross-session open-question workshop, KB scan).

**Agent-B reach-substitute note**: Axis-B is `landau-condensed-matter-theorist`, substituted for `loop-quantum-gravity-theorist` per the schedule's reach-substitute clause — LQG was lead author of the in-session §IX.7 LQG×phonon-first workshop (`session-92-lqg-phonon-first-workshop.md`, 2026-05-23) and arrives pre-loaded with substrate-first concessions that would blunt the adversarial tension. Landau owns the van-Hove-DOS / flat-band machinery (the load-bearing Axis-B physics) and is an independent route to the same Axis-B position.

**Central question**: Does the framework make a CDT-comparable spectral-dimension flow, or is "no UV reduction" a fiber-bulk-vs-van-Hove-fold-energy / low-L_max artifact? Two readings of the SAME S52 verdict via incompatible machinery (heat-kernel Weyl asymptotics vs DOS-controlled return-probability), spanning S31→S63.

**Focus Topics** (the four adjudication questions):
1. **(a) Structural vs artifact** — is the S52 "no reduction" verdict a structural property of D_K's heat kernel on the compact SU(3) fiber (KK), or an artifact of (i) bulk-vs-fold-energy evaluation + (ii) finite-L_max truncation (van-Hove)?
2. **(b) Fold-energy d_s** — does the PROVEN van-Hove DOS divergence at τ_fold=0.190 produce a non-standard d_s flow AT the fold energy (resolving S34 [F-4] from existing eigenvalue data), and does fold-energy d_s approach CDT ~2 or stay near the bulk Weyl value?
3. **(c) Premature-comparison** — do the S53 d_s_max=67.829 artifact flag + the L_max≫6 caution make the bulk d_s/CDT comparison structurally premature ("indeterminate-pending higher-L_max fold-energy heat-kernel gate" rather than positive "no reduction")?
4. **(d) Conditional cross-domain** — IF a real fold-energy/converged-L_max d_s flow exists, does n_s−1=(d_s−4)/2 and/or the Hawking relation n_s−1~−d(d_s)/dτ reproduce canonical n_s=0.9567 or diverge (d_s-route to n_s superseded)?

---

## Round 1 — kk: Opening Analysis

### K1: (a) Structural vs Artifact — Reading KK steelman (Weyl-asymptotic, measure-zero fold)

**Position (Reading KK):** The S52 DS-QUANTUM-52 verdict — "no CDT-like dimensional reduction in the internal SU(3) fiber; `d_s → 8` is the correct Weyl limit; the 2-and-8 crossings are crossings, not plateaus" (`session-52-results-workingpaper.md` W3-D lines 626–641, knowledge-MCP-verified `d_s^total = d_s^M4 + d_s^SU(3)`) — is a **structural property of the heat kernel on the compact homogeneous fiber**, not an artifact of bulk-vs-fold evaluation or finite L_max. I steelman this in four load-bearing steps, each grounded in the substrate's own spectral data.

**Substrate-first framing (mandatory, `phononic-framing.md`).** d_s is a property of the D_K heat kernel on the fiber. The fabric IS the spectral triple `(A_K, H_K, D_K)`; the spectral dimension is read off the small-σ asymptotics of `K(σ) = Tr exp(−σ D_K²)`. Spacetime dimension is emergent. CDT and asymptotic-safety are LABORATORY/analog comparisons — the substrate is logically prior. The question is therefore NOT "does the substrate match CDT?" but "does a CDT-like flow EMERGE from the D_K spectrum?" The KK claim is that it provably does not, by the structure of harmonic analysis on the group.

**Step 1 — The defining quantity is the UV (small-σ) asymptotic of the heat trace.** By definition (S31Aa / S44 / S52, all using the identical operator):
- (i) `K(σ) = Tr exp(−σ D_K²) = Σ_k d_k e^{−σ λ_k²}`  [heat trace; `d_k` = Peter-Weyl multiplicity dim(p,q)²]
- (ii) `d_s(σ) = −2 d ln K(σ) / d ln σ`  [knowledge-MCP-verified, `session-52-results-workingpaper.md` line 612; identical form S31Aa, S44]
- (iii) The **spectral dimension proper** is `lim_{σ→0⁺} d_s(σ)` — the UV limit. On a smooth closed Riemannian d-manifold, the Minakshisundaram–Pleijel expansion `K(σ) ~ (4πσ)^{−d/2} Σ_n a_n σ^n` gives `K(σ) ~ σ^{−d/2}`, hence `d_s(σ→0) = d`. This is a THEOREM (Weyl/MP asymptotics), not a fit.

**Step 2 — The fiber is a compact homogeneous Einstein manifold; its Weyl exponent is its dimension, full stop.** The substrate fiber is `K = SU(3)`, `dim K = 8` (agent memory; Baptista conventions `P = M⁴×K`, 12D). The Jensen deformation is a left-invariant metric (volume-preserving TT, agent memory + PROVEN registry). For ANY smooth left-invariant metric on a compact Lie group, the scalar/Dirac Laplacian has Weyl asymptotic `N(λ) ~ C·Vol·λ^{dim K}` with `dim K = 8` EXACTLY — the leading Weyl term depends only on the manifold dimension and the total volume, both of which the Jensen deformation holds fixed (volume-preserving). Therefore `d_s^{SU(3)}(σ→0) = 8` is forced by the geometry. This is why S52 reports the SU(3) factor saturating at 8 and labels it "the correct Weyl limit." It is the substrate telling us its own dimension. Verified: the s75 Weyl-exponent note (`zeta_D(s) ~ L^{2s}` on an 8-dim manifold) and the atlas-07 "F/B fiber ratio … Weyl's law" theorem both anchor `dim=8` as the operative Weyl exponent.

**Step 3 — "Crossings, not plateaus" is the diagnostic signature that there is NO intermediate fixed dimension.** In CDT and asymptotic-safety, the hallmark of genuine dimensional reduction is a **PLATEAU**: `d_s(σ)` sits at ~2 over a finite, parametrically wide window of diffusion times before flowing to 4 in the IR. The S52 data (knowledge-MCP-verified, lines 618–628) shows the opposite — `d_s` rises **monotonically** through every integer:

| scale t (M_KK⁻²) | d_s (HH) | character |
|:--|:--|:--|
| physical UV (1/ω_max²)=0.235 | 1.13 | truncation floor |
| 0.50 | 2.31 | passing through 2 |
| 1.0 | 4.23 | passing through 4 |
| 1.49 (1/ω_min²) | 5.83 | gap onset |
| 2.36 | 8.00 | Weyl crossing |

`d_s` *crosses* 2 at t≈0.42 and 8 at t≈2.36 (lines 628), with no flat region at either. A monotone strictly-increasing `d_s(σ)` with zero plateau is the structural antithesis of a CDT flow. The single power-law fit in the "Weyl window" t∈[0.24,1.49] giving `d_s≈2.6` (R²=0.98, line 629) is NOT a plateau — it is the average slope of a curve that is still climbing; the high R² of a power law merely confirms smooth monotonic growth, which is exactly what Weyl asymptotics on a gapped homogeneous space predicts.

**Step 4 — The two "contestable choices" do not threaten the asymptotic.** The seed (Axis-B) argues the verdict rests on (i) bulk-vs-fold-energy evaluation and (ii) finite L_max. Reading KK answers both at the structural level here (the quantitative DOS argument is K2, the L_max argument is K3):
- (i) **bulk-vs-fold**: the Weyl exponent that sets `d_s(σ→0)` is the LEADING term of the heat trace, governed by the full operator spectrum's growth rate `N(λ)~λ^8`. The fold at τ=0.190 is a feature of the eigenvalue **density at a finite energy** (van Hove; ρ_B2(fold)=14.023, knowledge-MCP-verified), i.e. a property of the spectral measure at O(1) λ, not of the λ→∞ counting exponent. S52 itself isolates this: it explicitly attributes the fold to "the eigenvalue *density* (Van Hove singularities, DOS reshaping), not the eigenvalue *product/counting* that defines" the Weyl/torsion invariants (W1-E line 180). The same operator, same eigenvalues, evaluated at the fold τ-value, still has Weyl exponent 8 — and S52 confirms `d_s` is "τ-independent to 0.5%" across the WDW weighting (line 627), precisely because the leading asymptotic cannot see a measure-zero density feature.
- (ii) **finite L_max** truncates the tower at max_pq_sum=3 (992 modes). This LOWERS the apparent UV `d_s` (S52: `d_s≈2.6` in the Weyl window "not the full d=8 because the Peter-Weyl truncation … captures only 992 modes of the infinite tower," line 629) — i.e. truncation pushes the answer AWAY from 8 toward smaller values, the OPPOSITE direction from what a structural-8 claim needs to defend. The structural claim (`d_s→8`) is the L_max→∞ statement; finite L_max is a known underestimate, addressed in K3.

**K1 finding (load-bearing for landau's Re:K1 + L1).** The S52 verdict has a **structural core** and a **truncation-limited numerical surface**, and they point the same way:
- **F-K1-CORE (structural):** `d_s^{SU(3)}(σ→0) = dim(SU(3)) = 8` is forced by Weyl/Minakshisundaram–Pleijel asymptotics on the compact homogeneous fiber, INDEPENDENT of τ (volume-preserving Jensen) and of the fold (a finite-energy DOS feature). "No intermediate CDT-like plateau in the fiber" is therefore a heat-kernel theorem about a Lie-group manifold, not a contingent computational outcome.
- **F-K1-ADDITIVITY:** `d_s^total = d_s^M4 + d_s^{SU(3)}` (knowledge-MCP-verified S52 inline). CDT's `d_s→2` is a statement about the **M⁴ path-integral / foam factor**, which acts on the FIRST summand. The S52 gate tested CDT on the SECOND summand (the fiber) — a category mismatch the gate itself flags ("Gate was mis-targeted … asking the wrong question," line 641). A CDT-like reduction can live in `d_s^M4` and is simply not a property of `D_K` on SU(3); the two are not in competition.
- **What I concede to Axis-B up front:** the NUMERICAL value of `d_s` in the accessible window IS truncation-dominated (the 1.13 UV floor and 2.6 window-fit are L_max=3 artifacts), and the fold-energy `d_s` was indeed never computed (S34 [F-4] open). So the *structural* claim "no plateau / d_s→8 Weyl" is robust, but any *numerical* CDT comparison at the accessible scales is premature. That tension is exactly K2 and K3.

### K2: (b) Fold-Energy d_s — does the van-Hove DOS divergence move the heat-kernel d_s?

**The decisive question.** Axis-B's strongest point: a van Hove singularity is the textbook condensed-matter mechanism for anomalous spectral-dimension flow, and S34 §3.4 explicitly observed that in 1D, `ρ(E) ~ |E−E_0|^{−1/2}` drives `d_s → 1` at the van Hove energy (`session-34-quantum-foam-collab.md` line 102, knowledge-MCP-verified). The fold IS a PROVEN DOS divergence at τ_fold=0.190 (constraint-mega-matrix "YES (DOS divergence)" S52–S62; ρ_B2=14.023). So: **can a DOS singularity at a finite energy E_0 shift the small-σ (UV) asymptotic of `K(σ)` that DEFINES `d_s`?** I write the substitution chain (`math-scripts.md` Double-Check Logic).

**Substitution chain (heat-kernel response to a DOS feature).**

```
Claim to test: "A van Hove DOS divergence at the fold energy E_0 = λ_{B2}(τ_fold)
                shifts d_s(σ→0) toward the CDT value ~2."

Step 1 (definition). Recast the heat trace as a Stieltjes integral over the
        spectral density ρ(λ) = Σ_k d_k δ(λ − λ_k):
            K(σ) = ∫_0^∞ ρ(λ) e^{−σ λ²} dλ                         [from K2-(i)]
        [source: K(σ)=Tr exp(−σD_K²), knowledge-MCP-verified S52 line 612]

Step 2 (definition). Spectral dimension:
            d_s(σ) = −2 d ln K(σ)/d ln σ = 2σ · [−K'(σ)/K(σ)]
                   = 2σ · ⟨λ²⟩_σ,   where ⟨λ²⟩_σ ≡ ∫ρ λ² e^{−σλ²}/∫ρ e^{−σλ²}
        [the heat-kernel-weighted mean square eigenvalue]                 [defn]

Step 3 (the UV limit selects the λ→∞ tail of ρ). As σ→0⁺ the Gaussian
        weight e^{−σλ²} → 1 for all FINITE λ and only cuts off at λ ~ σ^{−1/2}→∞.
        Hence ⟨λ²⟩_σ as σ→0 is dominated ENTIRELY by the large-λ growth of ρ:
            K(σ→0) ~ ∫_0^{~σ^{−1/2}} ρ(λ) dλ = N(σ^{−1/2})     [cumulative count]
        With Weyl ρ(λ) ~ λ^{7} ⇒ N(λ) ~ λ^{8} (K1 Step 2):
            K(σ) ~ (σ^{−1/2})^{8} = σ^{−4}  ⇒ d_s(σ→0) = −2·(−4) = 8.    [Weyl]

Step 4 (insert the van Hove feature as an ADDITIVE local term). A van Hove
        singularity at finite E_0 contributes an integrable local excess
            ρ_VH(λ) = c · |λ − E_0|^{−1/2} · 𝟙_{|λ−E_0|<w}      [1D form, S34 §3.4]
        Its heat-trace contribution is BOUNDED for all σ>0:
            ΔK_VH(σ) = ∫ ρ_VH(λ) e^{−σλ²} dλ
                     ≤ e^{−σ(E_0−w)²} ∫_{E_0−w}^{E_0+w} c|λ−E_0|^{−1/2} dλ
                     = e^{−σ(E_0−w)²} · c · 4√w   = FINITE, σ-INDEPENDENT prefactor
        [the |λ−E_0|^{−1/2} singularity is integrable: ∫|x|^{−1/2}dx = 2√x converges]

Step 5 (ratio — does ΔK_VH move d_s(σ→0)?). The fold sits at FINITE energy
        E_0 = λ_{B2}(τ_fold) ≈ 0.86–1.40 M_KK (S52 W1-I, S39). Compare its
        contribution to the leading Weyl term as σ→0:
            ΔK_VH(σ)/K_Weyl(σ) ~ [c·4√w · e^{−σE_0²}] / [σ^{−4}]
                               ~ (4c√w) · σ^{4} · e^{−σE_0²}  →  0   as σ→0.
        The van Hove excess is suppressed by σ^{+4} relative to the Weyl
        leading term in the UV limit. A finite-energy DOS feature contributes
        to K(σ) at O(σ^0) (a constant), which is SUBLEADING to the σ^{−4}
        Weyl singularity by FOUR powers of σ.

Conclusion: d_s(σ→0) = 8 is UNMOVED by the van Hove fold. The fold cannot
            shift the UV asymptotic; it can only contribute to the
            INTERMEDIATE-σ (O(1) diffusion time) shape of d_s(σ).
```

**Why the 1D `d_s→1` analogy does NOT transfer to the fiber UV.** S34 §3.4's `d_s→1` statement is about a 1D system where the van Hove singularity sits at a BAND EDGE that IS the bottom of the spectrum — there, the singularity controls the IR (large-σ) return probability, because at a band edge `E_0 = λ_min` and the small-energy density IS the asymptotically relevant region for `σ→∞`. The fiber fold is structurally different: it is an **interior** DOS feature at finite E_0, NOT the spectral bottom (the bottom is the gap `ω_min=0.82`, knowledge-MCP-verified S52 line 630, and is gapped — SU(3) has no harmonic spinors). An interior van Hove feature lives at O(1) λ and therefore at O(1) σ; by Step 5 it is invisible to both the σ→0 UV limit (Weyl-8) and the σ→∞ IR limit (gap-dominated, `d_s→∞`). It can only sculpt the crossover region — which is exactly where S52 already sees smooth monotone crossings.

**Independent confirmation from the framework's OWN prior computation (decisive).** This is not a new claim. The S35 Connes–spectral-geometer workshop pre-registered exactly this question ("Re: C7 — Spectral Dimension at the Fold") and computed the fold-induced step:
- `δd_s ~ (Δ/λ_{B2})² ~ 4×10⁻⁴` (knowledge-MCP-verified, `session-35-connes-spectral-geometer-workshop.md`, `δd_s = t·Δ_0² = 3.87×10⁻⁴`, `Δ_0/λ_{B2}=0.020`), with the workshop's own Proposition D stating verbatim: *"spectral dimension is NOT a sensitive probe of BCS condensation … a negative result"* (knowledge-MCP-verified eq_8365). The naive `δd_s~4/16=0.25` guess (eq_8335) was explicitly RETRACTED in favor of `~10⁻⁴` (eq_8370, "consensus achieved").
- So the framework already has a fold-localized spectral-dimension shift, and it is `~4×10⁻⁴` — four orders of magnitude below the S34 [F-4] decision threshold of 0.5. The fold does NOT move `d_s` by `>0.5`; it moves it by `~10⁻⁴`.

**K2 finding (load-bearing for landau's Re:K2 + L1 + L2).**
- **F-K2-UV-IMMUNE:** By the Step-1→5 substitution chain, a van Hove DOS divergence at a FINITE interior energy `E_0` contributes to `K(σ)` at O(σ⁰), suppressed by `σ^{+4}` relative to the `σ^{−4}` Weyl leading term. It is structurally incapable of shifting `d_s(σ→0)` — the UV asymptotic that DEFINES the spectral dimension stays at 8.
- **F-K2-MAGNITUDE:** The framework's own S35 computation of the fold-localized shift gives `δd_s ≈ 4×10⁻⁴`, vs the S34 [F-4] threshold 0.5 — a 1250× margin. Fold-energy `d_s` therefore remains essentially at the bulk Weyl value, NOT near CDT ~2.
- **F-K2-SCOPE (honest):** the van Hove feature CAN (and the framework expects it to) reshape the INTERMEDIATE-σ profile of `d_s(σ)` at O(1) diffusion times. If Axis-B means by "fold-energy `d_s`" a *windowed/return-probability* diagnostic at the fold energy scale (not the σ→0 asymptotic), that is a DIFFERENT observable than the heat-kernel spectral dimension — and the burden is on Axis-B to show that windowed diagnostic (i) is well-defined on a gapped finite spectrum and (ii) approaches 2 rather than tracking the smooth crossing S52 already reports. This is the genuine open seam I carry to R2.

### K3: (c) Premature-Comparison — are the S53 artifact + L_max≫6 caution decisive?

**The honest distinction Reading KK insists on: STRUCTURAL claim vs NUMERICAL claim.** The seed conflates two separable assertions inside "no CDT-like reduction." I separate them, and my position differs between them.

**Claim type A — STRUCTURAL ("no intermediate plateau; `d_s^{SU(3)} → 8` Weyl; crossings not plateaus"):** NOT premature. SETTLED at the structural level by K1 (Weyl/MP asymptotics on the compact homogeneous fiber) and K2 (the van-Hove feature is `σ^{+4}`-suppressed in the UV and shifts `d_s` by `~4×10⁻⁴`, S35-verified). This claim is L_max-INDEPENDENT in DIRECTION: the Weyl exponent equals `dim(SU(3))=8` for the full tower BY CONSTRUCTION, and the absence of a plateau is a statement about the monotone structure of `K(σ)` on a gapped Lie-group spectrum, which truncation does not create or destroy — it only rescales the accessible σ-window. Crucially, finite L_max biases the apparent `d_s` DOWNWARD (S52: 2.6 vs 8, line 629), so if anything the truncated data FLATTERS a CDT-2 reading; the structural claim survives precisely because the L_max→∞ correction moves AWAY from 2, toward 8.

**Claim type B — NUMERICAL (any quantitative `d_s(scale) ↔ CDT-d_s(scale)` comparison at accessible scales):** Premature, AND I say so without reservation. Two independent flags establish this, both knowledge-MCP-verified:
- **S53 `d_s_max = 67.829`** (`s53_condensed_ds_output.txt`, "3D K² weighting (ARTIFACT for finite graph): d_s_max = 67.829 … divergent at large t, K=0 weight → 0"). A spectral dimension of 67.8 on an 8-dimensional manifold is a manifestly unphysical truncation/weighting artifact — it is `~8.5×` the manifold dimension. Its existence proves the d_s pipeline at S53's settings produces numbers with no continuum meaning. (Note: this is the **graph/condensed K²-weighting** pipeline, structurally distinct from the heat-kernel `d_s` of K1–K2; it is a *weighting-scheme* artifact, doubly disqualifying any naive numerical CDT comparison.)
- **The L_max≫6 caution** (my own standing position, agent memory + S52 line 629): at max_pq_sum=3 (992 modes) the Weyl-window fit reads 2.6 instead of 8 ONLY because the tower is truncated. The S52 verdict itself attributes the shortfall-from-8 to "Peter-Weyl truncation … only 992 modes of the infinite tower." The number 2.6 is a truncation underestimate, not a CDT plateau.

**Are the two flags DECISIVE for declaring the comparison premature? Yes — for Claim B; No — for Claim A.** My honest position:

1. The framework's PUBLISHED framing should NOT read as a positive "no reduction" *numerical verdict* (the S52 gate is FAIL+mis-targeted, line 632/641 — it never was a clean positive). It SHOULD read as: **(A) a structural theorem** (no fiber plateau; `d_s^{SU(3)}→8` by Weyl; CDT lives in the M⁴ summand) **PLUS (B) a numerically-indeterminate CDT comparison pending a higher-L_max, fold-energy heat-kernel computation.** Both are true simultaneously and are not in tension — A is about the asymptotic STRUCTURE, B is about the accessible NUMBER.

2. **The genuinely-open item is S34 [F-4], and it is discharge-able from existing data.** [F-4] (INFORMATIVE, never closed, knowledge-MCP-verified `session-34-quantum-foam-collab.md` lines 98–107) asked for `d_s` AT τ_fold=0.190 from the heat kernel — which S52 partially did (5 τ values, τ-independent to 0.5%) but NEVER reported the fold-energy windowed `d_s(σ ~ 1/E_0²)` that [F-4] specifically wanted, nor at converged L_max. The S35 `δd_s~4×10⁻⁴` result strongly predicts the fold-energy `d_s` stays at the bulk value, but that is an analytic estimate, not the direct [F-4] computation. **This is a real carry-forward**, not hygiene: a new number (fold-energy heat-kernel `d_s`) from first principles against a pre-registered threshold ([F-4]'s `|d_s^fold − d_s^bulk| > 0.5` ⇒ transition; `≈2` ⇒ foam-NCG connection).

3. **Therefore the correct verdict-class is a 3-way split, not a binary.** Reading KK does NOT claim "settled: no reduction" across the board. It claims: Claim A is a structural theorem (settled); Claim B is "indeterminate-pending higher-L_max fold-energy gate." This is consistent with — and sharper than — the seed's option (iii).

**K3 finding (load-bearing for landau's Re:K3).**
- **F-K3-STRUCTURAL-SETTLED:** the "no fiber plateau / `d_s^{SU(3)}→8` Weyl / additive split" claim is structurally settled (K1+K2); the S53 67.829 artifact and L_max≫6 caution do NOT touch it (they bias `d_s` downward, away from 2 — the structural claim survives *a fortiori*).
- **F-K3-NUMERICAL-PREMATURE:** ANY quantitative `d_s ↔ CDT` comparison at accessible scales IS structurally premature; the S53 `d_s_max=67.829` (8.5× the manifold dim) and the L_max=3 underestimate are jointly decisive on this point.
- **F-K3-GATE:** the live discharge is a **fold-energy, converged-L_max heat-kernel `d_s` gate** to close S34 [F-4]. Pre-registered prediction from Reading KK (K2): fold-energy `d_s` ≈ bulk Weyl value (8 at L_max→∞; ~2.6 at the accessible window), NOT CDT-2, with `δd_s^fold ≈ 4×10⁻⁴` (S35-anchored). I will pin the 4-field spec in R3.

### K4: (d) Conditional Cross-Domain — the d_s-route to n_s (Weyl-asymptotic view)

**The conditional.** IF a real (fold-energy / converged-L_max) `d_s` flow exists, does `n_s−1=(d_s−4)/2` (S44) and/or the Hawking relation `n_s−1 ~ −d(d_s)/dτ` (S44) reproduce the canonical Bogoliubov-invariant `n_s = 0.9567` (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A, knowledge-MCP-verified `phonon_exflation_cosmology.md §8.7.9`), or diverge? Under Reading KK the answer is sharp and, I argue, **decisive against the d_s-route as a primary n_s generator.**

**Substitution chain — the direct relation at the Weyl asymptotic.**

```
Claim to test: "n_s − 1 = (d_s − 4)/2 reproduces n_s = 0.9567 at the
                Weyl-asymptotic d_s of the fiber."

Step 1 (relation). n_s − 1 = (d_s − 4)/2
        [knowledge-MCP-verified, S44 wave2, "Alternative: n_s from d_s directly";
         display eq n_s−1=(d_s−4)/2]

Step 2 (substitute the structural fiber asymptotic, K1: d_s^{SU(3)}→8):
        n_s − 1 = (8 − 4)/2 = 2   ⇒  n_s = 3.       [ABSURD: n_s>1 by a factor]

Step 3 (substitute the accessible-window value, S52: d_s ≈ 2.6):
        n_s − 1 = (2.6 − 4)/2 = −0.70  ⇒  n_s = 0.30.  [absurd: 67× too red]

Step 4 (what value of d_s WOULD give the canonical n_s = 0.9567?):
        0.9567 − 1 = (d_s − 4)/2  ⇒  d_s = 4 + 2(−0.0433) = 3.9134.
        i.e. the relation requires d_s ≈ 3.91 ≈ 4 (a NEAR-4D value).

Conclusion: n_s−1=(d_s−4)/2 reproduces 0.9567 ONLY at d_s ≈ 3.91 — a value
            characteristic of the 4D M⁴ FACTOR (d_s^M4 → 4), NOT the 8D fiber.
            At the fiber's Weyl asymptotic (8) or its truncated window (2.6),
            the relation gives n_s = 3 or n_s = 0.30 — both absurd.
```

**Reading-KK interpretation (the additive split resolves the apparent contradiction).** This is NOT a failure of the framework — it is a CONSISTENCY CHECK that confirms K1's additive structure. `n_s−1=(d_s−4)/2` is a relation imported from CDT/causal-set approaches where `d_s` is the **4D spacetime** spectral dimension flowing toward 4 in the IR. The "−4" baseline is the 4D reference. Feeding it the **8D fiber** `d_s` is the same category error S52 flagged for the gate itself (testing CDT on the fiber). The relation:
- gives a sensible near-scale-invariant `n_s` ONLY at `d_s ≈ 3.91 ≈ 4` ⇒ it is implicitly a statement about `d_s^{M4}`, not `d_s^{SU(3)}`;
- so under the additive split `d_s^total = d_s^M4 + d_s^{SU(3)}` (F-K1-ADDITIVITY), if any d_s-route to n_s is live, it must use the **M⁴ summand near 4**, and the fiber summand (8) does NOT enter the tilt at all.

**The Hawking relation `n_s−1 ~ −d(d_s)/dτ`.** Under Reading KK this is even more clearly suppressed in the fiber:
- `d_s^{SU(3)}(σ→0) = 8` is τ-INDEPENDENT (volume-preserving Jensen; K1 Step 4(i); S52 "τ-independent to 0.5%," line 627). Hence `−d(d_s^{SU(3)})/dτ ≈ 0` at the asymptotic — the fiber spectral dimension does not flow with τ, so it generates ZERO tilt by the Hawking route.
- Any nonzero `d(d_s)/dτ` must come from the τ-flow of the *windowed/intermediate-σ* `d_s` near the fold — which is exactly the `δd_s ~ 4×10⁻⁴` regime (K2, S35-verified). `−d(d_s)/dτ` from a `4×10⁻⁴`-scale fold wiggle is far too small to produce `n_s−1 = −0.043`. So the Hawking route in the fiber under-produces the tilt by orders of magnitude.

**Verdict on the d_s-route (Reading KK).** The d_s-route to n_s is **superseded** as a primary n_s generator, relative to the canonical Bogoliubov-invariant `n_s = 0.9567`:
- it reproduces 0.9567 only at a fine-tuned `d_s ≈ 3.91` that is a property of the M⁴ factor, not derivable from the fiber Weyl asymptotic;
- the fiber `d_s` (8, τ-flat) feeds BOTH relations to absurd or null results;
- this is fully consistent with the seed's Q2 staleness observation: the **atlas-07 `[NEW S44]` Lifshitz `η_eff=3.77`** entry (proven_1291, knowledge-MCP-verified) and the **S53 KZ `(ν=0.5,z=2): n_s=2.0647`** / `(z=1): n_s=2.0660` outputs (knowledge-MCP-verified `s53_kz_power_spectrum_output.txt`) all yield `n_s−1 > 0` (BLUE tilt) — opposite in SIGN to the canonical RED `n_s−1=−0.0433`. They are stale d_s/Lifshitz-route artifacts that the canonical `n_s=0.9567` tacitly supersedes. (Also knowledge-MCP-verified: the dynamical exponent itself was revised — `z=2` EXACT from phonon bands; the earlier `z=3.68` retracted — further destabilizing the KZ→n_s route.)

**K4 finding + 3 sharp questions for landau (load-bearing for L3 + R2).**
- **F-K4-SUPERSEDED:** the d_s-route to n_s (`n_s−1=(d_s−4)/2` and Hawking `n_s−1~−d(d_s)/dτ`) does NOT reproduce canonical `n_s=0.9567` from the fiber Weyl asymptotic — it needs `d_s≈3.91` (an M⁴-factor value) for the direct relation, and gives `~0` tilt from the τ-flat fiber `d_s` for the Hawking relation. The d_s-route is **superseded** by the canonical Bogoliubov n_s; the atlas-07 η_eff=3.77 + S53 KZ n_s≈2.06 entries are stale (wrong-sign tilt) and flagged for Q2 re-scope.

**Questions for landau (Axis-B / van-Hove):**
1. **Q-K4a (the decisive observable):** You will argue `d_s` is a DOS-controlled return-probability and that the **fold-energy** value is the physically decisive number. But the return probability `P(σ)` at the fold energy is dominated by `e^{−σ E_0²}` with `E_0` finite and the spectrum GAPPED (`ω_min=0.82`). Concretely: at what diffusion time σ do you evaluate the "fold-energy `d_s`", and how do you prevent the gap from forcing `d_s→∞` (S52 line 630) before any van-Hove plateau can form? In other words — is your "fold-energy `d_s`" the σ→0 asymptotic (which K2 shows is Weyl-8, van-Hove-immune), or a windowed quantity at O(1) σ that is NOT the heat-kernel spectral dimension at all?
2. **Q-K4b (magnitude, not mechanism):** I concede the van Hove singularity reshapes the intermediate-σ profile. But the framework's OWN S35 computation puts that reshaping at `δd_s ≈ 4×10⁻⁴` — 1250× below the S34 [F-4] threshold of 0.5. To get `d_s^fold → ~2` (a shift of order several from the bulk), you need a mechanism ~10⁴× stronger than the S35 estimate. What in the van-Hove DOS structure on the *gapped, interior-singularity* fiber spectrum supplies that, given the `|λ−E_0|^{−1/2}` excess is integrable (K2 Step 4) and hence bounded in `K(σ)`?
3. **Q-K4c (apples-to-apples):** You hold that CDT's `d_s→2` is itself a DOS/return-probability effect, so the comparison is fair. But CDT's reduction is on the **4D M⁴ path-integral** (a foam effect on the FIRST summand of `d_s^total = d_s^M4 + d_s^{SU(3)}`), where the relevant "DOS" is over geometries, not over `D_K` eigenvalues. Why should a van-Hove feature in the `D_K` eigenvalue density of the **8D fiber** be the same physical object as CDT's geometry-sum reduction on M⁴ — i.e. what is the bridge map that makes these the same `d_s`, rather than two different summands?

---

## Round 1 — landau: Response & Cross-Synthesis

### Part 1: Response to kk's Sections

#### Re: K1 — (a) Structural vs Artifact

**EMERGES — with a sharp scope amendment.** I AGREE with kk's F-K1-CORE as stated: `d_s^{SU(3)}(σ→0) = dim(SU(3)) = 8` is a Weyl/Minakshisundaram–Pleijel theorem on the compact homogeneous fiber, volume-preserving-Jensen-invariant, and a measure-zero DOS feature cannot move the σ→0 *leading* heat-trace exponent. kk's Step 1–4 are correct condensed-matter heat-kernel bookkeeping and I will not contest them. But the agreement is on a *different observable* than the one the CDT comparison lives on, and that is the whole tension.

**The sharpest line: the σ→0 UV asymptotic `lim_{σ→0} d_s(σ)` and the energy-resolved `d_s(E)` AT a finite van-Hove energy are DIFFERENT observables.** kk's theorem governs the former. CDT-style dimensional reduction — and S34's [F-4] gate — are read on the latter. Substrate-first: `d_s` is a property of the `D_K` return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber (the fabric IS the spectral triple). kk computes one functional of `P(σ)` — its `σ→0` slope. The CDT observable is a *different* functional of the *same* `P(σ)` — its slope in the diffusion-time *window* `σ ~ 1/E_0²` matched to the fold energy `E_0 = λ_{B2}(τ_fold) ≈ 0.845 M_KK`. These two numbers can differ by an arbitrary amount on one and the same heat kernel; demonstrating one says nothing about the other.

**Does kk's theorem actually bear on the fold-energy `d_s`? No — it answers a different question.** Here is the decisive textual fact, knowledge-MCP-verified from the [F-4] source itself (`session-34-quantum-foam-collab.md` §3.4 lines 100–104):

> "the spectral dimension flows to `d_s → 1` **at the fold energy** … Gate [F-4]: If `d_s` **at the fold energy** differs from the bulk value by `>0.5`, the fold is a spectral dimension transition point."

The gate is phrased twice as "at the fold energy" — never "in the `σ→0` limit." In the textbook 1D case the gate cites, the van-Hove `ρ(E) ~ |E−E_0|^{−1/2}` drives `d_s → 1` **at the fold energy**, while the same 1D system's `σ→0` asymptotic is still the bulk `d=1`. The two coexist precisely *because* they are different observables. So when kk proves `d_s(σ→0) = 8` is van-Hove-immune, that is a true and correct statement about the UV asymptotic — and it is **orthogonal** to the question [F-4] poses. kk's K1 closes the `σ→0` question; [F-4] never asked it.

**On F-K1-ADDITIVITY (`d_s^total = d_s^M4 + d_s^{SU(3)}`).** This is the asymptotic additive law, and as an `σ→0` statement it is correct (Weyl exponents add). But the CDT-2 plateau in CDT is *not* an `σ→0` statement either — in CDT, `d_s(σ)` sits at ~2 over an intermediate window and flows to 4 only in the deep IR. So the additive split, read as an `σ→0` law, does not forbid an intermediate-window dip; it governs the wrong end of the flow. The category-mismatch kk flags ("CDT tested on the fiber, wrong summand") is itself read on the asymptotic; whether CDT-like *intermediate-window* reduction can live in `d_s^{SU(3)}` is exactly the open question, and the additive asymptote is silent on it.

**What I concede to kk up front:** the σ→0 fiber asymptotic is structurally 8 and van-Hove-immune; I will not argue a plateau exists *there*. My claim is narrower and survives kk's K1 intact: the physically decisive CDT-comparable number is `d_s` evaluated in the *fold-energy diffusion window*, which kk's K1 does not compute and his asymptotic theorem does not bound. The structural verdict and the artifact verdict are about *different functionals of `P(σ)`* — and that is why the S52 "no reduction" headline is read on the wrong observable (the UV Weyl asymptotic) for a CDT comparison.

#### Re: K2 — (b) Fold-Energy d_s

**DISAGREE that the S35 card settles the question — it is the WRONG observable, by the framework's own definitions.** kk's K2 substitution chain (Steps 1–5) is correct for the claim it tests: a *finite-energy integrable DOS excess* contributes to `K(σ)` at `O(σ⁰)` and is `σ^{+4}`-suppressed relative to the `σ^{−4}` Weyl leading term, hence cannot move `d_s(σ→0)`. I grant every step. But this proves the UV-immunity of the *asymptotic* `d_s`, which Re:K1 already conceded. The S35 `δd_s ≈ 4×10⁻⁴` card is then deployed as if it measured the fold-energy `d_s`. It did not. I engage it head-on.

**What S35 actually computed (knowledge-MCP-verified, `session-35-connes-spectral-geometer-workshop.md` Re:C7 lines 598–612).** The S35 number is the response of `d_s` to the **BCS gap opening on the BdG operator**, NOT the response of `d_s` to the **van-Hove DOS divergence**. The exact derivation is:

```
d_s(t)      = 2t ⟨λ²⟩_t,  P_BdG(t) = 2 Σ_k e^{−t(λ_k² + |Δ_k|²)}   [S35 eq, BdG]
δP/P        ≈ −8 t Δ_0² / 16 = −(t Δ_0²)/2  for t Δ_0² ≪ 1          [gap perturbation]
δd_s        ≈ 2 t Δ_0² · (8/16) = t Δ_0²                            [S35 line 608]
δd_s(t=1.40)= 1.40 × (0.020 · λ_{B2})²  with Δ_0/λ_{B2}=0.020
            = 1.40 × Δ_0² = 3.87×10⁻⁴                               [S35 line 610]
            ∝ (Δ_0/λ_{B2})² = 4×10⁻⁴                                [S35 line 612]
```

Read the perturbation: the quantity being switched on is `|Δ_k|²` — the BCS gap. The fold's van-Hove DOS divergence (`ρ_B2 = 14.023`, the actual `|E−E_0|^{−γ}` feature) is held *fixed and identical* between the two compared states; only the gap is added. The S35 question (its C7) is verbatim: *"is spectral dimension a sensitive probe of BCS condensation?"* — answer **no**, because `Δ_0 ≪ λ_{B2}` so `(Δ_0/λ_{B2})²` is tiny. That is a true and useful negative result **about condensation**. It is silent about whether the van-Hove DOS feature *itself* produces a fold-energy `d_s` dip, because the van-Hove feature was never the perturbation — it was the unperturbed background common to both terms in `δP`.

The retracted "4/16 = 0.25" estimate (connes, S35 line 257) is telling: connes's original argument was a *DOS / gapped-mode-fraction* count — `δd_s ~ N_gapped/N_total = 4/16`. That estimate was retracted (S35 line 612) *not* because the DOS mechanism is wrong, but because connes had conflated the gap magnitude with the eigenvalue: "the 25% estimate would only apply if the gap were comparable to the eigenvalue itself." So the retraction is specifically a statement that *the gap is small*, not that *the DOS divergence does nothing*. kk inherits the post-retraction number `4×10⁻⁴` and treats it as the fold-energy `d_s` answer. It is the BCS-condensation answer.

**The two observables, made precise.** (i) **S35 / kk-K2 observable**: `δd_s(gap) = d_s[D_BdG, Δ≠0] − d_s[D_BdG, Δ=0]` at fixed background DOS → `(Δ_0/λ_{B2})² ≈ 4×10⁻⁴`. This is the gap sensitivity. (ii) **[F-4] observable**: `d_s(E_0) ≡ −2 d ln P(σ)/d ln σ |_{σ ~ 1/E_0²}` evaluated in the diffusion-time *window matched to the fold energy*, on the *normal-state* spectrum whose DOS carries the van-Hove divergence. These are not the same functional of `P(σ)`, and the S35 computation evaluates (i), not (ii).

**Is a measure-zero DOS spike suppressed in the `σ→0` trace? Yes (kk is right). Is it suppressed in the fold-energy window? No.** This is the quantitative heart. Take kk's own K2 Step 5 and *do not* take `σ→0`; evaluate at the window `σ_* = 1/E_0² = 1/λ_{B2}² ≈ 1.40 M_KK^{−2}`:

```
ΔK_VH(σ_*)/K(σ_*) ~ [c·4√w · e^{−σ_* E_0²}] / K(σ_*)
                  = [c·4√w · e^{−1}] / K(σ_*)            [σ_* E_0² = 1, not →∞]
```

At `σ_* E_0² = 1` the Gaussian weight `e^{−σ_* E_0²} = e^{−1} ≈ 0.368` is `O(1)`, NOT `→0`. kk's `σ^{+4}` suppression factor was `σ^{+4} · e^{−σE_0²}` evaluated *as σ→0*, where both `σ^4→0` and the comparison was against the *divergent* `σ^{−4}` Weyl term. At the fold window, `σ_*` is `O(1)`, the Weyl term is finite, and the van-Hove excess sits at `O(1)` weight on top of it — it is *not* parametrically suppressed. The whole `σ^{+4}` argument is a statement about the `σ→0` *limit*; it evaporates at finite `σ_*`. So kk's K2 conclusion ("the fold cannot shift `d_s`") holds verbatim for `d_s(σ→0)` and fails to constrain `d_s(σ_*)`. kk himself concedes exactly this in F-K2-SCOPE: "the van Hove feature CAN reshape the INTERMEDIATE-σ profile … that is a DIFFERENT observable." Agreed — and the [F-4] gate's literal text ("at the fold energy") *selects that different observable*.

**Be quantitative about how big the window dip can be — and why HOvHS makes it stronger than kk's bound.** kk's bound used the *ordinary* 1D van-Hove form `ρ_VH ~ |λ−E_0|^{−1/2}` (`γ = 1/2`, integrable). But the framework's fold is plausibly a **higher-order van Hove singularity (HOvHS)**, with **power-law** DOS `ρ(E) ~ D_± |E−E_0|^{−γ}`, `γ > 1/2` (Classen–Betouras 2024, my paper 38, Eq. 6; kagome HOvHS reaches `|δE|^{−1/4}`–`|δE|^{−1}`-class exponents). The framework's *own* evidence points to HOvHS, not ordinary VHS: the Pomeranchuk instability `f(0,0) = −4.687 < −3` is **PROVEN** (S22c, knowledge-MCP-verified) — and a Pomeranchuk (forward-scattering) instability at a saddle point is the textbook *signature* of a HOvHS (paper 37, Beidenkopf 2024: HOvHS "precipitates a d-wave Pomeranchuk instability"; paper 38: HOvHS "enhance all susceptibilities … power-law divergent"). For a HOvHS with `γ` approaching 1 the local contribution to `d_s(σ_*)` in the fold window is no longer the bounded `O(σ⁰)` integrable bump but a logarithmically-or-stronger enhanced `⟨λ²⟩_σ` *deficit* near `E_0` — driving `d_s(σ_*)` *down* from the bulk crossing value, in the same direction CDT flows. The magnitude is exactly what [F-4] asks us to compute; the point against kk's K2 is that **the `4×10⁻⁴` number bounds the gap response, not the DOS response, and the DOS response is not the integrable-bump-bounded quantity kk evaluated.**

**Where does fold-energy `d_s` land — CDT ~2 or bulk?** I do not pre-judge (R3 convergence). The honest statement: the bulk crossing at the fold window already passes through ~2.3–2.6 (S52: `d_s(t=0.5)=2.31`; Weyl-window fit 2.6). The CDT comparison asks whether the van-Hove DOS feature *pins* `d_s` near 2 over the window (a plateau/dip), or whether `d_s` merely crosses 2 monotonically (kk's reading). kk's `4×10⁻⁴` does not discriminate these because it measures the gap, not the DOS. The discriminating computation is the [F-4] gate on the normal-state windowed `d_s(σ_*)` at converged `L_max` — which has never been run.

#### Re: K3 — (c) Premature-Comparison

**AGREE on the A/B split — and this is exactly where the seed's tension resolves in Axis-B's favor on the published-framing question.** kk's K3 is the most honest of his four sections, and I endorse F-K3-STRUCTURAL-SETTLED and F-K3-NUMERICAL-PREMATURE *as written*. Claim A (no `σ→0` fiber plateau; `d_s^{SU(3)}→8` Weyl) is settled at the asymptotic level. Claim B (any quantitative `d_s ↔ CDT` comparison at accessible scales) is premature. The S53 `d_s_max = 67.829` artifact (8.5× the manifold dimension, knowledge-MCP-verified `s53_condensed_ds_output.txt`) and the `L_max ≫ 6` truncation caution are jointly decisive on B. We do not disagree on the partition.

**Where I press: the verdict-class for the framework's PUBLISHED position is "indeterminate-pending-compute," NOT a positive "no reduction" — and the premature-numerics point is exactly why.** kk treats Claim A's settledness as licensing the standing S52 headline. But the S52 headline as written — *"No CDT-like dimensional reduction in the internal SU(3) fiber"* (W3-D line 628) — is a **Claim-B-flavored** sentence dressed as a Claim-A theorem. "No CDT-like reduction" is a claim *about the CDT comparison*, and the CDT comparison is (by kk's own F-K3-NUMERICAL-PREMATURE) premature. A reader cannot tell from that sentence that it means only "the `σ→0` asymptotic is Weyl-8 (settled)" and *not* "the fold-energy windowed `d_s` does not approach 2 (uncomputed)." The sentence overclaims by silently extending the settled asymptotic verdict onto the uncomputed windowed observable.

The substrate-first reading is decisive here. `d_s` is a property of the `D_K` return probability; the framework HAS the eigenvalues; what it has NOT done is evaluate the [F-4] functional (normal-state windowed `d_s(σ_* = 1/E_0²)` at converged `L_max`). Until that functional is evaluated, the honest verdict-class is the seed's option (iii): **indeterminate-pending a higher-`L_max` fold-energy heat-kernel gate**, NOT a positive "no reduction." kk's K3 actually *grants* this (his point 1: "should NOT read as a positive 'no reduction' numerical verdict"; the S52 gate is "FAIL+mis-targeted … never was a clean positive"). So on the published-framing question, kk and I converge: the standing headline is too strong; the correct statement is the 3-way split with B held open as indeterminate.

**The one place I will not let "settled" stretch: kk's K3 reads Claim A as covering "no plateau anywhere in the fiber flow."** It does not. K1/K2 prove no plateau *at `σ→0`* and no shift *of the `σ→0` asymptotic*. Neither rules out an intermediate-window dip/plateau at `σ_* ~ 1/E_0²` — which is precisely the CDT signature (CDT's "plateau" is intermediate-window, never `σ→0`). So Claim A, correctly scoped, is "no `σ→0` plateau"; the CDT-relevant claim "no intermediate-window plateau at the fold energy" is part of Claim B and is *indeterminate-pending-compute*. kk's downward-`L_max`-bias argument (truncation pushes `d_s` toward 2, "flattering CDT") is real but cuts both ways: it means the *accessible* numerics cannot be used to argue *against* a CDT-2 fold dip either — they are biased toward 2, so an accessible `d_s ≈ 2.6` is uninformative on whether the converged fold-window `d_s` plateaus at 2 or crosses 8. Premature means premature in both directions; the seed's (iii) is the only defensible verdict-class.

**Convergence with kk on the gate.** I endorse F-K3-GATE: the live discharge is a fold-energy, converged-`L_max`, normal-state windowed `d_s` gate that closes S34 [F-4]. We differ only on the pre-registered *prediction*: kk pins fold-window `d_s ≈ bulk` (`δd_s^fold ≈ 4×10⁻⁴`, gap-anchored); Axis-B pins fold-window `d_s` *below* the bulk crossing value, by an amount set by the HOvHS DOS exponent `γ` (not the gap), with the CDT-2 hypothesis live. Both predictions go into the same pre-registered gate in R3; the gate adjudicates. That is the correct structure: not "no reduction" asserted now, but a number computed against a threshold.

#### Re: K4 — (d) Conditional Cross-Domain

**AGREE the fiber-`σ→0`-`d_s` route to n_s is superseded; the algebra is unimpeachable.** kk's K4 substitution chain is correct: feeding the fiber asymptotic `d_s^{SU(3)}=8` into `n_s−1=(d_s−4)/2` gives `n_s=3` (absurd), and `0.9567` requires `d_s≈3.91` — a near-4D value characteristic of the `M⁴` factor, not the 8D fiber. The Hawking relation `n_s−1 ~ −d(d_s)/dτ` gives ≈0 in the fiber because `d_s^{SU(3)}(σ→0)` is τ-flat to 0.5% (volume-preserving Jensen). I endorse F-K4-SUPERSEDED for the *asymptotic-fiber* route and concur the atlas-07 `η_eff=3.77` and S53 KZ `n_s≈2.06` entries are stale (wrong-SIGN, blue not red) and warrant the seed's Q2 re-scope. (Note: the workshop/seed cite canonical `n_s=0.9567` COMPOUND-NS; knowledge-MCP also carries `n_s_framework=0.9561` — both give `d_s≈3.91` as the relevant near-4 value, so the conclusion is value-robust to that 0.0006 difference.)

**Where I sharpen rather than disagree: kk's K4 inherits the same observable conflation as K1/K2.** The `n_s−1=(d_s−4)/2` and `n_s−1~−d(d_s)/dτ` relations were *never* meant to take the `σ→0` asymptotic `d_s` as input — in their CDT/causal-set provenance they take the **windowed / running** `d_s(σ_*)` at the *physically relevant diffusion scale* (the horizon-matched scale), which flows with the RG/τ. So kk's "the fiber `d_s` feeds the relation to absurdity" is true for the *asymptotic* `d_s` but is the wrong input by construction. The live question — which kk's asymptotic algebra cannot answer — is whether the *windowed* `d_s(σ_* = 1/E_0²; τ)` near the fold, which DOES flow with τ (that is the `δd_s ~ ...` regime), produces a nonzero `−d(d_s^window)/dτ` of the right sign and magnitude for `n_s−1=−0.043`. kk's K4 forecloses this only by setting the input to the τ-flat asymptotic. So the correct statement is: **the asymptotic-fiber `d_s`-route is dead (agreed); the windowed-`d_s`-route is conditional on the [F-4] gate's `d_s(σ_*;τ)` flow and is part of the same indeterminate-pending-compute bucket.** Whether it reproduces or diverges from `0.9567` is decided by the gate, not by the asymptotic algebra.

**This does not threaten the canonical n_s.** I want to be unambiguous against any "framework-undermining" reading: the canonical `n_s` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73), and it stands independent of the `d_s`-route entirely. The `d_s`-route is at best a *cross-check*, at worst superseded. If the [F-4] windowed-`d_s` flow reproduces `0.9567`, that is corroboration; if it diverges, that confirms the `d_s`-route is superseded — *either outcome leaves the canonical n_s untouched* because the canonical value does not derive from `d_s`. This is the constraint-map framing: the gate's outcome moves the status of the `d_s`-route, not the status of n_s.

**Answering kk's three Q-K4 questions (load-bearing; full answers also in L3).**
- **Q-K4a (the decisive observable + the gap):** My "fold-energy `d_s`" is the **windowed** quantity `d_s(σ_* = 1/E_0² ≈ 1.40)`, explicitly NOT the `σ→0` asymptotic (which Re:K1/Re:K2 concede is Weyl-8 and van-Hove-immune). The gap forcing `d_s→∞` is an **IR** (`σ→∞`) effect: `d_s(σ) → 2σ λ_min²` as `σ→∞` (S35 Re:C7 line 596). The fold window `σ_* ≈ 1.40` sits *below* the IR-divergence onset `σ ~ 1/ω_min² = 1/0.82² ≈ 1.49` (S52 line 623–630) — i.e., the fold energy `E_0 = λ_{B2} = 0.845` and the gap edge `ω_min = λ_{B1} = 0.819` are nearly coincident (`m_{B2}/m_{B1}=1.032`, knowledge-MCP-verified). So the window and the gap-onset are *the same scale* to 3%. This is not a problem — it is the physics: the van-Hove feature SITS at the gap edge (B2 fold at the spectral bottom-but-one), which is exactly the configuration where the DOS divergence most strongly shapes the return probability. The gate must evaluate `d_s(σ_*)` precisely in this `σ_* ≈ 1.4` window and disentangle the van-Hove dip (downward, finite) from the gap divergence (upward, sets in just above) — a well-posed finite computation, not an ambiguity.
- **Q-K4b (magnitude, not mechanism):** Conceded that `δd_s(gap) ≈ 4×10⁻⁴`. But that is the GAP response (Re:K2). The mechanism supplying a `d_s` shift of order-one is NOT the gap and NOT the integrable `|λ−E_0|^{−1/2}` bump — it is the **HOvHS power-law DOS** `ρ ~ |E−E_0|^{−γ}` with `γ` potentially `≳ 1/2` (paper 38), whose `⟨λ²⟩_{σ_*}` deficit near `E_0` is NOT the bounded integrable contribution kk's K2 Step-4 evaluated. The framework's PROVEN `f(0,0)=−4.687` Pomeranchuk instability is the diagnostic that the fold IS HOvHS-class (papers 37–38: Pomeranchuk ⟸ HOvHS), so the integrable-bump bound does not apply and the `10⁴×` factor kk demands is supplied by the difference between `γ=1/2` (integrable, bounded) and `γ→1` (power-law, log-or-stronger enhanced) DOS structure.
- **Q-K4c (apples-to-apples bridge map):** I take this head-on in L2. Short answer: the bridge map is the **return-probability functional itself**. CDT's `d_s→2` is *defined* as `−2 d ln P(σ)/d ln σ` on the CDT ensemble's return probability; the framework's fold-window `d_s(σ_*)` is the *same functional* on `D_K`'s return probability. They are the same `d_s` because they are the same operator-functional `P(σ) ↦ d_s(σ)`, evaluated on different `P(σ)`. CDT's reduction is a DOS/return-probability effect (it is *not* a statement about which summand — it is a statement about the *shape of `P(σ)` at intermediate `σ`*). The apples-to-apples comparison therefore does NOT require matching summands; it requires evaluating the *substrate's* `d_s(σ)` the way CDT evaluates *its* `d_s(σ)` — at intermediate `σ`, on the full return probability. The summand-additivity is an `σ→0` red herring for the comparison (L2).

### Part 2: Original Analysis

#### L1: van-Hove steelman — spectral dimension is a DOS-controlled return-probability; fold-energy d_s is decisive

**Substrate-first framing (mandatory, `phononic-framing.md`).** The spectral dimension is a property of the `D_K` heat kernel on the fiber — the fabric IS the spectral triple `(A_K, H_K, D_K)`, and `d_s(σ)` is read off its return probability `P(σ) = Tr e^{−σ D_K²}`. The flat-band / van-Hove condensed-matter results I cite below (papers 37–40) are LABORATORY analogs; the substrate is logically prior. I do NOT claim "the substrate is a flat-band material." I claim the substrate's *own* `D_K` return probability has a DOS structure at the fold that, by the universal return-probability identity, controls a windowed `d_s` feature — and the flat-band literature tells us how strong such features are.

**The return-probability definition and what controls it.** The spectral dimension is the running log-derivative of the diffusion return probability (knowledge-MCP-verified, identical form S31Aa/S35/S44/S52):

```
P(σ) = Tr e^{−σ D_K²} = ∫_0^∞ ρ(λ) e^{−σ λ²} dλ,   ρ(λ) = Σ_k d_k δ(λ−λ_k)   [heat trace as DOS integral]
d_s(σ) = −2 d ln P(σ)/d ln σ = 2σ ⟨λ²⟩_σ,   ⟨λ²⟩_σ = ∫ρ λ² e^{−σλ²} dλ / ∫ρ e^{−σλ²} dλ   [running d_s]
```

The content of this identity: **`d_s(σ)` at a given diffusion time `σ` is a weighted average of `λ²` over the DOS `ρ(λ)`, with Gaussian weight `e^{−σλ²}` peaked at `λ ~ σ^{−1/2}`.** So `d_s(σ)` is DOS-controlled *at the energy scale `E(σ) ≈ σ^{−1/2}`*. This is not analogy — it is the definition. As `σ → 0`, the weight selects the `λ→∞` tail (Weyl, → 8, kk's K1). At a *finite* `σ_*`, the weight selects the DOS *near the energy `E_0 = σ_*^{−1/2}`* — and if `ρ(λ)` has a singularity there, `⟨λ²⟩_{σ_*}` is distorted and `d_s(σ_*)` is pulled away from the smooth-DOS value.

**How a van-Hove DOS singularity produces a local `d_s` feature — the mechanism.** Set `σ_* = 1/E_0²` so the Gaussian weight peaks at the fold energy. Decompose `ρ = ρ_smooth + ρ_VH` where `ρ_VH(λ) = D_± |λ−E_0|^{−γ} 𝟙_{|λ−E_0|<w}`. Then:

```
⟨λ²⟩_{σ_*} = [∫ρ_smooth λ² e^{−σ_*λ²} + ∫ρ_VH λ² e^{−σ_*λ²}] / [∫ρ_smooth e^{−σ_*λ²} + ∫ρ_VH e^{−σ_*λ²}]
```

The van-Hove excess `ρ_VH` is concentrated at `λ ≈ E_0`, exactly where the Gaussian weight peaks (`σ_* E_0² = 1`, weight `e^{−1}`, O(1)). It ADDS DOS weight at `λ² ≈ E_0²` — pulling `⟨λ²⟩_{σ_*}` *toward* `E_0²` and *flattening* its `σ`-dependence over the window where the singularity dominates. A `d_s(σ)` that is *locally flattened* (its `σ`-derivative suppressed) over a finite window is precisely a **plateau/dip** — the CDT signature. The strength of the flattening scales with the *spectral weight* of the singularity relative to the smooth background, i.e. with `D_±` and the exponent `γ`:
- **Ordinary VHS** (`γ = 0` log, or 1D `γ = 1/2`): integrable, bounded excess → *modest* local feature. This is the regime kk's K2 bound assumes, and there the window feature is small (consistent with the bulk crossing being smooth).
- **Higher-order VHS (HOvHS)** (`γ ≳ 1/2`, power-law): the excess DOS weight grows; for `γ → 1` the local `⟨λ²⟩_{σ_*}` deficit/distortion is logarithmically-or-stronger enhanced → a *pronounced* window plateau. This is the regime that can drive `d_s(σ_*)` down toward 2.

**Why the framework's fold is HOvHS-class, not ordinary VHS — substrate evidence.** This is the load-bearing Axis-B physics. Three framework facts, all knowledge-MCP-verified, point to HOvHS:
1. **Pomeranchuk instability `f(0,0) = −4.687 < −3` is PROVEN** (S22c). A forward-scattering Pomeranchuk (nematic) instability at a saddle point is the *experimental fingerprint* of a HOvHS — paper 37 (Beidenkopf 2024, Co₃Sn₂S₂): the HOvHS "precipitates a d-wave Pomeranchuk instability"; paper 38 (Classen–Betouras 2024): HOvHS "enhance all susceptibilities … power-law divergent … superconductivity, density waves, and nematic (Pomeranchuk) orders compete." An *ordinary* log-VHS does not generically drive a Pomeranchuk instability of this strength; a HOvHS does.
2. **The fold is a near-flat-band bottleneck.** The B2 sector is the fold mode with vanishing group velocity (van-Hove `v=0`; knowledge-MCP-verified `Z_wall = ρv = 1/π`, the velocity vanishing exactly compensating the DOS divergence). Paper 38: "Flat bands generically produce HOVHS when perturbed"; the framework's τ-deformation of the SU(3) fiber is exactly such a perturbation of the near-flat B2 branch. The fold is the catastrophe-theoretic `A_2` point (knowledge-MCP-verified, atlas-04), and `A_2` is the *boundary* between ordinary and higher-order singularity structure — the framework sits on the edge where the quadratic coefficient vanishes.
3. **DOS enhancement `ρ_B2(fold) = 14.023`** (knowledge-MCP-verified) — a large finite-`L_max` proxy for the divergence; the continuum `L_max → ∞` limit is where the true power-law `|E−E_0|^{−γ}` emerges (the finite spectrum regularizes it, which is *why* the converged-`L_max` gate is required).

**Therefore the fold-energy `d_s` is the decisive number.** The bulk `σ→0` asymptotic (8) is the wrong observable for CDT (Re:K1). The gap-response `δd_s ≈ 4×10⁻⁴` is the wrong observable for the DOS question (Re:K2). The right observable — the one [F-4] literally specifies ("at the fold energy") — is `d_s(σ_* = 1/E_0²)` on the normal-state spectrum at converged `L_max`, where a HOvHS-class DOS singularity at `E_0 = λ_{B2} = 0.845` flattens the running `d_s` over the fold window. This number has never been computed. It is the physically decisive scale because it is the energy at which the framework's own PROVEN instability (Pomeranchuk) and its cosmogenesis transit (the fold at τ=0.190) both live.

#### L2: CDT's d_s→2 is itself a DOS/return-probability effect — the apples-to-apples comparison

**CDT's `d_s → 2` is defined by exactly the same return-probability functional.** In Causal Dynamical Triangulations and asymptotic safety, the spectral dimension is *not* a manifold dimension and *not* a Weyl exponent — it is the running log-derivative of the diffusion return probability `P(σ)` of a random walker on the (ensemble-averaged) geometry (knowledge-MCP-verified, identical form, `session-19d-tesla-collab.md`: "the spectral dimension `d_s` depends on the mode counting"; same `d_s(σ) = −2 d ln P(σ)/d ln σ`). CDT's headline `d_s(UV) → 2` is an *intermediate-`σ` plateau* in this running quantity — the walker's return probability scales as if in ~2 effective dimensions over a finite window of short-but-not-zero diffusion times, then flows to 4 in the IR. Two structural facts about CDT's number are decisive for the comparison:
1. **CDT's reduction is a property of the *shape of `P(σ)` at intermediate `σ`*, not of any summand decomposition.** It arises because the UV geometry's *mode counting / DOS* over the diffusion modes is anomalous at short scales — the effective DOS of the Laplacian on the fractal-like UV ensemble produces a `P(σ)` that runs at slope ~2. It is a DOS/return-probability effect, full stop.
2. **CDT's `d_s(σ)` is never the `σ→0` mathematical limit of a fixed smooth manifold.** On any fixed smooth `d`-manifold the `σ→0` limit is `d` (Weyl). CDT's 2 is an *emergent intermediate-window* value from the *ensemble*. So comparing the framework's *fixed-manifold `σ→0` Weyl asymptotic* (8) to *CDT's emergent intermediate-window plateau* (2) is a category error — and it is the SAME category error kk's K1 correctly flags ("CDT tested on the wrong object"). But kk resolves it by saying "CDT lives in the `M⁴` summand"; the *correct* resolution is "CDT lives at intermediate `σ`, and the framework's intermediate-`σ` `d_s` was never evaluated at the fold window."

**What observable a fair CDT comparison needs — stated precisely.** The apples-to-apples comparison does NOT compare:
- `d_s^{SU(3)}(σ→0) = 8`  ⟷  `d_s^{CDT}(UV-plateau) = 2`   ✗ (asymptotic-vs-window: category error, kk-K1)
- `δd_s(gap) = 4×10⁻⁴`  ⟷  `d_s^{CDT} = 2`   ✗ (gap-response-vs-DOS-effect: Re:K2)

It compares the *same functional at the same kind of scale* on both sides:

```
FAIR COMPARISON:
   d_s^{substrate}(σ_*) ≡ −2 d ln P(σ)/d ln σ |_{σ = σ_* ~ 1/E_0²}     [substrate, fold-energy window]
        ⟷
   d_s^{CDT}(intermediate-window plateau)                              [CDT, its own intermediate window]

where P(σ) = Tr e^{−σ D_K²} on the NORMAL-STATE D_K spectrum (Δ=0) at converged L_max,
and σ_* = 1/E_0² with E_0 = λ_{B2}(τ_fold) = 0.845 M_KK the van-Hove fold energy.
```

The fair comparison asks: **does the substrate's running `d_s(σ)` exhibit an intermediate-`σ` plateau/dip near the fold energy, the way CDT's running `d_s(σ)` exhibits its UV plateau?** That is a well-posed question about the *shape of `P(σ)`* at finite `σ`, on the substrate's own return probability, and it is the question [F-4] poses. Neither the Weyl asymptotic nor the BCS-gap step answers it.

**The precise specification for the fair-comparison gate (feeds R3 4-field spec).**
- **Observable**: `d_s(σ)` profile on `σ ∈ [0.5, 2.0] M_KK^{−2}` (the fold window, bracketing `σ_* ≈ 1.40` and the `d_s=2` bulk crossing at `σ ≈ 0.42` and the gap-onset at `σ ≈ 1.49`), from `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}` on the normal-state spectrum.
- **DOS resolution**: the van-Hove feature at `E_0 = λ_{B2}` must be resolved — requires converged `L_max` (the S52 `L_max=3`/992-mode truncation does NOT resolve the HOvHS power-law; the finite spectrum smears `|E−E_0|^{−γ}` into the `ρ_B2=14.023` finite proxy). Per the framework's `L_max ≫ 6` caution and the Casimir-bound feasibility protocol (`math-scripts.md`), bound the worst-case sector and report `L_max_operational`.
- **Discriminator**: PLATEAU metric — `max_{σ ∈ window} |d_s(σ) − 2| < threshold_plateau` (CDT-like dip) vs `d_s(σ)` strictly monotone through the window (kk crossing). Pre-register `threshold_plateau` and the monotonicity test.
- **Apples-to-apples pin**: the substrate `d_s(σ_*)` is compared to CDT's *intermediate-window* value (2), NOT CDT's IR value (4) and NOT the manifold dimension — this is the convention pin that makes the comparison fair.

**Bridge map (answering kk's Q-K4c at the structural level).** The bridge map between CDT's `d_s` and the substrate's `d_s` is the **return-probability functional `Φ: P(σ) ↦ −2 d ln P/d ln σ`** itself — a single universal functional applied to two different return probabilities. There is no need to match summands (`M⁴` vs `SU(3)`) because CDT's reduction is not a summand statement; it is a `P(σ)`-shape statement. The substrate-IS reading: the fabric IS the spectral triple, `P(σ)` IS the substrate's return probability, and `Φ[P]` at the fold window IS the substrate's fold-energy spectral dimension. CDT measures `Φ[P_CDT]` at its window; the framework measures `Φ[P_{D_K}]` at the fold window; same `Φ`, compared at the same kind of scale. That is the apples-to-apples bridge, and it routes directly to the [F-4] gate.

#### L3: Questions for kk

I posed answers to your Q-K4a/b/c in Re:K4 (windowed `σ_* = 1/E_0² ≈ 1.40`; HOvHS power-law DOS not gap; return-probability functional as bridge). Three sharp ones back:

**Q-L1 (the observable wedge — does S35 resolve `d_s(E_fold)` or only `d_s(τ)`?):** Your K2 decisive card is the S35 `δd_s ≈ 4×10⁻⁴`. But S35's computation (its eq, lines 598–612) perturbs by the **BCS gap `|Δ_k|²`** at fixed background DOS, and reports `δd_s ∝ (Δ_0/λ_{B2})²` — the response of `d_s` to *condensation*, evaluated at a single `t = 1/λ_{B2}² = 1.40`. It never varies the DOS singularity strength and never reports the *normal-state* (`Δ=0`) windowed `d_s(σ_*)` profile. So: do you agree S35 measured `d_s`'s sensitivity to the *gap* (a τ-flow / condensation observable, `d_s(τ)`-flavored), NOT `d_s` evaluated *at the fold energy on the singular normal-state DOS* (`d_s(E_fold)`)? If you agree, the `4×10⁻⁴` card constrains the gap response and is silent on the [F-4] observable — and your K2 needs a *different* number to close the fold-energy question. If you disagree, point to the line where S35 evaluates normal-state `d_s(σ_*)` with the van-Hove DOS as the *perturbation*.

**Q-L2 (ordinary VHS vs HOvHS — which DOS exponent did your K2 bound assume?):** Your K2 Step-4 bound used `ρ_VH ~ |λ−E_0|^{−1/2}` (the 1D *ordinary* van-Hove form, `γ=1/2`, integrable → bounded `O(σ⁰)` excess). But the framework's PROVEN Pomeranchuk instability `f(0,0) = −4.687 < −3` (S22c) is the laboratory fingerprint of a *higher-order* van-Hove singularity (papers 37–38: Pomeranchuk ⟸ HOvHS, power-law DOS `ρ ~ |E−E_0|^{−γ}`, `γ > 1/2`), and the fold is the `A_2` catastrophe point (atlas-04) at the ordinary/higher-order boundary. For a HOvHS with `γ ≳ 1/2`, the local `⟨λ²⟩_{σ_*}` distortion is no longer the bounded integrable bump your Step-4 evaluated. So: does your `σ^{+4}`-suppression / `O(σ⁰)`-bounded argument survive replacing `γ=1/2` with `γ` set by the *actual* fold catastrophe class — and if you maintain `γ=1/2`, how do you reconcile that with the PROVEN Pomeranchuk instability, which an ordinary log-VHS does not generically produce?

**Q-L3 (the right diffusion-time window — does the additive-Weyl asymptote govern the fold window at all?):** Your F-K1-ADDITIVITY (`d_s^total = d_s^M4 + d_s^{SU(3)}`) and the Weyl exponent are `σ→0` statements. CDT's reduction is an *intermediate-`σ`* plateau, never a `σ→0` limit. The fold window I specify is `σ_* ≈ 1.40`, which is `O(1)` — neither the UV (`σ→0`, Weyl-8) nor the deep IR (`σ→∞`, gap-divergence). Concretely: at `σ_* = 1.40`, does the additive-Weyl asymptotic law even apply, given that (i) the Gaussian weight peaks at the fold energy `E_0 = 0.845` (an `O(1)` eigenvalue, not the `λ→∞` tail), and (ii) the fold window sits *just below* the gap-onset `σ ~ 1/ω_min² = 1.49` where your own IR `d_s→∞` sets in? If the additive-Weyl law is a `σ→0` statement and the CDT-relevant scale is `σ_* ≈ 1.4`, then on what grounds does K1's asymptotic structure constrain the fold-window `d_s` — i.e., what forbids an intermediate-window plateau between the `d_s=2` crossing (`σ ≈ 0.42`) and the gap divergence (`σ ≈ 1.49`), given that this entire window is *outside* the regime your Weyl theorem governs?

---

## Round 2 — kk: Follow-up

### CONVERGENCE

landau's Re:K1 and Re:K2 land a genuine wedge, and I concede the parts that are correct rather than defend ground I do not hold. Substrate-first: `d_s` is a functional of the `D_K` return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber — the fabric IS the spectral triple `(A_K, H_K, D_K)`, and the question is which functional of that one return probability the CDT comparison reads. landau is right that I had not separated them cleanly. I concede four points.

**C-KK-1 — The two-observable distinction is correct and load-bearing.** landau's sharpest line (Re:K1) — that `lim_{σ→0} d_s(σ)` and the energy-resolved `d_s(σ_* = 1/E_0²)` at the finite fold energy are DIFFERENT functionals of the same `P(σ)` — is structurally right, and I accept it without reservation. My K1/K2 theorems govern the σ→0 leading heat-trace exponent (the Weyl/Minakshisundaram–Pleijel asymptotic). The S34 [F-4] gate, by its own literal text, reads `d_s` "at the fold energy" — a windowed slope of `ln P(σ)` near `σ_* ~ 1/E_0²`, NOT the σ→0 limit. These two numbers can differ by an arbitrary amount on one and the same heat kernel (the textbook 1D van-Hove case is exactly such an instance: `d_s(E_0) → 1` at the fold while `d_s(σ→0) = d = 1` bulk — they coexist precisely because they are different functionals). My K1 closes the σ→0 question. landau is correct that [F-4] never asked it. I retract any implication in K1/K3 that "no σ→0 plateau" covers "no intermediate-window plateau at the fold energy."

**C-KK-2 — S35 measured the GAP response, not the DOS response. I concede the misattribution.** I re-read the S35 provenance (`session-35-connes-spectral-geometer-workshop.md` Re:C7, the derivation landau quotes at lines 222–235; knowledge-MCP-verified `δd_s = t·Δ_0²`, `Δ_0/λ_{B2} = 0.020`). The substitution chain is unambiguous: the perturbation switched on is `|Δ_k|²` — the BCS gap on the BdG operator — and the result `δd_s ∝ (Δ_0/λ_{B2})² ≈ 4×10⁻⁴` is the response of `d_s` to *condensation* at FIXED background DOS. The van-Hove DOS divergence (`ρ_B2 = 14.023`) is the common, unperturbed background in both compared states; it is differenced AWAY in `δP/P`. S35's own Proposition D states the result verbatim: "spectral dimension is NOT a sensitive probe of BCS condensation." That is a true negative result *about the gap*. My K2 deployed `4×10⁻⁴` as if it bounded the fold-energy windowed `d_s`. It does not. The `4×10⁻⁴` card constrains `δd_s(gap) = d_s[Δ≠0] − d_s[Δ=0]`; the [F-4] observable is `d_s(σ_*)` on the *normal-state* (`Δ=0`) singular DOS. These are distinct functionals of `P(σ)`, and S35 evaluated the wrong one for the fold-energy question. F-K2-MAGNITUDE is withdrawn as an answer to [F-4]; it stands only as the (correct) statement that condensation does not move `d_s`.

**C-KK-3 — My σ^{+4}-suppression is a σ→0 statement and does not bound the windowed observable.** landau's arithmetic at line 239–246 is correct and I verified it directly. My K2 Step-5 suppression factor was `σ^{+4} · e^{−σ E_0²}` evaluated *as σ→0*, where `σ^4 → 0` and the comparison was against the *divergent* `σ^{−4}` Weyl term. At the fold window `σ_* E_0² = 1`, the Gaussian weight is `e^{−1} ≈ 0.368` — O(1), not →0 — and the Weyl term is finite, not divergent. The van-Hove excess sits at O(1) weight on top of a finite background; it is NOT parametrically suppressed there. The entire `σ^{+4}` argument is a UV-limit statement; it evaporates at finite `σ_*`. I had already half-conceded this in F-K2-SCOPE ("the van Hove feature CAN reshape the INTERMEDIATE-σ profile … a DIFFERENT observable"); landau is right that [F-4]'s "at the fold energy" text *selects* that different observable, so the concession is not a footnote — it is the gate.

**C-KK-4 — The published "no CDT-like reduction" headline overclaims; the honest verdict-class is indeterminate-pending-compute.** This is the central convergence and I state it plainly. The standing S52 headline — *"No CDT-like dimensional reduction in the internal SU(3) fiber"* (W3-D line 628) — is a sentence *about the CDT comparison*, and the CDT comparison is read on the windowed observable, which has never been computed at converged `L_max` on the normal-state spectrum. My own K3 already granted that the S52 gate was FAIL+mis-targeted and "never was a clean positive." Combining that with C-KK-1/2/3: the headline silently extends the SETTLED σ→0 asymptotic verdict (`d_s^{SU(3)} → 8`, Weyl, van-Hove-immune — which I still defend, see DISSENT) onto the UNCOMPUTED fold-energy windowed observable. A reader cannot tell from the headline that it means only "the σ→0 asymptotic is Weyl-8" and not "the fold-window `d_s(σ_*)` does not approach 2." On the published-framing question, landau and I converge: the correct statement is the seed's option (iii) — **indeterminate-pending a converged-`L_max`, normal-state, fold-energy heat-kernel `d_s` gate** — not a positive "no reduction." I endorse F-K3-NUMERICAL-PREMATURE applying to the windowed observable, and I withdraw the implicit licensing of the standing headline.

**What this convergence does NOT concede** (carried to DISSENT): that the windowed `d_s(σ_*)` *will* dip to ~2; that the fold is a higher-order van Hove singularity in the *energy* variable the heat kernel integrates; or that `δd_s(gap) ≈ 4×10⁻⁴` being the wrong observable means the right observable is large. "Uncomputed" is not "large." The gate adjudicates; I bring a quantitative finite-σ floor that bounds the dip from below.

### DISSENT

I hold two positions against landau, both with NEW arguments (not restatements of K1–K4). The first is settled and non-negotiable; the second is a quantitative bound that limits the windowed dip from below — it does not pre-judge the gate, but it forbids the CDT-2 outcome unless a specific, checkable structural fact obtains.

**D-KK-1 (settled, defended): `d_s^{SU(3)}(σ→0) = 8` is a theorem, and the additive split holds at the UV.** landau concedes this (Re:K1, Re:K2). I restate only to fix scope for the verdict: the σ→0 Weyl asymptotic equals `dim(SU(3)) = 8` exactly, τ-independent (volume-preserving Jensen), van-Hove-immune. No new argument needed; landau and I agree. This is Claim A, and it remains SETTLED. The dissent that follows is entirely about whether the WINDOWED `d_s(σ_*)` can dip to ~2 — Claim B.

**D-KK-2 (NEW — the decisive structural objection): landau's HOvHS argument conflates two orthogonal axes. The "infinite-order van Hove" is in the τ-MODULUS direction; the heat-kernel `d_s` integrates the ENERGY spectrum at FIXED τ, where the substrate's own computations use the ORDINARY γ=1/2 form.** This is my central R2 dissent and it is grounded in the framework's own data, not my preference.

The framework carries TWO superficially-conflicting characterizations of the fold's van-Hove order, and the resolution decides the gate:

- The B2 sector is classified "Infinite-order Van Hove — PROVEN (S22c)" (`Classification-of-phonon-exflation.md`, knowledge-MCP-verified). This is what landau's HOvHS argument leans on (his L1, L2, Re:K2, Q-L2).
- BUT the operative DOS the framework actually *computes with* at the fold is the **ordinary 1D square-root form** `g(ω) ~ 1/√(ω − ω_min)` (γ = 1/2; knowledge-MCP-verified `atlas-03-equation-flow.md`), and the PROVEN impedance theorem `Z_wall = ρv = [1/(π|v|)]·v = 1/π` (knowledge-MCP-verified `session-32-w4-r2-qa-landau.md`) is built on `ρ ~ 1/(π|v|)`, stated explicitly as "the correct 1D density of states for a quadratic band extremum (or flat-band bottleneck)."

These are not in contradiction once the axis is identified. Substitution chain (`math-scripts.md` Double-Check Logic):

```
Claim to test: "the B2 'infinite-order van Hove' makes the heat-kernel
                d_s(σ_*) DOS-singular with exponent γ ≳ 1/2 (HOvHS)."

Step 1 (definition — what the heat kernel integrates).
   P(σ) = ∫_0^∞ ρ_E(λ) e^{−σ λ²} dλ,  ρ_E(λ) = Σ_k d_k δ(λ − λ_k)   [at FIXED τ]
   d_s(σ_*) = 2σ_* ⟨λ²⟩_{σ_*}, weighted by ρ_E(λ) — the ENERGY-axis DOS.
   [source: P(σ)=Tr e^{−σD_K²}, knowledge-MCP-verified S52 line 612; landau L1 line 287]

Step 2 (definition — what "infinite-order van Hove" refers to).
   The B2 fold has v_{B2} = dE_{B2}/dτ = 0  (knowledge-MCP-verified
   session-34-quantum-foam-collab.md, session-34-baptista-collab.md "the van
   Hove singularity lives exactly at the fold center"). The vanishing velocity
   — and hence the "infinite-order" / A_2-catastrophe character — is a property
   of the eigenvalue flow λ(τ) in the MODULUS τ, NOT of the energy density
   ρ_E(λ) at fixed τ.
   [source: v_{B2}=dE_{B2}/dτ; atlas-04 "A_2 catastrophe" structural stability]

Step 3 (the two axes are different operators).
   ρ_τ-singularity:  measures how the eigenvalue moves as τ varies   (moduli axis)
   ρ_E-singularity:  measures how many eigenvalues sit near energy E (energy axis)
   The heat kernel P(σ) at FIXED τ_fold sees ρ_E ONLY. The infinite-order
   character lives in ρ_τ. Z_wall = ρ_E · v = 1/π is FINITE precisely because
   ρ_E ~ 1/(π|v|) is the ORDINARY γ=1/2 quadratic-extremum form (the divergence
   in ρ_E and the v→0 cancel EXACTLY to a finite impedance — knowledge-MCP-
   verified, PROVEN WALL gate, ρ=14.02, Z=1.016).

Step 4 (substitute into d_s(σ_*)).
   With the energy-axis DOS at γ = 1/2 (ordinary, integrable), kk-K2 Step-4's
   bound applies at the ENERGY axis: ρ_VH(λ) ~ |λ−E_0|^{−1/2}, integrable,
   ΔK_VH(σ_*) bounded. The windowed distortion of ⟨λ²⟩_{σ_*} is the MODEST
   ordinary-VHS feature, NOT the log-or-stronger HOvHS deficit landau's L1
   bullet (γ→1) requires.

Conclusion: landau's HOvHS-driven CDT-2 dip requires γ ≳ 1/2 in ρ_E (the
            energy-axis DOS). The framework's "infinite-order" lives in ρ_τ
            (the moduli axis), and its energy-axis DOS is the PROVEN ordinary
            γ=1/2 form (Z_wall=1/π demands it). The HOvHS argument imports the
            moduli-axis order onto the energy axis — a category error analogous
            to the summand error I made in K1. γ=1/2 in ρ_E ⇒ kk-K2 Step-4's
            integrable-bump bound DOES apply to the windowed observable, and the
            order-one dip landau needs is not supplied by the substrate's own DOS.
```

This is the quantitative answer to landau's Q-L2. I maintained `γ = 1/2` in K2 Step-4 NOT by oversight but because that is the energy-axis DOS the framework's own impedance theorem (Z = 1/π, PROVEN) forces — and Z finite is *incompatible* with a power-law γ > 1/2 energy-DOS, because then `ρ_E · v ~ |v|^{−(2γ−1)} → ∞` and the impedance would diverge, contradicting the PROVEN WALL result. landau cannot have BOTH the finite-Z WALL theorem AND an energy-axis HOvHS; they are mutually exclusive on the same fold. (The "infinite-order" in the τ-modulus axis is real and uncontested — it is the A_2 catastrophe of the fold geometry — but the heat kernel at fixed τ_fold does not integrate over τ, so it never sees that order.)

**D-KK-3 (NEW — the Pomeranchuk fingerprint is itself strong-coupling-superseded, so it cannot anchor a LIVE energy-axis HOvHS claim).** landau's L1/Q-L2 cites the PROVEN Pomeranchuk `f(0,0) = −4.687 < −3` (S22c) as the laboratory fingerprint that the fold is HOvHS-class. But the open-channel record (knowledge-MCP-verified, `permanent-results-registry.md` S22c F-1) reads: "**RESOLVED by S75 W4-K**: `g·N(0) = 3.24` is algebraic (permanent). `f(0,0) = −4.687` is correct perturbative math but `E_J/E_cond = 25` places physical system in strongly-coupled regime." So the `−4.687` value is *perturbative* and the physical system is strong-coupling, where the perturbative Landau-parameter formula is superseded by the self-consistent calculation (S75 W4-K — authored by landau himself). The Pomeranchuk *instability* (the qualitative fact `f < −3`) survives; the *value* `−4.687` does not transfer as a quantitative HOvHS-strength anchor without the strong-coupling caveat. landau's papers 37–38 (Pomeranchuk ⟸ HOvHS) establish a *correlation* in laboratory materials; they do not establish that the substrate's *energy-axis* DOS exponent is γ > 1/2 — and D-KK-2 shows the substrate's own Z = 1/π theorem forbids it. A Pomeranchuk instability is also generically a *moduli/forward-scattering* statement (a Fermi-surface deformation), consistent with the τ-axis infinite-order reading, NOT evidence for an energy-axis power-law DOS.

**D-KK-4 (NEW — a finite-σ floor: the realized return probability does NOT dip to 2 at the accessible window; it bottoms at d_s ≈ 6.3).** Even granting landau the windowed observable as the right functional, the framework has *already* evaluated the running `d_s(σ)` minimum on the actual (gapped, fold-bearing) spectrum, and it does not approach 2. Knowledge-MCP-verified, `s19a_false_vacuum_analysis_verdict.txt`: **`d_s_min = 6.3091` (spectral dimension minimum at σ = 1)**. The σ = 1 evaluation point sits squarely inside landau's fold window `[0.5, 2.0]` (his L2 spec; `σ_* ≈ 1.40`). The *minimum* of the running `d_s` over the window — the value most favorable to a CDT-2 dip — is 6.3, not 2. This is a direct measurement of the windowed observable's floor on the available spectrum, and it lands `3.15×` above the CDT-2 hypothesis. landau will (correctly) note this is finite-`L_max` and that the converged HOvHS could in principle lower it — but D-KK-2 removes the HOvHS mechanism on the energy axis, and the truncation bias (S52: truncation pushes the apparent `d_s` *downward*, toward 2.6 not 8) means the converged value moves *up* from 6.3 toward 8, AWAY from 2. The accessible windowed floor (6.3) plus the upward-converging truncation bias is a positive structural argument that the windowed `d_s(σ_*)` cannot reach 2. (Caveat I flag honestly: the `6.3091` is the *full-D_K* running-`d_s` minimum, not a fold-energy-windowed extraction on the converged normal-state spectrum — so it is a strong indicator, not the [F-4] number. The gate must confirm it survives the windowed extraction.)

**Net dissent position.** Claim A (σ→0 Weyl-8) settled (agreed). On Claim B (windowed `d_s(σ_*)` → 2): I no longer claim it is *settled* (CONVERGENCE C-KK-4), but I claim it is *bounded away from 2* by three checkable facts — (D-KK-2) the energy-axis DOS is γ=1/2 by the PROVEN Z=1/π theorem, not HOvHS; (D-KK-3) the Pomeranchuk anchor is strong-coupling-superseded and is a moduli/forward-scattering statement, not energy-axis γ > 1/2; (D-KK-4) the realized running-`d_s` floor is `d_s_min = 6.3091`, with truncation bias pushing it up. The gate should compute `d_s(σ_*)`, but my pre-registered prediction is `d_s(σ_*) ≳ 6` (near the accessible floor, converging up), NOT CDT-2. If landau can exhibit an energy-axis (not moduli-axis) DOS exponent γ > 1/2 *consistent with* the finite Z = 1/π WALL theorem, I will withdraw D-KK-2; that is the crux the gate must settle.

### EMERGENCE

The synthesis is sharper than either Round-1 position and it isolates a single, well-posed, finite computation as the adjudicator. Substrate-first: there is ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber; the entire dispute is which functional of it the CDT comparison reads, and whether that functional dips to 2.

**E-KK-1 — The fair-comparison observable, agreed and pinned.** landau's L2 specification is correct and I adopt it: the apples-to-apples CDT comparison is NOT `d_s^{SU(3)}(σ→0)=8` vs `d_s^{CDT}=2` (asymptotic-vs-window category error, my K1) and NOT `δd_s(gap)=4×10⁻⁴` vs `2` (gap-vs-DOS error, landau's Re:K2). It is the SAME functional `Φ: P(σ) ↦ −2 d ln P/d ln σ` evaluated at the SAME kind of scale on both sides — `Φ[P_{D_K}]` at the fold window `σ_* ~ 1/E_0²` on the normal-state spectrum, vs `Φ[P_CDT]` at CDT's intermediate window. landau's bridge map (the return-probability functional itself, answering my Q-K4c) is the correct one: no summand-matching is needed because CDT's reduction is a `P(σ)`-shape statement at intermediate σ, not a summand statement. I withdraw the "CDT lives in the M⁴ summand" resolution of K1 as the *primary* framing — it is true at σ→0 but, as landau notes (Re:K1 last ¶), silent on the intermediate window where CDT actually lives.

**E-KK-2 — The two readings make OPPOSITE, pre-registrable predictions for the same gate.** This is the productive emergence: the gate is not a fishing expedition; it discriminates two sharp hypotheses.
- **Reading KK (mine):** `d_s(σ_*) ≳ 6` at the accessible window, converging UP toward 8 as `L_max → ∞`. Mechanism: energy-axis DOS is ordinary γ=1/2 (Z=1/π theorem), windowed distortion is the bounded integrable-bump feature, the realized running-`d_s` floor is `6.3091`. Crossings, not plateaus, all the way down.
- **Reading landau (van-Hove):** `d_s(σ_*)` dips toward ~2 over the fold window, flattened by an energy-axis HOvHS power-law DOS (γ ≳ 1/2). A plateau/dip, the CDT signature.
The gate's discriminator is landau's L2 PLATEAU metric: `min_{σ∈window} d_s(σ)` near 2 with a flat region (landau) vs `d_s(σ)` monotone with windowed minimum ≳ 6 (kk). These cannot both hold; the converged-`L_max` normal-state `d_s(σ)` profile decides it.

**E-KK-3 — What the converged-`L_max` fold-energy gate must compute (4-field spec, feeds R3).** I align with landau's L2 spec and add the energy-vs-moduli-axis disambiguation as a mandatory sub-check:
1. **What**: normal-state (`Δ=0`) running `d_s(σ) = −2 d ln P(σ)/d ln σ` profile on `σ ∈ [0.5, 2.0] M_KK^{−2}` (brackets `σ_* ≈ 1.40`, the `d_s=2` bulk crossing at `σ ≈ 0.42`, and the gap-onset at `σ ≈ 1.49`), from `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}` at `τ_fold = 0.190`.
2. **Inputs**: converged-`L_max` D_K spectrum at `τ_fold` (the S52 `L_max=3`/992-mode cache does NOT resolve the energy-axis DOS shape at `E_0 = λ_{B2} = 0.845`; per `math-scripts.md` Casimir-bound feasibility protocol, bound the worst-case sector and report `L_max_operational` vs `L_max_plan`). The B2 fold energy `E_0 = 0.845`, gap edge `ω_min = 0.819` (`m_{B2}/m_{B1} = 1.032`, near-coincident — the window and gap-onset are the same scale to 3%).
3. **Gate (pre-registered, two-sided)**: PLATEAU metric `min_{σ∈[0.5,2.0]} d_s(σ)` vs threshold. **Reading landau PASS**: `min d_s(σ) < 3` (CDT-like dip, within ~1 of the CDT-2 value) AND a flat region (windowed `d/dlnσ` of `d_s` ≈ 0). **Reading KK PASS**: `min d_s(σ) > 5` AND `d_s(σ)` strictly monotone (no plateau; floor near the `6.3091` accessible value). Band `3 ≤ min d_s ≤ 5`: INDETERMINATE — neither reading confirmed, finer `L_max` scan required. PLUS a **mandatory axis sub-check**: fit the energy-axis DOS exponent `γ_E` from `ρ_E(λ) ~ |λ − E_0|^{−γ_E}` near `E_0` on the converged spectrum, and cross-check against the impedance constraint `Z = ρ_E · v` (finite Z ⟺ γ_E = 1/2). If `γ_E > 1/2` AND Z stays finite, D-KK-2 is refuted and Reading landau's mechanism is live; if `γ_E = 1/2` (Z=1/π preserved), D-KK-2 holds and the windowed dip is bounded.
4. **Effort**: ~1.0 wave-equivalents (one converged-`L_max` heat-trace evaluation + log-log windowed-slope fit + DOS-exponent fit near `E_0`; reuses the S52/S53 `d_s` pipeline at higher `L_max`). Discharges S34 [F-4] (INFORMATIVE since S34, never closed).

**E-KK-4 — Either gate outcome leaves the canonical `n_s = 0.9567` untouched (cross-domain closure).** I fully endorse landau's Re:K4 framing. The asymptotic-fiber `d_s`-route to `n_s` is dead (agreed — `d_s=8` → `n_s=3` absurd; `n_s=0.9567` needs `d_s≈3.91`, an M⁴-factor value). The *windowed* `d_s(σ_*; τ)`-route is conditional on the same [F-4] gate (does `−d(d_s^window)/dτ` have the right sign/magnitude for `n_s−1 = −0.043`?) and lives in the same indeterminate-pending-compute bucket. But the canonical `n_s` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A) and does NOT derive from `d_s` — so the gate moves the STATUS of the `d_s`-route (corroboration if it reproduces 0.9567, superseded if it diverges), not the status of `n_s`. The atlas-07 `η_eff = 3.77` and S53 KZ `n_s ≈ 2.06` entries remain wrong-sign (blue, not red) stale artifacts flagged for the seed's Q2 re-scope, independent of the gate outcome. This is the constraint-map reading, and it is the honest one.

### QUESTIONS

**Answers to landau's L3 (Q-L1, Q-L2, Q-L3).**

**A-to-Q-L1 (does S35 resolve `d_s(E_fold)` or only `d_s(τ)`?) — I AGREE, fully.** S35 measured `d_s`'s sensitivity to the *gap* `|Δ_k|²` (a condensation observable), NOT `d_s` evaluated at the fold energy on the singular normal-state DOS. I read the S35 eq (lines 222–235) the same way you do: the perturbation is `|Δ_k|²`, the result is `δd_s ∝ (Δ_0/λ_{B2})²`, and the van-Hove DOS is the common unperturbed background, differenced away. The `4×10⁻⁴` card constrains the gap response and is silent on the [F-4] observable. I cannot point to a line where S35 evaluates normal-state `d_s(σ_*)` with the van-Hove DOS as the perturbation, because there is none. K2's deployment of `4×10⁻⁴` as the fold-energy answer is withdrawn (CONVERGENCE C-KK-2). My K2 needs a *different* number — the windowed normal-state extraction — which is exactly the [F-4] gate.

**A-to-Q-L2 (ordinary VHS vs HOvHS — which exponent did K2 assume?) — I maintain γ=1/2, on the ENERGY axis, and the reconciliation with Pomeranchuk is the axis distinction.** My K2 Step-4 used `γ = 1/2` deliberately, because that is the *energy-axis* DOS the framework's PROVEN impedance theorem `Z_wall = ρ_E·v = 1/π` forces (D-KK-2 substitution chain). Finite Z is incompatible with an energy-axis power-law `γ > 1/2`: then `ρ_E·v ~ |v|^{−(2γ−1)} → ∞` and Z diverges, contradicting the PROVEN WALL result (ρ=14.02, Z=1.016). I reconcile this with the PROVEN Pomeranchuk by the axis distinction: the "infinite-order van Hove" (S22c) and the Pomeranchuk instability are *moduli-axis / forward-scattering* statements (the eigenvalue flow `v_{B2} = dE/dτ = 0`, the A_2 catastrophe in τ), NOT energy-axis DOS-exponent statements. The heat kernel at fixed `τ_fold` integrates the energy DOS only, where γ=1/2 holds. So my bound survives — *for the heat-kernel observable* — precisely because the infinite-order character lives on an axis the heat kernel does not see. Additionally (D-KK-3): the `−4.687` value is strong-coupling-superseded (S75 W4-K), so it is not a quantitative anchor for energy-axis γ even if one tried to import it. **The crux I concede is checkable**: IF the converged-`L_max` gate fits an energy-axis `γ_E > 1/2` near `E_0` *while* Z stays finite, D-KK-2 is refuted and your mechanism is live. I have pinned that as the mandatory axis sub-check in E-KK-3.

**A-to-Q-L3 (does the additive-Weyl asymptote govern the fold window at all?) — NO, and I concede the point with a correction to my K1.** You are right: at `σ_* ≈ 1.40` the Gaussian weight peaks at `E_0 = 0.845` (an O(1) eigenvalue, not the `λ→∞` tail), so the additive-Weyl asymptotic law — a σ→0 statement — does NOT govern the fold window. My F-K1-ADDITIVITY constrains the wrong end of the flow for the CDT comparison; I withdraw it as a bound on the windowed observable (it remains correct at σ→0). What I do NOT concede is that "outside the Weyl regime" means "unconstrained": the fold window is bounded by TWO computed facts that are not Weyl-asymptotic — the realized running-`d_s` floor `d_s_min = 6.3091` at σ=1 (D-KK-4, *inside* your window), and the energy-axis γ=1/2 from the Z=1/π theorem (D-KK-2). So the window is governed not by the Weyl asymptote but by the *actual spectrum's* DOS structure and its realized return-probability floor — both of which sit far above 2. What forbids an intermediate-window plateau at 2 is therefore not K1's asymptotic structure (you are right it does not reach there) but the energy-axis DOS being ordinary-γ and the measured floor being 6.3. The Weyl theorem governs σ→0; the finite-σ floor + Z-theorem govern the window; neither admits a dip to 2 absent an energy-axis HOvHS that the WALL theorem forbids.

**Sharp questions for landau (R3 verdict + gate design).**

**Q-K5a (the Z=1/π ⟺ γ_E=1/2 lock — the decisive crux):** The PROVEN WALL gate gives finite acoustic impedance `Z = ρ_E·v = 1/π` at the fold, which (D-KK-2 Step 3) requires the energy-axis DOS to be the ordinary `ρ_E ~ 1/(π|v|)` quadratic-extremum form, i.e. `γ_E = 1/2`. A higher-order energy-axis singularity `γ_E > 1/2` would drive `Z → ∞` (`ρ_E·v ~ |v|^{−(2γ_E−1)}`). Your CDT-2 dip requires `γ_E > 1/2` on the *energy* axis. So: can you exhibit an energy-axis HOvHS (`γ_E > 1/2`) that is *consistent with* the PROVEN finite `Z = 1/π`? If finite Z and energy-axis HOvHS are mutually exclusive (as the substitution chain says), then the PROVEN WALL theorem already forecloses the CDT-2 windowed dip — and the [F-4] gate's axis sub-check (E-KK-3) becomes a confirmation, not an open question. If you can reconcile them, that reconciliation IS your strongest R3 argument and I want to see it explicitly.

**Q-K5b (moduli-axis vs energy-axis — which one does CDT's plateau live on?):** CDT's intermediate-window `d_s` plateau is a property of the return probability `P(σ)` at FIXED ensemble geometry (it is read at fixed couplings, scanning diffusion time σ — the energy/diffusion axis). The substrate's infinite-order van Hove lives on the τ-MODULUS axis (`dE/dτ = 0`). These are different axes. So even granting that CDT-2 is a return-probability effect (which I now accept, E-KK-1): is the substrate analog of CDT's plateau read by (i) scanning σ at fixed `τ_fold` — the energy-axis heat kernel, where γ_E=1/2 and the floor is 6.3 — or (ii) scanning τ through the fold — the moduli axis, where the infinite-order character lives? If CDT's plateau is the σ-scan (i), the substrate's infinite-order (axis ii) is irrelevant to it and D-KK-2 bites. If you claim the substrate analog is the τ-scan (ii), then it is NOT the same functional as CDT's σ-scan, and the apples-to-apples bridge (E-KK-1) breaks — you'd be comparing a moduli flow to a diffusion flow. Which axis carries the fair comparison?

**Q-K5c (the floor — does `d_s_min = 6.3091` survive the windowed normal-state extraction?):** The framework's realized running-`d_s` minimum is 6.3091 at σ=1 (inside your `[0.5, 2.0]` window). I read this as a near-decisive floor: the most CDT-favorable point of the *actual* return probability bottoms at 6.3, not 2, and truncation bias pushes it up toward 8. You will note it is a full-D_K extraction, not the fold-energy-windowed normal-state number. Concretely: what is the mechanism by which the windowed extraction at converged `L_max` could push `min d_s(σ)` from 6.3 DOWN to ~2 — a factor-3 drop — given that (a) the energy-axis DOS is γ=1/2 (Z theorem), (b) truncation bias moves `d_s` UP not down, and (c) the gap-onset just above the window pushes `d_s → ∞` (S52 line 630), not toward 2? If no such mechanism survives D-KK-2's removal of energy-axis HOvHS, then the gate's Reading-KK PASS (`min d_s > 5`, monotone) is the structurally-favored outcome and the verdict-class should record that — indeterminate-pending-compute, but with the pre-registered prediction asymmetrically favoring `d_s ≳ 6`.

**Q-K5d (verdict-class wording for R3):** We converge that the published "no CDT-like reduction" headline overclaims (C-KK-4) and the honest class is indeterminate-pending the [F-4] gate. For the R3 Workshop Verdict table: do you accept the *split* — Claim A (σ→0 Weyl-8, no σ→0 plateau) = SETTLED/Converged; Claim B (windowed `d_s(σ_*) → 2`) = INDETERMINATE-pending-compute, with two pre-registered opposite predictions (kk: `≳6`, monotone, via γ_E=1/2; landau: `≲3`, plateau, via energy-axis HOvHS) discriminated by the E-KK-3 gate? If so, that is the structural verdict the workshop produces: not "no reduction" (overclaim) and not "reduction" (uncomputed), but a settled asymptotic + a pre-registered two-sided gate on the windowed observable, with the energy-vs-moduli axis sub-check as the mechanism discriminator.

---

## Round 2 — landau: Cross-Synthesis

### CONVERGENCE

kk's R2 is the right kind of move: it conceded the two-observable wedge in full (C-KK-1/2/3/4) and then mounted a genuinely new, structurally-grounded objection (D-KK-2/Q-K5a) instead of relitigating the conceded ground. That is exactly how an adversarial review should sharpen. I accept four of kk's R2 positions, and I want to be precise about which — because the place I dissent (DISSENT below) is narrow and the convergence is broad.

**Substrate-first frame (mandatory).** There is ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber — the fabric IS the spectral triple `(A_K, H_K, D_K)`. `d_s` is a functional of that one `P(σ)`. The flat-band / van-Hove condensed-matter results I cite (papers 37–40) are LABORATORY analogs; the substrate is logically prior. The entire dispute is (a) which functional of `P(σ)` the CDT comparison reads, and (b) whether that functional dips toward 2 at the fold window. kk and I now agree on (a) completely; (b) is the open gate.

**C-L1 — The S35 `4×10⁻⁴` card is withdrawn as the [F-4] answer; I accept kk's C-KK-2 in full.** kk re-read the S35 provenance and conceded (A-to-Q-L1, C-KK-2) that `δd_s ∝ (Δ_0/λ_{B2})² ≈ 4×10⁻⁴` is the response of `d_s` to the **BCS gap** `|Δ_k|²` at fixed background DOS — a *condensation* observable, with the van-Hove DOS differenced away — NOT the normal-state windowed `d_s(σ_*)` the [F-4] gate specifies. This is the wedge I pressed in Re:K2 and Q-L1, and kk granted it without reservation. We converge: the `4×10⁻⁴` number bounds the gap response and is silent on the fold-energy observable. F-K2-MAGNITUDE is correctly retired as an answer to [F-4]. I note this is the honest move — it removes kk's most-cited card from the table on *both* our readings.

**C-L2 — The `σ^{+4}`-suppression is a σ→0 statement and does not bound the windowed observable (C-KK-3, accepted).** kk verified my arithmetic at lines 239–246 directly and conceded that the suppression factor `σ^{+4}·e^{−σE_0²}` evaluated as σ→0 evaporates at the fold window where `σ_* E_0² = 1` and the Gaussian weight is `e^{−1} ≈ 0.368` = O(1). The van-Hove excess sits at O(1) weight on a finite background, not parametrically suppressed. We agree. This is the same concession as C-KK-1 (the σ→0 vs windowed functional distinction) viewed quantitatively, and it is correct.

**C-L3 — The published "no CDT-like reduction" headline overclaims; the honest verdict-class is indeterminate-pending-compute (C-KK-4, the central convergence).** This is the load-bearing agreement and it does not move under D-KK-2. kk endorsed F-K3-NUMERICAL-PREMATURE applying to the windowed observable and withdrew the implicit licensing of the standing S52 headline (W3-D line 628). We converge that the headline silently extends the SETTLED σ→0 asymptotic verdict (`d_s^{SU(3)} → 8`, Weyl, van-Hove-immune — D-KK-1, which I conceded in Re:K1/Re:K2 and do NOT contest) onto the UNCOMPUTED fold-energy windowed observable. The correct verdict-class is the seed's option (iii): **indeterminate-pending a converged-`L_max`, normal-state, fold-energy heat-kernel `d_s` gate.** Crucially — and this is what survives kk's strongest dissent — *this convergence holds regardless of how the gate resolves.* Even if kk's D-KK-2/D-KK-4 are entirely correct and the gate returns `d_s(σ_*) ≳ 6`, the standing headline is STILL an overclaim *as written*, because it asserts the result of a comparison that had not been computed. "Indeterminate, asymmetrically favoring d_s≳6" is a different (and honest) verdict-class than "no reduction (settled)." kk's D-KK-2 sharpens the *prediction within* the indeterminate class; it does not restore the headline.

**C-L4 — The two-sided pre-registered gate is the correct resolution (E-KK-2/E-KK-3, accepted with the axis sub-check).** kk adopted my L2 fair-comparison observable and bridge map (E-KK-1: the functional `Φ: P(σ) ↦ −2 d ln P/d ln σ` applied to both return probabilities, no summand-matching needed) and proposed a two-sided gate with opposite pre-registered predictions (E-KK-2). I accept this structure in full. I also accept kk's addition of the **mandatory energy-axis γ_E sub-check** (E-KK-3 item 3) — fitting `ρ_E(λ) ~ |λ−E_0|^{−γ_E}` near `E_0` on the converged spectrum and cross-checking against the impedance — as the correct mechanism discriminator. This sub-check is the right way to settle D-KK-2/Q-K5a *empirically* rather than by assertion from either side. I refine the gate threshold and the impedance cross-check in EMERGENCE, because (DISSENT) I believe kk's stated form of the impedance lock is not quite right — but the *structure* (two-sided gate + axis sub-check) is exactly correct and I adopt it.

**C-L5 — `d_s_min = 6.3091` is a real floor indicator, and I concede its weight honestly (D-KK-4, partially accepted).** kk's D-KK-4 cites the realized running-`d_s` minimum `d_s_min = 6.3091` (knowledge-MCP-verified, `s19a_false_vacuum_analysis_verdict.txt`; gate `T3-S19A-FALSE-VACUUM-ANALYSIS` INFO, S81). I will NOT wave this away. It is a direct evaluation of a windowed running-`d_s` minimum on the actual fold-bearing spectrum, and it bottoms at 6.3, a factor 3.15 above CDT-2. This is genuine evidence on kk's side and the constraint-map honesty requires me to say so. My narrow dissent (DISSENT) is about its *provenance scope*, not its existence: it is the τ-scan minimum at `τ_at_ds_min = 0.900` (knowledge-MCP-verified, same file), σ=1, full-D_K PW-weighted — NOT the fold-energy-windowed normal-state extraction at `τ_fold = 0.190` that [F-4] specifies. kk flags this caveat himself (D-KK-4 last sentence: "the gate must confirm it survives the windowed extraction"). So we agree it is a strong indicator that must be confirmed by the windowed gate; we differ only on whether the converged windowed number can sit materially below it. That difference is what the gate computes.

**What I do NOT concede** (carried to DISSENT, sharpened): kk's D-KK-2 claim that the PROVEN finite impedance `Z = 1/π` *pins* the energy-axis DOS exponent to `γ_E = 1/2` and therefore *forecloses* any energy-axis HOvHS at the fold. I will show (DISSENT) that (i) the impedance product `Z = ρ_E·v_g` is constant — finite — for an *entire family* of band-edge exponents, so finite Z does NOT lock γ_E=1/2; and (ii) the framework's OWN computation gives `Z_{B2}(τ_fold) = 0` at the exact fold center (not 1/π), which is the n→∞ flat-band signature, the OPPOSITE of the ordinary-γ lock kk asserts. The 1/π value is the *off-fold smooth-wall* impedance, not the fold-center value. So the Q-K5a lock, as stated, does not hold — but I will be equally clear that defeating the *lock* does not manufacture a *dip*; the magnitude remains the gate's to decide, with the floor evidence (C-L5) genuinely live.

### DISSENT

kk's Q-K5a is the crux and I will answer it head-on, quantitatively, the way the directive demands: **can I exhibit an energy-axis DOS exponent `γ_E > 1/2` CONSISTENT with the PROVEN finite impedance `Z = 1/π`?** My answer is **yes** — the Q-K5a lock as stated does not hold, for two independent reasons, both grounded in the framework's own data. But I will be equally honest that defeating the lock does not manufacture the dip: the magnitude is the gate's to decide, and the realized-floor evidence (C-L5) is genuinely on kk's side. I do not overclaim. I defend exactly one thing: that the WALL theorem does NOT asymmetrically foreclose the CDT-2 outcome the way D-KK-2 claims.

**D-L1 (NEW, decisive against the Q-K5a lock): the impedance product `Z = ρ_E·v_g` is FINITE for an ENTIRE FAMILY of band-edge exponents — finite Z does NOT pin `γ_E = 1/2`.** This is the substitution chain kk's D-KK-2 Step 3 skipped. Write the energy-axis DOS and group velocity for a band edge with dispersion `E − E_0 ~ |k|^n` (in the continuum k-integral at fixed τ), following the standard 1D van-Hove construction (the same construction that produces kk's cited `ρ_wall = 1/(π|v|)`, knowledge-MCP-verified `session-32-baptista-collab.md`):

```
Claim to test: "finite Z = ρ_E · v_g  ⇒  γ_E = 1/2 on the energy axis." (D-KK-2 / Q-K5a)

Step 1 (dispersion). Band edge at E_0, leading behavior  E(k) − E_0 = A|k|^n,  A>0, n≥2.
   [n=2 = ordinary quadratic extremum; n>2 = higher-order; n→∞ = flat band]

Step 2 (group velocity).  v_g(k) = dE/dk = nA|k|^{n−1}
   Invert: |k| = ((E−E_0)/A)^{1/n}  ⇒  v_g(E) = nA^{1/n}(E−E_0)^{(n−1)/n} = nA^{1/n}(E−E_0)^{1 − 1/n}.

Step 3 (energy-axis DOS, 1D).  ρ_E(E) = (1/π)|dk/dE| = (1/π)/v_g(E)
   = (1/π) · (1/n)A^{−1/n}(E−E_0)^{−(1 − 1/n)} = (1/(πn))A^{−1/n}(E−E_0)^{−(1 − 1/n)}.
   ⇒  ρ_E(E) ~ |E−E_0|^{−γ_E}  with  γ_E = 1 − 1/n.
   [n=2 ⇒ γ_E = 1/2 (kk's ordinary form); n=4 ⇒ γ_E = 3/4; n→∞ ⇒ γ_E → 1 (flat band)]

Step 4 (the impedance PRODUCT — the decisive cancellation).
   Z(E) = ρ_E(E) · v_g(E)
        = [(1/(πn))A^{−1/n}(E−E_0)^{−(1−1/n)}] · [nA^{1/n}(E−E_0)^{(1−1/n)}]
        = (1/π) · (E−E_0)^{[−(1−1/n)] + [(1−1/n)]}
        = (1/π) · (E−E_0)^{0}
        = 1/π    — INDEPENDENT of n.

Conclusion: Z = ρ_E · v_g = 1/π is FINITE and EQUALS 1/π for EVERY band-edge
            exponent n ≥ 2, i.e. for the ENTIRE family γ_E = 1 − 1/n ∈ [1/2, 1).
            The (E−E_0)^{−(1−1/n)} divergence in ρ_E and the (E−E_0)^{+(1−1/n)}
            vanishing in v_g cancel EXACTLY for any n. Finite Z does NOT select
            γ_E = 1/2; it is satisfied identically across all HOvHS exponents.
```

This is the exact structural fact the knowledge MCP records: `Z_wall = ρ·v = [1/(π·v)]·v = 1/π` (knowledge-MCP-verified, `session-32-w4-r2-qa-landau.md`), whose own gloss is "the DOS divergence (ρ ~ 1/(π·v)) and the velocity vanishing (v → 0) cancel exactly." Read it carefully: `ρ = 1/(πv)` is *written as a function of v*, and the cancellation is `ρ·v = 1/π` for *whatever* `v` is. It is the SAME cancellation for `v ~ |E−E_0|^{1/2}` (n=2) as for `v ~ |E−E_0|^{3/4}` (n=4) as for `v → 0` (flat band). **The impedance theorem proves the product is 1/π; it says NOTHING about the exponent `γ_E` separately.** kk's D-KK-2 Step 3 asserts "`ρ_E ~ 1/(π|v|)` is the ORDINARY γ=1/2 quadratic-extremum form" — but `ρ_E ~ 1/(π|v|)` is the form for *every* n; only `v(E)`'s own exponent distinguishes them, and the impedance product is exponent-blind by construction. **Q-K5a answered: γ_E ∈ [1/2, 1) is consistent with finite Z = 1/π for the entire family. The lock does not exist.** kk's stated implication `Z finite ⇒ γ_E = 1/2` is the algebra error of reading a *product* constraint as a *factor* constraint.

**D-L2 (NEW, decisive: the framework's OWN computation gives `Z(τ_fold) = 0` at the fold center — the n→∞ flat-band signature, the OPPOSITE of the ordinary-γ lock).** kk's D-KK-2 treats `Z = 1/π` as the fold-center value. It is not. The 1/π value is the **off-fold smooth-wall** impedance, where `v` is small-but-finite (the smooth-wall LDOS model, knowledge-MCP-verified `session-32-baptista-collab.md` §2.2 "W-32b: Sound, But the LDOS Model Is Simplified"). At the EXACT fold center, the framework computes something different — knowledge-MCP-verified, `session-40-tesla-collab-addendum.md`:

```
Z_{B2}(τ_fold) = ρ_DOS · v_{B2} = 0     [because v_{B2} = 0 exactly at the fold center]
```

This is the load-bearing structural fact kk's D-KK-2 omits. The framework's own ledger carries TWO impedance values: `Z = 1/π ≈ 0.318` (off-fold, smooth-wall, finite v) and `Z_{B2}(τ_fold) = 0` (fold-center, v_{B2}=0). The impedance does not stay pinned at 1/π through the fold — it *drops to zero* at the center. In the family of D-L1, `Z = 1/π·(E−E_0)^0` holds for finite n; but the limit `n → ∞` (the flat band) is exactly where the construction degenerates and `Z → 0`, because the flat band has `v_g ≡ 0` over a finite k-range and the DOS becomes a delta function, not a power law. **`Z_{B2}(τ_fold) = 0` is the substrate telling us the fold center is the `n → ∞` / flat-band end of the family — `γ_E → 1`, the strongest HOvHS, NOT the ordinary `γ_E = 1/2`.** This is corroborated independently by the FLAT BAND PROTECTION theorem (knowledge-MCP-verified, `s61_vanhove_dispersion_output.txt`): `H_B2(k, τ) = H_B2(0, τ) + E_J(τ)·λ_k·I_4` — the B2 band's k-dispersion enters ONLY through `E_J(τ)·λ_k`, and at the fold the B2 sector is the flat optical band (4 degenerate modes at `E_B2 = 0.845`, knowledge-MCP-verified `session-38-naz-tesla-workshop.md`). A flat band IS `n → ∞`. So the substrate's own fold-center impedance (zero) and its flat-band protection theorem BOTH place the fold at the `γ_E → 1` end of the D-L1 family — which is precisely the regime my L1/L2 HOvHS argument requires, and the *opposite* of the ordinary-γ regime D-KK-2 asserts.

**This is the explicit reconciliation kk asked for (A-to-Q-L2 last sentence: "that reconciliation IS your strongest R3 argument and I want to see it explicitly").** Energy-axis HOvHS (`γ_E → 1`) is not merely *consistent with* the impedance data — it is *required* by the framework's own `Z_{B2}(τ_fold) = 0` computation, because the only member of the D-L1 family with vanishing fold-center impedance is the flat-band `n → ∞` / `γ_E → 1` end. The finite `Z = 1/π` kk cites is the off-fold value and lives at finite n; it does not govern the fold center.

**D-L3 (the axis question, answered — D-KK-2's energy-vs-moduli split is real but cuts the OTHER way).** kk's D-KK-2 is right that there are two axes — the τ-modulus axis (`v_{B2} = dE_{B2}/dτ = 0`, the A_2 catastrophe, knowledge-MCP-verified `session-34-baptista-collab.md`, `session-53-baptista-volovik-workshop.md` "a property of the Dirac eigenvalue flow λ(τ)") and the energy axis (`ρ_E(λ)` at fixed τ, what the heat kernel integrates). I concede the "infinite-order van Hove" (S22c) label is primarily a τ-modulus / catastrophe-theoretic statement, and I was imprecise in Re:K2/Q-L2 to lean on it as though it were directly the energy-axis exponent. **But the energy-axis DOS at the fold is ALSO singular with `γ_E → 1`, independently of the τ-axis order — because of the flat-band k-structure (D-L2), not because of the τ-flow.** The two facts are: (i) τ-axis: `v_{B2} = dE/dτ = 0` (A_2 catastrophe, kk's correct reading); (ii) energy-axis: the B2 band is k-flat (FLAT BAND PROTECTION, `E_J → 0` limit), so the *continuum-k* energy DOS `ρ_E(λ)` at fixed τ has a near-delta-function pile-up at `E_B2 = 0.845` — `γ_E → 1`, the flat-band limit of D-L1. These are different mechanisms on different axes, and BOTH point to strong-singularity behavior at the fold. kk's D-KK-2 establishes that the τ-axis infinite-order does not *automatically* transfer to the energy axis — correct, and I concede I had conflated them — but it does NOT establish that the energy axis is ordinary-γ, because the *independent* flat-band k-structure makes the energy axis singular too. The Q-K5b question ("which axis carries the fair comparison?") has a clean answer: **the energy/diffusion axis (σ-scan at fixed τ_fold)** — same as CDT (which scans diffusion time at fixed ensemble couplings). And on that axis, the flat B2 band gives `γ_E → 1`, not 1/2. So the fair comparison lives on the energy axis (agreeing with kk's Q-K5b framing that CDT is an σ-scan), and the energy axis is singular at the fold via the flat-band structure (disagreeing with D-KK-2's claim that the energy axis is ordinary-γ).

**D-L4 (D-KK-3 conceded in part, but it does not rescue the lock).** kk's D-KK-3 is correct that the Pomeranchuk *value* `f(0,0) = −4.687` is strong-coupling-superseded (knowledge-MCP-verified, S75 W4-K, which I authored: `g·N(0) = 3.24` is the permanent algebraic content; `E_J/E_cond = 25` places the system strong-coupled; the perturbative `−4.687` does not transfer as a quantitative anchor). I concede this fully — I should not have cited `−4.687` as a *quantitative* HOvHS-strength anchor, and I withdraw that specific number. **But D-KK-3 does not rescue the Q-K5a lock**, for two reasons. First, my D-L1/D-L2 energy-axis HOvHS argument does NOT depend on the Pomeranchuk value at all — it rests on the impedance algebra (D-L1) and the framework's `Z_{B2}(τ_fold)=0` + flat-band protection (D-L2), neither of which cites `f(0,0)`. The Pomeranchuk citation was a *corroborating laboratory analogy* (papers 37–38), never the load-bearing step; removing it leaves D-L1/D-L2 intact. Second, kk's own framing of D-KK-3 ("a Pomeranchuk instability is generically a moduli/forward-scattering statement") actually *supports* the D-L3 picture: the instability lives on the moduli/forward-scattering axis, the energy-axis singularity lives on the flat-band k-structure, and they are independent — exactly the two-axis decomposition I now adopt. The qualitative fact that survives (`g·N(0) = 3.24`, the instability is real) is consistent with a flat-band-driven strong singularity; it neither proves nor is needed for the energy-axis `γ_E`.

**Net dissent position (honest, bounded).** I defend exactly one claim against kk's R2: **the Q-K5a lock `Z finite ⇒ γ_E = 1/2` does NOT hold** — finite Z = 1/π is satisfied by the entire HOvHS family `γ_E ∈ [1/2, 1)` (D-L1, exact algebra), and the framework's own fold-center impedance `Z_{B2}(τ_fold) = 0` plus the FLAT BAND PROTECTION theorem place the fold at the `γ_E → 1` flat-band end (D-L2), not the ordinary `γ_E = 1/2`. Therefore the WALL theorem does NOT asymmetrically foreclose the CDT-2 windowed dip; D-KK-2 is refuted *as a lock*. **What I do NOT claim**: that the dip *will* reach 2. The magnitude depends on (i) how strong the energy-axis singularity is at finite, converged `L_max` (the continuum `γ_E → 1` is regularized by the finite spectrum — `ρ_B2 = 14.023` is the finite proxy, knowledge-MCP-verified — so the realized `γ_E` is between 1/2 and 1 and the gate must fit it); (ii) whether the windowed `d_s(σ_*)` actually flattens given the gap-onset at `σ ~ 1/ω_min² = 1.49` sits just above the window and pushes `d_s → ∞` (kk's correct point); and (iii) whether the realized floor `d_s_min = 6.3091` (C-L5) drops materially under the *windowed normal-state* extraction at `τ_fold`. My honest pre-registered prediction: the windowed `d_s(σ_*)` sits **below** the bulk crossing value and below the un-windowed floor 6.3, driven down by the flat-band energy-axis singularity, with CDT-2 *live but not guaranteed*. kk's prediction (`≳ 6`, monotone) and mine (`≲ 3`, plateau) are both pre-registered against the same gate. **The crux kk named is exactly right and now has a sharp empirical form: the gate must fit `γ_E` near `E_0` on the converged spectrum.** If `γ_E = 1/2` (ordinary) AND the windowed floor stays ≳ 6, kk wins and D-KK-2 is vindicated empirically (the lock was wrong but the conclusion right). If `γ_E → 1` (flat-band, as `Z_{B2}(τ_fold)=0` predicts) AND the windowed `d_s` dips below 3, I win. The gate adjudicates; neither of us asserts the answer now.

### EMERGENCE

The R2 exchange has done what an adversarial review should: it has converged the dispute onto a SINGLE, well-posed, finite computation whose pre-registered two-sided gate discriminates two sharp mechanisms — and, decisively, it has identified the exact sub-quantity (`γ_E` near `E_0`, cross-checked against the impedance) that adjudicates kk's D-KK-2/Q-K5a crux empirically rather than by assertion. Substrate-first: ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber; one functional `Φ: P(σ) ↦ −2 d ln P/d ln σ`; the gate evaluates `Φ[P]` at the fold window and fits the energy-axis DOS exponent. I state the converged design, refining kk's E-KK-3 with the corrected impedance cross-check from D-L1/D-L2.

**E-L1 — The fair-comparison functional Φ at the fold window (agreed, pinned).** kk and I converge fully (E-KK-1, my L2) that the apples-to-apples CDT comparison is the SAME functional `Φ` at the SAME kind of scale on both sides:

```
FAIR COMPARISON (converged):
   d_s^{substrate}(σ_*) ≡ Φ[P_{D_K}](σ_*) = −2 d ln P(σ)/d ln σ |_{σ_* ~ 1/E_0²}     [substrate, fold window]
        ⟷
   d_s^{CDT}(intermediate-window plateau) = Φ[P_CDT](σ_CDT-window)                    [CDT, its own window]

   P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}  on the NORMAL-STATE (Δ=0) spectrum at converged L_max,
   σ_* = 1/E_0²,  E_0 = λ_{B2}(τ_fold) = 0.845 M_KK  (knowledge-MCP-verified).
```

No summand-matching is needed (the bridge map IS `Φ`; CDT's reduction is a `P(σ)`-shape statement at intermediate σ, not a summand statement — kk withdrew the "M⁴ summand" framing as primary, E-KK-1). The comparison is to CDT's *intermediate-window* value (~2), NOT its IR value (4) and NOT the manifold dimension (8) — that is the convention pin that makes it fair.

**E-L2 — The two readings make OPPOSITE pre-registered predictions (agreed, E-KK-2), and the mechanism behind each is now precisely localized.**
- **Reading KK:** `d_s(σ_*) ≳ 6` at the accessible window, converging UP toward 8 as `L_max → ∞`. Mechanism: energy-axis DOS ordinary `γ_E = 1/2`; windowed distortion is the bounded integrable-bump feature; realized running-`d_s` floor `6.3091` (D-KK-4); truncation bias pushes UP. Crossings, not plateaus.
- **Reading landau:** `d_s(σ_*) ≲ 3`, flattened over the fold window by an energy-axis flat-band singularity `γ_E → 1` (D-L1/D-L2: the fold-center `Z_{B2}(τ_fold) = 0` and FLAT BAND PROTECTION place the fold at the `n → ∞` / `γ_E → 1` end of the impedance family). A plateau/dip — the CDT signature.
- The predictions are mutually exclusive on the same `P(σ)`; the converged-`L_max` normal-state profile decides it.

**E-L3 — The γ_E vs Z=1/π sub-check, CORRECTED (this is the load-bearing refinement of E-KK-3 item 3).** kk's E-KK-3 proposed: fit `γ_E` near `E_0`, and "finite Z ⟺ γ_E = 1/2; if γ_E > 1/2 AND Z finite, D-KK-2 refuted." The first clause is wrong (D-L1: finite Z holds for ALL `γ_E ∈ [1/2, 1)`), and correcting it sharpens the sub-check into a genuinely decisive test:

```
CORRECTED IMPEDANCE SUB-CHECK (replaces E-KK-3 item-3 biconditional):
  (1) Fit the energy-axis exponent γ_E from ρ_E(λ) ~ |λ − E_0|^{−γ_E} near E_0 = λ_{B2}
      on the converged normal-state spectrum (continuum-k DOS at fixed τ_fold).
  (2) Independently compute the dispersion order n from E_{B2}(k) − E_0 ~ |k|^n
      (the FLAT BAND PROTECTION form H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4 gives n via E_J).
  (3) Consistency identity (D-L1):  γ_E = 1 − 1/n,  and  Z = ρ_E·v_g = 1/π for ALL n  (off-fold),
      with Z → 0 as n → ∞ at the fold center (D-L2, matching Z_{B2}(τ_fold) = 0).
  (4) DISCRIMINATOR:
        γ_E ≈ 1/2 (n ≈ 2, ordinary quadratic extremum)  ⇒  Reading KK mechanism;  windowed dip bounded; floor ≳ 6 expected.
        γ_E → 1   (n large, flat-band, as Z_{B2}(τ_fold)=0 predicts)  ⇒  Reading landau mechanism;  dip live.
  NOTE: finite Z = 1/π is NOT itself the discriminator (it holds for the whole family);
        the DISCRIMINATOR is the fitted γ_E / n directly, with Z_{B2}(τ_fold) = 0 as the
        flat-band corroborant. The impedance is a CONSISTENCY CHECK on the fit, not a lock.
```

This converts kk's Q-K5a from an *asserted lock* ("Z forces γ_E=1/2") into a *measured exponent* (fit `γ_E` directly; Z confirms internal consistency). It is the honest resolution: neither side asserts `γ_E`; the converged-`L_max` spectrum's DOS near `E_0` is fit and reported. If the fit gives `γ_E ≈ 1/2`, kk's conclusion is empirically vindicated (even though his lock-argument was algebraically wrong); if `γ_E → 1`, my flat-band reading is vindicated. The framework's `Z_{B2}(τ_fold) = 0` is a prior pointing toward the latter, but the fit settles it.

**E-L4 — Converged gate (4-field spec, feeds R3 verdict + S34 [F-4] discharge).** I align with kk's E-KK-3 and add the corrected sub-check:
1. **What**: normal-state (`Δ=0`) running `d_s(σ) = −2 d ln P(σ)/d ln σ` profile on `σ ∈ [0.5, 2.0] M_KK^{−2}` (brackets `σ_* ≈ 1.40`, the `d_s=2` bulk crossing at `σ ≈ 0.42`, and the gap-onset at `σ ≈ 1.49`), from `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}` at `τ_fold = 0.190`. PLUS the energy-axis DOS exponent `γ_E` fit near `E_0 = λ_{B2}` per E-L3.
2. **Inputs**: converged-`L_max` D_K spectrum at `τ_fold` — the S52 `L_max=3`/992-mode cache does NOT resolve the energy-axis DOS shape at `E_0 = 0.845` (the finite spectrum smears `|E−E_0|^{−γ_E}` into the `ρ_B2 = 14.023` finite proxy; this is WHY converged `L_max` is required). Per `math-scripts.md` Casimir-bound / Friedrich-Bär feasibility protocol, bound the worst-case sector and report `L_max_operational` vs `L_max_plan` (the bottom-of-spectrum B2 fold at `E_0 = 0.845` is well below the operative `|λ|` ceiling, so a modest converged `L_max` suffices — the D_K block-diagonality makes this feasible without full reconstruction). Gap edge `ω_min = λ_{B1} = 0.819`, `m_{B2}/m_{B1} = 1.032` (knowledge-MCP-verified) — the window and gap-onset coincide to 3%, so the windowed extraction must disentangle the van-Hove dip (downward) from the gap divergence (upward, just above).
3. **Gate (pre-registered, two-sided + axis sub-check)**:
   - PLATEAU metric `min_{σ∈[0.5,2.0]} d_s(σ)`. **Reading landau PASS**: `min d_s(σ) < 3` (CDT-like, within ~1 of CDT-2) AND a flat region (windowed `d(d_s)/d ln σ ≈ 0` over a finite sub-window). **Reading KK PASS**: `min d_s(σ) > 5` AND `d_s(σ)` strictly monotone (no plateau; floor near `6.3091`). Band `3 ≤ min d_s ≤ 5`: INDETERMINATE (finer `L_max` scan).
   - **Mandatory axis sub-check** (E-L3, corrected): fit `γ_E` near `E_0`; report `n = 1/(1−γ_E)`; cross-check `Z_{B2}(τ_fold)` (expect → 0 if flat-band). `γ_E ≈ 1/2` corroborates Reading KK; `γ_E → 1` corroborates Reading landau. The fitted `γ_E` is the mechanism verdict regardless of the `min d_s` band.
4. **Effort**: ~1.0 wave-equivalents (one converged-`L_max` heat-trace evaluation + log-log windowed-slope fit + DOS-exponent fit near `E_0`; reuses the S52/S53 `d_s` pipeline at higher `L_max`). Discharges S34 [F-4] (INFORMATIVE since S34, never closed, knowledge-MCP-verified `session-34-quantum-foam-collab.md`).

**E-L5 — The settled / open split for the R3 verdict (answering kk's Q-K5d).** I accept kk's proposed split:
- **Claim A — `d_s^{SU(3)}(σ→0) = 8`, no σ→0 plateau (Weyl/MP asymptotic, τ-flat, van-Hove-immune): SETTLED / CONVERGED.** D-KK-1, conceded by me in Re:K1/Re:K2. Not in dispute.
- **Claim B — windowed `d_s(σ_*) → 2` at the fold: INDETERMINATE-pending the E-L4 gate**, with two pre-registered opposite predictions (kk: `≳6`, monotone, via `γ_E=1/2`; landau: `≲3`, plateau, via flat-band `γ_E→1`) discriminated by the corrected axis sub-check.
- **The published-framing verdict (C-L3/C-KK-4): the standing "no CDT-like reduction" headline OVERCLAIMS** — it asserts a windowed comparison that was never computed. The honest verdict-class is the seed's option (iii): indeterminate-pending-compute, holding regardless of how the gate resolves.
- **One refinement I add to Q-K5d**: kk's D-KK-2 framing ("the gate asymmetrically favors `d_s ≳ 6`") is the prediction *under kk's reading*; my D-L1/D-L2 show the WALL theorem does NOT supply that asymmetry (the lock does not exist, and `Z_{B2}(τ_fold)=0` points the other way). So the verdict should record the gate as **genuinely two-sided** (not asymmetrically pre-loaded toward either reading by any *proven* wall), with the realized-floor `6.3091` as kk's strongest *empirical* prior and the flat-band `Z_{B2}(τ_fold)=0` + `γ_E→1` as landau's strongest *structural* prior. The fitted `γ_E` adjudicates.

**E-L6 — Either gate outcome leaves the canonical `n_s = 0.9567` untouched (cross-domain closure, agreed E-KK-4).** I fully endorse the closure. The asymptotic-fiber `d_s`-route to `n_s` is dead (agreed: `d_s=8 → n_s=3` absurd; `0.9567` needs `d_s≈3.91`, an M⁴-factor value; knowledge-MCP-verified the value is robust to the `n_s=0.9567` COMPOUND-NS vs `n_s_framework=0.9561` difference). The *windowed* `d_s(σ_*; τ)`-route is conditional on the same E-L4 gate (`−d(d_s^window)/dτ` sign/magnitude for `n_s−1 = −0.043`) and lives in the same indeterminate bucket. But canonical `n_s` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A) and does NOT derive from `d_s` — so the gate moves the STATUS of the `d_s`-route (corroboration if it reproduces `0.9567`, superseded if it diverges), not the status of `n_s`. The atlas-07 `η_eff = 3.77` and S53 KZ `n_s ≈ 2.06` entries are wrong-sign (blue, not red) stale artifacts flagged for the seed's Q2 re-scope, independent of the gate. This is the constraint-map reading, and it is the honest one: the gate is high-value (discharges S34 [F-4], settles a 10-session-old open question, discriminates two sharp mechanisms) and *zero-risk to the canonical n_s* either way.

---

## Round 3 — kk: Follow-up

### CONVERGENCE

landau's D-L1 lands a clean refutation of my Q-K5a/D-KK-2 lock, and the honest move is to concede it without reservation rather than defend an algebra error. Substrate-first: there is ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber — the fabric IS the spectral triple `(A_K, H_K, D_K)` — and the entire dispute has now collapsed to a single MAGNITUDE question (does the windowed functional `Φ[P_{D_K}](σ_*)` dip to ~2) on which neither of us holds a proven wall. I concede three points and lock in the converged verdict-class.

**C-KK-5 — ACCEPTED: the `Z = ρ_E·v_g = 1/π` impedance lock does NOT pin `γ_E = 1/2`. My Q-K5a/D-KK-2 biconditional was wrong, and I withdraw it.** landau's D-L1 substitution chain is the step my D-KK-2 Step 3 skipped, and it is exactly right. I verified the cancellation symbolically (Sage, exact): for a band edge `E(k) − E_0 = A|k|^n`, the energy-axis DOS is `ρ_E ~ |E−E_0|^{−(1−1/n)}` and the group velocity is `v_g ~ |E−E_0|^{+(1−1/n)}`, so the impedance product is

```
Z = ρ_E · v_g = (1/(πn))A^{−1/n}(E−E_0)^{−(1−1/n)} · nA^{1/n}(E−E_0)^{(1−1/n)}
              = (1/π) · (E−E_0)^{[−(1−1/n)] + [(1−1/n)]}
              = (1/π) · (E−E_0)^0 = 1/π     [Sage-exact, INDEPENDENT of n]
```

The diverging exponent in `ρ_E` and the vanishing exponent in `v_g` cancel EXACTLY for every `n ≥ 2`, i.e. across the whole HOvHS family `γ_E = 1 − 1/n ∈ [1/2, 1)`. `Z = 1/π` is a **product** constraint, invariant along the family; I read it as a **factor** constraint that fixes `γ_E = 1/2`. It does not. The PROVEN WALL theorem `Z = 1/π` says nothing about `γ_E` separately — only `v(E)`'s own exponent distinguishes the family members, and the impedance product is exponent-blind by construction. D-KK-2 is refuted *as a lock*. This is the honest concession: my strongest R2 structural objection rested on an algebra error, and removing it removes any *proven*-wall asymmetry that would have pre-loaded the gate toward my reading.

**C-KK-6 — ACCEPTED: `Z_{B2}(τ_fold) = 0` is the fold-CENTER value, distinct from the off-fold `1/π`, and it is a genuine structural prior toward `γ_E → 1`.** I had treated `Z = 1/π` as the fold-center value; landau's D-L2 corrects this with the framework's own ledger — `Z = 1/π ≈ 0.318` is the off-fold smooth-wall impedance (finite `v`), while `Z_{B2}(τ_fold) = ρ_DOS·v_{B2} = 0` at the exact center because `v_{B2} = 0` there (knowledge-MCP-verified, `session-40-tesla-collab-addendum.md`). In the D-L1 family `Z = (1/π)(E−E_0)^0` holds at finite `n`, but the `n → ∞` flat-band limit is exactly where the construction degenerates and `Z → 0` (the DOS becomes a delta-function pile-up, not a power law). The FLAT BAND PROTECTION form `H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4` corroborates the B2 sector as the (near-)flat optical band at `E_B2 = 0.845`. So `Z_{B2}(τ_fold) = 0` points the energy-axis DOS toward the `γ_E → 1` flat-band end, NOT the ordinary `γ_E = 1/2`. I accept this as landau's strongest *structural* prior for the gate. It is a prior, not a proof of the dip (the continuum `γ_E → 1` is regularized by the finite spectrum to some realized `γ_E ∈ [1/2, 1)`, which only the converged-`L_max` fit settles) — but it is a legitimate, framework-sourced prior, and it removes the asymmetry my lock had falsely claimed.

**C-KK-7 — ACCEPTED: the windowed fold-energy `d_s` magnitude is genuinely OPEN; the two-sided pre-registered gate (E-L4) is the correct resolution; `n_s = 0.9567` is untouched either way.** With the lock gone, I no longer assert any *proven* structural barrier to a CDT-2 windowed dip. The magnitude is the gate's to decide. I adopt landau's E-L4 converged gate in full — including the CORRECTED axis sub-check (E-L3): the discriminator is the directly-fitted energy-axis exponent `γ_E` near `E_0`, with the impedance as a CONSISTENCY CHECK (`Z = ρ_E·v_g = 1/π` for the whole family; `Z_{B2}(τ_fold) → 0` as the flat-band corroborant), NOT as a lock. And I reaffirm E-KK-4/E-L6: the canonical `n_s = 0.9567` (Bogoliubov-invariant, COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A; value-robust to the `n_s_framework = 0.9561` difference, both giving the relevant `d_s ≈ 3.91` M⁴-factor value) does NOT derive from `d_s` — so the gate moves the STATUS of the `d_s`-route, not the status of `n_s`, in either outcome.

**The final agreed verdict-class.** The dispute is now MAGNITUDE-only and resolves to a clean three-way split:
- **Claim A** — `d_s^{SU(3)}(σ→0) = 8`, no σ→0 plateau (Weyl/Minakshisundaram–Pleijel asymptotic, τ-flat, van-Hove-immune): **SETTLED / CONVERGED** (D-KK-1, conceded by landau in Re:K1/Re:K2; not in dispute).
- **Claim B** — windowed `d_s(σ_*) → 2` at the fold: **INDETERMINATE-pending a converged-`L_max`, normal-state, fold-energy heat-kernel `d_s` gate** discharging S34 [F-4], with two pre-registered opposite predictions discriminated by the directly-fitted `γ_E`.
- **The published "no CDT-like reduction" headline OVERCLAIMS** (C-KK-4 / C-L3): it asserts a windowed comparison that was never computed, silently extending the SETTLED σ→0 asymptotic onto the UNCOMPUTED windowed observable. This convergence holds *regardless of how the gate resolves* — even if the windowed `d_s` returns ≳ 6, the headline is still an overclaim as written. The honest class is the seed's option (iii): indeterminate-pending-compute.

### DISSENT

With the Z⟹γ_E=1/2 lock withdrawn (C-KK-5), I do NOT re-assert it under another name, and I do NOT manufacture dissent beyond the one genuine difference that survives: a magnitude PRIOR for the gate. The lock claimed a *proven structural wall* against the dip; that is gone. What remains is *prior evidence* on the magnitude, which is a different and weaker epistemic object — it informs my pre-registered prediction, it does not settle the gate. I state exactly what survives, honestly scoped.

**D-KK-5 (survives, scoped as a PRIOR not a wall): the realized running-`d_s` floor `d_s_min = 6.3091` bottoms a factor 3.15 above CDT-2.** Knowledge-MCP-verified (gate `T3-S19A-FALSE-VACUUM-ANALYSIS`, INFO, S81; `s19a_false_vacuum_analysis_verdict.txt`): the actual fold-bearing `D_K` spectrum's running-`d_s` minimum is `6.3091` at `σ = 1`. landau conceded its weight honestly (C-L5: "I will NOT wave this away … genuine evidence on kk's side"). But I now scope it exactly as the gate-spec demands and as landau correctly flagged — it is **NOT the [F-4] number**:
- It is the **τ-scan minimum at `tau_at_ds_min = 0.900`** (knowledge-MCP-verified, same verdict file), σ=1, full-`D_K` PW-weighted — NOT the fold-energy-windowed normal-state extraction at `τ_fold = 0.190` that the gate specifies. The τ at which the floor is realized (0.900) is far from the fold (0.190).
- `σ = 1` sits inside landau's fold window `[0.5, 2.0]`, so it IS a windowed-`d_s` evaluation in the right diffusion-time band — that is why it is a *relevant* prior — but it is the wrong τ-slice and uses the full-`D_K` weighting, not the fold-energy-resolved normal-state DOS. So it is a strong *indicator* that the realized return-probability floor lands near 6, not 2; it is not a measurement of the windowed observable at the fold. I do not overstate it: it is my strongest *empirical* prior, exactly as landau characterized it (E-L5).

**D-KK-6 (survives, scoped as a PRIOR): the σ-flow is monotone-crossing on the accessible spectrum.** The S52 data (knowledge-MCP-verified, lines 618–628) shows `d_s(σ)` rising monotonically through every integer — crossing 2 at `t ≈ 0.42` and 8 at `t ≈ 2.36`, with no flat region at either. On the accessible `L_max=3` spectrum, the running `d_s` is a strictly-increasing curve, not a plateau. This is consistent with a smooth crossing (my reading) and is the structural antithesis of a CDT flow. But I now flag the symmetric honesty landau pressed (Re:K3, last ¶): the accessible numerics are truncation-biased DOWNWARD (S52: window-fit reads 2.6, not 8), so they cannot be used to argue *against* a converged-`L_max` fold dip either — they are biased toward 2. The monotone character is a *prior* that the converged flow stays monotone; the converged-`L_max` fit at the fold can in principle resolve a dip the truncated spectrum smears. I hold the monotone-crossing reading as my prior, not as a settled fact.

**D-KK-7 (the genuine, irreducible difference — a magnitude PRIOR, symmetric to landau's): I expect `d_s(σ_*) ≳ 6`; landau expects `≲ 3`.** This is the one real residual disagreement, and it is a difference of PRIORS for the SAME gate, not a difference of verdicts. My prior `d_s(σ_*) ≳ 6` rests on: (i) the realized floor `6.3091` (D-KK-5, near the window though at the wrong τ); (ii) the truncation bias pushing the converged value UP toward 8, away from 2 (S52); (iii) the realized energy-axis DOS being regularized at finite `L_max` to some `γ_E < 1` (the continuum flat-band `γ_E → 1` is a continuum limit, and `ρ_B2 = 14.023` is the finite proxy — knowledge-MCP-verified — so the realized exponent is between 1/2 and 1, and the dip it drives is bounded). landau's prior `d_s(σ_*) ≲ 3` rests on the `Z_{B2}(τ_fold) = 0` flat-band structural prior (C-KK-6 / D-L2) driving `γ_E → 1` and flattening the windowed `d_s` over the fold sub-window. Both priors are framework-sourced; neither is a proven wall. The gap-onset just above the window (`σ ~ 1/ω_min² = 1.49`, where `d_s → ∞`) is a *kk-favorable* feature I retain — it pushes `d_s` up just above the window, working against a sustained dip — but landau's point that the van-Hove flattening (downward) and the gap divergence (upward) must be *disentangled* at the window is correct, and that disentangling is the gate's job, not assertable now.

**Net dissent position (honest, bounded).** I withdraw the structural wall (C-KK-5: the lock is refuted, Sage-verified). What I hold against landau is exactly ONE thing: a magnitude PRIOR — `d_s(σ_*) ≳ 6`, supported by the realized floor `6.3091` and the upward truncation bias — opposite to landau's `≲ 3` prior supported by `Z_{B2}(τ_fold) = 0` / flat-band `γ_E → 1`. These are PRE-REGISTERED PREDICTIONS for the E-L4 gate, NOT a settled verdict. The verdict-class on Claim B is INDETERMINATE-pending-compute, genuinely two-sided (no proven wall on either side, as landau correctly established in E-L5), with the directly-fitted `γ_E` adjudicating. I do not claim the gate's outcome; I claim only that my prior is `≳ 6` and the evidence for it is the realized floor and the truncation direction.

### EMERGENCE

The R2→R3 exchange has done exactly what an adversarial review should: it converged the dispute onto a SINGLE, well-posed, finite computation, and — decisively — the refutation of my lock (C-KK-5) SHARPENED the gate rather than weakening it, by converting an asserted `Z`-lock into a directly-measured exponent `γ_E`. Substrate-first: ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber; ONE functional `Φ: P(σ) ↦ −2 d ln P/d ln σ`; the gate evaluates `Φ[P]` at the fold window and fits the energy-axis DOS exponent that adjudicates the magnitude. I adopt landau's E-L1..L4 converged design in full and pin the four load-bearing pieces.

**E-KK-5 — The fair-comparison functional Φ at the fold window (converged, pinned; = landau E-L1, my E-KK-1).** The apples-to-apples CDT comparison is the SAME functional at the SAME kind of scale on both sides — NOT `d_s^{SU(3)}(σ→0)=8` vs `2` (asymptotic-vs-window category error, my K1), NOT `δd_s(gap)=4×10⁻⁴` vs `2` (gap-vs-DOS error, landau Re:K2):

```
FAIR COMPARISON (converged):
   d_s^{substrate}(σ_*) ≡ Φ[P_{D_K}](σ_*) = −2 d ln P(σ)/d ln σ |_{σ_* ~ 1/E_0²}     [substrate, fold window]
        ⟷
   d_s^{CDT}(intermediate-window plateau) = Φ[P_CDT](σ_CDT-window)                    [CDT, its own window]

   P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}  on the NORMAL-STATE (Δ=0) spectrum at converged L_max,
   σ_* = 1/E_0²,  E_0 = λ_{B2}(τ_fold) = 0.845 M_KK  (knowledge-MCP-verified).
```

The bridge map IS `Φ` itself — a single universal return-probability functional applied to two different return probabilities. No summand-matching is needed because CDT's reduction is a `P(σ)`-SHAPE statement at intermediate σ, not a summand statement. I withdrew the "CDT lives in the M⁴ summand" framing as primary in E-KK-1 (true at σ→0, silent on the intermediate window where CDT actually lives); the convention pin is comparison to CDT's *intermediate-window* value (~2), NOT its IR value (4) and NOT the manifold dimension (8).

**E-KK-6 — The CORRECTED γ_E sub-check (Z as CONSISTENCY CHECK, not lock; = landau E-L3, replacing my E-KK-3 item-3 biconditional).** This is the load-bearing refinement and I endorse landau's correction in full. My E-KK-3 proposed "finite Z ⟺ γ_E = 1/2"; C-KK-5 establishes that clause is wrong (finite `Z = 1/π` holds for ALL `γ_E ∈ [1/2, 1)`, Sage-verified). The corrected sub-check converts the asserted lock into a measured exponent:

```
CORRECTED IMPEDANCE SUB-CHECK (replaces the E-KK-3 item-3 biconditional):
  (1) Fit the energy-axis exponent γ_E from ρ_E(λ) ~ |λ − E_0|^{−γ_E} near E_0 = λ_{B2}
      on the converged normal-state spectrum (continuum-k DOS at fixed τ_fold).  ← the DIRECT discriminator
  (2) Independently compute the dispersion order n from E_{B2}(k) − E_0 ~ |k|^n
      (FLAT BAND PROTECTION form H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4 fixes n via E_J).
  (3) Consistency identity (D-L1):  γ_E = 1 − 1/n,  and  Z = ρ_E·v_g = 1/π for ALL n  (off-fold, Sage-exact),
      with Z → 0 as n → ∞ at the fold center (matching Z_{B2}(τ_fold) = 0).            ← CONSISTENCY CHECK only
  (4) DISCRIMINATOR (the fitted γ_E directly, NOT the impedance):
        γ_E ≈ 1/2 (n ≈ 2, ordinary quadratic extremum)  ⇒  Reading KK mechanism; windowed dip bounded; floor ≳ 6 expected.
        γ_E → 1   (n large, flat-band, as Z_{B2}(τ_fold)=0 predicts)  ⇒  Reading landau mechanism; dip live.
  NOTE: finite Z = 1/π is NOT the discriminator (it holds for the whole family); the impedance is a
        CONSISTENCY CHECK on the fit. Z_{B2}(τ_fold) → 0 is landau's flat-band PRIOR, not a proof of γ_E → 1.
```

The fitted `γ_E` is the mechanism verdict regardless of the `min d_s` band. This is the honest resolution my lock-argument failed to be: neither side asserts `γ_E`; the converged-`L_max` spectrum's DOS near `E_0` is fit and reported.

**E-KK-7 — The two readings make OPPOSITE pre-registered predictions for the same gate (= landau E-L2, my E-KK-2).** The gate discriminates two sharp, mutually-exclusive hypotheses on the same `P(σ)`:
- **Reading KK (mine):** `d_s(σ_*) ≳ 6` at the accessible window, converging UP toward 8 as `L_max → ∞`. Mechanism: energy-axis DOS regularized toward ordinary `γ_E ≈ 1/2`; windowed distortion is the bounded integrable-bump feature; realized running-`d_s` floor `6.3091` (D-KK-5, scoped as τ-scan-min prior); truncation bias pushes UP. Crossings, not plateaus.
- **Reading landau (van-Hove):** `d_s(σ_*) ≲ 3`, flattened over the fold sub-window by an energy-axis flat-band singularity `γ_E → 1` (the fold-center `Z_{B2}(τ_fold) = 0` and FLAT BAND PROTECTION place the fold at the `n → ∞` / `γ_E → 1` end of the impedance family). A plateau/dip — the CDT signature.

These are PRE-REGISTERED PRIORS (D-KK-7), not asserted verdicts; the gate is genuinely two-sided (E-L5: no *proven* wall on either side — the lock is refuted and `Z_{B2}(τ_fold)=0` is a prior). The realized floor `6.3091` is my strongest empirical prior; the flat-band `γ_E → 1` is landau's strongest structural prior; the fitted `γ_E` adjudicates.

**E-KK-8 — The converged gate (4-field spec, feeds the R3 verdict + discharges S34 [F-4]; = landau E-L4, adopted).**
1. **What**: normal-state (`Δ=0`) running `d_s(σ) = −2 d ln P(σ)/d ln σ` profile on `σ ∈ [0.5, 2.0] M_KK^{−2}` (brackets `σ_* ≈ 1.40`, the `d_s=2` bulk crossing at `σ ≈ 0.42`, and the gap-onset at `σ ≈ 1.49`), from `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}` at `τ_fold = 0.190`. PLUS the energy-axis DOS exponent `γ_E` fit near `E_0 = λ_{B2}` per the corrected E-KK-6 sub-check.
2. **Inputs**: converged-`L_max` `D_K` spectrum at `τ_fold` — the S52 `L_max=3`/992-mode cache does NOT resolve the energy-axis DOS shape at `E_0 = 0.845` (the finite spectrum smears `|E−E_0|^{−γ_E}` into the `ρ_B2 = 14.023` finite proxy; this is WHY converged `L_max` is required). Per `math-scripts.md` Casimir-bound / Friedrich-Bär feasibility protocol, bound the worst-case sector and report `L_max_operational` vs `L_max_plan` (the bottom-of-spectrum B2 fold at `E_0 = 0.845` is well below the operative `|λ|` ceiling, and `D_K` block-diagonality by Peter-Weyl makes this feasible without full reconstruction — agent-memory: largest single block at `L_max=15` is dim 9792, dense 1.53 GB, fits VRAM). Gap edge `ω_min = λ_{B1} = 0.819`, `m_{B2}/m_{B1} = 1.032` (knowledge-MCP-verified) — window and gap-onset coincide to 3%, so the windowed extraction must disentangle the van-Hove dip (downward) from the gap divergence (upward, just above).
3. **Gate (pre-registered, two-sided + axis sub-check)**:
   - PLATEAU metric `min_{σ∈[0.5,2.0]} d_s(σ)`. **Reading landau PASS**: `min d_s(σ) < 3` (CDT-like, within ~1 of CDT-2) AND a flat region (windowed `d(d_s)/d ln σ ≈ 0` over a finite sub-window). **Reading KK PASS**: `min d_s(σ) > 5` AND `d_s(σ)` strictly monotone (no plateau; floor near `6.3091`). Band `3 ≤ min d_s ≤ 5`: INDETERMINATE (finer `L_max` scan).
   - **Mandatory axis sub-check** (E-KK-6, corrected): fit `γ_E` near `E_0`; report `n = 1/(1−γ_E)`; cross-check `Z_{B2}(τ_fold)` (expect → 0 if flat-band). `γ_E ≈ 1/2` corroborates Reading KK; `γ_E → 1` corroborates Reading landau. The fitted `γ_E` is the mechanism verdict regardless of the `min d_s` band.
4. **Effort**: ~1.0 wave-equivalents (one converged-`L_max` heat-trace evaluation + log-log windowed-slope fit + DOS-exponent fit near `E_0`; reuses the S52/S53 `d_s` pipeline at higher `L_max`). Discharges S34 [F-4] (INFORMATIVE since S34, never closed, knowledge-MCP-verified `session-34-quantum-foam-collab.md`).

**E-KK-9 — Either gate outcome leaves the canonical `n_s = 0.9567` untouched (cross-domain closure, converged E-KK-4 / E-L6).** The asymptotic-fiber `d_s`-route to `n_s` is dead (agreed: `d_s=8 → n_s=3` absurd via `n_s−1=(d_s−4)/2`; `0.9567` needs `d_s ≈ 3.91`, an M⁴-factor value, NOT the 8D fiber; Hawking `n_s−1 ~ −d(d_s)/dτ ≈ 0` from the τ-flat fiber asymptotic; value-robust to `n_s_framework = 0.9561`). The *windowed* `d_s(σ_*; τ)`-route is conditional on the same E-KK-8 gate (`−d(d_s^window)/dτ` sign/magnitude for `n_s−1 = −0.043`) and lives in the same indeterminate bucket. But canonical `n_s` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A) and does NOT derive from `d_s` — so the gate moves the STATUS of the `d_s`-route (corroboration if it reproduces `0.9567`, superseded if it diverges), not the status of `n_s`, in either outcome. The atlas-07 `η_eff = 3.77` and S53 KZ `n_s ≈ 2.06` entries remain wrong-sign (blue, not red) stale artifacts flagged for the seed's Q2 re-scope, independent of the gate. The gate is high-value (discharges S34 [F-4], settles a 10-session-old open question, discriminates two sharp mechanisms) and *zero-risk to the canonical n_s* either way.

### QUESTIONS

**landau's R2 posed no NEW open questions to me.** R2 answered my Q-K5a head-on (D-L1/D-L2, refuting the lock — Sage-verified, conceded in C-KK-5), accepted my Q-K5d split (E-L5), and answered Q-K5b/Q-K5c within D-L3 (the fair comparison is the σ-scan at fixed `τ_fold`; the floor `6.3091` is the τ-scan-min prior, scoped). There is no outstanding Q-L* to kk. So I pose ONLY the three questions the final verdict table and the E-KK-8 gate spec need pinned before landau writes the Verdict — these are gate-specification questions, not new adversarial tensions.

**Q-K6a (the exact fold-matched diffusion-time window `σ_*` — pin the single number for the verdict table):** We have specified the window as `σ ∈ [0.5, 2.0]` bracketing `σ_* = 1/E_0² ≈ 1.40` with `E_0 = λ_{B2} = 0.845` (so `1/0.845² = 1.401`). But the gap edge `ω_min = λ_{B1} = 0.819` gives a near-coincident scale `1/0.819² = 1.491`, and the `d_s=2` bulk crossing sits at `σ ≈ 0.42`. For the verdict table's "Key Insight" column, do you agree the single pinned fold-match is `σ_* = 1/λ_{B2}²(τ_fold) = 1.401 M_KK^{−2}` (peaking the Gaussian weight exactly at the van-Hove fold energy), with the `[0.5, 2.0]` band as the scan range and the plateau-metric `min_{σ∈[0.5,2.0]} d_s(σ)` evaluated over that band? I want the verdict to carry the exact `σ_*` so the gate is unambiguous.

**Q-K6b (the `L_max` convergence target — what counts as "converged" for the [F-4] discharge?):** The gate requires converged `L_max` because the `L_max=3`/992-mode cache smears the energy-axis DOS at `E_0 = 0.845` into the `ρ_B2 = 14.023` finite proxy. Per the `math-scripts.md` Casimir-bound / Friedrich-Bär feasibility protocol, the B2 fold at `E_0 = 0.845` sits near the bottom of the spectrum, well below the operative `|λ|` ceiling — so the windowed `d_s(σ_*)` and the `γ_E` fit near `E_0` should saturate at modest `L_max`. Concretely: do you accept the convergence criterion as a two-point `L_max` stability check — `|d_s(σ_*; L_max) − d_s(σ_*; L_max−2)| < 0.1` AND `|γ_E(L_max) − γ_E(L_max−2)| < 0.05` — reporting `L_max_operational` vs `L_max_plan` per the feasibility protocol? My prior (D-KK-7) is that the floor converges UP from 6.3 toward 8; if the two-point check shows `d_s(σ_*)` still *descending* with `L_max`, that is evidence for your reading and the criterion should record the descent direction, not just the stability magnitude.

**Q-K6c (the `γ_E` fit protocol — fix the fit window and the regularization so the discriminator is unambiguous):** The directly-fitted `γ_E` from `ρ_E(λ) ~ |λ − E_0|^{−γ_E}` near `E_0` is the mechanism discriminator (E-KK-6). On a FINITE spectrum the continuum singularity is regularized — the eigenvalues near `E_0` are discrete, so `γ_E` must be fit from the *binned* or *cumulative-count* DOS over a fit window `|λ − E_0| < w_fit`. For the verdict to pin the discriminator unambiguously: (i) do you agree the fit uses the cumulative count `N(λ) = #{λ_k ≤ λ}` near `E_0` (more stable on discrete spectra than binned `ρ`), extracting `γ_E` from `N(λ) − N(E_0) ~ sign(λ−E_0)·|λ−E_0|^{1−γ_E}`? (ii) what fit window `w_fit` — I propose `w_fit = (λ_{B2} − λ_{B1}) = 0.845 − 0.819 = 0.026 M_KK`, the B1→B2 gap, as the natural near-fold scale, with a robustness check at `2·w_fit`? (iii) the discriminator bands: `γ_E ∈ [0.5, 0.6]` ⇒ Reading KK (ordinary), `γ_E ∈ [0.8, 1.0)` ⇒ Reading landau (flat-band), `γ_E ∈ (0.6, 0.8)` ⇒ INDETERMINATE — do these match your E-L3 intent? Pinning (i)–(iii) makes the axis sub-check a clean two-sided discriminator for the verdict, with `Z_{B2}(τ_fold) → 0` as the consistency corroborant (not the discriminator).

---

## Round 3 — landau: Cross-Synthesis (FINAL TURN)

### CONVERGENCE

kk's R3 closes the workshop the way an adversarial review should: he conceded the Q-K5a/D-KK-2 lock in full (C-KK-5, Sage-verified the cancellation himself), correctly re-scoped `Z_{B2}(τ_fold) = 0` as a structural prior toward `γ_E → 1` (C-KK-6), and locked the magnitude question as genuinely OPEN with the two-sided gate as the resolution (C-KK-7). I accept the converged verdict-class without reservation and answer his three gate-specification questions (Q-K6a/b/c) to pin the gate unambiguously. I do not re-open the refuted lock or the withdrawn `4×10⁻⁴` card — those are settled.

**Substrate-first frame (mandatory, `phononic-framing.md`).** There is ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber — the fabric IS the spectral triple `(A_K, H_K, D_K)`. `d_s` is a functional of that one `P(σ)`. The flat-band / van-Hove condensed-matter results I cite (papers 37–40) are LABORATORY analogs; the substrate is logically prior. The dispute has collapsed to a single MAGNITUDE question — does the windowed functional `Φ[P_{D_K}](σ_*)` dip toward 2 at the fold window — on which (post C-KK-5) neither of us holds a proven wall. That is the honest end-state.

**C-LF-1 — The Z⟹γ_E=1/2 lock is dead; I accept kk's withdrawal (C-KK-5) and do NOT re-assert my D-L1 over-reach either.** kk verified my D-L1 cancellation symbolically and conceded the biconditional was an algebra error: `Z = ρ_E·v_g = (1/π)(E−E_0)^0 = 1/π` holds for the ENTIRE family `γ_E = 1 − 1/n ∈ [1/2, 1)`, so finite `Z` is a *product* constraint, invariant along the family, not a *factor* constraint fixing `γ_E`. We are fully converged here. I want to record the *symmetric* honesty kk's concession earns: D-L1 refutes the lock — it establishes that finite `Z` is *consistent with* the whole HOvHS family — but D-L1 does NOT by itself *select* `γ_E → 1`. Finite `Z = 1/π` is exponent-blind in *both* directions: it neither forbids `γ_E → 1` (kk's error) nor forces it (which I never claimed, and explicitly disclaimed in D-L4/the net-dissent). The lock's death makes the gate two-sided; it does not pre-load it toward my reading. The `Z ⟹ γ_E` axis is simply silent, and the directly-fitted `γ_E` is the discriminator.

**C-LF-2 — `Z_{B2}(τ_fold) = 0` is my strongest STRUCTURAL prior, scoped exactly as a prior (C-KK-6, accepted as stated).** kk accepts (C-KK-6) that the fold-center impedance `Z_{B2}(τ_fold) = ρ_DOS·v_{B2} = 0` (knowledge-MCP-verified, `session-40-tesla-collab-addendum.md`; the framework's ledger carries `Z_{B2}(τ) ∝ |τ − τ_fold|`, `Z_{B2}=0` exactly at center) is distinct from the off-fold smooth-wall `Z = 1/π ≈ 0.318`, and that it points the energy-axis DOS toward the `n → ∞` / `γ_E → 1` flat-band end. I accept his scoping in full: this is a *prior*, not a proof of the dip. The continuum flat-band `γ_E → 1` is regularized by the finite spectrum to a realized `γ_E ∈ [1/2, 1)` (`ρ_B2 = 14.023` is the finite proxy, knowledge-MCP-verified), and only the converged-`L_max` fit settles where in that range it lands. `Z_{B2}(τ_fold) = 0` is the substrate's own statement that the fold center is the flat-band ($v_{B2}=0$) end of the D-L1 family — corroborated by FLAT BAND PROTECTION (`H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4`, knowledge-MCP-verified `s61_vanhove_dispersion_output.txt`) — but it is a prior pointing the gate, not a wall settling it. Symmetric to kk's `6.3091` empirical prior.

**C-LF-3 — `d_s_min = 6.3091` is kk's strongest EMPIRICAL prior; I reaffirm its weight and its τ-scan-min provenance scope (D-KK-5, accepted as a prior).** I conceded this in C-L5 and do not retreat: the realized running-`d_s` minimum `d_s_min = 6.3091` (knowledge-MCP-verified, gate `T3-S19A-FALSE-VACUUM-ANALYSIS`, INFO, S81; `s19a_false_vacuum_analysis_verdict.txt`) is genuine evidence on kk's side — a windowed-`d_s` evaluation at `σ = 1` (inside the `[0.5, 2.0]` fold window) that bottoms a factor 3.15 above CDT-2 (Sage-verified: `6.3091/2 = 3.155`). kk now scopes it exactly as I flagged: it is the **τ-scan minimum at `tau_at_ds_min = 0.900`** (knowledge-MCP-verified, same verdict file), full-`D_K` PW-weighted — the right diffusion-time band but the WRONG τ-slice (0.900, far from `τ_fold = 0.190`) and the un-windowed normal-state DOS. So it is kk's strongest *empirical* prior, not a measurement of the [F-4] observable. We agree completely on its weight and its scope.

**C-LF-4 — The published "no CDT-like reduction" headline OVERCLAIMS, regardless of how the gate resolves (C-KK-4 / C-L3 / C-KK-7, the central convergence).** This is the load-bearing agreement and it does not move. The standing S52 headline — *"No CDT-like dimensional reduction in the internal SU(3) fiber"* (W3-D line 628) — silently extends the SETTLED σ→0 asymptotic verdict (`d_s^{SU(3)} → 8`, Weyl/Minakshisundaram–Pleijel, τ-flat, van-Hove-immune, Claim A) onto the UNCOMPUTED fold-energy windowed observable (Claim B). The S52 gate was itself FAIL+mis-targeted ("never was a clean positive," kk K3). The honest verdict-class is the seed's option (iii): **indeterminate-pending a converged-`L_max`, normal-state, fold-energy heat-kernel `d_s` gate.** This holds even if the gate returns `d_s(σ_*) ≳ 6` — "indeterminate, asymmetrically favoring `d_s ≳ 6`" is a different and honest verdict-class than "no reduction (settled)." The convergence is on the *epistemic status of the published claim*, not on the gate's outcome.

**C-LF-5 — Answers to kk's three gate-specification questions (Q-K6a/b/c), pinning the gate for the verdict table.** These are specification questions, not adversarial tensions; I accept all three and pin the exact numbers.

- **A-to-Q-K6a (the exact `σ_*`): AGREED — `σ_* = 1/λ_{B2}²(τ_fold) = 1.4005 M_KK^{−2}`** (Sage-exact: `1/0.845² = 1.40051`), peaking the Gaussian weight `e^{−σλ²}` exactly at the van-Hove fold energy `E_0 = λ_{B2} = 0.845`. The `[0.5, 2.0]` band is the scan range; the plateau metric is `min_{σ∈[0.5,2.0]} d_s(σ)`. I pin `σ_* = 1.4005` (4-sig-fig `1.401`) as the single fold-match for the verdict table. Note the gap-onset `1/ω_min² = 1/0.819² = 1.4908` (Sage-exact) sits just *above* `σ_*` — the window and the gap-onset are the same scale to 6% (`(1.4908−1.4005)/1.4005 = 6.4%`; the eigenvalue ratio `m_{B2}/m_{B1} = 1.0317` gives the 3%-in-energy figure quoted upstream, which is `~6%` in `σ = 1/E²`). The windowed extraction must disentangle the van-Hove dip (downward, at `σ_*`) from the gap divergence (upward, setting in just above) — a well-posed finite computation.

- **A-to-Q-K6b (the `L_max` convergence target): AGREED with one sharpening.** I accept the two-point `L_max` stability check `|d_s(σ_*; L_max) − d_s(σ_*; L_max−2)| < 0.1` AND `|γ_E(L_max) − γ_E(L_max−2)| < 0.05`, reporting `L_max_operational` vs `L_max_plan` per the `math-scripts.md` Casimir-bound / Friedrich-Bär feasibility protocol. The sharpening kk himself proposed and I endorse: the criterion must record the **descent direction**, not just the stability magnitude. If `d_s(σ_*; L_max)` is still *descending* with `L_max` at the stability bound, that is evidence for my reading (the converged value sits below the truncated one); if it is *ascending* toward 8 (kk's prior, the truncation-bias direction S52 documents), that is evidence for his. The two-point check reports both `Δ` and `sign(Δ)`. (Feasibility: the B2 fold at `E_0 = 0.845` sits near the spectral bottom, well below the operative `|λ|` ceiling; `D_K` block-diagonality by Peter-Weyl means the bottom-of-spectrum window saturates at modest `L_max` without full reconstruction — agent-memory: largest single block at `L_max=15` is dim 9792, dense 1.53 GB, fits the 17 GB VRAM with margin.)

- **A-to-Q-K6c (the `γ_E` fit protocol): AGREED on all three sub-items — this makes the axis sub-check a clean two-sided discriminator.**
  - (i) **YES — fit from the cumulative count** `N(λ) = #{λ_k ≤ λ}` near `E_0`, extracting `γ_E` from `N(λ) − N(E_0) ~ sign(λ−E_0)·|λ−E_0|^{1−γ_E}`. On a discrete finite spectrum the cumulative count is far more stable than a binned `ρ_E` (binning near a near-degenerate pile-up is noise-dominated); the cumulative form integrates the singularity and is the correct estimator. This is the standard van-Hove exponent extraction on finite spectra and I endorse it as the canonical protocol.
  - (ii) **YES — fit window `w_fit = (λ_{B2} − λ_{B1}) = 0.845 − 0.819 = 0.026 M_KK`** (the B1→B2 gap, the natural near-fold scale), with the robustness check at `2·w_fit = 0.052`. If `γ_E` shifts by more than the `0.05` stability tolerance between `w_fit` and `2·w_fit`, the fit window is not yet in the asymptotic regime and `L_max` must increase — a built-in self-consistency guard.
  - (iii) **YES — the discriminator bands**: `γ_E ∈ [0.5, 0.6]` ⇒ Reading KK (ordinary quadratic extremum, `n ≈ 2`); `γ_E ∈ [0.8, 1.0)` ⇒ Reading landau (flat-band, `n` large); `γ_E ∈ (0.6, 0.8)` ⇒ INDETERMINATE (finer `L_max` scan). These match my E-L3 intent exactly. `Z_{B2}(τ_fold) → 0` is the consistency corroborant (it should track `n → ∞` if `γ_E → 1`), NOT the discriminator — the fitted `γ_E` is the verdict. Pinning (i)–(iii) closes the gate specification.

With Q-K6a/b/c pinned, the gate is fully specified and unambiguous. The workshop has converged.

### DISSENT

With the Z⟹γ_E=1/2 lock withdrawn by kk (C-KK-5) and my own D-L1 scoped honestly (C-LF-1: D-L1 refutes the lock but does not *select* `γ_E → 1`), I do NOT manufacture residual dissent. The one genuine difference that survives is a magnitude PRIOR for the gate — symmetric to kk's D-KK-7 — and it is a difference of *pre-registered predictions*, not of *verdicts*. I state exactly what I hold, scoped as a prior, not a wall.

**D-LF-1 (the one irreducible difference — a magnitude PRIOR, symmetric to kk's D-KK-7): I expect `d_s(σ_*) ≲ 3` with a flat sub-window; kk expects `≳ 6` monotone.** This is the sole residual disagreement and it is a difference of PRIORS for the SAME gate. My prior `d_s(σ_*) ≲ 3` rests on three framework-sourced facts, none of which is a proven wall:
1. **The fold-center impedance `Z_{B2}(τ_fold) = 0`** (C-LF-2, knowledge-MCP-verified) places the energy-axis DOS at the `n → ∞` / `γ_E → 1` flat-band end of the D-L1 family — the regime where the windowed `⟨λ²⟩_{σ_*}` is most strongly flattened by the DOS pile-up at `E_0`.
2. **FLAT BAND PROTECTION** (`H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4`, knowledge-MCP-verified): the B2 sector is the (near-)flat optical band (4 degenerate modes at `E_B2 = 0.845`, knowledge-MCP-verified `session-38-naz-tesla-workshop.md`), so the continuum-k energy DOS at fixed `τ_fold` has a near-delta pile-up at `E_0` — an energy-axis singularity *independent* of the τ-axis A_2 catastrophe (the D-L3 two-axis resolution: both axes are singular at the fold, via different mechanisms).
3. **The flattening is the CDT signature**: a `d_s(σ)` whose `σ`-derivative is suppressed over the fold sub-window IS a plateau/dip, the same `P(σ)`-shape feature CDT's `d_s → 2` is (E-L1 bridge map).

**D-LF-2 (what I do NOT claim — the honest bound).** I do NOT claim the dip *will* reach 2. The magnitude depends on: (i) how strong the energy-axis singularity is at finite, converged `L_max` (the continuum `γ_E → 1` is regularized to a realized `γ_E ∈ [1/2, 1)`; the gate fits it); (ii) whether the windowed `d_s(σ_*)` actually flattens given the gap-onset at `σ ~ 1/ω_min² = 1.4908` sits just above `σ_* = 1.4005` and pushes `d_s → ∞` (kk's correct kk-favorable point — the van-Hove dip and the gap divergence must be disentangled at the window, which is the gate's job); (iii) whether the realized floor `d_s_min = 6.3091` (C-LF-3) drops materially under the *windowed normal-state* extraction at `τ_fold` rather than the τ-scan minimum at `τ = 0.900`. My honest pre-registered prediction: `d_s(σ_*) ≲ 3` with CDT-2 *live but not guaranteed*. kk's: `≳ 6`, monotone. Both are framework-sourced priors; neither is a proven wall (C-LF-1 / E-L5 established the gate is genuinely two-sided).

**D-LF-3 (the crux has a sharp empirical form — the fitted `γ_E`).** The entire residual dissent reduces to one fitted number: `γ_E` near `E_0` on the converged-`L_max` normal-state spectrum (C-LF-5 A-to-Q-K6c). If `γ_E ∈ [0.5, 0.6]` (ordinary) AND the windowed floor stays ≳ 6, kk's prior is vindicated and the WALL's `γ_E = 1/2` reading is empirically correct (even though kk's lock-*argument* was algebraically wrong, C-KK-5). If `γ_E ∈ [0.8, 1.0)` (flat-band, as `Z_{B2}(τ_fold) = 0` predicts) AND the windowed `d_s` dips below 3, my prior is vindicated. The gate adjudicates; neither of us asserts the answer now. This is the correct end-state for an adversarial review: not a verdict asserted, but a sharp, pre-registered, two-sided, finite computation with the discriminating sub-quantity isolated.

**Net dissent position (honest, bounded).** I hold exactly ONE thing against kk's R3: a magnitude PRIOR — `d_s(σ_*) ≲ 3` with a flat sub-window, supported by `Z_{B2}(τ_fold) = 0` / FLAT BAND PROTECTION driving `γ_E → 1` — opposite to kk's `≳ 6` monotone prior supported by the realized floor `6.3091` and the upward truncation bias. These are PRE-REGISTERED PREDICTIONS for the converged gate (E-L4 / E-KK-8), NOT a settled verdict. The verdict-class on Claim B is INDETERMINATE-pending-compute, genuinely two-sided, with the directly-fitted `γ_E` adjudicating.

### EMERGENCE

The R1→R3 exchange did exactly what an adversarial review should: it converged the dispute onto a SINGLE, well-posed, finite computation, and the two strongest moves of the workshop — my D-L1 refutation of kk's lock (C-KK-5) and kk's correct re-scoping of both priors (C-KK-6/D-KK-5) — SHARPENED the gate rather than weakening it, by converting an *asserted* `Z`-lock into a *directly-measured* exponent `γ_E`. Substrate-first: ONE return probability `P(σ) = Tr e^{−σ D_K²}` on the fiber; ONE functional `Φ: P(σ) ↦ −2 d ln P/d ln σ`; the gate evaluates `Φ[P]` at the fold window and fits the energy-axis DOS exponent that adjudicates the magnitude. I pin the converged synthesis — it is identical to kk's E-KK-5..E-KK-9 (we fully converged), with the gate fully specified by Q-K6a/b/c.

**E-LF-1 — The fair-comparison functional Φ at the fold window (converged, pinned; = my E-L1 / kk E-KK-5).** The apples-to-apples CDT comparison is the SAME functional at the SAME kind of scale on both sides — NOT `d_s^{SU(3)}(σ→0)=8` vs `2` (asymptotic-vs-window category error, kk K1), NOT `δd_s(gap)=4×10⁻⁴` vs `2` (gap-vs-DOS error, my Re:K2):

```
FAIR COMPARISON (converged):
   d_s^{substrate}(σ_*) ≡ Φ[P_{D_K}](σ_*) = −2 d ln P(σ)/d ln σ |_{σ_* = 1.4005 M_KK^{−2}}   [substrate, fold window]
        ⟷
   d_s^{CDT}(intermediate-window plateau) = Φ[P_CDT](σ_CDT-window)                          [CDT, its own window]

   P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}  on the NORMAL-STATE (Δ=0) spectrum at converged L_max,
   σ_* = 1/E_0²,  E_0 = λ_{B2}(τ_fold) = 0.845 M_KK  (knowledge-MCP-verified; Sage-exact σ_* = 1.40051).
```

The bridge map IS `Φ` itself — a single universal return-probability functional applied to two different return probabilities. No summand-matching is needed because CDT's reduction is a `P(σ)`-SHAPE statement at intermediate σ, not a summand statement (kk withdrew the "CDT lives in the M⁴ summand" framing as primary, E-KK-1 — true at σ→0, silent on the intermediate window). The convention pin is comparison to CDT's *intermediate-window* value (~2), NOT its IR value (4) and NOT the manifold dimension (8).

**E-LF-2 — The CORRECTED γ_E sub-check (Z as CONSISTENCY CHECK, not lock; = my E-L3 / kk E-KK-6), with the Q-K6c fit protocol pinned.** This is the load-bearing refinement, fully converged. My E-L3 corrected kk's E-KK-3 "finite Z ⟺ γ_E = 1/2" biconditional (refuted by D-L1 / C-KK-5); the corrected sub-check converts the asserted lock into a measured exponent, and Q-K6c now pins the fit protocol:

```
CONVERGED IMPEDANCE SUB-CHECK (replaces the E-KK-3 item-3 biconditional; fit protocol per A-to-Q-K6c):
  (1) Fit γ_E from the CUMULATIVE COUNT  N(λ) − N(E_0) ~ sign(λ−E_0)·|λ−E_0|^{1−γ_E}  near E_0 = λ_{B2},
      fit window w_fit = (λ_{B2} − λ_{B1}) = 0.026 M_KK, robustness check at 2·w_fit = 0.052.   ← the DIRECT discriminator
  (2) Independently compute the dispersion order n from E_{B2}(k) − E_0 ~ |k|^n
      (FLAT BAND PROTECTION form H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4 fixes n via E_J).
  (3) Consistency identity (D-L1, Sage-exact):  γ_E = 1 − 1/n,  Z = ρ_E·v_g = 1/π for ALL n (off-fold),
      with Z → 0 as n → ∞ at the fold center (matching Z_{B2}(τ_fold) = 0).                    ← CONSISTENCY CHECK only
  (4) DISCRIMINATOR (the fitted γ_E directly, NOT the impedance):
        γ_E ∈ [0.5, 0.6] (n ≈ 2, ordinary quadratic extremum)  ⇒  Reading KK; windowed dip bounded; floor ≳ 6 expected.
        γ_E ∈ [0.8, 1.0) (n large, flat-band, as Z_{B2}(τ_fold)=0 predicts)  ⇒  Reading landau; dip live.
        γ_E ∈ (0.6, 0.8): INDETERMINATE (finer L_max scan).
  NOTE: finite Z = 1/π is NOT the discriminator (it holds for the whole family); the impedance is a
        CONSISTENCY CHECK on the fit. Z_{B2}(τ_fold) → 0 is landau's flat-band PRIOR, not a proof of γ_E → 1.
```

The fitted `γ_E` is the mechanism verdict regardless of the `min d_s` band. This is the honest resolution the lock-argument failed to be: neither side asserts `γ_E`; the converged-`L_max` spectrum's cumulative-count DOS near `E_0` is fit and reported.

**E-LF-3 — The two readings make OPPOSITE pre-registered predictions for the same gate (= my E-L2 / kk E-KK-7), scoped as PRIORS not verdicts.** The gate discriminates two sharp, mutually-exclusive hypotheses on the same `P(σ)`:
- **Reading KK (kk):** `d_s(σ_*) ≳ 6` at the accessible window, converging UP toward 8 as `L_max → ∞`. Mechanism: energy-axis DOS regularized toward ordinary `γ_E ≈ 1/2`; windowed distortion is the bounded integrable-bump feature; realized running-`d_s` floor `6.3091` (D-KK-5, scoped as τ-scan-min prior at τ=0.900); truncation bias pushes UP. Crossings, not plateaus.
- **Reading landau (mine):** `d_s(σ_*) ≲ 3`, flattened over the fold sub-window by an energy-axis flat-band singularity `γ_E → 1` (the fold-center `Z_{B2}(τ_fold) = 0` and FLAT BAND PROTECTION place the fold at the `n → ∞` / `γ_E → 1` end of the impedance family). A plateau/dip — the CDT signature.

These are PRE-REGISTERED PRIORS (D-LF-1 / D-KK-7), not asserted verdicts; the gate is genuinely two-sided (E-L5 / C-KK-7: no *proven* wall on either side — the lock is refuted and `Z_{B2}(τ_fold) = 0` is a prior). The realized floor `6.3091` is kk's strongest empirical prior; the flat-band `Z_{B2}(τ_fold) = 0` / `γ_E → 1` is my strongest structural prior; the fitted `γ_E` adjudicates.

**E-LF-4 — The converged gate (4-field spec, feeds the Verdict + discharges S34 [F-4]; = my E-L4 / kk E-KK-8, fully specified by Q-K6a/b/c).** Pinned in the Carry-Forward Computations section below. In brief: normal-state (`Δ=0`) running `d_s(σ)` profile on `σ ∈ [0.5, 2.0]` (brackets `σ_* = 1.4005`, the `d_s=2` bulk crossing at `σ ≈ 0.42`, the gap-onset at `σ ≈ 1.4908`), plus the cumulative-count `γ_E` fit near `E_0 = 0.845` (`w_fit = 0.026`); converged-`L_max` D_K spectrum at `τ_fold = 0.190`; two-sided PLATEAU + `γ_E`-band gate; ~1.0 wave-equivalents. Discharges S34 [F-4] (INFORMATIVE since S34, never closed).

**E-LF-5 — The settled / open split for the Verdict (= kk's Q-K5d split / kk E-KK-9 / my E-L5).** Three-way:
- **Claim A** — `d_s^{SU(3)}(σ→0) = 8`, no σ→0 plateau (Weyl/Minakshisundaram–Pleijel asymptotic, τ-flat, van-Hove-immune): **SETTLED / CONVERGED** (D-KK-1, conceded by me in Re:K1/Re:K2; not in dispute).
- **Claim B** — windowed `d_s(σ_*) → 2` at the fold: **INDETERMINATE-pending the E-LF-4 gate** (discharging S34 [F-4]), with two pre-registered opposite priors discriminated by the directly-fitted `γ_E`.
- **The published "no CDT-like reduction" headline OVERCLAIMS** (C-LF-4 / C-KK-4): it asserts a windowed comparison never computed. Honest class is the seed's option (iii): indeterminate-pending-compute, holding regardless of how the gate resolves.

**E-LF-6 — Either gate outcome leaves the canonical `n_s = 0.9567` untouched (cross-domain closure, converged E-KK-9 / E-L6).** The asymptotic-fiber `d_s`-route to `n_s` is dead (Sage-verified: `d_s=8 → n_s=3` absurd via `n_s−1=(d_s−4)/2`; `0.9567` needs `d_s = 3.913`, an M⁴-factor value, NOT the 8D fiber; Hawking `n_s−1 ~ −d(d_s)/dτ ≈ 0` from the τ-flat fiber asymptotic; value-robust to `n_s_framework = 0.9561`). The *windowed* `d_s(σ_*; τ)`-route is conditional on the same gate (`−d(d_s^window)/dτ` sign/magnitude for `n_s−1 = −0.0433`) and lives in the same indeterminate bucket. But canonical `n_s` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A, knowledge-MCP-verified `phonon_exflation_cosmology.md §8.7.9`) and does NOT derive from `d_s` — so the gate moves the STATUS of the `d_s`-route (corroboration if it reproduces `0.9567`, superseded if it diverges), not the status of `n_s`, in either outcome. The atlas-07 `η_eff = 3.77` (proven_1291) → `n_s = −2.77` and S53 KZ `n_s = 2.0647` (NS-ACOUSTIC-53) are wrong-sign (blue / negative, not the canonical red `n_s−1 = −0.0433`) stale artifacts flagged for the seed's Q2 re-scope (executed in-session below), independent of the gate. The gate is high-value (discharges S34 [F-4], settles a 10-session-old open question spanning S31→S63, discriminates two sharp mechanisms) and *zero-risk to the canonical n_s* either way.

---

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | (a) Structural vs artifact (bulk-vs-fold / finite-L) | K1, Re:K1, L1; C-KK-1, C-LF-4 | **Converged** | The σ→0 Weyl asymptotic `d_s^{SU(3)} → 8` is a structural theorem (Claim A, van-Hove-immune, τ-flat) — but it is a DIFFERENT functional of the same `P(σ)` than the CDT-comparable windowed `d_s(σ_*)`. The S52 "no reduction" headline OVERCLAIMS by silently extending the settled asymptotic onto the uncomputed windowed observable. |
| 2 | (b) Fold-energy d_s — CDT ~2 vs bulk Weyl | K2, Re:K2, L1, L2; D-L1, D-L2, C-KK-5/6, D-LF-1 | **Emerged** | The magnitude of windowed `d_s(σ_* = 1.4005)` is genuinely OPEN. kk's `Z ⟹ γ_E=1/2` lock is REFUTED (D-L1, Sage-exact: `Z = ρ_E·v_g = 1/π` for the whole HOvHS family `γ_E ∈ [1/2,1)`); `Z_{B2}(τ_fold) = 0` points toward flat-band `γ_E → 1`. Decided by a two-sided pre-registered gate: kk prior `≳6` monotone (floor `6.3091`), landau prior `≲3` plateau (flat-band `γ_E→1`); the directly-fitted `γ_E` adjudicates. |
| 3 | (c) Premature-comparison / indeterminate-pending-compute | K3, Re:K3; C-KK-4, C-L3, C-LF-4 | **Converged** | The standing position is the seed's option (iii): INDETERMINATE-pending a converged-`L_max`, normal-state, fold-energy heat-kernel gate. The S52 gate was FAIL+mis-targeted, never a clean positive; the S53 `d_s_max=67.829` artifact (8.5× the manifold dim) + `L_max≫6` caution are jointly decisive that any accessible numerical CDT comparison is premature. Holds regardless of how the gate resolves. |
| 4 | (d) d_s-route to n_s — reproduce 0.9567 or superseded | K4, Re:K4; E-KK-9, E-L6, E-LF-6 | **Converged** | The asymptotic-fiber `d_s`-route to `n_s` is SUPERSEDED (Sage: `d_s=8 → n_s=3` absurd; `0.9567` needs `d_s=3.913`, an M⁴-factor value). Canonical `n_s=0.9567` is the Bogoliubov-invariant (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A) and does NOT derive from `d_s` — UNTOUCHED either way. atlas-07 `η_eff=3.77` (→ `n_s=−2.77`, proven_1291) + S53 KZ `n_s=2.0647` (NS-ACOUSTIC-53) are wrong-sign stale artifacts → Q2 re-scope (effected in-session). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**Net**: 3 Converged (topics a, c, d) + 1 Emerged (topic b — the windowed-`d_s` magnitude, which neither R1 position had isolated as a distinct observable). The single residual disagreement is a magnitude PRIOR (kk `≳6` vs landau `≲3`) for the pre-registered two-sided gate (E-LF-4), NOT a settled verdict. No topic closes as Dissent: the lock kk asserted against the dip was refuted (C-KK-5), and the dip landau argued for is not claimed as proven (D-LF-2) — both readings reduce to pre-registered priors on one fitted number `γ_E`.

## Remaining Open Questions

1. **The fold-energy windowed `d_s` gate (discharges S34 [F-4]).** Compute the normal-state (`Δ=0`) running `d_s(σ) = −2 d ln P(σ)/d ln σ` on `σ ∈ [0.5, 2.0] M_KK^{−2}` (fold-match `σ_* = 1/λ_{B2}² = 1.4005`) from the converged-`L_max` `D_K` spectrum at `τ_fold = 0.190`, PLUS the cumulative-count energy-axis DOS exponent `γ_E` near `E_0 = λ_{B2} = 0.845` (`w_fit = 0.026`). Two-sided gate: Reading-KK PASS (`min d_s > 5` ∧ monotone ∧ `γ_E ∈ [0.5,0.6]`) vs Reading-landau PASS (`min d_s < 3` ∧ flat sub-window ∧ `γ_E ∈ [0.8,1.0)`). **This is the workshop's single carry-forward computation (CF-S93 below) and the highest-value item.** It closes a 10-session-old open gate (S34 [F-4], INFORMATIVE, never closed).

2. **The windowed `d_s(σ_*; τ)`-route to n_s (conditional on Q1).** IF Q1 finds a nonzero windowed `d_s(σ_*; τ)` τ-flow near the fold, compute `−d(d_s^{window})/dτ` and test the Hawking relation `n_s − 1 ~ −d(d_s)/dτ` against the canonical `n_s − 1 = −0.0433`. Conditional on Q1's gate outcome; zero-risk to canonical `n_s` either way (E-LF-6). A NEXT-NEXT-session item, gated on Q1 returning a non-flat windowed flow.

3. **Generalize the energy-axis vs moduli-axis DOS-exponent decomposition (D-L3 / D-KK-2) as a reusable substrate diagnostic.** The workshop established that the fold's "infinite-order van Hove" (S22c) lives primarily on the τ-modulus axis (A_2 catastrophe, `v_{B2} = dE/dτ = 0`) while the energy-axis exponent `γ_E` (what the fixed-τ heat kernel integrates) is set independently by the flat-band k-structure. Whether this two-axis split applies to the other branches (B1, B3) and to other τ-slices is an open structural question — a candidate methodology item, NOT a Q1-class adversarial tension (route via `/rclab-plan` if pursued, not a workshop).

4. **CDT-side: is the `M⁴`-summand `d_s^{M4}` flow ever computed in the framework?** The additive split `d_s^total = d_s^{M4} + d_s^{SU(3)}` (F-K1-ADDITIVITY) locates any genuine CDT-like reduction in the `M⁴` summand near 4 (the `d_s ≈ 3.913` value that reproduces canonical `n_s`). The framework has only ever computed the fiber summand. Whether the `M⁴` path-integral / foam factor admits a CDT-comparable `d_s^{M4}(σ)` flow is a distinct open question (likely outside the substrate's `D_K`-only machinery; flagged for completeness, low priority).

## Wrap-Up — Workshop Impact Summary

Workshop AH-PF-1 CONVERGED in 3 rounds (6 turns). Net: 3 Verdict topics Converged (a, c, d) + 1 Emerged (b, the windowed-`d_s` magnitude); one residual difference = a pre-registered magnitude PRIOR (kk `≳6` vs landau `≲3`) for a single two-sided gate that discharges S34 [F-4]. Sub-sections below per `output-standards.md §"What Changed — Numerical vs Structural Distinction"`.

### What Changed

#### (a) Numerical revisions

- `σ_*` pinned exactly: `σ_* = 1/λ_{B2}²(τ_fold) = 1/0.845² = 1.40051 M_KK^{−2}` (Sage-exact); gap-onset `1/ω_min² = 1/0.819² = 1.49084` (window and gap-onset coincide to ~6% in σ; `m_{B2}/m_{B1} = 1.0317`).
- d_s→n_s arithmetic pinned (Sage-exact): `d_s = 8 → n_s = 3`; `d_s = 2.6 → n_s = 0.30`; canonical `n_s = 0.9567` requires `d_s = 3.913`; Lifshitz `η_eff = 3.77 → n_s = −2.77`; KZ `n_s − 1 = +1.0647` (blue) vs canonical `n_s − 1 = −0.0433` (red).
- Realized floor / CDT-2 ratio: `d_s_min = 6.3091` (σ=1, τ=0.900) = `3.155 ×` CDT-2 (Sage-exact `6.3091/2`).
- DOS-exponent family: `γ_E = 1 − 1/n` for `n = 2 → 1/2`, `n = 4 → 3/4`, `n → ∞ → 1` (the impedance `Z = ρ_E·v_g = 1/π` invariant across the whole family, Sage-exact).

#### (b) Structural changes

- **One observable → two functionals of the same `P(σ)`.** The decisive structural reframe: `lim_{σ→0} d_s(σ)` (Weyl asymptotic, manifold dimension) and `d_s(σ_*)` (windowed, at the fold energy) are DISTINCT functionals of the same return probability and may differ arbitrarily. The S52 "no reduction" headline was read on the FIRST; the CDT comparison and S34 [F-4] live on the SECOND. (kk K1 → C-KK-1; landau Re:K1.)
- **`Z = ρ_E·v_g = 1/π` reclassified: PRODUCT constraint, not a FACTOR lock.** kk's R2 `Z ⟹ γ_E = 1/2` lock was REFUTED (D-L1, Sage-verified by kk in C-KK-5): finite `Z = 1/π` holds for the entire HOvHS family `γ_E ∈ [1/2, 1)`. The impedance is a CONSISTENCY CHECK on the `γ_E` fit, never a discriminator.
- **`Z_{B2}(τ_fold) = 0` reclassified: fold-CENTER value (flat-band `n→∞` prior), distinct from off-fold `1/π`.** (landau D-L2 → kk C-KK-6.)
- **S35 `δd_s ≈ 4×10⁻⁴` reclassified: GAP response, not DOS response.** It perturbs `|Δ_k|²` at fixed background DOS; it is silent on the normal-state DOS-feature windowed observable. (landau Re:K2/Q-L1 → kk C-KK-2.)
- **The CDT bridge map reclassified: the return-probability functional `Φ` itself, not summand-matching.** kk withdrew "CDT lives in the M⁴ summand" as the primary framing (E-KK-1); CDT's reduction is a `P(σ)`-SHAPE statement at intermediate σ.
- **S52 headline reclassified: "no CDT-like reduction" (positive verdict) → "indeterminate-pending-compute" (seed option iii).** The single most durable structural change: a published positive claim demoted to an open, pre-registered, two-sided gate. (C-LF-4 / C-KK-4.)
- **d_s-route to n_s reclassified: live prediction → superseded route.** The asymptotic-fiber route is dead; the windowed route is conditional on the gate; canonical `n_s` (Bogoliubov-invariant) is untouched either way. (Verdict topic d.)

### What Holds

- **Claim A: `d_s^{SU(3)}(σ→0) = dim(SU(3)) = 8`** — Weyl/Minakshisundaram–Pleijel theorem on the compact homogeneous fiber, τ-flat to 0.5% (volume-preserving Jensen), van-Hove-immune. SETTLED, conceded by landau (Re:K1/Re:K2), defended by kk (D-KK-1). Not in dispute.
- **The additive split `d_s^total = d_s^{M4} + d_s^{SU(3)}` at σ→0** — correct as a Weyl-exponent statement (exponents add). It governs the σ→0 end only; it is silent on the intermediate window (kk withdrew it as a bound on the windowed observable, A-to-Q-L3).
- **Canonical `n_s = 0.9567`** (Bogoliubov-invariant, COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A, `phonon_exflation_cosmology.md §8.7.9`) — does NOT derive from d_s; untouched by either gate outcome. Value-robust to the `n_s_framework = 0.9561` difference (both give the relevant `d_s ≈ 3.91` M⁴-factor value).
- **`Z = ρ_E·v_g = 1/π` (off-fold, PROVEN WALL)** — holds, but as a product invariant across the whole `γ_E ∈ [1/2,1)` family (the WALL theorem is intact; only its misreading as a `γ_E`-lock is refuted).
- **The S53 `d_s_max = 67.829` (3D K²-weighting) artifact + `L_max ≫ 6` caution** — jointly decisive that any accessible NUMERICAL CDT comparison is premature (Claim B numerical). Unchanged.
- **FLAT BAND PROTECTION `H_B2(k,τ) = H_B2(0,τ) + E_J(τ)·λ_k·I_4`** and **Pomeranchuk instability `f(0,0) < −3` (qualitative, `g·N(0) = 3.24` permanent)** — both hold; the latter's perturbative *value* `−4.687` is strong-coupling-superseded (S75 W4-K) and was withdrawn as a quantitative HOvHS anchor (D-KK-3 / D-L4), but the qualitative instability survives.

### What Breaks or Strains

- **The published S52 "no CDT-like dimensional reduction in the SU(3) fiber" headline BREAKS as written** — it asserts a windowed CDT comparison that was never computed, silently extending the settled σ→0 asymptotic onto the uncomputed windowed observable. Re-scoped to indeterminate-pending-compute (the seed's option iii). This holds regardless of how the gate resolves.
- **The atlas-07 `[NEW S44] Lifshitz η_eff = 3.77` entry (proven_1291) STRAINS / is SUPERSEDED** — `n_s − 1 = −η_eff` gives `n_s = −2.77` (and the Lifshitz route `n_s ≈ 2.06`, blue), wrong-sign vs canonical red `n_s − 1 = −0.0433`. Tacitly superseded by the canonical Bogoliubov `n_s = 0.9567`. → Q2 re-scope (effected in-session, §"Effected In-Session" item E1).
- **The S53 KZ `n_s = 2.0647` output (NS-ACOUSTIC-53) STRAINS / is SUPERSEDED** — `n_s − 1 = +1.065` (blue), wrong-sign; the dynamical exponent `z = 3.68` it partly rested on was retracted (`z = 2` EXACT from phonon bands). Same Q2 re-scope (E1).
- **kk's R2 `Z ⟹ γ_E = 1/2` lock BROKE** (C-KK-5, Sage-verified) — but cleanly: the algebra error was conceded, the WALL theorem itself is intact, and the gate is sharper for it (asserted lock → measured `γ_E`).
- **Nothing in the substrate's PROVEN walls strains.** The KO-dim, block-diagonality, monotonicity, Pomeranchuk-instability, FLAT-BAND-PROTECTION, and `Z = 1/π` results all hold. The strain is entirely in two STALE n_s-route registry entries and one OVERCLAIMING headline — all re-scoped, none requiring new physics.

### Carry-Forward Computations (MATH ONLY — propagate to S93)

#### CF-S93-AH-PF-1 — Fold-energy windowed `d_s` gate (discharges S34 [F-4])

> **Routing**: genuine future compute (a new number from first principles against a pre-registered two-sided threshold), NOT Q2 hygiene. Propagates to S93 via this Wrap-Up CF block. Discharges S34 [F-4] (INFORMATIVE since S34, never closed). NEW — not in any prior S92 WP CF.

1. **What**: Compute the windowed spectral dimension `d_s(σ) = −2 d ln P(σ)/d ln σ` on `σ ∈ [0.5, 2.0] M_KK^{−2}` (fold-match `σ_* = 1/λ_{B2}² = 1.40051`, bracketing the `d_s=2` bulk crossing at `σ ≈ 0.42` and the gap-onset at `σ ≈ 1.49084`) from `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}` on the NORMAL-STATE (`Δ=0`) converged-`L_max` `D_K` spectrum at `τ_fold = 0.190`. PLUS the direct energy-axis DOS-exponent fit: extract `γ_E` from the cumulative count `N(λ) − N(E_0) ~ sign(λ−E_0)·|λ−E_0|^{1−γ_E}` near `E_0 = λ_{B2} = 0.845`, fit window `w_fit = (λ_{B2} − λ_{B1}) = 0.026 M_KK`, robustness check at `2·w_fit`. Cross-check `Z = ρ_E·v_g` (expect `1/π` off-fold for all `n`; `Z_{B2}(τ_fold) → 0` flat-band corroborant) — a CONSISTENCY CHECK on the `γ_E` fit, NOT a lock (D-L1, Sage-verified).
2. **Inputs**: the `L_max = 12` (or higher) `D_K` spectrum cache at `τ_fold = 0.190` (the S52 `L_max=3`/992-mode cache does NOT resolve the energy-axis DOS shape at `E_0 = 0.845`; `ρ_B2 = 14.023` is the finite proxy); `λ_{B2} = 0.845`, `λ_{B1} = ω_min = 0.819`, `τ_fold = 0.190`; the return-probability functional `Φ: P(σ) ↦ −2 d ln P/d ln σ`. Per `math-scripts.md` Casimir-bound / Friedrich-Bär feasibility protocol, bound the worst-case sector and report `L_max_operational` vs `L_max_plan` (the B2 fold sits near the spectral bottom, well below the operative `|λ|` ceiling; `D_K` block-diagonality by Peter-Weyl makes a modest converged `L_max` feasible without full reconstruction — largest single block at `L_max=15` is dim 9792, dense 1.53 GB, fits VRAM).
3. **Gate (two-sided, pre-registered)**:
   - **PLATEAU metric** `min_{σ∈[0.5,2.0]} d_s(σ)`: **Reading-KK PASS** if `min d_s(σ) > 5` ∧ `d_s(σ)` strictly monotone (no plateau; floor near `6.3091`) ∧ `γ_E ∈ [0.5, 0.6]`. **Reading-van-Hove PASS** if `min d_s(σ) < 3` ∧ a flat region (`d(d_s)/d ln σ ≈ 0` over a finite sub-window) ∧ `γ_E ∈ [0.8, 1.0)`. **INDETERMINATE** band: `3 ≤ min d_s ≤ 5` OR `γ_E ∈ (0.6, 0.8)` ⇒ finer `L_max` scan.
   - **Tolerance + `L_max`-convergence criterion**: two-point stability check `|d_s(σ_*; L_max) − d_s(σ_*; L_max−2)| < 0.1` AND `|γ_E(L_max) − γ_E(L_max−2)| < 0.05`, reporting both `Δ` AND `sign(Δ)` (descending ⇒ landau-favorable, ascending toward 8 ⇒ kk-favorable). `γ_E` fit stable across `w_fit` and `2·w_fit` to within `0.05` (else `L_max` not yet in asymptotic regime).
   - **Z=1/π is a consistency check, NOT a PASS gate** (the directly-fitted `γ_E` is the mechanism verdict regardless of the `min d_s` band).
   - **Pre-registered priors** (NOT verdicts): kk `d_s(σ_*) ≳ 6` monotone (floor `6.3091` + upward truncation bias); landau `d_s(σ_*) ≲ 3` plateau (flat-band `γ_E → 1` via `Z_{B2}(τ_fold) = 0`).
4. **Effort**: ~1.0 wave-equivalents (one converged-`L_max` heat-trace evaluation + log-log windowed-slope fit + cumulative-count DOS-exponent fit near `E_0`; reuses the S52/S53 `d_s` pipeline at higher `L_max`).

**Depends on**: converged-`L_max` `D_K` spectrum cache at `τ_fold = 0.190` (UPSTREAM — the `s84_spectrum_cache_L12_tau019.npz` master cache or a higher-`L_max` extension); `canonical_constants.py`: `λ_{B2} = 0.845`, `λ_{B1} = 0.819`, `τ_fold = 0.190`; the workshop's converged gate spec (E-LF-4 / E-KK-8, THIS workshop). No other prerequisite — the gate reuses an existing pipeline.

> **Conditional follow-on (NOT a separate CF until Q1 returns)**: IF CF-S93-AH-PF-1 finds a nonzero windowed `d_s(σ_*; τ)` τ-flow near the fold, a NEXT-NEXT-session gate computes `−d(d_s^{window})/dτ` and tests the Hawking relation `n_s − 1 ~ −d(d_s)/dτ` against `−0.0433` (Open Question 2). Gated on Q1; zero-risk to canonical `n_s`.

### Effected In-Session (NON-MATH — executed by landau before terminating)

Every non-math item this workshop surfaces is enumerated below and EXECUTED (or staged per the edit-denial protocol) in this final turn, with file:line and a checked box. No item is deferred to Carry-Forward; the only forward item is the math gate above.

- [x] **E1 — atlas-07 staleness re-scope (the headline non-math item) → LOGGED to the canonical Q2 ledger.** The d_s-route to n_s is superseded; the atlas-07 PERMANENT entry `[NEW S44] Lifshitz anomalous dimension for n_s (η_eff = 3.77)` (theorem `proven_1291`) and the S53 KZ `z=2 → n_s = 2.0647` output (gate `NS-ACOUSTIC-53`) both carry the WRONG SIGN of `n_s − 1` (blue/negative, not canonical red `−0.0433`) and are tacitly superseded by canonical `n_s = 0.9567` (COMPOUND-NS, INFO-PERMANENT, triple-confirmed S73A). Recorded: precise superseded-entry IDs (`proven_1291`; `NS-ACOUSTIC-53` / KZ `n_s=2.0647`) + workshop-verdict basis (Verdict topic d Converged; `d_s=8→n_s=3` absurd, `0.9567` needs `d_s=3.913` M⁴-factor value, Sage-verified) + the canonical `n_s=0.9567` that supersedes them. The curated atlas doc is NOT edited directly (chronological/curation integrity); an inline atlas-row `[SUPERSEDED-S92-AH-PF-1]` annotation is flagged as an orchestrator/curator follow-up. **Executed at**: `sessions/archive/session-92/session-92-housekeeping.md §"§A (AH-PF-1)"` row AH-1.
- [x] **E2 — Fair-comparison-observable `.claude/rules/` directive → VERBATIM directive staged + ORCHESTRATOR-MIRROR-REQUIRED.** Subagent edit-denied on `.claude/rules/`; per the §18–§23 ORCHESTRATOR-RESERVED precedent, the verbatim directive ("do not compare a σ→0 asymptotic to a windowed observable; pre-register the (observable, diffusion-window) pair before any dimensional-reduction verdict; match scale-type not summand; impedance/product constraints are CONSISTENCY CHECKS not γ_E locks") is staged in the framework corpus. **Executed at**: verbatim directive staged at `sessions/framework/registry/cross-pillar-bridge-corpus.md §24` (§24.0 DIRECTIVE + ORCHESTRATOR-RESERVED verbatim mirror block + §24.1 K=1 calibration instance); **ORCHESTRATOR-MIRROR-REQUIRED** → `.claude/rules/cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` AND `.claude/rules/phononic-framing.md §"IS Space, Not IN Space"`.
- [x] **E3 — S52 "no CDT-like reduction" standing-position re-scope → LOGGED to the Q2 ledger.** The headline OVERCLAIMS (C-LF-4); re-scoped to INDETERMINATE-pending-compute (seed option iii). Recorded as a status observation on an existing session-WP claim; the historical S52 WP is NOT edited (chronological-integrity per `session-handoffs.md`); forward consumers cite this workshop's verdict. **Executed at**: `sessions/archive/session-92/session-92-housekeeping.md §"§A (AH-PF-1)"` row AH-3 + this workshop's Verdict table (topics a + c Converged).
- [x] **E4 — The structural-verdict text → WRITTEN in this workshop file.** The Round-3 landau CONVERGENCE/DISSENT/EMERGENCE sections, the 4-topic Workshop Verdict table, the Remaining Open Questions, and this Wrap-Up constitute the workshop's structural verdict (counts as executed per the directive). **Executed at**: `sessions/archive/session-92/workshops/s92-adhoc-spectral-dimension-ds-flow-vs-cdt.md` §"Round 3 — landau" + §"Workshop Verdict" + §"Remaining Open Questions" + §"Wrap-Up".
- [x] **E5 — knowledge-MCP / canonical_constants annotation → assessed; NO premature result-stub written (correct call).** Per `math-scripts.md` canonical-write-order, the fold-energy `d_s` value is the OUTPUT of CF-S93-AH-PF-1 (not yet computed) — promoting a value now would be a premature result-stub. `σ_* = 1.40051` is a derived diffusion-window pin from existing canonicals (`λ_{B2}=0.845`), to be tagged `# (local)` in the eventual gate script, NOT a new framework constant. Verified no `sigma_star`/fold-`d_s` constant exists (only `dS_fold`, unrelated). The staleness re-scope (E1) is recorded in the Q2 ledger (the correct home); knowledge.db picks it up at the session-close `/weave --update`. **Executed at**: assessed via `mcp__knowledge__list_constants` + `get_constant` (no write — the no-write IS the correct action).

### Closing Line

The σ→0 Weyl `d_s → 8` is a theorem, but it is a different functional of the substrate's return probability than the CDT-comparable windowed `d_s(σ_*)` the published "no reduction" headline silently claimed — so the real question (does the fold-energy `d_s` plateau toward 2) is genuinely open, reduces to one fitted exponent `γ_E` on the converged-`L_max` normal-state spectrum, and is discharged by a single two-sided gate that closes S34's decade-old [F-4] while leaving the canonical Bogoliubov `n_s = 0.9567` untouched either way.
