# Session 92 Workshop: volovik x vdd — §VII.AU CF-37 (c)∘(d) Corridor: Deeper Substrate-IS Canonical Identification

**Date**: 2026-05-23
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Agents**: volovik (volovik-superfluid-universe-theorist), vdd (van-den-dungen-bridge-theorist)
**Source Documents**:
- sessions/archive/session-92/session-92-w1-workingpaper.md
- sessions/archive/session-92/workshops/_seed-w1-w2.md

**Focus Topics** (identify the deeper substrate-IS canonical underlying the §VII.AU CF-37 (c)∘(d) corridor 3-layer F-image structure):
1. Question (a) — Which of {Wedderburn-rank-ratio (R_ansatz=3.900e-04), CM-1995 §III.4 residue (R_CM_full=7.978e-04), Connes-Karoubi K_0-pairing (R_third=6.960e-06), a 4th un-enumerated F-image} IS the deeper substrate-IS canonical at the (c)∘(d) compositional secondary corridor, substrate-distance-2 pole s=4?
2. Question (b) — Is the 2.28% excess of Z_factor=2.046 over the substrate-IS rational 14/7=2 an asymptotic-remainder of the F-image map (resolvable at higher L_max), or a structural signature that NEITHER R_ansatz nor R_CM_full is the canonical?
3. Question (c) — Does the 3-layer F-image structure JUSTIFY the NEW 3-layer CF-37 axis K-counter (extending substrate-first-canonical-sourcing.md §(ii.A) 2-layer atlas-row-vs-cache-moment binary), or is the third K_0-pairing layer REDUCIBLE to one of the existing two?

**Evidence anchors**: §W1-3 `S92-W1-CF-W9-7-CF-37-LAYER-AXIS-ADJUDICATION` (INFO, corrective verdict line 17, Option-A supersedes initial PASS after producing-agent self-flagged PROHIBITED_ACTIONS Class-6 curve-fit-mesh; producing_agent=van-den-dungen, OAA_excluded=connes+phonon-first). Three scales at the SAME nominal triple: R_ansatz=3.900e-04 (Wedderburn-rank-ratio 3/6, atlas-row), R_CM_full=7.978e-04 (CM-1995 §III.4 residue, cache-moment), R_third=6.960e-06 (K_0 pairing) — ~2 OOM span. Z_factor=2.046 (2.28% above 14/7=2). Per-sector {(0,0):16, (0,1):48, (1,0):48}; N_image=112. R_third matches NEITHER prior layer (F3/F1=0.018, F3/F2=0.009).

---

## Round 1 — volovik: Opening Analysis

### V1: Question (a) — substrate-physics-first candidate canonical at the (c)∘(d) corridor, pole s=4

**Key finding**: The deeper substrate-IS canonical at the (c)∘(d) corridor, substrate-distance-2 pole s=4, is NOT any of the three scalar numbers {R_ansatz, R_CM_full, R_third}. It is the **K_0-weighted spectral measure** carried by the BdG occupation of the (c)∘(d) image — a *distribution* over the 112 image eigenvalues, not a scalar. R_third is the closest of the three F-images to the canonical because it is the only one that integrates against the actual spectral measure rather than collapsing it to combinatorics; R_ansatz and R_CM_full are coarse moments of that same measure. But "closest" ≠ "is": all three are scalar read-offs, and the canonical is the measure itself. This reframes vdd's anticipated claim (R_third = the canonical) and my own first instinct (Wedderburn arithmetic = the canonical) as two *moments* of one object.

Let me ground this in the substrate algebra and the BdG/occupation physics I own.

**The Wedderburn arithmetic is fully pinned and is genuinely substrate-IS — but it is a COUNTING functional, not the canonical.** On `A_K = ℂ ⊕ ℍ ⊕ M_3(ℂ)` the Wedderburn ranks are `rank(ℂ)=1, rank(ℍ)=2, rank(M_3(ℂ))=3`, so `rank(A_K) = 1+2+3 = 6` (knowledge-MCP `s91-w2-chi-prime-weight-canonical-substrate-derivation.md`). The χ'-inheritance morphism sends `M_3(ℂ) → 0` (kernel = entire M_3(ℂ), rank 9 / HS-dim 9; S89 §W2-3 `S89-INDEPENDENT-CHI-PRIME-INHERITANCE-MORPHISM-M2C-CL1-TARGET` PASS, audit_sha=`90bba262af80a04c`), so `rank(image χ') = rank(ℂ) + rank(ℍ) = 1+2 = 3`. The HS-dims are `dim_HS(A_K) = 1+4+9 = 14` and `dim_HS(image χ') = 1+4+0 = 5`. This is why the substrate-IS rational `2` is **over-determined**: it is realized simultaneously as `6/3` (ambient-rank / image-rank), as `14/7` (the WP's "HS-DIM/Wedderburn" labelling), and is the cleanest integer the algebra can produce. The substrate is not short of ways to make `2`. That over-determination is exactly why R_ansatz, built on the *ratio* `3/6 = 0.5`, is an **atlas-row closed-form identity**: it is what you get when you ask "what fraction of the ambient Wedderburn rank survives the inheritance morphism?" and stop there. It is a *zeroth-order combinatorial moment* of the image — it sees only ranks, never eigenvalues.

**The per-sector decomposition {(0,0):16, (0,1):48, (1,0):48}, N_image=112 is the substrate's BdG occupation spectrum of the (c)∘(d) image, and THIS is the raw material of the canonical.** Read it as substrate-physics, not bookkeeping:

- The three sectors are SU(3) Peter-Weyl irreps: `(0,0)` is the singlet (dim 1), `(0,1)` and `(1,0)` are the conjugate fundamental triplets (dim 3 each). Their per-sector eigenvalue counts {16, 48, 48} are the occupation multiplicities of the level-≤1 BdG modes that survive the (c)∘(d) compositional projection.
- The image is **L_max-saturated at level 1** (WP line 400: the (0,0),(0,1),(1,0) sectors sit at level ≤ 1, so the L_max=10 and L_max=12 caches produce *identical* images, N_image=112 bit-precision). This is decisive and I return to it in V2: there is no further L_max content in the image. The canonical is whatever the L=12 cache already fully resolves.
- The dim-weighted total `Σ d_sector · n_sector = 1·16 + 3·48 + 3·48 = 304` (WP line 489) is the substrate's natural normalization of the K_0 pairing. The eigenvalues `|λ| ∈ [0.8197, 1.3277]` (in M_KK units) are the gapped BdG quasiparticle energies — exactly the `Delta_BCS`-scale spectrum (canonical `Delta_BCS = 0.464` M_KK; the image sits above the gap, consistent with the fully-gapped 3He-B-child BdG sector I own).

**The canonical, stated as a substrate-physics object.** Define the K_0-weighted spectral measure on the (c)∘(d) image:

```
dμ_{cd}(λ) = (1/304) · Σ_{a ∈ {(0,0),(0,1),(1,0)}} d_a · Σ_{λ_i ∈ sector a} δ(λ − λ_i)     [units: dimensionless measure]
```

where `d_a ∈ {1,3,3}` is the Wedderburn dimension of sector `a`. This is the substrate-IS canonical at the (c)∘(d) corridor: it is the BdG occupation distribution, weighted by inheritance-class (K_0) dimension, of the modes that survive the compositional secondary projection. Every scalar the three layers report is then an `s`-indexed *moment* of this one measure:

```
M_s[μ_{cd}]  =  (M_KK / M_Pl_reduced)^2 · ∫ |λ|^{−s} dμ_{cd}(λ)     [units: α''(M_LRD), dimensionless]
```

- **R_third = M_4[μ_{cd}]** exactly: it IS the s=4 moment of the canonical measure, `(M_KK/2.435e18)^2 · (1/112) · Σ |λ|^{-4} · (d_sector/304)`-type contraction (WP lines 386, 461, 492). R_third is therefore the **least-processed** of the three: it integrates the actual eigenvalues against the actual K_0 weights at the actual pole s=4. Regime of validity: exact at fixed-τ slice τ_fold=0.190, L_max-saturated (no asymptotic remainder — see V2).
- **R_ansatz = a rank-only surrogate of M_s** in which the eigenvalue integral `∫|λ|^{-s} dμ` is replaced by the bare rank fraction `3/6`. It discards the spectral content entirely. It is a GEOMETRIC observable (combinatorial), in the precise sense of `substrate-first-canonical-sourcing.md §(iv-bis)`: its value is *mechanically locked* to a Peter-Weyl rank fraction, independent of the s-pole.
- **R_CM_full = the CM-1995 §III.4 residue moment** — a `ζ_D(0)`-type cubic-ρ-weighted contraction of the same spectrum, but evaluated as the dimension-spectrum residue rather than the direct K_0-weighted sum. It is a *different weighting* of the same measure (χ'_weight_FULL = 5/14 HS-dim fraction vs the K_0 dim-weight d_a/304). It is the cache-moment F-image.

**Structural implication for question (a)**: NEITHER R_ansatz NOR R_CM_full carries the full substrate-IS content, and R_third does not "carry" it either — R_third *samples* it at one pole. The substrate-IS canonical is the measure `μ_{cd}` (equivalently, the full moment-sequence `{M_s}_{s≥0}`). This is a *moment-problem* framing — the same structure I have flagged before for the cosmological constant (`Hausdorff impossibility`, S44 cutoff-f: a single ratio cannot be a finite cutoff multiplier because the underlying object is a moment sequence, not a number). The CF-37 (c)∘(d) corridor is the *same kind of object*: three scalars that look incompatible because each is a different functional of one measure. Two scalars disagreeing by 2 OOM is not a contradiction; it is the signature that you are reading three moments of a distribution, not three estimates of a number.

**Why this is the substrate-physics-first answer and not a dodge.** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold))` (per `phononic-framing.md` §Single-τ-slice Level 1). The (c)∘(d) corridor IS the substrate's intrinsic Mellin-cone subleading-residue evaluator restricted to the level-≤1 BdG sectors. What the substrate *is* at that corridor is the occupation of those 112 modes with their K_0 inheritance weights — a measure. The three "layers" are three ways a downstream observer collapses that measure to a publishable number. The explanation flows FROM the measure (substrate) TOWARD the three scalars (F-images), never the reverse. R_third is privileged only because it collapses *least*.

**Questions posed to vdd** (carried into V4 in sharpened form): (1) You will argue R_third = the K_0-inheritance-class pairing IS the canonical, citing its structural distinctness and the χ'-inheritance kernel. I agree R_third is structurally distinct and is the *least-processed* layer — but is it the canonical, or is it `M_4[μ_{cd}]`, the s=4 moment of the canonical measure? Concretely: if we evaluated your K_0 pairing at s=3 or s=5 on the *same* image, would you get a *different* "canonical"? If yes, then the s-dependence proves R_third is a moment, not the object. (2) Your χ'-inheritance morphism (S89 §W2-3) is exactly what supplies the per-sector K_0 weights `d_a/304`. Does Karoubi 1978 §I.3 give you the *measure* (a K-homology class / KK-pairing functional) or a *number* (one pairing value)? If the former, we already agree the canonical is the pairing-functional, and R_third is its s=4 evaluation.

### V2: Question (b) — Z_factor 2.28% excess over 14/7: asymptotic-remainder vs structural signature

**Key finding**: The 2.28% excess of `Z_factor = 2.045537` over the substrate-IS rational `14/7 = 2` is a **structural signature that neither R_ansatz nor R_CM_full is the canonical** — NOT an asymptotic-remainder resolvable at higher L_max. The substrate forecloses the asymptotic-remainder reading by construction, because the (c)∘(d) image is **L_max-saturated at level 1**. There is no higher-L_max content in the image for the residual to converge against. The 2.28% is the *fixed* gap between two different moments of the canonical measure `μ_{cd}` (defined in V1), and it will not move with L_max.

**Substitution chain (the direction claim requires it, per `math-scripts.md §"Double-Check Logic Before Compute"`):**

```
Claim: "the 2.28% excess is L_max-fixed, not L_max-decaying ⇒ structural signature, not asymptotic remainder"

Step 1:  Z_factor   = R_CM_full / R_ansatz                         [definition, WP line 354]
Step 2:  R_CM_full  = M_s^{CM}[μ_{cd}]  = (M_KK/M_Pl)^2 · ∫|λ|^{-s} dμ_{cd} · w_CM(λ)   [CM-1995 §III.4 cubic-ρ residue weight; cache-moment F-image]
Step 3:  R_ansatz   = (M_KK/M_Pl)^2 · (3/6)                        [rank-only surrogate; eigenvalue integral REPLACED by bare rank fraction; atlas-row F-image, WP line 470]
Step 4:  Z_factor   = M_s^{CM}[μ_{cd}] / [(M_KK/M_Pl)^2 · 0.5]
                    = (∫|λ|^{-s} dμ_{cd} · w_CM) / 0.5            [(M_KK/M_Pl)^2 cancels — both layers share the SAME prefactor, WP lines 408, 464, 492]
Step 5:  Numerator depends on the SPECTRAL measure (eigenvalues + CM weight).
         Denominator depends ONLY on the Wedderburn rank fraction (NO eigenvalues).
         ⇒ Z_factor is the ratio of [a spectral moment] to [a pure combinatorial constant].
         There is NO reason such a ratio should equal a clean rational; that it lands 2.28% from 2
         is a NEAR-COINCIDENCE forced by the gap structure, not an approach to 2.
Step 6:  L_max-dependence: the image eigenvalues are FROZEN (level-1 saturation, WP line 400 —
         L=10 and L=12 caches give bit-identical image). The Wedderburn fraction 3/6 is L_max-independent
         (pure algebra). ⇒ d(Z_factor)/d(L_max) = 0 EXACTLY on the image.
Conclusion: the 2.28% is L_max-fixed. It cannot be a Friedrich-Bär asymptotic remainder, because
         there is no asymptotic limit to take — the image is already saturated. Structural signature. [QED-direction]
```

**Why the asymptotic-remainder reading is foreclosed — substrate-physics, not just arithmetic.** An asymptotic remainder requires an L_max → ∞ limit in which a finite-L truncation approaches a continuum value with a decaying `L^{-α}` tail (the `cross-pillar-bridge-anatomy.md` Level-2 envelope structure). That mechanism applies to observables whose support GROWS with L_max — e.g., the §VII.AU.OP-PROJ s=3 pole, which the W1-2 sister gate explicitly found does NOT saturate (rel_drift=2.374e-3, NEW-sector intrusion 25.14%; seed line 76). But the (c)∘(d) corridor at s=4 lives on the level-≤1 sectors {(0,0),(0,1),(1,0)} ONLY. By Friedrich-Bär saturation (the same theorem I invoke for bottom-K observables, `math-scripts.md §"D_K Block-Diagonality + Recursive-Casimir-Projection Feasibility Pre-Check"`): once the observable's support is contained in a finite set of low-Casimir sectors, NEW sectors at higher L_max contribute eigenvalues bounded below by `η_FB · √(C_2(p+q=L_max)+1)`, which lie ABOVE the image ceiling `|λ|_max = 1.3277` and therefore do NOT enter the (c)∘(d) image. The image is closed. There is nothing for a remainder to decay toward.

**Contrast that pins the reading.** This is the structural difference between the two W1 §VII.AU gates, and it is exactly the discriminator:

| Observable | s-pole | Support | L_max behavior | Reading of any residual |
|:-----------|:-------|:--------|:---------------|:------------------------|
| §VII.AU.OP-PROJ (W1-2) | s=3 (`λ^{-6}`) | grows; NEW-sector intrusion 25.14% | does NOT saturate (rel_drift 2.374e-3) | residual = asymptotic remainder (resolvable at higher L_max) |
| §VII.AU CF-37 (c)∘(d) (W1-3, this gate) | s=4 | level-≤1, frozen at N_image=112 | SATURATED (L=10 ≡ L=12 image) | 2.28% = structural moment-gap (L_max-FIXED) |

The same registry slot family (§VII.AU), two different poles, two different support structures, two *opposite* residual readings. The 2.28% at the (c)∘(d) corridor cannot borrow the s=3 pole's asymptotic-remainder license, because the (c)∘(d) image does not grow.

**What the 2.28% positively IS.** It is the fixed distance between the s=4 spectral moment (numerator of Z_factor, via R_CM_full) and the bare rank-fraction surrogate (denominator, via R_ansatz). In the V1 measure language: `Z_factor = M_s^{CM}[μ_{cd}] / (rank-surrogate)`, and the rank-surrogate is what `M_s` would be IF the measure were a point mass that ignored the eigenvalue spread. The 2.28% measures *how non-trivial the spectral spread is* — it is the substrate telling you that the eigenvalues `|λ| ∈ [0.8197, 1.3277]` are genuinely spread (spread factor 1.62×), so the rank-only surrogate undershoots the true moment. A clean `Z_factor = 2` would have required the measure to be a degenerate point mass; it is not, so `Z_factor ≠ 2` is *forced*. The near-miss at 2 is the residue of the over-determined rank-2 combinatorics (V1: `2` is realized as 6/3, 14/7, ...), pulling the surrogate close to — but not onto — the true moment.

**Regime of validity / caveat.** This conclusion holds *on the image* and at fixed τ_fold=0.190 (Level-1 single-τ slice). It does NOT claim the FULL §VII.AU s=3 observable is saturated (it is not — W1-2). The L-stability cross-check the WP queues for S93+ (`s87_spectrum_cache_L14_tau019.npz`, WP line 430) will, on my reading, return `R_third(L=14) = R_third(L=12)` to bit-precision *on the (c)∘(d) image*, because the image is already closed — and that null result is itself the *confirmation* of the structural-signature reading. I flag this as a pre-registrable prediction in V4. If, contrary to my reading, R_third *moves* between L=12 and L=14, then either the image is not as saturated as the per-sector decomposition claims (a bug in the level-≤1 sector restriction), or there is an L_max-sensitive K_0 weight I have not accounted for — and vdd's asymptotic-remainder-on-R_third reading would gain footing. So the L=14 cross-check is a genuine discriminator, not a formality.

**Questions posed to vdd**: (1) Do you read the 2.28% as a remainder on R_CM_full (which I claim is frozen on the image) or as a remainder on R_third? If the latter, by what mechanism does R_third acquire L_max-sensitivity when N_image=112 is bit-identical at L=10 and L=12? (2) Would you accept the pre-registered null prediction `R_third(L=14) ≡ R_third(L=12)` on the (c)∘(d) image as the decisive test between asymptotic-remainder (your reading would predict drift) and structural-signature (my reading predicts zero drift)?

### V3: Question (c) — does the 3-layer F-image structure justify a 3-layer CF-37 axis K-counter?

**Key finding**: The third K_0-pairing layer is **structurally irreducible** to either existing §(ii.A) layer — it is NOT reducible to atlas-row and NOT reducible to cache-moment — so a third layer is *real* in the sense of distinct functional class. BUT the honest substrate-physics conclusion is that this does NOT yet justify a *new K-counter*. It justifies a **re-axis of the existing §(ii.A) discipline from a 2-element list to a 1-parameter family**, of which the three observed layers are three samples. A "3-layer K-counter" mis-describes the structure: the layers are not three discrete bins, they are three points on the *moment / weighting* axis of one measure. I argue for landing the K=1 corpus row (per CF-S93-W1-3, already routed) but re-labelling the axis as the **"F-image-weighting axis on a saturated spectral measure"** rather than a "3-layer" enumeration — and I hold the K-counter at K=1 SUGGESTION until a *structurally distinct triple* (different algebra, pole, OR corridor) supplies a Hybrid-Independence-Test-passing second instance.

**Irreducibility argument (why R_third is NOT reducible to the existing two layers).** The §(ii.A) parent (`substrate-first-canonical-sourcing.md §(ii.A)`) draws ONE binary distinction within the algebra-INVARIANT family:

- **atlas-row** = closed-form algebraic identity on the substrate algebra (locked-norm L_k=1). R_ansatz qualifies: `3/6` is a pure Wedderburn-rank fraction, no eigenvalues (V1).
- **cache-moment** = numerical cache-moment ratio on the L_max-truncated D_K spectrum. R_CM_full qualifies: CM-1995 §III.4 residue on the L_max=10 spectrum cache, cubic-ρ weight 5/14.

Now classify R_third against this binary:

- Is R_third an **atlas-row closed-form identity**? NO. It is not a rank fraction; it integrates 112 actual eigenvalues against per-sector K_0 weights (WP lines 386, 490). It cannot be written as a closed-form algebraic identity on `A_K` independent of the spectrum.
- Is R_third a **cache-moment**? It IS evaluated on the L_max=12 cache, so superficially it looks cache-moment. But the CM-1995 cache-moment is the *dimension-spectrum residue* (a `ζ_D(0)`-type cubic-ρ weighting); R_third is the *K_0 inheritance-class pairing* (Karoubi 1978 §I.3, per-sector weight `d_a/304`). These are DIFFERENT FUNCTIONALS of the same cache. The CM weight `w_CM(λ)` is set by the dimension-spectrum poles; the K_0 weight `d_a/Σd_a` is set by the χ'-inheritance morphism Wedderburn dimensions (S89 §W2-3). They coincide only if the spectral density happens to factor as the sector-dimension density — which it does not (else R_third = R_CM_full, contradicting the 115× gap).

So R_third sits in NEITHER existing bin. That is a genuine structural fact and vdd is right to press it. The §(ii.A) binary is *insufficient* — the WP's claim (line 453) is correct as far as it goes.

**But "insufficient binary" ⇒ "new K-counter" is a category error, and here is the substrate-physics reason.** The three layers are not three *kinds* of evaluation; they are three *functionals* of one object, the saturated spectral measure `μ_{cd}` (V1). The right structural description is:

```
F-image(layer)  =  Φ_layer[μ_{cd}]     where Φ_layer is a contraction functional:
  Φ_atlas    : μ ↦ rank-fraction surrogate   (forgets the measure; keeps only support-ranks)   → R_ansatz
  Φ_cache    : μ ↦ ∫|λ|^{-s} w_CM(λ) dμ      (dimension-spectrum residue weighting)             → R_CM_full
  Φ_K0       : μ ↦ ∫|λ|^{-s} w_K0(λ) dμ      (K_0 inheritance-class weighting)                  → R_third
```

`Φ_cache` and `Φ_K0` differ only in the *weight function* (w_CM vs w_K0); `Φ_atlas` is the degenerate limit that drops the integral. This is a *one-parameter family of weightings*, not a three-element type-list. A K-counter is the right device for counting *structurally orthogonal axes* (per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` — e.g., algebra-INVARIANT vs algebra-DEPENDENT, a genuine binary partition of functional-class). It is the WRONG device for counting *samples of a continuous weighting parameter*. If I accept "three observed weightings ⇒ 3-layer K-counter," then the next session observes a fourth weighting (say, a Dixmier-trace weighting) and I am forced to a "4-layer K-counter," then 5, ad infinitum — the K-counter inflates with every new functional anyone evaluates, which is exactly the failure mode `feedback_rules-compensate-missing-structure.md` warns against (rule bloat = missing structure).

**The structurally honest landing.** Two things are simultaneously true and must be kept separate (they are the two-clause separation of `cross-pillar-bridge-anatomy.md §"Two-clause separation"`):

1. **Per-entry registry content**: the §VII.AU CF-37 (c)∘(d) corridor genuinely exhibits ≥3 distinct F-image functionals of one saturated measure. This is worth a calibration-corpus row. Land it (CF-S93-W1-3, already Q2-routed to housekeeping §D). The row's *content* should be re-stated: not "3 discrete layers" but "the §(ii.A) atlas-row/cache-moment binary is the support-forgetting vs dimension-spectrum-weighting *pair*; the K_0-pairing is a THIRD weighting functional on the same axis, demonstrating the axis is a *weighting family*, not a binary."
2. **Rule-level K-counter advancement**: this requires the Hybrid Independence Test (`cross-pillar-bridge-anatomy.md`, `(i ∨ ii ∨ iii) ∧ iv`). One instance at one triple (SU(3) algebra, s=4 pole, (c)∘(d) corridor) is K=1. It cannot self-advance. K=2 demands a *structurally distinct* triple — a different algebra (e.g., the M_4(ℂ)_PS Pati-Salam block per seed/S91 W9 `hit_iv_pass` Wedderburn-rank distinction), a different pole (s=3 or s=5), or a different corridor — where the SAME 3-functional structure recurs and the algebraic envelope is independent (clause iv). Absent that, the axis stays SUGGESTION at K=1.

**My verdict on (c)**: the third K_0-pairing layer is NOT reducible to either existing layer (vdd is right on irreducibility) — but the correct structural object is a **re-axis of §(ii.A) from a 2-element binary to a weighting-functional family on a saturated spectral measure**, landed as a K=1 calibration row, held at SUGGESTION, NOT promoted to a standalone "3-layer K-counter." Promotion waits for a Hybrid-Independence-Test second instance at a distinct (algebra, pole, corridor) triple. This is the more durable structural change (per `output-standards.md §"What Changed" (b)`): it reframes the axis's *type* (binary → family), which is epistemically more load-bearing than incrementing a layer count.

**Questions posed to vdd**: (1) You hold R_third structurally distinct (I agree) and will likely read that distinctness as justifying the third layer as a *peer* of the other two. Do you accept the weaker-but-more-precise framing — R_third is a third *weighting functional* `Φ_K0` on the one measure `μ_{cd}`, peer to `Φ_cache` in functional-CLASS but distinct in weight — rather than a third discrete *layer-type*? (2) Does the Karoubi §I.3 K_0 pairing give a *single* canonical weighting (so the family has a distinguished member, namely yours), or is `w_K0 = d_a/Σd_a` one admissible weight among several? If yours is distinguished by the χ'-inheritance morphism, that strengthens "R_third samples the canonical at its natural weight" (V1) — but it still samples a measure, it is not the measure. (3) If we both agree the canonical is `μ_{cd}` (the measure) and disagree only on whether to *call* the K_0-pairing "the canonical" vs "the natural moment of the canonical," is that a substrate-physics disagreement or a naming convention? I want us to isolate, by R2, whether our divergence is physical or lexical.

### V4: Cross-Cutting Observations

**1. The three scales are a moment-problem, not a measurement disagreement — and this is the recurring signature of substrate-IS canonicals in this framework.** The single most important cross-cutting point: a 2-OOM span between R_ansatz/R_CM_full (3.9e-4, 8.0e-4) and R_third (7.0e-6) reads as a *contradiction* only if one assumes all three estimate the same number. Under the V1 measure-framing they are three functionals `{Φ_atlas, Φ_cache, Φ_K0}` of one measure `μ_{cd}`, and a large functional-spread is generic, not pathological. I have seen this exact structure twice before and it is worth stating as a candidate cross-session pattern:
- **CC / Hausdorff impossibility** (S44 cutoff-f, MEMORY permanent theorem): `f_4/f_2 = 1.4e-121` cannot be two finite cutoff multipliers because the object is a moment sequence, not a number. The CC "problem" is a moment problem.
- **CF-37 (c)∘(d) corridor** (this gate): three scalars at one nominal triple because the object is the measure `μ_{cd}`, sampled by three weightings.
Both are cases where the *appearance* of incompatible numbers is the substrate signalling that the canonical is a distribution. I propose (for the corpus row, V3) that the durable structural lesson of CF-37 is methodological: **"OOM-separated scalars at one nominal triple are diagnostic of a measure-valued canonical, not of a failed identification."** This generalizes beyond CF-37 and is the kind of structural reframe `output-standards.md §"What Changed" (b)` flags as more load-bearing than the numerical values.

**2. Laboratory grounding — the (c)∘(d) image IS a 3He-B-child BdG occupation spectrum, and `μ_{cd}` has a lab counterpart.** This is the contribution I own as the superfluid-universe theorist, and it sharpens what "the canonical is a measure" *means* physically. The 112 image eigenvalues `|λ| ∈ [0.8197, 1.3277]` M_KK are gapped BdG quasiparticle energies sitting above `Delta_BCS = 0.464` M_KK (the substrate gap; MEMORY canonical). In the laboratory child realization (3He-B BdG sector, the inheritance morphism's target per `3HeB-inheritance-canonical.md`), the analog of `μ_{cd}` is the **density of Bogoliubov quasiparticle states weighted by sector degeneracy** — precisely the quantity a vortex-core spectroscopy (Caroli-Matricon ladder) or NMR absorption-line measurement integrates. The three F-image functionals have lab analogs too:
- `Φ_atlas` (rank-only) ↔ counting the *number* of bound-state branches (a degeneracy count, no energy resolution).
- `Φ_cache` (CM residue) ↔ a *spectrally-weighted* sum over the BdG spectrum (energy-resolved).
- `Φ_K0` (K_0 pairing) ↔ the *topologically-weighted* spectral sum — weighting each branch by its inheritance-class (BDI-protected) dimension.
The substrate-physics point: in 3He-B the topologically-weighted sum (Φ_K0) is the *physically distinguished* one because the BDI topology (Pf=−1, N_K=2; MEMORY) is what survives the inheritance morphism — exactly why vdd's K_0 pairing is the *natural* weight. But even in the lab, the topological weight gives you ONE number (one moment); the full information is the spectrum (the measure). So the lab confirms V1's structure: the K_0 pairing is privileged-but-not-total. This is a falsifier-relevant statement — it connects to the `inheritance-falsifier-protocol.md` Class-A/B tests (the `‖φ_67‖/‖φ_88‖ = 7.3250` cohomology-asymmetry ratio is *another* moment of the same inheritance structure).

**3. The OAA discipline is intact and the adversarial axis is well-posed.** vdd is the §W1-3 producing author and is OAA-clean (the excluded set is {connes-ncg, phonon-first}, not vdd; WP lines 437-440). My substrate-physics-first axis (the canonical is the BdG occupation measure) and vdd's bridge-anatomy axis (the canonical is the Connes-Karoubi K_0 pairing) are genuinely distinct *machinery* — I reason from the spectral triple's BdG sector and Friedrich-Bär saturation; vdd reasons from Karoubi 1978 §I.3 K-homology and the χ'-inheritance KK-class. They cannot both be "the canonical" in the naive sense, which is why this is a real adjudication and not parallel agreement. But I suspect (and want R2 to test) that our divergence collapses to: *is the canonical the measure, or its naturally-distinguished moment?* — which may be physical (if the K_0 pairing carries strictly more than any single moment) or lexical (if "the K_0 pairing" just IS shorthand for "the measure read at its natural weight").

**4. A note against the asymptotic-remainder temptation (cross-link to V2 and to the s=3 sister gate).** It would be tempting, given the W1-2 §VII.AU s=3 result (does NOT saturate, rel_drift 2.374e-3; seed line 76), to import that gate's "asymptotic remainder, resolve at higher L_max" reading to the CF-37 s=4 (c)∘(d) corridor. I have argued in V2 that this import is *invalid* because the (c)∘(d) image is saturated (level-≤1, N_image=112 frozen). The cross-cutting caution: **same registry-slot family (§VII.AU) does not imply same residual-reading.** The pole index and the support structure determine the reading, not the slot name. This is itself a methodology observation worth the corpus row: residual-reading must be keyed to *support-saturation status*, not to slot-family membership.

**The sharpest questions for vdd, consolidated** (these drive R2 toward the required output — identification of the canonical OR a pre-registered discriminating gate):

- **Q-V4-1 (the crux)**: Is the Connes-Karoubi K_0 pairing you compute a *number* (one KK-pairing value at s=4) or a *functional* (a K-homology class that, paired against the resolvent at variable s, yields a moment sequence)? If the latter, we already agree the canonical is the functional/measure, and R_third = its s=4 evaluation — the disagreement is lexical. If the former, you must explain why s=4 is privileged over s=3, s=5 on the same saturated image.
- **Q-V4-2 (the discriminator)**: Do you accept the pre-registered null `R_third(L=14) ≡ R_third(L=12)` on the (c)∘(d) image (V2) as the test between asymptotic-remainder (predicts drift) and structural-signature (predicts zero drift)? If you predict drift, name the mechanism given N_image=112 is bit-identical at L=10/12.
- **Q-V4-3 (the K-counter)**: Do you accept the V3 re-framing (the §(ii.A) axis is a *weighting family*, K_0-pairing is a third weighting `Φ_K0`, K=1 SUGGESTION held pending a Hybrid-Independence second instance) — or do you hold that R_third's structural distinctness justifies a standalone 3-layer K-counter NOW? If the latter, how do you prevent K-counter inflation when a fourth weighting (Dixmier, Wodzicki) is evaluated next session?
- **Q-V4-4 (laboratory)**: Does your K_0-pairing reading have a 3He-B BdG lab observable (vortex-core / NMR spectral-degeneracy-weighted sum)? If yes, we have a laboratory route to *measure* the canonical's natural moment, which would let the §VII.AU CF-37 corridor join the cross-pillar-bridge family with a Level-3 empirical anchor — turning an abstract identification into a falsifiable prediction.

---

## Round 1 — vdd: Response & Cross-Synthesis

### Part 1: Response to volovik's Sections

#### Re: V1 — candidate canonical (substrate-physics-first vs K_0-pairing)

**EMERGES** (with a sharp correction that re-locates the canonical one level below volovik's measure). volovik's reframe is the right *move* — he is correct that all three scalars are functionals and that R_third does not "carry" the canonical, it *samples* it. Where I diverge, and where the bridge-anatomy axis supplies content his BdG-occupation axis cannot, is the identity of the sampled object. **It is not the measure `μ_{cd}`. It is a K-homology class `[φ_{cd}] ∈ K^0(A_K)`. The measure is itself a representation-dependent realization of that class, and the class is strictly finer than the measure.** This is the canonical boundary I carry (agent memory line 7, and confirmed in the knowledge graph at S64 B28 `Index(D_total) = ⟨π_!, [D_{M^4}]⟩ (K-theory pairing)` with the explicit annotation "operates at the level of K-HOMOLOGY, not at the level of the spectral action"): the Connes-Karoubi / Kasparov pairing is a **topological** object, not an analytic one.

Let me ground this against volovik's own construction, because his `μ_{cd}` is the right *intermediate* object and I want to be precise about exactly where I get off the train.

**Where I AGREE with V1, completely.** The per-sector decomposition `{(0,0):16, (0,1):48, (1,0):48}`, `N_image=112`, dim-weighted total 304, eigenvalues `|λ| ∈ [0.8197, 1.3277]` above `Delta_BCS=0.464` M_KK — all of this is substrate-IS BdG occupation physics and it IS the raw material. R_ansatz IS a zeroth-order combinatorial moment (drops the integral). R_CM_full and R_third ARE two weightings of the same spectrum. The 2-OOM span IS generic for distinct functionals of one object and is NOT a contradiction. I concede every one of these points; they are the correct substrate-first reading and I will not re-litigate them.

**Where I get off the train: a measure is a weaker invariant than a K-homology class.** A spectral measure `μ_{cd}` on the image records eigenvalue *positions* and sector-degeneracy *weights*. A K-homology class `[φ_{cd}]` records the *pairing behavior* of the spectral triple against all projections `P ∈ K_0(A_K)` — it is an element of a quotient (cycles modulo boundaries), and two genuinely different spectral measures can realize the SAME class while one identical-looking measure can sit in two different classes once you specify the Fredholm module structure (the grading γ, the real structure J, the sign of the resolvent). The decisive evidence is already in the framework's own ledger, and it is a result I did not have to invoke a priori:

- `S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM` (knowledge graph, scheme=`Connes-Karoubi-pairing-HP1-cocycle-ratio-Sage-QQ-exact`, PASS): `target_ratio = 793346/108307`, `cancellation_residual = 0`, `n_pass = 25/25`. The Connes-Karoubi pairing in THIS framework, on THIS inheritance structure, was already shown to produce a **RANK-2-invariant RATIO of cocycle norms** — Sage-QQ exact, surviving the inheritance morphism. That ratio (the `‖φ_67‖/‖φ_88‖ = 7.3250` cohomology-asymmetry structure, `inheritance-falsifier-protocol.md` Class B) is a property of the *class*, not of any single moment of `μ_{cd}`. You cannot read `793346/108307` off a measure by integrating `|λ|^{-s}` against degeneracy weights at any single s — it is a *relative* invariant of two cocycles, intrinsically cohomological.

So the structure is a three-level descent, and volovik's `μ_{cd}` is the MIDDLE level:

```
[φ_{cd}] ∈ K^0(A_K)              (the canonical: K-homology class; topological; carries cocycle-RATIO invariants)
    │  realize via (H_K, D_K, γ, J) at τ_fold       [forgets the Fredholm-module fine structure]
    ▼
μ_{cd}  (volovik's K_0-weighted spectral measure)    (records eigenvalue positions + sector weights)
    │  contract via Φ_layer (a weighting functional at pole s)   [forgets all but one moment]
    ▼
{R_ansatz, R_CM_full, R_third}   (the three scalars: F-images / moments)
```

volovik collapses the top arrow — he treats `μ_{cd}` as the canonical. I claim the top arrow is a genuine forgetful map with non-trivial kernel: the cohomology-RATIO content (`793346/108307`, `7.3250`) lives in `[φ_{cd}]` and is NOT recoverable from `μ_{cd}` alone. That is the precise sense in which "the K_0 pairing is privileged-but-not-total" (V4.2) is half-right: it is not total over the *moments* of the measure, but the *class* it pairs against IS total over the measure — it sits strictly above it.

**Direct answer to V1's two posed questions:**

(V1-Q1, the s-dependence test): volovik asks — if I evaluated the K_0 pairing at s=3 or s=5 on the same image, would I get a different "canonical"? **Yes, I would get a different NUMBER — and that is exactly volovik's point and I concede it: R_third at fixed s IS a moment, not the canonical.** But the s-knob does NOT vary the pairing; it varies which *operator* I pair the class against (the resolvent power `D_K^{-2s}` is the operator; `[φ_{cd}]` is the class). Varying s slides along the *moment sequence of one fixed class*. So the s-dependence proves R_third is a moment (agreed) — it does NOT prove the canonical is the measure. It proves the canonical is whatever is INVARIANT under the s-slide, and that invariant is the class `[φ_{cd}]` (and its cocycle-ratio shadows), not the measure (which is also fixed under the s-slide but is a coarser invariant, per the descent above).

(V1-Q2, Karoubi §I.3 — measure or number?): **Neither, in the way volovik framed the dichotomy — and this is the crux I sharpen in Re:V4.** Karoubi 1978 §I.3 gives a **pairing of K-theory with K-homology**, `K_0(A) × K^0(A) → ℤ`. Evaluated against a single projection it yields ONE integer (the index — a number, volovik's "former"). But the object Karoubi hands you is the *pairing map itself*, equivalently the K-homology class `[φ]` viewed as a functional `K_0(A) → ℤ`. That functional is neither a single number nor a continuous measure — it is a **finitely-generated homomorphism on a discrete lattice** `K_0(A_K)`. For `A_K = ℂ ⊕ ℍ ⊕ M_3(ℂ)`, `K_0(A_K) = ℤ^3` (one ℤ per simple summand), so `[φ_{cd}]` is a point in `Hom(ℤ^3, ℤ) = ℤ^3` — *three integers*, the index pairings against the three minimal projections. That is the canonical: a triple of integers (a K-homology class), of which the per-sector weights `{1,3,3}` volovik uses are the *dimension shadows* and `R_third` is one ℝ-valued moment. So the honest answer to volovik's dichotomy is: the Karoubi object is a **functional on a discrete lattice (≅ ℤ^3), not a measure on a continuum and not a single scalar**. We agree it is not a scalar; we disagree that its natural home is the continuous measure `μ_{cd}` — its natural home is the discrete K_0 lattice, which is finer.

#### Re: V2 — Z_factor excess reading

**AGREE** on the verdict (structural signature, L_max-fixed, NOT asymptotic remainder), **AGREE** on the substitution chain, and I **accept the pre-registered null** `R_third(L=14) ≡ R_third(L=12)` on the (c)∘(d) image as the decisive discriminator. volovik's Step 6 (`d(Z_factor)/d(L_max) = 0` exactly, because the image eigenvalues are frozen at level-1 saturation and the Wedderburn fraction 3/6 is pure algebra) is correct and dimensionally clean; I have nothing to subtract from it. The Friedrich-Bär foreclosure argument (NEW sectors at higher L_max contribute eigenvalues `≥ η_FB·√(C_2(p+q=L_max)+1) > |λ|_max=1.3277`, so they cannot enter the closed (c)∘(d) image) is the same saturation machinery I rely on for bottom-K observables (agent memory: L_crit=3, N*~4 SDW truncation, Casimir-bound feasibility) and I endorse it on the image.

There are two refinements I add from the bridge-anatomy side — one that *strengthens* his reading and one that answers his posed question precisely.

**Refinement 1 (strengthens V2): the 2.28% is not merely "spectral spread is non-trivial" — it is an obstruction-class shadow, and that is WHY it cannot be a clean rational.** volovik reads the 2.28% as "the eigenvalues are genuinely spread (spread factor 1.62×), so the rank-only surrogate undershoots the true moment" (V2, "What the 2.28% positively IS"). True, but it does not explain why the miss lands so *tightly* near 2 (2.28%, not 30%). The bridge-anatomy explanation: `R_ansatz`'s rank fraction `3/6 = 1/2` is the K_0-RANK image of the inheritance morphism (image rank 3 / ambient rank 6), an INTEGER-lattice quantity. `R_CM_full` is a `ζ_D(0)`-residue, which by the local index formula (CM-1995 §5, knowledge-graph theorem: "requires a regular spectral triple with simple dimension spectrum") differs from the integer K_0-rank pairing by the *non-integer* part of the heat-kernel expansion — the local Seeley-DeWitt curvature content. So `Z_factor = R_CM_full/R_ansatz` is structurally `(integer index + analytic remainder) / (integer index)` `= 2 × (1 + ε_analytic)`, and `ε_analytic = 2.28%` is precisely the **fractional analytic (non-topological) content of the dimension-spectrum residue relative to its integer K_0-rank skeleton**. It is near 2 because the topological skeleton IS 2 (= 14/7 = 6/3, the over-determined rank-2, exactly as volovik says); it MISSES 2 by 2.28% because `ζ_D(0)` carries the local curvature term that the bare rank cannot. This is the same TOPOLOGY-vs-ANALYSIS split that is my canonical boundary (agent memory line 7): the integer part is the Kasparov/K_0 topology, the 2.28% is the spectral-action analysis. So the 2.28% is not just "spread" — it is the *measurable analytic-defect of the cache-moment from its own topological skeleton*, and that is a sharper structural statement than "the measure is not a point mass."

**Refinement 2 (answers V2-Q1 directly — which scale does the remainder live on?): the remainder lives on R_CM_full, NOT on R_third.** volovik asks whether I read the 2.28% as a remainder on R_CM_full (which he claims is frozen) or on R_third. **On R_CM_full** — and "frozen on the image" and "carries an analytic remainder" are not in tension, because the analytic remainder is a fixed (L_max-independent, on the saturated image) curvature term, not a converging tail. R_third does NOT acquire L_max-sensitivity: `N_image=112` is bit-identical at L=10/12 (and will be at L=14), the per-sector K_0 weights `d_a/304` are pure algebra, so `R_third(L=14) ≡ R_third(L=12)` is forced. I therefore predict ZERO drift on R_third, which is volovik's structural-signature prediction, NOT the asymptotic-remainder prediction. **I am not the agent who predicts drift.** If R_third *did* move between L=12 and L=14, my reading (like volovik's) would be falsified, and the most likely culprit would be a bug in the level-≤1 sector restriction (a NEW sector leaking into the image), exactly as volovik flags. So the L=14 null is a genuine shared discriminator and I co-sign the pre-registration. The one place I'd sharpen the pre-registration: the null should be checked SEPARATELY on R_CM_full and R_third, because they are frozen for DIFFERENT reasons (R_CM_full frozen because its support is closed; R_third frozen because its weights AND support are both pure-algebra/closed) — a single combined check could mask a sector-leak that moves R_CM_full's analytic remainder while leaving R_third's degeneracy-count untouched.

**Net on V2:** the 2.28% is a structural signature (full agreement with volovik); the bridge-anatomy adds that it is specifically the *analytic defect of the dimension-spectrum residue from its integer K_0-rank skeleton* (the TOPOLOGY/ANALYSIS split), which is why it sits tight-but-not-on 2; and the remainder is carried by R_CM_full, not R_third.

#### Re: V3 — 3-layer K-counter justification

**AGREE — and I retract the "NEW 3-layer CF-37 axis K-counter" language from my own §W1-3 WP draft.** volovik is right and I was wrong on the structural shape. My WP section (lines 453, 504, 516, 527) repeatedly asserts a "3-layer CF-37 axis K-counter" extension. That framing is a category error of exactly the kind volovik names: it counts *samples of a weighting parameter* as if they were *structurally orthogonal axes*, and it would inflate without bound the moment anyone evaluates a fourth functional (Dixmier, Wodzicki, Wodzicki∘HKR — and note §W1-4/§W2-3 in this very session are evaluating Wodzicki-class functionals on adjacent corridors). The Hybrid Independence Test (`cross-pillar-bridge-anatomy.md`, `(i ∨ ii ∨ iii) ∧ iv`) cannot be self-satisfied by one triple `(SU(3) algebra, s=4 pole, (c)∘(d) corridor)` reporting three functionals; clause (iv) "independent algebraic envelope" fails because the three functionals share the one image. I accept K=1 SUGGESTION held pending a Hybrid-Independence second instance at a *structurally distinct* triple. This is a correction to the registry-landing content, and per `feedback_fix-in-session-never-defer.md` it should be reflected when the corpus row lands (CF-S93-W1-3): the row must NOT say "3-layer K-counter," it must say "weighting-functional family."

I also **AGREE** with volovik's irreducibility argument in full: R_third is NOT an atlas-row (it integrates 112 eigenvalues, not a rank fraction) and NOT a cache-moment in the CM-1995 §III.4 sense (it uses the K_0 inheritance weight `d_a/304`, not the dimension-spectrum `ζ_D(0)` cubic-ρ weight `w_CM`). His `Φ_atlas / Φ_cache / Φ_K0` decomposition (V3, lines 119-123) is exactly correct: `Φ_cache` and `Φ_K0` differ only in the weight function (`w_CM` vs `w_K0`), `Φ_atlas` is the degenerate limit dropping the integral. That is one weighting family, not three bins. The §(ii.A) binary is genuinely insufficient (my WP line 453 was right about *that*) — but "insufficient binary ⇒ family re-axis," NOT "⇒ 3-bin K-counter."

**Two refinements from bridge-anatomy — one strengthens his anti-inflation rule, one bounds it.**

**Refinement 1 (strengthens, and supplies the principled STOPPING rule volovik's framing needs): the weighting family is not an open continuum — it is a fibration over a FINITE topological base, and every admissible weighting factors through the same class `[φ_{cd}]`.** volovik's anti-inflation worry is "the next session evaluates a Dixmier weighting, then a Wodzicki weighting, and I'm forced to K=4, K=5, ad infinitum." The bridge-anatomy answer removes the worry at the root: by the descent in Re:V1, EVERY one of these weightings (`Φ_cache`, `Φ_K0`, a future `Φ_Dixmier`, `Φ_Wodzicki`) is a contraction functional `μ_{cd} → ℝ` that factors through the SAME K-homology class `[φ_{cd}] ∈ K_0(A_K)^∨ ≅ ℤ^3`. They are all moments of one finite-rank object. So the family is *parameterized by a choice of weight function but anchored to a finite (rank-3 lattice) topological invariant* — it is a fiber bundle whose base is the 3-integer K_0 class and whose fiber is "which moment / weight you read." Counting fibers (weightings) is meaningless (volovik's point, sharpened); counting bases (structurally distinct K_0 classes at distinct triples) is the only K-counter-legitimate operation. This gives the anti-inflation rule a *derivation* rather than a heuristic: you do not increment the K-counter for a new weighting BECAUSE all weightings on a fixed image pair the same finite K_0 class — there is provably nothing new to count until the class itself changes (new algebra / new image / new corridor). That is exactly the Hybrid Independence Test's clause (iv), now with a topological reason behind it.

**Refinement 2 (a bound on the re-axis — answers V3-Q2, is `w_K0` distinguished?): yes, `w_K0 = d_a/Σd_a` IS the distinguished weighting, by the χ'-inheritance morphism — but "distinguished moment" still ≠ "the canonical."** volovik asks whether Karoubi §I.3 gives a single distinguished weighting or one among many. The χ'-inheritance morphism (S89 §W2-3, `kernel_M3C_dim=9`, `K_counter=2to3`, PASS, audit_sha=`90bba262af80a04c`) DOES distinguish `w_K0`: the per-sector weight `d_a` is forced to be the Wedderburn dimension of the sector in the IMAGE of χ' (M_3(ℂ)→0, so the (1,1)-adjoint-type content is killed and only the `{1,3,3}` survive with their image-dimensions). No other weight function is canonical *with respect to the inheritance morphism* — `w_CM` is canonical with respect to the dimension-spectrum, a different structure. So among the weighting family, `Φ_K0` is the member singled out by the substrate's OWN inheritance map, which is why R_third is "the natural moment" and why it deserves its place in the corpus row. **But** — and here I hold volovik's line against my own earlier instinct — a distinguished moment is still a moment. `Φ_K0` reads `[φ_{cd}]` at ONE weight; the cocycle-RATIO content (`793346/108307`, the rank-2 invariant from `S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM`) requires reading the class against TWO projections and taking a ratio — it is not any single `Φ`. So even the distinguished weighting does not exhaust the class. The canonical is the class; `Φ_K0` is its naturally-distinguished scalar shadow.

**Direct answer to V3-Q3 (is our divergence physical or lexical?):** It is **mostly lexical, with a small irreducibly-physical residue, and I can name the residue precisely.** The lexical part: "call it the canonical" vs "call it the natural moment of the canonical" — if "canonical" means `μ_{cd}` (volovik) vs `[φ_{cd}]` (me), and if `[φ_{cd}]` and `μ_{cd}` carried identical information, the choice would be naming. The physical residue: they do NOT carry identical information. `[φ_{cd}] ∈ ℤ^3` (a discrete K_0 class with cocycle-ratio invariants) is strictly FINER than `μ_{cd}` (a continuous measure), by the descent in Re:V1 and the concrete witness `793346/108307` (a class invariant not extractable from the measure's moments). So the divergence is physical *to the extent that the cohomology-ratio content is real* — and the framework's own ledger (`S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM` PASS, `7.3250` falsifier in `inheritance-falsifier-protocol.md`) says it is real and even *falsifiable*. The clean way to state the converged position: **the canonical is the K-homology class `[φ_{cd}]`; `μ_{cd}` is its representation-dependent measure realization (volovik's correct intermediate object); the three scalars are moments of the measure; and `Φ_K0` is the moment the inheritance morphism distinguishes.** That sentence is something I expect we both sign.

#### Re: V4 — cross-cutting

I take the four cross-cutting observations and then answer Q-V4-1…4 head-on.

**Re V4.1 (moment-problem pattern, CC/Hausdorff parallel): AGREE the pattern is real, REFINE the lesson.** volovik proposes the durable methodological lesson "OOM-separated scalars at one nominal triple are diagnostic of a measure-valued canonical, not of a failed identification." I agree this is a genuine cross-session pattern (the S44 CC/Hausdorff `f_4/f_2 = 1.4e-121` is the right precedent — a moment problem, not a number). I refine the lesson on the strength of Re:V1: the diagnostic should read **"…diagnostic of a canonical that is a moment-sequence / class, of which the OOM-separated scalars are distinct moments."** The word "measure" undersells it in the CF-37 case specifically, because the CF-37 canonical carries cohomology-RATIO content beyond any measure (the `793346/108307` witness). For the CC case volovik's "measure" wording may be exactly right (a Hausdorff moment sequence is a measure problem); for CF-37 the canonical is finer (a K-homology class). So the corpus lesson should be stated at the level both cases share — "moment-sequence / class-valued canonical, scalars are moments" — and flag that the *fineness* of the canonical (measure vs class) is case-dependent and determined by whether cohomology-ratio invariants are present.

**Re V4.2 (3He-B lab grounding, μ_{cd} has a lab counterpart): AGREE and this is the most valuable thing in V4 — it gives the corridor a Level-3 anchor route.** volovik's mapping is correct and I endorse it: `Φ_atlas` ↔ branch-count (degeneracy, no energy resolution); `Φ_cache` ↔ spectrally-weighted BdG sum (energy-resolved); `Φ_K0` ↔ topologically-weighted spectral sum (each branch weighted by inheritance-class dimension). And his substrate-physics point — that in 3He-B the topologically-weighted sum is physically distinguished because the BDI topology (Pf=−1, N_K=2) is what survives the inheritance morphism — is exactly the bridge-anatomy reason `w_K0` is the distinguished weight (my Re:V3 Refinement 2, reached from the χ'-morphism side; we converge). I add one bridge-anatomy sharpening that turns this into a *falsifiable* statement (see Q-V4-4 below): the topologically-weighted sum is not just "one number" — by the Connes-Karoubi index theorem it is an INTEGER on the closed image, and an integer-valued lab observable is a strong falsifier (it cannot drift continuously; it jumps or it doesn't).

**Re V4.3 (OAA discipline / adversarial axis well-posed): AGREE, and I confirm the axes are genuinely distinct machinery, not parallel agreement.** volovik reasons from the BdG sector + Friedrich-Bär saturation; I reason from Karoubi 1978 §I.3 K-homology + the χ'-inheritance KK-class + the TOPOLOGY/ANALYSIS boundary (agent memory line 7). We reach a CONVERGENT structure (the descent `[φ_{cd}] → μ_{cd} → {scalars}`) from opposite directions, which is the signature of a real adjudication producing a robust result rather than shared-context echo. His suspicion that the divergence "collapses to: is the canonical the measure or its naturally-distinguished moment?" is *almost* right — I have refined it (Re:V3-Q3) to: is the canonical the measure `μ_{cd}` or the FINER class `[φ_{cd}]` that the measure realizes? The residual disagreement is physical (the cohomology-ratio content), not lexical, but it is *small and localized* — we agree on everything except whether the top arrow of the descent has non-trivial kernel.

**Re V4.4 (against importing the s=3 sister-gate's asymptotic-remainder reading): AGREE without reservation.** "Same registry-slot family (§VII.AU) does not imply same residual-reading" is correct and important. The §VII.AU.OP-PROJ s=3 gate (W1-2, rel_drift=2.374e-3, NEW-sector intrusion 25.14%, does NOT saturate) and the CF-37 (c)∘(d) s=4 corridor (saturated, N_image=112 frozen) have OPPOSITE support structures and therefore opposite residual readings. Residual-reading keyed to support-saturation status, not slot-family membership — I co-sign this as a methodology observation worth the corpus row. (Bridge-anatomy footnote: this is itself the TOPOLOGY/ANALYSIS boundary again — the s=3 observable's support GROWS, so it has genuine `L^{-α}` Level-2-envelope analysis content; the s=4 (c)∘(d) image is CLOSED, so its only residual is the fixed analytic-defect from its integer skeleton, per my Re:V2 Refinement 1. The two gates differ in residual-reading precisely because one has open support (analysis) and one has closed support (a fixed topological skeleton plus a fixed analytic defect).)

---

**The four consolidated questions, answered directly:**

**A-V4-1 (THE CRUX — number or functional?): It is a FUNCTIONAL — specifically a homomorphism on the discrete lattice `K_0(A_K) ≅ ℤ^3` — which means we AGREE the disagreement is *largely* lexical, but I locate the canonical one level FINER than you do.** The Connes-Karoubi pairing `⟨[φ_{cd}], ·⟩ : K_0(A_K) → ℤ` is the canonical object. Paired against ONE projection it gives one integer (your "number" horn); but the object Karoubi hands you is the pairing MAP, equivalently the class `[φ_{cd}] ∈ ℤ^3` (your "functional/measure" horn — except the home is the discrete K_0 lattice, not a continuous measure). So: NOT a single scalar (we agree), and NOT privileged at s=4 over s=3/s=5 (I concede — the s-knob slides moments of one fixed class, R_third is the s=4 moment, you are right). The canonical is what is INVARIANT under the s-slide: the class `[φ_{cd}]`. Your `μ_{cd}` is ALSO invariant under the s-slide, so you could stop there — but `[φ_{cd}]` is strictly finer (carries `793346/108307`; `μ_{cd}` does not), so the honest stopping point is the class. **Lexical to the extent `μ_{cd}` and `[φ_{cd}]` agree; physical exactly at the cohomology-ratio content where they disagree.**

**A-V4-2 (the discriminator — accept the L=14 null?): YES, accepted and co-signed; I predict ZERO drift on R_third (your structural-signature prediction), and I am NOT the agent who predicts drift.** R_third cannot acquire L_max-sensitivity: `N_image=112` bit-identical at L=10/12 (and forced at L=14 by level-≤1 saturation), per-sector K_0 weights `d_a/304` are pure algebra. Mechanism for zero drift: the image is a CLOSED finite set and the K_0 weights are topological (integer dimensions), so neither the support nor the weights can move. I add the sharpening from Re:V2: check the null SEPARATELY on R_CM_full and R_third (different freezing reasons), so a sector-leak that moves R_CM_full's analytic remainder cannot hide behind R_third's frozen degeneracy count. If R_third drifts, BOTH our readings fail and the diagnosis is a level-≤1 restriction bug — agreed.

**A-V4-3 (the K-counter — re-axis or standalone 3-layer counter?): I accept your re-framing IN FULL and retract my WP's standalone-3-layer-counter language.** The §(ii.A) axis is a *weighting-functional family on a saturated K-homology class*, K_0-pairing is a third weighting `Φ_K0` (the inheritance-distinguished one), held at K=1 SUGGESTION pending a Hybrid-Independence second instance at a distinct (algebra, pole, corridor) triple. I PREVENT the inflation you fear with the Re:V3 Refinement-1 stopping rule: every weighting (Dixmier, Wodzicki, …) factors through the same finite K_0 class `[φ_{cd}] ∈ ℤ^3`, so there is provably nothing new to count until the CLASS changes — counting weightings is illegitimate, counting distinct classes is the only K-counter operation. This is the topological *derivation* of your anti-inflation heuristic. So no, R_third's distinctness does NOT justify a standalone counter now; it justifies the family re-axis, K=1.

**A-V4-4 (laboratory — does the K_0 reading have a 3He-B observable?): YES, and it is a STRONGER falsifier than a generic spectral measurement because it is INTEGER-valued — this is the route to a Level-3 empirical anchor and I propose pre-registering it.** The K_0 pairing on the closed (c)∘(d) image is, by the Connes-Karoubi index theorem, an INTEGER (the index of the Fredholm module against each minimal projection): the triple `[φ_{cd}] ∈ ℤ^3`. The 3He-B child observable is the **count of topologically-protected (BDI) bound-state branches in the vortex-core / surface Andreev spectrum, resolved per inheritance sector** — i.e., a spectral-degeneracy-weighted sum that, because the BDI winding `N_K=2` is integer-quantized, yields an INTEGER per sector. This is exactly volovik's `Φ_K0`-lab-analog, made precise: not a continuous spectrally-weighted sum, but the *integer* topological invariant (Caroli-Matricon ladder branch-count with the BDI protection enforcing integer steps). An integer-valued lab observable is the strongest kind of falsifier in the `inheritance-falsifier-protocol.md` framework — it is the Class-A kernel-signature test in its sharpest form (a NULL or a specific integer, not a continuous value to be conversion-shopped). This lets the §VII.AU CF-37 (c)∘(d) corridor join the cross-pillar-bridge family with a genuine Level-3 anchor: substrate-IS = `[φ_{cd}] ∈ ℤ^3` (K_0 class), laboratory-IN = integer BDI branch-count per inheritance sector in 3He-B vortex-core spectroscopy, bridge map = Connes-Karoubi pairing, Level-2 envelope = trivial (integer is L_max-saturated on the closed image — no envelope needed), Level-3 = the measured integer triple. I pre-register this as a sharp R2 carry-forward (see D3). Caveat (regime of validity): the integer-quantization holds only on the CLOSED image (level-≤1, saturated); it would NOT hold for the s=3 OP-PROJ observable whose support grows — there the lab observable is a continuous spectral sum, not an integer. The integer falsifier is special to the saturated corridor.

### Part 2: Original Analysis

#### D1: Connes-Karoubi K_0-inheritance-class pairing case (R_third structural distinctness; χ'-inheritance morphism kernel, S89 §W2-3 audit_sha=90bba262af80a04c)

The positive case for what R_third *is*, stated from the K-homology side. R_third's structural distinctness (`F3/F1 = 0.018`, `F3/F2 = 0.009` — matches NEITHER prior layer at 2 OOM, §W1-3 WP line 524) is not a numerical accident: it is the signature that `Φ_K0` reads the spectrum against a weight function that lives in a *different cohomological register* from both `Φ_atlas` (rank-combinatorics) and `Φ_cache` (dimension-spectrum). I build the case in four steps, then state the canonical identity.

**Step 1 — The χ'-inheritance morphism fixes the K_0 lattice and the surviving sectors.** From S89 §W2-3 (`S89-INDEPENDENT-CHI-PRIME-INHERITANCE-MORPHISM-M2C-CL1-TARGET`, PASS, audit_sha=`90bba262af80a04c`; knowledge-graph value `kernel_M3C_dim=9; indep_from_chi=True; dim_M3=9_vs_dim_target=8_contradiction=True; K_counter=2to3`): the inheritance morphism χ' acts on `A_K = ℂ ⊕ ℍ ⊕ M_3(ℂ)` by sending `M_3(ℂ) → 0` (kernel = entire M_3(ℂ), HS-dim 9) and `ℂ ⊕ ℍ → ` the canonical target. At the level of K-theory this is a homomorphism `χ'_* : K_0(A_K) → K_0(target)`. Now `K_0(A_K) = K_0(ℂ) ⊕ K_0(ℍ) ⊕ K_0(M_3(ℂ)) = ℤ ⊕ ℤ ⊕ ℤ = ℤ^3` (Morita: `K_0(M_n(ℂ)) = K_0(ℂ) = ℤ`, one generator per simple summand, the class of a minimal projection). The morphism kills the third generator (the `M_3(ℂ)` class), so `image(χ'_*) = ℤ^2` (the ℂ and ℍ generators survive) and `ker(χ'_*) ⊇ ` the `M_3(ℂ)` generator. **This is the substrate-IS origin of the per-sector survival `{(0,0), (0,1), (1,0)}`**: those are precisely the Peter-Weyl sectors whose K_0 content is NOT annihilated by the inheritance morphism. The adjoint-type `(1,1)` content (which would carry the `M_3(ℂ)` register) is gone.

**Step 2 — The K_0 weight `w_K0 = d_a/Σd_a` is the dimension-shadow of the surviving lattice generators, NOT a free choice.** Each surviving sector `a ∈ {(0,0),(0,1),(1,0)}` contributes to the pairing proportional to the rank of its image projection, i.e. its Wedderburn dimension in `image(χ')`: `d_{(0,0)}=1` (from ℂ), `d_{(0,1)}=d_{(1,0)}=3` (fundamental triplets, the ℍ→M_2(ℂ) and conjugate content). Normalization `Σ d_a = 1·16 + 3·48 + 3·48 = 304` is the substrate's natural pairing measure (§W1-3 WP line 489). This weight is *forced by the morphism* — it is the only weighting under which R_third equals the index pairing `⟨[φ_{cd}], [P_image]⟩` against the image projection. (Contrast `w_CM`, forced by the dimension-spectrum poles; and `3/6`, forced by nothing but bare rank-counting. Three different cohomological registers, three different forced weights — D2 develops this.)

**Step 3 — The pairing is, by Connes' index theorem, INTEGER-valued before the `(M_KK/M_Pl)^2` dimensional dressing.** The raw object `Σ_{λ∈image} sign-graded contribution · w_K0(λ)` paired against a projection in `K_0(A_K)` is `Index(P D_K^{-} P) ∈ ℤ` (Fredholm index of the off-diagonal Dirac block restricted to the projection). The continuous-looking value `R_third = 6.96e-06` is NOT the index — it is `(M_KK/M_Pl_reduced)^2 × K0_pairing_normalized` (§W1-3 WP line 492), where the `(M_KK/2.435e18)^2 ≈ 10^{-32}` prefactor is the α''(M_LRD) dimensional convention shared with R_ansatz/R_CM_full, and `K0_pairing_normalized = (1/112)·Σ|λ|^{-4}·(d_a/304)` is a real *moment* of the integer class. So R_third has TWO pieces of structure: a topological skeleton (the integer triple `[φ_{cd}] ∈ ℤ^3`) and an analytic dressing (the `|λ|^{-4}` resolvent moment + the dimensional prefactor). This two-piece structure is the TOPOLOGY/ANALYSIS split (agent memory line 7) made concrete on this corridor — and it is the same split I identified in Re:V2 as the source of the Z_factor's 2.28% (the integer skeleton is 2, the analytic dressing is the 2.28% defect).

**Step 4 — Why R_third matches NEITHER prior layer (the structural distinctness, derived not observed).** `F3/F1 = R_third/R_ansatz = 0.018` and `F3/F2 = R_third/R_CM_full = 0.009`. R_ansatz is `(M_KK/M_Pl)^2 · (3/6)` — pure rank fraction, the `|λ|^{-4}` moment REPLACED by 1/2 (it never sees an eigenvalue). R_third is `(M_KK/M_Pl)^2 · (1/112)·Σ|λ|^{-4}·(d_a/304)` — and the eigenvalues `|λ| ∈ [0.8197, 1.3277]` give `|λ|^{-4} ∈ [0.322, 2.215]`, averaged-and-degeneracy-weighted-and-divided-by-112 to a number ~10^{-2} smaller than the bare 1/2. The 56× gap is the difference between "fraction of surviving rank" (R_ansatz, dimensionless O(1)) and "K_0-weighted resolvent moment at s=4 normalized by mode-count" (R_third, O(10^{-2}) after the 1/112). It is NOT a discrepancy to be reconciled — it is two different functionals, exactly as volovik's `Φ_atlas` vs `Φ_K0` says. The 115× gap to R_CM_full is the same story with `w_CM` (dimension-spectrum cubic-ρ weight) in place of `w_K0`.

**The canonical identity (D1 verdict).** R_third is **not** the canonical. The canonical at the (c)∘(d) corridor is the **K-homology class `[φ_{cd}] ∈ K^0(A_K)`, equivalently the integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)}) ∈ ℤ^3` of index pairings against the three surviving minimal projections**, fixed by the χ'-inheritance morphism (M_3(ℂ)→0). R_third is the s=4 ℝ-valued moment of that class under the inheritance-distinguished weight `w_K0`, dressed by the α''(M_LRD) dimensional prefactor. This identification:
- explains the structural distinctness (different cohomological register from rank-combinatorics and dimension-spectrum) — derived in Step 4, not posited;
- is grounded in a LANDED structural theorem (S89 §W2-3, the morphism kernel, audit_sha=`90bba262af80a04c`) — not a new derivation;
- respects my canonical boundary (the pairing is TOPOLOGY — an integer class — not ANALYSIS — a spectral moment; agent memory line 7);
- and concedes volovik's central point: R_third is a moment, "closest but not the canonical" (V1). I differ only in placing the canonical at the class `[φ_{cd}]` rather than the measure `μ_{cd}` — a refinement, because the class is the finer invariant (Re:V1 descent; `793346/108307` witness).

#### D2: Further analysis — is R_third reducible to atlas-row or cache-moment, or a genuine third layer?

This is the (c)-question, settled from the bridge-anatomy side. The answer has two clauses that must be kept separate (per `cross-pillar-bridge-anatomy.md §"Two-clause separation"`): **(i) functional-class irreducibility** — R_third is genuinely NOT reducible to either §(ii.A) layer (volovik and I agree, and I derive it below); **(ii) K-counter status** — but irreducibility does NOT license a standalone "3-layer K-counter," only the family re-axis at K=1 SUGGESTION (volovik is right, I retract my WP over-reach).

**Clause (i) — functional-class irreducibility, derived via the three cohomological registers.** The §(ii.A) parent draws ONE binary within the algebra-INVARIANT family: atlas-row (closed-form algebraic identity, locked-norm L_k=1) vs cache-moment (numerical residue on the L_max-truncated spectrum). The decisive structural fact is that the three F-images live in three DISTINCT cohomological registers, each with its own forced weight:

| F-image | Functional `Φ` | Weight, and what FORCES it | Cohomological register |
|:--------|:---------------|:---------------------------|:-----------------------|
| R_ansatz | `Φ_atlas` | `3/6` — forced by bare K_0-RANK counting (image rank / ambient rank); NO eigenvalues | K_0-rank (zeroth Betti-type; degree-0 combinatorics) |
| R_CM_full | `Φ_cache` | `w_CM(λ)` — forced by the dimension-spectrum poles of `ζ_D(s)` (CM-1995 §III.4 cubic-ρ residue) | dimension-spectrum / Hochschild (heat-kernel Seeley-DeWitt register) |
| R_third | `Φ_K0` | `w_K0 = d_a/304` — forced by the χ'-inheritance morphism (S89 §W2-3, surviving-sector image-dimensions) | K-homology index pairing (Connes-Karoubi / Karoubi §I.3 register) |

Reducibility would require one register's forced weight to be expressible in another's. It cannot be:
- **R_third is NOT atlas-row**: an atlas-row identity is closed-form on `A_K` *independent of the spectrum* (e.g. `3/6`). R_third integrates 112 actual eigenvalues `|λ|^{-4}` (§W1-3 WP lines 386, 490). No spectrum-independent closed form gives `6.96e-06` — the value depends on `|λ| ∈ [0.8197, 1.3277]`. Irreducible to atlas-row. ✓ (volovik V3, agreed and re-derived.)
- **R_third is NOT cache-moment-in-the-CM-sense**: superficially it is evaluated on the L_max=12 cache, so it "looks" cache-moment. But the CM-1995 cache-moment is the `ζ_D(0)`-dimension-spectrum residue (weight set by the *analytic* pole structure); R_third is the K_0 index pairing (weight set by the *topological* inheritance morphism). They coincide ONLY if the spectral density happened to factor as the sector-dimension density — which it does not (else R_third = R_CM_full, contradicting the 115× gap). DIFFERENT FUNCTIONALS of the same cache. ✓ (volovik V3, agreed.)

So R_third sits in neither §(ii.A) bin. The §(ii.A) binary is genuinely insufficient — my WP line 453 was correct about *that*. The bridge-anatomy *derivation* of the insufficiency (three forced weights in three registers, no inter-register reduction) is the content I add beyond volovik's "different weight function" observation: the weights are not merely different, they are forced by three structurally orthogonal pieces of machinery (rank-counting / dimension-spectrum / inheritance-morphism), which is *why* no reduction exists.

**Clause (ii) — but this is a FAMILY re-axis, not a 3-bin K-counter (I retract the WP language).** The crucial move volovik makes, and which I now fully endorse, is that "three forced weights" does NOT mean "three discrete layer-types to be counted." All three weights are contraction functionals on the SAME object, and — this is the bridge-anatomy stopping rule from Re:V3 Refinement 1 — every admissible weighting factors through the SAME finite K-homology class `[φ_{cd}] ∈ ℤ^3`. Concretely:

```
§(ii.A), corrected reading:
  the axis is a WEIGHTING-FUNCTIONAL FAMILY Φ_w : [φ_{cd}] ↦ (M_KK/M_Pl)^2 · ∫|λ|^{-s} w(λ) dμ_{cd}
  parameterized by weight w, ANCHORED to the finite topological base [φ_{cd}] ∈ ℤ^3.
    w = (drop integral, keep rank)        → Φ_atlas  → R_ansatz   (degenerate fiber)
    w = w_CM  (dimension-spectrum)         → Φ_cache  → R_CM_full
    w = w_K0  (inheritance-morphism)       → Φ_K0     → R_third    (inheritance-DISTINGUISHED fiber)
    w = w_Dixmier, w_Wodzicki, …           → further fibers, ALL through the same [φ_{cd}]
```

Counting fibers (weightings) is illegitimate — it would force K=4 the moment §W1-4/§W2-3's Wodzicki functionals are read on an adjacent corridor, then K=5 for Dixmier, ad infinitum (the `feedback_rules-compensate-missing-structure.md` rule-bloat failure mode). Counting bases (distinct K_0 classes at distinct triples) is the only K-counter-legitimate operation, and it requires the Hybrid Independence Test (`(i ∨ ii ∨ iii) ∧ iv`). One triple `(SU(3), s=4, (c)∘(d))` with three fibers is K=1: clause (iv) "independent algebraic envelope" FAILS because the three fibers share one image (one envelope). **D2 K-counter verdict: K=1 SUGGESTION, held; the corpus row (CF-S93-W1-3) must be re-titled "weighting-functional family on a saturated K-homology class," NOT "3-layer CF-37 axis K-counter."** Promotion to K=2 requires a structurally distinct triple — the natural candidate is the M_4(ℂ)_PS Pati-Salam block (seed / S91 W9 `hit_iv_pass` Wedderburn-rank distinction), where the SAME three-register structure must recur with an *independent* image/envelope.

**Why the family re-axis is the more durable structural change (per `output-standards.md §"What Changed" (b)`).** Incrementing a layer count (2→3) is a numerical-style revision; re-typing the axis from a binary to a *family fibered over a finite topological base* is a structural change — it alters the epistemic TYPE of the §(ii.A) discipline and supplies a derivation (the topological stopping rule) for the anti-inflation behavior that was previously a heuristic. That re-typing is the load-bearing output of this workshop, and I credit volovik with the move; my contribution is the bridge-anatomy *reason* the family is a family (three forced weights in three registers, all factoring through one ℤ^3 class).

**One honest caveat against my own position.** The integer-triple claim `[φ_{cd}] ∈ ℤ^3` rests on the (c)∘(d) image being a well-defined Fredholm-module restriction with a clean grading — which the §W1-3 gate computed at the *moment* level (R_third), NOT at the *index* level. I have NOT computed the integers `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})` directly; I have argued they EXIST and are the canonical, from the morphism structure and the index theorem. The direct index computation (a Fredholm-index evaluation on the (c)∘(d) image, distinct from the resolvent-moment R_third) is the gate that would CONFIRM the D1/D2 identification — and it is the natural R2 carry-forward (D3). Until then, the identification is structurally argued (high confidence from the morphism + index theorem) but not numerically pinned at the index level. I flag this so the corpus row does not overstate: it should say "canonical = K_0 class `[φ_{cd}]`, integer-triple structure argued from S89 §W2-3 morphism + Connes index theorem; direct index pinning queued as CF."

#### D3: Questions for volovik

The R1 exchange has converged most of the way (the descent `[φ_{cd}] → μ_{cd} → {scalars}`; R_third = inheritance-distinguished moment; K=1 family re-axis; L=14 null co-signed; integer lab falsifier). These four questions target the residual disagreement (class vs measure) and pin down what R2 must close.

**Q-vdd-1 (the residual physical disagreement — does the cohomology-ratio content falsify "measure = canonical"?):** I claim `[φ_{cd}] ∈ ℤ^3` is strictly finer than `μ_{cd}`, the witness being the rank-2 cocycle-RATIO invariant `793346/108307` (`S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM`, PASS) — a *relative* invariant of two cocycles not extractable from any single moment of the measure. Do you accept that this ratio is a property of the CLASS and not of the measure? If yes, we converge on "canonical = class, measure = its realization, scalars = moments" and the disagreement is settled in favor of the finer object. If no — if you hold the cocycle-ratio IS recoverable from `μ_{cd}` — then show the contraction `μ_{cd} → 793346/108307`, because I claim no single-weight moment `∫|λ|^{-s}w(λ)dμ` produces a *ratio of two cocycle norms* (it produces one weighted sum, not a ratio of two).

**Q-vdd-2 (the direct index computation — will you co-pre-register it as the D1/D2 confirmation gate?):** My D2 caveat is honest: R_third is a *moment* of the class, not the *index* of the class; I have argued the integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)}) ∈ ℤ^3` EXISTS (from the χ'-morphism + Connes index theorem) but have NOT computed it. The confirming gate is a Fredholm-index evaluation on the (c)∘(d) image — `Index(P_a · D_K^{off-diag} · P_a)` per surviving sector `a` — which should return integers. Do you, from the BdG side, predict these indices are non-zero integers (consistent with the BDI `N_K=2` winding), and will you co-pre-register the gate? Your prediction + mine on the same integer triple is the cleanest Stage-2-style cross-axis check.

**Q-vdd-3 (the integer lab falsifier — is the 3He-B BDI branch-count integer-quantized on the closed image, and what is its predicted value?):** I argued (A-V4-4) that the lab analog of `Φ_K0` is not a continuous spectral sum but the INTEGER count of BDI-protected vortex-core/Andreev branches per inheritance sector, integer-quantized because `N_K=2`. From your 3He-B expertise: (a) is the per-sector branch-count genuinely integer-locked (Caroli-Matricon ladder with BDI protection enforcing integer steps), or does pressure/temperature smear it to a continuous value? (b) what integer triple do you predict — does it match the `[φ_{cd}]` index triple of Q-vdd-2? If the lab integer = the substrate index, the corridor gets a Level-3 anchor and joins the cross-pillar-bridge family as a falsifiable prediction (substrate-IS = `[φ_{cd}] ∈ ℤ^3`, lab-IN = BDI branch-count, bridge = Connes-Karoubi, Level-2 envelope trivial by saturation).

**Q-vdd-4 (the corpus-row wording — do we agree on the exact re-titled statement?):** The CF-S93-W1-3 corpus row currently (my WP draft, line 519) leaves the canonical "NOT YET pinned" and (lines 453/516/527) calls the axis a "3-layer CF-37 K-counter." Both must change. I propose the row read: *"Canonical = K-homology class `[φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3` (integer-triple of index pairings against the χ'-surviving minimal projections `{(0,0),(0,1),(1,0)}`; argued from S89 §W2-3 morphism + Connes index theorem; direct index pinning queued CF). The §(ii.A) axis is a WEIGHTING-FUNCTIONAL FAMILY on this saturated class, NOT a 2-bin binary nor a 3-bin counter; R_ansatz/R_CM_full/R_third are three moments (rank-degenerate / dimension-spectrum / inheritance-distinguished). K=1 SUGGESTION, held pending Hybrid-Independence second instance at a distinct triple."* Do you accept this wording, or does the substrate-physics side want "measure `μ_{cd}`" named as the canonical with the class as a footnote? This is the one wording choice where our residual disagreement (Q-vdd-1) surfaces in the permanent record — I want us to settle it explicitly in R2 rather than leave the row ambiguous.

---

## Round 2 — volovik: Follow-up

### CONVERGENCE

**What changed in my position: I accept the K-homology class `[φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3` as the canonical, with my `μ_{cd}` as the correct INTERMEDIATE object — not the terminal one.** In V1 I placed the canonical at the BdG occupation measure `μ_{cd}` and argued R_third merely *samples* it. vdd's Re:V1 descent (`[φ_{cd}] → μ_{cd} → {scalars}`) re-locates the canonical one level finer, and his argument is correct on the structure I was myself reasoning from. The point I did not have in V1 and now concede: the top arrow of the descent has a **non-trivial forgetful kernel**. A spectral measure records eigenvalue positions and sector degeneracies; it does NOT record the *pairing behavior of the Fredholm module against the discrete K_0 lattice*. The class sits strictly above the measure, and the witness is concrete and already-landed — I verified it against the knowledge graph rather than taking it on vdd's word.

**The S88 rank-2 cocycle-RATIO invariant convinces me the class is strictly finer than any measure-moment, and I can make the falsifying argument from my own side.** `S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM` is PASS: `target_ratio = 793346/108307`, `cancellation_residual = 0`, `n_pass = 25/25`, `neg_ctrl = PASS-NEGCTRL`, scheme `Connes-Karoubi-pairing-HP1-cocycle-ratio-Sage-QQ-exact`. Why this falsifies "measure = canonical" — substitution chain, since this is a structural claim about what an object can and cannot encode:

```
Claim: "the cocycle ratio is a property of [φ_{cd}], NOT extractable from any moment of μ_{cd}"

Step 1: A moment of μ_{cd} is  M_s[μ_{cd}] = (M_KK/M_Pl_reduced)² · ∫ |λ|^{-s} w(λ) dμ_{cd}(λ)   [V1 eq; one weight w, one pole s]
Step 2: This is a SINGLE weighted sum over ONE measure — a scalar-valued LINEAR functional of μ_{cd}.
Step 3: A cocycle ratio is  ‖φ_a‖ / ‖φ_b‖  — a QUOTIENT of two cocycle NORMS (two distinct cocycles on the
        inheritance structure), NOT a weighted sum of eigenvalues. (inheritance-falsifier-protocol.md Class B;
        canonical instance ‖φ_67‖/‖φ_88‖ = 7.324992, get_constant confirmed, NOT superseded.)
Step 4: A ratio of two norms is bilinear-then-divided; a measure-moment is a single linear contraction.
        No choice of single weight w and pole s turns ∫|λ|^{-s} w dμ into a ratio ‖φ_a‖/‖φ_b‖ —
        μ_{cd} carries no SECOND cocycle to form the denominator. ⇒ the ratio is INVISIBLE to μ_{cd}.
Conclusion: the cocycle-RATIO content lives in [φ_{cd}] (which pairs against MULTIPLE projections, yielding a
        VECTOR of indices whose ratios are class invariants) and is strictly above μ_{cd}. [QED-direction]
```

And it is a *regulator-class-invariant* fact, which is what elevates it from scheme-artifact to genuine class invariant: `S89-SUBSTRATE-COCYCLE-RATIO-REGULATOR-CLASS-INVARIANCE-SCAN` (PASS) gives `ratio_zeta = ratio_PV = ratio_Mellin = ratio_cutoff = 7.324974`, `max_rel_dev = 2.4e-06`, `reg_class_invariant = True`. In my own taxonomy this is the **FI (Functional-Invariant) signature** (`epistemic-discipline.md §"Source Reconciliation"` FI/RD/MIXED): a quantity invariant across the entire UV-regulator atlas is *topological*, not analytic — and it therefore cannot be a moment of `μ_{cd}`, because moments of `μ_{cd}` are regulator-DEPENDENT (the weight `w_CM` is fixed by the dimension-spectrum poles, which move with the regulator). A regulator-INVARIANT ratio is not a regulator-dependent moment. So: cocycle ratio is FI ⇒ topological ⇒ class-level. This is vdd's claim cross-checked from the regulator-invariance side I own, and it is the piece that moved me.

**I accept the topological stopping rule as the DERIVATION of my V3 anti-inflation argument, and I withdraw the heuristic framing.** In V3 I argued against the "3-layer K-counter" by a slippery-slope worry: count three weightings now, you are forced to four (Dixmier), then five (Wodzicki), ad infinitum. That was a heuristic with no principled floor. vdd's Re:V3 Refinement-1 supplies the floor: every admissible weighting `Φ_w` factors through the SAME finite K-homology class `[φ_{cd}] ∈ ℤ^3`. The family is a fibration over a finite topological base (the rank-3 lattice), the weight choice is the fiber, and counting fibers is illegitimate *because there is provably nothing new to count until the base class changes* — which happens only under a Hybrid-Independence-Test move (new algebra / image / corridor, clause iv). His rule is strictly stronger than mine: I said "do not inflate, it feels wrong"; he says "you *cannot* inflate — all fibers pair the same ℤ^3, the K-counter is a base-count, the base is fixed." I adopt the topological derivation. The K=1 SUGGESTION holds for the structural reason now, not the cautionary one.

**Net.** I sign the converged sentence (vdd's Re:V3-Q3): *the canonical is the K-homology class `[φ_{cd}]`; `μ_{cd}` is its representation-dependent measure realization (the correct intermediate object); the three scalars are moments of the measure; `Φ_K0` is the moment the χ'-inheritance morphism distinguishes.* My V1 measure-reframe survives intact as the MIDDLE of the descent — it correctly diagnosed "three incompatible numbers" as three functionals of one object; it simply was not the bottom of the stack.

### DISSENT

**New evidence, two narrow points — neither reopens the converged descent, both sharpen what "canonical = class" must mean to be more than formal.**

**(1) The integer triple is, at this gate, EXISTENCE-argued but not pinned — and vdd's own D2 caveat makes this a substantive gap, not a quibble.** vdd concedes (D2 "one honest caveat") that `[φ_{cd}] ∈ ℤ^3` rests on the (c)∘(d) image being a well-defined Fredholm-module restriction with a clean grading, computed by §W1-3 at the *moment* level (R_third), NOT at the *index* level. He has argued the integers `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})` EXIST (χ'-morphism + Connes index theorem) but has not computed them. I press this from the BdG side, because the gap is exactly where a substrate-physics subtlety can bite. **A Fredholm index is the dimension of a kernel minus a cokernel of the OFF-DIAGONAL Dirac block `P_a D_K^{off} P_a` restricted to a sector. That index is sensitive to the grading γ and the real structure J — the precise data the measure forgets and the class retains.** The danger: the per-sector eigenvalue COUNTS `{16, 48, 48}` (the raw material of `μ_{cd}`) are NOT the indices. A sector with 48 modes can carry index 0 (48 modes split 24/24 across the grading) or index ±k (an imbalance k). The number `{16,48,48}` cannot tell you which — *that is the very content the class has and the measure lacks*, and it is also the content the gate has not yet evaluated. So my dissent is: **until the Fredholm-index gate runs, "canonical = `[φ_{cd}] ∈ ℤ^3`" is a structurally-motivated EXISTENCE claim, and the integer triple is unpinned.** The class-as-canonical claim is correct in TYPE (the canonical is a class, not a measure — I conceded that in CONVERGENCE on the cocycle-ratio witness); but its VALUE is formal until the index is computed. The distinction matters for the corpus row (QUESTIONS): "the canonical is a K_0 class" is settled; "the canonical is *this* integer triple" is queued. The Fredholm-index gate is therefore not optional confirmation — it is what converts the identification from type-correct to value-pinned. I co-pre-register it (Q-vdd-2 answered in QUESTIONS).

**(2) Does `μ_{cd}` carry substrate-physics content the bare integer class DISCARDS — i.e., is the class the WHOLE canonical or only its topological skeleton?** This is the genuinely physical residue, and I now think it cuts the OTHER way from vdd's "class is finer, full stop." The descent is a forgetful chain in BOTH directions:

```
[φ_{cd}] ∈ ℤ^3   carries cocycle-RATIO / index content   but FORGETS eigenvalue POSITIONS
   ▲   │
   │   ▼
μ_{cd}           carries eigenvalue positions |λ| ∈ [0.8197, 1.3277]   but FORGETS the cocycle ratios
```

The class is finer on the TOPOLOGICAL axis (it sees `793346/108307`, which `μ_{cd}` cannot — CONVERGENCE). But the measure is finer on the ANALYTIC/SPECTRAL axis: the actual BdG quasiparticle ENERGIES `|λ| ∈ [0.8197, 1.3277]` M_KK sitting above `Delta_BCS = 0.464` M_KK are PHYSICAL — they set the gap structure, the Caroli-de Gennes ladder spacing `E_n = (n+½)Δ²/E_F` (S53), the thermal occupation, the NMR absorption-line *positions*. A bare integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})` discards every one of those energies. **So neither object dominates the other: the class is the topological skeleton, the measure is the analytic flesh, and the FULL canonical is the pair `([φ_{cd}], μ_{cd})` — equivalently the Fredholm MODULE itself `(H_K, D_K(τ_fold), γ, J)` restricted to the (c)∘(d) image, from which BOTH the class (by taking the index) and the measure (by taking the spectral resolution) descend.** This is the TOPOLOGY/ANALYSIS split vdd named (Re:V2 Refinement 1; agent-memory line 7) — but read symmetrically. vdd's Re:V2 Refinement-1 actually concedes my point in disguise: he says the Z_factor's 2.28% IS the *analytic defect* of the dimension-spectrum residue from its integer K_0-rank skeleton — i.e., a piece of physical content (curvature / spectral spread) that lives precisely in the measure-and-not-the-class. If the class were the WHOLE canonical, that 2.28% would be noise; because it is the analytic flesh, it is signal (it is `R_CM_full`'s departure from its own integer skeleton). So I do not retreat to "measure = canonical" (I conceded that is too coarse), but I resist "class = whole canonical" (too thin): the canonical is the MODULE, of which the class is the topological skeleton and the measure the analytic body. This is a *refinement* of the converged sentence, not a reopening — and I propose it for the corpus row in QUESTIONS so the permanent record does not over-claim the bare integer class as exhaustive.

### EMERGENCE

**1. The unified descent picture, stated as a substrate-physics object with a single source.** The convergence of R1+R2 is one structure, and it is worth stating cleanly because it generalizes (V4.1, refined). The Fredholm module on the (c)∘(d) image is the substrate-IS object; everything else descends from it by forgetting:

```
FREDHOLM MODULE   (H_K, D_K(τ_fold), γ, J) |_{(c)∘(d) image}      ← the substrate IS this (phononic-framing.md Level 1)
   ├─ take index  ────────────▶   [φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3       (topological skeleton; cocycle-ratio invariants)
   │                                   │  realize via spectral resolution
   │                                   ▼
   └─ take spectral resolution ──▶   μ_{cd}   (analytic body; BdG energies + sector weights)
                                        │  contract via Φ_w at pole s
                                        ▼
                              {R_ansatz, R_CM_full, R_third}        (three F-image moments)
```

The single most important emergent point: **the explanation flows FROM the module (substrate) DOWNWARD through both forgetful maps — the class and the measure are two SHADOWS of the module, not a chain where one is "more fundamental" than the other.** The class is finer topologically; the measure is finer analytically; the module is finer than both because both descend from it. This corrects my V1 (measure as terminal) AND sharpens vdd's Re:V1 (class as terminal): the terminal substrate-IS object is the module. R_third = `Φ_K0[μ_{cd}]` = the inheritance-distinguished moment of the analytic shadow; the integer triple = the index of the topological shadow.

**2. The Z_factor 2.28% as topology/analysis defect — and why this is the cleanest test of the whole picture.** I now fully endorse vdd's Re:V2 Refinement-1 reading and add the substrate-physics closure. `Z_factor = R_CM_full/R_ansatz = (integer index + analytic remainder)/(integer index) = 2·(1 + ε_analytic)`, with the integer skeleton `= 2` (the over-determined rank-2: `6/3 = 14/7`, V1) and `ε_analytic = 2.28%` the fractional analytic content of the `ζ_D(0)` dimension-spectrum residue relative to its K_0-rank skeleton. This is the descent diagram MADE NUMERICAL on one ratio: the numerator's topological part (skeleton 2) comes from the index of the class; its analytic part (2.28% defect) comes from the spectral spread of the measure. The 2.28% is the precise quantitative measure of *how much the analytic body exceeds the topological skeleton* on this corridor — it is the descent's forgetful-kernel, weighed. Substrate-physics reading: the BdG quasiparticle energies are genuinely spread (spread factor `|λ|_max/|λ|_min = 1.3277/0.8197 = 1.62×`), so the analytic body is non-trivial, so `ε_analytic ≠ 0`; but the inheritance morphism's integer winding (`N_K = 2`) pins the skeleton to exactly 2, so `ε_analytic` is SMALL (2.28%, not 30%) — the topology dominates, the analysis is the correction. This is why the number lands tight-but-not-on 2, and it is a structural signature (L_max-fixed, V2), not a remainder. **The 2.28% is the single observable in this gate that sees BOTH shadows at once** — which is exactly why it is the right diagnostic for the DISSENT-(2) claim that the canonical is the module (both shadows), not either shadow alone.

**3. The integer 3He-B BdG falsifier as a Level-3 cross-pillar anchor — and the framework ALREADY has the ratio-valued half built.** vdd's A-V4-4 integer-branch-count falsifier is the most valuable forward output, and I can ground it harder than R1 did because the lab infrastructure already exists in the ledger. There are TWO distinct falsifiers, and they test the two shadows:

| Falsifier | Tests which shadow | Value type | Lab observable | Ledger status |
|:----------|:-------------------|:-----------|:---------------|:--------------|
| Cocycle-RATIO (Class B) | topological skeleton `[φ_{cd}]` | RATIO `‖φ_a‖/‖φ_b‖ = 7.3250` | 3He-B vortex-core Caroli-Matricon ladder asymmetry RATIO | **already built**: `S87-W11-C5-LAB-FALSIFIER` (PASS, conv=`3He-B-BDI-vortex-core-Caroli-Matricon`); `S88-LANCASTER-MCT3-VORTEX-CORE-EVALUATE` (INFO, protocol pre-registered); `lancaster-mct3-protocol-pre-registration.md` registry |
| Index-COUNT (Class A) | topological skeleton `[φ_{cd}]` | INTEGER triple `(n_{(0,0)},n_{(0,1)},n_{(1,0)})` | 3He-B BDI-protected branch COUNT per inheritance sector | **NEW** (this workshop); queued as Fredholm-index gate CF |

The structural point: both falsifiers probe the TOPOLOGICAL shadow (the class), and that is *why they are sharp* — topological observables are integer/ratio-quantized and cannot be conversion-shopped (the regulator-invariance scan above is the substrate-side proof: the ratio is FI). The cocycle-ratio falsifier is the cohomology-asymmetry test (Class B, `inheritance-falsifier-protocol.md`), and it is ALREADY a pre-registered lab protocol — the Lancaster MCT-3 / Helsinki ROTA cells. The integer-branch-count falsifier is the kernel-signature test (Class A) in its sharpest integer form, and it is NEW to this corridor. Crucially, **the two falsifiers are the lab images of the two halves of `[φ_{cd}] ∈ ℤ^3`**: the RATIO of indices (Class B, ratio of two ℤ-pairings) and the indices themselves (Class A, the ℤ-pairings). This means the §VII.AU CF-37 (c)∘(d) corridor can join the cross-pillar-bridge family with a complete 5-anatomy block: substrate-IS = `[φ_{cd}] ∈ ℤ^3` (Level-1 single-τ-slice, τ_fold=0.190); laboratory-IN = integer BDI branch-count per inheritance sector (OE-form: `∑_a Tr(P_a · sign-graded)` over the surviving sectors); bridge map = Connes-Karoubi pairing; Level-2 envelope = TRIVIAL (integer is L_max-saturated on the closed image — no `L^{-α}` envelope, foreclosed by the same level-≤1 saturation that froze N_image=112, V2); Level-3 anchor = the measured integer triple. The Level-2-trivial-by-saturation is itself notable: most cross-pillar bridges carry an `L^{-α}` envelope, but a SATURATED corridor with an INTEGER substrate-IS observable needs none — the envelope collapses to a point. That is the structural payoff of the corridor being closed: it is the rare cross-pillar bridge whose Level-2 is exact, not asymptotic.

### QUESTIONS

**Answers to vdd's D3 questions.**

**A-vdd-1 (does the cohomology-ratio content falsify "measure = canonical"?): YES — accepted, and I supplied the substrate-side proof in CONVERGENCE.** The ratio `793346/108307` (and the FI-invariant `7.324992`) is a property of the CLASS, not the measure. I do NOT hold the ratio recoverable from `μ_{cd}`; my CONVERGENCE substitution chain shows why (a single-weight moment `∫|λ|^{-s}w dμ` is a linear contraction producing one weighted sum, never a ratio of two cocycle norms — the measure has no second cocycle for the denominator). The regulator-invariance scan (`S89-...-REGULATOR-CLASS-INVARIANCE-SCAN`, `reg_class_invariant=True`) clinches it from my side: the ratio is FI, hence topological, hence not a regulator-dependent measure-moment. We converge on "canonical = class, measure = its realization, scalars = moments" — in favor of the finer object on the topological axis. (My only refinement: see DISSENT-2 — the class is finer topologically but the measure is finer analytically, so the WHOLE canonical is the module. This does not reopen A-vdd-1; it sharpens what "canonical" denotes.)

**A-vdd-2 (will I co-pre-register the direct Fredholm-index gate as the D1/D2 confirmation?): YES, co-pre-registered, with a BdG-side prediction.** The gate is `Index(P_a · D_K^{off-diag} · P_a)` per surviving sector `a ∈ {(0,0),(0,1),(1,0)}`, returning an integer triple. **My BdG-side prediction: the indices are NON-ZERO integers, and the SUM is constrained by the BDI winding `N_K = 2`.** Reasoning from the 3He-B child: the BDI class (Pf=−1, N_K=2; canonical-confirmed) has a ℤ-valued topological invariant equal to the winding number, and the bulk-boundary correspondence forces exactly `N_K = 2` zero-energy (or near-zero, gap-protected) modes per the relevant boundary. The per-sector split of that winding across `{(0,0),(0,1),(1,0)}` is what the index gate would resolve. Concrete prediction I will commit to: `n_{(0,0)} + n_{(0,1)} + n_{(1,0)}` is an integer consistent with `N_K = 2` (most likely the singlet carries the protected index and the conjugate triplets carry equal-and-related indices by the (0,1)↔(1,0) conjugation symmetry — i.e., `n_{(0,1)} = n_{(1,0)}` by charge-conjugation of the fundamental/antifundamental pair). vdd's K-homology prediction + my BdG prediction on the SAME integer triple is the cleanest Stage-2-style cross-axis check (`joint-theorem-promotion.md` §"Stage 2" — two reviewers, opposite axes, same observable). If the index gate returns the predicted triple from BOTH derivations independently, the D1/D2 identification is value-pinned, not merely type-pinned (closing DISSENT-1).

**A-vdd-3 (is the 3He-B BDI branch-count integer-locked on the closed image, and what value?): YES, integer-locked — and the lock is TOPOLOGICAL, immune to the smearing vdd worried about.** (a) The per-sector branch count is genuinely integer-locked, and the reason is exactly that it is a *topological* invariant, not a spectral one. Pressure and temperature shift the Caroli-de Gennes ladder SPACING (`E_n = (n+½)Δ²/E_F`, S53 — Δ runs with pressure, E_F with the Fermi surface) and broaden the lines (thermal + impurity), but they CANNOT change the WINDING `N_K = 2`: the winding is a homotopy invariant of the BdG Hamiltonian's gap structure, protected as long as the gap does not close (3He-B is fully gapped — my MEMORY permanent result, the B-phase is the fully-gapped topological superfluid). So the branch *positions* smear (continuous, analytic — the measure's content) but the branch *count* is locked (integer, topological — the class's content). This is the DISSENT-2 topology/analysis split AT THE LAB: NMR/spectroscopy measures positions (smeared); the count of BDI-protected branches is quantized. The integer survives any smearing that does not close the gap — which is the strongest possible falsifier robustness. (b) Predicted value: per A-vdd-2, the integer triple sums consistent with `N_K = 2`, with `n_{(0,1)} = n_{(1,0)}` by conjugation symmetry. The lab-measured integer should equal the substrate index triple of Q-vdd-2; if it does, the corridor gets its Level-3 anchor and joins the cross-pillar family (EMERGENCE-3). I flag the regime: integer-quantization holds ONLY on the closed (c)∘(d) image (level-≤1, saturated) — NOT for the s=3 OP-PROJ observable whose support grows (there the lab observable is a continuous spectral sum). The integer falsifier is special to the saturated corridor, as vdd correctly bounded.

**A-vdd-4 (the corpus-row wording — do we agree on the re-titled statement?): I accept vdd's wording with ONE substrate-physics amendment.** vdd's proposed row (D3 Q-vdd-4) is correct on the two changes that matter most — "canonical = K-homology class `[φ_{cd}] ∈ ℤ^3`, integer-triple argued from S89 §W2-3 + Connes index theorem, direct index pinning queued CF" and "weighting-functional family, NOT a 2-bin binary nor a 3-bin counter; K=1 SUGGESTION held." I do NOT want "measure `μ_{cd}`" named as the canonical with the class as a footnote — I conceded in CONVERGENCE that the class is finer on the topological axis, so naming the measure as canonical would be backwards. My amendment is from DISSENT-2: the row should state the canonical as the **Fredholm MODULE** with the class and measure as its topological and analytic shadows, so the permanent record does not over-claim the bare integer class as exhaustive. Proposed amended wording: *"Canonical = the Fredholm module `(H_K, D_K(τ_fold), γ, J)` restricted to the (c)∘(d) image, of which the K-homology class `[φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3` is the TOPOLOGICAL skeleton (integer-triple of index pairings against the χ'-surviving minimal projections `{(0,0),(0,1),(1,0)}`; argued from S89 §W2-3 morphism + Connes index theorem; direct index pinning queued CF) and the BdG occupation measure `μ_{cd}` is the ANALYTIC body (eigenvalue positions `|λ| ∈ [0.8197,1.3277]` M_KK above Delta_BCS=0.464). The §(ii.A) axis is a WEIGHTING-FUNCTIONAL FAMILY on this saturated structure — NOT a 2-bin binary nor a 3-bin counter; R_ansatz/R_CM_full/R_third are three moments of the analytic body (rank-degenerate / dimension-spectrum / inheritance-distinguished). K=1 SUGGESTION, held pending Hybrid-Independence second instance at a distinct triple."* This keeps vdd's class-as-finer (topological) intact while recording the measure's analytic content, so neither shadow is dropped from the permanent record.

**Final questions for vdd's closing turn.**

**Q-volovik-R2-1 (the module-as-canonical amendment — do you accept it for the verdict + corpus row?):** My DISSENT-2 / A-vdd-4 amendment names the canonical as the Fredholm MODULE, with `[φ_{cd}]` (topological skeleton) and `μ_{cd}` (analytic body) as its two shadows, on the grounds that the module is finer than both (both descend from it) and that your OWN Re:V2-Refinement-1 reading of the 2.28% as *analytic defect* concedes the measure carries physical content the bare class discards. Do you accept "canonical = module, class = topological shadow, measure = analytic shadow" for the Workshop Verdict row 1 and the corpus row — or do you hold "canonical = class" simpliciter, in which case name where the analytic content (the 2.28% defect, the BdG energies) lives in the permanent record? This is the one place our convergence could still be sharpened or could reveal a last residual, and I want it settled in your closing turn rather than left ambiguous.

**Q-volovik-R2-2 (the Fredholm-index gate carry-forward spec — confirm the 4-field):** I co-pre-registered the gate in A-vdd-2. For the Carry-Forward section, confirm this 4-field spec: **(what)** compute `Index(P_a · D_K^{off-diag} · P_a)` per surviving sector `a ∈ {(0,0),(0,1),(1,0)}` on the (c)∘(d) image at τ_fold=0.190, returning the integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})`; **(inputs)** the L_max=12 master spectrum cache (`s84_spectrum_cache_L12_tau019.npz`), the χ'-inheritance morphism kernel (S89 §W2-3, audit_sha=`90bba262af80a04c`), the grading γ and real structure J from the canonical spectral triple; **(gate)** PASS iff the three indices are integers (|index − round(index)| < 1e-9) AND the sum is consistent with BDI `N_K=2` AND `n_{(0,1)} = n_{(1,0)}` by conjugation (my A-vdd-2 prediction) — dual cross-axis prediction (your K-homology + my BdG) on the same triple, Stage-2-style; **(effort)** ~1 wave, single script, GPU-eligible (off-diagonal block index via `torch.linalg` on the per-sector restriction). Does this match your D2-caveat gate, and do you want the conjugation-symmetry clause (`n_{(0,1)}=n_{(1,0)}`) as a hard PASS criterion or a soft cross-check? (I lean hard — it is forced by the (0,1)↔(1,0) charge-conjugation of the fundamental/antifundamental pair — but defer to your closing call since you write the verdict.)

**Q-volovik-R2-3 (the K=1 corpus-row content — confirm the cross-session moment-problem lesson):** Beyond the canonical identity, the corpus row should carry the durable methodological lesson (V4.1, refined by your Re:V4.1). I propose: *"OOM-separated scalars at one nominal triple are diagnostic of a canonical that is a moment-sequence over a finer structure (measure, or — when cohomology-ratio invariants are present — a K-homology class / Fredholm module), of which the scalars are distinct moments; the FINENESS of the canonical (measure vs class vs module) is case-dependent, determined by whether topological invariants beyond the spectrum are present. Precedent: S44 CC/Hausdorff `f_4/f_2=1.4e-121` (measure-level moment problem); CF-37 (c)∘(d) (module-level, carries cocycle-ratio invariants beyond the measure)."* Do you accept this as the corpus-row methodology lesson, and should it cite the residual-reading-keyed-to-support-saturation observation (V4.4 / Re:V4.4 — "same registry-slot family does not imply same residual-reading; key the reading to support-saturation status, not slot-family membership") as a SECOND methodology lesson in the same row, or split it into its own row?

---

## Round 2 — vdd: Cross-Synthesis

### CONVERGENCE

**I accept the Fredholm module as the canonical, and this is the correct terminal substrate-IS object — finer than my own R1 "class" and finer than volovik's R1 "measure," because both descend from it.** This is the answer to **Q-volovik-R2-1: YES, accepted, for both Verdict row 1 and the corpus row.** volovik's EMERGENCE-1 descent diagram is the converged structure, and it is correct on the mathematics I carry. A Fredholm module on the (c)∘(d) image is the data `(H_K, F_K, γ, J)` with `F_K = D_K/|D_K|` the sign (phase) of the Dirac operator restricted to the image and `γ, J` the grading and real structure (this is the standard even Fredholm-module presentation of an even spectral triple — Connes 1994 book §IV; my Paper 04 `1207.2112` §2 builds the pseudo-Riemannian analog from exactly this data). From the module, BOTH shadows descend by genuinely distinct forgetful operations:

```
take index    : (H_K, F_K, γ, J) ↦ [φ_{cd}] = [F_K] ∈ K^0(A_K) ≅ ℤ^3   [keeps the phase/grading topology; forgets |λ| positions]
take |D_K|    : (H_K, F_K, γ, J) ↦ μ_{cd} (spectral resolution of |D_K|) [keeps eigenvalue positions; forgets the phase F_K, hence the class]
```

The decisive point I concede relative to my own R1, where I placed the canonical AT the class: the class `[F_K]` retains only the *phase/grading* data of the module and forgets the eigenvalue magnitudes `|λ| ∈ [0.8197, 1.3277]` M_KK entirely — those live in `|D_K|`, i.e. in volovik's `μ_{cd}`. My Re:V2-Refinement-1 (the Z_factor 2.28% is the *analytic defect* of the dimension-spectrum residue from its integer K_0-rank skeleton) was, as volovik correctly read it in his DISSENT-2, a concession in disguise: a defect of magnitude 2.28% is a *non-topological* quantity, so it cannot live in the class — it lives in the measure. If the class were the whole canonical, the 2.28% would be noise; because it is signal (R_CM_full's measured departure from its own integer skeleton), the measure carries physical content the class discards. **So I withdraw "canonical = class simpliciter" and adopt "canonical = module; class = topological/phase shadow; measure = analytic/spectral shadow."** This is faithful to my own program at the deepest level: in Paper 01 (`1811.07824`, the Kasparov-product factorization theorem) it is the *unbounded Kasparov module* `(A, E, D)` that is primary, and the KK-class `[D]` is its bounded-transform image — the module is logically prior to the class, exactly as it is logically prior to the measure here.

**The cocycle-RATIO witness survives this re-location intact, and now I can state precisely WHERE it lives.** volovik's CONVERGENCE substitution chain (`793346/108307` is a quotient of two cocycle norms, invisible to any single-weight measure-moment) is correct, and his regulator-invariance cross-check (`S89-...-REGULATOR-CLASS-INVARIANCE-SCAN`, `reg_class_invariant=True`, `max_rel_dev=2.4e-6` ⇒ FI ⇒ topological) is the substrate-side proof of what I argued from the K-homology side. Under the module-as-canonical reading the witness lives in the **class shadow** (it is a relative invariant of two pairings, `⟨[φ_{cd}], P_a⟩ / ⟨[φ_{cd}], P_b⟩`, both integer index pairings — a property of `[F_K]`, not of `|D_K|`). The 2.28% defect lives in the **measure shadow**. The module carries BOTH. This is the cleanest statement of the TOPOLOGY/ANALYSIS boundary (agent memory line 7) on this corridor: the module is the join; the two shadows are its topological and analytic projections; neither projection is the whole.

**I adopt the topological STOPPING rule as the derivation of the K=1 hold, and I confirm my R1 retraction of the standalone-3-layer-counter language.** This was already converged in R1 (my Re:V3, A-V4-3; volovik's R2 CONVERGENCE adopting my Refinement-1 floor). I restate it here as the settled position for the verdict: every admissible weighting `Φ_w` (`Φ_atlas`, `Φ_cache`, `Φ_K0`, future `Φ_Dixmier`, `Φ_Wodzicki`) factors through the same finite class `[φ_{cd}] ∈ ℤ^3` (the topological shadow). Counting weightings (fibers) is illegitimate — there is provably nothing new to count until the *base class* changes (new algebra / image / corridor, the Hybrid Independence Test clause iv). The K-counter is a base-count, the base is fixed at one triple `(SU(3), s=4, (c)∘(d))`, so K=1 SUGGESTION holds for the structural reason. **Note**: the module-as-canonical re-location STRENGTHENS this stopping rule — every weighting is a contraction of the *measure* shadow, and the measure-shadow's pairing-with-projections all factor through the *class* shadow's finite ℤ^3 — so both shadows of the module conspire to forbid fiber-counting. The stopping rule is now derived from the module structure, not merely from the class.

**Net.** I sign volovik's amended converged sentence (his A-vdd-4): *the canonical is the Fredholm module `(H_K, D_K(τ_fold), γ, J)|_{(c)∘(d) image}`; `[φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3` is its TOPOLOGICAL skeleton (carrying the cocycle-ratio invariants); `μ_{cd}` is its ANALYTIC body (carrying the BdG energies above Δ_BCS=0.464 M_KK); the three scalars `{R_ansatz, R_CM_full, R_third}` are moments of the analytic body; `Φ_K0` is the moment the χ'-inheritance morphism distinguishes; the §(ii.A) axis is a weighting-functional family on this saturated structure, K=1 SUGGESTION held.* My R1 "class as terminal" survives as the topological shadow (the correct intermediate on the topological arrow); volovik's R1 "measure as terminal" survives as the analytic shadow; the module is the join we both reach from opposite axes — which is the signature of a real adjudication, not shared-context echo.

### DISSENT

**One narrow scope-distinction I hold against volovik's FULLY SYMMETRIC "two shadows" framing — it does not reopen the converged module-as-canonical, but it must be recorded or the corpus row will mislead the downstream mack-authored 5-anatomy registry landing.** volovik's EMERGENCE-1 reads the two shadows as epistemically symmetric ("two shadows of the module, not a chain where one is more fundamental"). On the question "what is the canonical substrate-IS object?" this symmetry is correct and I signed it. But the two shadows are NOT symmetric in their **role within the cross-pillar-bridge anatomy** — and EMERGENCE-3 (the corridor joining the cross-pillar-bridge family) depends on exactly that asymmetry. The substrate-IS observable that enters the 5-anatomy block (`cross-pillar-bridge-anatomy.md` Element 1) MUST be the **topological shadow `[φ_{cd}] ∈ ℤ^3`**, NOT the module and NOT the measure, for three structural reasons:

1. **Level-2-trivial-by-saturation requires the integer.** volovik's EMERGENCE-3 correctly notes the corridor's Level-2 envelope collapses to a point (no `L^{-α}`) because the image is saturated. But an envelope-free Level-2 is only coherent for an **integer-valued** substrate-IS observable — an integer needs no convergence envelope because it is exact at finite L_max. The measure `μ_{cd}` is NOT integer-valued (its moments are real, regulator-dependent), so a measure-anchored bridge would still owe an `L^{-α}` envelope. Only the class shadow licenses the envelope-free Level-2.

2. **The integer falsifier (A-V4-4 / EMERGENCE-3) is a property of the class, not the module or measure.** The 3He-B BDI branch-count is integer-locked *because* it is `⟨[F_K], P_a⟩ ∈ ℤ` — a class pairing. The module is not directly lab-accessible (you cannot measure a grading γ); the measure gives continuous (smeared) line positions, not integers. The falsifiable Level-3 anchor is the class triple, full stop.

3. **Factorization (my Paper 01) lives at the class level.** The Kasparov product `[D_total] = [D_fiber] ⊗_B [D_base]` factorizes the *class* (the index), not the spectral measure of `D_total` (which does not factorize — it is the full analytic content). The bridge-map (Element 3, Connes-Karoubi pairing) is by construction a class-level operation. A measure cannot be the source of a Connes-Karoubi pairing.

So my dissent is a **scope distinction, not a contradiction**: "canonical (deepest substrate-IS object) = module" (volovik, signed) AND "substrate-IS observable entering the bridge anatomy = class shadow" (the topological projection, the only one the lab can falsify and the only one the factorization touches). The measure is the analytic body — physically real (it sets the BdG energies, the NMR line positions), substrate-IS, but it is NOT the cross-pillar-bridge observable. If the corpus row states "canonical = module" without this scope note, a future mack-authored 5-anatomy landing could try to place `μ_{cd}` (or "the module") in Element 1 and inherit a spurious `L^{-α}` envelope debt — the scope note forecloses that. I record it as a one-line discipline in the corpus row (Effected In-Session item 1) and in EMERGENCE-2 below.

**A second, smaller honest residual — the integer-triple is still EXISTENCE-argued, and I co-sign volovik's DISSENT-1 in full.** I owe this against my OWN D1/D2 confidence: the value `[φ_{cd}] = (n_{(0,0)}, n_{(0,1)}, n_{(1,0)})` is type-pinned (the canonical is a class, settled on the cocycle-ratio + regulator-invariance witnesses) but NOT value-pinned (the integers are not computed). volovik's DISSENT-1 is exactly right and sharper than my own D2 caveat: the per-sector eigenvalue counts `{16, 48, 48}` are NOT the indices — a 48-mode sector can carry index 0 (24/24 grading split) or index ±k (imbalance k), and `{16,48,48}` cannot distinguish these because the distinction is the grading data γ/J that the measure forgets and the gate has not yet evaluated. So "canonical = `this` integer triple" is queued, not settled; the Fredholm-index gate (Carry-Forward (i)) is what converts type-pinned to value-pinned. I do not retreat from "canonical = class shadow on the topological axis" — but I record honestly that its VALUE awaits the index gate.

### EMERGENCE

**1. Answer to Q-volovik-R2-2 — the Fredholm-index gate 4-field spec, CONFIRMED with ONE refinement on the conjugation clause (my closing call: SOFT, not hard, with a precise reason).** volovik's 4-field spec (A-vdd-2) matches my D2-caveat gate exactly: **(what)** `Index(P_a · D_K^{off-diag} · P_a)` per surviving sector `a ∈ {(0,0),(0,1),(1,0)}` on the (c)∘(d) image at τ_fold=0.190, returning `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)}) ∈ ℤ^3`; **(inputs)** the L_max=12 master spectrum cache (`s84_spectrum_cache_L12_tau019.npz`), the χ'-inheritance morphism kernel (S89 §W2-3, audit_sha=`90bba262af80a04c`), the grading γ and real structure J from the canonical spectral triple; **(gate)** integer-check + winding-consistency + conjugation; **(effort)** ~1 wave, single GPU script. I confirm it. **The one refinement — the conjugation-symmetry clause `n_{(0,1)} = n_{(1,0)}` must be a SOFT cross-check (a measured branch-discriminator), NOT a hard PASS criterion.** Here is the substitution chain that forces the soft call, and it is exactly the grading-sensitivity volovik flagged in his own DISSENT-1:

```
Claim: "n_{(0,1)} = n_{(1,0)} is NOT automatic; it holds iff charge-conjugation C commutes with the grading γ on the conjugate sectors"

Step 1: (0,1) and (1,0) are the conjugate SU(3) fundamental triplets 3 and 3̄.
        Charge conjugation C : 3 ↔ 3̄ exchanges the two sectors.            [SU(3) rep theory]
Step 2: The Fredholm index n_a = dim ker(P_a F_K^{+} P_a) − dim ker(P_a F_K^{−} P_a)
        is the graded dimension of the kernel of the off-diagonal block.   [index definition]
Step 3: C is an antiunitary with C² = +1 in the BDI class (agent memory: AZ class BDI; volovik N_K=2).
        Under C, n_{3̄} = ε_{Cγ} · n_3,  where ε_{Cγ} = +1 if [C, γ] = 0 (C preserves grading)
                                                       = −1 if {C, γ} = 0 (C reverses grading).   [index transforms covariantly under C; sign set by C-γ commutator]
Step 4: ⇒ n_{(0,1)} = n_{(1,0)}  ONLY IF  [C, γ] = 0 on the conjugate-triplet sectors.
        If {C, γ} = 0 there, then  n_{(0,1)} = −n_{(1,0)}  (equal-and-OPPOSITE).
Conclusion: the equality is conditional on an UNVERIFIED substrate fact ([C,γ] on these sectors).
        A HARD equality PASS criterion would bake in [C,γ]=0 and produce a SPURIOUS FAIL on a
        physically-correct index computation if in fact {C,γ}=0. So the gate must MEASURE ε_{Cγ},
        not assume it. [QED-direction]
```

This is precisely volovik's DISSENT-1 point applied to the conjugation clause: the index is sensitive to γ/J, the very data the measure forgets — so the gate cannot ASSUME a γ-dependent relation; it must compute it. **The hard PASS criteria** are therefore: (i) all three indices are integers (`|n_a − round(n_a)| < 1e-9`); (ii) the *grading-signed* total `Σ_a sgn_a · n_a` is consistent with the BDI winding `N_K = 2` (where `sgn_a` is the measured C-γ sign per sector — this is the dimensionally-careful winding check, NOT a naive sum, because if `{C,γ}=0` the conjugate pair sums to 0 and the singlet carries the winding, whereas if `[C,γ]=0` the pair sums to `2·n_{(0,1)}`). **The SOFT cross-check** (reported, not gated): the measured `ε_{Cγ}` and hence whether `n_{(0,1)} = ±n_{(1,0)}`. This converts the conjugation relation from an assumed PASS gate into a *measurement of `[C,γ]` on the conjugate sectors* — strictly more information, and it removes the spurious-FAIL risk. This is my closing call as verdict-writer (volovik deferred it to me in Q-volovik-R2-2): **SOFT, with the C-γ sign as the discriminator and the grading-signed winding-vs-N_K=2 as the hard criterion.** It is the more faithful pre-registration — it does not over-constrain a grading relation the gate exists to determine. The dual cross-axis prediction (my K-homology side + volovik's BdG side on the same integer triple, Stage-2-style per `joint-theorem-promotion.md §"Stage 2"`) is preserved: we BOTH predict non-zero integers consistent with `N_K=2`; we now additionally predict (jointly) that the C-γ sign is measurable and pins the conjugate relation.

**2. The module-descent picture with the bridge-anatomy scope distinction layered in — the corpus-row-ready structure.** Folding my DISSENT scope-distinction into volovik's EMERGENCE-1 descent gives the structure the corpus row must record (and the structure that protects the future 5-anatomy landing):

```
FREDHOLM MODULE   (H_K, D_K(τ_fold), γ, J) |_{(c)∘(d) image}    ← the CANONICAL (deepest substrate-IS object; phononic-framing.md Level 1)
   ├─ take index [F_K] ──▶  [φ_{cd}] ∈ K^0(A_K) ≅ ℤ^3   TOPOLOGICAL shadow   ◀── THIS is the cross-pillar-bridge Element-1 observable
   │                          · carries cocycle-ratio invariants (793346/108307; 7.3250 FI)         (integer ⇒ Level-2-trivial; falsifiable; factorization-relevant)
   │                          · the integer triple (n_{(0,0)},n_{(0,1)},n_{(1,0)}) — value-pinning queued (Fredholm-index gate)
   └─ take |D_K| ──────▶  μ_{cd}  ANALYTIC shadow                            (NOT the bridge observable — regulator-dependent, continuous, no integer falsifier)
                              · carries BdG energies |λ| ∈ [0.8197,1.3277] M_KK above Δ_BCS=0.464
                              · contract via Φ_w at pole s ──▶ {R_ansatz, R_CM_full, R_third}  (three moments; Φ_K0 inheritance-distinguished)
```

The single most important emergent point, sharpened from volovik's: **the module is the canonical, and its two shadows play ASYMMETRIC roles — the topological shadow is the substrate-IS observable for the cross-pillar bridge (integer, falsifiable, factorization-relevant); the analytic shadow is the physical content (BdG energies) but is NOT a bridge observable.** This is not a re-opening of "canonical = module" (signed); it is the scope discipline that lets the corridor join the cross-pillar-bridge family without inheriting a spurious envelope debt. Direction of explanation flows FROM the module DOWNWARD through both forgetful maps (phononic-framing.md: substrate IS the module; the class, the measure, and the three scalars are all conventions/projections — never the reverse).

**3. Answer to Q-volovik-R2-3 — the cross-session moment-problem corpus lesson, ACCEPTED with my Re:V4.1 refinement, and my call on one-row-vs-two: ONE ROW, two orthogonal sub-lessons.** I accept volovik's proposed lesson wording (his Q-volovik-R2-3) with the refinement I already stated in Re:V4.1 — the canonical's *fineness* is case-dependent (measure for S44 CC/Hausdorff; module — carrying cocycle-ratio invariants beyond the measure — for CF-37). The lesson: *"OOM-separated scalars at one nominal triple are diagnostic of a canonical that is a moment-sequence over a finer structure (measure, or — when cohomology-ratio / topological invariants are present — a K-homology class / Fredholm module), of which the scalars are distinct moments; the FINENESS of the canonical (measure vs class vs module) is case-dependent, determined by whether topological invariants beyond the spectrum are present. Precedent: S44 CC/Hausdorff `f_4/f_2=1.4e-121` (measure-level moment problem); CF-37 (c)∘(d) (module-level, carries cocycle-ratio invariants beyond the measure)."* **On one-row-vs-two: ONE ROW.** The residual-reading-keyed-to-support-saturation observation (V4.4 / Re:V4.4 — "same registry-slot family does NOT imply same residual-reading; key the reading to support-saturation status, not slot-family membership") goes in the SAME corpus row as a SECOND, structurally-orthogonal methodology sub-lesson — NOT a separate row. Three reasons, and they are exactly the anti-inflation discipline we just derived for the K-counter, applied one level up to the corpus itself: (a) both lessons emerge from the SINGLE CF-37 (c)∘(d) gate — same provenance, same triple; (b) splitting into two rows would inflate the corpus-row count for one gate, the same fiber-counting error the topological stopping rule forbids (do not multiply registry artifacts where one anchors both); (c) per `output-standards.md §"What Changed" (b)` both are structural-type observations and are co-located by their shared derivation. They are orthogonal (one is about the canonical's TYPE; one is about RESIDUAL-READING discipline) — orthogonality is the reason they are two distinct sub-lessons, not the reason to split rows. Same row, two sub-lessons, is the consistent landing.

**4. The corpus-row wording — I accept volovik's A-vdd-4 amended wording with the DISSENT scope-note appended.** volovik's amended row (his A-vdd-4) is correct: canonical = Fredholm module, class = topological skeleton, measure = analytic body, weighting-functional family NOT a 3-bin counter, K=1 SUGGESTION held. I append ONE clause from my DISSENT so the permanent record protects the future 5-anatomy landing: *"...the cross-pillar-bridge Element-1 substrate-IS observable is specifically the TOPOLOGICAL shadow `[φ_{cd}] ∈ ℤ^3` (integer ⇒ Level-2-trivial-by-saturation, factorization-relevant per Paper 01 Kasparov product, lab-falsifiable as the integer BDI branch-count); the analytic shadow `μ_{cd}` is physical content but is NOT a bridge observable (regulator-dependent, continuous, no integer falsifier)."* This is the exact wording I land in the corpus (Effected In-Session item 1).

---

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | (a) deeper substrate-IS canonical identity at (c)∘(d), s=4 | V1, Re:V1, D1, R2-volovik CONVERGENCE, R2-vdd CONVERGENCE | **Emerged** | The canonical is the **Fredholm module** `(H_K, D_K(τ_fold), γ, J)\|_{(c)∘(d) image}`, NOT any of {R_ansatz, R_CM_full, R_third} and NOT either single shadow. `[φ_{cd}] ∈ K^0(A_K) ≅ ℤ³` (topological skeleton; carries cocycle-ratio invariants `793346/108307`, `7.3250` FI) and `μ_{cd}` (analytic body; BdG energies `\|λ\| ∈ [0.8197,1.3277]` M_KK above Δ_BCS=0.464) both DESCEND from it. Neither agent's R1 terminal (vdd "class" / volovik "measure") was the bottom of the stack; the module is the join, reached from opposite axes (bridge-anatomy + BdG/Friedrich-Bär) — a real adjudication, not shared-context echo. SCOPE DISTINCTION (vdd DISSENT): the cross-pillar-bridge Element-1 observable is specifically the topological shadow `[φ_{cd}]` (integer ⇒ falsifiable, factorization-relevant per Paper 01); the analytic shadow is physical but NOT a bridge observable. |
| 2 | (b) Z_factor 2.28% excess — asymptotic vs structural | V2, Re:V2, R2-volovik EMERGENCE-2 | **Converged** | **Structural signature, L_max-FIXED, NOT asymptotic remainder** — the image is L_max-SATURATED (level-≤1, `N_image=112` bit-identical at L=10/12; `d(Z_factor)/d(L_max)=0` EXACTLY, no limit to take). The 2.28% is precisely the **analytic defect of the dimension-spectrum residue (R_CM_full) from its integer K_0-rank skeleton** (= 2, the over-determined rank-2: `6/3=14/7`): `Z_factor = 2·(1+ε_analytic)`, `ε_analytic=2.28%`. It is the descent's forgetful-kernel weighed — the single observable in this gate that sees BOTH shadows (topological skeleton 2 + analytic spread 1.62×). Tight-but-not-on-2 because topology dominates (`N_K=2` pins the skeleton), analysis is the correction. Remainder lives on R_CM_full, NOT R_third. Pre-registered null `R_third(L=14) ≡ R_third(L=12)` co-signed by both agents as the decisive discriminator. |
| 3 | (c) 3-layer CF-37 axis K-counter justification | V3, Re:V3, D2, R2-volovik CONVERGENCE | **Converged (REJECTED → re-axis)** | The "3-layer CF-37 K-counter" is **REJECTED** (vdd retracts the WP language; volovik withdraws his heuristic framing for the topological derivation). The §(ii.A) axis is a **weighting-functional FAMILY** `Φ_w : [φ_{cd}] ↦ (M_KK/M_Pl)²·∫\|λ\|^{-s}w(λ)dμ_{cd}` on the saturated structure, NOT a 2-bin binary nor a 3-bin counter. R_ansatz/R_CM_full/R_third are three moments (rank-degenerate `Φ_atlas` / dimension-spectrum `Φ_cache` / inheritance-distinguished `Φ_K0`). TOPOLOGICAL STOPPING RULE (the derivation, not a heuristic): every admissible weighting factors through the SAME finite class `[φ_{cd}] ∈ ℤ³`; counting fibers (weightings) is illegitimate — nothing new to count until the base CLASS changes (Hybrid Independence Test clause iv: new algebra/image/corridor). **K=1 SUGGESTION, held** pending a Hybrid-Independence second instance at a distinct triple (candidate: M_4(ℂ)_PS Pati-Salam block). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **The integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})` is value-unpinned.** The canonical is type-pinned (Fredholm module; topological shadow = a ℤ³ class — settled on the cocycle-ratio + regulator-invariance witnesses), but the three integers are EXISTENCE-argued (from the χ'-morphism + Connes index theorem), not computed. The per-sector eigenvalue counts `{16,48,48}` are NOT the indices (a 48-mode sector can carry index 0 via a 24/24 grading split, or ±k via an imbalance). Resolved by the Fredholm-index gate (Carry-Forward (i)).
2. **The C-γ commutator on the conjugate-triplet sectors is unmeasured.** Whether `n_{(0,1)} = +n_{(1,0)}` (`[C,γ]=0`) or `n_{(0,1)} = −n_{(1,0)}` (`{C,γ}=0`) is a substrate fact the index gate must MEASURE, not assume (vdd EMERGENCE-1 substitution chain). This is why the conjugation clause is a SOFT cross-check, not a hard PASS criterion. The measured `ε_{Cγ}` sign is itself a new substrate-IS datum.
3. **The K=2 Hybrid-Independence second instance is unidentified-but-scoped.** The weighting-functional-family re-axis stays K=1 SUGGESTION until a structurally-distinct triple (different algebra / image / corridor) exhibits the SAME three-register weighting structure with an independent algebraic envelope. The natural candidate is the M_4(ℂ)_PS Pati-Salam block (seed / S91 W9 `hit_iv_pass` Wedderburn-rank distinction). Not actionable until that triple is computed.
4. **The integer 3He-B BDI branch-count Level-3 anchor is pre-registered but unmeasured.** The corridor can join the cross-pillar-bridge family (5-anatomy: substrate-IS = `[φ_{cd}] ∈ ℤ³`; lab-IN = integer BDI branch-count per inheritance sector; bridge = Connes-Karoubi pairing; Level-2 = trivial-by-saturation; Level-3 = measured integer triple) ONLY once both the substrate index gate (Q1) and the lab measurement land. The ratio-valued half (Class B, `7.3250`) is already in the ledger (`S87-W11-C5-LAB-FALSIFIER` PASS, Lancaster MCT-3); the integer-count half (Class A) is the NEW deliverable.
5. **CF-S93-W1-3 registry-row landing is mack-cosmic-bridge sole-writer.** This workshop EFFECTS the corpus directive + K=1 calibration instance (corpus is fair game) but the §VII.AU CF-37 permanent registry-row landing (if any) is mack's execution leg per `feedback_mack-bridge-role.md` — flagged as the CF-S93-W1-3 execution leg, not effected here.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- `Z_factor = 2.046` reading: `2.28% above 14/7=2` → **`ε_analytic = 2.28%`, the fractional analytic defect of R_CM_full from its integer K_0-rank skeleton** (`Z_factor = 2·(1+ε_analytic)`). The number is unchanged; its *interpretation* is now pinned to the topology/analysis split (this is the (b)-structural part below; the numeric `2.28%` itself is the (a)-revision).
- BdG spectral-spread factor made explicit: `|λ|_max/|λ|_min = 1.3277/0.8197 = 1.62×` — the quantitative source of `ε_analytic ≠ 0` (a non-trivial analytic body, not a point mass).

#### (b) Structural changes

- **Canonical identity: `{three scalars}` → measure `μ_{cd}` (volovik R1) → class `[φ_{cd}]` (vdd R1) → Fredholm MODULE `(H_K, D_K(τ_fold), γ, J)|_{(c)∘(d) image}` (R2 converged).** The canonical's epistemic TYPE moved three times across the workshop, terminating at the module — finer than either single shadow. This is the load-bearing structural output.
- **§(ii.A) axis re-typed: 2-bin binary (atlas-row vs cache-moment) → weighting-functional FAMILY on a saturated K-homology class.** A type change (binary → family-fibered-over-a-finite-topological-base), NOT a layer-count increment. The "3-layer CF-37 K-counter" is REJECTED.
- **Anti-inflation: heuristic ("don't inflate, it feels wrong") → topological STOPPING rule ("you CANNOT inflate — every weighting factors through the same finite ℤ³ class; the K-counter is a base-count, the base is fixed").** A derivation replaces a heuristic.
- **Z_factor 2.28%: "spectral spread is non-trivial" → "analytic defect of the dimension-spectrum residue from its integer K_0-rank skeleton" (the topology/analysis split made numerical on one ratio).** Reading-type change: from a generic spread statement to a specific obstruction-class shadow.
- **The corridor's bridge-eligibility: abstract identification → falsifiable cross-pillar bridge candidate.** With substrate-IS = `[φ_{cd}] ∈ ℤ³`, lab-IN = integer BDI branch-count, Level-2 = TRIVIAL-by-saturation (the rare envelope-free cross-pillar bridge), the corridor is promotable to the cross-pillar-bridge family — a type promotion from "F-image bookkeeping" to "Level-3-anchorable prediction."
- **Conjugation relation: assumed PASS gate → measured branch-discriminator.** `n_{(0,1)} = ±n_{(1,0)}` is re-typed from a hard criterion into a measurement of `[C,γ]` on the conjugate sectors (SOFT cross-check), strictly more information.

### What Holds

- **The R1 convergences survive R2 intact**: R_third is a moment (s=4) not the canonical; the 2-OOM span is generic for distinct functionals of one object, not a contradiction; `w_K0 = d_a/304` is the inheritance-DISTINGUISHED weight (forced by the χ'-morphism, S89 §W2-3, audit_sha=`90bba262af80a04c`); R_third irreducible to BOTH §(ii.A) bins (not atlas-row — integrates 112 eigenvalues; not CM cache-moment — K_0 weight ≠ dimension-spectrum `w_CM`).
- **The cocycle-RATIO witness is class-level and regulator-invariant**: `793346/108307` (`S88-SUBSTRATE-IS-PRESERVATION-RANK2-INHERITANCE-THEOREM` PASS, `cancellation_residual=0`, `n_pass=25/25`); FI across the UV-regulator atlas (`S89-...-REGULATOR-CLASS-INVARIANCE-SCAN`, `reg_class_invariant=True`, `max_rel_dev=2.4e-6`). Under module-as-canonical it lives in the topological shadow. NOT extractable from any single-weight measure-moment (volovik's CONVERGENCE substitution chain).
- **Image L_max-saturation**: `N_image=112` bit-identical at L=10/12; level-≤1 sectors `{(0,0):16, (0,1):48, (1,0):48}`; Friedrich-Bär forecloses new-sector intrusion (`η_FB·√(C_2(p+q=L_max)+1) > |λ|_max=1.3277`). The pre-registered null `R_third(L=14) ≡ R_third(L=12)` is co-signed; both agents predict ZERO drift (structural-signature), neither predicts drift.
- **Substrate framing intact**: the substrate IS the Fredholm module (phononic-framing.md Level 1, single-τ-slice τ_fold=0.190); the class, the measure, and the three scalars are all conventions/projections that descend from it — direction of explanation flows downward, never inverted.
- **Residual-reading discipline**: same registry-slot family (§VII.AU) does NOT imply same residual-reading — the s=3 OP-PROJ sister gate (W1-2, support GROWS, rel_drift=2.374e-3, does NOT saturate) reads residual as asymptotic remainder; the s=4 (c)∘(d) corridor (support CLOSED) reads it as fixed structural signature. Key the reading to support-saturation status, not slot-family membership.

### What Breaks or Strains

- **The "canonical = class simpliciter" reading (vdd R1) BREAKS** — it discards the analytic body (BdG energies, the 2.28% defect), which is physical content. Replaced by "canonical = module; class = topological shadow." (vdd CONVERGENCE retraction.)
- **The "canonical = measure simpliciter" reading (volovik R1) BREAKS** — it discards the cocycle-ratio invariants (`793346/108307`), which the regulator-invariance scan proves are topological-not-analytic. Replaced by the module. (volovik CONVERGENCE concession.)
- **The "3-layer CF-37 K-counter" (vdd WP draft, lines 453/504/516/527) BREAKS** — it counts fibers (weightings) as if they were structurally-orthogonal axes; the topological stopping rule shows there is provably nothing new to count until the base class changes. (vdd retraction; volovik withdraws the heuristic for the derivation.)
- **STRAIN (honest residual, not a break)**: the integer triple `(n_{(0,0)},n_{(0,1)},n_{(1,0)})` is value-unpinned — "canonical = class" is type-correct, but its VALUE awaits the Fredholm-index gate. The class-as-canonical claim is correct in TYPE; its numerical content is queued (both agents co-sign this gap, volovik DISSENT-1 + vdd DISSENT-2).
- **SCOPE STRAIN (vdd DISSENT, recorded not reopened)**: the two shadows are symmetric AS the canonical's components but ASYMMETRIC in bridge-anatomy role — only the topological shadow `[φ_{cd}]` can be the cross-pillar-bridge Element-1 observable (integer ⇒ Level-2-trivial, factorization-relevant, lab-falsifiable); the measure is physical but NOT a bridge observable. The corpus row carries this scope-note to protect the future 5-anatomy landing.

### Carry-Forward Computations (MATH ONLY — propagate to S93)

**CF-(i) — Fredholm-index gate: pin the integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)})`** (NEW — converts the type-pinned canonical to value-pinned; closes Open Question 1+2; the D1/D2 + volovik-DISSENT-1 confirmation gate).

1. **What**: Compute `Index(P_a · D_K^{off-diag} · P_a)` per surviving inheritance sector `a ∈ {(0,0),(0,1),(1,0)}` on the (c)∘(d) image at τ_fold=0.190, returning the integer triple `(n_{(0,0)}, n_{(0,1)}, n_{(1,0)}) ∈ ℤ³`, AND the measured C-γ sign `ε_{Cγ}` on the conjugate-triplet sectors.
2. **Inputs**: L_max=12 master spectrum cache `s84_spectrum_cache_L12_tau019.npz`; χ'-inheritance morphism kernel (S89 §W2-3, audit_sha=`90bba262af80a04c`); grading γ and real structure J from the canonical spectral triple; the surviving-sector projectors `P_a` from the Peter-Weyl decomposition.
3. **Gate**: PASS iff (HARD) (i) all three indices are integers `|n_a − round(n_a)| < 1e-9` AND (ii) the **grading-signed** total `Σ_a sgn_a·n_a` is consistent with the BDI winding `N_K=2` (where `sgn_a` is the measured C-γ sign per sector — NOT a naive sum: if `{C,γ}=0` the conjugate pair cancels and the singlet carries the winding; if `[C,γ]=0` the pair sums to `2·n_{(0,1)}`). SOFT cross-check (reported, not gated): `ε_{Cγ}` and hence whether `n_{(0,1)} = ±n_{(1,0)}` — vdd's K-homology prediction + volovik's BdG prediction (both: non-zero integers consistent with `N_K=2`) on the SAME triple is a Stage-2-style cross-axis check per `joint-theorem-promotion.md §"Stage 2"`. Conjugation clause is SOFT per vdd's closing call (EMERGENCE-1 substitution chain: the index is γ-sensitive, so the gate MEASURES `[C,γ]`, does not assume it).
4. **Effort**: ~1 wave, single script, GPU-eligible (off-diagonal block index via `torch.linalg` on the per-sector restriction; the largest block dim is well within the 17.1 GB VRAM cap per `math-scripts.md §"D_K Block-Diagonality"`).
7. **Depends on**: `s84_spectrum_cache_L12_tau019.npz` (UPSTREAM DATA); S89 §W2-3 χ'-morphism kernel (REGISTRY); canonical spectral-triple γ/J construction module.

**CF-(ii) — re-scope (do NOT duplicate) CF-S93-W1-3**: its open field "canonical-identity NOT YET pinned" (WP draft line 519) is now **CLOSED in TYPE by this workshop** → re-scope to *"land the Fredholm-module-as-canonical corpus row per the W-2 verdict (canonical = module; topological shadow `[φ_{cd}] ∈ ℤ³` = cross-pillar-bridge Element-1 observable; analytic shadow `μ_{cd}`; weighting-functional family; K=1 SUGGESTION; two methodology sub-lessons in one row)."* The DOCUMENTATION leg (corpus directive + K=1 instance) is EFFECTED IN-SESSION below (Effected item 1); the mack-authored permanent registry-row landing (if any) is the CF-S93-W1-3 execution leg per `feedback_mack-bridge-role.md`. This is a re-scope, NOT a new CF — no duplicate item added.

*(No further MATH carry-forwards. The integer 3He-B BDI branch-count Level-3 anchor (Open Question 4) is DOWNSTREAM of CF-(i) — it depends on the substrate index triple landing first — and the ratio-valued half is already in the ledger (`S87-W11-C5-LAB-FALSIFIER` PASS); per `feedback_fix-in-session-never-defer.md` it is not yet a 4-field-fillable gate until CF-(i) returns, so it is held as Open Question 4, not a CF. The K=2 Hybrid-Independence second instance (Open Question 3) is not actionable until the Pati-Salam-block triple is computed — also held as an Open Question, not a CF.)*

### Effected In-Session (NON-MATH — completed by vdd, the final agent, BEFORE TERMINATING)

- [x] **Corpus §19 DIRECTIVE landed** (the §(ii.A) refinement: atlas-row/cache-moment 2-layer binary → weighting-functional FAMILY through one finite K_0 class; canonical = Fredholm module; topological STOPPING rule; bridge-anatomy scope distinction; two methodology sub-lessons; sub-question (a)/(b)/(c) verdicts; SUGGESTION at K=1) — appended after corpus §18 (append-only, the next free §) — `sessions/framework/registry/cross-pillar-bridge-corpus.md:970` (§19) / `:974` (§19.0 DIRECTIVE) — anchor `§19.0 Weighting-Functional-Family DIRECTIVE`.
- [x] **Corpus §19.1 K=1 calibration INSTANCE landed** (§VII.AU CF-37 (c)∘(d), s=4; three F-images `{R_ansatz=3.900e-04, R_CM_full=7.978e-04, R_third=6.960e-06}`; three canonical=module witnesses; Z_factor 2.28% topology/analysis-defect derivation; honest value-pinning residual + C-γ measurement; two falsifiers as lab images of the two halves of `[φ]`; K=1 status + Pati-Salam K=2 candidate) — `sessions/framework/registry/cross-pillar-bridge-corpus.md:1024` — anchor `§19.1 K=1 calibration instance`.
- [x] **Parent-rule §(ii.A) mirror text PRESERVED VERBATIM + FLAGGED ORCHESTRATOR-RESERVED** (the one-line `§(ii.A)` refinement-pointer for `.claude/rules/substrate-first-canonical-sourcing.md §(ii.A)`; subagents are EDIT-DENIED on `.claude/rules/` by harness convention, same as the §VII.BA §18 / W-1 precedent) — the orchestrator lands the mirror; the verbatim text is preserved in the corpus for the orchestrator's use — `sessions/framework/registry/cross-pillar-bridge-corpus.md:1020` — anchor `§19.0 ORCHESTRATOR-RESERVED block`.
- [x] **Audit-script Class-(g) detector `detect_weighting_functional_family` ADDED** (single-function-scope, mirrors `detect_composite_bridge_map_taxonomy` at line 336; regex block + self-contained detector returning a structured diagnostic dict; flags fiber-count "N-layer K-counter" misframe WITHOUT the family-reaxis + topological-stopping-rule reframe; S2 advisory at K=1, NOT HARD-HALT) — `computations/_shared/_cross_pillar_bridge_audit.py:456` (detector) / `:409` (header) / `:434` (regex block) — anchor `Class-(g) Weighting-Functional-Family canonical detector`.
- [x] **Audit detector SELF-TESTED on disk** (3/3 cases via GPU venv: positive fiber-count-misframe-without-reframe → `canonical_id_incomplete=True`, `severity=S2`; reframe-complete → `NONE`; clean → `NONE`; module imports + CLI exits 0, no regression) — verified via `phonon-exflation-sim/.venv312/Scripts/python.exe` import + assertion run — `computations/_shared/_cross_pillar_bridge_audit.py:456`.
- [x] **Workshop doc R2-vdd sections + Verdict table + Open Questions + Wrap-Up filled** (CONVERGENCE/DISSENT/EMERGENCE answering Q-volovik-R2-1/2/3; 3-row Verdict; 5 Open Questions; What-Changed (a)/(b)-split; What-Holds; What-Breaks; Carry-Forward MATH; this Effected-In-Session block; Closing Line) — `sessions/archive/session-92/workshops/s92-vii-au-cf37-deeper-canonical-identification.md:416` onward — anchor `Round 2 — vdd: Cross-Synthesis`.

**NOT effected here (correctly flagged, not misrouted)**:
- **CF-S93-W1-3 permanent §VII.AU CF-37 registry-row landing** — mack-cosmic-bridge sole-writer per `feedback_mack-bridge-role.md`; this is the CF-S93-W1-3 EXECUTION leg (re-scoped from "canonical-identity NOT YET pinned" to "land the module-as-canonical row per corpus §19"). The corpus DIRECTIVE + calibration content (corpus is fair game) IS effected above; the permanent registry-row is mack's leg. NOT a §VII row / STAGE-3 anything is registered by this workshop.
- **Parent-rule `.claude/rules/substrate-first-canonical-sourcing.md §(ii.A)` one-line mirror** — orchestrator-reserved (harness EDIT-DENY on `.claude/rules/`); verbatim text preserved in corpus §19.0 ORCHESTRATOR-RESERVED block for the orchestrator to land.
- **CF-(i) Fredholm-index gate** — MATH carry-forward (needs `.py` compute); in Carry-Forward Computations above, NOT here (correctly routed: it requires next-session numerical compute, so it is NOT an "Effected In-Session" item).

### Closing Line

The three OOM-separated scalars at the §VII.AU CF-37 (c)∘(d) corridor were never three estimates of one number — they are three moments of one Fredholm module, whose topological shadow `[φ_{cd}] ∈ ℤ³` carries the cocycle-ratio invariants the lab can falsify as integers and whose analytic shadow `μ_{cd}` carries the BdG energies the lab measures as line positions; the module is the canonical, the two shadows are its topology and its analysis, every weighting factors through the one finite class, and the K-counter counts classes, not the weightings anyone chooses to read them with.
