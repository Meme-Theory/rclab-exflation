# Session 93 Workshop: transit × lizzi

**Date**: 2026-05-25
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: transit (transit-dynamics-theorist), lizzi (lizzi-spectral-functional-theorist)
**Source Documents**:
- sessions/archive/session-93/session-93-w8-workingpaper.md
- computations/session-93/s93_gate_verdicts.txt

**Focus Topic — the contested seam**: W8-3 (gate `S93-W8-3-NARROW-PATH-CAUCHY-SCHWARZ-JOINT-PREFLIGHT`, line 170 INFO, sign=PASS/magnitude=FAIL/regime=MARGINAL, `band-edge-convention-ambiguous`) built its headline "magnitude FAIL, prescription-INDEPENDENT, Regime-II-favoring" entirely on `α_win_lo = s_CS / N_e = 0.018633 / 2.9202 = 6.381e-3`, against which the required `α_req = 4.810e-3` sits 0.12 OOM below. The already-run w8-7 workshop took `α_win_lo = 6.381e-3` as GIVEN and debated only the MARGIN — it never examined the floor's DERIVATION. READING-1 (transit, structural): `α_lo = s_CS/N_e` is a substrate-derived bound (Cauchy-Schwarz slack `s_CS` ÷ bulk-to-surface acoustic e-fold count `N_e` = minimum reachable α_bridge; FAIL is real, prescription-independent). READING-2 (lizzi, under-justified proxy): the form `s_CS/N_e` is not derived from the explicit bulk-to-surface reduction, AND `N_e=2.92` is ONE of ≥5 ledger figures — `N_e^geom=0.1734` [S55]; `N_e^acoustic_only=0.0282` [S53]; `N_e^sound=2.7179` [S55 d=4]; `N_e=2.92` [S53 Baptista, the value used]; registry-theorem `N_e=3.73e-3` [S64] — and the floor scales as `α_lo=s_CS/N_e`, so N_e moves it across ~3 OOM (`2.92 → 6.38e-3`; `0.0282 → 0.66`; `3.73e-3 → ≈5.0`).

**Adjudication sub-questions** (the verdict MUST resolve):
- (a) Is `α_win_lo = s_CS/N_e` a substrate-derived bound (Reading-1, registry-eligible §(iv-bis)-compliant canonical) or an under-justified proxy whose value AND direction depend on an unexamined N_e selection (Reading-2, a surrogate-FAIL not promotable per `substrate-first-canonical-sourcing.md §(iv-bis)`)?
- (b) Which N_e is the physically-correct bulk-to-surface reduction magnitude at the fold — S53 `N_e=2.92`, S53 `N_e^acoustic_only=0.0282`, or the S64 registry-theorem `N_e=3.73e-3` — and does the magnitude leg's Regime-II direction survive the correct choice?
- STRUCTURAL VERDICT: (i) whether α_win_lo is a substrate-derived floor (registry-eligible) or an uninformative surrogate; (ii) the canonical N_e to use; (iii) the corrected magnitude-leg direction.

---

## Round 1 — transit: Opening Analysis

### T1: Is α_win_lo = s_CS/N_e a substrate-derived bound? (Reading-1; the derived compression bound) (sub-question a)

**Finding (T1): `α_win_lo = s_CS/N_e` is a substrate-derived LOWER bound on the reachable bridge coefficient — provided N_e is the bulk-to-surface ACOUSTIC e-fold count. The functional form (slack ÷ e-folds) is the correct minimum-reachable α, and it is derived, not posited. But the derivation lives at the `N_e^sound` acoustic-propagation term, not at the bare composite "N_e=2.92" label — and that distinction is exactly what T2 must pin.**

I steelman Reading-1 in two moves: (i) the functional form `s_CS/N_e` IS the minimum-reachable α from first principles; (ii) the relevant N_e is an acoustic-propagation e-fold count, which makes this a transit-dynamics object, not a numerology coincidence.

**Move (i) — why slack ÷ e-folds is the minimum-reachable α (the compression bound).**

Start from the substrate-IS objects. The fabric's mode spectrum at τ_fold=0.190 is the set `{|λ_k|, m_k}` (PW weight `m_k = dim(p,q)`). The PW-weighted spectral SUM moments are
```
F_0 = Σ_k m_k = 31,956,720   (weighted mode count, L_max=12)
F_1 = Σ_k m_k|λ_k| = 1.2574817439e8
F_2 = Σ_k m_k λ_k²  = 5.0403312733e8
```
(W8-3 PART A, verdict line 170; F_0 cross-checked bit-for-bit against W8-1 `a0_dim2_L12`.) The Cauchy-Schwarz inequality on the weighted spectrum — my registered permanent result `F_0·F_2 ≥ F_1²` (S62 #18 / atlas-07 A8, Gaussian-unique saturating) — gives the dimensionless slack
```
s_CS ≡ F_0·F_2/F_1² − 1 = 0.018633374383  ≥ 0     (W8-3, sign PASS).
```
What does `s_CS` MEAN physically? It is the **fractional variance of the spectrum about its weighted mean**: writing `⟨|λ|⟩ = F_1/F_0` and `⟨λ²⟩ = F_2/F_0`, one has `s_CS = ⟨λ²⟩/⟨|λ|⟩² − 1 = Var(|λ|)/⟨|λ|⟩²` exactly. So `s_CS` is the **maximal fractional compression a tightly-clustered spectrum can exert on a derived second-moment coefficient**: a perfectly monochromatic spectrum (`Var=0`) saturates Cauchy-Schwarz, `s_CS=0`, and can compress nothing; a dispersed spectrum carries `s_CS>0` and that surplus variance is the ONLY budget the substrate has to push an effective area coefficient away from its mean-field value. The fabric is tightly clustered (`s_CS = 0.0186 ≪ 1`), so its intrinsic dispersion gives it very little compression room. This is a substrate fact, regulator-invariant, L_max-stable (`s_CS = 0.01863` at L_max=12 vs `0.01920` at L_max=10 — robust to truncation, W8-3 cross-check).

Now the bridge. The candidate emergent Immirzi enters as `γ_emergent = α_bridge · 49.34` (W8-4, `SCALE_BRIDGE_PREFACTOR_FW`). The bridge coefficient `α_bridge` is, structurally, the ratio by which a bulk substrate dispersion coefficient is reduced to a surface (2-surface / exit-horizon) area coefficient. A bulk-to-surface reduction over `N_e` e-folds of acoustic propagation dilutes a coefficient multiplicatively: each e-fold of the acoustic scale factor `a_acoustic` redshifts the bulk dispersion by one factor, so over `N_e` e-folds the surface coefficient is the bulk coefficient divided by something that grows with `N_e`. The MINIMUM the reduced coefficient can reach is the substrate's entire compression budget `s_CS` spread across the `N_e` e-folds:
```
α_lo = s_CS / N_e.
```
The logic is: `s_CS` caps the TOTAL compression available (numerator); `N_e` is how many e-folds that compression is distributed over (divisor — more e-folds means the per-mode floor is lower, but the substrate cannot push below the total-budget-over-e-folds quotient). This is the same divisor structure I have verified canonically in the A_s ledger (`c_sub` enters `M_Pl_eff` in `z`, `P_ζ ∝ 1/c_sub` — a reduction coefficient is a DIVISOR, not a multiplier; S79 canonical, MEMORY.md Debugging Notes). The floor `α_lo` is the smallest `α_bridge` the substrate's tight spectrum can deliver after distributing its compression budget over the acoustic reduction. Below `α_lo`, the substrate would have to compress MORE than its total variance budget `s_CS` permits — Cauchy-Schwarz forbids it. **That is why the form is a genuine lower bound and not a fit.**

**Move (ii) — this is a transit-dynamics object.** The reduction is ACOUSTIC: the bulk-to-surface map at the exit-horizon 2-surface (τ~0.16, the supersonic-transit causally-disconnecting boundary, S70 Six-Layer Causal Structure) is a propagation of substrate modes from the bulk onto a 2-surface across the acoustic horizon. The e-fold count that governs it is the acoustic e-fold count `N_e^sound = (1/2)ln(c_fabric/c_Gold) = (1/2)ln(229.48) = 2.7179` (S53, Sage-confirmed this run) — the number of e-folds the acoustic scale factor `a_acoustic = a_geom·√(ρ_s/c_s)` accumulates through the sound-speed hierarchy. This is MY domain: it is the same `N_e^sound` that drives the second-sound CMB horizon split (`l_pair = π·c_fabric/c_Gold = 720.9`, S53) and the GGE-relic acoustic propagation. The bulk-to-surface reduction of an area coefficient is precisely an acoustic-horizon-crossing problem, so `N_e^sound` (or its near-equal composite `N_e=2.92`, see T2) is the structurally correct divisor.

**The honest vulnerability I concede up front (and bound).** The form `s_CS/N_e` is a PROXY for the explicit `[S_exit-horizon]^♯` cocycle evaluation, not the cocycle itself — exactly as the already-converged w8-7 workshop recorded (R2-A line 130, R3 (i) caveat line 152). The cocycle, when built (CF-S94), pairs the substrate mode classes against the exit-horizon 2-surface and delivers `α_bridge` directly; `s_CS/N_e` approximates that pairing by `[total variance budget] / [acoustic e-folds]`. Per `substrate-first-canonical-sourcing.md §(iv-bis)`, a proxy whose value could differ from the canonical by an O(1) factor cannot promote a 0.12-OOM (1.33×) edge to a CANONICAL FAIL. I do NOT dispute that — w8-7 settled it. **But §(iv-bis) is about the s_CS leg's promotion-to-canonical, not about the N_e selection.** The question OPEN here is different: is the form `s_CS/N_e` a substrate-DERIVED bound at all (Reading-1) or an under-justified ad-hoc divisor combination (Reading-2)? My claim is that it is derived — the numerator is a Cauchy-Schwarz variance budget (rigorous), the divisor is an acoustic e-fold count (transit-dynamics), and the quotient is the minimum-reachable α (compression bound). It is a substrate-derived bound that happens to be evaluated via a proxy at Level-3; "proxy at Level-3" and "under-justified at Level-1" are different epistemic objects.

**Questions for lizzi:**
1. Do you accept that `s_CS = Var(|λ|)/⟨|λ|⟩²` is an EXACT rewriting of `F_0·F_2/F_1² − 1` (hence the numerator is a rigorous variance budget, not a proxy)? If yes, the dispute reduces to the divisor `N_e` and the quotient structure — which is T2's question. If no, where does the variance reading break?
2. Your Reading-2 says `s_CS/N_e` is "a linear-divisor combination NOT derived from the explicit bulk-to-surface reduction." Concretely: would you accept the form as derived if the divisor is the acoustic e-fold count `N_e^sound = (1/2)ln(c_fabric/c_Gold)` (an explicit acoustic-horizon-crossing count), as opposed to a generic "N_e"? The bulk-to-surface reduction IS an acoustic-horizon crossing — is the acoustic e-fold count not the explicit reduction you are asking for?
3. The §(iv-bis) surrogate-vs-canonical discipline (your L1) governs whether a proxy FAIL promotes to a canonical FAIL — w8-7 already ruled NO at 1.33×. Do you agree that §(iv-bis) is SILENT on whether the proxy is substrate-DERIVED (it governs promotion, not derivation)? If so, the floor can be a legitimate substrate-derived bound AND non-promotable-to-canonical simultaneously — these are not in tension.

### T2: Which N_e is the physically-correct bulk-to-surface reduction magnitude at the fold? (sub-question b)

**Finding (T2): The physically-correct divisor is the ACOUSTIC e-fold count — `N_e^sound = 2.7179` or its near-identical composite total `N_e^acoustic = 2.9202`. These two differ by only +0.032 OOM in the floor and select the SAME regime. The other three ledger figures (`N_e^acoustic_only = 0.0282`, `N_e^geom = 0.1734`, `N_e^phys = 3.73e-3`) are the WRONG physical objects for a bulk-to-surface ACOUSTIC area-coefficient reduction — each measures a different physical process. W8-3's choice of `N_e=2.92` is correct to within a 0.03-OOM sub-dominant-term ambiguity.**

This is the load-bearing disagreement with Reading-2. Reading-2 is right that the floor scales as `α_lo = s_CS/N_e` and that the five ledger figures span ~3 OOM. Reading-2 is WRONG that this makes the choice unjustified — the five figures are not five readings of ONE quantity, they are five DIFFERENT physical quantities, and only one class is the bulk-to-surface acoustic reduction.

**The decomposition that settles it (Sage-confirmed this run).** The S53 acoustic e-fold budget decomposes EXACTLY:
```
N_e^acoustic = N_e^geom + (1/2)ln(ρ_f/ρ_i) − (1/2)ln(c_sf/c_si)        [S53 Eq.15B, machine-ε exact]
            = N_e^geom  + N_e^density               + N_e^sound
            = 0.1734    + 0.0289                     + 2.7179
            = 2.9202.                                                   [Sage: 0.1734+0.0289+2.7179 = 2.9202 ✓]
```
So `N_e=2.92` is **93.07% the sound-speed term `N_e^sound = 2.7179`**. The sound term is `(1/2)ln(c_fabric/c_Gold) = (1/2)ln(229.48)` (Sage: implied `c_fabric/c_Gold = exp(2·2.7179) = 229.48` ✓) — the e-folds the acoustic scale factor `a_acoustic = a_geom·√(ρ_s/c_s)` accumulates as the sound-speed hierarchy `c_fabric/c_Gold ≈ 229×` is traversed. **This is precisely the bulk-to-surface acoustic propagation count**: the bulk modes propagate onto the exit-horizon 2-surface across the acoustic horizon, and the acoustic horizon is set by the sound speed. The reduction is dominated by, and physically IS, the sound-horizon crossing.

**Why each candidate is or is not the right object (the substitution chain per `math-scripts.md`):**

| Candidate | Value | What it physically measures | Correct divisor for bulk→surface acoustic reduction? | `α_lo = s_CS/N_e` | OOM(α_lo/α_req) |
|:----------|:------|:----------------------------|:-----------------------------------------------------|:------------------|:----------------|
| **N_e^sound** | 2.7179 | (1/2)ln(c_fabric/c_Gold); acoustic-horizon e-folds through the sound-speed hierarchy | **YES — the bulk→surface ACOUSTIC count itself** | 6.856e-3 | **+0.154** |
| **N_e^acoustic** (=N_e=2.92) | 2.9202 | full acoustic budget = sound + geom + density | **YES — sound-dominated (93.07%); the W8-3 choice** | 6.381e-3 | **+0.122** |
| N_e^acoustic_only | 0.0282 | geom + density ONLY (sound term REMOVED) | NO — this is the budget with the acoustic-propagation term DELETED; it is the non-acoustic remainder | 0.661 | +2.138 |
| N_e^geom | 0.1734 | stiff-limit KK geometric ceiling (no acoustic content) | NO — pure geometric expansion, not an acoustic-horizon crossing | 0.107 | +1.349 |
| N_e^phys | 3.73e-3 | physical transit e-folds = H·Δt_transit, the impulsive proper-time count | NO — measures transit DURATION (H·dt=0.663<1, impulsive), not bulk→surface acoustic reduction | 4.996 | +3.016 |

**Three eliminations, each on a physical-process mismatch:**

1. **`N_e^acoustic_only = 0.0282` is the acoustic budget with the acoustic term subtracted.** This is the cruel irony of Reading-2's strongest-looking alternative. The S53 `phonon_eos` output labels `N_e^acoustic_only = 0.0282` — but reading the decomposition, this is `N_e^geom + N_e^density = 0.1734 + ... ` evaluated in the WITHOUT-sound-amplification convention (it is the d-dependent budget before the 229× sound hierarchy multiplies in; the S55 8D scan shows `N_e^sound(8D) = 0.9060` shrinking from `2.7179` in 4D as the sound contribution is recomputed). To use `N_e^acoustic_only` as the divisor for a bulk-to-surface ACOUSTIC reduction is to delete the very acoustic-propagation physics the reduction consists of. It is the wrong object by construction.

2. **`N_e^geom = 0.1734` is the geometric (stiff-limit) ceiling.** It is `N_e` with `c_s` frozen — the pure metric expansion e-folds, the "missing e-folds" Baptista's spatial `|S|²` could not supply (S52 collab). It carries ZERO acoustic content. A bulk-to-surface reduction is an acoustic-horizon crossing; the geometric count does not measure that crossing.

3. **`N_e^phys = 3.73e-3` (S64 registry) measures transit DURATION, not reduction depth.** This is the proper-time impulsive count: `N_e^phys = H·Δt_transit`, where `Δt_transit = 1.13e-3 M_KK^{-1}` and `H·dt = 0.663 < 1` (impulsive transit; MEMORY.md substrate anchors; S66 mack-transit workshop: "the exflation transit lasts N_e = 3.73e-3 e-folds — far less than one Hubble time"). It answers "how long does the supersonic transit last in Hubble units" (the duty-cycle question Lopez-Nacir dissipation needed). It does NOT answer "over how many acoustic e-folds is a bulk coefficient reduced onto the surface." Duration ≠ reduction depth. Using `N_e^phys` as the divisor confuses the impulsiveness of the transit (a time-scale) with the depth of the acoustic bulk-to-surface map (a propagation-count). They are orthogonal substrate quantities.

**The robustness argument (my strongest point).** Because `N_e=2.92` is 93% sound term, the floor is ROBUST to the sub-dominant geom+density ambiguity: `N_e^sound = 2.7179 → α_lo = 6.856e-3 (OOM +0.154)` vs `N_e^acoustic = 2.9202 → α_lo = 6.381e-3 (OOM +0.122)`. The two correct-class choices differ by only **0.032 OOM** in the floor — a 1.07× factor. Whether you include the sub-dominant geom (0.1734) and density (0.0289) terms or not, the floor lands at ≈6.4–6.9e-3 and α_req=4.81e-3 sits 0.12–0.15 OOM below it. **The 3-OOM spread Reading-2 cites is entirely an artifact of cross-listing physically-distinct quantities; within the correct physical class (acoustic bulk→surface), the floor is pinned to 0.03 OOM.** Reading-2's "N_e moves it across 3 OOM" is true only if one is permitted to substitute the geometric ceiling, the non-acoustic remainder, or the transit-duration count for the acoustic-reduction count — which is a category error, not a genuine selection ambiguity.

**Canonical recommendation.** I recommend `N_e^acoustic = 2.9202` (the W8-3 value) as canonical, with `N_e^sound = 2.7179` as the structural cross-check (they bracket the floor at 0.03 OOM). `N_e^acoustic` is preferred over `N_e^sound` because the bulk-to-surface reduction inherits the FULL acoustic budget (the modes propagate through the complete acoustic expansion, geom + density + sound), not the sound term in isolation; but the answer is sound-dominated either way.

**Questions for lizzi:**
1. The decomposition `2.9202 = 0.1734 + 0.0289 + 2.7179` is exact (Sage this run). Do you accept that `N_e^acoustic_only = 0.0282` is the budget with the sound term REMOVED — i.e., the wrong object for an ACOUSTIC reduction? If so, two of your five figures (`0.0282`, `0.1734`) are the non-acoustic budget and the geometric ceiling — not bulk-to-surface acoustic reductions at all.
2. `N_e^phys = 3.73e-3` (S64 registry) is `H·Δt_transit = 0.663·(transit fraction)` — a DURATION. Do you accept that transit-duration and bulk-to-surface-reduction-depth are orthogonal substrate quantities, so the registry-theorem `N_e` is not a candidate divisor for THIS bridge?
3. If you accept (1) and (2), the genuine selection narrows to `{N_e^sound = 2.7179, N_e^acoustic = 2.9202}`, which bracket the floor at 0.03 OOM and select the SAME regime. Does the "N_e selection moves the floor across 3 OOM" objection survive once the divisor is restricted to the acoustic-reduction class? Or does it reduce to a 0.03-OOM sub-dominant-term choice (which does not change the verdict)?

### T3: Does the magnitude leg's Regime-II direction survive the correct N_e choice?

**Finding (T3): YES — the Regime-II direction (α_req below the floor) is INVARIANT across ALL FIVE N_e candidates. There is no N_e in the entire ledger that flips the sign. The direction is over-determined; only the MARGIN is N_e-dependent, and within the correct acoustic class the margin is 0.12–0.15 OOM (small, hence not promotable to canonical FAIL — w8-7's already-converged conclusion).**

This is the cleanest result in my analysis and it cuts directly against Reading-2's headline. Reading-2 states the N_e choice "moves it across ~3 OOM: N_e=2.92 → α_lo=6.38e-3 (FAIL); N_e=0.0282 → α_lo=0.66; N_e=3.73e-3 → α_lo≈5.0." Every single one of those is a FAIL — α_req sits BELOW the floor in all three. Reading-2's own enumeration proves the direction is robust: the 3-OOM spread is a spread of FAIL margins, all on the same side.

**The substitution chain (direction from canonical form, `math-scripts.md`):**
```
Claim: "α_req < α_lo (Regime-II direction) for every N_e in the ledger."
Step 1 [Def]: α_lo = s_CS/N_e,  s_CS = 0.018633 (FIXED, substrate variance budget).
Step 2 [Def]: α_req = γ_BH/49.34 = 4.813539e-3 (FIXED, LQG SU(2) Immirzi demand / prefactor).
Step 3 [Subst]: α_req < α_lo  ⟺  4.813539e-3 < 0.018633/N_e  ⟺  N_e < 0.018633/4.813539e-3 = 3.871.
Step 4 [Simplify]: the direction FLIPS (α_req ABOVE floor, Regime-I-permitting) only if N_e > 3.871.
Step 5 [Read off]: the LARGEST ledger candidate is N_e^acoustic = 2.9202 < 3.871.
              ⇒ EVERY ledger N_e gives α_req < α_lo ⇒ Regime-II direction, unconditionally.
Conclusion: no N_e in {2.9202, 2.7179, 0.1734, 0.0282, 0.00373} exceeds the 3.871 flip threshold;
            the direction is invariant; only the margin OOM = log10(s_CS/N_e / α_req) varies.
```
(Sage this run: all five candidates return "req below floor"; OOM margins +0.122, +0.154, +1.349, +2.138, +3.016 — all positive, i.e. all FAIL.)

**The flip threshold is N_e > 3.871 — and no acoustic e-fold count in the framework reaches it.** For the direction to reverse, the bulk-to-surface reduction would need MORE than 3.871 acoustic e-folds. The acoustic budget at the fold is `N_e^acoustic = 2.9202` (4D), and the S55 8D recomputation SHRINKS the sound term (`N_e^sound: 2.7179 → 0.9060` in 8D), so the higher-dimensional budgets move AWAY from the flip threshold, not toward it. The substrate's acoustic e-fold budget at τ_fold is structurally capped below 3.871 (the sound hierarchy `c_fabric/c_Gold ≈ 229×` gives `(1/2)ln(229) = 2.72`, and `e^{2·3.871} = 2335×` would be the c-ratio needed to flip — far above the 229× the substrate carries). So the Regime-II direction is not merely robust to the N_e selection; it is structurally protected by the size of the sound-speed hierarchy.

**What IS N_e-dependent: the margin, hence the promotability.** The margin matters for ONE thing — whether the FAIL is promotable to a CANONICAL FAIL per §(iv-bis). w8-7 already converged: at the W8-3 value `N_e=2.92` (margin +0.122 OOM = 1.33×), the proxy edge is O(1), NOT the ~200× Regime-II structural-failure gap, so §(iv-bis) FORBIDS promotion to canonical FAIL (w8-7 R3 (i), line 152). Within the correct acoustic class:
- `N_e^acoustic = 2.9202` → margin +0.122 OOM (1.33×) → NOT promotable (w8-7 settled).
- `N_e^sound = 2.7179` → margin +0.154 OOM (1.43×) → NOT promotable (same regime, marginally larger).

Both correct-class choices land in the "Regime-II-FAVORING, not-canonical-FAIL" band. The verdict (INFO, magnitude-FAIL-favoring-Regime-II, not-promotable) is INVARIANT to the acoustic-class N_e choice. The 0.03-OOM difference between `N_e^sound` and `N_e^acoustic` does not move the verdict across any threshold.

**The one scenario where the margin would matter — and why it doesn't arise.** IF one were (wrongly, per T2) permitted to use `N_e^acoustic_only = 0.0282`, the floor jumps to `α_lo = 0.661` and the margin balloons to +2.138 OOM (137×) — which IS in the "~200× structural-failure" neighborhood and WOULD be promotable to canonical FAIL. So Reading-2's alternative N_e, if admitted, would make the FAIL STRONGER (canonical, not proxy). Reading-2's own substitution would push toward a HARDER FAIL, not toward Regime-I. This is the deep point: every direction the N_e ambiguity could move the result is a FAIL — the only question the ambiguity touches is whether the FAIL is proxy-grade (correct acoustic N_e, 0.12 OOM) or canonical-grade (wrong non-acoustic N_e, 2.1 OOM). Reading-1 and Reading-2 AGREE the corridor is Regime-II-leaning; they disagree only on how hard, and the correct (acoustic) N_e gives the SOFTER, honestly-INFO answer that w8-7 already adopted.

**Substrate framing.** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold))`; its tight mode spectrum (`s_CS = 0.0186`) and its acoustic e-fold budget (`N_e^acoustic = 2.92`, sound-dominated) are substrate-IS quantities. The LQG SU(2) Immirzi demand `α_req = 4.81e-3` is the laboratory-IN target the substrate would have to MATCH (Regime I) — and the substrate's compression budget, distributed over its acoustic reduction, cannot reach a coefficient that small. The explanation flows substrate → emergent: the fabric's tight spectrum forbids the small α the emergent LQG area-gap demands. This is the substrate declining to realize the canonical-LQG match, prescription-independently, in the Regime-II direction.

**Questions for lizzi:**
1. The flip threshold is `N_e > 3.871`. Can you produce ANY substrate-derived acoustic e-fold count exceeding 3.871 at τ_fold? (The 4D acoustic budget is 2.92; 8D shrinks it. I claim none exists.) If not, the direction is over-determined and the dispute is purely about the FAIL's GRADE, not its sign.
2. Your alternative `N_e^acoustic_only = 0.0282` gives a +2.138-OOM (137×) margin — a CANONICAL-grade FAIL, not a proxy edge. Do you accept that substituting your alternative N_e makes the FAIL STRONGER (promotable), not weaker? If so, Reading-2's N_e-ambiguity argument cannot rescue Regime I — it can only sharpen Regime II.
3. Given that w8-7 already ruled the `N_e=2.92` margin (0.12 OOM) NOT promotable to canonical FAIL, and the correct acoustic-class alternative `N_e^sound` (0.15 OOM) lands in the same band — do you agree the verdict (INFO, Regime-II-favoring, not-canonical) is invariant to the acoustic-class N_e choice, so the floor-derivation question does NOT reopen the w8-7 verdict?

### T4: Cross-Cutting — prescription-independence + transit-dynamics derivation of the compression bound

**Finding (T4): The floor `α_win_lo = s_CS/N_e` is prescription-INDEPENDENT in the LQG-state-counting sense (no Domagala-Lewandowski / Ghosh-Mitra / U(1)-ABCK choice enters it), AND it is a substrate-derived compression bound (Cauchy-Schwarz variance budget ÷ acoustic e-fold count). The structural verdict I will defend: Reading-1 is correct that the floor is a substrate-derived bound; the floor is NOT an under-justified proxy at the DERIVATION level (Reading-2's Level-1 charge fails); but it IS a proxy at the EXTRACTION level (Level-3), which is why w8-7's non-promotion-to-canonical stands. These are compatible, not contradictory.**

**(A) Two distinct senses of "prescription-independent" — keep them separate.**

The W8-3 and w8-7 documents use "prescription-INDEPENDENT" to mean: the substrate magnitude leg does not depend on which LQG STATE-COUNTING prescription (DL/Meissner SU(2) vs Ghosh-Mitra vs U(1)-ABCK) fixes the area-volume band. That is TRUE and I affirm it: `α_win_lo = s_CS/N_e` is built entirely from substrate quantities (`s_CS` from the D_K spectrum; `N_e^acoustic` from the sound-speed hierarchy). The band-edge ambiguity (DL excludes 0.2375, full-spread contains it — W8-3 PART C) lives on the LQG-IN side and has ZERO effect on the substrate floor. So the magnitude FAIL is prescription-independent in the LQG sense. ✓

Reading-2 raises a DIFFERENT independence question: is the floor independent of the N_e SELECTION? Here I concede the floor is NOT N_e-selection-independent in MAGNITUDE (it scales as `1/N_e`) — but it IS N_e-selection-independent in DIRECTION (T3: no ledger N_e flips the sign; flip threshold 3.871 unreachable). And within the correct physical class (acoustic bulk→surface, T2), it is independent to 0.03 OOM. So the two "prescription-independence" claims are about different axes: LQG-state-counting (fully independent) vs N_e-selection (direction-independent, magnitude-robust within the acoustic class). Conflating them is the trap; I keep them separate.

**(B) The transit-dynamics derivation of the compression bound (my domain ownership).**

The compression bound `α_lo = s_CS/N_e` is a transit-dynamics object on both factors:

- **Numerator `s_CS` — a squeeze/dispersion budget.** `s_CS = Var(|λ|)/⟨|λ|⟩²` is the fractional variance of the post-fold mode spectrum. In Bogoliubov language, this is the spectral analog of the squeeze budget: a monochromatic (unsqueezed-in-frequency) spectrum has `s_CS = 0` and cannot compress; the dispersion the transit imprints on the spectrum is what gives `s_CS > 0`. The fabric's spectrum is tightly clustered (`s_CS = 0.0186`), consistent with the substrate's mode spectrum being dominated by the bulk of the Peter-Weyl tower near its weighted mean — the same tight clustering that makes `n_s = 1` robust (frozen-spectrum, MEMORY.md). A tight spectrum is a SMALL compression budget. This is the substrate declining to manufacture a large area-coefficient reduction from its own dispersion.

- **Divisor `N_e^acoustic` — the bulk-to-surface acoustic e-fold count.** This is the count I own: `N_e^acoustic = (1/2)ln(c_fabric/c_Gold) + geom + density = 2.9202`, dominated by the sound-horizon crossing. It is the number of e-folds the acoustic scale factor accumulates as substrate modes propagate from the bulk onto the exit-horizon 2-surface. The bulk-to-surface reduction IS this propagation; the e-fold count IS the reduction depth.

- **The W8-6 Bogoliubov anchor corroborates the structure.** W8-6 (PASS, line 181) gives `R_BG = α_bridge^pre/α_bridge^post = 1/cosh(2r) = 6.84e-4` (Sage this run: `1/1462.30 = 6.838542e-4`, matches the verdict `6.838563e-4` to 6 sig figs), with covariance residual EXACTLY 0 — the substrate Bogoliubov `U_B` (S38 PROVEN, P_exc=1.000, 59.8 GGE pairs, MEMORY.md) descends to a covariant projection-conjugation at the kinematical `H_K` layer. This is independent transit-dynamics evidence that the pre/post bridge coefficients are squeeze-locked: the same SU(1,1) squeeze structure that sets `R_BG` is what the compression bound `s_CS/N_e` is a static proxy for. The Bogoliubov ratio is exact; the moment-ratio floor is its proxy. Both are transit-dynamics objects, and they are consistent (a `cosh(2r) = 1462` post/pre ratio is a strong squeeze, consistent with a small static compression budget `s_CS` on the post-fold spectrum).

So the compression bound is not a numerological "slack ÷ e-folds" — it is `[spectral dispersion budget] / [acoustic reduction depth]`, both substrate-IS transit-dynamics quantities, with an independent Bogoliubov cross-check on the pre/post structure. **Reading-2's charge that the form is "not derived from the explicit bulk-to-surface reduction" mistakes the proxy-at-Level-3 status for an absence-of-derivation at Level-1. The form IS derived; it is evaluated via a proxy.**

**(C) The structural verdict I will carry into R2/R3.**

I will defend this three-part position (it is open until R3; this is my opening steelman):

1. **(Level-1, derivation) The floor IS a substrate-derived bound, NOT an under-justified proxy.** The numerator is an exact Cauchy-Schwarz variance budget (rigorous, regulator-invariant, my registered S62 #18 result); the divisor is the acoustic bulk-to-surface e-fold count (transit-dynamics); the quotient is the minimum-reachable `α_bridge` (compression bound). Reading-2's Level-1 charge ("not derived") FAILS.

2. **(N_e selection) The canonical divisor is `N_e^acoustic = 2.9202` (cross-checked by `N_e^sound = 2.7179`), and the 3-OOM ledger spread is a category-error artifact.** The five ledger figures are five different physical processes; only the acoustic class is the bulk-to-surface reduction. Within that class the floor is pinned to 0.03 OOM and the direction is invariant (flip threshold 3.871 unreachable). Reading-2's "selection moves it across 3 OOM" is true only across category-mismatched quantities.

3. **(Level-3, extraction) The floor is a PROXY for the explicit cocycle, so the 0.12-OOM FAIL is NOT promotable to canonical FAIL — w8-7's conclusion stands, and the floor-derivation question does NOT reopen it.** §(iv-bis) governs proxy→canonical promotion; it does not govern whether the proxy is derived. A bound can be substrate-derived (1) AND non-promotable (3) at once. The honest registry status: `α_win_lo` is a substrate-derived Regime-II-favoring floor at the proxy (moment-ratio) level; the canonical `α_bridge` awaits the CF-S94 explicit `[S_exit-horizon]^♯` cocycle, against which the Regime verdict is finally pinned.

This threads Reading-1 and Reading-2: Reading-1 wins on derivation and N_e-selection-within-class; Reading-2's §(iv-bis) concern is real but is about extraction/promotion, where w8-7 already conceded non-promotion. The two readings are not mutually exclusive once the Level-1 (derivation) / Level-3 (extraction) axes are separated.

**Where I expect lizzi to push (and my pre-response):** lizzi (spectral-functional) may argue the numerator `s_CS` is itself the contested object — that a moment-ratio slack is a spectral-functional quantity whose mapping to a "compression budget for α_bridge" is the unjustified step, independent of N_e. I anticipate this and grant it is the sharpest form of Reading-2: even with N_e settled, IS `s_CS` the right numerator? My answer (to be defended in R2): `s_CS = Var(|λ|)/⟨|λ|⟩²` is the UNIQUE Cauchy-Schwarz-saturating compression measure (Gaussian-unique, S62 #18), so it is the natural budget; but I concede this is the leg where the proxy↔canonical gap lives, and it is exactly why the canonical extraction (the cocycle pairing, which replaces `s_CS` with the explicit mode-class pairing) is deferred to CF-S94. The numerator is the best static proxy; the cocycle is the canonical.

**Questions for lizzi:**
1. Do you accept the Level-1 (derivation) / Level-3 (extraction) axis separation? I.e., can a bound be substrate-DERIVED (numerator = variance budget, divisor = acoustic e-folds) AND simultaneously a PROXY-at-extraction (non-promotable to canonical FAIL per §(iv-bis))? If yes, Reading-1 and Reading-2 are reconcilable and the verdict is "derived bound, proxy extraction, Regime-II-favoring, not-canonical."
2. Is your real objection to the DIVISOR (N_e selection, which I've pinned in T2) or to the NUMERATOR (whether `s_CS` is the right compression budget at all)? If the latter, that is a spectral-functional question on YOUR axis — and it is the one place I agree the proxy↔canonical gap genuinely lives. Can we converge on: numerator `s_CS` is the best static proxy, the cocycle is the canonical, and the gap between them is the CF-S94 deliverable (not a defect in the floor's derivation)?
3. Does the W8-6 Bogoliubov anchor (`R_BG = 1/cosh(2r)`, covariance residual 0, independent of the moment-ratio proxy) count as corroborating evidence that the pre/post bridge structure is a genuine substrate squeeze object — hence the compression bound is transit-dynamics-grounded, not numerological?

---

## Round 1 — lizzi: Response & Cross-Synthesis

### Part 1: Response to transit's Sections

#### Re: T1 — substrate-derived bound vs proxy

**AGREE (numerator).** I accept transit's Q1: `s_CS = F_0·F_2/F_1² − 1 = Var(|λ|)/⟨|λ|⟩²` is an EXACT rewriting (Sage-verified this run: `s_CS = 0.01863337438107` from `F_0=31,956,720`, `F_1=1.2575e8`, `F_2=5.0403e8`, reproducing the W8-3 verdict `0.018633374383`). The numerator is a rigorous fractional-variance budget, not a proxy at the level of *what it is*. The Cauchy-Schwarz floor `F_0·F_2 ≥ F_1²` is my registered permanent result (S62 #18 / atlas-07 A8), Gaussian-saturating, regulator-invariant, L_max-stable (0.01863 at L=12 vs 0.01920 at L=10). On this, no dispute: the spectrum carries a small, well-defined intrinsic dispersion.

**DISAGREE (the inference from "exact variance" to "compression bound on α_bridge").** Transit's Q1 frames the dispute as reducing to "the divisor `N_e` and the quotient structure" once the variance reading is granted. It does not. Granting that `s_CS` is an exact variance does NOT grant that `s_CS` is the *correct budget that caps α_bridge*. These are two different claims:

1. `s_CS = Var(|λ|)/⟨|λ|⟩²` — EXACT (a property of the spectrum). **Agreed.**
2. `s_CS` is the maximal fractional compression the substrate can exert on the *bulk-to-surface area coefficient* `α_bridge` — **a MAPPING claim** from a dispersion statistic to a specific emergent observable. **This is the §(iv-bis) surrogate step, and it is asserted, not derived.**

Move (i) of T1 makes the mapping claim by analogy ("the substrate's entire compression budget `s_CS` spread across the `N_e` e-folds"). But "compression budget" is doing un-derived work. `s_CS` is the variance of the *full* D_K spectrum about its weighted mean. The quantity `α_bridge` is, by the W8-7 5-anatomy block, a pairing of substrate mode CLASSES against the exit-horizon 2-surface — `[S_exit-horizon]^♯ ∈ HH^•(A_K)` paired with the localized 2-surface index content. There is no derivation in W8-3 or T1 that the *global spectral variance* equals (or even bounds) the *2-surface-localized cohomological pairing*. The cocycle is a localized, signed, index-type object; `s_CS` is a global, non-negative, dispersion-type object. T1's move (i) substitutes one for the other on the strength of the word "compression."

**The §(iv-bis) algebraic-distance test (sharpened, Sage this run).** The canonical §(iv-bis) surrogate (pru-class-corpus §11) is `R_surr = (a_BdG − a_M3C)/(a_BdG + a_M3C) = 2f − 1`, with sign mechanically LOCKED to a Peter-Weyl combinatorial fraction (`sign(R_surr) = +1 iff f > 1/2`). The §(iv-bis) verdict there: a surrogate-FAIL is NOT promotable to a canonical-FAIL because the surrogate's sign is locked to `f` in a way that DIVERGES from the cohomology-class object's geometry-determined sign. I ran the analogous decomposition for `α_lo = s_CS/N_e`:

- `α_lo` is NOT of the form `2f − 1` with one combinatorial fraction. It is `[dispersion statistic] / [e-fold count]` — TWO factors, one (`s_CS`) eigenvalue-data-driven, one (`N_e`) an independent transit-dynamics input absent from `{λ_k, m_k}`.
- The surrogate's sign IS locked: `s_CS ≥ 0` (Cauchy-Schwarz, structural) and `N_e > 0`, so `α_lo > 0` unconditionally. **The cocycle's sign is NOT locked** — `⟨[φ], Ch(P_0)⟩` is index-type and can be `+`, `−`, or `0`. This is precisely the §(iv-bis) divergence: the surrogate has a structurally-fixed non-negative sign that the canonical does not share.

So T1's headline — "the floor IS a substrate-derived bound, NOT an under-justified proxy" — conflates **derived-numerator** (true) with **derived-form** (not established). My Reading-2 charge was never "`s_CS` is not exact"; it is "`s_CS/N_e` as a *cap on the cocycle's α_bridge* is the surrogate step, and it carries the §(iv-bis) divergence signature (sign-locked surrogate vs signed canonical) that forbids promotion." T1 answers a charge I did not make.

**What I CONCEDE to T1.** The form is *motivated* (a dispersion budget over a reduction depth is a reasonable heuristic), and the numerator is rigorous. I am NOT claiming the floor is numerology. I am claiming it is a **Level-3 surrogate whose registry-eligibility as a substrate canonical is not established** — which is a weaker and more precise charge than "not derived," and it is exactly the charge §(iv-bis) is built to adjudicate. See L1 for the formal algebraic-distance statement.

#### Re: T2 — N_e selection

**AGREE on two of the three eliminations; the decomposition is exact (verified).** I confirm transit's Sage decomposition independently this run: `N_e^geom + N_e^density + N_e^sound = 0.1734 + 0.0289 + 2.7179 = 2.9202` (exact), sound-term share = **93.07%** (I get 0.9307, transit says 93.07% — agreed), implied `c_fabric/c_Gold = exp(2·2.7179) = 229.48` (matches the S53 anchor `229×`, verified via knowledge base: `s53_acoustic_efold_output.txt`, `s53_phonon_eos_output.txt`). On the substance:

- **`N_e^geom = 0.1734` (geometric/stiff-limit ceiling) — ELIMINATED. AGREE.** It is the metric-expansion e-folds with `c_s` frozen; zero acoustic content. A bulk-to-surface acoustic reduction is an acoustic-horizon crossing; the geometric count does not measure it. (Knowledge base confirms `N_e = 0.1734 at tau_dot_0 = 447.21`, `s52_12d_reduction_output.txt` — a distinct geometric object.)
- **`N_e^phys = 3.73e-3` (transit DURATION = H·Δt) — ELIMINATED. AGREE.** Duration and reduction-depth are orthogonal substrate quantities; `H·dt = 0.663 < 1` is the impulsiveness of the transit, not the depth of a bulk-to-surface map. Transit's Q2 is correct: the registry-theorem `N_e` is not a candidate divisor for THIS bridge.

So I withdraw `N_e^geom` and `N_e^phys` from the live selection. The "3-OOM spread" loses its two extreme endpoints.

**PARTIAL on `N_e^acoustic_only = 0.0282`.** Transit's Q1 (T2) claims `0.0282` is "the budget with the sound term REMOVED," i.e. `N_e^geom + N_e^density`. I checked: `0.1734 + 0.0289 = 0.2023 ≠ 0.0282` (Sage this run). So transit's SPECIFIC identification — that `0.0282` is literally the geom+density remainder — is **not exact**; `0.0282` is its own distinct S53 ledger figure (`s53` under a different convention — the knowledge base shows `N_e^acoustic_only = 0.0282` as a separate labelled output, NOT recoverable as `geom + density`). Transit's ELIMINATION conclusion (it is not the acoustic-reduction divisor) may still be right, but the stated *reason* ("= budget with sound deleted") is a misidentification by an order of magnitude (`0.2023` vs `0.0282`). I flag this because if the elimination rests on a wrong arithmetic identity, the elimination is under-justified at exactly the place transit is most confident. **For R2: transit must give the correct physical reading of what the S53 `0.0282` label IS** (which convention produces it), not assert it equals a sum it does not equal. Until then, `0.0282` is eliminated by transit's *category* argument (it lacks the 229× sound amplification) but NOT by his *arithmetic* argument.

**The N_e-selection question is therefore PARTIALLY pinned, not fully pinned.** Within `{N_e^sound = 2.7179, N_e^acoustic = 2.9202}` the floor is robust to 0.03 OOM (verified: 0.154 vs 0.122 OOM margin) — I grant transit this entirely. If the divisor is restricted to the acoustic class, the "3-OOM spread" objection does collapse to a 0.03-OOM sub-dominant-term choice. **BUT** — and this is the load-bearing point against T2 settling the dispute — **pinning the divisor does not pin the FORM.** Transit's T2 answers sub-question (b) "which N_e." It does not answer whether `s_CS/N_e` (with *any* of these N_e) is the §(iv-bis)-derived cap on the cocycle. The divisor-selection question and the numerator-justification question are orthogonal (L1). Even granting `N_e^acoustic = 2.9202` as canonical — which I am now willing to do for the divisor — the surrogate-vs-canonical status of the QUOTIENT is untouched.

**Net Re:T2.** Divisor narrowed to the acoustic class (2 of 5 ledger figures withdrawn by me; the `0.0282` reason corrected but elimination provisionally accepted). The 3-OOM spread is NOT a genuine divisor ambiguity within the correct physical class — I concede that framing. The remaining dispute is NOT "which N_e" (largely transit's) but "is the form §(iv-bis)-derived at all" (mine, L1), which T2 does not reach.

#### Re: T3 — Regime-II direction robustness

**CONCEDE — the direction is invariant. T3 is correct.** I verified the flip-threshold substitution chain independently this run (Sage):

```
α_req < α_lo  ⟺  α_req < s_CS/N_e  ⟺  N_e < s_CS/α_req = 0.01863337/0.00481354 = 3.87103449
```

The flip threshold is `N_e* = 3.871` (I get 3.87103449247, transit reports 3.871 — exact agreement). The largest ledger candidate is `N_e^acoustic = 2.9202 < 3.871`, so EVERY ledger N_e gives `α_req < α_lo` → Regime-II direction, unconditionally. All five candidates return "req below floor" with margins {+0.122, +0.154, +1.349, +2.138, +3.016} OOM — all positive, all FAIL-direction. **My own Reading-2 enumeration was a spread of FAIL margins all on the same side; transit is right that this proves direction-robustness, not direction-ambiguity.** I withdraw any implication that the N_e spread could reach Regime I.

I also confirm transit's structural-protection argument: the c-ratio needed to flip is `exp(2·3.871) = 2303×` (Sage this run: 2303.23), against the substrate's `c_fabric/c_Gold = 229×`. To reach the flip threshold the sound hierarchy would have to be ~10× larger than it is. And the S55 8D recomputation SHRINKS the sound term (`N_e^sound: 2.7179 → 0.9060`, knowledge base `s55_blv_8d_output.txt`), moving higher-dimensional budgets AWAY from the threshold. The Regime-II direction is structurally protected by the size of the sound-speed hierarchy. **No substrate-derived acoustic e-fold count at τ_fold exceeds 3.871. Transit's Q1 (T3) answer: none exists. Conceded.**

**CONCEDE — my `N_e^acoustic_only = 0.0282` alternative makes the FAIL STRONGER, not weaker.** Transit's Q2 (T3) is correct and I should state it plainly: substituting `0.0282` gives a +2.138-OOM (137×) margin, which is a CANONICAL-grade FAIL (in the ~200× structural-failure neighborhood), not a proxy edge. My alternative N_e cannot rescue Regime I — it can only sharpen Regime II. The N_e-ambiguity argument, in EVERY direction it could move, lands on a FAIL. This is the deep point transit makes and I accept it: the N_e axis touches only the FAIL's GRADE (proxy vs canonical), never its SIGN.

**WHERE THE DISPUTE SURVIVES (the localization).** Transit's Q3 (T3) asks whether I agree "the verdict (INFO, Regime-II-favoring, not-canonical) is invariant to the acoustic-class N_e choice, so the floor-derivation question does NOT reopen the w8-7 verdict." I agree the **VERDICT is invariant** and I do NOT seek to reopen w8-7's INFO/non-promotion. But "the verdict is invariant" and "the floor is a registry-eligible substrate canonical" are different propositions:

1. **Direction (sign) is over-determined** — every N_e, FAIL-direction. **CONCEDED to transit.** This is real, robust, and informative: it says the substrate's tight spectrum, divided over *any* plausible reduction depth, sits below the LQG demand. As a *qualitative* substrate fact this survives even my §(iv-bis) objection — because it survives without `α_lo` being a canonical at all (it is a statement about `s_CS` being small and `α_req` being smaller still, robust across divisors).

2. **The floor's status as a SUBSTRATE CANONICAL** (registry-eligible §(iv-bis)-compliant bound, a number one could pin and cite) — **NOT settled by (1).** Direction-invariance is a property of the inequality `s_CS/N_e > α_req`; it is silent on whether `s_CS/N_e` is the right LHS to compare against the cocycle's `α_bridge`. A surrogate can have a robustly-signed direction AND be uninformative on the canonical — these are the two clauses §(iv-bis) explicitly separates (corpus §11 (ii) sign-locked vs (iii) informative-on-canonical).

So my axis is NOT "the direction might flip" (it does not — conceded) and NOT "a different N_e gives Regime I" (it does not — conceded). My axis is: **`α_win_lo = 6.381e-3` should NOT be registered as a substrate-derived FLOOR (a canonical bound on α_bridge), because the FORM `s_CS/N_e` is a §(iv-bis) surrogate whose sign-lock (≥0) diverges from the signed cocycle, and whose value carries an N_e degree-of-freedom the cocycle lacks.** The Regime-II *lean* is real and prescription-independent at the qualitative level (transit wins that); the floor as a *registry-eligible canonical number* is not (I hold that). These are compatible — see Re:T4 and L1.

#### Re: T4 — prescription-independence

**AGREE (A) — the two senses of "prescription-independent" must be kept separate.** Transit's distinction is correct and important: (LQG-state-counting) the substrate floor does not depend on DL/GM/U(1)-ABCK — TRUE, `s_CS` and `N_e` are pure substrate quantities; the band ambiguity lives entirely on the LQG-IN side. And (N_e-selection) the floor is direction-independent but magnitude-`1/N_e`-dependent. I affirm both. The magnitude FAIL is genuinely LQG-prescription-independent — no DL/GM/U(1) choice moves `α_win_lo`. This is a real and clean finding.

**PARTIAL on (B) — the Bogoliubov corroboration is weaker than T4 implies.** Transit's Q3 (T4) asks whether W8-6 (`R_BG = 1/cosh(2r) = 6.84e-4`, covariance residual exactly 0) counts as corroborating that the compression bound is transit-dynamics-grounded, not numerological. My answer: **W8-6 corroborates the pre/post RELATIVE structure, but NOT the s_CS-as-α_bridge-cap mapping.** W8-6 is a genuine PASS and a beautiful result — `α_bridge^post = W_BG·α_bridge^pre`, `W_BG = cosh(2r) = 1462.30`, the squeeze-amplification of a quadratic surface form, sign derived unconditionally (verified: `W_BG = |u|²+|v|² ≥ 1` alignment-independent). But note what W8-6 pins and what it does not:

- W8-6 pins the RATIO `α_bridge^pre/α_bridge^post` (a relative, dimensionless constraint between two unknowns).
- W8-6 does NOT pin the ABSOLUTE `α_bridge^post` — the W8-6 working paper itself states this explicitly (§W8-6 "the MAGNITUDE of `α_bridge^post` itself still awaits the explicit Ŝ algebra (Workshop 6, W8-7); what is pinned HERE is the RELATIVE pre/post constraint").
- The compression bound `α_lo = s_CS/N_e` is an ABSOLUTE floor on `α_bridge`. W8-6 says nothing about absolute floors.

So W8-6 corroborates that the pre/post structure is a real SU(1,1) squeeze object (which I never disputed). It is INDEPENDENT of the moment-ratio proxy (transit's own framing: "independent of the moment-ratio proxy") — which cuts both ways. Being independent of the proxy, W8-6 cannot corroborate the proxy's *mapping* to `α_bridge`. It corroborates a *different* (relative) object. Two transit-dynamics objects both being real does not make one a derivation of the other's absolute scale. **I grant W8-6 corroborates the squeeze structure; I deny it corroborates `s_CS/N_e` as the absolute cap.**

**AGREE (C) — the Level-1/Level-3 axis separation is valid, AND it is the resolution.** Transit's Q1 (T4): can a bound be substrate-DERIVED (Level-1) AND a PROXY-at-extraction (Level-3, non-promotable)? **Yes — I accept the axis separation.** But I apply it to reach a *different* placement than transit. Transit places the dispute as: Level-1 derivation = SETTLED (Reading-1 wins), Level-3 extraction = the only proxy gap (w8-7 settled non-promotion). I place it as:

- **Level-1 (regulator-invariant cohomology-class identity):** the CANONICAL object is the cocycle `[S_exit-horizon]^♯`, a signed index-type pairing. `s_CS/N_e` is NOT this object — it is a non-negative dispersion statistic over an e-fold count. So at Level-1, `s_CS/N_e` is not the cohomology-class identity; it is a surrogate FOR it.
- **Level-3 (empirical anchor at canonical L_max):** `α_win_lo = 6.381e-3` is a Level-3 NUMBER. Per the §(iv-bis) discipline, a Level-3 surrogate-FAIL promotes to a canonical FAIL only if the algebraic-distance theorem shows the surrogate's sign/magnitude is not mechanically locked in a way diverging from the canonical. It IS so locked (sign ≥0 vs signed cocycle). So **non-promotion is forced by §(iv-bis), exactly as w8-7 found — but the REASON is the sign-lock divergence, not merely "the margin is only 0.12 OOM."**

This matters: transit's framing makes the non-promotion contingent on the small margin (0.12 OOM), implying that a LARGER margin (e.g. the 2.138 OOM from `N_e^acoustic_only`) WOULD promote to canonical. **It would not** — a sign-locked surrogate is non-promotable at *any* margin, because the §(iv-bis) obstruction is the sign-lock divergence, not the margin size. The margin governs whether the surrogate and canonical *happen to agree numerically*; the sign-lock governs whether a surrogate-FAIL is *informative* on a canonical-FAIL. These are different.

**Net Re:T4.** I accept the two-senses distinction (A), the Level-1/Level-3 axis (C-structure), and that the magnitude FAIL is LQG-prescription-independent. I deny that W8-6 corroborates the absolute cap (B), and I relocate the §(iv-bis) obstruction from "small margin" to "sign-lock divergence" (C-placement). The structural verdict I will carry: **the Regime-II LEAN is real and prescription-independent (transit wins); `α_win_lo` is NOT a registry-eligible substrate canonical floor (I win); the resolution is to register the QUALITATIVE Regime-II lean as the finding and explicitly NOT register `6.381e-3` as a substrate-derived bound — pending the CF-S94 cocycle, which IS the Level-1 canonical.** This threads both readings without either claiming the floor is numerology (it is motivated) or claiming it is canonical (it is a sign-locked surrogate).

### Part 2: Original Analysis

#### L1: The §(iv-bis) surrogate-vs-canonical discipline — is the s_CS/N_e form a legitimate substrate canonical or an uninformative surrogate (algebraic-distance theorem)?

This is my axis. I state the §(iv-bis) algebraic-distance theorem for `α_win_lo` formally, following the three sub-tests of `substrate-first-canonical-sourcing.md §(iv-bis)` (canonical worked example: `R_surrogate = 2f − 1`, pru-class-corpus §11).

**The canonical object the surrogate stands in for.** Per the W8-7 5-anatomy block and the `lqg-narrow-path-bridge-class.md` registry entry (REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION), the Level-1 canonical is the Reading-(b) Hochschild cocycle
```
α_bridge^canonical = ⟨[S_exit-horizon]^♯, Ch(P_0(τ~0.16))⟩,   [S_exit-horizon]^♯ ∈ HH^•(A_K)
```
a pairing of substrate mode classes against the exit-horizon 2-surface's Chern character. It is index-type: signed, localized to the 2-surface, regulator-invariant at the cohomology-class layer (HKR + Cheeger-Simons suffix). This is the object `α_win_lo` is a Level-3 surrogate for.

**§(iv-bis) sub-test (i) — substitution chain.** Reduce the surrogate to its substrate-physics components (Sage this run):
```
α_win_lo = s_CS / N_e = [Var(|λ|)/⟨|λ|⟩²] / N_e^acoustic
         = [ (F_0 F_2 / F_1²) − 1 ] / N_e
```
This is a TWO-FACTOR object: a global spectral-moment dispersion statistic `s_CS` (carries eigenvalues `λ_k` via `F_1, F_2`) divided by an acoustic e-fold count `N_e` (a transit-dynamics input NOT in `{λ_k, m_k}`). Contrast the canonical surrogate `R_surr = 2f − 1`, which is a ONE-FACTOR object (a single Peter-Weyl combinatorial fraction `f`).

**§(iv-bis) sub-test (ii) — is sign/magnitude mechanically locked, and does the lock DIVERGE from the canonical?** This is the decisive test. Two locks, of opposite character:

| Property | Surrogate `α_win_lo = s_CS/N_e` | Canonical cocycle `⟨[φ],Ch(P_0)⟩` |
|:---------|:--------------------------------|:----------------------------------|
| Sign | **LOCKED ≥ 0** — `s_CS ≥ 0` by Cauchy-Schwarz (S62 #18, Gaussian-saturating, structural); `N_e > 0`. So `α_win_lo > 0` unconditionally. | **NOT LOCKED** — index-type pairing; can be `+`, `−`, or `0` depending on the 2-surface geometry and `Ch(P_0)`. |
| What sets magnitude | Global spectral variance ÷ e-fold count; the variance is data-driven (not combinatorial), the e-fold count is a CHOICE the canonical does not contain. | The localized 2-surface index pairing; no free e-fold divisor — the propagation depth is implicit in the cohomological geometry. |

The surrogate's sign is mechanically locked non-negative; the canonical's sign is free. **This is the §(iv-bis) divergence signature.** In the canonical worked example, the obstruction was `sign(R_surr) = +1 iff f > 1/2` — a sign mechanically locked to a combinatorial fraction, diverging from the cocycle's geometry-set sign. Here the lock is structurally *stronger*: `α_win_lo` cannot even change sign (it is a positive magnitude), whereas the cocycle it stands in for is a signed object. A surrogate that is structurally sign-definite cannot be informative on the sign of a structurally-signed canonical.

**§(iv-bis) sub-test (iii) — is the surrogate-FAIL informative on the canonical PASS/FAIL?** NO, for two independent reasons:

1. **Sign-lock divergence (from ii).** The surrogate's `> 0` is structural, not a substrate prediction; the canonical's sign is the physics. A surrogate-FAIL ("`α_req` below `α_win_lo`") is a statement about a positive-definite dispersion budget; it does not transfer to a statement about a signed cohomological pairing. Per §(iv-bis), a surrogate whose sign is mechanically locked in a way diverging from the canonical is **uninformative on the canonical** — its FAIL does NOT falsify the canonical.

2. **The N_e degree of freedom.** The surrogate's VALUE depends on `N_e`, which the canonical does NOT contain. The cocycle delivers `α_bridge` with the propagation depth implicit in its geometry; the surrogate makes that depth an explicit, separately-chosen divisor. So the surrogate carries a free parameter absent from the canonical — and the W8-3 choice of `N_e = 2.9202` is one selection. (Transit's T2 pins this selection well, and I now grant the acoustic-class choice for the divisor — but pinning the divisor does not remove the structural fact that the FORM has a divisor the canonical lacks.)

**The algebraic-distance theorem (the L1 deliverable).**
> `α_win_lo = s_CS/N_e` is a GEOMETRIC-type surrogate (a positive-definite spectral-variance-over-e-fold-count statistic), NOT a cohomology-class observable. Its sign is mechanically locked non-negative; the cocycle `⟨[S_exit-horizon]^♯, Ch(P_0)⟩` it stands in for is index-type signed. By `substrate-first-canonical-sourcing.md §(iv-bis)` sub-test (iii), a surrogate whose sign is mechanically locked in a way diverging from the canonical is uninformative on the canonical's PASS/FAIL. Therefore `α_win_lo` is **NOT a registry-eligible substrate-derived floor**; the surrogate-FAIL is NOT promotable to a canonical FAIL at ANY margin (not merely at 0.12 OOM). The margin governs numerical agreement; the sign-lock governs informativeness.

**Verdict on the L1 question: α_win_lo is an under-justified surrogate for registry purposes — NOT an uninformative numerology, but a motivated GEOMETRIC proxy that is structurally not the cohomology-class canonical.** It is admissible as a heuristic Regime-II *indicator* (its qualitative lean is robust, T3); it is NOT admissible as a *canonical floor on α_bridge* (registry-eligible Level-3 anchor). The honest registry status: register the Regime-II lean as prescription-independent qualitative evidence; do NOT pin `6.381e-3` as a substrate-derived bound; the Level-1 canonical IS the CF-S94 cocycle, against which the Regime verdict is finally extracted.

**Where this AGREES with transit's T4 part (3).** Transit's own structural verdict part (3) says the floor is "a PROXY for the explicit cocycle, so the 0.12-OOM FAIL is NOT promotable to canonical FAIL." We CONVERGE on non-promotion. We DIVERGE on the reason (transit: small margin + Level-3 extraction gap; me: sign-lock divergence, margin-independent) and on the registry consequence (transit: "substrate-derived Regime-II-favoring floor at the proxy level"; me: not a registry-eligible floor at all, register the lean not the number). The R2/R3 convergence target is whether "substrate-derived floor (transit)" vs "GEOMETRIC surrogate, not a canonical floor (me)" is the correct registry tag — that is the genuine open seam, and it is on my axis, untouched by T2/T3.

#### L2: The Cauchy-Schwarz spectral-moment slack s_CS — what does it legitimately cap, and does the N_e-selection ambiguity defeat prescription-independence?

**What s_CS legitimately caps (the rigorous part).** `s_CS = F_0F_2/F_1² − 1 = Var(|λ|)/⟨|λ|⟩²` is the fractional variance of the PW-weighted D_K spectrum about its weighted mean. By my S62 #18 Cauchy-Schwarz bound (Gaussian-saturating), it is the EXACT and minimal slack: `s_CS = 0` iff the spectrum is monochromatic. It legitimately caps **anything that is itself a fractional second-moment functional of the SAME weighted spectrum** — e.g. the fractional spread of a derived heat-kernel coefficient ratio built from `{F_p}`, or the maximal fractional deviation a mean-field spectral average can sustain. This is its home: it bounds *intrinsic spectral dispersion phenomena*. Within that class, `s_CS = 0.0186` is a tight, regulator-invariant, L_max-stable substrate fact (verified 0.01863 at L=12, 0.01920 at L=10).

**What s_CS does NOT legitimately cap (the surrogate overreach).** It does not, *by the Cauchy-Schwarz inequality alone*, cap a 2-surface-localized cohomological pairing. `α_bridge` is not a fractional second moment of `{F_p}` — it is `⟨[S_exit-horizon]^♯, Ch(P_0)⟩`, a pairing of mode CLASSES against a localized 2-surface index. The step "`s_CS` is the total compression budget for `α_bridge`" (T1 move (i)) is an EXTRA-CAUCHY-SCHWARZ assumption: it asserts that the global spectral variance equals (or bounds) the localized cocycle pairing. That equality is not in S62 #18, not in W8-3, and not in T1's derivation — it is the surrogate identification. Cauchy-Schwarz gives `s_CS ≥ 0`; it does NOT give `s_CS ≥ (cocycle pairing)`. The slack legitimately caps spectral dispersion; it is *applied* to cap an area coefficient by an un-derived mapping.

**Does the N_e-selection (or its resolution) defeat or PRESERVE prescription-independence?** This is the precise version of the workshop's deepest question, and the answer cuts BOTH agents' way:

1. **PRESERVES LQG-prescription-independence (transit's sense).** Resolving N_e to the acoustic class does not introduce any LQG state-counting choice — `s_CS` and `N_e` are pure substrate quantities. The DL/GM/U(1)-ABCK band ambiguity lives entirely on the LQG-IN side and never touches `α_win_lo`. So in transit's sense (independence from LQG state-counting), prescription-independence is PRESERVED — I affirm this fully, and it is a genuine, clean property of the substrate magnitude leg.

2. **Does NOT establish §(iv-bis)-canonical-independence (my sense).** "Prescription-independent" in the LQG sense is necessary but not sufficient for "registry-eligible substrate canonical." A quantity can be LQG-prescription-independent (built only from substrate `s_CS`, `N_e`) AND still be a §(iv-bis) surrogate (a GEOMETRIC dispersion-over-e-fold statistic, not the cohomology-class cocycle). The N_e-resolution removes the *divisor*-ambiguity; it does not remove the *form*-surrogacy. So the N_e-selection resolution PRESERVES transit's prescription-independence WITHOUT defeating OR establishing my surrogate charge — the two are on orthogonal axes (the LQG-state-counting axis and the §(iv-bis) substrate-canonical-vs-surrogate axis).

**The orthogonality, stated as a 2×2 (the L2 structural deliverable):**

| | LQG-prescription-DEPENDENT | LQG-prescription-INDEPENDENT |
|:--|:--|:--|
| **§(iv-bis)-canonical** | (the PART C band, if DL declared) | the CF-S94 cocycle `[S_exit-horizon]^♯` |
| **§(iv-bis)-surrogate** | — | **`α_win_lo = s_CS/N_e` ← HERE** |

`α_win_lo` sits in the (prescription-INDEPENDENT, surrogate) cell. Transit's T2/T3/T4(A) correctly establish the COLUMN (prescription-independent). My L1/L2 establish the ROW (surrogate). Both are true simultaneously; they place `α_win_lo` in one cell. The dispute is whether the registry tag reads "substrate-derived floor" (which would require the canonical row) or "GEOMETRIC surrogate / Regime-II indicator" (the surrogate row). The cell membership — prescription-independent surrogate — is what we should converge on.

**Consequence for the W8-3 headline.** W8-3's "magnitude FAIL, prescription-INDEPENDENT, Regime-II-favoring" is correct *as a column statement* (prescription-independent qualitative lean). It is NOT licensed to read as "substrate-derived floor at `6.381e-3`" — that would assert the canonical row, which the §(iv-bis) algebraic-distance theorem (L1) denies. The headline survives as a Regime-II *lean*; the specific number `α_win_lo = 6.381e-3` should carry a surrogate tag, not a canonical-floor tag, when it enters any registry or downstream gate. This is consistent with W8-7's own `band-edge-convention-ambiguous` deferred-pending landing and with the registry holding `lqg-narrow-path-bridge-class.md` at REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION — the Level-3 anchor is the cocycle, not the surrogate.

#### L3: Questions for transit

I have conceded T3 (direction invariant), conceded two of three T2 eliminations (geom, phys), and granted the acoustic-class divisor. My questions target the ONE seam that survives — the FORM `s_CS/N_e` as a §(iv-bis) canonical vs surrogate — which T2/T3 do not reach.

**Q1 (the load-bearing one — numerator mapping, not divisor).** You established `s_CS = Var(|λ|)/⟨|λ|⟩²` is exact and `N_e^acoustic` is the right divisor. Neither establishes that `s_CS` *caps the cocycle's α_bridge*. Cauchy-Schwarz gives `s_CS ≥ 0`; it does NOT give `s_CS ≥ ⟨[S_exit-horizon]^♯, Ch(P_0)⟩`. **Can you exhibit the step — from S62 #18 or any substrate identity — that bounds the localized 2-surface cocycle pairing by the global spectral variance?** If the mapping is "the bulk-to-surface reduction redshifts the bulk coefficient over `N_e` e-folds," show that the bulk coefficient at the START of the reduction IS `s_CS` (not merely "O(1) dispersion"), so that the quotient is a derived bound and not a dimensional-analysis ansatz. Absent that step, `s_CS/N_e` is a GEOMETRIC surrogate (L1), and the floor is a heuristic indicator, not a canonical bound.

**Q2 (sign-lock divergence, margin-independence).** Do you accept that the §(iv-bis) obstruction to promotion is the **sign-lock divergence** (`α_win_lo > 0` structurally, vs the cocycle `⟨[φ],Ch(P_0)⟩` being SIGNED), NOT the 0.12-OOM margin? The test: under your own T3, `N_e^acoustic_only = 0.0282` gives a +2.138-OOM margin. If non-promotion were margin-driven, that 137× FAIL WOULD promote to canonical. But a sign-locked surrogate is non-promotable at ANY margin. Which is it — does a large-margin surrogate-FAIL promote to canonical (your T3 footnote implies yes: "would be promotable to canonical FAIL"), or is it non-promotable because of the sign-lock (my L1)? This is a sharp disagreement: your T3 says the `0.0282` FAIL "IS in the ~200× structural-failure neighborhood and WOULD be promotable"; my L1 says no sign-locked surrogate promotes regardless of margin. We cannot both be right.

**Q3 (registry tag — the convergence target).** Independent of the verdict (we both hold INFO/non-reopening of w8-7), what should the REGISTRY say about `α_win_lo = 6.381e-3`? Your T4(3) calls it "a substrate-derived Regime-II-favoring floor at the proxy level." I call it a GEOMETRIC surrogate that should NOT be pinned as a substrate-derived floor — register the Regime-II *lean* (prescription-independent, robust), not the *number* as a canonical bound. Concretely: if a future gate cites `α_win_lo`, does it cite it as (a) a substrate-derived floor on α_bridge, or (b) a heuristic Regime-II indicator pending the CF-S94 cocycle? I argue (b) is forced by §(iv-bis); (a) would be a Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk (`substrate-first-canonical-sourcing.md §(v)`) if the cocycle is the real canonical. Can we converge on tag (b) — "prescription-independent Regime-II indicator, NOT a registry-eligible floor" — as the honest landing, leaving the canonical floor to the CF-S94 cocycle extraction?

---

## Round 2 — transit: Follow-up

### CONVERGENCE

Round 1 closed three of the four sub-questions in my favor and one in lizzi's, and I accept that partition. Here is what I now own, what I correct, and what changed.

**(1) The T2 arithmetic error — OWNED and CORRECTED in-session (the load-bearing concession).**

lizzi caught a real error in Re:T2: I wrote that `N_e^acoustic_only = 0.0282` is "the budget with the sound term REMOVED," i.e. `N_e^geom + N_e^density`. That identification is WRONG by an order of magnitude — `0.1734 + 0.0289 = 0.2023 ≠ 0.0282` (lizzi's Sage; reconfirmed mine this run). My elimination CONCLUSION (that `0.0282` is not the bulk-to-surface acoustic divisor) survives, but a conclusion resting on a false arithmetic identity is under-justified, and lizzi is right to flag it at exactly the place I was most confident. I owe the correct identification, and I now give it — sourced from the knowledge base, not asserted.

**The correct reading of `N_e^acoustic_only = 0.0282` (knowledge base, `s53_phonon_eos_output.txt`, Sage this run).** The `0.0282` figure is co-listed in the SAME S53 phonon-EOS output as `N_e^GL_internal = 0.0282` with the explicit annotation "Including c_fabric → c_Gold transition (ONSET)." So:
```
N_e^acoustic_only = N_e^GL_internal = 0.0282      [s53_phonon_eos; the Ginzburg-Landau INTERNAL
                                                   order-parameter relaxation e-fold count]
```
and the same file's `N_e^full = 0.2015` decomposes (Sage this run, machine-ε):
```
N_e^full = N_e^acoustic_only + N_e^geom = 0.0282 + 0.1734 = 0.2016 ≈ 0.2015   [reldev 5.0e-4]
```
with the full 4D acoustic budget recovered by ADDING the sound-ONSET amplification:
```
N_e^acoustic = N_e^full + N_e^sound-onset = 0.2015 + 2.7179 = 2.9194 ≈ 2.9202  [reldev 2.7e-4].
```
So the **correct** physical reading: `N_e^acoustic_only = 0.0282` is the **GL internal order-parameter relaxation count** — the e-folds the internal `|S|²` order parameter relaxes through BEFORE the `c_fabric → c_Gold` sound-onset transition (the 229× amplification, `N_e^sound-onset = 2.7179`) multiplies in. It is NOT "geom+density with sound deleted" (that is `N_e^full = 0.2015`, a DIFFERENT figure). My T2 table row and elimination-reason are wrong as stated; the corrected elimination reason is: `0.0282` is the **pre-sound-onset internal-relaxation count**, and a bulk-to-surface ACOUSTIC reduction onto the exit-horizon 2-surface IS the sound-onset propagation (the `c_fabric → c_Gold` crossing), so the divisor must INCLUDE the 2.7179 onset term — which `0.0282` excludes by construction. The category-elimination stands; the arithmetic that supported it is replaced. **CONVERGENCE on the correction: `N_e^acoustic_only = 0.0282 = N_e^GL_internal`, pre-sound-onset; eliminated as the divisor because the acoustic reduction IS the sound-onset, which it omits.**

**(2) The §(iv-bis) surrogate framing for the FORM — ACCEPTED for the numerator-to-cocycle mapping.**

lizzi's L1/L2 sharpened her Reading-2 from the charge I pre-rebutted ("not derived") to a charge I did NOT rebut and now accept: the step `s_CS = ` [the total compression budget that caps `α_bridge`] is an EXTRA-Cauchy-Schwarz mapping. S62 #18 gives `s_CS ≥ 0`; it does NOT give `s_CS ≥ ⟨[S_exit-horizon]^♯, Ch(P_0)⟩`. My T1 move (i) supplied that inequality by the word "compression budget," and lizzi is correct that the word is doing un-derived work: the global spectral variance is not shown to bound the localized 2-surface cocycle pairing. I CONCEDE this is the genuine seam, and it is on her axis (the numerator-to-cocycle mapping), not mine (the divisor selection, which I did pin). My T4 part (2) "Reading-2's Level-1 charge FAILS" was too strong — it defeated the "not derived" charge but not the sharpened "surrogate-mapping" charge. The mapping `s_CS ↦ cap on α_bridge` is asserted, and the derivation of it is exactly what the CF-S94 cocycle supplies. **CONVERGENCE: the FORM `s_CS/N_e` is a §(iv-bis) GEOMETRIC-type surrogate for the numerator-to-cocycle mapping; the numerator is rigorous (exact variance), the divisor is pinned (acoustic class), but the QUOTIENT-as-cap-on-the-signed-cocycle is the surrogate step.**

**(3) The registry tag — ACCEPTED: "Regime-II indicator," NOT "registry-eligible floor."**

This is lizzi's L3 Q3 convergence target, and I accept tag (b). I will NOT carry into R3 my T4 part (3) phrasing "a substrate-derived Regime-II-favoring FLOOR at the proxy level" as a registry-eligible canonical number. The honest registry status is lizzi's: register the **Regime-II LEAN** as prescription-independent qualitative substrate evidence (robust across all ledger N_e, T3 — that part is mine and survives), and do NOT pin `α_win_lo = 6.381e-3` as a substrate-derived bound on `α_bridge`. The Level-1 canonical IS the CF-S94 `[S_exit-horizon]^♯` cocycle; `α_win_lo` is a heuristic indicator pending that extraction. Pinning `6.381e-3` as a floor would be a Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk (`substrate-first-canonical-sourcing.md §(v)`) once the cocycle is the real canonical — lizzi's L3 Q3 point, accepted.

**What CHANGED (numerical vs structural, per `output-standards.md` T1-13):**

- **(a) Numerical revision**: `N_e^acoustic_only = 0.0282` re-identified `0.2023 (geom+density, WRONG)` → `0.0282 = N_e^GL_internal (pre-sound-onset, CORRECT)`; co-decomposition `N_e^full = 0.0282 + 0.1734 = 0.2016` and `N_e^acoustic = 0.2015 + 2.7179 = 2.9194` both machine-ε (Sage this run).
- **(b) Structural change**: `α_win_lo` registry status: "substrate-derived floor (proxy-level)" → "prescription-independent Regime-II INDICATOR, NOT a registry-eligible floor" (tag (a) → tag (b) of L3 Q3). The epistemic TYPE of the headline number changes: from a candidate canonical bound to a qualitative-lean indicator. The Regime-II *direction* remains a robust substrate finding (T3, conceded by lizzi); the *number* loses canonical-floor status.

What I do NOT concede, and carry into DISSENT: the **margin-independence of the sign-lock obstruction** (lizzi's L3 Q2). That is the one irreducible disagreement R1 surfaced, and it is genuinely open.

### DISSENT

The crux is lizzi's L3 Q2, restated sharply: my T3 footnote says a large-margin surrogate-FAIL (e.g. the `N_e^GL_internal = 0.0282` substitution, +2.138 OOM = 137×) "WOULD be promotable to canonical FAIL"; lizzi's L1 says a sign-locked surrogate is non-promotable at ANY margin because the §(iv-bis) obstruction is the **sign-lock divergence**, not the margin size. We cannot both be right. I have re-examined this against the substrate physics, and my position has MOVED — but not all the way to lizzi's. Here is the new evidence and the precise residual disagreement.

**Where lizzi is right (and my T3 footnote was loose).** lizzi's §(iv-bis) reading is correct on its own terms: a surrogate whose sign is *mechanically* locked (here `α_win_lo > 0` by Cauchy-Schwarz + `N_e > 0`) cannot, by magnitude alone, become informative about the SIGN of a signed canonical. If the question the canonical answers is "what is `sign⟨[S_exit-horizon]^♯, Ch(P_0)⟩`," then NO value of a positive-definite surrogate — at any margin — resolves that sign. My T3 footnote conflated two distinct promotion targets: (A) promoting a surrogate-FAIL to "the canonical's SIGN is thus-and-so," and (B) promoting a surrogate-FAIL to "the canonical's MAGNITUDE clears/misses the LQG demand." lizzi's sign-lock argument decisively forbids (A) at any margin. **I CONCEDE (A): the sign-lock makes the surrogate uninformative on the cocycle's SIGN, margin-independently. My T3 footnote was wrong to imply otherwise.**

**Where I still hold (the residual disagreement — target (B), the MAGNITUDE comparison).** The substrate-physics content of the Regime question is NOT "what is the cocycle's sign." It is a MAGNITUDE comparison: does the realized `|α_bridge|` sit at or above the LQG demand `α_req = 4.81e-3` (Regime-I-permitting) or below it (Regime-II)? The cocycle `⟨[S_exit-horizon]^♯, Ch(P_0)⟩` delivers a signed number, but `α_bridge` as it enters `γ_emergent = α_bridge · 49.34` (W8-4) is the MAGNITUDE of the reduced area coefficient — `γ_emergent > 0` (an Immirzi parameter), so the comparison that decides the Regime is `|α_bridge|` vs `α_req`. My claim, sharpened: the surrogate `α_win_lo = s_CS/N_e` is a candidate LOWER BOUND on `|α_bridge|`, and for a lower-bound-on-a-magnitude, the margin IS structurally informative, in a way the sign-lock objection does not touch. The substitution chain:

```
Claim: "A large-margin surrogate-FAIL is informative on the MAGNITUDE comparison |α_bridge| vs α_req,
        even though it is uninformative on sign⟨[φ],Ch(P_0)⟩ (lizzi's sign-lock, conceded)."

Step 1 [Def]: the Regime verdict is decided by  |α_bridge| ≷ α_req,  α_req = 4.81354e-3  (FIXED, W8-4).
              [γ_emergent = |α_bridge|·49.34 is an Immirzi magnitude; the comparison is on |α_bridge|.]
Step 2 [Def]: α_win_lo = s_CS/N_e  is PROPOSED as a lower bound:  |α_bridge| ≥ α_win_lo   (the surrogate claim).
Step 3 [Subst]: IF the proposed bound holds, then  α_win_lo > α_req  ⇒  |α_bridge| ≥ α_win_lo > α_req
              ⇒  Regime-I FORBIDDEN (|α_bridge| cannot reach down to α_req).   [the FAIL = Regime-II]
Step 4 [Key step — does the bound hold?]: the bound  |α_bridge| ≥ s_CS/N_e  is the SURROGATE MAPPING
              lizzi contests (CONVERGENCE (2)). It is NOT established. So Step 3's implication is
              CONDITIONAL on a bound that is, at present, asserted not derived.
Step 5 [Read off]: IF the lower-bound mapping were established, THEN at margin 137× the surrogate
              would force |α_bridge| ≥ 0.66 ≫ α_req — a MAGNITUDE statement the sign-lock does NOT block
              (it bounds |α_bridge|, makes no claim about sign⟨[φ],Ch(P_0)⟩).
              BUT the mapping is the open question; so the margin is informative-IF-the-bound-holds,
              and uninformative-if-it-does-not. The sign-lock is a RED HERRING for target (B);
              the real obstruction is whether s_CS/N_e is a lower bound on |α_bridge| at all.
Conclusion: promotability of a large-margin surrogate-FAIL hinges on the LOWER-BOUND-mapping
            (CONVERGENCE (2)'s open seam), NOT on the sign-lock. Sign-lock kills target (A) at any
            margin (conceded); it does NOT kill target (B). For (B), margin IS informative — conditional
            on the bound. So "promotable at sufficient margin" and "non-promotable by sign-lock" are
            answering DIFFERENT questions; both are true on their own targets.
```

**The substrate-physics case for why the sign-lock does not settle target (B).** Consider lizzi's own §(iv-bis) canonical worked example, `R_surr = 2f − 1` (pru-class-corpus §11). There the surrogate's sign is locked to `f > 1/2` AND the surrogate is a *direct stand-in* for the cocycle's signed VALUE — so a surrogate-FAIL is a claim about the cocycle's sign, which the lock corrupts. That is the structure where sign-lock kills promotion. But `α_win_lo = s_CS/N_e` is NOT proposed as a stand-in for the cocycle's signed value; it is proposed (T1 move (i)) as a stand-in for a LOWER BOUND on the cocycle's MAGNITUDE. A positive-definite quantity is a perfectly admissible lower bound on a magnitude (`|α_bridge| ≥ 0` is true and sign-locked and not thereby uninformative — it is just weak). The question is whether `s_CS/N_e` is a *non-trivial* lower bound (tighter than the trivial `≥ 0`), and THAT is the CONVERGENCE-(2) mapping question — a magnitude/derivation question, not a sign question. So lizzi's sign-lock, while correct against target (A), is aimed at the wrong target for the Regime verdict, which lives at (B).

**The honest residual.** I am NOT claiming the 137× FAIL IS promotable. I am claiming lizzi's REASON for non-promotion (sign-lock, margin-independent) is the wrong reason for the Regime-relevant target (B). The CORRECT reason non-promotion currently holds is the one we converged on in (2): the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is not yet derived. IF CF-S94 establishes that mapping (shows `s_CS/N_e` is a genuine lower bound on `|α_bridge|`), THEN a large-margin FAIL WOULD promote on target (B) — and the sign-lock would still not block it, because target (B) never needed the sign. So my dissent is precise: **for the Regime-deciding magnitude comparison, the margin is informative conditional on the lower-bound mapping; the sign-lock obstruction lizzi invokes governs a different target (the cocycle's sign) that the Regime verdict does not turn on.** lizzi wins "sign-lock ⇒ non-promotable on sign at any margin" (target A, conceded); I hold "the Regime verdict is a magnitude comparison (target B), where margin IS informative-given-the-bound, and the sign-lock is orthogonal."

If lizzi can show in her final turn that the Regime verdict genuinely requires the cocycle's SIGN (not just `|α_bridge|` vs `α_req`) — e.g. that a NEGATIVE `⟨[φ],Ch(P_0)⟩` would itself flip the Regime classification, making target (A) the operative one — then her sign-lock argument captures the Regime question and I concede fully. My QUESTIONS Q3 asks exactly this.

### EMERGENCE

**The resolution lives in lizzi's 2×2 — and the cell membership is the convergence, but the 2×2 is INCOMPLETE on one axis, which is the new insight.**

lizzi's L2 2×2 places `α_win_lo` in the (LQG-prescription-INDEPENDENT, §(iv-bis)-surrogate) cell, and I accept that placement entirely: transit's T2/T3/T4(A) establish the COLUMN (prescription-independent — `s_CS` and `N_e` are pure substrate quantities, no DL/GM/U(1)-ABCK choice enters), lizzi's L1/L2 establish the ROW (surrogate — the numerator-to-cocycle mapping is not derived). Both true; one cell. So the registry tag converges: **`α_win_lo` is a prescription-independent Regime-II indicator, NOT a registry-eligible floor** (CONVERGENCE (3)).

The NEW insight: the surrogate ROW is not monolithic — it splits along the DISSENT axis into two sub-rows that the 2×2 collapses, and the split is exactly the target-(A)/target-(B) distinction. A §(iv-bis) surrogate can be:
- **(surrogate-for-the-signed-VALUE)** — a stand-in for `⟨[φ],Ch(P_0)⟩` itself (lizzi's `R_surr = 2f−1` canonical example). Sign-lock makes it non-promotable AT ANY MARGIN (target A). This is the sub-row lizzi's L1 analysis lives in.
- **(surrogate-for-a-MAGNITUDE-bound)** — a stand-in for a lower bound on `|α_bridge|` (my T1 move (i) intent). Here the relevant promotion is the magnitude comparison (target B); the sign-lock is orthogonal; promotability hinges on whether the bound is genuine (the CF-S94 mapping). This is the sub-row I claim `α_win_lo` actually inhabits.

So the structural emergence is a **refinement of §(iv-bis)**: the "sign-lock ⇒ non-promotable" clause (corpus §11 sub-test iii) is decisive for surrogate-for-the-signed-VALUE, but for surrogate-for-a-magnitude-BOUND the operative obstruction is bound-genuineness, not sign-lock. The two sub-rows have DIFFERENT promotion criteria. `α_win_lo`'s correct placement is (prescription-independent, surrogate-for-a-magnitude-bound), and its non-promotion follows from "bound not yet derived" (CONVERGENCE 2), NOT from "sign-locked" (lizzi's L1) — even though both verdicts agree on NON-promotion TODAY. This matters for CF-S94: if the cocycle extraction establishes the lower-bound mapping, the §(iv-bis) status of `α_win_lo` CHANGES (it becomes a derived bound), whereas under lizzi's sign-lock reading it would be permanently non-promotable regardless of CF-S94. The two readings make DIFFERENT predictions about what CF-S94 can do — and that is a falsifiable structural fork, not a semantic difference.

This also clarifies what CF-S94 must deliver to be a Level-1 canonical: not merely "compute the cocycle's number," but specifically establish whether `|α_bridge^cocycle| ≥ s_CS/N_e` (validating the surrogate as a genuine bound, target-B promotion) and independently report `sign⟨[φ],Ch(P_0)⟩` (the target-A content the surrogate can never supply). Both are CF-S94 deliverables; the workshop has now separated them.

### QUESTIONS

**Answers to lizzi's L3:**

**A1 (numerator mapping — can I exhibit the step bounding the cocycle by `s_CS`?).** No — and I CONCEDE I cannot, at present. S62 #18 gives `s_CS ≥ 0` and that `s_CS` is the exact fractional variance; it does NOT give `s_CS ≥ ⟨[S_exit-horizon]^♯, Ch(P_0)⟩` or `|α_bridge| ≥ s_CS/N_e`. The T1 move (i) step "the bulk coefficient at the start of the reduction IS `s_CS`" is an ansatz (dimensional/structural plausibility: a tight spectrum has little compression room, so its reduced coefficient is floored by its variance budget), NOT a derived identity. The derivation that the bulk coefficient entering the reduction equals `s_CS` (not merely "O(1) dispersion") is exactly the CF-S94 deliverable. So `s_CS/N_e` is, as you charge, a GEOMETRIC surrogate at the mapping level. Conceded.

**A2 (sign-lock divergence vs margin — the crux).** Partial concession with a sharpened counter (full argument in DISSENT). I CONCEDE the sign-lock makes the surrogate uninformative on the cocycle's SIGN at any margin (target A) — my T3 footnote was loose. I HOLD that the Regime verdict is a MAGNITUDE comparison `|α_bridge|` vs `α_req` (target B), where the sign-lock is orthogonal and the margin IS informative — conditional on the lower-bound mapping (A1's open seam). So we are BOTH right on different targets: you on target (A) [sign, margin-independent non-promotion]; me on target (B) [magnitude, margin-informative-given-the-bound]. The non-promotion we agree on today is forced by A1's underived bound, NOT by the sign-lock — that is the precise relocation of the obstruction.

**A3 (registry tag — the convergence target).** CONVERGED on tag (b). A future gate citing `α_win_lo` MUST cite it as a heuristic Regime-II indicator pending the CF-S94 cocycle, NOT as a substrate-derived floor on `α_bridge`. Citing it as (a) would be the Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk you name (`substrate-first-canonical-sourcing.md §(v)`). The registry should record: "Regime-II lean prescription-independent and robust across all ledger N_e (T3); `α_win_lo = 6.381e-3` is a GEOMETRIC surrogate / Regime-II indicator, NOT a registry-eligible floor; Level-1 canonical = CF-S94 `[S_exit-horizon]^♯` cocycle." Tag (b), accepted.

**My sharpest follow-up for your final turn:**

**transit-Q (R2 → your R3, the operative target for the Regime verdict).** You established the sign-lock forbids promotion on the cocycle's SIGN at any margin (target A). I accept that. But the Regime classification, as it enters the framework, is decided by `γ_emergent = |α_bridge|·49.34` vs the LQG Immirzi demand — a MAGNITUDE comparison (target B), since `γ_emergent` is a positive Immirzi parameter. **Does the Regime verdict genuinely turn on `sign⟨[S_exit-horizon]^♯, Ch(P_0)⟩`, or only on `|α_bridge|` vs `α_req`?** Concretely: would a NEGATIVE cocycle `⟨[φ],Ch(P_0)⟩ < 0` change the Regime classification (Regime-I vs II), or does the Regime depend only on the magnitude `|⟨[φ],Ch(P_0)⟩|` reduced onto the 2-surface? If the Regime turns only on the magnitude (target B), then your sign-lock argument — though correct on target A — is orthogonal to the Regime verdict, and the operative obstruction is A1's underived lower-bound mapping (which CF-S94 could discharge, changing `α_win_lo`'s §(iv-bis) status). If instead a negative cocycle WOULD flip the Regime (target A is operative), then your sign-lock captures the Regime question and I concede fully: `α_win_lo` is permanently non-promotable regardless of CF-S94. This is the EMERGENCE fork — which target the Regime verdict lives at — and your spectral-functional read of how `⟨[φ],Ch(P_0)⟩` enters `γ_emergent` is the deciding input. Please pin it for R3.

---

## Round 2 — lizzi: Cross-Synthesis

### CONVERGENCE

transit's R2 closed almost everything I asked for in R1, and his concessions are clean. I accept all three, and I OWE him one adjudication in return — the target-A/target-B split is on my axis (§(iv-bis)), and I rule on it here.

**(1) The T2 arithmetic correction — ACCEPTED, and the corrected reading is right.** transit's R2 CONVERGENCE (1) owns the `0.2023 ≠ 0.0282` error I flagged in Re:T2 and replaces it with the knowledge-base reading `N_e^acoustic_only = 0.0282 = N_e^GL_internal` (the Ginzburg-Landau internal order-parameter relaxation count, pre-sound-onset; `s53_phonon_eos_output.txt`). I re-verified his co-decomposition this run (Sage, machine-ε): `N_e^full = 0.0282 + 0.1734 = 0.2016 ≈ 0.2015` and `N_e^acoustic = 0.2015 + 2.7179 = 2.9194 ≈ 2.9202`. The corrected elimination reason — `0.0282` excludes the `c_fabric→c_Gold` sound-onset (the 229× amplification, `N_e^sound = 2.7179`), and a bulk-to-surface ACOUSTIC reduction onto the exit-horizon 2-surface IS that sound-onset crossing — is sound, and it now rests on a correct identity rather than a wrong one. My Re:T2 flag is discharged: the elimination of `0.0282` is no longer a category-argument propped by a broken arithmetic step. **CONVERGENCE.** The divisor selection is fully pinned to the acoustic class `{N_e^sound = 2.7179, N_e^acoustic = 2.9202}`, floor robust to 0.03 OOM.

**(2) tag (b) — ACCEPTED, and it is the registry landing.** transit's R2 CONVERGENCE (3) accepts my L3 Q3 tag (b): `α_win_lo = 6.381e-3` is registered as a **prescription-independent Regime-II indicator, NOT a registry-eligible substrate-derived floor**. The Regime-II LEAN (robust across all five ledger N_e, T3) is the finding that lands; the *number* does not land as a canonical bound on `α_bridge`. We agree pinning `6.381e-3` as a floor would be the Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk (`substrate-first-canonical-sourcing.md §(v)`) once the CF-S94 cocycle is the real Level-1 canonical. **CONVERGENCE on the registry tag — this is now joint, not contested.**

**(3) The target-A sign-lock concession — ACCEPTED.** transit's R2 DISSENT concedes target A fully: a sign-locked surrogate (`α_win_lo > 0` by Cauchy-Schwarz + `N_e > 0`) cannot, at ANY margin, become informative about the SIGN of a signed canonical `⟨[φ],Ch(P_0)⟩`. His T3 footnote ("the 137× FAIL WOULD be promotable") was loose, and he withdraws it on target A. I accept the concession as stated; my L1 sign-lock argument is correct *on target A*. **CONVERGENCE on target A.**

**The adjudication I owe (target-A / target-B split — is it a legitimate §(iv-bis) refinement, or does the obstruction apply to BOTH targets?).** transit's R2 EMERGENCE splits my surrogate ROW into two sub-rows — surrogate-for-the-signed-VALUE (target A, sign-lock decisive) vs surrogate-for-a-MAGNITUDE-bound (target B, bound-genuineness decisive) — and claims `α_win_lo` inhabits the latter, where the sign-lock is orthogonal. **I rule: the SUB-SPLIT is a legitimate and useful refinement of the §(iv-bis) surrogate taxonomy — I adopt it. But transit's INFERENCE from it (that the target-B sub-row escapes the §(iv-bis) obstruction the target-A sub-row carries) is WRONG. The §(iv-bis) algebraic-distance obstruction applies to BOTH sub-rows; it merely changes WHICH sub-test fires.** Here is the adjudication, from the canonical §11 precedent:

- The canonical §(iv-bis) instance (`R_surr = 2f − 1`, pru-class-corpus §11) is, per the corpus text itself (line 410), "**NOT informative as a falsifier of the canonical Connes-Karoubi pairing because the surrogate-canonical algebraic distance does not bound the canonical's sign OR magnitude**." Read that carefully: the precedent obstruction is a **sign-AND-magnitude** obstruction, not a sign-only one. transit's EMERGENCE treats §(iv-bis)'s decisive clause as "sign-lock ⇒ non-promotable" (sub-test ii), and reasons that a magnitude-bound surrogate, having no sign claim, escapes it. But §11's forward-enforcement clause (line 412) is broader: a surrogate "mechanically locked to a substrate-distance-N spectral-moment combinatorial fraction" is "**uninformative on the canonical** AND a separate canonical-evaluation gate is required" — and the worked example demonstrates uninformativeness on the MAGNITUDE too, not only the sign.

- So the §(iv-bis) discipline already adjudicates magnitude-bound surrogates. The test for a magnitude-bound surrogate is sub-test (i) [the substitution chain that establishes the bound] + sub-test (iii) [is the surrogate-FAIL informative on the canonical's magnitude]. For `α_win_lo` as a *lower bound on* `|α_bridge|`, sub-test (i) asks: is `|α_bridge| ≥ s_CS/N_e` a derived inequality? transit's own R2 QUESTIONS A1 answers NO — S62 #18 gives `s_CS ≥ 0` and nothing more; the step "the bulk coefficient entering the reduction IS `s_CS`" is "an ansatz, NOT a derived identity." A magnitude-bound surrogate whose bounding step is undischarged FAILS sub-test (i), hence is uninformative-on-the-canonical's-magnitude per sub-test (iii) — **exactly the same §(iv-bis) verdict as the target-A sign-lock, reached by a different sub-test.**

**Net.** I CONVERGE with transit that the surrogate ROW splits into target-A and target-B sub-rows (his refinement is correct and I adopt it). I DISSENT from his claim that the target-B sub-row is §(iv-bis)-exempt: a magnitude-BOUND surrogate is equally non-promotable when its bounding step is underived — the obstruction migrates from sub-test (ii) (sign-lock) to sub-test (i) (underived-bound), but it does not vanish. The single §(iv-bis) verdict — "uninformative on the canonical, separate canonical-evaluation gate required" — covers both sub-rows. This is the precise adjudication his R2 EMERGENCE requested, and it reframes the residual disagreement (DISSENT below): we converge on NON-promotion via two §(iv-bis) sub-tests, and his "falsifiable structural fork" survives but resolves on the target-B reading, not on a "permanently non-promotable" reading.

### DISSENT

transit's R2 QUESTIONS pins the R3-closure crux on me: **does the Regime verdict turn on `sign⟨[S_exit-horizon]^♯, Ch(P_0)⟩` (target A — then my sign-lock captures it and he concedes fully) or only on `|α_bridge|` vs `α_req` (target B — then the operative obstruction is the underived lower-bound, which CF-S94 could discharge)?** I answer it directly, and the answer is NOT the binary transit framed. I CONCEDE the Regime verdict inhabits target B — but target B is non-promotable for an §(iv-bis) reason that is NOT the sign-lock, and that he has already conceded he cannot discharge. The dissent narrows to one sharp point.

**Answer to the crux — target B, conceded.** I checked how `α_bridge` enters the Regime classification, and transit is right on the substrate physics. W8-3 PART B (working-paper line 214, substitution-chain Step 2 line 237): `γ_emergent = α_bridge · 49.34`, and the Regime is decided by setting `γ_emergent = γ_BH = 0.2375`, a positive Immirzi parameter. The registry Regime bands (`lqg-narrow-path-bridge-class.md` lines 41-43) are magnitude bands: Regime I `α_bridge ∈ [10⁻³·³, 10⁻²·⁷]`, Regime II `α_bridge ∈ [10⁻¹, 10¹]` with `γ_emergent ∼ 50`. The classification is set by where `|α_bridge|` lands relative to `α_req = 4.81e-3`. So:

> **The Regime verdict is a MAGNITUDE comparison `|α_bridge|` vs `α_req` (target B). It does NOT turn on `sign⟨[φ],Ch(P_0)⟩`.** A negative cocycle would not flip the Regime classification — `γ_emergent = |α_bridge|·49.34` is read as a positive Immirzi magnitude regardless of the cocycle's sign. **I concede target A is NOT the operative target.** My R1 sign-lock argument is correct (transit conceded it in his R2 DISSENT), but it adjudicates a question — the cocycle's sign — that the Regime verdict does not ask. transit wins this: the Regime lives at target B.

So I do NOT carry into R3 the claim "the sign-lock captures the Regime question." It does not. Where I diverge from transit is the SECOND half of his binary — his claim that, because target B is the operative target, "the operative obstruction is the underived lower-bound, which CF-S94 could discharge," and therefore `α_win_lo`'s non-promotion is a CONTINGENT (CF-S94-removable) status rather than a structural §(iv-bis) verdict. That second half is where the dissent survives.

**The dissent (the lower-bound mapping is itself an §(iv-bis) surrogate-FAIL — target B is non-promotable TODAY, on §(iv-bis) grounds, not merely "pending CF-S94").** transit's target-B substitution chain (his R2 DISSENT, Steps 1-5) is honest: it makes Regime-II FORBID-Regime-I CONDITIONAL on the proposed bound `|α_bridge| ≥ s_CS/N_e` (Step 4: "the SURROGATE MAPPING lizzi contests… NOT established"). Read his Step 4 against his QUESTIONS A1, where he concedes he "CANNOT, at present" exhibit the step bounding `|α_bridge|` by `s_CS/N_e`. Put those two admissions together and the magnitude reading collapses to a surrogate-FAIL by the §(iv-bis) substitution-chain sub-test. NEW evidence — the trivial-vs-non-trivial bound distinction (Sage this run):

```
Claim: "The target-B lower bound on |α_bridge| is an undischarged §(iv-bis) surrogate step,
        hence the magnitude reading is a surrogate-FAIL, non-promotable independent of margin."

Step 1 [Def]: the Regime-II FAIL on target B requires a NON-TRIVIAL lower bound  |α_bridge| ≥ B  with B > α_req.
Step 2 [Fact]: the ONLY lower bound on |α_bridge| that holds WITHOUT any substrate mapping is the
              trivial  |α_bridge| ≥ 0  (a magnitude is non-negative). It is sign-locked AND
              uninformative (0 < α_req, so it FORBIDS nothing).
Step 3 [Subst]: to use  B = s_CS/N_e = 6.381e-3 > α_req  as the bound INSTEAD of 0 requires the
              inequality  |α_bridge| ≥ s_CS/N_e. This inequality IS the §(iv-bis) numerator-to-cocycle
              mapping (CONVERGENCE (2)); S62 #18 supplies only  s_CS ≥ 0, NOT  |α_bridge| ≥ s_CS/N_e.
Step 4 [Read off]: transit's QUESTIONS A1 concedes this inequality is "an ansatz, NOT a derived identity."
              So the only DERIVED sign-locked lower bound on |α_bridge| is the trivial 0 — which forbids
              no Regime. The non-trivial bound s_CS/N_e is the surrogate; it is undischarged.
Step 5 [Conclusion]: target B's Regime-II FAIL is informative on |α_bridge| ⟺ the mapping is derived.
              It is NOT derived (A1). Therefore the magnitude reading is itself a §(iv-bis) surrogate-FAIL,
              uninformative on the canonical magnitude per sub-test (iii) — NON-PROMOTABLE TODAY.
```

**Where this lands relative to transit's framing.** transit's R2 DISSENT calls the sign-lock "a RED HERRING for target (B)" (his Step 5). I half-agree and half-correct: the sign-lock is the wrong sub-test for target B (correct — sub-test ii does not fire on a magnitude bound), but it is NOT a red herring for the §(iv-bis) VERDICT, because the §(iv-bis) obstruction on target B is supplied by a DIFFERENT sub-test (sub-test i: the undischarged bound) that delivers the SAME non-promotion. transit's own framing ("promotability hinges on the lower-bound mapping… NOT on the sign-lock") is, read precisely, an §(iv-bis) sub-test (i) FAILURE in different words — he has restated the surrogate obstruction, not escaped it. The canonical §11 precedent (corpus line 410: the surrogate distance "does not bound the canonical's sign OR magnitude") already covers the magnitude leg; `α_win_lo` is the second instance of the same pattern, on the magnitude sub-row.

**The one place transit's fork is REAL (and resolves in his favor on target B, against my R1 over-reach).** transit's R2 EMERGENCE claims the two readings make DIFFERENT predictions about CF-S94: under his target-B reading, if CF-S94 establishes `|α_bridge^cocycle| ≥ s_CS/N_e`, the §(iv-bis) status of `α_win_lo` CHANGES (it becomes a derived bound, promotable); under a "sign-locked ⇒ permanently non-promotable" reading, CF-S94 cannot help. **This fork is real, and I concede it resolves transit's way for the Regime-relevant target.** My R1 L1 verdict said `α_win_lo` "is NOT promotable to a canonical FAIL at ANY margin… the sign-lock governs informativeness" — that statement is correct *for target A* but OVER-REACHED if read as "permanently non-promotable on target B too." It is not permanently non-promotable on target B: a derived lower-bound mapping from CF-S94 WOULD discharge the sub-test (i) FAIL and promote the magnitude reading. So the honest target-B status is **non-promotable TODAY (undischarged §(iv-bis) bound), discharge-ELIGIBLE at CF-S94** — not "permanently non-promotable." I correct my R1 over-statement here. The sign-lock's permanence is a property of target A only; target B's non-promotion is contingent on CF-S94, exactly as transit says — but it IS an §(iv-bis) surrogate-FAIL in the interim, not a "the floor is fine, just awaiting extraction" status.

**Net DISSENT.** (i) Regime verdict = target B (magnitude) — conceded to transit; the sign-lock is the wrong sub-test for it. (ii) Target B is nonetheless a §(iv-bis) surrogate-FAIL TODAY, via sub-test (i) (the undischarged lower-bound mapping `|α_bridge| ≥ s_CS/N_e`, which transit's A1 concedes is an ansatz) — so the residual disagreement is NOT "sign-lock vs margin" but "is the §(iv-bis) verdict structural-today (mine) or CF-S94-pending (transit's)" — and the precise answer is BOTH: structurally a surrogate-FAIL today, discharge-eligible at CF-S94. (iii) The "permanently non-promotable" reading of my R1 L1 is withdrawn for target B; it stands for target A only.

### EMERGENCE

The coherent picture: the 2×2 (LQG-prescription axis × §(iv-bis) canonical/surrogate axis) and transit's target-A/target-B sub-split compose into a **single 3-axis classification** of `α_win_lo`, and that classification *defines* what CF-S94 must deliver to promote it. I develop both and then write the CF-S94 deliverable spec.

**The composed classification (my L2 2×2 + transit's R2 sub-split).** `α_win_lo` sits at a fully-determined point in a 3-axis lattice:

| Axis | Coordinate of `α_win_lo` | Established by |
|:-----|:-------------------------|:---------------|
| **LQG-prescription** (my L2 columns) | prescription-INDEPENDENT (`s_CS`, `N_e` pure substrate; no DL/GM/U(1)-ABCK choice enters) | transit T2/T3/T4(A) — COLUMN |
| **§(iv-bis) canonical/surrogate** (my L2 rows) | SURROGATE (numerator-to-cocycle mapping undischarged) | my L1/L2 + transit R2 CONVERGENCE (2) — ROW |
| **surrogate sub-row** (transit R2 sub-split) | surrogate-for-a-MAGNITUDE-bound (NOT surrogate-for-the-signed-value) | DISSENT above (Regime = target B) — SUB-ROW |

So the full tag is: **`α_win_lo` is a prescription-INDEPENDENT, §(iv-bis)-SURROGATE-for-a-magnitude-bound, Regime-II-leaning indicator.** Every word is earned: prescription-independent (transit's column, robust), surrogate (my row, the mapping is undischarged), magnitude-bound sub-row (the DISSENT concession — Regime is target B), Regime-II-leaning (T3 direction-invariance across all five ledger N_e), indicator-not-floor (CONVERGENCE (2) tag (b)). This single point in the lattice is the registry landing, and it carries its own discharge pathway, which is CF-S94.

**What CF-S94 must deliver — and the structural insight: the sub-row determines the discharge criterion.** transit's R2 EMERGENCE correctly observed that CF-S94 has TWO separable deliverables (the cocycle's number vs the lower-bound validation). The composed classification SHARPENS this: because `α_win_lo` is the surrogate-for-a-magnitude-bound sub-row (not the signed-value sub-row), its discharge criterion is sub-test (i) on the MAGNITUDE bound — and that is a DIFFERENT and WEAKER requirement than computing the full signed cocycle. CF-S94 must deliver, as a stratified spec:

1. **[Discharge-CRITICAL for `α_win_lo` promotion — the magnitude-bound leg, target B].** Establish whether `|α_bridge^cocycle| ≥ s_CS/N_e` is a genuine substrate-derived inequality — i.e., exhibit the step (absent from S62 #18, conceded missing in transit A1) that the bulk area-coefficient entering the bulk-to-surface reduction is bounded BELOW by the spectral variance budget `s_CS` (not merely "O(1) dispersion"). This is the §(iv-bis) sub-test (i) discharge. If CF-S94 delivers it, `α_win_lo` promotes from "surrogate-for-a-magnitude-bound indicator" to "substrate-derived lower-bound floor" and the Regime-II FAIL becomes canonical-grade on target B. If CF-S94 shows NO non-trivial bound exists (only `|α_bridge| ≥ 0` survives), `α_win_lo` is permanently demoted to a heuristic with no bounding content. **This is the binary CF-S94 resolves.** Note: this does NOT require the cocycle's sign — a lower bound on a magnitude is sign-agnostic (the one place transit's "sign-lock is orthogonal to target B" is exactly right).

2. **[Independent deliverable — the signed-value leg, target A, NOT required for `α_win_lo`].** Compute `⟨[S_exit-horizon]^♯, Ch(P_0(τ~0.16))⟩` as a SIGNED index-type number. This is the Level-1 canonical the bridge-class registry (`lqg-narrow-path-bridge-class.md`, PENDING-FIRST-EXTRACTION Level 1) awaits, and it is what makes the bridge entry STAGE-1-CANDIDATE-eligible. But — and this is the structural payoff — **`α_win_lo`'s promotion does NOT depend on deliverable 2.** The sign-lock argument (my R1, conceded by transit) means `α_win_lo` can NEVER be a surrogate for the signed value; it is only ever a magnitude-bound surrogate. So deliverable 2 is required for the BRIDGE THEOREM (Level-1 cohomology-class identity) but is ORTHOGONAL to whether `α_win_lo` is a valid floor. The workshop has cleanly separated "what promotes `α_win_lo`" (deliverable 1, magnitude bound) from "what promotes the bridge entry to Level-1" (deliverable 2, signed cocycle).

3. **[Consistency cross-check, not a gate].** If both deliverables land, verify `s_CS/N_e ≤ |⟨[φ],Ch(P_0)⟩|` reduced onto the 2-surface (deliverable 1 consistent with deliverable 2's magnitude). A violation would mean the surrogate over-bounds the canonical — i.e., `α_win_lo` is not a lower bound but an over-estimate — which would itself be informative (it would demote the Regime-II lean). The W8-6 Bogoliubov anchor (`R_BG = 1/cosh(2r)`, covariance residual 0) constrains the pre/post RATIO but not this absolute consistency (my Re:T4 (B)), so deliverable 3 is genuinely new CF-S94 content.

**The falsifiable fork, sharpened.** transit's R2 EMERGENCE called the target-A/target-B reading "a falsifiable structural fork, not a semantic difference." I agree, and the composed classification states it precisely:

> CF-S94 deliverable 1 (the magnitude-bound discharge) has THREE outcomes, and they make distinct registry predictions: **(α)** `|α_bridge| ≥ s_CS/N_e` derived ⇒ `α_win_lo` promotes to a substrate-derived floor, Regime-II FAIL canonical-grade on target B; **(β)** only the trivial `|α_bridge| ≥ 0` survives ⇒ `α_win_lo` permanently demoted to a content-free heuristic, Regime verdict reverts to "undetermined pending the full cocycle magnitude"; **(γ)** a DIFFERENT non-trivial bound `B' ≠ s_CS/N_e` is derived ⇒ `α_win_lo` is replaced (the floor is `B'`, and whether `B' ≷ α_req` re-decides the Regime). All three are live; the substrate-side prior (`lqg-narrow-path-bridge-class.md` line 92, P(Regime II) ≥ 0.6 from the `N_e=2.92` O(1)-output evidence) leans toward (α) or (γ) over (β), but the §(iv-bis) discipline forbids asserting which until CF-S94 exhibits the bound.

**The single coherent picture.** `α_win_lo = 6.381e-3` is one point in the 3-axis lattice — prescription-independent, surrogate-for-a-magnitude-bound, Regime-II-leaning indicator — and its registry status is tag (b) (CONVERGENCE, joint). The Regime-II LEAN is robust and prescription-independent (transit's column + T3, joint). The promotion of the *number* to a *floor* is gated by ONE CF-S94 deliverable (the magnitude-bound discharge, sub-test i), which is WEAKER than and ORTHOGONAL to the signed-cocycle deliverable that promotes the bridge entry to Level-1. The sign-lock (my R1) permanently bars `α_win_lo` from the signed-value sub-row but says nothing about the magnitude sub-row; the underived-bound (transit's A1) bars the magnitude sub-row TODAY but is CF-S94-discharge-eligible. Both obstructions are §(iv-bis); they fire on different sub-tests; and the workshop's net structural output is the stratified CF-S94 spec above — the precise statement of what would turn `α_win_lo` from a Regime-II indicator into a registry-eligible floor.

---

## Round 3 — transit: Follow-up

### CONVERGENCE

lizzi's R2 adjudication closes the last open seam, and I accept it without reservation. The verdict is now joint on every axis. Here is the locked position and what moved across the three rounds.

**(1) The residual R2 disagreement is RESOLVED — target B is itself a §(iv-bis) surrogate-FAIL today, and I concede it.** My R2 DISSENT held that for the Regime-deciding magnitude comparison (target B, `|α_bridge|` vs `α_req`), the sign-lock obstruction is orthogonal and the margin is informative *conditional on the lower-bound mapping*. lizzi's R2 adjudication granted that framing exactly — the sign-lock is the wrong sub-test for target B — but then closed the loop I had left open: the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is **itself** an undischarged §(iv-bis) surrogate step, so target B FAILs §(iv-bis) sub-test (i) (the substitution chain establishing the bound) rather than sub-test (ii) (sign-lock). The obstruction does not vanish on target B; it migrates from sub-test (ii) to sub-test (i) and delivers the SAME non-promotion verdict. This is correct, and it is forced by my own R2 QUESTIONS A1, where I conceded I cannot exhibit the step bounding `|α_bridge|` by `s_CS/N_e` from S62 #18 — S62 #18 supplies only `s_CS ≥ 0`, never `|α_bridge| ≥ s_CS/N_e`. lizzi's NEW evidence (her R2 DISSENT trivial-vs-non-trivial chain) is the decisive lock, and I verified it this run (Sage): the ONLY derived, sign-locked lower bound on `|α_bridge|` that holds without a substrate mapping is the trivial `|α_bridge| ≥ 0`, and `0 < α_req` forbids no Regime. The non-trivial bound `s_CS/N_e = 6.381e-3 > α_req` is precisely the surrogate; it is undischarged. **So target B is non-promotable TODAY on §(iv-bis) sub-test (i) grounds — not "pending extraction of a floor that is otherwise fine," but a surrogate-FAIL in the interim.** My R2 phrasing "promotability hinges on the lower-bound mapping, NOT on the sign-lock" was, read precisely, a sub-test (i) failure stated in different words — I had restated the §(iv-bis) obstruction, not escaped it. I accept lizzi's relocation: same §(iv-bis) verdict, different sub-test.

**(2) The target-A / target-B sub-split SURVIVES as a legitimate §(iv-bis) refinement — and I accept its corrected reading.** lizzi adopted my R2 EMERGENCE sub-split (surrogate-for-the-signed-VALUE vs surrogate-for-a-MAGNITUDE-bound) as a genuine refinement of the §(iv-bis) surrogate taxonomy, which I am glad to see carried forward. But she corrected my INFERENCE from it: I had claimed the target-B sub-row is §(iv-bis)-EXEMPT (the sign-lock being orthogonal). That was wrong. The §(iv-bis) obstruction applies to BOTH sub-rows — the canonical §11 precedent (`R_surr = 2f−1`, pru-class-corpus §11, corpus line 410) is explicit that the surrogate-canonical algebraic distance "does not bound the canonical's sign OR magnitude." The precedent already covers the magnitude leg. So the sub-split changes WHICH sub-test fires (sign-lock for target A, undischarged-bound for target B), not WHETHER the obstruction fires. `α_win_lo` is the second framework instance of the §11 pattern, landing on the magnitude sub-row. **CONVERGENCE: the sub-split is a real refinement; the target-B sub-row is NOT exempt; both sub-rows are §(iv-bis)-non-promotable today, via different sub-tests, to the same verdict.**

**(3) The two prior-round concessions STAND and are now joint.** Both carry into the verdict unchanged:
- **T2 arithmetic correction (my R2 CONVERGENCE 1)**: `N_e^acoustic_only = 0.0282 = N_e^GL_internal` (Ginzburg-Landau internal order-parameter relaxation, pre-sound-onset; `s53_phonon_eos_output.txt`), NOT "geom+density with sound deleted" (`0.2023`). Co-decomposition `N_e^full = 0.0282 + 0.1734 = 0.2016 ≈ 0.2015` and `N_e^acoustic = 0.2015 + 2.7179 = 2.9194 ≈ 2.9202`, both machine-ε. lizzi re-verified and discharged her Re:T2 flag. The divisor is fully pinned to the acoustic class `{N_e^sound = 2.7179, N_e^acoustic = 2.9202}`, floor robust to 0.0312 OOM (Sage this run; 1.0744×).
- **Registry tag (b) (my R2 CONVERGENCE 3)**: `α_win_lo = 6.381e-3` is a prescription-independent Regime-II INDICATOR, NOT a registry-eligible substrate-derived floor. The Regime-II LEAN lands as the finding; the NUMBER does not land as a canonical bound. Pinning `6.381e-3` as a floor would be the Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk (`substrate-first-canonical-sourcing.md §(v)`) once the CF-S94 cocycle is the real Level-1 canonical.

**(4) The qualitative Regime-II lean is over-determined and prescription-independent — my T3, conceded by lizzi, STANDS.** No ledger N_e flips the direction (flip threshold `N_e* = 3.871033`, Sage-exact this run; largest ledger candidate `N_e^acoustic = 2.9202 < 3.871`). The direction is structurally protected: the c-ratio needed to flip is `exp(2·3.871) = 2303.23×` against the substrate's `c_fabric/c_Gold = 229.48×`, and the 8D recomputation SHRINKS the sound term (`N_e^sound: 2.7179 → 0.9060`), moving higher-D budgets AWAY from the flip threshold. This is a robust substrate fact — it survives WITHOUT `α_win_lo` being a canonical floor at all, because it is a statement about `s_CS` being small and `α_req` being smaller still across every plausible divisor. The lean is real; the floor is not registry-eligible. These are compatible — the lean is a column property (prescription-independent), the non-floor status is a row property (surrogate).

**The locked joint verdict.** `α_win_lo` is a **prescription-independent, §(iv-bis)-surrogate-for-a-magnitude-bound, Regime-II-leaning INDICATOR — NOT a registry-eligible floor today, but discharge-eligible at CF-S94.** Non-promotion is forced (today) by §(iv-bis) sub-test (i) — the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is an ansatz, not a derived identity (my A1). It is NOT permanently barred: a derived lower-bound mapping from CF-S94 WOULD discharge sub-test (i) and promote the magnitude reading. The sign-lock's permanence (lizzi's R1) is a property of target A ONLY; target B's non-promotion is contingent on CF-S94 — a falsifiable fork, not a semantic difference.

**What changed across the three rounds (numerical vs structural, per `output-standards.md` T1-13):**

*(a) Numerical revisions.*
- `N_e^acoustic_only = 0.0282` re-identified: `0.2023` (geom+density, WRONG, R1) → `0.0282 = N_e^GL_internal` (pre-sound-onset, CORRECT, R2), with machine-ε co-decompositions.
- Flip threshold pinned Sage-exact: `N_e* = s_CS/α_req = 3.871033` (R1 estimate "3.871" → R3 Sage-exact `3.87103345625`); c-ratio-to-flip `2303.23×` vs substrate `229.48×`.
- Acoustic-class floor separation pinned: `0.0312 OOM` (1.0744×) between `N_e^sound` and `N_e^acoustic`.

*(b) Structural changes.*
- `α_win_lo` registry status: "substrate-derived floor (proxy-level)" (my R1 T4-part-3) → "prescription-independent Regime-II INDICATOR, NOT a registry-eligible floor" (tag a → tag b). The epistemic TYPE of the headline number changed: candidate canonical bound → qualitative-lean indicator.
- §(iv-bis) obstruction LOCALIZED: from "small margin" (my loose R1 T3 framing) → "sign-lock divergence on target A (margin-independent) + undischarged-lower-bound on target B (sub-test i, CF-S94-discharge-eligible)" — two sub-tests, one verdict.
- §(iv-bis) surrogate taxonomy REFINED: the surrogate row splits into surrogate-for-the-signed-VALUE (target A) vs surrogate-for-a-MAGNITUDE-bound (target B), with DIFFERENT promotion criteria but the SAME non-promotion verdict today. `α_win_lo` is the first framework instance on the magnitude sub-row; `R_surr=2f−1` (§11) is the precedent on the signed-value sub-row.

### DISSENT

**Nothing substantive remains on the verdict.** This is an honest "no residual," not a courtesy one — and it is worth stating precisely WHY, because the R2 disagreement was real and I want the record to show it was resolved by argument, not abandoned.

The one irreducible disagreement R1/R2 surfaced was the LOCATION of the §(iv-bis) obstruction for the Regime-deciding target B: I held (R2 DISSENT) that the sign-lock is orthogonal to target B and the margin is informative *given the lower-bound mapping*; lizzi held the obstruction is structural-today. lizzi's R2 adjudication resolved this in a way I accept fully: she GRANTED my framing (sign-lock is the wrong sub-test for target B — she conceded the Regime verdict inhabits target B, withdrawing her own R1 "permanently non-promotable" over-reach for that target) AND closed my open conditional (the lower-bound mapping is itself a sub-test (i) surrogate-FAIL today, per my own A1). The result is not a split decision — it is a single coherent verdict where we each conceded our over-reach:
- I withdrew "target B is §(iv-bis)-exempt" (it is not; sub-test (i) fires).
- lizzi withdrew "permanently non-promotable" for target B (it is discharge-eligible at CF-S94; the permanence is target-A-only).

What remains is not a disagreement but a SHARED structural finding with a contingent fork (EMERGENCE): the target-B non-promotion is real today AND removable by CF-S94. That is a fork in what CF-S94 can do, not a dispute between us — we agree on every present-tense claim.

The two results I OWN and established — the **direction-invariance** of the Regime-II lean (T3: no ledger N_e flips it; flip threshold 3.871 structurally unreachable) and the **LQG-prescription-independence** of the magnitude leg (T4-A: `s_CS` and `N_e` are pure substrate quantities; no DL/GM/U(1)-ABCK choice enters) — STAND, are conceded by lizzi, and are not in dispute. They are the column of lizzi's 2×2; her row (surrogate) is orthogonal to them; both are simultaneously true and place `α_win_lo` in one cell. There is no tension to record.

### EMERGENCE

**The net structural output is a single 3-axis classification of `α_win_lo` plus a stratified CF-S94 deliverable spec — and the deepest insight is that the surrogate's SUB-ROW determines its discharge criterion, decoupling `α_win_lo`'s promotion from the bridge entry's Level-1 promotion.**

**(1) The 3-axis classification (joint — lizzi's L2 2×2 + my R2 sub-split, composed).** `α_win_lo` sits at a fully-determined point in a 3-axis lattice:

| Axis | Coordinate of `α_win_lo` | Established by |
|:-----|:-------------------------|:---------------|
| **LQG-prescription** | prescription-INDEPENDENT (`s_CS`, `N_e` pure substrate; no DL/GM/U(1)-ABCK choice enters) | transit T2/T3/T4(A) — COLUMN |
| **§(iv-bis) canonical/surrogate** | SURROGATE (numerator-to-cocycle mapping undischarged; A1) | lizzi L1/L2 + transit R2 CONVERGENCE (2) — ROW |
| **surrogate sub-row** | surrogate-for-a-MAGNITUDE-bound (NOT surrogate-for-the-signed-value) | transit R2 sub-split + lizzi R2 adjudication (Regime = target B) — SUB-ROW |

Full tag: **`α_win_lo` is a prescription-INDEPENDENT, §(iv-bis)-SURROGATE-for-a-magnitude-bound, Regime-II-leaning indicator.** Every word is earned: prescription-independent (column, robust); surrogate (row, mapping undischarged); magnitude-bound sub-row (Regime = target B, lizzi's adjudication); Regime-II-leaning (T3 direction-invariance across all five ledger N_e); indicator-not-floor (tag b, joint). One point in the lattice; it carries its own discharge pathway, CF-S94.

**(2) The structural insight — the sub-row determines the discharge criterion, and that criterion is WEAKER than and ORTHOGONAL to the bridge entry's Level-1 requirement.** Because `α_win_lo` is the surrogate-for-a-magnitude-bound sub-row (NOT the signed-value sub-row), its §(iv-bis) discharge is sub-test (i) on the MAGNITUDE bound — exhibit the step `|α_bridge| ≥ s_CS/N_e` as a genuine substrate-derived inequality. That is a different and WEAKER requirement than computing the full SIGNED cocycle. The sign-lock (lizzi's R1, conceded) permanently bars `α_win_lo` from ever being a surrogate for the signed VALUE — so the signed-cocycle deliverable (which promotes the bridge ENTRY to Level-1) is ORTHOGONAL to whether `α_win_lo` is a valid floor. The workshop has cleanly separated "what promotes `α_win_lo`" (a magnitude lower-bound, sign-agnostic) from "what promotes the bridge entry to Level-1" (the signed index-type cocycle). This is the payoff: a future planner need not conflate the two.

**(3) The stratified CF-S94 deliverable spec (the workshop's net forward output).** CF-S94 must deliver, as three separable items with distinct registry consequences:

- **Deliverable 1 — [discharge-CRITICAL for `α_win_lo`; the magnitude-bound leg, target B].** Establish whether `|α_bridge^cocycle| ≥ s_CS/N_e` is a genuine substrate-derived inequality — exhibit the step (absent from S62 #18, conceded missing in A1) that the bulk area-coefficient entering the bulk-to-surface reduction is bounded BELOW by the spectral variance budget `s_CS` (not merely "O(1) dispersion"). This is the §(iv-bis) sub-test (i) discharge. **Sign-agnostic** — a lower bound on a magnitude does not need the cocycle's sign (the one place my "sign-lock orthogonal to target B" is exactly right). This is the binary CF-S94 resolves for `α_win_lo`.
- **Deliverable 2 — [the signed-value leg, target A; required for the BRIDGE ENTRY's Level-1, NOT for `α_win_lo`].** Compute `⟨[S_exit-horizon]^♯, Ch(P_0(τ~0.16))⟩` as a SIGNED index-type number. This is the Level-1 canonical that `lqg-narrow-path-bridge-class.md` (REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION) awaits, making the bridge entry STAGE-1-CANDIDATE-eligible. `α_win_lo`'s promotion does NOT depend on it (sign-lock ⇒ `α_win_lo` can never be a surrogate for the signed value).
- **Deliverable 3 — [consistency cross-check, NOT a gate].** If both land, verify `s_CS/N_e ≤ |⟨[φ],Ch(P_0)⟩|` reduced onto the 2-surface (deliverable 1 consistent with deliverable 2's magnitude). A violation would mean the surrogate OVER-bounds the canonical (`α_win_lo` is an over-estimate, not a lower bound) — itself informative; it would demote the Regime-II lean. The W8-6 Bogoliubov anchor (`R_BG = 1/cosh(2r) = 6.84e-4`, covariance residual 0) constrains the pre/post RATIO but NOT this absolute consistency, so deliverable 3 is genuinely new CF-S94 content.

**(4) The falsifiable fork (sharpened, three outcomes).** Deliverable 1 has three live outcomes with distinct registry predictions:
- **(α)** `|α_bridge| ≥ s_CS/N_e` derived ⇒ `α_win_lo` PROMOTES to a substrate-derived floor; Regime-II FAIL canonical-grade on target B.
- **(β)** only the trivial `|α_bridge| ≥ 0` survives ⇒ `α_win_lo` permanently DEMOTED to a content-free heuristic; Regime verdict reverts to "undetermined pending the full cocycle magnitude."
- **(γ)** a DIFFERENT non-trivial bound `B' ≠ s_CS/N_e` is derived ⇒ `α_win_lo` is REPLACED (floor is `B'`; whether `B' ≷ α_req` re-decides the Regime).

The substrate-side prior (`lqg-narrow-path-bridge-class.md`, P(Regime II) ≥ 0.6 from the `N_e=2.92` O(1)-output evidence) leans toward (α) or (γ) over (β), but §(iv-bis) FORBIDS asserting which until CF-S94 exhibits the bound. This is the falsifiable structural fork — the two readings (mine and lizzi's) made DIFFERENT predictions about CF-S94 (mine: status CHANGES if the bound is derived; a "permanently non-promotable" reading: CF-S94 cannot help), and lizzi conceded the fork resolves my way for target B: `α_win_lo`'s non-promotion is contingent, not permanent.

### QUESTIONS

No questions — this is the final round and the verdict is locked. Instead, the EXACT inputs lizzi's verdict turn needs, per the spawn directive's three items:

**(i) The final registry tag wording (verdict-table Key Insight + any §VII / correspondence-ledger landing).** Use tag (b), verbatim recommendation:

> `α_win_lo = 6.381e-3` is a **prescription-independent, §(iv-bis)-surrogate-for-a-magnitude-bound, Regime-II-leaning INDICATOR — NOT a registry-eligible substrate-derived floor.** The Regime-II LEAN is the finding that lands (robust across all five ledger N_e, flip threshold `N_e* = 3.871` structurally unreachable; prescription-independent — no DL/GM/U(1)-ABCK choice enters). The NUMBER `6.381e-3` does NOT land as a canonical bound on `α_bridge`. Non-promotion is forced TODAY by `substrate-first-canonical-sourcing.md §(iv-bis)` sub-test (i): the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is an ansatz, not a derived identity (transit A1). DISCHARGE-ELIGIBLE at `CF-S94-NARROW-PATH-WORKSHOP-6-COCYCLE-CONSTRUCTION` deliverable 1. Level-1 canonical = the CF-S94 signed `[S_exit-horizon]^♯` cocycle. Citing `6.381e-3` as a substrate-derived floor (tag a) would be a Class-(f) PIN-PLACEHOLDER-treated-as-canonical risk (`§(v)`).

This is consistent with W8-7's `band-edge-convention-ambiguous` deferred-pending landing and the bridge entry's REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION hold — the Level-3 anchor is the cocycle, not the surrogate. The W8-3 verdict-line STATUS (INFO, sign=PASS/magnitude=FAIL/regime=MARGINAL) is NOT reopened; this workshop refines the INTERPRETATION of the magnitude leg, not the verdict.

**(ii) The SOURCE-RECON Class-(c) question — `N_e=2.92` vs the S64 registry-theorem `N_e=3.73e-3` (knowledge base checked this run).** A clarifying PROVENANCE note is warranted, NOT a re-pin. The substitution chain settles it:

- The S64 `N_e = 3.73e-3` is a **theorem** entity (`baseline-findings-s66.md`, "Physical transit e-folds (S64)"), NOT a `canonical_constants.py` pin — there is no `N_e` constant to "drift" from. (Knowledge base: `get_constant("N_e")` returns NO exact match; only `N_e_classical = 0.1734` exists as a pinned constant.)
- `N_e = 2.92` lives as an inline value in `session-53-baptista-collab.md` (the acoustic budget), also NOT a `canonical_constants.py` pin.
- My T2/T3 (conceded by lizzi) established these are **DIFFERENT physical quantities**: `N_e=3.73e-3 = H·Δt_transit` is the transit DURATION (impulsive, `H·dt=0.663<1`); `N_e^acoustic=2.92` is the bulk-to-surface acoustic REDUCTION depth (sound-dominated, 93.07%). Duration and reduction-depth are orthogonal substrate quantities. So the ~3-OOM gap is NOT a Class-(c) PIN-DRIFT-FROM-STALE-SOURCE; it is a category distinction between two correctly-valued theorem entities.

Recommendation for lizzi: add a clarifying PROVENANCE note (orchestrator-direct or `mack-cosmic-bridge` if it touches the falsifier inventory) distinguishing `N_e^acoustic ≈ 2.92` (bulk-to-surface acoustic reduction depth, the W8-3 divisor) from the S64 registry-theorem `N_e = 3.73e-3` (physical transit duration `H·Δt`) — they are co-listed under the bare label `N_e` but measure orthogonal processes. NO re-pin; NO Class-(c) remediation; the note prevents a future SOURCE-RECON false-positive when both surface under the same symbol. If `N_e^acoustic = 2.9202` is to be cited downstream (e.g., by CF-S94), it should be promoted to `canonical_constants.py` as `N_e_acoustic` WITH provenance (`S53`, `s53_acoustic_efold_output.txt`, decomposition `0.1734+0.0289+2.7179`) so the divisor is import-target — currently it is an inline collab value, a Class-(f) placeholder risk if cited as canonical.

**(iii) The `CF-S94-NARROW-PATH-WORKSHOP-6-COCYCLE-CONSTRUCTION` coupling (its Regime-selection gate consumes `α_win_lo`).** The stratified deliverable spec (EMERGENCE 3) IS the upstream input to CF-S94. The coupling is precise: CF-S94's pre-registered Gate (workingpaper line 489 / 531) decides Regime I (PASS) vs Regime II (FAIL) by extracting `α_bridge` against the recomputed DL/Meissner SU(2) band and comparing to `α_req=4.81e-3` — "the 0.12-OOM proxy edge must be resolved by the explicit cocycle, NOT the moment-ratio proxy." This workshop SHARPENS that gate in three ways lizzi should record as the upstream hand-off:
- CF-S94's Regime-selection is **target B** (a magnitude comparison `|α_bridge|` vs `α_req`), NOT target A — a negative cocycle does NOT flip the Regime (`γ_emergent = |α_bridge|·49.34` is a positive Immirzi magnitude). lizzi established this in her R2 adjudication; it tells CF-S94 it needs `|α_bridge|`, not `sign⟨[φ],Ch(P_0)⟩`, to decide the Regime.
- CF-S94 deliverable 1 (magnitude lower-bound discharge) is what determines whether `α_win_lo` was a valid floor — the (α)/(β)/(γ) fork (EMERGENCE 4). This is SEPARATE from CF-S94's own α_bridge OOM extraction; deliverable 1 validates the SURROGATE, the OOM extraction delivers the CANONICAL. Both can land; deliverable 3 cross-checks them.
- The signed-cocycle (CF-S94 deliverable 2 = the existing CF-S94 "What" item b) promotes the BRIDGE ENTRY to Level-1 STAGE-1-CANDIDATE — orthogonal to `α_win_lo`'s floor status. CF-S94 should NOT gate `α_win_lo`'s promotion on the signed cocycle; it gates it on deliverable 1 (the weaker, sign-agnostic magnitude bound).

Net: CF-S94's existing 4-field spec (workingpaper 483-490) is correct as written; this workshop adds the stratification (deliverable 1 / 2 / 3) and the target-B framing as the upstream input, so CF-S94's planner knows that `α_win_lo`'s promotion rides on a sign-agnostic magnitude bound, not the full signed cocycle.

---

## Round 3 — lizzi: Cross-Synthesis

### CONVERGENCE

transit's R3 closes the workshop on a single coherent verdict, and I affirm it on every axis. His R3 CONVERGENCE (1)-(4) state the locked position exactly as I would; I add nothing to (3) [the T2 arithmetic correction] or (4) [direction-invariance] beyond confirming I re-verified them this run. What I OWE in this final turn is (a) the formal close of my own R1 over-reach (which transit's R3 (1) correctly attributes to me), (b) the one independent re-verification the spawn directive requires before I pin the registry tag, and (c) the explicit statement of where the §(iv-bis) discipline I own LANDS the number. I do all three here.

**(1) The verdict is JOINT and I lock it.** `α_win_lo = s_CS/N_e = 6.380856e-3` (Sage-exact this run, reproducing the W8-3 verdict-line `6.380856e-3` from `s_CS = 0.018633374383`, `N_e^acoustic = 2.9202`) is a **prescription-independent, §(iv-bis)-surrogate-for-a-magnitude-bound, Regime-II-leaning INDICATOR — NOT a registry-eligible substrate-derived floor today, but DISCHARGE-ELIGIBLE at CF-S94.** Non-promotion is forced TODAY by `substrate-first-canonical-sourcing.md §(iv-bis)` sub-test (i): the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is an ansatz, not a derived identity (transit's R2 A1, conceded). Every word of this tag was earned across three rounds; no word is mine alone or transit's alone — the COLUMN (prescription-independent) is transit's (T2/T3/T4-A), the ROW (surrogate) is mine (L1/L2), the SUB-ROW (magnitude-bound, not signed-value) is the joint product of transit's R2 sub-split and my R2 adjudication. **This is the locked joint verdict.**

**(2) My R1 over-reach is formally withdrawn — and this is the substantive concession I owe, not a courtesy.** My R1 L1 algebraic-distance theorem stated `α_win_lo` "is NOT promotable to a canonical FAIL at ANY margin… the sign-lock governs informativeness." That sentence is CORRECT for target A (the cocycle's sign — a sign-locked surrogate can never resolve a signed canonical's sign, at any margin; transit conceded this in his R2 DISSENT and re-affirms it in R3). It OVER-REACHED if read as "permanently non-promotable on target B too." I withdrew this in my R2 DISSENT and I formalize the withdrawal now: **the permanence of non-promotion is a property of target A ONLY.** Target B (the Regime-deciding magnitude comparison `|α_bridge|` vs `α_req`) is non-promotable TODAY — but on §(iv-bis) sub-test (i) grounds (undischarged bound), which is CF-S94-discharge-ELIGIBLE, not permanent. A derived lower-bound mapping from CF-S94 deliverable 1 WOULD discharge sub-test (i) and promote the magnitude reading. So transit's falsifiable fork (R2/R3 EMERGENCE) resolves his way for target B, and I record that my "permanently non-promotable" reading was the over-reach his fork correctly identified. The two of us each retracted one over-claim: transit withdrew "target B is §(iv-bis)-exempt" (it is not — sub-test (i) fires); I withdrew "permanently non-promotable on target B" (it is not — discharge-eligible). Neither retraction is a defeat; both are the workshop converging by argument.

**(3) The independent re-verification the spawn directive requires (Sage-exact, this run).** Before pinning the registry tag I re-derived every load-bearing number from the R1/R2-agreed inputs, exact-rational (no float mnemonic near a threshold, per `math-scripts.md` mnemonic-vs-exact discipline):
- `α_lo = s_CS/N_e^acoustic = 6.380856e-3` (matches W8-3 verdict-line and WP line 219 `6.380856e-3`).
- Flip threshold `N_e* = s_CS/α_req = 3.8710334562504936` (transit's R3 Sage-exact `3.87103345625` confirmed to 11 sig figs); largest ledger candidate `N_e^acoustic = 2.9202 < 3.871` ⇒ Regime-II direction unconditional across all five ledger N_e — **CONFIRMED**.
- Acoustic-class floor separation `|OOM_sound − OOM_acoustic| = |0.1536 − 0.1224| = 0.0312 OOM` (1.0744×) — **CONFIRMED**, the divisor is pinned to the acoustic class to 0.03 OOM.
- The trivial-vs-non-trivial bound lock (my R2 DISSENT chain, re-run exact): `0 < α_req` (the trivial sign-locked bound forbids no Regime) AND `s_CS/N_e^acoustic = 6.38e-3 > α_req = 4.81e-3` (the non-trivial surrogate bound that DOES forbid Regime-I — but it is the undischarged §(iv-bis) step) — **CONFIRMED**. The ONLY derived sign-locked lower bound on `|α_bridge|` is the trivial `≥ 0`; the Regime-II-forbidding bound `≥ s_CS/N_e` is precisely the surrogate.
- Structural protection of the direction (transit's T3): c-ratio needed to flip `exp(2·N_e*) = 2303.23×` against the substrate's `c_fabric/c_Gold = 229.48×` — **CONFIRMED**; the sound hierarchy would have to be ~10× larger to reach the flip threshold.

All five reproduce the R1/R2 values to the cited precision. The verdict rests on verified numerics, not asserted ones.

**(4) Where the §(iv-bis) discipline I own LANDS the number — the registry consequence stated once, cleanly.** The number `6.380856e-3` enters the registry as an INDICATOR carrying the qualitative Regime-II lean, NOT as a canonical bound. Concretely: any future gate citing `α_win_lo` MUST cite it as tag (b) — "heuristic Regime-II indicator pending the CF-S94 cocycle" — never tag (a) "substrate-derived floor on `α_bridge`." Citing it as (a) is the Class-(f) PIN-PLACEHOLDER-PENDING-SUBSTRATE-CANONICAL risk (`substrate-first-canonical-sourcing.md §(v)`) once the CF-S94 cocycle is the real Level-1 canonical. This is consistent with the W8-3 verdict-line STATUS (INFO, `band-edge-convention-ambiguous`, 3-tuple sign=PASS/magnitude=FAIL/regime=MARGINAL) — **the verdict is NOT reopened**; this workshop refines the INTERPRETATION of the magnitude leg (surrogate-for-a-magnitude-bound, not a floor), and the §IX.7 narrow-path constraint-map row stays "prescription-independently Regime-II-favoring." The bridge entry `lqg-narrow-path-bridge-class.md` stays REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION; its Level-3 anchor is the cocycle, not the surrogate.

### DISSENT

**No residual dissent on the verdict — and, as transit's R3 stated for his side, this is an honest "no residual," reached by mutual retraction of over-reach, not a courtesy close.** The one irreducible R1/R2 disagreement was the LOCATION of the §(iv-bis) obstruction for the Regime-deciding target B. It is resolved, and the resolution is symmetric:

- transit held (R2 DISSENT) that the sign-lock is orthogonal to target B and the margin is informative *given the lower-bound mapping*. I GRANTED this framing in my R2 adjudication — the sign-lock IS the wrong sub-test for a magnitude bound (sub-test (ii) does not fire on a positive-definite quantity proposed as a lower bound). I conceded the Regime verdict inhabits target B.
- I held (R1 L1) that the obstruction is structural-today. I CLOSED transit's open conditional: the lower-bound mapping `|α_bridge| ≥ s_CS/N_e` is ITSELF an undischarged §(iv-bis) surrogate step (sub-test (i)), per transit's own R2 A1 ("an ansatz, NOT a derived identity"). The obstruction does not vanish on target B; it MIGRATES from sub-test (ii) [sign-lock, target A] to sub-test (i) [undischarged bound, target B], delivering the SAME non-promotion verdict.

transit's R3 CONVERGENCE (1) accepts this relocation in full ("same §(iv-bis) verdict, different sub-test"), and his R3 DISSENT records the symmetric retractions. I confirm there is nothing left to adjudicate. **The single residual is not a disagreement but a SHARED finding with a contingent fork**: target-B non-promotion is real TODAY (sub-test (i) fails) AND removable by CF-S94 (deliverable 1 can discharge sub-test (i)). That is a fork in what CF-S94 can do — a falsifiable structural prediction, EMERGENCE below — not a tension between transit and me. We agree on every present-tense claim.

One precision I add to close the record cleanly, lest a future reader mistake the convergence for hand-waving: the §11 canonical precedent (`R_surr = 2f − 1`, pru-class-corpus §11) is the load-bearing authority for ruling that target B is NOT §(iv-bis)-exempt. The corpus text (line 410) states the surrogate-canonical algebraic distance "does not bound the canonical's sign OR magnitude" — the precedent is a sign-AND-magnitude obstruction. transit's R2 EMERGENCE had read §(iv-bis)'s decisive clause as sign-lock-only (sub-test (ii)) and inferred a magnitude-bound surrogate escapes it; the precedent already forecloses that inference. `α_win_lo` is the SECOND framework instance of the §11 pattern — landing on the magnitude sub-row (sub-test (i) fires), where `R_surr = 2f−1` was the first instance on the signed-value sub-row (sub-test (ii) fires). Same discipline, different sub-test, same non-promotion. This is the structural reason the dissent has no residual: §(iv-bis) is not silent on magnitude-bound surrogates — it adjudicates them by a different sub-test that fires here.

### EMERGENCE

The net structural output is what transit's R3 EMERGENCE laid out — a single 3-axis classification of `α_win_lo` plus a stratified CF-S94 spec — and I affirm his (1)-(4) without amendment. My final-turn contribution to EMERGENCE is to state the **one structural theorem this workshop establishes that outlives the specific `α_win_lo` case**, and to pin the falsifiable fork as the CF-S94 gate. Both are below; the verdict table, open questions, and wrap-up follow.

**(1) The structural theorem (the durable output, generalizing beyond `α_win_lo`).** This workshop refines the §(iv-bis) surrogate taxonomy with a result that applies to ANY substrate-side surrogate for a signed cohomology-class canonical:

> **§(iv-bis) surrogate sub-row theorem (S93 W8-3, lizzi+transit joint).** A §(iv-bis) surrogate `Σ` for a SIGNED index-type canonical `C = ⟨[φ], Ch(P_0)⟩` partitions into two sub-rows by what `Σ` stands in for:
> - **(sub-row A) surrogate-for-the-signed-VALUE** — `Σ` stands in for `C` itself. The §(iv-bis) obstruction fires on **sub-test (ii)** (sign-lock divergence): if `sign(Σ)` is mechanically locked (combinatorial fraction, Cauchy-Schwarz positivity, etc.) while `sign(C)` is geometry-free, `Σ` is uninformative on `sign(C)` at **any margin**. Permanent non-promotion. Canonical instance: `R_surr = 2f−1` (pru-class-corpus §11).
> - **(sub-row B) surrogate-for-a-MAGNITUDE-bound** — `Σ` stands in for a lower (or upper) bound on `|C|`. The §(iv-bis) obstruction fires on **sub-test (i)** (undischarged substitution chain): the proposed inequality `|C| ≥ Σ` is non-promotable UNLESS the bounding step is a derived substrate identity (the trivial `|C| ≥ 0` is the only sign-lock-free derived bound, and it forbids nothing). **Contingent** non-promotion — discharge-eligible if the bound is later derived. First framework instance: `α_win_lo = s_CS/N_e` (THIS workshop).
>
> Both sub-rows are §(iv-bis)-non-promotable; they fire DIFFERENT sub-tests with DIFFERENT permanence (A permanent, B contingent). Crucially, **the sub-row is determined by what the surrogate stands in for, which is fixed by which question the canonical answers** — and `α_win_lo`'s sub-row is forced to B because the Regime verdict is a MAGNITUDE comparison (`γ_emergent = |α_bridge|·49.34` is a positive Immirzi parameter; a negative cocycle does not flip the Regime). This is the structural content that survives whatever CF-S94 returns.

This is a candidate calibration-corpus extension to `substrate-first-canonical-sourcing.md §(iv-bis)` (FLAGGED below — I do not edit the rule file; subagents are edit-denied on `.claude/rules/` and the ORCHESTRATOR-RESERVED corpus blocks).

**(2) The structural payoff — decoupling, restated as a planning directive.** Because `α_win_lo` is sub-row B (magnitude-bound), its discharge criterion is sub-test (i) on the MAGNITUDE bound — a WEAKER and sign-AGNOSTIC requirement, ORTHOGONAL to computing the full signed cocycle. The sign-lock (my R1, conceded by transit) permanently bars `α_win_lo` from sub-row A, so the signed-cocycle deliverable (which promotes the bridge ENTRY to Level-1) is ORTHOGONAL to whether `α_win_lo` is a valid floor. **A future planner MUST NOT gate `α_win_lo`'s promotion on the signed cocycle; it gates on the weaker sign-agnostic magnitude bound (CF-S94 deliverable 1).** This is the workshop's net forward instruction, and it is what the stratified CF-S94 spec below encodes.

**(3) The falsifiable fork as the CF-S94 gate (pinned).** CF-S94 deliverable 1 (the magnitude-bound discharge) has three live outcomes with distinct registry predictions — this IS the Regime-selection gate's actual input, and I pin it as the (α)/(β)/(γ) fork in the carry-forward spec:
- **(α)** `|α_bridge^cocycle| ≥ s_CS/N_e` derived ⇒ `α_win_lo` PROMOTES to a substrate-derived lower-bound floor; Regime-II FAIL canonical-grade on target B.
- **(β)** only the trivial `|α_bridge| ≥ 0` survives (no non-trivial substrate bound exists) ⇒ `α_win_lo` permanently DEMOTED to a content-free heuristic; Regime verdict reverts to "undetermined pending the full cocycle magnitude."
- **(γ)** a DIFFERENT non-trivial bound `B' ≠ s_CS/N_e` is derived ⇒ `α_win_lo` is REPLACED (the floor is `B'`; whether `B' ≷ α_req = 4.81e-3` re-decides the Regime).

The substrate-side prior (`lqg-narrow-path-bridge-class.md`, P(Regime II) ≥ 0.6 from the `N_e=2.92` O(1)-output evidence) leans toward (α) or (γ) over (β) — but §(iv-bis) FORBIDS asserting which until CF-S94 exhibits the bound. The fork is the gate; the prior is not a substitute for the derivation.

**Substrate framing (the explanation flows substrate → emergent, per `phononic-framing.md`).** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold))`; its tightly-clustered mode spectrum (`s_CS = 0.0186`, a small intrinsic fractional variance — the same tight clustering that protects `n_s = 1`) and its acoustic bulk-to-surface reduction depth (`N_e^acoustic = 2.92`, sound-dominated at 93.07%) are substrate-IS quantities. The LQG SU(2) Immirzi demand `α_req = 4.81e-3` is the laboratory-IN target the emergent geometry would have to MATCH for Regime I. The Regime-II lean is the substrate DECLINING to manufacture an area-coefficient as small as canonical LQG demands, from its own tight dispersion distributed over its acoustic reduction — prescription-independently, in the Regime-II direction. What this workshop adds is the honest epistemic status of the NUMBER that expresses the decline: `6.38e-3` is a surrogate INDICATOR of the lean, not yet the canonical cocycle pairing that IS the substrate's exit-horizon area coefficient. The fabric's lean is real; the canonical number that pins it awaits the CF-S94 cocycle.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | (a) Substrate-derived floor vs §(iv-bis) surrogate | T1, L1, R2/R3 | **Converged** | `α_win_lo = s_CS/N_e` is a §(iv-bis) **surrogate**, NOT a registry-eligible substrate-derived floor. The numerator `s_CS = Var(\|λ\|)/⟨\|λ\|⟩²` is EXACT (rigorous variance budget, S62 #18); the QUOTIENT-as-cap-on-`α_bridge` is the undischarged surrogate step (`s_CS ≥ 0` is all S62 #18 gives; `\|α_bridge\| ≥ s_CS/N_e` is an ansatz, transit A1). Tag (b) joint: "Regime-II indicator, not a floor." |
| 2 | (b) Canonical N_e selection (2.92 / 0.0282 / 3.73e-3) | T2, Re:T2, R2 | **Converged** | Divisor pinned to the ACOUSTIC class `{N_e^sound=2.7179, N_e^acoustic=2.9202}`, floor robust to **0.0312 OOM** (Sage-exact). The 3-OOM ledger spread is a category-error artifact: `N_e^geom=0.1734` (geometric ceiling) and `N_e^phys=3.73e-3` (transit DURATION, H·Δt) measure orthogonal processes — withdrawn. `N_e^acoustic_only=0.0282 = N_e^GL_internal` (GL relaxation, pre-sound-onset; transit's R1 "geom+density=0.2023" arithmetic CORRECTED in-session, conclusion survives). |
| 3 | Regime-II magnitude-leg direction robustness | T3, L2, R3 | **Converged** | Direction is **over-determined and prescription-independent**: NO ledger N_e flips it (flip threshold `N_e* = 3.8710334562`, Sage-exact; largest candidate `N_e^acoustic=2.9202 < 3.871`). Structurally protected — c-ratio to flip `exp(2·N_e*)=2303.23×` vs substrate `c_fabric/c_Gold=229.48×`; 8D recomputation SHRINKS the sound term, moving away from the threshold. The lean survives WITHOUT `α_win_lo` being canonical (a statement about `s_CS` small, `α_req` smaller). |
| 4 | Structural verdict (floor status + N_e + corrected direction) | T1–T4, L1–L2, R2/R3 | **Emerged** | 3-axis classification: `α_win_lo` is a **prescription-INDEPENDENT** (column, transit), **§(iv-bis)-SURROGATE** (row, lizzi), **surrogate-for-a-MAGNITUDE-bound** (sub-row, joint — Regime = target B since `γ_emergent=\|α_bridge\|·49.34` is a positive Immirzi parameter) **Regime-II-leaning INDICATOR**. §(iv-bis) sub-row theorem (NEW, durable): sub-row A (signed-value) fires sub-test (ii) [sign-lock, PERMANENT non-promotion]; sub-row B (magnitude-bound) fires sub-test (i) [undischarged bound, CONTINGENT — discharge-eligible at CF-S94]. Decoupling: `α_win_lo` promotion gates on the WEAKER sign-agnostic magnitude bound (deliverable 1), ORTHOGONAL to the signed cocycle that promotes the bridge ENTRY to Level-1. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**Sub-question resolution (the verdict the focus topic demanded):**
- **(a)** `α_win_lo` is a §(iv-bis) **surrogate** (Reading-2 wins on the FORM), NOT under-justified-numerology (the numerator is rigorous — Reading-1's numerator claim stands). Registry status: tag (b) indicator, NOT a floor. **Joint.**
- **(b)** Canonical divisor = `N_e^acoustic = 2.9202` (cross-checked `N_e^sound = 2.7179`); the other three ledger figures are category-mismatched. The magnitude leg's Regime-II direction SURVIVES every choice. **Joint (transit's T2/T3, conceded by lizzi).**
- **STRUCTURAL VERDICT**: (i) `α_win_lo` is a surrogate-for-a-magnitude-bound indicator, NOT a registry-eligible floor, DISCHARGE-ELIGIBLE at CF-S94 deliverable 1; (ii) canonical `N_e = N_e^acoustic = 2.9202`; (iii) corrected direction = Regime-II-favoring, prescription-independent, over-determined across all ledger N_e — the magnitude leg is a robust qualitative lean, the NUMBER `6.380856e-3` is a surrogate indicator. **The W8-3 verdict-line STATUS (INFO) is NOT reopened; the INTERPRETATION of the magnitude leg is refined.**

## Remaining Open Questions

1. **The magnitude-bound discharge (CF-S94 deliverable 1, the (α)/(β)/(γ) fork).** Does a non-trivial substrate-derived lower bound on `|α_bridge|` exist? Specifically: is the bulk area-coefficient entering the exit-horizon bulk-to-surface acoustic reduction bounded BELOW by the spectral variance budget `s_CS` (not merely "O(1) dispersion")? S62 #18 gives only `s_CS ≥ 0`; the step `|α_bridge| ≥ s_CS/N_e` is absent from it and conceded missing (transit A1). Outcome (α) promotes `α_win_lo` to a floor; (β) demotes it to a content-free heuristic; (γ) replaces it with `B' ≠ s_CS/N_e`. This is the single binary that promotes/demotes `α_win_lo`. **Sign-agnostic** — does NOT require the cocycle's sign.

2. **The signed exit-horizon cocycle (CF-S94 deliverable 2, ORTHOGONAL to Q1).** What is `⟨[S_exit-horizon]^♯, Ch(P_0(τ~0.16))⟩` as a SIGNED index-type number? This is the Level-1 canonical `lqg-narrow-path-bridge-class.md` (REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION) awaits — it promotes the BRIDGE ENTRY to STAGE-1-CANDIDATE. By the sign-lock theorem (R1, joint), `α_win_lo` can NEVER be a surrogate for this signed value; so Q2 is required for the bridge theorem but is ORTHOGONAL to `α_win_lo`'s floor status (Q1). The workshop has cleanly separated the two; CF-S94 must not conflate them.

3. **Surrogate-canonical consistency (CF-S94 deliverable 3, contingent on Q1∧Q2).** If both land, is `s_CS/N_e ≤ |⟨[φ], Ch(P_0)⟩|` reduced onto the 2-surface? A violation means the surrogate OVER-bounds the canonical (`α_win_lo` is an over-estimate, not a lower bound) — itself informative; it would demote the Regime-II lean. The W8-6 Bogoliubov anchor (`R_BG = 1/cosh(2r)`, covariance residual 0) constrains the pre/post RATIO but NOT this absolute consistency (lizzi Re:T4-B), so deliverable 3 is genuinely new CF-S94 content.

4. **§(iv-bis) sub-row theorem K-counter (methodology, S94+).** The §(iv-bis) surrogate sub-row theorem (EMERGENCE 1) is a candidate calibration-corpus extension. It currently has TWO instances on distinct sub-rows: `R_surr = 2f−1` (pru-class-corpus §11, sub-row A signed-value) and `α_win_lo = s_CS/N_e` (THIS workshop, sub-row B magnitude-bound). A third structurally-distinct instance would advance the K-counter toward MANDATORY per `feedback_rules-compensate-missing-structure.md`. Does a future surrogate land on a THIRD distinct sub-test? (FLAGGED for orchestrator — rule-file/corpus edit, not lizzi's to make.)

5. **Whether the §IX.7 narrow-path Regime selection itself reopens if Q1 returns (β).** If CF-S94 finds NO non-trivial substrate bound, the §IX.7 constraint-map row ("prescription-independently Regime-II-favoring") would need re-keying to "undetermined pending the full cocycle magnitude" — because the Regime-II LEAN currently rests partly on the `N_e=2.92` O(1)-output prior, and a (β) outcome removes the bound-content that the prior was standing in for. This is the one path by which the workshop's joint qualitative finding could weaken; it is gated on Q1=(β), which the substrate-side prior disfavors but §(iv-bis) forbids ruling out pre-CF-S94.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- `N_e^acoustic_only = 0.0282` re-identified: `0.2023` (geom+density, WRONG, transit R1 T2) → `0.0282 = N_e^GL_internal` (Ginzburg-Landau internal order-parameter relaxation, pre-sound-onset; `s53_phonon_eos_output.txt`, transit R2). Co-decompositions machine-ε: `N_e^full = 0.0282 + 0.1734 = 0.2016 ≈ 0.2015` and `N_e^acoustic = 0.2015 + 2.7179 = 2.9194 ≈ 2.9202`.
- Flip threshold pinned Sage-exact: `N_e* = s_CS/α_req = 3.8710334562504936` (R1 estimate "3.871" → R3 Sage-exact, 11 sig figs; lizzi + transit independent runs agree).
- Acoustic-class floor separation pinned: `|OOM_sound − OOM_acoustic| = |0.1536 − 0.1224| = 0.0312 OOM` (1.0744×) between `N_e^sound` and `N_e^acoustic`.
- Floor value confirmed Sage-exact: `α_lo = s_CS/N_e^acoustic = 6.380856e-3` (reproduces W8-3 verdict-line and WP line 219).
- Structural-protection numerics: c-ratio-to-flip `exp(2·N_e*) = 2303.23×` vs substrate `c_fabric/c_Gold = 229.48×` (the sound hierarchy would need ~10× amplification to flip the direction).

#### (b) Structural changes

- `α_win_lo` registry status: "substrate-derived floor (proxy-level)" (transit R1 T4-part-3) → "**prescription-independent Regime-II INDICATOR, NOT a registry-eligible floor**" (tag (a) → tag (b)). The epistemic TYPE of the headline number changed: candidate canonical bound → qualitative-lean indicator.
- §(iv-bis) obstruction LOCALIZED: "small margin" (transit's loose R1 T3 framing) → "**sign-lock divergence on target A (margin-independent, PERMANENT) + undischarged-lower-bound on target B (sub-test (i), CF-S94-discharge-eligible, CONTINGENT)**" — two sub-tests, one non-promotion verdict.
- §(iv-bis) surrogate taxonomy REFINED into the **sub-row theorem**: the surrogate row splits into surrogate-for-the-signed-VALUE (sub-row A, sub-test (ii) sign-lock) vs surrogate-for-a-MAGNITUDE-bound (sub-row B, sub-test (i) undischarged bound), with DIFFERENT permanence but the SAME non-promotion verdict today. `α_win_lo` is the FIRST framework instance on sub-row B; `R_surr = 2f−1` (pru-class-corpus §11) is the precedent on sub-row A.
- **Decoupling theorem**: `α_win_lo`'s promotion (sub-row B, sign-agnostic magnitude bound, CF-S94 deliverable 1) is ORTHOGONAL to the bridge ENTRY's Level-1 promotion (the signed cocycle, CF-S94 deliverable 2). A planner must not gate the former on the latter.
- **Mutual over-reach retraction**: transit withdrew "target B is §(iv-bis)-exempt" (sub-test (i) fires); lizzi withdrew "permanently non-promotable on target B" (it is discharge-eligible; permanence is target-A-only). The verdict converged by argument, not abandonment.

### What Holds

- **The W8-3 verdict-line STATUS is unchanged** — INFO, `band-edge-convention-ambiguous`, 3-tuple sign=PASS/magnitude=FAIL/regime=MARGINAL (line 170). This workshop refines the INTERPRETATION of the magnitude leg, NOT the verdict; the verdict is permanent per `gate-verdicts.md`.
- **The §IX.7 narrow-path constraint-map row holds** — "prescription-independently Regime-II-favoring" (WP line 538). The qualitative Regime-II lean is robust and prescription-independent (transit's column + T3, joint), and survives WITHOUT `α_win_lo` being a canonical floor. (Caveat: a CF-S94 (β) outcome could re-key it — Open Question 5.)
- **The numerator is rigorous** — `s_CS = Var(|λ|)/⟨|λ|⟩² = F_0F_2/F_1² − 1`, the exact Cauchy-Schwarz slack (S62 #18, Gaussian-saturating, regulator-invariant, L_max-stable: 0.01863 at L=12 vs 0.01920 at L=10). The surrogacy is in the QUOTIENT-as-cap, NOT the numerator.
- **The acoustic-class divisor pin holds** — `N_e^acoustic = 2.9202` (cross-checked `N_e^sound = 2.7179`), sound-dominated at 93.07%; the bulk-to-surface reduction IS the `c_fabric → c_Gold` sound-onset crossing.
- **The W8-6 Bogoliubov pre/post lock holds** — `R_BG = 1/cosh(2r) = 6.84e-4`, `W_BG = cosh(2r) = 1462.30`, covariance residual exactly 0 (line 181). It corroborates the pre/post RATIO structure; it does NOT corroborate the absolute `s_CS/N_e` cap (lizzi Re:T4-B) — that distinction holds.
- **The bridge entry status holds** — `lqg-narrow-path-bridge-class.md` stays REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION; its Level-3 anchor is the cocycle, not the surrogate.

### What Breaks or Strains

- **STRAIN (structural, contingent)**: if CF-S94 deliverable 1 returns (β) — no non-trivial substrate bound on `|α_bridge|` exists, only the trivial `≥ 0` — then `α_win_lo` is permanently demoted to a content-free heuristic AND the §IX.7 Regime-II lean reverts to "undetermined pending the full cocycle magnitude" (the lean's bound-content was being stood in for by the `N_e=2.92` O(1)-output prior, which a (β) outcome removes). This is the one path by which the workshop's joint qualitative finding weakens. The substrate-side prior (P(Regime II) ≥ 0.6) disfavors (β), but §(iv-bis) forbids ruling it out pre-CF-S94. Open Question 5.
- **STRAIN (numerical, latent)**: `N_e^acoustic = 2.9202` is currently an INLINE value in `session-53-baptista-collab.md`, NOT a `canonical_constants.py` pin. If CF-S94 (or any downstream gate) cites it as canonical without promotion, it is a Class-(f) PIN-PLACEHOLDER-PENDING-SUBSTRATE-CANONICAL risk (`substrate-first-canonical-sourcing.md §(v)`). Mitigation: the Effected-In-Session PROVENANCE clarifying note below distinguishes it from the orthogonal `N_e = 3.73e-3` and `N_e_classical = 0.1734`; promotion to `N_e_acoustic` is conditional on CF-S94 citing it (transit R3-ii recommendation, NOT done speculatively here).
- **NO BREAK**: nothing in the framework breaks. The W8-3 verdict, the §IX.7 row, the bridge-entry hold, the numerator rigor, and the W8-6 lock all stand. The workshop's net effect is to SHARPEN the epistemic status of one number (`6.38e-3`: surrogate indicator, not floor) and to refine the §(iv-bis) taxonomy with the sub-row theorem — both additive, neither subtractive.

### Carry-Forward Computations (MATH ONLY — propagate to S94)

#### CF-S94 stratification feeding `CF-S94-NARROW-PATH-WORKSHOP-6-COCYCLE-CONSTRUCTION` (WP lines 525-532)

This workshop SHARPENS the existing CF-S94 4-field spec (it does NOT replace it — the WP CF-S94 spec is correct as written) by stratifying its α_bridge extraction into three separable deliverables with distinct registry consequences, and by pinning that the Regime-selection gate is **target B** (a magnitude comparison `|α_bridge|` vs `α_req`, NOT a sign question — `γ_emergent = |α_bridge|·49.34` is a positive Immirzi parameter; a negative cocycle does NOT flip the Regime). The three deliverables:

**Deliverable 1 — magnitude-bound discharge (discharge-CRITICAL for `α_win_lo`; the actual Regime-selection-gate input).**
1. **What**: Establish whether `|α_bridge^cocycle| ≥ s_CS/N_e` is a genuine substrate-derived inequality — exhibit the step (absent from S62 #18, conceded missing in transit A1) that the bulk area-coefficient entering the exit-horizon bulk-to-surface acoustic reduction is bounded BELOW by the spectral variance budget `s_CS` (not merely "O(1) dispersion"). This IS the §(iv-bis) sub-test (i) discharge. **Sign-agnostic** — a lower bound on a magnitude does not need the cocycle's sign.
2. **Inputs**: `s93_w8_3_…npz` (`s_CS = 0.0186`, α window `[6.38e-3, 1.0]`); `N_e^acoustic = 2.9202` decomposition (`0.1734 + 0.0289 + 2.7179`, `s53_acoustic_efold_output.txt`); the S62 #18 Cauchy-Schwarz bound (`F_0F_2 ≥ F_1²`, atlas-07 A8); `s84_spectrum_cache_L12_tau019.npz`; `lqg-narrow-path-bridge-class.md`.
3. **Gate**: the (α)/(β)/(γ) falsifiable fork — **(α)** `|α_bridge| ≥ s_CS/N_e` derived ⇒ `α_win_lo` PROMOTES to a substrate-derived lower-bound floor, Regime-II FAIL canonical-grade on target B; **(β)** only the trivial `|α_bridge| ≥ 0` survives ⇒ `α_win_lo` permanently DEMOTED to a content-free heuristic, Regime verdict reverts to "undetermined pending full cocycle magnitude"; **(γ)** a DIFFERENT non-trivial bound `B' ≠ s_CS/N_e` is derived ⇒ `α_win_lo` REPLACED by `B'`, whether `B' ≷ α_req = 4.81e-3` re-decides the Regime. The substrate-side prior (P(Regime II) ≥ 0.6) disfavors (β); §(iv-bis) forbids asserting which pre-derivation.
4. **Effort**: ~0.5–1 wave-equivalent (the bounding-step derivation is narrower than the full cocycle construction; it is a substrate-physics lemma, sign-agnostic).

**Deliverable 2 — signed exit-horizon cocycle (promotes the BRIDGE ENTRY to Level-1; ORTHOGONAL to `α_win_lo`).**
1. **What**: Compute `⟨[S_exit-horizon]^♯, Ch(P_0(τ~0.16))⟩` as a SIGNED index-type number (= the existing CF-S94 "What" item (b), WP line 487). This is the Level-1 canonical `lqg-narrow-path-bridge-class.md` (REGISTRY-INCOMPLETE-PENDING-FIRST-EXTRACTION) awaits — it makes the bridge entry STAGE-1-CANDIDATE-eligible. By the sign-lock theorem (joint), `α_win_lo` can NEVER be a surrogate for this signed value, so deliverable 2 does NOT gate `α_win_lo`'s promotion.
2. **Inputs**: `s84_spectrum_cache_L12_tau019.npz`; the τ~0.16 exit-horizon 2-surface localization (S70 Six-Layer Causal Structure); the a_4 BCS-condensation kinematics; HKR + Cheeger-Simons suffix per `cross-pillar-bridge-anatomy.md §"Element 3 bridge-map-scheme suffix discipline"`; DL/Meissner SU(2) band prescription.
3. **Gate**: cocycle constructed + α_bridge OOM extracted + `γ_emergent = |α_bridge|·49.34` re-keyed against the recomputed DL/Meissner band (the existing WP CF-S94 gate, lines 531). Regime I if `|α_bridge − 4.81e-3|` within ~1 OOM AND γ_emergent within rel_tol of γ_BH = 0.2375; Regime II if `α_bridge ≳ 0.1` (~200×).
4. **Effort**: ~1–2 wave-equivalents (substantive Step-4 cocycle construction; unchanged from the WP CF-S94 spec).

**Deliverable 3 — surrogate-canonical consistency cross-check (NOT a gate; contingent on D1∧D2).**
1. **What**: If both land, verify `s_CS/N_e ≤ |⟨[φ], Ch(P_0)⟩|` reduced onto the 2-surface. A violation means the surrogate OVER-bounds the canonical (`α_win_lo` is an over-estimate, not a lower bound) — itself informative; it would demote the Regime-II lean.
2. **Inputs**: deliverable 1 output (the bound, if (α)/(γ)); deliverable 2 output (the signed cocycle magnitude); W8-6 `R_BG` (constrains pre/post RATIO only, NOT this absolute consistency — so D3 is genuinely new content).
3. **Gate**: consistency (`s_CS/N_e ≤ |cocycle|`) PASS = surrogate is a valid lower bound; FAIL = surrogate over-bounds, demote the lean. Cross-check, not a corridor-closing gate.
4. **Effort**: ~0.25 wave-equivalent (arithmetic comparison once D1, D2 land).

**Depends on**: D1 + D2 are independent (orthogonal axes); D3 depends on D1∧D2. All depend on `s93_w8_3_…npz`, `s84_spectrum_cache_L12_tau019.npz`, `lqg-narrow-path-bridge-class.md` (LANDED S92). The pre-registered fork (D1's (α)/(β)/(γ)) is the upstream input to the CF-S94 Regime-selection gate — that gate consumes `α_win_lo`'s promotion status, which D1 resolves.

### Effected In-Session (NON-MATH — completed by lizzi BEFORE TERMINATING)

**EXECUTED (on disk, verified):**

- [x] **W8-3 Regime/Constraint-Map row annotated with registry tag (b)** in `sessions/archive/session-93/session-93-w8-workingpaper.md` (§"Constraint-Map Updates", new row dated 2026-05-25, gate verdict NOT overwritten). Records that `α_win_lo = 6.380856e-3` is a "prescription-independent Regime-II INDICATOR, NOT a registry-eligible floor, pending CF-S94 cocycle (§(iv-bis) surrogate-for-a-magnitude-bound; the bounding step `|α_bridge| ≥ s_CS/N_e` is an ANSATZ)." Explicitly notes the W8-3 verdict-line STATUS (INFO) is NOT reopened; flags tag-(a) citation as a Class-(f) PIN-PLACEHOLDER risk; points to the CF-S94 (α)/(β)/(γ) fork. Verified: 1 grep hit for the tag-(b) row.

- [x] **N_e PROVENANCE / disambiguation note added** to `computations/_shared/canonical_constants.py` (above the `N_e_classical = 0.1734` entry, line 649 region). Distinguishes THREE orthogonal physical quantities co-listed under the bare label `N_e`: (1) `N_e_classical = 0.1734` (GEOMETRIC stiff-limit ceiling, the pinned constant); (2) `N_e^acoustic ≈ 2.9202` (bulk-to-surface ACOUSTIC reduction DEPTH, sound-dominated 93.07%, the W8-3 divisor — INLINE in collab, NOT pinned); (3) `N_e^phys = 3.73e-3` (transit DURATION H·Δt, an S64 theorem entity, NOT a pin). Per transit's SOURCE-RECON Class-(c) ruling: this is NOT a re-pin — they are DIFFERENT observables, each correctly valued; the ~3-OOM spread is a category distinction, not a value drift. The note states `N_e_acoustic` promotion is CONDITIONAL on CF-S94 citing it (NOT done speculatively here — `update_constant` deferred to the citing gate per transit R3-ii). Verified: 3 grep hits + module parses OK (`ast.parse`).

**FLAGGED (ORCHESTRATOR-DIRECT — subagents edit-denied on `.claude/rules/` and ORCHESTRATOR-RESERVED corpus blocks; I do NOT edit):**

- [ ] **`substrate-first-canonical-sourcing.md §(iv-bis)` calibration-corpus addition — the §(iv-bis) surrogate SUB-ROW theorem (EMERGENCE 1).** W-3 APPLIED §(iv-bis) (it did not need to extend the rule to reach the verdict), so this is OPTIONAL — but the workshop surfaced a genuine sub-row refinement worth a corpus row, and per the spawn directive I FLAG it with exact proposed text rather than edit it. The §(iv-bis) corpus is `sessions/framework/registry/pru-class-corpus.md §11` (the `R_surr = 2f−1` canonical worked example). Proposed corpus addition (exact text for orchestrator to land, or to decline):

  > **§(iv-bis) surrogate sub-row taxonomy (S93 W8-3, lizzi+transit joint; SUGGESTION at K=1).** A §(iv-bis) surrogate `Σ` for a SIGNED index-type canonical `C = ⟨[φ], Ch(P_0)⟩` partitions into two sub-rows by what `Σ` stands in for, with DIFFERENT firing sub-tests and DIFFERENT permanence:
  > - **(sub-row A) surrogate-for-the-signed-VALUE** — `Σ` stands in for `C` itself. The §(iv-bis) obstruction fires on **sub-test (ii)** (sign-lock divergence): a mechanically sign-locked `Σ` (combinatorial fraction, Cauchy-Schwarz positivity) is uninformative on `sign(C)` at ANY margin. **PERMANENT** non-promotion. Instance #1 (the §11 canonical worked example): `R_surr = 2f−1`, sign locked to `f > 1/2`.
  > - **(sub-row B) surrogate-for-a-MAGNITUDE-bound** — `Σ` stands in for a bound on `|C|`. The §(iv-bis) obstruction fires on **sub-test (i)** (undischarged substitution chain): the inequality `|C| ≥ Σ` is non-promotable unless the bounding step is a derived substrate identity (the trivial `|C| ≥ 0` is the only sign-lock-free derived bound, and it forbids nothing). **CONTINGENT** non-promotion — discharge-eligible if the bound is later derived. Instance #1 (THIS workshop): `α_win_lo = s_CS/N_e` (Regime verdict is a magnitude comparison `|α_bridge|` vs `α_req`, forcing sub-row B). Discharge-eligible at CF-S94 deliverable 1 (the (α)/(β)/(γ) fork).
  >
  > The sub-row is FORCED by which question the canonical answers (sign vs magnitude). Both sub-rows are §(iv-bis)-non-promotable; the precedent §11 obstruction ("does not bound the canonical's sign OR magnitude", corpus line 410) already covers BOTH legs, so neither sub-row is §(iv-bis)-exempt — the refinement is WHICH sub-test fires and WHETHER non-promotion is permanent. K=1 (two instances on distinct sub-rows: `R_surr=2f−1` on A, `α_win_lo=s_CS/N_e` on B); a THIRD structurally-distinct instance advances toward MANDATORY per `feedback_rules-compensate-missing-structure.md`.

  Routing note for the orchestrator: this is a methodology-rule/corpus extension that BOTH workshop agents agree on (no adversarial tension on the extension's content) — per `Investigating-Workshops.md` "is NOT" item 6 and the Q2 marker test, it is housekeeping (a rule-file/corpus diff), routed to `session-93-housekeeping.md §D` (methodology-rule extension), NOT a new workshop. It is OPTIONAL (the verdict stands without it); land it only if the corpus benefits from the second-sub-row calibration instance.

### Closing Line

`α_win_lo = 6.380856e-3` is the substrate declining — prescription-independently, in the Regime-II direction, over-determined across every ledger N_e — to manufacture an area coefficient as small as canonical LQG demands; but the NUMBER is a §(iv-bis)-surrogate-for-a-magnitude-bound INDICATOR of that decline, not yet the signed exit-horizon cocycle that IS the substrate's area coefficient — so we register the lean, not the floor, and hand CF-S94 a sign-agnostic magnitude-bound fork (α)/(β)/(γ) that is decoupled from, and weaker than, the signed cocycle promoting the bridge entry to Level-1.
