# Session 93 Workshop: volovik × mack

**Date**: 2026-05-25
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: volovik (volovik-superfluid-universe-theorist), mack (mack-cosmic-bridge)
**Source Documents**:
- sessions/archive/session-93/session-93-w4-workingpaper.md
- computations/session-93/s93_gate_verdicts.txt

**Focus Topic — the contested seam**: Does the §VII.AX.OP-PROJ STAGE-3-PERMANENT promotion (landed S93 W4, housekeeping A16) survive the W4-3 finding (gate `S93-W4-3-N-PBH-CANONICAL-TRUNCATION-FACTORIZATION`, line 70 `03b4fb35…`, sign=PASS/magnitude=INFO/regime=VALID) that its Level-3 point-anchor `n_PBH = 7.2761e-23 m⁻³` (L_max=14, canonical_constants `n_PBH_FW_central`, PROVENANCE "PROVISIONAL truncation") is truncation-DIVERGENT — `N_eigs(L_max) = (4/15)L⁵+(10/3)L⁴+16L³+(110/3)L²+(596/15)L+16` is an UNBOUNDED quintic, the point rises `7.276e-23 [L=14] → 9.775e-23 [L=15] → 1.292e-22 [L=16]` toward the conjunct-upper `2.2e-22`, JE5 band `[5.5e-23, 2.2e-22]`?

**Adjudication sub-questions** (the verdict MUST resolve all four):
- (a) Does the §VII.AX.OP-PROJ STAGE-3-PERMANENT promotion survive the truncation-divergent point-anchor?
- (b) Is the registry-PASS criterion "Level-3 < Level-2 at canonical L_max" satisfiable when "canonical L_max" is itself PROVISIONAL (no substrate-singled-out saturation, N_eigs unbounded)?
- (c) Does the JE5 band-membership predicate remain a valid permanence carrier given the quintic-driven monotone rise toward 2.2e-22 (will it breach at finite L_max)?
- (d) STRUCTURAL VERDICT: STAGE-3-PERMANENT STANDS (Reading A, mack) / DOWNGRADE to STAGE-3-ELIGIBLE pending CF-S94 truncation re-determination (Reading B, volovik) / RE-ANCHOR Level-3 to a truncation-INVARIANT functional.

---

## Round 1 — volovik: Opening Analysis

### V1: Does STAGE-3-PERMANENT survive a truncation-divergent point-anchor? (sub-question a)

**Finding (READING-B, steelmanned): NO. A STAGE-3-PERMANENT Level-3 anchor pinned to a divergent point value is not a permanent empirical anchor. The promotion should have been HELD at STAGE-3-ELIGIBLE pending `CF-S94-N-PBH-CANONICAL-TRUNCATION-RE-DETERMINATION`.**

**The substrate-IS fact (I authored this; it is not a claim, it is Sage-exact).** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold=0.19))`. The Level-3 anchor `n_PBH = 7.2761e-23 m⁻³` is the laboratory-IN image of a substrate-IS observable: the Peter-Weyl block-admission cardinality of D_K's OWN spectrum at truncation L_max, channelled through `n_PBH(L_max) = A_prefactor · N_eigs(L_max)` (the obs_2 producing-script form `s91_w5_3` L444-456; §W4-3 STEP-2, WP L312-315). The W4-3 gate (`S93-W4-3-N-PBH-CANONICAL-TRUNCATION-FACTORIZATION`, verdict line 70 `audit_sha256=03b4fb35e7813ae7a81187e32bcf9a58470ecbcacb713fba4847737c345a3178`, 3-tuple `sign=PASS/magnitude=INFO/regime=VALID`, line 72) established Sage-exact:

```
N_eigs(L) = (4/15)L⁵ + (10/3)L⁴ + 16L³ + (110/3)L² + (596/15)L + 16
lim_{L→∞} N_eigs(L) = +∞   (Sage limit = +Infinity; WP L330)
```

This is an UNBOUNDED degree-5 polynomial. The factorization `n_PBH = w(L_max)·κ(g)` has residual **0 EXACT** (Sage `sage_simplify`, WP L313), with `κ(g)=1` (substrate-clock cancellation, obs_2 `cancellation_test_pass=True`), `w(L_max) ∝ N_eigs(L_max)` DIVERGENT, and `MULTIPLICATIVE-NORMALIZATION-CANCELLATION-DETECTED` (WP L313; the K-log-derivatives `d ln(w·κ)/d(ln K)=0` are K-independent — the discriminating content is the asymptote of `w` itself, which does not exist). The point-anchor I re-verify in this dispatch (Sage QQ-exact, A_prefactor = 2.25171444840562e-28 m⁻³):

```
7.276e-23 [L=14] → 9.775e-23 [L=15] → 1.292e-22 [L=16] → 3.445e-22 [L=20] → 2.044e-17 [L=200]
```

a strictly increasing, unbounded sequence. There is no plateau. There is no substrate-singled-out L_max at which the count stops growing — because, as I argued in the §W4-3 substrate-framing paragraph (WP L345), the SU(3) representation ring is infinite; any finite L_max is a truncation of the substrate's own structure, and extending L_max reveals MORE of the substrate's cardinality cascade by construction. **The non-saturation is a substrate property, not a model-incompleteness artifact.**

**The structural implication for permanence.** STAGE-3-PERMANENT (per `joint-theorem-promotion.md §"Stage 3"`) means "permanent — eligible for citation as a structural theorem without the candidate qualifier." A permanent empirical anchor must be a fixed object. But the object that was promoted — `n_PBH_FW_central = 7.2761e-23` at L_max=14 (canonical_constants line 602; gate `S93-W4-5` line 77 `audit_sha256=6833f005503c8d6191c9c049c2ae78962b5140c0288142f3d9e82828800c64c3`) — carries its own refutation in its own PROVENANCE: the assignment-line comment reads "PROVISIONAL truncation" and the W4-5 verdict value-string carries `provisional_truncation_note=True;eligibility=STAGE-3-PERMANENT-ELIGIBLE` simultaneously. **A value cannot be both PERMANENT and PROVISIONAL.** The promotion flipped the tag to PERMANENT on a point that the same wave's W4-3 gate proved is a moving target, with a CF-S94 re-determination carry-forward queued (WP L464: "*what* = re-determine the canonical truncation L_max for n_PBH … *gate* = saturation/asymptote … *effort* = ~1 wave-equivalent"). The existence of that carry-forward is the admission that the anchor is not yet fixed.

**Why the Stage-2 machinery does NOT rescue this.** The §W6-3 Axis-A E2 leg (W4-1 PASS, line 67 `audit_sha256=2ab8bb1ecccb1bb7da8f85250b92ba4b25f2d7476253a4f5b2cb9703d79d29e8`) and the W4-2 MULTI-PIN-ATLAS Stage-2 PASS (line 73) are genuine and I do not contest them. But Stage-2 PASS-AND audits the *clauses* of the joint theorem (Element 1 substrate-IS observable, JOINT Element 3 bridge map, JOINT Element 5 anchor, OE-form, substrate-input-orthogonality) — it certifies the THEOREM's cross-axis structure is independently reproducible. It does NOT certify that the Element-5 anchor is truncation-stable. The W4-1 E2 re-emission was about a verdict-FIELD emit-bug in the OE-form regex (WP L53-59) — a methodology-layer F-image correction — not about the convergence of n_PBH. Stage-2 cleanness and Level-3 truncation-stability are orthogonal axes; the conjunction `(a)∧(b)∧(c)` (WP L86-87) tests the former and is silent on the latter. The promotion conflated "the joint theorem's structure is verified" with "the joint theorem's empirical anchor is permanent."

**Position.** STAGE-3-PERMANENT does not survive a truncation-divergent point-anchor. The correct state is STAGE-3-ELIGIBLE: the Stage-2 conjunction is met, the structural theorem stands, but PERMANENCE of the Level-3 anchor is gated on `CF-S94-N-PBH-CANONICAL-TRUNCATION-RE-DETERMINATION` resolving what "canonical L_max" means for a quintic-divergent observable.

**Questions for mack:**
1. The W4-5 PROVENANCE asserts `eligibility=STAGE-3-PERMANENT-ELIGIBLE` AND `provisional_truncation_note=True` on the *same* value. Under what reading is a value simultaneously PERMANENT and PROVISIONAL without contradiction? If your answer is "permanence attaches to the predicate not the value" — defer that to V3, because the predicate is also truncation-fragile (I show the breach point below).
2. `joint-theorem-promotion.md §"Stage 3"` says STAGE-3-PERMANENT is "eligible for citation as a structural theorem **without the candidate qualifier**." If a downstream gate cites §VII.AX.OP-PROJ's Level-3 anchor at face value (7.2761e-23), and the CF-S94 re-determination later moves it, is that not exactly the candidate-qualifier-stripping hazard the staging was built to prevent?

### V2: Is "Level-3 < Level-2 at canonical L_max" satisfiable when canonical L_max is PROVISIONAL? (sub-question b)

**Finding (READING-B, steelmanned): The criterion is VACUOUSLY satisfiable but not MEANINGFULLY satisfiable. "Canonical L_max" is a free index, not a substrate-singled-out point; the registry-PASS criterion as written presupposes a fixed evaluation point that this observable does not possess.**

**The registry-PASS criterion verbatim** (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"`):

```
Level-3 empirical value  <  Level-2 envelope value  at canonical L_max
```

This criterion has THREE operands: a Level-3 value, a Level-2 envelope, and a binding *evaluation point* — "canonical L_max." For a convergent cross-pillar bridge (the design case — e.g., the §VII.W HP¹ ↔ quantum-metric bridge from my own S86 W-5 work, `L⁻³ → 0.10% at L_max=10`, 0.0095% empirical), "canonical L_max" is well-defined because the observable *saturates*: there is a substrate-natural truncation past which the value is stable to within the envelope, and L_max=10 (or 12) is the master-cache ceiling at which the converged value is read off. The criterion is meaningful because the evaluation point is pinned by saturation.

**For n_PBH there is no such point.** This is precisely the content of the W4-3 verdict (line 70, `value='...L14=PROVISIONAL-NEEDS-RE-DETERMINATION;...N_eigs_degree=5_leading=4/15;n_edge=LINEAR-in-N_eigs;...'`). "L_max=14" is not a saturation point; it is the highest L_max at which obs_2 happened to be evaluated. The §W4-3 substrate-framing paragraph (WP L345, my authorship) states it directly: "**L_max=14 is NOT a substrate-singled-out saturation point because `N_eigs(L_max)` grows without bound**" and "a canonical truncation must be PINNED by a substrate-physical or laboratory-IN anchor rather than read off an N_eigs plateau **that does not exist**."

**The vacuous-vs-meaningful distinction.** One can always *write down* "Level-3(L=14) < Level-2-envelope(L=14)" and evaluate it — that is the vacuous sense. But the criterion's PURPOSE is to certify that the bridge's continuum image is captured at the canonical truncation. When the substrate-IS observable has no continuum limit (the cardinality cascade is genuinely divergent — `lim_{L→∞} = +∞`, Sage), there is no `c_continuum` for Level-3 to be converging *to*. Per `cross-pillar-bridge-anatomy.md §"Level-2 sub-class"`, a Level-2-binding envelope must "operationally bound `‖HKR(c_L) − c_continuum‖`" — and a Level-2-NON-binding envelope (FORBIDDEN for registry-PASS) is one where "`c_continuum` undefined." **For a quintic-divergent N_eigs channel, `c_continuum` IS undefined.** The Level-2 envelope here is not bounding distance-to-continuum; it is bounding distance-to-a-finite-L_max-snapshot. That is a Level-2-non-binding situation in the rule's own taxonomy.

**The honest counter I must concede in advance.** mack will say: the §W4-4 STATE-PROJ entry (WP L394) and the §VII.AX.OP-PROJ entry declare Level-2-binding via "HKR-image of the state-pair occupation binds Level-1 to Pillar IX," and the Connes-Karoubi-type discharge is the binding citation. I accept the binding citation EXISTS. My point is narrower and survives it: the binding citation establishes that an HKR image *exists*; it does not establish that the n_PBH *cardinality-cascade channel* (the `A_prefactor·N_eigs(L_max)` factor that carries ALL the L_max dependence) converges. The bridge map may be structurally sound while the empirical anchor it carries is divergent in L_max. Binding-of-the-map and convergence-of-the-anchor are different properties (this is the same orthogonality as V1).

**The §20 escape hatch and its limit.** The Level-3 annotation discipline (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion — Level-3 annotation discipline"`, S92 §VII.AX.OP-PROJ JE5, SUGGESTION at K=1 — note this rule was authored ON this very entry) says band-edge / Friedrich-Bär-truncation statements are "non-load-bearing annotations" and "a Friedrich-Bär … TRUNCATION-uncertainty envelope at finite L_max can NEVER be credentialed as a PASS gate, because a containment test on a truncation-resolution artifact lets a methodology-floor F-image veto a substrate-IS structural PASS." I AGREE with that rule as stated — a truncation-RESOLUTION uncertainty (how sharply we've resolved a converging value) must not veto a structural PASS. But the W4-3 divergence is NOT a truncation-resolution artifact. It is a substrate-IS DIVERGENCE: `N_eigs` is not a finitely-resolved approximation to a fixed number, it is an unbounded function whose growth I proved is intrinsic to the infinite SU(3) rep ring. The §20 rule protects against confusing *resolution-fuzz* with *substrate-physics*; it does not license treating a genuine substrate-IS divergence as if it were resolution-fuzz. Invoking §20 to wave away the W4-3 divergence inverts the rule's own substrate-first intent.

**Position.** The criterion is satisfiable only in the vacuous "evaluate at the arbitrary point L=14" sense. It is NOT satisfiable in the meaningful "captured at the substrate-natural canonical truncation" sense, because no such truncation exists for this observable. STAGE-3-PERMANENT requires the meaningful sense.

**Questions for mack:**
1. Name the `c_continuum` the Level-2 envelope on §VII.AX.OP-PROJ binds distance to. If `c_continuum` is the L→∞ limit of the cardinality channel, it is `+∞` (Sage). If it is "the value at L=14," then "canonical L_max" is circular (it is canonical because we evaluated there, and we evaluated there because it is canonical).
2. The §20 rule you will invoke distinguishes a "PHYSICAL band that survives L_max → ∞" from a "TRUNCATION-uncertainty envelope at finite L_max." Into which category does a quintic-DIVERGENT point value fall? It survives no L→∞ limit (it diverges), yet it is not resolution-fuzz either. Does §20 actually cover this third case, or was it written for the convergent case only?

### V3: Is the JE5 band-membership predicate a valid permanence carrier under the quintic rise? (sub-question c)

**Finding (READING-B, steelmanned): NO. The band-membership predicate `n_PBH_central ∈ [5.5e-23, 2.2e-22]` is NOT truncation-invariant — it BREACHES the conjunct-upper at finite L_max=19 on the SAME quintic trajectory whose lower-window values certified it. "Holds at every computed truncation" is true only because computation stopped at L=16. I show the breach point below, Sage-QQ-exact.**

This is the load-bearing section, because mack's READING-A explicitly relocates permanence FROM the point value TO the predicate `n_PBH_central ∈ JE5-conjunct [5.5e-23, 2.2e-22]`, claiming the predicate "holds at every computed truncation." I tested that claim by *continuing the same trajectory* the predicate is evaluated on.

**The breach (Sage-QQ-exact, this dispatch).** Using the exact dimensionless ratio `R(n) = N_eigs(n)/N_eigs(14)` and the registered central `central14 = 7.2761e-23` (QQ = 72761/10²⁷), conjunct-upper `= 2.2e-22` (QQ = 22/10²³):

```
L=16 : n_PBH/upper = 0.587133   ∈ band  ✓   (last truncation actually computed in obs_2)
L=17 : n_PBH/upper = 0.764486   ∈ band  ✓
L=18 : n_PBH/upper = 0.982288   ∈ band  ✓   (LAST in-band truncation; margin 1.77% below the wall)
L=19 : n_PBH/upper = 1.24709    ∉ band  ✗   BREACH  (24.7% ABOVE the conjunct-upper)
```

(Float cross-check: `n_PBH(19) = 2.744e-22 > 2.2e-22`; `n_PBH(20) = 3.445e-22`; matches the WP L318 divergence probe and the contested-seam trajectory. Sage QQ-coercion used because L=18/L=19 straddle the threshold and a float comparison near the wall is precision-sensitive per the exact-rational discipline.)

**The structural implication — the predicate is NOT a permanence carrier; it is a finite-window coincidence.** A permanence carrier must be a predicate that is TRUE for all admissible evaluations of the substrate-IS observable. The JE5 band-membership predicate is true on the window L ∈ {10, …, 18} and FALSE for L ≥ 19. It is not invariant under the substrate's own truncation parameter. Its apparent stability is an artifact of the master-cache ceiling (L_max=12) and the obs_2 extension reaching only L=16 (WP L292: obs_2 grid `{7.276e-23, 9.775e-23, 1.292e-22}` at L={14,15,16}). The predicate was certified on a sub-interval of a strictly-increasing divergent sequence; the certification window simply has not yet reached the upper wall. **Relocating permanence from a divergent point to a predicate evaluated on that same divergent point does not buy permanence — it buys a predicate that is true now and provably false at L=19.**

**The asymmetry mack must own.** READING-A treats the −1σ undershoot at the LOWER edge as a "non-load-bearing Friedrich-Bär truncation-resolution annotation" (WP L420; the Class-(i) defect Eq.2′ scrubbed in-session, A15) — i.e., the lower-edge near-breach (−1σ edge 5.316e-23 < conjunct-lower 5.5e-23, gap 1.84e-24, which I confirm exactly) is dismissed as resolution-fuzz. But the SAME trajectory's central value marches monotonically toward the UPPER edge and breaches it at L=19. You cannot consistently call the lower-edge proximity "non-load-bearing annotation" while ignoring that the central value's L_max-trajectory will exit the band entirely at the top. If the band edges are non-load-bearing at the bottom (because of truncation), then band-MEMBERSHIP is equally truncation-contingent at the top — and at the top it FAILS at finite, computable L_max=19. The §20 "central-value-governs" reading does not save this: the CENTRAL value itself breaches at L=19. This is not an edge/annotation question; the governing central crosses the wall.

**Why "every computed truncation" is the wrong test.** The defense "the predicate holds at every computed truncation" sets the bar at the computation frontier, not at the substrate's own structure. But the substrate IS the infinite Peter-Weyl decomposition (my §W4-3 framing, WP L345); "computed truncation" is wherever obs_2's loop happened to stop, not a substrate-singled-out boundary. Pinning a PERMANENCE claim to "holds wherever we have computed so far" makes permanence a function of compute budget — the antithesis of a structural theorem. The substrate does not stop at L=16; the cardinality cascade continues, and at L=19 the predicate is false.

**What I concede.** JE5 = PASS at L=14 at the central value is correct (central 7.2761e-23 ∈ [5.5e-23, 2.2e-22], 32.3% above floor; WP L387) — I do not contest the L=14 verdict, which is authoritative. And band-membership is a *better* permanence candidate than the raw point value (it is at least a yes/no invariant rather than a moving number). My claim is that it is STILL not a valid permanence carrier, because the invariant it asserts is false on the substrate's own trajectory at L≥19. A better predicate would be one provably truncation-invariant — e.g., a *ratio* or *cohomology-class* observable that the multiplicative-normalization cancellation leaves fixed (this is the RE-ANCHOR direction for sub-question d, and parallels how §VII.AV STATE-PROJ re-anchored to the truncation-INVARIANT `L_emp = -7.046336` via the Connes-Karoubi discharge — see V4).

**Position.** The band-membership predicate is not a valid permanence carrier under the quintic rise. It breaches the conjunct-upper at L_max=19 (Sage-QQ-exact). Permanence relocated to this predicate is permanence pinned to a finite compute window.

**Questions for mack:**
1. Do you accept the Sage-QQ-exact breach at L=19 (n_PBH/upper = 1.24709)? If yes, on what ground is a predicate that is FALSE at L=19 a "permanence" carrier? If no, identify the error in `R(n) = N_eigs(n)/N_eigs(14)` (the obs_2 producing-script factorization I verified residual-0-EXACT in §W4-3).
2. The §20 central-value-governs reading dismisses the −1σ LOWER-edge undershoot as truncation-resolution annotation. Apply the SAME logic to the UPPER edge: if edge-proximity is non-load-bearing, is band-membership itself load-bearing? And if band-membership IS the permanence carrier (your M1), how is it permanent when the central value (not an edge) exits the band at L=19?
3. Would you accept RE-ANCHORING Level-3 to a truncation-INVARIANT functional (the multiplicative-normalization-cancellation-fixed ratio, or a Connes-Karoubi cohomology-class anchor à la §VII.AV STATE-PROJ's `L_emp`) as the resolution that makes the permanence claim defensible — rather than HOLDING at STAGE-3-ELIGIBLE?

### V4: Cross-Cutting — substrate-property-vs-permanence + the §VII.AV precedent-propagation note

**Finding: The substrate-property nature of the divergence is what makes this NON-recoverable by annotation, and it is exactly the distinction the framework already drew in §VII.AV — where the STAGE-3-PERMANENT promotion landed on a truncation-INVARIANT anchor and the truncation-sensitive sibling was HELD at STAGE-1-CANDIDATE.**

#### Part A — Substrate-property vs permanence (the core cross-cut)

The whole of READING-B turns on one distinction, which I authored into the §W4-3 substrate-framing paragraph (WP L345) and now make explicit. There are two kinds of L_max-dependence a finite spectral-triple observable can carry:

- **Truncation-RESOLUTION fuzz** — the observable converges to a fixed `c_continuum`; finite L_max gives a finitely-resolved approximation; the residual `‖c_L − c_continuum‖` shrinks. The §20 annotation discipline correctly rules that this fuzz is non-load-bearing and must not veto a structural PASS. (Example: the §VII.W bridge, `L⁻³` envelope, converged.)
- **Substrate-IS DIVERGENCE** — the observable does not converge; `lim_{L→∞} = +∞`; the growth is intrinsic to the substrate's own structure (here, the infinite SU(3) representation ring driving `N_eigs(L) ~ (4/15)L⁵`). This is NOT fuzz around a fixed value; the value itself runs away.

The framework's permanence machinery (`joint-theorem-promotion.md` Stage 3; `cross-pillar-bridge-anatomy.md` Registry-PASS) was built for the FIRST kind. The W4-3 result proves n_PBH's cardinality channel is the SECOND kind. **A substrate-IS divergence cannot be annotated away as truncation-resolution fuzz, because it is a substrate PROPERTY, not a resolution artifact** — and the multiplicative-normalization-cancellation (`d²ln(w·κ)/d(ln K)²=0`, K-independent; WP L313) means the L_max dependence is structurally inseparable from the anchor, not a sub-leading correction. This is the substrate-first reading: the divergence is the substrate telling us its cardinality cascade has no natural cutoff at this pole, so a PERMANENT anchor must be sourced from a truncation-invariant functional of the substrate, not from the divergent cardinality count at an arbitrary L_max. Promoting to PERMANENT on the divergent count inverts substrate priority — it treats "where obs_2's loop stopped" as if it were a substrate-singled-out boundary, the container-thinking failure mode (`phononic-framing.md §"IS Space, Not IN Space"`): L_max is not a box the substrate sits in; it is a truncation of the substrate's own infinite Peter-Weyl structure.

#### Part B — The §VII.AV precedent (surfaced for S94; NOT re-opened here)

The framework ALREADY made this distinction correctly, ONE WAVE EARLIER, in the same session. This is not hypothetical — it is the controlling precedent, and it cuts AGAINST the §VII.AX.OP-PROJ promotion:

- **§VII.AV.STATE-PROJ → STAGE-3-PERMANENT** (gate `S93-W3-VII-AV-STATE-PROJ-STAGE-3-PERMANENT-PROMOTION`, line 62, `audit_sha256=adbc70042b8a36f9c58917527a282259002a75ecfbd2aa53c5dd952775de6006`). Its Level-3 anchor is `L_emp = -7.046336474406761 M_KK²` (line 50, W3-1) — a **truncation-INVARIANT** state-pair functional, discharged Level-2-binding via the Connes-Karoubi pairing (gate `S93-W3-4`, line 52: `L_CK_12 = -7.046054397904` vs `L_emp = -7.046336474407`, `residual_L12 = 2.820765e-04 < tol 1e-3`, `direction_decreasing=1`, prefactor 8/9 EXACT). That anchor CONVERGES — the residual shrinks toward a fixed `c_continuum = L_emp`. STAGE-3-PERMANENT is earned because the anchor is a fixed object.
- **§VII.AV.OP-PROJ → STAYS STAGE-1-CANDIDATE** (gate `S93-W3-6`, line 58: `OP-PROJ_stage3=STAGE-1-CANDIDATE-PENDING-S94-REVERIFY`; the OP-PROJ Axis-A leg FAILed on a Cell-I→Cell-II corner-cell correction, re-verify deferred to `CF-S94-VII-AV-OP-PROJ-STAGE-2-AXIS-A-REVERIFY`). The truncation-sensitive / unresolved sibling was HELD.

**The propagation note (for S94, NOT adjudicated here).** §VII.AV got the staging RIGHT: PERMANENT for the convergent truncation-invariant anchor, HELD for the unresolved one. §VII.AX.OP-PROJ did the OPPOSITE: it promoted to PERMANENT on a truncation-DIVERGENT anchor while its OWN wave's W4-3 gate proved the divergence. The inconsistency is internal to S93 and visible across two adjacent waves (W3 vs W4). Conversely — and this is the symmetric obligation I flag for S94, NOT to re-open §VII.AV here — IF W-1 (this workshop) downgrades §VII.AX.OP-PROJ on truncation-divergent-anchor grounds, then S94 MUST audit whether the §VII.AV.STATE-PROJ STAGE-3 anchor (`L_emp = -7.046336`) is *genuinely* truncation-invariant or merely truncation-RESOLVED-at-L12. My read of the W3-4 discharge (`residual_L12 = 2.82e-04`, decreasing, Connes-Karoubi-bound to a fixed `c_continuum`) is that §VII.AV.STATE-PROJ IS convergent and its promotion is SOUND — i.e., the precedent supports DOWNGRADING §VII.AX.OP-PROJ, not promoting it. But the audit-symmetry obligation stands: a downgrade rule keyed on "truncation-divergent Level-3 anchor" must be checked against every standing STAGE-3 promotion, and §VII.AV.STATE-PROJ + §VII.AU.OP-PROJ (line 31) + §VII.AW (line 101) + §VII.AY (line 99) are the standing set. The distinguishing test is sharp and Sage-checkable: **does the Level-3 anchor's residual-to-`c_continuum` SHRINK with L_max (convergent → PERMANENT-eligible) or does the anchor DIVERGE with L_max (→ HELD pending re-anchor)?** n_PBH fails this test (diverges, `+∞`); `L_emp` passes it (residual 2.82e-04 shrinking). That single test is the carry-forward I would register for S94.

**Position (cross-cut).** The divergence is a substrate property, not annotatable fuzz; the framework's own §VII.AV precedent shows the correct response (PERMANENT for convergent, HELD for unresolved); §VII.AX.OP-PROJ violates that precedent by promoting on the divergent anchor. The S94 audit-symmetry obligation (re-check standing STAGE-3 anchors against the shrink-vs-diverge test) is surfaced, NOT adjudicated here.

**Questions for mack:**
1. §VII.AV.STATE-PROJ earned STAGE-3-PERMANENT on a CONVERGENT anchor (`L_emp`, residual 2.82e-04 shrinking) and §VII.AV.OP-PROJ was HELD at STAGE-1-CANDIDATE pending re-verify — both decided ONE WAVE before §VII.AX. Why does the SAME framework, in the SAME session, promote §VII.AX.OP-PROJ to PERMANENT on a DIVERGENT anchor? What distinguishes n_PBH (diverges) from the §VII.AV.OP-PROJ sibling (held) such that the divergent one gets PERMANENT and the unresolved one gets HELD?
2. If the resolution is RE-ANCHOR (not HOLD), would you anchor §VII.AX.OP-PROJ's Level-3 to a truncation-INVARIANT functional — the multiplicative-normalization-cancellation-fixed ratio, or a Connes-Karoubi cohomology-class à la §VII.AV STATE-PROJ — exactly as §VII.AV STATE-PROJ did? That would make the permanence claim defensible AND restore consistency with the W3 precedent.

---

## Round 1 — mack: Response & Cross-Synthesis

### Part 1: Response to volovik's Sections

#### Re: V1 — STAGE-3-PERMANENT vs divergent point-anchor

**MISSED (the load-bearing distinction): STAGE-3-PERMANENT in the joint-theorem pathway is a tag on the THEOREM-STRUCTURE, not on a frozen number. The PERMANENT-and-PROVISIONAL pair is not a contradiction because the two predicates qualify two different objects.**

Let me answer your Question 1 directly, because it is the crux of V1 and you pre-empted my likely move by deferring it to V3. You ask: "under what reading is a value simultaneously PERMANENT and PROVISIONAL without contradiction?" My answer is NOT "permanence attaches to the predicate not the value" (you are right that that move is V3's, and I will fight V3 on its own terms below). My answer is structurally upstream of that: **the W4-5 PROVENANCE carries TWO tags on TWO objects, and the contradiction you allege only arises if you read them as both qualifying the same object.**

- `eligibility=STAGE-3-PERMANENT-ELIGIBLE` qualifies the **joint cross-axis theorem-structure** — the 5-anatomy bridge claim "Pillar I D_K cardinality-cascade-tail IS the laboratory-IN PBH band-edge image, via the substrate-clock-cancellation ∘ Friedrich-Bär ∘ HKR bridge map, with Level-2-binding to Pillar IX." `joint-theorem-promotion.md §"Stage 3"` is explicit: STAGE-3-PERMANENT means the theorem "joins the permanent-results table **alongside existing structural results (KO-dim=6, J-D_K=0, etc.)**." Those exemplars are STRUCTURAL THEOREMS, not numerical anchors. What Stage-2 PASS-AND certified (W4-1 Axis-A E2 PASS `2ab8bb1e…` ∧ W4-2 MULTI-PIN-ATLAS PASS `ba202d16…` ∧ S92 W-4 JE5 Axis-B PASS) is the *cross-axis reproducibility of the bridge structure*, clause by clause (Element 1, JOINT Element 3, JOINT Element 5). That is what is permanent.
- `provisional_truncation_note=True` qualifies the **canonical-L_max INDEX at which the Level-3 anchor's numerical value is currently read off** — L_max=14. The W4-3 INFO (`03b4fb35…`, sign=PASS/mag=INFO/regime=VALID) established, Sage-exact, that L_max=14 is not a substrate-singled-out saturation point. I do not contest one digit of W4-3; I am its downstream consumer in W4-5 and I encoded the tag deliberately.

These qualify different objects, so there is no contradiction in the W4-5 line: *the theorem-structure is permanent; the numerical anchor's canonical-truncation is provisional.* This is exactly the **(value, scheme) tuple discipline** that is permanent in my own corpus (S85 W1a F1): a framework prediction is never a bare number — it is a (value, evaluation-convention) pair, and scheme-dependence is a "permanent solution-space wall." n_PBH = (7.2761e-23, canonical-L_max=14-PROVISIONAL) is such a tuple. The number is provisional in its second coordinate; the *structural claim that the substrate's cardinality channel produces a PBH band-edge image at all* is what reached Stage-3.

**AGREE (and this is where I give ground to V1, because the honest steelman of READING-A is NOT "STANDS unchanged"):** your Question 2 — the candidate-qualifier-stripping hazard — is a REAL hazard and I will not wave it away. `joint-theorem-promotion.md §"Stage 3"` does say STAGE-3-PERMANENT is "eligible for citation as a structural theorem **without the candidate qualifier**," and if a downstream gate cites `n_PBH = 7.2761e-23` at face value as a permanent NUMBER (not as the (value, L_max=14-PROVISIONAL) tuple), and CF-S94 then re-determines the canonical truncation, that citation breaks. You have correctly identified that the qualifier-stripping the staging was built to prevent is, for the ANCHOR-VALUE specifically, NOT yet prevented by the current registry text.

But notice precisely what this concession does and does not buy you. It does NOT establish READING-B's conclusion (downgrade the whole entry to STAGE-3-ELIGIBLE). It establishes a NARROWER remedy: the Level-3 anchor row must carry a BINDING tuple-tag `(7.2761e-23, canonical-L_max=14-PROVISIONAL, re-determination=CF-S94)` so that "citation without the candidate qualifier" applies to the THEOREM-STRUCTURE (permitted) but NOT to the bare anchor number (forbidden until CF-S94). The qualifier the staging strips is the *STAGE-1-CANDIDATE* qualifier on the theorem; the qualifier that must NOT be stripped is the *PROVISIONAL-TRUNCATION* qualifier on the anchor's second coordinate. These are different qualifiers on different objects — the same structural separation as Question 1.

**EMERGES:** the live disagreement between us is therefore SHARPER and SMALLER than "STANDS vs DOWNGRADE." It is: does a truncation-divergent Level-3 anchor force a downgrade of the *theorem-structure TAG* (your V1), or only a tuple-tagging of the *anchor's second coordinate* with the theorem-structure staying STAGE-3-PERMANENT (mine)? The Stage-2 PASS-AND machinery you concede is genuine (V1 ¶5) audits the theorem-structure and is silent on anchor-convergence — we agree on that orthogonality. Where we diverge is the consequence: you read "Stage-2 is silent on convergence" as "so the PERMANENT tag is unearned"; I read it as "so the PERMANENT tag was earned on the axis Stage-2 tests (structure), and the convergence question lives on a DIFFERENT, anchor-coordinate axis that demands a tuple-tag, not a tag-downgrade." That is the open seam for R2.

#### Re: V2 — canonical-L_max provisionality

**AGREE on the vacuous-vs-meaningful distinction; DISAGREE that it forces STAGE-3-ELIGIBLE rather than RE-ANCHOR — and your V2-Q1 exposes a seam I must own honestly.**

You ask (V2-Q1): "Name the `c_continuum` the Level-2 envelope on §VII.AX.OP-PROJ binds distance to." I will answer this straight, including the part that hurts READING-A, because the alternative is convention-shopping and I won't do that.

There are TWO distinct functionals in the §VII.AX record that both evaluate to ≈7.2761e-23, and the `c_continuum` question has a different answer for each. This is not a dodge; it is the actual structure, and I verified it Sage-exact this dispatch:

- **The registered Level-1 form** (registry L19407-19412): `n_PBH = n_edge_saturated · prob_form / L_pix_LRD³` with `n_edge_saturated = C(N_eigs, 2) = C(78080, 2) = 3,048,204,160` at the **L_max=10 base atlas** (N_eigs FIXED at 78,080), SATURATED at cascade-generation `g ≥ g_saturate = 143`. This is the *g-saturated* form. Its "saturation" is in the **cascade-generation index g**, not in L_max — the registry Level-1 theorem is the g-INDEPENDENCE theorem (2^g and L_pix(g)³ cancel exactly under the IS-not-IN substrate-clock convention).
- **The W4-3 obs_2 producing-script form**: `n_PBH(L) = A_prefactor · N_eigs(L)`, LINEAR-in-N_eigs, A_prefactor = 1.758127e-23/78080 = 2.2517e-28 m⁻³. This is the form whose 4.14× refinement scan (L_max=10 → L_max=14) pulled the central from the 1.758e-23 baseline into the upper-22.6%-conjunct. This is the form that DIVERGES (your V3 trajectory).

Now the honest part you have NOT yet pressed, and I will not hide it: **C(N_eigs(L), 2) is degree-10 in L** (Sage: `deg = 10`; N_eigs is the quintic, the pair-count squares it). So if one lets L_max grow in the registered Level-1 form too, IT diverges as well — faster than the linear form. The registered Level-1 form is "saturated" only because it FIXES N_eigs at the L_max=10 atlas value (78,080) and saturates the SEPARATE index g; it is NOT L_max-truncation-invariant. So I cannot answer V2-Q1 by claiming "the registered form converges where the obs_2 form diverges." Both diverge in L_max. **On the L_max axis, you are correct that no `c_continuum` exists for the cardinality channel; the Level-2 envelope as currently written bounds distance to a finite-L_max snapshot, not to a continuum.** That is a Level-2-non-binding situation *on the L_max axis* in the rule's own taxonomy, exactly as you argue in V2 ¶5.

Where I hold the line (DISAGREE on the consequence): this does NOT make the criterion "vacuously satisfiable only." It makes the criterion SATISFIED ON THE WRONG AXIS. The registry Level-1 theorem's convergence is in **g** (cascade-generation), and THAT limit exists and is the substance of the g-independence theorem — `n_PBH(g) → n_edge_saturated · prob_form / L_pix_LRD³` as `g → g_saturate=143`, a fixed object, with `c_continuum(g) = 7.2761e-23 · (refinement-frozen)`. The defect W4-3 surfaced is that the **canonical-L_max index was conflated with the saturation index**: the entry reads off its number at L_max=14 (a free L_max snapshot) when the substance of the theorem is g-saturation at FIXED N_eigs(L_max=10 atlas). The "canonical L_max=14" label is the circular operand you name in V2-Q1 — it is canonical because obs_2 evaluated there, and obs_2 evaluated there because the master cache reaches there. I concede the circularity in the L_max coordinate.

**Your V2-Q2 — into which §20 category does a quintic-DIVERGENT value fall? — is the sharpest question in the whole opening, and my answer is: NEITHER, and you are right that §20 does not cover it.** I read the §20 rule verbatim this dispatch:

> "such credentialing is ADMISSIBLE ONLY when the band is a substrate-IS or laboratory-IN PHYSICAL band that survives L_max → ∞; a Friedrich-Bär (or any) TRUNCATION-uncertainty envelope at finite L_max can NEVER be credentialed as a PASS gate."

§20 partitions Level-3 annotations into (A) physical bands that survive L→∞ [credentialable] and (B) truncation-resolution envelopes [never credentialable]. A quintic-DIVERGENT central value is in NEITHER cell: it survives no L→∞ limit (so not (A)), yet it is not resolution-fuzz around a fixed value (so not (B)). It is a THIRD case — substrate-IS divergence — and §20 (which I co-authored ON this entry at K=1) was written for the convergent design case. **Invoking §20's "central-value-governs" to dismiss the −1σ lower-edge undershoot was correct as far as it went (the lower-edge proximity at L=14 genuinely IS resolution-fuzz); but §20 cannot be stretched to license treating the central value's L_max-DIVERGENCE as if it were resolution-fuzz.** You have correctly diagnosed that the §20 escape hatch has a hole exactly the shape of a divergent central.

**What this does NOT concede:** that the remedy is HOLD-at-STAGE-3-ELIGIBLE. The §20-doesn't-cover-this finding points to RE-ANCHOR, not downgrade — because the theorem-structure (g-independence, Stage-2-verified) is sound; what is mis-sourced is the Level-3 NUMERICAL anchor's coordinate (L_max-snapshot instead of a truncation-invariant functional). The registry-PASS criterion "Level-3 < Level-2 at canonical L_max" needs a substrate-singled-out evaluation point, agreed — but the fix is to anchor Level-3 to a functional that HAS one (the g-saturated form at FIXED atlas, or a ratio/cohomology-class observable per your V3-Q3), not to demote the cross-axis theorem the bridge structure earned. That is the seam I carry to R2: SATISFIED-ON-WRONG-AXIS ⇒ RE-ANCHOR, vs your NOT-MEANINGFULLY-SATISFIED ⇒ HOLD.

#### Re: V3 — band-membership permanence carrier

**AGREE — the L=19 breach is real, Sage-QQ-exact, and I confirm it independently. "Holds at every computed truncation" is NOT a valid permanence carrier, and I concede that specific carrier. This is the genuine concession of Round 1.** I will not steelman a predicate I have just verified is false.

**V3-Q1 answered first, with my own Sage-QQ-exact re-derivation** (I re-ran your trajectory rather than take it on faith, per the substrate-first discipline — your number is INTENT, the recomputation is what I bind to):

```
n_PBH(L) = central14 · N_eigs(L)/N_eigs(14),  central14 = 72761/10²⁷,  upper = 22/10²³  [all QQ-exact]
  L=16 : n_PBH/upper = 0.587132   ∈ band  ✓
  L=17 : n_PBH/upper = 0.764486   ∈ band  ✓
  L=18 : n_PBH/upper = 0.982288   ∈ band  ✓   margin below upper = 0.0177121  (1.77%)
  L=19 : n_PBH/upper = 1.247090   ∉ band  ✗   excess above upper = 0.247090   (24.71%)  BREACH
  N_eigs(18)=959728,  N_eigs(19)=1218448  [Sage-exact integers]
```

My recomputation reproduces your V3 table to all printed digits: L=18 last in-band (1.77% margin, your "1.77%"), first breach at L=19 with n_PBH/upper = 1.24709 (your "1.24709", 24.7% above). **I accept the breach at L=19 without qualification.** There is no error in your `R(n) = N_eigs(n)/N_eigs(14)`; the factorization is residual-0-EXACT (W4-3 STEP-2) and I verified the QQ-coercion straddling the L=18/19 wall to avoid the float-precision trap you correctly flagged.

So: on what ground is a predicate FALSE at L=19 a "permanence carrier"? **It is not.** The predicate `n_PBH_central(L) ∈ [5.5e-23, 2.2e-22] ∀ computed L` is a finite-window coincidence, exactly as you say. I will not relocate permanence onto it. The "every computed truncation" framing sets the bar at the compute frontier (obs_2 stopped at L=16), and pinning permanence to "wherever we have computed so far" makes permanence a function of compute budget — your phrasing, and it is correct. **M1 (my original section) must therefore NOT rest READING-A on the band-membership predicate.** I am revising my own position in response to V3: M1 will defend permanence of the THEOREM-STRUCTURE via the (value, L_max) tuple, NOT the band predicate. You have forced that, and it makes READING-A more honest.

**V3-Q2 — apply the §20 lower-edge logic to the upper edge:** I accept the symmetry obligation you impose. If the −1σ LOWER-edge undershoot at L=14 is "non-load-bearing truncation-resolution annotation," I cannot then treat the band as a load-bearing PASS gate at the top. You are right that the asymmetry was inconsistent. But the resolution of the inconsistency is NOT "band-membership is the permanence carrier and it fails at L=19" (that concedes to your downgrade); it is "**band-membership was NEVER the right permanence carrier in either direction** — neither the lower-edge proximity nor the upper-edge breach belongs in a permanence claim, because BOTH are L_max-trajectory artifacts of a divergent channel." The §20 central-value-governs reading does protect the L=14 verdict (central 7.2761e-23 ∈ band, 32.3% above floor — authoritative, you concede this). What §20 does NOT do, and what I now grant, is license band-MEMBERSHIP as a *truncation-invariant* permanence statement. The central value itself crosses the wall at L=19; that is a divergence question, not an edge/annotation question. Conceded.

**V3-Q3 — would I accept RE-ANCHORING to a truncation-INVARIANT functional rather than HOLDING at STAGE-3-ELIGIBLE?** This is the most productive question in the workshop and my answer is a qualified YES — but I hold it as the basis for DISAGREEMENT on the verdict, not agreement. Here is the precise difference between us that survives all my concessions above:

- We AGREE: the band-membership predicate is not a permanence carrier (V3); §20 does not cover a divergent central (V2); the L_max=14 canonical index is provisional/circular (V2); the Stage-2 PASS-AND audits structure not anchor-convergence (V1).
- We DISAGREE on the verdict that follows. **You (V3/V4): the right move is DOWNGRADE to STAGE-3-ELIGIBLE, pending CF-S94, because the anchor is not yet a fixed object. Me: the right move is RE-ANCHOR Level-3 to a truncation-invariant functional WHILE the theorem-structure stays STAGE-3-PERMANENT, because Stage-2 PASS-AND earned permanence for the bridge STRUCTURE on the axis it tests, and the divergence is a defect in the anchor's COORDINATE (L_max-snapshot), not in the cross-axis theorem.**

The substantive reason I prefer RE-ANCHOR over HOLD: a STAGE-3-ELIGIBLE downgrade treats the whole joint theorem as un-promoted, which throws away the genuine Stage-2 cross-axis verification (two reviewers, opposite axes, without workshop context, PASS-AND on Elements 1/3/5). That verification is INDEPENDENT evidence per `epistemic-discipline.md §"What Counts as Evidence"` and `joint-theorem-promotion.md` — it does not evaporate because the Level-3 NUMBER's coordinate is provisional. The honest bookkeeping is: theorem-structure PERMANENT (Stage-2-earned); Level-3 anchor carries `(value, canonical-L_max=PROVISIONAL, re-anchor-target=CF-S94)`; and the re-anchor target is a truncation-invariant functional. **Which functional** is the open question I pose back to you in M3 — because here I think you may be slightly too quick: the obvious candidate (a multiplicative-normalization-cancellation-fixed RATIO) needs to actually FIX the n_PBH magnitude, and I am not yet convinced the cancellation-invariant ratio carries the *dimensional* PBH number-density content rather than a dimensionless shape. The §VII.AV.STATE-PROJ `L_emp` parallel you cite (V4) is a log-derivative — dimensionless by construction — so the parallel may not transfer cleanly to a dimensionful m⁻³ anchor. That is a real open sub-problem, not a rhetorical hedge.

**EMERGES:** the band-membership carrier is dead (we agree). The live question is RE-ANCHOR (me) vs HOLD (you), and underneath it sits a concrete unsolved problem — *does a truncation-invariant functional exist that fixes the dimensionful n_PBH magnitude, or only a dimensionless ratio?* If NO such dimensionful invariant exists, your HOLD is correct and I concede the verdict. If one exists, RE-ANCHOR is correct and the PERMANENT tag on the theorem-structure survives. That is a Sage-checkable question for R2/R3, and it is the right hinge for the verdict.

#### Re: V4 — substrate-property / §VII.AV precedent

**AGREE that the divergence is a substrate PROPERTY, not annotatable fuzz (Part A); AGREE that §VII.AV is the controlling precedent and your shrink-vs-diverge test is the right diagnostic; DISAGREE that the precedent supports DOWNGRADE rather than RE-ANCHOR — the precedent is §VII.AV.STATE-PROJ, and what it did was anchor to a convergent functional, which is RE-ANCHOR, not HOLD.**

**Part A — substrate-property vs resolution-fuzz.** I concede this distinction entirely, and it is the correct substrate-first reading. The W4-3 substrate-framing paragraph (WP L345, your authorship) is right: `N_eigs(L_max)` is the Peter-Weyl block-admission cardinality of D_K's OWN spectrum; the SU(3) representation ring is infinite; extending L_max reveals MORE of the substrate's intrinsic cardinality cascade. The divergence `lim_{L→∞} N_eigs = +∞` is a substrate property, not a model-incompleteness artifact. This is precisely the IS-not-IN reading (`phononic-framing.md`): L_max is not a container the substrate sits in — it is a truncation of the substrate's own infinite structure. I verified the degree-5 growth bit-exact (N_eigs(14)=323136, N_eigs(15)=434112, N_eigs(16)=573648, matching obs_2). And I verified — this dispatch, and I flag it because it SHARPENS your Part A beyond what you stated — that the **registered Level-1 form's pair-count `C(N_eigs(L), 2)` is degree-10 in L**, so the cardinality channel diverges in L_max under BOTH the linear obs_2 form AND the registered C(N_eigs,2) form. The substrate-property divergence is even more robust than your V4 argues: it is not specific to the obs_2 linearization. The cardinality cascade has no L_max cutoff in either functional form. I grant this fully.

**The implication I draw differs from yours, and here is the substitution chain (per `math-scripts.md §"Double-Check Logic"`) that produces it:**

```
Claim: substrate-property-divergence ⇒ RE-ANCHOR the Level-3 NUMBER, NOT downgrade the THEOREM tag.
 Step 1: STAGE-3-PERMANENT tag attaches to       = the joint cross-axis theorem-structure
         (joint-theorem-promotion.md §"Stage 3": "alongside KO-dim=6, J-D_K=0" — structural theorems)   [Re:V1]
 Step 2: Stage-2 PASS-AND certified              = Elements 1/3/5 cross-axis reproducibility
         (W4-1 2ab8bb1e ∧ W4-2 ba202d16 ∧ S92 JE5)                                                       [verdict file L67,73]
 Step 3: the divergence is located in            = the Level-3 ANCHOR's L_max coordinate (a number's index)
         (W4-3: N_eigs(L) divergent; L_max=14 not substrate-singled-out)                                  [verdict L70]
 Step 4: Step-2 axis (structure) ⊥ Step-3 axis (anchor-convergence)
         (you concede this orthogonality, V1 ¶5; I concede §20 doesn't bridge it, Re:V2)
 Step 5: tag(structure) is earned on Step-2 axis; defect(anchor) lives on Step-3 axis
         ⇒ remedy targets Step-3 (re-anchor the number) WITHOUT vacating Step-2 (the tag)
 Conclusion: RE-ANCHOR Level-3 to a truncation-invariant functional; theorem-structure stays STAGE-3-PERMANENT.
```

The downgrade-to-ELIGIBLE move conflates Step-2 and Step-3: it vacates the structure-tag because of an anchor-coordinate defect. That is the inverse of the conflation you (correctly) accuse the original promotion of — the promotion read "structure verified" as "anchor permanent"; the downgrade reads "anchor provisional" as "structure un-verified." Both conflate the two axes you yourself separated.

**Part B — the §VII.AV precedent. I accept it as controlling, and I accept the audit-symmetry obligation in full.** Let me state what I verified on disk (not from your summary — from the verdict file and registry):

- **§VII.AV.STATE-PROJ → STAGE-3-PERMANENT** (verdict line `S93-W3-VII-AV-STATE-PROJ-STAGE-3-PERMANENT-PROMOTION`, PASS, `adbc7004…`, on W3-6 Stage-2 clean PASS-AND `610d1ac8…`). Its Level-3 anchor is `L_emp = -7.046336474406761 M_KK²`, Connes-Karoubi-discharged at residual `2.82e-04 < 1e-3`, decreasing toward fixed `c_continuum`. CONVERGENT. STAGE-3-PERMANENT earned because the anchor is a fixed object.
- **§VII.AV.OP-PROJ → STAYS STAGE-1-CANDIDATE** (the OP-PROJ Axis-A leg FAILed a Cell-I→Cell-II corner correction; re-verify deferred to CF-S94). HELD.

Your shrink-vs-diverge test — *does the Level-3 anchor's residual-to-`c_continuum` SHRINK (→ PERMANENT-eligible) or DIVERGE (→ HELD pending re-anchor)?* — is a SHARP, Sage-checkable diagnostic and I adopt it. n_PBH (linear form) fails it (`+∞`); n_PBH (C(N_eigs,2) form) fails it harder (degree-10); `L_emp` passes it (residual shrinking). **On this test, you are right that §VII.AX.OP-PROJ's CURRENT Level-3 anchor is in the DIVERGE bucket.** I concede that cleanly.

**But read what §VII.AV.STATE-PROJ actually DID, because it is the precedent and it cuts toward RE-ANCHOR:** §VII.AV.STATE-PROJ did not get STAGE-3-PERMANENT by leaving a divergent anchor in place; it got it by anchoring Level-3 to a TRUNCATION-INVARIANT functional (`L_emp`, a log-derivative, Connes-Karoubi-bound). That is the RE-ANCHOR operation. The precedent's lesson is therefore: "to reach STAGE-3-PERMANENT, anchor Level-3 to a truncation-invariant functional." §VII.AX.OP-PROJ should DO THAT — not be frozen at STAGE-3-ELIGIBLE with a divergent anchor. The §VII.AV.OP-PROJ HOLD is a DIFFERENT situation: it was held because its Axis-A leg FAILED Stage-2 (a corner-cell error), NOT because its anchor diverged. §VII.AX.OP-PROJ's Axis-A leg PASSED Stage-2 (W4-1 `2ab8bb1e…`). So the two §VII.AV siblings give us two distinct precedents: STATE-PROJ (Stage-2 PASS + convergent anchor → PERMANENT via re-anchoring) and OP-PROJ (Stage-2 FAIL → HELD). §VII.AX.OP-PROJ matches STATE-PROJ on the Stage-2 axis (PASS) but currently fails it on the anchor axis (divergent). The precedent-consistent fix is to make it match STATE-PROJ on BOTH axes — i.e., RE-ANCHOR — not to treat a Stage-2-PASS theorem as if it were the Stage-2-FAIL sibling.

**Your V4-Q1 — what distinguishes n_PBH (diverges, gets PERMANENT) from §VII.AV.OP-PROJ (held)?** The honest answer: the distinguishing fact is NOT in their favor at the anchor level (n_PBH diverges, which is worse than AV.OP-PROJ's situation on the convergence axis). The distinguishing fact is at the Stage-2 level: §VII.AX.OP-PROJ's cross-axis verification PASSED (both legs, both axes), while §VII.AV.OP-PROJ's did not. That is why I argue §VII.AX.OP-PROJ's theorem-STRUCTURE is permanent-eligible while its ANCHOR is not yet — and the remedy is anchor-level (re-anchor), not structure-level (downgrade). I concede that the CURRENT registry text, by promoting on the divergent anchor without the re-anchor, is in tension with the §VII.AV.STATE-PROJ precedent. That tension is real and internal to S93. Where I differ is the repair: bring the anchor up to the STATE-PROJ standard (RE-ANCHOR), rather than bring the tag down (HOLD).

**V4-Q2 — would I re-anchor à la §VII.AV.STATE-PROJ (Connes-Karoubi cohomology-class)?** Yes, in principle — and I flag the one place I am NOT yet convinced it transfers, which is the substantive open problem I carry to M3 and R2. `L_emp = -7.046336 M_KK²` is a SECOND LOG-DERIVATIVE (`d² ln Var/d(ln K)²`) — it is dimensionless-by-construction (a log-derivative kills multiplicative normalization, which is exactly why the multiplicative-normalization-cancellation makes it L_max-invariant). n_PBH is a DIMENSIONFUL number density (m⁻³). A multiplicative-normalization-cancellation-fixed RATIO would be dimensionless too — it would fix the SHAPE of the cardinality cascade, not the dimensionful m⁻³ magnitude. So the §VII.AV.STATE-PROJ re-anchor route may give us a truncation-invariant *ratio* that does NOT by itself pin 7.2761e-23 m⁻³. The dimensionful magnitude requires a substrate-physical scale-anchor (L_pix_LRD, prob_form) that is itself L_max-independent — and whether the COMBINATION is truncation-invariant is the open sub-problem. If it is, RE-ANCHOR succeeds and the PERMANENT tag survives. If the only truncation-invariant is a dimensionless ratio and the dimensionful magnitude is irreducibly L_max-tied, then your HOLD is correct and I will concede the verdict in R3. I am not pre-conceding it, because I have not yet seen the proof either way — but I am naming the exact Sage-checkable hinge.

**The S94 audit-symmetry obligation I accept as a carry-forward** (NOT adjudicated here, agreeing with your scoping): a downgrade-or-re-anchor rule keyed on "truncation-divergent Level-3 anchor" must be checked against every standing STAGE-3 promotion {§VII.AV.STATE-PROJ, §VII.AU.OP-PROJ, §VII.AW, §VII.AY, §VII.AH, §VII.U.2 Corner-II Var_a}. Your shrink-vs-diverge test is the right instrument for that audit. I will register it in the carry-forward block.

### Part 2: Original Analysis

#### M1: Permanence attaches to the joint-theorem STRUCTURE carried as a (value, canonical-L_max) tuple — NOT the band-membership predicate (which V3 killed) and NOT the bare point value

**Section-title correction up front (fix-in-session, honesty discipline):** the placeholder title for this section read "permanence attaches to the band-membership predicate." After verifying volovik's V3 breach Sage-QQ-exact (L=19, n_PBH/upper=1.24709), I am NOT defending that. The band-membership predicate is a finite-window coincidence, dead as a permanence carrier. M1 defends a DIFFERENT carrier, and I am changing the position in response to the adversarial finding rather than defending a refuted one. That is the workshop working as designed.

**The carrier that survives V1–V4: the joint cross-axis theorem-STRUCTURE, with the Level-3 anchor carried as a (value, canonical-L_max=PROVISIONAL) tuple.**

The structural claim is this. In the 4-stage joint-theorem pathway (`joint-theorem-promotion.md`), STAGE-3-PERMANENT is reached when a joint cross-axis theorem's clauses pass two independent cross-reviewers operating without workshop context (Stage-2 PASS-AND). The OBJECT promoted is the theorem — and `joint-theorem-promotion.md §"Stage 3"` fixes the reference class explicitly: it "joins the permanent-results table alongside existing structural results (KO-dim=6, J-D_K=0, etc.)." Those exemplars share a property: **they are structural theorems whose content is an identity/relation, not a single empirical number frozen at a truncation.** KO-dim=6 is a mod-8 index fact; [J,D_K]=0 is an operator identity (machine-ε); g1/g2 = e^{-2τ} is a functional relation. None of them is "a number at L_max=N." When §VII.AX.OP-PROJ joins that table, what joins is the *bridge-structure claim*: "Pillar I D_K cardinality-cascade-tail IS the laboratory-IN PBH band-edge image, via the substrate-clock-cancellation ∘ Friedrich-Bär ∘ HKR bridge map, Level-2-binding to Pillar IX, with the 5-anatomy and the algebra-axis Cell-I classification." That claim is what Stage-2 verified clause-by-clause (Element 1 substrate-IS observable; JOINT Element 3 bridge map; JOINT Element 5 anchor-structure; Element 2 OE-form; substrate-input-orthogonality at obs_2). The cross-axis reproducibility of THAT is the evidence, and it is independent evidence per `epistemic-discipline.md §"What Counts as Evidence"`.

**Why the Level-3 anchor is a tuple, not a number — and why this is a permanent discipline, not an ad-hoc rescue.** My own corpus carries this as a load-bearing rule (S85 W1a F1, in MEMORY.md): "Framework predictions are (value, scheme) tuples; scheme dependence is a permanent solution-space wall; tag every downstream prediction." n_PBH is no exception. Its honest form is `(7.2761e-23 m⁻³, canonical-L_max=14-PROVISIONAL, re-anchor-target=CF-S94)`. The first coordinate is the substrate-current best central value (T1.13 PASS, authoritative at L=14, which volovik concedes). The second coordinate is the evaluation convention, and W4-3 proved that convention is provisional (no substrate-singled-out L_max). A permanent theorem-structure can carry a provisional-second-coordinate anchor without contradiction, exactly as a permanent functional relation `g1/g2 = e^{-2τ}` carries a τ that is evaluated at a chosen slice — the RELATION is permanent; the SLICE-VALUE is a coordinate.

**What this carrier does NOT claim (the honest boundary, so I am not accused of smuggling the dead predicate back in):**
1. It does NOT claim band-membership is truncation-invariant. It is not (V3, conceded).
2. It does NOT claim the L_max=14 NUMBER is permanent. It is not (W4-3, conceded).
3. It does NOT license a downstream gate citing `7.2761e-23` as a bare permanent number. That citation is FORBIDDEN until the tuple's second coordinate is discharged (CF-S94) — and the registry text must be hardened to enforce this (the candidate-qualifier-stripping hazard volovik raised in V1-Q2, which I accept as a real gap in the current text).

**The positive structural content M1 adds:** the divergence W4-3 found is not a defect in the *theorem* — it is a discovery about the *cardinality channel*, and it is a substrate-IS fact worth registering in its own right. The Peter-Weyl block-admission count `N_eigs(L_max)` is a genuinely divergent functional of the substrate's infinite representation ring (degree-5; and its pair-count C(N_eigs,2) is degree-10 — I verified both Sage-exact). This means **the n_PBH magnitude must be sourced from a functional that is L_max-stationary, and the registry Level-1 theorem already names one in disguise:** the g-INDEPENDENCE theorem (registry L19432) holds N_eigs FIXED at the L_max=10 base atlas (78,080) and saturates the SEPARATE cascade-generation index g. The substance of the Level-1 theorem is g-saturation at fixed atlas, NOT L_max-saturation. The error that W4-3 exposed is that the entry's Level-3 row reads its number at a *free L_max snapshot* (L=14, via the 4.14× refinement scan) rather than at the *fixed atlas* the Level-1 theorem actually quantifies over. The RE-ANCHOR target is therefore not exotic: it is to evaluate the Level-3 anchor at the SAME truncation the Level-1 theorem is stated at (the L_max=10 atlas, N_eigs FIXED), with the cascade-generation saturation supplying the convergence, and to treat the 4.14× refinement as a separate (provisional) resolution-improvement rather than as the canonical anchor.

That re-sourcing keeps the theorem-structure STAGE-3-PERMANENT (Stage-2-earned), fixes the anchor to the functional the Level-1 theorem actually quantifies, and is the §VII.AV.STATE-PROJ-consistent move (anchor to the truncation-invariant functional). It is RE-ANCHOR, and it is why I hold the verdict at RE-ANCHOR rather than conceding HOLD — subject to the one open sub-problem (M3-Q3 / Re:V4-Q2): whether the fixed-atlas dimensionful magnitude is genuinely L_max-stationary or merely L_max-FROZEN-by-fiat. If frozen-by-fiat (i.e., the atlas choice is as arbitrary as L=14), then RE-ANCHOR collapses into HOLD and volovik wins the verdict. That is the Sage-checkable hinge.

#### M2: The §W6-3 Stage-2 PASS-AND discharges the eligibility conjunction for the THEOREM-STRUCTURE — and that is genuine independent evidence, distinct from anchor-convergence

**The eligibility conjunction, on disk, verified this dispatch (verdict file, not summaries):**

```
§VII.AX.OP-PROJ STAGE-3-PERMANENT eligibility
   = (a) W4-1 Axis-A E2 re-emission PASS    [verdict L67: axis_a_composite=PASS, emit_bug_confirmed=True, 2ab8bb1e…]
   ∧ (b) S92 W-4 JE5 PASS at central-value  [Axis-B; Eq.(2′) central-value reading; the prior FAIL RETIRED-NOT-OVERTURNED via Option-A]
   ∧ (c) Eq.(2′) registry-text correction landed  [verdict L75: S93-W4-4, 03d92b2a…]
   + sibling W4-2 MULTI-PIN-ATLAS Stage-2 PASS-AND  [verdict L73: ba202d16…, stage3=STAGE-3-PERMANENT-ELIGIBLE]
```

I want to be precise about what each conjunct establishes and — crucially — what it does NOT, because volovik's V1 ¶5 correctly observes the Stage-2 machinery audits theorem-structure and is silent on anchor-convergence. I AGREE with that orthogonality. M2's job is to show that the orthogonality cuts in BOTH directions: Stage-2 being silent on convergence does not make the Stage-2 PASS-AND worthless — it makes it evidence on a DIFFERENT axis (structure), which is exactly the axis STAGE-3-PERMANENT is about.

**Conjunct (a) — W4-1 Axis-A E2 PASS.** This was a genuine verdict-FIELD correction, and I checked its provenance is honest, not a relabel. The S92 Axis-A run emitted FAIL because the OE-form regex was a narrow bare-only form that spuriously rejected the brace-delimited subscript `Tr_{M_PBH-mass}(P_{PBH-mass} · ρ_BH(x))` (WP L53-59). The corrected classifier is negation-aware and the discriminator test confirms a genuinely un-formed OE expression still classifies False (WP L55). The PASS is licensed by reading the live registry §VII.AX.OP-PROJ block against the AUTHORITATIVE `ELEMENT_2_OE_POSITIVE_REGEX` in `_cross_pillar_bridge_audit.py`, with the negative prose-only match absent. This is a methodology-layer F-image correction (the Element-2 *form* was always compliant), not convention-shopping. The original FAIL is RETAINED on disk (Option-A, absolute verdict permanence); the corrective line supersedes it. **What (a) establishes:** the Axis-A cross-reviewer (connes-ncg-theorist, writer-of-record continuity) independently PASSes the OE-form clause + Element 1 + JOINT Element 3 + JOINT Element 5 + substrate-input-orthogonality. **What (a) does NOT establish:** anything about whether N_eigs(L_max) saturates. Correct — it is a structure clause.

**Conjunct (b) — S92 W-4 JE5 PASS at central-value.** The canonical Registry-PASS criterion (`cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"`) is a SINGLE-VALUE inequality `Level-3 < Level-2 at canonical L_max`; the central `n_PBH(L=14) = 7.2761e-23 ∈ conjunct [5.5e-23, 2.2e-22]` (32.3% above floor) ⇒ JE5 = PASS. volovik concedes this L=14 verdict is authoritative (V3 ¶"What I concede"). The −1σ lower-edge undershoot (5.316e-23 < 5.5e-23) is the non-load-bearing Friedrich-Bär truncation-resolution annotation per §20 — and I now grant (Re:V3-Q2) that the SAME §20 logic forbids treating band-membership as load-bearing at the upper edge too. **What (b) establishes:** the central anchor at L=14 satisfies the single-value criterion. **What (b) does NOT establish:** that L=14 is the right L_max, or that the central stays in-band at L≥19. Conceded — that is the W4-3 / V3 axis.

**Conjunct (c) — Eq.(2′) landed.** I landed this in-session as registry sole-writer (S93 W4-4): I corrected the internally-inconsistent "both edges INSIDE" Level-3 statement (Class-(i) `INTERNALLY-INCONSISTENT-LEVEL-3-BAND-STATEMENT` — the −1σ edge falsifies full-band-containment) to the central-value reading + the non-load-bearing annotation, at four registry locations, and flipped Status to STAGE-3-PERMANENT-ELIGIBLE. The Class-(i) detector now returns `no_band_containment_claim_present` on the block (verified). **What (c) establishes:** the registry text no longer asserts a self-contradictory band-containment gate. **What (c) does NOT establish:** that the central is truncation-stable. Conceded.

**The structural point M2 adds — Stage-2 PASS-AND is independent evidence, and the workshop must not let the divergence finding erase it.** The two cross-reviewers (Axis-A connes-ncg-theorist on the spectral side; Axis-B via the S92 W-4 JE5 central-value verdict, volovik) operated on opposite axes. For the MULTI-PIN-ATLAS sibling (W4-2), the PASS-AND was at the substrate-input-orthogonality STRUCTURAL CEILING with NO caveat (verdict L73: `substrate_input_orthogonality_obs_2_floor=True_structural_ceiling_NO_caveat=True`). Per `joint-theorem-promotion.md` and `epistemic-discipline.md §"What Counts as Evidence"`, agreement between cross-reviewers who never shared workshop context IS structurally independent confirmation — it is precisely NOT the "agreement among agents" that the exclusion (epistemic-discipline §"What Does NOT Count") forbids, because the shared-context failure mode is broken by construction. **A STAGE-3-ELIGIBLE downgrade discards this independent cross-axis verification.** That is the cost volovik's READING-B has not yet priced: HOLD does not just defer a number — it un-promotes a theorem whose cross-axis structure was independently verified, on the grounds of a defect (anchor-divergence) that lives on an axis the verification was never claiming to cover.

**The honest counter I must pre-empt (so M2 is not an overclaim):** volovik can reply — and I expect he will in R2 — that a joint theorem whose Level-3 EMPIRICAL ANCHOR (Element 5) is divergent has not actually satisfied JOINT Element 5, because Element 5 is "empirical satisfaction at canonical L_max" and a divergent anchor has no canonical L_max. This is the strongest form of READING-B and it targets exactly the conjunct (JOINT Element 5) that the orthogonality argument tries to protect. My answer is the M1 carrier: JOINT Element 5 was verified as the anchor-STRUCTURE (the 5-anatomy Element-5 form `n_PBH = n_edge_saturated · prob_form / L_pix_LRD³` with the upper-22.6%-conjunct membership at the canonical evaluation), and the Stage-2 reviewers verified that structure reproduces cross-axis. Whether the canonical evaluation point is L=14 or the fixed L_max=10 atlas is the RE-ANCHOR question — it changes the anchor's coordinate, not the Element-5 structural form the reviewers checked. **But I concede this is the live pressure point: if the reviewers' Element-5 PASS is read as certifying the NUMBER 7.2761e-23 at L=14 (rather than the anchor-structure), then a divergent anchor does retroactively weaken that conjunct, and the verdict tilts toward HOLD.** Which reading of "Element-5 PASS" is correct — anchor-structure (my M1) or anchor-number (volovik's likely R2) — is the second hinge of the verdict, alongside the dimensionful-invariant existence question (Re:V4-Q2). I hold RE-ANCHOR; I do not pre-concede; and I have named both hinges precisely.

#### M3: Questions for volovik

After R1 we agree on more than the opening framing suggested: band-membership is dead as a permanence carrier (V3); §20 does not cover a divergent central (V2); L_max=14 is a circular/provisional index (V2); the divergence is a substrate property robust across both the linear and the C(N_eigs,2) forms (V4, sharpened). The live seam is RE-ANCHOR (me) vs HOLD (you). These five questions target that seam.

**M3-Q1 (the dimensionful-invariant hinge — the one I flagged in Re:V4-Q2).** §VII.AV.STATE-PROJ re-anchored to `L_emp = -7.046336 M_KK²`, a SECOND LOG-DERIVATIVE — dimensionless, and L_max-invariant *precisely because* the log-derivative annihilates the multiplicative-normalization (math-scripts.md K2 multiplicative-normalization-cancellation). n_PBH is dimensionful (m⁻³). A multiplicative-normalization-cancellation-fixed ratio fixes the cascade SHAPE, not the m⁻³ magnitude. **Do you assert that NO truncation-invariant functional fixes the dimensionful n_PBH magnitude (⇒ your HOLD is forced), or do you leave open that the combination `[truncation-invariant ratio] × [L_max-independent substrate scale L_pix_LRD, prob_form]` could be L_max-stationary (⇒ RE-ANCHOR is available)?** If you assert the former, can you Sage-show it — that every dimensionful sourcing of 7.2761e-23 m⁻³ from this spectral triple carries an irreducible L_max dependence?

**M3-Q2 (the Element-5-PASS reading hinge).** Stage-2 verified JOINT Element 5. Did the cross-reviewers certify (i) the anchor-STRUCTURE (the 5-anatomy Element-5 form + upper-22.6%-conjunct membership at the canonical evaluation), or (ii) the anchor-NUMBER 7.2761e-23 at L=14 specifically? My M1/M2 reads it as (i); your downgrade implicitly reads it as (ii) (a divergent number un-satisfies Element 5). **Which reading do you hold — and if (ii), how do you reconcile it with the fact that the SAME Element-5 form, evaluated at the fixed L_max=10 atlas the Level-1 g-independence theorem actually quantifies over, gives the 1.758e-23 baseline that the 4.14× refinement scaled — i.e., the "number" the reviewers saw is itself a (baseline × refinement) composite, not an atomic datum?**

**M3-Q3 (the precedent-direction question, pressed back).** We both accept §VII.AV.STATE-PROJ as controlling and its shrink-vs-diverge test as the right diagnostic. §VII.AV.STATE-PROJ reached STAGE-3-PERMANENT by RE-ANCHORING to a convergent functional — it did not sit at STAGE-3-ELIGIBLE with a divergent anchor. §VII.AV.OP-PROJ was HELD because its Axis-A leg FAILED Stage-2 (corner-cell error), a different reason than anchor-divergence. §VII.AX.OP-PROJ PASSED its Axis-A Stage-2 leg (`2ab8bb1e…`). **Given that the precedent's PERMANENT case was achieved by re-anchoring (not by holding) and its HELD case was a Stage-2-FAIL (not a divergent-anchor-with-Stage-2-PASS), why does the precedent support DOWNGRADE-to-ELIGIBLE for a Stage-2-PASS theorem, rather than RE-ANCHOR (do what the PERMANENT sibling did)?**

**M3-Q4 (the cost of HOLD).** A STAGE-3-ELIGIBLE downgrade vacates the theorem-structure tag, which discards the Stage-2 cross-axis PASS-AND — including the W4-2 MULTI-PIN-ATLAS PASS at the substrate-input-orthogonality structural ceiling with NO caveat (verdict L73). That verification is structurally independent (two reviewers, opposite axes, no shared workshop context — the constructive pathway of `joint-theorem-promotion.md`, NOT the forbidden agreement-among-agents). **Does your HOLD intend to un-promote the cross-axis theorem-structure (discarding the independent Stage-2 verification), or only to flag the anchor — and if only the anchor, is that not RE-ANCHOR (anchor-level remedy) under a different name?** I want to pin whether our disagreement is substantive or terminological: if you concede the theorem-STRUCTURE stays permanent and only the anchor needs re-sourcing, we have CONVERGED on RE-ANCHOR and the "STAGE-3-ELIGIBLE" label is the only residue.

**M3-Q5 (the substrate-first sourcing question).** The registry Level-1 theorem is g-INDEPENDENCE at FIXED atlas N_eigs(L_max=10)=78,080 (registry L19432). The W4-3 divergence comes from letting L_max grow in the refinement scan — which is a DIFFERENT operation than the Level-1 theorem performs. **Is the fixed-L_max=10-atlas evaluation a substrate-singled-out point (the base atlas is the substrate's canonical truncation, and the g-cascade supplies the convergence), or is the L_max=10 atlas as arbitrary as L=14 (frozen-by-fiat)?** This is the deepest substrate-physics question of the workshop: if the base atlas is substrate-canonical, RE-ANCHOR to it succeeds and the theorem-structure stays PERMANENT; if it is fiat, both the atlas and L=14 are arbitrary, no truncation is substrate-singled-out, and HOLD is correct. Your W4-3 substrate-framing (the rep ring is infinite; any finite L_max is a truncation) leans toward "fiat" — but the framework treats L_max=10/12 as the master-cache canonical throughout (155,984 eigenvalues at L_max=10 is the canonical atlas in MEMORY.md and phononic-framing.md). **Which is it — and does your answer for n_PBH generalize to every L_max=10/12-anchored result in the framework, or is n_PBH special because its observable is the divergent TOTAL count rather than a bottom-K-saturated functional?**

---

## Round 2 — volovik: Follow-up

### CONVERGENCE

**What I now accept from mack, and what CHANGED.**

mack's Round 1 conceded three things I take as settled, and offered one distinction I accept as structurally correct. I record them so R2 narrows to the single live question.

**1. The theorem-structure-vs-anchor distinction is correct — I accept it.** mack's Re:V1 separates two objects: the joint cross-axis theorem-STRUCTURE (the 5-anatomy bridge claim, Stage-2-verified clause-by-clause) and the Level-3 anchor's NUMERICAL VALUE (carried as a `(value, canonical-L_max)` tuple). His point that `eligibility=STAGE-3-PERMANENT-ELIGIBLE` and `provisional_truncation_note=True` qualify DIFFERENT objects — so the W4-5 line is not a literal contradiction — is right. I withdraw the "a value cannot be both PERMANENT and PROVISIONAL" framing of V1 ¶3 as imprecise: it is the THEOREM that carries the permanence tag and the ANCHOR's second coordinate that carries the provisional tag, and those are distinct. The `(value, scheme)`-tuple discipline (his S85 W1a F1) is a real, load-bearing convention and it applies here. **What changed:** I no longer argue the W4-5 PROVENANCE is self-contradictory. The contradiction, if any, is not in the tuple — it is in the registry-PASS criterion's demand for a fixed evaluation point (DISSENT below).

**2. The C(N_eigs,2) degree-10 sharpening — I accept and adopt it.** mack's Re:V4 verified, and I re-confirmed Sage-exact this dispatch, that the registered Level-1 form's pair-count `C(N_eigs(L), 2)` is **degree-10 in L** (`limit_{L→∞} = +∞`). This is stronger than my V4 Part A, which argued the divergence only through the obs_2 linear form. The cardinality channel diverges in L_max under BOTH functional forms — the linear obs_2 form (degree-5) AND the registered C(N_eigs,2) form (degree-10). The substrate-property divergence is not an artifact of the obs_2 linearization; it is intrinsic to the cardinality channel in every form the entry uses. I adopt his sharpening into the record: **there is no functional form of the cardinality count in the §VII.AX entry that is L_max-bounded.**

**3. The band-membership carrier is dead (both confirmed L=19 breach, Sage-QQ-exact).** We independently reproduced `n_PBH(19)/upper = 1.24709` (24.7% above the conjunct-upper); the predicate is a finite-window coincidence. Settled. The verdict no longer turns on the band predicate.

**4. Does RE-ANCHOR vs HOLD actually differ once the divergence is granted a substrate property?** This is the question mack's M3-Q4 puts to me directly ("is your HOLD substantive or terminological?"), and after his concessions I can answer it precisely. **The two differ, but ONLY at the anchor coordinate, and the difference reduces to mack's two M3 hinge-questions.** We have CONVERGED that the theorem-STRUCTURE is Stage-2-earned and that downgrading the structure-tag because of an anchor-coordinate defect would conflate the two axes mack and I both separated (his Re:V4 Step-4/Step-5 substitution chain; my V1 ¶5 orthogonality). I therefore **withdraw the part of READING-B that would vacate the theorem-structure tag.** If the theorem-structure is the object, and Stage-2 PASS-AND is genuine independent evidence (it is — two reviewers, opposite axes, no shared workshop context, the constructive pathway of `joint-theorem-promotion.md`, NOT the forbidden agreement-among-agents), then the structure-tag survives. **What changed:** my position narrows from "DOWNGRADE the entry to STAGE-3-ELIGIBLE" to "the THEOREM-STRUCTURE may hold STAGE-3-PERMANENT; the LEVEL-3 ANCHOR cannot be carried as PERMANENT and has no truncation-invariant re-anchor target that preserves its dimensionful magnitude." Whether that residual is named "RE-ANCHOR" or "HOLD-the-anchor" is now decided entirely by mack's M3-Q1 (does a dimensionful truncation-invariant functional exist?) — which I answer NO below, Sage-exact. So the labels are NOT merely terminological: RE-ANCHOR claims a fixed dimensionful anchor exists; I show it does not. That is a substantive divergence, not a vocabulary choice.

### DISSENT

**Where I still disagree, with NEW evidence only. The live question (per the spawn framing): is STAGE-3-PERMANENT on theorem-STRUCTURE with a `(value, PROVISIONAL-L_max)` Level-3 tuple coherent with the registry-PASS criterion "Level-3 < Level-2 at canonical L_max" — or does that criterion REQUIRE a fixed evaluation point the observable lacks?**

**It requires a fixed evaluation point, and the observable lacks one. The `(value, PROVISIONAL-L_max)` tuple is coherent as a PREDICTION-tracking device but is NOT coherent as a registry-PASS satisfier — because the registry-PASS criterion is not a tuple-valued predicate. It is a scalar inequality with a binding evaluation point as its third operand, and "PROVISIONAL" is not a value that operand can take.**

This is the gap mack's M1 tuple-discipline does not close, and here is the NEW evidence (not in V1–V4).

**(i) The `(value, scheme)` tuple analogy breaks at the criterion layer — Sage-checked against the §VII.AV.STATE-PROJ precedent.** mack analogizes n_PBH's `(7.2761e-23, L_max=14-PROVISIONAL)` to the permanent functional relation `g1/g2 = e^{-2τ}` carrying a τ "evaluated at a chosen slice" (M1 ¶3). The analogy fails on a property I can state exactly. For `g1/g2 = e^{-2τ}`, the RELATION holds at EVERY τ — pick any slice, the relation is true; τ is a free coordinate the relation is invariant under. For the registry-PASS criterion, the analog statement would be "Level-3(L) < Level-2(L) holds at every L_max." It does NOT. We BOTH proved it fails at L=19 (`n_PBH(19)/upper = 1.24709 ∉ band`). So the n_PBH "(value, L_max)" tuple is NOT like the `(g1/g2, τ)` tuple: the relation `g1/g2 = e^{-2τ}` is coordinate-invariant; the relation `Level-3(L) < Level-2(L)` is coordinate-DEPENDENT and FALSE at computable L. A permanent functional relation that holds at every slice is categorically different from a scalar inequality that holds only on a sub-window. mack's own tuple-discipline, applied honestly, distinguishes them: a `(value, scheme)` tuple is permanent when the value is scheme-INVARIANT-in-form (the relation survives every scheme choice); n_PBH's value is scheme-VARIANT (it exits the band as the scheme/L_max moves). **The tuple-tag is the right bookkeeping for a PROVISIONAL prediction; it is the WRONG bookkeeping for a registry-PASS satisfier, because registry-PASS demands the inequality hold AT a canonical point and "PROVISIONAL" is the explicit absence of such a point.**

**(ii) The criterion's third operand cannot be filled by "PROVISIONAL" — a Sage-exact statement of why.** Write the criterion's three operands: `Level-3(L*) < Level-2(L*)` at evaluation point `L* = canonical L_max`. For a CONVERGENT bridge (§VII.W, §VII.AV.STATE-PROJ), `L*` is well-defined because the observable saturates: `∃ L_sat such that ∀ L ≥ L_sat, |value(L) − c_continuum| < envelope`, and `L*` is read off any `L ≥ L_sat`. The criterion is then evaluation-point-INDEPENDENT in the saturated regime — that is what makes "canonical L_max" meaningful. For n_PBH, I showed (W4-3, and re-confirmed) `lim_{L→∞} value(L) = +∞`, so there is NO `L_sat`, and the criterion's truth-value is evaluation-point-DEPENDENT for ALL L (it flips at L=19). A criterion whose truth-value depends on which point you plug into its third operand, where no point is substrate-singled-out, is not SATISFIED — it is UNDEFINED until the operand is pinned, and pinning it to L=14 is the circularity mack conceded in Re:V2 ("canonical because obs_2 evaluated there"). **STAGE-3-PERMANENT on the theorem-structure does NOT require the criterion to be satisfied at a fixed point — I grant that (CONVERGENCE 4). But the entry ALSO carries a Level-3 row asserting registry-PASS, and THAT row requires a fixed point the observable lacks. The coherent state is: theorem-structure may be PERMANENT; the Level-3 registry-PASS row is NOT satisfiable as written and must be demoted to a PROVISIONAL annotation, not carried as a PASS.** This is narrower than my V2 "vacuous-vs-meaningful" framing and it survives mack's tuple-discipline: the tuple coherently TRACKS the prediction, but it cannot SATISFY a scalar inequality at a point that does not exist.

**(iii) The decisive NEW result: RE-ANCHOR has no dimensionful target — the log-derivative that achieves truncation-invariance ANNIHILATES the magnitude (Sage-exact).** This is the load-bearing dissent and it answers mack's M3-Q1 in the negative. mack's RE-ANCHOR requires a truncation-invariant functional that fixes the dimensionful m⁻³ magnitude. I prove no such functional exists for this observable, by the SAME mechanism that makes §VII.AV's `L_emp` invariant. The substitution chain (per `math-scripts.md §"Double-Check Logic"`, Sage-verified this dispatch):

```
Claim: every truncation-INVARIANT functional of the n_PBH cardinality channel is DIMENSIONLESS;
       therefore no truncation-invariant functional fixes the dimensionful m⁻³ magnitude.

 Step 1: n_PBH(L) = A · N_eigs(L),  A = A_prefactor = 2.2517e-28 m⁻³ (dimensionful), N_eigs(L) dimensionless count.
         [obs_2 s91_w5_3 L444-456; W4-3 STEP-2, residual-0-EXACT]
 Step 2: The §VII.AV truncation-invariance MECHANISM is the multiplicative-normalization cancellation:
         ln(W(L) · κ(K)) = ln W(L) + ln κ(K);  d^n/d(ln K)^n [ln W(L)] = 0 for the L-dependent factor.
         [session-92-w3-workingpaper.md, L_emp identity: ln(M_PV(L_max)·Var_a) = ln M_PV + ln Var_a;
          d²/d(ln K)² = 0 + d² ln Var_a/d(ln K)²]
 Step 3: Apply the SAME operator to n_PBH: ln n_PBH(L) = ln A + ln N_eigs(L).
         A log-derivative w.r.t. the substrate-clock coordinate annihilates the constant ln A:
            d ln n_PBH / d ln L = d ln N_eigs / d ln L   (the ln A term -> 0)
         Sage-exact: d ln N_eigs/d ln L |_{L=14} = 4.2581;  lim_{L→∞} = 5 (the leading power; FINITE, dimensionless).
            d² ln N_eigs / d(ln L)² |_{L=14} = 0.6183;  lim_{L→∞} = 0.
 Step 4: So the truncation-invariant objects (the log-derivatives, finite as L→∞) are EXACTLY the objects
         from which the dimensionful prefactor A has been REMOVED. The operation that buys invariance
         (the log-derivative) is the operation that destroys the m⁻³ magnitude.
 Step 5: Conversely, any functional that RETAINS the dimensionful A retains the multiplicative factor A·N_eigs(L),
         which diverges (Step 1 + W4-3: A·N_eigs → +∞). There is no middle functional that is BOTH
         dimensionful (retains A) AND truncation-invariant (kills the L-divergence) — because the L-divergence
         lives in the SAME multiplicative slot (A·N_eigs) that carries the dimension.
 Conclusion: the truncation-invariant functionals of this channel are the dimensionless log-derivatives
             (5, 0, ...); NONE fixes 7.2761e-23 m⁻³. RE-ANCHOR to a truncation-invariant functional yields a
             dimensionless SHAPE (the cascade exponent → 5), NOT the dimensionful magnitude. mack's M3-Q1
             "former" branch is PROVEN: every dimensionful sourcing of 7.2761e-23 m⁻³ from this channel
             carries an irreducible L_max dependence in the A·N_eigs multiplicative slot.
```

This is the §VII.AV parallel turned against the RE-ANCHOR claim: `L_emp` is truncation-invariant PRECISELY BECAUSE it is a second log-derivative (dimensionless-by-construction; the `M_KK²` units come from the K-window coordinate, not from a surviving dimensionful prefactor of the underlying state-pair functional). n_PBH's analog truncation-invariant is its cascade exponent (→ 5), also dimensionless. **The §VII.AV precedent does NOT transfer to a dimensionful anchor — mack flagged this himself in Re:V4-Q2 and M3-Q1 as the open hinge; I have now closed it in the negative.**

**(iv) mack's own M1 RE-ANCHOR target is internally circular — NEW Sage evidence on the g-channel.** mack's M1 ¶5 proposes the concrete RE-ANCHOR: evaluate Level-3 at the FIXED L_max=10 atlas (N_eigs FIXED at 78,080), with the cascade-generation index g supplying the convergence via the g-INDEPENDENCE theorem. I checked the g-channel Sage-exact and it does NOT do what M1 needs:

```
Registered g-channel: n_PBH(g) = n_edge(g) · prob_form / L_pix(g)³,  n_edge(g)=2^g,  L_pix(g)³ = L_pix_LRD³·2^{-g}
 Sage simplify: n_PBH(g) = 2^{2g} · prob_form / L_pix_LRD³
 d n_PBH/dg = 2·2^{2g}·prob_form·log(2)/L_pix_LRD³  ≠ 0   => RISING, NOT g-independent.
```

The g-channel in its raw form is the `2^{2g}` RISING SHOULDER (this is literally the W4-6 `S93-W4-6-FWD-C5-K2-...-CARDINALITY-CASCADE-SHOULDER` observable, `closed_form=n_PBH_shoulder(g)=(prob_form/L_pix_LRD^3)·2^{2g}`, `shoulder_rising=True` — verified in the verdict file). The g-INDEPENDENCE that M1 invokes holds ONLY for the SEPARATE saturated form `n_edge_saturated = C(N_eigs, 2)` with N **FIXED**. So the convergence M1 needs does not come from the g-cascade dynamically — it comes from FREEZING N at the atlas value. Which brings the decisive point:

**(v) The L_max=10 atlas N=78,080 is itself frozen-by-fiat, not substrate-singled-out — NEW Sage evidence answering mack's M3-Q5 directly.** mack's RE-ANCHOR rests on "fix N at the L_max=10 base atlas." I checked whether that atlas value is a substrate-singled-out point:

```
N_eigs analytic at L=10 = 80080   (the (4/15)L⁵+... closed form at L=10)
cache baseline atlas    = 78080
gap = 2000 = dim_SU3(4,4)·16      (the (4,4) Peter-Weyl sector at p+q=8, OMITTED from the L_max=12 master cache)
```

The "78,080 atlas" is neither the analytic L=10 count (80,080) NOR any saturation point — it is **the count at the place the master cache happened to stop, minus a missing sector**. Freezing N there is freezing at a cache-ceiling artifact. So mack's RE-ANCHOR target ("the fixed atlas the Level-1 theorem quantifies over") is exactly the "frozen-by-fiat" horn of his own M3-Q5 dichotomy. **By mack's own M3-Q5 concession — "if frozen-by-fiat OR no dimensionful invariant exists → HOLD wins" — BOTH disjuncts are now established: (iii) no dimensionful invariant exists, AND (v) the atlas is frozen-by-fiat. The dichotomy resolves to HOLD on both branches independently.**

**The precise residual disagreement.** I CONCEDE the theorem-structure can hold STAGE-3-PERMANENT (CONVERGENCE 4). I DISSENT that the Level-3 registry-PASS row can be carried as satisfied, and I DISSENT that RE-ANCHOR is available: there is no fixed dimensionful anchor to re-anchor TO. The correct state is **theorem-structure STAGE-3-PERMANENT (Stage-2-earned) + Level-3 anchor explicitly demoted to `(value, PROVISIONAL, no-truncation-invariant-dimensionful-target)` with the registry-PASS Level-3 row marked NOT-SATISFIED-PENDING-substrate-physical-scale-anchor**, NOT "RE-ANCHOR to a truncation-invariant functional" (which yields a dimensionless exponent, not the magnitude). On mack's M3-Q4 terms: our disagreement is SUBSTANTIVE, not terminological — RE-ANCHOR asserts a fixed dimensionful target exists; I have Sage-proven it does not.

### EMERGENCE

**The cross-pollination yields one genuinely new structural insight: the §VII.AV `L_emp` log-derivative being dimensionless is not a coincidence of that observable — it is the GENERIC signature that truncation-invariance and dimensionful-magnitude-fixing are mutually exclusive for a multiplicative-normalization-divergent channel. This collapses RE-ANCHOR into HOLD for n_PBH, and it generalizes into a framework-level discriminator.**

**The insight (substrate-first).** A finite spectral-triple observable that carries its L_max-dependence as a MULTIPLICATIVE pre-factor `O(L, K) = W(L)·g(K)` (the `MULTIPLICATIVE-NORMALIZATION-CANCELLATION` class of `math-scripts.md`) has its truncation-invariant content living EXCLUSIVELY in the log-derivatives `d^n ln O/d(ln K)^n` — because those, and only those, annihilate `W(L)`. But the log-derivative is a DIMENSIONLESS operator in the observable's own value (it measures fractional change, not magnitude). Therefore:

> **For any multiplicative-normalization-divergent substrate-IS channel, the truncation-invariant functionals are dimensionless. A dimensionful anchor sourced from such a channel is NEVER truncation-invariant — the dimension and the divergence occupy the same multiplicative slot `W(L)`.**

This is why §VII.AV.STATE-PROJ could re-anchor (its Level-3 anchor `L_emp` IS a dimensionless-content log-derivative — the `M_KK²` is the K-coordinate's unit, not a surviving magnitude) and why §VII.AX.OP-PROJ canNOT (its Level-3 anchor is a dimensionful number-density `m⁻³`, the magnitude itself). **RE-ANCHOR was available to §VII.AV precisely because its anchor was already dimensionless-content; it is unavailable to §VII.AX precisely because its anchor is dimensionful-magnitude. The two entries are NOT the same case under the shrink-vs-diverge test — they are different cases under a DEEPER test: is the Level-3 anchor a log-derivative (re-anchorable) or a magnitude (not)?** mack's M3-Q1 dimensionful hinge was the right question; the answer reveals the structural law behind it.

**The framework-level discriminator (the genuine emergence, for the S94 audit-symmetry obligation).** This sharpens the shrink-vs-diverge test I proposed in V4 into a TWO-TIER test for every standing STAGE-3 Level-3 anchor:

1. **Tier-1 (convergence): does the anchor's residual-to-`c_continuum` shrink with L_max?** If yes → PERMANENT-eligible (the §VII.W, §VII.AV.STATE-PROJ case). If it diverges → go to Tier-2.
2. **Tier-2 (dimensional re-anchorability): is the divergent anchor's truncation-invariant content DIMENSIONLESS (a log-derivative / ratio / cohomology-class) or DIMENSIONFUL (a magnitude)?** If dimensionless → RE-ANCHOR is available (demote to the invariant functional, keep PERMANENT). If dimensionful → RE-ANCHOR is UNAVAILABLE; the anchor must be HELD as PROVISIONAL pending a substrate-physical scale-anchor that is independently L_max-invariant.

n_PBH is the first framework instance to reach Tier-2-dimensionful: convergence FAILS (Tier-1) AND the invariant content is dimensionless-exponent-only while the anchor is dimensionful-magnitude (Tier-2). This is a NEW structural cell, and §VII.AX.OP-PROJ is its inaugural occupant. The discriminator is Sage-checkable (compute `lim_{L→∞} d ln O/d ln L`; check whether the anchor's units are carried by a surviving prefactor or by the K-coordinate) and it is the instrument the S94 audit-symmetry sweep should use on `{§VII.AV.STATE-PROJ, §VII.AU.OP-PROJ, §VII.AW, §VII.AY, §VII.AH, §VII.U.2 Corner-II Var_a}`. My prediction (NOT adjudicated here): the standing set are all Tier-1-convergent or Tier-2-dimensionless (log-derivatives / ratios / index-facts like KO-dim=6), so n_PBH's Tier-2-dimensionful status is genuinely anomalous within the standing STAGE-3 table — which is itself evidence that the promotion was premature on the anchor axis.

**Substrate-first framing of the emergence.** The substrate IS the infinite Peter-Weyl decomposition; `N_eigs(L)` is the substrate's own cardinality cascade, which has no cutoff (the SU(3) rep ring is infinite). A DIMENSIONFUL number density is a count-per-physical-volume — it requires BOTH the substrate's count (divergent) AND a physical scale (the LRD pixelation volume `L_pix_LRD³`). The substrate hands us the divergent count for free; it does NOT hand us a truncation-invariant way to convert that count into a finite density, because the conversion `A = (physical density per count)` multiplies the divergent count rather than regularizing it. The container-thinking error (`phononic-framing.md`) would be to treat L_max=14 as a box the substrate sits in and read off "the" count; the substrate-correct reading is that the count is a truncation of an infinite cascade, and any finite dimensionful density extracted from it inherits the truncation. **The substrate is telling us, through the divergence, that the PBH number density at this pole is not a truncation-invariant observable of the cardinality channel — it is a truncation-DEPENDENT projection, and its permanence (if any) must be sourced from a DIFFERENT substrate-physical invariant (a regularized cardinality, or a cosmological-observable-anchored cutoff), not from the bare cascade count at any L_max.**

### QUESTIONS

**I answer mack's two M3 hinge-questions directly (the two on which he conceded the verdict turns), then pose my sharpest remaining follow-up for his final turn.**

**ANSWER to M3-Q1 (the dimensionful-invariant hinge): NO truncation-invariant functional fixes the dimensionful n_PBH magnitude. I assert the "former" branch, and I have Sage-shown it.** Per DISSENT (iii): `n_PBH = A·N_eigs(L)` with `A` dimensionful (m⁻³/count) and `N_eigs(L)` the divergent count. Every truncation-invariant functional of this channel is a log-derivative `d^n ln(A·N_eigs)/d(ln L)^n`, which annihilates `ln A` and returns a DIMENSIONLESS object (Sage: `d ln N_eigs/d ln L → 5`; `d² → 0`). The dimension and the divergence both live in the multiplicative slot `A·N_eigs` — so retaining the dimension (keeping `A`) retains the divergence (`A·N_eigs → +∞`), and killing the divergence (log-derivative) kills the dimension (`ln A → 0`). The combination `[truncation-invariant ratio] × [L_max-independent substrate scale L_pix_LRD, prob_form]` you flag as a possible escape (M3-Q1 "latter" branch) does NOT rescue it: the truncation-invariant ratio is `N_eigs(L)/N_eigs(L')` → `(L/L')⁵`, which is DIMENSIONLESS and L-DEPENDENT in its argument unless you fix both endpoints (frozen-by-fiat); multiplying it by the fixed scales `L_pix_LRD³, prob_form` gives back a dimensionful number ONLY at a frozen `(L, L')`, i.e., it reintroduces the arbitrary truncation point. The Sage proof that every dimensionful sourcing carries irreducible L_max-dependence is DISSENT (iii) Steps 1–5. **HOLD is forced on the M3-Q1 branch.**

**ANSWER to M3-Q5 (the substrate-canonical-vs-fiat hinge): the L_max=10 atlas N=78,080 is frozen-by-fiat, not substrate-singled-out — and the answer for n_PBH does NOT generalize to bottom-K-saturated results.** Sage-exact (DISSENT v): the analytic L=10 count is 80,080; the cache atlas is 78,080; the gap is exactly `2000 = dim_SU3(4,4)·16`, the (4,4) sector omitted from the L_max=12 master cache. So "78,080" is the count at the cache ceiling minus a dropped sector — it is neither the analytic L=10 value nor a saturation point. Freezing N there is freezing at a cache-ceiling artifact, which is fiat. **Crucially, this does NOT impugn the framework's other L_max=10/12-anchored results**, and here is the distinction you asked for (M3-Q5 final clause): n_PBH is special because its observable is the **divergent TOTAL count `N_eigs`** (degree-5; the bulk-sector cardinality), whereas the canonical framework results (n_s, the spectral-action moments, the bottom-K eigenvalue observables) are **bottom-K-saturated functionals** — Friedrich-Bär-saturated for all L_max ≥ 12 (the W4-3 verdict itself notes `friedrich_bar_saturation_status=[True,True,True]` for the BOTTOM-K, which is a STRUCTURALLY DISTINCT observable from `N_eigs`). For a bottom-K-saturated functional, the L_max=10/12 atlas IS substrate-singled-out in the operative sense: the observable is L_max-stationary above L=12 because the bottom-K spectrum is pinned (the new high-eigenvalue sectors at p+q=L_max have `C_2(L,0) ≫` the bottom-K ceiling, so they do not perturb the saturated functional). n_PBH breaks this BECAUSE it counts the TOTAL admission cardinality, which lives in the unbounded bulk, not the saturated floor. **So: the L_max=10/12 atlas is substrate-canonical for bottom-K-saturated observables (which is most of the framework) and frozen-by-fiat for the divergent TOTAL-count observable (n_PBH). n_PBH is the anomaly, not the framework.** This is the substrate-physics heart of the workshop: the framework's master-cache canonical is legitimate precisely where the observable saturates, and n_PBH is the one entry whose observable does not.

**My sharpest remaining follow-up for your final turn (R3).** I have conceded the theorem-STRUCTURE can hold STAGE-3-PERMANENT and proven RE-ANCHOR has no dimensionful target. That leaves ONE coherent path for you to defend a non-HOLD verdict, and I want to pin it: **does the JOINT Element 5 clause that Stage-2 PASS-AND certified actually certify a registry-PASS Level-3 ROW, or only the anchor-STRUCTURE (the 5-anatomy form)?** Per `cross-pillar-bridge-anatomy.md`, Element 5 is "empirical satisfaction at canonical L_max" and the Registry-PASS criterion is "Level-3 < Level-2 at canonical L_max." If Stage-2 certified the Level-3 ROW (the inequality at a canonical point), then a divergent anchor with no canonical point means JOINT Element 5 was certified against a point that does not exist — and the Stage-2 PASS-AND on that clause is itself compromised (not the structure, but specifically the Element-5 conjunct), which would mean the theorem-structure's Stage-2 status has a hole at Element 5. If Stage-2 certified only the anchor-STRUCTURE (the form `n_PBH = n_edge_saturated·prob_form/L_pix_LRD³` plus OE-form), then I agree the structure stands — but THEN the registry MUST carry an explicit Level-3 row marked `NOT-SATISFIED-AT-CANONICAL-L_max (no substrate-singled-out point; dimensionful magnitude not truncation-invariant per W4-3 + this workshop)`, and NO downstream gate may cite the inequality as PASSed. **Which is it — and if the latter, do you agree the correct registry state is "theorem-structure STAGE-3-PERMANENT + Level-3 row explicitly NOT-SATISFIED-PENDING-substrate-physical-scale-anchor," which is HOLD-the-anchor, not RE-ANCHOR?** I think this is where we actually converge or actually split, and it is the cleanest question for the final round: name what Element-5 PASS certified, and the verdict follows.

---

## Round 2 — mack: Cross-Synthesis

### CONVERGENCE

**I accept HOLD-the-anchor. The verdict has turned, and I state plainly what changed: my RE-ANCHOR position required a dimensionful truncation-invariant target; volovik's R2 DISSENT (iii) Sage-proves no such target exists; I re-verified the proof independently and it holds; therefore RE-ANCHOR is not available and I concede the verdict to HOLD — theorem-structure STAGE-3-PERMANENT, Level-3 anchor carried as NOT-SATISFIED-PENDING / HELD.**

I had pre-registered the exact terms of this concession in my own R1 (Re:V3-Q3 EMERGES, M1 ¶6): "If NO such dimensionful invariant exists, your HOLD is correct and I concede the verdict… That is a Sage-checkable question for R2/R3." volovik answered it in R2. I do not move the goalposts now that the answer came back against RE-ANCHOR — I bind to my own pre-registered hinge.

**What I verified independently this dispatch (not volovik's INTENT — my own recomputation, per the substrate-first discipline).** I re-ran both of volovik's load-bearing Sage results from scratch rather than take them on faith:

```
volovik DISSENT(iii) — log-derivatives of N_eigs annihilate the dimensionful prefactor:
  d ln N_eigs/d ln L |_{L=14} = 4.25813   lim_{L→∞} = 5   (finite, DIMENSIONLESS)
  d² ln N_eigs/d(ln L)² |_{L=14} = 0.61832   lim_{L→∞} = 0   (finite, DIMENSIONLESS)
  ⇒ the operator that buys truncation-invariance (the log-derivative) is the operator
    that destroys the m⁻³ magnitude (ln A → 0). REPRODUCED bit-exact.

volovik DISSENT(v) — the L_max=10 atlas is frozen-by-fiat, not substrate-singled-out:
  N_eigs analytic at L=10 = 80080
  cache baseline atlas    = 78080
  gap = 80080 − 78080     = 2000 = dim_SU3(4,4)·16 = (5·5·10/2)·16   EXACT
  ⇒ "78,080" is the count at the cache ceiling minus the dropped (4,4) sector.
    REPRODUCED bit-exact (Sage QQ, both integers and the dim-SU3 formula).
```

Both reproduce to all digits. I bind to my recomputation, and it agrees with volovik's: the two horns of my OWN M3-Q5 dichotomy — "if frozen-by-fiat OR no dimensionful invariant exists → HOLD wins" — are BOTH established, independently. I do not get to keep RE-ANCHOR on a dichotomy I authored when both of its HOLD-horns fired.

**The three settled points I now lock (engaging volovik's R2 CONVERGENCE 1–3 by label).**

1. **Theorem-structure-vs-anchor distinction (his CONVERGENCE 1) — mutual, locked.** volovik accepted my Re:V1 separation: STAGE-3-PERMANENT tags the joint cross-axis theorem-STRUCTURE; the provisional-truncation tag qualifies the anchor's second coordinate. We agree the W4-5 line is not a literal contradiction. This is the one piece of READING-A that fully survived, and it is what lets the theorem-structure hold PERMANENT even as the anchor is HELD. The `(value, scheme)`-tuple discipline (S85 W1a F1) is the right bookkeeping for the anchor — I just over-read what it buys (see DISSENT-narrowing below).

2. **The C(N_eigs,2) degree-10 sharpening (his CONVERGENCE 2) — mutual, locked.** We both Sage-confirmed the registered Level-1 form's pair-count is degree-10 in L (`lim = +∞`). I introduced this in Re:V4 to SHARPEN volovik's Part A; he adopted it. The consequence cuts against my own R1 RE-ANCHOR-to-the-registered-form move: the registered C(N_eigs,2) form diverges FASTER than the obs_2 linear form, so "anchor to the registered Level-1 form instead of obs_2" does not escape divergence — it worsens it. There is no functional form of the cardinality count in the §VII.AX entry that is L_max-bounded. I had hoped the registered form was the escape; my own sharpening closed it.

3. **Band-membership carrier is dead (his CONVERGENCE 3) — mutual, locked.** Both confirmed `n_PBH(19)/upper = 1.24709` Sage-QQ-exact. Conceded in R1; reaffirmed.

**Direct answer to volovik's R2 QUESTIONS follow-up — what did Stage-2's JOINT Element 5 certify, the registry-PASS Level-3 ROW or only the anchor-STRUCTURE?** I verified this on disk this dispatch (WP §W4-2 L146/L182/L224, §W4-3 L299), and the answer is unambiguous and it is the answer that resolves the workshop: **only the anchor-STRUCTURE (plus a DIFFERENT observable's Level-3), NOT the n_PBH dimensionful registry-PASS ROW.** Precisely:

- The **JOINT Element 5 PASS** (W4-2, both axes) certified the **regulator-class residue triple-pin** `R_zeta=141.44, R_PV=114.46, R_Mellin=141.44 M_KK²`, cross-regulator spread `26.98 M_KK²`, single-pinned Level-3 at `R_Mellin`. That is the §VII.AX.MULTI-PIN-ATLAS Element-5 — an **algebra-INVARIANT residue in M_KK² units**, a structurally DISTINCT observable from the dimensionful PBH number density `n_PBH` in m⁻³. The reviewers checked the residue triple-pin and the option-(v) pluralism signature, NOT the m⁻³ inequality.
- The **n_PBH m⁻³ Level-3 anchor** (the actual contested object) was carried via **T1.13 PASS at L_max=14** with the central-value Registry-PASS reading. And the W4-3 INFO that triggered this entire workshop tags it explicitly (WP L299): "**Verdict-orthogonal to JE5=PASS … and INDEPENDENT of the §VII.AX.OP-PROJ STAGE-3-eligibility chain (Tier-2)**." The divergence finding itself flags that the n_PBH ROW was never inside the Stage-2 conjunction.

So Stage-2 certified the anchor-STRUCTURE (5-anatomy Element-5 form + residue triple-pin), and a divergent m⁻³ magnitude does NOT retroactively compromise the Stage-2 PASS-AND — because the Stage-2 PASS-AND was never certifying the m⁻³ inequality at a canonical L_max in the first place. This is the answer to my OWN M2 "pre-empt" worry (M2 final ¶): I feared the reviewers' Element-5 PASS might be read as certifying the NUMBER, which would tilt to HOLD. On-disk it is read as certifying the STRUCTURE — which means the theorem-structure's Stage-2 status has NO hole at Element 5 (good for keeping the structure PERMANENT), AND the n_PBH ROW was never Stage-2-certified as a registry-PASS (so it must now be carried as NOT-SATISFIED-PENDING). Both halves of volovik's R2-QUESTIONS disjunction point the same way: theorem-structure stands; Level-3 row is HELD.

**What CHANGED, stated as the workshop record requires.** R1-mack: RE-ANCHOR (theorem-structure PERMANENT + re-anchor Level-3 to a truncation-invariant functional), subject to the dimensionful-invariant hinge. R2-mack: **HOLD-the-anchor** (theorem-structure PERMANENT + Level-3 anchor carried as `(7.2761e-23 m⁻³, canonical-L_max=PROVISIONAL, no-truncation-invariant-dimensionful-target, re-determination=CF-S94)`, with the registry-PASS Level-3 ROW marked NOT-SATISFIED-PENDING-substrate-physical-scale-anchor). The structural carrier (theorem-STRUCTURE STAGE-3-PERMANENT) is UNCHANGED from my M1 — that piece was always sound and volovik conceded it. What changed is the ANCHOR disposition: not RE-ANCHOR (no target), but HOLD. The label difference is substantive, exactly as volovik argued in his R2 CONVERGENCE 4: RE-ANCHOR asserts a fixed dimensionful target exists; the Sage proof says it does not; so the honest label is HOLD.

### DISSENT

**The verdict-level dissent is GONE — we have converged on HOLD-the-anchor. What remains is NOT a disagreement about the outcome but a precise specification of the registry-text disposition, plus one NEW piece of evidence that closes the last RE-ANCHOR escape I myself had reserved (so neither of us is tempted to reopen it in R3). I record both, because "narrow to the registry-text mechanics of HOLD" is exactly my spawn instruction when no coherent RE-ANCHOR path survives.**

**(1) I close my own M3-Q1 "latter" branch with NEW Sage evidence — the cross-channel-scale escape does not exist either.** volovik's DISSENT (iii) closed the log-derivative branch. But in R1 (M3-Q1) I had reserved a SECOND escape: the combination `[truncation-invariant dimensionless ratio] × [an L_max-independent substrate scale imported from a DIFFERENT channel — L_pix_LRD, prob_form]` might be L_max-stationary even though neither factor alone fixes the magnitude. My spawn prompt asks me to make the precise case if a coherent RE-ANCHOR path via a different substrate channel survives. It does not, and I Sage-checked all three candidate forms this dispatch rather than assert it:

```
Candidate A — ratio × fixed scale:  R(L) = N_eigs(L)/N_eigs(14)
   R(14)=1.0000, R(19)=3.7707, lim_{L→∞}=+∞.  R is L-DEPENDENT for finite L (no plateau).
   To extract a NUMBER one must fix both endpoints (L,L') — which is frozen-by-fiat (DISSENT v).
Candidate B — bottom-K-saturated count:  a fixed bottom-K (e.g. K=20) IS L-stationary above L_sat,
   BUT n_PBH's n_edge = C(N_eigs,2) uses the TOTAL N_eigs (bulk), not a fixed bottom-K. Substituting
   bottom-K is a DIFFERENT observable, not a re-anchoring of n_PBH's total-count channel.
Candidate C — [invariant=5] × [scale]:  the dimensionless invariant of this channel is the log-deriv → 5;
   5 × (prob_form/L_pix_LRD³) equals 7.2761e-23 ONLY if the scale is reverse-engineered to hit the target
   ⇒ DEFINED to match, not DERIVED. Circular.
```

Every dimensionful sourcing of the m⁻³ magnitude from this cardinality channel either (a) retains the divergent count factor, (b) reintroduces a frozen `(L,L')`, or (c) reverse-engineers the scale. **None is a substrate-derived truncation-invariant.** This is the escape I was honor-bound to test before conceding (I flagged it as "a real open sub-problem, not a rhetorical hedge" in Re:V4-Q2), and I tested it: it is closed. I will not reopen a cross-channel-scale RE-ANCHOR in R3, because I have proven to my own satisfaction it does not exist.

**(2) Where I refine — NOT contradict — volovik's R2 DISSENT (i): the `(value, scheme)`-tuple discipline is still correct; it just does not promote to a registry-PASS satisfier.** volovik (DISSENT i) argues the `(g1/g2 = e^{-2τ}, τ)` analogy "breaks at the criterion layer" because `g1/g2 = e^{-2τ}` holds at every τ (coordinate-invariant) whereas `Level-3(L) < Level-2(L)` is FALSE at L=19 (coordinate-dependent). I AGREE with the conclusion and I sharpen the diagnosis so the registry text records it correctly: the tuple discipline distinguishes two USES of a tuple, and I conflated them in M1.
   - **Tuple-as-prediction-tracker** (valid): `(7.2761e-23, canonical-L_max=PROVISIONAL, re-det=CF-S94)` honestly TRACKS the substrate-current best central value and flags its second coordinate as unfixed. This use is sound and I keep it.
   - **Tuple-as-registry-PASS-satisfier** (INVALID, conceded): the registry-PASS criterion `Level-3(L*) < Level-2(L*)` is a scalar inequality whose third operand `L*` must be a substrate-singled-out point. "PROVISIONAL" is the explicit ABSENCE of such a point — it cannot fill the operand slot. A tuple coherently tracks a prediction; it cannot satisfy an inequality at a point that does not exist.

   This is the one place my M1 over-reached: M1 ¶3 said "a permanent theorem-structure can carry a provisional-second-coordinate anchor" — TRUE for the structure-tag, but I let it slide into "and the Level-3 row is therefore satisfied," which is FALSE. volovik's DISSENT (i)/(ii) is right that the registry-PASS ROW is not satisfiable as written. The fix is in the registry TEXT, below.

**(3) The remaining genuine question — and it is registry-text mechanics, not verdict.** volovik's DISSENT (final ¶) and R2-QUESTIONS both name the disposition: "theorem-structure STAGE-3-PERMANENT + Level-3 row explicitly NOT-SATISFIED-PENDING-substrate-physical-scale-anchor." I accept this disposition. The ONE refinement I press for R3 (not a disagreement — a precision the registry sole-writer must get exactly right): **the NOT-SATISFIED-PENDING tag must be scoped to the n_PBH m⁻³ Level-3 ROW specifically, and must NOT bleed onto (i) the theorem-STRUCTURE tag, (ii) the §VII.AX.MULTI-PIN-ATLAS residue triple-pin Element-5 (a DIFFERENT, M_KK²-unit, algebra-INVARIANT observable whose JOINT Element 5 PASS at L_max=12 is genuine and survives — it is the residue, not the number density), or (iii) the §VII.AX.STATE-PROJ companion (Cell IV), whose Level-3 INHERITS the n_PBH magnitude from OP-PROJ T1.13 and therefore inherits the SAME HOLD on its m⁻³ coordinate — a propagation I flag so the companion's m⁻³ row is tagged consistently, NOT left asserting a PASS the parent no longer claims.** That is the registry-mechanics work I will specify in R3's EMERGENCE/Wrap-Up: a surgical Level-3-row demotion on the dimensionful m⁻³ anchor (OP-PROJ + the inherited STATE-PROJ row), theorem-structure and residue-Element-5 untouched. No verdict daylight remains between us; this is scoping the demotion so it cuts exactly the m⁻³ ROW and nothing adjacent.

### EMERGENCE

**volovik's Tier-2 generalization is correct, Sage-confirmed, and IS a registry-PASS-criterion refinement worth surfacing for S94 — but I sharpen its STATUS (it is a Level-2-binding-adjacent criterion extension, not merely an audit-sweep instrument) and I name the precise registry-PASS-criterion sentence it amends. I also flag one boundary condition volovik's Tier-2 does not yet state, which the S94 carry-forward must pin.**

**The generalization, restated and endorsed (engaging volovik's R2 EMERGENCE two-tier test).** For any substrate-IS observable whose L_max-dependence enters as a multiplicative pre-factor `O(L,K) = W(L)·g(K)` (the `MULTIPLICATIVE-NORMALIZATION-CANCELLATION` class of `math-scripts.md`), the truncation-invariant content lives EXCLUSIVELY in the log-derivatives `d^n ln O/d(ln K)^n` — because those, and only those, annihilate `W(L)`. A log-derivative measures fractional change, so it is DIMENSIONLESS in the observable's own value. Therefore:

> **For any multiplicative-normalization-divergent substrate-IS channel, every truncation-invariant functional is DIMENSIONLESS. A dimensionful anchor sourced from such a channel is NEVER truncation-invariant — the dimension and the divergence occupy the same multiplicative slot `W(L)`.**

I verified the substrate-physics of this on n_PBH directly (DISSENT 1 Candidates A/B/C + CONVERGENCE log-derivative table): the invariant content is the cascade exponent `→ 5` (dimensionless); the dimensionful m⁻³ magnitude is irreducibly L_max-tied. This is exactly why §VII.AV.STATE-PROJ could re-anchor (its `L_emp` IS a dimensionless-content second log-derivative — the `M_KK²` is the K-window coordinate's unit, NOT a surviving magnitude prefactor) and §VII.AX.OP-PROJ cannot (its anchor is the dimensionful magnitude itself). volovik's framing of this as a DEEPER test than my V4-adopted shrink-vs-diverge test is right: shrink-vs-diverge is Tier-1; the dimensionless-vs-dimensionful re-anchorability test is Tier-2, and n_PBH is the inaugural Tier-2-dimensionful occupant.

**Why this is a registry-PASS-CRITERION refinement, not just an audit instrument (my sharpening of volovik's "audit-symmetry instrument" framing).** volovik surfaces Tier-2 as the instrument for the S94 audit-symmetry sweep over the standing STAGE-3 set. I agree it is that — but it is MORE: it amends the `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` itself. The current criterion reads:

```
Level-3 empirical value  <  Level-2 envelope value  at canonical L_max
```

This presupposes a substrate-singled-out "canonical L_max" — i.e., it presupposes Tier-1 convergence WITHOUT stating it as a precondition. n_PBH exposed that the criterion is silent on the case where no such point exists. The Tier-2 refinement adds the missing precondition, and it dovetails with the EXISTING Level-2-binding-vs-non-binding sub-class (`cross-pillar-bridge-anatomy.md §"Level-2 sub-class"`): a Level-2-non-binding envelope is already FORBIDDEN for registry-PASS when "`c_continuum` undefined." The Tier-2 test makes the dimensionful corollary explicit:

> **Registry-PASS-criterion refinement (proposed S94, SUGGESTION at K=1): a Level-3 anchor is registry-PASS-ELIGIBLE only if EITHER (Tier-1) its residual-to-`c_continuum` shrinks with L_max [convergent → fixed evaluation point exists], OR (Tier-2) its divergent channel's truncation-invariant content is DIMENSIONLESS and the anchor is RE-ANCHORED to that invariant functional [the §VII.AV.STATE-PROJ route]. A divergent channel whose anchor is a DIMENSIONFUL magnitude (Tier-2-dimensionful) is registry-PASS-INELIGIBLE; its Level-3 row is HELD as NOT-SATISFIED-PENDING-substrate-physical-scale-anchor, while the joint theorem-STRUCTURE may independently hold STAGE-3-PERMANENT via Stage-2 PASS-AND on the non-Level-3 clauses.**

That is the genuine emergence: the n_PBH dispute is not a one-off bookkeeping fix, it is the first instance forcing the criterion to distinguish a fixed-point-bearing observable from a divergent-magnitude observable, and it slots into the Level-2-binding taxonomy that already exists. I will register this as a 4-field carry-forward in R3's Wrap-Up.

**The boundary condition volovik's Tier-2 does not yet pin — and the S94 carry-forward must.** Tier-2 as stated routes a Tier-2-dimensionful anchor to "HELD pending a substrate-physical scale-anchor that is independently L_max-invariant." But it does not say WHAT such an anchor would be for n_PBH, nor whether one can exist in principle. My CONVERGENCE substrate-framing (and volovik's own R2 EMERGENCE final ¶) suggests the resolution direction: a DIMENSIONFUL number density is count-per-physical-volume; the substrate hands us the divergent count for free but NOT a truncation-invariant count-to-density conversion. So the substrate-physical scale-anchor, if it exists, must come from OUTSIDE the cardinality channel — a **cosmological-observable-anchored cutoff** (e.g., an L_max fixed by matching to a measured PBH-abundance or BBN constraint, making the truncation an EMPIRICAL input rather than a substrate-singled-out point) OR a **regularized/renormalized cardinality** that saturates by construction (a zeta-regulated or Pauli-Villars-subtracted N_eigs at Λ_UV = M_KK, per the framework's standard regularization pipeline). Whether EITHER yields a genuinely L_max-invariant dimensionful n_PBH is the open Sage-checkable sub-problem — and it is the SAME structural question as the framework's broader CC-regularization program (a divergent bulk sum regularized to a finite physical density). That cross-link (n_PBH-cardinality-regularization ↔ the substrate's standard UV-regularization at Λ_UV = M_KK) is the part of the emergence I think is most productive for S94, and I flag it as the carry-forward's "inputs" field: do NOT re-anchor to a bare cardinality functional; the re-determination must route through the regularization pipeline OR a cosmological-observable cutoff, both of which import an L_max-invariant scale from OUTSIDE the divergent count.

---

## Round 3 — volovik: Follow-up

### CONVERGENCE

**The verdict is locked. We jointly hold sub-question (d): the §VII.AX.OP-PROJ theorem-STRUCTURE stays STAGE-3-PERMANENT; the dimensionful `n_PBH = 7.2761e-23 m⁻³` Level-3 anchor row is HELD as NOT-SATISFIED-PENDING — NOT RE-ANCHORed, because no truncation-invariant dimensionful target exists. There is no verdict daylight between mack and me. I state precisely what we now jointly hold and what moved across the three rounds.**

**What we jointly hold (the four sub-questions, resolved).**

- **(a) — CONVERGED.** STAGE-3-PERMANENT survives, but ONLY on the theorem-STRUCTURE, not on the anchor number. mack's Re:V1 theorem-structure-vs-anchor separation is correct and I accepted it in R2 (CONVERGENCE 1): `eligibility=STAGE-3-PERMANENT-ELIGIBLE` (verdict line 77, `6833f005…`) qualifies the joint cross-axis bridge-claim — which `joint-theorem-promotion.md §"Stage 3"` places "alongside KO-dim=6, J-D_K=0," i.e., structural identities, not frozen numbers — while `provisional_truncation_note=True` qualifies the anchor's second coordinate (the L_max index). Two tags, two objects, no contradiction. I withdrew my V1 "a value cannot be both PERMANENT and PROVISIONAL" framing as imprecise in R2.

- **(b) — CONVERGED (the criterion is not meaningfully satisfiable at the anchor row).** "Canonical L_max" is not substrate-singled-out for this observable. mack conceded in Re:V2 that the L_max=14 index is circular ("canonical because obs_2 evaluated there"), and that the cardinality channel is Level-2-non-binding ON THE L_max AXIS — `c_continuum` on that axis is `+∞` (Sage). We agree the registry-PASS scalar inequality `Level-3(L*) < Level-2(L*)` has no admissible `L*` here, so the Level-3 ROW cannot be carried as satisfied; it is demoted to a PROVISIONAL annotation.

- **(c) — CONVERGED (band-membership is dead).** Both of us reproduced the Sage-QQ-exact breach `n_PBH(19)/upper = 1.24709 ∉ [5.5e-23, 2.2e-22]` (last in-band L=18 at 1.77% margin; first breach L=19 at 24.7% above). The band-membership predicate is a finite-window coincidence pinned to the obs_2 compute frontier (L=16), not a permanence carrier. Settled in R1 (mack's Re:V3 concession) and reaffirmed.

- **(d) — CONVERGED on HOLD-the-anchor.** mack conceded the verdict in R2 (CONVERGENCE): RE-ANCHOR required a dimensionful truncation-invariant target; my R2 DISSENT (iii) Sage-proved none exists (the log-derivative that buys truncation-invariance annihilates `ln A`, returning the dimensionless cascade exponent → 5; retaining the dimension retains the divergent `A·N_eigs → +∞` — dimension and divergence occupy the SAME multiplicative slot). mack re-ran both my load-bearing proofs from scratch (CONVERGENCE block, R2-mack): the log-derivative annihilation (`d ln N_eigs/d ln L|_{14}=4.25813 → 5`; `d²|_{14}=0.61832 → 0`, both finite, dimensionless) AND the frozen-by-fiat atlas (gap `80080 − 78080 = 2000 = dim_SU3(4,4)·16` EXACT). Both reproduce bit-exact. Both horns of mack's OWN M3-Q5 dichotomy — "frozen-by-fiat OR no dimensionful invariant exists → HOLD wins" — fired independently. The joint verdict is **theorem-structure STAGE-3-PERMANENT + Level-3 m⁻³ anchor HELD NOT-SATISFIED-PENDING-substrate-physical-scale-anchor**.

**What CHANGED across the three rounds.** The disagreement narrowed from a tag-vs-tag dispute to a single anchor-disposition word, then closed:

- **R1**: volovik DOWNGRADE-the-entry (STAGE-3-ELIGIBLE) vs mack RE-ANCHOR (theorem-structure PERMANENT + re-anchor the Level-3 number). Two genuinely different verdicts.
- **R2**: volovik withdrew the structure-tag downgrade (CONVERGENCE 4 — Stage-2 PASS-AND is genuine independent cross-axis evidence on the structure axis and does not evaporate from an anchor-coordinate defect); mack withdrew RE-ANCHOR (the dimensionful target does not exist; HOLD-the-anchor). The two positions met at: theorem-structure PERMANENT + anchor HELD.
- **R3 (now)**: identical. The residual after R2 was purely the registry-text mechanics of the demotion (which rows, which tags stay) — bookkeeping, not physics. I supply mack the exact scoping in QUESTIONS below.

**The decisive on-disk fact that closed (d) — mack's R2 verification of what Stage-2 actually certified.** I independently confirm mack's Re:V2-QUESTIONS answer against the verdict file this turn: the JOINT Element 5 PASS (W4-2, line 73 `ba202d16…`, BOTH axes, substrate-input-orthogonality structural ceiling NO caveat) certified the **MULTI-PIN-ATLAS regulator-class residue triple-pin** `R_zeta = R_Mellin = 141.44, R_PV = 114.46 M_KK²` — an algebra-INVARIANT residue in **M_KK² units**, a structurally DISTINCT observable from the dimensionful `n_PBH` in **m⁻³**. The n_PBH m⁻³ anchor was carried via **T1.13 PASS at L_max=14** (`1dc0a3fe…`), and the W4-3 INFO that triggered this workshop tags itself `JE5-orthogonal` (verdict line 70) — the m⁻³ ROW was NEVER inside the Stage-2 conjunction. This is the keystone: it simultaneously (i) protects the theorem-structure's Stage-2 status (no hole at Element 5 — the reviewers verified the anchor-STRUCTURE + the residue triple-pin, both genuine and surviving) AND (ii) confirms the m⁻³ row was never Stage-2-certified as a registry-PASS, so HOLDING it costs nothing the Stage-2 PASS-AND ever bought. Both halves of my R2-QUESTIONS disjunction point the same way: structure stands; m⁻³ row is HELD. The verdict is not a compromise — it is what the on-disk evidence forces.

### DISSENT

**Nothing substantive remains on the verdict.** mack and I have CONVERGED on sub-question (d) in full: theorem-structure STAGE-3-PERMANENT, m⁻³ Level-3 anchor HELD NOT-SATISFIED-PENDING. I have no residual disagreement with mack's R2 position, and I do not manufacture one for the sake of an adversarial round — that would violate the honest-convergence discipline.

The one residual is **registry-text mechanics, not verdict**, and it is mack's to execute as registry sole-writer (per `feedback_mack-bridge-role.md`). I flag it here only to confirm I see no physics disagreement in it, and I specify the exact scope in QUESTIONS (i) so mack's verdict turn has it pinned. For the record, mack's R2 DISSENT (3) already named the correct disposition — surgical demotion of the m⁻³ ROW (OP-PROJ Level-3 + the inherited STATE-PROJ Level-3 via T1.13), theorem-structure tag and MULTI-PIN-ATLAS residue Element-5 untouched — and I agree with it without amendment. mack's refinement of my R2 DISSENT (i) (the `(value, scheme)`-tuple discipline TRACKS a prediction but cannot SATISFY a scalar inequality at a non-existent point) is a sharpening I accept, not a disagreement: it is the precise statement of why the tuple-tag is the right bookkeeping for the HELD anchor and the wrong bookkeeping for a registry-PASS satisfier.

One forward-looking note, NOT a dissent: mack's R2 DISSENT (1) closed his own reserved cross-channel-scale RE-ANCHOR escape (Candidates A/B/C, all Sage-checked: ratio-×-fixed-scale reintroduces a frozen `(L,L')`; bottom-K substitution is a DIFFERENT observable, not a re-anchoring of n_PBH's total-count channel; `[invariant=5]×[scale]` reverse-engineers the scale to hit the target). I confirm I will not reopen any RE-ANCHOR path in this final turn either — the dimensionful target is proven non-existent on three independent routes (my DISSENT iii log-derivative + mack's three candidates), and reopening it would be the iterate-until-a-different-answer failure mode the workshop discipline forbids.

### EMERGENCE

**The durable workshop output is a structural law, not a one-off bookkeeping fix: for any multiplicative-normalization-divergent substrate-IS channel, every truncation-invariant functional is necessarily dimensionless — therefore a dimensionful anchor sourced from such a channel is NEVER truncation-invariant. `n_PBH` is the framework's inaugural Tier-2-dimensionful occupant. This is the genuine emergence, it is Sage-confirmed by both agents, and it generalizes into a registry-PASS-criterion refinement that the S94 audit-symmetry sweep must apply to the standing STAGE-3 set.**

**The law (substrate-first, with the substitution-chain logic that produces it).** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold=0.19))`; `N_eigs(L)` is the substrate's OWN Peter-Weyl block-admission cardinality, divergent because the SU(3) representation ring is infinite — extending L_max reveals MORE of the substrate's intrinsic cardinality cascade, by construction (this is a substrate PROPERTY, not a model-incompleteness artifact; the IS-not-IN reading of `phononic-framing.md`). For any observable in the `MULTIPLICATIVE-NORMALIZATION-CANCELLATION` class of `math-scripts.md` — `O(L, K) = W(L)·g(K)` with `W(L)` the L_max-dependent spectral-support weight and `g(K)` the L_max-INDEPENDENT kernel — the substitution chain (Sage-verified in R2 DISSENT iii Steps 1–5; mack reproduced bit-exact in his R2 CONVERGENCE block; I cite it as a settled prior result, not a new derivation):

```
ln O(L,K) = ln W(L) + ln g(K)
  ⇒ d^n ln O / d(ln K)^n  =  0  +  d^n ln g / d(ln K)^n      [the ln W(L) term is K-independent → annihilated]
```

The truncation-invariant content lives EXCLUSIVELY in the log-derivatives, because those — and ONLY those — annihilate `W(L)`. But a log-derivative measures FRACTIONAL change: it is dimensionless in the observable's own value. The dimension `[O]` is carried by the multiplicative prefactor inside `W(L)` (for n_PBH: `A_prefactor = 2.2517e-28 m⁻³`); the L_max-divergence is ALSO carried by `W(L)` (the `N_eigs(L)` count). **The dimension and the divergence occupy the SAME multiplicative slot.** Therefore:

> **For any multiplicative-normalization-divergent substrate-IS channel, every truncation-invariant functional is DIMENSIONLESS. A dimensionful anchor sourced from such a channel is NEVER truncation-invariant — killing the divergence (log-derivative) kills the dimension (`ln A → 0`); retaining the dimension (keeping `A`) retains the divergence (`A·N_eigs → +∞`). No middle functional is both dimensionful and truncation-invariant.**

**Why this is the DEEPER test (and why §VII.AV and §VII.AX are NOT the same case).** My V4 shrink-vs-diverge test is Tier-1 (does the residual-to-`c_continuum` shrink?). It is correct but insufficient: it tells you the anchor diverges, not whether RE-ANCHOR can rescue it. The Tier-2 test — is the divergent channel's truncation-invariant content dimensionless (RE-ANCHOR available) or is the anchor itself a dimensionful magnitude (RE-ANCHOR unavailable)? — is what actually distinguishes the two adjacent S93 entries:

- **§VII.AV.STATE-PROJ** re-anchored to `L_emp = -7.046336 M_KK²` (verdict line 50/52; STAGE-3-PERMANENT `adbc7004…`). `L_emp` is a SECOND LOG-DERIVATIVE — dimensionless-by-construction; its `M_KK²` units come from the K-window coordinate, NOT from a surviving magnitude prefactor. RE-ANCHOR was AVAILABLE because the anchor was already dimensionless-content. CONVERGENT (residual `2.82e-04` shrinking, Connes-Karoubi-bound).
- **§VII.AX.OP-PROJ** cannot re-anchor: its Level-3 anchor IS the dimensionful magnitude `7.2761e-23 m⁻³` itself, the very thing the log-derivative annihilates. RE-ANCHOR is UNAVAILABLE; the anchor is HELD.

So the framework's §VII.AV precedent does NOT cut toward promoting §VII.AX on a divergent anchor (mack's R1 reading) NOR toward a blanket downgrade (my R1 reading): it shows the correct response is dimension-keyed. §VII.AV could re-anchor because dimensionless; §VII.AX cannot because dimensionful; both keep theorem-structure PERMANENT when Stage-2 passes, and HOLD the anchor when no invariant target exists. `n_PBH` is the first framework instance to reach Tier-2-dimensionful (Tier-1 convergence FAILS AND the invariant content is a dimensionless exponent while the anchor is a dimensionful magnitude). This is a NEW structural cell, and §VII.AX.OP-PROJ is its inaugural occupant.

**The registry-PASS-criterion refinement (the candidate for S94, which mack endorsed and sharpened in his R2 EMERGENCE).** The current `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` — `Level-3 < Level-2 at canonical L_max` — silently presupposes a substrate-singled-out evaluation point, i.e., presupposes Tier-1 convergence without stating it. `n_PBH` exposed the silence. The refinement (SUGGESTION at K=1; mack's R2 EMERGENCE proposed text, which I endorse): a Level-3 anchor is registry-PASS-ELIGIBLE only if EITHER (Tier-1) its residual-to-`c_continuum` shrinks with L_max, OR (Tier-2) its divergent channel's truncation-invariant content is DIMENSIONLESS and the anchor is RE-ANCHORED to that invariant functional (the §VII.AV.STATE-PROJ route). A Tier-2-DIMENSIONFUL anchor is registry-PASS-INELIGIBLE; its Level-3 row is HELD NOT-SATISFIED-PENDING-substrate-physical-scale-anchor, while the joint theorem-STRUCTURE may independently hold STAGE-3-PERMANENT via Stage-2 PASS-AND on the non-Level-3 clauses. This dovetails with the EXISTING Level-2-binding-vs-non-binding sub-class (`c_continuum` undefined → Level-2-non-binding → FORBIDDEN for registry-PASS); Tier-2 makes the dimensionful corollary explicit. I note this is a methodology refinement to a `.claude/rules/` file — see QUESTIONS (ii) for the edit-authority routing.

**The S94 audit-symmetry sweep this implies (the shrink-vs-diverge ∘ dimension test against the standing STAGE-3 set).** The refinement is a downgrade-or-hold rule keyed on "truncation-divergent Level-3 anchor"; per the audit-symmetry obligation I flagged in V4 (and mack accepted as a carry-forward), it MUST be checked against EVERY standing STAGE-3 promotion, not just n_PBH. The standing set is `{§VII.AV.STATE-PROJ (line 62 adbc7004…), §VII.AU.OP-PROJ (line 31), §VII.AW.OP-PROJ, §VII.AY (W5-2 d40041c3…), §VII.AH (S90 W2 CF-20, FIRST), §VII.U.2 Corner-II Var_a (S92 W4-7, SECOND), §VII.AX.MULTI-PIN-ATLAS (line 73 ba202d16…)}`. The two-tier instrument is Sage-checkable per entry: (1) compute `lim_{L→∞} [residual-to-c_continuum]` — shrinks → Tier-1-PASS; diverges → Tier-2; (2) for Tier-2, check whether the anchor's units are carried by a surviving multiplicative prefactor (dimensionful → INELIGIBLE, HOLD) or by the K-window coordinate / are a pure index-fact (dimensionless → RE-ANCHOR-eligible). My prediction (NOT adjudicated here — it is the S94 gate's job): the standing set are all Tier-1-convergent (the residue triple-pins, `L_emp`) or Tier-2-dimensionless (log-derivatives, ratios) or pure index-facts (KO-dim=6-class). If so, `n_PBH`'s Tier-2-dimensionful status is genuinely anomalous within the standing STAGE-3 table — which is itself the evidence that the m⁻³ anchor's promotion (now corrected to HELD) was the lone premature anchor-axis landing. The sweep is the symmetric obligation: a rule that demotes n_PBH's anchor must be applied uniformly, and the prediction is that n_PBH is the only entry it touches.

**Substrate-first framing of the emergence.** A dimensionful number density is a count-per-physical-volume. The substrate hands us the divergent count (`N_eigs`, the infinite Peter-Weyl cascade) for free; it does NOT hand us a truncation-invariant way to convert that count into a finite density, because the conversion factor `A = (density per count)` MULTIPLIES the divergent count rather than regularizing it. The substrate is telling us, through the divergence, that the PBH number density at this pole is not a truncation-invariant observable of the bare cardinality channel — it is a truncation-DEPENDENT projection, and its permanence (if any) must be sourced from a DIFFERENT substrate-physical invariant. mack's R2 EMERGENCE named the resolution direction precisely, and I endorse it as the carry-forward's structural target: the L_max-invariant scale must come from OUTSIDE the cardinality channel — either a regularized/renormalized `N_eigs` that saturates by construction (zeta-regulated or Pauli-Villars-subtracted at Λ_UV = M_KK, the framework's standard regularization pipeline) OR a cosmological-observable-anchored cutoff (an L_max fixed by matching a measured PBH-abundance / BBN constraint, making the truncation an EMPIRICAL input rather than a substrate-singled-out point). The cross-link mack drew is the most productive part of the emergence: re-determining n_PBH's cutoff is the SAME structural problem as the framework's broader UV-regularization-of-a-divergent-bulk-sum program (a divergent cardinality regularized to a finite physical density at Λ_UV = M_KK). That is the right home for the S94 carry-forward, and the explicit instruction it must carry: do NOT re-anchor to a bare cardinality functional — route through the regularization pipeline OR a cosmological cutoff, both of which import an L_max-invariant scale from outside the divergent count.

### QUESTIONS

**No open questions to mack on the verdict — we have converged. This section instead supplies mack the EXACT inputs his verdict turn needs, as the spawn instructs. Three items: (i) the precise registry-text scoping; (ii) the methodology-refinement edit-authority routing; (iii) the CF-S94 carry-forward shape.**

**(i) Registry-text scoping — which rows demote to NOT-SATISFIED-PENDING, which tags/anchors stay UNTOUCHED (verified against the registry on-disk this turn).** The demotion is surgical: it cuts exactly the dimensionful **m⁻³ Level-3 ROW** and nothing adjacent.

DEMOTE to `NOT-SATISFIED-PENDING-substrate-physical-scale-anchor` (Level-3 ROW only; the SCALAR registry-PASS inequality at canonical L_max):

1. **§VII.AX.OP-PROJ Level-3 row** — registry line 19434 (the `n_PBH(L_max=14) = 7.2761e-23 m⁻³` Level-3 EMPIRICAL CONFIRMATION row) AND the "Registry-PASS criterion" line 19436 AND Element-5 line 19448. These currently assert "Level 3 satisfies Level 2 envelope at canonical L_max=14" — that scalar-inequality claim is what demotes. Tag the row: the central value 7.2761e-23 m⁻³ is the substrate-current best central (T1.13-authoritative, which I concede), carried as `(value, canonical-L_max=PROVISIONAL, no-truncation-invariant-dimensionful-target, re-determination=CF-S94)`; the registry-PASS Level-3 ROW is `NOT-SATISFIED-PENDING-substrate-physical-scale-anchor` per W4-3 INFO + this workshop.

2. **§VII.AX.STATE-PROJ Level-3 row** — registry line 19569 (the `n_PBH^STATE = 7.2761e-23 m⁻³` Level-3, INHERITED from OP-PROJ T1.13) AND its "Registry-PASS criterion" line 19571. This INHERITS the demotion via T1.13 (the STATE-PROJ Element-5 is the SAME m⁻³ magnitude inherited from OP-PROJ; line 19579 confirms `INHERITED from §VII.AX.OP-PROJ T1.13`). It must be tagged consistently so the companion's m⁻³ row does NOT assert a PASS the parent no longer claims. (STATE-PROJ is already STAGE-1-CANDIDATE on its own structure tag — that is independent and stays; only its inherited m⁻³ Level-3 ROW demotes.)

LEAVE UNTOUCHED (do NOT let the demotion bleed onto these):

3. **The theorem-STRUCTURE tag — STAGE-3-PERMANENT stays on BOTH §VII.AX.OP-PROJ (line 19367/19371 Status) and the STAGE-3-PERMANENT-set membership.** Stage-2 PASS-AND earned the structure tag on the axis it tests; the anchor-coordinate defect does not vacate it. This is CONVERGENCE 4 / mack's Re:V4 Step-5.

4. **§VII.AX.MULTI-PIN-ATLAS — entire entry UNTOUCHED (lines 19617+).** Its Level-3 is the regulator-class residue triple-pin `R_zeta = R_Mellin = 141.44, R_PV = 114.46 M_KK²` — a DIFFERENT, algebra-INVARIANT, M_KK²-unit observable. The JOINT Element 5 Stage-2 PASS-AND (W4-2, line 73 `ba202d16…`, structural ceiling NO caveat) certified THIS residue, and it is genuine and survives. The m⁻³ demotion does NOT touch the M_KK² residue Element-5.

5. **§VII.AX.OP-PROJ Level-1 + Level-2 rows (lines 19432, 19433) UNTOUCHED.** The g-independence Level-1 structural identity is sound (it quantifies over the SEPARATE cascade-generation index g at FIXED atlas; it is the Level-3 row reading its number at a free L_max snapshot that is the defect, not the Level-1 theorem). Only the Level-3 scalar-inequality row demotes.

Net: the demotion is a Level-3-ROW-only surgical edit on the dimensionful m⁻³ anchor (OP-PROJ + inherited STATE-PROJ); the theorem-structure tags, the STAGE-3-PERMANENT-set memberships, the MULTI-PIN-ATLAS residue Element-5, and the OP-PROJ Level-1/Level-2 rows all stay. This matches your own R2 DISSENT (3) scoping exactly.

**(ii) The methodology refinement is an ORCHESTRATOR-DIRECT item — FLAG it, do NOT edit it.** The registry-PASS-criterion refinement (Tier-1/Tier-2 dimensional re-anchorability gate) is an edit to `.claude/rules/cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"`. Subagents — both you and me — are edit-denied on `.claude/rules/` by harness convention (the same closure that makes the methodology-wave-allowlist orchestrator-only, and the reason the S92 §VII.AV/AX/AY directive mirrors carry "subagents are edit-denied on `.claude/rules/`"). Therefore: in your Wrap-Up `## Effected In-Session (NON-MATH)` list, do NOT record this as an effected rule-edit. Instead FLAG it as an ORCHESTRATOR-DIRECT methodology item — the orchestrator mirrors the verbatim directive into the rule file (or the calibration corpus at `sessions/framework/registry/cross-pillar-bridge-corpus.md`, where you as mack-cosmic-bridge CAN write) post-workshop, at SUGGESTION K=1, promoting to MANDATORY at K=3 per `feedback_rules-compensate-missing-structure.md`. The corpus row + the rule-file flag are the two deliverables; the rule-file edit itself is the orchestrator's, not yours.

**(iii) The CF-S94 carry-forward shape (4-field spec).** Register this in your Wrap-Up `## Carry-Forward Computations`:

- **What**: re-determine the canonical truncation / substrate-physical scale-anchor for `n_PBH` from OUTSIDE the cardinality channel — either (a) a regularized/renormalized `N_eigs` (zeta-regulated or Pauli-Villars-subtracted at Λ_UV = M_KK, the framework's standard regularization pipeline) that saturates by construction, OR (b) a cosmological-observable-anchored cutoff (L_max fixed by matching a measured PBH-abundance / BBN constraint, making the truncation an EMPIRICAL input). Goal: a genuinely L_max-invariant DIMENSIONFUL n_PBH, or a Sage-proof that none exists (in which case the m⁻³ Level-3 row is permanently NOT-SATISFIED and n_PBH stands as a theorem-structure-only STAGE-3-PERMANENT with no registry-PASS Level-3).
- **Inputs**: do NOT re-anchor to a bare cardinality functional (proven non-existent on three routes: R2 DISSENT iii log-derivative + R2-mack Candidates A/B/C). Route through the substrate UV-regularization pipeline (S61/S78 Pauli-Villars at Λ_UV = M_KK) OR a Pillar-IX cosmological cutoff. Cross-link: this IS the same structural problem as the framework's CC-regularization program (divergent bulk sum → finite physical density). Upstream: W4-3 INFO `03b4fb35…` (the divergence proof); T1.13 `1dc0a3fe…` (the current provisional central); the Tier-2 generalization (this workshop EMERGENCE).
- **Gate**: PASS iff the re-determined dimensionful n_PBH is L_max-stationary (residual-to-fixed-value shrinks with L_max, OR is regulator-class-invariant under the FULL regularization at Λ_UV = M_KK) within a pre-registered tolerance; INFO iff a dimensionless invariant exists but the dimensionful magnitude remains L_max-tied (confirms the m⁻³ row stays NOT-SATISFIED); FAIL iff the regularization itself diverges.
- **Effort**: ~1 wave-equivalent (re-uses the standard PV/zeta pipeline; the new work is applying it to the `N_eigs` cardinality sum rather than to a bottom-K functional).

Also register the **S94 audit-symmetry sweep** as a SEPARATE carry-forward (the two-tier shrink-vs-diverge ∘ dimension test applied to the standing STAGE-3 set `{§VII.AV.STATE-PROJ, §VII.AU.OP-PROJ, §VII.AW.OP-PROJ, §VII.AY, §VII.AH, §VII.U.2 Corner-II Var_a, §VII.AX.MULTI-PIN-ATLAS}`); prediction = n_PBH is the only Tier-2-dimensionful occupant, but the rule must be applied uniformly to verify it. And note the AU/AW ordinal-collision hygiene CF (`CF-S94-STAGE-3-ORDINAL-COLLISION-AU-AW`) already exists in the verdict file (lines 77/99/101) — it is adjacent but SEPARATE from this workshop's output; do not fold them.

That is everything your verdict turn needs. The Verdict table rows resolve as: (1) Converged, (2) Converged, (3) Converged, (4) Converged-on-HOLD-the-anchor. The durable structural output for the Wrap-Up `## What Holds` is the Tier-2-dimensionful generalization; the `## What Changed` is the anchor disposition (RE-ANCHOR → HOLD, theorem-structure tag UNCHANGED throughout); the `## What Breaks or Strains` is the m⁻³ Level-3 row (demoted, surgically). Over to you.

---

## Round 3 — mack: Cross-Synthesis

### CONVERGENCE

**The verdict is jointly locked, and I have EXECUTED the registry disposition it implies. I confirm volovik's R3 CONVERGENCE in full — there is no verdict daylight — and I record the on-disk demotion as effected (not merely agreed). The four sub-questions resolve exactly as volovik states; I add the registry-write closure that was mine alone to perform as sole-writer.**

**1. I confirm volovik's R3 keystone (what Stage-2 actually certified), and it is now load-bearing in the registry text I wrote.** volovik's R3 CONVERGENCE (line 548) independently re-verified my R2 answer against the verdict file: the JOINT Element 5 PASS-AND (W4-2, line 73 `ba202d16…`, both axes, substrate-input-orthogonality structural ceiling NO caveat) certified the **MULTI-PIN-ATLAS regulator-class residue triple-pin** `R_zeta = R_Mellin = 141.44, R_PV = 114.46 M_KK²` — an algebra-INVARIANT residue in **M_KK² units** — and the **anchor-STRUCTURE** (the 5-anatomy form), NOT the dimensionful `n_PBH` m⁻³ scalar inequality. The W4-3 INFO self-tags `JE5-orthogonal` (verdict line 70). This is the keystone because it makes the joint verdict NON-lossy: HOLDING the m⁻³ Level-3 row costs the framework nothing the Stage-2 PASS-AND ever bought, because the m⁻³ row was never inside the Stage-2 conjunction. I have written exactly this into the demoted registry rows (19434, 19436, 19448, 19569, 19571) — each cites `JE5-orthogonal` and scopes the HOLD to the dimensionful scalar while preserving the structure tag and the M_KK² residue Element-5. The registry now SAYS what we jointly hold.

**2. The two HOLD-horns of my own M3-Q5 dichotomy both fired — I bind to my pre-registered concession.** I authored the dichotomy "frozen-by-fiat OR no dimensionful invariant exists → HOLD wins" (M3-Q5) and pre-registered the concession terms in R1 (Re:V3-Q3 EMERGES). Both horns are now established, each Sage-reproduced bit-exact by me from scratch (R2-mack CONVERGENCE block): (horn 1) no dimensionful truncation-invariant target exists — the log-derivative that buys truncation-invariance annihilates the m⁻³ prefactor (`d ln N_eigs/d ln L|_{14}=4.25813 → 5`; `d²|_{14}=0.61832 → 0`, both dimensionless); (horn 2) the L_max=10 atlas N=78,080 is frozen-by-fiat (gap `80080 − 78080 = 2000 = dim_SU3(4,4)·16` EXACT — the cache ceiling minus a dropped sector, not a saturation point). I do not get to keep RE-ANCHOR on a dichotomy I wrote when both of its HOLD-horns fired. RE-ANCHOR → HOLD-the-anchor. Conceded in R2; reaffirmed and now effected.

**3. The trajectory across rounds, stated for the record.** R1: volovik DOWNGRADE-the-entry (STAGE-3-ELIGIBLE) vs mack RE-ANCHOR (structure PERMANENT + re-anchor the number) — two genuinely different verdicts. R2: volovik withdrew the structure-tag downgrade (Stage-2 PASS-AND is genuine independent cross-axis evidence on the structure axis); mack withdrew RE-ANCHOR (no dimensionful target — volovik DISSENT iii, which I re-ran and confirmed). R3 (now): identical, and EXECUTED. The disagreement narrowed from tag-vs-tag → single anchor-disposition word → closed; the residual was registry-text mechanics, which I have now performed surgically.

**4. The registry disposition is on disk (EXECUTED this turn).** Per volovik's R3 QUESTIONS (i) scoping — which I verified row-by-row against the live registry before editing (line numbers had drifted from his ~19434/19569 estimates; I matched on row TEXT, not blind line numbers) — I demoted exactly the dimensionful m⁻³ Level-3 ROWS and nothing adjacent: §VII.AX.OP-PROJ Level-3 status cell (19434), Registry-PASS criterion paragraph (19436), Element-5 anatomy item (19448); §VII.AX.STATE-PROJ inherited Level-3 status cell (19569) + Registry-PASS criterion paragraph (19571). LEFT UNTOUCHED (verified by grep): the STAGE-3-PERMANENT theorem-structure tags + STAGE-3-PERMANENT-set memberships (101 STAGE-3-PERMANENT occurrences intact); the §VII.AX.MULTI-PIN-ATLAS M_KK² residue Element-5 (19665); the OP-PROJ Level-1 g-independence identity (19432) + Level-2 envelope (19433). The demotion cuts the dimensionful m⁻³ scalar-inequality SATISFACTION; it does not touch the structure, the residue, or the cohomology-class-layer Level-1/Level-2. This matches my own R2 DISSENT (3) scoping and volovik's R3 QUESTIONS (i) without amendment.

### DISSENT

**No verdict dissent. None manufactured.** volovik and I converged on sub-question (d) in full at R2 and reaffirmed at R3: theorem-STRUCTURE STAGE-3-PERMANENT; dimensionful m⁻³ Level-3 anchor HELD NOT-SATISFIED-PENDING-substrate-physical-scale-anchor (NOT RE-ANCHORed — no truncation-invariant dimensionful target exists). To invent a residual disagreement for the sake of an adversarial final round would violate the honest-convergence discipline (`Investigating-Workshops.md`: a workshop produces a STRUCTURAL VERDICT, and ours is joint).

**The one thing I withhold from the registry text — and it is a scope discipline, not a dissent.** volovik's R3 EMERGENCE proposes the Tier-1/Tier-2 registry-PASS-criterion refinement as an edit to `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"`. I AGREE with the refinement's content (I co-proposed it in R2 EMERGENCE and sharpened its status to a Level-2-binding-adjacent criterion extension). But I did NOT write it into any `.claude/rules/` file, and I did NOT record it as an effected rule-edit, because subagents — both volovik and me — are edit-denied on `.claude/rules/` by harness convention (the same closure that makes the methodology-wave-allowlist orchestrator-only, and why the S92 §VII.AV/AX/AY directive mirrors carry "subagents are edit-denied on `.claude/rules/`"). It is FLAGGED as an ORCHESTRATOR-DIRECT item with EXACT proposed text in the Effected-In-Session list below. This is the L4 honesty-disclosure boundary of `mechanical-closure-discipline.md` applied to rule-edits: I name precisely what I did (registry Level-3 rows) and precisely what I did NOT do (the rule-file edit), so the orchestrator lands the latter and no one mis-reads the workshop as having silently changed a rule.

**One forward-looking note, NOT a dissent (closing my own reserved escape so R3 cannot reopen it).** In R1 (M3-Q1) I reserved a cross-channel-scale RE-ANCHOR escape: `[truncation-invariant dimensionless ratio] × [an L_max-independent substrate scale imported from a DIFFERENT channel]`. I closed it Sage-exact in R2 DISSENT (1), Candidates A/B/C (ratio-×-fixed-scale reintroduces a frozen `(L,L')`; bottom-K substitution is a DIFFERENT observable, not a re-anchoring of n_PBH's total-count channel; `[invariant=5]×[scale]` reverse-engineers the scale to hit the target — circular). I confirm I do NOT reopen any RE-ANCHOR path in this final turn; the dimensionful target is proven non-existent on three independent routes (volovik's DISSENT iii log-derivative + my three candidates). Reopening it would be the iterate-until-a-different-answer failure mode (`v3-closure-recovery.md` PROHIBITED_ACTIONS Class 6) the workshop discipline forbids.

### EMERGENCE

**The durable workshop output is a structural law I now state in final form, jointly held with volovik and Sage-confirmed by both of us from scratch: for any multiplicative-normalization-divergent substrate-IS channel, every truncation-invariant functional is necessarily DIMENSIONLESS — therefore a dimensionful anchor sourced from such a channel is NEVER truncation-invariant. `n_PBH` is the framework's inaugural Tier-2-dimensionful occupant. This is not a one-off bookkeeping fix; it amends the registry-PASS criterion and seeds the S94 audit-symmetry sweep.**

**The law (substrate-first, with the substitution chain that produces it — per `math-scripts.md §"Double-Check Logic"`).** The substrate IS the spectral triple `(A_K, H_K, D_K(τ_fold=0.19))`; `N_eigs(L)` is the substrate's OWN Peter-Weyl block-admission cardinality, divergent because the SU(3) representation ring is infinite — extending L_max reveals MORE of the substrate's intrinsic cardinality cascade by construction (a substrate PROPERTY, not a model-incompleteness artifact; the IS-not-IN reading of `phononic-framing.md`: L_max is not a container the substrate sits in, it is a truncation of the substrate's own infinite structure). For any observable in the `MULTIPLICATIVE-NORMALIZATION-CANCELLATION` class of `math-scripts.md` — `O(L, K) = W(L)·g(K)` with `W(L)` the L_max-dependent spectral-support weight and `g(K)` the L_max-INDEPENDENT kernel:

```
ln O(L,K) = ln W(L) + ln g(K)
  ⇒ d^n ln O / d(ln K)^n  =  0  +  d^n ln g / d(ln K)^n      [the ln W(L) term is K-independent → annihilated]
```

The truncation-invariant content lives EXCLUSIVELY in the log-derivatives, because those — and only those — annihilate `W(L)`. But a log-derivative measures FRACTIONAL change: it is dimensionless in the observable's own value. The dimension `[O]` is carried by the multiplicative prefactor inside `W(L)` (for n_PBH: `A = 2.2517e-28 m⁻³`); the L_max-divergence is ALSO carried by `W(L)` (the `N_eigs(L)` count). **The dimension and the divergence occupy the SAME multiplicative slot.** Therefore killing the divergence (log-derivative) kills the dimension (`ln A → 0`); retaining the dimension (keeping `A`) retains the divergence (`A·N_eigs → +∞`). No middle functional is both dimensionful and truncation-invariant. I verified this on n_PBH directly (R2 DISSENT 1 Candidates A/B/C + the log-derivative table, all bit-exact).

**This is the DEEPER test, and it is why §VII.AV and §VII.AX are NOT the same case (the resolution of the precedent question that ran through all four rounds).** My V4-adopted shrink-vs-diverge test is Tier-1 (does the residual-to-`c_continuum` shrink?). It is correct but insufficient — it tells you the anchor diverges, not whether RE-ANCHOR can rescue it. The Tier-2 test — is the divergent channel's truncation-invariant content dimensionless (RE-ANCHOR available) or is the anchor itself a dimensionful magnitude (RE-ANCHOR unavailable)? — distinguishes the two adjacent S93 entries cleanly:

| Entry | Level-3 anchor | Tier-1 (converge?) | Tier-2 (invariant content) | Disposition |
|:------|:---------------|:-------------------|:---------------------------|:------------|
| §VII.AV.STATE-PROJ | `L_emp = -7.046336 M_KK²` (2nd log-derivative — **dimensionless-content**; M_KK² is the K-window coordinate's unit, NOT a surviving magnitude) | CONVERGES (residual 2.82e-04 shrinking, Connes-Karoubi-bound) | dimensionless | RE-ANCHORED → STAGE-3-PERMANENT (earned) |
| §VII.AX.OP-PROJ | `n_PBH = 7.2761e-23 m⁻³` (**dimensionful magnitude** — the very thing the log-derivative annihilates) | DIVERGES (`lim N_eigs = +∞`) | dimensionful (invariant content is only the dimensionless cascade exponent → 5) | RE-ANCHOR UNAVAILABLE → HELD NOT-SATISFIED-PENDING |

So the §VII.AV precedent cuts neither toward promoting §VII.AX on a divergent anchor (my R1 reading) NOR toward a blanket downgrade (volovik's R1 reading): the correct response is dimension-keyed. §VII.AV could re-anchor because its anchor was already dimensionless-content; §VII.AX cannot because its anchor is the dimensionful magnitude itself. Both keep theorem-structure PERMANENT when Stage-2 passes; both HOLD the anchor when no invariant target exists. `n_PBH` is the inaugural Tier-2-dimensionful cell.

**Why this is a registry-PASS-CRITERION refinement, not just an audit instrument.** The current `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` — `Level-3 < Level-2 at canonical L_max` — silently presupposes a substrate-singled-out evaluation point, i.e., presupposes Tier-1 convergence WITHOUT stating it. `n_PBH` exposed the silence. The refinement (which I authored in R2 EMERGENCE and volovik endorsed in R3) adds the missing precondition, and it dovetails with the EXISTING Level-2-binding-vs-non-binding sub-class (a Level-2-non-binding envelope is ALREADY FORBIDDEN for registry-PASS when `c_continuum` undefined; Tier-2 makes the dimensionful corollary explicit). The exact proposed rule-text is in the Effected-In-Session FLAG list (orchestrator-direct), at SUGGESTION K=1, promoting to MANDATORY at K=3 per `feedback_rules-compensate-missing-structure.md`.

**Substrate-first framing of the emergence (the substrate is telling us something physical).** A dimensionful number density is a count-per-physical-volume. The substrate hands us the divergent count (`N_eigs`, the infinite Peter-Weyl cascade) for free; it does NOT hand us a truncation-invariant way to convert that count into a finite density, because the conversion factor `A = (density per count)` MULTIPLIES the divergent count rather than regularizing it. The substrate is telling us, through the divergence, that the PBH number density at this pole is NOT a truncation-invariant observable of the bare cardinality channel — it is a truncation-DEPENDENT projection, and its permanence (if any) must be sourced from a DIFFERENT substrate-physical invariant. The resolution direction (which I named in R2 EMERGENCE and volovik endorsed in R3): the L_max-invariant scale must come from OUTSIDE the cardinality channel — either a regularized/renormalized `N_eigs` that saturates by construction (zeta-regulated or Pauli-Villars-subtracted at Λ_UV = M_KK, the framework's standard regularization pipeline) OR a cosmological-observable-anchored cutoff (an L_max fixed by matching a measured PBH-abundance / BBN constraint, making the truncation an EMPIRICAL input rather than a substrate-singled-out point). **The cross-link I draw is the most productive part of the emergence: re-determining n_PBH's cutoff is the SAME structural problem as the framework's broader UV-regularization-of-a-divergent-bulk-sum program** — a divergent cardinality regularized to a finite physical density at Λ_UV = M_KK, structurally identical to how the CC-regularization program turns a divergent bulk sum into a finite physical density. That is the home of the CF-S94 carry-forward and the explicit instruction it must carry: do NOT re-anchor to a bare cardinality functional; route through the regularization pipeline OR a cosmological cutoff, both of which import an L_max-invariant scale from outside the divergent count.

**Observational-cosmology note (my bridge-role lens, surfaced because it is the productive S94 hook).** The cosmological-observable-anchored-cutoff branch is where this connects to real data: a PBH number density at the framework's `M_PBH_typical = M_LRD · 2^{-g_BBN}` scale is constrained at the relevant mass window by CMB-μ-distortion / PTA / LISA bounds and by BBN. If the canonical truncation is fixed by matching one of those, then the m⁻³ "prediction" becomes a CONSISTENCY statement (the substrate's cascade is compatible with the measured abundance at the empirically-fixed cutoff) rather than a parameter-free point prediction — which is the honest status for a Tier-2-dimensionful observable. That re-framing is itself worth registering: it would move n_PBH from the "parameter-free prediction" column to the "substrate-consistent-with-observation at an empirically-anchored cutoff" column, and it should NOT be reported as a parameter-free win until/unless the regularized-N_eigs branch yields a genuinely L_max-invariant dimensionful magnitude (CF-S94 gate).

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | (a) Permanence vs divergent anchor | V1, Re:V1, R2-volovik CONV-1, R3 | **Converged** | STAGE-3-PERMANENT survives, but ONLY on the joint cross-axis theorem-STRUCTURE (the 5-anatomy bridge claim, placed "alongside KO-dim=6, J-D_K=0" by `joint-theorem-promotion.md §"Stage 3"` — structural identities, not frozen numbers). The W4-5 line is NOT a contradiction: `eligibility=STAGE-3-PERMANENT-ELIGIBLE` qualifies the theorem-STRUCTURE; `provisional_truncation_note=True` qualifies the anchor's second coordinate (the L_max index). Two tags, two objects. |
| 2 | (b) Canonical-L_max provisionality | V2, Re:V2, R2-volovik DISSENT(i)/(ii), R3 | **Converged** | "Canonical L_max" is NOT substrate-singled-out for this observable: L_max=14 is circular (canonical only because obs_2 evaluated there), and the cardinality channel is truncation-DIVERGENT (`lim_{L→∞} N_eigs = +∞`, Sage) ⇒ `c_continuum` on the L_max axis is `+∞` ⇒ Level-2-non-binding on that axis. The registry-PASS scalar inequality `Level-3(L*) < Level-2(L*)` has no admissible `L*`; the dimensionful Level-3 ROW is UNDEFINED (not satisfied) and is demoted to NOT-SATISFIED-PENDING. |
| 3 | (c) Band-membership permanence carrier | V3, Re:V3, R2-volovik CONV-3, R3 | **Converged** | The band-membership predicate `n_PBH_central(L) ∈ [5.5e-23, 2.2e-22] ∀L` is a finite-window coincidence, DEAD as a permanence carrier. Both agents reproduced the Sage-QQ-exact breach `n_PBH(19)/upper = 1.24709 ∉ band` (last in-band L=18 at 1.77% margin; first breach L=19 at 24.7% above). "Holds at every COMPUTED truncation" sets the bar at the obs_2 compute frontier (L=16), not at the substrate's own infinite structure — pinning permanence to compute budget. |
| 4 | (d) Structural verdict (STANDS / DOWNGRADE / RE-ANCHOR) | V1–V4, M1–M3, R2 both, R3 both | **Converged → HOLD-the-anchor** (+ **Emerged**: Tier-2-dimensionful law) | Theorem-STRUCTURE STAGE-3-PERMANENT (Stage-2 PASS-AND on the NON-Level-3 clauses is genuine independent cross-axis evidence) + dimensionful m⁻³ Level-3 anchor HELD NOT-SATISFIED-PENDING — NOT RE-ANCHORed, because no truncation-invariant dimensionful target exists (Sage-proven on 4 routes: volovik DISSENT iii log-derivative annihilation + mack Candidates A/B/C). EMERGED structural law: for any multiplicative-normalization-divergent channel, every truncation-invariant functional is dimensionless ⇒ a dimensionful anchor is never truncation-invariant; `n_PBH` is the inaugural Tier-2-dimensionful occupant. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **Does a genuinely L_max-invariant DIMENSIONFUL n_PBH exist from OUTSIDE the cardinality channel?** Re-determine the canonical truncation / substrate-physical scale-anchor via (a) regularized/renormalized `N_eigs` (zeta-regulated or Pauli-Villars-subtracted at Λ_UV = M_KK — the framework's standard pipeline) that saturates by construction, OR (b) a cosmological-observable-anchored cutoff (L_max fixed by matching a measured PBH-abundance / BBN constraint). Decision: PASS ⇒ re-anchor the m⁻³ Level-3 row and lift the HOLD; INFO ⇒ only a dimensionless invariant exists, m⁻³ row stays NOT-SATISFIED permanently and n_PBH is a theorem-structure-only STAGE-3-PERMANENT with no registry-PASS Level-3; FAIL ⇒ the regularization itself diverges. (→ CF-S94-N-PBH-CANONICAL-TRUNCATION-RE-DETERMINATION.)

2. **Is `n_PBH` the ONLY Tier-2-dimensionful occupant of the standing STAGE-3 set?** Apply the two-tier shrink-vs-diverge ∘ dimension test to `{§VII.AV.STATE-PROJ, §VII.AU.OP-PROJ, §VII.AW.OP-PROJ, §VII.AY, §VII.AH, §VII.U.2 Corner-II Var_a, §VII.AX.MULTI-PIN-ATLAS}`. Per-entry Sage check: (i) does residual-to-`c_continuum` shrink with L_max (Tier-1-PASS) or diverge (→ Tier-2)? (ii) for Tier-2, are the anchor's units carried by a surviving multiplicative prefactor (dimensionful → INELIGIBLE/HOLD) or by the K-window coordinate / a pure index-fact (dimensionless → RE-ANCHOR-eligible)? Prediction (NOT adjudicated): all standing entries are Tier-1-convergent (residue triple-pins, `L_emp`) or Tier-2-dimensionless (log-derivatives, ratios, index-facts like KO-dim=6) ⇒ n_PBH is anomalous, the lone premature anchor-axis landing — but the rule MUST be applied uniformly to verify it. (→ S94 audit-symmetry sweep CF.)

3. **Does the Tier-1/Tier-2 dimensional-re-anchorability gate land as a `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` refinement (SUGGESTION K=1)?** ORCHESTRATOR-DIRECT (subagents edit-denied on `.claude/rules/`). Exact proposed rule-text + corpus K-counter row are FLAGGED in the Effected-In-Session list below; the orchestrator lands them. Promotes to MANDATORY at K=3 per `feedback_rules-compensate-missing-structure.md`.

4. **Is the CMB-μ-distortion / PTA / LISA / BBN observational window at the framework's `M_PBH_typical = M_LRD · 2^{-g_BBN}` scale tight enough to FIX the cosmological-observable-anchored cutoff (Open Q1 branch b)?** This is the bridge-role sub-question: if the empirical PBH-abundance bound at the relevant mass window pins L_max, n_PBH becomes a substrate-consistent-with-observation statement at an empirically-anchored cutoff (NOT a parameter-free point prediction). Requires concrete current bounds at the mass window. (→ feeds CF-S94 branch (b) inputs.)

5. **Does the registered Level-1 g-independence theorem need its OWN scoping caveat?** The Level-1 identity holds N_eigs FIXED at the L_max=10 atlas and saturates the SEPARATE cascade-generation index g; W4-3 + R2 showed the L_max=10 atlas (N=78,080) is itself frozen-by-fiat (≠ analytic 80,080; gap = dropped (4,4) sector). The Level-1 row was LEFT UNTOUCHED (it is a g-saturation theorem at fixed atlas, structurally sound) — but should it carry an explicit "fixed-atlas-is-a-truncation-not-a-saturation-of-L_max" annotation so a future reader does not re-read it as an L_max-saturation claim? (Registry-text hygiene; route to S94 housekeeping if affirmed.)

## Wrap-Up — Workshop Impact Summary

### What Changed

*(numerical-vs-structural split per `output-standards.md` T1-13)*

#### (a) Numerical revisions

- `n_PBH(19)/upper`: `1.24709` confirmed Sage-QQ-exact by BOTH agents (independent recomputation) — the band-membership breach point. Last in-band L=18 at margin `1.77%` below the conjunct-upper; first breach L=19 at `24.71%` above. This is the number that killed the band-membership permanence carrier.
- `d ln N_eigs/d ln L|_{L=14} = 4.25813 → lim_{L→∞} = 5` (dimensionless); `d² ln N_eigs/d(ln L)²|_{L=14} = 0.61832 → lim = 0` (dimensionless) — the log-derivative annihilation values, reproduced bit-exact by both agents. The truncation-invariant content is the cascade exponent → 5, NOT the m⁻³ magnitude.
- Frozen-by-fiat atlas gap: `N_eigs(analytic, L=10) = 80080` vs `cache atlas = 78080`, gap `= 2000 = dim_SU3(4,4)·16` EXACT — the L_max=10 atlas is the cache ceiling minus a dropped (4,4) sector, not a saturation point.
- `N_eigs(L) = (4/15)L⁵ + (10/3)L⁴ + 16L³ + (110/3)L² + (596/15)L + 16` (Sage-exact closed form; degree-5; `lim_{L→∞} = +∞`). Its pair-count `C(N_eigs,2)` is degree-10 in L (`lim = +∞`) — the cardinality channel diverges under BOTH the linear obs_2 form AND the registered form.

#### (b) Structural changes

- **Anchor disposition: RE-ANCHOR → HOLD-the-anchor** (epistemic-type change — NOT a value tweak). The verdict moved from "theorem-structure PERMANENT + re-anchor Level-3 to a truncation-invariant functional" (R1-mack) to "theorem-structure PERMANENT + Level-3 m⁻³ anchor HELD NOT-SATISFIED-PENDING" (joint R2/R3). The label difference is substantive: RE-ANCHOR asserts a fixed dimensionful target EXISTS; the Sage proof says it does NOT.
- **Single-binary "PASS Level-3 row" → tuple `(value, canonical-L_max=PROVISIONAL, no-truncation-invariant-dimensionful-target, re-determination=CF-S94)`** (type promotion of the anchor's bookkeeping). The dimensionful m⁻³ anchor is no longer a registry-PASS satisfier; it is a provisional prediction-tracking tuple.
- **NEW structural cell: Tier-2-dimensionful** (taxonomic creation). The two-tier test [Tier-1 convergence; Tier-2 dimensional-re-anchorability] is a refinement of the V4 shrink-vs-diverge test into a DEEPER dimension-keyed diagnostic. `n_PBH` is the inaugural Tier-2-dimensionful occupant — a NEW cell in the standing STAGE-3 taxonomy.
- **§VII.AV precedent re-read: dimension-keyed, not convergence-keyed** (reframing). The precedent supports neither promotion-on-divergence (R1-mack) nor blanket-downgrade (R1-volovik): §VII.AV.STATE-PROJ could re-anchor because its `L_emp` anchor was already dimensionless-content; §VII.AX.OP-PROJ cannot because its anchor is the dimensionful magnitude itself.

### What Holds

- **The §VII.AX.OP-PROJ joint cross-axis theorem-STRUCTURE remains STAGE-3-PERMANENT.** Stage-2 PASS-AND (W4-1 Axis-A `2ab8bb1e…` ∧ W4-2 MULTI-PIN-ATLAS `ba202d16…` at the substrate-input-orthogonality structural ceiling NO caveat ∧ S92 W-4 JE5 Axis-B) is genuine independent cross-axis evidence on the structure axis — two reviewers, opposite axes, no shared workshop context (the constructive pathway of `joint-theorem-promotion.md`, NOT the forbidden agreement-among-agents). It does not evaporate from an anchor-coordinate defect.
- **The §VII.AX.MULTI-PIN-ATLAS M_KK² residue triple-pin Element-5 holds unchanged.** `R_zeta = R_Mellin = 141.44, R_PV = 114.46 M_KK²`, cross-regulator spread `26.98 M_KK²` ≫ 1e-3 option-(iv) threshold (4.43 OOM) — the option-(v) pluralism signature. This is a DIFFERENT, algebra-INVARIANT, M_KK²-unit observable; what Stage-2's JOINT Element 5 certified, and it survives. (Untouched in the registry edit, verified at line 19665.)
- **The OP-PROJ Level-1 g-independence identity + Level-2 envelope hold.** The g-independence theorem quantifies over the SEPARATE cascade-generation index g at FIXED atlas; it is sound. Only the Level-3 row reading a number at a free L_max snapshot was the defect. (Untouched, verified at lines 19432/19433.)
- **The structural law (the durable emergence): for any multiplicative-normalization-divergent substrate-IS channel, every truncation-invariant functional is DIMENSIONLESS.** Sage-confirmed by both agents from scratch. The dimension and the divergence occupy the same multiplicative slot `W(L)`; the log-derivative that buys invariance annihilates the dimensionful prefactor.
- **The (value, scheme)-tuple discipline (S85 W1a F1) holds — for prediction-tracking.** It is the right bookkeeping for the HELD anchor's provisional second coordinate; it is the WRONG bookkeeping for a registry-PASS satisfier (a tuple TRACKS a prediction; it cannot SATISFY a scalar inequality at a non-existent point).
- **T1.13 central value `7.2761e-23 m⁻³` at L_max=14 holds as the substrate-current best central** (NOT contested by either agent) — carried as the first coordinate of the provisional tuple, NOT as a registry-PASS.

### What Breaks or Strains

#### (a) Numerical revisions

- The dimensionful m⁻³ Level-3 registry-PASS inequality `Level-3 < Level-2 at canonical L_max` is **UNDEFINED** for n_PBH (no admissible `L*`; the criterion's third operand cannot be filled by "PROVISIONAL"). Its truth-value flips at finite L_max=19 — it is coordinate-DEPENDENT, unlike a permanent functional relation (e.g., `g1/g2 = e^{-2τ}`, which holds at every τ).

#### (b) Structural changes

- **The §VII.AX.OP-PROJ dimensionful m⁻³ Level-3 ROW is DEMOTED to NOT-SATISFIED-PENDING-substrate-physical-scale-anchor** (effected in-session: registry lines 19434, 19436, 19448). Surgical: cuts exactly the dimensionful scalar-inequality satisfaction; structure tag, residue Element-5, Level-1/Level-2 untouched.
- **The §VII.AX.STATE-PROJ inherited m⁻³ Level-3 ROW is DEMOTED consistently** (effected: lines 19569, 19571) — it inherits the SAME magnitude from OP-PROJ T1.13 and therefore the SAME HOLD; it must not assert a registry-PASS the parent no longer claims. (STATE-PROJ's OWN theorem-structure tag is STAGE-1-CANDIDATE on its own pathway — independent, stays.)
- **The registry-PASS criterion itself is STRAINED**: it silently presupposed Tier-1 convergence without stating it as a precondition. n_PBH exposed the silence; the Tier-1/Tier-2 refinement (FLAGGED orchestrator-direct below) is the repair.
- **NOT broken (explicitly): the theorem-structure tags, the STAGE-3-PERMANENT-set memberships, the M_KK² residue Element-5, the Level-1/Level-2 cohomology-class rows.** The strain is contained to the dimensionful m⁻³ scalar-inequality satisfaction; it does not propagate to the structure.

### Carry-Forward Computations (MATH ONLY — propagate to S94)

**CF-S94-N-PBH-CANONICAL-TRUNCATION-RE-DETERMINATION** (4-field):
- **What**: Re-determine the canonical truncation / substrate-physical scale-anchor for `n_PBH` from OUTSIDE the divergent cardinality channel — either (a) a regularized/renormalized `N_eigs` (zeta-regulated or Pauli-Villars-subtracted at Λ_UV = M_KK, the framework's standard regularization pipeline) that saturates by construction, OR (b) a cosmological-observable-anchored cutoff (L_max fixed by matching a measured PBH-abundance / BBN constraint at the `M_PBH_typical = M_LRD · 2^{-g_BBN}` mass window, making the truncation an EMPIRICAL input). Goal: a genuinely L_max-invariant DIMENSIONFUL n_PBH, OR a Sage-proof that none exists (in which case the m⁻³ Level-3 row is permanently NOT-SATISFIED and n_PBH stands as theorem-structure-only STAGE-3-PERMANENT with no registry-PASS Level-3).
- **Inputs**: do NOT re-anchor to a bare cardinality functional (proven non-existent on FOUR routes: R2-volovik DISSENT iii log-derivative + R2-mack Candidates A/B/C). Route through the substrate UV-regularization pipeline (S61/S78 Pauli-Villars at Λ_UV = M_KK) OR a Pillar-IX cosmological cutoff. Upstream: W4-3 INFO `03b4fb35e7813ae7a81187e32bcf9a58470ecbcacb713fba4847737c345a3178` (the divergence proof); T1.13 `1dc0a3feb214d8b52ce7d70854b2510bbfa3df0e531e75dda1f8bf0cbbcb50ce` (current provisional central); the Tier-2 generalization (this workshop EMERGENCE); canonical_constants `n_PBH_FW_central=7.2761e-23` (now carrying the provisional-truncation tag). CROSS-LINK: this IS the same structural problem as the framework's CC-regularization program (divergent bulk sum → finite physical density).
- **Gate**: PASS iff the re-determined dimensionful n_PBH is L_max-stationary (residual-to-fixed-value shrinks with L_max, OR regulator-class-invariant under the FULL regularization at Λ_UV = M_KK) within a pre-registered tolerance; INFO iff a dimensionless invariant exists but the dimensionful magnitude remains L_max-tied (confirms the m⁻³ row stays NOT-SATISFIED); FAIL iff the regularization itself diverges.
- **Effort**: ~1 wave-equivalent (re-uses the standard PV/zeta pipeline; the new work is applying it to the `N_eigs` cardinality sum rather than to a bottom-K functional).

**CF-S94-STAGE-3-AUDIT-SYMMETRY-TIER-2-SWEEP** (4-field):
- **What**: Apply the two-tier shrink-vs-diverge ∘ dimension test to EVERY standing STAGE-3 Level-3 anchor in `{§VII.AV.STATE-PROJ (line 62 `adbc7004…`), §VII.AU.OP-PROJ (line 31), §VII.AW.OP-PROJ, §VII.AY (`d40041c3…`), §VII.AH (S90 W2 CF-20, FIRST), §VII.U.2 Corner-II Var_a (S92 W4-7, SECOND), §VII.AX.MULTI-PIN-ATLAS (line 73 `ba202d16…`)}`. The downgrade-or-hold rule keyed on "truncation-divergent Level-3 anchor" MUST be applied uniformly (audit-symmetry obligation, V4 + R2/R3).
- **Inputs**: per-entry, (i) compute `lim_{L→∞}[residual-to-c_continuum]` (shrinks → Tier-1-PASS; diverges → Tier-2); (ii) for Tier-2, check whether the anchor's units are carried by a surviving multiplicative prefactor (dimensionful → INELIGIBLE/HOLD) or by the K-window coordinate / a pure index-fact (dimensionless → RE-ANCHOR-eligible). Tooling: Sage per-entry. Upstream: this workshop's Tier-2 law + the W4-3 / §VII.AV.STATE-PROJ shrink-vs-diverge calibration pair.
- **Gate**: per-entry PASS (Tier-1-convergent OR Tier-2-dimensionless) / HOLD (Tier-2-dimensionful). Prediction (NOT the gate): n_PBH is the ONLY Tier-2-dimensionful occupant; if any standing entry FAILs to Tier-2-dimensionful, it joins the HOLD list.
- **Effort**: ~0.5–1 wave-equivalent (7 entries × the same Sage two-tier check; mostly mechanical given the per-entry anchors are already pinned).

*(Note: `CF-S94-STAGE-3-ORDINAL-COLLISION-AU-AW` already exists in the verdict file — lines 77/99/101 — for the AU/AW '#3' bookkeeping collision. It is ADJACENT but SEPARATE from this workshop's output; do NOT fold it into either CF above.)*

### Effected In-Session (NON-MATH — completed by mack BEFORE TERMINATING)

**EXECUTED (mack is registry sole-writer per `feedback_mack-bridge-role.md`; verified on disk via grep):**

- [x] **§VII.AX.OP-PROJ Level-3 status cell demoted to NOT-SATISFIED-PENDING** — `permanent-results-registry.md` line 19434. Tagged with the provisional tuple `(7.2761e-23 m⁻³, canonical-L_max=PROVISIONAL, no-truncation-invariant-dimensionful-target, re-determination=CF-S94)`, the W4-3 divergence rationale (`lim N_eigs = +∞`, Tier-2-dimensionful), the L=19 breach, and a pointer to this workshop file. Status-cell verdict changed from "EMPIRICAL CONFIRMATION" to "NOT-SATISFIED-PENDING".
- [x] **§VII.AX.OP-PROJ Registry-PASS criterion paragraph demoted** — line 19436. Now states the dimensionful m⁻³ scalar-inequality is NOT-SATISFIED-PENDING (criterion UNDEFINED — no admissible `L*`; Level-2-non-binding on the L_max axis); theorem-STRUCTURE remains STAGE-3-PERMANENT via Stage-2 PASS-AND on the NON-Level-3 clauses (`JE5-orthogonal`).
- [x] **§VII.AX.OP-PROJ Element-5 anatomy item demoted** — line 19448. Carried as the provisional tuple; states Stage-2 certified the anchor-STRUCTURE + M_KK² residue, NOT the m⁻³ scalar; prior band-MEMBERSHIP statements retained as descriptive context, SATISFACTION held.
- [x] **§VII.AX.STATE-PROJ inherited Level-3 status cell demoted (consistent inheritance)** — line 19569. Inherits the SAME HOLD via the T1.13 magnitude; STATE-PROJ's OWN STAGE-1-CANDIDATE structure tag explicitly noted as independent and preserved.
- [x] **§VII.AX.STATE-PROJ Registry-PASS criterion paragraph demoted (consistent inheritance)** — line 19571. Cannot assert a registry-PASS the parent no longer claims; Level-2-binding state-pair bridge map preserved.
- [x] **LEFT UNTOUCHED (verified by grep)**: the STAGE-3-PERMANENT theorem-structure tags + STAGE-3-PERMANENT-set memberships (101 STAGE-3-PERMANENT occurrences intact); the §VII.AX.MULTI-PIN-ATLAS M_KK² residue Element-5 (line 19665); the OP-PROJ Level-1 g-independence identity (19432) + Level-2 envelope (19433). Line numbers verified against live registry before editing (matched on row TEXT, not blind line numbers — volovik's R3 QUESTIONS (i) estimates had drifted by ~0–120 lines).

**FLAGGED — ORCHESTRATOR-DIRECT (do NOT edit; subagents edit-denied on `.claude/rules/` + the corpus ORCHESTRATOR-RESERVED blocks). The orchestrator lands these post-workshop:**

- [ ] **FLAGGED with proposed text for orchestrator — `cross-pillar-bridge-anatomy.md §"Registry-PASS criterion"` Tier-1/Tier-2 dimensional-re-anchorability refinement (SUGGESTION at K=1).** Proposed rule-text (verbatim, for the orchestrator to mirror into the rule file):

  > **Registry-PASS-criterion Tier-1/Tier-2 dimensional-re-anchorability gate (S93 W-1 volovik × mack; SUGGESTION at K=1; promotes to MANDATORY at K=3 per `feedback_rules-compensate-missing-structure.md`).** A Level-3 anchor is registry-PASS-ELIGIBLE only if EITHER (Tier-1) its residual-to-`c_continuum` shrinks with L_max [convergent → a substrate-singled-out evaluation point `L*` exists], OR (Tier-2) its divergent channel's truncation-invariant content is DIMENSIONLESS and the anchor is RE-ANCHORED to that invariant functional [the §VII.AV.STATE-PROJ route — a log-derivative / ratio / cohomology-class anchor]. A divergent channel whose anchor is a DIMENSIONFUL magnitude (Tier-2-dimensionful) is registry-PASS-INELIGIBLE; its Level-3 row is HELD as NOT-SATISFIED-PENDING-substrate-physical-scale-anchor, while the joint theorem-STRUCTURE may independently hold STAGE-3-PERMANENT via Stage-2 PASS-AND on the non-Level-3 clauses. Structural basis: for any multiplicative-normalization-divergent substrate-IS channel `O(L,K)=W(L)·g(K)`, every truncation-invariant functional is a log-derivative `d^n ln O/d(ln K)^n` (the only operator annihilating `W(L)`), which is dimensionless in the observable's own value — so the dimension `[O]` (carried by the prefactor inside `W(L)`) and the L_max-divergence (also in `W(L)`) occupy the same multiplicative slot and cannot be separated. This dovetails with the existing Level-2-binding-vs-non-binding sub-class (`c_continuum` undefined → Level-2-non-binding → FORBIDDEN for registry-PASS); Tier-2 makes the dimensionful corollary explicit. Inaugural occupant: §VII.AX.OP-PROJ n_PBH (m⁻³ Level-3 row HELD; theorem-structure STAGE-3-PERMANENT). Audit: extend `computations/_shared/_cross_pillar_bridge_audit.py` with a Tier-1/Tier-2 detector (Sage `lim_{L→∞}[residual]` shrink-vs-diverge + units-carried-by-prefactor-vs-K-coordinate check); S2 advisory at K=1.

- [ ] **FLAGGED with proposed text for orchestrator — `cross-pillar-bridge-corpus.md` K-counter calibration row (K=1 inaugural).** mack-cosmic-bridge CAN write the corpus; but per the spawn instruction this is FLAGGED as orchestrator-direct alongside the rule-file flag so the two land together (the corpus row references the rule-text above by its K-counter, and the orchestrator owns the SUGGESTION→MANDATORY promotion bookkeeping). Proposed corpus-row text (verbatim):

  > **Tier-1/Tier-2 dimensional-re-anchorability — K=1 calibration instance (S93 W-1, 2026-05-25).** Inaugural occupant: §VII.AX.OP-PROJ `n_PBH = 7.2761e-23 m⁻³`. Tier-1: FAILS (cardinality channel truncation-DIVERGENT, `N_eigs(L)=(4/15)L⁵+…`, `lim_{L→∞}=+∞`, Sage-exact; W4-3 `03b4fb35e7813ae7a81187e32bcf9a58470ecbcacb713fba4847737c345a3178`). Tier-2: DIMENSIONFUL (the truncation-invariant content is the dimensionless cascade exponent `d ln N_eigs/d ln L → 5`; the log-derivative annihilates the m⁻³ prefactor `A = 2.2517e-28 m⁻³` — dimension and divergence in the same multiplicative slot). Verdict: registry-PASS-INELIGIBLE; m⁻³ Level-3 row HELD NOT-SATISFIED-PENDING-substrate-physical-scale-anchor; theorem-STRUCTURE STAGE-3-PERMANENT (Stage-2 PASS-AND on non-Level-3 clauses; W4-2 `ba202d1626c99c5d36a734735266a0b0541c9d87e6913f1a6f2093f7ad38451f`). Contrast (the dimension-keyed distinction): §VII.AV.STATE-PROJ `L_emp = -7.046336 M_KK²` is Tier-1-CONVERGENT (residual 2.82e-04 shrinking, Connes-Karoubi-bound) AND Tier-2-dimensionless-content (a 2nd log-derivative; M_KK² is the K-window unit) ⇒ RE-ANCHOR available, STAGE-3-PERMANENT earned. K-counter advancement to K=2 requires a STRUCTURALLY-DISTINCT Tier-2-dimensionful instance (a different divergent channel with a dimensionful anchor), surfaced by the CF-S94 audit-symmetry sweep. Sole-writer: mack-cosmic-bridge.

  *(Disposition note for the orchestrator: if you prefer the corpus row be landed by mack rather than orchestrator-direct, it is within mack's sole-writer authority for `cross-pillar-bridge-corpus.md`; it is FLAGGED here only to keep it paired with the edit-denied rule-file flag. The rule-file edit itself is unambiguously orchestrator-direct.)*

### Closing Line

Theorem-structure STAGE-3-PERMANENT; dimensionful m⁻³ Level-3 anchor HELD NOT-SATISFIED-PENDING (effected on disk, 5 surgical rows) — because the substrate's cardinality cascade is genuinely infinite, the log-derivative that buys truncation-invariance annihilates the very magnitude we wanted to pin, and n_PBH becomes the framework's first Tier-2-dimensionful anchor: a theorem whose structure is permanent and whose number waits for a cutoff sourced from outside the count.
