# Seed — Chunk w1 (Emergent-FRW a(t) route reconciliation — C1 keystone)

**Date**: 2026-05-31   **Investigator**: phonon-first-cosmologist   **Source**: session-98-w1-workingpaper.md + session-98-plan-w1.md
**Wave summary**: 1 gate, composite **FAIL** (`S98-W1-ROUTE-RECONCILIATION`; per-clause Clause-1 **PASS** / Clause-2 **FAIL** / sub-gate **PASS**; 3-tuple sign=PASS / magnitude=FAIL / regime=BREAKDOWN). Structural reading: the gate **DERIVED** (did not stipulate) the AOFT covariant spectral-action route as the unique a₂-canonical acoustic frame — VOL and GFT proven to be incomplete reconstructions of the SAME a₂-rate (route-vs-AOFT residual 1.13e-18 M_KK² ≪ 1e-2), so the dual-prior locks at **Track A** (route-invariance recovered at the a₂-rate level, 0.90). The FAIL is a NEW substrate-physics finding, not a script failure: under that canonical frame the conformal factor `Ω=√(ρ_s/a₂)` (the √Γ-effacement factor) almost exactly cancels the bare spectral-complexity growth `a_bare`, so the acoustic scale factor `a_eff=a_bare·Ω` is CONSTANT to rel-var 7.4e-7 (conformally **stationary**). On a stationary frame the ratio-form deceleration `q = −a_eff·ä_eff/ȧ_eff²` is a genuine 0/0 (both ä_eff and ȧ_eff² vanish as FD noise; only 116/999 finite points; band_frac=0). This re-explains the S97 `max_abs_dq=2.99e12` as q being undefined on a stationary frame, not as route-disagreement. C1 stays ASSUMED.

## No candidates

This single-gate keystone-FAIL wave produces **zero Slot-1 and zero Slot-2 candidates**. The honest reason — the seam my spawn prompt flagged (ratio-q-is-0/0 → non-ratio-observable vs AOFT-is-the-wrong-frame → different H(τ) backbone) does NOT survive as a genuine adversarial reading-divergence, and its remediation is already two listed genuine-math carry-forwards:

1. **Reading Y ("AOFT is the wrong canonical acoustic frame; pick a different substrate H(τ) backbone") is structurally FORECLOSED by Clause-1 PASS, not contested.** The gate did not assume AOFT — it DERIVED AOFT as the a₂/spectral-triple-unique acoustic frame and PROVED the only competing routes (Volovik-2fluid, GFT) carry no independent a₂-content (residual 1.13e-18 M_KK²; `dual_prior_track=Track_A_route_invariance_recovered_0.90` in the canonical verdict line). There is no surviving *alternative acoustic frame* to debate — the a₂ moment singles out AOFT uniquely. Track B (intrinsic route-sensitivity / "wrong frame") was the loser of the pre-registered discriminator, not a live competing claim. A workshop requires two agents who genuinely disagree about the reading (Investigating-Workshops.md §"A WORKSHOP IS" condition 1); here the ledger AFFIRMS one frame.

2. **What CF-S99-W2-RELAXATION-NONSTATIONARY-H actually asks for is the SAME substrate object as Reading X, not a competing one.** It does not request a rival *acoustic* frame; it requests a non-conformally-stationary substrate H(τ) *backbone for the friction ODE* — i.e. the un-effaced bare rate `H_bare` (mean +0.17841) BEFORE the √Γ conformal factor `dlnΩ` (mean −0.17841) cancels it. That is the identical substrate quantity as Reading X's "bare-rate q BEFORE conformal transport" (CF-S99-W1-Q-OBSERVABLE-REDERIVE). The two CFs converge on one object (the pre-conformal bare-rate growth) approached from two waves — a dependency/duplicate, not a contradiction.

3. **Both surviving forward routes are already a SINGLE listed CF with two sub-methods.** `CF-S99-W1-Q-OBSERVABLE-REDERIVE` (W1 WP, lines 106-113) internally enumerates BOTH "ä_eff sign-history directly" AND "the bare-rate q before conformal transport" as the two sub-methods of one gate (`S99-W1-Q-NONRATIO-OBSERVABLE`). The within-route choice is a single carry-forward computation, not two readings to adjudicate. Re-listing it as a workshop is exactly Investigating-Workshops.md §"A WORKSHOP is NOT" item 3 (re-listing WP-enumerated carry-forwards) — explicitly FORBIDDEN by my spawn prompt.

4. **The one borderline — "does C1 advance to CONFIRMED on the a₂-rate axis, or stay ASSUMED because the deceleration observable that would test it is undefined?" — is already adjudicated and is a Q2 registry-state item, not Q1.** The S98 capstone-hygiene 5-question gate (`session-98-housekeeping.md` Q1/Q3) already pinned the decision: **C1 stays ASSUMED, no register status change, no claim narrated above register.** Choosing a register status marker (ASSUMED vs CONFIRMED) for an already-landed atlas-04 C1 sub-object is a status-tag classification (Q2 marker: "status-tag edit"; Investigating-Workshops.md §"is NOT" item 7), and it is not even open — it is decided. No adversarial physics tension remains.

Applying the 3-question discriminator: the candidate fails **Q1** (no genuine math/physics adjudication — the readings do not genuinely diverge; Clause-1 PASS forecloses one and the other two collapse to the same already-listed CF); it is **Q2** at most (the C1 status-tag, already decided ASSUMED). Per the rule, "No candidates" with this explanation is the HONEST output; padding it with the listed CFs dressed as workshops would violate the workshop definition, fix-in-session-never-defer, and the honest-count discipline.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W1 FAIL → W2 `S98-W2-2-RELAXATION-CLOSURE` PRE-REG-INC is a DEPENDENCY (dedupe), not a contradiction (workshop).** The W2-2 verdict line (`s98_gate_verdicts.txt:33`) is an honest mechanical PRE-REG-INC closure: `value='PRE-REG-INC_blocked_by_S98-W1-ROUTE-RECONCILIATION_FAIL_AOFT-frame-conformally-stationary_q-attractor-0over0_full-run-CF-S99'`, emitted FAIL-not-PASS per `mechanical-closure-discipline.md`. W2 fully *agrees with and inherits* W1's conformal-stationarity finding and honestly defers the friction-ODE attractor to `CF-S99-W2-RELAXATION-NONSTATIONARY-H` (which needs the non-conformally-stationary H(τ) re-derivation as a blocking input). Both waves' ledgers are CONSISTENT — no contradiction, so no adjudication. Recommendation: the consolidator should DEDUPE (treat as the existing hard-ordering dependency), not schedule a workshop. (Cite: W1 WP §W1-1 verdict + `s98_gate_verdicts.txt:33-34` + `session-98-housekeeping.md` "Genuine-math carry-forwards" CF-S99-W2-RELAXATION-NONSTATIONARY-H.)
- **No NEW high-leverage open item absent from the EVOI table.** The W1 outcome is already current in `sessions/evoi-framework.md`: Tier-1 #1 A(t)-FRIEDMANN-RECONCILE is stamped "PARTIAL (SCALING pinned, SHAPE open)" with the resolving gate flagged. The conformal-stationarity finding refines the Tier-1 #1 status (the SHAPE sub-gate PASSED — τ̇ shape #49 uniquely selected — while the q-observable corridor closed pending the non-ratio re-derivation); this is a STATUS refinement of an already-listed Tier-1 item, not a new frontier. No `/rclab-plan` Step 1c-REGISTERS EVOI insertion is needed beyond the routine S99 re-stamp the planner already performs. (Cite: evoi-framework.md §1 row 1 + §6 item 1 KEYSTONE.)

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other]** — `CF-S99-W1-Q-OBSERVABLE-REDERIVE` (gate `S99-W1-Q-NONRATIO-OBSERVABLE`) is ALREADY a genuine-math carry-forward in the W1 WP (`session-98-w1-workingpaper.md` lines 106-113) and the housekeeping "Genuine-math carry-forwards" block. **Do NOT re-create.** It is a solo compute follow-up (Investigating-Workshops.md §"is NOT" item 1), not a workshop: re-derive the post-fold deceleration history via a non-ratio observable (ä_eff sign-history at the H_A=0 crossing, OR the bare-rate q before conformal transport), test band-membership vs SF54 [−0.97, 0.81]. `/rclab-plan` picks it up from the WP CF block directly.
- **[Q-other]** — `CF-S99-W2-RELAXATION-NONSTATIONARY-H` (W2 WP) and `CF-S99-W2-BBN-ADDITIONAL-RELIEF` (W2 WP) are ALREADY listed genuine-math CFs in the housekeeping ledger. Out of my chunk's scope (W2 WP not read per constraints); flagged only because CF-S99-W2-RELAXATION-NONSTATIONARY-H is the W2-side consumer of the same pre-conformal bare-rate H(τ) object W1's CF re-derives (see Cross-wave flags — dependency to dedupe).
- **[Q2-hygiene]** — none new surfaced by this chunk. (W1 wave-synthesis routed all Q2 items correctly; the C1 ASSUMED status-tag is already pinned in `session-98-housekeeping.md` Q1/Q3 — no upstream-miss to log.)
- **[Q3-wave-together]** — none. W1 is a single-gate composite, not an N-condition × N-axis parallel-compute structure.

## Wave-by-wave digest (consolidator background)

**Wave 1 — `S98-W1-ROUTE-RECONCILIATION` (gen-physicist, GEOMETRIC, [SIGN]) — composite FAIL.**

Three coupled clauses on cached S96/S97 npz arrays (no new D_K diagonalization; a₂ enters as the canonical scalar `a_2_FW_zeta=2776.165389`, ζ-regulated):

| Clause | Observable | Threshold | Computed | Verdict |
|:-------|:-----------|:----------|:---------|:--------|
| 1 — a₂-residual | `max_{VOL,GFT} ‖R_route − R_AOFT‖_{M_KK²}` | ≤ 1e-2 | 1.13e-18 (VOL); 0.0 (GFT, bit-identical) | **PASS** |
| 2 — band-membership | `frac{q_polefree ∈ [−0.97,0.81] over finite |H_A|≥pole_eps}` | ≥ 0.90 | 0.0 (no clean finite window; frame stationary) | **FAIL** |
| sub-gate — τ̇ rel-spread | `(max−min)/mean H_sel(τ)` over selected sub-family | ≤ 1e-2 | 0.0 (0.419 → 0.0; shape #49 unique) | **PASS** |

- **Clause 1 (PASS, the canonical-frame derivation)**: AOFT `H2_src` IS GFT `H2_substrate` bit-identical (max|Δ|=0.0); VOL total 2-fluid rate matches the shared a₂-rate to 0.04% (only VOL-specific term is the normal-fluid back-reaction 1.86e-5, itself a₂-content already present). VOL/GFT are incomplete reconstructions of the SAME a₂-rate; neither carries independent a₂-content ⇒ AOFT covariant spectral-action route (S96-W1-AOFT-FRIEDMANN-MAP, K→g_M Bianchi-lift residual 0) is the canonical acoustic frame DERIVED from a₂/spectral-triple uniqueness ⇒ **dual-prior Track A 0.90**. (Honestly-disclosed in-session correction: an earlier `H2_aeff`-proxy mis-projection returned 8.25 M_KK²; the fix is the substrate-correct `H2_src` basis, same 1e-2 pin — not convention-shopping.)
- **Clause 2 (FAIL, the conformal-stationarity finding)**: substitution chain Sage-exact (`q_pole − q_polefree = 0`); the recast `q=−a_eff·ä_eff/ȧ_eff²` is algebraically identical to standard q. Data reveals `mean(H_bare)=+0.17841`, `mean(dlnΩ)=−0.17841` (equal-and-opposite to 6-7 sig figs) ⇒ `a_eff=a_bare·Ω` constant to rel-var 7.4e-7 ⇒ `ȧ_eff≈0` across the WHOLE window ⇒ q is a genuine 0/0 (116/999 finite, band_frac=0). 3-tuple: sign=PASS (recast exact + sign(ä_eff) determinate at 18 crossings, 78% accelerating), magnitude=FAIL (band_frac=0), regime=BREAKDOWN (non-stationary-q regime breached ~100% of window). Composite-collapse `regime=BREAKDOWN ⇒ FAIL` (byte-unmodified pre-registered rule).
- **Sub-gate (PASS)**: joint criterion (AOFT a₂-rate consistency + κ_nat seconds-anchoring, `kappa_nat=8.86e-42`, `N_e=80.89`) uniquely selects shape #49 (param=1, widest-D family); `n_selected=1` ⇒ τ̇ band rel-spread 0.419 → 0.0.

**Standout**: the FAIL is a substrate-physics DISCOVERY (the √Γ-effacement conformal factor freezes the acoustic scale factor even as the bare fabric complexifies 11%), not a numerical breakage — the a(t) trajectory itself still EXISTS (S97-W1-1-AT-TRAJECTORY INFO) and the route-selected AOFT H(τ) is supplied to V.2 (HARD ordering preserved). Artifacts all present on disk (script 49,505 B; npz 70,126 B; png 339,879 B; verdict line audit_sha256=`75a45dd730aca2f94be4040ed6a69120dceb1efa893a5bd62659ea981c79e1b5`, sig_5 clean). The forward route is the single listed CF-S99-W1-Q-OBSERVABLE-REDERIVE (non-ratio observable); atlas-04 C1 stays ASSUMED until it lands.
