# Session 99 — Fermion Mass & Mixing as a Transit Freeze-In

**Panel**: s99-fermion-mass-panel · **Lens**: non-equilibrium transit / Bogoliubov / Kibble–Zurek freeze-in
**Author**: transit (transit-dynamics-theorist) · **Coordinator/synthesis**: team-lead
**Companion lenses**: connes (finite-triple textures), baptista (fiber-overlap localization), hawking (threshold magnitude/grading)

> **Reading discipline.** Every claim is tagged **[DERIVATION]** (follows from settled framework structure + a short calculation shown here), **[SPECULATION]** (a proposed mechanism, physically motivated but not yet gated), or **[CONJECTURE]** (a sharper, falsifiable bet inside a speculation). Substrate-first throughout: the fabric IS the mass spectrum; explanation flows `D_K(τ) → transit dynamics → frozen amplitudes → measured masses`, never the reverse.

---

## 1. The gap, restated through the freeze-in lens

The framework has PROVEN that **tree-level Yukawa vanishes** (S62, Peter–Weyl orthogonality) and that the **static** finite operator is **multiplicity-scalar** (S99 W3-1): the three generations are the SU(3) Z₃-triality multiplicity `t = (p−q) mod 3`, and the lightest eigenvalues of the three triality sectors are nearly degenerate,

```
gen_lambda0 = [0.81974111, 0.83589351, 0.83589351]   (t=0, t=1, t=2)   [S99 W3-1, s98_w3_1.npz]
```

with `t=1` and `t=2` **identical to machine ε** (`R_cross = 1.0197`, a multiplicity-scalar). That degeneracy is *exactly why* the S97 direct attempt returned the democratic `1:1:1` against the observed `1 : 0.0595 : 0.000288`. The static route did not fail by accident — it failed because **homogeneity (left-invariance) forces the multiplicity-scalar representation**, and a multiplicity-scalar operator cannot carry a generation index in its spectrum.

The lens this panel owns inverts the question. The framework already has a place where vacuum degeneracy is **dynamically lifted into an exponentially-spread, non-thermal occupation pattern**: the supersonic transit through the van Hove fold (`τ_fold = 0.190`, `Mach = 13.75`, `δt_transit = 1.13×10⁻³ M_KK⁻¹`, `P_exc = 1.000`), the same physics that froze in the GGE relic (`N_pair = 59.8`). During that crossing the per-sector modes evolve as parametric oscillators, and — this is the load-bearing structural fact, capstone §2.3 / §5.3 —

$$
u_k'' + \omega_k^2(\tau(t))\,u_k = 0,\qquad \omega_k = E_k=\sqrt{(\lambda_k^2-\mu^2)^2+\Delta_k^2},\qquad D_K=\bigoplus_{(p,q)}D_{(p,q)},
$$

the block-diagonality makes the **per-mode parametric-oscillator equation an exact identity, not a decoupling approximation**. The modes do not mix under `D_K`; each `(p,q)` sector transits independently with its own time-dependent frequency. So the freeze-in is genuinely computable mode-by-mode — and crucially, it acts on the **trajectory** `ω_gen(τ)` across the fold, not on the static endpoint `ω_min(τ_fold)`.

**The reframing [DERIVATION-of-the-question, SPECULATION-of-the-answer]:** *the fermion mass spectrum is not a static property of `D_F` at all. It is the frozen-in occupation/energy pattern that the three triality sectors acquire as they transit the fold at different adiabaticities — a Kibble–Zurek-style freeze-in pattern. The hierarchy is set by how diabatically each generation crosses; the mixing is the misalignment between the up- and down-sector frozen eigenbases.* The static multiplicity-scalar wall is real, but it is a statement about the **fixed point**; the three sectors are degenerate *at* `τ_fold` while transiting it **differently**.

This is consistent with — not in conflict with — connes' wall. The S99 W3-1 "Non-LI-Deformation Necessity" theorem (now STAGE-3-PERMANENT) says any generation-lifting datum must live **outside** the substrate's intrinsic (left-invariant, multiplicity-scalar) differential calculus — in a non-LI deformation. **A diabatic transit IS a non-left-invariant, time-dependent deformation of the operator.** The freeze-in lens is one concrete, substrate-internal realization of the "external non-LI deformation ε_LX" that W3-1 proved is *necessary*. Where connes asks "what static texture is admissible in the complement," I ask "what *dynamics* puts a texture there," and the transit is the obvious candidate generator because it is the one non-LI, time-dependent process the framework already has.

---

## 2. The structural obstruction the freeze-in must clear (and how it reshapes the mechanism)

Before proposing a mechanism I tested whether the freeze-in route survives the same wall that killed S97. It survives, but only in a specific form, and finding that form is the main result of this panel.

### 2.1 A freeze-in on the static spectrum collapses too — [DERIVATION]

The static `lambda_min` set has only **two distinct values** (t=1 = t=2 to machine ε). Any observable that is a function of the endpoint frequencies — including a naive sudden-Bogoliubov occupation `|β_k|²` evaluated at `τ_fold` — inherits that 2-fold degeneracy and **cannot produce three distinct masses**. (Verified: a Parker/Landau–Zener `n_k = exp(−2πγ_k)` built on `gen_lambda0` gives a 2-valued output.) So the freeze-in must act on the **transit trajectory** `ω_gen(τ)` integrated across the crossing, where the three sectors differ even though their endpoints coincide. The S63 triality computation already shows the sectors are **not** dynamically degenerate: distinct mode populations (`t=0: 464 modes`, `t=1: 264 modes`) and distinct triality-resolved density of states. Distinct DOS ⇒ distinct effective sweep rate ⇒ distinct freeze-in.

### 2.2 The DIAGONAL amplitude degenerates t=1 ↔ t=2; the split lives OFF-DIAGONAL — [DERIVATION, corrected & verified via connes Stage-2]

This is the sharp structural finding, in the form it took after connes' reality-axiom analysis corrected my first pass (the correction makes it stronger — see §4). Model the per-sector freeze-in action as Casimir-graded (the natural adiabaticity scaling, since the gap to the next level scales as `√C₂`): `S_gen = S₀·(C₂,gen − C₂,min)`. The fundamental `(1,0)` and antifundamental `(0,1)` — the lightest reps in the `t=1` and `t=2` triality classes — have **identical quadratic Casimir** `C₂(1,0) = C₂(0,1) = 4/3` (Sage-exact). So *any* **diagonal** amplitude observable (Parker occupation, Landau–Zener `|β|²`, Casimir-graded instanton action) keeps the 2nd and 3rd generations degenerate — **the same wall S97 hit, re-encountered**.

**The split must therefore live in the OFF-DIAGONAL inter-sector channel, not the diagonal amplitude.** connes confirmed the diagonal degeneracy independently from the reality axiom: modelling the frozen object as a Hermitian `ε_LX` on the `t1↔t2` doublet and imposing KO-6 reality `[J, ε_LX]=0` with `J = swap∘conj` (`J²=+1`, the framework's BDI class) forces the **diagonal** entries equal, `d1 = d2` — the W2 multiplicity degeneracy reasserts, the *same* wall I reached via the Casimir, by a second independent route. But reality leaves the **off-diagonal** `t1↔t2` entry `w = wr + i·wi` *completely unconstrained*. The reality-admissible block is `[[d, w],[w*, d]]`, and (Sage-exact, this turn) its eigenvalues are
$$
\lambda_\pm = d \pm |w| = d \pm \sqrt{wr^2 + wi^2},
$$
depending on `|w|` **only, not on `arg(w)`**. The corrected division:

> **[DERIVATION, verified] The 2nd↔3rd MASS split is the off-diagonal MAGNITUDE `|w|`** (a *real* off-diagonal `wr` already splits the masses — the phase is NOT needed for the mass split). **The triality-odd PHASE `arg(w)` survives reality and lives ENTIRELY in the diagonalizing unitary — i.e. it is purely the PMNS/CKM mixing angle + CP phase, not a mass datum.** The genuine "second lever" for the mass hierarchy is the off-diagonal inter-sector mixing magnitude, NOT a second diagonal Casimir amplitude (which would double-count, §4 baptista).

This is the convergence that makes the lens worth pursuing, now in its correct form: **one complex off-diagonal inter-sector Bogoliubov coefficient `w` does two jobs — `|w|` splits the 2↔3 masses, `arg(w)` mixes the states (PMNS/CKM + CP).** And it is *why* the freeze-in is the right generator: a diabatic transit naturally produces **off-diagonal inter-sector mixing** (the t1/t2 sectors are degenerate at the fold, so the crossing mixes them — off-diagonal `w` is exactly what a sudden quench of degenerate modes generates), which is precisely the only reality-admissible lever. This realizes the dormant S34 conjecture ("CKM/PMNS from inter-sector Bogoliubov coefficients") concretely. **Load-bearing scope caveat:** the phase's survival is KO-class-specific — connes checked that in DIII (`J²=−1`) reality forces `w` *real* and the CP phase dies. The framework is **BDI** (`J²=+1`, AZ class BDI, a foundational PROVEN result), so the mechanism is safe; but this is a BDI-conditional result, not a generic one.

---

## 3. Candidate mechanisms

### Mechanism A — Generation hierarchy as Casimir-graded diabatic freeze-in (the envelope) — [SPECULATION]

**Substrate-first idea.** As the fabric transits the fold, each `(p,q)` sector is a parametric oscillator with `ω_{(p,q)}(τ)`. The diagonal Dirac mass is the depth at which each sector's mode freezes. **Correction (hawking cross-talk, Sage-verified):** the graded object is *not* the Landau–Zener adiabaticity `γ = gap²/(4|Δ̇|)` — in the deep-diabatic limit `γ ~ 2×10⁻⁴` for *every* generation (nearly identical; `γ ≪ 1` is exactly what `P_exc → 1` means), so `2πγ ≈ 0` and LZ-γ gives **no** hierarchy. The correct graded object is the **GGE Lagrange multiplier / frozen squeeze depth** (capstone §5.3 KZ-note):
$$
\lambda^{\rm GGE}_{\rm gen} = -\ln\big|\psi_{\rm pair}[{\rm gen}]\big|^2 \;=\; \frac{2\pi\,\omega_{\rm gen}}{\kappa}\quad(\text{= hawking's horizon-greybody exponent}),\qquad
m_{\rm gen} \sim M_0\,e^{-S_{\rm gen}},\ \ S_{\rm gen} = S_0\,(C_2^{\rm gen}-C_2^{\rm min}).
$$
i.e. the freeze-in depth IS the greybody exponent `2πω/κ`, not an LZ action — production amplitude and horizon filter are the same exponent (§4 hawking). `S_0` is a **ratio**: numerator = the threshold-set gap/frequency `ω_gen` (hawking's KK threshold), denominator = the horizon scale `κ` (transit/SONIC, §4 hawking).

**What it predicts directionally.** `m_gen ∝ exp(−S₀·C₂,gen)` — heavier-Casimir sectors freeze deeper (lighter mass). The electron is the **trivial rep `(0,0)`, `C₂ = 0`** (genuinely Casimir-separated), so a magnitude *can* set the large e-vs-(μ,τ) envelope — `ln(m_τ/m_e) = 8.15`, the ~8-e-fold electron suppression. With `S₀` fit from the envelope I get `S₀ ≈ 3.2`; at `κ_GH = 1.365` this implies `ω_step ≈ 0.696 M_KK` (sub-fiber, comfortable — Sage-checked, matches hawking's 0.695). **Suggestive (NOT derived, flagged jointly with hawking):** `S₀/S_inst = 3.2/0.0686 = 46.7 ≈ κ_exit = 2π·7.578 = 47.6` (the `a₄` Kitaev surface gravity, 1.9%) — could mean `S₀` is the per-mode instanton action measured in units of `κ_exit`; worth a real derivation, not claimed. **Status:** the envelope (e-vs-heavy magnitude + `M₀`) is amplitude-driven and jointly pinned (my squeeze depth = hawking's greybody exponent = baptista's renormalized overlap slope); the t=1↔t=2 split is off-diagonal `|w|` (§2.2), NOT this diagonal amplitude.

**HONEST OPEN RESIDUAL (the `1.889` shape — hawking + baptista converge here).** A *linear* `S₀·C₂` envelope gives a fixed ln-gap shape ratio set by the Casimir spacings of the rep tower: on the fundamental `(k,0)` tower `C₂ = {4/3, 10/3, 6}`, consecutive gaps `{2, 8/3}` → ratio `(8/3)/2 = 1.333`; baptista's alternate sector triple gives `9/5 = 1.800`. **Neither linear-`C₂` (mine) nor linear-`ω` (hawking's) reproduces the observed `ln(μ/e)/ln(τ/μ) = 1.889` alone.** The `1.889` shape needs a **nonlinearity** — baptista's **Jensen tilt** (sector-content nonlinearity, the metric's `τ`-deformation making `ω` nonlinear in `C₂`). So the clean *joint win* is the **envelope magnitude** (e-vs-heavy ~8 e-folds + `M₀`, pinned three ways); the precise `1.889` shape is an **open residual living in baptista's Jensen tilt**, not closed by any single lens. Recorded honestly rather than fit.

**Rough computation to run.** For each triality class, extract `ω_{(p,q)}(τ)` from the L_max=12 master cache along a τ-grid bracketing the fold (`s84_spectrum_cache_L12_tau019.npz`, block-diagonal, no re-diagonalization). Integrate `u'' + ω²(τ(t)) u = 0` per sector with a Radau/DOP853 solver (rtol ≤ 1e-10 — piecewise-constant transfer matrices FAIL for smooth ω(τ), my standing debugging note), and — crucially — extract the **frozen squeeze depth `λ^GGE_gen = −ln|ψ_pair|²`** (NOT the LZ-γ) plus the **inter-sector off-diagonal `w`** between the t1/t2 modes. Output the diagonal `S_gen` (test against the Casimir envelope, and against the Jensen-tilt-corrected `ω(C₂)` for the `1.889` shape) and the off-diagonal `|w|, arg(w)` (the mass split + mixing).

### Mechanism B — 2↔3 mass split + mixing from one off-diagonal inter-sector Bogoliubov coefficient `w` (the core) — [CONJECTURE, corrected via connes Stage-2]

**Substrate-first idea.** The complete frozen datum is a **matrix**, not three independent diagonal amplitudes. On the `t1↔t2` doublet it is the reality-admissible block `[[d, w],[w*, d]]`: diagonal `d = M₀ e^{−S₀C₂}` (the Casimir-graded freeze-in amplitude, equal on the conjugate pair by reality/Casimir — §2.2), plus an **off-diagonal inter-sector coefficient `w = wr + i·wi = |w| e^{iΘ}`** that the diabatic crossing generates by mixing the fold-degenerate t1/t2 sectors. The conjugate-pair MASS split is `m(t1), m(t2) = d ∓ |w|` (Sage-exact eigenvalues `d ± |w|`) — set by the **off-diagonal magnitude `|w|`**, with a *real* `wr` already sufficient. The phase `Θ = arg(w)` does **not** enter the masses; it lives entirely in the diagonalizing unitary.

**What it predicts directionally.** Three shape knobs — `{S₀ (Casimir-amplitude slope, the t=0↔t=1 envelope), |w| (off-diagonal magnitude, the t=1↔t=2 mass split), Θ = arg(w) (off-diagonal phase, the mixing/CP)}` — set *all* generation ratios AND the mixing in *every* sector, because the Casimir + triality structure is fixed rep-data identical across leptons/up/down (the sector-specific freedom is only the overall normalization `M₀^{sector}`, i.e. the Higgs-vev/seesaw scale). Counting: **8 observed cross-sector mass ratios** + **mixing data** (3 CKM angles + `J_CP` + the PMNS analogues) against **`{S₀, |w|}` for masses** (2 knobs, possibly sector-graded) plus **`Θ` for mixing** ⇒ still strongly over-constrained, and falsifiable — the opposite of a texture fit. (The exact net-constraint count depends on whether `|w|`, `Θ` are single shared knobs or sector-graded; the over-constraint is robust either way because masses and mixing are predicted from the *same* `w`.)

**The mixing prediction (the payoff) — sharpened by connes' eigenvector result.** Because `arg(w)` lives entirely in the diagonalizing unitary `U_sector` (verified: eigenvectors carry `e^{−i arg(w)}`), CKM is the misalignment of the up- and down-sector frozen eigenbases:
$$
V_{\rm CKM} = U_{\rm up}^\dagger\,U_{\rm down},\qquad U_{\rm sector}=\text{(eigenbasis of the frozen block }[[d,w],[w^*,d]]^{\rm sector}\text{)},
$$
and the **same off-diagonal `w` whose magnitude `|w|` splits the 2↔3 masses carries, in its phase `arg(w)`, the CKM CP phase** `J_{CP} \sim \sin(\arg w)`. One complex inter-sector number, two jobs: `|w|` → the conjugate-pair mass split, `arg(w)` → the Jarlskog. PMNS is the same construction for charged-lepton-vs-neutrino frozen bases. This realizes the dormant S34 conjecture ("CKM/PMNS from inter-sector Bogoliubov coefficients") concretely — and connes' reality analysis shows it is the *only* axiom-admissible lever (the diagonal is degeneracy-locked). **Scope caveat (load-bearing):** the CP-phase survival is BDI-specific (`J²=+1`); in DIII (`J²=−1`) reality forces `w` real and `J_CP → 0`. The framework is BDI (PROVEN), so the prediction stands, but it is KO-class-conditional.

**Rough computation to run.** Extend Mechanism A's per-sector integration to capture the **complex** Bogoliubov coefficient (track the phase of the matched out-mode, not just `|β|`). Build the 3×3 frozen `a^{sector}` from the three triality-class amplitudes with the triality-odd phase assignment, diagonalize, and read `U_up`, `U_down`, then `V_CKM`. Falsify against the Wolfenstein angles + `J_CP`. The pre-registered failure mode is honest and sharp: if `{S₀, Θ}` fit to leptons over- or under-shoots the quark ratios by more than the gate tolerance, the mechanism is FALSE — there is nowhere to hide because the shape knobs are shared.

### Mechanism C — "3 generations" as a frozen mode count — [SPECULATION, weaker]

**Substrate-first idea.** The brief asks whether the same transit that produced `N_pair = 59.8` and `P_exc = 1` imprints the *number* 3. Here the honest reading is that the framework already sources "3" from Z₃-triality (the `t = (p−q) mod 3` classification), which is a **representation-theoretic** fact about SU(3), not a freeze-in count — and the capstone explicitly marks family replication OPEN, overriding the old "PROVEN from Z₃" tag. The transit does **not** generate the *number* 3; it generates the *splitting* of the three triality sectors that already exist. **I therefore do not claim a freeze-in origin for the generation count.** What the freeze-in can claim is narrower and defensible: given the three Z₃ sectors, the transit is what makes them *non-degenerate and exponentially spread*. (If anything, the relevant integer is the **fold's avoided-crossing multiplicity**, which is a property of the DOS at the van Hove cusp — a candidate worth checking against the triality-class count, but I tag it SPECULATION and would not lead with it.)

---

## 4. Honest assessment of the lens vs. the alternatives

All three cross-talks have landed and are folded in below (connes' verdict is decisive and corrected my mechanism for the better; baptista and hawking each sharpened a division of labor). The §2–3 mechanism above is the *post*-cross-talk form.

- **vs. connes (static finite-triple textures) — CROSS-TALK RESOLVED (one hinge open).** We are not in conflict; we are complementary at two layers of the same theorem. Connes characterizes the *space* of admissible textures in the multiplicity-acting complement (`⊕ 1_{V} ⊗ M_{m}(ℂ)`); I propose the *dynamical generator* that selects one. Connes confirms the transit is the natural dynamical home for `ε_LX` and the S34 CKM-from-Bogoliubov conjecture is mine to make concrete. Three substantive outcomes from the exchange:
  1. **Independent cross-axis confirmation of the conjugate-degeneracy wall.** Connes derives from `[J,D_K]=0` (BDI conjugation `(p,q)↔(q,p)`) that the t=1 and t=2 **equilibrium** spectra are IDENTICAL — the *same wall* I found as `C₂(fund)=C₂(antifund)=4/3` (§2.2), reached from the reality axiom rather than the Casimir. Two axes, one degeneracy: at equilibrium only **2** triality classes are distinct (t=0 vs t=1=t=2).
  2. **The two-origin widening ladder — connes' `(ε,ε)`-vs-`(ε²,ε)` negative, RESOLVED [DERIVATION].** Connes' honest negative: a *generic single-origin* perturbation of the democratic texture gives flat `(ε, ε)` steps, but the data needs a *widening* `(ε², ε)` ladder (a factor ~200 between the two light steps, `e:μ:τ = 0.000288 : 0.0595 : 1`). The freeze-in resolves this because its two steps have **two different physical origins**: the **big** step (t=0↔t=1) is the diagonal Casimir-amplitude `exp(−S₀ΔC₂)`, nonzero; the **small** step (t=1↔t=2) is *exactly zero from the diagonal amplitude* (the J/Casimir conjugate-degeneracy) and is supplied **entirely by the off-diagonal inter-sector magnitude `|w|`** (NOT by the phase — corrected per outcome 3). Two origins ⇒ a `(large, small)` widening ladder is the **generic** output, not a tuned one — connes' `(ε,ε)` negative is exactly right *for a single-origin perturbation*, and is precisely why the hierarchy is two-origin. (Sage-checked against PDG: observed `e/μ = 0.00484`, `μ/τ = 0.05946`, widening factor 12.3; `(e/μ)/(μ/τ)² = 1.37` — the `(ε², ε)` texture is real and order-1 in the freeze-in variables, with `ε = μ/τ`; the ~200 factor is the *composite* `e/τ`, exactly what a two-step widening ladder produces.)
  3. **The hinge — RESOLVED by connes' Stage-2 reality analysis (Sage-verified, this turn); a SPLIT verdict that corrects and strengthens the mechanism.** The decisive question was: does a triality-odd complex phase survive `[J,D_K+ε]=0`? connes modelled the frozen object as a Hermitian `ε_LX` on the `t1↔t2` doublet, `J = swap∘conj` (`J²=+1`, BDI). **Result (verified):** reality forces the *diagonal* `d1=d2` (the degeneracy reasserts — his independent confirmation of my `C₂(fund)=C₂(antifund)=4/3`) but leaves the *off-diagonal* `w = wr + i·wi` **completely unconstrained, phase included**. So the phase IS admissible — but the eigenvalues of `[[d,w],[w*,d]]` are `d ± |w|`, depending on `|w|` *only*. **The correction:** the 2↔3 MASS split is the off-diagonal *magnitude* `|w|` (a real `wr` suffices); the phase `arg(w)` survives and lives *entirely* in the diagonalizing unitary = **purely PMNS/CKM mixing + CP, not a mass datum**. I had said the phase splits the masses; connes' eigenvalue argument (which I reproduced symbolically: `λ_± = d ± √(wr²+wi²)`, eigenvectors `∝ e^{−i·arg w}`) shows the split is `|w|` and the phase is mixing. **This is a cleaner, stronger result:** one off-diagonal inter-sector `w` does both jobs — `|w|`→mass split, `arg(w)`→mixing/CP — exactly the S34 conjecture. **Load-bearing scope:** connes verified the phase survives *because* `J²=+1` (BDI); in DIII (`J²=−1`) reality forces `w` real and CP dies. The framework is BDI (PROVEN), so the prediction stands as a KO-class-conditional result.
- **vs. baptista (fiber-overlap localization) — CROSS-TALK RESOLVED (double-counting averted; envelope/split partition) [DERIVATION].** Baptista's lens gives a geometric Casimir-graded overlap `O_g ∝ exp(−k·C₂(p,q))` (equilibrium spectrum, sets the *coupling*); he proposed `mass = overlap × freeze-in-occupation` and asked whether my Bogoliubov coefficients are `C₂`-graded and could supply the unequal-ln-gap shape (his "ratio 1.889") that a single Casimir exponential misses. The honest answer is sharper than his framing and corrects a double-counting trap:
  1. **Yes, my occupation is `C₂`-graded** (`n_g ∝ exp(−S₀·C₂)`, Mechanism A). But that is exactly the problem with the naive product: if *both* his overlap and my occupation are Casimir exponentials, then `O_g × n_g ∝ exp(−(k+S₀)·C₂)` is **still a single Casimir exponential**, just a renormalized slope. **It creates no new shape.** So my amplitude CANNOT be the "second exponential lever" that fixes the `1.889` shape — two Casimir exponentials multiply to one. Stated plainly to baptista to avoid the double-count.
  2. **The real second lever is the off-diagonal inter-sector magnitude `|w|`, not a second diagonal amplitude.** Sage reproduces his numbers exactly: `ln(μ/e)/ln(τ/μ) = 5.3316/2.8224 = 1.889` (machine-exact), and `C₂(fund)=C₂(antifund)=4/3` (his "fixed 4/3"). The conjugate degeneracy means the small (t=1↔t=2) gap is *exactly zero* from any *diagonal* Casimir amplitude — his or mine — so the `1.889` unequal-gap shape comes from the **off-diagonal** entry `w` of the `[[d,w],[w*,d]]` block: the masses are `d ± |w|`, so the small split is set by `|w|` (a real `wr` suffices), tuned *independently* of the diagonal envelope. (connes' reality analysis, §4 connes outcome 3, established this: the phase `arg(w)` is mixing, not mass; the mass-split lever is `|w|`.)
  3. **The clean partition** (replacing his `overlap × occupation`): `mass-block = [diagonal Casimir ENVELOPE — baptista's exp(−kC₂), the big t=0↔t=1 gap] + [off-diagonal inter-sector w — my transit, with |w| the small t=1↔t=2 conjugate mass split and arg(w) the mixing]`. Hierarchy is geometric AND dynamical, cleanly factored: **baptista owns the diagonal envelope (Casimir magnitude); I own the off-diagonal inter-sector `w` (its magnitude splits the conjugate masses, its phase mixes the states)**. My freeze-in's genuine new content over his overlap is the *off-diagonal inter-sector coupling*, not a second diagonal amplitude — and `arg(w)` additionally predicts the mixing `J_CP`, which a pure-overlap picture cannot reach. (This bounds baptista's route too: the conjugate small-gap is off-diagonal for *both* of us; a pure-diagonal overlap, however graded, gives a `(large, 0)` ladder — the inter-sector channel is mandatory.)
- **vs. hawking (threshold magnitude/grading) — CROSS-TALK RESOLVED (production/filter split).** Hawking supplies the missing scale, and the exchange clarified a subtle physics point that refines *both* mechanisms. Hawking's greybody-hierarchy `y_i ∝ Γ(ω_i)·e^{−2πω_i/κ}` is itself a Bogoliubov form; he asked for the freeze-in **temperature/scale** to pin his surface gravity κ. The honest answer is a **production/filter split** [DERIVATION]:
  1. **The PRODUCTION has no temperature.** The transit is deep-sudden (`δt_transit/T_L = 1.25×10⁻⁵ ≪ 1`, `P_exc = 1.000`, relic `S_ent = 0`), so the primary Bogoliubov production is a multi-mode **squeezed vacuum, not a Gibbs state** — no single freeze-in T exists. Canonical: the GGE is an "8-temperature" state (S44; eight Lagrange multipliers) with a **mode-dependent** `T_GGE` (S63 "Level 0"), explicitly not one scale. So the production *amplitude* is set by the diabatic/non-adiabaticity route, with no κ in it.
  2. **The FILTER has a well-defined κ.** Hawking's `e^{−2πω/κ}` is a *horizon greybody* form — the escaping spectrum is (produced squeeze) × Γ(ω) of the exit surface (capstone §6.2). The κ for the fiber acoustic filter is the **SONIC** surface (`T = 0.112 M_KK`, `κ = 0.704805` — the genuine `v = c_BLV` Mach-1 crossing), NOT the `a₂`/`a₄` THERMODYNAMIC-channel-gradient surfaces (those are two-channel gradient ratios, KIND-tagged, not the sonic horizon his greybody sits on) and not `κ_GH = 1.365` (the emergent-4D Gibbons-Hawking horizon, a different object). His three κ candidates ARE my KIND-tagged ledger — we're on one structure; the SONIC κ is the fiber-mode answer.
  - **Joint division of labor** (the clean result): my non-thermal freeze-in sets the **exponent structure** (Casimir-graded `exp(−S₀ΔC₂)` + the triality phase, §2–3); hawking's horizon greybody Γ(ω) sets the per-mode **transmission envelope** on top — *two multiplied factors, not one κ*. Hawking's instinct that a single thermal `κ·e^{−2πω/κ}` grade cannot pin the leptons was correct, and the reason is exactly the `P_exc = 1` diabaticity: the production isn't thermal, so the scale enters the filter, not the production. The overall `M₀^{sector}` (top-quark scale, electron scale) remains hawking's threshold to supply (the `m_H = 131.8 GeV` family); whether the adiabaticity action `S₀` is *also* a threshold quantity (which would close magnitude AND hierarchy jointly) is the remaining open joint cross-question.

---

## 5. My single best bet — what I would compute first

**Mechanism B, restricted to its falsifiable core: the per-sector freeze-in `[[d,w],[w*,d]]` block from the diabatic transit — diagonal Casimir amplitude `d = e^{−S₀C₂}` + off-diagonal inter-sector `w = |w|e^{iΘ}` — fit `{S₀, |w|}` to the charged-lepton masses and `Θ` to one mixing datum, then *predict* the remaining quark/lepton mass ratios + the CKM/PMNS mixing + `J_CP`.**

Why this one, ranked by EVOI logic:

1. **It directly attacks why S97 failed.** S97 failed because the static route is multiplicity-scalar and *diagonal*-only. Mechanism B is the minimal modification that (a) moves to the transit trajectory where the sectors differ (§2.1) and (b) adds the **off-diagonal inter-sector coupling `w`** that connes' reality analysis proved is the *only* admissible lever for the conjugate-pair split (§2.2, §4-connes-3). It is not a new texture guess; it is the structurally-forced next move — and the diabatic transit *generates* off-diagonal inter-sector mixing naturally (the t1/t2 sectors are degenerate at the fold, so the crossing mixes them).
2. **It is over-constrained, so it can actually be falsified.** A small handful of shape knobs (`S₀`, `|w|`, `Θ`, possibly sector-graded) against 8 mass ratios + the full CKM/PMNS mixing + Jarlskog. A FAIL closes the "hierarchy+mixing is a transit freeze-in" corridor cleanly; a PASS would be the first derivation of the mass *and* mixing *shape* from substrate dynamics. Either verdict is high-information.
3. **It unifies the 2↔3 mass split, the mixing, and the CP phase in ONE off-diagonal `w`.** connes' verified eigenvalue/eigenvector result is the sharpener: `|w|` sets the conjugate-pair MASS split (the small `(ε², ε)` ladder step, §4-baptista-2), and the *same* `w`'s phase `arg(w)` sets the CKM/PMNS mixing + `J_CP ~ sin(arg w)` — one complex inter-sector number, two jobs. And the *same* `[J,D_K]=0` reality wall that locks the diagonal degenerate also zeroes the *internal* CP channel and forces CP external — **exactly** the W3-1 baryogenesis result (`φ_CP = π/2`, the external `φ_88`-Cartan source). So the off-diagonal phase `arg(w)` and the baryogenesis `φ_88` external phase are plausibly the **same non-LI datum** seen in three observables (quark mixing, lepton mixing, baryon asymmetry), all riding the off-diagonal inter-sector channel the same reality wall opens. (Supporting: my f_NL work measured real Bogoliubov squeeze phases `~0.005–0.012 rad` — small but nonzero, the order-of-magnitude home for a transit phase. Scope: the CP-phase survival is BDI-specific — dies in DIII — and the framework is BDI, PROVEN.)
4. **The machinery is in hand.** Block-diagonal cache exists; the per-mode parametric-oscillator integration is exactly my A_s/GGE toolchain (ODE solver, Bogoliubov matching). The new pieces are (i) the **inter-sector** (off-diagonal) Bogoliubov coefficient `w` between the t1/t2 modes — not just per-sector occupation — and (ii) tracking its phase, not only its modulus.

**Pre-registered shape of the first gate** (no formal block per the loose-bureaucracy brief): integrate the triality-class parametric oscillators across the fold from the L12 cache, *including the inter-sector t1↔t2 coupling*; extract the diagonal `d_gen` and the off-diagonal `w`; fit `{S₀, |w|}` to the charged-lepton masses and `Θ = arg(w)` to one lepton mixing datum; then **predict** `{m_u:m_c:m_t, m_d:m_s:m_b}`, the CKM angles, and `J_CP` with no further freedom. PASS iff the quark mass ratios + CKM land within a pre-set tolerance AND `J_CP` has the right sign and order of magnitude; INFO iff masses fit but the mixing needs a sector-dependent `w` (partial unification); FAIL iff the shared `{S₀, |w|, Θ}` cannot span quarks+mixing (closes the corridor). The honest caveat carried from the outset: like the S99 seesaw `m_D` normalization, the overall `M₀^{sector}` is **not** fixed by this mechanism — that is hawking's threshold to supply — so a PASS derives the *shape* (mass ratios + mixing), with the *scale* still a threshold input. That is already a strict improvement on `1:1:1`.

---

## 6. One-line headline

**The charged-fermion hierarchy and CKM/PMNS mixing are plausibly the diagonal and off-diagonal data of one inter-sector freeze-in block `[[d,w],[w*,d]]` generated by the supersonic fold transit: the diagonal Casimir-graded amplitude `d = e^{−S₀C₂}` sets the big (t0↔t1) mass envelope, and a single off-diagonal inter-sector Bogoliubov coefficient `w` does two jobs — its magnitude `|w|` splits the conjugate (t1↔t2 = 2nd/3rd) masses, its phase `arg(w)` sets the CKM/PMNS mixing + CP — a strongly over-constrained, falsifiable replacement for the S97 `1:1:1` static texture, with the overall sector scale left to hawking's thresholds. The off-diagonal phase's survival is reality-admissible precisely because the framework is BDI (`J²=+1`); it would die in DIII. Verified jointly with connes (Stage-2 reality analysis): reality locks the diagonal degenerate (`d1=d2`, his independent route to my `C₂(fund)=C₂(antifund)=4/3`) and leaves `w` free, so the conjugate split is necessarily off-diagonal — the only admissible lever.**

---

*Anchors: capstone §1.3/§2.3/§5.3 (block-diagonal exact per-mode oscillator; GGE freeze-in; KZ Lagrange multiplier); S99 W3-1 (multiplicity-scalar wall, Non-LI-Deformation-Necessity STAGE-3-PERMANENT, ε_LX reality-compatible); S97-YUKAWA-FAMILY-DERIVE (the 1:1:1 FAIL this targets); S62 (tree-Yukawa vanishes); S63 (triality-resolved DOS, distinct sector mode populations); Rank-1 Josephson J_12/J_23=19.52 (the static texture this dynamic route must beat); AZ class BDI (`J²=+1`, foundational PROVEN — the KO-class on which the off-diagonal phase survives). Research corpus: Parker 1966 (gravitational pair creation), Kofman–Linde–Starobinsky 1994 (mode-by-mode `|β_k|²~exp(2Nμ_k)`), Kibble 1980 / Zurek 1985 (impulse-adiabatic freeze-in), Bogoliubov–Valatin (complex coefficient), Calabrese–Essler / Rigol (GGE). Sage-checked: C₂(1,0)=C₂(0,1)=4/3 (diagonal conjugate degeneracy); eigenvalues of `[[d,w],[w*,d]] = d ± |w|` with eigenvectors `∝ e^{−i·arg w}` (mass split = `|w|`, phase = mixing — connes' Stage-2 result, reproduced symbolically); `ln(μ/e)/ln(τ/μ)=1.889` (the widening-ladder shape); S₀≈3.2 (O(1) instanton action reproduces the t0↔t1 envelope ratio). All quantitative claims here are SPECULATION/CONJECTURE pending the gate in §5; the DERIVATION results are the static-collapse obstruction (§2.1) and the corrected diagonal-degeneracy / off-diagonal-`|w|`-splits / `arg(w)`-mixes theorem (§2.2, verified jointly with connes, BDI-conditional).*
