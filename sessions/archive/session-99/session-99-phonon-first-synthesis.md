# Session 99 Synthesis: The Standard-Model Lagrangian as a₄, and the Fermion-Mass Block as ε_LX on the Multiplicity Bundle

**Date**: 2026-06-03
**Agent**: phonon-first-cosmologist (Phonon-First)
**Source Documents**:
- `downloads/standard-model-lagrangian-explained.md` (SM-Lagrangian plain-English explainer + §4 phonon-exflation / Kaluza–Klein framing coda)
- `sessions/archive/session-99/session-99-fermion-mass-panel.md` (S99 fermion-mass innovation panel: connes / baptista / transit / hawking, consolidated final pass)

---

## I. Session Outcome

The two sources are **complementary cross-sections of one substrate object** — the internal Dirac operator `D_K` on Jensen-deformed SU(3) — read at two depths. The SM-Lagrangian coda fixes the macroscopic arrow: the entire bosonic mug-equation is the **a₄ Seeley–DeWitt moment** of the spectral action `S = Tr f(D_K/Λ)`, gravity is its neighbour `a₂`, the gauge group `U(1)×SU(2)×SU(3)/Z₆` is the algebra `A_K = ℂ⊕ℍ⊕M₃(ℂ)`'s automorphism content (NCG route, PROVEN-and-scoped), and fermion masses are entries of `D_K`'s finite part. The S99 fermion-mass panel then drills into exactly that last claim — `D_K`'s finite part — at the single open block on the scorecard (`S97-YUKAWA-FAMILY-DERIVE` FAIL, `R_cross = 1.019704` democratic vs PDG `1:0.0595:0.000288`). The panel's decisive structural result: the `1:1:1` democracy is **not a computational miss but the PROVEN Homogeneity wall (W2)** — left-invariance of `D_K` forces a multiplicity-scalar representation, which cannot carry a generation index. This *orients* the search rather than ending it: the hierarchy is forced onto a non-left-invariant deformation `ε_LX` on the multiplicity bundle, organized as a single inter-sector mass block `[[d, w],[w*, d]]` whose division of labor (envelope on the diagonal `d`, μ↔τ split on `|w|`, mixing/CP on `arg w`) is **reality-axiom-forced, not a modeling choice**. No gate verdicts or registry entries were emitted — this is a candidate-mechanism panel leaning on one PROVEN wall.

---

## II. Key Results

### 1. The bosonic SM Lagrangian IS the a₄ spectral moment; gravity is a₂

**Result**: The entire bosonic content of the SM Lagrangian (gauge kinetic terms, gauge self-interactions, Higgs kinetic + potential) is the fourth Seeley–DeWitt coefficient `a₄` of the spectral action `S = Tr f(D_K/Λ)`, with `a₀` the vacuum-energy/cosmological term and `a₂` the Einstein–Hilbert action. **Classification: GEOMETRIC substrate of a PARTICLE observable** (the coda's own classification; the Lagrangian's content is representation-theoretic = PARTICLE, but its origin is the fabric's spectral geometry = GEOMETRIC).

This is the substrate-first inversion of the casual statement "gravity is not in this equation." In the fabric picture gravity is not *missing* — it sits one spectral moment down. The mug prints `a₄`; gravity is the un-printed neighbour `a₂` in the same Λ-expansion of the same operator. The direction of explanation is fixed and one-way: `substrate geometry → spectral moments → Lagrangian`, never the reverse. This is the modern non-commutative descendant of the original Kaluza–Klein move (geometry producing a gauge field): here *all* gauge fields plus the Higgs plus gravity fall out of one geometric object's eigenvalue spectrum.

The expansion is organized by spectral moment, and the moments are **algebraically independent**, not a single quantity re-graded — consistent with my own permanent note that the Wronskian `W ∝ R_K'(τ)³ = e^{-12τ}(e^{3τ}-1)⁶` proves the a₀/a₂/a₄ layers are independent, degenerate only at genesis τ=0. The CC/gravity/Yang-Mills separation is therefore structural: `a₀` (CC), `a₂` (gravity), `a₄` (SM bosons) are different coefficients, not different readings of one number. This is the same Spectral Moment Decoupling I carry as a permanent cross-pillar theorem (CC uses the `F_{-1}` moment, gravity the `F_{+1}` — different moments of the same `f`).

**Status (authoritative-as-cited)**: the coda flags this as **interpretive/defensible, not a closed proof** — `r ≈ 0.96` correlation against the Baptista effective potential. I do not upgrade it. It is the framework's organizing claim for the bosonic sector.

### 2. The gauge group from the finite algebra — PROVEN, scoped to the NCG route

**Result**: `Aut(A_K)` with `A_K = ℂ⊕ℍ⊕M₃(ℂ)` reproduces `U(1)×SU(2)×SU(3)/Z₆` — the three summands hand you U(1), SU(2) (from ℍ), and SU(3) (from M₃(ℂ), exactly the gluon color of Block 1). **Classification: GEOMETRIC** (the algebraic-symmetry content of the fabric's internal structure). Knowledge-base status **PROVEN** (atlas-07; the gauge-group-from-A_F equation appears verbatim across the framework-paasch and session-84 records; KO-dim=6 + SM quantum numbers is the foundational S7-8 result).

The coda's honest scoping is load-bearing and I confirm it against the knowledge base: the framework has **two** routes to a symmetry group — the NCG-algebra route (`Aut(A_K)`) and a Kaluza–Klein isometry route (`Isom(K, g_τ) = U(1)×SU(3)_R`, session-31 record). These do **not** give the same group. The chiral gauge content — including the left-handed-only SU(2) that drives Block 13's parity violation — lives on the **NCG route via the proven fermion branching `Ψ₊ = ℂ¹⁶`**, NOT on the KK-isometry route. The KK-isometry picture supplies *labels*, not the chiral gauge group. This is the correct anatomy: do not oversell "gauge from geometry" as if both routes deliver it.

This is a textbook instance of my native methodology — the **same symmetry group appearing in two structurally distinct constructions**. The constraint that the chiral content is NCG-route-only is exactly the kind of regime-of-validity boundary that sharpens the map: it tells you which route carries the physics and which carries only the bookkeeping.

### 3. The S97 democracy is the PROVEN Homogeneity wall (W2) — reframe, not failure

**Result**: `S97-YUKAWA-FAMILY-DERIVE` returned `1:1:1` (`R_cross_yukawa_t1_t2 = 1.019704`, canonical-constants pinned) vs PDG `1:0.0595:0.000288`. This is a **PROVEN consequence of substrate homogeneity (W2)**, not a computational miss. **Classification: GEOMETRIC** (a representation-theoretic obstruction on the multiplicity bundle of `D_K`).

The wall (verified PROVEN in `permanent-results-registry.md`; knowledge-base hit confirms the theorem text and its SOURCE-DOUBLE-CITE-CO-PRIMARY anchor structure): `D_K` is left-invariant on SU(3); by Peter–Weyl the algebra acts as `⊗ 𝟙_{m(p,q)}` on every multiplicity factor, and the generation index lives in that factor. A multiplicity-scalar operator **cannot** carry a generation index → democratic masses, by theorem. Three independent seals:

- **Reality is innocent.** The obstruction is homogeneity (W2), not the reality condition `[J,D_K]=0` (W1). Reality constrains (it forces the t=1 and t=2 spectra identical) but is never the wall — connes corrected the prior S97-era attribution that blamed reality.
- **The twisted escape is dead by Skolem–Noether.** `A_K = ℂ⊕ℍ⊕M₃(ℂ)` has three non-isomorphic simple summands, so every `σ ∈ Aut(A_K)` is block-inner ⇒ multiplicity-scalar. `Aut(A_K)` is multiplicity-blind.
- **The dead end has a normal vector.** The hierarchy is *forced* to live in a non-left-invariant deformation `ε_LX` on the multiplicity-acting complement `⊕ 𝟙_{V_{(p,q)}} ⊗ M_{m(p,q)}(ℂ)`, reality-compatible (`[J, D_K + ε_LX] = 0`). The question shifts from "why democratic?" (answered) to "what is `ε_LX`?".

This is the model failure-as-constraint pattern: the FAIL closed a corridor (multiplicity-scalar operators cannot grade generations) and the closure *produced* the search direction (break left-invariance on the multiplicity index). A negative result that hands you the normal vector to the excluded surface is worth more than a hundred narrative "further work needed."

### 4. The unified object — one inter-sector block `[[d, w],[w*, d]]`, four faces

**Result**: The four lenses converged on **one** object, the mass block on the conjugate generation doublet `M_(μ,τ) = [[d, w],[w*, d]]`, eigenvalues `d ± |w|`, mixing `∝ arg(w)`. **Classification: GEOMETRIC** (finite-part data of `D_K`; the matter contraction `⟨ψ, D_K ψ⟩` is the fermion sector per the coda).

The diagonal `d` (the e-vs-heavy-pair ~8 e-fold envelope) is **one exponential seen four ways** — the framework's "one operator, several faces" signature:

| Lens | The exponential | Reading of the diagonal envelope | Pillar |
|:-----|:----------------|:---------------------------------|:-------|
| baptista | `O_g ∝ exp(−k·C₂(p,q))` | Higgs-`|s(h)|²`-overlap of the Peter–Weyl mode, Jensen-weighted | VIII (KK/Jensen) |
| connes | `m_i ∝ exp(−d_i/ℓ)` | Connes distance between generation-states on the multiplicity bundle | III (NCG) |
| hawking | `y_i ∝ Γ(ω_i)·exp(−2πω_i/κ)` | greybody transmission at the exit horizon | I (analogue gravity) |
| transit | `m_gen ∝ exp(−S₀·C₂)` | Casimir-graded diabatic freeze-in amplitude | VI/V (solitons / non-equilibrium) |

The identification `d_i/ℓ ↔ 2πω_i/κ ↔ S₀C₂ ↔ k·C₂` spans four of the eight pillars — the analogue-gravity greybody, the NCG Connes-distance, the KK/Jensen Casimir overlap, and the non-equilibrium freeze-in are the *same* exponential. This is precisely the cross-domain structural-identity my role exists to detect. **Double-counting guard (correctly logged by the panel)**: transit's freeze-in is itself a Casimir exponential, so `exp(−kC₂)·exp(−S₀C₂) = exp(−(k+S₀)C₂)` is still one exponential — transit co-sets the slope `k → k_eff`, it does not add a second shape.

The off-diagonal `w` is transit's genuine new content (the inter-sector `t1↔t2` coupling the diabatic crossing generates) and does two jobs: `|w|` → μ↔τ mass split; `arg(w)` → CKM/PMNS mixing + CP phase — the S34 "mixing from inter-sector Bogoliubov coefficients" conjecture, realized.

### 5. The division of labor is reality-FORCED, and CP is tied to chirality (BDI)

**Result**: connes verified all three spectral-triple axioms Sage-exact on the greybody/overlap-reweighted `D_F`. **Classification: GEOMETRIC** (axiom-level structural verdict). The decisive item: reality `[J,D_F]=0` PASSes for the diagonal envelope **only**, and this forces the split off-diagonal.

`J` swap-conjugates the `(μ,τ) = t1↔t2` pair, so reality **forces `d_μ = d_τ`** on the diagonal. Therefore a diagonal `diag(d_e, d_h, d_h)` — distinct on the J-fixed electron (t=0, trivial rep, C₂=0), equal on the J-swapped heavy pair — is reality-exact and **legally carries the e-vs-heavy envelope**; a diagonal `d_μ ≠ d_τ` would break reality, so the **μ↔τ split is forbidden on the diagonal and forced onto `|w|`**. The eigenvalue arithmetic seals it: `M = [[d,w],[w*,d]]` has eigenvalues `d ± |w|` (magnitude-only) and eigenvectors depending on `arg(w)` only. One off-diagonal `w` does both jobs, and **split-is-magnitude / mixing-is-phase is a consequence of the reality axiom**.

**BDI-specificity (the cross-domain payoff).** connes checked the DIII alternative (`J²=−1`): there reality forces `w` real (phase killed) and `d_μ=d_τ`. So `arg(w)` survives **precisely because the framework is BDI (`J²=+1`, PROVEN)** — the same KO-dim-6 / BDI class that gives chirality also licenses the CP phase to live in the mixing matrix. CP-in-mixing and chirality are two consequences of one structural fact. This connects directly to my permanent note that chirality is `{γ₉, dD_K/dτ}=0`: the same anticommutation structure that the deformation respects is the class that admits the surviving phase. The order-one and KO-dim-6 axioms PASS unconditionally (the weight is a multiplicity-index scalar, blind to the generation index — index-disjointness), so **any** generation texture is order-one-admissible; reality is the only axiom that bites, and it bites in exactly the place that produces the observed structure.

### 6. Generation count is dual-Z₃; the widening ratio is a zero-parameter Casimir prediction

**Result**: Three generations require `Z₃ × Z₃` (Baptista Paper 18 App E), and the lepton mass-widening ratio is `9/5 = 1.800` exactly (Sage-QQ) vs PDG lepton `1.8894` — **4.7% off, zero free parameters**, *conditional on the generation-sector assignment being the triality-distinct tower* `(1,0)/(1,1)/(3,0)` with `C₂ = (4/3, 3, 6)`. **Classification: GEOMETRIC/PARTICLE** (Casimir quantization of the representation tower).

The two Z₃ factors are independent: (#1) triality `t = (p−q) mod 3`, which collapses `t=1 ≡ t=2` under BDI reality `(p,q)↔(q,p)`; (#2) the `s_φ` Higgs-mode phase, where baptista's lepton factor `c(φ) = 1/(1+8cos²φ)` at the Z₃ points `{0, 2π/3, 4π/3}` gives `{1/9, 1/3, 1/3}` — a second 2-fold collapse on an orthogonal label. Each single Z₃ → ≤2 rungs (why S97's naive single-Z₃ was doomed); the product yields 3. The distinct φ-factors `{1/9, 1/3}` are a **lepton-only** lever (quark matrices `Ω^D, Ω^c` carry no φ-term), which **explains the lepton-vs-quark hierarchy-shape difference the SM fits by hand** — a genuine structural prediction, not a fit.

The discriminating power matters: a generic Gaussian-overlap `n²` model gives `3.0` (59% off); the Casimir ladder gives `1.80`. The data selects the Casimir ladder over generic position-overlap. This is a discriminating test in the EVOI sense — the candidate makes a sharp, parameter-free prediction that a plausible alternative misses by an order of magnitude in the residual.

### 7. Production has no temperature; the filter has one κ; the scale is generation-blind

**Result**: A clean three-way separation of mechanism. **Classification: PHONONIC** (transit excitation physics) for production/filter; **GEOMETRIC** for the scale.

- **Production has no temperature.** The transit is deep-sudden (`δt/T_L = 1.25×10⁻⁵`, `P_exc = 1.000`, `S_ent = 0`), so primary Bogoliubov production is a multi-mode squeezed vacuum, not a Gibbs state — the canonical Ordered-Veil signature (the GGE relic never thermalizes). The production *amplitude* is the diabatic/Casimir-graded route — **no κ**. The naive Landau–Zener `γ ≈ 10⁻³` is generation-blind in the diabatic limit; the graded object is the GGE squeeze depth `−ln|ψ_pair|²`, which IS hawking's greybody exponent.
- **The filter has a well-defined κ.** hawking's `e^{−2πω/κ}` uses `κ_SONIC = 0.7048 M_KK` (`= 2π·0.112`, the genuine `v = c_BLV` Mach-1 crossing) — **not** `κ_GH = 1.365` (emergent-4D, explicitly corrected) and not the a₂/a₄ thermodynamic-gradient surfaces. This is the exit-horizon piece of the six-layer causal structure I carry: the exit sonic horizon (a₄ BCS condensation) is the relevant κ, distinct from the entry/kinematic horizon.
- **The overall scale is generation-blind.** A bare KK-threshold tower sum is power-law and saturating, not exponential, and multiplicity-scalar by the same Peter–Weyl argument as W2. So the KK threshold (the `m_H` machinery) sets the per-sector scale `M₀^{sector}` and is **NOT** the seat of the grading — killing "the KK threshold *is* the hierarchy."

This is dimensionally and structurally consistent: a saturating power-law tower cannot produce a factor-200 hierarchy, so the exponential must come from the greybody filter acting on `ε_LX` frequencies, not from the tower itself.

---

## III. Gate Verdicts

| Gate | Verdict | Decisive Number |
|:-----|:--------|:----------------|
| `S97-YUKAWA-FAMILY-DERIVE` | FAIL (reframed PROVEN W2) | `R_cross_yukawa_t1_t2 = 1.019704`; democratic `1:1:1` vs PDG `1:0.0595:0.000288` |
| (W2) Homogeneity wall | PROVEN (registry; §VII.BL E1) | left-invariance ⇒ multiplicity-scalar ⇒ democratic by theorem |
| `S99-E1-STAGE2-VERIFY` | Stage-2 cross-axis verify of §VII.BL E1 (panel-referenced; no verdict emitted) | — |
| `S99-W3-SEESAW-SUMMNU` | panel-referenced (neutrino structure + ordering substrate-first; one scale anchored) | — |
| `KK-THRESHOLD-64` | cited as the `m_H` machinery | `m_H = 131.8 GeV` (per both source docs) |
| g₁/g₂ = e^{−2τ} | PROVEN structural identity ("PASS then weakened" on the gate that used it) | `g₁/g₂ = 0.549` consistent |

> No new gate verdicts or registry entries were emitted in S99 — the fermion-mass panel was an innovation exercise (candidate mechanisms, "loose-bureaucracy brief"). The verdicts above are the prior/cited results the panel and coda lean on, carried at their knowledge-base-confirmed status. Per the synthesis rules, source-doc verdicts are authoritative and not re-adjudicated here.

---

## IV. Structural Implications

**What opened.** The charged-fermion `○✗` scorecard block is, for the first time since S97, **correctly posed**. The reframe converts a flat FAIL into an oriented search: the hierarchy must live in `ε_LX` on the multiplicity-acting complement, organized as `[[d,w],[w*,d]]`, with a reality-forced division of labor. The off-diagonal datum `w` plausibly sets **three** observables from one source: the μ↔τ mass split (`|w|`), the CKM/PMNS mixing + CP (`arg w`), and — via the same `[J,D_K]=0` wall that forces the external `φ₈₈` Cartan phase to π/2 — the baryon asymmetry. One inter-sector phase, three measured quantities, is a strong over-constraint and a clean falsifier.

**What closed.** Three corridors are now walls: (i) multiplicity-scalar operators cannot grade generations (W2, PROVEN); (ii) the twisted/inner-automorphism escape is dead by Skolem–Noether (`Aut(A_K)` is multiplicity-blind); (iii) the KK-threshold tower is NOT the seat of the hierarchy (power-law, saturating, multiplicity-scalar). connes also *retired* his own seesaw-squaring as the vehicle for the factor-200 ("right instinct — exponentiation; wrong vehicle — seesaw"); what survives is the narrower true observation that squaring is shape-preserving (the y-ladder log-gap ratio is also 1.889), which halves the frequency offset the envelope needs (`Δω ~ 0.9 M_KK`, one fiber gap).

**What shifted.** The two source documents together pin the **complete arrow from substrate to SM**: `D_K` eigenvalues → spectral moments (a₂ gravity, a₄ bosonic SM) and `D_K` finite part → fermion masses + mixing. The coda fixes the bosonic and gauge-group ends (a₄, `Aut(A_K)`); the panel attacks the fermionic end (`ε_LX` on the multiplicity bundle). The masses of matter ARE the eigenvalue data of the fabric's internal Dirac operator — the substrate IS the mass spectrum, and the panel's job is to find the one deformation `ε_LX` that breaks left-invariance on the right index.

**Cross-source consistency check.** The two documents are mutually reinforcing on the physics; I find no physics conflict. The coda's "Yukawa masses = entries of `D_K`'s finite part" (flagged interpretive) is exactly the object the panel makes concrete as `[[d,w],[w*,d]]`. One **numerical tension to flag explicitly** (not re-adjudicated): both source docs cite `m_H = 131.8 GeV` (via `KK-THRESHOLD-64`), while the canonical-constants store carries only the *observed* `m_H_obs = 125.1` (PDG, not a framework prediction) and my agent memory carries a stale framework value `127.5 GeV (KK-L5, Aitken)`. The two **source documents agree with each other** at 131.8 and are internally consistent; per the rules I take the sources as authoritative for this synthesis and note that the framework `m_H` prediction is scheme/extrapolation-dependent (a known open precision question, orthogonal to the fermion-mass result).

**Honest caveat on convergence (per `epistemic-discipline.md`).** Four agents given the same hard-facts block converge somewhat by construction; shared-context agreement is not independent confirmation. The parts that carry weight because they are NOT shared-prompt artifacts: (a) three structurally-orthogonal routes to "the split is off-diagonal" (transit's Casimir degeneracy `C₂(fund)=C₂(antifund)=4/3`, connes' reality forcing `d_μ=d_τ`, baptista's J-even diagonal overlap); (b) the BDI→CP tie (phase survives in BDI, dies in DIII) is derived; (c) the W2 Homogeneity wall is independently PROVEN. These three are the load-bearing results; the rest is oriented but not confirmed.

---

## V. Carry-Forward Computations

V.1. **baptista per-sector Higgs-overlap WITH the off-diagonal element [consensus lead]**
   - **What**: Compute `O_g = ∫_K Tr[ψ_g† |s(h)|² ψ_g] vol_{g_τ}` at `L_max=12`, `τ_fold`, **plus** the inter-sector `t1↔t2` matrix element `w`. This is the literal missing calculation from Baptista Paper 14 §3 (Laplacian matrices written; Dirac-mass overlap was not). Outputs: the diagonal envelope `{d_e, d_h}`, the off-diagonal `|w|` and `arg(w)`, and a direct `|s|²`-weighted check of the `9/5 = 1.800` widening.
   - **Inputs**: `D_K` spectrum cache at `L_max=12`, `τ_fold` (canonical_constants), Jensen metric `g_τ`, Higgs mode `|s(h)|²`; triality-distinct tower assignment `(1,0)/(1,1)/(3,0)`, `C₂ = (4/3, 3, 6)`.
   - **Gate**: new gate `S100-YUKAWA-OVERLAP-OFFDIAG` — PASS if `|w|`-derived μ↔τ ratio within tolerance of PDG AND `9/5` widening reproduced from the actual integral (not assumed); FAIL if the `|s|²` integral gives the generic `3.0` widening; INFO if envelope reproduces order-of-magnitude + gap-asymmetry direction but the ratio needs a 1-parameter normalization.
   - **Effort**: 4–6 hours, 1 agent session (irrep construction at `L_max=12` is the cost; the (p,q) sectors needed are low — `p+q ≤ 4` — so feasible).

V.2. **Cheapest sub-test: diagonalize `Ω^b_g` at the three Z₃ φ-points**
   - **What**: Diagonalize the closed-form 3×3 lepton matrix `Ω^b_g` at `φ ∈ {0, 2π/3, 4π/3}` and verify `c(φ) = 1/(1+8cos²φ)` gives `{1/9, 1/3, 1/3}`. Tests whether the `s_φ`-phase is genuinely the second Z₃ of the dual-Z₃ structure.
   - **Inputs**: baptista's closed-form 3×3 `Ω^b_g` (already in hand); `c(φ)` factor.
   - **Gate**: new gate `S100-DUAL-Z3-PHI-POINTS` — PASS if the `{1/9, 1/3, 1/3}` 2-fold collapse confirmed at the Z₃ points AND the quark matrices `Ω^D, Ω^c` show no φ-dependence (lepton-only lever); FAIL otherwise.
   - **Effort**: 1–2 hours, 1 agent session (closed-form 3×3, no eigensolve cost).

V.3. **connes Connes-distance ladder on the multiplicity bundle**
   - **What**: Compute the Connes distances `d_i` between generation-states on the multiplicity bundle; test `mass = e^{−d_i/ℓ}` and the widening signature `≈ 1.89`. Independent geometric route to the same envelope as V.1.
   - **Inputs**: machinery exists (`S88-CONNES-DISTANCE-FINITE-SPECTRUM-IDENTITY`); finite spectral triple `(A_K, H_K, D_K)`; `ε_LX`-split mode states.
   - **Gate**: new gate `S100-CONNES-DIST-LADDER` — PASS if `e^{−d_i/ℓ}` reproduces the e-vs-heavy envelope AND the widening lands near 1.89; INFO if envelope correct but widening needs the sector-assignment input from V.1; FAIL if distances are degenerate (would contradict the `ε_LX` premise).
   - **Effort**: 2–3 hours, 1 agent session.

V.4. **transit inter-sector freeze-in block — the over-constrained falsifier**
   - **What**: Fit `{S₀, |w|}` to charged-lepton masses and `arg(w)` to ONE mixing datum, then **predict** the six quark ratios + CKM angles + `J_CP` with no further freedom. Over-constrained: a clean FAIL closes the corridor; a PASS derives mass+mixing *shape* from substrate dynamics.
   - **Inputs**: `[[d,w],[w*,d]]` block form; `S₀ ≈ 3.2` (O(1) instanton action, a ratio `(ε_LX-split scale)/(horizon κ)`); charged-lepton masses (PDG); one CKM mixing datum to fix `arg w`.
   - **Gate**: new gate `S100-FREEZEIN-OVERCONSTRAINED` — PASS if predicted quark ratios + CKM angles + `J_CP` match PDG within tolerance given only `{S₀, |w|, arg w}` (3 inputs, ~12 predictions); FAIL if any predicted observable is off by > tolerance — a genuinely informative corridor closure.
   - **Effort**: 3–4 hours, 1 agent session (depends on V.1 for the `|w|` seed).

V.5. **hawking envelope over-determination — derive the exponent twice**
   - **What**: Extract the diagonal exponent from the greybody filter at the sonic horizon `κ_SONIC = 0.7048 M_KK` and compare against transit's `S₀`. If they coincide, the envelope is derived twice (independent confirmation, not shared-context agreement).
   - **Inputs**: `κ_SONIC = 0.7048 M_KK` (`= 2π·0.112`, the `v = c_BLV` Mach-1 crossing); `ε_LX` frequencies `ω_i`; transit's `S₀` from V.4.
   - **Gate**: new gate `S100-ENVELOPE-OVERDETERMINE` — PASS if greybody-derived exponent agrees with `S₀` within tolerance (the envelope is over-determined); INFO if they differ by an O(1) factor traceable to the κ-surface choice; FAIL if they disagree by > order of magnitude.
   - **Effort**: 2–3 hours, 1 agent session (depends on V.4 for `S₀`).

V.6. **Is `S₀` itself a threshold quantity? — the one open joint cross-question**
   - **What**: Test whether `S₀ = (ε_LX-split scale)/(horizon κ)` is fixed by the KK-threshold machinery. If yes, magnitude AND slope close *jointly* rather than separately — the single highest-leverage open question the panel flagged.
   - **Inputs**: KK-threshold tower (`KK-THRESHOLD-64` machinery); `ε_LX`-split scale `Δω ~ 0.9 M_KK` (one fiber gap, after the shape-preserving-squaring halving); `κ_SONIC`.
   - **Gate**: new gate `S100-S0-THRESHOLD-JOINT` — PASS if `S₀` derives from threshold quantities with no free normalization (closes envelope magnitude + slope together); INFO if `S₀` is threshold-constrained to O(1) but a 1-parameter normalization remains; FAIL if `S₀` is independent of the threshold (the scale stays an empirical anchor, as in the S99 W3-2 neutrino pattern).
   - **Effort**: 3–4 hours, 1 agent session.

V.7. **Stage-2 cross-axis verify of §VII.BL E1 (the Homogeneity wall)**
   - **What**: Run the panel-referenced `S99-E1-STAGE2-VERIFY` as a genuine two-agent parallel cross-check per `joint-theorem-promotion.md` Stage-2: one spectral/NCG-axis reviewer (W1 reality + multiplicity-scalar clause) and one transit/superfluid-axis reviewer (the `ε_LX` admissibility clause), both operating without the panel's transcript, PASS-AND on the joint clauses.
   - **Inputs**: registered §VII.BL E1 entry text only (NOT the panel files); `permanent-results-registry.md`; the Skolem–Noether and Peter–Weyl supporting lemmas.
   - **Gate**: `S99-E1-STAGE2-VERIFY` (existing ID) — PASS-AND promotes §VII.BL E1 from STAGE-1-CANDIDATE to STAGE-3-PERMANENT; FAIL on any clause keeps it at Stage 1.
   - **Effort**: 2–3 hours, 2 parallel agent sessions (one per axis, per the protocol).

---

## VI. Summary Table

| # | Result | Classification | Status | Implication |
|:--|:-------|:---------------|:-------|:------------|
| 1 | Bosonic SM Lagrangian = a₄; gravity = a₂; CC = a₀ | GEOMETRIC (substrate of PARTICLE) | Interpretive/defensible (`r ≈ 0.96`) | Gravity is not missing — it is the un-printed neighbour moment; one operator, three Λ-coefficients |
| 2 | `Aut(A_K)` = `U(1)×SU(2)×SU(3)/Z₆` | GEOMETRIC | PROVEN, scoped to NCG route | Chiral gauge content is NCG-route-only; KK-isometry gives labels, not the chiral group |
| 3 | S97 `1:1:1` IS the PROVEN Homogeneity wall (W2) | GEOMETRIC | PROVEN (registry §VII.BL E1) | Reframe: break left-invariance on the multiplicity index; the dead end has a normal vector |
| 4 | One inter-sector block `[[d,w],[w*,d]]`, diagonal = one exponential ×4 faces | GEOMETRIC | Candidate (panel convergence) | Same exponential across 4 pillars (greybody/Connes-dist/Casimir-overlap/freeze-in) |
| 5 | Split-is-`|w|` / mixing-is-`arg w`; CP survives because BDI | GEOMETRIC | Reality-axiom-FORCED (3 orthogonal routes); BDI→CP derived | One `w` sets μ↔τ split, mixing/CP, and (via φ₈₈) baryogenesis |
| 6 | Dual-Z₃; widening ratio `9/5 = 1.800` (zero-param) | GEOMETRIC/PARTICLE | Candidate, conditional on sector assignment | 4.7% off PDG `1.8894`; selects Casimir ladder over generic `n²` (3.0); explains lepton-vs-quark shape |
| 7 | Production = squeezed vacuum (no κ); filter κ = `κ_SONIC = 0.7048 M_KK`; scale generation-blind | PHONONIC (prod/filter) / GEOMETRIC (scale) | Candidate (well-separated mechanism) | KK threshold sets `M₀`, NOT the hierarchy; grading from greybody on `ε_LX` |

---

*Sources read in full: `downloads/standard-model-lagrangian-explained.md` (§1–§3 plain-English, §4 framework coda with its own PROVEN-vs-interpretive ledger); `sessions/archive/session-99/session-99-fermion-mass-panel.md` (consolidated final pass over the four specialist files). Load-bearing anchors cross-verified against the knowledge MCP: (W2) Homogeneity wall PROVEN in `permanent-results-registry.md` (§VII.BL, SOURCE-DOUBLE-CITE-CO-PRIMARY); `Aut(A_F) = U(1)×SU(2)×SU(3)/Z₆` PROVEN (atlas-07, KO-dim=6 S7-8); `g₁/g₂ = e^{−2τ}` PROVEN structural identity ("PASS then weakened" on its gate); `R_cross_yukawa_t1_t2 = 1.019704` canonical (S97). One cross-source numerical tension flagged: `m_H = 131.8 GeV` (both source docs, via `KK-THRESHOLD-64`) vs canonical `m_H_obs = 125.1` (PDG observed) — sources taken as authoritative per the synthesis rules; framework `m_H` prediction is a known scheme-dependent open question. No gate verdicts or registry entries emitted — candidate-mechanism panel, substrate-first throughout (`D_K → spectral/transit structure → emergent masses & mixing → measured`).*
