# Session 99 Synthesis: Spectral Geometry / Heat Kernels / Zeta-Mellin Pole Structure (G5 Math Sweep)

**Date**: 2026-06-04
**Agent**: spectral-geometer (Spectral-Geometer)
**Source Documents**:
- `downloads/research-sweep-s99/spectral-geometry-math/00-INDEX.md` (10-paper index; summaries derived ONLY from fetched paper text)
- `downloads/research-sweep-s99/spectral-geometry-math/01..10_*.pdf` (PDFs are password-protected for direct Read; index summaries are the authoritative fetched-text source per the sweep protocol; two load-bearing arithmetic claims spot-verified independently via Sage MCP — see §I)
- Canonical anchors via knowledge MCP: `tau_fold=0.19`, `d_s_fold_window_sigma=1.4005`, `residue_s6_PS_Linf=0.0009393639575775`, CHAOS-1 gate, CM-1995 dimension-spectrum theorem, eta(D_K)=0 (five proofs), FK-bound output (`s52_fk_bound_output.txt`), N3 axiom status (`atlas-04-assumptions.md`)
- Agent memory: `.claude/agent-memory/spectral-geometer/MEMORY.md`

---

## I. Session Outcome

This is a literature-adjudication pass, not a compute session — no new framework gates are decided here; it produces classified candidate-validation angles as 4-field carry-forward specs. The single most consequential finding is a **correction to the CF28 framing in the index**, established against canonical state: the substrate-distance poles `s∈{5,6}` are NOT virgin territory — they are already convergent (SU(4)_PS shell-sum threshold `s>9/2`; `residue_s6_PS_Linf=0.0009393639575775` is canonical, S95) and carry F_2-class anti-correlation PASSes (S88 §W12-148). What papers **06 (Fucci-Stanfill)** and **10 (Connes)** actually supply is the rigorous *certification mechanism* for an assumption the framework **already makes and depends on**: the framework's residue formula `a_n = Res[Tr(D^{−2s}); s=(d−n)/2]` (CM-1995 §III.4) presupposes a **simple** dimension spectrum, and the framework explicitly invokes Connes-Moscovici §5 ("local index formula requires a regular spectral triple with simple dimension spectrum `S_d⊂ℤ`"). Papers 06+10 give (i) the proof that the clean Hurwitz/simple-pole form is the **Friedrichs (canonical-self-adjoint-extension) case**, and (ii) the literal mechanism by which boundary/defect data injects `t^{n/2}log t` terms — making the CF28 question well-posed and sourcing it to Connes himself.

Second consequential finding: **paper 03 (Savale) supplies the rigorous continuum floor `8/7` for the framework's L^{−α} truncation envelopes and the R-Protection `O(L^{−rank})` drift**, and in doing so **catches an arithmetic error in the index**: the index states a rank-4 Pati-Salam extension "would carry exponent 6/5," but Savale's exact formula `1+(p−1)/(3p+1)` at `p=rank=4` gives **16/13 ≈ 1.2308, not 6/5 = 1.2** (Sage-exact; §III). This is precisely the factor-hiding the synthesis discipline exists to surface.

**Spot-verifications performed (Sage MCP, exact)**: SU(3) rank-2 Weyl exponent `1+(2−1)/(3·2+1) = 8/7 = 1.142857…` ✓ (matches index); SU(2) rank-1 limit → exponent `1` = Hörmander `O(h)` recovered ✓; choir-number comparison `3^{1/6}=1.20094 > 2^{1/4}=1.18921` ✓ (matches index); Pati-Salam rank-4 → `16/13`, index's `6/5` flagged as approximation/error.

---

## II. Key Results

### 1. CF28 pole-structure: simple-pole assumption is load-bearing and already in force; papers 06+10 certify it and bound where it breaks

**Result**: The framework's higher-`s` residue observables are well-defined **iff** the dimension-spectrum poles at those `s` are **simple** (clean residue, not `t^{n/2}log t`). This is a structural prerequisite the framework already relies on (CM-1995 §5, `S_d={0,2,4,6,8}` at d=8), not a new hypothesis. **GEOMETRIC.**

Connes (paper 10, Thm 1.1) computes a heat trace whose small-`t` expansion `Tr(e^{−tD²}) ∼ log(1/t)/(4√π√t) − …` carries a genuine `t^{−1/2}log(1/t)` term. The structural lesson is exact and substrate-relevant: a `t^{−s}log t` term in the heat trace is the **time-domain image of a non-simple pole** (a double pole, or a pole colliding with the trivial-zero/`Γ`-pole structure) in `ζ_D(s)`. Fucci-Stanfill (paper 06, Thm 1.1) supply the *mechanism switch*: for the Friedrichs / Friedrichs-Neumann / Neumann families the spectral zeta reduces **exactly** to a rescaled Hurwitz `ζ(s)=2^{−2s}ζ_H(2s,(1±μ±ν)/2)` with **only one simple pole** at `s=1/2`; but the general self-adjoint extension `T_{0,ν}^{(α,β)}` injects **logarithmic branch points at every nonpositive integer `s=−j`** plus parameter-dependent branch points. Log structure is therefore **not generic — it is switched on by boundary-condition/self-adjoint-extension data**, and the clean form is the special (Friedrichs) case.

The mapping to the substrate is direct. `D_K` on the **closed** SU(3) (no boundary) has a canonical self-adjoint extension; this is the Friedrichs-class, clean-simple-pole case papers 06+10 certify. The framework's residue formula `a_n = Res[Tr(D_K^{−2s}); s=(d−n)/2] = Σ_k m_k λ_k^{−(d−n)}` (CM-1995 §III.4) is only an SDW-moment extractor **at a simple pole**; at a non-simple pole the "residue" is a log-coefficient, not a clean curvature integral. The CF28 caution is therefore real and now literature-anchored: any **boundary-condition modification** — an APS-type condition at a transit/white-hole surface, or a topological defect at the fold `τ_fold=0.19` — could in principle inject log terms at nonpositive-integer or coincident-root `s`, exactly the Class-8.7 degenerate-observable concern. The `μ+ν=1` Hurwitz-parameter-collision locus (where `ζ` drops to `ζ_R`) is the analytic template for the substrate's pole-degeneracy/coincident-root structure.

**Critical distinction the synthesis must hold (and the index conflates):** shell-sum **convergence** (`Re(s)` threshold for `Tr(D_K^{−2s})` to converge as a Dirichlet series — the SU(4)_PS `s>9/2` result) is a **different question** from **pole order** (whether the analytic continuation has a simple vs non-simple pole at that `s`). The s=5/s=6 SU(4)_PS work (S94/S95) established convergence and produced finite residues; it did **not** by itself certify that the pole at s=6 is simple in the substrate's own (SU(3), not PS) dimension spectrum, nor classify s=7. The Connes/Fucci-Stanfill mechanism is exactly the missing certification: confirm the heat trace `Tr(e^{−tD_K²})` acquires **no** `t^{n/2}log t` term at the relevant order before treating the higher-`s` residue as a clean SDW moment.

### 2. Asymptotic-not-convergent: Connes' factorial divergence canonizes the S45-S46 heat-kernel-tier lesson

**Result**: The Seeley-DeWitt series is **asymptotic, not convergent** — `a_n` diverges factorially. This is the framework's S45-S46 validity-tier lesson made canonical by the literal Connes mechanism. **GEOMETRIC.**

Connes (paper 10, Thm 1.1) gives the coefficients in closed Bernoulli/Euler form, `a_{2k}=−¼Γ(k+½)E(2k)/(√π(2k)!)`, and notes `E(2k)/(2k)! ∼ (−1)^k 2^{2k}(4/π)π^{−2k}` — the series **diverges like a factorial**, an asymptotic series only. This IS the framework's documented S45-S46 result (agent memory: "the Seeley-DeWitt series is ASYMPTOTIC"; the `ζ_D(1)=2776.17 ≠ a_2^{SD}=0.728` factor-3812 discrepancy at the d=8 `s=1` pole, and the truncation-divergence of analytic torsion, are the framework's instances). Connes' explicit factorial bound supplies what the framework's tier lesson lacked: a **template for the optimal-truncation order** beyond which adding SDW terms stops improving the approximation. This bounds the framework's L_max-truncation heat-kernel tiers from the continuum side — the SDW tower is reliable only up to the order where the factorial growth of `a_n` overtakes the `t^{n/2}` (or `Λ^{−n}`) suppression.

Connes' intro also explicitly names **compact Lie groups with bi-invariant metric** as the canonical closed-form heat-kernel class ("heat kernel expressed as a sum over the group's representations … explicit because of representation theory"), alongside flat tori, spheres, symmetric spaces. This **certifies the framework's Peter-Weyl heat trace** `K(t)=Σ_ρ d_ρ e^{−t C_ρ}` is the well-understood class — and connects to paper 01 (Lai-Teh), the actual closed-form SU(3) realization.

### 3. τ=0 anchor stack: Lai-Teh cubic-point spectrum as the mandatory bi-invariant reduction test

**Result**: At the bi-invariant point the substrate's Dirac operator collapses (Kostant cubic point `t=1/3`) to `D²_{1/3}=1⊗Cas+3`, eigenvalue `λ(p,q)=p²+q²+pq` with multiplicity `2p²q²(p+q)²`. This is the **mandatory τ=0 reduction target** for every Jensen-deformed eigenvalue computation. **GEOMETRIC.**

Lai-Teh (paper 01) compute the full SU(3) Dirac spectrum for the torsion family `∇^t = ∇^0 + t[X,·]` (`t=1/2` Levi-Civita, `t=1/3` Kostant cubic, `t=0/1` trivializations), with `D²_t = (π⊗1)[(1−3t)(1⊗Cas+Cas⊗1−ΔCas)+1⊗Cas+9t²|ρ|²]`. The cubic collapse to `1⊗Cas+3` IS the framework's block-diagonal Peter-Weyl structure `D_K=⊕_{(p,q)}D_{(p,q)}` on `V_{(p,q)}⊗ℂ^{16}` (canonical: `lambda_{(p,q)}=C_2(p,q)/R²`). Canonical cross-check already on record: at τ=0 the eigenvalues are algebraic `λ²=n/36` with `R_{(30,0)}=0.633√330+0.555=12.05` (atlas-spectral-geometer-collab). The spectral-action expansion is a **4-term `Λ⁸/Λ⁶/Λ⁴/Λ²` polynomial** — the d=8 SDW tower `a_0/a_2/a_4/a_6` in disguise — with the `(3t−1)(3t−2)` torsion-twist that **vanishes at the cubic point** `t=1/3` (and `t=2/3`). Substrate-first: `D_K` eigenvalues → `Λ⁸` volume (`a_0`), `Λ⁶`, `Λ⁴` (Yang-Mills), `Λ²` → emergent physics.

The framework angle is a reduction *test*, not a new claim: any framework computation of the Jensen-deformed SU(3) spectrum must recover `λ(p,q)=p²+q²+pq`, mult `2p²q²(p+q)²`, and the 4-term polynomial at `t=1/3`. The `t`-dependence of the off-diagonal `ΔCas` term is the **analytic template for the τ-moduli deformation** (how the off-diagonal block coupling turns on away from the bi-invariant point).

### 4. Volume-rigidity: TT-constraint ⇒ first-order eigenvalue stationarity; asymmetry is second-order (the d=3 template for [J,D_K]=0 and η=0)

**Result**: First-order eigenvalue shift under metric perturbation depends **only on the volume increment**; volume-preserving deformation pins `λ^{(1)}=0`; spectral asymmetry (the η-invariant) is structurally a **second-order** effect. **GEOMETRIC.**

Fang-Levitin-Vassiliev (paper 02) prove for the d=3 massless Dirac (round S³ spectrum `±(k+½)`, lowest `±3/2` doubly degenerate): (i) eigenvalues **cannot split** under any metric perturbation because the d=3 Dirac operator commutes with antilinear charge conjugation, forcing even multiplicity; (ii) **Thm 2.1**: `λ^{(1)}_±=∓(1/4π²)V^{(1)}`, `V^{(1)}=½∫h_{αβ}g_0^{αβ}ρ_0 dy` the volume increment, with clean ratio `λ^{(1)}/λ^{(0)}=−⅓·V^{(1)}/V^{(0)}`; (iii) **spectral asymmetry appears only at second order** (Thm 2.2).

This is the d=3 incarnation of two PROVEN structural results. The charge-conjugation even-multiplicity/no-splitting theorem is the d=3 instance of `[J,D_K]=0` (CPT; five independent proofs, S25) and the EXACT `η(D_K)=0` / spectral-flow=0 result (canonical: `eta_pair_err 2.22e-14`; five proofs incl. BDI winding ν=0). Theorem 2.1 is the analytic reason the framework's **volume-preserving Jensen TT-deformation** kills first-order eigenvalue motion: TT constraint ⇒ `V^{(1)}=0` ⇒ `λ^{(1)}=0` ⇒ the bottom eigenvalues are **first-order-rigid**, any nonzero leading τ-motion lives at second order. Critically, FLV makes the η/asymmetry being second-order a **theorem with a mechanism** (under a real-structure-commuting Dirac operator asymmetry cannot be first-order), which the framework's `η=0` exact result is consistent with — `η` is structurally a second-order object, identically zero here by the additional BDI/conjugate-sector bijection. The generalized Berger spheres (squashed S³, Hopf-fibre rescaling, closed-form in FLV Appendices F/§6) are an **independent deformation family** to benchmark the SU(3) bi-axial U(2)-invariant deformation against for whether asymmetry onsets at first or second order.

### 5. Lauret SU(2): closed-form λ₁ with multiplicity-jump crossings + spectral RIGIDITY — the rank-1 prototype

**Result**: For every left-invariant SU(2) metric `g_{(a,b,c)}` (a≥b≥c>0), `λ₁=min{a²+b²+c², 4(b²+c²)}`, multiplicity **4 / 7 / 3** across the crossing `a²+b²+c² = 4(b²+c²)`; and the Laplace spectrum **determines the metric up to isometry** (spectral rigidity on the entire left-invariant moduli). **GEOMETRIC.**

Lauret (paper 09, Thm 1.1) gives the explicit min-form. Verified limiting cases (Sage-checked in the index): round S³ `a=b=c=1` → `λ₁=min(3,8)=3`, mult 4; Berger family `b=c=1` → crossing at `a²=6` where mult jumps **4→7→3**. The min-over-branches structure with multiplicity reorganization at the crossing IS the framework's eigenvalue-crossing / avoided-crossing / multiplicity-reorganization structure under deformation — the **direct rank-1 prototype for the B1/B2/B3 trajectory catalog** and the τ-moduli anticrossing-swap at `δ_τ_crit` (Level-2 moduli-deformation substrate-IS, per phononic-framing). The diameter-eigenvalue estimate is the SU(2) instance of the Cheeger/comparison-bound family the framework uses (Lichnerowicz, Friedrich-Kirchberg). The **rigidity** result (consequence (ii)) is the **encouraging non-abelian analog** for the framework's claim that the SU(3) Dirac spectrum reconstructs the Jensen-deformed geometry: unlike flat tori (paper 04), the non-abelian left-invariant deformation manifold is spectrally rigid.

### 6. Isospectrality boundary: flat-tori triplet (♭₆≥3) vs SU(2) rigidity — the spectrum-alone-vs-full-triple line

**Result**: There exists a triplet of mutually isospectral, non-isometric, **irreducible** flat 6-tori (♭₆≥3); choir numbers are supermultiplicative (`♭_{m+n}≥♭_m♭_n`) and grow super-polynomially but finitely. The spectrum **alone** cannot fix the geometry for abelian quotients; SU(2) (non-abelian) is rigid. **GEOMETRIC.**

Mårdby-Rowlett-Rydell (paper 04) construct the first such triplet via linear-code weight-distribution matching (Conway: equal weight distributions ⇒ isospectral lattices; Laplace spectrum = `4π²·{dual-lattice squared lengths}`). This directly cites Wolpert 1978 — the framework's isospectral-counterexample anchor (Milnor / Gordon-Webb-Wolpert lineage). The structural lesson for the substrate is sharp and bears on the N3 axiom status: the framework's geometry reconstruction **must invoke the FULL spectral triple (`A`, `J`, `D_K`), not the eigenvalue list alone** — exactly the gap Connes' reconstruction (commutative spectral triple, 5 axioms) closes by adding the algebra. Canonical record confirms the framework operates this way: the spectral-triple-axioms entry (N3) is `BROKEN at bare-axiom level + WEDDERBURN-FROBENIUS RESCUE STAGE-3-PERMANENT` (axiom 5 orientability fails for `D_total` at 4.0, rescued via the algebra structure) — i.e. the *eigenvalue data alone is known not to suffice*, the algebra `A_K=ℂ⊕ℍ⊕M_3(ℂ)` is load-bearing. The choir-number supermultiplicativity `♭_{m+n}≥♭_m♭_n` is the analytic warning that **product/direct-sum constructions proliferate isospectral ambiguity** — a caution for any M4×K product-spectrum claim where the K-spectrum alone might not pin K (`♭_{6n}≥3^n`, Sage-verified `3^{1/6}>2^{1/4}`). The pairing with paper 09 draws the line precisely: **abelian (torus) → spectrum under-determines geometry; non-abelian (SU(2), and the conjecture to test for SU(3)) → rigid**.

### 7. Non-minimal a₂̂: is the framework's a₄ hierarchy computed in the correct (non-Laplace-type) scheme for the TT/vector sectors?

**Result**: For non-minimal second-order operators (principal symbol NOT ∝ identity — the TT-graviton and Yang-Mills vector sectors), `tr â₂` carries parameter-dependent `(Ric², R², Riem²)` corrections beyond the Laplace-type Gilkey form. **GEOMETRIC.**

Sauro (paper 05) derives the local `tr â₂` for general non-minimal operators via the Barvinsky-Vilkovisky `Tr log F̂ = Tr log D̂ + Tr log(1+D̂^{−1}Ŷ)` split (principal-degenerate part + endomorphism part). The worked torsion/transverse example (Eq. 82): `tr â₂ ⊃ −[b²/4(2+b)²]R_{αβ}R^{αβ} + [(b−8)b/24(2+b)²]R² + [b²/8(2+b)²]R_{αβμν}R^{αβμν}`, with the transverse-mode degeneracy at `b=−2` (principal-symbol kernel jump, propagating only transverse modes). This is the heat-kernel machinery for the framework's non-Laplace-type fiber sectors: the standard a₄ (d=8) SDW computation assumes minimal (Laplace-type, principal symbol ∝ `𝟙`) operators, but the **TT-graviton** (volume-preserving Jensen deformation imposes exactly the TT constraint; the gauge-fixing parameter isolating transverse modes is this non-minimality parameter) and the **gauge-vector** sector are non-minimal. The BV `Tr-log` split maps onto the framework's separation of the geometric (a₂/a₄ curvature) sector from the endomorphism `E` (gauge-field-strength `F²`, Higgs potential) sector in the spectral action.

**Canonical caveat (tempers the gate's leverage):** the framework already carries a non-minimal correction estimate — `δa_4/a_4 = −3.4×10^{−4}` (session-35 C-FINAL-5, "heat-kernel factorization exact on a finite Peter-Weyl sector; expansion terminates, no asymptotic remainder"). At d=8 with `K/(20R)<2%` for all U(2)-invariant SU(3) metrics (agent memory), the simplified scalar-only a₂ is valid to 1.3% at fold; the non-minimal correction to a₄ is `O(10^{−4})`. So Sauro is a **verification/sharpening gate, not a regime-changing correction** — but it provides the explicit `b²/(2+b)²` parameter-dependent template to check the TT/vector-sector contributions are in the right scheme, and to cross-check the transverse-mode count at the degeneracy point against the TT-Lichnerowicz tally (canonical bosonic fiber: 1 scalar + 8 vector + 35 TT = 44 at τ=0; agent memory: 31 singlet + 81-per-sector non-LI all positive).

### 8. CDT spectral-dimension flow: compare ONLY at the same diffusion window (already done S92 at the fold window)

**Result**: CDT (Ambjørn-Loll) measures `D_s(σ)=4.02−119/(54+σ)` on the **same observable** (heat-kernel return probability) the framework uses — flowing `4` (IR) → `≈1.82` (UV). The fair comparison is window-locked; the framework's substrate-vs-CDT comparison was performed S92 at the fold window `σ_*=1.4005 M_KK^{−2}`. **PHONONIC** (return-probability / spectral-support reorganization).

Ambjørn-Loll (paper 07, §5.1) define `D_s` exactly as the framework does, via `∂_σ K_g=Δ_g K_g` and the return-probability log-derivative; for a smooth manifold `D_s=d`. The Monte-Carlo `D_s(σ)=4.02−119/(54+σ)` (N₄=360,000) extrapolates to `4` in the IR and `≈1.82` in the UV — both near `~2` UV / `~4` IR. The framework computes `d_s(σ)=−2 d(ln P)/d(ln σ)` from `P(σ)=Tr e^{−σD_K²}`. **Canonical correction to the index AND to my own prior memory:** the index asserts "framework flows 4.00 IR → 1.71 UV, resolved S80"; the canonical record does **not** carry a 1.71 UV figure. What canonical actually says: the **σ→0 (UV) Weyl asymptotic is `lim_{σ→0} d_s(σ)=8`** on the 8-dimensional SU(3) fiber (standard Weyl/manifold dimension; Phononic-Penrose-Diagrams.md, S52 decomposition `d_s^{total}=d_s^{M4}+d_s^{SU(3)}`), and the deep-UV `d_s→0` (NOT a reduction to ~2) is a **Type-I_∞ finite-truncation artifact** (agent memory: "d_s in UV → 0 not 8"). The actual substrate↔CDT comparison is the **S92 ad-hoc workshop** evaluating `Φ[P_{D_K}](σ_*)` vs `Φ[P_CDT](σ_CDT-window)` at the matched fold window `σ_*=1.4005`. Per the S80 / cross-pillar-bridge-corpus §24 K=2 discipline: `lim_{σ→0} d_s` (Weyl manifold dimension) and `d_s(σ_*)` (windowed) are **distinct functionals** of the same `P(σ)`; comparing the substrate's σ→0 asymptotic to CDT's intermediate-window fit is a category error. The discriminator is the **energy-axis DOS exponent `γ_E`**, with the impedance constraint as a consistency check — and the framework must NOT claim agreement on the σ→0 asymptotic where the two functionals diverge. The framework is **not** a CDT-style path integral over triangulations (one fixed spectral triple); its dimensional flow is **kinematic** (spectral-support reorganization), CDT's is **dynamical** — canonical `d_s as CC probe CLOSED (kinematic, not dynamical)`, S56.

### 9. kNN level-spacing surmise: sharpened Ordered-Veil integrability falsifier

**Result**: A corrected k-th-nearest-neighbor (kNN) spacing surmise distinguishes integrable (Berry-Tabor Poisson, no repulsion) from chaotic (Bohigas-Giannoni-Schmit / Wigner-RMT, level repulsion) spectra more accurately than the naive Wigner-kNN generalization. **PHONONIC** (fabric integrability / GGE non-thermalization).

Shir-Martinez-Azcona-Chenu (paper 08) frame the two governing conjectures explicitly and supply a variance-corrected kNN surmise validated against RMT data and the disordered XXZ chain. This is the quantitative tool for the framework's **Ordered-Veil** claim (the GGE relic never thermalizes because the fabric dynamics are INTEGRABLE — Berry-Tabor, Richardson-Gaudin). Canonical integrability state is the **CHAOS-1 gate** (S38/S46/S61/S73): single-particle `⟨r⟩=0.321` (sub-Poisson), multi-cell `r_pooled=0.422` — Poisson `≈0.386`, GUE `≈0.603`, **both readings integrable-leaning, DIAGNOSTIC: ORDERED**. The paper supplies the **sharpened falsifier**: a deterministic, massively-Weyl-degenerate Lie-group Dirac spectrum should show Poisson (or super-Poisson) NN **and kNN** spacings — level repulsion at **any** k would falsify integrability/non-thermalization, while Poisson at all k confirms it. **Load-bearing caveat the paper itself raises (and that the synthesis must carry):** classically-integrable systems WITH symmetry can deviate from naive Poisson (harmonic-oscillator example); the SU(3) spectrum is maximally symmetric (Weyl-group + Peter-Weyl degeneracies), so statistics MUST be **symmetry-resolved** (unfold within fixed (p,q)/symmetry sectors) before reading the spacing distribution. The current canonical `⟨r⟩=0.321` being **sub-Poisson** is itself the symptom of unresolved degeneracy — the kNN surmise is the tool to test the integrability claim cleanly inside resolved sectors. The three RMT universality classes (GOE/GUE/GSE) keyed to symmetry map onto the framework's AZ classification (BDI).

---

## III. Gate Verdicts

No new gates are decided in this literature-adjudication pass. The table records the **canonical state** of the framework gates each paper bears on (authoritative per knowledge MCP), plus the two **arithmetic spot-checks** this synthesis performed.

| Gate / Check | Canonical Verdict | Decisive Number |
|:-----|:--------|:----------------|
| CHAOS-1 (integrability / Ordered-Veil) | DIAGNOSTIC: ORDERED (S38/46/61/73) | `⟨r⟩=0.321` sub-Poisson; `r_pooled=0.422` |
| η(D_K)=0 (spectral asymmetry / flow) | PROVEN EXACT (five proofs, S25) | `eta_pair_err 2.22e-14`; BDI winding ν=0 |
| FK / Lichnerowicz bound at fold | SATISFIED (S52) | `λ₁=0.8197411121`, `λ₁²=0.6719754908` |
| SU(4)_PS Mellin residue s=6 (FWD-C4 §VII.BE) | convergent, canonical (S95) | `residue_s6_PS_Linf=0.0009393639575775` |
| F_2-class anti-correlation s=5/s=6 (§W12-148) | PASS-both, EXACT (S88) | `|ρ_S(s=5)+1|=|ρ_S(s=6)+1|=0.0` |
| d_s vs CDT (window-locked) | done S92 at fold window | `σ_*=1.4005`; σ→0 Weyl gives `d_s=8` |
| N3 spectral-triple axioms | BROKEN bare + WEDDERBURN RESCUE STAGE-3-PERMANENT | axiom-5 orientability fails 4.0, rescued |
| Savale SU(3) rank-2 Weyl exponent (spot-check) | Sage-exact ✓ matches index | `1+(p−1)/(3p+1)=8/7=1.142857…` |
| Savale Pati-Salam rank-4 exponent (spot-check) | index "6/5" FLAGGED — exact is 16/13 | `16/13=1.23077` vs index `6/5=1.2` |

---

## IV. Structural Implications

**CF28 is re-scoped, not opened.** The index's framing ("classify s∈{5,6,7} simple BEFORE Pillar-VII registration") is partly behind canonical: s=5 and s=6 are already convergent and carry residue evaluations (SU(4)_PS, S95) and F_2-class PASSes (S88). The genuinely-open items are (a) **s=7** (no convergence/residue/pole-order on record), and (b) — the deeper one papers 06+10 expose — whether the convergent higher-s residues sit at **simple** poles in the substrate's own dimension spectrum, since **convergence of the Dirichlet series does not certify pole order**. The framework's entire SDW-residue machinery (`a_n=Res[Tr D^{−2s}]`) is a simple-pole construction (CM-1995 §5, which the framework explicitly cites); a non-simple pole at higher `s` would mean the "residue" is a log-coefficient and the Pillar-VII entry would be a degenerate-observable (Class-8.7). Papers 06+10 turn this from a vague worry into a **decidable pre-flight check**: inspect `Tr(e^{−tD_K²})` for `t^{n/2}log t` terms at the order in question (Connes' Bernoulli/Euler template + the `N(E)↔heat-trace` link, Thm 5.1). The substrate is closed (no boundary), so the **expectation is clean simple poles** (Friedrichs class) — but the check must be run, not assumed, before any s=7 registration, and must be re-run if any APS/transit-surface boundary or fold-defect is introduced.

**The truncation envelopes now have a rigorous continuum floor.** Savale Cor. 3 gives the bi-invariant-SU(3) Weyl remainder `O(h^{8/7})`, with rank entering through the recurrence-set volume `ν(S)=O(ε^{p−1}T^p)`, `p=rank`. This is the **analytic origin** of the R-Protection `O(L^{−rank})` drift (R-protected SDW ratios drift as `L^{−2}` for SU(3); agent memory). The continuum statement (rank-2 ⇒ exponent 8/7, infinite Ehrenfest time because the integrable group-action geodesic flow has no chaotic recurrence) is the rigorous reason the framework's spectral sums converge with **rank-power** (not exponential-chaotic) remainders — directly tying paper 03 to the framework's proven Berry-Tabor / Richardson-Gaudin integrability. The L^{−α} envelopes on SDW moments and Mellin residues should inherit `8/7` as their continuum floor. **The index's "rank-4 → 6/5" is wrong**: the exact formula gives `16/13`; any Pati-Salam R-Protection drift mapping must use `16/13`, not `6/5` (a 2.6% error that would propagate into convergence-rate claims).

**The reconstruction boundary is mapped.** Paper 04 (abelian tori, ♭₆≥3, spectrum under-determines) and paper 09 (non-abelian SU(2), rigid, spectrum determines) bracket the framework's situation. The framework's reconstruction is on the **rigid (non-abelian) side**, but — consistent with the N3 axiom status — only with the **full triple**, never the eigenvalue list alone. The open structural question is the rank-2 generalization: does the SU(3) representation-theoretic eigenvalue structure (`λ(p,q)=p²+q²+pq` with Weyl multiplicities) admit isospectral non-isometric Jensen deformations, or is it rigid like SU(2)? The choir-number/theta-series machinery (paper 04) is the exact tool to probe this. The supermultiplicativity warning `♭_{m+n}≥♭_m♭_n` flags the M4×K product structure as a place where K-spectrum alone might not pin K.

**Non-minimal heat-kernel scheme is a verification, not a revision.** Sauro (paper 05) is the correct machinery for the TT/vector non-Laplace-type sectors, but canonical already bounds the correction at `δa_4/a_4=−3.4×10^{−4}` (finite-Peter-Weyl-sector factorization is exact, expansion terminates). The gate is real (confirm the TT-graviton/vector contributions are in the right scheme and the transverse-mode count matches the TT-Lichnerowicz tally) but low-leverage at the precision the framework currently quotes.

**Integrability falsifier is sharpened and the symmetry-resolution gap is named.** The kNN surmise (paper 08) is the refined Ordered-Veil test; the current `⟨r⟩=0.321` sub-Poisson value is itself a symptom of **unresolved degeneracy**, which the symmetry-resolved kNN analysis is built to fix. Poisson-at-all-k inside resolved (p,q) sectors confirms integrability; repulsion at any k falsifies it.

**Nothing here closes or breaks a wall.** All findings are organizational (papers map onto existing PROVEN structure) or candidate-validation angles (new compute corridors). No constraint-surface region is eliminated by this pass.

---

## V. Carry-Forward Computations

**V.1. CF28 simple-pole pre-flight: heat-trace log-term scan at s=7 (and re-certify s=5/s=6)**
   - **What**: Compute the small-`t` expansion of `Tr(e^{−σ D_K²})` on the SU(3) (NOT SU(4)_PS) finite spectral triple and test for a `t^{n/2}log t` term at the orders mapping to substrate-distance poles `s=5,6,7` (i.e. curvature grades `n=d−2s`). Per Connes (paper 10, Thm 1.1 + Thm 5.1): a log term ⇔ non-simple pole ⇔ residue is a log-coefficient, not a clean SDW moment. Use the Mellin transform `M[Tr e^{−tD²}](s/2)/Γ(s/2)=ζ_D(s)` and inspect pole order at each `s` via the Peter-Weyl `K(t)=Σ_{(p,q)} dim(p,q) e^{−t C_2(p,q)}` closed form.
   - **Inputs**: `D_K` Peter-Weyl spectrum cache (`s84_spectrum_cache_L12_tau019.npz`); `C_2(p,q)` Casimir formula; `tau_fold=0.19`; CM-1995 §III.4 residue formula; canonical `S_d={0,2,4,6,8}` (lizzi-spectral-functional). Regulator tag MANDATORY per regulator-pin-discipline (e.g. `a_n^{Mellin}` / `a_n^{ζ}`).
   - **Gate**: NEW gate `S100-CF28-SIMPLE-POLE-PREFLIGHT`. PASS = no `t^{n/2}log t` term at s∈{5,6,7} (poles simple, residues clean → s=7 Pillar-VII registration eligible); FAIL = log term present at some s (non-simple pole → Class-8.7 degenerate-observable, residue ill-defined, registration BLOCKED with remediation). INFO = s=7 shell-sum non-convergent (residue requires analytic continuation; pole-order check deferred to off-pole-Hankel contour per S86-MELLIN-CONE-RESIDUE-INFRASTRUCTURE).
   - **Effort**: 3-4 hours, 1 agent session (closed-form Peter-Weyl trace + Sage symbolic pole-order extraction).

**V.2. Savale continuum-floor cross-check: empirical L^{−α} envelope exponent vs rank-2 `8/7`**
   - **What**: Fit the empirical truncation-convergence exponent `α` of a representative R-protected SDW ratio (`R_n=a_0 a_{2n}/a_n²`) across `L_max∈{5,…,12}` and compare to the Savale rank-2 continuum floor. The R-Protection drift is `O(L^{−rank})=O(L^{−2})`; the Weyl-remainder exponent is `8/7`. Establish which exponent the empirical ratio-drift inherits and whether the two are consistent (they govern different objects: eigenvalue-density remainder vs ratio-of-moments drift — write the substitution chain before asserting any direction).
   - **Inputs**: SDW moment caches at `L_max=5..12`; `residue`/moment npz outputs; Savale exponent `1+(p−1)/(3p+1)=8/7` (Sage-exact, this session); R-Protection Theorem (S76, agent memory). For any Pati-Salam extension use `16/13` (Sage-exact, this session — NOT the index's `6/5`).
   - **Gate**: NEW gate `S100-WEYL-FLOOR-RPROTECT-XCHK`. PASS = empirical `α` consistent with the rank-controlled continuum floor (within the pre-asymptotic band documented at L=3-9, a_0 effective 5.23 vs asymptotic 8); INFO = empirical `α` differs because the ratio-drift and density-remainder are distinct functionals (document the multiplicative-normalization structure per math-scripts.md K=3); FAIL = empirical `α` violates the rank-2 floor (would indicate a computation error, since 8/7 is a rigorous lower-rate bound for integrable-flow compact groups).
   - **Effort**: 2-3 hours, 1 agent session (log-log regression on existing caches + substitution-chain write-up).

**V.3. Lai-Teh τ=0 bi-invariant reduction regression test**
   - **What**: Verify the Jensen-deformed `D_K` eigensolver reduces at `τ=0` to the Lai-Teh cubic-point spectrum: `λ(p,q)=p²+q²+pq` with multiplicity `2p²q²(p+q)²`, and that the spectral action recovers the 4-term `Λ⁸/Λ⁶/Λ⁴/Λ²` polynomial with the `(3t−1)(3t−2)` torsion-twist vanishing at `t=1/3`. This is a mandatory unit test, not a discovery gate.
   - **Inputs**: Jensen-deformation eigensolver (`dirac_spectrum.get_irrep(p,q)`); Lai-Teh Thm 2.2 closed form (paper 01); Weyl dimension `dim V_{(p,q)}=½(p+1)(q+1)(p+q+2)`; metric normalization `(ρ,ρ)=3`, `(λ₁,λ₁)=1`; canonical τ=0 algebraic check `R_{(30,0)}=12.05` (atlas-spectral-geometer-collab).
   - **Gate**: NEW gate `S100-TAU0-LAITEH-REDUCTION`. PASS = eigenvalues + multiplicities + 4-term polynomial match Lai-Teh to machine ε at τ=0; FAIL = any mismatch (indicates eigensolver or normalization bug — must fix before trusting any τ>0 deformation output).
   - **Effort**: 2 hours, 1 agent session (regression test against published closed form).

**V.4. Symmetry-resolved kNN spacing: Ordered-Veil integrability falsifier inside (p,q) sectors**
   - **What**: Compute symmetry-unfolded NN **and** kNN (k=1,2,3) level-spacing distributions of the bottom-N `D_K` eigenvalues **inside fixed Peter-Weyl (p,q) sectors** (removing the Weyl/Peter-Weyl degeneracy the paper-08 caveat flags), and test against Berry-Tabor Poisson vs the corrected Wigner-kNN surmise. The current canonical `⟨r⟩=0.321` is sub-Poisson precisely because degeneracy is unresolved; resolved-sector statistics are the clean test.
   - **Inputs**: `D_K` spectrum cache; (p,q)-sector labels; corrected kNN surmise variance form (paper 08, Shir et al. — closed-form coefficients marked INCOMPLETE in index, must extract from arXiv 2504.20134 HTML); CHAOS-1 baseline (`⟨r⟩=0.321`, Poisson `0.386`, GUE `0.603`); AZ class BDI.
   - **Gate**: feeds the integrability/Ordered-Veil falsifier (extends CHAOS-1). NEW sub-gate `S100-KNN-ORDERED-VEIL`. PASS (confirms integrability) = Poisson (or super-Poisson) spacing at all k inside resolved sectors, no repulsion; FAIL (falsifies non-thermalization) = level repulsion at any k; INFO = residual super-Poisson clustering attributable to remaining accidental degeneracy (document the symmetry-resolution residual).
   - **Effort**: 3-4 hours, 1 agent session (sector-resolved unfolding + kNN surmise fit; +1 hour to extract the exact surmise coefficients from the source).

**V.5. Non-minimal a₂̂ scheme verification for TT-graviton + vector sectors**
   - **What**: Recompute the TT-graviton and Yang-Mills vector-sector `tr â₂` contribution using Sauro's non-minimal master formula (Eq. 67/69) rather than the Laplace-type Gilkey form, and compare to the canonical `δa_4/a_4=−3.4×10^{−4}` estimate. Verify the transverse-mode count at the non-minimality degeneracy (`b=−2`) matches the TT-Lichnerowicz tally (1 scalar + 8 vector + 35 TT = 44 at τ=0; 31 singlet + 81-per-sector non-LI).
   - **Inputs**: Sauro Eq. 82 + master â₂ (paper 05, arXiv 2508.09017 — general closed form marked INCOMPLETE in index, must extract from source); canonical `δa_4/a_4=−3.4×10^{−4}` (session-35 C-FINAL-5); TT mode tally (session-20b-paasch-collab; agent memory); `K/(20R)<2%` bound; a₄ regulator tag MANDATORY (`a_4^{ζ}` / `a_4^{Mellin}`).
   - **Gate**: NEW gate `S100-NONMINIMAL-A4-SCHEME`. PASS = non-minimal `tr â₂` correction consistent with the canonical `O(10^{−4})` estimate AND transverse-mode count matches TT tally; INFO = correction is `O(10^{−4})` and below current quoted precision (low-leverage, as anticipated); FAIL = non-minimal scheme produces a correction `≥1%` that the Laplace-type computation missed (would revise the a₄ hierarchy).
   - **Effort**: 4-5 hours, 1 agent session (extract Sauro master formula + specialize to SU(3) TT/vector bundle + Sage cross-check).

**V.6. SU(3) spectral-rigidity probe vs flat-torus isospectral machinery (rank-2 reconstruction boundary)**
   - **What**: Test whether the Jensen-deformed SU(3) Dirac spectrum admits isospectral non-isometric deformations (like abelian flat tori, ♭₆≥3) or is spectrally rigid (like non-abelian SU(2), paper 09). Use the representation-number/theta-series matching machinery (paper 04) on the `λ(p,q)=p²+q²+pq` + Weyl-multiplicity structure: search for distinct (a,b,c)-type U(2)-invariant Jensen parameters giving identical full spectra.
   - **Inputs**: Jensen-deformed eigenvalue + multiplicity generator; flat-torus choir-number / weight-distribution machinery (paper 04, Mårdby-Rowlett-Rydell); Lauret SU(2) rigidity template (paper 09); N3 axiom status (full-triple reconstruction, `atlas-04-assumptions`); volume-preserving TT constraint.
   - **Gate**: feeds the reconstruction-boundary question. NEW gate `S100-SU3-SPECTRAL-RIGIDITY`. PASS (rigidity, encouraging) = no isospectral non-isometric Jensen deformations found (SU(3) behaves like SU(2), spectrum + full triple determine geometry); INFO = spectrum alone admits ambiguity but the full triple (A_K + J) resolves it (consistent with N3 WEDDERBURN-rescue status); FAIL (caution) = isospectral non-isometric Jensen deformations exist even with the full triple (would weaken the substrate-reconstruction claim and require the algebra to do more work).
   - **Effort**: 5-6 hours, 1 agent session (theta-series/representation-number matching is the heaviest item; may need a dedicated number-theory pass).

**V.7. Berger-sphere second-order asymmetry benchmark for the η=0 first-order rigidity claim**
   - **What**: Use the FLV generalized-Berger-sphere closed forms (paper 02, Appendices F/§6) as an independent squashed-fibre deformation family to benchmark whether the framework's bottom-N `D_K` eigenvalues are first-order-stationary under volume-preserving deformation and whether spectral asymmetry onsets at second order (as FLV Thm 2.1/2.2 require for a real-structure-commuting Dirac operator), consistent with the exact `η(D_K)=0`.
   - **Inputs**: FLV Berger-sphere spectrum (paper 02 — explicit λ^{(2)} form marked INCOMPLETE in index beyond "asymmetry second-order"; extract from arXiv 1605.08589); SU(3) bi-axial U(2)-invariant Jensen deformation; volume-preserving TT constraint; canonical `η(D_K)=0` (`eta_pair_err 2.22e-14`); `[J,D_K]=0` (five proofs).
   - **Gate**: cross-check on the `[J,D_K]=0` / `η=0` structural pair (does NOT re-decide them — they are PROVEN). NEW diagnostic `S100-BERGER-ASYMMETRY-ORDER`. PASS = first-order eigenvalue motion vanishes under volume-preserving deformation AND asymmetry is second-order (substrate matches the d=3 FLV template); INFO = first-order motion vanishes but second-order asymmetry is identically zero (the additional BDI/conjugate-sector bijection makes η=0 to all orders, stronger than FLV's generic second-order).
   - **Effort**: 2-3 hours, 1 agent session (benchmark against an independent closed-form deformation family).

---

## VI. Summary Table

| # | Result | Classification | Status | Implication |
|:--|:-------|:---------------|:-------|:------------|
| 1 | CF28: simple-pole assumption load-bearing & in force; 06+10 certify it (Friedrichs=clean) + bound the break (boundary/defect → log) | GEOMETRIC | Re-scoped (not opened); s=5/6 already convergent, s=7 + pole-order open | V.1 pre-flight before any s=7 registration |
| 2 | SDW series asymptotic-not-convergent; `a_n` diverges factorially (Connes Bernoulli/Euler) | GEOMETRIC | Canonical (S45-46), now literature-anchored | Bounds optimal-truncation order on L_max tiers |
| 3 | Lai-Teh cubic point `D²=1⊗Cas+3`, `λ=p²+q²+pq`, mult `2p²q²(p+q)²` | GEOMETRIC | τ=0 reduction target | V.3 mandatory eigensolver unit test |
| 4 | TT-constraint ⇒ `λ^{(1)}=0`; asymmetry second-order (FLV d=3) | GEOMETRIC | Consistent w/ PROVEN η=0, [J,D_K]=0 | V.7 Berger-sphere benchmark |
| 5 | Lauret SU(2): closed-form λ₁, mult-jump 4/7/3, spectral RIGID | GEOMETRIC | rank-1 prototype | B1/B2/B3 crossing-catalog template |
| 6 | Flat-tori triplet ♭₆≥3 (abelian under-determines) vs SU(2) rigid | GEOMETRIC | Reconstruction boundary mapped | Full-triple needed (consistent w/ N3 rescue); V.6 |
| 7 | Non-minimal `tr â₂` for TT/vector sectors (Sauro Eq.82) | GEOMETRIC | Verification gate; canonical `δa_4/a_4=−3.4e−4` | V.5 low-leverage scheme check |
| 8 | CDT `D_s(σ)=4.02−119/(54+σ)`; window-locked comparison done S92 | PHONONIC | Canonical (S92 `σ_*=1.4005`); index "1.71 UV" not on record (σ→0 gives 8) | Compare only at matched window (corpus §24) |
| 9 | Corrected kNN surmise; sharpened Ordered-Veil integrability falsifier | PHONONIC | Canonical CHAOS-1 ORDERED (`⟨r⟩=0.321`) | V.4 symmetry-resolved kNN |
| — | Savale rank-2 SU(3) Weyl exponent (spot-check) | GEOMETRIC | Sage-exact `8/7` ✓ matches index | Continuum floor for L^{−α} envelopes |
| — | Savale Pati-Salam rank-4 exponent (spot-check) | GEOMETRIC | index `6/5` FLAGGED; exact `16/13` | Use 16/13 in any PS R-Protection drift map |

---

## VII. Conflicts & Corrections Flagged (for downstream resolution)

1. **Index vs Savale exact formula (arithmetic)**: index §03 states rank-4 Pati-Salam Weyl exponent "6/5"; the exact Savale formula `1+(p−1)/(3p+1)` at p=4 gives **16/13 ≈ 1.2308** (Sage-exact, this session). `6/5=1.2` is an approximation, not the value. Downstream R-Protection drift maps for any rank-4 extension MUST use 16/13.

2. **Index vs canonical d_s flow (value provenance)**: index §07 and my own prior agent memory assert the framework "flows 4.00 IR → 1.71 UV." The canonical record carries NO 1.71-UV figure; it states `lim_{σ→0} d_s = 8` (Weyl manifold dimension of the 8-dim SU(3) fiber, S52/Phononic-Penrose-Diagrams) and a deep-UV `d_s→0` Type-I_∞ truncation artifact (agent memory), with the actual substrate↔CDT comparison performed S92 at the **fold window** `σ_*=1.4005` (ad-hoc workshop), not as a σ→0 asymptotic. Treat "1.71 UV" as unsourced; defer to canonical. (Agent-memory note to self: my MEMORY.md line "d_s flowing 4.00 (IR) → 1.71 (UV), resolved S80" conflates the windowed value with a UV asymptotic — flagged for correction.)

3. **Index CF28 scope vs canonical Pillar-VII state**: index §06/§10 frame s∈{5,6,7} as requiring classification "BEFORE any Pillar-VII registration." Canonical shows s=5/s=6 are already convergent (SU(4)_PS `s>9/2`, `residue_s6_PS_Linf` canonical S95) and carry F_2-class PASSes (S88 §W12-148). The accurate residual is: **s=7 is genuinely open**, and the **pole-order (simple-vs-log) question** — distinct from convergence — is open for the substrate's own (SU(3)) dimension spectrum at the higher poles. This is what V.1 addresses; the index's framing is corrected to this scope.

No source-internal gate verdicts were re-adjudicated (none present in the index — it is an idea-generator). All framework-state claims above are anchored to the knowledge MCP as cited.
