# Session 96 Synthesis: S96 Workshop-Campaign Closeout — the a(t) Frontier, the GW-Falsifier Death, and the Three Surviving Conditional Surfaces

**Date**: 2026-05-30
**Agent**: mack-cosmic-bridge (Katie Mack / Cosmic Bridge)
**Slot**: Workshop campaign Slot-3 (S-2) — combined-landscape CLOSEOUT for the S97 planner
**Source Documents**:
- `sessions/archive/session-96/workshops/w1-q-omega-band-divergence.md` (W-1: q_Ω-band A/B/C + C1 a(t)/§6.3 status)
- `sessions/archive/session-96/workshops/w2-fi-vs-rd-demarcation.md` (W-2: FI/RD demarcation predicate + §8.5 / JACOBSON-NONLOCAL-64 qualifiers)
- `sessions/archive/session-96/workshops/w3-omega-gw-acoustic-spectral-shape.md` (W-3: Ω_GW(f) shape + LISA-band + §7.2 flagship disposition)
- `sessions/archive/session-96/workshops/w4-d2-cosmogenesis-monist-vs-two-stage.md` (W-4: §5.3 wording + hot-BB stage)
- `sessions/archive/session-96/workshops/w5-d3-rank1-vs-rank2-covariance.md` (W-5: borrowed-H covariance rank + §7.3 multiplicativity rule)
- `sessions/archive/session-96/session-96-neutrino-detection-specialist-synthesis.md` (S-1: §0 seesaw scoping + CF-S97-W4-YUKAWA-FAMILY)
- `sessions/archive/session-96/session-96-w{1,2,4,6,7}-workingpaper.md`; `computations/session-96/s96_gate_verdicts.txt`

---

## I. Session Outcome

The S96 workshop campaign converged five Slot-2 workshops + one Slot-1 solo onto a single structural truth: **the framework's open frontiers are now sharp, and almost every one of them passes through the same upstream object — the substrate→FRW `a(t)` map.** Three standing surfaces resolved to a *conditional* status keyed to a named, pre-registered S97 gate, and one resolved to a *death*: the §7.2 GW-detector flagship is dead UNCONDITIONALLY (no causal IR slope and no normalization revives it — W-3), with the substrate's acoustic falsifier migrating intact from GW detectors to large-scale structure. The campaign produced NO new contradictions; it sharpened the constraint map by localizing scattered shortfalls onto pre-registered discriminating gates.

The single most consequential cross-surface finding: W-1 down-tagged the SF54 q_Ω deceleration band from a *standing falsifier* to a *proxy-conditional artifact* (B+C jointly ≈0.95, A-falsified), and in doing so identified one missing object — the order-parameter↔acoustic conformal profile `Ω(τ) = √(ρ_s/a₂)` — that simultaneously gates (i) the SF54 frame-comparison, (ii) the C1 effective-Friedmann `a(t)` trajectory, and (iii) the LRD halo-MF cash-out. The cooling-budget→κ pin (W-4) is an independent second route to the SAME `M_KK⁻¹→s` normalization knob. The a(t) gap is therefore not a peripheral incompleteness; it is the load-bearing hub of the surviving frontier, and S97's highest-leverage compute is the cluster that attacks it.

Per `feedback_reporting-framing.md`: no session-aggregate PASS/FAIL ratio, no master gate. Per-surface structural positions only, below.

---

## II. Key Results

### II.1 The combined a(t) / C1 landscape — the SF54 q_Ω band is a proxy-conditional artifact, and the a(t) status cascades into three other surfaces

**Result**: SF54 q_Ω band `[−0.97,+0.81]` re-classified *standing falsifier → conformal-frame-conditional / order-parameter-space proxy artifact*; A-reading (genuine back-reaction deficit) FALSIFIED; the robust C1 deliverable is the frame-invariant H²* triangulation ≤0.04% across three routes. **GEOMETRIC** (substrate spectral-moment readout; the deceleration is a second-derivative functional of the emergent metric, not a phononic excitation).

W-1 adjudicated the q_Ω-band divergence (SCALE-FACTOR-54's `+0.81` vs the routes' native ranges) to **B+C jointly (≈0.95), A ≈ 0.03** (residual 0.02), both agents (transit, volovik) converged. The structural facts:

- **A (genuine closure gap) is FALSIFIED by the data.** The only route-invariant observable, H²*, PASSes: route-1 AOFT `7.478844e-03`, route-2 Volovik `7.476023e-03`, route-3 GFT `7.478844e-03`, agreeing to ≤0.04% (the route-invariant Bogoliubov sum `ρ_relic = 26.553854` is shared by all three). With no invariant shortfall, there is nothing to attribute to a back-reaction deficit. (`S96-W1-AOFT-FRIEDMANN-MAP` PASS `audit edfe1f7f24ab6146…`; `S96-W1-VOLOVIK-2FLUID` FAIL `65c41afd…`; `S96-W1-GFT-FRIEDMANN` INFO `e2364cf5…`.)
- **B (frame mismatch) + C (route-dependence) are the metric-shadow and partition-shadow of ONE substrate inequality `ρ_s(τ)/a₂(τ) ≠ const`.** The SF54 band is the q-range of the order-parameter-space (Connes-distance / coherence-length) proxy R2, NOT the acoustic `a_eff` a comoving phonon clocks (phonons propagate on `g_M = a₂`-metric; `λ ∝ a_eff`; `G_N = M_KK²/(16π·a₂)`). The two proxies are conformally AND *layer*-distinct (`proxies_conformally_distinct=True`, `Omega_BA_fold=2.241353`, S95-W4-4 INFO). q_Ω is FUNCTIONAL-DEPENDENT (partition-keyed in `x=ρ_s/ρ_n`), the cosmological twin of `S96-SDW-EFT-CONTROL` (same D_K spectrum → Gaussian FAIL / Mellin PASS / a-ratio-driver INFO) — it fails the `O = w(param)·g(K)` multiplicative-cancellation premise because ρ_s and ρ_n are independent thermodynamic functions.
- **The +½ acoustic ceiling is a microscopic FEATURE, INSIDE band-tol of +0.81.** The two-fluid supremum is `q_Ω(x→0)=+½` EXACT; `+0.81 − ½ = 31/100 = 0.31 < band_tol = 89/250 = 0.356` (Sage-exact). The FAIL lives entirely in the unphysical `x→0` (vacuum-removed) tail; at the physical fold `x_fold=85.79` the deviation is `dev_fold=0.1887 < band_tol`. The relic EoS is bounded `w_n ∈ [−0.4076, 0]` because the Parker amplitude `|β_k|²` peaks on the flat-`∂E/∂k` van Hove modes (`τ_fold=0.190` IS the van Hove singularity) — a frozen integrable relic is a zero-/negative-pressure gas by construction. `x(τ)` is strictly monotone-increasing post-fold (`d ln x/d ln a = 3(1+w_n) > 0` for all admissible `w_n`, Sage-exact), so the band-reproduction regime is causally unreachable from the fold forward.

**The cascade into the other three surfaces is the load-bearing closeout finding.** The W-1 a(t) status is the upstream node for:
- **GW falsifier (W-3)**: the same open `M_KK⁻¹→s` knob κ blocks the CGWB peak placement (`S96-OBS-CGWB-PEAK-FREQ`). But — see II.2 — the GW-detector flagship dies *independent* of κ; the a(t) gap is NOT what kills it.
- **LRD cash-out (W-4)**: the LRD halo-MF is blocked by the SAME a(t)→t(z) absolute-normalization gap under BOTH the monist and two-stage readings (`halo_mf_blocked_by_at_gap=True`), so the W-4 cosmogenesis verdict is observationally moot until C1 closes.
- **Borrowed-H structure (W-5)**: the dagger-row covariance re-couples through the borrowed-H map, the SAME undelivered effective-Friedmann projection as the §6.3 a(t) gap (Atlas D04 C2). The §7.3 dagger correction is conditional precisely on this gap.

One missing object — `Ω(τ) = √(ρ_s/a₂)` — closes BOTH the SF54 frame-comparison (W-1 leg-ii) and the C1 a(t) trajectory (E2 in W-1): the historical SF54 `a(τ)` was computed in the order-parameter frame; the physical trajectory needs the acoustic frame; `Ω(τ)` is the bridge, `≠const` because the relic depletes ρ_s relative to a₂. **The §6.3 / Atlas D04 down-tag was staged by W-1's final-turn writer (volovik) for the capstone designated writer** (BEFORE/AFTER prose: SF54 band → "conformal-frame-conditional / proxy-conditional artifact"; frame-identity `a_eff` settled, trajectory pending the Ω(τ)-profile gate). Capstone-hygiene Q1 (a(t)/§6.3) fires — routed to housekeeping §A, designated-writer reviewed patch.

### II.2 The GW-falsifier survival verdict — the §7.2 LISA flagship is dead UNCONDITIONALLY; the acoustic falsifier migrates GW → LSS

**Result**: NO a(t)-independent LISA GW signature survives D4 (peak OR amplitude). The §7.2 acoustic-CGWB GW-detector flagship is RETIRED; the substrate's acoustic falsifier is alive but in galaxy surveys (first-sound BAO ring SNR 8.6 DESI-5yr; f·σ₈ suppression), not GW detectors. **PHONONIC** (the CGWB is the acoustic readout of the van Hove fold DOS transduced into the tensor sector).

W-3 (little-red-dots ∧ mack — I was the §7.2/inventory sole-writer in that workshop) resolved the −127.4 OOM contradiction between the peak placement and the LISA-band amplitude. Both legs are gone:

- **Peak FREQUENCY**: `f_obs = 8.4835e39 Hz` at κ_nat (`S96-OBS-CGWB-PEAK-FREQ` FAIL `audit 646e6ad087dae644…`, verdict line 155), 43.93 decades above the LISA low edge, GHz+ across the entire swept κ band [1e-20,1e-10] (121/121 points). Reaching LISA needs κ ≈ 25 s/M_KK⁻¹ (42.5 OOM from natural, 11.4 OOM beyond the band). The redshift factor `a_fold/a_now = 0.472291` is κ-independent (pure ratio), so this is normalization-robust.
- **LISA-band AMPLITUDE**: the historical `Omega_GW_Lambda_A_LISA = 1e-10` was a Case-A object — a *provenance-less pivot placeholder* ("OOM estimate AT LISA 3 mHz", `get_constant` → "No PROVENANCE entry") under an ASSUMED peak-at-3-mHz, the exact assumption W6-3 refuted. Under the resolved shape the LISA-band tail is `Ω_peak·(f_LISA/f_peak)^p` ≪ LISA-PLS. (`S96-OBS-OMEGAGW-GGE-VS-ZN` PASS `audit a9998118fdcb96bd…`, line 136 — the amplitude leg re-opened.)

**The death is UNCONDITIONAL — slope-robust AND κ-robust.** Even the shallowest physically-admissible causal IR exponent `p=1` puts the LISA tail 29.45 OOM below LISA-PLS (with Ω_peak normalized to the GW-energy ceiling `Ω_peak ≤ O(1)`); every steeper p (including any van-Hove-DOS enhancement) goes deeper. So §7.2's death does NOT wait on the S97 slope derivation — there is no causal p that revives the LISA flagship. The triple (pivot value 1e-10, slope p=3, peak frequency 8.4835e39 Hz) is mutually inconsistent: read forward it is the −127.4 OOM contradiction; read backward (propagating 1e-10 up an f³ rise to the peak) it gives `Ω_peak ~ 10^117`, violating the GW-energy bound by 117 OOM. So `1e-10` was never the peak height; it is the casualty.

**What survives.** Two structurally-orthogonal legs of the W6-4 gate are untouched: (i) the wall channel `Ω_GW^{walls} = 0` EXACTLY (π₀(U(1))=0, topological, regulator-invariant), and (ii) the (A)/(C) regulator-class split `47.080974 OOM` (Sage-exact, `Ω_GW^(C) = 8.299e-58` fully provenanced) — but this is a split between two LISA-sterile numbers, a regulator-class *separation* statement, never a *detectability* statement. The acoustic falsifier migrates intact to LSS: the **first-sound BAO ring** `A_FS = 0.204 = c₂²/c₁²` at `k₁ = 0.0193 Mpc⁻¹`, SNR 8.6341 at DESI-5yr / 5.0789 at DESI-DR1 (`S96-OBS-FIRST-SOUND-RING` PASS `audit b74ccd561136efc5…`, σ_exp FETCHED arXiv:2411.19738v2, zero free parameters, no ΛCDM counterpart), and **f·σ₈ suppression** (`S96-OBS-FSIGMA8-FORECAST` INFO `318df6edeadb6214…`, −4.06% product suppression, S₈-relieving, forward-edge 1.0σ DESI-5yr).

**This is the §7.2 surface I re-stated in W-3 as sole writer** — landed as `falsifier-master-inventory.md` Row #7.audit-3 (CGWB GW-detector-flagship RETIREMENT; amplitude leg superseded; falsifier migrates GW→LSS; supersession audit pin `646e6ad0…`). The constant is flagged `PENDING-SUBSTRATE-RECOMPUTE` (not retired — the IR-tail amplitude is a real substrate observable; not left at 1e-10 — 127 OOM inconsistent). §7.2 is now **normalization-conditional only in the narrow sense that the exact re-pin VALUE awaits S97**; the detector-sterility verdict itself is unconditional. Capstone-hygiene Q2 (§7 falsifier surface, my sole-writer domain) fired and was effected in W-3.

### II.3 The §7.3 joint-BF admissibility — multiply iff (algebraically) AND (statistically) independent; borrowed-H dagger rows collapse to one d.o.f. by default

**Result**: the §7.3 joint-Bayes-factor product is admissible ONLY over the zero-parameter spine {m_H, normal ν-ordering, σ/m=0, c_s²=0} (Tier-1, unconditional `BF_spine` floor); the borrowed-H dagger rows {w₀, wₐ, σ₈, CC} collapse to ONE effective d.o.f. by default (rank-1 substrate-forced); the within-layer pair {Ω_DM, σ₈} (both a₂) is never multiplied. **GEOMETRIC** (the partition is over spectral-moment channels a₀/a₂/a₄ and their borrowed-H projection).

W-5 (sagan ∧ mack — I owned CF-S97-D3-BF / CF-MACK-7) resolved the rank-1 vs rank-≥2 borrowed-H covariance question. The W7-7a covariance `Cov = s·sᵀ·Var(δH)` is rank-1 by construction, so `Corr(res_i,res_j) = sign(s_i·s_j)` with BOTH magnitudes cancelled — `Corr(a₀,a₂) = +1.0000` FORCED (same-sign sensitivities `s_a0=+1.234526e-02`, `s_a2=+6.398917e+00`; `S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE` FAIL `audit 7227c8c5dc6d4fbd…`, line 159). The 518× kernel disparity (`s_a2/s_a0 = 518.33`) is a DEAD lever — it cancels in rank-1 (`sign`) AND in rank-≥2 (`Corr = 1/√((1+r₀)(1+r₂))` depends on the variance mix r ALONE, no s-dependence).

**The §7.3 multiplicativity admissibility rule** (G1∧G2, 3-tier partition) — which I landed as the capstone §7.3 register-of-record in W-5 (`S96-HYG-JOINT-EVIDENCE-D3-RESTRICT` PASS `audit 588adb147d9ac240…`, line 171):

| Tier | Observables | G1 (algebraic, Wronskian W∝R_K′³≠0) | G2 (statistical, no shared knob) | Multiply? |
|:-----|:------------|:------------------------------------|:---------------------------------|:----------|
| **Tier-1 — spine** | m_H (a₄), normal ν-ordering, σ/m=0, c_s²=0 | YES (distinct substrate-IS structures) | YES (NO borrowed H) | **YES** — `BF_spine` floor |
| **Tier-2 — within-layer** | Ω_DM, σ₈ (both a₂) | NO (same a₂ moment) | — (moot) | **NO** |
| **Tier-3 — borrowed-H dagger** | w₀, wₐ, σ₈, CC | YES (a₀/a₂ Wronskian-independent) | NO (`Corr(a₀,a₂)=+1` at ρ_shared=1) | **collapse to 1 d.o.f.** |

The {a₀,a₂} dagger pair is admissible as a correlation-discounted joint factor (`Δlog BF = (1−|Corr|)·b_a0`) ONLY on a pinned `r ≥ 1` (⇔ `|Corr| < 0.5` ⇔ σ_Hprivate ≥ σ_Hshared) WITH demonstrated per-pipeline independence. At the knife-edge `r ~ O(1)` the burden to multiply is NOT met: the plausible pin range `σ_Hi/H ∈ [0.03,0.06]` gives `|Corr| ∈ [0.4098, 0.7353]`, straddling the 0.5 edge with 3 of 4 sample points on the rank-1 (collapse) side. The do-not-multiply default HOLDS pending CF-S97-D3-BF. **`BF_spine` is the framework's defensible joint-evidence headline regardless of the Tier-3 disposition** — the dagger recovery is a bounded LABORATORY-IN correction (`≤ b_a0`, the weaker dagger row), never a competitor. The CF-S97-D3-BF input-covariance disposition is the two-BF deliverable with the threshold `0.5` FIXED before compute (CITED — see §V; the W-5 addendum is already in the W7 WP CF). Capstone-hygiene Q3 (PROSE status change) + Q4 (prose vs ledger) fired and were effected in W-5.

### II.4 The matter-sector scoping — §0 seesaw decomposes; mass axis settled, Yukawa axis open

**Result**: §0 "no seesaw" / "no external seesaw parameter" decomposes onto two orthogonal axes — mass axis SETTLED (RH Majorana scale is internal: M_R are D_K eigenvalues), Yukawa axis OPEN (Y_3/Y_2 not yet D_K-derived). Final §0 wording: "no external *mass* parameter; Yukawa structure OPEN pending S97." **PARTICLE.**

The S-1 solo (neutrino-detection-specialist) adjudicated the D5 divergence. On the **mass-scale axis**, reading (A) is final: `M_R = {1.004, 1.079, 1.170} M_KK` ARE D_K B-branch fold eigenvalues (2/3 strict-1% PASS, all <2% against the L_max=12 cache), so the seesaw imports no external mass parameter (`S96-MATTER-SEESAW-D5` INFO `audit e58ecfba0895d798…`, value `2.2016`, verdict line 86). On the **Yukawa axis**, neither reading is settled: the `R_seesaw=31.57` vs `R_direct=9.86` divergence (`S96-MATTER-R-HIERARCHY` FAIL `audit 8e1de47dd3b129c9…`, line 72) is carried entirely by `(Y_3/Y_2)⁴`, which the framework does not yet derive — the a₄ Yukawa block is non-empty but OOM-only (`R_Yuk=1.588`). The divergence IS the residual Yukawa freedom made numerical.

The clean falsifiable sub-result, independent of the Yukawa axis: `δ_CP ∈ {0,π}` EXACT (from real M_R / `[J,D_K]=0`), a DUNE/Hyper-K target — high-σ exclusion of both CP-conserving values would falsify the real-M_R structure. **This is a §7 falsifier-surface companion in my domain** (with the m_ββ = 4.96–8.27 meV 0νββ row); the S-1 solo staged it as a recommendation, NOT edited. **Caveat the solo flagged (and I ratify):** the parameter-free R for any §7 row is `R_direct = 9.86` (FAIL, 3.4× below NuFit), NEVER the back-solved `R_seesaw = 31.57` — the latter's NuFit-proximity is an artifact of the S60 back-solve, not an independent prediction.

### II.5 The FI/RD demarcation predicate and which FI-survivor claims need the W-2 qualifier

**Result**: a regulator move falsifies an FI-survivor claim iff it passes a gated conjunction DP-0 ∧ DP-1 ∧ DP-2; the §8.5 "ratios survive truncation" headline is replaced by a three-stratum ladder, of which only Tier-1 (f-free) is unqualified. **GEOMETRIC** (spectral-moment ratios + their regulator-atlas reweighting).

W-2 (lizzi ∧ volovik) built the demarcation predicate, evaluated in order: **DP-0** (admissibility — is regulator R a UV-only, IR-preserving detector for THIS observable? PV is INADMISSIBLE for the IR-accumulation a₀ channel); **DP-1** (transversality, indexed by the *named* verdict — does R move the observable transverse or collinear to that verdict's band boundary?); **DP-2** (FI mechanism, tier-2 only — empirical-FI `mult_cancellation=False` is STRONGER evidence than structural-cancellation-FI, which is Phi-trivial). The §8.5 three-stratum ladder:

- **Tier-1 (f-free)** — a-ratio driver `a_{2(k+1)}/a_{2k}` (max 0.6808<1, INCREASING), E7 tree slope, degree-grading, no-saddle topology: UNQUALIFIED. This is the genuine §8.5 spine.
- **Tier-2 (f-invariant-within-family)** — `a_2/a_0 = 0.431082` and any Seeley-DeWitt moment ratio: FI-WITHIN the analytic-continuation family (empirical, `mult_cancellation=False`), RD-ACROSS the PV family (36.86%) but PV INADMISSIBLE per DP-0; Gilkey-normalization object-hygiene PENDING CF-S97-W2-1. (`S96-SDW-CC-GAP` INFO `audit da899b4da558ec00…`; `S96-SDW-WRONSKIAN-FI` `3dd0235a…`; `S96-SDW-SADDLE-REGINV` `1e32d548…`; `S96-SDW-EFT-CONTROL` `74158937…` supersedes `7d62a904…`.)
- **q-flow (C10-conditional)** — the CC closure `ρ_vac/ρ_obs = 1.032`: equilibrium floor `ρ_Λ=0` EXACT and representative-independent (S95 EQUILIBRIUM-CC-WARRANT), but the OBSERVED departure conditional-on-C10 (`ρ_vac~M_Pl²H²`, ASSUMED-PARTIALLY-PROVEN). **JACOBSON-NONLOCAL-64 status**: "located" is unqualified on BOTH axes (a₀ residue locates the CC on the regulator atlas; the equilibrium theorem locates ρ_Λ=0 exactly on the q-flow axis); "solved" is q-flow-DP-0-conditional (rests on C10).

**Which FI-survivor registry claims need the within-family/across-PV qualifier**: any §8.5 claim citing a Seeley-DeWitt moment ratio (Tier-2) — principally `a_2/a_0` (the §8.6 CC-ratio object). The unqualified "ratios survive truncation" is FALSE; the family-scoped version is TRUE. Tier-1 (f-free) claims do NOT need the qualifier. The CC closure (`ρ_vac/ρ_obs`) needs the q-flow-C10 qualifier, NOT the regulator-atlas family-scope qualifier — they are different qualifiers on different axes. This is methodology-floor / registry-corpus territory (NOT the §7 falsifier surface); landed by W-2's writers into `cross-pillar-bridge-corpus.md` / `pru-class-corpus.md`, not my domain to re-edit.

---

## III. Gate Verdicts

(Source-doc verdicts are authoritative; not re-adjudicated. This closeout integrates them. Full 64-hex audit SHAs in §II.)

| Gate | Verdict | Decisive Number | Surface |
|:-----|:--------|:----------------|:--------|
| S96-W1-AOFT-FRIEDMANN-MAP | PASS | H²*=7.478844e-03 (route-1); ≤0.04% cross-route | a(t)/C1 |
| S96-W1-VOLOVIK-2FLUID | FAIL | q_max=+0.5; +½ inside band_tol (0.31<0.356) | a(t)/C1 |
| S96-W1-GFT-FRIEDMANN | INFO | q∈[−0.21,+1.24]; H²* corr=1.000 | a(t)/C1 |
| S96-OBS-LRD-ASSEMBLY-CLOCK | FAIL | no κ in [1e-20,1e-10] matches ΛCDM cosmic time | a(t)/C1 → LRD |
| S96-OBS-CGWB-PEAK-FREQ | FAIL | f_peak=8.4835e39 Hz, 43.9 dec above LISA | GW §7.2 |
| S96-OBS-OMEGAGW-GGE-VS-ZN | PASS | walls=0 EXACT; (A)/(C)=47.081 OOM; 1e-10 re-opened | GW §7.2 |
| S96-OBS-FIRST-SOUND-RING | PASS | A_FS=0.204; SNR 8.6341 (DESI-5yr) | GW→LSS |
| S96-OBS-FSIGMA8-FORECAST | INFO | −4.06% supp; 1.0σ DESI-5yr | GW→LSS |
| S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE | FAIL | Corr(a₀,a₂)=+1.0000 (rank-1 forced) | joint-BF §7.3 |
| S96-HYG-JOINT-EVIDENCE-D3-RESTRICT | PASS | spine-only; dagger conditional | joint-BF §7.3 |
| S96-MATTER-SEESAW-D5 | INFO | reldiff 2.2016; M_R 2/3 strict-1%; δ_CP∈{0,π} | matter §0 |
| S96-MATTER-R-HIERARCHY | FAIL | R_direct=9.86 (∉[17,66]) | matter §0 |
| S96-SDW-CC-GAP | INFO | a₂/a₀=0.431082; PV-shift 36.86% | §8.5 / JACOBSON |
| S96-SDW-EFT-CONTROL | INFO | a-ratio max 0.6808<1; f-ratio FAIL/PASS flip | §8.5 |

---

## IV. Structural Implications — solution-space boundary per standing surface

Each surface's resolved verdict CLOSES a specific corridor. Stated as the wall the gate maps.

**Surface 1 — a(t) / C1 (§6.3, Atlas D04 C1/C2).** *Boundary closed*: the corridor "the SF54 q_Ω band `[−0.97,+0.81]` is a hard back-reaction target whose miss is a substrate deficit" is CLOSED. q_Ω is a route/frame/partition-dependent functional, not a route-invariant observable; the only invariant (H²*) PASSes at ≤0.04%; the +½ acoustic ceiling is a microscopic FEATURE inside band_tol; the band-reproduction "reach toward +0.81" lives in an `x→0` regime the substrate never occupies (`x(τ)` monotone). *Boundary still open*: the C1 a(t) *trajectory* — frame identified (`a_eff`), trajectory pending the `Ω(τ) = √(ρ_s/a₂)` profile. C2 (`K_pivot`) UNCHANGED. The down-tag preserves the substrate-first arrow (`D_K → {a₂, ρ_s} → metrics → q_Ω` unchanged; the register tag scopes the confidence).

**Surface 2 — GW falsifier §7.2.** *Boundary closed UNCONDITIONALLY*: the corridor "the substrate radiates a LISA-detectable stochastic GW background" is CLOSED, and it stays closed across the full causal-slope grid (p≥1) and the full normalization band (κ∈[1e-20,1e-10]). No detector verdict awaits S97. *What §7.2 is now normalization-conditional ON*: only the exact IR-tail re-pin VALUE (the number the canonical store carries), not the sterility verdict. *Boundary opened (constructive)*: the substrate's acoustic falsifier is alive in LSS — BAO ring (SNR 8.6, zero-param, no ΛCDM counterpart) is the live near-term discriminant; the GW-detector instrument was wrong from the start (a source's emission scale, ~10⁴⁰ Hz, decides the instrument). §7.2 is the only surface that resolved to a *death* rather than a *conditional*.

**Surface 3 — joint-BF §7.3.** *Boundary closed*: the corridor "the framework's joint evidence is the naive product over all matching observables (w₀ × wₐ × σ₈ × CC × spine)" is CLOSED — multiplying borrowed-H dagger rows as independent double-counts the one shared cosmological H and over-states the BF. *Boundary fixed*: the admissible product is the zero-parameter spine (`BF_spine`, unconditional floor); the dagger pair {a₀,a₂} enters only as a correlation-discounted factor (`≤ b_a0`) conditional on a pinned `r≥1`. *Boundary open*: whether the per-pipeline H-systematics deliver `r≥1` AND mutual independence — both unpinned, straddling the disposition edge; CF-S97-D3-BF buys the answer.

**Surface 4 — matter §0.** *Boundary closed*: the corridor "the seesaw mass scale is an external add-on" is CLOSED (M_R-INTERNAL, 2/3 strict-1%). *Boundary held open*: the corridor "§0 may assert the *entire* seesaw is parameter-free" is held OPEN by the 2.20 divergence — the Yukawa axis is underived. The surviving §0 scoping is the intersection: mass-internal (settled) AND Yukawa-open (pending CF-S97-W4-YUKAWA-FAMILY). δ_CP∈{0,π} is the falsifiable boundary, independent of the Yukawa axis.

**Cross-surface coherence check (no conflicts found).** I checked the one place the workshops could collide: W-3's redshift chain assumes `a_fold/a_now = 0.472291` (from `a_fold/a0 = 2.117`, the SAME Connes-distance scale factor W-1 down-tagged to an order-parameter-space proxy). This is NOT a conflict: W-3 uses the *ratio* `a_fold/a_now`, which is a pure number robust to the conformal frame (the κ-independent redshift factor), whereas W-1's down-tag concerns the *deceleration* q_Ω (a second-derivative functional that is NOT a conformal invariant). The redshift ratio survives the W-1 frame ambiguity exactly as H²* does. The two workshops are consistent: both treat the Connes-distance a(τ) as a legitimate substrate-IS observable at the order-parameter layer; W-1 says its *q* is not the acoustic q, W-3 uses its *ratio* which is frame-robust. No re-adjudication needed.

---

## V. Carry-Forward Computations

**Discipline note.** Per the task: the already-queued WP CFs are CITED, not re-created — CF-S97-W1-1 (`session-96-w1-workingpaper.md`), CF-S97-W4-YUKAWA-FAMILY + CF-S97-W4-BARYOGEN-SOURCE (`w4-workingpaper.md`), CF-S97-W2-1 (`w2-workingpaper.md`), CF-S97-D3-BF (`w7-workingpaper.md`), CF-S97-FSIGMA8-FORECAST-REFETCH (`w6-workingpaper.md`), CF-S97-W3-1 (`w3-workingpaper.md`), CF-S97-DK-DF-STAGE2 (`w8-workingpaper.md`). The W-1 and W-3 *workshops* also already pre-registered their own CFs (CF-S97-W1-OMEGA-PROFILE, CF-S97-W1-QOMEGA-ROUTE-INVARIANCE, CF-S97-W1-XTODAY; CF-S97-OMEGAGW-PEAK-HEIGHT, CF-S97-OMEGAGW-ACOUSTIC-SPECTRAL-SHAPE). The entries below are the **campaign-level integrative items the cross-surface read newly surfaces** — they do NOT duplicate the per-workshop CFs; they pin the *sequencing* and the *cross-surface joins* the individual workshops could not see in isolation.

```
V.1. a(t)-cluster sequencing spec — the upstream dependency the S97 planner must honor
   - What: enforce the wave-order Ω(τ)-profile → {SF54 frame-comparison (B-leg), C1 a(t) trajectory,
       cooling-budget κ-pin} so the shared upstream object (the M_KK⁻¹→s knob κ + the Ω(τ)=√(ρ_s/a₂) profile)
       is computed ONCE and consumed by all three downstream surfaces. The Ω(τ)-profile gate
       (CF-S97-W1-OMEGA-PROFILE, W-1 workshop) and the cooling-budget κ-pin (CF-S97-COOLING-BUDGET-KAPPA-PIN,
       W-4 workshop) are TWO INDEPENDENT ROUTES to the same κ — run both; cross-check κ_implied (cooling budget)
       against κ_nat=8.86e-42 AND against the W6-5 swept band, and against the Ω(τ)-profile's H²(τ*) anchor.
   - Inputs: CF-S97-W1-OMEGA-PROFILE output (Ω(τ), Ω̇, Ω̈ via √(ρ_s/a₂)); CF-S97-COOLING-BUDGET-KAPPA-PIN
       (H²(τ*)=7.478844e-03 M_KK² from S96-W1-AOFT-FRIEDMANN-MAP audit edfe1f7f…; cooling budget T_init=0.112·M_KK,
       N_e_exfl=80.89, exponent −0.8685; T_CMB=2.7255 K); M_KK_inv_seconds=8.86044e-42 s (W1-3, CLOSED);
       s54_scale_factor.npz; canonical M_KK=7.428660036284456e16.
   - Gate: feeds CF-S97-W1-1 (the C1 a(t) trajectory, existing WP CF) AND CF-S97-W1-QOMEGA-ROUTE-INVARIANCE.
       INFO-class join-gate: PASS iff the two κ routes (cooling-budget, Ω-profile-anchored) agree to
       |log10(κ_cooling/κ_Ω)| ≤ 0.5 (a genuine over-determination of κ); INFO iff they constrain but do not
       co-land κ; FAIL iff the cooling exponent −0.8685 secretly carries the seconds normalization (Class-8.7
       degenerate-observable, vacuous pin — the W-4 pre-flight check).
   - Effort: orchestration/sequencing pin (0 new compute) + the join cross-check (~0.3 wave); the underlying
       computes are the cited per-workshop CFs.

V.2. Cross-surface join: does the a(t) closure retire the §7.3 dagger-row conditionality? (EVOI flag)
   - What: test whether a substrate-derived a(t) (from V.1) replaces the borrowed-H map that induces the
       §7.3 Tier-3 rank-1 collapse. If the dagger residuals' H is a SUBSTRATE clock (not the container-observer
       H(t)), the rank-1-vs-rank-≥2 question (CF-S97-D3-BF) may be re-derivable from first principles —
       potentially retiring the whole borrowed-H caveat on the substrate side. Compute the conditional EVOI:
       does closing C1 a(t) change BF_spine+dagger relative to the borrowed-H-conditional value?
   - Inputs: CF-S97-W1-1 output (substrate a(t)); CF-S97-D3-BF two-BF deliverable (BF_spine, Δlog BF=(1−|Corr|)·b_a0);
       the W7-7a sensitivity vector (s_a0=1.234526e-02, s_a2=6.398917e+00); C10 tracking law ρ_vac~M_Pl²H².
   - Gate: new INFO-class join-gate S97-AT-RETIRES-DAGGER-CONDITIONALITY. INFO (diagnostic): emit whether the
       substrate a(t) makes the dagger covariance rank-1 by SUBSTRATE derivation (vs the current
       modeling-conditional rank-1 default). No PASS/FAIL physics threshold (it is a structural-dependency probe).
   - Effort: ~0.5 wave (depends on CF-S97-W1-1 + CF-S97-D3-BF both landing first; it is the downstream join,
       not a parallel gate). LOW priority relative to V.1 (it is a refinement once the a(t) hub closes).

V.3. §7 falsifier-surface consolidation pass (mack-cosmic-bridge sole-writer; post-S97-compute)
   - What: after the S97 a(t)-cluster + GW two-gate + matter Yukawa gates return, run ONE consolidating
       sole-writer pass over the §7 falsifier surface to (a) re-pin the §7.2 LISA-tail VALUE from the
       S97-OMEGAGW-PEAK-HEIGHT + SPECTRAL-SHAPE outputs (replacing PENDING-SUBSTRATE-RECOMPUTE), (b) land the
       δ_CP∈{0,π} + m_ββ=4.96–8.27 meV matter falsifier rows (S-1 staged, mine to write), (c) promote the
       BAO-ring + f·σ₈ LSS rows to the live-falsifier register the GW flagship vacated, (d) reconcile any
       §7.3 dagger-row status change from CF-S97-D3-BF.
   - Inputs: S97-OMEGAGW-PEAK-HEIGHT (Ω_peak) + S97-OMEGAGW-ACOUSTIC-SPECTRAL-SHAPE (p, LISA-tail value);
       CF-S97-W4-YUKAWA-FAMILY verdict (sets §0 final wording); CF-S97-D3-BF (dagger disposition);
       falsifier-master-inventory.md Rows #7.audit-3, #70, #71, #73.
   - Gate: NOT a physics gate — a sole-writer consolidation per the canonical write-order Step 3 + capstone-hygiene
       Q2 routing. Deliverable: the §7 surface with every S96-conditional row resolved to its S97 value.
   - Effort: ~0.5 wave (sole-writer registry pass; gated on the S97 compute outputs above; NO new physics).
```

---

## VI. Summary Table

| # | Surface | Resolved Verdict | Classification | Status | Solution-space boundary CLOSED |
|:--|:--------|:-----------------|:---------------|:-------|:-------------------------------|
| 1 | a(t) / C1 (§6.3, Atlas D04) | SF54 q_Ω band → proxy-conditional artifact; A-falsified; B+C≈0.95; H²* ≤0.04% robust | GEOMETRIC | CONDITIONAL — trajectory pending Ω(τ)=√(ρ_s/a₂) | "SF54 +0.81 is a hard back-reaction target whose miss is a substrate deficit" |
| 2 | GW falsifier §7.2 | LISA flagship DEAD unconditionally (slope+κ robust); falsifier migrates GW→LSS | PHONONIC | RETIRED (GW) / LIVE (LSS: BAO ring SNR 8.6) | "the substrate radiates a LISA-detectable GW background" |
| 3 | joint-BF §7.3 | multiply iff G1∧G2; dagger rows collapse to 1 d.o.f.; BF_spine floor | GEOMETRIC | CONDITIONAL — dagger recovery pending pinned r≥1 (CF-S97-D3-BF) | "joint evidence is the naive product over all matching observables" |
| 4 | matter §0 | §0 decomposes; mass internal (settled), Yukawa open (pending) | PARTICLE | CONDITIONAL — Yukawa pending CF-S97-W4-YUKAWA-FAMILY | "the seesaw mass scale is an external add-on" |
| 5 | §8.5 / JACOBSON-NONLOCAL-64 | DP-0∧DP-1∧DP-2 predicate; 3-stratum ladder; only Tier-1 unqualified | GEOMETRIC | a₂/a₀ tier-2 (within-family); CC closure q-flow-C10-conditional | "ratios survive truncation (unqualified)" |

**Closeout line for the S97 planner.** Four of the five surfaces resolved to a CONDITIONAL keyed to a named S97 gate; one (the GW flagship) resolved to an unconditional death with a constructive LSS migration. The structural hub is the substrate→FRW `a(t)` map: it gates the SF54 frame-comparison, the C1 trajectory, the LRD cash-out, and (through the borrowed-H projection) the §7.3 dagger conditionality — with TWO independent routes to the one missing knob κ (the Ω(τ)=√(ρ_s/a₂) profile and the cooling-budget over-determination). Sequence the a(t)-cluster FIRST; it is the highest-leverage compute in the S97 queue, and it is the only frontier whose closure cascades across multiple surfaces at once.
