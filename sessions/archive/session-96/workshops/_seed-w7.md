# Seed — Chunk w7 (Hygiene / pins / firewall + joint-evidence — C4/C5/C8 + D3)

**Date**: 2026-05-30   **Investigator**: phonon-first-cosmologist   **Source**: S96 W7 (WP + plan-w7 + verdicts)
**Wave summary**: 9 gates — **7 PASS / 1 INFO / 1 FAIL** (post-supersession finals). W7-2/3/4/5/6/7b/8 PASS (all hygiene/firewall/registry METHODOLOGY landings — every one a Q2 fix-in-session item, already mirrored to housekeeping §A); W7-1 INFO (`f_NL −1.505` confirmed = `−max_f_NL_FW`, a one-sided saturation BOUND, not a point; central `f_NL=1.03` inside Planck 1σ; sign-convention footnote → Track B); W7-7a **FAIL** (D3 cross-layer covariance: `max_offdiag |Corr| = 1.0000` on the a₀–a₂ pair under a shared C10 borrowed-H perturbation). Structural reading: this is a hygiene wave — eight of nine gates are settled-in-place Q2 bookkeeping. The ONE substrate-physics tension is D3 (W7-7a/b): the planner pre-flagged it as a `Investigating-Workshops.md §Q1` adversarial adjudication. The compute leg (W7-7a) and the edit leg (W7-7b) BOTH landed in-session — so the *literal* algebraic-vs-statistical question is numerically settled. What survives as a genuine reading-divergence is the **rank-1 shared-H model assumption** the compute leg used, which forces `Corr ∈ {0, ±1}` by construction and thereby pre-decided the very dependence it was meant to measure. That surviving core is the workshop candidate.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

_None._ The two non-PASS verdicts (W7-1 INFO, W7-7a FAIL) do not have a single-reading audit question that one agent settles alone: W7-1 is fully self-resolved in its WP (Track B sign-convention footnote, routed to mack as a forward §7.1 recommendation — Q2 capstone-hygiene, below); W7-7a's surviving question is intrinsically two-sided (rank-1 model correct vs over-restricting) and routes to Slot 2.

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

### S2-1 — D3 joint-evidence: is the W7-7a rank-1 shared-H covariance a genuine "one borrowed-H d.o.f." collapse, or a model artifact that over-restricts the joint Bayes factor?

**Tension**: W7-7a (`S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE`, FAIL, verdict line 159) computed the cross-layer residual covariance under a **single shared scalar** δH/H perturbation, with `Cov = s·sᵀ·Var(δH)` (a rank-1 channel). For ANY two H-sensitive rows this construction returns `Corr = sign(s_i·s_j) ∈ {+1, −1}` exactly — the WP confirms it (line 600: "the Var and magnitudes cancel in the normalized correlation of a single-source rank-1 channel"). So `Corr(a₀,a₂)=+1.0000` is not a measured intermediate dependence; it is the only non-zero value the model can return. The plan's own `dual_prior` (plan-w7 lines 901–904) pre-registered an **INFO band Corr ∈ (0.1, 0.5)** — a "valid to leading order, add a hedge" middle outcome — that the rank-1 machinery made **structurally unreachable**. Two readings now compete, each with a first-principles case:
  - **Reading A (rank-1 is physically correct)**: the dagger rows (w₀, wₐ, σ₈, CC) ALL borrow the *same* container-observer H(t) (caveat C10); there is one external Hubble quantity, so the residuals collapse to ONE effective d.o.f. — `Corr=+1` is the true statement, and W7-7b's strike-the-product restriction is exactly right.
  - **Reading B (rank-1 over-restricts)**: H(t) is not consumed identically — each dagger row integrates H against a *different* kernel (growth-ODE friction `∂lnD/∂lnH=−8.01` for σ₈; the C10 two-fluid partition `∂w₀/∂lnH=−1.23e−2` for w₀; the kernels differ by ~500×), and each observable carries its OWN systematic/measurement uncertainty on H in addition to the shared cosmological value. A rank-≥2 covariance (shared-H + per-layer independent components) would yield `|Corr| < 1`, and SOME cross-layer multiplicativity would survive — so striking the product entirely over-states the loss of joint evidence. The numerical magnitude (and sign) of `s_a2 = +6.40` vs `s_a0 = +1.23e−2` (line 581) is the lever: their 500× disparity is precisely what a rank-1 model erases in the normalized correlation but a rank-≥2 model preserves.

**Why workshop, not solo**: the verdict turns on which COVARIANCE MODEL is the correct laboratory-IN statement of the borrowed-H structure — a genuine UQ/statistics adjudication where the two readings cannot both be right and the resolution is a derivation (rank-1 single-source vs rank-≥2 shared+independent), producing a structural verdict on what the §7.3 joint-BF may legitimately multiply. A single reviewer would just re-assert one reading; the cross-rebuttal (does per-layer independent-H uncertainty exist, and does it lift `|Corr|` off the rank-1 boundary?) is the content. The planner explicitly pre-flagged D3 for this stream (plan-w7 line 1222; `_consolidated-findings.md §VI`); W7-7a/W7-7b landed the compute+edit legs precisely so this adversarial leg could run against a pinned number — the number does NOT replace the workshop, it is its input.

**Agents (EXACTLY 2)**: `sagan-empiricist` (authored the §7.3 over-reach flag + the W7-7b restriction; defends Reading A — rank-1 collapse is the honest statement), `mack-cosmic-bridge` (owns the EVOI prior-predictive-range / posterior-width reframe CF-MACK-7 and the falsifier-evidence surface; presses Reading B — the spine-vs-dagger partition + a rank-≥2 borrowed-H covariance recovers partial multiplicativity, and the BF magnitude over the spine, CF-S97-D3-BF, is the operational deliverable).

**Adjudication question**: (d) — derive whether the borrowed-H residual covariance is rank-1 (single shared scalar ⇒ `Corr=±1` ⇒ dagger rows collapse to one d.o.f., joint-BF restricted to the zero-parameter spine as W7-7b applied) OR rank-≥2 (shared cosmological H + per-layer independent H-uncertainty ⇒ `|Corr|<1` ⇒ partial cross-layer multiplicativity survives among the dagger rows), and pin the resulting admissibility rule for what §7.3 may multiply.

**Rounds**: 2 default.

**Output**: a pre-registered gate for the W7-7a follow-up (rank-≥2 borrowed-H covariance: enumerate the per-layer H-kernels {∂lnD/∂lnH, ∂w₀/∂lnH, ∂(CC)/∂lnH}, pin whether each observable carries an INDEPENDENT H-uncertainty component, recompute `|Corr|` off the rank-1 boundary) AND a structural verdict on the §7.3 multiplicativity rule (which observables are co-primary borrowed-H factors that may NOT be multiplied vs which retain partial independence). Feeds CF-S97-D3-BF (the EVOI BF magnitude already queued in the W7 WP).

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **D3 joint-BF spine pulls observables from ≥4 waves.** The W7-7b "zero-parameter structural spine" (the part the joint-BF may multiply) = {Higgs mass `m_H` (a₄ KK-threshold), normal mass ordering, σ/m=0 (N_Fock=1), c_s²=0 (Kasparov)}. `m_H` and the f·σ₈ / σ₈ anchors trace to the **W6 observational wave** (W6-1 f·σ₈, W6-7 σ₈/A_s); normal mass ordering traces to the **W4 neutrino wave** (W4-3/W4-7); c_s²=0 is **W7-8** (`§VII.BH`, this wave). Any S96-D3 workshop or CF-S97-D3-BF compute MUST cite these upstream verdicts as the spine inputs — the BF is not self-contained in W7.
- **The borrowed-H dagger rows conflict with the standing a(t)/C1 effective-Friedmann gap.** W7-7a's FAIL mechanism IS the §6.3 a(t) gap surfacing in the evidence layer: the dagger rows (w₀, wₐ, σ₈, CC) borrow the container-observer H(t) because the framework has no DERIVED effective-Friedmann a(t) (caveat C10, "ASSUMED-PARTIALLY-PROVEN"). Housekeeping §D records that **W6-5/W6-3 CONFIRMED C1 is load-bearing** (already-open gap). The D3 covariance result is downstream of that same gap — flag the dependency so the consolidator does not treat D3 as independent of the C1/a(t) status.
- **W7-1 f_NL relabel needs a mack-cosmic-bridge §7.1 scorecard-row write** (cross-wave to the falsifier surface). The W7-1 WP recommends propagating the bound-vs-point correction into the capstone §7.1 scorecard ROW (line 426: `−1.505 → central 1.03 + σ-distance; relabel −1.505 as −max_f_NL_FW`); the §7-table status cell is mack's sole-writer domain. This is a Q2 capstone-hygiene forward (below), but it crosses into the §7 falsifier surface mack governs.

## Carry-forwards (route to WP CF section, NOT this schedule)

- **[Q2-hygiene]** W7-1 f_NL §7.1 scorecard-row relabel — propagate the bound-vs-point correction (central `1.03` + σ-distance `0.38σ` folded / `0.57σ` equilateral; relabel `−1.505` as the `−max_f_NL_FW` saturation bound + sign-convention footnote) into the capstone §7.1 scorecard ROW (line 426). Already a forward RECOMMENDATION in the W7-1 WP to `mack-cosmic-bridge` (sole writer of the §7 status surface); a status-tag/prose edit on an already-derived number → capstone-hygiene Q2, NOT a workshop. (Routed in housekeeping §D's W5–W8 capstone-hygiene 5-question gate as a YES on Q3/Q4 already; this is the row-level execution.)
- **[Q-other / compute-CF]** CF-S97-D3-BF — prior-predictive-range Bayes factor over the zero-parameter spine (mack CF-MACK-7). ALREADY enumerated as a 4-field spec in the W7 WP `## Carry-Forward Computations`. Single pre-registered EVOI compute (replaces "chance of one random geometry"; honors within-layer-not-multiplied + cross-layer-only discipline) → compute carry-forward to `/rclab-plan`, NOT a workshop. NOTE: if the S2-1 workshop resolves toward Reading B (rank-≥2), this CF's input covariance changes — sequence the workshop BEFORE the BF compute.
- **[Q-other / verification]** Rank-≥2 borrowed-H covariance recompute — the pre-registered gate the S2-1 workshop would PRODUCE (not a workshop itself once the workshop pins its spec). Listed here so `/rclab-plan` can pick it up if the workshop lands a gate.

## Not investigated (only if pre-compute shell)

_N/A — W7 is fully closed (housekeeping confirms W5–W8 closed 2026-05-30; all 9 W7 verdicts present on disk with finals 7 PASS / 1 INFO / 1 FAIL, supersession chains followed)._

## Wave-by-wave digest (consolidator background)

**W7 = the terminal hygiene / provenance / convention-firewall wave for the S95-era capstone.** Harvests clusters C4 (f_NL bound-vs-point), C5 (single-value-for-multi-convention: Mellin pole-set + R_K normalization + canonical-pin absences), C7 (PROVEN results omitted from the §7/§9 scorecard), C8 (surface-gravity KIND-tagging), + dissonance D3 (§7.3 joint-evidence over-reach).

- **W7-1 FNL-BOUND-VS-POINT** (INFO, gen-physicist, COMPUTE): `−1.505 = −max_f_NL_FW` exactly (residual 0.000e+00) — a one-sided `|f_NL|` SATURATION BOUND (Bogoliubov-sudden channel), NOT a point prediction. Central GGE `f_NL^total=1.03` inside Planck 1σ (`σ_dist=0.378` folded / `0.575` equilateral, reproduces registry `0.57σ`). The capstone's quoted `0.47σ` is the BOUND's distance to Planck, not a central detection. INFO (not PASS) because the folded-config σ-ordering inverts vs the magnitude-ordering (a sign-coincidence: bound shares Planck's negative sign) → documented footnote, Track B. `f_NL_total_GGE_S67=1.03` promoted to canonical. Settled Q2.
- **W7-2 CANONICAL-PINS** (PASS, gen-physicist, METHODOLOGY): 7 cited-but-unpinned constants provenance-completed — 4 NEW (`t_star=0.08832` [≠ `mellin_f_star_f0`], `R1_lizzi=1.128655`=a₀a₄/a₂², `R_therm=5251.82`, `Mass_LeggettDM_over_Delta_BCS=11.97` CONDITIONAL) + 3 BACKFILL (`tau_NEC=1.383`, `Z_fold`, `Mach_max_framework=13.75`). Plan said 5-NEW+2-backfill; `tau_NEC` was pre-existing → fixed in-session to 4-NEW+3-backfill (no INFO/CF needed). Settled Q2.
- **W7-3 MELLIN-POLESET** (PASS, lizzi, METHODOLOGY): §3.3 Mellin firewall — printed `ζ=Σ λ^{−2s}` (Conv-A double-power) has pole set `S_s={0,1,2,3,4}` in s; `{0,2,4,6,8}` is the curvature-degree grading `n=d−2s`, stated separately. `α_s` "s=3" (Conv-A, n=2, a₂) ≡ `§VII.BE` "s=6" (Conv-B, n=2, a₂) — SAME a₂ residue, `s_B/s_A=2` exact. Factor-2 corpus mislabel risk closed; `## Mellin Pole-Set Labeling` pin landed in `regulator-pin-discipline.md` (orchestrator-direct). PASS supersedes a within-dispatch INFO (verdict-script bug, capstone content byte-identical). Settled Q2.
- **W7-4 RK-FIREWALL** (PASS, baptista, METHODOLOGY): §8.2a R_K(0) 3-form firewall `{2 internal, 4 12D, 1.5 P15}` + conversion factors `{×2, ×4/3}`; `R1_lizzi=1.1286546` + the Wronskian τ=0 SIXTH-order zero + the Lichnerowicz bound all convention-invariant (`R_K→c·R_K` moves MAGNITUDE not zero-order; `c` cancels in R1). PASS supersedes two FAILs (numeric W-zero-order limit artifact → resolved by Sage symbolic certification `W_tau0_zero_order=6.000000`). Settled Q2.
- **W7-5 SELF-INVENTORY** (PASS, cosmic-web/etc, METHODOLOGY): 4 omitted PROVEN results added to §7/§9 scorecard — f·σ₈ PRODUCT suppression `−4.058%@z0.51` (vs `−0.311%` bare-f; C5 conflation guarded), normal ν ordering (`B1<B2<B3`, `τ_cross=0.107`, zero-param), c_s²=0 SCORECARD pointer (→ W7-8 registry), Ω=0 trivial-holonomy scoped "computed-invariants-trivial, NOT topology-nontrivial". Settled Q2.
- **W7-6 KIND-TAG-S53** (PASS, hawking, METHODOLOGY): §5.3 KIND table — THERMODYNAMIC-modulus (`κ_V=0` double-root V=V′=0, V″=2), GIBBONS-HAWKING-emergent (`T_GH=0.2172`), SONIC (`0.112` relabel). Three DIFFERENT surface-gravity functionals at the SAME τ=0.190 — not a contradiction. Kitaev identity `2π·T(a₄)=2π×7.578=47.614=κ_exit` (MSS chaos-bound saturation IS the analog surface gravity). Settled Q2.
- **W7-7a JOINT-EVIDENCE-D3-COVARIANCE** (**FAIL**, gen-physicist, COMPUTE): `max_offdiag |Corr|=1.0000` (a₀–a₂) under shared C10 borrowed-H. ALGEBRAIC layer-independence (Wronskian `W∝R_K′³`, S75 W2-E) ≠ STATISTICAL independence of borrowed-H residuals. Sensitivities `s_a0=+1.23e−2`, `s_a2=+6.40`, `s_a4=0` (m_H decoupled). Sign=PASS (shared-H induces POSITIVE correlation, predicted), magnitude=FAIL (1.000>0.5), regime=VALID. **Model = rank-1 single-source** `Cov=s·sᵀ·Var(δH)` ⇒ `Corr=sign(s_i·s_j)∈{0,±1}` by construction (the surviving workshop tension — S2-1).
- **W7-7b JOINT-EVIDENCE-D3-RESTRICT** (PASS, sagan, METHODOLOGY): consumed W7-7a FAIL; applied the FAIL branch to capstone §7.3 — struck the naive `a₀×a₂×a₄` dagger-row product, scoped the joint-BF to the zero-parameter spine (no borrowed-H), replaced "chance of one random geometry" with EVOI prior-predictive-range (CF-MACK-7), declared Ω_DM/σ₈ (both a₂) NOT multiplied. Edit applied; BF magnitude → CF-S97-D3-BF. Settled-in-session (text), but the COVARIANCE-MODEL question it rests on is the live S2-1 workshop.
- **W7-8 CS2-REGISTRY** (PASS, van-den-dungen, METHODOLOGY): `§VII.BH` c_s²=0 cross-pillar bridge entry (Kasparov-product factorization `[D_M]=π_!⊗[D_B]`) STAGE-3-PERMANENT — passes `_cross_pillar_bridge_audit.py` (5/5 anatomy, 3/3 tiers, OE-form). Level-1 topological zero (`m_Goldstone^{4D}=0` exactly), Level-3 `c_s²_FW=0 < 9.21e-4` Kasparov bound. Pins `c_s2_FW=0.0`, `c_s2_kasparov_bound=9.21e-4`. Settled Q2.

**Honest count**: 1 Slot-2 workshop (D3 covariance-model adjudication), 0 Slot-1 solos. Eight of nine gates are Q2 hygiene/firewall/registry landings already mirrored to housekeeping §A/§D. The single genuine adversarial-physics tension is the rank-1-vs-rank-≥2 borrowed-H covariance model underlying the D3 joint-evidence FAIL — exactly the workshop the planner pre-flagged, and one the compute leg sharpened rather than closed.
