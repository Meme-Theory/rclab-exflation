# Seed — Chunk w1 (Emergent FRW a(t) closure — C1 FLAGSHIP)

**Date**: 2026-05-30   **Investigator**: phonon-first-cosmologist   **Source**: S96 W1 (WP + plan-w1 + verdicts)
**Wave summary**: 7/7 gates closed — 3 PASS (W1-1 AOFT-FRIEDMANN-MAP, W1-3 MKK-SECONDS, W1-7 QFLOW-RESIDUAL), 3 INFO (W1-2 ONEILL-NONFLAT, W1-5 GFT-FRIEDMANN, W1-6 TAUDOT-PROFILE), 1 FAIL (W1-4 VOLOVIK-2FLUID). Structural reading: the framework's #1-converged open item (the missing emergent FRW `a(t)`) reaches its first multi-route closure. The `a(t)` EXISTS as a derived object and its MAGNITUDE (`H²*=7.478844e-03`) is robust across three independent formalisms (route-1 AOFT scalar-tensor, route-2 Volovik two-fluid, route-3 GFT condensate) to ≤0.04%. The surviving open tension is on the `q_Ω` DECELERATION BAND, where the three routes diverge: route-1 PASS, route-2 FAIL (SF54 upper edge +0.81 unreachable), route-3 INFO (74% in-band). One genuine Q1 adversarial adjudication; everything else is settled-in-session hygiene or a queued solo trajectory compute.

## Slot 1 candidates — solo reviews (`/rclab-review`)
(Q1-YES, Q1b: independent reading suffices)

_No Slot 1 candidates._ The single workshop-eligible item (the `q_Ω` cross-route divergence) is genuinely adversarial — three formalisms give three different readings of the same substrate observable that cannot all be correct, so the cross-rebuttal converges (Slot 2, S2-1 below). It does NOT decompose into an independent single-agent reading. The other W1 substance is either an in-session-closed verdict-selection fix (O'Neill Reading-A/B, already supersession-corrected) or a queued solo compute with a pre-registered gate (CF-S97-W1-1 trajectory assembly) — neither is a Slot-1 solo synthesis.

## Slot 2 candidates — workshops (`/rclab-workshop`)
(Q1-YES, Q1a: cross-rebuttal essential)

### S2-1 — The `q_Ω` deceleration-band divergence: which route reads the SCALE-FACTOR-54 band correctly (and is +0.81 the right target)?

**Tension**: Three INDEPENDENT routes to the emergent FRW `a(t)` agree on the Hubble magnitude `H²*=7.478844e-03` to ≤0.04% (route-1 AOFT W1-1 PASS `7.478844e-03`; route-2 Volovik W1-4 FAIL `7.476023e-03`; route-3 GFT W1-5 INFO `7.478844e-03`) but DIVERGE on the deceleration parameter `q_Ω(τ)` against the SCALE-FACTOR-54 band `[−0.97, +0.81]`. Route-2 (Volovik two-fluid) cannot reach the upper edge `+0.81` under EITHER equation-of-state reading (ideal `w_n=0`: `q_max=+0.50`, 82.6% cover; Volovik `w_n=−0.4076`: `q_max=−0.11`, 48.2% cover) → `magnitude=FAIL` (`max|q−q_SF54|=1.81`). Route-3 (GFT condensate) covers only 74% in-band (`max|q_GFT−q_SF54|=0.837` vs PASS ceiling `0.356`) → `magnitude=FAIL ∧ regime=MARGINAL ⇒ INFO`. Route-1 reports sign+magnitude PASS but on the `H²*` non-collapse criterion, NOT on a `q_Ω`-band reproduction. The competing readings: (A) the SF54 `+0.81` upper edge is the correct substrate target and routes 2/3 genuinely fail to source enough deceleration (a real physics gap in the back-reaction closure); (B) the SF54 band is a Connes-distance `a(τ)`-proxy result subject to the S95-W4-4 conformal ambiguity, so `+0.81` is the wrong comparison and the route-2/3 `q_Ω` ranges are the substrate-faithful answer; (C) the deceleration-band reach is an intrinsically route-dependent observable (each formalism's `q_Ω` is a DIFFERENT functional of the shared `H²*`), so no single band-reproduction verdict is well-posed across routes. These three cannot all hold — the workshop derives which from first principles.

**Why workshop, not solo**: The divergence is between three named formalism-owners who each computed a different `q_Ω` from the SAME `H²*`; the resolution requires the transit-side (scalar-tensor AOFT) and the superfluid-side (Volovik two-fluid) to respond to each other's reading of what `q_Ω` even is — whether the band is a proxy artifact, whether `+0.81` is a target or a Connes-distance ceiling, and whether `q_Ω` is a route-invariant or route-dependent functional. A single agent reading one route's verdict cannot adjudicate the cross-route claim; the convergence is genuinely adversarial. The WP itself pre-registers this as the S97 cross-route-disagreement workshop seed (`session-96-w1-workingpaper.md §"Carry-Forward Computations"` line 448).

**Agents (EXACTLY 2)**: `transit-dynamics-theorist` (owner of route-1 AOFT scalar-tensor `H²(τ)`, the band-non-collapse criterion, and the `τ̇`/`q_Ω` transit-side accounting), `volovik-superfluid-universe-theorist` (owner of route-2 two-fluid `q_Ω(x)=½(1−2x)/(1+x)`, the `w_n` EoS choice, and the SF54 deceleration-band physics)

**Adjudication question**:
- (a) Is the SCALE-FACTOR-54 `+0.81` upper edge the substrate-faithful comparison target for `q_Ω`, or is it a Connes-distance `a(τ)`-proxy ceiling subject to the S95-W4-4 conformal ambiguity (in which case route-2/3 `q_Ω` ranges are correct and the FAIL/INFO are mis-keyed)?
- (b) Is `q_Ω` a route-INVARIANT observable (all three formalisms must reproduce the same band, so the divergence is a genuine physics gap) or a route-DEPENDENT functional of the shared `H²*` (so a single band-reproduction verdict is structurally ill-posed across routes)?
- (c) Under the Volovik two-fluid, is the upper-edge `+0.81` unreachability a feature of the `w=−1`/`w=0` split itself (the GGE relic `w=0` caps deceleration at `+½`), or an artifact of the two specific `w_n` readings tested — i.e., does a substrate-derived `w_n` exist that closes the band?
- (d) Pre-register the discriminating gate: a route-invariance test (compute the same `q_Ω` functional on all three `H²*` trajectories under a fixed conformal frame) OR a conformal-frame audit (re-evaluate the SF54 band in the AOFT/GFT frame vs the Connes-distance frame) — whichever the workshop converges on as the falsifier.

**Rounds**: 2 default. (Genuine ledger-dissonance, but a 2-round R1-steelman / R2-converge structure suffices — the competing readings (A)/(B)/(C) are clearly stated; no third-round rebuttal-of-rebuttal is needed to converge on the discriminating gate.)

**Output**: A structural verdict on whether the `q_Ω`-band divergence is (A) a real back-reaction-closure gap, (B) a conformal-proxy mis-comparison, or (C) a route-dependent ill-posed comparison; a pre-registered S97 discriminating gate (route-invariance test OR conformal-frame audit) with threshold + machinery pin; and a registry-state position on the cluster-C1 `q_Ω` observable (whether the SF54 band reproduction is a standing falsifier or a proxy-conditional artifact).

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **W1 `H²*`/`q_Ω` closure depends on the §6.3 SCALE-FACTOR-54 (SF54) band + the C10 borrowed-`H(t)` caveat in the capstone.** The W1 wave's entire `q_Ω`-band comparison is against the SF54 `[−0.97,+0.81]` Connes-distance result; the housekeeping ledger (§D, W5–W8 capstone-hygiene gate) records that the C1 `a(t)` gap and the C10 caveat are the capstone's load-bearing open items (W6-5/W6-3 CONFIRMED C1 load-bearing; W8-7 3-stratum separation). The S2-1 workshop's verdict on whether SF54-`+0.81` is a faithful target vs a conformal-proxy ceiling bears directly on the capstone §6.3 / Atlas D04 C1/C2 status — flag for the consolidator: if S2-1 lands "proxy mis-comparison," the capstone §6.3 SF54 reference may need a designated-writer down-tag (a capstone-hygiene Q1/Q3 item). Both sides: W1 WP §"Wave 1 Synthesis" (three-route table) vs `session-96-housekeeping.md §D` (W5–W8 capstone-hygiene 5-question gate, Q1 a(t)/effective-Friedmann gap).

- **W1-3 `M_KK_inv_seconds` + `GeV_to_J` canonical promotions feed the §B `/weave --update` session-close batch.** Not a tension — flagging that the W1-3 PASS keystone (the dimensional normalization the CF-S97-W1-1 trajectory consumes) has a pending knowledge-index/MCP rebuild already routed to housekeeping §B. The consolidator should confirm no S97 plan gate cites `M_KK_inv_seconds` via MCP before that batch runs (script-import is already clean).

## Carry-forwards (route to investigated wave's WP CF section, NOT this schedule)

- **[Q-other]** — **CF-S97-W1-1 (already in W1 WP `## Carry-Forward Computations`)**: assemble the explicit physical-seconds `a(t)` trajectory from the now-pinned W1 closure (`H²(τ)` route-1 + `t(τ)=∫dτ/τ̇` W1-6 family + `M_KK⁻¹` seconds + `{Z_norm=5.0, V0=0}` W1-7 + W1-2 effaced cross-terms). This is a SOLO compute with a pre-registered gate (`a(t)` monotone+finite; reproduces `H²(τ*)` to rel <1e-6; τ̇-family ambiguity collapses to unique shape OR persists as 1-param band), NOT a workshop. Routes to `/rclab-plan` via the existing WP CF block — already present, no new mirror needed.

- **[Q-other]** — **W1-6 `τ̇(τ)` shape under-determination → resolved BY CF-S97-W1-1, not a separate item.** The TAUDOT-PROFILE INFO (`unique_selection=False`, all 50 admissible shapes sub-adiabatic) is pre-registered to collapse to a unique `a(t)` shape under the gate-1 trajectory integration (or persist as a 1-param band). This is subsumed by CF-S97-W1-1's gate clause, not an independent carry-forward. No separate routing.

- **[Q2-hygiene]** — **S52 legacy units bug (`s52_12d_reduction_output.txt:189`, "1.680e3 s" ≈ 47.5 OOM wrong).** Surfaced by W1-3 MKK-SECONDS as a VERIFY cross-check disagreement; documented in WP §W1-3 + Wave-1 Synthesis and superseded by the correct `8.86044e-42 s`. ALREADY routed to `session-96-housekeeping.md §B` (documented + superseded; NO edit — chronological integrity of the archived S52 artifact preserved). No new routing; listed here only to confirm it is NOT a workshop and is already in the canonical Q2 ledger.

- **[process observation]** — **No new Q2 candidate surfaced for the first time by this investigator.** Every W1 hygiene/finalization item (O'Neill Option-A supersession FAIL→INFO; Volovik `solve_ivp` fix; M_KK_inv_seconds/GeV_to_J promotions; S52 units bug; `/weave --update` batch) is already catalogued in `session-96-housekeeping.md §A/§B`. The wave-synthesis caught them all; no upstream miss to log.

## Wave-by-wave digest (consolidator background)

**W1 — Emergent FRW `a(t)` closure (cluster C1; FLAGSHIP, multi-session); 7 gates; transit-dynamics-theorist compute-owner.**

| Gate | ID | Verdict | Key result |
|:--|:--|:--|:--|
| W1-1 | S96-W1-AOFT-FRIEDMANN-MAP | **PASS** | Flagship first leg: covariant `S_eff[g_M]` → `G_eff^{μν}=8πG_eff T_relic^{μν}`; Bianchi lift K→g_M (residual 0.000e+00, noether 1/2); `H²*=7.478844e-03` matches S95-W3-3 nominal rel `5.456e-08`; non-collapse vs `a_eff` proxy reldev 11.52. `a(t)` MAGNITUDE held INFO pending {Z_norm,V0}+seconds+O'Neill (all now landed). Track A. |
| W1-2 | S96-W1-ONEILL-NONFLAT | **INFO** | Submersion base↔fiber cross-terms EXIST `O(‖F‖²)` from 0 EXACT at flat product (S61 A=T=0), monotone (`κ=4.31`), but EFFACED at physical Hubble curvature (`ratio_Hubble=6.84e-117 ≪ 3e-7`). Additive layering `S_SA=a₀−a₂+a₄` safe at cosmological scale. Verdict-selection FAIL→INFO via Option-A supersession chain (`86a0ac54→440aa6c5→487f83e2` authoritative); keyed off physical Hubble (Reading A) not unphysical ‖F‖=1 stress-test (Reading B); thresholds unchanged, lands INFO not PASS. CLOSED in-session (housekeeping §A). |
| W1-3 | S96-W1-MKK-SECONDS | **PASS** | `M_KK_inv_seconds=8.86044e-42 s` (two SI routes agree rel `2.14e-5 < 1e-4`); promoted to canonical (+`GeV_to_J`). Closes S95-W3-1 `seconds_norm_open`. S52 cross-check FAILs (47.5 OOM legacy units bug, flagged hygiene). Dimensional keystone for CF-S97-W1-1. |
| W1-4 | S96-W1-VOLOVIK-2FLUID | **FAIL** | Two-fluid (superfluid `w=−1` + GGE normal `w=0`) `q_Ω(x)=½(1−2x)/(1+x)`. `H²*=7.476023e-03` (0.04% from route-1); sign PASS (`∂q/∂x<0`, normal sources deceleration, resolves 133,200× single-fluid overwhelm); regime VALID (continuity residual `4.55e-13`). FAIL on `q_Ω` band: `+0.81` upper edge UNREACHABLE under both EoS (ideal `q_max=+0.50` 82.6%; Volovik `w_n=−0.4076` `q_max=−0.11` 48.2%). The `q_Ω`-band FAIL is the S2-1 workshop core. |
| W1-5 | S96-W1-GFT-FRIEDMANN | **INFO** | GFT condensate (Oriti) `H_GFT²=(8πG_eff/3)ρ_relic`; LQC bounce term ABSENT (asymmetric white hole). `H²*=7.478844e-03` bit-identical to route-1. `q_Ω` 74% in-band (`max|q−q_SF54|=0.837` vs ceiling `0.356`); sign PASS (`corr=1.0`), magnitude FAIL, regime MARGINAL → composite INFO. Co-anchors the S2-1 `q_Ω` divergence. |
| W1-6 | S96-W1-TAUDOT-PROFILE | **INFO** | One-parameter `τ̇(τ)` family non-empty + sub-adiabatic across the WHOLE van Hove feature (50/50 admissible, `δt/T_L<1e-2` everywhere, 62×–800× below ceiling); `unique_selection=False` → INFO. Held as 1-param family pending the CF-S97-W1-1 trajectory integration to pin the shape. |
| W1-7 | S96-W1-QFLOW-RESIDUAL | **PASS** | `{Z_norm=G_DeWitt=5.0, V0_eq=0}` both substrate-fixed ⇒ S95-W3-2 residual count `2→0`; IKKT matrix-model genre (kinetic+potential, NO third scalar); split reproduces W3-2 closed-form bit-exact (`max_rel=8.05e-16` Sage). CC enters as a SUBTRACTION not a landscape selection. Closed-form genre route to `a(t)` UNBLOCKED. |

**Flagship triangulation (the standout finding):** three independent routes to `a(t)` AGREE on `H²*` (≤0.04%) and DIVERGE on the `q_Ω` deceleration band (route-1 PASS / route-2 FAIL / route-3 INFO). The existence + magnitude of the C1 `a(t)` closure is formalism-independent (Track A across the board); the `q_Ω`-band reach is the surviving open question → S2-1 workshop + CF-S97-W1-1 trajectory compute. Verdict integrity clean (11 distinct `audit_sha256`, sig_5 holds; O'Neill supersession chain preserves byte-level permanence).
