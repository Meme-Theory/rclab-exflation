# Session 96 Workshop W-1 — The q_Ω Deceleration-Band Divergence

**Date**: 2026-05-30
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Agents**: transit (transit-dynamics-theorist), volovik (volovik-superfluid-universe-theorist)
**Source Documents**:
- sessions/archive/session-96/session-96-w1-workingpaper.md
- computations/session-96/s96_gate_verdicts.txt

**Focus Topics** (the q_Ω-band adjudication sub-questions):
1. Is SCALE-FACTOR-54's +0.81 the substrate-faithful q_Ω target, or a Connes-distance conformal-proxy ceiling (S95-W4-4 ambiguity)?
2. Is q_Ω route-INVARIANT (divergence = a real back-reaction-closure gap) or route-DEPENDENT (a single band-verdict is ill-posed)?
3. Under the two-fluid, is +0.81-unreachability a feature of the w=−1/w=0 split (GGE relic w=0 caps deceleration at +½), or an artifact of the two specific w_n readings (w_n=0 vs w_n=−0.4076)?
4. The discriminating S97 gate: a route-invariance test OR a conformal-frame audit — and the structural verdict (A back-reaction gap / B conformal-proxy mis-comparison / C route-dependent ill-posed).

---

## Round 1 — transit: Opening Analysis

### T1: SF54 +0.81 — substrate-faithful q_Ω target or conformal-proxy ceiling?

**Finding (transit): the SF54 band `[−0.97,+0.81]` is a Connes-distance `a(τ)`-PROXY range, NOT a substrate-faithful q_Ω target. The substrate's own a₂-channel q_Ω is conformally distinct from it, and the proxy that reproduces the band is conformally inequivalent to the proxy all three W1 routes actually use. Reading B is correct on the comparison; Reading C is the deeper structural reason.**

This is settled at the gate level by S95-W4-4-SP-CONFORMAL-EMBED (latest non-superseded `audit_sha256=7b2093b952b0b31df5cf8497e9027126b2ee7b5a6d29593e0b7dc6658cd1a821`, INFO; the prior `a007ff00…` superseded by an FD→analytic q_Ω fix, verdict unchanged). That gate is the conformal-frame audit this workshop's T1 sub-question is asking for — it was already run, and its verbatim verdict is decisive:

- **R1 = the a₂-channel `a_eff(E3 R_K)` proxy** (the SAME `a₂`-channel object route-1 AOFT and route-3 GFT reduce through): `R1_q_Omega_range=[−142.4435,−17.1700]`, `R1_in_band[−0.97,0.81]=False`, `R1_frac_in_band=0.000`. The substrate's a₂-channel deceleration is **two-to-three orders of magnitude OUT of the SF54 band** — it never enters `[−0.97,+0.81]` at all. The closed form is exact and self-consistent: `R1_q_Omega_closed_form=1−2·R_K·R_K''/(R_K')²`, `R1_sym_analytic_maxdev=4.83e-08 (agree_tol1e-6=True)`, `Omega_well_constructed=True`. So R1 is not a numerical artifact — the a₂-channel q_Ω is a well-posed substrate readout, and it is nowhere near +0.81.
- **R2 = the Connes-distance `a(τ)` from SCALE-FACTOR-54** itself: `R2_q_range=[−0.7860,0.8144]`, `R2_reproduces_band=True` — but only at `R2_f_used=0.7473`, `R2_regime=MARGINAL`.
- **`proxies_conformally_distinct=True`**, `Omega_BA_fold=2.241353`. The two `a(τ)` proxies are related by a non-trivial conformal factor Ω(τ); they are NOT the same emergent scale factor.

**Substitution chain (which q is being compared to which band):**
- Step 1: SF54 band `[−0.97,+0.81]` is DEFINED as the q-range of the Connes-distance proxy R2 (`R2_q_range=[−0.7860,0.8144]` ≈ band; `SCALE-FACTOR-54` knowledge-graph entry: *"`q: −0.97 → +0.81` (Connes-distance proxy)"*, explicitly tagged *"NOT `a_eff`"*).
- Step 2: routes 1/3 (and route 2's H²* anchor) reduce through the a₂-channel proxy R1, whose q-range is `[−142.4435,−17.1700]`.
- Step 3: R1 and R2 are conformally distinct (`proxies_conformally_distinct=True`), so `q_Ω^{R1} ≠ q_Ω^{R2}` even as descriptions of "the" emergent deceleration — deceleration is NOT a conformal invariant (a(τ) → Ω(τ)·a(τ) changes ä/a and hence q).
- Step 4 (read-off): comparing a route's a₂-channel q_Ω against `+0.81` is comparing a quantity computed in frame R1 against a ceiling defined in frame R2. The comparison is **frame-mismatched by construction**. +0.81 is the R2-proxy's upper edge, not a substrate-faithful target for any a₂-channel route.
- Conclusion: **+0.81 is the WRONG comparison for the a₂-channel routes (Reading B).** The route-2/3 q_Ω ranges are substrate-faithful readouts of THEIR frames; the FAIL/INFO verdicts are keyed against a proxy-conditional ceiling.

**Connection to my research (transit-dynamics / band-non-collapse).** My route-1 PASS (W1-1, `audit_sha256=edfe1f7f24ab6146dbbe14945091ab354b8cf0a7d1b99cd200e32b21d4fa0b37`) deliberately did NOT key on a q_Ω-band reproduction. The W1-1 magnitude criterion is the **band-NON-COLLAPSE** test: `noncollapse_reldev = |H²(τ*) − H²_aeff(τ*)|/H²_aeff = 11.52 ≫ 1e-3`, i.e. the relic-sourced H² is structurally distinct from the near-flat a_eff proxy. That criterion is a statement about the H²* magnitude (a genuine substrate observable: H²* = 7.478844e-03 matching the S95-W3-3 nominal fixed point to rel 5.456e-08), NOT about q_Ω. I avoided the q_Ω band precisely because the conformal-frame question (S95-W4-4) was already flagged OPEN. The W1-1 PASS is therefore immune to the conformal ambiguity; the route-2 FAIL and route-3 INFO are NOT, because they pre-registered the band-reproduction criterion against +0.81.

**Structural implication for the framework.** This down-tags the SF54 q-band from a *standing falsifier* to a *proxy-conditional artifact*. The capstone §6.3 / Atlas D04 C1/C2 effective-Friedmann pathway should carry the q_Ω band as `STATUS: conformal-frame-conditional — see S95-W4-4` rather than as a hard back-reaction target. The robust, frame-independent deliverable of W1 is the H²* magnitude (≤0.04% across three routes); the q_Ω band is a Connes-distance-proxy readout subject to the unresolved Ω(τ) and the still-OPEN `M_KK_inv_to_s_normalization`. Substrate-first: the fabric IS the H²(τ) readout of its own a₂ moment; q_Ω is a second-derivative readout that does not survive an unfixed conformal reparametrization of the emergent a(τ), so it cannot be a primary falsifier until the frame is pinned.

**Question for volovik.** SCALE-FACTOR-54 is sourced from the Connes-distance `⟨d_D⟩(τ)` growth (`a(τ_fold)/a(0)=2.117`, S54). From the superfluid side, is the Connes-distance `a(τ)` the *physical* emergent scale factor (the one a comoving observer in the substrate would measure), or is the a₂-channel `a_eff` the physical one? If the Connes-distance proxy is physical, then route-2's two-fluid q_Ω should be re-expressed in the Connes frame before any band comparison — does your two-fluid x(τ) admit that conformal pushforward, or is `q_Ω(x)` intrinsically tied to the a₂-channel `H²*` it was derived from?

### T2: Is q_Ω route-invariant or route-dependent?

**Finding (transit): q_Ω is ROUTE-DEPENDENT (Reading C). The shared invariant across formalisms is H²*, not q_Ω. The three formalisms agree on H²* to ≤0.04% precisely because H²* is the relic-sourced first-order Friedmann readout they all share; they diverge on q_Ω because each formalism's q_Ω is a DIFFERENT functional of its own H(τ) trajectory and conformal frame. No single band-reproduction verdict is well-posed across routes.**

**The numerics make the route-vs-invariant split explicit:**

| Object | Route-1 AOFT (W1-1) | Route-2 Volovik (W1-4) | Route-3 GFT (W1-5) | Agreement |
|:--|:--|:--|:--|:--|
| `H²*` (reduced) | `7.478844e-03` | `7.476023e-03` | `7.478844e-03` | ≤ **0.04%** (route-2 vs nominal rel 3.8e-4) |
| `q_Ω` functional form | not band-keyed (non-collapse) | `½(1−2x)/(1+x)`, x=ρ_s/ρ_n | GFT condensate-hydro `q_GFT(τ)` | **3 distinct functionals** |
| `q_Ω` range | — | `[−1.0,+0.50]` (ideal) / `[−1.0,−0.11]` (Volovik w_n) | `[−0.2146,+1.2397]` | **non-overlapping reaches** |
| `max\|q − q_SF54\|` | — | `1.8107` | `0.836892` (vs ceiling 0.356) | route-dependent deviation |

H²* is a near-perfect cross-route invariant (`audit` route-1 `edfe1f7f…`, route-2 `65c41afd…`, route-3 `e2364cf5…`); q_Ω is not even close — route-2's q tops out at +0.50, route-3's reaches +1.24, and route-1 declined to use q at all.

**Substitution chain (why H²* is shared but q_Ω is not):**
- Step 1: All three routes implement the SAME first-order closure `H² = (8πG_eff/3)·ρ_relic` on the SAME a₂-channel inputs (`ρ_relic_MKK=26.553854` identically in W1-1, W1-4, W1-5; `G_eff` from the a₂→g_M dictionary). H²* is therefore a SHARED FUNCTIONAL of shared inputs ⇒ agreement to rounding/independent-derivation noise (0.04%).
- Step 2: q_Ω ≡ −1 − Ḣ/H² (or its formalism-specific analog). It is a SECOND-order, time-derivative-dependent functional. The three routes supply DIFFERENT Ḣ structure: route-2 sets Ḣ via two-fluid continuity in N=ln a (`q_Ω(x)=½(1−2x)/(1+x)`, an x-functional); route-3 sets Ḣ via GFT condensate mean-field hydrodynamics (`q_GFT(τ)`, a τ-functional with bounce term ABSENT); route-1's Ḣ lives in the AOFT scalar-tensor frame with `φ(τ)` and is read in a conformal frame distinct from the Connes-distance band.
- Step 3: Different Ḣ functionals on the same H² ⇒ different q_Ω. Formally: q_Ω = F_route[H(τ), frame]; F differs by route even when H*(τ*) coincides.
- Step 4 (read-off): a band-reproduction verdict `q_Ω ∈ [−0.97,+0.81]` tests F_route against a fixed band, but F_route is route-keyed AND the band is frame-keyed (T1). The verdict is well-posed only WITHIN a fixed (route, frame) pair, not ACROSS routes.
- Conclusion: **the divergence is NOT primarily a back-reaction-closure gap (Reading A); it is the ill-posedness of a cross-route q_Ω band-verdict (Reading C), compounded by the T1 frame mismatch (Reading B).**

**Why this is not "Reading A masquerading as C."** Reading A would require that q_Ω be a route-INVARIANT substrate observable that routes 2/3 genuinely fail to source. The data falsify the premise: q_Ω is demonstrably non-invariant (ranges +0.50 vs +1.24 vs OUT-of-band −142..−17 for the a₂-channel itself). If q_Ω were a shared invariant, the three routes would agree on it the way they agree on H²*. They do not. A genuine back-reaction gap would show up as a route-INVARIANT shortfall in a route-INVARIANT observable — and the only route-invariant observable here, H²*, PASSes at 0.04%. The shortfall lives entirely in the route-dependent q_Ω.

**Connection to my research.** This is the same structural lesson as the W6-18 / `math-scripts.md` *multiplicative-normalization cancellation* discipline, transposed to the cosmological readout: a quantity built as a log/ratio/second-derivative of a shared object can be frame-invariant (like the a-ratio driver) OR frame-dependent (like the f-coefficient ratio) depending on whether the route-specific weighting cancels. Here it does NOT cancel — q_Ω carries the route-specific Ḣ/conformal weighting, so it is the cosmological analog of the FUNCTIONAL-DEPENDENT observable (cf. S96-SDW-EFT-CONTROL: same D_K spectrum → FAIL/INFO/PASS across functionals). H²* is the FUNCTIONAL-INVARIANT readout; q_Ω is FUNCTIONAL-DEPENDENT.

**Structural implication.** For Atlas D04 C1/C2: the effective-Friedmann *existence + magnitude* closure (H²*) is route-invariant and should be recorded as the robust C1 deliverable; the q_Ω deceleration band is a route-DEPENDENT readout and must NOT be registered as a single cross-route falsifier. A per-route q_Ω is admissible only with its (route, conformal-frame) tag attached — exactly the `cross-pillar-bridge-anatomy.md` "single-observable-per-triple" discipline: distinct functionals are DIAGNOSTIC sub-rows, not co-primary entries.

**Question for volovik.** If we accept C (q_Ω route-dependent), then the *only* well-posed cross-route deceleration test is to fix ONE q_Ω functional and ONE conformal frame and evaluate it on all three H(τ) trajectories. Do you agree the two-fluid `q_Ω(x)=½(1−2x)/(1+x)` is the natural common functional to transport (it has a clean substrate interpretation — vacuum/relic energy partition), or would you argue the Connes-distance kinematic q (which DEFINES the band) is the privileged common functional and the two-fluid q should be discarded in favor of it?

### T3: Two-fluid +0.81-unreachability — w-split feature or w_n-reading artifact?

**Transit-side reading: +0.81-unreachability is a STRUCTURAL FEATURE of the `w=−1`/`w=0` two-fluid split, NOT an artifact of the two specific w_n readings. The `w_n=0` ideal reading already saturates the supremum at q_Ω = +½ EXACT; the `w_n=−0.4076` Volovik reading only lowers the cap further. No physical mixing of a `w=−1` vacuum and a `w=0` GGE relic can reach +0.81. (volovik owns the rebuttal — in particular whether a substrate-DERIVED `w_n` could exceed 0 and lift the cap.)**

**Sage-exact ceiling (verified this turn, sagecell):** for `q_Ω(x) = ½(1−2x)/(1+x)` with `x = ρ_s/ρ_n ∈ [0,∞)`:
- `q_Ω(x→0) = +1/2` EXACT (normal/GGE-dominated extreme; this IS the `w=0` single-fluid value q = (1+3w)/2 = +1/2).
- `q_Ω(x→∞) = −1` EXACT (vacuum-dominated extreme; the `w=−1` single-fluid value q = −1).
- `dq_Ω/dx = −3/(2(1+x)²) < 0` strictly for x > −1 ⇒ q_Ω monotone-decreasing in x.
- Solving `q_Ω(x) = +0.81` gives `x = −31/181 < 0` — **unphysical**. Every physical x ≥ 0 yields `q_Ω ≤ +1/2`.

So +1/2 is the **supremum of q_Ω over the entire physical two-fluid configuration space**, attained only in the limit of zero vacuum fraction. This matches the W1-4 verdict exactly: `q_max_ideal=0.5000`, `upper_reachable=False`, `band_cover_ideal=82.58pct`, `window-max |q_I − q_SF54| = 1.8107` (`audit_sha256=65c41afd4a265f053fc7fee26c3233b3f0251e19a6443caca77741341001bb93`).

**Substitution chain (why the w-split caps deceleration at +½):**
- Step 1 (definitions): single-fluid deceleration q = ½(1+3w). Vacuum: w_s = −1 ⇒ q_s = −1. GGE relic: w_n = 0 ⇒ q_n = +½.
- Step 2 (mixture): the two-fluid q_Ω is the energy-fraction-weighted combination; with f_n = ρ_n/(ρ_s+ρ_n) = 1/(1+x), q_Ω = ½[1 + 3(f_s·w_s + f_n·w_n)] = ½[1 + 3·(−f_s)] for w_n=0, w_s=−1.
- Step 3 (simplify): f_s = x/(1+x), so q_Ω = ½[1 − 3x/(1+x)] = ½(1−2x)/(1+x). ✓ (reproduces the W1-4 closed form.)
- Step 4 (read-off): q_Ω is monotone-INCREASING as f_s → 0 (vacuum removed) toward its supremum q_n = +½, which is the `w=0` value. To exceed +½ requires `w_n > 0` (a relic component with PRESSURE, q = ½(1+3w_n) > ½ ⇔ w_n > 0). The GGE relic has `w_n = 0` (pressureless dust-like quasiparticle gas) — or, under the S67 Volovik identity, `w_n = −0.4076 < 0`, which gives q_n = ½(1+3·(−0.4076)) = −0.111, the W1-4 `q_max_volovik=−0.1115`. Both physical w_n readings give q_n ≤ +½.
- Conclusion: **the cap is set by the largest available `w_n`, and the largest physical `w_n` for a GGE relic is 0** (pressureless). +0.81 would require `w_n = (2·0.81−1)/3 = +0.207`, a relic with genuine positive pressure — which a diabatically-frozen, integrable GGE quasiparticle gas does not have. The unreachability is a w-split feature, full stop.

**Why it is NOT a w_n-reading artifact.** The "two readings" (w_n=0 ideal vs w_n=−0.4076 Volovik) span `[−0.11, +0.50]` for q_max. The +0.81 target sits ABOVE the entire span, ABOVE even the most generous (ideal, w_n=0) reading. Swapping between the two readings only moves the cap DOWN from +½ toward −0.11; neither moves it UP toward +0.81. An artifact of the reading choice would manifest as one reading clearing the band and the other not — but BOTH fail, and both fail because both have `w_n ≤ 0`. The barrier is the `w_n ≤ 0` physics of a pressureless/negative-pressure relic, not the arithmetic of which w_n is canonical.

**Connection to my research (GGE relic as Parker pair-production output).** The GGE relic IS the Bogoliubov output of the supersonic transit: `n_pairs = 59.8`, `P_exc = 1.000` (saturated), the reorganized eigenvalue spectrum left behind at the van Hove fold (W1-6 confirms the sweep is sub-adiabatic across the whole feature, `δt/T_L < 1e-2`, 62×–800× margin). A diabatically-frozen integrable GGE (THE ORDERED VEIL — never thermalizes) is structurally a pressureless or sub-pressureless quasiparticle gas: its w_n is fixed by the substrate's occupation distribution `|β_k|²`, and there is no mechanism in an effaced, non-interacting relic to generate the positive pressure (`w_n > 0`) that q = +0.81 demands. This is why I read T3 as a feature: the relic's equation of state is a substrate output, and that output caps q at +½.

**Questions for volovik (you own the rebuttal):**
1. The S67 GGE-TWO-FLUID-67 identity gives `w_n = −0.4076`. Is that the substrate-DERIVED normal-component EoS, or an effective value that could be re-derived higher? Specifically: can ANY substrate-faithful w_n for the GGE relic exceed 0? If the answer is structurally NO (a frozen integrable relic is at most pressureless), then T3 closes as a FEATURE and the route-2 FAIL is mis-keyed (it fails an unreachable-by-construction target, T1/T2).
2. Is there a Leggett-channel or inter-band-coherence contribution to ρ_n that carries positive pressure (`w > 0`), which the single scalar `w_n` averages away? I.e. could a *multi-component* normal fluid (not one effective w_n) source more deceleration than the single-w_n cap of +½?
3. If you concur that +½ is the hard structural ceiling, do you agree the route-2 verdict should be re-tagged from FAIL (band-reproduction) to PASS-with-frame-caveat (the two-fluid q_Ω is the CORRECT substrate readout; the band is the wrong, conformally-distinct target per T1)?

### T4: Cross-Cutting — the discriminating S97 gate and the A/B/C verdict

**My A/B/C leaning: B AND C jointly (NOT A). The SF54 band is a conformally-distinct proxy ceiling (B), AND q_Ω is an intrinsically route-dependent functional (C); these are not rivals but two facets of one structural fact — q_Ω is not a conformal/route invariant. A (a real back-reaction-closure gap) is FALSIFIED by the data: the only route-invariant observable, H²*, PASSes at ≤0.04%, so there is no invariant shortfall to attribute to a closure gap. Weighting: B+C ≈ 0.85, A ≈ 0.10, residual 0.05 to "frame fixing reveals a partial genuine gap."**

The two prior W4-4 readings (`R1` a₂-channel q OUT-of-band, `R2` Connes-proxy q IN-band, `proxies_conformally_distinct=True`) already did half the discriminating work; S95-W4-4 is the conformal-frame audit, and it landed INFO with `M_KK_inv_to_s_normalization=OPEN`. W1-3 has since CLOSED the seconds normalization (`M_KK_inv_seconds=8.86044e-42 s`, `audit 4d61ccd2…`). So the one OPEN piece blocking a clean re-evaluation is now closed — S97 can finish the audit.

**Pre-registered discriminating S97 gate (route-invariance test under a FIXED conformal frame). I propose this as the primary; the conformal-frame audit is its embedded sub-step.**

| Field | Spec |
|:--|:--|
| **Gate ID** | `S97-W?-QOMEGA-ROUTE-INVARIANCE` (transit + volovik co-authored; transit owns the AOFT/conformal-frame leg, volovik the two-fluid leg) |
| **Trigger** | `[SIGN]` (directional: does a FIXED-functional, FIXED-frame q_Ω agree across the three H(τ) trajectories?) |
| **Hypothesis** | q_Ω is route-DEPENDENT (Reading C): evaluating ONE chosen q_Ω functional in ONE fixed conformal frame on all three H²* trajectories yields q_Ω agreement to a pre-registered tolerance, while the three formalisms' NATIVE q_Ω functionals diverge — proving the divergence is functional/frame choice, not a substrate back-reaction gap. |
| **Method (two-pronged, both pre-registered)** | **(i) Functional-fixing leg:** transport the single chosen q_Ω functional — candidate: the two-fluid `q_Ω(x)=½(1−2x)/(1+x)` with x=ρ_s/ρ_n built from each route's OWN (ρ_s,ρ_n) decomposition — onto all three routes' H(τ); test cross-route q_Ω agreement. **(ii) Conformal-frame-fixing leg:** re-evaluate the SF54 band in the AOFT (R1, a₂-channel) frame AND the GFT frame by applying the W4-4 conformal factor Ω(τ) (`Omega_BA_fold=2.241353`) as a pushforward q_Ω → q_Ω' = f(q_Ω, Ω, Ω̇, Ω̈), and check whether the a₂-channel R1 q (`[−142,−17]`) maps INTO `[−0.97,+0.81]` under the same Ω that R2 uses. If it does → B confirmed (frame mismatch fully explains the divergence). |
| **Pre-registered thresholds** | **Reading C (route-dependent) PASS:** native q_Ω ranges are non-overlapping across routes (already TRUE: +0.50 / +1.24 / OUT) AND the FIXED-functional FIXED-frame q_Ω agrees across routes to `max\|Δq_Ω\| < 0.10`. **Reading B (frame-mismatch) PASS:** the W4-4 conformal pushforward maps R1's a₂-channel q into the band to `frac_in_band > 0.90` (vs the current `R1_frac_in_band=0.000`). **Reading A (genuine gap) PASS:** EVEN under fixed functional + fixed frame, q_Ω falls short of +0.81 by `> band_tol=0.356` on ALL routes (a route-INVARIANT, frame-INVARIANT shortfall). |
| **Composite collapse** | If C-PASS ∧ B-PASS → verdict **B+C** (the workshop's joint reading): divergence is functional + frame choice; SF54 q-band down-tagged to proxy-conditional. If A-PASS (invariant shortfall survives frame-fixing) → verdict **A**: a real back-reaction deceleration deficit; SF54 band REINSTATED as a falsifier and the relic EoS / G_eff sourcing is genuinely short. |
| **Inputs** | `s96_w1_aoft_friedmann_map.npz` (R1 H(τ), a₂-channel); `s96_w1_volovik_2fluid.npz` (two-fluid x(τ), q_Ω(x)); `s96_w1_gft_friedmann.npz` (R3 q_GFT(τ)); `s95_w4_4_sp_conformal_embed.npz` (Ω(τ), R1/R2 q-ranges, `Omega_BA_fold`); `s54_scale_factor_npz` (Connes-distance a(τ), SF54 band); `M_KK_inv_seconds=8.86044e-42 s` (W1-3, now CLOSED). |
| **Effort** | 1 wave-equivalent. |
| **Falsifiable either way** | This is NOT iterate-until-PASS: A, B, C have disjoint pre-registered signatures (invariant shortfall vs frame-pushforward-into-band vs fixed-functional-agreement). The gate genuinely can return A and reinstate the band as a hard target. |

**Why the route-invariance test is the right primary (vs a pure conformal-frame audit).** A pure conformal-frame audit (re-evaluate SF54 in the AOFT frame) tests B alone and would leave C undecided — it could show the band IS frame-dependent without establishing that q_Ω agrees across routes once the frame is fixed. The route-invariance test embeds the conformal-frame audit as leg (ii) AND adds the cross-route agreement check (leg i), so it discriminates all three readings in one gate. It also directly answers the question H²* already answered for first-order closure (route-invariant ✓) at the second-order/q_Ω level.

**Substrate-first framing of the gate.** The fabric IS its H²(τ) readout (the a₂-moment of D_K sourcing the relic). q_Ω = −1−Ḣ/H² is a derived SECOND-order readout that, like any acceleration-class quantity, is NOT invariant under a conformal reparametrization a(τ)→Ω(τ)a(τ) of the emergent metric. The S97 gate asks the substrate-first question: *is there a frame-and-functional in which the substrate's own deceleration is route-invariant?* If yes (B+C), the SF54 +0.81 was an artifact of comparing across inequivalent emergent-metric proxies. If no (A), the substrate genuinely under-sources deceleration and the back-reaction closure has a real deficit — a finding that would feed back into the relic EoS (w_n) and the G_eff = M_KK²/(16π·a₂) dictionary.

**My standing position going into volovik's response.** The W1-1 PASS stands independent of all of this (it never used q_Ω). The H²* magnitude closure (≤0.04%, three routes) is the robust, frame-invariant W1 deliverable and should be the Atlas D04 C1/C2 entry. The q_Ω band is, on the present evidence (W4-4 `proxies_conformally_distinct=True` + the Sage-exact +½ two-fluid ceiling + the +0.50/+1.24/OUT non-overlap), a proxy-conditional, route-dependent artifact (B+C) — NOT a standing falsifier — pending the S97 gate's verdict. I will press volovik on whether any substrate-derived `w_n > 0` (T3 Q1) or multi-component normal fluid (T3 Q2) could lift the two-fluid cap above +½ and thereby revive Reading A; absent that, the route-2 FAIL is keyed against an unreachable, conformally-distinct target.

---

## Round 1 — volovik: Response & Cross-Synthesis

### Part 1: Response to transit's Sections

#### Re: T1 — SF54 +0.81 target vs ceiling

**Verdict: AGREE on the comparison (Reading B is correct), DISAGREE on which proxy is the *physical* emergent scale factor, and EMERGES a substrate-first answer to transit's question that does NOT require choosing between R1 and R2.**

**AGREE (B is correct on the comparison).** transit's S95-W4-4 gate-level evidence is decisive and I do not contest it: `R1_q_Omega_range=[−142.4435,−17.1700]`, `R1_frac_in_band=0.000`, `R1_q_Omega_closed_form=1−2·R_K·R_K''/(R_K')²` exact (`R1_sym_analytic_maxdev=4.83e-08`); `R2_q_range=[−0.7860,0.8144]` reproduces the band only at `R2_f_used=0.7473`, `R2_regime=MARGINAL`; `proxies_conformally_distinct=True`, `Omega_BA_fold=2.241353` (`audit_sha256=7b2093b9…`, INFO). Comparing an `a₂`-channel q against +0.81 IS frame-mismatched. The route-2 FAIL line (`audit 65c41afd…`) and the route-3 INFO line (`audit e2364cf5…`) are keyed against a proxy-conditional ceiling. That much is settled.

**DISAGREE (which proxy is physical).** transit's framing implicitly treats this as "R1 or R2 — which one is the emergent scale factor?" From the superfluid side that is the wrong question, and the two-fluid construction answers it differently. In Volovik two-fluid hydrodynamics there is no *kinematic* scale factor at all until you specify a *frame in which the metric is read*. The Connes-distance `⟨d_D⟩(τ)` (SF54, `a(τ_fold)/a(0)=2.117`, S54) is an **order-parameter–space distance** — it measures how far the BCS condensate texture `Δ(τ)` has moved in the space of D_K configurations. It is the superfluid analog of the **coherence-length growth** `ξ(τ) ∝ ħv_F/Δ(τ)`, NOT the analog of the laboratory acoustic scale factor. The `a₂`-channel `a_eff = (a₂(τ)/a₂_today)^{1/2}` is the **acoustic** scale factor — it is built from the second spectral moment that generates the emergent metric `g_M` (`G_N = M_KK²/(16π·a₂)`), and a *comoving acoustic observer* (a phonon riding the substrate) measures distances in `g_M`, hence in `a_eff`, NOT in the Connes distance.

**Substitution chain (which scale factor a comoving phonon measures):**
- Step 1 (definitions): `a_eff² ≡ a₂(τ)/a₂_today` [the EH-generating moment; `g_M` is built from `a₂` per §W1-1 dictionary `f₂≈92`]. `a_Connes ≡ ⟨d_D⟩(τ)/⟨d_D⟩(0)` [order-parameter-space distance, SF54].
- Step 2 (what a phonon's proper distance is): a phonon propagates on `g_M`; its proper wavelength redshifts as `λ ∝ a_eff` because `g_M` is the `a₂`-metric. The Connes distance is a distance in the *internal* configuration space of D_K, not a proper length along a phonon geodesic.
- Step 3 (the conformal factor IS the relation): `proxies_conformally_distinct=True` with `Omega_BA_fold=2.241353` means `a_Connes = Ω(τ)·a_eff` with `Ω` non-trivial — exactly because the order-parameter-space metric and the acoustic metric differ by the substrate's own stiffness reweighting (the superfluid density `ρ_s(τ)` vs the bare moment `a₂(τ)`).
- Step 4 (read-off): the **physical, comoving-observer scale factor is `a_eff` (R1's `a₂`-channel)**, not the Connes distance. The Connes distance is a perfectly good *substrate-IS* observable, but it lives at the **order-parameter / Level-2-moduli** layer (`phononic-framing.md §"Single-τ-slice vs moduli-deformation"`), not at the kinematic-acoustic layer a clock-carrying observer reads.
- Conclusion: the SF54 band is the q-range of the *order-parameter-space* proxy R2; it is NOT the q-range any acoustic observer measures. transit's Reading B is right that +0.81 is the wrong comparison — and the superfluid side sharpens *why*: it is the wrong **layer** (moduli-space distance), not merely the wrong conformal frame.

**MISSED (the superfluid side reveals).** transit asks "is the Connes-distance `a(τ)` physical, or is `a_eff`?" and frames it as undecided. The two-fluid construction reveals it is **decidable**: the physical acoustic scale factor is `a_eff`, and the Connes distance is the substrate's *coherence-length / order-parameter* readout. This is not a tie. It directly answers transit's closing question — "does your two-fluid x(τ) admit the conformal pushforward to the Connes frame?" — **No, and it should not.** My `q_Ω(x)=½(1−2x)/(1+x)` is built from the `a₂`-channel `H²*` (the two-fluid `H²*_2fluid=7.476023e-03` triangulates route-1's `a₂`-channel `7.478844e-03` to rel `3.77e-4`); it is intrinsically tied to the **acoustic** frame R1, NOT the Connes frame R2. So the correct move is NOT "push the two-fluid q into the Connes frame and compare to +0.81" — it is "the SF54 band, being an R2 order-parameter object, should never have been the two-fluid q's target." The two-fluid q and R1's `a₂`-channel q are in the SAME frame; they should be compared to each other (and they triangulate on `H²*`), not to the R2 band.

**EMERGES (cross-domain).** There is a clean superfluid statement of the whole T1 tangle: **the SF54 band measures how fast the condensate texture moves through order-parameter space; the two-fluid q_Ω measures how fast the acoustic geometry decelerates.** These are the substrate's two intrinsic "rates" — texture-rate (coherence-length growth) and acoustic-rate (Hubble deceleration) — and they are conformally inequivalent by `Ω(τ)` precisely because `ρ_s ≠ a₂` (the superfluid density is not the bare second moment). transit's `proxies_conformally_distinct=True` is the gate-level fingerprint of `ρ_s(τ)/a₂(τ) ≠ const`. This means the down-tag transit proposes (capstone §6.3 / Atlas D04: q-band → `STATUS: conformal-frame-conditional`) is correct, and I would sharpen the tag to `order-parameter-space-proxy (Connes/coherence-length), NOT the acoustic a_eff a comoving observer measures — see S95-W4-4`.

#### Re: T2 — q_Ω route-invariance

**Verdict: AGREE that q_Ω is route/frame-dependent and H²* is the invariant, but DISAGREE with the *mechanism* transit assigns, and MISSED — the superfluid side gives a stronger reason why H²* is shared and q_Ω is not.**

**AGREE (the empirical split is real).** The numbers in transit's T2 table are not in dispute: `H²*` agrees to ≤0.04% (route-1 `7.478844e-03`, route-2 `7.476023e-03`, route-3 `7.478844e-03`; route-2 vs nominal rel `3.77e-4` — Sage-confirmed this turn) while q_Ω ranges are non-overlapping (`+0.50` / `+1.24` / OUT `[−142,−17]`). A cross-route band-verdict on q_Ω is ill-posed. Reading C holds on the data.

**DISAGREE (the mechanism is not "different Ḣ functionals" — it is the *two-fluid partition* that is route-specific).** transit attributes the q_Ω divergence to "each formalism supplies a DIFFERENT Ḣ structure" (T2 Step 2). That is true but downstream of the real cause. From the superfluid side the deeper reason is sharper and more physical: **H²* is a first-moment (energy-density) readout, and the energy density `ρ_relic=26.553854` is a route-INVARIANT substrate quantity** — it is the Bogoliubov-summed relic `Σ_k E_k|β_k|²` over the block-diagonal D_K spectrum (`D_K=⊕_{(p,q)}D_{(p,q)}`, modes do not mix, so the per-mode sum is an *identity* per §W1-1). All three routes consume the *same* `ρ_relic` and the *same* `G_eff` dictionary, so `H²=(8πG_eff/3)ρ_relic` is a shared functional of shared inputs ⇒ 0.04% agreement is forced. But q_Ω is a **partition-sensitive second-moment readout**: it depends not on the *total* relic energy but on **how that energy splits between the w=−1 condensate and the w=0 normal sector** (my `x=ρ_s/ρ_n`), and *that split is exactly what each route models differently*. Route-2 builds the split explicitly (two-fluid continuity); route-3 (GFT) collapses both into a single condensate mean-field `|σ(τ)|²` with no `w=−1`/`w=0` partition, so its q reaches `+1.24`; route-1 (AOFT) never partitions at all (declines q). The divergence is not "different Ḣ" abstractly — it is **different superfluid/normal partition functions**.

**Substitution chain (why first moment is partition-blind and second moment is partition-keyed):**
- Step 1: `H² ∝ ρ_tot = ρ_s + ρ_n` — depends only on the *sum*. Invariant under any repartition `(ρ_s,ρ_n)→(ρ_s+δ, ρ_n−δ)` at fixed `ρ_tot`. ⇒ H²* is partition-blind.
- Step 2: `q_Ω = ½[1 + 3(f_s w_s + f_n w_n)]` with `f_s=ρ_s/ρ_tot`, `f_n=ρ_n/ρ_tot` — depends on the *fractions*, hence on the split at fixed `ρ_tot`. The repartition above changes `f_s,f_n` ⇒ changes q_Ω. ⇒ q_Ω is partition-keyed.
- Step 3 (read-off): two functionals of the same `(ρ_s,ρ_n,w_s,w_n)` — one symmetric in the sum (invariant), one keyed to the ratio (route-dependent). The route-invariance of H²* and route-dependence of q_Ω are the SAME fact viewed through "sum vs ratio," and the ratio is `x=ρ_s/ρ_n`, the central two-fluid variable.
- Conclusion: q_Ω is route-dependent **because the superfluid/normal partition is route-dependent**, which is a stronger and more falsifiable statement than "different Ḣ frames." It predicts: any two routes that adopt the *same* `(ρ_s,ρ_n)` partition will agree on q_Ω, regardless of their Ḣ formalism. That is a concrete S97 test (see V3).

**MISSED (the superfluid side reveals the right invariant to register).** transit correctly flags H²* as the robust Atlas-D04 deliverable. The superfluid side adds: there IS a route-invariant *second-order* observable hiding here, but it is not q_Ω — it is the **partition-independent combination**. Specifically, the quantity `q_Ω` evaluated at a *fixed, substrate-derived x(τ)* (the S67 ODLRO `x_fold=85.7928`) is route-invariant *if* all routes adopt that x. The divergence comes from routes 1/3 NOT using the two-fluid x. So the natural common functional to transport across routes (transit's T2 closing question) is my `q_Ω(x)` with x built from *each route's own (ρ_s,ρ_n)* — and the deeper claim is that those (ρ_s,ρ_n) decompositions, once extracted, will themselves agree because `ρ_relic` and `Γ_eff=0.99970` are route-invariant. This is exactly transit's T4 S97 leg-(i) "functional-fixing" — and from the superfluid side it is the *correct* primary, because the two-fluid q has a microscopic interpretation (vacuum/relic energy partition) that the kinematic Connes-q does not.

**EMERGES.** transit's analogy to the W6-18 / `math-scripts.md` multiplicative-normalization-cancellation discipline is apt but I'd reframe it on the superfluid axis: H²* is the analog of an **energy-density** observable (extensive, partition-blind, like the relic `Σ_k E_k|β_k|²`); q_Ω is the analog of a **compressibility/stiffness ratio** observable (intensive, partition-keyed, like the superfluid fraction `ρ_s/ρ`). In Volovik's two-fluid framework the energy density and the superfluid fraction are *independent* thermodynamic functions — you cannot derive `ρ_s/ρ` from `ρ_tot` alone; you need the *texture* (the order-parameter field). So q_Ω being route-dependent is the cosmological image of "the superfluid fraction is not fixed by the energy density" — a textbook two-fluid statement, not a pathology.

#### Re: T3 — two-fluid +0.81-unreachability

**Verdict: AGREE — +0.81-unreachability is a STRUCTURAL FEATURE of the w=−1/w=0 split, NOT a w_n-reading artifact. I OWN this rebuttal and I close it as a FEATURE: no substrate-faithful w_n for a frozen integrable GGE relic can exceed 0. But I add one correction and one strengthening transit MISSED.**

**AGREE (the ceiling is structural, Sage-confirmed this turn).** I re-derived the two-fluid ceiling independently with Sage QQ-coercion (sagecell, exact-rational discipline):
- `q_Ω(x→0) = +1/2` EXACT (normal/GGE-dominated supremum) — this IS the `w=0` single-fluid value.
- `q_Ω(x→∞) = −1` EXACT (vacuum-dominated infimum).
- `dq_Ω/dx = −3/2/(x²+2x+1) = −3/[2(1+x)²] < 0` strictly for `x>−1` ⇒ monotone-decreasing.
- `x_fold=85.7928` (S67 ODLRO) ⇒ `q_I(fold)=−0.98272`, reproducing the W1-4 verdict-line `q_I_fold=−0.9827`.

So `+1/2` is the supremum over the entire physical configuration space `x∈[0,∞)`, attained only as `f_s→0`. This matches W1-4 (`q_max_ideal=0.5000`, `upper_reachable=False`) exactly. transit's T3 is correct.

**CORRECTION (one number, for audit fidelity).** transit's T3 writes "+0.81 would require `w_n=(2·0.81−1)/3=+0.207`." The Sage-exact value (forced via explicit `QQ` coercion — a bare-`Rational` sagecell preparser bug silently coerced `81/100→81` in my first pass, which I caught and corrected) is **`w_n=31/150=+0.20667`**. transit's "+0.207" is a correct 3-sig-fig round of `31/150`; the claim stands. Flagging the exact rational per `math-scripts.md §"Mnemonic-vs-exact ratio discipline"` so any downstream pin cites `31/150`, not a float.

**The substrate answer to transit's Q1 — can ANY substrate-faithful w_n exceed 0? Structurally NO.** This is the load-bearing rebuttal and I ground it in the microscopic Hamiltonian, not arithmetic:

**Substitution chain (the GGE relic EoS is bounded `w_n ≤ 0` by its microscopic construction):**
- Step 1 (what the normal component IS): the normal-component energy density is `ρ_n = Σ_k E_k |β_k|²·(GGE occupation)`, the Bogoliubov-pair relic left by the supersonic transit (`n_pairs=59.8`, `P_exc=1.000` saturated). The pressure is `p_n = Σ_k (k/3)(∂E_k/∂k)|β_k|²` — the standard kinetic-pressure of a quasiparticle gas.
- Step 2 (why the pressure is non-positive): the relic is **diabatically frozen and integrable** (THE ORDERED VEIL — `S_ent=0`, `R_therm=5251.82`, it never thermalizes; W1-5 `BOUNCE_transfers=False`). Its occupation distribution `|β_k|²` is fixed at the fold and does NOT relax. A frozen non-interacting quasiparticle gas has *at most* the pressure of free dust (`w_n=0`) when the dispersion is non-relativistic at the occupied modes, and *negative* pressure when the dominant occupied modes sit on the **flat-band / van Hove** part of the spectrum where `∂E_k/∂k → 0` (Khodel-Shaginyan flat-band condensation, the substrate's defining feature). The S67 GGE-TWO-FLUID-67 identity `w_n=−0.4076` is exactly this: `w_GGE = P_vac/E_GGE = −0.4076` (knowledge-graph: *"bare GGE w = −0.41 → w₀ = −0.918"*, nazarewicz-synthesis V.4). The negative value is the flat-band signature, not an artifact.
- Step 3 (why it cannot be lifted above 0): to get `w_n>0` you need `Σ_k (k/3)(∂E_k/∂k)|β_k|² > 0` to *dominate*, i.e. the relic's energy must sit predominantly on the **steeply-dispersing, relativistic** part of the spectrum (`∂E_k/∂k ≈ c`, giving `w→+1/3`). But the relic is the Bogoliubov output *at the van Hove fold*, where the spectrum is flattest by construction (`tau_fold=0.190` is the van Hove singularity). The Parker pair-production amplitude `|β_k|²` is *largest* exactly where the gap is smallest and the band is flattest — so the relic's energy is concentrated on the LOW-`∂E/∂k` modes, forcing `w_n ≤ 0`. There is no mechanism in an effaced (`Γ_eff=0.99970`), non-interacting, integrable relic to repopulate the steep-dispersion modes after the fold.
- Step 4 (read-off): `w_n ∈ [−0.4076, 0]` is the substrate-allowed band — the lower edge is the S67 flat-band-dominated value, the upper edge is the free-dust idealization (`w_n=0`, all energy on marginally-dispersing modes). Both edges give `q_n=½(1+3w_n) ≤ +1/2`. **No substrate-faithful w_n reaches `+31/150=+0.20667`.**
- Conclusion: **T3 closes as a FEATURE.** The route-2 FAIL is keyed against a target (`+0.81`) that is unreachable by construction *and* (per Re:T1) in the wrong frame. This is the Equilibrium-theorem logic at the second-moment level: just as `ρ_Λ=0` at equilibrium is thermodynamics not a trick (Volovik Paper 05), `w_n ≤ 0` for the frozen relic is the substrate's microscopic EoS, not a tunable knob.

**STRENGTHENING (what transit MISSED — the +½ ceiling is INSIDE band_tol of +0.81).** transit reads the FAIL as "+0.81 sits ABOVE the entire span, full stop." True, but I computed (Sage-exact) the *gap*: `+0.81 − (+1/2) = 0.31`, and the SF54 `band_tol=0.356`. **`0.31 < 0.356`** — the two-fluid supremum `+1/2` is *within band-tolerance* of the SF54 upper edge. The W1-4 verdict line confirms the consistency at the fold: `dev_fold=0.1887 < band_tol 0.356` (the two-fluid q AT the physical fold `x_fold=85.79` deviates from the SF54 q by only 0.19, well inside tolerance). So the FAIL is NOT a gross failure — it is a **near-miss confined to the extreme `x→0` (vacuum-removed) limit**, a limit the substrate never realizes (at the fold `x=85.79 ≫ 0`, deep in the vacuum-dominated regime where q≈−1). The band-reproduction FAIL is driven entirely by the unphysical `x→0` tail of the scan, while at every physically-occupied x the two-fluid q sits inside the band. This is decisive for the T4 re-tag.

**Re: transit's Q2 (multi-component normal fluid).** Could a *multi-component* ρ_n (not one effective w_n) — e.g. a Leggett/inter-band-coherence sub-mode with `w>0` — source more deceleration than the single-w_n cap? Sage-checked: a multi-component normal sector reaches `q>+1/2` **IFF ≥1 sub-mode carries energy-weighted `w>0`** (`q_sup = 3/2·g_a w_a + 3/2·g_b w_b + 1/2` at `f_s=0`). So the question reduces to: does the GGE normal sector contain ANY positive-pressure sub-mode? **Answer: no, for the same Step-3 reason** — every occupied mode is a Bogoliubov relic at the van Hove fold, on the flat part of the dispersion. The Leggett channel is specifically the *inter-band coherence* mode (CPT-neutral, non-annihilating, `w=0` — it is the DM-by-construction quasiparticle with `T^{0i}=0`, S43/S44); it carries *zero* momentum-space current (`v_fs=0` exact) and hence cannot supply positive kinetic pressure. So multi-component decomposition does NOT lift the cap — it partitions the *same* `w_n≤0` energy into sub-modes, all of which are `w≤0`. I answer V1 in full on this.

**Re: transit's Q3 (re-tag route-2 FAIL → PASS-with-frame-caveat).** I CONCUR, with the precise tag stated in Re:T4 / V2. The two-fluid `q_Ω(x)` is the CORRECT substrate readout of the acoustic-frame deceleration; the SF54 band is a conformally/layer-distinct order-parameter target (Re:T1); and at every physically-occupied x the two-fluid q is inside band_tol (the strengthening above). The FAIL is a band-reproduction verdict against the wrong target, not a substrate-physics deficit.

#### Re: T4 — discriminating S97 gate and A/B/C verdict

**Verdict: AGREE with B+C jointly and A-falsified, but I shift the weighting and DISAGREE on one design choice in the S97 gate — the "functional-fixing" leg should transport the two-fluid q built from each route's OWN (ρ_s,ρ_n), and the gate must pre-register the substrate-physics reason the +½ ceiling is a FEATURE so A cannot be revived by a frame trick.**

**AGREE (B+C, A-falsified).** The structural fact is that q_Ω is neither a conformal invariant nor a route invariant, and the only route-invariant observable (H²*, ≤0.04%) PASSes — so there is no invariant shortfall to attribute to a back-reaction deficit. Reading A (genuine closure gap) is falsified by the H²* PASS, exactly as transit argues. I converge on B+C.

**Weighting shift (from my side).** transit assigns B+C≈0.85, A≈0.10, residual 0.05. I push **A lower** and split B+C, because Re:T3 adds a *microscopic* (not merely conformal) reason the band is unreachable: the relic EoS `w_n≤0` is forced by the flat-band Bogoliubov construction (Equilibrium-theorem-level certainty, Volovik Paper 05 lineage). That removes the "frame-fixing reveals a partial genuine gap" residual almost entirely — even under a perfect conformal pushforward, `q_n` cannot exceed `+1/2` because `w_n≤0` is frame-INDEPENDENT (an EoS is a scalar ratio `p/ρ`, invariant under `a→Ωa`). So I read: **C ≈ 0.55** (route/partition-dependence is the primary structural fact), **B ≈ 0.40** (frame/layer mismatch is the secondary), **A ≈ 0.03**, residual 0.02. The microscopic `w_n≤0` bound is what collapses A below transit's 0.10: A would require either a route-invariant shortfall (falsified by H²*) OR a substrate mechanism to lift `w_n>0` (falsified by Re:T3 Step 3).

**DISAGREE (one design choice in the S97 gate).** transit's leg-(i) "functional-fixing" proposes transporting the two-fluid `q_Ω(x)` with `x=ρ_s/ρ_n` built from each route's OWN decomposition — I AGREE this is the right primary. But the gate as written leaves the **A-PASS signature** under-constrained: it sets "A-PASS: even under fixed functional + fixed frame, q_Ω falls short of +0.81 by >band_tol=0.356 on ALL routes." That threshold (`0.356`) is the band-tolerance, and per my Re:T3 strengthening the two-fluid q at the physical fold deviates by only `0.1887 < 0.356` — so the gate as written would **never fire A-PASS at the physical x**, and could only fire it in the unphysical `x→0` tail. The A-PASS criterion must be re-scoped to the **physically-occupied x-window** (`x ∈ [x_fold, x_today]` from the substrate texture), not the full `x∈[0,∞)` scan. Otherwise A-PASS is testing the band-reproduction of an unphysical limit, which is the same mis-keying that produced the route-2 FAIL.

**My amendment to the S97 gate (co-authored, I own the two-fluid + EoS leg):**

| Field | Amendment |
|:--|:--|
| **A-PASS re-scope** | A-PASS fires ONLY if the route-invariant, frame-invariant q_Ω shortfall exceeds `band_tol` **on the physically-occupied x-window `x∈[x_fold=85.79, x_today]`** (substrate-texture-determined), NOT the full `x∈[0,∞)` scan. The `x→0` (vacuum-removed) tail is excluded — the substrate never realizes it. |
| **EoS-feature pre-registration** | Pre-register the `w_n∈[−0.4076,0]` substrate-allowed band (S67 ODLRO + flat-band Bogoliubov) as a FROZEN input. The gate may NOT scan `w_n>0` to chase +0.81 — that would be testing a non-substrate EoS (a PRU Class-8 / convention-shopping boundary). If a future computation derives a substrate-faithful `w_n>0` sub-mode, THAT is a separate gate; this gate freezes `w_n≤0`. |
| **Leg-(i) partition-extraction** | For each route, EXTRACT `(ρ_s,ρ_n)` from the route's own fields (route-1 AOFT: `ρ_s` from effacement residual `Γ_eff`, `ρ_n` from `T_relic`; route-3 GFT: `ρ_s,ρ_n` from condensate mean-field decomposition). Test cross-route agreement of `q_Ω(x_route)` to `max|Δq_Ω|<0.10`. PASS ⇒ C confirmed with the *partition* as the route-variable (my Re:T2 mechanism), strengthening transit's "different Ḣ" reading. |
| **Leg-(ii) conformal pushforward** | UNCHANGED from transit's spec — apply `Ω(τ)` (`Omega_BA_fold=2.241353`) to map R1's `a₂`-channel q `[−142,−17]` and check `frac_in_band>0.90`. PASS ⇒ B confirmed. |

**Why the amendment matters.** Without the A-PASS re-scope, the gate could spuriously fire A-PASS in the `x→0` tail and "reinstate" the SF54 band as a falsifier — a false positive driven by an unphysical limit. With the re-scope + the frozen `w_n≤0` band, the gate is genuinely three-way: C-PASS (partition-keyed agreement), B-PASS (frame pushforward), A-PASS (a *physical-window* invariant shortfall — which my Re:T3 predicts will NOT fire, but the gate can falsify me). This keeps it non-iterate-until-PASS while closing the unphysical-limit loophole.

**Substrate-first framing of the gate (my version).** The fabric IS its two intrinsic rates: the order-parameter texture-rate (Connes/coherence-length, SF54) and the acoustic deceleration-rate (`a₂`-channel q_Ω). The S97 gate asks: *is the acoustic deceleration-rate route-invariant once the superfluid/normal partition is fixed?* If yes (C), the divergence was the partition functions differing, and the SF54 band was an order-parameter-space object compared to an acoustic readout (B). If the gate finds a *physical-window* invariant shortfall (A), then the relic genuinely under-sources acoustic deceleration — which, given `w_n≤0` is microscopically forced, would point not at a closure bug but at the `G_eff=M_KK²/(16π·a₂)` dictionary or the `ρ_relic` band-weighting. I predict A does not fire; the gate is built to let it.

**My standing position into Round 2.** I concur the W1-1 PASS (`H²*`, never q-keyed) and the ≤0.04% three-route H²* triangulation are the robust, frame-invariant W1 deliverables for Atlas D04 C1/C2. The SF54 q-band is (i) an order-parameter-space proxy, not the acoustic `a_eff` (Re:T1); (ii) route/partition-dependent (Re:T2); (iii) unreachable above `+1/2` by the microscopic `w_n≤0` relic EoS, with the +½ ceiling itself *inside* band_tol of +0.81 and the two-fluid q inside the band at every physical x (Re:T3). Net: **B+C, A-falsified, route-2 FAIL re-tagged to a frame/target-mis-keying** — pending the amended S97 gate's verdict, which can still surprise us on A if a physical-window invariant shortfall survives frame-fixing.

### Part 2: Original Analysis

#### V1: Two-fluid q_Ω(x) and the w_n equation-of-state choice

**Claim: a positive-pressure normal component (`w_n>0`) is NOT physical for a diabatically-frozen integrable GGE relic. The substrate-allowed EoS band is `w_n∈[−0.4076, 0]`, closed below 0. This is a microscopic theorem at the level of the Equilibrium theorem, not a modeling choice — and it is what makes the +½ deceleration ceiling a substrate FEATURE.**

**The two-fluid construction is the substrate's own decomposition, not an imposed model.** The superfluid vacuum IS the substrate: the unbroken D_K condensate (`w_s=−1`, effacement `Γ_eff=0.99970`, DILUTION-CC-66). The "normal component" is not a second fluid moving IN spacetime — it is the **Bogoliubov-pair relic** left behind when the condensate texture `Δ(τ)` swept supersonically (Mach 13.75) through the van Hove fold at `tau_fold=0.190`: the reorganized eigenvalue spectrum, `n_pairs=59.8`, `P_exc=1.000` (saturated Parker pair-production). The two-fluid `q_Ω(x)=½(1−2x)/(1+x)`, `x=ρ_s/ρ_n`, is the emergent acoustic deceleration of how these two intrinsic substrate components partition the energy density. The W1-4 ODE cross-check (continuity residual `4.55e-13`) confirms `q_Ω(τ)` is the genuine two-fluid continuity solution, not a fitted ansatz — the route-2 analog of the §W1-1 emergent-Bianchi residual.

**The w_n equation-of-state: microscopic derivation of the `[−0.4076, 0]` band.**

The normal-component EoS is `w_n = p_n/ρ_n`, with both computed from the *frozen* relic occupation `|β_k|²`:
```
ρ_n = Σ_k E_k |β_k|²                         (Bogoliubov relic energy density = 26.553854 M_KK)
p_n = Σ_k (1/3) k (∂E_k/∂k) |β_k|²           (kinetic pressure of the quasiparticle gas)
w_n = p_n/ρ_n = ⟨(k/3)(∂E_k/∂k)/E_k⟩_|β|²    (occupation-weighted dispersion-stiffness ratio)
```

The sign and bound of `w_n` are fixed by WHERE the relic energy sits in the spectrum:

- **Relativistic modes** (`E_k≈k`, `∂E_k/∂k≈1`): contribute `w→+1/3`. Would lift the cap.
- **Non-relativistic / dust modes** (`E_k≈const+k²/2m`, `∂E_k/∂k≈k/m→0` at small k): contribute `w→0`. Cap at +½.
- **Flat-band / van Hove modes** (`∂E_k/∂k→0`, `E_k` near the gap floor): contribute `w<0` — a *negative-pressure* quasiparticle gas, the Khodel-Shaginyan flat-band signature.

**Why the relic energy is concentrated on the flat-band/van-Hove modes (the decisive step):** the Parker pair-production amplitude `|β_k|²` is largest where the adiabatic parameter is most violated, i.e. where the gap `Δ_k` is smallest and the band is flattest. `tau_fold=0.190` IS the van Hove singularity — the point of maximal density-of-states and minimal dispersion. So the relic is born with its energy *preferentially on the flat modes*, giving the S67 GGE-TWO-FLUID-67 value `w_GGE = P_vac/E_GGE = −0.4076` (nazarewicz-synthesis V.4; *"bare GGE w = −0.41 → w₀ = −0.918"*). The negative value is the substrate telling you the relic is a flat-band condensate, not a relativistic gas.

**Substitution chain (the upper edge of the band is `w_n=0`, and it is HARD):**
- Step 1: `w_n = ⟨(k/3)(∂E_k/∂k)/E_k⟩_{|β|²}`. Each term is `≥0` only if `∂E_k/∂k ≥ 0`; on the van Hove fold the dominant modes have `∂E_k/∂k ≈ 0` (flat) and the gap-floor modes can give `(∂E_k/∂k)<0` on the far side of the singularity (back-bending band), contributing negative pressure.
- Step 2: the maximum of `w_n` over physically-allowed occupations is achieved when ALL relic energy sits on marginally-dispersing dust modes (`∂E/∂k→0⁺`), giving `w_n→0⁺` — the ideal `Reading I` value.
- Step 3: to exceed `w_n=0` requires repopulating the *steep* relativistic modes. But the relic is FROZEN (`S_ent=0`, integrable, THE ORDERED VEIL — `R_therm=5251.82`, never thermalizes, W1-5 `BOUNCE_transfers=False`) and EFFACED (`Γ_eff=0.99970`). There is no interaction to scatter energy from flat to steep modes after the fold. The occupation `|β_k|²` is a fixed snapshot.
- Step 4 (read-off): `w_n ≤ 0` is FROZEN-IN. The substrate-allowed band is `w_n∈[−0.4076, 0]`: lower edge = S67 flat-band-dominated, upper edge = free-dust idealization. `q_n=½(1+3w_n)∈[−0.111, +0.5]`.
- Conclusion: **`w_n>0` is unphysical for the frozen GGE relic. The +½ deceleration ceiling is a substrate FEATURE.** This is the Equilibrium-theorem logic at the EoS level — just as the condensate ground-state energy does not gravitate (`ρ_Λ=0` at equilibrium, Volovik Paper 05), the frozen relic cannot manufacture positive pressure it was not born with.

**On the multi-component question (transit T3 Q2, answered fully).** A multi-component normal sector `ρ_n=Σ_i ρ_{n,i}` reaches `q>+1/2` IFF some sub-mode `i` carries energy-weighted `w_i>0` (Sage-exact: `q_sup=½+3/2·Σ_i g_i w_i` at `f_s=0`). The candidate positive-pressure sub-mode would be a **Leggett inter-band-coherence mode**. But the Leggett channel is precisely the DM-by-construction quasiparticle: CPT-neutral, non-annihilating, with `T^{0i}_4D=0` *algebraically* (S43/S44) ⇒ `v_fs=0` exact ⇒ **zero momentum-space current ⇒ zero kinetic pressure ⇒ `w=0`, not `w>0`**. So decomposing ρ_n into Leggett + Bogoliubov sub-modes partitions the *same* `w_n≤0` energy; no sub-mode supplies positive pressure. The single-scalar `w_n` does not "average away" a hidden positive-pressure component — there is none to average. The multi-component cap is identical to the single-w_n cap: `+1/2`.

**Net for V1.** The two-fluid q_Ω(x) is the correct substrate readout of acoustic-frame deceleration; the w_n EoS is microscopically pinned to `[−0.4076, 0]` by the frozen flat-band relic construction; and the +½ ceiling is a FEATURE, frame-independent (an EoS is a scalar `p/ρ`, invariant under `a→Ωa`). The route-2 FAIL is a band-reproduction verdict against a target that is both out-of-frame (Re:T1) and above the microscopic ceiling (here) — informative as a closed corridor, not a substrate deficit.

#### V2: The SF54 deceleration-band physics from the superfluid side

**Claim: the Connes-distance `a(τ)` (SF54) is NOT the physical scale factor a comoving substrate observer measures. It is the substrate's *order-parameter texture-distance* — the coherence-length / superfluid-stiffness readout. The acoustic `a_eff` (the `a₂`-channel) is what a comoving phonon clocks. The two are conformally distinct (`Omega_BA_fold=2.241353`) precisely because the superfluid density `ρ_s(τ)` is not the bare second moment `a₂(τ)`. The route-2 FAIL re-tags to a frame/target-mis-keying, NOT a substrate deficit.**

**What a comoving substrate observer actually measures.** In Volovik's superfluid-vacuum program there are two distinct metrics, and they are NOT interchangeable:

1. **The acoustic metric `g_M`** — built from the second spectral moment `a₂` (`G_N=M_KK²/(16π·a₂)`). Quasiparticle excitations (phonons, the relay patterns that ARE particles) propagate on `g_M`. A *comoving acoustic observer* — anything built from substrate excitations, i.e. any physical clock or ruler — measures proper distances in `g_M`, hence redshifts as `λ∝a_eff` where `a_eff²=a₂(τ)/a₂_today`. **This is the physical, observable scale factor.**

2. **The order-parameter-space distance `⟨d_D⟩(τ)`** (the Connes distance, SF54) — measures how far the condensate texture `Δ(τ)` has moved in the configuration space of D_K. This is the superfluid analog of **coherence-length growth** `ξ(τ)∝ħv_F/Δ(τ)`: it tracks the *internal* reorganization of the order parameter, not the proper distance any excitation traverses.

A comoving observer is made of phonons; phonons live on `g_M`; therefore the observer measures `a_eff`, full stop. The Connes distance is real substrate structure but it lives at a *different layer* — the Level-2 moduli-deformation layer (`phononic-framing.md §"Single-τ-slice vs moduli-deformation substrate-IS levels"`), the space of Jensen TT-deformations, not the single-τ acoustic geometry an observer inhabits.

**Why they are conformally distinct — the superfluid microscopic reason for `Omega_BA_fold=2.241353`.**

**Substitution chain (the conformal factor IS `ρ_s/a₂`):**
- Step 1: `a_eff²(τ) = a₂(τ)/a₂_today` — the acoustic scale factor, from the EH-generating moment.
- Step 2: `a_Connes(τ) = ⟨d_D⟩(τ)/⟨d_D⟩(0)` — the order-parameter distance. In two-fluid hydrodynamics, the order-parameter-space line element is weighted by the **superfluid density** `ρ_s(τ)` (the stiffness against texture gradients: `E_grad ∝ ρ_s|∇Δ|²`), so `⟨d_D⟩ ∝ ∫√(ρ_s(τ)) dτ` along the texture path.
- Step 3: the conformal factor relating them is `Ω(τ) = a_Connes/a_eff ∝ √(ρ_s(τ))/√(a₂(τ))`. This is `≠const` **iff** `ρ_s(τ)/a₂(τ) ≠ const` — i.e. iff the superfluid density flows differently from the bare second moment. It does: `ρ_s` is depleted by the normal-component relic (`ρ_s = ρ_tot·x/(1+x)`), while `a₂` is the full spectral moment. At the fold the depletion is dramatic (`x_fold=85.79`, so `ρ_s/ρ_tot=0.988` but the *rate* of change differs from `a₂`'s).
- Step 4 (read-off): `Omega_BA_fold=2.241353 ≠ 1` is the gate-level fingerprint of `ρ_s(τ)/a₂(τ) ≠ const` — the superfluid density and the bare moment are conformally inequivalent because the relic depletes the condensate stiffness. `proxies_conformally_distinct=True` is the substrate saying: *the texture-distance and the acoustic-distance disagree because the superfluid fraction is not the energy density.*
- Conclusion: **the SF54 band is the q-range of the texture-distance proxy (R2/Connes), NOT the acoustic proxy (R1/`a_eff`) a comoving observer measures.** transit's Reading B is confirmed from the microscopic side, and sharpened: the mismatch is `ρ_s ≠ a₂`, a defining two-fluid relation, not an accidental conformal factor.

**The re-tag, stated precisely.** The route-2 verdict `S96-W1-VOLOVIK-2FLUID: FAIL` (`audit 65c41afd…`) keyed `magnitude=FAIL` off band-reproduction of `[−0.97,+0.81]`. That band is an R2 order-parameter-space object. The two-fluid `q_Ω(x)` is an R1 acoustic-frame object (it triangulates route-1's `a₂`-channel `H²*` to rel `3.77e-4`). **Comparing them is the layer/frame mismatch of Re:T1.** The substrate-faithful reading:
- The two-fluid `q_Ω(x)` is the CORRECT acoustic-frame deceleration readout (`sign=PASS`, ODE-consistent `4.55e-13`, `H²*` triangulated).
- At every physically-occupied x (`x∈[x_fold, x_today]`, all ≫0, vacuum-dominated), the two-fluid q sits INSIDE band_tol (W1-4: `dev_fold=0.1887 < band_tol 0.356`).
- The band-reproduction FAIL is driven by the unphysical `x→0` tail and by the R2-vs-R1 frame mismatch.

So the substrate-first tag is: **`PASS-with-frame-caveat`** — the two-fluid q is the correct `a₂`-channel readout; the SF54 band is the wrong (order-parameter-space, conformally-distinct) target. For Atlas D04: register the route-invariant `H²*` (≤0.04%) as the C1 deliverable; carry the SF54 q-band as `STATUS: order-parameter-space-proxy (Connes/coherence-length), conformally-distinct from acoustic a_eff by Ω(τ)∝√(ρ_s/a₂) — NOT a standing falsifier; see S95-W4-4 + S96-W1 Re:T1/V2`.

**Substrate-first framing (the whole V2 in one arrow).** `D_K(τ) → {a₂(τ), ρ_s(τ)} → two distinct metrics: g_M (acoustic, a_eff, what observers measure) AND order-parameter-space (Connes distance, SF54, texture-rate) → q_Ω read in g_M is the physical deceleration; the Connes-q is the texture-deceleration → they differ by Ω(τ)=√(ρ_s/a₂) because the relic depletes the condensate stiffness.` The fabric IS both rates; only the acoustic one is what a comoving phonon clocks. The SF54 band measures the texture-rate; the two-fluid q measures the acoustic-rate; the FAIL is the substrate distinguishing its own two intrinsic clocks.

#### V3: Questions for transit

**Q1 (the partition-extraction leg — does AOFT even admit a ρ_s/ρ_n split?).** My Re:T2 mechanism claims q_Ω is route-dependent *because the superfluid/normal partition is route-dependent*, and my amended S97 leg-(i) asks each route to EXTRACT its own `(ρ_s,ρ_n)`. For route-1 AOFT: your action is a single scalar-tensor `S_eff[g_M,φ]` with `φ(τ)=f₂Λ²a₂/48π²` — there is no explicit `w=−1`/`w=0` two-fluid split; the relic enters as one `T_relic^{μν}`. **Can you decompose `T_relic^{μν}` into a `w_s=−1` effacement-residual piece (`ρ_s` from `Γ_eff=0.99970`) and a `w_n` GGE piece (`ρ_n` from the Bogoliubov `Σ_k E_k|β_k|²`)?** If AOFT's `T_relic` does NOT admit a clean `(ρ_s,ρ_n)` split, then the "transport the two-fluid q onto route-1's H(τ)" leg is ill-defined, and the route-invariance test reduces to route-2-vs-route-3 only. I need to know whether your AOFT `T_relic` carries the partition information or has already traced it out into a single effective fluid.

**Q2 (the conformal pushforward — is q_Ω(R1) actually frame-mappable into the band?).** Your leg-(ii) proposes applying `Ω(τ)` (`Omega_BA_fold=2.241353`) to push R1's `a₂`-channel q `[−142,−17]` into `[−0.97,+0.81]` and checking `frac_in_band>0.90`. But a conformal map of the deceleration is `q→q' = f(q,Ω,Ω̇,Ω̈)` — it requires `Ω̇` and `Ω̈`, not just `Ω_fold`. **Do you have `Ω(τ)` as a *function* (with derivatives) from S95-W4-4, or only the single fold-value `Omega_BA_fold=2.241353`?** If only the fold-value, leg-(ii) cannot be executed as a true pushforward (you'd be conformally rescaling with a constant Ω, which does NOT change q — a constant conformal factor leaves `ä/a` form-invariant). The B-PASS criterion needs the full `Ω(τ)` profile. Is it in `s95_w4_4_sp_conformal_embed.npz`?

**Q3 (the W6-18 multiplicative-cancellation analogy — is q_Ω a "log-derivative" that cancels the route weighting, or not?).** You drew the parallel to `math-scripts.md §"Multiplicative-normalization cancellation"`: a quantity built as a log/ratio/second-derivative of a shared object can be frame-invariant (driver-like) or frame-dependent (f-coefficient-like). My Re:T2 says q_Ω is partition-keyed (route-dependent) because it depends on the *ratio* `x=ρ_s/ρ_n`. **But here is the tension:** the W6-18 discipline says a *ratio* of a shared object is often *invariant* (the a-ratio driver cancels `w(L_max)`). q_Ω is a ratio-functional. Why does q_Ω NOT enjoy the same cancellation? My answer: because `ρ_s` and `ρ_n` are NOT a common multiplicative pre-factor times a shared kernel — they are two *independent* thermodynamic functions (energy density and superfluid fraction are independent in two-fluid theory, V1). **Do you concur that the q_Ω route-dependence is structurally the FUNCTIONAL-DEPENDENT case (like the f-coefficient ratio, S96-SDW-EFT-CONTROL: same D_K → FAIL/INFO/PASS), not the FUNCTIONAL-INVARIANT driver case?** If so, that pins the W6-18 analogy precisely and feeds the S97 leg-(i) pre-registration: we should expect cross-route q_Ω agreement ONLY when the partition `(ρ_s,ρ_n)` is held fixed, never as an automatic cancellation.

**Q4 (the A-PASS re-scope — do you accept excluding the x→0 tail?).** My Re:T4 amendment re-scopes A-PASS to the physically-occupied x-window `[x_fold=85.79, x_today]`, excluding the unphysical `x→0` (vacuum-removed) tail where the band-reproduction FAIL actually lives. **Do you accept this re-scope, or do you read the full `x∈[0,∞)` scan as the legitimate test domain?** The stakes: with the full scan, A-PASS can spuriously fire in the `x→0` tail and "reinstate" the SF54 band as a falsifier — a false positive driven by a limit the substrate never realizes (at the fold `x=85.79≫0`). With the re-scope, A-PASS only fires on a *physical-window* invariant shortfall, which I predict will NOT fire (because `w_n≤0` caps q at +½ frame-independently, and the two-fluid q is inside band_tol at every physical x). I want to know if you see the `x→0` tail as physical or as the unphysical idealization I claim it is.

---

## Round 2 — transit: Follow-up

### CONVERGENCE

volovik's R1 response moved three things from my "leaning" column to "settled," and one of them I was wrong to leave open. I record what changed.

**(1) The microscopic `w_n ≤ 0` bound closes T3 harder than I had it — and removes my residual.** In T3/T4 I argued the +½ ceiling is a w-split FEATURE but left a 0.05 residual for "frame-fixing reveals a partial genuine gap" (T4: B+C≈0.85, A≈0.10, residual 0.05). volovik's V1 Step-3 substitution chain kills that residual at the microscopic level, and I now accept it. The decisive move is the dispersion-stiffness derivation of the EoS:
```
w_n = ⟨(k/3)(∂E_k/∂k)/E_k⟩_{|β_k|²}   (occupation-weighted dispersion-stiffness ratio)
```
This is exactly my own GGE-relic picture made quantitative. I had asserted (T3 connection-to-research) that "there is no mechanism in an effaced, non-interacting relic to generate positive pressure" — but I stated it as a plausibility, not a bound. volovik converts it to a bound by locating WHERE the relic energy sits: the Parker amplitude `|β_k|²` peaks where the adiabatic parameter is most violated, i.e. where the gap `Δ_k` is smallest and `∂E_k/∂k → 0`. `tau_fold = 0.190` IS the van Hove singularity (my own substrate anchor, MEMORY) — so the relic is BORN with its energy on the flat-`∂E/∂k` modes, forcing `w_n ≤ 0`. This is the transit-dynamics content I should have pushed myself: the Bogoliubov output state of a supersonic sweep through a van Hove flat band is a negative-/zero-pressure quasiparticle gas BY CONSTRUCTION, because `|β_k|²` and `∂E_k/∂k` are anti-correlated across the fold (large pair-production exactly where the band is flat). I concur. `w_n ∈ [−0.4076, 0]` is the substrate-allowed band, closed above at the free-dust idealization. The residual collapses; I move to volovik's weighting on this axis.

**(2) The +½ ceiling is INSIDE band_tol of +0.81 — I MISSED this, and it changes the FAIL's character.** volovik's Re:T3 STRENGTHENING is the single most important number I did not compute. I read the route-2 FAIL as "+0.81 sits above the entire span, full stop" (T3). volovik computed the *gap*, and I have re-verified it exact this turn (exact-rational, `Fraction`):
```
+0.81 − (+1/2) = 31/100 = 0.31000…   ;   SF54 band_tol = 0.356
0.31 < 0.356  ⇒  the two-fluid supremum +1/2 is INSIDE band-tolerance of the SF54 upper edge.
```
And the fold-point check confirms it physically: W1-4 verdict line `dev_fold = 0.1887 < band_tol 0.356` (`audit 65c41afd…`) — at the physically-occupied fold `x_fold = 85.7928`, the two-fluid q deviates from the SF54 q by only 0.19, deep inside tolerance. I have also re-verified the x-window structure this turn: `q(x→0) = +0.5` exact, `q(x_fold=85.7928) = −0.98272` (reproducing the verdict-line `q_I_fold=−0.9827`). So the band-reproduction FAIL is NOT a gross substrate deficit — it is a **near-miss confined to the unphysical `x→0` (vacuum-removed) tail**. At every physically-occupied x (all ≫0, vacuum-dominated), the two-fluid q is INSIDE the band. This is what re-tags the route-2 verdict (Q3/T3 Q3): the FAIL is keyed against a target that is both out-of-frame (Re:T1/V2) AND above the microscopic ceiling AND only missed in a limit the substrate never realizes. I accept the re-tag to `PASS-with-frame-caveat`.

**(3) The physical frame IS decidable from the two-fluid side — I framed it as open; it is not.** My T1 closing question asked "is the Connes-distance `a(τ)` the physical emergent scale factor, or is the a₂-channel `a_eff`?" and I treated it as a genuine tie pending volovik's input. volovik's Re:T1/V2 substitution chain decides it, and I concur: the physical, comoving-observer scale factor is `a_eff` (the a₂-channel), because *a comoving observer is built of phonons, phonons propagate on `g_M`, and `g_M` is the a₂-metric* (`G_N = M_KK²/(16π·a₂)`). A phonon's proper wavelength redshifts as `λ ∝ a_eff`, never as the Connes distance. The Connes distance `⟨d_D⟩(τ)` is a perfectly good substrate-IS observable but it lives at the order-parameter / Level-2-moduli layer (the coherence-length / texture-rate `ξ(τ) ∝ ħv_F/Δ(τ)`), NOT the kinematic-acoustic layer a clock-carrying observer inhabits. This is the sharper version of my Reading B: the SF54 band is not merely in the wrong *conformal frame* — it is the wrong *layer* (order-parameter-space vs acoustic). I adopt volovik's sharpened down-tag wording for the capstone (`order-parameter-space-proxy (Connes/coherence-length), conformally-distinct from acoustic a_eff by Ω(τ)∝√(ρ_s/a₂)`), which is stronger than my T1 `conformal-frame-conditional`.

**(4) The A-PASS re-scope is correct — and my T4 gate as written had the loophole volovik flagged.** volovik's Re:T4 caught a real defect in my S97 A-PASS criterion. I wrote "A-PASS: even under fixed functional + fixed frame, q_Ω falls short of +0.81 by > band_tol = 0.356 on ALL routes." But per the strengthening above, the two-fluid q at the physical fold deviates by only 0.1887 < 0.356 — so my criterion would NEVER fire A-PASS at physical x and could fire it ONLY in the `x→0` tail. That is the SAME mis-keying that produced the route-2 FAIL, smuggled into the discriminating gate. I accept volovik's amendment: A-PASS must be re-scoped to the physically-occupied x-window `x ∈ [x_fold=85.79, x_today]`, excluding the `x→0` tail. I answer Q4 in full below (I accept the exclusion, with one sharpening on how `x_today` is pinned).

**Net convergence.** I concur with volovik's weighting **C ≈ 0.55, B ≈ 0.40, A ≈ 0.03** (residual 0.02), MOVING DOWN from my T4 A≈0.10. The microscopic `w_n ≤ 0` bound — which is a transit-dynamics statement about the Bogoliubov output state, not merely a Volovik EoS choice — is what collapses A below 0.10. A would require either a route-invariant shortfall (falsified by H²* at ≤0.04%) OR a substrate mechanism to lift `w_n > 0` (falsified by the van-Hove-flat-band Bogoliubov construction). Neither survives. The robust, frame-invariant W1 deliverable is the H²* magnitude triangulation (≤0.04%, three routes); the SF54 q-band is an order-parameter-space proxy, route/partition-dependent, with its +½ acoustic ceiling inside band_tol of the proxy edge.

### DISSENT

I have NO substantive physics dissent remaining on Topics 1–3. My residual disagreements are two, and both are scope/methodology, not substrate-physics:

**(D1) On the C/B split: I read C and B as less cleanly separable than the 0.55/0.40 numbers suggest — they are two projections of one fact, and the weighting is a presentational convenience, not two independent probabilities.** volovik splits C ≈ 0.55 (partition/route-dependence primary) and B ≈ 0.40 (frame/layer mismatch secondary). I agree with the ORDERING and I am happy to publish those numbers, but I want the record to state that B and C are not orthogonal events whose probabilities should be read additively. Here is the substitution chain for why they are the same fact viewed two ways:
- Step 1 (C, route-dependence): q_Ω is route-dependent because it is partition-keyed — it depends on `x = ρ_s/ρ_n`, and each route models the superfluid/normal partition differently (volovik's Re:T2). Route-1 AOFT traces the partition out entirely (Q1 below); route-3 GFT collapses both into one condensate mean-field (`q_GFT` reaches +1.24, no `w=−1`/`w=0` split); route-2 builds it explicitly.
- Step 2 (B, frame mismatch): the SF54 band is the q-range of the Connes/order-parameter proxy, conformally distinct from the acoustic `a_eff` by `Ω(τ) ∝ √(ρ_s/a₂)` (volovik's V2 Step-3).
- Step 3 (the shared root): BOTH the route-dependence (C) and the conformal factor (B) trace to the SAME structural fact — `ρ_s(τ)/a₂(τ) ≠ const`. The partition `x = ρ_s/ρ_n` is route-dependent BECAUSE `ρ_s` (superfluid density, depleted by the relic) flows differently from `a₂` (bare second moment); and `Ω ∝ √(ρ_s/a₂) ≠ 1` is the conformal fingerprint of the SAME inequality. `proxies_conformally_distinct=True` and "q_Ω is partition-keyed" are one substrate statement: *the superfluid fraction is not the energy density.*
- Conclusion: C and B are not two independent reasons; they are the second-moment (partition, C) and the metric (conformal, B) shadows of `ρ_s ≠ a₂`. The 0.55/0.40 split is a fine way to say "route-dependence is the more fundamental shadow," and I publish it — but the registry text should say B+C ≈ 0.95 is the load-bearing number, and the internal split is a reading-emphasis, not a joint distribution. This matches the `single-observable-per-triple` discipline (`cross-pillar-bridge-anatomy.md`): distinct functionals are DIAGNOSTIC sub-rows of one observable, not co-primary independent entries.

**(D2) On "decidable" — I accept `a_eff` is the physical acoustic scale factor, but I flag that this DECISION interacts with the still-open C1 a(t) gap, and is not free of a downstream caveat.** volovik says the frame question is "decidable, not a tie," and I concur for the *comoving-observer* question. But "decidable" here means "the substrate has TWO intrinsic rates and we have correctly identified which one a phonon clocks" — it does NOT mean the emergent FRW `a(t)` is closed. The W1-1 verdict explicitly carries `a_t_magnitude=INFO_pending_Znorm_V0_gate7_seconds_gate3_ONeill_gate2` (`audit edfe1f7f…`), and the W1 carry-forward is precisely "integrate `a(t)` in physical seconds." So the decision "the physical scale factor is `a_eff`" is correct at the *which-metric* level but the `a_eff(t)` *trajectory* is INFO-pending the {Z_norm, V0} + seconds + O'Neill pins (the latter two now closed: W1-3 seconds `8.86044e-42 s`; O'Neill cross-terms effaced at the Hubble scale, `ratio_Hubble=6.84e-117 ≪ 3e-7`). My narrow dissent: the workshop should NOT let "the frame is decided" be read as "the C1 effective-Friedmann a(t) gap is closed." It closes the *frame-identity* sub-question; the *trajectory* is the S97-A1 carry-forward. This is a capstone-hygiene point (Q3 of the 5-question gate fires: the a(t)/effective-Friedmann gap status is touched) — the prose tag must distinguish "frame identified (`a_eff`)" from "a(t) trajectory pending."

Beyond D1/D2 I have no dissent. On the central physics — `w_n ≤ 0` FEATURE, +½ inside band_tol, `a_eff` physical, A-falsified, route-2 re-tag — I converge fully.

### EMERGENCE

The cross-pollination produced three insights neither side had alone in Round 1.

**(E1) The route-dependence of q_Ω IS the superfluid/normal partition being route-specific — and that re-reads my W6-18 analogy as a partition statement, not a "different-Ḣ" statement.** In T2 I attributed the q_Ω divergence to "each formalism supplies a different Ḣ structure." volovik's Re:T2 sharpened this: the deeper cause is that H²* is a first-moment (energy-density) readout — partition-BLIND, depending only on `ρ_tot = ρ_s + ρ_n` (the route-invariant Bogoliubov sum `Σ_k E_k|β_k|² = 26.5539`) — while q_Ω is a second-moment readout — partition-KEYED, depending on the fractions `f_s, f_n`, hence on the ratio `x = ρ_s/ρ_n`. The emergent synthesis: **"different Ḣ" and "different partition" are the same statement, because Ḣ/H² in a two-fluid is fixed by the partition.** `q_Ω = ½[1 + 3(f_s w_s + f_n w_n)]` — the Ḣ structure IS the partition-weighted EoS. My "different Ḣ frames" was the kinematic surface; volovik's "different partition functions" is the thermodynamic root. This is a strictly better statement of Reading C, and it is FALSIFIABLE in a way mine was not: it predicts *any two routes that adopt the same `(ρ_s, ρ_n)` will agree on q_Ω regardless of their Ḣ formalism* — which is exactly the S97 leg-(i) test.

**(E2) The conformal-frame question and the C1 a(t) gap are coupled through `Ω(τ) ∝ √(ρ_s/a₂)` — and this is why C1 cannot be closed by H²* alone.** Here is the new cross-domain structure. The H²* triangulation (≤0.04%) closes the *first-order* effective-Friedmann existence+magnitude (C1's "does an emergent Friedmann equation exist with the right H²"). But the *trajectory* `a_eff(t)` — and hence q_Ω, the deceleration — requires the conformal relation between the order-parameter-space distance (what SF54/S54 actually computed: `a(τ_fold)/a(0)=2.117` from the Connes distance) and the acoustic `a_eff`. That relation is `Ω(τ) ∝ √(ρ_s(τ)/a₂(τ))`. So: **the historical SF54 a(τ) was computed in the order-parameter frame; the physical a(t) trajectory needs the acoustic frame; the two are bridged by `Ω(τ)`, which is `≠const` precisely because the relic depletes `ρ_s` relative to `a₂`.** The C1 a(t) gap is therefore NOT "we have H² but haven't integrated it" — it is "we have H² in the acoustic frame and a(τ) in the order-parameter frame, and closing the trajectory requires the full `Ω(τ)` profile that bridges them." This is why volovik's Q2 (is the full `Ω(τ)` available?) is load-bearing for BOTH the S97 deceleration gate AND the C1 a(t) closure: the same `Ω(τ)` profile that would let leg-(ii) pushforward R1's q into the band is the profile that converts the S54 order-parameter a(τ) into the physical acoustic a(t). One missing object blocks two closures.

**(E3) The substrate carries a partition-INDEPENDENT second-order invariant — but it is NOT q_Ω, and naming it sharpens the Atlas D04 entry.** volovik's Re:T2 "MISSED" observation, cross-pollinated with my T2 FUNCTIONAL-INVARIANT/FUNCTIONAL-DEPENDENT split, yields a clean statement. There IS a route-invariant second-order observable hiding here: q_Ω evaluated at the *substrate-derived* `x(τ)` (the S67 ODLRO `x_fold = 85.7928`) IS route-invariant — IF all routes adopt that x. The divergence (+0.50 / +1.24 / OUT) comes entirely from routes 1/3 NOT using the two-fluid partition. So the Atlas D04 C1 entry should read: the route-invariant deliverables are (i) H²* (first-order, partition-blind, ≤0.04%) AND (ii) q_Ω *at fixed substrate x(τ)* (second-order, partition-fixed) — and the *apparent* q_Ω divergence is the FUNCTIONAL-DEPENDENT image of the routes disagreeing on the partition, exactly the cosmological analog of S96-SDW-EFT-CONTROL (same D_K spectrum → Gaussian FAIL / Mellin PASS / a-ratio-driver INFO; `audit 7d62a904…`/`74158937…`). The Atlas entry gains a structural distinction it lacked: "q_Ω diverges across routes" is not a back-reaction finding, it is a partition-resolution finding — the routes are computing the same observable at different partitions, and the observable is partition-keyed.

### QUESTIONS

I answer volovik's V3 Q1–Q4 directly, each grounded in the source artifacts I inspected this turn, then pose two sharper follow-ups for his final (R2-B) turn.

**A1 — does AOFT's `T_relic` admit a `(ρ_s, ρ_n)` superfluid/normal split? ANSWER: NO as computed; YES only as a post-hoc re-derivation from canonical pins (a separate extraction step).** I inspected the route-1 npz directly this turn (`s96_w1_aoft_friedmann_map.npz`, 53 fields). The relic enters as a SINGLE effective `T_relic^{μν}`, with `ρ_relic` decomposed as:
```
rho_relic_MKK = 26.5539 = rho_contrib_B1 + rho_contrib_B2 + rho_contrib_B3
              = 2.7792 + 21.8876 + 1.8871
```
These B1/B2/B3 contributions are the **three Bogoliubov mode-band stages** (the 3-stage ladder: pre-fold → post-fold WKB → horizon-exit composition), NOT a `w_s=−1` / `w_n=0` superfluid/normal partition. There is NO `gamma_eff`, `w_n`, `w_s`, `rho_s`, `rho_n`, `superfluid`, or `normal` field anywhere in the 53-field npz. The only EoS-adjacent field is `beta_T_linear_order=True` (the T3 Scalar-Tensor Kasparov decoupling order, NOT an equation-of-state). So you are exactly right: **the AOFT action has already traced the partition out into one effective fluid** — `w_relic` is absorbed into the scalar-tensor `φ(τ) = f₂Λ²a₂/48π²` dynamics, and the on-shell `∇_μ T_relic^{μν}=0` (emergent-Bianchi residual `0.000e+00`) holds for the *total*, partition-blind.

This does NOT make leg-(i) ill-defined, but it makes it a TWO-STEP construction for route-1, not a field read: I can re-derive `(ρ_s, ρ_n)` post-hoc from canonical substrate pins — `ρ_s` from the effacement residual (`Γ_eff = 0.99970`, DILUTION-CC-66: `ρ_s = Γ_eff · ρ_tot`, the unbroken-condensate fraction) and `ρ_n = (1−Γ_eff)·ρ_tot + ρ_GGE` from the Bogoliubov sum — and then evaluate `q_Ω(x_route1)`. This is admissible because the inputs (`Γ_eff`, `Σ_k E_k|β_k|²`) are canonical and route-invariant; it is the SAME `ρ_relic = 26.5539` repartitioned, not new physics. But the S97 plan-block MUST pre-register this as an EXTRACTION step (route-1 `(ρ_s,ρ_n)` is DERIVED, not loaded), with its own substitution chain, so it is not mistaken for a field already in the npz. **Bottom line for your final turn: the route-invariance test is NOT reduced to route-2-vs-route-3 — route-1 admits the partition by post-hoc extraction from `Γ_eff` — but the partition is NOT in the AOFT `T_relic` as computed; it was traced out, and must be reconstructed.**

**A2 — is the full `Ω(τ)` profile (with derivatives) available, or only `Omega_BA_fold=2.241353`? ANSWER: only the fold-value scalar + a `Omega_derivable_1plus1D` flag; the full 4D `Ω(τ)` profile with `Ω̇, Ω̈` is NOT exported. Your concern is correct and it has a sharp consequence.** I traced the gate this turn. `Omega_BA_fold=2.241353` is gate-LOCAL — it is NOT in `canonical_constants.py` (`get_constant("Omega_BA_fold")` → not found), so it lives only inside `s95_w4_4_sp_conformal_embed.npz`. The gate's own verdict reason (knowledge MCP, verbatim) is:
```
verdict_reason = Omega_derivable_1plus1D AND fold_4D_causal_feature
                 BUT a2_proxy_q_Omega_OUT_of_band ... only_Connes_proxy_reproduces_band
                 AND M_KK_inv_to_s_normalization_OPEN
```
So the gate established (i) `Ω` is DERIVABLE in the 1+1D acoustic reduction, (ii) the fold is a 4D causal feature, (iii) the proxy distinction — but it did NOT export a full 4D `Ω(τ)` *function* with derivatives. You have flagged the decisive point: **a conformal map of the deceleration is `q → q' = f(q, Ω, Ω̇, Ω̈)`, and a CONSTANT `Ω` leaves `ä/a` form-invariant** (substitution: under `a → Ωa` with `Ω̇=Ω̈=0`, `ä/(Ω a) · Ω = ä/a`, so q is unchanged). Therefore leg-(ii) as written in my T4 **cannot be executed** with only `Omega_BA_fold` — conformally rescaling R1's q by a single constant `2.241353` would NOT move it from `[−142,−17]` into `[−0.97,+0.81]`; it would leave q exactly where it is. **The B-PASS criterion is currently NOT executable.** Consequence for the S97 plan-block: leg-(ii) is BLOCKED pending a prerequisite computation that exports the full `Ω(τ)` profile (with `Ω̇, Ω̈`) from the substrate spectrum — the relation `Ω(τ) ∝ √(ρ_s(τ)/a₂(τ))` you derived in V2 Step-3 is exactly the closed form to compute it, since both `ρ_s(τ)` (from the two-fluid `x(τ)`) and `a₂(τ)` (from the D_K spectral moment) are available as τ-functions. So I propose leg-(ii) be SPLIT: a prerequisite gate `S97-?-OMEGA-PROFILE` (export `Ω(τ), Ω̇, Ω̈` via `√(ρ_s/a₂)`) FEEDS the pushforward gate. Without it, B-PASS is INFO-pending, and the workshop verdict on B (≈0.40) rests on the *existence* argument (proxies conformally distinct, fold-value `≠1`) rather than a demonstrated pushforward-into-band. I flag this as a carry-forward, not a Round-2 blocker.

**A3 — confirm q_Ω is the FUNCTIONAL-DEPENDENT case per W6-18, NOT the FUNCTIONAL-INVARIANT driver case? ANSWER: CONFIRMED — q_Ω is FUNCTIONAL-DEPENDENT, and your `ρ_s/ρ_n`-are-independent-thermodynamic-functions argument is exactly why the multiplicative cancellation does NOT apply.** This pins the W6-18 analogy precisely, and I concur with your resolution of the tension. The substitution chain, with the cancellation criterion stated exactly (`math-scripts.md §"Multiplicative-normalization cancellation invariants"`):
- Step 1 (when cancellation DOES apply): the a-ratio driver `a_{2(k+1)}/a_{2k}` is FUNCTIONAL-INVARIANT because the L_max truncation enters as a MULTIPLICATIVE pre-factor `w(L_max)` times an L_max-INDEPENDENT kernel `g(K)`: `f^{(L_max)} = w(L_max)·g(K)`, so the ratio `w·g_{k+1} / w·g_k = g_{k+1}/g_k` annihilates `w`. The log-derivative `d ln f/d ln K` likewise kills any multiplicative `w`.
- Step 2 (why q_Ω does NOT factor this way): q_Ω is a ratio-FUNCTIONAL of `(ρ_s, ρ_n)`, but `ρ_s` and `ρ_n` are NOT `w(·)·g(K)` — a common pre-factor times a shared kernel. They are two INDEPENDENT thermodynamic functions: in two-fluid theory the energy density `ρ_tot` and the superfluid fraction `ρ_s/ρ` are independent functions of state (you cannot derive `ρ_s/ρ` from `ρ_tot` alone — you need the order-parameter texture). So `q_Ω = ½[1 + 3(f_s w_s + f_n w_n)]` carries the partition as genuinely independent content; there is no common multiplicative factor to cancel.
- Step 3 (read-off): the cancellation theorem requires `O = w(param)·g(K)` factorization; q_Ω fails the factorization premise because its two arguments are independent. Therefore q_Ω is FUNCTIONAL-DEPENDENT — the cosmological image of the f-coefficient ratio in S96-SDW-EFT-CONTROL, where the SAME D_K spectrum gives Gaussian `f₂/f₄=4.19` → FAIL-band and Mellin `f₂/f₄=0.033` → PASS-band (`audit 7d62a904…` superseded by `74158937…`), precisely because the f-coefficient weighting does NOT cancel.
- Conclusion: **q_Ω is FUNCTIONAL-DEPENDENT (partition-keyed), NOT FUNCTIONAL-INVARIANT (driver-like). CONFIRMED.** The S97 leg-(i) pre-registration follows directly: expect cross-route q_Ω agreement ONLY when the partition `(ρ_s, ρ_n)` is held fixed across routes — NEVER as an automatic cancellation. The "different functionals on the same D_K give FAIL/INFO/PASS" pattern is the SAME structural phenomenon at the cosmological readout layer. This is the cleanest possible statement of why the route-2 FAIL is not a substrate deficit: it is a functional-dependence artifact, the cosmological twin of the EFT-control scheme-dependence.

**A4 — accept the `x→0`-tail exclusion in the S97 gate? ANSWER: YES, accepted, with one sharpening on how `x_today` is pinned.** I accept the A-PASS re-scope to the physically-occupied x-window `x ∈ [x_fold=85.79, x_today]`, excluding the `x→0` (vacuum-removed) tail. The `x→0` limit is unphysical for the substrate: it is the configuration with ZERO superfluid fraction (all energy in the normal/GGE relic, no condensate), which the substrate never realizes — at the fold `x_fold=85.7928` (the relic is a 1.16% perturbation on a vacuum-dominated condensate, `ρ_n_frac_star=2.4831e-03` from the W1-4 detail row), and the substrate only moves DEEPER into vacuum-domination as the relic redshifts. The substitution chain confirms the FAIL lives entirely in the excluded tail: `q(x→0) = +0.5` (the band-reproduction supremum, unreachable above) vs `q(x_fold=85.79) = −0.98272` (deep in-band, `dev_fold=0.1887 < band_tol`). Every physically-occupied x yields q inside the band; only the `x→0` idealization approaches the +½ ceiling that misses +0.81. So the band-reproduction FAIL is a property of a limit the substrate does not occupy — exclude it.

**My sharpening on `x_today`:** the re-scope window `[x_fold, x_today]` needs `x_today` pinned from substrate texture, not chosen. As the relic redshifts (normal component `ρ_n ∝ a^{−3}` for `w_n≈0`, or steeper for `w_n<0`) while the condensate vacuum stays `w_s=−1` (`ρ_s ≈ const`), `x = ρ_s/ρ_n` GROWS monotonically from `x_fold=85.79` toward `x_today ≫ x_fold` (more vacuum-dominated today). So the physical window is `[85.79, x_today]` with `x_today` LARGE — entirely in the `q ≈ −1` (vacuum-dominated, accelerating) regime, FAR from both +0.5 and +0.81. This makes the A-PASS exclusion even cleaner than the fold-value alone suggests: the ENTIRE physical x-window sits at `q ≈ −1`, and the band-reproduction "reach toward +0.81" is a property of a `x→0` past the substrate never had AND a future it never reaches. I propose the S97 plan-block pin `x_today` from the redshift evolution of `ρ_n/ρ_s` over `[τ_fold, τ_now=0.6]` (the W1 physical window), not as a free scan endpoint — this closes the loophole structurally (the window is substrate-texture-determined) rather than by fiat.

**Two sharper follow-ups for your final (R2-B) turn:**

**(F1) The `Ω(τ) ∝ √(ρ_s/a₂)` closed form — can you commit it as the substrate-natural export for the prerequisite `S97-?-OMEGA-PROFILE` gate, and does it carry the `Ω̇, Ω̈` the pushforward needs?** Per A2, leg-(ii) is blocked on the full `Ω(τ)` profile. Your V2 Step-3 gives `Ω(τ) = a_Connes/a_eff ∝ √(ρ_s(τ))/√(a₂(τ))`. If you commit this as the closed form, then `Ω̇ ∝ d/dτ √(ρ_s/a₂)` and `Ω̈` are computable from the two-fluid `x(τ)` (giving `ρ_s(τ) = ρ_tot·x/(1+x)`) and the D_K spectral moment `a₂(τ)` — both available as τ-functions. The question for your final turn: is `Ω(τ) ∝ √(ρ_s/a₂)` EXACT (the order-parameter-space line element is `ds ∝ √(ρ_s) dτ` rigorously, from `E_grad ∝ ρ_s|∇Δ|²`), or is it a leading-order stiffness approximation that needs a gradient-correction term? The B-PASS executability depends on whether this is the full conformal factor or its leading piece.

**(F2) The relic redshift law and the `x(τ)` trajectory — does `w_n ∈ [−0.4076, 0]` make `x(τ)` monotone, guaranteeing the physical window never re-enters the band-reproduction regime?** My A4 sharpening assumes `x = ρ_s/ρ_n` grows monotonically post-fold (vacuum `w_s=−1` stays constant, relic `w_n≤0` redshifts away). But if `w_n` is sufficiently negative (`w_n=−0.4076` is the flat-band lower edge), the relic redshifts SLOWER than dust (`ρ_n ∝ a^{−3(1+w_n)} = a^{−1.777}` at the lower edge) — and a negative-pressure relic could in principle track the vacuum more closely, slowing the growth of x. Does the substrate `x(τ)` from the two-fluid continuity ODE (your W1-4 `ode_consistent`, residual `4.55e-13`) remain monotone-increasing across `[τ_fold, τ_now]` for the full `w_n ∈ [−0.4076, 0]` band, so the physical window `[x_fold, x_today]` is guaranteed to stay in the `q ≈ −1` regime and NEVER re-approach +0.5? If yes, the A-PASS exclusion is structurally airtight (the band-reproduction regime is causally unreachable from the fold forward); if `x(τ)` is non-monotone for some `w_n`, the window needs a more careful definition. This is the one place a substrate dynamical fact (the sign of `dx/dτ`) could still surprise us, and it is yours to close in the final turn.

---

## Round 2 — volovik: Cross-Synthesis

### CONVERGENCE

transit's R2 follow-up accepted the full microscopic spine I laid down in R1 (the `w_n ≤ 0` bound, the +½-inside-band_tol strengthening, the decidable acoustic frame, the A-PASS re-scope), and converged to my weighting **C ≈ 0.55, B ≈ 0.40, A ≈ 0.03** (residual 0.02). I accept his corrections to my own S97-gate design and his two scope-flags (D1, D2), and I answer his final follow-ups F1 and F2 from the superfluid side with Sage-exact backing. The physics is now closed on Topics 1–3 and the discriminating gate is fully specified for S97.

**(1) I accept transit's A1 — route-1's `T_relic` carries NO `(ρ_s,ρ_n)` field; the partition was traced out and must be RECONSTRUCTED, not read.** I had assumed (Re:T4 leg-(i) amendment) that each route "EXTRACT `(ρ_s,ρ_n)` from the route's own fields." transit inspected the route-1 npz directly (`s96_w1_aoft_friedmann_map.npz`, 53 fields) and found the relic enters as ONE effective `T_relic^{μν}`, with `ρ_relic = 26.5539 = ρ_contrib_B1 + ρ_contrib_B2 + ρ_contrib_B3 = 2.7792 + 21.8876 + 1.8871` — and crucially these B1/B2/B3 are the **3-stage Bogoliubov mode-band ladder** (pre-fold → post-fold WKB → horizon-exit composition), NOT a `w_s=−1`/`w_n=0` superfluid/normal split. There is no `gamma_eff`, `w_n`, `rho_s`, `rho_n`, `superfluid`, or `normal` field anywhere in the 53-field npz. This is exactly the substrate-physics statement I made in Re:T2 ("route-1 AOFT never partitions at all") made concrete at the artifact level. I correct my leg-(i) amendment: route-1's `(ρ_s,ρ_n)` is a **post-hoc reconstruction** from canonical route-invariant pins — `ρ_s = Γ_eff·ρ_tot` (the unbroken-condensate fraction, `Γ_eff=0.99970`, DILUTION-CC-66) and `ρ_n = (1−Γ_eff)·ρ_tot + ρ_GGE` from the Bogoliubov sum — NOT a field-read. The S97 plan-block MUST pre-register this as a DERIVED extraction step with its own substitution chain. The substrate point survives intact: the reconstruction uses the *same* `ρ_relic = 26.5539` repartitioned by route-invariant scalars, so it is admissible; it is not new physics, it is the partition the AOFT action averaged out. The route-invariance test is NOT reduced to route-2-vs-route-3 — but route-1 enters as a reconstruction, not a field.

**(2) I accept transit's A2 — leg-(ii) is currently NON-EXECUTABLE; only the fold-scalar `Omega_BA_fold=2.241353` was exported, and a CONSTANT Ω cannot move R1's q into the band.** This is the most consequential correction to MY R1 gate design, and I concede it fully. My Re:T4 left leg-(ii) "UNCHANGED from transit's spec — apply `Ω(τ)` … and check `frac_in_band>0.90`." transit traced the gate: `Omega_BA_fold=2.241353` is gate-LOCAL (`get_constant("Omega_BA_fold")` → not found; it lives only inside `s95_w4_4_sp_conformal_embed.npz`), and the gate's verdict_reason (knowledge MCP, verbatim, confirmed this turn) is `Omega_derivable_1plus1D AND fold_4D_causal_feature BUT a2_proxy_q_Omega_OUT_of_band ... only_Connes_proxy_reproduces_band AND M_KK_inv_to_s_normalization_OPEN`. So the gate established Ω is DERIVABLE in 1+1D and the fold is a 4D causal feature, but it did NOT export a full 4D `Ω(τ)` *function* with `Ω̇, Ω̈`.

I verified the consequence exact (Sage, this turn). A conformal map of deceleration is `q → q' = f(q, Ω, Ω̇, Ω̈)`. For a CONSTANT Ω:
```
diff(Omega_const, tau) = 0   ⇒   Ω̇ = Ω̈ = 0   ⇒   under a → Ω·a:  (ä/(Ω a))·Ω = ä/a   ⇒   q INVARIANT.
```
So rescaling R1's q by the single constant `2.241353` would leave it exactly at `[−142,−17]` — it would NOT enter `[−0.97,+0.81]`. **transit's B-PASS criterion is currently not executable.** I accept the split he proposes: a prerequisite gate `S97-?-OMEGA-PROFILE` that EXPORTS `Ω(τ), Ω̇, Ω̈` must FEED the pushforward gate. Until it lands, B-PASS is INFO-pending, and the workshop's B-weighting (≈0.40) rests on the *existence* argument (`proxies_conformally_distinct=True`, fold-value `≠1`, S95-W4-4 INFO), NOT on a demonstrated pushforward-into-band. I answer F1 below by committing the closed form that makes the prerequisite gate executable.

**(3) I accept transit's A3 — q_Ω is CONFIRMED FUNCTIONAL-DEPENDENT, the cosmological twin of S96-SDW-EFT-CONTROL.** transit confirmed my V3 Q3 resolution: q_Ω fails the `O = w(param)·g(K)` multiplicative-factorization premise of `math-scripts.md §"Multiplicative-normalization cancellation invariants"` because `ρ_s` and `ρ_n` are two INDEPENDENT thermodynamic functions (energy density and superfluid fraction are independent functions of state — you cannot derive `ρ_s/ρ` from `ρ_tot` alone; you need the order-parameter texture). There is no common multiplicative factor to cancel. q_Ω is therefore the cosmological image of the f-coefficient ratio in S96-SDW-EFT-CONTROL, where the SAME D_K spectrum gives Gaussian `f₂/f₄=4.19` → FAIL-band and Mellin `f₂/f₄=0.033` → PASS-band (`audit 7d62a904…` superseded by `74158937…`) precisely because the f-coefficient weighting does NOT cancel. This is the cleanest statement of why the route-2 FAIL is not a substrate deficit: it is a functional-dependence artifact. I own a registry-corpus calibration note on this (Effected In-Session below).

**(4) I accept transit's A4 + his `x_today` sharpening, and I CLOSE F2 exact: `x(τ)` is strictly monotone-increasing post-fold across the ENTIRE substrate band `w_n ∈ [−0.4076, 0]`.** transit accepted the `x→0`-tail exclusion and added the right structural refinement: `x_today` must be pinned from substrate redshift evolution, not chosen, and the physical window sits entirely at `q ≈ −1`. I close his F2 question (the one dynamical fact he left to me) with a Sage-exact monotonicity proof.

**Substitution chain (F2 — `x(τ)` is monotone-increasing post-fold for every admissible `w_n`):**
- Step 1 (definitions): superfluid vacuum `w_s = −1` ⇒ `ρ_s ∝ a^{−3(1+w_s)} = a^0 = const` (scale-independent). Normal relic `w_n ∈ [−0.4076, 0]` ⇒ `ρ_n ∝ a^{−3(1+w_n)}`.
- Step 2 (the ratio): `x = ρ_s/ρ_n ∝ a^{0}/a^{−3(1+w_n)} = a^{+3(1+w_n)}`.
- Step 3 (the log-derivative): `d ln x / d ln a = 3(1+w_n)`. Sign is `sign(1+w_n)`.
- Step 4 (the bound — Sage-exact, this turn): at the lower edge `w_n = −0.4076 = −1019/2500`, `1+w_n = 1481/2500 = 0.5924 > 0`; at the upper edge `w_n = 0`, `1+w_n = 1 > 0`. The ONLY divide where `1+w_n` flips sign is the phantom value `w_n = −1`, and the entire substrate band `[−0.4076, 0]` is **strictly above** it (`bool((1+wn_lo)>0 and (1+wn_hi)>0) = True`). Therefore `3(1+w_n) > 0` for every admissible `w_n` ⇒ `d ln x / d ln a > 0`.
- Step 5 (read-off): post-fold the acoustic `a_eff` INCREASES monotonically (the substrate's spectral complexity grows); since `d ln x/d ln a > 0`, `x` increases monotonically with `a`, i.e. `dx/dτ > 0` across `[τ_fold, τ_now]` for the full band. The slowest-growth case is the lower edge (`ρ_n ∝ a^{−1.7772}`, exponent `−4443/2500 < 0`, so the relic still DECAYS and `x` still GROWS, just more slowly).
- Conclusion: **the physical window `[x_fold=85.79, x_today]` is causally one-directional — x only grows from the fold forward, deeper into vacuum-domination (`q → −1`).** It NEVER re-approaches the `x→0` regime where `q → +½`. transit's A-PASS exclusion is **structurally airtight**: the band-reproduction "reach toward +0.81" is a property of an `x→0` past the substrate never had, and `x(τ)` monotonicity proves the substrate never returns to it. The phantom divide `w_n = −1` is the only thing that could break monotonicity, and the frozen flat-band relic is bounded `w_n ≥ −0.4076 > −1` (V1).

So I answer transit's F2 directly: **YES, `x(τ)` is monotone-increasing across the full `w_n ∈ [−0.4076, 0]` band; the A-PASS exclusion is airtight.** No `w_n` in the substrate-allowed band makes `x(τ)` non-monotone — that would require `w_n < −1` (phantom), which the frozen relic EoS forbids.

**Net convergence.** I confirm **C ≈ 0.55, B ≈ 0.40, A ≈ 0.03** (residual 0.02). The robust, frame-invariant W1 deliverable is the H²* magnitude triangulation (≤0.04%, three routes); the SF54 q-band is an order-parameter-space proxy, route/partition-dependent, with its +½ acoustic ceiling inside band_tol of the proxy edge (`31/100 = 0.31 < 89/250 = 0.356`, Sage-exact this turn). Reading A (genuine back-reaction deficit) is collapsed below transit's original 0.10 by the microscopic `w_n ≤ 0` bound, which is frame-INDEPENDENT (an EoS is a scalar `p/ρ`).

### DISSENT

I have NO substantive physics dissent. My two residuals are both refinements of transit's D1/D2, not disagreements — I concur with both flags and sharpen each.

**(D1-concur, sharpened) — B+C ≈ 0.95 is the load-bearing number; the 0.55/0.40 split is a reading-emphasis, NOT a joint distribution. I AGREE, and I name the shared root exactly.** transit reads C (route/partition-dependence) and B (frame/layer mismatch) as "two projections of one fact," with `B+C ≈ 0.95` load-bearing and the internal split presentational. I concur completely, and I sharpen the shared root from the superfluid side: **both B and C trace to the single two-fluid inequality `ρ_s(τ)/a₂(τ) ≠ const`.**

**Substitution chain (B and C are the metric-shadow and the partition-shadow of one inequality):**
- Step 1 (C, partition-shadow): q_Ω is route-dependent because it is partition-keyed in `x = ρ_s/ρ_n`; the partition is route-dependent because `ρ_s` (superfluid density, depleted by the relic) flows differently from `a₂` (the bare second moment). The route-variable IS `ρ_s/a₂ ≠ const`.
- Step 2 (B, metric-shadow): the conformal factor is `Ω(τ) = a_Connes/a_eff ∝ √(ρ_s(τ))/√(a₂(τ))` (V2 Step-3). `Ω ≠ 1 ⇔ ρ_s/a₂ ≠ const`. The frame-variable IS `ρ_s/a₂ ≠ const`.
- Step 3 (the identity): `proxies_conformally_distinct=True` (gate-level, S95-W4-4) and "q_Ω is partition-keyed" (Re:T2) are ONE substrate statement evaluated two ways: *the superfluid fraction is not the energy density.* In Volovik's two-fluid framework `ρ_tot` and `ρ_s/ρ` are independent thermodynamic functions of state — this is the textbook root, not a pathology.
- Conclusion: **B and C are not orthogonal events whose probabilities add; they are the metric-functor image (B) and the partition-functor image (C) of `ρ_s ≠ a₂`.** I publish 0.55/0.40 as "partition-dependence is the more fundamental shadow," but I agree with transit's record-keeping: the registry text states `B+C ≈ 0.95` as load-bearing and the split as reading-emphasis. This matches the `single-observable-per-triple` discipline (`cross-pillar-bridge-anatomy.md §16`): distinct functionals are DIAGNOSTIC sub-rows of ONE observable, not co-primary independent entries. (I land this as the registry-corpus calibration note below.)

**(D2-concur, sharpened) — "frame decided" ≠ "C1 a(t) gap closed"; the prose tag MUST distinguish frame-identity from trajectory. I AGREE, and E2 below shows WHY one missing object (`Ω(τ)`) blocks both closures.** transit flags that "the physical scale factor is `a_eff`" is decided at the *which-metric* level but the `a_eff(t)` *trajectory* is INFO-pending the W1-1 `a_t_magnitude=INFO_pending_Znorm_V0_gate7_seconds_gate3_ONeill_gate2` carry-forward. I concur without reservation: my Re:T1 "decidable, not a tie" closes the FRAME-IDENTITY sub-question (a comoving phonon clocks `a_eff`, full stop), NOT the FRW `a(t)` closure. The capstone-hygiene Q3 fires (the a(t)/effective-Friedmann gap status is touched), and the prose tag must read "frame identified (`a_eff`) ∧ trajectory pending" — I built this distinction into the staged §6.3 patch (Effected In-Session). I sharpen the coupling in E2.

Beyond D1/D2 I have no dissent. On the central physics — `w_n ≤ 0` FEATURE, +½ inside band_tol, `a_eff` physical, A-collapsed, route-2 re-tag, `x(τ)` monotone — I converge fully.

### EMERGENCE

The cross-pollination produced three insights neither side held alone, and I commit transit's two final follow-ups (F1, F2) into them.

**(E1) Commit (F1): `Ω(τ) = √(ρ_s/a₂)` is the EXACT order-parameter↔acoustic conformal factor (leading at the stiffness level), and it makes the prerequisite `S97-?-OMEGA-PROFILE` gate executable.** transit's F1 asks whether `Ω(τ) ∝ √(ρ_s/a₂)` is EXACT or a leading-stiffness approximation, because B-PASS executability depends on it. From the superfluid side the answer is: **it is exact at the texture-stiffness (gradient-energy) level, which is the level the conformal relation lives at.** The derivation:
- The order-parameter-space line element is set by the superfluid stiffness against texture gradients: `E_grad ∝ ρ_s(τ)|∇Δ|²` (Volovik two-fluid, the London/gradient term). So the proper distance in order-parameter space along the texture path is `ds_Connes ∝ √(ρ_s(τ)) dτ` — the stiffness IS the metric on order-parameter space.
- The acoustic line element is set by the second spectral moment: `ds_acoustic ∝ √(a₂(τ)) dτ` (the `a₂`-channel that builds `g_M`, `G_N = M_KK²/(16π·a₂)`).
- The conformal factor relating the two metrics is the RATIO of line elements: `Ω(τ) = ds_Connes/ds_acoustic = √(ρ_s(τ)/a₂(τ))`. This is EXACT given the two line-element definitions; it is NOT a perturbative expansion. The only caveat is that it is the **stiffness-level** conformal factor — if one demands sub-leading gradient corrections (higher-derivative texture terms `|∇²Δ|²`, beyond the London term), those add `O((∇Δ)⁴)` corrections to `ds_Connes`, suppressed by the relic's slow texture variation. At the fold the leading stiffness term dominates (the texture is supersonic but smooth, Mach 13.75 with `δt/T_L < 1e-2`, W1-6), so `Ω = √(ρ_s/a₂)` is the full conformal factor to the precision the deceleration band needs.

I verified (Sage, this turn) that `Ω = √(ρ_s/a₂)` carries the derivatives the pushforward needs:
```
Ω̇ = −½·(ρ_s·a₂' − a₂·ρ_s')/(√(ρ_s/a₂)·a₂²)    [well-defined, ≠ 0 iff d/dτ(ρ_s/a₂) ≠ 0]
Ω̈ = (well-defined 2nd derivative, symbolic, confirmed)
```
Both `ρ_s(τ) = ρ_tot·x(τ)/(1+x(τ))` (from the two-fluid `x(τ)`, W1-4 `ode_consistent` residual `4.55e-13`) and `a₂(τ)` (from the D_K spectral moment) are available as τ-functions, so the prerequisite gate is **executable**: it exports `Ω(τ), Ω̇, Ω̈` via `√(ρ_s/a₂)`, and these FEED the leg-(ii) pushforward. **Commit: `Ω(τ) = √(ρ_s/a₂)` is the substrate-natural export, exact at the stiffness level, with computable `Ω̇, Ω̈`.** This unblocks B-PASS (it becomes computable, not merely existence-argued) and — per E2 — the C1 a(t) trajectory simultaneously.

**(E2) One missing object (`Ω(τ)`) blocks BOTH the S97 deceleration gate AND the C1 a(t) closure — and that coupling is the deeper structure of D2.** This is the cross-domain insight that sharpens transit's D2. The H²* triangulation (≤0.04%) closes the *first-order* effective-Friedmann existence+magnitude. But the *trajectory* `a_eff(t)` — and hence q_Ω — requires bridging two frames: the historical SF54/S54 `a(τ)` was computed in the ORDER-PARAMETER frame (`a(τ_fold)/a(0)=2.117` from the Connes distance), while the physical trajectory needs the ACOUSTIC frame `a_eff`. The bridge is `Ω(τ) = √(ρ_s/a₂)`, which is `≠const` precisely because the relic depletes `ρ_s` relative to `a₂`. So:
- the SAME `Ω(τ)` that lets leg-(ii) push R1's `a₂`-channel q into the SF54 band (the B-test)
- IS the `Ω(τ)` that converts the historical S54 order-parameter `a(τ)` into the physical acoustic `a(t)` (the C1 trajectory).

**One profile, two closures.** This re-reads the C1 a(t) gap: it is NOT "we have H² but haven't integrated it" — it is "we have H² in the acoustic frame and `a(τ)` in the order-parameter frame, and closing the trajectory requires the `Ω(τ)` profile that bridges them." transit's D2 ("frame decided ≠ trajectory closed") is exactly right, and E2 names the missing object: the trajectory closes when `S97-?-OMEGA-PROFILE` exports `Ω(τ) = √(ρ_s/a₂)`. This is why I committed F1 — it is load-bearing for two closures, not one. (Note: the C1 a(t) trajectory assembly is the existing W1 carry-forward CF-S97-W1-1; the `Ω(τ)`-profile gate is its prerequisite, not a duplicate — see Carry-Forward Computations.)

**(E3) The substrate carries a partition-INDEPENDENT second-order invariant — but it is NOT q_Ω; naming it sharpens the Atlas D04 entry.** Cross-pollinating my Re:T2 "MISSED" with transit's FUNCTIONAL-INVARIANT/DEPENDENT split (E3 of his R2) yields the clean statement, and I confirm it. There IS a route-invariant second-order observable: **q_Ω evaluated at the substrate-derived `x(τ)`** (the S67 ODLRO `x_fold = 85.7928`) is route-invariant — IF all routes adopt that x. The apparent divergence (+0.50 / +1.24 / OUT `[−142,−17]`) comes entirely from routes 1/3 NOT using the two-fluid partition. So the Atlas D04 C1 entry should record TWO route-invariant deliverables: (i) H²* (first-order, partition-blind, ≤0.04%) AND (ii) q_Ω *at fixed substrate `x(τ)`* (second-order, partition-FIXED) — and the *apparent* q_Ω divergence is the FUNCTIONAL-DEPENDENT image of the routes disagreeing on the partition, the cosmological analog of S96-SDW-EFT-CONTROL (same D_K spectrum → Gaussian FAIL / Mellin PASS / a-ratio-driver INFO). The structural distinction the Atlas entry gains: **"q_Ω diverges across routes" is a partition-RESOLUTION finding, NOT a back-reaction finding** — the routes compute the same partition-keyed observable at different partitions. This is the substrate-first reading: the fabric IS one deceleration at one substrate-determined partition; "divergence" is the routes reading it at different `x`, not the substrate having three decelerations.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | SF54 +0.81 target vs ceiling | T1, Re:T1, V2 | **Converged-on-Reading-B** | +0.81 is the q-range of the order-parameter-space proxy R2 (Connes/coherence-length, `a(τ_fold)/a(0)=2.117`), NOT the acoustic `a_eff` a comoving phonon clocks (`λ ∝ a_eff`, built from `a₂`: `G_N=M_KK²/(16π·a₂)`). The mismatch is the wrong **layer** (order-parameter vs acoustic), sharper than wrong conformal frame. `proxies_conformally_distinct=True`, `Omega_BA_fold=2.241353` (S95-W4-4 INFO) is the gate-level fingerprint of `ρ_s/a₂ ≠ const`. The physical frame is DECIDABLE: `a_eff` is what observers measure. |
| 2 | q_Ω route-invariance vs dependence | T2, Re:T2, V1, A3 | **Converged-on-Reading-C** | q_Ω is route-DEPENDENT; H²* is the invariant (≤0.04% across 3 routes; `ρ_relic=26.5539` is the route-invariant Bogoliubov sum). Sharpened mechanism: H²* is a first-moment (partition-BLIND, depends on `ρ_tot=ρ_s+ρ_n`); q_Ω is second-moment (partition-KEYED in `x=ρ_s/ρ_n`). CONFIRMED FUNCTIONAL-DEPENDENT — fails the `w(param)·g(K)` multiplicative-cancellation premise because `ρ_s, ρ_n` are independent thermodynamic functions; the cosmological twin of S96-SDW-EFT-CONTROL (same D_K → FAIL/INFO/PASS). |
| 3 | two-fluid +0.81-unreachability | T3, Re:T3, V1 | **Converged-feature** | +0.81-unreachability is a STRUCTURAL FEATURE of the `w=−1`/`w=0` split, not a w_n-reading artifact. The microscopic EoS `w_n = ⟨(k/3)(∂E_k/∂k)/E_k⟩_{\|β_k\|²}` is bounded `w_n ∈ [−0.4076, 0]` because the Parker amplitude `\|β_k\|²` peaks on the flat-`∂E/∂k` van Hove modes (`tau_fold=0.190`), forcing the frozen integrable relic to be a zero-/negative-pressure gas. `q_n ≤ +½`. The +½ ceiling is INSIDE band_tol of +0.81 (`31/100=0.31 < 89/250=0.356`, Sage-exact); the FAIL lives only in the unphysical `x→0` tail. Route-2 re-tags to `PASS-with-frame-caveat`. |
| 4 | discriminating S97 gate (A/B/C verdict) | T4, Re:T4, A1–A4, F1–F2 | **Emerged** | **B+C jointly (≈0.95), A-collapsed (≈0.03)**; A would require a route-invariant shortfall (falsified by H²*) OR substrate `w_n>0` (falsified by the van-Hove Bogoliubov construction). The S97 gate is fully specified: leg-(i) route-1 partition is a DERIVED reconstruction from `Γ_eff` (NOT a field-read); leg-(ii) is BLOCKED pending a prerequisite `Ω(τ)`-profile gate (constant Ω leaves q invariant); A-PASS re-scoped to the physical x-window, which `x(τ)` monotonicity (Sage-exact across `w_n∈[−0.4076,0]`) makes airtight. `Ω(τ)=√(ρ_s/a₂)` committed as the exact stiffness-level conformal factor — one profile closes BOTH the B-test and the C1 a(t) trajectory. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

Each is specific enough to become an S97 computation gate; the pre-registered S97 gates are flagged inline.

**OQ1 — Export the full `Ω(τ)` profile with derivatives (prerequisite gate `S97-?-OMEGA-PROFILE`).** Compute `Ω(τ) = √(ρ_s(τ)/a₂(τ))`, `Ω̇(τ)`, `Ω̈(τ)` over `τ ∈ [τ_fold=0.190, τ_now=0.6]`, with `ρ_s(τ) = ρ_tot·x(τ)/(1+x(τ))` from the W1-4 two-fluid `x(τ)` (`ode_consistent`, residual `4.55e-13`) and `a₂(τ)` from the D_K spectral moment. PASS = a non-constant `Ω(τ)` with well-defined `Ω̇, Ω̈` is exported to npz (the constant case is the null — it leaves q invariant). This is the BLOCKER for leg-(ii) of the route-invariance gate AND for the C1 a(t) trajectory (one profile, two closures, E2). **Pre-registered.**

**OQ2 — The S97 q_Ω route-invariance gate (`S97-?-QOMEGA-ROUTE-INVARIANCE`).** Two pre-registered legs: leg-(i) functional-fixing (reconstruct route-1 `(ρ_s,ρ_n)` from `Γ_eff=0.99970` as a DERIVED step, transport the two-fluid `q_Ω(x)` onto all three H(τ), test `max|Δq_Ω| < 0.10`); leg-(ii) conformal pushforward (apply the OQ1 `Ω(τ)` profile to map R1's `a₂`-channel q `[−142,−17]`, check `frac_in_band > 0.90`). A-PASS re-scoped to the physical x-window `[x_fold=85.79, x_today]` with `x_today` pinned from substrate redshift evolution. C-PASS ∧ B-PASS ⇒ verdict B+C; A-PASS (physical-window invariant shortfall) ⇒ reinstate SF54 band (predicted NOT to fire). **Pre-registered; leg-(ii) DEPENDS on OQ1.**

**OQ3 — Pin `x_today` from substrate texture (sub-component of OQ2).** Integrate the relic redshift `ρ_n(τ) ∝ a^{−3(1+w_n)}` against `ρ_s ≈ const` over `[τ_fold, τ_now=0.6]` to compute `x_today = ρ_s/ρ_n|_{τ_now}`, closing the A-PASS x-window as substrate-texture-determined (NOT a free scan endpoint). The `x(τ)` monotonicity proof (CONVERGENCE-4, Sage-exact, `d ln x/d ln a = 3(1+w_n) > 0` for all `w_n ∈ [−0.4076, 0]`) guarantees `x_today > x_fold`, deep in the `q ≈ −1` regime.

**OQ4 — Sub-leading gradient corrections to `Ω(τ) = √(ρ_s/a₂)` (refinement, INFO-class).** The committed conformal factor is exact at the London/stiffness level (`E_grad ∝ ρ_s|∇Δ|²`). Quantify the `O((∇Δ)⁴)` higher-derivative texture correction (`|∇²Δ|²` term) and verify it is suppressed by the relic's smooth texture variation at the fold (`δt/T_L < 1e-2`, W1-6), so the stiffness-level `Ω` is the full conformal factor to the precision the deceleration band needs. Threshold: correction `< band_tol` contribution to q'.

**OQ5 — Does the partition-fixed q_Ω at substrate `x(τ)` agree across routes once leg-(i) extraction lands? (the deeper C-claim).** OQ2 leg-(i) tests cross-route agreement of `q_Ω(x_route)`. The stronger sub-claim (Re:T2 + E3): the routes' reconstructed `(ρ_s,ρ_n)` will themselves agree because `ρ_relic = 26.5539` and `Γ_eff = 0.99970` are route-invariant. If they agree, the route-invariant second-order deliverable for Atlas D04 C1 is q_Ω *at fixed substrate `x(τ)`* — distinct from, and additional to, the partition-blind H²*.

**OQ6 (capstone-hygiene, NON-MATH — STAGED).** The §6.3 / Atlas D04 C1/C2 prose tags must distinguish "frame identified (`a_eff`)" from "a(t) trajectory pending" (D2). Staged for the designated writer (Effected In-Session); routes to S97 housekeeping if not applied at campaign close.

## Wrap-Up — Workshop Impact Summary

### What Changed

**(a) Numerical revisions**
- A/B/C weighting `B+C≈0.85, A≈0.10` (transit T4) → `C≈0.55, B≈0.40, A≈0.03` (residual 0.02), both sides converged; A collapsed by the microscopic `w_n≤0` bound.
- `+0.81 − (+½) = 31/100 = 0.31 < band_tol = 89/250 = 0.356` (Sage-exact) — the +½ acoustic ceiling is INSIDE band-tolerance of the SF54 upper edge (a near-miss, not a gross deficit).
- `w_n` required for `q=+0.81` pinned exact: `31/150 = 0.2067` (above the substrate band `[−0.4076, 0]`).

**(b) Structural changes**
- SF54 q-band: *standing falsifier* → *order-parameter-space proxy* (Connes/coherence-length), conformally + LAYER-distinct from acoustic `a_eff` by `Ω(τ)=√(ρ_s/a₂)` — NOT a primary falsifier (down-tag, S95-W4-4 INFO).
- route-2 verdict: `FAIL` (band-reproduction) → `PASS-with-frame-caveat` (the two-fluid q is the correct acoustic readout; the band is the wrong target). The FAIL is a functional-dependence artifact.
- q_Ω epistemic type: ambiguous-divergence → CONFIRMED **FUNCTIONAL-DEPENDENT** (partition-keyed; cosmological twin of S96-SDW-EFT-CONTROL), vs H²* FUNCTIONAL-INVARIANT.
- S97 leg-(i): "route-1 extract `(ρ_s,ρ_n)` from fields" → "route-1 partition is a DERIVED reconstruction from `Γ_eff`" (the AOFT `T_relic` traced the partition out — A1).
- S97 leg-(ii): single-gate pushforward → SPLIT into a prerequisite `Ω(τ)`-profile gate + the pushforward (constant Ω leaves q invariant — A2).

### What Holds

- **H²* triangulation ≤0.04% across three routes** (route-1 `7.478844e-03`, route-2 `7.476023e-03`, route-3 `7.478844e-03`; route-invariant Bogoliubov sum `ρ_relic=26.5539`). The robust, frame-invariant W1 deliverable for Atlas D04 C1.
- **W1-1 PASS stands independent** of all of this — it never used q_Ω (band-NON-COLLAPSE criterion `noncollapse_reldev=11.52`, `audit edfe1f7f…`). Immune to the conformal/partition ambiguity.
- **`a_eff` is the physical comoving scale factor** (decided, not a tie): a comoving observer is built of phonons, phonons propagate on `g_M=a₂`-metric, `λ ∝ a_eff`. The Connes distance is the order-parameter/Level-2-moduli texture-rate.
- **`w_n ∈ [−0.4076, 0]` microscopic bound** — frame-independent (an EoS is a scalar `p/ρ`), Equilibrium-theorem-level (Volovik Paper 05 lineage), forced by the frozen flat-band Bogoliubov relic.
- **`x(τ)` strictly monotone-increasing post-fold** across the full `w_n` band (`d ln x/d ln a = 3(1+w_n) > 0`, Sage-exact) — the A-PASS exclusion is causally airtight.

### What Breaks or Strains

- **C1 a(t) trajectory remains OPEN** — the workshop closes the FRAME-IDENTITY sub-question (`a_eff` physical), NOT the trajectory. W1-1 carries `a_t_magnitude=INFO_pending_Znorm_V0_gate7_seconds_gate3_ONeill_gate2`; the `Ω(τ)`-profile gate (OQ1) is the missing object that closes both the B-test and the trajectory (E2).
- **leg-(ii) B-PASS is NOT currently executable** — only the fold-scalar `Omega_BA_fold=2.241353` was exported (gate-local, not canonical); the full `Ω(τ)` profile with `Ω̇, Ω̈` is required and does not yet exist. The B-weighting (≈0.40) rests on the existence argument until OQ1 lands.
- **SF54/S54 `a(τ)` was computed in the WRONG frame for trajectory purposes** — the order-parameter (Connes) frame, not the acoustic frame. Closing the physical a(t) requires the `Ω(τ)` bridge; the historical `a(τ_fold)/a(0)=2.117` is a texture-distance, not a proper acoustic distance.

### Carry-Forward Computations (MATH ONLY — propagate to S97)

**CF-S97-W1-OMEGA-PROFILE — the prerequisite `Ω(τ)`-profile gate (BLOCKER for the route-invariance B-leg AND C1 a(t)).**
1. **What**: export `Ω(τ) = √(ρ_s(τ)/a₂(τ))`, `Ω̇(τ)`, `Ω̈(τ)` over `τ ∈ [τ_fold=0.190, τ_now=0.6]` as the order-parameter↔acoustic conformal factor (exact at the London/stiffness level; E1 commit). This is the object S95-W4-4 did NOT export (only `Omega_BA_fold=2.241353` fold-scalar).
2. **Inputs**: two-fluid `x(τ)` from `s96_w1_volovik_2fluid.npz` (W1-4, `ode_consistent` residual `4.55e-13`) giving `ρ_s(τ)=ρ_tot·x/(1+x)`; `a₂(τ)` from the D_K spectral-moment cache (`s96_w1_aoft_friedmann_map.npz` a₂-channel); `ρ_relic=26.553854`; `Γ_eff=0.99970`.
3. **Gate** `[SIGN]`: PASS = non-constant `Ω(τ)` exported with well-defined `Ω̇, Ω̈` (the constant-Ω case is the null — `Ω̇=Ω̈=0` ⇒ q invariant, Sage-confirmed); `Ω(τ_fold)` reproduces `Omega_BA_fold=2.241353` to a pre-registered tolerance as a consistency anchor.
4. **Effort**: 0.5 wave-equivalent (closed-form `√(ρ_s/a₂)` + two τ-derivatives; both inputs are existing npz τ-functions).
   *Depends on*: W1-4 `x(τ)` npz; W1-1 a₂(τ) cache; OQ4 (sub-leading gradient correction) is a downstream INFO refinement, not a blocker.

**CF-S97-W1-QOMEGA-ROUTE-INVARIANCE — the discriminating route-invariance gate (A/B/C).**
1. **What**: leg-(i) reconstruct route-1 `(ρ_s,ρ_n)` from `Γ_eff` (DERIVED step, own substitution chain: `ρ_s=Γ_eff·ρ_tot`, `ρ_n=(1−Γ_eff)·ρ_tot+ρ_GGE`), transport two-fluid `q_Ω(x)=½(1−2x)/(1+x)` onto all three H(τ), test `max|Δq_Ω| < 0.10` (C-PASS); leg-(ii) apply the CF-S97-W1-OMEGA-PROFILE `Ω(τ)` to push R1's q `[−142,−17]`, check `frac_in_band > 0.90` (B-PASS); A-PASS = route- AND frame-invariant q_Ω shortfall `> band_tol=0.356` on the physical x-window `[x_fold=85.79, x_today]` (predicted NOT to fire).
2. **Inputs**: `s96_w1_aoft_friedmann_map.npz` (R1 H(τ)), `s96_w1_volovik_2fluid.npz` (`x(τ)`, q_Ω), `s96_w1_gft_friedmann.npz` (q_GFT(τ)), CF-S97-W1-OMEGA-PROFILE output (`Ω(τ), Ω̇, Ω̈`), `s54_scale_factor` (SF54 band), `M_KK_inv_seconds=8.86044e-42 s` (W1-3, CLOSED), `x_today` (CF-S97-W1-XTODAY).
3. **Gate** `[SIGN]`: composite — `C-PASS ∧ B-PASS ⇒ B+C` (SF54 q-band down-tagged); `A-PASS ⇒ A` (SF54 band reinstated as falsifier, relic-EoS/G_eff deficit). Disjoint pre-registered signatures (non-iterate-until-PASS): invariant shortfall vs frame-pushforward-into-band vs fixed-functional-agreement.
4. **Effort**: 1 wave-equivalent.
   *Depends on*: CF-S97-W1-OMEGA-PROFILE (leg-(ii) BLOCKED without it); CF-S97-W1-XTODAY (A-PASS window).

**CF-S97-W1-XTODAY — pin `x_today` from substrate redshift evolution (A-PASS window closure).**
1. **What**: compute `x_today = ρ_s/ρ_n|_{τ_now}` by integrating `ρ_n(τ) ∝ a^{−3(1+w_n)}` against `ρ_s ≈ const` over `[τ_fold, τ_now=0.6]`, so the A-PASS x-window is substrate-texture-determined, not a free scan endpoint.
2. **Inputs**: `w_n ∈ [−0.4076, 0]` (S67 GGE-TWO-FLUID-67, FROZEN band), `x_fold=85.7928` (S67 ODLRO), `a_eff(τ)` from the a₂ moment, W1-4 `x(τ)` trajectory.
3. **Gate** `[VERIFY]`: PASS = `x_today > x_fold` (guaranteed by the `d ln x/d ln a = 3(1+w_n) > 0` monotonicity proof, Sage-exact this turn) AND `q(x_today) ≈ −1` (deep vacuum-dominated, far from the +½ band-reproduction regime).
4. **Effort**: 0.3 wave-equivalent (single redshift integral; monotonicity already proven).
   *Depends on*: W1-4 `x(τ)` npz; folds into CF-S97-W1-QOMEGA-ROUTE-INVARIANCE as a sub-input.

**Reference (do NOT duplicate): CF-S97-W1-1 — the C1 a(t) trajectory assembly — already exists as a W1 WP carry-forward** (`session-96-w1-workingpaper.md`, the `a_t_magnitude=INFO_pending_{Z_norm,V0}+seconds+O'Neill` closure). E2 establishes that CF-S97-W1-OMEGA-PROFILE is its PREREQUISITE (the same `Ω(τ)` profile converts the S54 order-parameter `a(τ)` into the physical acoustic `a(t)`), NOT a duplicate. The S97 planner should sequence: OMEGA-PROFILE → {QOMEGA-ROUTE-INVARIANCE (B-leg), CF-S97-W1-1 (trajectory)}.

### Effected In-Session (NON-MATH — completed by volovik, the final agent, BEFORE TERMINATING)

Discipline note: the capstone `sessions/framework/phonic-exflation-equation.md §6.3` and the Atlas D04 are CURATED-doc / designated-writer territory (`feedback_framework-hygiene.md`, `capstone-hygiene-gate.md`). I do NOT bulk-edit them. Both items are STAGED below with explicit BEFORE/AFTER wording for the orchestrator to apply as a reviewed patch at campaign close (captured now = effected in-session, NOT deferred to S97). The one item I legitimately own — a `cross-pillar-bridge-corpus.md §16` calibration note (q_Ω as a FUNCTIONAL-DEPENDENT single-observable-per-triple instance) — I direct-edit and cite the concrete file:line.

- [x] **STAGED → designated-writer (capstone §6.3 down-tag, capstone-hygiene Q3-fire).** The §6.3 "What exists instead — labeled PROXY" paragraph (`sessions/framework/phonic-exflation-equation.md` ~line 450) narrates the SF54 q-band conformal-distinctness but does NOT carry the workshop down-tag (order-parameter-space proxy, frame-identity-vs-trajectory split). Patch:

  > **BEFORE** (current, ~line 450, the final clause of the PROXY paragraph):
  > "The `a(τ)` from Connes distance (SCALE-FACTOR-54, with `q` from `−0.97` to `+0.81`) is the one that carries the deceleration band, and a conformal-factor construction `Ω(τ)` embedding the modulus-space causal structure into the 4D one *does* exist and reproduces that `q`-range — but **only with the Connes-distance proxy, not `a_eff`** (S95 W4-4, INFO; the two proxies are conformally distinct in their deceleration structure). Neither is a derived FRW scale factor; neither is promoted; the `M_KK⁻¹ →` seconds normalization remains the open piece even after the conformal embedding is pinned."
  >
  > **AFTER** (insert the down-tag + frame-identity/trajectory split; cite S95-W4-4 + S96-W1):
  > "The `a(τ)` from Connes distance (SCALE-FACTOR-54, with `q` from `−0.97` to `+0.81`) is the **order-parameter-space texture-distance** (the superfluid coherence-length readout `ξ(τ) ∝ ħv_F/Δ(τ)`, `a(τ_fold)/a(0)=2.117`), NOT the acoustic `a_eff` a comoving phonon clocks; the two are conformally AND layer-distinct by `Ω(τ) ∝ √(ρ_s/a₂)`, the gate-level fingerprint `proxies_conformally_distinct=True`, `Omega_BA_fold=2.241353` (S95 W4-4, INFO). **The SF54 `q`-band `[−0.97,+0.81]` is therefore a conformal-frame-conditional / proxy-conditional artifact, NOT a standing falsifier** — it is the q-range of the order-parameter proxy, and the substrate's acoustic deceleration (the `a₂`-channel q a comoving observer measures) is conformally distinct from it (S95 W4-4; S96 W-1 workshop). The physical comoving scale factor is **decidably `a_eff`** (a comoving observer is built of phonons; phonons propagate on the `a₂`-metric `g_M`; `λ ∝ a_eff`) — **but this decides the FRAME IDENTITY, not the trajectory**: the `a_eff(t)` trajectory remains INFO-pending the `{Z_norm, V0}` + seconds (W1-3 CLOSED, `8.86044e-42 s`) + O'Neill (S96 W1-2: cross-terms effaced at the Hubble scale, `6.84e-117 ≪ 3e-7`) pins, and closing it requires the `Ω(τ)=√(ρ_s/a₂)` profile that bridges the historical order-parameter `a(τ)` to the physical acoustic `a(t)`. Neither proxy is a derived FRW scale factor; neither is promoted; the `M_KK⁻¹ →` seconds normalization is now closed (W1-3) but the `Ω(τ)` profile (S97 prerequisite gate) remains the open object that closes both the SF54 frame-comparison and the a(t) trajectory."

  Substrate-first preservation (load-bearing per `capstone-hygiene-gate.md §"Substrate-first framing preservation"`): the down-tag LOWERS over-confident wording (standing falsifier → proxy-conditional artifact) without inverting the explanation arrow. The substrate IS both intrinsic rates (texture-rate = Connes/coherence-length; acoustic-rate = `a₂`-channel q); the down-tag scopes the confidence; the arrow `D_K → {a₂, ρ_s} → metrics → q_Ω` is unchanged.

- [x] **STAGED → designated-writer (Atlas D04 C1/C2 reconciliation note).** Reconcile the C1/C2 effective-Friedmann pathway tags (`sessions/framework/Atlas/atlas-04-assumptions.md`) with the workshop verdict. Patch (append to the C1 entry):

  > **AFTER** (append to C1, the modulus→scale-factor pathway):
  > "S96 W-1 (workshop): two route-invariant deliverables land — (i) H²* magnitude triangulation ≤0.04% across three independent routes (AOFT/Volovik/GFT; `ρ_relic=26.5539` route-invariant) — the robust C1 first-order existence+magnitude closure; (ii) q_Ω at the substrate-derived `x(τ)` (second-order, partition-fixed, route-invariant IF all routes adopt the two-fluid partition). The APPARENT cross-route q_Ω divergence (+0.50/+1.24/OUT) is a partition-RESOLUTION finding, NOT a back-reaction finding — q_Ω is FUNCTIONAL-DEPENDENT (partition-keyed in `x=ρ_s/ρ_n`; the cosmological twin of S96-SDW-EFT-CONTROL), so distinct routes read the SAME observable at different partitions. The SF54 q-band `[−0.97,+0.81]` is RE-CLASSIFIED from a C1/C2 hard back-reaction target to a conformal-frame-conditional / proxy-conditional artifact (order-parameter-space, conformally distinct from acoustic `a_eff` by `Ω(τ)=√(ρ_s/a₂)`; S95-W4-4 INFO). The C1 a(t) TRAJECTORY remains open: frame identified (`a_eff`), trajectory pending the `Ω(τ)`-profile gate (S97 prerequisite). C2 (`K_pivot`) status UNCHANGED by this workshop."

- [x] **DIRECT-EDIT (owned) — registry-corpus calibration note.** Added q_Ω as calibration instance #2 to `sessions/framework/registry/cross-pillar-bridge-corpus.md §16` (Single-observable-per-triple structural filter): the three routes' native q_Ω are DIAGNOSTIC readouts of ONE partition-keyed observable; the route-variable (partition `x`) is CONTINUOUS (no discrete identity jump) ⇒ the slot stays SINGLE; cross-link to `math-scripts.md §"Multiplicative-normalization cancellation invariants"` (q_Ω is the NON-factorizing case: fails `O=w(param)·g(K)` because `ρ_s, ρ_n` are independent thermodynamic functions). Concrete landing: `cross-pillar-bridge-corpus.md` §16 "Calibration corpus instance #2 (S96 W-1 q_Ω)". (This is mine to write — it is the substrate-physics reading of a single-observable-per-triple instance, not a capstone/rule edit.)

### Closing Line

The q_Ω-band "divergence" was never a back-reaction deficit: the fabric carries ONE deceleration, read by the routes at different partitions of its own superfluid/normal split, and the SF54 +0.81 was the order-parameter texture-rate compared to the acoustic rate a phonon clocks — two intrinsic substrate rates conformally distinct by `Ω(τ)=√(ρ_s/a₂)`, because the superfluid fraction is not the energy density. H²* triangulates to ≤0.04% (frame-invariant), `w_n ≤ 0` caps deceleration at +½ (microscopic, frame-independent), and `x(τ)` is monotone (Sage-exact) — so the only open object is the `Ω(τ)` profile, and the same profile closes both the SF54 frame-comparison and the C1 a(t) trajectory.
