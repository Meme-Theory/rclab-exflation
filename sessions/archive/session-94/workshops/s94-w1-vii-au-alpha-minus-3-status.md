# W-1 JOINT STRUCTURAL VERDICT — §VII.AU.OP-PROJ α=−3 discharge status

**Workshop**: W-1, §VII.AU.OP-PROJ α=−3 discharge status adjudication (S94 campaign, Slot 2)
**Turn**: T4 (FINAL — balanced JOINT VERDICT; assembly of the canonical output doc + routed edit-text)
**Author of this verdict**: lizzi-spectral-functional-theorist (Reading B owner)
**Opponent (converged)**: connes-ncg-theorist (Reading A owner; author of the S94 W2-3 derivation)
**Gate adjudicated**: `S94-VII-AU-ALPHA-MINUS-3-LAYER-1` (PASS; verdict line 39 canonical, supersedes line 33; sign/mag/regime = PASS/PASS/VALID; `α_asymptotic = −3.000000`, rel = 1.282e-12, `C₁fit = −3.846`, `α_b_recov = 2.6926`)
**Output target**: `sessions/archive/session-94/workshops/s94-w1-vii-au-alpha-minus-3-status.md` (this file)

---

## Transcript pointer

The full two-round exchange lives in three turn files: **T1** (`s94-w1-T1-connes-R1.md`, connes steelman of Reading A — argued bare (a) FULL-RECOVERED is honest, with (c) two-layer split pre-positioned as the only admissible containment); **T2** (`s94-w1-T2-lizzi-R1.md`, lizzi steelman of Reading B + rebuttal — argued (b) consistency-confirmed with the SCHEMATIC cap preserved, (c) as the floor, bare (a) rejected via the exponent-vs-observable distinction and the orthogonality-cuts-both-ways proof §III.3); **T3** (`s94-w1-T3-connes-R2.md`, connes FINAL turn — **converged on (c)**, withdrew bare (a), conceded the exponent-vs-observable distinction, conceded S91 `CLASS=FULL`, conceded the Sage window-separability arithmetic, and argued the one residual: that (b) is a *lossy single-row projection* of (c), not a lossless contraction, because the corridor's deferred object IS the Level-1 exponent and there is no surviving distinct physical `c_continuum` number behind the SCHEMATIC tag). This T4 adjudicates that single residual and assembles the converged registry form.

---

## JOINT STRUCTURAL VERDICT

### Outcome: **(c) — the two-layer split**, registered as **TWO STRUCTURAL-ORTHOGONAL-COMPANION rows** (NOT co-primary). Bare **(a) WITHDRAWN** (by connes, T3 §I). Single-row **(b) REJECTED as a lossy axis-collapse**. **(d)'s reconciliation requirement is SATISFIED BY (c)** without a verdict-line supersede.

The convergence is full. The two agents agree on every load-bearing fact; the one residual — whether (b) survives as a defensible single-row contraction — is adjudicated here in connes's favor, on the framework's own record. I (lizzi, Reading B) **accept option (i): (b) is lossy and (c)'s two-row form is the lossless encoding.** My own §III.3 orthogonality proof demands two rows; I do not get to then collapse them back to one. This is honest convergence, not a split-the-difference.

#### What both sides concede (no reservation)

1. **The exponent `α = −(d−1) = −3` is a registry-PROVEN, regulator-invariant, L-independent cohomology-class identity** at the Cell I × substrate-distance-1 pole `s = 3`. Sage `−(d−1) = −3` exact. Knowledge-base PROVEN theorem hit verbatim: *"Level-1 layer (asymptotic universal): leading-term `−3` is REGULATOR-INVARIANT, L-INDEPENDENT cohomology-class identity at the Cell I × substrate-distance-1 pole `s=3`."* On my FI/RD taxonomy it is unambiguously **FI** — the leading-term sibling of the registry-PROVEN β_shell FI theorem at the same `d=4`, `s*=3` pole.

2. **The F₂ contour-deformation identity** `sub_term_R(Mellin) = sub_term_R(zeta) = 0` at `s=3` (Cauchy's theorem, no pole crossings) makes the exponent FI across {Mellin, zeta}. Conceded by both.

3. **The exponent was canonical with `CLASS=FULL` in S91 — three sessions before W2-3.** Verified this turn via `get_constant("alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC")` → value `−3.0`, **Session S91**, `CLASS=FULL`, deferred object `asymptotic-limit-derivation-DEFERRED-to-CF-S94-W5-3`, provenance added S93 W2-3. So the SCHEMATIC tag on the W2-3 verdict line is NOT tagging the exponent (independently FULL since S91) — it tags the **W2-3 discharge object**, the analytic-saturation evaluation ROUTE.

4. **The Sage window-separability arithmetic is correct**: in-window `α(L)` slopes shift rigidly with the assumed leading exponent `p`; the `(C₁/L)/(1+C₁/L)` tail is `p`-independent; the window [35,100] cannot independently pin `p` (the distinction lives only at the never-sampled `L→∞` limit; L≥13 undiagonalized). Both sides agree as arithmetic.

5. **Friedrich-Bär freezes the bottom-K above L=12** with a ~10× margin (NEW-sector floor 8.4317 ≫ bottom-K ceiling 0.8452 — verdict companion line 42). The residual `R_b(L)` in the window is *determined by* the L≤12 cache.

6. **The live FULL-physical Mellin-Barnes evaluation of `R_b(L)` at L≥13 was NOT performed and is infeasible** (recursive-Casimir feasibility pre-check). The asymptotic exponent was read by the SCHEMATIC analytic-saturation two-pin route, NOT by that pipeline. This is a real, non-cosmetic SCHEMATIC fact — it earns the Layer-2 SCHEMATIC-pending row **in full**.

7. **`SCHEMATIC(provenance) ⊥ STRUCTURAL(exponent)`** — the Level axis (SCHEMATIC-vs-FULL) and the cohomology-class axis (the exponent's structural value) are pairwise-independent per `regulator-pin-discipline.md §"four-axis orthogonality"`. The SCHEMATIC tag does not VETO the exponent's structural truth. **AND, by the same orthogonality, the exponent's FULL status does not LICENSE a single-tag FULL-RECOVERED status on the observable** (the §III.3 proof). Both directions of the orthogonality hold.

#### The one residual — adjudicated for connes (option (i))

My T2 built (b)/(c) on a two-object ontology: object (1) the cone exponent [conceded recovered], object (2) "the corridor observable — `R_b(L) = ρ_FULL(s=3,L) − ρ_FULL(s=3,∞)`, its amplitude-and-rate, the `c_continuum` reference" [claimed still SCHEMATIC, and claimed to be what `NUMERICAL-DEFERRED` deferred]. (b) is admissible **only if** object (2) is a *distinct physical number* that survives unrecovered behind the SCHEMATIC tag. **The registry record removes that escape hatch.** I queried the corridor's content this turn:

- **The corridor's content IS the Level-1 leading-term exponent, verbatim** (knowledge MCP, `permanent-results-registry.md`, status PROVEN): *"Asymptotic anchor: `alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC = −3` (Level-1 leading-term universal across Cell I × substrate-distance-1 pole `s=3` per CM-1995 §III.4)"* and *"Reading A — SCHEMATIC convergence-exponent two-pin protocol (RETAINED): ... Level-1 asymptotic `L^{−3}` leading[-term]."* The word "NUMERICAL" in `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED` names the analytic-saturation NUMERICAL **route** by which the exponent's asymptotic value is read — NOT a second physical number. My own T2 §II conceded the deferred object is `asymptotic-limit-DERIVATION`, "a statement about an *evaluation route* reaching the limit, not about the limit's value." That concession is fatal to (b)'s two-object ontology: the deferred object is the **route to the exponent**, not a `c_continuum` distinct from it.

- **The amplitude `c` (and correction `C₁`) in `R_b(L) = c·L^{−3}(1+C₁/L)` is a sub-leading fit detail, not the corridor's deferred object.** The corridor was registered as a convergence-RATE corridor (Level-1/Level-2); the Level-2 envelope's binding content is the RATE `α`, not the amplitude `c`. And `c, C₁` are *fit from the frozen FULL-physical L≤12 cache* (R²=0.99988; W2-3 recovered `C₁=−3.846` from the REAL frozen-cache residual, not a synthetic envelope). So even the amplitude is not an open deferral.

**Conclusion on the residual.** lizzi's object (2) collapses: insofar as it is "the rate," it IS the exponent (recovered); insofar as it is "the amplitude," it is fit from frozen FULL-physical data (also not an open deferral). There is **no surviving third object that is both physical and unrecovered.** What remains SCHEMATIC is the **evaluation ROUTE**, which is provenance, not an observable. This is connes's winning move (T3 §II/§III), and I credit it in full.

#### Why this forces (c) over (b) — my own orthogonality theorem turned on my own proposal

A single-row contraction of a two-row orthogonal structure is lossless **only when the two rows carry the *same* status**. Here Layer-1 carries FULL/PROVEN and Layer-2 carries SCHEMATIC — *opposite* statuses on an axis the framework holds orthogonal (concession 7). A single (b) row forces ONE status onto two orthogonal axes. That is the **same axis-collapse error I convicted bare (a) of, committed in the opposite direction**: (a) collapses both axes to FULL; (b) collapses both to SCHEMATIC. My §III.3 proof (`STRUCTURAL-FULL(exponent) ⇏ FULL-RECOVERED(single-tag corridor)`) is, read symmetrically, the demand that two independent axes get two independent rows — and it cuts against (b) exactly as it cuts against (a). (b)'s "structural-posit" label additionally **understates Layer-1**: the exponent is a registry-PROVEN theorem, not a posit; the §(iv) discipline read symmetrically forbids erasing PROVEN downward just as it forbids erasing SCHEMATIC upward. (c) gives each layer its correct status. **(b) is a lossy projection, not a lossless contraction.** I drop the framing of (b) as a correct single-row contraction.

#### Substrate-first framing (direction of explanation)

The substrate IS the spectral triple `(A_K, H_K, D_K)` at the substrate-distance-1 Mellin-cone pole `s=3`. `α` is a **substrate-IS spectral-functional** — the leading decay exponent of the residual between the finite-L bottom-K Mellin moment-ratio and its `L_max → ∞` HKR image, read off the dimension-spectrum POLE (orientability + dimension + regularity axioms), not measured IN a continuum container. The two companion rows are TWO methodology-floor F-images of the SAME substrate-IS observable: Layer-1 the cohomology-class exponent (substrate-IS structural fact), Layer-2 the analytic-saturation route by which that exponent's asymptotic value is sourced (a SOURCING/PROVENANCE fact, not a second physical observable). Neither row is "fundamental" with the other "derived" — inverting either direction is a container-thinking violation per `phononic-framing.md §"IS Space, Not IN Space"`.

---

## (1) ROUTED EDIT (for mack, registry sole-writer, to land) — §VII.AU.OP-PROJ corridor-discharge status two-row split

**Target file**: `sessions/permanent-results-registry.md`. **Sole writer**: `mack-cosmic-bridge` (per `feedback_mack-bridge-role.md`). **Do NOT** edit the registry from this workshop — this block is the exact text mack lands.

**Scope note for mack (read first).** The registry ALREADY carries a STRUCTURAL-ORTHOGONAL-COMPANION dual reading for §VII.AU.OP-PROJ at lines **18280–18319** (the S92 W1 CF-W9-8-2 landing). That existing dual reading splits a DIFFERENT seam: Reading A = SCHEMATIC convergence-exponent two-pin protocol (`alpha = −3` + `alpha_sample = 2.6926`) vs Reading B = FULL-CC **residue-VALUE** marginal-saturation anchor (`rho_FULL_CC_VII_AU_SAT_s3`). **That existing companion is NOT touched by this edit** — it is correct and stands. This W-1 edit operates on a SEPARATE object: the **corridor-discharge STATUS re-tag** `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED → FULL-RECOVERED` (housekeeping A9; the corridor-status header at the §VII.AU.OP-PROJ STAGE-3 content-host blocks, registry `permanent-results-registry.md:18938,19289` per the schedule W-1 entry). The single `FULL-RECOVERED` corridor-status tag splits into the two layer rows below. Land this as a new sub-section adjacent to the corridor-status header (or as an annotation on the A9 re-tag line), templated on the §VII.AF.1.OP-PROJ "S91 W7 STRUCTURAL-ORTHOGONAL-COMPANION dual reading" precedent (registry lines ~14938–14978).

```markdown
**S94 W-1 STRUCTURAL-ORTHOGONAL-COMPANION two-layer split of the `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED → FULL-RECOVERED` discharge status** (S94 W-1 workshop `sessions/archive/session-94/workshops/s94-w1-vii-au-alpha-minus-3-status.md`; adjudication outcome (c) two-layer split; bare (a) WITHDRAWN by connes-ncg T3 §I; single-row (b) REJECTED as lossy axis-collapse; lizzi-spectral-functional T4 balanced joint verdict + connes-ncg T3 convergence; mack-cosmic-bridge sole-writer per `feedback_mack-bridge-role.md`):

The `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED → FULL-RECOVERED` discharge (housekeeping A9; gate `S94-VII-AU-ALPHA-MINUS-3-LAYER-1` PASS, verdict line 39 canonical) admits a **STRUCTURAL-ORTHOGONAL-COMPANION two-layer reading** per `.claude/rules/registry-landing.md §"Operator-Projection Reading-A Naming Hygiene"` MANDATORY at K=3 (S88 W8-92 close) and `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` MANDATORY at K=3. The single `FULL-RECOVERED` corridor-status tag is REPLACED by TWO structural-orthogonal-companion rows on the level-pin axis (per `substrate-first-canonical-sourcing.md §(iv)` K=4 MANDATORY level-pin discipline) — NEITHER co-primary (cross-axis co-primary is FORBIDDEN per the algebra-axis K-counter). This follows the EXACT precedent of the §VII.AF.1.OP-PROJ "S91 W7 STRUCTURAL-ORTHOGONAL-COMPANION dual reading" (registry lines ~14938–14978; Reading A SCHEMATIC + Reading B FULL under ONE OP-PROJ slot).

- **Layer-1 (cohomology-class / exponent) — status: FULL-RECOVERED.**
  - content: `α = −(d−1) = −3`, Level-1 leading-term, Cell I × substrate-distance-1 pole `s=3`.
  - warrant: registry-PROVEN; FI (`sub_term_R(Mellin) = sub_term_R(zeta) = 0` at `s=3`, Cauchy's theorem, no pole crossings); regulator-invariant; L-independent; canonical with `CLASS=FULL` since S91 W-5 (`get_constant("alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC")` → −3.0, Session S91, CLASS=FULL — INDEPENDENT of the W2-3 gate). The leading-term sibling of the registry-PROVEN β_shell FI theorem at the same `d=4`, `s*=3` pole.
  - axis: cohomology-class axis (algebra-INVARIANT spectrum-only-functional family, Cell I).
  - The `FULL-RECOVERED` re-tag is HONEST **at this layer**: the corridor's REGISTERED content is the Level-1 leading-term exponent, and it is FULL/PROVEN.

- **Layer-2 (evaluation-route provenance) — status: SCHEMATIC-pending (`tier_pin=TIER-2`).**
  - content: the analytic-saturation NUMERICAL route that reads `α` off as `L→∞` (the SCHEMATIC two-pin convergence-exponent protocol; `rho_FULL_CC_VII_AU_SAT_s3` PROVENANCE chain; `alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC = −3` asymptotic-pin + `alpha_sample_VII_AU_OP_PROJ_FW_PATHWAY_B_L15_22 = 2.6926236951422458` finite-L sample).
  - warrant: NOT a live full-physical Mellin-Barnes extraction at L≥13 (infeasible per the recursive-Casimir feasibility pre-check; `math-scripts.md §"D_K Block-Diagonality + Recursive-Casimir-Projection Feasibility Pre-Check"`). The route is consistency-certified (R²=0.99988; canonical sample reproduced to rel_dev 5.94e-15) against the frozen FULL-physical L≤12 cache, which Friedrich-Bär saturation freezes above L=12 (NEW-sector floor 8.4317 ≫ bottom-K ceiling 0.8452, ~10× margin; verdict companion line 42).
  - axis: Level / sourcing axis (SCHEMATIC-vs-FULL; `tier_pin=TIER-2`).
  - The numerical-deferral that `NUMERICAL-DEFERRED` named is the analytic-saturation ROUTE to the exponent — NOT a distinct physical `c_continuum` observable. There is no surviving third object that is both physical and unrecovered; the residual is ROUTE-provenance.

**STRUCTURAL-ORTHOGONAL-COMPANION tagging**: Layer-1 + Layer-2 are STRUCTURAL-ORTHOGONAL-COMPANIONS on the level-pin axis (per `registry-landing.md §"Operator-Projection Reading-A Naming Hygiene"` MANDATORY at K=3; analogous to §VII.AF.1.OP-PROJ Reading A + Reading B at registry line ~14947 and the existing §VII.AU.OP-PROJ S92 W1 Reading A + Reading B at registry line ~18296). Neither layer is decoration of the other; each holds its status independently. Cross-axis co-primary (SOURCE-DOUBLE-CITE-CO-PRIMARY) is FORBIDDEN per `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` (the two layers live on orthogonal axes: cohomology-class vs Level/sourcing).

**Why NOT a single tag** (the adjudication, in one line): a single `FULL-RECOVERED` tag collapses two opposite-status orthogonal axes onto one (the §(iv) class-conflation pathology at the highest-leverage status layer); a single SCHEMATIC/consistency-confirmed tag commits the same axis-collapse in the opposite direction AND understates the PROVEN Layer-1. The §III.3 orthogonality theorem (`STRUCTURAL-FULL(exponent) ⇏ FULL-RECOVERED(single-tag corridor)`, AND its converse `SCHEMATIC(route) ⇏ ¬FULL(exponent)`) DEMANDS two rows; (c) is the registry-anatomy-faithful resolution.

**Verdict-line / `LEVEL_CLASS_PIN` reconciliation (option (d), satisfied without a supersede)**: verdict line 39 STAYS UNEDITED. It ALREADY carries the two-axis disclosure — `LEVEL_CLASS_PIN=SCHEMATIC tier_pin=TIER-2` (companion rows 38/44) on the route/Level axis, AND the clause *"the L^{−3} leading-term geometric envelope exponent −(d−1) at d=4 is the structural anchor"* on the cohomology-class axis. That companion row IS (c) in verdict-line form: the verdict line was honest all along. The dissonance was solely between ONE `FULL-RECOVERED` registry status tag and ONE `SCHEMATIC` verdict-line tag, read as predicating over the SAME object. (c) resolves it: they predicate over DIFFERENT axes (Layer-1 vs Layer-2). The `LEVEL_CLASS_PIN=SCHEMATIC` verdict-line tag IS the Layer-2 row's warrant; the "structural anchor" clause IS the Layer-1 row's warrant. The registry single-tag status is what splits; the verdict line is re-read as carrying both layers. NO Option-A supersede on line 39.

**Cross-references**:
- S94 W-1 workshop `sessions/archive/session-94/workshops/s94-w1-vii-au-alpha-minus-3-status.md` (T1/T2/T3 turn files + this JOINT VERDICT; adjudication outcome (c)).
- gate `S94-VII-AU-ALPHA-MINUS-3-LAYER-1` PASS, `computations/session-94/s94_gate_verdicts.txt:39` (canonical; supersedes line 33; `LEVEL_CLASS_PIN=SCHEMATIC tier_pin=TIER-2` companion rows 38/44; `α_asymptotic=−3.000000` rel=1.282e-12, `C₁fit=−3.846`).
- `get_constant("alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC")` → −3.0, Session S91, CLASS=FULL, deferred = asymptotic-limit-derivation-DEFERRED-to-CF-S94-W5-3 (provenance added S93 W2-3) — the Layer-1 exponent's S91 FULL status.
- §VII.AF.1.OP-PROJ "S91 W7 STRUCTURAL-ORTHOGONAL-COMPANION dual reading" at registry lines ~14938–14978 — precedent template for this dual-reading discipline at the level-pin axis.
- §VII.AU.OP-PROJ S92 W1 CF-W9-8-2 STRUCTURAL-ORTHOGONAL-COMPANION dual reading at registry lines ~18280–18319 — sibling companion (SCHEMATIC convergence-exponent vs FULL-CC residue-VALUE); this W-1 split is the SEPARATE corridor-discharge-STATUS companion, NOT touching that one.
- `registry-landing.md §"Operator-Projection Reading-A Naming Hygiene"` MANDATORY at K=3 (S88 W8-92) — STRUCTURAL-ORTHOGONAL-COMPANION discipline at the level-pin axis.
- `substrate-first-canonical-sourcing.md §(iv)` K=4 MANDATORY level-pin discipline — SCHEMATIC vs FULL physical level class taxonomy.
- `cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"` MANDATORY at K=3 — cross-axis co-primary FORBIDDEN; structural-orthogonal-companion is the correct anchor structure.
- `epistemic-discipline.md §"Layer-Decomposition"` Phi-correspondence — SCHEMATIC (Layer-2, sourcing) vs FULL (Layer-1, cohomology-class) as orthogonal methodology-floor F-images of the SAME substrate-IS observable.

**Substrate framing**: the substrate IS the spectral triple `(A_K, H_K, D_K)` at Cell I × substrate-distance-1 pole `s=3`. `α` is a substrate-IS spectral-functional read off the dimension-spectrum pole, NOT a measurement IN a container. Layer-1 (the cohomology-class exponent) is the substrate's structural F-image; Layer-2 (the analytic-saturation route) is the sourcing F-image of how the exponent's asymptotic value is read. Neither is fundamental-with-the-other-derived. FORBIDDEN inversion: "FULL-RECOVERED means the full-physical scalar was extracted" (it was not; that is Layer-2's SCHEMATIC content) OR "SCHEMATIC means the exponent is unrecovered" (it is recovered as a registry-PROVEN theorem; that is Layer-1's FULL content) → invert to "the substrate IS the spectral triple at the pole; BOTH layers are F-images at distinct methodology-floor classes; neither is fundamental with the other derived."
```

---

## (2) ROUTED CORPUS ROW (for the Slot-3 closeout to land) — SCHEMATIC-pin-discharge-re-tagged status calibration instance

**Target**: `sessions/framework/registry/substrate-first-canonical-sourcing.md` §(iv) 4-class corpus location — i.e. the CORPUS file `sessions/framework/registry/pru-class-corpus.md §12` (the §(iv) K=4 calibration corpus per the pointer table), **NOT** the rule file (per project memory rule #14). **Do NOT write the corpus now** — the Slot-3 closeout serializes corpus writes. This is the exact row text the closeout lands.

```markdown
### §(iv) K=4 calibration corpus — SCHEMATIC-route-discharge-re-tagged-status instance (S94 W-1)

**Instance**: §VII.AU.OP-PROJ `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED → FULL-RECOVERED` discharge-status adjudication (S94 W-1 workshop; gate `S94-VII-AU-ALPHA-MINUS-3-LAYER-1` PASS, verdict line 39).

**Pattern**: a corridor-discharge STATUS re-tag to `FULL-RECOVERED` where the producing verdict line carries `LEVEL_CLASS_PIN=SCHEMATIC tier_pin=TIER-2`. The naive single-tag reading fires the §(iv) class-conflation pathology at the highest-leverage (registry-status) layer — a `FULL-RECOVERED` status tells every downstream consumer "recovered, full stop", erasing the SCHEMATIC class the verdict line discloses.

**Resolution (adjudicated outcome (c), two-layer split)**: the status splits into STRUCTURAL-ORTHOGONAL-COMPANION rows — Layer-1 (cohomology-class exponent) FULL-RECOVERED + Layer-2 (analytic-saturation evaluation route) SCHEMATIC-pending. The discriminating structural fact: the corridor's REGISTERED content is the Level-1 leading-term exponent (registry-PROVEN; CLASS=FULL since S91, INDEPENDENT of the discharge gate), and what is SCHEMATIC is the **evaluation ROUTE** (the two-pin convergence-exponent protocol), NOT a distinct physical `c_continuum` observable. There is no surviving third object that is both physical and unrecovered.

**Two-axis fingerprint** (mirrors `regulator-pin-discipline.md §"four-axis orthogonality"` and the §22 2-bit anchor-vs-diagnostic fingerprint): the discharge object is `(cohomology-class-axis: FULL/PROVEN, L-INDEPENDENT) × (Level/sourcing-axis: SCHEMATIC, tier_pin=TIER-2, route-not-observable)`. A single-tag status on EITHER axis is a lossy axis-collapse: FULL-only erases the SCHEMATIC route (the (a) error); SCHEMATIC/consistency-only erases the PROVEN exponent and understates Layer-1 (the (b) error). The orthogonality theorem (`STRUCTURAL-FULL(exponent) ⇏ FULL-RECOVERED(single-tag)` AND `SCHEMATIC(route) ⇏ ¬FULL(exponent)`) demands two rows.

**Distinctness from prior §(iv) corpus instances**: distinct from the §VII.AF.1.OP-PROJ K=4 instance #1 (SCHEMATIC-SDW vs FULL-CC residue-VALUE seam) and the §VII.AU.OP-PROJ S92 W1 CF-W9-8-2 companion (SCHEMATIC convergence-exponent vs FULL-CC residue-VALUE) on the OBJECT axis: those split a SCHEMATIC-VALUE vs FULL-VALUE seam at the canonical-pin level; THIS instance splits a discharge-STATUS re-tag where the SCHEMATIC residual is the **evaluation ROUTE to a PROVEN exponent**, not a competing VALUE. Whether this ENRICHES (no K-advance; same OBJECT-vs-STATUS observable-identity family) or ADVANCES the §(iv) K-counter on a structurally-distinct axis (status-re-tag vs canonical-pin) is for the Slot-3 closeout to declare against the current §(iv) K-status; default reading = ENRICH (the resolution mechanism — STRUCTURAL-ORTHOGONAL-COMPANION two-row split — is the SAME as the prior instances; only the split object differs).

**Provenance**: S94 W-1 workshop `sessions/archive/session-94/workshops/s94-w1-vii-au-alpha-minus-3-status.md`; gate `S94-VII-AU-ALPHA-MINUS-3-LAYER-1` PASS (`computations/session-94/s94_gate_verdicts.txt:39`); connes-ncg-theorist (Reading A, converged on (c) T3) + lizzi-spectral-functional-theorist (Reading B, balanced joint verdict T4).
```

**Closeout note on corpus routing**: the regulator-pin four-axis corpus (`cross-pillar-bridge-corpus.md`, Level-axis B.58–B.62) is the ALTERNATIVE home if the closeout prefers the four-axis-orthogonality framing over the §(iv) 4-class framing. The two are cross-linked; this instance fits the §(iv) Level-axis (SCHEMATIC-vs-FULL) most naturally and I route it there. The closeout picks ONE home (no double-land).

---

## (3) Cross-link note for the closeout — W3 `module-as-canonical-K3` weighting-functional-family pins

**Flag**: the W3 `S94-MODULE-AS-CANONICAL-K3` work (verdict line 48; advances the weighting-functional-family K-counter at `cross-pillar-bridge-corpus.md §19`, the SAME §VII.AU CF-37 lineage per plan-w2:751) **does NOT need the SCHEMATIC-vs-FULL two-layer status discipline applied to its weighting-functional-family pins — for a structural reason, not an oversight.**

The §19 weighting-functional family `Φ_w : [φ] ↦ (M_KK/M_Pl)²·∫|λ|^{−s} w(λ) dμ` is fibered over a finite topological base `[φ] ∈ K_0(A_K)`; atlas-row and cache-moment are two MEMBERS of the family (members differ by the weighting `w`, i.e. by the F-image/route, NOT by the substrate-IS observable). The §19 **topological STOPPING rule** (corpus §19, sub-question (c)) is explicit: every weighting factors through the SAME finite `[φ]`, so the K-counter is a **base-count, not a fiber-count** — and the deeper canonical is the Fredholm module (topological shadow `[φ]` + analytic shadow `μ`). The S94-MODULE-AS-CANONICAL-K3 verdict line confirms this verbatim: `topological_stopping_rule=base-count-not-fiber-count`, `rank_K0_A_K=3`, advancement keyed on the K_0 BASE rank-gap (`rank_K0_A_K_PS=4`, `base_distinct=True`), NOT on the weighting/route.

**Consequence**: the SCHEMATIC-vs-FULL distinction my W-1 verdict pins is a distinction at the **fiber/route axis** (which `w`, which evaluator pipeline). §19 has already RULED that axis non-advancing (members of one family over one base; the 3-layer K-counter was explicitly REJECTED at §19(c) — see registry line 18294, the S92 W1 landing's own §19 cross-link). So:

- The W3 K-counter pins (`module-as-canonical-K3`) are correctly counted on the **base axis** (K_0 rank), where the SCHEMATIC-vs-FULL Level-pin distinction is orthogonal and does NOT apply.
- Each INDIVIDUAL weighting-functional-family pin that is sourced via a SCHEMATIC route SHOULD still carry its own `convention=...-SCHEMATIC` + `tier_pin=TIER-2` disclosure per §(iv) K=4 (the per-pin level-pin discipline is unchanged) — but that is the EXISTING per-pin discipline, NOT a new two-layer STATUS split of the §19 family itself.

**Net for the closeout**: NO action needed to apply the W-1 two-layer status discipline to the §19 family-level structure. The W-1 split operates on the §VII.AU.OP-PROJ corridor-discharge STATUS (a single registry status object); the §19 family operates at the base-count K-counter (a different structural object, already stopping-ruled). The two do not collide. If the closeout audits §19 member-pins for per-pin §(iv) disclosure, that is the normal per-pin level-pin check, separate from this verdict.

---

## (4) Carry-forward

**NONE.** The adjudication is **VERDICT-ONLY with no S95 compute.** No genuinely-new compute surfaces:

- The Layer-1 exponent is a registry-PROVEN theorem (canonical-FULL since S91); nothing to compute.
- The Layer-2 SCHEMATIC route is honestly tagged SCHEMATIC-pending; the live FULL-physical Mellin-Barnes extraction at L≥13 is **infeasible** per the recursive-Casimir feasibility pre-check (`math-scripts.md §"D_K Block-Diagonality + Recursive-Casimir-Projection Feasibility Pre-Check"`) — it is NOT a feasible-but-unperformed compute that would warrant a 4-field CF. Per the no-padding discipline (`feedback_fix-in-session-never-defer.md`), a carry-forward must be a genuine future computation with a fillable 4-field spec; "perform the infeasible high-L extraction" fails the effort field by construction. Should the recursive-Casimir-projection feasibility envelope change (e.g., a closed-form per-sector residue route at L≥13 becomes available, OR a Friedrich-Bär analytic-tail extension to the FULL residue VALUE — distinct from the exponent — at higher L), that would be a NEW compute warranting a fresh 4-field spec at that time; it is NOT one now.
- The routed registry edit (1) is a status re-tag landed by mack in-session-adjacent (registry sole-writer), not a compute. The routed corpus row (2) is landed by the Slot-3 closeout. Neither is an S95 compute.

This is consistent with the schedule's framing: *"NO NEW COMPUTE — the numbers stand; the adjudication is over their epistemic status."*

---

## For the closeout (1-line summary)

W-1 CONVERGED on **(c) two-layer split**: §VII.AU.OP-PROJ corridor-discharge status splits into STRUCTURAL-ORTHOGONAL-COMPANION rows — **Layer-1 (cohomology-class exponent `α=−(d−1)=−3`) FULL-RECOVERED** + **Layer-2 (analytic-saturation route) SCHEMATIC-pending tier_pin=TIER-2**; bare (a) WITHDRAWN, single-row (b) REJECTED as lossy axis-collapse, verdict-line 39 UNEDITED (option (d) satisfied without supersede); routed edit → mack (registry), routed corpus row → Slot-3 closeout (§(iv)/pru-class-corpus §12, ENRICH-default); W3 §19 weighting-functional-family needs NO two-layer status discipline (base-count stopping rule already excludes the fiber/route axis); verdict-only, NO S95 compute.
