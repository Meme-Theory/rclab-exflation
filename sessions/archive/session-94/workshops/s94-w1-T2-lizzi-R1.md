# W-1 Turn 2 — lizzi-spectral-functional (Reading B steelman + rebuttal of connes T1)

**Workshop**: W-1, §VII.AU.OP-PROJ α=−3 discharge status adjudication
**Turn**: T2 (steelman of Reading B + direct rebuttal of connes's T1; NO final verdict — that is T4)
**Agent**: lizzi-spectral-functional-theorist (Reading B owner)
**Opponent**: connes-ncg-theorist (Reading A; author of W2-3; T1 steelman complete)
**Adjudication options**: (a) FULL-RECOVERED honest; (b) structural-posit/consistency-confirmed, SCHEMATIC cap preserved; (c) two-layer split; (d) re-tag fine but verdict-line/registry reconciled.
**This turn argues for (b)** — and, where connes demands a containment, shows that **(c) is the floor, not (a)**: the two-layer split is correct PRECISELY because it concedes connes's exponent theorem in full AND preserves the SCHEMATIC cap on the OBSERVABLE. Bare (a) FULL-RECOVERED is rejected. The disagreement with connes is NOT about the exponent; it is about what "FULL-RECOVERED" predicates over.

---

## 0. The thesis, stated once and sharply

> connes has proven a theorem about the **EXPONENT**. He has not earned a status claim about the **OBSERVABLE**. The two are different objects on the substrate, and the status tag `FULL-RECOVERED` attaches to §VII.AU.OP-PROJ's corridor — whose content is the observable's binding Level-2/Level-3 numerical anchor, not the bare exponent. I concede every line of connes's §II/§III: `α = −(d−1) = −3` is a registry-PROVEN, regulator-invariant, L-independent cohomology-class identity, and the F₂ contour-deformation identity `sub_term_R(Mellin) = sub_term_R(zeta) = 0` is real. None of that is in dispute. What IS in dispute is whether a PROVEN structural EXPONENT discharges the CORRIDOR's numerical-FULL-extraction deferral, OR only its structural-exponent sub-question. My claim, sourced from the framework's own canon: **the canonical constant `alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC = −3` already carried `CLASS=FULL` BEFORE W2-3 ran** (get_constant provenance, S91 W-5, promoted S93 W2-3). The exponent was never the deferred object. The constant's own provenance names what was deferred: `asymptotic-limit-derivation-DEFERRED-to-CF-S94-W5-3` — the certification that the analytic-saturation EVALUATION ROUTE lands on the theorem. W2-3 discharged that route-certification, and it carries `LEVEL_CLASS_PIN=SCHEMATIC tier_pin=TIER-2` because the route is the SCHEMATIC two-pin convergence-exponent protocol. A SCHEMATIC route-certification of a structurally-known exponent is **(b) consistency-confirmed**, not (a) FULL-RECOVERED of the corridor; and the minimum honest containment is **(c)**, which writes the seam down explicitly: FULL at the cohomology-class/exponent layer, SCHEMATIC-pending at the numerical-FULL-extraction layer of the OBSERVABLE.

The rest of this turn is (I) the substrate-first framing that makes the exponent/observable distinction load-bearing, (II)–(IV) the steelman of (b)/(c) on the §(iv) level-pin axis, and (V)–(VIII) the direct rebuttal of connes's four pre-positioned moves.

---

## I. SUBSTRATE-FIRST framing — and where connes's direction-of-explanation argument over-reaches

connes opens (T1 §I) by asserting that the substrate-first direction "is the whole ballgame," and that Reading B "only has force if you (silently) invert this direction." I accept the direction completely and invert nothing. Here is the substrate-first reading, done correctly:

The substrate IS the spectral triple `(A_K, H_K, D_K)` at the substrate-distance-1 Mellin-cone pole `s = 3`. On this substrate there are TWO distinct substrate-IS objects, and conflating them is the error:

1. **The cone exponent** `α = −(d−1)`. This is a property of the **dimension-spectrum pole** — fixed by the orientability/dimension/regularity axioms. It is substrate-IS, regulator-invariant, L-independent. **I concede it is recovered as a structural fact.**

2. **The corridor observable** — the §VII.AU.OP-PROJ Level-2 binding envelope's numerical content: the residual `R_b(L) = ρ_FULL(s=3,L) − ρ_FULL(s=3,∞)` and its FULL-physical approach to the `L_max → ∞` HKR image, i.e. the *amplitude-and-rate* of the substrate's own presentation of its continuum shadow. This is ALSO substrate-IS. It is what a Level-3 anchor pins. It is what the corridor `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED` was deferring (the word is NUMERICAL).

connes's direction-of-explanation argument establishes that object (1) is prior to its finite-L numerical shadow. **Granted.** But it does not establish that object (1) IS object (2). The substrate-first direction tells you the exponent is logically prior to the numbers; it does NOT tell you that proving the exponent's value discharges a deferral whose own name is "NUMERICAL." The substrate has both a cone exponent and a continuum-approach amplitude/rate; recovering the first as a theorem leaves the second's FULL-physical numerical extraction exactly where it was. There is no inversion in Reading B — there is a refusal to let "the exponent is prior" silently become "the exponent is the whole corridor."

This is my home discipline: **what survives across spectral-functional choices is structural; what depends on the choice is a physical degree of freedom.** The exponent `−(d−1)` survives the F₂ regulator-class choice (connes's §III; conceded — it is FI). The corridor's numerical anchor is sourced through a SCHEMATIC route (the two-pin protocol via `rho_FULL_CC_VII_AU_SAT_s3`), and a SCHEMATIC-vs-FULL choice is, by `substrate-first-canonical-sourcing.md §(iv)`, a level-pin axis with observable consequences. The status tag must report BOTH facts. (a) reports only the first; (b)/(c) report both.

---

## II. The canonical record already separates exponent from corridor — connes's own pre-compute audit proves Reading B's premise

The single most decisive fact in this adjudication is in connes's OWN MCP pre-compute audit (WP §W2-3 line 153), which I now turn against (a):

```
get_constant("alpha_canonical_VII_AU_OP_PROJ_FW_ASYMPTOTIC") → −3.0
   CLASS=FULL ;  asymptotic-limit-derivation-DEFERRED-to-CF-S94-W5-3
   (S91 W-5 EMERGENCE row 5; promoted to canonical S93 W2-3)
```

Read this carefully, because it dismantles connes's framing of what W2-3 did.

- **The exponent value −3 was canonical, with `CLASS=FULL`, in S91 — three sessions before W2-3.** connes is therefore *right* (T1 §II.2) that "W2-3 did not establish −3; the structural answer was already PROVEN." I concede this entirely. But it cuts the other way: if the exponent was already canonical-FULL, then the SCHEMATIC tag on the W2-3 verdict line is NOT tagging the exponent (which is independently FULL). It is tagging the **W2-3 discharge object** — the analytic-saturation route-certification.

- **What the constant's provenance says was DEFERRED is not "the exponent" but "the asymptotic-limit-DERIVATION."** That is a statement about an *evaluation route* reaching the limit, not about the limit's value. W2-3's own hypothesis (WP §W2-3 line 143) confirms this verbatim: "the asymptotic α is **read by the analytic-saturation route**." The deferred object was the route-certification; the discharged object was the route-certification; and the route is SCHEMATIC.

So the structure is:

| Object | Status BEFORE W2-3 | What W2-3 did | Tag carried |
|:-------|:-------------------|:--------------|:------------|
| Cone exponent `−(d−1) = −3` | canonical, `CLASS=FULL`, PROVEN | nothing — already proven | (inherits FULL from the S91 constant) |
| Analytic-saturation route-certification | DEFERRED (`CF-S94-W5-3`) | DISCHARGED | `LEVEL_CLASS_PIN=SCHEMATIC tier_pin=TIER-2` |
| Corridor numerical-FULL-extraction (live Mellin-Barnes at L≥13) | infeasible / not performed | still not performed | (the residual caveat) |

connes's (a) collapses rows 1 and 2: he reads "FULL-RECOVERED" as "the corridor's content (= the exponent) is recovered as a structural fact," and since row 1 is FULL, he concludes the corridor is FULL-recovered. But the corridor's deferral was row 2/row 3 — the NUMERICAL route and its FULL extraction — not row 1. **You cannot discharge a NUMERICAL-DEFERRED corridor by pointing at a structural theorem that was already canonical before the corridor's discharge gate ran.** If the exponent-being-FULL were sufficient for FULL-RECOVERED, the corridor would have been FULL-RECOVERED in S91, and there would have been nothing for W2-3 to discharge. connes's own "W2-3 did not establish −3" admission forces this: the thing W2-3 discharged is exactly the SCHEMATIC route-certification, and a status tag honest to W2-3's actual deliverable is **(b)/(c)**, not (a).

---

## III. The §(iv) level-pin axis: a SCHEMATIC-tier discharge cannot license a FULL-RECOVERED status tag on the OBSERVABLE

This is my home turf (`substrate-first-canonical-sourcing.md §(iv)`, K=4 MANDATORY; the 4-class POSITIVE/PARTIAL-POSITIVE/MIXED-PARTIAL/NEGATIVE taxonomy). The rule's purpose statement is verbatim:

> "gate verdicts under SCHEMATIC helpers are NOT structurally indistinguishable from FULL-physical verdicts in downstream consumption — class-conflation pathology analogous to UV-regulator conflation (`UV_REGULARIZATION_CONFLATION`)."

connes (T1 §IV.1) reads this narrowly: the SCHEMATIC tag "answers exactly one question — if a downstream gate imports this scalar `−3` and treats it as a FULL Pauli-Villars number, is it being misled about the sourcing protocol?" That is correct as far as it goes. But notice what it concedes: **a downstream consumer of this row IS at risk of being misled if it treats the discharge as FULL.** The status tag `FULL-RECOVERED` in the registry IS a downstream-facing claim — it is the EXACT signal a future gate reads to decide "may I consume §VII.AU.OP-PROJ's asymptotic-α as a FULL-physical anchor?" If the registry says FULL-RECOVERED and the verdict line says SCHEMATIC, the §(iv) class-conflation pathology fires at the registry/status layer — the very layer that has the highest downstream leverage.

### III.1 The 4-class taxonomy classifies THIS discharge as NOT-FULL by construction

Apply the taxonomy to the W2-3 verdict line as it stands:

- It carries `LEVEL_CLASS_PIN=SCHEMATIC` (companion lines 38/44).
- It carries `tier_pin=TIER-2`.
- It carries `convention=RATIO-ASYMPTOTIC-LAYER-1` — which does NOT carry a `-SCHEMATIC` suffix on the convention field. (Per §(iv) item 2, SCHEMATIC requires `convention=<scheme>-SCHEMATIC`; this row pins SCHEMATIC via the LEVEL_CLASS_PIN companion row instead. That is a disclosure-completeness question separate from this adjudication, but it confirms the SCHEMATIC class IS declared.)

The taxonomy's whole architecture exists so that a SCHEMATIC-class verdict is **structurally distinguishable** from a FULL-physical verdict downstream. A registry status of `FULL-RECOVERED` **erases that distinction at the status layer** — it tells every downstream consumer "this is recovered, full stop," which is precisely the indistinguishability the rule forbids. The honest status must preserve the SCHEMATIC class into the status tag. That is what (b) does ("structural-posit / consistency-confirmed, SCHEMATIC tier_pin preserved") and what (c) does (FULL at the exponent layer, SCHEMATIC-pending at the numerical layer, written as distinct rows). (a) does neither.

### III.2 connes's orthogonality argument is correct AND insufficient — it licenses the EXPONENT's FULL status, not the OBSERVABLE's

connes's §IV.3 crisp claim is:

```
SCHEMATIC(scalar-pin provenance)  ⊥  STRUCTURAL(exponent = −(d−1))
```

**I concede this orthogonality entirely.** The Level axis (SCHEMATIC-vs-FULL) and the cohomology-class axis (the exponent's structural value) ARE pairwise-independent per `regulator-pin-discipline.md §"four-axis orthogonality"`. A producing script genuinely may PASS one while FAILing another. So: `SCHEMATIC(provenance) ∧ STRUCTURAL(exponent=−3)` is consistent. **Granted, with no reservation.**

But this is exactly where connes's argument runs out of road, and the over-reach is precise. Orthogonality means the SCHEMATIC tag does not VETO the exponent's structural truth. It does NOT mean the exponent's structural truth LICENSES a FULL-RECOVERED tag on the OBSERVABLE. Those are different inferential directions:

- connes needs (to defeat me): "exponent is structurally FULL on axis X" `⇒` "observable is FULL-RECOVERED on the binding axis Y."
- Orthogonality gives him only: "axis X's truth-value does not constrain axis Y's truth-value."

If the axes are orthogonal, then a FULL result on the cohomology-class axis says **nothing** about the FULL/SCHEMATIC status on the binding/Level axis — and the binding/Level axis reads SCHEMATIC (the verdict line says so). connes cannot have it both ways: he invokes orthogonality to protect the exponent from the SCHEMATIC tag, but orthogonality then equally forbids the exponent's FULL status from propagating onto the binding axis to make the OBSERVABLE FULL-RECOVERED. **An invariant exponent on one axis does not entail FULL recovery of the observable on the binding axis — that is the literal content of the orthogonality connes himself asserts.** So orthogonality, far from rescuing (a), is the cleanest proof of (c): the two axes carry two independent statuses, FULL on one, SCHEMATIC-pending on the other, and the honest status writes both.

### III.3 The formal restatement (turning connes's own proof sketch around)

```
Claim (Reading B):  STRUCTURAL-FULL(exponent, cohomology-class axis)
                    does NOT entail
                    FULL-RECOVERED(corridor observable, binding/Level axis).

Proof (from connes's own orthogonality):
  - connes proves: Level axis (SCHEMATIC) ⊥ cohomology-class axis (exponent).
  - Orthogonality ⇒ truth-value on the cohomology-class axis does not CONSTRAIN
    the truth-value on the Level axis (and vice versa).
  - "FULL-RECOVERED(corridor)" is a Level-axis / binding-axis status claim about the
    OBSERVABLE's numerical anchor (the corridor's NUMERICAL-DEFERRED content).
  - The Level axis independently reads SCHEMATIC (verdict companion lines 38/44).
  - By orthogonality, the cohomology-class FULL result cannot upgrade the Level-axis
    SCHEMATIC reading to FULL.
  ⇒ FULL-RECOVERED(corridor) is NOT licensed by STRUCTURAL-FULL(exponent).
  ⇒ The honest joint status is the PAIR: (cohomology-class: FULL/PROVEN ; Level: SCHEMATIC-pending)
     = option (c), with (b) as its single-row contraction.  ∎
```

connes needs `SCHEMATIC ⇏ ¬FULL-RECOVERED` (his §IV.3). I grant him that the SCHEMATIC tag does not by itself NEGATE recovery of the *exponent*. But the burden for (a) is the converse: he needs `STRUCTURAL-FULL(exponent) ⇒ FULL-RECOVERED(corridor)`, and orthogonality denies him exactly that implication.

---

## IV. The honest status names: (b) and (c), and why (a) is the only inadmissible option

- **(b) structural-posit / consistency-confirmed, SCHEMATIC tag preserved.** This is the single-row honest status: the exponent is a standing theorem (posit), and W2-3 confirmed the frozen-cache residual is *consistent* with it (R²=0.99988; the canonical sample reproduced to machine precision). The SCHEMATIC tier_pin stays on the status because the discharge route was SCHEMATIC. This is the most economical correct tag.

- **(c) two-layer split.** This is the structurally richest correct status, and it is the one I expect to converge on at T4 IF connes insists the exponent's recovery deserves a FULL annotation somewhere. (c) gives it to him — at the cohomology-class/exponent layer, FULL-RECOVERED — while pinning SCHEMATIC-pending at the numerical-FULL-extraction layer of the observable. (c) is the registry-anatomy-faithful option: it matches the framework's own Level-1 (cohomology-class identity, regulator-invariant) vs Level-2/3 (binding envelope, numerical anchor) ladder. The exponent lives at Level-1; the corridor's numerical anchor lives at Level-2/3; their statuses differ; (c) writes both.

- **(a) FULL-RECOVERED (bare).** Inadmissible. It propagates a SCHEMATIC-route discharge into a FULL-physical status claim on the observable, firing the §(iv) class-conflation pathology at the highest-leverage (status/registry) layer, and it relies on an inference (`exponent-FULL ⇒ corridor-FULL`) that connes's own orthogonality argument forbids.

Note what I am NOT claiming: I am not claiming the SCHEMATIC tag *caps the exponent*. The schedule's framing of Reading B ("the SCHEMATIC tag CAPS the recovery") is true only when "recovery" means *recovery of the corridor observable's FULL-physical numerical anchor* — NOT recovery of the exponent. I concede the exponent is recovered. The cap is on the OBSERVABLE-status claim, exactly where (a) overreaches.

---

## V. Rebuttal of connes's §V — "the in-window values CONFIRM −3" is consistency-with-the-posit, NOT independent extraction

connes's §V argues the pre-asymptotic in-window values (−2.876533 at L=35, −2.959997 at L=100) are "the positive-finite-L-correction signature, predicted by the structure," and that the model `α(L) = −(d−1) − (C₁/L)/(1+C₁/L)` reproducing them "reproduces the gate from the structural model." I rebut on the precise epistemic status of that reproduction.

### V.1 The reproduction is forced by the assumed basis — Sage-confirmed this turn

I re-ran connes's α(L) model exactly (C₁ = −3.846, d=4):

| L | α(L) at assumed p=−3 | verdict line |
|:--|:---------------------|:-------------|
| 35 | −2.876549 | −2.876533 ✓ (Δ 1.6e-5, C₁-rounding, as connes notes) |
| 100 | −2.960002 | −2.959997 ✓ |
| ∞ | −3.000000 | −3.000000 ✓ |

So connes is **right** that the model reproduces the verdict in-window numbers. But here is the structural point he omits. I tested whether the in-window local slope *pins* the exponent p, or merely *inherits* it:

```
Window-separability test — local slope α(L) for p ∈ {−2.7, −3.0, −3.3}, same C₁ tail:
  p=−2.7:  α(35)=−2.5765   α(100)=−2.6600   asymptote=−2.7
  p=−3.0:  α(35)=−2.8765   α(100)=−2.9600   asymptote=−3.0
  p=−3.3:  α(35)=−3.1765   α(100)=−3.2600   asymptote=−3.3
```

The in-window slopes shift **RIGIDLY** with the assumed leading exponent p. The `(C₁/L)/(1+C₁/L)` tail is **p-independent** (an additive offset). So the in-window data cannot separate p=−3 from p=−2.7 or p=−3.3 — those are distinguished ONLY by the `L → ∞` limit, which is **never sampled** (the window is [35,100], the limit is ∞, and L≥13 is undiagonalized).

**Consequence:** the fit `R_b(L) = c·L^{−3}(1+C₁/L)` fixes the leading exponent p = −3 *as INPUT* (the cohomology-class theorem, supplied), then fits the amplitude c and correction C₁ to the 11 frozen-cache points. The R²=0.99988 and the machine-precision reproduction of the sample 2.6926 certify that **the frozen-cache data is consistent with the posited p=−3**. They do NOT independently *extract* p=−3 from the data — because, as the separability test shows, the in-window data is equally consistent (with a different fitted C₁) with a nearby p. The asymptote is the input; the fit is the consistency certificate.

This is precisely my taxonomy: **what is FI (the exponent) was supplied as a theorem; what the fit measured (amplitude, correction) is the scheme-dependent / finite-L content.** connes's §V.2 says "the corridor was always about the leading exponent, which is C₁-independent." Correct — and that is exactly why the *fit cannot have measured it*: a quantity the fit holds C₁-independent and supplies as input is not a quantity the fit extracted. So "confirms −3" is true in the sense of *consistency-with-the-posit*, and false in the sense of *independent-extraction-of-the-asymptote*. The honest status of a consistency-confirmed posit is **(b)**, by definition of the words.

### V.2 Why this matters for the status tag

connes wants the in-window confirmation to upgrade the status to FULL-RECOVERED. But a consistency check of a structurally-supplied exponent against a frozen-cache residual is the textbook content of "structural-posit / consistency-confirmed" — option (b)'s exact name. It is strong evidence; I do not dispute its strength (R²=0.99988 is overwhelming consistency). I dispute that consistency-with-a-posit is the same epistemic object as FULL-physical extraction of the observable. They are not, and the §(iv) taxonomy exists to keep them distinct in downstream consumption.

---

## VI. Rebuttal of connes's §VI — Friedrich-Bär freezes the bottom-K, but freezing the GENERATING SPECTRUM is not FULL-EXTRACTING the OBSERVABLE

connes's §VI is his strongest section, and I engage it head-on rather than around it. His claim: the bottom-K is structurally frozen above L=12 (NEW-sector floor 8.43 ≫ bottom-K ceiling 0.845, ~10× margin), so the residual R_b(L) in the window is "the FULL-physical residual continued from the frozen cache, and its decay is the EXACT analytic L^{−3} tail — NOT an imposed fit basis." Therefore `1/L³` is "the *derived* asymptotic form of a residual whose generating spectrum is frozen."

I concede the freezing. The Friedrich-Bär saturation is real and the ~10× margin is not in doubt. But the inference from "generating spectrum frozen" to "observable FULL-extracted" has a gap:

### VI.1 What freezing buys, and what it does not

Freezing the bottom-K above L=12 establishes that the residual `R_b(L)` for L ∈ [35,100] is *determined by* the L≤12 cache — i.e., no NEW sector perturbs it. **Granted.** This means the residual is well-defined and computable in principle across the window from frozen data. But:

- The window L ∈ [35,100] was **never actually evaluated** as a FULL-physical residual. The R_b(L) data that was FIT is the L ∈ [12,22] cache (11 points; WP §W2-3 Step B). The [35,100] window is where the SIGNED exponent α(L) is *extrapolated* (WP §W2-3 Step C: "extrapolate the signed exponent to the saturated L[35,100] window"). The word in the WP is "extrapolate."
- "The decay is the EXACT analytic L^{−3} tail" is true **only if the leading exponent is −3** — which is the cohomology-class theorem, supplied as input. Freezing the spectrum tells you the residual is a fixed analytic function of L; it does NOT independently tell you that function's leading exponent is −3. That value still comes from the pole/axiom argument, not from the frozen cache. (The frozen cache is 11 points at L∈[12,22]; fitting them with p free vs p=−3 fixed is the separability question of §V.1 — the cache cannot pin p on its own.)

So Friedrich-Bär upgrades the claim from "we fit a synthetic envelope" (the superseded line 33, C₁=0 fallback) to "we fit the REAL frozen-cache residual" (the canonical line 39, C₁=−3.846). That is a genuine improvement, and it is why line 39 supersedes line 33 — honest in-session correction, not convention-shopping. **But it does not convert a supplied-exponent + consistency-fit into a FULL-physical extraction of the corridor observable's asymptote.** The asymptote is still the theorem; the window is still extrapolated; the FULL Mellin-Barnes evaluation at L≥13 is still infeasible and still not performed.

### VI.2 connes's §VI.3 reframe ("assumed the basis, but the basis is the theorem") supports (b)/(c), not (a)

connes concedes (§VI.3): "Yes, the fit assumed `L^{−3}`. But that is not circular, because `L^{−3}` is not a free hypothesis being tested — it is the cohomology-class theorem being USED as input, and the fit's job is to verify consistency."

This is, word for word, the definition of option (b). "The exponent is known (theorem); the fit checks that the FULL-physical residual is consistent with it (it is, overwhelmingly)." That is **structural-posit (the theorem) + consistency-confirmed (the fit)** with the SCHEMATIC route preserved. connes has described (b) and then labeled it (a). The label is the disagreement; the content is (b). When the producing agent's own steelman of (a) reduces, in its most careful section, to a verbatim statement of (b)'s definition, the honest tag is (b) — or (c) if we want to additionally record FULL at the exponent layer.

---

## VII. Rebuttal of connes's §II/§III — the PROVEN exponent theorem discharges the structural-exponent sub-question ONLY, not the corridor's numerical deferral

connes's §II/§III establish two registry-PROVEN facts:
1. `−(d−1) = −3` is a regulator-invariant, L-independent cohomology-class identity (§II; Sage `−(d−1)=−3`).
2. The F₂ contour-deformation identity `sub_term_R(Mellin) = sub_term_R(zeta) = 0` makes the exponent FI across {Mellin, zeta} (§III; Cauchy, no pole crossings at s=3).

**I concede both in full.** I have verified both against the knowledge base (search_knowledge returned the PROVEN Level-1 theorem and the structural F₂ equation). On my own FI/RD taxonomy, the exponent is unambiguously **FI** — it is the SHELL-axis/leading-term sibling of the β_shell FI theorem connes cites, and I do not contest its FI classification.

But concede-and-localize is the move. Ask the adjudication question precisely: **does the PROVEN exponent discharge the OP-PROJ corridor's numerical-extraction deferral, or only its structural-exponent sub-question?**

- The corridor's name is `CORRIDOR-CONFIRMED-NUMERICAL-DEFERRED`. The deferred object is, by its own name, **NUMERICAL** — the binding Level-2 envelope's numerical anchor / the observable's `c_continuum` reference.
- The PROVEN theorem is about the **EXPONENT** — a structural fact at Level-1.
- Level-1 (cohomology-class identity, regulator-invariant) and Level-2/3 (numerical binding envelope, empirical anchor) are DISTINCT rungs of the framework's own three-level ladder (`cross-pillar-bridge-anatomy.md`). The exponent's recovery discharges the **Level-1 structural-exponent sub-question**. It does not discharge the **Level-2/3 numerical sub-question**, because those are different rungs with different evidence types (regulator-invariant identity vs numerical evaluation at canonical L_max).

So the PROVEN exponent answers "is the exponent −3?" (yes, structurally) — NOT "is the corridor's numerical FULL anchor recovered?" The latter is what `NUMERICAL-DEFERRED` named, and it is sourced through the SCHEMATIC two-pin protocol. **Is the corridor's `c_continuum` / numerical anchor FULL-recovered, or only the exponent?** Only the exponent. The `c_continuum` (the `ρ_FULL(s=3,∞)` continuum image) is approached via the frozen-cache fit + supplied-exponent extrapolation, tagged SCHEMATIC — not via a FULL Mellin-Barnes evaluation. The honest discharge is therefore partial-by-layer: Level-1 FULL, Level-2/3 SCHEMATIC-pending = option (c).

---

## VIII. Summary of the steelman + rebuttal (Reading B)

**Conceded to connes (no reservation):**
1. `α = −(d−1) = −3` is a registry-PROVEN, regulator-invariant, L-independent cohomology-class identity. Sage `−(d−1)=−3` exact. Verified in the knowledge base.
2. The F₂ contour-deformation identity (`sub_term_R(Mellin)=sub_term_R(zeta)=0` at s=3) makes the exponent FI. Conceded; it is the leading-term sibling of the β_shell FI theorem on my own taxonomy.
3. Friedrich-Bär freezes the bottom-K above L=12 with a ~10× margin; the residual R_b(L) is determined by the L≤12 cache.
4. SCHEMATIC(provenance) ⊥ STRUCTURAL(exponent): the SCHEMATIC tag does not VETO the exponent's structural truth.

**Rebutted (where (a) overreaches):**
1. **The exponent was canonical-FULL (CLASS=FULL) in S91, before W2-3.** What W2-3 discharged is the `asymptotic-limit-DERIVATION` (route-certification), per the constant's own provenance — and that route is the SCHEMATIC two-pin protocol. A status tag honest to W2-3's actual deliverable is (b)/(c), not (a). (§II)
2. **Orthogonality cuts both ways.** It protects the exponent from the SCHEMATIC tag, but it equally forbids the exponent's FULL status from propagating onto the binding/Level axis to make the OBSERVABLE FULL-RECOVERED. `STRUCTURAL-FULL(exponent) ⇏ FULL-RECOVERED(corridor)` — by connes's own orthogonality. (§III)
3. **The in-window confirmation is consistency-with-the-posit, not independent extraction.** Sage separability test: in-window α(L) slopes shift rigidly with the assumed p; the window cannot pin p (distinguished only by the never-sampled L→∞ limit). The fit supplies p=−3 as input and certifies consistency (R²=0.99988) — the verbatim definition of (b). (§V)
4. **Freezing the generating spectrum ≠ FULL-extracting the observable.** The [35,100] window is extrapolated (WP's word), not evaluated; the asymptote is the theorem, not a cache extraction; the FULL Mellin-Barnes evaluation at L≥13 is infeasible and unperformed. (§VI)
5. **The PROVEN exponent discharges the Level-1 structural-exponent sub-question only.** The corridor's deferral is NUMERICAL (Level-2/3); the `c_continuum` numerical anchor is SCHEMATIC-sourced, not FULL-recovered. (§VII)
6. **connes's own §VI.3 reduces (a) to (b).** "The exponent is known (theorem); the fit checks consistency" IS the definition of structural-posit/consistency-confirmed. (§VI.2)

**Status argued this turn**: **(b) structural-posit / consistency-confirmed with the SCHEMATIC tier_pin PRESERVED** is the economical correct status; **(c) two-layer split** (FULL-RECOVERED at the cohomology-class/exponent Level-1 layer; SCHEMATIC-pending at the numerical-FULL-extraction Level-2/3 layer) is the floor I will accept if connes insists the exponent's recovery be annotated FULL somewhere. (c) is the registry-anatomy-faithful option and PRESERVES connes's exponent theorem in full while honoring the §(iv) level-pin discipline. **Bare (a) FULL-RECOVERED is rejected**: it propagates a SCHEMATIC route-discharge into a FULL status claim on the observable, fires the §(iv) class-conflation pathology at the highest-leverage status layer, relies on an inference connes's own orthogonality forbids, and — by connes's §VI.3 — its careful form just IS (b) under a different label.

The disagreement reduces to one sentence: **connes proved the EXPONENT; the status tag predicates over the OBSERVABLE's CORRIDOR; orthogonality forbids the first from upgrading the second.** What is structural (the exponent) survives every regulator choice — I built my career on saying so. What is scheme-dependent (the SCHEMATIC route to the corridor's numerical anchor) is a physical-degree-of-freedom-level fact that the status tag must report. (b)/(c) report both; (a) reports only the half that flatters the discharge.

*(Steelman + rebuttal only — final balanced JOINT VERDICT is my T4, after connes's T3 rebut-and-refine.)*
