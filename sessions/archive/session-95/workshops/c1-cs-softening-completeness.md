# Session 95 Workshop: sp x volovik — C1→ASYMMETRIC robustness to critical-softening c_s(τ)

**Date**: 2026-05-29
**Format**: Iterative 2-agent workshop (3 rounds, 6 turns)
**Agents**: sp (schwarzschild-penrose-geometer), volovik (volovik-superfluid-universe-theorist)
**Source Documents**:
- `sessions/archive/session-95/session-95-w4-workingpaper.md`
- `computations/session-95/s95_gate_verdicts.txt`

**Focus Topics** (the adjudication sub-questions):
1. **(a)** Is c_BLV = 0.485 M_KK constant THROUGH the van Hove fold (τ=0.190), or does c_s(τ) soften toward zero by DOS-divergence/criticality (diverging compressibility at the DOS singularity where BCS pairing turns on)?
2. **(b)** If c_s dips at the fold, can (c²−v²) admit a SECOND zero on the FAR side as c_s recovers — a c_s-driven symmetric mechanism ORTHOGONAL to the v-driven one W4-1 excluded — and does W4-1's N_zeros=1 survive a non-constant c_s(τ)?
3. **(c)** Does the C1 → ASYMMETRIC verdict (and the dropped transit V.6 "two distinct horizons" STRENGTHEN clause) hold under the substrate-correct c_s(τ), or must C1 be re-opened / re-stated as conditional?

**Anchor verdict under test**: W4-1 `S95-W4-1-WHITE-HOLE-KINEMATIC-CONSISTENCY` PASS (N_zeros=1 at τ₀=0.1125; c=c_BLV=0.485 held constant; only v(τ) varies; dS/dτ=+58673 keeps v supersonic past the fold; κ_entry=+18.520 M_KK; monotone_supersonic_exit=True). S85 symmetric two-crossing (τ_H±=0.18314/0.19686) was diagnosed by W4-1 as a tanh²-c_s-dip + constant-v model artifact.

**Deconfliction (orchestrator mandate)**: a concurrent phonon-first doc-incorporation agent is the SOLE writer of `sessions/framework/phonic-exflation-equation.md` this round and is HOLDING §6.2 white-hole-symmetry PENDING this workshop. The final agent MUST NOT edit `phonic-exflation-equation.md`; route §6.2 effects to the w4 WP constraint-map + the "Doc-routing (PENDING)" section of this file.

---

## Round 1 — sp: Opening Analysis (constant-c / monotone-dS/dτ steelman)

### SP1: Sub-question (a) — Is c_BLV constant through the van Hove fold?

**Key finding (steelman): the discriminant speed in W4-1 is c_BLV — the SCALAR transit/fabric mode — and the DOS-divergence softening at the fold belongs to a DIFFERENT branch (c_BA, the Bogoliubov-Anderson condensate mode). Constant c_BLV through the fold is the substrate-correct choice; it is not the mode that softens.**

The framework does not have *a* sound speed. It has a **four-speed hierarchy** (SOUND-SPEED-64, PASS): `c_mod = 1.0` (emergent photon, exact theorem), `c_BLV = 0.485` (Brillouin-Landau-Vortex scalar fabric mode, S64 `s64_sound_speed`), `c_BA = 0.39908398828309` (Bogoliubov-Anderson condensate mode, S56 `s56_cba_sound.py`), `c_L = 0.025` (Leggett phase-mode, R-protected). These are four DISTINCT phonon branches of the same D_K spectrum, with distinct group velocities and distinct τ-dependence. W4-1's acoustic metric `ds² = −(c²−v²)dt²` sources `c = c_BLV` because c_BLV is the canonical **scalar transit speed** — the mode in which the modulus deformation itself propagates (S62+, `s67_transit.py`; baseline-findings-s66 "Mach number = 13.75 = transit speed / sound speed", with sound speed = c_BLV).

**The softening volovik will invoke is real, but it is c_BA / c_B2, not c_BLV.** The decisive substrate fact (Landau, session-32-w4-r2-qa-landau; Tesla, session-40-tesla-collab-addendum): at the fold the **B2 group velocity vanishes**, `v_B2 = 0`, so the B2 acoustic impedance `Z_{B2}(τ_fold) = ρ_DOS · v_{B2} = 0`. The van Hove DOS divergence IS the statement that the **B2 optical-band-edge** mode goes dispersionless at the fold (`rho_B2(fold) = 14.023`, B2 mult-8 optical band edge, S22c/S52). The mode that "softens toward zero by DOS divergence" is the condensate/band-edge branch — exactly the mode whose vanishing group velocity CAUSES the DOS singularity (ρ ~ 1/(πv)). It is not the scalar transit mode c_BLV, whose value 0.485 is a global a_n-moment functional of the spectrum (an integrated quantity), not a band-edge group velocity that can be driven to zero at one τ.

**Substitution chain — why the softening mode and the discriminant mode are different objects:**
- Step 1: DOS at the fold, `ρ(τ) ~ 1/(π v_band(τ))` — diverges because `v_band = v_B2 → 0` (1D van Hove form, session-32-w4-r2-qa-landau). [Source: S32 W4, S52 ρ_B2=14.023]
- Step 2: c_BLV is NOT v_band. c_BLV = 0.485 is the scalar-mode transit speed, a fixed a_n-moment functional (S64; reproduced bit-stable across S63/S64/S67/S82 as `c_BLV = 0.48487503688809`). [Source: get_constant(c_BLV)=0.485; session-82 WP]
- Step 3: The Mach number that drives the transit is `Mach = v_flow / c_BLV = 13.75` with c_BLV the *denominator*. If c_BLV softened to 0 at the fold, Mach would DIVERGE, not peak at 13.75. The corpus reports a FINITE peak Mach 13.735 ≈ 13.75 (W4-1 Results; baseline-findings-s66). A finite peak Mach is direct evidence that the discriminant denominator c_BLV does NOT collapse at the fold. [Source: W4-1 `Mach 13.735`; S66 Mach=13.75]
- Conclusion: c_BLV is legitimately constant through the fold. The softening is a property of c_B2/c_BA (the band-edge condensate mode), which is a SEPARATE acoustic channel from the one (c²−v²) is built on.

**The impedance theorem makes the softening self-limiting even in its own channel.** Landau (S32 W4): "At any 1D van Hove fold singularity, the acoustic impedance Z = ρ·v is FINITE because the DOS divergence (ρ ~ 1/(πv)) and the velocity vanishing (v → 0) cancel exactly," `Z_wall = 1/π ≈ 0.318` (canonical Z = 1.016 at the WALL, atlas-07). So even in the B2 channel the softening v_B2 → 0 is the *cause* of the DOS divergence, not an independent free dip; the two are locked by ρv = const. This is the substrate's own statement that the fold is a controlled van Hove cusp (A₂ catastrophe, structurally stable — atlas-04), not a criticality where a transit speed runs to zero unboundedly.

**Structural implication for C1:** The premise of the c_s-softening challenge — "c_BLV = 0.485 may soften toward zero at the DOS singularity" — mis-assigns the softening to the wrong branch. The acoustic-white-hole discriminant in §6.2 is built on the scalar transit channel (c_BLV), and that channel's speed is a fixed spectral-moment functional. The B2 band-edge softening is real but lives in a different acoustic metric (the B2 channel, where v_B2→0 produces the DOS that DRIVES BCS, not a sonic horizon of the transit flow). C1 is a statement about the **transit-flow** acoustic metric; constant c_BLV is correct there.

**Question for volovik:** The softening you will invoke is, in the corpus, the B2 optical-band-edge group velocity v_B2 → 0 (Z_B2 = ρ·v_B2 = 0, S40/S52), which is the SAME vanishing that produces the van Hove DOS that drives BCS. Concretely: (i) Is your proposed c_s(τ) dip the B2 condensate mode c_BA/c_B2, or do you claim the SCALAR transit mode c_BLV itself develops τ-dependence near the fold? (ii) If it is the B2 mode, what is the substrate argument that the §6.2 transit-flow acoustic metric `ds² = −(c_BLV²−v²)dt²` should be re-built on the band-edge mode v_B2 rather than the scalar transit mode — given that the flow v(τ) is the modulus's own propagation, which couples to the scalar channel? (iii) Can you point to ANY substrate computation (not a tanh² toy) in which c_BLV — as opposed to c_BA or v_B2 — is evaluated as a non-constant function of τ across [0.05, 0.40]?

### SP2: Sub-question (b) — N_zeros=1 robustness and the second-zero mechanism

**Key finding (steelman): A second zero of (c²−v²) requires the flow Mach to return to 1 (v = c) somewhere past the entry. With v supersonic by a factor ~11–13 throughout the exit and dS/dτ one-signed (no deceleration mechanism), a c_s recovery would have to climb by an ORDER OF MAGNITUDE to overtake v. The corpus c_BA = 0.399 and c_BLV = 0.485 give NO such reservoir. N_zeros = 1 survives any physically-bounded c_s(τ).**

First, state the discriminator cleanly. A "second sonic horizon" is a second zero of `D(τ) = c(τ)² − v(τ)²`, i.e. a second point where the local Mach `M(τ) = v(τ)/c(τ) = 1`. W4-1 found `N_zeros = 1` at τ₀ = 0.112466 (entry), with the post-entry window **2875/2875 points supersonic (100%)**, exit-flank `max(c²−v²) = −1.94×10⁻³ < 0`, and `monotone_supersonic_exit = True` (LIVE line `5d1ac75a…`). The flow peaks at Mach 13.735 at the fold and exits at Mach 11.46 at τ=0.40 (W4-1 Results).

**The hard wall is the one-signed spectral-action gradient.** `dS/dτ = +58,673` (S73A W1-D), constant-sign on the transit window. The modulus velocity is energy-conservation-fixed: `dv/dτ = −(dS/dτ)/(M_ATDHFB·v)` (W4-2 per-surface provenance). With dS/dτ > 0 there is NO deceleration channel — v rises into the fold and the Jensen exit is monotone (S73A W1-A: Ma ∈ [20.71, 20.76] in c_BA units, "no τ at which v_τ = c_BA," "never decelerates"; AUDIT-74 `no_exit_horizon`). This is the structural reason the v-DRIVEN second-zero mechanism (v dropping back to c) is excluded: nothing decelerates v.

**Now the c_s-DRIVEN mechanism (volovik's): can c rise to v after a dip?** This is the legitimate orthogonal question W4-1 did not directly scan (W4-1 held c constant). Address it as a geometric bound, substrate-first:

- Step 1 [what a second zero needs]: a second zero past the entry requires `c(τ) = v(τ)` at some τ > τ₀. On the exit flank v ∈ [v(0.40), v_fold] with `v/c_BLV ∈ [11.46, 13.735]` → in M_KK units, `v_exit ≳ 11.46 × 0.485 = 5.56` up to `v_fold = 6.66875` (Sage QQ = 1067/160, W4-1). [Source: W4-1 Mach values + v_fold exact]
- Step 2 [what c would have to do]: for c to overtake v on the exit, c(τ) would have to RECOVER from its (hypothetical) dip and climb to `c ≳ 5.56` M_KK. But the corpus scalar/condensate speeds are c_BLV = 0.485 and c_BA = 0.399 — both `O(0.5)` M_KK. There is a **factor ~11–13 gap** between the largest corpus sound speed and the exit flow speed. A dip-and-recovery of c_s cannot manufacture a sound speed an order of magnitude above its own asymptotic value; recovery returns c toward 0.485, not toward 5.6. [Source: get_constant c_BLV=0.485, c_BA=0.399; W4-1 v_exit]
- Step 3 [the symmetric S85 crossings live in a different regime]: S85's two crossings at τ_H± = 0.18314 / 0.19686 (width 0.01372) were obtained with `c_s(τ_fold) = v_term / Mach_max = 26.545 / 13.75 = 1.9305 M_KK` (session-85-w6-workingpaper) — i.e. S85 BACK-COMPUTED a fold c_s of 1.93 by IMPOSING that the peak Mach be exactly 13.75 against a chosen v_term, then put a symmetric tanh² dip around it with v held constant. That 1.93 is not an independently-evaluated substrate sound speed; it is `v_term/Mach`. The crossings are an artifact of (a) a back-fit c_s scale and (b) constant v. W4-1 reproduced them EXACTLY (matched the S85 npz `tau_H_minus/plus` to all figures, Sage atanh(√(17/22))) precisely to certify they are a model property, not a physical broad-window feature. [Source: W4-1 modeling-robustness cross-check; S85 W6 WP]

**Does N_zeros = 1 survive a non-constant c_s(τ)?** Geometrically, the answer is YES for any c_s(τ) bounded by the corpus speeds, for one decisive reason: the entry zero exists because v RISES through c on the genesis flank (Mach 0.30 → 1 → 13.7), and there is no second zero because v never comes back down (dS/dτ one-signed) AND c never climbs to the supersonic flow level (c_BLV, c_BA ~ 0.5 ≪ v_exit ~ 5.6). A c_s dip near the fold would, if anything, push the local Mach HIGHER (smaller denominator) — deepening the supersonic interior, NOT creating a subsonic island. A c_s RECOVERY returns c to ~0.485, still a factor ~11 below v. The only way to get a second zero is to give c_s a recovery overshoot to ~5.6 M_KK — which would require a sound-speed reservoir the spectrum does not contain.

**The one genuinely open numerical question (concede honestly):** W4-1's scan held c constant, so it has not literally scanned `D(τ) = c_s(τ)² − v(τ)²` for a substrate-DERIVED c_s(τ). The geometric bound above (factor-11 gap) makes N_zeros = 1 robust to any c_s(τ) ≤ O(c_BLV), but it is a BOUND, not a scan. The clean discriminating computation is: build c_s(τ) from the actual B2/B-branch group velocity v_group(τ) = |dω/dk| off the BdG dispersion across [0.05, 0.40] (substrate-first, S71 BdG modes), and re-run the N_zeros scan on `c_s(τ)² − v(τ)²`. If c_s(τ) stays O(0.5), N_zeros = 1 is confirmed numerically; if it somehow overshoots to O(v), the question re-opens. I expect the former (group velocities of a gapped condensate do not exceed the scalar mode by 10×), but the scan is the honest closure.

**Structural implication for C1:** N_zeros = 1 is robust to c_s-softening under a physically-bounded recovery (the surviving region is "any c_s(τ) ≤ O(c_BLV)"). The second-zero mechanism volovik raises is geometrically possible ONLY if c_s recovers to an order of magnitude above its asymptotic value — a hard wall the corpus speed hierarchy forbids. C1 → ASYMMETRIC holds as a structural fact, conditional on the (expected-to-pass) c_s(τ) group-velocity scan.

**Question for volovik:** A c_s-driven second zero requires `c_s(τ) → v(τ) ≈ 5.6 M_KK` on the exit flank, where v is supersonic by Mach ~11. (i) What substrate reservoir lets the recovering condensate sound speed overshoot to ~5.6 M_KK — more than 11× its own asymptotic value c_BA = 0.399 / c_BLV = 0.485? (ii) In the impedance-locked picture (Z = ρ·v_B2 = const, S32 W4), a c_s dip is the v_B2 → 0 that DRIVES the DOS; on recovery v_B2 returns toward its band value, not toward the transit flow speed. Do you have a mechanism by which the band-edge group velocity recovers to the SUPERSONIC FLOW scale rather than to its own band-edge scale? (iii) Would you agree that the decisive closure is a single computation — c_s(τ) = |dω/dk| from the S71 BdG dispersion, fed into the N_zeros scan — and that absent a c_s overshoot to O(v), N_zeros = 1 is forced?

### SP3: Sub-question (c) — C1 → ASYMMETRIC verdict robustness

**Key finding (steelman): C1 → ASYMMETRIC STANDS as the canonical reading. At worst it is CONDITIONAL on the c_s(τ) group-velocity scan (SP2), but it does not RE-OPEN: the symmetric two-horizon reading requires a substrate mechanism (c_s overshoot to O(v) OR v deceleration) that two independent hard walls forbid. The dropped transit V.6 "two distinct horizons" STRENGTHEN clause should remain dropped; resurrecting it would require asserting a time-symmetric causal structure the dynamics cannot produce.**

Frame this in causal-structure (Penrose-diagram) terms, since that is the invariant content. The two C1 readings are two DIFFERENT global causal structures:

- **ASYMMETRIC (W4-1):** one entry sonic surface (a single 45° null line, κ_entry = +18.520 M_KK > 0, white-hole outflow) bounding a supersonic interior that opens into an unbounded expulsion region toward ℐ⁺. There is NO future-trapped exit horizon, no second null surface, and the causal disconnect is ONE-directional (S85 PROVEN causal-disconnect). This is the canonical asymmetric acoustic-white-hole diagram (W4-1 panel (d)).
- **SYMMETRIC (S85 / dropped V.6):** two sonic surfaces (τ_H± = 0.18314/0.19686), i.e. a bounded subsonic→supersonic→subsonic sandwich — the TIME-SYMMETRIC two-horizon diagram that a bounce / CCC cosmogenesis would require (W4-1 substrate-physics assessment).

The distinction is not cosmetic: a time-symmetric two-horizon structure is the signature of a bounce (contraction → minimum → re-expansion through a symmetric throat), whereas the asymmetric one-horizon structure is the signature of an IMPULSIVE one-way transit (white-hole expulsion). C1 is, geometrically, the question "is the substrate transit a bounce or a one-way expulsion?"

**Why ASYMMETRIC is forced by the dynamics (two independent hard walls):**
1. **Monotone dS/dτ wall (SP2):** dS/dτ = +58,673 one-signed → v never decelerates → the v-driven symmetric mechanism is excluded. A symmetric two-horizon structure requires v to come back DOWN through c, which requires a turning point in v, which requires dS/dτ to change sign. It does not (S73A W1-D; the off-Jensen Hessian is all-negative, S76 — the trajectory is on a maximal ridge, monotone descent). [Hard wall #1]
2. **Speed-hierarchy wall (SP1/SP2):** the c_s-driven symmetric mechanism requires c_s to recover to ~5.6 M_KK (Mach-1 against the supersonic exit flow), an order of magnitude above the corpus speeds c_BLV = 0.485 / c_BA = 0.399. The spectrum carries no such reservoir. [Hard wall #2]

A symmetric two-horizon reading needs to breach AT LEAST ONE of these. Neither is breachable by a bounded c_s(τ). Therefore C1 → ASYMMETRIC is not merely the W4-1 verdict — it is a structural consequence of the one-signed gradient plus the speed hierarchy.

**Does the substrate-correct c_s(τ) re-open C1?** No, with one honest caveat. The c_s-softening volovik raises modifies the SHAPE of c_s near the fold but cannot supply either breach: a dip DEEPENS the supersonic interior (smaller c → larger Mach → more one-signed), and a recovery returns c to O(0.5), still ≪ v. So the substrate-correct c_s(τ) leaves the global causal structure asymmetric. The caveat (per SP2): W4-1 did not literally scan a non-constant c_s(τ), so the strict status is "ASYMMETRIC, conditional on the c_s(τ) = |dω/dk| group-velocity scan returning c_s = O(0.5)." That is a CONDITIONAL on an expected-pass computation, not a RE-OPEN.

**The dropped transit V.6 "two distinct horizons" STRENGTHEN clause should stay dropped.** That clause asserted the §6.2 white hole has two distinct sonic horizons — i.e. the time-symmetric structure. Per the above, asserting it would require asserting either v-deceleration or a c_s overshoot to O(v); both are walls. Note the corpus already records the asymmetric reading as the PROVEN one (framework-parametric-amplification.md §5 "Asymmetric Fold: Entry Horizon, Open Exit"; S85 PROVEN causal-disconnect is ONE-directional). W4-2 is fully consistent: the a₄ "exit" surface (T = 7.578 M_KK) is a thermodynamic decoherence edge INSIDE the open expulsion region with a well-defined effective κ_exit = 47.615 > 0 — NOT a second sonic surface (S73a `no_exit_horizon = True`). The 3-temperature ledger holds in the asymmetric reading; it does NOT require a second sonic horizon.

**One subtlety to flag for volovik (where I will not over-claim):** "ASYMMETRIC" and "two analog temperatures" are NOT in tension. The framework has TWO entry/exit *thermodynamic* surfaces (a₂-kinematic T=72.8, a₄-condensation T=7.578) but only ONE *sonic* surface (the entry). The dropped V.6 clause conflated "two thermodynamic surfaces" with "two sonic horizons." The asymmetric reading keeps both thermodynamic surfaces (W4-2 ledger) while having one sonic horizon (W4-1). This is the substrate-correct picture and it is robust to c_s(τ): the a₄ surface's κ is a condensation-gradient quantity (a_4^{ζ}), not a Mach-1 crossing, so it is unaffected by whether c_s dips.

**Structural implication for C1:** STANDS (canonical) → at most CONDITIONAL on the SP2 group-velocity scan → does NOT re-open. The surviving solution space is the asymmetric one-horizon causal structure plus the two-thermodynamic-surface ledger; the time-symmetric two-horizon (bounce-signature) reading is closed for the canonical trajectory by two independent hard walls.

**Question for volovik:** The symmetric two-horizon reading is the bounce/CCC-signature causal structure. (i) Do you read the substrate transit as a BOUNCE (symmetric throat, v down-through-c on the far side) or a one-way EXPULSION? If a bounce, what is the substrate mechanism for v to decelerate back through c, given dS/dτ = +58,673 one-signed and the all-negative off-Jensen Hessian (S76)? (ii) If you accept the expulsion picture but still want a second SONIC surface from c_s recovery, this is hard wall #2 (c_s ↛ O(v)) — can you breach it? (iii) Do you agree the cleanest joint resolution is: keep TWO thermodynamic surfaces (W4-2 ledger, robust to c_s) but ONE sonic surface (W4-1, robust to bounded c_s) — i.e. the dropped V.6 clause was a thermodynamic-vs-sonic conflation, not a live symmetric-horizon claim?

### SP4: Cross-Cutting Observations

**CC1 — The whole adjudication reduces to ONE substrate computation, and I name it.** The three sub-questions collapse to a single discriminating test: **compute c_s(τ) = |dω/dk| from the S71 BdG dispersion (group velocity of the relevant phonon branch) across τ ∈ [0.05, 0.40], then re-run the W4-1 N_zeros scan on the substrate-correct discriminant `D(τ) = c_s(τ)² − v(τ)²`.** Pre-registered outcomes: (PASS-ASYMMETRIC) if `c_s(τ) = O(0.5)` everywhere → N_zeros = 1, C1 → ASYMMETRIC confirmed unconditionally; (RE-OPEN) if c_s recovers to `O(v) ≈ 5.6 M_KK` on the exit flank → second zero possible, C1 conditional. My structural prediction is PASS-ASYMMETRIC (the speed hierarchy c_BLV/c_BA ~ 0.5 forbids an order-of-magnitude overshoot), but I concede this is the honest closure W4-1 left open by holding c constant. This is the discriminating gate the workshop should converge on — it is well-defined, substrate-first, and decides C1 cleanly.

**CC2 — Branch-assignment is the real disagreement, not constant-vs-variable.** The framing "is c_BLV constant or does it soften?" hides the actual structural question: WHICH of the four phonon branches carries the §6.2 transit-flow acoustic metric? The corpus is unambiguous that the modulus transit propagates in the SCALAR channel (c_BLV; Mach = v/c_BLV = 13.75, baseline-findings-s66), and the softening is a property of the B2 condensate/band-edge channel (v_B2 → 0, Z_B2 = 0; S40/S52). volovik and I likely AGREE that c_B2 softens; the live question is whether C1's discriminant should be re-built on c_B2. My position: no — the flow v(τ) is the modulus's own propagation velocity (dτ/dt), which is a scalar-channel quantity, so the matched acoustic metric uses the scalar sound speed c_BLV. Re-building C1 on the band-edge mode would be a category error (matching a flow in one channel against a sound speed in another). This is the sharpest point of contact for Round 2.

**CC3 — The impedance theorem is a substrate-level guard against runaway softening.** Z = ρ·v = const at the 1D van Hove fold (Landau, S32 W4; Z_wall = 1/π; canonical Z = 1.016) is the framework's OWN statement that the fold is a controlled cusp (A₂ catastrophe, structurally stable — atlas-04), not an unbounded criticality. The DOS divergence and the velocity vanishing are LOCKED together (ρ ~ 1/v), so the softening cannot be a free parameter dialed to engineer a second horizon — it is fixed by the cusp geometry. This is independent of the C1 scan and bears on whether ANY c_s dip can be "tuned." It cannot; it is determined.

**CC4 — Geometric self-consistency: the asymmetric reading is what the 12D censorship picture requires.** W4-5 (PASS, audit `9ffb4aea…`) established the genuine singularity is at τ→∞ (anisotropic, censored), with the fold (τ=0.190) a first-order phase transition / extremal (κ=0) horizon — NOT a singularity, NOT a symmetric throat. W4-4 (INFO) found the fold maps to a finite, regular 4D conformal point (Ω_fold = 2.241353, a_eff'(fold) = +6.82×10⁻² > 0 — monotone, not a turning point). A SYMMETRIC two-horizon (bounce) reading of C1 would be in tension with both: a bounce throat is a turning point in the scale factor, but a_eff is monotone through the fold (W4-4), and the censored singularity sits at τ→∞ (one-way), not at a symmetric minimum. The asymmetric C1 reading is the one consistent with the rest of the W4 wave's causal geometry. The three SP-owned gates (W4-1, W4-4, W4-5) tell ONE coherent story: impulsive one-way transit, monotone exit, censored far-future singularity — no bounce, no symmetric throat, one sonic horizon.

**CC5 — What I am NOT claiming (boundary of the steelman).** I am not claiming c_s is constant in EVERY channel — the B2 condensate mode demonstrably softens (that is the DOS divergence). I am not claiming W4-1 literally scanned a substrate-derived c_s(τ) — it held c constant, and the strict robustness statement is a geometric bound (factor-11 gap), not a scan. I am not claiming the a₄ "exit temperature" surface does not exist — it does (W4-2), as a thermodynamic edge, just not as a second SONIC horizon. The steelman is precise: the SCALAR transit channel c_BLV is constant through the fold, that is the correct channel for C1's discriminant, and N_zeros = 1 is robust to any c_s(τ) bounded by the corpus speed hierarchy — closing cleanly via the CC1 group-velocity scan.

---

## Round 1 — volovik: Response & Cross-Synthesis (c_s-softening / superfluid-criticality steelman)

### Part 1: Response to sp's Sections

#### Re: SP1 — Sub-question (a), constant c_BLV through the fold

**AGREE (the branch separation is real and substrate-grounded — I concede the core claim).** sp's central move is correct on the substrate physics: the four-speed hierarchy (SOUND-SPEED-64) is four DISTINCT phonon branches of the same D_K spectrum, and the mode that goes critical at the van Hove fold is the B2 condensate/band-edge branch, NOT the scalar transit mode c_BLV. I confirm this from the Volovik corpus directly: in 3He (Volovik, *Universe in a Helium Droplet* §§3.2–3.4) the order-parameter (BCS amplitude) collapse and the Bogoliubov-Anderson density mode are **different excitation branches** of the same BdG Hamiltonian — the gap-amplitude/clapping modes soften near a transition while the propagating first-sound/density mode does not run to zero. The framework's c_BLV (scalar fabric mode) ↔ c_BA/c_B2 (condensate band-edge) distinction is the exact inheritance of that 3He branch separation (parent→child morphism, NOT analogy — `3HeB-inheritance-canonical.md`). So "the softening is c_B2, not c_BLV" is substrate-correct, and the naive premise of the challenge ("c_BLV = 0.485 itself may run to zero at the DOS singularity") is, as stated, mis-assigned. I do not defend that naive premise.

**DISAGREE (sp's IMPEDANCE-LOCK reason is built on an idealization the substrate-canonical value contradicts — this is the wedge).** sp argues (SP1 final paragraph + CC3) that the softening is "self-limiting" because `Z_{B2} = ρ·v_{B2} = 0` at the fold (S40/Tesla addendum) — i.e. v_B2 vanishes EXACTLY and the impedance is fixed by ρv = const. But the substrate-canonical value is NOT zero. The S94 rho-pinned, substrate-natural constant is

- `v_g_B2_fold = 0.022699323` M_KK (S94, `S94-DS-GAMMA-E-RESOLUTION`, NOT superseded) `= 1/(π·ρ_B2_per_mode) = 1/(π·14.023250)`.

Sage-exact: `1/(π·14.023250234055) = 0.0226987…`, and the impedance is `Z = ρ·v_g = 14.023250 × 0.0226987 = 0.31831 = 1/π` EXACTLY (Sage-confirmed; this IS Landau's Z_wall = 1/π). So the impedance lock is real, but it does NOT pin v_B2 to zero — it pins v_B2 to the **finite** value `1/(πρ)`. The "v_B2 = 0" in the S40 Tesla addendum is the idealized 1D van Hove form taken literally at the cusp; the substrate's own canonical (S94, rho-pinned) regularizes it to 0.0227. This matters because sp's CC3 claim — "the softening cannot be a free parameter dialed to engineer a second horizon — it is determined" — is TRUE, but for a different reason than sp gives: it is determined to be `1/(πρ) = 0.0227`, a finite floor, not zero. sp's "self-limiting at zero" framing is the weak version; the substrate-strong version is "self-limiting at a finite, rho-pinned floor of 0.0227 M_KK."

**MISSED (the superfluid-criticality view reveals: c_BLV's τ-flatness is ASSERTED, not computed — and the BCS pairing that DEFINES the transit lives at the band edge).** sp says (SP1 Step 2) c_BLV is "a fixed a_n-moment functional (S64), an integrated quantity, not a band-edge group velocity that can be driven to zero." The first half is the corpus convention; the second half is sp's inference, and the corpus does not actually contain a τ-scan of c_BLV. `get_constant(c_BLV)` returns a single fold-region value 0.485 with provenance "scalar c_s, 3He-B four-speed hierarchy inheritance" — there is no `c_BLV(τ)` trajectory on record (the corpus c_BLV is reproduced bit-stable across S63/S64/S67/S82, but always AT or NEAR the fold, never scanned across [0.05, 0.40]). So "c_BLV is constant through the fold" is an extrapolation from a fold-anchored constant, not a measured flat trajectory. The superfluid-criticality concern is sharper than "does c_BLV soften": it is **whether a global a_n-moment functional can stay smooth when one of the modes it integrates over (B2) goes dispersionless and its DOS diverges as 1/(πv)**. A moment `∫ dλ ρ(λ) f(λ)` over a spectrum whose DOS develops a 1/(πv) van Hove spike is NOT guaranteed smooth in the deformation parameter τ — the spike is exactly where a_n moments pick up their largest τ-derivatives (this is the same DOS spike that drives dS/dτ = +58,673 to its enormous value at the fold; S52 "Channel 1: τ→DOS→BCS coefficients"). The honest statement is: c_BLV being a global functional makes it MORE robust to a single mode softening than the band-edge mode itself, but it does not make it τ-flat by fiat. Whether c_BLV(τ) has a local dip riding the DOS spike is a computation (see VOL1), not a settled fact.

**EMERGES (cross-domain): the right substrate question is not "constant vs variable c_BLV" but "is the scalar-channel discriminant the correct acoustic metric for a transit DEFINED by band-edge pairing?").** sp frames this in CC2 and answers "yes, the flow v = dτ/dt is a scalar-channel quantity, so match it against c_BLV." That is a defensible matched-impedance argument and I take it seriously in Re:SP4. But there is a genuine cross-domain tension the superfluid picture surfaces: in 3He the analog white-hole / sonic-horizon physics (Volovik §32, Jacobson-Volovik) is built on the mode that carries the relevant *quasiparticle* — and the quasiparticles produced at the fold (the GGE relic, the squeeze P(ω) in W4-3) are BdG excitations of the CONDENSATE, i.e. they live in the c_B2/c_BA channel, not the scalar channel. The flow propagates in the scalar channel (sp is right about v), but the *horizon that matters for what radiates* is the surface where the flow goes sonic relative to the speed of the **emitted quasiparticles**. This is the real open question, and it is structural, not numerical — I develop it in VOL1. Verdict on (a): the naive "c_BLV→0" premise is mis-assigned (sp right); the branch separation is substrate-correct (sp right); but c_BLV's τ-flatness is an extrapolation not a scan, the impedance floor is finite-0.0227 not zero (sp's reason is the weak version), and which channel carries the radiating horizon is a live structural question.

#### Re: SP2 — Sub-question (b), N_zeros=1 robustness

**AGREE (the factor-gap wall survives — and in the soft channel it is STRONGER, not weaker; this is the most important thing I can contribute to (b)).** I steelman'd the opposite of my brief here and the substrate would not let me. sp's factor-11 wall (SP2 Steps 1–2): a far-side second zero needs `c_s(far) → v_exit ≈ 5.56 M_KK`, and the corpus speeds are O(0.5), so a recovery would need an order-of-magnitude overshoot the spectrum cannot supply. I tested whether redirecting the discriminant onto the SOFT channel (the legitimate worry from Re:SP1 — the BCS-pairing channel) escapes this wall. It does the opposite. Sage-exact (QQ where rational):

- Flow: `v_fold = Mach_max·c_BLV = (55/4)·(485/1000) = 1067/160 = 6.66875` M_KK; `v_exit = 11.46·c_BLV = 5.5581` M_KK.
- Scalar-channel gap (sp's wall): `v_exit/c_BLV = 11.46`; to get a 2nd zero, `c_BLV` must climb 11.46× → 5.56.
- **B2-channel gap (the soft channel)**: `v_fold/v_g_B2_fold = 6.66875/0.0226987 = 293.79`. The fold Mach in the B2 channel is ~294, not 13.75. To get a far-side second zero in the B2 channel, `c_B2` must climb to `v_exit = 5.56`, an overshoot of `5.56/0.0227 = 244.9×` over its rho-pinned fold floor, or `5.56/0.399 = 13.93×` over its band asymptote c_BA.

So the wall is **channel-monotone in the wrong direction for the symmetric reading**: the softer the channel you build the discriminant on, the LARGER the overshoot a second zero requires, because the flow is the same but the sound speed is smaller. There is no channel in the four-speed hierarchy where building the discriminant produces a smaller gap than the scalar channel — c_BLV = 0.485 is the LARGEST of the four (c_mod=1 is the emergent photon, not a transit-flow reference). I therefore AGREE with sp's conclusion N_zeros = 1, and I strengthen it: **N_zeros = 1 is robust not just for c_s ≤ O(c_BLV), but across the ENTIRE branch hierarchy**, because every alternative channel only deepens the supersonic interior. A c_s dip (smaller denominator) raises the local Mach; a recovery returns c to its band value (O(0.5) at most), never to O(v). Both directions forbid a second crossing.

**DISAGREE (sp's "no deceleration channel" hard-wall #1 is correct as stated, but the corpus has a SUBTLETY about the exit Mach that should be flagged before it is treated as a perfect wall).** sp invokes S73A W1-A "Ma ∈ [20.71, 20.76] in c_BA units, never decelerates" as hard-wall #1 (monotone dS/dτ). I confirm the dS/dτ = +58,673 one-signed gradient (S73A W1-D) and the all-negative off-Jensen Hessian (S76) — the trajectory is on a maximal ridge, monotone descent, so v does not turn around. But note an internal inconsistency the workshop should not paper over: S73A reports the exit Mach as ~20.7 in **c_BA units**, while W4-1 reports the fold peak Mach as 13.735 in **c_BLV units**, and W4-2's substrate Mach is 13.75 = v_fold/c_BLV. These are the SAME flow measured against TWO different sound speeds (Mach_BA = v/c_BA = v/0.399; Mach_BLV = v/c_BLV = v/0.485; ratio 0.485/0.399 = 1.216, so Mach_BA should be ~1.22× Mach_BLV — but 20.7/13.75 = 1.51, not 1.22). The discrepancy means S73A and W4-1 are not using the same v(τ) normalization, OR the "c_BA units" label hides a different flow definition. This does NOT threaten N_zeros = 1 (both readings are deeply supersonic and monotone), but it IS the same channel-bookkeeping ambiguity that CC2 identifies — and it shows the corpus already carries TWO Mach numbers for the transit, computed against TWO sound speeds, without a settled statement of which is the "horizon" Mach. That is the live question, not whether v decelerates.

**MISSED (the superfluid-criticality view reveals: a DOS-riding c_s dip can create a near-second-horizon GRAZE that W4-1's INFO band would catch — and W4-1 already grazes).** sp treats the second-zero question as binary (zero or no zero). The superfluid-criticality refinement: if c_BLV(τ) DOES inherit a local dip riding the DOS spike at the fold (the VOL1 mechanism), the discriminant `D(τ) = c_BLV(τ)² − v(τ)²` does not need a full second zero to change the causal structure — it needs only to GRAZE back toward zero on the exit flank to create a near-extremal second surface. W4-1's own LIVE numbers show the exit flank is already close to a graze: `disc_far_max = −1.94×10⁻³` and `graze_min_abs = 1.94×10⁻³`, sitting RIGHT AT the `GRAZE_INFO_CEIL = 1×10⁻³` boundary (W4-1 marked it "NOT a near-second-horizon" because `1.94×10⁻³ > 1×10⁻³` — but it is within a factor of 2 of the INFO downgrade). With c held CONSTANT this graze is harmless (the interior is −44 deep). But the graze metric is computed on the exit FLANK, where v has dropped to Mach 11.46 — and if c_BLV(τ) has even a few-percent local structure there, `c_BLV(τ)²` shifts the graze. The point is not that this creates a second zero (it almost certainly does not, per the factor-294 wall above) — it is that **the W4-1 scan held c constant and therefore cannot see whether a substrate-derived c_s(τ) moves the exit-flank graze across the INFO ceiling**. That is the genuine residual sp concedes at SP2's end, and it is a real one: the verdict is N_zeros = 1 with a possible INFO-graze sensitivity that only the c_s(τ) scan resolves.

**EMERGES (cross-domain): the decisive computation sp names (CC1) is correct, but the relevant group velocity is the QUASIPARTICLE branch's, and it must be scanned across τ, not evaluated at the fold).** sp's CC1 closure — compute `c_s(τ) = |dω/dk|` from the S71 BdG dispersion and re-run the N_zeros scan — is exactly right and I endorse it as the workshop's convergence target. One substrate-correction to the spec: the canonical S94 `v_g_B2_fold = 0.0227` is a SINGLE-POINT (fold) value, `1/(πρ_B2)`. The scan needs `v_g(τ)` across [0.05, 0.40], because the whole question is whether the band-edge group velocity RECOVERS on the exit flank (sp's hard-wall #2). The S94 machinery (`s94_ds_gamma_e_resolution_vg_b2_trajectory.npz` — note "trajectory" in the filename) may already contain the τ-trajectory; if so, the scan is nearly free. Verdict on (b): N_zeros = 1 holds and is STRONGER than sp claimed (channel-monotone across the whole hierarchy); the one honest residual is the exit-flank INFO-graze sensitivity to c_s(τ), resolved by the CC1 scan extended to the full τ-window using the quasiparticle branch group velocity.

#### Re: SP3 — Sub-question (c), C1 verdict robustness

**AGREE (the substrate transit is a one-way EXPULSION, not a bounce — the superfluid picture is unambiguous here, and it is the strongest argument in the wave).** sp's SP3 reframes C1 as "bounce vs one-way expulsion" and I agree completely, with corpus backing sp did not cite. The transit is NOT a bounce, for a reason deeper than the dynamics walls: the substrate transit is a **first-order phase transition through the van Hove fold** (W4-5 substrate-physics assessment: "τ=0.190 is a first-order phase transition, not a singularity"), driven by an INSTANTON GAS (the S37 paradigm, `THE ORDERED VEIL`), not a quasi-static throat. A bounce is a time-symmetric throat in a (semi-)reversible evolution — contraction to a minimum, re-expansion through the same configuration. The substrate transit is irreversible by construction: it is a quench (Volovik Kibble-Zurek mechanism, *Universe in a Helium Droplet* §§27–28), the GGE relic NEVER thermalizes (it is integrable, not chaotic — `ORDERED-VEIL-SUBSTRATE-CLOCK` FAIL this very session confirms the diabatic freeze-out: both substrate-clock ratios ≫ 1, "diabatic freeze-out, NOT integrability-limited"), and the produced squeeze is a Bogoliubov PRODUCT state with `Tr ρ² = 1, S_ent = 0` (this session's `HAWKING-GGE-PURITY` INFO). A pure, never-thermalized, one-way quench through a first-order transition has NO time-symmetric throat. The acoustic white hole is the laboratory analog OF this irreversible quench, not the other way around (substrate IS, laboratory IN). So C1 → ASYMMETRIC is not just "what the discriminant scan returns" — it is forced by the irreversibility of the transit, which is a substrate fact independent of the (c²−v²) bookkeeping. I rate this stronger than sp did: the bounce reading is excluded by the SECOND LAW direction of the quench, before any speed-hierarchy argument.

**DISAGREE (sp's "CONDITIONAL on an expected-pass scan" is the right STATUS, but the framing understates one genuine way C1 could be RE-STATED — not re-opened, but refined to specify WHICH horizon).** sp's status ladder is "STANDS → at most CONDITIONAL → does not re-open." I agree it does not re-open as symmetric/asymmetric. But I disagree that the only residual is the c_s(τ) magnitude scan. There is a second, orthogonal way the verdict needs refinement, surfaced by Re:SP1/Re:SP4: the §6.2 discriminant `ds² = −(c_BLV² − v²)dt²` defines the horizon as "where the flow goes sonic relative to the SCALAR mode." If the radiating horizon (the surface that sets what quasiparticles escape) is instead "where the flow goes sonic relative to the EMITTED quasiparticle branch" (c_B2/c_BA), then there are potentially TWO sonic surfaces of two DIFFERENT KINDS — a scalar-channel sonic surface (the entry, τ₀=0.1125, W4-1) AND a quasiparticle-channel sonic surface (where v = c_B2, which in the B2 channel at Mach ~294 is FAR upstream, possibly before genesis). This is NOT the symmetric two-horizon (bounce) reading sp closed — both surfaces would still be on the ENTRY side, both white-hole, no future-trapped exit. But it means "ONE sonic horizon" is channel-specific: one sonic horizon PER CHANNEL, and the framework has (at least) two relevant channels. C1 → ASYMMETRIC (no exit horizon, no bounce) STANDS; but "exactly one sonic horizon, full stop" is a scalar-channel statement that should be RE-STATED as "one scalar-channel entry horizon; the quasiparticle-channel horizon is a separate surface to be located." This is a refinement, not a re-open — I want to be precise that I am NOT resurrecting the symmetric reading.

**MISSED (the superfluid-criticality view reveals: the two THERMODYNAMIC surfaces sp keeps (W4-2) are ALREADY the two-channel structure in disguise).** sp's subtlety (SP3, "two thermodynamic surfaces ≠ two sonic horizons") is correct and important — the dropped V.6 clause conflated them. But the superfluid picture reveals WHY there are exactly two thermodynamic surfaces, and it is the channel structure: W4-2's entry surface is the a₂-KINEMATIC gradient (κ_entry from `a_2_FW_zeta`, the transit-velocity/scalar gradient) and the exit surface is the a₄-CONDENSATION gradient (κ_exit from `a_4_FW_zeta`, the BCS-condensation/decoherence gradient). a₂ = scalar/Einstein-Hilbert channel; a₄ = condensation/Yang-Mills channel. The two thermodynamic surfaces are the two channels' surface gravities. So sp is right that they are not two SONIC horizons of the SAME flow against the SAME speed — but they ARE two surfaces in two different acoustic channels, which is exactly the two-channel structure I raise in the DISAGREE above, seen from the thermodynamic (κ) side rather than the kinematic (Mach) side. This is a convergence point, not a conflict: sp's "keep two thermodynamic surfaces, one sonic horizon" and my "one sonic horizon PER CHANNEL" are the SAME structure described in two languages. The a₄ surface's κ being a condensation-gradient quantity (sp: "not a Mach-1 crossing, unaffected by whether c_s dips") is precisely my point that it lives in the condensate channel.

**EMERGES (cross-domain): C1's resolution is robust because it rests on irreversibility, but the framework should adopt the channel-tagged statement to avoid future conflation).** The durable verdict: C1 → ASYMMETRIC STANDS (one-way expulsion, no bounce, no future-trapped exit horizon) — and it is robust at THREE independent levels: (1) the speed-hierarchy wall (channel-monotone, Re:SP2); (2) the monotone-dS/dτ wall (sp's hard-wall #1); (3) the irreversibility of the first-order quench (the GGE never thermalizes, the squeeze is pure — the substrate-strongest argument, this Re:SP3). The bounce reading would have to breach all three. The ONE refinement: "one sonic horizon" should be stated channel-tagged ("one scalar-channel entry horizon"), because the framework demonstrably carries two acoustic channels (scalar c_BLV and condensate c_B2/c_BA) and two thermodynamic surfaces (a₂/a₄) that are the two channels' κ. The dropped V.6 "two distinct horizons" clause should stay dropped IF read as "two sonic horizons of one flow against one speed" (the bounce reading — correctly closed); but a channel-tagged "two acoustic channels, each with its own entry surface" is substrate-correct and is what W4-2's ledger already encodes. Verdict on (c): STANDS (not re-opened); refine to channel-tagged form; the bounce reading is closed by irreversibility + two speed/gradient walls.

#### Re: SP4 — Cross-cutting

**Re CC1 (the one decisive computation) — AGREE, with the substrate-correction already stated.** I endorse the CC1 group-velocity scan as the workshop's convergence target, refined per Re:SP2: scan `v_g(τ) = |dω/dk|` of the QUASIPARTICLE (B2/B) branch across the FULL [0.05, 0.40] window (not the single fold point), using the S94 trajectory if it already exists (`s94_ds_gamma_e_resolution_vg_b2_trajectory.npz`). Pre-registered outcomes as sp states them, plus one I add: (PASS-ASYMMETRIC) c_s(τ) = O(0.5) → N_zeros = 1 unconditional; (RE-OPEN) c_s overshoots to O(v) → second zero possible [I assign this near-zero prior: the factor-294 wall in the soft channel]; (INFO-GRAZE) c_s(τ) has a few-percent exit-flank structure that moves `disc_far_max` across the `GRAZE_INFO_CEIL = 1e-3` (W4-1 already sits at 1.94e-3, factor-2 from the ceiling) → N_zeros = 1 but with an INFO annotation on near-extremality. My structural prediction matches sp's (PASS-ASYMMETRIC), and I add that the scan should report the exit-flank graze metric explicitly, because that is the only quantity the constant-c W4-1 scan could not see.

**Re CC2 (branch-assignment is the real disagreement) — this is the sharpest point of contact, and I PARTIALLY DISAGREE with sp's resolution.** sp's CC2: the modulus flow v = dτ/dt is a scalar-channel quantity, so the matched acoustic metric uses c_BLV, and re-building C1 on the band-edge mode "would be a category error (matching a flow in one channel against a sound speed in another)." I agree the FLOW propagates in the scalar channel — v = dτ/dt is the modulus's own deformation rate, and that couples to c_BLV. But sp's category-error charge cuts BOTH ways, and here is the substrate subtlety: an acoustic horizon is not defined by "the flow's own channel" — it is defined by the surface where the flow speed equals the speed of the **mode being Doppler-trapped**. In Volovik's 3He analog-gravity construction (*Universe in a Helium Droplet* §32; Jacobson-Volovik 1998), the sonic horizon for a given quasiparticle species is where the superflow velocity equals THAT species' group velocity — and DIFFERENT quasiparticles (different branches) see DIFFERENT horizons in the same flow. A flow that is subsonic for the fast mode (c_BLV) can be supersonic for the slow mode (c_B2). So matching v against c_B2 is NOT a category error — it is the horizon FOR B2 quasiparticles, which is a physically distinct (and, at Mach ~294, far more deeply trapped) surface than the c_BLV horizon. The category error would be claiming there is ONE horizon; the substrate-correct statement is one horizon PER quasiparticle branch. NOW — does this re-open C1? No (per Re:SP3): both horizons are entry-side, white-hole, no exit. But it means sp's "the discriminant uses c_BLV because the flow is scalar" is too quick: the discriminant uses c_BLV because we are asking about the SCALAR mode's horizon; if we ask about the squeeze that actually radiates (B2/B3 BdG modes, W4-3's P(ω)), the relevant horizon is the c_B2 surface. The §6.2 white hole is well-defined per channel; "THE" horizon is channel-ambiguous, and that ambiguity is the live content, not whether c_BLV is constant.

**Re CC3 (impedance theorem as guard against runaway softening) — AGREE on the conclusion, DISAGREE on the zero.** sp: Z = ρ·v = const (Z_wall = 1/π ≈ 0.318; canonical Z = 1.016 at the WALL) means the softening is "determined, not tunable." I confirm this is the correct substrate guard AND I sharpen it: the impedance lock pins v_B2 to the FINITE value `1/(πρ_B2) = 0.0226987` M_KK (S94 canonical, Sage-exact `Z = 14.023250 × 0.0226987 = 0.31831 = 1/π`), NOT to zero. sp's CC1/SP1 lean on "v_B2 = 0" (the S40 idealization); the substrate-canonical value is 0.0227. The conclusion is the same (the softening floor is determined, not a free dip) but the substrate-correct floor is finite — which matters for the CC1 scan: c_B2(fold) = 0.0227 is the rho-pinned MINIMUM, and the scan's question is whether c_B2(τ) recovers from 0.0227 toward c_BA = 0.399 on the exit flank (band recovery) or stays pinned near the floor (DOS-locked). Either way it does not reach O(v); but "the impedance is locked at 1/π with finite v_B2 = 0.0227" is the precise statement, and the "Z_B2 = 0" form in the Tesla addendum should be read as the unregularized cusp limit, superseded by S94. (Note: the atlas-07 "canonical Z = 1.016" and Landau's "Z_wall = 1/π = 0.318" are TWO different normalizations of the same WALL — sp cites both as if interchangeable; they differ by a factor ~3.19. The workshop should not lean on Z's absolute value without pinning which normalization, but the QUALITATIVE conclusion — Z finite, softening determined — holds in both.)

**Re CC4 (geometric self-consistency, 12D censorship) — AGREE, and it is the cleanest cross-check in the wave.** sp's CC4: W4-5 (censored anisotropic singularity at τ→∞, NOT a symmetric throat) and W4-4 (a_eff monotone through the fold, a_eff'(fold) = +6.82e-2 > 0, not a turning point) are both inconsistent with a bounce. I confirm this is decisive and aligns with the irreversibility argument in Re:SP3: a bounce needs a turning point in the scale factor (ȧ = 0 at the throat), but a_eff is monotone (W4-4) AND the genuine singularity is one-way at τ→∞ (W4-5), AND the transit is an irreversible quench (the GGE never thermalizes). Three SP-owned gates (W4-1, W4-4, W4-5) plus the GGE-purity/ordered-veil results tell ONE coherent story: impulsive one-way transit, monotone exit, censored far-future singularity. The asymmetric C1 reading is over-determined by the rest of the W4 geometry. I have no dissent here.

**Re CC5 (boundary of the steelman) — I match sp's honesty and add my own boundary.** sp does not claim c_s is constant in every channel (the B2 mode softens — agreed), does not claim W4-1 scanned a substrate-c_s(τ) (it held c constant — agreed), does not claim the a₄ surface is absent (it exists thermodynamically — agreed). My own boundary, symmetrically: I do NOT claim c_BLV softens to zero (the naive premise is mis-assigned — Re:SP1); I do NOT claim a second sonic horizon exists in the scalar channel (the factor-11/294 wall forbids it — Re:SP2); I do NOT claim C1 re-opens as symmetric (irreversibility + three walls close the bounce — Re:SP3). What I DO claim, and what survives as the residual disagreement: (i) c_BLV(τ)'s flatness is an extrapolation from a fold-anchored constant, not a scan, and the DOS spike is exactly where a_n moments pick up large τ-derivatives — so a local c_BLV dip riding the DOS is a computation, not settled; (ii) the impedance floor is finite-0.0227, not zero, sharpening (not refuting) sp's self-limiting argument; (iii) "one sonic horizon" is channel-specific — one per quasiparticle branch — and which channel carries the RADIATING horizon (for the W4-3 squeeze) is the live structural question, distinct from the bounce/no-bounce question sp closed.

### Part 2: Original Analysis

#### VOL1: The c_s-softening steelman — criticality at the van Hove fold

This is the strongest substrate case I can build for the c_s-softening side. It does NOT recover a symmetric two-horizon C1 (the walls in Re:SP2/SP3 are real and I do not breach them). Instead it establishes the precise, substrate-grounded sense in which the §6.2 discriminant is INCOMPLETE — and names the computation that would close the gap honestly.

**(1) The substrate fact: the van Hove fold IS a quantum critical point for the condensate channel.** In superfluid 3He (Volovik, *Universe in a Helium Droplet* §§6.1–6.4, and the flat-band/Khodel-Shaginyan physics the framework inherits — Papers 16/17/18 in my corpus, mapped to "Khodel-Shaginyan condensation" in my memory), a van Hove singularity in the density of states is the engine of BCS pairing: `ρ(τ) ~ 1/(π v_band(τ))` diverges because the band-edge group velocity vanishes, and the divergent DOS is what makes the pairing instability turn on (the gap equation `1 = g·∫dλ ρ(λ)/...` is dominated by the DOS spike). The framework's own ledger states this directly: `ρ_B2(fold) = 14.023`, "Van Hove enhanced" (S52, S37), and "Channel 1: τ→DOS→BCS coefficients" (S52). So the fold is not a generic point on the trajectory — it is the critical point where the order parameter is born. At a critical point, SOME susceptibility diverges. The question for C1 is WHICH one, and whether the scalar transit mode is insulated from it.

**(2) The compressibility/sound-speed link Volovik makes — and where it bites.** The substrate sound speed of a given channel is `c² = ∂P/∂ρ = 1/(ρ_mass·κ_compressibility)` (Volovik Paper 05 q-theory; the same Gibbs-Duhem structure that gives `EQUILIBRIUM-CC-WARRANT` this session). A DIVERGING compressibility (the hallmark of a second-order critical point) drives `c → 0`. sp's branch separation says: the diverging compressibility is the CONDENSATE compressibility (B2 channel), so `c_B2 → 0` (regularized to 0.0227 by the impedance lock), while the SCALAR compressibility stays finite, so `c_BLV` stays 0.485. I accept this is the substrate-correct LEADING-ORDER picture. The steelman is at SUBLEADING order: the scalar mode and the condensate mode are not decoupled — they are different branches of the SAME BdG Hamiltonian, coupled through the order parameter. When the condensate compressibility diverges at the fold, the scalar mode's self-energy picks up a correction through the coupling (in 3He this is the standard mode-mode coupling between density sound and the gap-amplitude/clapping modes; Volovik §§3.4, 7). The magnitude of c_BLV's correction is `δc_BLV/c_BLV ~ (coupling)·(condensate susceptibility)` — which is LARGE exactly where the condensate susceptibility diverges, i.e. at the fold. So a global a_n-moment functional being "robust" to a single mode softening is true to leading order but does NOT mean its τ-derivative is small at the critical point — the moment integral `∫dλ ρ(λ) f(λ)` has its largest τ-sensitivity where ρ(λ) develops the 1/(πv) spike. This is the same DOS spike that drives dS/dτ = +58,673 to its ENORMOUS fold value (S73A W1-D): the spectral action's first moment is hyper-sensitive to τ at the fold, and c_BLV is built from the same moments. The steelman claim: **c_BLV(τ) plausibly has a LOCAL DIP at the fold riding the DOS spike, even though its asymptotic value is 0.485** — "constant 0.485" may be an average/asymptote that hides a local critical-point structure.

**(3) Why this does NOT recover symmetric C1 (honest boundary).** Even granting a local c_BLV dip at the fold, it does not create a far-side second zero, for the reason in Re:SP2: a dip DEEPENS the supersonic interior (smaller c → larger Mach), and the recovery returns c toward 0.485, a factor 11.46 below v_exit. The factor-294 wall in the soft channel is even more forbidding. A local dip moves the ENTRY zero slightly (τ₀ shifts) and could move the exit-flank GRAZE (W4-1's `disc_far_max = −1.94e-3`, factor-2 from the INFO ceiling), but it cannot manufacture a subsonic island on the far side. So the steelman's payload is NOT "C1 is symmetric." It is: **the W4-1 discriminant, computed with constant c, has an unquantified error bar at the fold and on the exit flank that only a c_BLV(τ) scan can bound** — and given that the fold is a critical point where the relevant susceptibility diverges, "the error bar is negligible" is an assumption, not a result.

**(4) The deeper structural payload: the RADIATING horizon is in the condensate channel, and W4-1 did not test it.** This is the part of the steelman that survives most strongly. The §6.2 white hole exists to explain what the transit RADIATES — the GGE relic, the squeeze P(ω) that becomes the CMB acoustic signature (W4-3; `n_s` from the post-transit GGE). Those quanta are BdG excitations of the CONDENSATE (the B2/B3 modes in W4-3's table: B2[0..3], B3[0..2] over ω ∈ [0.82, 1.06] M_KK). In Volovik's analog-gravity framework the Hawking/horizon emission of a given quasiparticle species is governed by the surface where the flow goes sonic relative to THAT species (Jacobson-Volovik 1998; Volovik §32). The emitted quasiparticles are condensate-channel modes, so the horizon that sets their emission is the c_B2 surface, not the c_BLV surface. W4-1 built the discriminant on c_BLV (the scalar mode, which does NOT radiate the relic) and correctly found the scalar-channel causal structure. But the scalar-channel horizon is the horizon for the WRONG species. **The radiating horizon — where v = c_B2 — is a different surface, and at Mach ~294 in the B2 channel it is deeply supersonic essentially everywhere on the transit, meaning the entire post-genesis trajectory is INSIDE the condensate-channel horizon.** This is actually CONSISTENT with and STRENGTHENS the asymmetric reading (the open expulsion region is, for condensate quanta, an unbounded supersonic interior — exactly the white-hole picture), but it means the W4-2 entry temperature (T = 72.8, from the a₂ scalar/kinematic gradient) is the SCALAR-channel temperature, while the temperature that governs the radiated relic should be read from the CONDENSATE-channel (a₄) surface (T = 7.578). The steelman's structural result: **W4-1/W4-2 correctly characterized the scalar channel; the channel that radiates the observable relic is the condensate channel, and its horizon/temperature is the a₄ surface (T = 7.578), not the a₂ surface (T = 72.8).** The C1 verdict (asymmetric, one-way) is unchanged and reinforced; but "which surface's temperature is the physical Hawking-analog temperature of the relic" is re-pointed from a₂ to a₄ by this argument. That is a substantive, falsifiable, substrate-first claim — and it is the most important thing the c_s-softening side contributes.

**Summary of VOL1.** The van Hove fold is a quantum critical point for the condensate channel (substrate fact). The scalar mode c_BLV is robust to leading order (sp right) but is built from a_n moments that are hyper-sensitive to τ at the DOS spike — so a local c_BLV dip at the fold is plausible and unquantified (steelman). It does not recover symmetric C1 (the walls hold). Its durable payload: (a) W4-1's constant-c discriminant has an unbounded fold/exit-flank error bar that the CC1 scan must close; (b) the radiating horizon for the OBSERVED relic is the condensate-channel (a₄) surface, not the scalar-channel (a₂) surface — re-pointing the physical Hawking-analog temperature from 72.8 to 7.578 M_KK.

#### VOL2: The c_s-driven second-zero mechanism (orthogonal to v-driven)

W4-1 explicitly excluded the v-DRIVEN second zero (v never decelerates back through c, because dS/dτ = +58,673 is one-signed). The c_s-DRIVEN mechanism is orthogonal: hold v supersonic-monotone, and ask whether c_s(τ) can RISE to meet v on the far side. I lay out the only two substrate forms this mechanism could take, run the geometric bound on each, and give the verdict.

**Mechanism A — band-edge recovery overshoot (the "c_B2 climbs back" route).** The DOS spike pins c_B2 to its floor 0.0227 at the fold (impedance lock). On the exit flank, as τ increases past the fold, the B2 band re-disperses and c_B2(τ) recovers from 0.0227 toward its band-asymptote c_BA = 0.399. A second zero of `D = c_B2(τ)² − v(τ)²` requires c_B2(τ) → v_exit ≈ 5.56 M_KK somewhere on the exit flank. Geometric bound (Sage-exact, Re:SP2): the required overshoot is `5.56/0.399 = 13.93×` over the band asymptote, or `5.56/0.0227 = 244.9×` over the fold floor. **Substrate verdict: forbidden.** A condensate mode's group velocity recovering past a DOS spike returns to its BAND value (the dispersion away from the band edge), which is O(c_BA) = O(0.4), set by the BdG band curvature. It does not overshoot to 14× its band value — there is no reservoir in the BdG spectrum that stores a sound speed an order of magnitude above the band scale. In Volovik's framework the maximum group velocity of a gapped BdG branch is bounded by the pair-breaking scale / the band width, not by the superflow speed. Mechanism A is geometrically possible only if the BdG band has a high-velocity shoulder at O(v) ≈ 5.6 M_KK, which the spectrum does not contain (the BdG modes in W4-3 sit at ω ∈ [0.82, 1.06] M_KK — there is no O(5.6) feature). I do not defend Mechanism A.

**Mechanism B — scalar-mode critical recovery (the "c_BLV dips and overshoots" route).** From VOL1, c_BLV(τ) may have a local dip at the fold riding the DOS spike. Could the RECOVERY from that dip overshoot to v_exit? A critical-point dip in a susceptibility typically recovers MONOTONICALLY to the asymptotic value (no overshoot) — Curie-Weiss-type recovery `c(τ) → c_∞` from below. An overshoot would require an UNDERDAMPED susceptibility (a resonance in the recovery), which in a gapped, fully-paired condensate (3He-B is fully gapped — BDI class, my memory) is suppressed by the gap. And even a generous overshoot to 2× the asymptote gives c_BLV ~ 0.97, still a factor ~5.7 below v_exit = 5.56. **Substrate verdict: forbidden.** The scalar mode's asymptote is 0.485; no critical recovery of a gapped-condensate susceptibility overshoots by 11×. Mechanism B does not produce a second zero either.

**The one regime where a c_s-driven second zero COULD live — and why the substrate excludes it.** Both mechanisms fail because the flow v ≈ 5.6 M_KK is enormous compared to ALL corpus sound speeds (≤ 0.485). The ONLY way to get c_s → v is if the flow itself were much slower — i.e. if v_fold were O(0.5) rather than O(6.7). But v_fold = Mach_max·c_BLV = 6.66875 is pinned by Mach_max = 13.75 (baseline-findings-s66, `Mach_max_framework`). So a c_s-driven second zero requires breaching the Mach-13.75 pin, which is a separate, well-anchored substrate result. Within the corpus's own speed hierarchy and Mach pin, NO c_s(τ) bounded by the spectrum produces a second zero. This is the substrate-strong form of sp's wall, and I confirm it: **the c_s-driven second-zero mechanism is excluded by the conjunction (Mach_max = 13.75 pin) ∧ (all branch sound speeds ≤ 0.485) ∧ (no BdG high-velocity shoulder at O(v))**. N_zeros = 1 survives any substrate-derived c_s(τ).

**What VOL2 leaves genuinely open (the honest residual, matching sp's SP2 concession).** The geometric bound is a BOUND, not a scan. Two things the constant-c W4-1 run cannot see: (i) the exit-flank GRAZE — whether c_s(τ)'s local structure moves W4-1's `disc_far_max = −1.94e-3` across the `GRAZE_INFO_CEIL = 1e-3` (it is factor-2 away), which would flip the verdict from clean-PASS to PASS-with-INFO-graze without creating a second zero; (ii) the τ₀ entry shift — a fold dip in c_BLV moves the entry crossing, changing κ_entry and hence the analog entry temperature (W4-2 row 1). Neither re-opens C1; both are quantitative refinements the CC1 scan delivers. The pre-registered gate (VOL3 + sp's CC1): scan `c_s(τ) = |dω/dk|` for BOTH the scalar and condensate branches across [0.05, 0.40], compute `D_channel(τ) = c_channel(τ)² − v(τ)²` for each, report N_zeros per channel AND the exit-flank graze metric per channel. Predicted: N_zeros = 1 in every channel; graze stays sub-ceiling in the scalar channel; the condensate channel is supersonic-everywhere (Mach ~294, no graze). If any channel grazes the ceiling, the verdict is PASS-with-INFO, not RE-OPEN.

#### VOL3: Questions for sp

These target the genuine residual disagreements (not the closed ones — I have conceded the branch separation, the no-bounce reading, and N_zeros = 1). The live questions are channel-identity and which-temperature-radiates.

**Q1 (the radiating-horizon re-pointing — my central new claim).** VOL1 §4 argues the surface that governs what the transit RADIATES (the GGE relic, W4-3's BdG squeeze P(ω) over ω ∈ [0.82, 1.06] M_KK) is the CONDENSATE-channel horizon (where v = c_B2), because the emitted quanta are BdG modes of the condensate, not scalar-mode quanta — and in Volovik's analog-gravity construction (Jacobson-Volovik 1998) each quasiparticle species sees the horizon defined by ITS OWN group velocity. If so, the physical Hawking-analog temperature of the OBSERVED relic is the a₄ condensation-gradient surface (T = 7.578 M_KK, W4-2 row 2), NOT the a₂ kinematic-entry surface (T = 72.8, W4-2 row 1). Do you agree that (i) the radiating quanta are condensate-channel BdG modes, and therefore (ii) the relic's analog temperature is the a₄ surface? If you disagree, what carries the scalar-channel (a₂, T=72.8) emission — i.e. what species is Hawking-radiated at the entry horizon if not the condensate quanta?

**Q2 (the impedance floor — finite or zero?).** Your SP1/CC1/CC3 lean on `Z_B2 = ρ·v_B2 = 0` (S40 Tesla addendum), i.e. v_B2 vanishes exactly at the fold. But the substrate-canonical S94 value is `v_g_B2_fold = 0.022699` M_KK (rho-pinned, `1/(πρ_B2)`, NOT superseded), giving `Z = 1/π = 0.31831` exactly with FINITE v_B2. Do you accept that the substrate-canonical band-edge speed at the fold is the finite 0.0227 (S94), and that "v_B2 = 0" is the unregularized cusp idealization (superseded by S94)? This matters for the CC1 scan: it sets the rho-pinned MINIMUM of c_B2(τ), from which the exit-flank recovery is measured. (I do not think this changes N_zeros = 1 — the factor-294 wall holds either way — but the scan must use 0.0227, not 0.)

**Q3 (is "one sonic horizon" channel-tagged or absolute?).** I argued (Re:SP3, Re:SP4-CC2) that "one sonic horizon" is a SCALAR-channel statement — there is one horizon per quasiparticle branch, and the framework carries (at least) two branches (scalar c_BLV, condensate c_B2/c_BA) plus two thermodynamic surfaces (a₂/a₄) that are the two channels' κ. C1 → ASYMMETRIC (no exit, no bounce) STANDS regardless. But do you accept the channel-tagged RESTATEMENT — "one scalar-channel entry horizon; the condensate-channel horizon is a separate (deeper, Mach-294) surface" — as the substrate-correct form, or do you maintain that the scalar channel is the ONLY one with a physically meaningful horizon (and if so, on what grounds, given that the radiating quanta are condensate modes — Q1)?

**Q4 (the Mach bookkeeping discrepancy).** S73A reports exit Mach ~20.7 in c_BA units; W4-1/W4-2 report fold Mach 13.75 in c_BLV units. The ratio c_BLV/c_BA = 1.216 predicts Mach_BA ≈ 1.22·Mach_BLV, but 20.7/13.75 = 1.51 — the two corpus Mach numbers are NOT consistent with a single v(τ) measured against the two sound speeds. Is this a flow-normalization difference between S55/S73A and W4-1, or does "c_BA units" in S73A hide a different v-definition? (This does not threaten N_zeros = 1 — both are deeply supersonic monotone — but it is the same channel-bookkeeping looseness as CC2, and the workshop should resolve which v(τ) and which c the canonical Mach is built on before §6.2 quotes "the" Mach.)

**Q5 (does the a₂/a₄ surface-gravity ratio carry channel information?).** W4-2's κ-ratio is 9.6117 = κ_entry/κ_exit = (a₂ gradient)/(a₄ gradient), reproducing the corpus 9.61. In my reading (Re:SP3) this is the ratio of the SCALAR-channel surface gravity to the CONDENSATE-channel surface gravity — two different acoustic channels, not two horizons of one flow. Do you read the 9.61 ratio as (your view) a kinematic-vs-condensation gradient ratio at two surfaces of the SAME (scalar) acoustic metric, or (my view) the ratio of surface gravities in two DISTINCT acoustic channels? This is the cleanest place to settle whether our pictures actually differ or are the same structure in two languages (I suspect the latter — see Re:SP3 EMERGES — but the ratio's channel-interpretation is the test).

---

## Round 2 — sp: Follow-up (rebuttal)

### CONVERGENCE

**C-R2-1 — I accept the N_zeros=1 strengthening across the ENTIRE branch hierarchy (Re:SP2), and it is a genuine upgrade to my SP2.** I had argued N_zeros=1 is robust for any `c_s(τ) ≤ O(c_BLV)` via the factor-11 scalar-channel gap. volovik's Re:SP2 shows the wall is **channel-monotone in the direction that forbids the symmetric reading**: because the flow v(τ) is the same in every channel and c_BLV=0.485 is the LARGEST of the four corpus speeds (c_mod=1 is the emergent photon, not a transit-flow reference), building the discriminant on ANY softer channel only deepens the supersonic interior. I verified the B2-channel fold Mach: `v_fold / v_g_B2_fold = 6.66875 / 0.022699323 = 293.79` (Sage; v_fold = Mach_max·c_BLV = (55/4)·(485/1000) = 1067/160 exact). A far-side second zero in the soft channel needs `c_B2 → v_exit = 5.558` M_KK, an overshoot of `5.558/0.0227 = 244.86×` over the rho-pinned floor or `5.558/0.399 = 13.93×` over the band asymptote (Sage-confirmed). My factor-11 was the LOOSEST wall; volovik's factor-294 is the strongest. **Converged: N_zeros=1 is robust across the whole speed hierarchy, not just for c_s ≤ O(c_BLV). The softer the channel, the more forbidding the second-zero wall.**

**C-R2-2 — I accept the irreversibility/one-way-quench argument as the SUBSTRATE-DEEPEST closure of the bounce reading (Re:SP3), and I now rank it above my two dynamics walls.** My SP3 closed the bounce via two hard walls (monotone dS/dτ + speed hierarchy). volovik's Re:SP3 adds a THIRD, deeper one that I did not have: the transit is a **first-order phase transition driven by an instanton gas** (W4-5; THE ORDERED VEIL, S37), i.e. an irreversible Kibble-Zurek quench — and the GGE relic NEVER thermalizes (this session's `ORDERED-VEIL-SUBSTRATE-CLOCK` FAIL: "diabatic freeze-out, NOT integrability-limited"; `HAWKING-GGE-PURITY` INFO: `Tr ρ² = 1, S_ent = 0`, a pure Bogoliubov product state). A bounce is a time-symmetric throat in a (semi-)reversible evolution; a pure, never-thermalized, one-way quench through a first-order transition has NO such throat by the SECOND-LAW direction of the quench, BEFORE any (c²−v²) bookkeeping. This is strictly stronger than my dynamics walls because it does not depend on the speed hierarchy or the gradient sign — it is an entropy-arrow statement. **Converged: the bounce reading is closed at THREE independent levels — irreversibility (deepest), monotone dS/dτ, speed hierarchy. C1 → ASYMMETRIC is over-determined.** This also tightens my CC4: the irreversibility argument and the 12D censorship picture (W4-5: censored singularity one-way at τ→∞; W4-4: a_eff monotone through the fold, a_eff'(fold)=+6.82×10⁻²>0) are the SAME statement seen kinematically (no turning point in a_eff) and thermodynamically (no entropy-reversal) — both forbid a symmetric minimum.

**C-R2-3 — I accept the finite impedance floor: v_B2(fold) = 0.0227, not 0 (Re:SP1/CC3, Q2 answered below).** volovik is right that my SP1/CC3 leaned on the S40/Tesla `Z_B2 = ρ·v_B2 = 0` idealization. The substrate-canonical value is `v_g_B2_fold = 0.022699323` M_KK (S94 `S94-DS-GAMMA-E-RESOLUTION`, get_constant confirms Superseded:False), and I verified the impedance lock is `Z = ρ_B2 · v_g_B2 = 14.023250 × 0.022699323 = 0.31831 = 1/π` EXACTLY (Sage; matches 1/π to <1e-4). So the softening is self-limiting at a **finite rho-pinned floor**, not at zero. This SHARPENS my CC3 ("the softening is determined, not tunable") rather than refuting it — and importantly it does not change N_zeros=1 (the factor-294 wall holds with 0.0227 as the floor). I retract the "v_B2 = 0" form; the correct statement is "v_B2 pinned to 1/(πρ_B2) = 0.0227, the unregularized cusp limit superseded by S94." (One bookkeeping note I concede to volovik's CC3 parenthetical: I cited both Z_wall=1/π≈0.318 and atlas-07 "canonical Z=1.016" as if interchangeable; they differ by ~3.19× and are two normalizations of the same wall. The qualitative conclusion — Z finite, softening determined — holds in both, but I should not have quoted both without pinning the normalization. For the CC1 scan, the rho-pinned form Z=1/π with v_B2=0.0227 is the one that matters.)

**C-R2-4 — I accept the channel-tagged RESTATEMENT of "one sonic horizon" as substrate-correct (Re:SP3 DISAGREE, Re:SP4-CC2, Q3 answered below) — but ONLY for true sonic surfaces, which forces my central dissent below.** volovik is right on the analog-gravity principle: in Volovik's construction (Jacobson-Volovik 1998; *Helium Droplet* §32) each quasiparticle species sees the sonic horizon defined by ITS OWN group velocity, so "one sonic horizon" is channel-specific — one per branch. I accept this. The scalar-channel entry horizon (τ₀=0.1125, where v=c_BLV) is the horizon for SCALAR quanta; the condensate-channel horizon (where v=c_B2) is a separate, deeper surface. I accept the restatement "one scalar-channel entry horizon; the condensate-channel horizon is a distinct (Mach-294) surface." Where I still dissent is volovik's IDENTIFICATION of that condensate-channel sonic surface with the a₄ thermodynamic surface (T=7.578) — those are two different objects, developed in DISSENT below. The channel-tagging is a convergence; the a₂→a₄ re-pointing is not.

### DISSENT

**D-R2-1 — The a₂→a₄ radiating-surface re-pointing (VOL1 §4, Q1) conflates a THERMODYNAMIC condensation edge with a SONIC horizon. The a₄ surface (T=7.578) is NOT the v=c_B2 sonic surface. New evidence: the verdict-line κ provenance + the W4-3 greybody barrier type.** This is my central Round 2 dissent, and it is sharpened by the very channel-tagging I just conceded in C-R2-4.

volovik's claim (VOL1 §4, Q1): the quanta that radiate (W4-3's BdG squeeze) are condensate modes, so per Jacobson-Volovik their Hawking-analog temperature is read off the condensate-channel horizon = the a₄ surface (T=7.578), re-pointing the physical relic temperature from a₂ (72.8) to a₄ (7.578).

The error is in the second step. By the analog-gravity principle volovik invokes (and I accepted in C-R2-4), the condensate-channel SONIC horizon is the surface where `v = c_B2`. I located it: in the B2 channel the trajectory sits at **Mach ~294 everywhere post-genesis** (`v_fold/v_g_B2 = 293.79`; v never drops below ~Mach 245 even on the exit flank since `v_exit/v_g_B2 = 244.86`). Therefore the `v = c_B2` (Mach_B2 = 1) surface is **UPSTREAM of genesis** — before the transit begins — NOT at the a₄ exit. The entire observed trajectory is DEEP INSIDE the condensate-channel horizon. So the a₄ surface at T=7.578 cannot be the condensate-channel sonic horizon; that horizon is not even on the post-genesis trajectory.

What IS the a₄ surface, then? The verdict line is explicit: `row2_exit_a4: kappa=47.6146` sourced from `a_4_FW_zeta=1350.72` — the a₄ **condensation gradient** (κ_exit = ½ ∂_n of the a₄-channel quantity), and S73a `no_exit_horizon=True`. It is a thermodynamic decoherence edge, NOT a Mach-1 crossing (W4-1 found N_zeros=1 — exactly ONE Mach-1 surface in the discriminant channel, the entry). The W4-3 greybody result is the clincher: the barrier that filters the radiated squeeze is `barrier=inverted-parabolic-Poeschl-Teller-POTENTIAL-BARRIER-NOT-S73B-group-velocity` (verdict line, explicit) — i.e. the a₄ surface radiates as a **potential-barrier greybody filter** (T_compound=7.578 is the temperature FEEDING the Pöschl-Teller filter over ω∈[0.82,1.06]), NOT as a sonic/group-velocity horizon. So the substrate's OWN W4-3 computation already tells us the a₄ surface emits via a potential barrier, not via a sonic-horizon Doppler mechanism.

**Corrected synthesis (where volovik is RIGHT and where the re-pointing fails):** volovik is right that the radiated quanta are condensate-channel BdG modes (W4-3's B2/B3 modes), and right that they are filtered/emitted at the a₄ surface (T_compound=7.578 feeds the greybody). What is WRONG is calling the a₄ surface "the condensate-channel Hawking-analog SONIC horizon" — it is a thermodynamic/potential-barrier edge, and the actual v=c_B2 sonic horizon is upstream of genesis (Mach-294 everywhere). The substrate-correct picture: the relic is emitted at the a₄ thermodynamic surface (T=7.578) through a Pöschl-Teller potential barrier, INSIDE an unbounded condensate-channel supersonic interior whose sonic horizon is pre-genesis. So "the physical temperature of the relic is 7.578" is defensible — but as the **condensation/decoherence (a₄) temperature feeding a potential-barrier greybody**, NOT as a re-pointed Hawking SONIC temperature. The a₂ surface (T=72.8) remains the scalar-channel KINEMATIC entry temperature (the genuine v=c_BLV Mach-1 surface). They are two different KINDS of surface (one sonic, one thermodynamic), not two candidate values for "the" Hawking temperature.

**D-R2-2 — On the c_BLV(τ)-dip claim (VOL1 §2, the mode-mode-coupling subleading correction): the steelman is admissible but the framework already contains the discriminating quantity that bounds it, and it is the FINITE peak Mach, not an unscanned extrapolation.** volovik's MISSED point (Re:SP1) and VOL1 §2 are sharp: c_BLV is a global a_n-moment functional, and a moment `∫dλ ρ(λ) f(λ)` over a spectrum whose DOS develops a 1/(πv) van Hove spike is NOT guaranteed τ-smooth — the spike is where a_n moments pick up their largest τ-derivatives (the same spike driving dS/dτ=+58673). I concede the abstract point: "c_BLV constant 0.485" is a fold-anchored value, and the corpus has no `c_BLV(τ)` trajectory on record. A subleading mode-mode-coupling correction `δc_BLV/c_BLV ~ (coupling)·(condensate susceptibility)` is real in 3He physics (density-sound ↔ gap-amplitude coupling).

But here is the NEW evidence that bounds it without a fresh scan: **the peak Mach is a DIRECTLY-computed finite number, 13.735, and it is the discriminant denominator's behavior made visible.** Mach(τ) = v(τ)/c_BLV(τ) by definition. If c_BLV(τ) had a deep local dip at the fold, Mach(τ) would SPIKE at the fold (small denominator). W4-1 computes the peak Mach at the fold = 13.735 (LIVE line `5d1ac75a…`; baseline-findings-s66 Mach=13.75). A finite, modest peak Mach at the fold is direct evidence that c_BLV(τ) does NOT collapse there — the denominator stays O(0.5). This is not the same as proving c_BLV(τ) is perfectly flat (volovik is right it could have a few-percent ripple), but it bounds the dip: a dip deep enough to matter for the discriminant would show as a Mach spike, and none is observed. So the residual is correctly stated as volovik states it — a possible **few-percent exit-flank ripple that could move the GRAZE metric**, NOT a fold collapse. The fold-collapse version of the steelman (which the original C1 challenge premise asserted) is bounded out by the finite peak Mach; the subleading-ripple version survives as an INFO-graze sensitivity, exactly the residual the CC1 scan closes. I dissent from any framing that the c_BLV(τ) question is "wide open" — it is bounded to a few-percent ripple by the finite peak Mach, and the only live quantity is the exit-flank graze, not the fold value.

**D-R2-3 — The exit-flank GRAZE (volovik's MISSED in Re:SP2) is real and I now flag it as the ONE quantity that could move the W4-1 verdict — but it moves it to INFO, never to RE-OPEN, and the geometry says which way.** volovik's near-second-horizon-graze point is well-taken: W4-1's `disc_far_max = −1.936988e-03` sits at `graze_min_abs = 1.936988e-03`, a factor ~1.94 above the `GRAZE_INFO_CEIL = 1e-3` (verdict line confirms both). I had treated the second-zero question as binary; volovik is right it is not. A few-percent c_BLV(τ) ripple on the exit flank shifts `c_BLV(τ)²` and could move `disc_far_max` across the 1e-3 ceiling. Here is the new geometric content: the graze is on the EXIT flank where v has dropped to Mach 11.46. The discriminant there is `D = c_BLV² − v²`. A c_BLV ripple of fractional size ε shifts D by `~2ε·c_BLV² ≈ 2ε·0.235`. To move `disc_far_max` from −1.94e-3 to above −1e-3 (toward the ceiling) requires `2ε·0.235 ≳ 0.94e-3`, i.e. `ε ≳ 0.2%` — a sub-percent ripple suffices to flip clean-PASS to PASS-with-INFO-graze. So the graze sensitivity is REAL and even a tiny ripple activates it. But — and this is the dissent — it activates the INFO band, not a second zero: D would graze toward zero from below but a full crossing needs `c_BLV → v_exit = 5.56` (factor-11.5 overshoot), which the finite peak Mach (D-R2-2) forbids. **The verdict-status consequence: W4-1 is PASS, with a genuine INFO-graze sensitivity on the exit flank that the constant-c scan could not see, resolved by the CC1 scan. This is a refinement of the PASS, not a path to RE-OPEN.** I am upgrading my SP2 concession: the residual is not just "scan c_s to confirm O(0.5)" — it is specifically "scan the exit-flank graze, where a sub-percent ripple matters," and report `disc_far_max` per channel as the discriminating output.

### EMERGENCE

**E-R2-1 — "One sonic horizon per quasiparticle channel" IS a genuine convergence, and it re-frames C1 from a SCALAR question into a SURFACE-TAXONOMY of the transit. This is the durable structural output of the workshop.** The cross-pollination of volovik's analog-gravity-per-species principle (each branch sees its own horizon) with my Penrose-diagram causal-structure framing produces a clean taxonomy of the transit's surfaces that NEITHER of us had alone. The substrate carries surfaces of TWO distinct KINDS, and the C1 confusion (and the dropped V.6 clause) came from not separating them:

- **SONIC surfaces** (Mach-1 crossings, `v = c_channel`): one per quasiparticle channel, by the analog-gravity principle. Scalar channel: the v=c_BLV surface at τ₀=0.1125 (W4-1, the only one ON the post-genesis trajectory, κ_entry=+18.52>0, white-hole). Condensate channel: the v=c_B2 surface, which at Mach-294 is UPSTREAM of genesis (off the observed trajectory entirely). BOTH are entry-side, white-hole, no future-trapped exit — so C1→ASYMMETRIC is unchanged and actually OVER-DETERMINED (both channels' sonic horizons are one-way).
- **THERMODYNAMIC surfaces** (gradient-defined κ from a_n moments, NOT Mach-1 crossings): the a₂ kinematic gradient (κ=457.66, T=72.8) and the a₄ condensation gradient (κ=47.61, T=7.578), W4-2 rows 1–2. These are surface gravities of the spectral-action moment gradients, NOT Doppler horizons.

The W4-2 ledger's "3 surfaces" are correctly read as: TWO thermodynamic surfaces (a₂, a₄) + ONE sonic surface (S63 BLV-acoustic, row 3, T=0.112, the genuine v=c_BLV internal-acoustic Mach-1 surface). The dropped V.6 "two distinct horizons" clause failed because it tried to read two THERMODYNAMIC surfaces as two SONIC horizons. volovik and I converge: keep all surfaces, but tag each by KIND. C1 is, in the new framing, the statement "there is exactly one POST-GENESIS sonic surface (the scalar entry), it is white-hole, and there is no future-trapped sonic exit." That survives untouched.

**E-R2-2 — The Penrose-diagram consequence: the transit diagram has ONE 45° null surface (the scalar entry horizon) bounding the supersonic interior, plus a NESTED structure of thermodynamic/decoherence edges INSIDE it — and the condensate-channel horizon is OUTSIDE the diagram (pre-genesis).** This is the canonical causal-structure picture the workshop produces. Drawing it: a single past null surface (ℐ⁻-side entry, κ>0) opens into an unbounded supersonic expulsion region toward ℐ⁺; the a₂ (T=72.8) and a₄ (T=7.578) thermodynamic surfaces are INTERIOR codim-1 slices (not horizons — geodesics cross them freely, S73a `no_exit_horizon=True`); the genuine censored singularity (W4-5) sits at τ→∞ at future timelike infinity i⁺ along the SU(2) block (timelike-character, cd_C2/U1 spacelike). There is NO symmetric throat, NO i⁰-bounded sandwich. The condensate-channel sonic horizon (v=c_B2, Mach-294) is at the FAR PAST, off the genesis-to-now diagram — meaning for condensate quanta the ENTIRE observable universe is inside the white-hole interior, which is exactly the "we live inside the white hole" reading and is consistent with (strengthens) the asymmetric picture. The diagram is over-determined as asymmetric by: one scalar sonic surface (W4-1), monotone a_eff (W4-4, no turning point at i⁰), censored one-way singularity at i⁺ (W4-5), irreversible quench (no entropy-reversal). I will draw this canonically via the `/penrose-diagram` skill if the workshop promotes it to a registry figure (carry-forward candidate).

**E-R2-3 — The a₂/a₄/c_B2 structure reveals the relic-emission mechanism is a TWO-STEP composite, not a single horizon — and this re-frames volovik's Q1 correctly.** The cross-pollination resolves volovik's radiating-surface question better than either R1 position: the relic emission is (step 1) a sonic horizon in the SCALAR channel at entry (κ=+18.52, the white-hole outflow that sets the OUTFLOW, the v=c_BLV surface) → (step 2) a potential-barrier greybody filter at the a₄ condensation/decoherence edge (T_compound=7.578, Pöschl-Teller, W4-3) that shapes the SPECTRUM of what escapes. The "temperature of the relic" is not a single number from a single horizon — it is a COMPOSITE: the outflow is set at the scalar sonic surface (a₂/kinematic, T=72.8), and the emergent SPECTRUM is shaped at the a₄ thermodynamic surface (T=7.578) by a potential barrier. This is why W4-3 reports T_compound=7.578 as the *greybody* temperature while W4-2 reports T_entry=72.8 as the *kinematic* temperature: they are two stages of one composite emission, not two competing values. volovik's instinct (the relic feels the a₄ surface) is RIGHT — but the a₄ surface acts as a potential-barrier spectral filter, not as a re-pointed sonic horizon. The emergent insight: the substrate's Hawking-analog emission is a sonic-horizon-outflow × potential-barrier-greybody composite, with the two stages living at two different surfaces of two different kinds. This is falsifiable and substrate-first, and it dissolves the a₂-vs-a₄ "which is THE temperature" dispute by showing both enter, at different stages.

### QUESTIONS

I answer volovik's VOL3 questions Q1–Q5 directly, then pose two sharper follow-ups.

**A1 (answering Q1 — the radiating-horizon re-pointing).** (i) YES — the radiating quanta are condensate-channel BdG modes (W4-3's B2[0..3], B3[0..2] over ω∈[0.82,1.06]); I do not dispute the species. (ii) NO to the re-pointing AS STATED, YES to a corrected version (D-R2-1, E-R2-3): the relic's emission feels the a₄ surface, but as a **potential-barrier greybody filter** (W4-3 explicitly: `inverted-parabolic-Poeschl-Teller-POTENTIAL-BARRIER-NOT-S73B-group-velocity`), NOT as a condensate-channel SONIC horizon — because the v=c_B2 sonic surface is at Mach-294 upstream of genesis, not at the a₄ exit. To your follow-up ("what is Hawking-radiated at the a₂ entry if not the condensate quanta?"): the a₂ (T=72.8) surface is the SCALAR-channel kinematic OUTFLOW surface — it sets the white-hole outflow rate of the transit itself (the modulus deformation's own propagation crossing v=c_BLV), which is the "carrier" the condensate quanta ride OUT on. The condensate quanta are then spectrally FILTERED at the a₄ potential barrier (T_compound=7.578). So both surfaces are in the emission chain: a₂ sets the outflow (scalar sonic), a₄ shapes the spectrum (condensate thermodynamic/potential-barrier). The relic carries the a₄ spectral temperature (you are right there) but is launched at the a₂ sonic surface.

**A2 (answering Q2 — the impedance floor).** YES, fully conceded (C-R2-3). The substrate-canonical band-edge speed at the fold is the finite `v_g_B2_fold = 0.022699323` M_KK (S94, Superseded:False), and I verified `Z = ρ_B2·v_g_B2 = 14.023250 × 0.022699323 = 0.31831 = 1/π` exactly (Sage). "v_B2 = 0" is the unregularized cusp idealization (S40/Tesla), superseded by S94. The CC1 scan must use 0.0227 as the rho-pinned MINIMUM of c_B2(τ), measuring the exit-flank recovery from there. Agreed it does not change N_zeros=1 (factor-294 wall holds with 0.0227 as floor).

**A3 (answering Q3 — channel-tagged or absolute).** Channel-tagged, conceded (C-R2-4) — WITH the surface-taxonomy refinement (E-R2-1). "One sonic horizon" is a per-channel statement: one scalar-channel sonic surface (v=c_BLV, on-trajectory at τ₀) and one condensate-channel sonic surface (v=c_B2, off-trajectory at Mach-294 upstream). I do NOT maintain the scalar channel is "the only one with a meaningful horizon" — both are meaningful. What I maintain (D-R2-1) is that the condensate-channel SONIC horizon is NOT the a₄ thermodynamic surface; they are different objects. So the correct channel-tagged statement is: "one scalar-channel entry sonic horizon (on-trajectory); one condensate-channel sonic horizon (pre-genesis, off-trajectory); plus two thermodynamic surfaces (a₂/a₄) that are NOT sonic." Three KINDS, not two horizons.

**A4 (answering Q4 — the Mach bookkeeping discrepancy).** RESOLVED, and it is a v-DEFINITION difference, not a sound-speed inconsistency — which actually supports my CC2 position. I checked all candidate flow velocities against the two sound speeds (Sage):
- v_fold = 6.66875 → Mach_BLV = 13.75 ✓, Mach_BA = v_fold/c_BA = 16.71 (NOT 20.7).
- v_term = 26.545 (S38/S55, canonical terminal asymptote) → Mach_BLV = 54.73, Mach_BA = 66.51 (NOT 20.7).
- v_exit-flank ≈ 5.558 → Mach_BLV = 11.46, Mach_BA = 13.93 (NOT 20.7).
None of the three corpus flow velocities gives 20.7 in c_BA units via a single rescale. The "20.7 in c_BA units" implies v = 20.7×0.399 = 8.26 M_KK — which is none of {v_fold, v_term, v_exit-flank}. So S73A's Mach 20.7 is built on yet a FOURTH v-definition (likely a Jensen-trajectory exit-window average or a different normalization of the velocity-energy-conservation integral, `dv/dτ = −(dS/dτ)/(M_ATDHFB·v)`, evaluated over a different window than W4-1's fold-peak). The discrepancy is entirely in WHICH v(τ) is used, not in the sound speed. **This SUPPORTS CC2**: the sound speed for C1's discriminant is unambiguously c_BLV (the scalar channel); the looseness in the corpus is which FLOW v(τ) the canonical Mach quotes. The workshop should pin: §6.2's "Mach" = v_fold/c_BLV = 13.75 (the fold-peak in the scalar channel), and flag that S73A's 20.7 is a different (terminal/window) v-definition. This is a carry-forward: reconcile the v(τ) normalizations across S55/S73A/W4-1 before §6.2 quotes "the" Mach.

**A5 (answering Q5 — does the 9.61 κ-ratio carry channel information).** I read the 9.6117 ratio = κ_entry(a₂)/κ_exit(a₄) as **the ratio of two thermodynamic surface gravities** (your view, mostly) — but with the refinement that they are NOT "two horizons of one flow" (my SP3 phrasing was loose) NOR "two sonic horizons of two channels" (an over-strong reading). They are the ratio of the a₂ KINEMATIC gradient to the a₄ CONDENSATION gradient — two spectral-action-moment surface gravities. I now think we AGREE (your Re:SP3 EMERGES suspicion is correct): your "two channels' κ" and my "two thermodynamic surfaces" are the SAME structure, because a₂ IS the scalar/kinematic channel's moment-gradient and a₄ IS the condensation channel's moment-gradient. The 9.61 ratio is a₂-gradient/a₄-gradient = (scalar-channel surface gravity)/(condensate-channel surface gravity) at the fold, which is BOTH "two channels' κ" AND "two thermodynamic surfaces' κ" — the descriptions coincide. Where we still differ is ONLY whether these thermodynamic surfaces are also SONIC horizons (they are not — D-R2-1). The κ-ratio carries CHANNEL information (a₂↔scalar, a₄↔condensate) but NOT sonic-horizon information. So: agreed it is a two-channel gradient ratio; not agreed that either is a sonic surface.

**My follow-up questions for volovik (Round 3 targets):**

**SP-Q-R2-1 (the crux test of D-R2-1).** Do you accept that the a₄ surface (T=7.578) is the condensate-channel THERMODYNAMIC/potential-barrier surface (per W4-3's explicit `POTENTIAL-BARRIER-NOT-group-velocity` tag and S73a `no_exit_horizon=True`), and that the condensate-channel SONIC horizon (v=c_B2, Mach-294) is a DISTINCT surface upstream of genesis? If you still want the a₄ surface to BE the condensate sonic horizon, you need v=c_B2 somewhere on the post-genesis trajectory — but I find Mach_B2≥245 everywhere on it. Where on the genesis-to-now trajectory is v=c_B2 = 0.0227? (I claim: nowhere — it is pre-genesis.)

**SP-Q-R2-2 (does the composite-emission reading (E-R2-3) resolve the dispute to your satisfaction?).** The two-step picture — scalar sonic OUTFLOW (a₂, T=72.8) × condensate potential-barrier GREYBODY (a₄, T=7.578) — keeps your physical instinct (the relic spectrum carries 7.578) while correcting the mechanism (a₄ is a spectral filter, not a sonic horizon). Does this composite reading converge our positions, or do you maintain a substantive claim that the a₄ surface must be a SONIC horizon (not a potential-barrier filter) for the Jacobson-Volovik construction to apply? Concretely: in 3He, does the relevant quasiparticle Hawking emission require a sonic horizon, or does a potential-barrier greybody at an interior decoherence edge suffice — given that W4-3's substrate computation found a potential barrier, not a group-velocity horizon?

---

## Round 2 — volovik: Cross-Synthesis (rebuttal)

### CONVERGENCE

**C-R2-V1 — I CONCEDE D-R2-1 in full: the a₄ surface (T=7.578) is the condensate-channel THERMODYNAMIC/potential-barrier surface, NOT a sonic horizon, and my a₂→a₄ "re-pointing of the sonic Hawking temperature" was a category error. The substrate kills it three ways, and I went to the Volovik corpus to check my own claim against the parent physics.** sp's D-R2-1 is correct and I do not defend the sonic re-pointing. The decisive point is the one sp located: by the very Jacobson-Volovik analog-gravity principle I invoked in VOL1 §4 (each quasiparticle species sees the sonic horizon defined by ITS OWN group velocity), the condensate-channel sonic horizon is the surface where `v = c_B2`, and that surface is NOT on the post-genesis trajectory. I verified sp's location numbers Sage-exact: with `v_fold = Mach_max·c_BLV = (55/4)·(485/1000) = 1067/160 = 6.66875` M_KK and the rho-pinned floor `v_g_B2_fold = 0.022699323` M_KK, the B2-channel fold Mach is `v_fold/v_g_B2 = 293.79`, and on the exit flank `v_exit/v_g_B2 = 244.86` — so `Mach_B2 ≥ 244.86` EVERYWHERE post-genesis, and the `v = c_B2` (Mach_B2 = 1) surface lies upstream of genesis, off the observed trajectory. The a₄ surface at T=7.578 therefore cannot BE the condensate sonic horizon; that horizon is not even on the diagram. I answer SP-Q-R2-1 directly in the ANSWERS block at the end of this Round 2 section, but the bottom line is conceded here: a₄ is a thermodynamic/decoherence edge, the condensate sonic horizon is pre-genesis, and "re-point the sonic temperature from a₂ to a₄" was wrong.

**The corpus check that settles it (and that I should have run in R1).** I read Volovik's own analog-Hawking construction — *Simulation of Painlevé-Gullstrand black hole in thin ³He-A film* (corpus #27, gr-qc/9901077) — to test whether the parent physics lets a thermodynamic edge stand in for the sonic horizon. It does not. In #27 the analog-Hawking temperature is `T_Hawking = ℏ|v'(r)|_{r=r_h}/(2π)` — the velocity-gradient (surface gravity) evaluated AT THE HORIZON `r_h`, and `r_h` is DEFINED as the surface where `|v| = c` (the Mach-1 crossing, Eq. 2/12). The Hawking-analog SONIC temperature is, in the parent, read off the v=c surface and nowhere else. There is no version of the Jacobson-Volovik construction in which the sonic Hawking temperature is read off an interior decoherence edge that the flow crosses freely. So sp's D-R2-1 is not just internally consistent — it is forced by the parent ³He physics, by parent→child inheritance (`3HeB-inheritance-canonical.md`), not analogy. My VOL1 §4 conflated "the surface whose temperature the relic CARRIES" (a₄, correct) with "the sonic horizon that DEFINES that temperature" (the v=c_B2 surface, pre-genesis) — two different objects. Conceded.

**C-R2-V2 — I accept the surface-taxonomy reframing (E-R2-1) as the durable structural output, and I accept sp's THREE-KINDS correction to my two-kinds picture (A3).** In Re:SP3/Re:SP4-CC2 I argued "one sonic horizon per channel" and "two thermodynamic surfaces (a₂/a₄) that are the two channels' κ." sp's E-R2-1 + A3 sharpen this correctly: there are not two kinds of surface but the taxonomy must keep them STRICTLY separate — (1) SONIC surfaces (Mach-1 crossings, `v=c_channel`): one per quasiparticle channel — scalar (v=c_BLV at τ₀=0.1125, on-trajectory) and condensate (v=c_B2, pre-genesis, off-trajectory); (2) THERMODYNAMIC surfaces (gradient-defined κ from a_n moments, NOT Mach-1 crossings): a₂ (T=72.8) and a₄ (T=7.578). My error in Re:SP3 was sliding from "a₄ is the condensate channel's κ" (true) to "a₄ is the condensate channel's sonic horizon" (false). The a₄ surface is the condensate channel's THERMODYNAMIC κ; the condensate channel's SONIC κ lives at the pre-genesis v=c_B2 surface. These are different κ's of the same channel. I accept this is the substrate-correct taxonomy and that it is the workshop's central durable result.

**C-R2-V3 — I accept that my A5 convergence with sp on the 9.61 κ-ratio is now COMPLETE, with the sonic-vs-thermodynamic distinction as the residual.** sp's A5 confirms my Re:SP3-EMERGES suspicion: "two channels' κ" (my language) and "two thermodynamic surfaces" (sp's language) are the SAME structure, because a₂ IS the scalar/kinematic channel's moment-gradient and a₄ IS the condensation channel's moment-gradient. The 9.6117 ratio = κ_entry(a₂)/κ_exit(a₄) is BOTH descriptions at once. I now agree the κ-ratio carries CHANNEL information (a₂↔scalar, a₄↔condensate) but NOT sonic-horizon information — because neither a₂ nor a₄ is a sonic surface (the only on-trajectory sonic surface is the scalar entry at τ₀). This is a clean convergence: we differed only on whether the thermodynamic surfaces are also sonic, and they are not (D-R2-1).

**C-R2-V4 — I accept D-R2-2's bound on my c_BLV(τ)-dip steelman: the finite peak Mach (13.735) bounds the fold-collapse version OUT, leaving only the sub-percent exit-flank ripple — which is exactly the residual I want.** sp's D-R2-2 is the right rebuttal to VOL1 §2 and I concede it. The substitution chain is clean: `Mach(τ) ≡ v(τ)/c_BLV(τ)` by definition; if c_BLV(τ) had a deep local dip at the fold, Mach(τ) would SPIKE there (small denominator); W4-1 computes a FINITE peak Mach = 13.735 at the fold (LIVE line `5d1ac75a…`); therefore c_BLV(τ) does NOT collapse at the fold — the denominator stays O(0.5). This bounds the fold-collapse version of the steelman OUT (which was the naive C1-challenge premise — and I never defended that premise, Re:SP1). What survives is precisely the sub-percent ripple: sp's own geometry (D-R2-3) shows a fractional ripple `ε ≳ 0.2%` on the exit flank suffices to move `disc_far_max` from −1.94e-3 across the `GRAZE_INFO_CEIL = 1e-3` — I confirm this Sage-exact (`ε = 0.94e-3/(2·c_BLV²) = 0.94e-3/(2·0.235225) = 0.001998`, i.e. 0.20%). So the live residual is correctly stated as "a sub-percent exit-flank ripple that moves the GRAZE, not a fold collapse and not a second zero." I retract any framing that c_BLV(τ) is "wide open"; it is bounded to a ~0.2%-sensitive exit-flank graze by the finite peak Mach. This is the residual the CC1 scan closes, and it moves the verdict to PASS-with-INFO at most, never RE-OPEN.

**C-R2-V5 — I accept D-R2-3: the exit-flank graze moves the verdict to INFO, never to RE-OPEN, and the direction is fixed.** sp's D-R2-3 closes the binary-vs-graze point I raised in Re:SP2. The graze sensitivity is real (sub-percent ripple activates it) but it grazes D toward zero FROM BELOW; a full crossing still needs `c_BLV → v_exit = 5.56` (factor-11.46 overshoot, factor-294 in the soft channel), which the finite peak Mach (C-R2-V4) and the speed hierarchy forbid. So the graze is an INFO annotation on near-extremality, not a second zero. Converged: W4-1 is PASS with a genuine exit-flank INFO-graze sensitivity the constant-c scan could not see; the CC1 scan must report `disc_far_max` per channel as the discriminating output.

### DISSENT

**D-R2-V1 — Conceding the SONIC re-pointing does NOT concede the OBSERVABLE-TEMPERATURE claim. The surviving substrate claim, stated precisely: the temperature that sets the OBSERVED relic spectrum is the a₄ condensate-channel temperature (T=7.578 M_KK), not the a₂ scalar-entry temperature (T=72.8). I sharpen this from "the relic feels the a₄ sonic horizon" (RETRACTED) to "the relic's spectral temperature is the a₄ value, because the observed quanta are condensate-channel BdG modes that are SPECTRALLY PROCESSED at the a₄ edge — and the a₂ scalar-entry temperature is NOT a temperature the relic carries."** sp's composite reading (E-R2-3, A1) keeps "the relic carries 7.578" while correcting the mechanism — and I accept the mechanism correction. But the composite reading as sp states it makes a₂ (72.8) the "outflow temperature" and a₄ (7.578) the "spectral temperature," presented as two co-equal stages of one emission. I dissent from the SYMMETRY of that presentation. The substrate-correct claim is asymmetric: **the a₂ scalar-entry surface gravity is a kinematic property of the CARRIER (the modulus deformation's own v=c_BLV crossing) — it is NOT a temperature any OBSERVED quantum carries, because no observed quantum is a scalar-mode quantum.** The observed relic is entirely condensate-channel BdG modes (W4-3's B2[0..3], B3[0..2]); their spectrum is set at the a₄ edge (T_compound=7.578); the a₂ value 72.8 governs the rate at which the modulus fabric itself transits, which is observationally invisible (the fabric IS space — there is no external quantum that carries the fabric's own transit temperature). So I agree both surfaces are in the EMISSION CHAIN; I dissent that both are TEMPERATURES OF THE RELIC. Exactly one is: a₄, 7.578.

**Why this is substrate-grounded and not a re-import of the retracted claim.** This survives precisely BECAUSE of Volovik #27 §F, which I read for C-R2-V1 and which turns out to be the strongest support for the surviving claim. In #27 Volovik distinguishes (i) the sonic horizon where `T_Hawking = ℏ|v'|_{r_h}/2π` is DEFINED (the v=c surface) from (ii) the interior processing that determines what actually escapes: "particle creation in a high gravity field can disturb the Minkowski vacuum INSIDE the horizon without any radiation to the exterior… radiated particles with energies outside the relativistic region can be Andreev scattered back into the black hole… pair creation inside the horizon may be more important… than Hawking radiation itself" (#27 §F). This is the parent statement of a TWO-STAGE structure where the OBSERVABLE spectrum is set by the interior processing (Andreev/pair-creation), not by the bare sonic-horizon temperature. The framework's a₄ Pöschl-Teller potential barrier (W4-3, `inverted-parabolic-Poeschl-Teller-POTENTIAL-BARRIER-NOT-S73B-group-velocity`) is the child realization of Volovik's interior-processing stage. So the surviving claim — the OBSERVED relic temperature is the a₄ interior-edge temperature (7.578), and the entry/sonic temperature (72.8/scalar) is NOT carried by any observed quantum — is exactly what #27 §F asserts for the parent. I am no longer claiming a₄ is a sonic horizon (retracted); I am claiming a₄ is the interior-processing edge that sets the observable spectrum, which is the substrate-correct two-stage Volovik picture.

**What would FALSIFY the surviving claim (precise, substrate-first).** The claim "the observed relic spectral temperature is the a₄ value 7.578, not the a₂ value 72.8" is falsified if EITHER: **(F1)** the radiated/observed quanta are shown to include a scalar-mode (c_BLV-channel) component that carries the a₂ entry temperature — i.e. if there is an observable channel in which the relic spectrum peaks at the 72.8 scale rather than the 7.578 scale. Concretely: the W4-3 squeeze P(ω) is supported on ω∈[0.82,1.06] M_KK with T_compound=7.578; if a substrate computation finds a SECOND observable squeeze branch supported near the 72.8 scale (an order of magnitude higher in ω), the a₂ temperature IS carried by an observed quantum and the asymmetry breaks. **(F2)** the Andreev/back-scattering processing at the a₄ edge is shown to be NEGLIGIBLE (transmitted fraction → 1, no spectral reshaping), so the observed spectrum is set at the sonic-horizon stage (the scalar entry, 72.8) and a₄ is a passive label. W4-3 already bounds F2 the right way — `transmitted_fraction=0.511872`, `band_narrows=True`, `spread_ratio=0.821732`: the a₄ barrier DOES reshape the spectrum (filters ~49%), so the interior-processing stage is active, consistent with #27 §F and against F2. The cleanest falsifier is F1: scan for a scalar-channel observable squeeze branch near the 72.8 M_KK scale; its ABSENCE confirms the surviving claim, its presence breaks it. I register this as the carry-forward gate (Round 3). This is a substantive, falsifiable, substrate-first claim — distinct from the retracted sonic-re-pointing, and it is the durable payload of the c_s-softening side.

**D-R2-V2 — A residual sharpening on the κ-provenance that sp's D-R2-1 cites: the a₄ κ_exit = 47.61 is a CONDENSATION-gradient surface gravity, and that κ being well-defined and POSITIVE (white-hole-sign) is itself evidence that a₄ is a radiating edge in the #27-§F sense, NOT merely a passive decoherence slice. I do not dispute D-R2-1's verdict (a₄ ≠ sonic horizon); I add that "thermodynamic edge" understates it.** sp's D-R2-1 reads a₄ as "a thermodynamic decoherence edge" with κ from the condensation gradient and `no_exit_horizon=True`. Correct — but I flag that a surface with a well-defined positive surface gravity `κ_exit=47.61>0` and a Pöschl-Teller barrier that demonstrably filters the spectrum (W4-3) is doing MORE than passively decohering: it is the active spectral-processing stage of the emission. In Volovik #27 §F the interior pair-creation/Andreev stage has no sonic horizon either, yet it is the dominant emission-shaping process. So "a₄ is a thermodynamic edge, not a sonic horizon" (sp, correct) and "a₄ is the active spectral-processing surface that sets the observable temperature" (my surviving claim) are BOTH true and not in tension — the second is the substrate content of the first. This is a sharpening, not a dissent from D-R2-1's verdict; I want the registry text to record a₄ as the active interior-processing edge (the #27-§F stage), not as a passive slice, because the observable temperature lives there.

### EMERGENCE

**E-R2-V1 — The convergence yields a clean substrate statement of the composite emission with a PARENT-PHYSICS anchor neither of us had in R1: the transit's Hawking-analog emission is the ³He two-stage structure of Volovik #27 — (stage 1) a SONIC horizon that defines the kinematic temperature, ×(stage 2) interior pair-creation/spectral-processing that sets what is OBSERVED — and the framework's two surfaces map ONE-TO-ONE onto Volovik's two stages.** sp's E-R2-3 composite (scalar sonic outflow × condensate potential-barrier greybody) is correct, and reading it against corpus #27 §F upgrades it from a framework-internal reconciliation to a parent→child inheritance. The map is exact:

| Volovik #27 (parent ³He-A analog black/white hole) | Framework (child, the transit) | Surface KIND |
|:---|:---|:---|
| Sonic horizon `r_h` where `\|v\|=c`; defines `T_Hawking=ℏ\|v'\|_{r_h}/2π` | Scalar entry sonic surface `v=c_BLV` at τ₀=0.1125; `κ_entry=+18.52` (W4-1); a₂ kinematic temperature T=72.8 | SONIC (kinematic) |
| Interior pair-creation/Andreev processing (#27 §F): sets what escapes, can trap partners, "more important than Hawking radiation itself" | a₄ condensation/decoherence edge; Pöschl-Teller potential barrier filters the BdG squeeze (W4-3, transmitted_fraction=0.512); T_compound=7.578 | THERMODYNAMIC (spectral-processing) |

The durable insight: the framework did not invent a two-surface emission; it INHERITED the ³He two-stage analog-Hawking structure, where the kinematic temperature (stage 1, sonic) and the observable temperature (stage 2, interior processing) are genuinely DIFFERENT numbers at DIFFERENT surfaces. This dissolves the R1 a₂-vs-a₄ "which is THE temperature" dispute exactly as sp said (E-R2-3) — but with the parent-physics reason WHY: in #27 §F the observable spectrum is set by interior processing, not by the bare sonic horizon, so a₄ (interior processing) carrying the relic temperature while a₂ (sonic) sets the carrier kinematics is the canonical Volovik structure, not a framework peculiarity.

**E-R2-V2 — "We live inside the white hole" gets a sharper substrate reading from the Mach-294 condensate horizon, and it is the strongest form of C1→ASYMMETRIC. The condensate-channel sonic horizon being PRE-GENESIS means the entire observable universe is interior to it for the radiating species — and Volovik #27's "T = −0 behind the horizon" / reversed-population picture is exactly the GGE inverted distribution.** sp's E-R2-2 Penrose diagram (one scalar sonic surface bounding the supersonic interior; condensate sonic horizon off-diagram pre-genesis) cross-pollinates with the Volovik corpus to give the substrate reason this is over-determined as asymmetric. In #27 §C–D, behind the horizon (|v|>c) the branch population is REVERSED — negative-energy states empty, positive-energy occupied, "corresponding to temperature T = −0" in the rest frame. The framework's post-transit GGE is exactly this reversed/inverted distribution (the squeeze is a Bogoliubov product state, `HAWKING-GGE-PURITY` Tr ρ²=1, S_ent=0). So "we live inside the white hole" is not loose: for the condensate (radiating) species, the entire post-genesis trajectory is at Mach_B2 ≥ 245 — deep inside the condensate sonic horizon — and the observed relic IS the reversed-population vacuum seen from inside, which is the parent #27 §C–D statement. C1→ASYMMETRIC is over-determined: (1) scalar sonic surface white-hole one-way (W4-1); (2) condensate sonic surface pre-genesis, entire trajectory interior (this workshop); (3) monotone a_eff, no turning point (W4-4); (4) censored one-way singularity at τ→∞ (W4-5); (5) irreversible quench, GGE never thermalizes (Re:SP3); (6) reversed-population interior matching #27 §C–D. Six independent levels; the bounce reading breaches all six. This is the substrate-strongest closure and it is the workshop's over-determination result.

**E-R2-V3 — A cross-domain note for §6.2's analog-T ledger (Doc-routing target): the surface-taxonomy implies the §6.2 ledger should carry a KIND column, and the "temperature of the relic" entry should be a₄ (7.578) tagged SPECTRAL/interior-processing, with a₂ (72.8) tagged KINEMATIC/sonic-carrier — NOT presented as two candidate Hawking temperatures.** The workshop's surface-taxonomy (E-R2-1) has a direct documentation consequence for the held §6.2 white-hole-symmetry section. The W4-2 3-surface ledger (a₂ T=72.8 / a₄ T=7.578 / S63-BLV T=0.112) currently lists three temperatures without a KIND tag, which is exactly what let the dropped V.6 clause conflate thermodynamic with sonic surfaces. The substrate-correct ledger needs each row tagged by KIND: **a₂ = THERMODYNAMIC-kinematic (scalar-channel moment-gradient κ; sets carrier kinematics; co-located with the scalar entry SONIC surface at τ₀); a₄ = THERMODYNAMIC-spectral (condensation moment-gradient κ; the #27-§F interior-processing edge; sets the OBSERVED relic spectral temperature); S63-BLV = SONIC (the genuine on-trajectory v=c_BLV internal-acoustic Mach-1 surface, T=0.112).** And the §6.2 narrative should state the relic temperature is the a₄ value (7.578) as the spectral/interior-processing temperature, with a₂ (72.8) as the kinematic-carrier temperature that no observed quantum carries (D-R2-V1). This keeps both surfaces in the ledger (sp's "keep all surfaces") while removing the conflation (each tagged by kind) and making the OBSERVABLE temperature unambiguous (a₄, spectral). I route this to the Doc-routing (PENDING) section and the w4 WP constraint-map per the deconfliction mandate — I do NOT edit `phonic-exflation-equation.md`. This is the cross-domain payload of the convergence for the documentation layer.

### ANSWERS to sp's SP-Q-R2-1 and SP-Q-R2-2

**A-V1 (answering SP-Q-R2-1 — is a₄ the condensate THERMODYNAMIC surface, and is the condensate SONIC horizon a distinct pre-genesis surface? Where on the genesis-to-now trajectory is v=c_B2?).** YES, fully accepted, and your "nowhere — it is pre-genesis" is correct; I verified it Sage-exact. The condensate-channel sonic horizon is the surface `v = c_B2 = 0.022699323` M_KK (Mach_B2 = 1). On the post-genesis trajectory the flow runs from `v_fold = 6.66875` (Mach_B2 = 293.79) down to `v_exit ≈ 5.558` (Mach_B2 = 244.86) — so `Mach_B2 ∈ [244.86, 293.79]` everywhere on genesis-to-now, never approaching 1. The v=c_B2 surface requires `v = 0.0227` M_KK, which is **a factor ~245 below the slowest post-genesis flow** (the exit-flank `v ≈ 5.56`). The flow only reaches 0.0227 M_KK BEFORE genesis (on the sub-fold genesis flank where v rises from 0 through the sound speeds: v crosses c_L=0.025 first, then c_B2≈0.0227 essentially at the same pre-genesis point, then c_BA=0.399, then c_BLV=0.485 at τ₀=0.1125 = the scalar entry). So the condensate sonic horizon is at the FAR PAST genesis flank, off the observed trajectory, exactly as you locate it. **The a₄ surface (T=7.578) is therefore NOT the condensate sonic horizon — it is the condensate-channel THERMODYNAMIC/spectral-processing edge** (κ_exit=47.61 from the a₄ condensation gradient; W4-3 Pöschl-Teller barrier; S73a `no_exit_horizon=True`). I do NOT want a₄ to BE the condensate sonic horizon — that was my R1 error, retracted in C-R2-V1. The condensate sonic horizon and the a₄ thermodynamic edge are two different objects at two different τ (one pre-genesis, one on the exit flank), and the substrate carries both. Crux test passed in your favor.

**A-V2 (answering SP-Q-R2-2 — does the composite-emission reading resolve the dispute? In ³He, does quasiparticle Hawking emission REQUIRE a sonic horizon, or does a potential-barrier greybody at an interior decoherence edge suffice?).** YES, the composite reading (E-R2-3) converges our positions — and I went to the parent ³He physics to answer your concrete sub-question authoritatively, because it is exactly a Volovik-corpus question. The answer is BOTH/two-stage, and it vindicates your composite:

- **The Hawking-analog SONIC TEMPERATURE requires a sonic horizon.** In Volovik #27 (Painlevé-Gullstrand ³He-A black hole), the analog-Hawking temperature `T_Hawking = ℏ|v'(r)|_{r=r_h}/(2π)` is DEFINED only at the sonic horizon `r_h` where `|v|=c` (Eqs. 11–12), via the WKB tunneling action `S = π E/|v'|_{r_h}` across that surface. There is no analog-Hawking SONIC temperature without a v=c surface. So if one insists on "the sonic Hawking temperature of the relic," one MUST point at a v=c surface — and for the condensate species that surface is pre-genesis (A-V1), so it is NOT the a₄ edge. This is why my R1 sonic-re-pointing fails: I pointed the sonic temperature at a non-sonic surface.

- **But the OBSERVED spectrum does NOT require the sonic horizon — it is set by interior processing, and a potential-barrier/decoherence edge suffices.** Volovik #27 §F is explicit: "particle creation in a high gravity field can disturb the Minkowski vacuum INSIDE the horizon without any radiation to the exterior… radiated particles with energies outside the relativistic region can be Andreev scattered back into the black hole… pair creation inside the horizon may be more important for dissipation… than Hawking radiation itself." So in the PARENT, the observable outcome is dominated by interior pair-creation/Andreev processing, NOT by the bare sonic-horizon emission. A potential-barrier greybody at an interior edge is precisely the child realization of this interior-processing stage. **W4-3 found a potential barrier, not a group-velocity horizon — and that is CORRECT substrate physics, not a defect: it is the #27-§F interior-processing stage.** So your concrete question — "does a potential-barrier greybody at an interior decoherence edge suffice?" — the parent answer is YES for setting the observable spectrum, and NO only for defining the bare sonic temperature (which the relic spectrum does not directly carry anyway).

**Net**: the composite reading is substrate-correct and inherited, not a compromise. Stage 1 (sonic, scalar entry, a₂, T=72.8) defines the kinematic carrier temperature; stage 2 (interior processing, a₄, T=7.578, Pöschl-Teller) sets the observed spectrum. My ONE surviving claim (D-R2-V1) is the asymmetry WITHIN the composite: the OBSERVED relic carries the a₄ spectral temperature (7.578), and the a₂ sonic temperature (72.8) is the carrier's, not the relic's — which is exactly #27 §F (observable set by interior stage). I do NOT maintain that a₄ must be a sonic horizon for Jacobson-Volovik to apply; the parent shows the interior stage suffices for the observable. So we converge, with my residual being which surface's NUMBER the observable temperature is (a₄), not which KIND of surface it is (thermodynamic — agreed).

---

## Round 3 — sp: Follow-up (convergence)

### CONVERGENCE

**This is the convergence round. I state the locked position plainly, then carry only one genuine residual into DISSENT.**

**C-R3-1 — C1 → ASYMMETRIC STANDS as a structural fact, over-determined at SIX independent levels, adversarially hardened against BOTH the v-driven and the c_s-driven symmetric mechanisms. This is no longer a verdict-under-test; it is a structural theorem of the W4 geometry.** The workshop was convened to test whether the substrate-correct c_s(τ) re-opens C1. It does not — and the process of testing it produced an over-determination neither of us had at R1. The bounce/symmetric-two-horizon reading must breach ALL SIX of the following, and a bounded c_s(τ) breaches none:

1. **Scalar-channel speed-hierarchy wall** (SP2, factor-11.46): a far-side second zero needs c_BLV → v_exit = 5.558 M_KK, an 11.46× overshoot the spectrum forbids.
2. **Channel-monotone strengthening** (Re:SP2, C-R2-1): the wall is channel-monotone in the forbidding direction — every softer channel only deepens the supersonic interior. In the B2 channel the fold Mach is `v_fold/v_g_B2 = 6.66875/0.022699323 = 293.79` (Sage-exact), so the soft-channel second-zero overshoot is 244.86× over the rho-pinned floor. c_BLV = 0.485 is the LARGEST transit-referencing speed; there is no channel where the gap is smaller.
3. **Monotone-dS/dτ wall** (SP2/SP3, hard-wall #1): dS/dτ = +58,673 one-signed → v never decelerates → no v-driven turning point. Reinforced by the all-negative off-Jensen Hessian (S76 — the trajectory is on a maximal ridge, monotone descent).
4. **Irreversibility / one-way-quench wall** (Re:SP3, C-R2-2 — the substrate-deepest): the transit is a first-order Kibble-Zurek quench driven by an instanton gas (W4-5; THE ORDERED VEIL, S37); the GGE relic NEVER thermalizes (`ORDERED-VEIL-SUBSTRATE-CLOCK` FAIL: "diabatic freeze-out"; `HAWKING-GGE-PURITY` INFO: Tr ρ²=1, S_ent=0, a pure Bogoliubov product state). A pure, never-thermalized, one-way quench has no time-symmetric throat by the entropy-arrow direction, BEFORE any (c²−v²) bookkeeping. This ranks above the dynamics walls because it does not depend on the speed hierarchy or the gradient sign.
5. **12D censorship / monotone-a_eff wall** (CC4, W4-4/W4-5): a_eff is monotone through the fold (a_eff'(fold) = +6.82×10⁻² > 0, no turning point — W4-4 latest `7b2093b9…`, closed-form q_Ω); the genuine singularity is censored and one-way at τ→∞ (W4-5 PASS `9ffb4aea…`, K12_slope=4.0, charA=timelike-SU2/spacelike-C2U1). A bounce needs ȧ=0 at a throat; a_eff has none.
6. **Reversed-population-interior wall** (volovik E-R2-V2, Volovik #27 §C–D): behind the condensate horizon the branch population is inverted ("T=−0"), and the post-transit GGE IS that inverted distribution. The "we live inside the white hole" reading is the substrate-correct form: for the radiating (condensate) species the entire post-genesis trajectory is at Mach_B2 ≥ 244.86 — deep interior.

Six independent levels. I accept volovik's count and add nothing to weaken it. **C1 → ASYMMETRIC is unconditional on the symmetric/asymmetric axis.** The only residual conditionality is the magnitude-of-the-graze refinement (C-R3-3 below), which moves the verdict to PASS-with-INFO at most, never to RE-OPEN.

**C-R3-2 — The surface-taxonomy is the shared resolution, and the two-stage ³He-#27 emission is its parent-physics anchor. Locked.** The R1 confusion (and the dropped V.6 "two distinct horizons" clause) was a conflation of two distinct KINDS of surface. The convergence keeps every surface but tags each by kind:

- **SONIC surfaces** (Mach-1 crossings, `v = c_channel`; one per quasiparticle branch by the Jacobson-Volovik analog-gravity principle): scalar channel → `v = c_BLV` at τ₀=0.112466, ON-trajectory, κ_entry = +18.52 > 0, white-hole (W4-1, the only post-genesis sonic surface); condensate channel → `v = c_B2`, PRE-genesis, OFF-trajectory (Mach_B2 ∈ [244.86, 293.79] everywhere post-genesis — A-V1 verified Sage-exact); plus the S63-BLV internal-acoustic surface (T=0.112).
- **THERMODYNAMIC surfaces** (gradient-defined κ from a_n^{ζ} moments, NOT Mach-1 crossings): a₂ kinematic (κ=457.66, T=72.8); a₄ condensation/spectral-processing (κ=47.61, T=7.578). Both well-defined, both positive, neither sonic (`no_exit_horizon=True`).

The two-stage emission inherited from Volovik #27 (Painlevé-Gullstrand ³He-A) is the parent structure: **stage 1** = a SONIC horizon that defines the kinematic temperature (`T_Hawking = ℏ|v'|_{r_h}/2π` at the `|v|=c` surface — the scalar entry, a₂, T=72.8); **stage 2** = interior pair-creation/Andreev spectral-processing (#27 §F: "more important than Hawking radiation itself") that sets the OBSERVED spectrum (the a₄ Pöschl-Teller barrier, T_compound=7.578). The framework's two surfaces map one-to-one onto Volovik's two stages. I accept this fully — it is the substrate-correct, inherited (parent→child, not analogy) structure, and it dissolves the R1 a₂-vs-a₄ "which is THE temperature" dispute by showing both enter, at different stages, at different surfaces, of different kinds. **Locked: surface-taxonomy (3 KINDS) + two-stage #27 emission is the shared resolution.**

**C-R3-3 — The verdict-status residual is settled: W4-1 is PASS, with a genuine exit-flank INFO-graze sensitivity (sub-percent ripple, ε ≳ 0.20%) that the constant-c scan could not see. Closed by the CC1/VOL3 group-velocity scan; never a RE-OPEN.** I verified Sage-exact that a fractional c_BLV(τ) ripple of `ε = 0.94e-3/(2·0.485²) = 0.001998 = 0.1998%` on the exit flank suffices to move `disc_far_max = −1.936988e-03` (W4-1 LIVE `5d1ac75a…`) across the `GRAZE_INFO_CEIL = 1e-3`. This grazes D toward zero FROM BELOW; a full second crossing still needs the 11.46× (scalar) / 244.86× (condensate) overshoot the finite peak Mach (13.735) and the speed hierarchy forbid. So the graze is an INFO annotation on near-extremality, not a path to a second zero. **Converged: the single discriminating output the CC1 scan must report is `disc_far_max` per channel; the predicted outcome is PASS-ASYMMETRIC (N_zeros=1 in every channel), with at most an exit-flank INFO-graze in the scalar channel.** The condensate channel is supersonic-everywhere (Mach ~245–294), so it cannot graze.

**C-R3-4 — All four of my R2 concessions stand confirmed by volovik's R2 (C-R2-V1..V5): finite impedance floor (v_B2 = 0.0227, not 0), channel-tagged "one sonic horizon," the 9.61 κ-ratio is a two-channel gradient ratio carrying channel-but-not-sonic information, and the finite peak Mach bounds the c_BLV(τ) fold-collapse OUT.** There is nothing left to negotiate on these. The c_BLV(τ) question is bounded — NOT "wide open" — to a ~0.2%-sensitive exit-flank ripple by the finite, directly-computed peak Mach. The impedance lock pins v_B2 to `1/(πρ_B2) = 0.318318/14.023 = 0.0226987` M_KK (`Z = 14.023250 × 0.022699323 = 0.318318 = 1/π`, Sage; matches 1/π to 8.4e-6). My one bookkeeping retraction stands: I should not have cited both Z_wall=1/π and atlas-07 "Z=1.016" as interchangeable (they differ ~3.19×); the rho-pinned Z=1/π form with v_B2=0.0227 is the one the CC1 scan uses.

### DISSENT

**Only ONE genuine residual remains, and I want to be precise about whether it needs a gate or is a reconcilable labeling difference. It is the latter — but with a specific empirical pre-condition.**

**D-R3-1 — On volovik's surviving D-R2-V1 ("the OBSERVED relic spectral temperature is the a₄ value 7.578, and the a₂ value 72.8 is NOT a temperature any observed quantum carries"): I converge on the OBSERVABLE-TEMPERATURE conclusion but hold a narrower form of the asymmetry, and the difference is genuinely reconcilable — it is a labeling refinement, not a substrate disagreement, contingent on volovik's own F1 falsifier returning the expected NULL.** Let me separate what is settled from what is the residual.

*Settled (I converge):* the radiated/observed quanta are condensate-channel BdG modes (W4-3's B2[0..3], B3[0..2] over ω∈[0.819741, 1.063714]); their spectrum is shaped at the a₄ Pöschl-Teller interior-processing edge (transmitted_fraction=0.511872 — the barrier DEMONSTRABLY reshapes ~49%, so the #27-§F interior stage is active, bounding volovik's own F2 the right way). The OBSERVED relic spectral temperature is the a₄ value, 7.578 M_KK. I do not dispute this. volovik's sharpening from "the relic feels the a₄ sonic horizon" (RETRACTED in C-R2-V1) to "the relic is spectrally PROCESSED at the a₄ edge" is substrate-correct and #27-§F-anchored.

*The residual (where I hold a narrower form):* volovik's D-R2-V1 makes the asymmetry CATEGORICAL — "a₂ (72.8) is NOT a temperature any observed quantum carries, because no observed quantum is a scalar-mode quantum." I agree this is the EXPECTED end-state, but I hold that this is a claim about the ABSENCE of a scalar-channel observable squeeze branch, and that absence is exactly what volovik's own F1 falsifier is designed to test — it is NOT yet established. The substrate-correct statement, stated at the precision the current evidence supports, is the COMPOSITE (my E-R2-3, which volovik accepted as "substrate-correct and inherited" in A-V2):

- a₂ (T=72.8, scalar sonic entry) sets the KINEMATIC CARRIER temperature — the rate at which the modulus fabric itself transits its `v=c_BLV` surface. This is the stage-1 (#27 sonic-horizon) temperature.
- a₄ (T=7.578, condensate interior-processing edge) sets the OBSERVED SPECTRAL temperature of the relic. This is the stage-2 (#27 §F interior-processing) temperature.

The two are NOT co-equal candidates for "the Hawking temperature" (R1 framing, dissolved) — they are two stages of one composite emission at two surfaces of two kinds. On THAT, volovik and I are fully converged (A-V2: "the composite reading is substrate-correct and inherited, not a compromise").

The ONLY remaining difference is whether to ASSERT NOW that a₂ is observationally invisible (volovik's categorical form) or to HOLD it pending F1 (my form). This is genuinely reconcilable and does NOT need a NEW gate to settle the WORKSHOP — but it is contingent on a pre-registered computation:

- volovik's F1 (D-R2-V1): scan for a scalar-channel observable squeeze branch near the 72.8 M_KK scale (an order of magnitude above the W4-3 condensate squeeze support ω∈[0.82,1.06]). Its ABSENCE confirms volovik's categorical asymmetry (a₂ carries no observed quantum); its PRESENCE breaks it (a₂ IS observationally carried, and the composite is genuinely two-temperature-observable).

My structural prediction matches volovik's: F1 returns NULL (the substrate produces the relic squeeze in the condensate channel; the scalar-mode "carrier" is the fabric's own transit, which IS space — there is no external quantum that carries the fabric's transit temperature, per `phononic-framing.md` "the fabric is not IN space"). So I EXPECT volovik's categorical form to be vindicated. But until F1 returns, the WORKSHOP-LOCKABLE statement is the composite (a₂=kinematic-carrier stage, a₄=observed-spectral stage), with the categorical "a₂ observationally invisible" form tagged as the EXPECTED-NULL outcome of F1, not an established result. **This is a labeling-precision residual, not a substrate disagreement: we agree the observed relic temperature is 7.578 (a₄); we agree a₂ (72.8) is kinematic; the open point is solely whether "a₂ carries no observed quantum" is ASSERTED (volovik) or HELD-pending-F1 (sp). It is reconcilable in the verdict text by stating the composite as the locked structure and F1 as the pre-registered discriminator of the categorical refinement.** It does not block the C1 verdict, which is over-determined independently of which temperature the relic carries.

**D-R3-2 — A precision-flag, not a dissent, on volovik's D-R2-V2 ("a₄ is the ACTIVE spectral-processing edge, not a passive decoherence slice"): I accept the upgrade, with one geometric caveat for the registry text. The a₄ κ_exit=47.61>0 is white-hole-SIGN, but it is NOT a surface gravity in the Visser `κ=½∂_n(c²−v²)` sense at a Mach-1 crossing — it is the a₄ CONDENSATION-gradient κ at a surface the flow crosses freely (no_exit_horizon=True).** volovik is right that "thermodynamic edge" understates a surface with positive κ and a demonstrably-filtering Pöschl-Teller barrier — it is the active stage-2 processing surface, and the registry should say so (the #27-§F interior stage). My only caveat: the registry text must not let "active surface with positive κ" slide back toward "sonic horizon," because the κ at a₄ is a condensation-moment-gradient quantity (κ_exit from a_4_FW_zeta=1350.72), NOT a velocity-gradient at a v=c surface. The two κ's of the condensate channel are distinct: the THERMODYNAMIC κ (a₄, on-trajectory, T=7.578, active spectral processing) and the SONIC κ (the pre-genesis v=c_B2 surface, off-trajectory, T defined by ℏ|v'|/2π there). volovik agreed this in C-R2-V2 ("these are different κ's of the same channel"). I am only flagging that "active" must be tagged "active-THERMODYNAMIC-spectral-processing," never "active-sonic," to keep the taxonomy clean for downstream consumers. This is a wording pin, not a disagreement — volovik already conceded the substance.

### EMERGENCE

**The workshop's durable joint output is a surface-taxonomy of the substrate transit, anchored to Volovik #27 two-stage analog-Hawking physics, that re-frames C1 from a scalar yes/no into a structural map of the transit's surfaces. Three pieces, all jointly authored across the six turns.**

**E-R3-1 — THE TRANSIT SURFACE-TAXONOMY (the central durable result, jointly authored: my Penrose causal-structure framing × volovik's analog-gravity-per-species principle).** The substrate transit carries surfaces of THREE KINDS, and the C1/V.6 confusion was the conflation of the first two. This is the canonical statement for the registry and the §6.2 ledger:

| KIND | Definition | Instances (τ, T, κ) | On post-genesis trajectory? | Causal role |
|:-----|:-----------|:--------------------|:----------------------------|:------------|
| **SONIC (kinematic)** | Mach-1 crossing `v = c_channel`; one per quasiparticle branch (Jacobson-Volovik) | scalar `v=c_BLV` at τ₀=0.112466, κ=+18.52 | YES (the only post-genesis sonic surface) | white-hole entry horizon; bounds supersonic interior |
| | | condensate `v=c_B2`, Mach_B2∈[244.86,293.79] post-genesis | NO (pre-genesis, off-diagram) | entire observable universe is interior to it for the radiating species |
| | | S63-BLV internal-acoustic, T=0.112 | YES (internal) | fiber acoustic horizon |
| **THERMODYNAMIC (kinematic-carrier)** | a₂ moment-gradient κ; NOT a Mach-1 crossing | a₂: κ=457.66, T=72.8 | YES (co-located with scalar entry sonic surface) | stage-1 kinematic-carrier temperature (#27 sonic-horizon stage) |
| **THERMODYNAMIC (spectral-processing)** | a₄ condensation-gradient κ; Pöschl-Teller interior barrier | a₄: κ=47.61, T=7.578 | YES (exit flank; flow crosses freely, no_exit_horizon) | stage-2 active spectral-processing; sets OBSERVED relic temperature (#27 §F interior stage) |

The W4-2 "3 surfaces" are correctly read with this taxonomy: 2 thermodynamic (a₂, a₄) + 1 internal-sonic (S63-BLV), with the scalar-entry sonic surface co-located with a₂ on-trajectory and the condensate-sonic surface off-trajectory pre-genesis. C1, re-framed: "there is exactly ONE post-genesis sonic surface (the scalar entry), it is white-hole, and there is NO future-trapped sonic exit." That survives all six over-determination walls untouched.

**E-R3-2 — THE TWO-STAGE COMPOSITE EMISSION, parent-anchored to Volovik #27 (jointly authored: my composite-emission reading E-R2-3 × volovik's #27-§F corpus check E-R2-V1).** The substrate's Hawking-analog relic emission is NOT a single-horizon, single-temperature process. It is the inherited ³He two-stage structure:

```
STAGE 1 (sonic, kinematic):     scalar entry horizon v=c_BLV (τ₀=0.1125, κ=+18.52)
                                 → a₂ kinematic-carrier temperature T=72.8 M_KK
                                 [#27: T_Hawking = ℏ|v'|_{r_h}/2π at the |v|=c surface]
            ↓ (the white-hole outflow the condensate quanta ride OUT on)
STAGE 2 (thermodynamic, spectral): a₄ condensation/decoherence edge
                                 → Pöschl-Teller potential barrier (transmitted_fraction=0.512)
                                 → a₄ OBSERVED spectral temperature T_compound=7.578 M_KK
                                 [#27 §F: interior pair-creation/Andreev "more important
                                  than Hawking radiation itself"; sets what is OBSERVED]
```

The durable insight: the framework did not invent a two-surface emission; it INHERITED the ³He two-stage analog-Hawking structure (parent→child, `3HeB-inheritance-canonical.md`), where the kinematic temperature (stage 1, sonic) and the observable temperature (stage 2, interior processing) are genuinely DIFFERENT numbers at DIFFERENT surfaces of DIFFERENT kinds. This is the substrate reason WHY there are two temperatures, and it is the reason the R1 "which is THE Hawking temperature" dispute was malformed: both enter, neither alone is "the" temperature.

**E-R3-3 — THE PENROSE DIAGRAM OF THE TRANSIT (my causal-structure deliverable, now fully specified by the convergence; carry-forward candidate for a registry figure).** The convergence fixes every element of the canonical conformal diagram of the genesis-to-now transit. I specify it here so volovik's verdict can promote it to a registry figure if the workshop warrants:

- ONE past null surface (45°): the scalar entry sonic horizon (κ=+18.52>0, white-hole), on the ℐ⁻ side, opening into an unbounded supersonic expulsion region toward ℐ⁺.
- INTERIOR codim-1 slices (NOT horizons — geodesics cross freely, `no_exit_horizon=True`): the a₂ (T=72.8, co-located with the entry null surface) and a₄ (T=7.578, exit-flank, the Pöschl-Teller processing edge) thermodynamic surfaces, drawn as NESTED interior slices inside the supersonic region, not as null lines.
- The genuine censored singularity (W4-5): at τ→∞ at future timelike infinity **i⁺ along the SU(2) block** (timelike-character, cd_C2/U1=2.582/1.291 spacelike) — NOT a spacelike singularity bounding the top, and NOT a symmetric throat.
- The condensate-channel sonic horizon (v=c_B2, Mach-294): OFF the genesis-to-now diagram, at the FAR PAST — meaning for the radiating species the entire drawn region is white-hole interior ("we live inside the white hole," E-R2-V2, with the #27 §C–D reversed-population interior = the GGE inverted distribution).
- NO symmetric throat, NO i⁰-bounded subsonic→supersonic→subsonic sandwich.

The diagram is over-determined as asymmetric by all six walls (C-R3-1). I will draw it canonically via the `/penrose-diagram` skill and save to `sessions/archive/session-95/figures/` (or `figures/penrose/transit-surface-taxonomy.tex`) IF volovik's verdict promotes it — I do not draw it speculatively here, per the deconfliction mandate and to leave the promotion decision to the verdict turn.

**E-R3-4 — THE §6.2 LEDGER KIND-COLUMN (the documentation payload, routed to Doc-routing-PENDING + the w4 WP constraint-map; I do NOT edit `phonic-exflation-equation.md`).** The taxonomy has a direct consequence for the held §6.2 analog-T ledger, converging with volovik's E-R2-V3. The W4-2 3-surface ledger currently lists three temperatures (72.8 / 7.578 / 0.112) WITHOUT a KIND tag — which is exactly what let the dropped V.6 clause conflate thermodynamic with sonic surfaces. The substrate-correct ledger needs a KIND column:

- **a₂ → THERMODYNAMIC-kinematic** (scalar-channel moment-gradient κ; sets stage-1 carrier kinematics; co-located with the scalar entry SONIC surface at τ₀).
- **a₄ → THERMODYNAMIC-spectral** (condensation moment-gradient κ; the #27-§F active interior-processing edge; sets the OBSERVED relic spectral temperature 7.578).
- **S63-BLV → SONIC** (the genuine on-trajectory v=c_BLV internal-acoustic Mach-1 surface, T=0.112).

And the §6.2 narrative should state the relic temperature is the a₄ value (7.578) as the spectral/interior-processing temperature, with a₂ (72.8) as the kinematic-carrier temperature — presented as TWO STAGES of one composite (E-R3-2), NOT as two competing Hawking temperatures, NOT as two sonic horizons. The dropped V.6 "two distinct horizons" clause STAYS DROPPED if read as "two sonic horizons of one flow" (the bounce reading, correctly closed); a channel-tagged "two thermodynamic surfaces + one post-genesis sonic surface" is the substrate-correct replacement. (On the one labeling residual D-R3-1: the ledger should record 7.578 as the observed spectral temperature; whether a₂ is annotated "kinematic-carrier, observationally invisible" categorically or "kinematic-carrier, observable-visibility HELD-pending-F1" is the precision choice for volovik's verdict — I recommend the latter until F1 returns.)

### QUESTIONS

Three final sharpenings for volovik's R3 verdict. These are not new disagreements — they are precision pins the verdict should resolve.

**SP-Q-R3-1 (the one labeling residual — does the verdict lock the COMPOSITE or the CATEGORICAL form?).** We are fully converged that the OBSERVED relic spectral temperature is the a₄ value (7.578) and that a₂ (72.8) is the kinematic-carrier stage. The only open precision (D-R3-1): do you want the verdict to ASSERT the categorical "a₂ carries no observed quantum" NOW, or to lock the COMPOSITE (a₂=stage-1 kinematic, a₄=stage-2 observed-spectral) with the categorical form tagged as the EXPECTED-NULL outcome of your F1 falsifier (scan for a scalar-channel squeeze branch near 72.8)? I recommend the latter — it is the precision the current evidence supports, and it makes F1 the clean pre-registered discriminator. If you prefer the categorical form, what established result (not F1's expected NULL) licenses asserting the ABSENCE of a scalar-channel observable branch before F1 runs? (I expect we agree on the substance and only need to pick the verdict-text form.)

**SP-Q-R3-2 (does the surface-taxonomy (E-R3-1) go to the registry as a permanent structural result, and at what status?).** The 3-KIND taxonomy (SONIC / THERMODYNAMIC-kinematic / THERMODYNAMIC-spectral) is jointly authored and over-determined. Do you read it as registry-eligible — a permanent structural statement of the transit's surface structure — or as a workshop-internal reconciliation? If registry-eligible, I would tag it a STAGE-1-CANDIDATE joint theorem (cross-axis: my causal-structure axis + your superfluid-analog axis) requiring the Stage-2 two-reviewer independent-verify per `joint-theorem-promotion.md`, since its clauses straddle both our axes. Your call on whether it warrants that pathway or lands as a §6.2-ledger structural note only.

**SP-Q-R3-3 (the Penrose diagram (E-R3-3) — promote to a registry figure?).** I have fully specified the conformal diagram (one scalar entry null surface; nested interior thermodynamic slices; censored singularity at i⁺ along SU(2); condensate sonic horizon off-diagram pre-genesis; no throat). Do you want me to draw it canonically via the `/penrose-diagram` skill as a registry figure (saved to `sessions/archive/session-95/figures/`), or is the specification in E-R3-3 sufficient for the §6.2 doc-routing? I will draw it only if your verdict promotes it (deconfliction: I leave the promotion to your turn).

---

## Round 3 — volovik: Cross-Synthesis (convergence + verdict)

### CONVERGENCE

**This is the convergence turn. I lock the shared position, accept sp's three R3 pins, and carry NO new dissent — the one residual is a verdict-text labeling choice I resolve below, not a substrate disagreement.**

**C-R3-V1 — I lock C-R3-1 in full: C1 → ASYMMETRIC is a structural theorem of the W4 geometry, over-determined at SIX independent levels, not a verdict-under-test.** sp's six-wall enumeration (C-R3-1) is correct and I add nothing to weaken it. I confirm each wall is independently load-bearing — the bounce/symmetric-two-horizon reading must breach ALL six, and a substrate-bounded c_s(τ) breaches none: (1) scalar speed-hierarchy (factor-11.46); (2) channel-monotone strengthening (factor-294 in the soft channel — the wall is monotone in the FORBIDDING direction, my Re:SP2); (3) monotone-dS/dτ (+58,673 one-signed, all-negative off-Jensen Hessian S76); (4) irreversibility / one-way Kibble-Zurek quench (the substrate-deepest — `ORDERED-VEIL-SUBSTRATE-CLOCK` FAIL diabatic freeze-out + `HAWKING-GGE-PURITY` Tr ρ²=1, S_ent=0 this session); (5) 12D censorship / monotone-a_eff (W4-5 PASS `9ffb4aea…`, W4-4 a_eff'(fold)=+6.82e-2>0); (6) reversed-population interior (Volovik #27 §C–D, "T=−0" = the GGE inverted distribution, my E-R2-V2). The substrate-physics ordering I want on record: wall (4) is logically prior to all the kinematic walls — it is an entropy-arrow statement that holds BEFORE any (c²−v²) bookkeeping. **The acoustic white hole is the laboratory analog OF this irreversible substrate quench, not the reverse** (substrate IS, laboratory IN — `phononic-framing.md`): the diagram is asymmetric because the quench is irreversible, not the quench irreversible because the diagram is asymmetric. **C1 → ASYMMETRIC STANDS unconditionally on the symmetric/asymmetric axis.**

**C-R3-V2 — I lock the c_s-softening resolution: the challenge is RESOLVED (different channel), N_zeros=1 is STRENGTHENED (channel-monotone, factor-294), and the exit-flank graze is INFO-only, never RE-OPEN.** The challenge I was briefed to steelman — "c_BLV = 0.485 may soften to zero at the DOS singularity and admit a far-side second zero" — is resolved at every level: the softening is the B2 condensate band-edge mode (`v_g_B2_fold = 0.022699323` M_KK, S94 canonical, rho-pinned to `1/(πρ_B2)`, Z = 1/π exactly), NOT the scalar transit mode c_BLV; the naive fold-collapse premise is bounded OUT by the finite directly-computed peak Mach 13.735 (C-R2-V4); and what survives — a sub-percent (ε ≳ 0.20%) exit-flank ripple that could move `disc_far_max = −1.936988e-03` across `GRAZE_INFO_CEIL = 1e-3` — moves the verdict to PASS-with-INFO at most. I confirm Sage-exact `ε = 0.94e-3/(2·0.485²) = 0.001998`. **N_zeros = 1 survives any substrate-derived c_s(τ), and is STRONGER than the brief's symmetric reading anticipated: every softer channel deepens the supersonic interior.** The c_s-softening side's durable payload was never a symmetric C1 — it was (a) the surface-taxonomy and (b) the a₄-observed-temperature claim, both below.

**C-R3-V3 — I lock the THREE-KIND surface-taxonomy + the two-stage ³He-#27 composite emission as the shared resolution (sp's C-R3-2, E-R3-1, E-R3-2).** This is the workshop's durable joint output and I accept it without reservation. The R1 confusion (and the dropped transit V.6 "two distinct horizons" clause) was the conflation of two distinct KINDS of surface; the convergence keeps every surface but tags each by kind — SONIC (Mach-1 crossing `v=c_channel`, one per quasiparticle branch by Jacobson-Volovik); THERMODYNAMIC-kinematic (a₂ moment-gradient κ, co-located with the scalar entry sonic surface); THERMODYNAMIC-spectral (a₄ condensation-gradient κ, the #27-§F active interior-processing edge). The two-stage emission (stage 1 = scalar sonic entry, a₂, T=72.8; stage 2 = a₄ Pöschl-Teller interior processing, T=7.578) is the inherited ³He structure of Volovik #27 — parent→child, NOT analogy (`3HeB-inheritance-canonical.md`). I emphasize the inheritance direction for the registry: the framework did not invent a two-surface emission; it INHERITED Volovik's two-stage analog-Hawking structure, where the kinematic temperature (stage 1, sonic) and the observable temperature (stage 2, interior processing) are genuinely DIFFERENT numbers at DIFFERENT surfaces of DIFFERENT kinds. That parent-physics anchor is WHY there are two temperatures, and it is why the R1 "which is THE Hawking temperature" dispute was malformed.

**C-R3-V4 — I accept sp's D-R3-2 wording pin in full: a₄ is "active-THERMODYNAMIC-spectral-processing," never "active-sonic."** sp is right to flag that "active surface with positive κ" must not slide back toward "sonic horizon." The a₄ κ_exit=47.61>0 is the condensation-moment-gradient κ (from `a_4_FW_zeta=1350.72`), at a surface the flow crosses freely (`no_exit_horizon=True`), NOT a Visser velocity-gradient at a v=c crossing. The condensate channel carries TWO distinct κ's: the THERMODYNAMIC κ (a₄, on-trajectory, T=7.578, active spectral processing) and the SONIC κ (the pre-genesis v=c_B2 surface, off-trajectory). I conceded this substance in C-R2-V2; I accept sp's wording pin that the registry/ledger text must tag "active" as "active-THERMODYNAMIC-spectral-processing" to keep the taxonomy clean for downstream consumers. Locked.

### DISSENT

**No new dissent. The single residual carried into this turn (sp's D-R3-1: the COMPOSITE vs CATEGORICAL form of the a₄-observed-temperature claim) I RESOLVE here in sp's favor — it is a labeling-precision choice, and the precision the current evidence supports is the COMPOSITE form. I do NOT assert the categorical form now.**

**D-R3-V1 — I adopt the COMPOSITE form (answering SP-Q-R3-1; this withdraws the categorical framing of my D-R2-V1).** sp's D-R3-1 is correct that my D-R2-V1 made the asymmetry CATEGORICAL — "a₂ (72.8) is NOT a temperature any observed quantum carries, because no observed quantum is a scalar-mode quantum." sp converges on the OBSERVABLE-TEMPERATURE conclusion (the observed relic spectral temperature is the a₄ value, 7.578) but holds the narrower form: the categorical claim is a statement about the ABSENCE of a scalar-channel observable squeeze branch, and that absence is exactly what my own F1 falsifier is designed to TEST — it is not yet established. **sp is right, and converging is the goal, so I adopt the composite form as the workshop-lockable statement:**

- a₂ (T=72.8, scalar sonic entry) sets the **stage-1 KINEMATIC-CARRIER** temperature — the rate at which the modulus fabric transits its own `v=c_BLV` surface (the #27 sonic-horizon stage `T_Hawking = ℏ|v'|_{r_h}/2π`).
- a₄ (T=7.578, condensate interior-processing edge) sets the **stage-2 OBSERVED-SPECTRAL** temperature of the relic (the #27 §F interior pair-creation / Andreev stage, "more important than Hawking radiation itself").

These are two stages of one composite emission at two surfaces of two kinds — NOT two competing candidates for "the Hawking temperature." On that, sp and I are fully converged (sp accepted this as "substrate-correct and inherited, not a compromise" in my A-V2). **I do NOT have decisive substrate grounds to assert the categorical form before F1 runs.** The grounds I cited in D-R2-V1 ("the fabric IS space, so no external quantum carries its transit temperature," `phononic-framing.md`) are a structural EXPECTATION — they are exactly why I predict F1 returns NULL — but a structural expectation is not an established absence, and pre-registration discipline (`epistemic-discipline.md`) forbids asserting a falsifier's expected outcome as a settled result before the falsifier fires. So: **the verdict locks the COMPOSITE structure; the categorical "a₂ carries no observed quantum" form is tagged as the EXPECTED-NULL outcome of the F1 falsifier**, which becomes the clean pre-registered discriminator (carry-forward). This is the precision the current evidence supports, and it is sp's recommended form. Converged — there is no substrate disagreement left, only the verdict-text form, and I have picked the form that converges.

**Why the composite, not the categorical, is the honest lock (the substrate reason, stated for the record).** The composite claim is FORCED by the evidence on disk; the categorical claim is FORCED only by a structural expectation. The evidence: the radiated quanta ARE condensate-channel BdG modes (W4-3's B2[0..3], B3[0..2] over ω∈[0.819741, 1.063714]); the a₄ barrier DEMONSTRABLY reshapes them (`transmitted_fraction=0.511872`, `band_narrows=True`, `spread_ratio=0.821732` — so the #27-§F interior stage is active, bounding F2 the right way); therefore the observed relic spectral temperature is unambiguously the a₄ value, 7.578. That is composite-locked. The categorical EXTRA step — that a₂'s 72.8 is carried by NO observed quantum — requires the NULL of F1 (no scalar-channel observable squeeze branch near the 72.8 scale). I expect that NULL; I do not assert it. The distinction matters because F1's presence-outcome, however unlikely, would make the emission genuinely two-observable-temperature, and a categorical verdict-text would then be a pre-registration violation. The composite form is robust to BOTH F1 outcomes; the categorical form is hostage to one.

### EMERGENCE

**E-R3-V1 — I accept the surface-taxonomy as registry-eligible and queue it as a STAGE-1-CANDIDATE joint theorem (answering SP-Q-R3-2), with a corpus check that confirms the cross-axis claim is parent-physics-anchored, not framework-internal.** sp asks whether the 3-KIND taxonomy goes to the registry. My answer: YES, it is registry-eligible — it is a permanent structural statement of the transit's surface structure, over-determined and jointly authored across both our axes (sp's Penrose causal-structure axis × my superfluid-analog-per-species axis). Its clauses straddle both axes, so per `joint-theorem-promotion.md` it requires the Stage-2 two-reviewer independent-verify pathway. **Per the deconfliction mandate and `joint-theorem-promotion.md` (Stage-1 registration is a NEXT-SESSION step), I do NOT write it into `permanent-results-registry.md` now. I draft it as a STAGE-0 workshop-internal candidate in the Wrap-Up (clause-by-clause cross-axis attribution) and queue its S96 Stage-1 registration as a carry-forward.** The corpus check that licenses the cross-axis claim: I confirmed against Volovik #27 (Painlevé-Gullstrand ³He-A, gr-qc/9901077) that the parent ³He physics carries BOTH the per-species sonic-horizon principle (each quasiparticle branch sees `v = c_species`; §32 / Jacobson-Volovik 1998) AND the two-stage emission (sonic horizon defines `T_Hawking = ℏ|v'|_{r_h}/2π`; interior pair-creation/Andreev §F sets the observable). The taxonomy is the child realization of both — so the joint theorem's superfluid-axis clauses are inherited, not invented, which is exactly the kind of cross-axis content the Stage-2 pathway is built to verify.

**E-R3-V2 — I promote the Penrose diagram to a registry-FIGURE candidate (answering SP-Q-R3-3), as a carry-forward — sp draws it next session, not now.** sp has fully specified the conformal diagram (one scalar entry null surface κ=+18.52>0; nested interior thermodynamic slices a₂/a₄ that geodesics cross freely; censored singularity at i⁺ along the SU(2) block; condensate sonic horizon OFF-diagram pre-genesis; no symmetric throat). The specification in E-R3-3 is complete and substrate-correct, and I endorse promoting it to a registry figure — it is the canonical causal-structure deliverable of the workshop and the visual statement of the six-wall over-determination. **But per the deconfliction mandate I do NOT have it drawn this turn; I queue it as a carry-forward figure candidate** (sp draws it via `/penrose-diagram` to `sessions/archive/session-95/figures/`, S96). The specification in E-R3-3 is sufficient for the §6.2 doc-routing in the interim — the figure is an enhancement, not a blocker, for the ledger KIND-column reframe.

**E-R3-V3 — The convergence's cross-domain payload, stated as a substrate-IS / laboratory-IN mapping for downstream consumers (a Volovik-corpus contribution to the joint output).** The durable structural content, in the framing law's direction (substrate IS, laboratory IN): the substrate transit IS the irreversible first-order Kibble-Zurek quench through the van Hove fold; its surface-taxonomy and two-stage emission are substrate-intrinsic structures; the ³He-A acoustic white hole of Volovik #27 is the laboratory realization IN which those structures were first written down. The map is one-to-one (parent #27 → child transit): the `|v|=c` sonic horizon ↔ the scalar entry surface (a₂, T=72.8, stage-1 kinematic); the §F interior pair-creation/Andreev processing ↔ the a₄ condensation edge (T=7.578, stage-2 observed-spectral, Pöschl-Teller); the behind-horizon reversed population "T=−0" (§C–D) ↔ the post-transit GGE inverted distribution (`HAWKING-GGE-PURITY` pure Bogoliubov product state). This is the substrate-strongest form of the result and the reason it is over-determined: it is not a model the framework chose, it is the structure ³He exhibits in the laboratory, inherited by the substrate that ³He is a projection of. I record this mapping in the Wrap-Up as the joint theorem's superfluid-axis attribution.

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | (a) c_s constant vs critical-softening through the fold | SP1, Re:SP1, VOL1, C-R2-3, C-R3-V2 | **Converged** | The softening is a DIFFERENT channel: the B2 condensate band-edge mode (`v_g_B2_fold = 0.022699323` M_KK, rho-pinned `1/(πρ_B2)`, Z=1/π exactly), NOT the scalar transit mode c_BLV=0.485. The naive "c_BLV→0 at the DOS singularity" premise is mis-assigned; the finite directly-computed peak Mach 13.735 bounds a fold-collapse of c_BLV OUT. The impedance floor is FINITE (0.0227), not zero — the S40/Tesla "v_B2=0" is the unregularized cusp idealization superseded by S94. Surviving residual: a sub-percent (ε≳0.20%) exit-flank c_BLV(τ) ripple, INFO-graze-only. |
| 2 | (b) N_zeros=1 robustness to non-constant c_s(τ) | SP2, Re:SP2, VOL2, C-R2-1, C-R3-V2 | **Converged** (strengthened) | N_zeros=1 is robust across the ENTIRE four-speed hierarchy, not just for c_s≤O(c_BLV). The wall is channel-MONOTONE in the forbidding direction: the flow is the same in every channel and c_BLV=0.485 is the largest transit-referencing speed, so every softer channel only DEEPENS the supersonic interior (B2-channel fold Mach = 6.66875/0.022699323 = 293.79; a far-side second zero needs a 244.86× overshoot over the rho-pinned floor — Sage-exact). A second zero requires c_s→v_exit≈5.56 M_KK, an 11.46× (scalar) / 244.86× (condensate) overshoot the spectrum has no reservoir for. The one discriminating output the closing scan must report is `disc_far_max` per channel. |
| 3 | (c) C1→ASYMMETRIC verdict: STANDS / CONDITIONAL / RE-OPENS | SP3, Re:SP3, C-R3-1, C-R3-V1; E-R3-1/2 (Emerged) | **Converged → STANDS** (over-determined; durable structure Emerged) | **C1 → ASYMMETRIC STANDS unconditionally** on the symmetric/asymmetric axis — a structural theorem over-determined at SIX independent walls (scalar speed-hierarchy / channel-monotone / monotone-dS/dτ / irreversibility-quench / 12D-censorship-monotone-a_eff / reversed-population-interior). The bounce reading breaches none. The deepest wall is irreversibility (entropy-arrow, prior to all kinematics). **Emerged**: the THREE-KIND surface-taxonomy (SONIC / THERMODYNAMIC-kinematic / THERMODYNAMIC-spectral) + the two-stage Volovik-#27 composite emission (a₂ stage-1 kinematic-carrier T=72.8; a₄ stage-2 observed-spectral T=7.578) — jointly authored, inherited parent→child, registry-eligible as a STAGE-1-CANDIDATE joint theorem. The dropped V.6 "two distinct horizons" clause stays dropped (it conflated thermodynamic with sonic surfaces). One labeling residual (a₂-observability) locked to the COMPOSITE form, F1 the pre-registered discriminator. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

**Verdict-pin resolutions (sp's SP-Q-R3-1/2/3, settled this turn):**
- **SP-Q-R3-1 (composite vs categorical a₄-observed-T):** locked the **COMPOSITE** form (a₂=stage-1 kinematic-carrier; a₄=stage-2 observed-spectral, 7.578). The categorical "a₂ carries no observed quantum" is tagged the EXPECTED-NULL of falsifier F1, NOT asserted now (D-R3-V1).
- **SP-Q-R3-2 (surface-taxonomy → registry?):** YES, registry-eligible as a **STAGE-1-CANDIDATE joint theorem** (cross-axis); drafted STAGE-0 in the Wrap-Up; S96 Stage-1 registration is a carry-forward (NOT written to the registry now, per `joint-theorem-promotion.md`).
- **SP-Q-R3-3 (Penrose diagram → registry figure?):** YES, promoted to a **carry-forward figure candidate**; sp draws it via `/penrose-diagram` next session (NOT drawn this turn, per deconfliction).

## Remaining Open Questions

These are the genuinely open items the convergence leaves. Each is specific enough to become a computation gate or a registry action; none re-opens C1 (over-determined at six walls).

1. **The c_s(τ)=v_g(τ) exit-flank scan (MATH gate — settles the W4-1 PASS-vs-INFO graze).** W4-1 held c constant, so it could not see whether a substrate-DERIVED c_s(τ) moves the exit-flank graze `disc_far_max = −1.936988e-03` across `GRAZE_INFO_CEIL = 1e-3` (a sub-percent ε≳0.20% ripple suffices). Compute `c_s(τ) = |dω/dk|` from the S71 BdG dispersion for BOTH the scalar (c_BLV) and condensate (c_B2) branches across τ∈[0.05, 0.40] — using the S94 `s94_ds_gamma_e_resolution_vg_b2_trajectory.npz` τ-trajectory if it already carries `v_g(τ)` (the filename suggests it does), with `v_g_B2_fold = 0.022699323` as the rho-pinned minimum — re-run the W4-1 N_zeros scan on `D_channel(τ) = c_channel(τ)² − v(τ)²` per channel, and report N_zeros AND `disc_far_max` per channel. Predicted: N_zeros=1 in every channel; scalar channel may graze the INFO ceiling; condensate channel supersonic-everywhere (Mach∈[244.86, 293.79], cannot graze). Outcome is PASS-ASYMMETRIC or PASS-with-INFO — never RE-OPEN. (4-field spec in Carry-Forward Computations CF-1.)

2. **F1 — the scalar-channel observable-squeeze-branch falsifier (MATH gate — the pre-registered discriminator of the categorical a₂-observability claim).** The composite-locked verdict states the observed relic spectral temperature is the a₄ value (7.578); the categorical refinement ("a₂'s 72.8 is carried by NO observed quantum") is held pending F1. Scan the substrate excitation spectrum for an OBSERVABLE squeeze branch near the 72.8 M_KK scale — an order of magnitude above the W4-3 condensate-squeeze support ω∈[0.819741, 1.063714]. ABSENCE confirms the categorical asymmetry (a₂ is the kinematic carrier only, observationally invisible — the expected NULL); PRESENCE breaks it (the emission is genuinely two-observable-temperature). Both readers predict NULL. (4-field spec in CF-2.)

3. **The v(τ)-normalization reconciliation across S55 / S73A / W4-1 (registry/bookkeeping action — pin "the" Mach before §6.2 quotes it).** S73A reports exit Mach ~20.7 in c_BA units; W4-1/W4-2 report fold Mach 13.75 in c_BLV units. sp's A4 showed (Sage) that none of {v_fold=6.66875, v_term=26.545, v_exit≈5.558} gives 20.7 in c_BA units via a single rescale — "20.7 in c_BA units" implies v=8.26 M_KK, a FOURTH v-definition (likely a Jensen exit-window average of `dv/dτ = −(dS/dτ)/(M_ATDHFB·v)`). This is a v-DEFINITION difference, NOT a sound-speed inconsistency (which supports CC2: the sound speed for C1's discriminant is unambiguously c_BLV). Pin §6.2's canonical "Mach" = v_fold/c_BLV = 13.75 (fold-peak, scalar channel) and document S73A's 20.7 as a distinct exit-window v-normalization. Does not threaten N_zeros=1 (both deeply supersonic monotone). (4-field spec in CF-3.)

4. **Stage-1 registry registration of the THREE-KIND surface-taxonomy joint theorem (registry action, S96).** Per `joint-theorem-promotion.md`, the STAGE-0 candidate drafted in the Wrap-Up (cross-axis: sp causal-structure + volovik superfluid-analog) is registered as a STAGE-1-CANDIDATE in `permanent-results-registry.md` next session, then routed to a Stage-2 two-reviewer independent-verify (axis-distinct, NOT the originating authors, no workshop transcripts). (4-field spec in CF-4.)

5. **The Penrose diagram of the transit as a registry figure (figure action, S96).** sp's fully-specified conformal diagram (E-R3-3) is drawn via `/penrose-diagram` and saved to `sessions/archive/session-95/figures/transit-surface-taxonomy.tex`, then cited from the surface-taxonomy registry entry and the §6.2 ledger. (4-field spec in CF-5.)

6. **Subleading scalar↔condensate mode-mode coupling magnitude (OPEN — bounded, not yet computed; lower priority).** VOL1 §2 raised that c_BLV is a global a_n-moment functional whose τ-derivative is largest at the DOS spike, so a subleading mode-mode-coupling correction `δc_BLV/c_BLV ~ (coupling)·(condensate susceptibility)` is real in the parent ³He physics (density-sound ↔ gap-amplitude). D-R2-2 bounded the FOLD value out via the finite peak Mach; what is not computed is the EXIT-FLANK ripple magnitude from first principles (the BdG self-energy correction to the scalar branch). This is subsumed by OQ-1 operationally (the scan measures the net c_BLV(τ)), but a first-principles derivation of the coupling magnitude would convert the ε≳0.20% sensitivity threshold from a geometric bound into a predicted value. Not blocking; flagged for completeness.

## Doc-routing (PENDING)

**PENDING-W-1 — §6.2 white-hole-symmetry resolution + analog-T ledger KIND-column reframe.** This section records the disposition for the phonon-first doc-incorporation follow-up to apply to `sessions/framework/phonic-exflation-equation.md §6.2` VERBATIM. The curated master doc is OFF-LIMITS this round (owned by the concurrent doc-incorporation agent, who is HOLDING §6.2 white-hole-symmetry PENDING this workshop); the durable effect is mirrored to the w4 WP constraint-map. Both effects are stated precisely below.

**Effect 1 — the §6.2 white-hole-symmetry PENDING marker resolves to ASYMMETRIC (unconditional).** The §6.2 white-hole structure is the ASYMMETRIC one-horizon causal structure: ONE post-genesis sonic surface (the scalar entry horizon, `v=c_BLV` at τ₀=0.112466, κ_entry=+18.52>0, white-hole outflow) bounding an unbounded supersonic expulsion region toward ℐ⁺; NO future-trapped exit horizon; NO symmetric throat; NO bounce. This is a structural theorem over-determined at SIX independent walls (C-R3-1): scalar speed-hierarchy / channel-monotone / monotone-dS/dτ / irreversibility-quench / 12D-censorship-monotone-a_eff / reversed-population-interior. The dropped transit V.6 "two distinct horizons" STRENGTHEN clause STAYS DROPPED — it conflated two THERMODYNAMIC surfaces with two SONIC horizons. Direction of explanation (framing law): the §6.2 acoustic white hole is the laboratory analog OF the substrate's irreversible first-order Kibble-Zurek quench through the van Hove fold; the diagram is asymmetric BECAUSE the quench is irreversible (substrate IS, laboratory IN).

**Effect 2 — the §6.2 analog-T ledger gets a KIND column (the W4-2 3-surface ledger reframe).** The W4-2 ledger currently lists three temperatures (72.8 / 7.578 / 0.112) WITHOUT a KIND tag — which is exactly what let the dropped V.6 clause conflate thermodynamic with sonic surfaces. The substrate-correct ledger tags each row by KIND (verbatim values for the follow-up to transcribe):

| Surface | T (M_KK) | κ | KIND tag | Causal role |
|:--------|:---------|:--|:---------|:------------|
| a₂ (kinematic-entry) | 72.8 (corpus 72.8; W4-2 row 1 T=72.8383, dev 0.0005) | 457.66 | **THERMODYNAMIC-kinematic** | stage-1 kinematic-CARRIER temperature; co-located with the scalar entry SONIC surface (v=c_BLV) at τ₀; the #27 sonic-horizon stage `T_Hawking=ℏ\|v'\|_{r_h}/2π` |
| a₄ (condensation-exit) | 7.578 (corpus 7.578; W4-2 row 2 T=7.5781, dev 0.0000) = OBSERVED relic spectral T | 47.61 | **THERMODYNAMIC-spectral** | stage-2 ACTIVE interior-processing edge (#27 §F; Pöschl-Teller barrier, transmitted_fraction=0.512); sets the OBSERVED relic spectral temperature. Tag "active-THERMODYNAMIC-spectral-processing", NEVER "active-sonic" (flow crosses freely, `no_exit_horizon=True`) |
| S63-BLV (internal-acoustic) | 0.112 (corpus 0.112; W4-2 row 3 T=0.112173, dev 0.0015) | 0.704805 | **SONIC** | the genuine on-trajectory v=c_BLV internal-acoustic Mach-1 surface |

**Effect 3 — the §6.2 narrative statement of the relic temperature (verbatim disposition).** The §6.2 narrative should state: the OBSERVED relic spectral temperature is the a₄ value (7.578 M_KK), as the SPECTRAL / interior-processing (stage-2) temperature; the a₂ value (72.8 M_KK) is the KINEMATIC-CARRIER (stage-1) temperature — presented as TWO STAGES of one composite emission inherited from Volovik #27 (parent→child, NOT analogy), NOT as two competing Hawking temperatures and NOT as two sonic horizons. The κ-ratio 9.6117 = κ_entry(a₂)/κ_exit(a₄) (corpus 9.61) is a two-CHANNEL gradient ratio (a₂↔scalar, a₄↔condensate) carrying channel information but NOT sonic-horizon information. **Labeling precision (locked COMPOSITE form):** the a₂ surface is annotated "kinematic-carrier, observable-visibility HELD-pending falsifier F1" — do NOT state categorically that a₂ is observationally invisible until F1 (scan for a scalar-channel observable squeeze branch near 72.8 M_KK) returns its expected NULL. F1 is the pre-registered discriminator (OQ-2 / CF-2).

**Status**: PENDING — for the phonon-first doc-incorporation agent to apply to `phonic-exflation-equation.md §6.2`. This workshop does NOT edit the master doc (deconfliction mandate). The follow-up agent applies Effects 1–3 verbatim.

## Wrap-Up — Workshop Impact Summary

### What Changed

Per `output-standards.md`, numerical revisions and structural changes are separated.

**(a) Numerical revisions**
- `v_B2(fold) = 0` (S40/Tesla idealization) → `v_g_B2_fold = 0.022699323` M_KK (S94 canonical, rho-pinned `1/(πρ_B2)`; impedance Z = 14.023250 × 0.022699323 = 0.31831 = 1/π exactly, Sage). The impedance floor is FINITE, not zero.
- B2-channel fold Mach pinned: `v_fold/v_g_B2 = 6.66875/0.022699323 = 293.79` (Sage-exact); exit-flank `v_exit/v_g_B2 = 244.86`. The second-zero overshoot in the soft channel is 244.86× over the rho-pinned floor (vs the scalar channel's 11.46×).
- Exit-flank graze sensitivity quantified: a fractional c_BLV(τ) ripple `ε = 0.94e-3/(2·0.485²) = 0.001998 = 0.20%` suffices to move `disc_far_max = −1.936988e-03` across `GRAZE_INFO_CEIL = 1e-3` (Sage-exact).
- Mach bookkeeping: S73A's "20.7 in c_BA units" implies v=8.26 M_KK, a FOURTH v-definition; none of {v_fold, v_term=26.545, v_exit≈5.558} reproduces it via single rescale (sp A4, Sage).

**(b) Structural changes**
- C1 → ASYMMETRIC promoted from a W4-1 verdict to a **structural theorem over-determined at SIX independent walls** (was: leaned-asymmetric S73A; one-gate PASS).
- The c_s-softening challenge reframed from "does c_BLV soften?" to **"WHICH channel carries the discriminant"** — resolved: the discriminant is built on the scalar channel (c_BLV), the softening is the condensate channel (c_B2). Branch-assignment, not constant-vs-variable, was the real question.
- N_zeros=1 robustness type-promoted: from "robust for c_s≤O(c_BLV)" (factor-11 scalar bound) to **"robust across the ENTIRE four-speed hierarchy"** (channel-monotone, factor-294) — a strengthening, not a re-statement.
- "ONE sonic horizon" → **THREE-KIND surface-taxonomy** (SONIC / THERMODYNAMIC-kinematic / THERMODYNAMIC-spectral): a dimensional reading change of the W4-2 "3 surfaces."
- Single-horizon single-temperature emission → **two-stage composite emission** inherited from Volovik #27 (a₂ stage-1 kinematic-carrier × a₄ stage-2 observed-spectral): an epistemic-type promotion (1 number → 2-stage composite at 2 surfaces of 2 kinds).
- The a₂-vs-a₄ "which is THE temperature" dispute DISSOLVED (it was malformed — both enter at different stages), with the surviving observable claim locked to the **COMPOSITE** form (categorical a₂-invisibility held pending falsifier F1).
- VOL1 §4 sonic re-pointing (a₂→a₄ Hawking SONIC temperature) RETRACTED as a category error (C-R2-V1) — corrected to a₄ = active interior-processing edge (#27 §F), not a sonic horizon.

### What Holds

- **C1 → ASYMMETRIC STANDS unconditionally** on the symmetric/asymmetric axis (W4-1 PASS `5d1ac75a…`, N_zeros=1, κ_entry=+18.52, `monotone_supersonic_exit=True`). Over-determined at six walls; the bounce reading breaches none.
- **N_zeros = 1** survives any substrate-derived c_s(τ) bounded by the four-speed hierarchy (the corpus contains no sound-speed reservoir at O(v_exit)≈5.56 M_KK).
- The **finite impedance lock** Z = 1/π at the van Hove fold (Landau S32; v_B2 pinned to the finite 0.0227, S94) — the softening is determined, not tunable.
- The **W4-2 3-surface analog-T ledger** (a₂ T=72.8 / a₄ T=7.578 / S63-BLV T=0.112), now KIND-tagged.
- The **observed relic spectral temperature is the a₄ value, 7.578 M_KK** (composite-locked; the a₄ Pöschl-Teller barrier demonstrably reshapes the condensate squeeze, `transmitted_fraction=0.512`).
- All four R2 concessions (C-R3-4): finite impedance floor, channel-tagged "one sonic horizon," the 9.61 κ-ratio as a two-channel gradient ratio, finite peak Mach bounding the c_BLV(τ) fold-collapse out.
- Parent→child inheritance from Volovik #27 (Painlevé-Gullstrand ³He-A): the two-stage analog-Hawking structure and the per-species sonic-horizon principle (NOT analogy — `3HeB-inheritance-canonical.md`).

### What Breaks or Strains

- **The dropped transit V.6 "two distinct horizons" STRENGTHEN clause** stays dropped (it conflated two THERMODYNAMIC surfaces with two SONIC horizons). Resurrecting it would require asserting either v-deceleration (breaches monotone-dS/dτ) or a c_s overshoot to O(v) (breaches the speed hierarchy) — both walls.
- **The S85 symmetric two-crossing** (τ_H±=0.18314/0.19686) is confirmed a tanh²-c_s-dip + constant-v MODEL ARTIFACT (W4-1 reproduced it exactly to certify it as a model property, not a physical feature). The back-fit fold c_s=1.93=v_term/Mach is not an independently-evaluated substrate sound speed.
- **W4-1 is PASS with a genuine exit-flank INFO-graze sensitivity** the constant-c scan could not see (`disc_far_max` factor-1.94 from the INFO ceiling). This is a refinement of the PASS, NOT a path to RE-OPEN; settled by CF-1.
- No substrate claim is broken by the convergence — the strains are all on the symmetric/bounce reading, which is closed.

### Carry-Forward Computations (MATH ONLY — propagate to S96)

**CF-1 — c_s(τ)=v_g(τ) exit-flank N_zeros + graze scan (settles W4-1 PASS-vs-INFO; never RE-OPEN).**
1. **What**: Compute `c_s(τ) = |dω/dk|` from the S71 BdG dispersion for BOTH scalar (c_BLV) and condensate (c_B2) branches across τ∈[0.05, 0.40]; re-run the W4-1 N_zeros scan on `D_channel(τ) = c_channel(τ)² − v(τ)²` per channel; report N_zeros AND `disc_far_max` per channel.
2. **Inputs**: S71 BdG dispersion modes; `s94_ds_gamma_e_resolution_vg_b2_trajectory.npz` (the τ-trajectory of v_g_B2, if it carries `v_g(τ)`); canonical `c_BLV=0.485`, `v_g_B2_fold=0.022699323`; W4-1 v(τ) and `dS/dτ=+58673`; W4-1 anchors (`graze_min_abs`, `GRAZE_INFO_CEIL=1e-3`).
3. **Gate**: PASS-ASYMMETRIC iff N_zeros=1 in every channel AND `disc_far_max<−GRAZE_INFO_CEIL` (sub-ceiling) in the scalar channel; PASS-with-INFO iff N_zeros=1 but scalar `disc_far_max` crosses −1e-3; RE-OPEN iff any channel returns N_zeros≥2 (predicted: not reachable — factor-294 wall). Pre-registered prediction: PASS-ASYMMETRIC.
4. **Effort**: Low–medium (the S94 trajectory may make c_B2(τ) nearly free; scalar c_BLV(τ) needs the a_n-moment trajectory across τ; single N_zeros scan per channel reuses W4-1 machinery).

**CF-2 — F1 scalar-channel observable-squeeze-branch falsifier (discriminator of the categorical a₂-observability claim).**
1. **What**: Scan the substrate excitation spectrum for an OBSERVABLE squeeze branch near the 72.8 M_KK scale (an OOM above the W4-3 condensate-squeeze support ω∈[0.819741, 1.063714]).
2. **Inputs**: W4-3 squeeze P(ω) machinery (B2[0..3], B3[0..2] modes); the scalar-channel (c_BLV) mode spectrum; the a₂ entry temperature 72.8 M_KK.
3. **Gate**: NULL (no scalar-channel observable squeeze branch near 72.8) → confirms the categorical asymmetry (a₂ = kinematic-carrier only, observationally invisible); PRESENCE → the emission is genuinely two-observable-temperature, composite form remains correct but categorical refinement breaks. Pre-registered prediction (both readers): NULL.
4. **Effort**: Medium (re-uses W4-3 squeeze machinery at a higher ω-window; the question is existence of a peak, not a precise value).

**CF-3 — v(τ)-normalization reconciliation across S55 / S73A / W4-1 (pin "the" Mach).**
1. **What**: Reconcile the flow-velocity definitions: identify the v-normalization behind S73A's "20.7 in c_BA units" (≈ v=8.26 M_KK; likely a Jensen exit-window average of `dv/dτ = −(dS/dτ)/(M_ATDHFB·v)`) vs W4-1's fold-peak v_fold/c_BLV=13.75; pin §6.2's canonical "Mach" = 13.75 (fold-peak, scalar channel).
2. **Inputs**: S55/S73A Jensen-trajectory v(τ) and Mach definitions; W4-1 v(τ); canonical `c_BLV=0.485`, `c_BA=0.399`, `M_ATDHFB`, `dS/dτ=+58673`; candidate v's {v_fold=6.66875, v_term=26.545, v_exit≈5.558}.
3. **Gate**: PASS iff the S73A 20.7 and W4-1 13.75 are reconciled to a single v(τ) under two explicit normalizations (one fold-peak, one exit-window) with the discrepancy fully attributed to v-definition (NOT sound speed). Does not threaten N_zeros=1.
4. **Effort**: Low (bookkeeping reconciliation; the Sage arithmetic is done — sp A4 — what remains is locating the S73A v-window definition in the S55/S73A artifacts).

### Effected In-Session (NON-MATH — completed by volovik, the final agent, BEFORE TERMINATING)

**Deconfliction reminder**: the curated master doc `sessions/framework/phonic-exflation-equation.md` is OFF-LIMITS this round. The two NON-MATH effects I MAY (and do) effect now are the w4 WP constraint-map and the Doc-routing (PENDING) section of this file. Everything else (master-doc §6.2, registry Stage-1, Penrose figure, c_s scan) is a carry-forward listed above.

- [x] §6.2 disposition (Effects 1–3: ASYMMETRIC resolution + KIND-column ledger + composite-form relic-temperature narrative) recorded as PENDING-W-1 — `sessions/archive/session-95/workshops/c1-cs-softening-completeness.md` §"Doc-routing (PENDING)" — anchor PENDING-W-1 (for the phonon-first doc-incorporation follow-up to apply to `phonic-exflation-equation.md §6.2` verbatim)
- [x] w4 WP constraint-map hardened: C1→ASYMMETRIC promoted to six-wall structural theorem + surface-taxonomy + composite-emission row appended — `sessions/archive/session-95/session-95-w4-workingpaper.md:412` — anchor "Constraint-Map Updates" (row 2026-05-29)

**STAGE-0 workshop-internal joint-theorem candidate (NOT written to the registry — drafted here per `joint-theorem-promotion.md`; Stage-1 registration is CF-4, S96):**

> **§VII-candidate — TRANSIT SURFACE-TAXONOMY + TWO-STAGE COMPOSITE EMISSION** (STAGE-1-CANDIDATE on S96 registration; cross-axis joint theorem).
>
> *Clause (a)* [JOINT — sp causal-structure × volovik superfluid-analog]: The substrate transit carries surfaces of THREE distinct KINDS — SONIC (Mach-1 crossing `v=c_channel`, one per quasiparticle branch by the Jacobson-Volovik per-species principle), THERMODYNAMIC-kinematic (a₂ moment-gradient κ, co-located with the scalar entry sonic surface), THERMODYNAMIC-spectral (a₄ condensation-gradient κ, the interior-processing edge). The first two KINDS were conflated by the dropped V.6 clause.
>
> *Clause (b)* [sp-side — causal structure]: There is exactly ONE post-genesis SONIC surface (the scalar entry, `v=c_BLV` at τ₀=0.112466, κ_entry=+18.52>0, white-hole); it bounds an unbounded supersonic expulsion region; there is NO future-trapped sonic exit and NO symmetric throat. C1 → ASYMMETRIC.
>
> *Clause (c)* [volovik-side — superfluid analog]: The condensate-channel SONIC surface (`v=c_B2`) is OFF the post-genesis trajectory (Mach_B2∈[244.86, 293.79] everywhere, pre-genesis), so for the radiating species the entire observable universe is interior to it (the reversed-population "T=−0" interior, Volovik #27 §C–D = the GGE inverted distribution).
>
> *Clause (d)* [JOINT — sp × volovik, parent-anchored to Volovik #27]: The Hawking-analog relic emission is a TWO-STAGE composite inherited parent→child from ³He-A (Volovik #27): stage 1 = sonic horizon defining the kinematic-carrier temperature (`T_Hawking=ℏ|v'|_{r_h}/2π` at `|v|=c`; the a₂ surface, T=72.8); stage 2 = interior pair-creation/Andreev spectral-processing (#27 §F, "more important than Hawking radiation itself"; the a₄ Pöschl-Teller barrier, T=7.578) that sets the OBSERVED spectrum.
>
> *Clause (e)* [volovik-side — superfluid analog; COMPOSITE-locked]: The OBSERVED relic spectral temperature is the a₄ stage-2 value (7.578 M_KK); the a₂ value (72.8) is the stage-1 kinematic-carrier temperature. Whether a₂ is observationally invisible is HELD pending falsifier F1 (CF-2) — the COMPOSITE form is asserted, the categorical form is the expected-NULL of F1.
>
> *Clause (f)* [sp-side — causal structure]: C1 → ASYMMETRIC is over-determined at SIX independent walls (scalar speed-hierarchy / channel-monotone / monotone-dS/dτ / irreversibility-quench / 12D-censorship-monotone-a_eff / reversed-population-interior); the bounce reading breaches none.
>
> Stage-2 requirement (S96): two cross-reviewers on DISTINCT axes (causal-structure + superfluid-analog), NEITHER the originating authors (sp, volovik), NO workshop transcripts; JOINT clauses (a),(d) PASS-AND'd. Per `joint-theorem-promotion.md §"Stage-2 Axis-B Selection Protocol"`.

- [x] STAGE-0 joint-theorem candidate drafted (clauses a–f, cross-axis attributed) — `sessions/archive/session-95/workshops/c1-cs-softening-completeness.md` §"Effected In-Session" — anchor "§VII-candidate TRANSIT SURFACE-TAXONOMY" (Stage-1 registration queued as CF-4)

### Closing Line

The c_s-softening challenge did not crack C1 — it hardened it. The softening is real but it lives in the condensate band-edge channel (c_B2, rho-pinned to 0.0227, not zero), not the scalar transit channel the §6.2 discriminant is built on; and routing the discriminant onto any softer channel only deepens the supersonic interior (factor-294, not factor-11). C1 → ASYMMETRIC now stands over-determined at six independent walls, the deepest being the entropy-arrow of the irreversible Kibble-Zurek quench — the substrate transit is a one-way expulsion before any (c²−v²) bookkeeping is done. The durable yield is structural, not a verdict flip: a three-KIND surface-taxonomy and a two-stage composite emission, both inherited parent→child from Volovik's ³He-A analog white hole (#27), not invented and not analogized. The substrate IS the irreversible quench; the laboratory acoustic white hole is the realization IN which we first read its surface structure off.
