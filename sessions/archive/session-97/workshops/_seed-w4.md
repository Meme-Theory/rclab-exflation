# Seed — Chunk w4 (Observational: Ω_GW shape + f·σ₈ + Bayes factor)

**Date**: 2026-05-30   **Investigator**: phonon-first-cosmologist   **Source**: session-97-w4-workingpaper.md + session-97-plan-w4.md
**Wave summary**: 2 PASS / 2 INFO. W4-1 OMEGAGW-PEAK-HEIGHT (PASS) + W4-2 OMEGAGW-ACOUSTIC-SPECTRAL-SHAPE (PASS) jointly CLOSE the 127-OOM Ω_GW number-vs-prose drift on BOTH legs — peak `Ω_peak=9.15e-5` (radiation-budget ceiling, log₁₀=−4.04, κ-invariant amplitude) and IR-tail `Ω_GW(3 mHz)=4.046e-132` (slope p=3 DERIVED from the finite linear-edge fold DOS, NOT assumed Hiramatsu) → LISA-STERILE (118 OOM below PLS); the retracted `1e-10`-at-pivot placeholder RETIRED (mack landed Row #7.audit-4). W4-3 FSIGMA8-FORECAST-REFETCH (INFO, branch-a fetch outage — NEVER FAILs on outage by pre-registration; substrate curve FINAL; W6-1 σ-distances reproduced exactly, S₈-relieving confirmed). W4-4 D3-BF (INFO, Track B): BF_spine FLOOR=2.0×10² DECISIVE model-class over random-geometry null; rank-1 collapse fired as PREDICTED (central |Corr|=1/2 EXACT on the immovable 0.5 edge + OQ3 unestablished). No FAILs, no contested-reading tensions, no cross-wave CONFLICTS (only shared-knob DEPENDENCIES, flagged below).

## Slot 1 candidates — solo reviews (/rclab-review)
(Q1-YES, Q1b)

None. No W4 result carries a buried structural question that one agent must synthesize independently. The one genuinely-new methodology landing (D3-BF's EVOI prior-predictive-range BF replacing the retired "chance of one random geometry" heuristic) was already co-reviewed in-gate by `sagan-empiricist`, its method is pre-registered, and the headline is a settled conservative FLOOR (b_mH=0.5 ACCOMMODATION-honoured per the user directive). A solo re-read would be a per-wave digest, NOT a Slot-1 synthesis (`Investigating-Workshops.md §"is NOT"` item 4); it adds no Q1-YES structural content.

## Slot 2 candidates — workshops (/rclab-workshop)
(Q1-YES, Q1a)

None. Applying the 3-question discriminator (`Investigating-Workshops.md §"Discriminating decision"`) to every W4 candidate, NONE reaches Q1-YES (math/physics adjudication between two competing readings of a substrate-physics observable). The discriminator walk:

- **W4-1 / W4-2 (both PASS, 127-OOM closure)** — clean, unambiguous, internally consistent; mack already effected the Row #7.audit-4 inventory landing (housekeeping §A9). No competing reading. Not a workshop.
- **W4-3 (INFO, fetch outage)** — the INFO is the pre-registered `track_B / branch-a` outcome; the gate NEVER FAILs on a paper-search outage BY CONSTRUCTION (the substrate f·σ₈ curve is FINAL, only the forecast-precision denominator is fetched). No reading-divergence — the disposition was pre-specified and matched. The σ₈ channel-keyed disambiguation + the un-lifted forecast-TABLE GAP are Q2-hygiene (already routed). Not a workshop.
- **W4-4 D3-BF (INFO, disposition-ambiguous)** — the "ambiguity" (central `|Corr|=1/2` lands EXACTLY on the immovable 0.5 edge; `sign=PASS/mag=INFO/regime=VALID`) is NOT a contested reading. The gate's own substitution chain DERIVED rank-1 collapse as the substrate-forced DEFAULT, the 0.5 disposition edge was PRE-REGISTERED IMMOVABLE, and the verdict followed the rule exactly (collapse fired as predicted ⇒ `sign_verdict=PASS`; the pre-registered `INFO_meaning` Track-B-0.75 outcome). What is genuinely UNRESOLVED is whether OQ3 (cross-pipeline mutual-independence of the {w0, σ8} private H-systematics) holds — and resolving it requires **COMPUTING a new cross-pipeline covariance number** from the derivation pipelines, with a pre-registered PASS/INFO/FAIL threshold. That is a Q2/Q3 compute carry-forward (`CF-S98-W4-4-OQ3-COVARIANCE`, already WP-resident), NOT a Q1 derivation between two competing readings. First-YES wins → NOT a workshop.

(Honest-count discipline: a 4-gate observational wave with 2 clean PASSes and 2 pre-registered-disposition INFOs is expected to produce 0 genuine workshops per `Investigating-Workshops.md §"Honest count discipline"`. The only Q1 seed in S97 is W2-2's composite-collapse INFO-vs-FAIL adjudication — that is W2's, recorded in `session-97-housekeeping.md §"Q1 workshop seed"`, and is NOT a W4 item.)

## Cross-wave flags (surface for consolidator; NOT resolved here)
- **W4-1/W4-2 ↔ W1 (shared κ knob; DEPENDENCY, not conflict).** The acoustic Ω_GW peak/shape gates consume the `M_KK⁻¹→s` knob κ that W1's `S97-COOLING-BUDGET-KAPPA-PIN` (item 1.5, PASS, consistency-identity) pins. W4 SWEEPS κ ∈ [1e-20, 1e-10] (121 pts) for robustness so it does NOT block on W1; both Ω_peak (amplitude, κ-invariant) and the IR-tail VALUE are κ-robust (band-flat). No conflict — note only that the substrate-natural κ_nat both waves use is the SAME `M_KK_inv_seconds = 8.860439881925477e-42 s` (W1.5 pin, S96).
- **W4-4 D3-BF dagger leg ↔ W1 a(t) gap (DEPENDENCY across waves).** The D3-BF `{a0,a2}` dagger pair's conditional-multiplicativity is explicitly tied to "the SAME undelivered effective-Friedmann / K_pivot projection as the Atlas D04 C2 a(t) gap (STILL-NOT-MET)" — i.e. the W1 route-reconciliation object (`CF-S98-W1-ROUTE-RECONCILIATION`). The dagger discount is the model-class-evidence shadow of the same borrowed-H map that W1's route-invariance gate (`S97-W1-QOMEGA-ROUTE-INVARIANCE`, INFO, route-SENSITIVE) characterizes. Not a W4 conflict; surfaced so the consolidator sees the structural through-line (a(t) gap → dagger discount).
- **W4-4 BF_spine ↔ §7.3 capstone scorecard + 2027 R_842 channel (capstone-surface DEPENDENCY).** BF_spine=2.0×10² is flagged for the session-close designated writer to insert into the §7.3 scorecard narrative (housekeeping §A10), and the verdict is explicitly NOT the w₀(−0.918)-vs-ΛCDM(−1) R_842 likelihood ratio (a SEPARATE channel, binding event 2027). Both routed; no adjudication owed — flagged only so the consolidator does not mistake the §7.3 prose insertion for an open workshop.

## Carry-forwards (route to W4 WP CF section, NOT this schedule)
- **[Q3-wave-together / Q2-hygiene] CF-S98-W4-4-OQ3-COVARIANCE** — establish (or refute) cross-pipeline mutual-independence of the BF_spine factors to license the rank-2 dagger. ALREADY WP-resident (`session-97-w4-workingpaper.md §"Carry-Forward Computations"`, lines 293–301): pre-registered gate `S98-W4-4-OQ3-COVARIANCE` (PASS iff |Corr|<0.5 with pipeline-independence demonstrated ⇒ rank-2 dagger; INFO iff covariance stays on the 0.5 edge ⇒ rank-1 collapse stands; FAIL iff pipelines shown statistically dependent). EVOI of resolving = Δlog BF = (1−0.5)·1.0 = 0.500. Inputs: `s97_d3_bf.npz` (audit `8f4f9abb`) + the four spine observables' derivation-pipeline provenance. Effort ~0.5–1 wave. `/rclab-plan` consumes it from the WP — do NOT list as a workshop.
- **[Q2-hygiene] CF-S98-HK-SIGMA8-CHANNEL-KEYED-PINS** — promote the two channel-keyed σ₈ values (`SIGMA8-OZ-50`=0.799 spectral-action vs `sigma8_growth_a2`=0.79317 a₂ growth-channel) to `canonical_constants.py` with channel-distinct provenance + cross-note. ALREADY in `session-97-housekeeping.md §B` and mirrored to the W6 WP CF (surfaced by W4-3 but routed to W6 per sub-keying-ambiguity, `math-scripts.md §"Canonical Write-Order"`). Effort < 0.1 wave. NOT a workshop.
- **[Q-other / housekeeping-note, NOT a CF] forecast-TABLE GAP** — the live per-z DESI-5yr/Euclid forecast-precision TABLE remains un-lifted (latex/PDF tables do not mine cleanly via the read endpoint; W4-3 corroborated the W6-1 estimated denominators to order-of-magnitude against fetched DESI-DR1 + Euclid IST:F but did NOT replace them). This is a mechanical MCP-dependent fetch gap, not a substrate compute — recorded as a housekeeping note (W4 synthesis line 277), NOT a forward-compute CF. If the per-z table is wanted at S98, it is a [VERIFY] re-fetch with the same dual_prior structure as W4-3 (never FAILs on outage).

## Wave-by-wave digest (consolidator background)
- **W4-1 `S97-OMEGAGW-PEAK-HEIGHT` — PASS** (`mack-cosmic-bridge`; [SIGN]; PHONONIC). `Ω_peak=9.15e-5` at f_peak=8.4835e39 Hz from the FINITE enhanced fold DOS (ρ_B2_per_mode=14.0233; van-Hove divergence REFUTED S94). log₁₀=−4.0386 ≤ 0 ⇒ GW-energy sanity ceiling satisfied with 4.04 decades margin. κ-INVARIANT amplitude (built from dimensionless ratios; flat across κ∈[1e-20,1e-10], 121 pts). `sign=PASS/mag=PASS/regime=VALID`. Step-2 promoted `Omega_GW_acoustic_peak=9.15e-5` canonical. audit `71fbc18f`.
- **W4-2 `S97-OMEGAGW-ACOUSTIC-SPECTRAL-SHAPE` — PASS** (`little-red-dots-jwst-analyst`; [SIGN]; PHONONIC). IR slope `p_derived=3` DERIVED from the n_dispersion=1 linear band-edge (γ_E=0, S94), NOT assumed Hiramatsu; van-Hove steepening flag False; Maggiore floor p≥1, signed distance p−p_floor=2.0. IR-tail `Ω_GW(3 mHz)=4.046e-132` (log₁₀=−131.39, 118.39 OOM below LISA-PLS), Sage-QQ-exact log-ratio `−42.451453809…`. κ-robust single VALUE (`-NO-SEELEY-DEWITT-MOMENT` convention — p from band-edge DOS, no a_n). `sign=PASS/mag=PASS/regime=VALID`. Step-2 promoted `Omega_GW_acoustic_LISA_tail=4.046e-132`. Together with W4-1 CLOSES the 127-OOM drift (mack Row #7.audit-4 supersedes PENDING-SUBSTRATE-RECOMPUTE). audit `c63d3869`.
- **W4-3 `S97-FSIGMA8-FORECAST-REFETCH` — INFO** (`mack-cosmic-bridge`; [VERIFY]; NON-PHONONIC). branch-a: paper-search `search_arxiv` DOWN at dispatch, `read_arxiv` UP (UPGRADED with fetched DESI-DR1 FS arXiv 2411.12022 σ₈=0.8121±0.0053 + Euclid IST:F 1910.09273 corroboration). Reproduced W6-1 per-z σ-distances EXACTLY (z=0.51: 0.506σ current → 1.013σ DESI-5yr → 1.534σ Euclid, |Δ|<1e-3 ⇒ `w61_reproduced=True`). Within-band DESI-5yr 7/7, Euclid 6/7 (at the 1.5σ ceiling). S₈-relieving: S₈_FW=0.8128 < S₈_ΛCDM=0.8310. σ₈ disambiguation flagged (SIGMA8-OZ-50=0.799 ≠ sigma8_growth_a2=0.79317, ~0.7% apart, no `sigma_8_FW` canonical). Gate never FAILs on outage by pre-registration. audit `a20043e7`.
- **W4-4 `S97-D3-BF` — INFO** (`mack-cosmic-bridge`, co-review `sagan-empiricist`; [SIGN]; NON-PHONONIC). BF_spine FLOOR=2.0×10² (log₁₀=2.3010) DECISIVE for the FRAMEWORK model-class over the random-geometry null (zero-borrowed-H spine {m_H, normal ν-ordering, σ/m=0, c_s²=0}); explicitly NOT the w₀-vs-ΛCDM R_842 likelihood ratio (separate 2027-binding channel). m_H ACCOMMODATION-honoured (b_mH=0.5 not 1.5, 1× evidence weight per user directive / rigor-reg #6). Disposition: central |Corr|=1/2 EXACT on the IMMOVABLE 0.5 edge (kernel disparity 518.330 CANCELS; |Corr|=1/√((1+r0)(1+r2)), fn of variance-mix r alone; pins {25/34,25/41,1/2,25/61} straddle 0.5, 3/4 on rank-1 side) + OQ3 mutual-independence UNESTABLISHED ⇒ rank-1 collapse fires AS PREDICTED. `sign=PASS/mag=INFO/regime=VALID`; dual_prior Track B (0.9). EVOI of resolving the covariance Δlog BF=0.500 (→ CF-S98-W4-4-OQ3-COVARIANCE). audit `8f4f9abb`.
