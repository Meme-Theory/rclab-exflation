# Session 97 Workshop: volovik x mack

**Date**: 2026-05-31
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Agents**: volovik (volovik-superfluid-universe-theorist), mack (mack-cosmic-bridge)
**Source Documents**:
- `sessions/archive/session-97/session-97-w2-workingpaper.md`
- `computations/session-97/s97_gate_verdicts.txt`
- `sessions/archive/session-97/session-97-housekeeping.md`

**Focus Topics** (W-1 — C10-N-exponent composite-collapse adjudication):
Gate S97-W2-2-C10-N-EXPONENT emitted a 3-tuple `sign=FAIL / magnitude=PASS / regime=VALID` (LATEST non-superseded line `b69da9f4`, verdict line 62; Option-A chain `30566894→0e6076f3→b69da9f4`, FAIL→INFO→INFO; canonical label INFO). Measurements: n_leg1=2.0000, n_leg2=1.9879 (|Δn|=0.012<0.05 leg-consistent); exponent-on-q = 1.978→2 as q→0 (quadratic V at q-stationary minimum, k=+3586.5 M_KK from 992 D_K modes); C_correction = −0.0219 (FROM BELOW), |C|/2 = 0.011<0.05 bounded ⇒ n_eff∈[1.9,2.1]. The mechanical composite-collapse rule (`gate-verdicts.md`; modifications are Class-3) gives FAIL on `sign_verdict=FAIL`; the agent emitted INFO via the gate's PRE-REGISTERED semantic INFO_meaning (FAIL_meaning antecedents "legs disagree" OR "correction free/unbounded" did NOT fire). Substrate-physics sign question: the directional [SIGN] prediction was `n_eff ≥ 2`; the measured anharmonic q³ correction is NEGATIVE so n_eff=1.978 approaches 2 FROM BELOW (Mack-contrast direction; Mack free-redshift n~1.778; gap now D_K-bounded-not-free, √(λ_n²+q) sets ω_n; resolves S66 OQ-3 open sign).

Competing readings:
- **(A) volovik** — from-below softening is a genuine bounded substrate consequence; the CORE limit n_eff→2 is CONFIRMED, the `≥2` was an over-tight one-sided antecedent, INFO is correct, C10 legitimately SHARPENED-held.
- **(B) mack** — the directional prediction `n_eff≥2` was substrate-FALSIFIED; from-below is the Mack-direction signature with BBN / observational-realization import (n<2 vs n=2 may change the present-epoch ρ_vac/ρ_obs=1.032 tracking extrapolation that W6 promoted carrying C10-conditionality); the C10 disposition must reflect the sign-violation.

Adjudication axes: (a) [SIGN]-gate label precedence — semantic INFO_meaning vs mechanical composite-collapse (one-off OR a general `gate-verdicts.md` clause diff); (b) robustness of the from-below sign (robust vs GGE-occupation-truncation-sensitive; weigh the gap-set mode-sum cross-check C=+0.0297, same OOM OPPOSITE sign); (c) reconciled C10 Atlas-04 tag + capstone §8.5 disposition; (d) consistency with the W6-promoted DILUTION-CC ρ_vac/ρ_obs=1.032 realization. Pin the LABEL + the sign-robustness, not the held-vs-discharged status (C10 substantive disposition is robust to the label per the WP).

---

## Round 1 — volovik: Opening Analysis

### V1: [SIGN]-trigger label precedence — semantic INFO_meaning vs mechanical composite-collapse

**Claim (reading A).** For gate `S97-W2-2-C10-N-EXPONENT` the canonical label INFO is correct, and it is correct as a **one-off** that was pre-registered for exactly this scenario — NOT as a `gate-verdicts.md` precedence-clause diff. The pre-registered semantic INFO_meaning governs the canonical label here because the FAIL_meaning antecedents never fired; the mechanical composite-collapse rule's FAIL output is the structurally honest *annotation* of the falsified one-sided sub-clause, retained at the 3-tuple layer.

**The two rules, side by side.** The mechanical composite-collapse rule (`gate-verdicts.md §"Composite-collapse rule"`) is deterministic: `elif sign_verdict == FAIL: composite = FAIL`. Run literally on the 3-tuple `sign=FAIL / magnitude=PASS / regime=VALID` (verdict-file line 63, the schema-v2 annotation under canonical line 62 `b69da9f4…`), it returns FAIL. The gate's OWN pre-registered classifier returned INFO. These are not in contradiction once you see what each is scoring:

- The mechanical rule scores a SINGLE scalar — `sign(value − threshold)` against a single pre-registered directional prediction. It has exactly one directional input slot.
- The gate's pre-registered semantic rubric scores a STRUCTURE: `INFO_meaning = "departure exponent well-defined, one-signed, bounded-but-nonzero correction from 2"`; `FAIL_meaning = "legs disagree OR correction free/unbounded"`. This rubric is a 3-clause classifier, and it was frozen at plan-freeze BEFORE the value was seen.

**Why the semantic rubric governs HERE (the substitution chain).** The [SIGN] directional pre-registration for this gate was not a bare scalar `n_eff ≥ 2`. It was a CORE claim plus a CONDITIONAL sub-clause (WP §W2-2 Verdict; SUBSTITUTION CHAIN "Direction"):

```
CORE:        n_eff → 2 in the bounded limit q→0                          [substrate-forced]
SUB-CLAUSE:  IF the GGE pressure correction is positive-and-small
             THEN n_eff ≥ 2                                              [conditional]
```

Substitute the measured values (verdict-file line 62 value-field, authoritative):
- CORE: `exponent-on-q = 1.978111 → 1.99920 at q ≤ 0.005` (WP table, row `exponent-on-q`). The limit is 2. **CORE CONFIRMED.**
- SUB-CLAUSE antecedent: `C_correction = −0.021889 < 0` (verdict-file `C_correction_measured=-2.188949e-02`). The antecedent "positive correction" is **FALSE**. A conditional with a false antecedent does not falsify the implication — it is vacuously true on the implication, and the consequent `≥2` simply does not obtain.

The schema-v2 `sign_verdict=FAIL` is computed against the *literal one-sided cell* `n_eff ≥ 2` (verdict-file line 63 comment: "SIGN=literal-n_eff>=2"). That cell IS violated: 1.978 < 2. So `sign=FAIL` is honest and must be retained. But the mechanical collapse rule was written for gates whose directional pre-registration IS a single literal cell — it has no slot for "the directional prediction had a CORE limit (confirmed) plus a conditional one-sided fringe (vacuous)." The gate's semantic rubric DOES have that resolution, and it was pre-registered. This is the layer-functor distinction (`epistemic-discipline.md §"Layer-Decomposition"`): the mechanical rule is the methodology-floor F-image that collapses a structure to a scalar; the semantic rubric is the substrate-physics-layer pre-image that retains the structure. When the structure is richer than a single sign, the pre-image governs the canonical label and the F-image is retained as the honest scalar annotation. That is exactly the schema-v2 design — the 3-tuple companion row EXISTS so the scalar sign-result is preserved even when the composite is set otherwise. The worked example in `gate-verdicts.md` (`S{N}-SECTOR-1-SR-FLOW-Z-FACTOR-PIVOT55`) shows the same dual-layer move in the opposite direction (sign=PASS retained while composite=FAIL via regime=BREAKDOWN): the 3-tuple and the composite are SEPARATE pinned objects by construction.

**The Class-3 firewall — why this is NOT post-hoc rule editing.** The hard constraint is `gate-verdicts.md`: "Modifying this collapse rule after seeing a verdict is a Class-3 PROHIBITED_ACTIONS violation." Reading A does NOT modify the collapse rule. The collapse rule still says `sign=FAIL ⇒ composite=FAIL` and still computes FAIL on the scalar `sign_verdict=FAIL` — that is preserved verbatim at line 63. What governs the canonical *label* is a DIFFERENT, ALSO-pre-registered object: the gate's own INFO_meaning/FAIL_meaning rubric (a Class-8.2 verifier-rubric pre-registration per `epistemic-discipline.md §"Verifier-Rubric Pre-Registration"`). Both objects were frozen at plan-freeze. The Option-A supersession chain (`30566894` FAIL → `0e6076f3` INFO → `b69da9f4` INFO, all retained at the byte level, each successor carrying `supersedes=`) is the byte-level proof that nothing was edited in place — the FAIL line is STILL THERE. So the honest reading is: **the mechanical FAIL was emitted first (line 55), then RECONCILED to the gate's pre-registered semantic rubric (line 58), with the scalar `sign=FAIL` carried forward as annotation.** This is the v3-closure-recovery sig_5/rubric-calibration corrective-emission pathway operating as designed, not a convention-shop.

**Where the substrate physics cuts AGAINST me (steelman honesty).** Three concessions I will not paper over:

1. **The one-off vs general-clause question is genuinely open, and I cannot foreclose the general-clause answer.** Reading A's *minimal* claim is the one-off: this gate's INFO_meaning was pre-registered for exactly this scenario, so it governs THIS label. But the scenario — "a [SIGN] gate whose directional prediction is a CORE-limit-plus-conditional-fringe, where the fringe antecedent is falsified but the magnitude is bounded" — is not obviously unique to C10. If it recurs (K≥3 per the SUGGESTION→MANDATORY contract, `feedback_rules-compensate-missing-structure.md`), the honest resolution is a *general* `gate-verdicts.md` clause: "when a [SIGN] gate's directional pre-registration decomposes into a CORE claim plus a conditional sub-clause, the composite-collapse `sign_verdict` is computed against the CORE; the conditional sub-clause's violation is recorded as a 3-tuple annotation, not a composite driver." I flag this as the live methodology fork for mack, and I lean toward "one-off now, general-clause candidate at K=3" rather than asserting the general clause unilaterally.

2. **A `sign=FAIL` that is overridden to INFO is structurally close to the failure mode the schema-v2 3-tuple was built to PREVENT.** The whole point of separating sign/magnitude/regime was so a sign-violation cannot be buried inside a magnitude PASS. Reading A must therefore carry the burden that the override is *rubric-driven and pre-registered*, not *outcome-driven*. The byte-level Option-A chain discharges this for THIS gate, but the precedent is dangerous if loosely generalized — which is exactly why I will not generalize it without K=3.

3. **The honest scalar is FAIL, and it should stay loud.** I am NOT arguing the sign result should be softened. n_eff=1.978 < 2 is a real, retained `sign=FAIL` on the literal cell. My claim is narrower: the literal cell was the wrong one-sided shape for a CORE-plus-fringe prediction, and the canonical *label* — the thing downstream consumers read for C10's disposition — correctly tracks the structure (INFO), while the scalar stays pinned (FAIL) for anyone who needs it.

**Questions for mack (V1).**
- Q1a. Do you accept the CORE-limit-plus-conditional-fringe decomposition as a faithful reading of the plan's [SIGN] pre-registration, or do you read the plan's directional prediction as a *single literal* `n_eff ≥ 2` cell (in which case `sign=FAIL ⇒ composite=FAIL` is the only honest collapse and the INFO label is a post-hoc rescue)? The plan-block text is the arbiter — which reading does it license?
- Q1b. If you agree the *label* is INFO for this gate, do you want the general `gate-verdicts.md` CORE-vs-fringe clause minted NOW (general-clause), or held as a K=1 SUGGESTION calibration instance pending recurrence (one-off)? I lean one-off; I want your observational-side reason for or against.

### V2: From-below anharmonic correction as a bounded substrate consequence (robustness vs GGE-occupation truncation; gap-set cross-check)

**Claim (reading A).** The *boundedness* of the GGE-pressure correction (`|C|/2 = 0.010945 < 0.05`, verdict-file `abs_C_over_2=1.0945e-02`) is a ROBUST substrate consequence — it is forced by the gap-set frequency structure `ω_n(q) = √(λ_n² + q)` and does not depend on the GGE-occupation truncation. The *sign* (from-below, `C_correction = −0.021889 < 0`) is robust as the sign of the *measured anharmonic* correction but is NOT yet a single-method-invariant substrate fact: the gap-set mode-sum cross-check returns `C = +0.029719` (verdict-file `C_modesum_gapset=2.9719e-02`) — same order of magnitude, OPPOSITE sign. I treat this honestly below. Net: the CORE result (n_eff → 2, bounded) is method-invariant; the from-below DIRECTION is the measured-anharmonicity reading and is the live point of genuine substrate uncertainty.

**Why boundedness is robust (the substrate-physics reason).** This is the Ordered-Veil consequence and it is the part I am most confident of. The GGE relic is an integrable, never-thermalizing distribution (the framework's paradigm result — the relic never equilibrates). Its pressure response `dp_k/dH` is therefore NOT a free thermodynamic quantity; it is structurally constrained by the gap-set dispersion. The substitution chain (WP §W2-2 SUBSTITUTION CHAIN, Sage-verified at plan-freeze):

```
Def 2:   ε(q) = E_ZP(q) = ½ Σ_n √(λ_n² + q) · (2N_n + 1) · d_n
Def 5:   dE_ZP/dq = (¼) Σ_n (2N_n + 1) d_n / ω_n(q) > 0   [Monotonicity #19, atlas-07 A9]
         with ω_n(q) = √(λ_n² + q)
⇒        dω_n/dq = 1/(2 ω_n)                              [Sage]
```

The Jacobian `dω_n/dq = 1/(2ω_n)` is bounded ABOVE by `1/(2|λ_min|)` and is strictly positive — it never blows up because the gap `√(λ_n²+q)` keeps `ω_n` away from zero for all `q > −λ_min²` (the relaxation window stays above the boundary `q_boundary = −λ_min² = −0.67197549`). This is the substrate analog of the standard Volovik vacuum-compressibility result `χ⁻¹ = q²·d²ε/dq² > 0` (Paper 03 Eq.3.9; WP Def 4) — a STABLE relaxation with a finite response. Because the correction `C = (exponent-on-q) − 2` is built from this bounded Jacobian acting through `dp_k/dH = (dp_k/dω_k)(dω_k/dq)(dq/dH)`, and `dq/dH = 0.15` is itself bounded (the linear relaxation map), `C` cannot be O(1). The bound `|C|/2 = 0.011` is the numerical realization of a structural ceiling, NOT a coincidence of the truncation. **This is the resolution of S66 OQ-3.** At S66 the workshop left the sign undetermined precisely because it could not decide whether `ω_k` was set by `H` (free, `ω ~ a⁻¹` adiabatic redshift — Mack's reading, giving the FREE correction `n ~ 1.778`) or by the spectral gap (bounded). T.61 (`n_eff = 2 + Σ_k (dp_k/dH) n_k / Σ_k ω_k n_k`, session-66-mack-transit-workshop) was never computed on the D_K spectrum until this gate. Computing it on the 992 eigenfrequencies SETTLES the `ω_k`-source question: `ω_n(q) = √(λ_n²+q)` is gap-set, so the Mack free-redshift reading is excluded and the correction is D_K-BOUNDED. **That conclusion — bounded-not-free — is robust to the truncation**, because it is a statement about which variable sets `ω_k`, not about the numerical occupation weights.

**The occupation truncation, examined directly.** The GGE occupations `N_n` are placed on the 8 lowest BCS-active modes; the 984 geometric spectators carry vacuum zero-point only (`N_n = 0`), per the WP machinery pin (`N_n = GGE occupations on the 8 lowest BCS-active modes, 0 ... on the 984 geometric spectators`). Does the from-below sign depend on this 8-vs-984 split? Two observations:

1. The CORE curvature `k = d²ρ_vac/dq²|₀ = +3586.531 M_KK` (verdict-file `k_curv_d2rhovac_dq2_0=3.5865e+03`) is dominated by the FULL 992-mode sum — it is `= −d²E/dq²|₀` and reproduces `ρ_vac(0) = 81493.046049` = the S62 `E_ZP(0)` to all printed digits. The "2" exponent is therefore NOT a property of the 8 active modes; it is a property of the entire spectrum's concavity at the q-stationary minimum. The CORE is truncation-robust.
2. The *anharmonic* correction `C` is a higher-derivative (q³) quantity and IS more occupation-sensitive than `k`, because the q³ term weights the low-`ω` (low-`λ`) modes — exactly the 8 BCS-active ones — more heavily through the `1/ω³`-type kernels. This is where the from-below sign lives, and this is where I must be honest.

**Weighing the gap-set mode-sum cross-check (C = +0.0297, OPPOSITE sign) — the honest core of V2.** The gate computed `C` two ways:
- **Measured anharmonicity** (the small-q regression of `exponent-on-q`): `C = exponent-on-q − 2 = 1.978111 − 2 = −0.021889` → FROM BELOW.
- **Gap-set T.61 mode-sum** (`Σ_k (dp_k/dH) n_k / Σ_k ω_k n_k` with gap-set `ω`): `C = +0.029719` → FROM ABOVE.

Both are bounded; both are the same OOM (|0.022| vs |0.030|). But they DISAGREE IN SIGN. This is the single most important fact in V2 and I will not minimize it. What it means structurally:

- It does NOT threaten the CORE (n_eff → 2): both methods put `|C| ≈ 0.02–0.03`, well inside `|C|/2 < 0.05`, so `n_eff ∈ [1.9, 2.1]` is method-invariant. The magnitude PASS is robust.
- It DOES mean the *from-below direction is not yet a single-method-invariant substrate fact*. The two estimators are computing slightly different objects: the measured-anharmonicity regression captures the FULL nonlinear `δρ_vac(q)` curvature beyond quadratic order (all 992 modes, integrated through the actual `ρ_vac(q) = ε − q·dε/dq` shape), whereas the T.61 mode-sum is the leading-order pressure-response formula `Σ(dp_k/dH)n_k / Σ ω_k n_k` evaluated on the 8 occupied modes' gap-set frequencies. They should agree at leading order and need not agree at the sub-leading order where the SIGN of a `±0.02` correction is decided.

So my honest reading-A position is calibrated: **the from-below sign is the reading of the more-complete estimator (the full-spectrum measured anharmonicity), and it is the physically preferred one because it integrates the actual `ρ_vac(q)` shape rather than a leading-order pressure formula — but the opposite-sign T.61 cross-check is a genuine warning that the sign of a `±0.02` correction is NOT pinned to the precision the two methods can currently deliver.** What IS pinned, and what I will defend hard: (i) the correction is BOUNDED (both methods, the Jacobian argument); (ii) it is D_K-controlled / not free (the `ω = √(λ²+q)` gap-set structure, robust to truncation); (iii) the CORE limit is 2 (full-spectrum curvature). The from-below *direction* is a Track-A-leaning reading with a flagged sign-robustness caveat, NOT a claim I will assert at the same confidence as the boundedness.

**This sharpens — rather than weakens — reading A on the LABEL.** Note the structural point for V1: BOTH `C` estimators have `|C| < 0.05`, so under EITHER sign the magnitude PASS holds and the gate's INFO_meaning ("bounded-but-nonzero correction from 2, well-defined, one-signed") is satisfied. The INFO label does not depend on which sign is correct. The sign question is downstream of the label question. This is why the workshop brief correctly says "pin the LABEL + the sign-robustness, not the held-vs-discharged status."

**Questions for mack (V2).**
- Q2a. The two `C` estimators disagree in sign at the `±0.02` level. Do you read the full-spectrum measured-anharmonicity regression (C=−0.022, from below) or the T.61 leading-order mode-sum (C=+0.030, from above) as the physically-preferred estimator — and on what observational-realization ground? Your MK1/MK2 (the Mack-direction reading) presumably wants from-below; does the OPPOSITE-sign T.61 cross-check actually HELP your "from-below is the Mack signature" case, or does it undercut it by showing the sign is method-dependent?
- Q2b. Is there an observational quantity that is sensitive to the SIGN of a `±0.02` exponent correction (n=1.978 vs n=2.030 vs n=2.000) at present epoch or at BBN? If no current/forecast probe can resolve `Δn ~ 0.02`, then the sign-robustness caveat is academic for the falsifier surface and only the boundedness matters — which is your domain to adjudicate.

### V3: C10 disposition under the volovik reading (Atlas-04 tag + §8.5)

**Claim (reading A).** The C10 disposition recorded in-session — Atlas-04 C10 HELD `ASSUMED-PARTIALLY-PROVEN`, SHARPENED; capstone §8.5 q-flow qualifier OPEN — is CORRECT and is the SAME under the INFO label and under a hypothetical FAIL label. The sign-violation on the literal `≥2` cell does NOT warrant a DIFFERENT Atlas-04 tag than the one housekeeping A5 already recorded. The reason is that what the gate moved is ORTHOGONAL to what C10's tag tracks.

**What C10's tag actually tracks (the substrate decomposition).** C10 is "Volovik tracking-vacuum scaling `ρ_vac ~ M_Pl² H²`" (knowledge-MCP C10 entry; atlas-04). The exponent factorizes (WP §W2-2 Verdict):

```
n = (exponent-on-q) × (d ln q / d ln H)
    └─ SUBSTRATE       └─ FLUID INPUT
       CONSEQUENCE         (q~H linear relaxation)
       (V~q² quadratic
        minimum, k>0)
```

C10's discharge requires BOTH factors substrate-derived. This gate moved the FIRST factor from ASSUMED to DERIVED (the "2" is now the curvature degree of `ρ_vac` at its q-stationary minimum, `k = +3586.5 M_KK` from 992 D_K modes — T.61 computed on the spectrum for the first time). It did NOT move the SECOND factor: the `q ~ H` relaxation linearity remains the simple-fluid closure (Sage-equivalent to the tracking ansatz itself; WP Leg-1). Therefore C10 is **partially** discharged — exactly `ASSUMED-PARTIALLY-PROVEN`, sharpened — and §8.5 stays OPEN pending the relaxation-closure derivation (the carry-forward CF-S98-W2-2-RELAXATION-CLOSURE).

**Why the sign-violation does NOT change the tag.** The literal `≥2` cell is a property of the FIRST factor's *fringe* (the conditional sub-clause on the sign of the anharmonic correction). The C10 *tag* tracks two things: (i) is the scaling FORM `ρ_vac ~ H^n` with n≈2 substrate-grounded? and (ii) is it FULLY substrate-grounded (both factors)? The sign of a `±0.02` correction to the first factor's exponent affects NEITHER:

- For (i): under the from-below reading n_eff = 1.978, and under the T.61 cross-check n_eff = 2.030 — both round to 2, both inside [1.9, 2.1], both confirm "the scaling form is n≈2 and the exponent-on-q origin is the substrate quadratic minimum." The FORM-is-substrate-grounded claim is sign-insensitive.
- For (ii): full discharge is blocked by the q~H *relaxation closure* being an input, FULL STOP. That blocker is present regardless of the anharmonic sign. The sign-violation cannot DOWNGRADE a tag that is already held at PARTIALLY-PROVEN for an orthogonal reason (the missing second factor), and it cannot UPGRADE it either.

So a sign-violation-driven tag change would be a category error: it would let a fringe-sign result on the first factor move a tag whose held-status is set by the second factor. The honest Atlas-04 row is the one A5 wrote: SHARPENED (first factor now substrate-pinned), HELD ASSUMED-PARTIALLY-PROVEN (second factor still input), no PROVEN flip.

**Capstone §8.5 under reading A.** §8.5 (the q-flow qualifier) stays OPEN. The substrate-first prose that should land at session-close (designated-writer, per the capstone-hygiene gate Q4 routing) is: "the tracking-vacuum scaling exponent's CORE value of 2 is now substrate-derived — it is the curvature degree of the q-theory vacuum energy `ρ_vac(q) = ε(q) − q·dε/dq` at its q-stationary minimum, computed on the 992 D_K eigenfrequencies (the first time T.61 was evaluated on the spectrum); the GGE-pressure correction is bounded (`|C|/2 < 0.05`) by the Ordered-Veil integrability; what remains for an unconditional q-flow closure is a substrate derivation of the `q ~ H` relaxation linearity, which currently enters as the simple-fluid closure." This is an ENRICHMENT (open→characterized-but-still-open), not a status DOWN-tag — §8.5 was already OPEN, and reading A does not over-claim it shut. Critically, the prose must NOT narrate §8.5 above its register status: the capstone-hygiene gate (`capstone-hygiene-gate.md` Q3) requires the prose tag = register tag. INFO label → "characterized, still open," not "closed."

**The substrate-first framing is preserved either way.** Whether the label reads INFO or FAIL, the explanation direction is unchanged (per `phononic-framing.md`): the CC IS the substrate's out-of-equilibrium q-flow departure from its `ρ_vac(eq) = 0` equilibrium (EQUILIBRIUM-CC-WARRANT S95, exact, representative-independent), and the "2" in `H²` IS the substrate's own quadratic V(q) curvature degree — NOT a dark-energy field tuned IN an FRW container. A status down-tag (had we chosen FAIL) would NOT have inverted this arrow; it would only have scoped the confidence on the FIRST factor lower. Since the first factor's CORE is confirmed and only its fringe-sign is in question, neither label touches the substrate-IS reading.

**Where this cuts against me (steelman honesty).** If mack establishes (V2 Q2b) that the SIGN of `Δn ~ 0.02` IS observationally consequential — e.g., that n<2 vs n=2 measurably changes the BBN or present-epoch tracking extrapolation — then the C10 *row should carry an explicit sign-robustness annotation* even though the TAG stays PARTIALLY-PROVEN. That is, the held-status is robust to the label (my V3 claim survives), but the Atlas-04 row's narrative would owe a "from-below direction flagged, opposite-sign T.61 cross-check, sign not pinned to ±0.02" caveat that the current A5 row does not carry. I concede that addition is warranted IF mack's V2 answer is "yes, the sign matters observationally." I do not concede a tag CHANGE — only a row ANNOTATION.

**Questions for mack (V3).**
- Q3a. Do you agree the held-status (ASSUMED-PARTIALLY-PROVEN, sharpened) is robust to the INFO-vs-FAIL label — i.e., that the label question and the disposition question are SEPARABLE (the workshop brief's framing)? Or do you read the sign-violation as substrate-falsifying the C10 *form-claim* itself (not just the fringe), which WOULD warrant a tag change?
- Q3b. Should the Atlas-04 C10 row carry a sign-robustness annotation (from-below measured vs from-above T.61, sign not pinned to ±0.02) regardless of the label? My V3 position: yes-annotation, no-tag-change. I want your sole-writer judgment on the §7-surface-adjacent wording (the C10 row is Atlas-04, but its falsifier-realization touches your §7 domain).

### V4: Cross-Cutting Observations

**The ρ_vac/ρ_obs = 1.032 consistency axis (adjudication axis d) — the load-bearing cross-cut.** This is where the from-below sign question contacts a CANONICAL framework number, and it is the axis I most want mack's adjudication on. The chain:

- `rho_vac_over_rho_obs = 1.032` is CANONICAL (knowledge-MCP; FRAMEWORK-PREDICTION; gate DILUTION-CC-66/S66 Scenario B; promoted to `canonical_constants.py` THIS session by W6-1, `S97-W6-1-OMDM-RHOVAC-PINS` PASS, audit `4ec12df8`).
- The W6 promotion CARRIES C10-conditionality explicitly (knowledge-MCP note: "C10 (Atlas-04) ρ_vac ~ M_Pl² H² ASSUMED-PARTIALLY-PROVEN — conditionality carried so the pin does not overstate its register status"; housekeeping A15: "rho_vac_over_rho_obs=1.032 FRAMEWORK-PREDICTION, DILUTION-CC-66, C10 conditionality carried").
- DILUTION-CC-66 Scenario B assumes `ρ_vac ~ M_Pl² H²` — i.e., **n = 2 EXACTLY, held through the entire expansion history** (knowledge-MCP T.61 context: "The Volovik relaxation assumes ρ_vac ~ H² M_Pl² through the entire expansion history. This is the specific tracking law with exponent n = 2").

**Is the present-epoch ρ_vac/ρ_obs = 1.032 realization SIGN-INSENSITIVE to the from-below correction? (My answer: at present epoch YES; the risk is at BBN.)** The substitution chain, done carefully because this is a magnitude claim across a huge lever arm (Double-Check Logic, `math-scripts.md`):

```
Step 1:  ρ_vac(a) / ρ_vac(today) = [H(a) / H_today]^n          [tracking law, exponent n]
Step 2:  the DILUTION-CC-66 closure pins ρ_vac(today)/ρ_obs = 1.032 at n = 2 EXACTLY
Step 3:  the present-epoch RATIO 1.032 is read at a = a_today (H = H_today),
         where [H/H_today]^n = 1^n = 1 FOR ANY n.
⇒        at present epoch, the ratio 1.032 is INDEPENDENT of n.
```

So the headline number `1.032` — read at today — is **sign-INSENSITIVE** to whether n = 1.978, 2.000, or 2.030. The exponent only matters when you EXTRAPOLATE the tracking law across a redshift lever arm (`[H(a)/H_today] ≠ 1`). At present epoch the lever arm is unity and the from-below correction cannot move the 1.032 realization. **This protects the W6-promoted pin from the W2-2 sign result.** Good news for the framework, and it is the honest reading: the workshop brief's adjudication axis (d) asks "does from-below change the 1.032 realization, or is it sign-INSENSITIVE?" — my answer is sign-insensitive at the point where 1.032 is defined.

**Where the lever arm is NOT unity — the BBN exposure (Window-8 / BBN-VOLOVIK-67).** The danger is at high redshift, where `[H(a)/H_today]` is enormous and `n` is raised to that large ratio. The framework already carries this as an OPEN channel: Window-8 / BBN-VOLOVIK-67 (`ρ_vac/ρ_rad = 0.67 at nucleosynthesis`, z ~ 10⁹; atlas-05; OPEN since S67, partial PASS at S72, xcorr OPEN since S85 W4). At BBN, `H_BBN/H_today ~ 10²⁷`-ish, so `(H_BBN/H_today)^n` with `n = 1.978` vs `n = 2.000` differs by `(H_BBN/H_today)^{−0.022}`. With `log10(H_BBN/H_today) ~ O(27)`, that is `10^{−0.022 × 27} ≈ 10^{−0.6} ≈ 0.25` — a FACTOR-OF-FEW shift in the BBN-epoch `ρ_vac/ρ_rad`. **This is NOT negligible at BBN.** So my honest cross-cut:
- Present epoch (1.032): sign-INSENSITIVE. The W6 pin is safe.
- BBN epoch (0.67): sign-SENSITIVE at the factor-few level. from-below (n<2) makes `ρ_vac` dilute SLIGHTLY FASTER going to high z relative to n=2, i.e., REDUCES the BBN-epoch `ρ_vac/ρ_rad` below 0.67 — which, if anything, RELIEVES the BBN over-contribution pressure (a lower vacuum energy at nucleosynthesis is easier on the `ΔN_eff` budget). But the sign-robustness caveat (V2: the T.61 cross-check gives n>2, which would WORSEN it) means I cannot assert the BBN direction confidently. **This is a real, computable cross-cut and it is exactly mack's domain (Window-8 is a mack-owned BBN channel).**

Let me write the BBN sign-direction substitution chain explicitly since it is a direction claim:
```
Claim: "n < 2 (from below) reduces ρ_vac/ρ_rad at BBN relative to n = 2"
Step 1: ρ_vac(z_BBN)/ρ_vac(today) = [H(z_BBN)/H_today]^n,  with H(z_BBN)/H_today =: X ≫ 1
Step 2: ρ_rad(z_BBN)/ρ_rad(today) = (1+z_BBN)^4 ∝ X^{8/3} (rad-dom: H ∝ a^{-2} ⇒ a ∝ X^{-1/2}, ρ_rad ∝ a^{-4} ∝ X^2; in rad-dom H²∝ρ_rad so ρ_rad ∝ H² ∝ X² exactly)
Step 3: [ρ_vac/ρ_rad](z_BBN) ∝ X^n / X^2 = X^{n−2}
Step 4: n − 2 = C_correction = −0.022 < 0, and X ≫ 1 ⇒ X^{n−2} = X^{−0.022} < 1
⇒ from-below (n<2) gives [ρ_vac/ρ_rad](BBN) SMALLER than the n=2 value 0.67 (by ~X^{−0.022}).
Conclusion: from-below RELIEVES the BBN vacuum over-contribution (lower ρ_vac/ρ_rad at nucleosynthesis).  [direction valid ONLY under the measured-anharmonicity sign; T.61 cross-check sign would reverse it]
```
This is a genuinely NEW cross-cut the gate enables: the S97-W2-2 exponent correction feeds directly into the S67 Window-8 BBN channel, and the SIGN of the correction sets the direction of the BBN relief/strain. It is a carry-forward candidate (a BBN re-evaluation under n = 2 + C with C's sign-robustness band).

**Cross-cut to V1 (the label) once more.** The label question is now visibly decoupled from the magnitude/consistency question: the 1.032 pin survives under EITHER label and EITHER sign (present-epoch, sign-insensitive); the BBN channel is sign-sensitive but that is a SEPARATE open channel (Window-8), not a function of whether line 62 reads INFO or FAIL. So the workshop's framing holds at the cross-cut level: pin the LABEL (V1) and the sign-robustness (V2) as the deliverables; the held-status (V3) and the 1.032 realization (V4) are robust to the label.

**Cross-cut to the DI1 guard.** W2-2 is on the q-flow CC-closure-dynamics axis and shares NO inputs with W2-1 (a₂/a₀ object-definedness) — the WP DI1 guard is explicit and correct. So this adjudication does not touch W2-1's INFO or W2-3's PASS. The only cross-pillar contact is W6's 1.032 pin, which I have shown is present-epoch sign-insensitive.

**Summary of reading-A positions (for the Verdict table).**
1. **Label (axis a):** INFO is correct as a ONE-OFF (the gate's pre-registered INFO_meaning governs; the mechanical FAIL is the retained 3-tuple annotation; Class-3 firewall intact via the Option-A chain). General `gate-verdicts.md` CORE-vs-fringe clause is a K=1 SUGGESTION candidate, NOT minted now. [Leaning; mack to test the plan-text reading.]
2. **Sign-robustness (axis b):** Boundedness is ROBUST (gap-set Jacobian, Ordered-Veil integrability, truncation-invariant). From-below DIRECTION is the more-complete-estimator reading but is NOT single-method-invariant (T.61 cross-check is same-OOM opposite-sign). CORE (n→2) is method-invariant. [Honest split — the sign is the live uncertainty.]
3. **C10 disposition (axis c):** HELD ASSUMED-PARTIALLY-PROVEN, sharpened; robust to the label; sign-violation warrants a row ANNOTATION (if mack establishes observational sign-sensitivity) but NOT a tag change. §8.5 OPEN, enriched not closed.
4. **ρ_vac/ρ_obs consistency (axis d):** present-epoch 1.032 is sign-INSENSITIVE (lever arm = 1); the W6 pin is safe; BBN (Window-8, 0.67) IS sign-sensitive at the factor-few level — a NEW computable cross-cut into the S67 channel.

**Open questions for mack (consolidated, V4).**
- Q4a. (axis d, the key one) Do you concur that present-epoch 1.032 is sign-insensitive (lever arm unity) while BBN `ρ_vac/ρ_rad = 0.67` is sign-sensitive at `X^{n−2} ~ X^{−0.022}` ≈ factor-few? And is the BBN re-evaluation under n = 2 + C (with C's sign band) worth a carry-forward into the Window-8 / BBN-VOLOVIK-67 channel you own?
- Q4b. My BBN direction chain says from-below (measured C<0) RELIEVES the BBN vacuum over-contribution. The T.61 cross-check (C>0) would STRAIN it. Given Window-8 has been open since S67 with the BBN over-contribution as the worry — does the sign-robustness uncertainty in W2-2 make the BBN channel MORE urgent (because we now know the exponent is not exactly 2 and the sign sets the direction of relief/strain), or is `X^{−0.022}` a factor-few that the existing partial-PASS BBN bound already absorbs?
- Q4c. (process) Do you want CF-S98-W2-2-RELAXATION-CLOSURE (the q~H derivation, the second factor) and a NEW BBN-exponent-sensitivity CF treated as ONE wave or TWO? The relaxation closure discharges C10's tag; the BBN sensitivity tests the realization at high z. They are separable but both downstream of this gate's V(q) curve.

---

## Round 1 — mack: Response & Cross-Synthesis

### Part 1: Response to volovik's Sections

#### Re: V1 — [SIGN]-trigger label precedence

**AGREE (on the label as emitted; the FAIL line stays loud).** I accept V1's core: the canonical label INFO is admissible for THIS gate, the mechanical composite-collapse `sign=FAIL ⇒ FAIL` is the honest 3-tuple annotation retained at line 63, and the Option-A chain (`30566894`→`0e6076f3`→`b69da9f4`, all retained byte-level, each successor `supersedes=`) discharges the Class-3 firewall by construction. I do NOT seek to flip the canonical label to FAIL. V1's concession 3 ("the honest scalar is FAIL, and it should stay loud") is exactly right and is the part I will defend hardest downstream — the sign-violation must not be buried.

**Q1a (the load-bearing one) — the plan-text reading is the arbiter, and it LICENSES the CORE-plus-conditional-fringe decomposition.** I read the WP §W2-2 Verdict's pre-registered Direction as V1 states it: a CORE claim (`n_eff → 2 in the bounded limit q→0`) PLUS a conditional sub-clause (`IF the GGE pressure correction is positive-and-small THEN n_eff ≥ 2`). The dispositive substrate-physics fact that makes this NOT a post-hoc rescue is the conditional's antecedent: the sub-clause is gated on `correction positive`. The measured `C_correction = −0.021889 < 0` makes the antecedent FALSE. A conditional with a false antecedent does not falsify the implication; the consequent `≥2` simply does not obtain. This is the structurally honest reading of the plan text — PROVIDED the antecedent direction (sign of C) is itself robust. And THAT is where my agreement becomes conditional on V2/V3: if the from-below sign is normalization-dependent (V2's same-OOM opposite-sign T.61 cross-check), then "the antecedent is false" is itself a normalization-shopped statement, and the CORE-plus-fringe decomposition loses its protection. So my answer to Q1a is: **the plan text licenses the decomposition, and the INFO label is admissible — CONDITIONAL on the from-below sign being substrate-robust, which I argue in Re-V2 it is (via a different mechanism than V1/V3 propose).** Absent that robustness, the decomposition would read as a literal `n_eff ≥ 2` cell that was violated 1.978 < 2, and INFO would be a rescue.

**Q1b (one-off vs general clause) — I LEAN one-off NOW, but with a sharper general-clause GUARD than V1's.** V1 leans "one-off now, general-clause candidate at K=3" and I concur on the timing. But the general clause V1 sketches ("compute `sign_verdict` against the CORE; record the sub-clause violation as a 3-tuple annotation") has a hole my observational-bridge domain sees: it does not require the from-below/from-above DIRECTION to be normalization-invariant before the INFO override fires. The guard I would add to any future general `gate-verdicts.md` clause is a fourth predicate beyond V1's: *(iv) the boundary-limit recovery DIRECTION (from-below vs from-above) must be normalization-invariant, or the gate must declare which observable the recovery predicate is about.* Without (iv), the INFO override is hostage to scheme choice — the gate could "recover n=2 from below" in one normalization and "from above" in another, and the INFO_meaning's "one-signed correction" qualifier becomes a normalization artifact. This is the K=3-candidate clause-diff I would support, and it COUPLES the label question (V1) to the sign-robustness question (V2/V3) — they are not separable at the general-clause layer even though they are separable for THIS gate's canonical label.

**MISSED (the methodology-vs-observational split).** V1 frames Q1b's resolution as "mack's call, observational-side." Partly: the clause-diff to `gate-verdicts.md` is a methodology-rule extension (housekeeping §D, M1-M4 allowlist per `Investigating-Workshops.md` Q2 routing) — we co-author its FORM. What IS purely my domain is whether the from-below DIRECTION carries enough observational import (MK2) to deserve preservation in the disposition at all. The label MECHANICS are methodology; the SIGN's survival in C10's disposition is observational. I keep that split sharp so neither of us over-claims the other's territory.

**EMERGES.** The schema-v2 3-tuple's whole purpose (sign cannot be buried inside a magnitude PASS) is satisfied HERE only because the Option-A chain makes the FAIL byte-permanent. The general-clause danger V1 flags is real precisely because a careless future author could pre-register a CORE-plus-fringe shape, get a sign-FAIL, and INFO-it WITHOUT the byte-level FAIL retention. The forward fix is structural: any general clause must mandate the 3-tuple `sign=FAIL` annotation row AND my guard (iv). One-off now; guarded-general at K=3.

#### Re: V2 — From-below correction robustness

**This is the most important thing V2 surfaced, and it is the single biggest threat to my own MK1/MK2, so I take it head-on.** V2 computed `C` two ways: the full-spectrum measured anharmonicity (`C = −0.021889`, from BELOW) and the gap-set T.61 mode-sum (`C = +0.029719`, from ABOVE) — same OOM, OPPOSITE sign. V2's honest position is that boundedness is robust but the from-below DIRECTION is "the more-complete-estimator reading" and NOT single-method-invariant. I agree with the boundedness and I want to SHARPEN the direction argument beyond "the full-spectrum estimator is preferred because it integrates the actual ρ_vac(q) shape."

**AGREE (boundedness is robust; the Jacobian + Ordered-Veil argument is correct).** `|C|/2 = 0.0109 < 0.05` under BOTH estimators, so `n_eff ∈ [1.9, 2.1]` is method-invariant. V2's gap-set Jacobian `dω_n/dq = 1/(2ω_n)` bounded above by `1/(2|λ_min|)`, forced by the gap keeping `ω_n` away from zero (relaxation window stays above `q_boundary = −λ_min² = −0.67197549`), and the Ordered-Veil integrability constraining `dp_k/dH` — this is the substrate-physics ceiling, and it is robust to the 8-vs-984 occupation truncation because it is a statement about which variable sets `ω_k`, not about occupation weights. I defend this as hard as V2 does.

**Q2a — the full-spectrum measured anharmonicity (C = −0.022, from below) is the physically-preferred estimator, and the OPPOSITE-sign T.61 cross-check HELPS my case rather than undercutting it — but only via a mechanism V2 did not state.** V2's reason ("more complete, integrates the actual ρ_vac(q) shape") is correct but soft. Here is the sharper one, as a substitution chain because it is a SIGN claim (`math-scripts.md`):

```
Claim: "the measured-anharmonicity C and the gap-set T.61 mode-sum C are exponents of DIFFERENT
        observables; only the measured one is the exponent the n_eff=2 prediction is about."

Step 1: measured anharmonicity. C_meas = (exponent-on-q) − 2, where exponent-on-q is the small-q
        regression of d ln ρ_vac(q) / d ln q on ρ_vac(q) = ε(q) − q·dε/dq, the FULL q-theory vacuum
        energy off its q-stationary minimum (verdict: C_correction_measured = −0.021889).
Step 2: T.61 mode-sum. C_T61 = Σ_k (dp_k/dH) n_k / Σ_k ω_k n_k, evaluated on the 8 occupied modes'
        gap-set ω, a LEADING-ORDER pressure-response formula (verdict: C_modesum_gapset = +0.029719).
Step 3: the two compute different orders of the SAME ρ_vac(q) expansion. C_meas captures the FULL
        nonlinear δρ_vac(q) curvature beyond quadratic order (the q³ anharmonic term in the actual
        substrate-IS ρ_vac shape). C_T61 is the leading pressure-response, which by construction
        truncates at the order BELOW where the sign of a ±0.02 correction is decided.
Conclusion: the n_eff=2 prediction is a statement about the EXPONENT OF ρ_vac(q) — i.e., about C_meas
            (the full-shape exponent), NOT about the leading pressure-response C_T61. The sign that
            matters for "does ρ_vac ~ H^n approach n=2 from below or above" is C_meas's = NEGATIVE =
            from below. C_T61's positive sign is the sign of a leading-order proxy, not the observable.
```

So the opposite-sign T.61 cross-check HELPS me: it is NOT a competing measurement of the same observable that happens to disagree; it is a leading-order proxy whose sign need not match the full-shape exponent's sign, and its disagreement is EXPECTED at the sub-leading order where ±0.02 lives. The Mack-direction (from below) is the full-shape exponent's sign, and that is the one the tracking-law exponent n inherits. **This is exactly my memory's discharge-status two-axis split (S94 W-1): before forcing one sign, check whether the two values predicate over the SAME object. They do not — C_meas is the full-shape exponent, C_T61 is the leading proxy.**

**DISAGREE (mildly, with V2's framing) — the sign is NOT "the live uncertainty"; it is robust once you identify which observable each C measures.** V2 calls the from-below direction "a Track-A-leaning reading with a flagged sign-robustness caveat, NOT a claim I will assert at the same confidence as boundedness." I am MORE confident than V2 here, for the Step-1-to-3 reason: the sign-robustness "caveat" dissolves once you stop treating C_meas and C_T61 as two estimators of one number. They are exponents of two different functionals. The from-below sign of the OBSERVABLE (the full ρ_vac(q) exponent) is robust; the from-above sign of the PROXY is also "robust" but irrelevant to the prediction. The uncertainty V2 flags is a same-object illusion.

**MISSED / the decisive cross-check (the q→0 limit test).** Neither V2 nor I have run the test that would settle Step 1-3 cleanly: evaluate the T.61 gap-set mode-sum's exponent in the q → 0 LIMIT, not just at the probe scale. My reading PREDICTS the two C's are different-order objects; the cleanest confirmation is that they have different q → 0 behavior. If the T.61 proxy and the full-shape exponent converge as q → 0 (both → the same value), my "different observable" reading is wrong and the sign IS method-dependent (V2's caveat stands, I withdraw). If they stay distinct, my reading holds. This is the lead MK3 item.

**Q2b — is any observable sensitive to the SIGN of a ±0.02 exponent correction (n=1.978 vs 2.030 vs 2.000)?** This is my domain. At PRESENT epoch: NO (lever arm = 1; MK2-Q4a; I concede this fully to V4). At BBN: YES, the lever arm `X^(n−2) = X^(−0.022)` with `log10 X ~ 18-27` amplifies the ±0.02 to an O(1) factor (MK2-Q4b). So the sign-robustness question is NOT academic for the falsifier surface — it is academic at z=0 and live at z~10⁹. V2's framing ("if no probe resolves Δn~0.02, only boundedness matters") is right at the present epoch and wrong across cosmic history. The BBN channel (Window-8) is exactly where Δn~0.02 stops being academic, and that is mack-owned. I develop this in MK2.

**EMERGES.** The workshop's central confusion is a single-observable-vs-two-observables question (`cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"`): is "the C10 dispersion/departure exponent" ONE substrate-IS observable under two normalizations, or TWO observables (full-shape vs leading-proxy)? My Step 1-3 says two, and the q→0-limit test (MK3) is the discontinuity-vs-continuity discriminant that licenses or forbids treating them as distinct. If they are distinct, the slot-split is licensed and the from-below sign is robust; if continuous, they are one object and the sign is a methodology-floor F-image at the regulator-class axis (V2's caveat). The filter is the structural lens that resolves V2 — and it is computable.

#### Re: V3 — C10 disposition

**AGREE (the held-status is robust to the label; the two-factor decomposition is correct).** V3's structural point is right and I adopt it: C10's tag tracks (i) is the scaling FORM `ρ_vac ~ H^n` with n≈2 substrate-grounded, and (ii) is it FULLY substrate-grounded (both factors). The W2-2 gate moved the FIRST factor (exponent-on-q → 2, the curvature degree of ρ_vac at its q-stationary minimum, k = +3586.5 from 992 D_K modes) from ASSUMED to DERIVED; it did NOT move the SECOND factor (the q ~ H relaxation linearity, still the simple-fluid closure). So C10 is `ASSUMED-PARTIALLY-PROVEN`, sharpened — exactly the A5 row. I AGREE the sign-violation does not DOWNGRADE the tag (the held-status is set by the missing second factor, an orthogonal reason) and does not UPGRADE it.

**Q3a — YES, the label question and the disposition question are SEPARABLE (the workshop brief's framing holds); the sign-violation does NOT substrate-falsify the C10 FORM-claim.** I do NOT read the sign-violation as falsifying the form. The form-claim is "ρ_vac ~ H^n with n≈2"; under from-below n=1.978 and under the T.61 proxy n=2.030, both round to 2, both inside [1.9, 2.1], so the FORM is sign-insensitive (V3 is right). What the sign-violation falsifies is the narrower one-sided STIFFNESS prediction `n_eff ≥ 2`, which is a property of the first factor's fringe, not the form. So: tag held (Q3a YES), form not falsified, but — and here I diverge from V3 — the DISPOSITION owes more than V3's "row annotation only."

**DISAGREE (on annotation-only vs the two-layer split) — and this is conditional on my V2/MK2.** V3's position is "yes-annotation, no-tag-change," with the annotation owed ONLY IF I establish (Q2b) that the sign matters observationally. I DO establish that (MK2-Q4b: BBN lever-arm sign-sensitivity). So the annotation is owed by V3's own conditional. But I go one step further: the annotation should not be a free-text caveat appended to an otherwise-unchanged A5 row; it should be a STRUCTURED two-layer split mirroring my memory's S94 W-1 STRUCTURAL-ORTHOGONAL-COMPANION pattern:
- **Layer 1 (acoustic-leading/quadratic-origin content)**: SHARPENED-CONFIRMED — first factor substrate-pinned, form n≈2, sign-insensitive. V3's correct part.
- **Layer 2 (approach-direction + BBN realization)**: from-below sign robust under the two-observables reading (Re-V2; pending MK3 q→0 test) AND BBN sign-sensitive (MK2-Q4b; pending the BBN vacuum-fraction compute). This layer carries the sign-robustness caveat AND the BBN-conditionality.

The tag stays `ASSUMED-PARTIALLY-PROVEN` (Q3a YES, no change), but the ROW gains the two-layer structure, not a one-line caveat. The difference matters because the BBN-conditionality (Layer 2) is load-bearing downstream (Window-8), not cosmetic.

**Q3b — YES annotation, and it is my sole-writer domain to land the §7-adjacent wording.** I agree with V3's "yes-annotation, no-tag-change" and confirm the §7 falsifier-realization wording is mine (`feedback_mack-bridge-role.md`; the A5/A7 housekeeping already routes the §7 surface to me). My proposed §7-adjacent wording for the C10 row's falsifier-realization: *"C10 first-factor (V~q² quadratic origin) substrate-pinned (SHARPENED); approach-direction from-below (full-shape exponent C_meas = −0.0219; leading-proxy T.61 C = +0.0297 is a different-order object, not a competing sign; q→0-limit cross-check pending CF); present-epoch ρ_vac/ρ_obs=1.032 sign-INSENSITIVE (lever=1); BBN-epoch ρ_vac/ρ_rad realization sign-SENSITIVE (X^(n−2) lever, Window-8 cross-check pending CF)."* This is the annotation; it does NOT narrate the tag above its register status.

**The capstone-hygiene constraint V3 invokes is exactly right and binds me too.** V3 correctly cites `capstone-hygiene-gate.md` Q3: §8.5 prose must equal the register status; the gate's canonical label is INFO, so §8.5 reads "characterized, still open," NOT "closed." I add the same discipline to my §7-surface annotation: the falsifier row narrates C10 at INFO/PARTIALLY-PROVEN, with the two-layer split disclosing what is confirmed (Layer 1) and what is pending (Layer 2). No section narrates C10 above its register status. V3's §8.5 ENRICHMENT prose (open→characterized-but-still-open) is correct; my §7 annotation is its falsifier-surface companion.

**MISSED (the disposition is one closed gate too early for V3's annotation to be final).** V3's annotation presumes the from-below sign is established enough to write into the row. It is established under my two-observables reading (Re-V2) but that reading needs the MK3 q→0-limit confirmation. So the HONEST interim disposition is: tag held (Q3a), two-layer ROW landed (Q3b/my split), with Layer 2's sign-robustness clause carrying an explicit `pending CF-MK3-1 (q→0-limit sign-robustness)` pointer per `capstone-hygiene-gate.md`'s "STATUS: unreconciled — see <forward gate>" discipline. I land the structure now; the sign-robustness clause discharges or holds at S98. (Final disposition is the Round-2 Verdict; this is my Round-1 position.)

#### Re: V4 — Cross-Cutting

**V4 is the cross-cut I most wanted, and volovik did most of my job for me — I AGREE with the present-epoch sign-insensitivity in full and EXTEND the BBN cross-cut with the observational adjudication V4 invited.**

**Q4a — YES, present-epoch ρ_vac/ρ_obs = 1.032 is sign-INSENSITIVE; V4's lever-arm = 1 chain is exactly right.** I confirm V4's substitution chain: the 1.032 ratio is read at `a = a_today` where `[H/H_today]^n = 1^n = 1` for ANY n, so n = 1.978 / 2.000 / 2.030 all give the same present-epoch number. The W6-promoted pin (`rho_vac_over_rho_obs = 1.032`, FRAMEWORK-PREDICTION, DILUTION-CC-66, audit `4ec12df8`, C10-conditionality carried) is SAFE from the W2-2 sign result. I cross-checked the canonical entry directly: it is non-superseded and the C10-conditionality is explicitly carried in its provenance note. The from-below correction cannot move 1.032. This is good news for the framework and I do not hedge it.

**Q4a continued — YES, the BBN re-evaluation under n = 2 + C is worth a carry-forward into Window-8.** V4 asks whether the BBN re-eval is CF-worthy. It is, and it is mine to own (Window-8 / BBN-VOLOVIK-67 is the mack-owned BBN channel). I confirm the channel state from the registry: Window-8 `ρ_vac/ρ_rad = 0.67 at nucleosynthesis, z ~ 10⁹`, OPEN since S67, partial PASS at S72, xcorr OPEN since S85 W4. The W2-2 exponent feeds this channel directly.

**Q4b — the BBN sign-direction, and a correction to V4's magnitude.** V4's direction chain (n < 2 from below REDUCES ρ_vac/ρ_rad at BBN, RELIEVING the over-contribution) is structurally correct, and I confirm the read-off: `[ρ_vac/ρ_rad](z_BBN) ∝ X^(n−2)`, and with `n−2 = −0.022 < 0` and `X ≫ 1`, `X^(−0.022) < 1` ⇒ from-below SUPPRESSES the BBN vacuum fraction below 0.67. The direction is RELIEF (good for the ΔN_eff budget), UNDER the measured-anharmonicity sign; the T.61 proxy sign would reverse it. But note my Re-V2 Step 1-3: the T.61 proxy is a different-order object, so I read the relief direction as the robust one (the full-shape exponent's sign governs the tracking-law exponent), not as a coin-flip. So I am MORE confident than V4 that the direction is relief.

The magnitude: V4 used `log10 X ~ 27` giving `X^(−0.022) ≈ 10^(−0.6) ≈ 0.25`. I get a smaller lever depending on what "H at BBN" means. If X = H_BBN/H_today in radiation domination with H ∝ (1+z)² and z_BBN ~ 10⁹, then `log10 X ~ 2·9 = 18`, giving `X^(−0.022) = 10^(−0.40) ≈ 0.40` (my MK2-Q4b chain). V4's `~27` would follow if X is taken to the GUT/transit scale rather than BBN. Either way the conclusion is identical and load-bearing: **the suppression factor is O(1) (0.25–0.40), NOT negligible** — the ±0.02 "small" exponent correction is amplified to a factor-few by the 18-27-decade lever. The exact lever is an MK3 compute; the qualitative cross-cut (BBN sign-sensitive, relief direction) is robust to which scale anchors X.

**The observational adjudication V4 invited (does the sign-robustness make Window-8 MORE urgent?).** Three categories (`output-standards.md`):
- **More urgent: YES, structurally.** Before W2-2, Window-8 carried `ρ_vac/ρ_rad = 0.67` as a fixed n=2 number. Now we know n ≠ 2 exactly and the SIGN sets the direction of BBN relief/strain. The partial-PASS at S72 was computed at n=2; a from-below n=1.978 SHIFTS the BBN number by factor ~0.40 (relief), which the existing partial-PASS bound may or may not already absorb depending on its margin.
- **But the urgency is bounded by the BBN vacuum FRACTION (the honest caveat).** A factor-0.40 shift on `ρ_vac/ρ_rad = 0.67` moves it to ~0.27 — a large fractional change. Whether that is observationally consequential depends on whether the partial-PASS bound was margin-limited at 0.67 or had headroom. If headroom, the relief is a comfort; if margin-limited, the shift matters. I cannot adjudicate that without the S72 partial-PASS margin, which is the MK3-2 / CF input.
- **Does it relax or tighten the existing bound?** RELAX, under the measured (from-below) sign — a lower BBN vacuum energy is easier on ΔN_eff. So the W2-2 sign result is, if anything, BBN-favorable. But "favorable" is not "negligible," and the factor-few magnitude means the BBN channel deserves a re-run, not a wave-through.

**Q4c (process — ONE wave or TWO).** TWO conceptually, but they can be ONE wave with two pre-registered sub-gates AND'd: CF-S98-W2-2-RELAXATION-CLOSURE (the q~H second-factor derivation; discharges C10's TAG) is separable from the BBN-exponent-sensitivity CF (tests the realization at high z; discharges Layer-2 of my C10 row). They share the V(q) shape from this gate but test orthogonal things (tag vs realization). I lean: ONE wave, two sub-gates, AND-closeout — because both are downstream of the same V(q) curve and routing them together keeps the C10 disposition coherent (the tag and the realization discharge in the same session). This is a Q3 parallel-compute-wave structure (`Investigating-Workshops.md` Q3), not a workshop.

**MISSED (the cross-cut V4 did not draw — the BBN sign feeds my §7 falsifier surface, not just Window-8).** V4 routed the BBN re-eval to Window-8. But the from-below exponent ALSO touches the §7 falsifier-realization surface I own: if BBN is sign-sensitive, then BBN light-element abundances become a (weak) DISCRIMINANT on the C10 exponent — a falsifier-realization channel that did not exist when n was assumed exactly 2. That is a §7 row addition (mack sole-writer), distinct from the Window-8 internal-consistency check. I flag it here; the actual row lands when the BBN CF closes (it would be premature to write a falsifier row on an unevaluated lever).

**EMERGES.** V4's decoupling claim ("the 1.032 pin survives under either label and either sign; the BBN channel is a separate open channel") is correct and is the cleanest statement of the workshop's structure: label (V1) and present-epoch realization (V4-Q4a) are robust; sign-robustness (V2/V3) and BBN realization (V4-Q4b, MK2) are the live items, and they are COUPLED to each other (the BBN sign-sensitivity is what makes the from-below sign worth pinning) but DECOUPLED from the canonical label. The 1.032 number does not care which line reads INFO; the BBN number cares which sign is real.

### Part 2: Original Analysis

#### MK1: Directional-prediction-violated reading — from-below is the Mack-direction signature

The header frames my reading as "the directional prediction `n_eff ≥ 2` was substrate-FALSIFIED." I want to sharpen that to something more precise and more interesting than a bare falsification, because "FALSIFIED" overstates what the gate shows and undersells what it found.

**What was falsified, and what was not.** The [SIGN] pre-registration `n_eff ≥ 2` encoded a STIFFNESS expectation: the substrate dispersion is acoustic or super-acoustic, approaching 2 from above. The substrate said NO to "≥": it returned `n_eff = 1.978`, sub-acoustic at the probe scale, approaching 2 from below (`C_correction_measured = −0.021889 < 0`). So the INEQUALITY direction was violated — the substrate is SOFTER than acoustic at finite q, not stiffer. That is a genuine sign-FAIL, and the 3-tuple records it honestly (`sign_verdict=FAIL`). But "≥2 falsified" is NOT "n=2 falsified": n=2 is recovered exactly in the q → 0 limit (`exponent-on-q → 1.99920 at q ≤ 0.005`). What was falsified is the STIFFNESS direction, not the acoustic-leading value. The distinction is the whole substance of my reading.

**Why from-below is the "Mack-direction signature" — and the contrast with the free-redshift n ~ 1.778.** This is the heart of MK1, and the verdict file gives me both numbers to make it precise: `n_eff_T61 = 1.978111` (the gate) vs `n_eff_mack_contrast = 1.778149` (the S66 free-redshift estimate). The free-redshift estimate (S66 OQ-3) gave n ~ 1.778 — far below 2, because a FREELY-REDSHIFTING mode (`ω ~ a⁻¹` adiabatic, the gap NOT used) has no spectral floor to pin it: the exponent can sag arbitrarily (here `2 − ⅓·f_acoustic` with f_acoustic = 39.8/59.8). That is the "Mack-contrast direction" in its naive, unbounded form: softness with nothing underneath. The gate result n = 1.978 is in the SAME direction (below 2) but is BOUNDED — it sags only 1.1%, and it climbs back to 2 as q → 0. The gap between 1.778 (free) and 1.978 (gated) IS the substrate-physics story:

> The gap `√(λ_n² + q)` is a real spectral feature of D_K's bottom-K ladder. It is the FLOOR the free-redshift estimate lacked. With the floor, the dispersion is **bounded-not-free**: it can soften toward sub-acoustic (Mack-direction, n < 2) but the gap prevents it from sagging to the free value 1.778 — it is pinned at 1.978, and the gap (`dω_n/dq = 1/(2ω_n)`, bounded) forces the q → 0 recovery to n = 2.

So from-below is the Mack-direction signature in this exact sense: the substrate CONFIRMS the qualitative direction my contrast predicted (softer than acoustic, n < 2) while REFUTING the quantitative free-redshift magnitude (1.778), because the substrate is D_K-bounded, not free-streaming. This resolves S66 OQ-3's open sign (per the W2 WP §"S66 OQ-3 resolution" and the MCP note "the S66 sign of the correction was left OPEN ... direction undetermined because it depends on whether ω_k is set by H or by the spectral gap"): the gate settled the ω_k-source question — `ω_n(q) = √(λ_n² + q)` is gap-set, so the free reading is EXCLUDED and the correction is D_K-bounded. The Mack-direction is VINDICATED as a direction and CORRECTED as a magnitude.

**Why this is the better outcome (not a defeat for my prior reading).** I will not frame the free-redshift refutation as a loss. A bounded softening (1.978) is observationally tractable and BBN-favorable (MK2); a free sag to 1.778 would have been in tension with the acoustic CMB peak and would have made the tracking-law extrapolation wild. The substrate gave me the direction I expected with a magnitude I should prefer. The honest claim is the weaker-and-truer one: **the substrate softens in the Mack direction but is bounded by the D_K gap** — not "free-redshift-soft" (1.978 ≠ 1.778), and not "stiff/acoustic-or-above" (n < 2, the ≥2 prediction is dead). Three facts, all load-bearing for MK2: the from-below sign is real, it is the Mack direction, and it is gap-bounded.

**Substrate-first framing.** The arrow holds FROM substrate TOWARD the observable (`phononic-framing.md`): `D_K eigenfrequencies ω_n(q) = √(λ_n²+q)` → `zero-point energy ε(q)` → `q-theory vacuum energy ρ_vac(q) = ε − q·dε/dq` → `the departure exponent n in the tracking law ρ_vac ~ H^n`. The exponent is NOT a property fitted to a dark-energy field IN an FRW container; it is the curvature degree of ρ_vac at its q-stationary minimum, read off the spectrum. The from-below correction is the substrate's own anharmonic q³ softening of that curvature — an intrinsic feature of the bottom-K ladder, not a phenomenological tilt imposed from outside.

#### MK2: BBN / observational-realization import — does from-below change ρ_vac/ρ_obs=1.032?

The realization question is whether the from-below exponent (n = 1.978 vs 2) changes the vacuum-tracking observable `ρ_vac/ρ_obs = 1.032` that DILUTION-CC closes (114 OOM closed via Volovik tracking vacuum; 1.032 is the present-epoch tracking residual, CC_OOM = 115.5). V1/V4 lean "sign-insensitive at the lever arm." I AGREE for the present epoch and DISAGREE for the BBN epoch — and the distinction is the whole point. Substrate-first: `ρ_vac/ρ_obs` is not a LCDM container quantity — it is a substrate-IS observable, the ratio of the spectral-action zeroth moment a₀ (vacuum) to the gravitating GGE-relic energy density (a₂ channel). The tracking IS the substrate's spectral floor following the expansion-driven complexification.

**Q4a — present epoch: sign-INSENSITIVE (I concede to volovik in full).**

```
Claim: "at the present epoch the from-below exponent does NOT change ρ_vac/ρ_obs = 1.032."
Step 1: tracking ansatz: ρ_vac(z)/ρ_vac,0 = [H(z)/H₀]^n     [vacuum floor tracks the expansion rate]
Step 2: deviation from the n=2 reference: D(z) ≡ ρ_vac(z;n)/ρ_vac(z;n=2) = [H(z)/H₀]^(n−2)
Step 3: present epoch H(z=0) = H₀ ⇒ H/H₀ = 1 ⇒ D(0) = 1^(n−2) = 1^(−0.022) = 1  for ALL n.
Step 4: lever arm [H/H₀]^(n−2) = 1 at z=0 regardless of exponent.
Conclusion: ρ_vac/ρ_obs = 1.032 is UNCHANGED by the from-below exponent at the present epoch.
```

Volovik's lever-arm = 1 reading is exactly right. The 1.032 present-epoch number does not move. The W6-promoted pin (`rho_vac_over_rho_obs = 1.032`, audit `4ec12df8`) is safe.

**Q4b — BBN epoch: sign-SENSITIVE, amplified by the lever arm.**

```
Claim: "at the BBN epoch the from-below exponent DOES shift the vacuum-tracking extrapolation."
Step 1: BBN redshift z ~ 10⁹; radiation domination H(z) ∝ (1+z)² ⇒ X ≡ H_BBN/H₀ ~ (10⁹)² ~ 10¹⁸
        (OOM lever; exact value is an MK3 compute; V4 used ~10²⁷ to the transit/GUT scale).
Step 2: deviation D_BBN = X^(n−2) = X^(−0.022)     [from-below: n−2 = 1.978 − 2 = −0.022]
Step 3: log₁₀ D_BBN = (n−2)·log₁₀ X = (−0.022)(18) = −0.396
Step 4: D_BBN = 10^(−0.396) ≈ 0.40.
Conclusion: a from-below exponent of n−2 = −0.022 SUPPRESSES the BBN-epoch vacuum-tracking extrapolation
            by factor ~0.40 relative to exact-n=2 — a ~60% shift, NOT negligible. The "small" 1.1%
            probe-scale correction is amplified to an O(1) factor by the 18-decade lever arm.
```

(With V4's `log10 X ~ 27`, D_BBN ≈ 10^(−0.6) ≈ 0.25 — same O(1) conclusion. The exact lever is MK3-2; the qualitative result is robust to which scale anchors X.) The DIRECTION is suppression = RELIEF: from-below makes ρ_vac dilute slightly faster to high z, lowering `ρ_vac/ρ_rad` below the n=2 value 0.67 (toward ~0.27), which EASES the ΔN_eff budget. Under my Re-V2 reading (the full-shape exponent C_meas governs, not the T.61 proxy), this relief direction is the robust one, not a coin-flip.

**The substrate-physics reading.** This is the cosmological analog of my permanent discipline that **"sub-leading at the probe scale" and "negligible across cosmic history" are different statements** — the same structure as the n_T 54-decade k-scale separation (transit-scale predictions are a geometric floor, not detector-comparable, until the lever arm is accounted for). Here the lever is 18-27 decades in H, and a 2.2% exponent deviation becomes a ~60% extrapolation shift. V1's "the n=2 part is what matters for the acoustic signature" is true where the lever is short (low-z BAO/CMB) and false where it is long (BBN vacuum tracking).

**The observational adjudication — and its honest caveat (the three categories, `output-standards.md`).**
- **What the data shows**: BBN light-element abundances (N_eff, ⁴He, D) constrain the radiation-era expansion history at the few-% level. A ~60% shift in a vacuum contribution at BBN would be observationally large IF the vacuum-tracking term is a non-negligible fraction of the BBN energy budget.
- **What it suggests**: the from-below exponent is observationally LIVE at BBN in a way it is not at z=0 — the realization import V4-Q4a set aside (correctly, for present epoch) is real at high z.
- **What it does NOT address (the caveat that keeps me honest)**: I have NOT shown that `ρ_vac(z_BBN)` is a non-negligible fraction of the BBN budget. The DILUTION-CC tracking is DESIGNED so ρ_vac tracks `M_Pl² H²`; if at BBN `ρ_vac/ρ_rad = 0.67` (the Window-8 number) then the vacuum is a ~40%-of-radiation contribution and a factor-0.40 shift on it (→ ~0.27) is large. If instead the tracking residual is smaller at BBN, the shift bites less. The sign-sensitivity is ESTABLISHED (factor ~0.40 lever amplification, rigorous); the OBSERVABILITY depends on the BBN-epoch vacuum fraction and the S72 partial-PASS margin, which is the MK3-2 compute. So my claim is bounded: **the from-below exponent is sign-sensitive at BBN (factor ~0.40 lever amplification, rigorous); whether that is observationally decisive depends on the BBN-epoch vacuum fraction and the existing partial-PASS margin, which are unevaluated.**

**Why this forces C10 to stay CONDITIONAL (the load-bearing downstream).** The W6 promotion of `rho_vac_over_rho_obs = 1.032` CARRIES C10-conditionality explicitly (verified in the canonical provenance note + housekeeping A15). Present-epoch (Q4a) is safe, but the BBN-epoch extrapolation (Q4b) is sign-sensitive, and Window-8 (BBN-VOLOVIK-67) is OPEN since S67. A promotion of C10 to CONFIRMED would silently discharge a conditionality the lever-arm math says is still live at z~10⁹. The 1.032 number is robust to the sign; the 0.67 BBN number is not. That asymmetry is exactly why the C10 disposition must be the two-layer split (Re-V3): Layer 1 (form/present-epoch) confirmed, Layer 2 (approach-direction + BBN realization) pending. The from-below sign is not cosmetic — it is the input that determines whether Window-8 relaxes or strains, and that channel is mine.

#### MK3: Questions for volovik

Three items, ordered by leverage. Items 1-2 are computes that would settle the workshop; item 3 maps concessions so Round 2 converges instead of restating.

**MK3-1 (the decisive compute — settles the V2/V3 sign-robustness; cheap, on the existing npz).** Re-run the T.61 gap-set mode-sum's exponent in the q → 0 LIMIT, not only at the probe scale, and report it against the measured-anharmonicity exponent-on-q in the same limit. My Re-V2 Step 1-3 reading is that C_meas (full-shape exponent, −0.0219) and C_T61 (leading pressure-response proxy, +0.0297) are exponents of DIFFERENT-order objects, so they need not share a sign and the from-below sign of the OBSERVABLE is robust. The clean discriminant:
- IF the T.61 proxy exponent and the full-shape exponent-on-q DIVERGE as q → 0 (different limits / different-order objects): my two-observables reading is CONFIRMED, the from-below C_meas is the prediction's sign, T.61's +0.0297 is an irrelevant proxy sign, and the sign-robustness tension dissolves. **You concede the from-below sign is robust.**
- IF they CONVERGE to the same value as q → 0 (one object, two estimators): my Step 1-3 is wrong, the sign IS method-dependent, and **I concede** — I withdraw the from-below import to a probe-scale-only statement and retract the BBN sign-sensitivity (MK2-Q4b). 
Either way we converge on the existing data. This is the lead item.

**MK3-2 (the realization compute — settles MK2 observability; a CF, but pre-register the gate now).** Two coupled quantities: (a) the exact lever `X = H_BBN/H₀` (I used OOM 10¹⁸ in rad-dom; V4 used ~10²⁷ to the transit scale — pin which scale anchors X), and (b) the BBN-epoch vacuum FRACTION `ρ_vac(z_BBN)/ρ_total(z_BBN)` under DILUTION-CC tracking, PLUS the S72 partial-PASS margin on Window-8. The disposition hinges on this:
- IF ρ_vac/ρ_total at BBN is %-level (tracking keeps the vacuum a real fraction, consistent with the 0.67 Window-8 number) AND the S72 margin is tight: the factor-~0.40 from-below shift is BBN-observable, C10's Layer-2 stays CONDITIONAL, and Window-8 needs a genuine re-run at n = 2 + C.
- IF the vacuum fraction is tiny or the S72 margin has headroom: the shift is a comfort (relief direction), C10's Layer-2 BBN-conditionality is benign, and your "the BBN channel already absorbs X^(−0.022)" reading (V4-Q4b) strengthens.
This is CF-worthy (Window-8 is mine); route it together with CF-S98-W2-2-RELAXATION-CLOSURE as a Q3 parallel-wave (Q4c: ONE wave, two sub-gates AND'd — tag-discharge + realization-discharge share the V(q) curve).

**MK3-3 (concession mapping — so Round 2 lands, not loops).** I have already conceded TWO things to you: (a) the present-epoch ρ_vac/ρ_obs = 1.032 is sign-INSENSITIVE (MK2-Q4a; your lever-arm = 1 chain is exactly right); (b) C10's held-status (ASSUMED-PARTIALLY-PROVEN, sharpened) is robust to the INFO-vs-FAIL label and the sign-violation does NOT falsify the C10 form-claim (Re-V3, Q3a YES). In return, two asks:
1. Do you concede that the from-below sign is a GENUINE sign-FAIL of the `n_eff ≥ 2` directional prediction (not "secondary" / not a magnitude footnote) — i.e., that the STIFFNESS direction was substrate-refuted even though the acoustic-leading VALUE was confirmed?
2. Do you accept the two-layer C10 ROW (Re-V3): Layer 1 SHARPENED-CONFIRMED (form/quadratic-origin, present-epoch), Layer 2 PENDING (from-below direction + BBN realization, carrying `pending CF-MK3-1/MK3-2` pointers per `capstone-hygiene-gate.md`) — rather than V3's single-row "annotation-only, no structural split"?

If yes to both, we converge on: INFO label (one-off, guarded-general at K=3 with my guard-(iv)); from-below sign robust pending MK3-1; C10 tag held with the two-layer row; present-epoch safe, BBN-conditional pending MK3-2. The ONLY place our readings still genuinely diverge is whether the T.61 +0.0297 is a competing sign (your V2 "live uncertainty") or a different-order proxy (my Re-V2 "different object") — and MK3-1 settles THAT computationally. If no to either, name the specific substrate-physics reason: for (1), why the stiffness-direction refutation is secondary; for (2), why a one-line annotation captures the BBN-conditionality better than the structured Layer-2 split. That is the one spot Round 2 should land.

**Net**: MK3-1 settles the sign (cheap, existing npz); MK3-3 settles the disposition shape; MK3-2 is the CF that discharges or holds C10's BBN-conditionality. Three moves to convergence.

---

## Round 2 — volovik: Follow-up

### CONVERGENCE

Four concessions, in order of how much each one changes my Round-1 position. mack's Re:V1–V4 + MK1–MK3 moved me on three of the four — and the one I already agreed on (present-epoch sign-insensitivity) I confirmed numerically before writing this turn.

**(1) I ACCEPT the two-different-order-objects resolution of the T.61 sign cross-check (Re:V2 Step 1–3). This is the biggest change and it RETRACTS the soft framing in V2.** In V2 I called the from-below direction "a Track-A-leaning reading with a flagged sign-robustness caveat, NOT a claim I will assert at the same confidence as boundedness," and I called the sign "the live uncertainty." mack's Step 1–3 substitution chain (Re:V2, lines 208–224) shows that framing conflated two distinct functionals of the SAME `ρ_vac(q)` expansion: `C_meas = (exponent-on-q) − 2 = −0.0219` is the FULL-shape exponent (the q³ anharmonic curvature of `ρ_vac(q) = ε − q·dε/dq` integrated over all 992 modes), while `C_T61 = Σ_k(dp_k/dH)n_k / Σ_k ω_k n_k = +0.0297` is a LEADING-ORDER pressure-response proxy on the 8 occupied modes, truncated at the order BELOW where a ±0.02 correction's sign is set. I verified the structural facts mack's chain rests on: `|C_T61/C_meas| = 1.358` (same OOM, confirming these are NOT two precise estimators of one number — a genuine same-number cross-check would not differ by 36%), and both `|C|/2` land inside the 0.05 info-band (0.0109 measured, 0.0149 proxy) so the magnitude PASS is sign-independent. mack's chain is correct: the `n_eff = 2` prediction is a statement about the exponent OF `ρ_vac(q)`, which is `C_meas`. The opposite-sign T.61 result is the sign of a DIFFERENT object, and its disagreement is EXPECTED at the sub-leading order where ±0.02 lives — so it does not undercut from-below; it is simply silent on the observable. **What this changes**: I withdraw "the sign is the live uncertainty" and replace it with "the from-below sign is robust as the full-shape exponent's sign, PENDING the MK3-1 q→0-limit confirmation that the two objects genuinely diverge in the limit." The caveat is no longer "the sign might flip"; it is "the two-observables reading needs its q→0 discontinuity-vs-continuity discriminant before the sign is pinned-not-just-preferred." This is a real concession — V2's posture was symmetric between the two C's; mack's chain breaks the symmetry by identifying which observable the prediction predicates over.

**(2) I ACCEPT the structured two-layer C10 row over my V3 annotation-only proposal (Re:V3).** In V3 I argued "yes-annotation, no-tag-change," with the annotation as a free-text caveat appended to the otherwise-unchanged A5 row, owed ONLY IF mack establishes observational sign-sensitivity. mack establishes exactly that (MK2-Q4b: the BBN lever-arm amplifies ±0.02 to a factor-~0.40), so the annotation IS owed by my own V3 conditional — and mack is right that the right SHAPE for it is a structured Layer-1/Layer-2 split, not a one-line caveat. The reason I now prefer the split is substrate-physical, not cosmetic: Layer 1 (the V~q² quadratic-origin / form / present-epoch content) and Layer 2 (the approach-direction + BBN realization) live on STRUCTURALLY ORTHOGONAL axes — the first is the curvature DEGREE of `ρ_vac` at its q-stationary minimum (sign-insensitive, both n round to 2), the second is the SIGN of the sub-leading correction read across an 18–27-decade lever (sign-sensitive). A one-line caveat collapses an orthogonal-axis distinction into a footnote; the two-layer split preserves it, which is exactly the discipline I apply to algebra-INVARIANT vs algebra-DEPENDENT observables on the substrate triple (`cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`). **What this changes**: I drop my V3 "annotation-only" position and endorse mack's Layer-1 SHARPENED-CONFIRMED / Layer-2 PENDING structure, with Layer 2 carrying the `pending CF-MK3-1` (sign-robustness) and `pending CF-MK3-2` (BBN realization) pointers per `capstone-hygiene-gate.md`'s "STATUS: unreconciled — see <forward gate>" discipline. The tag itself stays `ASSUMED-PARTIALLY-PROVEN` — on that mack and I never diverged.

**(3) I ACCEPT mack's fourth guard predicate (Re:V1, Q1b) for any future general `gate-verdicts.md` CORE-vs-fringe clause.** My V1 general-clause sketch had three implicit guards (CORE confirmed; conditional antecedent falsified-not-violated; magnitude bounded) and MISSED a fourth that mack's observational-bridge domain caught: **(iv) the boundary-limit recovery DIRECTION (from-below vs from-above) must be normalization-invariant before the INFO override fires, OR the gate must declare which observable the recovery predicate is about.** This is the right guard and it is load-bearing precisely BECAUSE of concession (1): the from-below/from-above question turned out to depend on WHICH observable you read (`C_meas` vs `C_T61`), so without guard (iv) a future author could "recover n=2 from below" under one normalization and "from above" under another, and the INFO_meaning's "one-signed correction" qualifier would become a normalization artifact. **What this changes**: I no longer treat the general clause as a clean three-guard structure I could mint at K=3 on my own authority. Guard (iv) COUPLES the label question (V1) to the sign-robustness question (V2/V3) at the general-clause layer — they are separable for THIS gate's canonical label (because the magnitude PASS holds under either sign), but NOT separable for the general rule. mack is right; I concede the coupling.

**(4) I CONFIRM (not merely re-agree) the present-epoch sign-insensitivity (V4/Q4a, MK2-Q4a).** This was already agreed in Round 1, but I want it on the record as numerically verified, not asserted: I evaluated `D(0) = 1^(n−2)` at n = 1.978111, 2.000, and 2.030 — all return exactly 1.0 to 60-bit precision. The lever arm is unity at z=0 for ANY exponent, so the W6-promoted pin `rho_vac_over_rho_obs = 1.032` (audit `4ec12df8`, DILUTION-CC-66, C10-conditionality carried) cannot be moved by the W2-2 sign result. mack cross-checked the canonical entry is non-superseded with the conditionality carried in its provenance note (Re:V4, Q4a); I confirm the arithmetic underneath. This is the cleanest decoupling in the workshop: the 1.032 number does not care which line reads INFO or which sign is real.

### DISSENT

One residual, and it is narrow — narrower than my Round-1 position, because concession (1) ate most of V2's hedge. I do NOT re-dispute the label, the tag, the two-layer split, or the present-epoch insensitivity; those are converged. The single place I still want a sharper distinction than mack's Re:V2 draws:

**The q→0-limit test (MK3-1) settles BOUNDEDNESS-of-the-divergence and the EXISTENCE of two distinct limits — but it does not, by itself, settle that the from-below sign is SUBSTRATE-PINNED rather than estimator-preferred.** mack's Re:V2 frames MK3-1 as a clean binary: "IF the two C's diverge as q→0, the from-below sign is robust and you concede; IF they converge, the sign is method-dependent and I concede." I accept that binary as the disposition-deciding test, and I will concede the sign-robustness on a divergence outcome. But I want the SUBSTRATE-PHYSICS content of "diverge" pinned before the verdict reads it as "sign-pinned," because there are two ways the two exponents can "diverge as q→0" and only ONE of them pins the sign:

- **Divergence-type A (sign-pinning)**: `C_meas(q) → C_meas(0) < 0` (from-below) as a well-defined limit, while `C_T61(q) → C_T61(0)` approaches a DIFFERENT, also-well-defined value. Two distinct finite limits ⇒ two genuinely different observables ⇒ the prediction predicates over `C_meas` ⇒ from-below is the observable's sign, substrate-pinned. This is the outcome mack's Step 1–3 predicts, and on this outcome I concede fully.
- **Divergence-type B (NOT sign-pinning)**: the two exponents diverge because the T.61 leading-order proxy is simply ILL-CONDITIONED as q→0 (the `Σ ω_k n_k` denominator on 8 gap-set modes develops a near-cancellation, or the proxy's truncation error grows), so `C_T61(q)` does not approach any clean limit at all. Then "they diverge" is true but UNINFORMATIVE — it tells us the proxy is bad near q=0, not that `C_meas`'s sign is the substrate's. The from-below sign of `C_meas` would still be the better-conditioned reading, but its robustness would rest on `C_meas`'s OWN small-q stability, not on the proxy's divergence.

So my dissent is a sharpening of the MK3-1 pre-registration, not a disagreement with its logic: **MK3-1 must report not just whether the two exponents diverge, but whether `C_meas(q)` itself has a clean, monotone, well-conditioned q→0 limit** (the substrate's own anharmonic q³ coefficient converging, the regression residual shrinking as the window tightens). That is the quantity that pins the sign. If `C_meas` is itself clean as q→0 (which I expect, because the full-shape `ρ_vac(q) = ε − q·dε/dq` is built from the bounded gap-set Jacobian `dω_n/dq = 1/(2ω_n) ≤ 1/(2|λ_min|) = 0.610`, which I verified is finite and strictly positive across the whole relaxation window `q > −λ_min² = −0.67198`), then divergence-type A holds and I concede. If `C_meas`'s own small-q regression is noisy, the sign is preferred-not-pinned regardless of what the proxy does. This is the one place where "the divergence settles it" needs the substrate-physics qualifier `C_meas-is-itself-well-conditioned` to be load-bearing — and it is cheap to add to the same npz re-run.

### EMERGENCE

Three things crystallized from the cross-pollination that neither of us had cleanly in Round 1.

**(1) The two-layer C10 disposition has a precise substrate-IS statement now, and it maps onto the algebra-axis orthogonality I work with on the spectral triple.** The split is not just "form vs realization" — it is a curvature-DEGREE observable (Layer 1) vs a sub-leading-SIGN observable (Layer 2), and these are orthogonal in exactly the sense that an algebra-INVARIANT spectrum-only functional is orthogonal to a state-pair functional. Layer 1 (`k = d²ρ_vac/dq²|₀ = +3586.5 M_KK`, the curvature degree = 2) is a property of the FULL spectrum's concavity at the q-stationary minimum — it reproduces `ρ_vac(0) = 81493.046` = the S62 `E_ZP(0)` to all printed digits, so it is a spectrum-summed invariant insensitive to which 8 modes carry GGE occupation. Layer 2 (the from-below sign of the q³ anharmonic correction) weights the low-ω modes more heavily through the `1/ω³`-type kernels — it is occupation-SENSITIVE in exactly the way a state-pair functional is. The precise disposition statement: **Layer 1 is the substrate's curvature-degree-2 invariant (SHARPENED-CONFIRMED, sign-insensitive, truncation-robust); Layer 2 is the substrate's sub-leading anharmonic-sign observable (PENDING the MK3-1 well-conditioned-limit confirmation + the MK3-2 BBN-realization compute).** Calling them "two layers" undersells it; they are two structurally-orthogonal observable classes of the same `ρ_vac(q)` curve, and conflating them would be the same category error as co-citing an algebra-INVARIANT and an algebra-DEPENDENT anchor at co-primary weight.

**(2) The BBN-relief direction is a new substrate-IS cross-cut, and the substrate-first framing makes it sharper than the cosmological statement.** mack and I converged that from-below RELIEVES the BBN vacuum over-contribution (suppression factor 0.40 at 18 decades / 0.25 at 27 decades — I verified both: `0.67 × 0.40 = 0.270`, `0.67 × 0.25 = 0.168`). The substrate-first reading of WHY relief is the robust direction: the BBN extrapolation is `[ρ_vac/ρ_rad](z_BBN) ∝ X^(n−2)` where `n−2 = C_meas < 0` is the full-shape anharmonic SOFTENING of the q-theory vacuum-energy curvature — i.e., the substrate's own bottom-K ladder, anharmonically soft at finite q, makes `ρ_vac` dilute slightly faster to high z than the exact-quadratic n=2 would. This is not a dark-energy field tuned in an FRW container to relieve BBN; it is the substrate's intrinsic q³ softening, read forward through the tracking law. The relief is a CONSEQUENCE of the same anharmonicity that gives the from-below sign — which is why mack is right that the relief direction inherits the from-below sign's robustness (Re:V4, Q4b) rather than being an independent coin-flip. The cross-cut: **the W2-2 exponent's sub-leading SIGN, which is invisible at present epoch (lever=1), becomes the substrate's BBN-budget knob through the 18–27-decade lever — the same "sub-leading at the probe scale ≠ negligible across cosmic history" structure as the n_T 54-decade k-scale separation.**

**(3) The q→0-limit test is the discriminator that converts the sign-robustness from CONTESTED to PINNED — and it is the single computable hinge of the whole workshop.** This is the cleanest emergent: every other live item (Layer-2 disposition, BBN-conditionality, the general-clause guard (iv)) is downstream of one cheap compute on the existing npz. Before MK3-1, V2's "live uncertainty" and mack's "different-order proxy" were two readings of the same `±0.02` opposite-sign fact with no decider. mack's Step 1–3 supplies the STRUCTURAL prediction (two different-order objects diverge in the limit); MK3-1 supplies the EMPIRICAL test (do they?); and my DISSENT supplies the substrate-physics qualifier (`C_meas` must itself be well-conditioned, not just "the proxy is bad"). Together these make the sign-robustness a settled binary on existing data: divergence-type-A + `C_meas`-clean ⇒ pinned (I concede); anything else ⇒ preferred-not-pinned (the Layer-2 caveat stands and is genuinely a caveat, not a flag-for-completeness). The whole workshop's contested surface reduces to one regression on `s97_w2_2` npz.

### QUESTIONS

For mack's FINAL turn (R2 Turn B) — these are the positions I endorse, stated concretely enough that mack can EFFECT the non-math items in-session in the Verdict + Effected-In-Session. I answer mack's MK3-3 asks first (both YES, with the qualifier from my DISSENT), then pin the four adjudication axes.

**Answering MK3-3 ask (1) — YES, the from-below sign is a GENUINE sign-FAIL of `n_eff ≥ 2`, not a magnitude footnote.** I concede this without reservation. The literal one-sided cell `n_eff ≥ 2` is violated (1.978 < 2); `sign_verdict=FAIL` on the 3-tuple (verdict line 63) is honest and must stay loud. What was refuted is the STIFFNESS DIRECTION (the substrate is sub-acoustic at finite q, softer-than-acoustic), NOT the acoustic-leading VALUE (n→2 as q→0, confirmed). mack's MK1 framing — "Mack-direction vindicated as a direction, corrected as a magnitude (1.978 ≠ the free-redshift 1.778, because gap-bounded)" — is the correct substrate-physics reading and I adopt it. The stiffness refutation is decisive, not secondary; my V1 concession 3 ("the honest scalar is FAIL, and it should stay loud") already committed me here, and I reaffirm it.

**Answering MK3-3 ask (2) — YES, I accept the two-layer C10 ROW over my V3 annotation-only.** Per CONVERGENCE (2). Layer 1 SHARPENED-CONFIRMED (curvature-degree-2 invariant, present-epoch, sign-insensitive); Layer 2 PENDING (from-below sign + BBN realization), carrying `pending CF-MK3-1` and `pending CF-MK3-2` pointers per `capstone-hygiene-gate.md`. My one addition is the DISSENT qualifier: Layer 2's sign-robustness clause should read "from-below robust PENDING MK3-1 confirming `C_meas` has a clean well-conditioned q→0 limit (not merely that the proxy diverges)," so the pending-pointer is to the right quantity.

Now the four axes mack must pin in the Workshop Verdict + Effected-In-Session:

**Q-R2-1 (axis a — LABEL precedence verdict mack must pin): I endorse INFO as a ONE-OFF now, general-clause as a K=1 SUGGESTION candidate (NOT minted now), and I endorse mack's guard (iv) as a REQUIRED predicate of that future clause.** Verdict-table Row 1 should read **Converged**: canonical label INFO (the gate's pre-registered semantic INFO_meaning governs; the mechanical composite-collapse FAIL is the retained 3-tuple annotation at line 63; Class-3 firewall intact via the Option-A chain `30566894→0e6076f3→b69da9f4`). The `gate-verdicts.md` CORE-vs-fringe clause is NOT minted this session — it is a K=1 SUGGESTION calibration instance per the `feedback_rules-compensate-missing-structure.md` K=3 promotion contract, and when it IS minted (at K=3 recurrence) it MUST carry mack's four guards: (i) CORE confirmed, (ii) conditional antecedent falsified-not-violated, (iii) magnitude bounded, (iv) recovery DIRECTION normalization-invariant OR the gate declares which observable the recovery predicate is about. Effected-In-Session: mack should record this as a Q2 methodology-rule-extension CANDIDATE in `session-97-housekeeping.md §D` (NOT minted; SUGGESTION K=1), per `Investigating-Workshops.md §Q2` routing — the FORM is co-authored methodology, but the clause does not land until K=3.

**Q-R2-2 (axis b — SIGN-ROBUSTNESS disposition mack must pin): I endorse "boundedness ROBUST (unconditional); from-below DIRECTION robust PENDING MK3-1."** Verdict-table Row 2 should read **Partial** (converged on the structure, pending the one compute): (i) the CORE limit n→2 is method-invariant (full-spectrum curvature, k=+3586.5); (ii) boundedness `|C|/2 < 0.05` is ROBUST under BOTH estimators and forced by the gap-set Jacobian ceiling (`dω/dq ≤ 0.610`, verified finite/positive across the relaxation window) + Ordered-Veil integrability — this is unconditional and I defend it as hard as mack does; (iii) the from-below DIRECTION is robust as the full-shape exponent's sign under mack's two-observables reading, PENDING MK3-1 confirming divergence-type-A (`C_meas` clean limit + distinct from `C_T61`'s limit). The T.61 +0.0297 is a different-order leading-proxy, NOT a competing sign — this is where mack's Re:V2 moved me off V2's "live uncertainty." The disposition mack pins: sign-robustness CONTESTED→PINNED on the MK3-1 divergence-A outcome; the Layer-2 caveat stands only if MK3-1 returns convergence or `C_meas`-ill-conditioning.

**Q-R2-3 (axis c — C10 Atlas-04 tag + §8.5 disposition mack must pin): I endorse tag HELD `ASSUMED-PARTIALLY-PROVEN` (unchanged from A5), two-layer ROW (mack's structure), §8.5 ENRICHED-not-closed.** Verdict-table Row 3 should read **Converged**: the tag is robust to the INFO-vs-FAIL label (the held-status is set by the missing SECOND factor — the q~H relaxation closure, still simple-fluid input — an orthogonal reason the sign-violation cannot touch). The Atlas-04 C10 ROW gains mack's Layer-1/Layer-2 structure (NOT my V3 single-row annotation). The §7 falsifier-realization wording is mack's sole-writer domain (`feedback_mack-bridge-role.md`); I endorse mack's proposed wording (Re:V3, Q3b) verbatim with one edit — the Layer-2 clause should say "from-below (full-shape exponent C_meas=−0.0219; leading-proxy T.61 C=+0.0297 is a different-order object, not a competing sign; q→0-limit + C_meas-well-conditioning cross-check pending CF-MK3-1)." Effected-In-Session: mack lands the §7-adjacent wording in `falsifier-master-inventory.md` (mack-owned) and the two-layer row reconciliation; the §8.5 capstone PROSE is the session-close DESIGNATED-WRITER's reviewed patch (per `capstone-hygiene-gate.md` Q4 + A4 §6.3-note timing), narrating C10 at INFO/PARTIALLY-PROVEN — "characterized, still open," NEVER "closed" (Q3 register-faithfulness). mack should FLAG the §8.5 designated-writer prose, not write it (it is curated-doc prose, not a §7-surface cell).

**Q-R2-4 (axis d — ρ_vac/ρ_obs=1.032 CONSISTENCY statement mack must pin): I endorse "present-epoch 1.032 sign-INSENSITIVE (lever=1, verified); BBN-epoch 0.67 sign-SENSITIVE (factor 0.40–0.25, RELIEF direction)."** Verdict-table Row 4 should read **Converged** (present-epoch) **+ Emerged** (BBN cross-cut): the W6 pin `rho_vac_over_rho_obs = 1.032` (audit `4ec12df8`) is SAFE from the W2-2 sign result — verified `1^(n−2) = 1` for all three n at 60-bit precision; the C10-conditionality the W6 promotion carries (A15) is CORRECT to retain and is NOT discharged by this gate. The BBN channel (Window-8 / BBN-VOLOVIK-67, `ρ_vac/ρ_rad = 0.67`, OPEN since S67) IS sign-sensitive at the factor-few level, RELIEF direction under the from-below (full-shape) sign. Effected-In-Session: this is a CONSISTENCY STATEMENT, not a new pin — mack should record in the Verdict that the 1.032 pin needs NO edit (present-epoch decoupling), and route the BBN re-evaluation as the math CF. Carry-Forward Computations: I endorse mack's Q4c structure — **ONE wave, two pre-registered sub-gates AND'd**: CF-S98-W2-2-RELAXATION-CLOSURE (the q~H second-factor derivation; discharges C10's TAG / Layer 1's remaining factor) + CF-S98-W2-2-BBN-EXPONENT-SENSITIVITY (the BBN vacuum-fraction `ρ_vac/ρ_total(z_BBN)` + exact lever X + S72 partial-PASS margin; discharges Layer-2 realization), routed as a Q3 parallel-compute-wave per `Investigating-Workshops.md §Q3`. MK3-1 (the q→0-limit + `C_meas`-well-conditioning sign test on the existing npz) is the THIRD CF and the cheapest — it is the lead item, settles the sign-robustness binary, and should be pre-registered to report BOTH "do the two exponents diverge" AND "is `C_meas`'s own q→0 limit clean" (my DISSENT qualifier).

One process note for mack's Verdict: every contested surface in this workshop reduces to MK3-1 on the existing `s97_w2_2` npz. The label (Row 1), the tag (Row 3), and the present-epoch consistency (Row 4) are converged independent of it; only the from-below sign-robustness (Row 2) and Layer-2's pending status (Row 3's Layer 2) wait on that one regression. That is a clean place to land a 2-round workshop: three rows Converged, one row Partial-pending-one-cheap-compute.

---

## Round 2 — mack: Cross-Synthesis

### CONVERGENCE

volovik's Turn A converged on the four axes I argued for in Round 1, and it did so by moving on the one substantive thing I most needed it to move on — the T.61 sign cross-check. I record what is now jointly pinned, then the one place I sharpen volovik's own DISSENT in volovik's favor.

**(1) I ACCEPT volovik's type-A/type-B sharpening of MK3-1, and it is correct and load-bearing.** This is the most important thing volovik's Turn A added, and it strictly improves my Re:V2 framing. My Round-1 binary was too coarse: I wrote MK3-1 as "IF the two exponents diverge as q→0, the from-below sign is robust; IF they converge, the sign is method-dependent." volovik is right that "diverge" is two different outcomes and only ONE of them pins the sign:
- **Divergence-type A (sign-pinning)**: `C_meas(q) → C_meas(0) < 0` is a clean, well-conditioned limit AND `C_T61(q) → C_T61(0)` approaches a DIFFERENT, also-well-defined value. Two distinct finite limits ⇒ two genuinely different observables ⇒ the prediction predicates over `C_meas` ⇒ from-below is the observable's substrate-pinned sign.
- **Divergence-type B (NOT sign-pinning)**: the two exponents "diverge" only because the T.61 leading-proxy is ILL-CONDITIONED as q→0 (`Σ ω_k n_k` near-cancellation on 8 gap-set modes, or proxy truncation error growing), so `C_T61(q)` approaches no clean limit. "They diverge" is then true but UNINFORMATIVE — it says the proxy is bad near q=0, not that `C_meas`'s sign is the substrate's.

The discriminator between A and B is exactly volovik's qualifier: **does `C_meas(q)` ITSELF have a clean, monotone, well-conditioned q→0 limit** (the anharmonic q³ coefficient converging, the regression residual shrinking as the window tightens). That is the quantity that pins the sign, and it is the quantity my Round-1 MK3-1 left implicit by treating "divergence" as a single outcome. I adopt volovik's sharpening verbatim into the MK3-1 pre-registration: the gate MUST report BOTH (a) whether the two exponents diverge as q→0 AND (b) whether `C_meas`'s own small-q limit is well-conditioned (monotone q³-coefficient convergence + shrinking regression residual). The from-below sign is PINNED only on `divergence-type-A AND C_meas-well-conditioned`; any other outcome leaves it preferred-not-pinned and the Layer-2 caveat stands as a genuine caveat. volovik's expectation that this returns type-A (the gap-set Jacobian ceiling `dω/dq ≤ 1/(2|λ_min|) = 0.610` is finite and strictly positive across the whole relaxation window `q > −λ_min² = −0.67198`, so the full-shape `ρ_vac(q) = ε − q·dε/dq` is built from a bounded, never-singular kernel and SHOULD have a clean small-q limit) is the physically expected outcome — but it is an expectation, and MK3-1 is the test, not the assertion. This is the right discipline: we pin the test, not the answer.

**(2) I ACCEPT volovik's concession that the from-below sign is a GENUINE sign-FAIL (volovik's answer to my MK3-3 ask 1), and I reaffirm the symmetric commitment.** volovik concedes without reservation that the literal one-sided cell `n_eff ≥ 2` is violated (1.978 < 2), `sign_verdict=FAIL` on verdict line 63 is honest and must stay loud, and what was refuted is the STIFFNESS DIRECTION (the substrate is sub-acoustic at finite q), NOT the acoustic-leading VALUE (n→2 as q→0, confirmed). This is the part I said in Round 1 I would defend hardest, and volovik's V1-concession-3 already committed it. Joint position: the honest scalar is FAIL, byte-permanent via the Option-A chain `30566894→0e6076f3→b69da9f4`; the canonical LABEL is INFO via the gate's pre-registered semantic INFO_meaning; the two are separate pinned objects and BOTH are correct. The sign-violation is not buried — it is the loudest annotation in the row.

**(3) I ACCEPT volovik's endorsement of the structured two-layer C10 row over volovik's own V3 annotation-only position (volovik's CONVERGENCE-2 + answer to my MK3-3 ask 2).** volovik dropped "annotation-only" and adopted my Layer-1 SHARPENED-CONFIRMED / Layer-2 PENDING split, and — better — volovik gave the split a sharper substrate-IS statement than I had: Layer 1 is a curvature-DEGREE observable (`k = d²ρ_vac/dq²|₀ = +3586.5 M_KK`, spectrum-summed invariant, reproduces the S62 `E_ZP(0) = 81493.046` to all printed digits, occupation-insensitive), Layer 2 is a sub-leading-SIGN observable (the q³ anharmonic correction, weighting low-ω modes through `1/ω³`-type kernels, occupation-SENSITIVE). volovik's mapping onto the algebra-INVARIANT-vs-algebra-DEPENDENT orthogonality (`cross-pillar-bridge-anatomy.md §"Algebra-axis orthogonality K-counter"`) is exactly right and it is a better justification for the two-layer structure than my "S94 W-1 STRUCTURAL-ORTHOGONAL-COMPANION" analogy alone — the two layers are not just "form vs realization," they are two structurally-orthogonal observable CLASSES of the same `ρ_vac(q)` curve, and conflating them would be the same category error as co-citing an algebra-INVARIANT and an algebra-DEPENDENT anchor at co-primary weight (FORBIDDEN per the K-counter MANDATORY clause). I adopt volovik's orthogonality framing into the row.

**(4) I ACCEPT volovik's endorsement of my guard (iv), and I confirm the coupling volovik conceded.** volovik adopts my fourth guard predicate — *(iv) the boundary-limit recovery DIRECTION (from-below vs from-above) must be normalization-invariant before the INFO override fires, OR the gate must declare which observable the recovery predicate is about* — as a REQUIRED predicate of any future general `gate-verdicts.md` CORE-vs-fringe clause, and concedes the structural point that makes (iv) load-bearing: the from-below/from-above question turned out to depend on WHICH observable you read (`C_meas` vs `C_T61`), exactly the failure (iv) guards. The four guards for the future clause are now jointly pinned: (i) CORE confirmed, (ii) conditional antecedent falsified-not-violated, (iii) magnitude bounded, (iv) recovery-direction normalization-invariant OR observable declared. We agree the clause is NOT minted this session — K=1 SUGGESTION, K=3 promotion contract.

**(5) I CONFIRM, jointly with volovik's 60-bit verification, the present-epoch sign-insensitivity.** volovik evaluated `D(0) = 1^(n−2)` at n = 1.978111, 2.000, 2.030 and got exactly 1.0 to 60-bit; I cross-checked the canonical entry `rho_vac_over_rho_obs = 1.032` (audit `4ec12df8`) is non-superseded with the C10-conditionality carried in its provenance note. Joint: the W6 pin needs no edit; it is decoupled from the W2-2 sign result at the point where 1.032 is defined (lever arm unity at z=0). This is the cleanest decoupling in the workshop.

So four axes converge; one residual remains, and it is volovik's, and I think volovik is right about it.

### DISSENT

I have no NEW dissent against volovik's Turn A — volovik conceded on the four axes I pressed, and the type-A/type-B sharpening resolved the one place I had under-specified. What remains is volovik's residual DISSENT, and my disposition is: **I agree with it, I adopt it, and I want to register one observational-bridge corollary that volovik's DISSENT implies but did not draw.**

**On volovik's residual (the type-A/type-B qualifier on MK3-1): CONCEDED, adopted into the CF pre-registration.** volovik's DISSENT is not against my two-observables reading — it is a sharpening of what MK3-1 must REPORT for that reading to convert from preferred to pinned. volovik is correct that "the proxy diverges" is insufficient; the load-bearing quantity is "`C_meas` is itself well-conditioned." I do not contest this; I have written it into CF-MK3-1 below as a dual deliverable (the divergence test AND the `C_meas`-conditioning test), and into the §7-surface Layer-2 clause. There is no daylight between us here. The only thing I add is a precision on the FAILURE branch, because it is my observational domain to say what happens to the BBN cross-cut if MK3-1 returns type-B or `C_meas`-ill-conditioned:

**The observational-bridge corollary volovik's DISSENT implies.** If MK3-1 returns divergence-type-B (proxy ill-conditioned, `C_meas` clean) the from-below sign is still the better-conditioned reading and the BBN relief direction (MK2-Q4b) survives — it just rests on `C_meas`'s own small-q stability rather than on the proxy's divergence, which is a WEAKER but still-directional footing. But if MK3-1 returns `C_meas`-ILL-conditioned (the regression residual does NOT shrink as the window tightens, the q³ coefficient does not converge), then the from-below sign is preferred-not-pinned AND the BBN lever-arm sign-sensitivity (MK2-Q4b) loses its directional content — the factor-`X^(n−2)` amplification is still real (the lever arm does not care about conditioning), but the SIGN of `n−2` it amplifies is no longer substrate-pinned, so "relief vs strain at BBN" becomes genuinely undetermined rather than relief-leaning. This is the precise downstream consequence of volovik's qualifier in my domain: **`C_meas`-conditioning is not just the sign-robustness hinge (volovik's point) — it is the BBN-direction hinge (the observational corollary).** The two are the same compute (MK3-1) feeding two different downstream channels. I note this so the Layer-2 clause's `pending CF-MK3-1` pointer carries the right semantic: it is pending on `C_meas`-well-conditioning, and on a `C_meas`-ill-conditioned outcome BOTH the from-below sign AND the BBN relief-direction down-tag to undetermined together.

That is the whole of the residual surface. It is a one-compute hinge, volovik named it correctly, and I have routed it to the lead CF with both deliverables pre-registered.

### EMERGENCE

Three things crystallized in Round 2 that neither Round-1 turn had cleanly. The first two extend volovik's EMERGENCE; the third is mine.

**(1) The whole contested surface reduces to ONE regression on the existing `s97_w2_2` npz — and it is now a TWO-DELIVERABLE regression, not one.** volovik's EMERGENCE-3 said the workshop's contested surface reduces to MK3-1. Round 2 sharpened WHAT MK3-1 must deliver: not just "do the two exponents diverge" (my Round-1 framing) but "do they diverge AND is `C_meas` itself well-conditioned" (volovik's type-A/type-B). The emergent precision is that these are TWO independent boolean deliverables on the same data, and the disposition truth-table is:
- `divergence-A ∧ C_meas-clean` ⇒ from-below PINNED; Layer-2 sign-robustness clause DISCHARGES; BBN relief-direction PINNED (my corollary).
- `divergence-B ∧ C_meas-clean` ⇒ from-below preferred-on-`C_meas`-stability; Layer-2 clause holds as a soft caveat; BBN relief-direction directional-but-weaker.
- `C_meas-ill-conditioned` (either divergence outcome) ⇒ from-below preferred-not-pinned; Layer-2 clause stands as a genuine caveat; BBN relief-direction UNDETERMINED.
This truth-table is the cleanest emergent object of the workshop — three contested items (sign-robustness, Layer-2 disposition, BBN direction) all collapse onto one 2-boolean compute, and the mapping is now explicit so S98 can read the disposition straight off the npz without re-litigating.

**(2) The substrate-first reading of WHY relief is the robust BBN direction is sharper than the cosmological statement — and it inherits its robustness from the SAME anharmonicity that gives the from-below sign.** volovik's EMERGENCE-2 made this point and I want to pin its substrate-IS form precisely because it is my domain to state the cosmological consequence correctly. The BBN extrapolation is `[ρ_vac/ρ_rad](z_BBN) ∝ X^(n−2)` where `n−2 = C_meas < 0` is the full-shape anharmonic SOFTENING of the q-theory vacuum-energy curvature — the substrate's own bottom-K ladder, anharmonically soft at finite q, makes `ρ_vac` dilute slightly FASTER to high z than exact-quadratic n=2 would. This is NOT a dark-energy field tuned in an FRW container to relieve a BBN tension (the container-thinking reading `phononic-framing.md` forbids); it is the substrate's intrinsic q³ softening read forward through the tracking law (`D_K eigenfrequencies ω_n(q)=√(λ_n²+q)` → `ρ_vac(q)=ε−q·dε/dq` → `the exponent n in ρ_vac~H^n` → `the BBN-epoch vacuum fraction`). The relief is a CONSEQUENCE of the same anharmonicity that gives the from-below sign — which is exactly why the relief direction inherits the from-below sign's robustness (not an independent coin-flip) and why, per my DISSENT corollary, a `C_meas`-ill-conditioned outcome down-tags BOTH together. The cross-cut: the W2-2 exponent's sub-leading SIGN, invisible at present epoch (lever=1), becomes the substrate's BBN-budget knob through the 18–27-decade lever — the same "sub-leading at the probe scale ≠ negligible across cosmic history" structure as my n_T 54-decade k-scale separation discipline (transit-scale predictions are a geometric floor, not detector-comparable, until the lever arm is accounted for).

**(3) (mine) The C10 disposition is now a THREE-OBJECT decomposition, and the third object is the one that keeps the W6 pin honest.** Round 1 framed C10 as two factors (substrate-exponent × fluid-relaxation) and the row as two layers (form/present-epoch confirmed; approach-direction/BBN pending). Round 2 reveals these are not the same partition, and the cross-product is the precise disposition. There are THREE substrate-IS objects in play, on three different scale-and-confidence footings:
- **Object A — the curvature degree** (Layer 1; first factor's CORE): `k = +3586.5`, n→2, SHARPENED-CONFIRMED, sign-insensitive, present-epoch-load-bearing, truncation-robust. This is what moved ASSUMED→DERIVED.
- **Object B — the sub-leading sign** (Layer 2's sign clause; first factor's fringe): `C_meas = −0.0219` from-below, PENDING `C_meas`-well-conditioning (MK3-1), occupation-sensitive, BBN-load-bearing-not-present-epoch.
- **Object C — the relaxation closure** (second factor): `q ~ H` linearity, still the simple-fluid input, UNMOVED by this gate, the reason the TAG stays ASSUMED-PARTIALLY-PROVEN.
The emergent point is that the W6 `rho_vac_over_rho_obs = 1.032` pin's C10-conditionality is carried for OBJECT C (the relaxation closure is an input), and is robust to OBJECT B (present-epoch lever=1) — so a future promotion of C10 to CONFIRMED would have to discharge Object C (CF-S98-W2-2-RELAXATION-CLOSURE), NOT Object B. Object B's sign-robustness (MK3-1) discharges the §7 falsifier-realization Layer-2 clause and the BBN cross-cut, but does NOT touch the tag. This three-object decomposition is why the tag, the §7 row, and the W6 pin discharge on three different gates and why none of them is hostage to the canonical INFO label — and it is the load-bearing reason the C10 disposition is coherent across the Atlas-04 tag (Object C), the §7 surface (Object B), and the W6 pin (Object C-conditional, Object-B-insensitive).

## Workshop Verdict

| # | Topic | Source | Status | Key Insight |
|:--|:------|:-------|:-------|:------------|
| 1 | [SIGN]-gate label precedence | V1, Re:V1 | **Converged** | The honest 3-tuple scalar is `sign=FAIL` (n_eff 1.978 < 2), byte-permanent on verdict line 63 via the Option-A chain `30566894→0e6076f3→b69da9f4`; the canonical LABEL is INFO via the gate's pre-registered semantic INFO_meaning. Both are correct simultaneously — a (verdict-scalar, label) pair, NOT a contradiction. The composite-collapse rule in `gate-verdicts.md` is UNEDITED (a post-verdict edit would be Class-3); the Class-3 firewall holds by construction. A future general CORE-vs-fringe override clause is K=1 SUGGESTION (K=3 promotion contract), carries 4 guards (i CORE-confirmed, ii conditional-antecedent-falsified-not-violated, iii magnitude-bounded, iv recovery-direction-normalization-invariant-OR-observable-declared), and is NOT minted this session. |
| 2 | From-below sign robustness | V2, Re:V2, MK1 | **Partial** | TWO sub-objects, separate dispositions. (a) **Boundedness** `|C_meas|/2 = 0.0109 < 0.05` is ROBUST/unconditional — the gap-set Jacobian ceiling `dω_n/dq = 1/(2ω_n) ≤ 1/(2|λ_min|) = 0.610` is finite + strictly positive across the whole relaxation window `q > −λ_min² = −0.67198`, and Ordered-Veil integrability forbids a runaway; the `ρ_vac(q)` kernel is never-singular. (b) **From-below sign** (n_eff → 2 from below, `C_meas = −0.0219`) is PENDING MK3-1: pinned only on `divergence-type-A ∧ C_meas-well-conditioned`. The two-observables resolution (`C_meas = −0.0219` full-shape exponent vs `C_T61 = +0.0297` leading-order pressure-response proxy; `|C_T61/C_meas| = 1.358` same-OOM ⇒ different-ORDER objects, not two estimators of one number) stands; T.61's `+0.0297` is NOT a competing sign, it is a different-order proxy. The type-A/type-B qualifier (volovik's DISSENT, ACCEPTED) is the correct sharpening: the q→0 divergence pins the sign only if `C_meas` ITSELF has a clean small-q limit, not merely if the T.61 proxy diverges. |
| 3 | C10 Atlas-04 tag + §8.5 disposition | V3, Re:V3 | **Converged** | Tag HELD `ASSUMED-PARTIALLY-PROVEN`; the gate ENRICHES C10, does not close it. Adopt the STRUCTURAL-ORTHOGONAL-COMPANION two-layer row (S94 W-1 pattern, mapped onto algebra-INVARIANT vs algebra-DEPENDENT orthogonality): Layer 1 = curvature-degree-2 invariant (`k = +3586.5 M_KK`, spectrum-summed, occupation-insensitive, reproduces S62 `E_ZP(0) = 81493.046`) SHARPENED-CONFIRMED, sign-insensitive, truncation-robust; Layer 2 = sub-leading anharmonic-sign + BBN-realization, PENDING (MK3-1 + BBN-VOLOVIK-67). §8.5 capstone prose: "enriched-not-closed; INFO label; two-layer C10" — routed to the designated writer (NOT mack's edit). The two layers are structurally-orthogonal observable classes of one `ρ_vac(q)` curve; cross-layer co-primary is FORBIDDEN per the K-counter MANDATORY clause. |
| 4 | ρ_vac/ρ_obs=1.032 consistency | MK2 | **Converged + Emerged** | Present-epoch `rho_vac_over_rho_obs = 1.032` (audit `4ec12df8`, non-superseded, C10-conditionality carried) is sign-INSENSITIVE: at z=0 the lever arm `[H/H₀]^(n−2) = 1^(n−2) = 1` for ANY n (volovik 60-bit verify at n = 1.978111/2.000/2.030 → exactly 1.0). The W6 pin needs NO edit — it is decoupled from the W2-2 sign result at the point 1.032 is defined. **EMERGED**: the SAME exponent's sub-leading sign is the BBN-budget knob through the 18–27-decade lever (`[ρ_vac/ρ_rad](z_BBN) ∝ X^(n−2)`); from-below (n<2) SUPPRESSES below the BBN-VOLOVIK-67 0.67 ratio (RELIEF direction). The BBN relief-direction is a NEW substrate-IS cross-cut feeding both Window-8 and the §7 surface, and it inherits the from-below sign's robustness from the same q³ anharmonicity (per the DISSENT corollary, a `C_meas`-ill-conditioned MK3-1 outcome down-tags from-below sign AND BBN relief-direction together). |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

Every item below is either a pre-registered compute carry-forward (with a named CF in the Carry-Forward block) or a designated-writer/registry action already effected this session. No item is "investigate X" without a fillable gate.

1. **Does MK3-1 return divergence-type-A AND `C_meas`-well-conditioned?** This is THE contested-surface hinge (Workshop Verdict Row 2b + the EMERGENCE truth-table). Two AND'd booleans on the existing `s97_w2_2` npz: (i) do `C_meas(q)` and `C_T61(q)` approach distinct finite q→0 limits, and (ii) is `C_meas`'s own small-q limit clean (monotone q³-coefficient convergence + shrinking regression residual). Disposition reads straight off the 2-bit outcome per the truth-table. Pre-registered as CF-MK3-1 below.

2. **What is the exact BBN lever magnitude at the relevant epoch?** The lever `X^(n−2)` with `n−2 = C_meas < 0` suppresses the BBN-VOLOVIK-67 ratio below 0.67; the suppression factor is `~0.40` at log10 X ~ 18 (radiation-dominated BBN) and `~0.25` at log10 X ~ 27 (transit/GUT scale). Both O(1); the exact value and the correct epoch-pin (which X enters the BBN-VOLOVIK-67 ratio) is the second deliverable of CF-MK3-2 below. This is the only place I retain a sharpening over volovik (which X is the BBN-relevant lever), and it is a compute, not a dispute.

3. **Does the substrate-friction relaxation `q'' + 3Hq' + V'(q) = 0` discharge Object C (the `q ~ H` simple-fluid closure)?** This is the gate that would move the C10 TAG (Atlas-04 `ASSUMED-PARTIALLY-PROVEN` → CONFIRMED), and it is ORTHOGONAL to MK3-1 (which discharges Object B / the §7 Layer-2 clause / the BBN cross-cut, but NOT the tag). Pre-registered as CF-S98-W2-2-RELAXATION-CLOSURE (the Q4c first AND'd sub-gate).

4. **Does the BBN-epoch exponent-sensitivity survive joint with the relaxation closure?** The Q4c wave's second AND'd sub-gate: CF-S98-W2-2-BBN-EXPONENT-SENSITIVITY. The wave-AND of (relaxation-closure PASS) ∧ (BBN-exponent-sensitivity within the BBN-VOLOVIK-67 envelope) is the structural precondition for any future C10 CONFIRMED promotion.

5. **Should the general `gate-verdicts.md` CORE-vs-fringe override clause (4 guards) be minted?** NOT this session (K=1 SUGGESTION; K=3 promotion contract). This is a methodology-rule-extension candidate, NOT a compute — it advances only when two more structurally-distinct composite-collapse instances calibrate the same 4-guard pattern. Recorded as a calibration instance this session (Effected-In-Session item 4); the clause itself waits for K=3.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- (none) — no value, σ-band, ratio, or OOM-split was recomputed in this workshop. The workshop adjudicated the LABEL and the DISPOSITIONS of values already computed at S97 W2-2; the values (`n_eff_T61 = 1.978111`, `C_meas = −0.0219`, `C_T61 = +0.0297`, `k = +3586.5`, `rho_vac_over_rho_obs = 1.032`) are authoritative and UNCHANGED.

#### (b) Structural changes

- **single-scalar verdict → (verdict-scalar, canonical-label) pair** — Row 1: the `sign=FAIL` 3-tuple scalar and the INFO canonical label are reclassified from an apparent contradiction to two co-correct pinned objects on separate axes (honest-result axis vs gate-semantic axis). The Option-A chain `30566894→0e6076f3→b69da9f4` is the structural carrier.
- **single sign-robustness claim → boundedness (ROBUST) ⊕ from-below-sign (PENDING) two-sub-object split** — Row 2: boundedness is now unconditional (gap-set Jacobian ceiling + Ordered-Veil integrability); the from-below sign is separated out as MK3-1-pending.
- **two-estimators reading → two-different-ORDER-observables reading** — Row 2: `C_meas` (full-shape exponent, from below) and `C_T61` (leading-order pressure-response proxy, from above) are reclassified as different-order objects (`|C_T61/C_meas| = 1.358` same-OOM ⇒ NOT two estimates of one number), so T.61's `+0.0297` is not a competing sign.
- **annotation-only C10 row → STRUCTURAL-ORTHOGONAL-COMPANION two-layer row** — Row 3: Layer-1 curvature-degree-2 (algebra-INVARIANT, CONFIRMED) + Layer-2 sub-leading-sign/BBN (algebra-DEPENDENT, PENDING); mapped onto the algebra-axis orthogonality K-counter (cross-layer co-primary FORBIDDEN).
- **one-factor C10 disposition → three-object decomposition (A curvature-degree / B sub-leading-sign / C relaxation-closure)** — EMERGENCE-3: the three objects discharge on three different gates (A done; B on MK3-1; C on RELAXATION-CLOSURE), which is why the Atlas-04 tag, the §7 surface, and the W6 pin are each robust to the canonical INFO label.
- **single MK3-1 deliverable → 2-boolean MK3-1 + disposition truth-table** — EMERGENCE-1: the contested surface (sign-robustness + Layer-2 disposition + BBN direction) collapses onto one 2-bit compute via the explicit truth-table, so S98 reads the disposition off the npz without re-litigation.
- **(NEW cross-cut) BBN relief-direction is a substrate-IS cross-cut feeding both Window-8 and the §7 surface** — Row 4 EMERGED: the sub-leading sign, invisible at z=0 (lever=1), becomes the BBN-budget knob through the 18–27-decade lever and inherits the from-below sign's robustness from the same q³ anharmonicity.

### What Holds

- **C10 stays `ASSUMED-PARTIALLY-PROVEN`** (Atlas-04 C10 row). The gate enriches; it does not close. The relaxation-closure (Object C) is the gate that would move the tag, and it is untouched.
- **The composite-collapse rule in `gate-verdicts.md` is UNEDITED.** No post-verdict edit was made; the Class-3 PROHIBITED-post-hoc-pre-registration firewall holds by construction. Any future general clause COMPOSES WITH the collapse rule, never modifies it.
- **The honest `sign=FAIL` is loud and permanent.** Verdict line 63 (`b69da9f4…`) stays; the violation of the literal one-sided `n_eff ≥ 2` cell is the loudest annotation in the row, not buried under the INFO label.
- **The present-epoch `rho_vac_over_rho_obs = 1.032` pin needs no edit.** Sign-insensitive at z=0 (lever = 1^(n−2) = 1, 60-bit). DILUTION-CC-66 / the W6 pin are decoupled from the W2-2 sign result.
- **Boundedness `|C_meas|/2 < 0.05` is unconditional** — robust to MK3-1, robust to regulator class, anchored by the gap-set Jacobian ceiling 0.610 + Ordered-Veil integrability.
- **The acoustic-leading VALUE n → 2 as q→0 is confirmed** (n_leg1 = 2.0000); only the STIFFNESS DIRECTION (sub-acoustic at finite q) was refuted, not the leading value.

### What Breaks or Strains

- **The literal one-sided cell `n_eff ≥ 2` is VIOLATED** (1.978111 < 2) — this is a genuine sign-FAIL, retained as such. The substrate is sub-acoustic at finite q. This is a corridor closed in the constraint map (the exact-quadratic-from-above reading is excluded), not a framework weakness; it is informative.
- **The from-below sign is NOT YET pinned** — it is preferred-not-pinned until MK3-1 returns `divergence-type-A ∧ C_meas-well-conditioned`. On a `C_meas`-ill-conditioned outcome the from-below sign AND the BBN relief-direction down-tag to undetermined TOGETHER (the DISSENT corollary). This is the single live strain on the C10 Layer-2 / BBN cross-cut, and it rests on one cheap compute.
- **The C10 TAG does not move this session** — Object C (the `q ~ H` simple-fluid relaxation closure) is unmoved; the tag stays `ASSUMED-PARTIALLY-PROVEN` until CF-S98-W2-2-RELAXATION-CLOSURE discharges it. The gate's substrate-derivation of the exponent-on-q (Object A) does NOT by itself license a CONFIRMED tag — that conflation is exactly what the three-object decomposition forbids.
- **The BBN-VOLOVIK-67 ratio at T_BBN remains OPEN** (Window-8, deferred since S67). The W2-2 sign result now feeds it a directional input (relief, if from-below pins), but does not close it; the exact lever magnitude (which X) is CF-MK3-2's second deliverable.

### Carry-Forward Computations (MATH ONLY — propagate to S98)

All three CFs run on the EXISTING `s97_w2_2` npz / spectrum cache (no new heavy compute); the lead (CF-MK3-1) is the cheap one-regression hinge.

**CF-MK3-1 — q→0 limit type-A/type-B + `C_meas`-well-conditioning test (the lead; cheap)**
1. **What**: On the existing `s97_w2_2` npz, scan `C_meas(q)` and `C_T61(q)` over a shrinking q-window toward q→0 and emit TWO independent booleans: (i) `divergence_type` ∈ {A, B} — do `C_meas(q)` and `C_T61(q)` approach DISTINCT finite limits (type-A) or does `C_T61(q)` fail to approach any clean limit (type-B, proxy ill-conditioned); (ii) `C_meas_well_conditioned` ∈ {True, False} — is `C_meas`'s own small-q limit clean (q³-coefficient monotone-convergent + regression residual shrinking as window tightens).
2. **Inputs**: `computations/session-97/s97_w2_2_*.npz` (the spectrum + `ρ_vac(q)` shape arrays already on disk; the 992 D_K eigenfrequencies + `λ_min` for the Jacobian-ceiling cross-check); `canonical_constants.py` (`Delta_BCS`, λ_min). No new spectrum diagonalization.
3. **Gate**: PASS (sign + BBN-direction PINNED) iff `divergence_type == A ∧ C_meas_well_conditioned == True`; INFO-soft-caveat iff `divergence_type == B ∧ C_meas_well_conditioned == True` (from-below preferred on `C_meas` stability, BBN-direction directional-but-weaker); FAIL-to-undetermined iff `C_meas_well_conditioned == False` (from-below sign AND BBN relief-direction BOTH down-tag to undetermined). Tolerance: q³-coefficient relative change `< 10%` across the last two window-halvings = "converged"; regression-residual ratio (tight-window/wide-window) `< 1` = "shrinking". Pre-register the truth-table mapping (workshop EMERGENCE-1) as the disposition decoder.
4. **Effort**: LOW (~1 regression on cached arrays; no GPU; minutes-scale). **Depends on**: `s97_w2_2` npz (UPSTREAM, on disk); the disposition truth-table (this workshop EMERGENCE-1); the §7 Layer-2 clause + Atlas-04 Layer-2 row (this session's Effected-In-Session items 1+2, consume the verdict).

**CF-MK3-2 — BBN lever-magnitude + epoch-pin (second deliverable, feeds Window-8)**
1. **What**: Given the CF-MK3-1 from-below sign verdict, compute the exact BBN suppression factor `X^(n−2)` with `n−2 = C_meas` and pin WHICH X (which redshift/temperature lever) enters the BBN-VOLOVIK-67 `ρ_vac/ρ_rad` ratio; report the suppressed ratio against the 0.67 BBN-VOLOVIK-67 envelope. Resolve the log10 X ~ 18 (rad-dom BBN, factor ~0.40) vs log10 X ~ 27 (transit/GUT, factor ~0.25) ambiguity by the substrate-correct epoch identification.
2. **Inputs**: CF-MK3-1 `C_meas` verdict + conditioning flag; `canonical_constants.py` (BBN temperature/redshift anchors, `rho_vac_over_rho_obs = 1.032` present-epoch normalization); Window-8 / BBN-VOLOVIK-67 0.67 envelope.
3. **Gate**: PASS iff the suppressed `ρ_vac/ρ_rad`(z_BBN) lands inside the BBN-VOLOVIK-67 survival envelope AND the from-below sign is pinned (CF-MK3-1 type-A ∧ clean); INFO iff sign directional-but-weaker (CF-MK3-1 type-B); UNDETERMINED iff CF-MK3-1 returned `C_meas`-ill-conditioned (lever real but amplified sign not substrate-pinned). Tolerance: epoch-pin must be a SINGLE substrate-justified X, not a scan band.
4. **Effort**: LOW (closed-form lever evaluation once `C_meas` sign + epoch fixed). **Depends on**: CF-MK3-1 (UPSTREAM — needs the conditioning flag); BBN-VOLOVIK-67 / Window-8 envelope (REGISTRY).

**CF-S98-W2-2-RELAXATION-CLOSURE + CF-S98-W2-2-BBN-EXPONENT-SENSITIVITY — ONE wave, two AND'd sub-gates (Object C; moves the tag)**
1. **What**: ONE S98 wave with two pre-registered sub-gates combined by logical AND. Sub-gate (α) RELAXATION-CLOSURE: derive the substrate-friction relaxation `q'' + 3Hq' + V'(q) = 0` from the substrate (NOT posited), testing whether the `q ~ H` linear tracking (Object C, the simple-fluid input) is a CONSEQUENCE of the substrate friction or remains an independent closure. Sub-gate (β) BBN-EXPONENT-SENSITIVITY: with the relaxation law in hand, test whether the BBN-epoch vacuum fraction (CF-MK3-2 output) survives jointly with the derived relaxation.
2. **Inputs**: substrate friction kernel (from the D_K spectral data + the Jensen-deformation gradient); `V(q) = ε(q) − μq` (Volovik q-theory closed form, Paper 13 Eq.4 / Paper 25 Eq.2.11); CF-MK3-2 BBN-fraction; `canonical_constants.py` H-anchors.
3. **Gate**: wave-AND. (α) PASS iff `q ~ H` emerges from `q'' + 3Hq' + V'(q) = 0` within the slow-roll regime to pre-registered tolerance; FAIL iff `q ~ H` requires an independent assumption (Object C stays an input). (β) PASS iff BBN fraction within BBN-VOLOVIK-67 envelope under the derived relaxation. Wave outcome `= (α) ∧ (β)`; a wave-AND PASS is the structural precondition for any future C10 `ASSUMED-PARTIALLY-PROVEN → CONFIRMED` Atlas-04 tag promotion (and ONLY this wave can move the tag — MK3-1 cannot).
4. **Effort**: MEDIUM (ODE derivation + slow-roll regime check; modest compute). **Depends on**: CF-MK3-2 (BBN fraction, UPSTREAM); Volovik q-theory closed forms (REGISTRY/corpus); Atlas-04 C10 tag (the promotion target).

### Effected In-Session (NON-MATH — completed by mack, the final agent, BEFORE TERMINATING)

- [x] Atlas-04 C10 two-layer row reconciliation — appended STRUCTURAL-ORTHOGONAL-COMPANION two-layer row (Layer-1 curvature-degree-2 CONFIRMED algebra-INVARIANT / Layer-2 sub-leading-sign+BBN PENDING algebra-DEPENDENT) + three-object decomposition (A/B/C; only C moves tag) + honest-FAIL/INFO-label split; tag HELD `ASSUMED-PARTIALLY-PROVEN` — `sessions/framework/Atlas/atlas-04-assumptions.md:69` (C10 row) — anchor: S97 W-1 verdict Rows 3+4; Option-A chain 30566894→0e6076f3→b69da9f4
- [x] §7 surface + falsifier-master-inventory C10 sign-robustness + BBN sign-sensitivity annotations — appended NEW Row #76 (C10-N-EXPONENT surface was ABSENT from the inventory; mack sole-writer NEW-row per Row #75 precedent) carrying BOTH the sign-robustness annotation (dual-MK3-1-outcome pin, two-different-order-observables `C_meas`/`C_T61`, unconditional boundedness via gap-set Jacobian ceiling, honest FAIL retained + INFO-label separate-axis) AND the BBN sign-sensitivity annotation (z=0 lever-unity sign-INSENSITIVE vs BBN X^(n−2) sign-SENSITIVE RELIEF; DISSENT-corollary joint down-tag) cross-cut to BBN-VOLOVIK-67/Window-8, + two-layer/three-object framing + substrate-IS framing — `sessions/framework/registry/falsifier-master-inventory.md` NEW Row #76 (appended after the BF_spine block, ~line 1712) — anchor: mack sole-writer per feedback_mack-bridge-role.md
- [x] §8.5 capstone disposition recommendation routed to housekeeping (designated-writer's domain; NOT mack's prose edit) — filled §A row A16 with the capstone-hygiene-gate Q3/Q4 routing recommendation (§8.5 prose tag MUST equal Atlas-04 register tag `ASSUMED-PARTIALLY-PROVEN`; "enriched-not-closed", INFO label, two-layer + three-object framing; designated-writer reviewed patch NOT bulk append); capstone prose itself NOT edited — `sessions/archive/session-97/session-97-housekeeping.md` §A row A16 — anchor: capstone-hygiene-gate.md Q3/Q4 + phonic-exflation-equation.md §8.5 (target, unedited)
- [x] K=1 SUGGESTION general-clause + 4th guard calibration instance landed (COMPOSES WITH gate-verdicts.md, does NOT modify it) — created corpus §19 (composite-collapse CORE-vs-fringe override-clause candidate, 4 guards, K=1 SUGGESTION, calibration instance #1 = S97-W2-2-C10-N-EXPONENT) + housekeeping §D row D1 (M3-class methodology-rule-extension candidate); gate-verdicts.md collapse rule UNEDITED (Class-3 firewall preserved) — `sessions/framework/registry/pru-class-corpus.md §19` + `sessions/archive/session-97/session-97-housekeeping.md` §D row D1 — anchor: feedback_rules-directive-only-no-session-info.md (calibration→corpus, not rule file)

### Closing Line

C10's exponent-2 is now a substrate consequence at the curvature-degree layer and a substrate-pinned sign-pending-one-regression at the anharmonic layer — the gate enriched C10 into a three-object structure (curvature-degree DONE, sub-leading-sign on MK3-1, relaxation-closure on the tag-gate) without closing it, the honest FAIL stays loud beside its INFO label with the composite-collapse rule untouched, and the same sub-leading sign that is invisible today becomes the substrate's BBN-budget knob through the 18-to-27-decade lever the moment MK3-1 reads `C_meas` clean off the npz we already have.
