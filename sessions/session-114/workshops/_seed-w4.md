# Seed — Chunk w4 (W4 A_s functional-selection + compact-object Tier-3)

**Date**: 2026-06-23   **Investigator**: phonon-first-cosmologist   **Source**: S114 W4 (AS-FUNCTIONAL FAIL, B5A-TFD FAIL)
**Wave summary**: 0 PASS / 2 FAIL / 0 INFO. Both FAILs are *informative corridor-closures*, not defeats. **W4-1** `CF-S114-AS-FUNCTIONAL-SELECTION` FAIL = FUNCTIONAL-PLURALISM-PERMANENT (the substrate singles out NO unique spectral functional for the post-transit A_s amplitude; sub-test `d|β_k̂|²/d(a_0/a_2)=0` EXACT confines the openness to the UNIFIED route, but the three-functional spread 1.2590 OOM persists with no scheme-independent normalization — the lizzi-signature, the A_s magnitude-as-a-Planck-number is a physical d.o.f. like the a_0/a_2 CC ratio). **W4-2** `CF-S113-B5A-TFD` FAIL (Tier-3 NON-BLOCKING) = the two-sided TFD doubling is sign-correct (`dR_TFD/df_bulk=+0.8557>0`, gap-closing) but the Mach-13.75 white-hole causal patch is too narrow even doubled (`|R_TFD−1|=0.4653>0.25`; `2/M`-ceiling 0.1455 ≪ PASS-band floor 0.4367), so the "A/4 microstate count via a causally-derived bulk-EE fraction" corridor CLOSES on BOTH single-sided (S112) and two-sided-TFD routes. SHELL check: both gates have `.py`/`.npz`/`.png` artifacts + verdict lines + dual-SHA + schema-v2 3-tuple companion rows on disk — NOT a pre-compute shell.

## No candidates

W4 produces ZERO workshops and ZERO solos. Both gates are clean Q1-NO corridor-closures whose status was already reconciled in-session, and the only forward moves are compute-on-a-new-axis (carry-forwards), not adversarial readings of an existing verdict. The 3-question discriminator (first YES wins) returns Q1-NO for both:

**W4-1 (AS-FUNCTIONAL FAIL):** Q1 asks whether FUNCTIONAL-PLURALISM-PERMANENT vs incomplete-machinery is a *live* two-reading adjudication. It is not — for three independent reasons. (i) The reading-divergence that *would* seed a workshop (lizzi = functional-pluralism advocate vs transit-dynamics = floor-point advocate) was ALREADY run and ALREADY converged: the S113 WS-5 AS-HTILDE workshop (transit-dynamics × lizzi) produced a clean two-object split with "both poles agreed on every number" (EVOI §EVOI.BF line 47, FIRM). A re-dispatch would be two agents agreeing in parallel — explicitly NOT a workshop per `Investigating-Workshops.md §"is NOT"` item 5. (ii) The status re-tag ("open, may converge → open, structurally") is already LANDED in-session: housekeeping §A8 (Row #12 / §EVOI.BF, mack sole-writer, `audit 395f6800c8d1`) + §A7 (capstone §8.5 reconciliation). A status-tag edit on an already-landed slot is the canonical Q2 marker. (iii) The substrate-physics content is settled by the structural sub-test, which is a derived structural-zero (`d|β_k̂|²/d(a_0/a_2)=0` EXACT, machine-zero, confirmed analytically and numerically over the full ±181× perturbation grid) — there is no competing-reading of a structural zero. The only genuinely-open move is a COMPUTE: does a *substrate-canonical functional-determination principle on a NEW axis* (one not already exhausted by the three sudden/slow-roll/adiabatic functionals) collapse the 1.2590-OOM spread? That is a carry-forward compute with a pre-registered selector, not a workshop — and the plan already pre-registered it to emit ONLY on an INFO outcome (`CF-S115-AS-FUNCTIONAL-REGIME`); the gate FAILed, so the closed corridor is recorded, not carried (per `Investigating-Workshops.md` — FAIL closes a corridor, it is not a deferred compute).

**W4-2 (B5A-TFD FAIL, Tier-3 NON-BLOCKING):** Q1 asks whether the edge-undershoot admits a live competing reading. It does not. The verdict is a clean 3-tuple corridor-closure (sign=PASS gap-closing, magnitude=FAIL, regime=VALID) where the FAIL mechanism is geometrically transparent and uncontested: the sound-cone half-angle `sin θ_c = 1/M ≈ 0.073` at Mach 13.75 caps the causally-accessible bulk-EE fraction at a few percent (`2/M = 0.1455` even doubled), far below the PASS band `[0.4367, 0.6704]`. No agent reads `R_TFD = 0.5347` as evidence of X while another reads it as Y — both would agree it is a sign-correct, magnitude-insufficient causal-patch result. The WP itself names the alternative as a NEW forward OBJECT, not a competing reading of the existing one: "a full two-sided island QES extremization rather than the linear bracket interpolant." That is a carry-forward compute (a different microstate-count machinery), Q1-NO → carry-forward — and per the plan it was pre-registered to emit ONLY on INFO (`CF-S115-B5A-TFD-QES`); the gate FAILed, so the corridor closes (recorded in the Constraint-Map, not carried).

Neither gate exhibits any `Investigating-Workshops.md §"How to identify a real workshop"` signal: no FAIL-with-multiple-structural-readings (W4-1's pluralism reading is already adjudicated-and-converged; W4-2's closure mechanism is uncontested geometry), no borderline INFO (both are decisive FAILs, not ~0.5–1σ marginal detections), no cross-wave PASS-vs-FAIL contradiction (see Cross-wave flags — W4-1 and W2 are *coherent*, not conflicting), no methodology-vs-substrate blur, no convention-divergence, no registered-theorem-needing-adversarial-sufficiency-audit. Honest-count discipline: W4 is "non-blocking precision" per the plan and legitimately yields zero workshops.

## Cross-wave flags (surface for consolidator; NOT resolved here)

- **[COHERENCE, not conflict] W4-1 a_0/a_2 horizon-exit invariance ↔ W2-1 KPIVOT C2-ratio "trivial-on-ratio".** W4-1's load-bearing structural result is `d|β_k̂|²/d(a_0/a_2 horizon-exit) = 0` EXACT — the impulse-quench A_s floor is a_0/a_2-INVARIANT because the box-delta `|β_k̂|²` reads only fold-transit/UV quantities (`z''/z`, Ω_z, η-window, ξ_KZ), NOT the horizon-exit `H̃` carrier on which the 181× SDW/Zubarev `a_0/a_2` freedom lives (`s114_cf_as_functional_selection.npz`; WP §W4-1 PART A). W2-1 KPIVOT (housekeeping §A6 / §A5) independently found the C2-ratio leg of the K_pivot scale-bridge is "partial-derivable-TRIVIAL" — an even (deg-0) transport degree extractable but trivial-on-ratio (no contraction to K*). BOTH waves find that the `a_0/a_2` (a_0^{ζ}, n=0/s=4 CC moment; a_2^{ζ}, n=2/s=3 EH moment, poleconv-A-double) spectral-action ratio does NOT propagate to the respective emergent observable. This is a structural COHERENCE worth noting (the a_0/a_2 freedom is consistently confined to the horizon-exit carrier across two independent S114 waves), NOT a tension. **No adjudication owed** — flagging so the consolidator sees the cross-wave a_0/a_2 confinement pattern is internally consistent (a candidate one-line note for a closeout synthesis if S114 gets one, NOT a workshop seed).

- **[shared transit substrate] W4-2 white-hole causal patch ↔ τ_fold / Mach-13.75 transit (W2-2, W3 fold physics).** W4-2's FAIL mechanism is the Mach-13.75 supersonic-flow causal disconnect at the fold (PROVEN S85), the same fold-transit physics that W2-2 (τ_fold region-selection, housekeeping §A5/§A6) and the broader session's fold treatment touch. No conflict — W4-2 consumes the fold causal structure as a settled input (the white-hole one-directionality is a PROVEN wall, `N_zeros=1`), and finds it geometrically insufficient to reach A/4. Flagging the shared substrate so the consolidator does not mistake the cross-wave τ_fold appearances for independent re-derivations: they are one PROVEN fold-causal-structure consumed by distinct observables.

## Carry-forwards (route to W4 WP CF section, NOT this schedule)

> NOTE (process, per `Investigating-Workshops.md §"Enforcement"`): the two FAIL gates emitted NO carry-forward — the plan pre-registered `CF-S115-AS-FUNCTIONAL-REGIME` and `CF-S115-B5A-TFD-QES` to fire ONLY on an INFO outcome; both gates FAILed, so both corridors CLOSE (recorded in the WP Constraint-Map, not deferred). The items below are NOT re-listings of WP carry-forwards (there are none); they are the OPTIONAL forward-compute objects each closed corridor names, surfaced here for the consolidator's awareness and tagged explicitly as carry-forward-class (Q1-NO), NOT workshop content. They are candidates for `/rclab-plan` to weigh on EVOI merit, not pre-registered obligations.

- **[Q-other — OPTIONAL forward compute, NOT a pre-registered CF]** A_s substrate-canonical functional selector on a NEW axis. *What*: test whether a functional-determination principle NOT already exhausted by the three {impulse-quench, UNIFIED-AS-79, Parker-adiabatic} functionals (e.g. a maximum-entropy / Jaynes selection on the post-transit occupation, or a Connes-distance-canonical normalization of the relic spectral functional) collapses the 1.2590-OOM cross-functional spread to one value. *Inputs*: `s100b_box_delta_bogoliubov.npz` (the impulse-quench β² spectrum); the three functional A_s literals (impulse +0.864 / UNIFIED +0.196 / Parker +1.455 OOM); a candidate substrate-canonical selector spec. *Gate*: PASS = selector collapses the spread → A_s becomes a typed one-functional prediction (retires §EVOI.BF A_s liability); FAIL = no new-axis selector → FUNCTIONAL-PLURALISM-PERMANENT confirmed on a wider axis-basis. *Effort*: ~1 wave. **This is a genuine new derivation (a new structural claim on a new axis), so if `/rclab-plan` picks it up it is a COMPUTE gate — NOT this campaign's workshop schedule.** Low priority: §EVOI.BF already prices A_s magnitude as a permanent physical d.o.f.; this would WIDEN the no-selector evidence, not change the headline. Defer-or-drop is a planner decision.

- **[Q-other — OPTIONAL forward compute, NOT a pre-registered CF]** B5A two-sided island QES extremization (vs the linear bracket interpolant). *What*: replace the closed-form linear bracket interpolant `R_TFD = R_edge + f·(R_island − R_edge)` with a full two-sided island quantum-extremal-surface extremization of `S = Area(∂I)/4 + S_bulk-EE(I)` over the island boundary `∂I`, to test whether the A/4 microstate count is reachable by a non-causal-patch mechanism (the WP names this as the surviving forward object after the causal-patch corridor closes). *Inputs*: `s111_b5a_island.npz` (L12 GGE bulk-EE profile, `R_edge`, `R_island`, `A_quarter`, `c_conical`); the L12 D_K spectrum cache for the QES variation. *Gate*: |R_QES − 1| ≤ 0.10 PASS / ≤ 0.25 INFO / > 0.25 FAIL (the standard B5A 3-band). *Effort*: ~1–2 waves (QES extremization is heavier than the interpolant). **Tier-3 NON-BLOCKING — internal-consistency corridor-narrowing, NOT an observational falsifier (no live falsifier row).** Lowest priority; the planner may legitimately drop it (the causal-patch corridor closing is itself a clean result and the white-hole microstate count gates nothing downstream).

## Wave-by-wave digest (consolidator background)

- **§W4-1 `CF-S114-AS-FUNCTIONAL-SELECTION` (transit-dynamics-theorist) — FAIL (composite); set-membership = PLURALISM.** Schema-v2 3-tuple: sign=PASS (`d|β_k̂|²/d(a_0/a_2)=0` EXACT, machine-zero, confirmed over the full ±181× SDW/Zubarev perturbation grid — the impulse-quench floor is a_0/a_2-invariant, transit-trajectory-fixed; `aH|_fold = 0.9753935 M_KK` reproduced EXACTLY rel-dev 0.0 from two independent fold-passage kinematic routes `a_fold_raw·H_fold/Λ_rescale` and `k_pivot/(k/aH)|_fold`), magnitude=FAIL (the cross-functional spread 1.2590 OOM = +1.455 Parker − +0.196 UNIFIED, impulse +0.864 between, persists; `n_in_band = 0` vs Planck ±5%; no substrate-canonical selector collapses it), regime=VALID (clean analytic structural-zero + deterministic spread assembly). Composite collapse `magnitude=FAIL ∧ regime=VALID ⇒ FAIL`. Dual-prior re-allocated 0.10/0.90 to Track B (PLURALISM-PERMANENT). **In-session correction** (honestly disclosed, `v3-closure-recovery.md` Class-1 boundary, NOT convention-shopping): plan §W4-1 chain Step-1 wrote `aH|_fold = H_fold/Λ_rescale` (dropped the fold scale-factor `a_fold_raw`, gave 0.00253 M_KK rel-dev 0.997 — wrong); corrected to the standard conformal `aH = a(τ)·H(τ)` form, rel-dev 0.0 EXACT, corroborated by the independent `k_pivot` route — STRENGTHENS the sub-test, recorded in the verdict companion row. Floor POINT (`A_s_FW = 1.5367e-08`) + floor INEQUALITY (`A_s/A_s^BD > 1`, permanent 3-axis, `proven_1097`) UNTOUCHED. regulator_pin: `a_0^{ζ}/a_2^{ζ}` 181× Path-B split (S82 Obs 6.3, poleconv-A-double). audit `395f6800c8d143e4…`. §EVOI.BF A_s axis re-typed "open, may converge → open, structurally" (mack Row #12, §A8).

- **§W4-2 `CF-S113-B5A-TFD` (hawking-theorist) — FAIL (composite); Tier-3 NON-BLOCKING.** Schema-v2 3-tuple: sign=PASS (`dR_TFD/df_bulk = R_island − R_edge = 1.3820 − 0.5263 = +0.8557 > 0`, gap-closing, Sage-QQ-exact; consistent with all three prior B5A verdicts' sign=PASS), magnitude=FAIL (`R_TFD = R_edge + f_bulk^TFD·slope = 0.5263 + 0.009757·0.8557 = 0.5347`; `|R_TFD − 1| = 0.4653 > 0.25` INFO ceiling), regime=VALID (`λ_causal^TFD = 1.0626` strictly inside island support `[0.8197, 2.4893]`; `f_bulk^TFD ∈ [0,1]`). Composite `magnitude=FAIL ∧ regime=VALID ⇒ FAIL`. The two-sided TFD purification partner supplies a SECOND sub-Mach cone; the union doubles the eigenvalue-window reach to `2W/M`, raising `f_bulk^TFD = 0.009757` (from single-sided 0.003960) → `R: 0.5297 → 0.5347` (sign-correct, gap-closing). But the Mach-13.75 sound-cone half-angle `1/M ≈ 0.073` caps the accessible fraction at `2/M = 0.1455`, far below the PASS-band floor 0.4367 — even the most generous diagnostic reading (D2, `f_bulk = 2/M` direct, `R = 0.6508`, `|R−1| = 0.349`) FAILs. ANTI-TAUTOLOGY held: forbidden R=1 crossing `f* = 0.5536` computed DIAGNOSTIC-ONLY, never the canonical `f_bulk^TFD`. Bracket basis = island-restricted `sbulk_primary = 15236.71` nats (NOT full-support `S_bulk_total = 180723.4`) — required for continuity with the pinned R_island endpoint; S112 single-sided continuity reproduced bit-for-bit (`f_bulk = 0.003960`, `R = 0.529711`, assert < 1e-5). regulator_pin: `c_conical = 0.2500001250001146 = a_2^{Pauli-Villars}` conical 2nd Seeley-DeWitt moment (DISTINCT regulator from W4-1's `a_2^{ζ}`). audit `b3a78eaec199238b…`. Corridor "A/4-microstate-count-via-causal-patch" CLOSES on both single-sided + two-sided routes; A/4 accounting needs a non-causal-patch mechanism (full two-sided island QES extremization).
