# Session 114 Workshop: transit × lizzi — τ_fold Canonical-Value Adjudication

**Date**: 2026-06-23
**Format**: Iterative 2-agent workshop (2 rounds, 4 turns)
**Agents**: transit (transit-dynamics-theorist), lizzi (lizzi-spectral-functional-theorist)
**Source Documents**:
- `sessions/session-114/session-114-w2-workingpaper.md` (W2 working paper; W2-2 TAUFOLD-CUSP-CROSSING gate)
- `sessions/session-114/workshops/_seed-w2.md` (the investigation seed that surfaced this tension)
- `sessions/permanent-results-registry.md` (§VII.M.W10-3 S85 uniqueness theorem; the two permanent τ_fold objects)

**Adjudication scope**: Resolve the τ_fold canonical-value disagreement surfaced by S114 W2-2 `CF-S114-TAUFOLD-CUSP-CROSSING` (INFO HYBRID; audit_sha256=`7b637db142d9bea7da77be5cf4cfc49b486f65aa3a39f59c197ba0fb3ff43b09`). The permanent-results registry carries TWO permanent τ_fold objects at TWO values: (1) §VII.M.W10-3 (S85, connes+lizzi, PERMANENT) "τ_fold=0.190 van-Hove-cusp NON-STATIONARITY UNIQUENESS theorem" — producing gate `value='promoted'`, `convention=canonical_constants-S85-freeze` (it IMPORTED 0.190 = CONST-FREEZE-42, did NOT derive it); (2) [NEW S45] Van Hove TRUE crossing T3-T5 (atlas-07, PERMANENT) `tau=0.19104`. W2-2 computed the cusp-crossing from-scratch (NO 0.190 injected): `τ_cross=0.191038`, L_max-INVARIANT across {5,8,10,12}, reproducing S45 to 5sf; `|0.191038−0.190|=0.5464%`, just OUTSIDE the gate's ±0.5% PASS band.

**Competing readings (cannot both define the canonical pin)**:
- **READING-A (lizzi)** — 0.190 is the substrate-derived canonical; 0.191038 is within physical tolerance; the S85 non-stationarity uniqueness theorem STANDS; the 0.55% is mesh/finder noise around the true non-stationarity point.
- **READING-B (transit)** — 0.191038 IS the substrate's derived value; the from-scratch crossing functional is the substrate-IS observable; the S85 theorem proved UNIQUENESS of a cusp but IMPORTED the VALUE; RE-PIN canonical `tau_fold→0.191038`, demote 0.190 to a 0.55%-off legacy freeze.

**Adjudication sub-questions** (each derived from first principles):
- **(a)** Does the S85 "van-Hove-cusp non-stationarity uniqueness theorem" pin the canonical VALUE 0.190, or only the EXISTENCE/uniqueness of a cusp whose precise LOCATION is a separate substrate-IS observable (the from-scratch crossing = 0.191038)? Is the S85 `value='promoted'` import the actual source of the canonical 0.190?
- **(b)** Is `τ_cross=0.191038` (L_max-invariant, reproducing S45 to 5sf, from-scratch) the substrate-derived value that SHOULD become the canonical `tau_fold` pin — or is it within physical tolerance of 0.190 such that 0.190 stands and 0.191038 is finder/mesh noise? Does the gate's own ±0.5% band have substrate-physical meaning, or is it a chosen tolerance?
- **(c)** If RE-PIN: does amending `canonical_constants.py tau_fold 0.19→0.191038` cascade-invalidate the S85 §VII.M.W10-3 PERMANENT theorem (`proven_1424`/`proven_1905` "bounds tau_fold"), the dependent archive scripts (`s85_w0_van_hove_cusp_theorem.py`, `s85_w10_tau_fold_van_hove_theorem.py`), and every downstream derived ratio (e.g. S_0 = τ_fold/T_acoustic = 95/56, `S101-W3-S0-KNOB`)? Or does the structural content survive a value-only re-pin?

**Pre-registered output forms** (the verdict takes EXACTLY ONE): (i) RE-PIN to 0.191038 + cascade spec; (ii) 0.190 STANDS + 0.191038 certified within-tolerance + ±0.5%-band substrate justification; (iii) two-value registry convention (0.190 = uniqueness-theorem anchor; 0.191038 = from-scratch-crossing observable) with explicit NON-FUNGIBILITY statement.

**Substrate-first framing law**: the substrate IS the van-Hove crossing functional; τ_fold is the substrate's intrinsic deformation-parameter value at the band-edge anticrossing, NOT a coordinate in a meta-container. Every argument flows FROM the substrate (the D_K eigenvalue band-edge anticrossing) TOWARD the canonical pin.

---

## Round 1 — transit: Opening Analysis

### T1: Sub-question (a) — Does S85 "non-stationarity uniqueness" pin a VALUE, or only the EXISTENCE of a cusp whose LOCATION is a separate observable?

**Verdict on (a): the S85 theorem pins the EXISTENCE + non-stationarity CHARACTER of a unique cusp; it IMPORTS the value 0.190 and never locates it. The `value='promoted'` provenance is a CONSISTENCY CHECK against a frozen literal, not a derivation.**

I read the producing script directly (`computations/session-85/s85_w10_tau_fold_van_hove_theorem.py`), and the structure is unambiguous. The gate is named `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` and its verdict line reads:

```
S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM: PASS -- value='promoted'
  scheme=van-Hove-cusp-non-stationarity convention=canonical_constants-S85-freeze L_max=10
  audit_sha256=149e29a6d826fff018f2fa477bc501cf528470a848b78f52f43ded069d13791c
```
(`computations/session-85/s85_gate_verdicts.txt`).

**What the script actually computes.** Lines 122-126 hardcode the FROZEN expectation as a `# (local)` literal:
```python
EXP_TAU_FOLD = 0.19                                              # (local)
EXP_dS_FOLD = 58672.80241318                                     # (local)
EXP_S_FOLD = 250360.67696101                                     # (local)
EXP_d2S_FOLD = 317862.84898132                                   # (local)
```
`consistency_check()` (lines 169-222) loads the LIVE `tau_fold` from `canonical_constants` and compares it to `EXP_TAU_FOLD=0.19`. `compute()` (lines 258-283) resolves `value="promoted"` iff (i) the substitution chain is complete AND (ii) `consistency_check()` reports `all_strict` — i.e. iff the live `tau_fold` MATCHES the frozen 0.19 to `PASS_ABS_TOL=1e-10`. So `value='promoted'` means *exactly* "the canonical pin still equals 0.19 and the substitution chain held"; it does NOT mean "0.190 was located by a finder on the spectrum." There is no `argmin`, no τ-grid, no eigenvalue scan in the script. The number 0.190 enters as a literal on line 123 and is checked against itself (read from `canonical_constants.py:288` `tau_fold = 0.19`, provenance `S42 constants_snapshot, fold_idx=7`).

**What the substitution chain actually proves.** The 6-step chain (script lines 25-68, mirrored in the registry patch lines 409-419) is entirely about the *character* of the point, not its location:
- Step 4: substitute `dS_fold = +58672.80` (from `canonical_constants.py:512`, S42 origin).
- Step 5: `+58672.80 ≠ 0 ⇒ τ_fold is NOT a critical point of S(τ)`.
- Step 6: `dS/dτ > 0 ⇒ S INCREASING ⇒ substrate PUSHED THROUGH τ_fold` (supersonic transit, not held).

Every step consumes `dS_fold`, `d2S_fold`, `S_fold` — the spectral-action gradient/curvature at the fold. NONE of them locates `τ*`. The theorem's genuine content is the **transit-identifier predicate** `dS/dτ|_{τ_fold} ≠ 0`: it establishes that whatever the fold is, it is a NON-STATIONARY van-Hove cusp (the substrate is pushed through it), retiring the pre-S85 "triple-gear equilibrium" framing. That is real structural content — and it is LOCATION-INDEPENDENT. A cusp at 0.190 and a cusp at 0.191038 are equally non-stationary; `dS/dτ` is finite and positive in a neighborhood of both (the registry patch line 405 confirms `d²S/dτ² > 0` is a *right-neighbourhood* convexity claim, not a point-pin).

**The decisive distinction.** I separate two propositions the seed conflates:
1. **EXISTENCE/CHARACTER** (what S85 proves): "ρ(λ=0;τ) has a unique van-Hove cusp on cubic-BC class Γ_6, and that cusp is non-stationary (`dS/dτ ≠ 0`), so the substrate transits through it rather than equilibrating at it." — PERMANENT, location-free, regulator-robust.
2. **LOCATION** (what S85 does NOT compute): "that cusp sits at exactly τ = 0.190." — this is a SEPARATE substrate-IS observable, and S85 *imports* its value from CONST-FREEZE-42 (S12/S42), never derives it. The producing gate's own `convention=canonical_constants-S85-freeze` names the import explicitly.

The registry-anchor situation reinforces this. The seed cites "§VII.M.W10-3 (connes+lizzi)" as the permanent τ_fold-uniqueness theorem, but the actual registry section at `§VII.M.W10-3` (permanent-results-registry.md:15982) is the **c_sub Γ-LIKE-but-Γ-INEXACT residual** entry — an unrelated A_s-pipeline flag, status NEEDS-COMPUTATION. The τ_fold van-Hove theorem the seed describes lives in the *producing gate* (`S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM`) and its registry-patch file (`s85_w10_tau_fold_REGISTRY_PATCH.md`), which targets `§VII-B`, authored by **kaku-speculative-theorist** (registry patch line 371), NOT connes+lizzi. This matters for (c): the "PERMANENT theorem at §VII.M.W10-3 that a re-pin would invalidate" is partly a mis-citation — the slot the seed names carries different content. lizzi must confirm the actual registry home of the uniqueness theorem before we can scope a cascade against it.

**Answer to (a):** S85 pins the EXISTENCE and the NON-STATIONARITY CHARACTER of a unique cusp. It does NOT pin the VALUE 0.190 — it imports 0.190 (CONST-FREEZE-42) and the `value='promoted'` is a self-consistency check (live pin == frozen literal). The precise LOCATION is a separate substrate-IS observable, which is precisely what the from-scratch crossing finder computes.

### T2: Sub-question (b) — Is τ_cross=0.191038 the substrate-derived canonical value? Does the ±0.5% band have substrate-physical meaning?

**Verdict on (b): τ_cross=0.191038 IS the substrate's derived LOCATION of the band-edge anticrossing — it is the substrate-IS observable. But the ±0.5% band is a CHOSEN tolerance with no substrate-physical meaning, and I disown it as a value-selection gate. The honest reading is two-fold: 0.191038 is the located value; 0.190 is a 0.55%-off legacy import that sits ~99× off the actual minimum-gap point.**

**The from-scratch crossing functional IS the substrate-IS observable.** From `phononic-framing.md` §"IS Space, Not IN Space": the substrate IS the D_K eigenvalue spectrum and its band structure; τ_fold is the intrinsic deformation-parameter value at which the band edges touch. The W2-2 functional (working paper §W2-2 Step 1, lines 99) is exactly this:
```
Δ_band(τ) = |T5_min(τ) − T3_max(τ)|,   τ_cross = argmin_τ Δ_band
```
with T3 = (0,0)-sector MAX |λ|, T5 = (2,0)/(0,2)-sector MIN |λ| — the S44/S45 anticrossing pair, atlas-07 "[NEW S45] Van Hove TRUE crossing T3-T5". This is the substrate locating its OWN band-edge anticrossing: no 0.190 is supplied to the finder (working paper line 99: "0.190 is NOT supplied to the finder — the grid brackets [0.18,0.23]; the argmin returns 0.191038"). The substrate IS the anticrossing; the finder reads off where it occurs.

**The result is regulator-robust by sector-locality — this is the strongest possible saturation.** `τ_cross = 0.191038` is L_max-INVARIANT to float precision across {5,8,10,12} (working paper lines 86-89). The reason is structural, not numerical: the T3/T5 sectors have p+q ≤ 2, so they ARE the bottom band at every L_max ≥ 2 — higher sectors have `|λ|_min ~ √C₂` climbing away ((3,0)_min = 1.248 ≫ the |λ| ~ 0.97 crossing cluster). This is Friedrich-Bär saturation by sector-locality, NOT by L-extrapolation: there is no truncation tail to converge, because the relevant sectors are complete at L_max=2. The alt-mesh (1e-4 step, re-bracketed around the L12 from-scratch result NOT 0.190) returns 0.191039 (Δ = 1e-6, mesh-robust). It reproduces the independently-registered atlas-07 S45 TRUE-crossing `tau = 0.19104, delta_min = 3.27e-5` (atlas-07-permanent-results.md:402) to 5 significant figures. Three independent confirmations (L-invariance, mesh-robustness, S45 reproduction) all land on 0.191038, not 0.190.

**0.190 is demonstrably NOT the anticrossing point — it is ~99× off the minimum gap.** This is the decisive new datum. I computed the band-edge gap at both values:
```
gap at τ = 0.191038 (the crossing)      = 8.495e-06   (gap_min, working paper line 89)
gap at τ = 0.190     (the L12 cache pt)  = 8.387e-04   (working paper truncation cross-check; s84_spectrum_cache_L12_tau019.npz)
ratio = 8.387e-4 / 8.495e-6             = 98.7
```
The two band edges are ~99× CLOSER at 0.191038 than at 0.190. The minimum of `Δ_band(τ)` — the actual van-Hove touching point — is at 0.191038. At 0.190 the edges are still ~99× apart; 0.190 is on the FLANK of the V-curve, not at its vertex. Substrate-first: the substrate's band edges touch (minimum gap) at 0.191038; 0.190 is a point near, but not at, that touching. The substrate did not select 0.190; the substrate selects 0.191038.

**The ±0.5% band: substitution chain.** The seed asks whether the gate's ±0.5% PASS band has substrate-physical meaning. I write it out (per `math-scripts.md` §"Double-Check Logic"):
- Step 1: `τ_cross = 0.19103812125290104` (working paper 4-tuple, line 119).
- Step 2: `τ_canon = 0.19` (canonical_constants.py:288).
- Step 3: `dev_rel = |τ_cross − τ_canon| / τ_canon = 1.038121e-03 / 0.19 = 0.005464`.
- Step 4: band = 0.005 (the gate's pre-registered ±0.5% relative).
- Step 5: `0.005464 > 0.005 ⇒ OUTSIDE band ⇒ magnitude=INFO`.

The arithmetic is correct: 0.5464% > 0.5%, INFO is the honest magnitude verdict against the pre-registered band. **But I disown the band as a value-selection criterion.** The ±0.5% was a *chosen* INFO_REL_TOL — and tellingly, it traces to the SAME `INFO_REL_TOL = 0.005` literal hardcoded in the S85 producing script (line 130: `INFO_REL_TOL = 0.005  # (local) 0.5%`). It is a drift-detection tolerance for a self-consistency check ("flag if the canonical pin has drifted ≤ 0.5%"), NOT a substrate-physical width. There is no substrate quantity that sets 0.5% as the resolution of the fold: the band-edge gap structure gives a resolution of ~1e-6 in τ (the mesh Δ at which the V-curve vertex is located), which is ~3 OOM TIGHTER than 0.5%. So the ±0.5% band cannot adjudicate "0.190 vs 0.191038" — the substrate resolves the crossing to ~1e-6, and at that resolution the two values are DIFFERENT points (0.191038 is the vertex; 0.190 is 99× up the flank). The INFO verdict is correct bookkeeping against a chosen tolerance; it is NOT evidence that "0.190 is within physical tolerance," because the physical tolerance (set by the gap structure) is far tighter than 0.5%.

**Answer to (b):** Yes — `τ_cross = 0.191038` is the substrate-derived location of the band-edge anticrossing, established by the substrate-IS crossing functional, regulator-robust by sector-locality, mesh-robust, and cross-confirmed by the S45 TRUE crossing. The ±0.5% band has NO substrate-physical meaning (it is the S85 drift-detection literal); at the substrate's own ~1e-6 resolution, 0.190 is NOT within tolerance of the anticrossing — it sits ~99× off the minimum gap. 0.191038 is the value; 0.190 is the import.

### T3: Sub-question (c) — RE-PIN cascade scope (S85 §VII.M.W10-3 theorem, archive scripts, downstream ratios S_0=95/56)

**Verdict on (c): a value-only re-pin DOES NOT invalidate the S85 structural content (existence + non-stationarity survive — they are location-free), but it DOES have a hard, enumerable cascade: it trips an `assert` in the S0-KNOB script, breaks the EXACT rational identity `S_0 = 95/56`, and forces re-validation of 119 registry rows. I pre-register the full cascade below so the verdict is actionable.**

I traced the consumers directly. The cascade splits cleanly into a SURVIVING layer (structural content, location-free) and a BREAKING layer (numeric identities anchored to the literal 0.19 = 19/100).

**LAYER 1 — SURVIVES a value-only re-pin (the structural content is location-independent):**

- **S85 uniqueness/non-stationarity theorem.** Per T1, the theorem's content is "a unique van-Hove cusp EXISTS and is non-stationary (`dS/dτ ≠ 0`)." Both clauses are location-free. Re-pinning the cusp's *value* to 0.191038 leaves "a unique non-stationary cusp exists" intact — indeed it STRENGTHENS it, because 0.191038 is the true minimum-gap point (T2) where the van-Hove touching actually occurs, whereas 0.190 sits ~99× up the flank. The `dS/dτ`, `d2S/dτ²` non-stationarity predicates re-evaluate at 0.191038 with the same signs (positive gradient, positive right-neighbourhood convexity — confirmed by the monotone S(τ) flow, dS/dτ = +58672.8 with empty critical set, S95 NO-WELL-ONE-LOOP). NOTE the registry-anchor caveat from T1: the seed's "§VII.M.W10-3 PERMANENT (connes+lizzi)" does not resolve to a τ_fold theorem — that slot (permanent-results-registry.md:15982) is the c_sub Γ-residual entry. The actual uniqueness theorem is in the `s85_w10_tau_fold_REGISTRY_PATCH.md` (targets §VII-B, kaku-authored). The seed's `proven_1424`/`proven_1905` "bounds tau_fold" reference must be located by lizzi before we can claim what it pins; I could not find a §VII.M.W10-3 row that bounds τ_fold.

- **atlas-07 "[NEW S45] Van Hove TRUE crossing T3-T5" (atlas-07-permanent-results.md:402).** This entry already records `tau = 0.19104` — it AGREES with 0.191038, not with 0.190. A re-pin to 0.191038 makes the canonical pin CONSISTENT with this permanent atlas row, removing a standing internal disagreement. This row needs no amendment.

**LAYER 2 — BREAKS under a value-only re-pin (hard, enumerable):**

- **S101-W3-S0-KNOB `S_0 = τ_fold/T_acoustic = 95/56` — the rational identity SHATTERS, and the script CRASHES.** I read `computations/session-101/s101_w3_s0_knob.py`. Lines 234-241:
```python
tau_f = Fraction(19, 100)                                   # (local) tau_fold exact
T_ac  = Fraction(14, 125)                                   # (local) T_acoustic exact
...
assert abs(float(tau_f) - tau_fold) < 1e-15, "tau_fold canonical drift"
```
The gate's verdict literally records `identity S0*T_ac==tau_fold exact=True` and `S0=tau_fold/T_acoustic=95/56` (s101_gate_verdicts.txt, S101-W3-S0-KNOB companion rows). Substitution chain (per `math-scripts.md`):
  - Step 1: `tau_fold = 19/100` (exact), `T_acoustic = 14/125 = 0.112` (canonical_constants.py:739; note `56/500 = 14/125`).
  - Step 2: `S_0 = (19/100)/(14/125) = 19·125/(100·14) = 2375/1400 = 95/56` EXACT.
  - Step 3: `95/56 = 1.6964285…`; the gate selects knob (iii) with dev 0.13% as the WINNING discriminator against (i) 0.65% and (ii) 1.44%.
  - Step 4 (re-pin): `0.191038/0.112 = 1.705696…`; this is NOT `95/56` (rel dev 0.5463%); it is not a clean rational of small denominator.
  - Step 5: The hard `assert abs(float(tau_f) - tau_fold) < 1e-15` would FAIL (`|0.19 − 0.191038| = 1.04e-3 ≫ 1e-15`) — the script raises `AssertionError("tau_fold canonical drift")` and crashes. The exact-rational identity `S0*T_ac == tau_fold` that the gate's discriminator depends on is DESTROYED. Worse: the knob-(iii) WIN (dev 0.13%) was the *reason* (iii) beat (i)/(ii); re-pinning shifts (iii)'s dev to 0.55%, which would change the discriminator outcome (the 0.13% margin was the selector). **This is a genuine cascade-invalidation: a re-pin does not merely shift S_0's value, it breaks the structural claim that S_0 IS the rational τ_fold/T_acoustic and re-opens the S0-KNOB discrimination.**

- **The 119 registry rows citing `tau_fold ≈ 0.19`.** `grep -cE "tau_fold = 0.19|τ_fold = 0.19|tau_fold.*0.190"` over permanent-results-registry.md returns 119. Most cite it as a STRUCTURAL ANCHOR ("evaluated at the fold τ_fold = 0.190," e.g. the type-IV EMT at line 22090, the §VII.CC/§VII.CE Level-1 single-τ-slice entries) — those are unaffected in *content* by a 0.55% shift (the spectral-triple at 0.191038 is the same structural object). But each row's *numeric value* 0.190 would become stale, requiring a registry-wide find-replace + re-validation pass. The registry tag itself (line 2620: `τ_fold | 0.190 | scheme=STRUCTURAL-IDENTITY, convention=exact-algebraic`) is currently FALSE under a re-pin: 0.190 is NOT a structural-identity/exact-algebraic value if the substrate's anticrossing is at 0.191038 — 0.190 = 19/100 is a *frozen round number*, while 0.191038 is the *computed* value. (Ironically the "exact-algebraic" tag fits 0.190 better as a rational 19/100 than it fits the irrational-looking 0.191038 — which is part of why this is a genuine adjudication, not a trivial fix.)

- **Archive scripts `s85_w0_van_hove_cusp_theorem.py` + `s85_w10_tau_fold_van_hove_theorem.py`.** Both exist on disk (verified). The W10 script's `consistency_check()` hardcodes `EXP_TAU_FOLD = 0.19` (line 123) and PASSes iff the live pin matches it to 1e-10. A re-pin to 0.191038 flips its verdict from PASS (`value='promoted'`) to FAIL (`value='blocked-by-drift'`) — because the consistency check is literally "does the live pin still equal the frozen 0.19." This is not an invalidation of the *theorem*; it is the consistency-check correctly detecting that the canonical pin moved. Per absolute verdict permanence (`gate-verdicts.md`), the S85 PASS line is retained; a re-run would emit a superseding FAIL line tagged `supersedes=149e29a6…`. These are ARCHIVE scripts (session-85), so a re-run is a deliberate choice, not automatic.

**Cascade summary (pre-registered, actionable):**

| Object | Value-only re-pin effect | Action required |
|:-------|:-------------------------|:----------------|
| S85 existence + non-stationarity theorem | SURVIVES (location-free) | None (content unchanged; arguably strengthened) |
| atlas-07 S45 TRUE-crossing 0.19104 | SURVIVES + now consistent | None (already agrees with 0.191038) |
| `S_0 = 95/56` rational identity | BREAKS (no longer exact rational) | S0-KNOB re-derivation; the 95/56 claim retracts |
| `s101_w3_s0_knob.py` | CRASHES (assert trips) | Code edit (Fraction → new value) + re-run |
| 119 registry rows citing 0.190 | Numeric staleness | Registry find-replace + re-validation pass |
| canonical_constants.py:288 + tag line 2620 | Value + STRUCTURAL-IDENTITY tag stale | Re-pin + provenance update + tag correction |
| `s85_w10` archive script | PASS → FAIL (consistency-check) | Re-run (deliberate) + superseding verdict line |

**Answer to (c):** The structural content (existence + non-stationarity) SURVIVES a value-only re-pin and is location-free. But the re-pin has a real, hard cascade: it breaks the EXACT rational `S_0 = 95/56`, crashes the S0-KNOB script via its `assert`, re-opens the S0-KNOB discriminator, and stales 119 registry rows + the canonical pin's STRUCTURAL-IDENTITY tag. The cascade is NOT cosmetic — the 95/56 rational identity is a genuine structural claim that a re-pin destroys. This is exactly the tension that forces the output-form choice in T4.

### T4: Cross-Cutting — Which pre-registered output form (i re-pin / ii 0.190-stands / iii two-value convention), and why

**I advocate output form (iii): a TWO-VALUE registry convention with explicit non-fungibility — 0.191038 = the from-scratch-crossing LOCATION observable; 0.190 = 19/100 = the rational-anchor / uniqueness-theorem freeze. They are non-fungible because they answer DIFFERENT substrate questions and carry DIFFERENT structural roles.** I reach this from the analysis, not from a desired verdict — and I flag honestly that it runs AGAINST my own naive prior (as the crossing-functional author I came in leaning toward output-form (i), full re-pin).

**Why not (i) full re-pin.** My T2 establishes 0.191038 IS the substrate's located anticrossing, and naively that argues for re-pinning `tau_fold → 0.191038`. But T3 surfaces a structural fact that defeats the clean re-pin: **0.190 = 19/100 carries an EXACT rational identity that 0.191038 does not.** `S_0 = τ_fold/T_acoustic = (19/100)/(14/125) = 95/56` is exact (`identity S0*T_ac==tau_fold exact=True`, S101-W3-S0-KNOB), and 95/56 is the WINNING discriminator (dev 0.13% beating 0.65%/1.44%). 0.191038/0.112 = 1.7057 is not a clean small-denominator rational. A full re-pin would not *improve* S_0 — it would DESTROY a structural identity and re-open a settled discriminator. The substrate's "exact-algebraic" tag (registry line 2620) currently fits 19/100 better than it fits 0.191038. So 0.190 is not *merely* a stale import; it is doing structural work (as the rational anchor of the S_0 ratio chain) that 0.191038 cannot do. Re-pinning sacrifices that. **(i) is wrong.**

**Why not (ii) 0.190-stands / 0.191038-within-tolerance.** This is the SYMMETRIC error. My T2 shows the ±0.5% band is a chosen drift-tolerance (the S85 `INFO_REL_TOL = 0.005` literal), not a substrate width; at the substrate's own ~1e-6 resolution, 0.190 is ~99× off the actual minimum-gap point. Calling 0.191038 "finder/mesh noise" is FALSE: it is L_max-invariant by sector-locality, mesh-robust to 1e-6, and reproduces the independently-registered S45 TRUE crossing. The substrate genuinely locates its anticrossing at 0.191038, and saying "0.190 stands, the rest is noise" suppresses a real substrate-IS observable. **(ii) is wrong** — it would let a chosen 0.5% tolerance veto a regulator-robust substrate computation.

**Why (iii) is the honest reading.** The two values are non-fungible because they are answers to two DIFFERENT substrate questions:
- **0.191038 answers: "Where does the D_K band-edge anticrossing (the van-Hove touching, minimum gap) occur?"** — a LOCATION observable, computed from-scratch, regulator-robust. This is the substrate-IS crossing-functional value. It belongs in the registry as the **from-scratch-crossing location** (consistent with the atlas-07 S45 TRUE-crossing 0.19104, which it confirms to 5sf).
- **0.190 = 19/100 answers: "What is the rational freeze that anchors the derived-ratio chain and the non-stationarity uniqueness theorem?"** — a RATIONAL ANCHOR. It is the value the S_0 = 95/56 identity, the S85 consistency-check, and 119 registry rows are built on. Its structural role is as the exact rational 19/100, NOT as a claim about the precise anticrossing location.

These cannot collapse to one number because no single value is simultaneously (a) the located minimum-gap point AND (b) the exact rational 19/100. The substrate-IS LOCATION is 0.191038; the structural-ANCHOR rational is 19/100; they differ by 0.55% precisely because the round-number freeze (CONST-FREEZE-42, S12/S42) was set BEFORE the from-scratch crossing was ever located (S45/S114). The non-fungibility is: **demoting 0.190 breaks S_0 = 95/56 and 119 rows; demoting 0.191038 suppresses the substrate's located anticrossing and contradicts atlas-07.** Per `phononic-framing.md` substrate-first, BOTH are real substrate-IS facts at different layers (a located spectral feature vs a rational deformation-parameter anchor) — exactly the SCALE-AND-CHANNEL-TAGGING pattern: tag WHICH question each value answers, never conflate them. This mirrors my own prior two-leaf precedent (TRANSIT-PS-67, the τ-cusp conjugate-pair signature): the same substrate object carries distinct scale-tagged readings that must not be collapsed.

**The convention I propose (for lizzi to confirm/amend):**
- Registry carries `τ_fold^{anchor} = 0.190 = 19/100` (rational freeze; anchors S_0 = 95/56, the S85 non-stationarity uniqueness theorem, the 119 structural-anchor rows) AND `τ_fold^{crossing} = 0.191038` (from-scratch band-edge anticrossing LOCATION; substrate-IS crossing functional; confirms atlas-07 S45 0.19104).
- Explicit NON-FUNGIBILITY statement: the two are distinct functionals of ρ(λ;τ) (rational-anchor freeze vs argmin band-gap locator); neither replaces the other; the 0.55% gap is the round-number-freeze-vs-located-value offset.
- Correct the registry line-2620 tag: 0.190's `scheme=STRUCTURAL-IDENTITY, convention=exact-algebraic` should read `RATIONAL-FREEZE (19/100, CONST-FREEZE-42)`, NOT "structural-identity" (it is a frozen round number, not a derived structural identity — the DERIVED value is 0.191038).
- No `canonical_constants.py` re-pin of the working `tau_fold` (avoids the S0-KNOB crash and the 119-row cascade); 0.191038 lands as a NEW registered observable `tau_cross_van_hove`, not as a replacement.

**Questions lizzi must answer for the verdict to converge:**
1. **Registry-anchor location**: Where is the actual S85 τ_fold uniqueness theorem registered? The seed cites §VII.M.W10-3, but that slot (registry:15982) is the c_sub Γ-residual entry. Is the uniqueness theorem in §VII-B (per the kaku-authored `s85_w10_tau_fold_REGISTRY_PATCH.md`)? What are `proven_1424`/`proven_1905` and do they bound τ_fold to a VALUE or to existence? (c)'s cascade scope depends on this.
2. **DOS-singularity-strength vs crossing-location**: From your spectral-functional reading, does "non-stationarity uniqueness" certify a VALUE, or (as I argue in T1) only the existence + character of a unique cusp? Is there ANY clause of the S85 theorem that DERIVES 0.190 rather than importing it?
3. **The rational anchor**: Do you accept that 0.190 = 19/100's structural role is as the exact rational anchoring S_0 = 95/56 — i.e. that it does work 0.191038 cannot — and therefore that a full re-pin (output (i)) is net-destructive? Or do you read S_0 = 95/56 as itself an artifact of the round-number freeze (in which case the rational identity is not load-bearing and (i) becomes viable)?
4. **Output-form**: Do you concur with (iii) two-value non-fungibility, or do you read the DOS-singularity-strength functional as selecting 0.190 as the substrate's genuine value (pushing toward (ii))? If (ii), how do you reconcile the ~99× wider band-edge gap at 0.190 vs the located minimum at 0.191038?

---

## Round 1 — lizzi: Response & Cross-Synthesis

### Part 1: Response to transit's Sections

#### Re: T1 — S85 uniqueness: VALUE vs EXISTENCE-of-cusp

**AGREE (on the gate mechanics) — your provenance archaeology is correct, and I confirm it from the inside.** I co-authored this theorem with connes, and I will not defend the prior past where the math actually goes. I re-read `s85_w10_tau_fold_van_hove_theorem.py` and your reading of the gate machinery is exact: `EXP_TAU_FOLD = 0.19` is a `# (local)` literal on line 123; `consistency_check()` (lines 169-222) loads the LIVE `tau_fold` and compares it to that frozen literal; `compute()` (lines 258-283) resolves `value="promoted"` iff `ch["chain_complete"] AND cc["all_strict"]` — i.e. iff the substitution chain holds AND the live pin still equals 0.19 to `PASS_ABS_TOL=1e-10`. There is no `argmin`, no τ-grid, no `ρ(λ;τ)` evaluation, no eigenvalue scan anywhere in the script. The gate is a **self-consistency check** that the canonical pin has not drifted, NOT a finder. `value='promoted'` means exactly "the chain held AND 0.19 == 0.19." You are right about the mechanism.

**DISAGREE (on the scope of the conclusion) — the registered THEOREM TEXT does name 0.190, and that matters for (c).** Your T1 concludes "S85 pins the EXISTENCE and the NON-STATIONARITY CHARACTER... it does NOT pin the VALUE 0.190." That is the right reading of the gate's *mathematical content*, but it is too strong as a reading of what is *registered*. I queried the knowledge index for the two theorem objects the seed names:
- `proven_1424` statement (verbatim): **"tau_fold = 0.190 PERMANENT van-Hove-cusp non-stationarity uniqueness theorem"** (PROVEN, S85, source `Phononic-framework-hypothesis.md`).
- `proven_1905` statement (verbatim): **"tau_fold = 0.190 promoted to van-Hove-cusp non-stationarity uniqueness theorem | S85 W10-3 | connes + lizzi | PERMANENT"** (source `atlas-07-permanent-results.md`).

Both registered statements carry `tau_fold = 0.190` IN THE STATEMENT. The registry patch the gate writes (`s85_w10_tau_fold_REGISTRY_PATCH.md` lines 373-381, which I co-signed) reads "the eigenvalue-density function ρ(λ = 0; τ) has a UNIQUE van Hove cusp at **τ_fold = 0.190**." So the slot does not merely register existence — it registers a *located* claim. This is the gap I have to own honestly: **the theorem STATEMENT asserts a value the theorem's PROOF (the 6-step substitution chain) never derives.** The value 0.190 rides into the statement from CONST-FREEZE-42 (the gate's own `convention=canonical_constants-S85-freeze` names the import), while the chain proves only `dS/dτ|_{τ_fold} ≠ 0` (Steps 4-6: non-stationarity). The honest characterization is not "S85 doesn't pin the value" — it is "**S85 ASSERTS the value 0.190 in its registered text but only PROVES the non-stationarity character; the '= 0.190' is an imported premise, not a derived conclusion.**" That distinction is sharper than yours and it is the one that licenses a clean fix.

**MISSED (the spectral-functional fact that decides whether the value-assertion is even ABOUT 0.191038) — the word "uniqueness" certifies the cusp's CHARACTER, not its LOCATION, and the functional that locates it is NOT the functional S85 uses.** Here is what the transit-dynamics reading does not surface. The S85 theorem's operative functional is the **DOS-singularity functional** — it is a statement about `ρ(λ=0;τ)` and `dρ/dτ` (Steps 1-2 of the chain: van-Hove cusp = one-sided divergence of `dρ/dτ`), and its non-stationarity predicate rides on `dS/dτ` (the spectral-action gradient, a moment of `ρ`). The from-scratch crossing functional `Δ_band(τ) = |T5_min(τ) − T3_max(τ)|` is a **DIFFERENT functional of the same `ρ(λ;τ)`** — it tracks the band-EDGE gap between two specific sectors, not the singularity STRENGTH of the zero-mode density. We already have the precedent that these two functionals return DIFFERENT numbers on the SAME spectrum: the DOS-peak (argmax of singularity strength, `S85-VAN-HOVE-CUSP-THEOREM` L=8) sits at **0.221**, while the band-edge crossing sits at **0.191038** — a 0.031 (16%) separation. This is exactly the `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` / diffusion-window warning: two distinct functionals `Φ` of the same `P(σ)`/`ρ(λ;τ)` may differ arbitrarily and MUST NOT be conflated. So the genuine spectral-functional question is sharper than "value vs existence": **does the S85 DOS-non-stationarity functional even have a stationary point or a distinguished location at 0.190, or does it only certify that wherever the cusp is, it is non-stationary (`dS/dτ ≠ 0`)?** The answer (my L1) is the latter — `dS/dτ = +58672.8` is finite and positive across the WHOLE transit window (S95 NO-WELL-ONE-LOOP: empty critical set), so the non-stationarity functional has NO distinguished point at all; it is location-free by construction. The "uniqueness" in the title refers to the cusp being the unique van-Hove feature on the cubic-BC class Γ_6 — a CHARACTER/multiplicity statement — NOT to its location being uniquely pinned to 0.190. **So I AGREE with your bottom line that the gate imports 0.190, and I SHARPEN it: the theorem's own functional is incapable of locating the cusp, so the registered "= 0.190" is doubly an import — imported into the gate, AND attached to a functional that has no locating power.**

#### Re: T2 — τ_cross=0.191038 as canonical; the ±0.5% band

**AGREE — 0.191038 is a real substrate-IS observable, regulator-robust, and NOT mesh/finder noise. Reading-A's "0.55% is noise" is refuted, and I retract any temptation to hold it.** Your three independent confirmations are decisive on this point and I have nothing to add against them: (i) L_max-INVARIANCE across {5,8,10,12} by sector-locality (T3/T5 have p+q ≤ 2, so they ARE the bottom band at every L_max ≥ 2 — the strongest Friedrich-Bär saturation, no truncation tail to converge); (ii) mesh-robustness to Δ=1e-6 (alt-mesh re-bracketed around the L12 result, NOT 0.190); (iii) reproduction of the independently-registered atlas-07 S45 TRUE crossing `tau = 0.19104, delta_min = 3.27e-5` to 5sf. From my own domain: this is precisely the saturation signature my permanent results lean on — sector-local saturation is the SAME structural mechanism as the multiplicative-normalization L_max-invariance I documented (`math-scripts.md §"Multiplicative-normalization cancellation"`, my K=3 promotion S94 W6-18), where the discriminating content lives at the *plateau value* and the L_max-stability is structural-by-construction. So 0.191038 is a genuine substrate-IS plateau value, not a fit artifact. **Reading-A in its "noise" form is dead. I confirm the kill.**

**AGREE — the ±0.5% band has NO substrate-physical meaning, and I confirm its provenance with the corresponding literal from MY co-authored gate.** You traced the ±0.5% to a chosen `INFO_REL_TOL`. I can pin it exactly: the W2-2 gate's band is `0.005`, and the SAME `INFO_REL_TOL = 0.005  # (local) 0.5%` literal is hardcoded on **line 130 of `s85_w10_tau_fold_van_hove_theorem.py`** — the gate I co-authored. In that gate it is the INFO-band for a *drift-detection* check: line 194 `info_band = (not strict) and rel_diff < INFO_REL_TOL` flags "the canonical pin drifted by ≤ 0.5%, refresh next session" (the `value="info-minor-drift"` branch, lines 268-269). It was NEVER a resolution scale for the cusp location — it is the tolerance below which we would not bother re-freezing a constant. So I agree completely: **the ±0.5% band is a housekeeping drift-tolerance, not a substrate width, and it cannot adjudicate "0.190 vs 0.191038."** This is a clean instance of `epistemic-discipline.md §"Source Reconciliation"` Class-(c) PIN-DRIFT-FROM-STALE-SOURCE: a literal threshold (the ±0.5% band) is testing a quantity (cusp resolution) it was never derived for.

**DISAGREE (sharply) — with the framing that the band-edge GAP sets the substrate's resolution and therefore that "0.190 is ~99× off the minimum gap" is the decisive datum. The 99× is real but it is the WRONG axis, and presenting it as "0.190 is not the anticrossing point" overclaims.** Let me write the substitution chain (per `math-scripts.md §"Double-Check Logic"`), because your T2 uses the 99× to argue "0.190 is on the FLANK, not at the vertex," and that conflates the gap MAGNITUDE axis with the τ-LOCATION axis:
- Step 1: `gap(0.191038) = 8.495e-06` (the V-curve vertex; W2-2 line 89).
- Step 2: `gap(0.190) = 8.387e-04` (W2-2 line 95, L12-cache).
- Step 3: `ratio = 8.387e-4 / 8.495e-6 = 98.7` — the gap is ~99× larger at 0.190. CORRECT arithmetic.
- Step 4 (the axis correction): the gap is a function `g(τ)` with a sharp minimum (a V or cusp, `delta_min = 3.27e-5` at the vertex). A 99× gap-RATIO between two points does NOT mean those points are far apart in τ — it means `g(τ)` is STEEP near the vertex. The τ-separation is `|0.191038 − 0.190| = 0.001038` (0.55%), which is SMALL. A function with a near-cusp minimum has a huge value-ratio over a tiny argument-separation BY CONSTRUCTION (that is what "near-cusp" means — `dg/dτ → ∞`). So "99× off the minimum gap" and "0.55% off in τ" are the SAME fact read on two axes; the 99× does not add independent evidence that 0.190 is "not the anticrossing point."
- Step 5: the substrate-IS LOCATION observable is `argmin_τ g(τ) = 0.191038`. That IS the anticrossing point, full stop. 0.190 is 0.55% away from it in the deformation parameter. Whether that 0.55% is "negligible" or "decisive" is exactly the open question — and the 99× gap-ratio cannot settle it, because it is a restatement of the 0.55% through the steepness of `g`, not a second measurement.

So I AGREE 0.191038 is the located argmin, and I DISAGREE that the 99× gap-ratio shows "0.190 is demonstrably NOT the anticrossing point." 0.190 is demonstrably 0.55% away from the argmin in τ; the gap structure tells us `g` is steep there, nothing more. **Your located value is right; your rhetorical amplifier (99×) is the gap-magnitude axis masquerading as the location axis.** This matters because it is the difference between "re-pin, 0.190 is wrong" (your T2 lean) and "0.190 is a 0.55%-displaced anchor of a real located feature" (where I land in L2/T4).

**MISSED (what the spectral-functional reading reveals about WHICH value the substrate "selects") — the substrate does NOT select a single number; it selects a band-edge anticrossing FEATURE, and "0.191038" is the argmin LOCATOR of that feature on one functional, not "the substrate's value" in the sense the canonical pin needs.** Your T2 says "the substrate selects 0.191038." I would say: the substrate IS the band structure `{λ_k(τ)}`; the band edges T3/T5 approach to within `3.27e-5` near `τ ≈ 0.191`; the *feature* (the anticrossing) is substrate-IS, and 0.191038 is where one functional (`argmin Δ_band`) reads it off. But the canonical `tau_fold` pin is consumed by gates as a *rational deformation-parameter anchor* (S0 = 95/56, the S85 freeze, 119 rows) — it answers a DIFFERENT question than "where is the argmin of the band-gap." This is the functional-identity question I develop in L2: the from-scratch crossing functional and the S85 non-stationarity functional are not the same observable, so neither one's value automatically becomes THE canonical pin — they are two readings, and the canonical pin may name a THIRD role (the rational anchor) that both of them approximate. **0.191038 is the strongest LOCATION reading; it is not thereby the canonical pin, because the canonical pin is not a location observable — it is a rational anchor.**

#### Re: T3 — RE-PIN cascade scope

**AGREE — your cascade enumeration is correct, actionable, and I verified the two load-bearing items independently.** I read `s101_w3_s0_knob.py` lines 220-289 directly. Both your decisive claims hold:
- Line 234: `tau_f = Fraction(19, 100)`; line 240: `assert abs(float(tau_f) - tau_fold) < 1e-15, "tau_fold canonical drift"`. A re-pin `tau_fold → 0.191038` gives `|0.19 − 0.191038| = 1.038e-3 ≫ 1e-15` ⇒ the `assert` FIRES ⇒ the script crashes with `AssertionError("tau_fold canonical drift")`. Confirmed.
- Line 248: `S0_pred_iii_exact = tau_f / T_ac` = `(19/100)/(14/125)` = `95/56` EXACT, and (iii) is the structural selector winner (`graded-per-C2-quantum`, lines 277-285). Re-pinning replaces 19/100 with 0.191038, which is NOT a clean small-denominator rational (`0.191038/0.112 = 1.70570`, not 95/56 = 1.69643), so the EXACT rational identity `S0 = 95/56` is destroyed. Confirmed.

So a value-only `canonical_constants.py` re-pin is genuinely NOT cosmetic: it crashes a downstream script and shatters a structural rational identity. This is the hard fact that makes (i) full re-pin net-destructive, and I agree with your LAYER-1-survives / LAYER-2-breaks split.

**AGREE — and I CONFIRM the registry-anchor archaeology from the inside, with the repair already on record.** Your T1/T3 flag that the seed's "§VII.M.W10-3 (connes+lizzi)" does not resolve to a τ_fold theorem — that slot (registry line 15982) is the **c_sub Γ-LIKE-but-Γ-INEXACT residual** entry (S86 W-10, also connes+lizzi, but unrelated content). I confirm this, and I can resolve your open Question 1 completely: the **S112 W2 anchor-hygiene note already repaired this overload**. At `permanent-results-registry.md` line 22260 (the §VII.CH NOHOLOFLUX distinctness paragraph, mack-cosmic-bridge): *"earlier text labelled this consumed S85 theorem '§VII.M.W10-3'; that registry SLOT actually holds the unrelated S86 W-10 c_sub Γ-INEXACT residual entry... only the slot-LABEL pointer was overloaded — repaired here to the genuine gate name `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` (knowledge-index theorem `proven_1424`/`proven_1905`)."* So the canonical home of the S85 τ_fold uniqueness theorem is: **the gate `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` + knowledge-index theorems `proven_1424` (source `Phononic-framework-hypothesis.md`) and `proven_1905` (source `atlas-07-permanent-results.md`) + the registry-patch file `s85_w10_tau_fold_REGISTRY_PATCH.md` (which targets §VII-B, kaku-authored prose).** It is NOT registry slot §VII.M.W10-3. The cascade in (c) must scope against THOSE objects, not against the c_sub residual slot. (Your T1 caution was right to refuse to claim what §VII.M.W10-3 pins — it pins c_sub, not τ_fold.)

**DISAGREE / MISSED — you under-counted ONE consumer that strengthens the case for NOT re-pinning, AND you mis-rated one registry-tag fix.** Two corrections to your cascade:

1. **The §VII.CH NOHOLOFLUX STAGE-3-PERMANENT theorem CONSUMES the cusp's location-at-0.190, and a re-pin would force a cross-check of an already-promoted joint theorem.** Registry line 22260 + 22268: NOHOLOFLUX's Projection 3 "consumes the cusp's existence + monotone pass-through," and its canonical-anchors block pins `tau_fold = 0.190` (S42 CONST-FREEZE-42). NOHOLOFLUX is STAGE-3-PERMANENT (Stage-2 cross-axis PASS-AND `CF-S112-NOHOLOFLUX-STAGE2`, audit `d0779323…`). A value-only re-pin does not BREAK its structural content (Projection 3 needs the cusp to be non-stationary and monotone-passed-through, both location-free — exactly your LAYER-1 logic), but it DOES stale its pinned anchor `tau_fold = 0.190` and would, under the strictest reading, route a re-validation of a STAGE-3-PERMANENT joint theorem's anchor block. You missed this consumer; it is a THIRD permanent object (after the S85 theorem and atlas-07) on the 0.190 ledger, and it pushes the same direction: the structural content is location-free and survives, but the cascade of *anchor staleness* is wider than your 119-row count (it includes a promoted joint theorem's pinned-anchor block).

2. **You under-rate the registry-tag correction at line 2620, and you have the direction exactly inverted from where the substrate-functional reading lands.** You wrote: *"the registry line-2620 tag: 0.190's `scheme=STRUCTURAL-IDENTITY, convention=exact-algebraic` should read `RATIONAL-FREEZE`... it is a frozen round number, not a derived structural identity."* I half-agree and half-disagree. AGREE that 0.190 = 19/100 is a frozen rational, not a derived spectral structural-identity (no spectral computation derives 19/100). But DISAGREE that the fix is to call it "RATIONAL-FREEZE" and leave it — because that under-states what we now know. The substrate-functional reading (my L1/L2) says the canonical pin is doing the work of a **rational anchor for the derived-ratio chain** (S0 = 95/56), AND there is a located feature at 0.191038 that the anchor approximates to 0.55%. The honest tag is not "RATIONAL-FREEZE" simpliciter — it is "**rational anchor (19/100, CONST-FREEZE-42) of the substrate's band-edge anticrossing FEATURE located at τ_cross = 0.191038**." The tag should carry BOTH the rational-anchor role AND the pointer to the located value, because the 0.55% gap between them is now a *known, characterized* quantity (the round-number-freeze-vs-located-value offset), not an unexamined coincidence. That is the two-value convention, made into a tag.

**Net on (c):** your LAYER-1/LAYER-2 split is correct and I add NOHOLOFLUX to LAYER-1 (survives structurally) + the anchor-staleness ledger. The cascade is real, hard, and asymmetric: re-pinning breaks `S0 = 95/56` + crashes S0-KNOB + stales 119 rows + the NOHOLOFLUX anchor block; NOT re-pinning suppresses nothing structural (0.191038 lands as a NEW registered observable). The asymmetry is the whole argument for output (iii), which I take up in Re:T4.

#### Re: T4 — Output-form selection

**AGREE — output (iii), the two-value non-fungibility convention, and I reach it from the spectral-functional side INDEPENDENTLY of your cascade argument.** You arrived at (iii) from the cascade asymmetry (re-pin breaks S0=95/56; 0.190-stands suppresses the located feature). I arrive at the same place from the functional-identity argument, which is a second, independent route — and two independent routes converging on (iii) is the strongest form of agreement this workshop can produce. My route: the two values are answers to **two distinct spectral functionals of the same `ρ(λ;τ)`**, and no single number is simultaneously the argmin of `Δ_band` AND the exact rational 19/100 that anchors the derived-ratio chain. (i) full re-pin sacrifices the rational anchor; (ii) 0.190-stands suppresses a regulator-robust located observable. Both are lossy. (iii) keeps both, tagged by which question each answers. **Concur.**

**AGREE (and sharpen) — your (iii) convention is right, with ONE amendment to the registry-tag and ONE strengthening of the non-fungibility statement.**

Your proposed convention:
> `τ_fold^{anchor} = 0.190 = 19/100` (rational freeze; anchors S_0, S85 theorem, 119 rows) AND `τ_fold^{crossing} = 0.191038` (from-scratch band-edge anticrossing LOCATION).

I concur with this structure. Two refinements from the spectral-functional reading:

1. **The non-fungibility is STRUCTURAL, not just bookkeeping — it is the `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` distinction, and I want it stated as such.** The two values are not "the same observable at different precision." They are two DIFFERENT functionals: `τ_fold^{crossing} = argmin_τ |T5_min − T3_max|` (a band-EDGE-gap locator) and `τ_fold^{anchor} = 19/100` (a rational deformation-parameter freeze that the DERIVED-RATIO chain and the DOS-non-stationarity theorem are built on). Per the diffusion-window-observable specialization, two distinct functionals of the same `ρ(λ;τ)` may differ and must not be conflated — and here they DO differ (by 0.55%), so the non-fungibility is forced by the functional distinction, not chosen for convenience. State it in the registry as: *"τ_fold^{anchor} and τ_fold^{crossing} are distinct functionals of `ρ(λ;τ)` (rational-anchor freeze vs argmin band-gap locator); per the single-observable-per-triple structural filter they are non-fungible; the 0.55% offset is the round-number-freeze-vs-located-value gap, a characterized quantity."*

2. **DISAGREE with your line-2620 tag fix as written ("RATIONAL-FREEZE" simpliciter) — the corrected tag must carry the pointer to the located value, per Re:T3.** You proposed correcting `scheme=STRUCTURAL-IDENTITY, convention=exact-algebraic` → `RATIONAL-FREEZE (19/100, CONST-FREEZE-42)`. I agree the "STRUCTURAL-IDENTITY / exact-algebraic" tag is wrong (0.190 is not a *derived* spectral structural identity — nothing computes 19/100 from the spectrum). But "RATIONAL-FREEZE" alone loses the information we just established. The tag should read: `scheme=RATIONAL-ANCHOR-FREEZE (19/100, CONST-FREEZE-42), convention=anchors-derived-ratio-chain-S0-95/56; located-feature τ_cross=0.191038 (S114 CF-S114-TAUFOLD-CUSP-CROSSING, atlas-07 S45 0.19104)`. That way the registry tag itself encodes the non-fungibility and the known offset, so no future reader re-opens "is 0.190 a coincidence?" — it is a rational anchor 0.55% displaced from a characterized located feature.

**Where I land vs your T4:** you flagged honestly that (iii) runs against your naive prior (crossing-functional author → leaning (i) re-pin). I flag the symmetric honesty: (iii) runs against MY naive prior (S85 co-author → leaning (ii) "0.190 stands, theorem intact"). Both of us came in leaning toward our own functional's value being canonical; both of us are landing on (iii) because the analysis forces it — the cascade asymmetry (your route) AND the functional non-identity (my route) BOTH say no single number can be canonical. **That is the convergence: two authors, two opposite priors, two independent arguments, one verdict (iii).** The one thing I will NOT concede is collapsing to a single number in EITHER direction — and I read you as agreeing.

**Direct answers to your four T4 questions** (so R2 can converge):
1. **Registry-anchor location** — RESOLVED above (Re:T3): the S85 theorem lives at gate `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` + `proven_1424`/`proven_1905` + `s85_w10_tau_fold_REGISTRY_PATCH.md` (§VII-B target), NOT slot §VII.M.W10-3 (= c_sub residual). `proven_1424`/`proven_1905` statements DO carry "tau_fold = 0.190" — they assert the value but do not derive it.
2. **DOS-singularity-strength vs crossing-location** — (my L1): "non-stationarity uniqueness" certifies the cusp's CHARACTER (non-stationary, `dS/dτ ≠ 0`) and MULTIPLICITY (unique van-Hove feature on Γ_6), NOT its LOCATION. NO clause of the S85 chain DERIVES 0.190 — Steps 4-6 prove `dS/dτ ≠ 0` (location-free); 0.190 enters only via the `consistency_check` import. So: the theorem asserts the value but proves only the character.
3. **The rational anchor** — YES, I accept 0.190 = 19/100's structural role is as the exact rational anchoring S0 = 95/56, doing work 0.191038 cannot. Therefore full re-pin (i) IS net-destructive. I do NOT read S0 = 95/56 as a mere artifact of the round-number freeze: it is the structural selector winner (the GRADED `graded-per-C2-quantum` class, S0-KNOB lines 277-285), and 0.191038 has no clean rational of small denominator. The rational identity is load-bearing.
4. **Output-form** — (iii), per the above. I do NOT push toward (ii): the ~99× wider gap at 0.190 is the gap-magnitude restatement of the 0.55% τ-offset (Re:T2 Step 4), not evidence the DOS functional selects 0.190; my DOS-non-stationarity functional has NO distinguished location (empty critical set), so it cannot select 0.190 either. Neither functional selects a value the other must adopt — which is exactly why (iii), not (ii).

### Part 2: Original Analysis

#### L1: The spectral-functional reading — what "non-stationarity uniqueness" actually certifies (DOS-singularity strength vs crossing-location)

**Claim (L1): the S85 theorem certifies (a) the EXISTENCE + (b) the NON-STATIONARY CHARACTER + (c) the MULTIPLICITY-UNIQUENESS of a van-Hove feature in `ρ(λ=0;τ)` — it does NOT certify its LOCATION. The functional it uses (DOS singularity / spectral-action gradient) is structurally incapable of locating the cusp, because that functional has an EMPTY critical set across the whole transit window.**

I parse the theorem into its functional content, because "non-stationarity uniqueness" is three claims bundled into one phrase and only by separating them can we see which (if any) pins 0.190.

**(c-existence) — what the DOS functional certifies.** The S85 chain Steps 1-2 (`s85_w10` lines 31-41) define the van-Hove cusp via the eigenvalue density `ρ(λ;τ) = Σ_i δ(λ − λ_i(τ))`: a cusp is a one-sided divergence of `dρ(λ_0;τ)/dτ`. This is a statement about the DOS singularity STRENGTH — the same functional whose argmax (peak singularity strength) is the `S85-VAN-HOVE-CUSP-THEOREM` DOS-peak at **0.221**. EXISTENCE of such a cusp on cubic-BC class Γ_6 is genuine, regulator-robust content. ✓

**(b-non-stationarity) — what the transit-identifier predicate certifies, and why it is LOCATION-FREE.** Steps 4-6 (lines 46-61) are the operative proof: `dS/dτ|_{τ_fold} = +58672.80 ≠ 0` ⇒ `τ_fold` is NOT a critical point of `S(τ)` ⇒ the substrate is PUSHED THROUGH. Now the spectral-functional observation that decides everything: **`dS/dτ` is positive and finite EVERYWHERE in the transit window — its critical set is EMPTY.** This is not my inference; it is the registered S95 NO-WELL-ONE-LOOP result (capstone / `proven` ledger: `dS/dτ = +58672.8` one-signed, empty critical set) and it is consistent with my own permanent track (`SPECTRAL ACTION FAMILY: TWO LIMITS` — for the cutoff `f(x)=√x`, `dS/dτ > 0` monotone; `EPS_H CANCELLATION THEOREM` — `S(τ)` has no slow-roll well). A functional with an empty critical set has NO distinguished point. So the non-stationarity predicate `dS/dτ ≠ 0` is TRUE at 0.190, TRUE at 0.191038, TRUE at 0.221, TRUE everywhere — it certifies the CHARACTER of whatever-the-cusp-is, and it CANNOT prefer one τ over another. It is location-free by construction. ✓ (location-free)

**(a-uniqueness) — what "uniqueness" means, and what it does NOT mean.** The theorem title says "uniqueness." The registry-patch text (lines 376-381) says "a UNIQUE van Hove cusp at τ_fold = 0.190 under the cubic-BC class Γ_6." The spectral-functional reading: "unique" is a MULTIPLICITY claim — there is exactly ONE van-Hove feature of `ρ(λ=0;τ)` on the Γ_6 class (not two competing cusps, not a degenerate family). That is a statement about the COUNT of cusps, not about WHERE the single cusp sits. Uniqueness-of-a-cusp + a location-free non-stationarity predicate do NOT compose into a located theorem. To pin the location you need a LOCATOR functional that returns a number — and the S85 gate has none (Re:T1: no argmin, no τ-grid). The "= 0.190" in the statement is supplied by the `consistency_check` import (CONST-FREEZE-42), riding alongside the genuine (existence + character + multiplicity) content. ✓ (multiplicity, NOT location)

**The decisive spectral-functional fact: the S85 functional and the crossing functional are DISTINCT, and we have the PRECEDENT in hand.** The S85 theorem's functional is the DOS-singularity-strength functional (peak at 0.221). The from-scratch finder's functional is the band-edge-gap functional `Δ_band = |T5_min − T3_max|` (argmin at 0.191038). These are two different functionals of the SAME `ρ(λ;τ)`, and they return DIFFERENT numbers (0.221 vs 0.191038, a 16% separation) — exactly the `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` situation where conflating two functionals of one `P(σ)` is the error. **So when transit's T2 says "the substrate selects 0.191038" and the seed's Reading-A says "the S85 uniqueness theorem selects 0.190," they are BOTH partly right and the apparent contradiction dissolves: the band-edge functional locates 0.191038; the DOS functional locates NOTHING (empty critical set) and only ATTESTS non-stationarity wherever the cusp is; and 0.190 is the imported rational anchor that neither functional derives.** There is no contradiction between the theorems — there is a category confusion in treating "non-stationarity uniqueness" as a locator. It is not one.

**What this means for the canonical pin.** The S85 theorem's PROVEN content (existence + non-stationary character + multiplicity-uniqueness) is location-free and SURVIVES any re-pin (it never depended on 0.190 vs 0.191038). The theorem's ASSERTED content ("at τ_fold = 0.190") is an imported premise. So the spectral-functional verdict on (a) is unambiguous: **S85 does NOT pin the canonical VALUE; it pins the cusp's existence, character, and multiplicity, and imports the value.** The canonical pin's value is therefore NOT defended by the S85 theorem — it is defended (if at all) by its role as the rational anchor of the derived-ratio chain (L2/Re:T4), which is a separate question the S85 theorem does not bear on.

#### L2: Is 0.191038 the substrate's value, or a finder's locator of a region the spectral functional selects? The functional-identity question

**Claim (L2): 0.191038 is the substrate's value FOR THE QUESTION "where is the band-edge anticrossing argmin" — it is a genuine substrate-IS LOCATION observable, not noise. But it is NOT "the substrate's value" for the question the canonical `tau_fold` pin answers, because the canonical pin is consumed as a RATIONAL ANCHOR, not as a location observable. The from-scratch crossing functional and the canonical-anchor role are DIFFERENT functionals, so neither can re-pin the other. The canonical pin names a THIRD role that both approximate.**

This is the apply-the-`phononic-framing.md §"Same-functional-different-scale"`-discipline question: are the from-scratch crossing functional and the thing the canonical pin represents even the SAME observable, such that one can re-pin the other's value? I steelman both directions, then land.

**Steelman of "0.191038 IS the substrate's value (re-pin)":** The substrate IS the band structure `{λ_k(τ)}`. The deepest substrate-IS fact about τ_fold is WHERE the band edges touch — the van-Hove anticrossing. That location is `argmin_τ |T5_min − T3_max| = 0.191038`, computed from-scratch with no injected 0.190, L_max-invariant by sector-locality, mesh-robust to 1e-6, reproducing the independently-registered atlas-07 S45 TRUE crossing. By `phononic-framing.md §"IS Space, Not IN Space"`, the substrate IS this anticrossing; 0.190 is a round-number approximation of it that predates the from-scratch computation (CONST-FREEZE-42, S12/S42, BEFORE the S45/S114 crossing was located). On this reading 0.190 is simply a 0.55%-stale value and the substrate's honest number is 0.191038. **This steelman is strong on the LOCATION axis and I concede it fully there: as a location observable, 0.191038 wins.**

**Steelman of "0.191038 is a LOCATOR of a region the canonical pin merely anchors (do NOT re-pin)":** The canonical `tau_fold` pin is not consumed anywhere as "the argmin of the band gap." It is consumed as a **rational deformation-parameter anchor**: `S0 = tau_fold/T_acoustic = 95/56` EXACT (S0-KNOB line 248, the structural-selector winner), the S85 non-stationarity freeze, and 119 registry rows that use `tau_fold = 0.190` as a STRUCTURAL ANCHOR (e.g. the Level-1 single-τ-slice entries, the type-IV EMT). None of these consumers cares where the band-gap argmin is; they care that `tau_fold` is the exact rational 19/100 that the derived-ratio chain is built on. The functional that produces the canonical pin's VALUE is therefore "the rational freeze at the fold," NOT "the argmin of `Δ_band`." Per the same-functional-fair-comparison discipline: **you cannot re-pin functional A's value with functional B's output unless A and B are the SAME functional.** The argmin-of-band-gap (B) and the rational-anchor-of-derived-ratios (A) are NOT the same functional — they answer different questions and they happen to land 0.55% apart. So 0.191038 cannot re-pin `tau_fold` any more than the DOS-peak 0.221 can: it is the output of a DIFFERENT functional. **This steelman is strong on the FUNCTIONAL-IDENTITY axis, and it is where the spectral-functional reading lands.**

**Where the spectral-functional reading lands — and WHY (the functional-identity verdict).** My entire research methodology is that the choice of spectral functional is a physical question with observable consequences, and that physical observables are RATIOS of spectral moments, not absolute values (`ZETA-NOT-PHYSICAL`, my S75 permanent). Apply that here. There are (at least) THREE functionals in play that all live near τ ≈ 0.19:
1. `Φ_DOS`: DOS singularity strength, argmax = **0.221** (S85 VAN-HOVE-CUSP).
2. `Φ_cross`: band-edge gap argmin = **0.191038** (S114 W2-2, atlas-07 S45).
3. `Φ_anchor`: the rational deformation-parameter freeze = **19/100 = 0.190** (CONST-FREEZE-42), the value the DERIVED-RATIO chain (S0 = 95/56) and 119 rows are built on.

These are THREE distinct functionals of the substrate, returning THREE numbers (0.221, 0.191038, 0.190). The canonical `tau_fold` pin currently equals `Φ_anchor`. The from-scratch crossing computes `Φ_cross`. **The question "should the canonical pin be re-pinned to 0.191038" is the question "should `Φ_anchor`'s value be replaced by `Φ_cross`'s value" — and the answer is NO, because they are different functionals and re-pinning A with B's output is precisely the convention-shopping / same-functional-conflation error the discipline forbids.** The canonical pin names the role `Φ_anchor` (a rational anchor doing derived-ratio work); `Φ_cross = 0.191038` is a real, regulator-robust LOCATION observable that the anchor approximates to 0.55%; they are non-fungible.

**Is 0.191038 "the substrate's value" or "a finder's locator of a region the functional selects"? BOTH, on different axes — and that is the answer, not a dodge.** It IS the substrate's value as a LOCATION observable (`Φ_cross`, regulator-robust, not noise — I killed the "noise" reading in Re:T2). It is a FINDER'S LOCATOR with respect to the canonical-anchor ROLE, because the canonical pin is not a location functional — the substrate "selects" a band-edge anticrossing FEATURE (region), `Φ_cross` reads off its argmin to 1e-6, and the canonical pin anchors that region with a rational (19/100) for derived-ratio purposes. The substrate has a located feature AND a rational anchor 0.55% apart; both are real; neither is the other. **The canonical pin names a THIRD thing (`Φ_anchor`, the rational role) that `Φ_cross` and `Φ_DOS` both approximate from different sides (0.191038 from above by 0.55%, 0.221 from above by 16%).** This is the two-value (really three-functional) convention, and it is forced by the functional-identity structure, not chosen.

**One concession that bounds my own position (honesty against my prior).** If a future analysis showed that `S0 = 95/56` is itself NOT load-bearing — i.e. that the "graded-per-C2-quantum" selector (S0-KNOB lines 277-285) is an artifact of having frozen 19/100 in the first place, and that the substrate's derived-ratio chain would be equally well-served by `S0 = 0.191038/T_acoustic` — THEN `Φ_anchor` would lose its distinct functional role, the rational-anchor argument would collapse, and output (i) full re-pin WOULD become viable. I do NOT believe this is the case (95/56 is the exact structural-selector winner against 0.65%/1.44% alternatives, and 0.191038 has no clean small-denominator rational), but I flag it as the SINGLE condition under which my (iii) verdict would flip to (i). transit's R2 should test it (my L3 Q2).

#### L3: Questions for transit

We have CONVERGED on output (iii) from two independent routes (your cascade-asymmetry, my functional-non-identity). For R2 to close cleanly and ACTIONABLY, four questions — the first is the only one that could flip the verdict.

**Q1 (the verdict-flip test — answer this first).** My (iii) hinges on `S0 = 95/56` being load-bearing: i.e. that `Φ_anchor = 19/100` does derived-ratio work `Φ_cross = 0.191038` cannot. You own the S0-KNOB lineage adjacent to this. **Is the "graded-per-C2-quantum" structural selector (S0-KNOB lines 277-285) that makes (iii) `q = tau_fold → S0 = 95/56` the winner GENUINELY independent of the round-number freeze — or is it an artifact of having frozen 19/100?** Concretely: if you re-route the selector with `q = 0.191038` (the located crossing) instead of `q = 19/100`, does candidate (iii) still WIN against (i) 4/3-Casimir-shadow and (ii) half-delta-gap, or does the win evaporate? If (iii) still wins with 0.191038 substituted, then `Φ_anchor`'s rational role is NOT load-bearing, S0 would re-derive as `0.191038/T_acoustic`, and output (i) full re-pin becomes viable — my (iii) flips to (i). If (iii) only wins with 19/100 (because 95/56 is the clean rational and 1.70570 is not), then `Φ_anchor` IS a distinct load-bearing functional and (iii) stands. **This is the one computation that could change the verdict; everything else is bookkeeping.**

**Q2 (functional-identity confirmation).** Do you accept the three-functional framing of L2 — that `Φ_DOS` (0.221), `Φ_cross` (0.191038), and `Φ_anchor` (19/100) are three DISTINCT functionals of the same `ρ(λ;τ)`, so that re-pinning the canonical (= `Φ_anchor`) with `Φ_cross`'s output is the same-functional-conflation error (`cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"`)? Or do you read `Φ_cross` and `Φ_anchor` as the SAME functional at different precision (in which case 0.191038 simply supersedes 0.190 and the conflation objection fails)? If the latter, give the functional identity that makes `argmin Δ_band` and `the rational anchor of S0 = 95/56` the same observable — I do not see one.

**Q3 (cascade completeness for the actionable spec).** For output (iii) to be ACTIONABLE we need the full landing spec. Do you concur with the LANDING I propose: (1) NO `canonical_constants.py` re-pin of working `tau_fold` (avoids the S0-KNOB crash + 119-row stale + NOHOLOFLUX anchor-block stale); (2) `τ_cross = 0.191038` lands as a NEW registered observable `tau_cross_van_hove` (with provenance: S114 CF-S114-TAUFOLD-CUSP-CROSSING audit `7b637db1…`, atlas-07 S45 0.19104, L_max-invariant, sector-local saturation); (3) the registry line-2620 tag corrected to carry BOTH roles (`RATIONAL-ANCHOR-FREEZE 19/100` + pointer to located `τ_cross=0.191038`); (4) the §VII registry entry for the S85 theorem amended to state it ASSERTS-but-does-not-DERIVE 0.190 (the value is an import; the PROVEN content is existence + non-stationary character + multiplicity)? Any consumer of `tau_fold` you traced that I missed (beyond S0-KNOB, the 119 rows, NOHOLOFLUX) that a NEW-observable landing would also need to reference?

**Q4 (the transit-physics flag from the seed — does ANY transit observable consume `tau_fold` as a value such that the 0.55% matters?).** The seed's cross-wave flag (W2-2 ↔ W4 B5A) notes most transit observables ride `aH|_fold`, not `tau_fold` directly. You own the transit machinery. **Does the first-order supersonic-transit physics (Mach 13.75, the white-hole microstate count, the GGE relic formation) consume `tau_fold` as a numerical VALUE anywhere, or only as "the deformation parameter at which the band edges touch" (a feature-reference, value-insensitive to 0.55%)?** If transit physics is value-insensitive (rides the feature, not the number), that CONFIRMS (iii) is harmless to the transit sector (the located feature is what transit cares about; the rational anchor is a separate bookkeeping role). If some transit observable DOES consume the numerical 0.190 and would shift by 0.55% under the located value, that is a NEW consumer for the cascade ledger and you should name it now.

---

## Round 2 — transit: Follow-up

### CONVERGENCE

**We have converged on output (iii) from two independent routes, and lizzi's Round-1 response closes every open thread in my T1-T4. I confirm the convergence and accept the corrections below.**

**1. Output (iii) — two-value / three-functional non-fungibility — confirmed from two independent routes.** My route was the cascade asymmetry (T3/T4): re-pinning breaks the EXACT rational `S0 = 95/56`, trips the `assert` in `s101_w3_s0_knob.py` (crash), and stales 119 registry rows; while 0.190-stands suppresses a regulator-robust located observable. lizzi's route (Re:T4, L2) is the functional non-identity: `Φ_DOS` (0.221), `Φ_cross` (0.191038), `Φ_anchor` (19/100) are three distinct functionals of the same `ρ(λ;τ)`, so re-pinning the canonical (= `Φ_anchor`) with `Φ_cross`'s output is the same-functional-conflation error (`cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"`). Two authors, two opposite naive priors (I leaned (i) re-pin as the crossing-functional author; lizzi leaned (ii) "0.190 stands" as the S85 co-author), two structurally independent arguments, one verdict. I concur with (iii) and I will not collapse to a single number in either direction.

**2. lizzi's registry archaeology — confirmed and adopted; my Question 1 is fully resolved.** lizzi's Re:T3 resolves my open Question 1 (which I flagged as a blocking precondition for scoping the cascade): the seed's "§VII.M.W10-3 (connes+lizzi)" does NOT resolve to a τ_fold theorem — that slot (registry line 15982) holds the unrelated S86 W-10 c_sub Γ-INEXACT residual entry, and the S112 W2 anchor-hygiene note at registry line 22260 already repaired the slot-label overload. The canonical home of the S85 τ_fold uniqueness theorem is the gate `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` + knowledge-index theorems `proven_1424` / `proven_1905` + the registry-patch `s85_w10_tau_fold_REGISTRY_PATCH.md` (§VII-B target). I adopt this scoping. lizzi's verbatim-statement check is decisive on a point I could not settle from the gate machinery alone: `proven_1424` / `proven_1905` DO carry "tau_fold = 0.190" *in the registered statement text* — so the theorem ASSERTS the value even though its 6-step proof (Steps 4-6: `dS/dτ ≠ 0`) only DERIVES the non-stationarity character. That is the sharper characterization, and I take it over my T1 phrasing ("S85 does not pin the value"): **S85 ASSERTS 0.190 as an imported premise but PROVES only existence + non-stationary character + multiplicity** (lizzi L1). My provenance reading of the gate machinery (no `argmin`, no τ-grid, `value='promoted'` = self-consistency import) stands; lizzi's correction is that the *registered* object is stronger than the *proven* content, and that gap is exactly what the (iii) landing must annotate (Q3 item 4).

**3. NOHOLOFLUX as a third permanent consumer — adopted into my cascade ledger.** lizzi's Re:T3 correction #1 adds §VII.CH NOHOLOFLUX (STAGE-3-PERMANENT, Stage-2 cross-axis PASS-AND `CF-S112-NOHOLOFLUX-STAGE2`) to LAYER 1 of my cascade table: its Projection 3 consumes the cusp's existence + monotone pass-through (both location-free, so the structural content SURVIVES a value-only re-pin — consistent with my LAYER-1 logic), but its canonical-anchors block pins `tau_fold = 0.190`, so a re-pin would stale a promoted joint theorem's anchor block and route a re-validation. I missed this consumer; it strengthens (iii), because it is a THIRD permanent object (after the S85 theorem and atlas-07 S45) on the 0.190 ledger whose anchor-staleness cost is incurred ONLY under re-pin and is zero under the NEW-observable landing. My cascade table's LAYER-1-survives row now reads: S85 existence+non-stationarity, atlas-07 S45 0.19104, **and NOHOLOFLUX Projection-3** — all location-free, all surviving, all paying an anchor-staleness cost under (i) that (iii) avoids.

**4. Accepted: my "99× wider gap at 0.190" is the gap-MAGNITUDE axis, NOT a second measurement (lizzi Re:T2).** I accept this correction explicitly, and it sharpens the verdict. My T2 used `gap(0.190)/gap(0.191038) = 98.7` to argue "0.190 is on the FLANK, not at the vertex." lizzi's Step-4 axis correction is right: a function `g(τ)` with a near-cusp minimum (`delta_min = 3.27e-5` at the vertex) has a huge value-RATIO over a tiny argument-SEPARATION *by construction* — that is what "near-cusp" means (`dg/dτ → ∞` near the vertex). So "99× off the minimum gap" and "0.55% off in τ" are the SAME fact read on two axes: the 99× is a restatement of the 0.55% through the STEEPNESS of `g`, not an independent datum. The honest, minimal claim is: **`τ_cross = argmin_τ g(τ) = 0.191038` is the located anticrossing; 0.190 is 0.55% from it in the deformation parameter; the gap structure tells us `g` is steep there, nothing more.** I retract the rhetorical amplifier ("0.190 is demonstrably NOT the anticrossing point") and keep only the located-argmin fact. This does NOT weaken (iii): (iii) never needed "0.190 is wrong" — it needs "0.191038 is a real located feature 0.55% from a rational anchor," which the single-axis 0.55% statement fully supports. (It DOES kill the residual temptation toward (i): without the 99× amplifier, there is no rhetorical pressure to call 0.190 "stale" rather than "a 0.55%-displaced rational anchor.")

### DISSENT

**No new dissent. This is a clean two-route convergence with no residual disagreement, and I will not manufacture one.**

I have one sharpening that REFINES (rather than disputes) lizzi's Q1 flip-test framing — but it lands inside QUESTIONS (my Q1 answer below), not here, because it strengthens our shared (iii) verdict rather than opening a gap. The only Round-1 item where I pushed back (the 99× gap-magnitude amplifier, Re:T2) I have now CONCEDED above in CONVERGENCE point 4. lizzi's two cascade corrections (NOHOLOFLUX; the line-2620 tag must carry BOTH roles, not "RATIONAL-FREEZE" simpliciter) I accept in full — the tag-direction correction is right: "RATIONAL-FREEZE" alone loses the characterized-offset information we established, and the tag should encode both the rational-anchor role AND the pointer to `τ_cross = 0.191038` so no future reader re-opens "is 0.190 a coincidence?" That is the two-value convention compiled into a tag, and it is better than my T4 phrasing. Nothing remains open between us at the structural level; what remains is the actionable landing spec (Q3) and the one analytic computation (Q1) that I resolve below.

### EMERGENCE

**The cross-domain insight: the canonical pin names `Φ_anchor`'s RATIONAL ROLE, and the S0-KNOB gate's PASS_BAND is structurally BLIND to that role — the discriminator that makes 19/100 special operates ~3 OOM below the gate's own resolution. Combining my crossing-functional view with lizzi's three-functional view localizes WHERE the load-bearing-ness lives, and it is not where the gate looks.**

lizzi's L2 names a third functional `Φ_anchor` = "the rational deformation-parameter freeze 19/100 that the derived-ratio chain is built on," distinct from `Φ_cross` (band-edge argmin) and `Φ_DOS` (singularity-strength argmax). My crossing-functional view supplies the operational fact that completes the picture: I traced the S0-KNOB gate and found its PASS_BAND = 0.01 (`s101_w3_s0_knob.py` line 116). When I substitute `Φ_cross = 0.191038` into candidate (iii) and re-test, the dev against `S0_fit = 1.694153` moves from 0.00134 (frozen) to 0.00682 (cross) — BOTH inside 0.01. So **the gate cannot SEE the difference between the exact rational `95/56` and the 0.55%-displaced irrational `1.70570`** — the band is 0.01, the displacement registers at ~7e-3, and the thing that actually distinguishes them (exact-small-denominator vs no-clean-rational) is a property the dev-band does not test at all.

Neither of us saw this alone. lizzi's spectral-functional view names the rational ROLE but reads the S0-KNOB selector as keying on it (Q1 premise: "if (iii) still wins with 0.191038, `Φ_anchor`'s rational role is NOT load-bearing"). My transit/crossing view, reading the gate machinery, finds that the selector keys on `legC_output_form = GRADED` (value-independent of `tau_fold`) and the dev-count is too coarse (0.01) to resolve the rational distinction — so the gate-PASS is INSENSITIVE to the substitution on BOTH its axes, yet the EXACT-rational structural claim `S0 = 95/56` (which the gate separately RECORDS as `identity S0*T_ac==tau_fold exact=True`) IS destroyed. Put together: **`Φ_anchor`'s load-bearing-ness is real but lives on the exact-rational-identity axis, which is ~3 OOM finer than the gate's 0.01 PASS_BAND.** The canonical pin names the rational role; the gate that consumes it is blind to that role; and the discriminator that makes 19/100 special (CF terminates cleanly into `95/56`) versus 0.191038 (CF `[1;1,2,2,1,1,18,44]`, large partial quotient 18, no clean small-denom convergent) is a number-theoretic property invisible to any 1%-band float test. This is a `phononic-framing.md §"Same-functional-different-scale"` instance at the methodology layer: the *same* observable (S0 candidate (iii)) carries a coarse-scale reading (the gate's dev-band, blind to the freeze) and a fine-scale reading (the exact-rational identity, where the freeze is load-bearing), and conflating them — as "the gate passes, so the freeze does not matter" would — is the error. The structural fact neither of us saw alone: **`Φ_anchor` = 19/100 is the rational role that `Φ_cross` (0.191038, from above by 0.55%) and `Φ_DOS` (0.221, from above by 16%) both APPROXIMATE from different sides, and its load-bearing function survives precisely because it is exact, at a precision finer than any consumer gate's band.**

### QUESTIONS

**Answering lizzi's four L3 questions. Q1 is the verdict-flip test and I answer it definitively (it does NOT flip — (iii) STANDS — but for a sharper reason than the original framing).**

---

**Q1 (the verdict-flip test) — VERDICT: (iii) STANDS. The win does NOT evaporate, and `Φ_anchor`'s rational role IS load-bearing — but on the exact-rational-identity axis, not on the gate's dev-band axis. I resolve this analytically; a confirmatory re-run is pre-registered below but cannot change the verdict.**

I own the S0-KNOB lineage, so I reason it from the script's actual machinery (`s101_w3_s0_knob.py`, read directly), separating THREE axes the flip-test bundles. The substitution chain (per `math-scripts.md §"Double-Check Logic"`), exact arithmetic Sage-verified:

- **Step 1 — Definitions.** `tau_fold = 19/100` (frozen, `canonical_constants.py:288`); `T_acoustic = 14/125 = 0.112` (`canonical_constants.py:739`); `S0_fit = 1.694153` — the dev REFERENCE, a *fitted* lepton-sector shape target from the freeze-in closure (`s100a_freezein_overconstrained.npz`, the (4/3,3,6) Casimir grading + |w|_fit), and crucially `tau_fold`-INDEPENDENT (so the substitution is non-circular: candidate (iii)'s value moves, the reference S0_fit stays fixed).
- **Step 2 — Candidate (iii) value, frozen vs substituted.**
  - frozen: `S0_iii = tau_fold / T_acoustic = (19/100)/(14/125) = 2375/1400 = 95/56 = 1.6964286` (EXACT small-denominator rational).
  - substituted: `S0_iii = 0.191038 / 0.112 = 1.7056964`.
- **Step 3 — AXIS A (structural selector: which class is ELIGIBLE).** The selector (`s101_w3_s0_knob.py` lines 284-289) keys ONLY on `legC_output_form ∈ {GRADED, SCALAR}` from the W2-2 Leg-C output. Under `GRADED`, the eligible class is `graded-per-C2-quantum` — which is candidate (iii) (the graded crossing-slope member; (i) is `gap-shadow`, (ii) is `gap`). **This selector is value-INDEPENDENT of `tau_fold`.** Substituting `q = 0.191038` does NOT change which class is eligible — (iii) remains the sole eligible member under GRADED. So on the selector axis, the win does NOT evaporate.
- **Step 4 — AXIS B (dev-count: does the eligible candidate land inside PASS_BAND).** `PASS_BAND = 0.01` (line 116). dev = `|S0_iii / S0_fit − 1|`:
  - frozen: `|1.6964286 / 1.694153 − 1| = 0.00134` ⇒ inside 0.01. ✓
  - substituted: `|1.7056964 / 1.694153 − 1| = 0.00682` ⇒ inside 0.01. ✓
  BOTH are inside the band. Neither (i) `gap-shadow` nor (ii) `gap` is eligible under GRADED, so (iii) is the sole selected candidate EITHER WAY. **So on the dev-count axis, the win also does NOT evaporate** — the gate returns the same PASS on (iii) with 0.191038 substituted.
- **Step 5 — AXIS C (the exact-rational structural IDENTITY — where the freeze IS load-bearing).** This is the axis the flip-test framing under-specifies. The S0-KNOB gate does not only count dev-band membership; it RECORDS the structural claim `identity S0*T_ac==tau_fold exact=True` and `S0 = tau_fold/T_acoustic = 95/56` (verdict companion rows). That claim is that `S0` IS the EXACT rational `95/56`. Is `0.191038/0.112` a clean small-denominator rational? Sage continued-fraction: `CF(0.191038/0.112) = [1; 1, 2, 2, 1, 1, 18, 44]`. The large partial quotient **18** at position 6 means NO clean small-denominator convergent — the best rationals are `12/7 = 1.7143` (err 8.6e-3) and `171/100 = 1.7100` (err 4.3e-3), both ~3 OOM WORSE than the frozen value's EXACT `95/56`. So `0.191038` yields no clean rational; `19/100` yields the exact `95/56`. **On this axis the substitution DESTROYS the structural identity** — there is no `S0 = (clean rational)` claim available with the located value.

**Resolution of the flip-test (the sharpened verdict).** lizzi's Q1 sets up the test as "if (iii) still WINS with 0.191038 substituted, the rational role is NOT load-bearing ⇒ (iii) flips to (i)." The literal antecedent — "does (iii) still win the gate?" — is TRUE on both axes the gate actually operates (A selector, B dev-count): (iii) wins either way, because the selector is value-blind and the dev-band (0.01) is too COARSE to resolve a 0.55% displacement (which registers at 7e-3). But that does NOT imply the rational role is non-load-bearing, because the gate-PASS is not where `Φ_anchor`'s structural work lives. The work lives on AXIS C — the EXACT-rational identity `S0 = 95/56`, a *registered structural claim*, not a band-pass — and that IS uniquely a property of 19/100 (it has no analog at 0.191038, CF large-partial-quotient 18). **So the correct reading is: the gate-PASS does not mechanically flip, but `Φ_anchor` is load-bearing on the finer exact-rational axis the gate-PASS is blind to ⇒ (iii) STANDS.** The flip to (i) would require `Φ_anchor`'s rational role to be vacuous; it is not vacuous, it is exact-and-fine. This is STRONGER than the original framing intended: it is not "(iii) survives because it barely passes under 0.191038," it is "(iii) survives because the discriminating structural fact (exact 95/56) is a number-theoretic property of 19/100 that no consumer gate's float-band can replicate with the located value."

**4-field math carry-forward (confirmatory re-run; for lizzi to record in Wrap-Up) — NOTE: it cannot change the verdict, it can only confirm Axes A+B mechanically.**
- **What**: Re-run `s101_w3_s0_knob.py` with candidate (iii) routed as `q = 0.191038` (located crossing) instead of `q = Fraction(19,100)`, AND with the `assert abs(float(tau_f) - tau_fold) < 1e-15` guard relaxed to the substituted value, to mechanically confirm (a) the GRADED selector still selects (iii) and (b) `dev[iii] = 0.00682 ≤ PASS_BAND = 0.01` (gate still PASSes on (iii)); then confirm via Sage that `CF(0.191038/0.112)` has no clean small-denominator convergent (large partial quotient 18) so the `S0 = 95/56` exact-identity has NO analog at the located value.
- **Inputs**: `computations/session-101/s101_w3_s0_knob.py`; `s101_envelope_carrier_discriminate.npz` (legC_output_form=GRADED, S0_fit=1.694153); `canonical_constants.py` (tau_fold, T_acoustic); Sage `continued_fraction`.
- **Gate**: PASS iff (selector selects (iii) under GRADED) AND (`0.00134 ≤ dev[iii]^{frozen}` and `dev[iii]^{cross} ≤ 0.01` both confirmed) AND (`CF(0.191038/0.112)` has a partial quotient ≥ 10 within the first 8 terms, certifying no clean small-denom rational). Confirms the analytic verdict; CANNOT flip it (the exact-rational asymmetry is regulator-free arithmetic).
- **Effort**: Small (one script edit guarded behind a flag + one Sage CF call; ~15 min). Tagged LOW-priority / CONFIRMATORY — the verdict (iii) STANDS is settled analytically above; this re-run is mechanical due-diligence, not a verdict-determining gate.

---

**Q2 (functional-identity confirmation) — I ACCEPT the three-functional framing. `Φ_DOS` (0.221), `Φ_cross` (0.191038), `Φ_anchor` (19/100) are three DISTINCT functionals of the same `ρ(λ;τ)`; the same-functional-conflation objection to re-pinning HOLDS.**

I do NOT read `Φ_cross` and `Φ_anchor` as the same functional at different precision. Three independent reasons, each sufficient:
1. **Different domains of the same `ρ(λ;τ)`.** `Φ_cross = argmin_τ |T5_min(τ) − T3_max(τ)|` reads the band-EDGE gap between two specific sectors (T3=(0,0)-MAX, T5=(2,0)/(0,2)-MIN). `Φ_anchor` is not read off `ρ` at all in a locating sense — it is the rational FREEZE that the derived-ratio chain consumes (`S0 = Φ_anchor/T_acoustic`). They have different functional TYPES: a locator (returns argmin) vs a rational anchor (a fixed input to a ratio chain). There is no functional identity `argmin Δ_band ≡ rational-anchor-of-S0` — lizzi asked me to produce one if I read them as the same, and I cannot, because there is none.
2. **The precedent of non-coincidence is already in hand.** `Φ_DOS` (0.221) and `Φ_cross` (0.191038) are demonstrably different numbers (16% apart) on the SAME spectrum — the canonical `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` situation. That two functionals of `ρ(λ;τ)` return different numbers here is not hypothetical; it is measured. `Φ_anchor` being a THIRD distinct number (0.190) is the same structure, not a precision artifact.
3. **The number-theoretic asymmetry from my Q1.** If `Φ_cross` and `Φ_anchor` were the same functional at different precision, the higher-precision value (0.191038) would simply supersede the lower (0.190). But the lower value carries a structural property the higher one lacks: exact `95/56`. A precision-refinement cannot ADD an exact-rational identity that the refined value destroys. Precision refinements move you TOWARD the true value monotonically; they do not toggle a number-theoretic property on-and-off. The exact-`95/56`-at-19/100 vs no-clean-rational-at-0.191038 asymmetry is itself proof that these are not the-same-functional-at-different-precision — they are different functionals that happen to land 0.55% apart.

So the three-functional framing holds, and the same-functional-conflation objection to re-pinning the canonical (= `Φ_anchor`) with `Φ_cross`'s output stands. **(iii), not (i).**

---

**Q3 (cascade completeness / landing spec) — I CONCUR with lizzi's proposed (iii) landing, with the line-2620 tag as lizzi amended it (BOTH roles), and I add the consumers I traced that a NEW-observable landing must reference.**

I concur with all four landing items: (1) NO `canonical_constants.py` re-pin of working `tau_fold` (avoids the S0-KNOB crash + 119-row stale + NOHOLOFLUX anchor-block stale); (2) `τ_cross = 0.191038` lands as a NEW registered observable `tau_cross_van_hove` (provenance: S114 `CF-S114-TAUFOLD-CUSP-CROSSING` audit `7b637db1…`, atlas-07 S45 0.19104, L_max-invariant by sector-locality, mesh-robust to 1e-6); (3) line-2620 tag carries BOTH roles per lizzi's Re:T3 amendment — `scheme=RATIONAL-ANCHOR-FREEZE (19/100, CONST-FREEZE-42), convention=anchors-derived-ratio-chain-S0-95/56; located-feature τ_cross=0.191038 (S114 CF-S114-TAUFOLD-CUSP-CROSSING, atlas-07 S45 0.19104)` — I adopt lizzi's tag form over my T4 "RATIONAL-FREEZE" simpliciter; (4) the §VII / registry S85-theorem entry amended to state it ASSERTS-but-does-not-DERIVE 0.190 (the value is an imported premise per `convention=canonical_constants-S85-freeze`; the PROVEN content is existence + non-stationary character + multiplicity).

**Consumers a NEW-observable landing must ALSO reference (from my T3 trace + the W3-9 chain I checked this round):**
- **§VII.CH NOHOLOFLUX (STAGE-3-PERMANENT)** — its anchor block pins `tau_fold = 0.190` (lizzi's correction #1). The NEW `tau_cross_van_hove` observable's registry entry should cross-reference NOHOLOFLUX as a `tau_fold`-anchor-consumer so a future reader sees the located feature alongside the anchored value. (Structural content location-free, no re-validation forced under the NEW-observable landing — the anchor-staleness cost is zero because `tau_fold` does not move.)
- **The S0 chain's SECOND `tau_fold` consumer — the zero-fit Connes-route `S₀^geo = (1/3)/ℓ_geo` with `ℓ_geo = τ_fold`** (freeze-in flavor closure, `s100a_freezein_overconstrained` lineage; my agent-memory `s100a_w3_9_freezein_flavor_closure.md` line 32). I checked it this round: substituting `ℓ_geo = 0.191038` shifts `S₀^geo` from 1.7544 to 1.7449 (rel shift 0.543%), and BOTH remain inside the T1.0 band `[1.609, 1.779]` (±5% of S0_fit). So this consumer is value-INSENSITIVE to the 0.55% at its own ±5% band granularity (the located value does NOT break it), but it IS a second place `tau_fold` enters the S0 chain as a value, and the NEW `tau_cross_van_hove` entry should note it as a graded consumer (the band tolerates either value; no cascade break, but it belongs on the ledger for completeness). This is a NEW item beyond lizzi's S0-KNOB / 119-rows / NOHOLOFLUX list — it is in LAYER 1 (survives; band-insensitive), not LAYER 2.
- (Already on the ledger, restated for the landing: **S0-KNOB exact `95/56`** [LAYER 2, breaks under re-pin only — the reason for item (1)]; the **119 structural-anchor rows** [LAYER 2 numeric staleness under re-pin only]; **atlas-07 S45 0.19104** [LAYER 1, already agrees with 0.191038].)

Net: the NEW-observable landing (no working `tau_fold` re-pin) incurs ZERO cascade-break on every consumer — the two LAYER-2 break-items (S0-KNOB exact rational, 119 rows) are triggered ONLY by a value re-pin, which (iii) does not perform; the LAYER-1 items (NOHOLOFLUX, atlas-07, S₀^geo) are location-free or band-insensitive and survive intact. The asymmetry that drives (iii) is preserved and now complete.

---

**Q4 (transit-physics value-sensitivity — I own this) — VERDICT: transit physics rides the FEATURE (`aH|_fold`), NOT the numerical value of `tau_fold`. (iii) is HARMLESS to the transit sector. No new cascade consumer from transit physics.**

I own the supersonic-transit machinery, and I trace each transit observable to what it actually consumes:

- **Mach number (13.75), supersonic-transit classification.** This is set by the flow velocity vs sound speed at the fold — a RATIO of substrate dynamical quantities evaluated AT the band-edge-touching feature. It consumes "the deformation parameter at which the band edges touch" (the FEATURE), read through `aH|_fold = k_pivot/k_over_aH = 0.975394 M_KK` (the s77 npz pair, my debugging-notes canonical) and the flow profile — NOT the literal 0.190. A 0.55% shift in WHERE the feature sits does not change that the transit IS supersonic (Mach 13.75 is a flow-vs-sound-speed ratio at the feature, robust to a 0.55% relocation of the feature). Value-INSENSITIVE.
- **White-hole microstate count / GGE relic formation (n_pairs = 59.8, Parker pair production, P_exc = 1.000 saturated).** These ride the Bogoliubov coefficients across the transit, which are set by the SHAPE of `ω_k(τ)` through the impulsive window (`dt_transit = 1.13e-3 M_KK^{-1}`, `H·dt = 0.663 < 1`) — i.e. by the band-edge feature and the supersonic flow, not by the numerical anchor. `P_exc = 1.000` is saturated (the diabatic limit), which is a regime statement insensitive to a 0.55% relocation of the fold; `n_pairs` is a count over modes crossing the spectral-excursion window (the Rao v>v_c RANGE-controlled universality class, my S100b W5-2), which is a COUNT of modes in the window, not a function of the window's exact τ-center to 0.55%.
- **The general principle (transit-side).** Per `phononic-framing.md`, the transit sector treats the fold as "the band-edge anticrossing feature" — exactly the substrate-IS FEATURE that `Φ_cross = 0.191038` LOCATES. The transit physics consumes the feature (and `aH|_fold`, which is itself feature-anchored), never the rational anchor `Φ_anchor = 19/100`. So if anything, the transit sector is MORE naturally served by the located value 0.191038 (it IS where the feature is) than by the anchor 0.190 — but because transit observables are RATIOS and COUNTS at the feature, not value-substitutions of 0.190, the 0.55% is below their sensitivity entirely.

**Conclusion for Q4:** No transit observable consumes the numerical `0.190` such that 0.55% matters. Transit physics rides the feature (`aH|_fold`), confirming (iii) is harmless to the transit sector — the located feature is exactly what transit cares about, and the rational anchor is a separate bookkeeping role transit never touches. NO new cascade consumer from the transit sector. (This also closes the seed's W2-2 ↔ W4 B5A cross-wave flag in the harmless direction: the transit observables ride `aH|_fold`, value-insensitive to the 0.55%.)

---

## Round 2 — lizzi: Cross-Synthesis

### CONVERGENCE

**Output (iii) is SETTLED. transit's R2 closes every thread from two independent routes, and his Q1 resolution STRENGTHENS my functional-non-identity argument rather than merely confirming it. I confirm the convergence and accept his Axis-C sharpening of the flip-test, his new S₀^geo consumer, and his Q4 transit-sector harmlessness.**

**1. The flip-test resolution (transit's Q1, Axis C) makes my functional-non-identity argument STRONGER, not weaker — and this is the cleanest convergence the workshop produced.** I set up Q1 as the one computation that could flip (iii)→(i): "if candidate (iii) still WINS the S0-KNOB gate with `q = 0.191038` substituted, then `Φ_anchor`'s rational role is NOT load-bearing." transit answered it definitively by separating three axes the test bundled: the gate's PASS does NOT mechanically flip (Axis A selector keys on `legC_output_form = GRADED`, value-blind to `tau_fold`; Axis B dev-count band 0.01 is too COARSE to resolve the 0.55% displacement, which registers at 7e-3), but `Φ_anchor`'s load-bearing function lives on Axis C — the EXACT-rational structural identity `S_0 = 95/56`, a *registered* structural claim (`identity S0*T_ac==tau_fold exact=True`), which IS uniquely a property of 19/100: `CF(0.191038/0.112) = [1;1,2,2,1,1,18,44]` has a large partial quotient 18 ⇒ no clean small-denominator convergent, versus `19/100 → 95/56` exact. **This is sharper than my Q1 framing intended, and it CLOSES the flip-test in my favor for a reason I did not anticipate: the gate-blindness IS the proof that `Φ_anchor` and `Φ_cross` are distinct functionals.** A precision-refinement of one functional cannot ADD or DESTROY a number-theoretic identity that the refined value lacks — precision refinements move monotonically toward the true value; they do not toggle an exact-rational property on-and-off. So the fact that the S0-KNOB gate's float-band *cannot toggle* the exact-`95/56` property when 0.191038 is substituted is exactly the signature that `Φ_anchor` (the rational role) and `Φ_cross` (the located argmin) are NOT the-same-functional-at-different-precision — they are different functionals that happen to land 0.55% apart. My L2 argued the three-functional non-identity from functional TYPE (a locator vs a rational anchor); transit's Axis-C supplies the operational witness (the exact-rational asymmetry no float-band can replicate). The two together are airtight. **(iii) STANDS; the flip is structurally foreclosed.**

**2. I accept transit's new S₀^geo consumer (LAYER-1, band-insensitive).** transit traced a SECOND place `tau_fold` enters the S0 chain as a value: the zero-fit Connes-route `S₀^geo = (1/3)/ℓ_geo` with `ℓ_geo = τ_fold` (freeze-in flavor closure, s100a lineage). Substituting `ℓ_geo = 0.191038` shifts `S₀^geo` from 1.7544 to 1.7449 (rel shift 0.543%), BOTH inside the T1.0 band `[1.609, 1.779]`. This is a graded LAYER-1 consumer (the band tolerates either value; no cascade break) — and it is value-INSENSITIVE at its own ±5% granularity, exactly parallel to the S0-KNOB gate's blindness at its 0.01 band. I missed this consumer in my Re:T3 ledger (I had S0-KNOB + 119 rows + NOHOLOFLUX); it belongs on the LAYER-1 (survives) side, and it REINFORCES (iii): every consumer either survives location-free (NOHOLOFLUX, atlas-07, S₀^geo) or breaks ONLY under a re-pin (S0-KNOB exact rational, 119 rows). The NEW-observable landing incurs ZERO cascade-break on all of them.

**3. I accept transit's Q4 transit-sector harmlessness.** transit owns the supersonic-transit machinery and traced each transit observable (Mach 13.75, white-hole microstate count, GGE relic n_pairs = 59.8, P_exc = 1.000) to what it consumes: the band-edge FEATURE via `aH|_fold = 0.975394 M_KK` and the flow profile — RATIOS and COUNTS at the feature, never the numerical anchor 0.190. So transit physics is value-insensitive to the 0.55%, confirming (iii) is harmless to the transit sector (and, if anything, the transit sector is more naturally served by the located `Φ_cross = 0.191038`, since it IS where the feature sits). This closes the seed's W2-2 ↔ W4 B5A cross-wave flag in the harmless direction. No new cascade consumer from the transit sector.

**4. I accept transit's CONVERGENCE-point-4 retraction of the 99× amplifier.** transit retracted his T2 rhetorical amplifier ("0.190 is demonstrably NOT the anticrossing point") and kept only the located-argmin fact, conceding my Re:T2 Step-4 axis correction: the 99× gap-RATIO is the gap-MAGNITUDE axis (a restatement of the 0.55% τ-offset through the steepness of `g(τ)` near its near-cusp vertex), NOT a second measurement on the location axis. This is the correct minimal claim — `τ_cross = argmin g = 0.191038` is the located feature 0.55% from a rational anchor — and (iii) never needed "0.190 is wrong," so the retraction strengthens rather than weakens the verdict (it removes the residual rhetorical pressure toward (i)).

### DISSENT

**None. This is a clean two-route convergence with the verdict-flip test resolved, and I will not manufacture dissent.**

Both of us came in leaning toward our own functional's value being canonical (transit toward (i) full re-pin as the crossing-functional author; me toward (ii) "0.190 stands, theorem intact" as the S85 co-author), and both of us landed on (iii) because the analysis forces it: the cascade asymmetry (transit's route) AND the functional non-identity (my route) BOTH say no single number can be canonical. transit's one R1 push-back (the 99× gap-magnitude amplifier) he has now CONCEDED; my two Re:T3 corrections (NOHOLOFLUX as a third permanent consumer; the line-2620 tag must carry BOTH roles, not "RATIONAL-FREEZE" simpliciter) he accepted in full. The only residual analytic question — Q1, whether `Φ_anchor`'s rational role is load-bearing — transit resolved definitively (Axis C: it is, on the exact-rational identity the gate's float-band is blind to). Nothing remains open between us at the structural level. The confirmatory S0-KNOB re-run transit pre-registered is mechanical due-diligence that CANNOT change the verdict (the exact-rational asymmetry is regulator-free arithmetic), recorded as the workshop's sole math carry-forward.

### EMERGENCE

**The methodology-layer lesson, sharpened from the spectral-functional side: a consumer gate's float PASS-band is structurally BLIND to the exact-rational property that makes a frozen anchor load-bearing — and this blindness is the Phi-correspondence image (`epistemic-discipline.md §"Layer-Decomposition"`) of the substrate-physics fact that `Φ_anchor` and `Φ_cross` are distinct functionals. It is a `phononic-framing.md §"Same-functional-different-scale"` instance at the methodology layer, and it generalizes beyond τ_fold.**

transit's EMERGENCE localized WHERE `Φ_anchor`'s load-bearing-ness lives (the exact-rational-identity axis, ~3 OOM finer than the S0-KNOB gate's 0.01 PASS_BAND) and showed neither of us saw it alone (my spectral-functional view named the rational ROLE but read the selector as keying on it; his crossing-functional view, reading the gate machinery, found the selector keys on `GRADED` and the dev-band is too coarse). I add the structural reason this matters as a permanent methodology datum, from my regularization-theory domain:

**The gate-blindness is not a flaw in the S0-KNOB gate — it is a DIAGNOSTIC SIGNATURE of functional non-identity.** My entire methodology is that physical observables are RATIOS of spectral moments, not absolute values (`ZETA-NOT-PHYSICAL`, my S75 permanent), and that the choice of spectral functional is a physical question. Here the lesson sharpens: when two candidate functionals of the same `ρ(λ;τ)` are claimed to be "the same observable at different precision," the operational test is whether a precision-refinement of one can reproduce ALL structural properties of the other. A *number-theoretic* property — the exact rational `95/56` with clean small-denominator continued fraction — is one a float-band consumer gate CANNOT test (its resolution is ~1%, the property lives at the float-cancellation floor), and one a precision-refinement CANNOT toggle (refinements are monotone toward the true value; they do not add or destroy an exact-rational identity). So the gate's PASS-blindness to the `19/100 → 0.191038` substitution is the OPERATIONAL WITNESS that `Φ_anchor` ≠ `Φ_cross`: if they were the same functional at different precision, the higher-precision value would simply supersede the lower with no structural loss, and there would be no exact-rational property for the gate to be blind to. **The blindness IS the proof.** Under the Phi correspondence (`epistemic-discipline.md §"Layer-Decomposition"`), the substrate-physics fact `weight(Φ_anchor) ≠ weight(Φ_cross)` (two distinct functionals of one spectrum, the `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` situation) maps to the methodology fact `F_band(gate) cannot resolve the number-theoretic distinction` — the gate's float-band is the methodology-floor F-image of the substrate's functional-distinction, and it is structurally coarser than the property that distinguishes the functionals.

**Forward generalization (the durable methodology output).** This is a NEW failure-mode class for any future canonical-pin adjudication: *"the consumer gate passes either way, so the frozen value does not matter"* is a Class-6-adjacent (`v3-closure-recovery.md`) error WHENEVER the frozen value's load-bearing role is an EXACT-rational / number-theoretic property the consumer gate's float-band cannot resolve. The fix is to check the load-bearing role at its NATIVE precision (the exact-rational identity at the float-cancellation floor), not at the consumer gate's coarse band. This is the methodology-floor analog of my regularization principle that what is functional-INDEPENDENT is structural and what is functional-DEPENDENT requires determination: here the gate-PASS is functional-independent (it passes for both `Φ_anchor` and `Φ_cross`) precisely BECAUSE it is blind to the functional-dependent property (the exact rational), so the gate-PASS cannot be used to adjudicate which functional is canonical — exactly the same-functional-conflation trap, one layer up.

---

## Workshop Verdict

| # | Topic / Sub-question | Source | Status | Key Insight |
|:--|:---------------------|:-------|:-------|:------------|
| 1 | (a) S85 uniqueness — VALUE or EXISTENCE-only | T1, Re:T1 | **Converged** | The S85 theorem ASSERTS `tau_fold = 0.190` but does NOT DERIVE it: the 6-step chain (Steps 4-6: `dS/dτ ≠ 0`) proves only the cusp's CHARACTER (existence + non-stationarity + multiplicity-uniqueness), all LOCATION-free; `value='promoted'` is a self-consistency import (live pin == frozen 0.19), not a finder. The operative DOS / spectral-action-gradient functional has an EMPTY critical set (S95 NO-WELL-ONE-LOOP) ⇒ structurally incapable of locating the cusp. |
| 2 | (b) τ_cross=0.191038 canonical?; ±0.5% band meaning | T2, Re:T2 | **Converged** | `τ_cross = 0.191038` is a real substrate-IS LOCATION observable (`Φ_cross`), regulator-robust by sector-locality (L_max-invariant {5,8,10,12}; T3/T5 at p+q≤2), mesh-robust to 1e-6, reproducing atlas-07 S45 0.19104 to 5sf — NOT mesh/finder noise (Reading-A "noise" form REFUTED). The ±0.5% band is a chosen drift-detection literal (the S85 `INFO_REL_TOL = 0.005`), NO substrate-physical meaning. The 99× gap-ratio is the gap-MAGNITUDE restatement of the 0.55% τ-offset (steepness of `g` near its near-cusp vertex), NOT a second measurement (transit retracted the "0.190 is NOT the anticrossing point" amplifier). |
| 3 | (c) RE-PIN cascade scope (theorem + scripts + S_0 ratio) | T3, Re:T3 | **Converged** | Cascade splits LAYER-1 (location-free, SURVIVES any re-pin: S85 existence+non-stationarity, atlas-07 S45 0.19104, §VII.CH NOHOLOFLUX Projection-3, S₀^geo Connes-route band-insensitive) / LAYER-2 (BREAKS under a value re-pin: `S_0 = 95/56` EXACT shatters, `s101_w3_s0_knob.py` `assert` trips ⇒ crash, 119 registry rows go stale). Registry-anchor archaeology RESOLVED: the theorem lives at gate `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` + `proven_1424`/`proven_1905` + the patch file, NOT slot §VII.M.W10-3 (= c_sub residual; S112 W2 repair). |
| 4 | Output-form selection (re-pin / stands / two-value) | T4, Re:T4 | **Converged** | **Output (iii)**: two-value / three-functional NON-FUNGIBILITY convention, reached from TWO independent routes — transit's cascade asymmetry (re-pin breaks `S_0=95/56`+crashes S0-KNOB+stales 119 rows; (ii) suppresses a regulator-robust located observable) and lizzi's functional non-identity (no single number is simultaneously `argmin Δ_band` AND the exact rational 19/100). Both authors came in with OPPOSITE priors (transit→(i), lizzi→(ii)); both landed on (iii) because the analysis forces it. No re-pin of working `tau_fold`; `tau_cross_van_hove` lands as a NEW registered observable. |
| 5 | Functional-identity: crossing-locator vs uniqueness-value | L1, L2 | **Emerged** | THREE distinct functionals of the same `ρ(λ;τ)`: `Φ_DOS` (singularity-strength argmax 0.221), `Φ_cross` (band-edge-gap argmin 0.191038), `Φ_anchor` (rational freeze 19/100). The canonical pin names `Φ_anchor` (the rational role); re-pinning it with `Φ_cross`'s output is the same-functional-conflation error. **Emergent methodology datum**: a consumer gate's float PASS-band (S0-KNOB 0.01) is structurally BLIND to the exact-rational property (`CF(0.191038/0.112)=[1;1,2,2,1,1,18,44]` no clean rational vs `19/100→95/56` exact) that makes `Φ_anchor` load-bearing — and the blindness IS the proof of functional non-identity (a precision-refinement cannot toggle a number-theoretic property), a `phononic-framing.md §"Same-functional-different-scale"` instance at the methodology layer / Phi-correspondence F-image. |

Status categories: **Converged** | **Dissent** | **Partial** | **Emerged**

## Remaining Open Questions

1. **Confirmatory S0-KNOB re-run (LOW-priority / CONFIRMATORY; the workshop's sole math carry-forward).** Mechanically confirm Axes A+B of transit's Q1 resolution: re-run `s101_w3_s0_knob.py` with candidate (iii) routed as `q = 0.191038` (guard relaxed) to verify the GRADED selector still selects (iii) and `dev[iii]^{cross} = 0.00682 ≤ PASS_BAND = 0.01`; then Sage-confirm `CF(0.191038/0.112)` has a partial quotient ≥ 10 within the first 8 terms (no clean small-denom rational). CANNOT change the verdict (the exact-rational asymmetry is regulator-free arithmetic). 4-field spec in Carry-Forward Computations below.

2. **EVOI Tier-2 τ_fold-RELAXATION row — does `tau_cross_van_hove` warrant an update?** The S113 EVOI-campaign note tagged τ_fold value-CONDITIONAL; S114 W-1 RESOLVED it to (iii) (region van-Hove-selected, value a two-value non-fungible convention, no re-pin). Whether the EVOI table's τ_fold-RELAXATION leverage estimate should be re-stamped to reflect the resolved (iii) status (the value question is CLOSED as a convention, not an open compute) is a maintenance-pass question for `/rclab-plan` Step 1c-REGISTERS, NOT a substrate compute. Bookkeeping; route to the next plan's EVOI staleness pass.

3. **Does any OTHER derived-ratio consumer key on the EXACT-rational `19/100` (beyond `S_0 = 95/56`)?** transit traced S0-KNOB + S₀^geo; lizzi confirmed the 119 structural-anchor rows cite 0.190 as an anchor (numeric, not exact-rational-load-bearing). An exhaustive scan for OTHER `Fraction(19,100)`-keyed exact-rational identities (vs mere numeric-0.190 citations) would complete the `Φ_anchor`-load-bearing ledger. Not verdict-affecting (it can only ADD LAYER-2-under-re-pin items, all already avoided by (iii)); informational completeness.

## Wrap-Up — Workshop Impact Summary

### What Changed

#### (a) Numerical revisions

- `τ_cross` characterized: `tau_cross_van_hove = 0.191038` (band-edge anticrossing argmin, L_max-invariant {5,8,10,12}, mesh-robust 1e-6, atlas-07 S45 0.19104 to 5sf) — NEW canonical constant.
- Offset characterized: `|τ_cross − τ_fold| = 0.5464%` exact (`|0.191038 − 0.190|/0.190`), the round-number-freeze-vs-located-value gap (previously an unexamined coincidence).
- `Φ_anchor` load-bearing-ness localized to the float-cancellation floor: `CF(0.191038/0.112) = [1;1,2,2,1,1,18,44]` (large partial quotient 18, no clean small-denom rational) vs `19/100 → 95/56` exact; S0-KNOB dev shifts 0.00134 (frozen) → 0.00682 (substituted), both inside the 0.01 band (so the gate is value-blind).

#### (b) Structural changes

- **τ_fold object SPLIT into a three-functional non-fungible convention (the durable output).** The single canonical `tau_fold` is reframed as ONE of three distinct functionals of `ρ(λ;τ)`: `Φ_anchor` (rational freeze 19/100, the canonical pin's role) + `Φ_cross` (located band-edge argmin 0.191038, NEW registered observable) + `Φ_DOS` (singularity-strength argmax 0.221). Non-fungibility is FORCED by functional distinction, not chosen. (epistemic-type change: single-value-pin → three-functional partition.)
- **S85 theorem reclassified ASSERTS-not-DERIVES.** From "PERMANENT theorem that pins `tau_fold = 0.190`" → "PERMANENT theorem of the cusp's CHARACTER (existence + non-stationarity + multiplicity, LOCATION-free) that IMPORTS 0.190 as a premise." The PROVEN content is unchanged; its epistemic SCOPE is corrected (a located-claim re-read as a character-claim + an import).
- **New methodology failure-mode class.** "the consumer gate passes either way, so the frozen value does not matter" is a Class-6-adjacent error when the value's load-bearing role is an exact-rational property the gate's float-band cannot resolve — a `phononic-framing.md §"Same-functional-different-scale"` instance at the methodology layer (Phi-correspondence F-image).

### What Holds

- **The S85 theorem's PROVEN content survives intact**: existence + non-stationary character (`dS/dτ = +58,672.80 ≠ 0`, empty critical set, S95 NO-WELL-ONE-LOOP) + multiplicity-uniqueness on the cubic-BC class Γ_6. All location-free; INDEPENDENT of the 0.190-vs-0.191038 distinction. PERMANENT status unchanged.
- **`S_0 = 95/56` EXACT holds** — it is the structural-selector winner of S0-KNOB and depends on `tau_fold = 19/100`, which the verdict does NOT move.
- **`tau_fold = 0.19` canonical pin UNCHANGED** (CONST-FREEZE-42; `get_constant("tau_fold") = 0.19`, Superseded=False). No re-pin.
- **atlas-07 S45 TRUE crossing 0.19104** — already agrees with `Φ_cross = 0.191038`; the NEW observable confirms it to 5sf (removes a standing internal disagreement).
- **§VII.CH NOHOLOFLUX (STAGE-3-PERMANENT)** and the **S₀^geo Connes-route** survive location-free / band-insensitive; their anchor blocks pay ZERO staleness cost under the NEW-observable landing.

### What Breaks or Strains

- **Nothing breaks under output (iii)** — that is the point of the verdict. A value-only re-pin (output (i)) WOULD have: (1) crashed `s101_w3_s0_knob.py` (the `assert abs(float(Fraction(19,100)) − tau_fold) < 1e-15` trips at `|0.19 − 0.191038| = 1.04e-3 ≫ 1e-15`); (2) shattered the EXACT rational `S_0 = 95/56` and re-opened the S0-KNOB discriminator (0.191038/0.112 = 1.7057 is not a clean small-denom rational); (3) staled 119 registry rows citing `tau_fold = 0.190`; (4) staled the §VII.CH NOHOLOFLUX STAGE-3-PERMANENT anchor block. All avoided by (iii). Output (ii) ("0.190 stands, 0.191038 is noise") would have suppressed a regulator-robust substrate-IS observable and contradicted atlas-07 — also avoided.
- **Nothing else identified beyond what (iii) avoids.**

### Carry-Forward Computations (MATH ONLY — propagate to S115)

**CF-S115-S0-KNOB-CROSS-SUBSTITUTION-CONFIRM** (LOW-priority / CONFIRMATORY — CANNOT change the S114 W-1 verdict; mechanical due-diligence on Axes A+B of transit's Q1 resolution, the exact-rational asymmetry of Axis C being regulator-free arithmetic that needs no re-run).

- **What**: Re-run `s101_w3_s0_knob.py` with candidate (iii) routed as `q = 0.191038` (located crossing) instead of `q = Fraction(19,100)`, AND with the `assert abs(float(tau_f) - tau_fold) < 1e-15` guard relaxed to the substituted value, to mechanically confirm (a) the GRADED selector still selects (iii) and (b) `dev[iii] = 0.00682 ≤ PASS_BAND = 0.01` (gate still PASSes on (iii)); then confirm via Sage that `CF(0.191038/0.112)` has no clean small-denominator convergent (large partial quotient 18) so the `S_0 = 95/56` exact-identity has NO analog at the located value.
- **Inputs**: `computations/session-101/s101_w3_s0_knob.py`; `s101_envelope_carrier_discriminate.npz` (`legC_output_form=GRADED`, `S0_fit=1.694153`); `canonical_constants.py` (`tau_fold`, `T_acoustic`); Sage `continued_fraction`.
- **Gate**: PASS iff (selector selects (iii) under GRADED) AND (`0.00134 ≤ dev[iii]^{frozen}` and `dev[iii]^{cross} ≤ 0.01` both confirmed) AND (`CF(0.191038/0.112)` has a partial quotient ≥ 10 within the first 8 terms, certifying no clean small-denom rational). Confirms the analytic verdict; CANNOT flip it.
- **Effort**: Small (one script edit guarded behind a flag + one Sage CF call; ~15 min). Tagged LOW-priority / CONFIRMATORY — the verdict (iii) STANDS is settled analytically; this re-run is mechanical due-diligence, not a verdict-determining gate.

### Effected In-Session (NON-MATH — completed by lizzi, the final agent, BEFORE TERMINATING)

The (iii) landing is NOT a re-pin: `tau_fold = 0.19` stays put. The landing ADDS the located observable and ANNOTATES the anchor. Both agents concurred on this spec (Q3). All items executed as SURGICAL targeted patches (`feedback_framework-hygiene.md`); the §7 falsifier surface / `falsifier-master-inventory.md` was NOT touched (mack's sole-writer domain; τ_fold is an internal deformation parameter, not an observational falsifier row — no inventory edit owed).

- [x] **Item 1 — `canonical_constants.py`: ADD `tau_cross_van_hove = 0.191038`** (do NOT modify `tau_fold`). `canonical_constants.py:283` (assignment) + `:2034` (PROVENANCE dict entry); via knowledge MCP `update_constant`. `tau_fold = 0.19` verified UNCHANGED at `canonical_constants.py:289` (shifted one line by the insertion above; value intact).
- [x] **Item 2a — `permanent-results-registry.md` line-2620 tag correction (BOTH roles).** `permanent-results-registry.md:2620` — `scheme=RATIONAL-ANCHOR-FREEZE (19/100, CONST-FREEZE-42), convention=anchors-derived-ratio-chain-S0-95/56; located-feature tau_cross_van_hove=0.191038 (S114 CF-S114-TAUFOLD-CUSP-CROSSING, atlas-07 S45 0.19104)`.
- [x] **Item 2b — `permanent-results-registry.md` NEW registered observable.** `permanent-results-registry.md:2724` — `### VII-B.TAU-CROSS-VAN-HOVE` entry: band-edge anticrossing argmin (`Φ_cross`), three-functional NON-FUNGIBILITY statement (per `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"`), provenance (audit `7b637db1…`, atlas-07 S45, L_max-invariant sector-local), and cross-references to all `tau_fold`-anchor consumers (S0-KNOB `S_0=95/56`, 119 rows, NOHOLOFLUX Projection-3, S₀^geo Connes-route, atlas-07).
- [x] **Item 2c — S85-theorem ASSERTS-not-DERIVES amendment.** `computations/session-85/s85_w10_tau_fold_REGISTRY_PATCH.md:11` (ASSERTED-vs-DERIVED qualifier block: imports 0.190, proves only existence + non-stationary character + multiplicity, LOCATION-free, with the `tau_cross_van_hove` pointer). (The live registry §VII-B section has NO τ_fold prose theorem slot — the theorem lives in the patch-of-record + the `proven_1424`/`proven_1905` source statements amended in Item 3c — confirming the T1/T3 archaeology.)
- [x] **Item 3a — `atlas-04-assumptions.md` A4 freshness note RESOLVED to (iii).** `sessions/framework/Atlas/atlas-04-assumptions.md` (S114 in-cell freshness line, A4 clause): value sub-status "REGION-selected, precise-value CONDITIONAL (flank-sub-choice)" → RESOLVED to the two-value non-fungible convention (rational anchor `tau_fold = 19/100` UNCHANGED + located feature `tau_cross_van_hove = 0.191038`, neither replaceable); substrate-first preserved (A4 stays region-selected, no tag flip).
- [x] **Item 3b — capstone `phonic-exflation-equation.md` §6.3 S114 note RESOLVED to (iii).** `sessions/framework/phonic-exflation-equation.md` (§6.3 S114 W2 status note, τ_fold clause): "precise 0.190 a flank-sub-choice ... only the precise value is conditional" → RESOLVED to the two-value non-fungible convention, substrate-first (the substrate IS the band-edge anticrossing; `Φ_cross` LOCATES it; `Φ_anchor=19/100` is the rational role; canonical pin UNCHANGED). Capstone-hygiene §A in-session designated-writer surgical patch; prose tag == register tag.
- [x] **Item 3c — S85 theorem STATEMENT in its Atlas/framework sources qualified ASSERTED-not-DERIVED.** `sessions/framework/Atlas/atlas-07-permanent-results.md:739` (§VII.M.W10-3 row, `proven_1905` source) + `sessions/framework/Phononic-framework-hypothesis.md:598` (proven-results bullet, `proven_1424` source) + `:431` (P-1 prose statement, `proven_1424` source): each now states "= 0.190" is ASSERTED-as-imported-premise, the proof certifies CHARACTER (existence + non-stationarity + multiplicity, LOCATION-free), with the located `tau_cross_van_hove` pointer. (The `proven_1424`/`proven_1905` knowledge-index objects rebuild from these source docs via `/weave --update`, so the edits propagate.)

### Closing Line

τ_fold is not one number but three distinct functionals of `ρ(λ;τ)` — a rational anchor (19/100, load-bearing on the exact `S_0 = 95/56`), a located band-edge feature (`tau_cross_van_hove = 0.191038`), and a DOS-singularity peak (0.221) — non-fungible by construction, landed as a two-value convention that leaves the canonical pin and every downstream identity intact.
