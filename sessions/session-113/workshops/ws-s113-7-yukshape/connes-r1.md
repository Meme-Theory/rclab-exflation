# WS-S112-7 YUKSHAPE — Round 1

**Workshop**: WS-S112-7 YUKSHAPE (off-Casimir-graded route vs permanent wall, fermion SHAPE branch)
**Participant**: `connes-ncg-theorist` — Round 1, steelman Reading A (**off-Casimir route exists**)
**Reading-A pole**: a construction on `(A_K, H_K, D_K)` *beyond the C₂-graded ω-ladder* carries the gen-2 non-monotone sign-changing G-invariant DOF; the `CF-S103-NO-SIGN-HANDLE` WALL is specific to the **C₂-graded sub-calculus**, not the full algebra.

**One-line thesis**: The wall is a *rank-counting* artifact — SU(3) has **two** primitive Casimirs (C₂, C₃), and the §VII.BV/§VII.BL exhaustion tables enumerated only C₂-graded kernels; the cubic Casimir C₃ is conjugation-ODD and *not* comonotone with C₂ across the quark sectors, so a C₃-graded G-invariant construction supplies a genuine sign-changing per-generation handle — **provided it is sourced through the chirality grading γ₉ (the orientability Hochschild 3-cycle), not through a `|λ|`-spectral moment**, which reality kills.

---

## 0. What the wall actually proves (steelman the opponent first, honestly)

I will not strawman. Reading B is anchored on two STAGE-3-PERMANENT registry theorems I co-authored, so I know precisely how strong they are:

- **§VII.BL Generation-Blindness Obstruction** (audit `0f0c4f65`): `D_K` left-invariant on SU(3) ⇒ Peter-Weyl rep is multiplicity-scalar `π(a) = ⊕ π_{(p,q)}(a) ⊗ 1_{m(p,q)}`. Every `A_K`-built form — inner fluctuation `A = Σaᵢ[D_K,bᵢ]`, real image `ε'JAJ⁻¹`, twisted-inner `Ω¹_σ` (Skolem–Noether DEAD) — is scalar on each `ℂ^{m(p,q)}`, hence cannot lift the generation degeneracy. `R_cross = 1.019704`, `n_distinct = 2` EXACT ∀ L_max.
- **§VII.BV No-Sign-Changing-Slope Handle** (audit `0fcf87bb`): every per-generation slope kernel `κ_g(τ)` built from Peter-Weyl-invariant content factors through `C₂(p,q)` (tower `{4/3, 3, 6}`) and inherits the monotone-in-C₂ kernel's fixed sign (E7 Structural Monotonicity, atlas-07 W7) ⇒ the per-gen sign-pattern vector is UNIFORM `(+,+,+)`, never sign-changing. The route-(b) exhaustion table closes routes (a) inner-fluctuation, (b) spectrum-only moment, (c) twisted-inner, (d) opposite-action.
- **S110 `CF1-YUK-C2COSET` FAIL** (CV-8 Arm-G DEAD): the rank-1 wall is confirmed off the **C²-coset** transverse modulus too (`|dY₁₂/dδ|₀ = 8.73e-16`, `gen_degen_lift` numerically zero), strengthening §VII.BL scope from `{U(2)-slice + su(2)-direction}` to **ALL left-invariant internal transverse moduli** (S110 WS-C2COSET CONVERGED R3).

This is a formidable, multiply-Stage-2-verified wall. **I concede its core**: no *metric deformation* and no *`|λ|`-spectral moment* on the homogeneous SU(3) fiber lifts the generation degeneracy or produces a sign-changing slope. My Reading-A case is NOT that these results are wrong. It is that they are **scope-limited to a sub-class of G-invariant constructions** that the exhaustion tables silently assumed exhaustive, and that a structurally distinct G-invariant class — the **cubic-Casimir / orientation-graded** class — lies outside their reach and supplies exactly the missing DOF.

---

## 1. The rank-counting gap (Sage-verified, machine-exact)

SU(3) has **rank 2**. The center of its universal enveloping algebra `Z(U(su(3)))` is therefore freely generated by **exactly two** primitive Casimir invariants: the quadratic `C₂` and the **cubic `C₃`** (Racah; the cubic is the SU(3) anomaly/d-symbol invariant). A G-invariant scalar functional of an irrep `(p,q)` is, in full generality, a function `f(C₂, C₃)` of **both** — *not* `f(C₂)` alone.

The §VII.BV route-(b) exhaustion table tests only kernels that "factor through `C₂(p,q)`" (verbatim, registry line 21694). **C₃ is never enumerated.** This is the gap.

The two Casimir towers on the three quark-generation sectors `{(1,0), (1,1), (3,0)}` (Sage-exact, `QQ`):

| sector | dim | C₂ | C₃ | t = (p−q) mod 3 |
|:-------|:---:|:--:|:--:|:---------------:|
| gen1 = (1,0) | 3  | **4/3** | **10/9** | 1 |
| gen2 = (1,1) | 8  | **3**   | **0**    | 0 |
| gen3 = (3,0) | 10 | **6**   | **9**    | 0 |

- **C₂ tower** `(4/3, 3, 6)` — strictly increasing, sign vector `(+, +, +)`. This is what the wall uses; monotone-in-C₂ ⇒ uniform-sign.
- **C₃ tower** `(10/9, 0, 9)` — **NOT comonotone with C₂**; it *vanishes on the self-conjugate octet* gen2=(1,1) and is positive on the two non-self-conjugate sectors.

**The sign-changing handle exists at the two-Casimir level.** A linear kernel `κ_g = a·C₂(g) + b·C₃(g)` realizes a *non-uniform* sign pattern. Sage-exact, with `(a, b) = (−1, +2)`:

```
κ = ( (4/3)(−1) + (10/9)(+2),  3(−1) + 0,  6(−1) + 9(+2) )
  = ( +8/9,  −3,  +12 )
  → sign vector (+, −, +)          [a SIGN-CHANGING per-generation slope vector]
```

This is exactly the `∃ g, g′ : sign(κ_g) ≠ sign(κ_{g′})` the §VII.BV STRUCTURAL VERDICT declares "unreachable when the vector is uniform." It is reachable the moment the kernel is allowed to depend on the second Cartan invariant. **The wall's `(+,+,+)` uniformity is a property of the C₂-only sub-calculus, precisely as Reading A asserts.**

---

## 2. The honest threat, named in full: reality kills the *spectral-moment* C₃ channel

I will not bank §1 as a win, because there is a deep reason the wall's authors (myself among them) only used C₂. **C₃ is conjugation-ODD**; the BDI reality structure `[J, D_K] = 0` makes it inaccessible to any `|λ|`-spectral moment.

**Substitution chain (Sage-exact):**

```
Step 1 (reality, S99 W1): [J,D_K]=0, J²=+1 (BDI) ⇒ V_{(p,q)} and its conjugate V_{(q,p)}
        carry IDENTICAL |λ| spectra (the t=1 ≡ t=2 spectra-identical theorem).
Step 2 (invariant parity under conjugation (p,q)↔(q,p)):
        C₂(p,q) = C₂(q,p)   — conjugation-EVEN.   [(1,0): 4/3=4/3 ✓]
        C₃(p,q) = −C₃(q,p)  — conjugation-ODD.    [(1,0): 10/9 vs −10/9 ✗]
Step 3: any spectral functional F({|λ_k|}) is built from conjugation-EVEN data (Step 1),
        so it can depend on C₂ but is BLIND to C₃ (an odd quantity averaged over a
        reality-symmetric spectral measure vanishes).
Conclusion: for a |λ|-SPECTRAL slope kernel κ_g = d/dτ F({|λ|}), the C₃ channel is
            STRUCTURALLY ZERO. The wall's monotone-in-C₂ uniformity is FORCED — for
            spectral moments.  ∎ (this is the wall's true engine, and it is correct)
```

So the off-Casimir route **cannot** be a metric deformation or a spectral moment. If it could, §VII.BV would be false, and it is not. **Reading A must therefore identify a G-invariant construction that is neither.** This is the crux, and it is where the SHAPE branch genuinely "lives outside the Casimir-graded calculus" — but *outside the C₂-graded **spectral** calculus*, not outside the full algebra.

---

## 3. The off-Casimir construction: the chirality grading γ₉ is the geometric home of C₃

The escape is structural and already half-built in the corpus. The triality-odd invariant C₃ has a canonical geometric carrier on a Lie-group spectral triple: the **chirality grading γ₉** and the **orientability Hochschild cycle**.

**Construction (full-SU(3) σ-model / orientation-graded functional).** Recall the NCG orientability axiom (axiom 6): there exists a Hochschild cycle `c ∈ Z_n(A_K, A_K)` with `π(c) = γ` (the corpus states this verbatim: "NCG axiom 6 (orientability): existence of a Hochschild cycle c with π(c) = γ (chirality)", `s86-r-dual-pathway`). On SU(3) the relevant cycle is a **3-chain** (the manifold-orientation / volume-form cycle, `Γ_K = γ₉` the internal volume element, `session-25-baptista-collab`). **A 3-chain is cubic — its natural invariant pairing is the cubic Casimir C₃, not C₂.** The quadratic C₂ is the pairing of the *2-chain* (the metric / Killing form); the cubic C₃ is the pairing of the *3-chain* (the orientation / WZW form). The two gradings are the degree-2 and degree-3 pieces of the same Hochschild complex.

Concretely, the candidate sign-changing per-generation handle is the **γ₉-graded slope**:

```
κ_g^{orient}(τ)  :=  d/dτ  Tr_{(p,q)_g}[ γ₉ · O(τ) ]
```

for a G-invariant operator `O(τ)` carrying the orientation grading — the chiral analog of the spectral moment `Tr[O]`, with the γ₉ insertion. Because γ₉ is **triality-odd in its action across conjugate sectors** (KO-dim 6: `J γ₉ = ε″ γ₉ J` with `ε″ = −1`, the chirality-J anticommutation that "CPT flips chirality", `s66_product_ko_dim`), the γ₉-graded trace is conjugation-ODD and therefore pairs with C₃, NOT C₂. The reality-killing of §2 does **not** apply: a γ₉-graded functional is *designed* to be conjugation-odd, so reality does not annihilate it — it constrains it (exactly as in my S99 result: reality forces the diagonal degenerate but leaves the off-diagonal `arg(w)` phase, which is triality-odd, surviving *because the framework is BDI*).

**This is genuinely G-invariant.** C₃ is bi-invariant (central in `U(su(3))`, commutes with the full left- AND right-SU(3) action). γ₉ is the SU(3)-invariant volume element. The product `γ₉ · (C₃-graded operator)` is invariant under the full isometry group. **It does NOT break left-invariance** — this is the decisive disanalogy with the external `ε_LX`. The wall's homogeneity premise (left-invariance ⇒ multiplicity-scalar) is *satisfied*, yet the conclusion (uniform-sign slope) *fails*, because the conclusion was derived only for the *un-graded* (C₂) sub-calculus. The γ₉-graded sector is left-invariant AND sign-changing.

**Why §VII.BV missed it.** The route-(b) table's functional class is `F({λ_k, m_k}) = Σ_k m_k g(λ_k)` (registry line 21694) — an **ungraded** spectral moment. Inserting γ₉ produces `Σ_k m_k γ₉|_k g(λ_k)` — a *graded* moment (a McKean–Singer / index-type functional), which is a **different element of the Hochschild/cyclic complex** (odd cocycle vs even cocycle). The exhaustion was exhaustive over the even (C₂) sub-complex and silent on the odd (C₃) sub-complex. Routes (a)–(d) are all even; none carries γ₉.

---

## 4. Does it evade the S110 `CF1-YUK-C2COSET` FAIL? (the strongest threat, engaged directly)

The spawn asks me to engage honestly: S110 `CF1-YUK-C2COSET` FAILed off **ALL left-invariant internal moduli**. Does the γ₉/C₃ route evade that, or does it require leaving left-invariance (making it non-substrate)?

**It evades it — and the reason is precise.** Read the FAIL's own leg-membership argument (`s110_cf1_yuk_c2coset.py`, verbatim): "`dD_K/dδ ∈ Ω¹_{D_K}(A_K)` maps INTO the irrep-leg subalgebra `⊕ B(V_{(p,q)}) ⊗ 1`; the projection onto the multiplicity-leg commutant `⊕ 1_V ⊗ M_m(ℂ)` is EXACTLY ZERO." That argument concerns a **metric deformation** parametrized by δ entering D_K only through the orthonormal frame `E(δ)` and the spinor curvature offset `Ω(δ)`. The C²-coset split `L₃·diag(e^{+3δ}, e^{−δ}, e^{−δ}, e^{−δ})` is a **symmetric (real, conjugation-even) deformation of the Killing metric** — it is a degree-2 (C₂-sector) object by construction. It therefore CANNOT source the degree-3 (C₃/γ₉) grading, for exactly the reality reason of §2. **The S110 FAIL is a C₂-sector result.** It tests the metric/frame channel and finds it generation-blind — confirming §2, not refuting §3.

The γ₉-graded slope `κ_g^{orient} = d/dτ Tr[γ₉ O(τ)]` is **not** a metric deformation. It is the orientation-cocycle channel — the WZW/θ-sector. It lives in the **odd** part of `Ω•_{D_K}(A_K)`, which the leg-membership argument never reached (that argument was about `Ω¹` mapping to the irrep-leg; the γ₉ insertion changes the *cyclic degree*, not just the leg). So the C²-coset FAIL does not touch it.

**Crucial honest concession (the live tension I hand to Round 2).** There is a real question whether the γ₉-graded operator, once written out, *acts non-scalar on the multiplicity leg* or *merely re-distributes weight on the irrep leg*. Three sub-cases:

1. **If γ₉ O(τ) acts on the irrep leg only** (like C₂, C₃ themselves, which are scalar on the multiplicity leg by Schur), then under **LABELING-A** (generation = multiplicity copies within one sector) it is STILL generation-blind — the wall stands for that labeling. But under **LABELING-B** (the labeling §VII.BV *actually uses* — generation = the three distinct Peter-Weyl SECTORS `{(1,0),(1,1),(3,0)}`), the γ₉-graded per-SECTOR slope is sign-changing and the handle EXISTS. The two registry walls use *different generation labelings*, and the SHAPE/SPLITTING branch (the quark *crossing*, §VII.BV) is on LABELING-B. **On LABELING-B my construction delivers.**
2. **If γ₉ O(τ) requires an off-diagonal (inter-sector) component** to be γ₉-graded and G-invariant, then it is exactly the off-diagonal `arg(w)` phase of my S99 four-lens synthesis — triality-odd, BDI-surviving, living in mixing/orientation. That component is internal (it is the orientation cocycle of the *same* spectral triple), not an external `ε_LX` fibre.
3. **If no G-invariant γ₉-graded operator with non-trivial τ-dependence exists** (i.e., the only orientation cocycle is τ-constant, so its slope vanishes identically), then Reading B wins: the SHAPE branch is a permanent wall and the cubic channel, though formally present, is dynamically empty.

Sub-case 3 is the wall's last line of defense, and it is a **computable question**, not a foregone conclusion. That is the pre-registrable gate.

---

## 5. Pre-registrable gate (the decisive forward compute)

**Gate name**: `S113-YUK-C3-ORIENTATION-SLOPE` ([SIGN]; CV-8 Arm-H, the orientation-cocycle corridor — the FIRST off-C₂-sector probe, distinct from the dead Arm-G left-invariant-*metric* corridor).

**Observable.** On the single-τ-slice triple `(A_K, H_K, D_K(τ))` at and around `τ_fold = 0.190`, compute the **γ₉-graded per-sector slope vector**
```
κ_g^{orient}(τ) = d/dτ  Tr_{(p,q)_g}[ γ₉ · D_K(τ) ] / dim V_{(p,q)_g},   g ∈ {(1,0),(1,1),(3,0)},
```
using the existing `dirac_spectrum.py` block-diagonal machinery (γ₉ = γ₅ ⊗ Γ_K with Γ_K the SU(3) internal volume element / chirality grading already in the codebase) on the AMD RX 9070 XT per-block path.

**Pre-registered dual prior** (open-verdict, NOT pre-judged): **0.45 PASS / 0.55 FAIL** — genuinely open, reflecting that §1 proves the *formal* C₃ handle exists but §4 sub-case 3 leaves the *dynamical realization* open. (Contrast the S110 C²-coset prior 0.10/0.90: that was correctly FAIL-leaning because it was a C₂-sector metric test; this is a structurally distinct C₃/orientation-sector test, so the prior re-opens.)

- **PASS** (Reading A): the γ₉-graded slope vector is **sign-changing** (`∃ g, g′ : sign κ_g^{orient} ≠ sign κ_{g′}^{orient}`, magnitude `> ε_lift = 1e-3` in M_KK² units) AND its sign pattern tracks the C₃ tower `(+, 0, +)`-type structure rather than the C₂ tower `(+,+,+)`. ⇒ the off-Casimir orientation route supplies the gen-2 non-monotone DOF; the WALL is C₂-sub-calculus-specific; §VII.BV gains a scope qualifier "on the EVEN (C₂) sub-complex." (A PASS is a §VII.BV scope-amendment, not a refutation: the even-sector uniformity stands; the odd-sector handle is newly opened.)
- **FAIL** (Reading B): the γ₉-graded slope is either (i) identically zero (the orientation cocycle is τ-constant, sub-case 3) OR (ii) uniform-sign anyway (γ₉ collapses to a C₂-proxy on this spectrum). ⇒ the SHAPE branch is a genuine permanent wall; the cubic channel is formally present but dynamically empty; §VII.BV/§VII.BL extend to the odd sub-complex too, COMPLETING the homogeneity-obstruction genus.
- **INFO**: sign-changing but sub-threshold (`0 < |κ| ≤ ε_lift`), OR γ₉-grading well-defined only up to the order-one residual (the 2.0450 first-order obstruction of §VII.BO) — route to the enlarged-algebra `A_K^{PS}` where orientation may be cleaner.

**Selection-rule pre-flight** (`math-scripts.md`, mandatory): γ₉ has center-character `t(γ₉)` set by the volume-form triality; `Tr[γ₉ D_K]` on sector `(p,q)` is admissible iff the orientation cocycle's triality matches — this is the C₃ ≠ 0 condition (`C₃ = 0` on the self-conjugate octet gen2 is the *prediction* that gen2 is the sign-pivot, exactly where a crossing turns over). The pre-flight is itself a discriminator: it forces the gen2=(1,1) slope toward zero (C₃(1,1)=0), the geometric signature of a crossing.

**Why this is genuinely new compute** (not a re-run): every prior gate (W4-15, S101-W3, S110-CF1/CF2) computed *ungraded* `Tr[O]` or `Y_ij = ⟨ψ_i|(1j·D_K)|ψ_j⟩` slopes — the EVEN sector. **No prior gate inserted γ₉ into the per-sector slope.** This is the first probe of the odd (C₃/orientation) sub-complex. The S102 witness `s102_quark_pergen_kernel.npz` does NOT contain a γ₉-graded key — confirmed by its key list (`C2_tower, r_gen1/2/3, slope_asym, crossing_realized, sign_flip`; no `gamma9` / `C3` / orientation key).

---

## 6. Distinguishing the internal C₃ route from the external `ε_LX` (the substrate-boundary question)

Reading B's deepest move will be: "your γ₉-graded operator is just `ε_LX` in disguise — an external non-LI fibre connection — and `ε_LX` is already conceded EXTERNAL (§VII.BL home in the `A_K`-bimodule complement)." I must pre-empt this honestly.

**The distinction is sharp and structural.** `ε_LX` is defined (§VII.BL) as a deformation **OUTSIDE every `A_K`-module** — it breaks left-invariance on the multiplicity leg (the registry states verbatim: "`ε_LX` MUST BREAK left-invariance on the multiplicity space"). My γ₉/C₃ construction is the **opposite**: C₃ is *bi-invariant* (central in `U(su(3))`), γ₉ is the *isometry-invariant* volume element; both are manifestly left-invariant. The γ₉-graded functional lives **INSIDE** the spectral triple's own orientation data (NCG axiom 6's Hochschild cycle), not in its complement.

The substrate-IS framing (`phononic-framing.md`) flows the right direction: **the fabric IS the oriented spectral triple `(A_K, H_K, D_K, γ₉, J)`**; its chirality grading γ₉ is intrinsic substrate data (the internal volume form), and the triality-odd C₃ slope `d/dτ Tr[γ₉ D_K]` is a substrate-IS observable — not a flavour container the fabric sits in. If the gate PASSes, the quark crossing emerges FROM the substrate's own orientation cocycle, never imported INTO it.

The boundary case (sub-case 3, §4) is precisely where internal collapses to external: **if** the only τ-dependent G-invariant γ₉-graded operator must break left-invariance to be non-trivial, **then** it IS `ε_LX` and Reading B wins on the SHAPE branch too. The gate decides this. That is why the verdict is genuinely open.

---

## 7. Honest current lean and the single most decisive consideration

**(i) Honest current lean.** I lean **weakly toward Reading A, ~0.45–0.55 split — genuinely open, tilting Reading-A-possible but NOT Reading-A-proven.** What I have ESTABLISHED (Sage-exact, machine-precision): (a) the wall's exhaustion tables are scope-limited to the C₂-graded EVEN sub-calculus; (b) C₃ is a second, independent, bi-invariant Cartan invariant that is NOT comonotone with C₂ and DOES produce a sign-changing per-sector slope vector `(+,−,+)`; (c) the natural geometric carrier of C₃ — the chirality grading γ₉ / orientation Hochschild 3-cycle — is genuinely G-invariant and left-invariant, evading both the reality-killing (§2) and the C²-coset metric-FAIL (§4), because it lives in the ODD cyclic sub-complex the wall never tested. What I have NOT established: whether the G-invariant γ₉-graded operator has *non-trivial τ-dependence* (a non-zero slope) on the actual `D_K(τ)` spectrum, or whether it is dynamically frozen (sub-case 3). That is the one open hinge, and it is exactly the pre-registered gate `S113-YUK-C3-ORIENTATION-SLOPE`. So: the off-Casimir route is **formally real and substrate-internal** (not external `ε_LX`); its *dynamical realization* is the open question. The wall is provably narrower than its registry text claims (it is an EVEN-sub-complex wall, not a full-algebra wall); whether the ODD sub-complex is non-empty is the live, computable physics.

**(ii) The single most decisive consideration.** **Reality (`[J,D_K]=0`, BDI, J²=+1) does NOT kill the cubic channel — it *selects* it.** The wall's true engine (§2) is that reality makes spectral moments conjugation-EVEN, hence C₂-only, hence uniform-sign. But that same reality leaves the conjugation-ODD orientation grading γ₉ *alive* (KO-dim 6, `ε″ = −1`, `J γ₉ = −γ₉ J`) — precisely as my S99 result proved the triality-odd `arg(w)` phase survives BDI reality while it would die in DIII. The crossing-sign DOF the SHAPE branch needs is the *imaginary/orientation* partner of the *real/spectral* moments the wall exhausted. Reading B's wall holds airtight on the real axis and is silent on the imaginary axis. **The decisive test is whether the substrate's own orientation cocycle carries a τ-slope — if it does, the off-Casimir route is real and internal; if it is frozen, the SHAPE branch is a permanent wall completing the homogeneity-obstruction genus.** Everything turns on `d/dτ Tr[γ₉ D_K(τ)]` being non-zero, sign-changing, and C₃-patterned — a single, pre-registered, GPU-feasible compute.

---

*End connes-ncg-theorist Round 1. Reading-A steelman delivered; verdict and opponent's section deliberately not written.*
