# WS-S112-7 YUKSHAPE — R3 Structural Verdict (open)

**Workshop**: WS-S112-7 YUKSHAPE — off-Casimir-graded route vs permanent wall (fermion SHAPE branch)
**Adjudication question**: Does an off-Casimir-graded (full-SU(3) σ-model) construction on `(A_K, H_K, D_K)` supply the gen-2 non-monotone sign-changing DOF the joint quark mass crossing requires, or is the SHAPE branch a permanent wall (the fermion-mass SHAPE hierarchy irreducibly external, structurally like the just-closed M_KK magnitude obstruction)?
**Participants**: `connes-ncg-theorist` (Reading-A pole — off-Casimir route exists) × `paasch-mass-quantization-analyst` (Reading-B pole — permanent wall). Rounds R1/R2/R3.
**Verdict author**: connes-ncg-theorist (NCG-construction proponent, held to the conservative standard: EXHIBIT a concrete G-invariant off-Casimir construction → Reading A; or CONCEDE the wall credibly → Reading B). Written NEUTRALLY; does not favor the assigned pole.

---

## 1. Each agent's FINAL lean (faithful to their R2)

### connes-ncg-theorist (Reading-A pole) — final lean: ~0.40 Reading-A / 0.60 Reading-B, concentrated on ONE surviving channel
Conceded substantially across R2: (a) the R1 cubic-Casimir-on-sectors handle `κ = −C₂ + 2C₃ = (+,−,+)` answers the **wrong** question — the framework's physical generation index is the **multiplicity leg** (`t = (p−q) mod 3`, R2/R3-precision, STAGE-3-PERMANENT, `proven_384`), not the sector index `{(1,0),(1,1),(3,0)}`, and C₂/C₃ are both Schur-scalar on the multiplicity leg; (b) the algebra-enlargement / drop-Axiom-5 route is external (S31 Pati–Salam, unobserved `SU(3)_gen` gauge bosons); (c) the diagonal generation kernel is reality-locked (`d₁=d₂`, S99 §4.0); (d) the left-metric holonomy corridor (S110 CV-8 Arm-G) is dead. Held ONE position: paasch's R1 §2 *stated reason* ("the multiplicity leg `ℂ^{m(p,q)}` is `R_X`-inert, so homogeneity-alone forbids any G-invariant non-scalar") is provably **backwards** — by Peter–Weyl the multiplicity leg carries the **right-regular SU(3)_R action** and is `R_X`-**active**; a left-invariant operator CAN act non-scalar there via `R_X`. The correct wall is the narrower "`A_K` acts **left-only**, so `Ω¹_{D_K}(A_K)` never reaches the right-active leg" — which is §VII.BL's own cohomological form (registry line 21170). This relocates the dispute onto one channel: the **right-regular SU(3)_R connection** `δA_R`, which is G-invariant, multiplicity-non-scalar, and off-diagonal (evading the BDI diagonal J-lock), and whose internal-vs-external status is undischarged.

### paasch-mass-quantization-analyst (Reading-B pole) — final lean: Reading B (permanent wall), ~0.80
Conceded the rank-counting gap is real (the exhaustion tables were literally C₂-only; Schur gives `f(C₂,C₃)`, not `f(C₂)`), the C₃ tower is sign-changing, and the S110 metric channel is the even sector. Then landed **two independent analytic kills** on the specific off-Casimir carrier: **KILL #1** — `{γ₉, D_K} = 0` (PERMANENT, S34/S56) makes connes's exact R1 observable `Tr[γ₉ D_K]` a supertrace of an odd operator, hence **identically zero** at every τ, so `κ^{orient}(τ) = d/dτ(0) = 0` — analytic FAIL, not an open compute; **KILL #2** — generations are the multiplicity leg (R2/R3-precision), and C₃ (central, bi-invariant) is multiplicity-scalar by Schur, hence generation-blind on the `t`-leg, foreclosing LABELING-B (the only labeling on which the C₃ kernel was non-blind). Extended the closure to the full odd-cyclic disjunction (trace `Tr[γ₉ D_K^{odd}]` → zero; even moment `Tr[γ₉ f(D_K²)]` → conjugation-even → C₂ not C₃; odd cyclic cocycle `Tr[γ₉ a₀[D,a₁][D,a₂][D,a₃]]` → `A_K`-multiplicity-scalar → leg-blind; any made `t`-leg-dependent → `a ∉ A_K` → external). Self-limited at 0.80: the closure of connes's three concrete forms is analytic + permanent-theorem, but a *general* no-go over **every** conceivable γ₉-graded G-invariant functional is a structural argument, not a machine-verified exhaustion.

---

## 2. The crux (neutral statement)

Both poles converged — from opposite directions — on the SAME single hinge. It is NOT the C₂-vs-C₃ degree (that axis was resolved: C₃ is real but generation-blind on the multiplicity leg). The crux is the **left-regular vs right-regular** representation question:

> **Is there a functional on the substrate spectral triple `(A_K, H_K, D_K, γ₉, J)` that is simultaneously (i) NON-ZERO (escapes the `{γ₉, D_K}=0` supertrace-of-odd vanishing), (ii) NON-SCALAR on the multiplicity (generation) leg `ℂ^{m(p,q)}` (escapes the Skolem–Noether leg-membership), and (iii) SUBSTRATE-INTERNAL (reachable from `(A_K, H_K, D_K, γ₉, J)`, NOT an external `ε_LX` fibre connection)?**

The two poles agree on the structure and disagree only on whether (i)∧(ii)∧(iii) is jointly satisfiable:
- **Reading B claim**: jointly UNSATISFIABLE. (i) forces γ₉-even ⇒ `f(D_K²)` ⇒ conjugation-even ⇒ C₂ (paasch's §2 reality argument, conceded by connes). (ii) forces a non-scalar action on the `R_X`-active leg, which `[D_K, A_K]` cannot reach (`A_K` acts left-only) ⇒ requires `a ∉ A_K` ⇒ external. The two requirements pull in opposite directions; nothing inside `A_K` satisfies both.
- **Reading A claim**: the **right-regular generator `R_X`** is a single explicit candidate that satisfies (i) and (ii) — it is γ₉-even (bosonic isometry generator, so `Tr[γ₉ R_X f(D_K²)]` need not vanish, escaping KILL #1) AND it acts non-scalar on the multiplicity leg (it IS the right-regular SU(3)_R action there, escaping KILL #2). Whether it satisfies (iii) — internal vs external — is the undischarged question, because `R_X` is a genuine isometry of the substrate group-manifold geometry but is NOT in `A_K`'s left-regular differential calculus.

---

## 3. STRUCTURAL VERDICT — what the physics actually forces

### VERDICT: **SYNTHESIS, tilted strongly to Reading B** — a permanent wall over the entire `A_K`-built / Casimir-graded / γ₉-trace functional class (a pinnable theorem completing most of the homogeneity-obstruction genus), with ONE structurally-distinct channel (the right-regular SU(3)_R connection) surviving as UNRESOLVED-pending-a-single-compute, NOT as a refutation.

**First-principles reason (machine-exact, this verdict re-verified both kills at the operator level):**

The SHAPE branch's "lives outside the Casimir-graded calculus" resolves into a precise four-door disjunction over G-invariant functionals on `(A_K, H_K, D_K, γ₉, J)`, three of which are CLOSED analytically and one of which is OPEN:

| Door | Functional class | Fate | Closing fact (verified) |
|:-----|:-----------------|:-----|:------------------------|
| **D1** | γ₉-graded odd-power trace `Tr[γ₉ D_K^{2k+1}]` | **CLOSED — identically 0** | `{γ₉, D_K}=0` ⇒ supertrace of odd operator vanishes. Verified: `Tr[γ₉ D_K]=0`, `Tr[γ₉ D_K³]=0` (Sage QQ, machine-exact). connes's R1 `S113-YUK-C3-ORIENTATION-SLOPE` is analytic FAIL `κ^{orient}≡0`. |
| **D2** | γ₉-graded even spectral moment `Tr[γ₉ f(D_K²)]` (McKean–Singer / index) | **CLOSED — C₂ only** | `D_K²` is γ₉-even ⇒ trace survives, BUT `f(D_K²)` is a function of conjugation-EVEN `|λ|²` ⇒ by the reality argument (`[J,D_K]=0`, BDI) carries C₂, NOT the conjugation-odd C₃. (connes's own §2; conceded by both.) |
| **D3** | γ₉-graded odd cyclic cocycle `Tr[γ₉ a₀[D,a₁][D,a₂][D,a₃]]`, `a_i ∈ A_K` (the actual orientation 3-cocycle) | **CLOSED — generation-blind on the t-leg** | Every `[D_K, a]`, `a ∈ A_K`, maps into `⊕ B(V_{(p,q)}) ⊗ 1` (Skolem–Noether leg-membership, registry 21120/21155; the S110 mechanism). A product of multiplicity-scalars is multiplicity-scalar ⇒ the cocycle is `⊗1` on the generation leg ⇒ distinguishes SECTORS (LABELING-B, registry-foreclosed) but NOT the `t`-generations (LABELING-A, the operative index). |
| **D4** | right-regular `R_X`-built operator `Y_R = Σ_a c_a R_{X_a}` (the SU(3)_R connection `δA_R`) | **OPEN — internal-vs-external undischarged** | `R_X` escapes D1 (γ₉-even ⇒ trace non-vanishing) AND escapes D3 (it is NON-`A_K`-built: the right-regular algebra, not in `[D_K, A_K]`). The leg-membership theorem covers `A_K`-built forms; `R_X ∉ A_K`-image. So the S110 leg-membership and KILL #1/#2 do NOT close D4. The open question is whether `δA_R` is substrate-internal (the group manifold's right isometry is real geometric data fermions can couple to) or the external `ε_LX` in new dress (`A_K` is pinned left-only as substrate physics, so coupling fermions to `R_X` is by-definition outside the fermion algebra). |

**Why "permanent wall, tilted to Reading B" and not "Reading B closed":** Doors D1–D3 are the *entire* class of functionals connes's R1 actually proposed (Casimir-graded, γ₉-trace, orientation-cocycle), and they are closed by **permanent theorems + machine-exact operator checks**, not by held numbers. For that class, paasch's permanent-wall theorem is EARNED and I endorse it as the NCG proponent: **no `A_K`-built, Casimir-graded, or γ₉-traced G-invariant functional supplies the multiplicity-leg generation crossing.** This completes most of the homogeneity-obstruction genus. But D4 is a genuinely distinct functional class (right-regular, NOT `A_K`-left-built) that the workshop's two kills provably do not reach — and connes's R2 located it precisely. A conservative verdict cannot pin D4 closed without computing the internal-vs-external discriminator (no prior gate has: S110 tested the left-metric channel, never the right-regular generators). Equally, a conservative verdict cannot declare Reading A wins, because connes did NOT *exhibit* a working construction — he exhibited a candidate whose decisive property (iii internal) is unproven.

### Reconciliation with the S110 `CF1-YUK-C2COSET` off-ALL-left-invariant-internal-moduli FAIL
The S110 FAIL is fully consistent with this verdict and does NOT close D4. BOTH poles agree (and this verdict confirms by reading the npz): the S110 deformation is a **left-invariant Killing-metric/frame scan** (`convention = deformed-L3.I4-split-metric-C2coset`; no `γ₉`/`C3` key; conjugation-even; `n_gen=2`, `gen_sector=[1,0]`, degenerate-multiplet). Its engine is the **leg-membership argument** — `dD_K/dδ ∈ Ω¹_{D_K}(A_K)` maps to the irrep leg, projection onto the multiplicity commutant exactly zero — asserted over "all 28 left-invariant params." That argument closes **D3-type `A_K`-built deformations** (which is why it covers "ALL left-invariant internal *moduli*"): metric moduli ARE `A_K`-left-reachable. It does NOT close D4, because **`R_X` is not a left-invariant *metric modulus* — it is a right-regular operator outside `Ω¹_{D_K}(A_K)`**. The S110 scope phrase "ALL left-invariant internal transverse moduli" is precise: moduli = metric deformations = `A_K`-reachable; the right-regular connection is not a metric modulus. So S110 confirms D3 (and the genus's left-invariant-metric corridor is dead) while leaving D4 untouched. There is no contradiction: the wall is complete over `A_K`-built forms; D4 asks whether the substrate's fermion sector is `A_K`-built or also right-regular.

---

## 4. Forward artifact

The verdict is SYNTHESIS, so BOTH artifacts are warranted — a pinned theorem for the closed class AND a forward gate for the open channel. Stated for next-session adoption:

### 4a. Pinned permanent-wall theorem (the CLOSED class — completes most of the genus)

> **§VII.[next] — SHAPE-Branch Homogeneity Obstruction over the `A_K`-Built / Casimir-Graded / γ₉-Trace Class (candidate permanent-wall theorem).** On the homogeneous Jensen-deformed spectral triple `(A_K, H_K, D_K, γ₉, J)`, `A_K = ℂ⊕ℍ⊕M₃(ℂ)`, KO-dim 6, NO G-invariant functional in the class {Casimir-graded scalar `f(C₂,C₃)` / γ₉-graded odd-power trace / γ₉-graded even spectral moment / γ₉-graded `A_K`-orientation cyclic cocycle} supplies a non-monotone sign-changing per-GENERATION (multiplicity-leg `t`) scalar. Proof: D1 `Tr[γ₉ D_K^{odd}]≡0` by `{γ₉,D_K}=0`; D2 `Tr[γ₉ f(D_K²)]` conjugation-even ⇒ C₂ by `[J,D_K]=0`; D3 every `A_K`-built form is multiplicity-scalar by Skolem–Noether leg-membership. The crossing SHAPE handle is external to this class — the same external `ε_LX` channel that carries the §VII.BL magnitude. **Scope qualifier (load-bearing): the class is `{A_K-built ∪ Casimir-graded ∪ γ₉-traced}`; the right-regular SU(3)_R connection (D4) is NOT covered and is OPEN (see 4b).**
> - STAGE-1-CANDIDATE; STRUCTURAL-ORTHOGONAL-COMPANION to §VII.BV (crossing-slope SIGN) and §VII.BL (hierarchy MAGNITUDE), sharing the multiplicity-scalar mechanism on a third functional axis (the γ₉/orientation axis).
> - NON-PROMOTION-BY-HELD-NUMBER, **sign-lock** differentia (uniform sign forced; the held quantity is a sign-pattern). Per `cross-pillar-bridge-anatomy.md`.
> - Two new permanent anchors brought to bear this workshop: `{γ₉, D_K}=0` (S34/S56) + the multiplicity-leg generation identification (R2/R3-precision, `proven_384`).

This is a STRONG, falsifier-relevant completion: it converts "we haven't found the σ-model SHAPE handle" into "the SHAPE handle is provably absent from the `A_K`/Casimir/γ₉-trace class, for the same homogeneity reason the magnitude is" — and any future model claiming a substrate-internal SHAPE handle in that class is refuted unless it exhibits a D4-type (right-regular) construction with the internal property discharged.

### 4b. Pre-registrable forward gate (the OPEN channel D4 — the single decisive compute)

> **Gate `S113-YUK-RIGHTREG-CONNECTION` ([SIGN]; CV-8 Arm-H, the right-regular-connection corridor — structurally distinct from the dead Arm-G left-metric corridor).**
> 1. **What**: On the single-τ-slice triple at/around `τ_fold=0.190`, construct the right-regular generation operator `Y_R = Σ_a c_a R_{X_a}` (right-translation generators of SU(3)_R acting on the multiplicity leg `ℂ^{m(p,q)}`) and test the four-part discriminator: **(i)** `[L_g, Y_R]=0 ∀g` (left/G-invariance — EXPECTED PASS by construction; the discriminator from the dead left-metric route, which deformed the metric not the right-action); **(ii)** `Y_R` non-scalar on `ℂ^{m(p,q)}` with a **sign-changing** generation-eigenvalue pattern across the `t=0,1,2` copies (the SHAPE handle — the open question); **(iii)** `[J, D_K + Y_R]=0` block-by-block (BDI reality / off-diagonal-survival check); **(iv) the load-bearing discriminator** — whether `Y_R ∈ closure(Ω¹_{D_K}(A_K))` (Reading B: external `ε_LX`) or `Y_R` is a connection on the substrate's OWN SU(3)_R principal bundle reachable from `(A_K, H_K, D_K, J)` geometry without enlarging `A_K` or dropping Axiom 5 (Reading A: internal).
> 2. **Inputs**: `dirac_spectrum.py` block-diagonal machinery (left-regular `D_K` already built); the right-regular generators `R_{X_a}` on the Peter-Weyl multiplicity leg (NEW — the right-translation action, authored here, the right-regular analog of the existing left-action); `J` (BDI real structure); canonical `tau_fold`, `M_KK`. AMD RX 9070 XT per-block path.
> 3. **Gate (PASS/FAIL/INFO)**: **PASS** (Reading A) = (i)∧(ii)∧(iii)∧(iv-internal): `Y_R` is left-invariant, sign-changing on the `t`-leg, reality-compatible, AND substrate-internal (a right-bundle connection, not in `Ω¹_{D_K}(A_K)` yet reachable from substrate geometry) ⇒ a named off-Casimir route exists; §VII.BV/§VII.BL/§4a gain the scope qualifier "for `A_K`-LEFT-built forms." **FAIL** (Reading B) = (iv-external): `Y_R` either lies in `closure(Ω¹_{D_K}(A_K))` (then it is multiplicity-scalar after all, contradicting ii) OR requires enlarging `A_K`/dropping Axiom 5 to act on fermions (then it adds unobserved structure) ⇒ D4 closed, §4a upgrades to UNCONDITIONAL permanent wall, genus complete. **INFO** = (ii) sign-changing but the internal/external discriminator (iv) is itself convention-dependent (e.g. the fermion right-action is a modeling choice not fixed by the 7 axioms) ⇒ the SHAPE handle is a CHOICE the framework has not pinned, routed to a representation-pinning workshop.
> 4. **Effort**: 1 compute agent, ~1 wave. The right-regular generators are a standard Peter-Weyl construction; the discriminator (iv) is the substantive physics. Dual prior **0.40 Reading-A-internal / 0.60 Reading-B-external** — genuinely open; discriminator (iv) is load-bearing and has NEVER been computed (all prior quark-Yukawa gates — W4-15, S101-W3, S110-CF1/CF2 — tested the left-action/left-metric channel; none constructed the right-regular generators).

### Falsifier / registry surface routing
The §VII registry candidate (4a) is a §VII NCG/geometric structural-theorem landing — **gen-physicist** orchestrator-direct registry sole-writer per `feedback_mack-bridge-role.md` (NOT a §7 falsifier-surface row). **No §7 falsifier-inventory edit is owed by this workshop**: the SHAPE-branch external-ness does not (yet) add or change an observational falsifier row — it is an internal structural completion. **IF** a future session promotes §4a to STAGE-3-PERMANENT (after the 4b gate FAILs and closes D4), the atlas-08 Q18b "Yukawa hierarchy beyond rank-1" SHAPE sub-status would flip toward "SHAPE branch external, genus complete" — that Q18b/atlas-08 status-cell update and any capstone §7 fermion-mass-prediction wording would then be **routed to `mack-cosmic-bridge`** (§7 falsifier/observable surface sole-writer) for the capstone-hygiene reconciliation. This workshop NAMES that future routing; it does not perform it (no falsifier row changes at this verdict, since D4 is open).

---

## 5. Residual dissent + the decisive compute

**Residual dissent (narrow, well-defined, honestly held by the Reading-A pole):** The Reading-A pole does NOT concede the SHAPE branch is an *unconditional* permanent wall, on the single ground that paasch's two kills + the S110 leg-membership cover the `A_K`-built / Casimir / γ₉-trace class (D1–D3) but provably do NOT cover the right-regular channel (D4). Paasch's own R2 §5 concedes this is the legitimate open hinge ("I have NOT proven a general no-go covering every conceivable γ₉-graded G-invariant functional"). The dissent is therefore SHARED in structure: both poles agree D1–D3 are closed and D4 is open; they differ only in prior weight on D4's outcome (paasch 0.80 external / connes 0.40 internal). This is not a contradiction in the ledger — it is a correctly-localized open channel with agreed structure and disputed prior.

**The decisive compute** is gate `S113-YUK-RIGHTREG-CONNECTION` (4b), and specifically its discriminator **(iv)**: does the right-regular SU(3)_R connection `δA_R` lie inside `closure(Ω¹_{D_K}(A_K))` (external by paasch) or is it a substrate-internal connection on the existing SU(3)_R isometry bundle (internal by connes)? This single membership question — `R_X ∈ [D_K, A_K]`-module-closure or not — collapses the residual dissent in one direction or the other:
- if `R_X` is NOT `A_K`-reachable AND is a legitimate substrate fermion DOF → Reading A, named off-Casimir route, §VII.BV scope-qualified;
- if `R_X` is either `A_K`-reachable (⇒ multiplicity-scalar, contradiction) or requires external augmentation to couple fermions → Reading B, §4a upgrades to unconditional, the homogeneity-obstruction genus is COMPLETE (both magnitude and shape of the fermion mass texture irreducibly external, the structural analog of the closed M_KK magnitude obstruction).

Until that gate runs, the honest ledger state is: **the SHAPE branch is a permanent wall over the `A_K`-built / Casimir-graded / γ₉-trace class (pinnable now, §4a STAGE-1-CANDIDATE), with the right-regular SU(3)_R channel the sole surviving open door (S113-YUK-RIGHTREG-CONNECTION, dual prior 0.40/0.60).** Neither "Reading A wins" nor "Reading B closed" is forced by the physics at R3; SYNTHESIS-tilted-to-B is.

---

*End WS-S112-7 YUKSHAPE R3 Structural Verdict. Workshop complete. Both forward artifacts (4a pinned-theorem candidate + 4b open gate) specified for next-session adoption; falsifier-surface routing to mack-cosmic-bridge named but not performed (D4 open).*
