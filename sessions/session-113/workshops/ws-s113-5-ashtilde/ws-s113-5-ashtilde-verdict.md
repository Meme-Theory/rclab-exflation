# WS-S112-5 AS-HTILDE — R3 Structural Verdict (open)

**Workshop**: WS-S112-5 AS-HTILDE (Session 113 EVOI-frontier campaign; spec `sessions/session-112/session-112-workshop-schedule.md` lines 66–75)
**Adjudication question**: Which leg (TD impulse-quench source vs LI H̃-divergence) sets the A_s amplitude floor? Is the magnitude pre-registrable, or is `A_s_FW = 1.537e-08` a floor-only statement?
**Poles**: transit-dynamics-theorist (Reading A — TD impulse-quench source) × lizzi-spectral-functional-theorist (Reading B — LI H̃-divergence rate-limiter)
**Rounds**: R1 steelman / R2 rebuttal / R3 convergence (this document)
**Date**: 2026-06-22
**Verdict written by**: transit-dynamics-theorist (writing NEUTRALLY per dispatch; the verdict does NOT favor the Reading-A pole)

**One-line verdict**: **SYNTHESIS (clean two-object split), arrived at by genuine convergence — both poles independently agreed on every number.** The A_s **floor-amplitude POINT** (`A_s_FW = 1.5367e-08`) is set by the **TD impulse-quench source**, fixed by UV/fold-epoch Bogoliubov scattering data, H̃-independent at the scale-origin level — Reading A wins the floor-origin question, conceded by Reading B on the merits. But the **magnitude-as-a-Planck-comparison-number is OPEN**: three physically-defensible functionals span 1.26 OOM with none in Planck's ±5% band, plus the separate open exit-greybody filter — so A_s **joins the §EVOI.BF route-unstable liability as a magnitude-open observable while its floor stands permanent**. The LI H̃-divergence is **scoped down** to the rate-limiter of the UNIFIED-AS-79 *route's* Planck-agreement (the diagnosed-wrong slow-roll functional), NOT the floor-setter.

---

## 1. Each agent's FINAL lean (from their R2)

### Reading A — transit-dynamics-theorist (final, `transit-r2.md`)

- **Conceded** (genuine corrections to its own R1): the clean-split frame (floor +1 leg / scale +2 leg) is the right organizing principle; the +2 leg-power leverage hierarchy of UNIFIED-AS-79 is correct *within that functional*; **retracted its R1 "no H̃ anywhere" overclaim**.
- **Final claim**: the TD impulse-quench source sets the amplitude scale *within the physically-correct functional* because the box-delta `|β_k̂|²` rides on the **fold-transit** conformal rate `aH|_fold = 0.975 M_KK` (the `z''/z` pump barrier) + Z-pump jumps + ξ_KZ, NOT the **horizon-exit** `H̃ = 5.9e-3 M_KK`. The two are **165× / 2.22 OOM apart at different epochs**. The decisive structural fact: the 181× SDW/Zubarev `a_0/a_2` functional freedom (the engine of Reading B's "magnitude-OPEN") is a property of the horizon-exit H̃ carrier, which the impulse-quench leg **does not read** — so `d|β_k̂|²/d(a_0/a_2 horizon-exit) = 0`; the openness is confined to the UNIFIED-AS-79 leg.
- **On pre-registrability**: floor-permanent + magnitude pre-registrable as a **typed, scheme-tagged POINT** (`1.5367e-08`), NOT as a scheme-independent number; conceded the cross-functional width is a real registered `SCHEME-DEPENDENT` tag.

### Reading B — lizzi-spectral-functional-theorist (final, `lizzi-r2.md`)

- **Conceded the floor-ORIGIN to Reading A on the merits.** Ran her own R1-nominated decisive test and found the KZ scale `k̂ = 1/ξ_KZ = 53.30 M_KK` is **9023× / 3.96 OOM separated** from the horizon-exit H̃; her R1 "back-door H̃" conjecture (the ξ_KZ normalization re-introduces an H̃-like factor) is **refuted by the substrate data**. The floor amplitude is fixed by UV Bogoliubov scattering data at 0 free params, H̃-independent.
- **Surviving Reading-B residue**: the magnitude-as-a-number is OPEN — three defensible functionals (impulse-quench +0.864, UNIFIED-AS-79 +0.196, Parker-adiabatic +1.455 OOM) span 1.26 OOM, none in Planck's band; plus the open exit-filter. The H̃-divergence is **demoted** from "floor rate-limiter" to "rate-limiter of the UNIFIED-AS-79 *route's* Planck-agreement" (the diagnosed-wrong slow-roll route).
- **Final lean**: *Floor-ORIGIN → Reading A (conceded); magnitude-as-number → floor-permanent / OPEN*; explicitly proposed a **CLEAN SPLIT pinning BOTH** as the most defensible verdict.

### Convergence state

This is a genuinely-converged workshop, not a stalemate. Both poles independently arrived at the **same clean split** and **agree on every number** (the impulse-quench POINT, the cross-functional spread, the two scale-separations, the floor-permanence). The residual disagreement is a single *definitional* question (what "the A_s floor" denotes), not a physics disagreement — which is the signature of a SYNTHESIS, not an UNRESOLVED.

---

## 2. The crux — which leg sets the floor, vs. does the scale trace through both/neither?

The crux, sharpened across three rounds, is **definitional, riding on a settled physics fact**:

> **What does "the A_s amplitude floor" REFER TO?**
> **(i)** the impulse-quench produced-amplitude **POINT** `1.5367e-08` (the substrate-determined, H̃-independent, 0-free-param amplitude of the produced relic) — **set by the TD source**; OR
> **(ii)** the cross-functional **envelope** within which the magnitude-as-Planck-comparison-number is pinned (three defensible functionals spanning 1.26 OOM, none in-band) — **OPEN**.

The underlying **physics fact is settled and agreed**: the impulse-quench `|β_k̂|²` is fixed by fold/UV-epoch transit data, H̃-independent. Both poles verified this from substrate data via two independent scale-separations, and a neutral cross-check (this verdict) confirms they are consistent:

```
Reading A (transit-r2): aH|_fold / H̃ = 165.1× = 2.22 OOM   (the production-barrier conformal rate)
Reading B (lizzi-r2):   k̂/H̃ = (1/ξ_KZ)/H̃ = 9023× = 3.96 OOM   (the KZ read-off wavenumber)
ratio between them: k̂/aH|_fold = 54.6×  — BOTH are fold/UV-epoch quantities; NEITHER is the horizon-exit H̃.
```

The two scale-separations differ (165× vs 9023×) because they measure different things — `aH|_fold` is the conformal rate *in the production barrier*, `k̂ = 1/ξ_KZ` is the comoving wavenumber *at which `|β|²` is read off*. Both are correct, both place the impulse-quench scale at the fold/UV epoch, and both establish the same conclusion: **the floor amplitude does NOT trace through the horizon-exit H̃.** The scale traces through *neither* leg's H̃ in the sense Reading B originally claimed — it traces through the fold-transit Bogoliubov scattering data, which is the TD source.

So: **the floor-amplitude POINT is set by the TD impulse-quench source; the H̃-divergence rate-limits only the parallel UNIFIED-AS-79 *route*, which is the slow-roll functional inv-1's 8-agent survey diagnosed as misapplied to the impulsive quench.** A quantity cannot be rate-limited by a parameter absent from its definition; H̃ is absent from the impulse-quench floor's definition (`A_s = |β_k̂|²/(2π²)`, KZ-volume normalized).

---

## 3. STRUCTURAL VERDICT

### Verdict: SYNTHESIS — a clean two-object split

The verdict is **SYNTHESIS**, not Reading A, Reading B, or UNRESOLVED. It is SYNTHESIS rather than "Reading A wins" because the workshop adjudicates TWO distinct objects, and each pole owns one:

> **OBJECT 1 — the A_s floor-amplitude POINT (`A_s_FW = 1.5367e-08`):**
> **SET by the TD impulse-quench source.** First-principles reason: the produced-relic amplitude is `A_s = |β_k̂|²/(2π²)` with `|β_k̂|²` read from the box-delta sudden scattering spectrum (3-code-path PASS to 1.4e-13) and the KZ coherence volume `ξ_KZ³` (substrate-natural, S89). Both inputs are governed by the *fold-transit* rate (`aH|_fold`, the `z''/z` pump barrier, Z-pump jumps) and the critical-slowing freeze-out scale (`k̂ = 1/ξ_KZ`), at the fold/UV epoch — 165× to 9023× separated from the horizon-exit H̃. **The floor's scale is H̃-INDEPENDENT** (`dA_s^{impulse}/dH̃ = 0`); a shared `a(τ)` trajectory read at two scale-separated epochs gives two independent observables, not one riding on the other. **Reading A wins the floor-origin question; Reading B conceded this on the merits in R2.**

> **OBJECT 2 — the A_s magnitude-as-a-Planck-comparison-number:**
> **OPEN (floor-permanent / magnitude scheme-dependent).** First-principles reason: three *physically-defensible* functionals of the same spectrum give different amplitudes — impulse-quench (+0.864 OOM), UNIFIED-AS-79 (+0.196 OOM), Parker-adiabatic (+1.455 OOM) — spanning **1.2590 OOM with NONE inside Planck's ±5% band** (would need |OOM| < 0.0212). This is the spectral-functional pluralism (lizzi-signature) made observable: what survives all functionals is the floor INEQUALITY; what depends on the functional is the magnitude. Plus the separately-open upper-edge exit-greybody filter (∫Γ_derived = 0.036 vs fitted 0.512, S110 AS2-GREYBODY FAIL). **Reading B's residual object stands.**

### The floor-vs-magnitude two-layer split (the decisive sub-question, answered explicitly)

**A_s JOINS the §EVOI.BF route-unstable liability as a magnitude-open observable while its floor stands permanent.** The honest status is the two-layer split both poles converged to:

- **(a) FLOOR `A_s ≥ A_s^{BD}` (`S_IC = 1 + 2n_k ≥ 1`):** PERMANENT, FUNCTIONAL-INDEPENDENT, confirmed on THREE orthogonal axes (reference-state inv-12 W1-2 `Δ=+1.04e-13>0`; families-index η-form inv-12 W2-5 η≡0 EXACT; dynamical-Bogoliubov inv-5/inv-6). `proven_1097`. **Only the inequality `>1` deserves "permanent."** NOT in dispute; co-owned by both poles.
- **(b) MAGNITUDE / upper-edge:** SCHEME-DEPENDENT and OPEN, with the openness now precisely localized to TWO sources:
  - **(b-i) cross-functional spread** — which spectral functional (impulse-quench / UNIFIED-AS-79 / Parker / near-floor-DOS) weights the spectrum; 1.26 OOM across the three defensible ones, none in-band.
  - **(b-ii) within the UNIFIED-AS-79 functional, the H̃-carrier's `a_0/a_2` SDW/Zubarev freedom** (181× Path-B split, the CC-in-H-form, S82 Obs 6.3) — **confined to the UNIFIED route**, NOT a property of the impulse-quench floor (the R2 finding both poles accept).

### Is the magnitude pre-registrable?

**As a TYPED, scheme-tagged POINT: YES.** `A_s_FW = 1.5367e-08` (scheme=IMPULSE-QUENCH-BOGOLIUBOV, +0.864 OOM vs Planck, epistemic-type POINT per S111 AS3b: per-charge GGE NO-SHIFT, Δλ_pivot=0 EXACT) is a defensible, substrate-determined floor-amplitude pin. This is STRONGER than "route-unstable" — it is a 0-free-param produced-amplitude with a named scheme.

**As a single scheme-INDEPENDENT Planck-comparison number: NO.** The cross-functional spread (b-i) is a genuine registered `SCHEME-DEPENDENT` width; no convergence criterion selects one functional's value as "the" prediction, and none lands in Planck's band. **A_s is therefore a magnitude-open observable on the falsifier surface** — it sharpens §EVOI.BF's ">3 OOM, no convergence" by *re-localizing* the liability: the >3-OOM spread is the **cross-functional + filter** openness (including the diagnosed-wrong slow-roll route), NOT a within-impulse-quench instability and NOT evidence that the H̃-divergence sets the scale.

### The H̃-divergence's correctly-scoped status

The LI H̃-divergence is a **real object, correctly scoped**: it is the rate-limiter of the **UNIFIED-AS-79 route's agreement with Planck** (within that functional `A_s ∝ H̃²`, `d ln A_s/d ln H̃ = +2`, TD/LI branches span 4.76 OOM = exactly 2× the 2.38-OOM H̃-space gap via CC3, machine-exact). The framework's own INV12-W3-5 script names it "the A_s rate-limiter" — **for that functional**. It is NOT the rate-limiter of the floor, because the floor is the impulse-quench functional (no H̃). It contributes to the (b-i) cross-functional spread as one component (the UNIFIED route's internal 4.76-OOM ambiguity), demoted from "floor rate-limiter" to "rate-limiter of the diagnosed-wrong slow-roll route."

---

## 4. Forward artifact

The verdict produces BOTH a defensible status-pin (the floor is set) AND a sharpened liability statement (the magnitude is open) — because the workshop adjudicated two objects. Per the dispatch routing, the falsifier-surface edits go to **mack-cosmic-bridge** (sole writer of the §7 falsifier surface + `falsifier-master-inventory.md` per `feedback_mack-bridge-role.md`); I do NOT write them here.

### 4A. Pinned status statement (the floor IS set; this is the verdict's primary deliverable)

```
A_s MAGNITUDE-ORIGIN — PINNED STATUS (WS-S112-5 verdict):

FLOOR-AMPLITUDE POINT  →  SET by the TD impulse-quench source.
  A_s_FW = 1.5367e-08 (scheme=IMPULSE-QUENCH-BOGOLIUBOV, |β_k̂|²/(2π²), KZ-volume ξ_KZ³,
  epistemic-type POINT per S111 AS3b). H̃-INDEPENDENT at the scale-origin: the box-delta
  |β_k̂|² rides on the fold-transit pump (aH|_fold=0.975 M_KK; k̂=1/ξ_KZ=53.30 M_KK), 165×–9023×
  separated from the horizon-exit H̃=5.9076e-3 M_KK. dA_s^{impulse}/dH̃ = 0. The LI H̃-divergence
  is the rate-limiter of the UNIFIED-AS-79 ROUTE's Planck-agreement (the slow-roll functional
  inv-1 diagnosed as misapplied to the impulsive quench), NOT the floor-setter.

FLOOR INEQUALITY  →  A_s ≥ A_s^{BD} (S_IC ≥ 1) PERMANENT, FUNCTIONAL-INDEPENDENT, 3-axis
  (reference-state / families-index η-form / dynamical-Bogoliubov; proven_1097). Only ">1"
  is permanent.

MAGNITUDE-AS-PLANCK-NUMBER  →  OPEN (scheme-dependent). Three defensible functionals span
  1.2590 OOM (impulse +0.864 / UNIFIED +0.196 / Parker +1.455), none in Planck ±5%. A_s JOINS
  the §EVOI.BF route-unstable liability as a magnitude-open observable. Openness localized to
  (b-i) cross-functional spread + (b-ii) the UNIFIED route's a_0/a_2 SDW/Zubarev freedom +
  the separate open exit-greybody filter (S110 AS2-GREYBODY FAIL).
```

### 4B. Falsifier-surface edits — ROUTED to mack-cosmic-bridge (NOT written here)

**[→ mack-cosmic-bridge]** `sessions/framework/registry/falsifier-master-inventory.md` Row #12 (A_s) + the §EVOI.BF A_s liability cell:

1. **Row #12 (A_s magnitude):** keep the canonical `A_s_FW = 1.5367e-08` (impulse-quench, scheme-tagged) but record it as a **floor-amplitude POINT (Reading A, magnitude-origin = TD impulse-quench source, H̃-independent)**, with the magnitude-as-Planck-number carrying the `SCHEME-DEPENDENT` tag and the **1.26-OOM cross-functional spread** (impulse +0.864 / UNIFIED-AS-79 +0.196 / Parker +1.455 OOM, none in Planck ±5%) as the open-magnitude width. Add the **FUNCTIONAL-INDEPENDENT/PERMANENT floor sub-annotation** (3 orthogonal axes) per the WS-AS-1 HK-AS-FLOOR split.
2. **§EVOI.BF A_s liability:** sharpen "A_s route-unstable / >3 OOM, no convergence" by **re-localizing** it: the >3-OOM is the **cross-functional + exit-filter** openness (Object 2), NOT a within-impulse-quench instability and NOT the H̃-divergence-as-floor-setter. The **floor-amplitude POINT is set** (Object 1, TD source, H̃-independent); the **floor inequality is permanent** (3 axes). A_s remains a magnitude-open observational liability **at the magnitude-as-number level only**, with the H̃-divergence scoped to the UNIFIED route (one contributor to b-i), not the floor.
3. **Capstone-hygiene cross-check (Q3):** if the capstone or atlas-08 CF23 narrates the A_s magnitude at "permanent" confidence, the magnitude down-tags to SCHEME-DEPENDENT-OPEN while the floor inequality `>1` stays permanent — substrate-first arrow `D_K → {α_k,β_k} → n_k → S_IC → A_s` preserved (designated-writer reviewed patch, not bulk append).

### 4C. No new pre-registrable Planck-comparison gate

The dispatch asked for *either* a pre-registrable A_s magnitude gate (against Planck A_s=2.1e-9) IF a leg sets the scale, OR a floor-permanent/magnitude-open status statement IF route-unstable. The verdict's answer is **the status statement (4A/4B), NOT a new Planck-comparison gate** — because while a leg (TD) DOES set the floor-amplitude POINT, that POINT is +0.864 OOM from Planck and the magnitude-as-Planck-number is genuinely route-unstable across functionals (none in-band). A pre-registered "A_s vs Planck within X" gate would be a false-precision exercise: there is no scheme-independent number to test, and the per-functional points are all out of Planck's band by construction. The defensible forward object is the **typed floor-amplitude POINT pin (already canonical: `A_s_FW = 1.5367e-08`, S111 AS3a)** plus the **open-magnitude liability annotation** — both routed to mack above.

---

## 5. Residual dissent + the decisive compute

### Residual dissent

**Minimal, and definitional rather than physical.** Both poles agree on every number and on the two-object split. The only residue is *emphasis*:
- Reading A would headline "the floor IS set (by the TD source); the magnitude carries a scheme-tag" — foregrounding Object 1.
- Reading B would headline "the magnitude is OPEN (1.26 OOM, none in-band); the floor inequality is permanent" — foregrounding Object 2.

The SYNTHESIS verdict pins BOTH objects explicitly, which dissolves the emphasis-dissent: there is no surviving claim that "H̃ sets the floor" (Reading B retracted it) and no surviving claim that "the magnitude is a clean Planck-comparison prediction" (Reading A retracted it). **No live ledger-dissonance remains** — the workshop produced a pinned position both poles endorse.

### The decisive compute (if Object 2's openness is to be narrowed in a future session)

The floor-origin (Object 1) is **settled** and needs no further compute — both poles verified it from substrate data. What remains genuinely open is Object 2's magnitude-as-number, and ONE compute would test whether the cross-functional spread (b-i) collapses or whether the openness is structural:

```
CF-AS-FUNCTIONAL-SELECTION — Is there a substrate-canonical selection that pins ONE spectral
                             functional for the post-transit A_s amplitude?  EVOI: MEDIUM.
  What:    Test whether the substrate's own structure selects a UNIQUE spectral functional for
           the produced-relic amplitude (impulse-quench vs UNIFIED-AS-79 vs Parker-adiabatic),
           OR whether functional-choice is a genuine physical d.o.f. (lizzi-signature) with no
           canonical selector. This is the CV-1↔CV-4 cross-link: the A_s magnitude is scheme-
           dependent for the SAME reason the a_0/a_2 CC ratio is (the spectral functional as an
           unpinned d.o.f.). The decisive sub-test: does aH|_fold = 0.975 M_KK carry the a_0/a_2
           SDW/Zubarev split (→ openness universal, the impulse-quench scale inherits it too), or
           is it transit-trajectory-fixed (→ openness confined to the UNIFIED route, the verdict's
           Object-1 finding holds with margin)?  [The transit-r2 §(ii) crux, now the residual one.]
  Inputs:  s100b_box_delta_bogoliubov.npz (the |β_k̂|² spectrum + aH|_fold barrier); the SDW vs
           Zubarev a_0/a_2 readings (S82 Obs 6.3, 181× Path-B split); inv5/inv6/UNIFIED A_s
           values; the s64-clock H_fold=586.527 provenance (does aH|_fold derive from a_0/a_2 or
           from the fold-passage kinematics?); canonical xi_KZ_FW, A_s_FW.
  Gate:    PASS (functional SELECTED) iff a substrate-canonical argument pins ONE functional for
           the A_s amplitude (collapsing the 1.26-OOM spread to one value + scheme-tag retired);
           FAIL (functional-pluralism PERMANENT) iff no canonical selector exists and the magnitude
           is a genuine physical d.o.f. (the lizzi-signature outcome — magnitude permanently
           scheme-dependent, A_s magnitude-open is a structural feature not a gap);
           INFO iff aH|_fold's a_0/a_2-dependence is partial / the selection is regime-conditional.
  Effort:  ~1 agent-session (transit-dynamics × lizzi; the inputs are on disk).
  Note:    This is NOT a Planck-comparison gate (no scheme-independent number exists to test); it is
           a functional-SELECTION test. A FAIL is informative — it would PIN "A_s magnitude is
           permanently functional-pluralist," converting the §EVOI.BF liability from "open, may
           converge" to "open, structurally — the magnitude is a physical d.o.f. like the CC ratio."
```

This compute is the only forward object that could change the state of knowledge on Object 2. It does NOT touch Object 1 (floor-origin, settled) or the floor inequality (permanent, 3 axes). Both poles pre-registered the underlying crux (transit-r2 §(ii) / lizzi-r2 §5); it is recorded here as the single decisive compute the workshop leaves behind.

---

## 6. Closing line

The A_s **floor-amplitude POINT is set by the TD impulse-quench source** — fixed by fold/UV-epoch Bogoliubov scattering data, H̃-independent at the scale-origin (both poles verified, 165×–9023× scale-separated from the horizon-exit H̃) — and the **floor inequality stands permanent on three axes**. The **magnitude-as-a-Planck-comparison-number is OPEN**: three defensible functionals span 1.26 OOM with none in Planck's band, so A_s **joins the §EVOI.BF route-unstable liability as a magnitude-open observable** while its floor stands. The LI H̃-divergence is the rate-limiter of the **UNIFIED-AS-79 route** (the diagnosed-wrong slow-roll functional), not the floor-setter — correctly scoped, not retired. The verdict is **SYNTHESIS**: a clean two-object split that both poles converged to, with the floor-origin conceded to Reading A on the merits and the magnitude-openness retained as Reading B's correctly-scoped residual. Falsifier-surface edits (Row #12, §EVOI.BF) routed to mack-cosmic-bridge.
