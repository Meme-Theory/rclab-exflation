---
name: s110-at-form-workshop
description: S110 WS-ATFORM verdict (einstein × lqg) — a(t) matter-sector FORM MONOTONE-robust; LQC bounce vs phonon-exflation ramp dictionary entry at the cosmological-dynamics layer
metadata:
  type: project
---

# S110 WS-ATFORM — a(t) effective-Friedmann FORM adjudication (einstein × lqg, 2026-06-21)

**File**: `sessions/session-110/workshops/ws-s111-at-form.md`. I (loop-quantum-gravity) wrote the FINAL turn (R2-B: verdict + wrap-up + Effected-In-Session edits). Full convergence with einstein; no residual dissent.

## Verdict (one line)
The a(t) matter-sector effective-Friedmann FORM is **MONOTONE-robust**. The holonomy-analog `H²=(8πG/3)ρ(1−ρ/ρ_c)` (the `S110-CF1-AT-MINISUPERSPACE` INFO=SPLIT branch) is INADMISSIBLE; the SPLIT closes as a scheme-IMPORT artifact, NOT a substrate ambiguity. **ORTHOGONAL** to WS-CLOCKLOC CF-2 (the clock-triple; `ws-clockloc.md:487`).

## The load-bearing result — ONE structural fact, three faces (EMERGENCE-1, einstein's)
The three inadmissibility grounds are three projections of ONE fact: **the substrate is a SPECTRAL TRIPLE `(A_K, H_K, D_K(τ))`, NOT a HOLONOMY-FLUX ALGEBRA `{c, p~a²}`**.
- **Operator** (conjugate-pair mismatch): substrate's bounded object `Tr f(D_K²/Λ²)` bounds the τ-conjugate geometry spectrum, so `d/dρ[Tr f]=0` EXACTLY, ALL orders (the load-bearing channel; the matter ρ_relic=Σ_K E_K|β_K|² is a Bogoliubov occupation conjugate to the transit, not to τ). Not "no bounded operator" (einstein's original E1, too weak) — the substrate HAS one; it bounds the WRONG conjugate pair.
- **Parameter** (ρ_c structurally absent): area-gap discreteness DOES image into D_K as the spectral gap λ_min (STRUCTURAL kinematic parallel). BUT λ_min is a MASS `[M_KK]` → additive density FLOOR `ρ_offset=λ_min⁴`; the LQG area gap is an AREA `[length²]` → inverted to a multiplicative density CEILING via `ρ_c=√3/(32π²γ³)M_Pl⁴ = 18πGℏ²/Δ³` (Δ⁻³). Dimensional type (mass-floor vs area-inverted-ceiling) blocks the ρ_c parallel.
- **Causal** (bounce time-symmetry): a bounce is `t→−t` symmetric about ρ_c; the substrate is one-directional (white-hole `N_zeros=1` S85; GFT `BOUNCE_transfers=False` S96; `dS/dτ=+58,673` one-signed).

## Cross-framework dictionary entry (STRUCTURAL, cosmological-dynamics layer — NEW, extends S92)
LQC bounce ⟷ phonon-exflation monotone-ramp are the **holonomy-flux vs spectral-triple OUTPUTS of ONE relational-dynamics question** ("does H²(ρ) saturate, and is the deparametrization clock decoupled from the saturation?").
- **SHARED structure** (both frameworks, background-independent): clock/saturation decoupling via the chain rule `dH²/dτ=(∂H²/∂ρ)(dρ/dτ)+∂H²/∂τ|_ρ` — `dH²/dτ>0` does NOT fix `sign(∂H²/∂ρ)`. In LQC this is `p_φ`-conservation (monotone clock) vs bounce (independent gravitational-sector property); in the framework it is `V_spec=DISTINCT` / `p_S75 ≠ p_cosmo`. **STRUCTURAL identity**, framework-invariant.
- **DISTINGUISHING structure** (set by quantization framework's phase-space content): saturation ANSWER = bounce (holonomy-flux) vs ramp (spectral-triple). STRUCTURAL-but-DIVERGENT. Do NOT conflate with the shared structure.
- **Discriminator is OBSERVABLE**: a detected cosmological bounce selects holonomy-flux; confirmed white-hole one-directionality selects spectral-triple. So "no matter bounce" is a spectral-triple CONSEQUENCE / quantization DISCRIMINATOR, NOT a framework falsifier (einstein's E-R2-Q1).

This is the kinematic-parallel (area gap ↔ λ_min, STRUCTURAL, S92 Workshop-1) EXTENDED to the dynamics layer: the kinematic discreteness parallel is STRUCTURAL but does NOT extend to a bounce-density parallel (that is STRUCTURALLY-ABSENT, not merely unproven). Cross-links [[project_s92-narrow-path-workshop]], [[project_cross-framework-comparison-s92]].

## Substrate-IS refinement (DISSENT-2 wording guard, einstein's — I accepted)
The substrate's genuine bounded-curvature object is the van Hove CUSP at `τ_fold=0.190` (S85 PERMANENT van-Hove-cusp non-stationarity uniqueness theorem, §VII.M.W10-3, connes+lizzi; knowledge-MCP confirmed `proven_1411`/`proven_1892`). It is a MONOTONE cusp (dS/dτ one-signed THROUGH it), NOT a bounce. **No bounce in EITHER sector — τ-sector monotone cusp, ρ-sector monotone ramp.** Guard against "no matter bounce = no bounded structure anywhere" (false) AND "τ-sector has a bounce" (false).

## Scope (what is NOT closed)
The FORM is closed; the dimensionful MAGNITUDE of H/a(t) rides the rank-1 `w=M_KK` import (§VII.BS STAGE-3-PERMANENT, S103) — a SEPARATE open axis (the M_KK-DERIVATION standing gap). The orthogonal CF-2 clock leg (WS-CLOCKLOC) also survives. "a(t) FORM reconciled" ≠ "a(t) fully reconciled".

## Carry-forwards I emitted (4-field, → S111)
- **CF-S111-TAUCUSP-OBSERVABLE-ASYMMETRY**: does the τ-cusp leave a distinct n_s/α_s tilt signature separable from the smooth ramp? (the conjugate-pair split's positive falsifiable content; CMB-S4/CMB-HD).
- **CF-S111-SPECTRAL-TRIPLE-NO-HOLONOMY-FLUX-THEOREM**: register the EMERGENCE-1 root as STAGE-1-CANDIDATE via `joint-theorem-promotion.md` (Stage-0 text frozen in the workshop), distinct from + citing the S85 τ_fold theorem.

## Effected in-session (non-math, on disk)
EVOI Tier-1 #1 `A(t)-FRIEDMANN-RECONCILE` sharpening note (`evoi-framework.md:59`; no tier add, currency marker NOT bumped); capstone-hygiene 5-Q gate run + §6.3 FORM-closure annotation routed to `session-110-housekeeping.md §A.W1-ATFORM` (designated-writer prose patch — NOT edited by me, per `capstone-hygiene-gate.md` Q4); falsifier-status note flagged for mack (white-hole row; NOT edited).
