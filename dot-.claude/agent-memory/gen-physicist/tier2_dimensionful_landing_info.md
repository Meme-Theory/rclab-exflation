---
name: tier2-dimensionful-landing-info
description: Cross-pillar-bridge registry landing where the plan-pre-registered Level-3 is a DIMENSIONFUL magnitude vs a DIMENSIONLESS L^-α envelope → INFO (Tier-2-dimensionful HELD), not FAIL; the dimensionless re-anchor is documented as the PASS-eligibility pathway but NEVER swapped into the pre-registered Level-3 (comparator-discipline).
metadata:
  type: feedback
---

When landing a per-Bulletin-per-pole cross-pillar-bridge §VII entry whose plan pre-registers **Level-3 = a substrate-IS residue/spectral-moment MAGNITUDE** (dimensionful, e.g. M_KK²) and **Level-2 = the `L^{-α}` convergence envelope** (dimensionless rate), the literal `Level-3 < Level-2` central-value comparison is **dimensionally inconsistent** (magnitude vs rate). Do NOT read the FALSE inequality as FAIL.

**Rule**: the verdict is **INFO** — STRUCTURE complete (verify_section_matches + all 5 anatomy + 3 levels + poleconv-DUAL + weighting-family + bridge-NAMED) ⇒ registration LANDS; the **Level-3 row is HELD `NOT-SATISFIED-PENDING-substrate-physical-scale-anchor`** (Tier-2-dimensionful) per `cross-pillar-bridge-anatomy.md §"Tier-1/Tier-2 dimensional-re-anchorability gate"`. FAIL is reserved for section-mismatch or a MISSING required declaration (a structural-registration failure), NOT a dimensional-class mismatch on the Level-3 row.

**Why**: a dimensionful magnitude on a divergent/genesis channel is registry-PASS-INELIGIBLE; the Non-Promotion-by-Held-Number differentia is **dimensionful-slot-collision** (a NUMBER held against substrate-natural extraction, NOT sideways-re-pinned to a methodology-floor F-image). The theorem-STRUCTURE holds independently of the Level-3 dimensional-class question.

**How to apply**:
- Three-way verdict in the producing script: FAIL = NOT structure_complete; PASS = structure_complete ∧ literal L3<L2 ∧ NOT tier2_dimensionful; INFO = structure_complete ∧ (literal L3<L2 fails on Tier-2-dimensionful ground).
- **Comparator-discipline (load-bearing)**: COMPUTE the dimensionless Tier-1 re-anchor (the certificate's truncation match-error, e.g. `peel_heldout`; gives match/env ≪ 1, the §VII.W calibration pattern) and DOCUMENT it as the PASS-eligibility PATHWAY — but do NOT swap it into the pre-registered Level-3. Swapping-to-yield-PASS is `v3-closure-recovery.md` PROHIBITED Class 1 (convention-shopping) / Class 3 (post-hoc pre-registration editing). The HELD status converts to PASS only when a forward gate PRE-REGISTERS the dimensionless (or substrate-physical-scale) Level-3.
- The plan's own INFO_meaning usually already names this ("Tier-2-dimensionful divergent-channel magnitude HELD ... while the structure otherwise holds") — honor the pre-registration, don't re-narrativize to FAIL or force-PASS.
- Carry-forward the Tier-1-reanchor promotion gate (DIMENSIONLESS Level-3 + Stage-2 PASS-AND) as genuine future compute (4-field spec), not a defect.

S102 W2-3 §VII.BT (s=7 LC genesis pole-tower): L3=|a_2^Mellin(LC)|=0.0126 M_KK² vs L2=1.04e-5 dimensionless → INFO/HELD; Tier-1 re-anchor peel_heldout=1.22e-11<L2 (match/env=1.18e-6) documented, not swapped.

Related: [[register_sourced_gate_machinery_recovery]] (binding-text discipline: consume the SHA-pinned certificate, re-derive NOTHING), [[verbatim_extraction_registry_landing]] (AFTER-pattern single-shot, next-free-LETTER scan, byte-restore on script-bug).
