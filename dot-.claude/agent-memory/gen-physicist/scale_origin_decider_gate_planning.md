---
name: scale-origin-decider-gate-planning
description: Planning a "scale-origin decider" compute wave — a workshop SYNTHESIS Reading-A-vs-B fork that reduces to ONE compute, where the load-bearing discipline is an anti-injection / OPEN-output foreclosure pinned so the gate cannot be ansatz-forced toward the expected answer.
metadata:
  type: project
---

A **scale-origin decider** gate closes a prior workshop's SYNTHESIS Reading-A-vs-Reading-B fork (carried as a "current-evidence default") with exactly ONE pre-registered compute. The recurring planning move across these (S114 W2 KPIVOT / TAUFOLD / CCRESID): the fork is decided by a quantity the workshop left UNCOMPUTED, and the LOAD-BEARING plan discipline is a **foreclosure that keeps the gate from importing the expected value** — pinned in BOTH `machinery_pin_map` (a named status pin) AND `audit_discriminators` (a FORBIDDEN-in-script/pinmap note), so a script reaching the expected answer by injection is detectable as a script-content violation.

**Why:** each fork has a "structurally-expected" outcome (both workshop poles' strong prior). A gate that hard-codes/seeds that expectation is iterate-to-match (PROHIBITED Class-6-adjacent, `v3-closure-recovery.md`). The pre-registration must make the expected value reachable ONLY as a post-hoc comparison target, never a finder seed/import.

**How to apply** — three instantiations, all S114 W2 (verdicts in `sessions/session-113/workshops/ws-s113-{1,2,3}-*-verdict.md`):
- **OPEN-output degree (KPIVOT)**: a transport degree is the factorization OUTPUT, NOT the pre-imported canonical (`deg_T_BZ_pivot=2.0` is the α_s/d_s degree; importing onto a structurally-distinct ratio is dedup-flag-iii per `cross-pillar-bridge-anatomy.md §23.0(5)`). Pin `transport_degree_status: OPEN`, read the prior extraction script for METHOD only (its value NOT in `audit_sha256` consumption), add a parity pre-flight (`d_A=0` ⇒ EVEN-morphism sector required; ODD = scale-leg contamination = FAIL). `[SIGN]` trigger.
- **No-injected-target (TAUFOLD)**: a from-scratch finder on a grid that merely BRACKETS the region; the canonical value (0.190) is NOT a seed/target. Pin `no_injected_target`; the prior PASS that "imported" the value (S85-W10 `value='promoted'`, `convention=canonical_constants-S85-freeze`) is the anti-pattern this gate must not repeat. Decide on the SATURATION-LIMIT + MONOTONE-TREND across L_max (NOT a mixed-L PASS/FAIL tally — coarse-L drift is expected). Distinguish the target observable (cusp-CROSSING, flank dS/dτ≠0) from a sibling (DOS-PEAK 0.221) — different functionals of the same ρ; conflation = single-observable-per-triple violation.
- **RATIO-on-fraction + OOM-fork (CCRESID)**: tolerance is a RATIO on a small fraction (`|frac−0.032|/0.032 ≤ 0.156`, Sage-exact 0.15625 = 0.005/0.032) PLUS an OOM run-down fork (≥100 PASS-A / <10 fold-frozen / [10,100) INFO). The run-down target (118.71 OOM) and today-needed value (1.78e-46 GeV⁴) are analytically derived (Sage RealField(200) at plan-freeze, not post-hoc). a₀-channel ⇒ `regulator_pin: a_0^{Mellin}`. `[CHAIN]` trigger. Anchor `χ_q(fold)=300,338 M_KK⁴` is a SESSION-source pin (S43), NOT in canonical_constants.py — cite S43, never hardcode a placeholder; `update_constant` is a single-call FIX-IN-SESSION if promotion warranted.

**Validator path**: `_yaml_gate_validator.py <plan.md>` (absolute paths, no `cd`) — PASS certifies the 8 PRDR keys non-empty (= `D_PRU_raw=0` cardinality). `_pru_cardinality_audit.py __main__` is only its Class-8.7 self-test, NOT a per-file scan; the cardinality check rides the validator. All three carry `schema_v2_3tuple_required: true` (any [SIGN]/directional gate). See [[plan_authoring_r3_yaml]], [[wp_shell_generation]].
