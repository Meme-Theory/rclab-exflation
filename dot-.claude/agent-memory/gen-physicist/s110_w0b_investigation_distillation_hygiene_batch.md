---
name: s110-w0b-investigation-distillation-hygiene-batch
description: Multi-patch designated-writer hygiene batch (registry/atlas/open-channel) sourced from an investigation-distillation triage — the boundary calls (sole-writer / already-current / verified-absent / investigation-track-not-promoted) that recur in W0 housekeeping waves
metadata:
  type: project
---

# Investigation-distillation hygiene batch (S110 W0b, 12 HK-* patches)

A W0 METHODOLOGY-class housekeeping wave: apply N fully-specified `HK-*` reviewed designated-writer patches from `sessions/investigation/_promotion-triage.md` Bucket-3 §A across the registry + atlas-04/08/09 + open-channel-ledger. NO verdict line (not compute gates), NO new physics. The recurring discipline is the BOUNDARY adjudication per patch, not the prose.

**Why:** these batches mix four patch-outcomes that look the same in the triage table but route differently on disk; getting the boundary wrong either pads (duplicate annotation) or violates a sole-writer fence.

**How to apply (the boundary decision tree, in order):**
1. **Sole-writer fence FIRST.** Before editing a §VII slot, grep its header for `mack-cosmic-bridge sole-writer`. The §VII.AV CC-bridge sub-slots, §VII.CB, and any §7-falsifier-surface row are mack's — a designated-writer (gen-physicist) hygiene patch must NOT edit inside them. Land the consolidation in the ADJACENT non-mack canonical home instead (e.g. CC scheme-dependence → `§VII-B.ZETA-NOT-PHYSICAL-75`, the Lizzi-track theorem, NOT mack's §VII.AV). The capstone §7 surface is likewise mack — FLAG, never edit; record "verified-absent" if the claim isn't there.
2. **already-current (no-padding).** The orchestrator often lands part of an `HK-*` item BEFORE the W0b-shared dispatch (task #N "completed" list). Re-read the target slot; if the annotation the triage asks for is ALREADY present (e.g. §VII.CA HK-VIICA berry-routing sharpening pointer was already at the entry's tail via task #20), record "verified already-current" — do NOT force a duplicate (`feedback_fix-in-session-never-defer.md` no-padding).
3. **verified-absent → add-as-new.** Several items say "if not in X/capstone, record verified-absent and add to the open-channel-ledger as a new entry." HK-BORN: the Born claim was ONLY in the raw `open_channels` FTS scrape (not a curated ledger, not the capstone) — so the down-tag lands as a NEW curated §A row that explicitly SUPERSEDES the scrape row.
4. **investigation-track NUMBER ≠ register FACT.** `gate-verdicts.md §"Investigation-Track"`: inv-N findings enter the permanent index only on session-promotion. So cite inv-N magnitudes (CC split −0.499/+2.320, a₄-Weyl 115.76 OOM, Connes-ladder 12.56, R_1=1.1286546) as investigation-track-SOURCED annotation, NEVER as new canonical pins. "scope, not status" / "register status UNCHANGED" triage rows mean: keep the status tag, add only the scope annotation, prose-tag == register-tag.

**Convention moves that recurred:**
- Out-of-frozen-block annotation per the §VII.BP BINDING-AMENDMENT precedent: a frozen Stage-0 blockquote is byte-IMMUNE; new scope annotations go in a SEPARATE block AFTER it, never editing the immune span (HK-SCOPED-VIIBS on §VII.BS, whose theorem-tag span SHA `e669ccd2…` is frozen).
- "cross-ref pointer ONLY — do NOT mint a §VII slot": HK-GEOM-WALL (atlas-05 W22) + HK-FIRD (fi-rd-manifest.md) land as POINTER rows citing the investigation-track home, because the underlying observable isn't session-promoted.
- atlas-09 retraction rows split by domain: mack sub-table for CC-magnitude/acoustic-falsifier rows (Items 51-52); a SEPARATE designated-writer sub-section for substrate-dynamics retirements (Item 53 HK-CHAOTIC scrambling-corridor — NOT a §7 row). Update BOTH the master-table total AND the header counts.
- Two-leg status split (HK-N37): N7-(i) algebraic-singleton UNCONDITIONAL vs N7-(ii) spectral-triple-for-D_total CONDITIONAL-on-LBA-5 (the axiom-5 orientability fail at 4.000 = the M₃(ℂ) sector). An ATOMIC 4-facet item (atlas-04 N3+N7, atlas-08 Q10+Q9, registry §VII.W-3) — apply ALL together, scope the existing "RESCUED" tag to the unconditional leg only, register the discharge-lemma (LBA-5) as PROMOTED-UNDISCHARGED pointing at the not-yet-run workshop (WS-M1-INTERTWINER, S110 W1).

**Completion check that caught nothing-missing:** grep each edited anchor phrase in its target file (counts: atlas-04=2, atlas-08=2, registry=8 distinct, atlas-09=4, ledger=2) — designated-writer prose patches have no SHA-uniqueness/verdict-line discipline, so the artifact-existence grep IS the closure.
