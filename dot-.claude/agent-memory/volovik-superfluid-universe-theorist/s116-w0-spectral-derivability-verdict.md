# S116 W-5 "w0 spectral-derivability" — converged verdict (baptista × volovik, 3 rounds, R3-FINAL-CLOSED 2026-06-28)

**Workshop**: `sessions/session-116/workshops/s116-w0-spectral-derivability.md`. I was Agent B (volovik), final turn.

## PINNED VERDICT
`w0 = -0.918` does NOT join {n_s, m_H, r} as spectral-action-derived. It is **Volovik-partition closed-form-ONLY** = a **Level-1 closed-form THERMODYNAMIC identity** (`w0_FW`; canonical `get_constant` → -0.918, **Gate: None**, S58 four-fold-lock = Volovik vacuum partition + effacement Γ_eff=0.99970, Superseded=False). The branch-iv (S102→S116) corridor as a w0-DERIVATION is **CLOSED-WITH-RESULT**. DESI DR3 (~2027) falsifier rests on the closed form (CLEAN stake per BD1; deg-conditional σ per BD2).

## Why branch-iv cannot DERIVE w0 (the decisive structural facts; both agents converged)
1. **Parallel-translate identity** (Sage QQ-exact): `offset_A − offset_B = w0_FW − w0_B = −0.075546` for EVERY L; the moment `ρ_B(L)` cancels. All three readings (−0.918 / −0.842454 / −1.340827) are ONE Zubarev moment `ρ_B = mean_Z/λ_max − 1` re-anchored to three offset-TARGETS. The value lives entirely in the offset (= a closed form); the moment is a value-neutral carrier. "The branch is in the OFFSET, not the moment" ⇒ Reading B.
2. **Admissibility ⟺ injection** (regulator-convention-lockdown demarcation theorem): admissible ≡ `w0^C(10) = w0_FW` exactly ≡ `offset = w0_FW − ρ(L*)` injecting w0_FW. EVERY admissible normalization injects; the one inadmissible one (RDC, no offset) outputs the moment's own value −1. Three-axis table (bare ρ(10)): running-edge −0.575 (→ −1 Weyl-forced), fixed-edge −0.575 (frozen), no-edge +0.987 (frozen) — NONE is −0.918; each reaches it only via the injected offset. **A construction that contains its target as an input cannot derive that target.** ⇒ branch-iv is derivation-NEUTRAL by the structure of its own admissibility class.
3. **The spread is value-NEUTRAL**: `spread_CAC = ρ_a − ρ_b` exactly (offset cancels in max−min; W9 residual 1.11e-16) ⇒ bit-identical anchored to −0.918, −0.842454, or −1.341. Registered `surrogate-spread-UNINFORMATIVE` (S101-W0-BRANCH-IV-EVALUATOR INFO `cd0492d6`). A narrowing spread of a sequence → −1 is convergence TO −1, not support FOR −0.918.
4. **The 3-level ladder is DEGENERATE for w0**: Level-3 anchor = Level-1 RE-INJECTED via the offset (definitional identity, not independent confirmation); Level-2 = the value-neutral spread. One value in a spectral costume — unlike n_s (a genuine spectral OUTPUT 0.9561 with no closed-form n_s subtracted-and-re-added).

## Complete closure of "spectral routes to w0" = CONJUNCTION with grade-asymmetry
- **(i) Weyl wall** — any `(FB-frozen IR numerator)/(Weyl-unbounded edge λ_max(L)) − offset → −1`, FORCED by Weyl's law (mean_Z → μ≈1.9879; λ_max → ∞, ∂λ_max/∂L≈0.375). A SPECTRAL THEOREM (machine-rigorous, regulator-invariant). Kills the **edge-NORMALIZED** class. **Theorem-grade / PERMANENT.**
- **(ii) categorical wall** — `w0 ∉ Tr f(D_K)`: the equation-of-STATE is the q-field vacuum partition's effacement response, not a static D_K spectral moment (the static CC MAGNITUDE is the a_0 zeroth moment; the dynamical w(z) RESPONSE is not in the static eigenvalue spread). Witnessed by mean_Z → +1.9879 ≠ −1 (a non-edge-normalized bottom-only statistic the Weyl wall does NOT govern). Kills the **non-edge-normalized** routes. **q-theory model-grade.**
- The Weyl wall alone is **necessary-not-sufficient**; (ii) is a COROLLARY of the SAME Volovik partition that produces −0.918 (closure + value share one source).

## My R3 DISSENT (VD2b sub-grade refinement — the one volovik-domain sharpening I carried into the close)
Wall (ii) is NOT flatly "model-grade." It splits:
- **(ii-a) categorical PLACEMENT** [w0 is the q-partition's effacement EoS response, not a static spectral moment] = as robust as **Volovik's equilibrium theorem** (ground-state energy does not gravitate; CC=0 at equilibrium), which is **LAB-GROUNDED in superfluid 3He-B** (the chemical-potential cancellation) — a universality-class fact, NEARLY theorem-grade, not a framework stipulation.
- **(ii-b) the specific VALUE** Γ_eff=0.99970 = the genuinely model-contingent part = **exactly what DESI tests.**
So baptista's "closure and value stand or fall together on the q-theory program" TIGHTENS to: the categorical closure is nearly as robust as the Weyl wall; only the value's precision is q-partition-contingent, and that precision is the falsifiable content. Feeds `CF-S117-W0-CATEGORICAL-WALL-GRADE` (can (ii-a) upgrade to theorem-grade via the static-a_0 vs dynamical-EoS-response structural separation?).

### → ANSWERED S117 W7-2 (volovik, INFO, 2026-06-28): YES for the PLACEMENT. VD2b two-grade reading COMPUTE-CONFIRMED.
`∂w/∂a₀ = 0` EXACT and **partition-INDEPENDENT** — confirmed (a) symbolically for ARBITRARY `F_dyn` [the dynamical EoS response `w_dyn = δp/δρ = −F_dyn'/(q F_dyn'')` is a ratio of q-DERIVATIVES ⇒ annihilates the additive a₀; holds for the entire function-space], and (b) across 5 effacement-partition members [`w_eff = −Γ/(Γ+(1−Γ)r)` is manifestly a₀-FREE; value sweeps −0.870→−0.971, Γ_eff=0.99970 → **−0.918 = w0_FW EXACT**, `r_eff = 409877/1377` QQ-exact]. CONTRAST (the bare-CC sensitivity w0 is NOT): the static ratio `∂w_static/∂a₀ = −q F_dyn'/(qF'−F)² ≠ 0`. Lab-grounding for theorem-grade: **Paper 04 §III** energy-reference shift-invariance `H−μN` (µ→µ+α absorbs α), "irrespective of details" (3He AND 4He) = the partition-independence; **Paper 13 Eq.4/12** self-tuning `ρ_vac(q₀)=0 ∀ a₀` (q₀(a₀)=±√(2a₀+k)/√k; the static magnitude does not gravitate). **VERDICT = INFO** (wall (ii) is a TWO-grade wall): **(ii-a) PLACEMENT = THEOREM-grade** (`∂w/∂a₀=0` partition-indep, lab-grounded) ∧ **(ii-b) Γ_eff value = model-contingent** (DESI-testable). OPERATOR-DISTINCT from S116 W-4: this gate's q-derivative annihilates additive-in-FREE-ENERGY; W-4's `d²/d(ln K)²` leaves additive-in-TRACE SURVIVING — different operators, NOT in tension (W-4 additive-survival NOT cited against this gate). `audit_sha256=e7fd9d20da481e39…`. Script `computations/session-117/s117_w0_categorical_wall_grade.py`; WP `sessions/session-117/session-117-w7-workingpaper.md §W7-2`. ⇒ the §5 EVOI categorical-wall register records (ii) as a TWO-grade wall; the CLEAN DESI stake hardens (placement leg theorem-grade alongside the Weyl leg (i); only the −0.918 magnitude column stays model-grade).

## Three legs jointly exhaustive on the VALUE (spawn question 3; volovik EMERGENCE 1)
transport (deg=0, no BZ→pivot morphism) ∧ normalization (value-neutral / Weyl-forced −1) ∧ observable-identity (w0 ∉ Tr f(D_K)). Mutually orthogonal, jointly close every spectral-action route ⇒ Level-1-only is the COMPLETE picture, not a stopgap.

## CLEAN-stake verdict (BD1, symmetric bracket — accepted)
Neither over-elevated to topological-invariant rigidity (VD1) NOR under-valued as compromised (BD1). **Truncation axis: STRONGER** than a spectral derivation (`truncation_error(w0) ≡ 0`; n_s/m_H are convergent-but-truncated, FB-bounded). **Fundamentality axis: EQUAL** (substrate-first: the fabric IS both its D_K vibrational spectrum AND its q-field vacuum partition; thermodynamic facet = vibrational facet, two facets of one substrate). "Compromised" would require a smuggled fit or convention-shop — w0 has neither (Gate: None). The stake is CLEAN.

## deg three-status split (BD2 — accepted)
(i) derivation grade (closed-form vs spectral): deg-ROBUST — freeze now. (ii) falsifiability (substrate −0.918 IS testable): deg-ROBUST — substrate_value is the transport INPUT, never its output. (iii) numerical DR3 σ-distances: deg-CONDITIONAL — computed under the PROVISIONAL connes §23 deg=0; if deg≠0 surfaces they re-compute vs the pivot image (substrate prediction unchanged). w0 is d_A=0 ⇒ trivial M_KK⁰ scale leg, transport carried entirely by the dimensionless morphism (T2-VACUOUS scalar, provisional). σ-metric discipline: 2.13σ is vs DESI DR2 (−0.803±0.054); 3.28σ is vs LCDM (−1) — do NOT conflate.

### → RESOLVED S117 W7-1 (volovik, PASS, 2026-06-28): deg=0 now CONFIRMED substrate-derived (NO LONGER the provisional connes-§23 deg=0). (iii) deg-CONDITIONAL element is now FROZEN.
`deg(T_{BZ→pivot})[w0] = 0` (T2-VACUOUS scalar) extracted on the de-λ_max'd representative. **Two-stage**: STAGE-1 the de-λ_max'd {running/fixed/no-edge} representative types the W9 gap; STAGE-2 the §23 degree. Degree chain: d_A(w0)=0 (a₀/a₂ EoS ratio is dimensionless) ⇒ §23.0(5) TRIVIAL M_KK⁰ scale leg ⇒ degree entirely in the EVEN morphism sector; SINGLE pole s=3 (no square/power — the gap is ADDITIVE −0.918−0.422827, NOT a factor) ⇒ Wodzicki −2(3−3)=0 (Sage-QQ exact); Δ_scheme=0 M_KK² across {APS-1975,Cheeger-Simons,Bismut-Cheeger} (a degree-0 secondary class is scheme-INDEP by Wodzicki uniqueness; §18 admissibility leg, mirrors S93-W7-1 delta_scheme=0.00 — Δ_scheme=0 is the ADMISSIBILITY signature for BOTH scalar & non-scalar; the DEGREE value is the discriminator). ⇒ substrate w0 = pivot w0 = −0.918; the W9 → −1.340827 gap is CONFIRMED PROXY-ARTIFACT (gap **100.51% running-λ_max-edge-sourced**, mean_Z-drift term −0.51% OPPOSING; mean_Z FROZEN 0.20% rel, per-shell collapses 2.95e-3→6.24e-6; fixed-edge spread 8.57e-4 & no-edge 2.02e-3 both FLAT ≡ −0.918; de-λ_max removes 99.29% of the drift). **NO §23 K-counter advance** (a deg=0 confirmation ≠ the factorization-EXTRACTED NON-SCALAR degree the K=3 slot reserves for r/α_t). REUSABLE PATTERN (carry forward): a DIMENSIONLESS substrate ratio (d_A=0, like d_s the log-derivative) is transport-INVARIANT ⇒ deg=0 SCALAR; a dimensionful AMPLITUDE P∼σ^{−d/2} carries deg=d/2=+2 NON-SCALAR (the S110-CF-CV6B dichotomy; canonical `deg_T_BZ_pivot=2.0` is the +2 sibling). The {running/fixed/no-edge} three-normalization + EXACT first-order gap-decomposition is the tool to TYPE a running-edge proxy artifact (separate λ_max-running term from mean_Z-drift term). `audit_sha256=bf2698789ec47216…`. Script `computations/session-117/s117_w0_transport_degree.py`; WP §W7-1. ⇒ route to mack: remove the "computed under provisional deg=0; pending CF-S117-W0-TRANSPORT-DEGREE" tag from the §7 falsifier-surface; the DR3 σ-distances (2.13σ vs DR2 −0.803±0.054; 3.28σ vs ΛCDM −1) FREEZE vs −0.918 DIRECTLY.

## Level-1 sub-class taxonomy (register form of VD1 — accepted)
| sub-class | exemplars | exactness | modeling content |
|:--|:--|:--|:--|
| topological/index | KO-dim=6 (mod-8), [J,D_K]=0 (operator id), AZ BDI | integer/Z₂/mod-8, axiom-forced | NONE |
| algebraic | g1/g2=e^{−2τ}, volume-preserving TT | real, algebraically exact in τ | deformation-geometry only |
| **closed-form thermodynamic** | **w0 = −0.918** (Volovik partition + Γ_eff) | real, exact as a thermodynamic derivation | q-field vacuum partition + impedance effacement |

## EVOI Tier-2 7c disposition (3-way split; routed to orchestrator)
- branch-iv-as-w0-DERIVATION → **CLOSED → §5, CLOSED-WITH-RESULT** (the (i)∧(ii) walls; an elimination per `epistemic-discipline.md`).
- branch-iv-as-L_max-STABILITY-diagnostic → survives, value-NEUTRAL (`surrogate-spread-UNINFORMATIVE`); the "extend the envelope" gates (CF-S117-BRANCH-IV-L16, CF-S117-W0-ANCHOR-FIDELITY) are **presentation-normalization hygiene**, NOT derivation gates.
- w0-on-the-DESI-surface → **stays LIVE (Tier-2)**, Level-1 closed-form prediction −0.918. NOT a wholesale retirement of 7c.

## Substrate-first closing
The fabric IS its q-field vacuum partition; w0 IS what that partition's effacement is; the DESI detector measures the emergent equation-of-state image of it. The branch-iv −1 was never a failed search for a hidden spectral derivation — it was the **substrate REFUSING the relocation** of a thermodynamic observable into the vibrational spectrum.

## Routings effected (this workshop close)
- `routed-to-orchestrator(EVOI)`: 7c line-177 → CLOSED-WITH-RESULT, retire derivation-track to §5 (SendMessage to main).
- `routed-to-mack`: DESI DR3 §7 falsifier-surface provenance note (CLEAN stake; deg-conditional σ; branch-iv −0.842454 = fenced reversibility-trigger alternate) + falsifier-master-inventory row (SendMessage to main).
- W9 WP overclaim annotation: `session-116-w9-workingpaper.md` "What strains" — corrected "the prediction's L_max-envelope" (value-neutral, not w0's envelope).
