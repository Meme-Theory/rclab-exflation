---
name: s114-taufold-cusp-crossing
description: S114 W2-2 from-scratch van Hove cusp-CROSSING location (0.19104, L-invariant) vs DOS-peak (0.221, L-sensitive) — distinct functionals; INFO region-selected
metadata:
  type: project
---

**CF-S114-TAUFOLD-CUSP-CROSSING (S114 W2-2, INFO, [SIGN] sign=PASS/mag=INFO/regime=VALID, audit_sha256=7b637db142d9bea7…)**: from-scratch location of the van Hove cusp-CROSSING with NO injected 0.190 (τ-grid brackets [0.18,0.23]; argmin band-edge anticrossing finder returns the value independently).

**The crossing IS the S44/S45 band-edge anticrossing** `Δ_band(τ) = |T5_min − T3_max|`, T3=(0,0)-sector MAX |λ|, T5=(2,0)/(0,2)-sector MIN |λ|. T3_max RISES with τ, T5_min FALLS; they cross at **τ_cross = 0.191038** (gap_min 8.5e-6), reproducing the atlas-07 S45 TRUE crossing 0.19104 to 5sf. The (2,0)/(0,2) conjugate pair is degenerate (identical |λ|) — the gap is against the DISTINCT (0,0) edge, so no spurious conjugate-pair zero.

**Permanent structural content (two distinct functionals of ρ(λ;τ))**:
- **cusp-CROSSING** = band-edge anticrossing (where the monotone S(τ) flow crosses the non-analytic threshold) = **0.19104**, and it is **L_max-INVARIANT** because the T3/T5 sectors have p+q≤2 — they ARE the bottom band at every L_max≥2 (higher sectors have |λ|_min~√C₂ climbing away; (3,0) min=1.248 ≫ the |λ|~0.97 crossing cluster). Sector-locality ⇒ τ_cross identical to float precision across {5,8,10,12}. This is the STRONGEST Friedrich-Bär saturation (saturated by sector-locality, not by L-extrapolation).
- **cusp-PEAK (DOS-peak)** = argmax of singularity STRENGTH `max_E|dρ/dE|` = **0.221** (S85-VAN-HOVE-CUSP-THEOREM FAIL, L_max=8, audit 9786c53949b776f3) and it IS L_max-SENSITIVE (a low-L_dos=4 shape gives 0.182; shifts with truncation).
- The monotone S(τ) (dS_fold=+58672.8>0, empty critical set, S95 NO-WELL-ONE-LOOP) keeps FLOWING through the crossing — the cusp lives in ρ, NOT as a critical point of S. crossing≠peak is a single-observable-per-triple distinction (do NOT conflate).

**Verdict = INFO (region-selected, value-conditional HYBRID)**: |τ_cross − 0.190| = 0.5464% sits JUST outside the ±0.5% PASS band (0.00095). Reading: the cusp-crossing REGION near τ≈0.19 is van-Hove-SELECTED (real substrate band-edge anticrossing; M_KK has no van-Hove analog) but the precise canonical 0.190 is a flank-sub-choice within the substrate-pinned window. Reading-B (location regulator-conditional) REFUTED — location is L-INVARIANT (regulator-robust). Reading-A (full ±0.5% value-selection) NOT reached. truncation_consistent=True (from-scratch T3/T5 at τ=0.19 = L12 master cache `s84_spectrum_cache_L12_tau019.npz`: gap 8.387e-04, |dT3|=0, |dT5|=2.2e-16). mesh-robust (alt-mesh 1e-4 step, re-bracketed around the from-scratch L12 result NOT 0.190: 0.191039).

**The prior S85-W10 PASS IMPORTED 0.190** (`value='promoted'`, convention=canonical_constants-S85-freeze) — this gate is the first from-scratch crossing-LOCATION compute at saturation L, and it does NOT land on 0.190 (lands on the substrate's own 0.191038), the signature the fence held.

**Method/perf note (crash-recovery)**: prior run crashed on a missing-`cache`-arg `band_edge_gap` (helper defined, never wired). Full-spectrum min-gap approach hits a CONJUGATE-SECTOR degeneracy bug ((p,q)/(q,p) give identical |λ| → spurious gap=0 at grid edge) — REJECTED in favor of the specific S44/S45 T3/T5 pair. DOS-peak at full L=8 is INFEASIBLE in one agent run (~290s; (8,0)/(0,8) recursive irrep CONSTRUCTION ~30s each) — cite S85 canonical 0.221 as the contrast, compute only a cheap low-L_dos=4 shape curve (labeled truncation-sensitive). Crossing finder uses only 3 tiny sectors {(0,0),(2,0),(0,2)} → 47s total. Runtime canonical_constants SHA a4b8b679 (≠ plan-pinned 9ee1a113; sibling gate promoted a constant) captured per substrate-first §(ii.B). Related: [[s111_floquet3_dtau_amp_afterglow]] (same fold transit physics).

**S115 W3-1 CONFIRMS-CANNOT-FLIP downstream (S115-S0-KNOB-CROSS-SUBSTITUTION-CONFIRM, [VERIFY] PASS, audit_sha256=c4943ae5f8873c9f…)**: confirmatory clone of `s101_w3_s0_knob.py` routing candidate (iii)'s graded crossing offset at the LOCATED crossing `q_cross=tau_cross_van_hove=0.191038` (this file's value) instead of the canonical flank `tau_fold=19/100`. THREE regulator-free legs, all PASS:
- **LEG A (dev-magnitude)**: `S0_pred_iii^cross = q_cross/T_acoustic = 0.191038/0.112 = 95519/56000 = 1.705696`; `dev[iii]^cross = |1.705696/S0_fit−1| = 0.6814% ≤ 1.00%` (S0_fit=1.6941531566, W3-9 echo dev 0). Candidate (i)=4/3 STILL inside naive 0.01 at the located value (dev_i=0.6539%; the 0.52% (4/3)·0.9≈2π·τ_fold pin-accident is q-INDEPENDENT), so naive count is again degenerate (i+iii).
- **LEG B (selector class-invariance)**: the s101 GRADED selector reads `legC_output_form=='GRADED'` (a CLASS LABEL on the W2-2 npz `s101_envelope_carrier_discriminate.npz`) → eligible class `graded-per-C2-quantum` → (iii) SOLE member → `N_inside(selector)=1`. The selector is INDEPENDENT of the numerical value of q, so re-routing q does NOT change (iii)'s membership. THIS is why the verdict cannot flip.
- **LEG C (exact-rational asymmetry)**: CANONICAL `19/100 / 14/125 = 95/56`, CF `[1,1,2,3,2,2]` max pq=3 (clean small-denom, identity `S0·T_ac=τ_fold` EXACT). LOCATED `191038/112000` reduces (gcd 2) to `95519/56000`, CF `[1,1,2,2,1,1,18,44,4]` first pq≥10 at idx 6 = **18** within 8 terms (Sage-confirmed) ⇒ NO clean small-denominator convergent ⇒ the 95/56 identity has NO analog at the located value.
- **Structural reading**: the `S0=95/56` identity is a FLANK SUB-CHOICE within the van-Hove-selected crossing window, NOT a tuned coincidence; (iii)'s selection is robust to the flank sub-choice because the selector is a regulator-free class-label read. Pre-registered CONFIRMS-CANNOT-FLIP — a non-PASS would have been a script/convention bug, never a W-1 (iii) re-opening. Re-confirms the S114 W-1 (iii) verdict; NO registry change. Method note: the s101-era literal W2-2 pin (463f320…) differs from the live W2-2 SHA — static artifact bytes unchanged, the literal was era-specific head-pin, non-gating (s101 line 369 pattern); the gate audit_sha256 derives from the LIVE runtime pin map.
