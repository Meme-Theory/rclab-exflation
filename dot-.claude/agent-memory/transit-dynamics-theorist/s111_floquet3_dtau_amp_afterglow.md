---
name: s111-floquet3-dtau-amp-afterglow
description: How to derive δτ_amp (hence h_par) from the post-fold modulus afterglow — the two-leg route, the S73B trajectory source, and why the result is regime-marginal (factor 3, INFO not PASS).
metadata:
  type: project
---

S111-CF-FLOQUET3 derived the Mathieu modulation depth `h_par` from first principles (afterglow), upgrading the S101-W1 guard-floor pin from asserted toward substrate-derived. **Verdict INFO** (sign=PASS, magnitude=INFO, regime=MARGINAL): `h_par_derived = 2.759e-4` vs guard-floor `8.3e-4` — factor 3.01 low, correct sign + scale, but not within 10%. NON-verdict-gating: §VII.BP DEAD unaffected (every reading ≪ DTC threshold 14/193).

**The exact reduction (reusable for ANY modulus-afterglow → Mathieu-depth gate):**
`h_par = (dω_k²/dτ)·δτ_amp/ω_{k,0}² = δτ_amp·(d ln E_k²/dτ)` (Eq. F3-1). Match to inv-12 W3-2 normalization `Ω_k²(t)=E_k²[1+h_par cos(ω_q t)]`. Two substrate-derived legs:
- **Leg 1 (δτ_amp)**: post-fold trajectory lives in `computations/session-73/s73b_efold_mapping.npz` keys `t_sol, tau_sol, dtau_sol, H_sol`. τ launches at τ_fold=0.19 with `dτ(0)=v_terminal=26.545` (canonical), overshoots to `τ_max=1.6136` at t=0.090, then runs away UNPHYSICALLY to τ=−99 (the S76-flagged V_eff clamping — do NOT use the post-overshoot tail). Residual ring-down amplitude = `A_launch·exp(−γT)`, γ=3H/2 (Hubble friction), with `H_post_fold=H_sol[0]=0.9754 M_KK` (the PHYSICAL post-fold Friedmann H, NOT the impulsive transit H_fold=586.53). Overdamped: Q=ω_d/(2γ)=0.472. Gives δτ_amp=1.84e-3.
- **Leg 2 (d ln E_k²/dτ)**: rebuild bottom-band spectrum via `dirac_spectrum.collect_spectrum(τ, gens, f_abc, gammas, max_pq_sum=4)` (the SAME module that built the L12 cache) at τ∈{0.186,0.190,0.194}, central FD. Returns `(all_eigenvalues, eval_data)` where all_eigenvalues = list of (complex_ev, mult); `|ev|` is physical |λ|. BdG `E_k=√(λ²+Δ_BCS²)`, Δ_BCS τ-independent ⇒ `dE²/dτ=d(λ²)/dτ`. Relic band median `d ln E²/dτ=0.075`, near-a=1 (λ=0.873) =0.150.

**WHY INFO not PASS (the genuine physics, permanent):** δτ_amp is highly sensitive to the post-fold damping regime, which S73B does NOT pin cleanly (runs away after one swing). H=0.975→overdamped Q=0.47→δτ_amp=1.8e-3→h_par=2.8e-4 (factor 3 low). H=0.396→underdamped Q=1.62→δτ_amp=0.20→h_par=0.03 (37× high). Undamped-SHM upper bound v_terminal/ω_q=13.19. So the afterglow grounds h_par's SCALE and SIGN (~10⁻³ residual ring-down, positive) but the precise 10% value needs a cleaner trajectory. Carry-forward to tighten: re-integrate coupled modulus+Friedmann ODE with a physical late-time V_eff (S66 Volovik-tracking) that settles at τ_fold instead of running to −99.

**S112 RESOLUTION (CF-S112-FLOQUET3-HPAR-TIGHTEN, INFO, sign=PASS/mag=INFO/regime=VALID, audit_sha256=dbb931952ee0fa5d…):** the carry-forward above was EXECUTED. Built the physical Volovik-tracking V_eff(τ)=V_KK(τ)+V_track(τ): V_KK = s52 quad fit (V_KK''=−72.42<0, dV_KK/dτ|_fold=−9.11 — **this negative curvature IS the bare-runaway source**); V_track = restoring term pinned by TWO substrate conditions (NOT the PASS band): (i) dV_eff/dτ|_fold=0 (DILUTION-CC tracking equilibrium) and (ii) V_eff''(τ_fold)=M_mod·ω_q²=116.632·2.012813²=472.52 M_KK⁴ with M_mod=G_mod_full=M_p²·G_DeWitt=116.632 (s52) ⇒ ω_τ=√(V''/M_mod)=2.012813=ω_q EXACT (QA-Q2.3 modulus-freq formula, S75). Coupled ODE τ̈+3Hτ̇+dV_eff/dτ=0, H²=⅓[½τ̇²+V_eff_shift], LSODA rtol1e-9/atol1e-12, launched from τ_fold with v_launch=26.545: overshoots to τ_max=1.298, rings DOWN, **settles** to τ_final=0.184 (NO runaway — the V_eff minimum holds it). **δτ_amp=6.292e-3** read as the late-time settled envelope (½ peak-to-trough over the FINAL ring-down period; launch transient EXCLUDED; robust last-1/2/3/5-period 6.29→7.29e-3 all INFO). h_par=δτ_amp·0.149795=**9.425e-4**, metric=|9.425e-4−8.3e-4|/8.3e-4=**0.1355** ⇒ INFO (just outside 10%). **Corridor narrowed 66.8% low → 13.6% HIGH** (factor 3.42 rise in δτ_amp, predicted direction), regime MARGINAL→VALID (δτ_amp/τ_fold=0.033≪1). **NEW permanent physics**: the late-time envelope decays d(ln amp)/dt=−0.0167, ~90× SLOWER than instantaneous γ=1.46, because **Hubble friction self-extinguishes as the cosmology dilutes** (H²∝ρ→small as the modulus settles) — so the residual ring-down is long-lived (plateaus ~6e-3, not →0), and the relic modes are driven by this slowly-decaying residual, not a transient. h_par now substrate-MOTIVATED (right scale+sign+side) but still asserted (guard-floor 8.3e-4 is an S101-W1 odd-floor pin from a distinct construction, need not coincide bit-for-bit). NON-BLOCKING: h_par=9.42e-4≪DTC 14/193=0.0725 (×77) ⇒ §VII.BP DEAD UNAFFECTED.

Cross-link: [[s101_qeq_relic_oddfloor]] (the guard-floor h_par source), the inv-12 W3-2 Floquet survey (consumer of h_par; §VII.BP DEAD). The inverse cross-check (guard-floor h_par implies δτ_amp=5.5e-3) lands the same order as the S112 settled δτ_amp=6.3e-3 — confirms the residual-ring-down scale (NOT the ~1.5 launch amplitude). The S111 one-period heuristic 1.8e-3 was the UNDERESTIMATE: it truncated at one period, but at Q≈0.47 with self-extinguishing friction the modulus rings many periods.
