---
name: wdw-bc-fork-hh-vilenkin
description: S116-W6 BC-fork — HH no-boundary canonical at WDW constraint layer, Vilenkin = decohered branch (layer assignment); reusable reflecting-datum J=0 Wronskian identity; residual is operator-canonicity not BC
metadata:
  type: project
---

S116-W6-BC-FORK (2026-06-27 workshop; hawking-theorist × me, I steelmanned Vilenkin). The Ψ(τ=0) boundary-condition fork resolved as a LAYER ASSIGNMENT, not a 50/50 SPLIT. Canonical sources are the workshop file `sessions/session-116/workshops/s116-w6-bc-fork.md` + atlas-08 Q12 (pending the §6 synthesis after S116-W6-WDW-IC-REFINE runs) — NOT this memory. What I keep here is the reusable identity + the lesson.

**Reusable structural identity — Eq. H-R3-1 (Sage-verified; KEEP).** On a REAL WDW/minisuperspace operator `[−(1/2G_DeWitt)∂_τ² + (V−E)]Ψ=0` (V, E, G real), a REFLECTING real τ=0 datum (Neumann `∂_τΨ(0)=0` or Dirichlet `Ψ(0)=0`) forces the conserved current `J=Im(Ψ*∂_τΨ)=u v'−v u' ≡ 0` GLOBALLY. Proof: write Ψ=u+iv, both obey the same real ODE `u''=Wu, v''=Wv` (W=2G(V−E)); `∂_τJ = u v''−v u'' = u(Wv)−v(Wu) = 0` (Wronskian conserved, in any s2 oscillatory region too); reflecting datum ⇒ J(0)=0 ⇒ J≡0 ⇒ Ψ real up to a global phase (v=λu) ⇒ HH-parent, no net outgoing flux. **Consequence:** a reflecting-boundary BC is a `|Ψ|²` WEIGHT on a fixed `|B|`, never a current source. This is WHY the e-fold count is BC-invariant: `N_e=N_e_class·(B_traj/B_class)`, bare-WDW `efold_ratio=1.0`, the BC flips `sign(exp(±B))` not `|B|=22.2552` ⇒ `N_e=0.1734` for HH AND the Vilenkin-branch. Extends EFOLD-MAPPING-52's IC-independence to the quantum-cosmological BC layer (a BC is the prototypical IC datum).

**Methodological lesson (KEEP).** A steelmanned BC fork's genuine residual localizes to OPERATOR-canonicity, not the BC itself. Here: s1 monotone-S (all-forbidden ⇒ J≡0 ⇒ HH unconditional) vs s2 holonomy (turning surface `ρ_c≈13.41 M_KK⁴`, `s2_turning_rho=13.4097`, IN-window ⇒ a classically-allowed region exists). My R2 over-read this as "s2 ⇒ J CAN be ≠0"; the sharpening (Eq. H-R3-1) is that the allowed region is NECESSARY-NOT-SUFFICIENT — the τ=0 reflecting ANCHOR still forces J≡0 unless the holonomy operator transmutes the reflecting wall into a transparent symmetric bounce. So the discriminator is OPERATOR-gated. Anchoring outgoing-only at the far edge `ρ_c` while the physical τ=0 wall reflects = answer-shopping (the BC-layer image of iterate-until-PASS) — disavow it.

**Verdict pinned:** canonical BC = Hartle-Hawking (no-boundary, WDW constraint layer); potential = S(τ) (τ=0 S-MINIMUM / regular South Pole, `d²S/dτ²|₀=+304638`); compute convention -HH canonical / -BOTH mandatory BC-invariance diagnostic; expected track B (BC-robust). Vilenkin = the layer label for Ψ_HH's decohered outgoing branch (Layers 1-2); the irreversible cascade is real but EMERGENT (the roll-away of WKB integral curves on `V_eff=−S+const`, τ=0 a V_eff-MAXIMUM), contained in Ψ_HH, not the datum that sets it.

**Compute closed (S116-W6-WDW-IC-REFINE, 2026-06-28):** INFO (composite), Track B confirmed. N_e_BC^HH = N_e_BC^Vil = 0.1734 BIT-EXACT (|ΔN_e|=0, efold_ratio=1.0 both, identical |B|=22.2552); J_HH=J_Vil=0; Abel residual W·(uv−vu)=0 machine-exact; B'(0)=0 ⇒ Neumann reflecting datum AUTOMATIC at the S-min (not imposed by hand). 3-tuple sign=PASS/mag=FAIL/regime=MARGINAL (S70 catches the mag-FAIL → composite INFO, count → TRANSIT-PS-67, NOT a hard BC-layer FAIL); dual_prior 0.90→Track B. convention=DeWitt-supermetric-G5-BOTH; audit_sha256 797b3edf…; artifacts `computations/session-116/s116_w6_wdw_ic_refine.{py,npz,png}`. This NUMERICALLY confirms the Eq. H-R3-1 prediction — no new physics, the prior-favored Track-B re-confirmation.

**Compute-method lesson (KEEP, re-use at CF-S117).** To demonstrate WDW current conservation ∂_τJ=0, do NOT difference a forward-integrated two-solution Wronskian J_W=uv'−vu' over the full window: forward-integrating two independent solutions of Ψ''=WΨ past ~5 e-foldings contaminates the subdominant with the exp(+B)~1e9 growing mode (|B|→22.26 at the fold), so J_W drifts ~1e8 — a NUMERICAL artifact, not a physics failure. Use instead the contamination-free analytic Abel residual `W·(uv−vu)` (≡0 elementwise, machine-exact) PLUS a clean-window (B≤5) J_W drift check. The CF-S117 Stage-2 `J at ρ_c` on the s2 allowed region will face the same issue — there J is genuinely oscillatory (allowed region) so the contamination structure differs, but pin the clean-window/analytic split at plan time.

**Residual carry-forward:** `CF-S117-Q45-TAU0-OPERATOR-CANONICITY` — resolve `S110-CF1-AT-MINISUPERSPACE` `schemes_agree=False` (s1 vs s2; does the τ=0 reflecting datum survive the holonomy operator?); conditional-on-s2 measure `J at ρ_c under -BOTH` (J≡0 = HH-parent lifted; J≠0 = fundamental outgoing).

**Walls reckoned with:** EFOLD-MAPPING-52 (FAIL-structural, N_e=0.1734 IC-independent — HH is its no-boundary-side confirmation); S70 (WKB inapplicable to the Mach-13.75 van-Hove transit ⇒ the e-fold COUNT routes to TRANSIT-PS-67, OFF the BC axis; BC reaches only the adiabatic cap τ<τ_fold). GEOMETRIC Level-2 moduli-deformation result; NO foam-phenomenology / W-FOAM-4 bearing. Master-collab anchors: HT-1 (no bound state on bare well ⇒ half-line geometry, fixes geometry not contour), HT-2 (Neumann/reflecting τ=0), HT-3 (condensate restores a locking minimum at τ₀ with HH RETAINED — HH completed by the condensate, never by Vilenkin). Cross-link [[foam-results-archive]] (INV11-W3 track).
