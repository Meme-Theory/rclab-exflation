---
name: s114-taufold-three-functional-distinction
description: τ_fold near 0.19 carries THREE distinct functionals of ρ(λ;τ) — DOS-peak 0.221, band-edge-crossing 0.191038, rational-anchor 19/100 — non-fungible; S114 W2 workshop verdict (iii) two/three-value convention
metadata:
  type: project
---

# τ_fold is THREE distinct spectral functionals, not one number (S114 W2 transit×lizzi workshop)

**Permanent functional-identity classification.** The canonical `tau_fold ≈ 0.19` region carries THREE STRUCTURALLY DISTINCT functionals of the SAME eigenvalue density `ρ(λ;τ)`. They return three different numbers; conflating any two is the `cross-pillar-bridge-anatomy.md §"Single-observable-per-triple structural filter"` / diffusion-window error.

1. **Φ_DOS** — DOS singularity STRENGTH (argmax of `|dρ/dτ|`-divergence). Value = **0.221** (`S85-VAN-HOVE-CUSP-THEOREM`, L=8, FAIL, audit 9786c53949b776f3). This is the functional the S85 cusp theorem's Steps 1-2 define.
2. **Φ_cross** — band-EDGE gap argmin `argmin_τ |T5_min − T3_max|` (T3=(0,0)-sector max|λ|, T5=(2,0)/(0,2)-sector min|λ|, S44/S45 anticrossing pair). Value = **0.191038** (S114 CF-S114-TAUFOLD-CUSP-CROSSING, audit 7b637db142d9bea7…; atlas-07 S45 "TRUE crossing" 0.19104, delta_min=3.27e-5). L_max-INVARIANT {5,8,10,12} by SECTOR-LOCALITY (p+q≤2 ⇒ bottom band at every L≥2; strongest Friedrich-Bär saturation, no truncation tail). Mesh-robust to 1e-6. A genuine substrate-IS LOCATION observable — NOT noise.
3. **Φ_anchor** — rational deformation-parameter freeze = **19/100 = 0.190** (CONST-FREEZE-42, S12/S42, NOT superseded). The value the DERIVED-RATIO chain consumes: `S0 = tau_fold/T_acoustic = 95/56` EXACT (S101-W3-S0-KNOB line 248, the GRADED `graded-per-C2-quantum` structural-selector winner, lines 277-285), the S85 freeze, and 119 registry rows.

`|Φ_cross − Φ_anchor| = 0.55%`; `|Φ_DOS − Φ_anchor| = 16%`. The canonical pin = Φ_anchor; the from-scratch crossing computes Φ_cross. **Re-pinning canonical to 0.191038 = replacing Φ_anchor's value with Φ_cross's output = same-functional-conflation error** (different functionals; one cannot re-pin the other) — the `phononic-framing.md §"Same-functional-different-scale"` discipline.

## S85 theorem: ASSERTS the value, PROVES only the character (own-prior correction)
The S85 `S85-W10-TAU-FOLD-UNIQUENESS-VAN-HOVE-THEOREM` (connes+lizzi, PERMANENT; knowledge-index `proven_1424`/`proven_1905`; NOT registry slot §VII.M.W10-3 which is the c_sub Γ-INEXACT residual — slot-label overload repaired S112 W2 at registry line 22260). The producing gate `value='promoted'`, `convention=canonical_constants-S85-freeze` is a **self-consistency check** (`EXP_TAU_FOLD=0.19` hardcoded line 123; `consistency_check` compares live pin to it; PASS iff equal to 1e-10) — NO argmin/finder. The 6-step chain proves only `dS/dτ ≠ 0` (NON-STATIONARITY, Steps 4-6), which is **location-free**: `dS/dτ = +58672.8` one-signed, EMPTY critical set across the whole transit window (S95 NO-WELL-ONE-LOOP). "Uniqueness" = MULTIPLICITY (one cusp on Γ_6), NOT location. The registered statements `proven_1424`/`proven_1905` carry "tau_fold = 0.190" in the TEXT but the value is an IMPORTED premise, not a derived conclusion. The theorem's PROVEN content (existence + character + multiplicity) is location-free and survives any re-pin.

## Workshop verdict: output (iii) two/three-value convention
Both authors landed (iii) from independent routes against their own priors: transit (Φ_cross author) leaned (i) re-pin; lizzi (S85 co-author) leaned (ii) 0.190-stands. (iii) forced by BOTH the cascade asymmetry (re-pin breaks S0=95/56, crashes S0-KNOB assert line 240, stales 119 rows + NOHOLOFLUX §VII.CH STAGE-3 anchor block) AND the functional non-identity. NO `canonical_constants.py` re-pin; 0.191038 lands as NEW observable `tau_cross_van_hove`; registry line-2620 tag corrected STRUCTURAL-IDENTITY→RATIONAL-ANCHOR-FREEZE + located-feature pointer.

**Verdict-flip condition** (lizzi L3 Q1): IF the S0-KNOB graded-selector still picks (iii) with `q=0.191038` substituted for `q=19/100`, THEN Φ_anchor's rational role is not load-bearing and output (i) re-pin becomes viable. Believed NO (95/56 clean rational; 0.191038/0.112=1.70570 has no small-denominator rational). transit R2 to test.

Cross-link [[permanent_theorems]] ZETA-NOT-PHYSICAL (observables are RATIOS not absolute values) + the multiplicative-normalization K=3 (sector-local saturation = plateau-value-is-the-content).
