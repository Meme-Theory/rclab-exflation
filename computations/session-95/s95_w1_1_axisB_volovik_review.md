# §VII.BG Stage-2 Independent Cross-Review — Axis-B (Transport / Superfluid)

**Gate**: CF-S95-HK-1, §W1-1. **Reviewer**: volovik-superfluid-universe-theorist (Axis-B, transport/superfluid). **Role**: independent two-agent Stage-2 cross-axis verify per `joint-theorem-promotion.md §"Two-Agent Independent-Verify"`. **Date**: 2026-05-28.

**Independence attestation**: derived ONLY from the registered §VII.BG STAGE-1-CANDIDATE block + `canonical_constants.py` + knowledge MCP + the two transport-side orthogonal anchors below. Did NOT read the S94 W1-3 workshop transcript, the S94 W1 working-paper §-section narrative, or `session-95-plan-w1.md`. NOT the original authoring agent (author = connes-ncg-theorist).

## Audit pins

- **Registry block read**: `sessions/permanent-results-registry.md` lines 20713–20789. SHA-256 = `18d365904f251b7f6da50650a3eecfb80a56a0deb795bd616d4093396ebafc8e` (18910 bytes).
- **Transport-side orthogonal anchor (PRIMARY)**: `computations/session-88/s88_w3b_chi_inheritance_kde_complete.npz`. SHA-256 = `afe52401fdcf923bb07aea337dc00df52b8344bb349d38d079ca018012656787`. BdG χ-image inheritance K_0-class occupation observable: per-summand χ-image Frobenius norms vs L_max.
- **Transport-side orthogonal anchor (SUPPORTING)**: `computations/session-88/s88_w4c_az_inheritance_cartesian_confirm.npz`. SHA-256 = `8c9fa3a218c810dd685d63d441119cf312f02173493c51400b879a8dfdadc7fe`. AZ-BDI-DIII χ-action matrices + kernel rank/cocycles.
- **SUBSTRATE-INPUT-ORTHOGONALITY**: both anchors are DISJOINT from the Axis-A reviewer's anchor (`s94_w1_3_vii_bx_t5_alpha_s_a4_recovery.npz`, the Yang-Mills a_4-channel residue). I did NOT load that file. Disjoint-input requirement satisfied → PASS-AND is structurally independent at ≥1 observable (`joint-theorem-promotion.md §"Substrate-input-orthogonality clause"`).
- **Independent corroboration (separate gates, NOT the S94 producing gate)**: `S90-AQ-SECONDARY-CLASS-SCHEME-DISCRIMINATOR` (S90) independently emits `delta_scheme=0.000e+00, GV_APS_L12=GV_CS_L12=-1.208158e+08, eta_L12=0e+00`; `S86-W-11-ETA-GV-JOINT-PROBE` (S86) PASS `eta_diff_max=0.000e+00` under `BDI-pair-cancellation`; `S88-W7-LF-D-CHEEGER-SIMONS-ODD-GRADING-PROXY` (S88) PASS `eta_diff=0.00e+00`. Structural wall: W17 Bare-Eigenvalue Parity-Blindness (PROVEN, S85).

## Clause-by-clause verdicts (NUMBERS → verdict → interpretation)

- **CLAUSE Element-2 OE-form (Laboratory-IN α_s running, BdG/transport image)**: PASS — Element 2 is given in operator-expression form `α_s ~ ∂²/∂(ln k)² Tr(P_{a_4}·D_K^{−4})`: integration/trace = `Tr(·)` over the substrate algebra; named projector `P_{a_4}` (a_4 Yang-Mills-channel projector, NOT a generic bare `P`); the second-`(ln k)`-derivative is the named running operator. This satisfies the three OE-form requirements of `cross-pillar-bridge-anatomy.md §"Element 2 OE-form discipline"` (domain+trace+named projector; prose-only forbidden). Well-posed: `D_K^{−4}` is the a_4 (s=2) heat-kernel weight, `P_{a_4}` projects the BdG/χ-image sector that survives inheritance (the M_2(C) image), the running operator `∂²/∂(ln k)²` is the standard transport-side measurement functional for a coupling's scale-dependence. Re-anchored at the CMB pivot, this is the BdG-transport α_s measurement image. OE-form well-posed.

- **CLAUSE BdG χ-image inheritance K_0-class (non-trivial)**: PASS — verified from the orthogonal anchor, NOT transcribed. `s88_w3b_chi_inheritance_kde_complete.npz`: `chi_image_norms_M3_per_Lmax` = all-zeros (8 generators × 3 L_max) ⇒ χ sends M_3(C) colour summand → 0 EXACTLY (matches registry `χ: C⊕H⊕M_3(C) → M_2(C)`). `chi_image_norms_C_per_Lmax = 1.41421356 = √2` and `chi_image_norms_H_per_Lmax = √2` ⇒ the C and H summands survive with NON-ZERO, L_max-INDEPENDENT norm ⇒ the χ-image carries a NON-TRIVIAL class (not the zero class). `s88_w4c` confirms `rank_ker_chi_at_Lmax_10 = 2`, `ker_cocycles=['phi_67','phi_88']` ⇒ the inheritance morphism is genuinely rank-2-kernel non-trivial. The surviving BdG sector (M_2(C) doublet × Nambu) carries the α_s observable into the χ-image K_0 class. Interpretation: this is the 3He-B child realization — the substrate IS class BDI (Pf=−1, N_K=2), and the M_2(C) χ-image is the BdG sector inherited as parent→child, NOT analogy.

- **CLAUSE substrate-natural NON-SCALAR binding**: PASS — Sage-exact (QQ): `f_χ = 4/16 = 1/4` confirmed (surviving fibre dim 4 = M_2(C) doublet × Nambu 2; full fibre 16). The registry correctly notes `f_χ` is L_max-INDEPENDENT and CANCELS in the a_4/a_2 ratio, so it does NOT spoil non-scalarity. The surviving L_max-dependence is the a_4/a_2 moment-ratio FLOW (registry ratio-spread over L∈{8,10,12} = 7.665e-01 ≠ 0 ⇒ the two poles respond differently to truncation ⇒ a genuine non-scalar morphism survives the dimensionless ratio). Degree check (Sage QQ): `deg(a_4/a_2) = 2·(s_a2−s_a4) = 2·(1−2) = −2`, `|deg| = 2 = |d_A|` with `d_A = +2` (transport `deg(T_BZ→pivot)`, NON-SCALAR since `d_A ≠ 0`). Conjunct-1 (deg-match) and Conjunct-2 (substrate-natural non-scalar) both hold; a canonical-import reference class would be a degree-matched SCALAR (VACUOUS, T2) — the bridge is NOT that. Per `cross-pillar-bridge-anatomy.md §"Composite Bridge-Map Dimensional-Class Admissibility"` Conjunct 2, binding is admissible.

- **CLAUSE JOINT (c) Δ_scheme → 0**: PASS — `|Δ_scheme| = 0.0 ≤ 1e-12` (PASS threshold satisfied with margin). Substitution chain: `Δ_scheme = max{|GV_APS−GV_CS|, |GV_APS−GV_BC|, |GV_CS−GV_BC|}`; with `GV_APS = GV_CS = GV_BC = −1.2081580929e+08`, each pairwise difference = 0 ⇒ `Δ_scheme = 0` (Sage QQ confirmed `0.0 ≤ 1e-12`). This is NOT mere transcription: the value is independently reproduced by the SEPARATE `S90-AQ-SECONDARY-CLASS-SCHEME-DISCRIMINATOR` gate (`delta_scheme=0.000e+00`, same `GV=-1.208158e+08`). STRUCTURAL reason (substrate-physics, my domain): the three secondary-class schemes {APS-1975 ρ-invariant, Cheeger-Simons differential character, Bismut-Cheeger η-form} differ ONLY by η-defect terms; on a BDI (parity-blind) substrate the η-invariant is even-grading and the η-defect VANISHES (`η_defect = 0` — corroborated by `S86-W-11` `eta_diff_max=0` and `S88-W7-LF-D` `eta_diff=0`, and the PROVEN W17 Bare-Eigenvalue Parity-Blindness Wall). The odd-grading GV-Heitsch secondary class carries the content; all three schemes collapse bit-identically to the cubic-ρ Dixmier-trace sum `−4·Σ dim·ρ³·|λ|^{−4}`. Δ_scheme=0 is therefore forced by the 3He-B substrate's BDI universality class, not numerical coincidence. PASS is robust.

## Supporting (registry-PASS predicate cross-check — not a JOINT clause, but transport-side load-bearing)

- Level-3 = 0.122985 < Level-2 = 0.132537 (Sage RDF: True); margin `(L2−L3)/L2 = 0.07207 > 1e-3` (True). Aitken-Δ² on the published GV ratio sequence `[1, 9.0950, 8.0338]` reproduces Φ_∞ = 8.156793 vs registry 8.156797 (5-sig-fig agreement; 4.7e-6 residual from truncated-ratio vs full-precision GV). Envelope object and the registry-PASS predicate independently confirmed. [SIGN] sub-check: substrate α_s = −0.08587279 (knowledge MCP canonical, S92) is NEGATIVE; the T5 image inherits the GV_APS<0 odd-grading sign (`t5_image_signed = −1.493993 < 0`) — negative running sign preserved.

AXIS-B COMPOSITE: PASS
