#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
S93-W5-3-CF-VII-AR-PASS-A-METHODOLOGY-FLOOR-ANNOTATION
======================================================

Gate: S93-W5-3-CF-VII-AR-PASS-A-METHODOLOGY-FLOOR-ANNOTATION  [VERIFY-THEOREM]
Classification: NON-PHONONIC (METHODOLOGY-class registry-write; PASS predicate is
                artifact-existence-with-substantive-content, not a numerical comparison
                per wave-classification.md M1-M4).
Owner: mack-cosmic-bridge (sole registry writer per `feedback_mack-bridge-role.md`).
Plan: sessions/session-plan/session-93-plan-w5.md §W5-3 ATTACHED registry annotation
      (lines 618-628; S-1 V.2 E5 pre-registration-scope correction) + the FAIL_meaning
      branch (lines 580-588: PASS-A RECLASSIFIES to METHODOLOGY-floor-only).

═══════════════════════════════════════════════════════════════════════════
WHAT THIS GATE DOES (the in-session consequence of the W5-3 FULL-FAIL)
═══════════════════════════════════════════════════════════════════════════

W5-3 (`S93-W5-3-VII-AR-FULL-TIER-N4-RETRY`, FAIL, audit_sha256=2e4a33bf...) ran the
genuine FULL-tier Connes-Chamseddine 1996 §2.2-2.3 N=4 Pauli-Villars mass tower and
found the §VII.AR PASS-A deep-IR rank-flip does NOT survive the SCHEMATIC -> FULL
transition (SCHEMATIC rank_change_per_anchor [0,0,0,0,1] reproduced bit-exact; FULL-tier
[1,1,1,1,1]; abs_diff=0.400 >> 1e-3; flip_reproduced=False). The pre-registered
dual-prior resolved to Track B (posterior 0.90): the §W4-1 (S92) PASS-A deep-IR flip is
a SCHEMATIC `M_PV²`-scalar-prefactor ARTIFACT.

This gate is the mack-cosmic-bridge sole-writer REGISTRY-TEXT annotation that is the
in-session consequence of that FAIL. It lands THREE annotations at the §VII.AR entry:

  (1) PASS-A bullet -> METHODOLOGY-FLOOR-ONLY reclassification (the deep-IR rank-flip is
      a SCHEMATIC M_PV²-scalar-prefactor artifact, FALSIFIED at FULL-tier per W5-3
      audit_sha256=2e4a33bf...; dual-prior resolved Track B posterior 0.90).
  (2) A new dated annotation block recording: PASS-A->floor + PASS-B-carries-eligibility
      (§VII.AR STAGE-3-PERMANENT eligibility rests on PASS-B [A_5_extended, ρ_S_FULL=1.0]
      ALONE) + the E5 pre-registration-scope correction (S-1 V.2; E5 pre-registers
      sub-atlas MEMBERSHIP, NOT the continuous (cutoff_frac, M_PV²_frac) vector) + the
      S92 §W4-1 verdict-permanence note (the §W4-1 composite REMAINS PASS on disk; the
      SCHEMATIC tagging was HONEST; PASS-B carries the composite).
  (3) Joint-structural-implication dual-annotation scope correction: under the FULL-FAIL
      outcome the §VII.K-DUAL.LEVEL-DRESSED corpus row inherits ONLY the
      `scoped to {A_5_extended-minus-ζ}` (PASS-B) annotation; the
      `realized via asymmetric coupling (F_2-axis FI sub-atlas)` tag is reclassified
      METHODOLOGY-floor-only.

═══════════════════════════════════════════════════════════════════════════
VERDICT-PERMANENCE DISCIPLINE (NOT a retroactive Class-1 conviction of §W4-1)
═══════════════════════════════════════════════════════════════════════════

The S92 §W4-1 composite verdict (audit_sha256=257e2619..., reading=PASS-A-AND-B) REMAINS
PASS on disk and is NOT altered by this gate. The §W4-1 SCHEMATIC tagging was HONEST
(`convention=...-SCHEMATIC-PENDING-FULL-TIER-N4` + `tier_pin=TIER-2`; POSITIVE 4-class
disclosure per substrate-first-canonical-sourcing.md §(iv)), and PASS-B carries the
composite. This gate annotates the §VII.AR REGISTRY entry's PASS-A INTERPRETATION,
citing the new FULL-tier evidence — it does NOT touch the §W4-1 verdict line.

═══════════════════════════════════════════════════════════════════════════
SUBSTITUTION CHAIN (math-scripts.md §"Double-Check Logic Before Compute")
═══════════════════════════════════════════════════════════════════════════

Claim: post-W5-3-FULL-FAIL, §VII.AR STAGE-3-PERMANENT eligibility rests on PASS-B ALONE
       (the eligible support set NARROWS from {PASS-A, PASS-B} to {PASS-B}).

  Step 1: pre_W5-3_eligible_set = {PASS-A, PASS-B}
          [S92 §W4-1 composite=PASS, reading=PASS-A-AND-B; registry "Joint structural
           implication" — BOTH folds project to clause-(d) PASS independently].
  Step 2: PASS-A_standing(SCHEMATIC) = clause-(d) PASS via rank_change_per_anchor=[0,0,0,0,1]
          (the deep-IR (1/M_KK²-internal) flip; registry PASS-A bullet).
  Step 3: W5-3 FULL-tier: rank_change_per_anchor_FULL=[1,1,1,1,1]; flip_reproduced=False;
          abs_diff=0.400 >> 1e-3 ⇒ PASS-A_standing(FULL) = clause-(d) FAIL
          [dual-prior posterior Track B = 0.90].
  Step 4: Substitute. The clause-(d) PASS placing PASS-A in the eligible set was a SCHEMATIC
          M_PV²-scalar-prefactor artifact (W5-3 substitution-chain Step 3: the {0.90,0.95,
          0.80,0.85} prefactor vector, NOT BdG physics — the S52 Bogoliubov amplitude
          v_a²=Δ²/(2(λ²+Δ²)) carries no per-regulator M_PV²-suppression knob).
          post_W5-3_PASS-A_standing = METHODOLOGY-floor-only (F-image of the scalar
          prefactor, not substrate-IS).
          post_W5-3_PASS-B_standing = UNCHANGED (ρ_S_FULL_a5extended=1.000000 at FULL tier).
  Step 5: post_W5-3_eligible_set = {PASS-A, PASS-B} ∖ {PASS-A-as-substrate-IS} = {PASS-B}.
          |eligible_set|: 2 -> 1 ⇒ the support NARROWS; eligibility rests on PASS-B ALONE.
  Conclusion: STAGE-3 eligibility for §VII.AR rests on PASS-B (A_5_extended sub-atlas,
              ρ_S_FULL=1.0) ALONE. [verified; the "narrows/decreases" direction claim
              required the chain].

═══════════════════════════════════════════════════════════════════════════
SINGLE-SHOT AFTER PATTERN (registry-landing.md §"Bridge-Landing Script Architecture")
═══════════════════════════════════════════════════════════════════════════

build_annotation_text (in memory) -> apply 3 substring replacements -> write_atomic ->
re-read + verify ALL annotation markers present -> emit exactly one verdict line.
A verify-FAIL emits FAIL once (no corrective rewrite in-script). The §VII.AR slot is
resolved by CONTENT (heading-anchor + unique substring), NOT the plan-pinned line
~17337/~17380 which is STALE-drifted per substrate-first-canonical-sourcing.md §(ii.B).

Verdict file: computations/session-93/s93_gate_verdicts.txt
"""

from __future__ import annotations

import os

os.environ.setdefault("OMP_NUM_THREADS", "8")  # (local) CPU-only (registry text edit + SHA cross-check; no compute)
os.environ.setdefault("MKL_NUM_THREADS", "8")  # (local)

import hashlib  # noqa: E402
import json  # noqa: E402
import re  # noqa: E402
import sys  # noqa: E402
from pathlib import Path  # noqa: E402

ROOT = Path(__file__).resolve().parent.parent.parent  # (local)
sys.path.insert(0, str(ROOT / "computations" / "_shared"))

# Per computations/_shared/CLAUDE.md ALL scripts MUST import canonical_constants
# (even METHODOLOGY-class; no physics constants are gate-load-bearing here, but the
# import is mandatory + the spawn prompt requires the literal `from canonical_constants import`).
from canonical_constants import *  # noqa: F401,F403,E402
from canonical_constants import M_KK, tau_fold  # noqa: E402 (metadata only; not gate-load-bearing)

import numpy as np  # noqa: E402
import matplotlib  # noqa: E402

matplotlib.use("Agg")
import matplotlib.pyplot as plt  # noqa: E402

# ---------------------------------------------------------------------------
# Gate identity + canonical paths
# ---------------------------------------------------------------------------
GATE_ID = "S93-W5-3-CF-VII-AR-PASS-A-METHODOLOGY-FLOOR-ANNOTATION"  # (local)
SCHEME = "registry-text-METHODOLOGY-class"  # (local)
CONVENTION = "VII-AR-PASS-A-METHODOLOGY-FLOOR-ANNOTATION-plus-E5-pre-reg-scope-correction"  # (local)
L_MAX = "N/A"  # (local) METHODOLOGY-class registry-text edit; no L_max
SCHEMA_VERSION = "S84+"  # (local)

SESSION_DIR = ROOT / "computations" / "session-93"  # (local)
OUT_NPZ = SESSION_DIR / "s93_w5_3_cf_vii_ar_pass_a_methodology_floor_annotation.npz"  # (local)
OUT_PNG = SESSION_DIR / "s93_w5_3_cf_vii_ar_pass_a_methodology_floor_annotation.png"  # (local)
OUT_JSON = SESSION_DIR / "s93_w5_3_cf_vii_ar_pass_a_methodology_floor_annotation.json"  # (local)
VERDICT_FILE = SESSION_DIR / "s93_gate_verdicts.txt"  # (local)

CANONICAL_CONSTANTS = ROOT / "computations" / "_shared" / "canonical_constants.py"  # (local)
REGISTRY = ROOT / "sessions" / "permanent-results-registry.md"  # (local)
W5_WP = ROOT / "sessions" / "session-93" / "session-93-w5-workingpaper.md"  # (local) W5-3 compute section source
SCRIPT_PATH = Path(__file__).resolve()  # (local)

# ---------------------------------------------------------------------------
# W5-3 evidence chain (verbatim full-64-hex; cited NOT recomputed).
# ---------------------------------------------------------------------------
W5_3_AUDIT_SHA = "2e4a33bf68bdeef7386ffe02b0efbc06727694919a62741b257e0d4efb557d13"  # (local) W5-3 FULL-tier FAIL
W5_3_CONTENT_SHA = "7574e901b7119f8f216e78b0f99ad759612b1c7ed069052455b983c96414cbb8"  # (local)
W4_1_COMPOSITE_AUDIT_SHA = "257e2619fe308645a8f87d127dde3764696d0432038725af77e3fa1fa96ce490"  # (local) S92 §W4-1 composite=PASS (RETAINED on disk; verdict permanence)
W4_1_CONTENT_SHA = "15aac20c27ed47b74c267b180c0ee55d636710f0c7e7ac84e86d3e5d80e1667f"  # (local)

# ---------------------------------------------------------------------------
# Registry CONTENT anchors (resolve §VII.AR by CONTENT, NOT plan-pinned ~17337/~17380).
# ---------------------------------------------------------------------------
AR_HDR_MARKER = "## §VII.AR — Rank-Ordering at s=4 substrate-distance-2 Mellin-cone Pole IS Regulator-PARAMETER-Dependent"  # (local) §VII.AR section heading (content-anchored)

# Anchor 1 — the PASS-A bullet opening (line ~17383; unique substring).
PASS_A_BULLET_ANCHOR = (  # (local)
    "- **PASS-A — asymmetric Bogoliubov coupling on the F_2-axis FI sub-atlas**: "
    "regulator-specific PARAMETER pins replace the uniform multiplicative PARAMETER overlay"
)

# Anchor 2 — the Joint-structural-implication dual-annotation sentence (line ~17387; unique substring).
JOINT_DUAL_ANNOTATION_ANCHOR = (  # (local)
    "under PASS-A-AND-B, the §VII.K-DUAL.LEVEL-DRESSED corpus row inherits dual annotation "
    "`scoped to {A_5_extended-minus-ζ}` + `realized via asymmetric coupling (F_2-axis FI "
    "sub-atlas)` — the joint annotation propagates UPSTREAM from §VII.AR (K=1 instance) to "
    "§VII.K-DUAL (4th-class proposal)."
)

# Anchor 3 — end of the Level-pin compliance paragraph (line ~17389; the new annotation block is inserted AFTER it).
LEVEL_PIN_PARA_END_ANCHOR = (  # (local)
    "The K=3 advancement here is at the SCHEMATIC tier; promotion to STAGE-3-PERMANENT requires "
    "the FULL-tier N=4 reinforcement, which is pre-registered as a downstream gate beyond S92."
)

# ---------------------------------------------------------------------------
# Annotation texts (built in memory; applied in a single read->replace->write cycle)
# ---------------------------------------------------------------------------

# (1) PASS-A bullet inline reclassification marker (prepended to the bullet body).
PASS_A_FLOOR_INLINE = (  # (local)
    "- **PASS-A — asymmetric Bogoliubov coupling on the F_2-axis FI sub-atlas "
    "[RECLASSIFIED METHODOLOGY-FLOOR-ONLY — S93 W5-3 FULL-tier FALSIFICATION, "
    "2026-05-24]**: **METHODOLOGY-FLOOR-ONLY** per the S93 W5-3 FULL-tier N=4 "
    "Connes-Chamseddine 1996 §2.2-2.3 retry (`S93-W5-3-VII-AR-FULL-TIER-N4-RETRY`, "
    f"composite=FAIL, audit_sha256=`{W5_3_AUDIT_SHA}`): the deep-IR rank-flip "
    "`rank_change_per_anchor=[0,0,0,0,1]` does NOT survive the SCHEMATIC -> FULL transition "
    "(FULL-tier `[1,1,1,1,1]`; `abs_diff=0.400 ≫ 1e-3`; `flip_reproduced=False`), so the "
    "clause-(d) PASS that placed PASS-A in the §VII.AR STAGE-3 eligible set was a SCHEMATIC "
    "`M_PV²`-scalar-prefactor ARTIFACT (the `{0.90, 0.95, 0.80, 0.85}` prefactor vector, NOT "
    "BdG physics — the S52 Bogoliubov amplitude `v_a²=Δ²/(2(λ²+Δ²))` carries no per-regulator "
    "`M_PV²`-suppression knob), NOT a substrate-IS feature; the pre-registered dual-prior "
    "resolved to **Track B (posterior 0.90)**. See the **S93 W5-3 FULL-FAIL reclassification "
    "annotation** below for the PASS-B-carries-eligibility + E5-scope-correction + verdict-"
    "permanence consequences. [Original SCHEMATIC-tier finding retained verbatim below for "
    "audit trail:] regulator-specific PARAMETER pins replace the uniform multiplicative PARAMETER overlay"
)

# (3) Joint-structural-implication dual-annotation scope correction (replaces the BOTH-FOLD dual-annotation sentence).
JOINT_DUAL_ANNOTATION_CORRECTED = (  # (local)
    "under PASS-A-AND-B, the §VII.K-DUAL.LEVEL-DRESSED corpus row inherits dual annotation "
    "`scoped to {A_5_extended-minus-ζ}` + `realized via asymmetric coupling (F_2-axis FI "
    "sub-atlas)` — the joint annotation propagates UPSTREAM from §VII.AR (K=1 instance) to "
    "§VII.K-DUAL (4th-class proposal). **[S93 W5-3 FULL-FAIL scope correction, 2026-05-24: "
    "the `realized via asymmetric coupling (F_2-axis FI sub-atlas)` half of this dual "
    "annotation is RECLASSIFIED METHODOLOGY-FLOOR-ONLY (W5-3 FULL-tier FALSIFICATION, "
    f"audit_sha256=`{W5_3_AUDIT_SHA}`); under the FULL-FAIL outcome the §VII.K-DUAL.LEVEL-DRESSED "
    "corpus row inherits ONLY the substrate-IS `scoped to {A_5_extended-minus-ζ}` (PASS-B) "
    "annotation. See the S93 W5-3 FULL-FAIL reclassification annotation below.]**"
)

# (2) The new dated annotation block (inserted AFTER the Level-pin compliance paragraph).
W5_3_FULL_FAIL_ANNOTATION_BLOCK = (  # (local)
    "\n\n**S93 W5-3 FULL-FAIL reclassification annotation (PASS-A -> METHODOLOGY-floor-only; "
    "E5 pre-registration-scope correction S-1 V.2; mack-cosmic-bridge sole-writer per "
    "`feedback_mack-bridge-role.md`; landed 2026-05-24)**: The FULL-tier N=4 Connes-Chamseddine "
    "1996 §2.2-2.3 retry `S93-W5-3-VII-AR-FULL-TIER-N4-RETRY` (composite=FAIL; S87 schema-v2 "
    "3-tuple `sign=PASS / magnitude=FAIL / regime=VALID`; "
    f"audit_sha256=`{W5_3_AUDIT_SHA}`, content_sha256=`{W5_3_CONTENT_SHA}`; "
    "`scheme=FULL-tier-N4-Connes-Chamseddine-1996`, `tier_pin=TIER-1`, CLASS=FULL, NO `-SCHEMATIC` "
    "suffix) executed the genuine FULL physical N=4 Pauli-Villars mass tower "
    "(`c_j=(-1)^j·C(4,j)=[1,-4,6,-4,1]`, `Σ_j c_j j^k=0` for k=0..3, first non-vanishing moment "
    "`Σ_j c_j j^4=24`; Λ_UV=M_KK) that distinguishes FULL tier from the SCHEMATIC scalar "
    "`(1−M_PV²_frac)` prefactor. Four consequences:\n"
    "\n"
    "1. **PASS-A RECLASSIFIES to METHODOLOGY-FLOOR-ONLY.** The §W4-1 (S92) PASS-A deep-IR "
    "rank-flip `rank_change_per_anchor=[0,0,0,0,1]` (the clause-(d) PASS on the asymmetric "
    "Bogoliubov coupling, F_2-axis FI sub-atlas) does NOT survive the SCHEMATIC -> FULL "
    "transition: the SCHEMATIC `[0,0,0,0,1]` is reproduced BIT-EXACT, but the FULL-tier N=4 "
    "tower restructures the rank at ALL FIVE anchors `[1,1,1,1,1]` (`flip_reproduced=False`), "
    "and the deep-IR PRIMARY-vs-SCHEMATIC Spearman FLIPS SIGN (`ρ_S_SCHEMATIC=-0.200000` -> "
    "`ρ_S_FULL=+0.200000`, `abs_diff=0.400000 ≫ 1e-3` agreement band). Substitution chain "
    "(`math-scripts.md §\"Double-Check Logic Before Compute\"`): the clause-(d) PASS placing "
    "PASS-A in the eligible set was driven by the scalar `(1−M_PV²_frac_r)` prefactor vector "
    "`{0.90, 0.95, 0.80, 0.85}` for `{F_2, cutoff_sqrt, anomaly, Zubarev}` at the saturated "
    "deep-IR anchor (where `e^{−t_ref·λ²}→1`), NOT BdG physics — the S52 Bogoliubov amplitude "
    "`v_a²=Δ²/(2(λ²+Δ²))` carries NO per-regulator `M_PV²_frac`-form mass-suppression knob. "
    "The deep-IR flip is therefore a methodology-floor F-image of the SCHEMATIC scalar "
    "prefactor (`epistemic-discipline.md §\"Layer-Decomposition\"`), NOT a substrate-IS "
    "structural feature. The pre-registered dual-prior (`epistemic-discipline.md §\"Dual-prior "
    "pre-registration as track-discriminator\"`) resolved per its `FULL-FAIL ⇒ 0.90 to Track B` "
    "mapping: **posterior Track A (substrate-IS) = 0.10; Track B (SCHEMATIC `M_PV²` artifact) "
    "= 0.90**.\n"
    "\n"
    "2. **§VII.AR STAGE-3-PERMANENT eligibility rests on PASS-B ALONE.** Substitution chain: "
    "pre-W5-3 eligible support set = {PASS-A, PASS-B} (S92 §W4-1 reading=PASS-A-AND-B); "
    "PASS-A's clause-(d) standing reclassifies to METHODOLOGY-floor-only (consequence 1) while "
    "PASS-B's standing is UNCHANGED at FULL tier (`ρ_S_FULL_a5extended = 1.000000` at canonical "
    "L_max=12; the A_5_extended = A_5 ∖ {ζ} = {Pauli-Villars, sharp_cutoff, sinc_lattice, "
    "sech_lattice} sub-atlas, `D²=0`, zero pairwise rank inversions); ⇒ post-W5-3 eligible set "
    "= {PASS-B} (`|eligible_set|: 2 -> 1`, the support NARROWS). **STAGE-3-PERMANENT eligibility "
    "for §VII.AR proceeds on PASS-B (A_5_extended sub-atlas, `ρ_S_FULL=1.000000`) ALONE — NOT on "
    "the now-floor-only PASS-A, and NOT on the BOTH-FOLD co-equal framing.** The §VII.K-DUAL."
    "LEVEL-DRESSED corpus row inherits ONLY the substrate-IS `scoped to {A_5_extended-minus-ζ}` "
    "(PASS-B) annotation; the `realized via asymmetric coupling (F_2-axis FI sub-atlas)` "
    "(PASS-A) annotation is METHODOLOGY-floor-only (Joint-structural-implication dual-annotation "
    "scope-corrected above).\n"
    "\n"
    "3. **E5 pre-registration-scope correction (S-1 V.2; category-conflation closure).** The "
    "PASS-A-RESTRICTED E5 enumeration (`W3 Edit E5`, three substrate-natural sub-atlas candidates "
    "{`A_5_extended-minus-ζ`, `A_5_extended-minus-cutoff_sqrt`, `A_5_extended-minus-anomaly`}) "
    "pre-registers sub-atlas **MEMBERSHIP** (which regulators are in the atlas), NOT the "
    "continuous `(cutoff_frac, M_PV²_frac)` PARAMETER vector. The PASS-A asymmetric-coupling pin "
    "vector's continuous degrees of freedom (`cutoff_frac ∈ {0.7, 0.5, 0.9, 1.2}`, "
    "`M_PV²_frac ∈ {0.1, 0.05, 0.2, 0.15}`) are **NOT constrained by E5's three-candidate "
    "enumeration** and require independent substrate-physics derivation OR FULL-tier confirmation "
    "per S93-W5-3 (S-1 V.1). This closes the category conflation (S-1 II.4) so future gates "
    "cannot cite E5 as cover for a continuous-parameter back-solve. This pre-registration-hygiene "
    "correction holds INDEPENDENT of the FULL-tier outcome (true under FULL-PASS or FULL-FAIL); "
    "W5-3 returned FULL-FAIL, which makes the distinction operative: the floored PASS-A pathway's "
    "continuous-parameter realization is exactly the unconstrained-DOF class E5 never covered.\n"
    "\n"
    "4. **S92 §W4-1 verdict permanence — NOT a retroactive Class-1 conviction.** The S92 §W4-1 "
    f"composite verdict REMAINS PASS on disk (audit_sha256=`{W4_1_COMPOSITE_AUDIT_SHA}`, "
    f"content_sha256=`{W4_1_CONTENT_SHA}`, reading=PASS-A-AND-B; UNCHANGED). This annotation does "
    "NOT alter the §W4-1 verdict line: the §W4-1 SCHEMATIC tagging was HONEST "
    "(`convention=…-SCHEMATIC-PENDING-FULL-TIER-N4` + companion `# tier_pin=TIER-2`; POSITIVE "
    "4-class disclosure per `substrate-first-canonical-sourcing.md §(iv)` K=4 MANDATORY), and "
    "**PASS-B (A_5_extended sub-atlas-minus-ζ) carries the composite**. Per the layer-functor F "
    "(`epistemic-discipline.md §\"Layer-Decomposition\"`), only PASS-A's epistemic STANDING is "
    "reclassified (substrate-IS -> methodology-floor); the §W4-1 gate's verdict is the F-image of "
    "its honest SCHEMATIC-tier compute and is preserved by absolute verdict permanence "
    "(`gate-verdicts.md §\"Option A\"`). The `-SCHEMATIC-PENDING-FULL-TIER-N4` suffix is now "
    "discharged in the sense that the FULL-tier retry has EXECUTED and returned its verdict; it is "
    "NOT discharged as a CONFIRMATION (FULL-tier did not confirm PASS-A).\n"
    "\n"
    "**Cross-link**: W5-3 compute section `sessions/archive/session-93/session-93-w5-workingpaper.md §W5-3`; "
    f"W5-3 verdict `computations/session-93/s93_gate_verdicts.txt` (`audit_sha256=`{W5_3_AUDIT_SHA}`); "
    "`substrate-first-canonical-sourcing.md §(iv)` K=4 MANDATORY level-pin (the FULL-vs-SCHEMATIC "
    "discipline that surfaced the reclassification); `epistemic-discipline.md §\"Dual-prior "
    "pre-registration as track-discriminator\"` (the pre-registered Track-B resolution)."
)


# ---------------------------------------------------------------------------
# SHA helpers
# ---------------------------------------------------------------------------
def sha256_of_file(p: Path) -> str:
    try:
        return hashlib.sha256(p.read_bytes()).hexdigest()
    except OSError:
        return "0" * 64


def log_input_pins(files: dict) -> dict:
    pins = {}  # (local)
    print("=" * 76)
    print(f"Gate: {GATE_ID}")
    print("=" * 76)
    print("Input SHA-256 pins (first lines of stdout):")
    for name, p in files.items():
        sha = sha256_of_file(p)  # (local)
        pins[name] = sha
        rel = str(p.relative_to(ROOT)).replace("\\", "/") if p.exists() else str(p)
        print(f"  {name:30s} = {sha[:16]}...  ({rel})")
    return pins


def extract_ar_block(registry_text: str) -> str:
    """Extract the §VII.AR entry block (from its `## §VII.AR` heading to the next
    `## §VII.` heading) for the content_sha256 leg. Content-anchored, NOT line-pinned
    (drift-robust per substrate-first-canonical-sourcing.md §(ii.B)). Returns '' if absent.
    """
    start = registry_text.find(AR_HDR_MARKER)  # (local)
    if start < 0:
        return ""
    rest = registry_text[start + len(AR_HDR_MARKER):]  # (local)
    nxt = rest.find("\n## §VII.")  # (local) next §VII top-level heading
    block = AR_HDR_MARKER + (rest if nxt < 0 else rest[:nxt])  # (local)
    return block


def compute_dual_sha(pins: dict, block_text: str) -> tuple[str, str]:
    """Dual-SHA. content_sha256 = SHA over the VERIFIED (re-read) annotated §VII.AR
    entry block (the artifact whose existence-with-content IS the METHODOLOGY-class PASS
    predicate). audit_sha256 = SHA over the input-pin map + the W5-3/§W4-1 evidence-chain
    SHAs + per-gate identity keys (gate-distinct per mechanical-closure-discipline.md item 3).
    """
    h_content = hashlib.sha256()  # (local)
    h_content.update(block_text.encode("utf-8"))
    content = h_content.hexdigest()  # (local)

    pinmap_json = json.dumps(dict(sorted(pins.items())), sort_keys=True).encode("utf-8")  # (local)
    h_audit = hashlib.sha256()  # (local)
    h_audit.update(pinmap_json)
    h_audit.update(
        f"{W5_3_AUDIT_SHA}|{W5_3_CONTENT_SHA}|{W4_1_COMPOSITE_AUDIT_SHA}|{W4_1_CONTENT_SHA}".encode("utf-8")
    )
    h_audit.update(f"{GATE_ID}|{SCHEME}|{CONVENTION}|L_max={L_MAX}".encode("utf-8"))
    audit = h_audit.hexdigest()  # (local)
    return audit, content


# ---------------------------------------------------------------------------
# Build + apply the 3 annotations (single read -> replace -> write -> re-read).
# Idempotent: skip an application whose landed marker is already present.
# ---------------------------------------------------------------------------
# Landed markers (unique substrings the verify step greps for on-disk).
M_FLOOR_INLINE = "[RECLASSIFIED METHODOLOGY-FLOOR-ONLY — S93 W5-3 FULL-tier FALSIFICATION, 2026-05-24]"  # (local)
M_ANNOTATION_BLOCK = (  # (local)
    "**S93 W5-3 FULL-FAIL reclassification annotation (PASS-A -> METHODOLOGY-floor-only; "
    "E5 pre-registration-scope correction S-1 V.2;"
)
M_JOINT_CORRECTION = "[S93 W5-3 FULL-FAIL scope correction, 2026-05-24:"  # (local)
# Substantive-content markers inside the annotation block (PASS predicate sub-conditions).
M_PASS_B_CARRIES = "STAGE-3-PERMANENT eligibility for §VII.AR proceeds on PASS-B (A_5_extended sub-atlas, `ρ_S_FULL=1.000000`) ALONE"  # (local)
M_E5_BOTH_CATS = "pre-registers sub-atlas **MEMBERSHIP** (which regulators are in the atlas), NOT the continuous `(cutoff_frac, M_PV²_frac)` PARAMETER vector"  # (local)
M_VERDICT_PERM = "S92 §W4-1 verdict permanence — NOT a retroactive Class-1 conviction"  # (local)
M_W5_3_XLINK = W5_3_AUDIT_SHA  # (local) cross-link to W5-3 audit_sha


def apply_annotations(registry_text: str) -> tuple[str, dict]:
    """Apply the 3 annotations in memory. Returns (new_text, per-annotation status dict).
    Each replacement is guarded by an idempotency check on its landed marker.
    """
    status = {  # (local)
        "anchor1_pass_a_found": PASS_A_BULLET_ANCHOR in registry_text,
        "anchor2_joint_found": JOINT_DUAL_ANNOTATION_ANCHOR in registry_text,
        "anchor3_levelpin_found": LEVEL_PIN_PARA_END_ANCHOR in registry_text,
        "ann1_floor_inline_applied": False,
        "ann1_floor_inline_idempotent_skip": False,
        "ann2_block_applied": False,
        "ann2_block_idempotent_skip": False,
        "ann3_joint_correction_applied": False,
        "ann3_joint_correction_idempotent_skip": False,
    }
    txt = registry_text  # (local)

    # --- Annotation 1: PASS-A bullet inline floor reclassification ---
    if M_FLOOR_INLINE in txt:
        status["ann1_floor_inline_idempotent_skip"] = True
    elif PASS_A_BULLET_ANCHOR in txt:
        # replace exactly once (count guard: anchor MUST be unique)
        if txt.count(PASS_A_BULLET_ANCHOR) == 1:
            txt = txt.replace(PASS_A_BULLET_ANCHOR, PASS_A_FLOOR_INLINE, 1)
            status["ann1_floor_inline_applied"] = True

    # --- Annotation 3: Joint-structural-implication dual-annotation scope correction ---
    if M_JOINT_CORRECTION in txt:
        status["ann3_joint_correction_idempotent_skip"] = True
    elif JOINT_DUAL_ANNOTATION_ANCHOR in txt:
        if txt.count(JOINT_DUAL_ANNOTATION_ANCHOR) == 1:
            txt = txt.replace(JOINT_DUAL_ANNOTATION_ANCHOR, JOINT_DUAL_ANNOTATION_CORRECTED, 1)
            status["ann3_joint_correction_applied"] = True

    # --- Annotation 2: new dated annotation block AFTER the Level-pin compliance paragraph ---
    if M_ANNOTATION_BLOCK in txt:
        status["ann2_block_idempotent_skip"] = True
    elif LEVEL_PIN_PARA_END_ANCHOR in txt:
        if txt.count(LEVEL_PIN_PARA_END_ANCHOR) == 1:
            txt = txt.replace(
                LEVEL_PIN_PARA_END_ANCHOR,
                LEVEL_PIN_PARA_END_ANCHOR + W5_3_FULL_FAIL_ANNOTATION_BLOCK,
                1,
            )
            status["ann2_block_applied"] = True

    return txt, status


def write_atomic(path: Path, text: str) -> None:
    """Atomic write with fsync (registry-landing.md single-shot AFTER pattern)."""
    tmp = path.with_suffix(path.suffix + ".tmp")  # (local)
    with tmp.open("w", encoding="utf-8", newline="") as f:
        f.write(text)
        f.flush()
        os.fsync(f.fileno())
    os.replace(tmp, path)


def verify_on_disk() -> dict:
    """Re-read the registry from disk and verify ALL annotation markers + substantive
    sub-condition markers are present. This is the FINAL verification step that
    determines the verdict (single-shot AFTER pattern).
    """
    txt = REGISTRY.read_text(encoding="utf-8")  # (local)
    out = {  # (local)
        "floor_inline_present": M_FLOOR_INLINE in txt,
        "annotation_block_present": M_ANNOTATION_BLOCK in txt,
        "joint_correction_present": M_JOINT_CORRECTION in txt,
        "pass_b_carries_eligibility_present": M_PASS_B_CARRIES in txt,
        "e5_both_categories_present": M_E5_BOTH_CATS in txt,
        "verdict_permanence_note_present": M_VERDICT_PERM in txt,
        "w5_3_xlink_present": M_W5_3_XLINK in txt,
    }
    # The annotation block must live INSIDE the §VII.AR entry block (drift-robust check).
    ar_block = extract_ar_block(txt)  # (local)
    out["annotation_in_ar_block"] = M_ANNOTATION_BLOCK in ar_block
    out["floor_inline_in_ar_block"] = M_FLOOR_INLINE in ar_block
    out["ar_block_word_count"] = len(ar_block.split())
    return out


# ---------------------------------------------------------------------------
# Option-A supersedes source (latest non-superseded prior line for this gate-ID)
# ---------------------------------------------------------------------------
def find_latest_prior_audit_sha() -> str | None:
    if not VERDICT_FILE.exists():
        return None
    superseded: set[str] = set()  # (local)
    candidates: list[str] = []  # (local)
    for ln in VERDICT_FILE.read_text(encoding="utf-8").splitlines():
        if ln.startswith(f"{GATE_ID}:") and "audit_sha256=" in ln:
            m = re.search(r"audit_sha256=([a-f0-9]{64})", ln)  # (local)
            if m:
                candidates.append(m.group(1))
            sm = re.search(r"supersedes=([a-f0-9]{64})", ln)  # (local)
            if sm:
                superseded.add(sm.group(1))
    live = [c for c in candidates if c not in superseded]  # (local)
    return live[-1] if live else None


def append_verdict(verdict: str, value_str: str, audit_sha: str, content_sha: str,
                   supersedes: str | None = None) -> None:
    """Single canonical dual-SHA verdict line + companion row. METHODOLOGY/registry
    annotation; [VERIFY-THEOREM] — no [SIGN] 3-tuple (artifact-existence predicate,
    not a sign/direction claim). Append-only single open("a") write.
    """
    value_field = value_str if supersedes is None else f"{value_str};supersedes={supersedes}"  # (local)
    canonical = (  # (local)
        f"{GATE_ID}: {verdict} -- value='{value_field}' "
        f"scheme={SCHEME} convention={CONVENTION} L_max={L_MAX} "
        f"audit_sha256={audit_sha} content_sha256={content_sha} "
        f"schema_version={SCHEMA_VERSION}\n"
    )
    supersedes_note = f"; supersedes={supersedes}" if supersedes else ""  # (local)
    companion = (  # (local)
        f"# audit_sha256_short={audit_sha[:16]} content_sha256_short={content_sha[:16]} "
        f"# {GATE_ID} dual-SHA companion row (W9a-99 split); METHODOLOGY-class "
        f"mack-cosmic-bridge sole-writer §VII.AR PASS-A METHODOLOGY-FLOOR-ONLY reclassification "
        f"(W5-3 FULL-tier FALSIFICATION audit_sha256={W5_3_AUDIT_SHA[:16]}; dual-prior Track B 0.90) "
        f"+ E5 pre-reg-scope correction (S-1 V.2) + PASS-B-carries-eligibility + S92 §W4-1 "
        f"verdict-permanence (composite 257e2619 RETAINED, NOT altered); [VERIFY-THEOREM] no "
        f"[SIGN] 3-tuple{supersedes_note}\n"
    )
    with VERDICT_FILE.open("a", encoding="utf-8") as f:
        f.write(canonical)
        f.write(companion)


def main() -> int:
    print(f"=== {GATE_ID} ===")
    input_files = {
        "canonical_constants": CANONICAL_CONSTANTS,
        "permanent_results_registry": REGISTRY,
        "w5_workingpaper": W5_WP,
        "script": SCRIPT_PATH,
    }
    pins = log_input_pins(input_files)

    # ---- Drift-correction disclosure (plan-pinned §VII.AR line is STALE) ----
    print("\n" + "=" * 76)
    print("Plan-text-drift correction (substrate-first-canonical-sourcing.md §(ii.B))")
    print("=" * 76)
    print("  plan-pinned §VII.AR line ~17337 / PASS-A bullet ~17380 = STALE plan-frozen refs.")
    print("  §VII.AR resolved by CONTENT (heading-anchor + unique PASS-A/Joint/Level-pin substrings).")

    registry_text = REGISTRY.read_text(encoding="utf-8")  # (local)

    # ---- Pre-condition: §VII.AR present + all 3 anchors resolvable by content ----
    print("\n" + "=" * 76)
    print("Pre-condition: §VII.AR heading + 3 content anchors resolvable")
    print("=" * 76)
    ar_present = AR_HDR_MARKER in registry_text  # (local)
    print(f"  §VII.AR heading present = {ar_present}")
    print(f"  anchor1 (PASS-A bullet) found = {PASS_A_BULLET_ANCHOR in registry_text}")
    print(f"  anchor2 (Joint dual-annotation) found = {JOINT_DUAL_ANNOTATION_ANCHOR in registry_text}")
    print(f"  anchor3 (Level-pin para end) found = {LEVEL_PIN_PARA_END_ANCHOR in registry_text}")

    # ---- Build + apply the 3 annotations IN MEMORY (single-shot AFTER pattern) ----
    print("\n" + "=" * 76)
    print("Apply 3 annotations in memory (idempotent; unique-anchor guarded)")
    print("=" * 76)
    new_text, ann_status = apply_annotations(registry_text)  # (local)
    for k, val in ann_status.items():
        print(f"  {k} = {val}")

    changed = new_text != registry_text  # (local)
    any_applied_or_present = (  # (local) each annotation either applied-now or already-present
        (ann_status["ann1_floor_inline_applied"] or ann_status["ann1_floor_inline_idempotent_skip"])
        and (ann_status["ann2_block_applied"] or ann_status["ann2_block_idempotent_skip"])
        and (ann_status["ann3_joint_correction_applied"] or ann_status["ann3_joint_correction_idempotent_skip"])
    )
    print(f"  >>> text changed this run = {changed}; all-3-applied-or-present = {any_applied_or_present}")

    # ---- Write atomically (only if changed) then RE-READ to verify ----
    if changed:
        write_atomic(REGISTRY, new_text)
        print(f"  registry written atomically -> {REGISTRY.relative_to(ROOT)}")
    else:
        print("  no change to write (all annotations already present; idempotent re-run)")

    # ---- FINAL verification step (determines verdict) ----
    print("\n" + "=" * 76)
    print("Re-read + verify ALL annotation markers on-disk (single point of decision)")
    print("=" * 76)
    v = verify_on_disk()  # (local)
    for k, val in v.items():
        print(f"  {k} = {val}")

    verify_pass = bool(
        ar_present
        and v["floor_inline_present"]
        and v["annotation_block_present"]
        and v["joint_correction_present"]
        and v["pass_b_carries_eligibility_present"]
        and v["e5_both_categories_present"]
        and v["verdict_permanence_note_present"]
        and v["w5_3_xlink_present"]
        and v["annotation_in_ar_block"]
        and v["floor_inline_in_ar_block"]
        and (v["ar_block_word_count"] >= 15)
    )
    verdict = "PASS" if verify_pass else "FAIL"  # (local)
    print(f"\n  >>> verify_pass = {verify_pass} -> verdict = {verdict}")

    # ---- value string ----
    value_str = (  # (local)
        f"VII-AR-PASS-A-RECLASSIFIED-METHODOLOGY-FLOOR-ONLY_"
        f"W5_3_FULL_FAIL_audit={W5_3_AUDIT_SHA}_dual_prior_track_B_posterior=0.90_"
        f"PASS_B_A5extended_rho_S_FULL=1.000000_carries_stage3_eligibility_ALONE_"
        f"E5_pre_reg_scope_correction_S-1_V.2_membership_NOT_continuous_param_vector_"
        f"S92_W4_1_composite_PASS_RETAINED_verdict_permanence_audit={W4_1_COMPOSITE_AUDIT_SHA}_"
        f"NOT_retroactive_class1_conviction;"
        f"floor_inline={v['floor_inline_present']};annotation_block={v['annotation_block_present']};"
        f"joint_scope_correction={v['joint_correction_present']};"
        f"pass_b_carries={v['pass_b_carries_eligibility_present']};"
        f"e5_both_categories={v['e5_both_categories_present']};"
        f"verdict_permanence_note={v['verdict_permanence_note_present']};"
        f"ann_in_ar_block={v['annotation_in_ar_block']};ar_block_words={v['ar_block_word_count']};"
        f"ann1_applied={ann_status['ann1_floor_inline_applied']};"
        f"ann2_applied={ann_status['ann2_block_applied']};"
        f"ann3_applied={ann_status['ann3_joint_correction_applied']};"
        f"slot_resolved_by_content_NOT_plan_pinned_line_17337_stale_drifted;"
        f"M4_allowlist_append=ORCHESTRATOR-ONLY"
    )

    ar_block_final = extract_ar_block(REGISTRY.read_text(encoding="utf-8"))  # (local) content leg = verified annotated block
    audit_sha, content_sha = compute_dual_sha(pins, ar_block_final)  # (local)
    supersedes = find_latest_prior_audit_sha()  # (local) None on first emission
    if supersedes:
        print(f"  prior verdict line detected; emitting corrective line with supersedes={supersedes[:16]}...")

    # ---- artifacts (npz + json + png) BEFORE verdict emission ----
    _emit_npz_and_json(ann_status, v, ar_block_final, verdict, value_str, audit_sha, content_sha,
                       supersedes)
    _emit_plot(v, verdict)

    append_verdict(verdict, value_str, audit_sha, content_sha, supersedes=supersedes)
    print(f"\n  4-tuple: (value=<...>, scheme={SCHEME}, convention={CONVENTION}, L_max={L_MAX})")
    print(f"  audit_sha256={audit_sha}")
    print(f"  content_sha256={content_sha}")
    print(f"\n  >>> VERDICT: {verdict}")
    print("  PASS-A reclassified METHODOLOGY-FLOOR-ONLY (W5-3 FULL-tier FALSIFICATION).")
    print("  PASS-B (A_5_extended, rho_S_FULL=1.0) carries STAGE-3-PERMANENT eligibility ALONE.")
    print("  E5 pre-reg-scope correction (S-1 V.2) landed: membership NOT continuous-param vector.")
    print(f"  S92 §W4-1 composite PASS RETAINED on disk (audit {W4_1_COMPOSITE_AUDIT_SHA[:16]}) — NOT altered.")
    print("  M4 allowlist append = ORCHESTRATOR-ONLY (flagged in WP §W5-3-CF).")
    return 0  # verdict is DATA; exit 0 regardless of PASS/FAIL


def _emit_npz_and_json(ann_status, v, ar_block, verdict, value_str, audit_sha, content_sha,
                       supersedes):
    np.savez(
        OUT_NPZ,
        # annotation application flags
        ann1_floor_inline_applied=np.bool_(ann_status["ann1_floor_inline_applied"]),
        ann1_floor_inline_idempotent_skip=np.bool_(ann_status["ann1_floor_inline_idempotent_skip"]),
        ann2_block_applied=np.bool_(ann_status["ann2_block_applied"]),
        ann2_block_idempotent_skip=np.bool_(ann_status["ann2_block_idempotent_skip"]),
        ann3_joint_correction_applied=np.bool_(ann_status["ann3_joint_correction_applied"]),
        ann3_joint_correction_idempotent_skip=np.bool_(ann_status["ann3_joint_correction_idempotent_skip"]),
        anchor1_pass_a_found=np.bool_(ann_status["anchor1_pass_a_found"]),
        anchor2_joint_found=np.bool_(ann_status["anchor2_joint_found"]),
        anchor3_levelpin_found=np.bool_(ann_status["anchor3_levelpin_found"]),
        # on-disk verification flags (the PASS predicate)
        floor_inline_present=np.bool_(v["floor_inline_present"]),
        annotation_block_present=np.bool_(v["annotation_block_present"]),
        joint_correction_present=np.bool_(v["joint_correction_present"]),
        pass_b_carries_eligibility_present=np.bool_(v["pass_b_carries_eligibility_present"]),
        e5_both_categories_present=np.bool_(v["e5_both_categories_present"]),
        verdict_permanence_note_present=np.bool_(v["verdict_permanence_note_present"]),
        w5_3_xlink_present=np.bool_(v["w5_3_xlink_present"]),
        annotation_in_ar_block=np.bool_(v["annotation_in_ar_block"]),
        floor_inline_in_ar_block=np.bool_(v["floor_inline_in_ar_block"]),
        ar_block_word_count=np.int64(v["ar_block_word_count"]),
        # cited evidence chain (full-64-hex)
        w5_3_audit_sha=str(W5_3_AUDIT_SHA),
        w5_3_content_sha=str(W5_3_CONTENT_SHA),
        w4_1_composite_audit_sha=str(W4_1_COMPOSITE_AUDIT_SHA),
        w4_1_content_sha=str(W4_1_CONTENT_SHA),
        # resolved findings
        pass_a_reclassification="METHODOLOGY-FLOOR-ONLY",
        dual_prior_posterior_track_A=np.float64(0.10),
        dual_prior_posterior_track_B=np.float64(0.90),
        pass_b_rho_S_FULL_a5extended=np.float64(1.000000),
        eligible_set_pre_w5_3="{PASS-A, PASS-B}",
        eligible_set_post_w5_3="{PASS-B}",
        eligible_set_cardinality_change="2->1 (narrows)",
        w4_1_verdict_status="PASS-RETAINED-verdict-permanence-NOT-altered",
        e5_scope_correction="membership-NOT-continuous-(cutoff_frac,M_PV2_frac)-vector",
        # metadata
        L_max=str(L_MAX),
        tau_fold=np.float64(tau_fold),
        M_KK=np.float64(M_KK),
        verdict=str(verdict),
        scheme=SCHEME,
        convention=CONVENTION,
        gate_id=GATE_ID,
        audit_sha256=str(audit_sha),
        content_sha256=str(content_sha),
        supersedes=str(supersedes) if supersedes else "NONE-first-emission",
        slot_resolution="content-anchored-NOT-plan-pinned-line-17337-stale-drifted",
        m1_artifact_existence_with_content=np.bool_(True),
        m4_allowlist="ORCHESTRATOR-ONLY",
    )
    print(f"  NPZ -> {OUT_NPZ.relative_to(ROOT)}")
    _chk = np.load(OUT_NPZ, allow_pickle=True)  # (local)
    rt_ok = bool(_chk["floor_inline_present"]) == v["floor_inline_present"]  # (local)
    print(f"  round-trip: npz floor_inline_present preserved: {rt_ok}")

    record = {  # (local)
        "gate_id": GATE_ID,
        "verdict": verdict,
        "value": value_str,
        "scheme": SCHEME,
        "convention": CONVENTION,
        "L_max": L_MAX,
        "audit_sha256": audit_sha,
        "content_sha256": content_sha,
        "supersedes": supersedes if supersedes else "NONE (first emission)",
        "task": "mack-cosmic-bridge sole-writer §VII.AR registry-text annotation; in-session consequence of W5-3 FULL-FAIL",
        "annotations_landed": {
            "1_pass_a_inline_floor_reclassification": "METHODOLOGY-FLOOR-ONLY at the PASS-A bullet",
            "2_dated_annotation_block": "PASS-A->floor + PASS-B-carries-eligibility + E5-scope-correction + verdict-permanence",
            "3_joint_dual_annotation_scope_correction": "K-DUAL.LEVEL-DRESSED row inherits ONLY scoped-to-{A_5_extended-minus-ζ}; asymmetric-coupling tag floored",
        },
        "findings": {
            "pass_a_reclassification": "METHODOLOGY-FLOOR-ONLY (SCHEMATIC M_PV²-scalar-prefactor artifact, FALSIFIED at FULL-tier)",
            "w5_3_full_tier_evidence": {
                "audit_sha256": W5_3_AUDIT_SHA,
                "rank_change_SCHEMATIC": "[0,0,0,0,1]",
                "rank_change_FULL": "[1,1,1,1,1]",
                "abs_diff": "0.400000 (>> 1e-3)",
                "flip_reproduced": False,
                "three_tuple": "sign=PASS / magnitude=FAIL / regime=VALID",
            },
            "dual_prior": {"posterior_track_A_substrate_IS": 0.10, "posterior_track_B_schematic_artifact": 0.90},
            "stage3_eligibility": "rests on PASS-B (A_5_extended sub-atlas, ρ_S_FULL=1.000000) ALONE",
            "eligible_set_change": "{PASS-A, PASS-B} -> {PASS-B} (cardinality 2 -> 1; support narrows)",
            "e5_scope_correction_S-1_V.2": "E5 pre-registers sub-atlas MEMBERSHIP, NOT the continuous (cutoff_frac, M_PV²_frac) PARAMETER vector; closes category conflation S-1 II.4; holds independent of FULL-PASS/FULL-FAIL",
            "verdict_permanence": "S92 §W4-1 composite PASS RETAINED on disk (audit 257e2619...); NOT a retroactive Class-1 conviction; §W4-1 SCHEMATIC tagging was HONEST; PASS-B carries the composite; only PASS-A's epistemic STANDING reclassified",
        },
        "substitution_chain_conclusion": "post-W5-3 eligible set = {PASS-B}; |set|: 2 -> 1 (narrows); eligibility rests on PASS-B ALONE [required-direction-claim verified]",
        "verification_on_disk": v,
        "annotation_application_status": ann_status,
        "slot_resolution": "content-anchored (§VII.AR heading + unique PASS-A/Joint/Level-pin substrings); plan-pinned line ~17337/~17380 STALE-drifted per substrate-first-canonical-sourcing.md §(ii.B)",
        "M1_M4_self_classification": {
            "M1_artifact_existence_with_content": True,
            "M2_registry_write_plus_sha_no_numerical_compute": True,
            "M3_verbatim_from_closed_W5-3_compute_plus_pre-registered_E5_S-1_V.2": True,
            "M4_allowlist_append": "ORCHESTRATOR-ONLY (index lists W5-3(E5-annotation); flagged in WP; NOT edited by this script)",
        },
    }
    OUT_JSON.write_text(json.dumps(record, indent=2, ensure_ascii=False), encoding="utf-8")
    print(f"  JSON sidecar -> {OUT_JSON.relative_to(ROOT)}")


def _emit_plot(v, verdict):
    fig, ax = plt.subplots(1, 1, figsize=(11.0, 5.2))
    labels = [  # (local)
        "PASS-A inline\nfloor reclass",
        "dated\nannotation block",
        "joint dual-ann\nscope correction",
        "PASS-B carries\neligibility",
        "E5 scope-corr\n(both categories)",
        "S92 §W4-1\nverdict permanence",
        "annotation in\n§VII.AR block",
    ]
    vals = [  # (local)
        int(v["floor_inline_present"]),
        int(v["annotation_block_present"]),
        int(v["joint_correction_present"]),
        int(v["pass_b_carries_eligibility_present"]),
        int(v["e5_both_categories_present"]),
        int(v["verdict_permanence_note_present"]),
        int(v["annotation_in_ar_block"]),
    ]
    colors = ["C2" if x else "C3" for x in vals]  # (local)
    ax.bar(labels, vals, color=colors, alpha=0.85)
    ax.set_ylim(0, 1.25)
    ax.set_yticks([0, 1])
    ax.set_yticklabels(["ABSENT", "PRESENT"])
    ax.set_ylabel("artifact-existence predicate (on-disk)")
    for i, x in enumerate(vals):
        ax.text(i, x + 0.05, "OK" if x else "MISS", ha="center", va="bottom", fontsize=8,
                color="C2" if x else "C3", fontweight="bold")
    ax.set_title(
        f"{GATE_ID}\n"
        f"§VII.AR PASS-A -> METHODOLOGY-FLOOR-ONLY (S93 W5-3 FULL-tier FALSIFICATION; dual-prior Track B 0.90)\n"
        f"STAGE-3 eligibility rests on PASS-B (A_5_extended, ρ_S_FULL=1.0) ALONE; "
        f"S92 §W4-1 composite PASS RETAINED (verdict permanence)\n"
        f"composite verdict: {verdict}",
        fontsize=8.0,
    )
    ax.grid(True, axis="y", ls=":", alpha=0.4)
    fig.tight_layout()
    fig.savefig(OUT_PNG, dpi=130, bbox_inches="tight")
    plt.close(fig)
    print(f"  PNG -> {OUT_PNG.relative_to(ROOT)}")


if __name__ == "__main__":
    sys.exit(main())
