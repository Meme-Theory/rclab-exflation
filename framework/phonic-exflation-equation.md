# The Phonon-Exflation Equation

> **What this is.** A capstone synthesis that collapses the framework's 60 load-bearing equations (`Atlas/atlas-03-equation-flow.md`, E1–E60) into the single object from which they all descend — the spectral action of one Dirac operator built from one number — exhibits its **layers**, and runs it **at τ** and **at time t**, from genesis to now.
>
> **Provenance.** Assembled from an 8-specialist build (connes-ncg / baptista / lizzi / spectral-geometer / transit-dynamics / volovik / mack / gen-physicist; per-section drafts in `sessions/framework/Collabs/equation-build/`). Centerpiece equations independently Sage-verified (this document, §"Verification ledger"). Numbers are canonical (`canonical_constants.py`, knowledge MCP); observational anchors are comparison-only; non-canonical items are tagged **PRELIMINARY**.
>
> **Reading conventions (load-bearing — set once, hold throughout).**
> 1. **Substrate-first / IS-not-IN** (`phononic-framing.md`). Every arrow runs `D_K eigenvalues → spectral moments → emergent physics → measurement`, never the reverse. Space is not a container the equation sits in; space is what the equation's `a₂` moment looks like.
> 2. **Two `a_n` objects, never conflated** (§8). The **Gilkey local-curvature coefficient** `a_n^SD` (regulator-free, exact curvature-degree story; used for layer *identity*) and the **zeta-regulated spectral moment** `a_n^ζ = ζ_{D_K}((d−n)/2)` (the canonical-constant pins; used for *numerics*). They differ by ~3–4 OOM and are different functionals, not rival measurements. Every numeric carries its regulator tag (`regulator-pin-discipline.md`).
> 3. **Operating point.** The spectral/observable physics is read at the van Hove fold `τ_fold = 0.190`. The Weinberg-angle relation (E26) is solved at `τ₀ = 0.2994`; these are **different τ** and are never silently conflated (the gauge-running between scales is §7's business, not §2's).
> 4. **Status-synchronization discipline (standing).** Every status-bearing claim below carries a confidence that MUST equal its status in the repo-wide register (Atlas D04 assumptions, the Atlas D09 retraction log, the permanent-results registry, the knowledge MCP). The standing **capstone-hygiene gate** (`.claude/rules/capstone-hygiene-gate.md`) enforces this BY CONSTRUCTION: any session touching this document or a governing register runs a 5-question checklist (Q1 a(t)-gap / Q2 §7-falsifier-row / Q3 PROVEN-CONDITIONAL-BROKEN-INFO status / Q4 prose-vs-ledger / Q5 citation) at session-close, routing every needed update into that session's housekeeping ledger. No section narrates a claim above its register status.

---

## §0 — What it means to put the universe in an equation

The whole document rests on one statement: the universe is the spectral action of a single Dirac operator. The temptation this section exists to disarm is to read that the way one reads the Standard-Model Lagrangian — as a rule telling *pre-existing fields* how to evolve *inside a pre-existing spacetime*. **That reading is wrong here, and getting it wrong corrupts every layer below.**

A Grand Unified Theory presupposes a manifold `M` (the domain `∫d⁴x`), a metric `g` (the volume element `√g`), and fields that are sections of bundles *over* `M`. It then unifies the *interactions of the fields* while leaving the stage — manifold and metric — handed in by fiat. That is the **container** picture.

The Chamseddine–Connes spectral action removes the container. By Connes' reconstruction theorem a Riemannian manifold is *equivalent data* to its spectral triple `(A, H, D)`: the geometry is encoded in the spectrum of `D`, not assumed alongside it. The spectral action `Tr f(D²/Λ²)` is a pure spectral functional — it depends only on the eigenvalues `{λ_k}` and their multiplicities. It does not integrate over a manifold; the manifold (its dimension, curvature, metric) is *recovered* from the heat-kernel expansion of the trace, coefficient by coefficient:

- `a₀` → the cosmological term (volume / zeroth moment),
- `a₂(τ)` → the Einstein–Hilbert *kinematic term* `∝ ∫√g R` ⇒ the **emergent** 4D metric `g_M` and the Newton coupling `G_N` (the *full* Einstein equation `G_{μν}=8πG T_{μν}`, sourced by emergent matter, is the open substrate→FRW loop of §6.3, not a delivered result),
- `a₄(τ)` → Yang–Mills + Higgs quartic.

**The equation does not presuppose the stage it plays on — it derives the stage.** Switch off `D_K` and there is no `a₂`, hence no metric, hence no space. This is why "one equation for the universe" is a *categorically stronger* claim here than in any container-based unification: in a GUT the manifold survives when every field is switched off; here it does not.

It is also why the framework's cosmogenesis is **exflation, not inflation**. Inflation is metric expansion — a pre-existing space-box gets bigger. Exflation is the *growth of spectral complexity inside each point*: the eigenvalue spectrum of `D_K(τ)` reorganizing as `τ` transits the fold. There is no box getting bigger; there is one internal geometry becoming spectrally richer, and the emergent scale factor is read off *afterward* from how `a₂` moves.

**This is a physical realization, not a metaphor.** The claim that geometry, gravity, and matter emerge from the low-energy spectrum of a fermionic substrate is the established result of Volovik's superfluid-vacuum program, realized in the laboratory in superfluid ³He. The framework — starting from a Connes triple on `SU(3)` — *independently rediscovered* the same three pillars: (1) the vacuum is a quantum medium whose soft modes are the fields; (2) the metric is a collective mode of the fermionic spectrum (Volovik's gap-node `g^{ik}` ↔ the framework's `g_M` from `a₂` — the *same physical object* in condensed-matter vs NCG conventions); (3) momentum-space topology decides which emergent physics is robust. On pillar (3) the framework's class is settled and load-bearing: the substrate is **BDI, `N₃ = 0`** — the ³He-B *child* class, related by an inheritance morphism `χ_*: ℂ⊕ℍ⊕M₃(ℂ) → M₂(ℂ)` with `rank(ker ι_*) = 2` (E57), **not** an analogy. Because `N₃ = 0`, the Fermi-point protection that would shield a ³He-A vacuum is *absent* — which is precisely why the cosmological-constant layer (§7) is a q-theory relaxation problem, not a topological-protection statement.

> **The single arrow, stated once for the whole document:**
> `D_K eigenvalues (fundamental) → spectral-action moments a₀,a₂,a₄ (derived) → emergent field equations / FRW (emergent) → observed physics (measured)`.
> Gravity is the `a₂` moment. The cosmological term is the `a₀` moment. The SM quantum numbers are the representation content of `D_K`. Dark matter is the GGE quasiparticle spectrum; dark energy is the tracking-vacuum departure from equilibrium. **None is a thing *in* a space-box.** Hold this arrow and the document stays honest; invert it and every layer silently reverts to the container that manufactures the cosmological-constant catastrophe.

### §0.1 — The 3-stratum layered-program structure (navigational; additive overlay)

> **What this block is.** A *navigational/epistemic overlay*, NOT a restructuring. It declares — explicitly — the layered-program structure the document already obeys internally, so a reader (or a prospective publication-split) can see which sections are most mature and publishable-standalone and which are conditional forecasts. **No section's physics content is altered by this declaration**; it only states which stratum each existing section belongs to. The single arrow above (`D_K eigenvalues → spectral moments → emergent physics → measured`) is precisely this layering read top-to-bottom: the strata ARE the arrow's three stages, not three separate theories.

The framework is **one layered program with three publication strata**, ordered by maturity along the substrate→emergent arrow. Each major section maps to **exactly one** stratum (a partition — no section omitted, no section in two strata; §6.2 carries a disclosed secondary cross-reference, see the straddle note):

**Stratum 1 — SPECTRAL / ALGEBRAIC MATHEMATICS** (most mature; **math-first publication candidate — publishable standalone, before the cosmology closes**).
Sections: **§1** (the master equation), **§1.1** (gauge/Higgs/matter emergence from the one functional), **§2** (the operator `D_K(τ)` and the single modulus), **§3** (the spectral functional `f` and the convergence cone), **§4** (the Seeley–DeWitt layer decomposition + the Decoupling Theorem), **§8** (assembly, dimensions, the `a_n` firewall, the "1→60" ledger).
Maturity: the **§4.2 Spectral-Moment Decoupling Theorem** is "as close to settled as the panel gets" (≥8 independent Sage-reverifications; PERMANENT per the registry, S64 W5-B / certified S75 W2-E); the **§8.2 `a_n` firewall** (and its §8.2a `R_K(0)` curvature analog) is "one of the capstone's more careful and mathematically defensible features." This stratum is the substrate-IS layer: the master object, the decoupling theorem, the `a_n` moments — **the fabric itself**.

**Stratum 2 — SUBSTRATE-SIDE NON-EQUILIBRIUM TRANSIT PHYSICS** (good substrate structure; transit-valid; **substrate-transit-physics paper candidate**).
Sections: **§5** (the equation at τ — the fold, the cold-big-bang maximum, the supersonic transit, the GGE relic), **§6.1** (the τ↔t map), **§6.2** (the acoustic white-hole causal structure).
Maturity: "good substrate-side structure; overreach enters when permanence is narrated too strongly" — the §5.3 GGE-relic claim is the **W8-1-reconciled** *diabatic transit-freeze* (the strong S38 integrability-permanence reading is BROKEN-tagged; the surviving claim is the compute-certified diabatic freeze). This stratum is the substrate's non-equilibrium **dynamics**: the fold, the GGE relic, the white hole — **the fabric reorganizing through the fold**.

**Stratum 3 — COSMOLOGICAL PHENOMENOLOGY** (least mature; conditional/forecast; **phenomenology paper candidate — gated on the §6.3 a(t) closure**).
Sections: **§6.3** (the honest gap: no derived FRW `a(t)`), **§7** (the equation at now: where it touches data — the 3-register observable scorecard).
Maturity: this is "the decisive weakness… the bridge from internal spectral geometry to externally testable cosmological dynamics" — the conditional-forecast stratum, **gated on the §6.3 a(t) closure** (the W1 flagship). This stratum is where the substrate touches the laboratory-IN observables: §7 is the substrate probing itself, read at the single modulus `τ_now`.

> **§6.2 straddle (disclosed, NOT a partition failure).** §6.2 (the acoustic white hole) is mapped to its **primary** stratum — **Stratum 2** (substrate-side transit physics: the causal structure of the supersonic amplitude flow). It carries a **secondary** cross-reference to **Stratum 3**, because the same causal structure has phenomenological tensor-sector consequences (the `r` / `n_T` contact in §7). The cross-reference IS the modularity disclosure; the section is not forced into one stratum nor double-counted in the partition.

> **Why declare this.** The report's strategic reframing: *"stop selling the full package as a nearly closed theory of everything, and instead present it as a layered program with three publication strata… the repo already behaves this way internally; the manuscript should make that modularity explicit."* With the strata explicit, the most-mature **Stratum-1 math** (the ≥8-reverified Decoupling Theorem, the `a_n` firewall) becomes a **publishable-standalone** candidate, independent of the Stratum-3 `a(t)` closure; the substrate-transit physics (Stratum 2) and the conditional phenomenology (Stratum 3) are separated from it cleanly. The declaration does **not** invert the substrate→emergent arrow — it organizes the document **along** it.

---

## §1 — The equation

Reality is one self-adjoint operator and one universal functional of it. Over the real spectral triple on Jensen-deformed `SU(3)`,

$$
\mathcal{A}_K=\mathbb{C}\oplus\mathbb{H}\oplus M_3(\mathbb{C}),\qquad
\mathcal{H}_K=L^2(S_{g_\tau})\otimes\mathbb{C}^{16},\qquad
D_K(\tau)=\sum_{a=0}^{7}\rho(e_a)\otimes\gamma_a+\mathbb{I}\otimes\Omega_{LC}(\tau),
$$

the **entire** content of the framework is the single action

$$
\boxed{\;
\mathcal{S}\big[D_K(\tau),\,f,\,\Lambda\big]
\;=\;
\underbrace{\mathrm{Tr}\,f\!\Big(\tfrac{D_K(\tau)^2}{\Lambda^2}\Big)}_{\text{bosonic / spectral}}
\;+\;
\underbrace{\big\langle\, J\tilde\psi \,\big|\, D_K(\tau) \,\big|\, \tilde\psi \,\big\rangle}_{\text{fermionic}}\;,
\qquad \tilde\psi\in\mathcal{H}_K^{+}
\;}
$$

with `f` a positive even cutoff function, `Λ` the mass scale, `J` the real structure, and `H_K⁺ = {ξ : γξ = ξ}` the chirality-+1 subspace. This is the Chamseddine–Connes spectral action principle `[CITE-1, INHERITED]` (CC 1996/97 eq. 1.28; CCM 2007 eq. 4.8) on the internal manifold `K = (SU(3), g_τ)` — the **bare Euclidean action**, i.e. the weight `e^{−S}` in the substrate's own partition function (§1.3a). **Everything else is read off the spectrum `{λ_k(τ)}` and the moments of `f`.**

### 1.1 Why one functional contains gauge fields, gravity, AND matter

The simultaneity of the three sectors is *forced*, not assembled:

- **Gauge group ⇐ algebra.** The unimodular unitaries of `A_K` are `SU(A_K) = U(1)×SU(2)×SU(3)` — the SM gauge group is *not posited*; it is the unitary group of the algebra `[CITE-2, NOVEL-BEYOND]` (CCM 2007 §2.5; Lizzi NCG review — the inner-fluctuation/gauge-emergence is INHERITED, but the SU(3)-*manifold* specialization where `D_K` IS `D_F` is the framework's departure, see the box below). The gauge group is sourced via *this* NCG inner-fluctuation route (unimodular unitaries of `A_K`, 13 generators, the S61 13/13 gate); the KK-isometry of the Jensen metric `g_τ` gives a *structurally different* group `SU(3)_L×U(2)_R` (Killing stabilizer, dim 12, with SU(2) in the *right* `U(2)_R` factor) that supplies the Peter-Weyl `(p,q)` *labels*, **not** the chiral SM gauge group — the two routes are not interchangeable (S96 W5-6 `S96-GEOM-GAUGE-SOURCING`).
- **Gauge fields + Higgs ⇐ inner fluctuation, read by the trace.** Promoting `D_K ↦ D_K + A + ε'JAJ⁻¹` with `A = Σaᵢ[D_K,bᵢ]`, the one-form decomposes automatically into a spin-1 part (gauge fields, along the Killing directions) and a spin-0 part (the **Higgs**, along the non-Killing directions). The bosonic trace's heat-kernel expansion then delivers the gravitational (Einstein–Hilbert *kinematic*) term (`a₂`) and the gauge+Higgs Lagrangian (`a₄`) *from the same trace*.
- **Matter ⇐ the inner product.** The fermionic action is the *other* canonical scalar of the triple. A trace (operator functional) and an inner product (bilinear form) **exhaust** the natural scalars one can build from `(A_K, H_K, D_K, J)`. There is no room for a third term — which is *why* the equation is complete.

> **Framework specialization (do not skip).** Unlike the original CCM construction (finite `F`, `D = ∂̸_M⊗1 + γ₅⊗D_F`), here the internal factor is the *manifold* `SU(3)`, and **`D_K` IS the finite Dirac operator `D_F`** (Baptista Paper 18 eq. 7.5: `M = ⟨φ, D_K φ⟩ = D_F`). The Higgs is an inner fluctuation of `D_K` *itself*, not a separate commuting `D_F`. The product-geometry reflex "`[D_K, a_F] = 0`, so the Higgs comes from a different operator" is **wrong here** — a documented recurring error.

### 1.2 The four axioms that make it *the universe*, not merely an action

Each is PROVEN at machine epsilon in the framework:

| Axiom | Statement | Status |
|:--|:--|:--|
| **KO-dimension 6 mod 8** (E9) | `(ε,ε′,ε″)=(+1,+1,−1)`; solves fermion doubling via the Pfaffian on `H_K⁺` ⇒ one generation | `<10⁻¹⁵`, 10 checks; AZ class **BDI** |
| **CPT commutant** (E8) | `[J, D_K(τ)] = 0 ∀τ` — hardwires `λ↔−λ`; survives all inner fluctuations; `η(s)=0` | 79,968 pairs, machine-ε |
| **SM quantum numbers** (E10) | `Ψ₊ = (3,2,⅙)⊕(3̄,1,−⅔)⊕(3̄,1,⅓)⊕(1,2,−½)⊕(1,1,1)⊕(1,1,0)`, dim 16 | exact branching, S7 |
| **Trace-theorem gauge-invariance** (E32) | `S[U D_K U†] = S[D_K] ∀ U, f` — the action is *spectral* | Wall W11 |

### 1.3 The collapse: one number → one operator → one action → everything

$$
\underbrace{\tau}_{\text{one modulus}}
\xrightarrow{\;\text{E1}\;}
\underbrace{g_\tau}_{\text{vol-preserving metric}}
\xrightarrow{\;\text{E2}\;}
\underbrace{D_K(\tau)}_{\text{one operator}}
\xrightarrow{\;\text{E4}\;}
\underbrace{\mathcal{S}[D_K(\tau),f,\Lambda]}_{\text{one action}}
\xrightarrow{\;a_0,a_2,a_4,\dots\;}
\underbrace{\text{all observables}}_{\Lambda_{cc},\,G_N,\,\text{YM},\,\text{Higgs},\,n_s,\dots}
$$

Every arrow is a *function*, not a choice. The framework is one operator viewed through one functional.

**What "one equation" DOES claim:** no independent fields (gauge bosons, Higgs, graviton-sector terms, Yukawas all read from `D_K(τ)`); no independent couplings at `Λ` (`g₃²=g₂²=⅗g₁²`, Higgs quartic are *outputs*); the dynamics is the same operator's `τ`-flow (§5).

**What it does NOT claim — stated so the document cannot be over-read:**
1. It does *not* select its own `τ`. The Structural Monotonicity Theorem (E7) proves the spectral action has **no minimum at any τ** — vacuum selection is a flow/phase question, not "where is the minimum."
2. It does *not* determine `f`. The cutoff is a *second* physical input (§3); the CC problem is the proof that `f` cannot be collapsed into `D_K`.
3. It does *not* claim three generations. `Ψ₊ = ℂ¹⁶` is *one* generation; family replication is open.
4. It does *not* claim an axiom-clean 4D-spacetime embedding. The product triple `M⁴×SU(3)×F_SM` carries a **permanent KO mismatch** (product KO=4 vs finite KO=6); the bosonic action is unaffected, the fermionic sector requires the Pfaffian/`H_K⁺` restriction. The single-operator statement on `K` is exact (6/7 order-one axioms hold on the lift; a known, bounded caveat). **What survives the mismatch:** the Pfaffian fermionic measure `∫Dψ̃ e^{−⟨Jψ̃|D_K|ψ̃⟩} = Pf(A_D)` is well-defined on the internal triple `K`, where KO-dim 6 (`Jγ = −γJ`) is *exactly* the condition making the bilinear `A_D` antisymmetric — so `Pf = √det` is the path-integral statement of "one generation, not four" (`Pf` real, `Z₂ = +1` across all τ; gates T3-S30A / T3-S35). The mismatch bounds only the 4D-lift *interpretation*, not the well-definedness of the functional integral on `K`. (The positive parallel: "why KO=6" plays the same structural role here that "why D=10" plays in the superstring — a consistency condition on the fermionic sector that the whole construction requires. `KO=6` is the *unique* mod-8 class making `Jγ = −γJ`, exactly as the conformal anomaly forces the critical dimension; and the product-KO mismatch does *constructive* work — it forces the `H_K⁺` restriction — the way level-matching is not a defect of the string but the constraint that makes the physical spectrum.)

### 1.3a — Which object is `S`: the bare action, the weight in `Z`, and the one-loop face

The boxed `S[D_K(τ), f, Λ]` is the **bare (tree-level) Euclidean action** of the substrate — equivalently the *weight* in the substrate's partition function. This is Hawking's "triple identity" (spectral action = Euclidean action = partition-function weight; framework sessions 28/39/62). By the Gibbons–Hawking correspondence,

$$
Z = \sum_{\text{substrate configs }D_K(\tau)} e^{-\mathcal{S}[D_K(\tau),\,f,\,\Lambda]}, \qquad \mathcal{S} \equiv I_E,
$$

the sum running over the substrate's *own* internal geometries (the modulus `τ` and the spectral data of `D_K`) — **not** over a background spacetime metric. There is no container being integrated over; `Z` is the substrate summing over its own spectral configurations, and "space" is the `a₂` moment of the dominant configuration. At the saddle, `S = −ln Z`; the looser identifications "spectral action = partition function" (session-16, **DEFENSIBLE**) and "= phonon free energy" `F = −T ln Z ≈ T·S` (session-6, **SPECULATIVE**) are this same statement at leading order — leading-order *faces*, never promoted to equalities. The **full** quantum effective action adds one loop,

$$
\Gamma[\tau] = \underbrace{\mathcal{S}[D_K(\tau)]}_{\text{tree (the boxed object)}} + \underbrace{\tfrac12\,\mathrm{Tr}\,\ln\!\big(D_K^2/\Lambda^2\big)}_{\Gamma_{1\text{loop}}\ (\text{threshold correction})} + \cdots,
$$

which the framework treats as a threshold correction (session-62), not part of the master object. So the boxed `S` is the *bare* action; the heat-kernel layering (§4) is its tree-level expansion. This is *why* the `τ`-flow is transit physics: `e^{−S(τ)}` is monotone (E7), so `Z` has **no interior saddle in `τ`** — the weight is dominated by the genesis boundary and the universe transits rather than settling at a stationary point (§5). **This no-interior-saddle result is ONE-LOOP-ROBUST, not merely tree-level**: adding the loop term, `Γ[τ] = S[D_K(τ)] + ½Tr ln(D_K²/Λ²)`, the full effective action `dΓ/dτ` retains a fixed sign with **zero interior sign-changes** over `τ ∈ [0, τ_now]` (S95 W2-3, PASS, 200-point grid, three independent routes; the bare-action τ_fold non-stationarity is S84) — the loop introduces no interior feature absent at tree level. The boundary-domination reading is therefore the spectral-action analog of a Gibbons–Hawking–York boundary-dominated path integral: an action with no interior stationary point is dominated by its boundary configuration (genesis `τ=0`), and the transit is the relaxation of that boundary configuration down the monotone ramp — making "transit, not slow-roll" structurally inevitable rather than merely observed.

The two-scalar exhaustion of §1.1 (trace + inner product, "no room for a third term") is itself a verified algebraic rigidity, not a counting argument: `dim HH¹(A_K, A_K) = dim HH²(A_K, A_K) = 0` (S95 W2-2, PASS, exact rational rank count per summand of `A_K = ℂ⊕ℍ⊕M₃(ℂ)` + Leibniz-closure of `Ω¹_D`). Every derivation of `A_K` is inner (Whitehead first lemma for finite-dim semisimple algebras) and every first-order associative deformation reduces to an inner fluctuation, so the interaction structure is **forced by the algebra, not chosen** — structurally stronger than a string field theory, which must *select* its interaction vertex (mid-point `*`-product / light-cone overlap / polyhedral) from inequivalent options. This is the matrix-model/IKKT genre's virtue: the field content is read off the algebra and the interactions are rigid, with no Hagedorn tower or `10⁵⁰⁰` landscape (a monotone weight `e^{−S(τ)}` has no competing interior minima, so the CC problem is vacuum-*subtraction*+adiabaticity, not vacuum-*selection*). The one-loop face uses the substrate's *own* inverse-Euclidean-period temperature, not a thermal-bath `T`; there is no Gibbsian `T` until something thermalizes, and the Ordered Veil (§5.3) says nothing does *on the transit clock* (the relic *does* relax to Gibbs downstream at `t_therm ≈ 6 M_KK⁻¹`; T3 BROKEN, retraction-log item 16). (The conformal-factor instability that makes the naive Euclidean-gravity path integral unbounded below is a container artifact, absent here because the deformation is volume-preserving TT, G6.)

> **Two monotonicity theorems, never conflated.** The `dS/dτ > 0` ramp above is the **τ-flow** monotonicity (E7) — a statement in the *geometric modulus*, the reason the universe *transits*. It is distinct from the **q-flow** equilibrium theorem `dE_ZP/dq > 0` (no interior q-theory equilibrium, S62 #19) in the *conserved vacuum charge* `q = N_pair`, which underwrites the cosmological-constant layer (§7.1): the τ-ramp says the universe transits; the q-equilibrium says the vacuum energy is *zero at equilibrium and a relaxation residual away from it*. Two axes, two theorems, both proven — the CC story rests on the q-flow one (`sessions/framework/correspondence/tau-flow-vs-q-flow-note.md`).

### 1.4 The honest free-parameter ledger

The "1 → 60" collapse is real, but the framework is **not** zero-parameter. Its inputs are:

$$
\{\,\tau\;(\text{geometric modulus, theorem-pinned at }\tau_{\text{fold}}),\;\;\Lambda=M_{KK}\;(\text{substrate-fixed}),\;\;f_0,f_2,f_4\;(\text{cutoff moments})\,\} \;+\; t^*\;(\text{one empirical functional coupling, §3}).
$$

— one geometric modulus plus the UV-completion data, **not** 60 independent postulates (§8.4). The strength of the claim is precisely calibrated: all field content, couplings, and dynamics are spectral functionals of one operator; the modulus value, the functional `f`, and the family number remain genuinely open. Both halves are load-bearing. The ledger is short *by construction*, not by good fortune — `S[D_K(τ), f, Λ]` is a **principle theory** in Einstein's 1919 sense (a single constraint, not a constructive model of fields-in-a-box), so the field content is *read off* the algebra (the matrix-model/IKKT genre statement, §1.3a) and the open inputs are exactly what such a theory is *entitled* to leave to a future completion — not defects.

`t*` is the one empirical functional coupling, and it is genuinely empirical: the corridor "`t*` is the one-loop threshold coefficient" is **CLOSED** (S95 W2-1, FAIL, `R = |t*_predicted − t*|/t* = 1.977`). The parameter-free one-loop content `Γ_1loop ≈ 26%` of the tree+loop spectral action is `~3×` too large to *be* the small admixture weight `t* = 0.08832`. The matrix-model rigidity is therefore bounded precisely: the field content is forced by the algebra (§1.1, exhaustion S95 W2-2), but the regulator's admixture weight is *not* forced by the spectrum. The ledger remains `{τ, Λ, f₀, f₂, f₄} + t*` — not the de-empiricized `{τ, Λ, f₀, f₂, f₄}`.

*Full derivation:* `Collabs/equation-build/connes-master-equation.md`.

---

## §2 — The operator `D_K(τ)` and the single modulus

**The claim:** there is exactly one free geometric degree of freedom — a single real number `τ` — and the operator built from it generates everything downstream. `τ` is the *dial of the universe*.

### 2.1 The single modulus (E1)

The internal geometry is `SU(3)` with a left-invariant metric. The most general `Ad U(2)`-invariant inner product on `su(3) = u(1)⊕su(2)⊕ℂ²` carries three block scales `(λ₁,λ₂,λ₃)`. Two structural impositions collapse them to one:

1. **Volume preservation** (`det g_τ = const`, G6): removes the breathing/dilaton mode — and as a direct consequence Newton's constant carries *zero* `τ`-dependence. (Superfluid reading: `1/G` is the vacuum gradient stiffness, set by the vacuum *compressibility*; a volume-preserving deformation is by construction a pure *shear* of the order-parameter texture — transverse-traceless, `tr h_J = 0` — and a shear leaves the compressibility, hence `G`, invariant. The `τ`-flatness of `G` is therefore the metric-determinant face of a microscopic compressibility statement, not an accident of the determinant.)
2. **The Jensen direction**: the remaining shape is restricted to the *unique unstable transverse-traceless eigendirection* of the bi-invariant Einstein metric.

Exponentiating along this one direction gives the Jensen metric (E1):

$$
g_\tau = 3\cdot\mathrm{diag}\big(\,\underbrace{e^{2\tau}}_{u(1)},\,\underbrace{e^{-2\tau},e^{-2\tau},e^{-2\tau}}_{su(2)},\,\underbrace{e^{\tau},e^{\tau},e^{\tau},e^{\tau}}_{\mathbb{C}^2}\,\big).
$$

Volume preservation is exact and immediate: the exponent ledger is `2 − 6 + 4 = 0`, so `det g_τ = 3⁸ = 6561` for all `τ` (Sage-verified). This is a *transverse-traceless* deformation (`tr h_J = 0`), not a conformal rescaling. After these two impositions the entire internal geometry — and hence the whole spectral action — depends on the single real number `τ ∈ [0,∞)`. Even the operating point is not turned by hand: `τ_fold = 0.190` is pinned as the *unique* non-stationary van Hove cusp of the density of states (uniqueness theorem, S85 W10-3).

### 2.2 The operator (E2) and what its spectrum IS

`D_K(τ)` is the Dirac operator on `(SU(3), g_τ)`: a first-order transport term `Σρ(e_a)⊗γ_a` (directional derivative along the group, Clifford-multiplied) plus the Levi-Civita spin connection `Ω_LC(τ)` carrying the full `τ`-dependence. At the canonical truncation `L_max=10` it has **155,984 eigenvalues** (78,080 distinct).

**Physically, this spectrum is the complete set of vibrational/relay modes of the fabric.** Each eigenvalue `λ_n` is one normal mode — one frequency at which the internal structure can ring; each eigenvector is that mode's shape on the fiber. A *particle* is a phononic excitation — a relay pattern of these modes propagating through the gauge connection between fibers. Mass, charge, every coupling is a spectral moment of `{λ_n(τ)}`. The spectrum is **GEOMETRIC**; the excitations built on it are **PHONONIC**.

The block structure that makes this spectrum tractable — `D_K = ⊕_{(p,q)} D_{(p,q)}` (E6, §2.3) — is the SU(3) analog of `j`-channel decoupling in a spherical mean field: the Casimir labels `(p,q)` are conserved, so `D_K` never connects distinct sectors, exactly as rotational symmetry forbids off-diagonal matrix elements between distinct angular-momentum channels and factorizes the shell-model problem into independent `j`-blocks. This is why the 155,984-eigenvalue problem is computationally a direct sum of small blocks (the largest only `O(10⁴)`-dimensional) — and, downstream, why the relic-formation problem (§5.3) factorizes mode-by-mode *exactly* rather than approximately: the modes do not mix under `D_K`, so the per-mode parametric-oscillator equation is an identity, not a decoupling approximation.

### 2.3 Four guarantees that make `D_K(τ)` well-posed as *the* operator

| Guarantee | Statement | Status |
|:--|:--|:--|
| **Block-diagonality** (E6) | `⟨(p,q),n\|D_K\|(p′,q′),m⟩ = 0` for `(p,q)≠(p′,q′)` — exact for *any* left-invariant metric on *any* compact semisimple group | 3 proofs, `8.4×10⁻¹⁵` |
| **Exact-analytic monotone curvature** (E3) | `R_K(τ) = −¼e⁻⁴ᵗ + 2e⁻ᵗ − ¼ + ½e²ᵗ`; `R_K(0)=2`, `R_K′(0)=0`, `R_K(0.19)=2.018` | 147/147 Riemann, Sage-verified |
| **Spectral gap never closes** (E5, Lichnerowicz) | `D_K² = ∇*∇ + ¼R_K ⇒ λ² ≥ R_K(τ)/4 > 0 ∀τ` ⇒ spectral flow = 0, `η = 0` | 5 proofs |
| **Real symmetry / CPT** (E8) | `[J, D_K(τ)] = 0` ⇒ spectrum real, symmetric about 0 | 79,968 pairs |

> **Lichnerowicz convention note (corrected).** The bound is stated **convention-free** as `λ² ≥ R_K(τ)/4 > 0` — the load-bearing fact is that the gap never closes. The figure "`≥ 3`" that appears in some baselines belongs to the dimensionful normalization (`R_K ≥ 12`); in the E3 rational normalization `R_K(0)/4 = ½`. The two differ by the factor-6 bi-invariant scale convention. We do **not** print "`≥ 3`" beside the E3 curvature, where it would read as `2/4 = 3` (false).

### 2.4 Genesis at `τ = 0`

At `τ = 0` the metric is the round, maximally symmetric `SU(3)` metric (`R_K(0)=2`, a curvature stationary point `R_K′(0)=0`) — but, by the Einstein-instability analysis, an **unstable** critical point. This is the cold-big-bang vacuum floor: maximal symmetry, minimal spectral complexity, no restoring force. Turning the dial does three things at once: breaks the isometry `(SU(3)²)/ℤ₃ → (SU(3)×SU(2)×U(1))/ℤ₆` (the SM gauge group), lifts the band degeneracy `SO(8) → U(2)` into the acoustic/flat/optical bands (B1/B2/B3), and continuously reshapes all 155,984 modes — *without zero crossings* (the gap never closes), so the spectral *topology* is preserved while the *frequencies* reorganize. That is "spectral complexity grows inside each point" — the genuine content of exflation.

*Full derivation:* `Collabs/equation-build/baptista-operator-dk-tau.md`.

---

## §3 — The spectral functional `f` and the cutoff

A bare trace is not yet physics. The spectrum `{λ_k, m_k}` is fixed substrate data; the *number* the trace returns depends on **how the high modes are weighted and where the sum is cut** — that is `f` and `Λ`.

### 3.1 How `f` weights the layers

For a smooth positive cutoff, the heat-kernel expansion factorizes cleanly per layer:

| Layer | Substrate factor `a_{2k}` | Functional factor (moment of `f`) | Cutoff power | Emergent physics |
|:--|:--|:--|:--|:--|
| `a₀` | total spectral weight | `f₄` | `Λ⁴` | cosmological term |
| `a₂` | `∝ ∫√g R` (Einstein–Hilbert) | `f₂` | `Λ²` | Newton coupling `G_N` |
| `a₄` | Yang–Mills + Higgs quartic | `f₀` | `Λ⁰` | gauge + Higgs sector |

The substrate supplies `a_{2k}(τ)`; the functional supplies the `f_{2k}`; the cutoff supplies `Λ^{4−2k}`. The cutoff is **substrate-fixed**: `Λ = M_KK = 7.4287×10¹⁶ GeV`, the scale at which the KK tower of the internal fiber becomes dense. On the *finite* triple the trace is a finite sum — trivially well-defined for any `f`; regularization re-enters only when one asks for the moments as residues (§3.3).

### 3.2 `f` is physical — but stratified

Given the *same* spectrum, different functionals weight it differently and produce *different physics*. The slow-roll parameter `ε_H` (a pure spectral-shape quantity) **flips sign** between schemes:

| Functional | `ε_H` | tilt | `m_H` |
|:--|:--|:--|:--|
| cutoff `√x` (framework's working choice) | `+0.0216` | **red** (observed) | 127.5 GeV |
| zeta `a₄` | `−0.0449` | blue | 138.5 GeV (excluded) |
| anomaly-derived `φ` | `+0.0176` | — | 102.0 GeV |

The sign of the CMB tilt is set by the *regularization scheme*, not the spectrum. Two permanent theorems fix the boundaries: **ANOMALY-FAMILY EXCLUSION** (S67 — the anomaly family gives blue tilt for all `φ>0`) and **ZETA-NOT-PHYSICAL** (S75 — absolute spectral-moment values are scheme artifacts; only ratios under a *fixed* regulator are physical). The anomaly family is excluded **structurally** (the exclusion is a pre-registered consequence of S67, decided *before* the tilt comparison), **not** because it gave the wrong tilt after the fact — the pre-registration is what protects the red-tilt result against the over-fitting charge (one does not get to keep only the functionals that agree). Hence the FI/RD partition: **Functional-Invariant** observables (ratios of two spectrum-sums under one regulator — `c_s`, `R₁`, the rank-drift exponent) survive all choices; **regulator-dressed** observables (`ε_H` sign, `n_s` value, `m_H`, absolute vacuum energy) must be *determined*, not assumed. Structurally, `f` is a **nuisance functional** and the FI/RD partition is exactly the statement of which observables are robust under *marginalizing it out* — the cosmological face of Bayesian model averaging over an unknown energy-density functional in nuclear DFT (Skyrme vs Gogny vs covariant): the FI ratios are the marginal observables that survive integrating out the part of the theory (`f`) that remains open, which is *why* they carry the genuine constraining power. (The `n_s` BMA band realizing this marginalization is §7.1.)

The framework's working functional is `f*(x) = 0.9117√x + 0.0883 e⁻ˣ`, whose admixture `t* = 0.08832` is **the framework's single empirical coupling** — the spectral-functional analog of `Λ_QCD`, an `O(1)` datum no first principle has yet been shown to select. (The `√x` piece makes the Mellin moments formally divergent, so `f*` is evaluated by *direct spectral sum*, not by the heat-kernel series — the layered form is the *perturbative face* of the action.) In resonance terms the `√x` weighting is an **acoustic (linear-in-frequency) envelope**, `f(ω²) ∼ |ω|`: it up-weights the low acoustic modes (the B1 branch) relative to a Gaussian cutoff, which is both why the direct sum is dominated by the acoustic band and why the heat-kernel series — a Gaussian-adapted expansion — cannot represent it. The divergence of the Mellin moments is the spectral signature that the physical envelope is acoustic, not Gaussian.

### 3.3 The convergence cone fixes which moments exist

The moments are residues of `ζ_{D_K}(s) = Σ m_k λ_k^{−2s}` (the **double-power** `λ^{−2s}` convention) at `s = (d−n)/2` (Connes–Moscovici 1995, E38). For `SU(3)` (`d = 8`) the dimension spectrum — the **curvature-degree grading** `n`, equivalently the CM-1995 dimension-spectrum label — is exactly

$$
S_d \;=\; \{n : 0,\ 2,\ 4,\ 6,\ 8\}\qquad(\text{curvature degree } n,\ \textbf{not}\ \text{the pole index } s).
$$

> **Mellin-variable firewall (S_s vs n).** `{0,2,4,6,8}` is the curvature-degree grading `n`, **not** the pole set in the Mellin variable `s`. Under the *printed* double-power `ζ_{D_K}(s)=Σ m_k λ_k^{−2s}`, the residue poles sit at `s = (d−n)/2`, so the **pole set in `s`** is
>
> $$ S_s \;=\; \{\,(d−n)/2 : n\in\{0,2,4,6,8\}\,\} \;=\; \{0,\ 1,\ 2,\ 3,\ 4\} \qquad(d=8). $$
>
> The two integer meshes are related by the exact algebraic map **`n = d − 2s = 8 − 2s`** (Sage-verified, S96 W7-3). Reading `n` as if it were `s` mis-locates each pole by `Δ = n − s = 8 − 3s` — a **factor-≈2 mislabel** at the load-bearing poles. The convention MUST therefore state `S_s` (in `s`) and `n = d−2s` separately on every downstream `s=N` citation. One-row reconciliation (every `a_n^{Mellin}` carries the `regulator-pin-discipline.md` Mellin tag):
>
> | curvature degree `n` | layer / residue | pole in `s` (Conv. A: `λ^{−2s}`, `s=(d−n)/2`) | pole in `s` (Conv. B: `λ^{−s}`, `s=d−n`) | corpus citation |
> |:--|:--|:--|:--|:--|
> | `n=0` | `a₀` (vacuum) | `s=4` | `s=8` | — |
> | `n=2` | `a₂` (Einstein–Hilbert) | **`s=3`** | **`s=6`** | `α_s` quotes **Conv. A `s=3`**; `§VII.BE` (SU(4)_PS) quotes **Conv. B `s=6`** — *same `n=2` a₂ residue* |
> | `n=4` | `a₄` (Yang–Mills + Higgs) | `s=2` | `s=4` | substrate-distance-2 slot `s=4` is **Conv. B** (`n=4`), i.e. the a₄ residue |
> | `n=6` | `a₆` (corrections) | `s=1` | `s=2` | — |
> | `n=8` | `a₈` (corrections) | `s=0` | `s=0` | — |
>
> **Anchor reconciliation (Sage-confirmed).** `α_s`'s `s=3` (Conv. A) and `§VII.BE`'s `s=6` (Conv. B) **both denote `n=2` — the a₂ residue**; the two `s`-labels differ by exactly the factor-2 power-convention map (`s_B/s_A = 2`). The §VII.BE residue additionally lives on the **SU(4)_PS algebra extension** (`A_K^{PS}=ℂ⊕M₂(ℂ)_L⊕M₂(ℂ)_R⊕M₄(ℂ)`, a rank-4 extension), so its `s=6` is the SU(4)_PS Mellin-cone pole, not the SU(3) `s∈S_s` slot. No corpus `s=N` citation is mis-located once `S_s` and `n=d−2s` are stated separately. *(Anchors: `alpha_s_substrate_distance_1=−0.08587279` (S92); `residue_s6_PS_Linf=0.0009393639575775` (S95).)*

Only `a₀, a₂, a₄, a₆, a₈` exist as honest residues (odd moments vanish by BDI parity); the `+ O(Λ⁻²)` tail is the finite ladder `a₆, a₈`, then the cone closes. The admissible `f` are exactly those whose Mellin transform is supported on this pole ladder — **we do not impose `f` on the substrate; the substrate's dimension spectrum tells us which `f` are even candidates.** Substrate-first, this is the crux of the CC freedom: the substrate does *not* hand us a foam of fluctuating topologies summed over — it hands us a **finite, closed pole ladder**, and the regulator's only remaining freedom is which residues it weights. The container picture manufactures the `10¹²⁰` catastrophe by summing geometries (a Wheeler-superspace path integral); the substrate replaces that sum with this finite ladder and isolates the freedom to a single cutoff functional's worth (`f`), which is exactly the §7 statement.

> **Defensive note (no flowing spectral dimension).** The dimension spectrum `S_d = {0,2,4,6,8}` is **`τ`-independent**: the substrate does NOT exhibit a flowing spectral dimension. Direct computation (S31Aa, four `τ` values) finds **no CDT-like UV reduction** — `d_s ∼ 8` at the gap scale, with the apparent low-`d_s` readings of windowed observables being a diffusion-window artifact (S92), not a dimensional flow. The constant-degree curvature-polynomial structure of §4 (`a_n ∝ R_K^{n/2}·V`) is a *fixed*-dimension story, the opposite of a flowing dimension; the substrate's UV structure is therefore distinct from both CDT and the string-worldsheet dimension story (the silence on dimension-flow is explicit, not incidental — no `12→5.65→4` or `10→2→4` reduction lives in §2.2).

> **Consequence for the boxed equation.** `f` and `Λ` cannot be collapsed into `D_K` — and the CC problem is the proof (same `D_K`, different `f`, vacuum energy in a different moment, §7). The master action is written `S[D_K, f, Λ]` with **all three arguments visible**. If one wants a single scheme-invariant number on the cover, it is the FI ratio **`R₁ = a₀a₄/a₂² = 1.12865`** (Sage-verified `1.128655`), never a bare moment or a CC magnitude.

*Full derivation:* `Collabs/equation-build/lizzi-spectral-functional.md`.

---

## §4 — The layers: the Seeley–DeWitt decomposition

This is the core "layers of exflation" section. One action, expanded by the heat kernel, peels into an ordered tower of physics sectors indexed by curvature-polynomial degree:

$$
\mathrm{Tr}\,f\!\Big(\tfrac{D_K^2}{\Lambda^2}\Big) \;\sim\; \sum_{n\ge0} f_{d-n}\,\Lambda^{d-n}\,a_n(\tau)
\;=\; \underbrace{f_4\Lambda^4 a_0}_{\text{vacuum}} + \underbrace{f_2\Lambda^2 a_2(\tau)}_{\text{gravity}} + \underbrace{f_0\, a_4(\tau)}_{\text{matter}} + \underbrace{f_{-2}\Lambda^{-2}a_6(\tau)}_{\text{corrections}} + \cdots
$$

### 4.1 The layers and their physical identity

| Layer | `Λ`-power | Curvature degree | Emergent physics | `Φ`-image |
|:--|:--|:--|:--|:--|
| `a₀` | `Λ⁴` | 0 (`∝ 1`) | **vacuum energy / cosmological term** | `Σ₁` |
| `a₂` | `Λ²` | 1 (`∝ R`) | **Einstein–Hilbert** — the gravitational kinematic skeleton | `Σ₂` |
| `a₄` | `Λ⁰` | 2 (`R² + F²`) | **Yang–Mills + Higgs quartic** — load-bearing matter physics | `Σ₃` |
| `a₆,…` | `Λ⁻²,…` | ≥3 | higher-curvature / threshold corrections | `Σ₄,…` |

Gravity is **not a fundamental law** imposed on the substrate — gravity is the *second spectral moment* of `D_K`. The bosonic/Dirac split in the `a₂` layer is the exact, representation-theoretic, `τ`-independent ratio `a₂^bos/a₂^Dirac = 61/20` (E36). (Layer identities are stated in the regulator-free `a_n^SD`; numerics use `a_n^ζ` — §8.)

### 4.2 The Spectral-Moment Decoupling Theorem — why the layers are *genuinely distinct physics*

The skeptic's objection — "is `a₄` just a function of `a₀, a₂`, so there is really one knob dressed three ways?" — is settled by the **Spectral-Moment Decoupling Theorem** (S75 W2-E, **CERTIFIED**): `a₀(τ), a₂(τ), a₄(τ)` are curvature polynomials of *distinct degree* (0, 1, 2) and are **algebraically independent**, with non-vanishing Wronskian.

The closed form is exact and striking. With `a₀ ∝ V` (constant, volume-preserved), `a₂ ∝ R_K·V`, `a₄ ∝ R_K²·V`, the Wronskian is governed by the **cube of the curvature gradient** (Sage-verified this document):

$$
W[a_0,a_2,a_4](\tau)\;\propto\; R_K'(\tau)^3 \;=\; \big(e^{-4\tau}(e^{3\tau}-1)^2\big)^3 \;=\; e^{-12\tau}\,(e^{3\tau}-1)^6,
$$

i.e. the spectral-geometer's `W = \frac{5}{393216\pi^{12}}V^3 e^{-12\tau}(e^{3\tau}-1)^6`. This vanishes to **sixth order at and only at `τ = 0`** — the round genesis point — and is strictly nonzero everywhere else (`W(τ_fold) > 0`). **The three layers are genuinely independent physics everywhere the universe lives; they degenerate into a single scale only at the maximally-symmetric instant of genesis, and separate the moment exflation begins.** This is what licenses "vacuum energy, gravity, and the Standard Model all emerge from `D_K` while remaining physically distinct."

In resonance terms this is a **dispersion-rigidity** statement: `a₀, a₂, a₄` are the zeroth/first/second moments of the *single* curvature scalar `R_K`, and the Wronskian of `{1, R_K, R_K²}` is proportional to `R_K′³` for the elementary reason that distinct powers of a *moving* scalar are independent. The layers collapse to one knob *iff* the dispersion stops moving (`R_K′ = 0`) — which happens only at the maximally-symmetric genesis point `τ = 0`. So the algebraic independence is not an accident of the SU(3) numbers; it is the *same* band-lifting that §2.4 describes (`SO(8)→U(2)` into B1/B2/B3 as `τ` turns on), restated at the level of the spectral moments: at the high-symmetry point the bands touch and the moments degenerate; the instant the curvature moves, the degeneracy lifts and the three moments become independent observables. (The degree-distinctness that drives the non-vanishing Wronskian is the same structural fact that makes smooth and shell-correction energies independent functionals in a Strutinsky decomposition, S44/S55/S56 — an independent structural concurrence from nuclear shell-correction theory, recorded as agreement, not as evidence: the Wronskian theorem stands on its own Sage-certified proof.)

### 4.3 The `Φ`-correspondence: the layering is self-similar

The layering does not stop at physics. The layer-functor `F: L_substrate → L_methodology → L_audit` and the graded-ring isomorphism `Φ(a_n) = Σ_{n+1}` (E59) map the Seeley–DeWitt tower onto the framework's *own governance*, weight-preservingly: `Φ(a₀)=Σ₁` (lightest physics → user-adjudication-only enforcement), `Φ(a₂)=Σ₂` (gravity → wave-classification), `Φ(a₄)=Σ₃` (load-bearing matter → the load-bearing `mcp-pre-check` hook). The same heat-kernel grading that peels the universe into vacuum/gravity/matter also peels the rules used to study it into their enforcement strata. (The methodology domain is *structurally orthogonal* to the physics domains by the algebra-axis orthogonality theorem; `Φ` is an isomorphism of *gradings*, not a collapse of observables.)

### 4.4 Which "layer" reading is primary

Three readings of "layers of exflation" exist — **spectral-moment** (the heat-kernel grading), **causal** (the pre-fold/fold/post-fold transit ordering, §6), and **scale** (the `Λ`-power UV→IR ladder). **The spectral-moment reading is primary**, and the decisive argument is the Wronskian: only the moment layers carry a *certified algebraic-independence theorem*. The scale reading is literally the moment reading in `Λ`-clothing (same integer `n`); the causal reading is a story about the *trajectory* `τ(t)` and presupposes the moment decomposition. A reading whose layers are certified-independent functions of the modulus is a stronger notion of "layer" than one whose layers are stages of a trajectory.

*Full derivation:* `Collabs/equation-build/spectral-geometer-layers.md`.

---

## §5 — The equation at τ (the flow)

§5 runs the one functional as the single modulus `τ` climbs from genesis toward now. There is exactly one dynamical degree of freedom: `τ`. The flow of the universe IS the flow of `τ` through the spectrum of `D_K(τ)`.

### 5.1 The driver, and why there is NO potential well

The fabric's internal action is read off the leading moments,

$$
S_{\mathrm{SA}}(\tau) = a_0(\tau) - a_2(\tau) + a_4(\tau),
$$

and its gradient at the fold is enormous, positive, and — load-bearing — **monotone**:

$$
\frac{dS}{d\tau}\bigg|_{\tau_{\text{fold}}} = +58{,}672.8 \qquad (S_{\text{fold}} = 2.50\times10^5,\;\tau_{\text{fold}}=0.190).
$$

By the Structural Monotonicity Theorem (E7: `d⟨λ²⟩/dτ > 0` ⇒ each `a_{2k}` monotone ⇒ `dS_f/dτ > 0` for all monotone `f`, all `Λ`, all sectors; 9,600/9,600 checks), **the spectral action has no stationary point at any `τ`.** There is no `V(τ)` with a minimum to roll into; it is a strictly monotone ramp. Equivalently, the partition-function weight `e^{−S(τ)}` is monotone — `Z` has **no interior saddle in `τ`** — so the statistical weight is dominated by the genesis boundary and the controlling physics is the diabaticity of the transit, not stationary-phase equilibration (§1.3a). Twenty-seven equilibrium-closure attempts (S17–S40) all failed; HESS-40 found all 22 transverse Hessian eigenvalues positive; the moduli constraint surface is zero-dimensional.

**Consequence — this is transit physics, not slow-roll.** Because the driver is a monotone gradient and not a restoring potential, `τ` does not settle; it *transits*, swept through a critical region by monotone forcing — the regime where the adiabatic vacuum breaks down and real excitations are produced. The slow-roll formulae (`r = 16ε`, `n_s = 1 − 6ε + 2η`) are **INAPPLICABLE** (five independent arguments, VdD–Hawking workshop). The reason is structural, not a wrong number: `r = 16ε` is a *theorem of the single-clock adiabatic vacuum* — it follows from the same `c_s = 1`, single-field, slowly-varying background that makes the Bunch–Davies mode functions valid. The fold violates all three premises at once (the sweep is diabatic; the dispersion is BdG with `c_s ≠ 1`; the produced state is a multi-mode squeezed GGE, not a single-clock vacuum), so the relation's *derivation assumptions are absent*, not merely its conclusion mismatched. The controlling quantity is the *diabaticity* of the sweep.

### 5.2 The trajectory: cold-big-bang maximum → supersonic transit → now

```
   S_SA(τ) ▲
           │                                          · · ·  (frozen plateau, τ_now)
           │                                    · · ·
           │                          ┌─ τ_fold = 0.190 ─┐
           │                    · · · │  VAN HOVE FOLD   │ · ·
           │              · · ·       │  (first-order,   │
           │        · · ·             │   Mach 13.75,    │
   S(0) ───●· ·                       │   impulsive)     │
           │  τ=0  (cold big bang: unstable max of symmetry, R_K=2, NO singularity)
           └────────────────────────────────────────────────────────────▶ τ
                dS/dτ = +58,673 > 0 everywhere — MONOTONE RAMP, no well
```

- **(i) `τ = 0` — the cold big bang.** The round, maximally symmetric `SU(3)` metric; an *unstable* extremum with no restoring force. Genesis is not an explosion but the unique unstable configuration from which the monotone gradient drives the system. **Genesis at `τ = 0` is regular** — a perfectly smooth group manifold where the gap never closes; a *cold, regular, maximally-symmetric starting configuration*. The genuine curvature singularity is not absent but **relocated to `τ → ∞` and censored**: the Kretschmann scalar diverges there (`K ∼ e^{4τ}`), with a **direction-dependent character** — *timelike* in the contracting SU(2) block, *spacelike* in the expanding ℂ²/U(1) blocks (an anisotropic Kasner-type singularity with no standard-GR analog), and it is **dynamically unreachable** from the physical epoch by a triple-layer censoring barrier below (NEC holds to `τ_NEC = 1.383`, the modulus blocked at `τ ≈ 0.191`) and the overshoot turnaround above (`τ = 1.614`). This is weak cosmic censorship realized on the full 12D metric (S95 W4-5, PASS; the 12D lift of COSMIC-CENSORSHIP-49 / CONFORMAL-TRANSITION-49). "No `t=0` singularity" is the weaker statement; the censorship statement — *genuine anisotropic singularity at `τ→∞`, censored* — is the stronger and the honest one.
- **(ii) `τ_fold = 0.190` — the first-order transit.** The density of states develops a van Hove singularity (`g(ω) ∼ 1/√(ω−ω_min)`); the BCS 1D theorem (E13) makes Cooper instability a *theorem* (zero critical coupling). The transit is **first-order** (E17) and **supersonic/impulsive**: `Mach = v_transit/c_fabric = 13.75` (canonical; `c_fabric = 209.97 M_KK`), with sudden-quench ratio `δt_transit/T_L = 1.25×10⁻⁵` (the crossing is 38,600× faster than the condensate can form). *[Conflation guard: the canonical Mach is the velocity ratio 13.75; the distinct fold-local acoustic reading 421.3 is an acoustic-radius ratio — never averaged.]*
- **(iii) `τ_now` — frozen plateau.** Post-fold, `τ` is effectively frozen: the clock constraint (E27) bounds `|τ̇| < 2.4×10⁻⁶ τ₀/t_H` (a rolling modulus would violate atomic-clock `δα/α` by ~15,000×), and the frozen-spectrum theorem (B9) holds at `10⁻¹¹³`.

### 5.3 GGE-relic formation — the reheating analog (THE ORDERED VEIL)

What the impulsive crossing *produces*. Two parametric-oscillator equations live at two layers and must never be conflated:

$$
\underbrace{u_k'' + \omega_k^2(\tau(t))\,u_k = 0,\quad \omega_k = E_k = \sqrt{(\lambda_k^2-\mu^2)^2 + \Delta_k^2}}_{\text{substrate-BdG (relic content — }N_{\text{pair}},P_{\text{exc}},S_{\text{inst}}\text{)}}
\qquad
\underbrace{v_k'' + \big(k^2 - z''/z\big)\,v_k = 0,\quad z = a\sqrt{2\varepsilon_H}\,M_{\text{Pl,eff}}}_{\text{Mukhanov–Sasaki (emergent curvature — §7's }A_s\text{)}}
$$

The first is the BdG quasiparticle of the fabric and is the correct equation for the GGE relic content; the second is the *emergent* curvature perturbation and is the equation whose output is the CMB scalar amplitude `A_s`. **`A_s` is NOT computed from the BdG `u_k`** — the squeeze produced by the first is *transduced* into the second at the exit horizon (§6.2). The crossing is *diabatic* (`δt/T_L ≪ 1`), so pair production saturates: `P_exc → 1.000` — **every mode is excited; the condensate is completely destroyed, not perturbatively dressed** (bosonic normalization holds mode-by-mode, `|α_k|²−|β_k|²=1`, `n_k=|β_k|²`; diabatic `P_exc→1` is the maximal-mixing regime, the analog-cosmology opposite of the adiabatic no-particle vacuum). The relic content (E18):

$$
N_{\text{pair}} = 59.8\ \text{quasiparticle pairs},\quad S_{\text{inst}} = 0.0686,\quad P_{\text{exc}} = 1.000,\quad E_{\text{exc}}/|E_{\text{cond}}| = 443.
$$

The Bogoliubov sudden-quench and the Kibble–Zurek impulse-matching are the same physics read two ways (both give `P_exc = 1`) `[CITE-5, NOVEL-BEYOND]` (the quench/defect-production framing is INHERITED — del Campo & Zurek 2014; the jump from KZM defect production to a concrete GGE relic that explains CMB structure, dark matter, and horizon resolution is the framework's NOVEL, still-contested extension). The output is an **analytic Generalized Gibbs Ensemble** — a product state (`S_ent = 0`) with three Lagrange multipliers (conjugate to the conserved charges of the post-fold integrable Hamiltonian, *not* to energy — hence no temperature), reflecting the `SU(3)` branch structure and determined by the *Bogoliubov coefficients*. This is the framework's "reheating": **THE ORDERED VEIL**.

The surviving claim is **diabatic transit-freeze, not integrability permanence**. *(Status reconciliation — register-pinned. The strong S38 reading — "the GGE relic is permanent because the post-fold Hamiltonian is Richardson–Gaudin integrable, so it never thermalizes even at Hubble time" — is **BROKEN**: Atlas D04 **T3 = BROKEN** [`V_phys` 13% non-separable; Brody β = 0.633; `t_therm ≈ 6 M_KK⁻¹`; `t_therm/t_Hubble = 9×10⁻⁴⁸`], retraction-log **item 16 = RETRACTION** [permanence claim removed; thermalizes in ~6 natural time units, not at Hubble time], retraction-log **item 27 = DOWNGRADE** [permanence → conditional on Josephson isolation]. What survives the BROKEN integrability-permanence claim is the strictly weaker, compute-certified **diabatic transit-freeze**; "thermalizes only on cosmological timescales / never thermalizes-on-the-transit-clock" is admissible ONLY inside this BROKEN-scoped, transit-scoped reading, never as an unqualified permanence.)* The relic is *frozen by diabaticity*: the crossing screens and freezes the GGE far faster than any rearrangement channel can act (`t_scr/t_transit = 814`; equivalently `t_therm/t_transit ≈ 5×10³` using the S39 density–density thermalization time `t_therm ≈ 6 M_KK⁻¹`). The Richardson–Gaudin integrability that would protect the GGE *as a permanent state* is **weakly broken** (S39 retraction, T3 BROKEN: a 13% non-separable density–density channel, Brody β = 0.633); but on the transit timescale that broken channel is irrelevant — the relic is dynamically frozen, not integrability-protected. The Ordered Veil is therefore a statement about the *transit*, not about permanent integrability. (Note the substrate-intrinsic denominator `t_transit`, never `t_Hubble`: a ratio against the FRW Hubble time is undefined until the `t(τ)` map closes, §6.3.) Two S95 computations make the survival *certified by compute, not by the retracted integrability* (Conflict C2 RESOLVED): the crossing is overwhelmingly diabatic (`R_therm = t_therm/t_transit = 5251.82 ≫ 1`, S95 W5) AND the relic is an exact pure product state (`S_ent = 0`, S95 W5) — both independent of the **BROKEN** Claim B (T3). A *third*, independent leg is geometric: `τ_fold = 0.190` is a **double-root extremal Killing horizon** (`V = V' = 0 ⟹ κ = 0, T_H = 0`), so zero Hawking temperature is the *causal-side* corroboration that the relic does not thermalize *across the transit* — requiring no integrability argument at all (and so untouched by the BROKEN T3 integrability claim).
>
> **S110 info-completeness refinement (HK-ORDVEIL, inv-9 MUDDLED-cell).** The Ordered-Veil's information-completeness is **COMPLETE as a unitarity assertion** (`S_ent = 0` pure product state ⇒ no information loss, no analog Page curve) but **INCOMPLETE as a mechanism-exhibition** — the saddle-class that would *display* the unitary evolution step-by-step is structurally absent (the GGE Fock trace is saddle-free — an analytic Boltzmann product, `βH_BdG`-linear ⇒ `Π_k` factorization; cf. HK-Q7 SUM-vs-NO-SUM 4th candidate). The substrate-IS frame is preserved: the relic IS a pure product state; the gap is in *exhibiting* the mechanism, NOT in the unitarity. (Down-tag: "info-complete" → "complete-as-unitarity / incomplete-as-mechanism".)

**Surface-gravity KIND table (extending the §6.2 KIND discipline backward to §5.3).** The "third leg" above invokes the modulus-double-root surface gravity `κ_V`; but the fold carries THREE KIND-distinct surface-gravity surfaces, and — exactly as in the §6.2 analog-temperature ledger — tagging the KIND is what prevents reading them as one functional giving inconsistent answers. At the *same* `τ = 0.190`, the modulus-metric double-root `κ_V = ½|V′| = 0` (`T_H = 0`) coexists with a Gibbons-Hawking emergent-horizon `T_GH = 0.2172` — **this is NOT a contradiction**: `κ_V` and `T_GH` are TWO DIFFERENT surface-gravity FUNCTIONALS acting on TWO DIFFERENT geometric objects (the 2D Jensen-modulus potential vs the emergent 4D acoustic horizon), not two values of one functional:

| Surface KIND | T (M_KK) | κ | Geometric object | Reproduction anchor |
|:--|:--|:--|:--|:--|
| **THERMODYNAMIC-modulus** | **0** (`T_H = κ_V/2π`) | `κ_V = ½\|V′(τ_h)\| = 0` (double root `V = V′ = 0`, `V″ = 2 > 0`) | 2D Jensen-modulus potential metric | `s85_w6_extremal_horizon_formal.npz` (`kappa_at_dump=0.0`, `is_double_root=True`; S85 W4-5 PASS) |
| **GIBBONS-HAWKING-emergent** | **0.2172** | emergent-horizon surface gravity (`T_GH = exp(−2τ)/π`) | emergent 4D acoustic horizon (`a₂` channel) | `s29c_gibbons_hawking_temperature` (closed form `exp(−0.38)/π = 0.2177`, recorded 0.2172; residual < 1e-3) |
| **SONIC** (relabeled) | **0.112** | `0.704805` | internal-acoustic `v = c_BLV` Mach-1 crossing | S63-BLV row of the §6.2 ledger |

**The `0.112 M_KK` relabel.** What S53/S63 named the **"GGE relic temperature"** (`s53_phonon_eos_output.txt`) is *not* the observed relic temperature — it is the **internal-acoustic SONIC surface** (the genuine on-trajectory `v = c_BLV` Mach-1 crossing, §6.2 S63-BLV row). The **OBSERVED relic spectral temperature is the `a₄` value `7.578 M_KK`** (the condensation-exit / interior-processing edge, §6.2 `a₄` row). The `0.112 M_KK` is a SONIC surface KIND, not the relic's spectral temperature; conflating the two is the §5.3 reader-trap this KIND table closes.

**The kitaev identity `2π·T(a₄) = κ_exit`.** The two are not independent: `2π · T(a₄ relic) = 2π × 7.578 = 47.614 M_KK = κ_exit` (the §6.2 `a₄`-row surface gravity, 47.61). The MSS chaos-bound saturation scale `2πT` *IS* the analog surface gravity `κ` at the `a₄` exit surface — exflation is a horizon process (`κ` real on the emergent metric) that is non-chaotic (`λ_L = 0`), so the surfaces are causal/thermodynamic edges, not scrambling edges. The substrate IS these three surfaces; the KIND tag is what keeps a downstream reader from collapsing them into one.

**Information-theoretically, the Ordered Veil resolves what would otherwise be an analog information paradox.** The transit is a Bogoliubov transformation — unitary by construction — and a thermalizing relic (`S_ent > 0`) would scramble that unitary into a mixed state, hiding the squeeze phase the way a black hole's thermal Hawking flux hides infalling information. The GGE relic instead stays pure (`S_ent = 0`): the Bogoliubov phase data is retained in the conserved charges. There is no Page curve to reproduce because nothing thermalizes *on the transit clock* (the diabatic-freeze reading — NOT the BROKEN T3 "never thermalizes at Hubble time" reading, which retraction-log item 16 removed; on cosmological timescales the relic *does* thermalize to Gibbs, T3 BROKEN), and the substrate carries no horizon-entropy debt out of the fold. The observed CMB is the acoustic signature of this GGE relic, not thermal-equilibrium radiation. *(Cosmogenesis-scenario cross-reference — STATUS: unreconciled. Whether "the observed CMB IS the GGE-relic acoustic signature" is the full structure-formation timeline, or whether exflation feeds a standard hot big bang (`_consolidated-findings.md §III` D2; SCENARIO A, `T_init ≈ 8.32×10¹⁵` GeV), is a substrate-physics adjudication, NOT a documentation drift — routed to the W6 D2 reconciliation gate, not resolved by this status-sync.)*

*(`N_pair` carries two readings, never the same symbol: the 59.8 figure is the BCS-projection count — itself inheriting a ~60% PBCS gap overestimate (S46, B4 CONDITIONAL) and a ~225× Richardson–Gaudin condensation-energy overestimate (S63), so `59.8` is a projected charge `⟨Q⟩_GGE`, NOT a literal pair count; the regime-robust structural claim is `P_exc = 1`. The `N_Fock = 1` exact reduction describes one Fock pair carrying that relic charge, S74.)*

*Full derivation:* `Collabs/equation-build/transit-flow-genesis-to-now.md §5`.

---

## §6 — The equation at time t

§5 ran the equation along `τ`. §6 asks the harder question — the map from `τ` to laboratory time `t` — and is scrupulous about the boundary between *derived* and *postulated*, because the central honest gap of the document lives here.

### 6.1 The τ↔t map

- **Postulated (C1).** "`τ` parameterizes cosmic time." The registry status is verbatim: *"The mapping from internal modulus to FRW scale factor is not derived from first principles."* So the *ordering* of the time arrow (`τ` increasing ⇔ `t` increasing, genesis at `τ=0`) is postulated.
- **Derived (E7 + transit).** That `τ` is a *legitimate globally-monotone clock* is derived: `dS/dτ > 0` with no stationary point makes `τ` a monotone, invertible function of `t`. The transit dynamics supplies the *local* rate near the fold (`Mach 13.75`, `δt_transit = 1.130×10⁻³ M_KK⁻¹`).

$$
t(\tau) = t_0 + \int_{\tau_0}^{\tau}\frac{d\tau'}{\dot\tau(\tau')},\qquad \dot\tau\ \text{known LOCALLY at the fold; GLOBALLY UNDETERMINED.}
$$

### 6.2 The acoustic white-hole causal structure

The causal architecture of exflation is an **acoustic white hole**: the fabric's own acoustic flow goes supersonic through the fold, and pre-fold/post-fold are causally disconnected. This is the substrate's resolution of the *horizon problem* — recast as a causal-disconnection statement (a supersonic acoustic flow), not as inflationary stretching of a pre-existing box (S85 PROVEN).

> **Change-history reconciliation (retraction-log item 22 — why the retracted analog-horizon motif does NOT quietly return under new wording).** An earlier (S48) version of this section claimed a *superfluid analog horizon* — a Mach-54 **superflow** in the standard BEC/superfluid sense. That claim was **RETRACTED (retraction-log item 22, S48 → S49)**: *"No superflow in the fabric (`φ = 0`). Amplitude gradient is not the same as phase gradient. Acoustic analog horizons require superflow; framework has none."* The substrate has **no phase-gradient superflow** (`φ = 0`). The CURRENT §6.2 structure is therefore NOT the retracted object: it is the **S85-PROVEN / S95 W-1-asymmetric causal-disconnection white hole** built on a supersonic acoustic flow of the **amplitude / spectral-weight field** (the `√(ρ_s/c_s)` acoustic metric of the scalar sector), with KIND-tagged surface-gravity surfaces (analog-T ledger below) — *not* a phase-gradient superflow. The motif survives in the *causal-structure* (white-hole outflow, pre/post-fold disconnection), which is a property of the amplitude/acoustic-metric flow and needs no superflow; the retracted *mechanism* (phase-gradient superflow) stays retracted. This is the substrate-IS reading: the white hole IS the substrate's own irreversible acoustic causal structure, NOT a superflow *in* a container. The squeeze that becomes the observed scalar amplitude `A_s` is *produced* in the interior at the fold and filtered as it leaves: by the **analog greybody factor** `Γ(ω) ∈ [0,1]` — the model-independent frequency-dependent transmission of the exit surface (a potential-barrier transmission coefficient, *not* the retracted S73B dispersive-group-velocity mechanism), so the escaping `A_s = (produced squeeze) × ∫Γ(ω)dω`, not the produced squeeze itself (S95 W4-3; the filter narrows the band). "The horizon determines what escapes, not what is produced."

**The white hole is sector-dependent (two null cones).** By [T3] (Scalar-Tensor Kasparov Decoupling, PERMANENT, `β_T = 0` exactly at linear order), the acoustic white hole is a *scalar-sector* structure: the scalar sector sees the acoustic metric `g_acoustic ∝ √(ρ_s/c_s)` and the white hole; the **tensor sector crosses the fold freely** on the `a₂`-emergent metric `g_M`. Exflation therefore carries *two* null cones, and the horizon problem is resolved for the observed scalar sector only — which is the geometric root of why the tensor observables (`r`, `n_T`, §7's falsifier #2) behave so differently from the scalar ones (`n_s`, `A_s`): they propagate on different cones.

**Analog-temperature ledger (KIND-tagged).** Three corpus analog temperatures are each `T_a = ħκ/2π` with `κ = ½∂_n(c²−v²)` the surface gravity of a *distinct* surface — but the three surfaces are of *different KINDS*, and tagging the KIND is what prevents the thermodynamic-vs-sonic conflation that the dropped V.6 clause fell into (S95 W4-2 placed the κ values, κ-ratio reproduces the corpus 9.61 to 0.018%, all κ>0; S95 W-1 added the KIND column):

| Surface | T (M_KK) | κ | KIND tag | Causal role |
|:--|:--|:--|:--|:--|
| **`a₂` (kinematic-entry)** | **72.8** | 457.66 | **THERMODYNAMIC-kinematic** | stage-1 kinematic-CARRIER temperature; *co-located* with the scalar entry SONIC surface (`v = c_BLV`) at `τ₀`; the kinematic stage `T = ħ|v'|_{r_h}/2π` |
| **`a₄` (condensation-exit)** | **7.578** = OBSERVED relic spectral `T` | 47.61 | **THERMODYNAMIC-spectral** | stage-2 ACTIVE interior-processing edge (Pöschl–Teller barrier, transmitted_fraction=0.512); sets the OBSERVED relic spectral temperature. *Active-thermodynamic-spectral processing*, NEVER "active-sonic" (flow crosses freely, `no_exit_horizon=True`) |
| **S63-BLV (internal-acoustic)** | **0.112** | 0.704805 | **SONIC** | the genuine on-trajectory `v = c_BLV` internal-acoustic Mach-1 surface |

The KIND tags are decisive: only the **S63-BLV row is a SONIC surface**; the `a₂` and `a₄` rows are THERMODYNAMIC surfaces (surface-gravity gradients of two *channels* — `a₂`↔scalar, `a₄`↔condensate), NOT two sonic horizons of one flow against one speed. A distinct, currently-unnamed surface — the BLV-scalar discriminant crossing at `τ₀ ≈ 0.1125` under the literal Visser convention — carries `T = 2.948 M_KK`; this is a *different surface read under a different κ-convention* (Visser-`c` vs bare-gradient, differing by exactly `c_BLV = 0.485`), NOT a competing entry temperature and NOT a contradiction. These are *analog/surface-gravity* temperatures, not thermodynamic temperatures of an equilibrium state — the GGE does not thermalize *across the transit* (the diabatic-freeze reading; NOT the BROKEN-T3 "never thermalizes at Hubble time" claim retraction-log item 16 removed — on cosmological timescales the relic *does* relax to Gibbs, T3 BROKEN), so there is no Gibbs temperature *in this transit-window story*; the `7.578 M_KK` is not a reheating temperature.

**The relic temperature is a two-stage composite, not two competing Hawking temperatures.** The OBSERVED relic spectral temperature is the `a₄` value (`7.578 M_KK`), as the SPECTRAL / interior-processing (stage-2) temperature; the `a₂` value (`72.8 M_KK`) is the KINEMATIC-CARRIER (stage-1) temperature — the two are TWO STAGES of *one* composite emission inherited parent→child from the ³He-A analog white hole (Painlevé–Gullstrand, NOT analogy), NOT two competing Hawking temperatures and NOT two sonic horizons. The κ-ratio `9.6117 = κ_entry(a₂)/κ_exit(a₄)` (corpus 9.61) is a two-CHANNEL gradient ratio (`a₂`↔scalar, `a₄`↔condensate) carrying channel information but NOT sonic-horizon information. (Labeling precision: the `a₂` surface is the kinematic carrier with *observable-visibility HELD pending* falsifier F1 — a scan for a scalar-channel observable squeeze branch near `72.8 M_KK`, an order of magnitude above the `a₄` condensate-squeeze support `ω∈[0.82, 1.06]`; both readers predict its expected NULL, which would confirm the `a₂` carrier is observationally invisible. Until F1 lands, the COMPOSITE form is asserted, the categorical "a₂ carries no observed quantum" is NOT.)

> **The white hole is ASYMMETRIC (one entry sonic surface, open supersonic exit) — a structural theorem over-determined at six independent walls (S95 W-1).** The causal structure is the *asymmetric* one: ONE post-genesis sonic surface (the scalar entry horizon, `v = c_BLV` at `τ₀ ≈ 0.1125`, `κ_entry = +18.52 M_KK > 0`, white-hole outflow) bounding an *unbounded* supersonic expulsion region toward `ℐ⁺` — **no future-trapped exit horizon, no symmetric throat, no bounce**. The `a₄` BCS-window edge (`τ≈0.235`) and the decoherence scale (`τ∼0.16`) are *thermodynamic* features inside the open region (a well-defined effective `κ_exit`, `no_exit_horizon=True`), NOT a second sonic surface. This is not merely the W4-1 verdict (`dS/dτ = +58,673` one-signed forbids a second `(c²−v²)` crossing): the c_s-softening challenge — *does `c_BLV` soften toward zero at the DOS singularity?* — was answered structurally, the softening lives in the *condensate* band-edge channel (`c_B2`, rho-pinned to `1/(πρ_B2) = 0.0227 M_KK`, finite — not zero), NOT in the *scalar* transit channel the discriminant is built on; and routing the discriminant onto any softer channel only *deepens* the supersonic interior (channel-monotone: the B2-channel fold Mach is `293.79`, not `13.75`). C1 → ASYMMETRIC is thus over-determined at **six independent walls** — scalar speed-hierarchy / channel-monotone / monotone-`dS/dτ` / irreversibility-quench / 12D-censorship-monotone-`a_eff` / reversed-population-interior — the bounce reading breaches none, the deepest wall being the entropy-arrow of the irreversible quench. The dropped transit-V.6 "two distinct horizons" STRENGTHEN clause STAYS DROPPED: it conflated two *thermodynamic* surfaces with two *sonic* horizons. **The diagram is asymmetric because the quench is irreversible** — the acoustic white hole is the laboratory realization *of* the substrate's irreversible first-order Kibble–Zurek transit through the van Hove fold (substrate IS, laboratory IN; the GGE relic does not thermalize *across the transit* — the diabatic-freeze reading, NOT the BROKEN-T3 Hubble-time permanence claim — and the squeeze is a pure Bogoliubov product, `S_ent = 0`). The analog-T ledger and the greybody filter above hold (the entry surface is sonic; the `a₄` surface carries a well-defined effective `κ`; the greybody filters the open exit). **(S110 HK-NOBOUNCE scoping: the "no bounce" holds UNDER monotone-ramp transit dynamics — the one-signed `dS/dτ` wall above IS the monotone ramp; inv-7 W4-1 reduced the effective-Friedmann form to the homogeneous-isotropic sector and converged MONOTONE with zero dissent, confirming no second `(c²−v²)` crossing and no bounce in that sector.)**

The relic does not poison the late-time equation: the effacement ratio `|E_BCS|/S_fold = 3×10⁻⁷` (E34) defeats all BCS corrections to the late-time `w`.

**BAO effacement.** The same effacement projection that protects `w` *suppresses* the per-gapped-branch BAO sub-features: transported through `(c_b²/c_Gold)²` (every Layer-2 branch speed `v_g ≤ c_Gold`), the per-branch sub-feature is `δP/P ≈ 1.4×10⁻³` — far below current acoustic-scale rulers (effacement is a suppression, not an amplification). The **live BAO channel is the S43 first-sound ring** (`A_FS/A_BAO = 0.204 = c_2²/c_1²`, `r_1 ≈ 325 Mpc`, `k_1 ≈ 0.0193 Mpc⁻¹`) — a distinctive, zero-parameter prediction with no ΛCDM counterpart, whose amplitude-detection forecast against a named experiment's sensitivity awaits the fetched value (S95 W6-2, INFO-by-unavailability: the comparison value, not the substrate forecast, was unavailable at compute). *(A six-stratum causal partition of the transit is a **PRELIMINARY** presentational synthesis of the S70/S71 data; it is distinct from the S62/S49 "six-layer censorship" — the separate set of mechanisms (energy, friction, no trapped surfaces, Josephson, fragmentation, one-loop stabilization) that *protect* the structure.)*

### 6.3 What the substrate fixes: the conformal class + all dimensionless dynamical shapes — and the one dimensional scale it imports (M_KK)

> **Re-scope (S102, designated-writer patch — supersedes the S101 A13 Q1 "open honest gap" status; status reconciled to register).** The most important caveat in this section is now stated as a POSITIVE finding about what the substrate **determines**, not as a deficit confession. The §6.3 a(t)-status down-tags from *"a(t) recoverable / open honest gap"* to **conformal-class-complete-PLUS-dimensionless-dynamics; the single dimensional second is the externally-calibrated cutoff M_KK**. The substrate spectral triple `(A_K, H_K, D_K)` determines the conformal class of the emergent cosmology AND *all dimensionless dynamical shapes* — every ratio, ordering, tilt, growth shape, and the late-time tracking exponent — from **zero continuous parameters**; what it does NOT determine is the dimensional metric *normalization*, which imports exactly ONE externally-calibrated dimensional scale, the cutoff `M_KK`, through the `O = w·Ô` factorization (`w = M_KK`). What dies is the *seconds-valued* `a(t)`; what survives is the *shape* of `a(t)`. This is a single **rank-1 normalization non-universality** with a topological cause (`N₃ = 0`, BDI class, S44 — the induced metric carries no Fermi-point protection). The structural source is the `§VII.BS — Normalization Non-Universality (N₃=0 corollary, rank-1)` theorem-tag (`sessions/permanent-results-registry.md`, **STAGE-3-PERMANENT** — promoted at S102 W1-4 Stage-2 cross-axis PASS-AND [audit `d309efb4…`] ∧ CF-α FAIL-confirming ∧ CF-β sustained rank-1, per `joint-theorem-promotion.md`; Stage-0 = S101 W-2 volovik×phonon-first workshop; falsifier pair + Stage-2 cross-axis verify = S102 Wave 1). **Four POSITIVE points (the re-scope's content):**
>
> 1. The **n=2 late-time tracking exponent is DERIVED, not fitted** (`S101-W1-QEQ-SELFCONS` PASS, `a_exp = 0.6554 ≈ 2/3`, the w=0 dust attractor self-consistent, `κ_inv = True`) — the *shape* of the late-time expansion is substrate-fixed; only its seconds-valued normalization is not.
> 2. The **conformal class** of the emergent metric is **invariant under the BLV→Connes representation change** (`connes_scales − acoustic_scales = {}`) — the frame-identity question is settled (the comoving scale factor is decidably `a_eff` on the `a₂`-metric `g_M`); only the dimensional trajectory is un-normalized.
> 3. The **dimensionless dynamical shapes** — tilts (n_s-class), growth shapes, the `A_FS` first-sound ratio — are **protected by the K=3-MANDATORY multiplicative-normalization cancellation invariant** (`math-scripts.md`), of which the FRW background is a fourth structurally-distinct instance.
> 4. The **zero-free-parameter spine is UNAFFECTED**: the single imported scale `M_KK` is a *calibration*, not a continuous fit parameter, so the ZFP claim is intact (the eigenvalue problem is silent only at the single terminal `× (ℏ/M_KK c²)` second, BY CONSTRUCTION, because `N₃ = 0`).
>
> **Register reconciliation (Atlas D04 C1/C2 — the prose tag EQUALS the register tag).** Atlas D04 **C1** ("τ parameterizes cosmic time / modulus→FRW scale-factor map") is `ASSUMED, now SCOPED to the dimensional-readout leg only` — verbatim: *"the substrate determines the conformal class + ALL dimensionless dynamical shapes from zero continuous parameters; ONLY the seconds-valued dimensional readout imports the externally-calibrated cutoff."* This prose narrates the a(t)-status at — never above — that C1 register status: the conformal-class + dimensionless-shape content is **complete** (zero continuous parameters), the dimensional-readout leg **stays ASSUMED** (one imported scale). Atlas D04 **C2** is the *cosmological-mapping* `K_pivot` tag (`BROKEN-WITH-LIVE-RESEARCH-PATHWAY`), which D04 explicitly disambiguates as **distinct** from the emergent-Friedmann *pathway* tags this section scopes — the K_pivot mapping is a separate object and is NOT re-scoped here. The substrate-IS arrow is **unchanged**: `D_K eigenvalues → spectral moments → dimensionless dynamical shapes (every ratio, ordering, tilt, growth shape, AND the n=2 tracking exponent) → measurement`; only the terminal `× (ℏ/M_KK c²)` is the external multiplier. *(This re-scope LOWERS the over-confident "open honest gap as deficit" wording to register status while preserving the substrate-first frame; per `capstone-hygiene-gate.md §"Substrate-first framing preservation"`, a status re-scope NEVER inverts the explanation direction. The detailed scope of what remains — the seconds-valued back-reaction closure — is preserved verbatim in the paragraphs below; the OLD "open honest gap" phrasing is quoted there only as the superseded wording, not as the live claim.)*
> **S111 W1+W2 status sharpening (the §6.3 a(t) flagship is HALF-CLOSED, not closed — the clock half is now PROVEN well-posed, the M_KK-magnitude half is FAIL-confirmed external).** This ADDITIVE note records the S111 split of the W1 flagship; it does NOT re-open the conformal-class content above and does NOT narrate §6.3 as closed. **(W1 — clock half PROVEN well-posed, a genuine forward step.)** The decisive CF-2 forward gate the S110 WS-CLOCKLOC workshop set up (the (C,E,D)-triple self-consistency in the substrate-natural frame) CLOSED at S111 W1: the cosmological clock IS τ (the Level-2 Jensen modulus, UPSTREAM of the a₀/a₂/a₄ Seeley-DeWitt grading), and the minisuperspace clock-triple is now PROVEN well-posed — `S111-CF-CLOCKLOC1-CED` PASS (audit `7ac41f0397a1c64b…`: the de-Sitter relation `Λ = 3H²` closes EXACT with `c_track = 3`, residual 2.9e-11, `D_wellposed = True`, `triple_closes = True`), `S111-CF-CLOCKLOC2-MONOTONE` PASS (audit `62619fb344de965f…`: τ̇ sign-corridor `+1`, `n_zero_corr = 0`, monotone on the transit interior — τ is a good clock), `S111-CF-CLOCKLOC4-UNIQUE` PASS (audit `59b44547ca52df82…`: the de-Sitter relation `Λ=3H²` is the UNIQUE reparametrization-invariant scalar — the clock choice is substrate-natural, not a residual gauge freedom). So the *well-posedness of the τ-clock* — which monotone variable is the clock, and that the (C,E,D) triple closes — is no longer ASSUMED; it is PROVEN. **(W2 — M_KK-magnitude half FAIL-confirmed as an external import, REINFORCING the C1 ASSUMED tag, not lowering it.)** The one place the eigenvalue problem is silent BY CONSTRUCTION (`N₃ = 0`) — the seconds-valued dimensional normalization — is now POSITIVELY confirmed to be an external import, not merely labeled ASSUMED: `S111-CF-MKK-RG-INVARIANCE` FAIL (audit `10010211894762b4…`) tested whether M_KK is τ-RG-invariant (a substrate fixed point that would let the dimensionful scale be substrate-natural) and found it is NOT — the dimensionless transmutation ratio `R(τ) = exp(−1/(λ_eff(τ)·N₀(τ)))` is τ-FLOWING over `[3.6e-11, 0.160]` (NOT a fixed point, `Δ_rel = 8.19 ≫ 5e-2`), and the dimensionful cutoff `Λ = M_Pl,reduced` is a borrowed CODATA scale (`leg2_no_import = False`). This is the substrate-physics RECEIPT that the M_KK-magnitude leg is a genuine external-import boundary — it CONFIRMS, at the substrate level, exactly the Atlas D04 C1 status (`ASSUMED, scoped to the dimensional-readout leg only`); it does not change it. **Net §6.3 status: HALF-CLOSED (the τ-clock well-posedness + the conformal class + ALL dimensionless dynamical shapes are substrate-fixed / PROVEN from zero continuous parameters) / HALF-OPEN (the one seconds-valued dimensional normalization imports the externally-calibrated cutoff M_KK — an external-import boundary, FAIL-confirmed not τ-RG-invariant).** The substrate-IS arrow is UNCHANGED: the substrate IS the a(t) trajectory read through `D_K eigenvalues → spectral moments → the τ-clock + dimensionless dynamical shapes → measurement`; W1 proves the clock leg of that arrow is well-posed, W2 confirms the terminal `× (ℏ/M_KK c²)` second is the one external multiplier — neither inverts the explanation direction (`capstone-hygiene-gate.md §"Substrate-first framing preservation"`). Provenance: `computations/session-111/s111_gate_verdicts.txt` (CLOCKLOC1/2/4 PASS, MKK-RG-INVARIANCE FAIL); reconciled to Atlas D04 C1 (`sessions/framework/Atlas/atlas-04-assumptions.md`, S111 in-cell note appended). **Capstone-hygiene Q1=YES (§6.3 a(t)-gap granularity altered) ∧ Q3=YES (the τ-clock well-posedness sub-claim → PROVEN); routed to `session-111-housekeeping.md §A` as an in-session designated-writer status note. No section narrates §6.3 as closed; the prose tag EQUALS the C1 register tag (ASSUMED on the dimensional-readout leg).**
>
> **S112 W1 status sharpening (the M_KK-magnitude half is now a PERMANENT external-import boundary — FAIL-confirmed under the SUBSTRATE-NATURAL reading too, not only the bare-import one).** This ADDITIVE note records the S112 closure of the magnitude half; it does NOT re-open the conformal-class content above and does NOT narrate §6.3 as closed — it makes the C1 dimensional-readout-leg ASSUMED tag *permanent* by ruling out the last substrate-internal escape, while leaving the conformal-class + dimensionless-shape content (substrate-fixed, PROVEN) untouched. S111 W2 FAIL-confirmed the *bare-import* reading (M_KK frozen from CODATA-routed M_Pl is not τ-RG-invariant); S112 W1-1 then gave the *substrate-natural* reading its fair, pre-registered test — `S112-CF-MKK-SUBSTRATE-ANCHOR` FAIL (audit_sha256 `3fa9be16e90ada96e6d1b0f43748b0ddc48626b1c428c1f01a769ed4561e39af`): can a substrate-internal dimensionful anchor `Λ_anchor`, supplied WITHOUT CODATA and WITHOUT routing through M_Pl, fix M_KK τ-RG-invariantly? Both pinned candidates (GAP-EMERGENT-LENGTH `Δ_BCS·M_KK`; EMERGENT-NEWTON `√(a₂^{ζ}/(48π²))·M_KK = 2.4208·M_KK`) reduce to `M_KK·(pure number)` — the substrate's spectral data (`a₂`, `Δ_BCS`) are DIMENSIONLESS in M_KK units, so they cannot bootstrap the absolute GeV scale (`gate_pass=False`, `MPl_eff/MKK=2.420762`, `continuity=True`). This is the **self-referential-unit-system no-go**: a system that measures every observable in M_KK units cannot fix M_KK's absolute GeV value from within — the lattice-QCD scale-setting analog (all dimensionless ratios predicted; exactly ONE external dimensionful anchor irreducibly required), the same structural shape as the S100a rank-1-Yukawa-wall *"irreducibly external, not a refinable approximation"* precedent. So the one place the eigenvalue problem is silent BY CONSTRUCTION (`N₃ = 0`) — the seconds-valued dimensional normalization — is now confirmed an IRREDUCIBLE external import, not a refinable approximation: the magnitude leg is a permanent external-import boundary, FAIL-confirmed under BOTH the bare-import (S111 W2) and the substrate-natural (S112 W1-1) readings. **H0-residual consequence (W1-2).** The downstream H0-residual band closure (`CF-S112-H0-BAND-CLOSURE` FAIL, audit_sha256 `f5a8498d1114e311db9d8aa51b22308df1a260169af4d10d8693ecd36a818796`) inherits the W1-1 FAIL: the d_A=+1 ODD M_KK^1 scale leg stays INADMISSIBLE (parity selection rule), so H0 relief is CAPPED at the dimensionless transport channel — `49/800 = 6.125%` EXACT — with the dimensionful remainder (~93.875%) pinned to the one external scale `M_KK`. The 6.125% is the substrate's honest, structurally-bounded contribution to the Hubble tension; the remainder is the external-import boundary, not a deficit the framework can close from within. **Net §6.3 status: HALF-CLOSED (τ-clock well-posedness + conformal class + ALL dimensionless dynamical shapes substrate-fixed / PROVEN from zero continuous parameters) / HALF-OPEN-NOW-PERMANENT (the one seconds-valued dimensional normalization imports the externally-calibrated cutoff M_KK — a PERMANENT external-import boundary, FAIL-confirmed not substrate-derivable under either reading; H0 relief capped at 6.125%).** The substrate-IS arrow is UNCHANGED: the substrate IS the a(t) trajectory and the H0 residual, read through `D_K eigenvalues → spectral moments → the τ-clock + dimensionless dynamical shapes + emergent transport → measurement`; W1-1 confirms the terminal `× (ℏ/M_KK c²)` second is the one external multiplier the substrate cannot self-supply — the FAIL is the substrate telling us exactly where its predictive reach ends, NOT an inversion to fields-expanding-in-a-container (`capstone-hygiene-gate.md §"Substrate-first framing preservation"`: a status sharpening NEVER inverts the explanation direction). Provenance: `computations/session-112/s112_gate_verdicts.txt` (CF-S112-MKK-SUBSTRATE-ANCHOR FAIL line 5, CF-S112-H0-BAND-CLOSURE FAIL line 17); falsifier-surface receipt at `falsifier-master-inventory.md` Row #81.audit-S112-W1-H0-CEILING-PERMANENT (the 6.125% H0-relief ceiling, mack-cosmic-bridge sole writer). **Capstone-hygiene Q1=YES (§6.3 a(t)/effective-Friedmann magnitude-leg status sharpened) ∧ Q3=YES (the M_KK-magnitude leg → PROVEN permanent external boundary) ∧ Q4 (PROSE claim); routed to `session-112-housekeeping.md §A` as an in-session designated-writer status note. No section narrates §6.3 as closed; the prose tag EQUALS the C1 register tag (ASSUMED → now permanently ASSUMED on the dimensional-readout leg, the escape ruled out), scope-qualified per the S100a "irreducibly external, not a refinable approximation" precedent.**
>
>
> **S114 W2 status note (the C2-ratio leg of the K_pivot bridge + the τ_fold fold-location, both SHARPENED — ADDITIVE; does NOT re-open the conformal-class content above and does NOT narrate §6.3 as closed).** The §6.3 bridge names "(ii) resolution of the `K_pivot` paradox (C2)" as one of its three legs. S114 W2 sharpened TWO §6.3-relevant objects. **(C2-ratio — `CF-S114-KPIVOT-EDGE-TRANSFER-DEGREE-OPEN` INFO, audit `3c12c706f3b3…`.)** The BZ-edge→K* ratio leg the S113 KPIVOT workshop left narrowly-OPEN is now resolved: a substrate-natural EVEN transport degree (`deg = 0`, parity-consistent with the dimensionless `d_A=0`) IS extractable — so the ratio is NOT scale-leg contamination — but it is the TRIVIAL dimensionless-ratio-preserving degree, which by the K=3-MANDATORY multiplicative-normalization cancellation invariant (`math-scripts.md`) cancels in every ratio and supplies NO contraction to the working pivot (1.6625 decades unaccounted; bridge image off the §VII `L^{-4}` envelope). So the C2-ratio is *partial-derivable* (a parity-correct even degree exists) but does NOT close the K_pivot bridge from within (the degree is trivial-on-the-ratio; not a §23 K=3 advancement). With the S113 C2-mag (`M_KK¹` magnitude CLOSED-PERMANENT-external) + C2-id (BZ-edge=pivot CLOSED-NEGATIVE), the K_pivot paradox is now confirmed structural-external on ALL THREE legs — the rank-1 §VII.BS / N₃=0 ceiling, exactly the `O = w·Ô` scope this section already states. **(τ_fold fold-location — `CF-S114-TAUFOLD-CUSP-CROSSING` INFO, audit `7b637db142d9…`.)** The fold the §6.3 trajectory crosses (`τ_fold = 0.190`) is sharpened from "imported value" to **REGION van-Hove-SELECTED, value RESOLVED to a two-value non-fungible convention** (S114 W-1 transit × lizzi workshop, output (iii)): the cusp-CROSSING location `τ_cross = tau_cross_van_hove = 0.191038` is L_max-INVARIANT (regulator-robust, computed from-scratch with NO injected 0.190 — sector-local saturation, T3/T5 at p+q ≤ 2; Reading-B "location regulator-conditional" REFUTED; reproduces atlas-07 S45 0.19104 to 5sf). The substrate IS the band-edge T3–T5 anticrossing FEATURE (M_KK has no analog); `Φ_cross` LOCATES it at 0.191038, while `tau_fold = 19/100 = 0.190` is the rational ANCHOR (`Φ_anchor`) the derived-ratio chain `S_0 = τ_fold/T_acoustic = 95/56` consumes — non-fungible (the EXACT-rational identity is unique to 19/100: `CF(0.191038/0.112) = [1;1,2,2,1,1,18,44]` admits no clean small-denominator rational). The `|τ_cross − 0.190| = 0.5464%` is NOT a "precise value still conditional"; it is the characterized round-number-freeze-vs-located-value offset between two real substrate-IS objects at different layers — the canonical `tau_fold` pin is UNCHANGED, and `tau_cross_van_hove` lands as a SEPARATE registered observable (registry §VII-B.TAU-CROSS-VAN-HOVE), neither replacing the other. The substrate-IS arrow is UNCHANGED (`D_K eigenvalues → spectral-triple tiling / ρ(λ;τ) cusp → bridge-map transport / transit-crossing → measurement`); neither sharpening inverts the explanation direction (`capstone-hygiene-gate.md §"Substrate-first framing preservation"`). **Capstone-hygiene Q1=YES (C2 is a §6.3 a(t)/effective-Friedmann bridge leg) ∧ Q3=YES (C2-ratio + τ_fold-region sub-statuses) ∧ Q4 (PROSE); routed to `session-114-housekeeping.md §A` as in-session designated-writer status notes. No section narrates §6.3 as closed; the prose tags EQUAL the Atlas D04 C2 / A4 register tags (reconciled in the atlas-04 S114 in-cell freshness note).** Provenance: `computations/session-114/s114_gate_verdicts.txt`.
>
> **What remains to be supplied — the seconds-valued readout (the scope of the dimensional-normalization leg).** This is the SCOPE of the one imported scale, NOT a re-opening of the conformal-class content above. C1 says verbatim the modulus→scale-factor map is "not derived from first principles." C2 (`K_pivot` mapping) is **BROKEN-WITH-LIVE-RESEARCH-PATHWAY** ("the framework's load-bearing gap"). T6 (Friedmann–BCS locking) is **BROKEN** (the 155,984-mode spectral action overwhelms the 8-mode BCS by 133,200×). The S74 W1-E Friedmann result is a *structural* FAIL: there is no substrate-derived `H² = (8πG/3)ρ` closing the loop to a metric scale factor. The Volovik tracking law (E44) and BBN-tracking Friedmann (E46) *assume* a standard FRW `H` as external input and feed `ρ_vac` into it; they do not derive `a(t)` from `D_K(τ)`.
>
> **What exists instead — labeled PROXY, never `a(t)`:** two *distinct* proxy scale factors that are NOT interchangeable. `a_eff(τ) = (a₂(τ)/a₂(today))^{1/2}` is a spectral-complexity relabeling — near-flat through the fold (`R_K` barely moves, `2.018 → 2.028`), so its deceleration `q_Ω` diverges and it does **not** reproduce a sensible expansion history. The `a(τ)` from Connes distance (SCALE-FACTOR-54, with `q` from `−0.97` to `+0.81`) is the **order-parameter-space texture-distance** (the superfluid coherence-length readout `ξ(τ) ∝ ħv_F/Δ(τ)`, `a(τ_fold)/a(0)=2.117`), NOT the acoustic `a_eff` a comoving phonon clocks; the two are conformally AND layer-distinct by `Ω(τ) ∝ √(ρ_s/a₂)`, the gate-level fingerprint `proxies_conformally_distinct=True`, `Omega_BA_fold=2.241353` (S95 W4-4, INFO). **The SF54 `q`-band `[−0.97,+0.81]` is therefore a conformal-frame-conditional / proxy-conditional artifact, NOT a standing falsifier** — it is the q-range of the order-parameter proxy, and the substrate's acoustic deceleration (the `a₂`-channel q a comoving observer measures) is conformally distinct from it (S95 W4-4; S96 W-1 workshop). The physical comoving scale factor is **decidably `a_eff`** (a comoving observer is built of phonons; phonons propagate on the `a₂`-metric `g_M`; `λ ∝ a_eff`) — and the frame identity AND the dimensionless *shape* are both substrate-fixed (the conformal class is invariant under BLV→Connes; the n=2 tracking exponent is derived); **what remains pending is the dimensional-normalization leg only** — the *seconds-valued* trajectory, the one place the eigenvalue problem is silent by construction (`N₃ = 0`). The `{Z_norm, V0}` + seconds (W1-3 CLOSED, `8.86044e-42 s` = the imported `M_KK`-scale `gamma_unit = ℏ/M_KK c²`) + O'Neill (S96 W1-2: cross-terms effaced at the Hubble scale, `6.84e-117 ≪ 3e-7`) pins, and the `Ω(τ)=√(ρ_s/a₂)` profile that bridges the historical order-parameter `a(τ)` to the physical acoustic `a(t)`, are the *seconds-valued readout* of an otherwise-determined dynamical shape — NOT an open-ended deficit. Neither proxy is a derived FRW scale factor; neither is promoted; the dimensionless-shape content is complete (zero continuous parameters, the conformal-class-complete `Ô` kernel), and only the dimensional readout imports the one external scale — exactly the Atlas D04 C1 register status (`ASSUMED, scoped to the dimensional-readout leg only`).
>
> **Why this is a *category statement about the fundamental object*, not a discarded obligation.** The substrate, not `a(t)`, is fundamental: "space does not expand; spectral complexity grows inside each point," and `H(t)` is the *readout* of that reorganization, not an external clock. This is the **Jacobson (1995) reading made microscopic** `[CITE-4, INFO: INHERITED genre / NOVEL same-object — contested]`: the Einstein/Friedmann equations are equations of *state* of the emergent metric (derivable from horizon thermodynamics, not fundamental), so a substrate theory is *expected* not to contain a fundamental Friedmann equation — the `a₂` moment IS the Einstein–Hilbert action Jacobson recovers thermodynamically, and the framework's own `Z = Σ e^{−S}` (§1.3a) is the partition function in question. *(The emergent/thermodynamic/analog-gravity lineage — Jacobson 1995; Barceló–Liberati–Visser 2005; Belenchia–Liberati–Mohd 2014; Volovik 2005/07 — is INHERITED genre; whether the substrate white hole §6.2 is "same genre" or "same object" is the calibration question the report flags, so CITE-4 carries the honest dual tag rather than a forced single label.)* But the framework does **not** thereby escape owing a Friedmann map — it owes a *derived effective* one, and it already *uses* the standard FRW `H(t)` as external input for every late-time observable (`w₀`, `wₐ`, `σ₈`, the CC tracking `ρ_vac ∼ M_Pl²H²` — caveat C10). So **"Friedmann is the wrong question" is right about the *fundamental* level and wrong about the *effective* level; both must be said.** Stated from the transit axis, the missing object is a **back-reaction-closure**, not a kinematics gap: the kinematics (the local sweep rate `τ̇` at the fold, the full Bogoliubov spectrum) are in hand; what is absent is the equation promoting the produced relic energy density into a source for the *global* expansion rate (`H² = f(ρ_relic, S_SA)`). The T6 FAIL is precisely the statement that the 155,984-mode spectral action cannot be closed against the 8-mode BCS source (the 133,200× overwhelm) — so the gap is a *derived `S_SA(τ) →` 4D gravitational action*, not "a Friedmann equation."
>
> **This is one gap, not two — the same gap as frontier #8 (emergent equivalence principle).** A *derived, generally-covariant* 4D action for `g_M` would simultaneously be (a) the effective Friedmann map, (b) the emergent equivalence principle (universal coupling of `g_M` to all excitations), and (c) the emergent Einstein–Infeld–Hoffmann theorem (geodesic motion of emergent matter as a *consequence* of the `a₂`-channel field equations, not a postulate, via an emergent Bianchi identity `∇_μ G_eff^{μν} = 0`). The framework already holds EIH on the *internal* `K` geometry (the modulus EOM satisfies a Bianchi identity, S44); what is owed is its lift to the *emergent* `g_M`. Closing this one object closes frontiers #1 and #8 jointly — which *reduces* the dimensionality of the open frontier. Closing the effective map requires (i) that derived `S_SA(τ) →` 4D gravitational action, (ii) resolution of the `K_pivot` paradox (C2), and (iii) the `M_KK⁻¹ →` seconds normalization — the three are one bridge. Until then: **`τ` is a derived monotone *parameter* (E7); that this parameter *is* cosmic time, and the global rate `τ̇(τ)` away from the fold, are postulated/undetermined (C1); the framework borrows the container-observer's FRW `H(t)` to read its late-time predictions (C10); and the global `t(τ)` and any FRW `a(t)` are not derived.**

> **Framing discipline for this section (the top container-relapse risk).** The instant the document narrates a *history*, a reader supplies a background spacetime and an external clock. Refuse "the vacuum energy decays *in* the expanding universe"; require "the `M_Pl²H²` reservoir dilutes *as* the substrate's spectral complexity reorganizes — `H(t)` is the *readout* of that reorganization, not a clock the vacuum decays in." Any trajectory figure is axis-labeled `τ`, never `t`.

*Full derivation:* `Collabs/equation-build/transit-flow-genesis-to-now.md §6`.

---

## §7 — The equation at now: where it touches data

The same equation, evaluated once at `τ_now`, emits numbers a detector can test today. **No observable below is fit** — each is a spectral moment of `D_K` at the same single modulus. When the substrate measures one of these, the substrate is probing itself; there is no container the measurement happens *in*.

### 7.1 Outputs by spectral-moment layer


> **Epistemic-register split (W8-2 consolidation, `S96-CONSOL-3REGISTER-TABLE`).** The single flat table below is re-presented as **three epistemic registers** so a referee reads each claim at its true register — the external review flagged the flat table for *"visually flattening conditional and unconditional claims into a common rhetorical register."* The 3-register split (keyed by the `S96-CONSOL-STATUS-SYNC`-reconciled status tags) is the PRIMARY view; the flat table is retained as a flat reference. **No value changes** — only the epistemic sorting. SUM-check: 7 robust + 6 conditional + 1 falsified = 14 rows (partition exact, no flattening; `m_H` is a disclosed dual-status straddle placed in CONDITIONAL).

> The single §7.1 'now' table is split into three epistemic registers (report §"Critique": the flat table *"visually flattens conditional and unconditional claims into a common rhetorical register"*). Each observable lands in **exactly one** register, keyed by its W8-1-reconciled status tag; **no row's epistemic type is flattened** (no BROKEN/CONDITIONAL row in the robust register); the substrate-moment-layer (a₀/a₂/a₄) provenance is preserved per row. Dual-status straddle rows (m_H) land in CONDITIONAL with an explicit disclosure, **not** forced into the robust register. **No observable below is fit** — each is a spectral moment of `D_K` at the same single modulus `τ_now`; when the substrate measures one of these, the substrate is probing itself.

#### Register A — ROBUST-STRUCTURAL outputs (zero-free-parameter spine)

*Epistemic class: **PROVEN / PASS-structural / Gaussian-by-Wick BOUND — substrate-IS predictions, the no-borrowed-H joint-BF spine**.*

| Observable | Layer (a₀/a₂/a₄) | Framework value | Comparison anchor | Reconciled status (W8-1) | Notes |
|:--|:--|:--|:--|:--|:--|
| **CC closure** | a₀ | rho_vac/rho_obs = 1.032 (0.01 OOM) | observed Lambda | PASS (DILUTION-CC-66) | doubly-conditional on C10 + external H (caveat box); robust as the NON-INHERITANCE identity (Clause A), conditional on observed magnitude (Clause B) -- see dual-status note |
| **r (tensor-to-scalar)** | a₂ tensor | 0.033 (dual-pathway: Path-H 0.00745 / Path-C 0.0117) | BICEP/Keck < 0.036 | PASS (within 2sigma; D04 IX row4 LIVE PASS<2sigma) | substrate-IS tensor sector; NO borrowed H; clean robust spine row |
| **f_NL (non-Gaussianity)** | bispectrum | \|f_NL\| <~ 1.5 BOUND (-1.505 = -max_f_NL_FW saturation; central GGE ~1.03) | Planck -0.9 +/- 5.1 | BOUND-Gaussian-by-Wick (re-tagged per C4; central ~1.03) | Gaussian-by-Wick to within a \|f_NL\|<~1.5 bound; 0.47sigma is the BOUND's distance, NOT a central detection |
| **sigma/m (DM self-interaction)** | E29 / N_Fock=1 | 0 exactly (N_Fock=1 superselection) | Bullet < 1.25 cm^2/g | structural N_Fock=1 (PASS) | structural zero distinct from any tuned cross-section; no borrowed H; clean robust spine row |
| **f*sigma_8(z) (RSD growth)** | a₂ growth | -4.058% f*sigma8 PRODUCT suppression vs LCDM @ z=0.51 (bare-f -0.311% -- the PRODUCT, NOT bare-f) | DESI-5yr / Euclid RSD | PASS-class (S8-tension-relieving sign; S77 PROVEN / S96-OBS-FSIGMA8-FORECAST; joint 7-bin below 3sigma decisive) | zero-free-parameter; JOINT 7-bin sigma-dist 1.95 DESI-Y5 / 2.96 Euclid (CF-S113-FSIGMA8-EUCLID-7BIN, S114 W1-1, PASS, margin +0.039sigma to the 3sigma decisive boundary -- LIVE and close to decisive; diagonal forecast cov) [per-bin max 1.01 DESI-Y5 / 1.53 Euclid @ z=0.51, cross-check]; C5 guard: -4.058% is the PRODUCT, -0.311% is bare-f; borrows H (modulation-on-borrowed-H caveat) |
| **nu mass ordering** | a₄/fiber neutrino | Normal B1<B2<B3 (zero-free-parameter; dynamical tau=0.107 (1,1,0)-crossing) | NuFit-6.0 (NO preferred ~2.5sigma) | PASS (structural, machine-eps; S8/S34-36/S52/S56) | zero-free-parameter substrate eigenvalue ordering; no borrowed H; clean robust spine row; JUNO/DUNE clean yes/no |
| **c_s^2 (dark-sector sound speed)** | a₂ Goldstone / Kasparov | 0 exactly (Layer-1 topological; m_Goldstone^4D=0 by Kasparov factorization; bound <9.21e-4) | dark-sector c_s^2 (DES/KiDS, future) | PASS-class (Level-1 topological; §VII.BH cross-pillar bridge PROVEN, S96 W7-8) | STRUCTURAL ZERO (regulator-invariant, L-independent); no borrowed H; clean robust spine row; full §VII anatomy = W7-8 (mack-review-at-W8-2: no §7-surface retrofit needed -- entry is a §VII permanent-results cross-pillar bridge, not a §7 falsifier-surface row) |

#### Register B — CONDITIONAL outputs (PASS contingent on an unresolved input)

*Epistemic class: **CONDITIONAL / SCHEME-DEPENDENT / route-dependent / doubly-conditional — the PASS holds GIVEN an unresolved input, scheme, route, or borrowed H(t)**.*

| Observable | Layer (a₀/a₂/a₄) | Framework value | Comparison anchor | Reconciled status (W8-1) | Notes |
|:--|:--|:--|:--|:--|:--|
| **w_0 (DE equation of state)** | a₀ | -0.918 (w0_FW, Volovik partition); branch-iv -0.842454 | -0.803 +/- 0.054 (joint, Popovic/DES-Dovekie 2511.07517v3) | C4 CONDITIONAL (DR3-binding 2027) | LIVE 2.13sigma / 0.73sigma (branch-iv); borrows external H(t) (dagger / C10) |
| **n_s (scalar tilt)** | a₂/Goldstone | COMMITTED-√x: 0.9590 (`n_s_FW_sqrt_cutoff`; BCS+1-loop √x reading) — disclosure tuples 0.9561 (const-ε) / 0.9595 (√x Window-7) | Planck 0.9649 +/- 0.0042; ACT-DR6 0.9709 +/- 0.0038 (CMB-only) / 0.974 +/- 0.003 (P-ACT); SPT-3G-D1 0.9679 +/- 0.0033 | C3/C9 FUNCTIONAL-SELECTION-COMMITTED (S103 W5-2: √x A₅→A₆ atlas-cardinality-ROBUST; Q28 Layer-2 DISCHARGED) | committed multi-anchor σ-spread on the FIXED 0.9590: **1.40σ (Planck) → 2.70σ (SPT-3G) → 3.13σ (ACT-only) → 5σ (P-ACT)**, one-sided low — the pre-registered live-watch FIRING in the falsifying direction (NOT a re-fit; functional re-shopping FORBIDDEN per anti-rescue fence, inventory Row #85.multi-anchor-s104, 2026-06-10); disclosure tuples 2.10σ (0.9561) / 1.29σ (0.9595, vs Planck); the BMA band 0.969 +/- 0.022 was the correct UQ object WHILE f was unselected — superseded by the committed √x point now the functional is selection-robust |
| **alpha_s (running dn_s/dlnk)** | a₂/a₄ | DUAL (scale, channel): substrate -0.0859 (s=3 Mellin) / pivot ~0 | Planck -0.0045 +/- 0.0067 | C12 CONDITIONAL on CMB-S4 (channel-artifact resolved S93 W7-1) | pivot image +0.67sigma consistent; substrate value awaits CMB-S4 ~34sigma-reach falsifier |
| **m_H (Higgs mass)** | a₄/fiber | 131.8 GeV (KK-threshold DIRECT, Route-B FORCED; S102 W4-20, audit `75ed7ffb`) | PDG 125.25 +/- 0.17 | PASS-class (~2% budget); route-PINNED (Route-B forced by a₄-KK saturation L_sat=6; D04 IX row8 PROVEN-AT-OBS w/ caveat) **[DUAL-STATUS straddle — disclosed]** | DUAL-STATUS STRADDLE: robust-on-magnitude (~2% theory budget PASS) BUT band-MISS +5.36% (= 67/1251; +38.5σ vs tight PDG); route now PINNED (Aitken KK-L5 127.5 spurious, overshoot 1.2416× floor; zeta 138.5 excluded; mu_BC 188 is ACCOMMODATION); placed in CONDITIONAL with disclosure, NOT flattened into robust. **S110 HK-R-PROTECTION-MH:** the +5.36% residual is PHYSICAL-but-UNDERIVED — BOTH the truncation route AND the named self-energy route are falsified; the quartic *gives-not-derives* 131.8. The Γ_eff effacement leak (0.03%) is a DISTINCT quantity from this 5.36% residual — do NOT conflate. No "PROVEN-AT-OBSERVATION" flattening; single-number collapse 131.8±2.15 |
| **Omega_DM h^2** | a₂ Leggett gap | 0.120 (Leggett-only) | Planck 0.1186 +/- 0.0020 | C7/C11 CONDITIONAL on LEGGETT-GRAV-DECAY-67 (margin PASS S95; D04 IX row9 PROVEN-AT-OBS) | PASS 0.7sigma GIVEN Gamma_grav<H_0 (satisfied with 65-OOM margin, 8.85e-66); the conditional is satisfied, but the PASS is contingent |
| **sigma_8 (growth amplitude)** | a₂ growth | 0.799 (zero-free-parameter) | Planck sigma_8 0.811; lensing ~0.76 | C9 PROVEN-with-conditional (VIABLE, ~2sigma between the S8-tension ends) | VIABLE not a resolution; conditional on the sqrt(x) functional being canonical; borrows H (dagger). ANCHOR FIX (W6-7): Planck sigma_8=0.811, NOT the S_8=0.829 the prose mis-cited. ANCHOR CURRENCY (S100b litrev): KiDS-Legacy S8=0.815(+0.016/-0.021) removes the KiDS-low anchor -- both framework channels (0.799 O-Z / 0.79317 a2-growth) now sit BELOW BOTH high anchors (Planck sigma_8 0.811 / KiDS-Legacy S_8 0.815); "between the ends" survives only via the DES-Y3/HSC residual low side (inventory Row #70.audit-S100b-KIDS-LEGACY) |
| **Σm_ν (neutrino mass sum)** | a₄/fiber seesaw | 0.0582 eV (substrate type-I seesaw; M_R = D_K B-branch fold energies, KO-dim-6 Pfaffian) | DESI 2024 < 0.072 eV (95% CL, arXiv:2404.03002) | PASS by 19% (S99 W3-2; inventory Row #77); route-dependent (m_D oscillation-anchored) **[DUAL-STATUS straddle — disclosed]** | DUAL-STATUS STRADDLE: substrate-FIRST on {M_R from D_K fold energies + seesaw structure + suppression DIRECTION (all dm_ν/dM_R<0) + normal ORDERING} BUT the absolute Σ is NOT-yet-zero-free-parameter (m_D Yukawa back-solved from measured m_2/m_3, so 0.0582 eV is the MINIMAL-NO value); placed in CONDITIONAL with disclosure, NOT flattened into robust. m_D-firming RAN S100a: `S100a-MD-NORMALIZATION` INFO (audit `4f92a551…`) — the bottom-triple → Y_i map is NON-UNIQUE (0.4742 ≫ 0.05; residual NOT 1-parameter), so the Dirac-scale anchor is irreducibly EXTERNAL (S99 caveat PERMANENT; §7.3 item-(4) S100a update). Distinct observable from the Register-A ν-mass-ORDERING row (eigenvalue ordering is ZFP; the absolute SUM is not). DESI-DR3 bound below 0.0582 eV would convert PASS→tension |

#### Register C — CURRENTLY-FALSIFIED outputs (the live wagers)

*Epistemic class: **BROKEN / advancing-tension / inversion-falsified — the register marks these against current data; reported as boundaries that sharpen the surviving solution space**.*

| Observable | Layer (a₀/a₂/a₄) | Framework value | Comparison anchor | Reconciled status (W8-1) | Notes |
|:--|:--|:--|:--|:--|:--|
| **w_a** | a₀ | 0 (structural four-fold lock) | -0.72 +/- 0.21 (same joint fit) | C5 BROKEN (3.43sigma post-Dovekie) | the live wager; prediction fixed, data moving away; item-34 'wa not a meaningful CPL parameter' |

> **SUM-check (partition correctness).** |robust|=7 + |conditional|=7 + |falsified|=1 = 15 == |§7.1 rows|=15 (no omission, no double-count; the S99 W3-2 Σm_ν row added to CONDITIONAL). No-flattening: no BROKEN/CONDITIONAL row in Register A. Dual-status straddles (disclosed, placed in CONDITIONAL): m_H (Higgs mass, route-PINNED to Route B / KK-threshold DIRECT 131.8 GeV per S102 W4-20 audit `75ed7ffb`, band-MISS persists — robust-on-magnitude ~2% budget BUT +5.36% / +38.5σ vs tight PDG, so it stays in CONDITIONAL as a dual-status straddle, NOT promoted) and Σm_ν (neutrino mass sum, m_D-normalization-dependent — substrate-FIRST on M_R+seesaw+suppression-direction+ordering, but absolute Σ NOT-yet-ZFP). The 3-register split changes **no value** and **no substrate-moment-layer attribution**; it only sorts the rows by epistemic register so the zero-parameter robust spine is not visually conflated with the conditional forecasts or the live wagers (the §7.3 honest-scorecard makes the same distinction in prose).

**Flat reference (all 14 rows, one list).** The same rows, un-stratified, for cross-checking the partition above:

| Observable | Layer / E-no. | Framework value (canonical) | Comparison anchor | Status |
|:--|:--|:--|:--|:--|
| **w₀†** (DE eq. of state) | `a₀` / E28,E44,E45 | **−0.918** (`w0_FW`, Volovik partition); branch-iv **−0.842454** | **−0.803 ± 0.054** (joint, see ‡) | LIVE; 2.13σ / 0.73σ |
| **wₐ†** | `a₀` / four-fold lock | **0** (structural) | **−0.72 ± 0.21** (same joint fit ‡) | **3.43σ — the live wager** |
| **CC closure†** | `a₀` / E45 | **ρ_vac/ρ_obs = 1.032** (0.01 OOM) | observed Λ | PASS (DILUTION-CC-66) |
| **n_s** | `a₂`/Goldstone / E22–E24,E31 | **COMMITTED-√x** 0.9590 (`n_s_FW_sqrt_cutoff`); disclosure tuples 0.9561 (const-ε) / 0.9595 (√x Window-7) | Planck `0.9649±0.0042`; ACT-DR6 `0.9709±0.0038` (CMB-only) / `0.974±0.003` (P-ACT); SPT-3G-D1 `0.9679±0.0033` | LIVE-COMMITTED; multi-anchor σ-spread **1.40σ (Planck) → 2.70σ (SPT) → 3.13σ (ACT-only) → 5σ (P-ACT)**, one-sided low (FIXED prediction; live-watch FIRING in the falsifying direction, NOT a re-fit — anti-rescue fence per inventory Row #85.multi-anchor-s104, 2026-06-10); Q28 Layer-2 DISCHARGED S103 W5-2 |
| **r** | `a₂` tensor / E2 | **0.033** (dual-pathway: Path-H 0.00745 / Path-C 0.0117) | BICEP/Keck `<0.036` | PASS (within 2σ) |
| **α_s** | `a₂/a₄` / E48 | **DUAL (scale, channel)** — see box | Planck `−0.0045±0.0067` | **RESOLVED-AS-CHANNEL-ARTIFACT** (S93 W7-1) |
| **f_NL** (non-Gaussianity) | bispectrum / squeezed-vacuum | **−1.505** (`|f_NL| ≲ 1.5`, Bogoliubov-Gaussian by Wick) | Planck `−0.9 ± 5.1` | PASS (0.47σ, structural) |
| **m_H** | `a₄`/fiber | **131.8 GeV** (KK-threshold DIRECT, Route-B forced) | PDG `125.25±0.17` | PASS-class (~2% budget); route-PINNED (S102 W4-20, audit `75ed7ffb`; a₄-KK saturation L_sat=6; Aitken 127.5 spurious) |
| **Ω_DM h²** | `a₂` Leggett gap / E47 | **0.120** (Leggett-only) | Planck `0.1186±0.0020` | PASS, 0.7σ |
| **σ/m** (DM self-int.) | E29 | **0 exactly** (`N_Fock=1`; `⟨Q⟩_GGE=59.8`, §5.3) | Bullet `<1.25 cm²/g` | PASS (structural) |
| **σ₈†** | `a₂` growth / E33 | **0.799** (zero-free-parameter) | Planck σ₈ `0.811` (S₈ `0.829`); lensing `~0.76` | VIABLE, not a resolution (~2σ below Planck; S100b KiDS-Legacy reposition: below BOTH high anchors, low side only via DES/HSC — Row #70.audit-S100b-KIDS-LEGACY) |
| **f·σ₈(z)†** (RSD growth) | `a₂` growth / E33 | **−4.058%** f·σ₈ PRODUCT suppression vs ΛCDM at z=0.51 (zero-free-parameter; `f_FW=0.525492` vs `f_LCDM=0.527130`, bare-f −0.311% — the PRODUCT, NOT bare-f) | DESI-5yr / Euclid RSD | **PASS-class** (S₈-tension-relieving sign; S77 PROVEN / S96-OBS-FSIGMA8-FORECAST; **JOINT 7-bin σ-dist 1.95 DESI-Y5 / 2.96 Euclid**, `CF-S113-FSIGMA8-EUCLID-7BIN` S114 W1-1 PASS, margin +0.039σ to the 3σ decisive boundary — LIVE; diagonal forecast cov; per-bin max 1.01/1.53 @ z=0.51 retained as cross-check) |
| **ν mass ordering** | `a₄`/fiber neutrino / E-seesaw | **Normal B1<B2<B3** (zero-free-parameter; dynamical via the τ=0.107 B1↓-below-B2 crossing of D_K's (1,1,0)-sector) | NuFit-6.0 (NO preferred ~2.5σ) | **PASS** (structural, machine-ε; S8/S34–36/S52/S56) |
| **Σm_ν** (neutrino mass sum) | `a₄`/fiber / E-seesaw | **0.0582 eV** (substrate type-I seesaw; M_R = D_K B-branch fold energies; MINIMAL-NO, m_D oscillation-anchored) | DESI 2024 `< 0.072 eV` (95% CL, arXiv:2404.03002) | **PASS by 19%**; DUAL-STATUS straddle (m_D-normalization-dependent; S100a-MD-NORMALIZATION INFO: irreducibly external) |
| **c_s² (dark-sector sound speed)** | `a₂` Goldstone / Kasparov-factorized | **0 exactly** (Layer-1 topological; `m_Goldstone^{4D}=0` by Kasparov product factorization; bound `<9.21×10⁻⁴`, scheme-independent) | dark-sector c_s² (DES/KiDS, future) | **PASS-class** (SCORECARD pointer; full §VII registry anatomy = W7-8) |

> **‡ Dark-energy anchor provenance + dagger note `[CITE-8, INHERITED]`** (the numerical comparison anchors are external data — Planck 2018; BICEP/Keck 2024; Popovic et al. 2025 / DES-Dovekie; DES Y3 — no framework novelty claimed; they prevent the phenomenology section from feeling internally self-referential). The `(w₀, wₐ)` pair is one joint posterior — **Popovic et al. (DES Collaboration), arXiv:2511.07517v3** (Flat `w₀wₐCDM` = DES-Dovekie SN + DESI DR2 BAO + Planck 2018 + ACT-DR6 + SPT-3G), with `ρ(w₀, wₐ) ≈ −0.85`. The σ-distances printed are 1-parameter *marginals*; the binding test is the 2D `(w₀, wₐ)` posterior — the `R_842` rectangle (Falsifier #1), not two independent 1D distances. **`†` marks rows evaluated using the container-observer's FRW `H(t)` as external input (caveat C10): the spectral *value* is from `D_K`; the *cosmological evaluation* borrows `H(t)`, the same undelivered effective-Friedmann map as the `a(t)` gap (§6.3). `n_s`, `r`, `α_s` are quoted at the CMB pivot via the transport map, not at the substrate/BZ scale (a 54-decade category error the framework has closed; see the α_s box).**

> **‡‡ w₀ provenance + branch-iv disposition `[S116-W5, baptista × volovik CONVERGED; capstone-hygiene Q2-YES; value / σ / R_842 UNCHANGED]`.** The staked `w₀ = −0.918` is a **Level-1 closed-form THERMODYNAMIC identity** — the Volovik vacuum partition + effacement `Γ_eff = 0.99970` (the `a₀` ratio dressed by impedance-mismatch leakage), **Gate: None**, NOT a `D_K` spectral-action derivation. It does NOT join `{n_s, m_H, r}` as spectral-derived; it stands on the closed form alone, a legitimate substrate-IS thermodynamic observable (a CLEAN stake — the DESI DR3 ~2027 falsifier rests on this closed-form value). The branch-iv corridor is **CLOSED-WITH-RESULT**: the regulator-convention-lockdown demarcation theorem makes "admissible ⟺ `w₀^C(10) = w0_FW` bit-exact ⟺ `offset = w0_FW − ρ_B(L*)` injects w0_FW", so every admissible branch-iv convention INJECTS the closed-form value (the Zubarev moment's own limit is `−1`, ruled inadmissible) — the branch-iv lineage is derivation-NEUTRAL on the VALUE (its real content is the offset-invariant L_max-stability envelope, value-blind). Hence **`−0.842454` is the FENCED reversibility-trigger alternate** (derivation-INADMISSIBLE per `§EVOI.BF` / S101 W4-3; the `1.w0-branch-resolution`/`1.w0-branch-iv-evaluator` inventory sub-rows: ρ_B(L) UNCOMPUTABLE post-S86, NO `w0_FW_R842` promotion), **NOT a co-equal branch-iv derivation** — it is the §5 reversal-band trigger only, not a rival staked value. **σ-distances are deg-CONDITIONAL**: w₀ is a `d_A=0` observable, provisionally **deg(T_{BZ→pivot}) = 0** (S-7 connes §23, T2-VACUOUS scalar ⟹ substrate w₀ = pivot w₀; the `→ −1.340827` CAC asymptote is PROXY-ARTIFACT-TYPED, the λ_max running-edge is not an admissible §23 morphism) — pending `CF-S117-W0-TRANSPORT-DEGREE`. **σ-metric discipline (do NOT conflate)**: the EVOI-fence `2.13σ` (canonical) / `0.73σ` (branch-iv) are distances vs the **DESI DR2 measurement** `−0.803 ± 0.054`; the decision-rule `3.28σ` / `6.30σ` are vs **ΛCDM** `−1`. The binding DR3 test is the 2D `(w₀, wₐ)` `R_842` rectangle, not a 1-parameter marginal.

**Substrate readings.** Dark energy is *not* quintessence (the clock constraint closes all rolling quintessence at 15,000× the bound); it is the **effacement residual** (0.03% leakage, `Γ_eff = 0.99970`) plus the Volovik tracking vacuum `ρ_vac(t) ∼ M_Pl²H²(t)` — which is the de Sitter horizon energy density (`H/2π = T_dS`), i.e. the substrate tracking its own emergent de Sitter horizon — closing the 114-OOM gap to 0.01 OOM *today*. The CMB tilt is *not* "density perturbations in expanding space"; it is the **interference pattern of post-transit GGE acoustic excitations**. Dark matter is the **Leggett-channel GGE quasiparticle** — CPT-neutral, **superselection-protected** (`N_pair` conserved, no annihilation channel) and **momentum-flux-free** (`T^{0i} = 0` exact; the relic was born at rest), *not* integrability-protected (the S39-broken Richardson–Gaudin integrability is not the mechanism). The geometry does **not** permit the DM abundance to be tuned: the full Goldstone spectrum over-produces by **260σ**, and *only* the Leggett-channel projection lands at 0.7σ; that same channel forces `σ/m = 0` exactly (vs Bullet `< 1.25 cm²/g`), a structural zero distinct from any tuned-cross-section dark-matter model — a stronger statement than "0.7σ PASS." The Higgs is the transverse `|S|²` fiber oscillation.

> **CC caveat box — what the equilibrium identity does and does not warrant (two clauses).** The framework does not inherit the 114-OOM catastrophe in the first place. **Clause A (non-inheritance — warranted exactly).** The equilibrium theorem (`dε/dq = μ ⇒ ρ_Λ = 0` at equilibrium, `q = N_pair`) makes the *equilibrium* vacuum energy **exactly zero** by an *exact thermodynamic identity* (`ε − μq = −P = 0`, Gibbs–Duhem, representative-independent; S95 W5-3 `EQUILIBRIUM-CC-WARRANT` PASS, Sage-rational `0`), *not* a tuned cancellation: the `μq` term subtracts the ground-state energy identically. The catastrophe is an artifact of computing vacuum energy in a container-EFT *without* a UV completion; the substrate has its UV completion (`D_K`), so the bare term is removed by an identity, not by fine-tuning — the "vacuum-energy test" the framework passes `[CITE-6, NOVEL-BEYOND]` (the q-theory vacuum-relaxation picture is INHERITED — Klinkhamer & Volovik 2008; Visser 2002; Volovik 2005; the Volovik-*partition* + effacement-residual mechanism closing 114 OOM to `ρ_vac/ρ_obs = 1.032`, doubly conditional on C10 + external `H(t)`, is the framework's NOVEL extension). This warrant is **thermodynamic (Gibbs–Duhem), not topological**: the substrate is the ³He-B class (`N₃ = 0`, BDI), not ³He-A (`N₃ = 2`), where the vacuum energy is *topologically* protected to zero (Volovik Paper 03, Thm 1). **Clause B (observed magnitude — re-scoped to C10).** With no topological protection, `ρ_Λ = 0` is a *reference/boundary value, not an attainable interior point*: there is no interior q-equilibrium in the gapped substrate (S62 #19, `dE_ZP/dq > 0`), and at the discrete physical ground state `N_pair = 1` the system sits *off* equilibrium (`P_vac = −0.688 ≠ 0`). So the *observed* Λ magnitude is the **non-equilibrium tracking residual**, whose "not-tuned" status rests on the C10 tracking law (`ρ_vac ∼ M_Pl²H²`, **ASSUMED-PARTIALLY-PROVEN**) evaluated at the off-equilibrium point — *not* on the equilibrium identity — closing to `ρ_vac/ρ_obs = 1.032` (DILUTION-CC-66). The CC closure is therefore **doubly conditional** (on C10 AND on the external `H` the tracking law feeds, the same undelivered effective-Friedmann map as the `a(t)` gap, §6.3): `1.032` is a genuine PASS *given* an external `H(t)`, not yet a from-`D_K` derivation of the dark-energy density. *"Exactly zero, not tuned" is true of the equilibrium reference / non-inheritance (Clause A); conditional of the observed magnitude (Clause B).*

> **α_s — the most-misread row, structurally resolved (S93 W7-1).** (Glossary: `α_s` here is the scalar spectral-index running `dn_s/d ln k`, **NOT** the QCD strong coupling, and **NOT** the topological-scheme identity `α_s = n_s²−1` — the symbol is overloaded three ways across the corpus.) "α_s = −0.086 vs Planck −0.0045 is a 12σ tension" is a single-label conflation. The substrate carries **two scale-separated `α_s` observables**: (a) a substrate-distance running **`−0.08587279`** at the `s=3` Mellin pole, *inside* the BZ at `O(M_KK)`, **FI-class (regulator-invariant, frozen-now — it cannot drift to meet CMB-S4)**; (b) a Goldstone-pivot running `≈ 0` at the CMB pivot. Which one a detector sees is set by the computable transport degree **`deg(T_{BZ→pivot}) = +2`** (non-scalar) — *derived, not chosen* (the same pre-registration discipline that excludes the anomaly family structurally, not by tilt). The `−12σ` was the *scalar-transport leaf*, now falsified; on the matched channel the **pivot image sits at +0.67σ — consistent** — and the substrate-distance value relocates to a **~34σ-reach CMB-S4/CMB-HD falsifier**. The status is therefore *tension resolved as a channel artifact; pivot image consistent (+0.67σ); substrate value `−0.0859` awaits CMB-S4* — a future test, not a present confirmation. *(The primordial-side `α_s = 0` is a Bogoliubov-saturation statement: `P_exc = 1` freezes the spectrum, Sasaki–Stewart exact at CMB to `10⁻¹¹³`; the CMB-observed `α_s` is Phase-2 isocurvature transfer.)*

> **Open gaps (genuine constraints, reported as boundaries).** **n_s functional selection — RESOLVED (S103 W5-2), the committed point is the √x reading `0.9590` at `1.40σ`.** The functional-selection question (Window-7 / FUNCTIONAL-SELECT-67) is now CLOSED: S67 selected Chamseddine-Connes `√x` first-principles (JOINT-FALSIFICATION-67 PASS, unique pass-all survivor; the anomaly family excluded *structurally* by `n_s>1 for all φ>0`), and the single previously-untested AND-conjunct — the Q28 Layer-2 atlas-cardinality robustness (`S87-C45-SIXTH-REGULATOR-PROMOTION` was `A₄→A₅` only) — is DISCHARGED ROBUST at `S103-Q28-LAYER2-A6` (PASS; `√x` is the unique pass-all survivor under `A₅→A₆`, `survivors(A₆)={√x}`, the `n_s>1` anomaly exclusion intact; sixth = heat-kernel/Gaussian `f(x)=exp(−x²)`, pinned anti-comparator-shopping — the `S87` `CM-Hopf` winner is a NCG-axiom regulator on a DIFFERENT atlas, not a cutoff-function sixth). So the committed framework point is the `√x` BCS+1-loop reading `n_s = 0.9590` (`n_s_FW_sqrt_cutoff`) at a REPORTED `1.40σ` from Planck (the σ-distance is a COMMIT *consequence*, never the decision driver — the W4-20 no-data-appeal template). The other readings remain `(value, scheme)` disclosure tuples — `0.9561` (const-ε gauge-invariant, `n_s_framework`, `2.10σ`) and `0.9595` (`√x` Window-7, `1.29σ`) — NEVER band-shopped for Planck-proximity; the COMMIT pins the `√x` FAMILY and `0.9590` is its BCS+1-loop value. The BMA band `n_s = 0.969 ± 0.022` (S67, marginalizing over the then-nuisance functional `f`) was the correct UQ object *while `f` was unselected*; with `f=√x` now selection-robust, the committed `√x` point supersedes the marginalized band (decisive gate E31 status: CONDITIONAL → COMMITTED). The remaining `1.40σ` lowness is a falsifiable LIVE prediction on the CMB scalar-tilt channel, no longer a scheme-spread ambiguity. (`falsifier-master-inventory.md` Row #85 HELD → COMMITTED-LIVE.) **m_H route — RESOLVED (S102 W4-20), residual open item is the M_KK-derivation ceiling** `[CITE-7, NOVEL-BEYOND]` (the NCG-Higgs tradition is INHERITED — CCM 2007; Devastato–Lizzi–Martinetti 2014; ATLAS/CMS PDG `125.25 ± 0.17` GeV; the KK-threshold route is the framework's NOVEL prediction): the intra-family route ambiguity is now CLOSED — `S102-MH-ROUTE-SELECTION` (PASS, audit `75ed7ffb`) FORCES the **KK-threshold DIRECT route = 131.8 GeV** via a₄-KK saturation (L_sat=6 < L_max=10), with the Aitken KK-L5 127.5 route SPURIOUS (overshoot 1.2416× floor) and no PDG appeal (Friedrich–Bär saturation is the structural certificate). So the headline is the route-PINNED 131.8 GeV (not the former 127.5–131.8 band); comparison is at the framework's **~2% theory budget**, not PDG's 0.17 GeV (a referee computing +38.5σ against the 0.17 GeV bar is comparing against a precision the prediction does not claim — the construction-quality figure is +5.36%). The zeta route (138.5 GeV) is excluded and the μ_BC fit (188 GeV) is an **ACCOMMODATION**, not a prediction. What REMAINS open is NOT the route but the **M_KK-derivation ceiling** (EVOI §6 standing gap): m_H IS the a₄/fiber |S|² KK-threshold mode riding the imported M_KK scale (the W-2 rank-1 N₃=0 corollary), so a clean first-principles M_KK derivation is what would convert the route-pinned-but-band-missing m_H into a clean discriminator; the band-MISS persists until then. **A_s — RESOLVED zero-parameter (S118 W1, regime-MARGINAL).** The dominant `0.668`-OOM `𝒩`-fork (prior band-cite `3.11–4.27×10⁻⁹`, CF-S117-conditional) is CLOSED: the substrate-first sound speed `c_s = 0.5685 M_KK` (the `a₂^{ζ}`-curvature-channel hydrodynamic-IR group velocity) lands IN the GS-1 window `[0.516, 0.650]`, so the `deg=+2` substrate→pivot transport is the SOLE fork-carrier and A_s lands on the `H̃`/+0.196 acoustic-horizon grid **`A_s = 3.2994×10⁻⁹`**, `+0.196` OOM = **1.57×** above Planck `2.1×10⁻⁹` — a LIVE zero-parameter over-production (NOT a ±5% agreement; the over-production SIGN is FORCED by `Γ_exit ≤ 1`), the box-δ/`+0.864` grid demoted to a regime-diagnostic (TWO-SPECTRA-TWO-ROLES). *regime-MARGINAL*: `a₄K⁴/a₂K² = 0.102` at the window edge is just above the 5% hydrodynamic-IR floor (a robust-but-marginal PASS, NOT unconditional). The exit-greybody factor that would carry `3.2994×10⁻⁹` down to the observed Planck amplitude stays a FITTED knob (`CF-S118-ALT-GREYBODY-WALL` FAIL, 4-class empirically-walled / structurally unproven), so the staked zero-parameter prediction is the un-attenuated `3.2994×10⁻⁹` (`CF-S118-AS-CS-SUBSTRATE-FIRST` PASS, audit `172c85be…`; curated-register fold SYNCED — atlas-08 Q23 dashboard + Decisive-Class + atlas-04 D04 folded at S118 W1 close). **Ω_DM h² = 0.120 is CONDITIONAL on LEGGETT-GRAV-DECAY-67 (D04 C7/C11)** — the PASS holds *given* `Γ_grav < H_0`; if the Leggett-mode gravitational decay rate exceeded `H_0`, the DM sector would collapse and the `0.120` would be meaningless. *(Status reconciliation — register-pinned, D1 dissonance RESOLVED. The knowledge graph dual-listed this as both a defined PASS gate (`PASS: Γ_grav < H_0`) and an "UNCOMPUTED-CRITICAL decisive test"; the conditional is no longer uncomputed. Two register facts now bound it: the **kinematic protection is PROVEN** — single-Leggett gravitational decay is FORBIDDEN by the graviton-gap, the same kinematic protection the BCS gap gives quasiparticle decay (S67 `Single-Leggett gravitational decay: FORBIDDEN`, PROVEN) — and the **explicit margin landed PASS** at S95 (`LEGGETT-GRAV-DECAY-CONDITIONAL` PASS: `Γ_grav/H_0 ~ 8.85×10⁻⁶⁶`, a 65-OOM safety margin). So the conditional is satisfied with an enormous margin; "CRITICAL-uncomputed" is the stale reading, "CONDITIONAL-and-satisfied" is the current one.)* **LRD/SF54 deceleration history — re-scoped to a substrate prediction (S100a W1-1).** The S99 band-miss against the SF54 deceleration band `[−0.97, +0.81]` is **FRAME-ROBUST** (`S100a-W1-1-SF54-MAPPING` INFO, Track B 0.9, audit `f41bdf1f…`): the exact frame-map theorem makes `q` a logarithmic-derivative invariant — under any conformal reframing `q̃ = A(τ)·q + B(τ)` with `A = (H_bare/H̃)² > 0`, and every CONSTANT conformal/amplitude normalization cancels IDENTICALLY (`A ≡ 1, B ≡ 0`) — while the three-map ladder exhausts the admissible corrections (const-`Ω_BA` = identity on q; acoustic-frame map ILL-DEFINED by the S98-proven 0/0; CD-frame map comparison-DEGENERATE/circular). No admissible frame or normalization correction can move `band_frac = 0.5015` toward the 0.90 floor. The SF54 band is the **WRONG comparison band**: it is generated in a Connes-distance conformal frame ~26× faster than the bare backbone (`H_CD/H_bare` median 26.10) and conflates pre-fold and post-fold regimes. The substrate-prediction reading REPLACES the mapping-defect reading: the substrate is **structurally mostly-accelerating post-fold** (median `q = −0.8662`; the miss is one-sided BELOW −0.97 — post-fold acceleration HARDER than the band's quasi-de-Sitter floor), and no matched laboratory deceleration band yet exists for that history. This *sharpens* the §6.3 proxy-distinctness reading (S95 W4-4 established the frames are conformally distinct; S100a W1-1 proves no frame correction can reconcile the q-histories, because q is frame-invariant under exactly the corrections a constant normalization can supply). **M₀^{sector} (absolute fermion-mass normalization)** — internal-consistency scope row, NOT a detector-horizon row. The fermion-mass RATIOS are clean substrate predictions (Item 12 scope; Spectral-Moment Decoupling, S75 W2-E); the absolute SCALE is |s(h)|²-anchored to the m_H fiber-embedding mode (S100a-M0-MH-INHERITANCE, INFO, audit `d00bbb3794ed207c…`) and therefore inherits the documented Higgs-sector over-prediction LINEARLY: δM₀/M₀ ∈ [+5.356%, +7.114%] (exact 67/1251, 89/1251 vs m_H_obs = 125.1). BCS −7% screening (S62) applied to m_H only, NOT to M₀ — a future M₀-side BCS application is the closure mechanism. Cross-sector echo: the neutrino Dirac-scale normalization is independently IRREDUCIBLE (S100a-MD-NORMALIZATION, INFO). The §IV absolute-normalization claim carries THIS confidence, no more. **Missing a(t)** (§6.3) means the "to-now" arc is grounded in *observables at τ_now*, not a derived expansion-history trajectory.

### 7.2 Falsifier anchors (live inventory)


> **Acoustic CGWB — the GW-detector flagship is RETIRED to a GW-detector-sterile / LSS-falsifier form (W-3 workshop, little-red-dots ∧ mack, 2026-05-30; supersedes the W8-2 scope-correction).** The substrate's acoustic readout transduces into the tensor sector with peak frequency `f_peak ≈ 8.4835×10³⁹ Hz` (van Hove fold scale `M_KK/(2π)` redshifted by `a_fold/a_now = 0.4723`; `S96-OBS-CGWB-PEAK-FREQ`, audit `646e6ad0…`, FAIL — D4 resolved AGAINST the mHz peak), ≥ GHz across the entire physically-swept `M_KK⁻¹→s` normalization band [1e-20, 1e-10] (reaching LISA needs `κ = 25.06 s/M_KK⁻¹`, 42.5 OOM above the natural `ħ/M_KK` and 11.4 OOM beyond the band; 121/121 swept points are GHz+). This is above the band of **EVERY realized or proposed GW detector** — LISA (mHz), PTA (nHz), and the GHz/MHz high-frequency program (≲ 10¹¹ Hz; the peak is `28.93` decades above this ceiling). The LISA band specifically is the causal IR tail of this peak: **LISA-band amplitude `< 10⁻⁴²`** — a *slope-independent* upper bound (for every causal IR exponent `p ≥ 1` with `Ω_peak ≤ O(1)`, `Ω(LISA) ≤ 10^(−42.45)`, ≥ `29.45` OOM below LISA-PLS; the exact value is `PENDING-SUBSTRATE-RECOMPUTE` per the S97 fold-DOS slope+height derivation). **The acoustic CGWB is NOT a GW-detector falsifier.** The substrate's acoustic structure is testable in **large-scale structure**: the first-sound BAO ring (`A_FS = 0.204`, SNR 8.6341 DESI-5yr; `S96-OBS-FIRST-SOUND-RING` PASS, audit `b74ccd56…`) and f·σ₈ growth suppression (−4.06%, S₈-relieving; `S96-OBS-FSIGMA8-FORECAST` INFO) are the live zero-parameter near-term acoustic falsifiers. The historical `Ω_GW^(A) ~ 1e-10`-at-3-mHz "flagship" was a provenance-less pivot placeholder under an assumed peak-at-3-mHz (Case A — `1e-10` carries no PROVENANCE entry; the peak-near-pivot assumption is refuted by `f_peak = 8.4835×10³⁹ Hz`, 42.45 decades away; under an `f³` tail it would land `~10⁻¹³⁷` at 3 mHz, 127 OOM inconsistent with the redshift-chain peak). Two SURVIVING structural results stay on this surface, each explicitly tagged non-detector-testable: the **wall-network null** `Ω_GW^{walls} = 0` EXACTLY (π₀(U(1))=0; W6-4) `[STRUCTURAL topological result — NOT a detector signal]`, and the **(A)/(C) regulator-class split** `47.081` OOM (Sage-exact `47.080974235`; `Ω_GW^(C) = 8.299×10⁻⁵⁸`) `[STRUCTURAL regulator-class SEPARATION — NOT detector-testable; a ratio between two LISA-STERILE numbers]`. Full split + supersession chain: `falsifier-master-inventory.md` Row #7.audit-3.
| Row | Observable | Detector | What it does |
|:--|:--|:--|:--|
| **#1** | w₀ | **DESI DR3** (2026) | binding instrument for the `R_842` branch; the **near-term cliff-edge** |
| **#2** | r / n_T | BICEP/Keck → **LiteBIRD 2030** | Path-H vs Path-C discriminator (4.25σ via `n_T = −r/8`, the *CMB-transferred* tensor consistency S66 — **not** the slow-roll relation, which is inapplicable at the fold) |
| **#3** | α_s | **CMB-S4 2030 → CMB-HD 2035** | ~34σ-reach falsifier of the `s=3` Mellin-residue identity at the matched channel |
| **#3-X** | α_s (**vs LQC**) | **SO DR1 2029 + LiteBIRD/CMB-S4 2030** | **cross-framework** discriminator (distinct from #3's framework-vs-data reach): the joint CMB low-ℓ × α_s × r measurement separates the `D_K` first-order transit from an LQC polymer/GFT-condensate smooth bounce. Decisive blade is α_s: framework `−0.069` (`= n_s²−1`, CMB-pivot leaf, geometry-fixed, `Q`-robust) vs LQC slow-roll `≈ 0` — separated at **>27σ at SO DR1** (~34σ CMB-S4). **Two σ-figures DISTINCT**: framework-vs-LQC = >27σ (the discriminator); framework-vs-Planck/ACT = 11.31σ (the #3 internal tension). Low-ℓ ℓ≲30 (framework: NO feature; LQC: suppression) is the consistency cross-check (S96 W-1 NYT-Q2 workshop; `falsifier-master-inventory.md` Row #74; cross-links FUNCTIONAL-SELECT-67 / atlas-08 Q28, still OPEN) |
| **#5** | f·σ₈(z) | **DESI-5yr 2029 → Euclid 2030s** | zero-parameter RSD discriminator; −4.058% product suppression, S₈-tension-relieving sign (JOINT 7-bin σ-dist 1.95 DESI-Y5 / 2.96 Euclid, `CF-S113-FSIGMA8-EUCLID-7BIN` S114 W1-1 PASS, margin +0.039σ to the 3σ decisive boundary — LIVE and close to decisive [per-bin max 1.01/1.53 @ z=0.51, cross-check]; the *shape+sign* breaks the static-σ₈ degeneracy) |
| **#6** | ν mass ordering | **JUNO 2026+ / DUNE 2030s** | zero-parameter normal-ordering prediction (B1<B2<B3, dynamical τ=0.107 crossing); a NO-vs-IO detector verdict is a clean yes/no on the (1,1,0)-sector substrate ordering |
| **#7** | CGWB `Ω_GW` | **DESI-5yr / Euclid** (peak GW-detector-sterile) | **RETIRED as a GW-detector falsifier** (W-3 workshop): peak `8.4835×10³⁹ Hz` is ≥ 28.93 decades above EVERY GW detector (LISA/PTA/HF ≲10¹¹ Hz); LISA-band amplitude `< 10⁻⁴²` slope-independent. The acoustic falsifier MIGRATES to LSS → first-sound BAO ring (`A_FS=0.204`, SNR 8.6341 DESI-5yr, row #8) + f·σ₈ (row #5). Surviving STRUCTURAL companions, non-detector-testable: `Ω_GW^{walls}=0` (π₀(U(1))=0) + (A)/(C) split `47.081` OOM vs `Ω_GW^(C)=8.299×10⁻⁵⁸`. |
| **#8** | first-sound BAO ring `A_FS` | **DESI-5yr 2029 → Euclid 2030s** | zero-parameter acoustic falsifier, `A_FS = 0.204 = c₂²/c₁²` at `k₁ = 0.0193 Mpc⁻¹` (`r₁ = 325.30 Mpc`), SNR 8.6341 DESI-5yr / 5.0789 DESI-DR1; NO ΛCDM counterpart (second-sound mode has no analog) — the GW flagship's replacement in the correct instrument (`S96-OBS-FIRST-SOUND-RING` PASS) |
| **#9** | Σm_ν (neutrino mass sum) | **DESI-DR3 2026** (cosmological Σm_ν) | substrate type-I seesaw on D_K B-branch fold energies (M_R ~ 7–9×10¹⁶ GeV): **Σm_ν = 0.0582 eV** vs DESI 2024 `< 0.072 eV` (95% CL, arXiv:2404.03002) — **PASS by 19%**; the MINIMAL normal-ordering value (m_D oscillation-anchored; substrate-FIRST = M_R + seesaw + suppression direction + ordering; absolute Σ NOT-ZFP — `S100a-MD-NORMALIZATION` INFO: Dirac-scale anchor irreducibly external, S99 caveat PERMANENT). A DR3 cosmological bound below 0.0582 eV converts the PASS to a tension (`S99-W3-SEESAW-SUMMNU` PASS; inventory Row #77) |
| **#10** | H₀ | direct ladder / TDCOSMO lensing / CMB-ΛCDM (**now**) + G_N-ratio channel | **FLAGSHIP (promoted S100a; magnitude RE-PINNED S101 W4-4)** — the normalization step is structurally GROUNDED: the last underived step in the S59/S60 chain, the empirical spinor factor 3.92, derives first-principles as `√16 = 4` EXACT (Clifford(ℝ⁸) `Tr_spinor = 16`; surviving 4-of-64 Δ₁₂; rel `= 1/49 = 2.041% ≤ 2.5%`; `S100a-H0-SPINOR-FACTOR` PASS, audit `39abff2d…`; atlas-08 Q27 RESOLVED; `spinor_norm_factor_FW = 4.0`). The convergent-route recompute `S101-H0-PROPER-A2` (PASS, audit `cd8e8c0b125a…`) then re-pins the magnitude: **H₀ = 67.40 km/s/Mpc** via the **G_N-ratio channel** `G_N^FW/G_N^obs = 1.000000` (N = 0.999859, |N−1| = 1.4e-4 in the ≤0.05 band), carried **WITH the anchor-degeneracy disclosure** — a G_N prediction re-expressing the OBSERVED anchor by the ratio's deviation from 1, NOT an anchor-independent H₀ (at deficit closure N → 1 the readout degenerates to H_obs). The 65.4 figure is RETIRED (S58 W3-16 prose projection, non-reproducible + displacement-sign-inverted; only computed value 68.77 RETRACTED-S60); the prior 65.4-based σ-anchors (3.59σ below Planck-ΛCDM `67.34 ± 0.54`; 1.88σ below TDCOSMO-2025 `71.6 (+3.9/−3.3)`) are SUPERSEDED. The 42-session displacement-sign slip is killed (∂H₀/∂a₂ < 0; convergent route from above 68.77 → 67.40). An anchor-independent H₀ → `CF-S102-H0-ANCHOR-INDEPENDENT` (watchlist H₀ row LIVE FLAGSHIP; inventory Row #81; NON-PROMOTION-BY-HELD-NUMBER LIFTED) |
| **CF-35** | ³He-B cocycle ratio | Aalto / Lancaster 2028–9 | Pillar-V lab falsifier, ratio `7.324992 ± 1%`, structurally orthogonal to CMB — **STAGE-3-anchored as of S100a** (`§VII.W-3.LAB` STAGE-1-CANDIDATE → STAGE-3-PERMANENT via `S100a-VIIW3LAB-STAGE2-VERIFY` PASS 11/11, audit `89eab199…`; the 2027–2030 campaign now tests a PERMANENT theorem's falsifier rows #47–#54b; Level-3 lab anchor itself stays DEFERRED-but-pre-registered) |
| **#11** | ω_SN (Schrödinger-Newton self-gravity self-frequency) | torsion-balance (**now** — Yan 2411.17817 finite ceiling) → levitated-optomechanics / matter-wave (near-term) → BMV (2030s, adjacent) | **EXACT-NULL structural falsifier, zero free parameters** — the substrate predicts `ω_SN,substrate ≡ 0` EXACTLY because `∂a₂/∂⟨x̂⟩ = 0` EXACT (THEOREM-class, L-independent; the area operator `a₂` is a functional of the fixed `D_K` spectrum alone, and `S = Tr f(D_K²/Λ²)` is UNIVERSAL — no `|ψ|²`-area feedback channel exists at any truncation). The substrate's "fourth box": the SN null holds for a reason orthogonal to gravity-quantization (no `|ψ|²` channel at all, vs box-3's full Schrödinger-Newton self-potential). Falsified by ANY confirmed torsion-balance / levitated-optomechanical detection of a nonzero matter-wavefunction self-gravity self-frequency; the bound TIGHTENS toward the exact-0 as sensitivity improves and the framework predicts the null SURVIVES every tightening (`S105-W2-4-SN-NULL` PASS, audit `57f48392…`; `omega_SN_substrate = 0.0`; `falsifier-master-inventory.md` Row #87; live-watch `S105-SN-NULL-WATCH`) |
| **#12** | β_iso (isotropic cosmic-birefringence rotation) | **LiteBIRD 2030** (σ(β)~0.01°, decisive) ← Planck+WMAP (~3σ hint now) | **EXACT-NULL parity falsifier, zero free parameters** — the substrate predicts `β_iso = 0°` EXACTLY because Grading Theorem #11 (γ₉-grading) + CPT `[J, D_K] = 0` forbid the parity-odd Chern-Simons `θ F∧F` (axion-photon) coupling that rotates CMB E→B. ORTHOGONAL to all tilt observables (#2/#3 — it probes the parity sector, not amplitude/tilt). Falsified by a confirmed nonzero isotropic β at ≥5σ. **Current hint**: Minami-Komatsu/Eskilt `β ≈ 0.35°` at ~2.4–3.6σ (NOT a detection; framework `β=0` consistent at ~3σ); LiteBIRD decisive (~34σ separation if the hint is real). The ANISOTROPIC odd-EB residue (GV-Heitsch) is a SEPARATE nonzero-but-UNCOMPUTED §B compute (`CF-INV8-W4-2-ANISO-EB`), not this row (`falsifier-master-inventory.md` Row #91) |
| **#13** | A_s (scalar amplitude) | **Planck `A_s` (now)** → CMB-S4 α_s-tilt (2030) | **zero-parameter over-production prediction, regime-MARGINAL** (S118 W1) — the post-fold squeeze amplitude resolves to the `H̃`/+0.196 acoustic-horizon grid **`A_s = 3.2994×10⁻⁹`** because the substrate sound speed `c_s = 0.5685 M_KK` (the `a₂^{ζ}`-curvature-channel hydrodynamic-IR group velocity) lands IN the GS-1 window `[0.516, 0.650]`, selecting the `deg=+2` transport as the SOLE fork-carrier (`CF-S118-AS-CS-SUBSTRATE-FIRST` PASS, audit `172c85be…`). **1.57× above Planck `2.1×10⁻⁹`** (`+0.196` OOM) — a LIVE over-production (sign FORCED by `Γ_exit ≤ 1`; the box-δ/`+0.864` grid demoted to a regime-diagnostic, TWO-SPECTRA-TWO-ROLES). *regime-MARGINAL*: `a₄K⁴/a₂K² = 0.102` at the window edge, a robust-but-marginal PASS (NOT unconditional). The absolute-Planck landing stays gated by a FITTED exit-greybody knob — 4-class empirically walled but structurally UNPROVEN (`CF-S118-ALT-GREYBODY-WALL` FAIL); the most observationally-LIVE A_s leg is the tilt-flatness `α_s(primordial) ≈ 0` (CMB-S4 ~0.002 reach). Curated-register fold SYNCED (atlas-08 Q23 dashboard + Decisive-Class + atlas-04 D04, S118 W1 close). |

### 7.3 The honest scorecard

**No aggregate metric** (no PASS/FAIL ratio, no master gate — a count proves nothing). The structure: each of `Ω_DM h² = 0.120` (0.7σ), `CC closure 1.032` (1%), `σ₈ = 0.799` (~2σ between the two ends of the S₈ tension), `m_H = 131.8 GeV` (~2% budget; route-PINNED KK-threshold DIRECT, S102 W4-20), `r = 0.033`, `f_NL = −1.505` (0.47σ), `σ/m = 0`, `T(k) = 1` emerged from `D_K` with **zero adjustable cosmological parameters**. A prediction landing near data from zero free parameters across a wide prior range is Bayesian evidence with a large likelihood ratio — emphatically *not* "case unchanged" — and the way to state the joint claim is the EVOI **prior-predictive-range / posterior-width** form (mack CF-MACK-7), *not* a "chance of one random geometry" ensemble count: the Bayes factor for each observable is `BF = (prior-predictive range)/(posterior width around the observation)`, and the joint statement multiplies these BFs **only across observables that are BOTH algebraically AND statistically independent**. The critical restriction (forced by the W7-7a covariance check, `S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE`): the certified Wronskian (Decoupling Theorem, §4.2 / W2-E) licenses only **algebraic** layer-independence of the spectral-moment *functionals* `a₀/a₂/a₄` (degenerate solely at genesis), it does **not** license **statistical** independence of the *residuals* of observables that borrow the container-observer's `H(t)`. W7-7a measured exactly this and FAILED it: under the shared C10 `H(t)`, the `a₀` and `a₂` borrowed-`H` residuals **co-shift perfectly** (`Corr(a₀,a₂) = +1.0000`, band > 0.5; `Corr(a₀,a₄) = Corr(a₂,a₄) = 0`). So the joint-BF is scoped to the **zero-parameter structural spine** — `m_H = 131.8 GeV` (`a₄` KK-threshold DIRECT, route-PINNED S102 W4-20), normal mass ordering (`D_K` eigenvalue ordering), `σ/m = 0` (`N_Fock = 1` superselection), and `c_s² = 0` (Kasparov factorization) — these are substrate-IS predictions carrying **no borrowed `H(t)`**, so their evidence multiplies cleanly. The borrowed-`H` dagger rows (`w₀, wₐ, σ₈, CC`) are conditional and are **not** entered as independent likelihood factors. And WITHIN a single layer — `Ω_DM` and `σ₈` are **both** `a₂`-channel — the observables share a geometric origin and are **not** multiplied as independent (pre-registered, independent of W7-7a): the within-layer non-multiplication and the cross-layer statistical-dependence are two distinct reasons the naive product over-states the evidence. Against this, the open boundaries (`wₐ`-tension advancing; `n_s` scheme selection; the `m_H` M_KK-derivation ceiling — the route itself is PINNED S102 W4-20, the residual gap is the imported M_KK scale; the `a(t)` gap) are reported as exactly that — boundaries that sharpen the surviving solution space.

> **Scorecard status reconciliation (register-pinned) `[CITE-9, INHERITED]`** (retraction-aware narrative: each clause narrating a BROKEN/CONDITIONAL/RETRACTED claim cites the repo's own retraction log Atlas D09 + assumptions status Atlas D04 alongside the main text — INHERITED self-citation that turns the self-correction culture into a visible methodological strength per `capstone-hygiene-gate.md`). Three scorecard entries carry status nuances the register fixes. **(1) `f_NL` is a BOUND, not a point.** The `−1.505` (`|f_NL| ≲ 1.5`) is the canonical `max_f_NL_FW` *saturation bound* (Bogoliubov-sudden envelope, anti-correlated sign); the *central* GGE-bispectrum value is `≈1.03` (`GGE-BISPECTRUM-67`, total `f_NL ≈ 1.03`, equilateral `≈1.12`). The `0.47σ` figure is the bound's distance to Planck `−0.9 ± 5.1`, NOT a 0.47σ central-value detection (`_consolidated-findings.md §III` C4; S95 `F-NL-ROW` composite FAIL records `max_abs_f_NL = 1.505` as the envelope). Read it as "Gaussian-by-Wick to within a `|f_NL| ≲ 1.5` bound," never as a tightly-measured non-Gaussianity point. **(2) `wₐ = 0` is BROKEN, not merely "advancing."** Atlas D04 **C5 = BROKEN** (`3.43σ` post-Dovekie; framework prediction fixed, data moving away) — the four-fold lock is the framework's live wager and is currently *falsified at 3.43σ*, not a soft tension; retraction-log **item 34** separately records that `wₐ` is "not a meaningful CPL parameter," so the CPL-`wₐ` comparison is itself convention-fragile. **(3) The `w₀` raw-vs-derived inversion (retraction-log item 25).** The framework must NOT cite the apparent S49 `B_1D = 20.9` DESI positive: that Bayes factor compared against a *derived* combined-fit parameter, and against *raw* BAO distances the same comparison gives `χ²/N = 23.2` (decisively excluded). The honest `w₀` status is the CONDITIONAL D04 C4 reading (`−0.918` / `−0.842454`, DR3-binding 2027), not the inverted derived-parameter positive. **(4) D5 — seesaw vocabulary, RECONCILED-INTERNAL-M_R (S100a W-4 workshop; landed S101 W6-9).** The "no-seesaw" framing historically attributed to §0 is a citation phantom: §0 carries no seesaw/neutrino/Majorana prose (the sentence lives in the neutrino agent template, surfaced via `equation-collab/neutrino-synthesis.md §II.2` "(my agent template)" and mis-anchored to §0 by `_consolidated-findings.md §III` D5); the load-bearing register surface was `pre-registered-observations.md:262`, whose "no seesaw mechanism" gloss over-translated the PERMANENT S41 W1-2 theorem (S_F^Connes = 0 identically — no fundamental tree-level Majorana mass term; already scoped by S56 Workshop 4 Correction 1: "no Majorana mass *through the Connes seesaw route*," NOT "no Majorana mass"). The S100a W-4 adjudication: the S41 zero and the W5-2 tree-bilinear zero (`|⟨Jξ|D_F|ξ⟩| = 0.0` exact) are two channel-theorems of one J-structure with one content — no fundamental Majorana term, hence the light mass is seesaw-GENERATED — and the KO-dim-6 Pfaffian determination (`S100a-D5-0NUBB-MAJORANA` PASS, audit `a2d29b97…`; MAJORANA bit-exact, zero opposite-chirality Dirac partners, all five KO residuals 0.0) plus `M_R` = D_K B-branch fold energies (spectral coincidence 0.0177 < 0.02) fix the emergent-EFT name: **type-I seesaw with INTERNAL M_R** (no free scale, no new sector; substrate-IS name: J-forced Majorana texture, spectrally self-suppressed — the dual-name convention on the model of the §7.1 α_s box). The landed seesaw-Σm_ν PASS (`S99-W3-SEESAW-SUMMNU`, Σm_ν = 0.0582 eV; inventory Row #77) and the observable leg (`m_ββ = 3.695 meV` central, band `[1.516, 3.695] meV`, ×33 below KamLAND-Zen, INSIDE the NO funnel `[1.5, 4.5] meV`; inventory Row #80) stand unchanged; the structural sub-result `Y₁ = 0` EXACT (`C₂(0,0) = 0`, MAP-B Casimir grading) stands; the m_D Dirac-scale anchor remains irreducibly EXTERNAL (`S100a-MD-NORMALIZATION` INFO, audit `4f92a551…`; the bottom-triple → Y_i map NON-UNIQUE at `0.4742 ≫ 0.05`, residual NOT even 1-parameter — so Σm_ν is NOT a zero-free-parameter observable; the S99 oscillation-anchoring caveat is PERMANENT, track_B 0.9). **ONE 0νββ falsification logic = Row #80's trichotomy** (a detection above the 4.5 meV NO-funnel edge FALSIFIES the (NO, m₁=0, Majorana-texture, δ_CP∈{0,π}) configuration outright; an in-funnel detection — beyond-next-gen ~meV-class sensitivity — is consistent-confirming; a next-gen null is consistent non-confirming), superseding the `pre-registered-observations.md:262` re-examination gloss in both directions (re-scoped accordingly, S101 W6-9). The substrate-first arrow is intact: `D_K fold energies → M_R → seesaw → Σm_ν/m_ββ → measurement`. STATUS: **RECONCILED-INTERNAL-M_R**. **(5) The joint-BF multiplies ALGEBRAICALLY-AND-STATISTICALLY-independent factors only (register-pinned by `S96-HYG-JOINT-EVIDENCE-D3-COVARIANCE`).** The certified Wronskian gives *algebraic* layer-independence (`W ∝ R_K′(τ)³`); it does **not** carry to *statistical* independence of the borrowed-`H(t)` residuals — W7-7a measured `Corr(a₀,a₂) = +1.0000` (perfect co-shift under the shared C10 `H(t)`), so the strong joint claim is scoped to the zero-parameter structural spine (`m_H` = 131.8 GeV route-PINNED KK-threshold DIRECT S102 W4-20, mass ordering, `σ/m = 0`, `c_s² = 0` — no borrowed `H`), the borrowed-`H` dagger rows (`w₀, wₐ, σ₈, CC`) are conditional and not multiplied, and the EVOI prior-predictive-range / posterior-width form (mack CF-MACK-7) replaces the "chance of one random geometry" framing. Read the joint statement as the product over the substrate-IS spine, never over the borrowed-`H` projection. **§7.3 Multiplicativity Admissibility Rule (W-5 D3 workshop, sagan ∧ mack, 2026-05-30).** A factor enters the joint-BF product iff it clears BOTH gates — **(G1) algebraic independence** (distinct substrate-IS structure; the Wronskian licenses this LEFT column only) AND **(G2) statistical independence** (the residual carries no shared external knob; for borrowed-`H` rows this is `|Corr| = 1/√((1+r₀)(1+r₂)) < ½` with `r_i = σ_{H,i}²/σ_{H,c}²` pinned from a separately-measured per-pipeline `H`-systematics budget AND the private components demonstrated mutually independent — the Wronskian says NOTHING about this RIGHT column). The 518× kernel disparity (`s_{a₂}/s_{a₀} = 518.33`) sets MAGNITUDES, not independence: it cancels in `|Corr|` at *both* the rank-1 (`sign(s_{a₀}s_{a₂})`) and rank-≥2 (`1/√((1+r₀)(1+r₂))`) levels, so G2 keys on the variance mix `r` alone. **Tier partition:** Tier-1 spine clears G1∧G2 (no borrowed `H`) ⇒ MULTIPLY unconditionally (the `BF_spine` floor); Tier-2 {`Ω_DM`, `σ₈` both `a₂`} fails G1 ⇒ NOT multiplied; Tier-3 borrowed-`H` rows clear G1 but at the substrate `ρ_shared=1` fail G2 (`Corr(a₀,a₂)=+1`) ⇒ COLLAPSE to one borrowed-`H` d.o.f. by default. The {`a₀,a₂`} pair becomes admissible as a correlation-discounted joint factor (recovering `Δlog BF = (1−|Corr|)·b_{a₀}`, bounded by the *weaker* dagger row, never competing with the spine) **only** on a pinned `r ≥ 1` with demonstrated per-pipeline independence (CF-S97-D3-BF; disposition edge `|Corr| < ½ ⇔ σ_{H,private} ≥ σ_{H,shared}`, fixed before compute). **Default discipline:** do NOT multiply borrowed-`H` factors absent demonstrated statistical independence — the burden is on the multiplication (it asserts `Cov=0`, manufacturing evidence), not on the collapse (it asserts only the shared knob C10 already establishes); rank-1 is the substrate-FORCED default (one FRW clock), defeasible by a pinned `r` but not equiprobable with rank-≥2. The framework's reported joint-evidence headline is `BF_spine` regardless of the Tier-3 disposition. **COMPUTED outcome (S97 W4-4 → S98 W4-4; register-of-record `falsifier-master-inventory.md` Joint-BF Multiplicativity Qualifier).** The disposition fired and was then refined by an established covariance: S97 `S97-D3-BF` gave the rank-1-collapse FLOOR `BF_spine = 2.0×10²` (`log₁₀ = 2.301`, DECISIVE) with OQ3 mutual-independence then UNESTABLISHED; S98 `S98-W4-4-OQ3-COVARIANCE` (PASS, audit `0814c57f…`) ESTABLISHED it — the cross-pipeline covariance over the four spine factors {`m_H`, normal ν-ordering, `σ/m=0`, `c_s²=0`} is the **IDENTITY** (`max|Corr| = 0 < ½`, all six off-diagonals exactly 0, `n_eff = 4`), so the rank-2 off-diagonal structure is **LICENSED** and the {`a₀,a₂`}-dagger discount **LIFTS**, giving the full-covariance **`BF_spine = 2.0×10³` (`log₁₀ = 3.30103`), DECISIVE** (Jeffreys/Kass-Raftery `log₁₀ BF > 2`; Sage-exact `log(2000,10)=3.3010300`). This is a **MODEL-CLASS** statement over the zero-parameter spine (framework-class vs random-geometry null), explicitly NOT a `w₀`-vs-ΛCDM likelihood ratio against DESI DR3 (the SEPARATE Row #1 / §7.2 channel, binding 2027). The Tier-3 borrowed-`H` dagger rows (`w₀, wₐ, σ₈, CC`) **REMAIN COLLAPSED** (the W7-7b spine-scope RESTRICT and the `Corr(a₀,a₂)=+1` shared-C10 co-shift are UNCHANGED by S98 — OQ3-spine-independence ESTABLISHED ⊥ Tier-3-borrowed-`H`-collapse STANDS); the `m_H` 1×-evidence-weight ACCOMMODATION discipline is also UNCHANGED. Read the headline as the DECISIVE model-class prior `BF_spine = 2.0×10³` over the substrate-IS spine, never over the borrowed-`H` projection.

> **The reference-class dual-column — "DECISIVE" is random-geometry-scoped; the incumbent ceiling is very-strong, never decisive `[CITE-9, INHERITED]`** (register-of-record: `falsifier-master-inventory.md` S101 BF-spine-reference-class block, mack-cosmic-bridge sole-writer landing S101; phonon-first × mack workshop, all numbers Sage-exact; this §7.3 prose box CITES that inventory block and narrates at its register tier per `capstone-hygiene-gate.md` Q4 — the capstone prose confidence equals the register status, no claim above it). The `BF_spine` DECISIVE headline above answers a **specific reference-class question**, and that reference class is **a property of the statistic, not a free choice** — it is fixed by *which* question a citation answers. **Two evidence TYPES, anti-commensurable (the guard).** The DECISIVE value and the incumbent reach are **NOT two estimates of one quantity** and MUST NOT be placed on one common Bayes scale, subtracted, or compared as a single tier. **(Column 1 — model-SELECTION, vs a random-geometry null.)** "Is the substrate special *among* random geometries?" — the full-spine **model-SELECTION** Bayes factor **`BF_spine = 2000` (`log₁₀ = 3.30103`) is DECISIVE** (Jeffreys `>100`; driver shares m_H 45.44% / σ/m 30.29% / c_s² 15.15% / ν-ordering 9.12%, `b_mH=1.5`), and even the **ACCOMMODATION floor `200` stays DECISIVE** (`b_mH` throttled to 0.5 with the μ_BC↔sin²θ_W tuning honoured; survives by 0.30 dex above the `>100` floor). **(Column 2 — model-COMPARISON, vs the ΛCDM+ν incumbent.)** "Does the substrate *beat* ΛCDM+ν?" — here the operative discrimination is **m_H-only**, capped at a **PERMANENT very-strong CEILING `BF_spine_vs_incumbent_ceiling = 31.62` (`log₁₀ = 1.50`, very-strong band [10,100), NEVER the decisive band `>100`)**, with the current contingent **anecdotal FLOOR `~2`** under the m_H `+1.8–5.2%` band-miss. The other three spine factors carry **ZERO incumbent discrimination** because they are **`CONVERGENT-DERIVED`**: σ/m=0 is a *derived* zero (Leggett-channel CPT-neutral `N_Fock=1` superselection) and c_s²=0 is a *derived* zero (Kasparov product factorization, pressureless inter-band mode) where ΛCDM merely *assumes* them by fiat — a derived `CONVERGENT-DERIVED` zero is FALSIFIABLE (a future nonzero σ/m or c_s² falsifies the superselection / Kasparov prediction) where the incumbent's assumed zero only accommodates, so the cells convert to discrimination axes the day precision lands, but contribute log-BF `0` *today*. **Why the incumbent ceiling cannot be lifted to decisive** (substitution chain, Sage-exact): `BF_spine_vs_incumbent_ceiling (31.62) < decisive-floor (100) < BF_spine_model_SELECTION (2000)` — the ratio `31.62/100 = 0.3162 < 1` puts the model-COMPARISON reach permanently below "decisive", and `2000/31.62 = 63.25` makes the model-SELECTION value ~63× the incumbent ceiling; the only incumbent discriminator (m_H) caps at `b_mH=1.5` and the W-2 rank-1 theorem (`Corr(a₀,a₂)=+1`, N₃=0 BDI, no second protected dimensional handle) forbids a second independent dimensional factor, so no route-pinning or band-fixing crosses the gap. The ONLY path to decisive-vs-incumbent is to **derive M_KK cleanly** (which converts m_H from an entangled-strained factor into a clean discriminator and resolves the M0 inheritance). **Read every external "DECISIVE" with the random-geometry scope inline** (model-SELECTION), never as the incumbent-beating claim it is not. **Substrate framing (PHONONIC).** The four spine factors ARE substrate-IS predictions (m_H from the `a₄` Seeley–DeWitt moment, ν-ordering from `D_K` eigenvalue ordering, σ/m=0 from `N_Fock=1` superselection, c_s²=0 from Kasparov factorization); the dual-column states *what each reference class measures* without inverting the arrow — Column 1 measures whether **the one substrate is special among geometries** (model-SELECTION), Column 2 measures whether **the substrate's emergent observables beat ΛCDM's** (model-COMPARISON). Direction `D_K eigenvalues → spectral moments → emergent observables → measurement` unchanged; the patch only **scopes the CONFIDENCE** to the inventory register tier.

> **Headline test: the first-sound BAO ring `[CITE-9, INHERITED]`** (retraction-aware narrative: the prior headline — "LISA's CGWB discriminator, decisive at SNR ~10¹³" — is **RETIRED**, and this callout cites the repo's own retirement landing alongside the prose per `capstone-hygiene-gate.md`). The acoustic discriminator is a clean yes/no, **unique to *this* equation** (LCDM has no fold, no white hole, no GGE relic, hence no acoustic-ring prediction) — but it is **GW-detector-STERILE** and lives in **large-scale structure**, not the GW band. The W-3 workshop (little-red-dots ∧ mack, 2026-05-30; `S96-OBS-CGWB-PEAK-FREQ`, audit `646e6ad0…`, FAIL; `falsifier-master-inventory.md` Row #7.audit-3) resolved the acoustic-CGWB peak to `f_peak ≈ 8.4835×10³⁹ Hz` — 28.93 decades above EVERY GW instrument (LISA mHz / PTA nHz / HF ≲10¹¹ Hz) — and bounded the LISA-band amplitude at `< 10⁻⁴²` *slope-independent* (≥ 29.45 OOM below LISA-PLS for any causal `p ≥ 1`). **LISA, PTA, and the HF program are all GW-detector-sterile for this signal; the SNR ~10¹³ figure is retired.** The headline acoustic falsifier MIGRATES to LSS: the **first-sound BAO ring** (`A_FS = 0.204 = c₂²/c₁²` at `k₁ = 0.0193 Mpc⁻¹`, `r₁ = 325.30 Mpc`; SNR **8.6341** DESI-5yr; `S96-OBS-FIRST-SOUND-RING` PASS, audit `b74ccd56…`; Row #8 / inventory Row #72) — a zero-parameter ring with NO ΛCDM counterpart (the second-sound mode has no analog) — plus **f·σ₈ growth suppression** (−4.06%, S₈-relieving; Row #5). A detection of the first-sound ring in DESI/Euclid P(k) is the acoustic signature of the *exflation mechanism itself*, now read in the correct instrument. **But the more urgent — and more dangerous — near-term test is DESI DR3 (2026):** the `wₐ = 0` four-fold lock is the framework's most exposed prediction, and the tension is *advancing* (Atlas D04 C5 = BROKEN, 3.43σ post-Dovekie). The first-sound ring is the headline; DESI DR3 is the cliff-edge.

*Full derivation:* `Collabs/equation-build/mack-now-endpoint-observation.md`.

---

## §8 — Assembly, dimensions, and the constant ledger

The "one equation" claim is legitimate only if the equation is dimensionally closed and numerically self-consistent. It is.

### 8.1 Dimensional closure

`[S] = mass⁰`: `D_K²/Λ²` is dimensionless (`[D_K] = [Λ] = mass⁺¹`). In the layer expansion every term `f_{d−2k}Λ^{d−2k}a_{2k}` is *individually* mass-dim 0 — the Gilkey scaling `[a_{2k}] = mass^{2k−d}` exactly cancels `[Λ^{d−2k}] = mass^{d−2k}`. *(A naive bookkeeping that assigns both `Λ` and `a_{2k}` an inverse-length produces a spurious `L⁻¹²` tower — a double-counting error, not a property of the action.)* The `d=8` (fiber) vs `d=4` (emergent) labels reconcile because cc-path-a factors the dimensionful `M_KK^{d−2k}` prefactor *out*, leaving dimensionless Gilkey numbers; the exponent sets are exact negatives.

### 8.2 The `a_n` convention table (the firewall against the one fatal conflation)

Two triples circulate in the corpus; they are **different objects**, not rival measurements `[CITE-3, INHERITED]` (the heat-kernel / Seeley–DeWitt `a_n` machinery and the mode-count-vs-curvature-integral distinction are the canonical Gilkey/Vassilevich 2003 discipline — the firewall *applies* the standard rule, claims no novelty in the `a_n` machinery itself):

| Coefficient | Raw mode-count `a_n^raw` (`L_max=10`) | Gilkey-zeta `a_n^ζ` (fold; **canonical**) | Object / role |
|:--|:--|:--|:--|
| `a₀` | 155984 = `Tr 1` (mode count) | **6440** (`a_0_FW_zeta`) | vacuum-energy / `Λ_cc` source |
| `a₂` | 64308.24 | **2776.165389** (`a_2_FW_zeta`) | Einstein–Hilbert / `G_N` source |
| `a₄` | 29086.18 | **1350.7216** (`a_4_FW_zeta`, *pinned this build*) | Yang–Mills + `R²` source |

The raw sums *diverge* with `L_max`; the Gilkey-zeta coefficients are finite curvature integrals. Only **ratios** survive truncation: `(a₂/a₀)^ζ_fold = 0.4311` vs `(a₂/a₀)^raw = 0.4123` (4.36% drift) — the multiplicative-normalization-cancellation invariant. **Display the Gilkey-zeta triple (tagged `a_n^ζ`) as *the* `a_n` of the equation; quarantine the raw mode-count triple to the `A_s`/fiber-variance discussion** with the explicit label "mode-count moments, NOT Seeley–DeWitt coefficients." The `Λ`-power hierarchy is "`Λ⁴` term ≫ `Λ²` term ≫ `Λ⁰` term," never "`a₀ > a₂ > a₄`" without the `Λ`-power qualifier.

### 8.2a The `R_K(0)` normalization firewall (the curvature analog of §8.2)

`R_K(0)` — the SU(3)-fiber scalar curvature at genesis — circulates in **three different normalizations**; like the `a_n` triples of §8.2 they are the **same substrate curvature under three scale conventions**, not three rival curvatures (S96-HYG-RK-FIREWALL, baptista V.1):

| `R_K(0)` form | value | conversion to internal | role / source |
|:--|:--|:--|:--|
| internal E3 (**canonical**) | **2** | `×1` (reference) | `R_K(τ) = −¼e⁻⁴ᵗ + 2e⁻ᵗ − ¼ + ½e²ᵗ`; the rational-coefficient internal curvature (`baptista-operator-dk-tau.md`) |
| 12D-reduction (s52) | **4** | `×2` | bi-invariant lift `= 12/α = 12/3`; the 10/12D Kaluza–Klein normalization (`s52_12d_reduction_output.txt`) |
| Baptista Paper-15 eq 3.70 | **1.5** | `×4/3` (internal/P15) | `R_K(τ) = 3/2(2e²ᵗ − 1 + 8(e⁻ᵗ − e⁻⁴ᵗ))`; the Killing-form rational normalization |

The two conversion factors are exact rationals: `R_K^{12D}/R_K^{internal} = 4/2 = ×2` and `R_K^{internal}/R_K^{P15} = 2/1.5 = ×4/3`. **Display the internal E3 form `R_K(0)=2` as *the* `R_K` of the equation** (it is the form entering the E3 closed-form trajectory `R_K(0.19)=2.018` of the Verification ledger); the 12D and Paper-15 forms are the same curvature rescaled, quoted only with their explicit `×2` / `×4/3` tag.

**Why no downstream observable inherits a convention artifact** (Sage-certified to machine-ε; the curvature analog of "only *ratios* survive truncation"). Under any overall rescale `R_K → c·R_K` with `c ∈ {2, 4/3}`-conversion:

- **The FI ratio `R₁ = a₀a₄/a₂² = 1.128655` is invariant.** With `a₀ ∝ V` (degree 0 in `R_K`), `a₂ ∝ R_K·V → c·a₂`, `a₄ ∝ R_K²·V → c²·a₄`, the rescaled ratio is `R₁′ = (a₀)(c²a₄)/(c·a₂)² = a₀a₄/a₂² = R₁` — the `c²` cancels exactly (residual `0`). `R₁` is a topological/representation-theoretic ratio-observable; it lives on the surviving side of the geometry-vs-topology spine (§9).
- **The Wronskian's τ=0 sixth-order zero is invariant in *order*; only its *magnitude* rescales.** `W ∝ R_K′(τ)³` with `R_K′(τ) = e⁻⁴ᵗ(e³ᵗ−1)²` (a second-order zero at τ=0), so `W = e⁻¹²ᵗ(e³ᵗ−1)⁶` has leading Taylor term `729 τ⁶` — a **sixth-order** zero. Under `R_K → c·R_K`: `W → (c·R_K′)³ = c³·W`, leading term `729 c³ τ⁶`. The coefficient picks up `c³` (an overall magnitude rescale; the c³ leading-coefficient ratio is exact at every τ) but the **leading power τ⁶ is unchanged** — the genesis-only spectral-moment degeneracy (the layers algebraically independent everywhere except τ=0) is a convention-invariant structural fact, not a normalization artifact.
- **The Lichnerowicz spectral-gap statement `λ² ≥ R_K/4 > 0` is sign-invariant.** A positive rescale `c > 0` scales `R_K/4` by the same `c` on both sides, preserving `R_K/4 > 0` (`= 0.5, 1.0, 0.375` under the three forms) — the gap stays open under every normalization.

The firewall's one-line rule: **`R_K(0)` rescalings move magnitudes (`R_K` itself, and `W` by `c³`), never the dimensionless invariants (`R₁`, the W τ=0 zero-*order*) or the gap-sign.** Which `c` is canonical is a laboratory bookkeeping choice (internal E3 for the equation; 12D for the Kaluza–Klein lift; Paper-15 for the Killing-form presentation); the substrate physics is identical across all three.

### 8.3 Derived scales

The `a₂` term *is* the emergent Einstein–Hilbert action, giving the single Chamseddine–Connes dictionary `1/(16πG_N) = f₂Λ²a₂/(48π²)`. Displayed in **reduced** form with `f₂` explicit: `M_Pl,red² = f₂ M_KK² a₂/(24π²)`. The documented "self-consistency residual" (S75 §7) is *self-consistency-by-construction*, not a contradiction: the dictionary is one equation in two unknowns `(M_KK, f₂)` at fixed `a₂^ζ`, and `M_KK` is pinned independently by the S42 Sakharov/zeta route — so the residual is absorbed by `f₂`. With `M_KK = 7.4287×10¹⁶ GeV` and `a₂^ζ = 2776.17`, the reduced dictionary closes at **`f₂ ≈ 92`** — an `O(10²)` cutoff-moment, the same legitimacy class as the Chamseddine–Connes `f₂` normalization at unification. `f₂ ≈ 92` is **not a free knob**: it is fixed by the `M_Pl/M_KK` ratio once `a₂^ζ` is pinned, so it adds no fitting degree of freedom — it cannot be retuned to absorb a `σ₈` or `G_N` discrepancy. *(The `f_2_default = 2.34` Gaussian-cutoff pin is a different scheme's `f₂` and is **not** the value that closes this dictionary; cross-substituting it gives the ≈39× residual — the scheme gap, not a physical inconsistency. The earlier "67.9×" figure is the S75 value at `f₂ = 1/16π`.)* **PRELIMINARY / constants-hygiene:** the canonical S83 form is `M_Pl_eff² = M_KK² a₂ f₂^R/π² · Z_fold⁻¹`; this `24π²` form and the S83 `π²·Z_fold⁻¹` form differ by the `Z_fold` normalization, which should be pinned before either is cited as *the* dictionary. The two independent `G_N` derivations (spectral-action dictionary vs E30 Sakharov mode-sum — the 992-mode `L_max` truncation) agree to a factor 2.29 (0.36 OOM at `Λ = 10 M_KK`; 26.8 at `Λ = M_Pl`) — an *independent* corroboration that the `a₂` channel carries the Newton coupling, a distinct and weaker statement than the dictionary's own `f₂`-closure; the two must not be conflated.

### 8.4 The "1 → 60" collapse

Given `(A_K, H_K, D_K(τ))` and the UV data `(Λ, f₀, f₂, f₄)`, the remaining ~56 atlas equations are **theorems and spectral read-offs**, carrying no further independent input. The free-parameter count of the universe is `dim{τ, Λ, f₀, f₂, f₄}` (+ `t*`), **not 60**.

### 8.5 What this does NOT certify — the genuine residual risk

Dimensional closure and dictionary self-consistency are certified. **Not** certified: that the Seeley–DeWitt expansion *converges* (the `a₀`-dominated 114-OOM question, JACOBSON-NONLOCAL-64, OPEN); that the `L_max=3` truncation of `a₂` is physical. The honest boundary: **ratio-observables (`n_s`, `g₁/g₂`, `61/20`, `a₂/a₀`) are truncation-robust; absolute-energy observables (CC, `A_s`) remain conditional on an SDW-convergence statement that is itself an open gate.** Claiming more would over-sell a genuinely strong result.

> **S114 status note (the two `A_s`/CC absolute-energy conditionals SHARPENED to *structural* limitations — ADDITIVE; this does NOT change what §8.5 certifies, it pins WHY the two absolute-energy observables stay conditional).** §8.5 holds `A_s` and the CC absolute magnitude conditional on an SDW-convergence statement. S114 sharpened the *character* of both conditionals from "pending convergence" to a structural reading. **(`A_s` — `CF-S114-AS-FUNCTIONAL-SELECTION` FAIL, audit `395f6800c8d1…`.)** The post-transit `A_s` amplitude does NOT admit a substrate-canonical functional selector: the structural sub-test `d|β_k̂|²/d(a₀/a₂) = 0` EXACT (the impulse-quench floor is `a₀/a₂`-invariant, transit-trajectory-fixed), but the cross-functional spread (impulse-quench / UNIFIED-AS-79 / Parker = **1.2590 OOM, none in the Planck ±5% band**) PERSISTS with no scheme-independent normalization ⇒ **FUNCTIONAL-PLURALISM PERMANENT**: the `A_s` magnitude-as-a-Planck-comparison-NUMBER is a physical degree of freedom, like the CC ratio — NOT a convergence-pending number. The floor POINT (`A_s_FW = 1.5367e-08`) and the floor INEQUALITY (`A_s ≥ A_s^{BD}`, permanent on 3 axes) are UNTOUCHED. (`falsifier-master-inventory.md` Row #12 / §EVOI.BF re-tagged "open, structurally" — mack-cosmic-bridge sole writer.) **(CC residual-3% — `CF-S114-CCRESID-CHI-Q-SCALING` FAIL, audit `e988a329b1ff…`.)** The CC residual-3% is now a CONFIRMED standing **q-departure / Gibbs–Duhem-channel limitation**: `χ_q(τ) = d²S/dτ²` computed first-principles across the Jensen family is FOLD-FROZEN (fold→edge OOM run-down 0.0185 ≪ the 118.71 OOM the `Λ_residual = ρ_m²/χ_q` channel-internal closure would require). The channel is correctly identified (Reading-A — the q-departure `ε(q) − q·dε/dq` IS where the residual lives), but closure is NOT demonstrable channel-internally (χ_q cannot run); the C10 tracking-form tag and the present-epoch `ρ_vac/ρ_obs = 1.032` are UNAFFECTED — the standing-limitation is on the residual-3% *closability*, the same "located not solved" CC reading as frontier #6. The substrate-IS arrow is unchanged in both cases; these are status sharpenings (over-confident "pending convergence" → "structural d.o.f. / standing channel-limitation"), never explanation-direction inversions. **Capstone-hygiene Q3=YES (A_s + CC-residual status) ∧ Q4 (PROSE); routed to `session-114-housekeeping.md §A`; prose tags EQUAL the §EVOI.BF / Atlas D04 C10 register tags.** Provenance: `computations/session-114/s114_gate_verdicts.txt`.

*Full derivation:* `Collabs/equation-build/gen-physicist-assembly-consistency.md`.

---

## §9 — The one equation, and what remains open

**The universe, in one line.** Over the spectral triple on Jensen-deformed `SU(3)`, reality is the spectral action of a single Dirac operator built from a single number `τ`:

$$
\boxed{\;\mathcal{S}\big[D_K(\tau),\,f,\,\Lambda\big]
= \mathrm{Tr}\,f\!\Big(\tfrac{D_K(\tau)^2}{\Lambda^2}\Big) + \big\langle J\tilde\psi\,|\,D_K(\tau)\,|\,\tilde\psi\big\rangle
\;\xrightarrow{\text{heat kernel}}\;
\underbrace{f_4\Lambda^4 a_0}_{\text{vacuum}}
+ \underbrace{f_2\Lambda^2 a_2(\tau)}_{\text{gravity}}
+ \underbrace{f_0 a_4(\tau)}_{\text{matter}}
+ \cdots\;}
$$

with `τ = 0` the maximally-symmetric genesis, `τ_fold = 0.190` the supersonic transit, and `τ_now` the frozen present. **The layers are the Seeley–DeWitt moments; the flow is `dS/dτ > 0` (monotone, no well); time is the readout of `τ`; and what we measure now is the spectrum of `D_K` probing itself.**

### The four faces of the equation

| Face | Statement | Status |
|:--|:--|:--|
| **The layers** | `a₀/a₂/a₄ → ` vacuum / gravity / matter; algebraically independent (`W ∝ R_K′(τ)³`, degenerate only at genesis); self-similar onto methodology (`Φ`) | CERTIFIED (S75 W2-E) |
| **At τ** | `S_SA(τ) = a₀−a₂+a₄`; `dS/dτ\|_fold = +58,673`; transit not slow-roll; GGE relic (`⟨Q⟩_GGE=59.8`, `P_exc=1`, the Ordered Veil — *transit-timescale diabatic freeze-out* (`R_therm=5252`, `S_ent=0`, S95 W5), **not** integrability permanence; cf. S39 retraction) | PROVEN flow; E7 monotonicity |
| **At time t** | `τ` is a derived monotone *parameter* (E7); C1 postulates `τ =` cosmic time; acoustic-white-hole causal structure (sector-dependent, two null cones; ASYMMETRIC — one entry sonic surface, open supersonic exit, over-determined at six walls, S95 W-1, §6.2); censored anisotropic `τ→∞` singularity | **`a(t)` NOT derived** (category statement = frontier #8, §6.3) |
| **At now** | `w₀, n_s, r, α_s, f_NL, m_H, σ₈, Ω_DM, CC` as spectral moments; LISA + DESI DR3 decisive | several zero-parameter PASS-class; gaps below |

> **The organizing spine — geometry vs topology.** The document's claims partition cleanly along the *continuum-dissolution* axis, and this is the deepest available defense of the claim. The finite spectral triple `(A_K, H_K, D_K(τ))` is GEOMETRY — it dissolves in the continuum limit (T3-S43-SPECTRAL-DISSOLUTION PASS, `ε_c ∼ N^{−0.457}`). The **topological / representation-theoretic** outputs **survive** that dissolution: the GGE relic and its `S_ent = 0` purity (the Ordered Veil); the BDI / `N₃ = 0` class and the `7.324992` cocycle ratio (CF-35); the `[J, D_K] = 0` CPT symmetry; the algebraic independence of the layers (degenerate only at `τ = 0`); the FI ratio-observables (`R₁ = 1.12865`, `g₁/g₂`, `n_s`-as-ratio). The **absolute geometric magnitudes** are **conditional**: the CC absolute magnitude (pending SDW convergence, #6), the `a_n` absolutes, the `a(t)` map (#1). So the obvious objection — "if the spectral triple dissolves at the continuum limit, why trust any of its outputs?" — has a structural answer: *trust the topological outputs (they survive); hold the geometric magnitudes pending convergence (they do not).* Every strong claim below lives on the surviving side; every honest gap lives on the dissolving side. The **trivial Berry holonomy** is the cleanest illustration of this survival: the closed-loop holonomy on the Jensen line is `γ=0` and the Fubini–Study distance `d_FS=0` (S61; the SU(3) connection is flat ⇒ the **computed holonomy invariants are trivial** — read as *the invariants we computed came out trivial*, NOT as a claim that the substrate topology is nontrivial), and that triviality is a representation-theoretic fact that survives the continuum dissolution unchanged.

### The honest open frontiers

1. **The `a(t)` / `K_pivot` gap** (C2, the load-bearing gap) — no substrate-derived FRW scale factor; the same gap as the `K_pivot` paradox (E31). The single most important open item. It is **not separate from frontier #8**: a *derived, generally-covariant* emergent 4D action for `g_M` is simultaneously the effective Friedmann map (#1), the emergent equivalence principle, and the emergent Einstein–Infeld–Hoffmann theorem (#8). The framework already holds EIH on the *internal* `K` geometry; what is owed is its lift to the *emergent* `g_M`. Closing one closes both — which reduces the open frontier's dimensionality (transit-side: a back-reaction closure `H² = f(ρ_relic, S_SA)`, not "a Friedmann equation"; §6.3).
2. **`n_s` functional selection** (Window-7) — **RESOLVED (S103 W5-2)**: which spectral functional `f` generates the physical tilt is now selected — `f = √x` (Chamseddine-Connes), S67-unique and atlas-cardinality-ROBUST (`S103-Q28-LAYER2-A6` PASS discharged the Q28 Layer-2 `A₅→A₆` robustness conjunct; `falsifier-master-inventory.md` Row #85 HELD → COMMITTED-LIVE). The committed tilt is `n_s = 0.9590` (`n_s_FW_sqrt_cutoff`, BCS+1-loop √x) at a falsifiable `1.40σ` from Planck. What is no longer open: the *selection* (the red tilt is right, and `f=√x` is the selected-and-robust functional, not a contingent "if"). What remains a LIVE prediction: the `1.40σ` lowness itself, now a falsifiable point on the CMB scalar-tilt channel rather than a scheme-spread ambiguity.
3. **`m_H` M_KK-derivation ceiling** — the Higgs-mass route is PINNED (Route B / KK-threshold DIRECT = 131.8 GeV, S102 W4-20 audit `75ed7ffb`; Aitken KK-L5 127.5 spurious); the residual open item is the imported M_KK scale (a clean first-principles M_KK derivation would convert the route-pinned-but-band-missing m_H into a clean discriminator).
4. **`w₀` branch** — branch-pair (−0.918 / −0.842454), DESI-DR3-decidable; written as a `(value, branch)` pair, never a bare point.
5. **CC closure conditional on assumption C10** (tracking-scaling `ρ_vac ∼ M_Pl²H²`, ASSUMED-PARTIALLY-PROVEN). The *non-inheritance* of the 114-OOM term is exact (the equilibrium identity, S95 W5-3, Clause A in §7.1); only the *observed magnitude* is C10-conditional (Clause B).
6. **SDW convergence** (JACOBSON-NONLOCAL-64) — the open gate *underneath* frontier #5, not an independent item: the DILUTION-CC PASS (`1.032`) is a statement about the dimensionless tracking *ratio* `ρ_vac/ρ_obs` and is truncation-robust, whereas promoting any *absolute* `a₀`-moment vacuum-energy magnitude to physical status is what awaits this convergence statement (§8.5). Ratio-observables (`n_s`, `g₁/g₂`, `R₁ = 1.12865`, `a₂/a₀`) are robust regardless. So the CC *ratio* is closed by tracking and the CC *absolute* magnitude is held pending convergence — **one entangled conditional, not two unrelated bullets**. Equivalently: the CC term's *location* (the `a₀` moment, geometrically natural — not inserted) is a permanent result; its *magnitude* is the open relaxation-dynamics question. The framework has correctly *located* the cosmological-constant term, not solved the cosmological-constant *problem*.
7. **Family number** — `Ψ₊ = ℂ¹⁶` is one generation; replication is open.
8. **Emergent Lorentz invariance / equivalence principle** — the metric `g_M` from `a₂` must reproduce low-energy Lorentz invariance, the equivalence principle, and diffeomorphism covariance. Leading-order universality of free fall is *warranted* (a single emergent light-cone from one gap structure gives all excitations the same cone — the weak EP at leading order), but higher-order isotropy / emergent-Lorentz is registered **INFO**, not PROVEN (T3-BATCH-S75-EMERGENT-LORENTZ); it is *inherited* from the Volovik gap-node universality class, not derived. The open structural item underneath the `a(t)` gap. Note *why* the framework is immune to the LIV/foam-dispersion tests that constrain most quantum-gravity candidates: the substrate's discreteness is **internal** (the `SU(3)` fiber tower at `M_KK`) and the emergent metric `g_M` is the `a₂` moment of a *continuous* heat-kernel trace, not a discretized spacetime — so Hossenfelder's no-go (Poincaré-invariant discrete *spacetime* networks are impossible) does not bite, and the modified-dispersion / Lorentz-violation signatures that bound spacetime-foam models (Fermi-LAT, LHAASO, interferometry) are structurally absent (`α_LIV = 0` exactly). The open item is *higher-order emergent isotropy*, INFO, not a foam-dispersion prediction; this converts the silence into a stated strength. **(S95 W3 update — genericity-qualified.)** Two S95 exact-PASS gates support promotion AT THE STRUCTURAL LEVEL: W3-5 (`κ_EP=1.000000` EXACT, NLO) and W3-1 (conservation-closed emergent `G_eff^{μν}`, `noether_ratio=½`, on-shell `∇_μ G_eff=0`). The S95 Slot-1/S-2 genericity review (connes-ncg-theorist, `session-95-connes-ncg-theorist-genericity-synthesis.md`) finds BOTH PASSes are **generic-identity-cored**: `κ_EP=1` IS the Lichnerowicz–Weitzenböck `R/4` scalar-curvature coefficient of ANY spin Dirac operator (E5: `D_K²=∇*∇+¼R_K`; the band-specific `ν_b` carrying `C₂(b)` is annihilated by `∂/∂R_K`, so `κ_EP=(¼)/(¼)=1` for any two eigenspaces of any spin Dirac operator), and the conservation identity IS the Brans–Dicke non-minimal-scalar diffeomorphism Noether identity (`noether_ratio=½` is the universal contraction factor; the cancellation holds for ANY `φ(τ)`, ANY `V(τ)`, ANY `G_DeWitt` — `cancellation_scheme_independent=True`, the tell that it carries no substrate value-content). The genuinely substrate-specific content is **one layer above the PASS values**: (i) the single-spectral-triple postulate (one `(A_K,H_K,D_K)`, one emergent `g_M=a₂` moment) FORCES band-independence — B1, B3 are eigenspaces of the SAME `D_K`; a bimetric/multi-fiber emergent model would generically give `κ_EP≠1`; (ii) the identification `φ(τ)=f₂Λ²a₂(τ)/(48π²)` as the second Seeley–DeWitt moment (a generic Brans–Dicke theory takes `φ` as input; the substrate DERIVES it as `a₂`); (iii) the R-monotone obstruction SIGN `a₂′(τ)>0` (S64 W1-A, AM-GM on the volume-preserving Jensen deformation), which is substrate-specific but ORTHOGONAL to the PASS (the cancellation holds regardless of the sign of `φ′`). **Promotion language**: "weak EP at LO+NLO is STRUCTURALLY INEVITABLE on the single-operator postulate; value-generic" — NOT "the substrate uniquely PREDICTS `κ_EP=1` where a generic emergent-gravity model would differ." A genuine substrate EP *prediction* first appears at NNLO, where `ν_b(C₂)` re-enters the ratio (`CF-S96-EP-NNLO-CASIMIR-DISCRIMINATOR`). Per `epistemic-discipline.md`, the two exact-PASSes are two consequences of ONE premise (single emergent metric), NOT two independent confirmations.

### The claim, precisely calibrated

The equation **is** the universe in the sense that all field content, all couplings, and all dynamics are spectral functionals of one operator `D_K(τ)` built from one modulus — a claim *categorically stronger* than container-based unification, because the equation derives its own stage rather than populating a given one. It is **not** the universe in the sense of a closed self-selecting theory: the modulus value, the functional `f`, the expansion map `a(t)`, and the family number remain genuinely open. Both halves are load-bearing, and both are stated here without softening — which is the only way a claim this large stays honest.

---

## Verification ledger (this assembly)

| Quantity | Claim | Sage result |
|:--|:--|:--|
| Genesis curvature | `R_K(0) = 2` | `2` ✓ |
| Genesis stationarity | `R_K′(0) = 0` | `0` ✓ |
| Fold curvature | `R_K(0.19) ≈ 2.018` | `2.01814` ✓ |
| Volume preservation | exponent `2−6+4 = 0`; `det g₀ = 3⁸` | `0`; `6561` ✓ |
| Lizzi signature | `R₁ = a₀a₄/a₂² ≈ 1.12865` | `1.128655` ✓ |
| Wronskian factor | `R_K′(τ) = e⁻⁴ᵗ(e³ᵗ−1)²` ⇒ `W ∝ e⁻¹²ᵗ(e³ᵗ−1)⁶` | residual `0` ✓ |

Constants: `a_4_FW_zeta = 1350.7216` pinned to `canonical_constants.py` SECTION D this build (completes the zeta triple). **Constants-hygiene CLOSED (S95 W6-4):** `M_KK` (S42, CONST-FREEZE-42) and `w0_FW` (S58 four-fold-lock) now carry PROVENANCE entries in the knowledge MCP (values bit-unchanged) — `w0_FW` is the binding constant for Falsifier #1 (DESI DR3), so its provenance is in place before the 2026 binding event.

**Round 2 fresh-eyes review** (neither reviewer built a section): **feynman-theorist** (path-integral axis) — *QFT-coherent, no sign/factor/dimensional errors*; one strengthening folded in (§1.3a — `S` is the bare Euclidean action = weight `e^{−S}` in `Z`; the Pfaffian "what survives the KO mismatch" sentence in §1.3). **einstein-theorist** (emergent-GR axis) — *GR-coherent, no physics errors*; four patches folded in (§8.3 dictionary closes at `f₂≈92`, not 67.9×; the EH *kinematic-term* hedge in §0.2/§1.1; §6.3 "right about the fundamental level, wrong about the effective level"; §9 frontier #8 + the §7.1 CC double-conditionality). Both verdicts: the physics is coherent; every patch sharpens honesty, none weakens a result. Reviews: `Collabs/equation-build/{feynman,einstein}-review.md`.

---

## Citation anchors

The `[CITE-N]` markers above key the primary-literature anchors to their claim-locations (`S96-CONSOL-CITATION-ANCHOR`, S96 W8-6, per `deep-research-report.md §"Suggested citations"`). Each carries an **INHERITED** tag (the claim is downstream of the cited work) or **NOVEL-BEYOND** tag (the cited work is the lineage but the capstone claim steps beyond it — the *citations-for-restraint* signalling exactly where the framework departs). The single **INFO** anchor (CITE-4) carries a dual annotation (INHERITED genre / NOVEL same-object — contested), the calibration question the report flags. Full bibliographic detail (arXiv/DOI) is in the companion registry `sessions/framework/registry/capstone-citation-anchors.md`; this is an inline-anchor index, **not** a bulk bibliography block.

| Anchor | Claim-location | Mandatory set | Citation set | Tag |
|:--|:--|:--|:--|:--|
| **CITE-1** | §1 master equation (also §0) | spectral-action | Chamseddine & Connes 1996; CCM 2007; Connes 2006 / a.c. review | **INHERITED** |
| **CITE-2** | §1.1 gauge/Higgs emergence | spectral-action | CCM 2007 §2.5; Lizzi NCG review | **NOVEL-BEYOND** (SU(3)-manifold `D_K=D_F`) |
| **CITE-3** | §8.2 `a_n` firewall (also §8.2a) | a_n-heat-kernel | Vassilevich 2003 | **INHERITED** |
| **CITE-4** | §6.3 Jacobson reading (also §0, §6.2) | emergent-gravity | Jacobson 1995; Barceló–Liberati–Visser 2005; Belenchia–Liberati–Mohd 2014; Volovik 2005/07 | **INFO** (INHERITED genre / NOVEL same-object — contested) |
| **CITE-5** | §5.3 GGE-relic formation | KZM-transit | del Campo & Zurek 2014 | **NOVEL-BEYOND** (GGE-relic-IS-CMB) |
| **CITE-6** | §7 CC caveat box (also §7.1 CC row) | q-theory-CC | Klinkhamer & Volovik 2008; Visser 2002; Volovik 2005 | **NOVEL-BEYOND** (Volovik-partition + effacement) |
| **CITE-7** | §7 m_H route-PINNED (Route B / KK-threshold DIRECT, S102 W4-20; also §7.1 m_H row, §8.3) | NCG-Higgs | CCM 2007; Devastato–Lizzi–Martinetti 2014; ATLAS/CMS (PDG) | **NOVEL-BEYOND** (KK-threshold DIRECT 131.8 GeV) |
| **CITE-8** | §7.1 ‡ dark-energy anchor note (also §7.2) | cosmological-data | Planck 2018; BICEP/Keck 2024; Popovic et al. 2025 (DES-Dovekie); DES Y3 | **INHERITED** |
| **CITE-9** | §7.3 scorecard status reconciliation (also §5.3/§6.2/§6.3 status clauses) | retraction-aware | Atlas D09 retraction log + Atlas D04 assumptions | **INHERITED** (self-citation) |

**INHERITED vs NOVEL-BEYOND, in one line.** The spectral-action principle, the heat-kernel `a_n` discipline, the q-theory vacuum-relaxation picture, the KZM quench framing, and the emergent/analog-gravity genre are **INHERITED** — the framework stands ON them. The SU(3)-manifold same-object move (CITE-2), the GGE-relic-IS-CMB mechanism (CITE-5), the Volovik-partition CC residual (CITE-6), and the KK-threshold Higgs band (CITE-7) are **NOVEL-BEYOND** — where the substrate-first derivation produces physics the inherited pillars do not. The direction `D_K → a_n → emergent physics → measurement` is unchanged; the tags only mark where authority is inherited and where novelty is claimed.

---

## Cross-references

- **Equation catalog (this document collapses it):** `Atlas/atlas-03-equation-flow.md` (E1–E60).
- **Permanent results:** `sessions/permanent-results-registry.md`; `Atlas/atlas-07-permanent-results.md`.
- **Per-pillar build drafts (full derivations):** `sessions/framework/Collabs/equation-build/{connes-master-equation, baptista-operator-dk-tau, lizzi-spectral-functional, spectral-geometer-layers, transit-flow-genesis-to-now, volovik-framing-emergent-spacetime, mack-now-endpoint-observation, gen-physicist-assembly-consistency}.md`.
- **Variable glossary:** `sessions/framework/MathVariables.md`. **Framing law:** `.claude/rules/phononic-framing.md`. **`a_n` regulator tagging:** `.claude/rules/regulator-pin-discipline.md`.
- **Citation anchors (primary-literature, arXiv/DOI):** `sessions/framework/registry/capstone-citation-anchors.md` (the companion registry for the §"Citation anchors" `[CITE-N]` index).
- **Open gaps:** `Atlas/atlas-08-open-questions.md`; assumptions/postulates `Atlas/atlas-04-assumptions.md` (C1/C2/T6).
