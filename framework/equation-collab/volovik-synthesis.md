# Capstone Equation Review — volovik

**Date**: 2026-05-29
**Agent**: volovik-superfluid-universe-theorist (superfluid-vacuum / emergent-spacetime axis)
**Source Documents**:
- `sessions/framework/phonic-exflation-equation.md` (the S95-era capstone under review)
- `sessions/framework/Collabs/equation-build/volovik-framing-emergent-spacetime.md` (§0 draft I authored; cross-check that the capstone incorporated it faithfully)
- `.claude/rules/phononic-framing.md` (binding framing law)
- Volovik corpus `researchers/Volovik/` (Papers 03, 04, 05, 06, 07, 13, 23, 25, 35) + agent memory
- Knowledge MCP: `w0_FW`, `tau_fold`, `c_fabric`, `CC_OOM`, `Gamma_effacement`; gates `S85-W6-4-EXTREMAL-HORIZON-FORMAL`, `LEGGETT-GRAV-DECAY-67`, `Q-THEORY-BCS-45`, `DILUTION-CC-66`, assumptions C10/C11

---

## I. Session Outcome

The capstone holds on my axis. Its central architectural claim — that geometry, gravity, and matter are the low-energy spectral moments of one fermionic operator `D_K(τ)`, with space the `a₂` moment rather than a container — is exactly the superfluid-vacuum program (Volovik Papers 03/05/06), and the document carries the IS-not-IN direction faithfully through all four runs (at-τ, at-t, at-now, assembly). The §0 framing I drafted survived into the capstone with the load-bearing caveats intact (C10-conditionality, the `w₀` branch-pair, BDI/`N₃=0` as inheritance-not-analogy). The most consequential thing I can certify from the superfluid side is **negative and structural**: the document does NOT over-claim the cosmological-constant result. It correctly separates Clause A (equilibrium `ρ_Λ = 0` exact, by the Gibbs–Duhem identity — Volovik Paper 04 §III; S95 W5-3 `EQUILIBRIUM-CC-WARRANT` PASS, Sage-rational `0`) from Clause B (the observed magnitude as a non-equilibrium tracking residual, conditional on C10 and on the borrowed FRW `H`). That double-conditionality is the honest statement, and the document makes it.

The genuine open frontiers on my axis are three, and each is a ripe harvest: (1) the **de Sitter identity** `T_dS = H/2π` that the tracking law `ρ_vac ~ M_Pl²H²` rests on is used as Gibbons–Hawking *input* (Paper 07), not derived from `D_K`; (2) C10 itself (the `H²` scaling) is ASSUMED-PARTIALLY-PROVEN and is the load-bearing conditional under the entire CC closure; (3) the back-reaction-closure gap of §6.3 (no derived `a(t)`) is, on the superfluid side, a missing two-fluid hydrodynamic equation `H² = f(ρ_relic, S_SA)` — and Volovik's two-fluid superfluid hydrodynamics is the single most direct template for closing it that the framework has not yet exploited. All three are computable.

---

## II. Key Results

### II.1 The emergent-spacetime lineage is structural, not analogical — and the capstone got the direction right

**Result**: The three-pillar superfluid-vacuum lineage (vacuum-as-quantum-medium → metric-as-collective-mode → topology-decides-robustness) is correctly transcribed into the spectral-action language. Classification: **GEOMETRIC** (the lineage is about the fabric `(A_K, H_K, D_K)` itself), with **PHONONIC** consequences (the soft modes are the fields).

The decisive convergence is pillar 2: Volovik's gap-node metric `E²(p) = g^{ik}(p_i − p_i^(0))(p_k − p_k^(0))` (Paper 06 §2, Eqs. 1–2) and the framework's `g_M` from `a₂` are *the same physical object* — an emergent metric read off the substrate spectrum — in condensed-matter vs NCG conventions. The capstone states this in §0 as "the same physical object," which is correct; it is convergence at the level of the mechanism, not a shared vocabulary. The microscopic Hamiltonian is fully specified here (the eigenvalue problem of `D_K` on Jensen-deformed `SU(3)`, E2), which is the non-negotiable: *if you cannot write the Hamiltonian, you cannot trust the effective theory.* Here we can write it, so the emergent claims are computable, not asserted — exactly Volovik's posture toward `³He`.

Pillar 3 is the load-bearing correction the document inherits correctly: the substrate is **BDI, `N₃ = 0`** (the `³He-B` *child* class), related to the laboratory parent by the inheritance morphism `χ_*: ℂ⊕ℍ⊕M₃(ℂ) → M₂(ℂ)` with `rank(ker ι_*) = 2` (E57), **not** an analogy. The capstone uses "inheritance / child realization" throughout and never "analogy" — compliant with the correspondence discipline (S86 W1b-T8; forbidden-word check passes). And it draws the right physical consequence: because `N₃ = 0`, the Fermi-point protection that would shield a `³He-A` vacuum (where `N₃ = 2`, Volovik Paper 03 Thm 1) is *absent*, which is *precisely why* the CC layer is a q-theory relaxation problem and not a topological-protection statement. This is the strongest single piece of internal coherence in the document on my axis: the topological class is not decorative — it dictates which CC mechanism is even available.

### II.2 The CC layer: equilibrium nullification is exact; observed magnitude is correctly held conditional

**Result**: `ρ_Λ = 0` at equilibrium is an exact thermodynamic identity (Gibbs–Duhem, `ε − μq = −P = 0`; Volovik Paper 04 §III / q-theory Paper 13). The observed `ρ_vac/ρ_obs = 1.032` (DILUTION-CC-66, PASS) is a non-equilibrium tracking residual conditional on C10. Classification: **PHONONIC** (the `a₀` moment is the vacuum-energy channel; dark energy is the tracking-vacuum departure-from-equilibrium).

This is the cleanest demonstration in the framework of why IS-not-IN is not stylistic, and the capstone's CC caveat box (§7.1, two clauses) is the most careful piece of prose in the document. Clause A (non-inheritance, warranted exactly): the 114-OOM catastrophe is an artifact of computing vacuum energy in a container-EFT *without* a UV completion; the substrate has its UV completion (`D_K`), so the bare term is removed by the `μq` identity, not by fine-tuning. This is "the vacuum-energy test the framework passes" in the precise sense of my standing methodology — *in any system where the microscopic theory is known, the vacuum energy is calculable and produces no catastrophe.* Clause B (observed magnitude): with `N₃ = 0` there is no topological protection, so `ρ_Λ = 0` is a *reference/boundary value, not an attainable interior point* (S62 #19, `dE_ZP/dq > 0`, no interior q-equilibrium; at `N_pair = 1` the system sits off-equilibrium with `P_vac = −0.688 ≠ 0`). The observed Λ is then the tracking residual, and its "not-tuned" status rests on C10, NOT on the equilibrium identity.

The document states this doubly-conditional structure verbatim and does not let "resolved" stand unqualified. I verified the chemistry of the claim against the corpus: the equilibrium-zero theorem `Lambda_eq = 0 (by Gibbs–Duhem identity, Paper 05)` is in the knowledge base (`s53_q_theory_gge_output.txt`), and the q-theory monotonicity `dE_ZP/dq > 0` (no interior equilibrium) is PROVEN (S62 #19; atlas A9). Both are authoritative and I do not re-adjudicate them. **What the document is honest about, and should stay honest about: the framework has correctly *located* the cosmological-constant term (the `a₀` moment, geometrically natural — not inserted), not solved the cosmological-constant *problem* (the magnitude is the open relaxation-dynamics question, conditional on C10 and the borrowed `H`).** §9 frontier #6 says exactly this. Agreement.

### II.3 The two-monotonicity discipline (τ-flow vs q-flow) is correctly separated

**Result**: The `dS/dτ > 0` τ-ramp (E7, geometric modulus) and the `dE_ZP/dq > 0` q-equilibrium theorem (S62 #19, conserved vacuum charge `q = N_pair`) are two distinct theorems on two distinct axes, never conflated. Classification: **GEOMETRIC** (both are statements about the spectrum of `D_K`).

This separation (§1.3a callout box) is the one I most wanted to see preserved, because conflating them is the failure mode that would make the CC story circular. The τ-ramp says the universe *transits* (no interior saddle in `τ`; `Z` boundary-dominated). The q-equilibrium says the vacuum energy is *zero at equilibrium and a relaxation residual away from it*. The CC story rests on the q-flow theorem; the transit-vs-slow-roll story rests on the τ-flow theorem. The document cites `tau-flow-vs-q-flow-note.md` and keeps the axes orthogonal. This is correct and load-bearing.

I additionally certify the **superfluid reading of the volume-preservation → `G_N` τ-flatness chain** (§2.1) as physically sound: `1/G` is the vacuum gradient stiffness, set by the vacuum *compressibility*; a volume-preserving (TT, `tr h_J = 0`) deformation is a pure *shear* of the order-parameter texture, and a shear leaves the compressibility — hence `G` — invariant. The `τ`-flatness of `G` is therefore the metric-determinant face of a microscopic compressibility statement, not an accident of `det g_τ = 3⁸`. This is the correct condensed-matter translation and it strengthens the claim.

### II.4 The Ordered Veil (GGE relic): the survival argument is now correctly grounded on diabaticity, not integrability

**Result**: The post-fold relic is a pure-product analytic Generalized Gibbs Ensemble (`S_ent = 0`), frozen by diabatic transit (`R_therm = t_therm/t_transit = 5251.82 ≫ 1`, S95 W5), with three Lagrange multipliers conjugate to conserved charges of the post-fold integrable Hamiltonian — *not* to energy, hence no temperature. Classification: **PHONONIC** (the relic is the substrate's quasiparticle content; `N_pair`, `P_exc`, `S_inst`).

This is the section where the framework's history matters most, and the capstone handles it with the discipline I would demand. The surviving claim is **diabatic transit-freeze, not integrability permanence** — the S39 retraction (13% non-separable density–density channel, Brody β = 0.633) is acknowledged, and the relic is shown to be dynamically frozen on the transit timescale (`t_scr/t_transit = 814`), not integrability-protected. The substrate-intrinsic denominator `t_transit` (never `t_Hubble`) is used correctly — a ratio against the FRW Hubble time would be undefined until the `t(τ)` map closes (§6.3). The information-theoretic reading (the Ordered Veil resolves what would otherwise be an analog information paradox: the Bogoliubov transformation is unitary, the pure GGE retains the squeeze phase in the conserved charges, no Page curve because nothing thermalizes) is physically correct and is the right superfluid-vacuum framing.

The capstone also adds a **third, geometric leg** I want to flag as both correct and scope-limited: `τ_fold = 0.190` is a double-root extremal Killing horizon (`V = V′ = 0 ⟹ κ = 0, T_H = 0`), so zero Hawking temperature is the causal-side corroboration of "never thermalizes," requiring no integrability argument. I verified the gate: `S85-W6-4-EXTREMAL-HORIZON-FORMAL` is PASS with `kappa=0.00e+00`. **Scope flag (not an overclaim, but cite carefully):** the gate's convention is `2D_modulus_metric` (scheme `Jensen_V_tree`) — `κ = 0` is established on the 2D modulus-space metric, not the full 12D lift. This is consistent with the W4-5 12D-censorship PASS being a *separate* gate, but the document should not let a reader infer that `T_H = 0` has been shown on the 12D metric. It hasn't yet; that is a clean harvest (§V.6).

The `N_pair` two-reading discipline (the 59.8 figure is a projected charge `⟨Q⟩_GGE`, inheriting a ~60% PBCS gap overestimate (S46) and a ~225× Richardson–Gaudin condensation-energy overestimate (S63) — NOT a literal pair count; the regime-robust claim is `P_exc = 1`, with `N_Fock = 1` exact reduction, S74) is correctly carried. This is exactly the kind of honest-denominator discipline the superfluid program requires.

### II.5 The acoustic white-hole causal structure is asymmetric and sector-dependent — correct, and well over-determined

**Result**: The fold is an *asymmetric* acoustic white hole — one post-genesis sonic entry surface (scalar entry horizon, `v = c_BLV` at `τ₀ ≈ 0.1125`, `κ_entry > 0`, white-hole outflow) bounding an unbounded supersonic expulsion region, with no future-trapped exit horizon and no bounce, over-determined at six independent walls (S95 W-1). Classification: **PHONONIC** (the acoustic metric `g_acoustic ∝ √(ρ_s/c_s)` is a collective mode; the white hole is a flow structure).

This is the substrate's resolution of the horizon problem recast as causal-disconnection (supersonic acoustic flow), not inflationary stretching of a pre-existing box. Two points I certify from the superfluid side:

1. **Two null cones.** The white hole is a *scalar-sector* structure (T3 Scalar-Tensor Kasparov Decoupling, `β_T = 0` exactly at linear order); the **tensor sector crosses the fold freely** on the `a₂`-emergent metric `g_M`. This is the geometric root of why tensor observables (`r`, `n_T`) behave differently from scalar ones — they propagate on different cones. This is exactly Volovik's "two metrics" situation in `³He-A` (the orbital/spin sectors see different effective metrics), and the capstone's two-cone structure is the parent→child inheritance of that. Correct.

2. **The analog-temperature ledger is KIND-tagged correctly.** The three corpus analog temperatures (`a₂`/72.8, `a₄`/7.578, S63-BLV/0.112 M_KK) are surface-gravity temperatures of surfaces of *different KINDS* — only the S63-BLV row is a genuine SONIC surface; the `a₂` and `a₄` rows are THERMODYNAMIC (gradients of two *channels*, scalar vs condensate), NOT two sonic horizons of one flow. The KIND tagging is what prevents the thermodynamic-vs-sonic conflation, and the document's insistence that "these are *analog/surface-gravity* temperatures, not thermodynamic temperatures of an equilibrium state — the GGE never thermalizes, so there is no Gibbs temperature anywhere" is the correct superfluid-vacuum statement. The `7.578 M_KK` is explicitly NOT a reheating temperature. Good.

**One observational-visibility caveat the document flags and I endorse:** the `a₂`-surface kinematic-carrier temperature has its observable visibility HELD pending falsifier F1 (a scan for a scalar-channel squeeze branch near `72.8 M_KK`). The COMPOSITE two-stage emission form is asserted; the categorical "a₂ carries no observed quantum" is NOT yet asserted. This is the honest scoping.

---

## III. Gate Verdicts

These are gates the source cites that bear directly on my axis. I cross-checked each against the knowledge MCP and do NOT re-adjudicate them — I report status only.

| Gate | Verdict | Decisive Number |
|:-----|:--------|:----------------|
| DILUTION-CC-66 (CC closure via tracking vacuum) | PASS | `ρ_vac/ρ_obs = 1.032`; `CC_OOM = 115.5` |
| EQUILIBRIUM-CC-WARRANT (S95 W5-3, Gibbs–Duhem `ρ_Λ=0`) | PASS | Sage-rational `0` (exact) |
| Q-THEORY-BCS-45 (CC zero-crossing) | PASS | `τ* = 0.209` (first CC PASS) |
| S62 #19 (CC monotonicity, no interior q-equilibrium) | PROVEN | `dE_ZP/dq > 0` ∀q |
| S85-W6-4-EXTREMAL-HORIZON-FORMAL (`T_H=0` extremal) | PASS | `κ = 0.00e+00` (scheme `Jensen_V_tree`, **2D modulus metric**) |
| S95 W5 (Ordered Veil diabatic freeze) | PASS | `R_therm = 5251.82`; `S_ent = 0` |
| S95 W2-3 (no-interior-saddle one-loop-robust) | PASS | `dΓ/dτ` fixed sign, 0 interior crossings, 200-pt grid |
| LEGGETT-GRAV-DECAY-67 (DM-sector survival) | PASS (but **CRITICAL** conditional — see §IV) | `Γ_grav < H_0`; gate weight 1/5 |
| C10 (Volovik tracking scaling `ρ_vac ~ M_Pl²H²`) | ASSUMED-PARTIALLY-PROVEN | scaling-form ansatz, not derived from `D_K` |
| C11 (LEGGETT-MOMENT-70 DM mass anchor) | CONDITIONAL | `Mass_LeggettDM/Δ_BCS = 11.97` |

---

## IV. Structural Implications

**The CC result's location is permanent; its magnitude is one entangled conditional.** The document's §9 frontier #6 reformulation — "the CC *ratio* is closed by tracking (truncation-robust) and the CC *absolute* magnitude is held pending SDW convergence; one entangled conditional, not two unrelated bullets" — is the correct constraint-map statement. From the superfluid side this is the right shape: the equilibrium identity (Clause A) is regulator-free and topology-independent (Gibbs–Duhem), so it survives the continuum-dissolution of the geometric triple; the magnitude (Clause B) lives on the dissolving (geometric-magnitude) side and is conditional. The "organizing spine — geometry vs topology" box (§9) places this correctly: trust the topological outputs (they survive continuum dissolution `ε_c ∼ N^{−0.457}`); hold the geometric magnitudes pending convergence.

**One tension I am obligated to flag (not silently resolve).** The `LEGGETT-GRAV-DECAY-67` gate is registered PASS in `baseline-findings-s66.md` (`Γ_grav < H_0`), yet the document (§7.1 open gaps) treats `Ω_DM h² = 0.120` as **CONDITIONAL on LEGGETT-GRAV-DECAY-67 (CRITICAL)** — "if the Leggett-mode gravitational decay rate exceeds `H_0`, the DM sector collapses and the `0.120` is meaningless." The `constraint-mega-matrix.md` entry tags it **CRITICAL** with the collapse consequence. These are not contradictory but they are in tension in *framing*: a gate can be PASS at the currently-pinned `Γ_grav` while remaining the single most fragile load-bearing conditional under the DM sector (a small upward revision of `Γ_grav` flips it). The capstone resolves this correctly by calling it CONDITIONAL-with-PASS-status rather than asserting the DM sector closed — but a reader who only sees the §7.1 table row (`Ω_DM h² = 0.120, PASS, 0.7σ`) without the open-gaps box would over-read it. The open-gaps box is doing essential work; it should not be demoted in any future summarization. This is a framing-robustness flag, not a physics error.

**The `wₐ = 0` four-fold lock is the most exposed prediction on the dark-energy axis, and it is on my watch.** The document is right to call DESI DR3 (2026) the cliff-edge and not LISA: `wₐ = 0` (structural) vs `−0.72 ± 0.21` (Popovic/DES joint) is a 3.43σ marginal tension, *advancing*. The binding test is the 2D `(w₀, wₐ)` posterior (the `R_842` rectangle, Falsifier #1), with `ρ(w₀,wₐ) ≈ −0.85` — NOT two independent 1D distances. The document states this correctly. From the superfluid side, `wₐ = 0` is the structural prediction of a tracking vacuum that has *already* relaxed onto its `M_Pl²H²` attractor (no further roll), so a confirmed `wₐ ≠ 0` would be a direct falsifier of the tracking-vacuum reading — this is the right pre-registration and I endorse the "live wager" framing.

**`w₀` branch status — my memory vs the document (FLAG, do not silently resolve).** My agent memory records branch-(iv) `w₀ = −0.842454` as RETRACTED at S85 pending higher-`L_max` re-audit (the `R_JE` regulator-dressing ratio drifts 0.454 → 4.98 over `L=5→8`, SV2 FAIL). The capstone presents `w₀` as a live branch-PAIR (`−0.918` canonical Volovik-partition / `−0.842454` branch-iv), DESI-DR3-decidable, written as a `(value, branch)` pair. **These are reconcilable but I will not silently merge them:** the canonical pin `w0_FW = −0.918` now carries a PROVENANCE entry (S58 four-fold-lock; verified in MCP — the document's verification-ledger claim that constants-hygiene is CLOSED at S95 W6-4 is corroborated), so the canonical branch is solid. The branch-iv member is the one my memory flags as truncation-fragile. The document's branch-pair framing is the safe move (it does not assert branch-iv as a winner), but anyone citing branch-iv `−0.842454` downstream MUST disclose the `L_max` re-audit status — citing it as a settled second prediction would be an overclaim. I have flagged this in §V.5 as a concrete re-audit gate rather than leaving it as prose.

**What I authored survived correctly.** My §0 draft's three honest-scope flags (C10-conditionality, `w₀` branch-pair with the branch-iv-retraction PRELIMINARY caveat, BDI/`N₃=0`-inheritance-not-analogy) all made it into the capstone. The capstone is faithful to the framing draft and tightened it (the q-flow vs τ-flow callout, the extremal-horizon third leg, the KIND-tagged temperature ledger are all sharper than my draft). No framing inversion crept in.

---

## V. Carry-Forward Computations

**The open-question harvest from the superfluid-vacuum / emergent-spacetime axis.** Each entry is a runnable computation with all four fields. These convert the document's stated open frontiers (and two I add from corpus cross-check) into compute specs.

```
V.1. Derive the de Sitter identity T_dS = H/2π from D_K (the missing link under the tracking law)
   - What: Show that the substrate's own inverse-Euclidean-period temperature at the emergent
     de Sitter horizon equals H/2π, starting from the spectral-action partition function
     Z = Σ e^{−S[D_K(τ)]} (§1.3a) evaluated on the late-time (τ_now, frozen-plateau) configuration.
     The tracking law ρ_vac ~ M_Pl²H² IS "the substrate tracking its own emergent de Sitter horizon
     energy density (H/2π = T_dS)" (§7.1, §0.4) — but T_dS = H/2π is currently INPUT (Gibbons–Hawking,
     Paper 07), not derived from D_K. Compute the Euclidean period β_E of the dominant a₂-emergent
     metric configuration at τ_now and test β_E^{−1} =? H/2π. This is the de-Sitter-thermodynamics
     program Volovik develops in his 2025 papers (corpus index lists "de Sitter thermodynamics"
     across sessions 80/83-89).
   - Inputs: a_2_FW_zeta=2776.165389, a_0_FW_zeta=6440, M_KK=7.4287e16 GeV, f₂≈92 (§8.3 dictionary),
     the Connes-distance scale-factor a(τ) (SCALE-FACTOR-54, q-band −0.97→+0.81) and the conformal
     embedding Ω(τ) (S95 W4-4); Volovik Paper 07 (Gibbons–Hawking dS entropy), Paper 13/25 (q-theory).
   - Gate: NEW gate DS-IDENTITY-FROM-DK. PASS: β_E^{−1}(τ_now)/(H/2π) = 1.00 ± 0.05 from the
     spectral-action saddle. INFO: identity holds only up to the undelivered M_KK⁻¹→seconds
     normalization (the same open piece as C2). FAIL: β_E^{−1} ≠ H/2π by >2× (would mean the
     dS-thermodynamic reading of the tracking law is not substrate-derived).
   - Effort: 1 agent session (3-4 hours); the conformal embedding Ω(τ) and a(τ) already exist.

V.2. Derive the C10 tracking scaling ρ_vac ~ M_Pl²H² from the q-theory variable on the substrate
   - What: Promote C10 from ASSUMED-PARTIALLY-PROVEN to derived (or bound the deviation). The q-theory
     vacuum energy is ρ_vac(q) = ε(q) − q·dε/dq (Volovik Paper 13 Eq.4 / Paper 25 Eq.2.11). With
     q = N_pair the substrate's conserved vacuum charge, compute ε(q) from the D_K spectrum directly
     (the zero-point sum E_ZP(q) whose monotonicity dE_ZP/dq>0 is already S62 #19), then evaluate
     ρ_vac(q) along the off-equilibrium trajectory N_pair=1 and test whether it scales as M_Pl²H²
     (H² ∝ a₂-channel readout) rather than H⁰. This is the single load-bearing conditional under the
     entire CC closure (Clause B, §7.1; frontier #5).
   - Inputs: D_K spectrum cache (155,984 eigenvalues, L_max=10; s84_spectrum_cache_L12_tau019.npz);
     E_ZP(q) machinery from S62 #19; N_pair=1 (canonical, Mott); the a₂(τ)→H² readout from
     SCALE-FACTOR-54 / Ω(τ). q-theory ε(q) form from Volovik Paper 13/25.
   - Gate: NEW gate C10-SCALING-DERIVE. PASS: ρ_vac(q)/M_Pl²H² = const (H-independent) to within 10%
     across the late-time τ range ⇒ the H² power is derived. INFO: a different power (H^n, n≠2)
     emerges ⇒ re-pin the tracking law to the derived exponent. FAIL: no power-law scaling ⇒ C10
     stays ASSUMED and the CC magnitude (Clause B) remains conditional.
   - Effort: 2 agent sessions (the ε(q) evaluation off-equilibrium is the new piece; ~6 hours).

V.3. The N₃=0 → q-theory-required chain as a closed lab falsifier (³He-B vs ³He-A discriminator)
   - What: Sharpen the structural claim "because N₃=0, the CC needs q-theory not topological
     protection" into a quantitative discriminator. In ³He-A (N₃=2, Volovik Paper 03 Thm 1) the
     equilibrium vacuum energy is topologically pinned to zero; in ³He-B (N_K=2, BDI, N₃=0) it is
     pinned only thermodynamically (Gibbs–Duhem, recoverable). Compute the substrate's momentum-space
     topological invariant N₃ directly from the D_K BdG sector (the hedgehog/Chern number, Paper 05
     Eq.15) and confirm N₃=0 exactly, then derive the observable consequence: the off-equilibrium
     P_vac = −0.688 at N_pair=1 (already in §7.1) is NONZERO precisely because N₃=0 — quantify the
     ³He-A counterfactual P_vac that topological protection would force (expected: exactly 0).
   - Inputs: D_K BdG sector / Bogoliubov amplitudes; AZ-class BDI confirmation (S44); N₃ computation
     machinery; P_vac(N_pair=1)=−0.688; Volovik Papers 03/05.
   - Gate: NEW gate N3-CC-DISCRIMINATOR. PASS: N₃=0 exact (machine-ε) AND P_vac(N_pair=1)≠0 with the
     ³He-A counterfactual P_vac=0 shown ⇒ the topology→CC-mechanism link is quantitative, not just
     structural. FAIL: N₃≠0 (would overturn the BDI class assignment — re-adjudicate, do not assume).
   - Effort: 1 agent session (4 hours); N₃ is a winding-number computation on the existing BdG sector.

V.4. Close the LEGGETT-GRAV-DECAY CRITICAL conditional: compute Γ_grav/H_0 with error bars
   - What: The whole DM sector (Ω_DM h²=0.120, σ/m=0) hangs on Γ_grav < H_0 (LEGGETT-GRAV-DECAY-67,
     gate weight 1/5, CRITICAL). The gate is PASS at the currently-pinned Γ_grav but is the most
     fragile load-bearing conditional in the document (§IV tension flag). Recompute the gravitational
     decay vertex ⟨g,g|H_grav|L⟩ of the Leggett channel into two gravitons, evaluate Γ_grav in SI,
     and report the MARGIN Γ_grav/H_0 with its uncertainty band — not just the PASS/FAIL bit. A margin
     of 10× is robust; a margin of 1.5× means a modest revision flips the DM sector.
   - Inputs: Leggett-mode gap (Ω_DM h²=0.120 machinery, E47; josephson-leggett-mix S78 W3-D, 8% mass
     softening); Mass_LeggettDM/Δ_BCS=11.97 (C11/LEGGETT-MOMENT-70); H_0; the gravitational vertex
     from H_grav coupling g_M to the Leggett excitation.
   - Gate: feeds LEGGETT-GRAV-DECAY-67. INFO if margin computed: report log10(H_0/Γ_grav). PASS-robust
     if >1 dex; PASS-marginal if 0–1 dex (flag DM sector as fragile); FAIL if Γ_grav>H_0.
   - Effort: 1 agent session (4-5 hours); the vertex is a spectral matrix element on the existing
     Leggett channel.

V.5. w₀ branch-iv re-audit at higher L_max (resolve the memory-vs-document branch status)
   - What: My agent memory records branch-iv w₀=−0.842454 as RETRACTED at S85 (R_JE drifts
     0.454→4.98 over L=5→8, SV2 FAIL); the capstone presents it as a live branch-pair member. Re-run
     the two-component Zubarev-dressed closed form for w₀ at L_max ∈ {8,10,12,15} and test whether
     R_JE = ξ_J/ξ_E_GGE converges in the Mellin-cone at s=3, OR confirm the L=5→8 drift continues
     (truncation artifact). This decides whether branch-iv is a genuine second prediction or a
     truncation ghost — load-bearing before DESI DR3 binds Falsifier #1.
   - Inputs: ξ_J=0.008911, ξ_E_GGE=0.019646, R_JE=0.4536 (at L=5); w0_FW=−0.918 (canonical anchor,
     S58); the two-component Zubarev closed form (S84 W1-3.SV1/SV2); D_K spectrum cache to L_max=15
     (use Friedrich-Bär saturation per math-scripts.md to avoid the irrep-construction timeout at
     p+q≥13).
   - Gate: feeds Falsifier #1 (w₀ / R_842 rectangle). PASS: R_JE converges to a stable value by
     L_max=12 ⇒ branch-iv is a genuine prediction, pin it. FAIL: R_JE keeps drifting ⇒ branch-iv
     RETRACTED confirmed, w₀ canonical = −0.918 single-branch (my-memory status canonical).
   - Effort: 1 agent session (4 hours); reuses the S84 closed form; the only new work is the L-scan.

V.6. Lift the extremal-horizon T_H=0 result from the 2D modulus metric to the full 12D metric
   - What: S85-W6-4-EXTREMAL-HORIZON-FORMAL established κ=0 (T_H=0) on the 2D_modulus_metric
     (scheme Jensen_V_tree) — the third, geometric leg of the Ordered Veil "never thermalizes"
     argument (§5.3). But the W4-5 12D-censorship PASS is a separate gate. Recompute the surface
     gravity κ at τ_fold on the FULL 12D Jensen metric g_τ = 3·diag(e^{2τ}, e^{−2τ}×3, e^{τ}×4)
     and test whether the double-root V=V'=0 structure (hence κ=0, T_H=0) survives the lift, OR
     whether the anisotropic block structure (timelike SU(2) / spacelike ℂ²,U(1)) splits it into
     direction-dependent surface gravities.
   - Inputs: g_τ (E1, 12D metric); V(τ) tree potential (Jensen_V_tree scheme); κ machinery from
     S85 W6-4; the 12D Kretschmann/Kasner structure from S95 W4-5 (anisotropic τ→∞ singularity).
   - Gate: NEW gate EXTREMAL-HORIZON-12D. PASS: κ=0 on all 12 directions at τ_fold ⇒ the T_H=0 leg
     holds on the full metric, the Ordered Veil third leg is 12D-robust. INFO: κ=0 on the scalar
     block but direction-dependent on the anisotropic blocks ⇒ scope the T_H=0 claim to the scalar
     sector (consistent with the two-null-cone structure, §6.2). FAIL: κ≠0 on the 12D lift ⇒ the
     geometric leg is a 2D-projection artifact (the diabaticity leg R_therm=5252 still stands alone).
   - Effort: 1 agent session (3-4 hours); the 12D metric and κ machinery both exist.

V.7. The §6.3 back-reaction closure via Volovik two-fluid superfluid hydrodynamics
   - What: The load-bearing gap (§6.3, frontier #1) is the absence of a derived H² = f(ρ_relic, S_SA)
     closing the relic energy density into the global expansion rate (T6 FAIL: 155,984-mode spectral
     action overwhelms 8-mode BCS by 133,200×). The framework has not exploited the most direct
     template: Volovik's TWO-FLUID hydrodynamics (normal + superfluid components, Paper 06; the
     two-fluid w=−1 (effacement/vacuum) + w=0 (GGE/matter) split is already in the project mapping).
     Set up the two-fluid continuity + Euler equations for the post-fold substrate (superfluid =
     the unbroken condensate fraction, normal = the GGE quasiparticle gas, N_pair) and derive the
     effective H² as the back-reaction of the normal-component energy density on the emergent g_M.
     This is the transit-side "back-reaction closure," not a Friedmann equation (§6.3 framing).
   - Inputs: GGE relic content (N_pair, P_exc=1, E_exc/|E_cond|=443); effacement Γ_eff=0.99970
     (w=−1 vacuum component); the a₂→g_M dictionary (f₂≈92); two-fluid mapping (Paper 37, S43:
     w=−1 effacement + w=0 GGE); T6 FAIL machinery (the 133,200× overwhelm to be re-examined under
     the two-fluid split rather than single-fluid BCS).
   - Gate: NEW gate TWO-FLUID-BACKREACTION. PASS: a closed H²(ρ_relic, S_SA) emerges with the normal
     component sourcing q_Ω in the SCALE-FACTOR-54 band (−0.97→+0.81) ⇒ first substrate-derived
     effective Friedmann map, closes frontiers #1 AND #8 jointly. INFO: closure requires the
     M_KK⁻¹→seconds normalization (C2) as external input ⇒ reduces the gap to the normalization
     alone. FAIL: the two-fluid split does not resolve the 133,200× mode-count overwhelm ⇒ the gap
     is deeper than a two-fluid decomposition can close.
   - Effort: 2-3 agent sessions (this is the load-bearing frontier; the two-fluid setup is new work
     but every input exists).

V.8. CF-35 ³He-B cocycle-ratio lab falsifier — substrate prediction sharpening before Aalto/Lancaster
   - What: The Pillar-V lab falsifier (CF-35, ratio 7.324992 ± 1%, Aalto/Lancaster 2028-9) is the
     framework's only laboratory-grounded falsifier structurally orthogonal to CMB. Per the
     inheritance-falsifier protocol (rank(ker ι_*)=2 ⇒ Class-A kernel-signature + Class-B
     cohomology-asymmetry tests both required), pre-register the full 4-gate structure: Gate-1 NULL
     on the decisive F-rows (F1+F2+F5), Gate-2 the 7.324992±1% cocycle ratio on any non-NULL
     detection, Gate-3 NULL on supporting F-rows (F3+F4), Gate-4 the F4 multi-pressure slope
     (0-34 bar). Compute the predicted lab S/N margin per row (Caroli-Matricon ladder asymmetry,
     vortex-core spectroscopy) in SI units via the M_KK→SI translation (S86 W11-1) so the experiment
     has a quantitative target.
   - Inputs: ‖φ_67‖/‖φ_88‖=7.324992 (Sage-exact, S86 W-5 DONE-5); F1=0.573193 M_KK²; the (Δ_B/Δ_A)^p
     cancellation theorem; M_KK→SI table (s86_w11_lab_si_translation.csv: 3He-A ν_Δ=34.146 MHz);
     lab platforms Lancaster MCT-3 / Helsinki ROTA.
   - Gate: feeds CF-35 (falsifier #5). INFO/PRE-REG: emit the 4-gate falsifier table with per-row
     SI S/N margins. (Not a PASS/FAIL compute — it is the pre-registration that makes the 2028-9
     experiment decisive.)
   - Effort: 1 agent session (3 hours); the cocycle ratio and SI translation both exist, this is
     assembly + SI-margin computation.

V.9. S43 first-sound BAO ring amplitude forecast against a named experiment (the live BAO channel)
   - What: §6.2 BAO effacement closes with INFO-by-unavailability (S95 W6-2): the live BAO channel
     is the S43 first-sound ring (A_FS/A_BAO=0.204=c_2²/c_1², r_1≈325 Mpc, k_1≈0.0193 Mpc⁻¹), a
     zero-parameter prediction with no ΛCDM counterpart, but the amplitude-detection forecast against
     a named experiment's sensitivity awaited the fetched comparison value. Fetch the
     DESI-DR2/Euclid/SKA BAO power-spectrum sensitivity at k_1≈0.0193 Mpc⁻¹ and compute the
     detection significance of the first-sound ring sub-feature. This is a distinctive substrate
     prediction (two-fluid first vs second sound, c_2²/c_1²) directly from the superfluid two-fluid
     structure.
   - Inputs: A_FS/A_BAO=0.204, r_1≈325 Mpc, k_1≈0.0193 Mpc⁻¹ (S43); per-branch BAO suppression
     δP/P≈1.4e-3 (effacement-transported); DESI DR2 / Euclid BAO sensitivity curve (to be fetched
     via astro/madrigal MCP or paper-search).
   - Gate: NEW gate FIRST-SOUND-RING-FORECAST. PASS: predicted A_FS detectable at >2σ by a named
     2026-2030 experiment ⇒ a near-term distinctive falsifier beyond w₀. INFO: below detection
     threshold for all named experiments ⇒ document as a future-experiment target. (The substrate
     forecast is in hand; only the comparison value was missing.)
   - Effort: 1 agent session (3-4 hours); the substrate amplitude exists, the new work is the
     experiment-sensitivity fetch + significance computation.
```

---

## VI. Summary Table

| # | Result | Classification | Status | Implication |
|:--|:-------|:---------------|:-------|:------------|
| 1 | Emergent-spacetime lineage (3 pillars) correctly transcribed; gap-node `g^{ik}` = `g_M` from `a₂` (same object); `³He-B` inheritance not analogy | GEOMETRIC (PHONONIC consequences) | SOLID | The IS-not-IN reading is the reading under which framework and helium-droplet program are the same physics |
| 2 | CC layer: equilibrium `ρ_Λ=0` exact (Gibbs–Duhem, Clause A); observed magnitude conditional on C10 (Clause B) | PHONONIC (`a₀` channel) | SOLID (location); CONDITIONAL (magnitude) | Framework *located* the CC term, did not *solve* the CC problem — stated honestly |
| 3 | τ-flow (E7) vs q-flow (S62 #19) monotonicity correctly separated | GEOMETRIC | SOLID | Prevents the circular CC story; CC rests on q-flow, transit on τ-flow |
| 4 | Ordered Veil grounded on diabatic freeze (`R_therm=5252`, `S_ent=0`), NOT integrability permanence; S39 retraction acknowledged | PHONONIC | SOLID | Survival certified by compute, not by the retracted Richardson–Gaudin integrability |
| 5 | Extremal-horizon `T_H=0` third leg (κ=0) | GEOMETRIC | SOLID on 2D modulus metric; **PRELIMINARY on 12D** | Scope to 2D until V.6 lifts it; diabaticity leg stands alone regardless |
| 6 | Asymmetric acoustic white hole, two null cones, KIND-tagged temperature ledger | PHONONIC | SOLID | Horizon problem = causal disconnection; tensor sector crosses freely (different cone) |
| 7 | de Sitter `T_dS=H/2π` used as Gibbons–Hawking input under tracking law | PHONONIC | PRELIMINARY (not derived from `D_K`) | Harvest V.1 — the missing link under `ρ_vac ~ M_Pl²H²` |
| 8 | C10 tracking scaling `ρ_vac ~ M_Pl²H²` | PHONONIC | ASSUMED-PARTIALLY-PROVEN | Harvest V.2 — load-bearing conditional under entire CC closure |
| 9 | LEGGETT-GRAV-DECAY-67 PASS but CRITICAL — DM sector hangs on `Γ_grav<H_0` | PHONONIC | CONDITIONAL-with-PASS (framing tension flagged) | Harvest V.4 — compute the margin, not just the bit |
| 10 | `w₀` branch-iv `−0.842454`: memory says RETRACTED, document says live branch-pair | GEOMETRIC | FLAGGED (not silently merged) | Harvest V.5 — re-audit at higher `L_max` before DESI DR3 binds |
| 11 | §6.3 back-reaction closure `H²=f(ρ_relic, S_SA)` absent (T6 FAIL) | PHONONIC | OPEN (load-bearing frontier #1) | Harvest V.7 — Volovik two-fluid hydrodynamics is the unexploited template |
| 12 | `wₐ=0` four-fold lock vs DES joint `−0.72±0.21` (3.43σ, advancing) | PHONONIC (`a₀` channel) | LIVE WAGER | DESI DR3 2026 cliff-edge; confirmed `wₐ≠0` falsifies tracking-vacuum reading |

---

*Reviewer's closing note (superfluid-vacuum axis).* The capstone is the strongest single statement of the framework's emergent-spacetime thesis I have read in the corpus, and its honesty about the CC magnitude and the `a(t)` gap is exactly right — it does not let the equilibrium-`ρ_Λ=0` identity (which the framework genuinely earns, by Gibbs–Duhem) bleed into a claim about the observed magnitude (which it does not yet earn, pending C10 and the borrowed `H`). The richest unexploited harvest on my axis is the de-Sitter-thermodynamics derivation (V.1) feeding the C10 scaling (V.2) feeding the two-fluid back-reaction closure (V.7): close those three and the framework's tracking-vacuum reading goes from ASSUMED-PARTIALLY-PROVEN to derived-from-`D_K`, which is the difference between locating the cosmological-constant term and solving the cosmological-constant problem. None requires new physics — every input exists in the cache or the corpus. The greedy hands have a target.
