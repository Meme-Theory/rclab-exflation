# Capstone Equation Review — mack

> **Reviewer**: mack-cosmic-bridge (Katie Mack — Cosmic Bridge). Independent solo synthesis; no coordination with the other ~30 reviewers.
> **Source under review**: `sessions/framework/phonic-exflation-equation.md` ("The Phonon-Exflation Equation"), S95-era capstone.
> **Vantage**: the particle-physics/astrophysics interface — where one Dirac operator's spectral moments meet observational cosmology (dark energy `w(z)`, the cosmological-constant magnitude, the CMB tilt sector, dark-matter relic abundance, σ₈/S₈, the GGE-relic CGWB). I own the falsifier inventory (`falsifier-master-inventory.md`) and the observational-anchor constraints (`mack-observational-constraints.md`), so I review §6.3 (the `a(t)` gap) and §7 (the endpoint) hardest, and I cross-check every comparison anchor against canonical provenance rather than against the capstone's own prose.
> **Discipline**: substrate-first / IS-not-IN throughout (`phononic-framing.md`). Gate verdicts and PROVEN results are authoritative and not re-adjudicated; numerical anchors ARE cross-checked against `canonical_constants.py` + knowledge MCP. Conflicts are flagged, never silently resolved.

---

## §I — Overview / Orientation

The capstone does the thing it sets out to do: it exhibits the framework as the spectral action of **one** self-adjoint operator `D_K(τ)` on Jensen-deformed `SU(3)`, and it derives the cosmological stage — metric, Newton coupling, vacuum term — rather than presupposing it. Read from my vantage, the single most important sentence in the document is **not** in §7 (the data section); it is the §0 statement that "switch off `D_K` and there is no `a₂`, hence no metric, hence no space." Everything I care about — whether `w₀` is a real prediction or a borrowed fit, whether the CC closure is a solution or a relabeling, whether σ₈ is evidence or coincidence — turns on whether that arrow (`D_K → a₂ → g_M → measurement`) actually closes, or whether the framework quietly re-imports the FRW container at the late-time end.

The document is unusually honest about exactly where the arrow does NOT close: **§6.3 is the most valuable section in the capstone for an observational cosmologist**, because it states without softening that there is *no substrate-derived FRW scale factor `a(t)`* and that every late-time observable I tabulate in §7 is evaluated using "the container-observer's FRW `H(t)` as external input (caveat C10)." That admission is what keeps §7 from being an over-claim. It is also, structurally, the single largest open harvest in the document, and I treat it as such in §V.

Where the capstone is strongest, from my desk:
- The **layer separation is genuine, not cosmetic** — the Spectral-Moment Decoupling Theorem (S75 W2-E, CERTIFIED, Wronskian `W ∝ R_K′(τ)³`) is what licenses multiplying `a₀ × a₂ × a₄` improbabilities as independent in the joint-evidence statement (§7.3). Without that theorem, "relic abundance AND CC scale AND Higgs mass" would be one knob dressed three ways, and the joint Bayesian-evidence claim would collapse. The capstone correctly *uses* the theorem to gate the multiplication, and correctly *forbids* multiplying within a layer (Ω_DM and σ₈ are both `a₂`).
- The **CC story is correctly factored into two clauses** (§7.1 CC caveat box): non-inheritance of the 114-OOM term is an exact thermodynamic identity (Gibbs-Duhem, `ρ_Λ=0` at equilibrium, S95 W5-3, verified Sage-rational `0`); the *observed magnitude* is a non-equilibrium tracking residual conditional on C10. This is the correct epistemic shape. The framework has **located** the CC term (the `a₀` moment, geometrically natural), not **solved** the CC problem. The document says exactly this in frontier #6 and I endorse it.

Where I push back lives in §III and §IV. In brief: the document occasionally lets a comparison anchor drift from canonical provenance (σ₈), it compresses the DESI-DR3 *window-open* date and the DESI-DR3 *data-release* horizon into a single "(2026)" tag in a way that over-sharpens the near-term cliff-edge, and the joint-evidence argument in §7.3 — while structurally correct — carries an unstated prior-volume assumption that a careful referee will press.

---

## §II — What is Solid

These are claims I cross-checked against canonical provenance and found to hold, OR structural results I am not entitled to re-adjudicate and which are internally coherent with my domain.

### II.1 The DE-anchor provenance is correct and current
The `(w₀, wₐ)` joint posterior is cited as **Popovic et al. (DES Collaboration), arXiv:2511.07517v3** — Flat `w₀wₐCDM` from DES-Dovekie SN + DESI DR2 BAO + Planck 2018 + ACT-DR6 + SPT-3G, with `ρ(w₀,wₐ) ≈ −0.85`, central `(w₀, wₐ) = (−0.803 ± 0.054, −0.72 ± 0.21)`. This matches my `mack-now-endpoint-observation.md` §7.1.1 draft exactly. The capstone correctly states that the 1-parameter marginals are NOT the binding test — the binding test is the 2D `(w₀, wₐ)` posterior against the `R_842` rectangle (Falsifier #1). **This is the right object.** A referee who quotes "2.13σ" or "3.43σ" as if they were independent is making the error the dagger note pre-empts.

### II.2 `w₀` dual canonical, sign-checked
`canonical_constants.py:w0_FW = −0.918` (S58 four-fold-lock, Volovik vacuum partition + effacement Γ=0.99970), confirmed via knowledge MCP `get_constant`. The branch-(iv) value `−0.842454` is a W0-workshop promotion *conditional on DR3 PASS*, correctly written as a `(value, branch)` pair throughout, never a bare point. Both values are red (`w₀ > −1`), consistent with the quintessence-like-but-not-quintessence reading (the clock constraint E27 closes rolling quintessence at 15,000× the bound). The capstone's branch discipline is exemplary and matches my memory.

### II.3 CC closure factoring
DILUTION-CC-66 (`ρ_vac/ρ_obs = 1.032`, CC_OOM = 115.5) is PROVEN per the knowledge MCP and is the sole-surviving CC-closure mechanism post-S66. The capstone correctly tags C10 (`ρ_vac ∼ M_Pl²H²`) as **ASSUMED-PARTIALLY-PROVEN** and makes the closure *doubly conditional* (on C10 AND on the external `H` C10 feeds). The §7.1 Clause-A / Clause-B split is the correct epistemic geometry: "exactly zero, not tuned" is true of the equilibrium reference (non-inheritance, exact Gibbs-Duhem identity); conditional of the observed magnitude. I verified the equilibrium identity is Sage-rational `0` (S95 W5-3 `EQUILIBRIUM-CC-WARRANT`). **Solid, and correctly hedged.**

### II.4 Dark matter — the structural zero is the strong claim
Ω_DM h² = 0.120 (Leggett-only, 0.7σ from Planck `0.1186±0.0020`), with the full-Goldstone route excluded at 260σ. The capstone correctly emphasizes that the geometry does **not permit tuning** the abundance — only the Leggett-channel projection lands at 0.7σ. The companion result `σ/m = 0 exactly` (N_Fock=1; structural, not a tuned cross-section) against Bullet `< 1.25 cm²/g` is the genuinely strong statement, distinct in *kind* from any WIMP model: a collisionless-by-superselection relic with `T^{0i}=0` exact (born at rest). This is the cleanest example in the document of a result that survives the continuum-dissolution axis (§9 spine) because it is *topological/representation-theoretic*, not a geometric magnitude. **Endorsed as the strongest DM statement.**

### II.5 f_NL magnitude
The capstone's `f_NL = −1.505` matches `max_f_NL_FW = 1.505` (S95 F-NL-ROW) in magnitude; the negative sign is the Bogoliubov-Gaussian-by-Wick squeezed-vacuum result. Against Planck `−0.9 ± 5.1`, the 0.47σ consistency is correct. The capstone is right that this is *structural* (Wick's theorem on a Gaussian squeezed state forces small `|f_NL|`), not a tuned bispectrum amplitude. Note the corpus carries a *separate* set of pathway-projected `f_NL` channels (`f_NL_FW_S82_equilateral = 0.0547`, `f_NL_FW_S67_folded = 0.129`, `f_NL_total_SKA1 = 0.9522`) for the SKA/21-cm forecast — these are a different observable family (template-projected detectability forecasts) and the capstone correctly does not conflate the `−1.505` headline with them. **No conflation; solid.**

### II.6 The "transit not slow-roll" / `r ≠ 16ε` structural argument
This is correct and load-bearing for my falsifier inventory. The capstone's §5.1 statement — that `r = 16ε` is a *theorem of the single-clock adiabatic vacuum* whose three premises (single clock, `c_s = 1`, slowly-varying background) are *all* violated at the fold (diabatic sweep, BdG `c_s ≠ 1`, multi-mode squeezed GGE) — is the right reason the relation is inapplicable, not "we got a different number." This matters downstream: Falsifier #2 (`r`/`n_T`) uses the *CMB-transferred* tensor consistency `n_T = −r/8` (S66), explicitly NOT the slow-roll relation. The capstone keeps this distinction clean. My standing memory note (the `n_T` convention trap, S85 W1a-9: transit-scale `n_T = +0.4676` is a GEOMETRIC FLOOR at `f_transit = 8.55×10³⁷ Hz`, 54 decades from any CMB detector, NOT detector-comparable) is honored — §7.2 row #2 says exactly this. **Solid.**

### II.7 The acoustic-white-hole horizon-problem resolution is causally honest
§6.2's recasting of the horizon problem as a *causal-disconnection* statement (supersonic acoustic flow at the fold), NOT inflationary stretching of a pre-existing box, is the substrate-first reading and is PROVEN (S85). The two-null-cone structure (scalar sees the acoustic metric + white hole via [T3] Scalar-Tensor Kasparov Decoupling `β_T = 0`; tensor crosses freely on `g_M`) is the geometric root of why `r`/`n_T` behave differently from `n_s`/`A_s`. This is a genuine prediction-shaping structural fact, and it is the reason LISA (scalar-sourced acoustic CGWB) is the flagship while the tensor sector is a separate cone. **Endorsed.**

---

## §III — What is PRELIMINARY or Conditional

These are items the capstone itself tags conditional, plus items I judge PRELIMINARY from my vantage even where the document reads them as settled.

### III.1 Every `†`-tagged row in §7.1 is C10-conditional — this is correctly disclosed but bears repeating
`w₀`, `wₐ`, CC closure, and σ₈ are all evaluated borrowing the FRW `H(t)`. The capstone marks this with the `†` dagger and the C10 caveat. **I endorse the disclosure but emphasize its weight**: these are the rows a detector tests *now*, and they are precisely the rows that are NOT yet from-`D_K` derivations of cosmological-history quantities. The spectral *values* are from `D_K`; the *cosmological evaluation* is borrowed. A reader who skips the dagger will over-read §7 as a closed expansion history. The capstone's §7.3 final bullet ("Missing a(t) means the 'to-now' arc is grounded in observables at τ_now, not a derived expansion-history trajectory") is the correct framing and should be treated as the headline caveat of the entire data section, not a footnote.

### III.2 `n_s` is genuinely PRELIMINARY pending functional selection
The three-value spread `n_s ∈ {0.9561, 0.9590, 0.9595}` is confirmed against canonical constants (`ns_framework = 0.9595`; `n_s_FW_exact = Fraction(9561,10000) = 0.9561`). The capstone is honest that even the most-favorable scheme (`0.9595`, `√x`) is 1.29σ low and that the decisive gate E31 (EFOLD-MAPPING-52) is CONDITIONAL. The BMA band `n_s = 0.969 ± 0.022` (S67, marginalizing the nuisance functional `f`) is the correct UQ object and IS stronger than three rival points — I agree with this framing (it is the cosmological face of Bayesian model averaging over an unknown energy-density functional). **But the BMA band is itself PRELIMINARY**: it is a marginalization over a *three-member* functional family, and there is no proof that the physical functional lives in that family. If the true `f` is outside `{√x, zeta-a₄, anomaly-φ}`, the band moves. The capstone does not claim otherwise, but the band's width is a property of the chosen prior over `f`, and that prior is unjustified. Flagged PRELIMINARY on the *functional-prior* axis.

### III.3 `A_s` is band-cited, not point-cited — correctly
`A_s ∈ [3.11, 4.27]×10⁻⁹` pending `ε_pivot`, against Planck `(2.10±0.03)×10⁻⁹`. The capstone is honest that the exit greybody (S95 W4-3, `Γ(ω)∈[0,1]`) narrows the band but does not collapse it. **PRELIMINARY and correctly so.** I note the band's *lower* edge (3.11×10⁻⁹) is already ~33σ above Planck's central `A_s` on Planck's own error bar — so the band does not currently *contain* the observed value. This is not flagged as sharply as I would flag it. The capstone treats `A_s` as "pending `ε_pivot`," which is fair if the greybody filter is expected to pull the band down toward 2.1×10⁻⁹; but if the filter narrows *within* `[3.11, 4.27]` rather than below it, `A_s` becomes a tension, not a pending band. **This is a genuine open question I convert to a computation in §V (CF-MACK-3).**

### III.4 `m_H` route-dependence is PRELIMINARY on the central value
The KK-threshold band 127.5–131.8 GeV is the defensible headline; comparison at the ~2% theory budget (NOT PDG's 0.17 GeV bar) is the correct comparison. The capstone correctly excludes the zeta route (138.5 GeV) and flags the μ_BC fit (188 GeV) as an ACCOMMODATION. **Endorsed as PRELIMINARY** pending a single canonical Higgs-mass route. This is a particle-sector item more than a cosmology item, but it bears on the joint-evidence count in §7.3 (m_H is one of the multiplied improbabilities), so its route-dependence weakens the joint statement by exactly the width of the route ambiguity.

### III.5 Ω_DM h² = 0.120 is CONDITIONAL on LEGGETT-GRAV-DECAY-67 (CRITICAL)
The capstone flags this in §7.1 open gaps: the 0.120 PASS holds *given* `Γ_grav < H_0`; if the Leggett-mode gravitational decay rate exceeds `H_0`, the DM sector collapses and 0.120 is meaningless. **This is the correct conditional and it is CRITICAL** — it is the one place where my strongest "solid" claim (§II.4) is hostage to an uncomputed rate. My memory flags this as the "2nd DM delicacy" (S95 W6-5 Leggett conditional-PASS). I convert the rate computation to a gate in §V (CF-MACK-5).

### III.6 σ₈ is VIABLE, not a resolution
σ₈ = 0.799 sits between Planck and lensing. The capstone is careful to call this "VIABLE (~2σ between, not a resolution)," not a PASS that resolves the S₈ tension. **I endorse the hedge.** The framework predicts a value with the correct *sign* of deviation (toward the lensing end) from zero free parameters; that is evidence, but the document is right not to over-read it as resolving the S₈ tension. (See §IV.1 for the anchor-provenance problem on the *Planck* side of this comparison.)

---

## §IV — Conflicts, Gaps, and Unstated Assumptions

This is where I do my job hardest. None of these overturns a gate verdict; each flags a precision or coherence issue I am not entitled to silently resolve.

### IV.1 CONFLICT — σ₈ Planck comparison anchor does not match canonical provenance
The capstone §7.1 σ₈ row cites the comparison anchor as **"Planck 0.829"** (and my own `mack-now-endpoint-observation.md` draft and my agent memory also carry Planck σ₈ = 0.829 ± 0.014). But `canonical_constants.py:92` pins `sigma_8 = 0.811  # matter fluctuation amplitude (Planck 2018)`. **These disagree by 0.018 — larger than the quoted Planck 1σ of 0.014.** This is NOT a framework-value problem (the framework value 0.799 is canonical); it is a **comparison-anchor provenance inconsistency**. Three readings are possible and I do not resolve them here:
  (a) `canonical_constants.py:sigma_8 = 0.811` is the Planck 2018 *base-ΛCDM TT,TE,EE+lowE+lensing* σ₈, and `0.829` is a different Planck data-combination (e.g. without lensing, or a different chain) — in which case the capstone should cite *which* Planck combination it means, because the FW-vs-Planck σ-distance changes from ~2.1σ (vs 0.829) to ~0.86σ (vs 0.811) — a factor-2.4 swing in the headline tension.
  (b) `0.829` is stale and `0.811` is the canonical pin — in which case my memory and the capstone both carry a stale anchor and the FW value 0.799 is actually only ~0.86σ from the canonical Planck pin (a *better* result than the capstone claims).
  (c) The two refer to σ₈ vs S₈ = σ₈√(Ω_m/0.3) and one is mislabeled.
**Action**: this must be pinned before σ₈ is cited in any falsifier row. I flag it for the falsifier inventory (which I own) and convert it to a hygiene-gate in §V (CF-MACK-1). Per task rules I do not silently pick one; I report the divergence.

### IV.2 GAP / over-sharpening — the DESI DR3 "(2026)" tag conflates window-open with data-release
The capstone §7.2 row #1 and §7.3 call DESI DR3 "(2026)" the "near-term cliff-edge." The registry is more careful: the `R_842` **window opened 2026-04-23** (confirmed: `permanent-results-registry.md`, `session-84-plan-w1a.md`, `session-85-s7-combined-landscape-mack.md` all carry `DR3_window_opens=2026-04-23`), but the actual DR3 *data-release* horizon is **"~1 yr (DESI DR3 2027)"** per `atlas-04-assumptions.md` (w_0 row) and `atlas-08-open-questions.md` (Q37: "LIVE (1-yr horizon 2027)"). Today is 2026-05-29. So the window is *open* (no further `R_842` modification permitted, lockouts A–F enforced) but the *binding data* is not yet in hand and is not expected until 2027. **The "(2026)" tag reads as "the test resolves this year," which is not what the registry says.** This is a precision point on the falsifier I own. It does not change the *status* of the prediction (`wₐ = 0` four-fold lock, the live wager), but it changes the *timeline* a reader infers. I convert this to a falsifier-inventory correction in §V (CF-MACK-2). The capstone's emphasis that DESI DR3 is "the cliff-edge" is correct; the *year* should read 2026-window-open / 2027-data, not a flat "2026."

### IV.3 UNSTATED ASSUMPTION — the joint-evidence argument assumes a flat prior over geometries
§7.3's joint statement — "the chance of one random geometry reproducing observables in the relic-abundance AND CC-scale AND matter sectors is the product of the individual improbabilities across distinct spectral-moment layers" — is **structurally correct** (the Wronskian licenses the layer-independence) but carries an unstated assumption: that the relevant prior over candidate geometries is flat (or at least, that "one random geometry" is a well-defined null). The framework has exactly ONE geometry (`SU(3)`, Jensen-deformed, one modulus `τ`); there is no ensemble of geometries being sampled. So "the chance one random geometry reproduces..." is a *counterfactual* over a space the framework does not actually have. The honest version of the argument is: *given* a single geometry with `dim{τ, Λ, f₀, f₂, f₄} + t*` free parameters, the prior-predictive range of each observable is wide (5-OOM for m_H per my memory), and landing near data across multiple decoupled layers from that few-parameter ledger is a high-likelihood-ratio result. **That is a defensible Bayesian-evidence claim, and it is the one the framework is entitled to.** The "random geometry" phrasing is rhetorically stronger but invokes an ensemble that does not exist. A skeptic will press exactly here. The capstone should state the joint claim in prior-predictive-range terms (which it *does* in the EVOI-style language elsewhere), not in "random geometry" terms. **Flagged as a framing precision issue, not a math error.** (Cross-link: `evoi-prioritization.md` — "weighted by prior predictive range / posterior width" is the correct formulation; §7.3's "random geometry" sentence drifts from it.)

### IV.4 GAP — the `a(t)` closure is one bridge, but the capstone's "one gap not two" claim needs the three sub-pieces individually
§6.3 and frontier #1/#8 argue that closing one derived generally-covariant 4D action for `g_M` simultaneously closes (a) the effective Friedmann map, (b) the emergent equivalence principle, and (c) the emergent EIH theorem — "closing one closes both, which reduces the open frontier's dimensionality." **I agree this is the right structural picture** (and the S95 W3 genericity-qualified EP results support the EP leg at the structural level). But the capstone also lists three *concrete* sub-requirements: (i) the derived `S_SA(τ) →` 4D gravitational action, (ii) resolution of the `K_pivot` paradox (C2, BROKEN-WITH-LIVE-RESEARCH-PATHWAY), (iii) the `M_KK⁻¹ →` seconds normalization. These three are *not* obviously a single bridge — (iii) in particular (the dimensional normalization) is a units-pinning problem that could in principle be solved independently of (i) the back-reaction closure. **The "one bridge" claim is an aspiration, not a theorem.** The capstone is honest that the bridge is undelivered; my flag is narrower: the *unity* of the three sub-pieces is asserted, not proven, and a referee could reasonably ask whether (iii) is separable. I convert the cleanest sub-piece — the `M_KK⁻¹ →` seconds normalization under the S95 W4-4 Connes-distance conformal embedding — to a tractable gate in §V (CF-MACK-4), because that is the one sub-piece that looks closable without the full back-reaction equation.

### IV.5 GAP — `α_s` resolution is a future test, correctly, but the "+0.67σ consistent" pivot value should carry its own anchor
The capstone §7.1 α_s box resolves the apparent 12σ tension as a channel artifact (transport degree `deg(T_{BZ→pivot}) = +2`, S93 W7-1): the pivot image sits at +0.67σ, consistent, and the substrate-distance value `−0.0859` relocates to a ~34σ-reach CMB-S4/CMB-HD falsifier. **I endorse the structure** (this is exactly the substrate-first move done right — the transport degree is *computed*, not chosen, and it leaves a *sharper* falsifier, which is the opposite of saving the phenomenon). One precision point: the capstone quotes the comparison anchor as "Planck `−0.0045±0.0067`," but my canonical pin (S86 W1b-8 update) is **ACT DR4 + Planck (Aiola 2020): α_s = +0.0023 ± 0.0063** (`alpha_s_canon`), which *supersedes* the Planck-2018-only value. The "+0.67σ" pivot consistency is computed against *which* anchor? If against Planck-2018-only (`−0.0045`), the σ-distance changes when re-evaluated against the canonical ACT+Planck (`+0.0023`). `canonical_constants.py:1877` explicitly marks `planck_alpha_s_err = 0.0067` as **LEGACY ... superseded by alpha_s_canon_2020_err per S86-W13 P12.** So the capstone is citing a superseded anchor in the α_s box. **The resolution (channel artifact) stands; the comparison anchor should be updated to ACT+Planck.** Flagged for the inventory; converted to CF-MACK-6.

### IV.6 MINOR — `Omega_DM` symbol collision (no conflict, a trap)
`canonical_constants.py:90` pins `Omega_DM = 0.266` (the dark-matter *density parameter* Ω_c = Ω_m − Ω_b), while the capstone correctly uses **Ω_DM h² = 0.120** (the physical density). No error in the capstone — but the bare symbol `Omega_DM` in canonical constants is the density parameter, not the `h²`-weighted physical density. Any downstream script importing `Omega_DM` and comparing to 0.120 would be off by `h²`. This is a constants-hygiene trap, not a capstone defect; I note it so the falsifier inventory does not inherit the collision.

### IV.7 No conflict found — items I checked and cleared
- The two-`a_n`-object firewall (§8.2) is internally consistent: `a_n^ζ` (canonical numerics: a₀=6440, a₂=2776.165389, a₄=1350.7216, all confirmed in canonical_constants.py) vs `a_n^SD` (regulator-free layer identity). The `a₄^ζ = 1350.7216` was pinned *this build* (S75/S88) and is now canonical — confirmed via `get_constant`.
- The `t* = 0.08832` empirical-coupling ledger is consistent with the S95 W2-1 FAIL (the "t* is the one-loop threshold coefficient" corridor is CLOSED, `R = 1.977`), so the ledger correctly remains `{τ, Λ, f₀, f₂, f₄} + t*`, NOT the de-empiricized version. My memory and the capstone agree.
- The R₁ = a₀a₄/a₂² = 1.12865 scheme-invariant FI ratio is Sage-verified (1.128655). I am not the spectral-functional reviewer, but the FI/RD partition (§3.2) is exactly the Bayesian-model-averaging-over-nuisance-functional structure I would demand, and the capstone frames it that way.

---

## §V — Carry-Forward Computations (the open-question harvest)

> Per the user's "ripe harvest" instruction, every open question I can identify from my vantage is converted to a concrete, runnable computation. Each carries all four fields (What / Inputs / Gate / Effort). These feed the next compute session. Ordered by my EVOI judgment (highest first): the items that most sharpen a falsifier or close a load-bearing gap rank first.

### CF-MACK-1 — Pin the σ₈ Planck comparison anchor (resolves IV.1)
- **What**: Determine which Planck-2018 data-combination the framework's σ₈ comparison anchor refers to, and reconcile `canonical_constants.py:sigma_8 = 0.811` against the capstone's "Planck 0.829" and my memory's `0.829 ± 0.014`. Recompute the FW(0.799)-vs-Planck σ-distance under the *correct* canonical anchor. Determine whether the relevant comparison is σ₈ or S₈ = σ₈√(Ω_m/0.3).
- **Inputs**: `canonical_constants.py:sigma_8` (0.811), `sigma_8_err`; Planck 2018 (Aghanim+2020 A&A 641 A6) Table 2 σ₈ and S₈ values for TT,TE,EE+lowE+lensing and TT+lowE chains; framework σ₈ = 0.799 (E33) and growth-factor variant 0.793 (S59); Ω_m = 0.3089.
- **Gate**: PASS if a single canonical Planck σ₈ (or S₈) anchor is pinned with its data-combination named, AND the FW-vs-Planck σ-distance is recomputed and recorded with that anchor (pre-register: |Δσ| consistency check that the new anchor's σ-distance is internally consistent across the σ₈ and S₈ formulations). INFO if the discrepancy traces to a σ₈/S₈ labeling difference. This is a hygiene/SOURCE-RECON gate, not a physics gate — verdict is the pinned anchor, not a new framework number.
- **Effort**: Low (≤1 hr). One canonical-constants reconciliation + one σ-distance recompute; no spectral computation. Output: corrected falsifier-inventory σ₈ row + canonical_constants provenance note.

### CF-MACK-2 — Correct the DESI DR3 timeline in the falsifier inventory (resolves IV.2)
- **What**: Split the single "DESI DR3 (2026)" tag in Falsifier #1 into window-open (2026-04-23, lockouts A–F enforced, no `R_842` modification) vs data-release horizon (DESI DR3 2027 per Q37). Update `falsifier-master-inventory.md` row #1 and the §7.2 timeline so a reader infers the correct binding-data date.
- **Inputs**: `permanent-results-registry.md` (`DR3 = 2026-04-23 window opens`); `atlas-08-open-questions.md` Q37 ("LIVE 1-yr horizon 2027"); `atlas-04-assumptions.md` w_0 row ("~1 yr DESI DR3 2027"); `session-84-plan-w1a.md` audit_flow_schedule.
- **Gate**: PASS if the inventory row #1 and the detector-decisive timeline both carry the two-date structure (window-open 2026-04-23 / data-release 2027) AND the "cliff-edge" language is re-anchored to the data-release date. This is a falsifier-inventory hygiene gate (I am sole writer).
- **Effort**: Low (≤30 min). Documentation correction; no computation. Output: corrected inventory row + timeline.

### CF-MACK-3 — Does the exit greybody filter pull `A_s` below the Planck value, or narrow within the band? (resolves III.3)
- **What**: Compute the post-greybody `A_s` central value (not just the band) using the S95 W4-3 analog greybody factor `Γ(ω)`, to decide whether `A_s = (produced squeeze) × ∫Γ(ω)dω` lands toward Planck's `2.10×10⁻⁹` (band collapses downward → PASS) or stays within `[3.11, 4.27]×10⁻⁹` (band narrows in place → `A_s` becomes a ~33σ tension). The current band's lower edge is already above Planck; this gate decides whether `A_s` is a pending band or a live tension.
- **Inputs**: `ε_pivot` (the open piece, W5a P3 carry-forward); the BdG squeeze amplitude (`N_pair`, `P_exc = 1`, §5.3); the greybody transmission `Γ(ω)` profile (S95 W4-3, Pöschl–Teller barrier, transmitted_fraction = 0.512); Mukhanov–Sasaki `z = a√(2ε_H) M_Pl,eff` (§5.3); Planck `A_s = (2.10±0.03)×10⁻⁹`.
- **Gate**: Pre-register: PASS if post-greybody `A_s` central ∈ Planck `2.10 ± 3σ` (i.e. `[2.01, 2.19]×10⁻⁹`); FAIL/TENSION if central remains ≥ 3.11×10⁻⁹ (band did not pull down); INFO if `ε_pivot` cannot be pinned and only a band survives. This is a genuine physics gate on a load-bearing CMB amplitude.
- **Effort**: Medium (~3–4 hr). Requires the `ε_pivot` determination (currently open) + the greybody integral `∫Γ(ω)dω` over the squeeze support `ω ∈ [0.82, 1.06]`. Blocked on `ε_pivot`; if `ε_pivot` is genuinely undetermined, this is a 2-gate chain (first pin `ε_pivot`, then `A_s`).

### CF-MACK-4 — The `M_KK⁻¹ →` seconds normalization under the Connes-distance conformal embedding (the most tractable piece of the `a(t)` gap, resolves IV.4 sub-piece iii)
- **What**: Using the S95 W4-4 conformal-factor construction `Ω(τ)` that embeds the modulus-space causal structure into 4D and reproduces the deceleration band `q ∈ [−0.97, +0.81]` (with the Connes-distance proxy `a(τ)`, NOT `a_eff`), attempt to fix the single dimensionful normalization `M_KK⁻¹ →` seconds by matching one observable anchor (e.g. the redshift of matter-radiation equality `z_eq = 3425`, or `H_0`). Test whether the normalization is *separable* from the back-reaction closure (frontier #1) or whether it is genuinely entangled.
- **Inputs**: `Ω(τ)` conformal embedding (S95 W4-4, INFO); Connes-distance `a(τ)` (SCALE-FACTOR-54, `q` from −0.97 to +0.81); `M_KK = 7.4287×10¹⁶ GeV`; one external anchor (`z_eq = 3425` per Z-EQ-CHECK-66, or `H_0 = 67.74`); the deceleration band.
- **Gate**: PASS if a single `M_KK⁻¹ →` seconds factor reproduces the chosen external anchor (`z_eq` or `H_0`) to within its observational error AND the same factor is consistent across a second anchor (over-determination check, pre-register both anchors). FAIL if the two anchors require different normalization factors (→ the normalization is *not* separable from the back-reaction closure, which is itself a useful structural result). INFO if only the Connes-distance proxy works (consistent with W4-4) but no single factor over-determines.
- **Effort**: Medium (~4–6 hr). Conformal-embedding + two-anchor over-determination. This is the cleanest closable sub-piece of the `a(t)` gap; even a FAIL is informative (it proves the three sub-pieces of frontier #1 are entangled, sharpening IV.4).

### CF-MACK-5 — Leggett-mode gravitational decay rate `Γ_grav` vs `H_0` (resolves III.5, the CRITICAL DM conditional)
- **What**: Compute the gravitational decay rate `Γ_grav` of the Leggett-channel GGE quasiparticle and compare to `H_0`. The entire Ω_DM h² = 0.120 PASS (my §II.4 strongest claim) is hostage to `Γ_grav < H_0` (LEGGETT-GRAV-DECAY-67, CRITICAL). If `Γ_grav > H_0`, the DM sector collapses.
- **Inputs**: Leggett-mode gap `Δ_Leggett`; quality factor `Q = 18.6`, `Z = 0.972` (LEGGETT-SPECTRAL-66); the Leggett-Josephson gap ratio `ε = Δ_Leggett/Δ_Josephson ≈ 0.005–0.011` (E47); the graviton coupling of the inter-band coherence mode (the `δq` oscillation); `H_0 = 67.74 km/s/Mpc` → natural units.
- **Gate**: PASS if `Γ_grav < H_0` (DM relic survives to today → 0.120 holds); FAIL if `Γ_grav ≥ H_0` (DM sector collapses, 0.120 meaningless → the strongest DM claim falls). Pre-register the decay-channel: gravitational radiation from the `δq` collective mode (CPT-neutral, `T^{0i}=0`, so the leading channel is quadrupolar at best — expect strong suppression).
- **Effort**: Medium (~3–5 hr). Requires the graviton-Leggett coupling vertex (the `a₂`-channel coupling of the collective mode) and a decay-rate estimate. High EVOI: it is the load-bearing conditional under my single strongest endorsed result.

### CF-MACK-6 — Re-evaluate the α_s pivot-image σ-distance against the canonical ACT+Planck anchor (resolves IV.5)
- **What**: Recompute the α_s pivot-image σ-distance (currently "+0.67σ consistent") against the *canonical* anchor **ACT DR4 + Planck (Aiola 2020): α_s = +0.0023 ± 0.0063** (`alpha_s_canon`), which supersedes the Planck-2018-only `−0.0045 ± 0.0067` the capstone cites. Also re-evaluate the substrate-distance `−0.0859` CMB-S4/CMB-HD reach against the canonical anchor.
- **Inputs**: `alpha_s_pivot_goldstone ≈ 0`; `alpha_s_substrate_distance_1 = −0.08587279`; canonical anchor ACT+Planck (`+0.0023 ± 0.0063`); superseded anchor (`−0.0045 ± 0.0067`, `canonical_constants.py:1877` LEGACY); CMB-S4 `σ_α ≈ 2.3×10⁻³`, CMB-HD `σ_α ≈ 1.1×10⁻³`.
- **Gate**: PASS if the pivot-image σ-distance against the canonical ACT+Planck anchor is < 1σ (consistency preserved under the updated anchor); the substrate-distance CMB-S4 reach recomputed and recorded. INFO if the anchor swap changes the pivot σ-distance materially (then the capstone's "+0.67σ" must be updated). SOURCE-RECON gate (anchor update), not a new framework number.
- **Effort**: Low (~1 hr). Two σ-distance recomputes against the corrected anchor. Output: corrected α_s box anchor + falsifier-inventory row #3.

### CF-MACK-7 — Reframe the §7.3 joint-evidence statement in prior-predictive-range terms (resolves IV.3)
- **What**: Replace the "chance of one random geometry" framing with the EVOI-canonical prior-predictive-range / posterior-width formulation. Quantify the joint likelihood ratio as the product, across decoupled `a₀/a₂/a₄` layers (Wronskian-licensed), of (observation falls in posterior) / (prior-predictive range), using the actual prior-predictive ranges of each observable (e.g. m_H's 5-OOM range). This converts a rhetorical claim into a computed BF.
- **Inputs**: prior-predictive ranges per observable (m_H ~5 OOM; Ω_DM h²; CC magnitude; σ₈); the Wronskian layer-independence (S75 W2-E); the decoupled-layer assignment (Ω_DM and σ₈ both `a₂` → NOT multiplied; `a₀ × a₂ × a₄` → multiplied); `evoi-prioritization.md` weighting rule.
- **Gate**: PASS if a single joint Bayes-factor (or likelihood-ratio bound) is computed across the three decoupled layers with explicit prior-predictive ranges, replacing the "random geometry" phrasing. Pre-register: the BF must NOT multiply within-layer observables (Ω_DM, σ₈). INFO if prior-predictive ranges cannot be pinned for ≥1 layer (then report a bound, not a point).
- **Effort**: Medium (~2–3 hr). No new physics; a UQ computation over existing predictions. Sharpens the single strongest rhetorical claim in §7 into a defensible number — high value against the inevitable referee challenge.

### CF-MACK-8 — The live BAO channel: S43 first-sound ring amplitude vs a named experiment (resolves §6.2 INFO-by-unavailability)
- **What**: Complete the S95 W6-2 forecast (INFO-by-unavailability) for the S43 first-sound ring (`A_FS/A_BAO = 0.204 = c_2²/c_1²`, `r_1 ≈ 325 Mpc`, `k_1 ≈ 0.0193 Mpc⁻¹`) — a zero-parameter prediction with NO ΛCDM counterpart — against a named experiment's acoustic-scale amplitude sensitivity (DESI full-shape, Euclid, or SKA). The substrate forecast `A_FS = 0.204` is in hand; the comparison value was unavailable at compute (paper-search MCP down).
- **Inputs**: `A_FS = 0.204` (S43, live channel); `r_1 = 325.3 Mpc`, `k_1 = 0.0193 Mpc⁻¹`; the per-branch BAO sub-feature `δP/P ≈ 1.4×10⁻³` (effacement-suppressed, OUTSIDE current rulers, B1); a named experiment's amplitude sensitivity at `k_1` (DESI DR2 full-shape `σ(δP/P)` at `k ~ 0.02 Mpc⁻¹`, or Euclid forecast).
- **Gate**: PASS if `A_FS = 0.204` is above the named experiment's amplitude sensitivity at `k_1` (detectable → a clean FW-vs-ΛCDM discriminant); INFO if below sensitivity (a pre-detector forward-edge prediction); pre-register the experiment and its sensitivity before fetching. Distinct from the suppressed per-branch sub-feature (which is OUTSIDE DESI by design).
- **Effort**: Low-Medium (~2 hr once the comparison value is fetched). Blocked on fetching the named-experiment sensitivity (curl arxiv fallback per my debugging notes if paper-search MCP is down). High value: this is a zero-parameter prediction with no ΛCDM analog and a real near-term ruler.

### CF-MACK-9 — Re-derive the n_s BMA band over a *justified* functional family (sharpens III.2)
- **What**: The S67 BMA band `n_s = 0.969 ± 0.022` marginalizes over a three-member functional family `{√x, zeta-a₄, anomaly-φ}`. Determine whether the physical functional `f` is constrained to lie in (or near) this family by a substrate-first argument (e.g. the convergence-cone constraint of §3.3: admissible `f` are exactly those whose Mellin transform is supported on the pole ladder `S_d = {0,2,4,6,8}`). If the cone constrains the family, the BMA band's prior is justified; if not, the band's width is a free choice.
- **Inputs**: the dimension spectrum `S_d = {0,2,4,6,8}` (CM-1995, E38, τ-independent per S31Aa); the three functional `n_s` values {0.9561, 0.9590, 0.9595}; ANOMALY-FAMILY-EXCLUSION (S67) and ZETA-NOT-PHYSICAL (S75); the cutoff `f*(x) = 0.9117√x + 0.0883e⁻ˣ`.
- **Gate**: PASS if the convergence-cone constraint (or another substrate-first argument) restricts the admissible `f` to a family whose `n_s` spread reproduces the BMA band width ±0.022 (band prior justified); FAIL if admissible `f` span a wider `n_s` range (band understates the functional uncertainty → BMA band is PRELIMINARY on the prior, per my III.2 flag); INFO if the cone admits `f` outside the three-member set but `n_s` cannot be computed for them. Pre-register the convergence-cone admissibility predicate.
- **Effort**: Medium-High (~5–8 hr). Requires enumerating cone-admissible `f` and computing `n_s` for representatives — genuine spectral-functional work. This is the deepest open item touching the CMB tilt and is contingent on the unsolved functional selection (frontier #2).

---

## §VI — Recommendation / Closing Assessment

**Structural assessment, not a verdict** (per my role).

The capstone is, from the observational-cosmology vantage, an honest document. Its central claim — the universe is the spectral action of one Dirac operator, and the cosmological stage is *derived* (`a₂ → g_M`), not presupposed — is categorically stronger than container-based unification, and the document is scrupulous about the one place that claim does NOT close: there is no derived FRW `a(t)`, and every late-time observable in §7 borrows the container-observer's `H(t)` (C10). That admission is what keeps the data section from over-claiming, and it is the largest open harvest in the document. I endorse the framing that "Friedmann is the wrong question at the *fundamental* level, the right question at the *effective* level" — both must be said, and the capstone says both.

What is solid from my desk: the **layer decoupling** (Wronskian-certified, the thing that licenses the joint-evidence multiplication); the **CC factoring** (exact non-inheritance via Gibbs-Duhem + C10-conditional magnitude); the **structural DM zero** (`σ/m = 0`, superselection-protected, distinct in kind from any WIMP); the **transit-not-slow-roll** structural argument (the right reason `r ≠ 16ε`); and the **acoustic-white-hole** horizon resolution with its two-null-cone consequence for `r`/`n_T`.

What I flag, in order of weight:
1. **σ₈ comparison-anchor inconsistency** (IV.1): `canonical_constants.py` says Planck σ₈ = 0.811; the capstone and my own memory say 0.829. This swings the headline σ-distance by a factor ~2.4 and MUST be pinned before σ₈ appears in any falsifier row. I do not resolve it here (per task rules); CF-MACK-1.
2. **DESI DR3 timeline over-sharpening** (IV.2): window-open 2026-04-23 ≠ data-release 2027. The "(2026) cliff-edge" tag reads as "resolves this year"; the registry says binding data is 2027. CF-MACK-2.
3. **`A_s` may be a tension, not a pending band** (III.3): the band's lower edge already sits ~33σ above Planck; whether the greybody filter pulls it down or narrows it in place is uncomputed. CF-MACK-3.
4. **The `Γ_grav < H_0` conditional is CRITICAL** (III.5): my single strongest endorsed result (Ω_DM h² = 0.120) is hostage to an uncomputed decay rate. CF-MACK-5.
5. **The joint-evidence "random geometry" framing** (IV.3) invokes an ensemble the framework does not have; restate in prior-predictive-range terms. CF-MACK-7.
6. **The α_s comparison anchor is superseded** (IV.5): the "+0.67σ consistent" pivot value is computed against Planck-2018-only; the canonical anchor is ACT+Planck. The *resolution* (channel artifact) stands; the *anchor* needs updating. CF-MACK-6.

None of these overturns a gate verdict. They are precision, provenance, and coherence issues — exactly the class of issue a falsifier-inventory owner is responsible for catching before a binding observational event. The two near-term binding events (DESI DR3 data ~2027 on `wₐ`; the σ₈/S₈ tension as Stage-IV surveys sharpen) are precisely where the borrowed-`H(t)` caveat (C10) and the anchor-provenance issues (σ₈, α_s) intersect — which is why CF-MACK-1, -2, and -6 (all low-effort hygiene gates) should run *first*, before the framework's predictions are quoted against the next data drop.

The harvest is real. The richest single computation is **CF-MACK-4** (the `M_KK⁻¹ →` seconds normalization), because it is the most tractable sub-piece of the load-bearing `a(t)` gap and even a FAIL is structurally informative; the highest-EVOI is **CF-MACK-5** (the `Γ_grav` rate), because it gates the strongest DM claim; and the one most likely to survive a hostile referee is **CF-MACK-7** (the prior-predictive-range reframing of the joint evidence). I would run the three low-effort anchor-hygiene gates (CF-1/2/6) in the next session's Wave 0, then the two medium physics gates (CF-4/5) in Wave 1.

---

*Files referenced (absolute paths):*
- *Source: `C:\sandbox\Ainulindale Exflation\sessions\framework\phonic-exflation-equation.md`*
- *My §7 build draft: `C:\sandbox\Ainulindale Exflation\sessions\framework\Collabs\equation-build\mack-now-endpoint-observation.md`*
- *Canonical constants: `C:\sandbox\Ainulindale Exflation\computations\_shared\canonical_constants.py` (sigma_8:92, w0_FW:1870, ns_framework:1921, n_s_FW_exact:2009, a_4_FW_zeta:464, planck_alpha_s_err LEGACY:1877, Omega_DM:90)*
- *Falsifier inventory (I am sole writer): `C:\sandbox\Ainulindale Exflation\sessions\framework\registry\falsifier-master-inventory.md`*
- *Observational anchors: `C:\sandbox\Ainulindale Exflation\sessions\framework\registry\mack-observational-constraints.md`*
