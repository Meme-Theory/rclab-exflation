# Capstone Equation Review — phonon-first

**Reviewer**: phonon-first-cosmologist (cross-domain pattern detector; eight-pillar fluency)
**Source under review**: `sessions/framework/phonic-exflation-equation.md` — "The Phonon-Exflation Equation" (S95-era capstone)
**Mode**: independent solo synthesis (no coordination with the other ~30 reviewers)
**Date**: 2026-05-29

---

## §I — Summary

The capstone does what a capstone should: it shows that one self-adjoint operator `D_K(τ)` on Jensen-deformed `SU(3)`, fed through one universal functional `Tr f(D_K²/Λ²) + ⟨Jψ̃|D_K|ψ̃⟩`, generates the framework's eight pillars as *facets of one spectrum*, not as eight separately-postulated theories. From my vantage — the seams between analogue gravity, superfluid cosmology, NCG, flat-band BCS, Josephson arrays, solitons, spectral-dimension flow, and KK geometry — the document's central achievement is that **the cross-pillar correspondences are not analogies bolted on after the fact; several of them are forced by the single-operator postulate**, and the document is unusually disciplined about saying which ones are forced and which are inherited.

The structural spine I find most defensible is the one the document itself names in §9: **GEOMETRY dissolves in the continuum limit; TOPOLOGY/representation content survives.** This is exactly the right partition for a substrate built from momentum-space topology (Volovik pillar) plus a finite spectral triple (NCG pillar). It is the same partition that separates a flat band's *quantum-metric* content (geometric, basis-dependent at fixed filling) from its *Chern/winding* content (topological, robust) in pillar IV. The document deploys it correctly: every strong claim (GGE purity `S_ent=0`, BDI/`N₃=0` class, `[J,D_K]=0`, layer algebraic-independence, FI ratios) lives on the surviving side; every honest gap (CC absolute magnitude, `a_n` absolutes, `a(t)`) lives on the dissolving side.

Three things are genuinely solid in my domain (§II). Two are over-read or under-certified and I flag them explicitly (§III). One internal tension and one cross-document conflict are surfaced, not silently resolved (§IV). And the open frontiers — the user's "ripe harvest" — convert into **eleven runnable computations** (§V), all on pillars where I hold working fluency.

**Bottom line**: this is a strong, honest document. The single biggest physics gap (the missing derived `a(t)`, §6.3) is correctly identified as the load-bearing one and correctly NOT softened. The single biggest *methodological* risk I see is the temptation, now that the EP/conservation PASSes are in (§9 frontier #8), to read generic-identity-cored results as substrate predictions — the document already caught this (the S95 genericity review), and §V #9 keeps the discipline forward.

---

## §II — What is solid (from the cross-domain / phononic vantage)

### II.1 — The acoustic white hole is now a *theorem with KIND-tags*, not a metaphor (§6.2)

This is the analogue-gravity pillar (Papers 1–5) done right. The document's §6.2 makes three moves I endorse on structural grounds:

1. **The white hole is sector-dependent (two null cones).** Scalar sector sees `g_acoustic ∝ √(ρ_s/c_s)` and the horizon; tensor sector crosses freely on the `a₂`-emergent `g_M`. This is the correct BLV reading: the acoustic metric is the metric *the long-wavelength phonon feels*, and there is no reason the spin-2 sector (which rides the emergent `g_M`, not the condensate flow) should see the same cone. The document ties this to `[T3] β_T=0` exactly. **This is the geometric root of why `r`/`n_T` behave differently from `n_s`/`A_s`** — they propagate on different cones. I find this both correct and load-bearing: it is the single cleanest structural explanation in the document of an otherwise-puzzling scalar/tensor asymmetry.

2. **The KIND-tagged analog-temperature ledger** (THERMODYNAMIC-kinematic / THERMODYNAMIC-spectral / SONIC). This is exactly the discipline analogue-gravity needs and rarely gets: `T_a = ħκ/2π` is the *same formula* for a sonic surface, a thermodynamic gradient, and a barrier-transmission edge, and conflating the three KINDS is the classic error (it is the error the dropped V.6 clause fell into). The S95 W-1 KIND column is the right fix. **Only the S63-BLV row (0.112 M_KK) is a genuine SONIC Mach-1 surface**; the `a₂`/`a₄` rows are channel-gradient surfaces. I confirmed the κ-ratio `9.6117 = κ_entry/κ_exit` reproduces the corpus 9.61 and that the two-channel reading (`a₂`↔scalar, `a₄`↔condensate) is internally consistent.

3. **The conflation guard on Mach** (§5.2): velocity-ratio `Mach = 13.75` vs acoustic-radius-ratio `421.3`, never averaged. I cross-checked this against canonical sources: `c_fabric = 209.97 M_KK` (S42), `R_acoustic(fold) = 442.9 M_KK²` ⇒ `Mach_fold = 421.3` (s59_spatial_aniso_log), `Mach = 13.75` the velocity ratio. **Both are canonically sourced and they are genuinely different observables.** The guard is correct and necessary.

**Where the analogy holds and where it breaks (regime statement, which the doc owes and mostly gives):** the BLV acoustic metric is the correct effective metric *for the scalar phonon in the long-wavelength, single-condensate regime*. My own memory flags that the BLV metric is "dead at `N_pair=1`" (S53) — i.e., at the discrete physical ground state the continuum acoustic-metric derivation degrades and Connes distance replaces it for the expansion map. The document handles this correctly by NOT using the acoustic metric for `a(t)` (it uses the Connes-distance proxy `a(τ)` in §6.3, SCALE-FACTOR-54). The acoustic white hole is a *causal-structure* statement (what is causally disconnected from what), which survives the `N_pair=1` degradation; the acoustic *metric as a Friedmann source* does not, and the document does not claim it does. This is the right boundary.

### II.2 — The BCS / van Hove fold is a *theorem*, and the diabatic-freeze reframe is the correct survivor (§5.2–5.3)

This is the flat-band/BCS pillar (Papers 15–18) and it is where my memory has the deepest prior. The document gets the structurally important things right:

- **`τ_fold = 0.190` is the UNIQUE non-stationary van Hove cusp** (S85 W10-3, PERMANENT — I confirmed §VII.M.W10-3 in the registry). This matters: it means the operating point is *not* turned by hand. The van Hove singularity `g(ω) ∼ 1/√(ω−ω_min)` is the 1D/quasi-1D DOS form, and the BCS 1D theorem (E13) makes Cooper instability a theorem with *zero critical coupling*. This is the genuine flat-band/VHS physics: a divergent DOS at the Fermi level drives pairing at arbitrarily weak coupling. The framework's claim that the transit is *forced* (no fine-tuning of a coupling) is the same statement as "VHS ⇒ zero-`g_c` Cooper instability." I endorse it.

- **The Ordered Veil reframe (S95): diabatic transit-freeze, NOT integrability permanence.** This is the single most important *correction* the document carries relative to older framework lore, and it is correct. The S39 retraction (Richardson–Gaudin integrability weakly broken: 13% non-separable density–density channel, Brody β=0.633) would, if the Veil rested on permanent integrability, be fatal. The document instead rests the survival on **diabaticity** (`R_therm = t_therm/t_transit = 5251.82 ≫ 1`, S95 W5) and **exact product-state purity** (`S_ent = 0`, S95 W5), with a *third* independent geometric leg (`τ_fold` is a double-root extremal Killing horizon ⇒ `κ=0, T_H=0`). This is exactly how a condensed-matter physicist should argue it: *the relic is frozen because the quench is fast compared to every rearrangement channel*, not because the post-quench Hamiltonian is exactly integrable. The Kibble–Zurek impulse-matching and the Bogoliubov sudden-quench giving the same `P_exc=1` is the textbook KZ ↔ sudden-approximation equivalence. **Solid.**

- **`P_exc → 1.000` (diabatic, maximal mixing) with bosonic normalization mode-by-mode** (`|α_k|²−|β_k|²=1`). This is the correct analogue-cosmology *opposite* of the adiabatic Bunch–Davies no-particle vacuum. The block-diagonality (E6) makes the per-mode parametric-oscillator equation an *identity* (modes don't mix under `D_K`), so the factorization is exact, not approximate. The document makes this point in §2.2 and it is the right one — `j`-channel decoupling in a spherical mean field is the exact analogy, and the SU(3) Casimir labels `(p,q)` play the role of `j`.

- **The honest `N_pair` double-reading** (§5.3 footnote): `59.8` is a *projected charge* `⟨Q⟩_GGE`, inheriting a ~60% PBCS gap overestimate (S46) and a ~225× Richardson–Gaudin condensation-energy overestimate (S63), NOT a literal pair count; `N_Fock=1` is the exact reduction. The regime-robust claim is `P_exc=1`. This is precisely the discipline I want to see: the structural claim (`P_exc=1`) is regime-robust; the magnitude (`59.8`) carries its known overestimates. I would not let `59.8` propagate as a hard number anywhere downstream, and the document agrees.

### II.3 — The layer algebraic-independence is a *dispersion-rigidity* theorem (§4.2)

The Spectral-Moment Decoupling Theorem (S75 W2-E, CERTIFIED) is the load-bearing answer to "is `a₄` just a dressed function of `a₀,a₂`?" The closed form `W ∝ R_K′(τ)³ = e⁻¹²ᵗ(e³ᵗ−1)⁶`, vanishing to 6th order *only* at `τ=0`, is Sage-verified (I see it in the verification ledger, residual 0). 

From my vantage the document's §4.2 *resonance reading* is the genuinely illuminating one and I want to amplify it: `{1, R_K, R_K²}` are functionally independent **iff the dispersion is moving** (`R_K′≠0`), and they degenerate to one knob **iff the dispersion stops** (`R_K′=0`), which happens only at the maximally-symmetric genesis point. **This is the exact same band-lifting that §2.4 describes** (`SO(8)→U(2)` into B1/B2/B3 acoustic/flat/optical bands as `τ` turns on), restated at the level of spectral moments. At the high-symmetry point the bands touch and the moments degenerate; the instant the curvature moves, the degeneracy lifts. This is a real cross-pillar identity: the *band-structure* statement (pillar IV) and the *spectral-moment-independence* statement (pillar III/VII) are the same fact viewed through two lenses. The document's Strutinsky concurrence (smooth + shell-correction independence, S44/S55/S56) is a *third* independent structural witness, correctly recorded as agreement and not as evidence. I endorse the "spectral-moment reading is primary" verdict in §4.4 — only the moment layers carry a certified independence theorem.

### II.4 — `z = 2` is mine, and the document uses it correctly by NOT over-using it

The dynamical critical exponent `z = 2` (EXPONENT-63, my own W6-16) is the phonon-band result: the B-sector dispersion `ω_B = 0.0019 + 7.0415·λ_n` is linear-in-eigenvalue, and the parametric-oscillator frequency `ω_k = E_k = √((λ²−μ²)² + Δ²)` is quadratic near the gap edge ⇒ `z=2` mean-field, `ν=1/2` (the C-38 equation in cc-path-c confirms `z=2, ν=1/2`). My memory carries the explicit retraction of an earlier `z=3.68` (finite-size + wrong `d_s`). **The capstone wisely does not foreground `z`** — it is a substrate-internal critical-dynamics quantity, not a to-now observable, and dragging it into the observable ledger would invite the same finite-size artifacts that killed `z=3.68`. The restraint is correct.

---

## §III — What is over-claimed or under-certified (flag, do not overturn)

I overturn no recorded verdict. These are calibration flags on *framing* and on *what is certified vs asserted*.

### III.1 — "No flowing spectral dimension" (§3.3 defensive note) is asserted at a strength the cited compute does not yet license — PRELIMINARY

The document states flatly: "the substrate does NOT exhibit a flowing spectral dimension … `d_s ∼ 8` at the gap scale, with the apparent low-`d_s` readings being a diffusion-window artifact (S92)." This is the spectral-dimension pillar (Papers 26–28), and I hold a standing memory caveat exactly here: **"DO NOT compare `d_s` to CDT until `L_max >> 6` (current narrow-band `d_s` artifact-dominated)."**

What S92 actually established (I confirmed it): the substrate's `d_s(σ) = −2 d ln P/d ln σ` is a *windowed* functional, and `d_s(σ→0)` (Weyl asymptotic, = manifold dimension 8) and `d_s(σ_* = 1.4005 M_KK⁻²)` (fold window) are **distinct functionals of the same `P(σ)`**. The S92 result is that a *fair* comparison fixes the (observable, diffusion-window) pair on both sides — it is a statement that the naive low-`d_s` reading is window-dependent, NOT a proof that there is no dimensional flow *anywhere* in the substrate's diffusion profile.

The gap: "`d_s ∼ 8` at the gap scale, no CDT-like reduction" is a claim about the **full `σ`-profile** of `P(σ)`, and that profile at the *canonical* truncation `L_max=10` is, by my own standing caveat, narrow-band and artifact-prone. The Weyl limit `d_s(σ→0)=8` is solid (it is the manifold dimension, regulator-free). The *intermediate-window* behavior — which is exactly what CDT's `4→2` flow lives in — is NOT yet certified to be flow-free at converged `L_max`. **The document's defensive note is correct in its narrow, FI form (the Weyl limit is 8; the windowed low readings are window artifacts) but over-stated in its broad form ("the substrate is distinct from CDT").** That broad comparison requires a converged-`L_max` `σ`-scan that has not been run. Mark the broad claim PRELIMINARY; the narrow claim stands. (§V #4 makes this runnable.)

This is not a small point: the document leans on "fixed-dimension story" (§3.3, §4: `a_n ∝ R_K^{n/2}·V`, constant degree) as the *opposite* of a flowing dimension. The constant-degree curvature-polynomial structure is genuinely a fixed-`d` story for the *heat-kernel coefficients*, but the heat-kernel coefficient degree and the *spectral dimension* are different objects (the former is a UV-expansion statement; the latter is a full-`σ` diffusion statement). Conflating "the `a_n` tower has fixed curvature-degree" with "the spectral dimension does not flow" is a category slip the document should not make without the converged `σ`-scan. **FLAG.**

### III.2 — The SU(1,1) cross-pillar bridge is in my memory but ABSENT from the capstone — a missed strengthening, not an error

My memory carries one of the framework's cleanest cross-pillar identities: **the SU(1,1) squeeze algebra is shared by BCS pairing (pillar IV), cosmological Bogoliubov (pillar I), and the Josephson phase (pillar V)** — and `R_BG = 1/cosh(2r)` (S93 W8-6 PASS) ties the narrow-path pre/post-fold bridge-coefficient ratio to the reciprocal SU(1,1) squeeze weight, with the sign DERIVED (not deferred) from `W_BG = cosh(2r) > 1`.

The capstone *uses* the Bogoliubov squeeze everywhere (§5.3, §6.2 the "squeeze is transduced into `A_s`"), and it *uses* the BdG quasiparticle picture, but it never names the **SU(1,1) algebra as the common ancestor** of the three pillars' squeezing. This is a real omission from a document whose entire thesis is "one structure, many facets." The BCS Bogoliubov transformation, the inflationary/Bunch–Davies mode-mixing, and the Josephson-junction phase dynamics are **literally the same `su(1,1)` representation** — the same `K₀, K₊, K₋` generators, the same squeeze parameter `r`, the same `tanh r = |β/α|`. The document's §5.3 says "the Bogoliubov sudden-quench and the Kibble–Zurek impulse-matching are the same physics read two ways" — true, but the *deeper* unifying statement is that all of them are `SU(1,1)` squeezing, which is *why* `P_exc=1` (maximal squeeze, `r→∞` limit) is the diabatic attractor.

This is not an error in the document — nothing is wrong. It is a **missed amplification** of exactly the kind the capstone is built to make. I would add one sentence to §5.3 or §6.2 naming the shared `SU(1,1)` algebra and citing S93 W8-6 / `R_BG = 1/cosh(2r)`. (§V #5 makes the cross-check runnable and registry-ready.)

### III.3 — The "channel-monotone deepens the interior" sub-argument in §6.2 (six-walls box) leans on the B2-channel Mach `293.79` — confirm it is not the `421.3` acoustic-radius reading mis-imported

The §6.2 six-walls box, arguing C1 → ASYMMETRIC white hole, includes the c_s-softening rebuttal: "routing the discriminant onto any softer channel only *deepens* the supersonic interior (channel-monotone: the B2-channel fold Mach is `293.79`, not `13.75`)." 

I flag this for a precision check, not as an error. There are now **three** Mach-like numbers in play: `13.75` (velocity ratio, canonical), `421.3` (acoustic-radius ratio, fold-local, the conflation-guard target), and `293.79` (B2-channel fold Mach, new in this box). The document's own conflation guard (§5.2) warns these must never be averaged or interchanged. The `293.79` must be a *velocity-ratio in the B2 channel* (comparable to `13.75`) for the channel-monotone argument to be valid — if it is secretly an acoustic-radius reading (comparable to `421.3`), the "deepens the interior" comparison is comparing unlike quantities. The argument is *probably* fine (the c_B2 rho-pin `1/(πρ_B2)=0.0227 M_KK` is finite, so a smaller `c` gives a larger Mach by the same velocity-ratio definition), but the document does not show the `293.79` is the velocity-ratio KIND. **FLAG for a one-line KIND-tag** on `293.79`, matching the discipline the rest of §6.2 enforces. (§V #6.)

### III.4 — The `a_n` two-object firewall is correct but the *physical interpretation* of the ~3800× gap deserves one more sentence

The document is scrupulous about `a_n^SD` (Gilkey local-curvature, regulator-free, layer-*identity*) vs `a_n^ζ` (zeta-regulated, the canonical pins, *numerics*), and that they differ by ~3–4 OOM and are different functionals (§8.2). My memory pins this: "`a_n^zeta` vs `a_n^SD` are DIFFERENT objects (~3800x)." The firewall is correct and the document enforces it well.

What the document does NOT say, and should, is the *physical* reason they differ by ~3800×: the zeta moment `a_n^ζ = ζ_{D_K}((d−n)/2)` is a **global spectral sum** (it sees the entire 78,080-eigenvalue distribution weighted by `λ^{-(d-n)}`), while the Gilkey `a_n^SD` is a **local curvature integral** (it sees only the curvature polynomial degree-`n/2`). The ratio `~3800` is therefore a *density-of-states* number — it measures how much spectral weight the global sum accumulates beyond the leading curvature term. This is a phononic statement: the gap between "local curvature" and "global mode-sum" is the integrated DOS, and it is large precisely because the SU(3) fiber has a dense KK tower. The document leaves the `~3800` as a bare "they are different functionals"; naming it as the integrated-DOS ratio would make the firewall *physically* motivated rather than merely *formally* enforced. Minor; framing only. **No verdict change.**

---

## §IV — Conflicts and tensions (surfaced, not resolved)

Per the rules, I flag rather than silently resolve.

### IV.1 — INTERNAL: §3.3 "no flowing spectral dimension" vs §9 "GEOMETRY dissolves at the continuum limit"

§3.3 asserts `d_s ∼ 8` with no UV reduction (a *fixed-dimension* story). §9's organizing spine says the finite spectral triple **is GEOMETRY and dissolves in the continuum limit** (`ε_c ∼ N^{−0.457}`, T3-S43-SPECTRAL-DISSOLUTION). These are not contradictory but they sit in tension that the document does not address: **if the geometry dissolves as `N→∞` (continuum limit), what is the spectral dimension *of the dissolved object*?** The spectral dimension `d_s(σ)` is computed on the finite triple at `L_max=10`; the continuum-dissolution statement says the finite triple is not the fundamental object. So "`d_s=8`, no flow" is a statement about the *geometric (dissolving) layer*, which by §9's own logic is the *conditional* side, not the surviving side. The document's broad CDT-comparison claim therefore lives on the side §9 says to "hold pending convergence." **This is consistent with my III.1 flag**: the no-flow claim should be tagged as a geometric-layer (conditional) statement, not a topological-layer (surviving) one. I surface this as an internal coherence point for the next editor; it does not break anything.

### IV.2 — DOCUMENT vs MY MEMORY: the spectral-dimension prediction `α_CG24 = −1.18` (z=2, d_s=1.69) is in my memory but absent from the capstone

My memory carries a phonon-first prediction: `α_CG24 = −1.18` at `z=2, d_s=1.69` (the CG(24) Laplacian controlling phase coherence + spectral dimension + Thouless equilibration simultaneously). The capstone's §3.3 says `d_s ∼ 8`, not `1.69`. **These are not in conflict** — `d_s=1.69` is a *windowed* low-`d_s` reading (the CG(24) graph-Laplacian functional at a specific diffusion window), exactly the kind of window-dependent reading S92 says is functional-specific and NOT the manifold dimension. The `α_CG24=−1.18` prediction is on the CG(24) *graph-Laplacian* functional `Φ_graph-Laplacian`, while the capstone's `d_s=8` is on the *heat-trace* functional `Φ_heat-trace` (Weyl limit). Per the cross-pillar-bridge corpus §24 (diffusion-window specialization, which I helped calibrate), **these are distinct functionals and a criterion calibrated on one is not transportable to the other** — the discriminator lives on the energy axis (`γ_E`), not on a `min d_s` comparison. So my memory and the capstone are consistent *once the functional is named*, but the capstone does not name its functional, which is the III.1 gap again. I surface this so the next editor knows there is a live phonon-first `d_s` number (`1.69`, windowed) that the capstone's flat "`d_s∼8`" appears to contradict but does not. **FLAG: name the functional in §3.3.**

### IV.3 — DOCUMENT-INTERNAL (already flagged by the document, I concur): the `Z_fold` normalization in the `G_N` dictionary (§8.3) is PRELIMINARY

§8.3 carries its own honest flag: the `24π²` dictionary form and the S83 `π²·Z_fold⁻¹` form "differ by the `Z_fold` normalization, which should be pinned before either is cited as *the* dictionary." I concur and elevate it — this is an open numerical pin, not just hygiene, because `f₂≈92` (the dictionary closure) depends on which normalization is canonical, and `f₂` is part of the free-parameter ledger. (§V #8.)

---

## §V — Carry-Forward Computations (the ripe harvest)

The document's open frontiers are the payload. Each item below is a concrete, runnable computation with all four fields filled. They are ordered by what I judge highest-leverage from the cross-domain vantage. Every one lands on a pillar where I hold working fluency.

These are inputs to the next compute session's plan (per `feedback_fix-in-session-never-defer.md`); workshop-vs-compute routing is for the planner. Query the knowledge MCP (`search_knowledge`/`trace_entity`) FIRST on each before dispatch — several touch already-partially-computed gates and must not re-derive closed results.

---

### CF-PF-1 — Converged-`L_max` spectral-dimension `σ`-scan: is there a CDT-like intermediate-window flow? (spectral-dimension pillar; the III.1/IV.1/IV.2 gap)

- **What**: Compute the full `d_s(σ) = −2 d ln P(σ)/d ln σ` profile, `P(σ) = Σ_{(p,q)} dim(p,q) Σ_i e^{−σ λ_i²}`, on the NORMAL-STATE (Δ=0) `D_K` spectrum, scanning `σ` from the Weyl regime (`σ→0`, expect `d_s→8`) through the fold window (`σ_*=1.4005 M_KK⁻²`) to the IR (`σ→∞`). Do it at `L_max ∈ {8, 10, 12}` (use the existing `s84_spectrum_cache_L12_tau019.npz` master cache; do NOT re-diagonalize — filter by Casimir-bound per the Friedrich-Bär saturation argument in `math-scripts.md`). Fit the energy-axis DOS exponent `γ_E` directly (the canonical discriminator per cross-pillar-bridge-corpus §24), NOT a `min d_s` heuristic. Report whether the *intermediate-window* `d_s` is `L_max`-stable or drifting.
- **Inputs**: `s84_spectrum_cache_L12_tau019.npz` (existing master spectrum cache); `d_s_fold_window_sigma = 1.4005` (canonical); the S92 ad-hoc workshop `s92-adhoc-spectral-dimension-ds-flow-vs-cdt.md` (functional definitions + CDT-window comparison machinery); `canonical_constants.py`.
- **Gate**: PASS if `d_s(σ)` profile is `L_max`-stable across {8,10,12} to within 5% at every `σ` in the intermediate window (⇒ the "no flow" claim is certified at converged `L_max`, III.1 broad claim upgraded from PRELIMINARY to FI); FAIL if the intermediate-window `d_s` drifts > 5% between `L_max=10` and `L_max=12` (⇒ the narrow-band-artifact caveat stands, the broad CDT-comparison claim stays PRELIMINARY, and the §3.3 defensive note must be re-scoped to the Weyl limit only). INFO if `γ_E` is well-defined but the CDT-window comparison is ambiguous (declare the (functional, window) pair per the corpus §24 specialization).
- **Effort**: Medium. Spectrum cache exists; the work is the `σ`-scan + log-log fit + `L_max`-stability check. ~1 agent-session. GPU not required (the cache is precomputed; the sum is `O(78k)` exponentials per `σ`-point).

---

### CF-PF-2 — Derived effective Friedmann map from the relic back-reaction: `H² = f(ρ_relic, S_SA)` (analogue-gravity × transit pillar; THE load-bearing gap, §6.3 / frontier #1+#8)

- **What**: Attempt the back-reaction closure the document names as the single most important open item. Concretely: construct the emergent stress tensor `T^{μν}_eff` of the post-fold GGE relic on the `a₂`-emergent metric `g_M` (the relic is born at rest, `T^{0i}=0` exact per §7.1; the energy density is `ρ_relic` from the Bogoliubov spectrum `E_exc/|E_cond|=443`), and test whether the emergent Bianchi identity `∇_μ G_eff^{μν}=0` (already PASS on-shell, S95 W3-1, `noether_ratio=½`) closes a *first-order-in-`τ̇`* effective Friedmann relation `H_eff² ∝ ρ_relic` when `H_eff` is *defined* as `½ d ln a₂/dt` via the Connes-distance proxy `a(τ)` (SCALE-FACTOR-54, the one carrying the `q`-band). Do NOT use `a_eff = (a₂/a₂_today)^{1/2}` (the document shows its `q_Ω` diverges). Check dimensional closure and the `M_KK⁻¹→`seconds normalization as a separate sub-result.
- **Inputs**: S95 W3-1 `G_eff^{μν}` conservation result (`noether_ratio=½`, on-shell `∇_μ G_eff=0`); SCALE-FACTOR-54 `a(τ)` Connes-distance proxy + its `q∈[−0.97,+0.81]` band; S95 W4-4 conformal embedding `Ω(τ)` (the construction that reproduces the `q`-range with the Connes proxy); the Bogoliubov relic spectrum (`N_pair`, `E_exc/E_cond=443`, `T^{0i}=0`); `φ(τ)=f₂Λ²a₂(τ)/(48π²)` identification.
- **Gate**: PASS if a dimensionally-closed `H_eff² = (8πG_eff/3)ρ_relic`-form relation emerges with `G_eff` matching the §8.3 spectral-action dictionary `G_N` to within the documented factor-2.29 corroboration band; FAIL (informative — closes a corridor) if the 155,984-mode spectral action overwhelms the relic source by the documented `133,200×` (T6 FAIL persists at the effective level too); INFO if the relation closes only up to the undelivered `M_KK⁻¹→`seconds normalization (then that normalization is isolated as the single remaining sub-gap).
- **Effort**: High. This is the frontier item; it is the joint #1+#8 close. Realistically a multi-wave workshop, not a single gate. But the *first* sub-gate (does `∇_μ G_eff=0` + the Connes-proxy `H_eff` definition produce a Friedmann-FORM at all, before normalization) is a tractable single-agent dispatch and should be split out as the entry point.

---

### CF-PF-3 — Greybody factor `Γ(ω)` of the exit surface: does the filtered band collapse `A_s` to a point? (analogue-gravity pillar; §6.2 + §7.1 A_s band)

- **What**: Compute the analog greybody / transmission coefficient `Γ(ω) ∈ [0,1]` of the exit surface as a Pöschl–Teller (or Regge–Wheeler-analog) barrier-transmission problem on the `a₄`-condensation edge, using the documented barrier (`transmitted_fraction=0.512`, `ω∈[0.82,1.06]` condensate-squeeze support). Then compute `A_s = (produced squeeze) × ∫Γ(ω)dω` and report whether the resulting `A_s` is a point or still a band. The document says the filter "narrows the band but does not yet collapse it to a point" (S95 W4-3) and `A_s` remains band-cited (`3.11–4.27×10⁻⁹`) pending `ε_pivot`. The computation is: does pinning `ε_pivot` via the greybody-narrowed produced squeeze collapse the band?
- **Inputs**: S95 W4-3 greybody result (`Γ(ω)`, `transmitted_fraction=0.512`); the produced-squeeze Bogoliubov coefficients at the fold (`|β_k|²`, `P_exc=1`); the condensate-squeeze support `ω∈[0.82,1.06]`; the Mukhanov–Sasaki `z=a√(2ε_H)M_Pl,eff` transduction (§5.3, second oscillator); `A_s` band `3.11–4.27×10⁻⁹`.
- **Gate**: PASS if `∫Γ(ω)dω` × produced-squeeze yields `A_s` within a factor 1.5 of the observed `2.1×10⁻⁹` AND the band collapses to ≤ 0.3 OOM width; FAIL if the filtered `A_s` lands > 1 OOM from observed (⇒ the squeeze-transduction has an unaccounted suppression/amplification); INFO if the band narrows but `ε_pivot` cannot be pinned without the `a(t)` map (then this item is *downstream* of CF-PF-2 and should be re-queued after it).
- **Effort**: Medium. The barrier-transmission integral is standard analogue-gravity machinery; the open piece is the transduction normalization `ε_pivot`, which may block on CF-PF-2. ~1–2 agent-sessions.

---

### CF-PF-4 — Josephson-plasma / BCS-gap reconciliation across the fold: is `ω_J/Δ=1.54` the relic-formation rate scale? (Josephson pillar; §5.3 + my memory `E_J/E_C=194`)

- **What**: My memory carries `E_J/E_C = 194` (superfluid regime), `ω_J/Δ = 1.54` (Josephson plasma INSIDE the BCS gap), `ω_J = ω_att` to 0.07% (the attractor IS the plasma oscillation), and `t_Th/t_transit = 65.12` (CG(24) λ₁=4, GGE survives transit). The capstone uses `t_scr/t_transit=814` and `R_therm=5251.82` for the diabatic-freeze argument but does NOT connect these to the Josephson-plasma frequency. Compute: is the screening/freeze timescale `t_scr` consistent with the Josephson-plasma period `2π/ω_J`? I.e., does the GGE freeze because the quench is faster than the *Josephson-plasma* oscillation that would otherwise re-phase the condensate? This would tie the §5.3 diabatic-freeze (transit pillar) to the Josephson-array phase dynamics (pillar V) as one mechanism.
- **Inputs**: `E_J/E_C=194`, `ω_J/Δ=1.54`, `t_Th/t_transit=65.12`, `ω_J=ω_att` (all in my memory / canonical_constants — verify via `get_constant`); `t_scr/t_transit=814` and `R_therm=5251.82` (S95 W5); `δt_transit`, `T_L` (the condensate formation time); `Δ_BCS` (canonical).
- **Gate**: PASS if `t_scr ≈ 2π/ω_J` to within a factor 2 (⇒ the freeze IS the Josephson-plasma re-phasing being outrun, a clean cross-pillar identification); FAIL if `t_scr` and `2π/ω_J` differ by > 1 OOM (⇒ the freeze mechanism is NOT Josephson-plasma-set, distinct timescales); INFO if the comparison reveals a *third* scale (then report the hierarchy).
- **Effort**: Low. All inputs exist; this is a timescale-ratio cross-check + a structural-interpretation statement. ~0.5 agent-session. **High leverage per unit effort** — it is a pure cross-pillar bridge test.

---

### CF-PF-5 — Register the `SU(1,1)` shared-squeeze-algebra cross-pillar bridge as a §VII entry (cross-pillar bridge; the III.2 missed amplification)

- **What**: Formalize and register the cross-pillar bridge I flagged in III.2: BCS Bogoliubov (pillar IV), cosmological Bogoliubov (pillar I), and Josephson phase (pillar V) share the *same* `su(1,1)` representation, with `R_BG = 1/cosh(2r)` (S93 W8-6 PASS) as the pre/post-fold bridge-coefficient ratio. Build the full 5-anatomy IS-not-IN block (per `cross-pillar-bridge-anatomy.md`): substrate-IS observable (the Bogoliubov squeeze weight `cosh(2r)` on the finite triple), laboratory-IN observable (the BCS-squeeze / Josephson-plasma measurement), bridge map (the `su(1,1)` co-cycle / Connes-Karoubi pairing), algebraic envelope (`L^{-α}`), empirical anchor. Declare the three-level confidence ladder. Verify the `R_BG = 1/cosh(2r)` identity to machine epsilon via Sage from the S38 squeeze data (`n_Bog=0.99863`, `⟨n⟩=730.6`, `cosh(2r)=1462.30`).
- **Inputs**: S93 W8-6 (`R_BG = 1/cosh(2r) = 6.8386e-4`, PASS); S38 Bogoliubov data (`n_Bog=0.99863`, `<n>=730.6`, `W_BG=cosh(2r)=1462.30`); the `su(1,1)` generator algebra `{K₀,K₊,K₋}`; `cross-pillar-bridge-anatomy.md` (5-anatomy template + 3-level ladder).
- **Gate**: PASS (registry-PASS) if all 5 anatomy elements + 3 levels declared AND Level-3 empirical anchor < Level-2 envelope at canonical `L_max` AND the `R_BG=1/cosh(2r)` Sage residual < 1e-12; this is a *registration* gate (Level-1 cohomology-class identity is the `su(1,1)` rep-equivalence, regulator-invariant), so the bar is anatomy-completeness, not a new number. INFO if the bridge map is `analogous`-grade rather than a named HKR/Connes-Karoubi pairing (then it stays a structural-companion, not a registry-PASS).
- **Effort**: Low–Medium. The physics is done (S93 W8-6); this is a registry-landing + one Sage verification + the one-sentence amplification to capstone §5.3/§6.2. ~0.5–1 agent-session.

---

### CF-PF-6 — KIND-tag the B2-channel Mach `293.79` (analogue-gravity hygiene; the III.3 flag)

- **What**: Verify `Mach_B2 = 293.79` (§6.2 six-walls box) is a *velocity-ratio* KIND (comparable to `13.75`), NOT an acoustic-radius-ratio KIND (comparable to `421.3`). Recompute it explicitly as `v_transit / c_B2` with `c_B2` rho-pinned to `1/(πρ_B2)=0.0227 M_KK`, and confirm the channel-monotone argument ("softer channel deepens the interior") compares like-with-like. Add the KIND tag to the §6.2 box.
- **Inputs**: `c_B2 = 1/(πρ_B2) = 0.0227 M_KK`; `v_transit`; `c_fabric=209.97`, `Mach=13.75` (the comparison KIND); `Mach_fold=421.3` (the contrast KIND); the §6.2 channel-monotone argument.
- **Gate**: PASS if `293.79 = v_transit/c_B2` (velocity-ratio KIND confirmed, channel-monotone argument valid); FAIL if `293.79` is an acoustic-radius reading (then the "deepens the interior" comparison is unlike-with-unlike and the six-walls argument loses one wall — still over-determined at five, but the box text must be corrected).
- **Effort**: Trivial. One ratio recompute + a KIND-tag edit. ~0.25 agent-session. Pure hygiene but it enforces the document's own discipline.

---

### CF-PF-7 — Domain-wall absence on the Jensen ridge: is the LISA `Ω_GW` flagship a wall-network signal or a GGE-acoustic signal? (soliton pillar; §7.2 falsifier #7 + my memory)

- **What**: The §7.2 FLAGSHIP falsifier is the LISA CGWB `Ω_GW` (acoustic class 11 OOM above LISA-PLS, vs Companion-null `8.299×10⁻⁵⁸`). My memory carries a critical caveat: **domain walls are ABSENT on the Jensen ridge** (GGE universality, `π₀(U(1))=0`; `τ_DW=0.1135` is a *geometric crossover*, not a phase boundary). The LISA prediction in older lore (project_lisa-gw-prediction) attributed `Ω_GW~10⁻¹⁰` to *domain walls*. The capstone attributes the flagship signal to the *acoustic class* (post-transit GGE excitations), NOT walls. **Verify these are consistent**: confirm the `Ω_GW` flagship is sourced by the GGE-acoustic spectrum (the squeezed-vacuum graviton production at the fold), NOT by a `Z_N` wall network — because if `π₀(U(1))=0` kills the walls, any residual wall-network contribution to `Ω_GW` must be separately accounted or shown to be zero.
- **Inputs**: `Ω_GW` acoustic-class prediction (S-source for the 11-OOM-above-PLS figure) + Companion-null `8.299×10⁻⁵⁸` (Sage-exact, regulator-pin-discipline); `τ_DW=0.1135` (geometric crossover); `π₀(U(1))=0` (no `Z_N` wall topology on the Jensen ridge); the GGE-acoustic graviton-production spectrum (§5.3 squeeze transduced into the tensor sector — note tensor sector crosses freely per [T3]).
- **Gate**: PASS if the `Ω_GW` flagship is fully sourced by the GGE-acoustic / squeezed-graviton channel with the wall-network contribution shown to be zero (consistent with `π₀(U(1))=0`); FAIL if a non-zero wall-network `Ω_GW` survives (⇒ the `τ_DW=0.1135` crossover does source GWs and the flagship is a two-channel signal needing re-decomposition); INFO if the wall contribution is non-zero but sub-dominant (report the channel split + the spectral-shape discriminator LISA would see between acoustic-class and wall-class spectra).
- **Effort**: Medium. The acoustic-class `Ω_GW` exists; the new work is confirming the wall contribution is zero on the Jensen ridge and computing the LISA-band spectral-shape discriminator. ~1–2 agent-sessions. Flagship-adjacent ⇒ high priority.

---

### CF-PF-8 — Pin the `Z_fold` normalization in the `G_N` dictionary (KK-geometry pillar; the IV.3 flag, document's own PRELIMINARY)

- **What**: Resolve the document's own §8.3 flag. Determine which `G_N` dictionary form is canonical: the `M_Pl,red² = f₂ M_KK² a₂/(24π²)` form or the S83 `M_Pl_eff² = M_KK² a₂ f₂^R/π² · Z_fold⁻¹` form, by computing `Z_fold` explicitly (the spectral-action normalization at the fold) and showing the two forms agree once `Z_fold` is substituted. Report the canonical `f₂` (the document gives `f₂≈92` for the `24π²` form) under the pinned `Z_fold`.
- **Inputs**: `M_KK = 7.4287×10¹⁶ GeV` (S42, CONST-FREEZE-42); `a_2_FW_zeta = 2776.165389`; both dictionary forms (§8.3); the S83 `π²·Z_fold⁻¹` provenance; `f₂≈92` (the `24π²`-form closure); the Chamseddine–Connes `f₂` normalization-class reference.
- **Gate**: PASS if `Z_fold` is computed and the two dictionary forms agree to < 1% once substituted, with a single canonical `f₂` reported; FAIL if they disagree by > 10% after `Z_fold` substitution (⇒ a genuine scheme inconsistency in the dictionary, not just a normalization gap); the value promotes to `canonical_constants.py` on PASS (Class-(e) PIN-PROMOTES-TO-CANONICAL per epistemic-discipline).
- **Effort**: Low–Medium. `Z_fold` is a spectral-action normalization at a fixed `τ`; the computation is well-defined. ~1 agent-session. This is a free-parameter-ledger item (`f₂` is in the ledger), so it is not pure hygiene.

---

### CF-PF-9 — NNLO emergent-EP Casimir discriminator: the FIRST genuine substrate EP prediction (KK/spectral pillar; §9 frontier #8, the genericity firewall)

- **What**: The document's §9 frontier #8 (S95 W3 update) establishes that the LO+NLO weak-EP PASSes (`κ_EP=1`, `noether_ratio=½`) are **generic-identity-cored** — they hold for ANY spin Dirac operator and ANY Brans–Dicke scalar, carrying no substrate value-content. The genuinely substrate-specific EP prediction first appears at **NNLO, where the band-specific Casimir `ν_b(C₂(b))` re-enters the ratio** (`CF-S96-EP-NNLO-CASIMIR-DISCRIMINATOR`, already named as a forward carry-forward). Compute it: expand the inter-band EP ratio to NNLO in the heat-kernel/Seeley–DeWitt series, retain the `C₂(p,q)`-dependent term, and report whether the substrate predicts a band-dependent `κ_EP^{NNLO} ≠ 1` that a generic emergent-gravity model would NOT predict. This is the one place the framework can claim an EP *prediction* rather than an inherited structural inevitability.
- **Inputs**: E5 Lichnerowicz `D_K²=∇*∇+¼R_K` (the LO identity that makes `κ_EP=1` generic); the band Casimir values `C₂(p,q)` for B1/B2/B3 (acoustic/flat/optical); the S95 W3-5 `κ_EP=1.000000` LO+NLO result + the connes-ncg genericity synthesis (`session-95-connes-ncg-theorist-genericity-synthesis.md`); the Seeley–DeWitt `a₆` coefficient (NNLO, the first place `C₂` re-enters after `R_K` annihilation).
- **Gate**: PASS if `κ_EP^{NNLO}` carries a non-zero `C₂`-dependent deviation from 1 that is (a) substrate-specific and (b) in principle observable as an emergent EP-violation at order `(E/M_KK)^4`; FAIL if the `C₂` term *also* cancels at NNLO (⇒ the EP is generic to even higher order and the substrate prediction recedes to NNNLO or beyond — informative, it bounds where substrate content first appears); INFO if the deviation is non-zero but `(E/M_KK)^4`-suppressed below any conceivable detector (⇒ structurally predicted, observationally null, the LIV/foam-immunity statement strengthens).
- **Effort**: High. NNLO Seeley–DeWitt on the Jensen-deformed `SU(3)` is real work (the `a₆` coefficient with `C₂`-resolution). But it is the *decisive* EP item and the document explicitly queues it. Multi-wave.

---

### CF-PF-10 — `t*` from a non-one-loop route: is the single empirical coupling secretly geometric? (spectral-functional pillar; §1.4 + §3.2)

- **What**: The document's §1.4 closes the corridor "`t*` = one-loop threshold coefficient" (S95 W2-1 FAIL, `R=1.977`): the parameter-free one-loop content `Γ_1loop≈26%` is ~3× too large to BE the admixture weight `t*=0.08832`. So `t*` remains the one genuinely empirical coupling. The harvest: test a *different* origin for `t*`. Specifically, the document (§3.2) reads the `√x` piece as an **acoustic (linear-in-frequency) envelope** `f(ω²)∼|ω|` that up-weights the B1 acoustic band. Compute whether `t* = 0.08832` is the ratio of acoustic-band spectral weight to total spectral weight (`Σ_{B1} 1 / Σ_all 1`, or a moment-weighted version) — i.e., is the admixture weight FIXED by the band-decomposition of the DOS rather than by a one-loop threshold? This would convert `t*` from empirical to geometric and shorten the free-parameter ledger to `{τ, Λ, f₀, f₂, f₄}`.
- **Inputs**: `t* = 0.08832` (the admixture weight, `f*(x)=0.9117√x+0.0883e⁻ˣ`); the B1/B2/B3 band decomposition (`SO(8)→U(2)` lifting, §2.4); the DOS `g(ω)` at `τ_fold`; the S95 W2-1 FAIL result (one-loop route closed) so this is explicitly a *different* route, not a re-run.
- **Gate**: PASS if `t*` matches an acoustic-band-weight ratio to within its `O(1)` precision (say < 5%) AND the match is parameter-free (no tuning of the band cut); this would be a major result (de-empiricizes the ledger). FAIL if no band-weight ratio lands near `0.08832` (⇒ `t*` stays empirical, the ledger stays `{τ,Λ,f₀,f₂,f₄}+t*` — informative, it closes a second `t*`-origin corridor after the one-loop one). INFO if a band-weight ratio is close but requires a band-cut choice (then the choice is the new freedom, no net reduction).
- **Effort**: Medium. The band decomposition exists; the work is the weight-ratio computation + comparison. ~1 agent-session. **High EVOI**: a PASS de-empiricizes the framework's single empirical coupling; a FAIL closes a corridor cleanly.

---

### CF-PF-11 — S43 first-sound BAO ring amplitude vs a named experiment (analogue-acoustics pillar; §6.2 BAO, the document's INFO-by-unavailability)

- **What**: The document's §6.2 names the live BAO channel as the **S43 first-sound ring** (`A_FS/A_BAO=0.204=c_2²/c_1²`, `r_1≈325 Mpc`, `k_1≈0.0193 Mpc⁻¹`) — a zero-parameter prediction with no ΛCDM counterpart — and reports S95 W6-2 as INFO-by-unavailability (the *comparison* sensitivity value, not the substrate forecast, was unavailable at compute). Harvest: fetch the named experiment's sensitivity at `k_1≈0.0193 Mpc⁻¹` (DESI DR2/DR3 BAO power-spectrum error bars, or Euclid) and complete the forecast — does the substrate's `A_FS/A_BAO=0.204` first-sound ring sit above or below the detection threshold? Use the astro MCP / paper-search MCP to fetch the sensitivity.
- **Inputs**: S43 first-sound ring (`A_FS/A_BAO=0.204`, `c_2²/c_1²`, `r_1≈325 Mpc`, `k_1≈0.0193 Mpc⁻¹`); the per-branch effacement suppression `δP/P≈1.4×10⁻³` (the contrast — the live channel is the first-sound ring, NOT the per-branch sub-features); a named experiment's BAO power-spectrum sensitivity at `k_1` (fetch via MCP).
- **Gate**: PASS if `A_FS/A_BAO=0.204` exceeds the named experiment's 1σ sensitivity at `k_1` (⇒ a live, near-term, zero-parameter BAO falsifier with no ΛCDM analog); FAIL if it sits below detectability at all named experiments (⇒ the first-sound ring is a real prediction but observationally out of reach near-term — report which future survey would reach it); INFO only if no sensitivity value can be fetched (the W6-2 status; avoid leaving it here — the MCP fetch is the whole point).
- **Effort**: Low–Medium. The substrate forecast is done (S43); the work is the MCP sensitivity fetch + the threshold comparison. ~0.5–1 agent-session. Completes a stranded INFO into a live falsifier.

---

## §VI — Closing assessment

**Structural pattern (the spine, restated from my vantage)**: the capstone is the cross-domain pattern-detector's dream and its discipline test at once. The dream is realized — eight pillars, one operator, and the correspondences (acoustic white hole ↔ supersonic transit; van Hove DOS ↔ zero-`g_c` BCS; band-lifting ↔ moment-independence; Bogoliubov squeeze ↔ KZ impulse) are *forced by the single-operator postulate* in the cases the document marks PROVEN, and *honestly labeled inherited* in the cases it marks INFO (the Volovik gap-node EP universality; the higher-order Lorentz invariance). The discipline test is passed: the document does not let the dream become over-reach. The GEOMETRY-dissolves / TOPOLOGY-survives partition (§9) is the correct organizing principle for a substrate built from momentum-space topology, and it places every strong claim on the surviving side and every gap on the dissolving side.

**The single honest arrow holds throughout.** `D_K eigenvalues → spectral moments → emergent physics → measurement` is maintained even in §6.3, the section most at risk of container-relapse, where the document explicitly refuses "vacuum energy decays *in* expanding space" and requires "the `M_Pl²H²` reservoir dilutes *as* the substrate reorganizes; `H(t)` is the readout, not a clock." That is exactly right and it is the hardest place to keep the discipline.

**Solid**: the acoustic white hole as a KIND-tagged sector-dependent theorem (II.1); the van Hove fold + diabatic-freeze Ordered Veil (II.2); the layer dispersion-rigidity / band-lifting identity (II.3); `z=2` used by being *not over-used* (II.4).

**Over-read / under-certified (flagged, no verdict overturned)**: the broad "no flowing spectral dimension / distinct from CDT" claim outruns its converged-`L_max` evidence — the Weyl-limit `d_s=8` is solid, the intermediate-window flow-free claim is PRELIMINARY (III.1, and the internal-tension IV.1, and the missing-functional-name IV.2); the `SU(1,1)` shared-squeeze algebra is a real cross-pillar ancestor that the capstone *uses* but never *names* (III.2 — a missed amplification, not an error); the B2-channel Mach `293.79` needs a KIND-tag to keep the document's own conflation discipline (III.3); the `~3800×` `a_n` gap deserves its physical (integrated-DOS) interpretation (III.4, framing only).

**The user's "ripe harvest"**: §V converts the open frontiers into eleven runnable computations. The three highest-leverage from my vantage are **CF-PF-2** (the derived effective Friedmann map — the load-bearing gap, joint #1+#8 close), **CF-PF-10** (`t*` from a band-weight ratio — a PASS de-empiricizes the framework's only empirical coupling), and **CF-PF-4** (Josephson-plasma ↔ diabatic-freeze timescale — a low-effort, high-value pure cross-pillar bridge test). The cheapest decisive item is **CF-PF-6** (KIND-tag the `293.79` — trivial, enforces the document's own rule). The flagship-adjacent item is **CF-PF-7** (confirm the LISA `Ω_GW` is GGE-acoustic, not a wall network, given `π₀(U(1))=0`).

None of these "further work needed" prose items; each has What / Inputs / Gate / Effort filled, ready for the next compute session's planner. The mathematics is, as the user says, waiting.

---

*Reviewer: phonon-first-cosmologist. Sole writer of this file. Cross-checked against `canonical_constants.py` and the knowledge MCP (tau_fold=0.19, c_fabric=209.97, a_4_FW_zeta=1350.7216, Mach_fold=421.3, van-Hove cusp uniqueness §VII.M.W10-3, S92 spectral-dimension diffusion-window). No recorded verdict overturned; all flags are calibration of framing or certification status. Source document: `sessions/framework/phonic-exflation-equation.md`.*
