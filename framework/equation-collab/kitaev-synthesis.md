# Capstone Equation Review — kitaev

**Date**: 2026-05-29
**Agent**: kitaev-quantum-chaos-theorist (Kitaev)
**Domain vantage**: quantum chaos diagnostics, information scrambling, SYK/many-body dynamics, spectral statistics, the MSS chaos bound, Ruelle–Pollicott resonances, Kibble–Zurek defect formation.
**Source Documents**:
- `sessions/framework/phonic-exflation-equation.md` (the capstone under review, S95-era)
- `.claude/rules/phononic-framing.md` (framing law — binding)
- `.claude/agent-memory/kitaev-quantum-chaos-theorist/MEMORY.md` (own prior diagnostics) + linked `integrability_hierarchy.md`, `methodology_and_data.md`
- Knowledge MCP cross-checks (T3/atlas-04, S38 CHAOS-1/2/3, S39 Richardson–Gaudin, S62 Hawking-QA workshop, S95 W5)

---

## I. Session Outcome

The capstone is, from a quantum-chaos vantage, **the most honest framing of the Ordered Veil the corpus has produced** — and the place where it tightens is *exactly* my domain. The document does NOT claim integrability permanence. It explicitly records T3 ("GGE never thermalizes via Richardson–Gaudin integrability") as **BROKEN** (S39: a 13% non-separable density–density channel, Brody β = 0.633 — i.e. 63% of the way from Poisson toward GOE), and re-grounds the relic's survival on **diabatic transit-freeze** (`R_therm = t_therm/t_transit = 5251.82 ≫ 1`, S95 W5) plus an **exact pure-product-state** structure (`S_ent = 0`, S95 W5) plus a **geometric extremal-horizon** corroboration (`κ = 0, T_H = 0` at a double-root Killing horizon). This is a genuine improvement over my own memory's top-line ("integrable at all levels"), which is correct at the *fabric* scale but glosses the single-cell partial chaos that the document and the S62 workshop both surface.

The kill condition I hold (does "lossy compression → quantum uncertainty / scrambling" violate the MSS bound `λ_L ≤ 2πT`, or is the scrambling time inconsistent with the freeze-out?) **remains NOT triggered, and is now triggered-impossible by construction**: the framework's measured Lyapunov exponent is `λ_L = 0` at every scale tested, so `λ_L ≤ 2πT` holds trivially at every analog temperature in the document (verified §III below). What the diabatic-freeze framing buys is that the survival of the relic *no longer rests on the broken integrability claim* — it rests on a timescale separation that is a chaos-theory statement in its own right and is certified by compute.

Two over-claims and one buried structural identity warrant flagging (§IV). The structural identity — that the document's §6.2 analog surface gravity `κ_exit = 47.61` for the `a₄` relic surface is *numerically equal* to `2π × T(a₄ relic) = 2π × 7.578 = 47.614 M_KK`, i.e. the **MSS chaos-bound saturation scale and the analog surface gravity are the same object** — is a clean, citable result the document does not state and that converts directly into a §V harvest item.

---

## II. Key Results

### II.1 The Ordered Veil is now a transit-timescale freeze, not an integrability statement — and this is the correct chaos-theory reading

**Result**: `R_therm = t_therm/t_transit = 5251.82` (S95 W5, PASS); `t_scr/t_transit = 814` (S38); `S_ent = 0` exactly (product state, S39/S95 W5); T3 integrability-permanence **BROKEN** (Brody β = 0.633). Classification: **PHONONIC** (relic content of the fabric's BdG quasiparticle spectrum).

The skeptic's question I always ask of a "the system stays ordered" claim is: *ordered by what mechanism, and on what timescale?* A frozen-by-integrability claim and a frozen-by-diabaticity claim are physically distinct and fail differently. The document gets this right. The relevant chaos diagnostics partition into two regimes:

1. **The transit (the only regime that matters for relic formation).** The crossing is impulsive: `Mach = v_transit/c_fabric = 13.75` (`c_fabric = 209.97 M_KK`, canonical-verified), sudden-quench ratio `δt_transit/T_L = 1.25×10⁻⁵`, the crossing 38,600× faster than the condensate can form. On this timescale the question "is the post-fold Hamiltonian integrable?" is *irrelevant* — even a fully chaotic Hamiltonian with the longest physically-admissible scrambling time `t_* ~ λ_L⁻¹ log S` cannot rearrange the state before the sweep freezes it, because `R_therm = 5252 ≫ 1` and `t_scr/t_transit = 814 ≫ 1`. This is the correct logic: a diabatic sweep produces a frozen non-equilibrium ensemble *regardless* of the late-time chaos class. The document's phrase "diabatic transit-freeze, not integrability permanence" is the precise statement.

2. **The frozen relic (late-time, where chaos diagnostics could in principle act but are out-run).** Here the broken-integrability channel lives. The S39 single-cell density–density channel is 13% non-separable, Brody β = 0.633 (63% GOE-leaning) — this is *not* an integrable spectrum; it is a weakly-chaotic one at the single-cell level. The document acknowledges this honestly. My memory's resolution (which the document does not cite but which is consistent with it) is the S62 Hawking-QA finding: **that single-cell β = 0.633 does NOT survive Josephson averaging on the CG(24) fabric** — at the multi-cell fabric scale `⟨r⟩ = 0.367` (Poisson), and my own multi-cell Plancherel diagnostic gives `r_pooled = 0.422` (S74, sub-GUE, integrable-leaning). So the chaos is a single-cell artifact that washes out at the fabric scale; but even where it does NOT wash out (single cell), the diabatic freeze out-runs it. **Two independent guards, either sufficient.** This is stronger than the integrability-permanence claim it replaces.

The information-theoretic framing in §5.3 ("the Ordered Veil resolves what would otherwise be an analog information paradox") is sound and is genuinely my domain. A thermalizing relic (`S_ent > 0`) would scramble the unitary Bogoliubov transformation into a mixed state, hiding the squeeze-phase data the way a black hole's thermal Hawking flux hides infalling information — there would be a Page curve to reproduce. The GGE relic stays pure (`S_ent = 0`), so the Bogoliubov phase data is retained in the conserved charges and there is no horizon-entropy debt out of the fold. This is correct and is the right way to say it: **no scrambling means no information paradox**, because scrambling is precisely the dynamical process that would manufacture one. This is the substrate-first reading (the relic IS a pure product state of the BdG spectrum; "no Page curve" is the emergent consequence), not the reverse.

### II.2 The MSS chaos bound is respected trivially — and the bound value IS the analog surface gravity

**Result**: `λ_L = 0` (all scales) ≤ `2πT` at every analog temperature. Cross-check (computed this review): `2π × T(a₄ relic) = 2π × 7.578 = 47.614 M_KK`, equal to the document's §6.2 `κ_exit(a₄) = 47.61` to four sig figs. Classification: **GEOMETRIC** (surface-gravity gradient of the `a₄` channel) feeding a **PHONONIC** bound (relic dynamics).

The Maldacena–Shenker–Stanford bound `λ_L ≤ 2πT/ℏ` is the physical law I hold most non-negotiable. The framework respects it trivially because its measured Lyapunov exponent vanishes (OTOC growth is `C(t) ~ t^{1.9}` — power-law, not exponential — at the 256-dim many-body scale, S38; no exponential regime exists to extract a `λ_L > 0` from). So the framework lives infinitely far *below* maximal chaos: it is the anti-SYK. SYK saturates `λ_L = 2πT`; the phonon-exflation substrate sits at `λ_L = 0`.

The buried identity is worth stating because the document's §6.2 analog-temperature ledger defines each `T_a = ℏκ/2π` with `κ` the surface gravity of a distinct surface. That definition makes the MSS saturation scale `2πT_a = ℏκ` **identically the surface gravity κ**. So when the document reports the `a₄` exit surface at `κ_exit = 47.61` and the relic spectral temperature at `7.578 M_KK`, those are not two facts — they are one fact (`κ = 2πT`), and the chaos-bound ceiling the substrate would have to reach to be maximally chaotic is the analog surface gravity of the condensation-exit surface. The substrate misses that ceiling by an infinite margin (`λ_L = 0` vs `κ = 47.61 M_KK`). This is the cleanest possible statement that **exflation is causally a horizon process but dynamically a non-chaotic one** — the horizon (κ) is real; the scrambling that horizons usually carry is absent. The document has the pieces (the κ ledger, the `λ_L = 0` corpus result) but does not connect them; doing so is §V.1.

### II.3 The Spectral-Moment Decoupling Wronskian is a non-degeneracy diagnostic — and it is the *spectral* sibling of a level-statistics statement

**Result**: `W[a₀,a₂,a₄](τ) ∝ R_K′(τ)³ = e⁻¹²ᵗ(e³ᵗ−1)⁶`, vanishing to sixth order at and only at `τ = 0`; `W(τ_fold) > 0` (S75 W2-E, CERTIFIED; Sage residual 0 this build). Classification: **GEOMETRIC** (algebraic independence of curvature-polynomial moments).

This is the document's strongest single theorem and it is recognizable from my vantage as a **non-degeneracy / level-repulsion statement read at the level of spectral moments rather than eigenvalues**. The three moments `{1, R_K, R_K²}` degenerate (Wronskian → 0) precisely when the curvature stops moving (`R_K′ = 0`), which happens only at the maximally-symmetric genesis point. This is the moment-space face of the same band-lifting that §2.4 describes (`SO(8) → U(2)` into B1/B2/B3): at the high-symmetry point the bands *touch* (degenerate spectrum — the level-clustering limit) and the moments degenerate; the instant the curvature moves, the degeneracy lifts. The document's §4.2 already makes this connection ("the same band-lifting … restated at the level of the spectral moments"), and from a spectral-statistics standpoint it is exactly right: **symmetry-point degeneracy ↔ moment degeneracy; symmetry-breaking ↔ moment independence**. The "algebraically independent everywhere the universe lives" claim is solid and the Wronskian proof is the correct instrument.

One sharpening (not a correction): the document uses the Wronskian as a *joint-evidence* multiplier in §7.3 (the chance of one geometry matching `a₀ × a₂ × a₄` observables is the product because the layers are independent). The Wronskian licenses *functional* independence of the moments-as-functions-of-τ; whether the *observational* likelihoods factorize additionally requires that the regulator nuisance `f` does not correlate them. The document is careful about this (it warns `Ω_DM` and `σ₈` are both `a₂`-channel and must NOT be multiplied), so the use is sound — but the FI/RD partition (§3.2) is the load-bearing guard, not the Wronskian alone. Flagged for precision, not as a defect.

### II.4 Kibble–Zurek = Bogoliubov sudden-quench is correctly identified — but the defect-density scaling is not computed

**Result**: "The Bogoliubov sudden-quench and the Kibble–Zurek impulse-matching are the same physics read two ways (both give `P_exc = 1`)" (§5.3). Classification: **PHONONIC**.

This identification is correct and standard: in the impulse (sudden) regime of a quench through a critical point, the adiabatic-impulse approximation gives the frozen excitation density, and for a sufficiently fast (diabatic) crossing every mode is excited (`P_exc → 1`). The document has the regime right (`δt/T_L = 1.25×10⁻⁵ ≪ 1` is deep in the impulse regime). What it does NOT do — and what KZ is *for* — is extract the **defect-density scaling exponent** `n_defect ~ τ_Q^{−d ν/(1+zν)}` from the quench-rate dependence. The framework asserts the saturated endpoint (`P_exc = 1`) but never sweeps the quench rate `τ_Q = 1/τ̇` to map the *scaling*. This is a missed harvest: the KZ exponent is a sharp, computable prediction that depends on the correlation-length exponent ν and dynamical exponent z of the van Hove transition, and it is observable (in the relic mode-occupation spectrum `n_k`). See §V.4 — this is the single most underexploited diagnostic in the document.

### II.5 No flowing spectral dimension — consistent with my Ruelle–Pollicott / late-time corpus

**Result**: dimension spectrum `S_d = {0,2,4,6,8}` is τ-independent; `d_s ~ 8` at the gap scale; no CDT-like UV reduction; windowed low-`d_s` readings are a diffusion-window artifact (S92). Classification: **GEOMETRIC**.

This is consistent with my own recorded diagnostics (memory: "Substrate 4D emerges from Seeley–DeWitt `a₂` of `D_K`, NOT graph spectral dim, S73b"; "d_s on small graphs" methodology note). The document's defensive note is correctly scoped. From the chaos vantage the relevant point is that a *fixed*-dimension heat-kernel structure with a closed finite pole ladder `{0,2,4,6,8}` is the spectral signature of an **integrable / non-mixing** UV structure — the opposite of the dimension-flow seen in genuinely scrambling holographic duals. The document does not make this connection and need not, but it is corroborating: the late-time decay of correlations (Ruelle–Pollicott resonances) in such a structure is governed by the discrete gap, not by a continuum of chaotic resonances. Recorded as **agreement**, not as evidence.

---

## III. Gate Verdicts

These are the chaos-relevant verdicts cited or implied in the source; all are AUTHORITATIVE and not re-adjudicated. The right-hand "MSS cross-check" column is my own (computed this review), not a re-litigation of the gate.

| Gate | Verdict | Decisive Number | MSS / chaos cross-check (this review) |
|:-----|:--------|:----------------|:--------------------------------------|
| CHAOS-1 (level spacing) | DIAGNOSTIC: ORDERED | single-particle `⟨r⟩ = 0.321` (sub-Poisson); multi-cell `r_pooled = 0.422` | Poisson `≈ 0.386`, GUE `≈ 0.603` — both readings integrable-leaning |
| CHAOS-2 (OTOC) | DIAGNOSTIC: ORDERED | `C(t) ~ t^{1.9}` (power-law, no Lyapunov) | no exponential regime ⇒ `λ_L = 0` extractable |
| CHAOS-3 (OTOC late) | DIAGNOSTIC: ORDERED | (no Lyapunov) | — |
| T3 (RG integrability-permanence) | **BROKEN** (S39) | Brody β = 0.633 (63% GOE); 13% non-separable; `t_therm ≈ 6 M_KK⁻¹` | single-cell weakly chaotic; washed out by Josephson averaging (S62: fabric `⟨r⟩ = 0.367`) |
| S95 W5 (diabatic freeze) | **PASS** | `R_therm = t_therm/t_transit = 5251.82` | freeze out-runs any `t_* ~ λ_L⁻¹ log S` ⇒ relic survives regardless of late-time chaos class |
| S95 W5 (purity) | **PASS** | `S_ent = 0` (exact product state) | no scrambling ⇒ no analog Page curve ⇒ no information paradox |
| S95 W5-3 (EQUILIBRIUM-CC-WARRANT) | **PASS** | `ε − μq = −P = 0` (Gibbs–Duhem, Sage-rational 0) | thermodynamic identity; no Gibbs `T` invoked (consistent: nothing thermalizes) |
| §6.2 extremal horizon | **PASS (S95 W4-5)** | double-root Killing horizon `V = V′ = 0 ⇒ κ = 0, T_H = 0` at `τ_fold` | `T_H = 0` ⇒ MSS ceiling `2πT_H = 0` ⇒ even a maximal scrambler would have `λ_L = 0` here (causal-side corroboration of "never thermalizes") |

**MSS bound table (computed this review, M_KK units):** `2π·T_Gibbs(0.113) = 0.710` (matches memory `λ_L_max = 0.710`); `2π·T(a₄ relic, 7.578) = 47.614 = κ_exit`; `2π·T(a₂ carrier, 72.8) = 457.42 ≈ κ_entry 457.66`. **`λ_L = 0` ≤ every entry.** The bound is satisfied at every surface; the bound *value* is the analog surface gravity κ at that surface.

---

## IV. Structural Implications

**What tightened (and should be propagated into memory).** The Ordered Veil's load-bearing support shifted from *integrability* (BROKEN, T3) to *diabatic timescale separation* (`R_therm = 5252`) + *exact purity* (`S_ent = 0`) + *extremal horizon* (`T_H = 0`). My memory's top-line "INTEGRABLE AT ALL LEVELS" is correct at the fabric scale but is now a *secondary* support, not the primary one — and it is false at the single-cell density–density scale (β = 0.633). I will update memory to reflect the diabatic-freeze primacy and the single-cell-vs-fabric distinction. This is the right epistemic move per the framing law: the survival is a property of the *transit* (substrate IS the diabatic sweep), not a permanent property of a late-time Hamiltonian.

**Over-claim #1 — "P_exc = 1.000 ⇒ every mode is excited" stated without a UV cutoff on the mode sum.** §5.3 says `P_exc → 1.000` mode-by-mode and "the condensate is completely destroyed." This is the diabatic-saturation statement and is correct *per mode*. But "every mode" over `155,984` modes with an acoustic (`√x`, linear-in-frequency) envelope that *up-weights low modes* needs a stated UV regime: the diabaticity condition `δt/T_L ≪ 1` is mode-dependent (`T_L = 1/ω_k` shrinks for high-frequency modes), so the highest-frequency modes are *not* in the impulse regime and do NOT saturate to `P_exc = 1`. The relic charge `N_pair = 59.8` (already correctly downgraded to a projected charge `⟨Q⟩_GGE`, not a literal count) is dominated by the low acoustic band where saturation holds — consistent — but the categorical "every mode is excited" should be scoped to "every *acoustic-band* mode below the diabaticity crossover frequency `ω_*` where `ω_* δt ~ 1`." This `ω_*` is computable and is a missing number. See §V.5.

**Over-claim #2 (mild) — `t_therm ≈ 6 M_KK⁻¹` is treated as a single thermalization time, but it is the density–density channel time only.** The document uses `R_therm = t_therm/t_transit` with `t_therm` from the S39 density–density channel. That channel is the *fastest* rearrangement channel (it is the one that broke integrability). Using it makes `R_therm` a *conservative lower bound* on the freeze margin (slower channels give larger `R_therm`), which is the right direction — but the document should state that `t_therm = 6 M_KK⁻¹` is the *minimum* over channels, so `R_therm = 5252` is a *floor*. As written it reads as if `t_therm` were unique. This strengthens the result if stated correctly; flagged for precision.

**Conflict I am flagging, not resolving (per instructions).** The document's §5.3 and my memory both cite `t_scr/t_transit = 814` (S38) AND `t_therm/t_transit ≈ 5×10³` (S39 density–density). These are two different timescale ratios with two different physical contents (`t_scr` = OTOC operator-growth scrambling time; `t_therm` = density–density thermalization time), and they differ by ~6×. The document treats them as interchangeable ("`t_scr/t_transit = 814`; equivalently `t_therm/t_transit ≈ 5×10³`") — they are NOT equivalent (a factor 6.4 apart) and "equivalently" is too strong. Both support the freeze conclusion (both ≫ 1), so the *conclusion* is robust, but the word "equivalently" conflates a scrambling time with a thermalization time, which in chaos theory are distinct observables (`t_scr ~ λ_L⁻¹ log S`; `t_therm ~ ` slowest hydrodynamic mode). For a `λ_L = 0` system the "scrambling time" is not even a Lyapunov scrambling time — it is a diffusive/power-law operator-growth time, so calling it `t_scr` is itself slightly misleading. Flagged. See §V.6 for the disambiguating computation.

**What this means for the kill authority.** My kill condition (lossy-compression → scrambling violating a bound) is **cleared and cannot fire**: there is no scrambling to violate any bound (`λ_L = 0`). If the framework's "quantum uncertainty from lossy compression of higher-D deterministic dynamics" thesis is taken at face value, the *internal* dynamics that gets compressed would need an OTOC that grows and a scrambling time matching the compactification timescale — and the framework's own diagnostics say that internal OTOC does *not* grow exponentially (`t^{1.9}`). So either (a) the lossy-compression-as-scrambling picture is metaphorical (the compression is geometric/spectral, not dynamical scrambling), or (b) it is a literal claim that is in tension with `λ_L = 0`. The document does not make the lossy-compression-as-scrambling claim explicitly — it is a framework-level thesis elsewhere — so I register this as a **boundary on any future statement of that thesis**, not a present contradiction. See §V.7.

**Constraint-map net.** Nothing in the capstone closes a chaos channel that was open, and nothing re-opens one that was closed. The capstone *correctly re-shelves* the Ordered Veil's support and is honest that the integrability is broken. The surviving solution region for "the substrate is non-chaotic" is now bounded by: (i) fabric-scale Poisson statistics (washes out single-cell chaos), (ii) `λ_L = 0` (no exponential OTOC), (iii) diabatic freeze (`R_therm ≫ 1`, out-runs any chaos), (iv) extremal horizon `T_H = 0` (MSS ceiling is zero at the fold). Four independent walls; the region is robust.

---

## V. Carry-Forward Computations

**MANDATORY.** Each open question I can identify from the chaos/scrambling vantage, converted to a runnable computation with all four fields. The first three are the high-EVOI items.

V.1. **Connect the analog surface gravity κ to the MSS bound: certify `2πT_a = κ_a` as a structural identity across all three §6.2 surfaces**
   - **What**: For each row of the §6.2 analog-temperature ledger, verify `2π·T_a = κ_a` to machine precision (it is true by the definition `T_a = ℏκ_a/2π`, so this is a consistency-and-naming gate, not a discovery), then state the corollary: the MSS chaos-bound saturation ceiling `λ_L^max(T_a) = 2πT_a` equals the analog surface gravity at each surface, and the substrate sits at `λ_L = 0` below every ceiling. Output: a 3-row table `(surface, T_a, κ_a, 2πT_a, |2πT_a − κ_a|, λ_L=0 margin)` + a one-paragraph structural reading (horizon present, scrambling absent).
   - **Inputs**: `c_fabric = 209.97`; the three κ values from §6.2 (κ_entry(a₂) ≈ 457.66, κ_exit(a₄) = 47.61, κ_SONIC = 0.704805) and the three T values (72.8, 7.578, 0.112); `canonical_constants.py` for any pinned analog-T constants (search `T_a4_relic`, `kappa_exit`); `λ_L = 0` (corpus, CHAOS-2/3).
   - **Gate**: NEW gate `MSS-KAPPA-IDENTITY-S96` — PASS iff `|2πT_a − κ_a|/κ_a < 10⁻³` for all three surfaces AND `λ_L = 0 < 2πT_a` for all three (trivially); INFO if any κ-convention mismatch (Visser-`c` vs bare-gradient, the `c_BLV = 0.485` factor §6.2 flags) forces a per-surface convention tag; FAIL if any surface gives `λ_L > 2πT_a` (would break MSS — not expected).
   - **Effort**: 1–2 hours, 1 agent session (arithmetic + structural prose; no diagonalization).

V.2. **Late-time OTOC decay of the frozen GGE relic: extract the leading Ruelle–Pollicott resonance**
   - **What**: The corpus has the *early-time* OTOC (`C(t) ~ t^{1.9}`, S38) but no *late-time* decay analysis. Compute the OTOC `C(t) = −⟨[W(t),V(0)]²⟩` for the frozen post-fold GGE Hamiltonian out to late times and fit the envelope decay `C(t) → C_∞ + A e^{−γ t}` to extract the leading Ruelle–Pollicott resonance `γ` (the Liouvillian spectral gap). For an integrable/weakly-broken system γ should be set by the discrete spectral gap, not by a continuum of chaotic resonances; confirm γ is real (non-mixing) or has a small imaginary part (weak mixing from the β = 0.633 channel). Output: `γ` (decay rate), its ratio to the spectral gap `Δ_gap`, and a verdict on whether the relic is mixing.
   - **Inputs**: the post-fold integrable+perturbation Hamiltonian from `s39_richardson_gaudin.py` / the 256-dim many-body construction in `s38_otoc_bcs.py`; `V_phys` 13%-non-separable channel (S39); the BdG spectrum `{E_k}`; `canonical_constants.py` (`tau_fold = 0.19`).
   - **Gate**: NEW gate `RPRES-RELIC-S96` — PASS (mixing absent) iff `Im(γ)/Re(γ) < 0.05` AND `Re(γ) ≈ Δ_gap` (decay gap-governed, not chaos-governed); INFO if `0.05 ≤ Im(γ)/Re(γ) < 0.3` (weak mixing from the broken channel, consistent with β = 0.633); FAIL if `Im(γ)/Re(γ) ≥ 0.3` (genuine mixing — would mean the relic decoheres faster than the document's freeze argument allows, a real tension with S95 W5).
   - **Effort**: 4–6 hours, 1 agent session (late-time OTOC is the longer compute; reuse S38 machinery + extend time grid).

V.3. **`t_scr` vs `t_therm` disambiguation: is the "814" an operator-growth time or a thermalization time, and do they bound the freeze independently?**
   - **What**: Resolve the Over-claim #2 / Conflict flag (§IV). Compute, on the *same* Hamiltonian and the *same* `t_transit` normalization: (a) the operator-growth/scrambling time `t_scr` from the OTOC `C(t)` reaching its `O(1)` plateau (S38 gives 814); (b) the density–density thermalization time `t_therm` from the autocorrelation decay (S39 gives ~6 M_KK⁻¹ ⇒ ratio ~5×10³). Establish which is which, whether `t_scr < t_therm` or vice versa, and report the *minimum* of the two as the conservative freeze margin (the document should cite the floor). For `λ_L = 0`, also state explicitly that `t_scr` here is a power-law/diffusive operator-growth time, NOT a Lyapunov `λ_L⁻¹ log S` scrambling time.
   - **Inputs**: `s38_otoc_bcs.py` (`t_scr`), `s39_richardson_gaudin.py` + the S39 density–density autocorrelation (`t_therm`); `t_transit = δt_transit = 1.130×10⁻³ M_KK⁻¹` (§6.1); `canonical_constants.py`.
   - **Gate**: NEW gate `TIMESCALE-DISAMBIG-S96` — PASS iff both ratios `> 1` (freeze robust) AND the report identifies `min(t_scr, t_therm)/t_transit` as the conservative floor AND tags `t_scr` as non-Lyapunov; INFO if the two ratios agree within a factor 2 (then "equivalently" in §5.3 would be defensible); the document's "equivalently" is RETRACTED-as-imprecise if they differ by >2× (they currently differ by ~6.4×).
   - **Effort**: 2–3 hours, 1 agent session (both data files exist; this is a re-analysis + normalization audit).

V.4. **Kibble–Zurek defect-density scaling exponent for the van Hove transit**
   - **What**: The document asserts the saturated endpoint `P_exc = 1` but never extracts the KZ scaling. Sweep the quench rate `τ_Q = 1/τ̇` across the diabatic→adiabatic crossover and fit the frozen excitation/defect density `n_defect(τ_Q) ~ τ_Q^{−d ν/(1+zν)}` to extract the combined exponent. Independently estimate the van Hove transition's correlation-length exponent ν and dynamical exponent z (the DOS `g(ω) ~ 1/√(ω−ω_min)` square-root van Hove edge fixes the single-particle scaling; z from the BdG dispersion at the fold). This yields a *sharp, zero-parameter* prediction for the relic mode-occupation spectrum `n_k` slope as a function of crossing rate — observable in the GGE relic structure.
   - **Inputs**: the substrate-BdG parametric oscillator `u_k'' + ω_k²(τ(t)) u_k = 0` with `ω_k = E_k = √((λ_k²−μ²)² + Δ_k²)` (§5.3); the local sweep rate `τ̇` at the fold (§6.1, known locally); van Hove DOS `g(ω) ~ (ω−ω_min)^{−1/2}`; `Δ_BCS`, `μ` from `canonical_constants.py`.
   - **Gate**: NEW gate `KZ-EXPONENT-S96` — PASS iff the fitted `n_defect(τ_Q)` is a clean power law over ≥1.5 decades of `τ_Q` with exponent matching `dν/(1+zν)` from the independently-estimated (ν, z) to within 15%; INFO if the exponent is clean but (ν, z) cannot be independently pinned (then the exponent is a measured-but-unexplained datum); FAIL if no power-law regime exists (would mean the impulse approximation is invalid even in the diabatic limit).
   - **Effort**: 6–8 hours, 1–2 agent sessions (parametric-oscillator integration over a τ_Q grid + critical-exponent estimation; the per-mode equation is exact by block-diagonality §2.2, so the sweep is embarrassingly parallel).

V.5. **Diabaticity crossover frequency `ω_*`: the UV scope of "every mode is excited"**
   - **What**: Resolve Over-claim #1 (§IV). Compute the mode-dependent diabaticity ratio `r_k = δt_transit/T_L(k) = δt_transit · ω_k` and find the crossover frequency `ω_*` where `r_k = 1` (above which modes are NOT in the impulse regime and `P_exc(k) < 1`). Report `P_exc(k)` as a function of `ω_k/ω_*` (it should be ≈1 well below `ω_*` and roll off above), and the fraction of the `155,984`-mode spectrum that genuinely saturates. State the scoped claim: "every *acoustic-band* mode below `ω_*` saturates," replacing the categorical "every mode."
   - **Inputs**: `δt_transit = 1.130×10⁻³ M_KK⁻¹` (§6.1); the BdG spectrum `{ω_k = E_k}` from the L_max=10 cache (78,080 distinct eigenvalues); the `√x` acoustic envelope `f*(x) = 0.9117√x + 0.0883 e⁻ˣ` (§3.2); `canonical_constants.py`.
   - **Gate**: NEW gate `DIABATIC-CROSSOVER-S96` — PASS iff `ω_* ` lands above the acoustic-band support (so the relic-charge-dominating low modes all saturate, vindicating `⟨Q⟩_GGE = 59.8` as low-band-dominated) AND the high-mode roll-off is documented; INFO if `ω_*` sits inside the acoustic band (then `P_exc = 1` is over-stated even for the dominant band); the categorical "every mode is excited" is RESCOPED to "every mode below `ω_*`" on either outcome.
   - **Effort**: 2–3 hours, 1 agent session (per-mode ratio over the cached spectrum; no new diagonalization).

V.6. **Spectral form factor of the frozen GGE relic Hamiltonian: ramp-plateau test for residual chaos**
   - **What**: My memory records SFF results at the pairing level (`N_pair=3`: slope/GUE = 0.002; `N_pair=4`: −0.002 — both flat, integrable). Extend the SFF `K(t) = |Σ_n e^{−iE_n t}|²` to the *full post-fold GGE Hamiltonian including the 13% non-separable density–density channel* (the channel that broke integrability), to test whether the broken channel produces a GUE-like *ramp* (the cleanest chaos signature) at the single-cell scale, and confirm it is absent/suppressed at the fabric scale after Josephson averaging (S62: `⟨r⟩ = 0.367`). This directly tests whether β = 0.633 manifests as a ramp or is a finite-size r-ratio artifact.
   - **Inputs**: the single-cell density–density Hamiltonian (`s39_richardson_gaudin.py` + the V_phys 13% channel); the CG(24) fabric construction for Josephson averaging (S62 / my memory `CG(24) graph`); unfolding procedure from `s46_spectral_form_factor.py`; `canonical_constants.py`.
   - **Gate**: NEW gate `SFF-BROKEN-CHANNEL-S96` — PASS (chaos absent at fabric scale) iff fabric-scale SFF slope/GUE `< 0.05` (consistent with my `r_pooled = 0.422`); INFO if single-cell SFF shows a partial ramp (slope/GUE `∈ [0.05, 0.5]`, consistent with β = 0.633) that suppresses on averaging; FAIL if the fabric-scale SFF shows a clean GUE ramp (slope/GUE `> 0.5` — would mean the fabric IS chaotic and the Ordered Veil's fabric-scale guard is wrong, leaving only the diabatic-freeze guard).
   - **Effort**: 4–5 hours, 1 agent session (SFF + unfolding is established machinery; the new piece is the V_phys channel + Josephson average).

V.7. **Boundary on the "lossy-compression → quantum uncertainty" thesis: does the internal (pre-compression) dynamics scramble?**
   - **What**: The framework's broader thesis (uncertainty from lossy compression of higher-D deterministic dynamics) is, IF read as a dynamical claim, a scrambling claim — the compressed-out information must be hidden by *some* dynamics, and scrambling is the natural candidate. Test it: compute the OTOC of the *internal* D_K dynamics (operators acting within a single fiber's eigenvalue spectrum, before any 4D projection) and ask whether it grows exponentially with a `λ_L > 0` and a scrambling time `t_* ~ λ_L⁻¹ log(dim H_fiber)` comparable to the compactification timescale `M_KK⁻¹`. If `λ_L = 0` for the internal dynamics too (expected, given block-diagonality `[iK_7, D_K] = 0`), then the lossy-compression picture must be *geometric/spectral*, not dynamical scrambling — a clean structural statement that protects the thesis from a chaos-bound objection.
   - **Inputs**: the single-fiber D_K block spectrum (Peter–Weyl block, largest dim `O(10⁴)`, fits in VRAM per math-scripts.md feasibility note); the conserved `iK_7` (U(1)_7 generator, `[iK_7, D_K] = 0` at all τ); a choice of probe operators W, V within one block; `canonical_constants.py` (`tau_fold`).
   - **Gate**: NEW gate `INTERNAL-OTOC-S96` — PASS (compression is non-dynamical) iff internal OTOC is power-law/bounded (`λ_L = 0`, consistent with block-diagonality), licensing "lossy compression is spectral, not scrambling"; INFO if a transient growth appears but saturates sub-exponentially; FAIL if internal `λ_L > 0` AND `t_* ~ M_KK⁻¹` (would make the compression a genuine scrambling process and put the thesis in tension with MSS — fires my kill condition on the *mechanism statement*, not on the framework's verified observables).
   - **Effort**: 4–6 hours, 1 agent session (single-block OTOC; GPU `torch.linalg` for the `O(10⁴)` block per math-scripts.md).

V.8. **`Im(γ)` from the extremal-horizon corroboration: is `T_H = 0` consistent with a real Ruelle–Pollicott gap?**
   - **What**: Cross-link V.2 and the §6.2 extremal-horizon result. At a `T_H = 0` (extremal, double-root `V = V′ = 0`) horizon, the MSS ceiling is `2πT_H = 0`, forcing `λ_L = 0` exactly — but extremal horizons are also where near-horizon dynamics can develop slow (power-law) quasinormal decay. Compute the relic's late-time decay (from V.2's `γ`) and check it is consistent with the *extremal* (`T_H = 0`) reading: an extremal horizon predicts power-law (not exponential) late-time tails. If V.2's OTOC envelope decays as a power law rather than `e^{−γt}`, that is the dynamical fingerprint of the extremal horizon and a non-trivial consistency check between the causal (§6.2) and dynamical (§5.3) sides.
   - **Inputs**: V.2 output (`γ` or power-law tail); the extremal-horizon `κ = 0` result (S95 W4-5); the BdG quasinormal spectrum at the fold.
   - **Gate**: NEW gate `EXTREMAL-QNM-S96` — PASS iff the relic late-time decay is power-law (consistent with `T_H = 0` extremal) OR `γ` is real-and-gap-governed (consistent with discrete-spectrum non-mixing); INFO if ambiguous; FAIL if exponential decay with `γ` requiring a `T_H > 0` (would contradict the extremal-horizon verdict).
   - **Effort**: 2–3 hours, 1 agent session (consumes V.2; mostly cross-consistency analysis).

---

## VI. Summary Table

| # | Result | Classification | Status | Implication |
|:--|:-------|:---------------|:-------|:------------|
| 1 | Ordered Veil = diabatic transit-freeze (`R_therm = 5252`, `S_ent = 0`), NOT integrability (T3 BROKEN, β = 0.633) | PHONONIC | Document SOLID; tightens my memory | Survival rests on timescale separation + purity + extremal horizon — 4 independent walls, stronger than the broken integrability claim |
| 2 | MSS bound respected trivially (`λ_L = 0 ≤ 2πT` everywhere); bound value = analog surface gravity (`2π·7.578 = 47.61 = κ_exit`) | GEOMETRIC→PHONONIC | SOLID (computed this review); identity not stated in doc | Exflation is a horizon process (κ real) but non-chaotic (λ_L = 0); kill condition cleared and cannot fire → §V.1 |
| 3 | Spectral-Moment Decoupling Wronskian `W ∝ R_K′³` = non-degeneracy diagnostic; degenerate only at `τ=0` symmetry point | GEOMETRIC | CERTIFIED (S75 W2-E) | Moment-space face of band-lifting (symmetry-degeneracy ↔ moment-degeneracy); joint-evidence use sound but guarded by FI/RD, not Wronskian alone |
| 4 | KZ = Bogoliubov sudden-quench correctly identified, but defect-density *scaling* never extracted | PHONONIC | PARTIAL — endpoint only | Most underexploited diagnostic; the KZ exponent is a sharp zero-parameter prediction → §V.4 |
| 5 | "Every mode excited, `P_exc = 1`" stated without a diabaticity-crossover UV cutoff `ω_*` | PHONONIC | OVER-CLAIM (mild) | Scope to "every acoustic-band mode below `ω_*`"; relic charge is low-band-dominated (consistent) → §V.5 |
| 6 | "`t_scr/t_transit = 814` equivalently `t_therm/t_transit ≈ 5×10³`" conflates two distinct timescales (~6.4× apart) | PHONONIC | CONFLICT flagged, not resolved | Both ≫ 1 (conclusion robust); "equivalently" too strong; `t_scr` is non-Lyapunov for `λ_L=0` → §V.3 |
| 7 | No flowing spectral dimension (`S_d = {0,2,4,6,8}`, `d_s ~ 8`) | GEOMETRIC | SOLID; agrees with my corpus | Fixed-dimension closed pole ladder = integrable/non-mixing UV signature (recorded as agreement, not evidence) |
| 8 | Lossy-compression-as-scrambling thesis bounded: internal D_K OTOC must be tested | PHONONIC | OPEN (boundary registered) | If internal `λ_L = 0` (expected from `[iK_7,D_K]=0`), compression is spectral not dynamical → §V.7 protects the thesis from MSS |

---

*Reviewer note (memory update queued, not part of this review):* my `MEMORY.md` top-line "INTEGRABLE AT ALL LEVELS" will be re-shelved to "ORDERED AT ALL LEVELS — primary support is diabatic transit-freeze (`R_therm = 5252`) + exact purity (`S_ent = 0`) + extremal horizon (`T_H = 0`); integrability is fabric-scale only (single-cell density–density channel is weakly chaotic, β = 0.633, washed out by Josephson averaging per S62). MSS bound saturation scale = analog surface gravity κ (2πT_a = κ_a)." The kill authority remains cleared.
